# drill 
 
# Bad smells
I found 8240 bad smells with 902 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| JavadocDeclaration | 2239 | false |
| JavadocReference | 643 | false |
| FinalMethodInFinalClass | 582 | false |
| FieldMayBeFinal | 529 | false |
| UnnecessaryModifier | 488 | true |
| Deprecation | 378 | false |
| ConstantValue | 268 | false |
| DataFlowIssue | 256 | false |
| UnnecessaryLocalVariable | 218 | true |
| FinalStaticMethod | 211 | false |
| DuplicatedCode | 209 | false |
| TrivialIf | 185 | false |
| UnusedAssignment | 143 | false |
| UNCHECKED_WARNING | 130 | false |
| AutoCloseableResource | 106 | false |
| FieldCanBeLocal | 103 | false |
| DuplicateBranchesInSwitch | 77 | false |
| CStyleArrayDeclaration | 76 | false |
| NullableProblems | 59 | false |
| RedundantMethodOverride | 56 | false |
| UnnecessaryToStringCall | 56 | true |
| DanglingJavadoc | 55 | false |
| CommentedOutCode | 52 | false |
| InnerClassMayBeStatic | 48 | true |
| DoubleNegation | 46 | false |
| IgnoreResultOfCall | 45 | false |
| DuplicateThrows | 43 | false |
| SwitchStatementWithTooFewBranches | 41 | false |
| EmptyStatementBody | 38 | false |
| StringBufferReplaceableByString | 38 | false |
| UnnecessarySemicolon | 34 | false |
| FinalPrivateMethod | 32 | false |
| RedundantCast | 32 | false |
| JavadocLinkAsPlainText | 30 | false |
| DeprecatedIsStillUsed | 26 | false |
| StringConcatenationInsideStringBufferAppend | 25 | false |
| IntegerMultiplicationImplicitCastToLong | 24 | false |
| PointlessBooleanExpression | 24 | true |
| DefaultAnnotationParam | 22 | false |
| RedundantStringFormatCall | 22 | false |
| RedundantClassCall | 21 | false |
| ConditionCoveredByFurtherCondition | 21 | false |
| StaticInitializerReferencesSubClass | 20 | false |
| UnnecessaryReturn | 19 | true |
| SimplifiableConditionalExpression | 19 | false |
| PointlessArithmeticExpression | 18 | false |
| MismatchedJavadocCode | 16 | false |
| ProtectedMemberInFinalClass | 15 | true |
| UnnecessaryStringEscape | 14 | true |
| CollectionContainsUrl | 13 | false |
| NonFinalFieldInEnum | 13 | false |
| CopyConstructorMissesField | 13 | false |
| ToArrayCallWithZeroLengthArrayArgument | 12 | true |
| UnnecessaryCallToStringValueOf | 12 | false |
| RefusedBequest | 12 | false |
| ManualMinMaxCalculation | 12 | false |
| InfiniteRecursion | 12 | false |
| CatchMayIgnoreException | 12 | false |
| UnnecessaryContinue | 12 | false |
| RegExpRedundantEscape | 10 | false |
| SimplifyStreamApiCallChains | 10 | false |
| StringOperationCanBeSimplified | 9 | false |
| BusyWait | 9 | false |
| LongLiteralsEndingWithLowercaseL | 9 | false |
| IfStatementWithIdenticalBranches | 9 | false |
| SuspiciousMethodCalls | 9 | false |
| ArraysAsListWithZeroOrOneArgument | 8 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 8 | false |
| UseBulkOperation | 7 | false |
| RedundantTypeArguments | 6 | false |
| MismatchedCollectionQueryUpdate | 6 | false |
| IntegerDivisionInFloatingPointContext | 6 | false |
| NegativeIntConstantInLongContext | 6 | false |
| OptionalGetWithoutIsPresent | 6 | false |
| SynchronizeOnNonFinalField | 6 | false |
| EqualsBetweenInconvertibleTypes | 5 | false |
| ReturnFromFinallyBlock | 5 | false |
| PointlessBitwiseExpression | 5 | false |
| SwitchStatementWithConfusingDeclaration | 5 | false |
| RedundantArrayCreation | 5 | true |
| SlowListContainsAll | 5 | false |
| NonAtomicOperationOnVolatileField | 5 | false |
| WrapperTypeMayBePrimitive | 4 | false |
| DuplicateCondition | 4 | false |
| FinallyBlockCannotCompleteNormally | 4 | false |
| CharsetObjectCanBeUsed | 4 | false |
| AssertWithSideEffects | 4 | false |
| EmptyFinallyBlock | 3 | false |
| ForLoopReplaceableByWhile | 3 | false |
| TrivialStringConcatenation | 3 | false |
| NonStrictComparisonCanBeEquality | 3 | true |
| CaughtExceptionImmediatelyRethrown | 3 | false |
| StringEquality | 3 | false |
| IOStreamConstructor | 3 | false |
| EqualsWhichDoesntCheckParameterClass | 3 | false |
| NumberEquality | 2 | false |
| ExplicitArrayFilling | 2 | false |
| StringConcatenationInLoops | 2 | false |
| MismatchedStringCase | 2 | false |
| OptionalUsedAsFieldOrParameterType | 2 | false |
| ComparatorMethodParameterNotUsed | 2 | false |
| SuspiciousToArrayCall | 2 | false |
| JavaReflectionInvocation | 2 | false |
| UnnecessaryUnicodeEscape | 2 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 2 | false |
| UnpredictableBigDecimalConstructorCall | 2 | false |
| OverwrittenKey | 2 | false |
| ExternalizableWithoutPublicNoArgConstructor | 2 | false |
| UnnecessaryLabelOnContinueStatement | 2 | false |
| ThrowFromFinallyBlock | 2 | false |
| SlowAbstractSetRemoveAll | 2 | false |
| OptionalIsPresent | 2 | false |
| RedundantCollectionOperation | 2 | false |
| CloneDeclaresCloneNotSupported | 2 | false |
| BigDecimalMethodWithoutRoundingCalled | 2 | false |
| MismatchedArrayReadWrite | 1 | false |
| SuspiciousDateFormat | 1 | false |
| SortedCollectionWithNonComparableKeys | 1 | false |
| UNUSED_IMPORT | 1 | false |
| TextLabelInSwitchStatement | 1 | false |
| ManualArrayToCollectionCopy | 1 | false |
| TypeParameterHidesVisibleType | 1 | false |
| ThreadRun | 1 | false |
| ComparatorResultComparison | 1 | false |
| NewObjectEquality | 1 | false |
| EmptyInitializer | 1 | false |
| ConditionalBreakInInfiniteLoop | 1 | false |
| UnnecessaryLabelOnBreakStatement | 1 | false |
| MismatchedStringBuilderQueryUpdate | 1 | false |
| InfiniteLoopStatement | 1 | false |
| TypeParameterExtendsObject | 1 | false |
| RedundantCompareToJavaTime | 1 | false |
| RedundantOperationOnEmptyContainer | 1 | false |
| RegExpSingleCharAlternation | 1 | false |
| RegExpUnnecessaryNonCapturingGroup | 1 | false |
| ThrowableNotThrown | 1 | false |
| NumericOverflow | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DrillBuf\[buffers.size()\]'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/AbstractMapVector.java`
#### Snippet
```java
    }

    return buffers.toArray(new DrillBuf[buffers.size()]);
  }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[decodedPathList.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
        }
      }
      return decodedPathList.toArray(new String[decodedPathList.size()]);
    } else {
      // original list did not have any encoded path, return as is
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DrillBuf\[buffers.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/WritableBatch.java`
#### Snippet
```java
  private WritableBatch(RecordBatchDef def, List<DrillBuf> buffers) {
    this.def = def;
    this.buffers = buffers.toArray(new DrillBuf[buffers.size()]);
  }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TemplateLoader\[loaders.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRestServer.java`
#### Snippet
```java
      logger.error("Could not set up file template loader.", e);
    }
    configuration.setTemplateLoader(new MultiTemplateLoader(loaders.toArray(new TemplateLoader[loaders.size()])));
    return configuration;
  }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SignatureHolder\[innerClasses.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/SignatureHolder.java`
#### Snippet
```java
      }
    }
    return new SignatureHolder(signature, innerClasses.toArray(new SignatureHolder[innerClasses.size()]));
  }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[dirs.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
    // build a list of DFSDirPartitionLocation.
    dirToFileMap.keySet().stream()
        .map(dirs -> new DFSDirPartitionLocation(dirs.toArray(new String[dirs.size()]), dirToFileMap.get(dirs)))
        .forEach(locations::add);

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TransferPair\[transfers.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/FilterRecordBatch.java`
#### Snippet
```java
    container.buildSchema(SelectionVectorMode.FOUR_BYTE);

    final TransferPair[] tx = transfers.toArray(new TransferPair[transfers.size()]);
    final Filterer filter = context.getImplementationClass(cg);
    filter.setup(context, incoming, this, tx);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TransferPair\[transfers.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/FilterRecordBatch.java`
#### Snippet
```java
    }

    final TransferPair[] tx = transfers.toArray(new TransferPair[transfers.size()]);
    CodeGenerator<Filterer> codeGen = cg.getCodeGenerator();
    codeGen.plainJavaCapable(true);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LogicalExpression\[hashFieldExps.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/RuntimeFilterRecordBatch.java`
#### Snippet
```java
          hashFieldExps.add(toHashFieldExp);
        }
        hash64 = hashHelper.getHash64(hashFieldExps.toArray(
            new LogicalExpression[hashFieldExps.size()]),
            typedFieldIds.toArray(new TypedFieldId[typedFieldIds.size()]));
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TypedFieldId\[typedFieldIds.size()\]'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/RuntimeFilterRecordBatch.java`
#### Snippet
```java
        hash64 = hashHelper.getHash64(hashFieldExps.toArray(
            new LogicalExpression[hashFieldExps.size()]),
            typedFieldIds.toArray(new TypedFieldId[typedFieldIds.size()]));
      } catch (Exception e) {
        throw UserException.internalError(e).build(logger);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new FieldPath\[scannedFieldsSet.size()\]'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
            includeId = true;
          }
          scannedFields = scannedFieldsSet.toArray(new FieldPath[scannedFieldsSet.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ByteBuf\[bufs.size()\]'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/OutboundRpcMessage.java`
#### Snippet
```java
      }
    }
    this.dBodies = bufs.toArray(new ByteBuf[bufs.size()]);
  }

```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseUrlFunction.java`
#### Snippet
```java
        }

        java.lang.Integer port = aURL.getPort();
        // If port number is not specified in URL string, it is assigned a default value of -1.
        // Include port number into resulting map only if it was specified.
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseUrlFunction.java`
#### Snippet
```java
        }

        java.lang.Integer port = aURL.getPort();
        // If port number is not specified in URL string, it is assigned a default value of -1.
        // Include port number into resulting map only if it was specified.
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/GenericMetadataDescriptor.java`
#### Snippet
```java
    }

    Integer hours = (int)(value / (Math.pow(60, 2)));
    Integer minutes = (int)((value / (Math.pow(60, 1))) - (hours * 60));
    Integer seconds = (int)Math.ceil((value / (Math.pow(60, 0))) - (minutes * 60));
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/GenericMetadataDescriptor.java`
#### Snippet
```java

    Integer hours = (int)(value / (Math.pow(60, 2)));
    Integer minutes = (int)((value / (Math.pow(60, 1))) - (hours * 60));
    Integer seconds = (int)Math.ceil((value / (Math.pow(60, 0))) - (minutes * 60));
    return String.format("%1$02d:%2$02d:%3$02d", hours, minutes, seconds);
```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `FUNCTIONS` are read, but never written to
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionFunction.java`
#### Snippet
```java
  }).getType();

  private static final Class<?>[] FUNCTIONS = {};

  private static final ImmutableMap<String, Constructor<LogicalExpression>> FUNCTION_MAP;
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/util/CallBack.java`
#### Snippet
```java

public interface CallBack {
  public void doWork();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/record/TransferPair.java`
#### Snippet
```java
  public void splitAndTransfer(int startIndex, int length);
  public ValueVector getTo();
  public void copyValueSafe(int from, int to);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/record/TransferPair.java`
#### Snippet
```java

public interface TransferPair {
  public void transfer();
  public void splitAndTransfer(int startIndex, int length);
  public ValueVector getTo();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/record/TransferPair.java`
#### Snippet
```java
public interface TransferPair {
  public void transfer();
  public void splitAndTransfer(int startIndex, int length);
  public ValueVector getTo();
  public void copyValueSafe(int from, int to);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/record/TransferPair.java`
#### Snippet
```java
  public void transfer();
  public void splitAndTransfer(int startIndex, int length);
  public ValueVector getTo();
  public void copyValueSafe(int from, int to);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/vector/VarLenBulkInput.java`
#### Snippet
```java
   * streamed.
   */
  public interface BulkInputCallback<T extends VarLenBulkEntry> {
    /**
     * Invoked when a new bulk entry is read (entry is immutable)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/vector/NullableVectorDefinitionSetter.java`
#### Snippet
```java
   * @param index value position
   */
  public void setIndexDefined(int index);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/vector/NullableVectorDefinitionSetter.java`
#### Snippet
```java
   * @param numValues of contiguous values
   */
  public void setIndexDefined(int index, int numValues);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/Positionable.java`
#### Snippet
```java

public interface Positionable {
  public void setPosition(int index);
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/WriterEvents.java`
#### Snippet
```java
  void postRollover();

  abstract void dump(HierarchicalFormatter format);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/SchemaChangeListener.java`
#### Snippet
```java

public interface SchemaChangeListener {
  public void schemaChanged(BatchSchema newSchema);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * @return {@code false}
   */
  public boolean nullsAreSortedAtEnd() throws SQLException;

  // For matching order of java.sql.DatabaseMetaData:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * @return {@code false}
   */
  public boolean nullsAreSortedLow() throws SQLException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * @return {@code true}
   */
  public boolean nullsAreSortedHigh() throws SQLException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * @return {@code false}
   */
  public boolean nullsAreSortedAtStart() throws SQLException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ControllerFactory.java`
#### Snippet
```java

public interface ControllerFactory {
  public static class ControllerFactoryException extends Exception {
    private static final long serialVersionUID = 1L;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ControllerFactory.java`
#### Snippet
```java

public interface ControllerFactory {
  public static class ControllerFactoryException extends Exception {
    private static final long serialVersionUID = 1L;

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java
    private String descrip;

    private Command(String cmd, String descrip) {
      this.cmd = cmd;
      this.descrip = descrip;
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java
    private String displayName;

    private TrackingState( String displayName ) {
      this.displayName = displayName;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Scheduler.java`
#### Snippet
```java

public interface Scheduler {
  public interface TaskManager {
    int maxConcurrentAllocs();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMRegistrar.java`
#### Snippet
```java

public interface AMRegistrar {
  public static class AMRegistrationException extends Exception {
    private static final long serialVersionUID = 1L;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMRegistrar.java`
#### Snippet
```java

public interface AMRegistrar {
  public static class AMRegistrationException extends Exception {
    private static final long serialVersionUID = 1L;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskLifecycleListener.java`
#### Snippet
```java

public interface TaskLifecycleListener {
  public enum Event {
    CREATED, ALLOCATED, RUNNING, ENDED
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Pollable.java`
#### Snippet
```java

public interface Pollable {
  public void tick(long curTime);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMYarnFacade.java`
#### Snippet
```java
   */

  public static class YarnAppHostReport {
    public String appId;
    public String amHost;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMYarnFacade.java`
#### Snippet
```java
   */

  public static class YarnAppHostReport {
    public String appId;
    public String amHost;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/graph/GraphVisitor.java`
#### Snippet
```java
public interface GraphVisitor<T> {
  public boolean enter(T o);
  public void leave(T o);
  public boolean visit(T o);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/graph/GraphVisitor.java`
#### Snippet
```java

public interface GraphVisitor<T> {
  public boolean enter(T o);
  public void leave(T o);
  public boolean visit(T o);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/graph/GraphVisitor.java`
#### Snippet
```java
  public boolean enter(T o);
  public void leave(T o);
  public boolean visit(T o);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/graph/Visitable.java`
#### Snippet
```java

public interface Visitable<T extends Visitable<?>> {
  public void accept(T node);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
public interface LogicalOperator extends GraphValue<LogicalOperator> {

  public void setupAndValidate(List<LogicalOperator> operators, Collection<ValidationError> errors);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
  public <T, X, E extends Throwable> T accept(LogicalVisitor<T, X, E> logicalVisitor, X value) throws E;

  public void registerAsSubscriber(LogicalOperator operator);

  NodeBuilder<?> nodeBuilder();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
   * @return
   */
  public <T, X, E extends Throwable> T accept(LogicalVisitor<T, X, E> logicalVisitor, X value) throws E;

  public void registerAsSubscriber(LogicalOperator operator);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
  NodeBuilder<?> nodeBuilder();

  public interface NodeBuilder<T extends LogicalOperator> {
    ObjectNode convert(ObjectMapper mapper, T operator, Integer inputId);
  }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `logical/src/main/java/org/apache/drill/common/logical/PlanProperties.java`
#### Snippet
```java
    public String info;

    public static enum ResultMode {
      EXEC, LOGICAL, PHYSICAL;
    }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `logical/src/main/java/org/apache/drill/common/logical/PlanProperties.java`
#### Snippet
```java
 */
public class PlanProperties {
  public static enum PlanType {APACHE_DRILL_LOGICAL, APACHE_DRILL_PHYSICAL}

  public PlanType type;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
 */
public interface LogicalVisitor<RETURN, EXTRA, EXCEP extends Throwable> {
    static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(LogicalVisitor.class);


```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
 */
public interface LogicalVisitor<RETURN, EXTRA, EXCEP extends Throwable> {
    static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(LogicalVisitor.class);


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitExcept(Except except, EXTRA value) throws EXCEP;
    public RETURN visitIntersect(Intersect intersect, EXTRA value) throws EXCEP;
    public RETURN visitWindow(Window window, EXTRA value) throws EXCEP;
    public RETURN visitWriter(Writer writer, EXTRA value) throws EXCEP;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java

    public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
    public RETURN visitValues(Values constant, EXTRA value) throws EXCEP;
    public RETURN visitOrder(Order order, EXTRA value) throws EXCEP;
    public RETURN visitJoin(Join join, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitAnalyze(Analyze analyze, EXTRA value) throws EXCEP;

    public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
    public RETURN visitValues(Values constant, EXTRA value) throws EXCEP;
    public RETURN visitOrder(Order order, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java

    public RETURN visitScan(Scan scan, EXTRA value) throws EXCEP;
    public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
    public RETURN visitGroupingAggregate(GroupingAggregate groupBy, EXTRA value) throws EXCEP;
    public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitJoin(Join join, EXTRA value) throws EXCEP;
    public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
    public RETURN visitRunningAggregate(RunningAggregate runningAggregate, EXTRA value) throws EXCEP;
    public RETURN visitTransform(Transform transform, EXTRA value) throws EXCEP;
    public RETURN visitUnion(Union union, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitWriter(Writer writer, EXTRA value) throws EXCEP;

    public RETURN visitUnnest(Unnest unnest, EXTRA value) throws EXCEP;
    public RETURN visitLateralJoin(LateralJoin lateralJoin, EXTRA value) throws EXCEP;
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
    public RETURN visitGroupingAggregate(GroupingAggregate groupBy, EXTRA value) throws EXCEP;
    public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
    public RETURN visitFlatten(Flatten flatten, EXTRA value) throws EXCEP;
    public RETURN visitAnalyze(Analyze analyze, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java

    public RETURN visitUnnest(Unnest unnest, EXTRA value) throws EXCEP;
    public RETURN visitLateralJoin(LateralJoin lateralJoin, EXTRA value) throws EXCEP;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitValues(Values constant, EXTRA value) throws EXCEP;
    public RETURN visitOrder(Order order, EXTRA value) throws EXCEP;
    public RETURN visitJoin(Join join, EXTRA value) throws EXCEP;
    public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
    public RETURN visitRunningAggregate(RunningAggregate runningAggregate, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitUnion(Union union, EXTRA value) throws EXCEP;
    public RETURN visitExcept(Except except, EXTRA value) throws EXCEP;
    public RETURN visitIntersect(Intersect intersect, EXTRA value) throws EXCEP;
    public RETURN visitWindow(Window window, EXTRA value) throws EXCEP;
    public RETURN visitWriter(Writer writer, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
    public RETURN visitFlatten(Flatten flatten, EXTRA value) throws EXCEP;
    public RETURN visitAnalyze(Analyze analyze, EXTRA value) throws EXCEP;

    public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitGroupingAggregate(GroupingAggregate groupBy, EXTRA value) throws EXCEP;
    public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
    public RETURN visitFlatten(Flatten flatten, EXTRA value) throws EXCEP;
    public RETURN visitAnalyze(Analyze analyze, EXTRA value) throws EXCEP;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitTransform(Transform transform, EXTRA value) throws EXCEP;
    public RETURN visitUnion(Union union, EXTRA value) throws EXCEP;
    public RETURN visitExcept(Except except, EXTRA value) throws EXCEP;
    public RETURN visitIntersect(Intersect intersect, EXTRA value) throws EXCEP;
    public RETURN visitWindow(Window window, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitScan(Scan scan, EXTRA value) throws EXCEP;
    public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
    public RETURN visitGroupingAggregate(GroupingAggregate groupBy, EXTRA value) throws EXCEP;
    public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
    public RETURN visitFlatten(Flatten flatten, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitIntersect(Intersect intersect, EXTRA value) throws EXCEP;
    public RETURN visitWindow(Window window, EXTRA value) throws EXCEP;
    public RETURN visitWriter(Writer writer, EXTRA value) throws EXCEP;

    public RETURN visitUnnest(Unnest unnest, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitRunningAggregate(RunningAggregate runningAggregate, EXTRA value) throws EXCEP;
    public RETURN visitTransform(Transform transform, EXTRA value) throws EXCEP;
    public RETURN visitUnion(Union union, EXTRA value) throws EXCEP;
    public RETURN visitExcept(Except except, EXTRA value) throws EXCEP;
    public RETURN visitIntersect(Intersect intersect, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
    public RETURN visitValues(Values constant, EXTRA value) throws EXCEP;
    public RETURN visitOrder(Order order, EXTRA value) throws EXCEP;
    public RETURN visitJoin(Join join, EXTRA value) throws EXCEP;
    public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitOrder(Order order, EXTRA value) throws EXCEP;
    public RETURN visitJoin(Join join, EXTRA value) throws EXCEP;
    public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
    public RETURN visitRunningAggregate(RunningAggregate runningAggregate, EXTRA value) throws EXCEP;
    public RETURN visitTransform(Transform transform, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java
    public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
    public RETURN visitRunningAggregate(RunningAggregate runningAggregate, EXTRA value) throws EXCEP;
    public RETURN visitTransform(Transform transform, EXTRA value) throws EXCEP;
    public RETURN visitUnion(Union union, EXTRA value) throws EXCEP;
    public RETURN visitExcept(Except except, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/logical/data/visitors/LogicalVisitor.java`
#### Snippet
```java


    public RETURN visitScan(Scan scan, EXTRA value) throws EXCEP;
    public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
    public RETURN visitGroupingAggregate(GroupingAggregate groupBy, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));

  public MajorType getOutputType(List<LogicalExpression> expressions);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));

  public MajorType getOutputType(List<LogicalExpression> expressions);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class SameAsFirstInput implements OutputTypeDeterminer{
    @Override
    public MajorType getOutputType(List<LogicalExpression> expressions) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class SameAsFirstInput implements OutputTypeDeterminer{
    @Override
    public MajorType getOutputType(List<LogicalExpression> expressions) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));

  public MajorType getOutputType(List<LogicalExpression> expressions);


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java

  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java

  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
public interface OutputTypeDeterminer {

  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
public interface OutputTypeDeterminer {

  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_BIT = new FixedType(Types.required(MinorType.BIT));
  public static OutputTypeDeterminer FIXED_INT = new FixedType(Types.required(MinorType.INT));
  public static OutputTypeDeterminer FIXED_BIGINT = new FixedType(Types.required(MinorType.BIGINT));
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class SameAsAnySoft implements OutputTypeDeterminer{
    @Override
    public MajorType getOutputType(List<LogicalExpression> expressions) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class SameAsAnySoft implements OutputTypeDeterminer{
    @Override
    public MajorType getOutputType(List<LogicalExpression> expressions) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  public static OutputTypeDeterminer FIXED_FLOAT4 = new FixedType(Types.required(MinorType.FLOAT4));
  public static OutputTypeDeterminer FIXED_FLOAT8 = new FixedType(Types.required(MinorType.FLOAT8));
  public static OutputTypeDeterminer FIXED_VARCHAR = new FixedType(Types.required(MinorType.VARCHAR));
  public static OutputTypeDeterminer FIXED_VARBINARY = new FixedType(Types.required(MinorType.VARBINARY));
  public static OutputTypeDeterminer FIXED_VAR16CHAR = new FixedType(Types.required(MinorType.VAR16CHAR));
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class NullIfNullType implements OutputTypeDeterminer{
    public MinorType outputMinorType;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java
  }

  public static class NullIfNullType implements OutputTypeDeterminer{
    public MinorType outputMinorType;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java


  public static class FixedType implements OutputTypeDeterminer{
    public MajorType outputType;

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `logical/src/main/java/org/apache/drill/common/expression/OutputTypeDeterminer.java`
#### Snippet
```java


  public static class FixedType implements OutputTypeDeterminer{
    public MajorType outputType;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
  boolean hasErrors();

  public int getErrorCount();

  String toErrorString();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
  public void addGeneralError(ExpressionPosition expr, String s);

  public void addUnexpectedArgumentType(ExpressionPosition expr, String name,
      MajorType actual, MajorType[] expected, int argumentIndex);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
  public void addUnexpectedType(ExpressionPosition expr, int index, MajorType actual);

  public void addExpectedConstantValue(ExpressionPosition expr, int actual, String s);

  boolean hasErrors();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
  public void addNonNumericType(ExpressionPosition expr, MajorType actual);

  public void addUnexpectedType(ExpressionPosition expr, int index, MajorType actual);

  public void addExpectedConstantValue(ExpressionPosition expr, int actual, String s);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
      int actual, int expected);

  public void addNonNumericType(ExpressionPosition expr, MajorType actual);

  public void addUnexpectedType(ExpressionPosition expr, int index, MajorType actual);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
      MajorType actual, MajorType[] expected, int argumentIndex);

  public void addUnexpectedArgumentCount(ExpressionPosition expr,
      int actual,  Range<Integer> expected);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
public interface ErrorCollector {

  public void addGeneralError(ExpressionPosition expr, String s);

  public void addUnexpectedArgumentType(ExpressionPosition expr, String name,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
      int actual,  Range<Integer> expected);

  public void addUnexpectedArgumentCount(ExpressionPosition expr,
      int actual, int expected);

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DataType.class);

  public static enum Comparability{
    UNKNOWN, NONE, EQUAL, ORDERED;
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/MetricDef.java`
#### Snippet
```java
 */
public interface MetricDef {
  public String name();
  public int metricId();
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/MetricDef.java`
#### Snippet
```java
public interface MetricDef {
  public String name();
  public int metricId();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OptimizerRulesContext.java`
#### Snippet
```java
   * @return PlannerSettings
   */
  public PlannerSettings getPlannerSettings();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OptimizerRulesContext.java`
#### Snippet
```java
   * @return FunctionImplementationRegistry
   */
  public FunctionImplementationRegistry getFunctionRegistry();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OptimizerRulesContext.java`
#### Snippet
```java
   * @return BufferAllocator
   */
  public BufferAllocator getAllocator();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/opt/Optimizer.java`
#### Snippet
```java
  public interface OptimizationContext {
    public int getPriority();
    public OptionManager getOptions();
  }
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/opt/Optimizer.java`
#### Snippet
```java

  public interface OptimizationContext {
    public int getPriority();
    public OptionManager getOptions();
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticator.java`
#### Snippet
```java
   * @throws UserAuthenticationException if authentication fails for given user and password.
   */
  public void authenticate(String user, String password) throws UserAuthenticationException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticator.java`
#### Snippet
```java
   * Setup for authenticating user credentials.
   */
  public void setup(DrillConfig drillConfig) throws DrillbitStartupException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
     *           throw this exception if there is an RPC failure that should be communicated to the sender.
     */
    public CustomResponse<RESPONSE> onMessage(REQUEST pBody, DrillBuf dBody) throws UserRpcException;

  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   * than Protobuf messages.
   */
  public interface CustomSerDe<MSG> {
    public byte[] serializeToSend(MSG send);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   *          The type of message that the respopnse contains. Must be a protobuf message type.
   */
  public interface CustomResponse<RESPONSE> {

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   */
  public interface CustomSerDe<MSG> {
    public byte[] serializeToSend(MSG send);

    public MSG deserializeReceived(byte[] bytes) throws Exception;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
     * @return null or one or more unstructured bodies.
     */
    public ByteBuf[] getBodies();
  }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
     * @return A protobuf message of type RESPONSE
     */
    public RESPONSE getMessage();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   *          CustomSerDe for serializing responses.
   */
  public <REQUEST, RESPONSE> void registerCustomHandler(int messageTypeId,
      CustomMessageHandler<REQUEST, RESPONSE> handler,
      CustomSerDe<REQUEST> requestSerde,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   *          The parser used to handle the types of messages the handler above handles.
   */
  public <REQUEST extends MessageLite, RESPONSE extends MessageLite> void registerCustomHandler(int messageTypeId,
      CustomMessageHandler<REQUEST, RESPONSE> handler, Parser<REQUEST> parser);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   *          The type of the response message.
   */
  public interface CustomMessageHandler<REQUEST, RESPONSE> {

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
    public byte[] serializeToSend(MSG send);

    public MSG deserializeReceived(byte[] bytes) throws Exception;
  }
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   * @return
   */
  public ControlTunnel getTunnel(DrillbitEndpoint node);

  public DrillbitEndpoint start(DrillbitEndpoint partialEndpoint, boolean allowPortHunting)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
  public ControlTunnel getTunnel(DrillbitEndpoint node);

  public DrillbitEndpoint start(DrillbitEndpoint partialEndpoint, boolean allowPortHunting)
      throws DrillbitStartupException;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/disk/Spool.java`
#### Snippet
```java

  public void write(RecordBatch batch) throws IOException;
  public void read(RecordBatch batch) throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/disk/Spool.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Spool.class);

  public void write(RecordBatch batch) throws IOException;
  public void read(RecordBatch batch) throws IOException;
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/disk/Spool.java`
#### Snippet
```java

public interface Spool {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Spool.class);

  public void write(RecordBatch batch) throws IOException;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/disk/Spool.java`
#### Snippet
```java

public interface Spool {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Spool.class);

  public void write(RecordBatch batch) throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DrillAggFunc.java`
#### Snippet
```java
  public void setup();
  public void add();
  public void output();
  public void reset();
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DrillAggFunc.java`
#### Snippet
```java
   */
  public void setup();
  public void add();
  public void output();
  public void reset();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DrillAggFunc.java`
#### Snippet
```java
   *
   */
  public void setup();
  public void add();
  public void output();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DrillAggFunc.java`
#### Snippet
```java
  public void add();
  public void output();
  public void reset();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionLookupContext.java`
#### Snippet
```java
   * @return DrillFuncHolder
   */
  public DrillFuncHolder findDrillFunction(FunctionResolver functionResolver, FunctionCall functionCall);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionLookupContext.java`
#### Snippet
```java
   * @return AbstractFuncHolder
   */
  public AbstractFuncHolder findNonDrillFunction(FunctionCall functionCall);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/PluggableFunctionRegistry.java`
#### Snippet
```java
   * @param operatorTable
   */
  public void register(DrillOperatorTable operatorTable);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/PluggableFunctionRegistry.java`
#### Snippet
```java
   * @return
   */
  public AbstractFuncHolder getFunction(FunctionCall functionCall);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
  public interface Hash64 {

    public void doSetup(RecordBatch incoming) throws SchemaChangeException;

    public long hash64Code(int incomingRowIdx, int seedValue, int fieldId) throws SchemaChangeException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
    public void doSetup(RecordBatch incoming) throws SchemaChangeException;

    public long hash64Code(int incomingRowIdx, int seedValue, int fieldId) throws SchemaChangeException;
  }

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java
    private final String ioTypeString;

    private RecordBatchIOType(String ioTypeString) {
      this.ioTypeString = ioTypeString;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/RawBatchBuffer.java`
#### Snippet
```java
   * @return Whether response should be returned.
   */
  public void enqueue(RawFragmentBatch batch) throws IOException;
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/RawBatchBuffer.java`
#### Snippet
```java
 */
public interface RawBatchBuffer extends RawFragmentBatchProvider {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RawBatchBuffer.class);

  /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/RawBatchBuffer.java`
#### Snippet
```java
 */
public interface RawBatchBuffer extends RawFragmentBatchProvider {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RawBatchBuffer.class);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java
  public RawBatchBuffer[] getBuffers();
  public int getTotalIncomingFragments();
  public void close() throws Exception;
  /**
   * Enables caller (e.g., receiver) to attach its buffer allocator to this Data Collector in order
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DataCollector.class);
  public boolean batchArrived(int minorFragmentId, RawFragmentBatch batch) throws IOException;
  public int getOppositeMajorFragmentId();
  public RawBatchBuffer[] getBuffers();
  public int getTotalIncomingFragments();
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java

public interface DataCollector extends AutoCloseable {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DataCollector.class);
  public boolean batchArrived(int minorFragmentId, RawFragmentBatch batch) throws IOException;
  public int getOppositeMajorFragmentId();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java

public interface DataCollector extends AutoCloseable {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DataCollector.class);
  public boolean batchArrived(int minorFragmentId, RawFragmentBatch batch) throws IOException;
  public int getOppositeMajorFragmentId();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java
  public int getOppositeMajorFragmentId();
  public RawBatchBuffer[] getBuffers();
  public int getTotalIncomingFragments();
  public void close() throws Exception;
  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java
  public boolean batchArrived(int minorFragmentId, RawFragmentBatch batch) throws IOException;
  public int getOppositeMajorFragmentId();
  public RawBatchBuffer[] getBuffers();
  public int getTotalIncomingFragments();
  public void close() throws Exception;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/DataCollector.java`
#### Snippet
```java
public interface DataCollector extends AutoCloseable {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DataCollector.class);
  public boolean batchArrived(int minorFragmentId, RawFragmentBatch batch) throws IOException;
  public int getOppositeMajorFragmentId();
  public RawBatchBuffer[] getBuffers();
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/SqlUnsupportedException.java`
#### Snippet
```java

public abstract class SqlUnsupportedException extends ForemanSetupException {
  public static enum ExceptionType {
    NONE("NONE"),
    RELATIONAL(UnsupportedRelOperatorException.class.getSimpleName()),
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/FragmentStatusListener.java`
#### Snippet
```java

public interface FragmentStatusListener {
  public void statusUpdate(FragmentStatus status);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryQueue.java`
#### Snippet
```java
   */

  public interface QueueLease {
    long queryMemoryPerNode();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryQueue.java`
#### Snippet
```java

  @SuppressWarnings("serial")
  public class QueueTimeoutException extends Exception {

    private final QueryId queryId;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryQueue.java`
#### Snippet
```java

  @SuppressWarnings("serial")
  public class QueryQueueException extends Exception {
    QueryQueueException(String msg, Exception e) {
      super(msg, e);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillSerializable.class);
  public void read(DataInput input) throws IOException;
  public void readFromStream(InputStream input) throws IOException;
  public void write(DataOutput output) throws IOException;
  public void writeToStream(OutputStream output) throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
public interface DrillSerializable extends Externalizable{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillSerializable.class);
  public void read(DataInput input) throws IOException;
  public void readFromStream(InputStream input) throws IOException;
  public void write(DataOutput output) throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
  public void read(DataInput input) throws IOException;
  public void readFromStream(InputStream input) throws IOException;
  public void write(DataOutput output) throws IOException;
  public void writeToStream(OutputStream output) throws IOException;
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
 */
public interface DrillSerializable extends Externalizable{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillSerializable.class);
  public void read(DataInput input) throws IOException;
  public void readFromStream(InputStream input) throws IOException;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
 */
public interface DrillSerializable extends Externalizable{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillSerializable.class);
  public void read(DataInput input) throws IOException;
  public void readFromStream(InputStream input) throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DrillSerializable.java`
#### Snippet
```java
  public void readFromStream(InputStream input) throws IOException;
  public void write(DataOutput output) throws IOException;
  public void writeToStream(OutputStream output) throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/Counter.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Counter.class);
  public long get();
  public long incrementAndGet();
  public long decrementAndGet();
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/Counter.java`
#### Snippet
```java

public interface Counter {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Counter.class);
  public long get();
  public long incrementAndGet();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/Counter.java`
#### Snippet
```java

public interface Counter {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Counter.class);
  public long get();
  public long incrementAndGet();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/Counter.java`
#### Snippet
```java
  public long get();
  public long incrementAndGet();
  public long decrementAndGet();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/Counter.java`
#### Snippet
```java
public interface Counter {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(Counter.class);
  public long get();
  public long incrementAndGet();
  public long decrementAndGet();
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/QueryManager.java`
#### Snippet
```java
     * An enum of possible signals that {@link SignalListener} listens to.
     */
    public static enum Signal { CANCEL, UNPAUSE }

    private final Signal signal;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMultiMap.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMultiMap.class);
  public Collection<V> get(K key);
  public Future<Boolean> put(K key, V value);
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMultiMap.java`
#### Snippet
```java

public interface DistributedMultiMap<K, V> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMultiMap.class);
  public Collection<V> get(K key);
  public Future<Boolean> put(K key, V value);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMultiMap.java`
#### Snippet
```java

public interface DistributedMultiMap<K, V> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMultiMap.class);
  public Collection<V> get(K key);
  public Future<Boolean> put(K key, V value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMultiMap.java`
#### Snippet
```java
public interface DistributedMultiMap<K, V> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMultiMap.class);
  public Collection<V> get(K key);
  public Future<Boolean> put(K key, V value);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
public interface DistributedMap<K, V>{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMap.class);
  public V get(K key);
  public Future<V> put(K key, V value);
  public Future<V> delete(K key);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
  public V get(K key);
  public Future<V> put(K key, V value);
  public Future<V> delete(K key);
  public Future<V> putIfAbsent(K key, V value);
  public Future<V> putIfAbsent(K key, V value, long ttl, TimeUnit timeUnit);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMap.class);
  public V get(K key);
  public Future<V> put(K key, V value);
  public Future<V> delete(K key);
  public Future<V> putIfAbsent(K key, V value);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java

public interface DistributedMap<K, V>{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMap.class);
  public V get(K key);
  public Future<V> put(K key, V value);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java

public interface DistributedMap<K, V>{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedMap.class);
  public V get(K key);
  public Future<V> put(K key, V value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
  public Future<V> delete(K key);
  public Future<V> putIfAbsent(K key, V value);
  public Future<V> putIfAbsent(K key, V value, long ttl, TimeUnit timeUnit);
  public Iterable<Map.Entry<K, V>> getLocalEntries();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
  public Future<V> put(K key, V value);
  public Future<V> delete(K key);
  public Future<V> putIfAbsent(K key, V value);
  public Future<V> putIfAbsent(K key, V value, long ttl, TimeUnit timeUnit);
  public Iterable<Map.Entry<K, V>> getLocalEntries();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedMap.java`
#### Snippet
```java
  public Future<V> putIfAbsent(K key, V value);
  public Future<V> putIfAbsent(K key, V value, long ttl, TimeUnit timeUnit);
  public Iterable<Map.Entry<K, V>> getLocalEntries();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedCache.class);

  public void run() throws DrillbitStartupException;

  public <K, V> DistributedMap<K, V> getMap(CacheConfig<K, V> config);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  }

  public static class CacheConfig<K, V>{
    private final Class<K> keyClass;
    private final Class<V> valueClass;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  }

  public static class CacheConfig<K, V>{
    private final Class<K> keyClass;
    private final Class<V> valueClass;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java

public interface DistributedCache extends AutoCloseable{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedCache.class);

  public void run() throws DrillbitStartupException;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java

public interface DistributedCache extends AutoCloseable{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DistributedCache.class);

  public void run() throws DrillbitStartupException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  public void run() throws DrillbitStartupException;

  public <K, V> DistributedMap<K, V> getMap(CacheConfig<K, V> config);
  public <K, V> DistributedMultiMap<K, V> getMultiMap(CacheConfig<K, V> config);

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java

    private final Class<?>[] classes;
    private SerializationMode(Class<?>... classes) {
      this.classes = classes;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java

  public <K, V> DistributedMap<K, V> getMap(CacheConfig<K, V> config);
  public <K, V> DistributedMultiMap<K, V> getMultiMap(CacheConfig<K, V> config);

  public Counter getCounter(String name);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  }

  public static class CacheConfigBuilder<K, V> {

    private Class<K> keyClass;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  }

  public static class CacheConfigBuilder<K, V> {

    private Class<K> keyClass;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  public <K, V> DistributedMultiMap<K, V> getMultiMap(CacheConfig<K, V> config);

  public Counter getCounter(String name);

  public static enum SerializationMode {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  public Counter getCounter(String name);

  public static enum SerializationMode {
    JACKSON(Object.class),
    DRILL_SERIALIZIABLE(String.class, DrillSerializable.class),
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  public Counter getCounter(String name);

  public static enum SerializationMode {
    JACKSON(Object.class),
    DRILL_SERIALIZIABLE(String.class, DrillSerializable.class),
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/DistributedSemaphore.java`
#### Snippet
```java

public interface DistributedSemaphore {
  public DistributedLease acquire(long time, TimeUnit unit) throws Exception;

  public interface DistributedLease extends AutoCloseable{}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/DistributedSemaphore.java`
#### Snippet
```java
  public DistributedLease acquire(long time, TimeUnit unit) throws Exception;

  public interface DistributedLease extends AutoCloseable{}
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
   * Check if the writer should start a new partition, and if so, start a new partition
   */
  public void checkForNewPartition(int index);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   */
  @SuppressWarnings("serial")
  public static class PluginNotFoundException extends PluginException {
    public PluginNotFoundException(String name) {
      super("No storage plugin exists with name: `" + name + "`");
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   */
  @SuppressWarnings("serial")
  public static class PluginNotFoundException extends PluginException {
    public PluginNotFoundException(String name) {
      super("No storage plugin exists with name: `" + name + "`");
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   */
  @SuppressWarnings("serial")
  public static class PluginEncodingException extends PluginException {
    public PluginEncodingException(String msg, Exception e) {
      super(msg, e);
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   */
  @SuppressWarnings("serial")
  public static class PluginEncodingException extends PluginException {
    public PluginEncodingException(String msg, Exception e) {
      super(msg, e);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java

  @SuppressWarnings("serial")
  public static class PluginException extends Exception {
    public PluginException(String msg) {
      super(msg);
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java

  @SuppressWarnings("serial")
  public static class PluginException extends Exception {
    public PluginException(String msg) {
      super(msg);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePlugin.java`
#### Snippet
```java
   * @return an empty set or a set of plugin specific physical optimizer rules.
   */
  public default Set<? extends RelOptRule> getOptimizerRules(OptimizerRulesContext optimizerContext, PlannerPhase phase) {
    return ImmutableSet.of();
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/OpenFileTracker.java`
#### Snippet
```java
   * Add new file location and {@link org.apache.drill.exec.store.dfs.DrillFSDataInputStream} to list.
   */
  public void fileOpened(Path path, DrillFSDataInputStream fsDataInputStream);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/OpenFileTracker.java`
#### Snippet
```java
   * @param fsDataInputStream
   */
  public void fileClosed(DrillFSDataInputStream fsDataInputStream);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/FilterPushDownListener.java`
#### Snippet
```java
   * Listener for a one specific group scan.
   */
  public interface ScanPushDownListener {

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/EndpointByteMap.java`
#### Snippet
```java
  public long get(DrillbitEndpoint endpoint);
  public boolean isEmpty();
  public long getMaxBytes();
  public void add(DrillbitEndpoint endpoint, long bytes);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/EndpointByteMap.java`
#### Snippet
```java
  public boolean isSet(DrillbitEndpoint endpoint);
  public long get(DrillbitEndpoint endpoint);
  public boolean isEmpty();
  public long getMaxBytes();
  public void add(DrillbitEndpoint endpoint, long bytes);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/EndpointByteMap.java`
#### Snippet
```java

  public boolean isSet(DrillbitEndpoint endpoint);
  public long get(DrillbitEndpoint endpoint);
  public boolean isEmpty();
  public long getMaxBytes();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/EndpointByteMap.java`
#### Snippet
```java
public interface EndpointByteMap extends Iterable<ObjectLongCursor<DrillbitEndpoint>>{

  public boolean isSet(DrillbitEndpoint endpoint);
  public long get(DrillbitEndpoint endpoint);
  public boolean isEmpty();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/EndpointByteMap.java`
#### Snippet
```java
  public boolean isEmpty();
  public long getMaxBytes();
  public void add(DrillbitEndpoint endpoint, long bytes);
}
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchSizerManager.java`
#### Snippet
```java

  /** An abstraction to allow column readers attach custom field overflow state */
  public static interface FieldOverflowState {

    /** Overflow data can become an input source for the next batch(s); this method
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteWork.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(CompleteWork.class);

  public long getTotalBytes();
  public EndpointByteMap getByteMap();
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteWork.java`
#### Snippet
```java

  public long getTotalBytes();
  public EndpointByteMap getByteMap();
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteWork.java`
#### Snippet
```java
 */
public interface CompleteWork extends Comparable<CompleteWork>{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(CompleteWork.class);

  public long getTotalBytes();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteWork.java`
#### Snippet
```java
 */
public interface CompleteWork extends Comparable<CompleteWork>{
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(CompleteWork.class);

  public long getTotalBytes();
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/ServerMethod.java`
#### Snippet
```java
  private final Version minVersion;

  private ServerMethod(RpcType rpcType, Version minVersion) {
    this.rpcType = rpcType;
    this.minVersion = minVersion;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RawFragmentBatchProvider.java`
#### Snippet
```java

  public RawFragmentBatch getNext() throws IOException, InterruptedException;
  public void kill(FragmentContext context);

  @Override
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RawFragmentBatchProvider.java`
#### Snippet
```java
public interface RawFragmentBatchProvider extends AutoCloseable{

  public RawFragmentBatch getNext() throws IOException, InterruptedException;
  public void kill(FragmentContext context);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RawFragmentBatchProvider.java`
#### Snippet
```java

  @Override
  public void close(); // Suppress the "throws Exception".
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator);
  public VectorWrapper<?> getChildWrapper(int[] ids);
  public void transfer(VectorWrapper<?> destination);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
public interface VectorWrapper<T extends ValueVector> {

  public Class<T> getVectorClass();
  public MaterializedField getField();
  public T getValueVector();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public boolean isHyper();
  public void clear();
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator);
  public VectorWrapper<?> getChildWrapper(int[] ids);
  public void transfer(VectorWrapper<?> destination);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public Class<T> getVectorClass();
  public MaterializedField getField();
  public T getValueVector();
  public T[] getValueVectors();
  public boolean isHyper();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public T[] getValueVectors();
  public boolean isHyper();
  public void clear();
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator);
  public VectorWrapper<?> getChildWrapper(int[] ids);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public MaterializedField getField();
  public T getValueVector();
  public T[] getValueVectors();
  public boolean isHyper();
  public void clear();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
   * @return TypedFieldId
   */
  public TypedFieldId getFieldIdIfMatches(int id, SchemaPath expectedPath);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public void clear();
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator);
  public VectorWrapper<?> getChildWrapper(int[] ids);
  public void transfer(VectorWrapper<?> destination);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java

  public Class<T> getVectorClass();
  public MaterializedField getField();
  public T getValueVector();
  public T[] getValueVectors();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorWrapper.java`
#### Snippet
```java
  public T getValueVector();
  public T[] getValueVectors();
  public boolean isHyper();
  public void clear();
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/Record.java`
#### Snippet
```java

public interface Record {
    public DiffSchema getSchemaChanges();
    public Object getField(int fieldId);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/Record.java`
#### Snippet
```java
public interface Record {
    public DiffSchema getSchemaChanges();
    public Object getField(int fieldId);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/IdGenerator.java`
#### Snippet
```java
public interface IdGenerator<T> {
    public T getNextId();
    public void reset();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/IdGenerator.java`
#### Snippet
```java

public interface IdGenerator<T> {
    public T getNextId();
    public void reset();
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/RecordSchema.java`
#### Snippet
```java

public interface RecordSchema {
    public void addField(Field field);
    public Field getField(String fieldName, int index);
    public void removeField(Field field, int index);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/RecordSchema.java`
#### Snippet
```java
    public Field getField(String fieldName, int index);
    public void removeField(Field field, int index);
    public Iterable<? extends Field> getFields();
    public String toSchemaString();
    void resetMarkedFields();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/RecordSchema.java`
#### Snippet
```java
    public void addField(Field field);
    public Field getField(String fieldName, int index);
    public void removeField(Field field, int index);
    public Iterable<? extends Field> getFields();
    public String toSchemaString();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/RecordSchema.java`
#### Snippet
```java
    public void removeField(Field field, int index);
    public Iterable<? extends Field> getFields();
    public String toSchemaString();
    void resetMarkedFields();
    Iterable<? extends Field> removeUnreadFields();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/RecordSchema.java`
#### Snippet
```java
public interface RecordSchema {
    public void addField(Field field);
    public Field getField(String fieldName, int index);
    public void removeField(Field field, int index);
    public Iterable<? extends Field> getFields();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  };

  final static Predicate<MinorFragmentProfile> hasOperatorsAndTimes = Predicates.and(Filters.hasOperators, Filters.hasTimes);

  final static Predicate<MinorFragmentProfile> missingOperatorsOrTimes = Predicates.not(hasOperatorsAndTimes);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  };

  final static Predicate<MinorFragmentProfile> hasOperatorsAndTimes = Predicates.and(Filters.hasOperators, Filters.hasTimes);

  final static Predicate<MinorFragmentProfile> missingOperatorsOrTimes = Predicates.not(hasOperatorsAndTimes);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  final static Predicate<MinorFragmentProfile> hasOperatorsAndTimes = Predicates.and(Filters.hasOperators, Filters.hasTimes);

  final static Predicate<MinorFragmentProfile> missingOperatorsOrTimes = Predicates.not(hasOperatorsAndTimes);
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  final static Predicate<MinorFragmentProfile> hasOperatorsAndTimes = Predicates.and(Filters.hasOperators, Filters.hasTimes);

  final static Predicate<MinorFragmentProfile> missingOperatorsOrTimes = Predicates.not(hasOperatorsAndTimes);
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  };

  final static Predicate<MinorFragmentProfile> hasTimes = new Predicate<MinorFragmentProfile>() {
    public boolean apply(MinorFragmentProfile arg0) {
      return arg0.hasStartTime() && arg0.hasEndTime();
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java
  };

  final static Predicate<MinorFragmentProfile> hasTimes = new Predicate<MinorFragmentProfile>() {
    public boolean apply(MinorFragmentProfile arg0) {
      return arg0.hasStartTime() && arg0.hasEndTime();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java

interface Filters {
  final static Predicate<MinorFragmentProfile> hasOperators = new Predicate<MinorFragmentProfile>() {
    public boolean apply(MinorFragmentProfile arg0) {
      return arg0.getOperatorProfileCount() != 0;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Filters.java`
#### Snippet
```java

interface Filters {
  final static Predicate<MinorFragmentProfile> hasOperators = new Predicate<MinorFragmentProfile>() {
    public boolean apply(MinorFragmentProfile arg0) {
      return arg0.getOperatorProfileCount() != 0;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<OperatorProfile> operatorId = new Comparator<OperatorProfile>() {
    @Override
    public int compare(final OperatorProfile o1, final OperatorProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<OperatorProfile> operatorId = new Comparator<OperatorProfile>() {
    @Override
    public int compare(final OperatorProfile o1, final OperatorProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> processTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> processTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> minorId = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> minorId = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> lastProgress = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> lastProgress = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> endTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> endTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> operatorPeakMemory = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> operatorPeakMemory = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> waitTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> waitTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> runTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> runTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
interface Comparators {

  final static Comparator<MajorFragmentProfile> majorId = new Comparator<MajorFragmentProfile>() {
    @Override
    public int compare(final MajorFragmentProfile o1, final MajorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
interface Comparators {

  final static Comparator<MajorFragmentProfile> majorId = new Comparator<MajorFragmentProfile>() {
    @Override
    public int compare(final MajorFragmentProfile o1, final MajorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> lastUpdate = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> lastUpdate = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> fragmentPeakMemory = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> fragmentPeakMemory = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> startTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<MinorFragmentProfile> startTime = new Comparator<MinorFragmentProfile>() {
    @Override
    public int compare(final MinorFragmentProfile o1, final MinorFragmentProfile o2) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> setupTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/Comparators.java`
#### Snippet
```java
  };

  final static Comparator<Pair<OperatorProfile, Integer>> setupTime = new Comparator<Pair<OperatorProfile, Integer>>() {
    @Override
    public int compare(final Pair<OperatorProfile, Integer> o1, final Pair<OperatorProfile, Integer> o2) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    public VALUE create(KEY key);
    public void destroy(VALUE value);
  }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java
  public interface MapValueFactory<KEY, VALUE> {

    public VALUE create(KEY key);
    public void destroy(VALUE value);
  }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
   * Types of an index: PRIMARY_KEY_INDEX, NATIVE_SECONDARY_INDEX, EXTERNAL_SECONDARY_INDEX
   */
  static enum IndexType {
    PRIMARY_KEY_INDEX,
    NATIVE_SECONDARY_INDEX,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/Statistics.java`
#### Snippet
```java
   *  @return the leading rowcount
   */
  public double getLeadingRowCount(RexNode condition, String tabIdxName, DrillScanRelBase scanRel);
  /** Returns the average row size for the specified filter condition
   * @param tabIdxName - The index name generated using {@code buildUniqueIndexIdentifier}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * Types of an index collections: NATIVE_SECONDARY_INDEX_COLLECTION, EXTERNAL_SECONDARY_INDEX_COLLECTION
   */
  public static enum IndexCollectionType {
    NATIVE_SECONDARY_INDEX_COLLECTION,
    EXTERNAL_SECONDARY_INDEX_COLLECTION
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * Types of an index collections: NATIVE_SECONDARY_INDEX_COLLECTION, EXTERNAL_SECONDARY_INDEX_COLLECTION
   */
  public static enum IndexCollectionType {
    NATIVE_SECONDARY_INDEX_COLLECTION,
    EXTERNAL_SECONDARY_INDEX_COLLECTION
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * may not have - in such cases, Drill needs to do the index selection.
   */
  public boolean supportsIndexSelection();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * Clears all entries from this index collection
   */
  public void clearAll();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return The estimated row count
   */
  public double getRows(RexNode indexCondition);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * Add a new index to the collection. Return True if index was successfully added; False otherwise
   */
  public boolean addIndex(IndexDescriptor index);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return True if index supports getting row count, False otherwise
   */
  public boolean supportsRowCountStats();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return True if an appropriate index is found, False otherwise
   */
  public boolean isColumnIndexed(SchemaPath path);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return one of the values in {@link IndexCollectionType}
   */
  public IndexCollectionType getIndexCollectionType();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * Return True if index was successfully removed; False otherwise
   */
  public boolean removeIndex(IndexDescriptor index);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return True if index supports full-text search, False otherwise
   */
  public boolean supportsFullTextSearch();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
   * @return GroupScan for this IndexCollection if available, otherwise null
   */
  public IndexGroupScan getGroupScan();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillTableSelection.java`
#### Snippet
```java
   * @return this selection's digest, normally a string built from its properties.
   */
  public String digest();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/FragmentVisitor.java`
#### Snippet
```java

public interface FragmentVisitor<T, V> {
  public T visit(Fragment n, V extra);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DrillScanPrel.java`
#### Snippet
```java
public interface DrillScanPrel extends Prel, HasDistributionAffinity{

  public GroupScan getGroupScan();

}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/WriteEntry.java`
#### Snippet
```java

public interface WriteEntry {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(WriteEntry.class);
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/WriteEntry.java`
#### Snippet
```java

public interface WriteEntry {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(WriteEntry.class);
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillType.java`
#### Snippet
```java
  private final String ending;

  private DotDrillType(){
    this.ending = '.' + name().toLowerCase() + ".drill";
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java

  @JsonProperty("sender-major-fragment")
  public int getOppositeMajorFragmentId();

  @JsonProperty("spooling")
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java
   * it is running.
   */
  public abstract List<MinorFragmentEndpoint> getProvidingEndpoints();

  /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java
   * it is running.
   */
  public abstract List<MinorFragmentEndpoint> getProvidingEndpoints();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java
   */
  @JsonIgnore
  public abstract boolean supportsOutOfOrderExchange();

  @JsonProperty("sender-major-fragment")
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java
   */
  @JsonIgnore
  public abstract boolean supportsOutOfOrderExchange();

  @JsonProperty("sender-major-fragment")
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Receiver.java`
#### Snippet
```java

  @JsonProperty("spooling")
  public boolean isSpooling();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Sender.java`
#### Snippet
```java
   */
  @JsonProperty("receiver-major-fragment")
  public int getOppositeMajorFragmentId();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Sender.java`
#### Snippet
```java
   * where it is running.
   */
  public abstract List<MinorFragmentEndpoint> getDestinations();

  /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Sender.java`
#### Snippet
```java
   * where it is running.
   */
  public abstract List<MinorFragmentEndpoint> getDestinations();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitSender(Sender sender, EXTRA value) throws EXCEP;
  public RETURN visitRowKeyJoin(RowKeyJoinPOP join, EXTRA value) throws EXCEP;
  public RETURN visitReceiver(Receiver receiver, EXTRA value) throws EXCEP;
  public RETURN visitStreamingAggregate(StreamingAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsAggregate(StatisticsAggregate agg, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java


  public RETURN visitExchange(Exchange exchange, EXTRA value) throws EXCEP;
  public RETURN visitGroupScan(GroupScan groupScan, EXTRA value) throws EXCEP;
  public RETURN visitSubScan(SubScan subScan, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitSetOp(SetOp setOp, EXTRA value) throws EXCEP;
  public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
  public RETURN visitTrace(Trace trace, EXTRA value) throws EXCEP;
  public RETURN visitSort(Sort sort, EXTRA value) throws EXCEP;
  public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitProducerConsumer(ProducerConsumer op, EXTRA value) throws EXCEP;
  public RETURN visitUnnest(UnnestPOP unnest, EXTRA value) throws EXCEP;
  public RETURN visitLateralJoin(LateralJoinPOP lateralJoinPOP, EXTRA value) throws EXCEP;

  public RETURN visitIteratorValidator(IteratorValidator op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
  public RETURN visitUnion(UnionAll union, EXTRA value) throws EXCEP;
  public RETURN visitSetOp(SetOp setOp, EXTRA value) throws EXCEP;
  public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
  public RETURN visitTrace(Trace trace, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java

  public RETURN visitExchange(Exchange exchange, EXTRA value) throws EXCEP;
  public RETURN visitGroupScan(GroupScan groupScan, EXTRA value) throws EXCEP;
  public RETURN visitSubScan(SubScan subScan, EXTRA value) throws EXCEP;
  public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitWindowFrame(WindowPOP op, EXTRA value) throws EXCEP;
  public RETURN visitProducerConsumer(ProducerConsumer op, EXTRA value) throws EXCEP;
  public RETURN visitUnnest(UnnestPOP unnest, EXTRA value) throws EXCEP;
  public RETURN visitLateralJoin(LateralJoinPOP lateralJoinPOP, EXTRA value) throws EXCEP;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitSingleSender(SingleSender op, EXTRA value) throws EXCEP;
  public RETURN visitWindowFrame(WindowPOP op, EXTRA value) throws EXCEP;
  public RETURN visitProducerConsumer(ProducerConsumer op, EXTRA value) throws EXCEP;
  public RETURN visitUnnest(UnnestPOP unnest, EXTRA value) throws EXCEP;
  public RETURN visitLateralJoin(LateralJoinPOP lateralJoinPOP, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitHashAggregate(HashAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitWriter(Writer op, EXTRA value) throws EXCEP;
  public RETURN visitUnpivot(UnpivotMaps op, EXTRA value) throws EXCEP;
  public RETURN visitValues(Values op, EXTRA value) throws EXCEP;
  public RETURN visitOp(PhysicalOperator op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitRowKeyJoin(RowKeyJoinPOP join, EXTRA value) throws EXCEP;
  public RETURN visitReceiver(Receiver receiver, EXTRA value) throws EXCEP;
  public RETURN visitStreamingAggregate(StreamingAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsAggregate(StatisticsAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsMerge(StatisticsMerge agg, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitUnorderedReceiver(UnorderedReceiver op, EXTRA value) throws EXCEP;
  public RETURN visitMergingReceiver(MergingReceiverPOP op, EXTRA value) throws EXCEP;
  public RETURN visitHashPartitionSender(HashToRandomExchange op, EXTRA value) throws EXCEP;
  public RETURN visitRangePartitionSender(RangePartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitBroadcastSender(BroadcastSender op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitFlatten(FlattenPOP flatten, EXTRA value) throws EXCEP;
  public RETURN visitSender(Sender sender, EXTRA value) throws EXCEP;
  public RETURN visitRowKeyJoin(RowKeyJoinPOP join, EXTRA value) throws EXCEP;
  public RETURN visitReceiver(Receiver receiver, EXTRA value) throws EXCEP;
  public RETURN visitStreamingAggregate(StreamingAggregate agg, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitSort(Sort sort, EXTRA value) throws EXCEP;
  public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
  public RETURN visitFlatten(FlattenPOP flatten, EXTRA value) throws EXCEP;
  public RETURN visitSender(Sender sender, EXTRA value) throws EXCEP;
  public RETURN visitRowKeyJoin(RowKeyJoinPOP join, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java

  public RETURN visitHashPartitionSender(HashPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitOrderedPartitionSender(OrderedPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitUnorderedReceiver(UnorderedReceiver op, EXTRA value) throws EXCEP;
  public RETURN visitMergingReceiver(MergingReceiverPOP op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitExchange(Exchange exchange, EXTRA value) throws EXCEP;
  public RETURN visitGroupScan(GroupScan groupScan, EXTRA value) throws EXCEP;
  public RETURN visitSubScan(SubScan subScan, EXTRA value) throws EXCEP;
  public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
  public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java

  public RETURN visitIteratorValidator(IteratorValidator op, EXTRA value) throws EXCEP;
  public RETURN visitTableModify(TableModify op, EXTRA value) throws EXCEP;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitOp(PhysicalOperator op, EXTRA value) throws EXCEP;

  public RETURN visitHashPartitionSender(HashPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitOrderedPartitionSender(OrderedPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitUnorderedReceiver(UnorderedReceiver op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitUnion(UnionAll union, EXTRA value) throws EXCEP;
  public RETURN visitSetOp(SetOp setOp, EXTRA value) throws EXCEP;
  public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
  public RETURN visitTrace(Trace trace, EXTRA value) throws EXCEP;
  public RETURN visitSort(Sort sort, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitMergingReceiver(MergingReceiverPOP op, EXTRA value) throws EXCEP;
  public RETURN visitHashPartitionSender(HashToRandomExchange op, EXTRA value) throws EXCEP;
  public RETURN visitRangePartitionSender(RangePartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitBroadcastSender(BroadcastSender op, EXTRA value) throws EXCEP;
  public RETURN visitScreen(Screen op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitSubScan(SubScan subScan, EXTRA value) throws EXCEP;
  public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
  public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
  public RETURN visitUnion(UnionAll union, EXTRA value) throws EXCEP;
  public RETURN visitSetOp(SetOp setOp, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitUnpivot(UnpivotMaps op, EXTRA value) throws EXCEP;
  public RETURN visitValues(Values op, EXTRA value) throws EXCEP;
  public RETURN visitOp(PhysicalOperator op, EXTRA value) throws EXCEP;

  public RETURN visitHashPartitionSender(HashPartitionSender op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitHashPartitionSender(HashToRandomExchange op, EXTRA value) throws EXCEP;
  public RETURN visitRangePartitionSender(RangePartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitBroadcastSender(BroadcastSender op, EXTRA value) throws EXCEP;
  public RETURN visitScreen(Screen op, EXTRA value) throws EXCEP;
  public RETURN visitSingleSender(SingleSender op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitTrace(Trace trace, EXTRA value) throws EXCEP;
  public RETURN visitSort(Sort sort, EXTRA value) throws EXCEP;
  public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
  public RETURN visitFlatten(FlattenPOP flatten, EXTRA value) throws EXCEP;
  public RETURN visitSender(Sender sender, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitGroupScan(GroupScan groupScan, EXTRA value) throws EXCEP;
  public RETURN visitSubScan(SubScan subScan, EXTRA value) throws EXCEP;
  public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
  public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
  public RETURN visitUnion(UnionAll union, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitScreen(Screen op, EXTRA value) throws EXCEP;
  public RETURN visitSingleSender(SingleSender op, EXTRA value) throws EXCEP;
  public RETURN visitWindowFrame(WindowPOP op, EXTRA value) throws EXCEP;
  public RETURN visitProducerConsumer(ProducerConsumer op, EXTRA value) throws EXCEP;
  public RETURN visitUnnest(UnnestPOP unnest, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitHashPartitionSender(HashPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitOrderedPartitionSender(OrderedPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitUnorderedReceiver(UnorderedReceiver op, EXTRA value) throws EXCEP;
  public RETURN visitMergingReceiver(MergingReceiverPOP op, EXTRA value) throws EXCEP;
  public RETURN visitHashPartitionSender(HashToRandomExchange op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitOrderedPartitionSender(OrderedPartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitUnorderedReceiver(UnorderedReceiver op, EXTRA value) throws EXCEP;
  public RETURN visitMergingReceiver(MergingReceiverPOP op, EXTRA value) throws EXCEP;
  public RETURN visitHashPartitionSender(HashToRandomExchange op, EXTRA value) throws EXCEP;
  public RETURN visitRangePartitionSender(RangePartitionSender op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
 */
public interface PhysicalVisitor<RETURN, EXTRA, EXCEP extends Throwable> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(PhysicalVisitor.class);


```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
 */
public interface PhysicalVisitor<RETURN, EXTRA, EXCEP extends Throwable> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(PhysicalVisitor.class);


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitWriter(Writer op, EXTRA value) throws EXCEP;
  public RETURN visitUnpivot(UnpivotMaps op, EXTRA value) throws EXCEP;
  public RETURN visitValues(Values op, EXTRA value) throws EXCEP;
  public RETURN visitOp(PhysicalOperator op, EXTRA value) throws EXCEP;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitRangePartitionSender(RangePartitionSender op, EXTRA value) throws EXCEP;
  public RETURN visitBroadcastSender(BroadcastSender op, EXTRA value) throws EXCEP;
  public RETURN visitScreen(Screen op, EXTRA value) throws EXCEP;
  public RETURN visitSingleSender(SingleSender op, EXTRA value) throws EXCEP;
  public RETURN visitWindowFrame(WindowPOP op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
  public RETURN visitFlatten(FlattenPOP flatten, EXTRA value) throws EXCEP;
  public RETURN visitSender(Sender sender, EXTRA value) throws EXCEP;
  public RETURN visitRowKeyJoin(RowKeyJoinPOP join, EXTRA value) throws EXCEP;
  public RETURN visitReceiver(Receiver receiver, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitStore(Store store, EXTRA value) throws EXCEP;
  public RETURN visitFilter(Filter filter, EXTRA value) throws EXCEP;
  public RETURN visitUnion(UnionAll union, EXTRA value) throws EXCEP;
  public RETURN visitSetOp(SetOp setOp, EXTRA value) throws EXCEP;
  public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitStatisticsAggregate(StatisticsAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsMerge(StatisticsMerge agg, EXTRA value) throws EXCEP;
  public RETURN visitHashAggregate(HashAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitWriter(Writer op, EXTRA value) throws EXCEP;
  public RETURN visitUnpivot(UnpivotMaps op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitProject(Project project, EXTRA value) throws EXCEP;
  public RETURN visitTrace(Trace trace, EXTRA value) throws EXCEP;
  public RETURN visitSort(Sort sort, EXTRA value) throws EXCEP;
  public RETURN visitLimit(Limit limit, EXTRA value) throws EXCEP;
  public RETURN visitFlatten(FlattenPOP flatten, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitStreamingAggregate(StreamingAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsAggregate(StatisticsAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsMerge(StatisticsMerge agg, EXTRA value) throws EXCEP;
  public RETURN visitHashAggregate(HashAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitWriter(Writer op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitBroadcastSender(BroadcastSender op, EXTRA value) throws EXCEP;
  public RETURN visitScreen(Screen op, EXTRA value) throws EXCEP;
  public RETURN visitSingleSender(SingleSender op, EXTRA value) throws EXCEP;
  public RETURN visitWindowFrame(WindowPOP op, EXTRA value) throws EXCEP;
  public RETURN visitProducerConsumer(ProducerConsumer op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitLateralJoin(LateralJoinPOP lateralJoinPOP, EXTRA value) throws EXCEP;

  public RETURN visitIteratorValidator(IteratorValidator op, EXTRA value) throws EXCEP;
  public RETURN visitTableModify(TableModify op, EXTRA value) throws EXCEP;
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitReceiver(Receiver receiver, EXTRA value) throws EXCEP;
  public RETURN visitStreamingAggregate(StreamingAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsAggregate(StatisticsAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitStatisticsMerge(StatisticsMerge agg, EXTRA value) throws EXCEP;
  public RETURN visitHashAggregate(HashAggregate agg, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalVisitor.java`
#### Snippet
```java
  public RETURN visitStatisticsMerge(StatisticsMerge agg, EXTRA value) throws EXCEP;
  public RETURN visitHashAggregate(HashAggregate agg, EXTRA value) throws EXCEP;
  public RETURN visitWriter(Writer op, EXTRA value) throws EXCEP;
  public RETURN visitUnpivot(UnpivotMaps op, EXTRA value) throws EXCEP;
  public RETURN visitValues(Values op, EXTRA value) throws EXCEP;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @return The child of this store operator.
   */
  public abstract PhysicalOperator getChild();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @return The child of this store operator.
   */
  public abstract PhysicalOperator getChild();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @throws PhysicalOperatorSetupException
   */
  public abstract Store getSpecificStore(PhysicalOperator child, int minorFragmentId)
      throws PhysicalOperatorSetupException;

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @throws PhysicalOperatorSetupException
   */
  public abstract Store getSpecificStore(PhysicalOperator child, int minorFragmentId)
      throws PhysicalOperatorSetupException;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @throws PhysicalOperatorSetupException
   */
  public abstract void applyAssignments(List<DrillbitEndpoint> endpoints) throws PhysicalOperatorSetupException;

  /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @throws PhysicalOperatorSetupException
   */
  public abstract void applyAssignments(List<DrillbitEndpoint> endpoints) throws PhysicalOperatorSetupException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   */
  @JsonIgnore
  public abstract int getMaxWidth();

  /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   */
  @JsonIgnore
  public abstract int getMaxWidth();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
   *           If the addition of this field is incompatible with this OutputMutator's capabilities.
   */
  public <T extends ValueVector> T addField(MaterializedField field, Class<T> clazz) throws SchemaChangeException;

  public void allocate(int recordCount);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
   * Clear this mutator i.e. reset it to pristine condition
   */
  public void clear();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
   * @return the CallBack object for this mutator
   */
  public CallBack getCallBack();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
   */
  // TODO(DRILL-2970)
  public boolean isNewSchema();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
  public <T extends ValueVector> T addField(MaterializedField field, Class<T> clazz) throws SchemaChangeException;

  public void allocate(int recordCount);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
   * @return A DrillBuf that will be released at the end of the current query (and can be resized as desired during use).
   */
  public DrillBuf getManagedBuffer();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java

  // Setup the input/output value vector references
  public void doSetup(FragmentContext context, VectorContainer rightContainer, RecordBatch leftBatch, RecordBatch outgoing);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java
      new TemplateClassDefinition<>(NestedLoopJoin.class, NestedLoopJoinTemplate.class);

  public void setupNestedLoopJoin(FragmentContext context, RecordBatch left,
                                  ExpandableHyperContainer rightContainer,
                                  LinkedList<Integer> rightCounts,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java
 */
public interface NestedLoopJoin {
  public static TemplateClassDefinition<NestedLoopJoin> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<>(NestedLoopJoin.class, NestedLoopJoinTemplate.class);

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java
 */
public interface NestedLoopJoin {
  public static TemplateClassDefinition<NestedLoopJoin> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<>(NestedLoopJoin.class, NestedLoopJoinTemplate.class);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java

  // Produce output records taking into account join type
  public int outputRecords(JoinRelType joinType);

  // Project the record at offset 'leftIndex' in the left input batch into the output container at offset 'outIndex'
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java

  // Project the record from the hyper container given the batch index and the record within the batch at 'outIndex'
  public void emitRight(int batchIndex, int recordIndexWithinBatch, int outIndex);

  // Setup the input/output value vector references
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoin.java`
#### Snippet
```java

  // Project the record at offset 'leftIndex' in the left input batch into the output container at offset 'outIndex'
  public void emitLeft(int leftIndex, int outIndex);

  // Project the record from the hyper container given the batch index and the record within the batch at 'outIndex'
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * @param newState
   */
  public void setBatchState(BatchState newState);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * Get the current BatchState (this is useful when performing row key join)
   */
  public BatchState getBatchState();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * condition then the state is transitioned to DONE.
   */
  public enum RowKeyJoinState {
    INITIAL, PROCESSING, DONE;
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   *    element is the number of row keys in this batch
   */
  public Pair<ValueVector, Integer> nextRowKeyBatch();


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * @param newState
   */
  public void setRowKeyJoinState(RowKeyJoinState newState);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * Get the current RowKeyJoinState.
   */
  public RowKeyJoinState getRowKeyJoinState();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   * @return True if ready, false if not
   */
  public boolean hasRowKeyBatch();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/ManagedReader.java`
#### Snippet
```java
   */
  @SuppressWarnings("serial")
  public class EarlyEofException extends Exception {
  }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/Sorter.java`
#### Snippet
```java

public interface Sorter {
  public void setup(FragmentContext context, SelectionVector4 vector4,
      VectorContainer hyperBatch) throws SchemaChangeException;
  public void sort(SelectionVector4 vector4, VectorContainer container);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/Sorter.java`
#### Snippet
```java
  public void sort(SelectionVector4 vector4, VectorContainer container);

  public static TemplateClassDefinition<Sorter> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<Sorter>(Sorter.class, SortTemplate.class);
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/Sorter.java`
#### Snippet
```java
  public void sort(SelectionVector4 vector4, VectorContainer container);

  public static TemplateClassDefinition<Sorter> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<Sorter>(Sorter.class, SortTemplate.class);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/Sorter.java`
#### Snippet
```java
  public void setup(FragmentContext context, SelectionVector4 vector4,
      VectorContainer hyperBatch) throws SchemaChangeException;
  public void sort(SelectionVector4 vector4, VectorContainer container);

  public static TemplateClassDefinition<Sorter> TEMPLATE_DEFINITION =
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java

public interface UnionAller {
  public abstract void setup(FragmentContext context, RecordBatch incoming,  RecordBatch outgoing, List<TransferPair> transfers)  throws SchemaChangeException;
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java

public interface UnionAller {
  public abstract void setup(FragmentContext context, RecordBatch incoming,  RecordBatch outgoing, List<TransferPair> transfers)  throws SchemaChangeException;
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java
public interface UnionAller {
  public abstract void setup(FragmentContext context, RecordBatch incoming,  RecordBatch outgoing, List<TransferPair> transfers)  throws SchemaChangeException;
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<UnionAller> TEMPLATE_DEFINITION = new TemplateClassDefinition<UnionAller>(UnionAller.class, UnionAllerTemplate.class);
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java
public interface UnionAller {
  public abstract void setup(FragmentContext context, RecordBatch incoming,  RecordBatch outgoing, List<TransferPair> transfers)  throws SchemaChangeException;
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<UnionAller> TEMPLATE_DEFINITION = new TemplateClassDefinition<UnionAller>(UnionAller.class, UnionAllerTemplate.class);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<UnionAller> TEMPLATE_DEFINITION = new TemplateClassDefinition<UnionAller>(UnionAller.class, UnionAllerTemplate.class);
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAller.java`
#### Snippet
```java
  public abstract int unionRecords(int startIndex, int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<UnionAller> TEMPLATE_DEFINITION = new TemplateClassDefinition<UnionAller>(UnionAller.class, UnionAllerTemplate.class);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SingleBatchSorter.java`
#### Snippet
```java
  public void sort(SelectionVector2 vector2) throws SchemaChangeException;

  public static TemplateClassDefinition<SingleBatchSorter> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<SingleBatchSorter>(SingleBatchSorter.class, SingleBatchSorterTemplate.class);
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SingleBatchSorter.java`
#### Snippet
```java
  public void sort(SelectionVector2 vector2) throws SchemaChangeException;

  public static TemplateClassDefinition<SingleBatchSorter> TEMPLATE_DEFINITION =
      new TemplateClassDefinition<SingleBatchSorter>(SingleBatchSorter.class, SingleBatchSorterTemplate.class);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SingleBatchSorter.java`
#### Snippet
```java

public interface SingleBatchSorter {
  public void setup(FragmentContext context, SelectionVector2 vector2, VectorAccessible incoming) throws SchemaChangeException;
  public void sort(SelectionVector2 vector2) throws SchemaChangeException;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SingleBatchSorter.java`
#### Snippet
```java
public interface SingleBatchSorter {
  public void setup(FragmentContext context, SelectionVector2 vector2, VectorAccessible incoming) throws SchemaChangeException;
  public void sort(SelectionVector2 vector2) throws SchemaChangeException;

  public static TemplateClassDefinition<SingleBatchSorter> TEMPLATE_DEFINITION =
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/FilterSignature.java`
#### Snippet
```java
public interface FilterSignature  extends CodeGeneratorSignature{

  public void doSetup(@Named("context") FragmentContextImpl context, @Named("incoming") RecordBatch incoming, @Named("outgoing") RecordBatch outgoing);
  public boolean doEval(@Named("inIndex") int inIndex, @Named("outIndex") int outIndex);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/FilterSignature.java`
#### Snippet
```java

  public void doSetup(@Named("context") FragmentContextImpl context, @Named("incoming") RecordBatch incoming, @Named("outgoing") RecordBatch outgoing);
  public boolean doEval(@Named("inIndex") int inIndex, @Named("outIndex") int outIndex);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/protocol/OperatorExec.java`
#### Snippet
```java
   */

  public void bind(OperatorContext context);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/BatchIterator.java`
#### Snippet
```java

public interface BatchIterator {
  public IterOutcome next();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/RecordMaterializer.java`
#### Snippet
```java
  public QueryWritableBatch convertNext();

  public QueryId queryId();

  public VectorContainer incoming();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/RecordMaterializer.java`
#### Snippet
```java
  public QueryId queryId();

  public VectorContainer incoming();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/RecordMaterializer.java`
#### Snippet
```java
public interface RecordMaterializer {

  public QueryWritableBatch convertNext();

  public QueryId queryId();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/QueryDataPackage.java`
#### Snippet
```java
   * Represents a batch of data with a schema.
   */
  public static class DataPackage implements QueryDataPackage {
    private final RecordMaterializer materializer;
    private final OperatorStats stats;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/QueryDataPackage.java`
#### Snippet
```java
   * Represents a batch of data with a schema.
   */
  public static class DataPackage implements QueryDataPackage {
    private final RecordMaterializer materializer;
    private final OperatorStats stats;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/QueryDataPackage.java`
#### Snippet
```java
   * finishes with no data. The results are null: no data, no schema.
   */
  public static class EmptyResultsPackage implements QueryDataPackage {

    private final QueryId queryId;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/materialize/QueryDataPackage.java`
#### Snippet
```java
   * finishes with no data. The results are null: no data, no schema.
   */
  public static class EmptyResultsPackage implements QueryDataPackage {

    private final QueryId queryId;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionOutgoingBatch.java`
#### Snippet
```java
  public long getTotalRecords();

  public void terminate();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionOutgoingBatch.java`
#### Snippet
```java
public interface PartitionOutgoingBatch {

  public long getTotalRecords();

  public void terminate();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java
  public static TemplateClassDefinition<SampleCopier> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleCopier>(SampleCopier.class, SampleCopierTemplate.class);

  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
  public abstract boolean copyRecords(int skip, int start, int total);
  public int getOutputRecords();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java

  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
  public abstract boolean copyRecords(int skip, int start, int total);
  public int getOutputRecords();

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java

  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
  public abstract boolean copyRecords(int skip, int start, int total);
  public int getOutputRecords();

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java
  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
  public abstract boolean copyRecords(int skip, int start, int total);
  public int getOutputRecords();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java

public interface SampleCopier {
  public static TemplateClassDefinition<SampleCopier> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleCopier>(SampleCopier.class, SampleCopierTemplate.class);

  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleCopier.java`
#### Snippet
```java

public interface SampleCopier {
  public static TemplateClassDefinition<SampleCopier> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleCopier>(SampleCopier.class, SampleCopierTemplate.class);

  public void setupCopier(FragmentContext context, SelectionVector4 sv4, VectorAccessible incoming, VectorAccessible outgoing) throws SchemaChangeException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleSorter.java`
#### Snippet
```java
  public void sort(SelectionVector2 vector2, VectorContainer container);

  public static TemplateClassDefinition<SampleSorter> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleSorter>(SampleSorter.class, SampleSortTemplate.class);

}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleSorter.java`
#### Snippet
```java
  public void sort(SelectionVector2 vector2, VectorContainer container);

  public static TemplateClassDefinition<SampleSorter> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleSorter>(SampleSorter.class, SampleSortTemplate.class);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleSorter.java`
#### Snippet
```java
public interface SampleSorter {
  public void setup(FragmentContext context, RecordBatch hyperBatch) throws SchemaChangeException;
  public void sort(SelectionVector2 vector2, VectorContainer container);

  public static TemplateClassDefinition<SampleSorter> TEMPLATE_DEFINITION = new TemplateClassDefinition<SampleSorter>(SampleSorter.class, SampleSortTemplate.class);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/SampleSorter.java`
#### Snippet
```java

public interface SampleSorter {
  public void setup(FragmentContext context, RecordBatch hyperBatch) throws SchemaChangeException;
  public void sort(SelectionVector2 vector2, VectorContainer container);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
                             VectorContainer partitionVectors, int partitions, SchemaPath outputField)
          throws SchemaChangeException;
  public abstract int projectRecords(int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<OrderedPartitionProjector> TEMPLATE_DEFINITION = new TemplateClassDefinition<OrderedPartitionProjector>(OrderedPartitionProjector.class, OrderedPartitionProjectorTemplate.class);
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
                             VectorContainer partitionVectors, int partitions, SchemaPath outputField)
          throws SchemaChangeException;
  public abstract int projectRecords(int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<OrderedPartitionProjector> TEMPLATE_DEFINITION = new TemplateClassDefinition<OrderedPartitionProjector>(OrderedPartitionProjector.class, OrderedPartitionProjectorTemplate.class);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
  public abstract int projectRecords(int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<OrderedPartitionProjector> TEMPLATE_DEFINITION = new TemplateClassDefinition<OrderedPartitionProjector>(OrderedPartitionProjector.class, OrderedPartitionProjectorTemplate.class);

}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
  public abstract int projectRecords(int recordCount, int firstOutputIndex);

  public static TemplateClassDefinition<OrderedPartitionProjector> TEMPLATE_DEFINITION = new TemplateClassDefinition<OrderedPartitionProjector>(OrderedPartitionProjector.class, OrderedPartitionProjectorTemplate.class);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
public interface OrderedPartitionProjector {

  public abstract void setup(FragmentContext context, VectorAccessible incoming, RecordBatch outgoing, List<TransferPair> transfers,
                             VectorContainer partitionVectors, int partitions, SchemaPath outputField)
          throws SchemaChangeException;
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjector.java`
#### Snippet
```java
public interface OrderedPartitionProjector {

  public abstract void setup(FragmentContext context, VectorAccessible incoming, RecordBatch outgoing, List<TransferPair> transfers,
                             VectorContainer partitionVectors, int partitions, SchemaPath outputField)
          throws SchemaChangeException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetLoader.java`
#### Snippet
```java
public interface ResultSetLoader {

  public static final int DEFAULT_ROW_COUNT = BaseValueVector.INITIAL_VALUE_ALLOCATION;

  /**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetLoader.java`
#### Snippet
```java
public interface ResultSetLoader {

  public static final int DEFAULT_ROW_COUNT = BaseValueVector.INITIAL_VALUE_ALLOCATION;

  /**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetLoader.java`
#### Snippet
```java
public interface ResultSetLoader {

  public static final int DEFAULT_ROW_COUNT = BaseValueVector.INITIAL_VALUE_ALLOCATION;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/TupleModel.java`
#### Snippet
```java
   */

  public interface RowSetModel extends TupleModel {
    VectorContainer container();
  }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/TupleModel.java`
#### Snippet
```java
   */

  public interface ColumnModel {
    ColumnMetadata schema();
    TupleModel mapModel();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/RequestedTuple.java`
#### Snippet
```java
public interface RequestedTuple {

  public enum TupleProjectionType {
    ALL, NONE, SOME
  }
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java

    // File creation modes
    public static enum Mode {
        CREATE,
        OVERWRITE
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  };

  public final static CheckValid<Integer> isNonNegative = new CheckValid<Integer>() {
     @Override
    public boolean isValid(Integer paramValue) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  };

  public final static CheckValid<Integer> isNonNegative = new CheckValid<Integer>() {
     @Override
    public boolean isValid(Integer paramValue) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  };

  public final static CheckValid<Integer> isNonNegative = new CheckValid<Integer>() {
     @Override
    public boolean isValid(Integer paramValue) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  Logger logger = LoggerFactory.getLogger(PluginConstants.class);

  public final static CheckValid<Integer> alwaysValid = new CheckValid<Integer>() {
    @Override
    public boolean isValid(Integer parameter) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  Logger logger = LoggerFactory.getLogger(PluginConstants.class);

  public final static CheckValid<Integer> alwaysValid = new CheckValid<Integer>() {
    @Override
    public boolean isValid(Integer parameter) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
  Logger logger = LoggerFactory.getLogger(PluginConstants.class);

  public final static CheckValid<Integer> alwaysValid = new CheckValid<Integer>() {
    @Override
    public boolean isValid(Integer parameter) {
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaDefProtos.java`
#### Snippet
```java
    private final int value;

    private ValueMode(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    private final int value;

    private RpcMode(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      private final int value;

      private State(int value) {
        this.value = value;
      }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
    private final int value;

    private MinorType(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
    private final int value;

    private DataMode(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private final int value;

    private RpcType(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ChannelListenerWithCoordinationId.java`
#### Snippet
```java

public interface ChannelListenerWithCoordinationId extends GenericFutureListener<ChannelFuture>{
  public int getCoordinationId();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/OutOfMemoryHandler.java`
#### Snippet
```java
public interface OutOfMemoryHandler {

  public static OutOfMemoryHandler DEFAULT_INSTANCE = new OutOfMemoryHandler() {
    @Override
    public void handle() {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/OutOfMemoryHandler.java`
#### Snippet
```java
public interface OutOfMemoryHandler {

  public static OutOfMemoryHandler DEFAULT_INSTANCE = new OutOfMemoryHandler() {
    @Override
    public void handle() {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/OutOfMemoryHandler.java`
#### Snippet
```java
  };

  public void handle();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ResponseSender.java`
#### Snippet
```java

public interface ResponseSender {
  public void send(Response r);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ConnectionThrottle.java`
#### Snippet
```java

public interface ConnectionThrottle {
  public void setAutoRead(boolean enableAutoRead);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcome.java`
#### Snippet
```java
  public void set(Object value, ByteBuf buffer);
  public void setException(Throwable t);
  public Class<T> getOutcomeType();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcome.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RpcOutcome.class);

  public void set(Object value, ByteBuf buffer);
  public void setException(Throwable t);
  public Class<T> getOutcomeType();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcome.java`
#### Snippet
```java

  public void set(Object value, ByteBuf buffer);
  public void setException(Throwable t);
  public Class<T> getOutcomeType();
}
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcome.java`
#### Snippet
```java

public interface RpcOutcome<T> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RpcOutcome.class);

  public void set(Object value, ByteBuf buffer);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcome.java`
#### Snippet
```java

public interface RpcOutcome<T> {
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RpcOutcome.class);

  public void set(Object value, ByteBuf buffer);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/ops/BufferManager.java`
#### Snippet
```java
   * @return A buffer
   */
  public DrillBuf getManagedBuffer(int size);

  BufferAllocator getAllocator();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/ops/BufferManager.java`
#### Snippet
```java
   * @return
   */
  public DrillBuf replace(DrillBuf old, int newSize);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/ops/BufferManager.java`
#### Snippet
```java
  BufferAllocator getAllocator();

  public void close();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/ops/BufferManager.java`
#### Snippet
```java
   * @return A buffer.
   */
  public DrillBuf getManagedBuffer();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */
  @Override
  public void close();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */

  public void write(DrillBuf buf, int length, OutputStream out) throws IOException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return the newly created reservation
   */
  public AllocationReservation newReservation();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * over its limit, all consumers of that allocator should aggressively try to addrss the overlimit situation.
   */
  public boolean isOverLimit();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * a no-op.
   */
  public void assertOpen();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */

  public void read(DrillBuf buf, int length, InputStream in) throws IOException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return the peak amount of memory allocated
   */
  public long getPeakMemoryAllocation();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return the amount of memory currently allocated
   */
  public long getAllocatedMemory();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return the new allocator, or null if it can't be created
   */
  public BufferAllocator newChildAllocator(String name, long initReservation, long maxAllocation);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * worry about them leaking or managing reference counts on them since they don't actually point to any memory.
   */
  public DrillBuf getEmpty();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return the underlying allocator used by this allocator
   */
  public ByteBufAllocator getAsByteBufAllocator();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   *           if buffer cannot be allocated
   */
  public DrillBuf buffer(int size);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return A very verbose description of the allocator hierarchy.
   */
  public String toVerboseString();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */

  public DrillBuf read(int length, InputStream in) throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */

  public boolean setLenient();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * coordinates about where this allocator was created
   */
  public String getName();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   *           if buffer cannot be allocated
   */
  public DrillBuf buffer(int size, BufferManager manager);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   * @return Limit in number of bytes.
   */
  public long getLimit();

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   */

  public void write(DrillBuf buf, OutputStream out) throws IOException;

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   *          The new Limit to apply to allocations
   */
  public void setLimit(long newLimit);

  /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/AllocationReservation.java`
#### Snippet
```java
  public boolean isClosed();

  public void close();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/AllocationReservation.java`
#### Snippet
```java
   * @return whether or not the reservation has been closed
   */
  public boolean isClosed();

  public void close();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/AllocationReservation.java`
#### Snippet
```java
   * @return whether or not the reservation has been used
   */
  public boolean isUsed();

  /**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/Accountant.java`
#### Snippet
```java
   * Describes the type of outcome that occurred when trying to account for allocation of memory.
   */
  public static enum AllocationOutcome {

    /**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
  }

  public static enum Verbosity {
    BASIC(false, false), // only include basic information
    LOG(true, false), // include basic
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private final int value;

    private RpcType(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `common/src/main/java/org/apache/drill/common/config/ConfigProvider.java`
#### Snippet
```java

public interface ConfigProvider {
  public Config getConfig();
}

```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private final int value;

    private FragmentState(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private final int value;

    private QueryType(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      private final int value;

      private ErrorType(int value) {
        this.value = value;
      }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private final int value;

    private RpcChannel(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      private final int value;

      private Type(int value) {
        this.value = value;
      }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private final int value;

    private SaslStatus(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      private final int value;

      private QueryState(int value) {
        this.value = value;
      }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private SubQuerySupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private RequestStatus(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private HandshakeStatus(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private QueryResultsMode(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private NullCollation(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private OuterJoinSupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private IdentifierCasing(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private OrderBySupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private RpcType(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private CollateSupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private ColumnSearchability(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private SaslSupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private GroupBySupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private UnionSupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private CorrelationNamesSupport(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private ColumnUpdatability(int value) {
      this.value = value;
    }
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private final int value;

    private DateTimeLiteralsSupport(int value) {
      this.value = value;
    }
```

## RuleId[id=SuspiciousDateFormat]
### SuspiciousDateFormat
Lowercase 'mm' (minute) pattern is used: probably 'MM' (month) was intended
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java
    // Start a year ago.
    baseTime = System.currentTimeMillis() - ONE_YEAR;
    fmt = new SimpleDateFormat("yyyy-mm-DD");
  }

```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * getSerDeOverheadFactor()` can be replaced with 'getSerDeOverheadFactor()'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveScan.java`
#### Snippet
```java
      // Hive's native reader is neither memory efficient nor fast. Increase the CPU cost
      // by a factor to let the planner choose HiveDrillNativeScan over HiveScan with SerDes.
      float cpuCost = 1 * getSerDeOverheadFactor();
      return new ScanStats(GroupScanProperty.NO_EXACT_ROW_COUNT, stats.getNumRows(), cpuCost, stats.getSizeInBytes());
    } catch (final IOException e) {
```

### PointlessArithmeticExpression
`seed + 0` can be replaced with 'seed'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/XXHash.java`
#### Snippet
```java
      long v1 = seed + PRIME64_1 + PRIME64_2;
      long v2 = seed + PRIME64_2;
      long v3 = seed + 0;
      long v4 = seed - PRIME64_1;

```

### PointlessArithmeticExpression
`1*1024*1024` can be replaced with '1024 \* 1024'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
  private static String DRILL_LOCAL_IMPL_STRING = "fs.drill-local.impl";
  private static final float STOP_SPOOLING_FRACTION = (float) 0.5;
  public static final long ALLOCATOR_INITIAL_RESERVATION = 1*1024*1024;
  public static final long ALLOCATOR_MAX_RESERVATION = 20L*1000*1000*1000;
  private static final int SPOOLING_SENDER_CREDIT = 20;
```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordReader.java`
#### Snippet
```java
    @JsonSubTypes.Type(value = FindLimit0Visitor.RelDataTypeReader.class, name = "RelDataTypeRecordReader") })
public interface RecordReader extends AutoCloseable {
  long ALLOCATOR_INITIAL_RESERVATION = 1 * 1024 * 1024;
  long ALLOCATOR_MAX_RESERVATION = 20L * 1000 * 1000 * 1000;

```

### PointlessArithmeticExpression
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortConfig.java`
#### Snippet
```java
   */

  public static final long MIN_SPILL_FILE_SIZE = 1 * 1024 * 1024;

  public static final int DEFAULT_SPILL_BATCH_SIZE = 8 * 1024 * 1024;
```

### PointlessArithmeticExpression
`1 * subType_.size()` can be replaced with 'subType_.size()'
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * subType_.size();
      }
      size += getUnknownFields().getSerializedSize();
```

### PointlessArithmeticExpression
`1 * getReceivingMinorFragmentIdList().size()` can be replaced with 'getReceivingMinorFragmentIdList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getReceivingMinorFragmentIdList().size();
      }
      if (((bitField0_ & 0x00000004) != 0)) {
```

### PointlessArithmeticExpression
`1 * getAuthenticationMechanismsList().size()` can be replaced with 'getAuthenticationMechanismsList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getAuthenticationMechanismsList().size();
      }
      size += getUnknownFields().getSerializedSize();
```

### PointlessArithmeticExpression
`1 * getBloomFilterSizeInBytesList().size()` can be replaced with 'getBloomFilterSizeInBytesList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getBloomFilterSizeInBytesList().size();
      }
      {
```

### PointlessArithmeticExpression
`1 * getProbeFieldsList().size()` can be replaced with 'getProbeFieldsList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getProbeFieldsList().size();
      }
      if (((bitField0_ & 0x00000010) != 0)) {
```

### PointlessArithmeticExpression
`1 * getAuthenticationMechanismsList().size()` can be replaced with 'getAuthenticationMechanismsList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getAuthenticationMechanismsList().size();
      }
      size += getUnknownFields().getSerializedSize();
```

### PointlessArithmeticExpression
`1 * getFunctionSignatureList().size()` can be replaced with 'getFunctionSignatureList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getFunctionSignatureList().size();
      }
      size += getUnknownFields().getSerializedSize();
```

### PointlessArithmeticExpression
`1 * getAuthenticationMechanismsList().size()` can be replaced with 'getAuthenticationMechanismsList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getAuthenticationMechanismsList().size();
      }
      {
```

### PointlessArithmeticExpression
`1 * supportedMethods_.size()` can be replaced with 'supportedMethods_.size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * supportedMethods_.size();
      }
      if (((bitField0_ & 0x00000020) != 0)) {
```

### PointlessArithmeticExpression
`1 * getTableTypeFilterList().size()` can be replaced with 'getTableTypeFilterList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getTableTypeFilterList().size();
      }
      size += getUnknownFields().getSerializedSize();
```

### PointlessArithmeticExpression
`1 * collateSupport_.size()` can be replaced with 'collateSupport_.size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * collateSupport_.size();
      }
      if (((bitField0_ & 0x00000020) != 0)) {
```

### PointlessArithmeticExpression
`1 * getDateTimeFunctionsList().size()` can be replaced with 'getDateTimeFunctionsList().size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getDateTimeFunctionsList().size();
      }
      {
```

### PointlessArithmeticExpression
`1 * dateTimeLiteralsSupport_.size()` can be replaced with 'dateTimeLiteralsSupport_.size()'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * dateTimeLiteralsSupport_.size();
      }
      if (((bitField0_ & 0x00000080) != 0)) {
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/IndexPaginator.java`
#### Snippet
```java

  public void setIndexValue(String indexValue) {
    if (StringUtils.isNumeric(indexValue)) {

    }
```

### EmptyStatementBody
`if` statement has empty body
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java

    String home = config.getString(DRILL_DIR_NAME);
    if (DoYUtil.isBlank(home)) {
      // Assume the archive expands without a subdirectory.
    }
```

### EmptyStatementBody
`if` statement has empty body
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AMSecurityManagerImpl.java`
#### Snippet
```java
      managerImpl = new SimpleSecurityManager();
      managerImpl.init();
    } else if (DoYUtil.isBlank(authType)
        || DrillOnYarnConfig.AUTH_TYPE_NONE.equals(authType)) {
    } else {
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          tdigest.add(in.value);
        } else {
          // do nothing since we track nulls outside the scope of the histogram
        }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        throw new IOException("Statistics writer encountered unexpected field");
      }
      if (nextField.equals(Statistic.COLTYPE)) {
        // Do not write out the type
      }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/ArrayValueParser.java`
#### Snippet
```java
      // Position: [ ^
      arrayParser.parse(tokenizer);
    } else if (token == JsonToken.VALUE_NULL) {
      // Treat as if the field was not present: ignore
    } else if (token.isScalarValue()) {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockStorageEngine.java`
#### Snippet
```java
      int n = Integer.parseInt(m.group(2));
      String unit = m.group(3);
      if (unit == null) { }
      else if (unit.equalsIgnoreCase("K")) { n *= 1000; }
      else if (unit.equalsIgnoreCase("M")) { n *= 1_000_000; }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/VectorValidator.java`
#### Snippet
```java
      }
    }
    if (hash == 0) {
//      System.out.println(hash);
    }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
          // No info found, based on remaining condition. Check if the leading columns are same as another index
          IndexConditionInfo origInfo = indexConditionRelatedToFields(leadingColumns, condition);
          if (origInfo == null || origInfo.hasIndexCol == false) {
            // do nothing
          } else {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
          scanRel);
      IndexDiscoverBase idxDiscover = constructor.newInstance(inScan, scanRel);
      if((targetIndexDiscoverClass != null) && (discoverClass != targetIndexDiscoverClass)) {

        //idxDiscover.setOriginalDiscover(targetIndexDiscoverClass);
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
          }
        }
      } else {
        // TODO: handle functional index
      }
```

### EmptyStatementBody
`else` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
      }
      idxFieldCount++;
    } else {
      // TODO: handle functional index
    }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillStatsTable.java`
#### Snippet
```java

  private void materializeFromStatistics() {
    if (statistics instanceof Statistics_v0) {
      // Do nothing
    } else if (statistics instanceof Statistics_v1) {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceExpressionsRule.java`
#### Snippet
```java
          final RexNode newConditionExp =
            expandedExprList.get(conditionIndex);
          if (newConditionExp.isAlwaysTrue()) {
            // condition is always TRUE - drop it.
          } else if (newConditionExp instanceof RexLiteral
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
      // reset state and continue to update
      isNewLeftBatch = false;
    } else if (inputIndex == RIGHT_INDEX && (rightJoinIndex == 0 || rightJoinIndex == -1)) {
      // continue to update
    } else {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanProjectionParser.java`
#### Snippet
```java
   */
  private void parseLeaf(ProjectedColumn parent, int depth) {
    if (parent.isSimple()) {
      // Nothing to do
    } else if (parent.isArray() && depth == 1) {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
  @Override
  public void close() {
    if (!spilledState.isFirstCycle()) { // spilling happened
      // In case closing due to cancellation, BaseRootExec.close() does not
      // close the open
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/NullColumnBuilder.java`
#### Snippet
```java

          col = new ResolvedNullColumn(outputCol, this, nullCols.size());
        } else if (type.getMinorType() != outputCol.type()) {

          // Conflict in type, can't use default value.
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/ExternalSortBatch.java`
#### Snippet
```java
      // First batch: we won't have a schema.
      schema = incomingSchema;
    } else if (incomingSchema.equals(schema)) {
      // Nothing to do.  Artificial schema changes are ignored.
    } else if (unionTypeEnabled) {
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      // empty map vector that causes validation to fail.
      ValueVector child = internalMap.getChild(type.name());
      if (child == null) {
        // Disabling this check for now. TopNBatch, SortBatch
        // and perhaps others will create vectors with a set of
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggBatch.java`
#### Snippet
```java
      }

      if (updateIfNeeded(newOutgoingRowWidth)) {
        // There is an update to outgoing row width.
        // un comment this if we want to adjust the batch row count of in flight batches.
```

### EmptyStatementBody
`while` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
          } else {
            // keep reading till we get a batch with record count > 0 or we have no more batches to read i.e. we get null
            while ((rawBatch = getNext(p)) != null && rawBatch.getHeader().getDef().getRecordCount() == 0) {
              // Do nothing
            }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/Projections.java`
#### Snippet
```java
  private static void parseLeaf(QualifierContainer parent) {
    Qualifier qual = parent.qualifier();
    if (qual == null) {
      // Nothing to do
    } else if (qual.isArray()) {
```

### EmptyStatementBody
`if` statement has empty body
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/TcpSession.java`
#### Snippet
```java
        // data,
        handshake.setFin();
      } else if (handshake.getCurrentSessionState() == TcpHandshake.State.CLOSE_WAIT) {

      }
```

### EmptyStatementBody
`if` statement has empty body
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
          C connection = this.get(remainingWaitTimeMills, TimeUnit.MILLISECONDS);

          if (connection == null) {
            //          logger.debug("Connection failed.");
          } else {
```

### EmptyStatementBody
`if` statement has empty body
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DistributionFunctions.java`
#### Snippet
```java
      if (v2 == 0) {
        resultHolder.value = 0;
      } else if (v1 == 0) {
        // No op
      } else {
```

### EmptyStatementBody
`else` statement has empty body
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DistributionFunctions.java`
#### Snippet
```java
        } else if ((xValue > prevXValue.value && yValue < prevYValue.value) || (xValue < prevXValue.value && yValue > prevYValue.value)) {
          discordantPairs.value = discordantPairs.value + 1;
        } else {
          // Tie...
        }
```

### EmptyStatementBody
`if` statement has empty body
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java
    if (cls.isArray()) {
      visitArray(obj, cls, state);
    } else if (cls.getName().startsWith("scala.reflect")) {
      // Many objects in the scala.reflect package reference global reflection objects which, in
      // turn, reference many other large global objects. Do nothing in this case.
```

### EmptyStatementBody
`if` statement has empty body
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java
      // Many objects in the scala.reflect package reference global reflection objects which, in
      // turn, reference many other large global objects. Do nothing in this case.
    } else if (obj instanceof ClassLoader || obj instanceof Class<?>) {
      // Hadoop JobConfs created in the interpreter have a ClassLoader, which greatly confuses
      // the size estimator since it references the whole REPL. Do nothing in this case. In
```

### EmptyStatementBody
`if` statement has empty body
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java
            // Java 9+ can throw InaccessibleObjectException but the class is Java 9+-only
          } catch (RuntimeException re) {
            if (re.getClass().getSimpleName().equals("InaccessibleObjectException")) {
              // do nothing
            } else {
```

## RuleId[id=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass KillingState from superclass TaskState initializer might lead to class loading deadlock
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
  public static final TaskState RUNNING = new RunningState();
  public static final TaskState ENDING = new EndingState();
  public static final TaskState KILLING = new KillingState();
  public static final TaskState WAIT_END_ACK = new WaitEndAckState();
  public static final TaskState END = new EndState();
```

### StaticInitializerReferencesSubClass
Referencing subclass EndingState from superclass TaskState initializer might lead to class loading deadlock
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
  public static final TaskState WAIT_START_ACK = new WaitStartAckState();
  public static final TaskState RUNNING = new RunningState();
  public static final TaskState ENDING = new EndingState();
  public static final TaskState KILLING = new KillingState();
  public static final TaskState WAIT_END_ACK = new WaitEndAckState();
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType SIGNED_BYTE = new AtomType("SIGNED_BYTE", Comparability.ORDERED, true);
  public static final DataType SIGNED_INT16 = new AtomType("SIGNED_INT16", Comparability.ORDERED, true);
  public static final DataType NVARCHAR = new AtomType("VARCHAR", Comparability.ORDERED, false);
  public static final DataType FLOAT32 = new AtomType("FLOAT32", Comparability.ORDERED, true);
  public static final DataType FLOAT64 = new AtomType("FLOAT64", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType BOOLEAN = new AtomType("BOOLEAN", Comparability.EQUAL, false);
  public static final DataType BYTES = new AtomType("BYTES", Comparability.ORDERED, false);
  public static final DataType SIGNED_BYTE = new AtomType("SIGNED_BYTE", Comparability.ORDERED, true);
  public static final DataType SIGNED_INT16 = new AtomType("SIGNED_INT16", Comparability.ORDERED, true);
  public static final DataType NVARCHAR = new AtomType("VARCHAR", Comparability.ORDERED, false);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType DATE = new AtomType("DATE", Comparability.ORDERED, false);
  public static final DataType DATETIME = new AtomType("DATETIME", Comparability.ORDERED, false);
  public static final DataType MAP = new AtomType("MAP", Comparability.NONE, false);
  public static final DataType ARRAY = new AtomType("ARRAY", Comparability.NONE, false);
  public static final DataType NULL = new AtomType("NULL", Comparability.NONE, false);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType LATEBIND = new LateBindType();
  public static final DataType BOOLEAN = new AtomType("BOOLEAN", Comparability.EQUAL, false);
  public static final DataType BYTES = new AtomType("BYTES", Comparability.ORDERED, false);
  public static final DataType SIGNED_BYTE = new AtomType("SIGNED_BYTE", Comparability.ORDERED, true);
  public static final DataType SIGNED_INT16 = new AtomType("SIGNED_INT16", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
//  public static final DataType BIG_DECIMAL = new AtomType("bigdecimal", Comparability.ORDERED, true);
  public static final DataType DATE = new AtomType("DATE", Comparability.ORDERED, false);
  public static final DataType DATETIME = new AtomType("DATETIME", Comparability.ORDERED, false);
  public static final DataType MAP = new AtomType("MAP", Comparability.NONE, false);
  public static final DataType ARRAY = new AtomType("ARRAY", Comparability.NONE, false);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType FLOAT64 = new AtomType("FLOAT64", Comparability.ORDERED, true);
  public static final DataType INT64 = new AtomType("INT64", Comparability.ORDERED, true);
  public static final DataType INT32 = new AtomType("INT32", Comparability.ORDERED, true);
  public static final DataType INT16 = new AtomType("INT16", Comparability.ORDERED, true);
  public static final DataType UINT16 = new AtomType("UINT16", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType NVARCHAR = new AtomType("VARCHAR", Comparability.ORDERED, false);
  public static final DataType FLOAT32 = new AtomType("FLOAT32", Comparability.ORDERED, true);
  public static final DataType FLOAT64 = new AtomType("FLOAT64", Comparability.ORDERED, true);
  public static final DataType INT64 = new AtomType("INT64", Comparability.ORDERED, true);
  public static final DataType INT32 = new AtomType("INT32", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType BYTES = new AtomType("BYTES", Comparability.ORDERED, false);
  public static final DataType SIGNED_BYTE = new AtomType("SIGNED_BYTE", Comparability.ORDERED, true);
  public static final DataType SIGNED_INT16 = new AtomType("SIGNED_INT16", Comparability.ORDERED, true);
  public static final DataType NVARCHAR = new AtomType("VARCHAR", Comparability.ORDERED, false);
  public static final DataType FLOAT32 = new AtomType("FLOAT32", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
//  public static final DataType BIG_INTEGER = new AtomType("bigint", Comparability.ORDERED, true);
//  public static final DataType BIG_DECIMAL = new AtomType("bigdecimal", Comparability.ORDERED, true);
  public static final DataType DATE = new AtomType("DATE", Comparability.ORDERED, false);
  public static final DataType DATETIME = new AtomType("DATETIME", Comparability.ORDERED, false);
  public static final DataType MAP = new AtomType("MAP", Comparability.NONE, false);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java

  public static final DataType LATEBIND = new LateBindType();
  public static final DataType BOOLEAN = new AtomType("BOOLEAN", Comparability.EQUAL, false);
  public static final DataType BYTES = new AtomType("BYTES", Comparability.ORDERED, false);
  public static final DataType SIGNED_BYTE = new AtomType("SIGNED_BYTE", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType INT64 = new AtomType("INT64", Comparability.ORDERED, true);
  public static final DataType INT32 = new AtomType("INT32", Comparability.ORDERED, true);
  public static final DataType INT16 = new AtomType("INT16", Comparability.ORDERED, true);
  public static final DataType UINT16 = new AtomType("UINT16", Comparability.ORDERED, true);
//  public static final DataType INT16 = new AtomType("int16", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType MAP = new AtomType("MAP", Comparability.NONE, false);
  public static final DataType ARRAY = new AtomType("ARRAY", Comparability.NONE, false);
  public static final DataType NULL = new AtomType("NULL", Comparability.NONE, false);

  //TODO: Hack to get some size data, needs to be fixed so that each type reveals it's size.
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType DATETIME = new AtomType("DATETIME", Comparability.ORDERED, false);
  public static final DataType MAP = new AtomType("MAP", Comparability.NONE, false);
  public static final DataType ARRAY = new AtomType("ARRAY", Comparability.NONE, false);
  public static final DataType NULL = new AtomType("NULL", Comparability.NONE, false);

```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType FLOAT32 = new AtomType("FLOAT32", Comparability.ORDERED, true);
  public static final DataType FLOAT64 = new AtomType("FLOAT64", Comparability.ORDERED, true);
  public static final DataType INT64 = new AtomType("INT64", Comparability.ORDERED, true);
  public static final DataType INT32 = new AtomType("INT32", Comparability.ORDERED, true);
  public static final DataType INT16 = new AtomType("INT16", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType SIGNED_INT16 = new AtomType("SIGNED_INT16", Comparability.ORDERED, true);
  public static final DataType NVARCHAR = new AtomType("VARCHAR", Comparability.ORDERED, false);
  public static final DataType FLOAT32 = new AtomType("FLOAT32", Comparability.ORDERED, true);
  public static final DataType FLOAT64 = new AtomType("FLOAT64", Comparability.ORDERED, true);
  public static final DataType INT64 = new AtomType("INT64", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass AtomType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType INT32 = new AtomType("INT32", Comparability.ORDERED, true);
  public static final DataType INT16 = new AtomType("INT16", Comparability.ORDERED, true);
  public static final DataType UINT16 = new AtomType("UINT16", Comparability.ORDERED, true);
//  public static final DataType INT16 = new AtomType("int16", Comparability.ORDERED, true);
//  public static final DataType BIG_INTEGER = new AtomType("bigint", Comparability.ORDERED, true);
```

### StaticInitializerReferencesSubClass
Referencing subclass LateBindType from superclass DataType initializer might lead to class loading deadlock
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java


  public static final DataType LATEBIND = new LateBindType();
  public static final DataType BOOLEAN = new AtomType("BOOLEAN", Comparability.EQUAL, false);
  public static final DataType BYTES = new AtomType("BYTES", Comparability.ORDERED, false);
```

### StaticInitializerReferencesSubClass
Referencing subclass PcapIsbUsrdeliv from superclass PcapColumn initializer might lead to class loading deadlock
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapColumn.java`
#### Snippet
```java
    summary_columns.put("isb_filteraccept", new PcapIsbFilterAccept());
    summary_columns.put("isb_osdrop", new PcapIsbOSdrop());
    summary_columns.put("isb_usrdeliv", new PcapIsbUsrdeliv());
  }

```

## RuleId[id=EmptyFinallyBlock]
### EmptyFinallyBlock
Empty `finally` block
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
        }
        parent.throwUserException(e, "Exception occurred while reading from disk.");
      } finally {
        //Nothing to do if isShuttingDown.
    }
```

### EmptyFinallyBlock
Empty `finally` block
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java
        logger.debug("Sent message.  Ending writer index was {}.", buf.writerIndex());
      }
    } finally {
      // make sure to release Rpc Messages underlying byte buffers.
      //msg.release();
```

### EmptyFinallyBlock
Empty `finally` block
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduRecordWriterImpl.java`
#### Snippet
```java
    } catch (Exception e) {
      throw new IOException(e);
    } finally {
      // context.getStats().stopWait();
    }
```

## RuleId[id=SortedCollectionWithNonComparableKeys]
### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseGroupScan.java`
#### Snippet
```java

      boolean foundStartRegion = false;
      regionsToScan = new TreeMap<>();
      for (HRegionLocation regionLocation : regionLocations) {
        HRegionInfo regionInfo = regionLocation.getRegionInfo();
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (7 lines)
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/ListVector.java`
#### Snippet
```java
//    field with children filled in, but with no expectation that the vector will
//    match its schema until this vector itself is loaded. Indeed a mess.
//
//    assert field.getChildren().size() <= 1;
//    for (MaterializedField child : field.getChildren()) {
```

### CommentedOutCode
Commented out code (3 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
  }

  // private String toKey(Container container) {
  // return zkDriver.toKey(container.getNodeId().getHost());
  // }
```

### CommentedOutCode
Commented out code (3 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/DrillOnYarn.java`
#### Snippet
```java
      // archive is fast enough that a "start without upload" option is not really
      // needed.
//    case RESTART:
//      cmd = new StartCommand(false, true);
//      break;
```

### CommentedOutCode
Commented out code (2 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillApplicationMaster.java`
#### Snippet
```java
    // processes other than Drillbits.

    // Dispatcher am = (new SimpleBatchFactory( )).build( );
    // Dispatcher am = (new MockDrillbitFactory( )).build( );
    Dispatcher dispatcher;
```

### CommentedOutCode
Commented out code (5 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
    // unreliable. Better to rely on the Resource Manager
    // completion request.
    // Task task = getTask(containerId);
    // if (task == null) {
    // return; }
```

### CommentedOutCode
Commented out code (8 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
    }

    // yarnNodeCount = yarn.getNodeCount();
    // LOG.info("YARN reports " + yarnNodeCount + " nodes.");

```

### CommentedOutCode
Commented out code (6 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/ControllerModel.java`
#### Snippet
```java
    // YARN RM UI instead.

//    if ( state == State.LIVE ) {
//      RegisterApplicationMasterResponse resp = impl.getYarn( ).getRegistrationResponse();
//      yarnVcores = resp.getMaximumResourceCapability().getVirtualCores();
```

### CommentedOutCode
Commented out code (3 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     */

    // @Override
    // public void containerStopped(EventContext context) {
    // }
```

### CommentedOutCode
Commented out code (4 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     */

//    @Override
//    public void containerStopped(EventContext context) {
//      transition(context, WAIT_COMPLETE);
```

### CommentedOutCode
Commented out code (4 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     */

//    @Override
//    public void containerStopped(EventContext context) {
//      transition(context, WAIT_COMPLETE);
```

### CommentedOutCode
Commented out code (4 lines)
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
    }

    // @Override
    // public void containerStopped(EventContext context) {
    // transition(context, WAIT_COMPLETE );
```

### CommentedOutCode
Commented out code (2 lines)
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionFunction.java`
#### Snippet
```java
  public static LogicalExpression create(String functionName, List<LogicalExpression> expressions)
      throws ExpressionValidationException {
    // logger.debug("Requesting generation of new function with name {}.",
    // functionName);
    if (!FUNCTION_MAP.containsKey(functionName)) {
```

### CommentedOutCode
Commented out code (3 lines)
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
  public static final DataType INT16 = new AtomType("INT16", Comparability.ORDERED, true);
  public static final DataType UINT16 = new AtomType("UINT16", Comparability.ORDERED, true);
//  public static final DataType INT16 = new AtomType("int16", Comparability.ORDERED, true);
//  public static final DataType BIG_INTEGER = new AtomType("bigint", Comparability.ORDERED, true);
//  public static final DataType BIG_DECIMAL = new AtomType("bigdecimal", Comparability.ORDERED, true);
```

### CommentedOutCode
Commented out code (5 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillComplexWriterFuncHolder.java`
#### Snippet
```java


//    JConditional jc = classGenerator.getEvalBlock()._if(complexWriter.invoke("ok").not());

//    jc._then().add(complexWriter.invoke("reset"));
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
    }
    final ValueVectorSetup setup = new ValueVectorSetup(batchName, fieldId);
//    JVar var = this.vvDeclaration.get(setup);
//    if(var != null) return var;

```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillFuncHolder.java`
#### Snippet
```java
      // from FieldReader to respective Holder.

      // FooHolder param = new FooHolder();
      // param.reader = inputVar;
      JType holderClass = getParamClass(model, parameter, inputHolder.type());
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillFuncHolder.java`
#### Snippet
```java
      // Then, if there is another function that use a holder, we can convert
      // from the UnionReader to a UnionHolder.
      //
      // UnionHolder param = new UnionHolder();
      // inputVar.read(param);
```

### CommentedOutCode
Commented out code (4 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
        // If the last read caused us to reach the end of stream
        // we are done.
        //InputStream input = in;
        //if (input != null && input.available() <= 0) {
        //  return bytesRead;
```

### CommentedOutCode
Commented out code (45 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
   * @param args
   */
  /*
  public static void main(String[] args) {
    final DrillConfig config = DrillConfig.create();
```

### CommentedOutCode
Commented out code (14 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
    myContainer.buildSchema(svMode);
    myContainer.setRecordCount(recordCount);
    /*
    // for debugging -- show values from the first row
    Object tmp0 = (myContainer).getValueAccessorById(NullableVarCharVector.class, 0).getValueVector();
```

### CommentedOutCode
Commented out code (8 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockSubScanPOP.java`
#### Snippet
```java
                        @JsonProperty("entries") List<MockScanEntry> readEntries) {
    this.readEntries = readEntries;
//    OperatorCost cost = new OperatorCost(0,0,0,0);
//    Size size = new Size(0,0);
//    for(MockGroupScanPOP.MockScanEntry r : readEntries){
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java
        }
      }
      /*if (!super.supportDirectoryReads() && selection.containsDirectories(fs)) {
        return null;
      }*/
```

### CommentedOutCode
Commented out code (5 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    }
    // TODO: allow conversion form bytes only when actual type of data is known (to obtain scale)
    /* else if (value instanceof byte[]) {
      return new BigInteger((byte[]) value).floatValue();
    } else if (value instanceof Binary) {
```

### CommentedOutCode
Commented out code (5 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    }
    // TODO: allow conversion form bytes only when actual type of data is known (to obtain scale)
    /* else if (value instanceof byte[]) {
      return new BigInteger((byte[]) value).doubleValue();
    } else if (value instanceof Binary) {
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/DrillCompressionCodecFactory.java`
#### Snippet
```java
  public synchronized void release() {
    // TODO: uncomment once PARQUET-2126 is fixed.
    // parqCodecFactory.release();
    // logger.debug("released {}", parqCodecFactory);

```

### CommentedOutCode
Commented out code (9 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
    // and for all here.  Instead we must encounter the dictionary page during calls to next()
    // and the code that follows remains commented out.
    /*
    long dictPageOffset = columnChunkMetaData.getDictionaryPageOffset();
    if (dictPageOffset < dataReader.getPos()) {
```

### CommentedOutCode
Commented out code (10 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorInitializer.java`
#### Snippet
```java
      allocateVector(vector, recordCount, hint);
    }
//    Set<BufferLedger> ledgers = new HashSet<>();
//    vector.collectLedgers(ledgers);
//    int size = 0;
```

### CommentedOutCode
Commented out code (7 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/HyperVectorWrapper.java`
#### Snippet
```java
  public VectorWrapper<T> cloneAndTransfer(BufferAllocator allocator) {
    return new HyperVectorWrapper<T>(f, vectors, false);
//    T[] newVectors = (T[]) Array.newInstance(vectors.getClass().getComponentType(), vectors.length);
//    for(int i =0; i < newVectors.length; i++) {
//      TransferPair tp = vectors[i].getTransferPair();
```

### CommentedOutCode
Commented out code (14 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
  }

//  /**
//   * Add a field where we don't have type information. In this case, DataType will be set to LATEBIND and valueClass
//   * will be set to null.
```

### CommentedOutCode
Commented out code (14 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
  }

//  private void setTypedField(short fieldId, DataType type, boolean nullable, ValueMode mode, Class<?> valueClass)
//      throws SchemaChangeException {
//    MaterializedField f = new MaterializedField(fieldId, type, nullable, mode, valueClass);
```

### CommentedOutCode
Commented out code (16 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
  }

//  public void addTypedField(short fieldId, DataType type, boolean nullable, ValueMode mode, Class<?> valueClass)
//      throws SchemaChangeException {
//    if (fields.containsKey(fieldId))
```

### CommentedOutCode
Commented out code (9 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassCompilerSelector.java`
#### Snippet
```java
    // see QueryClassLoader.

//    final File baseDir = new File( new File( System.getProperty("java.io.tmpdir") ), "classes" );
//    for ( int i = 0;  i < bc.length;  i++ ) {
//      File classFile = new File( baseDir, className.slash + i + ".class" );
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/MergeAdapter.java`
#### Snippet
```java
      mn.instructions.resetLabels();

      // mn.accept(new RemappingMethodAdapter(mn.access, mn.desc, mv, new
      // SimpleRemapper("org.apache.drill.exec.compile.ExampleTemplate", "Bunky")));
      ClassSet top = set;
```

### CommentedOutCode
Commented out code (7 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/MergeAdapter.java`
#### Snippet
```java

      // enable when you want all the generated merged class files to also be written to disk.
//      try {
//        File destDir = new File( "/tmp/scratch/drill-generated-classes" );
//        destDir.mkdirs();
```

### CommentedOutCode
Commented out code (50 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBodyBuilder.java`
#### Snippet
```java
  }

//  /**
//   * Heuristically parse IMPORT declarations at the beginning of the character stream produced by the given
//   * {@link Reader}. After this method returns, all characters up to and including that last IMPORT declaration have
```

### CommentedOutCode
Commented out code (8 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/QueryClassLoader.java`
#### Snippet
```java
    // codes before manipulation.

//    final File baseDir = new File( new File( System.getProperty("java.io.tmpdir") ), "classes" );
//    String path = className.replace( '.', '/' );
//    File classFile = new File( baseDir, path + ".class" );
```

### CommentedOutCode
Commented out code (9 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/AnalyzeTableHandler.java`
#### Snippet
```java
      columnList.add(new SqlIdentifier(SchemaPath.STAR_COLUMN.rootName(), SqlParserPos.ZERO));
    }
    /*final SqlNodeList columnList = new SqlNodeList(SqlParserPos.ZERO);
    final List<String> fields = sqlAnalyzeTable.getFieldNames();
    if (fields == null || fields.size() <= 0) {
```

### CommentedOutCode
Commented out code (8 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
  }

  /*
  private boolean isMatch(AggregateCall call) {
    if (call.getAggregation() instanceof SqlAvgAggFunction) {
```

### CommentedOutCode
Commented out code (9 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
    // NOTE:  these references are with respect to the output
    // of newAggRel
    /*
    RexNode numeratorRef =
        rexBuilder.makeCall(CastHighOp,
```

### CommentedOutCode
Commented out code (4 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillMergeFilterRule.java`
#### Snippet
```java
            mergedProgram.getCondition());

//    if(!RexUtil.isFlat(newCondition)){
//      RexCall newCall = (RexCall) newCondition;
//      newCondition = rexBuilder.makeFlatCall( newCall.getOperator(), newCall.getOperands());
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/RangePartitionExchangePrel.java`
#### Snippet
```java

    // commenting out following based on comments above
    // double rangePartitionCpuCost = DrillCostBase.RANGE_PARTITION_CPU_COST * inputRows;
    // double svrCpuCost = DrillCostBase.SVR_CPU_COST * inputRows;
    // double networkCost = DrillCostBase.BYTE_NETWORK_COST * inputRows * rowWidth;
```

### CommentedOutCode
Commented out code (4 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortImpl.java`
#### Snippet
```java
    // to kill the query because of a small, spurious over-allocation.

//    long maxMem = memManager.getMemoryLimit();
//    long newMax = (long)(maxMem * 1.10);
//    allocator.setLimit(newMax);
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/Unnest.java`
#### Snippet
```java
 */
public interface Unnest {
  //TemplateClassDefinition<Unnest> TEMPLATE_DEFINITION = new TemplateClassDefinition<Unnest>(Unnest.class, UnnestImpl
  // .class);

```

### CommentedOutCode
Commented out code (2 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    // This is a handy place to trace batches as they flow up
    // the DAG. Works best for single-threaded runs with a few records.
//     System.out.println(batch.getClass().getSimpleName());
//     RowSetFormatter.print(RowSets.wrap(batch));
    ErrorReporter reporter = errorReporter(batch);
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
        // and perhaps others will create vectors with a set of
        // types, but won't actually populate some of the types.
        //
        // error(name, vector, String.format(
        //     "Union vector includes type %s, but the internal map has no matching member",
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/SingleVectorState.java`
#### Snippet
```java
      // confusing off-by-one design.

      // VectorChecker.verifyOffsets("nested", sourceVector);
      // VectorPrinter.printOffsets(sourceVector, sourceStartIndex - 1, sourceEndIndex - sourceStartIndex + 3);
      // VectorPrinter.printOffsets((UInt4Vector) mainVector, 0, newIndex);
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    public void endBlock() throws IOException {
//        if (currentRecordCount == 0) {
//            throw new ParquetEncodingException("End block with zero record");
//        }
```

### CommentedOutCode
Commented out code (2 lines)
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5BatchReader.java`
#### Snippet
```java
  private HDF5DataWriter buildWriter(MinorType dataType) {
    switch (dataType) {
      /*case GENERIC_OBJECT:
        return new HDF5EnumDataWriter(hdfFile, writerSpec, readerConfig.defaultPath);*/
      case VARCHAR:
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
        });

//     if(TransportCheck.SUPPORTS_EPOLL){
//       b.option(EpollChannelOption.SO_REUSEPORT, true); //
//     }
```

### CommentedOutCode
Commented out code (3 lines)
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicClient.java`
#### Snippet
```java
      }); //

    // if(TransportCheck.SUPPORTS_EPOLL){
    // b.option(EpollChannelOption.SO_REUSEPORT, true); //
    // }
```

### CommentedOutCode
Commented out code (2 lines)
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
          hugeBufferSize.addAndGet(buf.capacity());
          largeBuffersHist.update(buf.capacity());
          // logger.debug("Allocating huge buffer of size {}", initialCapacity,
          // new Exception());
          return new UnsafeDirectLittleEndian(
```

### CommentedOutCode
Commented out code (2 lines)
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/config/MongoPersistentStore.java`
#### Snippet
```java

  public MongoPersistentStore(PersistentStoreConfig<V> config, MongoCollection<Document> collection) {
//    this.config = config;
//    this.collection = collection;
    throw new UnsupportedOperationException("Mongo DB PStore not currently supported");
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of field `offsetsFromUTF8`
in `exec/vector/src/main/java/org/apache/drill/exec/util/Text.java`
#### Snippet
```java
  }

  static final int offsetsFromUTF8[] =
  { 0x00000000, 0x00003080,
      0x000E2080, 0x03C82080, 0xFA082080, 0x82082080 };
```

### CStyleArrayDeclaration
C-style array declaration of parameter `accessors`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ColumnReaderFactory.java`
#### Snippet
```java
  }

  public static <T> T newAccessor(MinorType type, Class<? extends T> accessors[]) {
    try {
      Class<? extends T> accessorClass = accessors[type.ordinal()];
```

### CStyleArrayDeclaration
C-style array declaration of field `requiredReaders`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ColumnReaderFactory.java`
#### Snippet
```java

  private static final int typeCount = MinorType.values().length;
  private static final Class<? extends BaseScalarReader> requiredReaders[] = new Class[typeCount];

  static {
```

### CStyleArrayDeclaration
C-style array declaration of field `requiredWriters`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/ColumnWriterFactory.java`
#### Snippet
```java

  private static final int typeCount = MinorType.values().length;
  private static final Class<? extends BaseScalarWriter> requiredWriters[] = new Class[typeCount];

  static {
```

### CStyleArrayDeclaration
C-style array declaration of field `ZERO_BUF`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/AbstractFixedWidthWriter.java`
#### Snippet
```java
     * zeros.
     */
    private static final byte ZERO_BUF[] = new byte[256];

    public BaseFixedWidthWriter() {
```

### CStyleArrayDeclaration
C-style array declaration of field `emptyValue`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseScalarWriter.java`
#### Snippet
```java
   * value.
   */
  protected byte emptyValue[];

  protected DrillBuf drillBuf;
```

### CStyleArrayDeclaration
C-style array declaration of field `variants`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/UnionVectorShim.java`
#### Snippet
```java

  private final UnionVector vector;
  private final AbstractObjectWriter variants[];
  private UnionWriterImpl writer;

```

### CStyleArrayDeclaration
C-style array declaration of parameter `variants`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/UnionVectorShim.java`
#### Snippet
```java

  public UnionVectorShim(UnionVector vector,
      AbstractObjectWriter variants[]) {
    this.vector = vector;
    typeWriter = ColumnWriterFactory.newWriter(vector.getTypeVector());
```

### CStyleArrayDeclaration
C-style array declaration of parameter `variants`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/UnionWriterImpl.java`
#### Snippet
```java

  public UnionWriterImpl(ColumnMetadata schema, UnionVector vector,
      AbstractObjectWriter variants[]) {
    this(schema);
    bindShim(new UnionVectorShim(vector, variants));
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnNames`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillStatementImpl.java`
#### Snippet
```java

  @Override
  public boolean execute(String sql, String columnNames[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnIndexes`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillStatementImpl.java`
#### Snippet
```java

  @Override
  public boolean execute(String sql, int columnIndexes[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnIndexes`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java

  @Override
  public boolean execute(String sql, int columnIndexes[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnIndexes`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java

  @Override
  public int executeUpdate(String sql, int columnIndexes[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnNames`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java

  @Override
  public int executeUpdate(String sql, String columnNames[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnNames`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java

  @Override
  public boolean execute(String sql, String columnNames[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnNames`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillConnectionImpl.java`
#### Snippet
```java
  @Override
  public PreparedStatement prepareStatement(String sql,
                                            String columnNames[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `columnIndexes`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillConnectionImpl.java`
#### Snippet
```java
  @Override
  public PreparedStatement prepareStatement(String sql,
                                            int columnIndexes[]) throws SQLException {
    checkOpen();
    try {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `nodeArr`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java

    boolean relaxLocality = true;
    String nodeArr[] = null;
    if (!hosts.isEmpty()) {
      nodeArr = new String[hosts.size()];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rackArr`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java
      relaxLocality = false;
    }
    String rackArr[] = null;
    if (!racks.isEmpty()) {
      nodeArr = new String[racks.size()];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `data`
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
        // registration is badly formatted, etc.

        byte data[] = zkCoord.getCurator().getData().forPath(amPath);
        String existing;
        if (data == null) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `unpacked`
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
        } else {
          String packed = new String(data, StandardCharsets.UTF_8);
          String unpacked[] = packed.split(":");
          if (unpacked.length < 3) {
            existing = packed;
```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
  }

  public static Object dynamicCall(Object target, String fnName, Object args[],
      Class<?> types[]) {

```

### CStyleArrayDeclaration
C-style array declaration of parameter `types`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java

  public static Object dynamicCall(Object target, String fnName, Object args[],
      Class<?> types[]) {

    // First, look for the method using the names and types provided.
```

### CStyleArrayDeclaration
C-style array declaration of parameter `argv`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/DrillOnYarn.java`
#### Snippet
```java
  }

  public static void main(String argv[]) {
    BasicConfigurator.configure();
    ClientContext.init();
```

### CStyleArrayDeclaration
C-style array declaration of parameter `argv`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/DrillOnYarn.java`
#### Snippet
```java
  }

  public static void run(String argv[]) {
    ClientContext context = ClientContext.instance();

```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java
   * error option being set.
   */
  public boolean parse(String args[]) {
    for (int i = 0; i < args.length; i++) {
      String arg = args[i];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `cmd`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
        throw new ClientException("Failed to create site archive temp file", e);
      }
      String cmd[] = new String[] { "tar", "-C", localSiteDir.getAbsolutePath(),
          "-czf", siteArchiveFile.getAbsolutePath(), "." };
      List<String> cmdList = Arrays.asList(cmd);
```

### CStyleArrayDeclaration
C-style array declaration of field `envVars`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
  };

  private static String envVars[] = {
      APP_ID_ENV_VAR,
      DRILL_HOME_ENV_VAR,
```

### CStyleArrayDeclaration
C-style array declaration of field `suffixes`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
  }

  private static String suffixes[] = { ".tar.gz", ".tgz", ".zip" };

  public static String findSuffix(String baseName) {
```

### CStyleArrayDeclaration
C-style array declaration of field `keys`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
  }

  private static final String keys[] = {
    // drill.yarn

```

### CStyleArrayDeclaration
C-style array declaration of local variable `names`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMYarnFacadeImpl.java`
#### Snippet
```java
      // YARN seems to provide multiple names: MACHNAME.local/10.250.56.235
      // The second seems to be the IP address, which is what we want.
      String names[] = thisHostName.split("/");
      amHost = names[names.length - 1];
      appMasterTrackingUrl = trackingUrl.replace("<host>", amHost);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java

    public UnmanagedDrillbitModel( String endpoint ) {
      String parts[] = endpoint.split( ":" );
      if ( parts.length < 4 ) {
        // Should never occur, but better save than sorry.
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/kerberos/KerberosFactory.java`
#### Snippet
```java
    final String servicePrincipal = getServicePrincipal(properties);

    final String parts[] = KerberosUtil.splitPrincipalIntoParts(servicePrincipal);
    final String serviceName = parts[0];
    final String serviceHostName = parts[1];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `SALT`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  private void setMask(int key) {
    //8 odd numbers act as salt value to participate in the computation of the bucketMask.
    final int SALT[] = {0x47b6137b, 0x44974d91, 0x8824ad5b, 0xa2b7289d, 0x705495c7, 0x2df1424b, 0x9efc4947, 0x5c6bfb31};

    Arrays.fill(bucketMask, 0);
```

### CStyleArrayDeclaration
C-style array declaration of field `bucketMask`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  private int numBytes;

  private int bucketMask[] = new int[8];

  public BloomFilter(int numBytes, BufferAllocator bufferAllocator) {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ResourceInputStream.java`
#### Snippet
```java

  @Override
  public int read(long position, byte b[], int off, int len) {
    Preconditions.checkNotNull(b);
    Preconditions.checkPositionIndexes(off, off + len, b.length);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `registeredNames`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/FunctionsIterator.java`
#### Snippet
```java
   */
  private void populateFunctionMap(Map<String, FunctionInfo> functionMap, String jarName, DrillFuncHolder dfh) {
    String registeredNames[] = dfh.getRegisteredNames();
    String signature = dfh.getInputParameters();
    String returnType = dfh.getReturnType().getMinorType().toString();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `writers`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/BaseFieldFactory.java`
#### Snippet
```java
  public ElementParser buildOuterArrays(ObjectWriter writer, int dims,
      Function<ObjectWriter, ElementParser> innerCreator) {
    ObjectWriter writers[] = new ObjectWriter[dims];
    writers[0] = writer;
    for (int i = 1; i < dims; i++) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `types`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockGroupScanPOP.java`
#### Snippet
```java
    }
    MockScanEntry entry = readEntries.get(0);
    MockColumn types[] = new MockColumn[mockCols.size()];
    mockCols.toArray(types);
    MockScanEntry newEntry = new MockScanEntry(entry.records, true, 0, 1, types);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `cols`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ExtendedMockBatchReader.java`
#### Snippet
```java

    final Set<String> names = new HashSet<>();
    final MockColumn cols[] = config.getTypes();
    for (int i = 0; i < cols.length; i++) {
      final MockTableDef.MockColumn col = cols[i];
```

### CStyleArrayDeclaration
C-style array declaration of field `fields`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ExtendedMockBatchReader.java`
#### Snippet
```java

  private final MockScanEntry config;
  private final ColumnDef fields[];
  private ResultSetLoader loader;
  private RowSetLoader writer;
```

### CStyleArrayDeclaration
C-style array declaration of field `rowWidth`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(JoinBatchMemoryManager.class);

  private int rowWidth[];
  private RecordBatch recordBatch[];
  private Set<String> columnsToExclude;
```

### CStyleArrayDeclaration
C-style array declaration of field `recordBatch`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java

  private int rowWidth[];
  private RecordBatch recordBatch[];
  private Set<String> columnsToExclude;

```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
  private final static BufferAllocator allocator = RootAllocatorFactory.newRoot(drillConfig);

  public static void main(String args[]) throws Exception {
    final DumpCat dumpCat = new DumpCat();

```

### CStyleArrayDeclaration
C-style array declaration of parameter `args`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
public class QuerySubmitter {

  public static void main(String args[]) throws Exception {
    QuerySubmitter submitter = new QuerySubmitter();
    Options o = new Options();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/BootStrapContext.java`
#### Snippet
```java
        // service principal canonicalization
        final String principal = config.getString(ExecConstants.SERVICE_PRINCIPAL);
        final String parts[] = KerberosUtil.splitPrincipalIntoParts(principal);
        if (parts.length != 3) {
          throw new DrillbitStartupException(
```

### CStyleArrayDeclaration
C-style array declaration of local variable `values`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitFileColumnsHandler.java`
#### Snippet
```java
      return null;
    }
    Object values[] = new Object[columns.size()];
    for (int i = 0; i < columns.size(); i++) {
      values[i] = columns.get(i).resolve(fileInfo);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `leftExpr`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/MergeJoinBatch.java`
#### Snippet
```java
     * expected
     */
    LogicalExpression leftExpr[] = new LogicalExpression[conditions.size()];
    LogicalExpression rightExpr[] = new LogicalExpression[conditions.size()];
    IterOutcome lastLeftStatus = status.getLeftStatus();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rightExpr`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/MergeJoinBatch.java`
#### Snippet
```java
     */
    LogicalExpression leftExpr[] = new LogicalExpression[conditions.size()];
    LogicalExpression rightExpr[] = new LogicalExpression[conditions.size()];
    IterOutcome lastLeftStatus = status.getLeftStatus();
    IterOutcome lastRightStatus = status.getRightStatus();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `map`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ProjectedColumn.java`
#### Snippet
```java
    }
    final int max = maxIndex();
    final boolean map[] = new boolean[max+1];
    for (final Integer index : indexes) {
      map[index] = true;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `values`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitColumnResolver.java`
#### Snippet
```java

    public Object[] resolve(FileDescrip fileInfo) {
      Object values[] = new Object[columns.size()];
      for (int i = 0; i < values.length; i++) {
        values[i] = columns.get(i).resolve(fileInfo);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `reordered`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitColumnResolver.java`
#### Snippet
```java
    protected ParseResult(List<ImplicitColumnMarker> columns, TupleMetadata schema,
        boolean isMetadataScan) {
      ImplicitColumnMarker reordered[] = new ImplicitColumnMarker[columns.size()];
      for (ImplicitColumnMarker col : columns) {
        reordered[col.index()] = col;
```

### CStyleArrayDeclaration
C-style array declaration of field `writers`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/StaticBatchBuilder.java`
#### Snippet
```java

  public static class RepeatedBatchBuilder extends StaticBatchBuilder {
    private final ScalarWriter writers[];
    private final Object values[];

```

### CStyleArrayDeclaration
C-style array declaration of field `values`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/StaticBatchBuilder.java`
#### Snippet
```java
  public static class RepeatedBatchBuilder extends StaticBatchBuilder {
    private final ScalarWriter writers[];
    private final Object values[];

    public RepeatedBatchBuilder(ResultVectorCache vectorCache, TupleMetadata schema, Object values[]) {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `values`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/StaticBatchBuilder.java`
#### Snippet
```java
    private final Object values[];

    public RepeatedBatchBuilder(ResultVectorCache vectorCache, TupleMetadata schema, Object values[]) {
      super(vectorCache, schema);
      this.values = values;
```

### CStyleArrayDeclaration
C-style array declaration of field `vectorSources`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/OutputBatchBuilder.java`
#### Snippet
```java
  private final TupleMetadata outputSchema;
  private final List<BatchSource> sources;
  private final Object vectorSources[];
  private final VectorContainer outputContainer;
  private final List<MapVector> mapVectors = new ArrayList<>();
```

### CStyleArrayDeclaration
C-style array declaration of field `memberSources`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/OutputBatchBuilder.java`
#### Snippet
```java
    private final TupleMetadata mapSchema;
    private final List<MapSource> sourceMaps;
    private final Object memberSources[];
    private final List<MapVector> mapVectors = new ArrayList<>();

```

### CStyleArrayDeclaration
C-style array declaration of field `values`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ConstantColumnLoader.java`
#### Snippet
```java
  }

  private final String values[];
  private final List<? extends ConstantColumnSpec> constantCols;

```

### CStyleArrayDeclaration
C-style array declaration of local variable `readerProjectionMap`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/WildcardSchemaProjection.java`
#### Snippet
```java
    // columns provided by the reader.

    boolean readerProjectionMap[] = new boolean[readerSchema.size()];
    for (ColumnProjection col : scanProj.columns()) {
      if (col instanceof UnresolvedColumn) {
```

### CStyleArrayDeclaration
C-style array declaration of field `buffer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
  private static class WritableByteChannelImpl implements WritableByteChannel
  {
    private final byte buffer[];
    private OutputStream out;

```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    @Override
    public int read(byte b[], int off, int len) throws IOException {
      int n = in.read(b, off, len);
      if (n != -1) {
```

### CStyleArrayDeclaration
C-style array declaration of parameter `b`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    @Override
    public int read(byte b[]) throws IOException {
      int n = in.read(b);
      if (n != -1) {
```

### CStyleArrayDeclaration
C-style array declaration of field `buffer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
    private static final int TRANSFER_SIZE = 9 * 8 * 1024;

    private final byte buffer[];
    private FileSystem fs;

```

### CStyleArrayDeclaration
C-style array declaration of field `spilledBatchesCount`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  SpilledRecordBatch newIncoming; // when reading a spilled file - work like an "incoming"
  private Writer writers[]; // a vector writer for each spilled partition
  private int spilledBatchesCount[]; // count number of batches spilled, in each partition
  private String spillFiles[];
  private int originalPartition = -1; // the partition a secondary reads from
```

### CStyleArrayDeclaration
C-style array declaration of field `htables`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  private BufferAllocator allocator;

  private HashTable htables[];
  private ArrayList<BatchHolder> batchHolders[];
  private int outBatchIndex[];
```

### CStyleArrayDeclaration
C-style array declaration of field `writers`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  private SpillSet spillSet;
  SpilledRecordBatch newIncoming; // when reading a spilled file - work like an "incoming"
  private Writer writers[]; // a vector writer for each spilled partition
  private int spilledBatchesCount[]; // count number of batches spilled, in each partition
  private String spillFiles[];
```

### CStyleArrayDeclaration
C-style array declaration of field `outBatchIndex`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  private HashTable htables[];
  private ArrayList<BatchHolder> batchHolders[];
  private int outBatchIndex[];

  // For handling spilling
```

### CStyleArrayDeclaration
C-style array declaration of field `spillFiles`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  private Writer writers[]; // a vector writer for each spilled partition
  private int spilledBatchesCount[]; // count number of batches spilled, in each partition
  private String spillFiles[];
  private int originalPartition = -1; // the partition a secondary reads from

```

### CStyleArrayDeclaration
C-style array declaration of field `batchHolders`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java

  private HashTable htables[];
  private ArrayList<BatchHolder> batchHolders[];
  private int outBatchIndex[];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `variants`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/hyper/HyperReaderBuilder.java`
#### Snippet
```java
  private AbstractObjectReader buildUnion(VectorAccessor unionAccessor, ColumnMetadata metadata) {
    VariantMetadata unionSchema = metadata.variantSchema();
    final AbstractObjectReader variants[] = new AbstractObjectReader[MinorType.values().length];
    for (ColumnMetadata member : unionSchema.members()) {

```

### CStyleArrayDeclaration
C-style array declaration of local variable `variants`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/single/BaseWriterBuilder.java`
#### Snippet
```java
      throw new UnsupportedOperationException("Dummy variant writer not yet supported");
    }
    final AbstractObjectWriter variants[] = new AbstractObjectWriter[MinorType.values().length];
    final MetadataProvider mdProvider = descrip.childProvider();
    int i = 0;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `map`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/Qualifier.java`
#### Snippet
```java
    }
    final int max = maxIndex();
    final boolean map[] = new boolean[max+1];
    for (final Integer index : indexes) {
      map[index] = true;
```

### CStyleArrayDeclaration
C-style array declaration of field `typeMap`
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/schema/Schema.java`
#### Snippet
```java

  private final List<ColumnDto> columns = new ArrayList<>();
  private final MinorType typeMap[] = new MinorType[PcapTypes.values().length];
  private final boolean sessionizeTCPStreams;

```

### CStyleArrayDeclaration
C-style array declaration of local variable `rowKeyIds`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonRecordReader.java`
#### Snippet
```java

    while (recordCount < maxRecordsForThisBatch) {
      ByteBuffer rowKeyIds[] = rss.getRowKeyIdsToRead(batchSize);
      if (rowKeyIds == null) {
        break;
```

### CStyleArrayDeclaration
C-style array declaration of field `ioBuffer`
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
   */

  private byte ioBuffer[];

  protected BaseAllocator(
```

### CStyleArrayDeclaration
C-style array declaration of local variable `buf`
in `exec/memory/base/src/main/java/io/netty/buffer/DrillBuf.java`
#### Snippet
```java
   */
  public byte[] unsafeGetMemory(int srcOffset, int length) {
    byte buf[] = new byte[length];
    PlatformDependent.copyMemory(addr + srcOffset, buf, 0, length);
    return buf;
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseRegexParser.java`
#### Snippet
```java
   * </ol>
   */
  private static final Pattern SQL_LIKE_REGEX = Pattern.compile("([%_])|(\\[[^]]*\\])|([^%_\\[]+)");

  private static final String SQL_LIKE_ESCAPE_REGEX_STR = "(%s.?)|([%%_])|(\\[[^]]*\\])|([^%%_\\[%s]+)";
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

### RegExpRedundantEscape
Redundant character escape `\\+` in RegExp
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

### RegExpRedundantEscape
Redundant character escape `\\+` in RegExp
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageDirectoryProcessor.java`
#### Snippet
```java
                if (langProp.getPath().endsWith("/xml:lang")) {
                  String lang = langProp.getValue();
                  path = path.replaceFirst("\\[\\d+\\]$", "") +
                      (lang.equals("x-default") ? "" : "_" + lang);
                }
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/schema/GoogleSheetsDrillSchema.java`
#### Snippet
```java
public class GoogleSheetsDrillSchema extends AbstractSchema {
  private static final Logger logger = LoggerFactory.getLogger(GoogleSheetsDrillSchema.class);
  private static final Pattern TAB_PATTERN = Pattern.compile("^tab\\[(\\d+)\\]$");

  private final Map<String, DynamicDrillTable> activeTables = CaseInsensitiveMap.newHashMap();
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
  private static final String SHAPE_TYPE_FIELD_NAME = "shapeType";
  private static final String GEOM_FIELD_NAME = "geom";
  private static final String SRID_PATTERN_TEXT = "AUTHORITY\\[\"\\w+\"\\s*,\\s*\"*(\\d+)\"*\\]\\]$";
  private final FileDescrip file;
  private final Path hadoopShp;
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
  private static final String SHAPE_TYPE_FIELD_NAME = "shapeType";
  private static final String GEOM_FIELD_NAME = "geom";
  private static final String SRID_PATTERN_TEXT = "AUTHORITY\\[\"\\w+\"\\s*,\\s*\"*(\\d+)\"*\\]\\]$";
  private final FileDescrip file;
  private final Path hadoopShp;
```

## RuleId[id=FinalMethodInFinalClass]
### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillDatabaseMetaDataImpl.java`
#### Snippet
```java
    }

    public static final Set<SQLConvertSupport> toSQLConvertSupport(Iterable<ConvertSupport> convertSupportIterable) {
      ImmutableSet.Builder<SQLConvertSupport> sqlConvertSupportSet = ImmutableSet.builder();
      for(ConvertSupport convertSupport: convertSupportIterable) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
public final class MurmurHash3 extends DrillHash{

   public static final long fmix64(long k) {
    k ^= k >>> 33;
    k *= 0xff51afd7ed558ccdL;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    protected final int match(int start, int end, DrillBuf drillBuf) {
      return 1;
    }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    protected final int match(int start, int end, DrillBuf drillBuf) {
      final int lengthToProcess = end - start - 1;

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    protected final int match(int start, int end, DrillBuf drillBuf) {
      final int lengthToProcess = end - start - patternLength + 1;
      int patternIndex          = 0;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    protected final int match(int start, int end, DrillBuf drillBuf) {
      final int lengthToProcess = end - start - 2;

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    protected final int match(int start, int end, DrillBuf drillBuf) {
      final int lengthToProcess = end - start;

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
   * @return current line count
   */
  public final long lineCount() {
    return lineCount;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
   * @throws IOException for input file read errors
   */
  final void start() throws IOException {
    lineCount = 0;
    if(startPos > 0){
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
  }

  public final long charCount() {
    return charCount + bufferPtr;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
   * @throws IOException for input file read errors
   */
  public final byte nextCharNoNewLineCheck() throws IOException {

    if (length == -1) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
   * @throws IOException for input file read errors
   */
  public final byte nextChar() throws IOException {
    byte byteChar = nextCharNoNewLineCheck();
    int bufferPtrTemp = bufferPtr - 1;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
   * of lines
   */
  public final void skipLines(int lines) throws IOException {
    if (lines < 1) {
      return;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java
   * @throws IOException for input file read errors
   */
  public final void start() throws IOException {
    context.stop(false);
    input.start();
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java
   * @throws IOException for input file read errors
   */
  public final boolean parseNext() throws IOException {
    try {
      while (!context.isStopped()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valsToReadWithinPage) {
    final int[] valueLengths = entry.getValuesLength();
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableDictionaryReader.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  final VarLenColumnBulkEntry getEntry(int valuesToRead) {
    assert valuesToRead > 0;

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  private final void setBufferedPagePayload() {

    if (parentInst.pageReader.hasPage() && oprReadState.numPageFieldsProcessed < parentInst.pageReader.pageValueCount) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  final ColumnPrecisionInfo getColumnPrecisionInfo() {
    return columnPrecInfo;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  public final void done() {
    // Update the page reader state so that a future call to this method resumes
    // where we left off.
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  private final void setValuesReadersOnNewPage() {
    PageReader pageReader = parentInst.pageReader;
    if (pageReader.pageValueCount > 0) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  public final void remove() {
    throw new UnsupportedOperationException();
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  final int getReadBatchFields() {
    return oprReadState.batchFieldIndex;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  public final int getStartIndex() {
    return oprReadState.batchFieldIndex;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  public final VarLenBulkEntry next() {
    final int remaining = getRemainingRecords();
    final VarLenColumnBulkEntry result = buffPagePayload.getEntry(remaining);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
   * @throws IOException
   */
  private final void guessColumnPrecision(ColumnPrecisionInfo columnPrecInfo) throws IOException {
    columnPrecInfo.columnPrecisionType = ColumnPrecisionType.DT_PRECISION_IS_VARIABLE;

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenFixedEntryReader.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  final VarLenColumnBulkEntry getEntry(int valuesToRead) {
    load(true); // load new data to process

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  final void set(PageDataInfo pageInfoInput, boolean clear) {
    pageInfo.pageData = pageInfoInput.pageData;
    pageInfo.pageDataOff = pageInfoInput.pageDataOff;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  final VarLenColumnBulkEntry getEntry(int valuesToRead) {
    Preconditions.checkArgument(valuesToRead > 0, "Number of values to read [%s] should be greater than zero", valuesToRead);

```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getFixedEntry(int valuesToRead) {
    if (pageInfo.definitionLevels.hasDefinitionLevels()) {
      return nullableFixedReader.getEntry(valuesToRead);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getVarLenEntry(int valuesToRead) {
    // Let start with non-dictionary encoding as it is predominant
    if (!pageInfo.encodedValueReader.isDefined()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valsToReadWithinPage) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
  /** {@inheritDoc} */
  @Override
  final VarLenColumnBulkEntry getEntry(int valuesToRead) {
    // Bulk processing is in effect for smaller precisions
    if (bulkProcess()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthColumnReaders.java`
#### Snippet
```java

    @Override
    public final boolean setSafe(int index, DrillBuf value, int start, int length) {
      if (index >= varBinaryVector.getValueCapacity()) {
        return false;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {

    load(true); // load new data to process
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valuesToRead) {

    if (remainingPageData() < 4) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinStatus.java`
#### Snippet
```java
  public IterOutcome getRightStatus() { return right.getLastOutcome(); }

  public final int getOutPosition() {
    return outputPosition;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinStatus.java`
#### Snippet
```java
  }

  public final void resetOutputPos() {
    outputPosition = 0;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinStatus.java`
#### Snippet
```java
  }

  public final void incOutputPos() {
    ++outputPosition;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinStatus.java`
#### Snippet
```java
  }

  public final void setTargetOutputRowCount(int outputRowCount) {
    this.targetOutputRowCount = outputRowCount;
  }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinStatus.java`
#### Snippet
```java
  }

  public final boolean isOutgoingBatchFull() {
    Preconditions.checkArgument(outputPosition <= targetOutputRowCount);
    return outputPosition >= targetOutputRowCount;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_ServerPreparedStatementState_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_FragmentHandle_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.ServerPreparedStatementState)
        org.apache.drill.exec.proto.ExecProtos.ServerPreparedStatementStateOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_ServerPreparedStatementState_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.FragmentHandle)
        org.apache.drill.exec.proto.ExecProtos.FragmentHandleOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_FragmentHandle_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.CompleteRpcMessage)
        org.apache.drill.exec.proto.GeneralRPCProtos.CompleteRpcMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_CompleteRpcMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_CompleteRpcMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.Ack)
        org.apache.drill.exec.proto.GeneralRPCProtos.AckOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_Ack_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_RpcHeader_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.RpcHeader)
        org.apache.drill.exec.proto.GeneralRPCProtos.RpcHeaderOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_RpcHeader_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_Ack_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillbitEndpoint_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.DrillServiceInstance)
        org.apache.drill.exec.proto.CoordinationProtos.DrillServiceInstanceOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillServiceInstance_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.Roles)
        org.apache.drill.exec.proto.CoordinationProtos.RolesOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_Roles_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillServiceInstance_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_Roles_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.DrillbitEndpoint)
        org.apache.drill.exec.proto.CoordinationProtos.DrillbitEndpointOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillbitEndpoint_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:common.MajorType)
        org.apache.drill.common.types.TypeProtos.MajorTypeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.common.types.TypeProtos.internal_static_common_MajorType_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.common.types.TypeProtos.internal_static_common_MajorType_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.BitServerHandshake)
        org.apache.drill.exec.proto.BitData.BitServerHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitServerHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.BitClientHandshake)
        org.apache.drill.exec.proto.BitData.BitClientHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitClientHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.FragmentRecordBatch)
        org.apache.drill.exec.proto.BitData.FragmentRecordBatchOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_FragmentRecordBatch_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_AckWithCredit_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.RuntimeFilterBDef)
        org.apache.drill.exec.proto.BitData.RuntimeFilterBDefOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_RuntimeFilterBDef_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitServerHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_RuntimeFilterBDef_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.AckWithCredit)
        org.apache.drill.exec.proto.BitData.AckWithCreditOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_AckWithCredit_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitClientHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_FragmentRecordBatch_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.CustomMessage)
        org.apache.drill.exec.proto.BitControl.CustomMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_CustomMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_InitializeFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitControlHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FragmentStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.PlanFragment)
        org.apache.drill.exec.proto.BitControl.PlanFragmentOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_PlanFragment_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.FragmentStatus)
        org.apache.drill.exec.proto.BitControl.FragmentStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FragmentStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.BitStatus)
        org.apache.drill.exec.proto.BitControl.BitStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_QueryContextInformation_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_PlanFragment_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FinishedReceiver_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.BitControlHandshake)
        org.apache.drill.exec.proto.BitControl.BitControlHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitControlHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.QueryContextInformation)
        org.apache.drill.exec.proto.BitControl.QueryContextInformationOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_QueryContextInformation_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_Collector_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.InitializeFragments)
        org.apache.drill.exec.proto.BitControl.InitializeFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_InitializeFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_WorkQueueStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.Collector)
        org.apache.drill.exec.proto.BitControl.CollectorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_Collector_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.FinishedReceiver)
        org.apache.drill.exec.proto.BitControl.FinishedReceiverOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FinishedReceiver_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.WorkQueueStatus)
        org.apache.drill.exec.proto.BitControl.WorkQueueStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_WorkQueueStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_CustomMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_UserCredentials_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Registry_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MajorFragmentProfile)
        org.apache.drill.exec.proto.UserBitShared.MajorFragmentProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MajorFragmentProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryResult)
        org.apache.drill.exec.proto.UserBitShared.QueryResultOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryResult_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryResult_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NodeStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.SerializedField)
        org.apache.drill.exec.proto.UserBitShared.SerializedFieldOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SerializedField_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MetricValue)
        org.apache.drill.exec.proto.UserBitShared.MetricValueOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MetricValue_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MajorFragmentProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.Registry)
        org.apache.drill.exec.proto.UserBitShared.RegistryOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Registry_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.StackTraceElementWrapper)
        org.apache.drill.exec.proto.UserBitShared.StackTraceElementWrapperOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StackTraceElementWrapper_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_OperatorProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StreamProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryProfile)
        org.apache.drill.exec.proto.UserBitShared.QueryProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.UserCredentials)
        org.apache.drill.exec.proto.UserBitShared.UserCredentialsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_UserCredentials_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.OperatorProfile)
        org.apache.drill.exec.proto.UserBitShared.OperatorProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_OperatorProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NamePart_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Jar_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.RecordBatchDef)
        org.apache.drill.exec.proto.UserBitShared.RecordBatchDefOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_RecordBatchDef_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_RecordBatchDef_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.SaslMessage)
        org.apache.drill.exec.proto.UserBitShared.SaslMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SaslMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StackTraceElementWrapper_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SaslMessage_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.NamePart)
        org.apache.drill.exec.proto.UserBitShared.NamePartOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NamePart_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.DrillPBError)
        org.apache.drill.exec.proto.UserBitShared.DrillPBErrorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_DrillPBError_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.ExceptionWrapper)
        org.apache.drill.exec.proto.UserBitShared.ExceptionWrapperOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ExceptionWrapper_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.StreamProfile)
        org.apache.drill.exec.proto.UserBitShared.StreamProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StreamProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryInfo)
        org.apache.drill.exec.proto.UserBitShared.QueryInfoOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryInfo_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryInfo_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_DrillPBError_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SerializedField_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MinorFragmentProfile)
        org.apache.drill.exec.proto.UserBitShared.MinorFragmentProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MinorFragmentProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ParsingError_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ExceptionWrapper_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MinorFragmentProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.NodeStatus)
        org.apache.drill.exec.proto.UserBitShared.NodeStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NodeStatus_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MetricValue_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryProfile_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.ParsingError)
        org.apache.drill.exec.proto.UserBitShared.ParsingErrorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ParsingError_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryId)
        org.apache.drill.exec.proto.UserBitShared.QueryIdOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryId_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryData_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.Jar)
        org.apache.drill.exec.proto.UserBitShared.JarOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Jar_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryId_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryData)
        org.apache.drill.exec.proto.UserBitShared.QueryDataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryData_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.Property)
        org.apache.drill.exec.proto.UserProtos.PropertyOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_Property_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatementHandle_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetTablesResp)
        org.apache.drill.exec.proto.UserProtos.GetTablesRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetQueryPlanFragments)
        org.apache.drill.exec.proto.UserProtos.GetQueryPlanFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetQueryPlanFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (hasServerMeta()) {
          if (!getServerMeta().isInitialized()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.PreparedStatementHandle)
        org.apache.drill.exec.proto.UserProtos.PreparedStatementHandleOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatementHandle_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.SchemaMetadata)
        org.apache.drill.exec.proto.UserProtos.SchemaMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_SchemaMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_QueryPlanFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatement_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetTablesReq)
        org.apache.drill.exec.proto.UserProtos.GetTablesReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.UserToBitHandshake)
        org.apache.drill.exec.proto.UserProtos.UserToBitHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserToBitHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetColumnsReq)
        org.apache.drill.exec.proto.UserProtos.GetColumnsReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RequestResults)
        org.apache.drill.exec.proto.UserProtos.RequestResultsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RequestResults_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_SchemaMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ColumnMetadata)
        org.apache.drill.exec.proto.UserProtos.ColumnMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ColumnMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_Property_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ResultColumnMetadata)
        org.apache.drill.exec.proto.UserProtos.ResultColumnMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ResultColumnMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CatalogMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasKey()) {
          return false;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RunQuery)
        org.apache.drill.exec.proto.UserProtos.RunQueryOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RunQuery_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasStatus()) {
          return false;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetSchemasReq)
        org.apache.drill.exec.proto.UserProtos.GetSchemasReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        for (int i = 0; i < getConvertSupportCount(); i++) {
          if (!getConvertSupport(i).isInitialized()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.TableMetadata)
        org.apache.drill.exec.proto.UserProtos.TableMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_TableMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetCatalogsReq)
        org.apache.drill.exec.proto.UserProtos.GetCatalogsReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RequestResults_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.LikeFilter)
        org.apache.drill.exec.proto.UserProtos.LikeFilterOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_LikeFilter_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetSchemasResp)
        org.apache.drill.exec.proto.UserProtos.GetSchemasRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetCatalogsResp)
        org.apache.drill.exec.proto.UserProtos.GetCatalogsRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        for (int i = 0; i < getPropertiesCount(); i++) {
          if (!getProperties(i).isInitialized()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.UserProperties)
        org.apache.drill.exec.proto.UserProtos.UserPropertiesOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserProperties_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ServerMeta_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RpcEndpointInfos)
        org.apache.drill.exec.proto.UserProtos.RpcEndpointInfosOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RpcEndpointInfos_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ResultColumnMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ServerMeta)
        org.apache.drill.exec.proto.UserProtos.ServerMetaOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ServerMeta_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RunQuery_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserToBitHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_BitToUserHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetQueryPlanFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasFrom()) {
          return false;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasQuery()) {
          return false;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserProperties_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.QueryPlanFragments)
        org.apache.drill.exec.proto.UserProtos.QueryPlanFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_QueryPlanFragments_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ConvertSupport_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_LikeFilter_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.PreparedStatement)
        org.apache.drill.exec.proto.UserProtos.PreparedStatementOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatement_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CreatePreparedStatementResp)
        org.apache.drill.exec.proto.UserProtos.CreatePreparedStatementRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CatalogMetadata)
        org.apache.drill.exec.proto.UserProtos.CatalogMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CatalogMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.BitToUserHandshake)
        org.apache.drill.exec.proto.UserProtos.BitToUserHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_BitToUserHandshake_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ConvertSupport)
        org.apache.drill.exec.proto.UserProtos.ConvertSupportOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ConvertSupport_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RpcEndpointInfos_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        if (hasProperties()) {
          if (!getProperties().isInitialized()) {
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetServerMetaResp)
        org.apache.drill.exec.proto.UserProtos.GetServerMetaRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetColumnsResp)
        org.apache.drill.exec.proto.UserProtos.GetColumnsRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsResp_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetServerMetaReq)
        org.apache.drill.exec.proto.UserProtos.GetServerMetaReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ColumnMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CreatePreparedStatementReq)
        org.apache.drill.exec.proto.UserProtos.CreatePreparedStatementReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementReq_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_TableMetadata_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

## RuleId[id=ForLoopReplaceableByWhile]
### ForLoopReplaceableByWhile
`for` loop statement may be replace by 'while' loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        //then, we may find a match, and do replace.
        int i = text.start;
        for (; i <= text.end - fromL; ) {
          int j = from.start;
          for (; j < from.end; j++) {
```

### ForLoopReplaceableByWhile
`for` loop statement may be replace by 'while' loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableEntryReader.java`
#### Snippet
```java
    pageInfo.definitionLevels.readFirstIntegerIfNeeded();

    for (; numValues < readBatch; ) {
      // Non-null entry
      if (pageInfo.definitionLevels.readCurrInteger() == 1) {
```

### ForLoopReplaceableByWhile
`for` loop statement may be replace by 'while' loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
    int srcPos = 0;

    for (; numValues < readBatch; ) {
      if (srcPos > srcLen -4) {
        break;
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
  }

  private static final int memcmp(final long laddr, int lStart, int lEnd, final long raddr, int rStart, final int rEnd) {
    int lLen = lEnd - lStart;
    int rLen = rEnd - rStart;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
  }

  private static final int memEqual(final long laddr, int lStart, int lEnd, final long raddr, int rStart,
      final int rEnd) {

```

### FinalPrivateMethod
'private' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java


  private static final int memcmp(final long laddr, int lStart, int lEnd, final byte[] right, int rStart, final int rEnd) {
    int lLen = lEnd - lStart;
    int rLen = rEnd - rStart;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseVarWidthWriter.java`
#### Snippet
```java
  }

  private final int writeOffset(final int width) {

    // This is performance critical code; every operation counts.
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java
  }

  private static final void appendType(MajorType mt, StringBuilder sb) {
    sb.append(mt.getMinorType().name());
    sb.append(":");
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
  private static final int MIN_FILE_SIZE = ParquetFileWriter.MAGIC.length + FOOTER_METADATA_SIZE;

  private static final void readFully(FSDataInputStream stream, long start, byte[] output, int offset, int len) throws IOException{
    int bytesRead = 0;
    while(bytesRead > -1 && bytesRead < len){
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valsToReadWithinPage) {
    final int[] valueLengths = entry.getValuesLength();
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java
  }

  private static final void vlCopyLELongNoPadding(byte[] src, int srcIndex, byte[] dest, int destIndex, int length) {
    if (length == 1) {
      dest[destIndex] = src[srcIndex];
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java
  }

  private static final void vlCopyGTLongNoPadding(byte[] src, int srcIndex, byte[] dest, int destIndex, int length) {
    final int numLongEntries = length >> 3;
    final int remaining      = length & 0x7;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  private final void setBufferedPagePayload() {

    if (parentInst.pageReader.hasPage() && oprReadState.numPageFieldsProcessed < parentInst.pageReader.pageValueCount) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
  }

  private final void setValuesReadersOnNewPage() {
    PageReader pageReader = parentInst.pageReader;
    if (pageReader.pageValueCount > 0) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
   * @throws IOException
   */
  private final void guessColumnPrecision(ColumnPrecisionInfo columnPrecInfo) throws IOException {
    columnPrecInfo.columnPrecisionType = ColumnPrecisionType.DT_PRECISION_IS_VARIABLE;

```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java
  }

  private final void processPagesOrig(long recordsToReadInThisPass) throws IOException {
    readStartInBytes = 0;
    readLength = 0;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java
  }

  private final void processPagesBulk(long recordsToReadInThisPass) throws IOException {
    readStartInBytes = 0;
    readLength = 0;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getFixedEntry(int valuesToRead) {
    if (pageInfo.definitionLevels.hasDefinitionLevels()) {
      return nullableFixedReader.getEntry(valuesToRead);
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getVarLenEntry(int valuesToRead) {
    // Let start with non-dictionary encoding as it is predominant
    if (!pageInfo.encodedValueReader.isDefined()) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valsToReadWithinPage) {
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntryBulk(int valuesToRead) {

    load(true); // load new data to process
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
  }

  private final VarLenColumnBulkEntry getEntrySingle(int valuesToRead) {

    if (remainingPageData() < 4) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
  // Returns output container status after insertion of the given record. Caller must check the return value if it
  // plans to inserts more record into outgoing container.
  private final boolean outputToBatchPrev(InternalBatch b1, int inIndex, int outIndex) {
    assert outputCount < maxOutputRows :
        "Outgoing RecordBatch is not flushed. It reached its max capacity in the last update";
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
  }

  private final void incIndex() {
    underlyingIndex++;
    if (underlyingIndex >= incoming.getRecordCount()) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
   * @return outcome
   */
  private final AggOutcome setOkAndReturn(IterOutcome seenOutcome) {
    IterOutcome outcomeToReturn;
    firstBatchForDataSet = false;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
  }

  private final void resetIndex() {
    underlyingIndex = 0;
    currentIndex = Integer.MAX_VALUE;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
   * @return outcome
   */
  private final AggOutcome setOkAndReturnEmit() {
    IterOutcome outcomeToReturn;
    firstBatchForDataSet = true;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
  // Returns output container status after insertion of the given record. Caller must check the return value if it
  // plans to insert more records into outgoing container.
  private final boolean outputToBatch(int inIndex) {
    assert outputCount < maxOutputRows :
        "Outgoing RecordBatch is not flushed. It reached its max capacity in the last update";
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  }

  private final void incIndex() {
    underlyingIndex++;
    if (underlyingIndex >= currentBatchRecordCount) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
  }

  private final void resetIndex() {
    underlyingIndex = -1; // will become 0 in incIndex()
    incIndex();
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/rangepartitioner/RangePartitionRecordBatch.java`
#### Snippet
```java
   * @return the number of records projected
   */
  private final int projectRecords(int recordCount, int firstOutputIndex) {
    int countN = recordCount;
    int counter = 0;
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ResultSetCopierImpl.java`
#### Snippet
```java
  }

  private final void copyColumns() {
    rowWriter.start();
    for (CopyPair pair : projection) {
```

### FinalPrivateMethod
'private' method declared `final`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
        STATE end() throws IOException { return error(); }

        private final STATE error() throws IOException {
            throw new IOException("The file being written is in an invalid state. Probably caused by an error thrown previously. Current state: " + this.name());
        }
```

## RuleId[id=Deprecation]
### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java
    }

    public void get(int index, ObjectHolder holder){
      holder.obj = getObject(index);
    }
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java
    }

    public boolean setSafe(int index, ObjectHolder holder){
      set(index, holder);
      return true;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java
    }

    protected void set(int index, ObjectHolder holder) {
      set(index, holder.obj);
    }
```

### Deprecation
'MAX_ALLOCATION_SIZE' is deprecated
in `exec/vector/src/main/java/org/apache/drill/exec/vector/BitVector.java`
#### Snippet
```java

  private void allocateBytes(final long size) {
    if (size > MAX_ALLOCATION_SIZE) {
      throw new OversizedAllocationException("Requested amount of memory is more than max allowed allocation size");
    }
```

### Deprecation
'MAX_ALLOCATION_SIZE' is deprecated
in `exec/vector/src/main/java/org/apache/drill/exec/vector/BitVector.java`
#### Snippet
```java
    }

    if (newAllocationSize > MAX_ALLOCATION_SIZE) {
      throw new OversizedAllocationException("Requested amount of memory is more than max allowed allocation size");
    }
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidRecordReader.java`
#### Snippet
```java

  private JsonReader jsonReader;
  private VectorContainerWriter writer;

  private final FragmentContext fragmentContext;
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidRecordReader.java`
#### Snippet
```java
  @Override
  public void setup(OperatorContext context, OutputMutator output) {
    this.writer = new VectorContainerWriter(output);

    this.jsonReader =
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidRecordReader.java`
#### Snippet
```java

    this.jsonReader =
      new JsonReader.Builder(fragmentContext.getManagedBuffer())
        .schemaPathColumns(ImmutableList.copyOf(getColumns()))
        .skipOuterList(true)
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidRecordReader.java`
#### Snippet
```java
  private int maxRecordsToRead = -1;

  private JsonReader jsonReader;
  private VectorContainerWriter writer;

```

### Deprecation
'order(java.nio.ByteOrder)' is deprecated
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/CompareFunctionsProcessor.java`
#### Snippet
```java
  protected static ByteBuf newByteBuf(int size, boolean bigEndian) {
    return Unpooled.wrappedBuffer(new byte[size])
        .order(bigEndian ? ByteOrder.BIG_ENDIAN : ByteOrder.LITTLE_ENDIAN)
        .writerIndex(0);
  }
```

### Deprecation
'xmlDataLevel()' is deprecated
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpXMLBatchReader.java`
#### Snippet
```java
    // TODO Remove the XMLDataLevel parameter.  For now, check both
    if (subScan.tableSpec().connectionConfig().xmlOptions() == null) {
      this.dataLevel = subScan.tableSpec().connectionConfig().xmlDataLevel();
    } else {
      this.dataLevel = subScan.tableSpec().connectionConfig().xmlOptions().getDataLevel();
```

### Deprecation
'xmlDataLevel()' is deprecated
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpXMLBatchReader.java`
#### Snippet
```java

    if (subScan.tableSpec().connectionConfig().xmlOptions() == null) {
      this.dataLevel = subScan.tableSpec().connectionConfig().xmlDataLevel();
    } else {
      this.dataLevel = subScan.tableSpec().connectionConfig().xmlOptions().getDataLevel();
```

### Deprecation
'limitReached(int)' is deprecated
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpCSVBatchReader.java`
#### Snippet
```java
  public boolean next() {
    while (!rowWriter.isFull()) {
      if (rowWriter.limitReached(maxRecords)) {
        return false;
      }
```

### Deprecation
'build()' is deprecated
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/udfs/HttpHelperFunctions.java`
#### Snippet
```java
          .message("Error while reading JSON. ")
          .addContext(e.getMessage())
          .build();
      }
    }
```

### Deprecation
'build()' is deprecated
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/udfs/HttpHelperFunctions.java`
#### Snippet
```java
          .message("Error while reading JSON. ")
          .addContext(e.getMessage())
          .build();
      }
    }
```

### Deprecation
'HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS' is deprecated
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
        // TODO: Remove implicit using of convert_fromTIMESTAMP_IMPALA function
        // once "store.parquet.reader.int96_as_timestamp" will be true by default
        if (options.getBoolean(ExecConstants.HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS) ||
            options.getBoolean(ExecConstants.HIVE_OPTIMIZE_PARQUET_SCAN_WITH_NATIVE_READER)) {
          ruleBuilder.add(ConvertHiveParquetScanToDrillParquetScan.INSTANCE);
```

### Deprecation
'put(java.lang.String, com.fasterxml.jackson.databind.JsonNode)' is deprecated
in `logical/src/main/java/org/apache/drill/common/util/AbstractDynamicBean.java`
#### Snippet
```java
  @JsonAnySetter
  public void _anySetter(String name, JsonNode value){
    objectNode.put(name, value);
  }

```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/readers/HiveDefaultRecordReader.java`
#### Snippet
```java
   * Manages all writes to value vectors received using OutputMutator
   */
  protected VectorContainerWriter outputWriter;

  /**
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/readers/HiveDefaultRecordReader.java`
#### Snippet
```java
      this.selectedStructFieldRefs = new StructField[selectedColumnNames.size()];
      this.columnValueWriters = new HiveValueWriter[selectedColumnNames.size()];
      this.outputWriter = new VectorContainerWriter(output, /*enabled union*/ false);
      HiveValueWriterFactory hiveColumnValueWriterFactory = new HiveValueWriterFactory(drillBuf, outputWriter.getWriter());
      for (int refIdx = 0; refIdx < selectedStructFieldRefs.length; refIdx++) {
```

### Deprecation
'DrillSqlOperator(java.lang.String, int, boolean, boolean)' is deprecated
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/sql/logical/ConvertHiveParquetScanToDrillParquetScan.java`
#### Snippet
```java

  private static final DrillSqlOperator INT96_TO_TIMESTAMP =
      new DrillSqlOperator("convert_fromTIMESTAMP_IMPALA", 1, true, false);

  private static final DrillSqlOperator RTRIM = new DrillSqlOperator("RTRIM", 1, true, false);
```

### Deprecation
'DrillSqlOperator(java.lang.String, int, boolean, boolean)' is deprecated
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/sql/logical/ConvertHiveParquetScanToDrillParquetScan.java`
#### Snippet
```java
      new DrillSqlOperator("convert_fromTIMESTAMP_IMPALA", 1, true, false);

  private static final DrillSqlOperator RTRIM = new DrillSqlOperator("RTRIM", 1, true, false);

  private ConvertHiveParquetScanToDrillParquetScan() {
```

### Deprecation
'QUOTE_FIELD_NAMES' is deprecated
in `logical/src/main/java/org/apache/drill/common/config/LogicalPlanPersistence.java`
#### Snippet
```java
    mapper.enable(SerializationFeature.INDENT_OUTPUT);
    mapper.configure(Feature.ALLOW_UNQUOTED_FIELD_NAMES, true);
    mapper.configure(JsonGenerator.Feature.QUOTE_FIELD_NAMES, true);
    mapper.configure(Feature.ALLOW_COMMENTS, true);
    mapper.setFilterProvider(new SimpleFilterProvider().setFailOnUnknownId(false));
```

### Deprecation
'_parseString(com.fasterxml.jackson.core.JsonParser, com.fasterxml.jackson.databind.DeserializationContext)' is deprecated
in `logical/src/main/java/org/apache/drill/common/expression/FieldReference.java`
#### Snippet
```java
    public FieldReference deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      String ref = this._parseString(jp, ctxt);
      ref = ref.replace("`", "");
      return new FieldReference(ref, ExpressionPosition.UNKNOWN);
```

### Deprecation
'_parseString(com.fasterxml.jackson.core.JsonParser, com.fasterxml.jackson.databind.DeserializationContext)' is deprecated
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
    public DataType deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      return getDataType(this._parseString(jp, ctxt));
    }

```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataClient.java`
#### Snippet
```java
        RpcType.HANDSHAKE,
        BitServerHandshake.class,
        BitServerHandshake.PARSER);

    this.remoteEndpoint = remoteEndpoint;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataServerRequestHandler.java`
#### Snippet
```java

  private void handleRecordBatchRequest(ByteBuf pBody, ByteBuf dBody, ResponseSender sender) throws RpcException {
    final FragmentRecordBatch fragmentBatch = RpcBus.get(pBody, FragmentRecordBatch.PARSER);
    final AckSender ack = new AckSender(sender);

```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataServerRequestHandler.java`
#### Snippet
```java

  private void handleRuntimeFilterRequest(ByteBuf pBody, ByteBuf dBody, ResponseSender sender) throws RpcException {
    BitData.RuntimeFilterBDef runtimeFilterBDef = RpcBus.get(pBody, BitData.RuntimeFilterBDef.PARSER);
    if (dBody == null) {
      return;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataServer.java`
#### Snippet
```java
  @Override
  protected ServerHandshakeHandler<BitClientHandshake> getHandshakeHandler(final DataServerConnection connection) {
    return new ServerHandshakeHandler<BitClientHandshake>(RpcType.HANDSHAKE, BitClientHandshake.PARSER) {

      @Override
```

### Deprecation
'QUOTE_FIELD_NAMES' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/InboundImpersonationManager.java`
#### Snippet
```java

  static {
    impersonationPolicyMapper.configure(JsonGenerator.Feature.QUOTE_FIELD_NAMES, false);
    impersonationPolicyMapper.configure(JsonParser.Feature.ALLOW_UNQUOTED_FIELD_NAMES, true);
  }
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
      logger.debug("Received query to run.  Returning query handle.");
      try {
        final RunQuery query = RunQuery.PARSER.parseFrom(new ByteBufInputStream(pBody));
        final QueryId queryId = worker.submitWork(connection, query);
        responseSender.send(new Response(RpcType.QUERY_HANDLE, queryId));
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.CANCEL_QUERY_VALUE:
      try {
        final QueryId queryId = QueryId.PARSER.parseFrom(new ByteBufInputStream(pBody));
        final Ack ack = worker.cancelQuery(queryId);
        responseSender.send(new Response(RpcType.ACK, ack));
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.RESUME_PAUSED_QUERY_VALUE:
      try {
        final QueryId queryId = QueryId.PARSER.parseFrom(new ByteBufInputStream(pBody));
        final Ack ack = worker.resumeQuery(queryId);
        responseSender.send(new Response(RpcType.ACK, ack));
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.GET_QUERY_PLAN_FRAGMENTS_VALUE:
      try {
        final GetQueryPlanFragments req = GetQueryPlanFragments.PARSER.parseFrom(new ByteBufInputStream(pBody));
        responseSender.send(new Response(RpcType.QUERY_PLAN_FRAGMENTS, worker.getQueryPlan(connection, req)));
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.GET_CATALOGS_VALUE:
      try {
        final GetCatalogsReq req = GetCatalogsReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitCatalogMetadataWork(connection.getSession(), req, responseSender);
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.GET_SCHEMAS_VALUE:
      try {
        final GetSchemasReq req = GetSchemasReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitSchemasMetadataWork(connection.getSession(), req, responseSender);
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.GET_TABLES_VALUE:
      try {
        final GetTablesReq req = GetTablesReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitTablesMetadataWork(connection.getSession(), req, responseSender);
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
    case RpcType.GET_COLUMNS_VALUE:
      try {
        final GetColumnsReq req = GetColumnsReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitColumnsMetadataWork(connection.getSession(), req, responseSender);
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
      try {
        final CreatePreparedStatementReq req =
            CreatePreparedStatementReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitPreparedStatementWork(connection, req, responseSender);
        break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServerRequestHandler.java`
#### Snippet
```java
      try {
        final GetServerMetaReq req =
            GetServerMetaReq.PARSER.parseFrom(new ByteBufInputStream(pBody));
        worker.submitServerMetadataWork(connection.getSession(), req, responseSender);
        break;
```

### Deprecation
'UTF_8' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/HtpasswdFileUserAuthenticator.java`
#### Snippet
```java
      return hash.equals(Md5Crypt.apr1Crypt(password, hash));
    } else if (hash.startsWith("$1$")) {
      return hash.equals(Md5Crypt.md5Crypt(password.getBytes(Charsets.UTF_8), hash));
    } else if (hash.startsWith("{SHA}")) {
      return hash.substring(5).equals(Base64.getEncoder().encodeToString(DigestUtils.sha1(password)));
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlClient.java`
#### Snippet
```java
        RpcType.HANDSHAKE,
        BitControlHandshake.class,
        BitControlHandshake.PARSER);
    this.config = config;
    this.remoteEndpoint = remoteEndpoint;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/QueryResultHandler.java`
#### Snippet
```java
  public void batchArrived(ConnectionThrottle throttle,
                            ByteBuf pBody, ByteBuf dBody) throws RpcException {
    QueryData queryData = RpcBus.get(pBody, QueryData.PARSER);
    // Current batch coming in.
    DrillBuf drillBuf = (DrillBuf) dBody;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/QueryResultHandler.java`
#### Snippet
```java
   */
  public void resultArrived(ByteBuf pBody) throws RpcException {
    QueryResult queryResult = RpcBus.get(pBody, QueryResult.PARSER);

    QueryId queryId = queryResult.getQueryId();
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java
      DrillbitEndpoint endpoint) throws NonTransientRpcException {
    super(UserRpcConfig.getMapping(config, eventExecutor), allocator.getAsByteBufAllocator(),
        eventLoopGroup, RpcType.HANDSHAKE, BitToUserHandshake.class, BitToUserHandshake.PARSER);
    this.endpoint = endpoint; // save the endpoint; it might be needed by SSL init.
    this.clientName = clientName;
```

### Deprecation
'valueOf(int)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java
    if (properties.containsKey(DrillProperties.TEST_SASL_LEVEL)) {
      hsBuilder.setSaslSupport(
          SaslSupport.valueOf(Integer.parseInt(properties.getProperty(DrillProperties.TEST_SASL_LEVEL))));
    }

```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlServer.java`
#### Snippet
```java
  @Override
  protected ServerHandshakeHandler<BitControlHandshake> getHandshakeHandler(final ControlConnection connection) {
    return new ServerHandshakeHandler<BitControlHandshake>(RpcType.HANDSHAKE, BitControlHandshake.PARSER) {

      @Override
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServer.java`
#### Snippet
```java
  protected ServerHandshakeHandler<UserToBitHandshake> getHandshakeHandler(final BitToUserConnection connection) {

    return new ServerHandshakeHandler<UserToBitHandshake>(RpcType.HANDSHAKE, UserToBitHandshake.PARSER){

      @Override
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/ServerAuthenticationHandler.java`
#### Snippet
```java
      final SaslMessage saslResponse;
      try {
        saslResponse = SaslMessage.PARSER.parseFrom(new ByteBufInputStream(pBody));
      } catch (final InvalidProtocolBufferException e) {
        handleAuthFailure(connection, sender, e, saslResponseType);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CollectToListVarcharAggFunction.java`
#### Snippet
```java
  @Param NullableVarCharHolder input;
  @Output BaseWriter.ComplexWriter writer;
  @Workspace ObjectHolder writerHolder;

  @Override
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CollectToListVarcharAggFunction.java`
#### Snippet
```java
  @Override
  public void setup() {
    writerHolder = new ObjectHolder();
  }

```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CollectListMapsAggFunction.java`
#### Snippet
```java
  @Param FieldReader[] inputs;
  @Output BaseWriter.ComplexWriter writer;
  @Workspace ObjectHolder writerHolder;

  @Override
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CollectListMapsAggFunction.java`
#### Snippet
```java
  @Override
  public void setup() {
    writerHolder = new ObjectHolder();
  }

```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      columnsHolder = new ObjectHolder();
    }

```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      schemaHolder = new ObjectHolder();
    }

```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Output VarCharHolder out;
    @Inject DrillBuf buf;
    @Workspace ObjectHolder schemaHolder;

    @Override
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Output NullableVarCharHolder out;
    @Inject DrillBuf buf;
    @Workspace ObjectHolder columnsHolder;

    @Override
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Output NullableVarCharHolder out;
    @Inject DrillBuf buf;
    @Workspace ObjectHolder schemaHolder;

    @Override
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      schemaHolder = new ObjectHolder();
    }

```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class VarCharTDigestFunction implements DrillAggFunc {
    @Param VarCharHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableFloat8TDigestFunction implements DrillAggFunc {
    @Param NullableFloat8Holder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class Float4TDigestFunction implements DrillAggFunc {
    @Param Float4Holder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class TimeStampTDigestFunction implements DrillAggFunc {
    @Param TimeStampHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class TimeTDigestFunction implements DrillAggFunc {
    @Param TimeHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableTimeTDigestFunction implements DrillAggFunc {
    @Param NullableTimeHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class BigIntTDigestFunction implements DrillAggFunc {
    @Param BigIntHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableDateTDigestFunction implements DrillAggFunc {
    @Param NullableDateHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableBigIntTDigestFunction implements DrillAggFunc {
    @Param NullableBigIntHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class BitTDigestFunction implements DrillAggFunc {
    @Param BitHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableFloat4TDigestFunction implements DrillAggFunc {
    @Param NullableFloat4Holder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class TDigestMergeFunction implements DrillAggFunc {
    @Param NullableVarBinaryHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class VarBinaryTDigestFunction implements DrillAggFunc {
    @Param VarBinaryHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableVarBinaryTDigestFunction implements DrillAggFunc {
    @Param NullableVarBinaryHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class DateTDigestFunction implements DrillAggFunc {
    @Param DateHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class IntTDigestFunction implements DrillAggFunc {
    @Param IntHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableIntTDigestFunction implements DrillAggFunc {
    @Param NullableIntHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableVarCharTDigestFunction implements DrillAggFunc {
    @Param NullableVarCharHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class Float8TDigestFunction implements DrillAggFunc {
    @Param Float8Holder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableTimeStampTDigestFunction implements DrillAggFunc {
    @Param NullableTimeStampHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      compression.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.TDIGEST_COMPRESSION);
      work.obj = new com.tdunning.math.stats.MergingDigest(compression.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
  public static class NullableBitTDigestFunction implements DrillAggFunc {
    @Param NullableBitHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject DrillBuf buffer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Override
    public void setup() {
      jsonReader = new org.apache.drill.exec.vector.complex.fn.JsonReader.Builder(buffer)
          .defaultSchemaPathColumns()
          .build();
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Param NullableVarCharHolder in;
    @Inject DrillBuf buffer;
    @Workspace org.apache.drill.exec.vector.complex.fn.JsonReader jsonReader;

    @Output ComplexWriter writer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Param VarBinaryHolder in;
    @Inject DrillBuf buffer;
    @Workspace org.apache.drill.exec.vector.complex.fn.JsonReader jsonReader;

    @Output ComplexWriter writer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Param NullableVarBinaryHolder in;
    @Inject DrillBuf buffer;
    @Workspace org.apache.drill.exec.vector.complex.fn.JsonReader jsonReader;

    @Output ComplexWriter writer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Param VarCharHolder in;
    @Inject DrillBuf buffer;
    @Workspace org.apache.drill.exec.vector.complex.fn.JsonReader jsonReader;

    @Output ComplexWriter writer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Override
    public void setup() {
      jsonReader = new org.apache.drill.exec.vector.complex.fn.JsonReader.Builder(buffer)
          .defaultSchemaPathColumns()
          .build();
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Override
    public void setup() {
      jsonReader = new org.apache.drill.exec.vector.complex.fn.JsonReader.Builder(buffer)
          .defaultSchemaPathColumns()
          .build();
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
    @Override
    public void setup() {
      jsonReader = new org.apache.drill.exec.vector.complex.fn.JsonReader.Builder(buffer)
          .defaultSchemaPathColumns()
          .build();
```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        throw org.apache.drill.common.exceptions.UserException.functionError()
          .message("Start index in split_part can not be zero, value provided was " +
            "[start:" + start.value + "]").build();
      }
      if (start.value * end.value <= 0) {
```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        throw org.apache.drill.common.exceptions.UserException.functionError()
          .message("End index in split_part must has the same sign as the start " +
            "index, value provided was [start:" + start.value + ",end:" + end.value + "]").build();
      }
      if (end.value < start.value) {
```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        throw org.apache.drill.common.exceptions.UserException.functionError()
          .message("End index in split_part must be greater or equal to start " +
            "index, value provided was [start:" + start.value + ",end:" + end.value + "]").build();
      }

```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      if (index.value == 0) {
        throw org.apache.drill.common.exceptions.UserException.functionError()
          .message("Index in split_part can not be zero").build();
      }
      String inputString = org.apache.drill.exec.expr.fn.impl.
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_CANCEL_FRAGMENT_VALUE: {
      final FragmentHandle handle = get(pBody, FragmentHandle.PARSER);
      cancelFragment(handle);
      sender.send(ControlRpcConfig.OK);
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_CUSTOM_VALUE: {
      final CustomMessage customMessage = get(pBody, CustomMessage.PARSER);
      sender.send(handlerRegistry.handle(customMessage, (DrillBuf) dBody));
      break;
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_RECEIVER_FINISHED_VALUE: {
      final FinishedReceiver finishedReceiver = get(pBody, FinishedReceiver.PARSER);
      receivingFragmentFinished(finishedReceiver);
      sender.send(ControlRpcConfig.OK);
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_FRAGMENT_STATUS_VALUE:
      final FragmentStatus status = get(pBody, FragmentStatus.PARSER);
      requestFragmentStatus(status);
      // TODO: Support a type of message that has no response.
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_QUERY_CANCEL_VALUE: {
      final QueryId queryId = get(pBody, QueryId.PARSER);
      final Ack cancelStatus = requestQueryCancel(queryId);
      if (cancelStatus.getOk()) {
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_INITIALIZE_FRAGMENTS_VALUE: {
      final InitializeFragments fragments = get(pBody, InitializeFragments.PARSER);
      initializeFragment(fragments);
      sender.send(ControlRpcConfig.OK);
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_QUERY_STATUS_VALUE: {
      final QueryId queryId = get(pBody, QueryId.PARSER);
      final QueryProfile profile = requestQueryStatus(queryId);
      sender.send(new Response(RpcType.RESP_QUERY_STATUS, profile));
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java

    case RpcType.REQ_UNPAUSE_FRAGMENT_VALUE: {
      final FragmentHandle handle = get(pBody, FragmentHandle.PARSER);
      resumeFragment(handle);
      sender.send(ControlRpcConfig.OK);
```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
      wrapper.getNode().getRoot().accept(targetOpVisitor, null);
    } catch (Throwable e) {
      throw UserException.systemError(e).build();
    }
    boolean contain = targetOpVisitor.isContain();
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    TimeStampHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Decimal18Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Float4Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableBigIntHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Float8Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDateHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableTimeStampHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    TimeHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableIntHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDecimal18Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Workspace BigIntHolder dups;
    @Workspace
    ObjectHolder interval;
    @Output
    NullableBigIntHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    TimeHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    TimeStampHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Float8Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    VarBinaryHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableBitHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output NullableBigIntHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableTimeHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Decimal9Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    VarCharHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDateHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      dups.value = 0;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableTimeHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
  public static class HllMerge implements DrillAggFunc {
    @Param NullableVarBinaryHolder in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject OptionManager options;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Decimal18Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Var16CharHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    DateHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    ObjectHolder work;
    @Workspace
    ObjectHolder interval;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    BigIntHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    IntHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVar16CharHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    IntervalHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace
    ObjectHolder interval;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    DateHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableIntHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    BitHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output NullableBigIntHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
  public static class HllFieldReader implements DrillAggFunc {
    @Param FieldReader in;
    @Workspace ObjectHolder work;
    @Output NullableVarBinaryHolder out;
    @Inject OptionManager options;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Workspace BigIntHolder dups;
    @Workspace
    ObjectHolder interval;
    @Output
    NullableBigIntHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      dups.value = 0;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDecimal18Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Float4Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Decimal9Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVarCharHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableFloat8Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableTimeStampHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    VarCharHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    VarBinaryHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableFloat4Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    IntervalHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Workspace
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    BigIntHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    ObjectHolder work;
    @Workspace
    ObjectHolder interval;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVarBinaryHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableBitHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDecimal9Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    Var16CharHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    public void setup() {
      work = new ObjectHolder();
      interval = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableIntervalHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace
    ObjectHolder interval;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVarCharHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableBigIntHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableFloat8Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    IntHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableDecimal9Holder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Output
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    BitHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableIntervalHolder in;
    @Workspace
    ObjectHolder work;
    @Workspace BigIntHolder dups;
    @Workspace
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      dups.value = 0;
      ndvBloomFilterElts.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.NDV_BLOOM_FILTER_ELEMENTS);
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVar16CharHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableVarBinaryHolder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    NullableFloat4Holder in;
    @Workspace
    ObjectHolder work;
    @Output
    NullableVarBinaryHolder out;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
    @Override
    public void setup() {
      work = new ObjectHolder();
      hllAccuracy.value = (int) options.getLong(org.apache.drill.exec.ExecConstants.HLL_ACCURACY);
      work.obj = new com.clearspring.analytics.stream.cardinality.HyperLogLog(hllAccuracy.value);
```

### Deprecation
'PARSER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/Foreman.java`
#### Snippet
```java
    try {
      serverState =
          ServerPreparedStatementState.PARSER.parseFrom(preparedStatementHandle.getServerInfo());
    } catch (final InvalidProtocolBufferException ex) {
      throw UserException.parseError(ex)
```

### Deprecation
Overrides deprecated method in 'org.apache.drill.exec.store.dfs.FormatPlugin'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### Deprecation
'org.apache.drill.exec.store.easy.json.JSONRecordReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonBatchReader.java`
#### Snippet
```java
/**
 * EVF based reader. It is using by default to read JSON files - store.json.enable_v2_reader = true
 * The old deprecated one is {@link JSONRecordReader}
 */
public class JsonBatchReader implements ManagedReader<FileSchemaNegotiator> {
```

### Deprecation
'QUOTE_NON_NUMERIC_NUMBERS' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java

      JsonGenerator generator = factory.createGenerator(stream).useDefaultPrettyPrinter()
          .configure(JsonGenerator.Feature.QUOTE_NON_NUMERIC_NUMBERS,
              !Boolean.parseBoolean(writerOptions.get("enableNanInf")));
      if (uglify) {
```

### Deprecation
'org.apache.drill.exec.store.easy.json.JSONRecordReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JSONFormatPlugin.java`
#### Snippet
```java
                                      List<SchemaPath> columns,
                                      String userName) {
    return new JSONRecordReader(context, fileWork.getPath(), dfs, columns, formatConfig);
  }

```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/JsonLoaderImpl.java`
#### Snippet
```java
 * <h4>Comparison to Original JSON Reader</h4>
 *
 * This class replaces the {@link org.apache.drill.exec.vector.complex.fn.JsonReader} class used in Drill versions 1.17
 * and before. Compared with the previous version, this implementation:
 * <ul>
```

### Deprecation
'limitReached(int)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/JsonLoaderImpl.java`
#### Snippet
```java
        }
        rowWriter.save();
      } else if (rowWriter.limitReached(maxRows)) {
        eof = true;
        break;
```

### Deprecation
'ALLOW_NON_NUMERIC_NUMBERS' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java

  private static final ObjectMapper NAN_INF_MAPPER = getDefaultMapper()
    .configure(JsonParser.Feature.ALLOW_NON_NUMERIC_NUMBERS, true);

  private static final ObjectMapper ESCAPE_ANY_MAPPER = getDefaultMapper()
```

### Deprecation
'ALLOW_BACKSLASH_ESCAPING_ANY_CHARACTER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java

  private static final ObjectMapper ESCAPE_ANY_MAPPER = getDefaultMapper()
    .configure(JsonParser.Feature.ALLOW_BACKSLASH_ESCAPING_ANY_CHARACTER, true);

  private static final ObjectMapper NAN_INF_AND_ESCAPE_ANY_MAPPER = getDefaultMapper()
```

### Deprecation
'ALLOW_NON_NUMERIC_NUMBERS' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java

  private static final ObjectMapper NAN_INF_AND_ESCAPE_ANY_MAPPER = getDefaultMapper()
    .configure(JsonParser.Feature.ALLOW_NON_NUMERIC_NUMBERS, true).configure(JsonParser.Feature.ALLOW_BACKSLASH_ESCAPING_ANY_CHARACTER, true);

  private static final String JACKSON_PARSER_EOF_FILE_MSG = "Unexpected end-of-input:";
```

### Deprecation
'ALLOW_BACKSLASH_ESCAPING_ANY_CHARACTER' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java

  private static final ObjectMapper NAN_INF_AND_ESCAPE_ANY_MAPPER = getDefaultMapper()
    .configure(JsonParser.Feature.ALLOW_NON_NUMERIC_NUMBERS, true).configure(JsonParser.Feature.ALLOW_BACKSLASH_ESCAPING_ANY_CHARACTER, true);

  private static final String JACKSON_PARSER_EOF_FILE_MSG = "Unexpected end-of-input:";
```

### Deprecation
'generateTestData(int)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockRecordReader.java`
#### Snippet
```java
    for (final ValueVector v : valueVectors) {
      final ValueVector.Mutator m = v.getMutator();
      m.generateTestData(recordSetSize);
    }

```

### Deprecation
Overrides deprecated method in 'org.apache.drill.exec.store.dfs.FormatPlugin'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### Deprecation
'ParquetFileWriter(Configuration, MessageType, Path, org.apache.parquet.hadoop.ParquetFileWriter.Mode, long, int)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
      // BLOCK_FS_SCHEMES (ParquetFileWriter.java in parquet-mr), which includes HDFS.
      // For other filesystems, it uses default blockSize configured for the file system.
      parquetFileWriter = new ParquetFileWriter(conf, schema, path, ParquetFileWriter.Mode.OVERWRITE, blockSize, 0);
    } else {
      parquetFileWriter = new ParquetFileWriter(conf, schema, path, ParquetFileWriter.Mode.OVERWRITE);
```

### Deprecation
'ParquetFileWriter(Configuration, MessageType, Path, org.apache.parquet.hadoop.ParquetFileWriter.Mode)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
      parquetFileWriter = new ParquetFileWriter(conf, schema, path, ParquetFileWriter.Mode.OVERWRITE, blockSize, 0);
    } else {
      parquetFileWriter = new ParquetFileWriter(conf, schema, path, ParquetFileWriter.Mode.OVERWRITE);
    }
    storageStrategy.applyToFile(fs, path);
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetRecordMaterializer.java`
#### Snippet
```java
                                        Collection<SchemaPath> columns, OptionManager options,
                                        ParquetReaderUtility.DateCorruptionStatus containsCorruptedDates) {
    writer = new VectorContainerWriter(mutator);
    root = new DrillParquetGroupConverter(mutator, writer.rootAsMap(), schema, columns, options, containsCorruptedDates, false, "");
  }
```

### Deprecation
'HTTP_MEMORY_HEAP_FAILURE_THRESHOLD' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/RestQueryRunner.java`
#### Snippet
```java

    // Heap usage threshold/trigger to provide resiliency on web server for queries submitted via HTTP
    double memoryFailureThreshold = workManager.getContext().getConfig().getDouble(ExecConstants.HTTP_MEMORY_HEAP_FAILURE_THRESHOLD);

    boolean isComplete = false;
```

### Deprecation
'getOperatorType()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
      operatorType = firstProfile.getOperatorTypeName();
    } else {
      CoreOperatorType operatorType = CoreOperatorType.valueOf(firstProfile.getOperatorType());
      this.operatorType = operatorType != null
          ? Objects.requireNonNull(operatorType).name()
```

### Deprecation
'QUOTE_NON_NUMERIC_NUMBERS' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonWriter.java`
#### Snippet
```java

  public JsonWriter(OutputStream out, boolean pretty, boolean useExtendedOutput) throws IOException {
    JsonGenerator writer = factory.configure(JsonGenerator.Feature.QUOTE_NON_NUMERIC_NUMBERS, false).createGenerator(out);
    if (pretty) {
      writer = writer.useDefaultPrettyPrinter();
```

### Deprecation
'DrillSqlOperator(java.lang.String, int, boolean, boolean)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateTableHandler.java`
#### Snippet
```java
  private RexNode composeDisjunction(final RexBuilder rexBuilder, List<RexNode> compFuncs) {
    final DrillSqlOperator booleanOrFunc
             = new DrillSqlOperator("orNoShortCircuit", 2, true, false);
    RexNode node = compFuncs.remove(0);
    while (!compFuncs.isEmpty()) {
```

### Deprecation
'DrillSqlOperator(java.lang.String, int, boolean, boolean)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateTableHandler.java`
#### Snippet
```java

  private RexNode createPartitionColComparator(final RexBuilder rexBuilder, List<RexNode> inputs) {
    final DrillSqlOperator op = new DrillSqlOperator(WriterPrel.PARTITION_COMPARATOR_FUNC, 1, true, false);

    final List<RexNode> compFuncs = Lists.newArrayListWithExpectedSize(inputs.size());
```

### Deprecation
'toString(java.io.InputStream)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SchemaHandler.java`
#### Snippet
```java

        try (InputStream stream = fs.open(path)) {
          return IOUtils.toString(stream);
        }

```

### Deprecation
'addMixInAnnotations(java.lang.Class, java.lang.Class)' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExecutionControls.java`
#### Snippet
```java

  static {
    controlsOptionMapper.addMixInAnnotations(Injection.class, InjectionMixIn.class);
  }

```

### Deprecation
'complexWriters' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectBatchBuilder.java`
#### Snippet
```java
  private void initComplexWriters() {
    // Lazy initialization of the list of complex writers, if not done yet.
    if (projectBatch.complexWriters == null) {
      projectBatch.complexWriters = new ArrayList<>();
    } else {
```

### Deprecation
'complexWriters' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectBatchBuilder.java`
#### Snippet
```java
    // Lazy initialization of the list of complex writers, if not done yet.
    if (projectBatch.complexWriters == null) {
      projectBatch.complexWriters = new ArrayList<>();
    } else {
      projectBatch.complexWriters.clear();
```

### Deprecation
'complexWriters' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectBatchBuilder.java`
#### Snippet
```java
      projectBatch.complexWriters = new ArrayList<>();
    } else {
      projectBatch.complexWriters.clear();
    }
  }
```

### Deprecation
'build()' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsAggBatch.java`
#### Snippet
```java
            throw UserException.dataWriteError(e)
                .addContext("Failed to write statistics to JSON")
                .build();
          }
        }
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetLoader.java`
#### Snippet
```java
 * during testing, but will be disabled during production.
 *
 * @see {@link VectorContainerWriter}, the class which this class
 * replaces
 */
```

### Deprecation
Overrides deprecated method in 'org.apache.drill.exec.store.dfs.FormatPlugin'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/TableFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java

        @Override
        public Table load(final MapRDBTableCache.Key key) throws Exception {
          // getTable is already calling tableCache.get in correct user UGI context, so should be fine here.
          // key.Left is Path. key.Right is indexDesc.
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
          // getTable is already calling tableCache.get in correct user UGI context, so should be fine here.
          // key.Left is Path. key.Right is indexDesc.
          Table table = (key.indexDesc == null ? MapRDBImpl.getTable(key.path) : MapRDBImpl.getIndexTable(key.indexDesc));
          logger.debug("time {} opened the table for tablePath {} tableHandle {} index {} userName {}",
              System.nanoTime(),
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java

      RemovalListener<MapRDBTableCache.Key, Table> removalListener = new RemovalListener<MapRDBTableCache.Key, Table>() {
        public void onRemoval(RemovalNotification<MapRDBTableCache.Key, Table> removal) {
          Table table = removal.getValue();
          MapRDBTableCache.Key key = removal.getKey();
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
      RemovalListener<MapRDBTableCache.Key, Table> removalListener = new RemovalListener<MapRDBTableCache.Key, Table>() {
        public void onRemoval(RemovalNotification<MapRDBTableCache.Key, Table> removal) {
          Table table = removal.getValue();
          MapRDBTableCache.Key key = removal.getKey();
          logger.debug("time {} closing the tablePath {} tableHandle {} index {} userName {}",
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java

    try {
      dbTableHandle = proxyUserUgi.doAs(new PrivilegedExceptionAction<Table>() {
        public Table run() throws Exception {

```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
   * @param table table to be closed.
   */
  public void closeTable(Table table) {
    if (!tableCachingEnabled && table != null) {
      table.close();
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
    try {
      dbTableHandle = proxyUserUgi.doAs(new PrivilegedExceptionAction<Table>() {
        public Table run() throws Exception {

          if (logger.isTraceEnabled()) {
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java

          if (tableCachingEnabled) {
            Table table = tableCache.get(new MapRDBTableCache.Key(tablePath, indexDesc));
            logger.trace("time {} get the tablePath {} tableHandle {} index {} userName {} currentUser {}",
                System.nanoTime(), tablePath == null ? "null" : tablePath,
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
  private static final int MIN_TABLE_CACHE_ENTRY_TIMEOUT = 10;

  LoadingCache<MapRDBTableCache.Key, Table> tableCache;

  private final boolean tableCachingEnabled;
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
   * @param indexDesc index table Descriptor
   */
  public Table getTable(String tableName, IndexDesc indexDesc, String userName) {
    return getTable(new Path(tableName), indexDesc, userName);
  }
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
   * @param indexDesc index table descriptor
   */
  public Table getTable(final Path tablePath, final IndexDesc indexDesc, final String userName) throws DrillRuntimeException {

    final Table dbTableHandle;
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
  public Table getTable(final Path tablePath, final IndexDesc indexDesc, final String userName) throws DrillRuntimeException {

    final Table dbTableHandle;
    final UserGroupInformation proxyUserUgi = ImpersonationUtil.createProxyUgi(userName);

```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
   * @param tableName primary table path
   */
  public Table getTable(String tableName, String userName) {
    return getTable(new Path(tableName), null, userName);
  }
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
          expireAfterAccess(tableCacheExpiryTime, TimeUnit.MINUTES).
          maximumSize(tableCacheSize).
          removalListener(removalListener).build(new CacheLoader<MapRDBTableCache.Key, Table>() {

        @Override
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
      final int tableCacheExpiryTime = Math.max((int) (config.getDouble(FORMAT_MAPRDB_JSON_TABLE_CACHE_TIMEOUT)), MIN_TABLE_CACHE_ENTRY_TIMEOUT);

      RemovalListener<MapRDBTableCache.Key, Table> removalListener = new RemovalListener<MapRDBTableCache.Key, Table>() {
        public void onRemoval(RemovalNotification<MapRDBTableCache.Key, Table> removal) {
          Table table = removal.getValue();
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
   * @param tablePath primary table path
   */
  public Table getTable(Path tablePath, String userName) {
    return getTable(tablePath, null, userName);
  }
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
      final int tableCacheExpiryTime = Math.max((int) (config.getDouble(FORMAT_MAPRDB_JSON_TABLE_CACHE_TIMEOUT)), MIN_TABLE_CACHE_ENTRY_TIMEOUT);

      RemovalListener<MapRDBTableCache.Key, Table> removalListener = new RemovalListener<MapRDBTableCache.Key, Table>() {
        public void onRemoval(RemovalNotification<MapRDBTableCache.Key, Table> removal) {
          Table table = removal.getValue();
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/DocumentReaderVectorWriter.java`
#### Snippet
```java
  }

  protected abstract void writeDBDocument(final VectorContainerWriter writer, final DBDocumentReaderBase reader)
      throws SchemaChangeException;

```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/ProjectionPassthroughVectorWriter.java`
#### Snippet
```java

  @Override
  protected void writeDBDocument(VectorContainerWriter vectorWriter, DBDocumentReaderBase reader)
      throws SchemaChangeException {
    if (reader.next() != EventType.START_MAP) {
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/IdOnlyVectorWriter.java`
#### Snippet
```java

  @Override
  public void writeDBDocument(VectorContainerWriter vectorWriter, DBDocumentReaderBase reader)
      throws SchemaChangeException {
    MapWriter writer = vectorWriter.rootAsMap();
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RowCountVectorWriter.java`
#### Snippet
```java

  @Override
  protected void writeDBDocument(VectorContainerWriter vectorWriter, DBDocumentReaderBase reader)
      throws SchemaChangeException {
    vectorWriter.rootAsMap().bit("count").writeBit(1);
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/FieldTransferVectorWriter.java`
#### Snippet
```java

  @Override
  protected void writeDBDocument(VectorContainerWriter vectorWriter, DBDocumentReaderBase reader)
      throws SchemaChangeException {
    MapOrListWriterImpl writer = new MapOrListWriterImpl(vectorWriter.rootAsMap());
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableRangePartitionFunction.java`
#### Snippet
```java

    // get the table handle from the table cache
    Table table = plugin.getJsonTableCache().getTable(tableName, userName);

    // Get all scan ranges for the primary table.
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonRecordReader.java`
#### Snippet
```java
    }

    Table table = super.formatPlugin.getJsonTableCache().getTable(subScanSpec.getTableName(), subScanSpec.getUserName());
    final MultiGet multiGet = new MultiGet((BaseJsonTable) table, condition, false, projections);
    int recordCount = 0;
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
    // Get the index table or primary table and use the DB API to get the estimated number of rows. For size estimates,
    // we assume that all the columns would be read from the disk.
    final Table table = this.formatPlugin.getJsonTableCache().getTable(scanSpec.getTableName(), index, getUserName());

    if (table != null) {
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
    // Get the index table or primary table and use the DB API to get the estimated number of rows. For size estimates,
    // we assume that all the columns would be read from the disk.
    final Table table = this.formatPlugin.getJsonTableCache().getTable(scanSpec.getTableName(), indexDesc, getUserName());

    if (table != null) {
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
      // Get the fullTableRowCount only once i.e. if not already obtained before.
      if (fullTableRowCount == 0) {
        final Table t = this.formatPlugin.getJsonTableCache().getTable(
            scanSpec.getTableName(), scanSpec.getIndexDesc(), getUserName());
        final MetaTable metaTable = t.getMetaTable();
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
    double estimatedRowCount = ROWCOUNT_UNKNOWN;
    if (doNotAccessRegionsToScan == null) {
      final Table t = this.formatPlugin.getJsonTableCache().getTable(
          scanSpec.getTableName(), scanSpec.getIndexDesc(), getUserName());
      final MetaTable metaTable = t.getMetaTable();
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
        // Clear the ScanBatch vector container writer/mutator in order to be able to generate the new schema
        vectorWriterMutator.clear();
        vectorWriter = new VectorContainerWriter(vectorWriterMutator, unionEnabled);
        logger.debug("Encountered schema change earlier use new writer {}", vectorWriter.toString());
        document = lastDocument;
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
  @Override
  public void setup(OperatorContext context, OutputMutator output) throws ExecutionSetupException {
    this.vectorWriter = new VectorContainerWriter(output, unionEnabled);
    this.vectorWriterMutator = output;
    this.operatorContext = context;
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java

  private OperatorContext operatorContext;
  protected VectorContainerWriter vectorWriter;
  private DBDocumentReaderBase reader;
  Document document;
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
  }

  protected Table getTable() {
    return table;
  }
```

### Deprecation
'com.mapr.db.Table' is deprecated
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
  protected static final FieldPath[] ID_ONLY_PROJECTION = { ID_FIELD };

  protected Table table;
  protected QueryCondition condition;

```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaGeneralRPCProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setMode(org.apache.drill.exec.proto.GeneralRPCProtos.RpcMode.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaBitData.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setChannel(org.apache.drill.exec.proto.UserBitShared.RpcChannel.valueOf(input.readEnum()));
                            break;
                        default:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaCoordinationProtos.java`
#### Snippet
```java
                            break;
                        case 7:
                            builder.setState(org.apache.drill.exec.proto.CoordinationProtos.DrillbitEndpoint.State.valueOf(input.readEnum()));
                            break;
                        case 8:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaBitControl.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setChannel(org.apache.drill.exec.proto.UserBitShared.RpcChannel.valueOf(input.readEnum()));
                            break;
                        case 3:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/common/types/SchemaTypeProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setMinorType(org.apache.drill.common.types.TypeProtos.MinorType.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/common/types/SchemaTypeProtos.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setMode(org.apache.drill.common.types.TypeProtos.DataMode.valueOf(input.readEnum()));
                            break;
                        case 3:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/common/types/SchemaTypeProtos.java`
#### Snippet
```java
                            break;
                        case 7:
                            builder.addSubType(org.apache.drill.common.types.TypeProtos.MinorType.valueOf(input.readEnum()));
                            break;
                        default:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 3:
                            builder.setErrorType(org.apache.drill.exec.proto.UserBitShared.DrillPBError.ErrorType.valueOf(input.readEnum()));
                            break;
                        case 4:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setType(org.apache.drill.exec.proto.UserBitShared.NamePart.Type.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setState(org.apache.drill.exec.proto.UserBitShared.FragmentState.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'setOperatorType(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 4:
                            builder.setOperatorType(input.readInt32());
                            break;
                        case 5:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 3:
                            builder.setState(org.apache.drill.exec.proto.UserBitShared.QueryResult.QueryState.valueOf(input.readEnum()));
                            break;
                        case 4:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setQueryState(org.apache.drill.exec.proto.UserBitShared.QueryResult.QueryState.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 3:
                            builder.setStatus(org.apache.drill.exec.proto.UserBitShared.SaslStatus.valueOf(input.readEnum()));
                            break;
                        default:
```

### Deprecation
'hasOperatorType()' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                if(message.hasOperatorId())
                    output.writeInt32(3, message.getOperatorId(), false);
                if(message.hasOperatorType())
                    output.writeInt32(4, message.getOperatorType(), false);
                if(message.hasSetupNanos())
```

### Deprecation
'getOperatorType()' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                    output.writeInt32(3, message.getOperatorId(), false);
                if(message.hasOperatorType())
                    output.writeInt32(4, message.getOperatorType(), false);
                if(message.hasSetupNanos())
                    output.writeInt64(5, message.getSetupNanos(), false);
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setType(org.apache.drill.exec.proto.UserBitShared.QueryType.valueOf(input.readEnum()));
                            break;
                        case 3:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
                            break;
                        case 8:
                            builder.setState(org.apache.drill.exec.proto.UserBitShared.QueryResult.QueryState.valueOf(input.readEnum()));
                            break;
                        case 9:
```

### Deprecation
'limitReached(int)' is deprecated
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5BatchReader.java`
#### Snippet
```java

    // Limit pushdown
    if (rowWriter.limitReached(maxRecords)) {
      return false;
    }
```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java

      // write full length first (this is length delimited stream).
      cos.writeRawVarint32(fullLength);

      // write header
```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java

      // write header
      cos.writeRawVarint32(HEADER_TAG);
      cos.writeRawVarint32(headerLength);
      header.writeTo(cos);
```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java
      // write header
      cos.writeRawVarint32(HEADER_TAG);
      cos.writeRawVarint32(headerLength);
      header.writeTo(cos);

```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java

      // write protobuf body length and body
      cos.writeRawVarint32(PROTOBUF_BODY_TAG);
      cos.writeRawVarint32(protoBodyLength);
      msg.pBody.writeTo(cos);
```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java
      // write protobuf body length and body
      cos.writeRawVarint32(PROTOBUF_BODY_TAG);
      cos.writeRawVarint32(protoBodyLength);
      msg.pBody.writeTo(cos);

```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java
        }

        cos.writeRawVarint32(RAW_BODY_TAG);
        cos.writeRawVarint32(rawBodyLength);
        cos.flush(); // need to flush so that dbody goes after if cos is caching.
```

### Deprecation
'writeRawVarint32(int)' is deprecated
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcEncoder.java`
#### Snippet
```java

        cos.writeRawVarint32(RAW_BODY_TAG);
        cos.writeRawVarint32(rawBodyLength);
        cos.flush(); // need to flush so that dbody goes after if cos is caching.

```

### Deprecation
'limitReached(int)' is deprecated
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/read/IcebergRecordReader.java`
#### Snippet
```java

  private boolean nextLine(RowSetLoader rowWriter) {
    if (rowWriter.limitReached(maxRecords)) {
      return false;
    }
```

### Deprecation
'io.netty.buffer.SlicedByteBuf' is deprecated
in `exec/memory/base/src/main/java/io/netty/buffer/MutableWrappedByteBuf.java`
#### Snippet
```java
  @Override
  public ByteBuf slice(int index, int length) {
    return new SlicedByteBuf(this, index, length);
  }

```

### Deprecation
'order()' is deprecated
in `exec/memory/base/src/main/java/io/netty/buffer/MutableWrappedByteBuf.java`
#### Snippet
```java
  @Override
  public ByteOrder order() {
    return buffer.order();
  }

```

### Deprecation
'io.netty.buffer.DuplicatedByteBuf' is deprecated
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  private final long initCap;

  UnsafeDirectLittleEndian(DuplicatedByteBuf buf) {
    this(buf, true, null, null);
  }
```

### Deprecation
'io.netty.buffer.SlicedByteBuf' is deprecated
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  @Override
  public ByteBuf slice(int index, int length) {
    return new SlicedByteBuf(this, index, length);
  }

```

### Deprecation
'io.netty.buffer.DuplicatedByteBuf' is deprecated
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
    allocator = new InnerAllocator();
    empty = new UnsafeDirectLittleEndian(
        new DuplicatedByteBuf(Unpooled.EMPTY_BUFFER));
  }

```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 3:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.HandshakeStatus.valueOf(input.readEnum()));
                            break;
                        case 4:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 8:
                            builder.addSupportedMethods(org.apache.drill.exec.proto.UserProtos.RpcType.valueOf(input.readEnum()));
                            break;
                        case 9:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 6:
                            builder.addCollateSupport(org.apache.drill.exec.proto.UserProtos.CollateSupport.valueOf(input.readEnum()));
                            break;
                        case 7:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 9:
                            builder.setCorrelationNamesSupport(org.apache.drill.exec.proto.UserProtos.CorrelationNamesSupport.valueOf(input.readEnum()));
                            break;
                        case 10:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 11:
                            builder.addDateTimeLiteralsSupport(org.apache.drill.exec.proto.UserProtos.DateTimeLiteralsSupport.valueOf(input.readEnum()));
                            break;
                        case 12:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 12:
                            builder.setGroupBySupport(org.apache.drill.exec.proto.UserProtos.GroupBySupport.valueOf(input.readEnum()));
                            break;
                        case 13:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 13:
                            builder.setIdentifierCasing(org.apache.drill.exec.proto.UserProtos.IdentifierCasing.valueOf(input.readEnum()));
                            break;
                        case 14:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 32:
                            builder.setNullCollation(org.apache.drill.exec.proto.UserProtos.NullCollation.valueOf(input.readEnum()));
                            break;
                        case 33:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 35:
                            builder.addOrderBySupport(org.apache.drill.exec.proto.UserProtos.OrderBySupport.valueOf(input.readEnum()));
                            break;
                        case 36:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 36:
                            builder.addOuterJoinSupport(org.apache.drill.exec.proto.UserProtos.OuterJoinSupport.valueOf(input.readEnum()));
                            break;
                        case 37:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 37:
                            builder.setQuotedIdentifierCasing(org.apache.drill.exec.proto.UserProtos.IdentifierCasing.valueOf(input.readEnum()));
                            break;
                        case 38:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 45:
                            builder.addSubquerySupport(org.apache.drill.exec.proto.UserProtos.SubQuerySupport.valueOf(input.readEnum()));
                            break;
                        case 46:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 49:
                            builder.addUnionSupport(org.apache.drill.exec.proto.UserProtos.UnionSupport.valueOf(input.readEnum()));
                            break;
                        case 50:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setChannel(org.apache.drill.exec.proto.UserBitShared.RpcChannel.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 9:
                            builder.setSaslSupport(org.apache.drill.exec.proto.UserProtos.SaslSupport.valueOf(input.readEnum()));
                            break;
                        default:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserBitShared.QueryResult.QueryState.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setType(org.apache.drill.exec.proto.UserBitShared.QueryType.valueOf(input.readEnum()));
                            break;
                        case 3:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setFrom(org.apache.drill.common.types.TypeProtos.MinorType.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setTo(org.apache.drill.common.types.TypeProtos.MinorType.valueOf(input.readEnum()));
                            break;
                        default:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setStatus(org.apache.drill.exec.proto.UserProtos.RequestStatus.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            return;
                        case 1:
                            builder.setResultsMode(org.apache.drill.exec.proto.UserProtos.QueryResultsMode.valueOf(input.readEnum()));
                            break;
                        case 2:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 2:
                            builder.setType(org.apache.drill.exec.proto.UserBitShared.QueryType.valueOf(input.readEnum()));
                            break;
                        case 3:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 13:
                            builder.setSearchability(org.apache.drill.exec.proto.UserProtos.ColumnSearchability.valueOf(input.readEnum()));
                            break;
                        case 14:
```

### Deprecation
'valueOf(int)' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                            break;
                        case 14:
                            builder.setUpdatability(org.apache.drill.exec.proto.UserProtos.ColumnUpdatability.valueOf(input.readEnum()));
                            break;
                        case 15:
```

### Deprecation
'binary(java.lang.String)' is deprecated
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    vb.end = bytes.length;
    if (isList == false) {
      writer.binary(fieldName).write(vb);
    } else {
      writer.list.varBinary().write(vb);
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
  private MongoCursor<BsonDocument> cursor;

  private JsonReader jsonReader;
  private BsonRecordReader bsonReader;
  private VectorContainerWriter writer;
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
  @Override
  public void setup(OperatorContext context, OutputMutator output) throws ExecutionSetupException {
    this.writer = new VectorContainerWriter(output, unionEnabled);
    // Default is BsonReader and all text mode will not be honored in
    // BsonRecordReader
```

### Deprecation
'org.apache.drill.exec.vector.complex.fn.JsonReader' is deprecated
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
      logger.debug("Initialized BsonRecordReader. ");
    } else {
      this.jsonReader = new JsonReader.Builder(fragmentContext.getManagedBuffer())
          .schemaPathColumns(Lists.newArrayList(getColumns()))
          .allTextMode(enableAllTextMode)
```

### Deprecation
'org.apache.drill.exec.vector.complex.impl.VectorContainerWriter' is deprecated
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
  private JsonReader jsonReader;
  private BsonRecordReader bsonReader;
  private VectorContainerWriter writer;

  private Document filters;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java

  @Param NullableVarBinaryHolder in;
  @Workspace ObjectHolder value;
  @Workspace UInt1Holder init;
  @Workspace BigIntHolder nonNullCount;
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java
    nonNullCount.value = 0;
    init.value = 0;
    value = new ObjectHolder();
    value.obj = new java.util.ArrayList<com.esri.core.geometry.Geometry>();
  }
```

### Deprecation
'org.apache.drill.exec.expr.holders.ObjectHolder' is deprecated
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java
  @Override
  public void reset() {
    value = new ObjectHolder();
    value.obj = new java.util.ArrayList<com.esri.core.geometry.Geometry>();
    init.value = 0;
```

### Deprecation
'getMilliseconds(java.lang.String)' is deprecated
in `common/src/main/java/org/apache/drill/common/config/NestedConfig.java`
#### Snippet
```java
  @Override
  public Long getMilliseconds(String path) {
    return c.getMilliseconds(path);
  }

```

### Deprecation
'getNanoseconds(java.lang.String)' is deprecated
in `common/src/main/java/org/apache/drill/common/config/NestedConfig.java`
#### Snippet
```java
  @Override
  public Long getNanoseconds(String path) {
    return c.getNanoseconds(path);
  }

```

### Deprecation
'getNanosecondsList(java.lang.String)' is deprecated
in `common/src/main/java/org/apache/drill/common/config/NestedConfig.java`
#### Snippet
```java
  @Override
  public List<Long> getNanosecondsList(String path) {
    return c.getNanosecondsList(path);
  }

```

### Deprecation
'getMillisecondsList(java.lang.String)' is deprecated
in `common/src/main/java/org/apache/drill/common/config/NestedConfig.java`
#### Snippet
```java
  @Override
  public List<Long> getMillisecondsList(String path) {
    return c.getMillisecondsList(path);
  }

```

### Deprecation
'PARSER' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
                org.apache.drill.exec.proto.BitControl.PlanFragment m =
                    input.readMessage(
                        org.apache.drill.exec.proto.BitControl.PlanFragment.PARSER,
                        extensionRegistry);
                if (fragmentsBuilder_ == null) {
```

### Deprecation
'PARSER' is deprecated
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
                org.apache.drill.exec.proto.BitControl.PlanFragment m =
                    input.readMessage(
                        org.apache.drill.exec.proto.BitControl.PlanFragment.PARSER,
                        extensionRegistry);
                if (fragmentsBuilder_ == null) {
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
          if (parsedCreatedByVersion.hasSemanticVersion()) {
            SemanticVersion semVer = parsedCreatedByVersion.getSemanticVersion();
            String pre = semVer.pre + "";
            if (semVer.major == 1 && semVer.minor == 8 && semVer.patch == 1 && pre.contains("drill")) {
              return checkForCorruptDateValuesInStatistics(footer, columns, autoCorrectCorruptDates);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java
      totalValuesRead += runLength + nullRunLength;

      logger.trace("" + "recordsToReadInThisPass: {} \t "
              + "Run Length: {} \t Null Run Length: {} \t readCount: {} \t writeCount: {} \t "
              + "recordsReadInThisIteration: {} \t valuesReadInCurrentPass: {} \t "
```

### TrivialStringConcatenation
Empty string used in concatenation
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java

      if (logger.isTraceEnabled()) {
        logger.trace("" + "recordsToReadInThisPass: {} \t "
          + "Run Length: {} \t Null Run Length: {} \t valueCount: {} \t "
          + "recordsReadInThisIteration: {} \t valuesReadInCurrentPass: {} \t "
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
  }

  private static final int memcmp(final long laddr, int lStart, int lEnd, final long raddr, int rStart, final int rEnd) {
    int lLen = lEnd - lStart;
    int rLen = rEnd - rStart;
```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
  }

  private static final int memEqual(final long laddr, int lStart, int lEnd, final long raddr, int rStart,
      final int rEnd) {

```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
   * @return 1 if left input is greater, -1 if left input is smaller, 0 otherwise
   */
  public static final int compare(final DrillBuf left, int lStart, int lEnd, final byte[] right, int rStart, final int rEnd) {
    rangeCheck(left, lStart, lEnd);
    return memcmp(left.memoryAddress(), lStart, lEnd, right, rStart, rEnd);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java


  private static final int memcmp(final long laddr, int lStart, int lEnd, final byte[] right, int rStart, final int rEnd) {
    int lLen = lEnd - lStart;
    int rLen = rEnd - rStart;
```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
   * @return 1 if left input is greater, -1 if left input is smaller, 0 otherwise
   */
  public static final int compare(final DrillBuf left, int lStart, int lEnd, final DrillBuf right, int rStart, int rEnd){
    rangeCheck(left, lStart, lEnd, right, rStart, rEnd);
    return memcmp(left.memoryAddress(), lStart, lEnd, right.memoryAddress(), rStart, rEnd);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
   * @return 1 if left input is greater, -1 if left input is smaller, 0 otherwise
   */
  public static final int equal(final DrillBuf left, int lStart, int lEnd, final DrillBuf right, int rStart, int rEnd){
    rangeCheck(left, lStart, lEnd, right, rStart, rEnd);
    return memEqual(left.memoryAddress(), lStart, lEnd, right.memoryAddress(), rStart, rEnd);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/vector/src/main/java/org/apache/drill/exec/util/SerializationModule.java`
#### Snippet
```java
  }

  public static final SimpleModule getModule() {
    return drillModule;
  }
```

### FinalStaticMethod
'static' method declared `final`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillDatabaseMetaDataImpl.java`
#### Snippet
```java
    }

    public static final Set<SQLConvertSupport> toSQLConvertSupport(Iterable<ConvertSupport> convertSupportIterable) {
      ImmutableSet.Builder<SQLConvertSupport> sqlConvertSupportSet = ImmutableSet.builder();
      for(ConvertSupport convertSupport: convertSupportIterable) {
```

### FinalStaticMethod
'static' method declared `final`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/sql/logical/HivePushPartitionFilterIntoScan.java`
#### Snippet
```java
public abstract class HivePushPartitionFilterIntoScan {

  public static final StoragePluginOptimizerRule getFilterOnProject(OptimizerRulesContext optimizerRulesContext,
      final String defaultPartitionValue) {
    return new PruneScanRule(
```

### FinalStaticMethod
'static' method declared `final`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/sql/logical/HivePushPartitionFilterIntoScan.java`
#### Snippet
```java
  }

  public static final StoragePluginOptimizerRule getFilterOnScan(OptimizerRulesContext optimizerRulesContext,
      final String defaultPartitionValue) {
    return new PruneScanRule(
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java
  }

  private static final void appendType(MajorType mt, StringBuilder sb) {
    sb.append(mt.getMinorType().name());
    sb.append(":");
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  }

  public static final int compare(NullableVarBinaryHolder left, NullableVarCharHolder right) {
    if (left.isSet == 0) {
      if (right.isSet == 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  }

  public static final int compare(NullableVarBinaryHolder left, NullableVarBinaryHolder right) {
    if (left.isSet == 0) {
      if (right.isSet == 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(VarHelpers.class);

  public static final int compare(VarBinaryHolder left, VarCharHolder right) {
    for (int l = left.start, r = right.start; l < left.end && r < right.end; l++, r++) {
      byte leftByte = left.buffer.getByte(l);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  }

  public static final int compare(VarCharHolder left, VarCharHolder right) {
    for (int l = left.start, r = right.start; l < left.end && r < right.end; l++, r++) {
      byte leftByte = left.buffer.getByte(l);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  }

  public static final int compare(NullableVarCharHolder left, NullableVarCharHolder right) {
    if (left.isSet == 0) {
      if (right.isSet == 0) {
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
  }

  public static final int compare(VarBinaryHolder left, VarBinaryHolder right) {
    for (int l = left.start, r = right.start; l < left.end && r < right.end; l++, r++) {
      byte leftByte = left.buffer.getByte(l);
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/DrillHash.java`
#### Snippet
```java
public class DrillHash {

    public static final long getLongLittleEndian(long offset) {
        //return PlatformDependent.getLong(offset);
        return     ((long) PlatformDependent.getByte(offset+7)    << 56)
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/DrillHash.java`
#### Snippet
```java
    }

    public static final long getIntLittleEndian(long offset) {
        long retl = 0;
        retl = ((PlatformDependent.getByte(offset+3) &0xffL) << 24)
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
public final class MurmurHash3 extends DrillHash{

   public static final long fmix64(long k) {
    k ^= k >>> 33;
    k *= 0xff51afd7ed558ccdL;
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
  private static final int MIN_FILE_SIZE = ParquetFileWriter.MAGIC.length + FOOTER_METADATA_SIZE;

  private static final void readFully(FSDataInputStream stream, long start, byte[] output, int offset, int len) throws IOException{
    int bytesRead = 0;
    while(bytesRead > -1 && bytesRead < len){
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java
   * @return an integer encoded as a low endian
   */
  static final int getInt(final byte[] buff, final int pos) {
    return DrillBuf.getInt(buff, pos);
  }
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java
  }

  private static final void vlCopyLELongNoPadding(byte[] src, int srcIndex, byte[] dest, int destIndex, int length) {
    if (length == 1) {
      dest[destIndex] = src[srcIndex];
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java
  }

  private static final void vlCopyGTLongNoPadding(byte[] src, int srcIndex, byte[] dest, int destIndex, int length) {
    final int numLongEntries = length >> 3;
    final int remaining      = length & 0x7;
```

### FinalStaticMethod
'static' method declared `final`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/ServerMethod.java`
#### Snippet
```java
   * @return a immutable set of capabilities
   */
  static final Set<ServerMethod> getSupportedMethods(Iterable<RpcType> supportedMethods, RpcEndpointInfos serverInfos) {
    ImmutableSet.Builder<ServerMethod> builder = ImmutableSet.builder();

```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaDefProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_ServerPreparedStatementState_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_FragmentHandle_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.ServerPreparedStatementState)
        org.apache.drill.exec.proto.ExecProtos.ServerPreparedStatementStateOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_ServerPreparedStatementState_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.FragmentHandle)
        org.apache.drill.exec.proto.ExecProtos.FragmentHandleOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.ExecProtos.internal_static_exec_bit_FragmentHandle_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.CompleteRpcMessage)
        org.apache.drill.exec.proto.GeneralRPCProtos.CompleteRpcMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_CompleteRpcMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_CompleteRpcMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_RpcHeader_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.RpcHeader)
        org.apache.drill.exec.proto.GeneralRPCProtos.RpcHeaderOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_RpcHeader_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.rpc.Ack)
        org.apache.drill.exec.proto.GeneralRPCProtos.AckOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_Ack_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.GeneralRPCProtos.internal_static_exec_rpc_Ack_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.DrillbitEndpoint)
        org.apache.drill.exec.proto.CoordinationProtos.DrillbitEndpointOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillbitEndpoint_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillbitEndpoint_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        return getDescriptor();
      }
      public static final com.google.protobuf.Descriptors.EnumDescriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.DrillbitEndpoint.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.DrillServiceInstance)
        org.apache.drill.exec.proto.CoordinationProtos.DrillServiceInstanceOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillServiceInstance_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.Roles)
        org.apache.drill.exec.proto.CoordinationProtos.RolesOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_Roles_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_DrillServiceInstance_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.CoordinationProtos.internal_static_exec_Roles_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.common.types.TypeProtos.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:common.MajorType)
        org.apache.drill.common.types.TypeProtos.MajorTypeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.common.types.TypeProtos.internal_static_common_MajorType_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.common.types.TypeProtos.internal_static_common_MajorType_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.common.types.TypeProtos.getDescriptor().getEnumTypes().get(1);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitServerHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.BitServerHandshake)
        org.apache.drill.exec.proto.BitData.BitServerHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitServerHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.RuntimeFilterBDef)
        org.apache.drill.exec.proto.BitData.RuntimeFilterBDefOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_RuntimeFilterBDef_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_FragmentRecordBatch_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_RuntimeFilterBDef_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.BitClientHandshake)
        org.apache.drill.exec.proto.BitData.BitClientHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitClientHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.FragmentRecordBatch)
        org.apache.drill.exec.proto.BitData.FragmentRecordBatchOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_FragmentRecordBatch_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.data.AckWithCredit)
        org.apache.drill.exec.proto.BitData.AckWithCreditOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_AckWithCredit_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_BitClientHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitData.internal_static_exec_bit_data_AckWithCredit_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.BitControlHandshake)
        org.apache.drill.exec.proto.BitControl.BitControlHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitControlHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FragmentStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.QueryContextInformation)
        org.apache.drill.exec.proto.BitControl.QueryContextInformationOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_QueryContextInformation_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.PlanFragment)
        org.apache.drill.exec.proto.BitControl.PlanFragmentOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_PlanFragment_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.CustomMessage)
        org.apache.drill.exec.proto.BitControl.CustomMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_CustomMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.InitializeFragments)
        org.apache.drill.exec.proto.BitControl.InitializeFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_InitializeFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_WorkQueueStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.Collector)
        org.apache.drill.exec.proto.BitControl.CollectorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_Collector_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.FinishedReceiver)
        org.apache.drill.exec.proto.BitControl.FinishedReceiverOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FinishedReceiver_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_InitializeFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitControlHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_Collector_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.WorkQueueStatus)
        org.apache.drill.exec.proto.BitControl.WorkQueueStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_WorkQueueStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_CustomMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.FragmentStatus)
        org.apache.drill.exec.proto.BitControl.FragmentStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FragmentStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.bit.control.BitStatus)
        org.apache.drill.exec.proto.BitControl.BitStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_QueryContextInformation_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_PlanFragment_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_FinishedReceiver_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.internal_static_exec_bit_control_BitStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.BitControl.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DateConversionUtils.java`
#### Snippet
```java
   * @return the modified LocalDateTime
   */
  public final static java.time.LocalDateTime getDate(java.time.LocalDateTime d, String interval) {
    java.time.LocalDateTime newDate = d;
    int year = d.getYear();
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.NamePart)
        org.apache.drill.exec.proto.UserBitShared.NamePartOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NamePart_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_UserCredentials_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.getDescriptor().getEnumTypes().get(3);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.DrillPBError)
        org.apache.drill.exec.proto.UserBitShared.DrillPBErrorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_DrillPBError_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.getDescriptor().getEnumTypes().get(2);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Registry_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.ExceptionWrapper)
        org.apache.drill.exec.proto.UserBitShared.ExceptionWrapperOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ExceptionWrapper_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.StreamProfile)
        org.apache.drill.exec.proto.UserBitShared.StreamProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StreamProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MajorFragmentProfile)
        org.apache.drill.exec.proto.UserBitShared.MajorFragmentProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MajorFragmentProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryResult)
        org.apache.drill.exec.proto.UserBitShared.QueryResultOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryResult_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryInfo)
        org.apache.drill.exec.proto.UserBitShared.QueryInfoOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryInfo_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        return getDescriptor();
      }
      public static final com.google.protobuf.Descriptors.EnumDescriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.DrillPBError.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryInfo_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_DrillPBError_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryResult_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SerializedField_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NodeStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.SerializedField)
        org.apache.drill.exec.proto.UserBitShared.SerializedFieldOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SerializedField_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MinorFragmentProfile)
        org.apache.drill.exec.proto.UserBitShared.MinorFragmentProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MinorFragmentProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ParsingError_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ExceptionWrapper_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.MetricValue)
        org.apache.drill.exec.proto.UserBitShared.MetricValueOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MetricValue_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MinorFragmentProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MajorFragmentProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.Registry)
        org.apache.drill.exec.proto.UserBitShared.RegistryOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Registry_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.StackTraceElementWrapper)
        org.apache.drill.exec.proto.UserBitShared.StackTraceElementWrapperOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StackTraceElementWrapper_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_OperatorProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StreamProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.getDescriptor().getEnumTypes().get(1);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryProfile)
        org.apache.drill.exec.proto.UserBitShared.QueryProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.UserCredentials)
        org.apache.drill.exec.proto.UserBitShared.UserCredentialsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_UserCredentials_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.OperatorProfile)
        org.apache.drill.exec.proto.UserBitShared.OperatorProfileOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_OperatorProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.NodeStatus)
        org.apache.drill.exec.proto.UserBitShared.NodeStatusOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NodeStatus_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_MetricValue_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryProfile_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_NamePart_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.ParsingError)
        org.apache.drill.exec.proto.UserBitShared.ParsingErrorOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_ParsingError_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryId)
        org.apache.drill.exec.proto.UserBitShared.QueryIdOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryId_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryData_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Jar_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.RecordBatchDef)
        org.apache.drill.exec.proto.UserBitShared.RecordBatchDefOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_RecordBatchDef_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.Jar)
        org.apache.drill.exec.proto.UserBitShared.JarOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_Jar_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryId_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        return getDescriptor();
      }
      public static final com.google.protobuf.Descriptors.EnumDescriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.NamePart.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        return getDescriptor();
      }
      public static final com.google.protobuf.Descriptors.EnumDescriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.QueryResult.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_RecordBatchDef_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.QueryData)
        org.apache.drill.exec.proto.UserBitShared.QueryDataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_QueryData_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.shared.SaslMessage)
        org.apache.drill.exec.proto.UserBitShared.SaslMessageOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SaslMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_StackTraceElementWrapper_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserBitShared.internal_static_exec_shared_SaslMessage_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(2);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.Property)
        org.apache.drill.exec.proto.UserProtos.PropertyOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_Property_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(4);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ColumnMetadata)
        org.apache.drill.exec.proto.UserProtos.ColumnMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ColumnMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_Property_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CreatePreparedStatementResp)
        org.apache.drill.exec.proto.UserProtos.CreatePreparedStatementRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ResultColumnMetadata)
        org.apache.drill.exec.proto.UserProtos.ResultColumnMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ResultColumnMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CatalogMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CatalogMetadata)
        org.apache.drill.exec.proto.UserProtos.CatalogMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CatalogMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ServerMeta_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatementHandle_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.BitToUserHandshake)
        org.apache.drill.exec.proto.UserProtos.BitToUserHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_BitToUserHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(1);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RunQuery)
        org.apache.drill.exec.proto.UserProtos.RunQueryOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RunQuery_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RpcEndpointInfos)
        org.apache.drill.exec.proto.UserProtos.RpcEndpointInfosOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RpcEndpointInfos_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ResultColumnMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(8);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ServerMeta)
        org.apache.drill.exec.proto.UserProtos.ServerMetaOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ServerMeta_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RunQuery_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.ConvertSupport)
        org.apache.drill.exec.proto.UserProtos.ConvertSupportOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ConvertSupport_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RpcEndpointInfos_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserToBitHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(6);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetTablesResp)
        org.apache.drill.exec.proto.UserProtos.GetTablesRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(16);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetQueryPlanFragments)
        org.apache.drill.exec.proto.UserProtos.GetQueryPlanFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetQueryPlanFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(15);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(10);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_BitToUserHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(14);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(9);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.PreparedStatementHandle)
        org.apache.drill.exec.proto.UserProtos.PreparedStatementHandleOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatementHandle_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetQueryPlanFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetSchemasReq)
        org.apache.drill.exec.proto.UserProtos.GetSchemasReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetServerMetaResp)
        org.apache.drill.exec.proto.UserProtos.GetServerMetaRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(11);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(12);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(7);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.TableMetadata)
        org.apache.drill.exec.proto.UserProtos.TableMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_TableMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetCatalogsReq)
        org.apache.drill.exec.proto.UserProtos.GetCatalogsReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetColumnsResp)
        org.apache.drill.exec.proto.UserProtos.GetColumnsRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.SchemaMetadata)
        org.apache.drill.exec.proto.UserProtos.SchemaMetadataOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_SchemaMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_QueryPlanFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RequestResults_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(5);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetServerMetaReq)
        org.apache.drill.exec.proto.UserProtos.GetServerMetaReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.LikeFilter)
        org.apache.drill.exec.proto.UserProtos.LikeFilterOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_LikeFilter_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatement_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(3);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserProperties_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetTablesReq)
        org.apache.drill.exec.proto.UserProtos.GetTablesReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetTablesReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return getDescriptor();
    }
    public static final com.google.protobuf.Descriptors.EnumDescriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.getDescriptor().getEnumTypes().get(13);
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.UserToBitHandshake)
        org.apache.drill.exec.proto.UserProtos.UserToBitHandshakeOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserToBitHandshake_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetColumnsReq)
        org.apache.drill.exec.proto.UserProtos.GetColumnsReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetColumnsReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetSchemasResp)
        org.apache.drill.exec.proto.UserProtos.GetSchemasRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetSchemasResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ColumnMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.QueryPlanFragments)
        org.apache.drill.exec.proto.UserProtos.QueryPlanFragmentsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_QueryPlanFragments_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.GetCatalogsResp)
        org.apache.drill.exec.proto.UserProtos.GetCatalogsRespOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.RequestResults)
        org.apache.drill.exec.proto.UserProtos.RequestResultsOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_RequestResults_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetServerMetaResp_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_ConvertSupport_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_LikeFilter_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.PreparedStatement)
        org.apache.drill.exec.proto.UserProtos.PreparedStatementOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_PreparedStatement_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.CreatePreparedStatementReq)
        org.apache.drill.exec.proto.UserProtos.CreatePreparedStatementReqOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_CreatePreparedStatementReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_TableMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_GetCatalogsReq_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      return this.unknownFields;
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_SchemaMetadata_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:exec.user.UserProperties)
        org.apache.drill.exec.proto.UserProtos.UserPropertiesOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.drill.exec.proto.UserProtos.internal_static_exec_user_UserProperties_descriptor;
```

## RuleId[id=RedundantTypeArguments]
### RedundantTypeArguments
Explicit type arguments can be inferred
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/FileSystemUtil.java`
#### Snippet
```java
    } catch (CancellationException ex) {
      logger.debug("RecursiveListing task to list {} was cancelled.", path);
      return Collections.<FileStatus>emptyList();
    } finally {
      pool.shutdown();
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/SessionOptionManager.java`
#### Snippet
```java

  public SessionOptionManager(final OptionManager systemOptions, final UserSession session) {
    super(systemOptions, CaseInsensitiveMap.<OptionValue>newConcurrentMap());
    this.session = session;
  }
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/QueryOptionManager.java`
#### Snippet
```java

  public QueryOptionManager(OptionManager sessionOptions) {
    super(sessionOptions, CaseInsensitiveMap.<OptionValue>newHashMap());
  }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
      throws ExecutionSetupException {
    this(context, context.newOperatorContext(subScanConfig),
        readers, Collections.<Map<String, String>> emptyList());
  }

```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
      throws ExecutionSetupException {
    this(context, context.newOperatorContext(subScanConfig),
        readerList, Collections.<Map<String, String>> emptyList());
    this.readerList = readerList;
    this.isRepeatableScan = isRepeatableScan;
```

### RedundantTypeArguments
Explicit type arguments can be inferred
in `common/src/main/java/org/apache/drill/common/scanner/persistence/AnnotationDescriptor.java`
#### Snippet
```java
  public List<String> getValues(String attributeName) {
    AttributeDescriptor desc = attributeMap.get(attributeName);
    return desc == null ? Collections.<String>emptyList() : desc.getValues();
  }

```

## RuleId[id=NumberEquality]
### NumberEquality
Number objects are compared using `!=`, not 'equals()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (decExpr.getBigDecimal() != ((Decimal28Expression) value).getBigDecimal()) {
      return false;
    }
```

### NumberEquality
Number objects are compared using `!=`, not 'equals()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (decExpr.getBigDecimal() != ((Decimal38Expression) value).getBigDecimal()) {
      return false;
    }
```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidGroupScan.java`
#### Snippet
```java

    @Override
    public int compareTo(CompleteWork o) {
      return 0;
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public boolean retainAll(Collection<?> c) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public <T> T[] toArray(T[] a) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public boolean removeAll(Collection<?> c) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public boolean addAll(Collection<? extends T> c) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public boolean addAll(int index, Collection<? extends T> c) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public boolean containsAll(Collection<?> c) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenInterceptor.java`
#### Snippet
```java

  @Override
  public Response intercept(Chain chain) throws IOException {
    logger.debug("Intercepting call {}", chain.toString());
    String accessToken = accessTokenRepository.getAccessToken();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
    @NotNull
    @Override
    public Response intercept(Chain chain) throws IOException {
      // Get the existing request
      Request request = chain.request();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
    @NotNull
    @Override
    public Response intercept(Chain chain) throws IOException {

      Response response = chain.proceed(chain.request());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyList.java`
#### Snippet
```java
    }

    public int compareTo(final Node argNode) {
      // just do an identity compare since elsewhere you should ensure that only one node exists for each nodeValue.
      return argNode == this ? 0 : -1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java

    @Override
    public void write(byte[] b) {
      super.write(b, 0, b.length);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/concurrent/ExecutorServiceUtil.java`
#### Snippet
```java
    /** {@inheritDoc} */
    @Override
    public T get(long timeout, TimeUnit unit) throws InterruptedException, ExecutionException, TimeoutException {
      return future.get(timeout, unit);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java


  @Override public int read(byte[] buf, int off, int len) throws IOException {
    checkInputStreamState();
    Preconditions.checkArgument((off >= 0) && (len >= 0) && (buf.length) >= (off + len));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
      return input.read(b);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
      return input.read(b, off, len);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ResourceInputStream.java`
#### Snippet
```java

  @Override
  public int read(byte[] b) throws IOException {
    int l = read(pos, b, 0, b.length);
    pos += l;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFSDataInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b) throws IOException {
      operatorStats.startWait();
      try {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFSDataInputStream.java`
#### Snippet
```java

    @Override
    public int read(byte[] b, int off, int len) throws IOException {
      operatorStats.startWait();
      try {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/MetadataVersion.java`
#### Snippet
```java

  @Override
  public int compareTo(MetadataVersion o) {
    Preconditions.checkNotNull(o);
    return ComparisonChain.start()
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java

  @Override
  public int compareTo(CompleteWork o) {
    if(o instanceof CompleteFileWork){
      CompleteFileWork c = (CompleteFileWork) o;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java

    @Override
    public Set<String> keySet() {
      return originalMap.keySet();
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java

    @Override
    public Collection<ColumnSize> values() {
      return originalMap.values();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends ColumnSize> m) {
      originalMap.putAll(m);
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java

    @Override
    public Set<Entry<String, ColumnSize>> entrySet() {
      return originalMap.entrySet();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java
     */
    @Override
    public int compareTo(DrillbitInfo drillbitToCompare) {
      if (this.isCurrent()) {
        return -1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/LogWriter.java`
#### Snippet
```java

  @Override
  public void write(char[] cbuf, int off, int len) throws IOException {
    checkNotClosed();

```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public Object[] toArray() {
      return internalMap.keySet().toArray();
    }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public Iterator<KEY> iterator() {
      return internalMap.keySet().iterator();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends KEY> c) {
      throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> c) {
      throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public VALUE replace(KEY key, VALUE value) {
    return null;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public VALUE replace(KEY key, VALUE value) {
    return null;
  }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a) {
      return internalMap.keySet().toArray(a);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a) {
      return internalMap.keySet().toArray(a);
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public boolean remove(Object key, Object value) {
    return false;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public boolean replace(KEY key, VALUE oldValue, VALUE newValue) {
    return false;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public boolean replace(KEY key, VALUE oldValue, VALUE newValue) {
    return false;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public boolean replace(KEY key, VALUE oldValue, VALUE newValue) {
    return false;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public void putAll(Map<? extends KEY, ? extends VALUE> m) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> c) {
      throw new UnsupportedOperationException();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java

  @Override
  public VALUE putIfAbsent(KEY key, VALUE value) {
    throw new UnsupportedOperationException();
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    @Override
    public int read(byte b[], int off, int len) throws IOException {
      int n = in.read(b, off, len);
      if (n != -1) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    @Override
    public void write(byte[] b, int off, int len) throws IOException {
      count += len;
      out.write(b, off, len);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    @Override
    public int read(byte b[]) throws IOException {
      int n = in.read(b);
      if (n != -1) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBSubScanSpec.java`
#### Snippet
```java
   * compares the stopRows.
   */
  public int compareTo(MapRDBSubScanSpec o) {
    if (o == null) {
      return 1;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/TabletFragmentInfo.java`
#### Snippet
```java

  @Override
  public int compareTo(TabletFragmentInfo o) {
    return tabletInfoImpl != null ? tabletInfoImpl.compareTo(o.tabletInfoImpl) :
        regionInfo.compareTo(o.regionInfo);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/NamedThreadFactory.java`
#### Snippet
```java
  */
  @Override
  public Thread newThread(final Runnable runnable) {
    final Thread thread = new Thread(runnable, prefix + nextId.incrementAndGet());
    thread.setDaemon(true);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `exec/rpc/src/main/java/org/apache/drill/common/SerializedExecutor.java`
#### Snippet
```java

  @Override
  public void execute(Runnable command) {
    synchronized (queuedRunnables) {
      if (isProcessing) {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/IcebergCompleteWork.java`
#### Snippet
```java

  @Override
  public int compareTo(CompleteWork o) {
    return 0;
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduGroupScan.java`
#### Snippet
```java

    @Override
    public int compareTo(CompleteWork o) {
      return 0;
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaAsyncCloser.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable runnable) {
      Thread thread = delegate.newThread(runnable);
      thread.setName(THREAD_PREFIX + thread.getName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/Version.java`
#### Snippet
```java

  @Override
  public int compareTo(Version o) {
    Preconditions.checkNotNull(o);
    return ComparisonChain.start()
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/util/DataInputInputStream.java`
#### Snippet
```java

  @Override
  public int read(byte[] b, int off, int len) throws IOException {
    for (int i = off; i < off + len; i++) {
      try {
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/util/DataOutputOutputStream.java`
#### Snippet
```java

  @Override
  public void write(byte[] b) throws IOException {
    out.write(b);
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/util/DataOutputOutputStream.java`
#### Snippet
```java

  @Override
  public void write(byte[] b, int off, int len) throws IOException {
    out.write(b, off, len);
  }
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java

    @Override
    public Set<K> keySet() {
      return primary.keySet();
    }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java

  @Override
  public void putAll(Map<? extends K, ? extends V> m) {
    delegate.putAll(m);
  }
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> m) {
      throw new UnsupportedOperationException();
    }
```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
      int[] resultTable    = new int[TABLE_SIZE];

      for (int idx = 0; idx < resultTable.length; ++idx) {
        resultTable[idx] = patternLength;
      }
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
    // Nth bucket.
    buckets = new Double[numBuckets + 1];
    for (int i = 0; i < buckets.length; i++) {
      buckets[i] = new Double(0.0);
    }
```

## RuleId[id=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java
    Class<?> writerClass = BasicTypeHelper
        .getWriterImpl(v.getField().getType().getMinorType(), v.getField().getDataMode());
    if (writerClass.equals(SingleListWriter.class)) {
      writerClass = UnionListWriter.class;
    }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'DrillDistributionTrait' and 'RelTrait'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DrillDistributionTrait.java`
#### Snippet
```java
          // a join may end up with hash-distributions using different keys. This would
          // cause incorrect query result.
          return this.equals(trait);
        }
        else if (requiredDist == DistributionType.RANDOM_DISTRIBUTED) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'DrillDistributionTrait' and 'RelTrait'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DrillDistributionTrait.java`
#### Snippet
```java
    }

    return this.equals(trait);
  }

```

### EqualsBetweenInconvertibleTypes
No class found which is a subtype of both 'RecordBatch' and 'ProjectorTemplate'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectorTemplate.java`
#### Snippet
```java
  public final int projectRecords(RecordBatch incomingRecordBatch, int startIndex, int recordCount,
                                  int firstOutputIndex) {
    assert incomingRecordBatch != this; // mixed up incoming and outgoing batches?
    switch (svMode) {
    case FOUR_BYTE:
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `contrib/storage-cassandra/src/main/java/org/apache/drill/exec/store/cassandra/CassandraColumnConverterFactory.java`
#### Snippet
```java
      } else if (clazz == java.math.BigInteger.class) {
        return TypeProtos.MinorType.BIGINT;
      } else if (clazz == org.apache.calcite.avatica.util.ByteString.class) {
        return TypeProtos.MinorType.VARCHAR;
      }
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `false`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type seen and whose toString() renders value well.
          false
          || rawActualType == java.lang.Boolean.class
          || rawActualType == java.lang.Byte.class
```

### DuplicateCondition
Duplicate condition `false`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
          // Is type seen and whose toString() has rendered value well--in cases
          // seen so far.
          false
          || rawActualType == java.util.Properties.class
          || rawActualType.isEnum()
```

### DuplicateCondition
Duplicate condition `false`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type to warn about (one case).
          false
          || rawActualType == org.apache.drill.jdbc.DrillResultSet.class
          ) {
```

### DuplicateCondition
Duplicate condition `false`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type to warn about (second case).
          false
          // Note:  Using strings rather than compiled-in class references to
          // avoid failing when run using JDBC-all Jar, which excludes
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/ClientCommand.java`
#### Snippet
```java

  protected void removeAppIdFile() {
    getAppIdFile().delete();
  }
}
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java

      System.out.println(" Not running.");
      appIdFile.delete();
      return;
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
    if (reporter.isStopped()) {
      System.out.println(" Completed with state " + reporter.getState());
      appIdFile.delete();
      return;
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
        uploadSiteArchive(siteArchive);
      } finally {
        siteArchive.delete();
      }
    }
```

### IgnoreResultOfCall
Result of `Condition.await()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/FragmentContextImpl.java`
#### Snippet
```java
      try {
        if (rfIdentifier2RFW.get(rfIdentifier) == null) {
          condition4RF.await(maxWaitTime, timeUnit);
        }
      } catch (InterruptedException e) {
```

### IgnoreResultOfCall
Result of `Thread.interrupted()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
      eventProcessor.terminate();
      // Clear the interrupt flag if it is set.
      Thread.interrupted();

      // here we could be in FAILED, RUNNING, or CANCELLATION_REQUESTED
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java
        FileUtils.deleteDirectory(file);
      } else {
        file.delete();
      }
    } else if (file.isDirectory()) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java
      throw new IOException("Cannot delete directory");
    }
    file.delete();
    return true;
  }
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java
    public int read() throws IOException {
      byte[] b = new byte[1];
      input.read(b);
      position++;
      return b[0] & 0xFF;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBuilder.java`
#### Snippet
```java
    String pathName = name.slash + ".java";
    File codeFile = new File(codeDir, pathName);
    codeFile.getParentFile().mkdirs();
    try (final FileWriter writer = new FileWriter(codeFile)) {
      writer.write(code);
```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/service/ServiceEngine.java`
#### Snippet
```java
    p.shutdown();
    try {
      p.awaitTermination(3, TimeUnit.SECONDS);
    } catch (InterruptedException e) {
      Thread.currentThread().interrupt();
```

### IgnoreResultOfCall
Result of `CountDownLatch.await()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/PauseInjection.java`
#### Snippet
```java
    if (this.getMsPause() > 0L) {
      try {
        latch.await(getMsPause(), TimeUnit.MILLISECONDS);
      } catch (InterruptedException e) {
        //Unpausing self as this is timed
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
    public void deleteOnExit(String fragmentSpillDir) throws IOException {
      File dir = new File(baseDir, fragmentSpillDir);
      dir.mkdirs();
      dir.deleteOnExit();
    }
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
    @Override
    public void deleteFile(String fileName) throws IOException {
      new File(baseDir, fileName).delete();
    }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
      File spillDir = new File(baseDir, fragmentSpillDir);
      for (File spillFile : spillDir.listFiles()) {
        spillFile.delete(); // IO exception if file delete fails
      }
      spillDir.delete();// IO exception if dir delete fails
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
        spillFile.delete(); // IO exception if file delete fails
      }
      spillDir.delete();// IO exception if dir delete fails
    }

```

### IgnoreResultOfCall
Result of `Thread.interrupted()` is ignored
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
          }
          // Clear interrupt flag
          Thread.interrupted();
        }
      }
```

### IgnoreResultOfCall
Result of `Collections.unmodifiableList()` is ignored
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
      }
    }
    Collections.unmodifiableList(projectedColumns);
  }

```

### IgnoreResultOfCall
Result of `TypeProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaDefProtos.java`
#### Snippet
```java
          org.apache.drill.common.types.TypeProtos.getDescriptor(),
        });
    org.apache.drill.common.types.TypeProtos.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `CoordinationProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        internal_static_exec_bit_ServerPreparedStatementState_descriptor,
        new java.lang.String[] { "SqlQuery", });
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }
```

### IgnoreResultOfCall
Result of `UserBitShared.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        new java.lang.String[] { "SqlQuery", });
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `CoordinationProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
        internal_static_exec_rpc_CompleteRpcMessage_descriptor,
        new java.lang.String[] { "Header", "ProtobufBody", "RawBody", });
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `ExecProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        internal_static_exec_bit_data_AckWithCredit_descriptor,
        new java.lang.String[] { "AllowedCredit", });
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
```

### IgnoreResultOfCall
Result of `CoordinationProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        new java.lang.String[] { "AllowedCredit", });
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }
```

### IgnoreResultOfCall
Result of `UserBitShared.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `InnerAllocator.fail()` is ignored
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
              maxCapacity);
          if (!(buf instanceof PooledUnsafeDirectByteBuf)) {
            fail();
          }

```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
    for (int posn = 0; posn < length; posn += buffer.length) {
      int len = Math.min(buffer.length, length - posn);
      in.read(buffer, 0, len);
      buf.writeBytes(buffer, 0, len);
    }
```

### IgnoreResultOfCall
Result of `ExecProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        internal_static_exec_bit_control_FinishedReceiver_descriptor,
        new java.lang.String[] { "Receiver", "Sender", });
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
```

### IgnoreResultOfCall
Result of `CoordinationProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        new java.lang.String[] { "Receiver", "Sender", });
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }
```

### IgnoreResultOfCall
Result of `UserBitShared.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `Matcher.find()` is ignored
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DateUtilFunctions.java`
#### Snippet
```java
    if (DB_DATE.matcher(inputString).matches()) {
      dateMatcher = DB_DATE.matcher(inputString);
      dateMatcher.find();
      year = Integer.parseInt(dateMatcher.group(1));
      month = Integer.parseInt(dateMatcher.group(2));
```

### IgnoreResultOfCall
Result of `Matcher.find()` is ignored
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DateUtilFunctions.java`
#### Snippet
```java
    } else if (DATE_SLASH.matcher(inputString).matches()) {
      dateMatcher = DATE_SLASH.matcher(inputString);
      dateMatcher.find();
      year = Integer.parseInt(dateMatcher.group(3));
      if (leadingDay) {
```

### IgnoreResultOfCall
Result of `Matcher.find()` is ignored
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DateUtilFunctions.java`
#### Snippet
```java
    } else if (LEADING_SLASH_DATE.matcher(inputString).matches()) {
      dateMatcher = LEADING_SLASH_DATE.matcher(inputString);
      dateMatcher.find();
      year = Integer.parseInt(dateMatcher.group(1));
      month = Integer.parseInt(dateMatcher.group(2));
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
      fileReaderShp = file.fileSystem().openPossiblyCompressedStream(file.split().getPath());
      byte[] shpBuf = new byte[fileReaderShp.available()];
      fileReaderShp.read(shpBuf);

      ByteBuffer byteBuffer = ByteBuffer.wrap(shpBuf);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
      fileReaderPrj = file.fileSystem().openPossiblyCompressedStream(hadoopPrj);
      byte[] prjBuf = new byte[fileReaderPrj.available()];
      fileReaderPrj.read(prjBuf);
      fileReaderPrj.close();

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
          try { sleep(1_000); } catch (final Exception ex) { /* ignore interruptions */ }
        }
        try { outErr.delete(); } catch (final Exception ex) { } // cleanup - remove err msg file
      }

```

### IgnoreResultOfCall
Result of `TypeProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        internal_static_exec_shared_SaslMessage_descriptor,
        new java.lang.String[] { "Mechanism", "Data", "Status", });
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
```

### IgnoreResultOfCall
Result of `CoordinationProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        new java.lang.String[] { "Mechanism", "Data", "Status", });
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
  }
```

### IgnoreResultOfCall
Result of `SchemaDefProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.CoordinationProtos.getDescriptor();
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
  }

```

### IgnoreResultOfCall
Result of `SchemaDefProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        internal_static_exec_user_RunQuery_descriptor,
        new java.lang.String[] { "ResultsMode", "Type", "Plan", "Fragments", "PreparedStatementHandle", "AutolimitRowcount", });
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
```

### IgnoreResultOfCall
Result of `TypeProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        new java.lang.String[] { "ResultsMode", "Type", "Plan", "Fragments", "PreparedStatementHandle", "AutolimitRowcount", });
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
    org.apache.drill.exec.proto.BitData.getDescriptor();
```

### IgnoreResultOfCall
Result of `UserBitShared.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    org.apache.drill.exec.proto.SchemaDefProtos.getDescriptor();
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
    org.apache.drill.exec.proto.BitData.getDescriptor();
    org.apache.drill.exec.proto.BitControl.getDescriptor();
```

### IgnoreResultOfCall
Result of `BitData.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    org.apache.drill.common.types.TypeProtos.getDescriptor();
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
    org.apache.drill.exec.proto.BitData.getDescriptor();
    org.apache.drill.exec.proto.BitControl.getDescriptor();
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
```

### IgnoreResultOfCall
Result of `BitControl.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    org.apache.drill.exec.proto.UserBitShared.getDescriptor();
    org.apache.drill.exec.proto.BitData.getDescriptor();
    org.apache.drill.exec.proto.BitControl.getDescriptor();
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
  }
```

### IgnoreResultOfCall
Result of `ExecProtos.getDescriptor()` is ignored
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    org.apache.drill.exec.proto.BitData.getDescriptor();
    org.apache.drill.exec.proto.BitControl.getDescriptor();
    org.apache.drill.exec.proto.ExecProtos.getDescriptor();
  }

```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `expectedWidth()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/AbstractMapColumnMetadata.java`
#### Snippet
```java

  @Override
  public int expectedWidth() {
    return 0;
  }
```

### RedundantMethodOverride
Method `toNullable()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java

  @Override
  public void toNullable(ValueVector nullableVector) {
    throw new UnsupportedOperationException();
  }
```

### RedundantMethodOverride
Method `getField()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java

  @Override
  public MaterializedField getField() { return field; }

  @Override
```

### RedundantMethodOverride
Method `isNull()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/BitVector.java`
#### Snippet
```java

    @Override
    public boolean isNull(int index) {
      return false;
    }
```

### RedundantMethodOverride
Method `reset()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/UntypedNullVector.java`
#### Snippet
```java

  @Override
  public void reset() { }

  /**
```

### RedundantMethodOverride
Method `reset()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/MapVector.java`
#### Snippet
```java

    @Override
    public void reset() { }

    @Override
```

### RedundantMethodOverride
Method `generateTestData()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/MapVector.java`
#### Snippet
```java

    @Override
    public void generateTestData(int values) { }
  }

```

### RedundantMethodOverride
Method `isNull()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/BaseRepeatedValueVector.java`
#### Snippet
```java

    @Override
    public boolean isNull(int index) {
      return false;
    }
```

### RedundantMethodOverride
Method `getDataVector()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/ListVector.java`
#### Snippet
```java

  @Override
  public ValueVector getDataVector() { return vector; }

  public ValueVector getBitsVector() { return bits; }
```

### RedundantMethodOverride
Method `reposition()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/OffsetVectorReader.java`
#### Snippet
```java

  @Override
  public void reposition() { }

  @Override
```

### RedundantMethodOverride
Method `isProjected()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/MapWriter.java`
#### Snippet
```java

    @Override
    public boolean isProjected() { return true; }
  }

```

### RedundantMethodOverride
Method `isProjected()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/MapWriter.java`
#### Snippet
```java

    @Override
    public boolean isProjected() { return true; }
  }

```

### RedundantMethodOverride
Method `isProjected()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/DictEntryWriter.java`
#### Snippet
```java

  @Override
  public boolean isProjected() {
    return true;
  }
```

### RedundantMethodOverride
Method `bindListener()` is identical to its super method
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/dummy/DummyScalarWriter.java`
#### Snippet
```java

  @Override
  public void bindListener(ColumnWriterListener listener) { }

  @Override
```

### RedundantMethodOverride
Method `supportsWrite()` is identical to its super method
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidStoragePlugin.java`
#### Snippet
```java

  @Override
  public boolean supportsWrite() {
    return false;
  }
```

### RedundantMethodOverride
Method `getSubSchemaNames()` is identical to its super method
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/schema/DruidSchemaFactory.java`
#### Snippet
```java

    @Override
    public Set<String> getSubSchemaNames() {
      return Collections.emptySet();
    }
```

### RedundantMethodOverride
Method `getSubSchemaNames()` is identical to its super method
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseSchemaFactory.java`
#### Snippet
```java

    @Override
    public Set<String> getSubSchemaNames() {
      return Collections.emptySet();
    }
```

### RedundantMethodOverride
Method `getRemoteAddress()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServer.java`
#### Snippet
```java

    @Override
    public SocketAddress getRemoteAddress() {
      return getChannel().remoteAddress();
    }
```

### RedundantMethodOverride
Method `isNested()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillSimpleFuncHolder.java`
#### Snippet
```java

  @Override
  public boolean isNested() {
    return false;
  }
```

### RedundantMethodOverride
Method `visitFunctionCall()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java

    @Override
    public HoldingContainer visitFunctionCall(FunctionCall e, ClassGenerator<?> generator) throws RuntimeException {
      throw new UnsupportedOperationException("FunctionCall is not expected here. "
          + "It should have been converted to FunctionHolderExpression in materialization");
```

### RedundantMethodOverride
Method `hasFiles()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyGroupScan.java`
#### Snippet
```java

  @Override
  public boolean hasFiles() {
    return true;
  }
```

### RedundantMethodOverride
Method `readStatistics()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JSONFormatPlugin.java`
#### Snippet
```java

  @Override
  public TableStatistics readStatistics(FileSystem fs, Path statsTablePath) throws IOException {
    throw new UnsupportedOperationException("unimplemented");
  }
```

### RedundantMethodOverride
Method `getGroupScan()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/TextFormatPlugin.java`
#### Snippet
```java

  @Override
  public AbstractGroupScan getGroupScan(String userName, FileSelection selection,
      List<SchemaPath> columns, MetadataProviderManager metadataProviderManager)
      throws IOException {
```

### RedundantMethodOverride
Method `clone()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/direct/MetadataDirectGroupScan.java`
#### Snippet
```java

  @Override
  public GroupScan clone(List<SchemaPath> columns) {
    return this;
  }
```

### RedundantMethodOverride
Method `getConfigProperty()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/TypeValidators.java`
#### Snippet
```java

    @Override
    public String getConfigProperty() {
      return ExecConstants.bootDefaultFor(getOptionName());
    }
```

### RedundantMethodOverride
Method `getSupportedEncodings()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/BroadcastExchangePrel.java`
#### Snippet
```java

  @Override
  public SelectionVectorMode[] getSupportedEncodings() {
    return SelectionVectorMode.DEFAULT;
  }
```

### RedundantMethodOverride
Method `iterator()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/RowKeyJoinPrel.java`
#### Snippet
```java

  @Override
  public Iterator<Prel> iterator() {
    return PrelUtil.iter(getLeft(), getRight());
  }
```

### RedundantMethodOverride
Method `getSVMode()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractSubScan.java`
#### Snippet
```java

  @Override
  public SelectionVectorMode getSVMode() {
    return SelectionVectorMode.NONE;
  }
```

### RedundantMethodOverride
Method `isExecutable()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractSubScan.java`
#### Snippet
```java

  @Override
  public boolean isExecutable() {
    return true;
  }
```

### RedundantMethodOverride
Method `getSelectionVector2()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java

  @Override
  public SelectionVector2 getSelectionVector2() {
    throw new UnsupportedOperationException();
  }
```

### RedundantMethodOverride
Method `cancelIncoming()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoinBatch.java`
#### Snippet
```java

  @Override
  protected void cancelIncoming() {
    left.cancel();
    right.cancel();
```

### RedundantMethodOverride
Method `cancelIncoming()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java

  @Override
  protected void cancelIncoming() {
    left.cancel();
    right.cancel();
```

### RedundantMethodOverride
Method `name()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ResolvedTuple.java`
#### Snippet
```java

    @Override
    public String name() {
      return parentColumn.name();
    }
```

### RedundantMethodOverride
Method `name()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ResolvedTuple.java`
#### Snippet
```java

    @Override
    public String name() {
      return parentColumn.name();
    }
```

### RedundantMethodOverride
Method `getSelectionVector2()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortBatch.java`
#### Snippet
```java

  @Override
  public SelectionVector2 getSelectionVector2() {
    throw new UnsupportedOperationException();
  }
```

### RedundantMethodOverride
Method `getContext()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/FilterRecordBatch.java`
#### Snippet
```java

  @Override
  public FragmentContext getContext() {
    return context;
  }
```

### RedundantMethodOverride
Method `getContext()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/RuntimeFilterRecordBatch.java`
#### Snippet
```java

  @Override
  public FragmentContext getContext() {
    return context;
  }
```

### RedundantMethodOverride
Method `getWritableBatch()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/svremover/RemovingRecordBatch.java`
#### Snippet
```java

  @Override
  public WritableBatch getWritableBatch() {
    return WritableBatch.get(this);
  }
```

### RedundantMethodOverride
Method `getOrderings()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/TopN.java`
#### Snippet
```java

  @Override
  public List<Ordering> getOrderings() {
    return orderings;
  }
```

### RedundantMethodOverride
Method `getReverse()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/TopN.java`
#### Snippet
```java

  @Override
  public boolean getReverse() {
    return reverse;
  }
```

### RedundantMethodOverride
Method `getSVMode()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/SelectionVectorRemover.java`
#### Snippet
```java

  @Override
  public SelectionVectorMode getSVMode() {
    return SelectionVectorMode.NONE;
  }
```

### RedundantMethodOverride
Method `getSVMode()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/PartitionLimit.java`
#### Snippet
```java

  @Override
  public SelectionVectorMode getSVMode() {
    return SelectionVectorMode.TWO_BYTE;
  }
```

### RedundantMethodOverride
Method `allocate()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java

    @Override
    public int allocate(int cardinality) {
      return offsets.allocate(cardinality);
    }
```

### RedundantMethodOverride
Method `dump()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java

    @Override
    public void dump(HierarchicalFormatter format) {
      format.startObject(this)
          .attribute("column", parentColumn.schema().name())
```

### RedundantMethodOverride
Method `getSelectorType()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectors/TagSelector.java`
#### Snippet
```java

  @Override
  public SelectorType getSelectorType() {
    return SELECTOR_TYPE;
  }
```

### RedundantMethodOverride
Method `getSelectorType()` is identical to its super method
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectors/DefaultSelector.java`
#### Snippet
```java

  @Override
  public SelectorType getSelectorType() {
    return SELECTOR_TYPE;
  }
```

### RedundantMethodOverride
Method `getTableNames()` is identical to its super method
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/schema/GoogleSheetsDrillSchema.java`
#### Snippet
```java

  @Override
  public Set<String> getTableNames() {
    return Collections.emptySet();
  }
```

### RedundantMethodOverride
Method `iterator()` is identical to its super method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBSubScan.java`
#### Snippet
```java

  @Override
  public Iterator<PhysicalOperator> iterator() {
    return Collections.emptyIterator();
  }
```

### RedundantMethodOverride
Method `supportsAutoPartitioning()` is identical to its super method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/streams/StreamsFormatPlugin.java`
#### Snippet
```java

  @Override
  public boolean supportsAutoPartitioning() {
    return false;
  }
```

### RedundantMethodOverride
Method `getWriter()` is identical to its super method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/streams/StreamsFormatPlugin.java`
#### Snippet
```java

  @Override
  public AbstractWriter getWriter(PhysicalOperator child, String location,
      List<String> partitionColumns) throws IOException {
    throw new UnsupportedOperationException();
```

### RedundantMethodOverride
Method `supportsWrite()` is identical to its super method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/streams/StreamsFormatPlugin.java`
#### Snippet
```java

  @Override
  public boolean supportsWrite() {
    return false;
  }
```

### RedundantMethodOverride
Method `supportsRead()` is identical to its super method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/streams/StreamsFormatPlugin.java`
#### Snippet
```java

  @Override
  public boolean supportsRead() {
    return true;
  }
```

### RedundantMethodOverride
Method `getSubSchemaNames()` is identical to its super method
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduSchemaFactory.java`
#### Snippet
```java

    @Override
    public Set<String> getSubSchemaNames() {
      return Collections.emptySet();
    }
```

### RedundantMethodOverride
Method `getValue()` is identical to its super method
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/statistics/TableStatisticsKind.java`
#### Snippet
```java

        @Override
        public MetadataType getValue(Metadata metadata) {
          return metadata.getStatistic(this);
        }
```

### RedundantMethodOverride
Method `showInInformationSchema()` is identical to its super method
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkSchema.java`
#### Snippet
```java

  @Override
  public boolean showInInformationSchema() {
    return true;
  }
```

### RedundantMethodOverride
Method `showInInformationSchema()` is identical to its super method
in `contrib/storage-drill/src/main/java/org/apache/drill/exec/store/drill/plugin/schema/DrillPluginSchema.java`
#### Snippet
```java

  @Override
  public boolean showInInformationSchema() {
    return true;
  }
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
pollWaitSec \* 1000: integer multiplication implicitly cast to long
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StopCommand.java`
#### Snippet
```java
    private boolean poll() throws ClientException {
      try {
        Thread.sleep(pollWaitSec * 1000);
      } catch (InterruptedException e) {
        return false;
```

### IntegerMultiplicationImplicitCastToLong
pollWaitSec \* 1000: integer multiplication implicitly cast to long
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java
    private boolean poll() throws ClientException {
      try {
        Thread.sleep(pollWaitSec * 1000);
      } catch (InterruptedException e) {
        return false;
```

### IntegerMultiplicationImplicitCastToLong
timeoutSec \* 1000: integer multiplication implicitly cast to long
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
        return;
      }
      if (task.stateStartTime + timeoutSec * 1000 > curTime) {
        return;
      }
```

### IntegerMultiplicationImplicitCastToLong
clazz.fields().size() \* 2: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
   */
  private boolean hasMaxIndexValue() {
    return index + clazz.fields().size() * 2 / 3 > maxIndex;
  }

```

### IntegerMultiplicationImplicitCastToLong
rand.nextInt(365) \* ONE_DAY: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java
  @Override
  public void setValue() {
    long randTime = baseTime + baseTime + rand.nextInt(365) * ONE_DAY;
    String str = fmt.format(new Date(randTime));
    colWriter.setString(str);
```

### IntegerMultiplicationImplicitCastToLong
4 \* result.getNumNonNullValues(): integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
        // Page read position is meaningful only when dictionary mode is off
        if (!pageInfo.encodedValueReader.isDefined()) {
          oprReadState.pageReadPos += (result.getTotalLength() + 4 * result.getNumNonNullValues());
        }
        oprReadState.numPageFieldsProcessed += result.getNumValues();
```

### IntegerMultiplicationImplicitCastToLong
INT_VALUE_WIDTH \* (valueCount + 1): integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
    //         + next-power-of-two(DT_LEN * valueCount)                    // data storage
    long memoryUsage = BaseAllocator.longNextPowerOfTwo(averagePrecision * valueCount);
    memoryUsage += BaseAllocator.longNextPowerOfTwo(INT_VALUE_WIDTH * (valueCount + 1));

    if (column.getField().isNullable()) {
```

### IntegerMultiplicationImplicitCastToLong
getFixedColumnTypePrecision(column) \* valueCount: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
    //         + next-power-of-two(DT_LEN * valueCount)                    // data storage

    long memoryUsage = BaseAllocator.longNextPowerOfTwo(getFixedColumnTypePrecision(column) * valueCount);

    if (column.getField().isNullable()) {
```

### IntegerMultiplicationImplicitCastToLong
BYTE_VALUE_WIDTH \* valueCount: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java

    if (column.getField().isNullable()) {
      memoryUsage += BaseAllocator.longNextPowerOfTwo(BYTE_VALUE_WIDTH * valueCount);
    }

```

### IntegerMultiplicationImplicitCastToLong
currValueCount \* BYTE_VALUE_WIDTH: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
          vectorMemoryUsage.offsetsByteCapacity = values.getOffsetVector().getValueCapacity() * INT_VALUE_WIDTH;
          vectorMemoryUsage.dataByteCapacity = values.getByteCapacity();
          vectorMemoryUsage.bitsBytesUsed = currValueCount * BYTE_VALUE_WIDTH;
          vectorMemoryUsage.offsetsBytesUsed = values.getOffsetVector().getPayloadByteCount(currValueCount);
          vectorMemoryUsage.dataBytesUsed = values.getPayloadByteCount(currValueCount) - vectorMemoryUsage.offsetsBytesUsed;
```

### IntegerMultiplicationImplicitCastToLong
currValueCount \* BYTE_VALUE_WIDTH: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
          vectorMemoryUsage.offsetsByteCapacity = values.getOffsetVector().getValueCapacity() * INT_VALUE_WIDTH;
          vectorMemoryUsage.dataByteCapacity = values.getByteCapacity();
          vectorMemoryUsage.bitsBytesUsed = currValueCount * BYTE_VALUE_WIDTH;
          vectorMemoryUsage.offsetsBytesUsed = values.getOffsetVector().getPayloadByteCount(currValueCount);
          vectorMemoryUsage.dataBytesUsed = values.getPayloadByteCount(currValueCount) - vectorMemoryUsage.offsetsBytesUsed;
```

### IntegerMultiplicationImplicitCastToLong
currValueCount \* BYTE_VALUE_WIDTH: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
          vectorMemoryUsage.offsetsByteCapacity = values.getOffsetVector().getValueCapacity() * INT_VALUE_WIDTH;
          vectorMemoryUsage.dataByteCapacity = values.getByteCapacity();
          vectorMemoryUsage.bitsBytesUsed = currValueCount * BYTE_VALUE_WIDTH;
          vectorMemoryUsage.offsetsBytesUsed = values.getOffsetVector().getPayloadByteCount(currValueCount);
          vectorMemoryUsage.dataBytesUsed = values.getPayloadByteCount(currValueCount) - vectorMemoryUsage.offsetsBytesUsed;
```

### IntegerMultiplicationImplicitCastToLong
MIN_COLUMN_MEMORY_SZ \* numColumns: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchSizerManager.java`
#### Snippet
```java
    if (memorySizePerColumn < MIN_COLUMN_MEMORY_SZ) {
      final long prevValue   = normalizedMemorySize;
      normalizedMemorySize  = MIN_COLUMN_MEMORY_SZ * numColumns;

      final String message = String.format("The Parquet memory per batch [%d] byte(s) is too low for this query ; using [%d] bytes",
```

### IntegerMultiplicationImplicitCastToLong
outputPosition \* previousOutgoingWidth: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java
    // calculate memory used so far based on previous outgoing row width and how many rows we already processed.
    final int previousOutgoingWidth = getOutgoingRowWidth();
    final long memoryUsed = outputPosition * previousOutgoingWidth;

    final int configOutputBatchSize = getOutputBatchSize();
```

### IntegerMultiplicationImplicitCastToLong
outgoingRowWidth \* outputRecords: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchMemoryManager.java`
#### Snippet
```java
    outputBatchStats.incNumBatches();
    outputBatchStats.incTotalRecords(outputRecords);
    outputBatchStats.incSumBatchSizes(outgoingRowWidth * outputRecords);
  }

```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.maxBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // Finally, figure out when we must spill.

    bufferMemoryLimit = memoryLimit - 2 * spillBatchSize.maxBufferSize;
    bufferMemoryLimit = Math.max(bufferMemoryLimit, 0);

```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.maxBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // The rest can be give to the merge batch.

    long mergeBufferSize = memoryLimit - 2 * spillBatchSize.maxBufferSize;

    // The above calcs assume that the merge batch size is the same as
```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.expectedBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java

    mergeBatchRowCount = rowsPerBatch(mergeBatchSize.dataSize);
    mergeMemoryLimit = Math.max(2 * spillBatchSize.expectedBufferSize, memoryLimit - mergeBatchSize.maxBufferSize);
  }

```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.maxBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // so use the buffer size for the spill batch.

    bufferMemoryLimit = memoryLimit - 2 * spillBatchSize.maxBufferSize;
  }

```

### IntegerMultiplicationImplicitCastToLong
2 \* inputBatchSize.expectedBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // shows that spilled batches have close to the maximum fragmentation.

    long loadHeadroom = bufferMemoryLimit - 2 * inputBatchSize.expectedBufferSize;
    long mergeHeadroom = mergeMemoryLimit - 2 * spillBatchSize.maxBufferSize;
    isLowMemory = (loadHeadroom < 0  |  mergeHeadroom < 0);
```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.maxBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java

    long loadHeadroom = bufferMemoryLimit - 2 * inputBatchSize.expectedBufferSize;
    long mergeHeadroom = mergeMemoryLimit - 2 * spillBatchSize.maxBufferSize;
    isLowMemory = (loadHeadroom < 0  |  mergeHeadroom < 0);
    if (! isLowMemory) {
```

### IntegerMultiplicationImplicitCastToLong
2 \* inputBatchSize.expectedBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // configured terribly wrong.

    long minNeeds = 2 * inputBatchSize.expectedBufferSize + spillBatchSize.maxBufferSize;
    if (minNeeds > memoryLimit) {
      logger.warn("Potential memory overflow during load phase! " +
```

### IntegerMultiplicationImplicitCastToLong
2 \* spillBatchSize.expectedBufferSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
    // Sanity check

    minNeeds = 2 * spillBatchSize.expectedBufferSize + mergeBatchSize.expectedBufferSize;
    if (minNeeds > memoryLimit) {
      logger.warn("Potential memory overflow during merge phase! " +
```

### IntegerMultiplicationImplicitCastToLong
MAX_BATCH_ROW_COUNT \* 2 \* 2 \* 4 \* maxSize: integer multiplication implicitly cast to long
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
        maxSize = Math.max(maxSize, batchHolders[insp].size());
      }
      maxMemoryNeeded += MAX_BATCH_ROW_COUNT * 2 * 2 * 4 * maxSize; // 2 - double, 2 - max when %50 full, 4 - Uint4

      // log a detailed debug message explaining why a spill may be needed
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import org.apache.drill.exec.vector.accessor.ScalarWriter;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/package-info.java`
#### Snippet
```java
package org.apache.drill.exec.physical.impl.scan.convert;

import org.apache.drill.exec.vector.accessor.ScalarWriter;

```

## RuleId[id=ReturnFromFinallyBlock]
### ReturnFromFinallyBlock
'return' inside 'finally' block
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/TableStatsCalculator.java`
#### Snippet
```java
      } finally {
        if (clusterStatus == null) {
          return;
        }
      }
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setError(createPBError("get tables", e));
      } finally {
        return new Response(RpcType.TABLES, respBuilder.build());
      }
    }
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setError(createPBError("get columns", e));
      } finally {
        return new Response(RpcType.COLUMNS, respBuilder.build());
      }
    }
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setError(createPBError("get schemas", e));
      } finally {
        return new Response(RpcType.SCHEMAS, respBuilder.build());
      }
    }
```

### ReturnFromFinallyBlock
'return' inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setError(createPBError("get catalogs", e));
      } finally {
        return new Response(RpcType.CATALOGS, respBuilder.build());
      }
    }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `nodeMap` are updated, but never queried
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
  private boolean failed;

  private Map<String, String> nodeMap = new HashMap<>();

  /**
```

### MismatchedCollectionQueryUpdate
Contents of collection `children` are updated, but never queried
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperatorBase.java`
#### Snippet
```java
public abstract class LogicalOperatorBase implements LogicalOperator{

  private final List<LogicalOperator> children = new ArrayList<>();

  private String memo;
```

### MismatchedCollectionQueryUpdate
Contents of collection `incomplete` are queried, but never updated
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));
    final List<MinorFragmentProfile> incomplete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.missingOperatorsOrTimes));

```

### MismatchedCollectionQueryUpdate
Contents of collection `complete` are queried, but never updated
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    // Use only minor fragments that have complete profiles
    // Complete iff the fragment profile has at least one operator profile, and start and end times.
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));

```

### MismatchedCollectionQueryUpdate
Contents of collection `complete` are queried, but never updated
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    // Use only minor fragments that have complete profiles
    // Complete iff the fragment profile has at least one operator profile, and start and end times.
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));

```

### MismatchedCollectionQueryUpdate
Contents of collection `fieldMap` are queried, but never updated
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
            return number == null ? 0 : number.intValue();
        }
        private static final java.util.HashMap<java.lang.String,java.lang.Integer> fieldMap = new java.util.HashMap<java.lang.String,java.lang.Integer>();
        static
        {
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`entry & 0xFFFF_FFFF` can be replaced with 'entry'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
  public void reposition() {
    long entry = offsetReader.getEntry();
    elementIndex.reset((int) (entry >> 32), (int) (entry & 0xFFFF_FFFF));
  }

```

### PointlessBitwiseExpression
`val & 0x00FFFFFFFF` can be replaced with 'val'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/XXHash.java`
#### Snippet
```java
   */
  private static int convert64To32(long val) {
    return (int) (val & 0x00FFFFFFFF);
  }

```

### PointlessBitwiseExpression
`longValue ^ -1L` can be replaced with '\~longValue'
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ByteBufUtil.java`
#### Snippet
```java
        longValue = longValue | (byteValue & 0xFF);
      }
      return (isNegativeVInt(firstByte) ? (longValue ^ -1L) : longValue);
    }

```

### PointlessBitwiseExpression
`ch4 << 0` can be replaced with 'ch4'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
    int ch2 = in.getByte(offset + 2) & 0xff;
    int ch1 = in.getByte(offset + 3) & 0xff;
    return ((ch1 << 24) + (ch2 << 16) + (ch3 << 8) + (ch4 << 0));
  }

```

### PointlessBitwiseExpression
`~(alignSize - 1)` can be replaced with '-alignSize'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java
   */
  private static long alignSizeUp(final long size, final int alignSize) {
    return (size + alignSize - 1) & ~(alignSize - 1);
  }

```

## RuleId[id=TextLabelInSwitchStatement]
### TextLabelInSwitchStatement
Text label `outer:` in 'switch' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenTemplate.java`
#### Snippet
```java
        int innerValueIndexLocal = innerValueIndex;
        int currentInnerValueIndexLocal = currentInnerValueIndex;
        outer: {
          int outputIndex = firstOutputIndex;
          int recordsThisCall = 0;
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/vector/src/main/java/org/apache/drill/exec/util/SerializationModule.java`
#### Snippet
```java
    drillModule.addSerializer(LocalDateTime.class, new JsonSerializer<LocalDateTime>() {
      @Override
      public void serialize(LocalDateTime value, JsonGenerator gen, SerializerProvider serializers) throws IOException, JsonProcessingException {
        gen.writeString(formatTimeStamp.format(value));
      }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/vector/src/main/java/org/apache/drill/exec/util/SerializationModule.java`
#### Snippet
```java
    drillModule.addSerializer(LocalTime.class, new JsonSerializer<LocalTime>() {
      @Override
      public void serialize(LocalTime value, JsonGenerator gen, SerializerProvider serializers) throws IOException, JsonProcessingException {
        gen.writeString(formatTime.format(value));
      }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/vector/src/main/java/org/apache/drill/exec/util/SerializationModule.java`
#### Snippet
```java
    drillModule.addSerializer(LocalDate.class, new JsonSerializer<LocalDate>() {
      @Override
      public void serialize(LocalDate value, JsonGenerator gen, SerializerProvider serializers) throws IOException, JsonProcessingException {
        gen.writeString(formatDate.format(value));
      }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/vector/src/main/java/org/apache/drill/exec/util/Text.java`
#### Snippet
```java
    @Override
    public void serialize(Text text, JsonGenerator jsonGenerator, SerializerProvider serializerProvider)
        throws IOException, JsonGenerationException {
      jsonGenerator.writeString(text.toString());
    }
```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillConnection.java`
#### Snippet
```java
   */
  @Override
  void setAutoCommit(boolean autoCommit) throws SQLFeatureNotSupportedException,
                                                SQLException;
  /**
```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillConnection.java`
#### Snippet
```java
   */
  @Override
  void setTransactionIsolation(int level) throws SQLFeatureNotSupportedException,
                                                 SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  double getDouble(String columnLabel) throws SQLConversionOverflowException,
                                              SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  byte getByte(String columnLabel) throws SQLConversionOverflowException,
                                          SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  int getInt(int columnIndex) throws SQLConversionOverflowException,
                                     SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  double getDouble(int columnIndex) throws SQLConversionOverflowException,
                                           SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  float getFloat(String columnLabel) throws SQLConversionOverflowException,
                                            SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  long getLong(String columnLabel) throws SQLConversionOverflowException,
                                          SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  short getShort(int columnIndex) throws SQLConversionOverflowException,
                                         SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  byte getByte(int columnIndex) throws SQLConversionOverflowException,
                                       SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  int getInt(String columnLabel) throws SQLConversionOverflowException,
                                        SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  short getShort(String columnLabel) throws SQLConversionOverflowException,
                                            SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  float getFloat(int columnIndex) throws SQLConversionOverflowException,
                                         SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.sql.SQLException', in the throws list already.
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillResultSet.java`
#### Snippet
```java
   */
  @Override
  long getLong(int columnIndex) throws SQLConversionOverflowException,
                                       SQLException;

```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/JSONOptions.java`
#### Snippet
```java
    @Override
    public void serialize(JSONOptions value, JsonGenerator jgen, SerializerProvider provider) throws IOException,
        JsonGenerationException {
      if (value.opaque != null) {
        jgen.writeObject(value.opaque);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/JSONOptions.java`
#### Snippet
```java
    @Override
    public JSONOptions deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      JsonLocation l = jp.getTokenLocation();
//      logger.debug("Reading tree.");
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/expression/FieldReference.java`
#### Snippet
```java
    @Override
    public void serialize(FieldReference value, JsonGenerator jgen, SerializerProvider provider) throws IOException,
        JsonGenerationException {
      jgen.writeString('`' + value.getRootSegment().getNameSegment().getPath() + '`');
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/expression/FieldReference.java`
#### Snippet
```java
    @Override
    public FieldReference deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      String ref = this._parseString(jp, ctxt);
      ref = ref.replace("`", "");
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
    @Override
    public void serialize(DataType value, JsonGenerator jgen, SerializerProvider provider) throws IOException,
        JsonGenerationException {
      jgen.writeString(value.getName());
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java
    @Override
    public DataType deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      return getDataType(this._parseString(jp, ctxt));
    }
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
    }

    public RECEIVE get(long timeout, TimeUnit unit) throws Exception, InvalidProtocolBufferException {
      CustomMessage message = future.checkedGet(timeout, unit);
      return serde.deserializeReceived(message.getMessage().toByteArray());
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/DrillbitEndpointSerDe.java`
#### Snippet
```java
    @Override
    public DrillbitEndpoint deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      return DrillbitEndpoint.parseFrom(jp.getBinaryValue());
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/DrillbitEndpointSerDe.java`
#### Snippet
```java
    @Override
    public void serialize(DrillbitEndpoint value, JsonGenerator jgen, SerializerProvider provider) throws IOException,
        JsonGenerationException {
      jgen.writeBinary(value.toByteArray());
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public void access(final Path path, final FsAction mode) throws AccessControlException, FileNotFoundException, IOException {
    underlyingFs.access(path, mode);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public FileStatus[] listStatus(Path[] files) throws FileNotFoundException, IOException {
    return underlyingFs.listStatus(files);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public RemoteIterator<LocatedFileStatus> listLocatedStatus(Path f) throws FileNotFoundException, IOException {
    return underlyingFs.listLocatedStatus(f);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public FileStatus[] listStatus(Path f, PathFilter filter) throws FileNotFoundException, IOException {
    return underlyingFs.listStatus(f, filter);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public FileStatus[] listStatus(Path f) throws FileNotFoundException, IOException {
    return underlyingFs.listStatus(f);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public RemoteIterator<LocatedFileStatus> listFiles(Path f, boolean recursive) throws FileNotFoundException, IOException {
    return underlyingFs.listFiles(f, recursive);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public FileStatus[] listStatus(Path[] files, PathFilter filter) throws FileNotFoundException, IOException {
    return underlyingFs.listStatus(files, filter);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  @Override
  public RemoteIterator<FileStatus> listStatusIterator(final Path p) throws FileNotFoundException, IOException {
    return underlyingFs.listStatusIterator(p);
  }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
        @Override
        public Object deserializeKey(String key, com.fasterxml.jackson.databind.DeserializationContext ctxt)
            throws IOException, com.fasterxml.jackson.core.JsonProcessingException {
          return new Key(key.split("\\."));
        }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
      @Override
      public void serialize(ColumnMetadata_v2 value, JsonGenerator jgen, SerializerProvider provider)
          throws IOException, JsonProcessingException {
        jgen.writeStartObject();
        jgen.writeArrayFieldStart("name");
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
    public static class DeSerializer extends JsonDeserializer<ColumnMetadata_v2> {
      @Override public ColumnMetadata_v2 deserialize(JsonParser jp, DeserializationContext ctxt)
          throws IOException, JsonProcessingException {
        return null;
      }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/MajorTypeSerDe.java`
#### Snippet
```java
    @Override
    public MajorType deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException,
        JsonProcessingException {
      return jp.readValueAs(MajorTypeHolder.class).getMajorType();
    }
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/MajorTypeSerDe.java`
#### Snippet
```java
    @Override
    public void serialize(MajorType value, JsonGenerator jgen, SerializerProvider provider) throws IOException,
        JsonGenerationException {
      MajorTypeHolder holder = MajorTypeHolder.get(value);
      jgen.writeObject(holder);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonWriter.java`
#### Snippet
```java
  }

  public void write(FieldReader reader) throws JsonGenerationException, IOException{
    writeValue(reader);
    gen.flush();
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonWriter.java`
#### Snippet
```java
  }

  private void writeValue(FieldReader reader) throws JsonGenerationException, IOException{
    final DataMode m = reader.getType().getMode();
    final MinorType mt = reader.getType().getMinorType();
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/InjectionSite.java`
#### Snippet
```java
    @Override
    public Object deserializeKey(final String key, final DeserializationContext context)
      throws IOException, JsonProcessingException {
      final String[] fields = key.split(SEPARATOR);
      final Class<?> siteClass;
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+=` in loop
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveReadEntry.java`
#### Snippet
```java

    for (String value: partition.values) {
      partitionPath += "/" + value;
    }

```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
        for (String namePart : name) {
          if (s != null) {
            s += ".";
            s += namePart;
          } else {
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `maxRecordsToRead` initializer `-1` is redundant
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidRecordReader.java`
#### Snippet
```java
  private final DruidFilter filter;
  private BigInteger nextOffset = BigInteger.ZERO;
  private int maxRecordsToRead = -1;

  private JsonReader jsonReader;
```

### UnusedAssignment
Variable `columns` initializer `null` is redundant
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseScanBatchCreator.java`
#### Snippet
```java
    Preconditions.checkArgument(children.isEmpty());
    List<RecordReader> readers = new LinkedList<>();
    List<SchemaPath> columns = null;
    for(HBaseSubScan.HBaseSubScanSpec scanSpec : subScan.getRegionScanSpecList()){
      try {
```

### UnusedAssignment
Variable `matcher` initializer `null` is redundant
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseRegexParser.java`
#### Snippet
```java
    }

    Matcher matcher = null;
    StringBuilder prefixSB = new StringBuilder();
    StringBuilder regexSB = new StringBuilder("^"); // starts with
```

### UnusedAssignment
Variable `fragment` initializer `null` is redundant
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseRegexParser.java`
#### Snippet
```java
        .matcher(likeString_);
    }
    String fragment = null;
    boolean literalsSoFar = true;
    while (matcher.find()) {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseRecordReader.java`
#### Snippet
```java
    // if allocated memory for the first row is larger than allowed max in batch, it will be added anyway
    do {
      Result result = null;
      final OperatorStats operatorStats = operatorContext == null ? null : operatorContext.getStats();
      try {
```

### UnusedAssignment
The value `java.sql.Connection.class` assigned to `someReference` is never used
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    @SuppressWarnings("unused")
    Class<?> someReference;
    someReference = java.sql.Connection.class;
    someReference = javax.sql.PooledConnection.class;
    someReference = javax.sql.rowset.BaseRowSet.class;
```

### UnusedAssignment
The value `javax.sql.PooledConnection.class` assigned to `someReference` is never used
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    Class<?> someReference;
    someReference = java.sql.Connection.class;
    someReference = javax.sql.PooledConnection.class;
    someReference = javax.sql.rowset.BaseRowSet.class;
    someReference = javax.sql.rowset.serial.SerialJavaObject.class;
```

### UnusedAssignment
The value `javax.sql.rowset.BaseRowSet.class` assigned to `someReference` is never used
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    someReference = java.sql.Connection.class;
    someReference = javax.sql.PooledConnection.class;
    someReference = javax.sql.rowset.BaseRowSet.class;
    someReference = javax.sql.rowset.serial.SerialJavaObject.class;
    someReference = javax.sql.rowset.spi.SyncFactory.class;
```

### UnusedAssignment
The value `javax.sql.rowset.serial.SerialJavaObject.class` assigned to `someReference` is never used
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    someReference = javax.sql.PooledConnection.class;
    someReference = javax.sql.rowset.BaseRowSet.class;
    someReference = javax.sql.rowset.serial.SerialJavaObject.class;
    someReference = javax.sql.rowset.spi.SyncFactory.class;

```

### UnusedAssignment
The value `javax.sql.rowset.spi.SyncFactory.class` assigned to `someReference` is never used
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    someReference = javax.sql.rowset.BaseRowSet.class;
    someReference = javax.sql.rowset.serial.SerialJavaObject.class;
    someReference = javax.sql.rowset.spi.SyncFactory.class;

    Set<Package> set = new HashSet<>();
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
    // Next, call the method with the arguments provided.

    Object ret = null;
    try {
      ret = m.invoke(target, args);
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/SimpleRestClient.java`
#### Snippet
```java
          new InputStreamReader(response.getEntity().getContent()));
      StringBuilder buf = new StringBuilder();
      String line = null;
      while ((line = rd.readLine()) != null) {
        buf.append(line);
```

### UnusedAssignment
Variable `trackingUrl` initializer `null` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java

    dispatcher.setHttpPort(config.getInt(DrillOnYarnConfig.HTTP_PORT));
    String trackingUrl = null;
    if (config.getBoolean(DrillOnYarnConfig.HTTP_ENABLED)) {
      trackingUrl = "http://<host>:<port>/redirect";
```

### UnusedAssignment
Variable `response` initializer `null` is redundant
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
    // Build the request object
    Request request = requestBuilder.build();
    Response response = null;

    try {
```

### UnusedAssignment
Variable `autoLimitRowCount` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java
    int sessionMaxRowCount = queryOptions.getOption(ExecConstants.QUERY_MAX_ROWS).num_val.intValue();
    int defaultMaxRowCount = queryOptions.getOptionManager(OptionScope.SYSTEM).getOption(ExecConstants.QUERY_MAX_ROWS).num_val.intValue();
    int autoLimitRowCount = 0;
    if (sessionMaxRowCount > 0 && defaultMaxRowCount > 0) {
      autoLimitRowCount = Math.min(sessionMaxRowCount, defaultMaxRowCount);
```

### UnusedAssignment
Variable `cause` initializer `e.getCause()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/plain/PlainFactory.java`
#### Snippet
```java
    } catch (final IOException e) {
      logger.debug("Login failed.", e);
      final Throwable cause = e.getCause();
      if (cause instanceof LoginException) {
        throw new SaslException("Failed to login.", cause);
```

### UnusedAssignment
Variable `maprCredentialsProviderClass` initializer `""` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLCredentialsProvider.java`
#### Snippet
```java

  private static Optional<SSLCredentialsProvider> getMaprCredentialsProvider(SSLConfig.Mode mode) {
    String maprCredentialsProviderClass = "";
    switch (mode) {
      case SERVER:
```

### UnusedAssignment
Variable `charLen` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionUtil.java`
#### Snippet
```java
    }

    for (int idx = start, charLen = 0; idx < end; idx += charLen) {
      charLen = utf8CharLen(buffer, idx);
      ++charCount;  //Advance the counter, since we find one char.
```

### UnusedAssignment
Variable `charLen` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionUtil.java`
#### Snippet
```java
    int charCount = 0;

    for (int idx = start, charLen = 0; idx < end; idx += charLen) {
      charLen = utf8CharLen(buffer, idx);
      ++charCount;  //Advance the counter, since we find one char.
```

### UnusedAssignment
Variable `retl` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/DrillHash.java`
#### Snippet
```java

    public static final long getIntLittleEndian(long offset) {
        long retl = 0;
        retl = ((PlatformDependent.getByte(offset+3) &0xffL) << 24)
                | ((PlatformDependent.getByte(offset+2) & 0xffL) << 16)
```

### UnusedAssignment
Variable `arg` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
      generator.nestEvalBlock(eval);  // enter into nested block

      HoldingContainer arg = null;

      JExpression e = null;
```

### UnusedAssignment
Variable `earlyExit` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
        arg = expr.accept(this, generator);

        JBlock earlyExit = null;
        if (arg.isOptional()) {
          JExpression expr1 = arg.getIsSet().eq(JExpr.lit(1));
```

### UnusedAssignment
Variable `arg` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
      generator.nestEvalBlock(eval);   // enter into nested block.

      HoldingContainer arg = null;

      JExpression e = null;
```

### UnusedAssignment
Variable `earlyExit` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
        arg = expr.accept(this, generator);

        JBlock earlyExit = null;
        if (arg.isOptional()) {
          earlyExit = eval._if(arg.getIsSet().eq(JExpr.lit(1)).cand(arg.getValue().eq(JExpr.lit(1))))._then();
```

### UnusedAssignment
Variable `digit` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
    long index = memoryAddress + start;
    final long endIndex = memoryAddress + end;
    int digit = 0;

    // Stores three fields (year, month, day)
```

### UnusedAssignment
The value changed at `dateIndex++` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
    if (dateIndex < 3) {
      // If we reached the end of input, we would have not encountered a separator, store the last value
      dateFields[dateIndex++] = value;
    }

```

### UnusedAssignment
Variable `k1` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java

    int length = 8;
    long k1 = 0;

    k1 = val;
```

### UnusedAssignment
The value `h1` assigned to `h2` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java

    h1 += h2;
    h2 += h1;
    // murmur3_128 should return 128 bit (h1,h2), now we return only 64bits,
    return h1;
```

### UnusedAssignment
Variable `patternIndex` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java
    protected final int match(int start, int end, DrillBuf drillBuf) {
      final int lengthToProcess = end - start - patternLength + 1;
      int patternIndex          = 0;

      // simplePattern string has meta characters i.e % and _ and escape characters removed.
```

### UnusedAssignment
Variable `outputSize` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/output/OutputWidthCalculators.java`
#### Snippet
```java
        @Override
        public int getOutputWidth(List<FixedLenExpr> args) {
            int outputSize = 0;
            if (args == null || args.size() < 1) {
                throw new IllegalArgumentException();
```

### UnusedAssignment
Variable `errMsg` initializer `"Failed to get process user UserGroupInformation object."` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
      return UserGroupInformation.getLoginUser();
    } catch (IOException e) {
      final String errMsg = "Failed to get process user UserGroupInformation object.";
      logger.error(errMsg, e);
      throw new DrillRuntimeException(errMsg, e);
```

### UnusedAssignment
Variable `bytesRead` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
  public synchronized DrillBuf getNext(int bytes) throws IOException {
    DrillBuf b = allocator.buffer(bytes);
    int bytesRead = -1;
    try {
    bytesRead = read(b, 0, bytes);
```

### UnusedAssignment
Variable `enableHints` initializer `true` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
      org.slf4j.LoggerFactory.getLogger(DirectBufInputStream.class);

  protected boolean enableHints = true;
  protected String streamId; // a name for logging purposes only
  protected BufferAllocator allocator;
```

### UnusedAssignment
The value `null` assigned to `readAhead` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
    } catch (ClassNotFoundException e) {
      logger.info("Unable to call fadvise due to: {}", e.toString());
      readAhead = null;
      return;
    }
```

### UnusedAssignment
The value `null` assigned to `readAhead` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
    } catch (NoSuchMethodException e) {
      logger.info("Unable to call fadvise due to: {}", e.toString());
      readAhead = null;
      return;
    }
```

### UnusedAssignment
Variable `bytePerChar` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      out.start = out.end = text.start;

      int bytePerChar = 0;
      //Scan from right of "text", stop until find a char not in "from"
      for (int id = text.end - 1; id >= text.start; id -= bytePerChar) {
```

### UnusedAssignment
Variable `bytePerChar` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      out.start = out.end = text.end;

      int bytePerChar = 0;
      //Scan from left of "text", stop until find a char not in "from"
      for (int id = text.start; id < text.end; id += bytePerChar) {
```

### UnusedAssignment
Variable `currentByte` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      buffer = buffer.reallocIfNeeded(lengthNeeded);

      byte currentByte = 0;
      int id = 0;
      //get the char length of text.
```

### UnusedAssignment
Variable `id` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java

      byte currentByte = 0;
      int id = 0;
      //get the char length of text.
      int textCharCount = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharLength(text.buffer, text.start, text.end);
```

### UnusedAssignment
Variable `bytePerChar` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      out.buffer = text.buffer;
      out.start = out.end = text.start;
      int bytePerChar = 0;

      //Scan from left of "text", stop until find a char not in "from"
```

### UnusedAssignment
Variable `currentByte` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      final int lengthNeeded = (int) (theLength <= 0 ? 0 : theLength * 2);
      buffer = buffer.reallocIfNeeded(lengthNeeded);
      byte currentByte = 0;
      int id = 0;
      //get the char length of text.
```

### UnusedAssignment
Variable `id` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      buffer = buffer.reallocIfNeeded(lengthNeeded);
      byte currentByte = 0;
      int id = 0;
      //get the char length of text.
      int textCharCount = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharLength(text.buffer, text.start, text.end);
```

### UnusedAssignment
Variable `id` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      out.start = out.end = 0;

      int id = 0;
      for (id = left.start; id < left.end; id++) {
        out.buffer.setByte(out.end++, left.buffer.getByte(id));
```

### UnusedAssignment
Variable `bytesToRead` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
    }
    Stopwatch timer = Stopwatch.createStarted();
    int bytesToRead = 0;
    // We *cannot* rely on the totalByteSize being correct because
    // metadata for Parquet files is incorrect (sometimes). So we read
```

### UnusedAssignment
Variable `bytesRead` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
    // the API contract; but we still have to deal with it. So we make sure the size of the
    // buffer is exactly the same as the number of bytes requested
    int bytesRead = -1;
    int nBytes = 0;
    if (bytesToRead > 0) {
```

### UnusedAssignment
Variable `nBytes` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
    // buffer is exactly the same as the number of bytes requested
    int bytesRead = -1;
    int nBytes = 0;
    if (bytesToRead > 0) {
      try {
```

### UnusedAssignment
Variable `bytesSkipped` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
    checkInputStreamState();
    long bytesAvailable = this.count - this.curPosInBuffer;
    long bytesSkipped = 0;
    if (n <= 0) {
      return 0;
```

### UnusedAssignment
Variable `sampleTimes` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/UnlimitedRawBatchBuffer.java`
#### Snippet
```java
  private int runtimeSoftLimit = -1;
  private int runtimeAckCredit = 1;
  private int sampleTimes = 0;
  private long totalBatchSize = 0L;
  private final int fragmentCount;
```

### UnusedAssignment
Variable `toAggregate` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
    {
      while ((joinMjId2rfNumber == null || !joinMjId2rfNumber.isEmpty() ) && running.get()) {
        RuntimeFilterWritable toAggregate = null;
        synchronized (rfQueue) {
          try {
```

### UnusedAssignment
Variable `toAggregated` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
    int joinMajorId = runtimeFilterB.getMajorFragmentId();
    int buildSideRfNumber;
    RuntimeFilterWritable toAggregated = null;
    buildSideRfNumber = joinMjId2rfNumber.get(joinMajorId);
    buildSideRfNumber--;
```

### UnusedAssignment
Variable `lease` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DistributedQueryQueue.java`
#### Snippet
```java
  public QueueLease enqueue(QueryId queryId, double cost) throws QueryQueueException, QueueTimeoutException {
    final String queueName;
    DistributedLease lease = null;
    long queryMemory;
    final DistributedSemaphore distributedSemaphore;
```

### UnusedAssignment
Variable `cause` initializer `e.getCause()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
      return ephemeralPlugins.get(config).plugin();
    } catch (ExecutionException e) {
      Throwable cause = e.getCause();
      if (cause instanceof PluginException) {
        throw (PluginException) cause;
```

### UnusedAssignment
The value `null` assigned to `value` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/SimpleMessageParser.java`
#### Snippet
```java
    switch (token) {
      case VALUE_NULL:
        value = null;
      case VALUE_TRUE:
        value = Boolean.TRUE.toString();
```

### UnusedAssignment
The value `Preconditions.checkNotNull(workBuf)` assigned to `workBuf` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java
    this.enableNanInf = enableNanInf;
    this.enableEscapeAnyChar = enableEscapeAnyChar;
    workBuf = Preconditions.checkNotNull(workBuf);
  }

```

### UnusedAssignment
The value `! extractHeaders & providedSchema.booleanProperty(
TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine)` assigned to `skipFirst` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
      extractHeaders = providedSchema.booleanProperty(
          TextFormatPlugin.HAS_HEADERS_PROP, extractHeaders);
      skipFirst = ! extractHeaders & providedSchema.booleanProperty(
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
      skipFirst = ! extractHeaders & providedSchema.booleanProperty(
```

### UnusedAssignment
Variable `scanStats` initializer `ScanStats.TRIVIAL_TABLE` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockGroupScanPOP.java`
#### Snippet
```java

  private boolean extended;
  private ScanStats scanStats = ScanStats.TRIVIAL_TABLE;

  @JsonCreator
```

### UnusedAssignment
Variable `colWidth` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockGroupScanPOP.java`
#### Snippet
```java

        for (MockColumn col : entry.getTypes()) {
          int colWidth = 0;
          if (col.getWidthValue() == 0) {
            // Fixed width columns
```

### UnusedAssignment
Variable `prevCopied` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractEntryReader.java`
#### Snippet
```java

    final int remaining = length & 0x7;
    int prevCopied      = 0;

    if (numLongEntries == 1) {
```

### UnusedAssignment
Variable `entry` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
     */
    public Binary getEntry() {
      Binary entry = null;
      if (pushedBackValue == null) {
        entry = getNextEntry();
```

### UnusedAssignment
The value `true` assigned to `newMetadata` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata.java`
#### Snippet
```java
          parquetTableMetadataDirs =
              (createMetaFilesRecursivelyAsProcessUser(Path.getPathWithoutSchemeAndAuthority(path.getParent()), fs, true, null, true)).getRight();
          newMetadata = true;
        }
      } else {
```

### UnusedAssignment
Variable `numLeftoverVals` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/FixedWidthRepeatedReader.java`
#### Snippet
```java
  @Override
  protected boolean readAndStoreValueSizeInformation() {
    int numLeftoverVals = 0;
    if (notFishedReadingList) {
      numLeftoverVals = repeatedValuesInCurrentList;
```

### UnusedAssignment
Variable `recordsReadInCurrentPass` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBinaryReader.java`
#### Snippet
```java
    recordsToReadInThisPass = Math.min(recordsToReadInThisPass, batchSizer.getCurrentRecordsPerBatch());

    long recordsReadInCurrentPass = 0;

    if (!useBulkReader) {
```

### UnusedAssignment
Variable `runLength` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java
    // are located and copied together, rather than copying individual values

    int runLength = -1;     // number of non-null records in this pass.
    int nullRunLength = -1; // number of consecutive null records that we read.
    int currentDefinitionLevel = -1;
```

### UnusedAssignment
Variable `nullRunLength` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java

    int runLength = -1;     // number of non-null records in this pass.
    int nullRunLength = -1; // number of consecutive null records that we read.
    int currentDefinitionLevel = -1;
    int readCount = 0; // the record number we last read.
```

### UnusedAssignment
Variable `entry` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
    Preconditions.checkArgument(valuesToRead > 0, "Number of values to read [%s] should be greater than zero", valuesToRead);

    VarLenColumnBulkEntry entry = null;

    // If there is overflow data, then we need to consume it first
```

### UnusedAssignment
Variable `currDictValToWrite` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetFixedWidthDictionaryReaders.java`
#### Snippet
```java
        ValuesReader valReader = usingDictionary ? pageReader.getDictionaryValueReader() : pageReader.getValueReader();
        VarBinaryVector.Mutator mutator =  valueVec.getMutator();
        Binary currDictValToWrite = null;
        for (int i = 0; i < recordsReadInThisIteration; i++){
          currDictValToWrite = valReader.readBytes();
```

### UnusedAssignment
Variable `len` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java
      FieldSerializerContainer fieldSerializerContainer) {

      int len = 0;
      int totalLen = 0;

```

### UnusedAssignment
Variable `bytesRead` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
    public Void call() throws IOException {
      ReadStatus readStatus = new ReadStatus();
      long bytesRead = 0;
      long valuesRead = 0;
      final long totalValuesRead = parent.totalPageValuesRead;
```

### UnusedAssignment
The value `buf.reallocIfNeeded(length)` assigned to `buf` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
    public DrillFixedBinaryToVarbinaryConverter(VarBinaryWriter writer, int length, DrillBuf buf) {
      this.writer = writer;
      holder.buffer = buf = buf.reallocIfNeeded(length);
      holder.start = 0;
      holder.end = length;
```

### UnusedAssignment
Variable `sv2` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
  private int stdRowWidth;

  public SelectionVector2 sv2 = null;
  private int sv2Size;

```

### UnusedAssignment
Variable `usagePercent` initializer `getHeapUsage()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/RestQueryRunner.java`
#### Snippet
```java
    boolean isComplete = false;
    boolean nearlyOutOfHeapSpace = false;
    float usagePercent = getHeapUsage();

    // Wait until the query execution is complete or there is error submitting the query
```

### UnusedAssignment
Variable `successPage` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/OAuthRequests.java`
#### Snippet
```java

      // Get success page
      String successPage = null;
      try (InputStream inputStream = Resource.newClassPathResource(OAUTH_SUCCESS_PAGE).getInputStream()) {
        InputStreamReader reader = new InputStreamReader(inputStream, StandardCharsets.UTF_8);
```

### UnusedAssignment
The value `StringUtils.isNotEmpty(pluginGroup) ? pluginGroup.replace("/", "") : ALL_PLUGINS` assigned to `pluginGroup` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StorageResources.java`
#### Snippet
```java
      return Collections.emptyList();
    }
    pluginGroup = StringUtils.isNotEmpty(pluginGroup) ? pluginGroup.replace("/", "") : ALL_PLUGINS;
    return StreamSupport.stream(
      Spliterators.spliteratorUnknownSize(storage.storedConfigs(filter).entrySet().iterator(), Spliterator.ORDERED), false)
```

### UnusedAssignment
Variable `optionValue` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/DrillConfigIterator.java`
#### Snippet
```java
      final ConfigValue cv = e.getValue();
      final String name = e.getKey();
      OptionValue optionValue = null;
      switch(cv.valueType()) {
      case BOOLEAN:
```

### UnusedAssignment
The value `false` assigned to `addToBreadCrumb` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
        // only set remainder when it's the only array segment.
        if (addToBreadCrumb) {
          addToBreadCrumb = false;
          builder.remainder(seg);
        }
```

### UnusedAssignment
Variable `c` initializer `e.getCause()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ByteCodeLoader.java`
#### Snippet
```java
      return byteCode.get(path);
    } catch (ExecutionException e) {
      Throwable c = e.getCause();
      if (c instanceof ClassTransformationException) {
        throw (ClassTransformationException) c;
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassTransformer.java`
#### Snippet
```java
         *  we're using TRY.
         */
        MergedClassResult result = null;
        boolean scalarReplace = scalarReplacementOption != ScalarReplacementOption.OFF && entireClass.length() < MAX_SCALAR_REPLACE_CODE_SIZE;
        while(true) {
```

### UnusedAssignment
Variable `debug` initializer `false` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/AbstractClassCompiler.java`
#### Snippet
```java
@SuppressWarnings("unused")
public abstract class AbstractClassCompiler {
  protected boolean debug = false;

  AbstractClassCompiler(boolean debug) {
```

### UnusedAssignment
The value `getLocal(var)` assigned to `v` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/InstructionModifier.java`
#### Snippet
```java
      oldLocalToFirst.put(var, v.getIndex());
      return;
    } else if (opcode == Opcodes.ALOAD && (v = getLocal(var)) != null) {
      /*
       * Not forwarding this removes a now unnecessary ALOAD for a holder. The required LOAD/STORE
```

### UnusedAssignment
Variable `errorMessage` initializer `e.getCause() != null ? e.getCause().getMessage() : e.getMessage()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillSqlWorker.java`
#### Snippet
```java
      return convertPlan(context, sql, textPlan);
    } catch (ValidationException e) {
      String errorMessage = e.getCause() != null ? e.getCause().getMessage() : e.getMessage();
      throw UserException.validationError(e)
        .message(errorMessage)
```

### UnusedAssignment
Variable `allowNoTableRefCompoundIdentifier` initializer `false` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java

  private boolean enableComplex = true;
  private boolean allowNoTableRefCompoundIdentifier = false;

  public CompoundIdentifierConverter(boolean allowNoTableRefCompoundIdentifier) {
```

### UnusedAssignment
The value `window` assigned to `definedWindow` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
      // In a SELECT-SCOPE, only a partition can be defined
      if (definedWindow == null) {
        definedWindow = window;
      } else {
        if (!definedWindow.equalsDeep(window, Litmus.IGNORE)) {
```

### UnusedAssignment
Variable `dex` initializer `new DrillSqlParseException(sql, parseError)` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/SqlConverter.java`
#### Snippet
```java
      return parser.parseStmt();
    } catch (SqlParseException parseError) {
      DrillSqlParseException dex = new DrillSqlParseException(sql, parseError);
      UserException.Builder builder = UserException
          .parseError(dex)
```

### UnusedAssignment
Variable `builder` initializer `UserException .parseError(dex) .addContext(dex.getSqlWithErrorPointer())` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/SqlConverter.java`
#### Snippet
```java
    } catch (SqlParseException parseError) {
      DrillSqlParseException dex = new DrillSqlParseException(sql, parseError);
      UserException.Builder builder = UserException
          .parseError(dex)
          .addContext(dex.getSqlWithErrorPointer());
      if (isInnerQuery) {
        builder.message("Failure parsing a view your query is dependent upon.");
```

### UnusedAssignment
Variable `predList` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
        SchemaPath col = getColumn(pred, fieldNames);
        if (col != null) {
          List<RexNode> predList = null;
          if ((predList = colToRangePredicateMap.get(col)) != null) {
            predList.add(pred);
```

### UnusedAssignment
Variable `newRowType` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
    RelDataTypeField rowkey_primary;

    RelRecordType newRowType = null;

    DbGroupScan scan = (DbGroupScan) IndexPlanUtils.getGroupScan(origScan);
```

### UnusedAssignment
Variable `oplit` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
      RexNode input = null;
      if (operands.size() == 2 ) {
        RexLiteral oplit = null;
        if (operands.get(0) instanceof RexLiteral) {
          oplit = (RexLiteral) operands.get(0);
```

### UnusedAssignment
Variable `isCovering` initializer `false` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private double remainderSel = 1.0;  // selectivity of all remainder satisfiable conjuncts
    private boolean satisfiesCollation = false; // whether index satisfies collation
    private boolean isCovering = false;         // whether index is covering
    private double avgRowSize;          // avg row size in bytes of the selected part of index

```

### UnusedAssignment
Variable `primaryTableScan` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private int numProjectedFields;
    private double totalRows;
    private DrillScanRelBase primaryTableScan = null;
    private RelOptCost selfCost = null;

```

### UnusedAssignment
Variable `otherColumnsRemainderFilter` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private Map<LogicalExpression, RexNode> leadingPrefixMap;
    private RexNode indexColumnsRemainderFilter = null;
    private RexNode otherColumnsRemainderFilter = null;
    private RexBuilder rexBuilder;

```

### UnusedAssignment
Variable `sel` initializer `1.0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
      for (RexNode filter : leadingFilters) {
        double filterRows = stats.getRowCount(filter, idxIdentifier, primaryTableScan /* see comment above */);
        double sel = 1.0;
        if (filterRows != Statistics.ROWCOUNT_UNKNOWN) {
          sel = filterRows/totalRows;
```

### UnusedAssignment
Variable `newDist` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
    if ( upperProject != null) {
      RelCollation newCollation = RelCollations.of(RelCollations.EMPTY.getFieldCollations());
      DrillDistributionTrait newDist = null;

      newDist = upperProject.getInput().getTraitSet().getTrait(DrillDistributionTraitDef.INSTANCE);
```

### UnusedAssignment
Variable `leftIndexProjectPrel` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java

    // new Project's rowtype is original Project's rowtype [plus rowkey if rowkey is not in original rowtype]
    ProjectPrel leftIndexProjectPrel = null;
    if (origProject != null) {
      RelDataType origRowType = origProject.getRowType();
```

### UnusedAssignment
The value `false` assigned to `eligibleForCollation` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
            int maxIndex = getIndexFromCollation(ref.getIndex(), inputFieldCollations);
            if (maxIndex < 0) {
              eligibleForCollation = false;
              continue;
            }
```

### UnusedAssignment
The value changed at `idxFieldCount++` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
            RelFieldCollation.NullDirection.UNSPECIFIED));
      }
      idxFieldCount++;
    } else {
      // TODO: handle functional index
```

### UnusedAssignment
Variable `condition` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/rules/DbScanToIndexScanPrule.java`
#### Snippet
```java
    RexBuilder builder = indexContext.filter.getCluster().getRexBuilder();

    RexNode condition = null;
    if (indexContext.lowerProject == null) {
      condition = indexContext.filter.getCondition();
```

### UnusedAssignment
Variable `numRows` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
    double startBucketFraction = 1.0;
    double endBucketFraction = 1.0;
    long numRows = 0;
    int result;
    Double lowValue = null;
```

### UnusedAssignment
Variable `rootFragment` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
        List<MinorFragmentDefn> fragments = Lists.newArrayList();

        MinorFragmentDefn rootFragment = null;
        FragmentRoot rootOperator = null;

```

### UnusedAssignment
Variable `rootOperator` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java

        MinorFragmentDefn rootFragment = null;
        FragmentRoot rootOperator = null;

        IndexedFragmentNode iNode = new IndexedFragmentNode(minorFragmentId, wrapper,
```

### UnusedAssignment
Variable `numEndPoints` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashToMergeExchangePrel.java`
#### Snippet
```java

  private final List<DistributionField> distFields;
  private int numEndPoints = 0;
  private final RelCollation collation;

```

### UnusedAssignment
The value `condition.accept(reverseVisitor)` assigned to `condition` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java
      RewriteCombineBinaryOperators reverseVisitor = new RewriteCombineBinaryOperators(true, filterRel.getCluster().getRexBuilder());

      condition = condition.accept(reverseVisitor);
      pruneCondition = pruneCondition.accept(reverseVisitor);

```

### UnusedAssignment
The value `pruneCondition.accept(reverseVisitor)` assigned to `pruneCondition` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java

      condition = condition.accept(reverseVisitor);
      pruneCondition = pruneCondition.accept(reverseVisitor);

      if (descriptor.supportsMetadataCachePruning() && !wasAllPartitionsPruned) {
```

### UnusedAssignment
Variable `isRowKeyJoin` initializer `false` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashJoinPrel.java`
#### Snippet
```java
  private boolean swapped = false;
  private RuntimeFilterDef runtimeFilterDef;
  protected boolean isRowKeyJoin = false;
  private int joinControl;

```

### UnusedAssignment
Variable `swapped` initializer `false` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashJoinPrel.java`
#### Snippet
```java
public class HashJoinPrel  extends JoinPrel {

  private boolean swapped = false;
  private RuntimeFilterDef runtimeFilterDef;
  protected boolean isRowKeyJoin = false;
```

### UnusedAssignment
Variable `operPhase` initializer `OperatorPhase.PHASE_1of1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/AggPrelBase.java`
#### Snippet
```java
  }

  protected OperatorPhase operPhase = OperatorPhase.PHASE_1of1; // default phase
  protected List<NamedExpression> keys = Lists.newArrayList();
  protected List<NamedExpression> aggExprs = Lists.newArrayList();
```

### UnusedAssignment
The value `call.getPlanner().emptyTraitSet().plus(Prel.DRILL_PHYSICAL).plus(collation).plus(distOnOneKey)` assigned to `traits` is never used
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/StreamAggPrule.java`
#### Snippet
```java
                                       ImmutableList.copyOf(getDistributionField(aggregate, false)));

        traits = call.getPlanner().emptyTraitSet().plus(Prel.DRILL_PHYSICAL).plus(collation).plus(distOnOneKey);
        // Temporarily commenting out the single distkey plan since a few tpch queries (e.g 01.sql) get stuck
        // in VolcanoPlanner.canonize() method. Note that the corresponding single distkey plan for HashAggr works
```

### UnusedAssignment
Variable `options` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PlannerSettings.java`
#### Snippet
```java
  public static final RangeDoubleValidator STATISTICS_MULTICOL_NDV_ADJUST_FACTOR = new RangeDoubleValidator("planner.statistics.multicol_ndv_adjustment_factor", 0.0, 1.0, null);

  public OptionManager options = null;
  public FunctionImplementationRegistry functionImplementationRegistry = null;
  private final String queryUser;
```

### UnusedAssignment
Variable `functionImplementationRegistry` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PlannerSettings.java`
#### Snippet
```java

  public OptionManager options = null;
  public FunctionImplementationRegistry functionImplementationRegistry = null;
  private final String queryUser;
  private final AliasRegistryProvider aliasRegistryProvider;
```

### UnusedAssignment
Variable `list` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TraceInjector.java`
#### Snippet
```java

        List<PhysicalOperator> newChildren = Lists.newArrayList();
        List<PhysicalOperator> list = null;
        PhysicalOperator newOp = op;

```

### UnusedAssignment
Variable `numPartitions` initializer `1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
   * {@link #partitionNumTuning(int, HashJoinMemoryCalculator.BuildSidePartitioning)}.
   */
  protected int numPartitions = 1; // must be 2 to the power of bitsInMask

  /**
```

### UnusedAssignment
Variable `result` initializer `OK` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/ExternalSortBatch.java`
#### Snippet
```java
    // Loop over all input batches

    IterOutcome result = OK;
    loop: while (true) {
      result = loadBatch();
```

### UnusedAssignment
Variable `partitionNum` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashPartition.java`
#### Snippet
```java
  public static final String HASH_VALUE_COLUMN_NAME = "$Hash_Values$";

  private int partitionNum = -1; // the current number of this partition, as used by the operator

  private static final int VARIABLE_MIN_WIDTH_VALUE_SIZE = 8;
```

### UnusedAssignment
Variable `batchIndex` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
    private int maxOccupiedIdx = -1;
    private int targetBatchRowCount;
    private int batchIndex = 0;

    public void setTargetBatchRowCount(int targetBatchRowCount) {
```

### UnusedAssignment
Variable `lastEntryBatch` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
    int bucketIndex = getBucketIndex(hashCode, numBuckets());
     int startIdx = startIndices.getAccessor().get(bucketIndex);
     BatchHolder lastEntryBatch = null;

     for ( int currentIndex = startIdx;
```

### UnusedAssignment
Variable `next` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectRecordBatch.java`
#### Snippet
```java
    if (first && incomingRecordCount == 0) {
      if (!CollectionUtils.isEmpty(complexWriters) || rsLoader != null ) {
        IterOutcome next = null;
        while (incomingRecordCount == 0) {
          if (getLastKnownOutcome() == EMIT) {
```

### UnusedAssignment
Variable `width` initializer `-1` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectMemoryManager.java`
#### Snippet
```java
    for (String outputColumnName : varWidthColumnSizes.keySet()) {
      VariableWidthColumnInfo columnWidthInfo = varWidthColumnSizes.get(outputColumnName);
      int width = -1;
      //Walk the tree of OutputWidthExpressions to get a FixedLenExpr
      //As the tree is walked, the RecordBatchSizer and function annotations
```

### UnusedAssignment
Variable `putStatus` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
    int currentPartition = hashCode & spilledState.getPartitionMask();
    hashCode >>>= spilledState.getBitsInMask();
    HashTable.PutStatus putStatus = null;
    long allocatedBeforeHTput = allocator.getAllocatedMemory();
    String tryingTo = phase.is1st() ? "early return" : "spill";
```

### UnusedAssignment
Variable `samplePercent` initializer `100.0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsMergeBatch.java`
#### Snippet
```java
  private int schema;
  private List<String> columnsList;
  private double samplePercent = 100.0;
  private final List<MergedStatistic> mergedStatisticList;

```

### UnusedAssignment
Variable `startIndex` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionSenderRootExec.java`
#### Snippet
```java
    final int divisor = Math.max(1, outGoingBatchCount/actualPartitions);
    final int longTail = outGoingBatchCount % actualPartitions;
    int startIndex = 0;
    int endIndex = 0;

```

### UnusedAssignment
Variable `copier` initializer `null` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
      // Copy every Nth record from the samples into a candidate partition table, where N = totalSampledRecords/partitions
      // Attempt to push this to the distributed map. Only the first candidate to get pushed will be used.
      SampleCopier copier = null;
      List<ValueVector> localAllocationVectors = Lists.newArrayList();
      copier = getCopier(newSv4, allSamplesContainer, candidatePartitionTable, orderDefs, localAllocationVectors);
```

### UnusedAssignment
Variable `currCost` initializer `Float.POSITIVE_INFINITY` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/DefaultFunctionResolver.java`
#### Snippet
```java
  public DrillFuncHolder getBestMatch(List<DrillFuncHolder> methods, FunctionCall call) {

    float bestCost = Float.POSITIVE_INFINITY, currCost = Float.POSITIVE_INFINITY;
    DrillFuncHolder bestMatch = null;
    final List<DrillFuncHolder> bestMatchAlternatives = new LinkedList<>();
```

### UnusedAssignment
Variable `memoryPerNodeInBytes` initializer `0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/util/MemoryConfigParser.java`
#### Snippet
```java
    Matcher patternMatcher = pattern.matcher(memoryConfig);

    long memoryPerNodeInBytes = 0;
    if (patternMatcher.matches()) {
      memoryPerNodeInBytes = Long.parseLong(patternMatcher.group(1));
```

### UnusedAssignment
Variable `fileAppender` initializer `null` is redundant
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/write/ParquetFileWriter.java`
#### Snippet
```java
    OutputFile outputFile = table.io().newOutputFile(new Path(location, FileFormat.PARQUET.addExtension(name)).toUri().getPath());

    FileAppender<Record> fileAppender = null;
    try {
      fileAppender = Parquet.write(outputFile)
```

### UnusedAssignment
The value `tables.load(location)` assigned to `table` is never used
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/IcebergMetastore.java`
#### Snippet
```java
        return tables.create(schema.tableSchema(), schema.partitionSpec(), tableProperties, location);
      } catch (AlreadyExistsException ex) {
        table = tables.load(location);
      }
    }
```

### UnusedAssignment
Variable `startKey` initializer `HConstants.EMPTY_START_ROW` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
        } else {
          // WILDCARD present. This turns the LIKE predicate to RANGE predicate
          byte[] startKey = HConstants.EMPTY_START_ROW;
          byte[] stopKey = HConstants.EMPTY_END_ROW;
          startKey = prefix.getBytes(Charsets.UTF_8);
```

### UnusedAssignment
Variable `stopKey` initializer `HConstants.EMPTY_END_ROW` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
          // WILDCARD present. This turns the LIKE predicate to RANGE predicate
          byte[] startKey = HConstants.EMPTY_START_ROW;
          byte[] stopKey = HConstants.EMPTY_END_ROW;
          startKey = prefix.getBytes(Charsets.UTF_8);
          stopKey = startKey.clone();
```

### UnusedAssignment
Variable `tempData` initializer `new float[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5FloatDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    float[][] tempData = new float[0][];

    if (dimensions.length == 2) {
```

### UnusedAssignment
Variable `tempData` initializer `new byte[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5ByteDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    byte[][] tempData = new byte[0][];
    if (dimensions.length == 2) {
      tempData = transpose((byte[][]) reader.getDatasetByPath(datapath).getData());
```

### UnusedAssignment
Variable `tempData` initializer `new long[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5LongDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    long[][] tempData = new long[0][];
    if (dimensions.length == 2) {
      tempData = transpose((long[][]) reader.getDatasetByPath(datapath).getData());
```

### UnusedAssignment
Variable `tempData` initializer `new int[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5IntDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    int[][] tempData = new int[0][];
    if (dimensions.length == 2) {
      tempData = transpose((int[][]) reader.getDatasetByPath(datapath).getData());
```

### UnusedAssignment
Variable `tempData` initializer `new double[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5DoubleDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    double[][] tempData = new double[0][];
    if (dimensions.length == 2) {
      tempData = transpose((double[][]) reader.getDatasetByPath(datapath).getData());
```

### UnusedAssignment
Variable `tempData` initializer `new short[0][]` is redundant
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5SmallIntDataWriter.java`
#### Snippet
```java
    // Get dimensions
    int[] dimensions = reader.getDatasetByPath(datapath).getDimensions();
    short[][] tempData = new short[0][];
    if (dimensions.length == 2) {
      tempData = transpose((short[][]) reader.getDatasetByPath(datapath).getData());
```

### UnusedAssignment
Variable `timestamp` initializer `null` is redundant
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
    @Override
    public void load(RectangularTextContainer<?> cell) {
      Instant timestamp = null;
      if (Strings.isNullOrEmpty(this.dateFormat)) {
        timestamp = Instant.parse(cell.getText());
```

### UnusedAssignment
Variable `connection` initializer `connectionHolder.get()` is redundant
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
    @Override
    public void connectionSucceeded(C incoming) {
      C connection = connectionHolder.get();
      while (true) {
        boolean setted = connectionHolder.compareAndSet(null, incoming);
```

### UnusedAssignment
Variable `_writer` initializer `writer` is redundant
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
        reader.readStartDocument();
        // To handle nested Documents.
        MapOrListWriterImpl _writer = writer;
        if (!isList) {
          _writer = (MapOrListWriterImpl) writer.map(fieldName);
```

### UnusedAssignment
The value `null` assigned to `inputStream` is never used
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
      logger.warn("Error when closing {}: {}", name, e.getMessage());
    }
    inputStream = null;
  }
}
```

### UnusedAssignment
Variable `result` initializer `0.0` is redundant
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DistributionFunctions.java`
#### Snippet
```java
    @Override
    public void output() {
      double result = 0.0;
      result = (concordantPairs.value - discordantPairs.value) / (0.5 * n.value * (n.value - 1));
      tau.value = result;
```

### UnusedAssignment
Variable `geom` initializer `null` is redundant
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
  @Override
  public boolean next() {
    Geometry geom = null;

    while (!rowWriter.isFull()) {
```

### UnusedAssignment
Variable `result` initializer `""` is redundant
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSFunctions.java`
#### Snippet
```java
    public void eval() {
      String host = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(hostname.start, hostname.end, hostname.buffer);
      String result = "";
      try {
        java.net.Inet4Address ip = (java.net.Inet4Address) java.net.Inet4Address.getByName(host);
```

### UnusedAssignment
Variable `hostname` initializer `""` is redundant
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSFunctions.java`
#### Snippet
```java
    public void eval() {
      String ipString = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(ipaddress.start, ipaddress.end, ipaddress.buffer);
      String hostname = "";

      try {
```

### UnusedAssignment
Variable `columns` initializer `null` is redundant
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduScanBatchCreator.java`
#### Snippet
```java
    Preconditions.checkArgument(children.isEmpty());
    List<RecordReader> readers = new LinkedList<>();
    List<SchemaPath> columns = null;

    for (KuduSubScan.KuduSubScanSpec scanSpec : subScan.getTabletScanSpecList()) {
```

### UnusedAssignment
Variable `pointerSize` initializer `4` is redundant
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java
  private static final boolean is64bit;

  private static int pointerSize = 4;

  // Minimum size of a java.lang.Object
```

### UnusedAssignment
Variable `objectSize` initializer `8` is redundant
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/SizeEstimator.java`
#### Snippet
```java

  // Minimum size of a java.lang.Object
  private static int objectSize = 8;

  static {
```

## RuleId[id=MismatchedStringCase]
### MismatchedStringCase
Switch branch is unreachable: the label contains an uppercase symbol while the selector is lowercase-only
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/InfoSchemaFilterBuilder.java`
#### Snippet
```java
      }

      case FunctionNames.AND:
      case "booleanand": {  // TODO: Is this name correct?
        List<ExprNode> args = new ArrayList<>();
```

### MismatchedStringCase
Switch branch is unreachable: the label contains an uppercase symbol while the selector is lowercase-only
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/InfoSchemaFilterBuilder.java`
#### Snippet
```java
      }

      case FunctionNames.OR:
      case "booleanor": {  // TODO: Is this name correct?
        List<ExprNode> args = new ArrayList<>();
```

## RuleId[id=DoubleNegation]
### DoubleNegation
Double negation in `!((bitField0_ & 0x00000040) != 0)`
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureSubTypeIsMutable() {
        if (!((bitField0_ & 0x00000040) != 0)) {
          subType_ = new java.util.ArrayList<java.lang.Integer>(subType_);
          bitField0_ |= 0x00000040;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000010) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      private com.google.protobuf.Internal.IntList bloomFilterSizeInBytes_ = emptyIntList();
      private void ensureBloomFilterSizeInBytesIsMutable() {
        if (!((bitField0_ & 0x00000010) != 0)) {
          bloomFilterSizeInBytes_ = mutableCopy(bloomFilterSizeInBytes_);
          bitField0_ |= 0x00000010;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000020) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList probeFields_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureProbeFieldsIsMutable() {
        if (!((bitField0_ & 0x00000020) != 0)) {
          probeFields_ = new com.google.protobuf.LazyStringArrayList(probeFields_);
          bitField0_ |= 0x00000020;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList authenticationMechanisms_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureAuthenticationMechanismsIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          authenticationMechanisms_ = new com.google.protobuf.LazyStringArrayList(authenticationMechanisms_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      private com.google.protobuf.Internal.IntList receivingMinorFragmentId_ = emptyIntList();
      private void ensureReceivingMinorFragmentIdIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          receivingMinorFragmentId_ = mutableCopy(receivingMinorFragmentId_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000008) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList authenticationMechanisms_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureAuthenticationMechanismsIsMutable() {
        if (!((bitField0_ & 0x00000008) != 0)) {
          authenticationMechanisms_ = new com.google.protobuf.LazyStringArrayList(authenticationMechanisms_);
          bitField0_ |= 0x00000008;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFragmentStatusIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          fragmentStatus_ = new java.util.ArrayList<org.apache.drill.exec.proto.BitControl.FragmentStatus>(fragmentStatus_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00004000) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureCollectorIsMutable() {
        if (!((bitField0_ & 0x00004000) != 0)) {
          collector_ = new java.util.ArrayList<org.apache.drill.exec.proto.BitControl.Collector>(collector_);
          bitField0_ |= 0x00004000;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFragmentIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          fragment_ = new java.util.ArrayList<org.apache.drill.exec.proto.BitControl.PlanFragment>(fragment_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      private com.google.protobuf.Internal.IntList incomingMinorFragment_ = emptyIntList();
      private void ensureIncomingMinorFragmentIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          incomingMinorFragment_ = mutableCopy(incomingMinorFragment_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureJarIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          jar_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.Jar>(jar_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureInputProfileIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          inputProfile_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.StreamProfile>(inputProfile_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000008) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureOperatorProfileIsMutable() {
        if (!((bitField0_ & 0x00000008) != 0)) {
          operatorProfile_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.OperatorProfile>(operatorProfile_);
          bitField0_ |= 0x00000008;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureStackTraceIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          stackTrace_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.StackTraceElementWrapper>(stackTrace_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000040) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureMetricIsMutable() {
        if (!((bitField0_ & 0x00000040) != 0)) {
          metric_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.MetricValue>(metric_);
          bitField0_ |= 0x00000040;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFieldIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          field_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.SerializedField>(field_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList functionSignature_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureFunctionSignatureIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          functionSignature_ = new com.google.protobuf.LazyStringArrayList(functionSignature_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000020) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureParsingErrorIsMutable() {
        if (!((bitField0_ & 0x00000020) != 0)) {
          parsingError_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.ParsingError>(parsingError_);
          bitField0_ |= 0x00000020;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00800000) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList scannedPlugins_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureScannedPluginsIsMutable() {
        if (!((bitField0_ & 0x00800000) != 0)) {
          scannedPlugins_ = new com.google.protobuf.LazyStringArrayList(scannedPlugins_);
          bitField0_ |= 0x00800000;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureChildIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          child_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.SerializedField>(child_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureMinorFragmentProfileIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          minorFragmentProfile_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.MinorFragmentProfile>(minorFragmentProfile_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000400) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFragmentProfileIsMutable() {
        if (!((bitField0_ & 0x00000400) != 0)) {
          fragmentProfile_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.MajorFragmentProfile>(fragmentProfile_);
          bitField0_ |= 0x00000400;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureErrorIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          error_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserBitShared.DrillPBError>(error_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000080) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureConvertSupportIsMutable() {
        if (!((bitField0_ & 0x00000080) != 0)) {
          convertSupport_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.ConvertSupport>(convertSupport_);
          bitField0_ |= 0x00000080;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureTablesIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          tables_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.TableMetadata>(tables_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000400) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureDateTimeLiteralsSupportIsMutable() {
        if (!((bitField0_ & 0x00000400) != 0)) {
          dateTimeLiteralsSupport_ = new java.util.ArrayList<java.lang.Integer>(dateTimeLiteralsSupport_);
          bitField0_ |= 0x00000400;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureColumnsIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          columns_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.ResultColumnMetadata>(columns_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000020) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureCollateSupportIsMutable() {
        if (!((bitField0_ & 0x00000020) != 0)) {
          collateSupport_ = new java.util.ArrayList<java.lang.Integer>(collateSupport_);
          bitField0_ |= 0x00000020;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureSchemasIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          schemas_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.SchemaMetadata>(schemas_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00002000) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList systemFunctions_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureSystemFunctionsIsMutable() {
        if (!((bitField1_ & 0x00002000) != 0)) {
          systemFunctions_ = new com.google.protobuf.LazyStringArrayList(systemFunctions_);
          bitField1_ |= 0x00002000;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000020) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList authenticationMechanisms_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureAuthenticationMechanismsIsMutable() {
        if (!((bitField0_ & 0x00000020) != 0)) {
          authenticationMechanisms_ = new com.google.protobuf.LazyStringArrayList(authenticationMechanisms_);
          bitField0_ |= 0x00000020;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00000800) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList stringFunctions_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureStringFunctionsIsMutable() {
        if (!((bitField1_ & 0x00000800) != 0)) {
          stringFunctions_ = new com.google.protobuf.LazyStringArrayList(stringFunctions_);
          bitField1_ |= 0x00000800;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00001000) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureSubquerySupportIsMutable() {
        if (!((bitField1_ & 0x00001000) != 0)) {
          subquerySupport_ = new java.util.ArrayList<java.lang.Integer>(subquerySupport_);
          bitField1_ |= 0x00001000;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00000008) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureOuterJoinSupportIsMutable() {
        if (!((bitField1_ & 0x00000008) != 0)) {
          outerJoinSupport_ = new java.util.ArrayList<java.lang.Integer>(outerJoinSupport_);
          bitField1_ |= 0x00000008;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00000400) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList sqlKeywords_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureSqlKeywordsIsMutable() {
        if (!((bitField1_ & 0x00000400) != 0)) {
          sqlKeywords_ = new com.google.protobuf.LazyStringArrayList(sqlKeywords_);
          bitField1_ |= 0x00000400;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000200) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList dateTimeFunctions_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureDateTimeFunctionsIsMutable() {
        if (!((bitField0_ & 0x00000200) != 0)) {
          dateTimeFunctions_ = new com.google.protobuf.LazyStringArrayList(dateTimeFunctions_);
          bitField0_ |= 0x00000200;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000008) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFragmentsIsMutable() {
        if (!((bitField0_ & 0x00000008) != 0)) {
          fragments_ = new java.util.ArrayList<org.apache.drill.exec.proto.BitControl.PlanFragment>(fragments_);
          bitField0_ |= 0x00000008;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureCatalogsIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          catalogs_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.CatalogMetadata>(catalogs_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00010000) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureUnionSupportIsMutable() {
        if (!((bitField1_ & 0x00010000) != 0)) {
          unionSupport_ = new java.util.ArrayList<java.lang.Integer>(unionSupport_);
          bitField1_ |= 0x00010000;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureColumnsIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          columns_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.ColumnMetadata>(columns_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00000002) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList numericFunctions_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureNumericFunctionsIsMutable() {
        if (!((bitField1_ & 0x00000002) != 0)) {
          numericFunctions_ = new com.google.protobuf.LazyStringArrayList(numericFunctions_);
          bitField1_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureFragmentsIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          fragments_ = new java.util.ArrayList<org.apache.drill.exec.proto.BitControl.PlanFragment>(fragments_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensurePropertiesIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          properties_ = new java.util.ArrayList<org.apache.drill.exec.proto.UserProtos.Property>(properties_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField1_ & 0x00000004) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureOrderBySupportIsMutable() {
        if (!((bitField1_ & 0x00000004) != 0)) {
          orderBySupport_ = new java.util.ArrayList<java.lang.Integer>(orderBySupport_);
          bitField1_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000008) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList tableTypeFilter_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureTableTypeFilterIsMutable() {
        if (!((bitField0_ & 0x00000008) != 0)) {
          tableTypeFilter_ = new com.google.protobuf.LazyStringArrayList(tableTypeFilter_);
          bitField0_ |= 0x00000008;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000040) != 0)`
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureSupportedMethodsIsMutable() {
        if (!((bitField0_ & 0x00000040) != 0)) {
          supportedMethods_ = new java.util.ArrayList<java.lang.Integer>(supportedMethods_);
          bitField0_ |= 0x00000040;
```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `setObject()`
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/ColumnMetadata.java`
#### Snippet
```java
   *
   * @return the default value decode as an object that can be passed to
   * the {@link ColumnWriter#setObject()} method.
   */
  Object decodeDefaultValue();
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.vector.VarBinaryVector.BulkInput`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/VarLenBulkEntry.java`
#### Snippet
```java
/**
 * A bulk input entry enables us to process potentially multiple VL values in one shot (especially for very
 * small values); please refer to {@link org.apache.drill.exec.vector.VarBinaryVector.BulkInput}.
 *
 * <p><b>Format -</b>
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.memory.OutOfMemoryException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/AllocationHelper.java`
#### Snippet
```java
   * @param vector value vector we are trying to allocate
   * @param valueCount  size we are trying to allocate
   * @throws org.apache.drill.exec.memory.OutOfMemoryException if it can't allocate the memory
   */
  public static void allocateNew(ValueVector vector, int valueCount) {
```

### JavadocReference
Symbol `org.apache.drill.exec.record.DeadBuf` is inaccessible from here
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ValueVector.java`
#### Snippet
```java
 *
 * A vector when instantiated, relies on a
 * {@link org.apache.drill.exec.record.DeadBuf dead buffer}. It is important
 * that vector is allocated before attempting to read or write.
 *
```

### JavadocReference
Cannot resolve symbol `DRILL`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/ListVector.java`
#### Snippet
```java
 * change.
 *
 * @see DRILL-5955, DRILL-6037, DRILL-5958, DRILL-6046, DRILL-6062, DRILL-3806
 *      to get a flavor for the issues.
 */
```

### JavadocReference
Cannot resolve symbol `ScanBatch`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * and nullable types.</li>
 * <li>Is a generic interface. The other set is bound tightly to the
 * {@link ScanBatch} class.</li>
 * <li>Uses generic types such as <tt>getInt()</tt> for most numeric types. The
 * other set has accessors specific to each of the ~30 data types which Drill
```

### JavadocReference
Cannot resolve symbol `ScalarReader`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * <h4>Overview of the Code Structure</h4>
 *
 * {@link ScalarReader} and {@link ColumnWriter} are the core abstractions: they
 * provide simplified access to the myriad of Drill column types via a
 * simplified, uniform API. {@link TupleReader} and {@link TupleWriter} provide
```

### JavadocReference
Cannot resolve symbol `ColumnWriter`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * <h4>Overview of the Code Structure</h4>
 *
 * {@link ScalarReader} and {@link ColumnWriter} are the core abstractions: they
 * provide simplified access to the myriad of Drill column types via a
 * simplified, uniform API. {@link TupleReader} and {@link TupleWriter} provide
```

### JavadocReference
Cannot resolve symbol `TupleReader`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * {@link ScalarReader} and {@link ColumnWriter} are the core abstractions: they
 * provide simplified access to the myriad of Drill column types via a
 * simplified, uniform API. {@link TupleReader} and {@link TupleWriter} provide
 * a simplified API to rows or maps (both of which are tuples in Drill.)
 * {@link AccessorUtilities} provides a number of data conversion tools.
```

### JavadocReference
Cannot resolve symbol `TupleWriter`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * {@link ScalarReader} and {@link ColumnWriter} are the core abstractions: they
 * provide simplified access to the myriad of Drill column types via a
 * simplified, uniform API. {@link TupleReader} and {@link TupleWriter} provide
 * a simplified API to rows or maps (both of which are tuples in Drill.)
 * {@link AccessorUtilities} provides a number of data conversion tools.
```

### JavadocReference
Cannot resolve symbol `AccessorUtilities`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/package-info.java`
#### Snippet
```java
 * simplified, uniform API. {@link TupleReader} and {@link TupleWriter} provide
 * a simplified API to rows or maps (both of which are tuples in Drill.)
 * {@link AccessorUtilities} provides a number of data conversion tools.
 * <dl>
 * <dt>ObjectWriter, ObjectReader</dt>
```

### JavadocReference
Cannot resolve symbol `SingleMapWriter`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/TupleWriter.java`
#### Snippet
```java
 * methods throw an exception if the column is not of the requested type.
 *
 * @see {@link SingleMapWriter}, the class which this class replaces
 */
public interface TupleWriter extends ColumnWriter {
```

### JavadocReference
Cannot resolve symbol `elements()`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/ArrayReader.java`
#### Snippet
```java
 * Generic array reader. An array is one of the following:
 * <ul>
 * <li>Array of scalars. Read the values using {@link #elements()}, which provides
 * an array-like access to the scalars.</li>
 * <li>A repeated map. Use {@link #tuple(int)} to get a tuple reader for a
```

### JavadocReference
Cannot resolve symbol `tuple(int)`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/ArrayReader.java`
#### Snippet
```java
 * <li>Array of scalars. Read the values using {@link #elements()}, which provides
 * an array-like access to the scalars.</li>
 * <li>A repeated map. Use {@link #tuple(int)} to get a tuple reader for a
 * specific array element. Use {@link #size()} to learn the number of maps in
 * the array.</li>
```

### JavadocReference
Cannot resolve symbol `array(int)`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/ArrayReader.java`
#### Snippet
```java
 * specific array element. Use {@link #size()} to learn the number of maps in
 * the array.</li>
 * <li>List of lists. Use the {@link #array(int)} method to get the nested list
 * at a given index. Use {@link #size()} to learn the number of maps in
 * the array.</li>
```

### JavadocReference
Cannot resolve symbol `ColumnWriter`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/package-info.java`
#### Snippet
```java
 * writers.
 * <p>
 * Similarly, the {@link ColumnWriter} interface provides a uniform way to
 * access services common to all writer types, while allowing each JSON-like
 * writer to provide type-specific ways to access data.
```

### JavadocReference
Cannot resolve symbol `PromotableListWriter`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/package-info.java`
#### Snippet
```java
 * operator.
 * <p>
 * The {@link PromotableListWriter} handles the complex details of providing
 * the above simple API in the array-of-variant case.
 *
```

### JavadocReference
Cannot resolve symbol `EmptyValueFiller`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseVarWidthWriter.java`
#### Snippet
```java
   * Fill an empty slot with the default value set via a call to
   * {@link #setDefaultValue(Object)}. This is an implementation of the
   * {@link EmptyValueFiller} interface and is registered with the offset
   * writer when setting the default value. The offset vector writer calls
   * this method for each value that is to be filled. Note that the value
```

### JavadocReference
Cannot resolve symbol `RecordBatch`
in `exec/vector/src/main/java/org/apache/drill/exec/exception/OversizedAllocationException.java`
#### Snippet
```java
 *
 * <p>Operators should handle this exception to split the batch and later resume the execution on the next
 * {@link RecordBatch#next() iteration}.</p>
 *
 */
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/vector/src/main/java/org/apache/drill/exec/exception/OversizedAllocationException.java`
#### Snippet
```java
 *
 * <p>Operators should handle this exception to split the batch and later resume the execution on the next
 * {@link RecordBatch#next() iteration}.</p>
 *
 */
```

### JavadocReference
Cannot resolve symbol `Connection`
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePlugin.java`
#### Snippet
```java

  /**
   * An internal class which serves the key in a map of {@link HBaseStoragePlugin} => {@link Connection}.
   */
  class HBaseConnectionKey {
```

### JavadocReference
Cannot resolve symbol `Statement`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/ExecutionCanceledSqlException.java`
#### Snippet
```java
 * <p>
 *   Intended for throwing by methods that start SQL statement execution (e.g.,
 *   {@link Statement#execute}) or return data from execution (e.g.,
 *   {@link ResultSet} methods).
 * </p>
```

### JavadocReference
Cannot resolve symbol `execute`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/ExecutionCanceledSqlException.java`
#### Snippet
```java
 * <p>
 *   Intended for throwing by methods that start SQL statement execution (e.g.,
 *   {@link Statement#execute}) or return data from execution (e.g.,
 *   {@link ResultSet} methods).
 * </p>
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/ExecutionCanceledSqlException.java`
#### Snippet
```java
 *   Intended for throwing by methods that start SQL statement execution (e.g.,
 *   {@link Statement#execute}) or return data from execution (e.g.,
 *   {@link ResultSet} methods).
 * </p>
 */
```

### JavadocReference
Cannot resolve symbol `SQLFeatureNotSupportedException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillPreparedStatement.java`
#### Snippet
```java
 *   Setting parameters is not supported; parameter-setting methods such as
 *   {@link #setString(int, String)} throw
 *   {@link SQLFeatureNotSupportedException}.
 * </p>
 * @see #unwrap
```

### JavadocReference
Cannot resolve symbol `ConnectionConfigImpl`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillConnectionConfig.java`
#### Snippet
```java
 *    In the meantime, clients must not use the fact that
 *    {@code DrillConnectionConfig} currently extends
 *    {@link ConnectionConfigImpl}.  They must call only
 *    methods declared directly in DrillConnectionConfig (or inherited Object).
 *  </p>
```

### JavadocReference
Cannot resolve symbol `Quoting`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * <strong>Drill</strong>:
   * Reports current SQL identifier quoting character.
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
```

### JavadocReference
Cannot resolve symbol `BACK_TICK`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * <strong>Drill</strong>:
   * Reports current SQL identifier quoting character.
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
```

### JavadocReference
Cannot resolve symbol `Quoting`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * Reports current SQL identifier quoting character.
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
   *  "{@code ]}"; Unicode U+005D; 'RIGHT SQUARE BRACKET')</li>
```

### JavadocReference
Cannot resolve symbol `DOUBLE_QUOTE`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   * Reports current SQL identifier quoting character.
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
   *  "{@code ]}"; Unicode U+005D; 'RIGHT SQUARE BRACKET')</li>
```

### JavadocReference
Cannot resolve symbol `Quoting`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
   *  "{@code ]}"; Unicode U+005D; 'RIGHT SQUARE BRACKET')</li>
   *
```

### JavadocReference
Cannot resolve symbol `BRACKET`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   *  <li>{@link Quoting#BACK_TICK} - default back-quote character ("{@code `}"; Unicode U+0060; "GRAVE ACCENT") </li>
   *  <li>{@link Quoting#DOUBLE_QUOTE} - double quote character ("{@code "}"; Unicode U+0022; 'QUOTATION MARK')</li>
   *  <li>{@link Quoting#BRACKET} - brackets characters ("{@code [}"; Unicode U+005B; 'LEFT SQUARE BRACKET' and
   *  "{@code ]}"; Unicode U+005D; 'RIGHT SQUARE BRACKET')</li>
   *
```

### JavadocReference
Cannot resolve symbol `Quoting`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   *
   * @return current SQL identifier quoting character. Note: 'LEFT SQUARE BRACKET' is returned,
   *         when {@link Quoting#BRACKET} is set.
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `BRACKET`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/DrillDatabaseMetaData.java`
#### Snippet
```java
   *
   * @return current SQL identifier quoting character. Note: 'LEFT SQUARE BRACKET' is returned,
   *         when {@link Quoting#BRACKET} is set.
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `Statement`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/AlreadyClosedSqlException.java`
#### Snippet
```java
/**
 * SQLException for object-already-closed conditions, e.g., calling a method
 * on a closed {@link Statement}.
 */
public class AlreadyClosedSqlException extends JdbcApiSqlException {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, int, Throwable)}.
   */
  public JdbcApiSqlException( String reason,
```

### JavadocReference
Cannot resolve symbol `SQLException(String, String, int, Throwable)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, int, Throwable)}.
   */
  public JdbcApiSqlException( String reason,
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, Throwable)}.
   */
  public JdbcApiSqlException( String reason, Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(String, Throwable)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, Throwable)}.
   */
  public JdbcApiSqlException( String reason, Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException()}.
   * */
  public JdbcApiSqlException() {
```

### JavadocReference
Cannot resolve symbol `SQLException()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException()}.
   * */
  public JdbcApiSqlException() {
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 * <p>
 *   {@code JdbcApiSqlException} is intended for errors in using the JDBC API,
 *   such as calling {@link ResultSet#getString} before calling
 *   {@link ResultSet#next}.
 * </p>
```

### JavadocReference
Cannot resolve symbol `getString`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 * <p>
 *   {@code JdbcApiSqlException} is intended for errors in using the JDBC API,
 *   such as calling {@link ResultSet#getString} before calling
 *   {@link ResultSet#next}.
 * </p>
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 *   {@code JdbcApiSqlException} is intended for errors in using the JDBC API,
 *   such as calling {@link ResultSet#getString} before calling
 *   {@link ResultSet#next}.
 * </p>
 * <p>
```

### JavadocReference
Cannot resolve symbol `next`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 *   {@code JdbcApiSqlException} is intended for errors in using the JDBC API,
 *   such as calling {@link ResultSet#getString} before calling
 *   {@link ResultSet#next}.
 * </p>
 * <p>
```

### JavadocReference
Cannot resolve symbol `SQLSyntaxErrorException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 *   ({@code JdbcApiSqlException} is not for errors that are not under direct
 *   control of the programmer writing JDBC API calls, for example, invalid SQL
 *   syntax errors (which should use {@link SQLSyntaxErrorException}), errors
 *   from SQL-vs.-data mismatches (which likely should use {@link SQLDataException}),
 *   data file format errors, resource availability errors (which might use
```

### JavadocReference
Cannot resolve symbol `SQLDataException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 *   control of the programmer writing JDBC API calls, for example, invalid SQL
 *   syntax errors (which should use {@link SQLSyntaxErrorException}), errors
 *   from SQL-vs.-data mismatches (which likely should use {@link SQLDataException}),
 *   data file format errors, resource availability errors (which might use
 *   {@link SQLTransientException}), or internal Drill errors.)
```

### JavadocReference
Cannot resolve symbol `SQLTransientException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java
 *   from SQL-vs.-data mismatches (which likely should use {@link SQLDataException}),
 *   data file format errors, resource availability errors (which might use
 *   {@link SQLTransientException}), or internal Drill errors.)
 * </p>
 * <p>
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, Throwable)}.
   */
  public JdbcApiSqlException( String reason, String sqlState, Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(String, String, Throwable)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, Throwable)}.
   */
  public JdbcApiSqlException( String reason, String sqlState, Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(Throwable cause)}.
   */
  public JdbcApiSqlException( Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(Throwable cause)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(Throwable cause)}.
   */
  public JdbcApiSqlException( Throwable cause ) {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String)}.
   */
  public JdbcApiSqlException( String reason, String SQLState ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(String, String)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String)}.
   */
  public JdbcApiSqlException( String reason, String SQLState ) {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, int)}.
   */
  public JdbcApiSqlException( String reason, String SQLState, int vendorCode ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(String, String, int)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String, String, int)}.
   */
  public JdbcApiSqlException( String reason, String SQLState, int vendorCode ) {
```

### JavadocReference
Cannot resolve symbol `SQLException`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String)}.
   */
  public JdbcApiSqlException( String reason ) {
```

### JavadocReference
Cannot resolve symbol `SQLException(String)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/JdbcApiSqlException.java`
#### Snippet
```java

  /**
   * See {@link SQLException#SQLException(String)}.
   */
  public JdbcApiSqlException( String reason ) {
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/InvalidCursorStateSqlException.java`
#### Snippet
```java
/**
 * SQLException for invalid-cursor-state conditions, e.g., calling a column
 * accessor method before calling {@link ResultSet#next()} or after
 * {@link ResultSet#next()} returns false.
 */
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/InvalidCursorStateSqlException.java`
#### Snippet
```java
/**
 * SQLException for invalid-cursor-state conditions, e.g., calling a column
 * accessor method before calling {@link ResultSet#next()} or after
 * {@link ResultSet#next()} returns false.
 */
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/InvalidCursorStateSqlException.java`
#### Snippet
```java
 * SQLException for invalid-cursor-state conditions, e.g., calling a column
 * accessor method before calling {@link ResultSet#next()} or after
 * {@link ResultSet#next()} returns false.
 */
public class InvalidCursorStateSqlException extends JdbcApiSqlException {
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/InvalidCursorStateSqlException.java`
#### Snippet
```java
 * SQLException for invalid-cursor-state conditions, e.g., calling a column
 * accessor method before calling {@link ResultSet#next()} or after
 * {@link ResultSet#next()} returns false.
 */
public class InvalidCursorStateSqlException extends JdbcApiSqlException {
```

### JavadocReference
Cannot resolve symbol `net.hydromatic.avatica.AvaticaFactory`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillJdbc40Factory.java`
#### Snippet
```java

/**
 * Implementation of {@link net.hydromatic.avatica.AvaticaFactory}
 * for Drill and JDBC 4.0 (corresponds to JDK 1.6).
 */
```

### JavadocReference
Cannot resolve symbol `AvaticaFactory`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillFactory.java`
#### Snippet
```java

/**
 * Partial implementation of {@link AvaticaFactory}
 * (factory for main JDBC objects) for Drill's JDBC driver.
 * <p>
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillAccessorList.java`
#### Snippet
```java
  /**
   * Resets last-column-referenced information for {@link #wasNull}.
   * Must be called whenever row is advanced (when {@link ResultSet#next()}
   * is called).
   */
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillAccessorList.java`
#### Snippet
```java
  /**
   * Resets last-column-referenced information for {@link #wasNull}.
   * Must be called whenever row is advanced (when {@link ResultSet#next()}
   * is called).
   */
```

### JavadocReference
Cannot resolve symbol `SQLAccessor`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/AvaticaDrillSqlAccessor.java`
#### Snippet
```java

  /**
   * @see SQLAccessor#getObjectClass()
   */
  public Class<?> getObjectClass() {
```

### JavadocReference
Cannot resolve symbol `getObjectClass()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/AvaticaDrillSqlAccessor.java`
#### Snippet
```java

  /**
   * @see SQLAccessor#getObjectClass()
   */
  public Class<?> getObjectClass() {
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
   * <p>
   *   This implementation implements type conversions for
   *   {@link ResultSet#getByte(String)}/{@link ResultSet#getByte(int)}.
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `getByte(String)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
   * <p>
   *   This implementation implements type conversions for
   *   {@link ResultSet#getByte(String)}/{@link ResultSet#getByte(int)}.
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
   * <p>
   *   This implementation implements type conversions for
   *   {@link ResultSet#getByte(String)}/{@link ResultSet#getByte(int)}.
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `getByte(int)`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
   * <p>
   *   This implementation implements type conversions for
   *   {@link ResultSet#getByte(String)}/{@link ResultSet#getByte(int)}.
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `ResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
/**
 * SQL accessor implementing data type conversion for JDBC
 * {@link ResultSet}.get<i>Type</i>(<i>&lt;column ID></i>) column accessor methods.
 */
class TypeConvertingSqlAccessor implements SqlAccessor {
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.avatica.AvaticaFactory`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java
 * This class has sub-classes which implement JDBC 3.0 and JDBC 4.0 APIs; it is
 * instantiated using
 * {@link org.apache.calcite.avatica.AvaticaFactory#newPreparedStatement}.
 * </p>
 */
```

### JavadocReference
Cannot resolve symbol `newPreparedStatement`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillPreparedStatementImpl.java`
#### Snippet
```java
 * This class has sub-classes which implement JDBC 3.0 and JDBC 4.0 APIs; it is
 * instantiated using
 * {@link org.apache.calcite.avatica.AvaticaFactory#newPreparedStatement}.
 * </p>
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.avatica.AvaticaFactory`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillJdbc41Factory.java`
#### Snippet
```java

/**
 * Implementation of {@link org.apache.calcite.avatica.AvaticaFactory} for Drill and
 * JDBC 4.1 (corresponds to JDK 1.7).
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.avatica.AvaticaResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   *   Is to be called (once) from {@link #loadInitialSchema} for
   *   {@link DrillResultSetImpl#execute()}, and then (repeatedly) from
   *   {@link #next()} for {@link org.apache.calcite.avatica.AvaticaResultSet#next()}.
   * </p>
   *
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   *   Is to be called (once) from {@link #loadInitialSchema} for
   *   {@link DrillResultSetImpl#execute()}, and then (repeatedly) from
   *   {@link #next()} for {@link org.apache.calcite.avatica.AvaticaResultSet#next()}.
   * </p>
   *
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.avatica.AvaticaResultSet`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   * <p>
   *   To be called once from {@link DrillResultSetImpl#execute()} before
   *   {@link #next()} is called from {@link org.apache.calcite.avatica.AvaticaResultSet#next()}.
   * <p>
   */
```

### JavadocReference
Cannot resolve symbol `next()`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   * <p>
   *   To be called once from {@link DrillResultSetImpl#execute()} before
   *   {@link #next()} is called from {@link org.apache.calcite.avatica.AvaticaResultSet#next()}.
   * <p>
   */
```

### JavadocReference
Cannot resolve symbol `Scheduler`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java
  /**
   * Application-specific priority. Drill-on-Yarn uses the priority to associate
   * YARN requests with a {@link Scheduler}. When the resource allocation
   * arrives, we use the priority to trace back to the scheduler that requested
   * it, and from there to the task to be run in the allocation.
```

### JavadocReference
Cannot resolve symbol `$CLUSTER_TYPE`
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ClusterDef.java`
#### Snippet
```java
  /**
   * Defined cluster tier types. The value of the type appears as the value of
   * the {@link $CLUSTER_TYPE} parameter in the config file.
   */

```

### JavadocReference
Cannot resolve symbol `DrillOnYarn`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/package-info.java`
#### Snippet
```java
 * the app, etc.
 * <p>
 * Divided into a main program ({@link DrillOnYarn}) and a series of commands.
 * Some commands are further divided into tasks. Builds on the
 * YARN and DFS facades defined in the core module.
```

### JavadocReference
Cannot resolve symbol `ServerConnector`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java
   * version is tightly coupled to Drillbit configuration.
   *
   * @return Initialized {@link ServerConnector} for HTTPS connections.
   * @throws Exception when unable to create HTTPS connector
   */
```

### JavadocReference
Cannot resolve symbol `SessionHandler`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java

  /**
   * It creates A {@link SessionHandler}
   *
   * @param config Drill configs
```

### JavadocReference
Cannot resolve symbol `ServerConnector`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java
   * Create HTTP connector.
   *
   * @return Initialized {@link ServerConnector} instance for HTTP connections.
   */
  private ServerConnector createHttpConnector(Config config) {
```

### JavadocReference
Cannot resolve symbol `Partition`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HivePartition.java`
#### Snippet
```java

/**
 * This class is wrapper of {@link Partition} class and used for
 * storage of such additional information as index of list in column lists cache.
 */
```

### JavadocReference
Cannot resolve symbol `Table`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWithColumnCache.java`
#### Snippet
```java

/**
 * This class is wrapper of {@link Table} class and used for
 * storage of such additional information as column lists cache.
 */
```

### JavadocReference
Cannot resolve symbol `Partition`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java

  /**
   * Wrapper for {@link Partition} class. Used for serialization and deserialization of {@link HivePartition}.
   */
  public static class HivePartitionWrapper {
```

### JavadocReference
Cannot resolve symbol `StorageDescriptor`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java

  /**
   * Wrapper for {@link StorageDescriptor} class.
   * Used in {@link HivePartitionWrapper} and {@link HiveTableWrapper}
   * for serialization and deserialization of {@link StorageDescriptor}.
```

### JavadocReference
Cannot resolve symbol `StorageDescriptor`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java
   * Wrapper for {@link StorageDescriptor} class.
   * Used in {@link HivePartitionWrapper} and {@link HiveTableWrapper}
   * for serialization and deserialization of {@link StorageDescriptor}.
   */
  public static class StorageDescriptorWrapper {
```

### JavadocReference
Cannot resolve symbol `InputFormat`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveMetadataProvider.java`
#### Snippet
```java
  /**
   * Return {@link LogicalInputSplit}s for given {@link HiveReadEntry}. First splits are looked up in cache, if not
   * found go through {@link InputFormat#getSplits(JobConf, int)} to find the splits.
   *
   * @param hiveReadEntry Subset of the {@link HiveReadEntry} used when creating this object.
```

### JavadocReference
Cannot resolve symbol `getSplits(JobConf, int)`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveMetadataProvider.java`
#### Snippet
```java
  /**
   * Return {@link LogicalInputSplit}s for given {@link HiveReadEntry}. First splits are looked up in cache, if not
   * found go through {@link InputFormat#getSplits(JobConf, int)} to find the splits.
   *
   * @param hiveReadEntry Subset of the {@link HiveReadEntry} used when creating this object.
```

### JavadocReference
Cannot resolve symbol `JobConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveMetadataProvider.java`
#### Snippet
```java
  /**
   * Return {@link LogicalInputSplit}s for given {@link HiveReadEntry}. First splits are looked up in cache, if not
   * found go through {@link InputFormat#getSplits(JobConf, int)} to find the splits.
   *
   * @param hiveReadEntry Subset of the {@link HiveReadEntry} used when creating this object.
```

### JavadocReference
Cannot resolve symbol `Token`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClientFactory.java`
#### Snippet
```java
   * @param tokenStr     token string
   * @param tokenService token service
   * @return {@link Token} instance with decoded string
   * @throws IOException if error happened during decoding token string
   */
```

### JavadocReference
Cannot resolve symbol `MetaException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClientFactory.java`
#### Snippet
```java
   * @param hiveConf hive properties set in Drill storage plugin
   * @return instance of client
   * @throws MetaException when initialization failed
   */
  public static DrillHiveMetaStoreClient createCloseableClientWithCaching(final HiveConf hiveConf)
```

### JavadocReference
Cannot resolve symbol `MetaException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClient.java`
#### Snippet
```java
   *
   * @param hiveConf hive conf from storage plugin
   * @throws MetaException when initialization failed
   */
  DrillHiveMetaStoreClient(final HiveConf hiveConf) throws MetaException {
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClient.java`
#### Snippet
```java
   * @param ignoreAuthzErrors whether authorization errors should be ignored
   * @return list of Hive databases
   * @throws TException when client fails
   */
  public List<String> getDatabases(boolean ignoreAuthzErrors) throws TException {
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClient.java`
#### Snippet
```java
   * @param renewerKerberosPrincipalName kerberos user
   * @return the string of the token
   * @throws TException when client fails
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClient.java`
#### Snippet
```java
   * @param tableName name of table
   * @return {@link HiveReadEntry} containing table meta like columns, partitions etc.
   * @throws TException when client fails
   */
  public HiveReadEntry getHiveReadEntry(final String dbName, final String tableName, boolean ignoreAuthzErrors) throws TException {
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/DrillHiveMetaStoreClient.java`
#### Snippet
```java
   * @param ignoreAuthzErrors hint for handling authorization errors
   * @return map where keys are db object names values are types (VIEW or TABLE)
   * @throws TException in case when if loader thrown ExecutionException
   */
  public Map<String, TableType> getTableNamesAndTypes(final String dbName, boolean ignoreAuthzErrors) throws TException {
```

### JavadocReference
Cannot resolve symbol `TableType`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/TableNameCacheLoader.java`
#### Snippet
```java
  /**
   * Creates function used to map table or view name to appropriate
   * {@link TableType} value based on set of view names.
   *
   * @param viewNames set of view names
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   *
   * @return names of databases defined in Hive
   * @throws TException when loading failed
   */
  List<String> getDbNames() throws TException {
```

### JavadocReference
Cannot resolve symbol `HiveConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
  /**
   * Storage plugin option for altering cache expiration policy.
   * See {@link #isExpireAfterWrite(HiveConf)} for details.
   */
  private static final String HIVE_METASTORE_CACHE_EXPIRE = "hive.metastore.cache-expire-after";
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * @param dbName database name
   * @return  collection of names and types of table objects
   * @throws TException when loading failed
   */
  Map<String, Schema.TableType> getTableNamesAndTypes(String dbName) throws TException {
```

### JavadocReference
Cannot resolve symbol `Schema.TableType`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * Cache where each key is name of database and value is map
   * where each key is table or view name and value indicates type
   * {@link Schema.TableType#TABLE} or {@link Schema.TableType#VIEW}
   * accordingly.
   */
```

### JavadocReference
Cannot resolve symbol `TABLE`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * Cache where each key is name of database and value is map
   * where each key is table or view name and value indicates type
   * {@link Schema.TableType#TABLE} or {@link Schema.TableType#VIEW}
   * accordingly.
   */
```

### JavadocReference
Cannot resolve symbol `Schema.TableType`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * Cache where each key is name of database and value is map
   * where each key is table or view name and value indicates type
   * {@link Schema.TableType#TABLE} or {@link Schema.TableType#VIEW}
   * accordingly.
   */
```

### JavadocReference
Cannot resolve symbol `VIEW`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * Cache where each key is name of database and value is map
   * where each key is table or view name and value indicates type
   * {@link Schema.TableType#TABLE} or {@link Schema.TableType#VIEW}
   * accordingly.
   */
```

### JavadocReference
Cannot resolve symbol `HiveConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
  /**
   * One of possible values for option {@link #HIVE_METASTORE_CACHE_EXPIRE}.
   * See {@link #isExpireAfterWrite(HiveConf)} for details.
   */
  private static final String HIVE_METASTORE_CACHE_EXPIRE_AFTER_ACCESS = "access";
```

### JavadocReference
Cannot resolve symbol `HiveConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
  /**
   * Storage plugin option for altering cached values expiration time.
   * See {@link #getHmsCacheTTL(HiveConf)} for details.
   */
  private static final String HIVE_METASTORE_CACHE_TTL = "hive.metastore.cache-ttl-seconds";
```

### JavadocReference
Cannot resolve symbol `TException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveMetadataCache.java`
#### Snippet
```java
   * @param dbName database name
   * @return set of table and view names
   * @throws TException when loading failed
   */
  HiveReadEntry getHiveReadEntry(String dbName, String tableName) throws TException {
```

### JavadocReference
Cannot resolve symbol `HiveAccessControlException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java

  /**
   * Check authorization for "SHOW TABLES" command in given Hive db. A {@link HiveAccessControlException} is thrown
   * for illegal access.
   * @param dbName
```

### JavadocReference
Cannot resolve symbol `HiveAccessControlException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java

  /**
   * Check authorization for "READ TABLE" for given db.table. A {@link HiveAccessControlException} is thrown
   * for illegal access.
   * @param dbName
```

### JavadocReference
Cannot resolve symbol `HiveAccessControlException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java

  /**
   * Check authorization for "SHOW DATABASES" command. A {@link HiveAccessControlException} is thrown
   * for illegal access.
   */
```

### JavadocReference
Cannot resolve symbol `InputFormat`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java

  /**
   * Utility method which gets table or partition {@link InputFormat} class. First it
   * tries to get the class name from given StorageDescriptor object. If it doesn't contain it tries to get it from
   * StorageHandler class set in table properties. If not found throws an exception.
```

### JavadocReference
Cannot resolve symbol `JobConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
   * tries to get the class name from given StorageDescriptor object. If it doesn't contain it tries to get it from
   * StorageHandler class set in table properties. If not found throws an exception.
   * @param job {@link JobConf} instance needed incase the table is StorageHandler based table.
   * @param sd {@link StorageDescriptor} instance of currently reading partition or table (for non-partitioned tables).
   * @param table Table object
```

### JavadocReference
Cannot resolve symbol `StorageDescriptor`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
   * StorageHandler class set in table properties. If not found throws an exception.
   * @param job {@link JobConf} instance needed incase the table is StorageHandler based table.
   * @param sd {@link StorageDescriptor} instance of currently reading partition or table (for non-partitioned tables).
   * @param table Table object
   */
```

### JavadocReference
Cannot resolve symbol `JobConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java

  /**
   * This method checks whether the table is transactional and set necessary properties in {@link JobConf}.<br>
   * If schema evolution properties aren't set in job conf for the input format, method sets the column names
   * and types from table/partition properties or storage descriptor.
```

### JavadocReference
Cannot resolve symbol `StorageDescriptor`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java

  /**
   * Get the input format from given {@link StorageDescriptor}.
   *
   * @param properties table properties
```

### JavadocReference
Cannot resolve symbol `InputFormat`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
   * @param hiveReadEntry hive read entry
   * @param sd storage descriptor
   * @return {@link InputFormat} class or null if a failure has occurred. Failure is logged as warning.
   */
  private static Class<? extends InputFormat<?, ?>> getInputFormatFromSD(final Properties properties,
```

### JavadocReference
Cannot resolve symbol `JobConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java

  /**
   * Utility method which adds give configs to {@link JobConf} object.
   *
   * @param job {@link JobConf} instance.
```

### JavadocReference
Cannot resolve symbol `JobConf`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
   * Utility method which adds give configs to {@link JobConf} object.
   *
   * @param job {@link JobConf} instance.
   * @param properties New config properties
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.hive.metastore.api.FieldSchema`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/types/HiveToRelDataTypeConverter.java`
#### Snippet
```java
/**
 * This class is responsible for data type conversions
 * from {@link org.apache.hadoop.hive.metastore.api.FieldSchema} instances
 * to {@link  org.apache.calcite.rel.type.RelDataType} instances
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.type.RelDataType`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/planner/types/HiveToRelDataTypeConverter.java`
#### Snippet
```java
 * This class is responsible for data type conversions
 * from {@link org.apache.hadoop.hive.metastore.api.FieldSchema} instances
 * to {@link  org.apache.calcite.rel.type.RelDataType} instances
 */
public class HiveToRelDataTypeConverter {
```

### JavadocReference
Cannot resolve symbol `SerDeException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/readers/HiveDefaultRecordReader.java`
#### Snippet
```java
   * @param deserializer hive deserializer
   * @return StructObjectInspector instance
   * @throws SerDeException in case if can't get inspector from deserializer
   */
  private static StructObjectInspector getStructOI(final Deserializer deserializer) throws SerDeException {
```

### JavadocReference
Cannot resolve symbol `MapWriter`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/writers/HiveValueWriterFactory.java`
#### Snippet
```java
  /**
   * Used to extract child writer from parent writer, assuming that parent writer may be instance of
   * {@link MapWriter} or {@link ListWriter}
   *
   * @param name         column or struct field name
```

### JavadocReference
Cannot resolve symbol `ListWriter`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/writers/HiveValueWriterFactory.java`
#### Snippet
```java
  /**
   * Used to extract child writer from parent writer, assuming that parent writer may be instance of
   * {@link MapWriter} or {@link ListWriter}
   *
   * @param name         column or struct field name
```

### JavadocReference
Cannot resolve symbol `Direction`
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     *             allowed values: {@code "ASC"}, {@code "DESC"}, {@code null};
     *             null specifies default &lt;ordering specification>
     *                   ({@code "ASC"} / {@link Direction#ASCENDING})
     * @param  strNullOrdering   the &lt;null ordering> as string;
     *             allowed values: {@code "FIRST"}, {@code "LAST"},
```

### JavadocReference
Cannot resolve symbol `ASCENDING`
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     *             allowed values: {@code "ASC"}, {@code "DESC"}, {@code null};
     *             null specifies default &lt;ordering specification>
     *                   ({@code "ASC"} / {@link Direction#ASCENDING})
     * @param  strNullOrdering   the &lt;null ordering> as string;
     *             allowed values: {@code "FIRST"}, {@code "LAST"},
```

### JavadocReference
Cannot resolve symbol `NullDirection`
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     *             {@code "UNSPECIFIED"}, {@code null};
     *             null specifies default &lt;null ordering>
     *             (omitted / {@link NullDirection#UNSPECIFIED}, interpreted later)
     */
    @JsonCreator
```

### JavadocReference
Cannot resolve symbol `UNSPECIFIED`
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     *             {@code "UNSPECIFIED"}, {@code null};
     *             null specifies default &lt;null ordering>
     *             (omitted / {@link NullDirection#UNSPECIFIED}, interpreted later)
     */
    @JsonCreator
```

### JavadocReference
Cannot resolve symbol `Direction`
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
    private final NullDirection nullOrdering;
    /** The values in the plans for ordering specification are ASC, DESC, not the
     * full words featured in the Calcite {@link Direction} Enum, need to map between them. */
    private static ImmutableMap<String, Direction> DRILL_TO_CALCITE_DIR_MAPPING =
        ImmutableMap.<String, Direction>builder()
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.expr.fn.impl.DirectoryExplorers`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/UdfUtilities.java`
#### Snippet
```java
   * been evaluated during planning if you write UDFs against this interface.
   *
   * See {@link org.apache.drill.exec.expr.fn.impl.DirectoryExplorers} for
   * example usages of this interface.
   *
```

### JavadocReference
Cannot resolve symbol `WorkManager`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorContextImpl.java`
#### Snippet
```java
  /**
   * This lazily initialized executor service is used to submit a {@link Callable task} that needs a proxy user. There
   * is no pool that is created; this pool is a decorator around {@link WorkManager#executor the worker pool} that
   * returns a {@link ListenableFuture future} for every task that is submitted. For the shutdown sequence,
   * see {@link WorkManager#close}.
```

### JavadocReference
Cannot resolve symbol `executor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorContextImpl.java`
#### Snippet
```java
  /**
   * This lazily initialized executor service is used to submit a {@link Callable task} that needs a proxy user. There
   * is no pool that is created; this pool is a decorator around {@link WorkManager#executor the worker pool} that
   * returns a {@link ListenableFuture future} for every task that is submitted. For the shutdown sequence,
   * see {@link WorkManager#close}.
```

### JavadocReference
Cannot resolve symbol `ListenableFuture`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorContextImpl.java`
#### Snippet
```java
   * This lazily initialized executor service is used to submit a {@link Callable task} that needs a proxy user. There
   * is no pool that is created; this pool is a decorator around {@link WorkManager#executor the worker pool} that
   * returns a {@link ListenableFuture future} for every task that is submitted. For the shutdown sequence,
   * see {@link WorkManager#close}.
   */
```

### JavadocReference
Cannot resolve symbol `WorkManager`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorContextImpl.java`
#### Snippet
```java
   * is no pool that is created; this pool is a decorator around {@link WorkManager#executor the worker pool} that
   * returns a {@link ListenableFuture future} for every task that is submitted. For the shutdown sequence,
   * see {@link WorkManager#close}.
   */
  private ListeningExecutorService delegatePool;
```

### JavadocReference
Cannot resolve symbol `close`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorContextImpl.java`
#### Snippet
```java
   * is no pool that is created; this pool is a decorator around {@link WorkManager#executor the worker pool} that
   * returns a {@link ListenableFuture future} for every task that is submitted. For the shutdown sequence,
   * see {@link WorkManager#close}.
   */
  private ListeningExecutorService delegatePool;
```

### JavadocReference
Cannot resolve symbol `Logger`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/AccountingDataTunnel.java`
#### Snippet
```java

  /**
   * See {@link DataTunnel#setTestInjectionControls(ControlsInjector, ExecutionControls, Logger)}.
   */
  public void setTestInjectionControls(final ControlsInjector testInjector,
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.plan.RelOptTable.ToRelContext`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/ViewExpansionContext.java`
#### Snippet
```java
 * Contains context information about view expansion(s) in a query. Part of {@link org.apache.drill.exec.ops
 * .QueryContext}. Before expanding a view into its definition, as part of the
 * {@link org.apache.drill.exec.planner.logical.DrillViewTable#toRel(org.apache.calcite.plan.RelOptTable.ToRelContext,
 * org.apache.calcite.plan.RelOptTable)}, first a {@link ViewExpansionToken} is requested from ViewExpansionContext
 * through {@link #reserveViewExpansionToken(String)}.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.plan.RelOptTable`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/ViewExpansionContext.java`
#### Snippet
```java
 * .QueryContext}. Before expanding a view into its definition, as part of the
 * {@link org.apache.drill.exec.planner.logical.DrillViewTable#toRel(org.apache.calcite.plan.RelOptTable.ToRelContext,
 * org.apache.calcite.plan.RelOptTable)}, first a {@link ViewExpansionToken} is requested from ViewExpansionContext
 * through {@link #reserveViewExpansionToken(String)}.
 * Once view expansion is complete, a token is released through {@link ViewExpansionToken#release()}. A view definition
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.schema.SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java

  /**
   * Return reference to default schema instance in a schema tree. Each {@link org.apache.calcite.schema.SchemaPlus}
   * instance can refer to its parent and its children. From the returned reference to default schema instance,
   * clients can traverse the entire schema tree and know the default schema where to look up the tables first.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.schema.SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java

  /**
   *  Create and return a {@link org.apache.calcite.schema.SchemaPlus} with given <i>schemaConfig</i> but some schemas (from storage plugins)
   *  could be initialized later.
   * @param schemaConfig
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.schema.SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java
   *  could be initialized later.
   * @param schemaConfig
   * @return A {@link org.apache.calcite.schema.SchemaPlus} with given <i>schemaConfig</i>.
   */
  public SchemaPlus getRootSchema(SchemaConfig schemaConfig) {
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.proto.beans.QueryData`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserResultsListener.java`
#### Snippet
```java

  /**
   * A {@link org.apache.drill.exec.proto.beans.QueryData QueryData} message was received
   * @param result data batch received
   * @param throttle connection throttle
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.rpc.Acks.OK`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserResultsListener.java`
#### Snippet
```java
   *
   * @param queryId
   *          sent by the server along {@link org.apache.drill.exec.rpc.Acks.OK
   *          Acks.OK}
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.schema.SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserSession.java`
#### Snippet
```java
   * Get default schema from current default schema path and given schema tree.
   * @param rootSchema root schema
   * @return A {@link org.apache.calcite.schema.SchemaPlus} object.
   */
  public SchemaPlus getDefaultSchema(SchemaPlus rootSchema) {
```

### JavadocReference
Cannot resolve symbol `ValidationException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserSession.java`
#### Snippet
```java
   *                             absolute schema.
   * @param currentDefaultSchema Current default schema.
   * @throws ValidationException If the given default schema path is invalid in current schema tree.
   */
  public void setDefaultSchemaPath(String newDefaultSchemaPath, SchemaPlus currentDefaultSchema)
```

### JavadocReference
Cannot resolve symbol `DrillSimpleFunc`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/package-info.java`
#### Snippet
```java
 * tree. This generated source is built at run-time as schema is discovered.
 *
 * This package contains the {@link DrillSimpleFunc} and {@link DrillAggFunc}
 * interfaces that can be implemented by users to define their own aggregate
 * and scalar functions.
```

### JavadocReference
Cannot resolve symbol `DrillAggFunc`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/package-info.java`
#### Snippet
```java
 * tree. This generated source is built at run-time as schema is discovered.
 *
 * This package contains the {@link DrillSimpleFunc} and {@link DrillAggFunc}
 * interfaces that can be implemented by users to define their own aggregate
 * and scalar functions.
```

### JavadocReference
Cannot resolve symbol `org.apache.parquet.filter2.statisticslevel.StatisticsFilter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ComparisonPredicate.java`
#### Snippet
```java
  /**
   * Semantics of matches() is very similar to what is implemented in Parquet library's
   * {@link org.apache.parquet.filter2.statisticslevel.StatisticsFilter} and
   * {@link org.apache.parquet.filter2.predicate.FilterPredicate}
   *
```

### JavadocReference
Cannot resolve symbol `org.apache.parquet.filter2.predicate.FilterPredicate`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ComparisonPredicate.java`
#### Snippet
```java
   * Semantics of matches() is very similar to what is implemented in Parquet library's
   * {@link org.apache.parquet.filter2.statisticslevel.StatisticsFilter} and
   * {@link org.apache.parquet.filter2.predicate.FilterPredicate}
   *
   * Main difference :
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.MethodWriter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
   * <p>
   * Local variable has 1 index within the constant pool.
   * {@link org.objectweb.asm.MethodWriter#visitLocalVariable(String, String, String, Label, Label, int)}
   * <p>
   * For upper estimation of max index value, suppose that each field and local
```

### JavadocReference
Cannot resolve symbol `visitLocalVariable(String, String, String, Label, Label, int)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
   * <p>
   * Local variable has 1 index within the constant pool.
   * {@link org.objectweb.asm.MethodWriter#visitLocalVariable(String, String, String, Label, Label, int)}
   * <p>
   * For upper estimation of max index value, suppose that each field and local
```

### JavadocReference
Cannot resolve symbol `Label`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
   * <p>
   * Local variable has 1 index within the constant pool.
   * {@link org.objectweb.asm.MethodWriter#visitLocalVariable(String, String, String, Label, Label, int)}
   * <p>
   * For upper estimation of max index value, suppose that each field and local
```

### JavadocReference
Cannot resolve symbol `Label`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
   * <p>
   * Local variable has 1 index within the constant pool.
   * {@link org.objectweb.asm.MethodWriter#visitLocalVariable(String, String, String, Label, Label, int)}
   * <p>
   * For upper estimation of max index value, suppose that each field and local
```

### JavadocReference
Cannot resolve symbol `generateBody()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillSimpleFuncHolder.java`
#### Snippet
```java
   * if requested.
   *
   * @see {@link #generateBody()}
   */
  protected HoldingContainer generateEvalBody(ClassGenerator<?> g, HoldingContainer[] inputVariables, String body,
```

### JavadocReference
Cannot resolve symbol `Java.CompilationUnit`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/MethodGrabbingVisitor.java`
#### Snippet
```java
  /**
   * Creates a map with all method names and their modified bodies
   * from specified {@link Java.CompilationUnit}.
   *
   * @param compilationUnit the source of the methods to collect
```

### JavadocReference
Cannot resolve symbol `org.codehaus.janino.Java.CompilationUnit`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionInitializer.java`
#### Snippet
```java
  /**
   * Using class name generates path to class source code (*.java),
   * reads its content as string and parses it into {@link org.codehaus.janino.Java.CompilationUnit}.
   *
   * @param clazz function class
```

### JavadocReference
Symbol `org.apache.drill.exec.expr.fn.HiveFunctionRegistry` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionImplementationRegistry.java`
#### Snippet
```java
 * Registry for functions. Notably, in addition to Drill its functions (in
 * {@link LocalFunctionRegistry}), other PluggableFunctionRegistry (e.g.,
 * {@link org.apache.drill.exec.expr.fn.HiveFunctionRegistry}) is also
 * registered in this class
 */
```

### JavadocReference
Cannot resolve symbol `DrillFuncHolder`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
     * holders, complex variables use readers.
     * <p>
     * Code in {@link DrillFuncHolder#declareInputVariable} converts the local
     * variable into the form requested by the </code>{@literal @}Param</code>
     * annotation which itself can be a value holder or a reader.
```

### JavadocReference
Cannot resolve symbol `declareInputVariable`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
     * holders, complex variables use readers.
     * <p>
     * Code in {@link DrillFuncHolder#declareInputVariable} converts the local
     * variable into the form requested by the </code>{@literal @}Param</code>
     * annotation which itself can be a value holder or a reader.
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.vector.complex.reader.BaseReader.DictReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
    /**
     * Adds code to {@code eval} block which reads values by key from {@code expr} which is an instance of
     * {@link org.apache.drill.exec.vector.complex.reader.BaseReader.DictReader}.
     *
     *
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.vector.complex.writer.BaseWriter.ComplexWriter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/annotations/FunctionTemplate.java`
#### Snippet
```java
     *
     * <p>Not Supported for aggregate functions and for functions with {@link Output} of type
     * {@link org.apache.drill.exec.vector.complex.writer.BaseWriter.ComplexWriter}.</p>
     */
    NULL_IF_NULL
```

### JavadocReference
Cannot resolve symbol `DataInput`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ByteBufUtil.java`
#### Snippet
```java
  /**
   * Modeled after {@code org.apache.hadoop.io.WritableUtils}.
   * We copy the code to avoid wrapping {@link DrillBuf} to/from {@link DataInput}.
   */
  public static class HadoopWritables {
```

### JavadocReference
Cannot resolve symbol `ListMultimap`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/registry/FunctionRegistryHolder.java`
#### Snippet
```java
  /**
   * Returns list of functions with list of function signatures for each functions.
   * Uses guava {@link ListMultimap} structure to return data.
   * If no functions present, will return empty {@link ListMultimap}.
   * This is a read operation, so several users can perform this operation at the same time.
```

### JavadocReference
Cannot resolve symbol `ListMultimap`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/registry/FunctionRegistryHolder.java`
#### Snippet
```java
   * Returns list of functions with list of function signatures for each functions.
   * Uses guava {@link ListMultimap} structure to return data.
   * If no functions present, will return empty {@link ListMultimap}.
   * This is a read operation, so several users can perform this operation at the same time.
   *
```

### JavadocReference
Cannot resolve symbol `ListMultimap`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/registry/FunctionRegistryHolder.java`
#### Snippet
```java
  /**
   * Returns list of functions with list of function holders for each functions.
   * Uses guava {@link ListMultimap} structure to return data.
   * If no functions present, will return empty {@link ListMultimap}.
   * If version holder is not null, updates it with current registry version number.
```

### JavadocReference
Cannot resolve symbol `ListMultimap`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/registry/FunctionRegistryHolder.java`
#### Snippet
```java
   * Returns list of functions with list of function holders for each functions.
   * Uses guava {@link ListMultimap} structure to return data.
   * If no functions present, will return empty {@link ListMultimap}.
   * If version holder is not null, updates it with current registry version number.
   * This is a read operation, so several users can perform this operation at the same time.
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.UserGroupInformation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java

  /**
   * Create and return proxy user {@link org.apache.hadoop.security.UserGroupInformation} for given user name.
   *
   * @param proxyUserName Proxy user name (must be valid)
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.UserGroupInformation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java

  /**
   * Return the {@link org.apache.hadoop.security.UserGroupInformation} of user who is running the Drillbit.
   *
   * @return Drillbit process user {@link org.apache.hadoop.security.UserGroupInformation}.
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.UserGroupInformation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
   * Return the {@link org.apache.hadoop.security.UserGroupInformation} of user who is running the Drillbit.
   *
   * @return Drillbit process user {@link org.apache.hadoop.security.UserGroupInformation}.
   */
  public static UserGroupInformation getProcessUserUGI() {
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.UserGroupInformation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
  }
  /**
   * Create and return proxy user {@link org.apache.hadoop.security.UserGroupInformation} of operator owner if operator
   * owner is valid. Otherwise create and return proxy user {@link org.apache.hadoop.security.UserGroupInformation} for
   * query user.
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.UserGroupInformation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
  /**
   * Create and return proxy user {@link org.apache.hadoop.security.UserGroupInformation} of operator owner if operator
   * owner is valid. Otherwise create and return proxy user {@link org.apache.hadoop.security.UserGroupInformation} for
   * query user.
   *
```

### JavadocReference
Cannot resolve symbol `recursive`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/DrillFileSystemUtil.java`
#### Snippet
```java
   * @param fs current file system
   * @param path path to file or directory
   * @param recursive true if files in nested directories should be included
   * @param filters list of custom filters (optional)
   * @return list of at most one matching file status
```

### JavadocReference
Cannot resolve symbol `ResultSetMetaData`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
  /**
   * Static list of mappings from {@link MinorType} to JDBC ResultSet class name
   * (to be returned through {@link ResultSetMetaData#getColumnClassName(int)}.
   */
  private static final Map<MinorType, String> DRILL_TYPE_TO_JDBC_CLASSNAME = ImmutableMap.<MinorType, String>builder()
```

### JavadocReference
Cannot resolve symbol `getColumnClassName(int)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
  /**
   * Static list of mappings from {@link MinorType} to JDBC ResultSet class name
   * (to be returned through {@link ResultSetMetaData#getColumnClassName(int)}.
   */
  private static final Map<MinorType, String> DRILL_TYPE_TO_JDBC_CLASSNAME = ImmutableMap.<MinorType, String>builder()
```

### JavadocReference
Cannot resolve symbol `ACL`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProvider.java`
#### Snippet
```java

  /**
   * Returns the list of ZK ACLs {@link ACL} to apply for a specific
   * Drill znode
   * @param path The path for which the ACL is being looked up
```

### JavadocReference
Cannot resolve symbol `ACL`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProvider.java`
#### Snippet
```java
   * Drill znode
   * @param path The path for which the ACL is being looked up
   * @return List of ZK ACLs {@link ACL} for the provided path
   */
  List<ACL> getDrillAclForPath(String path);
```

### JavadocReference
Cannot resolve symbol `ACL`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProvider.java`
#### Snippet
```java

  /**
   * Returns the list of ZK ACLs {@link ACL} to apply by default
   * on Drill znodes
   * @return  List of ZK ACLs {@link ACL}
```

### JavadocReference
Cannot resolve symbol `ACL`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProvider.java`
#### Snippet
```java
   * Returns the list of ZK ACLs {@link ACL} to apply by default
   * on Drill znodes
   * @return  List of ZK ACLs {@link ACL}
   */
  List<ACL> getDrillDefaultAcl();
```

### JavadocReference
Cannot resolve symbol `Stat`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   *
   * If consistency flag is set to true and version holder is not null, passes version holder to get data change version.
   * Data change version is retrieved from {@link Stat} object, it increases each time znode data change is performed.
   * Link to Zookeeper documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
   *
```

### JavadocReference
Cannot resolve symbol `Stat`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   *
   * If consistency flag is set to true and version holder is not null, passes version holder to get data change version.
   * Data change version is retrieved from {@link Stat} object, it increases each time znode data change is performed.
   * Link to Zookeeper documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
   *
```

### JavadocReference
Cannot resolve symbol `org.apache.zookeeper.KeeperException.BadVersionException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   * Zookeeper maintains stat structure that holds version number which increases each time znode data change is performed.
   * If we pass version that doesn't match the actual version of the data,
   * the update will fail {@link org.apache.zookeeper.KeeperException.BadVersionException}.
   * We catch such exception and re-throw it as {@link VersionMismatchException}.
   * Link to documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.expr.fn.impl.DirectoryExplorers`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PartitionExplorer.java`
#### Snippet
```java
 * </pre>
 *
 * Look at {@link org.apache.drill.exec.expr.fn.impl.DirectoryExplorers}
 * for examples of UDFs that use this interface to query against
 * partition information.
```

### JavadocReference
Cannot resolve symbol `remove(PluginHandle)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
   * no entry by the given name
   * @throws PluginException for an attempt to remove a system plugin
   * @see {@link #remove(PluginHandle)
   */
  public synchronized PluginHandle remove(String name) throws PluginException {
```

### JavadocReference
Cannot resolve symbol `RelSubset`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SubsetRemover.java`
#### Snippet
```java

/**
 * Removes {@link RelSubset} nodes from the plan.
 * It may be useful when we want to do some operations on the plan
 * which aren't supported by {@link RelSubset} during the planning phase.
```

### JavadocReference
Cannot resolve symbol `RelSubset`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SubsetRemover.java`
#### Snippet
```java
 * Removes {@link RelSubset} nodes from the plan.
 * It may be useful when we want to do some operations on the plan
 * which aren't supported by {@link RelSubset} during the planning phase.
 */
public class SubsetRemover extends RelShuttleImpl {
```

### JavadocReference
Cannot resolve symbol `verifiedPut()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   * disabled, moves the plugin from the in-memory cache to the ephemeral
   * store. If a formerly disabled plugin is enabled, verifies that the plugin
   * can be instantiated as for {@link #verifiedPut()}.
   * <p>
   * Use this method when changing state. Do not obtain the config and change
```

### JavadocReference
Cannot resolve symbol `SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SchemaConfig.java`
#### Snippet
```java

  /**
   * @return User whom to impersonate as while creating {@link SchemaPlus} instances
   * interact with the underlying storage.
   */
```

### JavadocReference
Cannot resolve symbol `SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SchemaConfig.java`
#### Snippet
```java

  /**
   * @return Should ignore if authorization errors are reported while {@link SchemaPlus}
   * instances interact with the underlying storage.
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.type.SqlTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordDataType.java`
#### Snippet
```java

  /**
   * @return the {@link org.apache.calcite.sql.type.SqlTypeName} of columns in the table as a pair with its nullability
   */
  public abstract List<SimpleImmutableEntry<SqlTypeName, Boolean>> getFieldSqlTypeNames();
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.type.RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordDataType.java`
#### Snippet
```java

  /**
   * This method constructs a {@link org.apache.calcite.rel.type.RelDataType} based on the
   * {@link org.apache.drill.exec.store.RecordDataType}'s field sql types and field names.
   *
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.type.RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordDataType.java`
#### Snippet
```java
   * {@link org.apache.drill.exec.store.RecordDataType}'s field sql types and field names.
   *
   * @param factory helps construct a {@link org.apache.calcite.rel.type.RelDataType}
   * @return the constructed type
   */
```

### JavadocReference
Cannot resolve symbol `Table`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/AbstractSchema.java`
#### Snippet
```java

  /**
   * Get the collection of {@link Table} tables specified in the tableNames.
   *
   * @param  tableNames the requested tables, specified by the table names
```

### JavadocReference
Cannot resolve symbol `STORAGE_PLUGINS_OVERRIDE_CONF`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
  /**
   * Get the new storage plugins from the
   * {@link ConfigConstants#STORAGE_PLUGINS_OVERRIDE_CONF} file if it exists,
   * null otherwise
   */
```

### JavadocReference
Cannot resolve symbol `CompressionOutputStream`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/ZipCodec.java`
#### Snippet
```java
  /**
   * Extends {@link ZipOutputStream} to allow resetting compressor stream,
   * required by {@link CompressionOutputStream} implementation.
   */
  private static class ResetableZipOutputStream extends ZipOutputStream {
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.io.compress.CompressionCodecFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/ZipCodec.java`
#### Snippet
```java
 * <p/>
 * Note: Do not rename this class. Class naming must be 'ZipCodec' so it can be mapped by
 * {@link org.apache.hadoop.io.compress.CompressionCodecFactory} to the 'zip' extension.
 */
public class ZipCodec extends DefaultCodec {
```

### JavadocReference
Cannot resolve symbol `setPermission(Path, FsPermission)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
   * @param progress    the progress reporter
   * @throws IOException IO failure
   * @see #setPermission(Path, FsPermission)
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `Path`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
   * @param progress    the progress reporter
   * @throws IOException IO failure
   * @see #setPermission(Path, FsPermission)
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `FsPermission`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
   * @param progress    the progress reporter
   * @throws IOException IO failure
   * @see #setPermission(Path, FsPermission)
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  /**
   * Returns a copy of {@link Configuration} for this {@link DrillFileSystem}.
   * <b>Note: </b> a copy of the {@link Configuration} is returned in order to enforce immutability.
   * @return A copy of {@link Configuration} for this {@link DrillFileSystem}.
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java
  /**
   * Returns a copy of {@link Configuration} for this {@link DrillFileSystem}.
   * <b>Note: </b> a copy of the {@link Configuration} is returned in order to enforce immutability.
   * @return A copy of {@link Configuration} for this {@link DrillFileSystem}.
   */
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java
   * Returns a copy of {@link Configuration} for this {@link DrillFileSystem}.
   * <b>Note: </b> a copy of the {@link Configuration} is returned in order to enforce immutability.
   * @return A copy of {@link Configuration} for this {@link DrillFileSystem}.
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.io.compress.CompressionCodec`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  /**
   * Returns the {@link org.apache.hadoop.io.compress.CompressionCodec} for a given file.  This
   * can be used to determine the type of compression (if any) which was used.  Returns null if the
   * file is not compressed.
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.fs.FSDataInputStream`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  /**
   * If OperatorStats are provided return a instrumented {@link org.apache.hadoop.fs.FSDataInputStream}.
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.fs.FSDataInputStream`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  /**
   * If OperatorStats are provided return a instrumented {@link org.apache.hadoop.fs.FSDataInputStream}.
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `FileSystem`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java

  /**
   * Returns a copy of the underlying file system.  This class implements {@link FileSystem}
   * @return The underlying {@link FileSystem}
   */
```

### JavadocReference
Cannot resolve symbol `FileSystem`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DrillFileSystem.java`
#### Snippet
```java
  /**
   * Returns a copy of the underlying file system.  This class implements {@link FileSystem}
   * @return The underlying {@link FileSystem}
   */
  public FileSystem getUnderlyingFs() {
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.alias.CredentialProviderFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSystemPlugin.java`
#### Snippet
```java
  /**
   * Retrieve secret and access keys from configured (with
   * {@link org.apache.hadoop.security.alias.CredentialProviderFactory#CREDENTIAL_PROVIDER_PATH} property)
   * credential providers and set it into {@code conf}. If provider path is not configured or credential
   * is absent in providers, it will conditionally fallback to configuration setting. The fallback will occur unless
```

### JavadocReference
Cannot resolve symbol `CREDENTIAL_PROVIDER_PATH`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSystemPlugin.java`
#### Snippet
```java
  /**
   * Retrieve secret and access keys from configured (with
   * {@link org.apache.hadoop.security.alias.CredentialProviderFactory#CREDENTIAL_PROVIDER_PATH} property)
   * credential providers and set it into {@code conf}. If provider path is not configured or credential
   * is absent in providers, it will conditionally fallback to configuration setting. The fallback will occur unless
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.security.alias.CredentialProvider`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSystemPlugin.java`
#### Snippet
```java
   * credential providers and set it into {@code conf}. If provider path is not configured or credential
   * is absent in providers, it will conditionally fallback to configuration setting. The fallback will occur unless
   * {@link org.apache.hadoop.security.alias.CredentialProvider#CLEAR_TEXT_FALLBACK} is set to {@code false}.
   *
   * @param conf {@code Configuration} which will be updated with credentials from provider
```

### JavadocReference
Cannot resolve symbol `CLEAR_TEXT_FALLBACK`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSystemPlugin.java`
#### Snippet
```java
   * credential providers and set it into {@code conf}. If provider path is not configured or credential
   * is absent in providers, it will conditionally fallback to configuration setting. The fallback will occur unless
   * {@link org.apache.hadoop.security.alias.CredentialProvider#CLEAR_TEXT_FALLBACK} is set to {@code false}.
   *
   * @param conf {@code Configuration} which will be updated with credentials from provider
```

### JavadocReference
Cannot resolve symbol `Path`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
   *          otherwise a new selection.
   *
   * @see FileSelection#FileSelection(List, List, Path)
   */
  public static FileSelection create(List<FileStatus> statuses, List<Path> files, Path root,
```

### JavadocReference
Cannot resolve symbol `GenericRecord`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/avro/AvroColumnConverterFactory.java`
#### Snippet
```java
   * Converts and writes all map children using provided {@link #converters}.
   * If {@link #converters} are empty, generates their converters based on
   * {@link GenericRecord} schema.
   */
  public static class MapColumnConverter implements ColumnConverter {
```

### JavadocReference
Cannot resolve symbol
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/ClosingStreamIterator.java`
#### Snippet
```java
 * It allows setting the current value in the iterator and can be used once after {@link #next} call
 *
 * @param <T> type of the value
 */
public class ClosingStreamIterator implements Iterator<InputStream> {
```

### JavadocReference
Cannot resolve symbol `element(ValueDef)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/ArrayListener.java`
#### Snippet
```java
/**
 * Represents one level within array. The first time the parser sees the array element,
 * it will call the {@link #element(ValueDef)} method with the
 * look-ahead values visible to the parser. Since JSON is flexible, later
 * data shapes may not necessarily follow the first shape. The implementation
```

### JavadocReference
Symbol `org.apache.drill.exec.vector.complex.fn.VectorOutput.MapVectorOutput` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/extended/package-info.java`
#### Snippet
```java
 *
 *
 * @see org.apache.drill.exec.vector.complex.fn.VectorOutput.MapVectorOutput MapVectorOutput
 * for an older implementation
*/
```

### JavadocReference
Cannot resolve symbol `TestReverseImplicitCast`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 * Create a scan operator that looks like the following (from
 * <tt>/src/test/resources/functions/cast/two_way_implicit_cast.json</tt>,
 * used in {@link TestReverseImplicitCast}):
 * <pre><code>
 *    graph:[
```

### JavadocReference
Cannot resolve symbol `MinorType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 * to be the traditional names used in Drill tests.</li>
 * <li>The <tt>type</tt> is one of the supported Drill
 * {@link MinorType} names.</li>
 * <li>The <tt>mode</tt> is one of the supported Drill
 * {@link DataMode} names: usually <tt>OPTIONAL</tt> or <tt>REQUIRED</tt>.</li>
```

### JavadocReference
Cannot resolve symbol `DataMode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 * {@link MinorType} names.</li>
 * <li>The <tt>mode</tt> is one of the supported Drill
 * {@link DataMode} names: usually <tt>OPTIONAL</tt> or <tt>REQUIRED</tt>.</li>
 * </ul>
 * <p>
```

### JavadocReference
Cannot resolve symbol `MockTableDef.MockColumn`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 * controlled by custom generator classes. When
 * in a physical plan, the <tt>records</tt> section has additional
 * attributes as described in {@link MockTableDef.MockColumn}:
 * <ul>
 * <li>The <tt>generator</tt> lets you specify a class to generate the
```

### JavadocReference
Cannot resolve symbol `FieldGen`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 * generator class to generate the kind of data needed for that test.
 * <p>
 * All data generators implement the {@link FieldGen} interface, and must have
 * a non-argument constructor to allow dynamic instantiation. The mock data
 * source either picks a default generator (if no <tt>generator</tt> is provided)
```

### JavadocReference
Cannot resolve symbol `Integer.MIN_VALUE`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/IntGen.java`
#### Snippet
```java
 * Generates integer values uniformly randomly distributed over
 * the entire 32-bit integer range from
 * {@link Integer.MIN_VALUE} to {@link Integer.MAX_VALUE}.
 */

```

### JavadocReference
Cannot resolve symbol `Integer.MAX_VALUE`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/IntGen.java`
#### Snippet
```java
 * Generates integer values uniformly randomly distributed over
 * the entire 32-bit integer range from
 * {@link Integer.MIN_VALUE} to {@link Integer.MAX_VALUE}.
 */

```

### JavadocReference
Cannot resolve symbol `schema`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/FilterEvaluator.java`
#### Snippet
```java
   *
   * @param schemaName name of the schema
   * @param schema schema object
   * @return whether to prune this schema and all its descendants from the
   * search tree.
```

### JavadocReference
Cannot resolve symbol `ByteBufferAllocator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetDirectByteBufferAllocator.java`
#### Snippet
```java

/**
 * {@link ByteBufferAllocator} implementation that uses Drill's {@link BufferAllocator} to allocate and release
 * {@link ByteBuffer} objects.<br>
 * To properly release an allocated {@link DrillBuf}, this class keeps track of it's corresponding {@link ByteBuffer}
```

### JavadocReference
Cannot resolve symbol `org.apache.parquet.column.ParquetProperties.WriterVersion`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java
public class ParquetFormatPlugin implements FormatPlugin {

  /** {@link org.apache.parquet.column.ParquetProperties.WriterVersion} */
  public static final String[] PARQUET_FORMAT_VERSIONS = { "v1", "v2" };

```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.expr.holders.NullableTimeStampHolder`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
   *          represented by @param binaryTimeStampValue.
   *          The nanos precision is cut to millis. Therefore the length of single timestamp value is
   *          {@value org.apache.drill.exec.expr.holders.NullableTimeStampHolder#WIDTH} bytes instead of 12 bytes.
   */
    public static long getDateTimeValueFromBinary(Binary binaryTimeStampValue, boolean retainLocalTimezone) {
```

### JavadocReference
Cannot resolve symbol `WIDTH`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
   *          represented by @param binaryTimeStampValue.
   *          The nanos precision is cut to millis. Therefore the length of single timestamp value is
   *          {@value org.apache.drill.exec.expr.holders.NullableTimeStampHolder#WIDTH} bytes instead of 12 bytes.
   */
    public static long getDateTimeValueFromBinary(Binary binaryTimeStampValue, boolean retainLocalTimezone) {
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java

  /**
   * Converts list of {@link OriginalType}s to list of {@link org.apache.drill.common.types.TypeProtos.MajorType}s.
   * <b>NOTE</b>: current implementation cares about {@link OriginalType#MAP} and {@link OriginalType#LIST} only
   * converting it to {@link org.apache.drill.common.types.TypeProtos.MinorType#DICT}
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
  /**
   * Converts list of {@link OriginalType}s to list of {@link org.apache.drill.common.types.TypeProtos.MajorType}s.
   * <b>NOTE</b>: current implementation cares about {@link OriginalType#MAP} and {@link OriginalType#LIST} only
   * converting it to {@link org.apache.drill.common.types.TypeProtos.MinorType#DICT}
   * and {@link org.apache.drill.common.types.TypeProtos.MinorType#LIST} respectively.
```

### JavadocReference
Cannot resolve symbol `MAP`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
  /**
   * Converts list of {@link OriginalType}s to list of {@link org.apache.drill.common.types.TypeProtos.MajorType}s.
   * <b>NOTE</b>: current implementation cares about {@link OriginalType#MAP} and {@link OriginalType#LIST} only
   * converting it to {@link org.apache.drill.common.types.TypeProtos.MinorType#DICT}
   * and {@link org.apache.drill.common.types.TypeProtos.MinorType#LIST} respectively.
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
  /**
   * Converts list of {@link OriginalType}s to list of {@link org.apache.drill.common.types.TypeProtos.MajorType}s.
   * <b>NOTE</b>: current implementation cares about {@link OriginalType#MAP} and {@link OriginalType#LIST} only
   * converting it to {@link org.apache.drill.common.types.TypeProtos.MinorType#DICT}
   * and {@link org.apache.drill.common.types.TypeProtos.MinorType#LIST} respectively.
```

### JavadocReference
Cannot resolve symbol `LIST`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
  /**
   * Converts list of {@link OriginalType}s to list of {@link org.apache.drill.common.types.TypeProtos.MajorType}s.
   * <b>NOTE</b>: current implementation cares about {@link OriginalType#MAP} and {@link OriginalType#LIST} only
   * converting it to {@link org.apache.drill.common.types.TypeProtos.MinorType#DICT}
   * and {@link org.apache.drill.common.types.TypeProtos.MinorType#LIST} respectively.
```

### JavadocReference
Cannot resolve symbol `Type.Repetition`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java

  /**
   * Converts Parquet's {@link Type.Repetition} to Drill's {@link TypeProtos.DataMode}.
   * @param repetition repetition to be converted
   * @return data mode corresponding to Parquet's repetition
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java

  /**
   * Returns {@link OriginalType} type for the specified column.
   *
   * @param parquetTableMetadata the source of column type
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
   *
   * @param parquetTableMetadata the source of column type
   * @param column               column whose {@link OriginalType} should be returned
   * @return {@link OriginalType} type for the specified column
   */
```

### JavadocReference
Cannot resolve symbol `OriginalType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
   * @param parquetTableMetadata the source of column type
   * @param column               column whose {@link OriginalType} should be returned
   * @return {@link OriginalType} type for the specified column
   */
  public static OriginalType getOriginalType(MetadataBase.ParquetTableMetadataBase parquetTableMetadata, MetadataBase.ColumnMetadata column) {
```

### JavadocReference
Cannot resolve symbol `PrimitiveType.PrimitiveTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java

  /**
   * Returns {@link PrimitiveType.PrimitiveTypeName} type for the specified column.
   *
   * @param parquetTableMetadata the source of column type
```

### JavadocReference
Cannot resolve symbol `PrimitiveType.PrimitiveTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
   *
   * @param parquetTableMetadata the source of column type
   * @param column               column whose {@link PrimitiveType.PrimitiveTypeName} should be returned
   * @return {@link PrimitiveType.PrimitiveTypeName} type for the specified column
   */
```

### JavadocReference
Cannot resolve symbol `PrimitiveType.PrimitiveTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
   * @param parquetTableMetadata the source of column type
   * @param column               column whose {@link PrimitiveType.PrimitiveTypeName} should be returned
   * @return {@link PrimitiveType.PrimitiveTypeName} type for the specified column
   */
  public static PrimitiveType.PrimitiveTypeName getPrimitiveTypeName(MetadataBase.ParquetTableMetadataBase parquetTableMetadata, MetadataBase.ColumnMetadata column) {
```

### JavadocReference
Cannot resolve symbol `org.apache.parquet.schema.Type.Repetition`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
   * Obtain data mode from table metadata for a column. Algorithm for retrieving data mode depends on metadata version:
   * <ul>
   *   <li>starting from version {@code 4.2}, Parquet's {@link org.apache.parquet.schema.Type.Repetition}
   *   is stored in table metadata itself;</li>
   *   <li>starting from {@code 3.0} to {@code 4.2} (exclusively) the data mode is
```

### JavadocReference
Cannot resolve symbol `HashBasedTable`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/BaseParquetMetadataProvider.java`
#### Snippet
```java

  /**
   * {@link HashBasedTable} cannot contain nulls, used this object to represent null values.
   */
  public static final Object NULL_VALUE = new Object();
```

### JavadocReference
Cannot resolve symbol `org.apache.parquet.schema.Type.Repetition`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/MetadataVersion.java`
#### Snippet
```java

    /**
     * Version 4.2: Added {@link org.apache.parquet.schema.Type.Repetition} to {@link Metadata_V4.ColumnTypeMetadata_v4}.
     */
    public static final String V4_2 = "4.2";
```

### JavadocReference
Cannot resolve symbol `ValuesReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
     * Set the ValuesReader object; if a null value is passed, then it is understood
     * the current page doesn't use an encoding like dictionary or delta.
     * @param _rawReader {@link ValuesReader} object
     */
    void set(ValuesReader _rawReader) {
```

### JavadocReference
Cannot resolve symbol `ValuesReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
     * Set the {@link PageReader#definitionLevels} object; if a null value is passed, then it is understood
     * the current page doesn't have definition levels to be processed
     * @param definitionLevels {@link ValuesReader} object
     * @param numValues total number of values that can be read from the stream
     */
```

### JavadocReference
Cannot resolve symbol `ValuesReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java

    /**
     * @return internally reads the next integer from the underlying {@link ValuesReader}; false if the stream
     *         reached EOF
     */
```

### JavadocReference
Cannot resolve symbol `ValuesReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
    /**
     * @return the current integer from the page; this method has no side-effects (the underlying
     *         {@link ValuesReader} is not affected)
     */
    public int readCurrInteger() {
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java
    }

    /** @return "bits" buffer; null if the associated {@link ValueVector} is not nullable */
    private DrillBuf getBitsBuffer() {
      return buffers.length == 3 ? buffers[0] : null;
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

    /**
     * Copy the "values" vector if the {@link ValueVector}.
     *
     * @param firstValueIdx start index of the first value to copy
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

    /**
     * Copy the "offset" vector if the {@link ValueVector}; note that no adjustment of the offsets will be done.
     * This task will be done during overflow data de-serialization.
     *
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

    /**
     * Copy the "bits" vector if the {@link ValueVector} is nullable; NOOP otherwise.
     *
     * @param firstValueIdx start index of the first value to copy
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

  /**
   * Helper class for handling variable length {@link ValueVector} overflow serialization logic
   */
  private static final class VLVectorSerializer {
```

### JavadocReference
Cannot resolve symbol `ValueVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

    /**
     * The number of bytes used (by the {@link ValueVector}) to store a value range delimited
     * by the parameters "firstValueIdx" and "numValues".
     *
```

### JavadocReference
Cannot resolve symbol `TypeHelper`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
  /**
   * This method will return a default value for variable columns; it aims at minimizing internal fragmentation.
   * <p><b>Note</b> that the {@link TypeHelper} uses a large default value which might not be always appropriate.
   *
   * @param column fixed column's metadata
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/security/HadoopCredentialsProvider.java`
#### Snippet
```java
/**
 * Implementation of {@link CredentialsProvider} that obtains credential values from
 * {@link Configuration} properties.
 * <p>
 * Its constructor accepts a map with credential names as keys and property names as values.
```

### JavadocReference
Cannot resolve symbol `STOP`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatch.java`
#### Snippet
```java
   * <p>
   *   Once a RecordBatch's {@code next()} has returned {@link IterOutcome#NONE}
   *   or {@link IterOutcome#STOP}, the consumer should no longer call
   *   {@code next()}.  Behavior at this point is undefined and likely to
   *   throw an exception.
```

### JavadocReference
Cannot resolve symbol `requestMemory()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatch.java`
#### Snippet
```java
   * The former {@code OUT_OF_MEMORY} state was never really used.
   * It is now handled by calling
   * {@link FragmentContext#requestMemory()}
   * at the point that the operator realizes it is short on memory.
   * <p>
```

### JavadocReference
Cannot resolve symbol `Viewable`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java

/**
 * Overrides {@link Viewable} to create a model which contains additional info of what control to display in menubar.
 */
public class ViewableWithPermissions extends Viewable {
```

### JavadocReference
Cannot resolve symbol `org.apache.http.client.methods.HttpGet`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebUtils.java`
#### Snippet
```java
   * Send an HTTP request and return response body as String.
   *
   * @param httpRequest {@link org.apache.http.client.methods.HttpGet} or {@link org.apache.http.client.methods.HttpPost} instance representing an HTTP request.
   * @param drillConfig {@link DrillConfig} instance needed to setup HTTP Client.
   * @return String response body.
```

### JavadocReference
Cannot resolve symbol `org.apache.http.client.methods.HttpPost`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebUtils.java`
#### Snippet
```java
   * Send an HTTP request and return response body as String.
   *
   * @param httpRequest {@link org.apache.http.client.methods.HttpGet} or {@link org.apache.http.client.methods.HttpPost} instance representing an HTTP request.
   * @param drillConfig {@link DrillConfig} instance needed to setup HTTP Client.
   * @return String response body.
```

### JavadocReference
Cannot resolve symbol `ServerConnector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebServer.java`
#### Snippet
```java
   * certificate is generated and used.
   *
   * @return Initialized {@link ServerConnector} for HTTPS connections.
   */
  private ServerConnector createHttpsConnector(
```

### JavadocReference
Cannot resolve symbol `ServerConnector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebServer.java`
#### Snippet
```java
   * Create HTTP connector.
   *
   * @return Initialized {@link ServerConnector} instance for HTTP connections.
   */
  private ServerConnector createHttpConnector(String bindAddr, int port, int acceptors, int selectors) {
```

### JavadocReference
Cannot resolve symbol `SessionHandler`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebServer.java`
#### Snippet
```java

  /**
   * Create a {@link SessionHandler}
   *
   * @param securityHandler Set of init parameters that are used by the Authentication
```

### JavadocReference
Cannot resolve symbol `ConstraintSecurityHandler`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillHttpConstraintSecurityHandler.java`
#### Snippet
```java

/**
 * Accessor class that extends the {@link ConstraintSecurityHandler} to expose
 * protected method's for start and stop of Handler. This is needed since now
 * {@code DrillHttpSecurityHandlerProvider} composes of 2 security handlers -
```

### JavadocReference
Cannot resolve symbol `ServerAuthException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param mandatory
   * @return
   * @throws ServerAuthException
   */
  private Authentication authenticateSession(ServletRequest request, ServletResponse response, boolean mandatory)
```

### JavadocReference
Cannot resolve symbol `SpnegoAuthenticator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
  /**
   * Updated logic as compared to default implementation in
   * {@link SpnegoAuthenticator#validateRequest(ServletRequest, ServletResponse, boolean)} to handle below cases:
   * 1) Perform SPNEGO authentication only when spnegoLogin resource is requested. This helps to avoid authentication
   *    for each and every resource which the JETTY provided authenticator does.
```

### JavadocReference
Cannot resolve symbol `validateRequest(ServletRequest, ServletResponse, boolean)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
  /**
   * Updated logic as compared to default implementation in
   * {@link SpnegoAuthenticator#validateRequest(ServletRequest, ServletResponse, boolean)} to handle below cases:
   * 1) Perform SPNEGO authentication only when spnegoLogin resource is requested. This helps to avoid authentication
   *    for each and every resource which the JETTY provided authenticator does.
```

### JavadocReference
Cannot resolve symbol `ServerAuthException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param mandatoryAuth
   * @return
   * @throws ServerAuthException
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `SslContextFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ssl/SslContextFactoryConfigurator.java`
#### Snippet
```java

/**
 * Configures {@link SslContextFactory} when https is enabled for Web UI
 */
public class SslContextFactoryConfigurator {
```

### JavadocReference
Cannot resolve symbol `FragmentContextImpl`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/FragmentOptionManager.java`
#### Snippet
```java

/**
 * {@link OptionManager} that holds options within {@link FragmentContextImpl}.
 */
public class FragmentOptionManager extends InMemoryOptionManager {
```

### JavadocReference
Cannot resolve symbol `UserException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionManager.java`
#### Snippet
```java
   * @param name The name of the option to retrieve a validator for.
   * @return The option validator corresponding to the given option name.
   * @throws UserException - if the definition is not found
   */
  @NotNull
```

### JavadocReference
Cannot resolve symbol `UserException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValidator.java`
#### Snippet
```java
   * @param value the value to validate
   * @param manager the manager for accessing validation dependencies (options)
   * @throws UserException message to describe error with value, including range or list of expected values
   */
  public abstract void validate(OptionValue value, OptionMetaData optionMetaData, OptionSet manager);
```

### JavadocReference
Cannot resolve symbol `PersistentStore`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
 * {@link OptionManager} to store a run-time setting. This setting, for example,
 * could affect a query in an execution stage. Instances of this class are JSON
 * serializable and can be stored in a {@link PersistentStore persistent store}
 * (see {@link SystemOptionManager#options}), or in memory (see
 * {@link InMemoryOptionManager#options}).
```

### JavadocReference
Symbol `options` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
 * could affect a query in an execution stage. Instances of this class are JSON
 * serializable and can be stored in a {@link PersistentStore persistent store}
 * (see {@link SystemOptionManager#options}), or in memory (see
 * {@link InMemoryOptionManager#options}).
 * </p>
```

### JavadocReference
Cannot resolve symbol `ResultSetMetaData`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/accessor/SqlAccessor.java`
#### Snippet
```java
   * Reports the class returned by getObject() of this accessor.
   * <p>
   *  (Is for {@link ResultSetMetaData#getColumnClassName(...)}.)
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `getColumnClassName(...)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/accessor/SqlAccessor.java`
#### Snippet
```java
   * Reports the class returned by getObject() of this accessor.
   * <p>
   *  (Is for {@link ResultSetMetaData#getColumnClassName(...)}.)
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/RetargetableClassVisitor.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int)}.
   */
  public RetargetableClassVisitor(final int api) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor(int)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/RetargetableClassVisitor.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int)}.
   */
  public RetargetableClassVisitor(final int api) {
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/RetargetableClassVisitor.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public RetargetableClassVisitor(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor(int, ClassVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/RetargetableClassVisitor.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public RetargetableClassVisitor(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/RetargetableClassVisitor.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public RetargetableClassVisitor(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int)}.
   */
  public InnerClassAccessStripper(final int api) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor(int)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int)}.
   */
  public InnerClassAccessStripper(final int api) {
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.MethodVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.MethodVisitor#MethodVisitor(int, MethodVisitor)}.
   */
  public CheckMethodVisitorFsm(final int api, final MethodVisitor mv) {
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public InnerClassAccessStripper(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `MethodVisitor(int, MethodVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.MethodVisitor#MethodVisitor(int, MethodVisitor)}.
   */
  public CheckMethodVisitorFsm(final int api, final MethodVisitor mv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor(int, ClassVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public InnerClassAccessStripper(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `MethodVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.MethodVisitor#MethodVisitor(int, MethodVisitor)}.
   */
  public CheckMethodVisitorFsm(final int api, final MethodVisitor mv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public InnerClassAccessStripper(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java
   * Return the original class' access bits.
   *
   * <p>This may only be called after {@link ClassVisitor#visit(int, int, String, String, String, String[])}
   * has been called; that's where the bits are stripped and captured.
   *
```

### JavadocReference
Cannot resolve symbol `visit(int, int, String, String, String, String[])`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/InnerClassAccessStripper.java`
#### Snippet
```java
   * Return the original class' access bits.
   *
   * <p>This may only be called after {@link ClassVisitor#visit(int, int, String, String, String, String[])}
   * has been called; that's where the bits are stripped and captured.
   *
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/AsmUtil.java`
#### Snippet
```java
   *
   * @param classBytes the bytecode
   * @param asmReaderFlags flags for ASM; see {@link org.objectweb.asm.ClassReader#accept(org.objectweb.asm.ClassVisitor, int)}
   * @return the ClassNode
   */
```

### JavadocReference
Cannot resolve symbol `accept(org.objectweb.asm.ClassVisitor, int)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/AsmUtil.java`
#### Snippet
```java
   *
   * @param classBytes the bytecode
   * @param asmReaderFlags flags for ASM; see {@link org.objectweb.asm.ClassReader#accept(org.objectweb.asm.ClassVisitor, int)}
   * @return the ClassNode
   */
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/AsmUtil.java`
#### Snippet
```java
   *
   * @param classBytes the bytecode
   * @param asmReaderFlags flags for ASM; see {@link org.objectweb.asm.ClassReader#accept(org.objectweb.asm.ClassVisitor, int)}
   * @return the ClassNode
   */
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.util.CheckClassAdapter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   */
  public DrillCheckClassAdapter(final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `CheckClassAdapter(ClassVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   */
  public DrillCheckClassAdapter(final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   */
  public DrillCheckClassAdapter(final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.util.CheckClassAdapter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor, boolean)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `CheckClassAdapter(ClassVisitor, boolean)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor, boolean)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor, boolean)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.util.CheckClassAdapter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#verify(ClassReader, boolean, PrintWriter)}.
   */
  public static void verify(final ClassReader cr, final boolean dump,
```

### JavadocReference
Cannot resolve symbol `verify(ClassReader, boolean, PrintWriter)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#verify(ClassReader, boolean, PrintWriter)}.
   */
  public static void verify(final ClassReader cr, final boolean dump,
```

### JavadocReference
Cannot resolve symbol `ClassReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#verify(ClassReader, boolean, PrintWriter)}.
   */
  public static void verify(final ClassReader cr, final boolean dump,
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.util.CheckClassAdapter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `CheckClassAdapter(ClassVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillCheckClassAdapter.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.util.CheckClassAdapter#CheckClassAdapter(ClassVisitor)}.
   * @param api the api version
   */
```

### JavadocReference
Cannot resolve symbol `org.objectweb.asm.ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public CheckClassVisitorFsm(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor(int, ClassVisitor)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public CheckClassVisitorFsm(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `ClassVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java

  /**
   * See {@link org.objectweb.asm.ClassVisitor#ClassVisitor(int, ClassVisitor)}.
   */
  public CheckClassVisitorFsm(final int api, final ClassVisitor cv) {
```

### JavadocReference
Cannot resolve symbol `CompileException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBuilder.java`
#### Snippet
```java
   * @return the class, after code generation and (if needed) compilation.
   * @throws IOException if an error occurs when optionally writing code to disk.
   * @throws CompileException if the generated code has compile issues.
   * @throws ClassNotFoundException if the generated code references unknown classes.
   * @throws ClassTransformationException generic "something is wrong" error from
```

### JavadocReference
Cannot resolve symbol `RelBuilderFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java
  }

  /** Creates a {@link RelBuilderFactory} that uses a given set of factories. */
  public static RelBuilderFactory proto(Object... factories) {
    return proto(Contexts.of(factories));
```

### JavadocReference
Cannot resolve symbol `RelBuilder`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java

  /**
   * Original method {@link RelBuilder#empty} returns empty values rel.
   * In the order to preserve data row types, filter with false predicate is created.
   */
```

### JavadocReference
Cannot resolve symbol `empty`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java

  /**
   * Original method {@link RelBuilder#empty} returns empty values rel.
   * In the order to preserve data row types, filter with false predicate is created.
   */
```

### JavadocReference
Cannot resolve symbol `RelBuilderFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java
  }

  /** Creates a {@link RelBuilderFactory}, a partially-created DrillRelBuilder.
   * Just add a {@link RelOptCluster} and a {@link RelOptSchema} */
  public static RelBuilderFactory proto(final Context context) {
```

### JavadocReference
Cannot resolve symbol `RelOptCluster`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java

  /** Creates a {@link RelBuilderFactory}, a partially-created DrillRelBuilder.
   * Just add a {@link RelOptCluster} and a {@link RelOptSchema} */
  public static RelBuilderFactory proto(final Context context) {
    Config conf = Util.first(context.unwrap(Config.class), Config.DEFAULT)
```

### JavadocReference
Cannot resolve symbol `RelOptSchema`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DrillRelBuilder.java`
#### Snippet
```java

  /** Creates a {@link RelBuilderFactory}, a partially-created DrillRelBuilder.
   * Just add a {@link RelOptCluster} and a {@link RelOptSchema} */
  public static RelBuilderFactory proto(final Context context) {
    Config conf = Util.first(context.unwrap(Config.class), Config.DEFAULT)
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.core.Join`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/RuleInstance.java`
#### Snippet
```java
  /**
   * Instance of the rule that infers predicates from on a
   * {@link org.apache.calcite.rel.core.Join} and creates
   * {@link org.apache.calcite.rel.core.Filter}s if those predicates can be pushed
   * to its inputs.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.core.Filter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/RuleInstance.java`
#### Snippet
```java
   * Instance of the rule that infers predicates from on a
   * {@link org.apache.calcite.rel.core.Join} and creates
   * {@link org.apache.calcite.rel.core.Filter}s if those predicates can be pushed
   * to its inputs.
   */
```

### JavadocReference
Cannot resolve symbol `SqlOperator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlWrapper.java`
#### Snippet
```java
 * This interface is meant for the users of the wrappers, {@link DrillCalciteSqlOperatorWrapper},
 * {@link DrillCalciteSqlFunctionWrapper} and {@link DrillCalciteSqlAggFunctionWrapper}, to access the wrapped Calcite
 * {@link SqlOperator} without knowing exactly which wrapper it is.
 */
public interface DrillCalciteSqlWrapper {
```

### JavadocReference
Cannot resolve symbol `SqlOperator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlWrapper.java`
#### Snippet
```java
public interface DrillCalciteSqlWrapper {
  /**
   * Get the wrapped {@link SqlOperator}
   *
   * @return SqlOperator get the wrapped {@link SqlOperator}
```

### JavadocReference
Cannot resolve symbol `SqlOperator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlWrapper.java`
#### Snippet
```java
   * Get the wrapped {@link SqlOperator}
   *
   * @return SqlOperator get the wrapped {@link SqlOperator}
   */
  SqlOperator getOperator();
```

### JavadocReference
Cannot resolve symbol `SqlOperatorTable`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java

/**
 * Implementation of {@link SqlOperatorTable} that contains standard operators and functions provided through
 * {@link #inner SqlStdOperatorTable}, and Drill User Defined Functions.
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.type.SqlTypeUtil`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlBetweenOperatorWrapper.java`
#### Snippet
```java
  /**
   * Since Calcite has its rule for type compatibility
   * (see {@link org.apache.calcite.sql.type.SqlTypeUtil#isComparable(org.apache.calcite.rel.type.RelDataType,
   * org.apache.calcite.rel.type.RelDataType)}), which is usually different from Drill's, this method is overridden here to avoid
   * Calcite early terminating the queries.
```

### JavadocReference
Cannot resolve symbol `isComparable(org.apache.calcite.rel.type.RelDataType,
* org.apache.calcite.rel.type.RelDataType)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlBetweenOperatorWrapper.java`
#### Snippet
```java
  /**
   * Since Calcite has its rule for type compatibility
   * (see {@link org.apache.calcite.sql.type.SqlTypeUtil#isComparable(org.apache.calcite.rel.type.RelDataType,
   * org.apache.calcite.rel.type.RelDataType)}), which is usually different from Drill's, this method is overridden here to avoid
   * Calcite early terminating the queries.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.type.RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlBetweenOperatorWrapper.java`
#### Snippet
```java
  /**
   * Since Calcite has its rule for type compatibility
   * (see {@link org.apache.calcite.sql.type.SqlTypeUtil#isComparable(org.apache.calcite.rel.type.RelDataType,
   * org.apache.calcite.rel.type.RelDataType)}), which is usually different from Drill's, this method is overridden here to avoid
   * Calcite early terminating the queries.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.type.RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlBetweenOperatorWrapper.java`
#### Snippet
```java
   * Since Calcite has its rule for type compatibility
   * (see {@link org.apache.calcite.sql.type.SqlTypeUtil#isComparable(org.apache.calcite.rel.type.RelDataType,
   * org.apache.calcite.rel.type.RelDataType)}), which is usually different from Drill's, this method is overridden here to avoid
   * Calcite early terminating the queries.
   */
```

### JavadocReference
Cannot resolve symbol `SqlSumEmptyIsZeroAggFunction`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlSumEmptyIsZeroAggFunctionWrapper.java`
#### Snippet
```java

/**
 * This class serves as a wrapper class for {@link SqlSumEmptyIsZeroAggFunction}
 * with the same goal as {@link DrillCalciteSqlAggFunctionWrapper}
 * but extends {@link SqlSumEmptyIsZeroAggFunction} to allow using
```

### JavadocReference
Cannot resolve symbol `SqlSumEmptyIsZeroAggFunction`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlSumEmptyIsZeroAggFunctionWrapper.java`
#### Snippet
```java
 * This class serves as a wrapper class for {@link SqlSumEmptyIsZeroAggFunction}
 * with the same goal as {@link DrillCalciteSqlAggFunctionWrapper}
 * but extends {@link SqlSumEmptyIsZeroAggFunction} to allow using
 * additional Calcite functionality designated for {@link SqlSumEmptyIsZeroAggFunction}.
 */
```

### JavadocReference
Cannot resolve symbol `SqlSumEmptyIsZeroAggFunction`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillCalciteSqlSumEmptyIsZeroAggFunctionWrapper.java`
#### Snippet
```java
 * with the same goal as {@link DrillCalciteSqlAggFunctionWrapper}
 * but extends {@link SqlSumEmptyIsZeroAggFunction} to allow using
 * additional Calcite functionality designated for {@link SqlSumEmptyIsZeroAggFunction}.
 */
public class DrillCalciteSqlSumEmptyIsZeroAggFunctionWrapper
```

### JavadocReference
Cannot resolve symbol `DrillCalciteSqlOperatorWrapper`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
/**
 *
 * This utility contains the static functions to manipulate {@link DrillCalciteSqlWrapper}, {@link DrillCalciteSqlOperatorWrapper}
 * {@link DrillCalciteSqlFunctionWrapper} and {@link DrillCalciteSqlAggFunctionWrapper}.
 */
```

### JavadocReference
Cannot resolve symbol `DrillCalciteSqlFunctionWrapper`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
 *
 * This utility contains the static functions to manipulate {@link DrillCalciteSqlWrapper}, {@link DrillCalciteSqlOperatorWrapper}
 * {@link DrillCalciteSqlFunctionWrapper} and {@link DrillCalciteSqlAggFunctionWrapper}.
 */
public class DrillCalciteWrapperUtility {
```

### JavadocReference
Cannot resolve symbol `DrillCalciteSqlAggFunctionWrapper`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
 *
 * This utility contains the static functions to manipulate {@link DrillCalciteSqlWrapper}, {@link DrillCalciteSqlOperatorWrapper}
 * {@link DrillCalciteSqlFunctionWrapper} and {@link DrillCalciteSqlAggFunctionWrapper}.
 */
public class DrillCalciteWrapperUtility {
```

### JavadocReference
Cannot resolve symbol `SchemaPlus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/SchemaUtilities.java`
#### Snippet
```java

  /**
   * Same utility as {@link #findSchema(SchemaPlus, List)} except the search schema path given here is complete path
   * instead of list. Use "." separator to divided the schema into nested schema names.
   * @param defaultSchema default schema
```

### JavadocReference
Cannot resolve symbol `SqlCall`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
   * <p/>
   * <ul>
   * <li>Key  : Each rule corresponds to a {@link SqlCall} class;
   * <li>Value: It is an array of {@link RewriteType}, each being associated with a data field
   * in that class.
```

### JavadocReference
Cannot resolve symbol `SqlOrderBy`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
   * <p/>
   * For example, there are four data fields (query, orderList, offset, fetch)
   * in {@link SqlOrderBy}. Since only orderList needs to be written,
   * {@link RewriteType[]} should be {@code arrayOf(D, E, D, D)}.
   */
```

### JavadocReference
Cannot resolve symbol `SqlCall`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
  /**
   * Argument handler which accepts {@link CompoundIdentifierConverter}
   * for every operand of {@link SqlCall} and constructs new {@link SqlCall}
   * if one or more operands changed.
   */
```

### JavadocReference
Cannot resolve symbol `SqlCall`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
  /**
   * Argument handler which accepts {@link CompoundIdentifierConverter}
   * for every operand of {@link SqlCall} and constructs new {@link SqlCall}
   * if one or more operands changed.
   */
```

### JavadocReference
Cannot resolve symbol `SqlVisitor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java

/**
 * Implementation of {@link SqlVisitor} that converts bracketed compound {@link SqlIdentifier}
 * to bracket-less compound {@link SqlIdentifier} (also known as {@link DrillCompoundIdentifier})
 * to provide ease of use while querying complex types.
```

### JavadocReference
Cannot resolve symbol `SqlIdentifier`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java

/**
 * Implementation of {@link SqlVisitor} that converts bracketed compound {@link SqlIdentifier}
 * to bracket-less compound {@link SqlIdentifier} (also known as {@link DrillCompoundIdentifier})
 * to provide ease of use while querying complex types.
```

### JavadocReference
Cannot resolve symbol `SqlIdentifier`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
/**
 * Implementation of {@link SqlVisitor} that converts bracketed compound {@link SqlIdentifier}
 * to bracket-less compound {@link SqlIdentifier} (also known as {@link DrillCompoundIdentifier})
 * to provide ease of use while querying complex types.
 * <p/>
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.parser.SqlParserImplFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillParserWithCompoundIdConverter.java`
#### Snippet
```java

  /**
   * {@link org.apache.calcite.sql.parser.SqlParserImplFactory} implementation for creating parser.
   */

```

### JavadocReference
Cannot resolve symbol `SqlParseException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java

/**
 * Customized {@link SqlParseException} class
 */
public class DrillSqlParseException extends SqlParseException {
```

### JavadocReference
Cannot resolve symbol `SqlParseException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java

  /**
   * Builds error message just like the original {@link SqlParseException}
   * with special handling for {@link ParseException} class.
   * <p>
```

### JavadocReference
Cannot resolve symbol `ParseException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java
  /**
   * Builds error message just like the original {@link SqlParseException}
   * with special handling for {@link ParseException} class.
   * <p>
   * This is customized implementation of the original {@link ParseException#getMessage()}.
```

### JavadocReference
Cannot resolve symbol `ParseException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java
   * with special handling for {@link ParseException} class.
   * <p>
   * This is customized implementation of the original {@link ParseException#getMessage()}.
   * Any other underlying {@link SqlParseException} exception messages goes through without changes
   * <p>
```

### JavadocReference
Cannot resolve symbol `getMessage()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java
   * with special handling for {@link ParseException} class.
   * <p>
   * This is customized implementation of the original {@link ParseException#getMessage()}.
   * Any other underlying {@link SqlParseException} exception messages goes through without changes
   * <p>
```

### JavadocReference
Cannot resolve symbol `SqlParseException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java
   * <p>
   * This is customized implementation of the original {@link ParseException#getMessage()}.
   * Any other underlying {@link SqlParseException} exception messages goes through without changes
   * <p>
   * <p>
```

### JavadocReference
Cannot resolve symbol `SqlTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/TypeInferenceUtils.java`
#### Snippet
```java

  /**
   * Given a {@link SqlTypeName} and nullability, create a RelDataType from the RelDataTypeFactory
   *
   * @param typeFactory RelDataTypeFactory used to create the RelDataType
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.rules.PushProjector.OperatorExprCondition`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
   * A function that replies true or false for a given expression.
   *
   * @see org.apache.calcite.rel.rules.PushProjector.OperatorExprCondition
   */
  private interface SqlNodeCondition {
```

### JavadocReference
Cannot resolve symbol `SqlSetOption`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/AbstractSqlSetHandler.java`
#### Snippet
```java

  /**
   * Extracts query {@link OptionValue.OptionScope} from the {@link SqlSetOption}.
   * @param statement Statement object
   * @param options Options object
```

### JavadocReference
Cannot resolve symbol `SqlNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ShowTablesHandler.java`
#### Snippet
```java
   * name of jdbc catalog which isn't present in table_schema column.
   *
   * @param sqlNode node produced by {@link #rewrite(SqlNode)}
   * @return converted rel node
   * @throws ForemanSetupException when fragment setup or ser/de failed
```

### JavadocReference
Cannot resolve symbol `RelConversionException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ShowTablesHandler.java`
#### Snippet
```java
   * @return converted rel node
   * @throws ForemanSetupException when fragment setup or ser/de failed
   * @throws RelConversionException when conversion failed
   * @throws ValidationException when sql node validation failed
   */
```

### JavadocReference
Cannot resolve symbol `ValidationException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ShowTablesHandler.java`
#### Snippet
```java
   * @throws ForemanSetupException when fragment setup or ser/de failed
   * @throws RelConversionException when conversion failed
   * @throws ValidationException when sql node validation failed
   */
  @Override
```

### JavadocReference
Cannot resolve symbol `SqlNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ResetOptionHandler.java`
#### Snippet
```java

/**
 * Converts a {@link SqlNode} representing: "ALTER .. RESET option | ALL" statement to a {@link PhysicalPlan}.
 * See {@link DrillSqlResetOption}.
 * <p>
```

### JavadocReference
Cannot resolve symbol `RexFieldAccess`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java

/**
 * Visitor that moves non-{@link RexFieldAccess} rex node from project below {@link Uncollect}
 * to the left side of the {@link Correlate}.
 */
```

### JavadocReference
Cannot resolve symbol `Uncollect`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java

/**
 * Visitor that moves non-{@link RexFieldAccess} rex node from project below {@link Uncollect}
 * to the left side of the {@link Correlate}.
 */
```

### JavadocReference
Cannot resolve symbol `Correlate`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java
/**
 * Visitor that moves non-{@link RexFieldAccess} rex node from project below {@link Uncollect}
 * to the left side of the {@link Correlate}.
 */
public class ComplexUnnestVisitor extends RelShuttleImpl {
```

### JavadocReference
Cannot resolve symbol `RexFieldAccess`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java

  /**
   * Rewrites rel node tree and moves non-{@link RexFieldAccess} rex node from the project
   * below {@link Uncollect} to the left side of the {@link Correlate}.
   *
```

### JavadocReference
Cannot resolve symbol `Uncollect`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java
  /**
   * Rewrites rel node tree and moves non-{@link RexFieldAccess} rex node from the project
   * below {@link Uncollect} to the left side of the {@link Correlate}.
   *
   * @param relNode tree to be rewritten
```

### JavadocReference
Cannot resolve symbol `Correlate`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java
  /**
   * Rewrites rel node tree and moves non-{@link RexFieldAccess} rex node from the project
   * below {@link Uncollect} to the left side of the {@link Correlate}.
   *
   * @param relNode tree to be rewritten
```

### JavadocReference
Cannot resolve symbol `RexFieldAccess`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java

  /**
   * Visitor for RexNode which replaces {@link RexFieldAccess}
   * with a reference to the field used in {@link RexFieldAccess}.
   */
```

### JavadocReference
Cannot resolve symbol `RexFieldAccess`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java
  /**
   * Visitor for RexNode which replaces {@link RexFieldAccess}
   * with a reference to the field used in {@link RexFieldAccess}.
   */
  private static class RexFieldAccessReplacer extends RexShuttle {
```

### JavadocReference
Cannot resolve symbol `SqlNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SetOptionHandler.java`
#### Snippet
```java

/**
 * Converts a {@link SqlNode} representing: "ALTER .. SET option = value" or "ALTER ... SET option"
 * statement to a {@link PhysicalPlan}. See {@link DrillSqlSetOption}
 * <p>
```

### JavadocReference
Cannot resolve symbol `SqlNodeList`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SqlHandlerUtil.java`
#### Snippet
```java

  /**
   * Unparses given {@link SqlNodeList} into key / values pairs: (k1 = v1, k2 = v2).
   *
   * @param writer sql writer
```

### JavadocReference
Cannot resolve symbol `ValidationException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SqlHandlerUtil.java`
#### Snippet
```java
   *                        Ex. CREATE TABLE newTblName(col1, medianOfCol2, avgOfCol3) AS
   *                        SELECT col1, median(col2), avg(col3) FROM sourcetbl GROUP BY col1;
   * @throws ValidationException If table's fields list and field list specified in table definition are not valid.
   * @throws RelConversionException If failed to convert the table definition into a RelNode.
   */
```

### JavadocReference
Cannot resolve symbol `RelConversionException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SqlHandlerUtil.java`
#### Snippet
```java
   *                        SELECT col1, median(col2), avg(col3) FROM sourcetbl GROUP BY col1;
   * @throws ValidationException If table's fields list and field list specified in table definition are not valid.
   * @throws RelConversionException If failed to convert the table definition into a RelNode.
   */
  public static RelNode resolveNewTableRel(boolean isNewTableView, List<String> tableFieldNames,
```

### JavadocReference
Cannot resolve symbol `LoadingCache`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillTableKey.java`
#### Snippet
```java

/**
 * Key for storing / obtaining {@link MetadataProviderManager} instance from {@link LoadingCache}.
 */
final class DrillTableKey {
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rex.RexCall`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillRexBuilder.java`
#### Snippet
```java
   *
   * <p>Tries to expand the cast, and therefore the result may be something
   * other than a {@link org.apache.calcite.rex.RexCall} to the CAST operator, such as a
   * {@link RexLiteral} if {@code matchNullability} is false.
   *
```

### JavadocReference
Cannot resolve symbol `RexLiteral`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillRexBuilder.java`
#### Snippet
```java
   * <p>Tries to expand the cast, and therefore the result may be something
   * other than a {@link org.apache.calcite.rex.RexCall} to the CAST operator, such as a
   * {@link RexLiteral} if {@code matchNullability} is false.
   *
   * @param type             Type to cast to
```

### JavadocReference
Cannot resolve symbol `CalciteCatalogReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillCalciteCatalogReader.java`
#### Snippet
```java

/**
 * Extension of {@link CalciteCatalogReader} to add ability to check for temporary tables first
 * if schema is not indicated near table name during query parsing
 * or indicated workspace is default temporary workspace.
```

### JavadocReference
Cannot resolve symbol `CalciteConnectionConfigImpl`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillCalciteCatalogReader.java`
#### Snippet
```java

  /**
   * Creates {@link CalciteConnectionConfigImpl} instance with specified caseSensitive property.
   *
   * @param caseSensitive is case sensitive.
```

### JavadocReference
Cannot resolve symbol `CalciteConnectionConfigImpl`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/DrillCalciteCatalogReader.java`
#### Snippet
```java
   *
   * @param caseSensitive is case sensitive.
   * @return {@link CalciteConnectionConfigImpl} instance
   */
  private static CalciteConnectionConfigImpl getConnectionConfig(boolean caseSensitive) {
```

### JavadocReference
Cannot resolve symbol `SqlNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/SqlConverter.java`
#### Snippet
```java
 * <ul>
 *   <li>parsing - {@link #parse(String)}<li/>
 *   <li>validation - {@link #validate(SqlNode)}<li/>
 *   <li>conversion to rel - {@link #toRel(SqlNode)} (String)}<li/>
 * <ul/>
```

### JavadocReference
Cannot resolve symbol `SqlNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/conversion/SqlConverter.java`
#### Snippet
```java
 *   <li>parsing - {@link #parse(String)}<li/>
 *   <li>validation - {@link #validate(SqlNode)}<li/>
 *   <li>conversion to rel - {@link #toRel(SqlNode)} (String)}<li/>
 * <ul/>
 */
```

### JavadocReference
Cannot resolve symbol `RelMdDistinctRowCount`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * at the first time was used Drill join row count but at the second time
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
```

### JavadocReference
Cannot resolve symbol `getDistinctRowCount(Join, RelMetadataQuery,
* ImmutableBitSet, RexNode)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * at the first time was used Drill join row count but at the second time
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
```

### JavadocReference
Cannot resolve symbol `Join`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * at the first time was used Drill join row count but at the second time
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
```

### JavadocReference
Cannot resolve symbol `RelMetadataQuery`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * at the first time was used Drill join row count but at the second time
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
```

### JavadocReference
Cannot resolve symbol `ImmutableBitSet`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
   * row count of input rel node (our join rel).
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * Calcite row count was used. It may happen when
   * {@link RelMdDistinctRowCount#getDistinctRowCount(Join, RelMetadataQuery,
   * ImmutableBitSet, RexNode)} method is used and after that used
   * another getDistinctRowCount method for parent rel, which just uses
   * row count of input rel node (our join rel).
```

### JavadocReference
Cannot resolve symbol `RelSubset`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * row count of input rel node (our join rel).
   * It causes cost increase of best rel node when
   * {@link RelSubset#propagateCostImprovements} is called.
   *
   * This is a part of the fix for CALCITE-2018.
```

### JavadocReference
Cannot resolve symbol `propagateCostImprovements`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
   * row count of input rel node (our join rel).
   * It causes cost increase of best rel node when
   * {@link RelSubset#propagateCostImprovements} is called.
   *
   * This is a part of the fix for CALCITE-2018.
```

### JavadocReference
Cannot resolve symbol `RelConversionException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
   * @param validatedRowType final output row type
   * @return physical relational node
   * @throws RelConversionException
   * @throws SqlUnsupportedException
   */
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/Statistics.java`
#### Snippet
```java

  /** Returns the rowcount for the specified filter condition
   *  @param condition - Filter specified as a {@link RexNode}
   *  @param tabIdxName - The index name generated using {@code buildUniqueIndexIdentifier}
   *  @param scanRel - The current scan rel
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/Statistics.java`
#### Snippet
```java
  /** Returns the leading rowcount for the specified filter condition
   *  Leading rowcount means rowcount for filter condition only on leading index columns.
   *  @param condition - Filter specified as a {@link RexNode}
   *  @param tabIdxName - The index name generated using {@code buildUniqueIndexIdentifier}
   *  @param scanRel - The current scan rel
```

### JavadocReference
Cannot resolve symbol `RelDataTypeField`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/types/AbstractRelDataTypeHolder.java`
#### Snippet
```java

/**
 * Base class-holder for the list of {@link RelDataTypeField}s.
 */
public abstract class AbstractRelDataTypeHolder {
```

### JavadocReference
Cannot resolve symbol `RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillLateralJoinRelBase.java`
#### Snippet
```java

  /**
   * Returns number of fields in {@link RelDataType} for
   * input rel node with specified ordinal considering value of
   * {@code excludeCorrelateColumn}.
```

### JavadocReference
Cannot resolve symbol `RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillLateralJoinRelBase.java`
#### Snippet
```java
   *
   * @param ordinal ordinal of input rel node
   * @return number of fields in input's {@link RelDataType}
   */
  public int getInputSize(int ordinal) {
```

### JavadocReference
Cannot resolve symbol `TableScan`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/SelectionBasedTableScan.java`
#### Snippet
```java

/**
 * This class extends from {@link TableScan}. It puts the file selection string into its digest.
 * When directory-based partition pruning applied, file selection could be different for the same
 * table.
```

### JavadocReference
Cannot resolve symbol `RexInputRef`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
  /**
   * Returns whether the leading edge of a given array of expressions is
   * wholly {@link RexInputRef} objects with types and names corresponding
   * to the underlying row type. */
  private static boolean containIdentity(List<? extends RexNode> exps,
```

### JavadocReference
Cannot resolve symbol `RelNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
  /**
   * Returns whether statistics-based estimates or guesses are used by the optimizer
   * for the {@link RelNode} rel.
   * @param rel input
   * @return TRUE if the estimate is a guess, FALSE otherwise
```

### JavadocReference
Cannot resolve symbol `LogicalIntersect`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillSetOpRule.java`
#### Snippet
```java

/**
 * Rule that converts {@link LogicalIntersect} or {@link LogicalMinus} to
 * {@link DrillIntersectRel} or {@link DrillExceptRel}.
 */
```

### JavadocReference
Cannot resolve symbol `LogicalMinus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillSetOpRule.java`
#### Snippet
```java

/**
 * Rule that converts {@link LogicalIntersect} or {@link LogicalMinus} to
 * {@link DrillIntersectRel} or {@link DrillExceptRel}.
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.logical.LogicalFilter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillFilterRule.java`
#### Snippet
```java

/**
 * Rule that converts a {@link org.apache.calcite.rel.logical.LogicalFilter} to a Drill "filter" operation.
 */
public class DrillFilterRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `RelFactories.JoinFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.JoinFactory} that returns a vanilla
   * {@link DrillJoinRel}.
   */
```

### JavadocReference
Cannot resolve symbol `RelFactories.FilterFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.FilterFactory} that
   * returns a vanilla {@link DrillFilterRel}.
   */
```

### JavadocReference
Cannot resolve symbol `RelFactories.AggregateFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.AggregateFactory} that returns a vanilla
   * {@link DrillAggregateRel}.
   */
```

### JavadocReference
Cannot resolve symbol `RelFactories.ProjectFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.ProjectFactory} that returns a vanilla
   * {@link DrillProjectRel}.
   */
```

### JavadocReference
Cannot resolve symbol `RelFactories.SetOpFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.SetOpFactory} that returns
   * a vanilla {@link DrillExceptRel} or {@link DrillIntersectRel}
   * dependent on the particular kind of set operation (EXCEPT, INTERSECT)
```

### JavadocReference
Cannot resolve symbol `RelBuilderFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java
  }
  /**
   * A {@link RelBuilderFactory} that creates a {@link DrillRelBuilder} that will
   * create logical relational expressions for everything.
   */
```

### JavadocReference
Cannot resolve symbol `LogicalUnion`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillUnionAllRule.java`
#### Snippet
```java

/**
 * Rule that converts a {@link LogicalUnion} to a {@link DrillUnionRel}, implemented by a "union" operation.
 */
public class DrillUnionAllRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.logical.LogicalProject`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRule.java`
#### Snippet
```java

/**
 * Rule that converts a {@link org.apache.calcite.rel.logical.LogicalProject} to a Drill "project" operation.
 */
public class DrillProjectRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `LogicalValues`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillValuesRule.java`
#### Snippet
```java

/**
 * Rule that converts a {@link LogicalValues} to a Drill "values" operation.
 */
public class DrillValuesRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `Sort`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillSortRule.java`
#### Snippet
```java

/**
 * Rule that converts an {@link Sort} to a {@link DrillSortRel}, implemented by a Drill "order" operation.
 */
public class DrillSortRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `LogicalAggregate`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillAggregateRule.java`
#### Snippet
```java

/**
 * Rule that converts an {@link LogicalAggregate} to a {@link DrillAggregateRel}, implemented by a Drill "segment" operation
 * followed by a "collapseaggregate" operation.
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.core.SemiJoin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinRel.java`
#### Snippet
```java

  /**
   * Returns whether this RowKeyJoin represents a {@link org.apache.calcite.rel.core.SemiJoin}
   * @return true if join represents a {@link org.apache.calcite.rel.core.SemiJoin}, false otherwise.
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.core.SemiJoin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinRel.java`
#### Snippet
```java
  /**
   * Returns whether this RowKeyJoin represents a {@link org.apache.calcite.rel.core.SemiJoin}
   * @return true if join represents a {@link org.apache.calcite.rel.core.SemiJoin}, false otherwise.
   */
  public boolean isSemiJoin() {
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.core.SemiJoin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinRel.java`
#### Snippet
```java
  }

  /** The parent method relies the class being an instance of {@link org.apache.calcite.rel.core.SemiJoin}
   * in deciding row-type validity. We override this method to account for the RowKeyJoinRel logical rel
   * representing both regular and semi-joins */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.logical.LogicalJoin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRule.java`
#### Snippet
```java

/**
 * Rule that converts a {@link org.apache.calcite.rel.logical.LogicalJoin} to a {@link DrillJoinRel}, which is implemented by Drill "join" operation.
 */
public class DrillJoinRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `Filter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillMergeFilterRule.java`
#### Snippet
```java

/**
 * MergeFilterRule implements the rule for combining two {@link Filter}s
 */
public class DrillMergeFilterRule extends RelOptRule {
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillOptiq.java`
#### Snippet
```java

  /**
   * Converts a tree of {@link RexNode} operators into a scalar expression in Drill syntax using one input.
   *
   * @param context parse context which contains planner settings
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillOptiq.java`
#### Snippet
```java

  /**
   * Converts a tree of {@link RexNode} operators into a scalar expression in Drill syntax using multiple inputs.
   *
   * @param context parse context which contains planner settings
```

### JavadocReference
Cannot resolve symbol `Aggregate`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java

  /**
   * A rule which converts {@link Aggregate} on {@link TableScan} with directory columns
   * (see {@link org.apache.drill.exec.ExecConstants#FILESYSTEM_PARTITION_COLUMN_LABEL}) only
   * into {@link DrillValuesRel} to avoid scanning at all.
```

### JavadocReference
Cannot resolve symbol `TableScan`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java

  /**
   * A rule which converts {@link Aggregate} on {@link TableScan} with directory columns
   * (see {@link org.apache.drill.exec.ExecConstants#FILESYSTEM_PARTITION_COLUMN_LABEL}) only
   * into {@link DrillValuesRel} to avoid scanning at all.
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.rules.AggregateRemoveRule`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPrel.java`
#### Snippet
```java
   * null == null is FALSE whereas null IS NOT DISTINCT FROM null is TRUE
   * For a use case of the IS NOT DISTINCT FROM comparison, see
   * {@link org.apache.calcite.rel.rules.AggregateRemoveRule}
   * @param conditions populated list of join conditions
   * @param leftFields join fields from the left input
```

### JavadocReference
Cannot resolve symbol `ImmutableBitSet`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/AggPruleBase.java`
#### Snippet
```java
   *
   * @param groupSet ImmutableBitSet of aggregate rel node, whose group-by keys should be remapped.
   * @return {@link ImmutableBitSet} instance with remapped keys.
   */
  public static ImmutableBitSet remapGroupSet(ImmutableBitSet groupSet) {
```

### JavadocReference
Cannot resolve symbol `RelFactories.ProjectFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PrelFactories.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelFactories.ProjectFactory} that returns a vanilla
   * {@link org.apache.calcite.rel.logical.LogicalProject}.
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.rel.logical.LogicalProject`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PrelFactories.java`
#### Snippet
```java
  /**
   * Implementation of {@link RelFactories.ProjectFactory} that returns a vanilla
   * {@link org.apache.calcite.rel.logical.LogicalProject}.
   */
  private static class DrillProjectPrelFactory implements RelFactories.ProjectFactory {
```

### JavadocReference
Cannot resolve symbol `RelCollationImpl`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/MetadataAggPrule.java`
#### Snippet
```java

  /**
   * Implementation of {@link RelCollationImpl} with field name.
   * Stores {@link RelFieldCollation} list and corresponding field names to be used in sort operators.
   * Field name is required for the case of dynamic schema discovering
```

### JavadocReference
Cannot resolve symbol `RelFieldCollation`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/MetadataAggPrule.java`
#### Snippet
```java
  /**
   * Implementation of {@link RelCollationImpl} with field name.
   * Stores {@link RelFieldCollation} list and corresponding field names to be used in sort operators.
   * Field name is required for the case of dynamic schema discovering
   * when field is not present in rel data type at planning time.
```

### JavadocReference
Cannot resolve symbol `Sort`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SortConvertPrule.java`
#### Snippet
```java

/**
 * Rule that converts an {@link Sort} to a physical {@link SortPrel}, implemented by a Drill "order" operation.
 *
 * The {@link Sort} is added in optiq's AbstractConvert call, when it enforces certain "order" to the input stream.
```

### JavadocReference
Cannot resolve symbol `Sort`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SortConvertPrule.java`
#### Snippet
```java
 * Rule that converts an {@link Sort} to a physical {@link SortPrel}, implemented by a Drill "order" operation.
 *
 * The {@link Sort} is added in optiq's AbstractConvert call, when it enforces certain "order" to the input stream.
 * Drill uses this rule to convert such sort enforcer into physical {@link SortPrel}.
 */
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   * @param hashAsDouble Whether to use the hash as double function or regular hash64 function.
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   *               which is used to create function expressions.
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   * @param hashAsDouble Whether to use the hash as double function or regular hash64 function.
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
```

### JavadocReference
Cannot resolve symbol `DefaultSqlHandler`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/PrelVisualizerVisitor.java`
#### Snippet
```java
 * or change them. For example:
 * <p>
 * In file: {@link DefaultSqlHandler#convertToPrel()}:
 * <pre><code>
 * PrelVisualizerVisitor.print("Before EER", phyRelNode); // Debug only
```

### JavadocReference
Cannot resolve symbol `convertToPrel()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/PrelVisualizerVisitor.java`
#### Snippet
```java
 * or change them. For example:
 * <p>
 * In file: {@link DefaultSqlHandler#convertToPrel()}:
 * <pre><code>
 * PrelVisualizerVisitor.print("Before EER", phyRelNode); // Debug only
```

### JavadocReference
Cannot resolve symbol `RexShuttle`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/LateralUnnestRowIDVisitor.java`
#### Snippet
```java

  /**
   * Visitor for RelNodes which applies specified {@link RexShuttle} visitor
   * for every node in the tree.
   */
```

### JavadocReference
Cannot resolve symbol `FragmentContextImpl`
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExecutionControlsInjector.java`
#### Snippet
```java
 * Injects exceptions and pauses at execution time for testing. Any class that wants to simulate exceptions
 * or inject pauses for testing should have it's own private static instance of an injector (similar to the use
 * of loggers). Injection site either use {@link FragmentContextImpl} or
 * {@link org.apache.drill.exec.ops.QueryContext}.
 * See {@link ControlsInjector} for documentation.
```

### JavadocReference
Symbol `org.apache.drill.exec.testing.TestCountDownLatchInjection` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ControlsInjector.java`
#### Snippet
```java
  /**
   * Used to insert a latch in site class. See {@link CountDownLatchInjection} and
   * {@link org.apache.drill.exec.testing.TestCountDownLatchInjection} for usage.
   * @param executionControls the controls in the current context
   * @param desc              the site descriptor
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/DbGroupScan.java`
#### Snippet
```java

  /**
   * Get the row count after applying the {@link RexNode} condition
   *
   * @param condition filter to apply
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/DbGroupScan.java`
#### Snippet
```java

  /**
   * Set the artificial row count after applying the {@link RexNode} condition
   *
   * @param condition row expression
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.type.SqlTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    /**
     * Gets value type for fields whose type is
     * {@link org.apache.calcite.sql.type.SqlTypeName#MAP}
     *
     * @return value type of map
```

### JavadocReference
Cannot resolve symbol `MAP`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    /**
     * Gets value type for fields whose type is
     * {@link org.apache.calcite.sql.type.SqlTypeName#MAP}
     *
     * @return value type of map
```

### JavadocReference
Cannot resolve symbol `DynamicRecordType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
  /**
   * If view fields are present then attempts to gather them
   * into struct type, otherwise returns extension of  {@link DynamicRecordType}.
   *
   * @param factory factory for rel data types creation
```

### JavadocReference
Cannot resolve symbol `DynamicRecordType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
   * @param factory factory for rel data types creation
   * @return struct type that describes names and types of all
   *         view fields or extension of {@link DynamicRecordType}
   *         when view fields are empty
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.calcite.sql.type.SqlTypeName`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    /**
     * Gets key type for fields whose type is
     * {@link org.apache.calcite.sql.type.SqlTypeName#MAP}
     *
     * @return key type of map
```

### JavadocReference
Cannot resolve symbol `MAP`
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    /**
     * Gets key type for fields whose type is
     * {@link org.apache.calcite.sql.type.SqlTypeName#MAP}
     *
     * @return key type of map
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/IndexGroupScan.java`
#### Snippet
```java

  /**
   * Get the row count after applying the {@link RexNode} condition
   * @param condition filter to apply
   * @param scanRel the current scan rel
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/IndexGroupScan.java`
#### Snippet
```java

  /**
   * Set the artificial row count after applying the {@link RexNode} condition
   * Mainly used for debugging
   * @param condition filter to apply
```

### JavadocReference
Cannot resolve symbol `RelNode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinUtils.java`
#### Snippet
```java
   * Return true if find one. Otherwise, return false.
   *
   * @param relNode     {@link RelNode} instance to be inspected
   * @return            Return true if the given relNode contains Cartesian join.
   *                    Otherwise, return false
```

### JavadocReference
Cannot resolve symbol `ScanBatch`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/package-info.java`
#### Snippet
```java
 * <p>
 * Two versions of the scan operator exist:<ul>
 * <li>{@link ScanBatch}: the original version that uses readers based on the
 * {@link RecordReader} interface. <tt>ScanBatch</tt> cannot, however, handle
 * limited-length vectors.</li>
```

### JavadocReference
Cannot resolve symbol `RecordReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/package-info.java`
#### Snippet
```java
 * Two versions of the scan operator exist:<ul>
 * <li>{@link ScanBatch}: the original version that uses readers based on the
 * {@link RecordReader} interface. <tt>ScanBatch</tt> cannot, however, handle
 * limited-length vectors.</li>
 * <li>{@link ScanOperatorExec}: the revised version that uses a more modular
```

### JavadocReference
Cannot resolve symbol `ScanOperatorExec`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/package-info.java`
#### Snippet
```java
 * {@link RecordReader} interface. <tt>ScanBatch</tt> cannot, however, handle
 * limited-length vectors.</li>
 * <li>{@link ScanOperatorExec}: the revised version that uses a more modular
 * design and that offers a mutator that is a bit easier to use, and can limit
 * vector sizes.</li></ul>
```

### JavadocReference
Cannot resolve symbol `ScanOperatorExec`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/package-info.java`
#### Snippet
```java
 * include intensive tests to keep Drill quality high.
 * <p>
 * See {@link ScanOperatorExec} for details of the scan operator protocol
 * and components.
 *
```

### JavadocReference
Cannot resolve symbol `ScanSchemaOrchestrator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/ScanOperatorEvents.java`
#### Snippet
```java
 * that most actual scanners will use the "managed" framework that handles the
 * routine projection, vector management and other tasks that tend to be common
 * across scanners. See {@link ScanSchemaOrchestrator} for the managed
 * framework.
 */
```

### JavadocReference
Cannot resolve symbol `ScanLifecycleBuilder`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/package-info.java`
#### Snippet
```java
 * vectors.
 * <p>
 * Build a scan using the {@link ScanLifecycleBuilder}. Provide a subclass
 * of {@link ReaderFactory} that will create each of your
 * {@link ManagedReader} instances as needed. The framework handles the
```

### JavadocReference
Cannot resolve symbol `ReaderFactory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/package-info.java`
#### Snippet
```java
 * <p>
 * Build a scan using the {@link ScanLifecycleBuilder}. Provide a subclass
 * of {@link ReaderFactory} that will create each of your
 * {@link ManagedReader} instances as needed. The framework handles the
 * details of schemas, missing columns, implicit columns and the rest.
```

### JavadocReference
Cannot resolve symbol `ManagedReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/package-info.java`
#### Snippet
```java
 * Build a scan using the {@link ScanLifecycleBuilder}. Provide a subclass
 * of {@link ReaderFactory} that will create each of your
 * {@link ManagedReader} instances as needed. The framework handles the
 * details of schemas, missing columns, implicit columns and the rest.
 */
```

### JavadocReference
Cannot resolve symbol `next`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/RowBatchReader.java`
#### Snippet
```java
   * empty batch with the schema set. The scanner will return this schema
   * downstream to inform other operators of the schema.</li>
   * <li>Directly after a successful call to {@link next()} to retrieve
   * the batch produced by that call. (No call is made if {@code next()}
   * returns false.</li>
```

### JavadocReference
Cannot resolve symbol `output`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/RowBatchReader.java`
#### Snippet
```java

  /**
   * Return the version of the schema returned by {@link output()}. The schema
   * is assumed to start at -1 (no schema). The reader is free to use any
   * numbering system it likes as long as:
```

### JavadocReference
Cannot resolve symbol `RutimeException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/RowBatchReader.java`
#### Snippet
```java
   * throws an exception.
   *
   * @throws RutimeException ({@code UserException} preferred) if an
   * error occurs that should fail the query.
   */
```

### JavadocReference
Cannot resolve symbol `hasOutputSchema`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/SchemaNegotiator.java`
#### Snippet
```java
 * the proscribed rows, performing column type conversions as needed.
 * The reader can determine if the schema is defined by calling
 * {@link hasOutputSchema()}.
 * <p>
 * At present, the scan framework filters the "provided schema" against
```

### JavadocReference
Cannot resolve symbol `addColumn()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/SchemaNegotiator.java`
#### Snippet
```java
 * have sufficient structure (name/value pairs) to infer one.
 * <p>
 * The late schema reader calls {@link RowSetLoader#addColumn()} to
 * add each column as it is discovered during the scan.
 * <p>
```

### JavadocReference
Cannot resolve symbol `FileSystemPlugin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/FileScanLifecycle.java`
#### Snippet
```java
 * <p>
 * Only a single storage plugin uses the file scan framework:
 * the {@link FileSystemPlugin} via the {@link EasyFormatPlugin}. To
 * make client code as simple as possible, the Drill file system and list
 * of files is passed though this framework to the
```

### JavadocReference
Cannot resolve symbol `EasyFormatPlugin`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/FileScanLifecycle.java`
#### Snippet
```java
 * <p>
 * Only a single storage plugin uses the file scan framework:
 * the {@link FileSystemPlugin} via the {@link EasyFormatPlugin}. To
 * make client code as simple as possible, the Drill file system and list
 * of files is passed though this framework to the
```

### JavadocReference
Cannot resolve symbol `FileScanOptions`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/FileScanLifecycle.java`
#### Snippet
```java
 * for each format handle this common boilerplate code.
 * <p>
 * The {@link FileScanOptions} holds the list of splits to scan. The
 * {@link FileReaderFactory} iterates over those splits, and
 * creates each reader just-in-time to process that split.
```

### JavadocReference
Cannot resolve symbol `isProjectAll()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaTracker.java`
#### Snippet
```java
   * Drill defines a wildcard to expand not just reader columns, but also
   * partition columns. When the implicit column handlers sees that the
   * query has a wildcard (by calling {@link #isProjectAll()}), the handler
   * then determines which partition columns are needed and calls this
   * method to add each one.
```

### JavadocReference
Cannot resolve symbol `ImplicitColumnExplorer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaTracker.java`
#### Snippet
```java
 * column, and finds that the default type conflicts with the real type.
 * <p>
 * @see {@link ImplicitColumnExplorer}, the class from which this class
 * evolved
 */
```

### JavadocReference
Cannot resolve symbol `missingColumns()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaTracker.java`
#### Snippet
```java
  /**
   * The missing column handler obtains the list of missing columns from
   * {@link #missingColumns()}. Depending on the scan lifecycle, some of the
   * columns may have a type, others may be dynamic. The missing column handler
   * chooses a type for any dynamic columns, then calls this method to tell
```

### JavadocReference
Cannot resolve symbol `isProjectAll()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaTracker.java`
#### Snippet
```java
  /**
   * The schema which the reader should produce. Depending on the type of
   * the scan (specifically, if {@link #isProjectAll()} is {@code true}),
   * the reader may produce additional columns beyond those in the the
   * reader input schema. However, for any batch, the reader, plus the
```

### JavadocReference
Cannot resolve symbol `RelDataType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/SchemaUtils.java`
#### Snippet
```java
   *
   * @param name        name of the filed
   * @param relDataType {@link RelDataType} the source of the children for resulting schema
   * @return {@link MapColumnMetadata} column metadata
   */
```

### JavadocReference
Cannot resolve symbol `ScanSchemaTracker`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/lifecycle/package-info.java`
#### Snippet
```java
 * arises when the information available underdetermines the output schema:
 * the mechanism here attempts to fill in gaps, and flags conflicts. Only a
 * provided or defined schema (see {@link ScanSchemaTracker} resolves all
 * ambiguities.)
 */
```

### JavadocReference
Cannot resolve symbol `TestImplicitFileColumns`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitColumnResolver.java`
#### Snippet
```java
     * has one level of nesting.
     *
     * See {@link TestImplicitFileColumns#testImplicitColumns}
     */
    public ImplicitColumnOptions useLegacyWildcardExpansion(boolean flag) {
```

### JavadocReference
Cannot resolve symbol `testImplicitColumns`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitColumnResolver.java`
#### Snippet
```java
     * has one level of nesting.
     *
     * See {@link TestImplicitFileColumns#testImplicitColumns}
     */
    public ImplicitColumnOptions useLegacyWildcardExpansion(boolean flag) {
```

### JavadocReference
Cannot resolve symbol `ValuesWriter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/package-info.java`
#### Snippet
```java
 * Standard type conversion tools for the case in which the input
 * types are the standard Java types already supported by the
 * {@link ValuesWriter} interface. The classes here wrap a
 * {@link ScalarWriter}: either a simple column, or the scalar portion of
 * an array column.
```

### JavadocReference
Cannot resolve symbol `ScalarWriter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/package-info.java`
#### Snippet
```java
 * types are the standard Java types already supported by the
 * {@link ValuesWriter} interface. The classes here wrap a
 * {@link ScalarWriter}: either a simple column, or the scalar portion of
 * an array column.
 * <p>
```

### JavadocReference
Cannot resolve symbol `StandardConversions`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/package-info.java`
#### Snippet
```java
 * rules.
 * <p>
 * The {@link StandardConversions} class defines the types of conversions
 * needed:
 * <ul>
```

### JavadocReference
Cannot resolve symbol `BLANK_AS_DEFAULT`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/AbstractConvertFromString.java`
#### Snippet
```java
   * <ul>
   * <li>Leave nulls unchanged (default if no property is set.)</li>
   * <li>{@link #BLANK_AS_DEFAULT}: skip the value for the row, letting the
   * fill-empties logic fill in the default value.</li>
   * <li>{@link #BLANK_AS_ZERO}: for numeric types, replace the null value with
```

### JavadocReference
Cannot resolve symbol `BLANK_AS_ZERO`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/AbstractConvertFromString.java`
#### Snippet
```java
   * <li>{@link #BLANK_AS_DEFAULT}: skip the value for the row, letting the
   * fill-empties logic fill in the default value.</li>
   * <li>{@link #BLANK_AS_ZERO}: for numeric types, replace the null value with
   * "0", which will then be parsed to a numeric zero.</li>
   * <li>{@link #BLANK_AS_NULL}: for nullable modes, set the column to null
```

### JavadocReference
Cannot resolve symbol `BLANK_AS_NULL`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/AbstractConvertFromString.java`
#### Snippet
```java
   * <li>{@link #BLANK_AS_ZERO}: for numeric types, replace the null value with
   * "0", which will then be parsed to a numeric zero.</li>
   * <li>{@link #BLANK_AS_NULL}: for nullable modes, set the column to null
   * and ignore the value.</li>
   * </ul>
```

### JavadocReference
Cannot resolve symbol `SchemaPath`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/package-info.java`
#### Snippet
```java
 * <p>
 * <ul>
 * <li>Scan-level rewrite: convert {@link SchemaPath} entries into
 * internal column nodes, tagging the nodes with the column type:
 * wildcard, unresolved table column, or special columns (such as
```

### JavadocReference
Cannot resolve symbol `MajorType`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ResolvedColumn.java`
#### Snippet
```java
 * Although the table schema mechanism uses the newer "metadata"
 * mechanism, resolved columns revert back to the original
 * {@link MajorType} and {@link MaterializedField} mechanism used
 * by the rest of Drill. Doing so loses a bit of additional
 * information, but at present there is no way to export that information
```

### JavadocReference
Cannot resolve symbol `ImplicitColumnExplorer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ReaderLevelProjection.java`
#### Snippet
```java
 * vector container so that code can access columns by index as well as name.
 *
 * @see {@link ImplicitColumnExplorer}, the class from which this class
 * evolved
 */
```

### JavadocReference
Cannot resolve symbol `LogicalTupleSet`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * levels to classes:
 * <ul>
 * <li>Level 1: {@link LogicalTupleSet} in the {@link ResultSetLoader}
 * class.</li>
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
```

### JavadocReference
Cannot resolve symbol `ResultSetLoader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * levels to classes:
 * <ul>
 * <li>Level 1: {@link LogicalTupleSet} in the {@link ResultSetLoader}
 * class.</li>
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
```

### JavadocReference
Cannot resolve symbol `ProjectionPlanner`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * <li>Level 1: {@link LogicalTupleSet} in the {@link ResultSetLoader}
 * class.</li>
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
 * {@link ScanProjector}.</li>
 * <li>Level 3: {@link ScanProjector}.</li>
```

### JavadocReference
Cannot resolve symbol `ScanProjection`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * <li>Level 1: {@link LogicalTupleSet} in the {@link ResultSetLoader}
 * class.</li>
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
 * {@link ScanProjector}.</li>
 * <li>Level 3: {@link ScanProjector}.</li>
```

### JavadocReference
Cannot resolve symbol `ScanProjector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * class.</li>
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
 * {@link ScanProjector}.</li>
 * <li>Level 3: {@link ScanProjector}.</li>
 * <li>Level 4: {@link OperatorRecordBatch.SchemaTracker}.</li>
```

### JavadocReference
Cannot resolve symbol `ScanProjector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * <li>Level 2: {@link ProjectionPlanner}, {@link ScanProjection} and
 * {@link ScanProjector}.</li>
 * <li>Level 3: {@link ScanProjector}.</li>
 * <li>Level 4: {@link OperatorRecordBatch.SchemaTracker}.</li>
 * </ul>
```

### JavadocReference
Cannot resolve symbol `OperatorRecordBatch.SchemaTracker`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * {@link ScanProjector}.</li>
 * <li>Level 3: {@link ScanProjector}.</li>
 * <li>Level 4: {@link OperatorRecordBatch.SchemaTracker}.</li>
 * </ul>
 * <h4>Selection List Processing</h4>
```

### JavadocReference
Cannot resolve symbol `RowBatchReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * Some of the key classes here include:
 * <ul>
 * <li>{@link RowBatchReader} an extremely simple interface for reading data.
 * We would like many developers to create new plugins and readers. The simplified
 * interface pushes all complexity into the scan framework, leaving the reader to
```

### JavadocReference
Cannot resolve symbol `ShimBatchReader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * interface pushes all complexity into the scan framework, leaving the reader to
 * just read.</li>
 * <li>{@link ShimBatchReader} an implementation of the above that converts from
 * the simplified API to add additional structure to work with the result set loader.
 * (The base interface is agnostic about how rows are read.)</li>
```

### JavadocReference
Cannot resolve symbol `ScheamNegotiator`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/package-info.java`
#### Snippet
```java
 * the simplified API to add additional structure to work with the result set loader.
 * (The base interface is agnostic about how rows are read.)</li>
 * <li>{@link ScheamNegotiator} and interface that allows a batch reader to
 * "negotiate" a schema with the scan framework. The scan framework knows the
 * columns that are to be projected. The reader knows what columns it can offer.
```

### JavadocReference
Cannot resolve symbol `hasOutputSchema`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/SchemaNegotiator.java`
#### Snippet
```java
 * the proscribed rows, performing column type conversions as needed.
 * The reader can determine if the schema is defined by calling
 * {@link hasOutputSchema()}.
 * <p>
 * At present, the scan framework filters the "provided schema" against
```

### JavadocReference
Cannot resolve symbol `tableSchema(TupleMetadata)`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/SchemaNegotiator.java`
#### Snippet
```java
 * of which define a schema. This case is called "early schema." The
 * reader fefines the schema by calling
 * {@link #tableSchema(TupleMetadata)} to provide the known schema.
 *
 * <h4>Late Dynamic Schema</h4>
```

### JavadocReference
Cannot resolve symbol `addColumn()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/SchemaNegotiator.java`
#### Snippet
```java
 * have sufficient structure (name/value pairs) to infer one.
 * <p>
 * The late schema reader calls {@link RowSetLoader#addColumn()} to
 * add each column as it is discovered during the scan.
 * <p>
```

### JavadocReference
Cannot resolve symbol `ExternalSort`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SorterWrapper.java`
#### Snippet
```java
  /**
   * Generated sort operation used to sort each incoming batch according to
   * the sort criteria specified in the {@link ExternalSort} definition of
   * this operator.
   */
```

### JavadocReference
Cannot resolve symbol `BatchAccesor`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/protocol/OperatorExec.java`
#### Snippet
```java
 * The operator "publishes" a result set in response to returning
 * <tt>true</tt> from <tt>next()</tt> by populating a
 * {@link BatchAccesor} provided via {@link #batchAccessor()}. For
 * compatibility with other Drill operators, the set of vectors within
 * the batch must be the same from one batch to the next.
```

### JavadocReference
Cannot resolve symbol `VectorContainer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/protocol/OperatorExec.java`
#### Snippet
```java
   * This method is called after a successful return from
   * {@link #buildSchema()} and {@link #next()}. The batch itself
   * can be held in a standard {@link VectorContainer}, or in some
   * other structure more convenient for this operator.
   *
```

### JavadocReference
Cannot resolve symbol `detach()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/PullResultSetReader.java`
#### Snippet
```java
  /**
   * Close this reader. Releases any memory still assigned
   * to any attached batch. Call {@link #detach()} first if
   * you want to preserve the batch memory.
   */
```

### JavadocReference
Cannot resolve symbol `start()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/PullResultSetReader.java`
#### Snippet
```java
 * <li>For each incoming batch:
 *   <ol>
 *   <li>Call {@link #start()} to attach the batch. The associated
 *       {@link BatchAccessor} reports if the schema has changed.</li>
 *   <li>Call {@link #reader()} to obtain a reader.</li>
```

### JavadocReference
Cannot resolve symbol `release()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/PullResultSetReader.java`
#### Snippet
```java
 *   <li>Call {@link #reader()} to obtain a reader.</li>
 *   <li>Iterate over the batch using the reader.</li>
 *   <li>Call {@link #release()} to free the memory for the
 *       incoming batch. Or, to call {@link #detach()} to keep
 *       the batch memory.</li>
```

### JavadocReference
Cannot resolve symbol `detach()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/PullResultSetReader.java`
#### Snippet
```java
 *   <li>Iterate over the batch using the reader.</li>
 *   <li>Call {@link #release()} to free the memory for the
 *       incoming batch. Or, to call {@link #detach()} to keep
 *       the batch memory.</li>
 *   </ol>
```

### JavadocReference
Cannot resolve symbol `ColumnWriterIndex`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * array of ints.)
 * <p>
 * Writers handle this index tree via a tree of {@link ColumnWriterIndex}
 * objects, often specialized for various tasks.
 * <p>
```

### JavadocReference
Cannot resolve symbol `VectorContainer`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * Drill has vastly different implementations and interfaces for:
 * <ul>
 * <li>Result sets (as a {@link VectorContainer}),</li>
 * <li>Arrays (as a generated repeated vector),</li>
 * <li>Lists (as a {@link ListVector}),</li>
```

### JavadocReference
Cannot resolve symbol `ListVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * <li>Result sets (as a {@link VectorContainer}),</li>
 * <li>Arrays (as a generated repeated vector),</li>
 * <li>Lists (as a {@link ListVector}),</li>
 * <li>Repeated lists (as a {@link RepeatedList vector}, and</li>
 * <li>Repeated maps ({@link RepeatedMapVector}.</li>
```

### JavadocReference
Cannot resolve symbol `RepeatedList`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * <li>Arrays (as a generated repeated vector),</li>
 * <li>Lists (as a {@link ListVector}),</li>
 * <li>Repeated lists (as a {@link RepeatedList vector}, and</li>
 * <li>Repeated maps ({@link RepeatedMapVector}.</li>
 * </ul>
```

### JavadocReference
Cannot resolve symbol `RepeatedMapVector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * <li>Lists (as a {@link ListVector}),</li>
 * <li>Repeated lists (as a {@link RepeatedList vector}, and</li>
 * <li>Repeated maps ({@link RepeatedMapVector}.</li>
 * </ul>
 * If we were to work directly with the above abstractions the code would be
```

### JavadocReference
Cannot resolve symbol `TupleMode`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/package-info.java`
#### Snippet
```java
 * If we were to work directly with the above abstractions the code would be
 * vastly complex. Instead, we abstract out the common structure into the
 * {@link TupleMode} abstraction. In particular, we use the
 * single tuple model which works with a single batch. This model provides a
 * simple, uniform interface to work with columns and tuples (rows, maps),
```

### JavadocReference
Cannot resolve symbol `ResultSetLoader`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ResultVectorCacheImpl.java`
#### Snippet
```java
 * These rules create interesting constraints for the scan operator.
 * Conceptually, each batch is distinct. But, it must share vectors. The
 * {@link ResultSetLoader} class handles this by managing the set of vectors
 * used by a single reader.
 * <p>
```

### JavadocReference
Cannot resolve symbol `start()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/PullResultSetReaderImpl.java`
#### Snippet
```java
 * <li>For each incoming batch:
 *   <ol>
 *   <li>Call {@link #start()} to attach the batch. The associated
 *       {@link BatchAccessor} reports if the schema has changed.</li>
 *   <li>Call {@link #reader()} to obtain a reader.</li>
```

### JavadocReference
Cannot resolve symbol `release()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/PullResultSetReaderImpl.java`
#### Snippet
```java
 *   <li>Call {@link #reader()} to obtain a reader.</li>
 *   <li>Iterate over the batch using the reader.</li>
 *   <li>Call {@link #release()} to free the memory for the
 *       incoming batch. Or, to call {@link #detach()} to keep
 *       the batch memory.</li>
```

### JavadocReference
Cannot resolve symbol `detach()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/PullResultSetReaderImpl.java`
#### Snippet
```java
 *   <li>Iterate over the batch using the reader.</li>
 *   <li>Call {@link #release()} to free the memory for the
 *       incoming batch. Or, to call {@link #detach()} to keep
 *       the batch memory.</li>
 *   </ol>
```

### JavadocReference
Cannot resolve symbol `UnionWriter`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ListState.java`
#### Snippet
```java
 * Finally, when adding another type, we have a repeated union of two
 * types. The key is, in all cases, <tt>listArray.entry()</tt> returns
 * a {@link UnionWriter}, so the client gets a consistent view.
 * <p>
 * Since the list itself changes form (no type, single type, then
```

### JavadocReference
Cannot resolve symbol `TupleMetadata`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/package-info.java`
#### Snippet
```java
 * The "row set model" provides a "dual" of the vector structure used to create,
 * allocate and work with a collection of vectors. The model provides an enhanced
 * "metadata" schema, given by {@link TupleMetadata} and {@link ColumnMetadata},
 * with allocation hints that goes beyond the {@link MaterializedField}
 * used by value vectors.
```

### JavadocReference
Cannot resolve symbol `ColumnMetadata`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/package-info.java`
#### Snippet
```java
 * The "row set model" provides a "dual" of the vector structure used to create,
 * allocate and work with a collection of vectors. The model provides an enhanced
 * "metadata" schema, given by {@link TupleMetadata} and {@link ColumnMetadata},
 * with allocation hints that goes beyond the {@link MaterializedField}
 * used by value vectors.
```

### JavadocReference
Cannot resolve symbol `MaterializedField`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/package-info.java`
#### Snippet
```java
 * allocate and work with a collection of vectors. The model provides an enhanced
 * "metadata" schema, given by {@link TupleMetadata} and {@link ColumnMetadata},
 * with allocation hints that goes beyond the {@link MaterializedField}
 * used by value vectors.
 * <p>
```

### JavadocReference
Cannot resolve symbol `TableScan`
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/MetadataInfoCollector.java`
#### Snippet
```java

  /**
   * Returns {@link TableScan} instance which will be used when produced incremental analyze.
   * Table scan will contain minimal selection required for obtaining correct metadata.
   *
```

### JavadocReference
Cannot resolve symbol `TableScan`
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/MetadataInfoCollector.java`
#### Snippet
```java
   * Table scan will contain minimal selection required for obtaining correct metadata.
   *
   * @return {@link TableScan} instance
   */
  TableScan getPrunedScan();
```

### JavadocReference
Symbol `MAX_ADMISSIBLE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/package-info.java`
#### Snippet
```java
 *   from the queue. It supports following configurations:
 *   <ul>
 *     <li>{@link org.apache.drill.exec.resourcemgr.config.QueryQueueConfigImpl#MAX_ADMISSIBLE_KEY}: Upper bound on the
 *     total number of queries that can be admitted inside a queue. After this limit is reached all the queries
 *     will be moved to waiting state.</li>
```

### JavadocReference
Symbol `MAX_WAITING_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/package-info.java`
#### Snippet
```java
 *     total number of queries that can be admitted inside a queue. After this limit is reached all the queries
 *     will be moved to waiting state.</li>
 *     <li>{@link org.apache.drill.exec.resourcemgr.config.QueryQueueConfigImpl#MAX_WAITING_KEY}: Limits the
 *     total number of queries that can be in waiting state inside a queue. After this limit is reached all the new
 *     queries will be failed immediately.</li>
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/package-info.java`
#### Snippet
```java
 *     total number of queries that can be in waiting state inside a queue. After this limit is reached all the new
 *     queries will be failed immediately.</li>
 *     <li> {@link org.apache.drill.exec.resourcemgr.config.QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY}:
 *     Limits the maximum memory any query in this queue can consume on any node in the cluster. This is to limit a
 *     query from a queue to consume all the resources on a node so that other queues query can also have some
```

### JavadocReference
Symbol `WAIT_FOR_PREFERRED_NODES_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/package-info.java`
#### Snippet
```java
 *     exceed the total memory on a node.
 *     </li>
 *     <li> {@link org.apache.drill.exec.resourcemgr.config.QueryQueueConfigImpl#WAIT_FOR_PREFERRED_NODES_KEY}: This
 *     configuration helps to decide if an admitted query in a queue should wait until it has available resources on all
 *     the nodes assigned to it by planner for its execution. By default it's true. When set to false then for the nodes
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.proto.beans.DrillbitEndpoint`
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/QueryQueueConfigImpl.java`
#### Snippet
```java
 * Parses and initialize QueueConfiguration for a {@link ResourcePool}. It also generates a unique UUID for each queue
 * which will be later used by Drillbit to store in Zookeeper along with
 * {@link org.apache.drill.exec.proto.beans.DrillbitEndpoint}. This UUID is used in the leader election mechanism in
 * which Drillbit participates for each of the configured rm queue.
 */
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectionpolicy/package-info.java`
#### Snippet
```java
 *   <li>{@link org.apache.drill.exec.resourcemgr.config.selectionpolicy.BestFitQueueSelection}: Out of all the eligible pools
 *   this policy will choose a pool whose queue configuration
 *   {@link org.apache.drill.exec.resourcemgr.config.QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY} value is closest to
 *   the max memory on a node required by the query. It tries to find a pool whose value for MAX_QUERY_MEMORY_PER_NODE
 *   is equal to queries max memory per node requirement. If there is no such pool then find the pool with config value
```

### JavadocReference
Cannot resolve symbol `FileStatus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/FileMetadataInfoCollector.java`
#### Snippet
```java

  /**
   * Returns list of {@link FileStatus} file statuses obtained from specified {@link FileSelection} file selection.
   * Specified file selection may be expanded fully if it wasn't expanded before.
   *
```

### JavadocReference
Cannot resolve symbol `FileStatus`
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/FileMetadataInfoCollector.java`
#### Snippet
```java
   * @param fileSelection file selection
   * @param fs            file system
   * @return list of {@link FileStatus} file statuses
   */
  public static List<FileStatus> getFileStatuses(FileSelection fileSelection, DrillFileSystem fs) throws IOException {
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectionpolicy/BestFitQueueSelection.java`
#### Snippet
```java
 * <ul>
 *   <li>
 *     Queue whose {@link QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY} is
 *     equal to max memory per node of given query
 *   </li>
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectionpolicy/BestFitQueueSelection.java`
#### Snippet
```java
 *   </li>
 *   <li>
 *     Queue whose {@link QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY} is
 *     just greater than max memory per node of given query. From all queues whose max_query_memory_per_node is
 *     greater than what is needed by the query, the queue with minimum value is chosen.
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectionpolicy/BestFitQueueSelection.java`
#### Snippet
```java
 *   </li>
 *   <li>
 *     Queue whose {@link QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY} is
 *     just less than max memory per node of given query. From all queues whose max_query_memory_per_node is
 *     less than what is needed by the query, the queue with maximum value is chosen.
```

### JavadocReference
Symbol `MAX_QUERY_MEMORY_PER_NODE_KEY` is inaccessible from here
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/selectionpolicy/BestFitQueueSelection.java`
#### Snippet
```java
  /**
   * Comparator used to sort the leaf ResourcePool lists based on
   * {@link QueryQueueConfigImpl#MAX_QUERY_MEMORY_PER_NODE_KEY}
   */
  private static class BestFitComparator implements Comparator<ResourcePool> {
```

### JavadocReference
Cannot resolve symbol `getSubSchema(String, boolean)`
in `exec/java-exec/src/main/java/org/apache/calcite/jdbc/DynamicRootSchema.java`
#### Snippet
```java

/**
 * Loads schemas from storage plugins later when {@link #getSubSchema(String, boolean)}
 * is called.
 */
```

### JavadocReference
Cannot resolve symbol `org.apache.hadoop.conf.Configuration`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/config/IcebergConfigConstants.java`
#### Snippet
```java
  /**
   * Defines file system configuration properties which can set
   * using {@link org.apache.hadoop.conf.Configuration} class.
   */
  String CONFIG_PROPERTIES = BASE + "config.properties";
```

### JavadocReference
Cannot resolve symbol `TableProperties`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/operate/ExpirationHandler.java`
#### Snippet
```java
 * <p/>
 * Table metadata in Iceberg is expired automatically
 * if {@link TableProperties#METADATA_DELETE_AFTER_COMMIT_ENABLED} set to true.
 * Number of metadata files to be retained is configured using {@link TableProperties#METADATA_PREVIOUS_VERSIONS_MAX}.
 * Snapshots and data expiration should be called manually to align with metadata expiration process,
```

### JavadocReference
Cannot resolve symbol `METADATA_DELETE_AFTER_COMMIT_ENABLED`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/operate/ExpirationHandler.java`
#### Snippet
```java
 * <p/>
 * Table metadata in Iceberg is expired automatically
 * if {@link TableProperties#METADATA_DELETE_AFTER_COMMIT_ENABLED} set to true.
 * Number of metadata files to be retained is configured using {@link TableProperties#METADATA_PREVIOUS_VERSIONS_MAX}.
 * Snapshots and data expiration should be called manually to align with metadata expiration process,
```

### JavadocReference
Cannot resolve symbol `TableProperties`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/operate/ExpirationHandler.java`
#### Snippet
```java
 * Table metadata in Iceberg is expired automatically
 * if {@link TableProperties#METADATA_DELETE_AFTER_COMMIT_ENABLED} set to true.
 * Number of metadata files to be retained is configured using {@link TableProperties#METADATA_PREVIOUS_VERSIONS_MAX}.
 * Snapshots and data expiration should be called manually to align with metadata expiration process,
 * the same table properties are used to determine if expiration is needed and which number
```

### JavadocReference
Cannot resolve symbol `METADATA_PREVIOUS_VERSIONS_MAX`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/operate/ExpirationHandler.java`
#### Snippet
```java
 * Table metadata in Iceberg is expired automatically
 * if {@link TableProperties#METADATA_DELETE_AFTER_COMMIT_ENABLED} set to true.
 * Number of metadata files to be retained is configured using {@link TableProperties#METADATA_PREVIOUS_VERSIONS_MAX}.
 * Snapshots and data expiration should be called manually to align with metadata expiration process,
 * the same table properties are used to determine if expiration is needed and which number
```

### JavadocReference
Cannot resolve symbol `Expression`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/FilterTransformer.java`
#### Snippet
```java

/**
 * Transforms given input into Iceberg {@link Expression} which is used as filter
 * to retrieve, overwrite or delete Metastore component data.
 */
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/IcebergMetastore.java`
#### Snippet
```java

  /**
   * Initializes {@link Configuration} based on config properties.
   * if config properties are not indicated, returns default instance.
   *
```

### JavadocReference
Cannot resolve symbol `Configuration`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/IcebergMetastore.java`
#### Snippet
```java
   * if config properties are not indicated, returns default instance.
   *
   * @return {@link Configuration} instance
   */
  private Configuration configuration() {
```

### JavadocReference
Cannot resolve symbol `org.apache.iceberg.data.Record`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/components/tables/TablesOutputDataTransformer.java`
#### Snippet
```java
/**
 * Metastore Tables component output data transformer that transforms
 * {@link org.apache.iceberg.data.Record} into {@link TableMetadataUnit}.
 */
public class TablesOutputDataTransformer extends OutputDataTransformer<TableMetadataUnit> {
```

### JavadocReference
Cannot resolve symbol `Record`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/InputDataTransformer.java`
#### Snippet
```java
   * @param schema partition schema
   * @param unitGetters specific Metastore component unit getters
   * @return {@link Record} with partition values
   * @throws IcebergMetastoreException if getter to partition column is absent or
   *         partition column value is null
```

### JavadocReference
Cannot resolve symbol `Expression`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/FilterExpressionVisitor.java`
#### Snippet
```java

/**
 * Visits {@link FilterExpression} implementations and transforms them into Iceberg {@link Expression}.
 */
public class FilterExpressionVisitor implements FilterExpression.Visitor<Expression> {
```

### JavadocReference
Cannot resolve symbol `Record`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/OutputDataTransformer.java`
#### Snippet
```java

  /**
   * Converts given list of {@link Record} into Metastore component units.
   * Specific for each Metastore component.
   *
```

### JavadocReference
Cannot resolve symbol `Record`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/OutputDataTransformer.java`
#### Snippet
```java

/**
 * Base class to convert list of {@link Record}
 * into Metastore component units for the given list of column names.
 *
```

### JavadocReference
Cannot resolve symbol `BytesInput`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while writing
     * @deprecated this method does not support writing column indexes; Use
     *             {@link #writeDataPage(int, int, BytesInput, Statistics, long, Encoding, Encoding, Encoding)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `Statistics`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while writing
     * @deprecated this method does not support writing column indexes; Use
     *             {@link #writeDataPage(int, int, BytesInput, Statistics, long, Encoding, Encoding, Encoding)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `Encoding`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while writing
     * @deprecated this method does not support writing column indexes; Use
     *             {@link #writeDataPage(int, int, BytesInput, Statistics, long, Encoding, Encoding, Encoding)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `Encoding`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while writing
     * @deprecated this method does not support writing column indexes; Use
     *             {@link #writeDataPage(int, int, BytesInput, Statistics, long, Encoding, Encoding, Encoding)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `Encoding`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while writing
     * @deprecated this method does not support writing column indexes; Use
     *             {@link #writeDataPage(int, int, BytesInput, Statistics, long, Encoding, Encoding, Encoding)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `JobSummaryLevel`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java

    /**
     * writes _common_metadata file, and optionally a _metadata file depending on the {@link JobSummaryLevel} provided
     * @param configuration the configuration to use to get the FileSystem
     * @param outputPath the directory to write the _metadata file to
```

### JavadocReference
Cannot resolve symbol `SeekableInputStream`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while reading or writing
     * @deprecated will be removed in 2.0.0;
     *             use {@link #appendRowGroup(SeekableInputStream,BlockMetaData,boolean)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `BlockMetaData`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while reading or writing
     * @deprecated will be removed in 2.0.0;
     *             use {@link #appendRowGroup(SeekableInputStream,BlockMetaData,boolean)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `InputFile`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @param file a file path to append the contents of to this file
     * @throws IOException if there is an error while reading or writing
     * @deprecated will be removed in 2.0.0; use {@link #appendFile(InputFile)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `SeekableInputStream`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * Copy from a FS input stream to an output stream. Thread-safe
     *
     * @param from a {@link SeekableInputStream}
     * @param to any {@link PositionOutputStream}
     * @param start where in the from stream to start copying
```

### JavadocReference
Cannot resolve symbol `PositionOutputStream`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     *
     * @param from a {@link SeekableInputStream}
     * @param to any {@link PositionOutputStream}
     * @param start where in the from stream to start copying
     * @param length the number of bytes to copy
```

### JavadocReference
Cannot resolve symbol `SeekableInputStream`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @throws IOException if there is an error while reading or writing
     * @deprecated will be removed in 2.0.0;
     *             use {@link #appendRowGroups(SeekableInputStream,List,boolean)} instead
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `SQLDialect`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/QueryExecutorProvider.java`
#### Snippet
```java
  /**
   * Defines SQL dialect based on data source connection.
   * If unable to define the dialect, uses {@link SQLDialect#DEFAULT}.
   *
   * @return SQL dialect
```

### JavadocReference
Cannot resolve symbol `DEFAULT`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/QueryExecutorProvider.java`
#### Snippet
```java
  /**
   * Defines SQL dialect based on data source connection.
   * If unable to define the dialect, uses {@link SQLDialect#DEFAULT}.
   *
   * @return SQL dialect
```

### JavadocReference
Cannot resolve symbol `SQLDialect`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/util/DbHelper.java`
#### Snippet
```java

  /**
   * Initializes {@link DbHelper} implementation based on {@link SQLDialect}.
   *
   * @param config Metastore config
```

### JavadocReference
Cannot resolve symbol `DataAccessException`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/operate/RdbmsRead.java`
#### Snippet
```java
   * @param condition query condition
   * @return list of Metastore component units
   * @throws DataAccessException if unable to query data from table
   */
  private List<T> queryRecords(DSLContext executor,
```

### JavadocReference
Cannot resolve symbol `DataAccessException`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/operate/RdbmsRead.java`
#### Snippet
```java
   * @param condition query condition
   * @return list of Metastore component units
   * @throws DataAccessException if unable to query data from table
   */
  private List<T> countRecords(DSLContext executor,
```

### JavadocReference
Cannot resolve symbol `Condition`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/transform/RdbmsFilterExpressionVisitor.java`
#### Snippet
```java

/**
 * Visits {@link FilterExpression} implementations and transforms them into JOOQ {@link Condition}.
 */
@SuppressWarnings({"unchecked", "rawtypes"})
```

### JavadocReference
Cannot resolve symbol `nextRowKey`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/RestrictedMapRDBSubScanSpec.java`
#### Snippet
```java
  /**
   * Returns {@code true} if the iteration has more row keys.
   * (In other words, returns {@code true} if {@link #nextRowKey} would
   * return a non-null row key)
   * @return {@code true} if the iteration has more row keys
```

### JavadocReference
Cannot resolve symbol `Tables`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#ROW_GROUPS} table.
   */
  public static class RowGroupMapper extends TablesMetadataMapper<RowGroupsRecord> {
```

### JavadocReference
Cannot resolve symbol `ROW_GROUPS`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#ROW_GROUPS} table.
   */
  public static class RowGroupMapper extends TablesMetadataMapper<RowGroupsRecord> {
```

### JavadocReference
Cannot resolve symbol `Tables`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#PARTITIONS} table.
   */
  public static class PartitionMapper extends TablesMetadataMapper<PartitionsRecord> {
```

### JavadocReference
Cannot resolve symbol `PARTITIONS`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#PARTITIONS} table.
   */
  public static class PartitionMapper extends TablesMetadataMapper<PartitionsRecord> {
```

### JavadocReference
Cannot resolve symbol `Tables`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#SEGMENTS} table.
   */
  public static class SegmentMapper extends TablesMetadataMapper<SegmentsRecord> {
```

### JavadocReference
Cannot resolve symbol `SEGMENTS`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#SEGMENTS} table.
   */
  public static class SegmentMapper extends TablesMetadataMapper<SegmentsRecord> {
```

### JavadocReference
Cannot resolve symbol `Tables`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#FILES} table.
   */
  public static class FileMapper extends TablesMetadataMapper<FilesRecord> {
```

### JavadocReference
Cannot resolve symbol `FILES`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#FILES} table.
   */
  public static class FileMapper extends TablesMetadataMapper<FilesRecord> {
```

### JavadocReference
Cannot resolve symbol `Tables`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#TABLES} table.
   */
  public static class TableMapper extends TablesMetadataMapper<TablesRecord> {
```

### JavadocReference
Cannot resolve symbol `TABLES`
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java

  /**
   * {@link TablesMetadataMapper} implementation for {@link Tables#TABLES} table.
   */
  public static class TableMapper extends TablesMetadataMapper<TablesRecord> {
```

### JavadocReference
Cannot resolve symbol `FieldPath`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/FieldPathHelper.java`
#### Snippet
```java

  /**
   * Returns {@link FieldPath} equivalent of the specified {@link SchemaPath}.
   */
  public static FieldPath schemaPath2FieldPath(SchemaPath column) {
```

### JavadocReference
Cannot resolve symbol `FieldPath`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/FieldPathHelper.java`
#### Snippet
```java

  /**
   * Returns {@link SchemaPath} equivalent of the specified {@link FieldPath}.
   */
  public static SchemaPath fieldPath2SchemaPath(FieldPath fieldPath) {
```

### JavadocReference
Cannot resolve symbol `OTimestamp`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/CompareFunctionsProcessor.java`
#### Snippet
```java
   * Converts specified function call to be pushed into maprDB JSON scan.
   * For the case when timestamp value is used, it is converted to UTC timezone
   * before converting to {@link OTimestamp} instance.
   *
   * @param call function call to be pushed
```

### JavadocReference
Cannot resolve symbol `FieldPath`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/util/FieldPathHelper.java`
#### Snippet
```java

  /**
   * Returns {@link FieldPath} equivalent of the specified {@link SchemaPath}.
   *
   * @param schemaPath {@link SchemaPath} instance that should be converted
```

### JavadocReference
Cannot resolve symbol `FieldPath`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/util/FieldPathHelper.java`
#### Snippet
```java
   *
   * @param schemaPath {@link SchemaPath} instance that should be converted
   * @return {@link FieldPath} equivalent of the specified {@link SchemaPath}.
   */
  public static FieldPath schemaPathToFieldPath(SchemaPath schemaPath) {
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java

  /**
   * Get the row count after applying the {@link RexNode} condition
   * @param condition filter to apply
   * @param scanRel the current scan rel
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java

  /**
   * Get the estimated statistics after applying the {@link QueryCondition} condition
   * @param condition filter to apply
   * @param index to use for generating the estimate
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java

  /**
   * Set the row count resulting from applying the {@link RexNode} condition. Forced row counts will take
   * precedence over stats row counts
   * @param condition filter to apply
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java

  /**
   * Convert a given {@link LogicalExpression} condition into a {@link QueryCondition} condition
   * @param condition expressed as a {@link LogicalExpression}
   * @return {@link QueryCondition} condition equivalent to the given expression
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
   * Convert a given {@link LogicalExpression} condition into a {@link QueryCondition} condition
   * @param condition expressed as a {@link LogicalExpression}
   * @return {@link QueryCondition} condition equivalent to the given expression
   */
  @JsonIgnore
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java

  /**
   * Get the estimated statistics after applying the {@link RexNode} condition. DO NOT call this API directly.
   * Call the stats API instead which modifies the counts based on preference options.
   * @param condition filter to apply
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
   * This is the core statistics function for populating the statistics. The statistics populated correspond to the query
   * condition. Based on different types of plans, we would need statistics for different combinations of predicates. Currently,
   * we do not have a tree-walker for {@link QueryCondition}. Hence, instead of using the individual predicates stats, to construct
   * the stats for the overall predicates, we rely on using the final predicates. Hence, this has a limitation(susceptible) to
   * predicate modification post stats generation. Statistics computed/stored are rowcounts, leading rowcounts, average rowsize.
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java

  /** Returns the number of leading rows satisfying the given FILTER condition
   *  @param condition - FILTER specified as a {@link RexNode}
   *  @param tabIdxName - The table/index identifier
   *  @param scanRel - The current scanRel
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java

  /** Returns the number of rows satisfying the given FILTER condition
   *  @param condition - FILTER specified as a {@link QueryCondition}
   *  @param tabIdxName - The table/index identifier
   *  @return approximate rows satisfying the filter
```

### JavadocReference
Cannot resolve symbol `QueryCondition`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java

  /** Returns the number of leading rows satisfying the given FILTER condition
   *  @param condition - FILTER specified as a {@link QueryCondition}
   *  @param tabIdxName - The table/index identifier
   *  @return approximate rows satisfying the leading filter
```

### JavadocReference
Symbol `org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler` is inaccessible from here
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *    <li>  MESSAGE_DECODER {@link RpcDecoder}  </li>
   *    <li>  PROTOCOL_ENCODER {@link RpcEncoder} </li>
   *    <li>  HANDSHAKE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler} OR
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}  </li>
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.rpc.BasicClient.IdlePingHandler`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *    <li>  HANDSHAKE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler} OR
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}  </li>
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
   *                   - TIMEOUT_HANDLER {@link org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler}  </li>
   *    <li>  MESSAGE_HANDLER {@link org.apache.drill.exec.rpc.RpcBus.InboundHandler} </li>
```

### JavadocReference
Symbol `org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler` is inaccessible from here
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}  </li>
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
   *                   - TIMEOUT_HANDLER {@link org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler}  </li>
   *    <li>  MESSAGE_HANDLER {@link org.apache.drill.exec.rpc.RpcBus.InboundHandler} </li>
   *    <li>  EXCEPTION_HANDLER {@link RpcExceptionHandler} </li>
```

### JavadocReference
Symbol `org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler` is inaccessible from here
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *    <li>  CHUNK_CREATION_HANDLER {@link ChunkCreationHandler}
   *    <li>  PROTOCOL_ENCODER {@link RpcEncoder}
   *    <li>  HANDSHAKE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler} OR
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
```

### JavadocReference
Cannot resolve symbol `org.apache.drill.exec.rpc.BasicClient.IdlePingHandler`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *    <li>  HANDSHAKE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.ClientHandshakeHandler} OR
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
   *                   - TIMEOUT_HANDLER {@link org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler}
   *    <li>  MESSAGE_HANDLER {@link org.apache.drill.exec.rpc.RpcBus.InboundHandler}
```

### JavadocReference
Symbol `org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler` is inaccessible from here
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   *                            {@link org.apache.drill.exec.rpc.BasicServer.ServerHandshakeHandler}
   *    <li>  optional - IDLE_STATE_HANDLER {@link org.apache.drill.exec.rpc.BasicClient.IdlePingHandler} OR
   *                   - TIMEOUT_HANDLER {@link org.apache.drill.exec.rpc.BasicServer.LoggingReadTimeoutHandler}
   *    <li>  MESSAGE_HANDLER {@link org.apache.drill.exec.rpc.RpcBus.InboundHandler}
   *    <li>  EXCEPTION_HANDLER {@link RpcExceptionHandler}
```

### JavadocReference
Symbol `wrapSizeLimit` is inaccessible from here
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/AbstractRemoteConnection.java`
#### Snippet
```java
   * <p>
   *  If encryption is enabled ChunkCreationHandler is always added to divide the Rpc message into chunks of
   *  negotiated {@link EncryptionContextImpl#wrapSizeLimit} bytes. This helps to make a generic encryption handler.
   * </p>
   */
```

### JavadocReference
Cannot resolve symbol `CombinedScanTask`
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/IcebergWork.java`
#### Snippet
```java
   * Special serializer for {@link IcebergWork} class that serializes
   * {@code scanTask} field to byte array string created using {@link java.io.Serializable}
   * since {@link CombinedScanTask} doesn't use any Jackson annotations.
   */
  public static class IcebergWorkSerializer extends StdSerializer<IcebergWork> {
```

### JavadocReference
Cannot resolve symbol `OutOfMemoryException`
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   *          The size in bytes.
   * @return a new DrillBuf, or null if the request can't be satisfied
   * @throws OutOfMemoryException
   *           if buffer cannot be allocated
   */
```

### JavadocReference
Cannot resolve symbol `OutOfMemoryException`
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BufferAllocator.java`
#### Snippet
```java
   *          A buffer manager to manage reallocation.
   * @return a new DrillBuf, or null if the request can't be satisfied
   * @throws OutOfMemoryException
   *           if buffer cannot be allocated
   */
```

### JavadocReference
Cannot resolve symbol `Files`
in `contrib/format-access/src/main/java/org/apache/drill/exec/store/msaccess/MSAccessFormatPlugin.java`
#### Snippet
```java
   * First tries to get drill temporary directory value from config ${drill.tmp-dir},
   * then checks environmental variable $DRILL_TMP_DIR.
   * If value is still missing, generates directory using {@link Files#createTempDir()}.
   *
   * @return drill temporary directory path
```

### JavadocReference
Cannot resolve symbol `createTempDir()`
in `contrib/format-access/src/main/java/org/apache/drill/exec/store/msaccess/MSAccessFormatPlugin.java`
#### Snippet
```java
   * First tries to get drill temporary directory value from config ${drill.tmp-dir},
   * then checks environmental variable $DRILL_TMP_DIR.
   * If value is still missing, generates directory using {@link Files#createTempDir()}.
   *
   * @return drill temporary directory path
```

### JavadocReference
Cannot resolve symbol `RexNode`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/plan/RexToMongoTranslator.java`
#### Snippet
```java

/**
 * Translator from {@link RexNode} to strings in MongoDB's expression language.
 */
class RexToMongoTranslator extends RexVisitorImpl<BsonValue> {
```

### JavadocReference
Cannot resolve symbol `com.mongodb.client.MongoCollection`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/plan/MongoPluginImplementor.java`
#### Snippet
```java
/**
 * Implementation of {@link PluginImplementor} for Mongo.
 * This class tries to convert operators to use {@link com.mongodb.client.MongoCollection#find}
 * if only simple project and filter expressions are present,
 * otherwise {@link com.mongodb.client.MongoCollection#aggregate} is used to obtain data from Mongo.
```

### JavadocReference
Cannot resolve symbol `find`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/plan/MongoPluginImplementor.java`
#### Snippet
```java
/**
 * Implementation of {@link PluginImplementor} for Mongo.
 * This class tries to convert operators to use {@link com.mongodb.client.MongoCollection#find}
 * if only simple project and filter expressions are present,
 * otherwise {@link com.mongodb.client.MongoCollection#aggregate} is used to obtain data from Mongo.
```

### JavadocReference
Cannot resolve symbol `com.mongodb.client.MongoCollection`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/plan/MongoPluginImplementor.java`
#### Snippet
```java
 * This class tries to convert operators to use {@link com.mongodb.client.MongoCollection#find}
 * if only simple project and filter expressions are present,
 * otherwise {@link com.mongodb.client.MongoCollection#aggregate} is used to obtain data from Mongo.
 */
public class MongoPluginImplementor extends AbstractPluginImplementor {
```

### JavadocReference
Cannot resolve symbol `aggregate`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/plan/MongoPluginImplementor.java`
#### Snippet
```java
 * This class tries to convert operators to use {@link com.mongodb.client.MongoCollection#find}
 * if only simple project and filter expressions are present,
 * otherwise {@link com.mongodb.client.MongoCollection#aggregate} is used to obtain data from Mongo.
 */
public class MongoPluginImplementor extends AbstractPluginImplementor {
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java

  /**
   * Performs the actual DNS lookup and returns the results in a {@link ComplexWriter}.  Uses the local cache as the
   * DNS resolver.
   * @param domainName A {@link String} of a domain for which you want to look up.
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java
   * DNS resolver.
   * @param domainName A {@link String} of a domain for which you want to look up.
   * @param out The {@link ComplexWriter} to which the DNS results will be written.
   * @param buffer The {@link DrillBuf} to which the data will be written.
   * @throws TextParseException If the domain is unparsable, throw an exception.
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java
   * @param domainName The input domain name.
   * @param whoisServer A {@link String} containing the whois server
   * @param out A {@link ComplexWriter} to which the results will be written
   * @param buffer A {@link DrillBuf} which contains the output
   */
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java

  /**
   * Performs the actual DNS lookup and returns the results in a {@link ComplexWriter}.  If the resolver
   * is not null, we will use the provided resolver.  If a resolver is not provided, we'll use the local cache.
   * <p>
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java
   * @param domainName A {@link String} of a domain for which you want to look up.
   * @param resolverName A {@link String} containing the resolver name.
   * @param out The {@link ComplexWriter} to which the DNS results will be written.
   * @param buffer The {@link DrillBuf} to which the data will be written.
   * @throws TextParseException If the resolver is unparsable, throw an exception.
```

### JavadocReference
Cannot resolve symbol `ComplexWriter`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSUtils.java`
#### Snippet
```java
   * Performs a WHOIS lookup and populates the results into a Drill map.  This method uses a default server.
   * @param domainName The input domain name.
   * @param out A {@link ComplexWriter} to which the results will be written
   * @param buffer A {@link DrillBuf} which contains the output
   */
```

### JavadocReference
Cannot resolve symbol `org.apache.kafka.clients.consumer.KafkaConsumer`
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaAsyncCloser.java`
#### Snippet
```java
 * Closes Kafka resources asynchronously which result does not depend on close method
 * in order to improve query execution performance.
 * For example, {@link org.apache.kafka.clients.consumer.KafkaConsumer}.
 */
public class KafkaAsyncCloser implements AutoCloseable {
```

### JavadocReference
Cannot resolve symbol `Sheet`
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelBatchReader.java`
#### Snippet
```java
  /**
   * Helper function to get the selected sheet from the configuration
   * @return {@link Sheet} The selected sheet
   */
  private Sheet getSheet() {
```

### JavadocReference
Cannot resolve symbol `unknownField`
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkUtils.java`
#### Snippet
```java
     * Indicates whether the field in question is a special field and should be
     * pushed down to the query or not.
     * @param unknownField The field to be pushed down
     * @return true if the field is a special field, false if not.
     */
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/HistoricalLog.java`
#### Snippet
```java

  /**
   * Write the history of this object to the given {@link Logger}. The history
   * includes the identifying string provided at construction time, and all the recorded
   * events with their stack traces.
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/HistoricalLog.java`
#### Snippet
```java
   * events with their stack traces.
   *
   * @param logger {@link Logger} to write to
   */
  public void logHistory(final Logger logger) {
```

### JavadocReference
Cannot resolve symbol `ImmutableMap`
in `common/src/main/java/org/apache/drill/common/map/CaseInsensitiveMap.java`
#### Snippet
```java

  /**
   * Returns a new instance of {@link ImmutableMap} with key case-insensitivity. This map is built from the given
   * map. See {@link ImmutableMap}.
   *
```

### JavadocReference
Cannot resolve symbol `ImmutableMap`
in `common/src/main/java/org/apache/drill/common/map/CaseInsensitiveMap.java`
#### Snippet
```java
  /**
   * Returns a new instance of {@link ImmutableMap} with key case-insensitivity. This map is built from the given
   * map. See {@link ImmutableMap}.
   *
   * @param map map to copy from
```

### JavadocReference
Cannot resolve symbol `javassist.scopedpool.ScopedClassPool`
in `common/src/main/java/org/apache/drill/common/util/GuavaPatcher.java`
#### Snippet
```java

  /**
   * Returns {@link javassist.scopedpool.ScopedClassPool} instance which uses the same class loader
   * which was used for loading current class.
   *
```

### JavadocReference
Cannot resolve symbol `javassist.scopedpool.ScopedClassPool`
in `common/src/main/java/org/apache/drill/common/util/GuavaPatcher.java`
#### Snippet
```java
   * which was used for loading current class.
   *
   * @return {@link javassist.scopedpool.ScopedClassPool} instance
   */
  private static ClassPool getClassPool() {
```

### JavadocReference
Cannot resolve symbol `javassist.scopedpool.ScopedClassPool`
in `common/src/main/java/org/apache/drill/common/util/ProtobufPatcher.java`
#### Snippet
```java

  /**
   * Returns {@link javassist.scopedpool.ScopedClassPool} instance which uses the same class loader
   * which was used for loading current class.
   *
```

### JavadocReference
Cannot resolve symbol `javassist.scopedpool.ScopedClassPool`
in `common/src/main/java/org/apache/drill/common/util/ProtobufPatcher.java`
#### Snippet
```java
   * which was used for loading current class.
   *
   * @return {@link javassist.scopedpool.ScopedClassPool} instance
   */
  private static ClassPool getClassPool() {
```

### JavadocReference
Cannot resolve symbol `CannotCompileException`
in `common/src/main/java/org/apache/drill/common/util/ProtobufPatcher.java`
#### Snippet
```java
   * @param ctMethod   method to process.
   * @param methodBody method implementation.
   * @throws CannotCompileException if unable to compile given method body.
   */
  private static void addImplementation(CtMethod ctMethod, String methodBody) throws CannotCompileException {
```

### JavadocReference
Cannot resolve symbol `ListenableFuture`
in `common/src/main/java/org/apache/drill/common/concurrent/CheckedFuture.java`
#### Snippet
```java

/**
 * A {@code CheckedFuture} is a {@link ListenableFuture} that includes versions of the {@code get}
 * methods that can throw a checked exception. This makes it easier to create a future that executes
 * logic which can throw an exception.
```

### JavadocReference
Cannot resolve symbol `Futures`
in `common/src/main/java/org/apache/drill/common/concurrent/CheckedFuture.java`
#### Snippet
```java
 *
 * <p>For a simpler alternative to CheckedFuture, consider accessing Future values with {@link
 * Futures#getChecked(Future, Class) Futures.getChecked()}.
 *
 * This interface is moved from Guava, since there it was deleted.
```

### JavadocReference
Cannot resolve symbol `getChecked(Future, Class)`
in `common/src/main/java/org/apache/drill/common/concurrent/CheckedFuture.java`
#### Snippet
```java
 *
 * <p>For a simpler alternative to CheckedFuture, consider accessing Future values with {@link
 * Futures#getChecked(Future, Class) Futures.getChecked()}.
 *
 * This interface is moved from Guava, since there it was deleted.
```

### JavadocReference
Cannot resolve symbol `ListenableFuture`
in `common/src/main/java/org/apache/drill/common/concurrent/AbstractCheckedFuture.java`
#### Snippet
```java

/**
 * A delegating wrapper around a {@link ListenableFuture} that adds support for the {@link
 * #checkedGet()} and {@link #checkedGet(long, TimeUnit)} methods.
 *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a data write error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a plan error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
    /**
     * builds a user exception or returns the wrapped one. If the error is a system error, the error message is logged
     * to the given {@link Logger}.
     *
     * @param logger the logger to write to
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a data read error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `rebuild`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
     * Sets the error type. This method is normally not needed because of
     * the per-error-type static initialiser methods above but it may be
     * used in conjunction with {@link rebuild()} to clone an existing
     * UserException modified to use a different error type.
     * @param errorType the error type to set.
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a resource error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * wraps the passed exception inside a validation error.
   * <p>the cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>if the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a parse error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a system error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a function error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a connection error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a unsupported error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by
   * {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception
   * as well.
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
     *
     * @return user exception
     * @deprecated Use {@link #build(Logger)} instead. If the error is a system error, the error message is logged to
     * this {@link UserException#logger}.
     */
```

### JavadocReference
Cannot resolve symbol `Logger`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
   * Wraps the passed exception inside a permission error.
   * <p>The cause message will be used unless {@link Builder#message(String, Object...)} is called.
   * <p>If the wrapped exception is, or wraps, a user exception it will be returned by {@link Builder#build(Logger)}
   * instead of creating a new exception. Any added context will be added to the user exception as well.
   *
```

### JavadocReference
Cannot resolve symbol `Document`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/transform/FilterTransformer.java`
#### Snippet
```java

/**
 * Transforms given input into Mongo {@link Document} which is used as filter
 * to retrieve, overwrite or delete Metastore component data.
 */
```

### JavadocReference
Cannot resolve symbol `Document`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/transform/OutputDataTransformer.java`
#### Snippet
```java

  /**
   * Converts given list of {@link Document} into Metastore component units.
   * Specific for each Metastore component.
   *
```

### JavadocReference
Cannot resolve symbol `Document`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/transform/OutputDataTransformer.java`
#### Snippet
```java

/**
 * Base class to convert list of {@link Document}
 * into Metastore component units for the given list of column names.
 *
```

### JavadocReference
Cannot resolve symbol `org.bson.Document`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/components/tables/TablesOutputDataTransformer.java`
#### Snippet
```java
/**
 * Metastore Tables component output data transformer that transforms
 * {@link org.bson.Document} into {@link TableMetadataUnit}.
 */
public class TablesOutputDataTransformer extends OutputDataTransformer<TableMetadataUnit> {
```

### JavadocReference
Cannot resolve symbol `Bson`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/transform/FilterExpressionVisitor.java`
#### Snippet
```java

/**
 * Visits {@link FilterExpression} implementations and transforms them into {@link Bson} implementations.
 */
public class FilterExpressionVisitor implements FilterExpression.Visitor<Bson> {
```

### JavadocReference
Cannot resolve symbol `Document`
in `metastore/mongo-metastore/src/main/java/org/apache/drill/metastore/mongo/transform/InputDataTransformer.java`
#### Snippet
```java

/**
 * Converts list of Metastore component units into {@link Document}.
 *
 * @param <T> Metastore component unit type
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/WritableBatch.java`
#### Snippet
```java
      }

      for (DrillBuf b : vv.getBuffers(true)) {
        buffers.add(b);
      }
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/ComplexWriterImpl.java`
#### Snippet
```java
  private final boolean unionEnabled;

  private enum Mode { INIT, MAP, LIST };

  public ComplexWriterImpl(String name, MapVector container, boolean unionEnabled){
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
      protected RequestStatus getStatus(GetTablesResp response) {
        return response.getStatus();
      };

      @Override
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
      protected DrillPBError getError(GetTablesResp response) {
        return response.getError();
      };

      @Override
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
      protected MetaImpl.MetaTable adapt(TableMetadata protoValue) {
        return new MetaImpl.MetaTable(protoValue.getCatalogName(), protoValue.getSchemaName(), protoValue.getTableName(), protoValue.getType());
      };
    }.getMeta(connection.getClient().getTables(catalogNameFilter, schemaNameFilter, tableNameFilter, typeList));
  }
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
      protected List<ColumnMetadata> getResult(GetColumnsResp response) {
        return response.getColumnsList();
      };

      private int getDataType(ColumnMetadata value) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
    }

    private enum FormatState { PRE_STACK, IN_STACK, POST_STACK };

    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebUiPageTree.java`
#### Snippet
```java
    public enum Mode {
      SHRINK, STOP, CANCEL, KILL
    };

    Mode mode;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebUiPageTree.java`
#### Snippet
```java
    public enum Mode {
      STOPPED, INVALID_RESIZE, INVALID_ACTION, NULL_RESIZE, RESIZED, CANCELLED, INVALID_TASK
    };

    Mode mode;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `logical/src/main/java/org/apache/drill/common/logical/PlanProperties.java`
#### Snippet
```java

    public static enum ResultMode {
      EXEC, LOGICAL, PHYSICAL;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `logical/src/main/java/org/apache/drill/common/expression/types/DataType.java`
#### Snippet
```java

  public static enum Comparability{
    UNKNOWN, NONE, EQUAL, ORDERED;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java
public class QueryContext implements AutoCloseable, OptimizerRulesContext, SchemaConfigInfoProvider {
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(QueryContext.class);
  public enum SqlStatementType {OTHER, ANALYZE, CTAS, EXPLAIN, DESCRIBE_TABLE, DESCRIBE_SCHEMA, REFRESH, SELECT, SETOPTION, INSERT};

  private final DrillbitContext drillbitContext;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/RegexpUtil.java`
#### Snippet
```java
    CONSTANT, // Is a constant string (ABC)
    COMPLEX // Not a simple pattern. Needs regex evaluation.
  };

  public static class SqlPatternInfo {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/concurrent/ExecutorServiceUtil.java`
#### Snippet
```java
      RUNNING,
      DONE
    };

    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryQueue.java`
#### Snippet
```java

    String queueName();
  };

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
  Map<String, StoragePluginConfig> storedConfigs();

  enum PluginFilter { ALL, ENABLED, DISABLED, TRANSLATES_USERS };

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
            .compare(left.getSchemaName(), right.getSchemaName())
            .result();
      };
    };

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
    }
    pluginsOverrideFileUrl = urlSet.iterator().next();
    try (InputStream is = pluginsOverrideFileUrl.openStream();) {
      return context.hoconMapper().readValue(is, StoragePlugins.class);
    } catch (IOException e) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/OptionIterator.java`
#### Snippet
```java
    // for internal options is to enable support to easily debug.
    SYS_SESS_INTERNAL
  };

  private final OptionManager fragmentOptions;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchMemoryManager.java`
#### Snippet
```java
  }

  public void update() {};

  public void update(RecordBatch recordBatch) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ByteCodeLoader.java`
#### Snippet
```java
      return Resources.toByteArray(u);
    }
  };

  public byte[] getClassByteCodeFromPath(String path) throws ClassTransformationException, IOException {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassCompilerSelector.java`
#### Snippet
```java

  public enum CompilerPolicy {
    DEFAULT, JDK, JANINO;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
    NATIVE_SECONDARY_INDEX,
    EXTERNAL_SECONDARY_INDEX
  };

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
    NATIVE_SECONDARY_INDEX_COLLECTION,
    EXTERNAL_SECONDARY_INDEX_COLLECTION
  };

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
public class RowKeyJoinCallContext {

  public enum RowKey {NONE, LEFT, RIGHT, BOTH};
  // RelOptRule call
  private RelOptRuleCall call;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinWorker.java`
#### Snippet
```java

  enum JoinOutcome {
    NO_MORE_DATA, BATCH_RETURNED, SCHEMA_CHANGED, WAITING, FAILURE;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
   */
  public enum RowKeyJoinState {
    INITIAL, PROCESSING, DONE;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/ReaderState.java`
#### Snippet
```java

    CLOSED
  };

  final ScanOperatorExec scanOp;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
  float DEFAULT_LOAD_FACTOR = 0.75f;

  enum PutStatus {KEY_PRESENT, KEY_ADDED, NEW_BATCH_ADDED, KEY_ADDED_LAST, PUT_FAILED;}

  /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/ChainedHashTable.java`
#### Snippet
```java
  private final RecordBatch outgoing;

  private enum SetupWork {DO_BUILD, DO_PROBE, CHECK_BOTH_NULLS};

  public ChainedHashTable(HashTableConfig htConfig, FragmentContext context, BufferAllocator allocator,
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggregator.java`
#### Snippet
```java
    CLEANUP_AND_RETURN,
    UPDATE_AGGREGATOR,
    RETURN_AND_RESET;
  }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java
    @RuntimeOverridden
    protected void doSetup(@Named("incoming") RecordBatch incoming,
                           @Named("outgoing") VectorAccessible outgoing) throws SchemaChangeException { };

    @RuntimeOverridden
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java
    @RuntimeOverridden
    protected void doEval(@Named("inIndex") int inIndex,
                          @Named("outIndex") int outIndex) throws SchemaChangeException { };

    public void flush(boolean schemaChanged) throws IOException {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
            STATE endColumn() {
                return BLOCK;
            };
            STATE write() {
                return this;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
public class MaprDBJsonRecordReader extends AbstractRecordReader {
  private static final Logger logger = LoggerFactory.getLogger(MaprDBJsonRecordReader.class);
  protected enum SchemaState {SCHEMA_UNKNOWN, SCHEMA_INIT, SCHEMA_CHANGE};

  protected static final FieldPath[] ID_ONLY_PROJECTION = { ID_FIELD };
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
    // to evict a plugin. So, we just use a simple per-handle iteration here.
    plugins().stream()
      .forEach(e -> e.close());
    configMap.clear();
    nameMap.clear();
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
  @Override
  public void init() {
    locators.stream().forEach(loc -> loc.init());
    try {
      loadIntrinsicPlugins();
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
    }
    pluginStore.putAll(bootstrapPlugins);
    locators.stream().forEach(loc -> loc.onUpgrade());
  }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/NodeResource.java`
#### Snippet
```java
  public static Map<DrillbitEndpoint, NodeResource> merge(Map<DrillbitEndpoint, NodeResource> to,
                                                          Map<DrillbitEndpoint, NodeResource> from) {
    to.entrySet().stream().forEach((toEntry) -> toEntry.getValue().add(from.get(toEntry.getKey())));
    return to;
  }
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/MemoryCalculator.java`
#### Snippet
```java
    }
    final int totalSendingFrags = sendingFragsPerDrillBit.entrySet().stream()
                                                         .mapToInt((x) -> x.getValue()).reduce(0, (x, y) -> x+y);
    merge(fragment,
          getMinorFragCountPerDrillbit(fragment),
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
    // Get the physical operators which are above the node memory limit.
    Map<DrillbitEndpoint, List<Pair<PhysicalOperator, Long>>> onlyMemoryAboveLimitOperators = new HashMap<>();
    memoryPerOperator.entrySet().stream().forEach((entry) -> {
      onlyMemoryAboveLimitOperators.putIfAbsent(entry.getKey(), new ArrayList<>());
      if (nodeResourceMap.get(entry.getKey()).getMemory() > nodeLimit) {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
    // Then use the total memory to adjust the memory requirement based on the permissible node limit.
    Map<DrillbitEndpoint, List<Pair<PhysicalOperator, Long>>> memoryAdjustedDrillbits = new HashMap<>();
    onlyMemoryAboveLimitOperators.entrySet().stream().forEach(
      entry -> {
        Long totalMemory = entry.getValue().stream().mapToLong(Pair::getValue).sum();
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
    );

    memoryAdjustedDrillbits.entrySet().stream().forEach(
      operatorMemory -> {
        allDrillbits.put(operatorMemory.getKey(), operatorMemory.getValue());
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
        operators.entrySet()
                  .stream()
                  .forEach((entry) -> entry.getValue()
                                           .addAll(calculator.getBufferedOperators(entry.getKey())));
      }));
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
    //queryrm.selectQueue( pass the max node Resource) returns queue configuration.
    Map<DrillbitEndpoint, List<Pair<PhysicalOperator, Long>>> memoryAdjustedOperators = ensureOperatorMemoryWithinLimits(operators, totalNodeResources, 10);
    memoryAdjustedOperators.entrySet().stream().forEach((x) -> {
      Map<PhysicalOperator, Long> memoryPerOperator = x.getValue().stream()
                                                                  .collect(Collectors.toMap(operatorLongPair -> operatorLongPair.getLeft(),
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
    runner.remoteSiteDir = uploader.remoteSiteDir;
    if (uploader.isLocalized()) {
      runner.drillArchivePath = uploader.drillArchivePath.toString();
      if (uploader.hasSiteDir()) {
        runner.siteArchivePath = uploader.siteArchivePath.toString();
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
      runner.drillArchivePath = uploader.drillArchivePath.toString();
      if (uploader.hasSiteDir()) {
        runner.siteArchivePath = uploader.siteArchivePath.toString();
      }
    }
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
    }

    public String getState( ) { return state.toString(); }
    public String getStateHint( ) { return taskStateHint; }
    public boolean isCancelled( ) { return cancelled; }
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
        .arg(JExpr.lit(udfName))
        .arg(JExpr.lit(false))
        .arg(JExpr.lit(udfClazz.getCanonicalName().toString()));
    }
  }
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `logical/src/main/java/org/apache/drill/common/expression/ConvertExpression.java`
#### Snippet
```java
    super(pos);
    this.input = input;
    this.convertFunction = CONVERT_FROM.equals(convertFunction.toLowerCase()) ? CONVERT_FROM : CONVERT_TO;
    this.encodingType = encodingType.toUpperCase();
    this.type = Types.getMajorTypeFromName(encodingType.split("_", 2)[0].toLowerCase());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
      throw UserException.validationError(e)
        .message("'%s' is not a valid value option '%s' of type %s",
            val.toString(), name, kind.name())
        .build(logger);
    }
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
          for (int i = 0; i < classes.length; i++) {
            if (i == classes.length - 1) {
              sb.append(classes[i].getCanonicalName().toString());
            } else {
              sb.append(classes[i].getCanonicalName().toString() + "->");
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
              sb.append(classes[i].getCanonicalName().toString());
            } else {
              sb.append(classes[i].getCanonicalName().toString() + "->");
            }
          }
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java

    if (fqn != null && fqn.length() > maxLength) {
      fqn = fqn.substring(fqn.length() - maxLength, fqn.length());
    }

```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/BasicList.java`
#### Snippet
```java

  @Override
  public <T> T[] toArray(T[] a) {
    throw new UnsupportedOperationException();
  }
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java

      String amPath = AM_REGISTRY + "/" + clusterId;
      String content = amHost + ":" + Integer.toString(amPort) + ":" + amAppId;
      try {
        zkCoord.getCurator().create().withMode(CreateMode.EPHEMERAL)
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ClusterDef.java`
#### Snippet
```java
      }
      if (DoYUtil.isBlank(theName)) {
        theName = "tier-" + Integer.toString(index);
      }
      name = theName;
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TraceInjector.java`
#### Snippet
```java
        for (int i = 0; i < newChildren.size(); i++)
        {
            String traceTag = newChildren.get(i).toString() + Integer.toString(traceTagCount++);

            /* Trace operator */
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/BaseRootExec.java`
#### Snippet
```java
      final StringBuilder sb = new StringBuilder();
      sb.append("BaseRootExec(");
      sb.append(Integer.toString(System.identityHashCode(this)));
      sb.append(") operators: ");
      for(final CloseableRecordBatch crb : operators) {
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/BaseRootExec.java`
#### Snippet
```java
        sb.append(crb.getClass().getName());
        sb.append(' ');
        sb.append(Integer.toString(System.identityHashCode(crb)));
        sb.append(", ");
      }
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashPartition.java`
#### Snippet
```java
    if (writer == null) {
      final String side = processingOuter ? "outer" : "inner";
      final String suffix = cycleNum > 0 ? side + "_" + Integer.toString(cycleNum) : side;
      spillFile = spillSet.getNextSpillFile(suffix);

```

### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/GenericMetadataDescriptor.java`
#### Snippet
```java
      return null;
    }
    return Long.toString(size) + " bytes";
  }

```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
      final UnsafeDirectLittleEndian udle = ledger.getUnderlying();
      sb.append("UnsafeDirectLittleEndian[dentityHashCode == ");
      sb.append(Integer.toString(System.identityHashCode(udle)));
      sb.append("] size ");
      sb.append(Integer.toString(udle.capacity()));
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
      sb.append(Integer.toString(System.identityHashCode(udle)));
      sb.append("] size ");
      sb.append(Integer.toString(udle.capacity()));
      sb.append('\n');
    }
```

### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
        if (bufferTotal != 0) {
          sb.append("buffer total: ");
          sb.append(Long.toString(bufferTotal));
          sb.append('\n');
          dumpBuffers(sb, ledgerSet);
```

### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/NetworkFunctions.java`
#### Snippet
```java
      long inputInt = in.value;
      for (int i = 0; i < 4; i++) {
        result.insert(0, Long.toString(inputInt & 0xff));
        if (i < 3) {
          result.insert(0, '.');
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `common/src/main/java/org/apache/drill/common/StackTrace.java`
#### Snippet
```java
          .append(ste.getFileName())
          .append(':')
          .append(Integer.toString(ste.getLineNumber()))
          .append(")\n");
    }
```

## RuleId[id=FinallyBlockCannotCompleteNormally]
### FinallyBlockCannotCompleteNormally
`finally` block can not complete normally
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setStatus(RequestStatus.FAILED);
        respBuilder.setError(createPBError("get tables", e));
      } finally {
        return new Response(RpcType.TABLES, respBuilder.build());
      }
```

### FinallyBlockCannotCompleteNormally
`finally` block can not complete normally
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setStatus(RequestStatus.FAILED);
        respBuilder.setError(createPBError("get columns", e));
      } finally {
        return new Response(RpcType.COLUMNS, respBuilder.build());
      }
```

### FinallyBlockCannotCompleteNormally
`finally` block can not complete normally
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setStatus(RequestStatus.FAILED);
        respBuilder.setError(createPBError("get schemas", e));
      } finally {
        return new Response(RpcType.SCHEMAS, respBuilder.build());
      }
```

### FinallyBlockCannotCompleteNormally
`finally` block can not complete normally
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
        respBuilder.setStatus(RequestStatus.FAILED);
        respBuilder.setError(createPBError("get catalogs", e));
      } finally {
        return new Response(RpcType.CATALOGS, respBuilder.build());
      }
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
    switch (type) {
      case TIME:
      case TIMETZ:
      case DATE:
      case TIMESTAMP:
      case TIMESTAMPTZ:
        return true;
      default:
        return false;
    }
```

### DuplicatedCode
Duplicated code
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/format/DeltaFormatPlugin.java`
#### Snippet
```java
    switch (phase) {
      case PHYSICAL:
      case LOGICAL:
        return storagePluginRulesSupplier.getOptimizerRules();
      case LOGICAL_PRUNE_AND_JOIN:
      case LOGICAL_PRUNE:
      case PARTITION_PRUNING:
      case JOIN_PLANNING:
      default:
        return Collections.emptySet();
    }
```

### DuplicatedCode
Duplicated code
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
    visitChild(project.getInput());

    DrillParseContext context = new DrillParseContext(PrelUtil.getPlannerSettings(project.getCluster().getPlanner()));
    RelNode input = project.getInput();

    List<SchemaPath> projects = project.getProjects().stream()
      .map(e -> (SchemaPath) DrillOptiq.toDrill(context, input, e))
      .collect(Collectors.toList());
    groupScan = groupScan.clone(projects);
```

### DuplicatedCode
Duplicated code
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
    if (hasPluginGroupScan(limit)) {
      FirstLimitFinder finder = new FirstLimitFinder();
      limit.getInput().accept(finder);
      int oldLimit = getArtificialLimit(finder.getFetch(), finder.getOffset());
      int newLimit = getArtificialLimit(limit);
      return newLimit >= 0 && (oldLimit < 0 || newLimit < oldLimit);
    }
    return false;
```

### DuplicatedCode
Duplicated code
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
      if (other instanceof DrillLimitRelBase) {
        DrillLimitRelBase limitRelBase = (DrillLimitRelBase) other;
        fetch = limitRelBase.getFetch();
        offset = limitRelBase.getOffset();
        return other;
      } else if (other instanceof RelSubset) {
        RelSubset relSubset = (RelSubset) other;
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
      }
      return super.visit(other);
```

### DuplicatedCode
Duplicated code
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DrillExprToDeltaTranslator.java`
#### Snippet
```java
    StringBuilder sb = new StringBuilder();
    PathSegment segment = schemaPath.getRootSegment();
    sb.append(segment.getNameSegment().getPath());

    while ((segment = segment.getChild()) != null) {
      sb.append('.')
        .append(segment.isNamed()
          ? segment.getNameSegment().getPath()
          : "element");
    }
    return sb.toString();
```

### DuplicatedCode
Duplicated code
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
    int index = rowWriter.tupleSchema().index(name);
    if (index == -1) {
      ColumnMetadata colSchema = MetadataUtils.newScalar(name, TypeProtos.MinorType.VARCHAR, TypeProtos.DataMode.OPTIONAL);
      index = rowWriter.addColumn(colSchema);
    }
    ScalarWriter colWriter = rowWriter.scalar(index);
```

### DuplicatedCode
Duplicated code
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5BatchReader.java`
#### Snippet
```java
    int index = rowWriter.tupleSchema().index(INT_COLUMN_NAME);
    if (index == -1) {
      index = rowWriter.addColumn(nestedSchema.column(INT_COLUMN_NAME));
    }

    // The outer array
    ArrayWriter listWriter = rowWriter.column(index).array();
    // The inner array
    ArrayWriter innerWriter = listWriter.array();
    // The strings within the inner array
    ScalarWriter intWriter = innerWriter.scalar();
```

### DuplicatedCode
Duplicated code
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/read/IcebergScanBatchCreator.java`
#### Snippet
```java
    Preconditions.checkArgument(children.isEmpty());

    try {
      ManagedScanFramework.ScanFrameworkBuilder builder = createBuilder(subScan);
      return builder.buildScanOperator(context, subScan);
    } catch (UserException e) {
      // Rethrow user exceptions directly
      throw e;
    } catch (Throwable e) {
      // Wrap all others
      throw new ExecutionSetupException(e);
    }
```

### DuplicatedCode
Duplicated code
in `contrib/format-ltsv/src/main/java/org/apache/drill/exec/store/ltsv/LTSVBatchReader.java`
#### Snippet
```java
            if (Strings.isNullOrEmpty(dateFormat)) {
              timestamp = Instant.parse(value);
            } else {
              try {
                SimpleDateFormat simpleDateFormat = new SimpleDateFormat(dateFormat);
                Date parsedDate = simpleDateFormat.parse(value);
                timestamp = Instant.ofEpochMilli(parsedDate.getTime());
              } catch (ParseException e) {
                throw UserException.parseError(e)
                    .message("Cannot parse " + value + " as a timestamp. You can specify a format string in the provided schema to correct this.")
                    .addContext(errorContext)
                    .build(logger);
              }
            }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
        if (payloadMap != null) {
          if (payloadMap.get(tabIdxName) != null) {
            return payloadMap.get(tabIdxName).getRowCount();
          } else {
            // We might not have computed rowcount for the given condition from the tab/index in question.
            // For rowcount it does not matter which index was used to get the rowcount for the given condition.
            // Hence, just use the first one!
            for (String payloadKey : payloadMap.keySet()) {
              if (payloadKey != null && payloadMap.get(payloadKey) != null) {
                return payloadMap.get(payloadKey).getRowCount();
              }
            }
            StatisticsPayload anyPayload = payloadMap.entrySet().iterator().next().getValue();
            return anyPayload.getRowCount();
          }
        }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
          startKey = prefix.getBytes(Charsets.UTF_8);
          stopKey = startKey.clone();
          boolean isMaxVal = true;
          for (int i = stopKey.length - 1; i >= 0; --i) {
            int nextByteValue = (0xff & stopKey[i]) + 1;
            if (nextByteValue < 0xff) {
              stopKey[i] = (byte) nextByteValue;
              isMaxVal = false;
              break;
            } else {
              stopKey[i] = 0;
            }
          }
          if (isMaxVal) {
            stopKey = HConstants.EMPTY_END_ROW;
          }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
    final int numSlots = incomingEndpoints.size();
    Preconditions.checkArgument(numSlots <= regionsToScan.size(),
        String.format("Incoming endpoints %d is greater than number of scan regions %d", numSlots, regionsToScan.size()));

    /*
     * Minimum/Maximum number of assignment per slot
     */
    final int minPerEndpointSlot = (int) Math.floor((double)regionsToScan.size() / numSlots);
    final int maxPerEndpointSlot = (int) Math.ceil((double)regionsToScan.size() / numSlots);

    /*
     * initialize (endpoint index => HBaseSubScanSpec list) map
     */
    endpointFragmentMapping = Maps.newHashMapWithExpectedSize(numSlots);
```

### DuplicatedCode
Duplicated code
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseGroupScan.java`
#### Snippet
```java
    Preconditions.checkArgument(numSlots <= regionsToScan.size(),
        String.format("Incoming endpoints %d is greater than number of scan regions %d", numSlots, regionsToScan.size()));

    /*
     * Minimum/Maximum number of assignment per slot
     */
    final int minPerEndpointSlot = (int) Math.floor((double)regionsToScan.size() / numSlots);
    final int maxPerEndpointSlot = (int) Math.ceil((double)regionsToScan.size() / numSlots);

    /*
     * initialize (endpoint index => HBaseSubScanSpec list) map
     */
    endpointFragmentMapping = Maps.newHashMapWithExpectedSize(numSlots);
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushFilterIntoScan.java`
#### Snippet
```java
    newGroupsScan.setFilterPushedDown(true);

    final ScanPrel newScanPrel = new ScanPrel(scan.getCluster(), filter.getTraitSet(), newGroupsScan, scan.getRowType(), scan.getTable());

    // Depending on whether is a project in the middle, assign either scan or copy of project to childRel.
    final RelNode childRel = project == null ? newScanPrel : project.copy(project.getTraitSet(), ImmutableList.of((RelNode)newScanPrel));

    if (jsonConditionBuilder.isAllExpressionsConverted()) {
        /*
         * Since we could convert the entire filter condition expression into an HBase filter,
         * we can eliminate the filter operator altogether.
         */
      call.transformTo(childRel);
    } else {
      call.transformTo(filter.copy(filter.getTraitSet(), ImmutableList.of(childRel)));
    }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/binary/MapRDBFilterBuilder.java`
#### Snippet
```java
      switch (functionName) {
      case FunctionNames.AND:
      case FunctionNames.OR:
        HBaseScanSpec firstScanSpec = args.get(0).accept(this, null);
        for (int i = 1; i < args.size(); ++i) {
          HBaseScanSpec nextScanSpec = args.get(i).accept(this, null);
          if (firstScanSpec != null && nextScanSpec != null) {
            nodeScanSpec = mergeScanSpecs(functionName, firstScanSpec, nextScanSpec);
          } else {
            allExpressionsConverted = false;
            if (FunctionNames.AND.equals(functionName)) {
              nodeScanSpec = firstScanSpec == null ? nextScanSpec : firstScanSpec;
            }
          }
          firstScanSpec = nodeScanSpec;
        }
        break;
      }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/binary/MapRDBFilterBuilder.java`
#### Snippet
```java
    String functionName = processor.getFunctionName();
    SchemaPath field = processor.getPath();
    byte[] fieldValue = processor.getValue();
    boolean sortOrderAscending = processor.isSortOrderAscending();
    boolean isRowKey = field.getRootSegmentPath().equals(ROW_KEY);
    if (!(isRowKey
        || (!field.getRootSegment().isLastPath()
            && field.getRootSegment().getChild().isLastPath()
            && field.getRootSegment().getChild().isNamed())
           )
        ) {
      /*
       * if the field in this function is neither the row_key nor a qualified HBase column, return.
       */
      return null;
    }

    if (processor.isRowKeyPrefixComparison()) {
      return createRowKeyPrefixScanSpec(call, processor);
    }

    CompareOp compareOp = null;
    boolean isNullTest = false;
    ByteArrayComparable comparator = new BinaryComparator(fieldValue);
    byte[] startRow = HConstants.EMPTY_START_ROW;
    byte[] stopRow = HConstants.EMPTY_END_ROW;
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/binary/MapRDBFilterBuilder.java`
#### Snippet
```java
      if (isRowKey) {
        String prefix = parser.getPrefixString();
        if (prefix != null) { // group 3 is literal
          /*
           * If there is a literal prefix, it can help us prune the scan to a sub range
           */
          if (prefix.equals(parser.getLikeString())) {
            /* The operand value is literal. This turns the LIKE operator to EQUAL operator */
            startRow = stopRow = fieldValue;
            compareOp = null;
          } else {
            startRow = prefix.getBytes(Charsets.UTF_8);
            stopRow = startRow.clone();
            boolean isMaxVal = true;
            for (int i = stopRow.length - 1; i >= 0; --i) {
              int nextByteValue = (0xff & stopRow[i]) + 1;
              if (nextByteValue < 0xff) {
                stopRow[i] = (byte) nextByteValue;
                isMaxVal = false;
                break;
              } else {
                stopRow[i] = 0;
              }
            }
            if (isMaxVal) {
              stopRow = HConstants.EMPTY_END_ROW;
            }
          }
        }
      }
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/binary/MapRDBFilterBuilder.java`
#### Snippet
```java
    if (compareOp != null || startRow != HConstants.EMPTY_START_ROW || stopRow != HConstants.EMPTY_END_ROW) {
      Filter filter = null;
      if (isRowKey) {
        if (compareOp != null) {
          filter = new RowFilter(compareOp, comparator);
        }
      } else {
        byte[] family = HBaseUtils.getBytes(field.getRootSegment().getPath());
        byte[] qualifier = HBaseUtils.getBytes(field.getRootSegment().getChild().getNameSegment().getPath());
        filter = new SingleColumnValueFilter(family, qualifier, compareOp, comparator);
        ((SingleColumnValueFilter)filter).setLatestVersionOnly(true);
        if (!isNullTest) {
          ((SingleColumnValueFilter)filter).setFilterIfMissing(true);
        }
      }
      return new HBaseScanSpec(groupScan.getTableName(), startRow, stopRow, filter);
    }
    // else
    return null;
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/binary/MapRDBFilterBuilder.java`
#### Snippet
```java
    byte[] startRow = processor.getRowKeyPrefixStartRow();
    byte[] stopRow  = processor.getRowKeyPrefixStopRow();
    Filter filter   = processor.getRowKeyPrefixFilter();

    if (startRow != HConstants.EMPTY_START_ROW ||
      stopRow != HConstants.EMPTY_END_ROW ||
      filter != null) {
      return new HBaseScanSpec(groupScan.getTableName(), startRow, stopRow, filter);
    }

    // else
    return null;
```

### DuplicatedCode
Duplicated code
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonScanSpec.java`
#### Snippet
```java
    if (this.condition != null) {
      ByteBuffer bbuf = ((ConditionImpl)this.condition).getDescriptor().getSerialized();
      byte[] serFilter = new byte[bbuf.limit() - bbuf.position()];
      bbuf.get(serFilter);
      return serFilter;
    }
    return null;
```

### DuplicatedCode
Duplicated code
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/Murmur128.java`
#### Snippet
```java
      k1 *= c1;
      k1 = Long.rotateLeft(k1, 31);
      k1 *= c2;
      h1 ^= k1;
      h1 = Long.rotateLeft(h1, 27);
      h1 += h2;
      h1 = h1 * 5 + 0x52dce729;
      k2 *= c2;
      k2 = Long.rotateLeft(k2, 33);
      k2 *= c1;
      h2 ^= k2;
      h2 = Long.rotateLeft(h2, 31);
      h2 += h1;
      h2 = h2 * 5 + 0x38495ab5;
```

### DuplicatedCode
Duplicated code
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/Murmur128.java`
#### Snippet
```java
    h1 ^= length;
    h2 ^= length;

    h1 += h2;
    h2 += h1;

    h1 = fmix64(h1);
    h2 = fmix64(h2);

    h1 += h2;
    h2 += h1;
```

### DuplicatedCode
Duplicated code
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/TcpSession.java`
#### Snippet
```java
    for (int i = 0; i < packetsFromSender.size(); i++) {
      // Get the packet;
      Packet p = packetsFromSender.get(i);
      dataFromPacket = p.getData();
      if (dataFromPacket != null) {
        for (int j = 0; j < dataFromPacket.length; j++) {
          sentData[dataOffset] = dataFromPacket[j];
          dataOffset++;
        }
      }
    }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-cassandra/src/main/java/org/apache/drill/exec/store/cassandra/CassandraStorageConfig.java`
#### Snippet
```java
    Map<String, Object> result = new HashMap<>();
    result.put("host", host);
    result.put("port", port);
    if (credentials.isPresent()) {
      result.put("username", credentials.get().getUsername());
      result.put("password", credentials.get().getPassword());
    }
    return result;
```

### DuplicatedCode
Duplicated code
in `contrib/storage-cassandra/src/main/java/org/apache/drill/exec/store/cassandra/CassandraStoragePlugin.java`
#### Snippet
```java
    if (config.getAuthMode() == AuthMode.USER_TRANSLATION) {
      Optional<UsernamePasswordCredentials> userCreds = config.getUsernamePasswordCredentials(schemaConfig.getUserName());
      if (! userCreds.isPresent()) {
        logger.debug(
            "No schemas will be registered in {} for query user {}.",
            getName(), schemaConfig.getUserName()
        );
        return;
      }
    }
    schemaFactory.registerSchemas(schemaConfig, parent);
```

### DuplicatedCode
Duplicated code
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidCompareFunctionProcessor.java`
#### Snippet
```java
String encodingType = e.getEncodingType();
      switch (encodingType) {
        case "INT_BE":
        case "INT":
        case "UINT_BE":
        case "UINT":
        case "UINT4_BE":
        case "UINT4":
          if (valueArg instanceof IntExpression
            && (isEqualityFn || encodingType.startsWith("U"))) {
            this.value = ((IntExpression) valueArg).getInt();
          }
          break;
        case "BIGINT_BE":
        case "BIGINT":
        case "UINT8_BE":
        case "UINT8":
          if (valueArg instanceof LongExpression
            && (isEqualityFn || encodingType.startsWith("U"))) {
            this.value = ((LongExpression) valueArg).getLong();
          }
          break;
        case "FLOAT":
          if (valueArg instanceof FloatExpression && isEqualityFn) {
            this.value = ((FloatExpression) valueArg).getFloat();
          }
          break;
        case "DOUBLE":
          if (valueArg instanceof DoubleExpression && isEqualityFn) {
            this.value = ((DoubleExpression) valueArg).getDouble();
          }
          break;
        case "TIME_EPOCH":
        case "TIME_EPOCH_BE":
          if (valueArg instanceof TimeExpression) {
            this.value = ((TimeExpression) valueArg).getTime();
          }
          break;
        case "DATE_EPOCH":
        case "DATE_EPOCH_BE":
          if (valueArg instanceof DateExpression) {
            this.value = ((DateExpression) valueArg).getDate();
          }
          break;
        case "BOOLEAN_BYTE":
          if (valueArg instanceof BooleanExpression) {
            this.value = ((BooleanExpression) valueArg).getBoolean();
          }
          break;
        case "UTF8":
          // let visitSchemaPath() handle this.
          return e.getInput().accept(this, valueArg);
      }

      if (value != null) {
        this.path = (SchemaPath) e.getInput();
        return true;
      }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidCompareFunctionProcessor.java`
#### Snippet
```java
    if (valueArg instanceof QuotedString) {
      this.value = ((QuotedString) valueArg).value;
      this.path = path;
      return true;
    }

    if (valueArg instanceof IntExpression) {
      this.value = ((IntExpression) valueArg).getInt();
      this.path = path;
      return true;
    }

    if (valueArg instanceof LongExpression) {
      this.value = ((LongExpression) valueArg).getLong();
      this.path = path;
      return true;
    }

    if (valueArg instanceof FloatExpression) {
      this.value = ((FloatExpression) valueArg).getFloat();
      this.path = path;
      return true;
    }

    if (valueArg instanceof DoubleExpression) {
      this.value = ((DoubleExpression) valueArg).getDouble();
      this.path = path;
      return true;
    }

    if (valueArg instanceof BooleanExpression) {
      this.value = ((BooleanExpression) valueArg).getBoolean();
      this.path = path;
      return true;
    }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/ElasticsearchStorageConfig.java`
#### Snippet
```java
    ImmutableMap.Builder<String, Object> builder = ImmutableMap.builder();
    builder.put(HOSTS, OBJECT_WRITER.writeValueAsString(hosts));
    builder.put(PATH_PREFIX, pathPrefix != null ? pathPrefix : EMPTY_STRING);
    builder.put(USERNAME, credentials.getOrDefault(USERNAME, EMPTY_STRING));
    builder.put(PASSWORD, credentials.getOrDefault(PASSWORD, EMPTY_STRING));
    builder.put(DISABLE_SSL_VERIFICATION, Boolean.valueOf(disableSSLVerification).toString());

    credentials.remove(USERNAME);
    credentials.remove(PASSWORD);
    builder.putAll(credentials);
    return builder.build();
```

### DuplicatedCode
Duplicated code
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/GoogleSheetsGroupScan.java`
#### Snippet
```java
    if (hasFilters()) {
      estRowCount *= filterSelectivity;
    }

    double estColCount = Utilities.isStarQuery(columns) ? DrillScanRel.STAR_COLUMN_COST : columns.size();
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

    // Force the caller to use our costs rather than the
    // defaults (which sets IO cost to zero).
    return new ScanStats(ScanStats.GroupScanProperty.ESTIMATED_TOTAL_COST,
      estRowCount, cpuCost, ioCost);
```

### DuplicatedCode
Duplicated code
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkGroupScan.java`
#### Snippet
```java
    double estColCount = Utilities.isStarQuery(columns) ? DrillScanRel.STAR_COLUMN_COST : columns.size();
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

    // Force the caller to use our costs rather than the
    // defaults (which sets IO cost to zero).
    return new ScanStats(ScanStats.GroupScanProperty.ESTIMATED_TOTAL_COST,
      estRowCount, cpuCost, ioCost);
```

### DuplicatedCode
Duplicated code
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/GoogleSheetsPushDownListener.java`
#### Snippet
```java
      switch (type) {
        case BIGINT:
        case BIT:
        case FLOAT4:
        case FLOAT8:
        case INT:
        case SMALLINT:
        case VARCHAR:
        case VARDECIMAL:
          return true;
        default:
          return false;
      }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/GoogleSheetsPushDownListener.java`
#### Snippet
```java
      Map<String, ExprNode.ColRelOpConstNode> filters = new HashMap<>();
      double selectivity = 1;
      for (ExprNode expr : andNode.children) {
        ColRelOpConstNode relOp = (ColRelOpConstNode) expr;
        filters.put(filterParams.get(relOp.colName), relOp);
        selectivity *= relOp.op.selectivity();
      }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFunctionRegistry.java`
#### Snippet
```java
        for (int j = 0; j < opBinding.getOperandCount(); ++j) {
          RelDataType operandType = opBinding.getOperandType(j);
          operandTypes.append(operandType.getSqlTypeName());
          if (operandType.isNullable()) {
            operandTypes.append(":OPTIONAL");
          }
          if (j < opBinding.getOperandCount() - 1) {
            operandTypes.append(",");
          }
        }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpBatchReader.java`
#### Snippet
```java
    final ProxyBuilder builder = HttpProxyConfig.builder()
        .fromConfigForURL(drillConfig, url.toString());
    final String proxyType = config.proxyType();
    if (proxyType != null && !"direct".equals(proxyType)) {
      builder
        .type(config.proxyType())
        .host(config.proxyHost())
        .port(config.proxyPort());

      Optional<UsernamePasswordWithProxyCredentials> credentials = config.getUsernamePasswordCredentials();
      if (credentials.isPresent()) {
        builder.username(credentials.get().getUsername()).password(credentials.get().getPassword());
      }
    }
    return builder.build();
```

### DuplicatedCode
Duplicated code
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpCSVBatchReader.java`
#### Snippet
```java
    CustomErrorContext errorContext = new ChildErrorContext(negotiator.parentErrorContext()) {
      @Override
      public void addContext(UserException.Builder builder) {
        super.addContext(builder);
        builder.addContext("URL", url.toString());
      }
    };
    negotiator.setErrorContext(errorContext);

    // Http client setup
    SimpleHttp http = SimpleHttp.builder()
      .scanDefn(subScan)
      .url(url)
      .tempDir(new File(tempDirPath))
      .paginator(paginator)
      .proxyConfig(proxySettings(negotiator.drillConfig(), url))
      .errorContext(errorContext)
      .build();

    // CSV loader setup
    inStream = http.getInputStream();
```

### DuplicatedCode
Duplicated code
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpScanBatchCreator.java`
#### Snippet
```java
    Preconditions.checkArgument(children.isEmpty());
    try {
      ScanFrameworkBuilder builder = createBuilder(context.getOptions(), subScan);
      return builder.buildScanOperator(context, subScan);
    } catch (UserException e) {
      // Rethrow user exceptions directly
      throw e;
    } catch (Throwable e) {
      // Wrap all others
      throw new ExecutionSetupException(e);
    }
```

### DuplicatedCode
Duplicated code
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenAuthenticator.java`
#### Snippet
```java
    String tokenType = accessTokenRepository.getTokenType();
    if (StringUtils.isNotEmpty(tokenType)) {
      accessToken = tokenType + " " + accessToken;
    }

    if (accessTokenRepository.getOAuthConfig().isAccessTokenInHeader()) {
      HttpUrl rawUrl = HttpUrl.parse(request.url().toString());
      rawUrl.newBuilder().addQueryParameter("access_token", accessToken);
      return request.newBuilder().url(rawUrl.url()).build();
    } else {
      return request.newBuilder().header("Authorization", accessToken).build();
    }
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ComplexSchemaUtils.java`
#### Snippet
```java
      byte[] rowStringBytes = dataType.getBytes(java.nio.charset.StandardCharsets.UTF_8);
      buffer = buffer.reallocIfNeeded(rowStringBytes.length);
      buffer.setBytes(0, rowStringBytes);

      rowHolder.start = 0;
      rowHolder.end = rowStringBytes.length;
      rowHolder.buffer = buffer;

      queryMapWriter.varChar(fieldName).write(rowHolder);
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/CryptoFunctions.java`
#### Snippet
```java
      String key = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(rawKey.start, rawKey.end, rawKey.buffer);
      String input = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(rawInput.start, rawInput.end, rawInput.buffer);
      String encryptedText = "";
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/CryptoFunctions.java`
#### Snippet
```java
        byte[] keyByteArray = key.getBytes(java.nio.charset.StandardCharsets.UTF_8);
        java.security.MessageDigest sha = java.security.MessageDigest.getInstance("SHA-1");
        keyByteArray = sha.digest(keyByteArray);
        keyByteArray = java.util.Arrays.copyOf(keyByteArray, 16);
        javax.crypto.spec.SecretKeySpec secretKey = new javax.crypto.spec.SecretKeySpec(keyByteArray, "AES");

        javax.crypto.Cipher cipher = javax.crypto.Cipher.getInstance("AES/ECB/PKCS5Padding");
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSFunctions.java`
#### Snippet
```java
      if (rawDomainName.isSet == 0) {
        org.apache.drill.exec.vector.complex.writer.BaseWriter.ListWriter listWriter = out.rootAsList();
        org.apache.drill.exec.vector.complex.writer.BaseWriter.MapWriter rowMapWriter = listWriter.map();
        listWriter.startList();
        rowMapWriter.start();
        rowMapWriter.end();
        listWriter.endList();
        return;
      }
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DNSFunctions.java`
#### Snippet
```java
      } catch (Exception e) {
        org.apache.drill.exec.vector.complex.writer.BaseWriter.ListWriter listWriter = out.rootAsList();
        org.apache.drill.exec.vector.complex.writer.BaseWriter.MapWriter rowMapWriter = listWriter.map();
        listWriter.startList();
        rowMapWriter.start();
        rowMapWriter.end();
        listWriter.endList();
      }
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DistributionFunctions.java`
#### Snippet
```java
      recordCount.value = 0;
      sum_y.value = 0;
      sum_x.value = 0;
      avg_x.value = 0;
      avg_y.value = 0;
      diff_x.value = 0;
      diff_y.value = 0;
      ss_x.value = 0;
      ss_xy.value = 0;
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DistributionFunctions.java`
#### Snippet
```java
      recordCount.value += 1;
      sum_x.value += xInput.value;
      avg_x.value = sum_x.value / recordCount.value;
      diff_x.value = avg_x.value - xInput.value;
      ss_x.value = (diff_x.value * diff_x.value) + ss_x.value;

      // Now compute the sum of squares for the y
      sum_y.value = sum_y.value + yInput.value;
      avg_y.value = sum_y.value / recordCount.value;
      diff_y.value = avg_y.value - yInput.value;

      ss_xy.value = (diff_x.value * diff_y.value) + ss_xy.value;
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/NetworkFunctions.java`
#### Snippet
```java
      String ipString = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(inputTextA.start, inputTextA.end, inputTextA.buffer);
      org.apache.commons.validator.routines.InetAddressValidator validator = org.apache.commons.validator.routines.InetAddressValidator.getInstance();
      if (!validator.isValidInet4Address(ipString)) {
        return;
      }

      String[] ipAddressInArray = ipString.split("\\.");
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/StringDistanceFunctions.java`
#### Snippet
```java
      String input1 = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(rawInput1.start, rawInput1.end, rawInput1.buffer);
      String input2 = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(rawInput2.start, rawInput2.end, rawInput2.buffer);


      out.value = d.apply(input1, input2);
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java
      java.util.Set<Character> chars = new java.util.HashSet();

      for (char ch : input.toCharArray()) {
        chars.add(ch);
      }

      java.util.Map<Character, Double> probabilities = new java.util.HashMap();
      int length = input.length();

      // Get the probabilities
      for (Character character : chars) {
        double charCount = org.apache.commons.lang3.StringUtils.countMatches(input, character);
        double probability = charCount / length;
        probabilities.put(character, probability);
      }

      // Now get the entropy
      double entropy = 0.0;
      for (Double probability : probabilities.values()) {
        entropy += (probability * java.lang.Math.log(probability) / java.lang.Math.log(2.0));
      }
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/UserAgentFunctions.java`
#### Snippet
```java
        String field = agent.getValue(fieldName);

        byte[] rowStringBytes = field.getBytes(java.nio.charset.StandardCharsets.UTF_8);
        outBuffer = outBuffer.reallocIfNeeded(rowStringBytes.length);
        outBuffer.setBytes(0, rowStringBytes);

        rowHolder.start = 0;
        rowHolder.end = rowStringBytes.length;
        rowHolder.buffer = outBuffer;
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STAsGeoJSON.java`
#### Snippet
```java
    byte[] geoJsonBytes = geoJson.getBytes(java.nio.charset.StandardCharsets.UTF_8);
    int outputSize = geoJsonBytes.length;

    buffer = out.buffer = buffer.reallocIfNeeded(outputSize);
    out.start = 0;
    out.end = outputSize;
    buffer.setBytes(0, geoJsonBytes);
```

### DuplicatedCode
Duplicated code
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STContains.java`
#### Snippet
```java
    com.esri.core.geometry.ogc.OGCGeometry geom1;
    com.esri.core.geometry.ogc.OGCGeometry geom2;

    geom1 = com.esri.core.geometry.ogc.OGCGeometry
        .fromBinary(geom1Param.buffer.nioBuffer(geom1Param.start, geom1Param.end - geom1Param.start));
    geom2 = com.esri.core.geometry.ogc.OGCGeometry
        .fromBinary(geom2Param.buffer.nioBuffer(geom2Param.start, geom2Param.end - geom2Param.start));
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AuthDynamicFeature.java`
#### Snippet
```java
    AnnotatedMethod am = new AnnotatedMethod(resourceInfo.getResourceMethod());

    // RolesAllowed on the method takes precedence over PermitAll
    RolesAllowed ra = am.getAnnotation(RolesAllowed.class);
    if (ra != null) {
      configuration.register(AuthCheckFilter.INSTANCE);
      return;
    }

    // PermitAll takes precedence over RolesAllowed on the class
    if (am.isAnnotationPresent(PermitAll.class)) {
      // Do nothing.
      return;
    }

    // RolesAllowed on the class takes precedence over PermitAll
    ra = resourceInfo.getResourceClass().getAnnotation(RolesAllowed.class);
    if (ra != null) {
      configuration.register(AuthCheckFilter.INSTANCE);
    }
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/CsrfTokenValidateFilter.java`
#### Snippet
```java
    HttpServletRequest httpRequest = (HttpServletRequest) request;
    HttpServletResponse httpResponse = (HttpServletResponse) response;
    if (HttpMethod.POST.equalsIgnoreCase(httpRequest.getMethod())) {
      String csrfTokenSession = WebUtils.getCsrfTokenFromHttpRequest(httpRequest);
      String csrfTokenRequest = httpRequest.getParameter(WebServerConstants.CSRF_TOKEN);
      if (csrfTokenSession != null && !csrfTokenSession.equals(csrfTokenRequest)) {
        httpResponse.reset();
        httpResponse.sendError(HttpServletResponse.SC_FORBIDDEN, "Potential CSRF detected");
        return;
      }
    }
    chain.doFilter(request, response);
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java
        final HttpSession session = se.getSession();
        if (session == null) {
          return;
        }

        final Object authCreds = session
            .getAttribute(SessionAuthentication.__J_AUTHENTICATED);
        if (authCreds != null) {
          final SessionAuthentication sessionAuth = (SessionAuthentication) authCreds;
          securityHandler.logout(sessionAuth);
          session.removeAttribute(SessionAuthentication.__J_AUTHENTICATED);
        }
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java
    final Date notBefore = now.minusMinutes(1).toDate();
    final Date notAfter = now.plusYears(5).toDate();
    final BigInteger serialNumber = new BigInteger(128, random);

    // Create a certificate valid for 5years from now.
    final X509v3CertificateBuilder certificateBuilder = new JcaX509v3CertificateBuilder(
        nameBuilder.build(), // attributes
        serialNumber, notBefore, notAfter, nameBuilder.build(),
        keyPair.getPublic());

    // Sign the certificate using the private key
    final ContentSigner contentSigner = new JcaContentSignerBuilder(
        "SHA256WithRSAEncryption").build(keyPair.getPrivate());
    final X509Certificate certificate = new JcaX509CertificateConverter()
        .getCertificate(certificateBuilder.build(contentSigner));

    // Check the validity
    certificate.checkValidity(now.toDate());

    // Make sure the certificate is self-signed.
    certificate.verify(certificate.getPublicKey());

    // Generate a random password for keystore protection
    final String keyStorePasswd = RandomStringUtils.random(20);
    final KeyStore keyStore = KeyStore.getInstance("JKS");
    keyStore.load(null, null);
    keyStore.setKeyEntry("DrillAutoGeneratedCert", keyPair.getPrivate(),
        keyStorePasswd.toCharArray(),
        new java.security.cert.Certificate[] { certificate });

    sslContextFactory.setKeyStore(keyStore);
    sslContextFactory.setKeyStorePassword(keyStorePasswd);
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CleanCommand.java`
#### Snippet
```java
    try {
      System.out.print("Connecting to DFS...");
      dfs = new DfsFacade(config);
      dfs.connect();
      System.out.println(" Connected.");
    } catch (DfsFacadeException e) {
      System.out.println("Failed.");
      throw new ClientException("Failed to connect to DFS", e);
    }
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinator.java`
#### Snippet
```java
    logger.debug("Starting ZKClusterCoordination.");
    discovery.start();

    if (millisToWait != 0) {
      boolean success = this.initialConnection.await(millisToWait,
          TimeUnit.MILLISECONDS);
      if (!success) {
        throw new IOException(String.format(
            "Failure to connect to the zookeeper cluster service within the allotted time of %d milliseconds.",
            millisToWait));
      }
    } else {
      this.initialConnection.await();
    }

    serviceCache = discovery.serviceCacheBuilder().name(serviceName).build();
    serviceCache.addListener(new EndpointListener());
    serviceCache.start();
    updateEndpoints();
```

### DuplicatedCode
Duplicated code
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinator.java`
#### Snippet
```java
    if (!(handle instanceof ZKRegistrationHandle)) {
      throw new UnsupportedOperationException(
          "Unknown handle type: " + handle.getClass().getName());
    }

    // when Drillbit is unregistered, clean all the listeners registered in CC.
    this.listeners.clear();

    ZKRegistrationHandle h = (ZKRegistrationHandle) handle;
    try {
      ServiceInstance<DrillbitEndpoint> serviceInstance = ServiceInstance
          .<DrillbitEndpoint> builder().address("").port(0).id(h.id)
          .name(serviceName).build();
      discovery.unregisterService(serviceInstance);
    } catch (Exception e) {
      Throwables.throwIfUnchecked(e);
      throw new RuntimeException(e);
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProviderFactory.java`
#### Snippet
```java
        if (!config.hasPath(ZK_ACL_PROVIDER)) {
            throw new DrillbitStartupException(String.format("BOOT option '%s' is missing in config.", ZK_ACL_PROVIDER));
        }

        final String aclProviderName = config.getString(ZK_ACL_PROVIDER);

        if (Strings.isNullOrEmpty(aclProviderName)) {
            throw new DrillbitStartupException(String.format("Invalid value '%s' for BOOT option '%s'", aclProviderName,
                    ZK_ACL_PROVIDER));
        }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZkEphemeralStore.java`
#### Snippet
```java
    return Iterators.transform(getClient().entries(), new Function<Map.Entry<String, byte[]>, Map.Entry<String, V>>() {
      @Nullable
      @Override
      public Map.Entry<String, V> apply(@Nullable Map.Entry<String, byte[]> input) {
        try {
          final V value = config.getSerializer().deserialize(input.getValue());
          return new ImmutableEntry<>(input.getKey(), value);
        } catch (final IOException e) {
          throw new DrillRuntimeException(String.format("unable to deserialize value at key %s", input.getKey()), e);
        }
      }
    });
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
      String convertFunctionName = e.getConvertFunction() + e.getEncodingType();

      List<LogicalExpression> newArgs = new ArrayList<>();
      newArgs.add(e.getInput()); // input_expr

      FunctionCall fc = new FunctionCall(convertFunctionName, newArgs, e.getPosition());
      return fc.accept(this, value);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
        assert (e != null);

        JConditional notSetJC = eval._if(e.eq(JExpr.lit(0)));
        notSetJC._then().assign(out.getIsSet(), JExpr.lit(0));

        JBlock setBlock = notSetJC._else().block();
        setBlock.assign(out.getIsSet(), JExpr.lit(1));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
    FunctionCall funcCall = new FunctionCall(funcName, args, ExpressionPosition.UNKNOWN);
    FunctionResolver resolver = FunctionResolverFactory.getResolver(funcCall);

    DrillFuncHolder matchedConvertToNullableFuncHolder = functionLookupContext.findDrillFunction(resolver, funcCall);
    if (matchedConvertToNullableFuncHolder == null) {
      logFunctionResolutionError(errorCollector, funcCall);
      return NullExpression.INSTANCE;
    }

    return matchedConvertToNullableFuncHolder.getExpr(funcName, args, ExpressionPosition.UNKNOWN);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/GetSetVectorHelper.java`
#### Snippet
```java
            eval.assign(out.getHolder().ref("buffer"), vector.invoke("getBuffer"));
            JVar se = eval.decl(model.LONG, "startEnd", getValueAccessor.invoke("getStartEnd").arg(indexVariable));
            eval.assign(out.getHolder().ref("start"), JExpr.cast(model._ref(int.class), se));
            eval.assign(out.getHolder().ref("end"), JExpr.cast(model._ref(int.class), se.shr(JExpr.lit(32))));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillAggFuncHolder.java`
#### Snippet
```java
    HoldingContainer out = null;
    JVar internalOutput = null;
    if (getReturnType().getMinorType() != TypeProtos.MinorType.LATE) {
      out = classGenerator.declare(getReturnType(), false);
    }
    JBlock sub = new JBlock();
    if (getReturnType().getMinorType() != TypeProtos.MinorType.LATE) {
      internalOutput = sub.decl(JMod.FINAL, classGenerator.getHolderType(getReturnType()), getReturnValue().getName(), JExpr._new(classGenerator.getHolderType(getReturnType())));
    }
    classGenerator.getEvalBlock().add(sub);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/DateTypeFunctions.java`
#### Snippet
```java
      out.days = (int) inputDays.value;
      out.milliseconds = (int) ((inputHours.value * org.apache.drill.exec.vector.DateUtilities.hoursToMillis)
          + (inputMinutes.value * org.apache.drill.exec.vector.DateUtilities.minutesToMillis)
          + (inputSeconds.value * org.apache.drill.exec.vector.DateUtilities.secondsToMillis)
          + (inputMilliSeconds.value));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MathFunctions.java`
#### Snippet
```java
      byte[] buf1 = new byte[left.end - left.start];
      left.buffer.getBytes(left.start, buf1, 0, left.end - left.start);
      String input = new String(buf1);
      try {
        out.value = inputFormat.parse(input).doubleValue();
      }  catch (java.text.ParseException e) {
         throw new UnsupportedOperationException("Cannot parse input: " + input + " with pattern : " + inputFormat.toPattern());
      }

      // Round the value
      java.math.BigDecimal roundedValue = new java.math.BigDecimal(out.value);
      out.value = (roundedValue.setScale(decimalDigits, java.math.BigDecimal.ROUND_HALF_UP)).doubleValue();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MathFunctions.java`
#### Snippet
```java
      out.isSet = 1;

      byte[] buf1 = new byte[left.end - left.start];
      left.buffer.getBytes(left.start, buf1, 0, left.end - left.start);
      String input = new String(buf1);
      try {
        out.value = inputFormat.parse(input).doubleValue();
      } catch (java.text.ParseException e) {
        throw new UnsupportedOperationException("Cannot parse input: " + input + " with pattern : " + inputFormat.toPattern());
      }

      // Round the value
      java.math.BigDecimal roundedValue = new java.math.BigDecimal(out.value);
      out.value = (roundedValue.setScale(decimalDigits, java.math.BigDecimal.ROUND_HALF_UP)).doubleValue();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseQueryFunction.java`
#### Snippet
```java
      int questionMarkIndex = queryString.indexOf("?");
      if (questionMarkIndex > -1) {
        queryString = queryString.substring(questionMarkIndex + 1);
      }

      if (queryString.trim().isEmpty() || queryString.equalsIgnoreCase("null")) {
        queryString = "";
      }

      if (!queryString.isEmpty()) {
        char firstLetter = queryString.charAt(0);

        // If the first character is a &, it doesn't split properly.
        // This checks if the first character is an & and if so, removes it.
        if (firstLetter == '&') {
          queryString = queryString.substring(1);
        }
      }

      String[] queryParameters = queryString.split("&");
      mapWriter.start();
      for (String parameter : queryParameters) {
        String[] keyValue = parameter.split("=", 2);
        if (keyValue.length != 2) {
          // Ignore malformed key-value pair
          continue;
        }

        byte[] valueBytes = keyValue[1].getBytes(java.nio.charset.StandardCharsets.UTF_8);
        outBuffer = outBuffer.reallocIfNeeded(valueBytes.length);
        outBuffer.setBytes(0, valueBytes);

        org.apache.drill.exec.expr.holders.VarCharHolder valueHolder =
            new org.apache.drill.exec.expr.holders.VarCharHolder();
        valueHolder.start = 0;
        valueHolder.end = valueBytes.length;
        valueHolder.buffer = outBuffer;

        mapWriter.varChar(keyValue[0]).write(valueHolder);
      }
      mapWriter.end();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseQueryFunction.java`
#### Snippet
```java
      org.apache.drill.exec.vector.complex.writer.BaseWriter.MapWriter mapWriter = outWriter.rootAsMap();

      if (in.isSet == 0) {
        // Return empty map
        mapWriter.start();
        mapWriter.end();
        return;
      }

      String queryString =
            org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(in.start, in.end, in.buffer);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseUrlFunction.java`
#### Snippet
```java
try {
        mapWriter.start();

        java.net.URL aURL = new java.net.URL(urlString);
        // Diamond operator can not be used here because Janino does not support
        // type inference for generic instance creation.
        // LinkedHashMap is used to preserve insertion-order.
        java.util.Map<String, String> urlComponents = new java.util.LinkedHashMap<String, String>();
        urlComponents.put("protocol", aURL.getProtocol());
        urlComponents.put("authority", aURL.getAuthority());
        urlComponents.put("host", aURL.getHost());
        urlComponents.put("path", aURL.getPath());
        urlComponents.put("query", aURL.getQuery());
        urlComponents.put("filename", aURL.getFile());
        urlComponents.put("ref", aURL.getRef());

        org.apache.drill.exec.expr.holders.VarCharHolder rowHolder =
            new org.apache.drill.exec.expr.holders.VarCharHolder();
        for (java.util.Map.Entry<String, String> entry : urlComponents.entrySet()) {
          if (entry.getValue() != null) {
            // Explicit casting to String is required because of Janino's limitations regarding generics.
            byte[] protocolBytes = ((String) entry.getValue()).getBytes(java.nio.charset.StandardCharsets.UTF_8);
            outBuffer = outBuffer.reallocIfNeeded(protocolBytes.length);
            outBuffer.setBytes(0, protocolBytes);
            rowHolder.start = 0;
            rowHolder.end = protocolBytes.length;
            rowHolder.buffer = outBuffer;
            mapWriter.varChar((String) entry.getKey()).write(rowHolder);
          }
        }

        java.lang.Integer port = aURL.getPort();
        // If port number is not specified in URL string, it is assigned a default value of -1.
        // Include port number into resulting map only if it was specified.
        if (port != -1) {
          org.apache.drill.exec.expr.holders.IntHolder intHolder = new org.apache.drill.exec.expr.holders.IntHolder();
          intHolder.value = port;
          mapWriter.integer("port").write(intHolder);
        }

        mapWriter.end();
      } catch (Exception e) {
        // Enclose map
        mapWriter.end();
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
      if (schemaHolder.obj == null) {
        schemaHolder.obj = currentSchema;
        return;
      }

      org.apache.drill.exec.record.metadata.TupleMetadata resolvedSchema =
          (org.apache.drill.exec.record.metadata.TupleMetadata) schemaHolder.obj;

      if (!resolvedSchema.isEquivalent(currentSchema)) {
        schemaHolder.obj = org.apache.drill.exec.physical.impl.scan.v3.FixedReceiver.Builder
          .mergeSchemas(currentSchema, resolvedSchema);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
      org.apache.drill.exec.record.metadata.TupleMetadata resolvedSchema =
          (org.apache.drill.exec.record.metadata.TupleMetadata) schemaHolder.obj;

      byte[] type = resolvedSchema.jsonString().getBytes(java.nio.charset.StandardCharsets.UTF_8);
      buf = buf.reallocIfNeeded(type.length);
      buf.setBytes(0, type);
      out.buffer = buf;
      out.start = 0;
      out.end = type.length;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SimpleRepeatedFunctions.java`
#### Snippet
```java
      matcher = java.util.regex.Pattern.compile(
          org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(targetValue.start,  targetValue.end,  targetValue.buffer)).matcher("");
      charSequenceWrapper = new org.apache.drill.exec.expr.fn.impl.CharSequenceWrapper();
      matcher.reset(charSequenceWrapper);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StreamingMedianHelpers.java`
#### Snippet
```java
      if (!minHeap.isEmpty() && n < minHeap.peek()) {
        maxHeap.offer(n);
        if (maxHeap.size() > minHeap.size() + 1) {
          minHeap.offer(maxHeap.poll());
        }
      } else {
        minHeap.offer(n);
        if (minHeap.size() > maxHeap.size() + 1) {
          maxHeap.offer(minHeap.poll());
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionUtil.java`
#### Snippet
```java
    if (firstByte >= 0) { // 1-byte char. First byte is 0xxxxxxx.
      return 1;
    } else if ((firstByte & 0xE0) == 0xC0) { // 2-byte char. First byte is 110xxxxx
      return 2;
    } else if ((firstByte & 0xF0) == 0xE0) { // 3-byte char. First byte is 1110xxxx
      return 3;
    } else if ((firstByte & 0xF8) == 0xF0) { //4-byte char. First byte is 11110xxx
      return 4;
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        out.isSet = 1;
        out.buffer = input.buffer;
        out.start = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharPosition(input.buffer, input.start, input.end, matcher.start());
        out.end = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharPosition(input.buffer, input.start, input.end, matcher.end());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      final long theLength = length.value;
      final int lengthNeeded = (int) (theLength <= 0 ? 0 : theLength * 2);
      buffer = buffer.reallocIfNeeded(lengthNeeded);
      byte currentByte = 0;
      int id = 0;
      //get the char length of text.
      int textCharCount = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharLength(text.buffer, text.start, text.end);

      //get the char length of fill.
      int fillCharCount = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharLength(fill.buffer, fill.start, fill.end);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        while (count < theLength - textCharCount) {
          for (id = fill.start; id < fill.end; id++) {
            if (count == theLength - textCharCount) {
              break;
            }

            currentByte = fill.buffer.getByte(id);
            if (currentByte < 0x128  ||           // 1-byte char. First byte is 0xxxxxxx.
                (currentByte & 0xE0) == 0xC0 ||   // 2-byte char. First byte is 110xxxxx
                (currentByte & 0xF0) == 0xE0 ||   // 3-byte char. First byte is 1110xxxx
                (currentByte & 0xF8) == 0xF0) {   //4-byte char. First byte is 11110xxx
              count++;  //Advance the counter, since we find one char.
            }
            out.buffer.setByte(out.end++, currentByte);
          }
        } // end of while
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      final long theLength = length.value;
      final int lengthNeeded = (int) (theLength <= 0 ? 0 : theLength * 2);
      buffer = buffer.reallocIfNeeded(lengthNeeded);
      //get the char length of text.
      int textCharCount = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.getUTF8CharLength(text.buffer, text.start, text.end);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      int bytePerChar = 0;
      //Scan from left of "text", stop until find a char not in "from"
      for (int id = text.start; id < text.end; id += bytePerChar) {
        bytePerChar = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.utf8CharLen(text.buffer, id);
        int pos = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.stringLeftMatchUTF8(from.buffer, from.start, from.end,
                                                                                            text.buffer, id, id + bytePerChar);
        if (pos < 0) { // Found the 1st char not in "from", stop
          out.start = id;
          break;
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      for (int id = text.start; id < text.end; id += bytePerChar) {
        bytePerChar = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.utf8CharLen(text.buffer, id);
        int pos = org.apache.drill.exec.expr.fn.impl.StringFunctionUtil.stringLeftMatchUTF8(from.buffer, from.start, from.end,
                                                                                            text.buffer, id, id + bytePerChar);
        if (pos < 0) { // Found the 1st char not in "from", stop
          out.start = id;
          break;
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      for (int id = text.end - 1; id >= text.start; --id) {
        while ((text.buffer.getByte(id) & 0xC0) == 0x80 && id >= text.start) {
          id--;
        }
        if (text.buffer.getByte(id) != spaceInByte) { // Found the 1st char not in " ", stop
          out.end = id + 1;
          break;
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      out.buffer = buffer = buffer.reallocIfNeeded((left.end - left.start) + (right.end - right.start));
      out.start = out.end = 0;

      for (int id = left.start; id < left.end; id++) {
        out.buffer.setByte(out.end++, left.buffer.getByte(id));
      }

      for (int id = right.start; id < right.end; id++) {
        out.buffer.setByte(out.end++, right.buffer.getByte(id));
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
      if (right.isSet == 1) {
        out.buffer = buffer = buffer.reallocIfNeeded((left.end - left.start) + (right.end - right.start));
        out.start = out.end = 0;

        for (int id = left.start; id < left.end; id++) {
          out.buffer.setByte(out.end++, left.buffer.getByte(id));
        }

        for (int id = right.start; id < right.end; id++) {
          out.buffer.setByte(out.end++, right.buffer.getByte(id));
        }
      } else {
        out.buffer = left.buffer;
        out.start = left.start;
        out.end = left.end;
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
      if (work.obj != null) {
        com.tdunning.math.stats.MergingDigest tdigest = (com.tdunning.math.stats.MergingDigest) work.obj;
        try {
          if (tdigest.size() > 0) {
            int size = tdigest.smallByteSize();
            java.nio.ByteBuffer byteBuf = java.nio.ByteBuffer.allocate(size);
            tdigest.asSmallBytes(byteBuf);
            out.buffer = buffer = buffer.reallocIfNeeded(size);
            out.start = 0;
            out.end = size;
            out.buffer.setBytes(0, byteBuf.array());
            out.isSet = 1;
          } else {
            out.isSet = 0;
          }
        } catch (Exception e) {
          throw new org.apache.drill.common.exceptions.DrillRuntimeException("Failed to get TDigest output", e);
        }
      } else {
        out.isSet = 0;
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TDigestFunctions.java`
#### Snippet
```java
          int size = tdigest.smallByteSize();
          java.nio.ByteBuffer byteBuf = java.nio.ByteBuffer.allocate(size);
          tdigest.asSmallBytes(byteBuf);
          out.buffer = buffer = buffer.reallocIfNeeded(size);
          out.start = 0;
          out.end = size;
          out.buffer.setBytes(0, byteBuf.array());
          out.isSet = 1;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/TypeFunctions.java`
#### Snippet
```java
      byte[] type = typeName.getBytes(java.nio.charset.StandardCharsets.UTF_8);
      buf = buf.reallocIfNeeded(type.length);
      buf.setBytes(0, type);
      out.buffer = buf;
      out.start = 0;
      out.end = type.length;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
    for (int l = left.start, r = right.start; l < left.end && r < right.end; l++, r++) {
      byte leftByte = left.buffer.getByte(l);
      byte rightByte = right.buffer.getByte(r);
      if (leftByte != rightByte) {
        return ((leftByte & 0xFF) - (rightByte & 0xFF)) > 0 ? 1 : -1;
      }
    }

    int l = (left.end - left.start) - (right.end - right.start);
    if (l > 0) {
      return 1;
    } else if (l == 0) {
      return 0;
    } else {
      return -1;
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/VarHelpers.java`
#### Snippet
```java
    if (left.isSet == 0) {
      if (right.isSet == 0) {
        return 0;
      }
      return -1;
    } else if (right.isSet == 0) {
      return 1;
    }

    for (int l = left.start, r = right.start; l < left.end && r < right.end; l++, r++) {
      byte leftByte = left.buffer.getByte(l);
      byte rightByte = right.buffer.getByte(r);
      if (leftByte != rightByte) {
        return ((leftByte & 0xFF) - (rightByte & 0xFF)) > 0 ? 1 : -1;
      }
    }

    int l = (left.end - left.start) - (right.end - right.start);
    if (l > 0) {
      return 1;
    } else if (l == 0) {
      return 0;
    } else {
      return -1;
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/XXHash.java`
#### Snippet
```java
        v1 += PlatformDependent.getLong(p) * PRIME64_2;
        p = p + 8;
        v1 = Long.rotateLeft(v1, 31);
        v1 *= PRIME64_1;

        v2 += PlatformDependent.getLong(p) * PRIME64_2;
        p = p + 8;
        v2 = Long.rotateLeft(v2, 31);
        v2 *= PRIME64_1;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/XXHash.java`
#### Snippet
```java
      v1 *= PRIME64_2;
      v1 = Long.rotateLeft(v1, 31);
      v1 *= PRIME64_1;
      h64 ^= v1;

      h64 = h64 * PRIME64_1 + PRIME64_4;

      v2 *= PRIME64_2;
      v2 = Long.rotateLeft(v2, 31);
      v2 *= PRIME64_1;
      h64 ^= v2;

      h64 = h64 * PRIME64_1 + PRIME64_4;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/conv/JsonConvertFrom.java`
#### Snippet
```java
      if (in.isSet == 0) {
        // Return empty map
        org.apache.drill.exec.vector.complex.writer.BaseWriter.MapWriter mapWriter = writer.rootAsMap();
        mapWriter.start();
        mapWriter.end();
        return;
      }

      try {
        jsonReader.setSource(in.start, in.end, in.buffer);
        jsonReader.write(writer);
        buffer = jsonReader.getWorkBuf();
      } catch (Exception e) {
        throw new org.apache.drill.common.exceptions.DrillRuntimeException("Error while converting from JSON. ", e);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/output/DecimalReturnTypeInference.java`
#### Snippet
```java
      for (LogicalExpression e : logicalExpressions) {
        scale = Math.max(scale, e.getMajorType().getScale());
        precision = Math.max(precision, e.getMajorType().getPrecision());
      }

      return TypeProtos.MajorType.newBuilder()
          .setMinorType(attributes.getReturnValue().getType().getMinorType())
          .setScale(scale)
          .setPrecision(precision)
          .setMode(mode)
          .build();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/FragmentContextImpl.java`
#### Snippet
```java
    Map<MinorType, ValueHolder> holdersByType = constantValueHolderCache.get(value);
    ValueHolder valueHolder = holdersByType.get(type);
    if (valueHolder == null) {
      valueHolder = holderInitializer.apply(getManagedBuffer());
      holdersByType.put(type, valueHolder);
    }
    return valueHolder;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
      Map<Path, FileMetadata> prunedFiles;
      if (!source.getPartitionsMetadata().isEmpty()
          && source.getPartitionsMetadata().size() > getPartitions().size()) {
        // prunes files to leave only files which are contained by pruned partitions
        prunedFiles = pruneForPartitions(source.getFilesMetadata(), getPartitions());
      } else if (!source.getSegmentsMetadata().isEmpty()
          && source.getSegmentsMetadata().size() > getSegments().size()) {
        // prunes row groups to leave only row groups which are contained by pruned segments
        prunedFiles = pruneForSegments(source.getFilesMetadata(), getSegments());
      } else {
        prunedFiles = source.getFilesMetadata();
      }

      if (isMatchAllMetadata()) {
        files = prunedFiles;
        return;
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/AbstractDeMuxExchange.java`
#### Snippet
```java
    List<DrillbitEndpoint> drillbitEndpoints = ImmutableSet.copyOf(receiverFragmentEndpoints).asList();

    List<EndpointAffinity> affinities = Lists.newArrayList();
    for(DrillbitEndpoint ep : drillbitEndpoints) {
      affinities.add(new EndpointAffinity(ep, Double.POSITIVE_INFINITY));
    }

    return ParallelizationInfo.create(affinities.size(), affinities.size(), affinities);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/StatisticsWriterRecordBatch.java`
#### Snippet
```java
    final VarCharVector fragmentIdVector = (VarCharVector) container.getValueAccessorById(
        VarCharVector.class,
        container.getValueVectorId(SchemaPath.getSimplePath("Fragment")).getFieldIds())
        .getValueVector();
    AllocationHelper.allocate(fragmentIdVector, 1, 50);
    final BigIntVector summaryVector = (BigIntVector) container.getValueAccessorById(BigIntVector.class,
        container.getValueVectorId(SchemaPath.getSimplePath("Number of records written")).getFieldIds())
        .getValueVector();
    AllocationHelper.allocate(summaryVector, 1, 8);
    fragmentIdVector.getMutator().setSafe(0, fragmentUniqueId.getBytes(StandardCharsets.UTF_8));
    fragmentIdVector.getMutator().setValueCount(1);
    summaryVector.getMutator().setSafe(0, counter);
    summaryVector.getMutator().setValueCount(1);

    container.setRecordCount(1);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java
      collector.reportErrors(logger);
      g.setMappingSet(leftMapping);
      HoldingContainer left = g.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);
      g.setMappingSet(rightMapping);
      HoldingContainer right = g.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);
      g.setMappingSet(mainMapping);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java
      HoldingContainer out = g.addExpr(fh, ClassGenerator.BlkCreateMode.FALSE);
      JConditional jc = g.getEvalBlock()._if(out.getValue().ne(JExpr.lit(0)));

      if (od.getDirection() == Direction.ASCENDING) {
        jc._then()._return(out.getValue());
      } else {
        jc._then()._return(out.getValue().minus());
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggBatch.java`
#### Snippet
```java
    IterOutcome outcome = next(incoming);
    switch (outcome) {
      case NONE:
        state = BatchState.DONE;
        container.buildSchema(SelectionVectorMode.NONE);
        return;
      default:
        break;
    }

    incomingSchema = incoming.getSchema();
    createAggregator();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggBatch.java`
#### Snippet
```java
    setupIsSame(cg, keyExprs);
    setupIsSameApart(cg, keyExprs);
    addRecordValues(cg, valueExprs);
    outputRecordKeys(cg, keyOutputIds, keyExprs);
    outputRecordKeysPrev(cg, keyOutputIds, keyExprs);

    cg.getBlock("resetValues")._return(JExpr.TRUE);
    getIndex(cg);

    container.buildSchema(SelectionVectorMode.NONE);
    StreamingAggregator agg = context.getImplementationClass(cg);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggBatch.java`
#### Snippet
```java
    cg.setMappingSet(IS_SAME_I1);
    for (LogicalExpression expr : keyExprs) {
      // first, we rewrite the evaluation stack for each side of the comparison.
      cg.setMappingSet(IS_SAME_I1);
      HoldingContainer first = cg.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);
      cg.setMappingSet(IS_SAME_I2);
      HoldingContainer second = cg.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);

      LogicalExpression fh =
          FunctionGenerationHelper
          .getOrderingComparatorNullsHigh(first, second, context.getFunctionRegistry());
      HoldingContainer out = cg.addExpr(fh, ClassGenerator.BlkCreateMode.FALSE);
      cg.getEvalBlock()._if(out.getValue().ne(JExpr.lit(0)))._then()._return(JExpr.FALSE);
    }
    cg.getEvalBlock()._return(JExpr.TRUE);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/ChainedHashTable.java`
#### Snippet
```java
      final LogicalExpression expr = ExpressionTreeMaterializer.materialize(ne.getExpr(), incomingBuild, collector, context.getFunctionRegistry());
      if (collector.hasErrors()) {
        throw new SchemaChangeException("Failure while materializing expression. " + collector.toErrorString());
      }
      if (expr == null) {
        continue;
      }
      keyExprsBuild[i] = expr;
      i++;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
              newLinks.getMutator().set(entryIdxWithinBatch, EMPTY_SLOT); // update the newLink entry in this
              // batch to mark end of the hash chain
              newHashValues.getMutator().set(entryIdxWithinBatch, hash);

              if (EXTRA_DEBUG) {
                logger.debug("Followed hash chain in new bucket. bucketIdx = {}, newLinks[ {} ] = {}, " +
                    "newLinks[ {} ] = {}, hash value = {}.", bucketIdx, idxWithinBatch,
                    newLinks.getAccessor().get(idxWithinBatch), entryIdxWithinBatch,
                    newLinks.getAccessor().get(entryIdxWithinBatch),
                    newHashValues.getAccessor().get(entryIdxWithinBatch));
              }

              break;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
      int avgRowWidthSingleFlattenEntry =
        RecordBatchSizer.safeDivide(columnSize.getTotalNetSize() - (getOffsetVectorWidth() * columnSize.getValueCount()),
          columnSize.getElementCount());

      // Average rowWidth of outgoing batch.
      int avgOutgoingRowWidth = avgRowWidthWithOutFlattenColumn + avgRowWidthSingleFlattenEntry;

      int outputBatchSize = getOutputBatchSize();
      // Number of rows in outgoing batch
      setOutputRowCount(outputBatchSize, avgOutgoingRowWidth);

      setOutgoingRowWidth(avgOutgoingRowWidth);

      // Limit to lower bound of total number of rows possible for this batch
      // i.e. all rows fit within memory budget.
      setOutputRowCount(Math.min(columnSize.getElementCount(), getOutputRowCount()));

      RecordBatchStats.logRecordBatchStats(RecordBatchIOType.INPUT, getRecordBatchSizer(), getRecordBatchStatsContext());

      updateIncomingStats();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
    stats.setLongStat(Metric.INPUT_BATCH_COUNT, flattenMemoryManager.getNumIncomingBatches());
    stats.setLongStat(Metric.AVG_INPUT_BATCH_BYTES, flattenMemoryManager.getAvgInputBatchSize());
    stats.setLongStat(Metric.AVG_INPUT_ROW_BYTES, flattenMemoryManager.getAvgInputRowWidth());
    stats.setLongStat(Metric.INPUT_RECORD_COUNT, flattenMemoryManager.getTotalInputRecords());
    stats.setLongStat(Metric.OUTPUT_BATCH_COUNT, flattenMemoryManager.getNumOutgoingBatches());
    stats.setLongStat(Metric.AVG_OUTPUT_BATCH_BYTES, flattenMemoryManager.getAvgOutputBatchSize());
    stats.setLongStat(Metric.AVG_OUTPUT_ROW_BYTES, flattenMemoryManager.getAvgOutputRowWidth());
    stats.setLongStat(Metric.OUTPUT_RECORD_COUNT, flattenMemoryManager.getTotalOutputRecords());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
        "incoming aggregate left: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
        batchMemoryManager
            .getNumIncomingBatches(JoinBatchMemoryManager.LEFT_INDEX),
        batchMemoryManager
            .getAvgInputBatchSize(JoinBatchMemoryManager.LEFT_INDEX),
        batchMemoryManager
            .getAvgInputRowWidth(JoinBatchMemoryManager.LEFT_INDEX),
        batchMemoryManager
            .getTotalInputRecords(JoinBatchMemoryManager.LEFT_INDEX));

    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
        "incoming aggregate right: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
        batchMemoryManager
            .getNumIncomingBatches(JoinBatchMemoryManager.RIGHT_INDEX),
        batchMemoryManager
            .getAvgInputBatchSize(JoinBatchMemoryManager.RIGHT_INDEX),
        batchMemoryManager
            .getAvgInputRowWidth(JoinBatchMemoryManager.RIGHT_INDEX),
        batchMemoryManager
            .getTotalInputRecords(JoinBatchMemoryManager.RIGHT_INDEX));

    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
        "outgoing aggregate: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
        batchMemoryManager.getNumOutgoingBatches(),
        batchMemoryManager.getAvgOutputBatchSize(),
        batchMemoryManager.getAvgOutputRowWidth(),
        batchMemoryManager.getTotalOutputRecords());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashTableSizeCalculatorConservativeImpl.java`
#### Snippet
```java
    Preconditions.checkArgument(!keySizes.isEmpty());
    Preconditions.checkArgument(!partitionStat.isSpilled());
    Preconditions.checkArgument(partitionStat.getNumInMemoryRecords() > 0);

    // The number of entries in the global index array. Note this array may not be completed populated and the degree of population depends on the load factor.
    long numBuckets = (long) (((double) partitionStat.getNumInMemoryRecords()) * (1.0 / loadFactor));
    // The number of pairs in the hash table
    long numEntries = partitionStat.getNumInMemoryRecords();
    // The number of Batch Holders in the hash table. Note that entries are tightly packed in the Batch Holders (no gaps).
    long numBatchHolders = (numEntries + maxNumRecords - 1) / maxNumRecords;

    // Include the size of the buckets array
    long hashTableSize = RecordBatchSizer.multiplyByFactors(computeValueVectorSize(numBuckets, IntVector.VALUE_WIDTH), hashTableDoublingFactor);
    // Each Batch Holder has an int vector of max size for holding links and hash values
    hashTableSize += numBatchHolders * 2L * IntVector.VALUE_WIDTH * ((long) maxNumRecords);

    long numFullBatchHolders = numEntries % maxNumRecords == 0? numBatchHolders: numBatchHolders - 1;
    // Compute the size of the value vectors holding keys in each full bucket
    hashTableSize += numFullBatchHolders * computeVectorSizes(keySizes, maxNumRecords, safetyFactor);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/MergeJoinBatch.java`
#### Snippet
```java
    updateBatchMemoryManagerStats();

    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
      "incoming aggregate left: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
      batchMemoryManager.getNumIncomingBatches(JoinBatchMemoryManager.LEFT_INDEX),
      batchMemoryManager.getAvgInputBatchSize(JoinBatchMemoryManager.LEFT_INDEX),
      batchMemoryManager.getAvgInputRowWidth(JoinBatchMemoryManager.LEFT_INDEX),
      batchMemoryManager.getTotalInputRecords(JoinBatchMemoryManager.LEFT_INDEX));

    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
      "incoming aggregate right: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
      batchMemoryManager.getNumIncomingBatches(JoinBatchMemoryManager.RIGHT_INDEX),
      batchMemoryManager.getAvgInputBatchSize(JoinBatchMemoryManager.RIGHT_INDEX),
      batchMemoryManager.getAvgInputRowWidth(JoinBatchMemoryManager.RIGHT_INDEX),
      batchMemoryManager.getTotalInputRecords(JoinBatchMemoryManager.RIGHT_INDEX));

    RecordBatchStats.logRecordBatchStats(getRecordBatchStatsContext(),
      "outgoing aggregate: batch count : %d, avg bytes : %d,  avg row bytes : %d, record count : %d",
      batchMemoryManager.getNumOutgoingBatches(), batchMemoryManager.getAvgOutputBatchSize(),
      batchMemoryManager.getAvgOutputRowWidth(), batchMemoryManager.getTotalOutputRecords());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/MergeJoinBatch.java`
#### Snippet
```java
        TypedFieldId outTypedFieldId = new TypedFieldId.Builder().finalType(outputType)
            .addId(vectorId)
            .build();
        JVar vvOut = cg.declareVectorValueSetupAndMember("outgoing", outTypedFieldId);
        // todo: check result of copyFromSafe and grow allocation
        cg.getEvalBlock().add(vvOut.invoke("copyFromSafe")
          .arg(copyLeftMapping.getValueReadIndex())
          .arg(copyLeftMapping.getValueWriteIndex())
          .arg(vvIn));
        cg.rotateBlock();
        ++vectorId;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjectorTemplate.java`
#### Snippet
```java
    int counter = 0;
    for (int i = 0; i < countN; i++, firstOutputIndex++) {
      int partition = getPartition(i);
      partitionValues.getMutator().setSafe(i, partition);
      counter++;
    }
    for(TransferPair t : transfers){
        t.transfer();
    }
    return counter;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ProjectedColumn.java`
#### Snippet
```java
    if (!hasIndexes()) {
      return null;
    }
    final int max = maxIndex();
    final boolean map[] = new boolean[max+1];
    for (final Integer index : indexes) {
      map[index] = true;
    }
    return map;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ProjectedColumn.java`
#### Snippet
```java
    if (isArray()) {
      buf.append("[");
      if (indexes == ALL_INDEXES) {
        buf.append("*");
      } else {
        List<String> idxs = indexes.stream().sorted().map(i -> Integer.toString(i)).collect(Collectors.toList());
        buf.append(String.join(", ", idxs));
      }
      buf.append("]");
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/SchemaUtils.java`
#### Snippet
```java
    if (colReq.isArray()) {
      if (colReq.arrayDims() == 1) {
        return readCol.isArray() || readCol.isDict() || readCol.isVariant();
      } else {
        return readCol.type() == MinorType.LIST || readCol.isDict() || readCol.isVariant();
      }
    }
    return true;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/AvgWidthMergedStatistic.java`
#### Snippet
```java
    byte[] typeAsBytes = types.getStat(colName);
    int type  = -1;
    SimpleModule deModule = new SimpleModule("StatisticsSerDeModule") //
        .addDeserializer(TypeProtos.MajorType.class, new MajorTypeSerDe.De());
    ObjectMapper mapper = JacksonUtils.createJsonMapperBuilder()
        .addModule(deModule)
        .build();
    try {
      type = mapper.readValue(typeAsBytes, TypeProtos.MajorType.class).getMinorType().getNumber();
    } catch (IOException ex) {
      //Ignore exception
    }
    // If variable length type - then use the nonNullCount. Otherwise, use the Count,
    // since even NULL values take up the same space.
    if (type == TypeProtos.MinorType.VAR16CHAR.getNumber()
        || type == TypeProtos.MinorType.VARCHAR.getNumber()
        || type == TypeProtos.MinorType.VARBINARY.getNumber()) {
      return nonNullStatCounts.getStat(colName);
    } else {
      return statCounts.getStat(colName);
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/CntDupsMergedStatistic.java`
#### Snippet
```java
            long sum = 0;
            if (sumHolder.get(colName) != null) {
                sum = sumHolder.get(colName);
            }
            if (!accessor.isNull(0)) {
                sum += accessor.get(0);
                sumHolder.put(colName, sum);
            }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/HLLMergedStatistic.java`
#### Snippet
```java
      String colName = vv.getField().getName();
      HyperLogLog colHLLHolder = null;
      if (hllHolder.get(colName) != null) {
        colHLLHolder = hllHolder.get(colName);
      }
      NullableVarBinaryVector hllVector = (NullableVarBinaryVector) vv;
      NullableVarBinaryVector.Accessor accessor = hllVector.getAccessor();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/HLLMergedStatistic.java`
#### Snippet
```java
        if (!accessor.isNull(0)) {
          ByteArrayInputStream bais = new ByteArrayInputStream(accessor.get(0), 0, vv.getBufferSize());
          HyperLogLog other = HyperLogLog.Builder.build(new DataInputStream(bais));
          if (colHLLHolder != null) {
            colHLLHolder.addAll(other);
            hllHolder.put(colName, colHLLHolder);
          } else {
            hllHolder.put(colName, other);
          }
        }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/NNRowCountMergedStatistic.java`
#### Snippet
```java
    assert (input.getField().getType().getMinorType() == TypeProtos.MinorType.MAP);
    for (ValueVector vv : input) {
      String colName = vv.getField().getName();
      NullableBigIntVector biv = (NullableBigIntVector) vv;
      NullableBigIntVector.Accessor accessor = biv.getAccessor();
      long sum = 0;
      if (sumHolder.get(colName) != null) {
        sum = sumHolder.get(colName);
      }
      if (!accessor.isNull(0)) {
        sum += accessor.get(0);
        sumHolder.put(colName, sum);
      }
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/NNRowCountMergedStatistic.java`
#### Snippet
```java
    assert (output.getField().getType().getMinorType() == TypeProtos.MinorType.MAP);
    for (ValueVector outMapCol : output) {
      String colName = outMapCol.getField().getName();
      NullableBigIntVector vv = (NullableBigIntVector) outMapCol;
      vv.allocateNewSafe();
      if (sumHolder.get(colName) != null) {
        vv.getMutator().setSafe(0, (long)(100.0*sumHolder.get(colName)/ samplePercent));
      } else {
        vv.getMutator().setNull(0);
      }
    }
    state = State.COMPLETE;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      final GeneratorMapping mapping = GeneratorMapping.create("setupPartition", "outputRow", "resetValues", "cleanup");
      final MappingSet mappingSet = new MappingSet(null, "outIndex", mapping, mapping);

      cg.setMappingSet(mappingSet);
      final JVar vv = cg.declareVectorValueSetupAndMember(cg.getMappingSet().getOutgoing(), fieldId);
      final JExpression outIndex = cg.getMappingSet().getValueWriteIndex();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      final FunctionCall call = (FunctionCall) ne.getExpr();
      final LogicalExpression input = ExpressionTreeMaterializer.materializeAndCheckErrors(call.arg(0), batch, registry);
      if (input == null) {
        return false;
      }

      // make sure output vector type is Nullable, because we will write a null value in the first row of each partition
      TypeProtos.MajorType majorType = input.getMajorType();
      if (majorType.getMode() == TypeProtos.DataMode.REQUIRED) {
        majorType = Types.optional(majorType.getMinorType());
      }

      // add corresponding ValueVector to container
      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), majorType);
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId =  batch.getValueVectorId(ne.getRef());

      writeInputToLead = new ValueVectorWriteExpression(outputId, input, true);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      final FunctionCall call = (FunctionCall) ne.getExpr();
      final LogicalExpression input = ExpressionTreeMaterializer.materializeAndCheckErrors(call.arg(0), batch, registry);
      if (input == null) {
        return false;
      }

      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), input.getMajorType());
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId = batch.getValueVectorId(ne.getRef());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/BaseSortWrapper.java`
#### Snippet
```java
      ErrorCollector collector = new ErrorCollectorImpl();
      final LogicalExpression expr = ExpressionTreeMaterializer.materialize(od.getExpr(), batch, collector,
          context.getFragmentContext().getFunctionRegistry());
      if (collector.hasErrors()) {
        throw UserException.unsupportedError()
              .message("Failure while materializing expression. " + collector.toErrorString())
              .build(logger);
      }
      g.setMappingSet(LEFT_MAPPING);
      HoldingContainer left = g.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);
      g.setMappingSet(RIGHT_MAPPING);
      HoldingContainer right = g.addExpr(expr, ClassGenerator.BlkCreateMode.FALSE);
      g.setMappingSet(MAIN_MAPPING);

      // next we wrap the two comparison sides and add the expression block for the comparison.
      LogicalExpression fh =
          FunctionGenerationHelper.getOrderingComparator(od.nullsSortHigh(), left, right,
                                                         context.getFragmentContext().getFunctionRegistry());
      HoldingContainer out = g.addExpr(fh, ClassGenerator.BlkCreateMode.FALSE);
      JConditional jc = g.getEvalBlock()._if(out.getValue().ne(JExpr.lit(0)));

      if (od.getDirection() == Direction.ASCENDING) {
        jc._then()._return(out.getValue());
      }else{
        jc._then()._return(out.getValue().minus());
      }
      g.rotateBlock();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java
      if (isVersioned()) {
        mapVector.putChild(colSchema.name(), vector);
      }
      final int index = outputSchema.addColumn(colSchema);
      assert mapVector.size() == outputSchema.size();
      assert mapVector.getField().getChildren().size() == outputSchema.size();
      return index;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/ParquetPartitionDescriptor.java`
#### Snippet
```java
        Object s = groupScan.getPartitionValue(path, column, Object.class);
        byte[] bytes;
        if (s == null) {
          varBinaryVector.getMutator().setNull(index);
          return;
        } else {
          bytes = getBytes(type, s);
        }
        varBinaryVector.getMutator().setSafe(index, bytes, 0, bytes.length);
        return;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillAggregateRelBase.java`
#### Snippet
```java
    if(PrelUtil.getSettings(getCluster()).useDefaultCosting()) {
      return super.computeSelfCost(planner, mq).multiplyBy(.1);
    }
    RelNode child = this.getInput();
    double inputRows = mq.getRowCount(child);

    int numGroupByFields = this.getGroupCount();
    int numAggrFields = this.aggCalls.size();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Materializer.java`
#### Snippet
```java
    iNode.addAllocation(op);
    List<PhysicalOperator> children = Lists.newArrayList();
    for (PhysicalOperator child : op) {
      children.add(child.accept(this, iNode));
    }
    PhysicalOperator newOp = op.getNewWithChildren(children);
    newOp.setCost(op.getCost());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
      if (o1.size() == 1) {
        o1Covering = o1.get(0).isCovering();
        o1NumLeadingFilters = o1.get(0).numLeadingFilters();
        o1SatisfiesCollation = o1.get(0).satisfiesCollation();
        o1LeadingSelectivity = o1.get(0).getLeadingSelectivity();
      } else {
        o1Covering = false;
        // If the intersect plan is a left-deep join tree, the last index
        // in the join would satisfy the collation. For now, assume no collation.
        o1SatisfiesCollation = false;
        o1NumLeadingFilters = o1.get(0).numLeadingFilters();
        for (int idx=1; idx<o1.size(); idx++) {
          o1NumLeadingFilters+=o1.get(idx).numLeadingFilters();
        }
        o1LeadingSelectivity = o1.get(0).getLeadingSelectivity();
        for (int idx=1; idx<o1.size(); idx++) {
          o1LeadingSelectivity*=o1.get(idx).getLeadingSelectivity();
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringIndexPlanGenerator.java`
#### Snippet
```java
        List<RexNode> newProjects = Lists.newArrayList();
        DrillParseContext parseContxt = new DrillParseContext(PrelUtil.getPlannerSettings(newProject.getCluster()));
        for(RexNode projectRex: newProject.getProjects()) {
          RexNode newRex = IndexPlanUtils.rewriteFunctionalRex(indexContext, parseContxt, null, origScan, projectRex, indexScanPrel.getRowType(), functionInfo);
          newProjects.add(newRex);
        }

        ProjectPrel rewrittenProject = new ProjectPrel(newProject.getCluster(),
            newCollation==null? newProject.getTraitSet() : newProject.getTraitSet().plus(newCollation),
            indexFilterPrel, newProjects, newProject.getRowType());

        cap = rewrittenProject;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    final int leftRowKeyIdx = getRowKeyIndex(left.getRowType(), origScan);
    final int rightRowKeyIdx = 0; // only rowkey field is being projected from right side

    assert leftRowKeyIdx >= 0;

    List<Integer> leftJoinKeys = ImmutableList.of(leftRowKeyIdx);
    List<Integer> rightJoinKeys = ImmutableList.of(rightRowKeyIdx);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    final RelDataTypeFactory.FieldInfoBuilder finalFieldTypeBuilder =
        origScan.getCluster().getTypeFactory().builder();

    List<RelDataTypeField> hjRowFields = newRel.getRowType().getFieldList();
    int toRemoveRowKeyCount = 1;
    if (getRowKeyIndex(origRowType, origScan)  < 0 ) {
      toRemoveRowKeyCount = 2;
    }
    finalFieldTypeBuilder.addAll(hjRowFields.subList(0, hjRowFields.size()-toRemoveRowKeyCount));
    final RelDataType finalProjectRowType = finalFieldTypeBuilder.build();

    List<RexNode> resetExprs = Lists.newArrayList();
    for (int idx=0; idx<hjRowFields.size()-toRemoveRowKeyCount; ++idx) {
      resetExprs.add(RexInputRef.of(idx, newRel.getRowType()));
    }

    final ProjectPrel resetProjectPrel = new ProjectPrel(newRel.getCluster(), newRel.getTraitSet(),
        newRel, resetExprs, finalProjectRowType);
    newRel = resetProjectPrel;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    RelDataType indexScanRowType = FunctionalIndexHelper.convertRowTypeForIndexScan(
        origScan, indexContext.getOrigMarker(), indexScan, functionInfo);
    DrillDistributionTrait partition = IndexPlanUtils.scanIsPartition(IndexPlanUtils.getGroupScan(origScan))?
        DrillDistributionTrait.RANDOM_DISTRIBUTED : DrillDistributionTrait.SINGLETON;

    ScanPrel indexScanPrel = new ScanPrel(origScan.getCluster(),
        origScan.getTraitSet().plus(Prel.DRILL_PHYSICAL).plus(partition), indexScan, indexScanRowType, origScan.getTable());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    List<RexNode> leftProjectExprs = Lists.newArrayList();
    int leftRowKeyIndex = getRowKeyIndex(dbScan.getRowType(), origScan);
    final RelDataTypeField leftRowKeyField = dbScan.getRowType().getFieldList().get(leftRowKeyIndex);
    final RelDataTypeFactory.FieldInfoBuilder leftFieldTypeBuilder =
        dbScan.getCluster().getTypeFactory().builder();

    FilterPrel leftIndexFilterPrel = null;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
      List<RelDataTypeField> origProjFields = origRowType.getFieldList();
      leftFieldTypeBuilder.addAll(origProjFields);
      // get the exprs from the original Project

      leftProjectExprs.addAll(IndexPlanUtils.getProjects(origProject));
      // add the rowkey IFF rowkey is not in orig scan
      if (getRowKeyIndex(origRowType, origScan) < 0) {
        leftFieldTypeBuilder.add(leftRowKeyField);
        leftProjectExprs.add(RexInputRef.of(leftRowKeyIndex, dbScan.getRowType()));
      }

      final RelDataType leftProjectRowType = leftFieldTypeBuilder.build();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
      leftFieldTypeBuilder.addAll(origProjFields);
      // get the exprs from the original Project IFF there is a project

      leftProjectExprs.addAll(IndexPlanUtils.getProjects(origProject));
      // add the rowkey IFF rowkey is not in orig scan
      if (getRowKeyIndex(origRowType, origScan) < 0) {
        leftFieldTypeBuilder.add(leftRowKeyField);
        leftProjectExprs.add(RexInputRef.of(leftRowKeyIndex, dbScan.getRowType()));
      }

      final RelDataType leftProjectRowType = leftFieldTypeBuilder.build();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ConvertCountToDirectScanRule.java`
#### Snippet
```java
    final RelDataType scanRowType = CountToDirectScanUtils.constructDataType(agg, result.keySet());

    final DynamicPojoRecordReader<Long> reader = new DynamicPojoRecordReader<>(
        CountToDirectScanUtils.buildSchema(scanRowType.getFieldNames()),
        Collections.singletonList(new ArrayList<>(result.values())));

    final ScanStats scanStats = new ScanStats(ScanStats.GroupScanProperty.EXACT_ROW_COUNT, 1, 1, scanRowType.getFieldCount());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ConvertCountToDirectScanRule.java`
#### Snippet
```java
        int index = aggCall.getArgList().get(0);

        if (project != null) {
          // project in the middle of Agg and Scan : Only when input of AggCall is a RexInputRef in Project, we find the index of Scan's field.
          // For instance,
          // Agg - count($0)
          //  \
          //  Proj - Exp={$1}
          //    \
          //   Scan (col1, col2).
          // return count of "col2" in Scan's metadata, if found.
          if (!(project.getProjects().get(index) instanceof RexInputRef)) {
            return ImmutableMap.of(); // do not apply for all other cases.
          }

          index = ((RexInputRef) project.getProjects().get(index)).getIndex();
        }

        String columnName = scan.getRowType().getFieldNames().get(index).toLowerCase();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillCorrelateRule.java`
#### Snippet
```java
    final RelNode left = correlate.getLeft();
    final RelNode right = correlate.getRight();
    final RelNode convertedLeft = convert(left, left.getTraitSet().plus(DrillRel.DRILL_LOGICAL).simplify());
    final RelNode convertedRight = convert(right, right.getTraitSet().plus(DrillRel.DRILL_LOGICAL).simplify());

    final RelTraitSet traits = correlate.getTraitSet().plus(DrillRel.DRILL_LOGICAL);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
    final List<String> fields = getRowType().getFieldNames();
    assert isUnique(fields);
    final int leftCount = left.getRowType().getFieldCount();
    final List<String> leftFields = fields.subList(0, leftCount);
    final List<String> rightFields = fields.subList(leftCount, fields.size());
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
    final PlannerSettings plannerSettings = (PlannerSettings) oldAggRel.getCluster().getPlanner().getContext();
    final boolean isInferenceEnabled = plannerSettings.isTypeInferenceEnabled();
    final int nGroups = oldAggRel.getGroupCount();
    RelDataTypeFactory typeFactory =
        oldAggRel.getCluster().getTypeFactory();
    RexBuilder rexBuilder = oldAggRel.getCluster().getRexBuilder();
    int iAvgInput = oldCall.getArgList().get(0);
    RelDataType avgInputType =
        getFieldType(
            oldAggRel.getInput(),
            iAvgInput);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
    AggregateCall sumCall = getAggCall(oldCall, sumAgg, sumType);
    final SqlCountAggFunction countAgg = (SqlCountAggFunction) SqlStdOperatorTable.COUNT;
    final RelDataType countType = countAgg.getReturnType(typeFactory);
    AggregateCall countCall = getAggCall(oldCall, countAgg, countType);

    RexNode tmpsumRef =
        rexBuilder.addAggCall(
            sumCall,
            nGroups,
            newCalls,
            aggCallMapping,
            ImmutableList.of(avgInputType));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
    final AggregateCall sumArgSquaredAggCall =
        AggregateCall.create(
            new DrillCalciteSqlAggFunctionWrapper(
                new SqlSumAggFunction(sumType), sumType),
            oldCall.isDistinct(),
            oldCall.isApproximate(),
            oldCall.ignoreNulls(),
            ImmutableIntList.of(argSquaredOrdinal),
            oldCall.filterArg,
            oldCall.distinctKeys,
            oldCall.getCollation(),
            sumType,
            null);
    final RexNode sumArgSquared =
        rexBuilder.addAggCall(
            sumArgSquaredAggCall,
            nGroups,
            newCalls,
            aggCallMapping,
            ImmutableList.of(argType));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DirectScanPrel.java`
#### Snippet
```java
    double cpuCost = rowCount * Math.max(columnCount, 1);

    // If a positive value for CPU cost is given multiply the default CPU cost by given CPU cost.
    if (stats.getCpuCost() > 0) {
      cpuCost *= stats.getCpuCost();
    }

    // Even though scan is reading from disk, in the currently generated plans all plans will
    // need to read the same amount of data, so keeping the disk io cost 0 is ok for now.
    // In the future we might consider alternative scans that go against projections or
    // different compression schemes etc that affect the amount of data read. Such alternatives
    // would affect both cpu and io cost.
    //double ioCost = 0;
    double ioCost = stats.getDiskCost();
    DrillCostFactory costFactory = (DrillCostFactory)planner.getCostFactory();
    return costFactory.makeCost(rowCount, cpuCost, ioCost, 0);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
    final String functionName = hashAsDouble ? HASH32_DOUBLE_FUNCTION_NAME : HASH32_FUNCTION_NAME;

    T func = helper.createCall(functionName,  ImmutableList.of(inputExprs.get(0), seed));
    for (int i = 1; i<inputExprs.size(); i++) {
      func = helper.createCall(functionName, ImmutableList.of(inputExprs.get(i), func));
    }

    return func;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPrel.java`
#### Snippet
```java
    List<RexNode> exprs = Lists.newArrayList();

    for (RelDataTypeField field : inputFields) {
      RexNode expr = input.getCluster().getRexBuilder().makeInputRef(field.getType(), field.getIndex());
      exprs.add(expr);
    }

    RelDataType rowType = RexUtil.createStructType(input.getCluster().getTypeFactory(),
        exprs, outputFieldNames, null);

    ProjectPrel proj = new ProjectPrel(input.getCluster(), input.getTraitSet(), input, exprs, rowType);

    return proj;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SingleMergeExchangePrel.java`
#### Snippet
```java
    if (PrelUtil.getSettings(getCluster()).useDefaultCosting()) {
      return super.computeSelfCost(planner, mq).multiplyBy(.1);
    }
    RelNode child = this.getInput();
    double inputRows = mq.getRowCount(child);
    int  rowWidth = child.getRowType().getFieldCount() * DrillCostBase.AVG_FIELD_WIDTH;
    double svrCpuCost = DrillCostBase.SVR_CPU_COST * inputRows;
    double networkCost = DrillCostBase.BYTE_NETWORK_COST * inputRows * rowWidth;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/TopNPrel.java`
#### Snippet
```java
    RelNode child = this.getInput();
    double inputRows = mq.getRowCount(child);
    int numSortFields = this.collation.getFieldCollations().size();
    double cpuCost = DrillCostBase.COMPARE_CPU_COST * numSortFields * inputRows * (Math.log(limit)/Math.log(2));
    double diskIOCost = 0; // assume in-memory for now until we enforce operator-level memory constraints
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SortPrel.java`
#### Snippet
```java
    List<RelFieldCollation> relFieldCollations = Lists.newArrayList();
    relFieldCollations.add(new RelFieldCollation(0,
                            RelFieldCollation.Direction.ASCENDING, RelFieldCollation.NullDirection.FIRST));
    for (RelFieldCollation fieldCollation : this.collation.getFieldCollations()) {
      relFieldCollations.add(new RelFieldCollation(fieldCollation.getFieldIndex() + 1,
              fieldCollation.direction, fieldCollation.nullDirection));
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/UnionAllPrel.java`
#### Snippet
```java
    if(PrelUtil.getSettings(getCluster()).useDefaultCosting()) {
      return super.computeSelfCost(planner, mq).multiplyBy(.1);
    }
    double totalInputRowCount = 0;
    for (int i = 0; i < this.getInputs().size(); i++) {
      totalInputRowCount += mq.getRowCount(this.getInputs().get(i));
    }

    double cpuCost = totalInputRowCount * DrillCostBase.BASE_CPU_COST;
    DrillCostFactory costFactory = (DrillCostFactory)planner.getCostFactory();
    return costFactory.makeCost(totalInputRowCount, cpuCost, 0, 0);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/UnionAllPrel.java`
#### Snippet
```java
    List<PhysicalOperator> inputPops = Lists.newArrayList();

    for (int i = 0; i < this.getInputs().size(); i++) {
      inputPops.add( ((Prel)this.getInputs().get(i)).getPhysicalOperator(creator));
    }

    UnionAll unionall = new UnionAll(inputPops);
    return creator.addMetadata(this, unionall);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
    List<RelNode> children = new ArrayList<>();
    for (Prel child : prel) {
      child = child.accept(this, value);
      children.add(child);
    }
    if (children.equals(prel.getInputs())) {
      return prel;
    }
    return (Prel) prel.copy(prel.getTraitSet(), children);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/TypeInferenceUtils.java`
#### Snippet
```java
      final RelDataTypeFactory factory = opBinding.getTypeFactory();
      // If there is group-by and the imput type is Non-nullable,
      // the output is Non-nullable;
      // Otherwise, the output is nullable.
      final boolean isNullable = opBinding.getGroupCount() == 0
          || opBinding.getOperandType(0).isNullable();

      if(getDrillTypeFromCalciteType(opBinding.getOperandType(0)) == TypeProtos.MinorType.LATE) {
        return createCalciteTypeWithNullability(
            factory,
            SqlTypeName.ANY,
            isNullable);
      }

      // Determines SqlTypeName of the result.
      // When the cheapest cast is is to
      //  - BIGINT then the result is a BIGINT
      //  - FLOAT8 then the result is DOUBLE
      //  - VARDECIMAL then the result is a DECIMAL with the same scale as the input and
      //    the max allowed numeric precision.
      // When none of these conditions are satisfied an error is thrown.
      final RelDataType operandType = opBinding.getOperandType(0);
      final TypeProtos.MinorType inputMinorType = getDrillTypeFromCalciteType(operandType);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeTableHandler.java`
#### Snippet
```java
        SqlNode column = new SqlIdentifier(COLS_COL_COLUMN_NAME, SqlParserPos.ZERO);
        if (columnQualifier instanceof SqlCharStringLiteral) {
          NlsString conditionString = ((SqlCharStringLiteral) columnQualifier).getNlsString();
          columnQualifier = SqlCharStringLiteral.createCharString(
              conditionString.getValue().toLowerCase(),
              conditionString.getCharsetName(),
              columnQualifier.getParserPosition());
          column = SqlStdOperatorTable.LOWER.createCall(SqlParserPos.ZERO, column);
        }
        columnFilter = DrillParserUtil.createCondition(column, SqlStdOperatorTable.LIKE, columnQualifier);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SqlHandlerUtil.java`
#### Snippet
```java
    writer.keyword("(");
    fieldList.get(0).unparse(writer, leftPrec, rightPrec);
    for (int i = 1; i<fieldList.size(); i++) {
      writer.keyword(",");
      fieldList.get(i).unparse(writer, leftPrec, rightPrec);
    }
    writer.keyword(")");
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
    rule.add(MinorType.SMALLINT);
    rule.add(MinorType.INT);
    rule.add(MinorType.BIGINT);
    rule.add(MinorType.UINT1);
    rule.add(MinorType.UINT2);
    rule.add(MinorType.UINT4);
    rule.add(MinorType.UINT8);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.TINYINT);
    rule.add(MinorType.SMALLINT);
    rule.add(MinorType.INT);
    rule.add(MinorType.BIGINT);
    rule.add(MinorType.UINT1);
    rule.add(MinorType.UINT2);
    rule.add(MinorType.UINT4);
    rule.add(MinorType.UINT8);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.UINT1);
    rule.add(MinorType.UINT2);
    rule.add(MinorType.UINT4);
    rule.add(MinorType.UINT8);
    rule.add(MinorType.DECIMAL9);
    rule.add(MinorType.DECIMAL18);
    rule.add(MinorType.DECIMAL28SPARSE);
    rule.add(MinorType.DECIMAL28DENSE);
    rule.add(MinorType.DECIMAL38SPARSE);
    rule.add(MinorType.DECIMAL38DENSE);
    rule.add(MinorType.VARDECIMAL);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.DATE);
    rule.add(MinorType.TIMESTAMP);
    rule.add(MinorType.TIMESTAMPTZ);
    rule.add(MinorType.FIXEDCHAR);
    rule.add(MinorType.FIXED16CHAR);
    rule.add(MinorType.FIXEDBINARY);
    rule.add(MinorType.VARCHAR);
    rule.add(MinorType.VAR16CHAR);
    rule.add(MinorType.VARBINARY);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.FLOAT4);
    rule.add(MinorType.FLOAT8);
    rule.add(MinorType.BIT);
    rule.add(MinorType.FIXEDCHAR);
    rule.add(MinorType.FIXED16CHAR);
    rule.add(MinorType.VARCHAR);
    rule.add(MinorType.VAR16CHAR);
    rule.add(MinorType.VARBINARY);
    rule.add(MinorType.FIXEDBINARY);
    rule.add(MinorType.DATE);
    rule.add(MinorType.TIME);
    rule.add(MinorType.TIMESTAMP);
    rule.add(MinorType.TIMESTAMPTZ);
    rule.add(MinorType.INTERVAL);
    rule.add(MinorType.INTERVALYEAR);
    rule.add(MinorType.INTERVALDAY);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.FIXEDCHAR);
    rule.add(MinorType.FIXED16CHAR);
    rule.add(MinorType.VARCHAR);
    rule.add(MinorType.VAR16CHAR);
    rule.add(MinorType.VARBINARY);
    rule.add(MinorType.FIXEDBINARY);
    rule.add(MinorType.DATE);
    rule.add(MinorType.TIME);
    rule.add(MinorType.TIMESTAMP);
    rule.add(MinorType.TIMESTAMPTZ);
    rule.add(MinorType.INTERVAL);
    rule.add(MinorType.INTERVALYEAR);
    rule.add(MinorType.INTERVALDAY);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/AbstractClientConnection.java`
#### Snippet
```java
    if (isEncryptionEnabled()) {
      saslCodec = new SaslCodec() {

        @Override
        public byte[] wrap(byte[] data, int offset, int len) throws SaslException {
          checkState(saslClient != null);
          return saslClient.wrap(data, offset, len);
        }

        @Override
        public byte[] unwrap(byte[] data, int offset, int len) throws SaslException {
          checkState(saslClient != null);
          return saslClient.unwrap(data, offset, len);
        }
      };
    }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/ServerAuthenticationHandler.java`
#### Snippet
```java
      if (!(negotiatedQOP.equals(expectedQOP))) {
        throw new SaslException(String.format("Mismatch in negotiated QOP value: %s and Expected QOP value: %s",
            negotiatedQOP, expectedQOP));
      }

      // Update the rawWrapSendSize with the negotiated rawSendSize since we cannot call encode with more than the
      // negotiated size of buffer
      if (connection.isEncryptionEnabled()) {
        final int negotiatedRawSendSize = Integer.parseInt(
            saslServer.getNegotiatedProperty(Sasl.RAW_SEND_SIZE).toString());
        if (negotiatedRawSendSize <= 0) {
          throw new SaslException(String.format("Negotiated rawSendSize: %d is invalid. Please check the configured " +
              "value of encryption.sasl.max_wrapped_size. It might be configured to a very small value.",
              negotiatedRawSendSize));
        }
        connection.setWrapSizeLimit(negotiatedRawSendSize);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/ListSchema.java`
#### Snippet
```java
        Iterables.removeIf(fields, new Predicate<Field>() {
            @Override
            public boolean apply(Field field) {
                if (!field.isRead()) {
                    removedFields.add(field);
                    return true;
                } else if (field.hasSchema()) {
                    Iterables.addAll(removedFields, field.getAssignedSchema().removeUnreadFields());
                }

                return false;
            }
        });
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillRestLoginService.java`
#### Snippet
```java
      final SystemOptionManager sysOptions = drillbitContext.getOptionManager();

      final boolean isAdmin = ImpersonationUtil.hasAdminPrivileges(username,
              ExecConstants.ADMIN_USERS_VALIDATOR.getAdminUsers(sysOptions),
              ExecConstants.ADMIN_USER_GROUPS_VALIDATOR.getAdminUserGroups(sysOptions));

      // Create the UserPrincipal corresponding to logged in user.
      final Principal userPrincipal = new DrillUserPrincipal(username, isAdmin);

      final Subject subject = new Subject();
      subject.getPrincipals().add(userPrincipal);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));

    tb.appendCell(new OperatorPathBuilder().setMajor(major).build());
    tb.appendCell(complete.size() + " / " + major.getMinorFragmentProfileCount());

    // If there are no stats to aggregate, create an empty row
    if (complete.size() < 1) {
      tb.appendRepeated("", NUM_NULLABLE_ACTIVE_OVERVIEW_COLUMNS);
      return;
    }

    final MinorFragmentProfile firstStart = Collections.min(complete, Comparators.startTime);
    final MinorFragmentProfile lastStart = Collections.max(complete, Comparators.startTime);
    tb.appendMillis(firstStart.getStartTime() - start);
    tb.appendMillis(lastStart.getStartTime() - start);

    final MinorFragmentProfile firstEnd = Collections.min(complete, Comparators.endTime);
    final MinorFragmentProfile lastEnd = Collections.max(complete, Comparators.endTime);
    tb.appendMillis(firstEnd.getEndTime() - start);
    tb.appendMillis(lastEnd.getEndTime() - start);

    long cumulativeFragmentDurationInMillis = 0L;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    final MinorFragmentProfile shortRun = Collections.min(complete, Comparators.runTime);
    final MinorFragmentProfile longRun = Collections.max(complete, Comparators.runTime);
    tb.appendMillis(shortRun.getEndTime() - shortRun.getStartTime());
    tb.appendMillis(cumulativeFragmentDurationInMillis / complete.size());
    tb.appendMillis(longRun.getEndTime() - longRun.getStartTime());

    Map<String, String> percBusyAttrMap = new HashMap<>();
    //#8721 is the summation sign: sum(Busy): ## + sum(Wait): ##
    percBusyAttrMap.put(HtmlAttribute.TITLE,
        String.format("&#8721;Busy: %,.2fs + &#8721;Wait: %,.2fs", totalProcessInMillis/1E3, totalWaitInMillis/1E3));
    tb.appendPercent(totalProcessInMillis / (totalProcessInMillis + totalWaitInMillis), percBusyAttrMap);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/ProfileWrapper.java`
#### Snippet
```java
      long estimatedPlanEnd = Long.MAX_VALUE;
      //Using Screen MajorFragment as reference
      MajorFragmentProfile majorFrag0 = profile.getFragmentProfile(0);
      //Searching for earliest starting fragment
      for (MinorFragmentProfile fragmentWrapper : majorFrag0.getMinorFragmentProfileList()) {
        long minorFragmentStart = fragmentWrapper.getStartTime();
        if (minorFragmentStart > 0 && minorFragmentStart < estimatedPlanEnd) {
          estimatedPlanEnd = minorFragmentStart;
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/SeekableByteArrayInputStream.java`
#### Snippet
```java
    if (position >= buf.length) {
      throw new IllegalArgumentException();
    }
    if (position + length > buf.length) {
      throw new IllegalArgumentException();
    }
    if (length > buffer.length) {
      throw new IllegalArgumentException();
    }

    System.arraycopy(buf, (int) position, buffer, offset, length);
    return length;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/extended/BaseExtendedValueParser.java`
#### Snippet
```java
    JsonToken token = tokenizer.requireNext();

    // Null: assume the value is null
    // (Extension to extended types)
    if (token == JsonToken.VALUE_NULL) {
      listener.onValue(token, tokenizer);
      return;
    }

    // Value is a scalar, assume "Relaxed format"
    // (Extension to extended types: allow strings.)
    if (token.isScalarValue()) {
      listener.onValue(token, tokenizer);
      return;
    }

    // Must be an object
    requireToken(token, JsonToken.START_OBJECT);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/InfoSchemaFilterBuilder.java`
#### Snippet
```java
      String field = fieldRef.getRootSegmentPath().toUpperCase();
      if (field.equals(CATS_COL_CATALOG_NAME)
          || field.equals(SCHS_COL_SCHEMA_NAME)
          || field.equals(SHRD_COL_TABLE_NAME)
          || field.equals(SHRD_COL_TABLE_SCHEMA)
          || field.equals(COLS_COL_COLUMN_NAME)
          || field.equals(FILES_COL_ROOT_SCHEMA_NAME)
          || field.equals(FILES_COL_WORKSPACE_NAME)) {
        return new FieldExprNode(field);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/Records.java`
#### Snippet
```java
      this.TABLE_CATALOG = catalog;
      this.TABLE_SCHEMA = schemaName;
      this.TABLE_NAME = segmentMetadata.getTableInfo().name();
      this.METADATA_KEY = segmentMetadata.getMetadataInfo().key();
      this.METADATA_TYPE = segmentMetadata.getMetadataInfo().type().name();
      this.METADATA_IDENTIFIER = segmentMetadata.getMetadataInfo().identifier();
      this.PARTITION_COLUMN = segmentMetadata.getColumn().toString();
      this.PARTITION_VALUE = value;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
      ByteBuffer input = inputPageData.nioBuffer(0, inputSize);
      ByteBuffer output = outputPageData.nioBuffer(0, outputSize);

      decomp.decompress(input, inputSize, output, outputSize);
      outputPageData.writerIndex(outputSize);
      timeToRead = timer.elapsed(TimeUnit.NANOSECONDS);

      if (logger.isTraceEnabled()) {
        logger.trace(
          "Col: {}  readPos: {}  Uncompressed_size: {}  pageData: {}",
          columnChunkMetaData.toString(),
          dataReader.getPos(), // TODO: see comment on earlier call to getPos()
          outputSize,
          ByteBufUtil.hexDump(outputPageData)
        );
      }

      this.updateStats(pageHeader, "Decompress", start, timeToRead, inputSize, outputSize);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
    int inputSize = pageHeader.getCompressed_page_size();
    int repLevelSize = pageHeader.data_page_header_v2.getRepetition_levels_byte_length();
    int defLevelSize = pageHeader.data_page_header_v2.getDefinition_levels_byte_length();
    int compDataOffset = repLevelSize + defLevelSize;
    int outputSize = pageHeader.uncompressed_page_size;
    long start = dataReader.getPos();
    long timeToRead;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
      ByteBuffer input = inputPageData.nioBuffer(compDataOffset, inputSize - compDataOffset);
      ByteBuffer output = outputPageData.nioBuffer(compDataOffset, outputSize - compDataOffset);
      decomp.decompress(
        input,
        inputSize - compDataOffset,
        output,
        outputSize - compDataOffset
      );
      outputPageData.writerIndex(outputSize);
      timeToRead = timer.elapsed(TimeUnit.NANOSECONDS);

      if (logger.isTraceEnabled()) {
        logger.trace(
          "Col: {}  readPos: {}  Uncompressed_size: {}  pageData: {}",
          columnChunkMetaData.toString(),
          dataReader.getPos(), // TODO: see comment on earlier call to getPos()
          outputSize,
          ByteBufUtil.hexDump(outputPageData)
        );
      }

      this.updateStats(pageHeader, "Decompress", start, timeToRead, inputSize, outputSize);
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/NullableColumnReader.java`
#### Snippet
```java
        this.recordsReadInThisIteration += runLength;

        this.readStartInBytes = pageReader.readPosInBytes;
        this.readLengthInBits = (long) runLength * dataTypeLengthInBits;
        this.readLength = (int) Math.ceil(readLengthInBits / 8.0);

        readField(runLength);

        writeCount += runLength;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
      timer.start();
      inputPageData = dataReader.getNext(inputSize);
      timeToRead = timer.elapsed(TimeUnit.NANOSECONDS);
      this.updateStats(pageHeader, "Page Read", start, timeToRead, inputSize, inputSize);
      timer.reset();

      timer.start();
      start = dataReader.getPos();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetFixedWidthDictionaryReaders.java`
#### Snippet
```java
      if (recordsRequireDecoding()) {
        ValuesReader valReader = usingDictionary ? pageReader.getDictionaryValueReader() : pageReader.getValueReader();
        recordsReadInThisIteration = Math.min(pageReader.pageValueCount
          - pageReader.valuesRead, recordsToReadInThisPass - valuesReadInCurrentPass);
        for (int i = 0; i < recordsReadInThisIteration; i++) {
          valueVec.getMutator().setSafe(valuesReadInCurrentPass + i, valReader.readInteger());
        }
      } else {
        super.readField(recordsToReadInThisPass);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
    final ValuesReaderWrapper valueReader = pageInfo.encodedValueReader;
    final int[] valueLengths = entry.getValuesLength();
    final int readBatch = Math.min(entry.getMaxEntries(), valuesToRead);
    Preconditions.checkState(readBatch > 0, "Read batch count [%s] should be greater than zero", readBatch);

    final byte[] tgtBuff = entry.getInternalDataArray();
    final int tgtLen = tgtBuff.length;

    // Counters
    int numValues = 0;
    int tgtPos = 0;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryDictionaryReader.java`
#### Snippet
```java
      final Binary currEntry = valueReader.getEntry();
      final int dataLen = currEntry.length();

      if (tgtLen < (tgtPos + dataLen)) {
        valueReader.pushBack(currEntry); // push back this value since we're exiting from the loop
        break;
      }

      valueLengths[numValues++] = dataLen;

      if (dataLen > 0) {
        vlCopyNoPadding(currEntry.getBytes(), 0, tgtBuff, tgtPos, dataLen);

        // Update the counters
        tgtPos += dataLen;
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
    load(true); // load new data to process

    final int[] valueLengths = entry.getValuesLength();
    final int readBatch = Math.min(entry.getMaxEntries(), valuesToRead);
    Preconditions.checkState(readBatch > 0, "Read batch count [%s] should be greater than zero", readBatch);

    final byte[] tgtBuff = entry.getInternalDataArray();
    final byte[] srcBuff = buffer.array();
    final int srcLen = buffer.remaining();
    final int tgtLen = tgtBuff.length;

    // Counters
    int numValues = 0;
    int tgtPos = 0;
    int srcPos = 0;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
      if (srcPos > srcLen -4) {
        break;
      }

      final int dataLen = getInt(srcBuff, srcPos);
      srcPos += 4;

      if (srcLen < (srcPos + dataLen)
       || tgtLen < (tgtPos + dataLen)) {

        break;
      }

      valueLengths[numValues++] = dataLen;

      if (dataLen > 0) {
        vlCopy(srcBuff, srcPos, tgtBuff, tgtPos, dataLen);

        // Update the counters
        srcPos += dataLen;
        tgtPos += dataLen;
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java
    pageInfo.pageDataOff += (numValues * 4 + tgtPos);

    if (remainingPageData() < 0) {
      final String message = String.format("Invalid Parquet page data offset [%d]..", pageInfo.pageDataOff);
      throw new DrillRuntimeException(message);
    }

    // Now set the bulk entry
    entry.set(0, tgtPos, numValues, numValues);

    return entry;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenFixedEntryReader.java`
#### Snippet
```java
      final int currPos = idx * entrySz;
      final int dataLen = getInt(srcBuff, currPos);

      if (dataLen != expectedDataLen) {
        return null; // this is a soft error; caller needs to revert to variable length processing
      }

      valueLengths[idx] = dataLen;
      final int tgt_pos = idx * expectedDataLen;

      if (expectedDataLen > 0) {
        vlCopy(srcBuff, currPos + 4, tgtBuff, tgt_pos, dataLen);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthColumnReaders.java`
#### Snippet
```java
      if (index >= varDecimalVector.getValueCapacity()) {
        return false;
      }
      if (recordsRequireDecoding()) {
        ValuesReader valReader = usingDictionary ? pageReader.getDictionaryValueReader() : pageReader.getValueReader();
        currDecodedValToWrite = valReader.readBytes();
        ByteBuffer buf = currDecodedValToWrite.toByteBuffer();
        mutator.setSafe(index, buf, buf.position(), currDecodedValToWrite.length());
      } else {
        mutator.setSafe(index, start, start + length, value);
      }
      return true;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthColumnReaders.java`
#### Snippet
```java
      if (index >= nullableVarDecimalVector.getValueCapacity()) {
        return false;
      }
      if (recordsRequireDecoding()) {
        ByteBuffer buf = currDecodedValToWrite.toByteBuffer();
        mutator.setSafe(index, buf, buf.position(), currDecodedValToWrite.length());
      } else {
        mutator.setSafe(index, 1, start, start + length, value);
      }
      return true;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/BatchSizingMemoryUtil.java`
#### Snippet
```java
          vectorMemoryUsage.bitsBytesCapacity = vector.getBitsValueCapacity();
          vectorMemoryUsage.offsetsByteCapacity = values.getOffsetVector().getValueCapacity() * INT_VALUE_WIDTH;
          vectorMemoryUsage.dataByteCapacity = values.getByteCapacity();
          vectorMemoryUsage.bitsBytesUsed = currValueCount * BYTE_VALUE_WIDTH;
          vectorMemoryUsage.offsetsBytesUsed = values.getOffsetVector().getPayloadByteCount(currValueCount);
          vectorMemoryUsage.dataBytesUsed = values.getPayloadByteCount(currValueCount) - vectorMemoryUsage.offsetsBytesUsed;
          break;
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/security/UsernamePasswordCredentials.java`
#### Snippet
```java
      if (credentialsProvider == null) {
        return Optional.empty();
      }

      Map<String, String> credentials = queryUser != null
        ? credentialsProvider.getUserCredentials(queryUser)
        : credentialsProvider.getCredentials();

      if (credentials.size() == 0) {
        return Optional.empty();
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
      if (j % 50 == 0) {
        System.out.println(StringUtils.repeat("-", width * 17 + 1));
        for (String column : columns) {
          System.out.printf("| %-15s", width <= 15 ? column : column.substring(0, 14));
        }
        System.out.printf("|\n");
        System.out.println(StringUtils.repeat("-", width * 17 + 1));
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
        String value;
        if (o == null) {
          value = "null";
        } else if (o instanceof byte[]) {
          value = new String((byte[]) o);
        } else {
          value = o.toString();
        }
        System.out.printf("| %-15s", value.length() <= 15 ? value : value.substring(0, 14));
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
    List<String> columns = Lists.newArrayList();
    List<ValueVector> vectors = Lists.newArrayList();
    for (VectorWrapper<?> vw : batch) {
      columns.add(vw.getValueVector().getField().getName());
      vectors.add(vw.getValueVector());
    }
    int width = columns.size();
    int rows = vectors.get(0).getMetadata().getValueCount();
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
      for (ValueVector vv : vectors) {
        Object o = vv.getAccessor().getObject(row);
        String value;
        if (o == null) {
          value = "null";
        } else if (o instanceof byte[]) {
          value = new String((byte[]) o);
        } else {
          value = o.toString();
        }
        System.out.printf("| %-15s", value.length() <= 15 ? value : value.substring(0, 14));
      }
      System.out.printf("|\n");
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
        JsonToken t;
        if (moveForward) {
          t = parser.nextToken();
        } else {
          t = parser.getCurrentToken();
          moveForward = true;
        }
        if (t == JsonToken.NOT_AVAILABLE || t == JsonToken.END_OBJECT) {
          return;
        }

        assert t == JsonToken.FIELD_NAME : String.format(
            "Expected FIELD_NAME but got %s.", t.name());

        final String fieldName = parser.getText();
        this.currentFieldName = fieldName;
        FieldSelection childSelection = selection.getChild(fieldName);
        if (childSelection.isNeverValid()) {
          consumeEntireNextValue();
          continue outside;
        }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/VectorOutput.java`
#### Snippet
```java
      if (!isNull) {
        byte[] binaryData = parser.getBinaryValue();
        if (hasType()) {
          //Ignoring type info as of now.
          long type = getType();
          if (type < 0 || type > 255) {
            throw UserException.validationError()
            .message("$type should be between 0 to 255")
            .build(logger);
          }
        }
        work.prepareBinary(binaryData, binary);
        bin.write(binary);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/VectorOutput.java`
#### Snippet
```java
      if (!isNull) {
        switch (parser.getCurrentToken()) {
        case VALUE_NUMBER_INT:
          DateTime dt = new DateTime(parser.getLongValue(), org.joda.time.DateTimeZone.UTC);
          ts.writeTimeStamp(dt.getMillis());
          break;
        case VALUE_STRING:
          // Note mongo use the UTC format to specify the date value by default.
          // See the mongo specs and the Drill handler (in new JSON loader) :
          // 1. https://docs.mongodb.com/manual/reference/mongodb-extended-json
          // 2. org.apache.drill.exec.store.easy.json.values.UtcTimestampValueListener
          Instant instant = ISO_DATETIME_FORMATTER.parse(parser.getValueAsString(), Instant::from);
          long offset = ZoneId.systemDefault().getRules().getOffset(instant).getTotalSeconds() * 1000L;
          ts.writeTimeStamp(instant.toEpochMilli() + offset);
          break;
        default:
          throw UserException.unsupportedError()
              .message(parser.getCurrentToken().toString())
              .build(logger);
        }
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/VectorOutput.java`
#### Snippet
```java
      if (!isNull) {
        final Period p = ISOPeriodFormat.standard().parsePeriod(parser.getValueAsString());
        int months = DateUtilities.monthsFromPeriod(p);
        int days = p.getDays();
        int millis = DateUtilities.periodToMillis(p);
        intervalWriter.writeInterval(months, days, millis);
      }
```

### DuplicatedCode
Duplicated code
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java
      this.uncompressedLength += uncompressedSize;
      this.compressedLength += compressedSize;
      this.totalValueCount += valueCount;
      this.pageCount += 1;

      // Copying the statistics if it is not initialized yet so we have the correct typed one
      if (totalStatistics == null) {
        totalStatistics = statistics.copy();
      } else {
        totalStatistics.mergeStatistics(statistics);
      }

      columnIndexBuilder.add(statistics);
```

### DuplicatedCode
Duplicated code
in `exec/vector/src/main/java/org/apache/drill/exec/record/MaterializedField.java`
#### Snippet
```java
    if (type.getMinorType() != MinorType.MAP) {
      return true;
    }

    if (children == null || other.children == null) {
      return children == other.children;
    }
    if (children.size() != other.children.size()) {
      return false;
    }

    // Maps are name-based, not position. But, for our
    // purposes, we insist on identical ordering.
    final Iterator<MaterializedField> thisIter = children.iterator();
    final Iterator<MaterializedField> otherIter = other.children.iterator();
```

### DuplicatedCode
Duplicated code
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/AbstractRepeatedMapVector.java`
#### Snippet
```java
      this.to = to;
      this.pairs = new TransferPair[from.size()];

      int i = 0;
      ValueVector vector;
      for (String child : from.getChildFieldNames()) {
        int preSize = to.size();
        vector = from.getChild(child);
        if (vector == null) {
          continue;
        }

        ValueVector newVector = to.addOrGet(child, vector.getField().getType(), vector.getClass());
        if (allocate && to.size() != preSize) {
          newVector.allocateNew();
        }

        pairs[i++] = vector.makeTransferPair(newVector);
      }
```

### DuplicatedCode
Duplicated code
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/AbstractRepeatedMapVector.java`
#### Snippet
```java
      int i = 0;
      ValueVector vector;
      for (String child : from.getChildFieldNames()) {
        int preSize = to.size();
        vector = from.getChild(child);
        if (vector == null) {
          continue;
        }
        ValueVector newVector = to.addOrGet(child, vector.getField().getType(), vector.getClass());
        if (allocate && to.size() != preSize) {
          newVector.allocateNew();
        }
        pairs[i++] = vector.makeTransferPair(newVector);
      }
```

### DuplicatedCode
Duplicated code
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/RepeatedDictReaderImpl.java`
#### Snippet
```java
    container.getAccessor().get(index, h);
    if (h.start == h.end) {
      currentOffset = NO_VALUES;
    } else {
      currentOffset = h.start - 1;
      maxOffset = h.end - 1;
      if (reader != null) {
        reader.setPosition(currentOffset);
      }
    }
```

### DuplicatedCode
Duplicated code
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
    sb.append(call.getName());
    sb.append("(");
    for (int i = 0; i < args.size(); i++) {
      if (i != 0) {
        sb.append(", ");
      }
      args.get(i).accept(this, sb);
    }
    sb.append(") ");
    return null;
```

### DuplicatedCode
Duplicated code
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java
      record.setStoragePlugin(unit.storagePlugin());
      record.setWorkspace(unit.workspace());
      record.setTableName(unit.tableName());
      record.setMetadataKey(unit.metadataKey());
      record.setMetadataIdentifier(unit.metadataIdentifier());
      record.setMetadataType(unit.metadataType());
      record.setLocation(unit.location());
      record.setSchema(unit.schema());
      record.setColumnStatistics(ConverterUtil.convertToString(unit.columnsStatistics()));
      record.setMetadataStatistics(ConverterUtil.convertToString(unit.metadataStatistics()));
```

### DuplicatedCode
Duplicated code
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/components/tables/TablesMetadataMapper.java`
#### Snippet
```java
      record.setSchema(unit.schema());
      record.setColumnStatistics(ConverterUtil.convertToString(unit.columnsStatistics()));
      record.setMetadataStatistics(ConverterUtil.convertToString(unit.metadataStatistics()));
      record.setColumn(unit.column());
      record.setLocations(ConverterUtil.convertToString(unit.locations()));
      record.setPartitionValues(ConverterUtil.convertToString(unit.partitionValues()));
```

### DuplicatedCode
Duplicated code
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      switch (value) {
        case 0: return OPTIONAL;
        case 1: return REQUIRED;
        case 2: return REPEATED;
        default: return null;
      }
```

### DuplicatedCode
Duplicated code
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
                int tmpRaw = input.readEnum();
                org.apache.drill.common.types.TypeProtos.MinorType tmpValue =
                    org.apache.drill.common.types.TypeProtos.MinorType.forNumber(tmpRaw);
                if (tmpValue == null) {
                  mergeUnknownVarintField(7, tmpRaw);
                } else {
                  ensureSubTypeIsMutable();
                  subType_.add(tmpRaw);
                }
```

### DuplicatedCode
Duplicated code
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
        Builder builder = newBuilder();
        try {
          builder.mergeFrom(input, extensionRegistry);
        } catch (com.google.protobuf.InvalidProtocolBufferException e) {
          throw e.setUnfinishedMessage(builder.buildPartial());
        } catch (com.google.protobuf.UninitializedMessageException e) {
          throw e.asInvalidProtocolBufferException().setUnfinishedMessage(builder.buildPartial());
        } catch (java.io.IOException e) {
          throw new com.google.protobuf.InvalidProtocolBufferException(e)
              .setUnfinishedMessage(builder.buildPartial());
        }
        return builder.buildPartial();
```

### DuplicatedCode
Duplicated code
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
        if (queryIdBuilder_ == null) {
          if (value == null) {
            throw new NullPointerException();
          }
          queryId_ = value;
          onChanged();
        } else {
          queryIdBuilder_.setMessage(value);
        }
        bitField0_ |= 0x00000001;
        return this;
```

## RuleId[id=CollectionContainsUrl]
### CollectionContainsUrl
Map `pluginURLMap` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
   */
  private void applyFormatPlugins(StoragePlugins bootstrapPlugins,
      Map<String, URL> pluginURLMap) throws IOException {
    String formatBootstrapFileName = context.config().getString(
        ExecConstants.BOOTSTRAP_FORMAT_PLUGINS_FILE);
```

### CollectionContainsUrl
Set `formatUrls` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
    String formatBootstrapFileName = context.config().getString(
        ExecConstants.BOOTSTRAP_FORMAT_PLUGINS_FILE);
    Set<URL> formatUrls = ClassPathScanner.forResource(formatBootstrapFileName, false);
    if (formatUrls == null) {
      return;
```

### CollectionContainsUrl
Map `pluginURLMap` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java

  @VisibleForTesting
  protected StoragePlugins loadBootstrapPlugins(Map<String, URL> pluginURLMap) throws IOException {
    // bootstrap load the config since no plugins are stored.
    String storageBootstrapFileName = context.config().getString(
```

### CollectionContainsUrl
Set `storageUrls` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
    String storageBootstrapFileName = context.config().getString(
        ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE);
    Set<URL> storageUrls = ClassPathScanner.forResource(storageBootstrapFileName, false);
    if (storageUrls == null || storageUrls.isEmpty()) {
      throw new IOException("Cannot find storage plugin boostrap file: " + storageBootstrapFileName);
```

### CollectionContainsUrl
Map `pluginURLMap` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
  @Override
  public StoragePlugins bootstrapPlugins() throws IOException {
    Map<String, URL> pluginURLMap = new HashMap<>();
    StoragePlugins bootstrapPlugins = loadBootstrapPlugins(pluginURLMap);

```

### CollectionContainsUrl
Map `pluginURLMap` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
   */
  private void loadStoragePlugins(URL url, StoragePlugins bootstrapPlugins,
      Map<String, URL> pluginURLMap) throws IOException {
    StoragePlugins plugins = getPluginsFromResource(url);
    plugins.forEach(plugin -> {
```

### CollectionContainsUrl
Map `pluginURLMap` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
   */
  private void loadFormatPlugins(URL url, StoragePlugins bootstrapPlugins,
      Map<String, URL> pluginURLMap) throws IOException {
    StoragePlugins plugins = getPluginsFromResource(url);
    for (Entry<String, StoragePluginConfig> sourceEntry : plugins) {
```

### CollectionContainsUrl
Set `urlSet` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
    String upgradeFileName = context.config().getString(
        ExecConstants.UPGRADE_STORAGE_PLUGINS_FILE);
    Set<URL> urlSet = ClassPathScanner.forResource(upgradeFileName, false);
    if (urlSet.isEmpty()) {
      logger.trace(
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillSqlLineApplication.java`
#### Snippet
```java

  private Config loadConfig(String configName) {
    Set<URL> urls = ClassPathScanner.forResource(configName, false);
    if (urls.size() != 1) {
      if (logger.isDebugEnabled()) {
```

### CollectionContainsUrl
Set `markedPath` may contain URL objects
in `common/src/main/java/org/apache/drill/common/scanner/RunTimeScan.java`
#### Snippet
```java
   * @return the scan result with list of packages, classes and annotations found
   */
  public static ScanResult dynamicPackageScan(DrillConfig config, Set<URL> markedPath) {
    List<String> packagePrefixes = ClassPathScanner.getPackagePrefixes(config);
    return ClassPathScanner.scan(
```

### CollectionContainsUrl
Set `resultUrlSet` may contain URL objects
in `common/src/main/java/org/apache/drill/common/scanner/ClassPathScanner.java`
#### Snippet
```java
    logger.debug("Scanning classpath for resources with pathname \"{}\".",
                 resourcePathname);
    final Set<URL> resultUrlSet = new HashSet<>();
    final ClassLoader classLoader = ClassPathScanner.class.getClassLoader();
    try {
```

### CollectionContainsUrl
Set `markedPaths` may contain URL objects
in `common/src/main/java/org/apache/drill/common/scanner/BuildTimeScan.java`
#### Snippet
```java
    }
    URL url = new URL("file:" + basePath);
    Set<URL> markedPaths = ClassPathScanner.getMarkedPaths(ConfigConstants.DRILL_JAR_MARKER_FILE_RESOURCE_PATHNAME);
    if (!markedPaths.contains(url)) {
      throw new IllegalArgumentException(url + " not in " + markedPaths);
```

### CollectionContainsUrl
Set `preScanned` may contain URL objects
in `common/src/main/java/org/apache/drill/common/scanner/BuildTimeScan.java`
#### Snippet
```java
   */
  private static ScanResult loadExcept(URL ignored) {
    Set<URL> preScanned = ClassPathScanner.forResource(REGISTRY_FILE, false);
    ScanResult result = null;
    for (URL u : preScanned) {
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ObjectVector.java`
#### Snippet
```java
    }

    protected void set(int index, ObjectHolder holder) {
      set(index, holder.obj);
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   */

  protected List<Task> pendingTasks = new LinkedList<>();

  /**
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   */

  protected Map<ContainerId, Task> activeContainers = new HashMap<>();

  public SchedulerStateImpl(ClusterControllerImpl controller,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   */

  protected List<Task> allocatingTasks = new LinkedList<>();

  /**
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(FormatPluginOptionsDescriptor.class);

  protected final Class<? extends FormatPluginConfig> pluginConfigClass;
  protected final String typeName;
  private final Map<String, TableParamDef> functionParamsByName;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
   * @return the signature
   */
  protected TableSignature getTableSignature(String tableName, List<TableParamDef> tableParameters) {
    return TableSignature.of(tableName, tableParameters, params());
  }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
   * @return a readable String of the parameters and their names
   */
  protected String presentParams() {
    StringBuilder sb = new StringBuilder("(");
    List<TableParamDef> params = params();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java

  protected final Class<? extends FormatPluginConfig> pluginConfigClass;
  protected final String typeName;
  private final Map<String, TableParamDef> functionParamsByName;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthColumnReaders.java`
#### Snippet
```java

    // store a hard reference to the vector (which is also stored in the superclass) to prevent repetitive casting
    protected final NullableVarCharVector.Mutator mutator;
    private final NullableVarCharVector vector;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRestServer.java`
#### Snippet
```java
    register(new AbstractBinder() {
      @Override
      protected void configure() {
        DrillbitContext context = workManager.getContext();
        bind(drillbit).to(Drillbit.class);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillSqlSetOption.java`
#### Snippet
```java

  @Override
  protected void unparseAlterOperation(SqlWriter writer, int leftPrec, int rightPrec) {
    writer.keyword("SET");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillSqlResetOption.java`
#### Snippet
```java

  @Override
  protected void unparseAlterOperation(SqlWriter writer, int leftPrec, int rightPrec) {
    writer.keyword("RESET");

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/NoOpControlsInjector.java`
#### Snippet
```java
   * @param clazz the owning class
   */
  protected NoOpControlsInjector(final Class<?> clazz) {
    this.clazz = clazz;
  }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ResettableBarrier.java`
#### Snippet
```java
    }

    protected void reset() {
      setState(1);
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  // than throwing an exception.

  protected boolean hasCapacity(int index, int fieldLength) {
    assert fieldLength >= 0;
    return (! (index < 0 || index > capacity() - fieldLength));
```

## RuleId[id=ThreadRun]
### ThreadRun
Calls to `run()` should probably be replaced with 'start()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/TestMemoryRetention.java`
#### Snippet
```java
        d.start();
      } else {
        d.run();
      }
    }
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedFixedWidthVectorLike.java`
#### Snippet
```java
  void allocateNew(int valueCount, int innerValueCount);

  /**
   * Load the records in the provided buffer based on the given number of values.
   * @param valueCount   Number of separate repeating groupings.
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/plain/PlainFactory.java`
#### Snippet
```java

    return FastSaslClientFactory.getInstance().createSaslClient(new String[]{SIMPLE_NAME},
        null /** authorization ID */, null, null, properties, new CallbackHandler() {
          @Override
          public void handle(final Callback[] callbacks) throws IOException, UnsupportedCallbackException {
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/RegexpUtil.java`
#### Snippet
```java
package org.apache.drill.exec.expr.fn.impl;

/**
 * Code copied from optiq (https://github.com/julianhyde/optiq).
 * Thanks goes to Julian Hyde and other contributors of optiq.
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/BufferedDirectBufInputStream.java`
#### Snippet
```java
  }

  /**
   * Uncomment For testing Parquet files that are too big to use in unit tests
   * @param args
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java
   */
  public Ack cancelFragment(final FragmentHandle handle) {
    /**
     * For case 1, see {@link org.apache.drill.exec.work.foreman.QueryManager#cancelExecutingFragments}.
     * In comments below, "active" refers to fragment states: SENDING, AWAITING_ALLOCATION, RUNNING and
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    final MinorType minorType = majorType.getMinorType();

    /**
     * Defaults to "DRILL" as drill has as only one catalog.
     */
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setCatalogName(InfoSchemaConstants.IS_CATALOG_NAME);

    /**
     * Designated column's schema name. Empty string if not applicable. Initial
     * implementation defaults to empty string as we use LIMIT 0 queries to get
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setSchemaName("");

    /**
     * Designated column's table name. Not set if not applicable. Initial
     * implementation defaults to empty string as we use LIMIT 0 queries to get
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setColumnName(field.getNamePart().getName());

    /**
     * Column label name for display or print purposes.
     * Ex. a column named "empName" might be labeled as "Employee Name".
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setLabel(field.getNamePart().getName());

    /**
     * Data type in string format. Value is SQL standard type.
     */
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setIsNullable(majorType.getMode() == DataMode.OPTIONAL);

    /**
     * For numeric data, this is the maximum precision.
     * For character data, this is the length in characters.
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setPrecision(Types.getPrecision(field.getMajorType()));

    /**
     * Column's number of digits to right of the decimal point. 0 is returned
     * for types where the scale is not applicable
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setScale(Types.getScale(majorType));

    /**
     * Indicates whether values in the designated column are signed numbers.
     */
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setSigned(Types.isNumericType(majorType));

    /**
     * Maximum number of characters required to display data from the column.
     */
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setDisplaySize(Types.getJdbcDisplaySize(majorType));

    /**
     * Is the column an aliased column. Initial implementation defaults to true
     * as we derive schema from LIMIT 0 query and not plan
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
    builder.setSortable(Types.isSortable(minorType));

    /**
     * Returns the fully-qualified name of the Java class whose instances are manufactured if the method
     * ResultSet.getObject is called to retrieve a value from the column. Applicable only to JDBC clients.
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java
import java.io.IOException;

/*******************************************************************************
 * Portions Copyright 2014 uniVocity Software Pty Ltd
 ******************************************************************************/
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/FallbackOptionManager.java`
#### Snippet
```java

  public FallbackOptionManager(OptionManager fallback) {
    /**
     * TODO(DRILL-2097): Add a Preconditions.checkNotNull(fallback, "A fallback manager must be provided.") and remove
     * the null check in {@link #getOption(String)}. This is not added currently only to avoid modifying the long list
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/DFSDirPartitionLocation.java`
#### Snippet
```java
 * limitations under the License.
 */
/**
 * Class defines a single partition corresponding to a directory in a DFS table.
 */
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java
 * limitations under the License.
 */
/**
  * Licensed to the Apache Software Foundation (ASF) under one
  * or more contributor license agreements.  See the NOTICE file
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
    }
    //TODO check whether to enable RuntimeFilter according to the NDV percent
    /**
     double threshold = 0.5;
     double percent = leftNDV / rightDNV;
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
        }

        /**
         * Hold onto the previous incoming batch. When the incoming uses an
         * SV4, the InternalBatch DOES NOT clone or transfer the data. Instead,
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    Set<MinorType> rule;

    /** TINYINT cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.TINYINT, rule);

    /** SMALLINT cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.SMALLINT, rule);

    /** INT cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.INT, rule);

    /** BIGINT cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.BIGINT, rule);

    /** UINT4 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.UINT4, rule);

    /** UINT8 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.UINT8, rule);

    /** DECIMAL9 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DECIMAL9);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.DECIMAL9, rule);

    /** DECIMAL18 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DECIMAL18);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.DECIMAL18, rule);

    /** DECIMAL28Dense cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DECIMAL28DENSE);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.DECIMAL28DENSE, rule);

    /** DECIMAL28Sparse cast able from **/

    rule = new HashSet<>();
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.VARDECIMAL, rule);

    /** DECIMAL38Dense cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DECIMAL38DENSE);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java


    /** DECIMAL38Sparse cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DECIMAL38SPARSE);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.DECIMAL38SPARSE, rule);

    /** MONEY cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.MONEY, rule);

    /** DATE cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.DATE);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.DATE, rule);

    /** TIME cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TIME);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.TIME, rule);

    /** TIMESTAMP cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.VAR16CHAR);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.TIMESTAMP, rule);

    /** TIMESTAMPTZ cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TIMESTAMPTZ);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.TIMESTAMPTZ, rule);

    /** Interval cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.INTERVAL);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.INTERVAL, rule);

    /** INTERVAL YEAR cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.INTERVALYEAR);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.INTERVALYEAR, rule);

    /** INTERVAL DAY cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.INTERVALDAY);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.INTERVALDAY, rule);

    /** FLOAT4 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.FLOAT4, rule);

    /** FLOAT8 cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.FLOAT8, rule);

    /** BIT cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.BIT, rule);

    /** FIXEDCHAR cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.FIXEDCHAR, rule);

    /** FIXED16CHAR cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.FIXED16CHAR, rule);

    /** FIXEDBINARY cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.FIXEDBINARY, rule);

    /** VARCHAR cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.VARCHAR, rule);

    /** VAR16CHAR cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rules.put(MinorType.VAR16CHAR, rule);

    /** VARBINARY cast able from **/
    rule = new HashSet<>();
    rule.add(MinorType.TINYINT);
```

### DanglingJavadoc
Dangling Javadoc comment
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java

  @Override
  /** Returns the number of rows satisfying the given FILTER condition
   *  @param condition - FILTER specified as a {@link RexNode}
   *  @param tabIdxName - The table/index identifier
```

### DanglingJavadoc
Dangling Javadoc comment
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
    }

    /**
     * We've arrived here without a connection, let's make sure only one of us makes a connection. (fyi, another
     * endpoint could create a reverse connection
```

### DanglingJavadoc
Dangling Javadoc comment
in `common/src/main/java/org/apache/drill/common/config/DrillProperties.java`
#### Snippet
```java

  public static final ImmutableSet<String> ACCEPTED_BY_SERVER = ImmutableSet.of(
      USER /** deprecated */, PASSWORD /** deprecated */,
      SCHEMA,
      IMPERSONATION_TARGET,
```

### DanglingJavadoc
Dangling Javadoc comment
in `common/src/main/java/org/apache/drill/common/config/DrillProperties.java`
#### Snippet
```java

  public static final ImmutableSet<String> ACCEPTED_BY_SERVER = ImmutableSet.of(
      USER /** deprecated */, PASSWORD /** deprecated */,
      SCHEMA,
      IMPERSONATION_TARGET,
```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `ruleBuilder` declared in one 'switch' branch and used in another
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
      case PARTITION_PRUNING:
        final String defaultPartitionValue = hiveConf.get(ConfVars.DEFAULTPARTITIONNAME.varname);
        ImmutableSet.Builder<StoragePluginOptimizerRule> ruleBuilder = ImmutableSet.builder();
        ruleBuilder.add(HivePushPartitionFilterIntoScan.getFilterOnProject(optimizerContext, defaultPartitionValue));
        ruleBuilder.add(HivePushPartitionFilterIntoScan.getFilterOnScan(optimizerContext, defaultPartitionValue));
```

### SwitchStatementWithConfusingDeclaration
Local variable `buf` declared in one 'switch' branch and used in another
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
              pageHeaderInfo = DataPageHeaderInfoProvider.builder(pageHeader);
              valueReadSoFar += pageHeaderInfo.getNumValues();
              ByteBuf buf = allocator.buffer(compPageSize);
              lastPage = buf;
              ByteBuffer pageBuf = buf.nioBuffer(0, compPageSize);
```

### SwitchStatementWithConfusingDeclaration
Local variable `pageBuf` declared in one 'switch' branch and used in another
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
              ByteBuf buf = allocator.buffer(compPageSize);
              lastPage = buf;
              ByteBuffer pageBuf = buf.nioBuffer(0, compPageSize);
              HadoopStreams.wrap(in).readFully(pageBuf);
              pageBuf.flip();
```

### SwitchStatementWithConfusingDeclaration
Local variable `stats` declared in one 'switch' branch and used in another
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
              BytesInput pageBytes = decompressor.decompress(BytesInput.from(pageBuf), pageSize);

              Statistics stats = METADATA_CONVERTER.fromParquetStatistics(
                null,
                pageHeaderInfo.getStatistics(),
```

### SwitchStatementWithConfusingDeclaration
Local variable `dateFormat` declared in one 'switch' branch and used in another
in `contrib/format-ltsv/src/main/java/org/apache/drill/exec/store/ltsv/LTSVBatchReader.java`
#### Snippet
```java
          case TIME:
            columnMetadata = rowWriter.tupleSchema().metadata(key);
            String dateFormat = columnMetadata.property("drill.format");

            if (Strings.isNullOrEmpty(dateFormat)) {
```

## RuleId[id=ComparatorResultComparison]
### ComparatorResultComparison
Comparison of compare method result with specific constant
in `exec/vector/src/main/java/org/apache/drill/exec/util/DecimalUtility.java`
#### Snippet
```java
    BigDecimal base = new BigDecimal(DIGITS_BASE);

    while (integerPart.compareTo(BigDecimal.ZERO) == 1) {
      // store the modulo as the integer value
      data.setInt(startIndex + destIndex * INTEGER_SIZE, integerPart.remainder(base).intValue());
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `exec/vector/src/main/java/org/apache/drill/exec/util/DecimalUtility.java`
#### Snippet
```java
    boolean sign = false;

    if ((data.getByte(startIndex) & 0x80) != 0) {
      sign = true;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePlugin.java`
#### Snippet
```java
      }
      if (storeConfig == null) {
        if (other.storeConfig != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePlugin.java`
#### Snippet
```java
          return false;
        }
      } else if (!storeConfig.equals(other.storeConfig)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePluginConfig.java`
#### Snippet
```java
    HiveStoragePluginConfig that = (HiveStoragePluginConfig) o;

    if (configProps != null ? !configProps.equals(that.configProps) : that.configProps != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataClientConnection.java`
#### Snippet
```java
    DataClientConnection other = (DataClientConnection) obj;
    if (id == null) {
      if (other.id != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataClientConnection.java`
#### Snippet
```java
        return false;
      }
    } else if (!id.equals(other.id)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserSession.java`
#### Snippet
```java
    if (temporaryTableName != null) {
      Table temporaryTable = SqlHandlerUtil.getTableFromSchema(drillSchema, temporaryTableName);
      if (temporaryTable != null && temporaryTable.getJdbcTableType() == Schema.TableType.TABLE) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlConnection.java`
#### Snippet
```java
    ControlConnection other = (ControlConnection) obj;
    if (id == null) {
      if (other.id != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlConnection.java`
#### Snippet
```java
        return false;
      }
    } else if (!id.equals(other.id)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/CodeGenerator.java`
#### Snippet
```java
    }
    if (generifiedCode == null) {
      if (other.generifiedCode != null){
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/CodeGenerator.java`
#### Snippet
```java
      }

    } else if (!generifiedCode.equals(other.generifiedCode)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (decExpr.getBigDecimal() != ((Decimal28Expression) value).getBigDecimal()) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (!decExpr.getBigDecimal().equals(((VarDecimalExpression) value).getBigDecimal())) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (decExpr.getPrecision() != ((Decimal18Expression) value).getPrecision()) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (!decExpr.getMajorType().equals(((Decimal38Expression) value).getMajorType())) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (decExpr.getPrecision() != ((Decimal9Expression) value).getPrecision()) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DirectExpression.java`
#### Snippet
```java
    DirectExpression other = (DirectExpression) obj;
    if (source == null) {
      if (other.source != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/DirectExpression.java`
#### Snippet
```java
        return false;
      }
    } else if (!source.equals(other.source)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
      }
      if (fieldId == null) {
        if (other.fieldId != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java
          return false;
        }
      } else if (!fieldId.equals(other.fieldId)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java

  private static boolean isComparableType(MajorType type) {
    if (type.getMinorType() == MinorType.MAP ||
        type.getMinorType() == MinorType.LIST ||
        type.getMode() == TypeProtos.DataMode.REPEATED ) {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
      }
      if (proxyUserName == null) {
        if (other.proxyUserName != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ImpersonationUtil.java`
#### Snippet
```java
          return false;
        }
      } else if (!proxyUserName.equals(other.proxyUserName)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
      }
      if (valueClass == null) {
        if (other.valueClass != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
          return false;
        }
      } else if (!valueClass.equals(other.valueClass)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/local/LocalClusterCoordinator.java`
#### Snippet
```java
      }
      if (id == null) {
        if (other.id != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/local/LocalClusterCoordinator.java`
#### Snippet
```java
          return false;
        }
      } else if (!id.equals(other.id)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
          }

          if ("YES".equalsIgnoreCase(c.IS_NULLABLE)) {
            columnBuilder.setIsNullable(true);
          } else {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/WorkspaceConfig.java`
#### Snippet
```java
      return false;
    }
    if (allowAccessOutsideWorkspace != other.allowAccessOutsideWorkspace) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
  public boolean supportsDirPruning() {
    if (isExpandedFully() || isExpandedPartial()) {
      if (!wasAllPartitionsPruned) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/ProfileIterator.java`
#### Snippet
```java
  private boolean hasAdminPrivileges(ExecutorFragmentContext context) {
    OptionManager options = context.getOptions();
    if (context.isUserAuthenticationEnabled() &&
        !ImpersonationUtil.hasAdminPrivileges(
            context.getQueryUserName(),
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/BitToUserConnectionIterator.java`
#### Snippet
```java
  private boolean hasAdminPrivileges(ExecutorFragmentContext context) {
    OptionManager options = context.getOptions();
    if (context.isUserAuthenticationEnabled() &&
        !ImpersonationUtil.hasAdminPrivileges(
          this.queryingUsername,
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/DrillbitIterator.java`
#### Snippet
```java
    String currentEpAddress = current.getAddress();
    int currentEpPort = current.getUserPort();
    if (currentEpAddress.equals(epAddress) && currentEpPort == epPort) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockStorageEngineConfig.java`
#### Snippet
```java
    MockStorageEngineConfig that = (MockStorageEngineConfig) o;

    if (url != null ? !url.equals(that.url) : that.url != null) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java
      } else {
        // Check if the older version of metadata file exists
        if (fs.exists(getOldMetadataPath(dir))) {
          return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
      // processing it in a bulk-manner or not.

      if (numValues >= minNumVals) {
        columnPrecInfo.bulkProcess = true;
      } else {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata.java`
#### Snippet
```java
    Path metadataDirFile = new Path(metadataParentDir, METADATA_DIRECTORIES_FILENAME);
    Path fileMetadataFile = new Path(metadataParentDir, METADATA_FILENAME);
    if (!fs.exists(summaryFile) && !fs.exists(metadataDirFile) && !fs.exists(fileMetadataFile)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/FixedWidthRepeatedReader.java`
#### Snippet
```java
      return true;
    }
    if (valuesReadInCurrentPass + pageReader.valuesReadyToRead + repeatedValuesInCurrentList >= valueVec.getValueCapacity()) {
      return true;
    } else {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthColumn.java`
#### Snippet
```java
                  SchemaElement schemaElement) throws ExecutionSetupException {
    super(parentReader, descriptor, columnChunkMetaData, fixedLength, v, schemaElement);
      if (!Collections.disjoint(columnChunkMetaData.getEncodings(), ColumnReader.DICTIONARY_ENCODINGS)) {
        usingDictionary = true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
    }
    if (secondaryFinal == null) {
      if (other.secondaryFinal != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
        return false;
      }
    } else if (!secondaryFinal.equals(other.secondaryFinal)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java

    public boolean isRepeatedList() {
      if (metadata.getType().getMinorType() == MinorType.LIST &&
        metadata.getDataMode() == DataMode.REPEATED) {
        return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/DrillbitContext.java`
#### Snippet
```java
  public boolean isForeman(DrillbitEndpoint endpoint) {
    DrillbitEndpoint foreman = getEndpoint();
    if (endpoint.getAddress().equals(foreman.getAddress()) &&
            endpoint.getUserPort() == foreman.getUserPort()) {
      return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
    }
    if (string_val == null) {
      if (other.string_val != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
        return false;
      }
    } else if (!string_val.equals(other.string_val)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassTransformer.java`
#### Snippet
```java
      }
      if (precompiled == null) {
        if (other.precompiled != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassTransformer.java`
#### Snippet
```java
          return false;
        }
      } else if (!precompiled.equals(other.precompiled)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassTransformer.java`
#### Snippet
```java
      }
      if (slash == null) {
        if (other.slash != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassTransformer.java`
#### Snippet
```java
          return false;
        }
      } else if (!slash.equals(other.slash)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/ExpandingConcurrentMap.java`
#### Snippet
```java
    @SuppressWarnings("unchecked") KEY key = (KEY) k;

    if (internalMap.containsKey(key)) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
      if (sqlNode instanceof SqlCall) {
        final SqlOperator operator = DrillCalciteWrapperUtility.extractSqlOperatorFromWrapper(((SqlCall) sqlNode).getOperator());
          if (operator == SqlStdOperatorTable.GROUPING
              || operator == SqlStdOperatorTable.GROUPING_ID
              || operator == SqlStdOperatorTable.GROUP_ID) {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
      if (sqlNode instanceof SqlCall) {
        final SqlOperator operator = DrillCalciteWrapperUtility.extractSqlOperatorFromWrapper(((SqlCall) sqlNode).getOperator());
        if (operator == SqlStdOperatorTable.ROLLUP
            || operator == SqlStdOperatorTable.CUBE
            || operator == SqlStdOperatorTable.GROUPING_SETS) {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java
  @Override
  public Boolean visitInputRef(RexInputRef inputRef) {
    if (inputRef.getIndex() == currentFieldIndex) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
      return true;
    }
    if ((rex instanceof RexCall) && "ITEM".equals(((RexCall)rex).getOperator().getName())) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/PathInExpr.java`
#### Snippet
```java

  private boolean preProcess(LogicalExpression inExpr) {
    if (pathsInExpr.containsKey(inExpr)) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/RexSeparator.java`
#### Snippet
```java
    protected boolean inputRefToPush(RexInputRef inputRef) {
      //this class will based on the schemaPath to decide what to push
      if (markMap.containsKey(inputRef) || inAcceptedPath) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    }

    if ((inputCollation != null) &&
         (inputCollation.satisfies(indexContext.getCollation()))) {
      return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/types/RelDataTypeDrillImpl.java`
#### Snippet
```java
      RelDataTypeDrillImpl other = (RelDataTypeDrillImpl) obj;
      if (holder == null) {
        if (other.holder != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/types/RelDataTypeDrillImpl.java`
#### Snippet
```java
          return false;
        }
      } else if (!holder.equals(other.holder)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
    if (indexContext.getCall().getPlanner().getRoot() instanceof RelSubset) {
      final RelSubset rootSet = (RelSubset) indexContext.getCall().getPlanner().getRoot();
      if (rootSet.getRelList().contains(rootInCall)) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
      }
    } else {
      if (indexContext.getCall().getPlanner().getRoot().equals(rootInCall)) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushLimitToScanRule.java`
#### Snippet
```java
      // to know the output type.
      // Cannot pushdown limit and offset in to flatten as long as we don't know data distribution in flattened field
      if (!limitRel.isPushDown() && (limitRel.getFetch() != null)
          && (!DrillRelOptUtil.isLimit0(limitRel.getFetch())
            || !DrillRelOptUtil.isProjectOutputSchemaUnknown(projectRel))
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushLimitToScanRule.java`
#### Snippet
```java
      // For now only applies to Parquet. And pushdown only applies to LIMIT but not OFFSET,
      // so if getFetch() return null no need to run this rule.
      if (scanRel.getGroupScan().supportsLimitPushdown() && (limitRel.getFetch() != null)) {
        return true;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
  private static boolean isConversionToSumZeroNeeded(SqlOperator sqlOperator, RelDataType type) {
    sqlOperator = DrillCalciteWrapperUtility.extractSqlOperatorFromWrapper(sqlOperator);
    if (sqlOperator instanceof SqlSumAggFunction
        && !type.isNullable()) {
      // If SUM(x) is not nullable, the validator must have determined that
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/FindPartitionConditions.java`
#### Snippet
```java
    }

    if (call.getOperator().getSyntax() == SqlSyntax.SPECIAL ||
        call.getOperator().getSyntax() == SqlSyntax.FUNCTION) {
      return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Fragment.java`
#### Snippet
```java
    }
    if (sendingExchange == null) {
      if (other.sendingExchange != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Fragment.java`
#### Snippet
```java
        return false;
      }
    } else if (!sendingExchange.equals(other.sendingExchange)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java
    if (supportsScan(scan)) {
      final Object selection = DrillRelOptUtil.getDrillTable(scan).getSelection();
      if (selection instanceof FormatSelection
          && ((FormatSelection)selection).supportsDirPruning()) {
        return true;  // Do directory-based pruning in Calcite logical
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPruleBase.java`
#### Snippet
```java

    double estimatedRightRowCount = RelMetadataQuery.instance().getRowCount(right);
    if (estimatedRightRowCount < PrelUtil.getSettings(join.getCluster()).getBroadcastThreshold()
        && ! left.getTraitSet().getTrait(DrillDistributionTraitDef.INSTANCE).equals(DrillDistributionTrait.SINGLETON)
        && (join.getJoinType() == JoinRelType.INNER || join.getJoinType() == JoinRelType.LEFT)
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/RowKeyJoinPrule.java`
#### Snippet
```java

    try {
      if (!settings.isRowKeyJoinConversionUsingHashJoin()) {
        // For now, lets assume rowkey join does not preserve collation
        createRangePartitionRightPlan(call, join, PhysicalJoinType.HASH_JOIN, true,
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
    // 2) the fetch() is null (indicating we have to fetch all the remaining rows starting from offset.
    if (!limit.isPushDown() && limit.getFetch() != null) {
      if (!(exchange instanceof SingleMergeExchangePrel)
          || !findRowKeyJoin(exchange)) {
        return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
    if (rel instanceof RelSubset) {
      if (((RelSubset) rel).getBest() != null) {
        if (findRowKeyJoin(((RelSubset) rel).getBest())) {
          return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
        }
      } else if (((RelSubset) rel).getOriginal() != null) {
        if (findRowKeyJoin(((RelSubset) rel).getOriginal())) {
          return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
    } else if (rel instanceof HepRelVertex) {
      if (((HepRelVertex) rel).getCurrentRel() != null) {
        if (findRowKeyJoin(((HepRelVertex) rel).getCurrentRel())) {
          return true;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/PrelSequencer.java`
#### Snippet
```java
      }
      if (root == null) {
        if (other.root != null) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/PrelSequencer.java`
#### Snippet
```java
          return false;
        }
      } else if (!root.equals(other.root)) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/PrelSequencer.java`
#### Snippet
```java
        return false;
      }
      if (opId != other.opId) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/InjectionSite.java`
#### Snippet
```java
    }

    if (!desc.equals(other.desc)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/store/NoWriteLocalStore.java`
#### Snippet
```java
  public boolean putIfAbsent(final String key, final V value) {
    final V old = store.putIfAbsent(key, value);
    if (old == null) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinUtils.java`
#### Snippet
```java

    // allow implicit cast if both the input types are varbinary/ varchar
    if ((input1 == MinorType.VARCHAR || input1 == MinorType.VARBINARY) &&
        (input2 == MinorType.VARCHAR || input2 == MinorType.VARBINARY)) {
      return true;
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortRecordBatchBuilder.java`
#### Snippet
```java
      .build(logger);
    }
    if (batches.keys().size() < 1) {
      assert false : "Invalid to have an empty set of batches with no schemas.";
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortImpl.java`
#### Snippet
```java
    // Make sure we don't exceed the maximum number of batches SV4 can address.

    if (bufferedBatches.size() > Character.MAX_VALUE) {
      return false; }

```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/RuntimeFilterRecordBatch.java`
#### Snippet
```java
      long hash = hash64.hash64Code(rowIndex, 0, fieldId);
      boolean contain = bloomFilter.find(hash);
      if (contain) {
        bitSet.set(rowIndex, true);
      } else {
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
          for (MaterializedField field : incomingSchema) {
            String incomingName = field.getName();
            if (refContainsStar) {
              addToResultMaps(incomingName, result, true); // allow dups since
                                                           // this is likely
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
        } else {
          String newName = expr.getPath();
          if (!refHasPrefix && !exprHasPrefix) {
            addToResultMaps(newName, result, true); // allow dups since this is
                                                    // likely top-level project
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
      for (VectorWrapper<?> wrapper : incomingBatch) {
        ValueVector vvIn = wrapper.getValueVector();
        if (k > result.outputNames.size() - 1) {
          assert false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
        SchemaPath originalPath = SchemaPath
            .getSimplePath(field.getName());
        if (k > result.outputNames.size() - 1) {
          assert false;
        }
```

### TrivialIf
`if` statement can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      }
      errorCount++;
      if (errorCount >= MAX_ERRORS) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/TcpHandshake.java`
#### Snippet
```java
   */
  public boolean isClosed() {
    if (currentSessionState == State.CLOSE_WAIT ||
      currentSessionState == State.FORCED_CLOSED ||
      currentSessionState == State.CLOSED ||
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatPluginConfig.java`
#### Snippet
```java
    } else if (!index.equals(other.index)) {
      return false;
    } else if (readTimestampWithZoneOffset != other.readTimestampWithZoneOffset) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatPluginConfig.java`
#### Snippet
```java
    } else if (!index.equals(other.index)) {
      return false;
    } else if (readTimestampWithZoneOffset != other.readTimestampWithZoneOffset) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushLimitIntoScan.java`
#### Snippet
```java
      if (scan.getGroupScan().supportsLimitPushdown()
            && !limit.isPushDown() && limit.getFetch() != null) {
        if ((scan.getGroupScan() instanceof JsonTableGroupScan
              && ((JsonTableGroupScan) scan.getGroupScan()).isIndexScan())
            || (scan.getGroupScan() instanceof RestrictedJsonTableGroupScan)) {
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushLimitIntoScan.java`
#### Snippet
```java
      // the output schema after evaluation is performed. When input has 0 row, Drill essentially does not have a way
      // to know the output type.
      if (!limitPrel.isPushDown() && (limitPrel.getFetch() != null)
          && (!DrillRelOptUtil.isLimit0(limitPrel.getFetch())
          || !DrillRelOptUtil.isProjectOutputSchemaUnknown(projectPrel))) {
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/TabletFragmentInfo.java`
#### Snippet
```java
    }
    if (tabletInfoImpl == null) {
      if (other.tabletInfoImpl != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/TabletFragmentInfo.java`
#### Snippet
```java
        return false;
      }
    } else if (!tabletInfoImpl.equals(other.tabletInfoImpl)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
  @Override
  public boolean supportsLimitPushdown() {
    if (maxRecordsToRead < 0) {
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
            .equals(other.getParentQueryId())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
            .equals(other.getSqlQuery())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
            .equals(other.getRawBody())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
            != other.getRpcType()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
            != other.getOk()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
            .equals(other.getEndpoint())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
            != other.getDistributedCache()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
            != other.getHttpPort()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      }
      if (!subType_.equals(other.subType_)) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
        if (channel_ != other.channel_) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
            != other.getAllowedCredit()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
            != other.getIsLastBatch()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
            != other.getRfIdentifier()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      if (!getAuthenticationMechanismsList()
          .equals(other.getAuthenticationMechanismsList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/TransportCheck.java`
#### Snippet
```java
    // Epoll is disabled by default (see distribution/src/resources/drill-env.sh) due to
    // https://github.com/netty/netty/issues/3539
    if (name.startsWith("linux") && SystemPropertyUtil.getBoolean(USE_LINUX_EPOLL, false)) {
      SUPPORTS_EPOLL = true;
    } else {
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoCnxnKey.java`
#### Snippet
```java
    }
    if (user == null) {
      if (other.user != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoCnxnKey.java`
#### Snippet
```java
        return false;
      }
    } else if (!user.equals(other.user)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            .equals(other.getMessage())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            .equals(other.getSender())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            != other.getReportTime()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      if (!getFragmentStatusList()
          .equals(other.getFragmentStatusList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            .equals(other.getSessionId())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      if (!getCollectorList()
          .equals(other.getCollectorList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            != other.getEnableDynamicFc()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      if (!getFragmentList()
          .equals(other.getFragmentList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      if (!getAuthenticationMechanismsList()
          .equals(other.getAuthenticationMechanismsList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
            .equals(other.getHandle())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/format-esri/src/main/java/org/apache/drill/exec/store/esri/ShpBatchReader.java`
#### Snippet
```java
    }
    boolean boolean_value = true;
    if (value == 0) {
      boolean_value = false;
    }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduStoragePluginConfig.java`
#### Snippet
```java
    KuduStoragePluginConfig other = (KuduStoragePluginConfig) obj;
    if (masterAddresses == null) {
      if (other.masterAddresses != null) {
        return false;
      }
```

### TrivialIf
`if` statement can be simplified
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduStoragePluginConfig.java`
#### Snippet
```java
        return false;
      }
    } else if (!masterAddresses.equals(other.masterAddresses)) {
      return false;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getJarList()
          .equals(other.getJarList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getScannedPluginsList()
          .equals(other.getScannedPluginsList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getPart2()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getErrorList()
          .equals(other.getErrorList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getMemoryFootprint()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getBufferLength()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getAffectedRowsCount()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
        if (status_ != other.status_) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            .equals(other.getQueueName())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            .equals(other.getOperatorTypeName())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getMinorFragmentProfileList()
          .equals(other.getMinorFragmentProfileList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getAffectedRowsCount()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            .equals(other.getChild())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getFunctionSignatureList()
          .equals(other.getFunctionSignatureList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            .equals(other.getUserName())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            .equals(other.getCause())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      if (!getParsingErrorList()
          .equals(other.getParsingErrorList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getLastProgress()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getIsNativeMethod()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
                other.getDoubleValue())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getEndRow()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
            != other.getSchemas()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getIsCurrency()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
          return false;
        }
        if (!hasTo()) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getSqlQuery())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
          return false;
        }
        if (!hasValue()) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        if (to_ != other.to_) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasQuery()) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      org.apache.drill.exec.proto.UserProtos.GetServerMetaReq other = (org.apache.drill.exec.proto.UserProtos.GetServerMetaReq) obj;

      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getMutable())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      if (!getTableTypeFilterList()
          .equals(other.getTableTypeFilterList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getCurrentSchema())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getConnect())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      if (!getPropertiesList()
          .equals(other.getPropertiesList())) return false;
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getEscape())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getServerHandle())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getServerInfo())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getMaxWrappedSize()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getSchemaNameFilter())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
        if (saslSupport_ != other.saslSupport_) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getValue())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getColumnNameFilter())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      @java.lang.Override
      public final boolean isInitialized() {
        if (!hasStatus()) {
          return false;
        }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getAutolimitRowcount()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      public final boolean isInitialized() {
        if (hasProperties()) {
          if (!getProperties().isInitialized()) {
            return false;
          }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getError())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      public final boolean isInitialized() {
        if (hasServerMeta()) {
          if (!getServerMeta().isInitialized()) {
            return false;
          }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getSplitPlan()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getMaximumResponses()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getType())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            != other.getColumnSize()) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getCatalogNameFilter())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
            .equals(other.getVersionQualifier())) return false;
      }
      if (!getUnknownFields().equals(other.getUnknownFields())) return false;
      return true;
    }
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'tokenTable'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/security/oauth/OAuthTokenCredentials.java`
#### Snippet
```java
  private final String tokenURI;

  private Optional<PersistentTokenTable> tokenTable;

  /**
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'credentials'
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkConnection.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SplunkConnection.class);

  private final Optional<UsernamePasswordCredentials> credentials;
  private final String scheme;
  private final String hostname;
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveScan.java`
#### Snippet
```java
        splitTypes.add(split.getType());
      }
      if (parts.size() <= 0) {
        parts = null;
      }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/AbstractMuxExchange.java`
#### Snippet
```java

    logger.debug("Minor fragment {}, receives data from following senders {}", minorFragmentId, senders);
    if (senders == null || senders.size() <= 0) {
      throw new IllegalStateException(String.format("Failed to find senders for receiver [%d]", minorFragmentId));
    }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/AbstractDeMuxExchange.java`
#### Snippet
```java

    List<MinorFragmentEndpoint> receivers = senderToReceiversMapping.get(minorFragmentId);
    if (receivers == null || receivers.size() <= 0) {
      throw new IllegalStateException(String.format("Failed to find receivers for sender [%d]", minorFragmentId));
    }
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
  private static String decodeUTF(byte[] input, int offset, int length) {
    try {
      return new String(input, offset, length, UTF_8);
    } catch (UnsupportedEncodingException e) {
      throw new DrillRuntimeException(e); // should never come to this
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
  private static String decodeUTF(byte[] input) {
    try {
      return new String(input, UTF_8);
    } catch (UnsupportedEncodingException e) {
      throw new DrillRuntimeException(e); // should never come to this
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
  private static byte[] encodeUTF(String input) {
    try {
      return input.getBytes(UTF_8);
    } catch (UnsupportedEncodingException e) {
      throw new DrillRuntimeException(e); // should never come to this
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    byte[] strBytes;
    try {
      strBytes = readString.getBytes("UTF-8");
    } catch (UnsupportedEncodingException e) {
      throw new DrillRuntimeException("Unable to read string value for field: " + fieldName, e);
```

## RuleId[id=NewObjectEquality]
### NewObjectEquality
New object is compared using '=='
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
    final List<MinorType> subtypes1 = type1.getSubTypeList();
    final List<MinorType> subtypes2 = type2.getSubTypeList();
    if (subtypes1 == subtypes2) { // Only occurs if both are null
      return true;
    }
```

## RuleId[id=AutoCloseableResource]
### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/AbstractMapVector.java`
#### Snippet
```java
   */
  protected void putVector(String name, ValueVector vector) {
    final ValueVector old = vectors.put(
        Preconditions.checkNotNull(name, "field name cannot be null").toLowerCase(),
        Preconditions.checkNotNull(vector, "vector cannot be null")
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/BitColumnReader.java`
#### Snippet
```java
  @Override
  public boolean getBoolean() {
    final BitVector.Accessor accessor = ((BitVector) vectorAccessor.vector()).getAccessor();
    final int readOffset = vectorIndex.offset();
    return accessor.get(readOffset) != 0;
```

### AutoCloseableResource
'DrillBuf' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/OffsetVectorReader.java`
#### Snippet
```java

  public long getEntry() {
    final DrillBuf buf = bufferAccessor.buffer();
    final int readOffset = vectorIndex.offset() * VALUE_WIDTH;
    long start = buf.getInt(readOffset);
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/BaseScalarReader.java`
#### Snippet
```java
    @Override
    public DrillBuf buffer() {
      BaseDataValueVector vector = va.vector();
      return vector.getBuffer();
    }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/AbstractScalarWriterImpl.java`
#### Snippet
```java
      .startObject(this)
      .attributeIdentity("vector", vector())
      .attribute("schema", vector().getField())
      .endObject();
  }
```

### AutoCloseableResource
'ValueVector \& UnionVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/UnionReaderImpl.java`
#### Snippet
```java
      typeAccessor = new UnionTypeHyperVectorAccessor(va);
    } else {
      UnionVector unionVector = va.vector();
      typeAccessor = new SingleVectorAccessor(unionVector.getTypeVector());
    }
```

### AutoCloseableResource
'AbstractMapVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      AbstractMapVector vector = mapAccessor.vector();
      return (T) vector.getChildByOrdinal(index);
    }
```

### AutoCloseableResource
'VariableWidthVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new VarWidthOffsetHyperVectorAccessor(varWidthAccessor);
    } else {
      VariableWidthVector vector = varWidthAccessor.vector();
      return new SingleVectorAccessor(vector.getOffsetVector());
    }
```

### AutoCloseableResource
'RepeatedValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      RepeatedValueVector vector = repeatedVectorAccessor.vector();
      return (T) vector.getOffsetVector();
    }
```

### AutoCloseableResource
'NullableVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      NullableVector vector = nullableAccessor.vector();
      return (T) vector.getBitsVector();
    }
```

### AutoCloseableResource
'NullableVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      NullableVector vector = nullableAccessor.vector();
      return (T) vector.getValuesVector();
    }
```

### AutoCloseableResource
'NullableVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new NullableBitsHyperVectorStateReader(nullableAccessor);
    } else {
      NullableVector vector = nullableAccessor.vector();
      return new SingleVectorAccessor(
          vector.getBitsVector());
```

### AutoCloseableResource
'NullableVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new NullableValuesHyperVectorAccessor(nullableAccessor);
    } else {
      NullableVector vector = nullableAccessor.vector();
      return new SingleVectorAccessor(
          vector.getValuesVector());
```

### AutoCloseableResource
'ListVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      ListVector vector = listAccessor.vector();
      return (T) vector.getDataVector();
    }
```

### AutoCloseableResource
'VariableWidthVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      VariableWidthVector vector = varWidthVectorAccessor.vector();
      return (T) vector.getOffsetVector();
    }
```

### AutoCloseableResource
'RepeatedValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      RepeatedValueVector vector = repeatedVectorAccessor.vector();
      return (T) vector.getDataVector();
    }
```

### AutoCloseableResource
'ListVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      ListVector vector = listAccessor.vector();
      return (T) vector.getBitsVector();
    }
```

### AutoCloseableResource
'RepeatedValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new ArrayOffsetHyperVectorAccessor(repeatedAccessor);
    } else {
      RepeatedValueVector vector = repeatedAccessor.vector();
      return new SingleVectorAccessor(vector.getOffsetVector());
    }
```

### AutoCloseableResource
'ValueVector \& UnionVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      UnionVector vector = unionVectorAccessor.vector();
      return (T) vector.getTypeVector();
    }
```

### AutoCloseableResource
'RepeatedValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new ArrayDataHyperVectorAccessor(repeatedAccessor);
    } else {
      RepeatedValueVector vector = repeatedAccessor.vector();
      return new SingleVectorAccessor(
          vector.getDataVector());
```

### AutoCloseableResource
'ValueVector \& UnionVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
    @Override
    public <T extends ValueVector> T vector() {
      UnionVector vector = unionVectorAccessor.vector();
      return vector.member(typeKey);
    }
```

### AutoCloseableResource
'ListVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
      return new ListBitsHyperVectorStateReader(nullableAccessor);
    } else {
      ListVector vector = nullableAccessor.vector();
      return new SingleVectorAccessor(
          vector.getBitsVector());
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseVarWidthWriter.java`
#### Snippet
```java
  @Override
  protected final void setBuffer() {
    drillBuf = vector().getBuffer();
    capacity = drillBuf.capacity();
  }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseVarWidthWriter.java`
#### Snippet
```java
  public final void endWrite() {
    fillEmpties();
    vector().getBuffer().writerIndex(offsetsWriter.nextOffset());
    offsetsWriter.endWrite();
  }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseVarWidthWriter.java`
#### Snippet
```java
  @Override
  public final void preRollover() {
    vector().getBuffer().writerIndex(offsetsWriter.rowStartOffset());
    offsetsWriter.preRollover();
  }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/AbstractFixedWidthWriter.java`
#### Snippet
```java
    mandatoryResize(valueCount - 1);
    fillEmpties(valueCount);
    vector().getBuffer().writerIndex(valueCount * width());

    // Last write index is either the last value we just filled,
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/AbstractFixedWidthWriter.java`
#### Snippet
```java
  @Override
  protected final void setBuffer() {
    drillBuf = vector().getBuffer();
    capacity = drillBuf.capacity() / width();
  }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/BaseScalarWriter.java`
#### Snippet
```java

  protected void realloc(int size) {
    vector().reallocRaw(size);
    setBuffer();
  }
```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/OffsetVectorWriterImpl.java`
#### Snippet
```java
      // Serialization code relies on this fact.

      vector().getBuffer().writerIndex(0);
    } else {

```

### AutoCloseableResource
'BaseDataValueVector' used without 'try'-with-resources statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/OffsetVectorWriterImpl.java`
#### Snippet
```java
      mandatoryResize(offsetCount);
      fillEmpties(valueCount - lastWriteIndex - 1);
      vector().getBuffer().writerIndex(offsetCount * VALUE_WIDTH);
    }
  }
```

### AutoCloseableResource
'Response' used without 'try'-with-resources statement
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java

    // Execute the request
    Response response = SIMPLE_CLIENT.newCall(request).execute();
    return response.body();
  }
```

### AutoCloseableResource
'ResponseBody' used without 'try'-with-resources statement
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
  public static InputStream getRequestAndStreamResponse(String url) {
    try {
      return makeSimpleGetRequest(url).byteStream();
    } catch (IOException e) {
      throw UserException
```

### AutoCloseableResource
'JsonParser' used without 'try'-with-resources statement
in `logical/src/main/java/org/apache/drill/common/JSONOptions.java`
#### Snippet
```java
      return null;
    }
    return mapper.treeAsTokens(root).readValueAs(t);
  }

```

### AutoCloseableResource
'DrillBuf' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/BufferManagerImpl.java`
#### Snippet
```java
  @Override
  public DrillBuf replace(DrillBuf old, int newSize) {
    if (managedBuffers.remove(old.memoryAddress()) == null) {
      throw new IllegalStateException("Tried to remove unmanaged buffer.");
    }
```

### AutoCloseableResource
'RuntimeFilterWritable' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
    joinMjId2AggregatedRF.put(joinMajorId, toAggregated);
    if (buildSideRfNumber == 0) {
      joinMjId2AggregatedRF.remove(joinMajorId);
      route(toAggregated);
      joinMjId2rfNumber.remove(joinMajorId);
```

### AutoCloseableResource
'Context' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorSerializer.java`
#### Snippet
```java
      checkNotNull(batch);
      checkNotNull(channel);
      final Timer.Context timerContext = metrics.timer(WRITER_TIMER).time();

      final DrillBuf[] incomingBuffers = batch.getBuffers();
```

### AutoCloseableResource
'Context' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
  public void writeToStream(OutputStream output) throws IOException {
    Preconditions.checkNotNull(output);
    final Timer.Context timerContext = metrics.timer(WRITER_TIMER).time();

    final DrillBuf[] incomingBuffers = batch.getBuffers();
```

### AutoCloseableResource
'StoragePlugin' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
      PluginHandle entry = restoreFromEphemeral(name, config);
      try {
        entry.plugin();
      } catch (UserException e) {
        // Provide helpful error messages.
```

### AutoCloseableResource
'StoragePlugin' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
        if (oldEntry == null || !oldEntry.config().isEnabled()) {
          // entry has the new plugin config attached to it so is appropriate for onEnabled
          entry.plugin().onEnabled();
        }
      } catch (Exception e) {
```

### AutoCloseableResource
'StoragePlugin' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
        if (oldEntry != null && oldEntry.config().isEnabled()) {
          // oldEntry has the old plugin config attached to it so is appropriate for onDisabled
          oldEntry.plugin().onDisabled();
        }
      } catch (Exception e) {
```

### AutoCloseableResource
'StoragePlugin' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
        entry = base.next();
        try {
          entry.plugin();
          return true;
        } catch (Exception e) {
```

### AutoCloseableResource
'DrillBuf' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetDirectByteBufferAllocator.java`
#### Snippet
```java
  @Override
  public void release(ByteBuffer byteBuffer) {
    final DrillBuf drillBuf = allocatedBuffers.remove(byteBuffer);
    // The ByteBuffer passed in may already have been freed or not allocated by this allocator.
    // If it is not found in the allocated buffers, do nothing
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ReadState.java`
#### Snippet
```java
      // there is no need to spend resources to init readers, when schema will be output
      for (ParquetColumnMetadata columnMetadata : schema.getColumnMetadata()) {
        columnMetadata.buildVector(output);
      }
    } else {
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ReadState.java`
#### Snippet
```java
          columnMetadata.columnChunkMetaData = rowGroupMetadata.getColumns().get(
            columnChunkMetadataPositionsInList.get(Arrays.toString(column.getPath())));
          columnMetadata.buildVector(output);
          if (!columnMetadata.isFixedLength()) {
            // create a reader and add it to the appropriate list
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
  public void copySchemaFrom(VectorAccessible other) {
    for (VectorWrapper<?> wrapper : other) {
      addOrGet(wrapper.getField());
    }
  }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
    this.allocator = allocator;
    for (MaterializedField field : schema) {
      addOrGet(field, null);
    }
    this.schema = schema;
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordIterator.java`
#### Snippet
```java
          if (!initialized) {
            for (VectorWrapper<?> w : rbd.getContainer()) {
              container.addOrGet(w.getField());
            }
            container.buildSchema(rbd.getContainer().getSchema().getSelectionVectorMode());
```

### AutoCloseableResource
'SystemOptionManager' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/Drillbit.java`
#### Snippet
```java
    storageRegistry = drillbitContext.getStorage();
    storageRegistry.init();
    drillbitContext.getOptionManager().init();
    javaPropertiesToSystemOptions();
    manager.getContext().getRemoteFunctionRegistry().init(context.getConfig(), storeProvider, coord);
```

### AutoCloseableResource
'Drillbit' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/Drillbit.java`
#### Snippet
```java
  public static void main(final String[] cli) throws DrillbitStartupException {
    final StartupOptions options = StartupOptions.parse(cli);
    start(options);
  }

```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DrillTableInfo.java`
#### Snippet
```java
        SqlIdentifier tableIdentifier = tableMacro.getSqlIdentifier();

        AbstractSchema drillSchema = SchemaUtilities.resolveToDrillSchema(
            config.getConverter().getDefaultSchema(), SchemaUtilities.getSchemaPath(tableIdentifier));

```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ShowTablesHandler.java`
#### Snippet
```java
    }

    AbstractSchema drillSchema = SchemaUtilities.unwrapAsDrillSchemaInstance(schemaPlus);

    SqlNode where = DrillParserUtil.createCondition(
```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeSchemaHandler.java`
#### Snippet
```java
    }

    AbstractSchema drillSchema = SchemaUtilities.unwrapAsDrillSchemaInstance(schemaPlus);
    StoragePlugin storagePlugin;
    try {
```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/MetastoreDropTableMetadataHandler.java`
#### Snippet
```java
    SqlDropTableMetadata dropTableMetadata = unwrap(sqlNode, SqlDropTableMetadata.class);

    AbstractSchema drillSchema = SchemaUtilities.resolveToDrillSchema(
        config.getConverter().getDefaultSchema(), dropTableMetadata.getSchemaPath());

```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeTableHandler.java`
#### Snippet
```java

      // find resolved schema path
      AbstractSchema drillSchema = SchemaUtilities.unwrapAsDrillSchemaInstance(schema);
      String schemaPath = drillSchema.getFullSchemaName();
      String tableName = Util.last(node.getTable().names);
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
        final Class vvClass = TypeHelper.getValueVectorClass(type.getMinorType(), type.getMode());
        try {
          output.addField(field, vvClass);
        } catch (SchemaChangeException e) {
          throw new ExecutionSetupException(e);
```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateAliasHandler.java`
#### Snippet
```java

  private String getStorageQualifier(List<String> path) {
    SchemaUtilities.resolveToDrillSchema(
      config.getConverter().getDefaultSchema(), path);
    if (path.size() > 1) {
```

### AutoCloseableResource
'AbstractSchema' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/TableModifyPrel.java`
#### Snippet
```java
    SchemaPlus schema = ((CalciteCatalogReader) getTable().getRelOptSchema()).getRootSchema().plus();

    ModifyTableEntry modifyTableEntry = SchemaUtilities.resolveToDrillSchema(schema, schemaPath)
      .modifyTable(tableName);

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/InsertWriterRecordBatch.java`
#### Snippet
```java
        Types.required(TypeProtos.MinorType.BIGINT));

    container.addOrGet(rowcountField);
  }
}
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/StatisticsWriterRecordBatch.java`
#### Snippet
```java
              Types.required(TypeProtos.MinorType.BIGINT));

      container.addOrGet(fragmentIdField);
      container.addOrGet(summaryField);
      container.buildSchema(BatchSchema.SelectionVectorMode.NONE);
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/StatisticsWriterRecordBatch.java`
#### Snippet
```java

      container.addOrGet(fragmentIdField);
      container.addOrGet(summaryField);
      container.buildSchema(BatchSchema.SelectionVectorMode.NONE);
    } finally {
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/WriterRecordBatch.java`
#### Snippet
```java
            Types.required(MinorType.BIGINT));

    container.addOrGet(fragmentIdField);
    container.addOrGet(summaryField);
  }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/WriterRecordBatch.java`
#### Snippet
```java

    container.addOrGet(fragmentIdField);
    container.addOrGet(summaryField);
  }

```

### AutoCloseableResource
'T' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
        for (String column : implicitColumnList.get(0).keySet()) {
          final MaterializedField field = MaterializedField.create(column, Types.optional(MinorType.VARCHAR));
          mutator.addField(field, NullableVarCharVector.class, true /*implicit field*/);
        }
      }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoinBatch.java`
#### Snippet
```java

        // Add the vector to the output container
        container.addOrGet(field);

        TypedFieldId inFieldId = new TypedFieldId.Builder().finalType(fieldType)
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoinBatch.java`
#### Snippet
```java

        MaterializedField newField = MaterializedField.create(field.getName(), outputType);
        container.addOrGet(newField);

        TypedFieldId inFieldId = new TypedFieldId.Builder().finalType(inputType)
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
        continue;
      }
      container.addOrGet(leftField);
    }

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
        rightField = rightField.withType(outputType);
      }
      container.addOrGet(rightField);
    }

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
        MaterializedField projected = field.withType(outputType);
        // Add the vector to our output container
        container.addOrGet(projected);
      }
    }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
            .setMode(leftField.getDataMode());
        builder = Types.calculateTypePrecisionAndScale(leftField.getType(), rightField.getType(), builder);
        container.addOrGet(MaterializedField.create(leftField.getName(), builder.build()), callBack);
      } else if (Types.isUntypedNull(rightField.getType())) {
        container.addOrGet(leftField, callBack);
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
        container.addOrGet(MaterializedField.create(leftField.getName(), builder.build()), callBack);
      } else if (Types.isUntypedNull(rightField.getType())) {
        container.addOrGet(leftField, callBack);
      } else if (Types.isUntypedNull(leftField.getType())) {
        container.addOrGet(MaterializedField.create(leftField.getName(), rightField.getType()), callBack);
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
        container.addOrGet(leftField, callBack);
      } else if (Types.isUntypedNull(leftField.getType())) {
        container.addOrGet(MaterializedField.create(leftField.getName(), rightField.getType()), callBack);
      } else {
        // If the output type is not the same,
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
        ));

        container.addOrGet(MaterializedField.create(leftField.getName(), builder.build()), callBack);
      }
      ++index;
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
  private void inferOutputFieldsOneSide(final BatchSchema schema) {
    for (MaterializedField field : schema) {
      container.addOrGet(field, callBack);
    }
  }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/RuntimeFilterRecordBatch.java`
#### Snippet
```java
    // Prepare the output container
    for (final VectorWrapper<?> v : incoming) {
      container.addOrGet(v.getField(), callBack);
    }

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/FrameSupportTemplate.java`
#### Snippet
```java
  private void allocateInternal() {
    for (VectorWrapper<?> w : container) {
      ValueVector vv = internal.addOrGet(w.getField());
      vv.allocateNew();
    }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/NoFrameSupportTemplate.java`
#### Snippet
```java
  private void allocateInternal() {
    for (VectorWrapper<?> w : container) {
      ValueVector vv = internal.addOrGet(w.getField());
      vv.allocateNew();
    }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      // add corresponding ValueVector to container
      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), aggregate.getMajorType());
      batch.addOrGet(output).allocateNew();
      TypedFieldId outputId = batch.getValueVectorId(ne.getRef());
      writeAggregationToOutput = new ValueVectorWriteExpression(outputId, aggregate, true);
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
        throws SchemaChangeException {
      final MaterializedField outputField = MaterializedField.create(ne.getRef().getAsNamePart().getName(), getMajorType());
      batch.addOrGet(outputField).allocateNew();
      fieldId = batch.getValueVectorId(ne.getRef());
      return true;
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      final MaterializedField output = MaterializedField.create(
          ne.getRef().getAsNamePart().getName(), input.getMajorType());
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId = batch.getValueVectorId(ne.getRef());

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java

      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), input.getMajorType());
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId = batch.getValueVectorId(ne.getRef());

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      final LogicalExpression argument = call.arg(0);
      final MaterializedField outputField = MaterializedField.create(ne.getRef().getAsNamePart().getName(), argument.getMajorType());
      batch.addOrGet(outputField).allocateNew();
      fieldId = batch.getValueVectorId(ne.getRef());

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      // add lag output ValueVector to container
      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), majorType);
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId = batch.getValueVectorId(ne.getRef());

```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFunction.java`
#### Snippet
```java
      // add corresponding ValueVector to container
      final MaterializedField output = MaterializedField.create(ne.getRef().getAsNamePart().getName(), majorType);
      batch.addOrGet(output).allocateNew();
      final TypedFieldId outputId =  batch.getValueVectorId(ne.getRef());

```

### AutoCloseableResource
'MapVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectRecordBatch.java`
#### Snippet
```java
    // We have to re-build the schema.
    if (rsLoader != null) {
      MapVector map = container.addOrGet(container.getLast().getField().getName(), Types.required(TypeProtos.MinorType.MAP), MapVector.class);
      map.setMapValueCount(recordCount);
      for (VectorWrapper<?> vectorWrapper : rsLoader.harvest()) {
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unpivot/UnpivotMapsRecordBatch.java`
#### Snippet
```java
        MaterializedField mf = MaterializedField.create(colName, mt);
        container.add(TypeHelper.getNewVector(mf, oContext.getAllocator()));
        copySrcVecMap.put(mf, vw.getValueVector());
        continue;
      }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unpivot/UnpivotMapsRecordBatch.java`
#### Snippet
```java
      assert mapVector.getPrimitiveVectors().size() > 0;

      MajorType mt = mapVector.iterator().next().getField().getType();
      MaterializedField mf = MaterializedField.create(colName, mt);
      assert !dataSrcVecMap.containsKey(mf);
```

### AutoCloseableResource
'BitVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataControllerBatch.java`
#### Snippet
```java
    container.clear();
    BitVector bitVector =
        container.addOrGet(MetastoreAnalyzeConstants.OK_FIELD_NAME, Types.required(TypeProtos.MinorType.BIT), null);
    VarCharVector varCharVector =
        container.addOrGet(MetastoreAnalyzeConstants.SUMMARY_FIELD_NAME, Types.required(TypeProtos.MinorType.VARCHAR), null);
```

### AutoCloseableResource
'ValueVector \& VarCharVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataControllerBatch.java`
#### Snippet
```java
        container.addOrGet(MetastoreAnalyzeConstants.OK_FIELD_NAME, Types.required(TypeProtos.MinorType.BIT), null);
    VarCharVector varCharVector =
        container.addOrGet(MetastoreAnalyzeConstants.SUMMARY_FIELD_NAME, Types.required(TypeProtos.MinorType.VARCHAR), null);

    bitVector.allocateNew();
```

### AutoCloseableResource
'ValueVector \& VarCharVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataHandlerBatch.java`
#### Snippet
```java
  private IterOutcome doWorkInternal() {
    container.transferIn(incoming.getContainer());
    VarCharVector metadataTypeVector = container.addOrGet(
        MaterializedField.create(MetastoreAnalyzeConstants.METADATA_TYPE, Types.required(MinorType.VARCHAR)));
    metadataTypeVector.allocateNew();
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggBatch.java`
#### Snippet
```java
      MaterializedField outputField = MaterializedField.create(ne.getRef().getLastSegment().getNameSegment().getPath(),
                                                                      expr.getMajorType());
      container.addOrGet(outputField);
      keyOutputIds[i] = container.getValueVectorId(ne.getRef());
    }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggBatch.java`
#### Snippet
```java
        MaterializedField outputField = MaterializedField.create(ne.getRef().getLastSegment().getNameSegment().getPath(),
            expr.getMajorType());
        container.addOrGet(outputField);
        TypedFieldId id = container.getValueVectorId(ne.getRef());
        valueExprs[i] = new ValueVectorWriteExpression(id, expr, true);
```

### AutoCloseableResource
'Writer' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java

      try {
        writers[part] = spillSet.writer(spillFiles[part]);
      } catch (IOException ioe) {
        throw UserException.resourceError(ioe)
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
      tempBatchHolder[i] = batch;
      for (final SerializedField field : batch.getHeader().getDef().getFieldList()) {
        final ValueVector v = container.addOrGet(MaterializedField.create(field));
        v.allocateNew();
      }
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java

        // allocate a new value vector
        container.addOrGet(v.getField());
      }
      allocateOutgoing();
```

### AutoCloseableResource
'AbstractMapVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java
    @Override
    public int addOutputColumn(ValueVector vector, ColumnMetadata colSchema) {
      final AbstractMapVector mapVector = parentColumn.vector();
      if (isVersioned()) {
        mapVector.putChild(colSchema.name(), vector);
```

### AutoCloseableResource
'RepeatedDictVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java
    @Override
    public int addOutputColumn(ValueVector vector, ColumnMetadata colSchema) {
      final RepeatedDictVector repeatedDictVector = parentColumn.vector();
      DictVector dictVector = (DictVector) repeatedDictVector.getDataVector();
      if (isVersioned()) {
```

### AutoCloseableResource
'AbstractMapVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/TupleState.java`
#### Snippet
```java

      if (! isVersioned()) {
        final AbstractMapVector mapVector = parentColumn.vector();
        mapVector.putChild(colState.schema().name(), colState.vector());
      }
```

### AutoCloseableResource
'ListVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ListState.java`
#### Snippet
```java
    // Add the member to the list as its data vector.

    listVector().setChildVector(colState.vector());

    // Don't add the type to the vector state; we manage it as part
```

### AutoCloseableResource
'ListVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ListState.java`
#### Snippet
```java
  private void addSecondType(ColumnState colState) {
    final UnionWriterImpl unionWriter = unionWriter();
    final ListVector listVector = listVector();

    // Going from one type to a union
```

### AutoCloseableResource
'ValueVector' used without 'try'-with-resources statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/hyper/HyperReaderBuilder.java`
#### Snippet
```java
    boolean isArray = metadata.isArray();

    ValueVector vector = va.vector();
    VectorAccessor dictAccessor;
    if (isArray) {
```

### AutoCloseableResource
'QueryExecutorProvider' used without 'try'-with-resources statement
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/operate/RdbmsModify.java`
#### Snippet
```java
   */
  private void executeOperations(List<RdbmsOperation> operations) {
    try (DSLContext executor = context.executorProvider().executor()) {
      executor.transaction(configuration -> {
        DSLContext transactionalExecutor = DSL.using(configuration);
```

### AutoCloseableResource
'QueryExecutorProvider' used without 'try'-with-resources statement
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/operate/RdbmsRead.java`
#### Snippet
```java

    List<T> units = new ArrayList<>();
    try (DSLContext executor = context.executorProvider().executor()) {
      for (MetadataMapper<T, ? extends Record> mapper : mappers) {
        Condition condition = mapper.toCondition(filter);
```

### AutoCloseableResource
'Admin' used without 'try'-with-resources statement
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBIndexDiscover.java`
#### Snippet
```java
    try {
      Set<DrillIndexDescriptor> idxSet = new HashSet<>();
      Collection<IndexDesc> indexes = admin().getTableIndexes(new Path(tableName));
      if (indexes.size() == 0 ) {
        logger.error("No index returned from Admin.getTableIndexes for table {}", tableName);
```

### AutoCloseableResource
'DrillClient' used without 'try'-with-resources statement
in `contrib/storage-drill/src/main/java/org/apache/drill/exec/store/drill/plugin/DrillStoragePlugin.java`
#### Snippet
```java

  public DrillClient getClient(String userName) {
    userClients.computeIfAbsent(userName, this::createClient);
    // recompute drill client for the case of closed connection
    return userClients.computeIfPresent(userName, (name, value) -> {
```

### AutoCloseableResource
'CapitalizingJdbcSchema' used without 'try'-with-resources statement
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/JdbcCatalogSchema.java`
#### Snippet
```java
    if (connectionSchemaName == null ||
        (connSchema = schemaMap.get(connectionSchemaName.toLowerCase())) == null) {
      connSchema = schemaMap.values().iterator().next();
    }
    return connSchema.getDefaultSchema();
```

### AutoCloseableResource
'CapitalizingJdbcSchema' used without 'try'-with-resources statement
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/CapitalizingJdbcSchema.java`
#### Snippet
```java
  public CapitalizingJdbcSchema getDefaultSchema() {
    return isCatalogSchema()
        ? schemaMap.values().iterator().next().getDefaultSchema()
        : this;
  }
```

## RuleId[id=EmptyInitializer]
### EmptyInitializer
Empty class initializer
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        private static final java.util.HashMap<java.lang.String,java.lang.Integer> fieldMap = new java.util.HashMap<java.lang.String,java.lang.Integer>();
        static
        {
        }
    }
```

## RuleId[id=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator never returns positive values
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyList.java`
#### Snippet
```java
    }

    public int compareTo(final Node argNode) {
      // just do an identity compare since elsewhere you should ensure that only one node exists for each nodeValue.
      return argNode == this ? 0 : -1;
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `logical/src/main/java/org/apache/drill/common/expression/visitors/ConditionalExprOptimizer.java`
#### Snippet
```java
    @Override
    public int compare(LogicalExpression e1, LogicalExpression e2) {
      return e1.getCumulativeCost() <= e2.getCumulativeCost() ? -1 : 1;
    }
  };
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataHandlerBatch.java`
#### Snippet
```java
        arguments.add(((SegmentMetadata) metadata).getLocations().stream()
            .map(path -> path.toUri().getPath())
            .toArray(String[]::new));
      }

```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataHandlerBatch.java`
#### Snippet
```java
            arguments.add(((SegmentMetadata) metadata).getLocations().stream()
                .map(path -> path.toUri().getPath())
                .toArray(String[]::new));
          } else {
            arguments.add(null);
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `container` to `MapVector` is redundant
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/ComplexWriterImpl.java`
#### Snippet
```java

    case INIT:
      MapVector map = (MapVector) container;
      mapRoot = new SingleMapWriter(map, this, unionEnabled);
      mapRoot.setPosition(idx());
```

### RedundantCast
Casting `value` to `String` is redundant
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
    return
        "\""
        + ( ((String) value)
             .replace( "\\", "\\\\" )  // first encode backslashes (esc. char.)
             .replace( "\"", "\\\"" )  // then encode quotes (via backslash)
```

### RedundantCast
Casting `null` to `Object` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebUiPageTree.java`
#### Snippet
```java
      }

      return new Viewable("/drill-am/login.ftl", toModel(sc, (Object) null));
    }

```

### RedundantCast
Casting `reader.readValue(...)` to `MSG` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
    @Override
    public MSG deserializeReceived(byte[] bytes) throws Exception {
      return (MSG) reader.readValue(bytes);
    }
  }
```

### RedundantCast
Casting `value` to `Decimal38Expression` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EqualityVisitor.java`
#### Snippet
```java
      return false;
    }
    if (!decExpr.getMajorType().equals(((Decimal38Expression) value).getMajorType())) {
      return false;
    }
```

### RedundantCast
Casting `materializedField` to `org.apache.drill.exec.record.MaterializedField` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
      for (org.apache.drill.exec.record.MaterializedField materializedField : columns.values()) {
        // Janino compiler cannot infer types from generics :(
        schemaBuilder.add((org.apache.drill.exec.record.MaterializedField) materializedField);
      }

```

### RedundantCast
Casting `columns.get(...)` to `org.apache.drill.exec.record.MaterializedField` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
        // Janino cannot infer type
        org.apache.drill.exec.record.MaterializedField materializedField =
            (org.apache.drill.exec.record.MaterializedField) columns.get(columnName);
        org.apache.drill.common.types.TypeProtos.MajorType type = inputs[i + 1].getType();
        if (materializedField != null && !materializedField.getType().equals(type)) {
```

### RedundantCast
Casting `entry.getValue()` to `String` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseUrlFunction.java`
#### Snippet
```java
          if (entry.getValue() != null) {
            // Explicit casting to String is required because of Janino's limitations regarding generics.
            byte[] protocolBytes = ((String) entry.getValue()).getBytes(java.nio.charset.StandardCharsets.UTF_8);
            outBuffer = outBuffer.reallocIfNeeded(protocolBytes.length);
            outBuffer.setBytes(0, protocolBytes);
```

### RedundantCast
Casting `entry.getValue()` to `String` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ParseUrlFunction.java`
#### Snippet
```java
          if (entry.getValue() != null) {
            // Explicit casting to String is required because of Janino's limitations regarding generics.
            byte[] protocolBytes = ((String) entry.getValue()).getBytes(java.nio.charset.StandardCharsets.UTF_8);
            outBuffer = outBuffer.reallocIfNeeded(protocolBytes.length);
            outBuffer.setBytes(0, protocolBytes);
```

### RedundantCast
Casting `splits.get(...)` to `String` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        int arrayIndex = size + index.value;
        if (arrayIndex >= 0) {
          result = (String) splits.get(arrayIndex);
        }
      } else {
```

### RedundantCast
Casting `com.google.common.collect.Iterables.get(...)` to `String` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        int arrayIndex = index.value - 1;
        result =
          (String) com.google.common.collect.Iterables.get(splitter.split(inputString), arrayIndex, "");
      }
      byte[] strBytes = result.getBytes(com.google.common.base.Charsets.UTF_8);
```

### RedundantCast
Casting `ex` to `UserException` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DynamicResourceManager.java`
#### Snippet
```java
      return;
    } else if (ex instanceof UserException) {
      throw (UserException) ex;
    } else {
      throw UserException.systemError(ex)
```

### RedundantCast
Casting `DEFAULT_READ_SIZE` to `long` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
      Preconditions.checkArgument(fileLength >= MIN_FILE_SIZE, "%s is not a Parquet file (too small)", status.getPath());

      int len = (int) Math.min( fileLength, (long) DEFAULT_READ_SIZE);
      byte[] footerBytes = new byte[len];
      readFully(file, fileLength - len, footerBytes, 0, len);
```

### RedundantCast
Casting `valuesReadInCurrentPass` to `int` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthValuesColumn.java`
#### Snippet
```java
    dataTypeLengthInBits = variableWidthVector.getAccessor().getValueLength(valuesReadInCurrentPass);
    // again, I am re-purposing the unused field here, it is a length n BYTES, not bits
    boolean success = setSafe((int) valuesReadInCurrentPass, pageReader.pageData,
        (int) pageReader.readPosInBytes + 4, dataTypeLengthInBits);
    assert success;
```

### RedundantCast
Casting `valuesReadInCurrentPass` to `int` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLengthValuesColumn.java`
#### Snippet
```java

    // this should not fail
    variableWidthVector.getMutator().setValueLengthSafe((int) valuesReadInCurrentPass + pageReader.valuesReadyToRead,
        dataTypeLengthInBits);
    return false;
```

### RedundantCast
Casting `parentInst.pageReader.byteLength` to `int` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
        pageInfo.pageData  = parentInst.pageReader.pageData;
        pageInfo.pageDataOff = (int) oprReadState.pageReadPos;
        pageInfo.pageDataLen = (int) parentInst.pageReader.byteLength;
      }

```

### RedundantCast
Casting `scan.getGroupScan()` to `AbstractDbGroupScan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPhysicalPlanCallContext.java`
#### Snippet
```java

  public List<SchemaPath> getScanColumns() {
    return ((AbstractDbGroupScan)scan.getGroupScan()).getColumns();
  }

```

### RedundantCast
Casting `primaryTableGroupScan` to `DbGroupScan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
      // for the primary table join-back each row may belong to a different block, so in general num_blocks = num_rows;
      // however, num_blocks cannot exceed the total number of blocks of the table
      DbGroupScan dbGroupScan = (DbGroupScan) primaryTableGroupScan;
      double totalBlocksPrimary = Math.ceil((dbGroupScan.getColumns().size() *
          costBase.getAverageColumnSize(dbGroupScan) * totalRows) / costBase.getBlockSize(dbGroupScan));
```

### RedundantCast
Casting `origDbGroupScan.getRestrictedScan(...)` to `DbGroupScan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java

    // Create a restricted groupscan from the primary table's groupscan
    DbGroupScan restrictedGroupScan  = (DbGroupScan)origDbGroupScan.getRestrictedScan(cols);
    if (restrictedGroupScan == null) {
      logger.error("Null restricted groupscan in NonCoveringIndexPlanGenerator.convertChild");
```

### RedundantCast
Casting `(buckets[startBucket + 1] - lowValue)` to `double` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
         startBucketFraction = 1.0 / ndv;
       } else {
          startBucketFraction = ((double) (buckets[startBucket + 1] - lowValue)) / (buckets[startBucket + 1] - buckets[startBucket]);
        }
      }
```

### RedundantCast
Casting `(highValue - buckets[endBucket])` to `double` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
          endBucketFraction = 1.0/ndv;
        } else {
          endBucketFraction = ((double) (highValue - buckets[endBucket])) / (buckets[endBucket + 1] - buckets[endBucket]);
        }
      }
```

### RedundantCast
Casting `Math.min(...)` to `double` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillStatsTable.java`
#### Snippet
```java
    // Ndv estimation techniques like HLL may over-estimate, hence cap it at rowCount
    if (ndvCol != null) {
      return (double) Math.min(ndvCol, rowCount);
    }
    return null;
```

### RedundantCast
Casting `child` to `RelNode` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/ExchangePrel.java`
#### Snippet
```java
   */
  public Prel constructMuxPrel(Prel child, OptionManager options) {
    return (Prel)copy(getTraitSet(), Collections.singletonList(((RelNode)child)));
  }
}
```

### RedundantCast
Casting `batch` to `RecordBatch` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/RecordBatchData.java`
#### Snippet
```java

    if (batch instanceof RecordBatch && batch.getSchema().getSelectionVectorMode() == SelectionVectorMode.TWO_BYTE) {
      this.sv2 = ((RecordBatch)batch).getSelectionVector2().clone();
    } else {
      this.sv2 = null;
```

### RedundantCast
Casting `output` to `MapVector` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/ColumnMergedStatistic.java`
#### Snippet
```java
    // Check the input is a Map Vector
    assert (output.getField().getType().getMinorType() == TypeProtos.MinorType.MAP);
    MapVector outputMap = (MapVector) output;
    for (ValueVector outMapCol : outputMap) {
      String colName = outMapCol.getField().getName();
```

### RedundantCast
Casting `listVectorState().memberVectorState().vector()` to `UnionVector` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ListState.java`
#### Snippet
```java

  private UnionVector unionVector() {
    return (UnionVector) listVectorState().memberVectorState().vector();
  }

```

### RedundantCast
Casting `rootColumn` to `MapColumnDefn` is redundant
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageDirectoryProcessor.java`
#### Snippet
```java
                    rootColumn = new ListColumnDefn(formatName).builder((ArrayWriter) subColumn);
                  } else { // for map
                    subColumn = ((MapColumnDefn) rootColumn).addMap(formatName);
                    // set up the current writer in nested structure
                    rootColumn = new MapColumnDefn(formatName).builder((TupleWriter) subColumn);
```

### RedundantCast
Casting `zeros.length` to `long` is redundant
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
                LOG.debug("Adding {} bytes of padding (row group size={}B, block size={}B)", remaining, rowGroupSize, dfsBlockSize);
                for (; remaining > 0; remaining -= zeros.length) {
                    out.write(zeros, 0, (int) Math.min((long) zeros.length, remaining));
                }
            }
```

### RedundantCast
Casting `scanRel` to `DrillScanRel` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
    String conditionAsStr = nullConditionAsString;
    Map<String, StatisticsPayload> payloadMap;
    if ((scanRel instanceof DrillScanRel && ((DrillScanRel)scanRel).getGroupScan() instanceof DbGroupScan)
        || (scanRel instanceof ScanPrel && ((ScanPrel)scanRel).getGroupScan() instanceof DbGroupScan)) {
      if (condition == null && fullTableScanPayload != null) {
```

### RedundantCast
Casting `scanRel` to `ScanPrel` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
    Map<String, StatisticsPayload> payloadMap;
    if ((scanRel instanceof DrillScanRel && ((DrillScanRel)scanRel).getGroupScan() instanceof DbGroupScan)
        || (scanRel instanceof ScanPrel && ((ScanPrel)scanRel).getGroupScan() instanceof DbGroupScan)) {
      if (condition == null && fullTableScanPayload != null) {
        return fullTableScanPayload.getLeadingRowCount();
```

### RedundantCast
Casting `tmp.toArray(...)` to `com.esri.core.geometry.Geometry[]` is redundant
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java
      }
      com.esri.core.geometry.Geometry[] geomArr =
          (com.esri.core.geometry.Geometry[]) tmp.toArray(new com.esri.core.geometry.Geometry[0]);
      com.esri.core.geometry.Geometry geom = com.esri.core.geometry.GeometryEngine.union(geomArr, spatialRef);

```

### RedundantCast
Casting `Comparator.nullsFirst(...)` to `Comparator` is redundant
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/util/TableMetadataUtils.java`
#### Snippet
```java
        // case if T is byte[]. But, this is a horrible hack and should
        // be fixed.
        return (Comparator<T>) (Comparator<?>)
            Comparator.nullsFirst(UnsignedBytes.lexicographicalComparator());
      case UINT1:
```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
`@return` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/ColumnMetadata.java`
#### Snippet
```java
   * date/time columns.
   *
   * @return
   */
  DateTimeFormatter dateTimeFormatter();
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/vector/src/main/java/org/apache/drill/exec/record/MaterializedField.java`
#### Snippet
```java
   * Field object whose children field list is different which is not correct.
   * Same holds true for
   * {@link MaterializedField#isEquivalent(MaterializedField)} method.
   * </p>
   * </ul>
```

### JavadocDeclaration
`@param newType` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/record/MaterializedField.java`
#### Snippet
```java
   * By allowing the non-critical metadata to change, we preserve the
   * child relationships as a list or union evolves.
   * @param newType
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/VariableWidthVector.java`
#### Snippet
```java
  /**
   * Provide the maximum amount of variable width bytes that can be stored int his vector.
   * @return
   */
  int getByteCapacity();
```

### JavadocDeclaration
`@param numRecords` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/ValueVector.java`
#### Snippet
```java
  /**
   * Set the initial record capacity
   * @param numRecords
   */
  void setInitialCapacity(int numRecords);
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedVariableWidthVectorLike.java`
#### Snippet
```java
  /**
   * Provide the maximum amount of variable width bytes that can be stored int his vector.
   * @return
   */
  int getByteCapacity();
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/InvalidConversionError.java`
#### Snippet
```java
 * column.
 *
 * @see {UnsupportedConversionError} for a setup tie exception where the conversion
 * is not supported for any values.
 */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/VariantReader.java`
#### Snippet
```java
 * uses the proper type-specific reader to access that value.
 *
 * @see {@link VariantWriter}
 */

```

### JavadocDeclaration
Class reference expected
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/TupleReader.java`
#### Snippet
```java
   * @param colIndex column index
   * @return reader for the column
   * @throws {@link java.lang.IndexOutOfBoundsException} if the index is invalid
   */

```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/TupleReader.java`
#### Snippet
```java
 * Java object or as a string.
 *
 * @see {@link TupleWriter}
 */

```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/TupleWriter.java`
#### Snippet
```java
 * methods throw an exception if the column is not of the requested type.
 *
 * @see {@link SingleMapWriter}, the class which this class replaces
 */
public interface TupleWriter extends ColumnWriter {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/VariantWriter.java`
#### Snippet
```java
 * </ul>
 *
 * @see {@link VariantReader}
 */
public interface VariantWriter extends ColumnWriter {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/UnsupportedConversionError.java`
#### Snippet
```java
 * of the code.
 *
 * @see {InvalidConversionError} for a runtime exception where the conversion
 * is supported, but a specific value is invalid for that conversion.
 */
```

### JavadocDeclaration
`@param listAccessor` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   * scalar, a map, a union, or another list.
   *
   * @param listAccessor
   * @param elementReader
   * @return
```

### JavadocDeclaration
`@param elementReader` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   *
   * @param listAccessor
   * @param elementReader
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   * @param listAccessor
   * @param elementReader
   * @return
   */

```

### JavadocDeclaration
`@param schema` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   * repeated of some other type.
   *
   * @param schema
   * @param listAccessor
   * @param elementReader
```

### JavadocDeclaration
`@param listAccessor` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   *
   * @param schema
   * @param listAccessor
   * @param elementReader
   * @return
```

### JavadocDeclaration
`@param elementReader` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   * @param schema
   * @param listAccessor
   * @param elementReader
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
   * @param listAccessor
   * @param elementReader
   * @return
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/rest/RestClient.java`
#### Snippet
```java
   * @param url request URL
   * @return a Response object that the caller is responsible for closing.
   * @throws IOException
   */
  Response get(String url) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/rest/RestClient.java`
#### Snippet
```java
   * @param body request body.
   * @return a Response object that the caller is responsible for closing.
   * @throws IOException
   */
  Response post(String url, String body) throws IOException;
```

### JavadocDeclaration
`@param statement` tag description is missing
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
  /**
   *
   * @param statement
   * @param signature
   * @throws SQLException
```

### JavadocDeclaration
`@param signature` tag description is missing
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   *
   * @param statement
   * @param signature
   * @throws SQLException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
   * @param statement
   * @param signature
   * @throws SQLException
   */
  DrillCursor(DrillConnectionImpl connection, AvaticaStatement statement, Signature signature) throws SQLException {
```

### JavadocDeclaration
`@param metadata` tag description is missing
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillColumnMetaDataList.java`
#### Snippet
```java
  /**
   * Update the metadata with given metadata received from server.
   * @param metadata
   */
  public void updateColumnMetaData(List<ResultColumnMetadata> metadata) {
```

### JavadocDeclaration
`@param xmlDataLevel` tag description is missing
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpApiConfig.java`
#### Snippet
```java
    /**
     * Do not use.  Use xmlOptions instead to set XML data level.
     * @param xmlDataLevel
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpApiConfig.java`
#### Snippet
```java
     * Do not use.  Use xmlOptions instead to set XML data level.
     * @param xmlDataLevel
     * @return
     */
    @Deprecated
```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java
   * Create a YARN ContainerRequest object from the information in this object.
   *
   * @return
   */
  public ContainerRequest makeRequest() {
```

### JavadocDeclaration
`@param bit` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java
   * advertise the HTTP port, so it does not appear in the generated string.
   *
   * @param bit
   * @return A string representation of a Drillbit.
   */
```

### JavadocDeclaration
`@param connect` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java
   * Specify connect string in the form: host:/zkRoot/clusterId
   *
   * @param connect
   * @return This {@link ZKClusterCoordinatorDriver}.
   * @throws ZKConfigException
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java
   * @param connect
   * @return This {@link ZKClusterCoordinatorDriver}.
   * @throws ZKConfigException
   */
  public ZKClusterCoordinatorDriver setConnect(String connect)
```

### JavadocDeclaration
`@param newState` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java
   * with the time of the first disconnect not followed by a connect.
   *
   * @param newState
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
   * can locate the problem.
   *
   * @throws ZKRuntimeException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/YarnRMClient.java`
#### Snippet
```java
   * intended use is when debugging unmanaged applications.
   *
   * @throws YarnClientException
   */
  public ApplicationAttemptId waitForStart() throws YarnClientException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/YarnRMClient.java`
#### Snippet
```java
   * informal implementation useful for testing.
   *
   * @throws YarnClientException
   */

```

### JavadocDeclaration
`@param n` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ClusterDef.java`
#### Snippet
```java
   * Deserialize a node tier from the configuration file.
   *
   * @param n
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ClusterDef.java`
#### Snippet
```java
   *
   * @param n
   * @return
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   * cases where tasks hang in this state. This is a potential work-around.
   *
   * @param task
   * @return True if the given task is regestered. False otherwise.
   */
```

### JavadocDeclaration
`@param dbe` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   * </ol>
   *
   * @param dbe
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   * notification before we learn from the NM that the task is alive.)
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   * or something is seriously wrong.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param container` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
   * log messages.
   *
   * @param container
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
   *
   * @param container
   * @return
   */

```

### JavadocDeclaration
`@param trackingUrl` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
   * the user, "unwrap" that redirect URL to get the actual site URL.
   *
   * @param trackingUrl
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
   *
   * @param trackingUrl
   * @return
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/ClientCommand.java`
#### Snippet
```java
   * name.)
   *
   * @return
   */
  protected static File getAppIdFile() {
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
     *
     * @return file status
     * @throws DfsFacadeException
     */

```

### JavadocDeclaration
IOException is not declared to be thrown by method makeResource
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
   * @return a YARN local resource records that contains information about path,
   *         size, type, resource and so on that YARN requires.
   * @throws IOException
   *           if the resource does not exist on the configured file system
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StopCommand.java`
#### Snippet
```java
   * asks the AM's NM to kill the AM process.
   *
   * @throws ClientException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
   * is not running, then clean up the "orphan" app id file.
   *
   * @throws ClientException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java
   * so we can find our Drill AM on the YARN cluster.
   *
   * @throws ClientException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     * the uploaded code based on the file name.
     *
     * @throws ClientException
     */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     * always upload the site; we never reuse an existing one.
     *
     * @throws ClientException
     */

```

### JavadocDeclaration
`@param siteArchive` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     * use existing files, which users should not do in production.
     *
     * @param siteArchive
     * @throws ClientException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     *
     * @param siteArchive
     * @throws ClientException
     */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     * the uploaded code based on the file name.
     *
     * @throws ClientException
     */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     * changes.
     *
     * @return
     * @throws ClientException
     */
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
     *
     * @return
     * @throws ClientException
     */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
   * Common setup of the Drill and site directories.
   *
   * @throws ClientException
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java
   * files already on the worker nodes.
   *
   * @return
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
   *
   * @return the remote path, with the "$PWD" environment variable.
   * @throws DoyConfigException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
   * to that name obviously differs.
   *
   * @throws DoyConfigException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
   * drill-am.sh launch script.
   *
   * @throws DoyConfigException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
   * provided class loader.
   *
   * @throws DoyConfigException
   */
  protected void doLoad(ClassLoader cl) throws DoyConfigException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
   * DFS.
   *
   * @throws DoyConfigException
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java
   * Label for this task displayed in log messages.
   *
   * @return
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java
   * Special constructor to create a static copy of the current
   * task. The copy is placed in the completed tasks list.
   * @param task
   */

```

### JavadocDeclaration
`@param n` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/BatchScheduler.java`
#### Snippet
```java
   * before canceling.
   *
   * @param n
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerState.java`
#### Snippet
```java
   * Start the given task.
   *
   * @param task
   */

```

### JavadocDeclaration
Wrong tag `returnThe`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerState.java`
#### Snippet
```java
   * ended.
   *
   * @returnThe number of active tasks that have been cancelled, but have not yet
   * ended.
   */
```

### JavadocDeclaration
`@param cancelCount` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/PersistentTaskScheduler.java`
#### Snippet
```java
   * be that we want to reduce the task count, but there is nothing left to cancel.
   *
   * @param cancelCount
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/PersistentTaskScheduler.java`
#### Snippet
```java
   * when shutting down excess tasks.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param priority` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Scheduler.java`
#### Snippet
```java
   * container requests by scheduler.
   *
   * @param priority
   */

```

### JavadocDeclaration
`@param level` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Scheduler.java`
#### Snippet
```java
   * Set the number of desired tasks to the given level.
   *
   * @param level
   * @return the actual resize level, which may be lower than the requested
   * level if the system cannot provide the requested level
```

### JavadocDeclaration
`@param state` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Scheduler.java`
#### Snippet
```java
   * Register the state object that tracks tasks launched by this scheduler.
   *
   * @param state
   */

```

### JavadocDeclaration
`@param delta` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Scheduler.java`
#### Snippet
```java
   * the given amount.
   *
   * @param delta
   */
  void change(int delta);
```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateActions.java`
#### Snippet
```java
   * task waiting for a container and launch the task.
   *
   * @param context
   * @param container
   */
```

### JavadocDeclaration
`@param container` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateActions.java`
#### Snippet
```java
   *
   * @param context
   * @param container
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateActions.java`
#### Snippet
```java
   * Shut down this task group by canceling all tasks not already cancelled.
   *
   * @param context
   */

```

### JavadocDeclaration
`@param hostName` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
   * Report if the given host name is in use.
   *
   * @param hostName
   * @return true if the host is reserved (in use by a container) or
   * blacklisted (failed.)
```

### JavadocDeclaration
`@param id` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * when reducing cluster size.
   *
   * @param id
   */

```

### JavadocDeclaration
`@param containerId` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * The Node Manager reports that a container has stopped.
   *
   * @param containerId
   */
  void containerStopped(ContainerId containerId);
```

### JavadocDeclaration
`@param visitor` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * Allow an observer to see a consistent view of the controller's
   * state by performing the visit in a synchronized block.
   * @param visitor
   */

```

### JavadocDeclaration
`@param visitor` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * task state by performing the visit in a synchronized block.
   *
   * @param visitor
   */

```

### JavadocDeclaration
`@param statuses` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * statuses. Find the task for each container and mark them as completed.
   *
   * @param statuses
   */

```

### JavadocDeclaration
`@param curTime` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterController.java`
#### Snippet
```java
   * Called by the timer ("pulse") thread to trigger time-based events.
   *
   * @param curTime
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/EventContext.java`
#### Snippet
```java
   * For testing only, omits the controller and YARN.
   *
   * @param task
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
   *
   * @return resources
   * @throws YarnFacadeException
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
   * @param resources the means to set up the required environment variables
   * @return task specification
   * @throws DoyConfigException
   */

```

### JavadocDeclaration
`@param url` tag description is missing
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
  /**
   *
   * @param url
   * @return response body which the caller is responsible for closing.
   * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
   * @param url
   * @return response body which the caller is responsible for closing.
   * @throws IOException
   */
  public static ResponseBody makeSimpleGetRequest(String url) throws IOException {
```

### JavadocDeclaration
`@param url` tag description is missing
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
  /**
   *
   * @param url
   * @return an input stream which the caller is responsible for closing.
   */
```

### JavadocDeclaration
`@param base` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/PageTree.java`
#### Snippet
```java
   * cluster name (used as a title on each UI page.)
   *
   * @param base
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/PageTree.java`
#### Snippet
```java
   *
   * @param base
   * @return
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
     * prevent retries after a few tries.
     *
     * @param context
     */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * moving the task to the next collection.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * container will be requested.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * Put a task into the queue waiting to send a container request to YARN.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * completed.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * the current try.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * queue or the completed task list.
   *
   * @param task
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * life-cycle: pending, allocating or active.
   *
   * @return
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * the group itself as completed.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * ID. Prior to this, the task is not associated with a container.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param task` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java
   * Put a task onto the queue awaiting an allocation response from YARN.
   *
   * @param task
   */

```

### JavadocDeclaration
`@param curTime` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   * Adjust the number of running tasks to match the desired level.
   *
   * @param curTime
   */

```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   * Controller state.
   *
   * @see {@link State}
   */

```

### JavadocDeclaration
`@param curTime` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   * Periodically check tasks, handling any timeout issues.
   *
   * @param curTime
   */

```

### JavadocDeclaration
`@param scheduler` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   * callbacks start.
   *
   * @param scheduler
   */

```

### JavadocDeclaration
`@param curTime` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   * is enabled, then automatically exit since no useful work can be done.
   *
   * @param curTime
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/ControllerModel.java`
#### Snippet
```java
   * Create a table of user-visible descriptions for each controller state.
   *
   * @return
   */

```

### JavadocDeclaration
`@param controller` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/ControllerModel.java`
#### Snippet
```java
   * the cluster controller and ZK registry.
   *
   * @param controller
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * The node manager request to stop a task failed.
   *
   * @param context
   * @param t
   */
```

### JavadocDeclaration
`@param t` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param t
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * states that implement a timeout.
   *
   * @param context
   * @param newState
   */
```

### JavadocDeclaration
`@param newState` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param newState
   */

```

### JavadocDeclaration
`@param action` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * and hope it goes away.
   *
   * @param action
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * Node Manager reports that the task has started execution.
   *
   * @param context
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * registered in case we decide to retry.
   *
   * @param context
   * @param disposition
   */
```

### JavadocDeclaration
`@param disposition` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param disposition
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * Cluster manager wishes to cancel this task.
   *
   * @param context
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * END --> STARTING
   *
   * @param context
   */

```

### JavadocDeclaration
`@param status` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     * Normal ENDING --> WAIT_END_ACK | END transition.
     *
     * @param status
     */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * the completion status.
   *
   * @param context
   * @param status
   */
```

### JavadocDeclaration
`@param status` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param status
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * Resource Manager reports that the task has been allocated a container.
   *
   * @param context
   * @param container
   */
```

### JavadocDeclaration
`@param container` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param container
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * then move from<br>
   * END --> STARTING
   * @param context
   * @param disposition
   */
```

### JavadocDeclaration
`@param disposition` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * END --> STARTING
   * @param context
   * @param disposition
   */

```

### JavadocDeclaration
`@param status` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     * Normal task completion. Implements the RUNNING --> END transition.
     *
     * @param status
     */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * Otherwise, starts the whole life cycle over again.
   *
   * @param context
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * if this was a success or failure.
   *
   * @param context
   */

```

### JavadocDeclaration
`@param context` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   * The launch of the container failed.
   *
   * @param context
   * @param t
   */
```

### JavadocDeclaration
`@param t` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
   *
   * @param context
   * @param t
   */

```

### JavadocDeclaration
`@param status` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
     * Normal KILLING --> WAIT_END_ACK | END transition.
     *
     * @param status
     */

```

### JavadocDeclaration
`@param controller` tag description is missing
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
     * we want to leave the controller unlocked and instead lock only the ZK registry.
     *
     * @param controller
     */

```

### JavadocDeclaration
`@param call` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFunctionRegistry.java`
#### Snippet
```java
   * Find the UDF class for given function name and check if it accepts the given input argument
   * types. If a match is found, create a holder and return
   * @param call
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFunctionRegistry.java`
#### Snippet
```java
   * types. If a match is found, create a holder and return
   * @param call
   * @return
   */
  @Override
```

### JavadocDeclaration
`@param config` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFunctionRegistry.java`
#### Snippet
```java
   * extracts function annotation and store the
   * (function name) --> (implementation class) mappings.
   * @param config
   */
  public HiveFunctionRegistry(DrillConfig config) {
```

### JavadocDeclaration
`@param argTypes` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * Create holder for GenericUDF
   * @param genericUdfClazz implementation class
   * @param argTypes
   * @param returnOI
   * @param returnType
```

### JavadocDeclaration
`@param returnOI` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * @param genericUdfClazz implementation class
   * @param argTypes
   * @param returnOI
   * @param returnType
   */
```

### JavadocDeclaration
`@param returnType` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * @param argTypes
   * @param returnOI
   * @param returnType
   */
  public HiveFuncHolder(Class<? extends GenericUDF> genericUdfClazz, MajorType[] argTypes,
```

### JavadocDeclaration
`@param argTypes` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * @param udfName name of the UDF class
   * @param udfClazz UDF implementation class
   * @param argTypes
   * @param returnOI
   * @param returnType
```

### JavadocDeclaration
`@param returnOI` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * @param udfClazz UDF implementation class
   * @param argTypes
   * @param returnOI
   * @param returnType
   */
```

### JavadocDeclaration
`@param returnType` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
   * @param argTypes
   * @param returnOI
   * @param returnType
   */
  public HiveFuncHolder(String udfName, Class< ? extends UDF> udfClazz, MajorType[] argTypes,
```

### JavadocDeclaration
`@param dbName` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java
   * Check authorization for "SHOW TABLES" command in given Hive db. A {@link HiveAccessControlException} is thrown
   * for illegal access.
   * @param dbName
   */
  void authorizeShowTables(final String dbName) throws HiveAccessControlException {
```

### JavadocDeclaration
`@param dbName` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java
   * Check authorization for "READ TABLE" for given db.table. A {@link HiveAccessControlException} is thrown
   * for illegal access.
   * @param dbName
   * @param tableName
   */
```

### JavadocDeclaration
`@param tableName` tag description is missing
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java
   * for illegal access.
   * @param dbName
   * @param tableName
   */
  void authorizeReadTable(final String dbName, final String tableName) throws HiveAccessControlException {
```

### JavadocDeclaration
`@return` tag description is missing
in `logical/src/main/java/org/apache/drill/common/graph/GraphAlgos.java`
#### Snippet
```java
     * @param reverse true if reversed, otherwise false
     *
     * @return
     */
    static <V extends GraphValue<V>> List<AdjacencyList<V>.Node> sortInternal(AdjacencyList<V> graph, boolean reverse) {
```

### JavadocDeclaration
`@return` tag description is missing
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyList.java`
#### Snippet
```java
   * Get a list of nodes that have no outbound edges.
   *
   * @return
   */
  public Collection<V> getLeafNodes() {
```

### JavadocDeclaration
`@return` tag description is missing
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyList.java`
#### Snippet
```java
   * Get a list of all nodes that have no incoming edges.
   *
   * @return
   */
  public List<V> getRootNodes() {
```

### JavadocDeclaration
`@param logicalVisitor` tag description is missing
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
   * Provides capability to build a set of output based on traversing a query graph tree.
   *
   * @param logicalVisitor
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
   *
   * @param logicalVisitor
   * @return
   */
  public <T, X, E extends Throwable> T accept(LogicalVisitor<T, X, E> logicalVisitor, X value) throws E;
```

### JavadocDeclaration
`@param nullDirection` tag description is missing
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     * Provides a default value of UNSPECIFIED in the case of a null.
     *
     * @param nullDirection
     * @return - a sanitized direction value
     */
```

### JavadocDeclaration
`@param direction` tag description is missing
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
     * Provides a default value of ASCENDING in the case of a null.
     *
     * @param direction
     * @return - a sanitized direction value
     */
```

### JavadocDeclaration
`@param i` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/FunctionHolderExpression.java`
#### Snippet
```java
  /**
   * constant input expected for i'th argument?
   * @param i
   * @return True if a constant input is expected for the i'th argument. False otherwise.
   */
```

### JavadocDeclaration
`@param logger` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/ErrorCollector.java`
#### Snippet
```java
   * call this method.
   *
   * @param logger
   */
  void reportErrors(Logger logger);
```

### JavadocDeclaration
`@param input` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/AnyValueExpression.java`
#### Snippet
```java

  /**
   * @param input
   * @param pos
   */
```

### JavadocDeclaration
`@param pos` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/AnyValueExpression.java`
#### Snippet
```java
  /**
   * @param input
   * @param pos
   */
  public AnyValueExpression(LogicalExpression input, ExpressionPosition pos) {
```

### JavadocDeclaration
`@param encodingType` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/ConvertExpression.java`
#### Snippet
```java

  /**
   * @param encodingType
   * @param convertFunction
   * @param input
```

### JavadocDeclaration
`@param convertFunction` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/ConvertExpression.java`
#### Snippet
```java
  /**
   * @param encodingType
   * @param convertFunction
   * @param input
   * @param pos
```

### JavadocDeclaration
`@param input` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/ConvertExpression.java`
#### Snippet
```java
   * @param encodingType
   * @param convertFunction
   * @param input
   * @param pos
   */
```

### JavadocDeclaration
`@param pos` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/ConvertExpression.java`
#### Snippet
```java
   * @param convertFunction
   * @param input
   * @param pos
   */
  public ConvertExpression(String convertFunction, String encodingType, LogicalExpression input, ExpressionPosition pos) {
```

### JavadocDeclaration
`@param pos` tag description is missing
in `logical/src/main/java/org/apache/drill/common/expression/fn/FuncHolder.java`
#### Snippet
```java
   * @param name alias used in this specific call
   * @param args expressions of the actual function arguments
   * @param pos
   * @return an expression that holds the function definition (this object),
   * actual parameters and related information
```

### JavadocDeclaration
`@param suggestedSemaphoreVal` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/DynamicSemaphore.java`
#### Snippet
```java
     * initial static value
     *
     * @param suggestedSemaphoreVal
     */
    public void tryToIncreaseCredit(int suggestedSemaphoreVal) {
```

### JavadocDeclaration
`@param skipWriting` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java
  /**
   * Skip writing profile
   * @param skipWriting
   */
  public void skipWritingProfile(boolean skipWriting) {
```

### JavadocDeclaration
`@param schemaConfig` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/QueryContext.java`
#### Snippet
```java
   *  Create and return a {@link org.apache.calcite.schema.SchemaPlus} with given <i>schemaConfig</i> but some schemas (from storage plugins)
   *  could be initialized later.
   * @param schemaConfig
   * @return A {@link org.apache.calcite.schema.SchemaPlus} with given <i>schemaConfig</i>.
   */
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
   * for all table names at once. Then as part of DRILL-4826 was added option to regulate bulk size,
   * because big amount of views was causing performance degradation. After last improvements for
   * DRILL-7115 both options ({@link ExecConstants#ENABLE_BULK_LOAD_TABLE_LIST_KEY}
   * and {@link ExecConstants#BULK_LOAD_TABLE_LIST_BULK_SIZE_KEY}) became obsolete and may be removed
   * in future releases.
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/AbstractDisposableUserClientConnection.java`
#### Snippet
```java
   * Wait until the query has completed or timeout is passed.
   *
   * @throws InterruptedException
   */
  public boolean await(final long timeoutMillis) throws InterruptedException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/AbstractDisposableUserClientConnection.java`
#### Snippet
```java
   * Wait indefinitely until the query is completed. Used only in case of WebUser
   *
   * @throws Exception
   */
  public void await() throws Exception {
```

### JavadocDeclaration
`@param useEncryptedCounter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataRpcMetrics.java`
#### Snippet
```java
   * initialize.
   *
   * @param useEncryptedCounter
   * @param allocator
   */
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataRpcMetrics.java`
#### Snippet
```java
   *
   * @param useEncryptedCounter
   * @param allocator
   */
  @Override
```

### JavadocDeclaration
`@param testInjector` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataTunnel.java`
#### Snippet
```java
   *    is available before acquiring the sending slot. Site name is: "data-tunnel-send-batch-wait-for-interrupt"
   *
   * @param testInjector
   * @param testControls
   * @param testLogger
```

### JavadocDeclaration
`@param testControls` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataTunnel.java`
#### Snippet
```java
   *
   * @param testInjector
   * @param testControls
   * @param testLogger
   */
```

### JavadocDeclaration
`@param testLogger` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataTunnel.java`
#### Snippet
```java
   * @param testInjector
   * @param testControls
   * @param testLogger
   */
  public void setTestInjectionControls(final ControlsInjector testInjector,
```

### JavadocDeclaration
`@param useEncryptedCounter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserRpcMetrics.java`
#### Snippet
```java
   * BitToUserConnection using the singleton instance should not call initialize.
   *
   * @param useEncryptedCounter
   * @param allocator
   */
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserRpcMetrics.java`
#### Snippet
```java
   *
   * @param useEncryptedCounter
   * @param allocator
   */
  @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticatorTemplate.java`
#### Snippet
```java
  /**
   * {@link org.apache.drill.exec.rpc.user.security.UserAuthenticator} implementation type.
   * @return
   */
  String type();
```

### JavadocDeclaration
`@param user` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticator.java`
#### Snippet
```java
   * Authenticate the given <i>user</i> and <i>password</i> combination.
   *
   * @param user
   * @param password
   * @throws UserAuthenticationException if authentication fails for given user and password.
```

### JavadocDeclaration
`@param password` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticator.java`
#### Snippet
```java
   *
   * @param user
   * @param password
   * @throws UserAuthenticationException if authentication fails for given user and password.
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/security/UserAuthenticator.java`
#### Snippet
```java
   * such connections resources are released in a safe manner as part of close.
   *
   * @throws IOException
   */
  @Override
```

### JavadocDeclaration
`@param cancel` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/WorkEventBus.java`
#### Snippet
```java
   * only once (the first call).
   * @param handle the handle to the fragment
   * @param cancel
   * @return if the fragment was found and removed from the event bus
   */
```

### JavadocDeclaration
`@param useEncryptedCounter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlRpcMetrics.java`
#### Snippet
```java
   * {@link ControlConnection} using the singleton instance should not call initialize.
   *
   * @param useEncryptedCounter
   * @param allocator
   */
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlRpcMetrics.java`
#### Snippet
```java
   *
   * @param useEncryptedCounter
   * @param allocator
   */
  @Override
```

### JavadocDeclaration
`@param node` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   * start creating one. This create the connection asynchronously.
   *
   * @param node
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/Controller.java`
#### Snippet
```java
   *
   * @param node
   * @return
   */
  public ControlTunnel getTunnel(DrillbitEndpoint node);
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServer.java`
#### Snippet
```java
   * @param errMsg  Error message.
   * @param exception Optional exception.
   * @return
   */
  private static BitToUserHandshake handleFailure(BitToUserHandshake.Builder respBuilder, HandshakeStatus status,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/LocalControlConnectionManager.java`
#### Snippet
```java
   * @param byteBuffArray - input array of ByteBuf's
   * @return DrillBuf - output Drillbuf with all the input bytes.
   * @throws OutOfMemoryException
   */
  private DrillBuf convertToByteBuf(ByteBuf[] byteBuffArray) throws OutOfMemoryException {
```

### JavadocDeclaration
`@param operatorTable` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/PluggableFunctionRegistry.java`
#### Snippet
```java
   * The other is addOperatorWithoutInference whose added operators will be used
   * when planner.type_inference.enable is set to false;
   * @param operatorTable
   */
  public void register(DrillOperatorTable operatorTable);
```

### JavadocDeclaration
`@param functionCall` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/PluggableFunctionRegistry.java`
#### Snippet
```java
   * otherwise null.
   *
   * @param functionCall
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/PluggableFunctionRegistry.java`
#### Snippet
```java
   *
   * @param functionCall
   * @return
   */
  public AbstractFuncHolder getFunction(FunctionCall functionCall);
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillSimpleFuncHolder.java`
#### Snippet
```java
   * if requested.
   *
   * @see {@link #generateBody()}
   */
  protected HoldingContainer generateEvalBody(ClassGenerator<?> g, HoldingContainer[] inputVariables, String body,
```

### JavadocDeclaration
`@param comparisonFunction` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java
   * if the types are equivialent
   *
   * @param comparisonFunction
   * @param args
   * @return
```

### JavadocDeclaration
`@param args` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java
   *
   * @param comparisonFunction
   * @param args
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java
   * @param comparisonFunction
   * @param args
   * @return
   */
  private static LogicalExpression getTypeComparisonFunction(LogicalExpression comparisonFunction, HoldingContainer... args) {
```

### JavadocDeclaration
Wrong tag `Param`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillFuncHolder.java`
#### Snippet
```java
   * into the function. Given the following declaration: <code><pre>
   * public static class UnionIsBigInt implements DrillSimpleFunc {
   *   @Param UnionHolder in;
   * </pre></code>, we generate code like the following: <code><pre>
   *  final BitHolder out = new BitHolder();
```

### JavadocDeclaration
`@param type` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * Returns the function which asserts that the current subtype of a union type is a specific type, and allows the materializer
     * to bind to that specific type when doing function resolution
     * @param type
     * @param arg
     * @return
```

### JavadocDeclaration
`@param arg` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * to bind to that specific type when doing function resolution
     * @param type
     * @param arg
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * @param type
     * @param arg
     * @return
     */
    private LogicalExpression getUnionAssertFunctionForType(MinorType type, LogicalExpression arg) {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * Returns the function call whose purpose is to throw an Exception if that code is hit during execution
     * @param message the exception message
     * @return
     */
    private LogicalExpression getExceptionFunction(String message) {
```

### JavadocDeclaration
`@param type` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
    /**
     * Get the function that tests whether a union type is a specific type
     * @param type
     * @param arg
     * @return
```

### JavadocDeclaration
`@param arg` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * Get the function that tests whether a union type is a specific type
     * @param type
     * @param arg
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
     * @param type
     * @param arg
     * @return
     */
    private LogicalExpression getIsTypeExpressionForType(MinorType type, LogicalExpression arg) {
```

### JavadocDeclaration
`@param buf` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
   * returns true if the value can be read as a date.
   *
   * @param buf
   * @param start
   * @param end
```

### JavadocDeclaration
`@param start` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
   *
   * @param buf
   * @param start
   * @param end
   * @return true iff the string value can be read as a date
```

### JavadocDeclaration
`@param end` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
   * @param buf
   * @param start
   * @param end
   * @return true iff the string value can be read as a date
   */
```

### JavadocDeclaration
`@param start` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
  /**
   * Test if the buffer contains only ASCII bytes.
   * @param start
   * @param end
   * @param buffer
```

### JavadocDeclaration
`@param end` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   * Test if the buffer contains only ASCII bytes.
   * @param start
   * @param end
   * @param buffer
   * @return
```

### JavadocDeclaration
`@param buffer` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   * @param start
   * @param end
   * @param buffer
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   * @param end
   * @param buffer
   * @return
   */
  private boolean isAscii(int start, int end, DrillBuf buffer) {
```

### JavadocDeclaration
`@param byteBuf` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
  /**
   * Decode the buffer using the CharsetDecoder.
   * @param byteBuf
   * @return false if failed because the charbuffer was not big enough
   * @throws RuntimeException if it fails for encoding errors
```

### JavadocDeclaration
`@param start` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   * CharSequenceWrapper and thus re-using the CharSequenceWrapper object.
   *
   * @param start
   * @param end
   * @param buffer
```

### JavadocDeclaration
`@param end` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   *
   * @param start
   * @param end
   * @param buffer
   */
```

### JavadocDeclaration
`@param buffer` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSequenceWrapper.java`
#### Snippet
```java
   * @param start
   * @param end
   * @param buffer
   */
  public void setBuffer(int start, int end, DrillBuf buffer) {
```

### JavadocDeclaration
`@param args` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/output/OutputWidthCalculators.java`
#### Snippet
```java
         * it is replaced with {@link Types#MAX_VARCHAR_LENGTH}.
         *
         * @param args
         * @return return type
         */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
   * We take advantage of the fact that Java's modified utf-8 encoding can never contain
   * embedded null byte.
   * @see <a>http://docs.oracle.com/javase/8/docs/api/java/io/DataInput.html#modified-utf-8</a>
   */
  public static String[] encode(final String... schemaPaths) {
```

### JavadocDeclaration
Wrong tag `See`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
 * Tool for printing the content of record batches to screen. Used for debugging.
 *
 * @See {@link org.apache.drill.exec.physical.rowSet.RowSetFormatter RowSetFormatter}
 */

```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/concurrent/ExecutorServiceUtil.java`
#### Snippet
```java
     * </ul>
     *
     * @see {@link Future#cancel(boolean)}
     */
    @Override
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/concurrent/ExecutorServiceUtil.java`
#### Snippet
```java
   *
   * @return decorated future object upon successful submission
   * @see {@link ExecutorService#submit(Callable)
   */
  public static <T> Future<T> submit(ExecutorService service, Callable<T> callable) {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java

  /**
   * @see {@link RecordBatchStats#logRecordBatchStats(RecordBatchIOType, String, RecordBatch, RecordBatchStatsContext)}
   */
  public static void logRecordBatchStats(RecordBatchIOType ioType,
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java

  /**
   * @see {@link RecordBatchStats#logRecordBatchStats(RecordBatchIOType, String, RecordBatch, RecordBatchStatsContext)}
   */
  public static void logRecordBatchStats(RecordBatchIOType ioType,
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java

  /**
   * @see {@link RecordBatchStats#logRecordBatchStats(RecordBatchIOType, String, RecordBatchSizer, RecordBatchStatsContext)}
   */
  public static void logRecordBatchStats(RecordBatchIOType ioType,
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java

  /**
   * @see {@link RecordBatchStats#logRecordBatchStats(RecordBatchIOType, RecordBatch, RecordBatchStatsContext)}
   */
  public static void logRecordBatchStats(RecordBatchIOType ioType,
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java

  /**
   * @see {@link RecordBatchStats#logRecordBatchStats(RecordBatchIOType, RecordBatch, RecordBatchStatsContext)}
   */
  public static void logRecordBatchStats(RecordBatchIOType ioType,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/RawBatchBuffer.java`
#### Snippet
```java
   * @param batch
   *          Batch to enqueue
   * @throws IOException
   * @return Whether response should be returned.
   */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/RawBatchBuffer.java`
#### Snippet
```java
   *          Batch to enqueue
   * @throws IOException
   * @return Whether response should be returned.
   */
  public void enqueue(RawFragmentBatch batch) throws IOException;
```

### JavadocDeclaration
`@param parentAccounter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/AbstractDataCollector.java`
#### Snippet
```java

  /**
   * @param parentAccounter
   * @param numBuffers Number of RawBatchBuffer inputs required to store the incoming data
   * @param bufferCapacity Capacity of each RawBatchBuffer.
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/AbstractDataCollector.java`
#### Snippet
```java
   * @param numBuffers Number of RawBatchBuffer inputs required to store the incoming data
   * @param bufferCapacity Capacity of each RawBatchBuffer.
   * @param context
   */
  public AbstractDataCollector(AtomicInteger parentAccounter,
```

### JavadocDeclaration
`@param dContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/PlanSplitter.java`
#### Snippet
```java
   * Method to plan the query and return list of fragments
   * it will return query plan "as is" or split plans based on the req setting: split_plan
   * @param dContext
   * @param queryId
   * @param req
```

### JavadocDeclaration
`@param queryId` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/PlanSplitter.java`
#### Snippet
```java
   * it will return query plan "as is" or split plans based on the req setting: split_plan
   * @param dContext
   * @param queryId
   * @param req
   * @param connection
```

### JavadocDeclaration
`@param req` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/PlanSplitter.java`
#### Snippet
```java
   * @param dContext
   * @param queryId
   * @param req
   * @param connection
   * @return
```

### JavadocDeclaration
`@param connection` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/PlanSplitter.java`
#### Snippet
```java
   * @param queryId
   * @param req
   * @param connection
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/PlanSplitter.java`
#### Snippet
```java
   * @param req
   * @param connection
   * @return
   */
  public QueryPlanFragments planFragments(DrillbitContext dContext, QueryId queryId,
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/BaseRawBatchBuffer.java`
#### Snippet
```java
   * implementation specific method to enqueue batch
   *
   * @param batch
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/BaseRawBatchBuffer.java`
#### Snippet
```java
   *
   * @param batch
   * @throws IOException
   */
  protected abstract void enqueueInner(final RawFragmentBatch batch) throws IOException;
```

### JavadocDeclaration
`@param count` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ResponseSenderQueue.java`
#### Snippet
```java
  /**
   * Flush only up to a count responses
   * @param count
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ResponseSenderQueue.java`
#### Snippet
```java
   * Flush only up to a count responses
   * @param count
   * @return
   */
  public int flushResponses(int count){
```

### JavadocDeclaration
`@param fragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java
   * Start a new fragment on this node. These fragments can be leaf or intermediate fragments
   * which are scheduled by remote or local Foreman node.
   * @param fragment
   * @throws UserRpcException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ControlMessageHandler.java`
#### Snippet
```java
   * which are scheduled by remote or local Foreman node.
   * @param fragment
   * @throws UserRpcException
   */
  public void startNewFragment(final PlanFragment fragment, final DrillbitContext drillbitContext)
```

### JavadocDeclaration
`@param workUnit` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
   * holds the global PhysicalOperator tree and contains the minor fragment endpoints.
   *
   * @param workUnit
   * @param drillbitContext
   */
```

### JavadocDeclaration
`@param drillbitContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
   *
   * @param workUnit
   * @param drillbitContext
   */
  public RuntimeFilterRouter(QueryWorkUnit workUnit, DrillbitContext drillbitContext) {
```

### JavadocDeclaration
`@param srcRuntimeFilterWritable` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
   * This method is passively invoked by receiving a runtime filter from the network
   *
   * @param srcRuntimeFilterWritable
   */
  public void register(RuntimeFilterWritable srcRuntimeFilterWritable) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/FragmentsRunner.java`
#### Snippet
```java
   * @param rootFragment root fragment
   * @param rootOperator root operator
   * @throws ExecutionSetupException
   */
  private void setupRootFragment(final PlanFragment rootFragment, final FragmentRoot rootOperator) throws ExecutionSetupException {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryResourceManager.java`
#### Snippet
```java
   * memory and parallelism is required for the query.
   * @param memoryPlanning memory planning needs to be done during parallelization
   * @return
   */
  QueryParallelizer getParallelizer(boolean memoryPlanning);
```

### JavadocDeclaration
`@param cost` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/QueryResourceManager.java`
#### Snippet
```java
   * this case, this object will not plan memory, but still needs the cost
   * to place the job into the correct queue.
   * @param cost
   */

```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentManager.java`
#### Snippet
```java
   * Handle the next incoming record batch.
   *
   * @param batch
   * @return True if the fragment has enough incoming data to be able to be run.
   * @throws FragmentSetupException, IOException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentManager.java`
#### Snippet
```java
   * Get the fragment runner for this incoming fragment. Note, this can only be requested once.
   *
   * @return
   */
  FragmentExecutor getRunnable();
```

### JavadocDeclaration
`@param status` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentStatusReporter.java`
#### Snippet
```java
   * Control Tunnel and using WorkEventBus.
   *
   * @param status
   */
  void sendStatus(final FragmentStatus status) {
```

### JavadocDeclaration
`@param root` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     * contain.
     *
     * @param root
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     *
     * @param root
     * @return
     */

```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     * sorts.
     *
     * @return
     */

```

### JavadocDeclaration
`@param nodeAddr` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     * operators.
     *
     * @param nodeAddr
     * @param bufferedOps
     * @param width
```

### JavadocDeclaration
`@param bufferedOps` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     *
     * @param nodeAddr
     * @param bufferedOps
     * @param width
     */
```

### JavadocDeclaration
`@param width` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
     * @param nodeAddr
     * @param bufferedOps
     * @param width
     */

```

### JavadocDeclaration
`@param field` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
  /**
   * Serialize the given {@link SerializedField} into a {@link ResultColumnMetadata}.
   * @param field
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/prepare/PreparedStatementProvider.java`
#### Snippet
```java
   * Serialize the given {@link SerializedField} into a {@link ResultColumnMetadata}.
   * @param field
   * @return
   */
  private static ResultColumnMetadata serializeColumn(SerializedField field) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/Foreman.java`
#### Snippet
```java
   * at some point of time
   * @param fragmentsList fragment list
   * @throws ExecutionSetupException
   */
  private void runFragment(List<PlanFragment> fragmentsList) throws ExecutionSetupException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/Foreman.java`
#### Snippet
```java
   *
   * @param preparedStatementHandle prepared statement handle
   * @throws ExecutionSetupException
   */
  private void runPreparedStatement(final PreparedStatementHandle preparedStatementHandle)
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   * Create a FragmentExecutor where we already have a root operator in memory.
   *
   * @param context
   * @param fragment
   * @param statusReporter
```

### JavadocDeclaration
`@param fragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   *
   * @param context
   * @param fragment
   * @param statusReporter
   * @param rootOperator
```

### JavadocDeclaration
`@param statusReporter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   * @param context
   * @param fragment
   * @param statusReporter
   * @param rootOperator
   */
```

### JavadocDeclaration
`@param rootOperator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   * @param fragment
   * @param statusReporter
   * @param rootOperator
   */
  public FragmentExecutor(final ExecutorFragmentContext context, final PlanFragment fragment,
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   * Create a FragmentExecutor where we need to parse and materialize the root operator.
   *
   * @param context
   * @param fragment
   * @param statusReporter
```

### JavadocDeclaration
`@param fragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   *
   * @param context
   * @param fragment
   * @param statusReporter
   */
```

### JavadocDeclaration
`@param statusReporter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
   * @param context
   * @param fragment
   * @param statusReporter
   */
  public FragmentExecutor(final ExecutorFragmentContext context, final PlanFragment fragment,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
   * Serializes the VectorAccessible va and writes it to an output stream
   * @param output the OutputStream to write to
   * @throws IOException
   */
  @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
   * @param input
   *          the InputStream to read from
   * @throws IOException
   */
  @Override
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
   * never be released by this class, and ownership is maintained by caller.
   *
   * @param batch
   * @param sv2
   * @param allocator
```

### JavadocDeclaration
`@param sv2` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
   *
   * @param batch
   * @param sv2
   * @param allocator
   */
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
   * @param batch
   * @param sv2
   * @param allocator
   */
  public VectorAccessibleSerializable(WritableBatch batch, SelectionVector2 sv2, BufferAllocator allocator) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
  /**
   * For a blocking writer, called after processing all the records to flush out the writes
   * @throws IOException
   */
  void flushBlockingWriter() throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
   *
   * @param writerOptions Contains key, value pair of settings.
   * @throws IOException
   */
  void init(Map<String, String> writerOptions);
```

### JavadocDeclaration
IOException is not declared to be thrown by method init
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
   *
   * @param writerOptions Contains key, value pair of settings.
   * @throws IOException
   */
  void init(Map<String, String> writerOptions);
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
  /**
   * Update the schema in RecordWriter. Called at least once before starting writing the records.
   * @param batch
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
   * Update the schema in RecordWriter. Called at least once before starting writing the records.
   * @param batch
   * @throws IOException
   */
  void updateSchema(VectorAccessible batch);
```

### JavadocDeclaration
IOException is not declared to be thrown by method updateSchema
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StatisticsRecordWriter.java`
#### Snippet
```java
   * Update the schema in RecordWriter. Called at least once before starting writing the records.
   * @param batch
   * @throws IOException
   */
  void updateSchema(VectorAccessible batch);
```

### JavadocDeclaration
IOException is not declared to be thrown by method updatedPlugins
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ConnectorLocator.java`
#### Snippet
```java
   * @return the set of plugin configurations to refresh in the
   * persistent store, or null if none to update
   * @throws IOException for errors
   */
  StoragePlugins updatedPlugins();
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ConnectorLocator.java`
#### Snippet
```java
   * @return the set of bootstrap plugins, or {@code null} if this locator
   * does not provide bootstrap plugins
   * @throws IOException
   */
  StoragePlugins bootstrapPlugins() throws IOException;
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
   * no entry by the given name
   * @throws PluginException for an attempt to remove a system plugin
   * @see {@link #remove(PluginHandle)
   */
  public synchronized PluginHandle remove(String name) throws PluginException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   *
   * @param name The name of the storage plugin to remove
   * @throws PluginException
   */
  void remove(String name) throws PluginException;
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   * @param name The name of the plugin
   * @param config The plugin configuration
   * @return The StoragePlugin instance.
   * @throws PluginException if plugin cannot be created
   */
```

### JavadocDeclaration
IOException is not declared to be thrown by method putJson
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistry.java`
#### Snippet
```java
   * the UI or other external source.
   *
   * @throws IOException if the JSON is invalid
   * @throws PluginException if the underlying
   * {@link #validatedPut(String, StoragePluginConfig)} fails
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SchemaConfig.java`
#### Snippet
```java
   * @param userName Name of the user accessing the storage sources.
   * @param provider Implementation {@link SchemaConfigInfoProvider}
   * @return
   */
  public static Builder newBuilder(final String userName, final SchemaConfigInfoProvider provider) {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * @param failedFunction Brief description of the failed function.
   * @param ex Exception thrown
   * @return
   */
  static DrillPBError createPBError(final String failedFunction, final Throwable ex) {
```

### JavadocDeclaration
`@param session` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
  /**
   * Helper method to create a {@link SchemaConfigInfoProvider} instance for metadata purposes.
   * @param session
   * @param schemaTreeProvider
   * @return
```

### JavadocDeclaration
`@param schemaTreeProvider` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * Helper method to create a {@link SchemaConfigInfoProvider} instance for metadata purposes.
   * @param session
   * @param schemaTreeProvider
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * @param session
   * @param schemaTreeProvider
   * @return
   */
  private static SchemaConfigInfoProvider newSchemaConfigInfoProvider(final DrillConfig config, final UserSession session, final SchemaTreeProvider schemaTreeProvider) {
```

### JavadocDeclaration
`@param likeFilter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * Helper method to create {@link FunctionExprNode} from {@link LikeFilter}.
   * @param fieldName Name of the filed on which the like expression is applied.
   * @param likeFilter
   * @return {@link FunctionExprNode} for given arguments. Null if the <code>likeFilter</code> is null.
   */
```

### JavadocDeclaration
`@param tableType` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
  /**
   * Helper method to create a {@link PojoRecordReader} for given arguments.
   * @param tableType
   * @param filter
   * @param provider
```

### JavadocDeclaration
`@param filter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * Helper method to create a {@link PojoRecordReader} for given arguments.
   * @param tableType
   * @param filter
   * @param provider
   * @param userSession
```

### JavadocDeclaration
`@param provider` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * @param tableType
   * @param filter
   * @param provider
   * @param userSession
   * @return
```

### JavadocDeclaration
`@param userSession` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * @param filter
   * @param provider
   * @param userSession
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/MetadataProvider.java`
#### Snippet
```java
   * @param provider
   * @param userSession
   * @return
   */
  private static <S> PojoRecordReader<S> getPojoRecordReader(final InfoSchemaTableType tableType, final InfoSchemaFilter filter, final DrillConfig config,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordReader.java`
#### Snippet
```java
   *          The place where output for a particular scan should be written. The record reader is responsible for
   *          mutating the set of schema values for that particular record.
   * @throws ExecutionSetupException
   */
  void setup(OperatorContext context, OutputMutator output) throws ExecutionSetupException;
```

### JavadocDeclaration
`@param schemaConfig` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SchemaTreeProvider.java`
#### Snippet
```java
  /**
   * Create and return a SchemaTree with given <i>schemaConfig</i>.
   * @param schemaConfig
   * @return Root of the schema tree.
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/DrillSchemaFactory.java`
#### Snippet
```java
   * @param schemaConfig Configuration for schema objects.
   * @param parent Reference to parent schema.
   * @throws IOException
   */
  @Override
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SystemPluginLocator.java`
#### Snippet
```java
   * class implementation, name absence are detected.
   *
   * @return map with system plugins stored by name
   */
  @Override
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * modules to include formats during bootstrap. The file must still indicate which
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * plugin (config) to modify, and must list the same format for each supported file
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * system plugin such as {@code dfs} and {@ cp}. Despite this limitation, the additional
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * file makes the non-core format plugins independent of the core files.
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * The resulting bootstrap process is to 1) gather the storage plugins, and 2)
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * merge any format bootstrap into these plugins.
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Upgrade Files</h4>
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Bootstrap files populate a new system. There are times when a user upgrades
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * their Drill install and we would like to push out configurations for newly
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * added plugins. This is done via the optional upgrade file.
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is new, configurations from the upgrade file replace
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * (override) values from the bootstrap file. The net effect, bootstrap plus
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * upgrades, are written into the persistent store.
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <p>
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * If the system is upgraded, then the current technique uses a highly
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * unreliable system: an upgrade file exists. Any upgrades are applied on top
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * of the user's stored values. The upgrade file is then deleted to avoid
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * applying the same upgrades multiple times. Not idea, but it is what it
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is for now.
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Format Plugins</h4>
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
`@param fsDataInputStream` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/OpenFileTracker.java`
#### Snippet
```java
  /**
   * Remove the given {@link org.apache.drill.exec.store.dfs.DrillFSDataInputStream} from opened file list.
   * @param fsDataInputStream
   */
  public void fileClosed(DrillFSDataInputStream fsDataInputStream);
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * Earlier versions of this code provided support for format plugins. However,
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * this code never worked. The original design of Drill has format plugins
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
`@param mapper` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionExtractor.java`
#### Snippet
```java
   *          returned by
   *          {@link FormatPluginOptionExtractor#getTableSignatures(String, List)})
   * @param mapper
   * @return the config
   */
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * as a set of attributes of the file system plugin config. Additional work
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * is needed to allow file system configs to share plugins. Such work is a
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * good idea, so the ill-fated format plugin code still appears, but does
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyFileScanBuilder.java`
#### Snippet
```java
   * @param scan the physical operation definition for the scan operation. Contains
   * one or more files to read. (The Easy format plugin works only for files.)
   * @return the scan framework which orchestrates the scan operation across
   * potentially many files
   * @throws ExecutionSetupException for all setup failures
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * nothing.
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
ExecutionSetupException is not declared to be thrown by method EasyFileScanBuilder
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyFileScanBuilder.java`
#### Snippet
```java
   * @return the scan framework which orchestrates the scan operation across
   * potentially many files
   * @throws ExecutionSetupException for all setup failures
   */
  public EasyFileScanBuilder(FragmentContext context, EasySubScan scan,
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 * <h4>Config Properties</h4>
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
Illegal character
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
 *
 * <dl>
 * <dt>{@code ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE}
 * ({@code drill.exec.storage.bootstrap.storage})</dt>
 * <dd>The name of the bootstrap file, normally
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
     * Also, this style does not allow retaining some fields
     * while customizing others. (DRILL-6168).
     * @return
     */
    @SuppressWarnings("unused")
```

### JavadocDeclaration
`@param mapper` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
   *
   * @param t the signature and the parameters passed to the table function
   * @param mapper
   * @return the corresponding config
   */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EvfV1ScanBuilder.java`
#### Snippet
```java
 * This is for "version 1" of EVF. Newer code should use "version 2."
 *
 * @return the scan framework which orchestrates the scan operation across
 * potentially many files
 * @throws ExecutionSetupException for all setup failures
```

### JavadocDeclaration
Tag `throws` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EvfV1ScanBuilder.java`
#### Snippet
```java
 * @return the scan framework which orchestrates the scan operation across
 * potentially many files
 * @throws ExecutionSetupException for all setup failures
 */
class EvfV1ScanBuilder {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/FilterPushDownListener.java`
#### Snippet
```java
     * push-down, @{code null} if not and thus the relop should remain in
     * the Drill plan
     * @see {@link ConstantHolder#normalize(org.apache.drill.common.types.TypeProtos.MinorType)}
     */
    ExprNode accept(ExprNode conjunct);
```

### JavadocDeclaration
`@param rowWriter` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/avro/AvroBatchReader.java`
#### Snippet
```java
  /**
   * Process one row of records.
   * @param rowWriter
   * @return true true if one row is processed, false the EOF is reached.
   */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/RelOp.java`
#### Snippet
```java
   *
   * @return crude estimate of operator selectivity
   * @see {@code package org.apache.calcite.rel.metadata.RelMdUtil}
   */
  public double selectivity() {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/ConstantHolder.java`
#### Snippet
```java
   * @return a constant of the requested type
   * @throws RuntimeException if the conversion is not legal
   * @see {@link #normalize(MinorType)} for a "save" version of this
   * method
   */
```

### JavadocDeclaration
Class reference expected
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/JsonLoader.java`
#### Snippet
```java
   *
   * @return {@code true} if at least one record was loaded, {@code false} if EOF.
   * @throws {org.apache.drill.common.exceptions.UserException
   * for most errors
   * @throws RuntimeException for unexpected errors, most often due
```

### JavadocDeclaration
Wrong tag `id:1,`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/package-info.java`
#### Snippet
```java
 *    graph:[
 *        {
 *            @id:1,
 *            pop:"mock-scan",
 *            url: "http://apache.org",
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/CompliantTextBatchReader.java`
#### Snippet
```java
  /**
   * Create and open the text reader.
   * @throws EarlyEofException
   */
  public CompliantTextBatchReader(FileSchemaNegotiator schemaNegotiator,
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockTableDef.java`
#### Snippet
```java
   * group can have a distinct schema; each may be read in a separate
   * fragment.
   * @return
   */

```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
   * @param status file status
   * @return Footer
   * @throws IOException
   */
  public static Footer readFooter(final Configuration config, final FileStatus status) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
   * @param parallelism parallelism
   * @return a list of footers
   * @throws IOException
   */
  public static List<Footer> getFooters(final Configuration conf, List<FileStatus> statuses, int parallelism) throws IOException {
```

### JavadocDeclaration
`@param fs` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java
     * Check if the metadata cache files exist
     * @param dir the path of the directory
     * @param fs
     * @return true if both file metadata and summary cache file exist
     * @throws IOException in case of problems during accessing files
```

### JavadocDeclaration
`@param min` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V1.java`
#### Snippet
```java
     * setter used during deserialization of the 'min' field of the metadata cache file.
     *
     * @param min
     */
    @JsonProperty(value = "min")
```

### JavadocDeclaration
`@param max` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V1.java`
#### Snippet
```java
     * setter used during deserialization of the 'max' field of the metadata cache file.
     *
     * @param max
     */
    @JsonProperty(value = "max")
```

### JavadocDeclaration
`@param type` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetColumnMetadata.java`
#### Snippet
```java

  /**
   * @param type
   * @param type a fixed length type from the parquet library enum
   * @return the length in pageDataByteArray of the type
```

### JavadocDeclaration
Duplicate @param tag for parameter 'type'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetColumnMetadata.java`
#### Snippet
```java
  /**
   * @param type
   * @param type a fixed length type from the parquet library enum
   * @return the length in pageDataByteArray of the type
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
     * @param skipCount the number of rows to skip (optional)
     *
     * @throws IOException
     */
    void resetDefinitionLevelReader(int skipCount) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkInput.java`
#### Snippet
```java
   *
   * @param columnPrecInfo input/output precision info container
   * @throws IOException
   */
  private final void guessColumnPrecision(ColumnPrecisionInfo columnPrecInfo) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
   *
   * @return true if we need more data and page is not read successfully
   * @throws IOException
   */
  public boolean readPage() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
     *
     * @return records to read
     * @throws IOException
     */
    @Override public Long call() throws IOException{
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
     *
     * @return records to read
     * @throws IOException
     */
    @Override public Integer call() throws IOException{
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
   * @param recordsReadInCurrentPass records read in current pass
   * @return true if we should stop reading
   * @throws IOException
   */
  public boolean determineSize(long recordsReadInCurrentPass) throws IOException {
```

### JavadocDeclaration
Wrong tag `returns`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  /**
   * Decodes any repetition and definition level data in this page
   * @returns the offset into the page buffer after any levels have been decoded.
   */
  protected int decodeLevels() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * @param skipCount the number of rows to skip (optional)
   *
   * @throws IOException
   */
  void resetDefinitionLevelReader(int skipCount) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * Inspects the type of the next page and dispatches it for dictionary loading
   * or data decompression accordingly.
   * @throws IOException
   */
  protected void nextInternal() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   *
   * @return true if a page was found to read
   * @throws IOException
   */
  public boolean next() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  /**
   * Reads the next page header available in the backing input stream.
   * @throws IOException
   */
  protected void readPageHeader() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  /**
   * Reads and stores this column chunk's dictionary.
   * @throws IOException
   */
  protected void loadDictionary() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * sections from compression.
   * @return decompressed Parquet page data
   * @throws IOException
   */
  protected DrillBuf readCompressedPageV2() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * Reads an uncompressed Parquet page without copying the buffer returned by the backing input stream.
   * @return uncompressed Parquet page data
   * @throws IOException
   */
  protected DrillBuf readUncompressedPage() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * Skip over n bytes of column data
   * @param n number of bytes to skip
   * @throws IOException
   */
  protected void skip(long n) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
   * in their entirety.
   * @return decompressed Parquet page data
   * @throws IOException
   */
  protected DrillBuf readCompressedPageV1() throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchSizerManager.java`
#### Snippet
```java
   *
   * @param vectorMap a collection of value vectors keyed by their field names
   * @throws OutOfMemoryException
   */
  public void allocate(Map<String, ValueVector> vectorMap) throws OutOfMemoryException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
  /**
   * Reads and stores this column chunk's dictionary page.
   * @throws IOException
   */
  protected void loadDictionary(ReadStatus readStatus) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
   * in their entirety.
   * @return decompressed Parquet page data
   * @throws IOException
   */
  protected DrillBuf decompressPageV1(ReadStatus readStatus) throws IOException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
   * sections from compression.
   * @return decompressed Parquet page data
   * @throws IOException
   */
  protected DrillBuf decompressPageV2(ReadStatus readStatus) throws IOException {
```

### JavadocDeclaration
Wrong tag `returns`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
   * Blocks for a page to become available in the queue then takes it and schedules a new page
   * read task if the queue was full.
   * @returns ReadStatus the page taken from the queue
   */
  private ReadStatus nextPageFromQueue() throws InterruptedException, ExecutionException {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/AssignmentCreator.java`
#### Snippet
```java
   * distribute work assigned to a given endpoint to all corresponding minor fragments evenly
   *
   * @return
   */
  private Map<DrillbitEndpoint,FragIteratorWrapper> getEndpointIterators() {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/BlockMapBuilder.java`
#### Snippet
```java
   *
   * @param work the FileWork to calculate endpoint bytes for
   * @throws IOException
   */
  public EndpointByteMap getEndpointByteMap(Set<String> noDrillbitHosts, FileWork work) throws IOException {
```

### JavadocDeclaration
`@param targetBatchNum` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
   * name: col1, minor_type: int4, data_mode: nullable
   * name: col2, minor_type: int4, data_mode: non-nullable
   * @param targetBatchNum
   * @throws Exception
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
   * name: col2, minor_type: int4, data_mode: non-nullable
   * @param targetBatchNum
   * @throws Exception
   */
  protected void doBatch(FileInputStream input, int targetBatchNum, boolean showHeader) throws Exception {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
   *   Schema changes: 1
   *   Schema change batch indices: 0
   * @throws Exception
   */
  protected void doQuery(FileInputStream input) throws Exception{
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TableFunctionContract.java`
#### Snippet
```java
   * sets the incoming record batch for the operator. The innerNext() method requires one of the setIncoming methods
   * to be called before it is called.
   * @param incoming
   */
  void setIncoming(RecordBatch incoming);
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TableFunctionContract.java`
#### Snippet
```java
   * sets the incoming Lateral Contract for the operator. The innerNext() method requires one of the setIncoming methods
   * to be called before it is called.
   * @param incoming
   */
  void setIncoming(LateralContract incoming);
```

### JavadocDeclaration
`@param outcome` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/AbstractUnaryRecordBatch.java`
#### Snippet
```java
  /**
   * Set's the outcome received with current input batch in processing
   * @param outcome
   */
  protected void setLastKnownOutcome(IterOutcome outcome) {
```

### JavadocDeclaration
`@param t1` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java
  /**
   * We treat fields with same set of Subtypes as equal, even if they are in a different order
   * @param t1
   * @param t2
   * @return
```

### JavadocDeclaration
`@param t2` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java
   * We treat fields with same set of Subtypes as equal, even if they are in a different order
   * @param t1
   * @param t2
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java
   * @param t1
   * @param t2
   * @return
   */
  private static boolean majorTypeEqual(MajorType t1, MajorType t2) {
```

### JavadocDeclaration
`@param field` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
   * the field type is MAP. Otherwise after calling this method if caller is not creating TransferPair on the
   * ValueVector, then the new ValueVector will not have information about it's list of children MaterializedField.
   * @param field
   * @param callBack
   * @param <T>
```

### JavadocDeclaration
`@param callBack` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
   * ValueVector, then the new ValueVector will not have information about it's list of children MaterializedField.
   * @param field
   * @param callBack
   * @param <T>
   * @return
```

### JavadocDeclaration
`@param ` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
   * @param field
   * @param callBack
   * @param <T>
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
   * @param callBack
   * @param <T>
   * @return
   */
  @SuppressWarnings("unchecked")
```

### JavadocDeclaration
`@param newTargetOutputCount` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchMemoryManager.java`
#### Snippet
```java
   * Set the max row count which the current output batch (already allocated) can contain. Since this setter doesn't
   * adjust the input value we make sure it doesn't go above MAX_NUM_ROWS
   * @param newTargetOutputCount
   */
  public void setCurrentOutgoingMaxRowCount(int newTargetOutputCount) {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
  /**
   * Generate a new BatchSchema object based on the current state of the builder.
   * @return
   * @throws SchemaChangeException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
   * Generate a new BatchSchema object based on the current state of the builder.
   * @return
   * @throws SchemaChangeException
   */
  public BatchSchema build() {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method build
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
   * Generate a new BatchSchema object based on the current state of the builder.
   * @return
   * @throws SchemaChangeException
   */
  public BatchSchema build() {
```

### JavadocDeclaration
`@param hasMoreRecordInBoundary` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/AbstractSingleRecordBatch.java`
#### Snippet
```java
   * or OK outcome.
   *
   * @param hasMoreRecordInBoundary
   * @return EMIT - If the lastknownOutcome was EMIT and output records
   *         corresponding to all the incoming records in current record
```

### JavadocDeclaration
`@param in` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
  /**
   * Creates a copy a record batch, converting any fields as necessary to coerce it into the provided schema
   * @param in
   * @param toSchema
   * @param context
```

### JavadocDeclaration
`@param toSchema` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
   * Creates a copy a record batch, converting any fields as necessary to coerce it into the provided schema
   * @param in
   * @param toSchema
   * @param context
   * @return
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
   * @param in
   * @param toSchema
   * @param context
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
   * @param toSchema
   * @param context
   * @return
   */
  public static VectorContainer coerceContainer(VectorAccessible in, BatchSchema toSchema, OperatorContext context) {
```

### JavadocDeclaration
`@param schemas` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
   * Returns the merger of schemas. The merged schema will include the union all columns. If there is a type conflict
   * between columns with the same schemapath but different types, the merged schema will contain a Union type.
   * @param schemas
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java
   * between columns with the same schemapath but different types, the merged schema will contain a Union type.
   * @param schemas
   * @return
   */
  public static BatchSchema mergeSchemas(BatchSchema... schemas) {
```

### JavadocDeclaration
`@param type` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * Run query based on list of fragments that were supposedly produced during query planning phase. Supported
   * query type is {@link QueryType#EXECUTION}
   * @param type
   * @param planFragments
   * @param resultsListener
```

### JavadocDeclaration
`@param planFragments` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * query type is {@link QueryType#EXECUTION}
   * @param type
   * @param planFragments
   * @param resultsListener
   * @throws RpcException
```

### JavadocDeclaration
`@param resultsListener` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @param type
   * @param planFragments
   * @param resultsListener
   * @throws RpcException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @param planFragments
   * @param resultsListener
   * @throws RpcException
   */
  public void runQuery(QueryType type, List<PlanFragment> planFragments, UserResultsListener resultsListener)
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @param plan Query to execute
   * @return a handle for the query result
   * @throws RpcException
   */
  public List<QueryDataBatch> runQuery(QueryType type, String plan) throws RpcException {
```

### JavadocDeclaration
Duplicate @return tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @return the server version, or null if not connected or if the server
   *         doesn't provide the version
   * @return The server version.
   */
  public Version getServerVersion() {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * Connects the client to a Drillbit server
   *
   * @throws RpcException
   */
  public void connect() throws RpcException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   *                                {@link #createPreparedStatement(String)}.
   * @return List of {@link QueryDataBatch}s. It is responsibility of the caller to release query data batches.
   * @throws RpcException
   */
  @VisibleForTesting
```

### JavadocDeclaration
`@param type` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * API to just plan a query without execution
   *
   * @param type
   * @param query
   * @param isSplitPlan
```

### JavadocDeclaration
`@param query` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   *
   * @param type
   * @param query
   * @param isSplitPlan
   *          option to tell whether to return single or split plans for a query
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * Start's a connection from client to server
   * @param props - not null {@link Properties} filled with connection url parameters
   * @throws RpcException
   */
  public void connect(Properties props) throws RpcException {
```

### JavadocDeclaration
Duplicate @return tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @return the server name, or null if not connected or if the server
   *         doesn't provide the name
   * @return The server name.
   */
  public String getServerName() {
```

### JavadocDeclaration
`@param query` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * Create a prepared statement for given the {@code query}.
   *
   * @param query
   * @return The prepared statement for given the {@code query}.
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
   * @param connect - Zookeeper connection string provided at connection URL
   * @param props - not null {@link Properties} filled with connection url parameters
   * @throws RpcException
   */
  public synchronized void connect(String connect, Properties props) throws RpcException {
```

### JavadocDeclaration
`@param typeFieldId` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
   * Generates the full path to a field from the typefield ids
   *
   * @param typeFieldId
   * @param recordBatch
   * @return
```

### JavadocDeclaration
`@param recordBatch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
   *
   * @param typeFieldId
   * @param recordBatch
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
   * @param typeFieldId
   * @param recordBatch
   * @return
   */
  public static String getPath(TypedFieldId typeFieldId, RecordBatch recordBatch) {
```

### JavadocDeclaration
`@param templateName` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
  /**
   * Create the web page using the given template and {@link SecurityContext} after authentication is done.
   * @param templateName
   * @param sc
   * @return
```

### JavadocDeclaration
`@param sc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * Create the web page using the given template and {@link SecurityContext} after authentication is done.
   * @param templateName
   * @param sc
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * @param templateName
   * @param sc
   * @return
   */
  public static Viewable create(final boolean authEnabled, final String templateName, final SecurityContext sc) {
```

### JavadocDeclaration
`@param templateName` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
  /**
   * Create a web page using the given template, {@link SecurityContext} and model data.
   * @param templateName
   * @param sc
   * @param model
```

### JavadocDeclaration
`@param sc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * Create a web page using the given template, {@link SecurityContext} and model data.
   * @param templateName
   * @param sc
   * @param model
   * @return
```

### JavadocDeclaration
`@param model` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * @param templateName
   * @param sc
   * @param model
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * @param sc
   * @param model
   * @return
   */
  public static Viewable create(final boolean authEnabled, final String templateName,
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/ViewableWithPermissions.java`
#### Snippet
```java
   * Create a login page.
   * @param errorMsg Optional error messages to be shown in case when the page is requested after login attempt failure.
   * @return
   */
  public static Viewable createLoginPage(final String errorMsg) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/LogInLogOutResources.java`
#### Snippet
```java
   * @param redirect - Redirect parameter in the request URI
   * @param request  - Http Servlet Request
   * @throws Exception
   */
  private void updateSessionRedirectInfo(String redirect, HttpServletRequest request) throws Exception {
```

### JavadocDeclaration
`@param request` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
  /**
   * Method to authenticate a user session using the SPNEGO token passed in AUTHORIZATION header of request.
   * @param request
   * @param response
   * @param mandatory
```

### JavadocDeclaration
`@param response` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * Method to authenticate a user session using the SPNEGO token passed in AUTHORIZATION header of request.
   * @param request
   * @param response
   * @param mandatory
   * @return
```

### JavadocDeclaration
`@param mandatory` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param request
   * @param response
   * @param mandatory
   * @return
   * @throws ServerAuthException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param response
   * @param mandatory
   * @return
   * @throws ServerAuthException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param mandatory
   * @return
   * @throws ServerAuthException
   */
  private Authentication authenticateSession(ServletRequest request, ServletResponse response, boolean mandatory)
```

### JavadocDeclaration
`@param request` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * 3) Clear-Up in memory session information once LogOut is triggered such that any future request also triggers SPNEGO
   *    authentication.
   * @param request
   * @param response
   * @param mandatoryAuth
```

### JavadocDeclaration
`@param response` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   *    authentication.
   * @param request
   * @param response
   * @param mandatoryAuth
   * @return
```

### JavadocDeclaration
`@param mandatoryAuth` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param request
   * @param response
   * @param mandatoryAuth
   * @return
   * @throws ServerAuthException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param response
   * @param mandatoryAuth
   * @return
   * @throws ServerAuthException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoAuthenticator.java`
#### Snippet
```java
   * @param mandatoryAuth
   * @return
   * @throws ServerAuthException
   */
  @Override
```

### JavadocDeclaration
Wrong tag `link{org.apache.drill.exec.server.rest.QueryResources#submitQuery}`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRestServer.java`
#### Snippet
```java
     * unauthenticated Drill is also not enough for general impersonation.
     * Choose one way to request impersonation over HTTP, this or the other.
     * @link{org.apache.drill.exec.server.rest.QueryResources#submitQuery}
     */
    @Deprecated
```

### JavadocDeclaration
`@param startTimeMillis` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/SimpleDurationFormat.java`
#### Snippet
```java
  /**
   * If end time is less than the start time, current epoch time is assumed as the end time.
   * @param startTimeMillis
   * @param endTimeMillis
   */
```

### JavadocDeclaration
`@param endTimeMillis` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/SimpleDurationFormat.java`
#### Snippet
```java
   * If end time is less than the start time, current epoch time is assumed as the end time.
   * @param startTimeMillis
   * @param endTimeMillis
   */
  public SimpleDurationFormat(long startTimeMillis, long endTimeMillis) {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillHttpSecurityHandlerProvider.java`
#### Snippet
```java
   * compatibility if authentication is enabled it will include FORM mechanism by default.
   * @param config - {@link DrillConfig}
   * @return
   */
  public static Set<String> getHttpAuthMechanisms(DrillConfig config) {
```

### JavadocDeclaration
`@param queryText` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/ProfileResources.java`
#### Snippet
```java
     * Extract only the first 150 characters of the query.
     * If this spans more than 8 lines, we truncate excess lines for sake of readability
     * @param queryText
     * @return truncated text
     */
```

### JavadocDeclaration
`@param operatorType` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
  /**
   * Returns index of Spill Count/Cycle metric
   * @param operatorType
   * @return index of spill metric
   */
```

### JavadocDeclaration
`@param fields` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/FieldSelection.java`
#### Snippet
```java
  /**
   * Generates a field selection based on a list of fields.  Assumes that a partial path a.b is equivalent to a.b.*
   * @param fields
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/FieldSelection.java`
#### Snippet
```java
   * Generates a field selection based on a list of fields.  Assumes that a partial path a.b is equivalent to a.b.*
   * @param fields
   * @return
   */
  public static FieldSelection getFieldSelection(List<SchemaPath> fields){
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/impl/VectorContainerWriter.java`
#### Snippet
```java

/**
 * @see {@link org.apache.drill.exec.physical.resultSet.ResultSetLoader} - the replacement for this class
 */
@Deprecated
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBuilder.java`
#### Snippet
```java
   * plain Java code
   * @return the class that the code generator defines
   * @throws ClassTransformationException
   */
  public Class<?> getImplementationClass(CodeGenerator<?> cg) throws ClassTransformationException {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ReplacingBasicValue.java`
#### Snippet
```java
   * @param index the original local variable slot assigned to the value
   * @param valueList TODO
   * @return
   */
  public static ReplacingBasicValue create(final Type type, final ValueHolderIden iden, final int index,
```

### JavadocDeclaration
`@param adder` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java
    /**
     *
     * @param adder
     * @param owner
     * @param name
```

### JavadocDeclaration
`@param owner` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java
     *
     * @param adder
     * @param owner
     * @param name
     * @param desc
```

### JavadocDeclaration
`@param name` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java
     * @param adder
     * @param owner
     * @param name
     * @param desc
     * @return amount of additional stack space that will be required for this instruction stream
```

### JavadocDeclaration
`@param desc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java
     * @param owner
     * @param name
     * @param desc
     * @return amount of additional stack space that will be required for this instruction stream
     */
```

### JavadocDeclaration
`@param newPartitions` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * Create a new TableScan rel node, given the lists of new partitions or new files to scan and a path
   * to a metadata cache file
   * @param newPartitions
   * @param cacheFileRoot
   * @param wasAllPartitionsPruned
```

### JavadocDeclaration
`@param cacheFileRoot` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * to a metadata cache file
   * @param newPartitions
   * @param cacheFileRoot
   * @param wasAllPartitionsPruned
   * @param metaContext
```

### JavadocDeclaration
`@param wasAllPartitionsPruned` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * @param newPartitions
   * @param cacheFileRoot
   * @param wasAllPartitionsPruned
   * @param metaContext
   * @throws Exception
```

### JavadocDeclaration
`@param metaContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * @param cacheFileRoot
   * @param wasAllPartitionsPruned
   * @param metaContext
   * @throws Exception
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * @param wasAllPartitionsPruned
   * @param metaContext
   * @throws Exception
   */
  TableScan createTableScan(List<PartitionLocation> newPartitions, Path cacheFileRoot,
```

### JavadocDeclaration
`@param plannerSettings` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * Method returns the Major type associated with the given column
   * @param column - column whose type should be determined
   * @param plannerSettings
   */
  TypeProtos.MajorType getVectorType(SchemaPath column, PlannerSettings plannerSettings);
```

### JavadocDeclaration
`@param newPartitions` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
  /**
   * Create a new TableScan rel node, given the lists of new partitions or new files to SCAN.
   * @param newPartitions
   * @param wasAllPartitionsPruned
   * @throws Exception
```

### JavadocDeclaration
`@param wasAllPartitionsPruned` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * Create a new TableScan rel node, given the lists of new partitions or new files to SCAN.
   * @param newPartitions
   * @param wasAllPartitionsPruned
   * @throws Exception
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/PartitionDescriptor.java`
#### Snippet
```java
   * @param newPartitions
   * @param wasAllPartitionsPruned
   * @throws Exception
   */
  TableScan createTableScan(List<PartitionLocation> newPartitions,
```

### JavadocDeclaration
`@param index` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/ParquetPartitionLocation.java`
#### Snippet
```java
   * Parquet CTAS auto partitioning scheme does not support getting the partition value
   * based on the location string.
   * @param index
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/ParquetPartitionLocation.java`
#### Snippet
```java
   * based on the location string.
   * @param index
   * @return
   */
  @Override
```

### JavadocDeclaration
`@param map` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
  /**
   *
   * @param map
   * @param selection
   * @param moveForward
```

### JavadocDeclaration
`@param selection` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   *
   * @param map
   * @param selection
   * @param moveForward
   *          Whether or not we should start with using the current token or the
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   *          next token. If moveForward = true, we should start with the next
   *          token and ignore the current one.
   * @throws IOException
   */
  private void writeData(MapWriter map, FieldSelection selection,
```

### JavadocDeclaration
`@param writer` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * (if extended mode is enabled). Whether extended is enable or disabled, will
   * consume the next token in the stream.
   * @param writer
   * @param fieldName
   * @return
```

### JavadocDeclaration
`@param fieldName` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * consume the next token in the stream.
   * @param writer
   * @param fieldName
   * @return
   * @throws IOException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * @param writer
   * @param fieldName
   * @return
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * @param fieldName
   * @return
   * @throws IOException
   */
  private boolean writeMapDataIfTyped(MapWriter writer, String fieldName) throws IOException {
```

### JavadocDeclaration
`@param writer` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * (if extended mode is enabled). Whether extended is enable or disabled, will
   * consume the next token in the stream.
   * @param writer
   * @return
   * @throws IOException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * consume the next token in the stream.
   * @param writer
   * @return
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
   * @param writer
   * @return
   * @throws IOException
   */
  private boolean writeListDataIfTyped(ListWriter writer) throws IOException {
```

### JavadocDeclaration
`@param defaultSchema` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/SchemaUtilities.java`
#### Snippet
```java
   *   <li>Schema found for given <i>schemaPath</i> is a root schema.</li>
   *
   * @param defaultSchema
   * @param schemaPath
   * @return schema, if found. Otherwise, throws an {@link UserException}
```

### JavadocDeclaration
`@param schemaPath` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/SchemaUtilities.java`
#### Snippet
```java
   *
   * @param defaultSchema
   * @param schemaPath
   * @return schema, if found. Otherwise, throws an {@link UserException}
   */
```

### JavadocDeclaration
`@param relTree` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindHardDistributionScans.java`
#### Snippet
```java
   * <code>relTree</code> contains one or more scans with hard affinity requirements.
   *
   * @param relTree
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindHardDistributionScans.java`
#### Snippet
```java
   *
   * @param relTree
   * @return
   */
  public static boolean canForceSingleMode(final RelNode relTree) {
```

### JavadocDeclaration
`@param table` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/DrillIndexDescriptor.java`
#### Snippet
```java
  /**
   * Set the drill table corresponding to the index
   * @param table
   */
  public void setDrillTable(DrillTable table) {
```

### JavadocDeclaration
`@param storageName` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/DrillIndexDescriptor.java`
#### Snippet
```java
  /**
   * Set the storage plugin name
   * @param storageName
   */
  public void setStorageName(String storageName) {
```

### JavadocDeclaration
`@param path` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexInfo.java`
#### Snippet
```java
   * getNewPath: for an original path, return new rename '$N' path, notice there could be multiple renamed paths
   * if the there are multiple functional indexes refer original path.
   * @param path
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexInfo.java`
#### Snippet
```java
   * if the there are multiple functional indexes refer original path.
   * @param path
   * @return
   */
  SchemaPath getNewPath(SchemaPath path);
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
    /**
     * Get a single IndexConditionInfo in which indexCondition has field  on all indexes in this.indexes
     * @return
     */
    public IndexConditionInfo getCollectiveInfo(IndexLogicalPlanCallContext indexContext) {
```

### JavadocDeclaration
`@param indexDesc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
     * check if one or more columns involved in the condition tree form a prefix of the columns in the
     * index keys.
     * @param indexDesc
     * @param initCondition
     * @return True if prefix, False if not
```

### JavadocDeclaration
`@param initCondition` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
     * index keys.
     * @param indexDesc
     * @param initCondition
     * @return True if prefix, False if not
     */
```

### JavadocDeclaration
`@param relevantPaths` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
     *     1), relevant subset of conditions (by relevant, it means at least one given index Expression was found) and,
     *     2), the rest in remainderCondition
     * @param relevantPaths
     * @param condition
     * @return
```

### JavadocDeclaration
`@param condition` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
     *     2), the rest in remainderCondition
     * @param relevantPaths
     * @param condition
     * @return
     */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
     * @param relevantPaths
     * @param condition
     * @return
     */
    public IndexConditionInfo indexConditionRelatedToFields(List<LogicalExpression> relevantPaths, RexNode condition) {
```

### JavadocDeclaration
`@param columns` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
  /**
   * Check if this index 'covers' all the columns specified in the supplied list of columns
   * @param columns
   * @return True for covering index, False for non-covering
   */
```

### JavadocDeclaration
`@param columns` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
  /**
   * Check if this index has some columns specified in the supplied list of columns indexed
   * @param columns
   * @return True if some fields are indexed, False if none of the fields are indexed
   */
```

### JavadocDeclaration
`@param columns` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
  /**
   * Check if this index have all the columns specified in the supplied list of columns indexed
   * @param columns
   * @return True if all fields are indexed, False for some or all fields is not indexed
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDefinition.java`
#### Snippet
```java
  /**
   * Get the list of columns (typically 1 column) that constitute the row key (primary key)
   * @return
   */
  List<LogicalExpression> getRowKeyColumns();
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
   * @param validatedRowType final output row type
   * @return physical relational node
   * @throws RelConversionException
   * @throws SqlUnsupportedException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
   * @return physical relational node
   * @throws RelConversionException
   * @throws SqlUnsupportedException
   */
  protected Prel convertToPrel(RelNode drel, RelDataType validatedRowType)
```

### JavadocDeclaration
`@param conjuncts` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
   * </p>
   *
   * @param conjuncts
   * @param fieldNames
   * @param rexBuilder
```

### JavadocDeclaration
`@param fieldNames` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
   *
   * @param conjuncts
   * @param fieldNames
   * @param rexBuilder
   * @param combinedRangePredicates
```

### JavadocDeclaration
`@param rexBuilder` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
   * @param conjuncts
   * @param fieldNames
   * @param rexBuilder
   * @param combinedRangePredicates
   * @return A list of predicates that includes the newly created predicate
```

### JavadocDeclaration
`@param combinedRangePredicates` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
   * @param fieldNames
   * @param rexBuilder
   * @param combinedRangePredicates
   * @return A list of predicates that includes the newly created predicate
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
  /**
   * return the expressions that were only in equality condition _and_ only once. ( a.b = 'value' )
   * @return
   */
  public Set<LogicalExpression> getExpressionsOnlyInEquality() {
```

### JavadocDeclaration
`@param path` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexCollection.java`
#### Snippet
```java
  /**
   * Check if the field name is the leading key of any of the indexes in this collection
   * @param path
   * @return True if an appropriate index is found, False otherwise
   */
```

### JavadocDeclaration
`@param discover` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverable.java`
#### Snippet
```java
  /**
   * return the found DrillTable with path (e.g. names={"elasticsearch", "staffidx", "stjson"})
   * @param discover
   * @param desc
   * @return
```

### JavadocDeclaration
`@param desc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverable.java`
#### Snippet
```java
   * return the found DrillTable with path (e.g. names={"elasticsearch", "staffidx", "stjson"})
   * @param discover
   * @param desc
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverable.java`
#### Snippet
```java
   * @param discover
   * @param desc
   * @return
   */
    DrillTable findTable(IndexDiscover discover, DrillIndexDescriptor desc);
```

### JavadocDeclaration
`@param idxDesc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * When there is storageName in IndexDescriptor, get a DrillTable instance based on the
   * StorageName and other informaiton in idxDesc that helps identifying the table.
   * @param idxDesc
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * StorageName and other informaiton in idxDesc that helps identifying the table.
   * @param idxDesc
   * @return
   */
  public DrillTable getExternalDrillTable(IndexDescriptor idxDesc) {
```

### JavadocDeclaration
`@param idxDesc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * since the implementations of AbstractStoragePlugin, IndexDescriptor and DrillTable in that storage plugin may have
   * the implement details.
   * @param idxDesc

   * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * @param idxDesc

   * @return
   */
  public DrillTable buildDrillTable(IndexDescriptor idxDesc) {
```

### JavadocDeclaration
`@param idxDesc` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * Otherwise, we call IndexDiscoverable interface exposed from external storage plugin's SchemaFactory
   * to get the desired DrillTable.
   * @param idxDesc
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
   * to get the desired DrillTable.
   * @param idxDesc
   * @return
   */
  public abstract DrillTable getNativeDrillTable(IndexDescriptor idxDesc);
```

### JavadocDeclaration
`@param origScan` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
   * This will impact the columns of scan, the rowType of ScanRel
   *
   * @param origScan
   * @param idxMarker  the IndexableExprMarker that has analyzed original index condition on top of index scan
   * @param idxScan
```

### JavadocDeclaration
`@param idxScan` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
   * @param origScan
   * @param idxMarker  the IndexableExprMarker that has analyzed original index condition on top of index scan
   * @param idxScan
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
   * @param idxMarker  the IndexableExprMarker that has analyzed original index condition on top of index scan
   * @param idxScan
   * @return
   */
  public static RelDataType convertRowTypeForIndexScan(DrillScanRelBase origScan,
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
   * @param indexContext the index plan context
   * @param functionInfo functional index information that may impact rewrite
   * @return
   */
  public static RelDataType rewriteFunctionalRowType(RelNode origScan, IndexCallContext indexContext,
```

### JavadocDeclaration
`@param exprMap` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
  /**
   * Set the map of src expression to target expression, expressions not in the map do not have assigned destinations
   * @param exprMap
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
   * Set the map of src expression to target expression, expressions not in the map do not have assigned destinations
   * @param exprMap
   * @return
   */
  public SimpleRexRemap setExpressionMap(Map<LogicalExpression, LogicalExpression>  exprMap) {
```

### JavadocDeclaration
`@param inputIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringIndexPlanGenerator.java`
#### Snippet
```java
  /**
   *
   * @param inputIndex
   * @param functionInfo functional index information that may impact rewrite
   * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringIndexPlanGenerator.java`
#### Snippet
```java
   * @param inputIndex
   * @param functionInfo functional index information that may impact rewrite
   * @return
   */
  private RexNode rewriteFunctionalCondition(RexNode inputIndex, RelDataType newRowType,
```

### JavadocDeclaration
`@param indexContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  /**
   * This constructor is to build selector for no index condition case (no filter)
   * @param indexContext
   */
  public IndexSelector(IndexCallContext indexContext) {
```

### JavadocDeclaration
`@param indexProps` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
   * match the prefix columns.  If there are additional conditions that don't match prefix
   * columns, that condition is set as a remainder condition.
   * @param indexProps
   */
  public void analyzePrefixMatches(IndexProperties indexProps) {
```

### JavadocDeclaration
`@param settings` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/rules/DbScanToIndexScanPrule.java`
#### Snippet
```java
  /**
   * Return the index collection relevant for the underlying data source
   * @param settings
   * @param scan
   */
```

### JavadocDeclaration
`@param scan` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/rules/DbScanToIndexScanPrule.java`
#### Snippet
```java
   * Return the index collection relevant for the underlying data source
   * @param settings
   * @param scan
   */
  public IndexCollection getIndexCollection(PlannerSettings settings, DrillScanRel scan) {
```

### JavadocDeclaration
`@param tdigest_array` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
  /**
   * Build a Numeric Equi-Depth Histogram from a t-digest byte array
   * @param tdigest_array
   * @param numBuckets
   * @param nonNullCount
```

### JavadocDeclaration
`@param numBuckets` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
   * Build a Numeric Equi-Depth Histogram from a t-digest byte array
   * @param tdigest_array
   * @param numBuckets
   * @param nonNullCount
   * @return An instance of NumericEquiDepthHistogram
```

### JavadocDeclaration
`@param nonNullCount` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
   * @param tdigest_array
   * @param numBuckets
   * @param nonNullCount
   * @return An instance of NumericEquiDepthHistogram
   */
```

### JavadocDeclaration
`@param lastEndPointIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
   * same start point, return either the first matching or last matching depending on firstMatching flag
   * @param value the input double value
   * @param lastEndPointIndex
   * @param firstMatching If true, return the first bucket that matches the specified criteria otherwise return the last one
   * @return index of either the first or last matching bucket if a match was found, otherwise return -1
```

### JavadocDeclaration
`@param implementor` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
  /**
   * Check to make sure that the fields of the inputs are the same as the output field names.  If not, insert a project renaming them.
   * @param implementor
   * @param i
   * @param offset
```

### JavadocDeclaration
`@param i` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * Check to make sure that the fields of the inputs are the same as the output field names.  If not, insert a project renaming them.
   * @param implementor
   * @param i
   * @param offset
   * @param input
```

### JavadocDeclaration
`@param offset` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param implementor
   * @param i
   * @param offset
   * @param input
   * @return
```

### JavadocDeclaration
`@param input` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param i
   * @param offset
   * @param input
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param offset
   * @param input
   * @return
   */
  private LogicalOperator implementInput(DrillImplementor implementor, int i, int offset, RelNode input) {
```

### JavadocDeclaration
`@param implementor` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * Check to make sure that the fields of the inputs are the same as the output field names.
   * If not, insert a project renaming them.
   * @param implementor
   * @param i
   * @param offset
```

### JavadocDeclaration
`@param i` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * If not, insert a project renaming them.
   * @param implementor
   * @param i
   * @param offset
   * @param input
```

### JavadocDeclaration
`@param offset` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param implementor
   * @param i
   * @param offset
   * @param input
   * @param currentNode the node to be implemented
```

### JavadocDeclaration
`@param input` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param i
   * @param offset
   * @param input
   * @param currentNode the node to be implemented
   * @return
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * @param input
   * @param currentNode the node to be implemented
   * @return
   */
  public static LogicalOperator implementInput(DrillImplementor implementor, int i, int offset,
```

### JavadocDeclaration
`@param topProject` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillMergeProjectRule.java`
#### Snippet
```java
   * a single merged project with the assumption that caller knows exactly the semantics/correctness of merging
   * the two projects. This is not applying the full fledged DrillMergeProjectRule.
   * @param topProject
   * @param bottomProject
   * @return new project after replacement
```

### JavadocDeclaration
`@param bottomProject` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillMergeProjectRule.java`
#### Snippet
```java
   * the two projects. This is not applying the full fledged DrillMergeProjectRule.
   * @param topProject
   * @param bottomProject
   * @return new project after replacement
   */
```

### JavadocDeclaration
`@param cluster` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRel.java`
#### Snippet
```java
  /** provide a public method to create an instance of DrillProjectRel.
   *
   * @param cluster
   * @param traits
   * @param child
```

### JavadocDeclaration
`@param traits` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRel.java`
#### Snippet
```java
   *
   * @param cluster
   * @param traits
   * @param child
   * @param exps
```

### JavadocDeclaration
`@param child` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRel.java`
#### Snippet
```java
   * @param cluster
   * @param traits
   * @param child
   * @param exps
   * @param rowType
```

### JavadocDeclaration
`@param exps` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRel.java`
#### Snippet
```java
   * @param traits
   * @param child
   * @param exps
   * @param rowType
   * @return new instance of DrillProjectRel
```

### JavadocDeclaration
`@param rowType` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillProjectRel.java`
#### Snippet
```java
   * @param child
   * @param exps
   * @param rowType
   * @return new instance of DrillProjectRel
   */
```

### JavadocDeclaration
`@param traits` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillWindowRel.java`
#### Snippet
```java
   *
   * @param cluster Cluster
   * @param traits
   * @param child   Input relational expression
   * @param rowType Output row type
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueryParallelizer.java`
#### Snippet
```java
   * @param queryContextInfo query context.
   * @return Executable query plan which contains all the information like minor frags and major frags.
   * @throws ExecutionSetupException
   */
  QueryWorkUnit generateWorkUnit(OptionList options, DrillbitEndpoint foremanNode, QueryId queryId,
```

### JavadocDeclaration
`@param distributionAffinity` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/DistributionAffinity.java`
#### Snippet
```java
   * DistributionAffinity?
   *
   * @param distributionAffinity
   * @return True if the current DistributionAffinity less restrictive than the
   *         given DistributionAffinity. False otherwise.
```

### JavadocDeclaration
`@param fragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/FragmentParallelizer.java`
#### Snippet
```java
   * Parallelize the given fragment.
   *
   * @param fragment
   * @param parameters
   * @param activeEndpoints
```

### JavadocDeclaration
`@param parameters` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/FragmentParallelizer.java`
#### Snippet
```java
   *
   * @param fragment
   * @param parameters
   * @param activeEndpoints
   * @throws PhysicalOperatorSetupException
```

### JavadocDeclaration
`@param activeEndpoints` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/FragmentParallelizer.java`
#### Snippet
```java
   * @param fragment
   * @param parameters
   * @param activeEndpoints
   * @throws PhysicalOperatorSetupException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/FragmentParallelizer.java`
#### Snippet
```java
   * @param parameters
   * @param activeEndpoints
   * @throws PhysicalOperatorSetupException
   */
  void parallelizeFragment(final Wrapper fragment, final ParallelizationParameters parameters,
```

### JavadocDeclaration
`@param rootFragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/PlanningSet.java`
#### Snippet
```java
   * find the root Wrapper which contains the root Fragment and
   * hold the root wrapper
   * @param rootFragment
   * @return root wrapper
   */
```

### JavadocDeclaration
`@param dependsOn` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Wrapper.java`
#### Snippet
```java
   * Add a parallelization dependency on given fragment.
   *
   * @param dependsOn
   */
  public void addFragmentDependency(Wrapper dependsOn) {
```

### JavadocDeclaration
`@param options` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * End goal is to get single major fragment: Screen with chain that ends up with a single minor fragment
   * from Leaf Exchange. This way each plan can run independently without any exchange involvement
   * @param options
   * @param foremanNode - not really applicable
   * @param queryId
```

### JavadocDeclaration
`@param queryId` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param options
   * @param foremanNode - not really applicable
   * @param queryId
   * @param reader
   * @param rootNode
```

### JavadocDeclaration
`@param reader` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param foremanNode - not really applicable
   * @param queryId
   * @param reader
   * @param rootNode
   * @param planningSet
```

### JavadocDeclaration
`@param rootNode` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param queryId
   * @param reader
   * @param rootNode
   * @param planningSet
   * @param session
```

### JavadocDeclaration
`@param planningSet` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param reader
   * @param rootNode
   * @param planningSet
   * @param session
   * @param queryContextInfo
```

### JavadocDeclaration
`@param session` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param rootNode
   * @param planningSet
   * @param session
   * @param queryContextInfo
   * @return
```

### JavadocDeclaration
`@param queryContextInfo` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param planningSet
   * @param session
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param session
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
   */
  private List<QueryWorkUnit> generateWorkUnits(OptionList options, DrillbitEndpoint foremanNode, QueryId queryId,
```

### JavadocDeclaration
`@param options` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
  /**
   * Create multiple physical plans from original query planning, it will allow execute them eventually independently
   * @param options
   * @param foremanNode
   * @param queryId
```

### JavadocDeclaration
`@param foremanNode` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * Create multiple physical plans from original query planning, it will allow execute them eventually independently
   * @param options
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
```

### JavadocDeclaration
`@param queryId` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param options
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
   * @param reader
```

### JavadocDeclaration
`@param activeEndpoints` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
```

### JavadocDeclaration
`@param reader` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param queryId
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
   * @param session
```

### JavadocDeclaration
`@param rootFragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
   * @param session
   * @param queryContextInfo
```

### JavadocDeclaration
`@param session` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param reader
   * @param rootFragment
   * @param session
   * @param queryContextInfo
   * @return
```

### JavadocDeclaration
`@param queryContextInfo` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param rootFragment
   * @param session
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param session
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
   * @param queryContextInfo
   * @return
   * @throws ExecutionSetupException
   */
  @Override
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param roots Root nodes of the plan.
   * @param activeEndpoints currently active drillbit endpoints.
   * @throws PhysicalOperatorSetupException
   */
  public void collectStatsAndParallelizeFragments(PlanningSet planningSet, Set<Wrapper> roots,
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param session session context.
   * @param queryContextInfo query context.
   * @return
   * @throws ExecutionSetupException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param queryContextInfo query context.
   * @return
   * @throws ExecutionSetupException
   */
  @Override
```

### JavadocDeclaration
`@param planningSet` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * Based on the affinity of the Exchange that separates two fragments, setup fragment dependencies.
   *
   * @param planningSet
   * @return Returns a list of leaf fragments in fragment dependency graph.
   */
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   *
   * @param planningSet
   * @return Returns a list of leaf fragments in fragment dependency graph.
   */
  private void constructFragmentDependencyGraph(Fragment rootFragment, PlanningSet planningSet) {
```

### JavadocDeclaration
`@param options` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
  /**
   * Create multiple physical plans from original query planning, it will allow execute them eventually independently
   * @param options
   * @param foremanNode
   * @param queryId
```

### JavadocDeclaration
`@param foremanNode` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * Create multiple physical plans from original query planning, it will allow execute them eventually independently
   * @param options
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
```

### JavadocDeclaration
`@param queryId` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param options
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
   * @param reader
```

### JavadocDeclaration
`@param activeEndpoints` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param foremanNode
   * @param queryId
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
```

### JavadocDeclaration
`@param reader` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param queryId
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
   * @param session
```

### JavadocDeclaration
`@param rootFragment` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param activeEndpoints
   * @param reader
   * @param rootFragment
   * @param session
   * @param queryContextInfo
```

### JavadocDeclaration
`@param session` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param reader
   * @param rootFragment
   * @param session
   * @param queryContextInfo
   * @return The {@link QueryWorkUnit}s.
```

### JavadocDeclaration
`@param queryContextInfo` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param rootFragment
   * @param session
   * @param queryContextInfo
   * @return The {@link QueryWorkUnit}s.
   * @throws ExecutionSetupException
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
   * @param queryContextInfo
   * @return The {@link QueryWorkUnit}s.
   * @throws ExecutionSetupException
   */
  public List<QueryWorkUnit> getSplitFragments(OptionList options, DrillbitEndpoint foremanNode, QueryId queryId,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
   * @param roots root fragments.
   * @param activeEndpoints currently active endpoints.
   * @throws PhysicalOperatorSetupException
   */
  public void adjustMemory(PlanningSet planningSet, Set<Wrapper> roots,
```

### JavadocDeclaration
`@param cluster` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/ProjectPrel.java`
#### Snippet
```java
  /**
   * Constructor for ProjectPrel.
   * @param cluster
   * @param traits traits of ProjectPrel node
   * @param child  input
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
  public static <T> T createHash64Expression(
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
  public static <T> T createHashBasedPartitionExpression(
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashPrelUtil.java`
#### Snippet
```java
   * @param <T> Input and output expression type.
   *           Currently it could be either {@link RexNode} or {@link LogicalExpression}
   * @return
   */
  public static <T> T createHashExpression(
```

### JavadocDeclaration
`@param hashJoinPrel` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
   * to be set later.
   *
   * @param hashJoinPrel
   * @return null or a partial information RuntimeFilterDef
   */
```

### JavadocDeclaration
`@param types` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillType.java`
#### Snippet
```java
  /**
   * Return Glob pattern for given Dot Drill Types.
   * @param types
   * @return Glob pattern representing For Dot Drill Types provided as types param
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
   * @param types Dot Drill Types to be matched
   * @return List of matched DotDrillFile objects
   * @throws IOException
   */
  public static List<DotDrillFile> getDotDrills(DrillFileSystem fs, Path root, String name, DotDrillType... types) throws IOException{
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
   * @param types Dot Drill Types to be matched
   * @return List of matched DotDrillFile objects
   * @throws IOException
   */
  public static List<DotDrillFile> getDotDrills(DrillFileSystem fs, Path root, DotDrillType... types) throws IOException{
```

### JavadocDeclaration
`@param maxWidth` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/EndpointAffinity.java`
#### Snippet
```java
  /**
   * Set the new max width as the minimum of the the given value and current max width.
   * @param maxWidth
   */
  public void setMaxWidth(final int maxWidth) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   *          The particular minor fragment id associated with this particular fragment materialization.
   * @return A materialized Store Operator.
   * @throws PhysicalOperatorSetupException
   */
  public abstract Store getSpecificStore(PhysicalOperator child, int minorFragmentId)
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Store.java`
#### Snippet
```java
   * @param endpoints
   *          The list of endpoints that this Store node are going to be executed on.
   * @throws PhysicalOperatorSetupException
   */
  public abstract void applyAssignments(List<DrillbitEndpoint> endpoints) throws PhysicalOperatorSetupException;
```

### JavadocDeclaration
`@param senderLocations` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Exchange.java`
#### Snippet
```java
   * minorFragmentIds for each sender.
   *
   * @param senderLocations
   */
  void setupSenders(int majorFragmentId, List<DrillbitEndpoint> senderLocations) throws PhysicalOperatorSetupException;
```

### JavadocDeclaration
`@param receiverLocations` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/Exchange.java`
#### Snippet
```java
   * minorFragmentIds for each receiver.
   *
   * @param receiverLocations
   */
  void setupReceivers(int majorFragmentId, List<DrillbitEndpoint> receiverLocations) throws PhysicalOperatorSetupException;
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/DbSubScan.java`
#### Snippet
```java
   * fragment must have a corresponding rowkey join batch instance from which it will retrieve its set of
   * rowkeys to perform the restricted scan.
   * @param batch
   */
  @JsonIgnore
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractBase.java`
#### Snippet
```java
   * Any operator that supports spilling should override this method (and return true)
   * @return false
   * @param queryContext
   */
  @Override @JsonIgnore
```

### JavadocDeclaration
`@param children` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalOperator.java`
#### Snippet
```java
  /**
   * Regenerate with this node with a new set of children.  This is used in the case of materialization or optimization.
   * @param children
   */
  @JsonIgnore
```

### JavadocDeclaration
`@param physicalVisitor` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalOperator.java`
#### Snippet
```java
   * Provides capability to build a set of output based on traversing a query graph tree.
   *
   * @param physicalVisitor
   */
  <T, X, E extends Throwable> T accept(PhysicalVisitor<T, X, E> physicalVisitor, X value) throws E;
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalOperator.java`
#### Snippet
```java
   *
   * @return True iff this operator manages its memory (including disk spilling)
   * @param queryContext
   */
  @JsonIgnore
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TraceInjector.java`
#### Snippet
```java
     * @return same physical operator as passed in, but its child will be a trace operator
     *         whose child will be the original child of this operator
     * @throws ExecutionSetupException
     */
    @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OutputMutator.java`
#### Snippet
```java
  /**
   * Whether or not the fields added to the OutputMutator generated a new schema event.
   * @return
   */
  // TODO(DRILL-2970)
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ImplCreator.java`
#### Snippet
```java
   *          FragmentRoot.
   * @return RootExec of fragment.
   * @throws ExecutionSetupException
   */
  public static RootExec getExec(ExecutorFragmentContext context, FragmentRoot root) throws ExecutionSetupException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
  /**
   * This method must be called before fetching the final priority queue hyper batch and final Sv4 vector.
   * @throws SchemaChangeException
   */
  void generate();
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method generate
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
  /**
   * This method must be called before fetching the final priority queue hyper batch and final Sv4 vector.
   * @throws SchemaChangeException
   */
  void generate();
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
   * @param allocator The {@link BufferAllocator} to use when creating the priority queue.
   * @param hasSv2 True when incoming batches have 2 byte selection vectors. False otherwise.
   * @throws SchemaChangeException
   */
  void init(int limit, BufferAllocator allocator, boolean hasSv2) throws SchemaChangeException;
```

### JavadocDeclaration
`@param container` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
   * Cleanup the old state of queue and recreate a new one with HyperContainer containing vectors in input container
   * and the corresponding indexes (in SV4 format) from input SelectionVector4
   * @param container
   * @param vector4
   * @throws SchemaChangeException
```

### JavadocDeclaration
`@param vector4` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
   * and the corresponding indexes (in SV4 format) from input SelectionVector4
   * @param container
   * @param vector4
   * @throws SchemaChangeException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
   * @param container
   * @param vector4
   * @throws SchemaChangeException
   */
  void resetQueue(VectorContainer container, SelectionVector4 vector4) throws SchemaChangeException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueue.java`
#### Snippet
```java
   * method.
   * @param batch The batch containing elements we want to add.
   * @throws SchemaChangeException
   */
  void add(RecordBatchData batch) throws SchemaChangeException;
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinState.java`
#### Snippet
```java
   * kept in memory, it is joined and sent out. If data in the probe side does not match a build side
   * partition, then it is spilled to disk. After all the probe side data is consumed processing moves
   * on to the {@link HashJoinState#POST_BUILD_CALCULATIONS} state if build side partitions are small enough
   * to fit into memory. If build side partitions can't fit into memory processing moves to the
   * {@link HashJoinState#POST_BUILD_CALCULATIONS}
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinState.java`
#### Snippet
```java
   * on to the {@link HashJoinState#POST_BUILD_CALCULATIONS} state if build side partitions are small enough
   * to fit into memory. If build side partitions can't fit into memory processing moves to the
   * {@link HashJoinState#POST_BUILD_CALCULATIONS}
   * state.
   */
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
    /**
     * Reports whether schema has changed (field was added or re-added) since
     * last call to {@link #isNewSchema}.  Returns true at first call.
     */
    @Override
```

### JavadocDeclaration
`@param numReaders` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   *   - Either implicit column list is empty;
   *   - Or implicit column list has same sie as reader list, and the key set is same across all the readers.
   * @param numReaders
   * @param implicitColumnList
   * @return return true if
```

### JavadocDeclaration
`@param implicitColumnList` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   *   - Or implicit column list has same sie as reader list, and the key set is same across all the readers.
   * @param numReaders
   * @param implicitColumnList
   * @return return true if
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   * and return the decision whether the iteration should continue
   * @return whether we could continue iteration
   * @throws Exception
   */
  private boolean shouldContinueAfterNoRecords() {
```

### JavadocDeclaration
Exception is not declared to be thrown by method shouldContinueAfterNoRecords
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   * and return the decision whether the iteration should continue
   * @return whether we could continue iteration
   * @throws Exception
   */
  private boolean shouldContinueAfterNoRecords() {
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
  /**
   *
   * @param context
   * @param oContext
   * @param readerList
```

### JavadocDeclaration
`@param oContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   *
   * @param context
   * @param oContext
   * @param readerList
   * @param implicitColumnList : either an empty list when all the readers do not have implicit
```

### JavadocDeclaration
`@param readerList` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
   * @param context
   * @param oContext
   * @param readerList
   * @param implicitColumnList : either an empty list when all the readers do not have implicit
   *                        columns, or there is a one-to-one mapping between reader and implicitColumns.
```

### JavadocDeclaration
`@param leftIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * Evaluates the value of record at leftIndex and rightIndex w.r.t min heap condition. It is used in
   * {@link PriorityQueueTemplate#compare(int, int)} method while Heapifying the queue.
   * @param leftIndex
   * @param rightIndex
   * @return
```

### JavadocDeclaration
`@param rightIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * {@link PriorityQueueTemplate#compare(int, int)} method while Heapifying the queue.
   * @param leftIndex
   * @param rightIndex
   * @return
   * @throws SchemaChangeException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * @param leftIndex
   * @param rightIndex
   * @return
   * @throws SchemaChangeException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * @param rightIndex
   * @return
   * @throws SchemaChangeException
   */
  public abstract int doEval(@Named("leftIndex") int leftIndex,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * new root element.
   * This is done for all the records which are seen after the queue is filled with limit number of record indexes.
   * @throws SchemaChangeException
   */
  private void siftDown() throws SchemaChangeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * compared with the record stored at parent index. Since the new record index will flow up in the array hence the
   * name siftUp
   * @throws SchemaChangeException
   */
  private void siftUp() throws SchemaChangeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
   * @param incoming - reference to hyperBatch
   * @param outgoing - null
   * @throws SchemaChangeException
   */
  public abstract void doSetup(@Named("incoming") VectorContainer incoming,
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinStateCalculator.java`
#### Snippet
```java
  /**
   * The current {@link HashJoinState} corresponding to this calculator.
   * @return
   */
  HashJoinState getState();
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java
   * @param newContainer - New container holding the ValueVectors with selected records
   * @param batchBuilder - Builder to build hyper vectors batches
   * @throws SchemaChangeException
   */
  private void copyToPurge(VectorContainer newContainer, SortRecordBatchBuilder batchBuilder) {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method copyToPurge
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java
   * @param newContainer - New container holding the ValueVectors with selected records
   * @param batchBuilder - Builder to build hyper vectors batches
   * @throws SchemaChangeException
   */
  private void copyToPurge(VectorContainer newContainer, SortRecordBatchBuilder batchBuilder) {
```

### JavadocDeclaration
`@param leftIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinTemplate.java`
#### Snippet
```java
   * or equal to the right.
   *
   * @param leftIndex
   * @param rightIndex
   * @return  0 if both keys are equal
```

### JavadocDeclaration
`@param rightIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinTemplate.java`
#### Snippet
```java
   *
   * @param leftIndex
   * @param rightIndex
   * @return  0 if both keys are equal
   *         -1 if left is < right
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinTemplate.java`
#### Snippet
```java
   * @param  leftIndex   position of batch (lower 16 bits) and record (upper 16 bits) in left SV4
   * @param  outIndex  position of the output record batch
   * @return Whether or not the data was copied.
   */
  public abstract void doCopyLeft(@Named("leftIndex") int leftIndex, @Named("outIndex") int outIndex);
```

### JavadocDeclaration
`@param newState` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
  /**
   * Set the BatchState (this is useful when performing row key join)
   * @param newState
   */
  public void setBatchState(BatchState newState);
```

### JavadocDeclaration
`@param newState` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoin.java`
#### Snippet
```java
  /**
   * Set the RowKeyJoinState (this is useful for maintaining state for row key join algorithm)
   * @param newState
   */
  public void setRowKeyJoinState(RowKeyJoinState newState);
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method buildSchema
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/NestedLoopJoinBatch.java`
#### Snippet
```java
   * Builds the output container's schema. Goes over the left and the right
   * batch and adds the corresponding vectors to the output container.
   * @throws SchemaChangeException if batch schema was changed during execution
   */
  @Override
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaTracker.java`
#### Snippet
```java
 * column, and finds that the default type conflicts with the real type.
 * <p>
 * @see {@link ImplicitColumnExplorer}, the class from which this class
 * evolved
 */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
  /**
   * Helps to create the outgoing container vectors based on known left and right batch schemas
   * @throws SchemaChangeException
   */
  private void setupNewSchema() {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method setupNewSchema
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
  /**
   * Helps to create the outgoing container vectors based on known left and right batch schemas
   * @throws SchemaChangeException
   */
  private void setupNewSchema() {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method buildSchema
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
   * which UNNEST should abide by.
   *
   * @throws SchemaChangeException if batch schema was changed during execution
   */
  @Override
```

### JavadocDeclaration
Wrong tag `See`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/file/FileScanFramework.java`
#### Snippet
```java
 * reader creation, we have a file system, context and so on.
 * <p>
 * @See {AbstractScanFramework} for details.
 */
public class FileScanFramework extends ManagedScanFramework {
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ReaderLevelProjection.java`
#### Snippet
```java
 * vector container so that code can access columns by index as well as name.
 *
 * @see {@link ImplicitColumnExplorer}, the class from which this class
 * evolved
 */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ScanLevelProjection.java`
#### Snippet
```java
 * </ul>
 * <p>
 * @see {@link org.apache.drill.exec.store.ColumnExplorer}, the class from which this class
 * evolved
 */
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortRecordBatchBuilder.java`
#### Snippet
```java
   * Add another record batch to the set of record batches. TODO: Refactor this and other {@link #add
   * (RecordBatchData)} method into one method.
   * @param batch
   * @return True if the requested add completed successfully.  Returns false in the case that this builder is full and cannot receive additional packages.
   * @throws SchemaChangeException
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortRecordBatchBuilder.java`
#### Snippet
```java
   * @param batch
   * @return True if the requested add completed successfully.  Returns false in the case that this builder is full and cannot receive additional packages.
   * @throws SchemaChangeException
   */
  public boolean add(VectorAccessible batch) {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method add
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortRecordBatchBuilder.java`
#### Snippet
```java
   * @param batch
   * @return True if the requested add completed successfully.  Returns false in the case that this builder is full and cannot receive additional packages.
   * @throws SchemaChangeException
   */
  public boolean add(VectorAccessible batch) {
```

### JavadocDeclaration
`@param hasRemainder` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/limit/PartitionLimitRecordBatch.java`
#### Snippet
```java
   * record boundary.
   *
   * @param hasRemainder
   * @return - IterOutcome to send downstream
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
     *
     * @param channel created by the file manager
     * @return
     */
    long getWriteBytes(WritableByteChannel channel);
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
     *
     * @param inputStream input stream created by the file manager
     * @return
     */
    long getReadBytes(InputStream inputStream);
```

### JavadocDeclaration
`@param schema` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/BatchGroup.java`
#### Snippet
```java
   * Updates the schema for this batch group. The current as well as any
   * deserialized batches will be coerced to this schema.
   * @param schema
   */
  public void setSchema(BatchSchema schema) {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/MergeSortWrapper.java`
#### Snippet
```java
   * @param batchGroups the complete set of in-memory batches
   * @param outputBatchSize output batch size for in-memory merge
   * @return the sv4 for this operator
   */

```

### JavadocDeclaration
`@param allocHelper` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
     * @param outputContainer merges output batch into the given output container
     * @param targetRecordCount number of records for each output batch
     * @param allocHelper
     */
    private BatchMerger(PriorityQueueCopierWrapper holder, BatchSchema schema, List<? extends BatchGroup> batchGroupList,
```

### JavadocDeclaration
`@param schema` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   * the final merge operation.
   *
   * @param schema
   * @param batchGroupList
   * @param outputContainer
```

### JavadocDeclaration
`@param batchGroupList` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   *
   * @param schema
   * @param batchGroupList
   * @param outputContainer
   * @param targetRecordCount
```

### JavadocDeclaration
`@param outputContainer` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   * @param schema
   * @param batchGroupList
   * @param outputContainer
   * @param targetRecordCount
   * @param allocHelper
```

### JavadocDeclaration
`@param targetRecordCount` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   * @param batchGroupList
   * @param outputContainer
   * @param targetRecordCount
   * @param allocHelper
   * @return
```

### JavadocDeclaration
`@param allocHelper` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   * @param outputContainer
   * @param targetRecordCount
   * @param allocHelper
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
   * @param targetRecordCount
   * @param allocHelper
   * @return
   */
  public BatchMerger startMerge(BatchSchema schema, List<? extends BatchGroup> batchGroupList,
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/BufferedBatches.java`
#### Snippet
```java
  /**
   * Convert an incoming batch into the agree-upon format.
   * @param incoming
   *
   * @return the converted batch, or null if the incoming batch is empty
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param outContainer The destination container for the key columns.
   * @param numRecords The number of key recorts to transfer.
   * @return
   */
  boolean outputKeys(int batchIdx, VectorContainer outContainer, int numRecords);
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
  /**
   * The amount of direct memory consumed by the hash table.
   * @return
   */
  long getActualSize();
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @return Returns -1 if the data in the probe batch at the given incomingRowIdx is not in the hash table. Otherwise returns
   * the composite index of the key in the hash table (index of BatchHolder and record in Batch Holder).
   * @throws SchemaChangeException
   */
  int probeForKey(int incomingRowIdx, int hashCode) throws SchemaChangeException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * Updates the incoming (build and probe side) value vectors references in the {@link HashTableTemplate.BatchHolder}s.
   * This is useful on OK_NEW_SCHEMA (need to verify).
   * @throws SchemaChangeException
   */
  void updateBatches() throws SchemaChangeException;
```

### JavadocDeclaration
Javadoc pointing to itself
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java

  /**
   * {@link HashTable#setup} must be called before anything can be done to the {@link HashTable}.
   *
   * @param htConfig
```

### JavadocDeclaration
`@param htConfig` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * {@link HashTable#setup} must be called before anything can be done to the {@link HashTable}.
   *
   * @param htConfig
   * @param allocator
   * @param incomingBuild
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   *
   * @param htConfig
   * @param allocator
   * @param incomingBuild
   * @param incomingProbe
```

### JavadocDeclaration
`@param incomingBuild` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param htConfig
   * @param allocator
   * @param incomingBuild
   * @param incomingProbe
   * @param outgoing
```

### JavadocDeclaration
`@param incomingProbe` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param allocator
   * @param incomingBuild
   * @param incomingProbe
   * @param outgoing
   * @param htContainerOrig
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortImpl.java`
#### Snippet
```java
   * Process the converted incoming batch by adding it to the in-memory store
   * of data, or spilling data to disk when necessary.
   * @param incoming
   */

```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortImpl.java`
#### Snippet
```java
   * Scan the vectors in the incoming batch to determine batch size.
   *
   * @return an analysis of the incoming batch
   */

```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortImpl.java`
#### Snippet
```java
     * downstream. This is a fixed value for all returned
     * results.
     * @return
     */
    VectorContainer getContainer();
```

### JavadocDeclaration
`@param outgoing` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param incomingBuild
   * @param incomingProbe
   * @param outgoing
   * @param htContainerOrig
   * @param context
```

### JavadocDeclaration
`@param htContainerOrig` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param incomingProbe
   * @param outgoing
   * @param htContainerOrig
   * @param context
   * @param cg
```

### JavadocDeclaration
`@param context` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param outgoing
   * @param htContainerOrig
   * @param context
   * @param cg
   */
```

### JavadocDeclaration
`@param cg` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param htContainerOrig
   * @param context
   * @param cg
   */
  void setup(HashTableConfig htConfig, BufferAllocator allocator, VectorContainer incomingBuild, RecordBatch incomingProbe, RecordBatch outgoing,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param incomingRowIdx The index of the probe side record of interest.
   * @return The hash code for the record at the given index in the probe side batch.
   * @throws SchemaChangeException
   */
  int getProbeHashCode(int incomingRowIdx) throws SchemaChangeException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTable.java`
#### Snippet
```java
   * @param incomingRowIdx The index of the build side record of interest.
   * @return The hash code for the record at the given index in the build side batch.
   * @throws SchemaChangeException
   */
  int getBuildHashCode(int incomingRowIdx) throws SchemaChangeException;
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/SpilledState.java`
#### Snippet
```java
   * Add the partition metadata to the end of the queue to be processed.
   * @param spilledPartition The partition metadata to process.
   * @return
   */
  public boolean addPartition(T spilledPartition) {
```

### JavadocDeclaration
`@param recordCount` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/Unnest.java`
#### Snippet
```java
  /**
   * Performs the actual unnest operation.
   * @param recordCount
   * @return number of values in output
   */
```

### JavadocDeclaration
`@param outputCount` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/Unnest.java`
#### Snippet
```java
  /**
   * Set the maximum number of values allowed in the output.
   * @param outputCount
   */
  void setOutputCount(int outputCount);
```

### JavadocDeclaration
`@param repeatedColumn` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/Unnest.java`
#### Snippet
```java
  /**
   * Set the field to be unnested
   * @param repeatedColumn
   */
  void setUnnestField(RepeatedValueVector repeatedColumn);
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFramer.java`
#### Snippet
```java
  /**
   * process the inner batch and write the aggregated values in the container
   * @throws SchemaChangeException
   * @throws DrillException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFramer.java`
#### Snippet
```java
   * process the inner batch and write the aggregated values in the container
   * @throws SchemaChangeException
   * @throws DrillException
   */
  void doWork() throws SchemaChangeException;
```

### JavadocDeclaration
`@param incomingRowIdx` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   *   Return the Hash Value for the row in the Probe incoming batch at index:
   *
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   *
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
   */
  @Override
```

### JavadocDeclaration
`@param incomingRowIdx` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   *
   *
   * @param incomingRowIdx
   * @param hashCode - The hash code for the Probe-side key
   * @return -1 if key is not found, else return the global index of the key
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   * @param hashCode - The hash code for the Probe-side key
   * @return -1 if key is not found, else return the global index of the key
   * @throws SchemaChangeException
   */
   @Override
```

### JavadocDeclaration
`@param incomingRowIdx` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   *   (For Hash Aggregate there's no "Build" side -- only one batch - this one)
   *
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   *
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
   * @param incomingRowIdx
   * @return
   * @throws SchemaChangeException
   */
  @Override
```

### JavadocDeclaration
DrillException is not declared to be thrown by method doWork
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFramer.java`
#### Snippet
```java
   * process the inner batch and write the aggregated values in the container
   * @throws SchemaChangeException
   * @throws DrillException
   */
  void doWork() throws SchemaChangeException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/FrameSupportTemplate.java`
#### Snippet
```java
   * @param outgoing batch we will be writing to
   *
   * @throws SchemaChangeException
   */
  public abstract void setupPartition(@Named("incoming") WindowDataBatch incoming,
```

### JavadocDeclaration
DrillException is not declared to be thrown by method processPartition
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/FrameSupportTemplate.java`
#### Snippet
```java
   * @param currentRow first unprocessed row
   * @return index of next unprocessed row
   * @throws DrillException if it can't write into the container
   */
  private int processPartition(final int currentRow) throws SchemaChangeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/NoFrameSupportTemplate.java`
#### Snippet
```java
   *          first unprocessed row
   * @return index of next unprocessed row
   * @throws SchemaChangeException
   */
  private int processPartition(int currentRow) throws SchemaChangeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/NoFrameSupportTemplate.java`
#### Snippet
```java
   * @param start starting row of the current frame
   * @return num peer rows for current row
   * @throws SchemaChangeException
   */
  private long countPeers(int start) throws SchemaChangeException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/NoFrameSupportTemplate.java`
#### Snippet
```java
   * @param outgoing batch we will be writing to
   *
   * @throws SchemaChangeException
   */
  public abstract void setupPartition(@Named("incoming") WindowDataBatch incoming,
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/IteratorValidatorInjector.java`
#### Snippet
```java
   * @return same physical operator as passed in, but its child will be a IteratorValidator operator whose child will be the
   *         original child of this operator
   * @throws ExecutionSetupException
   */
  @Override
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggBatch.java`
#### Snippet
```java
   * failure state, as well is informing the upstream operators.
   *
   * @return true if the aggregator was setup successfully. false if there was a
   *         failure.
   */
```

### JavadocDeclaration
`@param index` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/Partitioner.java`
#### Snippet
```java
  /**
   * Get PartitionOutgoingBatch based on the fact that there can be > 1 Partitioner
   * @param index
   * @return PartitionOutgoingBatch that matches index within Partitioner. This method can
   * return null if index does not fall within boundary of this Partitioner
```

### JavadocDeclaration
`@param prefix` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
   *  Generate a detailed error message in case of "Out Of Memory"
   * @return err msg
   * @param prefix
   */
  private String getOOMErrorMsg(String prefix) {
```

### JavadocDeclaration
`@param records` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
   *   Allocate space for the returned aggregate columns
   *   (Note DRILL-5588: Maybe can eliminate this allocation (and copy))
   * @param records
   */
  private void allocateOutgoing(int records) {
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
   *  Update the estimated max batch size to be used in the Hash Aggr Op.
   *  using the record batch size to get the row width.
   * @param incoming
   */
  private void updateEstMaxBatchSize(RecordBatch incoming) {
```

### JavadocDeclaration
`@param htables` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java
   * Note: For spilled partitions, their hash-table stats from before the spill are lost.
   * And the SPILLED_PARTITIONS only counts the spilled partitions in the primary, not SECONDARY etc.
   * @param htables
   */
  private void updateStats(HashTable[] htables) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/rangepartitioner/RangePartitionRecordBatch.java`
#### Snippet
```java
   * the partition column based on which partition a record falls into
   *
   * @throws SchemaChangeException
   * @return True if the new schema differs from old schema, False otherwise
   */
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method setupNewSchema
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/rangepartitioner/RangePartitionRecordBatch.java`
#### Snippet
```java
   * the partition column based on which partition a record falls into
   *
   * @throws SchemaChangeException
   * @return True if the new schema differs from old schema, False otherwise
   */
```

### JavadocDeclaration
`@param isLastBatch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
  /**
   * flushOutgoingBatches - decorator to call real Partitioner(s) flushOutgoingBatches
   * @param isLastBatch
   * @param schemaChanged
   * @throws ExecutionException
```

### JavadocDeclaration
`@param schemaChanged` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
   * flushOutgoingBatches - decorator to call real Partitioner(s) flushOutgoingBatches
   * @param isLastBatch
   * @param schemaChanged
   * @throws ExecutionException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
   * @param isLastBatch
   * @param schemaChanged
   * @throws ExecutionException
   */
  public void flushOutgoingBatches(final boolean isLastBatch, final boolean schemaChanged) throws ExecutionException {
```

### JavadocDeclaration
`@param iface` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
  /**
   * Helper to execute the different methods wrapped into same logic
   * @param iface
   * @throws ExecutionException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
   * Helper to execute the different methods wrapped into same logic
   * @param iface
   * @throws ExecutionException
   */
  @VisibleForTesting
```

### JavadocDeclaration
`@param incoming` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
   * partitionBatch - decorator method to call real Partitioner(s) to process incoming batch
   * uses either threading or not threading approach based on number Partitioners
   * @param incoming
   * @throws ExecutionException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
   * uses either threading or not threading approach based on number Partitioners
   * @param incoming
   * @throws ExecutionException
   */
  public void partitionBatch(final RecordBatch incoming) throws ExecutionException {
```

### JavadocDeclaration
`@param svIndex` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java
  /**
   * Helper method to copy data based on partition
   * @param svIndex
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java
   * Helper method to copy data based on partition
   * @param svIndex
   * @throws IOException
   */
  private void doCopy(int svIndex) throws IOException {
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/HashJoinPOP.java`
#### Snippet
```java
   * The Hash Aggregate operator supports spilling
   * @return true (unless a single partition is forced)
   * @param queryContext
   */
  @Override
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/HashAggregate.java`
#### Snippet
```java
   * The Hash Aggregate operator supports spilling
   * @return true (unless a single partition is forced)
   * @param queryContext
   */
  @Override
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/ExternalSort.java`
#### Snippet
```java
   * The External Sort operator supports spilling
   * @return true
   * @param queryContext
   */
  @Override
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
   * @param timeout
   *          Timeout in milliseconds.
   * @return True if the given timeout is expired. False when interrupted and
   *         the fragment status is not runnable.
   */
```

### JavadocDeclaration
`@param batch` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
   * in the partition table
   *
   * @param batch
   */
  protected void setupNewSchema(VectorAccessible batch) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
   *
   * @return instance of a new merger based on generated code
   * @throws SchemaChangeException
   */
  private MergingReceiverGeneratorBase createMerger() {
```

### JavadocDeclaration
SchemaChangeException is not declared to be thrown by method createMerger
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
   *
   * @return instance of a new merger based on generated code
   * @throws SchemaChangeException
   */
  private MergingReceiverGeneratorBase createMerger() {
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetCopier.java`
#### Snippet
```java
   * will finish the copy. Do that before start a new input
   * batch.
   * @return
   */
  boolean isCopyPending();
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/ResultSetLoader.java`
#### Snippet
```java
 * during testing, but will be disabled during production.
 *
 * @see {@link VectorContainerWriter}, the class which this class
 * replaces
 */
```

### JavadocDeclaration
Class/method reference, quoted text, or HTML link are expected after @see tag
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ResultSetLoaderImpl.java`
#### Snippet
```java
 * Implementation of the result set loader. Caches vectors for a row or map.
 *
 * @see {@link ResultSetLoader}
 */
public class ResultSetLoaderImpl implements ResultSetLoader, LoaderInternals {
```

### JavadocDeclaration
Tag `return` is not allowed here
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/VectorState.java`
#### Snippet
```java
   * @param cardinality the number of elements in the new vector. If this
   * vector is an array, then this is the number of arrays
   * @return the new next write position for the vector index associated
   * with the writer for this vector
   */
```

### JavadocDeclaration
`@param assignmentResult` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/ResourcePool.java`
#### Snippet
```java
   * the child pools selectors as well. During traversal it builds the QueueAssignment result which consists of all
   * the selected leaf pools and all rejected intermediate pools.
   * @param assignmentResult
   * @param queryContext
   */
```

### JavadocDeclaration
`@param queryContext` tag description is missing
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/ResourcePool.java`
#### Snippet
```java
   * the selected leaf pools and all rejected intermediate pools.
   * @param assignmentResult
   * @param queryContext
   */
  void visitAndSelectPool(QueueAssignmentResult assignmentResult, QueryContext queryContext);
```

### JavadocDeclaration
Wrong tag `returns`
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
  /**
   * Decide whether it's legal to do implicit cast.
   * @returns
   *      0: param types equal arg types, no casting needed.
   * (0,+∞): the cost of implicit casting.
```

### JavadocDeclaration
`@param tagName` tag description is missing
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageMetadataUtils.java`
#### Snippet
```java
  /**
   * Format the tag name (remove the spaces and special characters)
   * @param tagName
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageMetadataUtils.java`
#### Snippet
```java
   * Format the tag name (remove the spaces and special characters)
   * @param tagName
   * @return
   */
  public static String formatName(final String tagName) {
```

### JavadocDeclaration
`@param name` tag description is missing
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageMetadataUtils.java`
#### Snippet
```java
  /**
   * Skip the tag if parameter (fileSystemMetadata) is false
   * @param name
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageMetadataUtils.java`
#### Snippet
```java
   * Skip the tag if parameter (fileSystemMetadata) is false
   * @param name
   * @return
   */
  public static boolean isSkipTag(String name) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     * @param columnIndex the column index for this chunk
     * @param offsetIndex the offset index for this chunk
     * @throws IOException
     */
    public void appendColumnChunk(ColumnDescriptor descriptor, SeekableInputStream from, ColumnChunkMetaData chunk,
```

### JavadocDeclaration
`@param fs` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
  /**
   * Get an instance of DrillTable for a particular native secondary index
   * @param fs
   * @param selection
   * @param fsPlugin
```

### JavadocDeclaration
`@param selection` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * Get an instance of DrillTable for a particular native secondary index
   * @param fs
   * @param selection
   * @param fsPlugin
   * @param storageEngineName
```

### JavadocDeclaration
`@param fsPlugin` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param fs
   * @param selection
   * @param fsPlugin
   * @param storageEngineName
   * @param userName
```

### JavadocDeclaration
`@param storageEngineName` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param selection
   * @param fsPlugin
   * @param storageEngineName
   * @param userName
   * @param secondaryIndexDesc
```

### JavadocDeclaration
`@param userName` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param fsPlugin
   * @param storageEngineName
   * @param userName
   * @param secondaryIndexDesc
   * @return
```

### JavadocDeclaration
`@param secondaryIndexDesc` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param storageEngineName
   * @param userName
   * @param secondaryIndexDesc
   * @return
   * @throws IOException
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param userName
   * @param secondaryIndexDesc
   * @return
   * @throws IOException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatMatcher.java`
#### Snippet
```java
   * @param secondaryIndexDesc
   * @return
   * @throws IOException
   */
  public DrillTable isReadableIndex(DrillFileSystem fs,
```

### JavadocDeclaration
`@param incomingEndpoints` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
  /**
   *
   * @param incomingEndpoints
   */
  @Override
```

### JavadocDeclaration
`@param path` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java
   * getNewPath: for an original path, return new rename '$N' path, notice there could be multiple renamed paths
   * if the there are multiple functional indexes refer original path.
   * @param path
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java
   * if the there are multiple functional indexes refer original path.
   * @param path
   * @return
   */
  public SchemaPath getNewPath(SchemaPath path) {
```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/PhoenixReader.java`
#### Snippet
```java
   * Fetch and process one row.
   * @return return true if one row is processed, return false if there is no next row.
   * @throws SQLException
   */
  public boolean processRow() throws SQLException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RequestHandler.java`
#### Snippet
```java
   * @param dBody      data, maybe null
   * @param sender     used to {@link ResponseSender#send send} the response
   * @throws RpcException
   */
  void handle(S connection, int rpcType, ByteBuf pBody, ByteBuf dBody, ResponseSender sender)
```

### JavadocDeclaration
`@param ex` tag description is missing
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcOutcomeListener.java`
#### Snippet
```java
  /**
   * Called when an error occurred while waiting for the RPC outcome.
   * @param ex
   */
  void failed(RpcException ex);
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/security/AuthenticatorFactory.java`
#### Snippet
```java
   * @param properties config properties
   * @return sasl client
   * @throws SaslException
   */
  SaslClient createSaslClient(UserGroupInformation ugi, Map<String, ?> properties) throws SaslException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/security/AuthenticatorFactory.java`
#### Snippet
```java
   * @param properties config properties
   * @return ugi
   * @throws IOException
   */
  UserGroupInformation createAndLoginUser(Map<String, ?> properties) throws IOException;
```

### JavadocDeclaration
`@throws` tag description is missing
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/security/AuthenticatorFactory.java`
#### Snippet
```java
   * @param properties config properties
   * @return sasl server
   * @throws SaslException
   */
  SaslServer createSaslServer(UserGroupInformation ugi, Map<String, ?> properties) throws SaslException;
```

### JavadocDeclaration
`@return` tag description is missing
in `exec/memory/base/src/main/java/org/apache/drill/exec/ops/BufferManager.java`
#### Snippet
```java
   * @param newSize
   *          Size of new replacement buffer.
   * @return
   */
  public DrillBuf replace(DrillBuf old, int newSize);
```

### JavadocDeclaration
`@param allocator` tag description is missing
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/AllocationManager.java`
#### Snippet
```java
     * use.
     *
     * @param allocator
     * @return The ledger associated with a particular BufferAllocator.
     */
```

### JavadocDeclaration
`@param incomingEndpoints` tag description is missing
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduGroupScan.java`
#### Snippet
```java
  /**
   *
   * @param incomingEndpoints
   */
  @Override
```

### JavadocDeclaration
`@return` tag description is missing
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaGroupScan.java`
#### Snippet
```java
   * It can be replaced with Kafka implementation once it will be introduced.
   * @param consumer Kafka consumer whom need to get assignments
   * @return
   * @throws InterruptedException
   */
```

### JavadocDeclaration
`@throws` tag description is missing
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaGroupScan.java`
#### Snippet
```java
   * @param consumer Kafka consumer whom need to get assignments
   * @return
   * @throws InterruptedException
   */
  private Set<TopicPartition> waitForConsumerAssignment(Consumer consumer) throws InterruptedException {
```

### JavadocDeclaration
`@throws` tag description is missing
in `common/src/main/java/org/apache/drill/common/DeferredException.java`
#### Snippet
```java
   * DeferredException, we don't double report the same exception.
   *
   * @throws Exception
   */
  public synchronized void throwAndClear() throws Exception {
```

### JavadocDeclaration
`@param str` tag description is missing
in `common/src/main/java/org/apache/drill/common/util/DrillStringUtils.java`
#### Snippet
```java
   * Copied form commons-lang 2.x code as common-lang 3.x has this API removed.
   * (http://commons.apache.org/proper/commons-lang/article3_0.html#StringEscapeUtils.escapeSql)
   * @param str
   * @return
   */
```

### JavadocDeclaration
`@return` tag description is missing
in `common/src/main/java/org/apache/drill/common/util/DrillStringUtils.java`
#### Snippet
```java
   * (http://commons.apache.org/proper/commons-lang/article3_0.html#StringEscapeUtils.escapeSql)
   * @param str
   * @return
   */
  public static String escapeSql(String str) {
```

### JavadocDeclaration
`@param other` tag description is missing
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
  /**
   * merges this and other together into a new result object
   * @param other
   * @return the resulting merge
   */
```

### JavadocDeclaration
Javadoc pointing to itself
in `common/src/main/java/org/apache/drill/common/concurrent/AbstractCheckedFuture.java`
#### Snippet
```java
   * <p>If {@code e} is an {@code InterruptedException}, the calling
   * {@code checkedGet} method has already restored the interrupt after catching
   * the exception. If an implementation of {@link #mapException(Exception)}
   * wishes to swallow the interrupt, it can do so by calling
   * {@link Thread#interrupted()}.
```

### JavadocDeclaration
`@throws` tag description is missing
in `common/src/main/java/org/apache/drill/common/scanner/BuildTimeScan.java`
#### Snippet
```java
   * to generate the prescan file during build
   * @param args the root path for the classes where {@link BuildTimeScan#REGISTRY_FILE} is generated
   * @throws Exception
   */
  public static void main(String[] args) throws Exception {
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `DECODER_FACTORY` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/util/Text.java`
#### Snippet
```java
      };

  private static ThreadLocal<CharsetDecoder> DECODER_FACTORY =
      new ThreadLocal<CharsetDecoder>() {
        @Override
```

### FieldMayBeFinal
Field `ENCODER_FACTORY` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/util/Text.java`
#### Snippet
```java
public class Text {

  private static ThreadLocal<CharsetEncoder> ENCODER_FACTORY =
      new ThreadLocal<CharsetEncoder>() {
        @Override
```

### FieldMayBeFinal
Field `delegate` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/SingleLikeRepeatedMapReaderImpl.java`
#### Snippet
```java
public class SingleLikeRepeatedMapReaderImpl extends AbstractFieldReader{

  private RepeatedMapReaderImpl delegate;

  public SingleLikeRepeatedMapReaderImpl(RepeatedMapVector vector, FieldReader delegate) {
```

### FieldMayBeFinal
Field `vector` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/UnionListReader.java`
#### Snippet
```java
public class UnionListReader extends AbstractFieldReader {

  private ListVector vector;
  private ValueVector data;
  private UInt4Vector offsets;
```

### FieldMayBeFinal
Field `offsets` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/UnionListReader.java`
#### Snippet
```java
  private ListVector vector;
  private ValueVector data;
  private UInt4Vector offsets;

  public UnionListReader(ListVector vector) {
```

### FieldMayBeFinal
Field `data` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/UnionListReader.java`
#### Snippet
```java

  private ListVector vector;
  private ValueVector data;
  private UInt4Vector offsets;

```

### FieldMayBeFinal
Field `stack` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/impl/HierarchicalPrinter.java`
#### Snippet
```java

  private final PrintStream out;
  private Deque<ObjState> stack = new ArrayDeque<ObjState>();
  private int pendingExtensions = 0;
  private ObjState curObject;
```

### FieldMayBeFinal
Field `varWidthVectorAccessor` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
  public static class VarWidthOffsetHyperVectorAccessor extends BaseHyperVectorAccessor {

    private VectorAccessor varWidthVectorAccessor;

    public VarWidthOffsetHyperVectorAccessor(VectorAccessor va) {
```

### FieldMayBeFinal
Field `repeatedVectorAccessor` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
  public static class ArrayDataHyperVectorAccessor implements VectorAccessor {

    private VectorAccessor repeatedVectorAccessor;

    private ArrayDataHyperVectorAccessor(VectorAccessor va) {
```

### FieldMayBeFinal
Field `unionVectorAccessor` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
  public static class UnionTypeHyperVectorAccessor extends BaseHyperVectorAccessor {

    private VectorAccessor unionVectorAccessor;

    public UnionTypeHyperVectorAccessor(VectorAccessor va) {
```

### FieldMayBeFinal
Field `nullableAccessor` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
  public static class NullableValuesHyperVectorAccessor implements VectorAccessor {

    private VectorAccessor nullableAccessor;

    private NullableValuesHyperVectorAccessor(VectorAccessor va) {
```

### FieldMayBeFinal
Field `repeatedVectorAccessor` may be 'final'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/VectorAccessors.java`
#### Snippet
```java
  public static class ArrayOffsetHyperVectorAccessor extends BaseHyperVectorAccessor {

    private VectorAccessor repeatedVectorAccessor;

    public ArrayOffsetHyperVectorAccessor(VectorAccessor va) {
```

### FieldMayBeFinal
Field `sizeCalculatorEnabled` may be 'final'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePluginConfig.java`
#### Snippet
```java

  @JsonIgnore
  private Boolean sizeCalculatorEnabled;

  public static final String NAME = "hbase";
```

### FieldMayBeFinal
Field `hbaseConf` may be 'final'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/config/HBasePersistentStoreProvider.java`
#### Snippet
```java
  private final TableName hbaseTableName;

  private Configuration hbaseConf;

  private Connection connection;
```

### FieldMayBeFinal
Field `result` may be 'final'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/config/HBasePersistentStore.java`
#### Snippet
```java
  private class DeferredEntry implements Entry<String, V>{

    private Result result;

    public DeferredEntry(Result result) {
```

### FieldMayBeFinal
Field `scanner` may be 'final'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/config/HBasePersistentStore.java`
#### Snippet
```java

  private class Iter implements Iterator<Entry<String, V>> {
    private ResultScanner scanner;
    private Result current = null;
    private Result last = null;
```

### FieldMayBeFinal
Field `cursor` may be 'final'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/AvaticaDrillSqlAccessor.java`
#### Snippet
```java

  private SqlAccessor underlyingAccessor;
  private DrillCursor cursor;

  AvaticaDrillSqlAccessor(SqlAccessor drillSqlAccessor, DrillCursor cursor) {
```

### FieldMayBeFinal
Field `underlyingAccessor` may be 'final'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/AvaticaDrillSqlAccessor.java`
#### Snippet
```java
  private final static boolean BOOLEAN_NULL_VALUE = false;

  private SqlAccessor underlyingAccessor;
  private DrillCursor cursor;

```

### FieldMayBeFinal
Field `proxiedsToProxiesMap` may be 'final'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/ProxiesManager.java`
#### Snippet
```java
  /** Map of proxied original objects from proxied JDBC driver to
   *  proxy objects to be returned by tracing proxy driver. */
  private Map<Object, Object> proxiedsToProxiesMap = new IdentityHashMap<>();


```

### FieldMayBeFinal
Field `interfacesToProxyClassesMap` may be 'final'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/ProxiesManager.java`
#### Snippet
```java
  private final InvocationReporter reporter;

  private Map<Class<?>, Class<Proxy>> interfacesToProxyClassesMap =
      new IdentityHashMap<>();

```

### FieldMayBeFinal
Field `objectsToIdsMap` may be 'final'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java

  private int lastObjNum = 0;
  private Map<Object, String> objectsToIdsMap = new IdentityHashMap<>();


```

### FieldMayBeFinal
Field `pluginConfig` may be 'final'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenRepository.java`
#### Snippet
```java
  private final OAuthTokenCredentials credentials;
  private final CredentialsProvider credentialsProvider;
  private HttpStoragePluginConfig pluginConfig;
  private PersistentTokenTable tokenTable;
  private String accessToken;
```

### FieldMayBeFinal
Field `tokenTable` may be 'final'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenRepository.java`
#### Snippet
```java
  private final CredentialsProvider credentialsProvider;
  private HttpStoragePluginConfig pluginConfig;
  private PersistentTokenTable tokenTable;
  private String accessToken;
  private String refreshToken;
```

### FieldMayBeFinal
Field `lastIndexValue` may be 'final'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/IndexPaginator.java`
#### Snippet
```java

  private String indexValue;
  private String lastIndexValue;
  private String lastNextPageValue;
  private Boolean hasMoreValue;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/NameValuePair.java`
#### Snippet
```java

public class NameValuePair {
  private String name;
  private Object value;

```

### FieldMayBeFinal
Field `value` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/NameValuePair.java`
#### Snippet
```java
public class NameValuePair {
  private String name;
  private Object value;

  public NameValuePair(String name, Object value) {
```

### FieldMayBeFinal
Field `stateListener` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java

  private List<DrillbitEndpoint> initialEndpoints;
  private ConnectionStateListener stateListener = new ConnectionStateListener() {

    @Override
```

### FieldMayBeFinal
Field `zkCoord` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
  private static final String AM_REGISTRY = "/drill-on-yarn";

  private ZKClusterCoordinator zkCoord;
  @SuppressWarnings("unused")
  private String amHost;
```

### FieldMayBeFinal
Field `curator` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinator.java`
#### Snippet
```java
      .getLog(ZKClusterCoordinator.class);

  private CuratorFramework curator;
  private ServiceDiscovery<DrillbitEndpoint> discovery;
  private volatile Collection<DrillbitEndpoint> endpoints = Collections
```

### FieldMayBeFinal
Field `discovery` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinator.java`
#### Snippet
```java

  private CuratorFramework curator;
  private ServiceDiscovery<DrillbitEndpoint> discovery;
  private volatile Collection<DrillbitEndpoint> endpoints = Collections
      .emptyList();
```

### FieldMayBeFinal
Field `registry` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   */

  private Map<String, DrillbitTracker> registry = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `zkDriver` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
   */

  private ZKClusterCoordinatorDriver zkDriver;

  /**
```

### FieldMayBeFinal
Field `localize` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
  private Configuration yarnConf;
  private Config config;
  private boolean localize;

  public DfsFacade(Config config) {
```

### FieldMayBeFinal
Field `label` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
    protected Path dfsArchivePath;
    FileStatus fileStatus;
    private String label;

    /**
```

### FieldMayBeFinal
Field `config` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
  private FileSystem fs;
  private Configuration yarnConf;
  private Config config;
  private boolean localize;

```

### FieldMayBeFinal
Field `shutdownWaitSec` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StopCommand.java`
#### Snippet
```java
    private YarnApplicationState state;
    private int pollWaitSec;
    private int shutdownWaitSec;

    StopMonitor(Config config, StatusCommand.Reporter reporter) {
```

### FieldMayBeFinal
Field `client` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StatusCommand.java`
#### Snippet
```java
public class StatusCommand extends ClientCommand {
  public static class Reporter {
    private YarnRMClient client;
    ApplicationReport report;

```

### FieldMayBeFinal
Field `launch` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
  private Config config;
  private boolean upload;
  private boolean launch;
  private boolean dryRun;

```

### FieldMayBeFinal
Field `upload` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StartCommand.java`
#### Snippet
```java
public class StartCommand extends ClientCommand {
  private Config config;
  private boolean upload;
  private boolean launch;
  private boolean dryRun;
```

### FieldMayBeFinal
Field `cmd` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java
    CLEAN( "clean", "Remove archives stored in DFS." );

    private String cmd;
    private String descrip;

```

### FieldMayBeFinal
Field `descrip` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java

    private String cmd;
    private String descrip;

    private Command(String cmd, String descrip) {
```

### FieldMayBeFinal
Field `verbose` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java
public class AMRunner {
  private Config config;
  private boolean verbose;
  private ApplicationId appId;
  public Map<String, LocalResource> resources;
```

### FieldMayBeFinal
Field `dryRun` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java
  private YarnRMClient client;
  private GetNewApplicationResponse appResponse;
  private boolean dryRun;

  public AMRunner(Config config, boolean verbose, boolean dryRun) {
```

### FieldMayBeFinal
Field `startupWaitSec` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java
    private YarnApplicationState state;
    private int pollWaitSec;
    private int startupWaitSec;

    public StartMonitor() {
```

### FieldMayBeFinal
Field `config` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/AMRunner.java`
#### Snippet
```java

public class AMRunner {
  private Config config;
  private boolean verbose;
  private ApplicationId appId;
```

### FieldMayBeFinal
Field `force` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/FileUploader.java`
#### Snippet
```java

  public static class UploadFiles extends FileUploader {
    private boolean force;

    public UploadFiles(boolean force, boolean dryRun, boolean verbose) {
```

### FieldMayBeFinal
Field `envVars` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
  };

  private static String envVars[] = {
      APP_ID_ENV_VAR,
      DRILL_HOME_ENV_VAR,
```

### FieldMayBeFinal
Field `suffixes` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
  }

  private static String suffixes[] = { ".tar.gz", ".tgz", ".zip" };

  public static String findSuffix(String baseName) {
```

### FieldMayBeFinal
Field `displayName` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java
    END_ACK( "Deregistered" );

    private String displayName;

    private TrackingState( String displayName ) {
```

### FieldMayBeFinal
Field `nodesInUse` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
   */

  private Set<String> nodesInUse = new HashSet<>();

  /**
```

### FieldMayBeFinal
Field `yarnNodes` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
   */

  private Map<String, NodeReport> yarnNodes = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `blacklist` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
   */

  private Set<String> blacklist = new HashSet<>();
  private final AMYarnFacade yarn;

```

### FieldMayBeFinal
Field `nodeMap` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
  private boolean failed;

  private Map<String, String> nodeMap = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `pollPeriodMs` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AMYarnFacadeImpl.java`
#### Snippet
```java
  private RegisterApplicationMasterResponse registration;
  private YarnClient client;
  private int pollPeriodMs;

  private String appMasterTrackingUrl;
```

### FieldMayBeFinal
Field `requestTimeoutSecs` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillbitScheduler.java`
#### Snippet
```java

public class DrillbitScheduler extends AbstractDrillbitScheduler {
  private int requestTimeoutSecs;
  private int maxExtraNodes;

```

### FieldMayBeFinal
Field `maxExtraNodes` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillbitScheduler.java`
#### Snippet
```java
public class DrillbitScheduler extends AbstractDrillbitScheduler {
  private int requestTimeoutSecs;
  private int maxExtraNodes;


```

### FieldMayBeFinal
Field `config` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
public class DrillControllerFactory implements ControllerFactory {
  private static final Log LOG = LogFactory.getLog(DrillControllerFactory.class);
  private Config config = DrillOnYarnConfig.config();
  private String drillArchivePath;
  private String siteArchivePath;
```

### FieldMayBeFinal
Field `addOns` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Dispatcher.java`
#### Snippet
```java
   */

  private List<DispatcherAddOn> addOns = new ArrayList<>();
  private String trackingUrl;
  private AMRegistrar amRegistrar;
```

### FieldMayBeFinal
Field `pollables` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Dispatcher.java`
#### Snippet
```java
   */

  private List<Pollable> pollables = new ArrayList<>();

  /**
```

### FieldMayBeFinal
Field `INSTANCE` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AuthDynamicFeature.java`
#### Snippet
```java
                                       // first before all other filters.
  private static class AuthCheckFilter implements ContainerRequestFilter {
    private static AuthCheckFilter INSTANCE = new AuthCheckFilter();

    @Override
```

### FieldMayBeFinal
Field `failureCheckPeriodMs` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private long lastFailureCheckTime;

  private int failureCheckPeriodMs = 60_000;

  private int taskCheckPeriodMs = 10_000;
```

### FieldMayBeFinal
Field `configPollPeriod` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private int configPollPeriod = 60_000;
  private long nextResourcePollTime;

```

### FieldMayBeFinal
Field `stopTimoutMs` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private int maxRequestsPerTick = 2;

  private int stopTimoutMs = 10_000;

  /**
```

### FieldMayBeFinal
Field `maxRequestsPerTick` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private int maxRequestsPerTick = 2;

  private int stopTimoutMs = 10_000;
```

### FieldMayBeFinal
Field `lifecycleListeners` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private List<TaskLifecycleListener> lifecycleListeners = new ArrayList<>();

  /**
```

### FieldMayBeFinal
Field `prioritizedGroups` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private List<SchedulerStateActions> prioritizedGroups = new ArrayList<>();

  /**
```

### FieldMayBeFinal
Field `taskPools` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private Map<String, SchedulerStateActions> taskPools = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `properties` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private Map<String, Object> properties = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `allocatedContainers` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private Set<ContainerId> allocatedContainers = new HashSet<>();

  /**
```

### FieldMayBeFinal
Field `taskCheckPeriodMs` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private int failureCheckPeriodMs = 60_000;

  private int taskCheckPeriodMs = 10_000;
  private long lastTaskCheckTime;

```

### FieldMayBeFinal
Field `completedTasks` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private List<Task> completedTasks = new LinkedList<>();

  /**
```

### FieldMayBeFinal
Field `completionMutex` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private Object completionMutex = new Object();

  /**
```

### FieldMayBeFinal
Field `activeContainers` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private Map<ContainerId, Task> activeContainers = new HashMap<>();

  /**
```

### FieldMayBeFinal
Field `stateHints` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/ControllerModel.java`
#### Snippet
```java
  public int getFreeNodeCount( ) { return freeNodeCount; }

  private static Map<ClusterControllerImpl.State,String> stateHints = makeStateHints( );

  @Override
```

### FieldMayBeFinal
Field `trackingStateHints` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java

    private Map<TaskState,String> stateHints = makeStateHints( );
    private Map<TrackingState,String> trackingStateHints = makeTrackingStateHints( );

    public TaskModel(Task task, boolean live) {
```

### FieldMayBeFinal
Field `stateHints` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
    protected int tryCount;

    private Map<TaskState,String> stateHints = makeStateHints( );
    private Map<TrackingState,String> trackingStateHints = makeTrackingStateHints( );

```

### FieldMayBeFinal
Field `dispatcher` may be 'final'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/WebServer.java`
#### Snippet
```java
  private static final Log LOG = LogFactory.getLog(WebServer.class);
  private final Server jettyServer;
  private Dispatcher dispatcher;

  public WebServer(Dispatcher dispatcher) {
```

### FieldMayBeFinal
Field `columnListIndex` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HivePartition.java`
#### Snippet
```java
public class HivePartition extends Partition {
  // index of partition column list in the table's column list cache
  private int columnListIndex;

  public HivePartition(
```

### FieldMayBeFinal
Field `argTypes` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
public class HiveFuncHolder extends AbstractFuncHolder {

  private MajorType[] argTypes;
  private ObjectInspector returnOI;
  private MajorType returnType;
```

### FieldMayBeFinal
Field `isRandom` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
  private Class<? extends UDF> udfClazz = null;
  private String udfName = "";
  private boolean isRandom;


```

### FieldMayBeFinal
Field `returnOI` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java

  private MajorType[] argTypes;
  private ObjectInspector returnOI;
  private MajorType returnType;
  private Class<? extends GenericUDF> genericUdfClazz;
```

### FieldMayBeFinal
Field `genericUdfClazz` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
  private ObjectInspector returnOI;
  private MajorType returnType;
  private Class<? extends GenericUDF> genericUdfClazz;
  private boolean isGenericUDF = true;
  private Class<? extends UDF> udfClazz = null;
```

### FieldMayBeFinal
Field `returnType` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
  private MajorType[] argTypes;
  private ObjectInspector returnOI;
  private MajorType returnType;
  private Class<? extends GenericUDF> genericUdfClazz;
  private boolean isGenericUDF = true;
```

### FieldMayBeFinal
Field `fieldSchema` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java
  public static class FieldSchemaWrapper {
    @JsonIgnore
    private FieldSchema fieldSchema;
    @JsonProperty
    public String name;
```

### FieldMayBeFinal
Field `serDeInfo` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java
  public static class SerDeInfoWrapper {
    @JsonIgnore
    private SerDeInfo serDeInfo;
    @JsonProperty
    public String name;
```

### FieldMayBeFinal
Field `sd` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java

    @JsonIgnore
    private StorageDescriptor sd;

    // column lists stored in ColumnListsCache
```

### FieldMayBeFinal
Field `partitionsUnwrapped` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveReadEntry.java`
#### Snippet
```java

  @JsonIgnore
  private List<HivePartition> partitionsUnwrapped = Lists.newArrayList();

  @JsonCreator
```

### FieldMayBeFinal
Field `HIVE_SERDE_SCAN_OVERHEAD_FACTOR_PER_COLUMN` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveScan.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(HiveScan.class);

  private static int HIVE_SERDE_SCAN_OVERHEAD_FACTOR_PER_COLUMN = 20;

  private final HiveStoragePlugin hiveStoragePlugin;
```

### FieldMayBeFinal
Field `footerBuffer` may be 'final'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/readers/inspectors/SkipFooterRecordsInspector.java`
#### Snippet
```java

  private final int footerCount;
  private Queue<Object> footerBuffer;
  private final List<Object> valueHolders;
  private long readRecordsCount;
```

### FieldMayBeFinal
Field `objectNode` may be 'final'
in `logical/src/main/java/org/apache/drill/common/util/AbstractDynamicBean.java`
#### Snippet
```java
  private static volatile ObjectMapper MAPPER;

  private ObjectNode objectNode = new ObjectNode(null);

  @JsonAnySetter
```

### FieldMayBeFinal
Field `ops` may be 'final'
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyListBuilder.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(AdjacencyListBuilder.class);

  private Map<V, AdjacencyList<V>.Node> ops = new HashMap<V, AdjacencyList<V>.Node>();
  private final AdjacencyList<V> parent;

```

### FieldMayBeFinal
Field `adjList` may be 'final'
in `logical/src/main/java/org/apache/drill/common/graph/Graph.java`
#### Snippet
```java
  static final Logger logger = LoggerFactory.getLogger(Graph.class);

  private AdjacencyList<G> adjList;
  private final List<R> roots;
  private final List<T> leaves;
```

### FieldMayBeFinal
Field `SCC` may be 'final'
in `logical/src/main/java/org/apache/drill/common/graph/GraphAlgos.java`
#### Snippet
```java
    private int index = 0;
    private List<AdjacencyList<V>.Node> stack = new LinkedList<AdjacencyList<V>.Node>();
    private List<List<AdjacencyList<V>.Node>> SCC = new LinkedList<List<AdjacencyList<V>.Node>>();

    public List<List<AdjacencyList<V>.Node>> executeTarjan(AdjacencyList<V> graph) {
```

### FieldMayBeFinal
Field `stack` may be 'final'
in `logical/src/main/java/org/apache/drill/common/graph/GraphAlgos.java`
#### Snippet
```java

    private int index = 0;
    private List<AdjacencyList<V>.Node> stack = new LinkedList<AdjacencyList<V>.Node>();
    private List<List<AdjacencyList<V>.Node>> SCC = new LinkedList<List<AdjacencyList<V>.Node>>();

```

### FieldMayBeFinal
Field `storageEngines` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/LogicalPlanBuilder.java`
#### Snippet
```java
public class LogicalPlanBuilder {
  private PlanProperties planProperties;
  private ImmutableMap.Builder<String, StoragePluginConfig> storageEngines = ImmutableMap.builder();
  private ImmutableList.Builder<LogicalOperator> operators = ImmutableList.builder();

```

### FieldMayBeFinal
Field `operators` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/LogicalPlanBuilder.java`
#### Snippet
```java
  private PlanProperties planProperties;
  private ImmutableMap.Builder<String, StoragePluginConfig> storageEngines = ImmutableMap.builder();
  private ImmutableList.Builder<LogicalOperator> operators = ImmutableList.builder();

  public LogicalPlanBuilder planProperties(PlanProperties planProperties) {
```

### FieldMayBeFinal
Field `conditions` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Join.java`
#### Snippet
```java
    private LogicalOperator right;
    private JoinRelType type;
    private List<JoinCondition> conditions = Lists.newArrayList();

    public Builder type(JoinRelType type) {
```

### FieldMayBeFinal
Field `exprs` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Project.java`
#### Snippet
```java
  public static class Builder extends AbstractSingleBuilder<Project, Builder> {

    private List<NamedExpression> exprs = Lists.newArrayList();

    public Builder addExpr(NamedExpression expr) {
```

### FieldMayBeFinal
Field `inputs` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Union.java`
#### Snippet
```java

    public static class Builder extends AbstractBuilder<Union>{
      private List<LogicalOperator> inputs = Lists.newArrayList();
      private boolean distinct;

```

### FieldMayBeFinal
Field `start` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
    private List<NamedExpression> withins = Lists.newArrayList();
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
    private long end = Long.MIN_VALUE;

```

### FieldMayBeFinal
Field `withins` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
  public static class Builder extends AbstractSingleBuilder<Window, Builder>{
    private List<NamedExpression> aggregations = Lists.newArrayList();
    private List<NamedExpression> withins = Lists.newArrayList();
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
```

### FieldMayBeFinal
Field `end` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
    private long end = Long.MIN_VALUE;


```

### FieldMayBeFinal
Field `orderings` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
    private List<NamedExpression> aggregations = Lists.newArrayList();
    private List<NamedExpression> withins = Lists.newArrayList();
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
    private long end = Long.MIN_VALUE;
```

### FieldMayBeFinal
Field `aggregations` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java

  public static class Builder extends AbstractSingleBuilder<Window, Builder>{
    private List<NamedExpression> aggregations = Lists.newArrayList();
    private List<NamedExpression> withins = Lists.newArrayList();
    private List<Order.Ordering> orderings = Lists.newArrayList();
```

### FieldMayBeFinal
Field `orderings` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java

  public static class Builder extends AbstractSingleBuilder<Order, Builder>{
    private List<Ordering> orderings = Lists.newArrayList();
    private FieldReference within;

```

### FieldMayBeFinal
Field `DRILL_TO_CALCITE_NULL_DIR_MAPPING` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
        .put(ORDER_ASCENDING, Direction.ASCENDING)
        .put(ORDER_DESCENDING, Direction.DESCENDING).build();
    private static ImmutableMap<String, NullDirection> DRILL_TO_CALCITE_NULL_DIR_MAPPING =
        ImmutableMap.<String, NullDirection>builder()
            .put(NULLS_FIRST, NullDirection.FIRST)
```

### FieldMayBeFinal
Field `DRILL_TO_CALCITE_DIR_MAPPING` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Order.java`
#### Snippet
```java
    /** The values in the plans for ordering specification are ASC, DESC, not the
     * full words featured in the Calcite {@link Direction} Enum, need to map between them. */
    private static ImmutableMap<String, Direction> DRILL_TO_CALCITE_DIR_MAPPING =
        ImmutableMap.<String, Direction>builder()
        .put(ORDER_ASC, Direction.ASCENDING)
```

### FieldMayBeFinal
Field `inputs` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Except.java`
#### Snippet
```java

    public static class Builder extends AbstractBuilder<Except>{
      private List<LogicalOperator> inputs = Lists.newArrayList();
      private boolean distinct;

```

### FieldMayBeFinal
Field `inputs` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/Intersect.java`
#### Snippet
```java

    public static class Builder extends AbstractBuilder<Intersect>{
      private List<LogicalOperator> inputs = Lists.newArrayList();
      private boolean distinct;

```

### FieldMayBeFinal
Field `keys` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/GroupingAggregate.java`
#### Snippet
```java

  public static class Builder extends AbstractSingleBuilder<GroupingAggregate, Builder>{
    private List<NamedExpression> keys = Lists.newArrayList();
    private List<NamedExpression> exprs = Lists.newArrayList();

```

### FieldMayBeFinal
Field `exprs` may be 'final'
in `logical/src/main/java/org/apache/drill/common/logical/data/GroupingAggregate.java`
#### Snippet
```java
  public static class Builder extends AbstractSingleBuilder<GroupingAggregate, Builder>{
    private List<NamedExpression> keys = Lists.newArrayList();
    private List<NamedExpression> exprs = Lists.newArrayList();

    public Builder addKey(FieldReference ref, LogicalExpression expr){
```

### FieldMayBeFinal
Field `majorType` may be 'final'
in `logical/src/main/java/org/apache/drill/common/expression/MajorTypeInLogicalExpression.java`
#### Snippet
```java
 */
public class MajorTypeInLogicalExpression implements LogicalExpression {
  private TypeProtos.MajorType majorType;

  public MajorTypeInLogicalExpression(TypeProtos.MajorType majorType) {
```

### FieldMayBeFinal
Field `name` may be 'final'
in `logical/src/main/java/org/apache/drill/common/expression/types/AtomType.java`
#### Snippet
```java

public class AtomType extends DataType {
  private String name;
  private Comparability comparability;
  private boolean isNumericType;
```

### FieldMayBeFinal
Field `comparability` may be 'final'
in `logical/src/main/java/org/apache/drill/common/expression/types/AtomType.java`
#### Snippet
```java
public class AtomType extends DataType {
  private String name;
  private Comparability comparability;
  private boolean isNumericType;

```

### FieldMayBeFinal
Field `isNumericType` may be 'final'
in `logical/src/main/java/org/apache/drill/common/expression/types/AtomType.java`
#### Snippet
```java
  private String name;
  private Comparability comparability;
  private boolean isNumericType;

  public AtomType(String name, Comparability comparability, boolean isNumericType) {
```

### FieldMayBeFinal
Field `costComparator` may be 'final'
in `logical/src/main/java/org/apache/drill/common/expression/visitors/ConditionalExprOptimizer.java`
#### Snippet
```java
  }

  private static Comparator<LogicalExpression> costComparator = new Comparator<LogicalExpression> () {
    @Override
    public int compare(LogicalExpression e1, LogicalExpression e2) {
```

### FieldMayBeFinal
Field `managedBuffers` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/BufferManagerImpl.java`
#### Snippet
```java

public class BufferManagerImpl implements BufferManager {
  private LongObjectHashMap<DrillBuf> managedBuffers = new LongObjectHashMap<>();
  private final BufferAllocator allocator;

```

### FieldMayBeFinal
Field `fileSystems` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/BaseOperatorContext.java`
#### Snippet
```java
  protected final PhysicalOperator popConfig;
  protected final BufferManager manager;
  private List<DrillFileSystem> fileSystems;
  private ControlsInjector injector;
  private boolean allowCreatingFileSystem = true;
```

### FieldMayBeFinal
Field `sender` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/AckSender.java`
#### Snippet
```java

  private AtomicInteger count = new AtomicInteger(0);
  private ResponseSender sender;
  private int everLargestAdviceCredit = Acks.NO_SUGGESTED_CREDIT;

```

### FieldMayBeFinal
Field `count` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/AckSender.java`
#### Snippet
```java
public class AckSender {

  private AtomicInteger count = new AtomicInteger(0);
  private ResponseSender sender;
  private int everLargestAdviceCredit = Acks.NO_SUGGESTED_CREDIT;
```

### FieldMayBeFinal
Field `handler` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataServer.java`
#### Snippet
```java
  private class ProxyCloseHandler implements GenericFutureListener<ChannelFuture> {

    private volatile GenericFutureListener<ChannelFuture> handler;

    public ProxyCloseHandler(GenericFutureListener<ChannelFuture> handler) {
```

### FieldMayBeFinal
Field `properties` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java
  private DrillbitEndpoint endpoint;

  private DrillProperties properties;

  public UserClient(String clientName, DrillConfig config, Properties properties, boolean supportComplexTypes,
```

### FieldMayBeFinal
Field `sslConfig` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java
  private Set<RpcType> supportedMethods = null;

  private SSLConfig sslConfig;
  private DrillbitEndpoint endpoint;

```

### FieldMayBeFinal
Field `endpoint` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java

  private SSLConfig sslConfig;
  private DrillbitEndpoint endpoint;

  private DrillProperties properties;
```

### FieldMayBeFinal
Field `dataBodies` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java

    private CustomMessage message;
    private ByteBuf[] dataBodies;

    public SyncCustomMessageSender(CustomMessage message, ByteBuf[] dataBodies) {
```

### FieldMayBeFinal
Field `message` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
    FutureBitCommand<CustomMessage, ControlConnection, RpcType, CustomMessage> {

    private CustomMessage message;
    private ByteBuf[] dataBodies;

```

### FieldMayBeFinal
Field `message` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
    ListeningCommand<CustomMessage, ControlConnection, RpcType, CustomMessage> {

    private CustomMessage message;
    private ByteBuf[] dataBodies;

```

### FieldMayBeFinal
Field `messageTypeId` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
   */
  public class CustomTunnel<SEND, RECEIVE> {
    private int messageTypeId;
    private CustomSerDe<SEND> send;
    private CustomSerDe<RECEIVE> receive;
```

### FieldMayBeFinal
Field `dataBodies` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java

    private CustomMessage message;
    private ByteBuf[] dataBodies;

    public CustomMessageSender(RpcOutcomeListener<CustomMessage> listener, CustomMessage message, ByteBuf[] dataBodies) {
```

### FieldMayBeFinal
Field `send` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
  public class CustomTunnel<SEND, RECEIVE> {
    private int messageTypeId;
    private CustomSerDe<SEND> send;
    private CustomSerDe<RECEIVE> receive;

```

### FieldMayBeFinal
Field `receive` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
    private int messageTypeId;
    private CustomSerDe<SEND> send;
    private CustomSerDe<RECEIVE> receive;

    private CustomTunnel(int messageTypeId, CustomSerDe<SEND> send, CustomSerDe<RECEIVE> receive) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/SingleClassStringWriter.java`
#### Snippet
```java

  private boolean used;
  private StringWriter writer = new StringWriter();

  @Override
```

### FieldMayBeFinal
Field `TEMPLATE_DEFINITION` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
  private FragmentContext context;

  private TemplateClassDefinition<Hash64> TEMPLATE_DEFINITION = new TemplateClassDefinition<Hash64>(Hash64.class, Hash64Template.class);

  private static final GeneratorMapping DO_SETUP_CONSTANT = GeneratorMapping.create("doSetup" /* setup method */, "doSetup" /* eval method */, null /* reset */, null /* cleanup */);
```

### FieldMayBeFinal
Field `context` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
  private RecordBatch recordBatch;

  private FragmentContext context;

  private TemplateClassDefinition<Hash64> TEMPLATE_DEFINITION = new TemplateClassDefinition<Hash64>(Hash64.class, Hash64Template.class);
```

### FieldMayBeFinal
Field `recordBatch` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
public class ValueVectorHashHelper {

  private RecordBatch recordBatch;

  private FragmentContext context;
```

### FieldMayBeFinal
Field `fragment` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/QueryWorkUnit.java`
#### Snippet
```java

  public static class MinorFragmentDefn {
    private PlanFragment fragment;
    private final FragmentRoot root;
    private final OptionList options;
```

### FieldMayBeFinal
Field `fragments` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/QueryWorkUnit.java`
#### Snippet
```java
  private final MinorFragmentDefn rootFragmentDefn;
  private final FragmentRoot rootOperator; // for local
  private List<PlanFragment> fragments = new ArrayList<>();
  private final List<MinorFragmentDefn> minorFragmentDefns;
  private final Wrapper rootWrapper;
```

### FieldMayBeFinal
Field `q` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/ResponseSenderQueue.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(ResponseSenderQueue.class);

  private Queue<AckSender> q = Queues.newConcurrentLinkedQueue();

  public void enqueueResponse(AckSender sender){
```

### FieldMayBeFinal
Field `numBytes` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilterDef.java`
#### Snippet
```java
public class BloomFilterDef {
  //bloom filter size in bytes
  private int numBytes;
  //true send to itself, false send to foreman
  private boolean local;
```

### FieldMayBeFinal
Field `probeField` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilterDef.java`
#### Snippet
```java
  private boolean local;

  private String probeField;

  private String buildField;
```

### FieldMayBeFinal
Field `buildField` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilterDef.java`
#### Snippet
```java
  private String probeField;

  private String buildField;
  //TODO
  @JsonIgnore
```

### FieldMayBeFinal
Field `byteBuf` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  private static final int MINIMUM_BLOOM_SIZE_IN_BYTES = 256;

  private DrillBuf byteBuf;

  private int numBytes;
```

### FieldMayBeFinal
Field `numBytes` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  private DrillBuf byteBuf;

  private int numBytes;

  private int bucketMask[] = new int[8];
```

### FieldMayBeFinal
Field `context` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterReporter.java`
#### Snippet
```java
public class RuntimeFilterReporter {

  private ExecutorFragmentContext context;

  public RuntimeFilterReporter(ExecutorFragmentContext context) {
```

### FieldMayBeFinal
Field `bucketMask` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  private int numBytes;

  private int bucketMask[] = new int[8];

  public BloomFilter(int numBytes, BufferAllocator bufferAllocator) {
```

### FieldMayBeFinal
Field `exceptionType` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/SqlUnsupportedException.java`
#### Snippet
```java
    FUNCTION(UnsupportedFunctionException.class.getSimpleName());

    private String exceptionType;
    ExceptionType(String exceptionType) {
      this.exceptionType = exceptionType;
```

### FieldMayBeFinal
Field `identifier` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterWritable.java`
#### Snippet
```java
  private DrillBuf[] data;

  private String identifier;

  public RuntimeFilterWritable(BitData.RuntimeFilterBDef runtimeFilterBDef, DrillBuf... data) {
```

### FieldMayBeFinal
Field `runtimeFilterBDef` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterWritable.java`
#### Snippet
```java
public class RuntimeFilterWritable implements AutoCloseables.Closeable{

  private BitData.RuntimeFilterBDef runtimeFilterBDef;

  private DrillBuf[] data;
```

### FieldMayBeFinal
Field `holders` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
  protected class RuntimeFilterParallelismCollector extends AbstractPhysicalVisitor<Void, RFHelperHolder, RuntimeException> {

    private List<RFHelperHolder> holders = new ArrayList<>();

    @Override
```

### FieldMayBeFinal
Field `sendingAccountor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
  private Wrapper rootWrapper;

  private SendingAccountor sendingAccountor = new SendingAccountor();

  private RuntimeFilterSink runtimeFilterSink;
```

### FieldMayBeFinal
Field `targetOp` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
    private boolean contain = false;

    private PhysicalOperator targetOp;

    public WrapperOperatorsVisitor(PhysicalOperator targetOp) {
```

### FieldMayBeFinal
Field `runtimeFilterSink` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
  private SendingAccountor sendingAccountor = new SendingAccountor();

  private RuntimeFilterSink runtimeFilterSink;

  private static final Logger logger = LoggerFactory.getLogger(RuntimeFilterRouter.class);
```

### FieldMayBeFinal
Field `identifier` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
    private boolean contain = false;

    private long identifier;


```

### FieldMayBeFinal
Field `rootWrapper` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
public class RuntimeFilterRouter {

  private Wrapper rootWrapper;

  private SendingAccountor sendingAccountor = new SendingAccountor();
```

### FieldMayBeFinal
Field `asyncAggregateWorker` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private SendingAccountor sendingAccountor;

  private  AsyncAggregateWorker asyncAggregateWorker;

  private AtomicBoolean running = new AtomicBoolean(true);
```

### FieldMayBeFinal
Field `running` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private  AsyncAggregateWorker asyncAggregateWorker;

  private AtomicBoolean running = new AtomicBoolean(true);

  private static final Logger logger = LoggerFactory.getLogger(RuntimeFilterSink.class);
```

### FieldMayBeFinal
Field `drillbitContext` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private Map<Integer, Stopwatch> joinMjId2Stopwatch = new HashMap<>();

  private DrillbitContext drillbitContext;

  private SendingAccountor sendingAccountor;
```

### FieldMayBeFinal
Field `joinMjId2AggregatedRF` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java

  //HashJoin node's major fragment id to its aggregated RuntimeFilterWritable
  private Map<Integer, RuntimeFilterWritable> joinMjId2AggregatedRF = new HashMap<>();
  //for debug usage
  private Map<Integer, Stopwatch> joinMjId2Stopwatch = new HashMap<>();
```

### FieldMayBeFinal
Field `rfQueue` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
{

  private BlockingQueue<RuntimeFilterWritable> rfQueue = new LinkedBlockingQueue<>();

  private Map<Integer, Integer> joinMjId2rfNumber;
```

### FieldMayBeFinal
Field `joinMjId2Stopwatch` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private Map<Integer, RuntimeFilterWritable> joinMjId2AggregatedRF = new HashMap<>();
  //for debug usage
  private Map<Integer, Stopwatch> joinMjId2Stopwatch = new HashMap<>();

  private DrillbitContext drillbitContext;
```

### FieldMayBeFinal
Field `sendingAccountor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private DrillbitContext drillbitContext;

  private SendingAccountor sendingAccountor;

  private  AsyncAggregateWorker asyncAggregateWorker;
```

### FieldMayBeFinal
Field `over` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
  private class AsyncAggregateWorker implements Runnable
  {
    private AtomicBoolean over = new AtomicBoolean(false);

    @Override
```

### FieldMayBeFinal
Field `context` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ResourceManagerBuilder.java`
#### Snippet
```java
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(ResourceManagerBuilder.class);

  private DrillbitContext context;

  public ResourceManagerBuilder(final DrillbitContext context) {
```

### FieldMayBeFinal
Field `latch` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
    private RawFragmentBatch batch;
    private volatile boolean available;
    private CountDownLatch latch;
    private volatile int bodyLength;
    private volatile boolean outOfMemory = false;
```

### FieldMayBeFinal
Field `DRILL_LOCAL_IMPL_STRING` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(SpoolingRawBatchBuffer.class);

  private static String DRILL_LOCAL_IMPL_STRING = "fs.drill-local.impl";
  private static final float STOP_SPOOLING_FRACTION = (float) 0.5;
  public static final long ALLOCATOR_INITIAL_RESERVATION = 1*1024*1024;
```

### FieldMayBeFinal
Field `queryContext` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DefaultResourceManager.java`
#### Snippet
```java
  public static class DefaultResourceAllocator implements QueryResourceAllocator {

    private QueryContext queryContext;

    protected DefaultResourceAllocator(QueryContext queryContext) {
```

### FieldMayBeFinal
Field `queryMemory` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/EmbeddedQueryQueue.java`
#### Snippet
```java
    private final QueryId queryId;
    private boolean released;
    private long queryMemory;

    public EmbeddedQueueLease(QueryId queryId, long queryMemory) {
```

### FieldMayBeFinal
Field `valueClass` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java

    private Class<K> keyClass;
    private Class<V> valueClass;
    private String name;
    private SerializationMode mode = SerializationMode.DRILL_SERIALIZIABLE;
```

### FieldMayBeFinal
Field `keyClass` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/DistributedCache.java`
#### Snippet
```java
  public static class CacheConfigBuilder<K, V> {

    private Class<K> keyClass;
    private Class<V> valueClass;
    private String name;
```

### FieldMayBeFinal
Field `discovery` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java

  private CuratorFramework curator;
  private ServiceDiscovery<DrillbitEndpoint> discovery;
  private volatile Collection<DrillbitEndpoint> endpoints = Collections.emptyList();
  private final String serviceName;
```

### FieldMayBeFinal
Field `curator` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(ZKClusterCoordinator.class);

  private CuratorFramework curator;
  private ServiceDiscovery<DrillbitEndpoint> discovery;
  private volatile Collection<DrillbitEndpoint> endpoints = Collections.emptyList();
```

### FieldMayBeFinal
Field `endpointsMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java

  // endpointsMap maps Multikey( comprises of endoint address and port) to Drillbit endpoints
  private ConcurrentHashMap<MultiKey, DrillbitEndpoint> endpointsMap = new ConcurrentHashMap<MultiKey,DrillbitEndpoint>();
  private static final Pattern ZK_COMPLEX_STRING = Pattern.compile("(^.*?)/(.*)/([^/]*)$");

```

### FieldMayBeFinal
Field `TIMEOUT_PER_RUNNABLE_IN_MSECS` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/TimedCallable.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(TimedCallable.class);

  private static long TIMEOUT_PER_RUNNABLE_IN_MSECS = 15000;

  private volatile long startTime = 0;
```

### FieldMayBeFinal
Field `bytes` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/MagicString.java`
#### Snippet
```java
public class MagicString {
  private long offset;
  private byte[] bytes;

  public MagicString(long offset, byte[] bytes) {
```

### FieldMayBeFinal
Field `offset` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/MagicString.java`
#### Snippet
```java

public class MagicString {
  private long offset;
  private byte[] bytes;

```

### FieldMayBeFinal
Field `factories` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSystemSchemaFactory.java`
#### Snippet
```java
  public static final String LOCAL_FS_SCHEME = "file";

  private List<WorkspaceSchemaFactory> factories;
  protected FileSystemPlugin plugin;

```

### FieldMayBeFinal
Field `dirModifCheckMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/MetadataContext.java`
#### Snippet
```java
   *  is expected to be relatively small.
   */
  private Map<Path, Boolean> dirModifCheckMap = Maps.newHashMap();

  private PruneStatus pruneStatus = PruneStatus.NOT_STARTED;
```

### FieldMayBeFinal
Field `length` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/ReadEntryFromHDFS.java`
#### Snippet
```java

  private long start;
  private long length;

  @JsonCreator
```

### FieldMayBeFinal
Field `start` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/ReadEntryFromHDFS.java`
#### Snippet
```java
public class ReadEntryFromHDFS extends ReadEntryWithPath implements FileWork {

  private long start;
  private long length;

```

### FieldMayBeFinal
Field `wasAllPartitionsPruned` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
  private boolean hadWildcard;
  // whether all partitions were previously pruned for this selection, false by default
  private boolean wasAllPartitionsPruned;

  /**
```

### FieldMayBeFinal
Field `serializer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/PersistentStoreConfig.java`
#### Snippet
```java
  public static class StoreConfigBuilder<V> {
    private String name;
    private InstanceSerializer<V> serializer;
    private PersistentStoreMode mode = PersistentStoreMode.PERSISTENT;
    private int capacity;
```

### FieldMayBeFinal
Field `isAdmin` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/BitToUserConnectionIterator.java`
#### Snippet
```java
  Iterator<ConnectionInfo> itr;
  private String queryingUsername;
  private boolean isAdmin;

  public BitToUserConnectionIterator(ExecutorFragmentContext context) {
```

### FieldMayBeFinal
Field `queryingUsername` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/BitToUserConnectionIterator.java`
#### Snippet
```java

  Iterator<ConnectionInfo> itr;
  private String queryingUsername;
  private boolean isAdmin;

```

### FieldMayBeFinal
Field `endpoints` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/DrillbitIterator.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillbitIterator.class);

  private Iterator<DrillbitEndpoint> endpoints;
  private DrillbitEndpoint current;

```

### FieldMayBeFinal
Field `current` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/DrillbitIterator.java`
#### Snippet
```java

  private Iterator<DrillbitEndpoint> endpoints;
  private DrillbitEndpoint current;

  public DrillbitIterator(ExecutorFragmentContext c) {
```

### FieldMayBeFinal
Field `capacity` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/store/provider/InMemoryStoreProvider.java`
#### Snippet
```java
public class InMemoryStoreProvider implements PersistentStoreProvider {

  private int capacity;

  public InMemoryStoreProvider(int capacity) {
```

### FieldMayBeFinal
Field `rand` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/VaryingStringGen.java`
#### Snippet
```java
public class VaryingStringGen extends AbstractFieldGen {

  private Random rand = new Random();
  private int length;
  private int span;
```

### FieldMayBeFinal
Field `descrip` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockTableDef.java`
#### Snippet
```java
  }

  private String descrip;
  MockTableSelection entries;

```

### FieldMayBeFinal
Field `baseTime` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java
  private final int ONE_YEAR = ONE_DAY * 365;

  private long baseTime;
  private SimpleDateFormat fmt;

```

### FieldMayBeFinal
Field `fmt` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java

  private long baseTime;
  private SimpleDateFormat fmt;

  public DateGen() {
```

### FieldMayBeFinal
Field `numRecordsToRead` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/RowGroupReadEntry.java`
#### Snippet
```java

  private int rowGroupIndex;
  private long numRecordsToRead;

  @JsonCreator
```

### FieldMayBeFinal
Field `rowGroupIndex` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/RowGroupReadEntry.java`
#### Snippet
```java
public class RowGroupReadEntry extends ReadEntryFromHDFS {

  private int rowGroupIndex;
  private long numRecordsToRead;

```

### FieldMayBeFinal
Field `rowCount` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/RowGroupInfo.java`
#### Snippet
```java
  private int rowGroupIndex;
  private List<? extends ColumnMetadata> columns;
  private long rowCount;  // rowCount = -1 indicates to include all rows.
  private long numRecordsToRead;

```

### FieldMayBeFinal
Field `rowGroupIndex` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/RowGroupInfo.java`
#### Snippet
```java

  private EndpointByteMap byteMap;
  private int rowGroupIndex;
  private List<? extends ColumnMetadata> columns;
  private long rowCount;  // rowCount = -1 indicates to include all rows.
```

### FieldMayBeFinal
Field `cleanUpLocations` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private FileSystem fs;
  private String location;
  private List<Path> cleanUpLocations;
  private String prefix;
  private int index = 0;
```

### FieldMayBeFinal
Field `codecFactory` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private CompressionCodecName codec = CompressionCodecName.SNAPPY;
  private WriterVersion writerVersion = WriterVersion.PARQUET_1_0;
  private CompressionCodecFactory codecFactory;

  private long recordCount = 0;
```

### FieldMayBeFinal
Field `oContext` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private String prefix;
  private int index = 0;
  private OperatorContext oContext;
  private List<String> partitionColumns;
  private boolean hasPartitions;
```

### FieldMayBeFinal
Field `extraMetaData` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private ParquetFileWriter parquetFileWriter;
  private MessageType schema;
  private Map<String, String> extraMetaData = new HashMap<>();
  private int blockSize;
  private int pageSize;
```

### FieldMayBeFinal
Field `conf` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private BatchSchema batchSchema;

  private Configuration conf;
  private FileSystem fs;
  private String location;
```

### FieldMayBeFinal
Field `partitionColumns` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private int index = 0;
  private OperatorContext oContext;
  private List<String> partitionColumns;
  private boolean hasPartitions;
  private PrimitiveTypeName logicalTypeForDecimals;
```

### FieldMayBeFinal
Field `hasPartitions` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private OperatorContext oContext;
  private List<String> partitionColumns;
  private boolean hasPartitions;
  private PrimitiveTypeName logicalTypeForDecimals;
  private boolean usePrimitiveTypesForDecimals;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V4.java`
#### Snippet
```java

    public static class Key {
      private SchemaPath name;
      private int hashCode = 0;

```

### FieldMayBeFinal
Field `allocator` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/AirliftBytesInputCompressor.java`
#### Snippet
```java

  // the direct memory allocator to be used for (de)compression outputs
  private ByteBufferAllocator allocator;

  // all the direct memory buffers we've allocated, and must release
```

### FieldMayBeFinal
Field `airDecomp` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/AirliftBytesInputCompressor.java`
#### Snippet
```java

  // backing aircompressor decompressor
  private Decompressor airDecomp;

  // the direct memory allocator to be used for (de)compression outputs
```

### FieldMayBeFinal
Field `allocatedBuffers` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/AirliftBytesInputCompressor.java`
#### Snippet
```java

  // all the direct memory buffers we've allocated, and must release
  private Deque<ByteBuffer> allocatedBuffers;

  public AirliftBytesInputCompressor(CompressionCodecName codecName, ByteBufferAllocator allocator) {
```

### FieldMayBeFinal
Field `codecName` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/AirliftBytesInputCompressor.java`
#### Snippet
```java

  // name of the codec provided by this compressor
  private CompressionCodecName codecName;

  // backing aircompressor compressor
```

### FieldMayBeFinal
Field `airComp` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/AirliftBytesInputCompressor.java`
#### Snippet
```java

  // backing aircompressor compressor
  private Compressor airComp;

  // backing aircompressor decompressor
```

### FieldMayBeFinal
Field `name` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V3.java`
#### Snippet
```java

    public static class Key {
      private SchemaPath name;
      private int hashCode = 0;

```

### FieldMayBeFinal
Field `name` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java

    public static class Key {
      private String[] name;
      private int hashCode = 0;

```

### FieldMayBeFinal
Field `selectedColumnMetadata` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetSchema.java`
#### Snippet
```java
   * in the SELECT clause but not in the file) appear elsewhere.
   */
  private List<ParquetColumnMetadata> selectedColumnMetadata = new ArrayList<>();
  private int bitWidthAllFixedFields;
  private boolean allFieldsFixedLength;
```

### FieldMayBeFinal
Field `useAsyncColReader` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ReadState.java`
#### Snippet
```java
   */
  private long totalRecordsRead;
  private boolean useAsyncColReader;

  public ReadState(ParquetSchema schema,
```

### FieldMayBeFinal
Field `threadPool` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
  long readLengthInBits = 0;
  long recordsReadInThisIteration = 0;
  private ExecutorService threadPool;

  volatile boolean isShuttingDown; //Indicate to not submit any new AsyncPageReader Tasks during clear()
```

### FieldMayBeFinal
Field `fieldOverflowMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchSizerManager.java`
#### Snippet
```java
   * b) overflow is a result of batch constraints enforcement which this class manages the overflow logic
   */
  private Map<String, FieldOverflowStateContainer> fieldOverflowMap = CaseInsensitiveMap.newHashMap();

  /** For controlling batch statistics logging */
```

### FieldMayBeFinal
Field `start` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java
  public static class FileWorkImpl implements FileWork {

    private long start;
    private long length;
    private Path path;
```

### FieldMayBeFinal
Field `path` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java
    private long start;
    private long length;
    private Path path;

    @JsonCreator
```

### FieldMayBeFinal
Field `length` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java

    private long start;
    private long length;
    private Path path;

```

### FieldMayBeFinal
Field `byteMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java
  private long length;
  private Path path;
  private EndpointByteMap byteMap;

  public CompleteFileWork(EndpointByteMap byteMap, long start, long length, Path path) {
```

### FieldMayBeFinal
Field `length` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java

  private long start;
  private long length;
  private Path path;
  private EndpointByteMap byteMap;
```

### FieldMayBeFinal
Field `path` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java
  private long start;
  private long length;
  private Path path;
  private EndpointByteMap byteMap;

```

### FieldMayBeFinal
Field `start` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/CompleteFileWork.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(CompleteFileWork.class);

  private long start;
  private long length;
  private Path path;
```

### FieldMayBeFinal
Field `queueSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java

  private ExecutorService threadPool;
  private long queueSize;
  private LinkedBlockingQueue<ReadStatus> pageQueue;
  private ConcurrentLinkedQueue<Future<Void>> asyncPageRead;
```

### FieldMayBeFinal
Field `asyncPageRead` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
  private long queueSize;
  private LinkedBlockingQueue<ReadStatus> pageQueue;
  private ConcurrentLinkedQueue<Future<Void>> asyncPageRead;
  private long totalPageValuesRead = 0;
  // Object to use to synchronize access to the page Queue.
```

### FieldMayBeFinal
Field `threadPool` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
  static final Logger logger = LoggerFactory.getLogger(AsyncPageReader.class);

  private ExecutorService threadPool;
  private long queueSize;
  private LinkedBlockingQueue<ReadStatus> pageQueue;
```

### FieldMayBeFinal
Field `pageQueue` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
  private ExecutorService threadPool;
  private long queueSize;
  private LinkedBlockingQueue<ReadStatus> pageQueue;
  private ConcurrentLinkedQueue<Future<Void>> asyncPageRead;
  private long totalPageValuesRead = 0;
```

### FieldMayBeFinal
Field `comparator` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/AssignmentCreator.java`
#### Snippet
```java
   * Comparator used to sort in order of decreasing affinity
   */
  private static Comparator<Entry<DrillbitEndpoint,Long>> comparator = new Comparator<Entry<DrillbitEndpoint,Long>>() {
    @Override
    public int compare(Entry<DrillbitEndpoint, Long> o1, Entry<DrillbitEndpoint,Long> o2) {
```

### FieldMayBeFinal
Field `tokenTable` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/security/oauth/OAuthTokenCredentials.java`
#### Snippet
```java
  private final String tokenURI;

  private Optional<PersistentTokenTable> tokenTable;

  /**
```

### FieldMayBeFinal
Field `status` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/BlockMapBuilder.java`
#### Snippet
```java

  private class FileStatusWork implements FileWork{
    private FileStatus status;

    public FileStatusWork(FileStatus status) {
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/LoggingResultsListener.java`
#### Snippet
```java

public class LoggingResultsListener implements UserResultsListener {
  private static Logger logger = LoggerFactory.getLogger(LoggingResultsListener.class);

  private final AtomicInteger count = new AtomicInteger();
```

### FieldMayBeFinal
Field `rowWidth` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(JoinBatchMemoryManager.class);

  private int rowWidth[];
  private RecordBatch recordBatch[];
  private Set<String> columnsToExclude;
```

### FieldMayBeFinal
Field `columnsToExclude` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java
  private int rowWidth[];
  private RecordBatch recordBatch[];
  private Set<String> columnsToExclude;

  private static final int numInputs = 2;
```

### FieldMayBeFinal
Field `recordBatch` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/JoinBatchMemoryManager.java`
#### Snippet
```java

  private int rowWidth[];
  private RecordBatch recordBatch[];
  private Set<String> columnsToExclude;

```

### FieldMayBeFinal
Field `hints` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorInitializer.java`
#### Snippet
```java
  }

  private Map<String, AllocationHint> hints = CaseInsensitiveMap.newHashMap();

  public void variableWidth(String name, int width) {
```

### FieldMayBeFinal
Field `vector` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SimpleVectorWrapper.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(SimpleVectorWrapper.class);

  private T vector;

  public SimpleVectorWrapper(T v) {
```

### FieldMayBeFinal
Field `EMPTY_DEF` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/FragmentWritableBatch.java`
#### Snippet
```java
public class FragmentWritableBatch {

  private static RecordBatchDef EMPTY_DEF = RecordBatchDef.newBuilder().setRecordCount(0).build();

  private final ByteBuf[] buffers;
```

### FieldMayBeFinal
Field `f` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/HyperVectorWrapper.java`
#### Snippet
```java

  private T[] vectors;
  private MaterializedField f;
  private final boolean releasable;

```

### FieldMayBeFinal
Field `fields` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaBuilder.java`
#### Snippet
```java
 */
public class SchemaBuilder {
  private List<MaterializedField> fields = Lists.newArrayList();

  private BatchSchema.SelectionVectorMode selectionVectorMode = SelectionVectorMode.NONE;
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
 */
public class DrillClient implements Closeable, ConnectionThrottle {
  private static Logger logger = LoggerFactory.getLogger(DrillClient.class);
  public static final String DEFAULT_CLIENT_NAME = "Apache Drill Java client";
  private static final ObjectMapper objectMapper = JacksonUtils.createObjectMapper();
```

### FieldMayBeFinal
Field `schemas` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/selection/SelectionVector4Builder.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(SelectionVector4Builder.class);

  private List<BatchSchema> schemas = Lists.newArrayList();

  public void add(RecordBatch batch, boolean newSchema) throws SchemaChangeException {
```

### FieldMayBeFinal
Field `buf` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
    private VarDecimalWriter writer;
    private VarDecimalHolder holder = new VarDecimalHolder();
    private DrillBuf buf;

    public DrillVarDecimalConverter(VarDecimalWriter writer, int precision, int scale, DrillBuf buf) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class CorruptionDetectingDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillFixedBinaryToTimeStampConverter extends PrimitiveConverter {
    private TimeStampWriter writer;
    private TimeStampHolder holder = new TimeStampHolder();

    public DrillFixedBinaryToTimeStampConverter(TimeStampWriter writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillFloat4Converter extends PrimitiveConverter {
    private Float4Writer writer;
    private Float4Holder holder = new Float4Holder();

    public DrillFloat4Converter(Float4Writer writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillIntConverter extends PrimitiveConverter {
    private IntWriter writer;
    private IntHolder holder = new IntHolder();

    public DrillIntConverter(IntWriter writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillVarDecimalConverter extends PrimitiveConverter {
    private VarDecimalWriter writer;
    private VarDecimalHolder holder = new VarDecimalHolder();
    private DrillBuf buf;

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillBoolConverter extends PrimitiveConverter {
    private BitWriter writer;
    private BitHolder holder = new BitHolder();

    public DrillBoolConverter(BitWriter writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

    public DrillDateConverter(DateWriter writer) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
   */
  public static class DrillFixedBinaryToTimeStampConverter extends PrimitiveConverter {
    private TimeStampWriter writer;
    private TimeStampHolder holder = new TimeStampHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillFloat4Converter extends PrimitiveConverter {
    private Float4Writer writer;
    private Float4Holder holder = new Float4Holder();

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
    private VarBinaryWriter writer;
    private DrillBuf buf;
    private VarBinaryHolder holder = new VarBinaryHolder();

    public DrillVarBinaryConverter(VarBinaryWriter writer, DrillBuf buf) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillTimeStampConverter extends PrimitiveConverter {
    private TimeStampWriter writer;
    private TimeStampHolder holder = new TimeStampHolder();

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillFloat8Converter extends PrimitiveConverter {
    private Float8Writer writer;
    private Float8Holder holder = new Float8Holder();

    public DrillFloat8Converter(Float8Writer writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillCorruptedDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

    public DrillCorruptedDateConverter(DateWriter writer) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillFixedBinaryToVarbinaryConverter extends PrimitiveConverter {
    private VarBinaryWriter writer;
    private VarBinaryHolder holder = new VarBinaryHolder();

    public DrillFixedBinaryToVarbinaryConverter(VarBinaryWriter writer, int length, DrillBuf buf) {
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class CorruptionDetectingDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

    public CorruptionDetectingDateConverter(DateWriter writer) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillBoolConverter extends PrimitiveConverter {
    private BitWriter writer;
    private BitHolder holder = new BitHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillIntConverter extends PrimitiveConverter {
    private IntWriter writer;
    private IntHolder holder = new IntHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillVarDecimalConverter extends PrimitiveConverter {
    private VarDecimalWriter writer;
    private VarDecimalHolder holder = new VarDecimalHolder();
    private DrillBuf buf;
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillVarCharConverter extends PrimitiveConverter {
    private VarCharWriter writer;
    private VarCharHolder holder = new VarCharHolder();
    private DrillBuf buf;
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillTimeStampConverter extends PrimitiveConverter {
    private TimeStampWriter writer;
    private TimeStampHolder holder = new TimeStampHolder();

    public DrillTimeStampConverter(TimeStampWriter writer) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillVarBinaryConverter extends PrimitiveConverter {
    private VarBinaryWriter writer;
    private DrillBuf buf;
    private VarBinaryHolder holder = new VarBinaryHolder();
```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillBigIntConverter extends PrimitiveConverter {
    private BigIntWriter writer;
    private BigIntHolder holder = new BigIntHolder();

    public DrillBigIntConverter(BigIntWriter writer) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
   */
  public static class DrillFixedBinaryToVarbinaryConverter extends PrimitiveConverter {
    private VarBinaryWriter writer;
    private VarBinaryHolder holder = new VarBinaryHolder();

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillVarCharConverter extends PrimitiveConverter {
    private VarCharWriter writer;
    private VarCharHolder holder = new VarCharHolder();
    private DrillBuf buf;

```

### FieldMayBeFinal
Field `holder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  public static class DrillTimeConverter extends PrimitiveConverter {
    private TimeWriter writer;
    private TimeHolder holder = new TimeHolder();

    public DrillTimeConverter(TimeWriter writer) {
```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillFloat8Converter extends PrimitiveConverter {
    private Float8Writer writer;
    private Float8Holder holder = new Float8Holder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillTimeConverter extends PrimitiveConverter {
    private TimeWriter writer;
    private TimeHolder holder = new TimeHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillCorruptedDateConverter extends PrimitiveConverter {
    private DateWriter writer;
    private DateHolder holder = new DateHolder();

```

### FieldMayBeFinal
Field `writer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java

  public static class DrillBigIntConverter extends PrimitiveConverter {
    private BigIntWriter writer;
    private BigIntHolder holder = new BigIntHolder();

```

### FieldMayBeFinal
Field `isVariableWidth` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
     */

    private boolean isVariableWidth;

    /**
```

### FieldMayBeFinal
Field `totalNetSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
     */

    private int totalNetSize;

    /**
```

### FieldMayBeFinal
Field `fields` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/ListSchema.java`
#### Snippet
```java

public class ListSchema implements RecordSchema {
    private List<Field> fields;

    public ListSchema() {
```

### FieldMayBeFinal
Field `configLocation` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java

  @Parameter(names= {"-c", "--config"}, description="Configuration file you want to load.  Defaults to loading 'drill-override.conf' from the classpath.", required=false)
  private String configLocation = null;

  @Parameter
```

### FieldMayBeFinal
Field `debug` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java

  @Parameter(names= {"-d", "--debug"}, description="Whether you want to run the program in debug mode.", required=false)
  private boolean debug = false;

  @Parameter(names= {"-c", "--config"}, description="Configuration file you want to load.  Defaults to loading 'drill-override.conf' from the classpath.", required=false)
```

### FieldMayBeFinal
Field `help` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java

  @Parameter(names={"-h", "--help"}, description="Provide description of usage.", help=true)
  private boolean help = false;

  @Parameter(names= {"-d", "--debug"}, description="Whether you want to run the program in debug mode.", required=false)
```

### FieldMayBeFinal
Field `exccess` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java

  @Parameter
  private List<String> exccess = new ArrayList<String>();

  public boolean isDebug() {
```

### FieldMayBeFinal
Field `INSTANCE` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/AuthDynamicFeature.java`
#### Snippet
```java
  @Priority(Priorities.AUTHENTICATION) // authentication filter - should go first before all other filters.
  private static class AuthCheckFilter implements ContainerRequestFilter {
    private static AuthCheckFilter INSTANCE = new AuthCheckFilter();

    @Override
```

### FieldMayBeFinal
Field `optionScope` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java
    private OptionValue.AccessibleScopes accessibleScopes;
    private String kind;
    private String optionScope;

    @JsonCreator
```

### FieldMayBeFinal
Field `value` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java

    private String name;
    private Object value;
    private String defaultValue;
    private OptionValue.AccessibleScopes accessibleScopes;
```

### FieldMayBeFinal
Field `name` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java
  public static class OptionWrapper {

    private String name;
    private Object value;
    private String defaultValue;
```

### FieldMayBeFinal
Field `defaultValue` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java
    private String name;
    private Object value;
    private String defaultValue;
    private OptionValue.AccessibleScopes accessibleScopes;
    private String kind;
```

### FieldMayBeFinal
Field `kind` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java
    private String defaultValue;
    private OptionValue.AccessibleScopes accessibleScopes;
    private String kind;
    private String optionScope;

```

### FieldMayBeFinal
Field `accessibleScopes` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/StatusResources.java`
#### Snippet
```java
    private Object value;
    private String defaultValue;
    private OptionValue.AccessibleScopes accessibleScopes;
    private String kind;
    private String optionScope;
```

### FieldMayBeFinal
Field `width` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
  private StringBuilder sb;
  private int w = 0;
  private int width;

  public TableBuilder(final String[] columns, final String[] columnTooltip) {
```

### FieldMayBeFinal
Field `description` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValidator.java`
#### Snippet
```java

  public static class OptionDescription {
    private String description;
    private String shortDescription;

```

### FieldMayBeFinal
Field `shortDescription` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValidator.java`
#### Snippet
```java
  public static class OptionDescription {
    private String description;
    private String shortDescription;

    /**
```

### FieldMayBeFinal
Field `value` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/PersistedOptionValue.java`
#### Snippet
```java
  public static final String JSON_INTEGER_VAL = "int_val";

  private String value;
  private OptionValue.Kind kind;
  private String name;
```

### FieldMayBeFinal
Field `scopes` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
    ALL(EnumSet.of(OptionScope.BOOT, OptionScope.SYSTEM, OptionScope.SESSION, OptionScope.QUERY)); // Can be configured at the system, session, or query level

    private EnumSet<OptionScope> scopes;

    AccessibleScopes(EnumSet<OptionScope> scopes) {
```

### FieldMayBeFinal
Field `v` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/accessor/GenericAccessor.java`
#### Snippet
```java
public class GenericAccessor extends AbstractSqlAccessor {

  private ValueVector v;

  public GenericAccessor(ValueVector v) {
```

### FieldMayBeFinal
Field `cache` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CachedClassLoader.java`
#### Snippet
```java
   */

  private ConcurrentMap<String, Class<?>> cache = Maps.newConcurrentMap();

  public CachedClassLoader() {
```

### FieldMayBeFinal
Field `index` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/QueryClassLoader.java`
#### Snippet
```java
  private ClassCompilerSelector compilerSelector;

  private AtomicLong index = new AtomicLong(0);

  private ConcurrentMap<String, byte[]> customClasses = new MapMaker().concurrencyLevel(4).makeMap();
```

### FieldMayBeFinal
Field `customClasses` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/QueryClassLoader.java`
#### Snippet
```java
  private AtomicLong index = new AtomicLong(0);

  private ConcurrentMap<String, byte[]> customClasses = new MapMaker().concurrencyLevel(4).makeMap();

  public QueryClassLoader(DrillConfig config, OptionSet sessionOptions) {
```

### FieldMayBeFinal
Field `compilerSelector` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/QueryClassLoader.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(QueryClassLoader.class);

  private ClassCompilerSelector compilerSelector;

  private AtomicLong index = new AtomicLong(0);
```

### FieldMayBeFinal
Field `writeIndex` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java
  private GeneratorMapping current;
  private DirectExpression readIndex;
  private DirectExpression writeIndex;
  private DirectExpression incoming;
  private DirectExpression outgoing;
```

### FieldMayBeFinal
Field `readIndex` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java
  private int mappingIndex;
  private GeneratorMapping current;
  private DirectExpression readIndex;
  private DirectExpression writeIndex;
  private DirectExpression incoming;
```

### FieldMayBeFinal
Field `outgoing` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java
  private DirectExpression writeIndex;
  private DirectExpression incoming;
  private DirectExpression outgoing;
  private DirectExpression workspace;
  private DirectExpression workspaceIndex;
```

### FieldMayBeFinal
Field `incoming` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java
  private DirectExpression readIndex;
  private DirectExpression writeIndex;
  private DirectExpression incoming;
  private DirectExpression outgoing;
  private DirectExpression workspace;
```

### FieldMayBeFinal
Field `mappings` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java

  private GeneratorMapping constant;
  private GeneratorMapping[] mappings;
  private int mappingIndex;
  private GeneratorMapping current;
```

### FieldMayBeFinal
Field `range` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/Checker.java`
#### Snippet
```java

class Checker implements SqlOperandTypeChecker {
  private SqlOperandCountRange range;

  public static final Checker ANY_CHECKER = new Checker();
```

### FieldMayBeFinal
Field `schema` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlUseSchema.java`
#### Snippet
```java
  };

  private SqlIdentifier schema;

  public SqlUseSchema(SqlParserPos pos, SqlIdentifier schema) {
```

### FieldMayBeFinal
Field `identifiers` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCompoundIdentifier.java`
#### Snippet
```java

  public static class Builder {
    private List<IdentifierHolder> identifiers = Lists.newArrayList();

    public DrillCompoundIdentifier build() {
```

### FieldMayBeFinal
Field `tblName` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlRefreshMetadata.java`
#### Snippet
```java
  };

  private SqlIdentifier tblName;
  private final SqlLiteral allColumns;
  private final SqlNodeList fieldList;
```

### FieldMayBeFinal
Field `fieldList` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlCreateView.java`
#### Snippet
```java

  private SqlIdentifier viewName;
  private SqlNodeList fieldList;
  private SqlNode query;
  private SqlLiteral createType;
```

### FieldMayBeFinal
Field `viewName` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlCreateView.java`
#### Snippet
```java
  };

  private SqlIdentifier viewName;
  private SqlNodeList fieldList;
  private SqlNode query;
```

### FieldMayBeFinal
Field `createType` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlCreateView.java`
#### Snippet
```java
  private SqlNodeList fieldList;
  private SqlNode query;
  private SqlLiteral createType;

  public SqlCreateView(SqlParserPos pos, SqlIdentifier viewName, SqlNodeList fieldList,
```

### FieldMayBeFinal
Field `query` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlCreateView.java`
#### Snippet
```java
  private SqlIdentifier viewName;
  private SqlNodeList fieldList;
  private SqlNode query;
  private SqlLiteral createType;

```

### FieldMayBeFinal
Field `dirExplorers` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
  private static List<String> disabledType = Lists.newArrayList();
  private static List<String> disabledOperators = Lists.newArrayList();
  private static List<String> dirExplorers = Lists.newArrayList();

  static {
```

### FieldMayBeFinal
Field `disabledOperators` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
  private QueryContext context;
  private static List<String> disabledType = Lists.newArrayList();
  private static List<String> disabledOperators = Lists.newArrayList();
  private static List<String> dirExplorers = Lists.newArrayList();

```

### FieldMayBeFinal
Field `disabledType` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
public class UnsupportedOperatorsVisitor extends SqlShuttle {
  private QueryContext context;
  private static List<String> disabledType = Lists.newArrayList();
  private static List<String> disabledOperators = Lists.newArrayList();
  private static List<String> dirExplorers = Lists.newArrayList();
```

### FieldMayBeFinal
Field `context` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java

public class UnsupportedOperatorsVisitor extends SqlShuttle {
  private QueryContext context;
  private static List<String> disabledType = Lists.newArrayList();
  private static List<String> disabledOperators = Lists.newArrayList();
```

### FieldMayBeFinal
Field `unsupportedOperatorCollector` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
  }

  private UnsupportedOperatorCollector unsupportedOperatorCollector;

  private UnsupportedOperatorsVisitor(QueryContext context) {
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DropTableHandler.java`
#### Snippet
```java
public class DropTableHandler extends DefaultSqlHandler {

  private static org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DropTableHandler.class);

  public DropTableHandler(SqlHandlerConfig config) {
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DropFunctionHandler.java`
#### Snippet
```java

public class DropFunctionHandler extends DefaultSqlHandler {
  private static Logger logger = LoggerFactory.getLogger(DropFunctionHandler.class);

  public DropFunctionHandler(SqlHandlerConfig config) {
```

### FieldMayBeFinal
Field `logger` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateFunctionHandler.java`
#### Snippet
```java

public class CreateFunctionHandler extends DefaultSqlHandler {
  private static Logger logger = LoggerFactory.getLogger(CreateFunctionHandler.class);

  public CreateFunctionHandler(SqlHandlerConfig config) {
```

### FieldMayBeFinal
Field `scannedPluginCache` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SqlHandlerConfig.java`
#### Snippet
```java
  private final QueryContext context;
  private final SqlConverter converter;
  private Map<RelNode, Collection<StoragePlugin>> scannedPluginCache = new HashMap<>();

  public SqlHandlerConfig(QueryContext context, SqlConverter converter) {
```

### FieldMayBeFinal
Field `inputRowType` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java

  // input rel's rowtype
  private RelDataType inputRowType;

  private int currentFieldIndex;
```

### FieldMayBeFinal
Field `allowedComparisons` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java

  // set of comparison operators that allow collation
  private Set<SqlKind> allowedComparisons = Sets.newHashSet();

  // input rel's rowtype
```

### FieldMayBeFinal
Field `collationFilterMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java
  // map of field collation (e.g [0]) to the list of filter conditions
  // involving the same field
  private Map<Integer, List<RexNode> > collationFilterMap = Maps.newHashMap();

  // set of comparison operators that allow collation
```

### FieldMayBeFinal
Field `condition` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
    final RelNode scan;
    final Iterable<IndexDescriptor> indexes;
    private RexNode condition;

    public Builder(RexNode condition,
```

### FieldMayBeFinal
Field `indexProps` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexGroup.java`
#### Snippet
```java
 */
public class IndexGroup {
  private List<IndexProperties> indexProps;

  public IndexGroup() {
```

### FieldMayBeFinal
Field `scanRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java

  private AbstractDbGroupScan scan; // group scan corresponding to the primary table
  private RelNode scanRel;   // physical scan rel corresponding to the primary table

  public IndexDiscoverBase(AbstractDbGroupScan inScan, DrillScanRelBase inScanPrel) {
```

### FieldMayBeFinal
Field `scan` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverBase.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(IndexDiscoverBase.class);

  private AbstractDbGroupScan scan; // group scan corresponding to the primary table
  private RelNode scanRel;   // physical scan rel corresponding to the primary table

```

### FieldMayBeFinal
Field `destExprMap` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java

  private RexBuilder builder;
  private Map<LogicalExpression, LogicalExpression> destExprMap;

  public SimpleRexRemap(RelNode origRel,
```

### FieldMayBeFinal
Field `builder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
  final RelDataType newRowType;

  private RexBuilder builder;
  private Map<LogicalExpression, LogicalExpression> destExprMap;

```

### FieldMayBeFinal
Field `numProjectedFields` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private double avgRowSize;          // avg row size in bytes of the selected part of index

    private int numProjectedFields;
    private double totalRows;
    private DrillScanRelBase primaryTableScan = null;
```

### FieldMayBeFinal
Field `indexContext` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private List<IndexProperties> indexPropList;
  private DrillScanRelBase primaryTableScan;
  private IndexCallContext indexContext;
  private RexBuilder rexBuilder;

```

### FieldMayBeFinal
Field `rexBuilder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private RexNode indexColumnsRemainderFilter = null;
    private RexNode otherColumnsRemainderFilter = null;
    private RexBuilder rexBuilder;

    public DrillIndexProperties(IndexDescriptor indexDescriptor,
```

### FieldMayBeFinal
Field `primaryTableScan` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private IndexConditionInfo.Builder builder;
  private List<IndexProperties> indexPropList;
  private DrillScanRelBase primaryTableScan;
  private IndexCallContext indexContext;
  private RexBuilder rexBuilder;
```

### FieldMayBeFinal
Field `totalRows` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java

    private int numProjectedFields;
    private double totalRows;
    private DrillScanRelBase primaryTableScan = null;
    private RelOptCost selfCost = null;
```

### FieldMayBeFinal
Field `builder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java

    private RelOptPlanner planner;
    private IndexConditionInfo.Builder builder;
    private PlannerSettings settings;

```

### FieldMayBeFinal
Field `rexBuilder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private DrillScanRelBase primaryTableScan;
  private IndexCallContext indexContext;
  private RexBuilder rexBuilder;

  public IndexSelector(RexNode indexCondition,
```

### FieldMayBeFinal
Field `builder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private double totalRows;
  private Statistics stats;         // a Statistics instance that will be used to get estimated rowcount for filter conditions
  private IndexConditionInfo.Builder builder;
  private List<IndexProperties> indexPropList;
  private DrillScanRelBase primaryTableScan;
```

### FieldMayBeFinal
Field `stats` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private RexNode otherRemainderCondition;  // remainder condition on all other columns
  private double totalRows;
  private Statistics stats;         // a Statistics instance that will be used to get estimated rowcount for filter conditions
  private IndexConditionInfo.Builder builder;
  private List<IndexProperties> indexPropList;
```

### FieldMayBeFinal
Field `otherRemainderCondition` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private static final double COVERING_TO_NONCOVERING_FACTOR = 100.0;
  private RexNode indexCondition;   // filter condition on indexed columns
  private RexNode otherRemainderCondition;  // remainder condition on all other columns
  private double totalRows;
  private Statistics stats;         // a Statistics instance that will be used to get estimated rowcount for filter conditions
```

### FieldMayBeFinal
Field `totalRows` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private RexNode indexCondition;   // filter condition on indexed columns
  private RexNode otherRemainderCondition;  // remainder condition on all other columns
  private double totalRows;
  private Statistics stats;         // a Statistics instance that will be used to get estimated rowcount for filter conditions
  private IndexConditionInfo.Builder builder;
```

### FieldMayBeFinal
Field `indexDescriptor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
   */
  public static class DrillIndexProperties  implements IndexProperties {
    private IndexDescriptor indexDescriptor; // index descriptor

    private double leadingSel = 1.0;    // selectivity of leading satisfiable conjunct
```

### FieldMayBeFinal
Field `indexCondition` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(IndexSelector.class);
  private static final double COVERING_TO_NONCOVERING_FACTOR = 100.0;
  private RexNode indexCondition;   // filter condition on indexed columns
  private RexNode otherRemainderCondition;  // remainder condition on all other columns
  private double totalRows;
```

### FieldMayBeFinal
Field `settings` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private RelOptPlanner planner;
    private IndexConditionInfo.Builder builder;
    private PlannerSettings settings;

    public IndexComparator(RelOptPlanner planner, IndexConditionInfo.Builder builder) {
```

### FieldMayBeFinal
Field `planner` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  public static class IndexComparator implements Comparator<IndexGroup> {

    private RelOptPlanner planner;
    private IndexConditionInfo.Builder builder;
    private PlannerSettings settings;
```

### FieldMayBeFinal
Field `indexPropList` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
  private Statistics stats;         // a Statistics instance that will be used to get estimated rowcount for filter conditions
  private IndexConditionInfo.Builder builder;
  private List<IndexProperties> indexPropList;
  private DrillScanRelBase primaryTableScan;
  private IndexCallContext indexContext;
```

### FieldMayBeFinal
Field `rowKeyLoc` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private RelOptRuleCall call;
  // Row key present on which side of the join
  private RowKey rowKeyLoc;
  // 0-based index of the row-key column in the join input
  private int rowKeyPos;
```

### FieldMayBeFinal
Field `swapInputs` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private int rowKeyPos;
  // swapping of row-key join inputs necessary
  private boolean swapInputs;
  private DrillJoin joinRel;
  // rels on the rowkey side of the join to be transformed
```

### FieldMayBeFinal
Field `lowerProjectRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private DrillProjectRel upperProjectRel;
  private DrillFilterRel filterRel;
  private DrillProjectRel lowerProjectRel;
  private DrillScanRel scanRel;

```

### FieldMayBeFinal
Field `joinRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  // swapping of row-key join inputs necessary
  private boolean swapInputs;
  private DrillJoin joinRel;
  // rels on the rowkey side of the join to be transformed
  private DrillProjectRel upperProjectRel;
```

### FieldMayBeFinal
Field `call` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  public enum RowKey {NONE, LEFT, RIGHT, BOTH};
  // RelOptRule call
  private RelOptRuleCall call;
  // Row key present on which side of the join
  private RowKey rowKeyLoc;
```

### FieldMayBeFinal
Field `filterRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  // rels on the rowkey side of the join to be transformed
  private DrillProjectRel upperProjectRel;
  private DrillFilterRel filterRel;
  private DrillProjectRel lowerProjectRel;
  private DrillScanRel scanRel;
```

### FieldMayBeFinal
Field `scanRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private DrillFilterRel filterRel;
  private DrillProjectRel lowerProjectRel;
  private DrillScanRel scanRel;

  public RowKeyJoinCallContext (RelOptRuleCall call, RowKey rowKeyLoc, int rowKeyPos, boolean swapInputs,
```

### FieldMayBeFinal
Field `upperProjectRel` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private DrillJoin joinRel;
  // rels on the rowkey side of the join to be transformed
  private DrillProjectRel upperProjectRel;
  private DrillFilterRel filterRel;
  private DrillProjectRel lowerProjectRel;
```

### FieldMayBeFinal
Field `rowKeyPos` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinCallContext.java`
#### Snippet
```java
  private RowKey rowKeyLoc;
  // 0-based index of the row-key column in the join input
  private int rowKeyPos;
  // swapping of row-key join inputs necessary
  private boolean swapInputs;
```

### FieldMayBeFinal
Field `tables` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillImplementor.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(DrillImplementor.class);

  private Set<DrillTable> tables = Sets.newHashSet();
  private Set<String> storageEngineNames = Sets.newHashSet();
  private LogicalPlanBuilder planBuilder = new LogicalPlanBuilder();
```

### FieldMayBeFinal
Field `storageEngineNames` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillImplementor.java`
#### Snippet
```java

  private Set<DrillTable> tables = Sets.newHashSet();
  private Set<String> storageEngineNames = Sets.newHashSet();
  private LogicalPlanBuilder planBuilder = new LogicalPlanBuilder();
  private LogicalPlan plan;
```

### FieldMayBeFinal
Field `scanFields` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ScanFieldDeterminer.java`
#### Snippet
```java
public class ScanFieldDeterminer extends AbstractLogicalVisitor<Void, ScanFieldDeterminer.FieldList, RuntimeException> {

  private Map<Scan, FieldList> scanFields = Maps.newHashMap();

  public static Map<Scan, FieldList> getFieldLists(LogicalPlan plan){
```

### FieldMayBeFinal
Field `projected` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ScanFieldDeterminer.java`
#### Snippet
```java

  public static class FieldList {
    private Set<SchemaPath> projected = Sets.newHashSet();
    private Set<SchemaPath> referenced = Sets.newHashSet();

```

### FieldMayBeFinal
Field `referenced` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ScanFieldDeterminer.java`
#### Snippet
```java
  public static class FieldList {
    private Set<SchemaPath> projected = Sets.newHashSet();
    private Set<SchemaPath> referenced = Sets.newHashSet();

    public void addProjected(SchemaPath path) {
```

### FieldMayBeFinal
Field `children` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/FindPartitionConditions.java`
#### Snippet
```java
  public class OpState {
    private SqlOperator sqlOperator;
    private List<RexNode> children = Lists.newArrayList();
    public OpState(SqlOperator op) {
      sqlOperator = op;
```

### FieldMayBeFinal
Field `sqlOperator` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/FindPartitionConditions.java`
#### Snippet
```java
   */
  public class OpState {
    private SqlOperator sqlOperator;
    private List<RexNode> children = Lists.newArrayList();
    public OpState(SqlOperator op) {
```

### FieldMayBeFinal
Field `fragmentXchgTo` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Fragment.java`
#### Snippet
```java
  public class ExchangeFragmentPair {
    private Exchange exchange;
    private Fragment fragmentXchgTo;

    public ExchangeFragmentPair(Exchange exchange, Fragment fragXchgTo) {
```

### FieldMayBeFinal
Field `exchange` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Fragment.java`
#### Snippet
```java

  public class ExchangeFragmentPair {
    private Exchange exchange;
    private Fragment fragmentXchgTo;

```

### FieldMayBeFinal
Field `EOL` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/HardAffinityFragmentParallelizer.java`
#### Snippet
```java
  public static final HardAffinityFragmentParallelizer INSTANCE = new HardAffinityFragmentParallelizer();

  private static String EOL = System.getProperty("line.separator");

  private HardAffinityFragmentParallelizer() { /* singleton */}
```

### FieldMayBeFinal
Field `enableDynamicFC` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
public class SplittingParallelizer extends DefaultQueryParallelizer {

  private boolean enableDynamicFC;
  private static final Logger logger = LoggerFactory.getLogger(SplittingParallelizer.class);

```

### FieldMayBeFinal
Field `queueSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/ProducerConsumerPrel.java`
#### Snippet
```java
public class ProducerConsumerPrel extends SingleRel implements DrillRelNode, Prel {

  private int queueSize;

  public ProducerConsumerPrel(RelNode child, int queueSize) {
```

### FieldMayBeFinal
Field `enableDynamicFC` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java

  protected static class CountRequiredFragments extends AbstractPhysicalVisitor<Void, List<Collector>, RuntimeException> {
    private boolean enableDynamicFC;

    CountRequiredFragments(boolean enableDynamicFC) {
```

### FieldMayBeFinal
Field `identifier` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/RuntimeFilterPrel.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RuntimeFilterPrel.class);

  private long identifier;

  public RuntimeFilterPrel(Prel child, long identifier){
```

### FieldMayBeFinal
Field `functions` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/StatsAggPrel.java`
#### Snippet
```java
public class StatsAggPrel extends SingleRel implements DrillRelNode, Prel {

  private List<String> functions;

  public StatsAggPrel(RelOptCluster cluster, RelTraitSet traits, RelNode child, List<String> functions) {
```

### FieldMayBeFinal
Field `joinControl` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashJoinPrel.java`
#### Snippet
```java
  private RuntimeFilterDef runtimeFilterDef;
  protected boolean isRowKeyJoin = false;
  private int joinControl;

  public HashJoinPrel(RelOptCluster cluster, RelTraitSet traits, RelNode left, RelNode right, RexNode condition,
```

### FieldMayBeFinal
Field `PHASE_2_FUNCTIONS` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/AnalyzePrule.java`
#### Snippet
```java
  // Mapping between output functions (from StatsMergeBatch) and
  // input functions (from StatsAggBatch)
  private static Map<String, String> PHASE_2_FUNCTIONS = new HashMap<>();
  static {
    PHASE_2_FUNCTIONS.put(Statistic.ROWCOUNT, Statistic.ROWCOUNT);
```

### FieldMayBeFinal
Field `functions` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/StatsMergePrel.java`
#### Snippet
```java
public class StatsMergePrel extends SingleRel implements DrillRelNode, Prel {

  private Map<String, String> functions;
  // Percent will be used to extrapolate statistics
  private double samplePercent;
```

### FieldMayBeFinal
Field `samplePercent` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/StatsMergePrel.java`
#### Snippet
```java
  private Map<String, String> functions;
  // Percent will be used to extrapolate statistics
  private double samplePercent;

  public StatsMergePrel(RelOptCluster cluster, RelTraitSet traits, RelNode child,
```

### FieldMayBeFinal
Field `mapFieldsNames` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/UnpivotMapsPrel.java`
#### Snippet
```java
public class UnpivotMapsPrel extends SingleRel implements Prel, DrillRelNode {

  private List<String> mapFieldsNames;

  public UnpivotMapsPrel(RelOptCluster cluster, RelTraitSet traits, RelNode child, List<String> mapFieldsNames) {
```

### FieldMayBeFinal
Field `frags` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/PrelSequencer.java`
#### Snippet
```java
public class PrelSequencer extends BasePrelVisitor<Void, PrelSequencer.Frag, RuntimeException> {

  private List<Frag> frags = Lists.newLinkedList();

  public static String printWithIds(final Prel rel, SqlExplainLevel explainlevel) {
```

### FieldMayBeFinal
Field `queueSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/ProducerConsumerPrelVisitor.java`
#### Snippet
```java
public class ProducerConsumerPrelVisitor extends BasePrelVisitor<Prel, Void, RuntimeException>{

  private int queueSize;

  public static Prel addProducerConsumerToScans(Prel prel, int queueSize){
```

### FieldMayBeFinal
Field `INSTANCE` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/SwapHashJoinVisitor.java`
#### Snippet
```java
public class SwapHashJoinVisitor extends BasePrelVisitor<Prel, Double, RuntimeException> {

  private static SwapHashJoinVisitor INSTANCE = new SwapHashJoinVisitor();

  public static Prel swapHashJoin(Prel prel, Double marginFactor){
```

### FieldMayBeFinal
Field `INSTANCE` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/AdjustOperatorsSchemaVisitor.java`
#### Snippet
```java
  private Prel registeredPrel = null;

  private static AdjustOperatorsSchemaVisitor INSTANCE = new AdjustOperatorsSchemaVisitor();

  public static Prel adjustSchema(Prel prel){
```

### FieldMayBeFinal
Field `intervalQualifier` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    private Integer precision;
    private Integer scale;
    private SqlIntervalQualifier intervalQualifier;
    private Field keyType;
    private Field valueType;
```

### FieldMayBeFinal
Field `hasExactColumnValueCount` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/ScanStats.java`
#### Snippet
```java
    ESTIMATED_TOTAL_COST(false, true);

    private boolean hasExactRowCount, hasExactColumnValueCount;

    GroupScanProperty (boolean hasExactRowCount, boolean hasExactColumnValueCount) {
```

### FieldMayBeFinal
Field `hasExactRowCount` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/ScanStats.java`
#### Snippet
```java
    ESTIMATED_TOTAL_COST(false, true);

    private boolean hasExactRowCount, hasExactColumnValueCount;

    GroupScanProperty (boolean hasExactRowCount, boolean hasExactColumnValueCount) {
```

### FieldMayBeFinal
Field `instanceRegistry` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OperatorCreatorRegistry.java`
#### Snippet
```java

  private volatile Map<Class<?>, Constructor<?>> constructorRegistry = new HashMap<Class<?>, Constructor<?>>();
  private volatile Map<Class<?>, Object> instanceRegistry = new HashMap<Class<?>, Object>();

  public OperatorCreatorRegistry(ScanResult scanResult) {
```

### FieldMayBeFinal
Field `constructorRegistry` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/OperatorCreatorRegistry.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(OperatorCreatorRegistry.class);

  private volatile Map<Class<?>, Constructor<?>> constructorRegistry = new HashMap<Class<?>, Constructor<?>>();
  private volatile Map<Class<?>, Object> instanceRegistry = new HashMap<Class<?>, Object>();

```

### FieldMayBeFinal
Field `safetyFactor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/BatchSizePredictorImpl.java`
#### Snippet
```java
  private RecordBatch batch;
  private double fragmentationFactor;
  private double safetyFactor;

  private long batchSize;
```

### FieldMayBeFinal
Field `batch` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/BatchSizePredictorImpl.java`
#### Snippet
```java

public class BatchSizePredictorImpl implements BatchSizePredictor {
  private RecordBatch batch;
  private double fragmentationFactor;
  private double safetyFactor;
```

### FieldMayBeFinal
Field `fragmentationFactor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/BatchSizePredictorImpl.java`
#### Snippet
```java
public class BatchSizePredictorImpl implements BatchSizePredictor {
  private RecordBatch batch;
  private double fragmentationFactor;
  private double safetyFactor;

```

### FieldMayBeFinal
Field `recordCount` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinHelper.java`
#### Snippet
```java

    // number of records in this batch
    private int recordCount;

    public BuildInfo(SelectionVector4 links, BitSet keyMatchBitVector, int recordCount) {
```

### FieldMayBeFinal
Field `links` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinHelper.java`
#### Snippet
```java
  public class BuildInfo {
    // List of links. Logically it helps maintain a linked list of records with the same key value
    private SelectionVector4 links;

    // List of bitvectors. Keeps track of records on the build side that matched a record on the probe side
```

### FieldMayBeFinal
Field `keyMatchBitVector` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinHelper.java`
#### Snippet
```java

    // List of bitvectors. Keeps track of records on the build side that matched a record on the probe side
    private BitSet keyMatchBitVector;

    // number of records in this batch
```

### FieldMayBeFinal
Field `batchSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculator.java`
#### Snippet
```java
  class BatchStat {
    private int numRecords;
    private long batchSize;

    public BatchStat(int numRecords, long batchSize) {
```

### FieldMayBeFinal
Field `numRecords` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculator.java`
#### Snippet
```java

  class BatchStat {
    private int numRecords;
    private long batchSize;

```

### FieldMayBeFinal
Field `fos` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/trace/TraceRecordBatch.java`
#### Snippet
```java

  /* File descriptors needed to be able to dump to log file */
  private OutputStream fos;

  public TraceRecordBatch(Trace pop, RecordBatch incoming, FragmentContext context) throws ExecutionSetupException {
```

### FieldMayBeFinal
Field `fs` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java

    private final byte buffer[];
    private FileSystem fs;

    protected HadoopFileManager(String fsName) {
```

### FieldMayBeFinal
Field `fileManager` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
  private int fileCount = 0;

  private FileManager fileManager;

  private long readBytes;
```

### FieldMayBeFinal
Field `out` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
  public static class CountingOutputStream extends OutputStream {

    private OutputStream out;
    private long count;

```

### FieldMayBeFinal
Field `currSpillDirs` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
   */

  private Set<String> currSpillDirs = Sets.newTreeSet();

  /**
```

### FieldMayBeFinal
Field `baseDir` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
  private static class LocalFileManager implements FileManager {

    private File baseDir;

    public LocalFileManager(String fsName) {
```

### FieldMayBeFinal
Field `in` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
  public static class CountingInputStream extends InputStream
  {
    private InputStream in;
    private long count;

```

### FieldMayBeFinal
Field `stats` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMetrics.java`
#### Snippet
```java

  private long minimumBufferSpace;
  private OperatorStats stats;
  private int spillCount;
  private int mergeCount;
```

### FieldMayBeFinal
Field `config` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
  private int mergeBatchRowCount;

  private SortConfig config;

  private boolean potentialOverflow;
```

### FieldMayBeFinal
Field `preferredSpillBatchSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
   * The configured size for each spill batch.
   */
  private int preferredSpillBatchSize;

  /**
```

### FieldMayBeFinal
Field `preferredMergeBatchSize` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SortMemoryManager.java`
#### Snippet
```java
   */

  private int preferredMergeBatchSize;

  /**
```

### FieldMayBeFinal
Field `queue` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/SpilledState.java`
#### Snippet
```java

  private int cycle = 0;
  private Queue<T> queue = new LinkedList<>();
  private boolean initialized = false;

```

### FieldMayBeFinal
Field `htConfig` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/ChainedHashTable.java`
#### Snippet
```java
          "outgoing" /* write container */, SETUP_INTERIOR_CONSTANT, OUTPUT_KEYS);

  private HashTableConfig htConfig;
  private final FragmentContext context;
  private final BufferAllocator allocator;
```

### FieldMayBeFinal
Field `child` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/ReturnValueExpression.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(ReturnValueExpression.class);

  private LogicalExpression child;
  private boolean returnTrueOnOne;

```

### FieldMayBeFinal
Field `returnTrueOnOne` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/filter/ReturnValueExpression.java`
#### Snippet
```java

  private LogicalExpression child;
  private boolean returnTrueOnOne;

  public ReturnValueExpression(LogicalExpression child) {
```

### FieldMayBeFinal
Field `htContainer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java

    // Container of vectors to hold type-specific keys
    private VectorContainer htContainer;

    // Array of 'link' values
```

### FieldMayBeFinal
Field `fixedDataWidth` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/OutputWidthExpression.java`
#### Snippet
```java
         * metadata width in the calculations.
         */
        private int fixedDataWidth;

        public FixedLenExpr(int fixedWidth) {
```

### FieldMayBeFinal
Field `currentVectors` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/protocol/SchemaTracker.java`
#### Snippet
```java
  private int schemaVersion;
  private BatchSchema currentSchema;
  private List<ValueVector> currentVectors = new ArrayList<>();

  public void trackSchema(VectorContainer newBatch) {
```

### FieldMayBeFinal
Field `pairs` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/svremover/StraightCopier.java`
#### Snippet
```java

public class StraightCopier implements Copier {
    private List<TransferPair> pairs = Lists.newArrayList();
    private RecordBatch incoming;
    private VectorContainer outputContainer;
```

### FieldMayBeFinal
Field `incoming` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/svremover/StraightCopier.java`
#### Snippet
```java
public class StraightCopier implements Copier {
    private List<TransferPair> pairs = Lists.newArrayList();
    private RecordBatch incoming;
    private VectorContainer outputContainer;
    private SchemaChangeCallBack callBack;
```

### FieldMayBeFinal
Field `callBack` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/svremover/StraightCopier.java`
#### Snippet
```java
    private RecordBatch incoming;
    private VectorContainer outputContainer;
    private SchemaChangeCallBack callBack;

    public StraightCopier(RecordBatch incomingBatch, VectorContainer outputContainer, SchemaChangeCallBack callBack) {
```

### FieldMayBeFinal
Field `outputContainer` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/svremover/StraightCopier.java`
#### Snippet
```java
    private List<TransferPair> pairs = Lists.newArrayList();
    private RecordBatch incoming;
    private VectorContainer outputContainer;
    private SchemaChangeCallBack callBack;

```

### FieldMayBeFinal
Field `instance` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/MergedStatisticFactory.java`
#### Snippet
```java
   * Creates the appropriate statistics object given the name of the statistics and the input statistic
   */
  private static MergedStatisticFactory instance = new MergedStatisticFactory();
  //Can not instantiate
  private MergedStatisticFactory() {
```

### FieldMayBeFinal
Field `statsClasses` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/MergedStatisticFactory.java`
#### Snippet
```java

public class MergedStatisticFactory {
  private HashMap<String,Class<? extends MergedStatistic>> statsClasses = new HashMap<>( );
  /*
   * Creates the appropriate statistics object given the name of the statistics and the input statistic
```

### FieldMayBeFinal
Field `tdigestHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/TDigestMergedStatistic.java`
#### Snippet
```java

public class TDigestMergedStatistic extends AbstractMergedStatistic {
  private Map<String, MergingDigest> tdigestHolder;
  private int compression;

```

### FieldMayBeFinal
Field `sumHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/NNRowCountMergedStatistic.java`
#### Snippet
```java
public class NNRowCountMergedStatistic extends AbstractMergedStatistic {

  private Map<String, Long> sumHolder;

  public NNRowCountMergedStatistic() {
```

### FieldMayBeFinal
Field `sumHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/RowCountMergedStatistic.java`
#### Snippet
```java
public class RowCountMergedStatistic extends AbstractMergedStatistic {

  private Map<String, Long> sumHolder;

  public RowCountMergedStatistic() {
```

### FieldMayBeFinal
Field `typeHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/ColTypeMergedStatistic.java`
#### Snippet
```java

public class ColTypeMergedStatistic extends AbstractMergedStatistic {
  private Map<String, byte[]> typeHolder;

  public ColTypeMergedStatistic () {
```

### FieldMayBeFinal
Field `hllHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/HLLMergedStatistic.java`
#### Snippet
```java

public class HLLMergedStatistic extends AbstractMergedStatistic {
  private Map<String, HyperLogLog> hllHolder;
  private long accuracy;

```

### FieldMayBeFinal
Field `sumHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/CntDupsMergedStatistic.java`
#### Snippet
```java

public class CntDupsMergedStatistic extends AbstractMergedStatistic {
    private Map<String, Long> sumHolder;

    public CntDupsMergedStatistic () {
```

### FieldMayBeFinal
Field `hllHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/NDVMergedStatistic.java`
#### Snippet
```java

public class NDVMergedStatistic extends AbstractMergedStatistic {
  private Map<String, HyperLogLog> hllHolder;
  ColTypeMergedStatistic types;
  NNRowCountMergedStatistic nonNullStatCounts;
```

### FieldMayBeFinal
Field `ok` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/broadcastsender/BroadcastSenderRootExec.java`
#### Snippet
```java
  private final AccountingDataTunnel[] tunnels;
  private final ExecProtos.FragmentHandle handle;
  private volatile boolean ok;
  private final RecordBatch incoming;

```

### FieldMayBeFinal
Field `sumHolder` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/AvgWidthMergedStatistic.java`
#### Snippet
```java

public class AvgWidthMergedStatistic extends AbstractMergedStatistic {
  private Map<String, Double> sumHolder;
  ColTypeMergedStatistic types;
  NNRowCountMergedStatistic nonNullStatCounts;
```

### FieldMayBeFinal
Field `finish` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/PartitionRange.java`
#### Snippet
```java

  private LogicalExpression start;
  private LogicalExpression finish;

  @JsonCreator
```

### FieldMayBeFinal
Field `start` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/PartitionRange.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(PartitionRange.class);

  private LogicalExpression start;
  private LogicalExpression finish;

```

### FieldMayBeFinal
Field `instance` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/BuildFromSchema.java`
#### Snippet
```java
  }

  private static BuildFromSchema instance = new BuildFromSchema();

  private BuildFromSchema() { }
```

### FieldMayBeFinal
Field `cacheFileRoot` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/ParquetTableMetadataProviderImpl.java`
#### Snippet
```java
  // may change when filter push down / partition pruning is applied
  private Path selectionRoot;
  private Path cacheFileRoot;
  private final boolean corruptDatesAutoCorrected;
  private boolean usedMetadataCache; // false by default
```

### FieldMayBeFinal
Field `segmentsInfo` may be 'final'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/FileMetadataInfoCollector.java`
#### Snippet
```java
  private List<MetadataInfo> rowGroupsInfo = Collections.emptyList();
  private List<MetadataInfo> filesInfo = Collections.emptyList();
  private Multimap<Integer, MetadataInfo> segmentsInfo = ArrayListMultimap.create();

  private TableScan tableScan;
```

### FieldMayBeFinal
Field `parquetMetadataConverter` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java
  private static final Logger LOG = LoggerFactory.getLogger(ParquetColumnChunkPageWriteStore.class);

  private static ParquetMetadataConverter parquetMetadataConverter = new ParquetMetadataConverter();

  private static final class ColumnChunkPageWriter implements PageWriter, BloomFilterWriter, Closeable {
```

### FieldMayBeFinal
Field `dataEncodings` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java
    private Set<Encoding> rlEncodings = new HashSet<Encoding>();
    private Set<Encoding> dlEncodings = new HashSet<Encoding>();
    private List<Encoding> dataEncodings = new ArrayList<Encoding>();

    private BloomFilter bloomFilter;
```

### FieldMayBeFinal
Field `dlEncodings` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java
    // repetition and definition level encodings are used only for v1 pages and don't change
    private Set<Encoding> rlEncodings = new HashSet<Encoding>();
    private Set<Encoding> dlEncodings = new HashSet<Encoding>();
    private List<Encoding> dataEncodings = new ArrayList<Encoding>();

```

### FieldMayBeFinal
Field `rlEncodings` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java

    // repetition and definition level encodings are used only for v1 pages and don't change
    private Set<Encoding> rlEncodings = new HashSet<Encoding>();
    private Set<Encoding> dlEncodings = new HashSet<Encoding>();
    private List<Encoding> dataEncodings = new ArrayList<Encoding>();
```

### FieldMayBeFinal
Field `rowCount` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private FileSystem fs;
  private Path path;
  private long rowCount;
  private List<FSDataInputStream> streams = new ArrayList<>();

```

### FieldMayBeFinal
Field `in` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java

    private DictionaryPage dictionaryPage;
    private FSDataInputStream in;
    private BytesInputDecompressor decompressor;

```

### FieldMayBeFinal
Field `streams` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private Path path;
  private long rowCount;
  private List<FSDataInputStream> streams = new ArrayList<>();

  public ColumnChunkIncReadStore(long rowCount, CompressionCodecFactory codecFactory, BufferAllocator allocator,
```

### FieldMayBeFinal
Field `path` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private BufferAllocator allocator;
  private FileSystem fs;
  private Path path;
  private long rowCount;
  private List<FSDataInputStream> streams = new ArrayList<>();
```

### FieldMayBeFinal
Field `fs` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private CompressionCodecFactory codecFactory;
  private BufferAllocator allocator;
  private FileSystem fs;
  private Path path;
  private long rowCount;
```

### FieldMayBeFinal
Field `decompressor` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
    private DictionaryPage dictionaryPage;
    private FSDataInputStream in;
    private BytesInputDecompressor decompressor;

    private ByteBuf lastPage;
```

### FieldMayBeFinal
Field `codecFactory` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private static ParquetMetadataConverter METADATA_CONVERTER = new ParquetMetadataConverter();

  private CompressionCodecFactory codecFactory;
  private BufferAllocator allocator;
  private FileSystem fs;
```

### FieldMayBeFinal
Field `columns` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  }

  private Map<ColumnDescriptor, ColumnChunkIncPageReader> columns = new HashMap<>();

  public void addColumn(ColumnDescriptor descriptor, ColumnChunkMetaData metaData) throws IOException {
```

### FieldMayBeFinal
Field `allocator` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java

  private CompressionCodecFactory codecFactory;
  private BufferAllocator allocator;
  private FileSystem fs;
  private Path path;
```

### FieldMayBeFinal
Field `METADATA_CONVERTER` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(ColumnChunkIncReadStore.class);

  private static ParquetMetadataConverter METADATA_CONVERTER = new ParquetMetadataConverter();

  private CompressionCodecFactory codecFactory;
```

### FieldMayBeFinal
Field `input` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/PacketDecoder.java`
#### Snippet
```java
  private FileFormat fileFormat;

  private InputStream input;

  public PacketDecoder(final InputStream input) throws IOException {
```

### FieldMayBeFinal
Field `rowWriter` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/PcapBatchReader.java`
#### Snippet
```java
  private PacketDecoder decoder;
  private InputStream fsStream;
  private RowSetLoader rowWriter;
  private int validBytes;
  private byte[] buffer;
```

### FieldMayBeFinal
Field `buf` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/Murmur128.java`
#### Snippet
```java
  private long h1;
  private long h2;
  private byte[] buf = new byte[8];

  public Murmur128(long h1, long h2) {
```

### FieldMayBeFinal
Field `blocks` may be 'final'
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java

    // file data
    private List<BlockMetaData> blocks = new ArrayList<BlockMetaData>();

    // The column/offset indexes per blocks per column chunks
```

### FieldMayBeFinal
Field `loader` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private Iterator<IPcapngType> pcapIterator;
  private IPcapngType block;
  private RowSetLoader loader;
  private InputStream in;
  private Path path;
```

### FieldMayBeFinal
Field `processor` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java

    private final String name;
    private PcapColumn processor;
    private ScalarWriter writer;

```

### FieldMayBeFinal
Field `path` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private RowSetLoader loader;
  private InputStream in;
  private Path path;

  public PcapngBatchReader(final PcapFormatConfig config, final EasySubScan scan,
```

### FieldMayBeFinal
Field `pcapIterator` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private List<SchemaPath> columns;
  private List<ColumnDefn> projectedColumns;
  private Iterator<IPcapngType> pcapIterator;
  private IPcapngType block;
  private RowSetLoader loader;
```

### FieldMayBeFinal
Field `columns` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java

  private CustomErrorContext errorContext;
  private List<SchemaPath> columns;
  private List<ColumnDefn> projectedColumns;
  private Iterator<IPcapngType> pcapIterator;
```

### FieldMayBeFinal
Field `in` may be 'final'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private IPcapngType block;
  private RowSetLoader loader;
  private InputStream in;
  private Path path;

```

### FieldMayBeFinal
Field `numRows` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableStats.java`
#### Snippet
```java
  private static volatile HBaseAdminImpl admin = null;

  private long numRows;

  public MapRDBTableStats(Configuration conf, String tablePath) throws Exception {
```

### FieldMayBeFinal
Field `watch` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
  private boolean filterPushedDown = false;

  private Stopwatch watch = Stopwatch.createUnstarted();

  private TableMetadataProvider metadataProvider;
```

### FieldMayBeFinal
Field `metadataProvider` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
  private Stopwatch watch = Stopwatch.createUnstarted();

  private TableMetadataProvider metadataProvider;

  private TableMetadata tableMetadata;
```

### FieldMayBeFinal
Field `JSON_BLOCK_SEQ_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_AVG_COLUMN_SIZE;
  private int JSON_TABLE_BLOCK_SIZE;  // bytes per block
  private int JSON_BLOCK_SEQ_READ_COST;
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
```

### FieldMayBeFinal
Field `JSON_HDD_BLOCK_RANDOM_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;
```

### FieldMayBeFinal
Field `JSON_SSD_BLOCK_RANDOM_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;

  public MapRDBCost(DrillConfig config, String mediaType) {
```

### FieldMayBeFinal
Field `JSON_SSD_BLOCK_SEQ_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;

```

### FieldMayBeFinal
Field `JSON_BLOCK_RANDOM_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_TABLE_BLOCK_SIZE;  // bytes per block
  private int JSON_BLOCK_SEQ_READ_COST;
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
```

### FieldMayBeFinal
Field `JSON_HDD_BLOCK_SEQ_READ_COST` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_BLOCK_SEQ_READ_COST;
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
```

### FieldMayBeFinal
Field `JSON_TABLE_BLOCK_SIZE` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java

  private int JSON_AVG_COLUMN_SIZE;
  private int JSON_TABLE_BLOCK_SIZE;  // bytes per block
  private int JSON_BLOCK_SEQ_READ_COST;
  private int JSON_BLOCK_RANDOM_READ_COST;
```

### FieldMayBeFinal
Field `JSON_AVG_COLUMN_SIZE` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
public class MapRDBCost implements PluginCost {

  private int JSON_AVG_COLUMN_SIZE;
  private int JSON_TABLE_BLOCK_SIZE;  // bytes per block
  private int JSON_BLOCK_SEQ_READ_COST;
```

### FieldMayBeFinal
Field `batchSize` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonRecordReader.java`
#### Snippet
```java
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(RestrictedJsonRecordReader.class);

  private int batchSize; // batchSize for rowKey based document get

  private String [] projections = null; // multiGet projections
```

### FieldMayBeFinal
Field `matcher` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/streams/StreamsFormatPlugin.java`
#### Snippet
```java
public class StreamsFormatPlugin extends TableFormatPlugin {
  private static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(StreamsFormatPlugin.class);
  private StreamsFormatMatcher matcher;

  public StreamsFormatPlugin(String name, DrillbitContext context, Configuration fsConf,
```

### FieldMayBeFinal
Field `allPathsInFunction` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java
  private Set<SchemaPath> newPathsForIndexedFunction;

  private Set<SchemaPath> allPathsInFunction;

  public MapRDBFunctionalIndexInfo(IndexDescriptor indexDesc) {
```

### FieldMayBeFinal
Field `newPathsForIndexedFunction` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java
  private Map<LogicalExpression, Set<SchemaPath>> pathsInExpr;

  private Set<SchemaPath> newPathsForIndexedFunction;

  private Set<SchemaPath> allPathsInFunction;
```

### FieldMayBeFinal
Field `pathsInExpr` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java

  // map of SchemaPath involved in a functional field
  private Map<LogicalExpression, Set<SchemaPath>> pathsInExpr;

  private Set<SchemaPath> newPathsForIndexedFunction;
```

### FieldMayBeFinal
Field `exprToConvert` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java

  // map of functional index expression to destination SchemaPath e.g. $N
  private Map<LogicalExpression, LogicalExpression> exprToConvert;

  // map of SchemaPath involved in a functional field
```

### FieldMayBeFinal
Field `columnToConvert` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBFunctionalIndexInfo.java`
#### Snippet
```java
  // When there are more than two functions on the same column in index, CAST(a.b as INT), CAST(a.b as VARCHAR),
  // then we should map SchemaPath a.b to a set of SchemaPath, e.g. $1, $2
  private Map<SchemaPath, Set<SchemaPath>> columnToConvert;

  // map of functional index expression to destination SchemaPath e.g. $N
```

### FieldMayBeFinal
Field `buffer` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
  protected OutputMutator vectorWriterMutator;

  private DrillBuf buffer;

  private DocumentStream documentStream;
```

### FieldMayBeFinal
Field `disablePushdown` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
  private final boolean readNumbersAsDouble;
  private final boolean readTimestampWithZoneOffset;
  private boolean disablePushdown;
  private final boolean allTextMode;
  private final boolean ignoreSchemaChange;
```

### FieldMayBeFinal
Field `conditionRexNodeMap` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
   * of a comparator.
   */
  private Map<String, String> conditionRexNodeMap;

  public MapRDBStatistics() {
```

### FieldMayBeFinal
Field `fIStatsCache` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
   * e.g. average row size.
   */
  private Map<String, StatisticsPayload> fIStatsCache;
  /*
  /*
```

### FieldMayBeFinal
Field `statsCache` may be 'final'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
   * e.g. average row size.
   */
  private Map<String, Map<String, StatisticsPayload>> statsCache;
  /*
   * The filter independent computed statistics are cached in <fIStatsCache> so that any subsequent
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaDefProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `showMetadataPreview` may be 'final'
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5BatchReader.java`
#### Snippet
```java
  private CustomErrorContext errorContext;

  private boolean showMetadataPreview;

  private int[] dimensions;
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `rowWriter` may be 'final'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
  private final FileDescrip file;
  private BufferedReader reader;
  private RowSetLoader rowWriter;
  private List<ScalarWriter> writerArray;
  private ScalarWriter rawColumnWriter;
```

### FieldMayBeFinal
Field `errorContext` may be 'final'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
  private int lineCount;
  private int errorCount;
  private CustomErrorContext errorContext;
  private InputStream fsStream;
  private final FileDescrip file;
```

### FieldMayBeFinal
Field `rawColumnWriter` may be 'final'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
  private RowSetLoader rowWriter;
  private List<ScalarWriter> writerArray;
  private ScalarWriter rawColumnWriter;
  private ScalarWriter messageWriter;
  private TupleWriter structuredDataWriter;
```

### FieldMayBeFinal
Field `writerArray` may be 'final'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
  private BufferedReader reader;
  private RowSetLoader rowWriter;
  private List<ScalarWriter> writerArray;
  private ScalarWriter rawColumnWriter;
  private ScalarWriter messageWriter;
```

### FieldMayBeFinal
Field `messageWriter` may be 'final'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
  private List<ScalarWriter> writerArray;
  private ScalarWriter rawColumnWriter;
  private ScalarWriter messageWriter;
  private TupleWriter structuredDataWriter;

```

### FieldMayBeFinal
Field `builder` may be 'final'
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
  private PDDocument document;

  private SchemaBuilder builder;
  private List<String> columnHeaders;
  private Table currentTable;
```

### FieldMayBeFinal
Field `metadataReader` may be 'final'
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
  private final int startingTableIndex;
  private final FileDescrip file;
  private PdfMetadataReader metadataReader;
  private CustomErrorContext errorContext;
  private RowSetLoader rowWriter;
```

### FieldMayBeFinal
Field `rowWriter` may be 'final'
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
  private PdfMetadataReader metadataReader;
  private CustomErrorContext errorContext;
  private RowSetLoader rowWriter;
  private PDDocument document;

```

### FieldMayBeFinal
Field `errorContext` may be 'final'
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
  private final FileDescrip file;
  private PdfMetadataReader metadataReader;
  private CustomErrorContext errorContext;
  private RowSetLoader rowWriter;
  private PDDocument document;
```

### FieldMayBeFinal
Field `receiveMap` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcConfig.java`
#### Snippet
```java
    private Executor executor;
    private Map<EnumLite, RpcMessageType<?, ?, ?>> sendMap = Maps.newHashMap();
    private Map<Integer, RpcMessageType<?, ?, ?>> receiveMap = Maps.newHashMap();

    private RpcConfigBuilder() {
```

### FieldMayBeFinal
Field `sendMap` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcConfig.java`
#### Snippet
```java
    private int timeout = -1;
    private Executor executor;
    private Map<EnumLite, RpcMessageType<?, ?, ?>> sendMap = Maps.newHashMap();
    private Map<Integer, RpcMessageType<?, ?, ?>> receiveMap = Maps.newHashMap();

```

### FieldMayBeFinal
Field `outOfMemoryHandler` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ProtobufLengthDecoder.java`
#### Snippet
```java

  private BufferAllocator allocator;
  private OutOfMemoryHandler outOfMemoryHandler;

  public ProtobufLengthDecoder(BufferAllocator allocator, OutOfMemoryHandler outOfMemoryHandler) {
```

### FieldMayBeFinal
Field `allocator` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ProtobufLengthDecoder.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(ProtobufLengthDecoder.class);

  private BufferAllocator allocator;
  private OutOfMemoryHandler outOfMemoryHandler;

```

### FieldMayBeFinal
Field `cmd` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
      implements RpcConnectionHandler<C> {

    private RpcCommand<R, C, E, M> cmd;

    public ConnectionListeningFuture(RpcCommand<R, C, E, M> cmd) {
```

### FieldMayBeFinal
Field `parent` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
  protected class CloseHandler implements GenericFutureListener<ChannelFuture> {
    private C connection;
    private GenericFutureListener<ChannelFuture> parent;

    public CloseHandler(C connection, GenericFutureListener<ChannelFuture> parent) {
```

### FieldMayBeFinal
Field `connection` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
   */
  protected class CloseHandler implements GenericFutureListener<ChannelFuture> {
    private C connection;
    private GenericFutureListener<ChannelFuture> parent;

```

### FieldMayBeFinal
Field `origMsgBuffer` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/SaslEncryptionHandler.java`
#### Snippet
```java
  private final int wrapSizeLimit;

  private byte[] origMsgBuffer;

  private final ByteBuffer lengthOctets;
```

### FieldMayBeFinal
Field `heartBeatHandler` may be 'final'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicClient.java`
#### Snippet
```java
  private final Parser<HR> handshakeParser;

  private HeartBeatHandler heartBeatHandler;
  private ConnectionMultiListener.SSLHandshakeListener sslHandshakeListener = null;

```

### FieldMayBeFinal
Field `loader` may be 'final'
in `contrib/format-log/src/main/java/org/apache/drill/exec/store/log/LogBatchReader.java`
#### Snippet
```java
  private final FileDescrip file;
  private BufferedReader reader;
  private ResultSetLoader loader;
  private VectorWriter vectorWriter;
  private ScalarWriter rawColWriter;
```

### FieldMayBeFinal
Field `user` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoCnxnKey.java`
#### Snippet
```java

  private ServerAddress address;
  private String user;

  public MongoCnxnKey(ServerAddress address, String user) {
```

### FieldMayBeFinal
Field `address` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoCnxnKey.java`
#### Snippet
```java
public class MongoCnxnKey {

  private ServerAddress address;
  private String user;

```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `chunkLocList` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/common/ChunkInfo.java`
#### Snippet
```java
public class ChunkInfo {

  private List<String> chunkLocList;

  private String chunkId;
```

### FieldMayBeFinal
Field `chunkId` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/common/ChunkInfo.java`
#### Snippet
```java
  private List<String> chunkLocList;

  private String chunkId;

  private Map<String, Object> minFilters;
```

### FieldMayBeFinal
Field `result` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/config/MongoPersistentStore.java`
#### Snippet
```java
  private class DeferredEntry implements Entry<String, V> {

    private Document result;

    public DeferredEntry(Document result) {
```

### FieldMayBeFinal
Field `cursor` may be 'final'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/config/MongoPersistentStore.java`
#### Snippet
```java
  private class MongoIterator implements Iterator<Entry<String, V>> {

    private MongoCursor<Document> cursor;

    public MongoIterator(final MongoCursor<Document> cursor) {
```

### FieldMayBeFinal
Field `file` may be 'final'
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelBatchReader.java`
#### Snippet
```java
  private ScalarWriter sheetNameWriter;
  private Iterator<Row> rowIterator;
  private FileDescrip file;
  private int totalColumnCount;
  private boolean firstLine;
```

### FieldMayBeFinal
Field `path` may be 'final'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/metadata/RowGroupMetadata.java`
#### Snippet
```java
  private Map<String, Float> hostAffinity;
  private int rowGroupIndex;
  private Path path;

  private RowGroupMetadata(RowGroupMetadataBuilder builder) {
```

### FieldMayBeFinal
Field `rowGroupIndex` may be 'final'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/metadata/RowGroupMetadata.java`
#### Snippet
```java
public class RowGroupMetadata extends BaseMetadata implements LocationProvider {
  private Map<String, Float> hostAffinity;
  private int rowGroupIndex;
  private Path path;

```

### FieldMayBeFinal
Field `hostAffinity` may be 'final'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/metadata/RowGroupMetadata.java`
#### Snippet
```java
 */
public class RowGroupMetadata extends BaseMetadata implements LocationProvider {
  private Map<String, Float> hostAffinity;
  private int rowGroupIndex;
  private Path path;
```

### FieldMayBeFinal
Field `metadataTypes` may be 'final'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/components/tables/BasicTablesRequests.java`
#### Snippet
```java
  public static class RequestMetadata {

    private Set<MetadataType> metadataTypes;
    private final FilterExpression filter;
    private final List<MetastoreColumn> columns;
```

### FieldMayBeFinal
Field `metadataTypes` may be 'final'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/components/tables/BasicTablesRequests.java`
#### Snippet
```java
      private List<String> identifiers;
      private FilterExpression customFilter;
      private Set<MetadataType> metadataTypes = new HashSet<>();
      private final List<MetastoreColumn> requestColumns = new ArrayList<>();

```

### FieldMayBeFinal
Field `rowWriter` may be 'final'
in `contrib/format-spss/src/main/java/org/apache/drill/exec/store/spss/SpssBatchReader.java`
#### Snippet
```java
  private InputStream fsStream;
  private SpssDataFileReader spssReader;
  private RowSetLoader rowWriter;
  private List<SpssVariable> variableList;
  private List<SpssColumnWriter> writerList;
```

### FieldMayBeFinal
Field `errorContext` may be 'final'
in `contrib/format-spss/src/main/java/org/apache/drill/exec/store/spss/SpssBatchReader.java`
#### Snippet
```java
  private List<SpssVariable> variableList;
  private List<SpssColumnWriter> writerList;
  private CustomErrorContext errorContext;


```

### FieldMayBeFinal
Field `osMXBean` may be 'final'
in `common/src/main/java/org/apache/drill/exec/metrics/CpuGaugeSet.java`
#### Snippet
```java
@SuppressWarnings("restriction")
final class DrillbitProcessLoad implements Gauge<Double> {
  private OperatingSystemMXBean osMXBean;
  public DrillbitProcessLoad(OperatingSystemMXBean osBean) {
    this.osMXBean = osBean;
```

### FieldMayBeFinal
Field `osMXBean` may be 'final'
in `common/src/main/java/org/apache/drill/exec/metrics/CpuGaugeSet.java`
#### Snippet
```java
@SuppressWarnings("restriction")
final class OperatingSystemLoad implements Gauge<Double> {
  private OperatingSystemMXBean osMXBean;
  public OperatingSystemLoad(OperatingSystemMXBean osBean) {
    this.osMXBean = osBean;
```

### FieldMayBeFinal
Field `rtMXBean` may be 'final'
in `common/src/main/java/org/apache/drill/exec/metrics/CpuGaugeSet.java`
#### Snippet
```java
 */
final class DrillbitUptime implements Gauge<Long> {
  private RuntimeMXBean rtMXBean;
  public DrillbitUptime(RuntimeMXBean osBean) {
    this.rtMXBean = osBean;
```

### FieldMayBeFinal
Field `className` may be 'final'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/TypeDescriptor.java`
#### Snippet
```java

  private static final class ClassTypeDescriptor extends TypeDescriptor {
    private String className;

    private ClassTypeDescriptor(String className, int arrayDim) {
```

### FieldMayBeFinal
Field `params` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/OpenTSDBRecordReader.java`
#### Snippet
```java
  private ImmutableList<ProjectedColumnInfo> projectedCols;

  private Map<String, String> params;

  public OpenTSDBRecordReader(Service client, OpenTSDBSubScan.OpenTSDBSubScanSpec subScanSpec,
```

### FieldMayBeFinal
Field `db` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/OpenTSDBRecordReader.java`
#### Snippet
```java
  private static final Map<OpenTSDBTypes, MinorType> TYPES;

  private Service db;

  private Iterator<MetricDTO> tableIterator;
```

### FieldMayBeFinal
Field `columnName` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/Schema.java`
#### Snippet
```java
    AGGREGATED_VALUE("aggregated value");

    private String columnName;

    DefaultColumns(String name) {
```

### FieldMayBeFinal
Field `start` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/DBQuery.java`
#### Snippet
```java
   * The start time for the query. This can be a relative or absolute timestamp.
   */
  private String start;
  /**
   * An end time for the query. If not supplied, the TSD will assume the local system time on the server.
```

### FieldMayBeFinal
Field `queries` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/DBQuery.java`
#### Snippet
```java
   * One or more sub subQueries used to select the time series to return.
   */
  private Set<Query> queries;

  private DBQuery(Builder builder) {
```

### FieldMayBeFinal
Field `end` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/DBQuery.java`
#### Snippet
```java
   * to the db in this field, but in this case db will assume the local system time on the server.
   */
  private String end;
  /**
   * One or more sub subQueries used to select the time series to return.
```

### FieldMayBeFinal
Field `tags` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/Query.java`
#### Snippet
```java
   * supply one or more map values in the same format as the query string.
   */
  private Map<String, String> tags;

  private Query(Builder builder) {
```

### FieldMayBeFinal
Field `metric` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/Query.java`
#### Snippet
```java
   * The name of a metric stored in the system
   */
  private String metric;
  /**
   * Whether or not the data should be converted into deltas before returning.
```

### FieldMayBeFinal
Field `downsample` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/Query.java`
#### Snippet
```java
   * An optional downsampling function to reduce the amount of data returned.
   */
  private String downsample;
  /**
   * To drill down to specific timeseries or group results by tag,
```

### FieldMayBeFinal
Field `rate` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/Query.java`
#### Snippet
```java
   * and you want to view the rate of change between data points.
   */
  private String rate;
  /**
   * An optional downsampling function to reduce the amount of data returned.
```

### FieldMayBeFinal
Field `aggregator` may be 'final'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/query/Query.java`
#### Snippet
```java
   * The name of an aggregation function to use.
   */
  private String aggregator;
  /**
   * The name of a metric stored in the system
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

## RuleId[id=JavaReflectionInvocation]
### JavaReflectionInvocation
Array item is not assignable to 'Configuration'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
                  StoragePluginConfig.class, mapRDBFormatPluginConfigClass})
          .newInstance(
              new Object[]{HIVE_MAPRDB_FORMAT_PLUGIN_NAME, context, hiveConf, config, formatConfig});
      }
    } catch (ReflectiveOperationException e) {
```

### JavaReflectionInvocation
Array item is not assignable to 'org.apache.drill.exec.store.mapr.db.MapRDBFormatPluginConfig'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
                  StoragePluginConfig.class, mapRDBFormatPluginConfigClass})
          .newInstance(
              new Object[]{HIVE_MAPRDB_FORMAT_PLUGIN_NAME, context, hiveConf, config, formatConfig});
      }
    } catch (ReflectiveOperationException e) {
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/TableEntryCacheLoader.java`
#### Snippet
```java
    try {
      table = client.getTable(key.getDbName(), key.getTableName());
    } catch (MetaException | NoSuchObjectException e) {
      throw e;
    } catch (TException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/TableEntryCacheLoader.java`
#### Snippet
```java
    try {
      partitions = client.listPartitions(key.getDbName(), key.getTableName(), (short) -1);
    } catch (NoSuchObjectException | MetaException e) {
      throw e;
    } catch (TException e) {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/client/HiveAuthorizationHelper.java`
#### Snippet
```java

      authorizerV2.checkPrivileges(hiveOpType, toRead, toWrite, authzContextBuilder.build());
    } catch (final HiveAccessControlException e) {
      throw e;
    } catch (final Exception e) {
```

## RuleId[id=UnnecessaryUnicodeEscape]
### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a6` can be replaced with '¦'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/StarColumnHelper.java`
#### Snippet
```java
public class StarColumnHelper {

  public final static String PREFIX_DELIMITER = "\u00a6\u00a6";

  public final static String PREFIXED_STAR_COLUMN = PREFIX_DELIMITER + SchemaPath.DYNAMIC_STAR;
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00a6` can be replaced with '¦'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/StarColumnHelper.java`
#### Snippet
```java
public class StarColumnHelper {

  public final static String PREFIX_DELIMITER = "\u00a6\u00a6";

  public final static String PREFIXED_STAR_COLUMN = PREFIX_DELIMITER + SchemaPath.DYNAMIC_STAR;
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `controller`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
    public void listAnomalies(ClusterController controller) {
        listUnmanaged(controller);
        synchronized( controller ) {
          blacklist = ((ClusterControllerImpl) controller).getNodeInventory().getBlacklist();
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `collector`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/IncomingBuffers.java`
#### Snippet
```java
      BufferAllocator ownerAllocator = collector.getAllocator();

      synchronized (collector) {
        final RawFragmentBatch newRawFragmentBatch = incomingBatch.newRawFragmentBatch(ownerAllocator);
        boolean decrementedToZero = collector
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `map` is redundant
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/ComplexWriterImpl.java`
#### Snippet
```java

    case INIT:
      MapVector map = (MapVector) container;
      mapRoot = new SingleMapWriter(map, this, unionEnabled);
      mapRoot.setPosition(idx());
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      }
    }
    final String result = declaredTypePart + actualTypePart + actualValuePart;
    return result;
  }
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      current = current.getCause();
    }
    final String result = s.toString();
    return result;
  }
```

### UnnecessaryLocalVariable
Local variable `cpuCost` is redundant
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpGroupScan.java`
#### Snippet
```java
    double estColCount = Utilities.isStarQuery(columns) ? DrillScanRel.STAR_COLUMN_COST : columns.size();
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

```

### UnnecessaryLocalVariable
Local variable `ioCost` is redundant
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpGroupScan.java`
#### Snippet
```java
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

    // Force the caller to use our costs rather than the
```

### UnnecessaryLocalVariable
Local variable `store` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinator.java`
#### Snippet
```java
  public <V> TransientStore<V> getOrCreateTransientStore(
      final TransientStoreConfig<V> config) {
    final ZkEphemeralStore<V> store = (ZkEphemeralStore<V>) factory
        .getOrCreateStore(config);
    return store;
```

### UnnecessaryLocalVariable
Local variable `resource` is redundant
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DfsFacade.java`
#### Snippet
```java
          "Unable to convert dfs file to a URL: " + dfsPath.toString(), e);
    }
    LocalResource resource = LocalResource.newInstance(destUrl, type,
        visibility, dfsFileStatus.getLen(),
        dfsFileStatus.getModificationTime());
```

### UnnecessaryLocalVariable
Local variable `plan` is redundant
in `logical/src/main/java/org/apache/drill/common/logical/LogicalPlan.java`
#### Snippet
```java
    ObjectMapper mapper = config.getMapper();
    try {
      LogicalPlan plan = mapper.readValue(planString, LogicalPlan.class);
      return plan;
    } catch (IOException e) {
```

### UnnecessaryLocalVariable
Local variable `ga` is redundant
in `logical/src/main/java/org/apache/drill/common/logical/data/GroupingAggregate.java`
#### Snippet
```java
    @Override
    public GroupingAggregate internalBuild(){
      GroupingAggregate ga =  new GroupingAggregate(keys, exprs);
      return ga;
    }
```

### UnnecessaryLocalVariable
Local variable `fromMetrics` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java

  public OperatorStats mergeMetrics(OperatorStats from) {
    final IntLongHashMap fromMetrics = from.longMetrics;

    final Iterator<IntLongCursor> iter = fromMetrics.iterator();
```

### UnnecessaryLocalVariable
Local variable `fromDMetrics` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java
    }

    final IntDoubleHashMap fromDMetrics = from.doubleMetrics;
    final Iterator<IntDoubleCursor> iterD = fromDMetrics.iterator();

```

### UnnecessaryLocalVariable
Local variable `infos` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserRpcUtils.java`
#### Snippet
```java
   */
  public static RpcEndpointInfos getRpcEndpointInfos(String name) {
    RpcEndpointInfos infos = RpcEndpointInfos.newBuilder(INFOS_TEMPLATE)
        .setName(Preconditions.checkNotNull(name))
        .build();
```

### UnnecessaryLocalVariable
Local variable `topSub` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillComplexWriterFuncHolder.java`
#### Snippet
```java

    JBlock sub = new JBlock(true, true);
    JBlock topSub = sub;

    JVar rsLoader = null;
```

### UnnecessaryLocalVariable
Local variable `ifExpression` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/FunctionGenerationHelper.java`
#### Snippet
```java

    IfExpression.IfCondition ifCondition = new IfCondition(notEqual, call);
    IfExpression ifExpression = IfExpression.newBuilder().setIfCondition(ifCondition).setElse(comparisonFunction).build();
    return ifExpression;
  }
```

### UnnecessaryLocalVariable
Local variable `hc` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/EvaluationVisitor.java`
#### Snippet
```java
        }

        HoldingContainer hc = new HoldingContainer(type, complexReader, null, null, false, true);
        return hc;
      } else {
```

### UnnecessaryLocalVariable
Local variable `converted` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/HashHelper.java`
#### Snippet
```java
  }
  public static int hash32(float val, long seed){
    double converted = val;
    return hash32(converted, seed);
  }
```

### UnnecessaryLocalVariable
Local variable `converted` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/HashHelper.java`
#### Snippet
```java
  }
  public static long hash64(long val, long seed){
    double converted = val;
    return hash64(converted, seed);
  }
```

### UnnecessaryLocalVariable
Local variable `converted` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/HashHelper.java`
#### Snippet
```java
  }
  public static int hash32(long val, long seed) {
    double converted = val;
    return hash32(converted, seed);
  }
```

### UnnecessaryLocalVariable
Local variable `converted` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/HashHelper.java`
#### Snippet
```java

  public static int hash32(int val, long seed) {
    double converted = val;
    return hash32(converted, seed);
  }
```

### UnnecessaryLocalVariable
Local variable `converted` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/HashHelper.java`
#### Snippet
```java

  public static long hash64(float val, long seed){
    double converted = val;
    return hash64(converted, seed);
  }
```

### UnnecessaryLocalVariable
Local variable `s` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctionHelpers.java`
#### Snippet
```java
    byte[] buf = new byte[end - start];
    buffer.getBytes(start, buf, 0, end - start);
    String s = new String(buf, Charsets.UTF_8);
    return s;
  }
```

### UnnecessaryLocalVariable
Local variable `outputSize` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/output/OutputWidthCalculators.java`
#### Snippet
```java
        public int getOutputWidth(List<FixedLenExpr> args) {
            //TODO: Read value from options?
            int outputSize = adjustOutputWidth(50, "DefaultOutputWidthCalculator:");
            return outputSize;
        }
```

### UnnecessaryLocalVariable
Local variable `bestMatch` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/ApproximateStringMatcher.java`
#### Snippet
```java
        }
        final int minIndex = editDistances.indexOf(Collections.min(editDistances));
        final String bestMatch = namesToSearch.get(minIndex);
        return bestMatch;
    }
```

### UnnecessaryLocalVariable
Local variable `qPlanFragments` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/UserWorker.java`
#### Snippet
```java
      GetQueryPlanFragments req) {
    final QueryId queryId = queryIdGenerator();
    final QueryPlanFragments qPlanFragments = new PlanSplitter().planFragments(bee.getContext(), queryId, req, connection);
    return qPlanFragments;
  }
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/user/UserWorker.java`
#### Snippet
```java
    final long p1 = ((Integer.MAX_VALUE - time) << 32) + r.nextInt();
    final long p2 = r.nextLong();
    final QueryId id = QueryId.newBuilder().setPart1(p1).setPart2(p2).build();
    return id;
  }
```

### UnnecessaryLocalVariable
Local variable `bytes` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
    bits |= bits >> 16;
    bits++;
    int bytes = bits / 8;
    return bytes;
  }
```

### UnnecessaryLocalVariable
Local variable `aclProvider` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKACLProviderFactory.java`
#### Snippet
```java
        if (config.getBoolean(ZK_APPLY_SECURE_ACL)) {
            logger.trace("Using secure ZK ACL. Drill cluster path " + drillClusterPath);
            ZKACLProviderDelegate aclProvider = findACLProvider(config, stateProvider, context);
            return aclProvider;
        }
```

### UnnecessaryLocalVariable
Local variable `store` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java
  @Override
  public <V> TransientStore<V> getOrCreateTransientStore(final TransientStoreConfig<V> config) {
    final ZkEphemeralStore<V> store = (ZkEphemeralStore<V>)factory.getOrCreateStore(config);
    return store;
  }
```

### UnnecessaryLocalVariable
Local variable `ipAddr` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/BitToUserConnectionIterator.java`
#### Snippet
```java

    private String extractIpAddr(String clientAddrString) {
      String ipAddr = clientAddrString
          .replaceFirst("/","") //Remove any leading '/'
          .split(":")[0]; //Remove any connected port reference
```

### UnnecessaryLocalVariable
Local variable `f` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockRecordReader.java`
#### Snippet
```java
  private MaterializedField getVector(String name, MajorType type) {
    assert context != null : "Context shouldn't be null.";
    final MaterializedField f = MaterializedField.create(name, type);
    return f;
  }
```

### UnnecessaryLocalVariable
Local variable `footer` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/FooterGatherer.java`
#### Snippet
```java
      final ByteArrayInputStream from = new ByteArrayInputStream(footerBytes);
      ParquetMetadata metadata = ParquetFormatPlugin.parquetMetadataConverter.readParquetMetadata(from, NO_FILTER);
      Footer footer = new Footer(status.getPath(), metadata);
      return footer;
    }
```

### UnnecessaryLocalVariable
Local variable `listType` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
            .list(dataMode == DataMode.OPTIONAL ? Repetition.OPTIONAL : Repetition.REQUIRED);
        addElementType(listBuilder, elementField);
        GroupType listType = listBuilder.named(field.getName());
        return listType;
      case NULL:
```

### UnnecessaryLocalVariable
Local variable `maxNumValues` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkEntry.java`
#### Snippet
```java
    // values were fixed but not the rest).
    final int largestDataLen = buffSz - UInt4Vector.VALUE_WIDTH;
    final int maxNumValues = buffSz / UInt4Vector.VALUE_WIDTH;
    final int lengthSz = maxNumValues;
    final int dataSz = largestDataLen + PADDING;
```

### UnnecessaryLocalVariable
Local variable `lengthSz` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenColumnBulkEntry.java`
#### Snippet
```java
    final int largestDataLen = buffSz - UInt4Vector.VALUE_WIDTH;
    final int maxNumValues = buffSz / UInt4Vector.VALUE_WIDTH;
    final int lengthSz = maxNumValues;
    final int dataSz = largestDataLen + PADDING;

```

### UnnecessaryLocalVariable
Local variable `r` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java

  public Future<Long> processPagesAsync(long recordsToReadInThisPass){
    Future<Long> r = (isShuttingDown ? null : threadPool.submit(new ColumnReaderProcessPagesTask(recordsToReadInThisPass)));
    return r;
  }
```

### UnnecessaryLocalVariable
Local variable `ex` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
      pageReader.readPosInBytes = readStartInBytes + readLength;
    } catch (Exception e) {
      UserException ex = UserException.dataReadError(e)
          .message("Error reading from Parquet file")
          .pushContext("Row Group Start: ", this.columnChunkMetaData.getStartingPos())
```

### UnnecessaryLocalVariable
Local variable `r` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java

  protected Future<Integer> readRecordsAsync(int recordsToRead) {
    Future<Integer> r = (isShuttingDown ? null : threadPool.submit(new ColumnReaderReadRecordsTask(recordsToRead)));
    return r;
  }
```

### UnnecessaryLocalVariable
Local variable `ex` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java
      pageReader.init();
    } catch (IOException e) {
      UserException ex = UserException.dataReadError(e)
          .message("Error initializing page reader for Parquet file")
          .pushContext("Row Group Start: ", this.columnChunkMetaData.getStartingPos())
```

### UnnecessaryLocalVariable
Local variable `f` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReader.java`
#### Snippet
```java

  public Future<Boolean> readPageAsync() {
    Future<Boolean> f = threadPool.submit(new Callable<Boolean>() {
      @Override public Boolean call() throws Exception {
        return Boolean.valueOf(readPage());
```

### UnnecessaryLocalVariable
Local variable `ex` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java

  protected void throwUserException(Exception e, String msg) throws UserException {
    UserException ex = UserException.dataReadError(e).message(msg)
        .pushContext("Row Group Start: ", columnChunkMetaData.getStartingPos())
        .pushContext("Column: ", this.parentColumnReader.schemaElement.getName())
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchOverflow.java`
#### Snippet
```java
    public RecordBatchOverflow build() {
      RecordOverflowContainer overflowContainer = OverflowSerDeUtil.serialize(fieldOverflowEntries, allocator, batchStatsContext);
      RecordBatchOverflow result = new RecordBatchOverflow(overflowContainer.recordOverflowDef, allocator);

      return result;
```

### UnnecessaryLocalVariable
Local variable `b` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/WritableBatch.java`
#### Snippet
```java
        .setCarriesTwoByteSelectionVector(isSV2)
        .build();
    WritableBatch b = new WritableBatch(batchDef, buffers);
    return b;
  }
```

### UnnecessaryLocalVariable
Local variable `s` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemaUtil.java`
#### Snippet
```java

    SchemaBuilder schemaBuilder = new SchemaBuilder();
    BatchSchema s = schemaBuilder.addFields(fields).setSelectionVectorMode(schemas[0].getSelectionVectorMode()).build();
    return s;
  }
```

### UnnecessaryLocalVariable
Local variable `map` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
  private ColumnSize getComplexColumn(String path) {
    String[] segments = Strings.split(path, '.');
    Map<String, ColumnSize> map = columnSizes;
    return getComplexColumnImpl(segments, 0, map);
  }
```

### UnnecessaryLocalVariable
Local variable `childCount` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
      // that changes the value count of the contained vectors.
      UInt4Vector offsetVector = ((RepeatedValueVector) v).getOffsetVector();
      int childCount = valueCount == 0 ? 0 : offsetVector.getAccessor().get(valueCount);

      return childCount;
```

### UnnecessaryLocalVariable
Local variable `liveOptions` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/SessionOptionManager.java`
#### Snippet
```java
  @Override
  Iterable<OptionValue> getLocalOptions() {
    final Collection<OptionValue> liveOptions = Collections2.filter(options.values(), isLive);
    return liveOptions;
  }
```

### UnnecessaryLocalVariable
Local variable `systemOptionManager` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/SessionOptionManager.java`
#### Snippet
```java
   */
  public SystemOptionManager getSystemOptionManager() {
    final SystemOptionManager systemOptionManager = (SystemOptionManager) fallback;
    return systemOptionManager;
  }
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
  public AnnotationVisitor visitAnnotationDefault() {
    fsmCursor.transition("visitAnnotationDefault");
    final AnnotationVisitor annotationVisitor = super.visitAnnotationDefault();
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
  }
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final boolean visible) {
    fsmCursor.transition("visitAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitAnnotation(desc,
        visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final TypePath typePath, final String desc, final boolean visible) {
    fsmCursor.transition("visitTryCatchAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitTryCatchAnnotation(
        typeRef, typePath, desc, visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final TypePath typePath, final String desc, final boolean visible) {
    fsmCursor.transition("visitInsnAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitInsnAnnotation(
        typeRef, typePath, desc, visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final String desc, final boolean visible) {
    fsmCursor.transition("visitParameterAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitParameterAnnotation(
        parameter, desc, visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final int[] index, final String desc, final boolean visible) {
    fsmCursor.transition("visitLocalVariableAnnotation");
    final AnnotationVisitor annotationVisitor = super
        .visitLocalVariableAnnotation(typeRef, typePath, start, end, index,
            desc, visible);
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
      final TypePath typePath, final String desc, final boolean visible) {
    fsmCursor.transition("visitTypeAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitTypeAnnotation(
        typeRef, typePath, desc, visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `bc` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassCompilerSelector.java`
#### Snippet
```java
      throws CompileException, ClassNotFoundException, ClassTransformationException, IOException {

    byte[][] bc = getCompiler(sourceCode).getClassByteCode(className, sourceCode);

    // Uncomment the following to save the generated byte codes.
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java
      final boolean visible) {
    fsmCursor.transition("visitAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitAnnotation(desc,
        visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `fieldVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java
      final String desc, final String signature, final Object value) {
    fsmCursor.transition("visitField");
    final FieldVisitor fieldVisitor = super.visitField(access, name, desc,
        signature, value);
    return fieldVisitor; // TODO: add CheckFieldVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `annotationVisitor` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java
      final TypePath typePath, final String desc, final boolean visible) {
    fsmCursor.transition("visitTypeAnnotation");
    final AnnotationVisitor annotationVisitor = super.visitTypeAnnotation(
        typeRef, typePath, desc, visible);
    return annotationVisitor; // TODO: add CheckAnnotationVisitorFsm
```

### UnnecessaryLocalVariable
Local variable `ops` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlDropTable.java`
#### Snippet
```java
  @Override
  public List<SqlNode> getOperandList() {
    final List<SqlNode> ops =
        ImmutableList.of(
            tableName,
```

### UnnecessaryLocalVariable
Local variable `ops` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/SqlDropView.java`
#### Snippet
```java
  @Override
  public List<SqlNode> getOperandList() {
    final List<SqlNode> ops =
        ImmutableList.of(
            viewName,
```

### UnnecessaryLocalVariable
Local variable `plan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
    propsBuilder.generator(this.getClass().getSimpleName(), "");
    propsBuilder.scannedPluginNames(scannedPluginNames);
    PhysicalPlan plan =  new PhysicalPlan(propsBuilder.build(), getPops(op));
    return plan;

```

### UnnecessaryLocalVariable
Local variable `op` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
  protected PhysicalOperator convertToPop(Prel prel) throws IOException {
    PhysicalPlanCreator creator = new PhysicalPlanCreator(context, PrelSequencer.getIdMap(prel));
    PhysicalOperator op = prel.getPhysicalOperator(creator);
    return op;
  }
```

### UnnecessaryLocalVariable
Local variable `desc` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/DrillIndexDefinition.java`
#### Snippet
```java
  public String toString() {
    String columnsDesc = " Index columns: " + indexColumns.toString() + " Non-Index columns: " + nonIndexColumns.toString();
    String desc = "Table: " + tableName + " Index: " + indexName + columnsDesc;
    return desc;
  }
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/DrillIndexDefinition.java`
#### Snippet
```java
  @Override
  public int getIndexColumnOrdinal(LogicalExpression path) {
    int id = indexColumns.indexOf(path);
    return id;
  }
```

### UnnecessaryLocalVariable
Local variable `rewrittenProject` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringPlanNoFilterGenerator.java`
#### Snippet
```java
        }

        ProjectPrel rewrittenProject = new ProjectPrel(newProject.getCluster(),
                collation==null? newProject.getTraitSet() : newProject.getTraitSet().plus(collation),
                indexScanPrel, newProjects, newProject.getRowType());
```

### UnnecessaryLocalVariable
Local variable `resultRex` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
    // Visit through the nodes, if destExprMap has an entry to provide substitute to replace a rexNode, replace the rexNode
    RexReplace replacer = new RexReplace(destNodeMap);
    RexNode resultRex = srcRex.accept(replacer);
    return resultRex;
  }
```

### UnnecessaryLocalVariable
Local variable `resultRex` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
    if (srcToReplace.size() > 0) {
      RexReplace replacer = new RexReplace(srcToReplace);
      RexNode resultRex = expr.accept(replacer);
      return resultRex;
    }
```

### UnnecessaryLocalVariable
Local variable `newFieldPath` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/SimpleRexRemap.java`
#### Snippet
```java
        if (mapOrArray != null) {
          if (call.operands.get(1) instanceof RexLiteral) {
            PathSegment newFieldPath = newPath(
                mapOrArray.cloneWithNewChild(Utilities.convertLiteral((RexLiteral) call.operands.get(1))),
                call);
```

### UnnecessaryLocalVariable
Local variable `rewrittenProject` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringIndexPlanGenerator.java`
#### Snippet
```java
        }

        ProjectPrel rewrittenProject = new ProjectPrel(newProject.getCluster(),
            newCollation==null? newProject.getTraitSet() : newProject.getTraitSet().plus(newCollation),
            indexFilterPrel, newProjects, newProject.getRowType());
```

### UnnecessaryLocalVariable
Local variable `dbGroupScan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
      // for the primary table join-back each row may belong to a different block, so in general num_blocks = num_rows;
      // however, num_blocks cannot exceed the total number of blocks of the table
      DbGroupScan dbGroupScan = (DbGroupScan) primaryTableGroupScan;
      double totalBlocksPrimary = Math.ceil((dbGroupScan.getColumns().size() *
          costBase.getAverageColumnSize(dbGroupScan) * totalRows) / costBase.getBlockSize(dbGroupScan));
```

### UnnecessaryLocalVariable
Local variable `rangeDistRight` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
    // create a RANGE PARTITION on the right side (this could be removed later during ExcessiveExchangeIdentifier phase
    // if the estimated row count is smaller than slice_target
    final RelNode rangeDistRight = createRangeDistRight(rightIndexProjectPrel, rightRowKeyField, origDbGroupScan);

    // the range partitioning adds an extra column for the partition id but in the final plan we already have a
```

### UnnecessaryLocalVariable
Local variable `convertedRight` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
    // renaming Project for the _id field inserted as part of the JoinPrelRenameVisitor. Thus, we are not inserting
    // a separate Project here.
    final RelNode convertedRight = rangeDistRight;

    // left (probe) side of the rowkey join
```

### UnnecessaryLocalVariable
Local variable `leftIndexProjectPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
      }

      final ProjectPrel leftIndexProjectPrel = new ProjectPrel(dbScan.getCluster(),
          collation != null ? dbScan.getTraitSet().plus(collation) : dbScan.getTraitSet(),
          leftIndexFilterPrel == null ? dbScan : leftIndexFilterPrel, leftProjectExprs, leftProjectRowType);
```

### UnnecessaryLocalVariable
Local variable `hjPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
    if (settings.isIndexUseHashJoinNonCovering()) {
      //for hash join, collation will be cleared
      HashJoinPrel hjPrel = new HashJoinPrel(topRel.getCluster(), leftTraits, convertedLeft,
          convertedRight, joinCondition, JoinRelType.INNER, false /* no swap */,
          null /* no runtime filter */,
```

### UnnecessaryLocalVariable
Local variable `resetProjectPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java

    //rewrite the collation for this projectPrel
    final ProjectPrel resetProjectPrel = new ProjectPrel(newRel.getCluster(), newRel.getTraitSet(),
        newRel, resetExprs, finalProjectRowType);
    newRel = resetProjectPrel;
```

### UnnecessaryLocalVariable
Local variable `cap` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
      }
      RelTraitSet newProjectTraits = newTraitSet(Prel.DRILL_PHYSICAL, newDist, newCollation);
      ProjectPrel cap = new ProjectPrel(upperProject.getCluster(),
          newProjectTraits,
          newRel, IndexPlanUtils.getProjects(upperProject), upperProject.getRowType());
```

### UnnecessaryLocalVariable
Local variable `resetProjectPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    }

    final ProjectPrel resetProjectPrel = new ProjectPrel(newRel.getCluster(), newRel.getTraitSet(),
        newRel, resetExprs, finalProjectRowType);
    newRel = resetProjectPrel;
```

### UnnecessaryLocalVariable
Local variable `finalRel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    newRel = resetProjectPrel;

    RelNode finalRel = Prule.convert(newRel, newRel.getTraitSet());
    return finalRel;
  }
```

### UnnecessaryLocalVariable
Local variable `cap` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    RelNode finalRel = buildRowKeyJoin(leftRelAndScan.left, rangeDistRight, true, JoinControl.DEFAULT);
    if (upperProject != null) {
      ProjectPrel cap = new ProjectPrel(finalRel.getCluster(), finalRel.getTraitSet(),
          finalRel, IndexPlanUtils.getProjects(upperProject), upperProject.getRowType());
      finalRel = cap;
```

### UnnecessaryLocalVariable
Local variable `totalRemainder` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
      operands.add(indexColsRemFilter);
      operands.add(incColsRemFilter);
      RexNode totalRemainder = RexUtil.composeConjunction(rexBuilder, operands, false);
      return totalRemainder;
    } else if (indexColsRemFilter != null) {
```

### UnnecessaryLocalVariable
Local variable `proj` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    final RelDataType projectRowType = rightFieldTypeBuilder.build();

    ProjectPrel proj = new ProjectPrel(inputRel.getCluster(), inputRel.getTraitSet(), inputRel, exprs, projectRowType);

    return proj;
```

### UnnecessaryLocalVariable
Local variable `collation` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
    }

    final RelCollation collation = RelCollations.of(fieldCollations);
    return collation;
  }
```

### UnnecessaryLocalVariable
Local variable `hjPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
      RelNode newRel;
      if (settings.isIndexUseHashJoinNonCovering()) {
        HashJoinPrel hjPrel = new HashJoinPrel(left.getCluster(), left.getTraitSet(), left,
            right, joinCondition, JoinRelType.INNER, false /* no swap */, null /* no runtime filter */,
            isRowKeyJoin, htControl);
```

### UnnecessaryLocalVariable
Local variable `leadingColumnsFilter` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
  public static RexNode getLeadingColumnsFilter(List<RexNode> leadingFilters, RexBuilder rexBuilder) {
    if (leadingFilters.size() > 0) {
      RexNode leadingColumnsFilter = RexUtil.composeConjunction(rexBuilder, leadingFilters, false);
      return leadingColumnsFilter;
    }
```

### UnnecessaryLocalVariable
Local variable `rjPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
        newRel = hjPrel;
      } else {
        RowKeyJoinPrel rjPrel = new RowKeyJoinPrel(left.getCluster(), left.getTraitSet(),
            left, right, joinCondition, JoinRelType.INNER);
        newRel = rjPrel;
```

### UnnecessaryLocalVariable
Local variable `condition` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java

  public static RexNode getTotalFilter(RexNode leadColsFilter, RexNode totRemColsFilter, RexBuilder rexBuilder) {
    RexNode condition = leadColsFilter;
    if (leadColsFilter != null && totRemColsFilter != null && !totRemColsFilter.isAlwaysTrue()) {
      List<RexNode> conditions = new ArrayList<RexNode>();
```

### UnnecessaryLocalVariable
Local variable `indexScanPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
    }

    ScanPrel indexScanPrel = new ScanPrel(origScan.getCluster(),
        indexScanTraitSet, indexGroupScan,
        newRowType, origScan.getTable());
```

### UnnecessaryLocalVariable
Local variable `convertedRight` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/AbstractIndexPlanGenerator.java`
#### Snippet
```java

    RelTraitSet rightTraits = newTraitSet(distRight).plus(Prel.DRILL_PHYSICAL);
    RelNode convertedRight = Prule.convert(rightPrel, rightTraits);

    return convertedRight;
```

### UnnecessaryLocalVariable
Local variable `rexCondition` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
      );
    }
    RexNode rexCondition = RexUtil.composeConjunction(context.getRexBuilder(), joinConditions, false);
    return rexCondition;
  }
```

### UnnecessaryLocalVariable
Local variable `joinRel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
    Pair<RelNode, RelNode> inputs = getJoinInputs(join, context);
    RexNode rexCondition = getJoinCondition(join, context);
    DrillJoinRel joinRel = new DrillJoinRel(context.getCluster(), context.getLogicalTraits(),
        inputs.left, inputs.right, rexCondition, join.getJoinType());
    return joinRel;
```

### UnnecessaryLocalVariable
Local variable `tableStatistics` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillStatsTable.java`
#### Snippet
```java
  public static List<StatisticsHolder<?>> getEstimatedTableStats(DrillStatsTable statsProvider) {
    if (statsProvider != null && statsProvider.isMaterialized()) {
      List<StatisticsHolder<?>> tableStatistics = Arrays.asList(
          new StatisticsHolder<>(statsProvider.getRowCount(), TableStatisticsKind.EST_ROW_COUNT),
          new StatisticsHolder<>(Boolean.TRUE, TableStatisticsKind.HAS_DESCRIPTIVE_STATISTICS));
```

### UnnecessaryLocalVariable
Local variable `joinRel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/RowKeyJoinRel.java`
#### Snippet
```java
    Pair<RelNode, RelNode> inputs = getJoinInputs(join, context);
    RexNode rexCondition = getJoinCondition(join, context);
    RowKeyJoinRel joinRel = new RowKeyJoinRel(context.getCluster(), context.getLogicalTraits(),
        inputs.left, inputs.right, rexCondition, join.getJoinType());
    return joinRel;
```

### UnnecessaryLocalVariable
Local variable `frame` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillWindowRel.java`
#### Snippet
```java
    }
    builder.setInput(inputOp);
    org.apache.drill.common.logical.data.Window frame = builder.build();
    return frame;
  }
```

### UnnecessaryLocalVariable
Local variable `expr` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillWindowRel.java`
#### Snippet
```java
      args.add(new ValueExpressions.LongExpression(1l));
    }
    LogicalExpression expr = new FunctionCall(call.getAggregation().getName().toLowerCase(), args, ExpressionPosition.UNKNOWN);
    return expr;
  }
```

### UnnecessaryLocalVariable
Local variable `func` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillOptiq.java`
#### Snippet
```java

      if (FunctionCallFactory.isBooleanOperator(funcName)) {
        LogicalExpression func = FunctionCallFactory.createBooleanOperator(funcName, args);
        return func;
      } else {
```

### UnnecessaryLocalVariable
Local variable `child` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/ExchangeRemoverMaterializer.java`
#### Snippet
```java
  @Override
  public PhysicalOperator visitGroupScan(GroupScan groupScan, IndexedFragmentNode iNode) throws ExecutionSetupException {
    PhysicalOperator child = groupScan.getSpecificScan(iNode.getMinorFragmentId());
    return child;
  }
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/ExchangeRemoverMaterializer.java`
#### Snippet
```java

    try {
      PhysicalOperator o = store.getSpecificStore(child, iNode.getMinorFragmentId());
      return o;
    } catch (PhysicalOperatorSetupException e) {
```

### UnnecessaryLocalVariable
Local variable `childEx` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/ExchangeRemoverMaterializer.java`
#### Snippet
```java
  public PhysicalOperator visitExchange(Exchange exchange, IndexedFragmentNode iNode) throws ExecutionSetupException {
    iNode.addAllocation(exchange);
    PhysicalOperator childEx = exchange.getChild().accept(this, iNode);
    return childEx;
  }
```

### UnnecessaryLocalVariable
Local variable `removeColumnProjectPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashToRandomExchangePrel.java`
#### Snippet
```java
          removeUpdatedExpr, childFields, null);

      ProjectPrel removeColumnProjectPrel = new ProjectPrel(newPrel.getCluster(), newPrel.getTraitSet(),
                                                            newPrel, removeUpdatedExpr, removeRowType);
      return removeColumnProjectPrel;
```

### UnnecessaryLocalVariable
Local variable `proj` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LateralJoinPrel.java`
#### Snippet
```java
        exprs, outputFieldNames, null);

    ProjectPrel proj = new ProjectPrel(input.getCluster(), input.getTraitSet(), input, exprs, rowType);

    return proj;
```

### UnnecessaryLocalVariable
Local variable `proj` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPrel.java`
#### Snippet
```java
        exprs, outputFieldNames, null);

    ProjectPrel proj = new ProjectPrel(input.getCluster(), input.getTraitSet(), input, exprs, rowType);

    return proj;
```

### UnnecessaryLocalVariable
Local variable `rowType` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/WindowPrel.java`
#### Snippet
```java
    }

    final RelDataType rowType = this.getCluster().getRexBuilder().getTypeFactory().createStructType(fieldList);

    return rowType;
```

### UnnecessaryLocalVariable
Local variable `limitPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/TopNPrel.java`
#### Snippet
```java
            this.getCluster().getTypeFactory().createSqlType(SqlTypeName.INTEGER));
    //SMEX is not needed here because Lateral/Unnest pipeline doesn't support exchanges.
    LimitPrel limitPrel = new LimitPrel(this.getCluster(), traits, sortprel, offset, limit, false, true);
    return limitPrel;
  }
```

### UnnecessaryLocalVariable
Local variable `flatten` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RewriteProjectToFlatten.java`
#### Snippet
```java
      Prel newChild = ((Prel)project.getInput(0)).accept(this, null);
      ProjectPrel newProject = new ProjectPrel(project.getCluster(), project.getTraitSet(), newChild, exprList, new RelRecordType(relDataTypes));
      FlattenPrel flatten = new FlattenPrel(project.getCluster(), project.getTraitSet(), newProject, flatttenExpr);
      return flatten;
    }
```

### UnnecessaryLocalVariable
Local variable `proj` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/StarColumnConverter.java`
#### Snippet
```java

      // insert a PAS.
      ProjectPrel proj = new ProjectPrel(prel.getCluster(), prel.getTraitSet(), prel, exprs, rowType);

      return proj;
```

### UnnecessaryLocalVariable
Local variable `unnestPrel` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/AdjustOperatorsSchemaVisitor.java`
#### Snippet
```java
    }

    UnnestPrel unnestPrel = new UnnestPrel(prel.getCluster(), prel.getTraitSet(),
            prel.getCluster().getTypeFactory().createStructType(fieldTypes, fieldNames), fieldAccess);
    return unnestPrel;
```

### UnnecessaryLocalVariable
Local variable `e` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExceptionInjection.java`
#### Snippet
```java

    if (throwable instanceof RuntimeException) {
      final RuntimeException e = (RuntimeException) throwable;
      throw e;
    }
```

### UnnecessaryLocalVariable
Local variable `e` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExceptionInjection.java`
#### Snippet
```java
    }
    if (throwable instanceof Error) {
      final Error e = (Error) throwable;
      throw e;
    }
```

### UnnecessaryLocalVariable
Local variable `exception` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExceptionInjection.java`
#### Snippet
```java

    if (exceptionClass.isAssignableFrom(throwable.getClass())) {
      final T exception = exceptionClass.cast(throwable);
      throw exception;
    }
```

### UnnecessaryLocalVariable
Local variable `plan` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/PhysicalPlan.java`
#### Snippet
```java
  public static PhysicalPlan parse(ObjectReader reader, String planString) {
    try {
      PhysicalPlan plan = reader.readValue(planString);
      return plan;
    } catch (IOException e) {
```

### UnnecessaryLocalVariable
Local variable `newOp` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TraceInjector.java`
#### Snippet
```java
    public static PhysicalOperator getExec(FragmentContext context, FragmentRoot root) throws ExecutionSetupException {
        TraceInjector tI = new TraceInjector();
        PhysicalOperator newOp = root.accept(tI, context);

        return newOp;
```

### UnnecessaryLocalVariable
Local variable `vv0` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
      Object tmp = (htContainer).getValueAccessorById(BigIntVector.class, index).getValueVector();
      if (tmp != null) {
        BigIntVector vv0 = ((BigIntVector) tmp);
        return vv0;
      }
```

### UnnecessaryLocalVariable
Local variable `entryIdxWithinBatch` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java

      for (int i = 0; i <= maxOccupiedIdx; i++) {
        int entryIdxWithinBatch = i;
        int entryIdx = entryIdxWithinBatch + batchStartIdx;
        int hash = hashValues.getAccessor().get(entryIdxWithinBatch); // get the already saved hash value
```

### UnnecessaryLocalVariable
Local variable `rounded` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java

  private static int roundUpToPowerOf2(int number) {
    int rounded = number >= MAXIMUM_CAPACITY
        ? MAXIMUM_CAPACITY
        : (rounded = Integer.highestOneBit(number)) != 0
```

### UnnecessaryLocalVariable
Local variable `count` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectorTemplate.java`
#### Snippet
```java

    case TWO_BYTE:
      int count = recordCount;
      for (int i = 0; i < count; i++, firstOutputIndex++) {
        try {
```

### UnnecessaryLocalVariable
Local variable `countN` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectorTemplate.java`
#### Snippet
```java

    case NONE:
      int countN = recordCount;
      int i;
      for (i = startIndex; i < startIndex + countN; i++, firstOutputIndex++) {
```

### UnnecessaryLocalVariable
Local variable `type` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/OutputWidthVisitor.java`
#### Snippet
```java

  private OutputWidthExpression getFixedLenExpr(MajorType majorType) {
    MajorType type = majorType;
    if (Types.isFixedWidthType(type)) {
      // Use only the width of the data. Metadata width will be accounted for at the end
```

### UnnecessaryLocalVariable
Local variable `stat` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/MergedStatisticFactory.java`
#### Snippet
```java
  private MergedStatistic newMergedStatistic(String outputStatName)
      throws InstantiationException, IllegalAccessException {
    MergedStatistic stat = statsClasses.get(outputStatName).newInstance();
    return stat;
  }
```

### UnnecessaryLocalVariable
Local variable `outputMap` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/ColumnMergedStatistic.java`
#### Snippet
```java
    // Check the input is a Map Vector
    assert (output.getField().getType().getMinorType() == TypeProtos.MinorType.MAP);
    MapVector outputMap = (MapVector) output;
    for (ValueVector outMapCol : outputMap) {
      String colName = outMapCol.getField().getName();
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsAggBatch.java`
#### Snippet
```java
    TypedFieldId.Builder builder = TypedFieldId.newBuilder();
    builder.addId(pfid.getFieldIds()[0]);
    TypedFieldId id =
        FieldIdUtil.getFieldIdIfMatches(parent, builder, true,
            SchemaPath.getSimplePath(vv.getField().getName()).getRootSegment());
```

### UnnecessaryLocalVariable
Local variable `numOutputRecords` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggTemplate.java`
#### Snippet
```java

    currPartition.get(currOutBatchIndex).outputValues();
    int numOutputRecords = numPendingOutput;
    htables[partitionToReturn].outputKeys(currOutBatchIndex, outContainer, numPendingOutput);

```

### UnnecessaryLocalVariable
Local variable `countN` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/rangepartitioner/RangePartitionRecordBatch.java`
#### Snippet
```java
   */
  private final int projectRecords(int recordCount, int firstOutputIndex) {
    int countN = recordCount;
    int counter = 0;
    for (int i = 0; i < countN; i++, firstOutputIndex++) {
```

### UnnecessaryLocalVariable
Local variable `countN` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjectorTemplate.java`
#### Snippet
```java
  @Override
  public final int projectRecords(final int recordCount, int firstOutputIndex) {
    final int countN = recordCount;
    int counter = 0;
    for (int i = 0; i < countN; i++, firstOutputIndex++) {
```

### UnnecessaryLocalVariable
Local variable `newDummyField` is redundant
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
    }

    UserBitShared.SerializedField newDummyField = newDummyFieldBuilder.build();

    return newDummyField;
```

### UnnecessaryLocalVariable
Local variable `cpuCost` is redundant
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/GoogleSheetsGroupScan.java`
#### Snippet
```java
    double estColCount = Utilities.isStarQuery(columns) ? DrillScanRel.STAR_COLUMN_COST : columns.size();
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

```

### UnnecessaryLocalVariable
Local variable `ioCost` is redundant
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/GoogleSheetsGroupScan.java`
#### Snippet
```java
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

    // Force the caller to use our costs rather than the
```

### UnnecessaryLocalVariable
Local variable `config` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableStats.java`
#### Snippet
```java
      synchronized (MapRDBTableStats.class) {
        if (admin == null) {
          Configuration config = conf;
          admin = new HBaseAdminImpl(config, TableMappingRulesFactory.create(conf));
        }
```

### UnnecessaryLocalVariable
Local variable `newProject` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushLimitIntoScan.java`
#### Snippet
```java
          join.getCondition(), join.getJoinType());
      if (project != null) {
        final ProjectPrel newProject = new ProjectPrel(project.getCluster(), project.getTraitSet(), newJoin,
            project.getProjects(), project.getRowType());
        newChild = newProject;
```

### UnnecessaryLocalVariable
Local variable `newProject` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushLimitIntoScan.java`
#### Snippet
```java
      final RelNode newChild;
      if (project != null) {
        final ProjectPrel newProject = new ProjectPrel(project.getCluster(), project.getTraitSet(),
            newScan, project.getProjects(), project.getRowType());
        newChild = newProject;
```

### UnnecessaryLocalVariable
Local variable `subscan` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonTableGroupScan.java`
#### Snippet
```java
        "Mappings length [%d] should be greater than minor fragment id [%d] but it isn't.", endpointFragmentMapping.size(),
        minorFragmentId);
    RestrictedMapRDBSubScan subscan =
        new RestrictedMapRDBSubScan(getUserName(), formatPlugin,
        getEndPointFragmentMapping(minorFragmentId), columns, maxRecordsToRead, TABLE_JSON, getSchema());
```

### UnnecessaryLocalVariable
Local variable `subScanSpec` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonTableGroupScan.java`
#### Snippet
```java
  protected RestrictedMapRDBSubScanSpec getSubScanSpec(TabletFragmentInfo tfi) {
    JsonScanSpec spec = scanSpec;
    RestrictedMapRDBSubScanSpec subScanSpec =
        new RestrictedMapRDBSubScanSpec(
        spec.getTableName(),
```

### UnnecessaryLocalVariable
Local variable `subScanSpec` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
    byte[] startRow = condition == null ? null : ((ConditionImpl) condition).getRowkeyRanges().get(0).getStartRow();
    byte[] stopRow = condition == null ? null : ((ConditionImpl) condition).getRowkeyRanges().get(0).getStopRow();
    JsonSubScanSpec subScanSpec = new JsonSubScanSpec(
        spec.getTableName(),
        spec.getIndexDesc(),
```

### UnnecessaryLocalVariable
Local variable `networkCost` is redundant
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBIndexDescriptor.java`
#### Snippet
```java
      // db server will send only the projected columns to the db client for the selected
      // number of rows, so network cost is based on the number of actual projected columns
      double networkCost = leadRowCount * numProjectedFields * pluginCost.getAverageColumnSize(primaryTableGroupScan);
      return networkCost;
    } else {
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/ExecProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/GeneralRPCProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/CoordinationProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `bindException` is redundant
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
        }

        final UserException bindException =
            UserException
              .resourceError( e )
```

### UnnecessaryLocalVariable
Local variable `released` is redundant
in `exec/memory/base/src/main/java/io/netty/buffer/MutableWrappedByteBuf.java`
#### Snippet
```java
  @Override
  public boolean release(int decrement) {
    boolean released = unwrap().release(decrement);
    return released;
  }
```

### UnnecessaryLocalVariable
Local variable `v` is redundant
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  @Override
  public short getShort(int index) {
    short v = PlatformDependent.getShort(addr(index));
    return v;
  }
```

### UnnecessaryLocalVariable
Local variable `v` is redundant
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  @Override
  public int getInt(int index) {
    int v = PlatformDependent.getInt(addr(index));
    return v;
  }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `shpGeometryRow` is redundant
in `contrib/format-esri/src/main/java/com/esri/core/geometry/ShapefileByteBufferCursor.java`
#### Snippet
```java
  @Override
  public ByteBuffer next() {
    ByteBuffer shpGeometryRow = getSingleRowFromShp();
    return shpGeometryRow;
  }
```

### UnnecessaryLocalVariable
Local variable `readerConfig` is redundant
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java

  public ExcelReaderConfig getReaderConfig(ExcelFormatPlugin plugin) {
    ExcelReaderConfig readerConfig = new ExcelReaderConfig(plugin);
    return readerConfig;
  }
```

### UnnecessaryLocalVariable
Local variable `cpuCost` is redundant
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkGroupScan.java`
#### Snippet
```java
    double estColCount = Utilities.isStarQuery(columns) ? DrillScanRel.STAR_COLUMN_COST : columns.size();
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

```

### UnnecessaryLocalVariable
Local variable `ioCost` is redundant
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkGroupScan.java`
#### Snippet
```java
    double valueCount = estRowCount * estColCount;
    double cpuCost = valueCount;
    double ioCost = valueCount;

    // Force the caller to use our costs rather than the
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

## RuleId[id=UnpredictableBigDecimalConstructorCall]
### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
        break;
      case FLOAT4:
        result = new BigDecimal( innerAccessor.getFloat( rowOffset ) );
        break;
      case FLOAT8:
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
        break;
      case FLOAT8:
        result = new BigDecimal( innerAccessor.getDouble( rowOffset ) );
        break;

```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/YarnRMClient.java`
#### Snippet
```java
      System.out.println("App State: " + appState);
      try {
        Thread.sleep(1000);
      } catch (InterruptedException e) {
        // Should never occur.
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/YarnRMClient.java`
#### Snippet
```java
      }
      try {
        Thread.sleep(100);
      } catch (InterruptedException e) {
        // Should never occur.
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/PulseRunnable.java`
#### Snippet
```java
    while (isLive.get()) {
      try {
        Thread.sleep(pulsePeriod);
      } catch (InterruptedException e) {
        break;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/WorkManager.java`
#### Snippet
```java

        try {
          Thread.sleep(STATUS_PERIOD_SECONDS * 1000);
        } catch (final InterruptedException e) {
          // Preserve evidence that the interruption occurred so that code higher up on the call stack can learn of the
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
    while (!asyncAggregateWorker.over.get()) {
      try {
        Thread.sleep(100);
      } catch (InterruptedException e) {
        logger.error("interrupted while sleeping to wait for the aggregating worker thread to exit", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
    while(true) {
      try {
        Thread.sleep(timeout);
      } catch (final InterruptedException e) {
        checkContinue();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
              PooledByteBufAllocatorL.this.toString());
          try {
            Thread.sleep(MEMORY_LOGGER_FREQUENCY_SECONDS * 1000);
          } catch (InterruptedException e) {
            return;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaGroupScan.java`
#### Snippet
```java

    while (assignments.isEmpty() && timeout < waitingForAssigmentTimeout) {
      Thread.sleep(500);
      timeout += 500;
      assignments = consumer.assignment();
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
        }
        while (SPIN_FILE.exists()) {
          try { sleep(1_000); } catch (final Exception ex) { /* ignore interruptions */ }
        }
        try { outErr.delete(); } catch (final Exception ex) { } // cleanup - remove err msg file
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
      return null;
    }
    FileSelection fileSel = create(Arrays.asList(statuses), null, root);
    if (timer != null) {
      logger.debug("FileSelection.create() took {} ms ", timer.elapsed(TimeUnit.MILLISECONDS));
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PrelUtil.java`
#### Snippet
```java

  public static Iterator<Prel> iter(RelNode... nodes) {
    return (Iterator<Prel>) (Object) Arrays.asList(nodes).iterator();
  }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
      FileStatus[] status = fs.globStatus(new Path(root, name));
      if (status != null) {
        statuses.addAll(Arrays.asList(status));
      }
    } else {
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
        FileStatus[] status = fs.globStatus(new Path(root, patternAppliedName));
        if (status != null) {
          statuses.addAll(Arrays.asList(status));
        }
      } else { // use list status if no glob_pattern/wildcards exist in path
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
          try {
            FileStatus[] status = fs.listStatus(new Path(root, name + dotType.getEnding()));
            statuses.addAll(Arrays.asList(status));
          } catch (FileNotFoundException ex) {
          }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/schema/IcebergTableSchema.java`
#### Snippet
```java
      throw new IcebergMetastoreException(String.format(
        "Some of partition fields are missing in the class [%s]. Partition keys: %s. Partition values: %s.",
        clazz.getCanonicalName(), partitionKeys, Arrays.asList(partitionSpecSchemaFields)));
    }

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/PhoenixSchemaFactory.java`
#### Snippet
```java
      while (rs.next()) {
        final String schemaName = rs.getString(1); // lookup the schema (or called database).
        PhoenixSchema schema = new PhoenixSchema(schemaConfig, plugin, Arrays.asList(getName()), schemaName);
        schemaMap.put(schemaName, schema);
      }
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `common/src/main/java/org/apache/drill/common/scanner/BuildTimeScan.java`
#### Snippet
```java
    ScanResult preScanned = loadExcept(url);
    ScanResult scan = ClassPathScanner.scan(
        asList(url),
        packagePrefixes,
        baseClasses,
```

## RuleId[id=IntegerDivisionInFloatingPointContext]
### IntegerDivisionInFloatingPointContext
`columns.size() / statsCalculator.getColsPerRow()`: integer division in floating-point context
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseGroupScan.java`
#### Snippet
```java
    long rowCount = scanSizeInBytes / statsCalculator.getAvgRowSizeInBytes();
    // the following calculation is not precise since 'columns' could specify CFs while getColsPerRow() returns the number of qualifier
    float diskCost = scanSizeInBytes * ((columns == null || columns.isEmpty()) ? 1 : columns.size() / statsCalculator.getColsPerRow());
    // if filter push down is used, reduce estimated row count and disk cost by half to ensure plan cost will be less then without it
    if (hbaseScanSpec.getFilter() != null) {
```

### IntegerDivisionInFloatingPointContext
`size / 1024`: integer division in floating-point context
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
    }

    final double k = size / 1024;
    if (k >= 1) {
      return intformat.format(k).concat("KB");
```

### IntegerDivisionInFloatingPointContext
`memoryCost/fragment.getAssignedEndpoints().size()`: integer division in floating-point context
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/MemoryCalculator.java`
#### Snippet
```java
      // minor fragments. Divide this memory estimation by fragment width to get the memory
      // requirement per minor fragment.
      long memoryCostPerMinorFrag = (int) Math.ceil(memoryCost/fragment.getAssignedEndpoints().size());
      Map<DrillbitEndpoint, Integer> drillbitEndpointMinorFragMap = getMinorFragCountPerDrillbit(fragment);

```

### IntegerDivisionInFloatingPointContext
`(memoryInBytes / 1024L) / 1024L`: integer division in floating-point context
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/NodeResources.java`
#### Snippet
```java

  public long getMemoryInMB() {
    return Math.round((memoryInBytes / 1024L) / 1024L);
  }

```

### IntegerDivisionInFloatingPointContext
`memoryInBytes / 1024L`: integer division in floating-point context
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/NodeResources.java`
#### Snippet
```java

  public long getMemoryInMB() {
    return Math.round((memoryInBytes / 1024L) / 1024L);
  }

```

### IntegerDivisionInFloatingPointContext
`getMemoryInMB() / 1024L`: integer division in floating-point context
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/NodeResources.java`
#### Snippet
```java

  public long getMemoryInGB() {
    return Math.round(getMemoryInMB() / 1024L);
  }

```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`ignoreSign == true` can be simplified to 'ignoreSign'
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
    int startIndex = index * DecimalUtility.INTEGER_SIZE;

    if (index == 0 && ignoreSign == true) {
      return (b[startIndex + 3] & 0xFF) |
             (b[startIndex + 2] & 0xFF) << 8 |
```

### PointlessBooleanExpression
`sign == true` can be simplified to 'sign'
in `exec/vector/src/main/java/org/apache/drill/exec/expr/fn/impl/ByteFunctionHelpers.java`
#### Snippet
```java
  public static void setSign(byte[] b, boolean sign) {
    int value = getInteger(b, 0);
    if (sign == true) {
      setInteger(b, 0, value | 0x80000000);
    } else {
```

### PointlessBooleanExpression
`false || rawActualType == java.lang.Boolean.class || rawActualType == java.lang....` can be simplified to 'rawActualType == java.lang.Boolean.class \|\| rawActualType == java.lang.Byte.class \|\| rawActualType == java.lang.Double.class \|\| rawActualType == java.lang.Float.class \|\| rawActualType == java.lang.Integer.class \|\| rawActualType == java.lang.Long.class \|\| rawActualType == java.lang.Short.class \|\| rawActualType == java.math.BigDecimal.class \|\| rawActualType == java.lang.Class.class \|\| rawActualType == java.sql.Date.class \|\| rawActualType == java.sql.Timestamp.class'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type seen and whose toString() renders value well.
          false
          || rawActualType == java.lang.Boolean.class
          || rawActualType == java.lang.Byte.class
          || rawActualType == java.lang.Double.class
          || rawActualType == java.lang.Float.class
          || rawActualType == java.lang.Integer.class
          || rawActualType == java.lang.Long.class
          || rawActualType == java.lang.Short.class
          || rawActualType == java.math.BigDecimal.class
          || rawActualType == java.lang.Class.class
          || rawActualType == java.sql.Date.class
          || rawActualType == java.sql.Timestamp.class
          ) {
        result = value.toString();
```

### PointlessBooleanExpression
`false || rawActualType == java.util.Properties.class || rawActualType.isEnum()` can be simplified to 'rawActualType == java.util.Properties.class \|\| rawActualType.isEnum()'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
          // Is type seen and whose toString() has rendered value well--in cases
          // seen so far.
          false
          || rawActualType == java.util.Properties.class
          || rawActualType.isEnum()
          ) {
        result = value.toString();
```

### PointlessBooleanExpression
`false || rawActualType == org.apache.drill.jdbc.DrillResultSet.class` can be simplified to 'rawActualType == org.apache.drill.jdbc.DrillResultSet.class'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type to warn about (one case).
          false
          || rawActualType == org.apache.drill.jdbc.DrillResultSet.class
          ) {
        printWarningLine(
```

### PointlessBooleanExpression
`false // Note: Using strings rather than compiled-in class references to // avo...` can be simplified to 'rawActualType.getName().equals( "org.apache.hadoop.io.Text" ) \|\| rawActualType.getName().equals( "org.joda.time.Period" ) \|\| rawActualType == org.apache.drill.exec.vector.accessor.sql.TimePrintMillis.class'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      else if (
          // Is type to warn about (second case).
          false
          // Note:  Using strings rather than compiled-in class references to
          // avoid failing when run using JDBC-all Jar, which excludes
          // org.apache.hadoop.io.Text.
          // Note:  org.apache.hadoop.io.Text should no longer appear (see
          // DRILL-3347, but leaving warning in for now in case Text returns).
          || rawActualType.getName().equals( "org.apache.hadoop.io.Text" )
          || rawActualType.getName().equals( "org.joda.time.Period" )
          || rawActualType ==
             org.apache.drill.exec.vector.accessor.sql.TimePrintMillis.class
          ) {
        printWarningLine( "Should " + rawActualType
```

### PointlessBooleanExpression
`e.getBoolean() == false` can be simplified to '!e.getBoolean()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
    @Override
    public ValueHolder visitBooleanConstant(ValueExpressions.BooleanExpression e,Integer value) throws RuntimeException {
      return ValueHolderHelper.getBitHolder(e.getBoolean() == false ? 0 : 1);
    }

```

### PointlessBooleanExpression
`true || planHasMemory` can be simplified to 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/ThrottledResourceManager.java`
#### Snippet
```java
    public QueryParallelizer getParallelizer(boolean planHasMemory) {
      // currently memory planning is disabled. Enable it once the RM functionality is fully implemented.
      return new QueueQueryParallelizer(true || planHasMemory, this.getQueryContext());
    }

```

### PointlessBooleanExpression
`metadata.tableModified(metadataDirs.getDirectories(), summaryFile, metadataParentDir, metaContext, f...` can be simplified to 'metadata.tableModified(metadataDirs.getDirectories(), summaryFile, metadataParentDir, metaContext, fs)'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata.java`
#### Snippet
```java
          }
          ParquetTableMetadataDirs metadataDirs  = readMetadataDirs(fs, metadataDirFile, metaContext, readerConfig);
          if (metadata.tableModified(metadataDirs.getDirectories(), summaryFile, metadataParentDir, metaContext, fs) && true) {
            ParquetTableMetadata_v4 parquetTableMetadata = (metadata.createMetaFilesRecursivelyAsProcessUser(Path.getPathWithoutSchemeAndAuthority(summaryFile.getParent()), fs, true, null, true)).getLeft();
            return parquetTableMetadata.getSummary();
```

### PointlessBooleanExpression
`info.hasIndexCol == false` can be simplified to '!info.hasIndexCol'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
        }
        IndexConditionInfo info = indexConditionRelatedToFields(index.getIndexColumns(), initCondition);
        if (info == null || info.hasIndexCol == false) {
          continue;
        }
```

### PointlessBooleanExpression
`info.hasIndexCol == false` can be simplified to '!info.hasIndexCol'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
        leadingColumns.add(index.getIndexColumns().get(0));
        IndexConditionInfo info = indexConditionRelatedToFields(leadingColumns, initCondition);
        if (info == null || info.hasIndexCol == false) {
          // No info found, based on remaining condition. Check if the leading columns are same as another index
          IndexConditionInfo origInfo = indexConditionRelatedToFields(leadingColumns, condition);
```

### PointlessBooleanExpression
`origInfo.hasIndexCol == false` can be simplified to '!origInfo.hasIndexCol'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexConditionInfo.java`
#### Snippet
```java
          // No info found, based on remaining condition. Check if the leading columns are same as another index
          IndexConditionInfo origInfo = indexConditionRelatedToFields(leadingColumns, condition);
          if (origInfo == null || origInfo.hasIndexCol == false) {
            // do nothing
          } else {
```

### PointlessBooleanExpression
`bret == false` can be simplified to '!bret'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/PathInExpr.java`
#### Snippet
```java
    for (LogicalExpression arg : call.args()) {
      bret &= arg.accept(this, null);
      if(bret == false) {
        break;
      }
```

### PointlessBooleanExpression
`expr.accept(exprSearch, null) == false` can be simplified to '!expr.accept(exprSearch, null)'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java

    for (LogicalExpression expr: exprs) {
      if (expr.accept(exprSearch, null) == false) {
        return false;
      }
```

### PointlessBooleanExpression
`first == false` can be simplified to '!first'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unpivot/UnpivotMapsRecordBatch.java`
#### Snippet
```java

      case OK:
        assert first == false : "First batch should be OK_NEW_SCHEMA";
        container.zeroVectors();
        IterOutcome out = doWork();
```

### PointlessBooleanExpression
`first == false` can be simplified to '!first'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsMergeBatch.java`
#### Snippet
```java
          //fall through
        case OK:
          assert first == false : "First batch should be OK_NEW_SCHEMA";
          IterOutcome out = doWork();
          didSomeWork = true;
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    final IntHolder ih = new IntHolder();
    ih.value = readInt32;
    if (isList == false) {
      writer.integer(fieldName).write(ih);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    DateTime dateTime = new DateTime(timestamp*1000L);
    TimeStampWriter t;
    if (isList == false) {
      t = writer.map.timeStamp(fieldName);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    vh.start = 0;
    vh.end = length;
    if (isList == false) {
      writer.varChar(fieldName).write(vh);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    final BigIntHolder bh = new BigIntHolder();
    bh.value = readInt64;
    if (isList == false) {
      writer.bigInt(fieldName).write(bh);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    DateTime date = new DateTime(readDateTime);
    TimeStampWriter dt;
    if (isList == false) {
      dt = writer.map.timeStamp(fieldName);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    vb.start = 0;
    vb.end = bytes.length;
    if (isList == false) {
      writer.binary(fieldName).write(vb);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    final BitHolder bit = new BitHolder();
    bit.value = readBoolean ? 1 : 0;
    if (isList == false) {
      writer.bit(fieldName).write(bit);
    } else {
```

### PointlessBooleanExpression
`isList == false` can be simplified to '!isList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    final Float8Holder f8h = new Float8Holder();
    f8h.value = readDouble;
    if (isList == false) {
      writer.float8(fieldName).write(f8h);
    } else {
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
        if (filters != null) {
          for (Map.Entry<String, String> filter : filters.entrySet()) {
            json.put(filter.getKey(), filter.getValue());
          }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `logical/src/main/java/org/apache/drill/common/expression/IfExpression.java`
#### Snippet
```java
      Set<MinorType> subtypes = Sets.newHashSet();
      for (MinorType subtype : elseType.getSubTypeList()) {
        subtypes.add(subtype);
      }
      for (MinorType subtype : ifType.getSubTypeList()) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `logical/src/main/java/org/apache/drill/common/expression/IfExpression.java`
#### Snippet
```java
      }
      for (MinorType subtype : ifType.getSubTypeList()) {
        subtypes.add(subtype);
      }
      MajorType.Builder builder = MajorType.newBuilder().setMinorType(MinorType.UNION).setMode(DataMode.OPTIONAL);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/store/CachingTransientStoreFactory.java`
#### Snippet
```java
    final List<AutoCloseable> closeables = Lists.newArrayList();
    for(final AutoCloseable store : cache.values()){
      closeables.add(store);
    }
    closeables.add(delegate);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/WritableBatch.java`
#### Snippet
```java

      for (DrillBuf b : vv.getBuffers(true)) {
        buffers.add(b);
      }
      // remove vv access to buffers.
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/SystemOptionManager.java`
#### Snippet
```java
    // populate the default options
    for (final Map.Entry<String, OptionValue> entry : defaults.entrySet()) {
      buildList.put(entry.getKey(), entry.getValue());
    }
    // override if changed
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/PrelSequencer.java`
#### Snippet
```java
          List<Prel> children = Lists.reverse(Lists.newArrayList(p.iterator()));
          for (Prel child : children) {
            ops.add(child);
          }
        }
```

## RuleId[id=OverwrittenKey]
### OverwrittenKey
Duplicate Set element
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.VARDECIMAL);
    rule.add(MinorType.MONEY);
    rule.add(MinorType.TIMESTAMPTZ);
    rule.add(MinorType.FLOAT4);
    rule.add(MinorType.FLOAT8);
```

### OverwrittenKey
Duplicate Set element
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java
    rule.add(MinorType.TIME);
    rule.add(MinorType.TIMESTAMP);
    rule.add(MinorType.TIMESTAMPTZ);
    rule.add(MinorType.INTERVAL);
    rule.add(MinorType.INTERVALYEAR);
```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `!=`, not 'equals()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
      int rows = listener.await();
      System.out.println(String.format("%d record%s selected (%f seconds)", rows, rows > 1 ? "s" : "", (float) watch.elapsed(TimeUnit.MILLISECONDS) / (float) 1000));
      if (query != queries[queries.length - 1]) {
        System.out.println();
      }
```

### StringEquality
String values are compared using `==`, not 'equals()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
    if (w == 0) {
      sb.append("<tr"
          + (rowBackgroundColor == null || rowBackgroundColor == NO_BGCOLOR ? "" : " style=\"background-color:"+rowBackgroundColor+"\"")
          + ">");
    }
```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
      if (config != null ) {
        String discoverClassName = config.getValue(INDEX_DISCOVER_CLASS_KEY);
        if(discoverClassName!= null && discoverClassName != "") {
          discoverClass = Class.forName(discoverClassName);
        }
```

## RuleId[id=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
      // Unwrap exception
      Throwable ex = e;
      while (true) {
        // Case for failing on an invalid cached connection
        if (ex instanceof MetaException ||
```

## RuleId[id=ExternalizableWithoutPublicNoArgConstructor]
### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `CachedVectorContainer` has no 'public' no-arg constructor
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/CachedVectorContainer.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class CachedVectorContainer extends LoopedAbstractDrillSerializable {
  private static final Logger logger = LoggerFactory.getLogger(CachedVectorContainer.class);

```

### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `VectorAccessibleSerializable` has no 'public' no-arg constructor
in `exec/java-exec/src/main/java/org/apache/drill/exec/cache/VectorAccessibleSerializable.java`
#### Snippet
```java
 * VectorContainer.
 */
public class VectorAccessibleSerializable extends AbstractStreamSerializable {
  static final MetricRegistry metrics = DrillMetrics.getRegistry();
  static final String WRITER_TIMER = MetricRegistry.name(VectorAccessibleSerializable.class, "writerTime");
```

## RuleId[id=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedListVector.java`
#### Snippet
```java
    public void copyFromSafe(int fromIndex, int thisIndex, DelegateRepeatedVector from) {
      if (ephPair == null || ephPair.target != from) {
        ephPair = DelegateTransferPair.class.cast(from.makeTransferPair(this));
      }
      ephPair.copyValueSafe(fromIndex, thisIndex);
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedListVector.java`
#### Snippet
```java
  @Override
  public TransferPair makeTransferPair(ValueVector to) {
    final RepeatedListVector target = RepeatedListVector.class.cast(to);
    return new RepeatedListTransferPair(delegate.makeTransferPair(target.delegate));
  }
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedListVector.java`
#### Snippet
```java
    @Override
    public ValueVector getTo() {
      final DelegateRepeatedVector delegateVector = DelegateRepeatedVector.class.cast(delegate.getTo());
      return new RepeatedListVector(getField(), allocator, callBack, delegateVector);
    }
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/RepeatedListVector.java`
#### Snippet
```java
    @Override
    public TransferPair makeTransferPair(ValueVector target) {
      return new DelegateTransferPair(DelegateRepeatedVector.class.cast(target));
    }

```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/plain/PlainFactory.java`
#### Snippet
```java
            for (final Callback callback : callbacks) {
              if (callback instanceof NameCallback) {
                NameCallback.class.cast(callback).setName(userName);
                continue;
              }
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/security/plain/PlainFactory.java`
#### Snippet
```java
              }
              if (callback instanceof PasswordCallback) {
                PasswordCallback.class.cast(callback).setPassword(password.toCharArray());
                continue;
              }
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/PersistentStoreConfig.java`
#### Snippet
```java
  public boolean equals(Object obj) {
    if (obj instanceof PersistentStoreConfig) {
      final PersistentStoreConfig<?> other = PersistentStoreConfig.class.cast(obj);
      return Objects.equal(name, other.name)
          && Objects.equal(valueSerializer, other.valueSerializer)
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetColumnMetadata.java`
#### Snippet
```java

  FixedWidthRepeatedReader makeRepeatedFixedWidthReader(ParquetRecordReader reader) throws Exception {
    final RepeatedValueVector repeatedVector = RepeatedValueVector.class.cast(vector);
    ColumnReader<?> dataReader = ColumnReaderFactory.createFixedColumnReader(reader, true,
        column, columnChunkMetaData,
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/FixedWidthRepeatedReader.java`
#### Snippet
```java
  @Override
  public int capacity() {
    return BaseDataValueVector.class.cast(valueVec.getDataVector()).getBuffer().capacity();
  }

```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/FixedWidthRepeatedReader.java`
#### Snippet
```java
    valuesReadInCurrentPass = 0;
    pageReader.valuesReadyToRead = 0;
    dataReader.vectorData = BaseDataValueVector.class.cast(valueVec.getDataVector()).getBuffer();
    dataReader.valuesReadInCurrentPass = 0;
    repeatedGroupsReadInCurrentPass = 0;
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SimpleVectorWrapper.java`
#### Snippet
```java
    ValueVector vector = this.vector;
    for (int i = 1; i < ids.length; i++) {
      final AbstractMapVector mapLike = AbstractMapVector.class.cast(vector);
      if (mapLike == null) {
        return null;
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/HyperVectorWrapper.java`
#### Snippet
```java
      ValueVector vector = v;
      for (int i = 1; i < ids.length; i++) {
        final AbstractMapVector mapLike = AbstractMapVector.class.cast(vector);
        if (mapLike == null) {
          return null;
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/UnnestImpl.java`
#### Snippet
```java
  public void setUnnestField(RepeatedValueVector unnestField) {
    this.fieldToUnnest = unnestField;
    this.accessor = RepeatedValueVector.RepeatedAccessor.class.cast(unnestField.getAccessor());
  }

```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenTemplate.java`
#### Snippet
```java
  public void setFlattenField(RepeatedValueVector flattenField) {
    this.fieldToFlatten = flattenField;
    this.accessor = RepeatedValueVector.RepeatedAccessor.class.cast(flattenField.getAccessor());
  }

```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/UnnestRecordBatch.java`
#### Snippet
```java
      vector = new RepeatedMapVector(field, oContext.getAllocator(), null);
    } else {
      vector = RepeatedValueVector.class.cast(inVV);
    }
    unnest.setUnnestField(vector);
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/UnnestRecordBatch.java`
#### Snippet
```java
      //when incoming recordCount is 0, don't throw exception since the type being seen here is not solid
      ValueVector vv = new RepeatedMapVector(unnestField.getField(), oContext.getAllocator(), null);
      tp = RepeatedValueVector.class.cast(vv)
          .getTransferPair(reference.getAsNamePart().getName(), oContext.getAllocator());
    } else {
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/UnnestRecordBatch.java`
#### Snippet
```java
          .getTransferPair(reference.getAsNamePart().getName(), oContext.getAllocator());
    } else {
      ValueVector vvIn = RepeatedValueVector.class.cast(unnestField).getDataVector();
      // vvIn may be null because of fast schema return for repeated list vectors
      if (vvIn != null) {
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
      vector = new RepeatedMapVector(field, oContext.getAllocator(), null);
    } else {
      vector = RepeatedValueVector.class.cast(inVV);
    }
    flattener.setFlattenField(vector);
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
        MaterializedField outputField;
        if (expr instanceof ValueVectorReadExpression) {
          TypedFieldId id = ValueVectorReadExpression.class.cast(expr).getFieldId();
          ValueVector incomingVector = incoming.getValueAccessorById(
              id.getIntermediateClass(), id.getFieldIds()).getValueVector();
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
      //when incoming recordCount is 0, don't throw exception since the type being seen here is not solid
      ValueVector vv = new RepeatedMapVector(flattenField.getField(), oContext.getAllocator(), null);
      tp = RepeatedValueVector.class.cast(vv).getTransferPair(
          reference.getAsNamePart().getName(), oContext.getAllocator());
    } else {
```

### RedundantClassCall
Redundant call to `cast()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
          reference.getAsNamePart().getName(), oContext.getAllocator());
    } else {
      ValueVector vvIn = RepeatedValueVector.class.cast(flattenField).getDataVector();
      // vvIn may be null because of fast schema return for repeated list vectors
      if (vvIn != null) {
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS = "store.parquet.writer.logical_type_for_decimals";
  public static final OptionValidator PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS_VALIDATOR = new EnumeratedStringValidator(PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS,
      new OptionDescription("Parquet writer logical type for decimal; supported types \'fixed_len_byte_array\' and \'binary\'"),
      "fixed_len_byte_array", "binary");
  public static final String PARQUET_WRITER_FORMAT_VERSION = "store.parquet.writer.format_version";
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS = "store.parquet.writer.logical_type_for_decimals";
  public static final OptionValidator PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS_VALIDATOR = new EnumeratedStringValidator(PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS,
      new OptionDescription("Parquet writer logical type for decimal; supported types \'fixed_len_byte_array\' and \'binary\'"),
      "fixed_len_byte_array", "binary");
  public static final String PARQUET_WRITER_FORMAT_VERSION = "store.parquet.writer.format_version";
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS = "store.parquet.writer.logical_type_for_decimals";
  public static final OptionValidator PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS_VALIDATOR = new EnumeratedStringValidator(PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS,
      new OptionDescription("Parquet writer logical type for decimal; supported types \'fixed_len_byte_array\' and \'binary\'"),
      "fixed_len_byte_array", "binary");
  public static final String PARQUET_WRITER_FORMAT_VERSION = "store.parquet.writer.format_version";
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS = "store.parquet.writer.logical_type_for_decimals";
  public static final OptionValidator PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS_VALIDATOR = new EnumeratedStringValidator(PARQUET_WRITER_LOGICAL_TYPE_FOR_DECIMALS,
      new OptionDescription("Parquet writer logical type for decimal; supported types \'fixed_len_byte_array\' and \'binary\'"),
      "fixed_len_byte_array", "binary");
  public static final String PARQUET_WRITER_FORMAT_VERSION = "store.parquet.writer.format_version";
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PlannerSettings.java`
#### Snippet
```java
          new MinRangeDoubleValidator("planner.filter.min_selectivity_estimate_factor",
          0.0, 1.0, "planner.filter.max_selectivity_estimate_factor",
          new OptionDescription("Available as of Drill 1.8. Sets the minimum filter selectivity estimate to increase the parallelization of the major fragment performing a join. This option is useful for deeply nested queries with complicated predicates and serves as a workaround when statistics are insufficient or unavailable. The selectivity can vary between 0 and 1. The value of this option caps the estimated SELECTIVITY. The estimated ROWCOUNT is derived by multiplying the estimated SELECTIVITY by the estimated ROWCOUNT of the upstream operator. The estimated ROWCOUNT displays when you use the EXPLAIN PLAN INCLUDING ALL ATTRIBUTES FOR command. This option does not control the estimated ROWCOUNT of downstream operators (post FILTER). However, estimated ROWCOUNTs may change because the operator ROWCOUNTs depend on their downstream operators. The FILTER operator relies on the input of its immediate upstream operator, for example SCAN, AGGREGATE. If two filters are present in a plan, each filter may have a different estimated ROWCOUNT based on the immediate upstream operator\'s estimated ROWCOUNT."));
  public static final DoubleValidator FILTER_MAX_SELECTIVITY_ESTIMATE_FACTOR =
          new MaxRangeDoubleValidator("planner.filter.max_selectivity_estimate_factor",
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/common/types/TypeProtos.java`
#### Snippet
```java
      "\n\005UINT4\020\037\022\t\n\005UINT8\020 \022\022\n\016DECIMAL28DENSE\020!" +
      "\022\022\n\016DECIMAL38DENSE\020\"\022\010\n\004NULL\020%\022\020\n\014INTERV" +
      "ALYEAR\020&\022\017\n\013INTERVALDAY\020\'\022\010\n\004LIST\020(\022\022\n\016G" +
      "ENERIC_OBJECT\020)\022\t\n\005UNION\020*\022\016\n\nVARDECIMAL" +
      "\020+\022\010\n\004DICT\020,*4\n\010DataMode\022\014\n\010OPTIONAL\020\000\022\014" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/BitData.java`
#### Snippet
```java
      "om_filter_size_in_bytes\030\005 \003(\005\022\024\n\014probe_f" +
      "ields\030\006 \003(\t\022\020\n\010hj_op_id\030\007 \001(\005\022\025\n\rrf_iden" +
      "tifier\030\010 \001(\003\"\'\n\rAckWithCredit\022\026\n\016allowed" +
      "_credit\030\001 \001(\005*\210\001\n\007RpcType\022\r\n\tHANDSHAKE\020\000" +
      "\022\007\n\003ACK\020\001\022\013\n\007GOODBYE\020\002\022\024\n\020REQ_RECORD_BAT" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/BitControl.java`
#### Snippet
```java
      "\022\025\n\rfragment_json\030\010 \001(\t\022\025\n\rleaf_fragment" +
      "\030\t \001(\010\022*\n\nassignment\030\n \001(\0132\026.exec.Drillb" +
      "itEndpoint\022\'\n\007foreman\030\013 \001(\0132\026.exec.Drill" +
      "bitEndpoint\022\035\n\013mem_initial\030\014 \001(\003:\010200000" +
      "00\022\033\n\007mem_max\030\r \001(\003:\n2000000000\0221\n\013crede" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      "s.proto\032\022Coordination.proto\032\017SchemaDef.p" +
      "roto\"$\n\017UserCredentials\022\021\n\tuser_name\030\001 \001" +
      "(\t\"\'\n\007QueryId\022\r\n\005part1\030\001 \001(\020\022\r\n\005part2\030\002 " +
      "\001(\020\"\371\003\n\014DrillPBError\022\020\n\010error_id\030\001 \001(\t\022(" +
      "\n\010endpoint\030\002 \001(\0132\026.exec.DrillbitEndpoint" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      "\r\n\005start\030\002 \001(\003\0222\n\005state\030\003 \001(\0162#.exec.sha" +
      "red.QueryResult.QueryState\022\017\n\004user\030\004 \001(\t" +
      ":\001-\022\'\n\007foreman\030\005 \001(\0132\026.exec.DrillbitEndp" +
      "oint\022\024\n\014options_json\030\006 \001(\t\022\022\n\ntotal_cost" +
      "\030\007 \001(\001\022\025\n\nqueue_name\030\010 \001(\t:\001-\"\337\004\n\014QueryP" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      "\022$\n\004type\030\002 \001(\0162\026.exec.shared.QueryType\022\r" +
      "\n\005start\030\003 \001(\003\022\013\n\003end\030\004 \001(\003\022\r\n\005query\030\005 \001(" +
      "\t\022\014\n\004plan\030\006 \001(\t\022\'\n\007foreman\030\007 \001(\0132\026.exec." +
      "DrillbitEndpoint\0222\n\005state\030\010 \001(\0162#.exec.s" +
      "hared.QueryResult.QueryState\022\027\n\017total_fr" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserBitShared.java`
#### Snippet
```java
      "\003Jar\022\014\n\004name\030\001 \001(\t\022\032\n\022function_signature" +
      "\030\002 \003(\t\"W\n\013SaslMessage\022\021\n\tmechanism\030\001 \001(\t" +
      "\022\014\n\004data\030\002 \001(\014\022\'\n\006status\030\003 \001(\0162\027.exec.sh" +
      "ared.SaslStatus*5\n\nRpcChannel\022\017\n\013BIT_CON" +
      "TROL\020\000\022\014\n\010BIT_DATA\020\001\022\010\n\004USER\020\002*V\n\tQueryT" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      "ata.proto\032\020BitControl.proto\032\025ExecutionPr" +
      "otos.proto\"&\n\010Property\022\013\n\003key\030\001 \002(\t\022\r\n\005v" +
      "alue\030\002 \002(\t\"9\n\016UserProperties\022\'\n\nproperti" +
      "es\030\001 \003(\0132\023.exec.user.Property\"\267\001\n\020RpcEnd" +
      "pointInfos\022\014\n\004name\030\001 \001(\t\022\017\n\007version\030\002 \001(" +
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
      "dentifier_casing\030% \001(\0162\033.exec.user.Ident" +
      "ifierCasing\022\021\n\tread_only\030& \001(\010\022\023\n\013schema" +
      "_term\030\' \001(\t\022\034\n\024search_escape_string\030( \001(" +
      "\t\022#\n\033select_for_update_supported\030) \001(\010\022\032" +
      "\n\022special_characters\030* \001(\t\022\024\n\014sql_keywor" +
```

## RuleId[id=UnnecessaryLabelOnBreakStatement]
### UnnecessaryLabelOnBreakStatement
Unnecessary label `outside` on break statement
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/OjaiValueWriter.java`
#### Snippet
```java
          || event == EventType.END_MAP
          || event == EventType.END_ARRAY) {
        break outside;
      } else if (reader.inMap()) {
        fieldName = reader.getFieldName();
```

## RuleId[id=UnnecessaryLabelOnContinueStatement]
### UnnecessaryLabelOnContinueStatement
Unnecessary label `outside` on continue statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
        if (childSelection.isNeverValid()) {
          consumeEntireNextValue();
          continue outside;
        }

```

### UnnecessaryLabelOnContinueStatement
Unnecessary label `outside` on continue statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
      if (childSelection.isNeverValid()) {
        consumeEntireNextValue();
        continue outside;
      }

```

## RuleId[id=AssertWithSideEffects]
### AssertWithSideEffects
`assert` has side effects: ++outputCounts\[node.batchId\]
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
    assert outgoingPosition < OUTGOING_BATCH_SIZE
        : String.format("Outgoing position %d must be less than bath size %d", outgoingPosition, OUTGOING_BATCH_SIZE);
    assert ++outputCounts[node.batchId] <= inputCounts[node.batchId]
        : String.format("Stream %d input count: %d output count %d", node.batchId, inputCounts[node.batchId], outputCounts[node.batchId]);
    final int inIndex = (node.batchId << 16) + node.valueIndex;
```

### AssertWithSideEffects
`assert` has side effects: isAssertEnabled = ...
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
  static {
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }
```

### AssertWithSideEffects
`assert` has side effects: isAssertEnabled = ...
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
  static {
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }
```

### AssertWithSideEffects
`assert` has side effects: isAssertEnabled = ...
in `common/src/main/java/org/apache/drill/exec/util/AssertionUtil.java`
#### Snippet
```java
  static {
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java
   */
  public synchronized void clear() {
    processingNanos = 0l;
    setupNanos = 0l;
    waitNanos = 0l;
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java
  public synchronized void clear() {
    processingNanos = 0l;
    setupNanos = 0l;
    waitNanos = 0l;
    longMetrics.clear();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java
    processingNanos = 0l;
    setupNanos = 0l;
    waitNanos = 0l;
    longMetrics.clear();
    doubleMetrics.clear();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordIterator.java`
#### Snippet
```java
    }
    // Release all batches before current batch. [0 to startBatchPosition).
    final Map<Range<Long>,RecordBatchData> oldBatches = batches.subRangeMap(Range.closedOpen(0l, startBatchPosition)).asMapOfRanges();
    for (RecordBatchData rbd : oldBatches.values()) {
      rbd.clear();
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordIterator.java`
#### Snippet
```java
      rbd.clear();
    }
    batches.remove(Range.closedOpen(0l, startBatchPosition));
    markedInnerPosition = innerPosition;
    markedOuterPosition = outerPosition;
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillWindowRel.java`
#### Snippet
```java
    // for count(1).
    if (args.isEmpty()) {
      args.add(new ValueExpressions.LongExpression(1l));
    }
    LogicalExpression expr = new FunctionCall(call.getAggregation().getName().toLowerCase(), args, ExpressionPosition.UNKNOWN);
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/WindowPrel.java`
#### Snippet
```java
    // for count(1).
    if (args.isEmpty()) {
      args.add(new ValueExpressions.LongExpression(1l));
    }

```

### LongLiteralsEndingWithLowercaseL
'long' literal `16l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PlannerSettings.java`
#### Snippet
```java
  private static final long DEFAULT_MAX_OFF_HEAP_ALLOCATION_IN_BYTES = 256 * 1024 * 1024;
  // max off heap memory for planning (16G)
  private static final long MAX_OFF_HEAP_ALLOCATION_IN_BYTES = 16l * 1024 * 1024 * 1024;

  public static final OptionValidator CONSTANT_FOLDING = new BooleanValidator("planner.enable_constant_folding",
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java

  private void processPartitionerTasks(List<PartitionerTask> partitionerTasks, ExecutionException executionException) throws ExecutionException {
    long maxProcessTime = 0l;
    for (PartitionerTask partitionerTask : partitionerTasks) {
      ExecutionException e = partitionerTask.getException();
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/vector/src/main/java/org/apache/drill/exec/record/MaterializedField.java`
#### Snippet
```java

  @Override
  public MaterializedField clone() {
    return withPathAndType(name, getType());
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rel/PluginIntermediatePrel.java`
#### Snippet
```java

  @Override
  protected Object clone() throws CloneNotSupportedException {
    return copy(getTraitSet(), getInputs());
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/EnumerableIntermediatePrel.java`
#### Snippet
```java

  @Override
  protected Object clone() {
    return copy(getTraitSet(), getInputs());
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/VertexDrel.java`
#### Snippet
```java

  @Override
  protected Object clone() {
    return copy(getTraitSet(), getInputs());
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java

  @Override
  public BatchSchema clone() {
    List<MaterializedField> newFields = Lists.newArrayList();
    newFields.addAll(fields);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/selection/SelectionVector2.java`
#### Snippet
```java

  @Override
  public SelectionVector2 clone() {
    SelectionVector2 newSV = new SelectionVector2(allocator);
    newSV.recordCount = recordCount;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ScanFieldDeterminer.java`
#### Snippet
```java

    @Override
    public FieldList clone() {
      FieldList newList = new FieldList();
      for (SchemaPath p : projected) {
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/ScanPrel.java`
#### Snippet
```java

  @Override
  protected Object clone() throws CloneNotSupportedException {
    return new ScanPrel(this.getCluster(), this.getTraitSet(), getCopy(this.getGroupScan()),
        this.rowType, this.getTable());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DirectScanPrel.java`
#### Snippet
```java

  @Override
  protected Object clone() throws CloneNotSupportedException {
    return new DirectScanPrel(this.getCluster(), this.getTraitSet(), getCopy(this.getGroupScan()),
            this.rowType);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/rules/PhoenixIntermediatePrel.java`
#### Snippet
```java

  @Override
  protected Object clone() throws CloneNotSupportedException {
    return copy(getTraitSet(), getInputs());
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaPartitionScanSpec.java`
#### Snippet
```java

  @Override
  public KafkaPartitionScanSpec clone() {
    return new KafkaPartitionScanSpec(topicName, partitionId, startOffset, endOffset);
  }
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/JdbcIntermediatePrel.java`
#### Snippet
```java

  @Override
  protected Object clone() throws CloneNotSupportedException {
    return copy(getTraitSet(), getInputs());
  }
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
      Class<? extends FSDataInputStream> inputStreamClass = inputStream.getClass();
      readAhead =
          inputStreamClass.getMethod("adviseFile", new Class[] {adviceType, long.class, long.class});
    } catch (NoSuchMethodException e) {
      logger.info("Unable to call fadvise due to: {}", e.toString());
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/MappingSet.java`
#### Snippet
```java

  public MappingSet(GeneratorMapping mapping) {
    this("inIndex", "outIndex", new GeneratorMapping[] { mapping, mapping });
  }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `common/src/main/java/org/apache/drill/common/KerberosUtil.java`
#### Snippet
```java
        Class.forName("sun.security.krb5.Config");

    final Method getInstanceMethod = classRef.getMethod("getInstance", new Class[0]);
    final Object kerbConf = getInstanceMethod.invoke(classRef, new Object[0]);
    final Method getDefaultRealmMethod = classRef.getDeclaredMethod("getDefaultRealm", new Class[0]);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `common/src/main/java/org/apache/drill/common/KerberosUtil.java`
#### Snippet
```java

    final Method getInstanceMethod = classRef.getMethod("getInstance", new Class[0]);
    final Object kerbConf = getInstanceMethod.invoke(classRef, new Object[0]);
    final Method getDefaultRealmMethod = classRef.getDeclaredMethod("getDefaultRealm", new Class[0]);
    return (String) getDefaultRealmMethod.invoke(kerbConf, new Object[0]);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `common/src/main/java/org/apache/drill/common/KerberosUtil.java`
#### Snippet
```java
    final Method getInstanceMethod = classRef.getMethod("getInstance", new Class[0]);
    final Object kerbConf = getInstanceMethod.invoke(classRef, new Object[0]);
    final Method getDefaultRealmMethod = classRef.getDeclaredMethod("getDefaultRealm", new Class[0]);
    return (String) getDefaultRealmMethod.invoke(kerbConf, new Object[0]);
  }
```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      // 2. Converted-from types:
      case TINYINT:
        result = innerAccessor.getByte( rowOffset );
        break;
      case INT:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      // 1. Regular type:
      case INT:
        result = innerAccessor.getInt( rowOffset );
        break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      // 1. Regular type:
      case BIGINT:
        result = innerAccessor.getLong( rowOffset );
        break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
        break;
      case BIGINT:
        result = innerAccessor.getLong( rowOffset );
        break;
      case FLOAT4:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      // 2. Converted-from types:
      case INT:
        result = innerAccessor.getInt( rowOffset );
        break;
      case BIGINT:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      // 1. Regular type:
      case TINYINT:
        result = innerAccessor.getByte( rowOffset );
        break;

```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/TypeConvertingSqlAccessor.java`
#### Snippet
```java
      case DECIMAL38SPARSE:
      case VARDECIMAL:
        result = innerAccessor.getBigDecimal( rowOffset );
        break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillColumnMetaDataList.java`
#### Snippet
```java
        // Should REPEATED still map to columnNoNulls? or to columnNullable?
        case REPEATED:
          nullability = ResultSetMetaData.columnNoNulls;
          break;
        default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
          return Types.INTEGER;
        case "INTERVAL":
          return Types.OTHER;

        // Resolve: Not seen in Drill yet. Can it ever appear?:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

        case "MAP":
          return Types.OTHER;

        // Resolve: Not seen in Drill yet. Can it appear?:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
        // Resolve: Unexpectedly, has appeared in Drill. Should it?
        case "OTHER":
          return Types.OTHER;

        case "REAL":
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
        case STRING:
        case VARCHAR:
          return value.getBytes(StandardCharsets.UTF_8);
        case CHAR:
          return value.trim().getBytes(StandardCharsets.UTF_8);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/FilterPushDownUtils.java`
#### Snippet
```java
          break;
        case FunctionNames.AND:
          break;
        default:
          return null;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/JsonValueParser.java`
#### Snippet
```java
      case VALUE_STRING:
        //json.append("\"");
        json.append(textValue);
        //json.append("\"");
        break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
    switch (minorType) {
    case BIGINT:
      break;
    case BIT:
      generator = new BooleanGen();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DATE:
      break;
    case DECIMAL18:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL18:
      break;
    case DECIMAL28DENSE:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL28DENSE:
      break;
    case DECIMAL28SPARSE:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL28SPARSE:
      break;
    case DECIMAL38DENSE:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL38DENSE:
      break;
    case DECIMAL38SPARSE:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL38SPARSE:
      break;
    case VARDECIMAL:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case VARDECIMAL:
      break;
    case DECIMAL9:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case DECIMAL9:
      break;
    case FIXED16CHAR:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case FIXED16CHAR:
      break;
    case FIXEDBINARY:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case FIXEDBINARY:
      break;
    case FIXEDCHAR:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case FIXEDCHAR:
      break;
    case FLOAT4:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case FLOAT4:
      break;
    case FLOAT8:
      generator = new DoubleGen();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case GENERIC_OBJECT:
      break;
    case INT:
      generator = new IntGen();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case INTERVAL:
      break;
    case INTERVALDAY:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case INTERVALDAY:
      break;
    case INTERVALYEAR:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case INTERVALYEAR:
      break;
    case LATE:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case LATE:
      break;
    case LIST:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case LIST:
      break;
    case MAP:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case MAP:
      break;
    case MONEY:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case MONEY:
      break;
    case NULL:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case NULL:
      break;
    case SMALLINT:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case SMALLINT:
      break;
    case TIME:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case TIME:
      break;
    case TIMESTAMP:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case TIMESTAMP:
      break;
    case TIMESTAMPTZ:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case TIMESTAMPTZ:
      break;
    case TIMETZ:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case TIMETZ:
      break;
    case TINYINT:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case TINYINT:
      break;
    case UINT1:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case UINT1:
      break;
    case UINT2:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case UINT2:
      break;
    case UINT4:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case UINT4:
      break;
    case UINT8:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case UINT8:
      break;
    case UNION:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case UNION:
      break;
    case VAR16CHAR:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case VAR16CHAR:
      break;
    case VARBINARY:
      break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      break;
    case VARBINARY:
      break;
    case VARCHAR:
      generator = new StringGen();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/Records.java`
#### Snippet
```java
              break;
            case FLOAT:
              NUMERIC_PRECISION = 24;
              break;
            case DOUBLE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetColumnMetadata.java`
#### Snippet
```java
      case INT32:   return 32;
      case BOOLEAN: return 1;
      case FLOAT:   return 32;
      case DOUBLE:  return 64;
      case INT96:   return 96;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetColumnMetadata.java`
#### Snippet
```java
      case BOOLEAN: return 1;
      case FLOAT:   return 32;
      case DOUBLE:  return 64;
      case INT96:   return 96;
      // binary and fixed length byte array
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillValuesRelBase.java`
#### Snippet
```java
        // Converting exact decimal into double since values in the list may have different scales
        // so the resulting scale wouldn't be calculated correctly
        if (isLiteralNull(literal)) {
          out.writeDoubleNull();
        } else {
          out.writeDouble(((BigDecimal) literal.getValue()).doubleValue());
        }
        return;

      case VARCHAR:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillOptiq.java`
#### Snippet
```java
        return ValueExpressions.getFloat8(dbl);
      case VARCHAR:
        if (isLiteralNull(literal)) {
          return createStringNullExpr(literal.getType().getPrecision());
        }
        return ValueExpressions.getChar(((NlsString)literal.getValue()).getValue(), literal.getType().getPrecision());
      case SYMBOL:
        if (isLiteralNull(literal)) {
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/TopNBatch.java`
#### Snippet
```java
        break outer;
      case NOT_YET:
        throw new UnsupportedOperationException();
      case OK_NEW_SCHEMA:
        // only change in the case that the schema truly changes.  Artificial schema changes are ignored.
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/sort/SortBatch.java`
#### Snippet
```java
        break outer;
      case NOT_YET:
        throw new UnsupportedOperationException();
      case OK_NEW_SCHEMA:
        // only change in the case that the schema truly changes.  Artificial schema changes are ignored.
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenTemplate.java`
#### Snippet
```java
        throw new UnsupportedOperationException("Flatten does not support selection vector inputs.");
      case TWO_BYTE:
        throw new UnsupportedOperationException("Flatten does not support selection vector inputs.");
      default:
    }
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenTemplate.java`
#### Snippet
```java

      case TWO_BYTE:
        throw new UnsupportedOperationException("Flatten does not support selection vector inputs.");

      case NONE:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectorTemplate.java`
#### Snippet
```java
    switch (svMode) {
    case FOUR_BYTE:
      throw new UnsupportedOperationException();

    case TWO_BYTE:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/PullResultSetReaderImpl.java`
#### Snippet
```java
        return true;
      case BATCH:
        source.release();
        break;
      case CLOSED:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/ResultSetLoaderImpl.java`
#### Snippet
```java
        return true;
      case LIMITED:
        return true;
      default:
        return false;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    case 13:
      // JavaScript 13
      writeString(new String(data, StandardCharsets.UTF_8), writer, fieldName, isList);
      break;
    case 14:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    case 14:
      // Symbol 14
      writeString(new String(data, StandardCharsets.UTF_8), writer, fieldName, isList);
      break;
    case 15:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    case 15:
      // JavaScript (with scope) 15
      writeString(new String(data, StandardCharsets.UTF_8), writer, fieldName, isList);
      break;
    case 16:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java

      case INTERVAL:        return "INTERVAL";
      case MONEY:           return "DECIMAL";
      case TINYINT:         return "TINYINT";

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java

      case UINT1:          return "TINYINT";
      case UINT2:          return "SMALLINT";
      case UINT4:          return "INTEGER";
      case UINT8:          return "BIGINT";
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case UINT1:          return "TINYINT";
      case UINT2:          return "SMALLINT";
      case UINT4:          return "INTEGER";
      case UINT8:          return "BIGINT";

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case UINT2:          return "SMALLINT";
      case UINT4:          return "INTEGER";
      case UINT8:          return "BIGINT";

      default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
    switch (sqlTypeName) {
      case "ANY":                           return java.sql.Types.OTHER;
      case "ARRAY":                         return java.sql.Types.OTHER; // Drill doesn't support java.sql.Array
      case "BIGINT":                        return java.sql.Types.BIGINT;
      case "BINARY VARYING":                return java.sql.Types.VARBINARY;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "FLOAT":                         return java.sql.Types.FLOAT;
      case "INTEGER":                       return java.sql.Types.INTEGER;
      case "INTERVAL":                      return java.sql.Types.OTHER;  // JDBC (4.1) has nothing for INTERVAL
      case "INTERVAL YEAR TO MONTH":        return java.sql.Types.OTHER;
      case "INTERVAL DAY TO SECOND":        return java.sql.Types.OTHER;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "INTEGER":                       return java.sql.Types.INTEGER;
      case "INTERVAL":                      return java.sql.Types.OTHER;  // JDBC (4.1) has nothing for INTERVAL
      case "INTERVAL YEAR TO MONTH":        return java.sql.Types.OTHER;
      case "INTERVAL DAY TO SECOND":        return java.sql.Types.OTHER;
      case "STRUCT":                        return java.sql.Types.OTHER; // Drill doesn't support java.sql.Struct
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "INTERVAL":                      return java.sql.Types.OTHER;  // JDBC (4.1) has nothing for INTERVAL
      case "INTERVAL YEAR TO MONTH":        return java.sql.Types.OTHER;
      case "INTERVAL DAY TO SECOND":        return java.sql.Types.OTHER;
      case "STRUCT":                        return java.sql.Types.OTHER; // Drill doesn't support java.sql.Struct
      case "MAP":                           return java.sql.Types.OTHER;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "INTERVAL YEAR TO MONTH":        return java.sql.Types.OTHER;
      case "INTERVAL DAY TO SECOND":        return java.sql.Types.OTHER;
      case "STRUCT":                        return java.sql.Types.OTHER; // Drill doesn't support java.sql.Struct
      case "MAP":                           return java.sql.Types.OTHER;
      case "NATIONAL CHARACTER VARYING":    return java.sql.Types.NVARCHAR;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "INTERVAL DAY TO SECOND":        return java.sql.Types.OTHER;
      case "STRUCT":                        return java.sql.Types.OTHER; // Drill doesn't support java.sql.Struct
      case "MAP":                           return java.sql.Types.OTHER;
      case "NATIONAL CHARACTER VARYING":    return java.sql.Types.NVARCHAR;
      case "NATIONAL CHARACTER":            return java.sql.Types.NCHAR;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "MAP":                           return java.sql.Types.OTHER;
      case "NATIONAL CHARACTER VARYING":    return java.sql.Types.NVARCHAR;
      case "NATIONAL CHARACTER":            return java.sql.Types.NCHAR;
      case "NULL":                          return java.sql.Types.NULL;
      case "SMALLINT":                      return java.sql.Types.SMALLINT;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      case "TIMESTAMP":                     return java.sql.Types.TIMESTAMP;
      case "TINYINT":                       return java.sql.Types.TINYINT;
      case "UNION":                         return java.sql.Types.OTHER;
      case "JAVA_OBJECT":                   return java.sql.Types.JAVA_OBJECT;
      default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      return 9;
    case VARDECIMAL:
      return 38;
    default:
      return 0;
```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `cmd` in enum 'Command'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java
    CLEAN( "clean", "Remove archives stored in DFS." );

    private String cmd;
    private String descrip;

```

### NonFinalFieldInEnum
Non-final field `descrip` in enum 'Command'
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java

    private String cmd;
    private String descrip;

    private Command(String cmd, String descrip) {
```

### NonFinalFieldInEnum
Non-final field `displayName` in enum 'TrackingState'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java
    END_ACK( "Deregistered" );

    private String displayName;

    private TrackingState( String displayName ) {
```

### NonFinalFieldInEnum
Non-final field `outputWidthCalculator` in enum 'OutputWidthCalculatorType'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/annotations/FunctionTemplate.java`
#### Snippet
```java
    // size  does not easily map to the input size but is likely to be at most the size of the input.
    CUSTOM_CLONE_DEFAULT(OutputWidthCalculators.CloneOutputWidthCalculator.INSTANCE);
    OutputWidthCalculator outputWidthCalculator;

    OutputWidthCalculatorType(OutputWidthCalculator outputWidthCalculator) {
```

### NonFinalFieldInEnum
Non-final field `exceptionType` in enum 'ExceptionType'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/SqlUnsupportedException.java`
#### Snippet
```java
    FUNCTION(UnsupportedFunctionException.class.getSimpleName());

    private String exceptionType;
    ExceptionType(String exceptionType) {
      this.exceptionType = exceptionType;
```

### NonFinalFieldInEnum
Non-final field `propValue` in enum 'ImplicitFileColumns'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ColumnExplorer.java`
#### Snippet
```java

    String optionName;
    String propValue;

    ImplicitFileColumns(String optionName, String propValue) {
```

### NonFinalFieldInEnum
Non-final field `optionName` in enum 'ImplicitFileColumns'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ColumnExplorer.java`
#### Snippet
```java
    };

    String optionName;
    String propValue;

```

### NonFinalFieldInEnum
Non-final field `NONE_AND_FOUR` in enum 'SelectionVectorMode'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java
    public static SelectionVectorMode[] DEFAULT = {NONE};
    public static SelectionVectorMode[] NONE_AND_TWO = {NONE, TWO_BYTE};
    public static SelectionVectorMode[] NONE_AND_FOUR = {NONE, FOUR_BYTE};
    public static SelectionVectorMode[] ALL = {NONE, TWO_BYTE, FOUR_BYTE};
  }
```

### NonFinalFieldInEnum
Non-final field `NONE_AND_TWO` in enum 'SelectionVectorMode'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/BatchSchema.java`
#### Snippet
```java

    public static SelectionVectorMode[] DEFAULT = {NONE};
    public static SelectionVectorMode[] NONE_AND_TWO = {NONE, TWO_BYTE};
    public static SelectionVectorMode[] NONE_AND_FOUR = {NONE, FOUR_BYTE};
    public static SelectionVectorMode[] ALL = {NONE, TWO_BYTE, FOUR_BYTE};
```

### NonFinalFieldInEnum
Non-final field `scopes` in enum 'AccessibleScopes'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
    ALL(EnumSet.of(OptionScope.BOOT, OptionScope.SYSTEM, OptionScope.SESSION, OptionScope.QUERY)); // Can be configured at the system, session, or query level

    private EnumSet<OptionScope> scopes;

    AccessibleScopes(EnumSet<OptionScope> scopes) {
```

### NonFinalFieldInEnum
Non-final field `hasExactColumnValueCount` in enum 'GroupScanProperty'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/ScanStats.java`
#### Snippet
```java
    ESTIMATED_TOTAL_COST(false, true);

    private boolean hasExactRowCount, hasExactColumnValueCount;

    GroupScanProperty (boolean hasExactRowCount, boolean hasExactColumnValueCount) {
```

### NonFinalFieldInEnum
Non-final field `hasExactRowCount` in enum 'GroupScanProperty'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/ScanStats.java`
#### Snippet
```java
    ESTIMATED_TOTAL_COST(false, true);

    private boolean hasExactRowCount, hasExactColumnValueCount;

    GroupScanProperty (boolean hasExactRowCount, boolean hasExactColumnValueCount) {
```

### NonFinalFieldInEnum
Non-final field `columnName` in enum 'DefaultColumns'
in `contrib/storage-opentsdb/src/main/java/org/apache/drill/exec/store/openTSDB/client/Schema.java`
#### Snippet
```java
    AGGREGATED_VALUE("aggregated value");

    private String columnName;

    DefaultColumns(String name) {
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/DynamicColumn.java`
#### Snippet
```java
    // Dynamic columns carry no type information, just the
    // "DYNAMIC" marker.
    return new StringBuilder()
      .append("`")
      .append(escapeSpecialSymbols(name()))
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/VariantSchema.java`
#### Snippet
```java
  @Override
  public String toString() {
    return new StringBuilder()
        .append("[")
        .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/AbstractArrayWriter.java`
#### Snippet
```java
    @Override
    public String toString() {
      return new StringBuilder()
        .append("[")
        .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

  private MetaResultSet clientGetCatalogs() {
    StringBuilder sb = new StringBuilder();
    sb.append("select "
        + "CATALOG_NAME as TABLE_CAT "
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKClusterCoordinatorDriver.java`
#### Snippet
```java
  public static String formatKey(String host, int userPort, int controlPort,
      int dataPort) {
    StringBuilder buf = new StringBuilder();
    buf.append(host).append(":").append(userPort).append(':')
        .append(controlPort).append(':').append(dataPort);
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java

  public static String describeContainer(Container container) {
    StringBuilder buf = new StringBuilder()
        .append("[id: ")
        .append(container.getId())
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java

  public static String labelContainer(Container container) {
    StringBuilder buf = new StringBuilder()
        .append("[id: ")
        .append(container.getId())
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java
    @Override
    public String toString() {
      StringBuilder sb = new StringBuilder("Partition(");
      sb.append("values:");
      sb.append(this.values);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveTableWrapper.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder("Table(");

    sb.append("dbName:");
```

### StringBufferReplaceableByString
`StringBuilder errMsg` can be replaced with 'String'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
   */
  public static void throwUnsupportedHiveDataTypeError(String unsupportedType) {
    StringBuilder errMsg = new StringBuilder()
        .append("Unsupported Hive data type ").append(unsupportedType).append(". ")
        .append(System.lineSeparator())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/OperatorStats.java`
#### Snippet
```java

  public String getId() {
    return new StringBuilder()
        .append(this.operatorId)
        .append(":")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserClient.java`
#### Snippet
```java
    // should not happen
    catch(TimeoutException e) {
      String msg = new StringBuilder().append("Connecting to the server timed out. This is sometimes due to a " +
        "mismatch in the SSL configuration between" + " client and server. [ Exception: ").append(e.getMessage())
        .append("]").toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfig.java`
#### Snippet
```java
    } catch (Exception e) {
      throw new DrillException(
          new StringBuilder()
              .append("Exception while initializing the keystore: [")
              .append(e.getMessage())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfig.java`
#### Snippet
```java
      // Catch any SSL initialization Exceptions here and abort.
      throw new DrillException(
          new StringBuilder()
              .append("Exception while initializing the truststore: [")
              .append(e.getMessage())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfigServer.java`
#### Snippet
```java
    } catch (Exception e) {
      // Catch any SSL initialization Exceptions here and abort.
      throw new DrillException(new StringBuilder()
          .append("SSL is enabled but cannot be initialized - ")
          .append("[ ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfigServer.java`
#### Snippet
```java
      // Catch any SSL initialization Exceptions here and abort.
      throw new DrillException(
          new StringBuilder().append("SSL is enabled but cannot be initialized - ")
              .append("[ ")
              .append(e.getMessage())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfigClient.java`
#### Snippet
```java
    } catch (Exception e) {
      // Catch any SSL initialization Exceptions here and abort.
      throw new DrillException(new StringBuilder()
          .append("SSL is enabled but cannot be initialized due to the following exception: ")
          .append("[ ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfigClient.java`
#### Snippet
```java
    } catch (Exception e) {
      // Catch any SSL initialization Exceptions here and abort.
      throw new DrillException(new StringBuilder()
          .append("SSL is enabled but cannot be initialized due to the following exception: ")
          .append("[ ")
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java
   */
  public static String printAllocatorStats(BufferAllocator allocator) {
    StringBuilder msg = new StringBuilder();
    msg.append(BATCH_STATS_PREFIX);
    msg.append(": dumping allocator statistics:\n");
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/record/RecordBatchStats.java`
#### Snippet
```java
      }

      contextOperatorId = new StringBuilder()
        .append(getQueryId(context))
        .append(":")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/EmbeddedQueryQueue.java`
#### Snippet
```java
    @Override
    public String toString( ) {
      return new StringBuilder()
          .append("Embedded queue lease for ")
          .append(QueryIdHelper.getQueryId(queryId))
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
  @Override
  public String toString() {
    final StringBuilder builder = new StringBuilder();
    builder.append("FragmentExecutor [fragmentContext=");
    builder.append(fragmentContext);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/FunctionsIterator.java`
#### Snippet
```java
      //Generate a unique key for a function holder as 'functionName#functionSignature'
      //Bumping capacity from default 16 to 64 (since key is expected to be bigger, and reduce probability of resizing)
      String funcSignatureKey = new StringBuilder(64).append(name).append('#').append(signature).toString();
      functionMap.put(funcSignatureKey, new FunctionInfo(name, signature, returnType, jarName, isInternal));
    }
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/TokenIterator.java`
#### Snippet
```java
      token = "<unknown>";
    }
    return new StringBuilder()
        .append("line ")
        .append(location.getLineNr())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
    this.stats = parentColumnReader.parentReader.parquetReaderStats;
    this.fileName = path.toString();
    debugName = new StringBuilder()
       .append(this.parentColumnReader.parentReader.getFragmentContext().getFragIdString())
       .append(":")
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorPathBuilder.java`
#### Snippet
```java

  public String build() {
    StringBuffer sb = new StringBuffer();
    return sb.append(major).append("-").append(minor).append("-").append(operator).toString();
  }
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/TemplateClassDefinition.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder buf = new StringBuilder();
    buf.append("TemplateClassDefinition [interface=");
    buf.append((iface == null) ? "null" : iface.getName());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/NodeResource.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("CPU: ").append(cpu).append("Memory: ").append(memory);
    return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder s` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/PreProcessLogicalRel.java`
#### Snippet
```java
      final String bestMatch = ApproximateStringMatcher.getBestMatch(ops, newFunctionName);
      if (bestMatch != null && bestMatch.length() > functionName.length() && bestMatch.toLowerCase().startsWith("convert")) {
        final StringBuilder s = new StringBuilder("Did you mean ")
                .append(bestMatch.substring(functionName.length()))
                .append("?");
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/file/MetadataColumn.java`
#### Snippet
```java
  @Override
  public String toString() {
    return new StringBuilder()
        .append("[")
        .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/file/FileMetadataColumnDefn.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder buf = new StringBuilder()
      .append("[FileInfoColumnDefn name=\"")
      .append(colName)
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ResolvedTableColumn.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder buf = new StringBuilder();
    buf
      .append("[")
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ResolvedNullColumn.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder buf = new StringBuilder();
    buf
      .append("[")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ScanLevelProjection.java`
#### Snippet
```java
  @Override
  public String toString() {
    return new StringBuilder()
        .append("[")
        .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/rowSet/RowSetWriterImpl.java`
#### Snippet
```java
    @Override
    public String toString() {
      return new StringBuilder()
        .append("[")
        .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/impl/WriterIndexImpl.java`
#### Snippet
```java
  @Override
  public String toString() {
    return new StringBuilder()
      .append("[")
      .append(getClass().getSimpleName())
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/exception/SchemaChangeException.java`
#### Snippet
```java

  public static SchemaChangeException schemaChanged(String message, BatchSchema priorSchema, BatchSchema newSchema) {
    final String errorMsg = new StringBuilder()
        .append(message)
        .append("\n")
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/ResourcePoolTreeImpl.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("{ NodeResources: ").append(totalNodeResources.toString());
    sb.append(", ResourcePercent: ").append(resourceShare);
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/CommandLineOptions.java`
#### Snippet
```java
    } else {
      command = null;
      return;
    }
  }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfig.java`
#### Snippet
```java
      logger.debug("Initialized SSL context.");
    }
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
      }
    }
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
    }
    getInputStream().seek(this.startOffset);
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DynamicResourceManager.java`
#### Snippet
```java
    activeRm = null;
    if (ex == null) {
      return;
    } else if (ex instanceof UserException) {
      throw (UserException) ex;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DropboxFileSystem.java`
#### Snippet
```java
  private void updateTokens() {
    if (client == null || usesDeveloperToken) {
      return;
    } else if (dbxCredential.aboutToExpire()) {
      try {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
  private void updateTokens() {
    if (client == null || usesDeveloperToken) {
      return;
    } else if (client.canRefresh() && client.needsRefresh()) {
      if (!client.canRefresh()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/values/ScalarListener.java`
#### Snippet
```java

    if (listenerColumnMap == null || listenerColumnMap.isEmpty()) {
      return;
    } else if (listenerColumnMap.containsKey(key) && StringUtils.isNotEmpty(value)) {
      listenerColumnMap.put(key, value);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/values/ScalarListener.java`
#### Snippet
```java

    if (listenerColumnMap == null || listenerColumnMap.isEmpty()) {
      return;
    } else if (listenerColumnMap.containsKey(key) && value != null) {
      listenerColumnMap.put(key, value);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
      remainingChildren--;
    }
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
      // hit a single value, do nothing as the token was already read
      // in the switch statement
      return;
    }
  }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitColumnResolver.java`
#### Snippet
```java
      if (colType != null) {
        resolveTaggedColumn(parser, col, colType);
        return;
      } else if (col.column().isDynamic()) {
        matchByName(col);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggBatch.java`
#### Snippet
```java
        cg.getBlock("doSetup").assign(var, JExpr.direct("incoming").invoke("getSelectionVector2"));
        cg.getBlock("getVectorIndex")._return(var.invoke("getIndex").arg(JExpr.direct("recordIndex")));
        return;
      }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/single/BuildVectorsFromMetadata.java`
#### Snippet
```java
  private void populateList(ListVector vector, VariantMetadata variantSchema) {
    if (variantSchema.size() == 0) {
      return;
    } else if (variantSchema.size() == 1) {
      final ColumnMetadata subtype = variantSchema.listSubtype();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonRecordReader.java`
#### Snippet
```java
      super.setup(context, output);
    }
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcExceptionHandler.java`
#### Snippet
```java
    if(!ctx.channel().isOpen() || cause.getMessage().equals("Connection reset by peer")){
      logger.warn("Exception occurred with closed channel.  Connection: {}", connection.getName(), cause);
      return;
    }else{
      logger.error("Exception in RPC communication.  Connection: {}.  Closing connection.", connection.getName(), cause);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
//        logger.debug("Connection available and active, command now being run inline.");
      }
      return;

    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
  // Save the SslChannel after the SSL handshake so it can be closed later
  public void setSslChannel(Channel c) {
    return;
  }

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java

  protected void closeSSL() {
    return;
  }

```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`client != null ? !client.connectionIsActive() : false` can be simplified to 'client != null \&\& !client.connectionIsActive()'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillConnectionImpl.java`
#### Snippet
```java
        return true;
      } else {
        return client != null ? !client.connectionIsActive() : false;
      }
    } catch (SQLException e) {
```

### SimplifiableConditionalExpression
`(useAuth)
? AuthDynamicFeature.isUserLoggedIn(sc) : false` can be simplified to 'useAuth \&\& AuthDynamicFeature.isUserLoggedIn(sc)'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/PageTree.java`
#### Snippet
```java
    model.put("clusterName", config.getString(DrillOnYarnConfig.APP_NAME));
    boolean useAuth = AMSecurityManagerImpl.isEnabled();
    final boolean isUserLoggedIn = (useAuth)
        ? AuthDynamicFeature.isUserLoggedIn(sc) : false;
    model.put("showLogin", useAuth && !isUserLoggedIn);
```

### SimplifiableConditionalExpression
`distinct == null ? false : distinct` can be simplified to 'distinct!=null \&\& distinct'
in `logical/src/main/java/org/apache/drill/common/logical/data/Union.java`
#### Snippet
```java
          o.registerAsSubscriber(this);
      }
    this.distinct = distinct == null ? false : distinct;
  }

```

### SimplifiableConditionalExpression
`distinct == null ? false : distinct` can be simplified to 'distinct!=null \&\& distinct'
in `logical/src/main/java/org/apache/drill/common/logical/data/Except.java`
#### Snippet
```java
          o.registerAsSubscriber(this);
      }
    this.distinct = distinct == null ? false : distinct;
  }

```

### SimplifiableConditionalExpression
`distinct == null ? false : distinct` can be simplified to 'distinct!=null \&\& distinct'
in `logical/src/main/java/org/apache/drill/common/logical/data/Intersect.java`
#### Snippet
```java
          o.registerAsSubscriber(this);
      }
    this.distinct = distinct == null ? false : distinct;
  }

```

### SimplifiableConditionalExpression
`isArray ? loader.options().classicArrayNulls : false` can be simplified to 'isArray \&\& loader.options().classicArrayNulls'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/values/VarCharListener.java`
#### Snippet
```java
  public VarCharListener(JsonLoaderImpl loader, ScalarWriter writer) {
    super(loader, writer);
    classicArrayNulls = isArray ? loader.options().classicArrayNulls : false;
  }

```

### SimplifiableConditionalExpression
`skipFirstLine == null ? false : skipFirstLine` can be simplified to 'skipFirstLine!=null \&\& skipFirstLine'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/TextFormatConfig.java`
#### Snippet
```java
    this.escape = Strings.isNullOrEmpty(escape) ? '"' : escape.charAt(0);
    this.comment = Strings.isNullOrEmpty(comment) ? '#' : comment.charAt(0);
    this.skipFirstLine = skipFirstLine == null ? false : skipFirstLine;
    this.extractHeader = extractHeader == null ? false : extractHeader;
  }
```

### SimplifiableConditionalExpression
`extractHeader == null ? false : extractHeader` can be simplified to 'extractHeader!=null \&\& extractHeader'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/TextFormatConfig.java`
#### Snippet
```java
    this.comment = Strings.isNullOrEmpty(comment) ? '#' : comment.charAt(0);
    this.skipFirstLine = skipFirstLine == null ? false : skipFirstLine;
    this.extractHeader = extractHeader == null ? false : extractHeader;
  }

```

### SimplifiableConditionalExpression
`(extended == null) ? false : extended` can be simplified to 'extended!=null \&\& extended'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockTableDef.java`
#### Snippet
```java
      this.records = records;
      this.types = types;
      this.extended = (extended == null) ? false : extended;
      this.batchSize = (batchSize == null) ? 0 : batchSize;
      this.repeat = (repeat == null) ? 1 : repeat;
```

### SimplifiableConditionalExpression
`extended == null ? false : extended` can be simplified to 'extended!=null \&\& extended'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockSubScanPOP.java`
#### Snippet
```java
//    this.size = size;
    this.url = url;
    this.extended = extended == null ? false : extended;
  }

```

### SimplifiableConditionalExpression
`autoCorrectCorruptDates == null ? true : autoCorrectCorruptDates` can be simplified to 'autoCorrectCorruptDates == null \|\| autoCorrectCorruptDates'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatConfig.java`
#### Snippet
```java
    @JsonProperty("writerFormatVersion") String writerFormatVersion
  ) {
    this.autoCorrectCorruptDates = autoCorrectCorruptDates == null ? true : autoCorrectCorruptDates;
    this.enableStringsSignedMinMax = enableStringsSignedMinMax;
    this.blockSize = blockSize;
```

### SimplifiableConditionalExpression
`allProjected ? !Projections.excludeFromWildcard(columnSchema) : false` can be simplified to 'allProjected \&\& !Projections.excludeFromWildcard(columnSchema)'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/ImpliedTupleRequest.java`
#### Snippet
```java
  @Override
  public boolean isProjected(ColumnMetadata columnSchema) {
    return allProjected ? !Projections.excludeFromWildcard(columnSchema) : false;
  }

```

### SimplifiableConditionalExpression
`projectionType == TupleProjectionType.ALL ?
!Projections.excludeFromWildcard(columnSchema) : true` can be simplified to 'projectionType!=TupleProjectionType.ALL \|\| !Projections.excludeFromWildcard(columnSchema)'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/RequestedTupleImpl.java`
#### Snippet
```java
      return false;
    }
    return projectionType == TupleProjectionType.ALL ?
        !Projections.excludeFromWildcard(columnSchema) : true;
  }
```

### SimplifiableConditionalExpression
`projectionType == TupleProjectionType.ALL ? true : get(colName) != null` can be simplified to 'projectionType == TupleProjectionType.ALL \|\| get(colName) != null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/RequestedTupleImpl.java`
#### Snippet
```java
  @Override
  public boolean isProjected(String colName) {
    return projectionType == TupleProjectionType.ALL ? true : get(colName) != null;
  }

```

### SimplifiableConditionalExpression
`flattenStructuredData == null ? false : flattenStructuredData` can be simplified to 'flattenStructuredData!=null \&\& flattenStructuredData'
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogFormatConfig.java`
#### Snippet
```java
        ImmutableList.of() : ImmutableList.copyOf(extensions);
    this.maxErrors = maxErrors == null ? 10 : maxErrors;
    this.flattenStructuredData = flattenStructuredData == null ? false : flattenStructuredData;
  }

```

### SimplifiableConditionalExpression
`(data == null || data.length == 0) ? false : data[0] != 0x00` can be simplified to 'data!=null \&\& data.length!=0 \&\& data\[0\] != 0x00'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    case 8:
      // Boolean 8
      boolean boolValue = (data == null || data.length == 0) ? false : data[0] != 0x00;
      writeBoolean(boolValue, writer, fieldName, isList);
      break;
```

### SimplifiableConditionalExpression
`allTextMode == null ? false : allTextMode` can be simplified to 'allTextMode!=null \&\& allTextMode'
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java
    this.firstColumn = firstColumn == null ? 0 : firstColumn;
    this.lastColumn = lastColumn == null ? 0 : lastColumn;
    this.allTextMode = allTextMode == null ? false : allTextMode;
    this.ignoreErrors = ignoreErrors == null ? true : ignoreErrors;
    this.sheetName = sheetName == null ? "" : sheetName;
```

### SimplifiableConditionalExpression
`ignoreErrors == null ? true : ignoreErrors` can be simplified to 'ignoreErrors == null \|\| ignoreErrors'
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java
    this.lastColumn = lastColumn == null ? 0 : lastColumn;
    this.allTextMode = allTextMode == null ? false : allTextMode;
    this.ignoreErrors = ignoreErrors == null ? true : ignoreErrors;
    this.sheetName = sheetName == null ? "" : sheetName;
    this.maxArraySize = maxArraySize == null ? -1 : maxArraySize;
```

### SimplifiableConditionalExpression
`useTempFilePackageParts == null ? false : useTempFilePackageParts` can be simplified to 'useTempFilePackageParts!=null \&\& useTempFilePackageParts'
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java
    this.maxArraySize = maxArraySize == null ? -1 : maxArraySize;
    this.thresholdBytesForTempFiles = thresholdBytesForTempFiles == null ? -1 : thresholdBytesForTempFiles;
    this.useTempFilePackageParts = useTempFilePackageParts == null ? false : useTempFilePackageParts;
    validateConfig();
  }
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/TableStatsCalculator.java`
#### Snippet
```java
      if (rowsToSample > 0) {
        Scan scan = new Scan(hbaseScanSpec.getStartRow(), hbaseScanSpec.getStopRow());
        scan.setCaching(rowsToSample < DEFAULT_SAMPLE_SIZE ? rowsToSample : DEFAULT_SAMPLE_SIZE);
        scan.setMaxVersions(1);
        ResultScanner scanner = table.getScanner(scan);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/output/ConcatReturnTypeInference.java`
#### Snippet
```java
      }
    }
    return builder.setPrecision(totalPrecision > Types.MAX_VARCHAR_LENGTH ? Types.MAX_VARCHAR_LENGTH : totalPrecision).build();
  }
}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/IncomingBuffers.java`
#### Snippet
```java
  public int getRemainingRequired() {
    int rem = remainingRequired.get();
    if (rem < 0) {
      return 0;
    }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenAbstractPageEntryReader.java`
#### Snippet
```java
    int remaining = remainingPageData();
    int bufferCapacity = buffer.capacity() - VarLenBulkPageReader.PADDING;
    int toCopy = remaining > bufferCapacity ? bufferCapacity : remaining;

    buffer.limit(toCopy); // Update the limit regardless to indicate the number of bytes available for reading
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/BootStrapContext.java`
#### Snippet
```java
    final int numScanDecodeThreads = (int) config.getDouble(ExecConstants.SCAN_DECODE_THREADPOOL_SIZE);
    final int scanThreadPoolSize =
        MIN_SCAN_THREADPOOL_SIZE > numScanThreads ? MIN_SCAN_THREADPOOL_SIZE : numScanThreads;
    final int scanDecodeThreadPoolSize =
        (numCores + 1) / 2 > numScanDecodeThreads ? (numCores + 1) / 2 : numScanDecodeThreads;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/BootStrapContext.java`
#### Snippet
```java
        MIN_SCAN_THREADPOOL_SIZE > numScanThreads ? MIN_SCAN_THREADPOOL_SIZE : numScanThreads;
    final int scanDecodeThreadPoolSize =
        (numCores + 1) / 2 > numScanDecodeThreads ? (numCores + 1) / 2 : numScanDecodeThreads;
    this.scanExecutor = Executors.newFixedThreadPool(scanThreadPoolSize, new NamedThreadFactory("scan-"));
    this.scanDecodeExecutor =
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/TypeInferenceUtils.java`
#### Snippet
```java
      }

      totalPrecision = totalPrecision > Types.MAX_VARCHAR_LENGTH ? Types.MAX_VARCHAR_LENGTH : totalPrecision;
      boolean isNullable = isNullIfNull && isNullable(opBinding.collectOperandTypes());

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
    }
    // Cap selectivity if it exceeds 1.0
    return (sel > 1.0) ? 1.0 : sel;
  }

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
        }
        int bloomFilterSizeInBytes = BloomFilter.optimalNumOfBytes(ndv.longValue(), fpp);
        bloomFilterSizeInBytes = bloomFilterSizeInBytes > bloomFilterMaxSizeInBytesDef ? bloomFilterMaxSizeInBytesDef : bloomFilterSizeInBytes;
        //left the local parameter to be set later.
        BloomFilterDef bloomFilterDef = new BloomFilterDef(bloomFilterSizeInBytes, false, leftFieldName, rightFieldName);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerDecorator.java`
#### Snippet
```java
        if (enableParallelTaskExecution) {
          long currentProcessingNanos = localStats.getProcessingNanos();
          maxProcessTime = (currentProcessingNanos > maxProcessTime) ? currentProcessingNanos : maxProcessTime;
        } else {
          maxProcessTime += localStats.getWaitNanos();
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionSenderRootExec.java`
#### Snippet
```java
    }
    logger.info("Preliminary number of sending threads is: " + numberPartitions);
    this.actualPartitions = outGoingBatchCount > numberPartitions ? numberPartitions : outGoingBatchCount;
    this.stats.setLongStat(Metric.SENDING_THREADS_COUNT, actualPartitions);
    this.stats.setDoubleStat(Metric.COST, this.cost);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
    this.writers = new ArrayList<>();
    this.config = readerConfig;
    this.startingTableIndex = readerConfig.plugin.getConfig().defaultTableIndex() < 0 ? 0 : readerConfig.plugin.getConfig().defaultTableIndex();
    this.currentTableIndex = this.startingTableIndex;
    this.columnHeaders = new ArrayList<>();
```

## RuleId[id=MismatchedStringBuilderQueryUpdate]
### MismatchedStringBuilderQueryUpdate
Contents of `StringBuilder sb` are updated, but never queried
in `common/src/main/java/org/apache/drill/common/scanner/BuildTimeScan.java`
#### Snippet
```java
    if (result != null) {
      if (logger.isInfoEnabled()) {
        StringBuilder sb = new StringBuilder();
        sb.append(format("Loaded prescanned packages %s from locations:\n", result.getScannedPackages()));
        for (URL u : preScanned) {
```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpPushDownListener.java`
#### Snippet
```java
 * a string. (That is, not dates, binary, maps, etc.)</li>
 * <li>  A field from the URL.  For instance in some APIs you have parameters that are part of the path.
 * For example, https://github.com/orgs/{org}/repos.  In this instance, the query must contain
 * a parameter called org.
 * </li>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/OffsetPaginator.java`
#### Snippet
```java
  /**
   * This class implements the idea of an Offset Paginator. See here for complete explanation:
   * https://nordicapis.com/everything-you-need-to-know-about-api-pagination/
   * <p>
   *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
  /**
   * This function is used to extract the default parameter supplied in a URL. For instance,
   * if the supplied URL is http://someapi.com/path/{p1=foo}, the function will return foo. If there
   * is not a matching parameter or no default value, the function will return null.
   * @param url The URL containing a default parameter
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
  /**
   * APIs are sometimes structured with parameters in the URL itself.  For instance, to request a list of
   * an organization's repositories in github, the URL is: https://api.github.com/orgs/{org}/repos, where
   * you can replace the org with the actual organization name.
   *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
 * MurmurHash3 was written by Austin Appleby, and is placed in the public
 * domain.
 * See http://smhasher.googlecode.com/svn/trunk/MurmurHash3.cpp
 * MurmurHash3_x64_128
 * MurmurHash3_x86_32
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/BloomFilter.java`
#### Snippet
```java
  /**
   * Calculate optimal size according to the number of distinct values and false positive probability.
   * See http://en.wikipedia.org/wiki/Bloom_filter#Probability_of_false_positives for the formula.
   *
   * @param ndv The number of distinct values.
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   * If consistency flag is set to true and version holder is not null, passes version holder to get data change version.
   * Data change version is retrieved from {@link Stat} object, it increases each time znode data change is performed.
   * Link to Zookeeper documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
   *
   * @param path path to check
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   * If consistency flag is set to true and version holder is not null, passes version holder to get data change version.
   * Data change version is retrieved from {@link Stat} object, it increases each time znode data change is performed.
   * Link to Zookeeper documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
   *
   * @param path  target path
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
   * the update will fail {@link org.apache.zookeeper.KeeperException.BadVersionException}.
   * We catch such exception and re-throw it as {@link VersionMismatchException}.
   * Link to documentation - https://zookeeper.apache.org/doc/r3.2.2/zookeeperProgrammers.html#sc_zkDataModel_znodes
   *
   * @param path    target path
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
   * }
   * </pre>
   * (See for more details: https://github.com/apache/parquet-format/blob/master/LogicalTypes.md#lists)
   *
   * Note, that standard field names 'list' and 'element' aren't checked intentionally,
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/compression/DrillCompressionCodecFactory.java`
#### Snippet
```java
/**
 * A delegating compression codec factory that returns (de)compressors based on
 * https://github.com/airlift/aircompressor when possible and falls back to
 * parquet-mr otherwise.  The aircompressor lib was introduced into Drill
 * because of difficulties encountered with the JNI-based implementations of
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebServer.java`
#### Snippet
```java

  /**
   * Generate Options Description JavaScript to serve http://drillhost/options ACE library search features
   * @throws IOException when unable to generate functions JS file
   */
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckMethodVisitorFsm.java`
#### Snippet
```java
/**
 * A MethodVisitor that verifies the required call sequence according to
 * http://asm.ow2.org/asm50/javadoc/user/org/objectweb/asm/MethodVisitor.html .
 *
 * <p>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/FsmDescriptor.java`
#### Snippet
```java
 * visitors (classes like CheckClassAdapter only validate per-call
 * arguments, not the entire sequence of calls, according to
 * http://asm.ow2.org/asm50/javadoc/user/org/objectweb/asm/util/CheckClassAdapter.html
 *
 * <p>The current implementation is very simple, and requires some user setup.
```

### JavadocLinkAsPlainText
Link specified as plain text
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CheckClassVisitorFsm.java`
#### Snippet
```java
/**
 * A ClassVisitor that verifies the required call sequence described in
 * http://asm.ow2.org/asm50/javadoc/user/org/objectweb/asm/ClassVisitor.html .
 *
 * <p>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
   * +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+
   * </pre>
   * https://pcapng.github.io/pcapng/draft-tuexen-opsawg-pcapng.html#name-physical-file-layout
   */
  @Override
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/TimeBucketFunctions.java`
#### Snippet
```java
  /**
   * This function is used for facilitating time series analysis by creating buckets of time intervals.  See
   * https://blog.timescale.com/blog/simplified-time-series-analytics-using-the-time_bucket-function/ for usage. The function takes two arguments:
   * 1. The timestamp in milliseconds
   * 2. The desired bucket interval IN milliseconds
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/TimeBucketFunctions.java`
#### Snippet
```java
  /**
   * This function is used for facilitating time series analysis by creating buckets of time intervals.  See
   * https://blog.timescale.com/blog/simplified-time-series-analytics-using-the-time_bucket-function/ for usage. The function takes two arguments:
   * 1. The timestamp (as a Drill timestamp)
   * 2. The desired bucket interval IN milliseconds
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/TimeBucketFunctions.java`
#### Snippet
```java
  /**
   * This function is used for facilitating time series analysis by creating buckets of time intervals.  See
   * https://blog.timescale.com/blog/simplified-time-series-analytics-using-the-time_bucket-function/ for usage. The function takes two arguments:
   * 1. The timestamp in nanoseconds
   * 2. The desired bucket interval IN MILLISECONDS
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/TimeBucketFunctions.java`
#### Snippet
```java
  /**
   * This function is used for facilitating time series analysis by creating buckets of time intervals.  See
   * https://blog.timescale.com/blog/simplified-time-series-analytics-using-the-time_bucket-function/ for usage. The function takes two arguments:
   * 1. The timestamp (as a Drill timestamp)
   * 2. The desired bucket interval IN milliseconds
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java
  /**
   * This function calculates the Shannon Entropy of a given string of text, normed for the string length.
   * See: https://en.wikipedia.org/wiki/Entropy_(information_theory) for full definition.
   * <p>
   * Usage:
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java
  /**
   * This function calculates the Shannon Entropy of a given string of text.
   * See: https://en.wikipedia.org/wiki/Entropy_(information_theory) for full definition.
   * <p>
   * Usage:
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/PhoneticFunctions.java`
#### Snippet
```java

  /**
   * Implements the Metaphone phonetic algorithm (https://en.wikipedia.org/wiki/Metaphone),
   * and calculates a given string's Metaphone value.
   * <p>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/PhoneticFunctions.java`
#### Snippet
```java

  /**
   * Implements the Double Metaphone phonetic algorithm (https://en.wikipedia.org/wiki/Metaphone),
   * and calculates a given string's Double Metaphone value.
   * <p>
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/StringDistanceFunctions.java`
#### Snippet
```java
   * CharSequence's left and right respectively, the runtime of the algorithm is O(m*n).
   * <p>
   * This implementation is based on the Longest Commons Substring algorithm from https://en.wikipedia.org/wiki/Longest_common_subsequence_problem.
   * <p>
   * Usage:  SELECT longest_common_substring_distance( string1, string2 ) FROM...
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/StringDistanceFunctions.java`
#### Snippet
```java
   * <p>
   * For further explanation about the Hamming Distance, take a look at its
   * Wikipedia page at http://en.wikipedia.org/wiki/Hamming_distance.
   * <p>
   * Usage:  SELECT hamming_distance( string1, string2 ) FROM...
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/StringDistanceFunctions.java`
#### Snippet
```java
   * <p>
   * This implementation is based on the Jaro Winkler similarity algorithm
   * from https://en.wikipedia.org/wiki/Jaro–Winkler_distance
   * <p>
   * Usage: SELECT jaro_distance( string1, string2 ) FROM...
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/StringDistanceFunctions.java`
#### Snippet
```java
   * <p>
   * For further explanation about Jaccard Distance, refer
   * https://en.wikipedia.org/wiki/Jaccard_index
   * <p>
   * Usage:  SELECT jaccard_distance( string1, string2 ) FROM ...
```

### JavadocLinkAsPlainText
Link specified as plain text
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/CryptoFunctions.java`
#### Snippet
```java
  /**
   * sha(<text>) / sha1(<text>): Calculates an SHA-1 160-bit checksum for the string, as described in RFC 3174 (Secure Hash Algorithm).
   * (https://en.wikipedia.org/wiki/SHA-1) The value is returned as a string of 40 hexadecimal digits, or NULL if the argument was NULL.
   * Note that sha() and sha1() are aliases for the same function.
   *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `common/src/main/java/org/apache/drill/common/util/DrillStringUtils.java`
#### Snippet
```java
  /**
   * Copied form commons-lang 2.x code as common-lang 3.x has this API removed.
   * (http://commons.apache.org/proper/commons-lang/article3_0.html#StringEscapeUtils.escapeSql)
   * @param str
   * @return
```

## RuleId[id=NegativeIntConstantInLongContext]
### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/ArrayReaderImpl.java`
#### Snippet
```java
  public void reposition() {
    long entry = offsetReader.getEntry();
    elementIndex.reset((int) (entry >> 32), (int) (entry & 0xFFFF_FFFF));
  }

```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
    long end = buffer.memoryAddress() + bEnd;
    long length = bEnd - bStart;
    long roundedEnd = start + ( length & 0xFFFFFFF0);  // round down to 16 byte block
    for (long i=start; i<roundedEnd; i+=16) {
      long k1 = getLongLittleEndian(i);
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
    long UINT_MASK=0xffffffffL;
    long lh1 = seed;
    long roundedEnd = start + (length & 0xfffffffc);  // round down to 4 byte block

    for (long i=start; i<roundedEnd; i+=4) {
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java

    lh1 ^= lh1 >>> 16;
    lh1 *= 0x85ebca6b;
    lh1 = UINT_MASK & lh1;
    lh1 ^= lh1 >>> 13;
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/MurmurHash3.java`
#### Snippet
```java
    lh1 ^= lh1 >>> 13;

    lh1 *= 0xc2b2ae35;
    lh1 = UINT_MASK & lh1;
    lh1 ^= lh1 >>> 16;
```

### NegativeIntConstantInLongContext
Negative int hexadecimal constant in long context
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/XXHash.java`
#### Snippet
```java
   */
  private static int convert64To32(long val) {
    return (int) (val & 0x00FFFFFFFF);
  }

```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/TracingProxyDriver.java`
#### Snippet
```java
        + "+ proxied (original) JDBC URL";

    private final String classSpec;
    private final String proxiedURL;
    private final Driver proxiedDriverForProxiedUrl;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpApiConfig.java`
#### Snippet
```java
    private HttpPaginatorConfig paginator;

    private boolean directCredentials;

    public HttpApiConfig build() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenRepository.java`
#### Snippet
```java
  private final OkHttpClient client;
  private final OAuthTokenCredentials credentials;
  private final CredentialsProvider credentialsProvider;
  private HttpStoragePluginConfig pluginConfig;
  private PersistentTokenTable tokenTable;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/IndexPaginator.java`
#### Snippet
```java
  private String lastIndexValue;
  private String lastNextPageValue;
  private Boolean hasMoreValue;
  private String nextPageValue;
  private int pageCount;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
  private String amHost;
  @SuppressWarnings("unused")
  private int amPort;
  @SuppressWarnings("unused")
  private String amAppId;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
  private ZKClusterCoordinator zkCoord;
  @SuppressWarnings("unused")
  private String amHost;
  @SuppressWarnings("unused")
  private int amPort;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/AMRegistry.java`
#### Snippet
```java
  private int amPort;
  @SuppressWarnings("unused")
  private String amAppId;

  private String zkRoot;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/ResizeCommand.java`
#### Snippet
```java
public class ResizeCommand extends ClientCommand {
  private Config config;
  private YarnRMClient client;

  @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
  private String drillArchivePath;
  private String siteArchivePath;
  private boolean localized;

  @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
  private static final Log LOG = LogFactory.getLog(DrillControllerFactory.class);
  private Config config = DrillOnYarnConfig.config();
  private String drillArchivePath;
  private String siteArchivePath;
  private boolean localized;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/DrillControllerFactory.java`
#### Snippet
```java
  private Config config = DrillOnYarnConfig.config();
  private String drillArchivePath;
  private String siteArchivePath;
  private boolean localized;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private long lastFailureCheckTime;

  private int failureCheckPeriodMs = 60_000;

  private int taskCheckPeriodMs = 10_000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private int configPollPeriod = 60_000;
  private long nextResourcePollTime;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private int maxRequestsPerTick = 2;

  private int stopTimoutMs = 10_000;

  /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
   */

  private int maxRequestsPerTick = 2;

  private int stopTimoutMs = 10_000;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private int failureCheckPeriodMs = 60_000;

  private int taskCheckPeriodMs = 10_000;
  private long lastTaskCheckTime;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java

    private Map<TaskState,String> stateHints = makeStateHints( );
    private Map<TrackingState,String> trackingStateHints = makeTrackingStateHints( );

    public TaskModel(Task task, boolean live) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/http/AbstractTasksModel.java`
#### Snippet
```java
    protected int tryCount;

    private Map<TaskState,String> stateHints = makeStateHints( );
    private Map<TrackingState,String> trackingStateHints = makeTrackingStateHints( );

```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveScan.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(HiveScan.class);

  private static int HIVE_SERDE_SCAN_OVERHEAD_FACTOR_PER_COLUMN = 20;

  private final HiveStoragePlugin hiveStoragePlugin;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `logical/src/main/java/org/apache/drill/common/logical/data/Writer.java`
#### Snippet
```java

  // TODO: Hack - Make the type generic until "common" and "java-exec" modules are merged (DRILL-507).
  private final Object createTableEntry;

  @JsonCreator
```

### FieldCanBeLocal
Field can be converted to a local variable
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
    private List<NamedExpression> withins = Lists.newArrayList();
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
    private long end = Long.MIN_VALUE;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `logical/src/main/java/org/apache/drill/common/logical/data/Window.java`
#### Snippet
```java
    private List<Order.Ordering> orderings = Lists.newArrayList();
    private long start = Long.MIN_VALUE;
    private long end = Long.MIN_VALUE;


```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/FragmentContextImpl.java`
#### Snippet
```java
  };

  private final RpcOutcomeListener<Ack> statusHandler = new StatusHandler(exceptionConsumer, sendingAccountor);
  private final RpcOutcomeListener<BitData.AckWithCredit> dataTunnelStatusHandler = new DataTunnelStatusHandler(exceptionConsumer, sendingAccountor);
  private final AccountingUserConnection accountingUserConnection;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/ops/FragmentContextImpl.java`
#### Snippet
```java
  private ExecutorState executorState;
  private final ExecutionControls executionControls;
  private final boolean enableRuntimeFilter;
  private final boolean enableRFWaiting;
  private Lock lock4RF;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/IncomingBuffers.java`
#### Snippet
```java
  private final AtomicInteger remainingRequired;
  private final Map<Integer, DataCollector> collectorMap;
  private final FragmentContext context;

  /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/UnlimitedRawBatchBuffer.java`
#### Snippet
```java

  private final int softlimit;
  private final int startlimit;
  //which controls the receiver total credit when we enable the dynamic credit based flow control.
  private int runtimeSoftLimit = -1;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
  static final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(SpoolingRawBatchBuffer.class);

  private static String DRILL_LOCAL_IMPL_STRING = "fs.drill-local.impl";
  private static final float STOP_SPOOLING_FRACTION = (float) 0.5;
  public static final long ALLOCATOR_INITIAL_RESERVATION = 1*1024*1024;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DefaultResourceManager.java`
#### Snippet
```java

    @SuppressWarnings("unused")
    private final DefaultResourceManager rm;

    public DefaultQueryResourceManager(final DefaultResourceManager rm, final Foreman foreman) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DistributedResourceManager.java`
#### Snippet
```java
  private final ResourcePoolTree rmPoolTree;

  private final DrillbitContext context;

  private final DrillConfig rmConfig;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/rm/DistributedResourceManager.java`
#### Snippet
```java
  private final DrillbitContext context;

  private final DrillConfig rmConfig;

  private final ResourceManager delegatedRM;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/metadata/ServerMetaProvider.java`
#### Snippet
```java
    private final DrillbitContext context;
    @SuppressWarnings("unused")
    private final GetServerMetaReq req;
    private final ResponseSender responseSender;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java
  private final TransientStoreFactory factory;
  private ServiceCache<DrillbitEndpoint> serviceCache;
  private DrillbitEndpoint endpoint;

  // endpointsMap maps Multikey( comprises of endoint address and port) to Drillbit endpoints
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DropboxFileSystem.java`
#### Snippet
```java
  private DbxCredential dbxCredential;
  private DbxRequestConfig config;
  private FileStatus[] fileStatuses;
  private final Map<String,FileStatus> fileStatusCache = new HashMap<>();
  private boolean usesDeveloperToken;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatCreator.java`
#### Snippet
```java
  private final Map<String, FormatPlugin> configuredPlugins;
  /** The format plugin classes retrieved from classpath scanning */
  private final Collection<Class<? extends FormatPlugin>> pluginClasses;
  /** a Map from the FormatPlugin Config class to the constructor of the format plugin that accepts it.*/
  private final Map<Class<?>, Constructor<?>> configConstructors;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java
  private String prefix;

  private String extension;
  private boolean useExtendedOutput;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java

  private Path cleanUpLocation;
  private String location;
  private String prefix;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java

  private String extension;
  private boolean useExtendedOutput;

  private int index;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java
  private Path cleanUpLocation;
  private String location;
  private String prefix;

  private String extension;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/JsonStructureParser.java`
#### Snippet
```java
  private final RootParser rootState;
  private final FieldParserFactory fieldFactory;
  private final Map<String, Object> listenerColumnMap;
  private int errorRecoveryCount;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
  private final byte comment;

  private final long maxCharsPerColumn = TextFormatPlugin.MAX_CHARS_PER_COLUMN;
  private final byte normalizedNewLine = b('\n');
  private final byte[] newLineDelimiter;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java

  private final int ONE_DAY = 24 * 60 * 60 * 1000;
  private final int ONE_YEAR = ONE_DAY * 365;

  private long baseTime;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private int blockSize;
  private int pageSize;
  private int dictionaryPageSize;
  private boolean enableDictionary = false;
  private boolean useSingleFSBlock = false;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
  private int index = 0;
  private OperatorContext oContext;
  private List<String> partitionColumns;
  private boolean hasPartitions;
  private PrimitiveTypeName logicalTypeForDecimals;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
  private final ColumnPrecisionInfo columnPrecInfo;
  /** Bulk entry */
  private final VarLenColumnBulkEntry entry;
  /** A callback to allow bulk readers interact with their container */
  private final VarLenColumnBulkInputCallback containerCallback;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  private final boolean useBufferedReader;
  private final int scanBufferSize;
  private final boolean useFadvise;
  private final boolean enforceTotalSize;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java

  protected final ParquetReaderStats stats;
  private final boolean useBufferedReader;
  private final int scanBufferSize;
  private final boolean useFadvise;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  private final int scanBufferSize;
  private final boolean useFadvise;
  private final boolean enforceTotalSize;

  protected final String debugName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/PageReader.java`
#### Snippet
```java
  protected final ParquetReaderStats stats;
  private final boolean useBufferedReader;
  private final int scanBufferSize;
  private final boolean useFadvise;
  private final boolean enforceTotalSize;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/OverflowSerDeUtil.java`
#### Snippet
```java

    /** Field overflow entry */
    private final FieldOverflowEntry fieldOverflowEntry;

    /** Set of DrillBuf's that make up the underlying ValueVector. Only nullable
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java

  private ExecutorService threadPool;
  private long queueSize;
  private LinkedBlockingQueue<ReadStatus> pageQueue;
  private ConcurrentLinkedQueue<Future<Void>> asyncPageRead;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/AbstractRecordBatch.java`
#### Snippet
```java
  // Represents last outcome of next(). If an Exception is thrown
  // during the method's execution a value IterOutcome.STOP will be assigned.
  private IterOutcome lastOutcome;

  protected AbstractRecordBatch(final T popConfig, final FragmentContext context) throws OutOfMemoryException {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java

  @Parameter(names= {"-d", "--debug"}, description="Whether you want to run the program in debug mode.", required=false)
  private boolean debug = false;

  @Parameter(names= {"-c", "--config"}, description="Configuration file you want to load.  Defaults to loading 'drill-override.conf' from the classpath.", required=false)
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java
    }

    private final long ONE_MB = 1024 * 1024;

    private String toBytes(long memory) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/SimpleDurationFormat.java`
#### Snippet
```java
  private long seconds;
  private long milliSeconds;
  private long durationInMillis;

  //Block creation of any default objects
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonWriter.java`
#### Snippet
```java
  static final Logger logger = LoggerFactory.getLogger(JsonWriter.class);

  private final JsonFactory factory = new JsonFactory();
  private final JsonOutput gen;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java

    private List<RexNode> leadingFilters = Lists.newArrayList();
    private Map<LogicalExpression, RexNode> leadingPrefixMap;
    private RexNode indexColumnsRemainderFilter = null;
    private RexNode otherColumnsRemainderFilter = null;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
    private RelOptPlanner planner;
    private IndexConditionInfo.Builder builder;
    private PlannerSettings settings;

    public IndexComparator(RelOptPlanner planner, IndexConditionInfo.Builder builder) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillJoinRel.java`
#### Snippet
```java
   * to detect the unsupported join type, and throw exception.
   * */
  private int joinControl = JoinControl.DEFAULT;

  public DrillJoinRel(RelOptCluster cluster, RelTraitSet traits, RelNode left, RelNode right, RexNode condition,
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
  private final BufferAllocator allocator;
  private final List<Map<String, String>> implicitColumnList;
  private String currentReaderClassName;
  private final RecordBatchStatsContext batchStatsContext;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
  private final Iterator<Map<String, String>> implicitColumns;
  private Map<String, String> implicitValues;
  private final BufferAllocator allocator;
  private final List<Map<String, String>> implicitColumnList;
  private String currentReaderClassName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculatorImpl.java`
#### Snippet
```java

    private PartitionStatSet partitionStatsSet;
    private long partitionBuildBatchSize;
    private long partitionProbeBatchSize;
    private long reservedMemory;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/file/ImplicitFileColumnsHandler.java`
#### Snippet
```java

  private final DrillFileSystem dfs;
  private final ImplicitColumnResolver parser;
  private final ResultVectorCache vectorCache;
  private final Path rootDir;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierTemplate.java`
#### Snippet
```java
  private VectorAccessible hyperBatch;
  private VectorAccessible outgoing;
  private int size;
  private int queueSize;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashPartition.java`
#### Snippet
```java

  private static final int VARIABLE_MIN_WIDTH_VALUE_SIZE = 8;
  private final int maxColumnWidth = VARIABLE_MIN_WIDTH_VALUE_SIZE; // to control memory allocation for varchars

  public static final MajorType HVtype = MajorType.newBuilder()
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unnest/UnnestImpl.java`
#### Snippet
```java
  private RecordBatch outgoing;

  private IntVector rowIdVector; // Allocated and owned by the UnnestRecordBatch
  private IntVector.Mutator rowIdVectorMutator;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectorTemplate.java`
#### Snippet
```java
  private SelectionVector2 vector2;
  @SuppressWarnings("unused")
  private SelectionVector4 vector4;
  private SelectionVectorMode svMode;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/TDigestMergedStatistic.java`
#### Snippet
```java
public class TDigestMergedStatistic extends AbstractMergedStatistic {
  private Map<String, MergingDigest> tdigestHolder;
  private int compression;

  public TDigestMergedStatistic() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/HLLMergedStatistic.java`
#### Snippet
```java
public class HLLMergedStatistic extends AbstractMergedStatistic {
  private Map<String, HyperLogLog> hllHolder;
  private long accuracy;

  public HLLMergedStatistic () {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/NDVMergedStatistic.java`
#### Snippet
```java

  public static class NDVConfiguration {
    private final OptionManager optionManager;
    private final List<MergedStatistic> dependencies;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjectorTemplate.java`
#### Snippet
```java
  private int partitions;
  private SelectionVectorMode svMode;
  private RecordBatch outBatch;
  private IntVector partitionValues;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionProjectorTemplate.java`
#### Snippet
```java
  private ImmutableList<TransferPair> transfers;
  private int partitions;
  private SelectionVectorMode svMode;
  private RecordBatch outBatch;
  private IntVector partitionValues;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionSenderRootExec.java`
#### Snippet
```java
  private final int outGoingBatchCount;
  private final HashPartitionSender popConfig;
  private final double cost;

  private final AtomicIntegerArray remainingReceivers;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unorderedreceiver/UnorderedReceiverBatch.java`
#### Snippet
```java
  // Represents last outcome of next(). If an Exception is thrown
  // during the method's execution a value IterOutcome.STOP will be assigned.
  private IterOutcome lastOutcome;

  public enum Metric implements MetricDef {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unorderedreceiver/UnorderedReceiverBatch.java`
#### Snippet
```java
  private boolean first = true;
  private final UnorderedReceiver config;
  private final OperatorContext oContext;
  // Represents last outcome of next(). If an Exception is thrown
  // during the method's execution a value IterOutcome.STOP will be assigned.
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java

  private int outgoingPosition;
  private int senderCount;
  private RawFragmentBatch[] incomingBatches;
  private int[] batchOffsets;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/ResourcePoolTreeImpl.java`
#### Snippet
```java
  private final ResourcePool rootPool;

  private final Config rmConfig;

  private final NodeResources totalNodeResources;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/drill/exec/resourcemgr/config/ResourcePoolImpl.java`
#### Snippet
```java
  private List<ResourcePool> childPools;

  private final double parentResourceShare;

  private final double poolResourceShare;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetColumnChunkPageWriteStore.java`
#### Snippet
```java
    private OffsetIndexBuilder offsetIndexBuilder;
    private Statistics totalStatistics;
    private final ByteBufferAllocator allocator;

    private final CRC32 crc;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageBatchReader.java`
#### Snippet
```java
  private final EasySubScan scan;
  private CustomErrorContext errorContext;
  private Path path;
  private FileStatus fileStatus;
  private BufferedInputStream metaInputStream;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/TcpHandshake.java`
#### Snippet
```java
  private State currentSessionState = State.NONE;

  private long sessionID;

  /**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/decoder/TcpSession.java`
#### Snippet
```java
  private String dstMac;
  private long synTime;
  private long ackTime;
  private long connectTime;
  private byte[] sentData;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private final PcapFormatConfig config;
  private final EasySubScan scan;
  private final FileDescrip file;

  private CustomErrorContext errorContext;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcapng/PcapngBatchReader.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(PcapngBatchReader.class);
  private final PcapFormatConfig config;
  private final EasySubScan scan;
  private final FileDescrip file;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/columns/GoogleSheetsColumnRange.java`
#### Snippet
```java
  private Integer startColIndex;
  private Integer endColIndex;
  private final String sheetName;

  public GoogleSheetsColumnRange(String sheetName) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/schema/GoogleSheetsDrillSchema.java`
#### Snippet
```java
  private final Sheets sheetsService;
  private final SchemaConfig schemaConfig;
  private final GoogleSheetsRootSchema parent;
  private final String fileToken;
  private final String fileName;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;

  public MapRDBCost(DrillConfig config, String mediaType) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
  private int JSON_SSD_BLOCK_RANDOM_READ_COST;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
  private int JSON_BLOCK_SEQ_READ_COST;
  private int JSON_BLOCK_RANDOM_READ_COST;
  private int JSON_HDD_BLOCK_SEQ_READ_COST;
  private int JSON_HDD_BLOCK_RANDOM_READ_COST;
  private int JSON_SSD_BLOCK_SEQ_READ_COST;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBFormatPlugin.java`
#### Snippet
```java
  private final MapRDBTableCache jsonTableCache;
  private final int scanRangeSizeMB;
  private final String mediaType;
  private final MapRDBCost pluginCostModel;
  private final int restrictedScanRangeSizeMB;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-syslog/src/main/java/org/apache/drill/exec/store/syslog/SyslogBatchReader.java`
#### Snippet
```java
public class SyslogBatchReader implements ManagedReader {
  private static final Logger logger = LoggerFactory.getLogger(SyslogBatchReader.class);
  private final String STRUCTURED_DATA_PREFIX = "structured_data_";
  private final String STRUCTURED_DATA_MAP_NAME = "structured_data";
  private final String RAW_COLUMN_NAME = "_raw";
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-pdf/src/main/java/org/apache/drill/exec/store/pdf/PdfBatchReader.java`
#### Snippet
```java
  private final List<PdfColumnWriter> writers;
  private final PdfReaderConfig config;
  private final int startingTableIndex;
  private final FileDescrip file;
  private PdfMetadataReader metadataReader;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ConnectionMultiListener.java`
#### Snippet
```java
    boolean enableSSL = false;

    private ConnectionMultiListener<T, CC, HS, HR, BC> cml;

    private Builder(RpcConnectionHandler<CC> connectionListener, HS handshakeValue, BC basicClient) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcBus.java`
#### Snippet
```java

    final C clientConnection;
    private final Channel channel;

    public ChannelClosedHandler(C clientConnection, Channel channel) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java

    private final PoolArena<ByteBuffer>[] directArenas;
    private final MemoryStatusThread statusThread;
    private final Histogram largeBuffersHist;
    private final Histogram normalBuffersHist;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduRecordWriterImpl.java`
#### Snippet
```java
  private final KuduClient client;
  private final String name;
  private final OperatorContext context;
  private KuduTable table;
  private final KuduSession session;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java

  // This is the theoretical maximum number of rows in an Excel spreadsheet
  private final int MAX_ROWS = SpreadsheetVersion.EXCEL2007.getMaxRows();

  private final List<String> extensions;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/decoders/JsonMessageReader.java`
#### Snippet
```java
  private SchemaNegotiator negotiator;
  private ReadOptions readOptions;
  private Properties kafkaConsumerProps;

  @Override
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkSchemaFactory.java`
#### Snippet
```java

  private final SplunkStoragePlugin plugin;
  private String queryUserName;

  public SplunkSchemaFactory(SplunkStoragePlugin plugin) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkBatchWriter.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SplunkBatchWriter.class);
  private static final String DEFAULT_SOURCETYPE = "drill";
  private final UserCredentials userCredentials;
  private final List<String> tableIdentifier;
  private final SplunkWriter config;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/format-spss/src/main/java/org/apache/drill/exec/store/spss/SpssFormatPlugin.java`
#### Snippet
```java
  private static class SpssReaderFactory extends FileReaderFactory {

    private final EasySubScan scan;

    public SpssReaderFactory(EasySubScan scan) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/writers/JdbcVardecimalWriter.java`
#### Snippet
```java
public class JdbcVardecimalWriter extends JdbcColumnWriter {

  private final int scale;
  private final int precision;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/writers/JdbcVardecimalWriter.java`
#### Snippet
```java

  private final int scale;
  private final int precision;

  public JdbcVardecimalWriter(String colName, RowSetLoader rowWriter, int columnIndex, int scale, int precision) {
```

## RuleId[id=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java

@JsonPropertyOrder({"@id", "memo", "input"}) // op will always be first since it is wrapped.
@JsonIdentityInfo(generator = ObjectIdGenerators.IntSequenceGenerator.class, property = "@id")
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property = "op")
public interface LogicalOperator extends GraphValue<LogicalOperator> {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `logical/src/main/java/org/apache/drill/common/logical/data/LogicalOperator.java`
#### Snippet
```java
@JsonPropertyOrder({"@id", "memo", "input"}) // op will always be first since it is wrapped.
@JsonIdentityInfo(generator = ObjectIdGenerators.IntSequenceGenerator.class, property = "@id")
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property = "op")
public interface LogicalOperator extends GraphValue<LogicalOperator> {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordReader.java`
#### Snippet
```java
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.PROPERTY,
    property = "type")
@JsonSubTypes({
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    public int batch = -1;

    @Parameter(names = {"-include-headers"}, description = "whether include header of batch", required = false)
    public boolean include_headers = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    public String location = null;

    @Parameter(names = {"-batch"}, description = "id of batch to show", required = false, validateWith = BatchNumValidator.class)
    public int batch = -1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public String location = null;

    @Parameter(names = {"-q", "-e", "--query"}, description = "query string", required = false)
    public String queryString = null;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public String zk = "localhost:2181";

    @Parameter(names = {"-l", "--local"}, description = "run query in local mode", required=false)
    public boolean local;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public boolean help = false;

    @Parameter(names = {"--format"}, description = "output format, csv,tsv,table", required = false)
    public String format = "table";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public boolean local;

    @Parameter(names = {"-b", "--bits"}, description = "number of drillbits to run. local mode only", required=false)
    public int bits = 1;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public String planType;

    @Parameter(names = {"-z", "--zookeeper"}, description = "zookeeper connect string.", required=false)
    public String zk = "localhost:2181";

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
    public String format = "table";

    @Parameter(names = {"-w", "--width"}, description = "max column width", required = false)
    public int width = VectorUtil.DEFAULT_COLUMN_WIDTH;
  }
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java

  static class Options {
    @Parameter(names = {"-f", "--file"}, description = "file containing plan", required=false)
    public String location = null;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java
  private boolean debug = false;

  @Parameter(names= {"-c", "--config"}, description="Configuration file you want to load.  Defaults to loading 'drill-override.conf' from the classpath.", required=false)
  private String configLocation = null;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/StartupOptions.java`
#### Snippet
```java
  private boolean help = false;

  @Parameter(names= {"-d", "--debug"}, description="Whether you want to run the program in debug mode.", required=false)
  private boolean debug = false;

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillStatsTable.java`
#### Snippet
```java
   * exec.statistics.capability_version
   */
  @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY,
      property = "statistics_version")
  @JsonSubTypes({
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/CreateTableEntry.java`
#### Snippet
```java
 * which supports creating new tables, should implement this interface.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property="type")
@JsonSubTypes({ // TODO: hack until we merge "common" and "java-exec" modules (DRILL-507).
    @Type(name = "filesystem", value = FileSystemCreateTableEntry.class)
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PartitionFunction.java`
#### Snippet
```java
import com.fasterxml.jackson.annotation.JsonTypeInfo;

@JsonTypeInfo(use=JsonTypeInfo.Id.CLASS, include=JsonTypeInfo.As.PROPERTY, property="@class")
public interface PartitionFunction  {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/AbstractRangePartitionFunction.java`
#### Snippet
```java
import com.fasterxml.jackson.annotation.JsonTypeInfo;

@JsonTypeInfo(use=JsonTypeInfo.Id.CLASS, include=JsonTypeInfo.As.PROPERTY, property="@class")
public abstract class AbstractRangePartitionFunction  implements PartitionFunction {
  public static final String RANGE_PARTITION_EXPR_NAME = "R_A_N_G_E_E_X_P_R";
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/testing/ExecutionControls.java`
#### Snippet
```java
  @JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.PROPERTY,
    property = "type")
  @JsonSubTypes({
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalOperator.java`
#### Snippet
```java
@JsonInclude(Include.NON_NULL)
@JsonPropertyOrder({ "@id" })
@JsonIdentityInfo(generator = ObjectIdGenerators.PropertyGenerator.class, property = "@id")
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property = "pop")
public interface PhysicalOperator extends GraphValue<PhysicalOperator> {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/PhysicalOperator.java`
#### Snippet
```java
@JsonPropertyOrder({ "@id" })
@JsonIdentityInfo(generator = ObjectIdGenerators.PropertyGenerator.class, property = "@id")
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property = "pop")
public interface PhysicalOperator extends GraphValue<PhysicalOperator> {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/RestrictedJsonTableGroupScan.java`
#### Snippet
```java
                                      @JsonProperty("scanSpec") JsonScanSpec scanSpec, /* scan spec of the original table */
                                      @JsonProperty("columns") List<SchemaPath> columns,
                                      @JsonProperty("") MapRDBStatistics statistics,
                                      @JsonProperty("schema") TupleMetadata schema) {
    super(userName, storagePlugin, formatPlugin, scanSpec, columns,
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/TimedCallable.java`
#### Snippet
```java
            " Total number of tasks %d, parallelism %d.", timeout, futureMapper.count, activity, tasks.size(), parallelism);
        logger.error(errMsg, futureMapper.throwable);
        throw UserException.resourceError(futureMapper.throwable)
            .message(errMsg)
            .build(logger);
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/TimedCallable.java`
#### Snippet
```java
      }
      if (futureMapper.throwable != null) {
        throw (futureMapper.throwable instanceof IOException) ?
            (IOException)futureMapper.throwable : new IOException(futureMapper.throwable);
      }
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
'if' statement can be collapsed with side effect extraction
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/common/DruidFilterDeserializer.java`
#### Snippet
```java
      }
    }
    if (filterKnowsItsType && root.get("type").asText().equals(TYPE_SELECTOR.getCompareOp())) {
      return mapper.readValue(root.toString(), DruidSelectorFilter.class);
    }
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/ColumnListsCache.java`
#### Snippet
```java
    }
    Integer index = keys.get(columns);
    if (index != null) {
      return index;
    } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
        final int fromCharIdx; //the start position of char (inclusive)
        final int charLen; // the end position of char (inclusive)
        if (length.value > 0) {
          fromCharIdx = Math.max(charCount - (int) length.value + 1, 1); // right('abc', 5) ==> 'abc' fromCharIdx=1.
          charLen = charCount - fromCharIdx + 1;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java

    public RawFragmentBatch get() throws InterruptedException, IOException {
      if (available) {
        assert batch.getHeader() != null : "batch header null";
        return batch;
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java
          }
        }
        if (ch == quoteEscape) {
          prev = ch;
        } else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
  private boolean shouldContinueAfterNoRecords() {
    logger.trace("scan got 0 record.");
    if (isRepeatableScan) {
      if (!currentReader.hasNext()) {
        currentReader = null;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unpivot/UnpivotMapsRecordBatch.java`
#### Snippet
```java
    for (VectorWrapper<?> vw : container) {
      MaterializedField mf = vw.getField();
      if (dataSrcVecMap.containsKey(mf)) {
        String k = keyList.get(keyIndex);
        vv = dataSrcVecMap.get(mf).get(k);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
    String finalTableKey = mapKey + "final";

    if (val == fragmentsBeforeProceed) { // we crossed the barrier, build table and get data.
      buildTable();
      finalTable = tableMap.get(finalTableKey);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/PcapBatchReader.java`
#### Snippet
```java

  private void populateColumnWriters(RowSetLoader rowWriter) {
    if (readerConfig.getSessionizeTCPStreams()) {
      srcMacAddressWriter = rowWriter.scalar("src_mac_address");
      dstMacAddressWriter = rowWriter.scalar("dst_mac_address");
```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillColumnMetaDataList.java`
#### Snippet
```java
  @Override
  public boolean containsAll(Collection<?> c) {
    return columns.containsAll(c);
  }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ConvertMetadataAggregateToDirectScanRule.java`
#### Snippet
```java
    if (!(oldGrpScan instanceof ParquetGroupScan)
        || (oldGrpScan.getTableMetadata().getInterestingColumns() != null
          && !oldGrpScan.getTableMetadata().getInterestingColumns().containsAll(agg.getContext().interestingColumns()))) {
      return;
    }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/unpivot/UnpivotMapsRecordBatch.java`
#### Snippet
```java
        lastMapKeyList = keyList;
      } else {
        if (keyList.size() != lastMapKeyList.size() || !lastMapKeyList.containsAll(keyList)) {
          throw new UnsupportedOperationException("Maps have different fields");
        }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsMergeBatch.java`
#### Snippet
```java
      }
      if (columnsList.size() != lastMapColumnsList.size()
          || !lastMapColumnsList.containsAll(columnsList)) {
        // Error!! Maps with different size and/or keys. The map for each statistics (e.g. NDV)
        // should match exactly with the column map i.e. we did not run into any issues while
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/analyze/FileMetadataInfoCollector.java`
#### Snippet
```java
    if (!Objects.equals(metastoreInterestingColumns, interestingColumns)
        && metastoreInterestingColumns != null &&
        (interestingColumns == null || !metastoreInterestingColumns.containsAll(interestingColumns))
        || TableStatisticsKind.ANALYZE_METADATA_LEVEL.getValue(basicRequests.tableMetadata(tableInfo)).compareTo(metadataLevel) != 0) {
      // do not update table scan and lists of segments / files / row groups,
```

## RuleId[id=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
   */
  private void waitUntilTimeOut(final long timeout) {
    while(true) {
      try {
        Thread.sleep(timeout);
```

## RuleId[id=SlowAbstractSetRemoveAll]
### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
      final List<VectorWrapper<?>> ignored = Lists.newArrayList(ignoreWrappers);
      final Set<VectorWrapper<?>> resultant = Sets.newLinkedHashSet(incoming);
      resultant.removeAll(ignored);
      wrappers = resultant;
    }
```

### SlowAbstractSetRemoveAll
Call to 'set.removeAll(list)' may work slowly
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillJoinRelBase.java`
#### Snippet
```java
   */
  private static <T> boolean intersects(List<T> left, List<T> right) {
    return new HashSet<>(left).removeAll(right);
  }

```

## RuleId[id=RedundantStringFormatCall]
### RedundantStringFormatCall
Redundant call to `format()`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
    udfInitCatch.body()
      ._throw(JExpr._new(m.directClass(RuntimeException.class.getCanonicalName()))
        .arg(JExpr.lit(String.format("Failed to initialize GenericUDF"))).arg(exVar));

    sub.add(ObjectInspectorHelper.initReturnValueHolder(g, m, workspaceJVars[4], returnOI, returnType.getMinorType()));
```

### RedundantStringFormatCall
Redundant call to `format()`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/expr/fn/HiveFuncHolder.java`
#### Snippet
```java
    udfEvalCatch.body()
      ._throw(JExpr._new(m.directClass(RuntimeException.class.getCanonicalName()))
        .arg(JExpr.lit(String.format("GenericUDF.evaluate method failed"))).arg(exVar));

    // get the ValueHolder from retVal and return ObjectInspector
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
          System.out.printf("| %-15s", width <= 15 ? column : column.substring(0, 14));
        }
        System.out.printf("|\n");
        System.out.println(StringUtils.repeat("-", width * 17 + 1));
      }
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
        System.out.printf("| %-15s", value.length() <= 15 ? value : value.substring(0, 14));
      }
      System.out.printf("|\n");
    }
  }
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
          System.out.printf("| %-15s", width <= 15 ? column : column.substring(0, 14));
        }
        System.out.printf("|\n");
        System.out.println(StringUtils.repeat("-", width * 17 + 1));
      }
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
        System.out.printf("| %-15s", value.length() <= 15 ? value : value.substring(0, 14));
      }
      System.out.printf("|\n");
    }
  }
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
          System.out.printf("| %-15s", width <= 15 ? column : column.substring(0, 14));
        }
        System.out.printf("|\n");
        System.out.println(StringUtils.repeat("-", width * 17 + 1));
      }
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
        System.out.printf("| %-15s", value.length() <= 15 ? value : value.substring(0, 14));
      }
      System.out.printf("|\n");
    }
    System.out.printf("|\n");
```

### RedundantStringFormatCall
Redundant call to `printf()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/BatchPrinter.java`
#### Snippet
```java
      System.out.printf("|\n");
    }
    System.out.printf("|\n");
  }

```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableEntryReader.java`
#### Snippet
```java

      if (remainingPageData() < 4) {
        final String message = String.format("Invalid Parquet page metadata; cannot process advertised page count..");
        throw new DrillRuntimeException(message);
      }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenNullableEntryReader.java`
#### Snippet
```java

      if (remainingPageData() < (4 + dataLen)) {
        final String message = String.format("Invalid Parquet page metadata; cannot process advertised page count..");
        throw new DrillRuntimeException(message);
      }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java

    if (remainingPageData() < 4) {
      final String message = String.format("Invalid Parquet page metadata; cannot process advertised page count..");
      throw new DrillRuntimeException(message);
    }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenEntryReader.java`
#### Snippet
```java

    if (remainingPageData() < (4 + dataLen)) {
      final String message = String.format("Invalid Parquet page metadata; cannot process advertised page count..");
      throw new DrillRuntimeException(message);
    }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java

    if (batchNum < targetBatchNum) {
      System.out.println(String.format("Wrong input of batch # ! Total # of batch in the file is %d. Please input a number 0..%d as batch #", batchNum+1, batchNum));
      input.close();
      System.exit(-1);
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java

    /* output the summary stat */
    System.out.println(String.format("Total # of batches: %d", batchNum));
    //output: rows, selectedRows, avg rec size, total data size.
    System.out.println(aggBatchMetaInfo.toString());
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    //output: rows, selectedRows, avg rec size, total data size.
    System.out.println(aggBatchMetaInfo.toString());
    System.out.println(String.format("Empty batch : %d", emptyBatchNum));
    System.out.println(String.format("Schema changes : %d", schemaChangeIdx.size()));
    System.out.println(String.format("Schema change batch index : %s", schemaChangeIdx.toString()));
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    System.out.println(aggBatchMetaInfo.toString());
    System.out.println(String.format("Empty batch : %d", emptyBatchNum));
    System.out.println(String.format("Schema changes : %d", schemaChangeIdx.size()));
    System.out.println(String.format("Schema change batch index : %s", schemaChangeIdx.toString()));
  }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    System.out.println(String.format("Empty batch : %d", emptyBatchNum));
    System.out.println(String.format("Schema changes : %d", schemaChangeIdx.size()));
    System.out.println(String.format("Schema change batch index : %s", schemaChangeIdx.toString()));
  }

```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
      for (final VectorWrapper<?> w : vectorContainer) {
        final MaterializedField field = w.getValueVector().getField();
        System.out.println (String.format("name : %s, minor_type : %s, data_mode : %s",
                                          field.getName(),
                                          field.getType().getMinorType().toString(),
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    final File file = new File(o.location);
    if (!file.exists()) {
      System.out.println(String.format("Trace file %s not created", o.location));
      System.exit(-1);
    }
```

### RedundantStringFormatCall
Redundant call to `format()`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
      client.runQuery(queryType, query, listener);
      int rows = listener.await();
      System.out.println(String.format("%d record%s selected (%f seconds)", rows, rows > 1 ? "s" : "", (float) watch.elapsed(TimeUnit.MILLISECONDS) / (float) 1000));
      if (query != queries[queries.length - 1]) {
        System.out.println();
```

### RedundantStringFormatCall
Redundant call to `format()`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
  @Override
  public void setStatistics(Statistics statistics) {
    assert statistics instanceof MapRDBStatistics : String.format(
        "Passed unexpected statistics instance. Expects MAPR-DB Statistics instance");
    this.stats = ((MapRDBStatistics) statistics);
```

## RuleId[id=InfiniteRecursion]
### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginSortRule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode rel) {
    Sort sort = (Sort) rel;
    RelNode input = convert(sort.getInput(), sort.getInput().getTraitSet().replace(getOutConvention()).simplify());
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginJoinRule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode rel) {
    Join join = (Join) rel;
    RelTraitSet traits = join.getTraitSet().replace(getOutConvention());
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginAggregateRule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode rel) {
    Aggregate agg = (Aggregate) rel;
    return new PluginAggregateRel(
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginFilterRule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode rel) {
    Filter filter = (Filter) rel;
    PluginFilterRel pluginFilterRel = new PluginFilterRel(
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/VertexDrelConverterRule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode in) {
    return new VertexDrel(in.getCluster(), in.getTraitSet().replace(DrillRel.DRILL_LOGICAL),
        convert(in, in.getTraitSet().replace(this.getInTrait()).simplify()));
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/DrillJdbcRuleBase.java`
#### Snippet
```java

    @Override
    public RelNode convert(RelNode rel) {
      Sort sort = (Sort) rel;

```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/DrillJdbcRuleBase.java`
#### Snippet
```java

    @Override
    public RelNode convert(RelNode rel) {
      LogicalFilter filter = (LogicalFilter) rel;

```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/DrillJdbcRuleBase.java`
#### Snippet
```java

    @Override
    public RelNode convert(RelNode rel) {
      LogicalProject project = (LogicalProject) rel;
      return new JdbcRules.JdbcProject(rel.getCluster(), rel.getTraitSet().replace(this.out), convert(
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/DrillJdbcRuleBase.java`
#### Snippet
```java

    @Override
    public RelNode convert(RelNode rel) {
      DrillLimitRelBase limit = (DrillLimitRelBase) rel;

```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SortConvertPrule.java`
#### Snippet
```java

  @Override
  public RelNode convert(RelNode r) {
    Sort rel = (Sort) r;
    RelTraitSet traits = rel.getInput().getTraitSet().replace(Prel.DRILL_PHYSICAL);
```

### InfiniteRecursion
Method `dataReadError()` recurses infinitely, and can only end by throwing an exception
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginErrorHandler.java`
#### Snippet
```java
  }

  public static UserException dataReadError(Logger logger, String format, Object... args) {
    return dataReadError(null, format, args);
  }
```

### InfiniteRecursion
Method `convert()` recurses infinitely, and can only end by throwing an exception
in `contrib/storage-cassandra/src/main/java/org/apache/drill/exec/store/cassandra/plan/DrillCassandraLimitRule.java`
#### Snippet
```java
  }

  public RelNode convert(RelNode relNode) {
    DrillLimitRelBase limit = (DrillLimitRelBase) relNode;
    final RelTraitSet traitSet =
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `common/src/main/java/org/apache/drill/common/config/NestedConfig.java`
#### Snippet
```java

  @Override
  public List<? extends Object> getAnyRefList(String path) {
    return c.getAnyRefList(path);
  }
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenRepository.java`
#### Snippet
```java
      .setCredentialsProvider(credentialsProvider)
      .setTokenTable(tokenTable)
      .build().get();

    // Add proxy info
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
        credentials = getCredentials();
      }
      builder.addInterceptor(new BasicAuthInterceptor(credentials.get().getUsername(), credentials.get().getPassword()));
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/DropboxFileSystem.java`
#### Snippet
```java
        .setTokenTable(tokenTable)
        .build()
        .get();

      long expiresIn = 0;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
        .setTokenTable(tokenTable)
        .build()
        .get();

    BoxAPIConnection newClient = new BoxAPIConnection(credentials.getClientID(), credentials.getClientSecret(),
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/JdbcStoragePlugin.java`
#### Snippet
```java
          sd = JdbcSchema.createDialect(
            SqlDialectFactoryImpl.INSTANCE,
            getDataSource(userCredentials).get()
          );
          sqlDialect = sd;
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `contrib/storage-jdbc/src/main/java/org/apache/drill/exec/store/jdbc/JdbcRecordWriter.java`
#### Snippet
```java

    try {
      this.connection = config.getPlugin().getDataSource(userCredentials).get().getConnection();
    } catch (SQLException e) {
      throw UserException.connectionError()
```

## RuleId[id=ConstantValue]
### ConstantValue
Value `value` is always 'true'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/AbstractPropertied.java`
#### Snippet
```java
  public void setBooleanProperty(String key, boolean value) {
    if (value) {
      setProperty(key, Boolean.toString(value));
    } else {
      setProperty(key, null);
```

### ConstantValue
Condition `types == null` is always `false`
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/VariantColumnMetadata.java`
#### Snippet
```java
      }
    }
    if (types == null) {
      return;
    }
```

### ConstantValue
Result of `valueCount * VALUE_WIDTH` is always '0'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/UntypedNullVector.java`
#### Snippet
```java
    final int actualLength = metadata.getBufferLength();
    final int valueCount = metadata.getValueCount();
    final int expectedLength = valueCount * VALUE_WIDTH;
    assert actualLength == expectedLength : String.format("Expected to load %d bytes but actually loaded %d bytes", expectedLength, actualLength);

```

### ConstantValue
Value `writer` is always 'null'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/RepeatedDictReaderImpl.java`
#### Snippet
```java
      srcId = ((RepeatedDictWriter) writer).idx();
    } else {
      vector = ((SingleDictWriter) writer).container;
      srcId = ((SingleDictWriter) writer).idx();
    }
```

### ConstantValue
Condition `v instanceof ZeroVector` is always `false`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java
      unionVector = (UnionVector) v;
      writer = new UnionWriter(unionVector);
    } else if (v instanceof ZeroVector) {
      state = State.UNTYPED;
    } else {
```

### ConstantValue
Value `v` is always 'null'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java
      unionVector = (UnionVector) v;
      writer = new UnionWriter(unionVector);
    } else if (v instanceof ZeroVector) {
      state = State.UNTYPED;
    } else {
```

### ConstantValue
Value `v` is always 'null'
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java
      state = State.UNTYPED;
    } else {
      setWriter(v);
    }
  }
```

### ConstantValue
Condition `resp.getError() != null` is always `true`
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillJdbc41Factory.java`
#### Snippet
```java
      final RequestStatus status = resp.getStatus();
      if (status != RequestStatus.OK) {
        final String errMsgFromServer = resp.getError() != null ? resp.getError().getMessage() : "";

        if (status == RequestStatus.TIMEOUT) {
```

### ConstantValue
Value `value` is always 'null'
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
      // Null--show no actual type or object ID.
      actualTypePart = "";
      actualValuePart = formatValue( value );
    }
    else {
```

### ConstantValue
Condition `rawActualType == org.apache.drill.jdbc.DrillResultSet.class` is always `false` when reached
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/proxy/InvocationReporterImpl.java`
#### Snippet
```java
          // Is type to warn about (one case).
          false
          || rawActualType == org.apache.drill.jdbc.DrillResultSet.class
          ) {
        printWarningLine(
```

### ConstantValue
Condition `results == null` is always `false`
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/udfs/HttpHelperFunctions.java`
#### Snippet
```java
      java.io.InputStream results = org.apache.drill.exec.store.http.util.SimpleHttp.getRequestAndStreamResponse(finalUrl);
      // If the result string is null or empty, return an empty map
      if (results == null) {
        return;
      }
```

### ConstantValue
Value `value` is always 'null'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpStoragePluginConfig.java`
#### Snippet
```java
  private static String normalize(String value) {
    if (value == null) {
      return value;
    }
    value = value.trim();
```

### ConstantValue
Value `rackArr` is always 'null'
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java
    if (!racks.isEmpty()) {
      nodeArr = new String[racks.size()];
      racks.toArray(rackArr);
      relaxLocality = false;
    }
```

### ConstantValue
Condition `tracker == null` is always `false`
in `drill-yarn/src/main/java/org/apache/drill/yarn/zk/ZKRegistry.java`
#### Snippet
```java
    DrillbitTracker tracker = registry.get(key);
    assert tracker != null;
    if (tracker == null) {
      // Something is terribly wrong.
      // Have seen this when a user kills the Drillbit just after it starts. Evidently, the
```

### ConstantValue
Condition `quantity == 0` is always `false`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/PersistentTaskScheduler.java`
#### Snippet
```java

    assert quantity > 0;
    if (quantity == 0) {
      LOG.error("Container timed out, but target quantity is already 0!");
    } else {
```

### ConstantValue
Condition `!registered` is always `true`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
          .append(" failed with status ").append(exitCode).append(" after ")
          .append(duration).append(" secs. with");
      if (!registered) {
        buf.append("out");
      }
```

### ConstantValue
Condition `!registered` is always `true` when reached
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
      }
      buf.append(" ZK registration");
      if (duration < 60 && !registered) {
        buf.append(
            "\n    Probable configuration problem, check Drill log file on host ")
```

### ConstantValue
Condition `task == null` is always `true`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
      Task task = getTask(status.getContainerId());
      if (task == null) {
        if (task == null) {
          // Will occur if a container was allocated but rejected.
          // Any other occurrence is unexpected and an error.
```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
        offset = limitRelBase.getOffset();
        return other;
      } else if (other instanceof RelSubset) {
        RelSubset relSubset = (RelSubset) other;
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
      }
      return super.visit(other);
    }

```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
        this.filter = other;
        return other;
      } else if (other instanceof RelSubset) {
        RelSubset relSubset = (RelSubset) other;
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/plan/DeltaPluginImplementor.java`
#### Snippet
```java
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
      }
      return super.visit(other);
    }

```

### ConstantValue
Condition `constructor == null` is always `false`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveSubScan.java`
#### Snippet
```java
      throws IOException, ReflectiveOperationException{
    Constructor<?> constructor = Class.forName(className).getDeclaredConstructor();
    if (constructor == null) {
      throw new ReflectiveOperationException("Class " + className + " does not implement a default constructor.");
    }
```

### ConstantValue
Condition `ex instanceof MetaException` is always `true`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
      while (true) {
        // Case for failing on an invalid cached connection
        if (ex instanceof MetaException ||
            // Case for a timed-out impersonated connection, and
            // an invalid non-secure connection used to get security
```

### ConstantValue
Condition `ex instanceof MetaException || // Case for a timed-out impersonated connection, and ...` is always `true`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
      while (true) {
        // Case for failing on an invalid cached connection
        if (ex instanceof MetaException ||
            // Case for a timed-out impersonated connection, and
            // an invalid non-secure connection used to get security
            // tokens.
            ex instanceof TTransportException) {
          break;
        }
```

### ConstantValue
Condition `recordCount < TARGET_RECORD_COUNT` is always `true`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/readers/HiveDefaultRecordReader.java`
#### Snippet
```java
    try {
      int recordCount;
      for (recordCount = 0; (recordCount < TARGET_RECORD_COUNT && hasNextValue(valueHolder)); recordCount++) {
        Object deserializedHiveRecord = partitionToTableSchemaConverter.convert(partitionDeserializer.deserialize((Writable) valueHolder));
        outputWriter.setPosition(recordCount);
```

### ConstantValue
Value `parentWriter` is always 'null'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/writers/HiveValueWriterFactory.java`
#### Snippet
```java
    } else if (parentWriter instanceof UnionVectorWriter) {
      return fromUnion.apply((UnionVectorWriter) parentWriter);
    } else if (parentWriter instanceof ListWriter) {
      return fromList.apply((ListWriter) parentWriter);
    } else {
```

### ConstantValue
Value `args` is always 'null'
in `logical/src/main/java/org/apache/drill/common/expression/FunctionCall.java`
#### Snippet
```java

    if (!(args instanceof ImmutableList)) {
      args = ImmutableList.copyOf(args);
    }
    this.args = (ImmutableList<LogicalExpression>) args;
```

### ConstantValue
Condition `bit2userConn != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServer.java`
#### Snippet
```java
  private BitToUserConnection registerAndGetConnection(SocketChannel channel) {
    BitToUserConnection bit2userConn = new BitToUserConnection(channel);
    if (bit2userConn != null) {
      userConnectionMap.put(bit2userConn, new BitToUserConnectionConfig());
    }
```

### ConstantValue
Condition `currentByte < 128` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSubstring.java`
#### Snippet
```java
        }

        if (currentByte < 128) {
          ++charCount;
        }
```

### ConstantValue
Condition `currentByte < 128` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSubstring.java`
#### Snippet
```java
            while (byteCount <= endBytePos) {
              currentByte = string.buffer.getByte(byteCount);
              if (currentByte < 128) {
                ++charCount;
              }
```

### ConstantValue
Condition `currentByte < 128` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/CharSubstring.java`
#### Snippet
```java
            break;
          }
          if (currentByte < 128) {
            --charCount;
          }
```

### ConstantValue
Condition `leastRestrictive != elseType` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
            conditions = new IfExpression.IfCondition(newCondition,
            addCastExpression(conditions.expression, newElseExpr.getMajorType(), functionLookupContext, errorCollector));
          } else if (leastRestrictive != elseType) {
            // Implicitly cast the else expression
            newElseExpr = addCastExpression(newElseExpr, conditions.expression.getMajorType(), functionLookupContext, errorCollector);
```

### ConstantValue
Condition `maxHeap.isEmpty()` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StreamingMedianHelpers.java`
#### Snippet
```java
      } else if (minHeap.size() > maxHeap.size()) {
        median = minHeap.peek();
      } else if (minHeap.isEmpty() && maxHeap.isEmpty()) {
        median = 0L;
      } else {
```

### ConstantValue
Condition `maxHeap.isEmpty()` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StreamingMedianHelpers.java`
#### Snippet
```java
      } else if (minHeap.size() > maxHeap.size()) {
        median = minHeap.peek();
      } else if (minHeap.isEmpty() && maxHeap.isEmpty()) {
        median = 0.0;
      } else {
```

### ConstantValue
Condition `holder instanceof NullableBitHolder` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java

    private Trivalent isBitOn(ValueHolder holder) {
      Preconditions.checkArgument(holder instanceof BitHolder || holder instanceof NullableBitHolder,
          "Input does not have type of BitHolder or NullableBitHolder.");

```

### ConstantValue
Value `holder` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java

    private Trivalent isBitOn(ValueHolder holder) {
      Preconditions.checkArgument(holder instanceof BitHolder || holder instanceof NullableBitHolder,
          "Input does not have type of BitHolder or NullableBitHolder.");

```

### ConstantValue
Condition `condHolder instanceof NullableBitHolder` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      ValueHolder condHolder = ifExpr.ifCondition.condition.accept(this, inIndex);

      Preconditions.checkArgument (condHolder instanceof BitHolder || condHolder instanceof NullableBitHolder,
          "IfExpression's condition does not have type of BitHolder or NullableBitHolder.");

```

### ConstantValue
Value `condHolder` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      ValueHolder condHolder = ifExpr.ifCondition.condition.accept(this, inIndex);

      Preconditions.checkArgument (condHolder instanceof BitHolder || condHolder instanceof NullableBitHolder,
          "IfExpression's condition does not have type of BitHolder or NullableBitHolder.");

```

### ConstantValue
Condition `readAhead != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
      return;
    }
    if (readAhead != null) {
      Object[] adviceTypeValues = adviceType.getEnumConstants();
      for (int idx = 0; idx < adviceTypeValues.length; idx++) {
```

### ConstantValue
Condition `currentByte < 0x128` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java

            currentByte = fill.buffer.getByte(id);
            if (currentByte < 0x128  ||           // 1-byte char. First byte is 0xxxxxxx.
                (currentByte & 0xE0) == 0xC0 ||   // 2-byte char. First byte is 110xxxxx
                (currentByte & 0xF0) == 0xE0 ||   // 3-byte char. First byte is 1110xxxx
```

### ConstantValue
Condition `currentByte < 0x128 || // 1-byte char. First byte is 0xxxxxxx. (currentBy...` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java

            currentByte = fill.buffer.getByte(id);
            if (currentByte < 0x128  ||           // 1-byte char. First byte is 0xxxxxxx.
                (currentByte & 0xE0) == 0xC0 ||   // 2-byte char. First byte is 110xxxxx
                (currentByte & 0xF0) == 0xE0 ||   // 3-byte char. First byte is 1110xxxx
                (currentByte & 0xF8) == 0xF0) {   //4-byte char. First byte is 11110xxx
              count++;  //Advance the counter, since we find one char.
            }
```

### ConstantValue
Condition `currentByte < 0x128` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java

            currentByte = fill.buffer.getByte(id);
            if (currentByte < 0x128  ||           // 1-byte char. First byte is 0xxxxxxx.
                (currentByte & 0xE0) == 0xC0 ||   // 2-byte char. First byte is 110xxxxx
                (currentByte & 0xF0) == 0xE0 ||   // 3-byte char. First byte is 1110xxxx
```

### ConstantValue
Condition `currentByte < 0x128 || // 1-byte char. First byte is 0xxxxxxx. (currentBy...` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java

            currentByte = fill.buffer.getByte(id);
            if (currentByte < 0x128  ||           // 1-byte char. First byte is 0xxxxxxx.
                (currentByte & 0xE0) == 0xC0 ||   // 2-byte char. First byte is 110xxxxx
                (currentByte & 0xF0) == 0xE0 ||   // 3-byte char. First byte is 1110xxxx
                (currentByte & 0xF8) == 0xF0) {   //4-byte char. First byte is 11110xxx
              count++;  //Advance the counter, since we find one char.
            }
```

### ConstantValue
Condition `id == null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/local/LocalClusterCoordinator.java`
#### Snippet
```java
        return false;
      }
      if (id == null) {
        if (other.id != null) {
          return false;
```

### ConstantValue
Condition `id == null` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/local/LocalClusterCoordinator.java`
#### Snippet
```java
      int result = 1;
      result = prime * result + getOuterType().hashCode();
      result = prime * result + ((id == null) ? 0 : id.hashCode());
      return result;
    }
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/SubsetRemover.java`
#### Snippet
```java
      return Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
    } else {
      return super.visit(other);
    }
  }
```

### ConstantValue
Condition `table == null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/AbstractSchema.java`
#### Snippet
```java
      if (table instanceof DrillTable) {
        return applyFunctionParameters((DrillTable) table, parameters, arguments);
      } else if (table == null) {
        return null;
      }
```

### ConstantValue
Condition `formatUrls == null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
        ExecConstants.BOOTSTRAP_FORMAT_PLUGINS_FILE);
    Set<URL> formatUrls = ClassPathScanner.forResource(formatBootstrapFileName, false);
    if (formatUrls == null) {
      return;
    }
```

### ConstantValue
Condition `storageUrls == null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/PluginBootstrapLoaderImpl.java`
#### Snippet
```java
        ExecConstants.BOOTSTRAP_STORAGE_PLUGINS_FILE);
    Set<URL> storageUrls = ClassPathScanner.forResource(storageBootstrapFileName, false);
    if (storageUrls == null || storageUrls.isEmpty()) {
      throw new IOException("Cannot find storage plugin boostrap file: " + storageBootstrapFileName);
    }
```

### ConstantValue
Condition `upgraded != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
    for (ConnectorLocator locator : locators) {
      StoragePlugins locatorPlugins = locator.updatedPlugins();
      if (upgraded != null) {
        upgraded.putAll(locatorPlugins);
      }
```

### ConstantValue
Condition `generator != null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JSONFormatPlugin.java`
#### Snippet
```java
      logger.error("Unable to create file " + statsTablePath.getName(), ex);
    } finally {
      if (generator != null) {
        generator.flush();
      }
```

### ConstantValue
Condition `token == JsonToken.VALUE_NULL` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/ScalarValueParser.java`
#### Snippet
```java
      } else {
        listener.onText(
            token == JsonToken.VALUE_NULL ? null : tokenizer.textValue());
      }
    }
```

### ConstantValue
Value `skipFirstLine` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
          TextFormatPlugin.HAS_HEADERS_PROP, extractHeaders);
      skipFirst = ! extractHeaders & providedSchema.booleanProperty(
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
      skipFirst = ! extractHeaders & providedSchema.booleanProperty(
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
```

### ConstantValue
Value `skipFirstLine` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
      skipFirst = ! extractHeaders & providedSchema.booleanProperty(
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
      ignoreLeadingWhitespace = providedSchema.booleanProperty(
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreLeadingWhitespace);
```

### ConstantValue
Value `ignoreLeadingWhitespace` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
          TextFormatPlugin.SKIP_FIRST_LINE_PROP, skipFirstLine);
      ignoreLeadingWhitespace = providedSchema.booleanProperty(
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreLeadingWhitespace);
      ignoreTrailingWhitespace = providedSchema.booleanProperty(
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreTrailingWhitespace);
```

### ConstantValue
Value `ignoreTrailingWhitespace` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreLeadingWhitespace);
      ignoreTrailingWhitespace = providedSchema.booleanProperty(
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreTrailingWhitespace);
      parseUnescapedQuotes = providedSchema.booleanProperty(
          TextFormatPlugin.PARSE_UNESCAPED_QUOTES_PROP, parseUnescapedQuotes);
```

### ConstantValue
Value `parseUnescapedQuotes` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextParsingSettings.java`
#### Snippet
```java
          TextFormatPlugin.TRIM_WHITESPACE_PROP, ignoreTrailingWhitespace);
      parseUnescapedQuotes = providedSchema.booleanProperty(
          TextFormatPlugin.PARSE_UNESCAPED_QUOTES_PROP, parseUnescapedQuotes);
      delimChar = overrideChar(providedSchema, TextFormatPlugin.DELIMITER_PROP, delimChar);
      quoteChar = overrideChar(providedSchema, TextFormatPlugin.QUOTE_PROP, quoteChar);
```

### ConstantValue
Condition `ch == newLine` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java

      // there's more stuff after the quoted value, not only empty spaces.
      if (!(ch == delimiter || ch == newLine) && parseUnescapedQuotes) {

        output.append(quote);
```

### ConstantValue
Condition `chars != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextReader.java`
#### Snippet
```java
    String tmp = input.getStringSinceMarkForError();
    char[] chars = tmp.toCharArray();
    if (chars != null) {
      int length = chars.length;
      if (length > settings.getMaxCharsPerColumn()) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    if (rel instanceof Aggregate) {
      canImplement = pluginImplementor.canImplement(((Aggregate) rel));
    } else if (rel instanceof Filter) {
      canImplement = pluginImplementor.canImplement(((Filter) rel));
    } else if (rel instanceof DrillLimitRelBase) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    } else if (rel instanceof Filter) {
      canImplement = pluginImplementor.canImplement(((Filter) rel));
    } else if (rel instanceof DrillLimitRelBase) {
      canImplement = pluginImplementor.canImplement(((DrillLimitRelBase) rel));
    } else if (rel instanceof Project) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    } else if (rel instanceof DrillLimitRelBase) {
      canImplement = pluginImplementor.canImplement(((DrillLimitRelBase) rel));
    } else if (rel instanceof Project) {
      canImplement = pluginImplementor.canImplement(((Project) rel));
    } else if (rel instanceof Sort) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    } else if (rel instanceof Project) {
      canImplement = pluginImplementor.canImplement(((Project) rel));
    } else if (rel instanceof Sort) {
      canImplement = pluginImplementor.canImplement(((Sort) rel));
    } else if (rel instanceof Union) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    } else if (rel instanceof Sort) {
      canImplement = pluginImplementor.canImplement(((Sort) rel));
    } else if (rel instanceof Union) {
      canImplement = pluginImplementor.canImplement(((Union) rel));
    } else if (rel instanceof Join) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/plan/rule/PluginConverterRule.java`
#### Snippet
```java
    } else if (rel instanceof Union) {
      canImplement = pluginImplementor.canImplement(((Union) rel));
    } else if (rel instanceof Join) {
      canImplement = pluginImplementor.canImplement(((Join) rel));
    }
```

### ConstantValue
Condition `!errors.isEmpty()` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetRecordWriter.java`
#### Snippet
```java
      }
    }
    if (!errors.isEmpty()) {
      throw new IOException(String.format("Failed to delete the following locations %s on file system [%s]" +
          " during aborting writer", errors, fs.getUri()));
```

### ConstantValue
Value `this.enableBytesReadCounter` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderConfig.java`
#### Snippet
```java
                             @JsonProperty("autoCorrectCorruptedDates") Boolean autoCorrectCorruptedDates,
                             @JsonProperty("enableStringsSignedMinMax") Boolean enableStringsSignedMinMax) {
    this.enableBytesReadCounter = enableBytesReadCounter == null ? this.enableBytesReadCounter : enableBytesReadCounter;
    this.enableBytesTotalCounter = enableBytesTotalCounter == null ? this.enableBytesTotalCounter : enableBytesTotalCounter;
    this.enableTimeReadCounter = enableTimeReadCounter == null ? this.enableTimeReadCounter : enableTimeReadCounter;
```

### ConstantValue
Value `this.enableBytesTotalCounter` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderConfig.java`
#### Snippet
```java
                             @JsonProperty("enableStringsSignedMinMax") Boolean enableStringsSignedMinMax) {
    this.enableBytesReadCounter = enableBytesReadCounter == null ? this.enableBytesReadCounter : enableBytesReadCounter;
    this.enableBytesTotalCounter = enableBytesTotalCounter == null ? this.enableBytesTotalCounter : enableBytesTotalCounter;
    this.enableTimeReadCounter = enableTimeReadCounter == null ? this.enableTimeReadCounter : enableTimeReadCounter;
    this.autoCorrectCorruptedDates = autoCorrectCorruptedDates == null ? this.autoCorrectCorruptedDates : autoCorrectCorruptedDates;
```

### ConstantValue
Value `this.enableTimeReadCounter` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderConfig.java`
#### Snippet
```java
    this.enableBytesReadCounter = enableBytesReadCounter == null ? this.enableBytesReadCounter : enableBytesReadCounter;
    this.enableBytesTotalCounter = enableBytesTotalCounter == null ? this.enableBytesTotalCounter : enableBytesTotalCounter;
    this.enableTimeReadCounter = enableTimeReadCounter == null ? this.enableTimeReadCounter : enableTimeReadCounter;
    this.autoCorrectCorruptedDates = autoCorrectCorruptedDates == null ? this.autoCorrectCorruptedDates : autoCorrectCorruptedDates;
    this.enableStringsSignedMinMax = enableStringsSignedMinMax == null ? this.enableStringsSignedMinMax : enableStringsSignedMinMax;
```

### ConstantValue
Value `this.autoCorrectCorruptedDates` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderConfig.java`
#### Snippet
```java
    this.enableBytesTotalCounter = enableBytesTotalCounter == null ? this.enableBytesTotalCounter : enableBytesTotalCounter;
    this.enableTimeReadCounter = enableTimeReadCounter == null ? this.enableTimeReadCounter : enableTimeReadCounter;
    this.autoCorrectCorruptedDates = autoCorrectCorruptedDates == null ? this.autoCorrectCorruptedDates : autoCorrectCorruptedDates;
    this.enableStringsSignedMinMax = enableStringsSignedMinMax == null ? this.enableStringsSignedMinMax : enableStringsSignedMinMax;
  }
```

### ConstantValue
Value `this.enableStringsSignedMinMax` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderConfig.java`
#### Snippet
```java
    this.enableTimeReadCounter = enableTimeReadCounter == null ? this.enableTimeReadCounter : enableTimeReadCounter;
    this.autoCorrectCorruptedDates = autoCorrectCorruptedDates == null ? this.autoCorrectCorruptedDates : autoCorrectCorruptedDates;
    this.enableStringsSignedMinMax = enableStringsSignedMinMax == null ? this.enableStringsSignedMinMax : enableStringsSignedMinMax;
  }

```

### ConstantValue
Condition `minValue instanceof Binary && maxValue instanceof Binary` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
          maxBytes = hasSingleValue ? minBytes : Base64.decodeBase64(maxBytes);
        }
      } else if (minValue instanceof Binary && maxValue instanceof Binary) {
        // for the case when cache file was auto-refreshed, values from parquet footers are used,
        // so there is no need to convert values, but they should be set in ColumnMetadata
```

### ConstantValue
Condition `maxValue instanceof Binary` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetReaderUtility.java`
#### Snippet
```java
          maxBytes = hasSingleValue ? minBytes : Base64.decodeBase64(maxBytes);
        }
      } else if (minValue instanceof Binary && maxValue instanceof Binary) {
        // for the case when cache file was auto-refreshed, values from parquet footers are used,
        // so there is no need to convert values, but they should be set in ColumnMetadata
```

### ConstantValue
Condition `value instanceof Binary` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
      byte[] bytes = (byte[]) value;
      return bytes.length == 0 ? 0L : new BigInteger(bytes).longValue();
    } else if (value instanceof Binary) {
      byte[] bytes = ((Binary) value).getBytes();
      return bytes.length == 0 ? 0L : new BigInteger(bytes).longValue();
```

### ConstantValue
Condition `value instanceof Binary` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
      byte[] bytes = (byte[]) value;
      return bytes.length == 0 ? 0 : new BigInteger(bytes).intValue();
    } else if (value instanceof Binary) {
      byte[] bytes = ((Binary) value).getBytes();
      return bytes.length == 0 ? 0 : new BigInteger(bytes).intValue();
```

### ConstantValue
Condition `value instanceof String` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    } else if (value instanceof byte[]) {
      return (byte[]) value;
    } else if (value instanceof String) { // value is obtained from metadata cache v2+
      return ((String) value).getBytes(StandardCharsets.UTF_8);
    } else if (value instanceof Map) { // value is obtained from metadata cache v1
```

### ConstantValue
Condition `value instanceof Map` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    } else if (value instanceof String) { // value is obtained from metadata cache v2+
      return ((String) value).getBytes(StandardCharsets.UTF_8);
    } else if (value instanceof Map) { // value is obtained from metadata cache v1
      @SuppressWarnings("unchecked")
      String bytesString = ((Map<String,String>) value).get("bytes");
```

### ConstantValue
Condition `value instanceof Long` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
        return bytesString.getBytes(StandardCharsets.UTF_8);
      }
    } else if (value instanceof Long) {
      return Longs.toByteArray((Long) value);
    } else if (value instanceof Integer) {
```

### ConstantValue
Condition `value instanceof Integer` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    } else if (value instanceof Long) {
      return Longs.toByteArray((Long) value);
    } else if (value instanceof Integer) {
      return Longs.toByteArray((Integer) value);
    } else if (value instanceof Float) {
```

### ConstantValue
Condition `value instanceof Float` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    } else if (value instanceof Integer) {
      return Longs.toByteArray((Integer) value);
    } else if (value instanceof Float) {
      return BigDecimal.valueOf((Float) value).unscaledValue().toByteArray();
    } else if (value instanceof Double) {
```

### ConstantValue
Condition `value instanceof Double` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetTableMetadataUtils.java`
#### Snippet
```java
    } else if (value instanceof Float) {
      return BigDecimal.valueOf((Float) value).unscaledValue().toByteArray();
    } else if (value instanceof Double) {
      return BigDecimal.valueOf((Double) value).unscaledValue().toByteArray();
    }
```

### ConstantValue
Condition `value.mxValue != null` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
        if (value.mxValue != null) {
          Object val;
          if (value.primitiveType == PrimitiveType.PrimitiveTypeName.BINARY && value.mxValue != null) {
            val = new String(((Binary) value.mxValue).getBytes(), StandardCharsets.UTF_8);
          } else {
```

### ConstantValue
Condition `currentValueIdx < overflowDef.numValues` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenOverflowReader.java`
#### Snippet
```java
        throw new RuntimeException();
      }
      assert currentValueIdx < overflowDef.numValues;

      if (numCachedValues > 0
```

### ConstantValue
Condition `recordsPerBatch == newRecordsPerBatch` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/batchsizing/RecordBatchSizerManager.java`
#### Snippet
```java
        continue;
      }
      assert recordsPerBatch == newRecordsPerBatch;

      // Alright, we have now found the target number of records; we need
```

### ConstantValue
Condition `totalValuesRead <= totalValuesCount` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
          readStatus.setValuesRead(valuesRead);
          readStatus.setDiskScanTime(timeToRead);
          assert (totalValuesRead <= totalValuesCount);
        }
        // You do need the synchronized block
```

### ConstantValue
Condition `buffer != DeadBuf.DEAD_BUFFER` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/selection/SelectionVector2.java`
#### Snippet
```java

  public void clear() {
    if (buffer != null && buffer != DeadBuf.DEAD_BUFFER) {
      buffer.release();
      buffer = DeadBuf.DEAD_BUFFER;
```

### ConstantValue
Value `baseWriter` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
  boolean isMapWriter() {
    return baseWriter instanceof SingleMapWriter
        || baseWriter instanceof AbstractRepeatedMapWriter;
  }

```

### ConstantValue
Value `isUserLoggedIn` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java

      logger.debug("Admin info: user: {} user group: {} userLoggedIn {} shouldShowAdminInfo: {}",
      adminUsers, adminUserGroups, isUserLoggedIn, shouldShowAdminInfo);

      return new ClusterInfo(drillbits, currentVersion, mismatchedVersions,
```

### ConstantValue
Value `shouldShowAdminInfo` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java

      logger.debug("Admin info: user: {} user group: {} userLoggedIn {} shouldShowAdminInfo: {}",
      adminUsers, adminUserGroups, isUserLoggedIn, shouldShowAdminInfo);

      return new ClusterInfo(drillbits, currentVersion, mismatchedVersions,
```

### ConstantValue
Value `shouldShowAdminInfo` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java

      return new ClusterInfo(drillbits, currentVersion, mismatchedVersions,
        userEncryptionEnabled, bitEncryptionEnabled, shouldShowAdminInfo,
        QueueInfo.build(dbContext.getResourceManager()),
        processUser, processUserGroups, adminUsers, adminUserGroups, authEnabled.get());
```

### ConstantValue
Value `shouldShowAdminInfo` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java

    return new ClusterInfo(drillbits, currentVersion, mismatchedVersions,
      userEncryptionEnabled, bitEncryptionEnabled, shouldShowAdminInfo,
      QueueInfo.build(dbContext.getResourceManager()), authEnabled.get());
  }
```

### ConstantValue
Condition `retry < PORT_HUNT_TRIES` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/WebServer.java`
#### Snippet
```java
    embeddedJetty.setDumpAfterStart(config.getBoolean(ExecConstants.HTTP_JETTY_SERVER_DUMP_AFTER_START));
    final boolean portHunt = config.getBoolean(ExecConstants.HTTP_PORT_HUNT);
    for (int retry = 0; retry < PORT_HUNT_TRIES; retry++) {
      connector.setPort(port);
      try {
```

### ConstantValue
Value `identity` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/auth/DrillSpnegoLoginService.java`
#### Snippet
```java
    }

    return identity;
  }

```

### ConstantValue
Condition `vector instanceof UnionVector` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java

    TypedFieldId.Builder builder = TypedFieldId.newBuilder().hyper(hyper);
    if (vector instanceof UnionVector) {
      builder.addId(id).remainder(expectedPath.getRootSegment().getChild());
      List<MinorType> minorTypes = ((UnionVector) vector).getSubTypes();
```

### ConstantValue
Condition `fieldReader instanceof UnionReader` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/MapUtility.java`
#### Snippet
```java
      MajorType valueMajorType = fieldReader.getType();
      MinorType valueMinorType = valueMajorType.getMinorType();
      WriterExtractor extractor = new WriterExtractor(fieldName, valueMajorType, mapWriter, fieldReader instanceof UnionReader);
      switch (valueMinorType) {
        case TINYINT:
```

### ConstantValue
Condition `cause instanceof IOException` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/JDKClassCompiler.java`
#### Snippet
```java
          throw (CompileException) cause;
        }
        if (cause instanceof IOException) {
          throw (IOException) cause;
        }
```

### ConstantValue
Value `cause` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/JDKClassCompiler.java`
#### Snippet
```java
          throw (CompileException) cause;
        }
        if (cause instanceof IOException) {
          throw (IOException) cause;
        }
```

### ConstantValue
Condition `profile.getError() != null` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/ProfileWrapper.java`
#### Snippet
```java

  public boolean hasError() {
    return profile.hasError() && profile.getError() != null;
  }

```

### ConstantValue
Condition `flagSet == largerFlags` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ReplacingBasicValue.java`
#### Snippet
```java
    assert associates == largerSet;
    assert largerSet.size() == largerSize + smallerSet.size();
    assert flagSet == largerFlags;
    assert largerSet.containsKey(this);
    assert other.associates == largerSet;
```

### ConstantValue
Condition `supportsScan(scanRel)` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
  public FileSystemPartitionDescriptor(PlannerSettings settings, TableScan scanRel) {
    Preconditions.checkArgument(scanRel instanceof DrillScanRel
      || supportsScan(scanRel));
    this.partitionLabel = settings.getFsPartitionColumnLabel();
    this.partitionLabelLength = partitionLabel.length();
```

### ConstantValue
Value `scanRel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
  public FileSystemPartitionDescriptor(PlannerSettings settings, TableScan scanRel) {
    Preconditions.checkArgument(scanRel instanceof DrillScanRel
      || supportsScan(scanRel));
    this.partitionLabel = settings.getFsPartitionColumnLabel();
    this.partitionLabelLength = partitionLabel.length();
```

### ConstantValue
Condition `supportsScan(scanRel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
        isExpandedPartial = selection.isExpandedPartial();
      }
    } else if (supportsScan(scanRel)) {
      FileSelection selection = ((FormatSelection) table.getSelection()).getSelection();
      fileLocations = selection.getFiles();
```

### ConstantValue
Value `scanRel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
        isExpandedPartial = selection.isExpandedPartial();
      }
    } else if (supportsScan(scanRel)) {
      FileSelection selection = ((FormatSelection) table.getSelection()).getSelection();
      fileLocations = selection.getFiles();
```

### ConstantValue
Value `calciteOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
          (SqlSumEmptyIsZeroAggFunction) calciteOperator,
          getFunctionListWithInference(calciteOperator.getName()));
      } else if (calciteOperator instanceof SqlAggFunction) {
        wrapper = new DrillCalciteSqlAggFunctionWrapper((SqlAggFunction) calciteOperator,
            getFunctionListWithInference(calciteOperator.getName()));
```

### ConstantValue
Value `calciteOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
        wrapper = new DrillCalciteSqlAggFunctionWrapper((SqlAggFunction) calciteOperator,
            getFunctionListWithInference(calciteOperator.getName()));
      } else if (calciteOperator instanceof SqlFunction) {
        wrapper = new DrillCalciteSqlFunctionWrapper((SqlFunction) calciteOperator,
            getFunctionListWithInference(calciteOperator.getName()));
```

### ConstantValue
Value `calciteOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
        wrapper = new DrillCalciteSqlFunctionWrapper((SqlFunction) calciteOperator,
            getFunctionListWithInference(calciteOperator.getName()));
      } else if (calciteOperator instanceof SqlBetweenOperator) {
        // During the procedure of converting to RexNode,
        // StandardConvertletTable.convertBetween expects the SqlOperator to be a subclass of SqlBetweenOperator
```

### ConstantValue
Value `calciteOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
        // For UNARY_MINUS (-) or UNARY_PLUS (+), we do not rename them as function_add or function_subtract.
        // Otherwise, Calcite will mix them up with binary operator subtract (-) or add (+)
        if (calciteOperator == SqlStdOperatorTable.UNARY_MINUS || calciteOperator == SqlStdOperatorTable.UNARY_PLUS) {
          drillOpName = calciteOperator.getName();
        } else {
```

### ConstantValue
Value `calciteOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
        // For UNARY_MINUS (-) or UNARY_PLUS (+), we do not rename them as function_add or function_subtract.
        // Otherwise, Calcite will mix them up with binary operator subtract (-) or add (+)
        if (calciteOperator == SqlStdOperatorTable.UNARY_MINUS || calciteOperator == SqlStdOperatorTable.UNARY_PLUS) {
          drillOpName = calciteOperator.getName();
        } else {
```

### ConstantValue
Value `sqlFunction` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
      return (SqlFunction) ((DrillCalciteSqlWrapper) sqlFunction).getOperator();
    } else {
      return sqlFunction;
    }
  }
```

### ConstantValue
Value `sqlAggFunction` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
      return (SqlAggFunction) ((DrillCalciteSqlWrapper) sqlAggFunction).getOperator();
    } else {
      return sqlAggFunction;
    }
  }
```

### ConstantValue
Value `sqlOperator` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/DrillCalciteWrapperUtility.java`
#### Snippet
```java
      return ((DrillCalciteSqlWrapper) sqlOperator).getOperator();
    } else {
      return sqlOperator;
    }
  }
```

### ConstantValue
Value `id` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/CompoundIdentifierConverter.java`
#### Snippet
```java
      }
    } else {
      return id;
    }
  }
```

### ConstantValue
Condition `!(opBinding instanceof SqlCallBinding)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/TypeInferenceUtils.java`
#### Snippet
```java
      final boolean isNullable = opBinding.getOperandType(1).isNullable();

      if (!(opBinding instanceof SqlCallBinding) || !(((SqlCallBinding) opBinding).operand(0) instanceof SqlCharStringLiteral)) {
        return createCalciteTypeWithNullability(factory,
            SqlTypeName.ANY,
```

### ConstantValue
Condition `!(window instanceof SqlIdentifier)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
        }

        assert !(window instanceof SqlIdentifier) : "Identifier should have been expanded as a window defined in the window list";
      }

```

### ConstantValue
Condition `definedWindow == null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java

      // In a SELECT-SCOPE, only a partition can be defined
      if (definedWindow == null) {
        definedWindow = window;
      } else {
```

### ConstantValue
Value `window` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
      // In a SELECT-SCOPE, only a partition can be defined
      if (definedWindow == null) {
        definedWindow = window;
      } else {
        if (!definedWindow.equalsDeep(window, Litmus.IGNORE)) {
```

### ConstantValue
Condition `!(table instanceof DrillTable)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/AnalyzeTableHandler.java`
#### Snippet
```java
              drillSchema.getFullSchemaName())
          .build(logger);
    } else if (!(table instanceof DrillTable)) {
      return DrillStatsTable.notSupported(context, tableName);
    }
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/ComplexUnnestVisitor.java`
#### Snippet
```java
      return visit((Uncollect) other);
    }
    return super.visit(other);
  }

```

### ConstantValue
Value `columnQualifier` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeTableHandler.java`
#### Snippet
```java
          column = SqlStdOperatorTable.LOWER.createCall(SqlParserPos.ZERO, column);
        }
        columnFilter = DrillParserUtil.createCondition(column, SqlStdOperatorTable.LIKE, columnQualifier);
      }

```

### ConstantValue
Condition `!(optionValue instanceof SqlLiteral)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SetOptionHandler.java`
#### Snippet
```java
        checkAdminPrivileges(context.getOptions());
      }
      if (!(optionValue instanceof SqlLiteral)) {
        throw UserException.validationError()
            .message("Drill does not support assigning non-literal values in SET statements.")
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
  public RelNode visit(RelNode other) {
    if (other instanceof DrillJoinRelBase ||
        other instanceof DrillAggregateRelBase ||
        other instanceof DrillSetOpRel) {
      return other;
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
    if (other instanceof DrillJoinRelBase ||
        other instanceof DrillAggregateRelBase ||
        other instanceof DrillSetOpRel) {
      return other;
    }
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
      return other;
    }
    if (other instanceof DrillLimitRel) {
      if (DrillRelOptUtil.isLimit0(((DrillLimitRel) other).getFetch())) {
        contains = true;
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
    }

    return super.visit(other);
  }

```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
          isUnsupported.value = true;
          return other;
        } else if (other instanceof DrillProjectRelBase) {
          if (!isUnsupported.value) {
            other.accept(unsupportedFunctionsVisitor);
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
          }
        }
        return super.visit(other);
      }
    };
```

### ConstantValue
Condition `!(table instanceof DrillTable)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/RefreshMetadataHandler.java`
#### Snippet
```java
      }

      if (!(table instanceof DrillTable)) {
        return notSupported(tableName);
      }
```

### ConstantValue
Condition `paramValue <= Integer.MAX_VALUE` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/PluginCost.java`
#### Snippet
```java
    public boolean isValid(Integer paramValue) {
      if (paramValue >= atleastEqualsTo &&
          paramValue <= Integer.MAX_VALUE) {
        return true;
      } else {
```

### ConstantValue
Value `createNewAggregations` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/MetastoreAnalyzeTableHandler.java`
#### Snippet
```java
          .build();

      convertedRelNode = getRowGroupAggRelNode(segmentExpressions, convertedRelNode, createNewAggregations,
          statisticsColumns, handlerContext);

```

### ConstantValue
Condition `rel instanceof DrillScanRelBase || rel.getTable().unwrap(Table.class).getStatistic().getRowC...` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdRowCount.java`
#### Snippet
```java
    // If guessing, return selectivity from RelMDRowCount
    if (DrillRelOptUtil.guessRows(rel)) {
      if (rel instanceof DrillScanRelBase
        || rel.getTable().unwrap(Table.class).getStatistic().getRowCount() != null) {
        return super.getRowCount(rel, mq);
      } else {
```

### ConstantValue
Condition `!(rel instanceof DrillScanRelBase)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdRowCount.java`
#### Snippet
```java
          return TableStatisticsKind.EST_ROW_COUNT.getValue(table.getGroupScan().getTableMetadata());
        } else {
          if (!(rel instanceof DrillScanRelBase)) {
            return table.getGroupScan().getScanStats(settings).getRecordCount();
          }
```

### ConstantValue
Condition `rel instanceof SingleRel && !DrillRelOptUtil.guessRows(rel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
        }
      }
    } else if (rel instanceof SingleRel && !DrillRelOptUtil.guessRows(rel)) {
      if (rel instanceof Window) {
        int childFieldCount = ((Window) rel).getInput().getRowType().getFieldCount();
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
        }
      }
    } else if (rel instanceof SingleRel && !DrillRelOptUtil.guessRows(rel)) {
      if (rel instanceof Window) {
        int childFieldCount = ((Window) rel).getInput().getRowType().getFieldCount();
```

### ConstantValue
Condition `rel instanceof DrillJoinRelBase && !DrillRelOptUtil.guessRows(rel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      }
      return mq.getDistinctRowCount(((SingleRel) rel).getInput(), groupKey, predicate);
    } else if (rel instanceof DrillJoinRelBase && !DrillRelOptUtil.guessRows(rel)) {
      //Assume ndv is unaffected by the join
      return getDistinctRowCountInternal(((DrillJoinRelBase) rel), mq, groupKey, predicate);
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      }
      return mq.getDistinctRowCount(((SingleRel) rel).getInput(), groupKey, predicate);
    } else if (rel instanceof DrillJoinRelBase && !DrillRelOptUtil.guessRows(rel)) {
      //Assume ndv is unaffected by the join
      return getDistinctRowCountInternal(((DrillJoinRelBase) rel), mq, groupKey, predicate);
```

### ConstantValue
Condition `rel instanceof RelSubset && !DrillRelOptUtil.guessRows(rel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      //Assume ndv is unaffected by the join
      return getDistinctRowCountInternal(((DrillJoinRelBase) rel), mq, groupKey, predicate);
    } else if (rel instanceof RelSubset && !DrillRelOptUtil.guessRows(rel)) {
      if (((RelSubset) rel).getBest() != null) {
        return mq.getDistinctRowCount(((RelSubset) rel).getBest(), groupKey, predicate);
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      //Assume ndv is unaffected by the join
      return getDistinctRowCountInternal(((DrillJoinRelBase) rel), mq, groupKey, predicate);
    } else if (rel instanceof RelSubset && !DrillRelOptUtil.guessRows(rel)) {
      if (((RelSubset) rel).getBest() != null) {
        return mq.getDistinctRowCount(((RelSubset) rel).getBest(), groupKey, predicate);
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      }
    }
    return super.getDistinctRowCount(rel, mq, groupKey, predicate);
  }

```

### ConstantValue
Condition `rel instanceof Project` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java

    // Add a final non-trivial Project to get the validatedRowType, if child is not project.
    if (rel instanceof Project && DrillRelOptUtil.isTrivialProject(topProj, true)) {
      return rel;
    } else{
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
        return ((PrelFinalizable) other).finalizeRel();
      } else {
        return super.visit(other);
      }
    }
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
    } else if (rel instanceof TableScan) {
      return getScanSelectivity(rel, mq, predicate);
    } else if (rel instanceof DrillJoinRelBase) {
      return getJoinSelectivity(((DrillJoinRelBase) rel), mq, predicate);
    } /*else if (rel instanceof SingleRel && !DrillRelOptUtil.guessRows(rel)) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
      return getSelectivity(((SingleRel)rel).getInput(), mq, predicate);
    }*/ else {
      return super.getSelectivity(rel, mq, predicate);
    }
  }
```

### ConstantValue
Condition `uniqueRefs.size() == 1` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
          uniqueRefs.add(DrillRelOptUtil.findAllRexInputRefs(orPred));
          // If equality predicate involving single column - selectivity is 1.0
          if (uniqueRefs.size() == 1) {
            try {
              RexVisitor<Void> visitor =
```

### ConstantValue
Condition `(castOp instanceof RexCall) && ("ITEM".equals(((RexCall)castOp).getOperator().getName()))` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
          return true;
        }
        if ((castOp instanceof RexCall) && ("ITEM".equals(((RexCall)castOp).getOperator().getName()))) {
          return true;
        }
```

### ConstantValue
Value `castOp` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
          return true;
        }
        if ((castOp instanceof RexCall) && ("ITEM".equals(((RexCall)castOp).getOperator().getName()))) {
          return true;
        }
```

### ConstantValue
Condition `(rex instanceof RexCall) && "ITEM".equals(((RexCall)rex).getOperator().getName())` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
      return true;
    }
    if ((rex instanceof RexCall) && "ITEM".equals(((RexCall)rex).getOperator().getName())) {
      return true;
    }
```

### ConstantValue
Value `rex` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexableExprMarker.java`
#### Snippet
```java
      return true;
    }
    if ((rex instanceof RexCall) && "ITEM".equals(((RexCall)rex).getOperator().getName())) {
      return true;
    }
```

### ConstantValue
Condition `bret` at the left side of assignment expression is always `true`. Can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/PathInExpr.java`
#### Snippet
```java
    boolean bret = true;
    for (LogicalExpression arg : call.args()) {
      bret &= arg.accept(this, null);
      if(bret == false) {
        break;
```

### ConstantValue
Result of `fields.size()` is always '0'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FunctionalIndexHelper.java`
#### Snippet
```java
    //first add row_key of primary table,
    rowkey_primary = new RelDataTypeFieldImpl(
        scan.getRowKeyName(), fields.size(),
        typeFactory.createSqlType(SqlTypeName.ANY));
    fields.add(rowkey_primary);
```

### ConstantValue
Value `leftIndexFilterPrel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
      new FilterPrel(dbScan.getCluster(), dbScan.getTraitSet(),
            dbScan, indexContext.getOrigCondition());
      lastRelNode = leftIndexFilterPrel;
    }

```

### ConstantValue
Condition `leftIndexFilterPrel == null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
      final RelDataType leftProjectRowType = leftFieldTypeBuilder.build();
      leftIndexProjectPrel = new ProjectPrel(dbScan.getCluster(), dbScan.getTraitSet(),
          leftIndexFilterPrel == null ? dbScan : leftIndexFilterPrel, leftProjectExprs, leftProjectRowType);
      lastRelNode = leftIndexProjectPrel;
    }
```

### ConstantValue
Value `isRowKeyJoin` is always 'true'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
        HashJoinPrel hjPrel = new HashJoinPrel(left.getCluster(), left.getTraitSet(), left,
            right, joinCondition, JoinRelType.INNER, false /* no swap */, null /* no runtime filter */,
            isRowKeyJoin, htControl);
        newRel = hjPrel;
      } else {
```

### ConstantValue
Value `isRowKeyJoin` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
      HashJoinPrel hjPrel = new HashJoinPrel(left.getCluster(), left.getTraitSet(), left,
          right, joinCondition, JoinRelType.INNER, false /* no swap */, null /* no runtime filter */,
          isRowKeyJoin, htControl);
      return buildRowKeyProject(hjPrel, leftRowKeyIdx);
    }
```

### ConstantValue
Value `top` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/AbstractIndexPlanGenerator.java`
#### Snippet
```java
      input = topProject.getInput();
    }
    else if (top instanceof DrillFilterRel) {
      DrillFilterRel topFilter = (DrillFilterRel)top;
      input = topFilter.getInput();
```

### ConstantValue
Value `top` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/AbstractIndexPlanGenerator.java`
#### Snippet
```java
      DrillFilterRel topFilter = (DrillFilterRel)top;
      input = topFilter.getInput();
    } else if (top instanceof DrillSortRel) {
      DrillSortRel topSort = (DrillSortRel)top;
      input = topSort.getInput();
```

### ConstantValue
Condition `startBucket >= 0` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
    }

    Preconditions.checkArgument(startBucket >= 0 && startBucket + 1 <= lastEndPointIndex, "Invalid startBucket: " + startBucket);
    Preconditions.checkArgument(endBucket >= 0 && endBucket + 1 <= lastEndPointIndex, "Invalid endBucket: " +  endBucket);
    Preconditions.checkArgument(startBucket <= endBucket,
```

### ConstantValue
Value `expr` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillProjectRelBase.java`
#### Snippet
```java
        // Simple Field reference.
        cnt++;
      } else if (expr instanceof RexCall && expr.accept(complexFieldIdentifer)) {
        // Complex field with named segments only.
        cnt++;
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
      if (rel instanceof TableScan) {
        return (TableScan) rel;
      } else if (rel instanceof RelSubset) {
        RelSubset relSubset = (RelSubset) rel;
        return findScan(Util.first(relSubset.getBest(), relSubset.getOriginal()));
```

### ConstantValue
Value `rex` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
        }
        return call.clone(call.getType(), builder.build());
      } else if (rex instanceof RexInputRef) {
        RexInputRef var = (RexInputRef) rex;
        int index = var.getIndex();
```

### ConstantValue
Value `rex` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
        return rexBuilder.makeInputRef(var.getType(), inputRefMap.get(index));
      } else {
        return rex;
      }
    }
```

### ConstantValue
Condition `rel instanceof DrillProjectRel || rel instanceof DrillFilterRel || rel instanceof Dr...` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    if (rel instanceof DrillScanRel) {
      return rel;
    } else if (rel instanceof DrillProjectRel
        || rel instanceof DrillFilterRel
        || rel instanceof DrillLimitRel) {
      for (RelNode child : rel.getInputs()) {
        RelNode tgt = getValidJoinInput(child);
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    if (rel instanceof DrillScanRel) {
      return rel;
    } else if (rel instanceof DrillProjectRel
        || rel instanceof DrillFilterRel
        || rel instanceof DrillLimitRel) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
      return rel;
    } else if (rel instanceof DrillProjectRel
        || rel instanceof DrillFilterRel
        || rel instanceof DrillLimitRel) {
      for (RelNode child : rel.getInputs()) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    } else if (rel instanceof DrillProjectRel
        || rel instanceof DrillFilterRel
        || rel instanceof DrillLimitRel) {
      for (RelNode child : rel.getInputs()) {
        RelNode tgt = getValidJoinInput(child);
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
        }
      }
    } else if (rel instanceof HepRelVertex) {
      return getValidJoinInput(((HepRelVertex) rel).getCurrentRel());
    } else if (rel instanceof RelSubset) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    } else if (rel instanceof HepRelVertex) {
      return getValidJoinInput(((HepRelVertex) rel).getCurrentRel());
    } else if (rel instanceof RelSubset) {
      if (((RelSubset) rel).getBest() != null) {
        return getValidJoinInput(((RelSubset) rel).getBest());
```

### ConstantValue
Condition `curRel != null && !(curRel instanceof DrillScanRel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    RelNode curRel = rel;
    int curIndex = index;
    while (curRel != null && !(curRel instanceof DrillScanRel)) {
      logger.debug("IsRowKeyColumn: Rel={}, RowTypePos={}, RowType={}", curRel.toString(), curIndex,
          curRel.getRowType().toString());
```

### ConstantValue
Condition `!(curRel instanceof DrillScanRel)` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    RelNode curRel = rel;
    int curIndex = index;
    while (curRel != null && !(curRel instanceof DrillScanRel)) {
      logger.debug("IsRowKeyColumn: Rel={}, RowTypePos={}, RowType={}", curRel.toString(), curIndex,
          curRel.getRowType().toString());
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
      if (rel instanceof HepRelVertex) {
        findRelSequenceInternal(classes, idx, ((HepRelVertex) rel).getCurrentRel(), matchingRels);
      } else if (rel instanceof RelSubset) {
        if (((RelSubset) rel).getBest() != null) {
          findRelSequenceInternal(classes, idx, ((RelSubset) rel).getBest(), matchingRels);
```

### ConstantValue
Condition `classes[idx].isInstance(rel)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
          findRelSequenceInternal(classes, idx, ((RelSubset) rel).getOriginal(), matchingRels);
        }
      } else if (classes[idx].isInstance(rel)) {
        matchingRels.add(rel);
        if (idx + 1 < classes.length && rel.getInputs().size() > 0) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
          findRelSequenceInternal(classes, idx, ((RelSubset) rel).getOriginal(), matchingRels);
        }
      } else if (classes[idx].isInstance(rel)) {
        matchingRels.add(rel);
        if (idx + 1 < classes.length && rel.getInputs().size() > 0) {
```

### ConstantValue
Condition `rowKeyLoc == RowKey.LEFT` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    }
    if (rightScan != null && hasRightRowKeyCol) {
      if (rowKeyLoc == RowKey.LEFT) {
        rowKeyLoc = RowKey.BOTH;
      } else {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
    } else if (rel instanceof HepRelVertex) {
      return canSwapJoinInputsInternal(((HepRelVertex) rel).getCurrentRel());
    } else if (rel instanceof RelSubset) {
      if (((RelSubset) rel).getBest() != null) {
        return canSwapJoinInputsInternal(((RelSubset) rel).getBest());
```

### ConstantValue
Value `sqlAggFunction` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
      SqlAggFunction sqlAggFunction = DrillCalciteWrapperUtility.extractSqlOperatorFromWrapper(call.getAggregation());
      if (sqlAggFunction instanceof SqlAvgAggFunction
          || sqlAggFunction instanceof SqlSumAggFunction) {
        return true;
      }
```

### ConstantValue
Value `sqlAggFunction` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
      return reduceSum(oldAggRel, oldCall, newCalls, aggCallMapping);
    }
    if (sqlAggFunction instanceof SqlAvgAggFunction) {
      // for DECIMAL data types does not produce rewriting of complex calls,
      // since SUM returns value with 38 precision and further handling of the value
```

### ConstantValue
Value `planHasMemory` is always 'false'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/DefaultQueryParallelizer.java`
#### Snippet
```java
    }
    List<PhysicalOperator> bufferedOpers = planningSet.getRootWrapper().getNode().getBufferedOperators(queryContext);
    MemoryAllocationUtilities.setupBufferedOpsMemoryAllocations(planHasMemory, bufferedOpers, queryContext);
  }

```

### ConstantValue
Condition `currentDist.equals(DrillDistributionTrait.DEFAULT) && !(rel instanceof RelSubset)` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DrillDistributionTraitDef.java`
#### Snippet
```java
    // We do not want to convert from "ANY", since it's abstract.
    // Source trait should be concrete type: SINGLETON, HASH_DISTRIBUTED, etc.
    if (currentDist.equals(DrillDistributionTrait.DEFAULT) && !(rel instanceof RelSubset) ) {
        return null;
    }
```

### ConstantValue
Condition `!(rel instanceof RelSubset)` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DrillDistributionTraitDef.java`
#### Snippet
```java
    // We do not want to convert from "ANY", since it's abstract.
    // Source trait should be concrete type: SINGLETON, HASH_DISTRIBUTED, etc.
    if (currentDist.equals(DrillDistributionTrait.DEFAULT) && !(rel instanceof RelSubset) ) {
        return null;
    }
```

### ConstantValue
Condition `output != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/PruneScanRule.java`
#### Snippet
```java
      } finally {
        container.clear();
        if (output != null) {
          output.clear();
        }
```

### ConstantValue
Condition `!isSemiJoin` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/HashJoinPrel.java`
#### Snippet
```java
      boolean isRowKeyJoin, int joinControl, boolean semiJoin) throws InvalidRelException {
    super(cluster, traits, left, right, condition, joinType, semiJoin);
    Preconditions.checkArgument(isSemiJoin && !swapped || swapped && !isSemiJoin || (!swapped && !isSemiJoin));
    if (isSemiJoin) {
      Preconditions.checkArgument(!swapped, "swapping of inputs is not allowed for semi-joins");
```

### ConstantValue
Condition `traitProp` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPruleBase.java`
#### Snippet
```java
    boolean traitProp = false;

    if(traitProp){
      if (physicalJoinType == PhysicalJoinType.MERGE_JOIN) {
        new SubsetTransformer<DrillJoin, InvalidRelException>(call) {
```

### ConstantValue
Condition `!(setOp instanceof DrillExceptRel)` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/SetOpPrule.java`
#### Snippet
```java

  private boolean needAddAgg(SetOp setOp) {
    if (setOp.all || !(setOp instanceof DrillExceptRel)) {
      return false;
    }
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
      return true;
    }
    if (rel instanceof RelSubset) {
      if (((RelSubset) rel).getBest() != null) {
        if (findRowKeyJoin(((RelSubset) rel).getBest())) {
```

### ConstantValue
Value `rel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
        }
      }
    } else if (rel instanceof HepRelVertex) {
      if (((HepRelVertex) rel).getCurrentRel() != null) {
        if (findRowKeyJoin(((HepRelVertex) rel).getCurrentRel())) {
```

### ConstantValue
Condition `prel instanceof Project` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/TopProjectVisitor.java`
#### Snippet
```java
        true);  //outputProj = true : NONE -> OK_NEW_SCHEMA, also handle expression with NULL type.

    if (prel instanceof Project && DrillRelOptUtil.isTrivialProject(topProject, true)) {
      return new ProjectPrel(prel.getCluster(),
          prel.getTraitSet(),
```

### ConstantValue
Value `rightRelNode` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
      return (ExchangePrel) rightRelNode;
    }
    if (rightRelNode instanceof ScanPrel) {
      return null;
    } else if (rightRelNode instanceof RelSubset) {
```

### ConstantValue
Value `rightRelNode` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
    if (rightRelNode instanceof ScanPrel) {
      return null;
    } else if (rightRelNode instanceof RelSubset) {
      RelNode bestNode = ((RelSubset) rightRelNode).getBest();
      if (bestNode != null) {
```

### ConstantValue
Value `leftRelNode` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
        return null;
      }
    } else if (leftRelNode instanceof RelSubset) {
      RelNode bestNode = ((RelSubset) leftRelNode).getBest();
      if (bestNode != null) {
```

### ConstantValue
Value `prel` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
        currentPrel = (Prel) ((RelSubset) prel).getBest();
      } else {
        currentPrel = prel;
      }

```

### ConstantValue
Condition `this.iterator() == null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractBase.java`
#### Snippet
```java
  public void accept(GraphVisitor<PhysicalOperator> visitor) {
    visitor.enter(this);
    if (this.iterator() == null) {
      throw new IllegalArgumentException("Null iterator for pop." + this);
    }
```

### ConstantValue
Value `stats` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/BaseRootExec.java`
#### Snippet
```java
                      final PhysicalOperator config) throws OutOfMemoryException {
    if (oContext == null) {
      this.oContext = fragmentContext.newOperatorContext(config, stats);
    } else {
      this.oContext = oContext;
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinUtils.java`
#### Snippet
```java
      if (other instanceof RelSubset) {
        return visit((RelSubset) other);
      } else if (other instanceof Project) {
        return visit((Project) other);
      }
```

### ConstantValue
Value `other` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinUtils.java`
#### Snippet
```java
        return visit((Project) other);
      }
      return super.visit(other);
    }

```

### ConstantValue
Condition `result != leftType` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/JoinUtils.java`
#### Snippet
```java
              ExpressionTreeMaterializer.materialize(castExpr, rightBatch, errorCollector,
                  context.getFunctionRegistry());
        } else if (result != leftType) {
          // Add a cast expression on top of the left expression
          LogicalExpression castExpr = ExpressionTreeMaterializer.addCastExpression(leftExpression, rightExpression.getMajorType(), context.getFunctionRegistry(), errorCollector);
```

### ConstantValue
Condition `leftRowId <= currentRowId` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
        }
      } else {
        Preconditions.checkState(leftRowId <= currentRowId, "Unexpected case where rowId " +
          "%s in right batch of lateral is smaller than rowId %s in left batch being processed",
          currentRowId, leftRowId);
```

### ConstantValue
Condition `nullCols != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/NullColumnBuilder.java`
#### Snippet
```java

  public boolean hasColumns() {
    return nullCols != null && ! nullCols.isEmpty();
  }

```

### ConstantValue
Condition `tableLoader != null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/framework/ShimBatchReader.java`
#### Snippet
```java

      assert tableLoader == null;
      if (tableLoader != null) {
        logger.warn("Reader " + reader.getClass().getSimpleName() +
            " returned false from open, but negotiated a schema.");
```

### ConstantValue
Condition `path != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
    public void deleteDir(String fragmentSpillDir) throws IOException {
      Path path = new Path(fragmentSpillDir);
      if (path != null && fs.exists(path)) {
        if (fs.delete(path, true)) {
            fs.cancelDeleteOnExit(path);
```

### ConstantValue
Condition `nextBatch != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashPartition.java`
#### Snippet
```java
      final IndexPointer htIndex = new IndexPointer();

      assert nextBatch != null;
      assert probeBatch != null;

```

### ConstantValue
Condition `idxWithinBatch == EMPTY_SLOT` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashTableTemplate.java`
#### Snippet
```java
      while (true) {
        int idxWithinBatch = idx & BATCH_MASK;
        if (idxWithinBatch == EMPTY_SLOT) {
          break;
        } else {
```

### ConstantValue
Condition `result != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java

      String outputName = getRef(namedExpression).getRootSegment().getPath();
      if (result != null && result.outputNames != null && result.outputNames.size() > 0) {
        for (int j = 0; j < result.outputNames.size(); j++) {
          if (!result.outputNames.get(j).equals(EMPTY_STRING)) {
```

### ConstantValue
Condition `refHasPrefix` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
    }
    // input and output have prefixes although they could be different...
    else if (exprHasPrefix && refHasPrefix) {
      String[] input = expr.getPath().split(StarColumnHelper.PREFIX_DELIMITER,
          2);
```

### ConstantValue
Condition `result != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectionMaterializer.java`
#### Snippet
```java
    }
    String outputName = getRef(namedExpression).getRootSegment().getPath();
    if (result != null && result.outputNames != null
        && result.outputNames.size() > 0) {
      boolean isMatched = false;
```

### ConstantValue
Condition `previous != null` is always `true`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggTemplate.java`
#### Snippet
```java
        } finally {
          // make sure to clear previous
          if (previous != null) {
            previous.clear();
          }
```

### ConstantValue
Condition `vector instanceof NullableVarBinaryVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarCharVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVarBinaryVector) {
      return ((NullableVarBinaryVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarCharVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVarBinaryVector) {
      return ((NullableVarBinaryVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Condition `vector instanceof NullableVarDecimalVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarBinaryVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVarDecimalVector) {
      return ((NullableVarDecimalVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarBinaryVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVarDecimalVector) {
      return ((NullableVarDecimalVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Condition `vector instanceof NullableVar16CharVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarDecimalVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVar16CharVector) {
      return ((NullableVar16CharVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      return ((NullableVarDecimalVector) vector).getMutator().getLastSet();
    }
    if (vector instanceof NullableVar16CharVector) {
      return ((NullableVar16CharVector) vector).getMutator().getLastSet();
    }
```

### ConstantValue
Condition `vector instanceof NullableVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof RepeatedBitVector) {
      validateRepeatedBitVector(name, (RepeatedBitVector) vector);
    } else if (vector instanceof NullableVector) {
      validateNullableVector(name, (NullableVector) vector);
    } else if (vector instanceof VarCharVector) {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof RepeatedBitVector) {
      validateRepeatedBitVector(name, (RepeatedBitVector) vector);
    } else if (vector instanceof NullableVector) {
      validateNullableVector(name, (NullableVector) vector);
    } else if (vector instanceof VarCharVector) {
```

### ConstantValue
Condition `vector instanceof VarCharVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof NullableVector) {
      validateNullableVector(name, (NullableVector) vector);
    } else if (vector instanceof VarCharVector) {
      validateVarCharVector(name, (VarCharVector) vector);
    } else if (vector instanceof VarBinaryVector) {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof NullableVector) {
      validateNullableVector(name, (NullableVector) vector);
    } else if (vector instanceof VarCharVector) {
      validateVarCharVector(name, (VarCharVector) vector);
    } else if (vector instanceof VarBinaryVector) {
```

### ConstantValue
Condition `vector instanceof VarBinaryVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarCharVector) {
      validateVarCharVector(name, (VarCharVector) vector);
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarCharVector) {
      validateVarCharVector(name, (VarCharVector) vector);
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
```

### ConstantValue
Condition `vector instanceof FixedWidthVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
```

### ConstantValue
Condition `vector instanceof FixedWidthVector || vector instanceof ZeroVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
      // count itself, which was already done. There is no inner
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof VarBinaryVector) {
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
```

### ConstantValue
Condition `vector instanceof ZeroVector` is always `false` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
      // count itself, which was already done. There is no inner
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      validateVarBinaryVector(name, (VarBinaryVector) vector);
    } else if (vector instanceof FixedWidthVector ||
               vector instanceof ZeroVector) {
      // Not much to do. The only item to check is the vector
      // count itself, which was already done. There is no inner
```

### ConstantValue
Condition `vector instanceof BaseRepeatedValueVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      // count itself, which was already done. There is no inner
      // structure to check.
    } else if (vector instanceof BaseRepeatedValueVector) {
      validateRepeatedVector(name, (BaseRepeatedValueVector) vector);
    } else if (vector instanceof AbstractRepeatedMapVector) { // RepeatedMapVector or DictVector
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      // count itself, which was already done. There is no inner
      // structure to check.
    } else if (vector instanceof BaseRepeatedValueVector) {
      validateRepeatedVector(name, (BaseRepeatedValueVector) vector);
    } else if (vector instanceof AbstractRepeatedMapVector) { // RepeatedMapVector or DictVector
```

### ConstantValue
Condition `vector instanceof AbstractRepeatedMapVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof BaseRepeatedValueVector) {
      validateRepeatedVector(name, (BaseRepeatedValueVector) vector);
    } else if (vector instanceof AbstractRepeatedMapVector) { // RepeatedMapVector or DictVector
      // In case of DictVector, keys and values vectors are not validated explicitly to avoid NPE
      // when keys and values vectors are not set. This happens when output dict vector's keys and
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof BaseRepeatedValueVector) {
      validateRepeatedVector(name, (BaseRepeatedValueVector) vector);
    } else if (vector instanceof AbstractRepeatedMapVector) { // RepeatedMapVector or DictVector
      // In case of DictVector, keys and values vectors are not validated explicitly to avoid NPE
      // when keys and values vectors are not set. This happens when output dict vector's keys and
```

### ConstantValue
Condition `vector instanceof MapVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      // input reader is an instance of NullReader for all rows which does not have schema.
      validateRepeatedMapVector(name, (AbstractRepeatedMapVector) vector);
    } else if (vector instanceof MapVector) {
      validateMapVector(name, (MapVector) vector);
    } else if (vector instanceof RepeatedListVector) {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
      // input reader is an instance of NullReader for all rows which does not have schema.
      validateRepeatedMapVector(name, (AbstractRepeatedMapVector) vector);
    } else if (vector instanceof MapVector) {
      validateMapVector(name, (MapVector) vector);
    } else if (vector instanceof RepeatedListVector) {
```

### ConstantValue
Condition `vector instanceof RepeatedListVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof MapVector) {
      validateMapVector(name, (MapVector) vector);
    } else if (vector instanceof RepeatedListVector) {
      validateRepeatedListVector(name, (RepeatedListVector) vector);
    } else if (vector instanceof UnionVector) {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof MapVector) {
      validateMapVector(name, (MapVector) vector);
    } else if (vector instanceof RepeatedListVector) {
      validateRepeatedListVector(name, (RepeatedListVector) vector);
    } else if (vector instanceof UnionVector) {
```

### ConstantValue
Condition `vector instanceof UnionVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof RepeatedListVector) {
      validateRepeatedListVector(name, (RepeatedListVector) vector);
    } else if (vector instanceof UnionVector) {
      validateUnionVector(name, (UnionVector) vector);
    } else if (vector instanceof VarDecimalVector) {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof RepeatedListVector) {
      validateRepeatedListVector(name, (RepeatedListVector) vector);
    } else if (vector instanceof UnionVector) {
      validateUnionVector(name, (UnionVector) vector);
    } else if (vector instanceof VarDecimalVector) {
```

### ConstantValue
Condition `vector instanceof VarDecimalVector` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof UnionVector) {
      validateUnionVector(name, (UnionVector) vector);
    } else if (vector instanceof VarDecimalVector) {
      validateVarDecimalVector(name, (VarDecimalVector) vector);
    } else {
```

### ConstantValue
Value `vector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else if (vector instanceof UnionVector) {
      validateUnionVector(name, (UnionVector) vector);
    } else if (vector instanceof VarDecimalVector) {
      validateVarDecimalVector(name, (VarDecimalVector) vector);
    } else {
```

### ConstantValue
Condition `statistic == null` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/MergedStatisticFactory.java`
#### Snippet
```java
    try {
      MergedStatistic statistic = instance.newMergedStatistic(outputStatName);
      if (statistic == null) {
        throw new IllegalArgumentException("No implementation found for " + outputStatName);
      } else {
```

### ConstantValue
Condition `upstream == IterOutcome.NONE` is always `false`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
        builder.add(incoming);
        recordsSampled += incoming.getRecordCount();
        if (upstream == IterOutcome.NONE) {
          break;
        }
```

### ConstantValue
Condition `finalTable == null` is always `true` when reached
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
        waitUntilTimeOut(10);
      }
      for (int i = 0; i < 100 && finalTable == null; i++) {
        finalTable = tableMap.get(finalTableKey);
        if (finalTable != null) {
```

### ConstantValue
Value `rawBatch` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
            checkContinue();
            createDummyBatch = true;
            rawBatches.add(rawBatch);
            p++; // move to next sender
            continue;
```

### ConstantValue
Value `dataVector` is always 'null'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/single/SingleSchemaInference.java`
#### Snippet
```java
    final VariantSchema schema = new VariantSchema();
    if (! vector.isEmptyType()) {
      schema.addType(inferVector(dataVector));
    }
    return schema;
```

### ConstantValue
Condition `Stream.of(partitionSpecSchemaFields).anyMatch(Objects::isNull)` is always `false`
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/schema/IcebergTableSchema.java`
#### Snippet
```java
    }

    if (Stream.of(partitionSpecSchemaFields).anyMatch(Objects::isNull)) {
      throw new IcebergMetastoreException(String.format(
        "Some of partition fields are missing in the class [%s]. Partition keys: %s. Partition values: %s.",
```

### ConstantValue
Method reference result is always 'false'
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/schema/IcebergTableSchema.java`
#### Snippet
```java
    }

    if (Stream.of(partitionSpecSchemaFields).anyMatch(Objects::isNull)) {
      throw new IcebergMetastoreException(String.format(
        "Some of partition fields are missing in the class [%s]. Partition keys: %s. Partition values: %s.",
```

### ConstantValue
Condition `paramValue <= Integer.MAX_VALUE` is always `true` when reached
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/PluginConstants.java`
#### Snippet
```java
     @Override
    public boolean isValid(Integer paramValue) {
      if (paramValue > 0 && paramValue <= Integer.MAX_VALUE) {
        return true;
      } else {
```

### ConstantValue
Condition `listOfScan.size() > minPerEndpointSlot` is always `true`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
      if (listOfScan.size() <= minPerEndpointSlot) {
        minHeap.offer(listOfScan);
      } else if (listOfScan.size() > minPerEndpointSlot){
        maxHeap.offer(listOfScan);
      }
```

### ConstantValue
Condition `columnInfo == null` is always `false`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushProjectIntoScan.java`
#### Snippet
```java
      DrillRelOptUtil.ProjectPushInfo columnInfo =
          DrillRelOptUtil.getFieldsInformation(scan.getRowType(), project.getProjects());
      if (columnInfo == null || Utilities.isStarQuery(columnInfo.getFields())
          || !groupScan.canPushdownProjects(columnInfo.getFields())) {
        return;
```

### ConstantValue
Value `trait` is always 'null'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushProjectIntoScan.java`
#### Snippet
```java
      for (RelTrait trait : scan.getTraitSet()) {
        if (!(trait instanceof RelCollation)) {
          newTraits.plus(trait);
        }
      }
```

### ConstantValue
Condition `processor != null` is always `true`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonConditionBuilder.java`
#### Snippet
```java
        case "ojai_condition": {
          final OjaiFunctionsProcessor processor = OjaiFunctionsProcessor.process(call);
          if (processor != null) {
                  return new JsonScanSpec(groupScan.getTableName(), groupScan.getIndexDesc(),
                                  processor.getCondition());
```

### ConstantValue
Condition `ignoreSchemaChange` is always `false`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/MaprDBJsonRecordReader.java`
#### Snippet
```java
    } catch (SchemaChangeException e) {
      String err_row = reader.getId().asJsonString();
      if (ignoreSchemaChange) {
        logger.warn("{}. Dropping row '{}' from result.", e.getMessage(), err_row);
        logger.debug("Stack trace:", e);
```

### ConstantValue
Value `condition` is always 'null'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
        getInputRefMapping(op, rowType, mapping);
      }
    } else if (condition instanceof RexInputRef) {
      mapping.put(condition.toString(),
          rowType.getFieldNames().get(condition.hashCode()));
```

### ConstantValue
Condition `scanRel instanceof ScanPrel && ((ScanPrel)scanRel).getGroupScan() instanceof DbGroupScan` is always `false` when reached
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
    Map<String, StatisticsPayload> payloadMap;
    if ((scanRel instanceof DrillScanRel && ((DrillScanRel)scanRel).getGroupScan() instanceof DbGroupScan)
        || (scanRel instanceof ScanPrel && ((ScanPrel)scanRel).getGroupScan() instanceof DbGroupScan)) {
      if (condition == null && fullTableScanPayload != null) {
        return fullTableScanPayload.getRowCount();
```

### ConstantValue
Condition `isInterrupted` is always `false`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ReconnectingConnection.java`
#### Snippet
```java
      }

      if (isInterrupted) {
        // Preserve evidence that the interruption occurred so that code higher up on the call stack can learn of the
        // interruption and respond to it if it wants to.
```

### ConstantValue
Condition `e instanceof BindException` is always `false`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
        // One case is "java.io.IOException: bind() failed: Address already in
        // use".
        if (e instanceof BindException && allowPortHunting) {
          continue;
        }
```

### ConstantValue
Condition `e instanceof BindException && allowPortHunting` is always `false`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
        // One case is "java.io.IOException: bind() failed: Address already in
        // use".
        if (e instanceof BindException && allowPortHunting) {
          continue;
        }
```

### ConstantValue
Condition `pBuffer != null` is always `false`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RpcBus.java`
#### Snippet
```java

      if (!completed) {
        if (pBuffer != null) {
          pBuffer.release();
        }
```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/plan/IcebergPluginImplementor.java`
#### Snippet
```java
        offset = limitRelBase.getOffset();
        return other;
      } else if (other instanceof RelSubset) {
        RelSubset relSubset = (RelSubset) other;
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
```

### ConstantValue
Value `other` is always 'null'
in `contrib/format-iceberg/src/main/java/org/apache/drill/exec/store/iceberg/plan/IcebergPluginImplementor.java`
#### Snippet
```java
        Util.first(relSubset.getBest(), relSubset.getOriginal()).accept(this);
      }
      return super.visit(other);
    }

```

### ConstantValue
Value `isAssertEnabled` is always 'true'
in `exec/memory/base/src/main/java/io/netty/buffer/UnsafeDirectLittleEndian.java`
#### Snippet
```java
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }
}
```

### ConstantValue
Value `isAssertEnabled` is always 'true'
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }
}
```

### ConstantValue
Condition `lastColumn < 0` is always `false`
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelFormatConfig.java`
#### Snippet
```java
    }

    if (lastColumn < 0) {
      throw UserException
        .validationError()
```

### ConstantValue
Value `readerConfig.useTempFilePackageParts` is always 'true'
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelBatchReader.java`
#### Snippet
```java

      if (readerConfig.useTempFilePackageParts) {
        ZipPackage.setUseTempFilePackageParts(readerConfig.useTempFilePackageParts);
      }

```

### ConstantValue
Result of `names.size()` is always '0'
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/util/SchemaPathUtils.java`
#### Snippet
```java
    List<String> names = new ArrayList<>(types.size());
    // Used in case of LIST; defined here to avoid many instantiations inside while-loop
    List<String> nextNames = new ArrayList<>(names.size());
    ColumnMetadata colMetadata;
    while (!colPath.isLastPath()) {
```

### ConstantValue
Value `isAssertEnabled` is always 'true'
in `common/src/main/java/org/apache/drill/exec/util/AssertionUtil.java`
#### Snippet
```java
    boolean isAssertEnabled = false;
    assert isAssertEnabled = true;
    ASSERT_ENABLED = isAssertEnabled;
  }

```

### ConstantValue
Condition `subtypes1 == null` is always `false`
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      return true;
    }
    if (subtypes1 == null || subtypes2 == null) {
      return false;
    }
```

### ConstantValue
Condition `subtypes1 == null || subtypes2 == null` is always `false`
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      return true;
    }
    if (subtypes1 == null || subtypes2 == null) {
      return false;
    }
```

### ConstantValue
Condition `subtypes2 == null` is always `false` when reached
in `common/src/main/java/org/apache/drill/common/types/Types.java`
#### Snippet
```java
      return true;
    }
    if (subtypes1 == null || subtypes2 == null) {
      return false;
    }
```

### ConstantValue
Value `other` is always 'null'
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/plan/ElasticPlanTransformer.java`
#### Snippet
```java
        return input;
      }
    } else if (other instanceof ElasticsearchFilter) {
      ElasticsearchFilter filter = (ElasticsearchFilter) other;
      RexNode convertedCondition = filter.getCondition().accept(
```

### ConstantValue
Value `other` is always 'null'
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/plan/ElasticPlanTransformer.java`
#### Snippet
```java
          new ElasticExpressionMapper(other.getCluster().getRexBuilder(), filter.getInput().getRowType(), mapField));
      return filter.copy(other.getTraitSet(), filter.getInput().accept(this), convertedCondition);
    } else if (other instanceof ElasticsearchSort) {
      ElasticsearchSort sort = (ElasticsearchSort) other;
      RelNode input = getMappedInput(sort.getInput());
```

### ConstantValue
Value `other` is always 'null'
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/plan/ElasticPlanTransformer.java`
#### Snippet
```java
      RelNode input = getMappedInput(sort.getInput());
      return sort.copy(other.getTraitSet(), input, sort.getCollation(), sort.offset, sort.fetch);
    } else if (other instanceof ElasticsearchAggregate) {
      ElasticsearchAggregate aggregate = (ElasticsearchAggregate) other;
      RelNode input = getMappedInput(aggregate.getInput());
```

### ConstantValue
Value `other` is always 'null'
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/plan/ElasticPlanTransformer.java`
#### Snippet
```java
    }

    return super.visit(other);
  }

```

## RuleId[id=NonAtomicOperationOnVolatileField]
### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `taskCounter`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/Task.java`
#### Snippet
```java

  public Task(Scheduler scheduler, TaskSpec taskSpec) {
    taskId = ++taskCounter;
    this.scheduler = scheduler;
    this.taskSpec = taskSpec;
```

### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `currentSizeInMemory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
    DrillBuf body = batch.getBody();
    if (body != null) {
      currentSizeInMemory -= body.capacity();
    }
    if (isCurrentlySpooling() && currentSizeInMemory < threshold * STOP_SPOOLING_FRACTION) {
```

### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `currentSizeInMemory`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
    boolean spoolCurrentBatch = isCurrentlySpooling();
    wrapper = new RawFragmentBatchWrapper(batch, !spoolCurrentBatch);
    currentSizeInMemory += wrapper.getBodySize();
    if (spoolCurrentBatch) {
      if (spooler == null) {
```

### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `instanceCount`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/IteratorValidatorBatchIterator.java`
#### Snippet
```java
  private final int instNum;
  {
    instNum = ++instanceCount;
  }

```

### NonAtomicOperationOnVolatileField
Non-atomic operation on volatile field `connect`
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicServer.java`
#### Snippet
```java
    }

    connect = !connect;
    logger.debug("Server of type {} started on port {}.", getClass().getSimpleName(), port);
    return port;
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

    if (catalog != null) {
      sb.append(" AND TABLE_CATALOG = '" + DrillStringUtils.escapeSql(catalog) + "' ");
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

    if (schemaPattern.s != null) {
      sb.append(" AND TABLE_SCHEMA like '" + DrillStringUtils.escapeSql(schemaPattern.s) + "'");
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

    if (tableNamePattern.s != null) {
      sb.append(" AND TABLE_NAME like '" + DrillStringUtils.escapeSql(tableNamePattern.s) + "'");
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
          sb.append(" OR ");
        }
        sb.append(" TABLE_TYPE LIKE '" + DrillStringUtils.escapeSql(typeList.get(t)) + "' ");
      }
      sb.append(")");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

    if (catalog != null) {
      sb.append("\n  AND TABLE_CATALOG = '" + DrillStringUtils.escapeSql(catalog) + "'");
    }
    if (schemaPattern.s != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
    }
    if (schemaPattern.s != null) {
      sb.append("\n  AND TABLE_SCHEMA like '" + DrillStringUtils.escapeSql(schemaPattern.s) + "'");
    }
    if (tableNamePattern.s != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
    }
    if (tableNamePattern.s != null) {
      sb.append("\n  AND TABLE_NAME like '" + DrillStringUtils.escapeSql(tableNamePattern.s) + "'");
    }
    if (columnNamePattern.s != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
    }
    if (columnNamePattern.s != null) {
      sb.append("\n  AND COLUMN_NAME like '" + DrillStringUtils.escapeSql(columnNamePattern.s) + "'");
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java

    if (catalog != null) {
      sb.append(" AND CATALOG_NAME = '" + DrillStringUtils.escapeSql(catalog) + "' ");
    }
    if (schemaPattern.s != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillMetaImpl.java`
#### Snippet
```java
    }
    if (schemaPattern.s != null) {
      sb.append(" AND SCHEMA_NAME like '" + DrillStringUtils.escapeSql(schemaPattern.s) + "'");
    }
    sb.append(" ORDER BY CATALOG_NAME, SCHEMA_NAME");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorInitializer.java`
#### Snippet
```java
  public String toString() {
    StringBuilder buf = new StringBuilder();
    buf.append("[" + getClass().getSimpleName())
       .append(" ");
    boolean first = true;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/TypedFieldId.java`
#### Snippet
```java
      lookupLevel[1] = fieldIds[i + 1];
      topLevel = topLevel.getChildWrapper(lookupLevel);
      name.append(SEPARATOR + topLevel.getField().getName());
    }
    return name.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
    format.setMaximumFractionDigits(3);

    sb.append("<table class=\"table table-bordered text-right"+(isSortable? " sortable" : NO_BGCOLOR)+"\">\n<thead><tr>");
    for (int i = 0; i < columns.length; i++) {
      String cn = columns[i];
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
        }
      }
      sb.append("<th" + ctt + ">" + cn + "</th>");
    }
    sb.append("</tr></thead>\n<tbody>\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/TableBuilder.java`
#### Snippet
```java
    //Check if this is first column?
    if (w == 0) {
      sb.append("<tr"
          + (rowBackgroundColor == null || rowBackgroundColor == NO_BGCOLOR ? "" : " style=\"background-color:"+rowBackgroundColor+"\"")
          + ">");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ReplacingBasicValue.java`
#### Snippet
```java
    sb.append('\n');
    indent(sb, indentLevel + 1);
    sb.append(" index: " + index);
    sb.append(' ');
    flagSet.dump(sb);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java

  public void dump(final StringBuilder sb) {
    sb.append("ValueHolderIden: type=" + type + '\n');
    for (ObjectIntCursor<String> oic : fieldMap) {
      sb.append("  " + oic.key + ": i=" + oic.value + ", type=" + types[oic.value] +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ValueHolderIden.java`
#### Snippet
```java
    sb.append("ValueHolderIden: type=" + type + '\n');
    for (ObjectIntCursor<String> oic : fieldMap) {
      sb.append("  " + oic.key + ": i=" + oic.value + ", type=" + types[oic.value] +
          ", offset=" + offsets[oic.value] + '\n');
    }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/ScalarReplacementNode.java`
#### Snippet
```java
    if (logger.isTraceEnabled()) {
      final StringBuilder sb = new StringBuilder();
      sb.append("ReplacingBasicValues for " + className + "\n");
      for(final ReplacingBasicValue value : valueList) {
        value.dump(sb, 2);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/InstructionModifier.java`
#### Snippet
```java
        if (!seenIdens.containsKey(iden)) {
          seenIdens.put(iden, ++idenId);
          sb.append("ValueHolderIden[" + idenId + "]:\n");
          iden.dump(sb);
        }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/InstructionModifier.java`
#### Snippet
```java
        }

        sb.append("  " + ioc.key + " => " + ioc.value + '[' + seenIdens.get(iden) + "]\n");
        ++itemCount;
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/bytecode/InstructionModifier.java`
#### Snippet
```java
      sb.append(" .oldLocalToFirst:\n");
      for (final IntIntCursor iic : oldLocalToFirst) {
        sb.append("  " + iic.key + " => " + iic.value + '\n');
        ++itemCount;
      }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/types/RelDataTypeDrillImpl.java`
#### Snippet
```java
    @Override
    protected void generateTypeString(StringBuilder sb, boolean withDetail) {
       sb.append("(DrillRecordRow" + getFieldNames() + ")");
    }

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
              sb.append(classes[i].getCanonicalName().toString());
            } else {
              sb.append(classes[i].getCanonicalName().toString() + "->");
            }
          }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
              sb.append(matchingRels.get(i).getClass().getCanonicalName().toString());
            } else {
              sb.append(matchingRels.get(i).getClass().getCanonicalName().toString() + "->");
            }
          }
```

## RuleId[id=RedundantCompareToJavaTime]
### RedundantCompareToJavaTime
Expression with 'java.time' `compareTo()` call can be simplified
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        LocalDate computedTime = reader.readLocalDate();
        if (dirComputedTime == null
            || computedTime.compareTo(dirComputedTime) > 0) {
          dirComputedTime = computedTime;
        }
```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Array `FUNCTIONS` is always empty
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionFunction.java`
#### Snippet
```java
  static {
    ImmutableMap.Builder<String, Constructor<LogicalExpression>> builder = ImmutableMap.builder();
    for (Class<?> c : FUNCTIONS) {

      // logger.debug("Adding {} and function", c);
```

## RuleId[id=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpBatchReader.java`
#### Snippet
```java

      Optional<UsernamePasswordWithProxyCredentials> credentials = config.getUsernamePasswordCredentials();
      if (credentials.isPresent()) {
        builder.username(credentials.get().getUsername()).password(credentials.get().getPassword());
      }
```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
      Optional<UsernamePasswordWithProxyCredentials> credentials = config.getUsernamePasswordCredentials();

      if (credentials.isPresent()) {
        builder.username(credentials.get().getUsername())
          .password(credentials.get().getPassword());
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidStoragePlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules(
    OptimizerRulesContext optimizerRulesContext,
    PlannerPhase phase
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules(
    OptimizerRulesContext optimizerRulesContext,
    PlannerPhase phase
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.TreeSet' to 'java.util.Set'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/TableStatsCalculator.java`
#### Snippet
```java
      // get regions for table
      List<HRegionLocation> tableRegionInfos = locator.getAllRegionLocations();
      Set<byte[]> tableRegions = new TreeSet<>(Bytes.BYTES_COMPARATOR);
      for (HRegionLocation regionInfo : tableRegionInfos) {
        tableRegions.add(regionInfo.getRegionInfo().getRegionName());
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.TreeMap' to 'java.util.Map'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/TableStatsCalculator.java`
#### Snippet
```java
      }

      sizeMap = new TreeMap<>(Bytes.BYTES_COMPARATOR);

      Collection<ServerName> servers = clusterStatus.getServers();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/udfs/HttpHelperFunctions.java`
#### Snippet
```java
        return;
      }
      String finalUrl = org.apache.drill.exec.store.http.util.SimpleHttp.mapPositionalParameters(url, args);
      // Make the API call
      java.io.InputStream results = org.apache.drill.exec.store.http.util.SimpleHttp.getRequestAndStreamResponse(finalUrl);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/udfs/HttpHelperFunctions.java`
#### Snippet
```java
        return;
      }
      java.io.InputStream results = org.apache.drill.exec.store.http.util.SimpleHttp.apiCall(plugin, endpointConfig, drillbitContext, args)
        .getInputStream();
      // If the result string is null or empty, return an empty map
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
        //Split into key/value
        String[] parts = line.split("=");
        jsonObject.put(parts[0], parts[1]);
      }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/util/SimpleHttp.java`
#### Snippet
```java
        if (filters != null) {
          for (Map.Entry<String, String> filter : filters.entrySet()) {
            json.put(filter.getKey(), filter.getValue());
          }
        }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/DeltaGroupScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), null, null, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/DeltaGroupScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), null, null, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveDrillNativeParquetScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), null, null, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveDrillNativeParquetScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), null, null, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class\>'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
      return (Class<? extends InputFormat<?, ?>>) storageHandler.getInputFormatClass();
    } else {
      return (Class<? extends InputFormat<?, ?>>) Class.forName(inputFormatName);
    }
  }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class\>'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
      final String inputFormatName = sd.getInputFormat();
      if (!Strings.isNullOrEmpty(inputFormatName)) {
        return (Class<? extends InputFormat<?, ?>>) Class.forName(inputFormatName);
      }

```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules(OptimizerRulesContext optimizerContext, PlannerPhase phase) {
    switch (phase) {
      case PARTITION_PRUNING:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.drill.common.graph.AdjacencyList.Node'
in `logical/src/main/java/org/apache/drill/common/graph/AdjacencyList.java`
#### Snippet
```java
        return false;
      }
      return Objects.equals(nodeValue, ((Node) that).getNodeValue());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.drill.common.logical.data.AbstractSingleBuilder' to 'X'
in `logical/src/main/java/org/apache/drill/common/logical/data/AbstractSingleBuilder.java`
#### Snippet
```java
  public X setInput(LogicalOperator input){
    this.input = input;
    return (X) this;
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java

      java.util.Map<String, org.apache.drill.exec.record.MaterializedField> columns =
          (java.util.Map<String, org.apache.drill.exec.record.MaterializedField>) columnsHolder.obj;

      if (columns == null) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SchemaFunctions.java`
#### Snippet
```java
      }

      columns = (java.util.Map<String, org.apache.drill.exec.record.MaterializedField>) columnsHolder.obj;

      for (int i = 0; i < inputs.length; i += 2) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
        com.clearspring.analytics.stream.cardinality.HyperLogLog hll =
            (com.clearspring.analytics.stream.cardinality.HyperLogLog) work.obj;
        java.util.ArrayList<Integer> intervalList = (java.util.ArrayList<Integer>)interval.obj;
        intervalList.clear();
        intervalList.add(in.days);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StatisticsAggrFunctions.java`
#### Snippet
```java
        if (in.isSet == 1) {
          if (interval.obj != null) {
            java.util.ArrayList<Integer> intervalList = (java.util.ArrayList<Integer>)interval.obj;
            intervalList.clear();
            intervalList.add(in.days);
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.apache.drill.exec.coord.store.TransientStore' to 'org.apache.drill.exec.coord.store.TransientStore'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/store/CachingTransientStoreFactory.java`
#### Snippet
```java
  @Override
  public <V> TransientStore<V> getOrCreateStore(final TransientStoreConfig<V> config) {
    final TransientStore<V> store = cache.get(Preconditions.checkNotNull(config, "config is required"));
    if (store != null) {
      return store;
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
    } else {
      // Split the path by the slash
      List<String> pathParts = new ArrayList<>(Arrays.asList(new_dir.toString().split("/")));
      for (String pathPart : pathParts) {
        BoxSearch search = new BoxSearch(client);
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/BoxFileSystem.java`
#### Snippet
```java
    } else {
      // Split the path by the slash
      List<String> pathParts = new ArrayList<>(Arrays.asList(new_dir.toString().split("/")));
      for (String pathPart : pathParts) {
        BoxSearch search = new BoxSearch(client);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
    List<Path> dirs = selection.hadWildcard() ? selection.getFileStatuses().stream()
        .map(FileStatus::getPath)
        .collect(Collectors.toList()) : new ArrayList<>(dirPaths);

    Path rootPath = handleWildCard(root);
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FileSelection.java`
#### Snippet
```java
    List<Path> dirs = selection.hadWildcard() ? selection.getFileStatuses().stream()
        .map(FileStatus::getPath)
        .collect(Collectors.toList()) : new ArrayList<>(dirPaths);

    Path rootPath = handleWildCard(root);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyGroupScan.java`
#### Snippet
```java
      if (MapUtils.isNotEmpty(filesMetadata)) {
        newScan.fileSet = filesMetadata.keySet();
        newScan.selection = FileSelection.create(null, new ArrayList<>(newScan.fileSet), newScan.selectionRoot);
      }
      try {
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/easy/EasyGroupScan.java`
#### Snippet
```java
      if (MapUtils.isNotEmpty(filesMetadata)) {
        newScan.fileSet = filesMetadata.keySet();
        newScan.selection = FileSelection.create(null, new ArrayList<>(newScan.fileSet), newScan.selectionRoot);
      }
      try {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/WorkspaceSchemaFactory.java`
#### Snippet
```java
    public List<Function> getFunctions(String name) {
      // add parent functions first
      List<Function> functions = new ArrayList<>(super.getFunctions(name));

      List<TableParamDef> tableParameters = getFunctionParameters();
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/WorkspaceSchemaFactory.java`
#### Snippet
```java
    public List<Function> getFunctions(String name) {
      // add parent functions first
      List<Function> functions = new ArrayList<>(super.getFunctions(name));

      List<TableParamDef> tableParameters = getFunctionParameters();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map' to 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/http/oauth/OAuthUtils.java`
#### Snippet
```java

      logger.debug("Response: {}", responseBody);
      Map<String, Object> parsedJson = MAPPER.readValue(responseBody, Map.class);

      if (parsedJson.containsKey("access_token")) {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/table/function/WithOptionsTableMacro.java`
#### Snippet
```java
  @Override
  public TranslatableTable apply(List<?> arguments) {
    DrillTable drillTable = function.apply((List<Object>) arguments);
    if (drillTable == null) {
      throw UserException
```

### UNCHECKED_WARNING
Unchecked call to 'DynamicPojoRecordReader(LinkedHashMap\>, List\>, int)' as a member of raw type 'org.apache.drill.exec.store.pojo.DynamicPojoRecordReader'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/pojo/DynamicPojoRecordReader.java`
#### Snippet
```java

      int maxRecordsToRead = value.get("recordsPerBatch").asInt();
      return new DynamicPojoRecordReader(schema, records, maxRecordsToRead);
    }
  }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/InfoSchemaStoragePlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules(
    OptimizerRulesContext optimizerRulesContext,
    PlannerPhase phase
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetGroupScanStatistics.java`
#### Snippet
```java
    this.partitionValueMap = HashBasedTable.create(that.partitionValueMap);
    this.partitionColTypeMap = new HashMap<>(that.partitionColTypeMap);
    this.columnValueCounts = new HashMap<>(that.columnValueCounts);
    this.rowCount = that.rowCount;
  }
```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetGroupScanStatistics.java`
#### Snippet
```java
    this.partitionValueMap = HashBasedTable.create(that.partitionValueMap);
    this.partitionColTypeMap = new HashMap<>(that.partitionColTypeMap);
    this.columnValueCounts = new HashMap<>(that.columnValueCounts);
    this.rowCount = that.rowCount;
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetGroupScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), getSelectionRoot(), cacheFileRoot, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetGroupScan.java`
#### Snippet
```java
  @Override
  protected AbstractParquetGroupScan cloneWithFileSelection(Collection<Path> filePaths) throws IOException {
    FileSelection newSelection = new FileSelection(null, new ArrayList<>(filePaths), getSelectionRoot(), cacheFileRoot, false);
    return clone(newSelection);
  }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ParquetFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java
            .collect(Collectors.toList());

        newScan.fileSet = new HashSet<>(newScan.getFilesMetadata().keySet());
      } else if (!newScan.getRowGroupsMetadata().isEmpty()) {
        newScan.entries = newScan.getRowGroupsMetadata().keySet().stream()
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java
            .collect(Collectors.toList());

        newScan.fileSet = new HashSet<>(newScan.getFilesMetadata().keySet());
      } else if (!newScan.getRowGroupsMetadata().isEmpty()) {
        newScan.entries = newScan.getRowGroupsMetadata().keySet().stream()
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java
            .collect(Collectors.toList());

        newScan.fileSet = new HashSet<>(newScan.getRowGroupsMetadata().keySet());
      }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java

    List<Path> files = selection.getFiles();
    fileSet = new HashSet<>(files);
    entries = new ArrayList<>(files.size());

```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java

    List<Path> files = selection.getFiles();
    fileSet = new HashSet<>(files);
    entries = new ArrayList<>(files.size());

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java
      AbstractParquetGroupScan abstractParquetGroupScan = (AbstractParquetGroupScan) source;

      Map<Path, FileMetadata> filesToFilter = new HashMap<>(prunedFiles);
      if (!abstractParquetGroupScan.getRowGroupsMetadata().isEmpty()) {
        prunedFiles.forEach((path, fileMetadata) -> {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/BaseParquetMetadataProvider.java`
#### Snippet
```java
  @Override
  public List<RowGroupMetadata> getRowGroupsMeta() {
    return new ArrayList<>(getRowGroupsMetadataMap().values());
  }

```

### UNCHECKED_WARNING
Unchecked call to 'HashMap(Map)' as a member of raw type 'java.util.HashMap'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java
      AbstractParquetGroupScan abstractParquetGroupScan = (AbstractParquetGroupScan) source;

      Map<Path, FileMetadata> filesToFilter = new HashMap<>(prunedFiles);
      if (!abstractParquetGroupScan.getRowGroupsMetadata().isEmpty()) {
        prunedFiles.forEach((path, fileMetadata) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V1.java`
#### Snippet
```java
    @JsonIgnore
    @Override public void assignFiles(List<? extends ParquetFileMetadata> newFiles) {
      this.files = (List<ParquetFileMetadata_v1>) newFiles;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/MetadataPathUtils.java`
#### Snippet
```java
        if (file instanceof ParquetFileMetadata_v4) {
          fileWithAbsolutePath = (relativePath.isAbsolute()) ? file
              : new ParquetFileMetadata_v4(new Path(baseDir, relativePath), file.getLength(), (List<Metadata_V4.RowGroupMetadata_v4>) file.getRowGroups());
        } else if (file instanceof ParquetFileMetadata_v3) {
          fileWithAbsolutePath = (relativePath.isAbsolute()) ? file
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/MetadataPathUtils.java`
#### Snippet
```java
        } else if (file instanceof ParquetFileMetadata_v3) {
          fileWithAbsolutePath = (relativePath.isAbsolute()) ? file
              : new ParquetFileMetadata_v3(new Path(baseDir, relativePath), file.getLength(), (List<Metadata_V3.RowGroupMetadata_v3>) file.getRowGroups());
        }
        filesWithAbsolutePaths.add(fileWithAbsolutePath);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/MetadataPathUtils.java`
#### Snippet
```java
    }
    List<ParquetFileMetadata_v4> filesWithRelativePaths = new ArrayList<>();
    for (ParquetFileMetadata_v4 file : (List<ParquetFileMetadata_v4>) tableMetadataWithAbsolutePaths.getFiles()) {
      filesWithRelativePaths.add(new ParquetFileMetadata_v4(
          relativize(baseDir, file.getPath()), file.length, file.rowGroups));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V4.java`
#### Snippet
```java
    @JsonIgnore
    public void assignFiles(List<? extends ParquetFileMetadata> newFiles) {
      this.files = (List<ParquetFileMetadata_v4>) newFiles;
    }
  }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V4.java`
#### Snippet
```java

      // update files paths to absolute ones
      this.fileMetadata.files = (List<ParquetFileMetadata_v4>) MetadataPathUtils.convertToFilesWithAbsolutePaths(fileMetadata.files, baseDir);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V2.java`
#### Snippet
```java
    @JsonIgnore
    @Override public void assignFiles(List<? extends ParquetFileMetadata> newFiles) {
      this.files = (List<ParquetFileMetadata_v2>) newFiles;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V3.java`
#### Snippet
```java

      // update files paths to absolute ones
      this.files = (List<ParquetFileMetadata_v3>) MetadataPathUtils.convertToFilesWithAbsolutePaths(files, baseDir);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata_V3.java`
#### Snippet
```java
    @JsonIgnore
    @Override public void assignFiles(List<? extends ParquetFileMetadata> newFiles) {
      this.files = (List<ParquetFileMetadata_v3>) newFiles;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List\>' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata.java`
#### Snippet
```java
        ParquetTableMetadata_v4 subTableMetadata = (createMetaFilesRecursively(file.getPath(), fs, allColumnsInteresting, columnSet)).getLeft();
        ConcurrentHashMap<ColumnTypeMetadata_v4.Key, ColumnTypeMetadata_v4> subTableColumnTypeInfo = subTableMetadata.getColumnTypeInfoMap();
        metaDataList.addAll((List<ParquetFileMetadata_v4>) subTableMetadata.getFiles());
        directoryList.addAll(subTableMetadata.getDirectories());
        directoryList.add(file.getPath());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/DataRecord.java`
#### Snippet
```java
        } else {
            if(isList) {
                ((List<Object>)dataMap.get(fieldId)).add(data);
            } else {
                throw new IllegalStateException("Overriding field id existing data!");
```

### UNCHECKED_WARNING
Unchecked assignment: 'io.netty.util.concurrent.DefaultPromise' to 'io.netty.util.concurrent.Promise'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRestServer.java`
#### Snippet
```java
      // The invocation of this close future is no-op as it will be triggered after query completion in unsecure case.
      // But we need this close future as it's expected by Foreman.
      final Promise<Void> closeFuture = new DefaultPromise(executor);

      final WebSessionResources webSessionResources = new WebSessionResources(sessionAllocator, remoteAddress,
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    // Use only minor fragments that have complete profiles
    // Complete iff the fragment profile has at least one operator profile, and start and end times.
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));
    final List<MinorFragmentProfile> incomplete = new ArrayList<>(
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));
    final List<MinorFragmentProfile> incomplete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.missingOperatorsOrTimes));

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    // Use only minor fragments that have complete profiles
    // Complete iff the fragment profile has at least one operator profile, and start and end times.
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
    // Use only minor fragments that have complete profiles
    // Complete iff the fragment profile has at least one operator profile, and start and end times.
    final List<MinorFragmentProfile> complete = new ArrayList<>(
        Collections2.filter(major.getMinorFragmentProfileList(), Filters.hasOperatorsAndTimes));

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List\>'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/ProfileWrapper.java`
#### Snippet
```java
    }

    final List<ImmutablePair<Integer, Integer>> keys = new ArrayList<>(opmap.keySet());
    Collections.sort(keys);

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/ProfileWrapper.java`
#### Snippet
```java
    }

    final List<ImmutablePair<Integer, Integer>> keys = new ArrayList<>(opmap.keySet());
    Collections.sort(keys);

```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeSchemaHandler.java`
#### Snippet
```java
      if (workspace != null) {
        Map workspaceMap = (Map) map.get(key);
        configMap.putAll(workspaceMap);
      } else if (SchemaFactory.DEFAULT_WS_NAME.equals(key)) {
        configMap.putAll(mapper.convertValue(WorkspaceConfig.DEFAULT, Map.class));
```

### UNCHECKED_WARNING
Unchecked call to 'putAll(Map)' as a member of raw type 'java.util.Map'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DescribeSchemaHandler.java`
#### Snippet
```java
        configMap.putAll(workspaceMap);
      } else if (SchemaFactory.DEFAULT_WS_NAME.equals(key)) {
        configMap.putAll(mapper.convertValue(WorkspaceConfig.DEFAULT, Map.class));
      }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateTableHandler.java`
#### Snippet
```java

      final List<RexNode> exprs = Lists.newArrayListWithExpectedSize(queryRowType.getFieldCount() + 1);
      final List<String> fieldNames = new ArrayList<>(queryRowType.getFieldNames());

      for (final RelDataTypeField field : queryRowType.getFieldList()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.Class' to 'java.lang.Class'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
        final Class vvClass = TypeHelper.getValueVectorClass(type.getMinorType(), type.getMode());
        try {
          output.addField(field, vvClass);
        } catch (SchemaChangeException e) {
          throw new ExecutionSetupException(e);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/MetastoreAnalyzeTableHandler.java`
#### Snippet
```java
        MetadataHandlerContext handlerContext = MetadataHandlerContext.builder()
            .tableInfo(tableInfo)
            .metadataToHandle(new ArrayList<>(segments.get(i - 1)))
            .metadataType(MetadataType.SEGMENT)
            .depthLevel(i)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdDistinctRowCount.java`
#### Snippet
```java
      List<RexNode> leftFilters = new ArrayList<>();
      List<RexNode> rightFilters = new ArrayList<>();
      List<RexNode> joinFilters = new ArrayList();
      List<RexNode> predList = RelOptUtil.conjunctions(
        RexUtil.expandSearch(joinRel.getCluster().getRexBuilder(), null, predicate));
```

### UNCHECKED_WARNING
Unchecked call to 'add(E)' as a member of raw type 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java
        if (isMultiColumnPredicate(orPred) && !combinedRangePredicates.contains(orPred)) {
          Set uniqueRefs = new HashSet<>();
          uniqueRefs.add(DrillRelOptUtil.findAllRexInputRefs(orPred));
          // If equality predicate involving single column - selectivity is 1.0
          if (uniqueRefs.size() == 1) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPhysicalPlanCallContext.java`
#### Snippet
```java
      return ((HashToRandomExchangePrel) exch).getFields();
    } else {
      return Collections.EMPTY_LIST;
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.reflect.Constructor' to 'java.lang.reflect.Constructor'. Reason: 'discoverClass' has raw type, so result of getConstructor is erased
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
  private static Constructor<? extends IndexDiscoverBase> getConstructor(Class discoverClass,RelNode scanRel) throws Exception {
    if (scanRel instanceof DrillScanRelBase) {
      return discoverClass.getConstructor(GroupScan.class, DrillScanRelBase.class);
    } else {
      return discoverClass.getConstructor(GroupScan.class, ScanPrel.class);
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
  private static Constructor<? extends IndexDiscoverBase> getConstructor(Class discoverClass,RelNode scanRel) throws Exception {
    if (scanRel instanceof DrillScanRelBase) {
      return discoverClass.getConstructor(GroupScan.class, DrillScanRelBase.class);
    } else {
      return discoverClass.getConstructor(GroupScan.class, ScanPrel.class);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.reflect.Constructor' to 'java.lang.reflect.Constructor'. Reason: 'discoverClass' has raw type, so result of getConstructor is erased
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
      return discoverClass.getConstructor(GroupScan.class, DrillScanRelBase.class);
    } else {
      return discoverClass.getConstructor(GroupScan.class, ScanPrel.class);
    }
  }
```

### UNCHECKED_WARNING
Unchecked call to 'getConstructor(Class...)' as a member of raw type 'java.lang.Class'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexDiscoverFactory.java`
#### Snippet
```java
      return discoverClass.getConstructor(GroupScan.class, DrillScanRelBase.class);
    } else {
      return discoverClass.getConstructor(GroupScan.class, ScanPrel.class);
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillFilterItemStarReWriterRule.java`
#### Snippet
```java
      // add new projects to the already existing in original project
      int projectIndex = scanRel.getRowType().getFieldCount();
      List<RexNode> newProjects = new ArrayList<>(projectRel.getProjects());
      for (DesiredField desiredField : itemStarFields.values()) {
        newProjects.add(new RexInputRef(projectIndex, desiredField.getType()));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/RewriteAsBinaryOperators.java`
#### Snippet
```java
    if (kind == SqlKind.OR || kind == SqlKind.AND) {
      if (call.getOperands().size() > 2) {
        List<RexNode> children = new ArrayList<>(call.getOperands());
        RexNode left = children.remove(0).accept(this);
        RexNode right = builder.makeCall(type, op, children).accept(this);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Iterator'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PrelUtil.java`
#### Snippet
```java

  public static Iterator<Prel> iter(List<RelNode> nodes) {
    return (Iterator<Prel>) (Object) nodes.iterator();
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Iterator'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PrelUtil.java`
#### Snippet
```java

  public static Iterator<Prel> iter(RelNode... nodes) {
    return (Iterator<Prel>) (Object) Arrays.asList(nodes).iterator();
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/WindowPrel.java`
#### Snippet
```java
    final RelDataType windowRowType = this.getRowType();

    final List<RelDataTypeField> fieldList = new ArrayList<>(inputRowType.getFieldList());
    final int inputFieldCount = inputRowType.getFieldCount();
    final int windowFieldCount = windowRowType.getFieldCount();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/DirectScanPrel.java`
#### Snippet
```java
  private static GroupScan getCopy(GroupScan scan){
    try {
      return (GroupScan) scan.getNewWithChildren((List<PhysicalOperator>) (Object) Collections.emptyList());
    } catch (ExecutionSetupException e) {
      throw new DrillRuntimeException("Unexpected failure while coping node.", e);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.List' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/View.java`
#### Snippet
```java
    // for backward compatibility since now all schemas and workspaces are case insensitive and stored in lower case
    // make sure that given workspace schema path is also in lower case
    this.workspaceSchemaPath = workspaceSchemaPath == null ? Collections.EMPTY_LIST :
        workspaceSchemaPath.stream()
            .map(String::toLowerCase)
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
      long lastModifiedTime = metadataProvider.getTableMetadata().getLastModifiedTime();

      Set<Path> removedFiles = new HashSet<>(metadataProvider.getFilesMetadataMap().keySet());
      Set<Path> newFiles = new HashSet<>();

```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
      long lastModifiedTime = metadataProvider.getTableMetadata().getLastModifiedTime();

      Set<Path> removedFiles = new HashSet<>(metadataProvider.getFilesMetadataMap().keySet());
      Set<Path> newFiles = new HashSet<>();

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
    this.usedMetastore = that.usedMetastore;
    this.nonInterestingColumnsMetadata = that.nonInterestingColumnsMetadata;
    this.fileSet = that.fileSet == null ? null : new HashSet<>(that.fileSet);
    this.limit = that.limit;
  }
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
    this.usedMetastore = that.usedMetastore;
    this.nonInterestingColumnsMetadata = that.nonInterestingColumnsMetadata;
    this.fileSet = that.fileSet == null ? null : new HashSet<>(that.fileSet);
    this.limit = that.limit;
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
  @Override
  public void modifyFileSelection(FileSelection selection) {
    fileSet = new HashSet<>(selection.getFiles());
  }

```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
  @Override
  public void modifyFileSelection(FileSelection selection) {
    fileSet = new HashSet<>(selection.getFiles());
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/MetastoreParquetTableMetadataProvider.java`
#### Snippet
```java
  @Override
  public List<Path> getLocations() {
    return new ArrayList<>(getFilesMetadataMap().keySet());
  }

```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/MetastoreParquetTableMetadataProvider.java`
#### Snippet
```java
  @Override
  public List<Path> getLocations() {
    return new ArrayList<>(getFilesMetadataMap().keySet());
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/MetastoreParquetTableMetadataProvider.java`
#### Snippet
```java
  @Override
  public List<RowGroupMetadata> getRowGroupsMeta() {
    return new ArrayList<>(getRowGroupsMetadataMap().values());
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.apache.drill.exec.metastore.store.MetastoreFileTableMetadataProvider.Builder' to 'T'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/MetastoreFileTableMetadataProvider.java`
#### Snippet
```java

    protected T self() {
      return (T) this;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/ParquetTableMetadataProviderImpl.java`
#### Snippet
```java
    }

    List<Path> fileNames = new ArrayList<>(fileSet);

    // when creating the file selection, set the selection root without the URI prefix
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/ParquetTableMetadataProviderImpl.java`
#### Snippet
```java
    }

    List<Path> fileNames = new ArrayList<>(fileSet);

    // when creating the file selection, set the selection root without the URI prefix
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.drill.exec.serialization.JacksonSerializer'
in `exec/java-exec/src/main/java/org/apache/drill/exec/serialization/JacksonSerializer.java`
#### Snippet
```java
  public boolean equals(Object obj) {
    if (obj instanceof JacksonSerializer && obj.getClass().equals(getClass())) {
      final JacksonSerializer<T> other = (JacksonSerializer<T>)obj;
      return Objects.equal(reader, other.reader) && Objects.equal(writer, other.writer);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'com.google.protobuf.Message' to 'T'
in `exec/java-exec/src/main/java/org/apache/drill/exec/serialization/ProtoSerializer.java`
#### Snippet
```java
    final B builder = readSchema.newMessage();
    JsonIOUtil.mergeFrom(raw, builder, readSchema, false);
    return (T)builder.build();
  }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.apache.drill.exec.serialization.ProtoSerializer'
in `exec/java-exec/src/main/java/org/apache/drill/exec/serialization/ProtoSerializer.java`
#### Snippet
```java
  public boolean equals(final Object obj) {
    if (obj instanceof ProtoSerializer && obj.getClass().equals(getClass())) {
      final ProtoSerializer<T, B> other = (ProtoSerializer<T, B>)obj;
      return Objects.equal(readSchema, other.readSchema) && Objects.equal(writeSchema, other.writeSchema);
    }
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/TableFormatPlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules() {
    return ImmutableSet.of();
  }
```

### UNCHECKED_WARNING
Unchecked call to 'isValid(T)' as a member of raw type 'org.apache.drill.exec.planner.cost.PluginCost.CheckValid'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBCost.java`
#### Snippet
```java
    try {
      configValue = config.getInt(configPath);
      if (!check.isValid(configValue)) { configValue = defaultValue; }
    } catch (Exception ex) {
      // Use defaults, if config values not present or any other issue
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.LinkedHashMap'
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/writers/HDF5MapDataWriter.java`
#### Snippet
```java
    super(reader, datapath);
    // Get the members of the dataset
    compoundData = (LinkedHashMap<String, ?>)reader.getDatasetByPath(datapath).getData();
    data = ((CompoundDataType) reader.getDatasetByPath(datapath).getDataType()).getMembers();
    dataWriters = new ArrayList<>();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.LinkedHashMap'
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5BatchReader.java`
#### Snippet
```java
      String fieldName = dataMember.getName();
      int[] dataLength = reader.getDatasetByPath(path).getDimensions();
      Object rawData = ((LinkedHashMap<String, ?>)reader.getDatasetByPath(path).getData()).get(fieldName);

      ArrayWriter innerWriter = listWriter.array(fieldName);
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/rules/PhoenixPrel.java`
#### Snippet
```java
  @Override
  public PhysicalOperator getPhysicalOperator(PhysicalPlanCreator creator) {
    List<SchemaPath> schemaPaths = new ArrayList<>(rowType.getFieldCount());
    List<String> columns = new ArrayList<>(rowType.getFieldCount());
    for (String col : rowType.getFieldNames()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/rules/PhoenixPrel.java`
#### Snippet
```java
  public PhysicalOperator getPhysicalOperator(PhysicalPlanCreator creator) {
    List<SchemaPath> schemaPaths = new ArrayList<>(rowType.getFieldCount());
    List<String> columns = new ArrayList<>(rowType.getFieldCount());
    for (String col : rowType.getFieldNames()) {
      schemaPaths.add(SchemaPath.getSimplePath(col));
```

### UNCHECKED_WARNING
Unchecked call to 'operationComplete(Future)' as a member of raw type 'org.apache.drill.exec.rpc.ConnectionMultiListener.SSLConnectionHandler'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/ConnectionMultiListener.java`
#### Snippet
```java
        if(future.isSuccess()) {
          Channel c = future.get();
          parent.sslConnectionHandler.operationComplete(future);
          parent.parent.setSslChannel(c);
        } else {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class

*** ** * ** ***

' to 'java.lang.Class'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/BasicClient.java`
#### Snippet
```java
  void send(RpcOutcomeListener<RECEIVE> listener, SEND protobufBody, boolean allowInEventLoop,
            ByteBuf... dataBodies) {
    super.send(listener, connection, handshakeType, protobufBody, (Class<RECEIVE>) responseClass,
      allowInEventLoop, dataBodies);
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
      MongoIterable<BsonDocument> projection;
      if (CollectionUtils.isNotEmpty(operations)) {
        List<Bson> operations = new ArrayList<>(this.operations);
        if (!fields.isEmpty()) {
          operations.add(Aggregates.project(fields));
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoRecordReader.java`
#### Snippet
```java
      MongoIterable<BsonDocument> projection;
      if (CollectionUtils.isNotEmpty(operations)) {
        List<Bson> operations = new ArrayList<>(this.operations);
        if (!fields.isEmpty()) {
          operations.add(Aggregates.project(fields));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java

      String input = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.getStringFromVarCharHolder(rawInput);
      java.util.Set<Character> chars = new java.util.HashSet();

      for (char ch : input.toCharArray()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java
      }

      java.util.Map<Character, Double> probabilities = new java.util.HashMap();
      int length = input.length();

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java

      String input = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(rawInput1.start, rawInput1.end, rawInput1.buffer);
      java.util.Set<Character> chars = new java.util.HashSet();

      for (char ch : input.toCharArray()) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashMap' to 'java.util.Map'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/ThreatHuntingFunctions.java`
#### Snippet
```java
      }

      java.util.Map<Character, Double> probabilities = new java.util.HashMap();
      int length = input.length();

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.ArrayList'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java
      out.isSet = 1;

      java.util.List<com.esri.core.geometry.Geometry> tmp = (java.util.ArrayList<com.esri.core.geometry.Geometry>) value.obj;

      com.esri.core.geometry.SpatialReference spatialRef = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.ArrayList'
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/gis/STUnionAggregate.java`
#### Snippet
```java
    }
    nonNullCount.value = 1;
    java.util.List<com.esri.core.geometry.Geometry> tmp = (java.util.ArrayList<com.esri.core.geometry.Geometry>) value.obj;

    com.esri.core.geometry.ogc.OGCGeometry geom;
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.util.Set', required 'java.util.Set'
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaStoragePlugin.java`
#### Snippet
```java

  @Override
  public Set<StoragePluginOptimizerRule> getOptimizerRules(
    OptimizerRulesContext optimizerRulesContext,
    PlannerPhase phase
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkBatchWriter.java`
#### Snippet
```java
      if (text != null && text.getLength() > 0) {
        byte[] bytes =text.copyBytes();
        splunkEvent.put(fieldName, new String(bytes));
      }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkBatchWriter.java`
#### Snippet
```java
    @Override
    public void writeField() throws IOException {
      splunkEvent.put(fieldName, reader.readObject());
    }
  }
```

### UNCHECKED_WARNING
Unchecked call to 'put(K, V)' as a member of raw type 'java.util.HashMap'
in `contrib/storage-splunk/src/main/java/org/apache/drill/exec/store/splunk/SplunkBatchWriter.java`
#### Snippet
```java
    @Override
    public void writeField() {
      splunkEvent.put(fieldName, String.valueOf(reader.readObject()));
    }
  }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.lang.reflect.Constructor\[\]' to 'java.lang.reflect.Constructor\[\]'. Reason: 'clazz' has raw type, so result of getConstructors is erased
in `common/src/main/java/org/apache/drill/common/util/DrillExceptionUtil.java`
#### Snippet
```java
      return new Exception(message, inner);
    }
    Constructor<Throwable>[] constructors = clazz.getConstructors();
    Class<?>[] defaultParameterTypes = new Class<?>[]{String.class, Throwable.class};
    Class<?>[] revertParameterTypes = new Class<?>[]{Throwable.class, String.class};
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
      @JsonProperty("annotatedClasses") Collection<AnnotatedClassDescriptor> annotatedClasses,
      @JsonProperty("implementations") Collection<ParentClassDescriptor> implementations) {
    this.scannedPackages = unmodifiableList(new ArrayList<>(checkNotNull(scannedPackages)));
    this.scannedClasses = unmodifiableSet(new HashSet<>(checkNotNull(scannedClasses)));
    this.scannedAnnotations = unmodifiableSet(new HashSet<>(checkNotNull(scannedAnnotations)));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
      @JsonProperty("implementations") Collection<ParentClassDescriptor> implementations) {
    this.scannedPackages = unmodifiableList(new ArrayList<>(checkNotNull(scannedPackages)));
    this.scannedClasses = unmodifiableSet(new HashSet<>(checkNotNull(scannedClasses)));
    this.scannedAnnotations = unmodifiableSet(new HashSet<>(checkNotNull(scannedAnnotations)));
    this.annotatedClasses = unmodifiableList(new ArrayList<>(checkNotNull(annotatedClasses)));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
    this.scannedPackages = unmodifiableList(new ArrayList<>(checkNotNull(scannedPackages)));
    this.scannedClasses = unmodifiableSet(new HashSet<>(checkNotNull(scannedClasses)));
    this.scannedAnnotations = unmodifiableSet(new HashSet<>(checkNotNull(scannedAnnotations)));
    this.annotatedClasses = unmodifiableList(new ArrayList<>(checkNotNull(annotatedClasses)));
    this.implementations = unmodifiableList(new ArrayList<>(checkNotNull(implementations)));
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
    this.scannedClasses = unmodifiableSet(new HashSet<>(checkNotNull(scannedClasses)));
    this.scannedAnnotations = unmodifiableSet(new HashSet<>(checkNotNull(scannedAnnotations)));
    this.annotatedClasses = unmodifiableList(new ArrayList<>(checkNotNull(annotatedClasses)));
    this.implementations = unmodifiableList(new ArrayList<>(checkNotNull(implementations)));
    this.parentClassByName = new HashMap<>();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
    this.scannedAnnotations = unmodifiableSet(new HashSet<>(checkNotNull(scannedAnnotations)));
    this.annotatedClasses = unmodifiableList(new ArrayList<>(checkNotNull(annotatedClasses)));
    this.implementations = unmodifiableList(new ArrayList<>(checkNotNull(implementations)));
    this.parentClassByName = new HashMap<>();
    for (ParentClassDescriptor parentClassDescriptor : implementations) {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `common/src/main/java/org/apache/drill/common/scanner/persistence/ScanResult.java`
#### Snippet
```java
              + scannedAnnotations);
    }
    return new ArrayList<>(annotationsByName.get(c));
  }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List'
in `common/src/main/java/org/apache/drill/common/scanner/ClassPathScanner.java`
#### Snippet
```java
        return Collections.emptyList();
      }
      List<AnnotationDescriptor> annotationDescriptors = new ArrayList<>(annotationsAttr.numAnnotations());
      for (javassist.bytecode.annotation.Annotation annotation : annotationsAttr.getAnnotations()) {
        // Sigh: javassist uses raw collections (is this 2002?)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java
        // swap the last element with the deleted one
        secondary.put(oldPair.getKey(), last);
        primary.put((K) key, new AbstractMap.SimpleImmutableEntry<>(oldPair.getKey(), last));
      }
      return oldPair==null ? null:oldPair.getValue();
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator' to 'java.util.Iterator'
in `contrib/format-xml/src/main/java/org/apache/drill/exec/store/xml/XMLReader.java`
#### Snippet
```java
          }

          Iterator<Attribute> attributes = startElement.getAttributes();
          if (attributes != null && attributes.hasNext()) {
            writeAttributes(attributePrefix, attributes);
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Dereference of `((SingleDictWriter) writer)` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/RepeatedDictReaderImpl.java`
#### Snippet
```java
      srcId = ((RepeatedDictWriter) writer).idx();
    } else {
      vector = ((SingleDictWriter) writer).container;
      srcId = ((SingleDictWriter) writer).idx();
    }
```

### DataFlowIssue
Method invocation `uInt2` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public UInt2Writer uInt2(String name) {
    return (map != null) ? map.uInt2(name) : list.uInt2();
  }

```

### DataFlowIssue
Method invocation `float4` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public Float4Writer float4(final String name) {
    return (map != null) ? map.float4(name) : list.float4();
  }

```

### DataFlowIssue
Method invocation `tinyInt` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public TinyIntWriter tinyInt(String name) {
    return (map != null) ? map.tinyInt(name) : list.tinyInt();
  }

```

### DataFlowIssue
Method invocation `decimal28Sparse` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal28SparseWriter decimal28Sparse(String name) {
    return (map != null) ? map.decimal28Sparse(name) : list.decimal28Sparse();
  }

```

### DataFlowIssue
Method invocation `date` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public DateWriter date(String name) {
    return (map != null) ? map.date(name) : list.date();
  }

```

### DataFlowIssue
Method invocation `integer` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public IntWriter integer(final String name) {
    return (map != null) ? map.integer(name) : list.integer();
  }

```

### DataFlowIssue
Method invocation `time` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public TimeWriter time(String name) {
    return (map != null) ? map.time(name) : list.time();
  }

```

### DataFlowIssue
Method invocation `uInt4` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public UInt4Writer uInt4(String name) {
    return (map != null) ? map.uInt4(name) : list.uInt4();
  }

```

### DataFlowIssue
Method invocation `bit` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public BitWriter bit(final String name) {
    return (map != null) ? map.bit(name) : list.bit();
  }

```

### DataFlowIssue
Method invocation `intervalDay` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public IntervalDayWriter intervalDay(String name) {
    return (map != null) ? map.intervalDay(name) : list.intervalDay();
  }

```

### DataFlowIssue
Method invocation `decimal38Sparse` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal38SparseWriter decimal38Sparse(String name) {
    return (map != null) ? map.decimal38Sparse(name) : list.decimal38Sparse();
  }

```

### DataFlowIssue
Method invocation `decimal28Dense` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal28DenseWriter decimal28Dense(String name) {
    return (map != null) ? map.decimal28Dense(name) : list.decimal28Dense();
  }

```

### DataFlowIssue
Method invocation `startList` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
      map.start();
    } else {
      list.startList();
    }
  }
```

### DataFlowIssue
Method invocation `float8` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public Float8Writer float8(final String name) {
    return (map != null) ? map.float8(name) : list.float8();
  }

```

### DataFlowIssue
Method invocation `dict` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public MapOrListWriter dict(String name) {
    return new MapOrListWriterImpl(map != null ? map.dict(name) : list.dict());
  }

```

### DataFlowIssue
Method invocation `uInt1` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public UInt1Writer uInt1(String name) {
    return (map != null) ? map.uInt1(name) : list.uInt1();
  }

```

### DataFlowIssue
Method invocation `bigInt` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public BigIntWriter bigInt(final String name) {
    return (map != null) ? map.bigInt(name) : list.bigInt();
  }

```

### DataFlowIssue
Method invocation `var16Char` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Var16CharWriter var16Char(String name) {
    return (map != null) ? map.var16Char(name) : list.var16Char();
  }

```

### DataFlowIssue
Method invocation `varBinary` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Deprecated
  public VarBinaryWriter binary(final String name) {
    return (map != null) ? map.varBinary(name) : list.varBinary();
  }

```

### DataFlowIssue
Method invocation `varDecimal` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public VarDecimalWriter varDecimal(String name, int precision, int scale) {
    return (map != null) ? map.varDecimal(name, precision, scale) : list.varDecimal(precision, scale);
  }

```

### DataFlowIssue
Method invocation `decimal38Dense` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal38DenseWriter decimal38Dense(String name) {
    return (map != null) ? map.decimal38Dense(name) : list.decimal38Dense();
  }

```

### DataFlowIssue
Method invocation `uInt8` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public UInt8Writer uInt8(String name) {
    return (map != null) ? map.uInt8(name) : list.uInt8();
  }

```

### DataFlowIssue
Method invocation `interval` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public IntervalWriter interval(String name) {
    return (map != null) ? map.interval(name) : list.interval();
  }

```

### DataFlowIssue
Method invocation `endList` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
      map.end();
    } else {
      list.endList();
    }
  }
```

### DataFlowIssue
Method invocation `smallInt` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public SmallIntWriter smallInt(String name) {
    return (map != null) ? map.smallInt(name) : list.smallInt();
  }

```

### DataFlowIssue
Method invocation `varDecimal` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public VarDecimalWriter varDecimal(String name) {
    return (map != null) ? map.varDecimal(name) : list.varDecimal();
  }

```

### DataFlowIssue
Method invocation `decimal18` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal18Writer decimal18(String name) {
    return (map != null) ? map.decimal18(name) : list.decimal18();
  }

```

### DataFlowIssue
Method invocation `varChar` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java

  public VarCharWriter varChar(final String name) {
    return (map != null) ? map.varChar(name) : list.varChar();
  }

```

### DataFlowIssue
Method invocation `timeStamp` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public TimeStampWriter timeStamp(String name) {
    return (map != null) ? map.timeStamp(name) : list.timeStamp();
  }

```

### DataFlowIssue
Method invocation `decimal9` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public Decimal9Writer decimal9(String name) {
    return (map != null) ? map.decimal9(name) : list.decimal9();
  }

```

### DataFlowIssue
Method invocation `intervalYear` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public IntervalYearWriter intervalYear(String name) {
    return (map != null) ? map.intervalYear(name) : list.intervalYear();
  }

```

### DataFlowIssue
Method invocation `varBinary` may produce `NullPointerException`
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/MapOrListWriterImpl.java`
#### Snippet
```java
  @Override
  public VarBinaryWriter varBinary(String name) {
    return (map != null) ? map.varBinary(name) : list.varBinary();
  }

```

### DataFlowIssue
Condition `v instanceof UnionVector` is redundant and can be replaced with a null check
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java

  private void init(ValueVector v) {
    if (v instanceof UnionVector) {
      state = State.UNION;
      unionVector = (UnionVector) v;
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/PromotableWriter.java`
#### Snippet
```java
      state = State.UNTYPED;
    } else {
      setWriter(v);
    }
  }
```

### DataFlowIssue
Method invocation `byteStream` may produce `NullPointerException`
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/rest/DruidAdminClient.java`
#### Snippet
```java
      }

      InputStream responseStream = response.body().byteStream();
      return mapper.readValue(responseStream, new TypeReference<List<SimpleDatasourceInfo>>(){});
    }
```

### DataFlowIssue
Method invocation `byteStream` may produce `NullPointerException`
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/rest/DruidQueryClient.java`
#### Snippet
```java
      }

      InputStream responseStream = response.body().byteStream();
      ArrayNode responses = mapper.readValue(responseStream, ArrayNode.class);
      return parseResponse(responses);
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseGroupScan.java`
#### Snippet
```java
      for (Entry<HRegionInfo, ServerName> regionEntry : regionsToAssignSet) {
        List<HBaseSubScanSpec> smallestList = minHeap.poll();
        smallestList.add(regionInfoToSubScanSpec(regionEntry.getKey()));
        if (smallestList.size() < maxPerEndpointSlot) {
          minHeap.offer(smallestList);
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseGroupScan.java`
#### Snippet
```java
      List<HBaseSubScanSpec> smallestList = minHeap.poll();
      List<HBaseSubScanSpec> largestList = maxHeap.poll();
      smallestList.add(largestList.remove(largestList.size()-1));
      if (largestList.size() > minPerEndpointSlot) {
        maxHeap.offer(largestList);
```

### DataFlowIssue
Argument `nextPage` might be null
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

    // Clean up any extraneous garbage from the header field.
    Matcher urlMatcher = URL_REGEX.matcher(nextPage);
    if (urlMatcher.find()) {
      return urlMatcher.group(1);
```

### DataFlowIssue
Method invocation `newBuilder` may produce `NullPointerException`
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpBatchReader.java`
#### Snippet
```java
    baseUrl = SimpleHttp.mapURLParameters(parsedURL, subScan.filters());

    HttpUrl.Builder urlBuilder = HttpUrl.parse(baseUrl).newBuilder();
    if (apiConfig.params() != null &&
      !apiConfig.params().isEmpty() &&
```

### DataFlowIssue
Method invocation `newBuilder` may produce `NullPointerException`
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenInterceptor.java`
#### Snippet
```java
    if (accessTokenRepository.getOAuthConfig().isAccessTokenInHeader()) {
      HttpUrl rawUrl = HttpUrl.parse(request.url().toString());
      rawUrl.newBuilder().addQueryParameter("access_token", accessToken);
      return request.newBuilder().url(rawUrl.url()).build();
    } else {
```

### DataFlowIssue
Method invocation `newBuilder` may produce `NullPointerException`
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/oauth/AccessTokenAuthenticator.java`
#### Snippet
```java
    if (accessTokenRepository.getOAuthConfig().isAccessTokenInHeader()) {
      HttpUrl rawUrl = HttpUrl.parse(request.url().toString());
      rawUrl.newBuilder().addQueryParameter("access_token", accessToken);
      return request.newBuilder().url(rawUrl.url()).build();
    } else {
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/ContainerRequestSpec.java`
#### Snippet
```java
    if (!racks.isEmpty()) {
      nodeArr = new String[racks.size()];
      racks.toArray(rackArr);
      relaxLocality = false;
    }
```

### DataFlowIssue
Method invocation `getAmUrl` may produce `NullPointerException`
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/ResizeCommand.java`
#### Snippet
```java
      }
    }
    if (sendResize(reporter.getAmUrl(), cmd, quantity)) {
      System.out.println("Use web UI or status command to check progress.");
    }
```

### DataFlowIssue
Method invocation `getNodeInventory` may produce `NullPointerException`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
      // in YARN's blacklist.

      context.controller.getNodeInventory().blacklist(hostName);
    }
  }
```

### DataFlowIssue
Method invocation `getNodeInventory` may produce `NullPointerException`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java

      if (context.task.container != null) {
        context.controller.getNodeInventory().release(context.task.container);
      }
      analyzeResult(context);
```

### DataFlowIssue
Method invocation `getNodeInventory` may produce `NullPointerException`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
      // just allocated.

      context.controller.getNodeInventory().reserve(context.task.container);
    }

```

### DataFlowIssue
Method invocation `getProperty` may produce `NullPointerException`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
    @Override
    public boolean isLive(EventContext context) {
      ZKRegistry reg = (ZKRegistry) context.controller.getProperty(ZKRegistry.CONTROLLER_PROPERTY);
      return reg.isRegistered(context.task);
    }
```

### DataFlowIssue
Argument `structType` might be null
in `contrib/format-deltalake/src/main/java/org/apache/drill/exec/store/delta/DeltaGroupScan.java`
#### Snippet
```java
      Snapshot snapshot = DeltaSnapshotFactory.INSTANCE.create(context).apply(log);
      StructType structType = snapshot.getMetadata().getSchema();
      schema = toSchema(structType);

      DeltaScan scan = Optional.ofNullable(condition)
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/writers/HiveValueWriterFactory.java`
#### Snippet
```java
      return fromList.apply((ListWriter) parentWriter);
    } else {
      throw new IllegalStateException(String.format("Parent writer with type [%s] is unsupported", parentWriter.getClass()));
    }
  }
```

### DataFlowIssue
Condition `args instanceof ImmutableList` is redundant and can be replaced with a null check
in `logical/src/main/java/org/apache/drill/common/expression/FunctionCall.java`
#### Snippet
```java
    }

    if (!(args instanceof ImmutableList)) {
      args = ImmutableList.copyOf(args);
    }
```

### DataFlowIssue
Variable is already assigned to this value
in `logical/src/main/java/org/apache/drill/common/expression/visitors/ExpressionValidator.java`
#### Snippet
```java
    // confirm that all outcomes are the same type.
    final MajorType mt = ifExpr.elseExpression.getMajorType();
    cond = ifExpr.ifCondition;
    MajorType innerT = cond.expression.getMajorType();
    if ((innerT.getMode() == DataMode.REPEATED && mt.getMode() != DataMode.REPEATED) || //
```

### DataFlowIssue
Method invocation `unwrap` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/AbstractClientConnection.java`
#### Snippet
```java
        public byte[] unwrap(byte[] data, int offset, int len) throws SaslException {
          checkState(saslClient != null);
          return saslClient.unwrap(data, offset, len);
        }
      };
```

### DataFlowIssue
Method invocation `wrap` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/AbstractClientConnection.java`
#### Snippet
```java
        public byte[] wrap(byte[] data, int offset, int len) throws SaslException {
          checkState(saslClient != null);
          return saslClient.wrap(data, offset, len);
        }

```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfigServer.java`
#### Snippet
```java
  private String getHadoopConfigParam(String name) {
    Preconditions.checkArgument(this.hadoopConfig != null);
    String value = hadoopConfig.get(name, "");
    value = value.trim();
    return value;
```

### DataFlowIssue
Unboxing of `minValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
      switch (srcType) {
        case INT :
          minHolder = ValueHolderHelper.getIntHolder((Integer) minValue);
          maxHolder = ValueHolderHelper.getIntHolder((Integer) maxValue);
          break;
```

### DataFlowIssue
Unboxing of `minValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
          break;
        case BIGINT:
          minHolder = ValueHolderHelper.getBigIntHolder((Long) minValue);
          maxHolder = ValueHolderHelper.getBigIntHolder((Long) maxValue);
          break;
```

### DataFlowIssue
Unboxing of `minValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
          break;
        case FLOAT4:
          minHolder = ValueHolderHelper.getFloat4Holder((Float) minValue);
          maxHolder = ValueHolderHelper.getFloat4Holder((Float) maxValue);
          break;
```

### DataFlowIssue
Unboxing of `minValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
          break;
        case FLOAT8:
          minHolder = ValueHolderHelper.getFloat8Holder((Double) minValue);
          maxHolder = ValueHolderHelper.getFloat8Holder((Double) maxValue);
          break;
```

### DataFlowIssue
Unboxing of `minValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
          break;
        case DATE:
          minHolder = ValueHolderHelper.getDateHolder((Long) minValue);
          maxHolder = ValueHolderHelper.getDateHolder((Long) maxValue);
          break;
```

### DataFlowIssue
Argument `minValue` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/StatisticsProvider.java`
#### Snippet
```java
          break;
        case VARCHAR:
          minHolder = ValueHolderHelper.getVarCharHolder(udfUtilities.getManagedBuffer(), (String) minValue);
          maxHolder = ValueHolderHelper.getVarCharHolder(udfUtilities.getManagedBuffer(), (String) maxValue);
          break;
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/GetSetVectorHelper.java`
#### Snippet
```java
    switch(type.getMode()){
      case OPTIONAL:
        setMethod = setMethod.arg(in.f("isSet"));
        // Fall through

```

### DataFlowIssue
Method invocation `getHolder` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillComplexWriterAggFuncHolder.java`
#### Snippet
```java
    addProtectedBlock(classGenerator, sub, output(), null, workspaceJVars, false);
    if (getReturnType().getMinorType() != TypeProtos.MinorType.LATE) {
      sub.assign(out.getHolder(), internalOutput);
    }
    //hash aggregate uses workspace vectors. Initialization is done in "setup" and does not require "reset" block.
```

### DataFlowIssue
Method invocation `getHolder` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillAggFuncHolder.java`
#### Snippet
```java
    addProtectedBlock(classGenerator, sub, output(), null, workspaceJVars, false);
    if (getReturnType().getMinorType() != TypeProtos.MinorType.LATE) {
      sub.assign(out.getHolder(), internalOutput);
    }
    //hash aggregate uses workspace vectors. Initialization is done in "setup" and does not require "reset" block.
```

### DataFlowIssue
Dereference of `ifConditions.poll()` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ExpressionTreeMaterializer.java`
#### Snippet
```java
        }

        LogicalExpression ifExpression = ifConditions.poll().expression;

        while (!ifConditions.isEmpty()) {
```

### DataFlowIssue
Dereference of `keyExprs` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/ValueVectorHashHelper.java`
#### Snippet
```java
    ClassGenerator.HoldingContainer fieldIdParamHolder = cg.addExpr(fieldIdParamExpr);
    int i = 0;
    for (LogicalExpression expr : keyExprs) {
      TypedFieldId targetTypeFieldId = toHashKeyFieldIds[i];
      ValueExpressions.IntExpression targetBuildFieldIdExp = new ValueExpressions.IntExpression(targetTypeFieldId.getFieldIds()[0], ExpressionPosition.UNKNOWN);
```

### DataFlowIssue
Argument `encodedPaths` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
    // Sorting ensures that the original order is restored before concatenating the
    // components back to the full encoded String.
    Arrays.sort(encodedPaths);

    List<String> decodedPathList = Lists.newArrayList();
```

### DataFlowIssue
Dereference of `schemaPaths` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/EncodedSchemaPathSet.java`
#### Snippet
```java
    NoCopyByteArrayOutputStream out = new NoCopyByteArrayOutputStream(ESTIMATED_ENCODED_SIZE);
    int bufOffset = 1; // 1st byte is NULL
    for (String schemaPath : schemaPaths) {
      out.write(0);
      out.write(encodeUTF(schemaPath));
```

### DataFlowIssue
Condition `holder instanceof BitHolder` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java

    private Trivalent isBitOn(ValueHolder holder) {
      Preconditions.checkArgument(holder instanceof BitHolder || holder instanceof NullableBitHolder,
          "Input does not have type of BitHolder or NullableBitHolder.");

```

### DataFlowIssue
Condition `holder instanceof BitHolder` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
          "Input does not have type of BitHolder or NullableBitHolder.");

      if ( (holder instanceof BitHolder && ((BitHolder) holder).value == 1)) {
        return Trivalent.TRUE;
      } else if (holder instanceof NullableBitHolder && ((NullableBitHolder) holder).isSet == 1 && ((NullableBitHolder) holder).value == 1) {
```

### DataFlowIssue
Condition `holder instanceof NullableBitHolder` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      if ( (holder instanceof BitHolder && ((BitHolder) holder).value == 1)) {
        return Trivalent.TRUE;
      } else if (holder instanceof NullableBitHolder && ((NullableBitHolder) holder).isSet == 1 && ((NullableBitHolder) holder).value == 1) {
        return Trivalent.TRUE;
      } else if (holder instanceof NullableBitHolder && ((NullableBitHolder) holder).isSet == 0) {
```

### DataFlowIssue
Condition `holder instanceof NullableBitHolder` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      } else if (holder instanceof NullableBitHolder && ((NullableBitHolder) holder).isSet == 1 && ((NullableBitHolder) holder).value == 1) {
        return Trivalent.TRUE;
      } else if (holder instanceof NullableBitHolder && ((NullableBitHolder) holder).isSet == 0) {
        return Trivalent.NULL;
      } else {
```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
    public ValueHolder visitDecimal38Constant(final ValueExpressions.Decimal38Expression decExpr,Integer value) throws RuntimeException {
      return getConstantValueHolder(decExpr.getBigDecimal().toString(), decExpr.getMajorType().getMinorType(), new Function<DrillBuf, ValueHolder>() {
        @Nullable
        @Override
        public ValueHolder apply(DrillBuf buffer) {
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
    Field outField = null;
    try {
      Field[] fields = interpreter.getClass().getDeclaredFields();
      for (Field f : fields) {
        // if this is annotated as a parameter to the function
```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
    public ValueHolder visitDecimal28Constant(final ValueExpressions.Decimal28Expression decExpr,Integer value) throws RuntimeException {
      return getConstantValueHolder(decExpr.getBigDecimal().toString(), decExpr.getMajorType().getMinorType(), new Function<DrillBuf, ValueHolder>() {
        @Nullable
        @Override
        public ValueHolder apply(DrillBuf buffer) {
```

### DataFlowIssue
Condition `condHolder instanceof BitHolder` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      ValueHolder condHolder = ifExpr.ifCondition.condition.accept(this, inIndex);

      Preconditions.checkArgument (condHolder instanceof BitHolder || condHolder instanceof NullableBitHolder,
          "IfExpression's condition does not have type of BitHolder or NullableBitHolder.");

```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
    public ValueHolder visitQuotedStringConstant(final ValueExpressions.QuotedString e, Integer value) throws RuntimeException {
      return getConstantValueHolder(e.value, e.getMajorType().getMinorType(), new Function<DrillBuf, ValueHolder>() {
        @Nullable
        @Override
        public ValueHolder apply(DrillBuf buffer) {
```

### DataFlowIssue
Condition `in instanceof ByteBufferReadable` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/util/filereader/DirectBufInputStream.java`
#### Snippet
```java
  protected void checkStreamSupportsByteBuffer() throws UnsupportedOperationException {
    // Check input stream supports ByteBuffer
    if (!(in instanceof ByteBufferReadable)) {
      throw new UnsupportedOperationException("The input stream is not ByteBuffer readable.");
    }
```

### DataFlowIssue
Method invocation `getBytes` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/StringFunctions.java`
#### Snippet
```java
          (String) com.google.common.collect.Iterables.get(splitter.split(inputString), arrayIndex, "");
      }
      byte[] strBytes = result.getBytes(com.google.common.base.Charsets.UTF_8);

      out.buffer = buffer = buffer.reallocIfNeeded(strBytes.length);
```

### DataFlowIssue
Dereference of `asyncAggregateWorker` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
      }
    }
    while (!asyncAggregateWorker.over.get()) {
      try {
        Thread.sleep(100);
```

### DataFlowIssue
Method invocation `isOutOfMemory` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
    @Override
    public boolean checkForOutOfMemory() {
      return buffer.peek().isOutOfMemory();
    }

```

### DataFlowIssue
Method invocation `getBytes` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
      }
      if (bodyLength > 0) {
        buf.getBytes(0, stream, bodyLength);
      }
      stream.hsync();
```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZkEphemeralStore.java`
#### Snippet
```java
  public Iterator<Map.Entry<String, V>> entries() {
    return Iterators.transform(getClient().entries(), new Function<Map.Entry<String, byte[]>, Map.Entry<String, V>>() {
      @Nullable
      @Override
      public Map.Entry<String, V> apply(@Nullable Map.Entry<String, byte[]> input) {
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZkEphemeralStore.java`
#### Snippet
```java
      public Map.Entry<String, V> apply(@Nullable Map.Entry<String, byte[]> input) {
        try {
          final V value = config.getSerializer().deserialize(input.getValue());
          return new ImmutableEntry<>(input.getKey(), value);
        } catch (final IOException e) {
```

### DataFlowIssue
Method invocation `getKey` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/store/BaseTransientStore.java`
#### Snippet
```java
      @Override
      public String apply(@Nullable Map.Entry<String, V> input) {
        return input.getKey();
      }
    });
```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZookeeperClient.java`
#### Snippet
```java
    final String prefix = PathUtils.join(root, "/");
    return Iterables.transform(getCache().getCurrentData(), new Function<ChildData, Map.Entry<String, byte[]>>() {
      @Nullable
      @Override
      public Map.Entry<String, byte[]> apply(final ChildData data) {
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
    Preconditions.checkArgument(oldPlugin != null);
    Preconditions.checkArgument(newPlugin != null);
    Preconditions.checkArgument(oldPlugin.name().equalsIgnoreCase(newPlugin.name()));
    Preconditions.checkArgument(oldPlugin != newPlugin);
    if (oldPlugin.isIntrinsic()) {
```

### DataFlowIssue
Method invocation `name` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
    Preconditions.checkArgument(oldPlugin != null);
    Preconditions.checkArgument(newPlugin != null);
    Preconditions.checkArgument(oldPlugin.name().equalsIgnoreCase(newPlugin.name()));
    Preconditions.checkArgument(oldPlugin != newPlugin);
    if (oldPlugin.isIntrinsic()) {
```

### DataFlowIssue
@Nullable method 'apply' always returns a non-null value
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/store/ZookeeperPersistentStore.java`
#### Snippet
```java
    Iterators.advance(entries, skip);
    return Iterators.transform(Iterators.limit(entries, take), new Function<Map.Entry<String, byte[]>, Map.Entry<String, V>>() {
      @Nullable
      @Override
      public Map.Entry<String, V> apply(@Nullable Map.Entry<String, byte[]> input) {
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/store/ZookeeperPersistentStore.java`
#### Snippet
```java
      public Map.Entry<String, V> apply(@Nullable Map.Entry<String, byte[]> input) {
        try {
          final V value = config.getSerializer().deserialize(input.getValue());
          return new ImmutableEntry<>(input.getKey(), value);
        } catch (final IOException e) {
```

### DataFlowIssue
Method invocation `setEmptyDirectoryStatus` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/WorkspaceSchemaFactory.java`
#### Snippet
```java
      if (newSelection == null) {
        // empty directory / selection means that this is the empty and schemaless table
        fileSelection.setEmptyDirectoryStatus();
        return fileSelection;
      }
```

### DataFlowIssue
Method invocation `bytes` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/avro/AvroColumnConverterFactory.java`
#### Snippet
```java
          } else {
            GenericFixed genericFixed = (GenericFixed) value;
            bytes = genericFixed.bytes();
          }
          BigInteger bigInteger = bytes.length == 0 ? BigInteger.ZERO : new BigInteger(bytes);
```

### DataFlowIssue
Switch label `Statistic.HLL` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
      if (!skipNullFields || this.reader.isSet()) {
        switch (fieldName) {
          case Statistic.HLL:
          case Statistic.HLL_MERGE:
          case Statistic.TDIGEST_MERGE:
```

### DataFlowIssue
Switch label `Statistic.HLL_MERGE` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        switch (fieldName) {
          case Statistic.HLL:
          case Statistic.HLL_MERGE:
          case Statistic.TDIGEST_MERGE:
            nextField = fieldName;
```

### DataFlowIssue
Switch label `Statistic.TDIGEST_MERGE` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
          case Statistic.HLL:
          case Statistic.HLL_MERGE:
          case Statistic.TDIGEST_MERGE:
            nextField = fieldName;
            break;
```

### DataFlowIssue
Switch label `Statistic.SCHEMA` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
    public void startField() throws IOException {
      switch (fieldName) {
        case Statistic.SCHEMA:
        case Statistic.ROWCOUNT:
        case Statistic.NNROWCOUNT:
```

### DataFlowIssue
Switch label `Statistic.ROWCOUNT` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
      switch (fieldName) {
        case Statistic.SCHEMA:
        case Statistic.ROWCOUNT:
        case Statistic.NNROWCOUNT:
        case Statistic.NDV:
```

### DataFlowIssue
Switch label `Statistic.NNROWCOUNT` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        case Statistic.SCHEMA:
        case Statistic.ROWCOUNT:
        case Statistic.NNROWCOUNT:
        case Statistic.NDV:
        case Statistic.AVG_WIDTH:
```

### DataFlowIssue
Switch label `Statistic.NDV` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        case Statistic.ROWCOUNT:
        case Statistic.NNROWCOUNT:
        case Statistic.NDV:
        case Statistic.AVG_WIDTH:
        case Statistic.SUM_DUPS:
```

### DataFlowIssue
Switch label `Statistic.AVG_WIDTH` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        case Statistic.NNROWCOUNT:
        case Statistic.NDV:
        case Statistic.AVG_WIDTH:
        case Statistic.SUM_DUPS:
          nextField = fieldName;
```

### DataFlowIssue
Switch label `Statistic.SUM_DUPS` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        case Statistic.NDV:
        case Statistic.AVG_WIDTH:
        case Statistic.SUM_DUPS:
          nextField = fieldName;
      }
```

### DataFlowIssue
Switch label `Statistic.COLNAME` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
    public void startField() throws IOException {
      switch (fieldName) {
        case Statistic.COLNAME:
        case Statistic.COLTYPE:
          nextField = fieldName;
```

### DataFlowIssue
Switch label `Statistic.COLTYPE` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
      switch (fieldName) {
        case Statistic.COLNAME:
        case Statistic.COLTYPE:
          nextField = fieldName;
      }
```

### DataFlowIssue
Switch label `Statistic.ROWCOUNT` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
      if (!skipNullFields || this.reader.isSet()) {
        switch (fieldName) {
          case Statistic.ROWCOUNT:
          case Statistic.NNROWCOUNT:
          case Statistic.NDV:
```

### DataFlowIssue
Switch label `Statistic.NNROWCOUNT` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
        switch (fieldName) {
          case Statistic.ROWCOUNT:
          case Statistic.NNROWCOUNT:
          case Statistic.NDV:
          case Statistic.SUM_DUPS:
```

### DataFlowIssue
Switch label `Statistic.NDV` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
          case Statistic.ROWCOUNT:
          case Statistic.NNROWCOUNT:
          case Statistic.NDV:
          case Statistic.SUM_DUPS:
            nextField = fieldName;
```

### DataFlowIssue
Switch label `Statistic.SUM_DUPS` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/StatisticsCollectorImpl.java`
#### Snippet
```java
          case Statistic.NNROWCOUNT:
          case Statistic.NDV:
          case Statistic.SUM_DUPS:
            nextField = fieldName;
            break;
```

### DataFlowIssue
Argument `elementSchema` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/loader/ProvidedFieldFactory.java`
#### Snippet
```java
    int dims = 1; // For inner array
    ColumnMetadata elementSchema = fieldDefn.providedColumn();
    while (MetadataUtils.isRepeatedList(elementSchema)) {
      dims++;
      elementSchema = elementSchema.childSchema();
```

### DataFlowIssue
Condition `input instanceof Seekable` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
    this.lineSeparator = settings.getNewLineDelimiter();
    byte normalizedLineSeparator = settings.getNormalizedNewLine();
    Preconditions.checkArgument(input instanceof Seekable, "Text input only supports an InputStream that supports Seekable.");
    boolean isCompressed = input instanceof CompressionInputStream;
    Preconditions.checkArgument(!isCompressed || startPos == 0, "Cannot use split on compressed stream.");
```

### DataFlowIssue
Condition `input instanceof CompressionInputStream` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
    byte normalizedLineSeparator = settings.getNormalizedNewLine();
    Preconditions.checkArgument(input instanceof Seekable, "Text input only supports an InputStream that supports Seekable.");
    boolean isCompressed = input instanceof CompressionInputStream;
    Preconditions.checkArgument(!isCompressed || startPos == 0, "Cannot use split on compressed stream.");

```

### DataFlowIssue
Condition `input instanceof FSDataInputStream` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/TextInput.java`
#### Snippet
```java
    this.settings = settings;

    if (input instanceof FSDataInputStream) {
      this.inputFS = (FSDataInputStream) input;
      this.bufferReadable = inputFS.getWrappedStream() instanceof ByteBufferReadable;
```

### DataFlowIssue
Method invocation `string` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/http/oauth/OAuthUtils.java`
#### Snippet
```java
    try {
      response = client.newCall(request).execute();
      String responseBody = response.body().string();

      if (!response.isSuccessful()) {
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/http/oauth/OAuthUtils.java`
#### Snippet
```java
        .build(logger);
    } finally {
      response.close();
    }
  }
```

### DataFlowIssue
Switch label `0` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReaderFactory.java`
#### Snippet
```java
    ConvertedType convertedType = schemaElement.getConverted_type();
    switch (descriptor.getMaxDefinitionLevel()) {
      case 0:
        if (convertedType == null) {
          return new VarLengthColumnReaders.VarBinaryColumn(parentReader, descriptor, columnChunkMetaData, fixedLength, (VarBinaryVector) v, schemaElement);
```

### DataFlowIssue
Condition `v instanceof NullableVarDecimalVector` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReaderFactory.java`
#### Snippet
```java
            return new VarLengthColumnReaders.NullableVarCharColumn(parentReader, descriptor, columnChunkMetaData, fixedLength, (NullableVarCharVector) v, schemaElement);
          case DECIMAL:
            if (v instanceof NullableVarDecimalVector) {
              return new VarLengthColumnReaders.NullableVarDecimalColumn(parentReader, descriptor, columnChunkMetaData, fixedLength, (NullableVarDecimalVector) v, schemaElement);
            }
```

### DataFlowIssue
Method invocation `getDirectories` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/metadata/Metadata.java`
#### Snippet
```java
          }
          ParquetTableMetadataDirs metadataDirs  = readMetadataDirs(fs, metadataDirFile, metaContext, readerConfig);
          if (metadata.tableModified(metadataDirs.getDirectories(), summaryFile, metadataParentDir, metaContext, fs) && true) {
            ParquetTableMetadata_v4 parquetTableMetadata = (metadata.createMetaFilesRecursivelyAsProcessUser(Path.getPathWithoutSchemeAndAuthority(summaryFile.getParent()), fs, true, null, true)).getLeft();
            return parquetTableMetadata.getSummary();
```

### DataFlowIssue
Method invocation `setReadFromPage` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/VarLenBulkPageReader.java`
#### Snippet
```java
    if (overflowDataAvailable()) {
      entry = overflowReader.getEntry(valuesToRead);
      entry.setReadFromPage(false); // entry was read from the overflow data

      return entry;
```

### DataFlowIssue
Method invocation `getRecordsReadInCurrentPass` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/BatchReader.java`
#### Snippet
```java

      Preconditions.checkNotNull(firstColumnStatus != null);
      readState.setValuesReadInCurrentPass(firstColumnStatus.getRecordsReadInCurrentPass()); // get the number of rows read

      readState.updateCounts((int) recordsToRead); // update the shared Reader State
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/AsyncPageReader.java`
#### Snippet
```java
    // Get the execution result but don't remove the Future object from the "asyncPageRead" queue yet;
    // this will ensure that cleanup will happen properly in case of an exception being thrown
    asyncPageRead.peek().get(); // get the result of execution
    // Alright now remove the Future object
    asyncPageRead.poll();
```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SchemalessBatch.java`
#### Snippet
```java
  @Override
  public Iterator<VectorWrapper<?>> iterator() {
    return null;
  }

```

### DataFlowIssue
Argument `planLocation` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
      String plan;
      if (queryString == null) {
        plan = Charsets.UTF_8.decode(ByteBuffer.wrap(Files.readAllBytes(Paths.get(planLocation)))).toString();
      } else {
        plan = queryString;
```

### DataFlowIssue
Dereference of `drillbits` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
      }
      if (local) {
        for (Drillbit b : drillbits) {
          b.close();
        }
```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/QuerySubmitter.java`
#### Snippet
```java
          b.close();
        }
        serviceSet.close();
      }
    }
```

### DataFlowIssue
Method invocation `getFileMetadata` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetScanBatchCreator.java`
#### Snippet
```java
            }

            MetadataBase.RowGroupMetadata rowGroupMetadata = fileMetadataV4.getFileMetadata().getRowGroups().get(rowGroup.getRowGroupIndex());

            Map<SchemaPath, ColumnStatistics<?>> columnsStatistics = ParquetTableMetadataUtils.getRowGroupColumnStatistics(tableMetadataV4, rowGroupMetadata);
```

### DataFlowIssue
Argument `tableMetadataV4` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetScanBatchCreator.java`
#### Snippet
```java
            try {
              Map<SchemaPath, TypeProtos.MajorType> intermediateColumns =
                  ParquetTableMetadataUtils.getIntermediateFields(tableMetadataV4, rowGroupMetadata);
              Map<SchemaPath, TypeProtos.MajorType> rowGroupFields =
                  ParquetTableMetadataUtils.getRowGroupFields(tableMetadataV4, rowGroupMetadata);
```

### DataFlowIssue
Casting `destination` to `SimpleVectorWrapper` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/SimpleVectorWrapper.java`
#### Snippet
```java
    Preconditions.checkArgument(destination instanceof SimpleVectorWrapper);
    Preconditions.checkArgument(getField().getType().equals(destination.getField().getType()));
    vector.makeTransferPair(((SimpleVectorWrapper<?>)destination).vector).transfer();
  }

```

### DataFlowIssue
Method invocation `getValueVector` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/VectorContainer.java`
#### Snippet
```java
    final ValueVector vector;
    if (id != null) {
      vector = getValueAccessorById(id.getFieldIds()).getValueVector();
      if (id.getFieldIds().length == 1 && !vector.getField().isEquivalent(field)) {
        final ValueVector newVector = TypeHelper.getNewVector(field, this.getAllocator(), callBack);
```

### DataFlowIssue
Casting `destination` to `HyperVectorWrapper` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/HyperVectorWrapper.java`
#### Snippet
```java
    Preconditions.checkArgument(destination instanceof HyperVectorWrapper);
    Preconditions.checkArgument(getField().getType().equals(destination.getField().getType()));
    Preconditions.checkArgument(vectors.length == ((HyperVectorWrapper<?>)destination).vectors.length);

    final ValueVector[] destionationVectors = ((HyperVectorWrapper<?>)destination).vectors;
```

### DataFlowIssue
Casting `destination` to `HyperVectorWrapper` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/HyperVectorWrapper.java`
#### Snippet
```java
    Preconditions.checkArgument(vectors.length == ((HyperVectorWrapper<?>)destination).vectors.length);

    final ValueVector[] destionationVectors = ((HyperVectorWrapper<?>)destination).vectors;
    for (int i = 0; i < vectors.length; ++i) {
      vectors[i].makeTransferPair(destionationVectors[i]).transfer();
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet2/DrillParquetGroupConverter.java`
#### Snippet
```java
      return fromList.apply((ListWriter) baseWriter);
    } else {
      throw new IllegalStateException(String.format("Parent writer with type [%s] is unsupported", baseWriter.getClass()));
    }
  }
```

### DataFlowIssue
Casting `v` to `VariableWidthVector` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchSizer.java`
#### Snippet
```java
          // Calculate pure data size.
          if (isVariableWidth) {
            totalDataSize = ((VariableWidthVector) v).getCurrentSizeInBytes();
          } else {
            totalDataSize = v.getPayloadByteCount(valueCount);
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/LogsResources.java`
#### Snippet
```java
  private File getFileByName(File folder, final String name) {
    File[] files = folder.listFiles((dir, fileName) -> fileName.equals(name));
    if (files.length == 0) {
      throw new DrillRuntimeException(name + " doesn't exist");
    }
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/LogsResources.java`
#### Snippet
```java
    File[] files = getLogFolder().listFiles(file_filter);

    for (File file : files) {
      logs.add(new Log(file.getName(), file.length(), file.lastModified()));
    }
```

### DataFlowIssue
Condition `v instanceof UnionVector` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
    if (v instanceof AbstractContainerVector || v instanceof ListVector || v instanceof RepeatedDictVector) {
      return getFieldIdIfMatches(v, builder, addToBreadCrumb, seg.getChild(), depth + 1);
    } else if (v instanceof UnionVector) {
      return getFieldIdIfMatchesUnion((UnionVector) v, builder, addToBreadCrumb, seg.getChild());
    } else {
```

### DataFlowIssue
Method invocation `getField` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
      if (seg.isNamed()) {
        if(addToBreadCrumb) {
          builder.intermediateType(v.getField().getType());
        }
        builder.finalType(finalType != null ? finalType : v.getField().getType());
```

### DataFlowIssue
Method invocation `getField` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
          builder.intermediateType(v.getField().getType());
        }
        builder.finalType(finalType != null ? finalType : v.getField().getType());
      } else {
        builder.finalType(v.getField().getType().toBuilder().setMode(DataMode.OPTIONAL).build());
```

### DataFlowIssue
Method invocation `getField` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
        builder.finalType(finalType != null ? finalType : v.getField().getType());
      } else {
        builder.finalType(v.getField().getType().toBuilder().setMode(DataMode.OPTIONAL).build());
      }

```

### DataFlowIssue
Method invocation `getField` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/FieldIdUtil.java`
#### Snippet
```java
          }
          builder.withIndex();
          builder.finalType(v.getField().getType().toBuilder().setMode(DataMode.OPTIONAL).build());
          return builder.build();
        } else {
```

### DataFlowIssue
Method invocation `trim` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
      String val, OptionScope scope, Kind kind) {
    Preconditions.checkArgument(val != null);
    val = val.trim();
    try {
      switch (kind) {
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java
    }

    throw new IllegalArgumentException(String.format("Unsupported type %s", val.getClass()));
  }

```

### DataFlowIssue
Condition `cause instanceof CompileException` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/JDKClassCompiler.java`
#### Snippet
```java
      if (cause != null) {
        cause = cause.getCause();
        if (cause instanceof CompileException) {
          throw (CompileException) cause;
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/FileSystemPartitionDescriptor.java`
#### Snippet
```java
      if (drillScan.getGroupScan().hasFiles()) {
        fileLocations = drillScan.getGroupScan().getFiles();
        isExpandedPartial = false;
      } else {
        FileSelection selection = ((FormatSelection) table.getSelection()).getSelection();
```

### DataFlowIssue
Method invocation `getName` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
        // Otherwise, Calcite will mix them up with binary operator subtract (-) or add (+)
        if (calciteOperator == SqlStdOperatorTable.UNARY_MINUS || calciteOperator == SqlStdOperatorTable.UNARY_PLUS) {
          drillOpName = calciteOperator.getName();
        } else {
          drillOpName = FunctionCallFactory.convertToDrillFunctionName(calciteOperator.getName());
```

### DataFlowIssue
Method invocation `getName` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/DrillOperatorTable.java`
#### Snippet
```java
          drillOpName = calciteOperator.getName();
        } else {
          drillOpName = FunctionCallFactory.convertToDrillFunctionName(calciteOperator.getName());
        }

```

### DataFlowIssue
Argument `schema` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/SchemaUtilities.java`
#### Snippet
```java
    }

    final AbstractSchema drillSchema = unwrapAsDrillSchemaInstance(schema);
    if (checkMutable && !drillSchema.isMutable()) {
      throw UserException.validationError()
```

### DataFlowIssue
Condition `ex instanceof ParseException` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/impl/DrillSqlParseException.java`
#### Snippet
```java
                                String[] tokenImages, Throwable ex) {
    super(message, pos, expectedTokenSequences, tokenImages, ex);
    this.parseException = (ex instanceof ParseException) ? (ParseException) ex : null;
    this.sql = sql;
  }
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/parser/UnsupportedOperatorsVisitor.java`
#### Snippet
```java
      // In a SELECT-SCOPE, only a partition can be defined
      if (definedWindow == null) {
        definedWindow = window;
      } else {
        if (!definedWindow.equalsDeep(window, Litmus.IGNORE)) {
```

### DataFlowIssue
Casting `drillSchema` to `WorkspaceSchemaFactory.WorkspaceSchema` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/SchemaHandler.java`
#### Snippet
```java

    Preconditions.checkState(drillSchema instanceof WorkspaceSchemaFactory.WorkspaceSchema);
    return (WorkspaceSchemaFactory.WorkspaceSchema) drillSchema;
  }

```

### DataFlowIssue
Argument `finalRel` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/CoveringIndexPlanGenerator.java`
#### Snippet
```java
    }

    finalRel = Prule.convert(finalRel, finalRel.getTraitSet().plus(Prel.DRILL_PHYSICAL));

    logger.debug("CoveringIndexPlanGenerator got finalRel {} from origScan {}, original digest {}, new digest {}.",
```

### DataFlowIssue
Condition `primaryTableGroupScan instanceof DbGroupScan` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexSelector.java`
#### Snippet
```java
        totLeadRowCount *= indexProps.getLeadingSelectivity();
        double avgRowSize = indexProps.getAvgRowSize();
        Preconditions.checkArgument(primaryTableGroupScan instanceof DbGroupScan);
        // IO Cost (Filter evaluation CPU cost for pushed down filters)
        double numBlocksIndex = Math.ceil((leadRowCount * avgRowSize) / costBase.getBlockSize(primaryTableGroupScan));
```

### DataFlowIssue
Argument `newRel` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/NonCoveringIndexPlanGenerator.java`
#### Snippet
```java
    }

    RelNode finalRel = Prule.convert(newRel, newRel.getTraitSet());
    logger.debug("NonCoveringIndexPlanGenerator got finalRel {} from origScan {}",
        finalRel.toString(), origScan.toString());
```

### DataFlowIssue
Method invocation `getRowType` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    }

    final RelDataTypeField rightRowKeyField = indexPlan.getRowType().getFieldList().get(0);
    final RelNode rangeDistRight = createRangeDistRight(indexPlan, rightRowKeyField,
        (DbGroupScan)IndexPlanUtils.getGroupScan(origScan));
```

### DataFlowIssue
Dereference of `leftRelAndScan` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/IndexIntersectPlanGenerator.java`
#### Snippet
```java
    Pair<RelNode, DbGroupScan> leftRelAndScan = buildRestrictedDBScan(remnant, isAnyIndexAsync);

    RelNode finalRel = buildRowKeyJoin(leftRelAndScan.left, rangeDistRight, true, JoinControl.DEFAULT);
    if (upperProject != null) {
      ProjectPrel cap = new ProjectPrel(finalRel.getCluster(), finalRel.getTraitSet(),
```

### DataFlowIssue
Dereference of `conditions` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/IndexPlanUtils.java`
#### Snippet
```java
                break;
              } else {
                for (RexNode r : conditions) {
                  if (!(r.getKind() == SqlKind.EQUALS)) {
                    eligibleForCollation = false;
```

### DataFlowIssue
Method invocation `getCluster` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/generators/AbstractIndexPlanGenerator.java`
#### Snippet
```java
                    child, ((TopNPrel)sortNode).getLimit(), indexContext.getCollation());
    }
    return new SortPrel(sortNode.getCluster(),
            newRel.getTraitSet().replace(Prel.DRILL_PHYSICAL).plus(indexContext.getCollation()),
            child, indexContext.getCollation());
```

### DataFlowIssue
Method invocation `onMatch` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/rules/DbScanToIndexScanPrule.java`
#### Snippet
```java
  public void onMatch(RelOptRuleCall call) {
    if (getMatchIfRoot(call) != null) {
      getMatchIfRoot(call).onMatch(call);
      return;
    }
```

### DataFlowIssue
Method invocation `getCluster` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/rules/DbScanToIndexScanPrule.java`
#### Snippet
```java
    logger.debug("Index Rule {} starts", this.description);

    RexBuilder builder = indexContext.filter.getCluster().getRexBuilder();

    RexNode condition = null;
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
      // rows in first bucket are included
      if (result <= 0) {
        startBucket = firstStartPointIndex;
        startBucketFraction = 1.0;
      } else {
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
      if (result <= 0) {
        startBucket = firstStartPointIndex;
        startBucketFraction = 1.0;
      } else {
        startBucket = getContainingBucket(lowValue, lastEndPointIndex, true);
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
       if (buckets[startBucket + 1].doubleValue() == buckets[startBucket].doubleValue()) {
         // if start and end points of the bucket are the same, consider entire bucket
         startBucketFraction = 1.0;
       } else if (range.lowerBoundType() == BoundType.CLOSED && buckets[startBucket + 1].doubleValue() == lowValue.doubleValue()) {
         // predicate is of type >= 5.0 and 5.0 happens to be the start point of the bucket
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
      if (result >= 0) {
        endBucket = lastEndPointIndex - 1;
        endBucketFraction = 1.0;
      } else {
        endBucket = getContainingBucket(highValue, lastEndPointIndex, false);
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/NumericEquiDepthHistogram.java`
#### Snippet
```java
        if (buckets[endBucket + 1].doubleValue() == buckets[endBucket].doubleValue()) {
          // if start and end points of the bucket are the same, consider entire bucket
          endBucketFraction = 1.0;
        } else if (range.upperBoundType() == BoundType.CLOSED && buckets[endBucket].doubleValue() == highValue.doubleValue()) {
          // predicate is of type <= 5.0 and 5.0 happens to be the start point of the bucket
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/common/DrillRelOptUtil.java`
#### Snippet
```java
        return findScan(Util.first(relSubset.getBest(), relSubset.getOriginal()));
      } else {
        return findScan(rel.getInputs().toArray(new RelNode[0]));
      }
    }
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillWriterRel.java`
#### Snippet
```java
      Preconditions.checkArgument(field != null,
          String.format("partition col %s could not be resolved in table's column lists!", col));
      keys.add(field.getIndex());
    }

```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
          matchingSequence = sb.toString();
          logger.debug("FindRelSequence: ABORT: Unexpected Rel={}, After={}, CurSeq={}",
              rel.getClass().getCanonicalName().toString(), matchingSequence, sequence);
        }
        matchingRels.clear();
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
      }
    } else {
      for (RelNode child : rel.getInputs()) {
        if (!canSwapJoinInputsInternal(child)) {
          return false;
```

### DataFlowIssue
Casting `formatConfig` to `ParquetFormatConfig` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/ConvertCountToDirectScanRule.java`
#### Snippet
```java

    ParquetReaderConfig parquetReaderConfig= ParquetReaderConfig.builder()
            .withFormatConfig((ParquetFormatConfig) formatConfig)
            .withOptions(settings.getOptions())
            .build();
```

### DataFlowIssue
Method invocation `getKind` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillOptiq.java`
#### Snippet
```java
      if (operand instanceof RexLiteral) {
        literal = (RexLiteral) operand;
      } else if (isMap && operand.getKind() == SqlKind.CAST) {
        SqlTypeName castType = operand.getType().getSqlTypeName();
        SqlTypeName keyType = dataType.getKeyType().getSqlTypeName();
```

### DataFlowIssue
Casting `op` to `FragmentRoot` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
            accept(OperatorIdVisitor.INSTANCE, 0);
        Preconditions.checkArgument(op instanceof FragmentRoot);
        FragmentRoot root = (FragmentRoot) op;

        PlanFragment fragment = PlanFragment.newBuilder()
```

### DataFlowIssue
Argument `root` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/contrib/SplittingParallelizer.java`
#### Snippet
```java
            .setMemMax(wrapper.getMaxAllocation()) // TODO - for some reason OOM is using leaf fragment max allocation divided by width
            .setCredentials(session.getCredentials())
            .addAllCollector(CountRequiredFragments.getCollectors(root, enableDynamicFC))
            .build();

```

### DataFlowIssue
Casting `op` to `FragmentRoot` may produce `ClassCastException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
        PhysicalOperator op = physicalOperatorRoot.accept(Materializer.INSTANCE, iNode);
        Preconditions.checkArgument(op instanceof FragmentRoot);
        FragmentRoot root = (FragmentRoot) op;

        FragmentHandle handle = FragmentHandle
```

### DataFlowIssue
Argument `root` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
            .setMemMax(wrapper.getMaxAllocation())
            .setCredentials(session.getCredentials())
            .addAllCollector(CountRequiredFragments.getCollectors(root, enableDynamicFC))
            .build();

```

### DataFlowIssue
Method invocation `getGroupScan` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/JoinPruleBase.java`
#### Snippet
```java
        DrillDistributionTrait.DistributionType.RANGE_DISTRIBUTED,
        ImmutableList.copyOf(rangeDistFields),
        ((DbGroupScan)((DrillScanRelBase) leftScan).getGroupScan()).getRangePartitionFunction(rangeDistRefList));

    RelTraitSet traitsLeft = null;
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/LimitExchangeTransposeRule.java`
#### Snippet
```java
      }
    } else {
      for (RelNode child : rel.getInputs()) {
        if (findRowKeyJoin(child)) {
          return true;
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/NumberingRelWriter.java`
#### Snippet
```java
      this.explainInputs((LateralJoinPrel) rel);
    } else {
      List<RelNode> inputs = rel.getInputs();
      if (rel instanceof HashJoinPrel && ((HashJoinPrel) rel).isSwapped()) {
        HashJoinPrel joinPrel = (HashJoinPrel) rel;
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
      }
    } else {
      List<RelNode> relNodes = rightRelNode.getInputs();
      if (relNodes.size() == 1) {
        RelNode leftNode = relNodes.get(0);
```

### DataFlowIssue
Method invocation `getInputs` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java
      }
    } else {
      List<RelNode> relNodes = leftRelNode.getInputs();
      RelNode leftNode = relNodes.get(0);
      return findLeftScanPrel(fieldName, leftNode);
```

### DataFlowIssue
Condition `prel instanceof RelSubset` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/RuntimeFilterVisitor.java`
#### Snippet
```java

      Prel currentPrel;
      if (prel instanceof RelSubset) {
        currentPrel = (Prel) ((RelSubset) prel).getBest();
      } else {
```

### DataFlowIssue
Argument `senderFragmentEndpoints` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractExchange.java`
#### Snippet
```java
        "Sender fragment endpoint list should not be empty");

    return ParallelizationInfo.create(1, Integer.MAX_VALUE, getDefaultAffinityMap(senderFragmentEndpoints));
  }

```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
    // one or many batches. For each new record siftUp (or heapify) to adjust min heap property is called.
    for (; queueSize < limit && count < batch.getRecordCount();  count++) {
      heapSv4.set(queueSize, batchCount, hasSv2 ? sv2.getIndex(count) : count);
      queueSize++;
      siftUp();
```

### DataFlowIssue
Method invocation `getIndex` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
    // are still keeping all the records unless purge is called.
    for (; count < batch.getRecordCount(); count++) {
      heapSv4.set(limit, batchCount, hasSv2 ? sv2.getIndex(count) : count);
      if (compare(limit, 0) < 0) {
        swap(limit, 0);
```

### DataFlowIssue
Method invocation `clear` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/TopN/PriorityQueueTemplate.java`
#### Snippet
```java
    batchCount++;
    if (hasSv2) {
      sv2.clear();
    }
    logger.debug("Took {} us to add {} records", watch.elapsed(TimeUnit.MICROSECONDS), count);
```

### DataFlowIssue
Result of 'min' is the same as the first argument making the call meaningless
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/ScanLifecycleBuilder.java`
#### Snippet
```java
  public void batchByteLimit(int byteLimit) {
    scanBatchByteLimit = Math.max(MIN_BATCH_BYTE_SIZE,
        Math.min(byteLimit, MAX_BATCH_BYTE_SIZE));
  }

```

### DataFlowIssue
Result of 'min' is the same as the first argument making the call meaningless
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/ScanLifecycleBuilder.java`
#### Snippet
```java

  public int scanBatchByteLimit() {
    return Math.max(MIN_BATCH_BYTE_SIZE, Math.min(scanBatchByteLimit, MAX_BATCH_BYTE_SIZE));
  }

```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/LateralJoinBatch.java`
#### Snippet
```java
            // batch with EMIT outcome
            if ((leftUpstream == EMIT || leftUpstream == OK_NEW_SCHEMA) && left.getRecordCount() == 0) {
              isLeftProcessed = true;
              break;
            }
```

### DataFlowIssue
Argument `result` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaConfigBuilder.java`
#### Snippet
```java
      // No defined schema: this is a projection-based tracker, possibly
      // constrained by a provided schema.
      ProjectionSchemaTracker tracker = new ProjectionSchemaTracker(result, allowSchemaChange, errorContext);

      // Apply the provided schema. Doing so forces resolution of the projection
```

### DataFlowIssue
Argument `result` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/ScanSchemaConfigBuilder.java`
#### Snippet
```java
        // The defined schema has not dynamic columns: it is fully defined, just like
        // in a "classic" DB. Use a schema-driven schema tracker.
        return new ProjectionSchemaTracker(definedSchema, result, errorContext);
      }
    }
```

### DataFlowIssue
Method invocation `initialize` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
      buildCalc = calc.next();

      buildCalc.initialize(true, true, // TODO Fix after growing hash values bug
                                       // fixed
          buildBatch, probeBatch, buildJoinColumns,
```

### DataFlowIssue
Method invocation `initialize` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
      buildCalc = calc.next();

      buildCalc.initialize(spilledState.isFirstCycle(), true, // TODO Fix after
                                                              // growing hash
                                                              // values bug
```

### DataFlowIssue
Method invocation `initialize` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/AbstractHashBinaryRecordBatch.java`
#### Snippet
```java
    HashJoinMemoryCalculator.PostBuildCalculations postBuildCalc = buildCalc
        .next();
    postBuildCalc.initialize(probeSideIsEmpty.booleanValue()); // probeEmpty

    // Traverse all the in-memory partitions' incoming batches, and build their
```

### DataFlowIssue
Dereference of `resolvers` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ReaderLevelProjection.java`
#### Snippet
```java
        List<ReaderProjectionResolver> resolvers) {
    this.resolvers = resolvers == null ? new ArrayList<>() : resolvers;
    for (ReaderProjectionResolver resolver : resolvers) {
      resolver.startResolution();
    }
```

### DataFlowIssue
Argument `defn.conversionClass` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/StandardConversions.java`
#### Snippet
```java
      case IMPLICIT:
      case IMPLICIT_UNSAFE:
        return newInstance(defn.conversionClass, scalarWriter, columnProps);
      default:
        return scalarWriter;
```

### DataFlowIssue
Result of 'min' is the same as the first argument making the call meaningless
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ScanSchemaOrchestrator.java`
#### Snippet
```java
    public void batchByteLimit(int byteLimit) {
      scanBatchByteLimit = Math.max(MIN_BATCH_BYTE_SIZE,
          Math.min(byteLimit, MAX_BATCH_BYTE_SIZE));
    }

```

### DataFlowIssue
Method invocation `resolve` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ReaderSchemaOrchestrator.java`
#### Snippet
```java
  private void doSmoothedProjection(TupleMetadata tableSchema) {
    rootTuple = newRootTuple();
    scanOrchestrator.schemaSmoother.resolve(tableSchema, rootTuple);
  }

```

### DataFlowIssue
Dereference of `spillDir.listFiles()` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
    public void deleteDir(String fragmentSpillDir) throws IOException {
      File spillDir = new File(baseDir, fragmentSpillDir);
      for (File spillFile : spillDir.listFiles()) {
        spillFile.delete(); // IO exception if file delete fails
      }
```

### DataFlowIssue
Method `setup` is too complex to analyze by data flow algorithm
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/MSortTemplate.java`
#### Snippet
```java

  @Override
  public void setup(FragmentContext context, BufferAllocator allocator, SelectionVector4 vector4,
                    VectorContainer hyperBatch, int outputBatchSize, int desiredBatchSize) throws SchemaChangeException{
    // we pass in the local hyperBatch since that is where we'll be reading data.
```

### DataFlowIssue
Unboxing of `runStarts.poll()` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/MSortTemplate.java`
#### Snippet
```java
      int size = runStarts.size();
      for (int i = 0; i < size / 2; i++) {
        int left = runStarts.poll();
        int right = runStarts.poll();
        Integer end = runStarts.peek();
```

### DataFlowIssue
Unboxing of `runStarts.poll()` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/MSortTemplate.java`
#### Snippet
```java
      for (int i = 0; i < size / 2; i++) {
        int left = runStarts.poll();
        int right = runStarts.poll();
        Integer end = runStarts.peek();
        if (end == null) {
```

### DataFlowIssue
Switch label `FINISHED` is unreachable
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataControllerBatch.java`
#### Snippet
```java
          return IterOutcome.OK_NEW_SCHEMA;

        case FINISHED:
          break;

```

### DataFlowIssue
Condition `vector instanceof NullableVarCharVector` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java

  private int getLastSet(NullableVector vector) {
    if (vector instanceof NullableVarCharVector) {
      return ((NullableVarCharVector) vector).getMutator().getLastSet();
    }
```

### DataFlowIssue
Condition `vector instanceof RepeatedBitVector` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    if (vector instanceof BitVector) {
      validateBitVector(name, (BitVector) vector);
    } else if (vector instanceof RepeatedBitVector) {
      validateRepeatedBitVector(name, (RepeatedBitVector) vector);
    } else if (vector instanceof NullableVector) {
```

### DataFlowIssue
Method invocation `getClass` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/validate/BatchValidator.java`
#### Snippet
```java
    } else {
      logger.debug("Don't know how to validate vector: {}  of class {}",
          name, vector.getClass().getSimpleName());
    }
  }
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/statistics/StatisticsMergeBatch.java`
#### Snippet
```java
            inputFunc));
      }
      if (columnsList.size() != lastMapColumnsList.size()
          || !lastMapColumnsList.containsAll(columnsList)) {
        // Error!! Maps with different size and/or keys. The map for each statistics (e.g. NDV)
```

### DataFlowIssue
Method invocation `terminate` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionSenderRootExec.java`
#### Snippet
```java
        terminations.add(id);
      } else {
        partitioner.getOutgoingBatches(id).terminate();
      }

```

### DataFlowIssue
Method invocation `terminate` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionSenderRootExec.java`
#### Snippet
```java
      partitioner = new PartitionerDecorator(subPartitioners, stats, context);
      for (int index = 0; index < terminations.size(); index++) {
        partitioner.getOutgoingBatches(terminations.buffer[index]).terminate();
      }
      terminations.clear();
```

### DataFlowIssue
Argument `senderFragmentEndpoints` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/SingleMergeExchange.java`
#### Snippet
```java
        "Sender fragment endpoint list should not be empty");

    return ParallelizationInfo.create(1, 1, getDefaultAffinityMap(senderFragmentEndpoints));
  }

```

### DataFlowIssue
Method invocation `getMultiMap` will produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
    // Clearly, this code is not used!
    // cache can only be null here!
    this.mmap = cache.getMultiMap(MULTI_CACHE_CONFIG);
    this.tableMap = cache.getMap(SINGLE_CACHE_CONFIG);
    Preconditions.checkNotNull(tableMap);
```

### DataFlowIssue
Argument `vc` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
      // Now that we have the partition vectors, we immediately process the first batch on the queue
      VectorContainer vc = batchQueue.poll();
      setupNewSchema(vc);
      doWork(vc);
      vc.zeroVectors();
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/orderedpartitioner/OrderedPartitionRecordBatch.java`
#### Snippet
```java
    // Extract vectors from the wrapper, and add to partition vectors. These vectors will be used for partitioning in
    // the rest of this operator
    for (VectorWrapper<?> w : finalTable.get()) {
      partitionVectors.add(w.getValueVector());
    }
```

### DataFlowIssue
Argument `batch` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java

      g.setMappingSet(COPIER_MAPPING_SET);
      CopyUtil.generateCopies(g, batch, true);
      g.setMappingSet(MAIN_MAPPING);
      final MergingReceiverGeneratorBase merger = context.getImplementationClass(cg);
```

### DataFlowIssue
Argument `senderFragmentEndpoints` might be null
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/config/UnionExchange.java`
#### Snippet
```java
        "Sender fragment endpoint list should not be empty");

    return ParallelizationInfo.create(1, 1, getDefaultAffinityMap(senderFragmentEndpoints));
  }

```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/rowSet/RowSetWriterImpl.java`
#### Snippet
```java
      // Should not call next() again once batch is full.
      assert rowIndex == ValueVector.MAX_ROW_COUNT;
      rowIndex = ValueVector.MAX_ROW_COUNT;
      state = state == State.OK ? State.END_OF_BATCH : state;
      return false;
```

### DataFlowIssue
Method invocation `isVariant` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/hyper/HyperSchemaInference.java`
#### Snippet
```java
    // Special handling of lists and unions

    if (commonSchema.isVariant()) {
      VariantMetadata variantSchema = commonSchema.variantSchema();
      if (variantSchema.size() == 1) {
```

### DataFlowIssue
Condition `dataVector instanceof UnionVector` is redundant and can be replaced with a null check
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/single/SingleSchemaInference.java`
#### Snippet
```java
  private VariantSchema inferListSchema(ListVector vector) {
    final ValueVector dataVector = vector.getDataVector();
    if (dataVector instanceof UnionVector) {
      return inferUnionSchema((UnionVector) dataVector);
    }
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/model/single/SingleSchemaInference.java`
#### Snippet
```java
    final VariantSchema schema = new VariantSchema();
    if (! vector.isEmptyType()) {
      schema.addType(inferVector(dataVector));
    }
    return schema;
```

### DataFlowIssue
Dereference of `vertexDatum` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/ResolverTypePrecedence.java`
#### Snippet
```java
      // Poll the datum with lowest totalDistance in remaining
      VertexDatum vertexDatum = remaining.pollFirst();
      MinorType vertex = vertexDatum.vertex;
      shortestPath.add(vertex);

```

### DataFlowIssue
Method invocation `getFiles` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/ParquetTableMetadataProviderImpl.java`
#### Snippet
```java
            return selection;
          }
          for (MetadataBase.ParquetFileMetadata file : metadata.getFiles()) {
            fileSet.add(file.getPath());
          }
```

### DataFlowIssue
Dereference of `lastAttemptEx` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/calcite/jdbc/DynamicRootSchema.java`
#### Snippet
```java
    }

    throw lastAttemptEx;
  }

```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/calcite/jdbc/DynamicRootSchema.java`
#### Snippet
```java
        String msg = String.format(
          "The plugin %s will now be disabled (see SYSTEM option %s)",
          plugin.getName(),
          ExecConstants.STORAGE_PLUGIN_AUTO_DISABLE
        );
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java
              pageBufOffset += repLevelSize;

              bb = (ByteBuffer) pageBuf.position(pageBufOffset);
              final BytesInput defLevelBytes = BytesInput.from(
                (ByteBuffer) bb.slice().limit(pageBufOffset + defLevelSize)
```

### DataFlowIssue
Variable is already assigned to this value
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ColumnChunkIncReadStore.java`
#### Snippet
```java

              // we've now reached the beginning of compressed column data
              bb = (ByteBuffer) pageBuf.position(pageBufOffset);
              final BytesInput colDataBytes = decompressor.decompress(
                BytesInput.from((ByteBuffer) bb.slice()),
```

### DataFlowIssue
Casting `rootColumn` to `MapColumnDefn` may produce `ClassCastException`
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageDirectoryProcessor.java`
#### Snippet
```java
                    rootColumn = new ListColumnDefn(formatName).builder((ArrayWriter) subColumn);
                  } else { // for map
                    subColumn = ((MapColumnDefn) rootColumn).addMap(formatName);
                    // set up the current writer in nested structure
                    rootColumn = new MapColumnDefn(formatName).builder((TupleWriter) subColumn);
```

### DataFlowIssue
Method invocation `setObject` may produce `NullPointerException`
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageDirectoryProcessor.java`
#### Snippet
```java
              String parent = elements[elements.length - 1];
              if (parent.startsWith("[")) {
                subColumn.setObject(new String[] { value });
              } else {
                rootColumn.addText(ImageMetadataUtils.formatName(parent)).setString(value);
```

### DataFlowIssue
Method invocation `merge` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java

        // collapse GlobalMetaData into a single FileMetaData, which will throw if they are not compatible
        return new ParquetMetadata(globalMetaData.merge(keyValueMetadataMergeStrategy), blocks);
    }

```

### DataFlowIssue
Method invocation `merge` may produce `NullPointerException`
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
            }
        }
        return new ParquetMetadata(fileMetaData.merge(keyValueMergeStrategy), blocks);
    }

```

### DataFlowIssue
Method invocation `toInstant` may produce `NullPointerException`
in `contrib/storage-googlesheets/src/main/java/org/apache/drill/exec/store/googlesheets/columns/GoogleSheetsColumnWriter.java`
#### Snippet
```java
        Instant finalValue;
        try {
          finalValue = Typifier.stringAsDateTime(stringValue).toInstant(ZoneOffset.UTC);
        } catch (NumberFormatException e) {
          finalValue = null;
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
        List<MapRDBSubScanSpec> smallestList = minHeap.poll();
        MapRDBSubScanSpec subScanSpec = getSubScanSpec(regionEntry.getKey());
        smallestList.add(subScanSpec);
        if (smallestList.size() < maxPerEndpointSlot) {
          minHeap.offer(smallestList);
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
      List<MapRDBSubScanSpec> smallestList = minHeap.poll();
      List<MapRDBSubScanSpec> largestList = maxHeap.poll();
      smallestList.add(largestList.remove(largestList.size() - 1));
      if (largestList.size() > minPerEndpointSlot) {
        maxHeap.offer(largestList);
```

### DataFlowIssue
Method invocation `getTableIndex` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
      logger.error("Null IndexDiscover was found for {}!", scanRel);
    }
    return discover.getTableIndex(getTableName());
  }

```

### DataFlowIssue
Method invocation `close` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
              key.indexDesc == null ? "null" : key.indexDesc.getIndexName(),
              key.ugi.getUserName() == null ? "null" : key.ugi.getUserName());
          table.close(); // close the table
        }
      };
```

### DataFlowIssue
Casting `child` to `PathSegment.NameSegment` may produce `ClassCastException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/FieldPathHelper.java`
#### Snippet
```java
      }
    }
    return new SchemaPath((PathSegment.NameSegment)child);
  }

```

### DataFlowIssue
Method invocation `getIndexDesc` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/json/JsonTableGroupScan.java`
#### Snippet
```java
    }
    int numColumns = (columns == null || columns.isEmpty()) ?  totalColNum: columns.size();
    String idxIdentifier = stats.buildUniqueIndexIdentifier(scanSpec.getIndexDesc().getPrimaryTablePath(),
        scanSpec.getIndexDesc().getIndexName());
    double rowCount = stats.getRowCount(scanSpec.getCondition(), idxIdentifier);
```

### DataFlowIssue
Casting `primaryTableGroupScan` to `DbGroupScan` may produce `ClassCastException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBIndexDescriptor.java`
#### Snippet
```java
      int numProjectedFields, GroupScan primaryTableGroupScan) {
    Preconditions.checkArgument(primaryTableGroupScan instanceof DbGroupScan);
    DbGroupScan dbGroupScan = (DbGroupScan) primaryTableGroupScan;
    DrillCostFactory costFactory = (DrillCostFactory)planner.getCostFactory();
    double totalRows = indexProps.getTotalRows();
```

### DataFlowIssue
Method invocation `getColumns` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBIndexDescriptor.java`
#### Snippet
```java
      // for the primary table join-back each row may belong to a different block, so in general num_blocks = num_rows;
      // however, num_blocks cannot exceed the total number of blocks of the table
      double totalBlocksPrimary = Math.ceil((dbGroupScan.getColumns().size() *
          pluginCost.getAverageColumnSize(primaryTableGroupScan) * totalRows)/
          pluginCost.getBlockSize(primaryTableGroupScan));
```

### DataFlowIssue
Variable is already assigned to this value
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
      }
    } else {
      guess = false;
      if (baseConditionMap.get(conditionAsStr) != null) {
        double rowCount = baseConditionMap.get(conditionAsStr);
```

### DataFlowIssue
Method invocation `getTypeName` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
      }
      // Get the PATTERN strings from the corresponding RexLiteral
      if (pattern.getTypeName() == SqlTypeName.DECIMAL ||
          pattern.getTypeName() == SqlTypeName.INTEGER) {
        patternStr = pattern.getValue().toString();
```

### DataFlowIssue
Method invocation `getTypeName` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
      }
      // Get the PATTERN and ESCAPE strings from the corresponding RexLiteral
      if (pattern.getTypeName() == SqlTypeName.DECIMAL ||
          pattern.getTypeName() == SqlTypeName.INTEGER) {
        patternStr = pattern.getValue().toString();
```

### DataFlowIssue
Method invocation `getTypeName` may produce `NullPointerException`
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBStatistics.java`
#### Snippet
```java
        patternStr = pattern.getValue2().toString();
      }
      if (escape.getTypeName() == SqlTypeName.DECIMAL ||
          escape.getTypeName() == SqlTypeName.INTEGER) {
        escapeStr = escape.getValue().toString();
```

### DataFlowIssue
Casting `input[i]` to `boolean[]` may produce `ClassCastException`
in `contrib/format-hdf5/src/main/java/org/apache/drill/exec/store/hdf5/HDF5Utils.java`
#### Snippet
```java

    for (int i = 0; i <  rows; i++) {
      boolean[] row = (boolean[])input[i];
      for (int j = 0; j < cols; j++) {
        result[j][i] = row[j];
```

### DataFlowIssue
Casting `buf` to `PooledUnsafeDirectByteBuf` may produce `ClassCastException`
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
          }

          return new UnsafeDirectLittleEndian((PooledUnsafeDirectByteBuf) buf,
              normalBufferCount, normalBufferSize);
        }
```

### DataFlowIssue
Dereference of `childAllocator` may produce `NullPointerException`
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
        final Object object = childAllocators.remove(childAllocator);
        if (object == null) {
          childAllocator.historicalLog.logHistory(logger);
          throw new IllegalStateException("Child allocator[" + childAllocator.name
              + "] not found in parent allocator[" + name + "]'s childAllocators");
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
        for (ChunkInfo chunkInfo : chunkEntry.getValue()) {
          List<BaseMongoSubScanSpec> smallestList = minHeap.poll();
          smallestList.add(buildSubScanSpecAndGet(chunkInfo));
          minHeap.offer(smallestList);
        }
```

### DataFlowIssue
Method invocation `remove` may produce `NullPointerException`
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
      List<BaseMongoSubScanSpec> smallestList = minHeap.poll();
      List<BaseMongoSubScanSpec> largestList = maxHeap.poll();
      smallestList.add(largestList.remove(largestList.size() - 1));
      if (largestList.size() > minPerEndpointSlot) {
        maxHeap.offer(largestList);
```

### DataFlowIssue
Switch label `"PRIMARY"` is unreachable
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java

    switch (readPreference.getName().toUpperCase()) {
    case "PRIMARY":
    case "PRIMARYPREFERRED":
      if (primaryHost == null) {
```

### DataFlowIssue
Switch label `"PRIMARYPREFERRED"` is unreachable
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
    switch (readPreference.getName().toUpperCase()) {
    case "PRIMARY":
    case "PRIMARYPREFERRED":
      if (primaryHost == null) {
        return null;
```

### DataFlowIssue
Switch label `"SECONDARY"` is unreachable
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
      addressList.add(new ServerAddress(primaryHost));
      return addressList;
    case "SECONDARY":
    case "SECONDARYPREFERRED":
      if (primaryHost == null || hostsList == null) {
```

### DataFlowIssue
Switch label `"SECONDARYPREFERRED"` is unreachable
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
      return addressList;
    case "SECONDARY":
    case "SECONDARYPREFERRED":
      if (primaryHost == null || hostsList == null) {
        return null;
```

### DataFlowIssue
Switch label `"NEAREST"` is unreachable
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/mongo/MongoGroupScan.java`
#### Snippet
```java
      }
      return addressList;
    case "NEAREST":
      if (hostsList == null) {
        return null;
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `contrib/udfs/src/main/java/org/apache/drill/exec/udfs/DateFunctions.java`
#### Snippet
```java
      String input = org.apache.drill.exec.expr.fn.impl.StringFunctionHelpers.toStringFromUTF8(inputHolder.start, inputHolder.end, inputHolder.buffer);
      java.time.LocalDateTime dt = org.apache.drill.exec.udfs.DateUtilFunctions.getTimestampFromString(input);
      int week = dt.get(java.time.temporal.IsoFields.WEEK_OF_WEEK_BASED_YEAR);
      int year = dt.getYear();
      out.value = (year * 100) + week;
```

### DataFlowIssue
Method invocation `accept` may produce `NullPointerException`
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaNodeProcessor.java`
#### Snippet
```java
      evaluator.functionName = COMPARE_FUNCTIONS_TRANSPOSE_MAP.get(functionName);
    }
    evaluator.success = nameArg.accept(evaluator, valueArg);
    return evaluator;
  }
```

### DataFlowIssue
Method invocation `delete` may produce `NullPointerException`
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
          try { sleep(1_000); } catch (final Exception ex) { /* ignore interruptions */ }
        }
        try { outErr.delete(); } catch (final Exception ex) { } // cleanup - remove err msg file
      }

```

### DataFlowIssue
Immutable object is modified
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java
  @Override
  public V put(K key, V value) {
    return delegate.put(key, value);
  }

```

### DataFlowIssue
Immutable object is modified
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java
  @Override
  public V remove(Object key) {
    return delegate.remove(key);
  }

```

### DataFlowIssue
Immutable object is modified
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java
  @Override
  public void putAll(Map<? extends K, ? extends V> m) {
    delegate.putAll(m);
  }

```

### DataFlowIssue
Immutable object is modified
in `common/src/main/java/org/apache/drill/common/collections/MapWithOrdinal.java`
#### Snippet
```java
  @Override
  public void clear() {
    delegate.clear();
  }

```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'MAX_ALLOCATION_SIZE' is still used
in `exec/vector/src/main/java/org/apache/drill/exec/vector/BaseValueVector.java`
#### Snippet
```java

  @Deprecated
  public static final int MAX_ALLOCATION_SIZE = Integer.MAX_VALUE;
  public static final int INITIAL_VALUE_ALLOCATION = 4096;

```

### DeprecatedIsStillUsed
Deprecated member 'xmlDataLevel' is still used
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpApiConfig.java`
#### Snippet
```java

  @Deprecated
  public int xmlDataLevel() {
    return this.xmlDataLevel;
  }
```

### DeprecatedIsStillUsed
Deprecated member 'xmlDataLevel' is still used
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpApiConfig.java`
#### Snippet
```java
  @JsonProperty
  @Deprecated
  private final int xmlDataLevel;
  @JsonProperty
  private final String limitQueryParam;
```

### DeprecatedIsStillUsed
Deprecated member 'HTTP_MEMORY_HEAP_FAILURE_THRESHOLD' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String HTTP_WEB_CLIENT_RESULTSET_ROWS_PER_PAGE_VALUES = "drill.exec.http.web.client.resultset.rowsPerPageValues";
  @Deprecated // TODO: Remove any logic based on this option now that REST query results stream.
  public static final String HTTP_MEMORY_HEAP_FAILURE_THRESHOLD = "drill.exec.http.memory.heap.failure.threshold";
  //Customize filters in options
  public static final String HTTP_WEB_OPTIONS_FILTERS = "drill.exec.http.web.options.filters";
```

### DeprecatedIsStillUsed
Deprecated member 'PARQUET_VECTOR_FILL_THRESHOLD_VALIDATOR' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_VECTOR_FILL_THRESHOLD = "store.parquet.vector_fill_threshold";
  @Deprecated // TODO: DRILL-6527
  public static final OptionValidator PARQUET_VECTOR_FILL_THRESHOLD_VALIDATOR = new PositiveLongValidator(PARQUET_VECTOR_FILL_THRESHOLD, 99L,
      new OptionDescription("Deprecated."));
  @Deprecated // TODO: DRILL-6527
```

### DeprecatedIsStillUsed
Deprecated member 'ENABLE_BULK_LOAD_TABLE_LIST' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
   */
  @Deprecated
  public static final BooleanValidator ENABLE_BULK_LOAD_TABLE_LIST = new BooleanValidator(ENABLE_BULK_LOAD_TABLE_LIST_KEY,
      new OptionDescription("Deprecated after DRILL-7115 improvement."));

```

### DeprecatedIsStillUsed
Deprecated member 'EXTERNAL_SORT_DISABLE_MANAGED_OPTION' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String EXTERNAL_SORT_DISABLE_MANAGED = "drill.exec.sort.external.disable_managed";
  @Deprecated
  public static final BooleanValidator EXTERNAL_SORT_DISABLE_MANAGED_OPTION = new BooleanValidator("exec.sort.disable_managed", null);

  // Hash Join Options
```

### DeprecatedIsStillUsed
Deprecated member 'ENABLE_V3_TEXT_READER' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String ENABLE_V3_TEXT_READER_KEY = "exec.storage.enable_v3_text_reader";
  @Deprecated
  public static final OptionValidator ENABLE_V3_TEXT_READER = new BooleanValidator(ENABLE_V3_TEXT_READER_KEY,
      new OptionDescription("Deprecated. The \"V3\" text reader is always used."));

```

### DeprecatedIsStillUsed
Deprecated member 'ENABLE_NEW_TEXT_READER' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String ENABLE_NEW_TEXT_READER_KEY = "exec.storage.enable_new_text_reader";
  @Deprecated
  public static final OptionValidator ENABLE_NEW_TEXT_READER = new BooleanValidator(ENABLE_NEW_TEXT_READER_KEY,
      new OptionDescription("Deprecated. Drill's text reader complies with the RFC 4180 standard for text/csv files."));

```

### DeprecatedIsStillUsed
Deprecated member 'HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  // in core which is not right. Move this option and above two mongo plugin related options once we have the feature.
  @Deprecated // TODO: DRILL-6527
  public static final String HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS = "store.hive.optimize_scan_with_native_readers";
  @Deprecated // TODO: DRILL-6527
  public static final OptionValidator HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS_VALIDATOR = new BooleanValidator(HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS,
```

### DeprecatedIsStillUsed
Deprecated member 'HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS_VALIDATOR' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS = "store.hive.optimize_scan_with_native_readers";
  @Deprecated // TODO: DRILL-6527
  public static final OptionValidator HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS_VALIDATOR = new BooleanValidator(HIVE_OPTIMIZE_SCAN_WITH_NATIVE_READERS,
      new OptionDescription("Deprecated as of Drill 1.14. Use the store.hive.parquet.optimize_scan_with_native_reader option instead. Enables Drill to use the Drill native reader (instead of the Hive Serde interface) to optimize reads of Parquet-backed tables from Hive. Default is false."));
  public static final String HIVE_OPTIMIZE_PARQUET_SCAN_WITH_NATIVE_READER = "store.hive.parquet.optimize_scan_with_native_reader";
```

### DeprecatedIsStillUsed
Deprecated member 'BULK_LOAD_TABLE_LIST_BULK_SIZE' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
   */
  @Deprecated
  public static final PositiveLongValidator BULK_LOAD_TABLE_LIST_BULK_SIZE = new PositiveLongValidator(BULK_LOAD_TABLE_LIST_BULK_SIZE_KEY, Integer.MAX_VALUE,
      new OptionDescription("Deprecated after DRILL-7115 improvement."));

```

### DeprecatedIsStillUsed
Deprecated member 'PARQUET_VECTOR_FILL_CHECK_THRESHOLD_VALIDATOR' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/ExecConstants.java`
#### Snippet
```java
  public static final String PARQUET_VECTOR_FILL_CHECK_THRESHOLD = "store.parquet.vector_fill_check_threshold";
  @Deprecated // TODO: DRILL-6527
  public static final OptionValidator PARQUET_VECTOR_FILL_CHECK_THRESHOLD_VALIDATOR = new PositiveLongValidator(PARQUET_VECTOR_FILL_CHECK_THRESHOLD, 100L,
      new OptionDescription("Deprecated."));

```

### DeprecatedIsStillUsed
Deprecated member 'build_email' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/VersionIterator.java`
#### Snippet
```java
    public String commit_time = "";
    /** @deprecated No longer populated in order to achieve reproducible builds */
    public String build_email = "Unknown";
    /** @deprecated No longer populated in order to achieve reproducible builds */
    public String build_time = "";
```

### DeprecatedIsStillUsed
Deprecated member 'build_time' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/VersionIterator.java`
#### Snippet
```java
    public String build_email = "Unknown";
    /** @deprecated No longer populated in order to achieve reproducible builds */
    public String build_time = "";

    public VersionInfo(){
```

### DeprecatedIsStillUsed
Deprecated member 'JSONRecordReader' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JSONRecordReader.java`
#### Snippet
```java
 */
@Deprecated
public class JSONRecordReader extends AbstractRecordReader {
  private static final Logger logger = LoggerFactory.getLogger(JSONRecordReader.class);

```

### DeprecatedIsStillUsed
Deprecated member 'createSessionUserPrincipal' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRestServer.java`
#### Snippet
```java
     */
    @Deprecated
    private Principal createSessionUserPrincipal(DrillConfig config, HttpServletRequest request) {
      if (WebServer.isOnlyImpersonationEnabled(config)) {
        final String userName = request.getHeader("User-Name");
```

### DeprecatedIsStillUsed
Deprecated member 'JsonReader' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/vector/complex/fn/JsonReader.java`
#### Snippet
```java
 */
@Deprecated
public class JsonReader extends BaseJsonReader {
  private static final Logger logger = LoggerFactory.getLogger(JsonReader.class);
  public final static int MAX_RECORD_SIZE = 128 * 1024;
```

### DeprecatedIsStillUsed
Deprecated member 'complexWriters' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/project/ProjectRecordBatch.java`
#### Snippet
```java
  protected List<ValueVector> allocationVectors;
  @Deprecated // use new writer rsLoader
  protected List<ComplexWriter> complexWriters;
  protected ResultSetLoader rsLoader;
  protected List<FieldReference> complexFieldReferencesList;
```

### DeprecatedIsStillUsed
Deprecated member 'limitReached' is still used
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/RowSetLoader.java`
#### Snippet
```java
   */
  @Deprecated() // use the limit in options instead
  boolean limitReached(int maxRecords);

  /**
```

### DeprecatedIsStillUsed
Deprecated member 'PcapngFormatConfig' is still used
in `contrib/format-pcapng/src/main/java/org/apache/drill/exec/store/pcap/plugin/PcapngFormatConfig.java`
#### Snippet
```java
@JsonInclude(JsonInclude.Include.NON_DEFAULT)
@Deprecated // for backward compatibility
public class  PcapngFormatConfig extends PcapFormatConfig {
  private static final List<String> DEFAULT_EXTNS = ImmutableList.of("pcapng");
  public static final String NAME = "pcapng";
```

### DeprecatedIsStillUsed
Deprecated member 'ParquetFileWriter' is still used
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    @Deprecated
    public ParquetFileWriter(Configuration configuration, MessageType schema,
                             Path file) throws IOException {
        this(HadoopOutputFile.fromPath(file, configuration),
```

### DeprecatedIsStillUsed
Deprecated member 'ParquetFileWriter' is still used
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    @Deprecated
    public ParquetFileWriter(Configuration configuration, MessageType schema,
                             Path file, Mode mode) throws IOException {
        this(HadoopOutputFile.fromPath(file, configuration),
```

### DeprecatedIsStillUsed
Deprecated member 'writeDataPage' is still used
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    @Deprecated
    public void writeDataPage(
            int valueCount, int uncompressedPageSize,
            BytesInput bytes,
```

### DeprecatedIsStillUsed
Deprecated member 'appendRowGroup' is still used
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    @Deprecated
    public void appendRowGroup(FSDataInputStream from, BlockMetaData rowGroup,
                               boolean dropColumns) throws IOException {
        appendRowGroup(HadoopStreams.wrap(from), rowGroup, dropColumns);
```

### DeprecatedIsStillUsed
Deprecated member 'ParquetFileWriter' is still used
in `exec/java-exec/src/main/java/org/apache/parquet/hadoop/ParquetFileWriter.java`
#### Snippet
```java
     */
    @Deprecated
    public ParquetFileWriter(Configuration configuration, MessageType schema,
                             Path file, Mode mode, long rowGroupSize,
                             int maxPaddingSize)
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `logical/src/main/java/org/apache/drill/common/expression/fn/FunctionReplacementUtils.java`
#### Snippet
```java
   */
  public static boolean isCastFunction(String functionName) {
    return TYPE_TO_CAST_FUNC.values().contains(functionName);
  }

```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/SimpleParallelizer.java`
#### Snippet
```java
      if (fragmentDependencies != null && fragmentDependencies.size() > 0) {
        for (Wrapper dependency : fragmentDependencies) {
          if (roots.contains(dependency)) {
            roots.remove(dependency);
          }
```

## RuleId[id=SwitchStatementWithTooFewBranches]
### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/ScalarArrayWriter.java`
#### Snippet
```java
      // bytes is represented as an array of byte arrays.
      final char third = objClass.charAt(2);
      switch (third) {
      case 'B':
        setBytesArray((byte[][]) array);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidStoragePlugin.java`
#### Snippet
```java
    PlannerPhase phase
  ) {
    switch (phase) {
      case PHYSICAL:
        return ImmutableSet.of(DruidPushDownFilterForScan.INSTANCE);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/HBaseStoragePlugin.java`
#### Snippet
```java
    PlannerPhase phase
  ) {
    switch (phase) {
      case PHYSICAL:
        return ImmutableSet.of(
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpPushDownListener.java`
#### Snippet
```java
     */
    private ColRelOpConstNode acceptRelOp(ColRelOpConstNode relOp) {
      switch (relOp.op) {
      case EQ:
        return acceptColumn(relOp.colName) &&
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/user/UserServer.java`
#### Snippet
```java
  protected MessageLite getResponseDefaultInstance(int rpcType) throws RpcException {
    // a user server only expects acknowledgments on messages it creates.
    switch (rpcType) {
    case RpcType.ACK_VALUE:
      return Ack.getDefaultInstance();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/QueryStateProcessor.java`
#### Snippet
```java

  private void checkCommonStates(final QueryState newState, final Exception exception) {
    switch (newState) {
      case FAILED:
        assert exception != null;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/RecordDataType.java`
#### Snippet
```java
      final SqlTypeName typeName = sqlTypePair.getKey();
      final RelDataType tempDataType;
      switch (typeName) {
        case VARCHAR:
          tempDataType = factory.createSqlType(typeName, Integer.MAX_VALUE);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/WorkspaceSchemaFactory.java`
#### Snippet
```java

        for (DotDrillFile f : files) {
          switch (f.getType()) {
            case VIEW:
              try {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/parser/ArrayParser.java`
#### Snippet
```java
      // Position: [ (value, )* ^ ?
      JsonToken token = tokenizer.requireNext();
      switch (token) {
        case END_ARRAY:
          break top;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/InfoSchemaStoragePlugin.java`
#### Snippet
```java
    PlannerPhase phase
  ) {
    switch (phase) {
      case PHYSICAL:
        return ImmutableSet.of(
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/Records.java`
#### Snippet
```java

            case MONTH:
              switch (end) {
              case MONTH:
                this.COLUMN_SIZE = this.INTERVAL_PRECISION + 2;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/ischema/Records.java`
#### Snippet
```java

            case SECOND:
              switch (end) {
              case SECOND:
                this.COLUMN_SIZE = this.INTERVAL_PRECISION + 3 + extraSecondIntervalSize;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ColumnReaderFactory.java`
#### Snippet
```java
  ) throws ExecutionSetupException {
    ConvertedType convertedType = schemaElement.getConverted_type();
    switch (descriptor.getMaxDefinitionLevel()) {
      case 0:
        if (convertedType == null) {
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/AbstractRecordBatch.java`
#### Snippet
```java
        case BUILD_SCHEMA: {
          buildSchema();
          switch (state) {
            case DONE:
              lastOutcome = IterOutcome.NONE;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillRelFactories.java`
#### Snippet
```java
                              RexNode condition, Set<CorrelationId> variablesSet,
                              JoinRelType joinType, boolean semiJoinDone) {
      switch (joinType) {
        case SEMI:
          JoinInfo joinInfo = JoinInfo.of(left, right, condition);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/v3/schema/AbstractSchemaTracker.java`
#### Snippet
```java
      return;
    }
    switch (projectionType()) {
    case ALL:
      isResolved = !schema.isEmpty() && schema.isResolved();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/convert/StandardConversions.java`
#### Snippet
```java
      case INTERVALYEAR:
      case INTERVALDAY:
        switch (outputSchema.type()) {
        case VARCHAR:
          return new ConversionDefn(ConvertIntervalToString.class);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/window/WindowFrameRecordBatch.java`
#### Snippet
```java
    logger.trace("buildSchema()");
    IterOutcome outcome = next(incoming);
    switch (outcome) {
    case NONE:
      state = BatchState.DONE;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/HashAggBatch.java`
#### Snippet
```java
  public void buildSchema() {
    IterOutcome outcome = next(incoming);
    switch (outcome) {
      case NONE:
        state = BatchState.DONE;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/aggregate/StreamingAggBatch.java`
#### Snippet
```java
  public void buildSchema() {
    IterOutcome outcome = next(incoming);
    switch (outcome) {
      case NONE:
        state = BatchState.DONE;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `metastore/rdbms-metastore/src/main/java/org/apache/drill/metastore/rdbms/util/DbHelper.java`
#### Snippet
```java
  static DbHelper init(DrillConfig config) {
    SQLDialect dialect = JDBCUtils.dialect(config.getString(RdbmsConfigConstants.DATA_SOURCE_URL));
    switch (dialect) {
      case SQLITE:
        return new SQLiteHelper(config);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaExecProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "sqlQuery";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaGeneralRPCProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "ok";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaBitData.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "allowedCredit";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaBitControl.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "fragmentStatus";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaBitControl.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "fragment";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "userName";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserBitShared.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "jar";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/PhoenixStoragePlugin.java`
#### Snippet
```java
    PlannerPhase phase
  ) {
    switch (phase) {
      case PHYSICAL:
        return convention.getRules();
```

### SwitchStatementWithTooFewBranches
'switch' statement has only 'default' case
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                default: return null;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "serverInfo";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "sqlQuery";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "properties";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
                for(int number = input.readFieldNumber(this);; number = input.readFieldNumber(this))
                {
                    switch(number)
                    {
                        case 0:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/SchemaUserProtos.java`
#### Snippet
```java
        public static java.lang.String getFieldName(int number)
        {
            switch(number)
            {
                case 1: return "catalogNameFilter";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-mongo/src/main/java/org/apache/drill/exec/store/bson/BsonRecordReader.java`
#### Snippet
```java
    reader.readStartDocument();
    BsonType readBsonType = reader.getCurrentBsonType();
    switch (readBsonType) {
    case DOCUMENT:
      writeToListOrMap(reader, new MapOrListWriterImpl(writer.rootAsMap()), false, null);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaStoragePlugin.java`
#### Snippet
```java
    PlannerPhase phase
  ) {
    switch (phase) {
      case PHYSICAL:
        return ImmutableSet.of(KafkaPushDownFilterIntoScan.INSTANCE);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/format-excel/src/main/java/org/apache/drill/exec/store/excel/ExcelBatchReader.java`
#### Snippet
```java
        Cell dataCell = firstDataRow.getCell(colPosition, Row.MissingCellPolicy.CREATE_NULL_AS_BLANK);

        switch (dataCell.getCellType()) {
          case STRING:
            tempColumnName = cell.getStringCellValue()
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `contrib/storage-elasticsearch/src/main/java/org/apache/drill/exec/store/elasticsearch/ElasticsearchColumnConverterFactory.java`
#### Snippet
```java
  @Override
  public ColumnConverter.ScalarColumnConverter buildScalar(ColumnMetadata readerSchema, ValueWriter writer) {
    switch (readerSchema.type()) {
      case BIT:
        return new ColumnConverter.ScalarColumnConverter(value -> writer.setBoolean((Boolean) value));
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
     */
    public static QueryResultsMode forNumber(int value) {
      switch (value) {
        case 1: return STREAM_FULL;
        default: return null;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `protocol/src/main/java/org/apache/drill/exec/proto/UserProtos.java`
#### Snippet
```java
          while (!done) {
            int tag = input.readTag();
            switch (tag) {
              case 0:
                done = true;
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `logical/src/main/java/org/apache/drill/common/config/LogicalPlanPersistence.java`
#### Snippet
```java
   *
   * @param classpathScan Drill configuration object used to find the packages to scan
   * @return list of classes that implement the interface.
   */
  public static <T> Set<Class<? extends T>> getSubTypes(final ScanResult classpathScan, Class<T> parent) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/annotations/FunctionTemplate.java`
#### Snippet
```java
   *   have both "less_than" and "less than".
   * </p>
   * @return list of function names
   */
  String[] names() default {};
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/registry/FunctionRegistryHolder.java`
#### Snippet
```java
   * Retrieves all functions (holders) associated with all the jars
   * This is read operation, so several users can perform this operation at the same time.
   * @return list of all functions, mapped by their sources
   */
  public Map<String, List<FunctionHolder>> getAllJarsWithFunctionHolders() {
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
   * @param name plugin name
   * @param oldConfig expected config of the doomed plugin
   * @return true if the plugin was removed and closed, false otherwise
   * @throws PluginException for an attempt to remove a system plugin
   */
```

### MismatchedJavadocCode
Method is specified to return 'false' but its return type is not boolean
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginMap.java`
#### Snippet
```java
   * @param name plugin name
   * @param oldConfig expected config of the doomed plugin
   * @return true if the plugin was removed and closed, false otherwise
   * @throws PluginException for an attempt to remove a system plugin
   */
```

### MismatchedJavadocCode
Method is specified to return number but the return type is boolean
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/text/reader/CompliantTextBatchReader.java`
#### Snippet
```java
  /**
   * Generates the next record batch
   * @return number of records in the batch
   */
  @Override
```

### MismatchedJavadocCode
Method is specified to return 'true' but there's no such enum constant in DateCorruptionStatus
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/columnreaders/ParquetRecordReader.java`
#### Snippet
```java
   * in this file, see DRILL-4203.
   *
   * @return true if the dates are corrupted and need to be corrected
   */
  public ParquetReaderUtility.DateCorruptionStatus getDateCorruptionStatus() {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/sig/ConstantExpressionIdentifier.java`
#### Snippet
```java
   *
   * @param e expression to check for constants
   * @return list of expressions that mark boundaries into a constant space
   */
  public static Set<LogicalExpression> getConstantExpressionSet(LogicalExpression e) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/FindFiltersForCollation.java`
#### Snippet
```java
   *
   * @param indexCondition index condition to analyze
   * @return list of output RelFieldCollation
   */
  public Map<Integer, List<RexNode> > analyze(RexNode indexCondition) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/QueueQueryParallelizer.java`
#### Snippet
```java
   * @param nodeResourceMap resources per drillbit.
   * @param nodeLimit permissible node limit.
   * @return list of operators which contain adjusted memory limits.
   */
  private Map<DrillbitEndpoint, List<Pair<PhysicalOperator, Long>>>
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractGroupScanWithMetadata.java`
#### Snippet
```java
   * @param filteredPartitionMetadata list of partition metadata which was pruned
   * @param <T>                       type of metadata to filter
   * @return list with metadata which belongs to pruned partitions
   */
  protected static <T extends BaseMetadata & LocationProvider> Map<Path, T> pruneForPartitions(Map<Path, T> metadataToPrune, List<PartitionMetadata> filteredPartitionMetadata) {
```

### MismatchedJavadocCode
Method is specified to return set but the return type is list
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/ScanLevelProjection.java`
#### Snippet
```java
   * that specified in the SELECT (for an explicit list of columns) or
   * table order (for SELECT * queries).
   * @return the set of output columns in output order
   */
  public List<ColumnProjection> columns() { return outputCols; }
```

### MismatchedJavadocCode
Method is specified to return number but the return type is boolean
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/PriorityQueueCopierWrapper.java`
#### Snippet
```java
     * may be less if this is the last batch.
     *
     * @return the number of rows in the batch, or 0 if no more batches
     * are available
     */
```

### MismatchedJavadocCode
Method is specified to return list but the return type is set
in `exec/java-exec/src/main/java/org/apache/drill/exec/metastore/store/parquet/ParquetMetadataProvider.java`
#### Snippet
```java
   * Returns list of file locations for table.
   *
   * @return list of file locations for table
   */
  Set<Path> getFileSet();
```

### MismatchedJavadocCode
Method is specified to return 'false' but there's no such enum constant in AllocationOutcome
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/Accountant.java`
#### Snippet
```java
   * @param size
   *          The amount of memory to reserve in bytes.
   * @return True if the allocation was successful, false if the allocation failed.
   */
  AllocationOutcome allocateBytes(long size) {
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `metastore/metastore-api/src/main/java/org/apache/drill/metastore/util/TableMetadataUtils.java`
#### Snippet
```java
   * @param columns             set of columns whose statistics should be merged
   * @param statisticsToCollect kinds of statistics that should be collected
   * @return list of merged metadata
   */
  @SuppressWarnings({ "unchecked", "rawtypes" })
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
    // job properly. See DRILL-5510.

    } catch (Throwable e) {
      // Unwrap exception
      Throwable ex = e;
```

### CatchMayIgnoreException
Empty `catch` block
in `logical/src/main/java/org/apache/drill/common/expression/ValueExpressions.java`
#### Snippet
```java
      int a = Integer.parseInt(numStr);
      return new IntExpression(a, ep);
    } catch (Exception e) { }

    try {
```

### CatchMayIgnoreException
Empty `catch` block
in `logical/src/main/java/org/apache/drill/common/expression/ValueExpressions.java`
#### Snippet
```java
      long l = Long.parseLong(numStr);
      return new LongExpression(l, ep);
    } catch (Exception e) { }

    try {
```

### CatchMayIgnoreException
Empty `catch` block
in `logical/src/main/java/org/apache/drill/common/expression/ValueExpressions.java`
#### Snippet
```java
      double d = Double.parseDouble(numStr);
      return new DoubleExpression(d, ep);
    } catch (Exception e) { }

    throw new IllegalArgumentException(String.format("Unable to parse string %s as integer or floating point number.",
```

### CatchMayIgnoreException
Empty `catch` block
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DrillClient.java`
#### Snippet
```java
        connect(endpoints.iterator().next());
        return true;
      } catch (Exception e) {
      }
    }
```

### CatchMayIgnoreException
Empty `catch` block
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/RestQueryRunner.java`
#### Snippet
```java
      try {
        isComplete = webUserConnection.await(TimeUnit.SECONDS.toMillis(1)); //periodically timeout 1 sec to check heap
      } catch (InterruptedException e) {}
      usagePercent = getHeapUsage();
      if (memoryFailureThreshold > 0 && usagePercent > memoryFailureThreshold) {
```

### CatchMayIgnoreException
Empty `catch` block
in `exec/java-exec/src/main/java/org/apache/drill/exec/dotdrill/DotDrillUtil.java`
#### Snippet
```java
            FileStatus[] status = fs.listStatus(new Path(root, name + dotType.getEnding()));
            statuses.addAll(Arrays.asList(status));
          } catch (FileNotFoundException ex) {
          }
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/common/HashPartition.java`
#### Snippet
```java
        currHVVector.getMutator().set(ind, hashCode);   // store the hash value in the new HV column
      }
    } catch(SchemaChangeException sce) {}

    VectorContainer container = new VectorContainer();
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/ImageDirectoryProcessor.java`
#### Snippet
```java
          }
        }
      } catch (XMPException ignored) {
        logger.warn("Error in processing xmp directory : {}", ignored.getMessage());
      }
```

### CatchMayIgnoreException
'catch' parameter named `ignored` is used
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/NamedThreadFactory.java`
#### Snippet
```java
        thread.setPriority(Thread.MAX_PRIORITY);
      }
    } catch (Exception ignored) {
      // Doesn't matter even if failed to set.
      logger.info("ignored exception " + ignored);
```

### CatchMayIgnoreException
Empty `catch` block
in `common/src/main/java/org/apache/drill/common/CatastrophicFailure.java`
#### Snippet
```java
    try {
      Thread.sleep(1000);
    } catch (InterruptedException e2) {
    }
    System.exit(code);
```

### CatchMayIgnoreException
Empty `catch` block
in `common/src/main/java/org/apache/drill/common/exceptions/UserException.java`
#### Snippet
```java
          try { sleep(1_000); } catch (final Exception ex) { /* ignore interruptions */ }
        }
        try { outErr.delete(); } catch (final Exception ex) { } // cleanup - remove err msg file
      }

```

## RuleId[id=IOStreamConstructor]
### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfig.java`
#### Snippet
```java
      //initialize the key manager factory
      // Will throw an exception if the file is not found/accessible.
      InputStream ksStream = new FileInputStream(keyStorePath);
      // A key password CANNOT be null or an empty string.
      if (keyStorePassword.isEmpty()) {
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/ssl/SSLConfig.java`
#### Snippet
```java
          logger.debug("Initializing truststore {}.", getTrustStorePath());
          ts = KeyStore.getInstance(!trustStoreType.isEmpty() ? trustStoreType : KeyStore.getDefaultType());
          InputStream tsStream = new FileInputStream(getTrustStorePath());
          ts.load(tsStream, getTrustStorePassword().toCharArray());
      } else {
```

### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/spill/SpillSet.java`
#### Snippet
```java
      return new CountingInputStream(
                new BufferedInputStream(
                    new FileInputStream(new File(baseDir, fileName))));
    }

```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
  public int getFreeNodeCount() {
    Set<String> free = new HashSet<>( );
    free.addAll( yarnNodes.keySet() );
    free.removeAll( nodesInUse );
    free.removeAll( blacklist );
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/NodeInventory.java`
#### Snippet
```java
  public List<String> getBlacklist() {
    List<String> copy = new ArrayList<>( );
    copy.addAll(blacklist);
    return copy;
  }
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/SchedulerStateImpl.java`
#### Snippet
```java

    List<Task> temp = new ArrayList<>( );
    temp.addAll( allocatingTasks );
    for (Task task : temp) {
      context.setTask(task);
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/store/provider/CachingPersistentStoreProvider.java`
#### Snippet
```java

    // add un-versioned stores
    closeables.addAll(storeCache.values());
    storeCache.clear();

```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/base/filter/FilterPushDownStrategy.java`
#### Snippet
```java
      // Gather unqualified and rewritten predicates
      List<RexNode> remainingPreds = new ArrayList<>();
      remainingPreds.addAll(nonConvertedPreds);
      if (translated.right != null) {
        remainingPreds.addAll(translated.right);
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/cost/DrillRelMdSelectivity.java`
#### Snippet
```java

    List<RexNode> newPredsList = new ArrayList<>();
    newPredsList.addAll(nonRangePredList);

    // for the predicates on same column, combine them into a single conjunct
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/SpilledRuns.java`
#### Snippet
```java
                    VectorContainer container, int mergeRowCount, VectorInitializer allocHelper) {
    List<BatchGroup> allBatches = new LinkedList<>();
    allBatches.addAll(bufferedBatches);
    bufferedBatches.clear();
    allBatches.addAll(spilledRuns);
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/BufferedBatches.java`
#### Snippet
```java
  public List<InputBatch> removeAll() {
    List<InputBatch> batches = new ArrayList<>( );
    batches.addAll(bufferedBatches);
    bufferedBatches.clear();
    return batches;
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/TupleMetadata.java`
#### Snippet
```java
      return WRITER.writeValueAsString(this);
    } catch (JsonProcessingException e) {
      throw new IllegalStateException("Unable to convert tuple metadata into JSON string: " + toString(), e);
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/record/MaterializedField.java`
#### Snippet
```java
        builder
          .append(", subtypes=(")
          .append(type.getSubTypeList().toString())
          .append(")");
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/VariantSchema.java`
#### Snippet
```java
        .append("[")
        .append(getClass().getSimpleName())
        .append(types.toString())
        .append(", simple: ")
        .append(isSimple)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/PrimitiveColumnMetadata.java`
#### Snippet
```java
            ? DateTimeFormatter.ISO_LOCAL_DATE_TIME : DateTimeFormatter.ofPattern(formatValue);
        default:
          throw new IllegalArgumentException("Column is not a date/time type: " + type.toString());
      }
    } catch (IllegalArgumentException e) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/PrimitiveColumnMetadata.java`
#### Snippet
```java
          return Period.parse(value);
        default:
          throw new IllegalArgumentException("Unsupported conversion: " + type.toString());
      }
    } catch (IllegalArgumentException e) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/SingleDictReaderImpl.java`
#### Snippet
```java
        return new Text(String.valueOf(key));
      default:
        String message = String.format("Unknown value %d for key of type %s", key, keyType.getMinorType().toString());
        throw new IllegalArgumentException(message);
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/vector/complex/impl/SingleDictReaderImpl.java`
#### Snippet
```java
        return DateUtility.parseLocalTime(key);
      default:
        String message = String.format("Unknown value %s for key of type %s", key, keyType.getMinorType().toString());
        throw new IllegalArgumentException(message);
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/impl/HierarchicalPrinter.java`
#### Snippet
```java
      out.print("\"");
    } else {
      out.print(value.toString());
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/reader/UnionReaderImpl.java`
#### Snippet
```java
    ObjectReader reader = member(type);
    if (reader == null) {
      throw new IllegalArgumentException("Union does not include type " + type.toString());
    }
    return reader;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/vector/src/main/java/org/apache/drill/exec/vector/accessor/writer/ScalarArrayWriter.java`
#### Snippet
```java
      throw new IllegalArgumentException(
          String.format("Argument must be an array. Column `%s`, value = %s",
              schema().name(), array.toString()));
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/jdbc/src/main/java/org/apache/drill/jdbc/impl/DrillCursor.java`
#### Snippet
```java
      }
      catch (RuntimeException e) {
        throw new SQLException("Unexpected RuntimeException: " + e.toString(), e);
      }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
      }
      LOG.trace("Not supported in this YARN distribution: " + methodLabel + "("
          + buf.toString() + ")");
      CodeSource src = target.getClass().getProtectionDomain().getCodeSource();
      if (src != null) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DoYUtil.java`
#### Snippet
```java
    }
    LOG.trace(
        "Successfully called " + methodLabel + "( " + buf.toString() + ")");

    // Return any return value. Will be null if the method is returns void.
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/client/StatusCommand.java`
#### Snippet
```java
      return;
    }
    System.out.println(label + ": " + object.toString());
  }
}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/core/DrillOnYarnConfig.java`
#### Snippet
```java
        throw new DoyConfigException(
            "Invalid path to Drill-on-YARN configuration file: "
                + url.toString(),
            e);
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
      sep = ", ";
    }
    LOG.info("YARN scheduler resource types: " + buf.toString());
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/TaskState.java`
#### Snippet
```java
    assert false;
    LOG.error(context.task.getLabel() + " - Action " + action
        + " in wrong state: " + toString(),
        new IllegalStateException("Action in wrong state"));
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveUtilities.java`
#### Snippet
```java
    } catch (NumberFormatException e) {
      throw new NumberFormatException(String.format("Hive table property %s value '%s' is non-numeric",
          propertyName, propertyObject.toString()));
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitDecimal9Constant(Decimal9Expression decExpr, StringBuilder sb) throws RuntimeException {
    BigDecimal value = new BigDecimal(decExpr.getIntFromDecimal());
    sb.append((value.setScale(decExpr.getScale())).toString());
    return null;
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitIntervalDayConstant(IntervalDayExpression lExpr, StringBuilder sb) throws RuntimeException {
    sb.append("cast( '");
    sb.append(Period.days(lExpr.getIntervalDay()).plusMillis(lExpr.getIntervalMillis()).toString());
    sb.append("' as INTERVALDAY)");
    return null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitDecimal18Constant(Decimal18Expression decExpr, StringBuilder sb) throws RuntimeException {
    BigDecimal value = new BigDecimal(decExpr.getLongFromDecimal());
    sb.append((value.setScale(decExpr.getScale())).toString());
    return null;
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitIntervalYearConstant(IntervalYearExpression lExpr, StringBuilder sb) throws RuntimeException {
    sb.append("cast( '");
    sb.append(Period.months(lExpr.getIntervalYear()).toString());
    sb.append("' as INTERVALYEAR)");
    return null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/ClassGenerator.java`
#### Snippet
```java

    b._if(obj.eq(JExpr._null()))._then()._throw(JExpr._new(t).arg(JExpr.lit(
        String.format("Failure while loading vector %s with id: %s.", vv.name(), fieldId.toString()))));
    //b.assign(vv, JExpr.cast(retClass, ((JExpression) JExpr.cast(wrapperClass, obj)).invoke(vectorAccess)));
    b.assign(vv, JExpr.cast(retClass, obj));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillFuncHolder.java`
#### Snippet
```java
      MajorType type = ref.getType();
      builder.append(",");
      builder.append(type.getMinorType().toString());
      builder.append("-");
      builder.append(type.getMode().toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/DrillFuncHolder.java`
#### Snippet
```java
      builder.append(type.getMinorType().toString());
      builder.append("-");
      builder.append(type.getMode().toString());
    }
    if (isVarArg() && getParamCount() > 0) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZKClusterCoordinator.java`
#### Snippet
```java
      }
    }
    logger.debug("Online endpoints in ZK are" + runningEndPoints.toString());
    return runningEndPoints;
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    System.out.println(String.format("Empty batch : %d", emptyBatchNum));
    System.out.println(String.format("Schema changes : %d", schemaChangeIdx.size()));
    System.out.println(String.format("Schema change batch index : %s", schemaChangeIdx.toString()));
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
    /* show the header of the batch */
    if (showHeader) {
      System.out.println(getBatchMetaInfo(vcSerializable).toString());

      System.out.println("Schema Information");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
        System.out.println (String.format("name : %s, minor_type : %s, data_mode : %s",
                                          field.getName(),
                                          field.getType().getMinorType().toString(),
                                          field.isNullable() ? "nullable":"non-nullable"
                          ));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/schema/json/jackson/JacksonHelper.java`
#### Snippet
```java
        return null;
      default:
        throw new RuntimeException("Unexpected Field type to return value: " + fieldType.toString());
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillDiagnosticListener.java`
#### Snippet
```java
  public void report(Diagnostic<? extends JavaFileObject> diagnostic) {
    if (diagnostic.getKind() == javax.tools.Diagnostic.Kind.ERROR) {
      String message = diagnostic.toString() + " (" + diagnostic.getCode() + ")";
      logger.error(message);
      Location loc = new Location( //
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/DrillDiagnosticListener.java`
#### Snippet
```java
      throw new RuntimeException(new CompileException(message, loc));
    } else if (logger.isTraceEnabled()) {
      logger.trace(diagnostic.toString() + " (" + diagnostic.getCode() + ")");
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBodyBuilder.java`
#### Snippet
```java
      }

      return sw1.toString() + body + sw2.toString();

    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ClassBodyBuilder.java`
#### Snippet
```java
      }

      return sw1.toString() + body + sw2.toString();

    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
      List<String> columnTypesList = new ArrayList<>(columnTypes.size());
      for (TypeProtos.MajorType columnType : columnTypes) {
        columnTypesList.add(columnType.getMinorType().toString() + "-" + columnType.getMode().toString());
      }
      builder.append(columnTypesList);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
      List<String> columnTypesList = new ArrayList<>(columnTypes.size());
      for (TypeProtos.MajorType columnType : columnTypes) {
        columnTypesList.add(columnType.getMinorType().toString() + "-" + columnType.getMode().toString());
      }
      builder.append(columnTypesList);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Stats.java`
#### Snippet
```java
  @Override
  public String toString() {
    return "Stats [maxCost=" + maxCost +", parallelizationInfo=" + collector.toString() + "]";
  }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/PhysicalPlanCreator.java`
#### Snippet
```java
    } catch (IOException e) {
      plan = null;
      throw new UnsupportedOperationException("Physical plan created failed with error : " + e.toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/visitor/PrelVisualizerVisitor.java`
#### Snippet
```java
          buf.append("null");
        } else {
          buf.append(obj.toString());
        }
      }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculator.java`
#### Snippet
```java
      }

      return sizeSb.toString() + "\n" + batchCountSb.toString() + "\n" + recordCountSb.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculator.java`
#### Snippet
```java
      }

      return sizeSb.toString() + "\n" + batchCountSb.toString() + "\n" + recordCountSb.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculator.java`
#### Snippet
```java
      }

      return sizeSb.toString() + "\n" + batchCountSb.toString() + "\n" + recordCountSb.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculatorImpl.java`
#### Snippet
```java
      return calcVars
        + "\n" + buildPartitionStatSet.makeDebugString()
        + "\n" + hashJoinHelperSb.toString()
        + "\n" + hashTableSb.toString();
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinMemoryCalculatorImpl.java`
#### Snippet
```java
        + "\n" + buildPartitionStatSet.makeDebugString()
        + "\n" + hashJoinHelperSb.toString()
        + "\n" + hashTableSb.toString();
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/scan/project/AbstractUnresolvedColumn.java`
#### Snippet
```java
      buf
        .append(", incol=")
        .append(inCol.toString());
    }
    return buf.append("]").toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
          );
          if (outputMinorType == null) {
            throw new DrillRuntimeException("Type mismatch between " + leftField.getType().getMinorType().toString() +
                " on the left side and " + rightField.getType().getMinorType().toString() +
                " on the right side in column " + index + " of UNION ALL");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
          if (outputMinorType == null) {
            throw new DrillRuntimeException("Type mismatch between " + leftField.getType().getMinorType().toString() +
                " on the left side and " + rightField.getType().getMinorType().toString() +
                " on the right side in column " + index + " of UNION ALL");
          }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java

      default:
        throw new UnsupportedOperationException("Unknown selection vector mode: " + svMode.toString());
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/partitionsender/PartitionerTemplate.java`
#### Snippet
```java

      default:
        throw new UnsupportedOperationException("Unknown selection vector mode: " + svMode.toString());
    }
  }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/RequestedColumnImpl.java`
#### Snippet
```java
        .append(name());
    if (qualifier != null) {
      buf.append(qualifier.toString());
    }
    return buf.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/resultSet/project/Qualifier.java`
#### Snippet
```java
    }
    if (members != null) {
      buf.append(members.toString());
    }
    return buf.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBPushLimitIntoScan.java`
#### Snippet
```java
      }
      call.transformTo(newChild);
      logger.debug("pushLimitIntoRowKeyJoin: Pushed limit on left side of Join " + join.toString());
    } catch (Exception e) {
      logger.warn("pushLimitIntoRowKeyJoin: Exception while trying limit pushdown!", e);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
    } catch (Exception e) {
      throw new DrillRuntimeException("Error getting table: " + tablePath.toString() + (indexDesc == null ? "" : (", " +
          "IndexDesc: " + indexDesc.toString())), e);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/memory/base/src/main/java/org/apache/drill/exec/memory/BaseAllocator.java`
#### Snippet
```java
    if (allocated > 0) {
      throw new IllegalStateException(
          String.format("Memory was leaked by query. Memory leaked: (%d)\n%s", allocated, toString()));
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `exec/memory/base/src/main/java/io/netty/buffer/DrillBuf.java`
#### Snippet
```java

  public void print(StringBuilder sb, int indent, Verbosity verbosity) {
    BaseAllocator.indent(sb, indent).append(toString());

    if (BaseAllocator.DEBUG && !isEmpty && verbosity.includeHistoricalLog) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `common/src/main/java/org/apache/drill/common/scanner/persistence/AnnotationDescriptor.java`
#### Snippet
```java
      throw new IllegalStateException(String.format(
          "Expected a single value for the attribute named %s but found %d (%s)",
          attributeName, values.size(), values.toString()));
    }
    return values.isEmpty() ? defaultValue : values.get(0);
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'Map\>' may not contain keys of type 'HivePartition'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveMetadataProvider.java`
#### Snippet
```java
   */
  private List<LogicalInputSplit> getPartitionInputSplits(final HivePartition partition) {
    if (partitionInputSplitMap.containsKey(partition)) {
      return partitionInputSplitMap.get(partition);
    }
```

### SuspiciousMethodCalls
'Map\>' may not contain keys of type 'HivePartition'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveMetadataProvider.java`
#### Snippet
```java
  private List<LogicalInputSplit> getPartitionInputSplits(final HivePartition partition) {
    if (partitionInputSplitMap.containsKey(partition)) {
      return partitionInputSplitMap.get(partition);
    }

```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'RexInputRef'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/RexSeparator.java`
#### Snippet
```java
    protected boolean inputRefToPush(RexInputRef inputRef) {
      //this class will based on the schemaPath to decide what to push
      if (markMap.containsKey(inputRef) || inAcceptedPath) {
        return true;
      }
```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'RexCall'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/index/RexSeparator.java`
#### Snippet
```java
      boolean oldValue = inAcceptedPath;
      try {
        if (markMap.containsKey(call)) {
          inAcceptedPath = true;

```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'RexCall'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/FieldsReWriterUtil.java`
#### Snippet
```java
    @Override
    public RexNode visitCall(final RexCall call) {
      Integer index = mapper.get(call);
      if (index != null) {
        return new RexInputRef(index, call.getType());
```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'RexInputRef'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/FieldsReWriterUtil.java`
#### Snippet
```java
    @Override
    public RexNode visitInputRef(RexInputRef inputRef) {
      Integer index = mapper.get(inputRef);
      if (index != null) {
        return new RexInputRef(index, inputRef.getType());
```

### SuspiciousMethodCalls
'Map' may not contain keys of type 'RelNode'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/NumberingRelWriter.java`
#### Snippet
```java

    StringBuilder s = new StringBuilder();
    OpId id = ids.get(rel);
    if (id != null) {
      s.append(String.format("%02d-%02d", id.fragmentId, id.opId));
```

### SuspiciousMethodCalls
'Map' may not contain keys of type ''
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/metadata/MetadataControllerBatch.java`
#### Snippet
```java

    columnStatistics.asMap().forEach((fieldName, statisticsHolders) ->
        resultingStats.put(SchemaPath.parseFromString(fieldName), new ColumnStatistics<>(statisticsHolders, columnTypes.get(fieldName))));
    return resultingStats;
  }
```

### SuspiciousMethodCalls
Suspicious call to 'Map.get()'
in `metastore/iceberg-metastore/src/main/java/org/apache/drill/metastore/iceberg/transform/OutputDataTransformer.java`
#### Snippet
```java
        .filter(column -> record.getField(column) != null)
        .collect(Collectors.toMap(
          unitSetters::get,
          record::getField,
          (o, n) -> n)))
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `DrillbitManager` may be 'static'
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/AbstractDrillbitScheduler.java`
#### Snippet
```java
   */

  public class DrillbitManager extends AbstractTaskManager {
    /**
     * Allow only one concurrent container request by default to ensure that the
```

### InnerClassMayBeStatic
Inner class `HiveDrillNativeParquetDrillFileSystemManager` may be 'static'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveDrillNativeParquetScanBatchCreator.java`
#### Snippet
```java
   * Creates only non-tracking file systems.
   */
  private class HiveDrillNativeParquetDrillFileSystemManager extends AbstractDrillFileSystemManager {

    private final Map<Path, DrillFileSystem> fileSystems;
```

### InnerClassMayBeStatic
Inner class `SendRuntimeFilterAsyncListen` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataTunnel.java`
#### Snippet
```java
  }

  private class SendRuntimeFilterAsyncListen extends ListeningCommand<BitData.AckWithCredit, DataClientConnection, RpcType, MessageLite> {
    final RuntimeFilterWritable runtimeFilter;

```

### InnerClassMayBeStatic
Inner class `ProxyCloseHandler` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/data/DataServer.java`
#### Snippet
```java
  }

  private class ProxyCloseHandler implements GenericFutureListener<ChannelFuture> {

    private volatile GenericFutureListener<ChannelFuture> handler;
```

### InnerClassMayBeStatic
Inner class `ProxyCloseHandler` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlServer.java`
#### Snippet
```java
  }

  private class ProxyCloseHandler implements GenericFutureListener<ChannelFuture> {

    private volatile GenericFutureListener<ChannelFuture>  handler;
```

### InnerClassMayBeStatic
Inner class `CustomFuture` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/rpc/control/ControlTunnel.java`
#### Snippet
```java
   *          The type of message that will be returned.
   */
  public class CustomFuture<RECEIVE> {

    private final CustomSerDe<RECEIVE> serde;
```

### InnerClassMayBeStatic
Inner class `TargetPhysicalOperatorVisitor` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterRouter.java`
#### Snippet
```java
  }

  private abstract class TargetPhysicalOperatorVisitor<T, X, E extends Throwable> extends AbstractPhysicalVisitor<T, X, E> {

    protected Exchange sendingExchange;
```

### InnerClassMayBeStatic
Inner class `StateEvent` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/QueryStateProcessor.java`
#### Snippet
```java
  }

  private class StateEvent {
    final QueryState newState;
    final Exception exception;
```

### InnerClassMayBeStatic
Inner class `InnerIter` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/foreman/QueryManager.java`
#### Snippet
```java
  }

  private class InnerIter implements IntObjectPredicate<FragmentData> {
    private final MajorFragmentProfile.Builder builder;

```

### InnerClassMayBeStatic
Inner class `FragmentEvent` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/fragment/FragmentExecutor.java`
#### Snippet
```java
  }

  private class FragmentEvent {
    private final EventType type;
    private final FragmentHandle handle;
```

### InnerClassMayBeStatic
Inner class `LeaseHolder` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/zk/ZkDistributedSemaphore.java`
#### Snippet
```java
  }

  private class LeaseHolder implements DistributedLease{
    Lease lease;

```

### InnerClassMayBeStatic
Inner class `LocalSemaphore` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/coord/local/LocalClusterCoordinator.java`
#### Snippet
```java
  }

  public class LocalSemaphore implements DistributedSemaphore {
    private final Semaphore semaphore;
    private final LocalLease localLease = new LocalLease();
```

### InnerClassMayBeStatic
Inner class `LocalSyncableOutputStream` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java
  }

  public class LocalSyncableOutputStream extends OutputStream implements Syncable {
    private final FileOutputStream fos;
    private final BufferedOutputStream output;
```

### InnerClassMayBeStatic
Inner class `LocalInputStream` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/LocalSyncableFileSystem.java`
#### Snippet
```java
  }

  public class LocalInputStream extends InputStream implements Seekable, PositionedReadable, ByteBufferReadable {

    private BufferedInputStream input;
```

### InnerClassMayBeStatic
Inner class `SubDirectoryIterator` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/SubDirectoryList.java`
#### Snippet
```java
  }

  private class SubDirectoryIterator implements Iterator<String> {

    final Iterator<FileStatus> fileStatusIterator;
```

### InnerClassMayBeStatic
Inner class `SystemSchema` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/sys/SystemTablePlugin.java`
#### Snippet
```java
   * This class defines a namespace for {@link org.apache.drill.exec.store.sys.SystemTable}s
   */
  private class SystemSchema extends AbstractSchema {

    private final Map<String, StaticDrillTable> tables;
```

### InnerClassMayBeStatic
Inner class `UnionJsonConverter` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/JsonRecordWriter.java`
#### Snippet
```java
  }

  public class UnionJsonConverter extends FieldConverter {

    public UnionJsonConverter(int fieldId, String fieldName, FieldReader reader) {
```

### InnerClassMayBeStatic
Inner class `HadoopBytesInput` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/ColumnDataReader.java`
#### Snippet
```java
  }

  public class HadoopBytesInput extends BytesInput{

    private final byte[] pageBytes;
```

### InnerClassMayBeStatic
Inner class `FileStatusWork` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/schedule/BlockMapBuilder.java`
#### Snippet
```java
  }

  private class FileStatusWork implements FileWork{
    private FileStatus status;

```

### InnerClassMayBeStatic
Inner class `BatchMetaInfo` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/client/DumpCat.java`
#### Snippet
```java
   * Contains : # of rows, # of selected rows, data size (byte #).
   */
  private class BatchMetaInfo {
    private long rows = 0;
    private long selectedRows = 0;
```

### InnerClassMayBeStatic
Inner class `BatchStats` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/RecordBatchMemoryManager.java`
#### Snippet
```java
  private int numInputs = 1;

  private class BatchStats {
    /**
     * operator metric stats
```

### InnerClassMayBeStatic
Inner class `VectorSchemaBuilder` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/record/selection/SelectionVector4Builder.java`
#### Snippet
```java
   */

  private class VectorSchemaBuilder {
  }

```

### InnerClassMayBeStatic
Inner class `Log` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/LogsResources.java`
#### Snippet
```java

  @XmlRootElement
  public class Log implements Comparable<Log> {

    private final String name;
```

### InnerClassMayBeStatic
Inner class `LogContent` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/LogsResources.java`
#### Snippet
```java

  @XmlRootElement
  public class LogContent {
    private final String name;
    private final Collection<String> lines;
```

### InnerClassMayBeStatic
Inner class `Iter` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/DrillConfigIterator.java`
#### Snippet
```java
  }

  public class Iter implements Iterator<OptionValue>{

    Iterator<Entry<String, ConfigValue>> entries;
```

### InnerClassMayBeStatic
Inner class `FragmentTblTxt` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
  }

  private class FragmentTblTxt {
    static final String MINOR_FRAGMENT = "Minor Fragment";
    static final String HOSTNAME = "Hostname";
```

### InnerClassMayBeStatic
Inner class `OverviewTblTxt` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
  }

  private class OverviewTblTxt {
    static final String MAJOR_FRAGMENT = "Major Fragment";
    static final String MINOR_FRAGMENTS_REPORTING = "Minor Fragments Reporting";
```

### InnerClassMayBeStatic
Inner class `OverviewTblTooltip` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
  }

  private class OverviewTblTooltip {
    static final String MAJOR_FRAGMENT = "Major fragment ID seen in the visual plan";
    static final String MINOR_FRAGMENTS_REPORTING = "Number of minor fragments started";
```

### InnerClassMayBeStatic
Inner class `FragmentTblTooltip` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/FragmentWrapper.java`
#### Snippet
```java
  }

  private class FragmentTblTooltip {
    static final String MINOR_FRAGMENT = "Minor Fragment ID";
    static final String HOSTNAME = "Host on which the fragment ran";
```

### InnerClassMayBeStatic
Inner class `OperatorTblTxt` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
  }

  private class OperatorTblTxt {
    static final String MINOR_FRAGMENT = "Minor Fragment";
    static final String HOSTNAME = "Hostname";
```

### InnerClassMayBeStatic
Inner class `OverviewTblTxt` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
  }

  private class OverviewTblTxt {
    static final String OPERATOR_ID = "Operator ID";
    static final String TYPE_OF_OPERATOR = "Type";
```

### InnerClassMayBeStatic
Inner class `OverviewTblTooltip` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
  }

  private class OverviewTblTooltip {
    static final String OPERATOR_ID = "Operator ID";
    static final String TYPE_OF_OPERATOR = "Operator Type";
```

### InnerClassMayBeStatic
Inner class `OperatorTblTooltip` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/profile/OperatorWrapper.java`
#### Snippet
```java
  }

  private class OperatorTblTooltip {
    static final String MINOR_FRAGMENT = "Operator's Minor Fragment";
    static final String HOSTNAME = "Host on which the minor fragment ran";
```

### InnerClassMayBeStatic
Inner class `ClassBytesCacheLoader` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/ByteCodeLoader.java`
#### Snippet
```java
      .expireAfterWrite(10, TimeUnit.MINUTES).build(new ClassBytesCacheLoader());

  private class ClassBytesCacheLoader extends CacheLoader<String, byte[]> {
    @Override
    public byte[] load(String path) throws ClassTransformationException, IOException {
```

### InnerClassMayBeStatic
Inner class `GeneratedClassEntry` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/compile/CodeCompiler.java`
#### Snippet
```java
  }

  private class GeneratedClassEntry {
    private final Class<?> clazz;

```

### InnerClassMayBeStatic
Inner class `JarManager` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/CreateFunctionHandler.java`
#### Snippet
```java
   * Is responsible for validation, copying and deletion actions.
   */
  private class JarManager {

    private final String binaryName;
```

### InnerClassMayBeStatic
Inner class `ConvertedRelNode` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/DefaultSqlHandler.java`
#### Snippet
```java
  }

  protected class ConvertedRelNode {
    private final RelNode relNode;
    private final RelDataType validatedRowType;
```

### InnerClassMayBeStatic
Inner class `OpState` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/partition/FindPartitionConditions.java`
#### Snippet
```java
   * encountered directory filters.
   */
  public class OpState {
    private SqlOperator sqlOperator;
    private List<RexNode> children = Lists.newArrayList();
```

### InnerClassMayBeStatic
Inner class `ExchangeFragmentPair` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/fragment/Fragment.java`
#### Snippet
```java
  }

  public class ExchangeFragmentPair {
    private Exchange exchange;
    private Fragment fragmentXchgTo;
```

### InnerClassMayBeStatic
Inner class `WriteTraitPull` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/WriterPrule.java`
#### Snippet
```java
  }

  private class WriteTraitPull extends SubsetTransformer<DrillWriterRelBase, RuntimeException> {

    public WriteTraitPull(RelOptRuleCall call) {
```

### InnerClassMayBeStatic
Inner class `Subset` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/FilterPrule.java`
#### Snippet
```java


  private class Subset extends SubsetTransformer<DrillFilterRel, RuntimeException> {

    public Subset(RelOptRuleCall call) {
```

### InnerClassMayBeStatic
Inner class `BuildInfo` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinHelper.java`
#### Snippet
```java
  }

  public class BuildInfo {
    // List of links. Logically it helps maintain a linked list of records with the same key value
    private SelectionVector4 links;
```

### InnerClassMayBeStatic
Inner class `BatchStatusWrappper` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/union/UnionAllRecordBatch.java`
#### Snippet
```java
  }

  private class BatchStatusWrappper {
    boolean prefetched;
    final RecordBatch batch;
```

### InnerClassMayBeStatic
Inner class `ClassifierResult` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/flatten/FlattenRecordBatch.java`
#### Snippet
```java
  private static final String EMPTY_STRING = "";

  private class ClassifierResult {
    public List<String> outputNames;

```

### InnerClassMayBeStatic
Inner class `Node` may be 'static'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/mergereceiver/MergingRecordBatch.java`
#### Snippet
```java
   * as a wrapper for the priority queue.
   */
  public class Node {
    public int batchId;      // incoming batch
    public int valueIndex;   // value within the batch
```

### InnerClassMayBeStatic
Inner class `DecodePathinExpr` may be 'static'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/planner/index/MapRDBIndexDescriptor.java`
#### Snippet
```java
   * the end of returned list of expressions from parseExpressions.
   */
  private class DecodePathinExpr extends CloneVisitor {
    Set<SchemaPath> schemaPathSet = Sets.newHashSet();

```

### InnerClassMayBeStatic
Inner class `SetExceptionProcedure` may be 'static'
in `exec/rpc/src/main/java/org/apache/drill/exec/rpc/RequestIdMap.java`
#### Snippet
```java
  }

  private class SetExceptionProcedure implements IntObjectProcedure<RpcOutcome<?>> {
    final RpcException exception;

```

### InnerClassMayBeStatic
Inner class `TransferResult` may be 'static'
in `exec/memory/base/src/main/java/io/netty/buffer/DrillBuf.java`
#### Snippet
```java
   * The outcome of a Transfer.
   */
  public class TransferResult {

    /**
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `completionMutex`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  public boolean waitForCompletion() {
    start();
    synchronized (completionMutex) {
      try {
        completionMutex.wait();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `completionMutex`
in `drill-yarn/src/main/java/org/apache/drill/yarn/appMaster/ClusterControllerImpl.java`
#### Snippet
```java
  private void terminate(State state) {
    this.state = state;
    synchronized (completionMutex) {
      completionMutex.notify();
    }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `rfQueue`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
      while ((joinMjId2rfNumber == null || !joinMjId2rfNumber.isEmpty() ) && running.get()) {
        RuntimeFilterWritable toAggregate = null;
        synchronized (rfQueue) {
          try {
            toAggregate = rfQueue.poll();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `rfQueue`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
          // Set running to false and cleanup pending RFW in queue. This will make sure producer
          // thread is also indicated to stop and queue is cleaned up properly in failure cases
          synchronized (rfQueue) {
            running.set(false);
          }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `rfQueue`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
    running.set(false);
    if (asyncAggregateWorker != null) {
      synchronized (rfQueue) {
        rfQueue.notify();
      }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `rfQueue`
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/filter/RuntimeFilterSink.java`
#### Snippet
```java
      joinMjId2Stopwatch.put(joinMjId, stopwatch);
    }
    synchronized (rfQueue) {
      if (!running.get()) {
        runtimeFilterWritable.close();
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'valueArg1 == null' covered by subsequent condition '!(valueArg1 instanceof IntExpression)'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/CompareFunctionsProcessor.java`
#### Snippet
```java

        if (!(nameArg instanceof SchemaPath)
            || (valueArg1 == null) || !(valueArg1 instanceof IntExpression)
            || (valueArg2 == null) || !(valueArg2 instanceof IntExpression)) {
          return false;
```

### ConditionCoveredByFurtherCondition
Condition 'valueArg2 == null' covered by subsequent condition '!(valueArg2 instanceof IntExpression)'
in `contrib/storage-hbase/src/main/java/org/apache/drill/exec/store/hbase/CompareFunctionsProcessor.java`
#### Snippet
```java
        if (!(nameArg instanceof SchemaPath)
            || (valueArg1 == null) || !(valueArg1 instanceof IntExpression)
            || (valueArg2 == null) || !(valueArg2 instanceof IntExpression)) {
          return false;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'ex instanceof MetaException' covered by subsequent condition 'ex instanceof TTransportException'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveStoragePlugin.java`
#### Snippet
```java
      while (true) {
        // Case for failing on an invalid cached connection
        if (ex instanceof MetaException ||
            // Case for a timed-out impersonated connection, and
            // an invalid non-secure connection used to get security
```

### ConditionCoveredByFurtherCondition
Condition 'holder instanceof BitHolder' covered by subsequent condition 'holder instanceof NullableBitHolder'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java

    private Trivalent isBitOn(ValueHolder holder) {
      Preconditions.checkArgument(holder instanceof BitHolder || holder instanceof NullableBitHolder,
          "Input does not have type of BitHolder or NullableBitHolder.");

```

### ConditionCoveredByFurtherCondition
Condition 'condHolder instanceof BitHolder' covered by subsequent condition 'condHolder instanceof NullableBitHolder'
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
      ValueHolder condHolder = ifExpr.ifCondition.condition.accept(this, inIndex);

      Preconditions.checkArgument (condHolder instanceof BitHolder || condHolder instanceof NullableBitHolder,
          "IfExpression's condition does not have type of BitHolder or NullableBitHolder.");

```

### ConditionCoveredByFurtherCondition
Condition 'cause != null' covered by subsequent condition 'cause instanceof ExecutionSetupException'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/StoragePluginRegistryImpl.java`
#### Snippet
```java
  private ExecutionSetupException translateException(PluginException e) {
    Throwable cause = e.getCause();
    if (cause != null && cause instanceof ExecutionSetupException) {
      return (ExecutionSetupException) cause;
    }
```

### ConditionCoveredByFurtherCondition
Condition 'param != null' covered by subsequent condition 'param instanceof String'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/dfs/FormatPluginOptionsDescriptor.java`
#### Snippet
```java
    private Object paramValue(int i) {
      Object param = formatParamsValues.get(i);
      if (param != null && param instanceof String) {
        // normalize Java literals, ex: \t, \n, \r
        param = StringEscapeUtils.unescapeJava((String) param);
```

### ConditionCoveredByFurtherCondition
Condition 'value != null' covered by subsequent condition 'value instanceof Integer'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/ColumnDef.java`
#### Snippet
```java
      if (mockCol.properties != null) {
        Object value = mockCol.properties.get("nulls");
        if (value != null  && value instanceof Integer) {
          nullablePercent = (Integer) value;
        }
```

### ConditionCoveredByFurtherCondition
Condition 'rm != null' covered by subsequent condition 'rm instanceof DynamicResourceManager'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java

      ThrottledResourceManager throttledRM = null;
      if (rm != null && rm instanceof DynamicResourceManager) {
        DynamicResourceManager dynamicRM = (DynamicResourceManager) rm;
        rm = dynamicRM.activeRM();
```

### ConditionCoveredByFurtherCondition
Condition 'rm != null' covered by subsequent condition 'rm instanceof ThrottledResourceManager'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java
        rm = dynamicRM.activeRM();
      }
      if (rm != null && rm instanceof ThrottledResourceManager) {
        throttledRM = (ThrottledResourceManager) rm;
      }
```

### ConditionCoveredByFurtherCondition
Condition 'queue == null' covered by subsequent condition '!(queue instanceof DistributedQueryQueue)'
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/rest/DrillRoot.java`
#### Snippet
```java
      }
      QueryQueue queue = throttledRM.queue();
      if (queue == null || !(queue instanceof DistributedQueryQueue)) {
        return new QueueInfo(null);
      }
```

### ConditionCoveredByFurtherCondition
Condition 'other instanceof DrillJoinRelBase' covered by subsequent condition 'other instanceof DrillAggregateRelBase'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/sql/handlers/FindLimit0Visitor.java`
#### Snippet
```java
  @Override
  public RelNode visit(RelNode other) {
    if (other instanceof DrillJoinRelBase ||
        other instanceof DrillAggregateRelBase ||
        other instanceof DrillSetOpRel) {
```

### ConditionCoveredByFurtherCondition
Condition 'curRel != null' covered by subsequent condition 'curRel instanceof DrillScanRel'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillPushRowKeyJoinToScanRule.java`
#### Snippet
```java
        curRel != null ? curRel.getRowType().getFieldNames().get(curIndex) : "??");
    // Get the primary-key col name from the scan and match with the column being referenced.
    if (curRel != null && curRel instanceof DrillScanRel) {
      if (((DrillScanRel) curRel).getGroupScan() instanceof DbGroupScan) {
        DbGroupScan dbGroupScan = (DbGroupScan) ((DrillScanRel) curRel).getGroupScan();
```

### ConditionCoveredByFurtherCondition
Condition 'sqlAggFunction instanceof SqlAvgAggFunction' covered by subsequent condition 'sqlAggFunction instanceof SqlSumAggFunction'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillReduceAggregatesRule.java`
#### Snippet
```java
    for (AggregateCall call : aggCallList) {
      SqlAggFunction sqlAggFunction = DrillCalciteWrapperUtility.extractSqlOperatorFromWrapper(call.getAggregation());
      if (sqlAggFunction instanceof SqlAvgAggFunction
          || sqlAggFunction instanceof SqlSumAggFunction) {
        return true;
```

### ConditionCoveredByFurtherCondition
Condition 'parent != null' covered by subsequent condition 'parent instanceof LateralJoinPrel'
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/physical/explain/NumberingRelWriter.java`
#### Snippet
```java
  private String getDependentSrcOp(UnnestPrel unnest) {
    Prel parent = this.getRegisteredPrel(unnest.getParentClass());
    if (parent != null && parent instanceof LateralJoinPrel) {
      OpId id = ids.get(parent);
      return String.format(" [srcOp=%02d-%02d] ", id.fragmentId, id.opId);
```

### ConditionCoveredByFurtherCondition
Condition 'subScan != null' covered by subsequent condition 'subScan instanceof DbSubScan'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/RowKeyJoinBatchCreator.java`
#### Snippet
```java
    RowKeyJoinBatch rjBatch = new RowKeyJoinBatch(config, context, children.get(0), children.get(1));
    SubScan subScan = config.getSubScanForRowKeyJoin();
    if (subScan != null
        && subScan instanceof DbSubScan
        && ((DbSubScan)subScan).isRestrictedSubScan()) {
```

### ConditionCoveredByFurtherCondition
Condition 'subScan != null' covered by subsequent condition 'subScan instanceof DbSubScan'
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/join/HashJoinBatchCreator.java`
#### Snippet
```java
    HashJoinBatch hjBatch = new HashJoinBatch(config, context, children.get(0), children.get(1));
    SubScan subScan = config.getSubScanForRowKeyJoin();
    if (subScan != null
        && subScan instanceof DbSubScan
        && ((DbSubScan)subScan).isRestrictedSubScan()) {
```

### ConditionCoveredByFurtherCondition
Condition 'obj != null' covered by subsequent condition 'obj instanceof MapRDBTableCache.Key'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBTableCache.java`
#### Snippet
```java
      if (obj == this) {
        return true;
      } else if (obj != null && obj instanceof MapRDBTableCache.Key) {
        MapRDBTableCache.Key that = (MapRDBTableCache.Key) obj;
        return isEqual(this.path, that.path)
```

### ConditionCoveredByFurtherCondition
Condition 'o == null' covered by subsequent condition '!(o instanceof PhoenixStoragePluginConfig)'
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/PhoenixStoragePluginConfig.java`
#### Snippet
```java
      return true;
    }
    if (o == null || !(o instanceof PhoenixStoragePluginConfig)) {
      return false;
    }
```

### ConditionCoveredByFurtherCondition
Condition 'o == null' covered by subsequent condition '!(o instanceof LogFormatField)'
in `contrib/format-log/src/main/java/org/apache/drill/exec/store/log/LogFormatField.java`
#### Snippet
```java
  @Override
  public boolean equals(Object o) {
    if (o == null || ! (o instanceof LogFormatField)) {
      return false;
    }
```

### ConditionCoveredByFurtherCondition
Condition 'other == null' covered by subsequent condition '!(other instanceof Map.Entry)'
in `common/src/main/java/org/apache/drill/common/collections/ImmutableEntry.java`
#### Snippet
```java
      return true;
    }
    if (other == null || !(other instanceof  Map.Entry)) {
      return false;
    }
```

## RuleId[id=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/MockStorageEngine.java`
#### Snippet
```java

    private Table getDirectTable(String name) {
      Pattern p = Pattern.compile("(\\w+)_(\\d+)(k|m)?", Pattern.CASE_INSENSITIVE);
      Matcher m = p.matcher(name);
      if (! m.matches()) {
```

## RuleId[id=CloneDeclaresCloneNotSupported]
### CloneDeclaresCloneNotSupported
`clone()` does not declare 'CloneNotSupportedException'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/EnumerableIntermediatePrel.java`
#### Snippet
```java

  @Override
  protected Object clone() {
    return copy(getTraitSet(), getInputs());
  }
```

### CloneDeclaresCloneNotSupported
`clone()` does not declare 'CloneNotSupportedException'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/enumerable/plan/VertexDrel.java`
#### Snippet
```java

  @Override
  protected Object clone() {
    return copy(getTraitSet(), getInputs());
  }
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java

        if (firstPatternByte != firstInByte) {
          continue;
        } else {
          final byte secondInByte = drillBuf.getByte(start + idx + 1);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/impl/SqlPatternContainsMatcher.java`
#### Snippet
```java

        if (firstPatternByte != inputByte) {
          continue;
        } else {
          final byte secondInByte = drillBuf.getByte(start + idx + 1);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/expr/fn/interpreter/InterpreterEvaluator.java`
#### Snippet
```java
            }
          } else { // do nothing with non-inject fields here
            continue;
          }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/BaseRawBatchBuffer.java`
#### Snippet
```java
      } catch (IOException e) {
        context.getExecutorState().fail(e);
        continue;
      } catch (InterruptedException e) {
        context.getExecutorState().fail(e);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/BaseRawBatchBuffer.java`
#### Snippet
```java
        // keep the state that the thread is interrupted
        Thread.currentThread().interrupt();
        continue;
      } finally {
        if (batch != null && batch.getBody() != null) {
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
        } catch (InterruptedException e) {
          logger.warn("Interrupted while waiting for spooling thread to exit");
          continue;
        }
      }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/work/batch/SpoolingRawBatchBuffer.java`
#### Snippet
```java
          duration = Math.max(1, duration * 2);
          if (duration < 60000) {
            continue;
          } else {
            throw e;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/easy/json/reader/BaseJsonProcessor.java`
#### Snippet
```java
          return JsonExceptionProcessingState.END_OF_STREAM;
        }
       continue;
       }
    }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/ScanBatch.java`
#### Snippet
```java
      if (recordCount == 0) {
        if (toContinueIter) {
          continue; // Skip to next loop iteration if reader returns 0 row and has same schema.
        } else {
          // Return NONE if recordCount == 0 && !isNewSchema
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/impl/xsort/MSortTemplate.java`
#### Snippet
```java
      int newBatch = this.vector4.get(i) >>> 16;
      if (newBatch == batch) {
        continue;
      } else if (newBatch == batch + 1) {
        runStarts.add(i);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `exec/java-exec/src/main/java/org/apache/drill/exec/resolver/TypeCastRules.java`
#### Snippet
```java

      if (isCastable(next, result) && resultCastCost <= nextCastCost && resultCastCost < Float.POSITIVE_INFINITY) {
        continue;
      } else if (isCastable(result, next) && nextCastCost <= resultCastCost && nextCastCost < Float.POSITIVE_INFINITY) {
        result = next;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `contrib/format-image/src/main/java/org/apache/drill/exec/store/image/GenericMetadataReader.java`
#### Snippet
```java
          // Nothing needs to be done
        }
        continue;
      }
    }
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy fields 'bytes' and 'length'
in `exec/vector/src/main/java/org/apache/drill/exec/util/Text.java`
#### Snippet
```java

  /** Construct from another text. */
  public Text(Text utf8) {
    set(utf8);
  }
```

### CopyConstructorMissesField
Copy constructor does not copy field 'parentTuple'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/AbstractMapColumnMetadata.java`
#### Snippet
```java
  }

  public AbstractMapColumnMetadata(AbstractMapColumnMetadata from) {
    super(from);
    schema = from.schema.copy();
```

### CopyConstructorMissesField
Copy constructor does not copy field 'properties'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/AbstractPropertied.java`
#### Snippet
```java
  protected AbstractPropertied() { }

  protected AbstractPropertied(AbstractPropertied from) {
    setProperties(from.properties);
  }
```

### CopyConstructorMissesField
Copy constructor does not copy field 'forUnknownSchema'
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/PrimitiveColumnMetadata.java`
#### Snippet
```java
  }

  public PrimitiveColumnMetadata(PrimitiveColumnMetadata from) {
    super(from);
  }
```

### CopyConstructorMissesField
Copy constructor does not copy field 'affinities'
in `contrib/storage-druid/src/main/java/org/apache/drill/exec/store/druid/DruidGroupScan.java`
#### Snippet
```java
   * @param that The DruidGroupScan to clone
   */
  private DruidGroupScan(DruidGroupScan that) {
    super(that);
    this.columns = that.columns;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'hashCode'
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/HttpGroupScan.java`
#### Snippet
```java
   * Copies the group scan during many stages of Calcite operation.
   */
  public HttpGroupScan(HttpGroupScan that) {
    super(that);
    this.httpScanSpec = that.httpScanSpec;
```

### CopyConstructorMissesField
Copy constructor does not copy fields 'mappings' and 'inputSplits'
in `contrib/storage-hive/core/src/main/java/org/apache/drill/exec/store/hive/HiveScan.java`
#### Snippet
```java
  }

  public HiveScan(final HiveScan that) {
    super(that);
    this.columns = that.columns;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'rowGroupInfos'
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/parquet/AbstractParquetGroupScan.java`
#### Snippet
```java

  // immutable copy constructor
  protected AbstractParquetGroupScan(AbstractParquetGroupScan that) {
    super(that);

```

### CopyConstructorMissesField
Copy constructor does not copy 4 fields
in `exec/java-exec/src/main/java/org/apache/drill/exec/physical/base/AbstractBase.java`
#### Snippet
```java
  }

  public AbstractBase(AbstractBase that) {
    Preconditions.checkNotNull(that, "Unable to clone: source is null.");
    this.userName = that.userName;
```

### CopyConstructorMissesField
Copy constructor does not copy fields 'endpointFragmentMapping', 'watch' and 'tableMetadata'
in `contrib/format-maprdb/src/main/java/org/apache/drill/exec/store/mapr/db/MapRDBGroupScan.java`
#### Snippet
```java
  private TableMetadata tableMetadata;

  public MapRDBGroupScan(MapRDBGroupScan that) {
    super(that);
    this.columns = that.columns;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'hashCode'
in `contrib/storage-phoenix/src/main/java/org/apache/drill/exec/store/phoenix/PhoenixGroupScan.java`
#### Snippet
```java
  }

  public PhoenixGroupScan(PhoenixGroupScan scan) {
    super(scan);
    this.sql = scan.sql;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'affinities'
in `contrib/storage-kudu/src/main/java/org/apache/drill/exec/store/kudu/KuduGroupScan.java`
#### Snippet
```java
   * @param that The KuduGroupScan to clone
   */
  private KuduGroupScan(KuduGroupScan that) {
    super(that);
    this.kuduStoragePlugin = that.kuduStoragePlugin;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'affinities'
in `contrib/storage-kafka/src/main/java/org/apache/drill/exec/store/kafka/KafkaGroupScan.java`
#### Snippet
```java
  }

  public KafkaGroupScan(KafkaGroupScan that) {
    super(that);
    this.kafkaStoragePlugin = that.kafkaStoragePlugin;
```

## RuleId[id=RegExpUnnecessaryNonCapturingGroup]
### RegExpUnnecessaryNonCapturingGroup
Unnecessary non-capturing group `(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*)`
in `contrib/storage-http/src/main/java/org/apache/drill/exec/store/http/paginator/HeaderIndexPaginator.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(HeaderIndexPaginator.class);
  private static final Pattern URL_REGEX = Pattern.compile("(https?:\\/\\/(?:www\\.)?[-a-zA-Z0-9@:%._\\+~#=]{1,256}\\.[a-zA-Z0-9()]{1,6}\\b(?:[-a-zA-Z0-9()@:%_\\+.~#?&\\/=]*))");

  private final String nextPageParam;
```

## RuleId[id=ThrowableNotThrown]
### ThrowableNotThrown
Result of `fail()` not thrown
in `exec/memory/base/src/main/java/io/netty/buffer/PooledByteBufAllocatorL.java`
#### Snippet
```java
              maxCapacity);
          if (!(buf instanceof PooledUnsafeDirectByteBuf)) {
            fail();
          }

```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `exec/vector/src/main/java/org/apache/drill/exec/record/metadata/AbstractPropertied.java`
#### Snippet
```java

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/server/options/OptionValue.java`
#### Snippet
```java

  @Override
  public boolean equals(Object obj) {
    if (!equalsIgnoreType(obj)) {
      return false;
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `exec/java-exec/src/main/java/org/apache/drill/exec/planner/logical/DrillTranslatableTable.java`
#### Snippet
```java

  @Override
  public boolean equals(Object obj) {
    return drillTable.equals(obj);
  }
```

## RuleId[id=BigDecimalMethodWithoutRoundingCalled]
### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.setScale()' called without a rounding mode argument
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitDecimal9Constant(Decimal9Expression decExpr, StringBuilder sb) throws RuntimeException {
    BigDecimal value = new BigDecimal(decExpr.getIntFromDecimal());
    sb.append((value.setScale(decExpr.getScale())).toString());
    return null;
  }
```

### BigDecimalMethodWithoutRoundingCalled
'BigDecimal.setScale()' called without a rounding mode argument
in `logical/src/main/java/org/apache/drill/common/expression/ExpressionStringBuilder.java`
#### Snippet
```java
  public Void visitDecimal18Constant(Decimal18Expression decExpr, StringBuilder sb) throws RuntimeException {
    BigDecimal value = new BigDecimal(decExpr.getLongFromDecimal());
    sb.append((value.setScale(decExpr.getScale())).toString());
    return null;
  }
```

## RuleId[id=NumericOverflow]
### NumericOverflow
Numeric overflow in expression
in `exec/java-exec/src/main/java/org/apache/drill/exec/store/mock/DateGen.java`
#### Snippet
```java

  private final int ONE_DAY = 24 * 60 * 60 * 1000;
  private final int ONE_YEAR = ONE_DAY * 365;

  private long baseTime;
```

