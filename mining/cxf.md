# cxf 
 
# Bad smells
I found 9073 bad smells with 573 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| ReturnNull | 1930 | false |
| UnnecessaryFullyQualifiedName | 953 | false |
| BoundedWildcard | 615 | false |
| AssignmentToMethodParameter | 597 | false |
| DataFlowIssue | 401 | false |
| PublicFieldAccessedInSynchronizedContext | 358 | false |
| ConstantValue | 346 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 264 | false |
| UnnecessarySuperQualifier | 250 | false |
| ZeroLengthArrayInitialization | 205 | false |
| UnnecessaryBoxing | 140 | false |
| SizeReplaceableByIsEmpty | 132 | true |
| SystemOutErr | 114 | false |
| NonProtectedConstructorInAbstractClass | 113 | true |
| SynchronizeOnThis | 107 | false |
| StringEqualsEmptyString | 107 | false |
| UnnecessaryUnboxing | 102 | false |
| RedundantArrayCreation | 93 | true |
| ThrowablePrintStackTrace | 86 | false |
| NonShortCircuitBoolean | 84 | false |
| EmptyMethod | 84 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 77 | false |
| CommentedOutCode | 69 | false |
| InnerClassMayBeStatic | 56 | true |
| UnnecessaryToStringCall | 54 | true |
| DynamicRegexReplaceableByCompiledPattern | 53 | false |
| StringOperationCanBeSimplified | 50 | false |
| RegExpSimplifiable | 49 | false |
| IgnoreResultOfCall | 46 | false |
| RedundantFieldInitialization | 43 | false |
| MarkedForRemoval | 40 | false |
| UnusedAssignment | 40 | false |
| CharsetObjectCanBeUsed | 40 | false |
| Java9CollectionFactory | 40 | false |
| SynchronizeOnNonFinalField | 39 | false |
| CodeBlock2Expr | 37 | true |
| NonSerializableFieldInSerializableClass | 37 | false |
| TypeParameterExtendsObject | 35 | false |
| StringBufferReplaceableByString | 34 | false |
| NonFinalFieldOfException | 32 | false |
| RedundantCollectionOperation | 32 | false |
| Convert2Lambda | 32 | false |
| ClassNameSameAsAncestorName | 31 | false |
| IndexOfReplaceableByContains | 31 | false |
| RedundantClassCall | 30 | false |
| ExceptionNameDoesntEndWithException | 29 | false |
| WhileCanBeForeach | 28 | false |
| RegExpRedundantNestedCharacterClass | 28 | false |
| AssignmentToForLoopParameter | 27 | false |
| UnnecessaryContinue | 27 | false |
| RegExpRedundantEscape | 24 | false |
| RedundantMethodOverride | 22 | false |
| ReplaceAssignmentWithOperatorAssignment | 22 | false |
| RedundantSuppression | 22 | false |
| UnnecessaryLocalVariable | 22 | true |
| DuplicateExpressions | 22 | false |
| UseOfPropertiesAsHashtable | 22 | false |
| UseBulkOperation | 21 | false |
| Anonymous2MethodRef | 21 | false |
| DuplicateBranchesInSwitch | 20 | false |
| ConditionCoveredByFurtherCondition | 20 | false |
| ProtectedMemberInFinalClass | 19 | true |
| ReplaceNullCheck | 18 | false |
| Java8MapApi | 18 | false |
| UnnecessarySemicolon | 17 | false |
| ObsoleteCollection | 17 | false |
| NestedAssignment | 16 | false |
| ToArrayCallWithZeroLengthArrayArgument | 15 | true |
| EqualsBetweenInconvertibleTypes | 15 | false |
| SlowListContainsAll | 15 | false |
| InstanceofCatchParameter | 15 | false |
| Convert2MethodRef | 15 | false |
| DuplicateThrows | 14 | false |
| UnnecessaryStringEscape | 14 | true |
| RedundantImplements | 14 | false |
| IOResource | 14 | false |
| CastCanBeRemovedNarrowingVariableType | 13 | false |
| UnnecessaryReturn | 12 | true |
| IfStatementMissingBreakInLoop | 12 | false |
| ManualArrayToCollectionCopy | 11 | false |
| ManualMinMaxCalculation | 11 | false |
| ThrowFromFinallyBlock | 11 | false |
| WrapperTypeMayBePrimitive | 9 | false |
| AssignmentToStaticFieldFromInstanceMethod | 9 | false |
| AnonymousHasLambdaAlternative | 9 | false |
| StringConcatenationInsideStringBufferAppend | 9 | false |
| MismatchedCollectionQueryUpdate | 8 | false |
| SimplifyStreamApiCallChains | 8 | false |
| CaughtExceptionImmediatelyRethrown | 8 | false |
| UseCompareMethod | 8 | false |
| WaitNotInLoop | 8 | false |
| DefaultAnnotationParam | 8 | false |
| IfStatementWithIdenticalBranches | 8 | false |
| SuspiciousMethodCalls | 8 | false |
| UnnecessaryCallToStringValueOf | 7 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 7 | false |
| BusyWait | 7 | false |
| SuspiciousSystemArraycopy | 7 | false |
| RedundantOperationOnEmptyContainer | 7 | false |
| UnnecessaryQualifierForThis | 6 | false |
| CopyConstructorMissesField | 6 | false |
| ExtendsAnnotation | 5 | false |
| StringRepeatCanBeUsed | 5 | false |
| SuspiciousToArrayCall | 5 | false |
| WhileLoopSpinsOnField | 5 | false |
| MethodOverridesStaticMethod | 5 | false |
| CatchMayIgnoreException | 5 | false |
| OverwrittenKey | 4 | false |
| MisspelledEquals | 4 | false |
| DeprecatedIsStillUsed | 4 | false |
| MismatchedJavadocCode | 4 | false |
| Finalize | 3 | false |
| StaticInitializerReferencesSubClass | 3 | false |
| FinalStaticMethod | 3 | false |
| UNUSED_IMPORT | 3 | false |
| JavaReflectionMemberAccess | 3 | false |
| OptionalUsedAsFieldOrParameterType | 3 | false |
| LoopStatementsThatDontLoop | 3 | false |
| AbstractClassNeverImplemented | 3 | false |
| UtilityClassWithoutPrivateConstructor | 3 | true |
| RegExpSingleCharAlternation | 3 | false |
| EqualsWhichDoesntCheckParameterClass | 3 | false |
| KeySetIterationMayUseEntrySet | 2 | false |
| SuspiciousInvocationHandlerImplementation | 2 | false |
| OptionalAssignedToNull | 2 | false |
| SimplifiableBooleanExpression | 2 | false |
| RedundantExplicitClose | 2 | false |
| HtmlWrongAttributeValue | 2 | false |
| StringEqualsCharSequence | 2 | false |
| MethodOverloadsParentMethod | 2 | false |
| InstantiatingObjectToGetClassObject | 2 | false |
| RedundantEscapeInRegexReplacement | 2 | false |
| NonStrictComparisonCanBeEquality | 2 | true |
| StringEquality | 2 | false |
| StaticCallOnSubclass | 2 | false |
| OptionalIsPresent | 2 | false |
| RedundantLengthCheck | 2 | false |
| NestedSynchronizedStatement | 2 | false |
| ThrowableNotThrown | 2 | false |
| MismatchedArrayReadWrite | 1 | false |
| PointlessArithmeticExpression | 1 | false |
| IntegerMultiplicationImplicitCastToLong | 1 | false |
| PointlessBitwiseExpression | 1 | false |
| CallToStringConcatCanBeReplacedByOperator | 1 | false |
| CastConflictsWithInstanceof | 1 | false |
| CollectionContainsUrl | 1 | false |
| ThreadRun | 1 | false |
| UnnecessaryInitCause | 1 | false |
| ComparatorMethodParameterNotUsed | 1 | false |
| PrimitiveArrayArgumentToVariableArgMethod | 1 | false |
| PointlessBooleanExpression | 1 | true |
| ConditionalBreakInInfiniteLoop | 1 | false |
| ManualArrayCopy | 1 | false |
| EmptySynchronizedStatement | 1 | false |
| RegExpEmptyAlternationBranch | 1 | false |
| SimplifyOptionalCallChains | 1 | false |
| InfiniteLoopStatement | 1 | false |
| ArrayEquality | 1 | false |
| FuseStreamOperations | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| SetReplaceableByEnumSet | 1 | false |
## RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new JettyHTTPServerEngine\[portMap.values().size()\]'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
        // To avoid the CurrentModificationException,
        // do not use portMap.values directly
        JettyHTTPServerEngine[] engines = portMap.values().toArray(new JettyHTTPServerEngine[portMap.values().size()]);
        for (JettyHTTPServerEngine engine : engines) {
            engine.shutdown();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new UndertowHTTPServerEngine\[portMap.values().size()\]'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
        // do not use portMap.values directly
        UndertowHTTPServerEngine[] engines =
            portMap.values().toArray(new UndertowHTTPServerEngine[portMap.values().size()]);
        for (UndertowHTTPServerEngine engine : engines) {
            engine.shutdown();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Source\[schemaSourcesMap2.size()\]'
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                        b != null ? b : BusFactory.getThreadDefaultBus(false)));
                schema = factory.newSchema(schemaSourcesMap2.values()
                                           .toArray(new Source[schemaSourcesMap2.size()]));


```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Principal\[subject.getPrincipals().size()\]'
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractSecurityContextInInterceptor.java`
#### Snippet
```java

    protected Principal getPrincipal(Principal originalPrincipal, Subject subject) {
        Principal[] ps = subject.getPrincipals().toArray(new Principal[subject.getPrincipals().size()]);
        if (ps != null && ps.length > 0 
            && !DefaultSecurityContext.isGroupPrincipal(ps[0])) {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new NettyHttpServerEngine\[portMap.values().size()\]'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngineFactory.java`
#### Snippet
```java
        // To avoid the CurrentModificationException,
        // do not use portMap.values directly
        NettyHttpServerEngine[] engines = portMap.values().toArray(new NettyHttpServerEngine[portMap.values().size()]);
        for (NettyHttpServerEngine engine : engines) {
            engine.shutdown();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[attributes.keySet().size()\]'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpSession.java`
#### Snippet
```java
            return null;
        }
        return attributes.keySet().toArray(
                new String[attributes.keySet().size()]);
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Map.Entry\[nsMap.size()\]'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                @SuppressWarnings("unchecked")
                final Map.Entry<String, String>[] namespaces
                    = nsMap.entrySet().toArray(new Map.Entry[nsMap.size()]);
                //OK. we have extra namespaces.  We'll need to wrapper the reader
                //with a new one that will fake extra namespace events
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Book\[books.size()\]'
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
        }
        Books b = new Books();
        b.setBooks(books.values().toArray(new Book[books.size()]));
        return b;
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new CorbaStreamable\[this.arguments.size()\]'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaMessage.java`
#### Snippet
```java

    public CorbaStreamable[] getStreamableArguments() {
        return this.arguments.toArray(new CorbaStreamable[this.arguments.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new UnionMember\[members.size()\]'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                   getTypeCodeName(unionType.getName()),
                                   discTC,
                                   members.values().toArray(
                                       new UnionMember[members.size()]));
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[filtersProps.keySet().size()\]'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
            LOG.config("get Filter " + propFile + "::" + (String)i.next());
        }
        return filtersProps.keySet().toArray(new String[filtersProps.keySet().size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ModelMBeanOperationInfo\[operations.values().size()\]'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanInfoSupporter.java`
#### Snippet
```java

        ModelMBeanOperationInfo[] ops =
            operations.values().toArray(new ModelMBeanOperationInfo[operations.values().size()]);

        ModelMBeanAttributeInfo[] atts =
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ModelMBeanAttributeInfo\[attributes.values().size()\]'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanInfoSupporter.java`
#### Snippet
```java

        ModelMBeanAttributeInfo[] atts =
            attributes.values().toArray(new ModelMBeanAttributeInfo[attributes.values().size()]);

        ModelMBeanConstructorInfo[] cons =
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ModelMBeanConstructorInfo\[constructors.values().size()\]'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanInfoSupporter.java`
#### Snippet
```java

        ModelMBeanConstructorInfo[] cons =
            constructors.values().toArray(new ModelMBeanConstructorInfo[constructors.values().size()]);

        ModelMBeanNotificationInfo[] notifs =
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ModelMBeanNotificationInfo\[notifications.values().size()\]'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanInfoSupporter.java`
#### Snippet
```java

        ModelMBeanNotificationInfo[] notifs =
            notifications.values().toArray(new ModelMBeanNotificationInfo[notifications.values().size()]);

        return new ModelMBeanInfoSupport("javax.management.modelmbean.ModelMBeanInfo",
```

## RuleId[ruleID=Finalize]
### Finalize
'finalize()' should not be overridden
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    }
    @SuppressWarnings("deprecation")
    protected void finalize() throws Throwable {
        close();
        super.finalize();
```

### Finalize
'finalize()' should not be overridden
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("deprecation")
    protected void finalize() throws Throwable {
        close();
        super.finalize();
```

### Finalize
'finalize()' should not be overridden
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxy.java`
#### Snippet
```java
    @SuppressWarnings("deprecation")
    @Override
    protected void finalize() throws Throwable {
        close();
        super.finalize();
```

## RuleId[ruleID=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
            return -1L; // not declared
        }
        Long l;
        if (o instanceof Long) {
            l = (Long) o;
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/SamlTokenInterceptor.java`
#### Snippet
```java
                        assertTokens(message, SPConstants.SAML_TOKEN, signed);

                        Integer key = WSConstants.ST_UNSIGNED;
                        if (signed) {
                            key = WSConstants.ST_SIGNED;
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            if (value != null) {
                try {
                    Long len = Long.valueOf(value);
                    return len <= 0;
                } catch (NumberFormatException ex) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStoreClientCloseable.java`
#### Snippet
```java
        new Thread(() -> {
            try {
                final Integer id = Integer.valueOf(lastEventId);
                final Builder builder = getSse().newEventBuilder();

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
            public void run() {
                try {
                    final Integer id = Integer.valueOf(lastEventId);
                    final OutboundSseEvent.Builder builder = sse.newEventBuilder();

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
            public void run() {
                try {
                    final Integer id = Integer.valueOf(lastEventId);
                    final OutboundSseEvent.Builder builder = sse.newEventBuilder();

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore2.java`
#### Snippet
```java
            public void run() {
                try {
                    final Integer id = Integer.valueOf(lastEventId);
                    final OutboundSseEvent.Builder builder = sse.newEventBuilder();

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore2.java`
#### Snippet
```java
            public void run() {
                try {
                    final Integer id = Integer.valueOf(lastEventId);
                    final OutboundSseEvent.Builder builder = sse.newEventBuilder();

```

### WrapperTypeMayBePrimitive
Type may be primitive
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
        case TCKind._tk_char:
            // A char is mapped to a byte, we need it as a character
            Byte byteValue = Byte.valueOf(data);
            // for values < 0 + 256
            // This means that we can directly write out the chars in the normal
```

## RuleId[ruleID=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `childrenDestinations` are written to, but never read
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPublisher.java`
#### Snippet
```java
            if (topic != null && !topic.isEmpty()) {
                int size = topic.size();
                ActiveMQTopic[] childrenDestinations = new ActiveMQTopic[size];
                for (int i = 0; i < size; i++) {
                    childrenDestinations[i] = topicConverter.toActiveMQTopic(topic.get(i));
```

## RuleId[ruleID=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * 60 * 1000` can be replaced with '60 \* 1000'
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/CXFWorkAdapter.java`
#### Snippet
```java
public class CXFWorkAdapter implements WorkListener {

    public static final long DEFAULT_START_TIME_OUT = 1 * 60 * 1000; // 1 minute

    private static final Logger LOG = LogUtils.getL7dLogger(CXFWorkAdapter.class);
```

## RuleId[ruleID=MarkedForRemoval]
### MarkedForRemoval
'com.sun.javadoc.DocErrorReporter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
    }

    public static boolean validOptions(String[][] options, DocErrorReporter reporter) {
        boolean foundTagOption = false;
        for (int i = 0; i < options.length; i++) {
```

### MarkedForRemoval
'com.sun.javadoc.DocErrorReporter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
            if ("-dumpJavaDocFile".equals(opt[0])) {
                if (foundTagOption) {
                    reporter.printError("Only one -dumpJavaDocFile option allowed.");
                    return false;
                }
```

### MarkedForRemoval
'com.sun.javadoc.DocErrorReporter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        }
        if (!foundTagOption) {
            reporter.printError("Usage: -dumpJavaDocFile theFileToDumpJavaDocForLaterUse...");
        }
        return foundTagOption;
```

### MarkedForRemoval
'com.sun.javadoc.RootDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
    }

    public static boolean start(RootDoc root) throws IOException {
        String dumpFileName = readOptions(root.options());
        OutputStream os = Files.newOutputStream(Paths.get(dumpFileName));
```

### MarkedForRemoval
'com.sun.javadoc.RootDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java

    public static boolean start(RootDoc root) throws IOException {
        String dumpFileName = readOptions(root.options());
        OutputStream os = Files.newOutputStream(Paths.get(dumpFileName));
        Properties javaDocMap = new Properties();
```

### MarkedForRemoval
'com.sun.javadoc.ClassDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        OutputStream os = Files.newOutputStream(Paths.get(dumpFileName));
        Properties javaDocMap = new Properties();
        for (ClassDoc classDoc : root.classes()) {
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
```

### MarkedForRemoval
'com.sun.javadoc.RootDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        OutputStream os = Files.newOutputStream(Paths.get(dumpFileName));
        Properties javaDocMap = new Properties();
        for (ClassDoc classDoc : root.classes()) {
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
```

### MarkedForRemoval
'com.sun.javadoc.Doc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        Properties javaDocMap = new Properties();
        for (ClassDoc classDoc : root.classes()) {
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
```

### MarkedForRemoval
'com.sun.javadoc.MethodDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        for (ClassDoc classDoc : root.classes()) {
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
```

### MarkedForRemoval
'com.sun.javadoc.ClassDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
        for (ClassDoc classDoc : root.classes()) {
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
```

### MarkedForRemoval
'com.sun.javadoc.ProgramElementDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
```

### MarkedForRemoval
'com.sun.javadoc.Doc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
            javaDocMap.put(classDoc.toString(), classDoc.commentText());
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
```

### MarkedForRemoval
'com.sun.javadoc.ParamTag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
```

### MarkedForRemoval
'com.sun.javadoc.ExecutableMemberDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
            for (MethodDoc method : classDoc.methods()) {
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
```

### MarkedForRemoval
'com.sun.javadoc.Parameter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
                        if (parameters[i].name().equals(paramTag.parameterName())) {
```

### MarkedForRemoval
'com.sun.javadoc.ExecutableMemberDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                javaDocMap.put(method.qualifiedName(), method.commentText());
                for (ParamTag paramTag : method.paramTags()) {
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
                        if (parameters[i].name().equals(paramTag.parameterName())) {
```

### MarkedForRemoval
'com.sun.javadoc.Parameter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
                        if (parameters[i].name().equals(paramTag.parameterName())) {
                            javaDocMap.put(method.qualifiedName() + ".paramCommentTag." + i,
                                   paramTag.parameterComment());
```

### MarkedForRemoval
'com.sun.javadoc.ParamTag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    Parameter[] parameters = method.parameters();
                    for (int i = 0; i < parameters.length; ++i) {
                        if (parameters[i].name().equals(paramTag.parameterName())) {
                            javaDocMap.put(method.qualifiedName() + ".paramCommentTag." + i,
                                   paramTag.parameterComment());
```

### MarkedForRemoval
'com.sun.javadoc.ProgramElementDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    for (int i = 0; i < parameters.length; ++i) {
                        if (parameters[i].name().equals(paramTag.parameterName())) {
                            javaDocMap.put(method.qualifiedName() + ".paramCommentTag." + i,
                                   paramTag.parameterComment());
                        }
```

### MarkedForRemoval
'com.sun.javadoc.ParamTag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                        if (parameters[i].name().equals(paramTag.parameterName())) {
                            javaDocMap.put(method.qualifiedName() + ".paramCommentTag." + i,
                                   paramTag.parameterComment());
                        }
                    }
```

### MarkedForRemoval
'com.sun.javadoc.Tag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    }
                }
                Tag[] retTags = method.tags("return");
                if (retTags != null && retTags.length == 1) {
                    Tag retTag = method.tags("return")[0];
```

### MarkedForRemoval
'com.sun.javadoc.Doc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    }
                }
                Tag[] retTags = method.tags("return");
                if (retTags != null && retTags.length == 1) {
                    Tag retTag = method.tags("return")[0];
```

### MarkedForRemoval
'com.sun.javadoc.Tag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                Tag[] retTags = method.tags("return");
                if (retTags != null && retTags.length == 1) {
                    Tag retTag = method.tags("return")[0];
                    javaDocMap.put(method.qualifiedName() + "." + "returnCommentTag",
                                   retTag.text());
```

### MarkedForRemoval
'com.sun.javadoc.Doc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                Tag[] retTags = method.tags("return");
                if (retTags != null && retTags.length == 1) {
                    Tag retTag = method.tags("return")[0];
                    javaDocMap.put(method.qualifiedName() + "." + "returnCommentTag",
                                   retTag.text());
```

### MarkedForRemoval
'com.sun.javadoc.ProgramElementDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                if (retTags != null && retTags.length == 1) {
                    Tag retTag = method.tags("return")[0];
                    javaDocMap.put(method.qualifiedName() + "." + "returnCommentTag",
                                   retTag.text());
                }
```

### MarkedForRemoval
'com.sun.javadoc.Tag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
                    Tag retTag = method.tags("return")[0];
                    javaDocMap.put(method.qualifiedName() + "." + "returnCommentTag",
                                   retTag.text());
                }
            }
```

### MarkedForRemoval
'com.sun.javadoc.ClassDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import java.util.Properties;

import com.sun.javadoc.ClassDoc;
import com.sun.javadoc.DocErrorReporter;
import com.sun.javadoc.MethodDoc;
```

### MarkedForRemoval
'com.sun.javadoc.DocErrorReporter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java

import com.sun.javadoc.ClassDoc;
import com.sun.javadoc.DocErrorReporter;
import com.sun.javadoc.MethodDoc;
import com.sun.javadoc.ParamTag;
```

### MarkedForRemoval
'com.sun.javadoc.MethodDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import com.sun.javadoc.ClassDoc;
import com.sun.javadoc.DocErrorReporter;
import com.sun.javadoc.MethodDoc;
import com.sun.javadoc.ParamTag;
import com.sun.javadoc.Parameter;
```

### MarkedForRemoval
'com.sun.javadoc.ParamTag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import com.sun.javadoc.DocErrorReporter;
import com.sun.javadoc.MethodDoc;
import com.sun.javadoc.ParamTag;
import com.sun.javadoc.Parameter;
import com.sun.javadoc.RootDoc;
```

### MarkedForRemoval
'com.sun.javadoc.Parameter' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import com.sun.javadoc.MethodDoc;
import com.sun.javadoc.ParamTag;
import com.sun.javadoc.Parameter;
import com.sun.javadoc.RootDoc;
import com.sun.javadoc.Tag;
```

### MarkedForRemoval
'com.sun.javadoc.RootDoc' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import com.sun.javadoc.ParamTag;
import com.sun.javadoc.Parameter;
import com.sun.javadoc.RootDoc;
import com.sun.javadoc.Tag;

```

### MarkedForRemoval
'com.sun.javadoc.Tag' is deprecated and marked for removal
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/DumpJavaDoc.java`
#### Snippet
```java
import com.sun.javadoc.Parameter;
import com.sun.javadoc.RootDoc;
import com.sun.javadoc.Tag;


```

### MarkedForRemoval
'setAuthnContextClassRef(java.lang.String)' is deprecated and marked for removal
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlpRequestComponentBuilder.java`
#### Snippet
```java
        }
        AuthnContextClassRef authnCtxClassRef = requestedAuthnCtxClassRefBuilder.buildObject();
        authnCtxClassRef.setAuthnContextClassRef(authnCtxClassRefValue);

        return authnCtxClassRef;
```

### MarkedForRemoval
'getAudienceURI()' is deprecated and marked for removal
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
                    boolean matchFound = false;
                    for (org.opensaml.saml.saml2.core.Audience audience : audienceRestriction.getAudiences()) {
                        if (appliesTo.equals(audience.getAudienceURI())) {
                            matchFound = true;
                            oneMatchFound = true;
```

### MarkedForRemoval
'getAudienceURI()' is deprecated and marked for removal
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
                if (audienceRestriction.getAudiences() != null) {
                    for (org.opensaml.saml.saml2.core.Audience audience : audienceRestriction.getAudiences()) {
                        if (appliesTo.equals(audience.getAudienceURI())) {
                            return true;
                        }
```

### MarkedForRemoval
'getUri()' is deprecated and marked for removal
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
                if (restrCondition.getAudiences() != null) {
                    for (Audience audience : restrCondition.getAudiences()) {
                        if (appliesTo.equals(audience.getUri())) {
                            return true;
                        }
```

### MarkedForRemoval
'getUri()' is deprecated and marked for removal
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
                : assertion.getSaml1().getConditions().getAudienceRestrictionConditions()) {
                for (org.opensaml.saml.saml1.core.Audience audience : restriction.getAudiences()) {
                    addresses.add(audience.getUri());
                }
            }
```

### MarkedForRemoval
'getAudienceURI()' is deprecated and marked for removal
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
                : assertion.getSaml2().getConditions().getAudienceRestrictions()) {
                for (org.opensaml.saml.saml2.core.Audience audience : restriction.getAudiences()) {
                    addresses.add(audience.getAudienceURI());
                }
            }
```

### MarkedForRemoval
'getAudienceURI()' is deprecated and marked for removal
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/saml/SamlOAuthValidator.java`
#### Snippet
```java
            List<Audience> audiences = ar.getAudiences();
            for (Audience a : audiences) {
                if (absoluteAddress.equals(a.getAudienceURI())) {
                    return;
                }
```

## RuleId[ruleID=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/helpers/NSStack.java`
#### Snippet
```java
            ListIterator<NSDecl> lsIterator = scope.listIterator();

            while (lsIterator.hasNext()) {
                NSDecl nsd = lsIterator.next();

```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/helpers/NSStack.java`
#### Snippet
```java
            ListIterator<NSDecl> lsIterator = scope.listIterator();

            while (lsIterator.hasNext()) {
                NSDecl nsd = lsIterator.next();

```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
        public boolean containsKey(Object key) {
            Iterator<Attachment> it = collection.iterator();
            while (it.hasNext()) {
                Attachment at = it.next();
                if (key.equals(at.getId())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
        public DataHandler get(Object key) {
            Iterator<Attachment> it = collection.iterator();
            while (it.hasNext()) {
                Attachment at = it.next();
                if (key.equals(at.getId())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
        public boolean containsValue(Object value) {
            Iterator<Attachment> it = collection.iterator();
            while (it.hasNext()) {
                Attachment at = it.next();
                if (value.equals(at.getDataHandler())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
        public DataHandler remove(Object key) {
            Iterator<Attachment> it = collection.iterator();
            while (it.hasNext()) {
                Attachment at = it.next();
                if (key.equals(at.getId())) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
            List<ApplicationContext> inactiveApplicationContexts = new ArrayList<>();
            Iterator<ApplicationContext> it = appContexts.iterator();
            while (it.hasNext()) {
                ApplicationContext c = it.next();
                if (c instanceof ConfigurableApplicationContext
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetInterceptor.java`
#### Snippet
```java
        // if its already configured for the endpoint.
        Iterator<Interceptor<? extends Message>> iterator = outMessage.getInterceptorChain().iterator();
        while (iterator.hasNext()) {
            Interceptor<? extends Message> inInterceptor = iterator.next();
            if (!inInterceptor.getClass().equals(StaxOutInterceptor.class)
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
                if (sm != null) {
                    Iterator<?> iter = list.iterator();
                    while (iter.hasNext()) {
                        Header header = (Header) iter.next();
                        if (header.getDirection() != Header.Direction.DIRECTION_IN
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
                if (sm != null) {
                    Iterator<?> iter = list.iterator();
                    while (iter.hasNext()) {
                        sm.getHeaders().add((Header) iter.next());
                    }
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        if (bindingExtensors != null) {
            Iterator<ExtensibilityElement> extensionElements = bindingExtensors.iterator();
            while (extensionElements.hasNext()) {
                ExtensibilityElement ext = extensionElements.next();
                if (ext instanceof UnknownExtensibilityElement && Boolean.TRUE.equals(ext.getRequired())
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        if (null != bindingOperations) {
            Iterator<BindingOperationInfo> iterator = bindingOperations.iterator();
            while (iterator.hasNext()) {
                BindingOperationInfo operationInfo = iterator.next();
                BindingMessageInfo inputInfo = operationInfo.getInput();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
                if (null != faults) {
                    Iterator<BindingFaultInfo> faultIterator = faults.iterator();
                    while (faultIterator.hasNext()) {
                        BindingFaultInfo faultInfo = faultIterator.next();

```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapInInterceptor.java`
#### Snippet
```java
            EncoderDecoder codec = null;
            Iterator<Header> iter = headers.iterator();
            while (iter.hasNext()) {
                Object node = iter.next().getObject();
                if (node instanceof Element) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                QName invalidCardinalityQName = null;
                Iterator<Header> iter = header.iterator();
                while (iter.hasNext()) {
                    Header hdr = iter.next();
                    if (hdr.getObject() instanceof Element) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/jwt/DefaultJWTClaimsProvider.java`
#### Snippet
```java
        if (retrievedClaims != null) {
            Iterator<ProcessedClaim> claimIterator = retrievedClaims.iterator();
            while (claimIterator.hasNext()) {
                ProcessedClaim claim = claimIterator.next();
                if (claim.getClaimType() != null && claim.getValues() != null && !claim.getValues().isEmpty()) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
        if (typesElement != null) {
            Iterator<?> ite = typesElement.getExtensibilityElements().iterator();
            while (ite.hasNext()) {
                Object obj = ite.next();
                if (obj instanceof Schema) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaObjectReferenceHelper.java`
#### Snippet
```java
            // recursivly check imports
            Iterator<?> importLists = wsdlDef.getImports().values().iterator();
            while (importLists.hasNext()) {
                List<?> imports = (List<?>) importLists.next();
                for (java.lang.Object imp : imports) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
    protected FaultInfo getFaultInfo(OperationInfo opInfo, QName faultName) {
        Iterator<FaultInfo> faults = opInfo.getFaults().iterator();
        while (faults.hasNext()) {
            FaultInfo fault = faults.next();
            MessagePartInfo partInfo = fault.getMessageParts().get(0);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
    protected MessagePartInfo getFaultMessagePartInfo(OperationInfo opInfo, QName faultName) {
        Iterator<FaultInfo> faults = opInfo.getFaults().iterator();
        while (faults.hasNext()) {
            FaultInfo fault = faults.next();
            MessagePartInfo partInfo = fault.getMessageParts().get(0);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/InvocationHandlerFactory.java`
#### Snippet
```java
        p.load(url.openStream());
        Iterator<Object> keys = p.keySet().iterator();
        while (keys.hasNext()) {
            String key = (String)keys.next();
            handlersMap.put(Long.valueOf(key), p.getProperty(key));
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
            Iterator<SchemaInfo> it = serviceInfo.getSchemas().iterator();
            while (it.hasNext()) {
                XmlSchema schema = it.next().getSchema();
                return isAttributeFormQualified(schema, uri);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
            Iterator<SchemaInfo> it = serviceInfo.getSchemas().iterator();
            while (it.hasNext()) {
                XmlSchema schema = it.next().getSchema();
                return isElementFormQualified(schema, uri);
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
        Properties filtersProps = getProperties(parent, propFile);
        Iterator<Object> i = filtersProps.keySet().iterator();
        while (i.hasNext()) {
            LOG.config("get Filter " + propFile + "::" + (String)i.next());
        }
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
        @SuppressWarnings("rawtypes")
        Iterator iter = mcs.iterator();
        while (iter.hasNext()) {
            Object obj = iter.next();
            if (!(obj instanceof ManagedConnectionImpl)) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
        synchronized (listeners) {
            Iterator<ConnectionEventListener> iterator = listeners.iterator();
            while (iterator.hasNext()) {
                sendEventToListener(iterator.next(), coEvent);
            }
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/RPCInInterceptor.java`
#### Snippet
```java
        boolean hasNext = true;
        Iterator<MessagePartInfo> itr = msg.getMessageParts().iterator();
        while (itr.hasNext()) {
            MessagePartInfo part = itr.next();
            if (hasNext) {
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JwkUtils.java`
#### Snippet
```java
        List<JsonWebKey> parsedKeys = new ArrayList<>(keys.size());
        Iterator<JsonWebKey> iter = keys.iterator();
        while (iter.hasNext()) {
            JsonWebKey key = iter.next();
            if (!(key.containsProperty("k") || key.getKeyType() == KeyType.OCTET)) {
```

## RuleId[ruleID=StaticInitializerReferencesSubClass]
### StaticInitializerReferencesSubClass
Referencing subclass WSDVersion11 from superclass WSDVersion initializer might lead to class loading deadlock
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDVersion.java`
#### Snippet
```java

    public static final WSDVersion INSTANCE_1_0 = new WSDVersion10();
    public static final WSDVersion INSTANCE_1_1 = new WSDVersion11();

    public abstract String getNamespace();
```

### StaticInitializerReferencesSubClass
Referencing subclass WSDVersion10 from superclass WSDVersion initializer might lead to class loading deadlock
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDVersion.java`
#### Snippet
```java
    public static final String TO_1_1 = "urn:docs-oasis-open-org:ws-dd:ns:discovery:2009:01";

    public static final WSDVersion INSTANCE_1_0 = new WSDVersion10();
    public static final WSDVersion INSTANCE_1_1 = new WSDVersion11();

```

### StaticInitializerReferencesSubClass
Referencing subclass CglibProxyHelper from superclass ProxyHelper initializer might lead to class loading deadlock
in `core/src/main/java/org/apache/cxf/common/util/ProxyHelper.java`
#### Snippet
```java
        ProxyHelper theHelper;
        try {
            theHelper = new CglibProxyHelper();
        } catch (Throwable ex) {
            theHelper = new ProxyHelper();
```

## RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `anyPrefixCounter` from instance context
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
    private void serializeAny(ParticleInfo itemInfo, JavascriptUtils bodyUtils) {
        String prefix = "cxfjsany" + anyPrefixCounter;
        anyPrefixCounter++;
        bodyUtils.generateCodeToSerializeAny(itemInfo, prefix, xmlSchemaCollection);
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `anyTypePrefixCounter` from instance context
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        startIf("anyHolder.xsiType");
        appendLine("var typePrefix = 'cxfjst" + anyTypePrefixCounter + "';");
        anyTypePrefixCounter++;
        appendLine("var typeAttr = 'xmlns:' + typePrefix + '=\\\''"
                   + " + anyHolder.namespaceURI + '\\\'';");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `hasLoggedAsyncWarning` from instance context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                    if (!hasLoggedAsyncWarning) {
                        LOG.warning("EXECUTOR_FULL_WARNING");
                        hasLoggedAsyncWarning = true;
                    }
                    LOG.fine("EXECUTOR_FULL");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `weblogicWarned` from instance context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
                if (connection.getClass().getName().contains("weblogic")) {
                    if (!weblogicWarned) {
                        weblogicWarned = true;
                        LOG.warning("Could not configure SSLSocketFactory on Weblogic.  "
                                    + " Use the Weblogic control panel to configure the SSL settings.");
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `qNamePattern` from instance context
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageQName.java`
#### Snippet
```java
        if (qNamePattern == null) {
            String qName = getQNamePatternString();
            qNamePattern = Pattern.compile("^" + qName);
        }
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `instanceCount` from instance context
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/util/IdGenerator.java`
#### Snippet
```java
    public IdGenerator(String prefix) {
        synchronized (UNIQUE_STUB) {
            this.seed = prefix + UNIQUE_STUB + (instanceCount++) + ":";
        }
    }
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `applicationContext` from instance context
in `testutils/src/main/java/org/apache/cxf/test/AbstractCXFSpringTest.java`
#### Snippet
```java
            return;
        }
        applicationContext = new GenericApplicationContext();
        resourceLoader = new DefaultResourceLoader(configContextClass.getClassLoader());
        for (String beanDefinitionPath : getConfigLocations()) {
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `applicationContext` from instance context
in `testutils/src/main/java/org/apache/cxf/test/AbstractCXFSpringTest.java`
#### Snippet
```java
    public void teardownBeans() {
        applicationContext.close();
        applicationContext = null;
    }

```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `mbsc` from instance context
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        JMXServiceURL url = new JMXServiceURL(jmxServerURL);
        JMXConnector jmxc = JMXConnectorFactory.connect(url, null);
        mbsc = jmxc.getMBeanServerConnection();
    }

```

## RuleId[ruleID=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                W3CEndpointReferenceBuilder builder = new W3CEndpointReferenceBuilder();
                builder.address(add);
                //builder.serviceName(type);
                //builder.endpointName(type);
                er.add(builder.build());
```

### CommentedOutCode
Commented out code (7 lines)
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
        // it.
        // It might be the URI for the wsdl TNS, which might have no schema.
        //if (xmlSchemaCollection.getSchemaByTargetNamespace(referencingURI) == null) {
        //    referencingURI = null;
        //}
```

### CommentedOutCode
Commented out code (13 lines)
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
                wrappedStream.close();
            }
            /*
            try {
                //make sure the input stream is also closed in this
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
        return sslSocketFactory.getSupportedCipherSuites();
    }
    /*
    public Socket createSocket() throws IOException {
        return enableCipherSuites(sslSocketFactory.createSocket(),
```

### CommentedOutCode
Commented out code (8 lines)
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractRequestAssertionConsumerHandler.java`
#### Snippet
```java

        String samlResponseDecoded = samlResponse;
        /*
        // URL Decoding only applies for the re-direct binding
        if (!postBinding) {
```

### CommentedOutCode
Commented out code (20 lines)
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/MetadataWriter.java`
#### Snippet
```java
        writer.writeEndElement(); // AssertionConsumerService

        /*
        if (protocol.getClaimTypesRequested() != null && !protocol.getClaimTypesRequested().isEmpty()) {
            writer.writeStartElement("md", "AttributeConsumingService", SSOConstants.SAML2_METADATA_NS);
```

### CommentedOutCode
Commented out code (5 lines)
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/SwaggerUi.java`
#### Snippet
```java
        SwaggerUiResolver theHelper;
        //TODO: [OSGi+Jakarta] uncomment this when osgi comes back
        /*try {
            theHelper = new OsgiSwaggerUiResolver(OpenAPIDefinition.class);
        } catch (Throwable ex) {
```

### CommentedOutCode
Commented out code (10 lines)
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBus.java`
#### Snippet
```java
            id = super.getId();
            //TODO:[OSGi+Jakarta] uncomment this when osgi comes back
            /*try {
                Class<?> clsbc = Class.forName("org.osgi.framework.BundleContext");
                Class<?> clsb = Class.forName("org.osgi.framework.Bundle");
```

### CommentedOutCode
Commented out code (2 lines)
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
        internal.setAddress(convert(exposed.getAddress()));
        // TODO ref parameters not present in 2004/03
        // internal.setReferenceParameters(convert(exposed
        // .getReferenceParameters()));
        org.apache.cxf.ws.addressing.v200403.ServiceNameType serviceName = exposed.getServiceName();
```

### CommentedOutCode
Commented out code (2 lines)
in `core/src/main/java/org/apache/cxf/bus/spring/Jsr250BeanPostProcessor.java`
#### Snippet
```java

    private boolean isProcessing = true;
    //private int count;
    //private int count2;

```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/bus/spring/Jsr250BeanPostProcessor.java`
#### Snippet
```java
            getResourceManager(bean);
        }
        /*
        if (bean.getClass().getName().contains("Corb")) {
            Thread.dumpStack();
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        lst.addAll(orig.getBeansOfType(type));
        //TODO: [OSGi+Jakarta] Uncomment this when osgi comes back
        /*if (lst.isEmpty()) {
            tryOSGI(lst, type);
        }*/
```

### CommentedOutCode
Commented out code (27 lines)
in `core/src/main/java/org/apache/cxf/common/spi/NamespaceClassGenerator.java`
#### Snippet
```java
    }

    /*
    // This is the "prototype" for the ASM generated class below
    public static class MapNamespacePrefixMapper2
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
        if (type == null) {
            type = mapToSchemaType(origCls, namespace);
            /*
            if (type == null) {
                type = mapToSchemaType(cls, namespace);
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
            }
        }
        /*
        if (type == null) {
            System.out.println("HELP: " + cls.getName());
```

### CommentedOutCode
Commented out code (9 lines)
in `core/src/main/java/org/apache/cxf/common/util/SpringClasspathScanner.java`
#### Snippet
```java
    }
    //TODO: [OSGi+Jakarta] uncoment this when osgi comes back
    /*private static boolean isSpringInOsgi() {
        try {
            Class.forName("org.springframework.osgi.io.OsgiBundleResourcePatternResolver");
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/common/util/SpringClasspathScanner.java`
#### Snippet
```java
        ResourcePatternResolver resolver = null;
        //TODO: [OSGi+Jakarta] uncoment this when osgi comes back
        /*if (IN_OSGI) {
            resolver = SpringOsgiUtil.getResolver(loader);
        }*/
```

### CommentedOutCode
Commented out code (6 lines)
in `core/src/main/java/org/apache/cxf/message/MessageUtils.java`
#### Snippet
```java
    public static boolean isDOMPresent(Message m) {
        return m != null && m.getContent(Node.class) != null;
        /*
        for (Class c : m.getContentFormats()) {
            if (c.equals(Node.class) || "jakarta.xml.soap.SOAPMessage".equals(c.getName())) {
```

### CommentedOutCode
Commented out code (13 lines)
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java

            // uncomment this to see if anything is actually setting anything into the empty doc
            /*
            final Document doc  = createDocument();
            emptyDocument = (Document)org.apache.cxf.common.util.ProxyHelper.getProxy(
```

### CommentedOutCode
Commented out code (5 lines)
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
        if (host != null) {
            //comment out unused port to pass pmd check
            /*String ports = SystemPropertyAction.getProperty("http.proxyPort");
            int port = 80;
            if (ports != null) {
```

### CommentedOutCode
Commented out code (3 lines)
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
    @Override
    public void writeStartElement(String uri, String local) throws XMLStreamException {
        /*
        pushedAheadEvents.push(null);
        elementsStack.push(new QName(uri, local));
```

### CommentedOutCode
Commented out code (8 lines)
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java

    private XMLStreamWriter getOutput(OutputStream out) {
        /*
        StAXDocumentSerializer serializer = (StAXDocumentSerializer)m.getExchange().getEndpoint()
            .remove(StAXDocumentSerializer.class.getName());
```

### CommentedOutCode
Commented out code (2 lines)
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/ClassLoaderSwitcher.java`
#### Snippet
```java
                    buf.append(a.getFile().getAbsolutePath());
                    buf.append(File.pathSeparatorChar);
                    // System.out.println("     " +
                    // a.getFile().getAbsolutePath());
                }
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedOutputBuffer.java`
#### Snippet
```java
                        buffer.position(p + i);
                    }
                    /*
                    System.out.println("p: " + p + "  " + i + " " + this.buffer.position()
                                       + " " + this.buffer.hasRemaining());
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedInputBuffer.java`
#### Snippet
```java
    private volatile ByteBuffer waitingBuffer;

    //private volatile int waitCnt;
    //private volatile int nowaitCnt;

```

### CommentedOutCode
Commented out code (2 lines)
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    outbuf.shutdown();
                    inbuf.shutdown();
                    //outbuf = null;
                    //inbuf = null;

```

### CommentedOutCode
Commented out code (2 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
                o = ((JAXBElement<?>)o).getValue();
            }
            //System.out.println(pfx + DOMUtils.getElementQName(element) + " ->  "
            //    + (o == null ? "null" : o.getClass()));
            if (o == null && parent != null) {
```

### CommentedOutCode
Commented out code (3 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
        //if that bug is ever fixed and we can detect it, we can remove this
        //complete and total HACK HACK HACK and replace with just:
        //Unmarshaller unmarshaller = jaxbContext.createUnmarshaller();
        //JAXBElement<?> jaxbElement = (JAXBElement<?>) unmarshaller.unmarshal(source);
        //return jaxbElement.getValue();
```

### CommentedOutCode
Commented out code (5 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
        PolicyUtils.assertPolicy(aim, new QName(namespace, SPConstants.PROTECT_TOKENS));

        /*
        // Check ProtectTokens
        if (binding.isTokenProtection() && !isTokenProtected(results, signedResults)) {
```

### CommentedOutCode
Commented out code (6 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        properties.addAction(WSSConstants.KERBEROS_TOKEN);

        /*
        if (endorsing) {
            String action = (String)config.get(ConfigurationConstants.ACTION);
```

### CommentedOutCode
Commented out code (3 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SamlTokenPolicyValidator.java`
#### Snippet
```java
                    }
                }
                /*
                    if (!checkIssuerName(samlToken, assertionWrapper)) {
                        ai.setNotAsserted("Wrong IssuerName");
```

### CommentedOutCode
Commented out code (21 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenInInterceptor.java`
#### Snippet
```java
        }

        /*
        private void writeAuthenticator(
            W3CDOMStreamWriter writer,
```

### CommentedOutCode
Commented out code (8 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenInInterceptor.java`
#### Snippet
```java
            writer.writeEndElement();

            /*
            // Second RequestSecurityTokenResponse containing the Authenticator
            // TODO
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
        List<org.apache.wss4j.policy.model.XPath> xpaths = elements.getXPaths();

        //Map<String, String> namespaces = elements.getDeclaredNamespaces();
        //List<String> xpaths = elements.getXPathExpressions();

```

### CommentedOutCode
Commented out code (4 lines)
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WebServiceContextImpl.java`
#### Snippet
```java
        }

        /*
        if (ep.getEndpointInfo().getService().getDescription() != null) {
            builder.wsdlDocumentLocation(ep.getEndpointInfo().getService()
```

### CommentedOutCode
Commented out code (3 lines)
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
                }

                /*if (serviceName != null) {
                    serverFactory.getServiceFactory().setServiceName(serviceName);
                }*/
```

### CommentedOutCode
Commented out code (6 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
                // sequence header containing a lastMessage element.

                /*
                Proxy proxy = new Proxy(reliableEndpoint);
                try {
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java

    //Not relevant unless ws-rm is used for non-http protocols
//    @ManagedAttribute(description = "TransportId Attribute", currencyTimeLimit = 60)
//    public String getTransportId() {
//        return endpoint.getApplicationEndpoint().getEndpointInfo().getTransportId();
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
    }

//    @ManagedAttribute(description = "Number of Outbound Completed Messages", currencyTimeLimit = 10)
//    public int getCompletedMessagesOutboundCount() {
//        return endpoint.getManager().countCompleted();
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
    }

//    @ManagedAttribute(description = "Number of Inbound Completed Messages", currencyTimeLimit = 10)
//    public int getCompletedMessagesInboundCount() {
//        return endpoint.getManager().countCompleted();
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMManager.java`
#### Snippet
```java


//    @ManagedAttribute(description = "Total Number of Inbound Queued Messages", currencyTimeLimit = 10)
//    public int getQueuedMessagesInboundCount() {
//        return manager.getRedeliveryQueue().countUndelivered();
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMFeatureBeanDefinitionParser.java`
#### Snippet
```java
                org.apache.cxf.ws.rmp.v200502.RMAssertion.class);
        // TODO: handle
//        mapElementToJaxbProperty(element, bean,
//                new QName("http://docs.oasis-open.org/ws-rx/wsrmp/200702", "RMAssertion"),
//                "RMAssertion",
```

### CommentedOutCode
Commented out code (4 lines)
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMManagerBeanDefinitionParser.java`
#### Snippet
```java
                org.apache.cxf.ws.rmp.v200502.RMAssertion.class);
        // TODO: handle
//        mapElementToJaxbProperty(element, bean,
//                new QName("http://docs.oasis-open.org/ws-rx/wsrmp/200702", "RMAssertion"),
//                "RMAssertion",
```

### CommentedOutCode
Commented out code (7 lines)
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
    private void doCollection(ASMHelper.MethodVisitor mv, int x, Class<?> wrapperType, Method[] setMethods,
                              Method[] getMethods, ASMHelper asmhelper) {
        // List aVal = obj.getA();
        // List newA = (List)lst.get(99);
        // if (aVal == null) {
```

### CommentedOutCode
Commented out code (13 lines)
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java

    public Marshaller createMarshaller(Object elValue, MessagePartInfo part) {
        //Class<?> cls = null;
        //if (part != null) {
        //    cls = part.getTypeClass();
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                // validation failure => dispatch is aborted, response MAPs
                // must be aggregated
                //isFault = true;
                //aggregate(message, isFault);
                if (isSOAP12(message)) {
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java

            // remove server-specific properties
            //inMessage.remove(AbstractHTTPDestination.HTTP_REQUEST);
            //inMessage.remove(AbstractHTTPDestination.HTTP_RESPONSE);
            inMessage.remove(Message.ASYNC_POST_RESPONSE_DISPATCH);
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                    }
                }
                //StaxUtils.print(r.getNode());
                //System.out.println();
                addSchemaDocument(serviceInfo,
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestInterceptor.java`
#### Snippet
```java
//        You can customise it, extract the token from the message, example, get
//        WS-Security Binary token put on the message by WSS4JInInterceptor
//
//        String token = getTokenFromCurrentMessage(mc);
//        return new String[] {"Bearer", token};
```

### CommentedOutCode
Commented out code (4 lines)
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/AbstractSTSFailureEvent.java`
#### Snippet
```java
    }

    /*@Override
    public String getOperation() {
        return OPERATION;
```

### CommentedOutCode
Commented out code (2 lines)
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/JexlClaimsMapper.java`
#### Snippet
```java

    public JexlClaimsMapper() {
        // jexl.setCache(512);
        // jexl.setLenient(false);

```

### CommentedOutCode
Commented out code (3 lines)
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java
        c.setPrincipal(principal);
        c.setValues(new ArrayList<>(filteredGroups));
        // c.setIssuer(issuer);
        // c.setOriginalIssuer(originalIssuer);
        // c.setNamespace(namespace);
```

### CommentedOutCode
Commented out code (3 lines)
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
            ProcessedClaim c = processClaim(claim, ldapAttributes, principal);
            if (c != null) {
                // c.setIssuer(issuer);
                // c.setOriginalIssuer(originalIssuer);
                // c.setNamespace(namespace);
```

### CommentedOutCode
Commented out code (6 lines)
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java
            return;
        }
        // for (ExtensibilityElement element : extensiblilityElement) {
        // LOG.info("the extensibility is " + element.getClass().getName());
        // if (element instanceof SOAPHeader) {
```

### CommentedOutCode
Commented out code (2 lines)
in `testutils/src/main/java/org/apache/hello_world_soap_http/RPCLitGreeterImpl.java`
#### Snippet
```java

    public String greetUs(String you, String me) {
        //System.out.println("Executing operation greetUs");
        //System.out.println("Message received: you are " + you + " I'm " + me + "\n");
        return "Hello " + you + " and " + me;
```

### CommentedOutCode
Commented out code (2 lines)
in `testutils/src/main/java/org/apache/hello_world_soap_http/RPCLitGreeterImpl.java`
#### Snippet
```java

    public String greetMe(String me) {
        //System.out.println("Executing operation greetMe");
        //System.out.println("Message received: " + me + "\n");
        if ("return null".equals(me)) {
```

### CommentedOutCode
Commented out code (3 lines)
in `testutils/src/main/java/org/apache/hello_world_soap_http/RPCLitGreeterImpl.java`
#### Snippet
```java

    public MyComplexStruct sendReceiveData(MyComplexStruct in) {
        //System.out.println("Executing operation sendReceiveData");
        //System.out.println("Received struct with values :\nElement-1 : " + in.getElem1() + "\nElement-2 : "
        //                   + in.getElem2() + "\nElement-3 : " + in.getElem3() + "\n");
```

### CommentedOutCode
Commented out code (2 lines)
in `testutils/src/main/java/org/apache/hello_world_soap_http/HWSoapMessageProvider.java`
#### Snippet
```java
public class HWSoapMessageProvider implements Provider<SOAPMessage> {

    //private static final Logger LOG =
    //    Logger.getLogger(AnnotatedGreeterImpl.class.getName());

```

### CommentedOutCode
Commented out code (4 lines)
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/SkipStartEndEventProducer.java`
#### Snippet
```java
        name = eventProducer.getName();
        peekedEvent = eventProducer.next();
        /*
        if (peekedEvent == XMLStreamReader.END_ELEMENT) {
            hasNext = false;
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/ContextUtils.java`
#### Snippet
```java
        if (dataWriter == null) {
            //throw a fault
            //throw new Fault(new org.apache.cxf.common.i18n.Message("NO_DATAWRITER", BUNDLE, service
            //    .getName()));
        }
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/ContextUtils.java`
#### Snippet
```java
        if (dataReader == null) {
            //throw a fault
            //throw new Fault(new org.apache.cxf.common.i18n.Message("NO_DATAREADER", BUNDLE, service
            //    .getName()));
        }
```

### CommentedOutCode
Commented out code (20 lines)
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
    }

    /*protected Class<?> findClass(String name) throws ClassNotFoundException {
        if (negativeFilters != null) {
            for (int i = 0; i < negativeFilters.length; i++) {
```

### CommentedOutCode
Commented out code (6 lines)
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        }
        this.requestHeaders = WebSocketUtils.readHeaders(in);
        /*String path = requestHeaders.get(WebSocketUtils.URI_KEY);
        String origin = channel.getUrl();
        path = path.substring(0,  path.length() - 10);
```

### CommentedOutCode
Commented out code (2 lines)
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/common/ClassLoaderSwitcher.java`
#### Snippet
```java
                    ret.add(a.getFile().toURI());
                    buf.append(File.pathSeparatorChar);
                    // System.out.println("     " +
                    // a.getFile().getAbsolutePath());
                }
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementReader.java`
#### Snippet
```java
     */
    public ElementReader(InputStream is) throws XMLStreamException {
        // XMLInputFactory factory = XMLInputFactory.newInstance();
        // XMLStreamReader xmlReader = factory.createXMLStreamReader(is);
        XMLStreamReader xmlReader = StaxUtils.createXMLStreamReader(is, null);
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapArrayType.java`
#### Snippet
```java
                values.addAll(Collections.nCopies(maxSize - values.size(), null));
                // todo is this an error?
                // throw new DatabindingException("The number of elements in " + getSchemaType() +
                //         " is less then the expected size of " + expectedSize);
            }
```

### CommentedOutCode
Commented out code (2 lines)
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/ClassLoaderSwitcher.java`
#### Snippet
```java
                    ret.add(a.getFile().toURI());
                    buf.append(File.pathSeparatorChar);
                    // System.out.println("     " +
                    // a.getFile().getAbsolutePath());
                }
```

### CommentedOutCode
Commented out code (3 lines)
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
                    part.setProperty("nillable", b == null ? isNil : b);
                }
                /*
                if (miValue == -1 && (b == null ? isNil : b)) {
                    part.setProperty("minOccurs", "1");
```

### CommentedOutCode
Commented out code (2 lines)
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
                 */

                // case XMLStreamConstants.ATTRIBUTE:
                // case XMLStreamConstants.NAMESPACE:
            default:
```

## RuleId[ruleID=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/BasicNameManager.java`
#### Snippet
```java
     */
    protected String transformURI(String uri) {
        return uri.replaceAll("http:/*", "").replace("uri:", "").replaceAll("[\\.:/-]", "_");
    }

```

### RegExpRedundantEscape
Redundant character escape `\\/` in RegExp
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapCertificateRepo.java`
#### Snippet
```java

    protected String getDnForIdentifier(String id) {
        String escapedIdentifier = id.replaceAll("\\/", Matcher.quoteReplacement("\\/"));
        return String.format(ldapConfig.getServiceCertRDNTemplate(), escapedIdentifier) + "," + rootDN;
    }
```

### RegExpRedundantEscape
Redundant character escape `\\}` in RegExp
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        String str = new String(bout.toByteArray());
        Pattern pattern = Pattern.compile("(?<=There's\\sno\\sObjectFactory\\swith\\san\\s"
                                          + "@XmlElementDecl\\sfor\\sthe\\selement\\s\\{)\\S*(?=\\})");
        java.util.regex.Matcher matcher = pattern.matcher(str);
        while (matcher.find()) {
```

### RegExpRedundantEscape
Redundant character escape `\\+` in RegExp
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MediaTypeHeaderProvider.java`
#### Snippet
```java
        "org.apache.cxf.jaxrs.mediaTypeCheck.strict";
    private static final Pattern COMPLEX_PARAMETERS =
        Pattern.compile("(([\\w-]+=\"[^\"]*\")|([\\w-]+=[\\w-/\\+]+))");

    private static Map<String, MediaType> map = new ConcurrentHashMap<>();
```

### RegExpRedundantEscape
Redundant character escape `\\<` in RegExp
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
public final class ResponseImpl extends Response {
    public static final String RESPONSE_STREAM_AUTO_CLOSE = "response.stream.auto.close";
    private static final Pattern LINK_DELIMITER = Pattern.compile(",\\s*(?=\\<|$)");

    private StatusType status;
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
     */
    private static final class Variable extends UriChunk {
        private static final Pattern VARIABLE_PATTERN = Pattern.compile("(\\w[-\\w\\.]*[ ]*)(\\:(.+))?");
        private String name;
        private Pattern pattern;
```

### RegExpRedundantEscape
Redundant character escape `\\:` in RegExp
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
     */
    private static final class Variable extends UriChunk {
        private static final Pattern VARIABLE_PATTERN = Pattern.compile("(\\w[-\\w\\.]*[ ]*)(\\:(.+))?");
        private String name;
        private Pattern pattern;
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

    public static class Wsdl11XPointer {
        private static final String SINGLE_PATH = "\\(([^\\)]*)\\)$";
        private static final String DOUBLE_PATH = "\\(([^/]*)/([^\\)]*)\\)$";
        private static final String TRIPPLE_PATH = "\\(([^/]*)/([^/]*)/([^\\)]*)\\)$";
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_MESSAGE
            = Pattern.compile("^wsdl11.message" + SINGLE_PATH);

        private static final Pattern PATTERN_MESSAGE_PART
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_SERVICE
            = Pattern.compile("^wsdl11.service" + SINGLE_PATH);

        private static final Pattern PATTERN_PORT
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_BINDING_OPERATION_INPUT =
            Pattern.compile("^wsdl11.bindingOperation.input" + DOUBLE_PATH);

        private static final Pattern PATTERN_BINDING_OPERATION_OUTPUT =
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_FAULT
            = Pattern.compile("^wsdl11.portTypeOperation.fault" + TRIPPLE_PATH);

        private static final Pattern PATTERN_MESSAGE
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_BINDING_OPERATION =
            Pattern.compile("^wsdl11.bindingOperation" + DOUBLE_PATH);

        private static final Pattern PATTERN_BINDING_OPERATION_INPUT =
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_BINDING
            = Pattern.compile("^wsdl11.binding" + SINGLE_PATH);

        private static final Pattern PATTERN_BINDING_OPERATION =
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_BINDING_OPERATION_FAULT =
            Pattern.compile("^wsdl11.bindingOperation.fault" + TRIPPLE_PATH);

        private final String targetNamespace;
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_OUTPUT
            = Pattern.compile("^wsdl11.portTypeOperation.output" + DOUBLE_PATH);

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_FAULT
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT_TYPE_OPERATION
            = Pattern.compile("^wsdl11.portTypeOperation" + DOUBLE_PATH);

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_INPUT
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT
            = Pattern.compile("^wsdl11.port" + DOUBLE_PATH);

        private static final Pattern PATTERN_BINDING
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java
        private static final String SINGLE_PATH = "\\(([^\\)]*)\\)$";
        private static final String DOUBLE_PATH = "\\(([^/]*)/([^\\)]*)\\)$";
        private static final String TRIPPLE_PATH = "\\(([^/]*)/([^/]*)/([^\\)]*)\\)$";

        private static final Pattern PATTERN_DEFINITIONS
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_MESSAGE_PART
            = Pattern.compile("^wsdl11.messagePart" + DOUBLE_PATH);

        private static final Pattern PATTERN_SERVICE
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_BINDING_OPERATION_OUTPUT =
            Pattern.compile("^wsdl11.bindingOperation.output" + DOUBLE_PATH);

        private static final Pattern PATTERN_BINDING_OPERATION_FAULT =
```

### RegExpRedundantEscape
Redundant character escape `\\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java
    public static class Wsdl11XPointer {
        private static final String SINGLE_PATH = "\\(([^\\)]*)\\)$";
        private static final String DOUBLE_PATH = "\\(([^/]*)/([^\\)]*)\\)$";
        private static final String TRIPPLE_PATH = "\\(([^/]*)/([^/]*)/([^\\)]*)\\)$";

```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_INPUT
            = Pattern.compile("^wsdl11.portTypeOperation.input" + DOUBLE_PATH);

        private static final Pattern PATTERN_PORT_TYPE_OPERATION_OUTPUT
```

### RegExpRedundantEscape
Redundant character escape `\)` in RegExp
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpression.java`
#### Snippet
```java

        private static final Pattern PATTERN_PORT_TYPE
            = Pattern.compile("^wsdl11.portType" + SINGLE_PATH);

        private static final Pattern PATTERN_PORT_TYPE_OPERATION
```

## RuleId[ruleID=KeySetIterationMayUseEntrySet]
### KeySetIterationMayUseEntrySet
Iteration over `mapKeys` may be replaced with 'entrySet()' iteration
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
        }

        for (K key : mapKeys) {
            List<V> localValues = this.get(key);
            List<V> mapValues = map.get(key);
```

### KeySetIterationMayUseEntrySet
Iteration over `s.keySet()` may be replaced with 'entrySet()' iteration
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
            Map<Class<?>, String> implMap = new HashMap<>();
            // now for a really annoying case, the .implementation objects.
            for (String key : s.keySet()) {
                if (key.endsWith(".implementation")) {
                    String className = key.substring(0, key.length() - ".implementation".length());
```

## RuleId[ruleID=UnnecessaryQualifierForThis]
### UnnecessaryQualifierForThis
Qualifier `Injections` on 'this' is unnecessary in this context
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/Injections.java`
#### Snippet
```java
                    try {
                        // the standard name otherwise not portable
                        return f.get(Injections.this) != null ? f.getName() + "=" + f.getType().getSimpleName() : "";
                    } catch (final IllegalAccessException e) {
                        return "";
```

### UnnecessaryQualifierForThis
Qualifier `TypeHelperClassLoader` on 'super' is unnecessary in this context
in `core/src/main/java/org/apache/cxf/common/spi/ClassGeneratorClassLoader.java`
#### Snippet
```java

            ret = defined.computeIfAbsent(StringUtils.slashesToPeriod(name),
                key -> TypeHelperClassLoader.super.defineClass(key, bytes, 0, bytes.length));

            return ret;
```

### UnnecessaryQualifierForThis
Qualifier `TypeHelperClassLoader` on 'super' is unnecessary in this context
in `core/src/main/java/org/apache/cxf/common/spi/GeneratedClassClassLoader.java`
#### Snippet
```java

            ret = defined.computeIfAbsent(StringUtils.slashesToPeriod(name),
                key -> TypeHelperClassLoader.super.defineClass(key, bytes, 0, bytes.length));

            return ret;
```

### UnnecessaryQualifierForThis
Qualifier `StaxSource` on 'this' is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public void parse(InputSource input) throws SAXException {
        StaxSource.this.parse();
    }

```

### UnnecessaryQualifierForThis
Qualifier `StaxSource` on 'this' is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public void parse(String systemId) throws SAXException {
        StaxSource.this.parse();
    }

```

### UnnecessaryQualifierForThis
Qualifier `OAuthRequestFilter` on 'this' is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java

        final UserSubject theSubject =
            OAuthRequestFilter.this.useUserSubject ? resourceOwnerSubject : clientSubject;

        return new SecurityContext() {
```

## RuleId[ruleID=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`moreNamespaces.length() > 0` can be replaced with '!moreNamespaces.isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
        // now add any accumulated namespaces.
        String moreNamespaces = prefixAccumulator.getAttributes();
        if (moreNamespaces.length() > 0) {
            utils.appendString(" ");
            utils.appendString(moreNamespaces);
```

### SizeReplaceableByIsEmpty
`continuationsStr.length() > 0` can be replaced with '!continuationsStr.isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String continuationsStr = element.getAttribute("continuationsEnabled");
        if (continuationsStr != null && continuationsStr.length() > 0) {
            bean.addPropertyValue("continuationsEnabled", continuationsStr);
        }
```

### SizeReplaceableByIsEmpty
`sendServerVersionStr.length() > 0` can be replaced with '!sendServerVersionStr.isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String sendServerVersionStr = element.getAttribute("sendServerVersion");
        if (sendServerVersionStr != null && sendServerVersionStr.length() > 0) {
            bean.addPropertyValue("sendServerVersion", sendServerVersionStr);
        }
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/interceptor/WSS4JBasicAuthValidator.java`
#### Snippet
```java
            String roleAttributeName =
                (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.SAML_ROLE_ATTRIBUTENAME, msg);
            if (roleAttributeName == null || roleAttributeName.length() == 0) {
                roleAttributeName = SAML_ROLE_ATTRIBUTENAME_DEFAULT;
            }
```

### SizeReplaceableByIsEmpty
`outputMessage.getMessageParts().size() != 0` can be replaced with '!outputMessage.getMessageParts().isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

        MessageInfo outputMessage = op.getOutput();
        nonVoidOutput = outputMessage != null && outputMessage.getMessageParts().size() != 0;

        if (!op.isOneWay()) {
```

### SizeReplaceableByIsEmpty
`getPath().length() > 0` can be replaced with '!getPath().isEmpty()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Cookie.java`
#### Snippet
```java
        b.append("; ").append(getName())
            .append('=').append(getValue());
        if (getPath() != null && getPath().length() > 0) {
            b.append("; $Path=").append(getPath());
        }
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`((String)obj).length() > 0` can be replaced with '!((String)obj).isEmpty()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                Object obj = objs.get(0);
                return obj.getClass() != String.class
                    || (obj.getClass() == String.class && ((String)obj).length() > 0);
            }
        }
```

### SizeReplaceableByIsEmpty
`authHeader.length() > 0` can be replaced with '!authHeader.isEmpty()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListJAASAuthenticator.java`
#### Snippet
```java
        // Return immediately if the header is missing
        String authHeader = request.getHeader(HEADER_AUTHORIZATION);
        if (authHeader != null && authHeader.length() > 0) {

            // Get the authType (Basic, Digest) and authInfo (user/password)
```

### SizeReplaceableByIsEmpty
`name.getPrefix().length() == 0` can be replaced with 'name.getPrefix().isEmpty()'
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSigOutInterceptor.java`
#### Snippet
```java

    public void setEnvelopeQName(QName name) {
        if (name.getPrefix().length() == 0) {
            name = new QName(name.getNamespaceURI(), name.getLocalPart(), DEFAULT_ENV_PREFIX);
        }
```

### SizeReplaceableByIsEmpty
`name.getPrefix().length() == 0` can be replaced with 'name.getPrefix().isEmpty()'
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java

    public void setEnvelopeQName(QName name) {
        if (name.getPrefix().length() == 0) {
            name = new QName(name.getNamespaceURI(), name.getLocalPart(), DEFAULT_ENV_PREFIX);
        }
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
            String roleAttributeName =
                (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.SAML_ROLE_ATTRIBUTENAME, m);
            if (roleAttributeName == null || roleAttributeName.length() == 0) {
                roleAttributeName = "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/role";
            }
```

### SizeReplaceableByIsEmpty
`url.getPath().length() == 0` can be replaced with 'url.getPath().isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        path = Handlers.path(new NotFoundHandler());

        if (url.getPath().length() == 0) {
            result = result.setHandler(Handlers.trace(undertowHTTPHandler));
        } else {
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`continuationsStr.length() > 0` can be replaced with '!continuationsStr.isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String continuationsStr = element.getAttribute("continuationsEnabled");
        if (continuationsStr != null && continuationsStr.length() > 0) {
            bean.addPropertyValue("continuationsEnabled", continuationsStr);
        }
```

### SizeReplaceableByIsEmpty
`path.length() > 0` can be replaced with '!path.isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    protected String getPath(String path) {
        String thePath;
        if (ignoreForwardSlash && path.startsWith("/") && path.length() > 0) {
            thePath = path.substring(1);
        } else {
```

### SizeReplaceableByIsEmpty
`d.target().length() == 0` can be replaced with 'd.target().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (a.annotationType() == Description.class) {
                Description d = (Description)a;
                if (d.target().length() == 0 && !allowDefault || d.target().length() > 0
                    && !d.target().equals(category)) {
                    continue;
```

### SizeReplaceableByIsEmpty
`d.target().length() > 0` can be replaced with '!d.target().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (a.annotationType() == Description.class) {
                Description d = (Description)a;
                if (d.target().length() == 0 && !allowDefault || d.target().length() > 0
                    && !d.target().equals(category)) {
                    continue;
```

### SizeReplaceableByIsEmpty
`d.lang().length() > 0` can be replaced with '!d.lang().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

                sb.append("<doc");
                if (!isJson && d.lang().length() > 0) {
                    sb.append(" xml:lang=\"").append(d.lang()).append('"');
                }
```

### SizeReplaceableByIsEmpty
`d.title().length() > 0` can be replaced with '!d.title().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                    sb.append(" xml:lang=\"").append(d.lang()).append('"');
                }
                if (d.title().length() > 0) {
                    sb.append(" title=\"").append(xmlEncodeIfNeeded(d.title())).append('"');
                }
```

### SizeReplaceableByIsEmpty
`d.value().length() > 0` can be replaced with '!d.value().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                }
                sb.append('>');
                if (d.value().length() > 0) {
                    sb.append(xmlEncodeIfNeeded(d.value()));
                } else if (d.docuri().length() > 0) {
```

### SizeReplaceableByIsEmpty
`d.docuri().length() > 0` can be replaced with '!d.docuri().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                if (d.value().length() > 0) {
                    sb.append(xmlEncodeIfNeeded(d.value()));
                } else if (d.docuri().length() > 0) {
                    InputStream is;
                    if (d.docuri().startsWith(CLASSPATH_PREFIX)) {
```

### SizeReplaceableByIsEmpty
`qname.getPrefix().length() > 0` can be replaced with '!qname.getPrefix().isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (name != null) {
                QName qname = DOMUtils.convertStringToQName(name.value(), name.prefix());
                if (qname.getPrefix().length() > 0) {
                    return qname;
                }
```

### SizeReplaceableByIsEmpty
`namespace.length() == 0` can be replaced with 'namespace.isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

    private QName getQNameFromParts(String name, String namespace, Class<?> type, Map<Class<?>, QName> clsMap) {
        if (namespace == null || JAXB_DEFAULT_NAMESPACE.equals(namespace) || namespace.length() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            return null;
        }
        if (name == null || name.length() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `core/src/main/java/org/apache/cxf/helpers/ServiceUtils.java`
#### Snippet
```java
            } else if ("FALSE".equals(value)) {
                return SchemaValidationType.NONE;
            } else if (value.length() > 0) {
                return SchemaValidationType.valueOf(value);
            }
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    public static String nameToIdentifier(String name, IdentifierType type) {

        if (null == name || name.length() == 0) {
            return name;
        }
```

### SizeReplaceableByIsEmpty
`namespaceURI.length() > 0` can be replaced with '!namespaceURI.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StreamWriterContentHandler.java`
#### Snippet
```java
        if (idx != -1) {
            return ns.substring(0, idx);
        } else if (namespaceURI != null && namespaceURI.length() > 0) {
            //this is the case that have namespaceURI but use DEFAULT_NS_PREFIX
            return "";
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java
            String prefix = streamReader.getAttributePrefix(i);
            String qName;
            if (prefix != null && prefix.length() > 0) {
                qName = prefix + ':' + localName;
            } else {
```

### SizeReplaceableByIsEmpty
`ns.length() > 0` can be replaced with '!ns.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/transform/DelegatingNamespaceContext.java`
#### Snippet
```java
        }
        String ns = nc.getNamespaceURI(prefix);
        if (ns != null && ns.length() > 0) {
            addPrefix(prefix, ns);
        }
```

### SizeReplaceableByIsEmpty
`trimmed.length() > 0` can be replaced with '!trimmed.isEmpty()'
in `core/src/main/java/org/apache/cxf/common/util/ClasspathScanner.java`
#### Snippet
```java
                basePackages.add(trimmed);
                break;
            } else if (trimmed.length() > 0) {
                basePackages.add(trimmed);
            }
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static void declare(Element node, String uri, String prefix) {
        String qualname;
        if (prefix != null && prefix.length() > 0) {
            qualname = "xmlns:" + prefix;
        } else {
```

### SizeReplaceableByIsEmpty
`attPrefix.length() > 0` can be replaced with '!attPrefix.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            String attName = qname.getLocalPart();
            String attPrefix = qname.getPrefix();
            if (attPrefix != null && attPrefix.length() > 0) {
                attName = attPrefix + ":" + attName;
            }
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                    String name = reader.getAttributeLocalName(att);
                    String prefix = reader.getAttributePrefix(att);
                    if (prefix != null && prefix.length() > 0) {
                        name = prefix + ":" + name;
                    }
```

### SizeReplaceableByIsEmpty
`actualNs.length() > 0` can be replaced with '!actualNs.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/transform/InTransformReader.java`
#### Snippet
```java
        String actualNs = nsMap.get(ns);
        if (actualNs != null) {
            if (actualNs.length() > 0) {
                return super.getNamespacePrefix(index);
            }
```

### SizeReplaceableByIsEmpty
`expected.getPrefix().length() > 0` can be replaced with '!expected.getPrefix().isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                    if (theprefix == null
                        && (appendQName.getNamespaceURI().equals(expected.getNamespaceURI())
                            && expected.getPrefix().length() > 0)) {
                        theprefix = expected.getPrefix();
                    } else if (theprefix == null) {
```

### SizeReplaceableByIsEmpty
`theprefix.length() > 0` can be replaced with '!theprefix.isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                }
                write(new QName(appendQName.getNamespaceURI(), appendQName.getLocalPart(), theprefix), false);
                if (nsadded && theprefix.length() > 0) {
                    writeNamespace(theprefix, appendQName.getNamespaceURI());
                }
```

### SizeReplaceableByIsEmpty
`val.trim().length() > 0` can be replaced with '!val.trim().isEmpty()'
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
                                        BeanDefinitionBuilder bean,
                                        String val) {
        if (val != null && val.trim().length() > 0) {
            if (ctx.getRegistry().containsBeanDefinition(val)) {
                bean.addPropertyReference("bus", val);
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`ref.length() > 0` can be replaced with '!ref.isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                name = qname.getLocalPart();
                String prefix = qname.getPrefix();
                if (prefix.length() > 0) {
                    name = prefix + "." + name;
                }
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSLoginModule.java`
#### Snippet
```java
                                                                   msg);
            }
            if (roleAttributeName == null || roleAttributeName.length() == 0) {
                roleAttributeName = WSS4JInInterceptor.SAML_ROLE_ATTRIBUTENAME_DEFAULT;
            }
```

### SizeReplaceableByIsEmpty
`attachments.size() > 0` can be replaced with '!attachments.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
                if (attachmentId != null) {
                    // Calling LazyAttachmentCollection.size() here to force it to load the attachments
                    if (attachments != null && attachments.size() > 0) {  // NOPMD
                        for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                            iterator.hasNext();) {
```

### SizeReplaceableByIsEmpty
`attachments.size() > 0` can be replaced with '!attachments.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
    ) throws IOException {
        // Calling LazyAttachmentCollection.size() here to force it to load the attachments
        if (attachments != null && attachments.size() > 0) { // NOPMD
            for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                iterator.hasNext();) {
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
                        String roleAttributeName = (String)SecurityUtils.getSecurityPropertyValue(
                                SecurityConstants.SAML_ROLE_ATTRIBUTENAME, msg);
                        if (roleAttributeName == null || roleAttributeName.length() == 0) {
                            roleAttributeName = SAML_ROLE_ATTRIBUTENAME_DEFAULT;
                        }
```

### SizeReplaceableByIsEmpty
`actor.length() > 0` can be replaced with '!actor.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
        SoapHeader sh = new SoapHeader(new QName(WSS4JConstants.WSSE_NS, "Security"), el);
        sh.setMustUnderstand(true);
        if (actor != null && actor.length() > 0) {
            sh.setActor(actor);
        }
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultWSS4JSecurityContextCreator.java`
#### Snippet
```java
                String roleAttributeName = (String)SecurityUtils.getSecurityPropertyValue(
                        SecurityConstants.SAML_ROLE_ATTRIBUTENAME, msg);
                if (roleAttributeName == null || roleAttributeName.length() == 0) {
                    roleAttributeName = WSS4JInInterceptor.SAML_ROLE_ATTRIBUTENAME_DEFAULT;
                }
```

### SizeReplaceableByIsEmpty
`roleAttributeName.length() == 0` can be replaced with 'roleAttributeName.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
        String roleAttributeName =
            (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.SAML_ROLE_ATTRIBUTENAME, msg);
        if (roleAttributeName == null || roleAttributeName.length() == 0) {
            roleAttributeName = WSS4JInInterceptor.SAML_ROLE_ATTRIBUTENAME_DEFAULT;
        }
```

### SizeReplaceableByIsEmpty
`secProps.getActions().size() > 0` can be replaced with '!secProps.getActions().isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java
            configureProperties(soapMessage, secProps);

            if (secProps.getActions() != null && secProps.getActions().size() > 0) {
                soapMessage.getInterceptorChain().add(new StaxActionInInterceptor(secProps.getActions()));
            }
```

### SizeReplaceableByIsEmpty
`suppTokens.getTokens().size() > 0` can be replaced with '!suppTokens.getTokens().isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxTransportBindingHandler.java`
#### Snippet
```java
                SupportingTokens suppTokens = (SupportingTokens)ai.getAssertion();
                if (suppTokens != null && suppTokens.getTokens() != null
                    && suppTokens.getTokens().size() > 0) {
                    handleSupportingTokens(suppTokens, false, false);
                }
```

### SizeReplaceableByIsEmpty
`suppTokens.getTokens().size() > 0` can be replaced with '!suppTokens.getTokens().isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/TransportBindingHandler.java`
#### Snippet
```java
                SupportingTokens suppTokens = (SupportingTokens)ai.getAssertion();
                if (suppTokens != null && suppTokens.getTokens() != null
                    && suppTokens.getTokens().size() > 0) {
                    handleSupportingTokens(suppTokens, false, new ArrayList<>());
                }
```

### SizeReplaceableByIsEmpty
`encryptedKeyID.length() != 0` can be replaced with '!encryptedKeyID.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                for (WSSecurityEngineResult wser : encryptedResults) {
                    String encryptedKeyID = (String)wser.get(WSSecurityEngineResult.TAG_ID);
                    if (encryptedKeyID != null && encryptedKeyID.length() != 0) {
                        return wser;
                    }
```

### SizeReplaceableByIsEmpty
`bstTokenId.length() > 0` can be replaced with '!bstTokenId.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
        //If direct ref is used to refer to the cert
        //then add the cert to the sec header now
        if (bstTokenId != null && bstTokenId.length() > 0) {
            encrKey.prependBSTElementToHeader();
        }
```

### SizeReplaceableByIsEmpty
`utID.length() == 0` can be replaced with 'utID.isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                for (WSSecurityEngineResult wser : wsSecEngineResults) {
                    String utID = (String)wser.get(WSSecurityEngineResult.TAG_ID);
                    if (utID == null || utID.length() == 0) {
                        utID = wssConfig.getIdAllocator().createId("UsernameToken-", null);
                    }
```

### SizeReplaceableByIsEmpty
`outMessage.getAttachments().size() > 0` can be replaced with '!outMessage.getAttachments().isEmpty()'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
        boolean isTextPayload = JMSConstants.TEXT_MESSAGE_TYPE.equals(jmsConfig.getMessageType());
        if (isTextPayload && MessageUtils.getContextualBoolean(outMessage, org.apache.cxf.message.Message.MTOM_ENABLED)
            && outMessage.getAttachments() != null && outMessage.getAttachments().size() > 0) {
            org.apache.cxf.common.i18n.Message msg =
                new org.apache.cxf.common.i18n.Message("INVALID_MESSAGE_TYPE", LOG);
```

### SizeReplaceableByIsEmpty
`val.length() > 0` can be replaced with '!val.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java
            while (m.find()) {
                String val = m.group().trim();
                if (val.length() > 0) {
                    values.add(val);
                }
```

### SizeReplaceableByIsEmpty
`f.length() > 0` can be replaced with '!f.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java
            }
            f = f.length() == 2 ? "" : f.substring(1, f.length() - 1);
            if (f.length() > 0) {
                String[] values = f.split(",");
                for (String v : values) {
```

### SizeReplaceableByIsEmpty
`val.length() > 0` can be replaced with '!val.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
        while (m.find()) {
            String val = m.group().trim();
            if (val.length() > 0) {
                values.add(val);
            }
```

### SizeReplaceableByIsEmpty
`tag.length() > 0` can be replaced with '!tag.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/EntityTagHeaderProvider.java`
#### Snippet
```java
            tag = trimmed;
        }
        if (tag.length() > 0 && !tag.startsWith("\"") && !tag.endsWith("\"")) {
            return new EntityTag(tag, weak);
        }
```

### SizeReplaceableByIsEmpty
`p.length() != 0` can be replaced with '!p.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            PathSegment ps = iter.next();
            String p = ps.getPath();
            if (p.length() != 0 || !iter.hasNext()) {
                p = URITemplate.createExactTemplate(p).encodeLiteralCharacters(false);
                if (sb.length() == 0 && leadingSlash) {
```

### SizeReplaceableByIsEmpty
`thePath.length() > 0` can be replaced with '!thePath.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                b.append(':').append(port);
            }
            if (thePath != null && thePath.length() > 0) {
                b.append(thePath.startsWith("/") || b.length() == 0 || originalPathEmpty
                    ? thePath : '/' + thePath);
```

### SizeReplaceableByIsEmpty
`theQuery.length() != 0` can be replaced with '!theQuery.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                    ? thePath : '/' + thePath);
            }
            if (theQuery != null && theQuery.length() != 0) {
                b.append('?').append(theQuery);
            }
```

### SizeReplaceableByIsEmpty
`thePath.length() != 0` can be replaced with '!thePath.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        } else if (!isSchemeOpaque()) {
            if ((scheme != null || host != null || userInfo != null)
                && thePath.length() != 0 && !(thePath.startsWith("/") || thePath.startsWith(";"))) {
                thePath = "/" + thePath;
            }
```

### SizeReplaceableByIsEmpty
`fragmentTempl.getVariables().size() > 0` can be replaced with '!fragmentTempl.getVariables().isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        if (theFragment != null) {
            URITemplate fragmentTempl = URITemplate.createExactTemplate(theFragment);
            if (fragmentTempl.getVariables().size() > 0) {
                int lengthDiff = values.length  + resolvedTsSize
                    - alreadyResolvedTs.size() - alreadyResolvedTsPathEnc.size() - alreadyResolvedEncTs.size()
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
            while (i <= groupCount) {
                String value = m.group(i++);
                if ((value == null || value.length() == 0 && i < groupCount)
                    && variables.size() + 1 < groupCount) {
                    continue;
```

### SizeReplaceableByIsEmpty
`encoded.length() == 0` can be replaced with 'encoded.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
     */
    public static String encodePartiallyEncoded(String encoded, boolean query) {
        if (encoded.length() == 0) {
            return encoded;
        }
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
                path = HttpUtils.toAbsoluteUri(path, m).getRawPath();
            }
            return (path == null || path.length() == 0) ? "/" : path;
        } catch (URISyntaxException ex) {
            return endpointAddress;
```

### SizeReplaceableByIsEmpty
`thePair.length() == 0` can be replaced with 'thePair.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringConfigurationFactory.java`
#### Snippet
```java
            for (String pair : pairs) {
                String thePair = pair.trim();
                if (thePair.length() == 0) {
                    continue;
                }
```

### SizeReplaceableByIsEmpty
`theValue.length() > 0` can be replaced with '!theValue.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/JAXRSServerFactoryBeanDefinitionParser.java`
#### Snippet
```java
            for (String v : values) {
                String theValue = v.trim();
                if (theValue.length() > 0) {
                    tempFactories.add(new SpringResourceFactory(theValue));
                }
```

### SizeReplaceableByIsEmpty
`thePair.length() == 0` can be replaced with 'thePair.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
            for (String pair : pairs) {
                String thePair = pair.trim();
                if (thePair.length() == 0) {
                    continue;
                }
```

### SizeReplaceableByIsEmpty
`ns.length() > 0` can be replaced with '!ns.isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
        Marshaller ms = createMarshaller(obj, cls, genericType, enc);
        ms.setProperty(Marshaller.JAXB_FRAGMENT, Boolean.TRUE);
        if (ns.length() > 0) {
            Map<String, String> map = new HashMap<>();
            // set the default just in case
```

### SizeReplaceableByIsEmpty
`pkg.length() == 0` can be replaced with 'pkg.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
    private String getPackageName(Method method) {
        String pkg = PackageUtils.getPackageName(method.getDeclaringClass());
        return pkg.length() == 0 ? DEFAULT_PACKAGE_NAME : pkg;
    }

```

### SizeReplaceableByIsEmpty
`pkg.length() == 0` can be replaced with 'pkg.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/WrapperClassLoader.java`
#### Snippet
```java
    private String getPackageName(Method method) {
        String pkg = PackageUtils.getPackageName(method.getDeclaringClass());
        return pkg.length() == 0 ? WrapperClassGenerator.DEFAULT_PACKAGE_NAME : pkg;
    }
}
```

### SizeReplaceableByIsEmpty
`serviceName.getPrefix().length() == 0` can be replaced with 'serviceName.getPrefix().isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
                if (serviceName != null) {
                    String serviceNamePrefix =
                        (serviceName.getPrefix() == null || serviceName.getPrefix().length() == 0)
                        ? "ns2" : serviceName.getPrefix();

```

### SizeReplaceableByIsEmpty
`epi.length() > 0` can be replaced with '!epi.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
            WebService webService = serviceEndpointInterface.getAnnotation(WebService.class);
            String epi = webService.endpointInterface();
            if (epi.length() > 0) {
                try {
                    seiClass = Thread.currentThread().getContextClassLoader().loadClass(epi);
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
        WebService webService = seiClass.getAnnotation(WebService.class);
        String name = webService.name();
        if (name.length() == 0) {
            name = seiClass.getSimpleName();
        }
```

### SizeReplaceableByIsEmpty
`wsdlLocation.length() > 0` can be replaced with '!wsdlLocation.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/WebServiceProviderConfiguration.java`
#### Snippet
```java
    public String getWsdlURL() {
        String wsdlLocation = implInfo.getWsdlLocation();
        if (wsdlLocation != null && wsdlLocation.length() > 0) {
            return wsdlLocation;
        }
```

### SizeReplaceableByIsEmpty
`country.length() > 0` can be replaced with '!country.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
                        String convertedLang = lang.getLanguage();
                        String country = lang.getCountry();
                        if (country.length() > 0) {
                            convertedLang = convertedLang + '-' + country;
                        }
```

### SizeReplaceableByIsEmpty
`bmsg.getMessageParts().size() > 0` can be replaced with '!bmsg.getMessageParts().isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
                }
                if (bmsg != null && bmsg.getMessageParts() != null
                    && bmsg.getMessageParts().size() > 0) {
                    index = bmsg.getMessageParts().get(0).getIndex();
                }
```

### SizeReplaceableByIsEmpty
`tns.length() == 0` can be replaced with 'tns.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (tns == null || tns.length() == 0) {
            final QName wrappername;
            if (input) {
```

### SizeReplaceableByIsEmpty
`tns.length() == 0` can be replaced with 'tns.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            }
        }
        if (tns == null || tns.length() == 0) {
            tns = op.getName().getNamespaceURI();
        }
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (local == null || local.length() == 0) {
            if (Boolean.TRUE.equals(isRPC(method)) || !Boolean.FALSE.equals(isWrapped(method))) {
                local = getDefaultLocalName(op, method, paramNumber, partIndex, prefix);
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
                local = param.partName();
            }
            if (local == null || local.length() == 0) {
                local = param.name();
            }
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (local == null || local.length() == 0) {
            if (Boolean.TRUE.equals(isRPC(method)) || !Boolean.FALSE.equals(isWrapped(method))) {
                local = getDefaultLocalName(op, method, paramNumber, partIndex, prefix);
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        if (wm != null) {
            String name = wm.operationName();
            if (name.length() == 0) {
                name = method.getName();
            }
```

### SizeReplaceableByIsEmpty
`clsName.length() > 0` can be replaced with '!clsName.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            clsName = rw.className();
        }
        if (clsName.length() > 0) {
            return clsName;
        }
```

### SizeReplaceableByIsEmpty
`tns.length() == 0` can be replaced with 'tns.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            local = webResult.name();
        }
        if (tns == null || tns.length() == 0) {
            QName wrappername = getResponseWrapperName(op, method);
            if (wrappername != null) {
```

### SizeReplaceableByIsEmpty
`tns.length() == 0` can be replaced with 'tns.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (tns == null || tns.length() == 0) {
            tns = op.getName().getNamespaceURI();
        }
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (local == null || local.length() == 0) {
            if (Boolean.TRUE.equals(isRPC(method)) || !Boolean.FALSE.equals(isWrapped(method))) {
                local = getDefaultLocalName(op, method, paramNumber, op.getOutput().size(), "return");
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        if (fault != null) {
            String name = fault.name();
            if (name.length() == 0) {
                name = exClass.getSimpleName();
            }
```

### SizeReplaceableByIsEmpty
`ns.length() == 0` can be replaced with 'ns.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            }
            String ns = fault.targetNamespace();
            if (ns.length() == 0) {
                ns = service.getName().getNamespaceURI();
            }
```

### SizeReplaceableByIsEmpty
`wsdlLocation.length() > 0` can be replaced with '!wsdlLocation.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    public String getWsdlURL() {
        String wsdlLocation = implInfo.getWsdlLocation();
        if (wsdlLocation != null && wsdlLocation.length() > 0) {
            return wsdlLocation;
        }
```

### SizeReplaceableByIsEmpty
`clsName.length() > 0` can be replaced with '!clsName.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            clsName = rw.className();
        }
        if (clsName.length() > 0) {
            return clsName;
        }
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
                local = webResult.partName();
            }
            if (local == null || local.length() == 0) {
                local = webResult.name();
            }
```

### SizeReplaceableByIsEmpty
`local.length() == 0` can be replaced with 'local.isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        if (local == null || local.length() == 0) {
            if (Boolean.TRUE.equals(isRPC(method)) || !Boolean.FALSE.equals(isWrapped(method))) {
                local = "return";
```

### SizeReplaceableByIsEmpty
`rmpsOut.getAcks().size() > 0` can be replaced with '!rmpsOut.getAcks().isEmpty()'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMOutInterceptor.java`
#### Snippet
```java
            assert null != to;
            addAcknowledgements(destination, rmpsOut, inSeqId, to);
            if (isPartialResponse && rmpsOut.getAcks() != null && rmpsOut.getAcks().size() > 0) {
                setAction(maps, constants.getSequenceAckAction());
                msg.remove(Message.EMPTY_PARTIAL_RESPONSE_MESSAGE);
```

### SizeReplaceableByIsEmpty
`val.trim().length() > 0` can be replaced with '!val.trim().isEmpty()'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMTxStoreBeanDefinitionParser.java`
#### Snippet
```java
    protected void mapAttribute(BeanDefinitionBuilder bean, Element e, String name, String val) {
        if ("dataSource".equals(name)) {
            if (val != null && val.trim().length() > 0) {
                bean.addPropertyReference("dataSource", val);
            }
```

### SizeReplaceableByIsEmpty
`str.length() == 0` can be replaced with 'str.isEmpty()'
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthJSONProvider.java`
#### Snippet
```java
    public Map<String, String> readJSONResponse(InputStream is) throws IOException  {
        String str = IOUtils.readStringFromStream(is).trim();
        if (str.length() == 0) {
            return Collections.emptyMap();
        }
```

### SizeReplaceableByIsEmpty
`handlerRealmSupport.getSupportedRealms().size() > 0` can be replaced with '!handlerRealmSupport.getSupportedRealms().isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
        // Check whether the handler supports the current realm
        if (handlerRealmSupport.getSupportedRealms() != null
                && handlerRealmSupport.getSupportedRealms().size() > 0
                && handlerRealmSupport.getSupportedRealms().indexOf(parameters.getRealm()) == -1) {
            if (LOG.isLoggable(Level.FINER)) {
```

### SizeReplaceableByIsEmpty
`c.getValues().size() > 0` can be replaced with '!c.getValues().isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
            resultClaimCollection = new ProcessedClaimCollection();
            for (ProcessedClaim c : processedClaims) {
                if (c.getValues() != null && c.getValues().size() > 0) {
                    resultClaimCollection.add(c);
                }
```

### SizeReplaceableByIsEmpty
`op.getFaults().size() > 0` can be replaced with '!op.getFaults().isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/PartialWSDLProcessor.java`
#### Snippet
```java
                bindingOperation.setBindingOutput(getBindingOutput(op.getOutput(), wsdlDefinition, extReg));
            }
            if (op.getFaults() != null && op.getFaults().size() > 0) {
                addSoapFaults(op, bindingOperation, wsdlDefinition, extReg);
            }
```

### SizeReplaceableByIsEmpty
`msgInfo.getMessageParts().size() > 0` can be replaced with '!msgInfo.getMessageParts().isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java

                    if (msgInfo != null && msgInfo.getMessageParts() != null
                        && msgInfo.getMessageParts().size() > 0) {
                        if (msgInfo.getMessageParts().size() > paramNum) {
                            p = msgInfo.getMessageParts().get(paramNum);
```

### SizeReplaceableByIsEmpty
`cfgResource.length() > 0` can be replaced with '!cfgResource.isEmpty()'
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
        String original = System.clearProperty(Configurer.USER_CFG_FILE_PROPERTY_NAME);
        try {
            if (cfgResource != null && cfgResource.length() > 0) {
                System.setProperty(Configurer.USER_CFG_FILE_PROPERTY_NAME, cfgResource);
            }
```

### SizeReplaceableByIsEmpty
`service.getSchemas().size() > 0` can be replaced with '!service.getSchemas().isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
                portTypes.add(buildPortType(service.getInterface(), portTypeDef));

                if (service.getSchemas() != null && service.getSchemas().size() > 0) {
                    if (!useSchemaImports) {
                        buildTypes(service.getSchemas(), imports, portTypeDef);
```

### SizeReplaceableByIsEmpty
`tmpPath.length() > 0` can be replaced with '!tmpPath.isEmpty()'
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
        String tmpPath = new String(path);

        while (tmpPath.length() > 0) {
            if (tmpPath.indexOf("!/") == -1) {
                tokens.add(tmpPath);
```

### SizeReplaceableByIsEmpty
`line.length() > 0` can be replaced with '!line.isEmpty()'
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
        // read headers
        while ((line = readLine(in)) != null) {
            if (line.length() > 0) {
                del = line.indexOf(':');
                if (del < 0) {
```

### SizeReplaceableByIsEmpty
`codeNs.length() > 0` can be replaced with '!codeNs.isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/AbstractSoapInterceptor.java`
#### Snippet
```java
        String codeNs = faultCode.getNamespaceURI();
        String prefix = null;
        if (codeNs.length() > 0) {
            prefix = faultCode.getPrefix();
            if (!StringUtils.isEmpty(prefix)) {
```

### SizeReplaceableByIsEmpty
`event.getPayload().length() > 0` can be replaced with '!event.getPayload().isEmpty()'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrettyLoggingFilter.java`
#### Snippet
```java
            && contentType.toLowerCase().indexOf("multipart/related") < 0
            && event.getPayload() != null
            && event.getPayload().length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`ma.persistPolicy().length() > 0` can be replaced with '!ma.persistPolicy().isEmpty()'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanInfoSupporter.java`
#### Snippet
```java
        }

        if (ma.persistPolicy().length() > 0) {
            desc.setField("persistPolicy", ma.persistPolicy());
        }
```

### SizeReplaceableByIsEmpty
`namespace.length() > 0` can be replaced with '!namespace.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/AttributeWriter.java`
#### Snippet
```java

        try {
            if (namespace.length() > 0) {
                if (XMLConstants.W3C_XML_SCHEMA_INSTANCE_NS_URI.equals(namespace)) {
                    prefix = NamespaceHelper.getUniquePrefix(writer, namespace, "xsi", true);
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java
        String prefix = getPrefixForNamespace(type.getNamespaceURI(), type.getPrefix());
        String value;
        if (prefix != null && prefix.length() > 0) {
            StringBuilder sb = new StringBuilder(prefix.length() + 1 + type.getLocalPart().length());
            sb.append(prefix);
```

### SizeReplaceableByIsEmpty
`nillableVal.length() > 0` can be replaced with '!nillableVal.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/XMLBeanTypeInfo.java`
#### Snippet
```java

            String nillableVal = DOMUtils.getAttributeValueEmptyNull(e, "nillable");
            if (nillableVal != null && nillableVal.length() > 0) {
                ensurePropertyInfo(mappedName).setNillable(Boolean.parseBoolean(nillableVal));
            }
```

### SizeReplaceableByIsEmpty
`minOccurs.length() > 0` can be replaced with '!minOccurs.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/XMLBeanTypeInfo.java`
#### Snippet
```java

            String minOccurs = DOMUtils.getAttributeValueEmptyNull(e, "minOccurs");
            if (minOccurs != null && minOccurs.length() > 0) {
                ensurePropertyInfo(mappedName).setMinOccurs(Integer.parseInt(minOccurs));
            }
```

### SizeReplaceableByIsEmpty
`maxOccurs.length() > 0` can be replaced with '!maxOccurs.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/XMLBeanTypeInfo.java`
#### Snippet
```java
            }
            String maxOccurs = DOMUtils.getAttributeValueEmptyNull(e, "maxOccurs");
            if (maxOccurs != null && maxOccurs.length() > 0) {
                ensurePropertyInfo(mappedName).setMinOccurs(Integer.parseInt(maxOccurs));
            }
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
     */
    protected QName createQName(Element e, String value) {
        if (value == null || value.length() == 0) {
            return null;
        }
```

### SizeReplaceableByIsEmpty
`typeName.getPrefix().length() > 0` can be replaced with '!typeName.getPrefix().isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/ArrayTypeInfo.java`
#### Snippet
```java

        // typeName: foo:bar
        if (typeName.getPrefix() != null && typeName.getPrefix().length() > 0) {
            string.append(typeName.getPrefix()).append(':');
        }
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/W3CNamespaceContext.java`
#### Snippet
```java
    public String getNamespaceURI(String prefix) {
        String name = prefix;
        if (name.length() == 0) {
            name = "xmlns";
        } else {
```

### SizeReplaceableByIsEmpty
`elPrefix.length() == 0` can be replaced with 'elPrefix.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/jdom/StaxSerializer.java`
#### Snippet
```java

        if (writeElementNS) {
            if (elPrefix == null || elPrefix.length() == 0) {
                writer.writeDefaultNamespace(elUri);
            } else {
```

### SizeReplaceableByIsEmpty
`name.length() == 0` can be replaced with 'name.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        }

        if (name == null || name.length() == 0) {
            name = ServiceUtils.makeServiceNameFromClassName(typeClass);
        }
```

### SizeReplaceableByIsEmpty
`ns.length() == 0` can be replaced with 'ns.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        }
        //check from aegis type annotation
        if (ns == null || ns.length() == 0) {
            ns = annotationReader.getNamespace(typeClass);
        }
```

### SizeReplaceableByIsEmpty
`ns.length() == 0` can be replaced with 'ns.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        }
        // check jaxb package annotation
        if (ns == null || ns.length() == 0) {
            ns = annotationReader.getNamespace(typeClass.getPackage());
        }
```

### SizeReplaceableByIsEmpty
`ns.length() == 0` can be replaced with 'ns.isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
            ns = annotationReader.getNamespace(typeClass.getPackage());
        }
        if (ns == null || ns.length() == 0) {
            ns = NamespaceHelper.makeNamespaceFromClassName(typeClass.getName(), "http");
        }
```

### SizeReplaceableByIsEmpty
`v.length() != 0` can be replaced with '!v.isEmpty()'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactoryBean.java`
#### Snippet
```java
            String[] values = value.split(",");
            for (String v : values) {
                if (v.length() != 0) {
                    headers.add(key, v);
                }
```

### SizeReplaceableByIsEmpty
`entry.getValue().size() > 0` can be replaced with '!entry.getValue().isEmpty()'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientResponseFilterInterceptor.java`
#### Snippet
```java
                continue;
            }
            if (entry.getValue().size() > 0) {
                for (String val : entry.getValue()) {
                    rb.header(entry.getKey(), val);
```

### SizeReplaceableByIsEmpty
`typeStr.length() > 0` can be replaced with '!typeStr.isEmpty()'
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java

    private static int resolveAttrType(String typeStr) {
        if (typeStr != null && typeStr.length() > 0) {
            Integer i = ATTR_TYPES.get(typeStr);
            if (i != null) {
```

### SizeReplaceableByIsEmpty
`nsURI.length() == 0` can be replaced with 'nsURI.isEmpty()'
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
                String ln = r.getLocalName();

                if (nsURI == null || nsURI.length() == 0) {
                    if (elemPrefix == null || elemPrefix.length() == 0) {
                        newElem = f.element(ln);
```

### SizeReplaceableByIsEmpty
`elemPrefix.length() == 0` can be replaced with 'elemPrefix.isEmpty()'
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java

                if (nsURI == null || nsURI.length() == 0) {
                    if (elemPrefix == null || elemPrefix.length() == 0) {
                        newElem = f.element(ln);
                    } else {
```

### SizeReplaceableByIsEmpty
`prefix.length() == 0` can be replaced with 'prefix.isEmpty()'
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
                    Namespace ns;

                    if (prefix == null || prefix.length() == 0) {
                        // Attribute not in any namespace
                        ns = Namespace.NO_NAMESPACE;
```

## RuleId[ruleID=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java


    protected static final boolean equals(Object o1, Object o2) {
        return Objects.equals(o1, o2);
    }
```

### FinalStaticMethod
'static' method declared `final`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
    }

    static final TokenStore getTokenStore(Message message) throws TokenStoreException {
        if (message == null) {
            return null;
```

### FinalStaticMethod
'static' method declared `final`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spring/EndpointDefinitionParser.java`
#### Snippet
```java
    }

    public static final void setBlocking(ApplicationContext ctx, EndpointImpl impl) {
        AutowireCapableBeanFactory fact = ctx.getAutowireCapableBeanFactory();
        if (fact instanceof DefaultListableBeanFactory) {
```

## RuleId[ruleID=RegExpRedundantNestedCharacterClass]
### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/2001/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/2001/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `core/src/main/resources/schemas/wsdl/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpRedundantNestedCharacterClass
Redundant nested character class
in `core/src/main/resources/schemas/wsdl/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

## RuleId[ruleID=SuspiciousInvocationHandlerImplementation]
### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/InterfaceProxy.java`
#### Snippet
```java
        }
        map.put(property, args[0]);
        return null;
    }

```

### SuspiciousInvocationHandlerImplementation
Null might be returned when proxying method 'equals()': this may cause NullPointerException
in `core/src/main/java/org/apache/cxf/common/jaxb/EscapeHandlerInvocationHandler.java`
#### Snippet
```java
            result =  method.invoke(target, args);
        } 
        return result;
    }
    
```

## RuleId[ruleID=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/FragmentDialect.java`
#### Snippet
```java
    public JAXBElement<ValueType> processGet(Get body, Representation representation) {
        for (Object o : body.getAny()) {
            if (o instanceof JAXBElement<?> && ((JAXBElement<?>)o).getDeclaredType() == ExpressionType.class) {
                ExpressionType expression = (ExpressionType) ((JAXBElement<?>)o).getValue();
                String languageIRI = expression.getLanguage();
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
            ret = codec.decodeMAP(clz, headerElement, unmarshaller);
        } else if (Names200408.WSA_NAMESPACE_NAME.equals(encodedAs)) {
            if (AttributedURIType.class.equals(clz)) {
                ret = clz.cast(convert(codec.decodeMAP(AttributedURI.class, headerElement, unmarshaller)));
            } else if (EndpointReferenceType.class.equals(clz)) {
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
            if (AttributedURIType.class.equals(clz)) {
                ret = clz.cast(convert(codec.decodeMAP(AttributedURI.class, headerElement, unmarshaller)));
            } else if (EndpointReferenceType.class.equals(clz)) {
                ret = clz.cast(convert(codec.decodeMAP(Names200408.EPR_TYPE,
                                                       headerElement, unmarshaller)));
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                ret = clz.cast(convert(codec.decodeMAP(Names200408.EPR_TYPE,
                                                       headerElement, unmarshaller)));
            } else if (RelatesToType.class.equals(clz)) {
                ret = clz.cast(convert(codec.decodeMAP(Relationship.class, headerElement, unmarshaller)));
            }
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
            }
        } else if (org.apache.cxf.ws.addressing.VersionTransformer.Names200403.WSA_NAMESPACE_NAME.equals(encodedAs)) {
            if (AttributedURIType.class.equals(clz)) {
                ret = clz.cast(convert(codec
                    .decodeMAP(org.apache.cxf.ws.addressing.v200403.AttributedURI.class, headerElement,
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                    .decodeMAP(org.apache.cxf.ws.addressing.v200403.AttributedURI.class, headerElement,
                               unmarshaller)));
            } else if (EndpointReferenceType.class.equals(clz)) {
                ret = clz.cast(convert(codec.decodeMAP(Names200403.EPR_TYPE,
                                                       headerElement, unmarshaller)));
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                ret = clz.cast(convert(codec.decodeMAP(Names200403.EPR_TYPE,
                                                       headerElement, unmarshaller)));
            } else if (RelatesToType.class.equals(clz)) {
                ret = clz.cast(convert(codec
                    .decodeMAP(org.apache.cxf.ws.addressing.v200403.Relationship.class, headerElement,
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
            } else if (org.apache.cxf.ws.addressing.VersionTransformer.Names200408
                            .WSA_NAMESPACE_NAME.equals(exposeAs)) {
                if (AttributedURIType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convert((AttributedURIType)value), new QName(exposeAs, localName),
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                                    convert((AttributedURIType)value), new QName(exposeAs, localName),
                                    AttributedURI.class, marshaller, mustUnderstand);
                } else if (EndpointReferenceType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convert((EndpointReferenceType)value), new QName(exposeAs, localName),
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                                    org.apache.cxf.ws.addressing.VersionTransformer.Names200408.EPR_TYPE,
                                    marshaller, mustUnderstand);
                } else if (RelatesToType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convert((RelatesToType)value), new QName(exposeAs, localName),
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
            } else if (org.apache.cxf.ws.addressing.VersionTransformer.Names200403
                            .WSA_NAMESPACE_NAME.equals(exposeAs)) {
                if (AttributedURIType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convertTo200403((AttributedURIType)value),
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                                    org.apache.cxf.ws.addressing.v200403.AttributedURI.class,
                                    marshaller, mustUnderstand);
                } else if (EndpointReferenceType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convertTo200403((EndpointReferenceType)value), new QName(exposeAs,
```

### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'Class' and 'Class'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
                                    org.apache.cxf.ws.addressing.VersionTransformer.Names200403.EPR_TYPE,
                                    marshaller, mustUnderstand);
                } else if (RelatesToType.class.equals(clz)) {
                    codec.encodeMAP(message,
                                    convertTo200403((RelatesToType)value), new QName(exposeAs, localName),
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            JAXBElement<?> useKeyJaxb = (JAXBElement<?>)useKey.getAny();
            Object obj = useKeyJaxb.getValue();
            if (KeyInfoType.class == useKeyJaxb.getDeclaredType() || obj instanceof KeyInfoType) {
                KeyInfoType keyInfoType = KeyInfoType.class.cast(useKeyJaxb.getValue());
                LOG.fine("Found KeyInfo UseKey type");
```

### EqualsBetweenInconvertibleTypes
`==` between objects of inconvertible types 'Class' and 'Class'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
                    }
                }
            } else if (SecurityTokenReferenceType.class == useKeyJaxb.getDeclaredType()
                || obj instanceof SecurityTokenReferenceType) {
                SecurityTokenReferenceType strType =
```

## RuleId[ruleID=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/cache/EHCacheXKMSClientCache.java`
#### Snippet
```java
                    File file = diskstorePath.toFile();
                    if (file.exists() && file.canWrite()) {
                        file.delete();
                    }
                }
```

### IgnoreResultOfCall
Result of `ResourceBundle.getLocale()` is ignored
in `core/src/main/java/org/apache/cxf/common/logging/LogUtils.java`
#### Snippet
```java
            }
            if (b != null) {
                b.getLocale();
            }

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
            }
            if (file.createNewFile()) {
                file.delete();
            }
        } catch (IOException e) {
```

### IgnoreResultOfCall
Result of `PushbackInputStream.read()` is ignored
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                // Last mime boundary should have a succeeding "--"
                // as we are on it, read the terminating CRLF
                inStream.read();
                inStream.read();
            }
```

### IgnoreResultOfCall
Result of `PushbackInputStream.read()` is ignored
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                // as we are on it, read the terminating CRLF
                inStream.read();
                inStream.read();
            }
            return -1;
```

### IgnoreResultOfCall
Result of `PushbackInputStream.read()` is ignored
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
                    // boundary found, read the newline
                    if (value == 13) {
                        pushbackInStream.read();
                    }
                    return true;
```

### IgnoreResultOfCall
Result of `String.getBytes()` is ignored
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        }
        try {
            "0".getBytes(enc);
            return enc;
        } catch (UnsupportedEncodingException ex) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
        System.setProperty("org.apache.cxf.JDKBugHacks.defaultUsesCaches", "true");
        File outputDirFile = new File(outputDir);
        outputDirFile.mkdirs();

        if (idltowsdlOptions == null) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
                try {
                    IDLToWSDL.run(list.toArray(new String[0]));
                    doneFile.delete();
                    doneFile.createNewFile();
                } catch (Throwable e) {
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
                    IDLToWSDL.run(list.toArray(new String[0]));
                    doneFile.delete();
                    doneFile.createNewFile();
                } catch (Throwable e) {
                    e.printStackTrace();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
        }

        outputDir.mkdirs();

        List<URL> urlList = new ArrayList<>();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
                    try {
                        WSDLToIDL.run(list.toArray(new String[0]));
                        doneFile.delete();
                        doneFile.createNewFile();
                    } catch (Throwable e) {
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
                        WSDLToIDL.run(list.toArray(new String[0]));
                        doneFile.delete();
                        doneFile.createNewFile();
                    } catch (Throwable e) {
                        e.printStackTrace();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
            } else {
                if (now) {
                    f.delete();
                } else {
                    f.deleteOnExit();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
        }
        if (now) {
            dir.delete();
        } else {
            dir.deleteOnExit();
```

### IgnoreResultOfCall
Result of `ByteArrayInputStream.read()` is ignored
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java

            case CONSTANT_METHOD_HANDLE:
                read(); // reference kind
                readShort(); // reference index
                break;
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            byte[] data = new byte[fileSize *  count];
            for (int x = 0; x < count; x++) {
                this.getClass().getResourceAsStream("/wsdl/mtom_xop.wsdl").read(data,
                                                                                fileSize * x,
                                                                                fileSize);
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
            // down the server
            //
            System.in.read();
            System.out.println("stopping bus");
            tearDown();
```

### IgnoreResultOfCall
Result of `InputStream.read()` is ignored
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/InMessageRecorder.java`
#### Snippet
```java
            if (is != null) {
                byte[] b = new byte[is.available()];
                is.read(b);
                inbound.add(b);
                if (LOG.isLoggable(Level.FINE)) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java

        if (doWork) {
            doneFile.delete();

            List<String> list = new ArrayList<>();
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java
                ok = validator.executeForMaven();

                doneFile.createNewFile();
            } catch (Throwable e) {
                throw new MojoExecutionException(file.getName() + ": "
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java
        System.setProperty("org.apache.cxf.JDKBugHacks.defaultUsesCaches", "true");

        markerDirectory.mkdirs();

        List<File> wsdls = new ArrayList<>();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java

            File certFile = new File(storageDir + "/" + CRLS_PATH, path);
            certFile.getParentFile().mkdirs();
            try (OutputStream os = Files.newOutputStream(certFile.toPath());
                BufferedOutputStream bos = new BufferedOutputStream(os)) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            File certFile = new File(storageDir + "/" + category,
                                     getCertPath(cert, id));
            certFile.getParentFile().mkdirs();
            try (OutputStream os = Files.newOutputStream(certFile.toPath());
                BufferedOutputStream bos = new BufferedOutputStream(os)) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WADL2JavaMojo.java`
#### Snippet
```java

        File classesDir = new File(classesDirectory);
        classesDir.mkdirs();
        markerDirectory.mkdirs();

```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WADL2JavaMojo.java`
#### Snippet
```java
        File classesDir = new File(classesDirectory);
        classesDir.mkdirs();
        markerDirectory.mkdirs();

        // add the generated source into compile source
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java

        if (file != null) {
            file.delete();
        }
        if (StringUtils.isNotEmpty(err.getOutput()) && err.getOutput().contains("WADL2Java Error")) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
                              Set<URI> classPath) throws MojoExecutionException {
        File outputDirFile = option.getOutputDir();
        outputDirFile.mkdirs();
        URI basedir = project.getBasedir().toURI();

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
                return bus;
            }
            doneFile.delete();

            List<String> list = option.generateCommandLine(outputDirFile, basedir, wadlURI, getLog()
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java

            try {
                doneFile.createNewFile();
            } catch (Throwable e) {
                getLog().warn("Could not create marker file " + doneFile.getAbsolutePath());
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
        for (WadlOption option : effectiveOptions) {
            File outputDirFile = option.getOutputDir();
            outputDirFile.mkdirs();
            URI basedir = project.getBasedir().toURI();
            for (URI wadlURI : option.getWadlURIs(basedir, getResourceLoader())) {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
                    continue;
                }
                doneFile.delete();

                toDo.add(option);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
                File doneFile = getDoneFile(basedir, wadlURI);
                try {
                    doneFile.createNewFile();
                } catch (Throwable e) {
                    getLog().warn("Could not create marker file " + doneFile.getAbsolutePath());
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
        WsdlOption wsdlOption = (WsdlOption)genericWsdlOption;
        File outputDirFile = wsdlOption.getOutputDir();
        outputDirFile.mkdirs();
        URI basedir = project.getBasedir().toURI();
        URI wsdlURI;
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
            return bus;
        }
        doneFile.delete();

        List<String> list = wsdlOption.generateCommandLine(outputDirFile, basedir, wsdlURI, getLog()
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
        WsdlOption wsdlOption = (WsdlOption) genericWsdlOption;
        File outputDirFile = wsdlOption.getOutputDir();
        outputDirFile.mkdirs();
        URI basedir = project.getBasedir().toURI();
        URI wsdlURI = wsdlOption.getWsdlURI(basedir);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
            return bus;
        }
        doneFile.delete();

        try {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
                    for (File f : oldFiles) {
                        if (!files.contains(f)) {
                            f.delete();
                            buildContext.refresh(f);
                        }
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java

    protected void createMarkerFile(GenericWsdlOption wsdlOption, File doneFile, URI wsdlURI) throws IOException {
        doneFile.createNewFile();
        URI basedir = project.getBasedir().toURI();
        String options = wsdlOption.generateCommandLine(null, basedir, wsdlURI, false).toString();
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        }

        file.delete();
        if (b.toString().contains("WSDL2Java Error")) {
            StringBuilder msg = new StringBuilder();
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
         * This shouldn't be needed, but it's harmless.
         */
        classesDir.mkdirs();

        if (includes == null) {
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        }

        markerDirectory.mkdirs();

        String originalProxyHost = SystemPropertyAction.getProperty(HTTP_PROXY_HOST);
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        for (GenericWsdlOption wsdlOption : effectiveWsdlOptions) {
            File outputDirFile = wsdlOption.getOutputDir();
            outputDirFile.mkdirs();
            URI basedir = project.getBasedir().toURI();
            URI wsdlURI = getWsdlURI(wsdlOption, basedir);
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                continue;
            }
            doneFile.delete();

            toDo.add(wsdlOption);
```

### IgnoreResultOfCall
Result of `File.createNewFile()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java

    protected void createMarkerFile(GenericWsdlOption wsdlOption, File doneFile, URI wsdlURI) throws IOException {
        doneFile.createNewFile();
    }

```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java

        File outputDirFile = wsdlOption.getOutputDir();
        outputDirFile.mkdirs();
        URI basedir = project.getBasedir().toURI();
        URI wsdlURI = getWsdlURI(wsdlOption, basedir);
```

## RuleId[ruleID=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/WSAContextUtils.java`
#### Snippet
```java
    public static boolean retrieveUsingAddressing(Message message) {
        Boolean override = (Boolean)message.get(USING_PROPERTY);
        return override == null || override.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
                      ? (Boolean)message.getExchange().get("defer.uncorrelated.message.abort")
                      : null;
        return ret != null && ret.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
    public static boolean retrievePartialResponseSent(Message message) {
        Boolean ret = (Boolean)message.get(PARTIAL_RESPONSE_SENT_PROPERTY);
        return ret != null && ret.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
    public static boolean retrieveAsyncPostResponseDispatch(Message message) {
        Boolean ret = (Boolean)message.get(Message.ASYNC_POST_RESPONSE_DISPATCH);
        return ret != null && ret.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
                      ? (Boolean)message.getExchange().get("deferred.uncorrelated.message.abort")
                      : null;
        return ret != null && ret.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
    public static boolean isRequestor(Message message) {
        Boolean requestor = (Boolean)message.get(Message.REQUESTOR_ROLE);
        return requestor != null && requestor.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/common/logging/JDKBugHacks.java`
#### Snippet
```java
                        Method method = clazz.getDeclaredMethod("currentLatencyTarget");
                        Long l = (Long)method.invoke(null);
                        if (l != null && l.longValue() == 0) {
                            //something already set it, move on
                            method = clazz.getDeclaredMethod("requestLatency", Long.TYPE);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/message/MessageUtils.java`
#### Snippet
```java
        Integer i = (Integer)message.get(Message.RESPONSE_CODE);
        if (i != null) {
            return i.intValue();
        }
        int code = hasNoResponseContent(message) ? HttpURLConnection.HTTP_ACCEPTED : HttpURLConnection.HTTP_OK;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            final boolean local;
            if (o instanceof Boolean) {
                local = ((Boolean)o).booleanValue();
            } else {
                local = Boolean.parseBoolean(o.toString());
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        */
        final StAXDocumentSerializer stAXDocumentSerializer = new StAXDocumentSerializer(out);
        if (serializerAttributeValueMapMemoryLimit != null && serializerAttributeValueMapMemoryLimit.intValue() > 0) {
            stAXDocumentSerializer.setAttributeValueMapMemoryLimit(serializerAttributeValueMapMemoryLimit.intValue());
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        final StAXDocumentSerializer stAXDocumentSerializer = new StAXDocumentSerializer(out);
        if (serializerAttributeValueMapMemoryLimit != null && serializerAttributeValueMapMemoryLimit.intValue() > 0) {
            stAXDocumentSerializer.setAttributeValueMapMemoryLimit(serializerAttributeValueMapMemoryLimit.intValue());
        }
        if (serializerMinAttributeValueSize != null && serializerMinAttributeValueSize.intValue() > 0) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
            stAXDocumentSerializer.setAttributeValueMapMemoryLimit(serializerAttributeValueMapMemoryLimit.intValue());
        }
        if (serializerMinAttributeValueSize != null && serializerMinAttributeValueSize.intValue() > 0) {
            stAXDocumentSerializer.setMinAttributeValueSize(serializerMinAttributeValueSize.intValue());
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        }
        if (serializerMinAttributeValueSize != null && serializerMinAttributeValueSize.intValue() > 0) {
            stAXDocumentSerializer.setMinAttributeValueSize(serializerMinAttributeValueSize.intValue());
        }
        if (serializerMaxAttributeValueSize != null && serializerMaxAttributeValueSize.intValue() > 0) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
            stAXDocumentSerializer.setMinAttributeValueSize(serializerMinAttributeValueSize.intValue());
        }
        if (serializerMaxAttributeValueSize != null && serializerMaxAttributeValueSize.intValue() > 0) {
            stAXDocumentSerializer.setMaxAttributeValueSize(serializerMaxAttributeValueSize.intValue());
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        }
        if (serializerMaxAttributeValueSize != null && serializerMaxAttributeValueSize.intValue() > 0) {
            stAXDocumentSerializer.setMaxAttributeValueSize(serializerMaxAttributeValueSize.intValue());
        }
        if (serializerCharacterContentChunkMapMemoryLimit != null
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        }
        if (serializerCharacterContentChunkMapMemoryLimit != null
                && serializerCharacterContentChunkMapMemoryLimit.intValue() > 0) {
            stAXDocumentSerializer
                    .setCharacterContentChunkMapMemoryLimit(serializerCharacterContentChunkMapMemoryLimit.intValue());
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
                && serializerCharacterContentChunkMapMemoryLimit.intValue() > 0) {
            stAXDocumentSerializer
                    .setCharacterContentChunkMapMemoryLimit(serializerCharacterContentChunkMapMemoryLimit.intValue());
        }
        if (serializerMinCharacterContentChunkSize != null && serializerMinCharacterContentChunkSize.intValue() > 0) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
                    .setCharacterContentChunkMapMemoryLimit(serializerCharacterContentChunkMapMemoryLimit.intValue());
        }
        if (serializerMinCharacterContentChunkSize != null && serializerMinCharacterContentChunkSize.intValue() > 0) {
            stAXDocumentSerializer.setMinCharacterContentChunkSize(serializerMinCharacterContentChunkSize.intValue());
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        }
        if (serializerMinCharacterContentChunkSize != null && serializerMinCharacterContentChunkSize.intValue() > 0) {
            stAXDocumentSerializer.setMinCharacterContentChunkSize(serializerMinCharacterContentChunkSize.intValue());
        }
        if (serializerMaxCharacterContentChunkSize != null && serializerMaxCharacterContentChunkSize.intValue() > 0) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
            stAXDocumentSerializer.setMinCharacterContentChunkSize(serializerMinCharacterContentChunkSize.intValue());
        }
        if (serializerMaxCharacterContentChunkSize != null && serializerMaxCharacterContentChunkSize.intValue() > 0) {
            stAXDocumentSerializer.setMaxCharacterContentChunkSize(serializerMaxCharacterContentChunkSize.intValue());
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
        }
        if (serializerMaxCharacterContentChunkSize != null && serializerMaxCharacterContentChunkSize.intValue() > 0) {
            stAXDocumentSerializer.setMaxCharacterContentChunkSize(serializerMaxCharacterContentChunkSize.intValue());
        }
        return stAXDocumentSerializer;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsSubscription.java`
#### Snippet
```java
                XPathExpression exp = xpath.compile(contentFilter.getContent().get(0).toString());
                Boolean ret = (Boolean) exp.evaluate(content, XPathConstants.BOOLEAN);
                return ret.booleanValue();
            } catch (XPathExpressionException e) {
                LOGGER.log(Level.WARNING, "Could not filter notification", e);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java

        // soap12 arg
        if (soap12 != null && soap12.booleanValue()) {
            args.add("-soap12");
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java

        // verbose arg
        if (verbose != null && verbose.booleanValue()) {
            args.add("-verbose");
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java

        // quiet arg
        if (quiet != null && quiet.booleanValue()) {
            args.add("-quiet");
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            return Boolean.parseBoolean((String)s);
        } else if (s instanceof Boolean) {
            return ((Boolean)s).booleanValue();
        }
        return defaultv;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            }
            if (st instanceof Boolean) {
                return ((Boolean)st).booleanValue() ? ALWAYS : NEVER;
            }
            return ASYNC_ONLY;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            return Boolean.parseBoolean((String)s);
        } else if (s instanceof Boolean) {
            return ((Boolean)s).booleanValue();
        }
        return defaultv;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            }
            if (st instanceof Boolean) {
                return ((Boolean)st).booleanValue() ? ALWAYS : NEVER;
            }
            return ASYNC_ONLY;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JOutInterceptor.java`
#### Snippet
```java
                        if (entry.getValue() instanceof Class<?>) {
                            removedAction = config.setAction(
                                    entry.getKey().intValue(),
                                    (Class<?>)entry.getValue());
                        } else if (entry.getValue() instanceof Action) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JOutInterceptor.java`
#### Snippet
```java
                        } else if (entry.getValue() instanceof Action) {
                            removedAction = config.setAction(
                                    entry.getKey().intValue(),
                                    (Action)entry.getValue());
                        } else {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
            for (WSSecurityEngineResult wser : wsSecEngineResults) {
                Integer actInt = (Integer)wser.get(WSSecurityEngineResult.TAG_ACTION);
                if (actInt.intValue() == WSConstants.ST_SIGNED
                    || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                    Instant created = Instant.now();
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
                Integer actInt = (Integer)wser.get(WSSecurityEngineResult.TAG_ACTION);
                if (actInt.intValue() == WSConstants.ST_SIGNED
                    || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                    Instant created = Instant.now();
                    Instant expires = created.plusSeconds(WSS4JUtils.getSecurityTokenLifetime(message) / 1000L);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
            WSSecurityEngineResult result = results.get(i);
            Integer actInt = (Integer)result.get(WSSecurityEngineResult.TAG_ACTION);
            if (actInt.intValue() == WSConstants.SIGN && !foundPrimarySignature) {
                foundPrimarySignature = true;
                Element sigElement =
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
                    return false;
                }
            } else if (actInt.intValue() == WSConstants.SC) {
                Element sigElement =
                    (Element)result.get(WSSecurityEngineResult.TAG_TOKEN_ELEMENT);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
                CastUtils.cast((List<?>)result.get(WSSecurityEngineResult.TAG_DATA_REF_URIS));

            if (actInt.intValue() == WSConstants.ENCR && el != null) {
                encrypted = true;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
            }
            // Don't count an endorsing signature
            if (actInt.intValue() == WSConstants.SIGN && el != null
                && !(el.size() == 1 && el.get(0).getName().equals(SIG_QNAME))) {
                return encrypted;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
                    return token;
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
                    (SamlAssertionWrapper)token.get(WSSecurityEngineResult.TAG_SAML_ASSERTION);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
        for (WSSecurityEngineResult wser : results) {
            Integer actInt = (Integer)wser.get(WSSecurityEngineResult.TAG_ACTION);
            if (actInt.intValue() == WSConstants.ENCR) {
                List<WSDataRef> el =
                    CastUtils.cast((List<?>)wser.get(WSSecurityEngineResult.TAG_DATA_REF_URIS));
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java

            // Don't count an endorsing signature
            if (actInt.intValue() == WSConstants.SIGN && el != null
                && !(el.size() == 1 && el.get(0).getName().equals(SIG_QNAME))) {
                signed = true;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
                signed = true;
            }
            if (actInt.intValue() == WSConstants.ENCR && el != null) {
                return signed;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
                    return i;
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
                    (SamlAssertionWrapper)token.get(WSSecurityEngineResult.TAG_SAML_ASSERTION);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
            }
            Integer firstAction = (Integer)results.get(results.size() - 1).get(WSSecurityEngineResult.TAG_ACTION);
            if (firstAction.intValue() != WSConstants.TS) {
                return false;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
            Integer lastAction =
                (Integer)results.get(0).get(WSSecurityEngineResult.TAG_ACTION);
            if (lastAction.intValue() != WSConstants.TS) {
                return false;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            for (WSSecurityEngineResult wser : wsSecEngineResults) {
                Integer actInt = (Integer)wser.get(WSSecurityEngineResult.TAG_ACTION);
                if (actInt.intValue() == WSConstants.UT) {
                    UsernameTokenPrincipal principal
                        = (UsernameTokenPrincipal)wser.get(WSSecurityEngineResult.TAG_PRINCIPAL);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
                    return true;
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
                }
            } else if (actInt.intValue() == WSConstants.ST_SIGNED
                || actInt.intValue() == WSConstants.ST_UNSIGNED) {
                SamlAssertionWrapper assertionWrapper =
                    (SamlAssertionWrapper)token.get(WSSecurityEngineResult.TAG_SAML_ASSERTION);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    protected static boolean isSetReplyTo(Message message) {
        Boolean ret = (Boolean)message.get(JMSConstants.JMS_SET_REPLY_TO);
        return ret == null || ret.booleanValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurationImpl.java`
#### Snippet
```java
            Feature feature = entry.getKey();
            Boolean enabled = entry.getValue();
            if (f.isAssignableFrom(feature.getClass()) && enabled.booleanValue()) {
                return true;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBAttachmentMarshaller.java`
#### Snippet
```java
        super();
        if (mtomThreshold != null) {
            threshold = mtomThreshold.intValue();
        }
        atts = attachments;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderCache.java`
#### Snippet
```java
            public Integer run() {
                return SystemPropertyAction.getInteger("org.apache.cxf.jaxrs.max_provider_cache_size", 100);
            } }).intValue();

    private final Map<String, List<ProviderInfo<MessageBodyReader<?>>>>
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMConfiguration.java`
#### Snippet
```java
     */
    public long getInactivityTimeoutTime() {
        return inactivityTimeout == null ? 0 : inactivityTimeout.longValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMConfiguration.java`
#### Snippet
```java
     */
    public long getAcknowledgementIntervalTime() {
        return acknowledgementInterval == null ? 0 : acknowledgementInterval.longValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            RMConfiguration cfg = manager.getEffectiveConfiguration(message);
            long baseRetransmissionInterval =
                cfg.getBaseRetransmissionInterval().longValue();
            backoff = cfg.isExponentialBackoff()  ? RetransmissionQueue.DEFAULT_EXPONENTIAL_BACKOFF : 1;
            next = new Date(System.currentTimeMillis() + baseRetransmissionInterval);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/attachment/JAXBAttachmentMarshaller.java`
#### Snippet
```java
        super();
        if (mtomThreshold != null) {
            threshold = mtomThreshold.intValue();
        }
        atts = attachments;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                endpoint.put(USING_ADDRESSING, b);
            } else {
                ret = b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            keyRequirements.setKeyType(keyType);
        } else if (QNameConstants.KEY_SIZE.equals(jaxbElement.getName())) {
            long keySize = ((Long)jaxbElement.getValue()).longValue();
            keyRequirements.setKeySize(keySize);
        } else if (QNameConstants.SIGNATURE_ALGORITHM.equals(jaxbElement.getName())) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
            return DEFAULT_INDENT_LEVEL;
        }
        return result.intValue();
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/cxf/ordered_param_holder/OrderedParamHolderImpl.java`
#### Snippet
```java

    public void orderedParamHolder(Holder<ComplexStruct> part3, Holder<Integer> part2, Holder<String> part1) {
        part2.value = Integer.valueOf(part2.value.intValue() + 1);
        part1.value = "return " + part1.value;
        part3.value.elem1 = "return " + part3.value.elem1;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/cxf/test/XPathAssert.java`
#### Snippet
```java
        }

        if (!b.booleanValue()) {
            Assert.fail("Boolean XPath assertion evaluated to false:\n"
                        + xpath
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
    public int getInvocationCount(String method) {
        if (invocationCount.containsKey(method)) {
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
    public int getInvocationCount(String method) {
        if (invocationCount.containsKey(method)) {
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    public int getInvocationCount(String method) {
        if (invocationCount.containsKey(method)) {
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    public int getInvocationCount(String method) {
        if (invocationCount.containsKey(method)) {
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.hasOutMessage(m);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isInParam(method, j);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isInOutParam(method, j);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isOperation(method);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                if (b != null) {
                    wrappedCache.put(method, b);
                    return b.booleanValue();
                }
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isAsync(method);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isHeader(method, j);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                if (b != null) {
                    isRpcCache.put(method, b);
                    return b.booleanValue();
                }
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isHolder(cls, type);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Boolean b = c.isOutParam(method, j);
            if (b != null) {
                return b.booleanValue();
            }
        }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_char:
            char charValue = ((Character)value).charValue();
            // outside the normal range it will -256
            data = Byte.toString((byte)(charValue > Byte.MAX_VALUE
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_char:
                any.insert_char(((Character)value).charValue());
                break;
            case TCKind._tk_wchar:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_wchar:
                any.insert_wchar(((Character)value).charValue());
                break;
            case TCKind._tk_octet:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_octet:
                any.insert_octet(((Byte)value).byteValue());
                break;
            case TCKind._tk_short:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_short:
                any.insert_short(((Short)value).shortValue());
                break;
            case TCKind._tk_ushort:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_long:
                any.insert_long(((Integer)value).intValue());
                break;
            case TCKind._tk_longlong:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_longlong:
                any.insert_longlong(((Long)value).longValue());
                break;
            case TCKind._tk_ulong:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            // This means that we can directly write out the chars in the normal
            // range 0-127 even when using UTF-8
            obj = Character.valueOf((char)(byteValue.byteValue() < 0
                                         ? byteValue.byteValue() + UNSIGNED_MAX
                                         : byteValue.byteValue()));
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            // range 0-127 even when using UTF-8
            obj = Character.valueOf((char)(byteValue.byteValue() < 0
                                         ? byteValue.byteValue() + UNSIGNED_MAX
                                         : byteValue.byteValue()));
            break;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            obj = Character.valueOf((char)(byteValue.byteValue() < 0
                                         ? byteValue.byteValue() + UNSIGNED_MAX
                                         : byteValue.byteValue()));
            break;
        case TCKind._tk_wchar:
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaUnionEventProducer.java`
#### Snippet
```java
            CorbaPrimitiveHandler descHandler = (CorbaPrimitiveHandler) handler.getDiscriminator();
            Boolean descValue = (Boolean) descHandler.getValue();
            if (!descValue.booleanValue()) {
                isItNil = true;
            }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_char((char)0);
        } else {
            stream.write_char(c.charValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_long(0);
        } else {
            stream.write_long(l.intValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_wchar((char)0);
        } else {
            stream.write_wchar(c.charValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_double(0.0);
        } else {
            stream.write_double(d.doubleValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_float((float)0.0);
        } else {
            stream.write_float(f.floatValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_boolean(false);
        } else {
            stream.write_boolean(b.booleanValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_short((short)0);
        } else {
            stream.write_short(s.shortValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_octet((byte)0);
        } else {
            stream.write_octet(b.byteValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
            stream.write_longlong(0);
        } else {
            stream.write_longlong(l.longValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
        boolean retVal = false;

        if (mcf.getMonitorEJBServiceProperties().booleanValue()) {
            URL url = mcf.getEJBServicePropertiesURLInstance();
            if (url == null) {
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
    private void startPropertiesMonitorWorker() throws ResourceException {
        Integer pollIntervalInteger = mcf.getEJBServicePropertiesPollInterval();
        int pollInterval = pollIntervalInteger.intValue();

        LOG.info("Ejb service properties poll interval is: [" + pollInterval + " seconds]");
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BooleanType.java`
#### Snippet
```java
            writer.writeXsiNil();
        } else {
            writer.writeValueAsBoolean(((Boolean)object).booleanValue());
        }
    }
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CharacterAsStringType.java`
#### Snippet
```java
        throws DatabindingException {
        Character charObject = (Character) object;
        intType.writeObject(Integer.valueOf(charObject.charValue()), writer, context);
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduitFactory.java`
#### Snippet
```java
            }
            if (st instanceof Boolean) {
                return ((Boolean)st).booleanValue() ? ALWAYS : NEVER;
            }
            return ASYNC_ONLY;
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
            Integer i = ATTR_TYPES.get(typeStr);
            if (i != null) {
                return i.intValue();
            }
        }
```

## RuleId[ruleID=RedundantMethodOverride]
### RedundantMethodOverride
Method `end()` is identical to its super method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void end(InterfaceInfo intf) {
    }

```

### RedundantMethodOverride
Method `end()` is identical to its super method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void end(MessagePartInfo part) {
    }

```

### RedundantMethodOverride
Method `end()` is identical to its super method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void end(MessageInfo msg) {
    }

```

### RedundantMethodOverride
Method `begin()` is identical to its super method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void begin(FaultInfo fault) {
    }

```

### RedundantMethodOverride
Method `end()` is identical to its super method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void end(FaultInfo fault) {
    }

```

### RedundantMethodOverride
Method `close()` only delegates to its super method
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java

    @PreDestroy
    public void close() {
        super.close();
    }
```

### RedundantMethodOverride
Method `getTargetSelector()` is identical to its super method
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    @Override
    public FailoverTargetSelector getTargetSelector() {
        return delegate.getTargetSelector();
    }
```

### RedundantMethodOverride
Method `setTargetSelector()` is identical to its super method
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    @Override
    public void setTargetSelector(FailoverTargetSelector targetSelector) {
        delegate.setTargetSelector(targetSelector);
    }
```

### RedundantMethodOverride
Method `visit()` only delegates to its super method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPALanguageVisitor.java`
#### Snippet
```java
    }

    public void visit(SearchCondition<T> sc) {
        // provide more customizations as needed
        super.visit(sc);
```

### RedundantMethodOverride
Method `onWrite()` is identical to its super method
in `core/src/main/java/org/apache/cxf/io/CacheAndWriteOutputStream.java`
#### Snippet
```java
    
    @Override
    protected void onWrite() throws IOException {
        // does nothing
    }
```

### RedundantMethodOverride
Method `doInitializeProvider()` is identical to its super method
in `core/src/main/java/org/apache/cxf/feature/validation/SchemaValidationFeature.java`
#### Snippet
```java

        @Override
        public void doInitializeProvider(InterceptorProvider interceptorProvider, Bus bus) {
            // no-op
        }
```

### RedundantMethodOverride
Method `handleFault()` is identical to its super method
in `core/src/main/java/org/apache/cxf/interceptor/AttachmentInInterceptor.java`
#### Snippet
```java

    @Override
    public void handleFault(Message messageParam) {
    }

```

### RedundantMethodOverride
Method `hashCode()` only delegates to its super method
in `core/src/main/java/org/apache/cxf/interceptor/AbstractAttributedInterceptorProvider.java`
#### Snippet
```java

    @Override
    public int hashCode() {     //NOPMD
        return super.hashCode();
    }
```

### RedundantMethodOverride
Method `readFrom()` only delegates to its super method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementTypedProvider.java`
#### Snippet
```java
    implements MessageBodyWriter<JAXBElement>, MessageBodyReader<JAXBElement> {

    public JAXBElement readFrom(Class<JAXBElement> type, Type genericType, Annotation[] anns, MediaType mt,
        MultivaluedMap<String, String> headers, InputStream is)
        throws IOException {
```

### RedundantMethodOverride
Method `handleFault()` is identical to its super method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutExceptionMapperInterceptor.java`
#### Snippet
```java
    }
    @Override
    public void handleFault(Message message) {
        //complete
    }
```

### RedundantMethodOverride
Method `handleFault()` is identical to its super method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
    }

    public void handleFault(Message message) {
        // complete
    }
```

### RedundantMethodOverride
Method `getAuthorizationParts()` only delegates to its super method
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestInterceptor.java`
#### Snippet
```java
    }

    protected String[] getAuthorizationParts(Message message) {
        return super.getAuthorizationParts(message);

```

### RedundantMethodOverride
Method `equals()` only delegates to its super method
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/AssociatedManagedConnectionFactoryImpl.java`
#### Snippet
```java
    }

    public boolean equals(Object o) {
        return super.equals(o);
    }
```

### RedundantMethodOverride
Method `getMetaData()` is identical to its super method
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionImpl.java`
#### Snippet
```java
    }

    public ManagedConnectionMetaData getMetaData() throws ResourceException {
        return new CXFManagedConnectionMetaData();
    }
```

### RedundantMethodOverride
Method `shutdown()` only delegates to its super method
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java

    @Override
    public void shutdown() {
        super.shutdown();
    }
```

### RedundantMethodOverride
Method `getXMLStreamWriter()` is identical to its super method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/RPCOutInterceptor.java`
#### Snippet
```java
    }

    protected XMLStreamWriter getXMLStreamWriter(Message message) {
        return message.getContent(XMLStreamWriter.class);
    }
```

### RedundantMethodOverride
Method `getEncryptedContentEncryptionKey()` is identical to its super method
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AesCbcHmacJweEncryption.java`
#### Snippet
```java

    @Override
    protected byte[] getEncryptedContentEncryptionKey(JweHeaders headers, byte[] theCek) {
        return getKeyEncryptionAlgo().getEncryptedContentEncryptionKey(headers, theCek);
    }
```

## RuleId[ruleID=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("engine-factory",
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("conduit",
```

### ClassNameSameAsAncestorName
Class name `Portable` is the same as one of its superclass' names
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/LoadDistributorFeature.java`
#### Snippet
```java
    }

    public static class Portable extends FailoverFeature.Portable {
        public Portable() {

```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("failover",
```

### ClassNameSameAsAncestorName
Class name `Portable` is the same as one of its superclass' names
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java
    }

    public static class Portable extends FailoverFeature.Portable {
        private int threshold;
        private long timeout;
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("engine-factory",
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `core/src/main/java/org/apache/cxf/bus/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.ParserContext;

public class NamespaceHandler extends NamespaceHandlerSupport {
    @SuppressWarnings("deprecation")
    public void init() {
```

### ClassNameSameAsAncestorName
Class name `Exception` is the same as one of its superclass' names
in `core/src/main/java/org/apache/cxf/common/i18n/Exception.java`
#### Snippet
```java


public class Exception extends java.lang.Exception {

    private static final long serialVersionUID = 1L;
```

### ClassNameSameAsAncestorName
Class name `AbstractBeanDefinitionParser` is the same as one of its superclass' names
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.ParserContext;

public abstract class AbstractBeanDefinitionParser
    extends org.springframework.beans.factory.xml.AbstractSingleBeanDefinitionParser {
    public static final String WIRE_BUS_ATTRIBUTE = AbstractBeanDefinitionParser.class.getName() + ".wireBus";
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("engine-factory",
```

### ClassNameSameAsAncestorName
Class name `JAXRSServerFactoryCustomizationExtension` is the same as one of its superclass' names
in `integration/cdi/src/main/java/org/apache/cxf/cdi/extension/JAXRSServerFactoryCustomizationExtension.java`
#### Snippet
```java
 */
@Deprecated
public interface JAXRSServerFactoryCustomizationExtension extends
        org.apache.cxf.jaxrs.ext.JAXRSServerFactoryCustomizationExtension {
}
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("enableColoc", new ColocBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `SecurityConstants` is the same as one of its superclass' names
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/SecurityConstants.java`
#### Snippet
```java
 * used by the non WS-SecurityPolicy approach in the WSS4J(Out|In)Interceptors.
 */
public final class SecurityConstants extends org.apache.cxf.rt.security.SecurityConstants {

    //
```

### ClassNameSameAsAncestorName
Class name `FaultInfoException` is the same as one of its superclass' names
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/FaultInfoException.java`
#### Snippet
```java
import org.apache.cxf.interceptor.FaultOutInterceptor;

public class FaultInfoException extends Exception
    implements FaultOutInterceptor.FaultInfoException {

```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("client", new ClientProxyFactoryBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `ContentDisposition` is the same as one of its superclass' names
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/ContentDisposition.java`
#### Snippet
```java


public class ContentDisposition extends org.apache.cxf.attachment.ContentDisposition {

    public ContentDisposition(String value) {
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("server", new JAXRSServerFactoryBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.context.ApplicationContextAware;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("client", new JaxWsProxyFactoryBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `EndpointImpl` is the same as one of its superclass' names
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws22/EndpointImpl.java`
#### Snippet
```java
 */
@Deprecated
public class EndpointImpl extends org.apache.cxf.jaxws.EndpointImpl {

    /**
```

### ClassNameSameAsAncestorName
Class name `ProviderImpl` is the same as one of its superclass' names
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws22/spi/ProviderImpl.java`
#### Snippet
```java
 */
@Deprecated
public class ProviderImpl extends org.apache.cxf.jaxws.spi.ProviderImpl {

}
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("rmManager", new RMManagerBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("addressing", new AddressingBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `VersionTransformer` is the same as one of its superclass' names
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/VersionTransformer.java`
#### Snippet
```java
 * on an earlier version of WS-Adressing (for example WS-RM depends on the 2004/08 version).
 */
public class VersionTransformer extends org.apache.cxf.ws.addressing.VersionTransformer {

    public static final Set<QName> HEADERS;
```

### ClassNameSameAsAncestorName
Class name `ResourceAdapterInternalException` is the same as one of its superclass' names
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/ResourceAdapterInternalException.java`
#### Snippet
```java
import org.apache.cxf.common.logging.LogUtils;

public class ResourceAdapterInternalException extends jakarta.resource.spi.ResourceAdapterInternalException {

    private static final long serialVersionUID = 6769505138041263456L;
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("soapBinding", new SoapBindingInfoConfigBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("policies", new PolicyFeatureBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `PrimitiveAssertion` is the same as one of its superclass' names
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/primitive/PrimitiveAssertion.java`
#### Snippet
```java
 *
 */
public class PrimitiveAssertion
    extends org.apache.neethi.builders.PrimitiveAssertion {

```

### ClassNameSameAsAncestorName
Class name `WsdlArtifact` is the same as one of its superclass' names
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WsdlArtifact.java`
#### Snippet
```java
 * This exists to move this into the same package as the mojo.
 */
public class WsdlArtifact extends org.apache.cxf.maven_plugin.WsdlArtifact {

    public WsdlArtifact() {
```

### ClassNameSameAsAncestorName
Class name `WsdlArtifact` is the same as one of its superclass' names
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlArtifact.java`
#### Snippet
```java
 * THis exists to move this into the same package as the mojo.
 */
public class WsdlArtifact extends org.apache.cxf.maven_plugin.WsdlArtifact {

    public WsdlArtifact() {
```

### ClassNameSameAsAncestorName
Class name `NamespaceHandler` is the same as one of its superclass' names
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spring/NamespaceHandler.java`
#### Snippet
```java
import org.springframework.beans.factory.xml.NamespaceHandlerSupport;

public class NamespaceHandler extends NamespaceHandlerSupport {
    public void init() {
        registerBeanDefinitionParser("client", new JAXRSClientFactoryBeanDefinitionParser());
```

### ClassNameSameAsAncestorName
Class name `Portable` is the same as one of its superclass' names
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/validation/JAXRSClientBeanValidationFeature.java`
#### Snippet
```java
    }

    public static class Portable extends ClientBeanValidationFeature.Portable {
        private boolean wrapInProcessingException;
        @Override
```

## RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
session .getMaxInactiveInterval() \* 1000: integer multiplication implicitly cast to long
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/session/DefaultHttpSessionStore.java`
#### Snippet
```java
            long currentMillis = System.currentTimeMillis();

            if (currentMillis - session.getLastAccessedTime() > session
                    .getMaxInactiveInterval() * 1000) {

                destroySession(entry.getKey());
```

## RuleId[ruleID=OptionalAssignedToNull]
### OptionalAssignedToNull
Optional value is compared with null
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

    public static void setMinimumEscapeHandler(Marshaller marshaller) {
        if (jaxbMinimumEscapeHandler == null) {
            jaxbMinimumEscapeHandler = Optional.ofNullable(createMininumEscapeHandler(marshaller.getClass()));
        }
```

### OptionalAssignedToNull
Optional value is compared with null
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

    public static void setNoEscapeHandler(final Marshaller marshaller) {
        if (jaxbNoEscapeHandler == null) {
            jaxbNoEscapeHandler = Optional.ofNullable(createNoEscapeHandler(marshaller.getClass()));
        }
```

## RuleId[ruleID=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import io.swagger.v3.oas.annotations.OpenAPIDefinition;`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/SwaggerUi.java`
#### Snippet
```java
import org.apache.cxf.jaxrs.swagger.ui.SwaggerUiResolver;

import io.swagger.v3.oas.annotations.OpenAPIDefinition;

/**
```

### UNUSED_IMPORT
Unused import `import org.apache.wss4j.dom.util.WSSecurityUtil;`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/STSInvoker.java`
#### Snippet
```java
import org.apache.wss4j.common.util.DateUtil;
import org.apache.wss4j.dom.message.token.SecurityContextToken;
import org.apache.wss4j.dom.util.WSSecurityUtil;
import org.apache.xml.security.utils.XMLUtils;

```

### UNUSED_IMPORT
Unused import `import org.apache.wss4j.dom.util.WSSecurityUtil;`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SymmetricKeyHandler.java`
#### Snippet
```java
import org.apache.wss4j.common.derivedKey.P_SHA1;
import org.apache.wss4j.common.ext.WSSecurityException;
import org.apache.wss4j.dom.util.WSSecurityUtil;
import org.apache.xml.security.exceptions.XMLSecurityException;
import org.apache.xml.security.stax.ext.XMLSecurityConstants;
```

## RuleId[ruleID=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java

        String line;
        while ((line = r.readLine()) != null) {
            if (line.isEmpty()) {
                continue;
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java
        List<String> exceptions = null;
        String line;
        while ((line = r.readLine()) != null) {
            if (line.isEmpty()) {
                continue;
```

### NestedAssignment
Result of assignment expression used
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/sse/SseSubscription.java`
#### Snippet
```java
                InboundSseEvent bufferedEvent;
                while (delivered.get() < requested.get()
                       && (bufferedEvent = buffer.pollFirst()) != null) {

                    subscriber.onNext(bufferedEvent);
```

### NestedAssignment
Result of assignment expression used
in `core/src/main/java/org/apache/cxf/attachment/QuotedPrintableDecoderStream.java`
#### Snippet
```java
        // if the character is a line terminator, we need to discard the blanks.
        // scan forward, counting the characters.
        while ((ch = in.read()) == ' ') {
            deferredWhitespace++;
        }
```

### NestedAssignment
Result of assignment expression used
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
        int c;

        while ((c = in.read()) != -1) {
            // a linefeed is a terminator, always.
            if (c == '\n') {
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedInputBuffer.java`
#### Snippet
```java
            int bytesRead;
            if (waitingBuffer != null && this.buffer.position() == 0) {
                while ((bytesRead = decoder.read(this.waitingBuffer)) > 0) {
                    totalRead += bytesRead;
                }
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedInputBuffer.java`
#### Snippet
```java
            }
            //read more
            while ((bytesRead = decoder.read(this.buffer)) > 0) {
                totalRead += bytesRead;
            }
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/MutableHttpEntity.java`
#### Snippet
```java
                int count;
                final byte[] tmp = new byte[OUTPUT_BUFFER_SIZE];
                while ((count = inStream.read(tmp)) != -1) {
                    outStream.write(tmp, 0, count);
                }
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/SharedInputBuffer.java`
#### Snippet
```java
            int bytesRead;
            if (waitingBuffer != null && buffer().position() == 0) {
                while ((bytesRead = transfer(buffer, this.waitingBuffer)) > 0) {
                    totalRead += bytesRead;
                }
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/SharedInputBuffer.java`
#### Snippet
```java
            }
            //read more
            while ((bytesRead = transfer(buffer, buffer())) > 0) {
                totalRead += bytesRead;
            }
```

### NestedAssignment
Result of assignment expression used
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            readHeaders(reader, message);
            int event;
            while ((event = reader.nextTag()) != XMLStreamConstants.START_ELEMENT) {
                if (event == XMLStreamConstants.END_ELEMENT) {
                    throw new IllegalStateException("No body content present");
```

### NestedAssignment
Result of assignment expression used
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ProviderFactory.java`
#### Snippet
```java
        try (BufferedReader bufrd = new BufferedReader(new FileReader(f))) {
            String line;
            while (null != (line = bufrd.readLine())) {
                sb.append(line).append('\n');
            }
```

### NestedAssignment
Result of assignment expression used
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java
            Book book = null;
            
            while ((s = br.readLine()) != null) {
                if (s.trim().isEmpty()) {
                    if (id == null && book == null) {
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            String line;
            boolean first = true;
            while ((line = readLine()) != null) {
                if (first && isStatusCode(line)) {
                    statusCode = Integer.parseInt(line);
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java

        // read headers
        while ((line = readLine(in)) != null) {
            if (line.length() > 0) {
                del = line.indexOf(':');
```

### NestedAssignment
Result of assignment expression used
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
        int c;

        while ((c = in.read()) != -1) {
            // a linefeed is a terminator, always.
            if (c == '\n') {
```

## RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`jsVar = jsVar + "[ax]"` could be simplified to 'jsVar += "\[ax\]"'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
            startIf(jsVar + " != null");
            startFor("var ax = 0", "ax < " +  jsVar + ".length", "ax ++");
            jsVar = jsVar + "[ax]";
            // we need an extra level of 'nil' testing here. Or do we, depending
            // on the type structure?
```

### ReplaceAssignmentWithOperatorAssignment
`pathString = pathString + "/"` could be simplified to 'pathString += "/"'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HttpUrlUtil.java`
#### Snippet
```java
        String pathString = urlName;
        if (!pathString.endsWith("/")) {
            pathString = pathString + "/";
        }
        return target.startsWith(pathString);
```

### ReplaceAssignmentWithOperatorAssignment
`result = result + pathInfo` could be simplified to 'result += pathInfo'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
        // REVISIT: is this really correct?
        if (null != pathInfo && !result.endsWith(pathInfo)) {
            result = result + pathInfo;
        }
        if (queryString != null) {
```

### ReplaceAssignmentWithOperatorAssignment
`endpointAddress = endpointAddress + (startsWithSlash ? pathInfo.substring(1) : pathInfo)` could be simplified to 'endpointAddress += (startsWithSlash ? pathInfo.substring(1) : pathInfo)'
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
                boolean startsWithSlash = pathInfo.startsWith(slash);
                if (endpointAddress.endsWith(slash)) {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo.substring(1) : pathInfo);
                } else {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo : (slash + pathInfo));
```

### ReplaceAssignmentWithOperatorAssignment
`endpointAddress = endpointAddress + (startsWithSlash ? pathInfo : (slash + pathInfo))` could be simplified to 'endpointAddress += (startsWithSlash ? pathInfo : (slash + pathInfo))'
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo.substring(1) : pathInfo);
                } else {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo : (slash + pathInfo));
                }
                message.put(Message.ENDPOINT_ADDRESS, endpointAddress);
```

### ReplaceAssignmentWithOperatorAssignment
`servantCount = servantCount + 1` could be simplified to 'servantCount += 1'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        handler.setName(smap);
        registedPaths.put(url.getPath(), handler);
        servantCount = servantCount + 1;
    }

```

### ReplaceAssignmentWithOperatorAssignment
`bus = bus + ".config"` could be simplified to 'bus += ".config"'
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java
                bus = Bus.DEFAULT_BUS_ID + ".config" + counter.getAndIncrement();
            } else {
                bus = bus + ".config";
            }
            try {
```

### ReplaceAssignmentWithOperatorAssignment
`prefix = prefix + "cxf"` could be simplified to 'prefix += "cxf"'
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
            prefix = "cxf";
        } else if (prefix.length() < 3) {
            prefix = prefix + "cxf";
        }
        File result = Files.createTempFile(parent.toPath(), prefix, suffix).toFile();
```

### ReplaceAssignmentWithOperatorAssignment
`i = i - boundaryIndex` could be simplified to 'i -= boundaryIndex'
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
            }
            if (needUnread0d0a) { //Pushing all,  returning 13
                i = i - boundaryIndex;
                i--; //for 10
//                value = 13;
```

### ReplaceAssignmentWithOperatorAssignment
`i = i - boundaryIndex` could be simplified to 'i -= boundaryIndex'
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
//                value = 13;
            } else {
                i = i - boundaryIndex;
                i++;
//                value = boundary[0];
```

### ReplaceAssignmentWithOperatorAssignment
`id = id + getSuffix()` could be simplified to 'id += getSuffix()'
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractFactoryBeanDefinitionParser.java`
#### Snippet
```java
        }
        if (createdFromAPI) {
            id = id + getSuffix();
        }

```

### ReplaceAssignmentWithOperatorAssignment
`address = address + "/"` could be simplified to 'address += "/"'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriInfoImpl.java`
#### Snippet
```java
        }
        if (!path.isEmpty() && !path.startsWith("/") && !address.endsWith("/")) {
            address = address + "/";
        }
        return address + path;
```

### ReplaceAssignmentWithOperatorAssignment
`className = className + "Response"` could be simplified to 'className += "Response"'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
            + StringUtils.capitalize(op.getName().getLocalPart());
        if (!isRequest) {
            className = className + "Response";
        }
        String pname = pkg + ".package-info";
```

### ReplaceAssignmentWithOperatorAssignment
`className = className + "Response"` could be simplified to 'className += "Response"'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/WrapperClassLoader.java`
#### Snippet
```java
                + StringUtils.capitalize(op.getName().getLocalPart());
        if (!isRequest) {
            className = className + "Response";
        }

```

### ReplaceAssignmentWithOperatorAssignment
`reconnectDelay = reconnectDelay * useExponentialBackOff` could be simplified to 'reconnectDelay \*= useExponentialBackOff'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
                    if (nextReconnectAttempt < System.currentTimeMillis()) {
                        nextReconnectAttempt = System.currentTimeMillis() + reconnectDelay;
                        reconnectDelay = reconnectDelay * useExponentialBackOff;
                    }
                }
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 5` could be simplified to 'i += 5'
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/crypto/ModelEncryptionSupport.java`
#### Snippet
```java
            List<OAuthPermission> perms = new LinkedList<>();
            String[] allPermParts = parts[9].split("\\.");
            for (int i = 0; i + 4 < allPermParts.length; i = i + 5) {
                OAuthPermission perm = new OAuthPermission(allPermParts[i], allPermParts[i + 1]);
                perm.setDefaultPermission(Boolean.parseBoolean(allPermParts[i + 2]));
```

### ReplaceAssignmentWithOperatorAssignment
`count = count - 1` could be simplified to 'count -= 1'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaBindingHelper.java`
#### Snippet
```java
        }

        count = count - 1;

        if (count < 1) {
```

### ReplaceAssignmentWithOperatorAssignment
`absoluteURI = absoluteURI + ref.getURI()` could be simplified to 'absoluteURI += ref.getURI()'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
            absoluteURI = Integer.toString(di.hashCode()) + ref.getURI();
        } else {
            absoluteURI = absoluteURI + ref.getURI();
        }
        Policy resolved = registry.lookup(absoluteURI);
```

### ReplaceAssignmentWithOperatorAssignment
`payload = payload + str` could be simplified to 'payload += str'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
                        payload = payload + "\r\n" + CONTENT_SUPPRESSED + "\r\n";
                    } else {
                        payload = payload + str;
                        payload = payload + boundary;
                    }
```

### ReplaceAssignmentWithOperatorAssignment
`payload = payload + boundary` could be simplified to 'payload += boundary'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
                    } else {
                        payload = payload + str;
                        payload = payload + boundary;
                    }
                }
```

### ReplaceAssignmentWithOperatorAssignment
`off = off + lenToCopy` could be simplified to 'off += lenToCopy'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
            int lenToCopy = remaining < len ? remaining : len;
            lastRawDataChunk = newArray(lastRawDataChunk, 0, lastRawDataChunk.length, b, off, lenToCopy);
            off = off + lenToCopy;
            len -= lenToCopy;
            if (lastRawDataChunk.length < blockSize) {
```

### ReplaceAssignmentWithOperatorAssignment
`i = i + 2` could be simplified to 'i += 2'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/ArrayTypeInfo.java`
#### Snippet
```java

        // Dimensions [1,2,3,4]
        for (int i = 1; i < tokens.size(); i = i + 2) {
            String dimension = tokens.get(i);
            if ("]".equals(dimension)) {
```

## RuleId[ruleID=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`redirectList != null && matchPath(redirectQueryCheck, redirectList, request) || redi...` can be simplified to 'redirectList == null\|\|matchPath(redirectQueryCheck, redirectList, request)'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        throws ServletException {
        if ((dispatcherServletPath != null || dispatcherServletName != null)
            && (redirectList != null && matchPath(redirectQueryCheck, redirectList, request)
                || redirectList == null)) {
            // if no redirectList is provided then this servlet is redirecting only
            redirect(request, response, request.getPathInfo());
```

### SimplifiableBooleanExpression
`(property == null && !propertyType.isPrimitive()) || (property != null)` can be simplified to 'property != null\|\|!propertyType.isPrimitive()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java

            Class<?> propertyType = desc.getPropertyType();
            if ((property == null && !propertyType.isPrimitive()) || (property != null)) {
                m.invoke(object, new Object[] {
                    property
```

## RuleId[ruleID=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `exchange` are updated, but never queried
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            LOG.fine("Create a new message for processing");
            inMessage = new MessageImpl();
            ExchangeImpl exchange = new ExchangeImpl();
            exchange.setInMessage(inMessage);
            setupMessage(inMessage,
```

### MismatchedCollectionQueryUpdate
Contents of collection `classResourceNames` are queried, but never updated
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/Java2WADLMojo.java`
#### Snippet
```java
     * @parameter
     */
    private List<String> classResourceNames;

    /**
```

### MismatchedCollectionQueryUpdate
Contents of collection `ex` are updated, but never queried
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalConduit.java`
#### Snippet
```java

        // Create a new incoming exchange and store the original exchange for the response
        ExchangeImpl ex = new ExchangeImpl();
        ex.setInMessage(copy);
        ex.put(IN_EXCHANGE, message.getExchange());
```

### MismatchedCollectionQueryUpdate
Contents of collection `pluginArtifacts` are queried, but never updated
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java
     * @readonly
     */
    private List<Artifact> pluginArtifacts;

    /**
```

### MismatchedCollectionQueryUpdate
Contents of collection `exchange` are updated, but never queried
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/JAXWSHttpSpiDestination.java`
#### Snippet
```java
        }
        Message inMessage = new MessageImpl();
        ExchangeImpl exchange = new ExchangeImpl();
        exchange.setInMessage(inMessage);

```

### MismatchedCollectionQueryUpdate
Contents of collection `exchange` are updated, but never queried
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
        protected void processStreamIo(IoSession session, InputStream in, OutputStream out) {
            final MessageImpl m = new MessageImpl();
            final Exchange exchange = new ExchangeImpl();
            exchange.setDestination(UDPDestination.this);
            m.setDestination(UDPDestination.this);
```

### MismatchedCollectionQueryUpdate
Contents of collection `exchange` are updated, but never queried
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java

                    final MessageImpl m = new MessageImpl();
                    final Exchange exchange = new ExchangeImpl();
                    exchange.setDestination(UDPDestination.this);
                    m.setDestination(UDPDestination.this);
```

### MismatchedCollectionQueryUpdate
Contents of collection `pluginArtifacts` are queried, but never updated
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
     */
    @Parameter(readonly = true, required = true, property = "plugin.artifacts")
    private List<Artifact> pluginArtifacts;


```

## RuleId[ruleID=ExtendsAnnotation]
### ExtendsAnnotation
Class 'DefaultLiteral' implements annotation interface `Default`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/RestClientBean.java`
#### Snippet
```java
    }

    private static final class DefaultLiteral extends AnnotationLiteral<Default> implements Default {
        private static final long serialVersionUID = 1L;

```

### ExtendsAnnotation
Class 'ContextResolvedLiteral' implements annotation interface `ContextResolved`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/ContextResolved.java`
#### Snippet
```java
    ContextResolved LITERAL = new ContextResolvedLiteral();

    final class ContextResolvedLiteral extends AnnotationLiteral<ContextResolved> implements ContextResolved {
        private static final long serialVersionUID = 1L;
    }
```

### ExtendsAnnotation
Class 'AnyLiteral' implements annotation interface `Any`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/AbstractCXFBean.java`
#### Snippet
```java
    }

    private static class AnyLiteral extends AnnotationLiteral<Any> implements Any {
        private static final long serialVersionUID = 1L;
    }
```

### ExtendsAnnotation
Class 'DefaultLiteral' implements annotation interface `Default`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/AbstractCXFBean.java`
#### Snippet
```java
    }

    private static class DefaultLiteral extends AnnotationLiteral<Default> implements Default {
        private static final long serialVersionUID = 1L;
    }
```

### ExtendsAnnotation
Class 'InjectLiteral' implements annotation interface `Inject`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/DelegateContextAnnotatedType.java`
#### Snippet
```java
    }

    private static final class InjectLiteral extends AnnotationLiteral<Inject> implements Inject {
        private static final long serialVersionUID = 1L;

```

## RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `dispatch` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    JAXBContext jaxbContext;
    ServiceImpl service;
    Dispatch<Object> dispatch;
    ObjectFactory factory = new ObjectFactory();
    final Bus bus;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `address` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java

    public static final QName SERVICE_QNAME = new QName(WSDVersion.NS_1_1, "DiscoveryProxy");
    String address = "soap.udp://239.255.255.250:3702";
    String ipv6Address = "soap.udp://[FF02::C]:3702";
    boolean adHoc = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `adHoc` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    String address = "soap.udp://239.255.255.250:3702";
    String ipv6Address = "soap.udp://[FF02::C]:3702";
    boolean adHoc = true;
    AtomicInteger msgId = new AtomicInteger(1);
    long instanceId = System.currentTimeMillis();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `version` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    final Bus bus;
    int defaultProbeTimeout = 1000;
    WSDVersion version = WSDVersion.INSTANCE_1_1;
    String soapVersion = SOAPBinding.SOAP12HTTP_BINDING;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `soapVersion` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    int defaultProbeTimeout = 1000;
    WSDVersion version = WSDVersion.INSTANCE_1_1;
    String soapVersion = SOAPBinding.SOAP12HTTP_BINDING;

    public WSDiscoveryClient() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `service` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    long instanceId = System.currentTimeMillis();
    JAXBContext jaxbContext;
    ServiceImpl service;
    Dispatch<Object> dispatch;
    ObjectFactory factory = new ObjectFactory();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `client` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
    Bus bus;
    Endpoint udpEndpoint;
    WSDiscoveryClient client;
    List<HelloType> registered = new CopyOnWriteArrayList<>();
    ObjectFactory factory = new ObjectFactory();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(WSDiscoveryService.class);

    Bus bus;
    Endpoint udpEndpoint;
    WSDiscoveryClient client;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mBeanContainer` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
     * The Jetty {@link MBeanContainer} to use when enabling JMX in Jetty.
     */
    private Container.Listener mBeanContainer;

    public JettyHTTPServerEngineFactory() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fallbackThreadingParameters` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
        new TreeMap<>();

    private ThreadingParameters fallbackThreadingParameters;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tlsParametersMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
     * configure a subsequently created JettyHTTPServerEngine.
     */
    private Map<String, TLSServerParameters> tlsParametersMap =
        new TreeMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `portMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    // Still use the static map to hold the port information
    // in the same JVM
    private static ConcurrentHashMap<Integer, JettyHTTPServerEngine> portMap =
        new ConcurrentHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private Boolean sendServerVersion = true;
    private int servantCount;
    private Server server;
    private Connector connector;
    private List<Handler> handlers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registedPaths` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private boolean configFinalized;

    private List<String> registedPaths = new CopyOnWriteArrayList<>();

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `handlers` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private Server server;
    private Connector connector;
    private List<Handler> handlers;
    private ContextHandlerCollection contexts;
    private Container.Listener mBeanContainer;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tlsServerParameters` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
     * placed by SpringConfig.
     */
    private TLSServerParameters tlsServerParameters;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `contexts` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private Connector connector;
    private List<Handler> handlers;
    private ContextHandlerCollection contexts;
    private Container.Listener mBeanContainer;
    private SessionHandler sessionHandler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isSessionSupport` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private String protocol = "http";

    private Boolean isSessionSupport = false;
    private int sessionTimeout = -1;
    private Boolean isReuseAddress = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mBeanContainer` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private List<Handler> handlers;
    private ContextHandlerCollection contexts;
    private Container.Listener mBeanContainer;
    private SessionHandler sessionHandler;
    private ThreadPool threadPool;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connector` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private int servantCount;
    private Server server;
    private Connector connector;
    private List<Handler> handlers;
    private ContextHandlerCollection contexts;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `servantCount` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private int maxIdleTime = 200000;
    private Boolean sendServerVersion = true;
    private int servantCount;
    private Server server;
    private Connector connector;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `port` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
     * This is the network port for which this engine is allocated.
     */
    private int port;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `threadPool` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
    private Container.Listener mBeanContainer;
    private SessionHandler sessionHandler;
    private ThreadPool threadPool;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `decodedDestinations` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
    private ConcurrentMap<String, AbstractHTTPDestination> destinations
        = new ConcurrentHashMap<>();
    private Map<String, AbstractHTTPDestination> decodedDestinations =
        new ConcurrentHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `destinations` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
public class DestinationRegistryImpl implements DestinationRegistry, ServletConfigAware {
    private static final String SLASH = "/";
    private ConcurrentMap<String, AbstractHTTPDestination> destinations
        = new ConcurrentHashMap<>();
    private Map<String, AbstractHTTPDestination> decodedDestinations =
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registry` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
    }

    protected DestinationRegistry registry;

    private final ReadWriteLock lock = new ReentrantReadWriteLock();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `registry` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java

    protected final Bus bus;
    protected DestinationRegistry registry;
    protected final String path;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fromEndpointReferenceType` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    protected volatile Address defaultAddress;

    protected boolean fromEndpointReferenceType;

    protected ProxyFactory proxyFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `socketFactory` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
     * Cache the last SSLContext to avoid recreation
     */
    SSLSocketFactory socketFactory;
    int lastTlsHash;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `destinationRegistry` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java

    protected Bus bus;
    private DestinationRegistry destinationRegistry;
    private boolean globalRegistry;
    private ServletController controller;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `symmetricKey` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlEncOutInterceptor.java`
#### Snippet
```java

    private boolean encryptSymmetricKey = true;
    private SecretKey symmetricKey;

    private EncryptionProperties encProps = new EncryptionProperties();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `symmetricKey` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
    private SignatureProperties sigProps = new SignatureProperties();
    private boolean encryptSymmetricKey = true;
    private SecretKey symmetricKey;
    private boolean signRequest;
    private boolean encryptRequest;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `count` is accessed in both synchronized and unsynchronized contexts
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/CountingInputStream.java`
#### Snippet
```java
public final class CountingInputStream extends FilterInputStream {

    private long count;
    private long mark = -1;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `failoverStrategy` is accessed in both synchronized and unsynchronized contexts
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
        "org.apache.cxf.transport.complete_if_service_not_available";

    protected FailoverStrategy failoverStrategy;
    private ConcurrentHashMap<String, InvocationContext> inProgress = new ConcurrentHashMap<>();
    private boolean supportNotAvailableErrorsOnly = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fallbackThreadingParameters` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
        new TreeMap<>();

    private ThreadingParameters fallbackThreadingParameters;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `portMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
    // Still use the static map to hold the port information
    // in the same JVM
    private static ConcurrentHashMap<Integer, UndertowHTTPServerEngine> portMap =
        new ConcurrentHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tempFile` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java

    private boolean tempFileFailed;
    private File tempFile;
    private boolean allowDeleteOfFile = true;
    private CipherPair ciphers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContext` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextJAXBUtils.java`
#### Snippet
```java
 */
public final class ContextJAXBUtils {
    private static JAXBContext jaxbContext;
    private static Set<Class<?>> jaxbContextClasses;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tempFile` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java

    private boolean tempFileFailed;
    private File tempFile;
    private boolean allowDeleteOfFile = true;
    private CipherPair ciphers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContext` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
            = org.apache.cxf.ws.addressing.v200408.EndpointReferenceType.class;

        private static JAXBContext jaxbContext;

        protected Names200408() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContext` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
            org.apache.cxf.ws.addressing.v200403.EndpointReferenceType.class;

        private static JAXBContext jaxbContext;

        protected Names200403() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `listeners` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/managers/ServerLifeCycleManagerImpl.java`
#### Snippet
```java
public class ServerLifeCycleManagerImpl implements ServerLifeCycleManager, BusExtension {

    private CopyOnWriteArrayList<ServerLifeCycleListener> listeners =
            new CopyOnWriteArrayList<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resolvers` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/managers/ServiceContractResolverRegistryImpl.java`
#### Snippet
```java
public class ServiceContractResolverRegistryImpl implements ServiceContractResolverRegistry {

    private List<ServiceContractResolver> resolvers
        = new CopyOnWriteArrayList<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resolvers` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/managers/EndpointResolverRegistryImpl.java`
#### Snippet
```java

    private Bus bus;
    private List<EndpointResolver> resolvers
        = new CopyOnWriteArrayList<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `namedQueues` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
        LogUtils.getL7dLogger(WorkQueueManagerImpl.class);

    Map<String, AutomaticWorkQueue> namedQueues
        = new ConcurrentHashMap<>(4, 0.75f, 2);

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `imanager` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java

    boolean inShutdown;
    InstrumentationManager imanager;
    Bus bus;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `interfaceName` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
    protected volatile Class<?> clazz;
    protected volatile Class<?> intf;
    protected String interfaceName;
    protected boolean deferred;
    protected Collection<String> namespaces = new ArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `className` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
    private static final String PROBLEM_CREATING_EXTENSION_CLASS = "PROBLEM_CREATING_EXTENSION_CLASS";

    protected String className;
    protected ClassLoader classloader;
    protected volatile Class<?> clazz;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `state` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
    protected final Set<Class<?>> missingExtensions;
    protected String id;
    private BusState state;
    private final Collection<Feature> features = new CopyOnWriteArrayList<>();
    private final Map<String, Object> properties = new ConcurrentHashMap<>(16, 0.75f, 4);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `heads` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java

    // heads[phase] refers to the first interceptor of the given phase
    private InterceptorHolder[] heads;
    // tails[phase] refers to the last interceptor of the given phase
    private InterceptorHolder[] tails;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `faultObserver` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
    private State state;
    private Message pausedMessage;
    private MessageObserver faultObserver;
    private PhaseInterceptorIterator iterator;
    private final boolean isFineLogging;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `iterator` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
    private Message pausedMessage;
    private MessageObserver faultObserver;
    private PhaseInterceptorIterator iterator;
    private final boolean isFineLogging;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `backingStore` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/common/util/WeakIdentityHashMap.java`
#### Snippet
```java
public class WeakIdentityHashMap<K, V> implements Map<K, V> {
    private final ReferenceQueue<K> queue = new ReferenceQueue<>();
    private Map<IdentityWeakReference, V> backingStore = new HashMap<>();


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ordered` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    private ResourceManager resourceManager;
    private Map<String, Extension> all = new ConcurrentHashMap<>();
    private List<Extension> ordered = new CopyOnWriteArrayList<>();
    private final Map<Class<?>, Object> activated;
    private final Bus bus;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `all` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    private final ClassLoader loader;
    private ResourceManager resourceManager;
    private Map<String, Extension> all = new ConcurrentHashMap<>();
    private List<Extension> ordered = new CopyOnWriteArrayList<>();
    private final Map<Class<?>, Object> activated;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `resourceManager` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java

    private final ClassLoader loader;
    private ResourceManager resourceManager;
    private Map<String, Extension> all = new ConcurrentHashMap<>();
    private List<Extension> ordered = new CopyOnWriteArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cwClass` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java

    protected boolean badASM;
    private Class<?> cwClass;

    public ASMHelperImpl() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `defaultTempDir` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
public final class FileUtils {
    private static final long RETRY_SLEEP_MILLIS = 10L;
    private static File defaultTempDir;
    private static Thread shutdownHook;
    private static final char[] ILLEGAL_CHARACTERS
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `faults` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
    String outName;
    MessageInfo outputMessage;
    Map<QName, FaultInfo> faults;
    OperationInfo unwrappedOperation;
    List<String> parameterOrdering;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `factory` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/SingletonFactory.java`
#### Snippet
```java
public class SingletonFactory implements Factory {
    Object bean;
    Factory factory;
    public SingletonFactory(final Object bean) {
        this.bean = bean;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bean` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/SingletonFactory.java`
#### Snippet
```java
 */
public class SingletonFactory implements Factory {
    Object bean;
    Factory factory;
    public SingletonFactory(final Object bean) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fullVersion` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/version/Version.java`
#### Snippet
```java
    private static String version;
    private static String name;
    private static String fullVersion;
    private static String buildNumber;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `version` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/version/Version.java`
#### Snippet
```java
public final class Version {

    private static String version;
    private static String name;
    private static String fullVersion;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `name` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/version/Version.java`
#### Snippet
```java

    private static String version;
    private static String name;
    private static String fullVersion;
    private static String buildNumber;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `factory` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
public class PooledFactory implements Factory {
    BlockingQueue<Object> pool;
    Factory factory;
    int count;
    int max;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pool` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
 */
public class PooledFactory implements Factory {
    BlockingQueue<Object> pool;
    Factory factory;
    int count;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `count` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
    BlockingQueue<Object> pool;
    Factory factory;
    int count;
    int max;
    boolean createMore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `max` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
    Factory factory;
    int count;
    int max;
    boolean createMore;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `createMore` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
    int count;
    int max;
    boolean createMore;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cleanupHooks` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/endpoint/EndpointImpl.java`
#### Snippet
```java
    private MessageObserver outFaultObserver;
    private List<Feature> activeFeatures;
    private List<Closeable> cleanupHooks;

    public EndpointImpl(Bus bus, Service s, QName endpointName) throws EndpointException {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `conduits` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java

    //collection of conduits that were created so we can close them all at the end
    protected List<Conduit> conduits = new CopyOnWriteArrayList<>();

   
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `endpoint` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java

   
    protected Endpoint endpoint;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingObserver` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
public abstract class AbstractObservable implements Observable {

    protected MessageObserver incomingObserver;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `is` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/attachment/DelegatingInputStream.java`
#### Snippet
```java

public class DelegatingInputStream extends InputStream implements Transferable {
    private InputStream is;
    private AttachmentDeserializer deserializer;
    private boolean isClosed;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `addWorkerMethod` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    ThreadPoolExecutor executor;
    Method addWorkerMethod;
    Object[] addWorkerArgs;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `threadFactory` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    Object[] addWorkerArgs;

    AWQThreadFactory threadFactory;
    ReentrantLock mainLock;
    final ReentrantLock addThreadLock = new ReentrantLock();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `watchDog` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    DelayQueue<DelayedTaskWrapper> delayQueue;
    WatchDog watchDog;

    boolean shared;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `name` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java


    String name = "default";
    int maxQueueSize;
    int initialThreads;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lowWaterMark` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    int maxQueueSize;
    int initialThreads;
    int lowWaterMark;
    int highWaterMark;
    long dequeueTimeout;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mainLock` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    AWQThreadFactory threadFactory;
    ReentrantLock mainLock;
    final ReentrantLock addThreadLock = new ReentrantLock();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxQueueSize` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    String name = "default";
    int maxQueueSize;
    int initialThreads;
    int lowWaterMark;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executor` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    AtomicInteger approxThreadCount = new AtomicInteger();

    ThreadPoolExecutor executor;
    Method addWorkerMethod;
    Object[] addWorkerArgs;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialThreads` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    String name = "default";
    int maxQueueSize;
    int initialThreads;
    int lowWaterMark;
    int highWaterMark;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `addWorkerArgs` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    ThreadPoolExecutor executor;
    Method addWorkerMethod;
    Object[] addWorkerArgs;

    AWQThreadFactory threadFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `highWaterMark` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    int initialThreads;
    int lowWaterMark;
    int highWaterMark;
    long dequeueTimeout;
    AtomicInteger approxThreadCount = new AtomicInteger();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dataSource` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/attachment/LazyDataSource.java`
#### Snippet
```java
 */
public class LazyDataSource implements DataSource {
    private DataSource dataSource;
    private Collection<Attachment> attachments;
    private String id;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `customProvider` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/validation/AbstractValidationInterceptor.java`
#### Snippet
```java

    private Object serviceObject;
    private boolean customProvider;
    private volatile BeanValidationProvider provider;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `conduitSelector` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java

    protected Bus bus;
    protected ConduitSelector conduitSelector;
    protected ClientOutFaultObserver outFaultObserver;
    protected int synchronousTimeout = 60000; // default 60 second timeout
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `synchronousTimeout` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
    protected ConduitSelector conduitSelector;
    protected ClientOutFaultObserver outFaultObserver;
    protected int synchronousTimeout = 60000; // default 60 second timeout

    protected PhaseChainCache outboundChainCache = new PhaseChainCache();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `manager` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    protected String address;

    protected EndpointManager manager;

    protected Endpoint endpoint;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `beanFactory` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
    private Set<ApplicationContext> appContexts;
    private final Map<String, List<MatcherHolder>> wildCardBeanDefinitions = new TreeMap<>();
    private BeanFactory beanFactory;

    static class MatcherHolder implements Comparable<MatcherHolder> {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `appContexts` is accessed in both synchronized and unsynchronized contexts
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(ConfigurerImpl.class);

    private Set<ApplicationContext> appContexts;
    private final Map<String, List<MatcherHolder>> wildCardBeanDefinitions = new TreeMap<>();
    private BeanFactory beanFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connection` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPullPoint.java`
#### Snippet
```java
    private JAXBContext jaxbContext;

    private Connection connection;

    private Session producerSession;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `topicConverter` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPublisher.java`
#### Snippet
```java
    private Connection connection;

    private JmsTopicExpressionConverter topicConverter;

    private JAXBContext jaxbContext;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `producers` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPublisher.java`
#### Snippet
```java
    private MessageConsumer notificationConsumer;
    
    private Map<Destination, Object> producers;
    
    private final String notificationTopicName;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `readXsiType` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java

    protected boolean writeXsiType = true;
    protected boolean readXsiType = true;
    @Context
    protected ContextResolver<AegisContext> resolver;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `writeXsiType` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
        = new WeakHashMap<>();

    protected boolean writeXsiType = true;
    protected boolean readXsiType = true;
    @Context
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `classContexts` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
    implements MessageBodyReader<T>, MessageBodyWriter<T> {

    private static Map<java.lang.reflect.Type, AegisContext> classContexts
        = new WeakHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `portMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngineFactory.java`
#### Snippet
```java
            LogUtils.getL7dLogger(NettyHttpServerEngineFactory.class);

    private static ConcurrentHashMap<Integer, NettyHttpServerEngine> portMap =
            new ConcurrentHashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tlsServerParametersMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngineFactory.java`
#### Snippet
```java
        new TreeMap<>();

    private Map<String, TLSServerParameters> tlsServerParametersMap =
        new TreeMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionMaxIdle` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int maxPerRoute = 1000;
    int connectionTTL = 60000;
    int connectionMaxIdle = 60000;

    int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `soTimeout` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    boolean interestOpQueued = IOReactorConfig.DEFAULT.isInterestOpQueued();
    int soLinger = IOReactorConfig.DEFAULT.getSoLinger();
    int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout();
    boolean soKeepalive = IOReactorConfig.DEFAULT.isSoKeepalive();
    boolean tcpNoDelay = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ioThreadCount` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int connectionMaxIdle = 60000;

    int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
    long selectInterval = IOReactorConfig.DEFAULT.getSelectInterval();
    boolean interestOpQueued = IOReactorConfig.DEFAULT.isInterestOpQueued();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxConnections` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    boolean isShutdown;
    UseAsyncPolicy policy;
    int maxConnections = 5000;
    int maxPerRoute = 1000;
    int connectionTTL = 60000;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `interestOpQueued` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
    long selectInterval = IOReactorConfig.DEFAULT.getSelectInterval();
    boolean interestOpQueued = IOReactorConfig.DEFAULT.isInterestOpQueued();
    int soLinger = IOReactorConfig.DEFAULT.getSoLinger();
    int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tcpNoDelay` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout();
    boolean soKeepalive = IOReactorConfig.DEFAULT.isSoKeepalive();
    boolean tcpNoDelay = true;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `selectInterval` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

    int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
    long selectInterval = IOReactorConfig.DEFAULT.getSelectInterval();
    boolean interestOpQueued = IOReactorConfig.DEFAULT.isInterestOpQueued();
    int soLinger = IOReactorConfig.DEFAULT.getSoLinger();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionTTL` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int maxConnections = 5000;
    int maxPerRoute = 1000;
    int connectionTTL = 60000;
    int connectionMaxIdle = 60000;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxPerRoute` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    UseAsyncPolicy policy;
    int maxConnections = 5000;
    int maxPerRoute = 1000;
    int connectionTTL = 60000;
    int connectionMaxIdle = 60000;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `soLinger` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    long selectInterval = IOReactorConfig.DEFAULT.getSelectInterval();
    boolean interestOpQueued = IOReactorConfig.DEFAULT.isInterestOpQueued();
    int soLinger = IOReactorConfig.DEFAULT.getSoLinger();
    int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout();
    boolean soKeepalive = IOReactorConfig.DEFAULT.isSoKeepalive();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `soKeepalive` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    int soLinger = IOReactorConfig.DEFAULT.getSoLinger();
    int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout();
    boolean soKeepalive = IOReactorConfig.DEFAULT.isSoKeepalive();
    boolean tcpNoDelay = true;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isAsync` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        BasicHttpEntity basicEntity;

        boolean isAsync;
        SharedInputBuffer inbuf;
        SharedOutputBuffer outbuf;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inbuf` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

        boolean isAsync;
        SharedInputBuffer inbuf;
        SharedOutputBuffer outbuf;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outbuf` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        boolean isAsync;
        SharedInputBuffer inbuf;
        SharedOutputBuffer outbuf;

        // Objects for the response
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `useOnBehalfOf` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
    private boolean alwaysValidateToSts;
    private boolean useIssueBinding;
    private boolean useOnBehalfOf = true;
    private STSClient stsClient;
    private TokenStore tokenStore;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `disableCaching` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
    private STSClient stsClient;
    private TokenStore tokenStore;
    private boolean disableCaching;

    public STSTokenValidator() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `useIssueBinding` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
    private STSSamlAssertionValidator samlValidator = new STSSamlAssertionValidator();
    private boolean alwaysValidateToSts;
    private boolean useIssueBinding;
    private boolean useOnBehalfOf = true;
    private STSClient stsClient;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionMaxIdle` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    private int maxPerRoute = 1000;
    private int connectionTTL = 60000;
    private int connectionMaxIdle = 60000;

    private int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connectionTTL` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    private int maxConnections = 5000;
    private int maxPerRoute = 1000;
    private int connectionTTL = 60000;
    private int connectionMaxIdle = 60000;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `selectInterval` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java

    private int ioThreadCount = IOReactorConfig.DEFAULT.getIoThreadCount();
    private long selectInterval = IOReactorConfig.DEFAULT.getSelectInterval().toMilliseconds();
    private int soLinger = IOReactorConfig.DEFAULT.getSoLinger().toMillisecondsIntBound();
    private int soTimeout = IOReactorConfig.DEFAULT.getSoTimeout().toMillisecondsIntBound();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isAsync` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        private MutableHttpEntity basicEntity;

        private boolean isAsync;
        private SharedInputBuffer inbuf;
        private SharedOutputBuffer outbuf;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inbuf` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java

        private boolean isAsync;
        private SharedInputBuffer inbuf;
        private SharedOutputBuffer outbuf;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outbuf` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        private boolean isAsync;
        private SharedInputBuffer inbuf;
        private SharedOutputBuffer outbuf;

        // Objects for the response
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tokens` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/MemoryTokenStore.java`
#### Snippet
```java
    public static final long MAX_TTL = DEFAULT_TTL * 12L;

    private Map<String, CacheEntry> tokens = new ConcurrentHashMap<>();
    private long ttl = DEFAULT_TTL;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `shutdown` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSDestination.java`
#### Snippet
```java
    private ClassLoader loader;
    private Connection connection;
    private boolean shutdown;

    public JMSDestination(Bus b, EndpointInfo info, JMSConfiguration jmsConfig) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `destinationResolver` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
    private String userName;
    private String password;
    private DestinationResolver destinationResolver = new JMSDestinationResolver();
    private boolean pubSubNoLocal;
    private Long clientReceiveTimeout = 60000L;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replyPubSubDomain` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
    private String messageType = JMSConstants.TEXT_MESSAGE_TYPE;
    private boolean pubSubDomain;
    private boolean replyPubSubDomain;

    /**
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replyToDestination` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
     * Destination name to send out as replyTo address in the message
     */
    private String replyToDestination;
    private volatile Destination replyToDestinationDest;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `replyDestination` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
     * Destination name to listen on for reply messages
     */
    private String replyDestination;
    private volatile Destination replyDestinationDest;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jmsListener` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    private JMSConfiguration jmsConfig;
    private Map<String, Exchange> correlationMap = new ConcurrentHashMap<>();
    private JMSListenerContainer jmsListener;
    private String conduitId;
    private final AtomicLong messageCount = new AtomicLong(0);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jmsConfig` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    private static final String CORRELATED = JMSConduit.class.getName() + ".correlated";

    private JMSConfiguration jmsConfig;
    private Map<String, Exchange> correlationMap = new ConcurrentHashMap<>();
    private JMSListenerContainer jmsListener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `conduitId` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    private Map<String, Exchange> correlationMap = new ConcurrentHashMap<>();
    private JMSListenerContainer jmsListener;
    private String conduitId;
    private final AtomicLong messageCount = new AtomicLong(0);
    private JMSBusLifeCycleListener listener;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `correlationMap` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java

    private JMSConfiguration jmsConfig;
    private Map<String, Exchange> correlationMap = new ConcurrentHashMap<>();
    private JMSListenerContainer jmsListener;
    private String conduitId;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `suspendendContinuations` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/continuations/JMSContinuation.java`
#### Snippet
```java
    private Message inMessage;
    private MessageObserver incomingObserver;
    private Counter suspendendContinuations;

    private volatile Object userObject;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inMessage` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/continuations/JMSContinuation.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(JMSContinuation.class);
    private Bus bus;
    private Message inMessage;
    private MessageObserver incomingObserver;
    private Counter suspendendContinuations;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `obj` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/spring/ClientProxyFactoryBeanDefinitionParser.java`
#### Snippet
```java
        implements ApplicationContextAware, FactoryBean<Object>, DisposableBean {

        private Object obj;

        public SpringClientProxyFactoryBean() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
    private String password;
    private Map<String, Object> properties;
    private Bus bus;
    private List<Feature> features = new ArrayList<>();
    private DataBinding dataBinding;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `username` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
    protected boolean configured;
    private ClientFactoryBean clientFactoryBean;
    private String username;
    private String password;
    private Map<String, Object> properties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `dataBinding` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
    private Bus bus;
    private List<Feature> features = new ArrayList<>();
    private DataBinding dataBinding;

    public ClientProxyFactoryBean() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `password` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
    private ClientFactoryBean clientFactoryBean;
    private String username;
    private String password;
    private Map<String, Object> properties;
    private Bus bus;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientFactoryBean` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
public class ClientProxyFactoryBean extends AbstractBasicInterceptorProvider {
    protected boolean configured;
    private ClientFactoryBean clientFactoryBean;
    private String username;
    private String password;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `properties` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
    private String username;
    private String password;
    private Map<String, Object> properties;
    private Bus bus;
    private List<Feature> features = new ArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `context` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/client/NotificationBroker.java`
#### Snippet
```java
    private final W3CEndpointReference epr;
    private Class<?>[] extraClasses;
    private JAXBContext context;

    public NotificationBroker(String address, Class<?> ... cls) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `extraClasses` is accessed in both synchronized and unsynchronized contexts
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/client/NotificationBroker.java`
#### Snippet
```java
    private org.oasis_open.docs.wsn.brw_2.NotificationBroker broker;
    private final W3CEndpointReference epr;
    private Class<?>[] extraClasses;
    private JAXBContext context;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cont` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java
public class AsyncResponseImpl implements AsyncResponse, ContinuationCallback {

    private Continuation cont;
    private Message inMessage;
    private TimeoutHandler timeoutHandler;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inMessage` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java

    private Continuation cont;
    private Message inMessage;
    private TimeoutHandler timeoutHandler;
    private volatile boolean initialSuspend;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `timeoutHandler` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java
    private Continuation cont;
    private Message inMessage;
    private TimeoutHandler timeoutHandler;
    private volatile boolean initialSuspend;
    private volatile boolean cancelled;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `injectedSubInstances` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java
    private Set<String> nameBindings = Collections.emptySet();
    private ClassResourceInfo parent;
    private Set<String> injectedSubInstances = new HashSet<>();
    public ClassResourceInfo(ClassResourceInfo cri) {
        super(cri.getBus());
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    private Map<Class<?>, List<Field>> contextFields;
    private Map<Class<?>, Map<Class<?>, Method>> contextMethods;
    private Bus bus;
    private boolean constructorProxiesAvailable;
    private boolean contextsAvailable;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `wadlGenerator` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java

    private Map<Class<?>, BeanParamInfo> beanParams = new ConcurrentHashMap<>();
    private ProviderInfo<ContainerRequestFilter> wadlGenerator;

    private ServerProviderFactory(Bus bus) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cls` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        final String className;
        volatile boolean initialized;
        Class<?> cls;

        LazyProviderClass(String cn) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `handlers` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
    protected boolean doInit;
    @SuppressWarnings("rawtypes")
    protected List<Handler> handlers = new ArrayList<>();

    private boolean blockPostConstruct;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `continuation` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
    class JaxwsServerHandler implements AsyncHandler<Object> {
        Response<Object> r;
        Continuation continuation;
        boolean done;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `r` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java

    class JaxwsServerHandler implements AsyncHandler<Object> {
        Response<Object> r;
        Continuation continuation;
        boolean done;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `done` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
        Response<Object> r;
        Continuation continuation;
        boolean done;

        JaxwsServerHandler(Continuation c) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `obj` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spring/JaxWsProxyFactoryBeanDefinitionParser.java`
#### Snippet
```java
        implements ApplicationContextAware, FactoryBean<Object>, DisposableBean {

        private Object obj;

        public JAXWSSpringClientProxyFactoryBean() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `handlers` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        = new ModCountCopyOnWriteArrayList<>();
    @SuppressWarnings("rawtypes")
    private List<Handler> handlers = new ModCountCopyOnWriteArrayList<>();
    private EndpointContext endpointContext;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `schemaLocations` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Class<?> implementorClass;

    private List<String> schemaLocations;
    private List<Feature> features;
    private List<Interceptor<? extends Message>> in
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outFault` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private List<Interceptor<? extends Message>> out
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> outFault
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> inFault
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverFactory` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Object implementor;
    private Server server;
    private JaxWsServerFactoryBean serverFactory;
    private JaxWsServiceFactoryBean serviceFactory;
    private Service service;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceFactory` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Server server;
    private JaxWsServerFactoryBean serverFactory;
    private JaxWsServiceFactoryBean serviceFactory;
    private Service service;
    private Map<String, Object> properties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `in` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private List<String> schemaLocations;
    private List<Feature> features;
    private List<Interceptor<? extends Message>> in
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> out
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `invoker` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Map<String, Object> properties;
    private List<Source> metadata;
    private Invoker invoker;
    private Executor executor;
    private String bindingUri;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `executor` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private List<Source> metadata;
    private Invoker invoker;
    private Executor executor;
    private String bindingUri;
    private String wsdlLocation;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serviceName` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private String publishedEndpointUrl;
    private QName endpointName;
    private QName serviceName;
    private Class<?> implementorClass;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `implementorClass` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private QName endpointName;
    private QName serviceName;
    private Class<?> implementorClass;

    private List<String> schemaLocations;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inFault` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private List<Interceptor<? extends Message>> outFault
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> inFault
        = new ModCountCopyOnWriteArrayList<>();
    @SuppressWarnings("rawtypes")
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `implementor` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java

    private Bus bus;
    private Object implementor;
    private Server server;
    private JaxWsServerFactoryBean serverFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bindingUri` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Invoker invoker;
    private Executor executor;
    private String bindingUri;
    private String wsdlLocation;
    private String address;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `endpointName` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private String address;
    private String publishedEndpointUrl;
    private QName endpointName;
    private QName serviceName;
    private Class<?> implementorClass;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `properties` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private JaxWsServiceFactoryBean serviceFactory;
    private Service service;
    private Map<String, Object> properties;
    private List<Source> metadata;
    private Invoker invoker;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `out` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private List<Interceptor<? extends Message>> in
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> out
        = new ModCountCopyOnWriteArrayList<>();
    private List<Interceptor<? extends Message>> outFault
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `server` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private Bus bus;
    private Object implementor;
    private Server server;
    private JaxWsServerFactoryBean serverFactory;
    private JaxWsServiceFactoryBean serviceFactory;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `service` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private JaxWsServerFactoryBean serverFactory;
    private JaxWsServiceFactoryBean serviceFactory;
    private Service service;
    private Map<String, Object> properties;
    private List<Source> metadata;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(EndpointImpl.class);

    private Bus bus;
    private Object implementor;
    private Server server;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `destination` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(DestinationSequence.class);

    private Destination destination;
    private EndpointReferenceType acksTo;
    private long lastMessageNumber;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxInactivityTimeout` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    final class SequenceTermination extends TimerTask {

        private long maxInactivityTimeout;

        void updateInactivityTimeout(long timeout) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deliveringMessageNumbers` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    private Map<Long, Continuation> continuations = new TreeMap<>();
    // this map is used for robust and redelivery tracking. for redelivery it holds the beingDeliverd messages
    private Set<Long> deliveringMessageNumbers = new HashSet<>();

    public DestinationSequence(Identifier i, EndpointReferenceType a, Destination d, ProtocolVariation pv) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `deferredAcknowledgments` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    private volatile boolean acknowledgeOnNextOccasion;
    private boolean terminated;
    private List<DeferredAcknowledgment> deferredAcknowledgments;
    private SequenceTermination scheduledTermination;
    private String correlationID;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContextReference` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder11Impl.java`
#### Snippet
```java
    public static final EncoderDecoder11Impl INSTANCE = new EncoderDecoder11Impl();

    private static AtomicReference<JAXBContext> jaxbContextReference = new AtomicReference<>();

    private static final Logger LOG = LogUtils.getL7dLogger(EncoderDecoder11Impl.class);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `source` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SourceSequence.java`
#### Snippet
```java

    private Date expires;
    private Source source;
    private long currentMessageNumber;
    private boolean lastMessage;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `currentMessageNumber` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SourceSequence.java`
#### Snippet
```java
    private Date expires;
    private Source source;
    private long currentMessageNumber;
    private boolean lastMessage;
    private Identifier offeringId;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastMessage` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SourceSequence.java`
#### Snippet
```java
    private Source source;
    private long currentMessageNumber;
    private boolean lastMessage;
    private Identifier offeringId;
    private EndpointReferenceType target;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `target` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SourceSequence.java`
#### Snippet
```java
    private boolean lastMessage;
    private Identifier offeringId;
    private EndpointReferenceType target;

    public SourceSequence(Identifier i, ProtocolVariation pv) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContextReference` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java
    public static final EncoderDecoder10AImpl INSTANCE = new EncoderDecoder10AImpl();

    private static AtomicReference<JAXBContext> jaxbContextReference = new AtomicReference<>();

    private static final Logger LOG = LogUtils.getL7dLogger(EncoderDecoder10AImpl.class);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jaxbContextReference` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java
    public static final EncoderDecoder10Impl INSTANCE = new EncoderDecoder10Impl();

    private static AtomicReference<JAXBContext> jaxbContextReference = new AtomicReference<>();

    private static final Logger LOG = LogUtils.getL7dLogger(EncoderDecoder10Impl.class);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `reliableEndpoints` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    private RetransmissionQueue retransmissionQueue;
    private RedeliveryQueue redeliveryQueue;
    private Map<Endpoint, RMEndpoint> reliableEndpoints = new ConcurrentHashMap<>();
    private AtomicReference<Timer> timer = new AtomicReference<>();
    private RMConfiguration configuration;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `next` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private Message message;
        private long number;
        private Date next;
        private TimerTask nextTask;
        private int retries;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nextTask` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private long number;
        private Date next;
        private TimerTask nextTask;
        private int retries;
        private int maxRetries;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pending` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private long nextInterval;
        private long backoff;
        private boolean pending;
        private boolean suspended;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `suspendedCandidates` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    private Map<String, List<RedeliverCandidate>> candidates =
        new HashMap<>();
    private Map<String, List<RedeliverCandidate>> suspendedCandidates =
        new HashMap<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxRetries` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private TimerTask nextTask;
        private int retries;
        private int maxRetries;
        private long nextInterval;
        private long backoff;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `retries` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private Date next;
        private TimerTask nextTask;
        private int retries;
        private int maxRetries;
        private long nextInterval;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `candidates` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(RedeliveryQueueImpl.class);

    private Map<String, List<RedeliverCandidate>> candidates =
        new HashMap<>();
    private Map<String, List<RedeliverCandidate>> suspendedCandidates =
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `backoff` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private int maxRetries;
        private long nextInterval;
        private long backoff;
        private boolean pending;
        private boolean suspended;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nextInterval` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        private int retries;
        private int maxRetries;
        private long nextInterval;
        private long backoff;
        private boolean pending;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `manager` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        new HashMap<>();

    private RMManager manager;

    private int undeliveredCount;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `undeliveredCount` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    private RMManager manager;

    private int undeliveredCount;

    public RedeliveryQueueImpl(RMManager m) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `retries` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private Date next;
        private TimerTask nextTask;
        private int retries;
        private int maxRetries;
        private long nextInterval;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxRetries` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private TimerTask nextTask;
        private int retries;
        private int maxRetries;
        private long nextInterval;
        private long backoff;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `message` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
     */
    protected class ResendCandidate implements Runnable, RetryStatus {
        private Message message;
        private long number;
        private Date next;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `nextInterval` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private int retries;
        private int maxRetries;
        private long nextInterval;
        private long backoff;
        private boolean pending;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `pending` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private long nextInterval;
        private long backoff;
        private boolean pending;
        private boolean suspended;
        private boolean includeAckRequested;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `suspended` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private long backoff;
        private boolean pending;
        private boolean suspended;
        private boolean includeAckRequested;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `unacknowledgedCount` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
    private RMManager manager;

    private int unacknowledgedCount;

    public RetransmissionQueueImpl(RMManager m) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `backoff` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private int maxRetries;
        private long nextInterval;
        private long backoff;
        private boolean pending;
        private boolean suspended;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `manager` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
    private final Map<String, List<ResendCandidate>> suspendedCandidates = new HashMap<>();
    private Resender resender;
    private RMManager manager;

    private int unacknowledgedCount;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `next` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        private Message message;
        private long number;
        private Date next;
        private TimerTask nextTask;
        private int retries;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `createdConnection` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    private boolean keepConnection = true;
    private Connection connection;
    private boolean createdConnection = true;

    private Map<Statement, Lock> statementLocks;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `schemaName` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    private String userName;
    private String password;
    private String schemaName;

    private long initialReconnectDelay = 60000L;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialReconnectDelay` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    private String schemaName;

    private long initialReconnectDelay = 60000L;
    private int useExponentialBackOff = 2;
    private int maxReconnectAttempts = 10;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `maxReconnectAttempts` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    private long initialReconnectDelay = 60000L;
    private int useExponentialBackOff = 2;
    private int maxReconnectAttempts = 10;

    private long reconnectDelay;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `keepConnection` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java

    // the connection and statements are cached only if
    private boolean keepConnection = true;
    private Connection connection;
    private boolean createdConnection = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `connection` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    // the connection and statements are cached only if
    private boolean keepConnection = true;
    private Connection connection;
    private boolean createdConnection = true;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `tokenSupplier` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/CodeAuthSupplier.java`
#### Snippet
```java
public class CodeAuthSupplier implements HttpAuthSupplier {
    private volatile String code;
    private BearerAuthSupplier tokenSupplier = new BearerAuthSupplier();
    public CodeAuthSupplier() {
    }
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `contextClasses` is accessed in both synchronized and unsynchronized contexts
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

    JAXBContext context;
    Set<Class<?>> contextClasses;
    Collection<Object> typeRefs = new ArrayList<>();

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `context` is accessed in both synchronized and unsynchronized contexts
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    Class<?>[] extraClass;

    JAXBContext context;
    Set<Class<?>> contextClasses;
    Collection<Object> typeRefs = new ArrayList<>();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `qualifiedSchemas` is accessed in both synchronized and unsynchronized contexts
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    private boolean unwrapJAXBElement = true;
    private boolean scanPackages = true;
    private boolean qualifiedSchemas;

    public JAXBDataBinding() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `typeRefs` is accessed in both synchronized and unsynchronized contexts
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    JAXBContext context;
    Set<Class<?>> contextClasses;
    Collection<Object> typeRefs = new ArrayList<>();

    Class<?> cls;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `refreshTokenLock` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
    private long refreshTokenLifetime; // refresh tokens are eternal by default
    private boolean recycleRefreshTokens = true;
    private Object refreshTokenLock;
    private Map<String, OAuthPermission> permissionMap = new HashMap<>();
    private MessageContext messageContext;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `extensionClass` is accessed in both synchronized and unsynchronized contexts
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
    final Class<?> typeClass;
    final String namespace;
    Class<?> extensionClass;
    String jaxbNamespace;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `lastOnewayArg` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getLogger(AbstractGreeterImpl.class);
    private long delay;
    private String lastOnewayArg;
    private boolean throwAlways;
    private AtomicBoolean useLastOnewayArg = new AtomicBoolean();
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverLaunchFailed` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
    private boolean serverIsReady;
    private boolean serverIsStopped;
    private boolean serverLaunchFailed;
    private Map<String, String> properties;
    private String[] serverArgs;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverIsReady` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java

    private Process process;
    private boolean serverIsReady;
    private boolean serverIsStopped;
    private boolean serverLaunchFailed;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `serverIsStopped` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
    private Process process;
    private boolean serverIsReady;
    private boolean serverIsStopped;
    private boolean serverLaunchFailed;
    private Map<String, String> properties;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `inProcess` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
    final String className;

    private boolean inProcess = DEFAULT_IN_PROCESS;
    private AbstractTestServerBase inProcessServer;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `process` is accessed in both synchronized and unsynchronized contexts
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
    private AbstractTestServerBase inProcessServer;

    private Process process;
    private boolean serverIsReady;
    private boolean serverIsStopped;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `invoker` is accessed in both synchronized and unsynchronized contexts
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        new ArrayList<>();
    private QName serviceName;
    private Invoker invoker;
    private Executor executor;
    private List<String> ignoredClasses = new ArrayList<>(Arrays.asList(
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `incomingObserver` is accessed in both synchronized and unsynchronized contexts
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
    private EndpointInfo endpointInfo;
    private OrbConfig orbConfig;
    private MessageObserver incomingObserver;
    private CorbaTypeMap typeMap;
    private byte[] objectId;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `orb` is accessed in both synchronized and unsynchronized contexts
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
    private EndpointReferenceType target;
    private MessageObserver incomingObserver;
    private ORB orb;
    private OrbConfig orbConfig;
    private CorbaTypeMap typeMap;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `orbConfig` is accessed in both synchronized and unsynchronized contexts
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
    private MessageObserver incomingObserver;
    private ORB orb;
    private OrbConfig orbConfig;
    private CorbaTypeMap typeMap;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `jcaBusFactory` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
    private static final ResourceBundle BUNDLE = BundleUtils.getBundle(ManagedConnectionFactoryImpl.class);

    protected JCABusFactory jcaBusFactory;

    public ManagedConnectionFactoryImpl() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
    private static final ResourceBundle BUNDLE = BundleUtils.getBundle(JCABusFactory.class);

    private Bus bus;
    private List<Server> servantsCache = new ArrayList<>();
    private ClassLoader appserverClassLoader;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `servantsCache` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java

    private Bus bus;
    private List<Server> servantsCache = new ArrayList<>();
    private ClassLoader appserverClassLoader;
    private ManagedConnectionFactoryImpl mcf;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `writer` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/core/logging/WriterHandler.java`
#### Snippet
```java
    private boolean doneHeader;

    private Writer writer;

    public WriterHandler(Writer inWriter) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientProxy` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
    private Bus bus;
    private Object associatedHandle;
    private Object clientProxy;

    public ManagedConnectionImpl(ManagedConnectionFactoryImpl mcf,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `mcf` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
    private PrintWriter printWriter;

    private ManagedConnectionFactoryImpl mcf;
    private ConnectionRequestInfo connReqInfo;
    private boolean isClosed;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
    private ConnectionRequestInfo connReqInfo;
    private boolean isClosed;
    private Bus bus;
    private Object associatedHandle;
    private Object clientProxy;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `entity` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java

    public class AhcWebSocketWrappedOutputStream extends WrappedOutputStream {
        private AhcWebSocketConduitRequest entity;
        private volatile Response response;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionBuilderRegistryImpl.java`
#### Snippet
```java
    private boolean ignoreUnknownAssertions = true;
    private Set<QName> ignored = new HashSet<>();
    private Bus bus;
    private boolean dynamicLoaded;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyInterceptorProviderRegistryImpl.java`
#### Snippet
```java
    implements PolicyInterceptorProviderRegistry, BusExtension {

    private Bus bus;
    private boolean dynamicLoaded;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `data` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/jaxb/JaxbAssertion.java`
#### Snippet
```java
    private Set<Class<?>> classes;

    private T data;

    public JaxbAssertion() {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `alternativeSelector` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
    private Collection<Element> policyReferenceElements;
    private boolean ignoreUnknownAssertions;
    private AlternativeSelector alternativeSelector;
    private boolean enabled = true;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ignoreUnknownAssertions` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
    private Collection<Element> policyElements;
    private Collection<Element> policyReferenceElements;
    private boolean ignoreUnknownAssertions;
    private AlternativeSelector alternativeSelector;
    private boolean enabled = true;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `enabled` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
    private boolean ignoreUnknownAssertions;
    private AlternativeSelector alternativeSelector;
    private boolean enabled = true;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `requestor` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
    private EndpointInfo ei;
    private PolicyEngineImpl engine;
    private boolean requestor;
    private Assertor assertor;

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `faultVocabulary` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java

    private volatile Collection<Assertion> vocabulary;
    private Collection<Assertion> faultVocabulary;
    private volatile List<Interceptor<? extends Message>> interceptors;
    private List<Interceptor<? extends Message>> faultInterceptors;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `engine` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java

    private EndpointInfo ei;
    private PolicyEngineImpl engine;
    private boolean requestor;
    private Assertor assertor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ei` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
    private List<Interceptor<? extends Message>> faultInterceptors;

    private EndpointInfo ei;
    private PolicyEngineImpl engine;
    private boolean requestor;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `faultInterceptors` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
    private Collection<Assertion> faultVocabulary;
    private volatile List<Interceptor<? extends Message>> interceptors;
    private List<Interceptor<? extends Message>> faultInterceptors;

    private EndpointInfo ei;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `policyProviders` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    private Bus bus;
    private PolicyRegistry registry;
    private Collection<PolicyProvider> policyProviders;
    private Collection<PolicyProvider> preSetPolicyProviders = new LinkedList<>();
    private Policy busPolicy;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `alternativeSelector` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    private Boolean ignoreUnknownAssertions;
    private boolean addedBusInterceptors;
    private AlternativeSelector alternativeSelector;


```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    private static final String POLICY_INFO_ENDPOINT_CLIENT = "policy-engine-info-client-endpoint";

    private Bus bus;
    private PolicyRegistry registry;
    private Collection<PolicyProvider> policyProviders;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `ignoreUnknownAssertions` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    private Policy busPolicy;
    private boolean enabled = true;
    private Boolean ignoreUnknownAssertions;
    private boolean addedBusInterceptors;
    private AlternativeSelector alternativeSelector;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `enabled` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    private Collection<PolicyProvider> preSetPolicyProviders = new LinkedList<>();
    private Policy busPolicy;
    private boolean enabled = true;
    private Boolean ignoreUnknownAssertions;
    private boolean addedBusInterceptors;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `bus` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/DomainExpressionBuilderRegistry.java`
#### Snippet
```java

    private boolean dynamicLoaded;
    private Bus bus;
    public DomainExpressionBuilderRegistry() {
        super(null);
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cek` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractContentEncryptionAlgorithm.java`
#### Snippet
```java
    implements ContentEncryptionProvider {
    private static final int DEFAULT_IV_SIZE = 128;
    private byte[] cek;
    private byte[] iv;
    private AtomicInteger providedIvUsageCount;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `descriptors` is accessed in both synchronized and unsynchronized contexts
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java
    private List<QName> attributes = new ArrayList<>();
    private List<QName> elements = new ArrayList<>();
    private PropertyDescriptor[] descriptors;
    private TypeMapping typeMapping;
    private volatile boolean initialized;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `entity` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
    public class NettyWrappedOutputStream extends WrappedOutputStream {
        final HTTPClientPolicy csPolicy;
        NettyHttpClientRequest entity;
        volatile HttpResponse httpResponse;
        volatile Throwable exception;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `isAsync` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        volatile Channel channel;
        volatile SSLSession session;
        boolean isAsync;
        ByteBuf outBuffer;
        OutputStream outputStream;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `outputStream` is accessed in both synchronized and unsynchronized contexts
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        boolean isAsync;
        ByteBuf outBuffer;
        OutputStream outputStream;

        protected NettyWrappedOutputStream(Message message, boolean possibleRetransmit,
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `notificator` is accessed in both synchronized and unsynchronized contexts
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
    private final String subscriptionIdElementName;
    private String url;
    private NotificatorService notificator;

    public SubscriptionManagerImpl(String url) {
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `baseClients` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
    private TLSConfiguration secConfig;
    private boolean closed;
    private Set<WebClient> baseClients =
        Collections.synchronizedSet(Collections.newSetFromMap(new WeakHashMap<WebClient, Boolean>()));

```

### FieldAccessedSynchronizedAndUnsynchronized
Field `cfg` is accessed in both synchronized and unsynchronized contexts
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        Arrays.asList("GET", "POST", "HEAD", "OPTIONS", "PUT", "DELETE", "TRACE"));

    protected ClientConfiguration cfg = new ClientConfiguration();
    private ClientState state;
    private final AtomicBoolean closed = new AtomicBoolean();
```

## RuleId[ruleID=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`b >> 0` can be replaced with 'b'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/UnsafeUriCharactersEncoder.java`
#### Snippet
```java
        sb.append('%');
        sb.append(HEX_DIGITS[(b >> 4) & 0x0f]);
        sb.append(HEX_DIGITS[(b >> 0) & 0x0f]);
    }

```

## RuleId[ruleID=RedundantExplicitClose]
### RedundantExplicitClose
Redundant 'close()'
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                BufferedOutputStream bos = new BufferedOutputStream(os)) {
                bos.write(crl.getEncoded());
                bos.close();
            }
        } catch (Exception e) {
```

### RedundantExplicitClose
Redundant 'close()'
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                BufferedOutputStream bos = new BufferedOutputStream(os)) {
                bos.write(cert.getEncoded());
                bos.close();
            }
        } catch (Exception e) {
```

## RuleId[ruleID=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `distribution/src/main/release/samples/jax_rs/basic_oidc/src/main/webapp/simpleLogin.html`
#### Snippet
```java
<p/>
     <form action="https://localhost:8080/user/service/info/token" method="POST">
         <input type="submit" id="submitButton" disabled="true" value="Display IdToken Properties"/>
     </form>
</div>
```

### HtmlWrongAttributeValue
Wrong attribute value
in `distribution/src/main/release/samples/jax_rs/search/src/main/resources/browser/index.html`
#### Snippet
```java
            <h2 class="form-upload-heading">Please upload PDF or ODT file</h2>
            <div class="form-group">
                <input id="file" name="file" type="file" multiple="true" class="file"></input>
            </div>
        </form>
```

## RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
### CallToStringConcatCanBeReplacedByOperator
Call to `concat()` can be replaced with '+' expression
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java

    protected Class<?> findClass(String name) throws ClassNotFoundException {
        String path = name.replace('.', '/').concat(".class");
        LOG.fine("findClass " + path);
        byte[] bytes = null;
```

## RuleId[ruleID=StringRepeatCanBeUsed]
### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
        String rResolved = new URI(getRoot(sb.toString())).relativize(new URI(toBeRelativized)).toString();
        StringBuilder relativizedPath = new StringBuilder();
        for (int i = 0; i < slashes; i++) {
            relativizedPath.append("../");
        }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java

    private static void indent(StringBuilder buf, int level) {
        for (int i = 0; i < level; i++) {
            buf.append(INDENT);
        }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/ArrayTypeInfo.java`
#### Snippet
```java
        if (ranks > 0) {
            string.append('[');
            for (int i = 1; i < ranks; i++) {
                string.append(',');
            }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/date/XsDateTimeFormat.java`
#### Snippet
```java
    private void append(StringBuffer pBuffer, int pNum, int pMinLen) {
        String s = Integer.toString(pNum);
        for (int i = s.length(); i < pMinLen; i++) {
            pBuffer.append('0');
        }
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapArrayType.java`
#### Snippet
```java
        StringBuilder expectedFormat = new StringBuilder();
        expectedFormat.append("[x");
        for (int i = 1; i < dimensions.size(); i++) {
            expectedFormat.append(",x");
        }
```

## RuleId[ruleID=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java
    public Proxy getDefaultProxy() {
        if (defaultProxy == null) {
            synchronized (this) {
                if (defaultProxy == null) {
                    defaultProxy = chooseProxy(uri);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java
    public URL getURL() throws MalformedURLException {
        if (url == null) {
            synchronized (this) {
                if (url == null) {
                    url = uri.toURL();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    @Override
    protected void deactivate() {
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    @Override
    protected void activate() {
        synchronized (this) {
            if (registry != null) {
                registry.addDestination(this);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    @Override
    public void shutdown() {
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixMatcherLoader.java`
#### Snippet
```java
    public static PublicSuffixMatcher getDefault() {
        if (defaultInstance == null) {
            synchronized (PublicSuffixMatcherLoader.class) {
                if (defaultInstance == null) {
                    final URL url = PublicSuffixMatcherLoader.class.getResource(
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    private Address setAndGetDefaultAddress() throws URISyntaxException {
        if (defaultAddress == null) {
            synchronized (this) {
                if (defaultAddress == null) {
                    if (fromEndpointReferenceType && getTarget().getAddress().getValue() != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    private Address setAndGetDefaultAddress(String curAddr) throws URISyntaxException {
        if (defaultAddress == null) {
            synchronized (this) {
                if (defaultAddress == null) {
                    if (curAddr != null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlEncOutInterceptor.java`
#### Snippet
```java

    private byte[] getSymmetricKey(String symEncAlgo) throws Exception {
        synchronized (this) {
            if (symmetricKey == null) {
                KeyGenerator keyGen = KeyUtils.getKeyGenerator(symEncAlgo);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java

    private SecretKey getSymmetricKey(String symEncAlgo) throws Exception {
        synchronized (this) {
            if (symmetricKey == null) {
                KeyGenerator keyGen = KeyUtils.getKeyGenerator(symEncAlgo);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextJAXBUtils.java`
#### Snippet
```java
     */
    public static JAXBContext getJAXBContext() throws JAXBException {
        synchronized (ContextJAXBUtils.class) {
            if (jaxbContext == null || jaxbContextClasses == null) {
                Set<Class<?>> tmp = new HashSet<>();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextJAXBUtils.java`
#### Snippet
```java
     */
    public static void setJAXBContext(JAXBContext ctx) {
        synchronized (ContextJAXBUtils.class) {
            jaxbContext = ctx;
            jaxbContextClasses = new HashSet<>();
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
         */
        public static JAXBContext getJAXBContext() throws JAXBException {
            synchronized (Names200408.class) {
                if (jaxbContext == null) {
                    jaxbContext = JAXBContext.newInstance(ObjectFactory.class);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
         */
        public static void setJAXBContext(JAXBContext ctx) throws JAXBException {
            synchronized (Names200408.class) {
                jaxbContext = ctx;
            }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
         */
        public static JAXBContext getJAXBContext() throws JAXBException {
            synchronized (Names200403.class) {
                if (jaxbContext == null) {
                    jaxbContext = JAXBContext.newInstance(org.apache.cxf.ws.addressing.v200403.ObjectFactory.class);
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
         */
        public static void setJAXBContext(JAXBContext ctx) throws JAXBException {
            synchronized (Names200403.class) {
                jaxbContext = ctx;
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
        }

        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java

        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java

    public void run() {
        synchronized (this) {
            while (!inShutdown) {
                try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
            while (!inShutdown) {
                try {
                    wait();
                } catch (InterruptedException ex) {
                    // ignore
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                while (!q.isShutdown()) {
                    try {
                        wait(100L);
                    } catch (InterruptedException ex) {
                        // ignore
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
            return clazz;
        }
        synchronized (this) {
            if (clazz == null) {
                clazz = tryClass(className, cl);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
            return intf;
        }
        synchronized (this) {
            if (intf == null) {
                intf = tryClass(interfaceName, cl);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
            lifeCycleManager.preShutdown();
        }
        synchronized (this) {
            state = BusState.SHUTTING_DOWN;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
        }
        destroyBeans();
        synchronized (this) {
            state = BusState.SHUTDOWN;
            notifyAll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
        synchronized (this) {
            state = BusState.SHUTDOWN;
            notifyAll();
        }
        if (null != lifeCycleManager) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
    public synchronized void releaseChain() {
        this.chainReleased = true;
        this.notifyAll();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
        while (!chainReleased) {
            try {
                this.wait();
            } catch (InterruptedException ex) {
                // ignore
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
        delegate.complete(res);

        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java

        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
            delegate.cancel(mayInterruptIfRunning);
            
            synchronized (this) {
                notifyAll();
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
            
            synchronized (this) {
                notifyAll();
            }
            
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
     */
    public Map<String, Object> getResponseContext() throws InterruptedException, ExecutionException {
        synchronized (this) {
            if (!delegate.isDone()) {
                wait();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
        synchronized (this) {
            if (!delegate.isDone()) {
                wait();
            }
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
        delegate.completeExceptionally(ex);
        
        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
        
        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `core/src/main/java/org/apache/cxf/validation/AbstractValidationInterceptor.java`
#### Snippet
```java
            } else {
                // don't create 2 validator factories and one not released!
                synchronized (this) {
                    if (provider == null) {
                        provider = new BeanValidationProvider();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            try {
                while (client.isRunning()) {
                    synchronized (this) {
                        sleep(selectInterval);
                        // make sure pending leases fail in a timely manner,
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait();
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }
        protected synchronized void handleCancelled() {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }
        protected synchronized void setException(Exception ex) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        }
        protected synchronized void handleCancelled() {
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            try {
                while (client.getStatus() == IOReactorStatus.ACTIVE) {
                    synchronized (this) {
                        sleep(selectInterval);

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait();
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java

        protected synchronized void handleCancelled() {
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
        Destination result = replyToDestinationDest;
        if (result == null) {
            synchronized (this) {
                result = replyToDestinationDest;
                if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
        ConnectionFactory factory = connectionFactory;
        if (factory == null) {
            synchronized (this) {
                factory = connectionFactory;
                if (factory == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java

    public void resetCachedReplyDestination() {
        synchronized (this) {
            if (replyDestinationDest instanceof TemporaryQueue) {
                try {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
    public Destination getReplyDestination(Session session) throws JMSException {
        if (this.replyDestinationDest == null) {
            synchronized (this) {
                if (this.replyDestinationDest == null) {
                    this.replyDestinationDest = getReplyDestinationInternal(session);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    private void setupReplyDestination(Session session) throws JMSException {
        if (staticReplyDestination == null) {
            synchronized (this) {
                if (staticReplyDestination == null) {
                    Destination staticReplyDestinationTmp = jmsConfig.getReplyDestination(session);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
        Connection result = connection;
        if (result == null) {
            synchronized (this) {
                result = connection;
                if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/continuations/JMSContinuation.java`
#### Snippet
```java
        workQueue.schedule(new Runnable() {
            public void run() {
                synchronized (JMSContinuation.this) {
                    if (isPending && !isCanceled) {
                        doResume();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
            return instantiator;
        }
        synchronized (this) {
            if (instantiator != null) {
                return instantiator;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
    public C register(Object provider, int bindingPriority) {
        if (Instantiator.class.isInstance(provider)) {
            synchronized (this) {
                instantiator = Instantiator.class.cast(provider);
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java
        // from within singleton root resources (not default)
        if (resolveContexts && cri != null && cri.isSingleton() && instance != null && cri.contextsAvailable()) {
            synchronized (this) {
                if (!injectedSubInstances.contains(instance.toString())) {
                    Application app = null;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        delegate.complete(res);
        
        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        
        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        delegate.completeExceptionally(mapThrowable(ex));
        
        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        
        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
            while (!done) {
                try {
                    wait();
                } catch (InterruptedException e) {
                    //ignore
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
                continuation.resume();
            }
            notifyAll();
        }
        public boolean hasContinuation() {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionInputStream.java`
#### Snippet
```java
            exception = e;
        }
        notifyAll();
    }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionInputStream.java`
#### Snippet
```java
        if (exception == null && buf == null) {
            try {
                wait();
            } catch (InterruptedException e) {
                throw new IOException();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionInputStream.java`
#### Snippet
```java
        buf.put(b);
        buf.flip();
        notifyAll();
    }
}
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder.java`
#### Snippet
```java
        DataBinding result = databinding;
        if (result == null) {
            synchronized (this) {
                result = databinding;
                if (result == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
            //next wasn't found, nothing to resume, assume it will come in later...
        } finally {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java

        public void run() {
            synchronized (DestinationSequence.this) {
                DestinationSequence.this.scheduledTermination = null;
                RMEndpoint rme = destination.getReliableEndpoint();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
        boolean updated = false;

        synchronized (this) {
            boolean done = false;
            int i = 0;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                //if we get here, there isn't a continuation available
                //so we need to block/wait
                wait();
            } catch (InterruptedException ie) {
                // ignore
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                // already logged
            } finally {
                synchronized (DestinationSequence.this) {
                    DestinationSequence.this.deferredAcknowledgments.remove(this);
                }
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder11Impl.java`
#### Snippet
```java
        JAXBContext jaxbContext = jaxbContextReference.get();
        if (jaxbContext == null) {
            synchronized (EncoderDecoder11Impl.class) {
                jaxbContext = jaxbContextReference.get();
                if (jaxbContext == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SourceSequence.java`
#### Snippet
```java

        final long result;
        synchronized (this) {
            currentMessageNumber++;
            if (last) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java
        JAXBContext jaxbContext = jaxbContextReference.get();
        if (jaxbContext == null) {
            synchronized (EncoderDecoder10AImpl.class) {
                jaxbContext = jaxbContextReference.get();
                if (jaxbContext == null) {
```

### SynchronizeOnThis
Lock operations on a class may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java
        JAXBContext jaxbContext = jaxbContextReference.get();
        if (jaxbContext == null) {
            synchronized (EncoderDecoder10Impl.class) {
                jaxbContext = jaxbContextReference.get();
                if (jaxbContext == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        String key = sid.getValue();

        synchronized (this) {
            List<RedeliverCandidate> sequenceCandidates = getSequenceCandidates(key);
            if (null != sequenceCandidates) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java

    public void stop(DestinationSequence seq) {
        synchronized (this) {
            List<RedeliverCandidate> sequenceCandidates = getSequenceCandidates(seq);
            if (null != sequenceCandidates) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java

    public void suspend(DestinationSequence seq) {
        synchronized (this) {
            String key = seq.getIdentifier().getValue();
            List<RedeliverCandidate> sequenceCandidates = candidates.remove(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    public void purgeAll(DestinationSequence seq) {
        Collection<Long> purged = new ArrayList<>();
        synchronized (this) {
            LOG.fine("Start purging redeliver candidates.");
            List<RedeliverCandidate> sequenceCandidates = getSequenceCandidates(seq);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
        RedeliverCandidate candidate;

        synchronized (this) {
            List<RedeliverCandidate> sequenceCandidates = getSequenceCandidates(key);
            if (null == sequenceCandidates) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java

    public void resume(DestinationSequence seq) {
        synchronized (this) {
            String key = seq.getIdentifier().getValue();
            List<RedeliverCandidate> sequenceCandidates = suspendedCandidates.remove(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        final ResendCandidate candidate;

        synchronized (this) {
            List<ResendCandidate> sequenceCandidates = getSequenceCandidates(key);
            if (null == sequenceCandidates) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java

    public void suspend(SourceSequence seq) {
        synchronized (this) {
            String key = seq.getIdentifier().getValue();
            List<ResendCandidate> sequenceCandidates = candidates.remove(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
        Collection<ResendCandidate> resends = new ArrayList<>();
        Identifier sid = seq.getIdentifier();
        synchronized (this) {
            LOG.fine("Start purging resend candidates.");
            List<ResendCandidate> sequenceCandidates = getSequenceCandidates(seq);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java

    public void resume(SourceSequence seq) {
        synchronized (this) {
            String key = seq.getIdentifier().getValue();
            List<ResendCandidate> sequenceCandidates = suspendedCandidates.remove(key);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
     */
    public void stop(SourceSequence seq) {
        synchronized (this) {
            List<ResendCandidate> sequenceCandidates = getSequenceCandidates(seq);
            if (null != sequenceCandidates) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
            }
        } else {
            synchronized (this) {
                // update the status of the cached connection
                if (e == null) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
        } else {
            // return the cached connection or create and cache a new one if the old one is dead
            synchronized (this) {
                if (createdConnection && nextReconnectAttempt > 0
                    && (maxReconnectAttempts < 0 || maxReconnectAttempts > reconnectAttempts)) {
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
                }
            } finally {
                synchronized (this) {
                    written = true;
                    notifyAll();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
                synchronized (this) {
                    written = true;
                    notifyAll();
                }
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java

        public ByteArrayOutputStream getResponseStream() throws Exception {
            synchronized (this) {
                while (!written) {
                    wait(10 * 1000);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            synchronized (this) {
                while (!written) {
                    wait(10 * 1000);
                }
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public void resetLastOnewayArg() {
        synchronized (this) {
            lastOnewayArg = null;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public void greetMeOneWay(String arg0) {
        synchronized (this) {
            lastOnewayArg = arg0;
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractContentEncryptionAlgorithm.java`
#### Snippet
```java
            theCek = secretKey.getEncoded();
            if (generateCekOnce) {
                synchronized (this) {
                    cek = theCek;
                }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait(entity.getReceiveTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected synchronized void setChannel(Channel ch) {
            channel = ch;
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    try {
                        // connection timeout
                        wait(entity.getConnectionTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                }
            }
            notifyAll();
        }

```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java

        delegate.completeExceptionally(ex);
        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
        delegate.completeExceptionally(ex);
        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
        
        delegate.complete(res);
        synchronized (this) {
            notifyAll();
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
        delegate.complete(res);
        synchronized (this) {
            notifyAll();
        }
    }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
            
            delegate.cancel(mayInterruptIfRunning);
            synchronized (this) {
                notifyAll();
            }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
            delegate.cancel(mayInterruptIfRunning);
            synchronized (this) {
                notifyAll();
            }
            
```

## RuleId[ruleID=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
    }

    public Socket createSocket(String host, int port) throws IOException, UnknownHostException {
        return enableCipherSuites(sslSocketFactory.createSocket(host, port),
                                  new Object[]{host, port});
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
    */
    public Socket createSocket(Socket s, String host, int port, boolean autoClose)
        throws IOException, UnknownHostException  {
        return enableCipherSuites(sslSocketFactory.createSocket(s, host, port, autoClose),
                                  new Object[]{host, port});
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java

    public Socket createSocket(String host, int port, InetAddress localHost, int localPort)
        throws IOException, UnknownHostException {
        return enableCipherSuites(sslSocketFactory.createSocket(host, port, localHost, localPort),
                                  new Object[]{host, port});
```

### DuplicateThrows
There is a more general exception, 'org.apache.cxf.jaxrs.ext.search.SearchParseException', in the throws list already.
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java

    protected TypeInfo getTypeInfo(String setter, String value)
        throws SearchParseException, PropertyNotFoundException {

        String name = getSetter(setter);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
    }

    public String resolveURI(String uri) throws MalformedURLException, IOException {
        if (catalog == null) {
            return null;
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
    }

    public String resolveSystem(String sys) throws MalformedURLException, IOException {
        if (catalog == null) {
            return null;
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
        return ((Catalog)catalog).resolveURI(uri);
    }
    public String resolvePublic(String uri, String parent) throws MalformedURLException, IOException {
        if (resolver == null) {
            return null;
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
    }

    private void tryFileSystem(String baseUriStr, String uriStr) throws IOException, MalformedURLException {
        // It is possible that spaces have been encoded.  We should decode them first.
        String fileStr = uriStr.replace("%20", " ");
```

### DuplicateThrows
There is a more general exception, 'java.lang.Throwable', in the throws list already.
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                                                   Message m,
                                                   OperationResourceInfo ori,
                                                   Method invoked) throws IOException, Throwable {
        List<ProviderInfo<ContainerResponseFilter>> containerFilters =
            pf.getContainerResponseFilters(ori == null ? null : ori.getNameBindings());
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
    }

    public X509CRL readCRL(File crlFile) throws FileNotFoundException, CRLException, IOException {
        try (InputStream is = Files.newInputStream(crlFile.toPath())) {
            return (X509CRL)certFactory.generateCRL(is);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
    }

    public X509Certificate readCertificate(File certFile) throws CertificateException, FileNotFoundException,
        IOException {
        try (InputStream is = Files.newInputStream(certFile.toPath())) {
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
    }

    private static void readArchive(String name) throws MalformedURLException, IOException {
        List<String> nameComponents = tokenizePathComponents(name);
        final Map<String, Object> map;
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java

    public static synchronized Map<?, ?> getJarContents(String path)
        throws MalformedURLException, IOException {
        if (!archives.containsKey(path)) {
            loadArchive(path);
```

### DuplicateThrows
There is a more general exception, 'java.io.IOException', in the throws list already.
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
    }

    private static void loadArchive(String path) throws MalformedURLException, IOException {
        List<String> nameComponents = tokenizePathComponents(path);

```

## RuleId[ruleID=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'Element' type conflicts with preceding 'instanceof Node' check
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            return StaxUtils.createXMLStreamWriter((OutputStream)source);
        } else if (source instanceof Node) {
            return new W3CDOMStreamWriter((Element)source);
        }
        throw new Fault(new Message("UNKNOWN_SOURCE", LOG, source.getClass().getName()));
```

## RuleId[ruleID=UnusedAssignment]
### UnusedAssignment
Variable `allowX509FromJKS` initializer `true` is redundant
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
    private Crypto fallbackCrypto;
    private XKMSClientCache xkmsClientCache;
    private boolean allowX509FromJKS = true;

    public XkmsCryptoProvider(XKMSPortType xkmsConsumer) throws XKMSClientCacheException {
```

### UnusedAssignment
Variable `conditionType` initializer `null` is redundant
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/odata/ODataParser.java`
#### Snippet
```java
            }

            ConditionType conditionType = null;
            switch (operator) {
            case EQ:
```

### UnusedAssignment
Variable `threshold` initializer `defaultThreshold` is redundant
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
    protected OutputStream currentStream;

    private long threshold = defaultThreshold;
    private long maxSize = defaultMaxSize;
    private File outputDir = DEFAULT_TEMP_DIR;
```

### UnusedAssignment
Variable `threshold` initializer `defaultThreshold` is redundant
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java

    private boolean cosClosed;
    private long threshold = defaultThreshold;
    private long maxSize = defaultMaxSize;
    private File outputDir = DEFAULT_TEMP_DIR;
```

### UnusedAssignment
Variable `ap` initializer `null` is redundant
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        for (int x = 0; x < value.length(); x++) {
            char ch = value.charAt(x);
            String ap = null;
            switch (ch) {
            case '\"':
```

### UnusedAssignment
Variable `ctx` initializer `null` is redundant
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    public static JAXBContext createContext(final Set<Class<?>> classes,
                                            final Map<String, Object> map) throws JAXBException {
        JAXBContext ctx = null;
        try {
            ctx = AccessController.doPrivileged(new PrivilegedExceptionAction<JAXBContext>() {
```

### UnusedAssignment
Variable `indentAmount` initializer `DEFAULT_INDENT_LEVEL` is redundant
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java

    int curIndent;
    int indentAmount = DEFAULT_INDENT_LEVEL;
    final Deque<CurrentElement> elems = new ArrayDeque<>();

```

### UnusedAssignment
Variable `name` initializer `"default"` is redundant
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java


    String name = "default";
    int maxQueueSize;
    int initialThreads;
```

### UnusedAssignment
Variable `blockOriginalReader` initializer `true` is redundant
in `core/src/main/java/org/apache/cxf/staxutils/transform/InTransformReader.java`
#### Snippet
```java
    private ParsingEvent currentEvent;
    private List<Integer> attributesIndexes = new ArrayList<>();
    private boolean blockOriginalReader = true;
    private boolean attributesIndexed;
    private DelegatingNamespaceContext namespaceContext;
```

### UnusedAssignment
Variable `maxHeaderLength` initializer `DEFAULT_MAX_HEADER_SIZE` is redundant
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
    private List<String> supportedTypes;

    private int maxHeaderLength = DEFAULT_MAX_HEADER_SIZE;

    public AttachmentDeserializer(Message message) {
```

### UnusedAssignment
Variable `locale` initializer `null` is redundant
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java

            // Parse the locale
            Locale locale = null;
            String[] l = arr[0].split("_");
            switch (l.length) {
```

### UnusedAssignment
Variable `refsToCheck` initializer `null` is redundant
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageChecker.java`
#### Snippet
```java

        for (XPathExpression xPathExpression : this.xPaths) {
            Collection<WSDataRef> refsToCheck = null;

            switch (xPathExpression.getType()) {
```

### UnusedAssignment
Variable `randomBytes` initializer `null` is redundant
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/STSInvoker.java`
#### Snippet
```java
        final byte[] secret;
        writer.writeStartElement(prefix, "RequestedProofToken", namespace);
        byte[] randomBytes = null;
        try {
            randomBytes = XMLSecurityConstants.generateBytes(keySize / 8);
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
        }

        Object result = null;
        ClassLoaderHolder contextLoader = null;
        AsyncResponseImpl asyncResponse = null;
```

### UnusedAssignment
Variable `res` initializer `null` is redundant
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java

        final MessageContext oldCtx = WebServiceContextImpl.setMessageContext(ctx);
        List<Object> res = null;
        try {
            if ((params == null || params.isEmpty()) && serviceObject instanceof Provider) {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
        boolean isAsync = isAsync(method);

        Object result = null;
        try {
            if (isAsync) {
```

### UnusedAssignment
Variable `ds` initializer `null` is redundant
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java

    private DataSource createDataSource(Source o, String ct) {
        DataSource ds = null;

        if (o instanceof StreamSource) {
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/PersistenceUtils.java`
#### Snippet
```java

    public SequenceAcknowledgement deserialiseAcknowledgment(InputStream is) {
        Object obj = null;
        XMLStreamReader reader = StaxUtils.createXMLStreamReader(is);
        try {  //NOPMD
```

### UnusedAssignment
The value `Collections.emptyList()` assigned to `this.codeVerifierTransformers` is never used
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AuthorizationCodeGrantHandler.java`
#### Snippet
```java
    public void setCodeVerifierTransformers(List<CodeVerifierTransformer> codeVerifierTransformers) {
        if (codeVerifierTransformers == null) {
            this.codeVerifierTransformers = Collections.emptyList();
        }
        this.codeVerifierTransformers = new ArrayList<>(codeVerifierTransformers);
```

### UnusedAssignment
Variable `theNewScopes` initializer `null` is redundant
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
                                                     List<String> restrictedScopes) {

        List<OAuthPermission> theNewScopes = null;

        if (restrictedScopes.isEmpty()) {
```

### UnusedAssignment
Variable `validFrom` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            response.setTokenId(renewedAssertion.getId());

            Instant validFrom = null;
            Instant validTill = null;
            if (renewedAssertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
```

### UnusedAssignment
Variable `validTill` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java

            Instant validFrom = null;
            Instant validTill = null;
            if (renewedAssertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
                validFrom = renewedAssertion.getSaml2().getConditions().getNotBefore();
```

### UnusedAssignment
Variable `validFrom` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java
            response.setToken(token);

            Instant validFrom = null;
            Instant validTill = null;
            if (assertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
```

### UnusedAssignment
Variable `validTill` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java

            Instant validFrom = null;
            Instant validTill = null;
            if (assertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
                validFrom = assertion.getSaml2().getConditions().getNotBefore();
```

### UnusedAssignment
Variable `validFrom` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/SAMLTokenValidator.java`
#### Snippet
```java
        SamlAssertionWrapper assertion, ReceivedToken validateTarget
    ) {
        Instant validFrom = null;
        Instant validTill = null;
        Instant issueInstant = null;
```

### UnusedAssignment
Variable `validTill` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/SAMLTokenValidator.java`
#### Snippet
```java
    ) {
        Instant validFrom = null;
        Instant validTill = null;
        Instant issueInstant = null;
        if (assertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
```

### UnusedAssignment
Variable `issueInstant` initializer `null` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/SAMLTokenValidator.java`
#### Snippet
```java
        Instant validFrom = null;
        Instant validTill = null;
        Instant issueInstant = null;
        if (assertion.getSamlVersion().equals(SAMLVersion.VERSION_20)) {
            validFrom = assertion.getSaml2().getConditions().getNotBefore();
```

### UnusedAssignment
Variable `inProcess` initializer `DEFAULT_IN_PROCESS` is redundant
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
    final String className;

    private boolean inProcess = DEFAULT_IN_PROCESS;
    private AbstractTestServerBase inProcessServer;

```

### UnusedAssignment
Variable `member` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaStructListener.java`
#### Snippet
```java
        if (currentTypeListener == null) {
            QName elName = name;
            MemberType member = null;
            while (true) {
                member = structMembers.get(memberCount++);
```

### UnusedAssignment
Variable `data` initializer `""` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
    }
    public String getDataFromAny() {
        String data = "";
        if (valueAsString != null) {
            return valueAsString;
```

### UnusedAssignment
Variable `data` initializer `""` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            return valueAsString;
        }
        String data = "";

        switch (this.typeCode.kind().value()) {
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java

    public void setValueFromData(String data) {
        Object obj = null;
        switch (typeCode.kind().value()) {
        case TCKind._tk_boolean:
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
    public static QName getPrimitiveIdlTypeFromTypeCode(TypeCode tc) {
        TCKind type = tc.kind();
        QName result = null;

        switch(type.value()) {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
        QName idlType = handler.getIdlType();
        TypeCode tc = handler.getTypeCode();
        CorbaTypeEventProducer result = null;
        if (CorbaUtils.isPrimitiveIdlType(idlType)) {
            result = new CorbaPrimitiveTypeEventProducer(handler);
```

### UnusedAssignment
Variable `handler` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
                                                       QName idlType,
                                                       CorbaTypeMap typeMap) {
        CorbaObjectHandler handler = null;
        TypeCode tc = CorbaUtils.getTypeCode(orb, idlType, typeMap);
        try {
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
            throw new CorbaBindingException(ex);
        }
        CorbaTypeListener result = null;
        if (CorbaUtils.isPrimitiveIdlType(idlType)) {
            handler = new CorbaPrimitiveHandler(name, idlType, tc, null);
```

### UnusedAssignment
Variable `ex` initializer `null` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java

        int n = binarySearch(IDS, id);
        SystemException ex = null;
        switch (n) {
        case BAD_CONTEXT:
```

### UnusedAssignment
Variable `target` initializer `null` is redundant
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBInvoker.java`
#### Snippet
```java
        }

        Object target = null;

        if (ep == null) {
```

### UnusedAssignment
Variable `doc` initializer `null` is redundant
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/RemoteReferenceResolver.java`
#### Snippet
```java
            return null;
        }
        Document doc = null;
        try {
            doc = StaxUtils.read(is.getByteStream());
```

### UnusedAssignment
Variable `newElem` initializer `null` is redundant
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
            {
                // Ok, need to add a new element and simulate recursion
                Element newElem = null;
                String nsURI = r.getNamespaceURI();
                String elemPrefix = r.getPrefix(); // needed for special
```

## RuleId[ruleID=IndexOfReplaceableByContains]
### IndexOfReplaceableByContains
`ct.indexOf("charset=") == -1` can be replaced with '!ct.contains("charset=")'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
        if (null != ct) {
            if (enc != null
                && ct.indexOf("charset=") == -1
                && !ct.toLowerCase().contains("multipart/related")) {
                ct = ct + "; charset=" + enc;
```

### IndexOfReplaceableByContains
`ct.indexOf("charset=") == -1` can be replaced with '!ct.contains("charset=")'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
        if (null != ct
            && null != enc
            && ct.indexOf("charset=") == -1
            && !ct.toLowerCase().contains("multipart/related")) {
            return ct + "; charset=" + enc;
```

### IndexOfReplaceableByContains
`value.indexOf("\'\'") == -1` can be replaced with '!value.contains("\\'\\'")'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java

    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
```

### IndexOfReplaceableByContains
`SystemPropertyAction.getProperty("os.name").toLowerCase().indexOf("windows") > -1` can be replaced with 'SystemPropertyAction.getProperty("os.name").toLowerCase().contains("windows")'
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
        String platformjavacname = "javac";

        if (SystemPropertyAction.getProperty("os.name").toLowerCase().indexOf("windows") > -1) {
            platformjavacname = "javac.exe";
        }
```

### IndexOfReplaceableByContains
`args[i].indexOf("package-info.java") > -1` can be replaced with 'args\[i\].contains("package-info.java")'
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                            // http://bugs.sun.com/bugdatabase/view_bug.do?bug_id=6198196
                            //
                            if (args[i].indexOf("package-info.java") > -1
                                && SystemPropertyAction.getProperty("os.name")
                                    .toLowerCase().indexOf("windows") > -1) {
```

### IndexOfReplaceableByContains
`SystemPropertyAction.getProperty("os.name") .toLowerCase().index...` can be replaced with 'SystemPropertyAction.getProperty("os.name") .toLowerCase().contains("windows")'
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                            //
                            if (args[i].indexOf("package-info.java") > -1
                                && SystemPropertyAction.getProperty("os.name")
                                    .toLowerCase().indexOf("windows") > -1) {
                                out.println('"' + args[i].replaceAll("/", "\\\\\\\\") + '"');
                            } else {
```

### IndexOfReplaceableByContains
`SystemPropertyAction.getProperty("os.name").toLowerCase().indexOf("windows") > -1` can be replaced with 'SystemPropertyAction.getProperty("os.name").toLowerCase().contains("windows")'
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            }

            if (SystemPropertyAction.getProperty("os.name").toLowerCase().indexOf("windows") > -1) {
                for (int i = 0; i < cmdArray.length; i++) {
                    if (cmdArray[i].indexOf("package-info") == -1) {
```

### IndexOfReplaceableByContains
`cmdArray[i].indexOf("package-info") == -1` can be replaced with '!cmdArray\[i\].contains("package-info")'
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            if (SystemPropertyAction.getProperty("os.name").toLowerCase().indexOf("windows") > -1) {
                for (int i = 0; i < cmdArray.length; i++) {
                    if (cmdArray[i].indexOf("package-info") == -1) {
                        cmdArray[i] = cmdArray[i].replace('\\', '/');
                    }
```

### IndexOfReplaceableByContains
`format.indexOf("{0") >= 0` can be replaced with 'format.contains("{0")'
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
                return format;
            }
            if (format.indexOf("{0") >= 0 || format.indexOf("{1") >= 0
                        || format.indexOf("{2") >= 0 || format.indexOf("{3") >= 0) {
                return java.text.MessageFormat.format(format, parameters);
```

### IndexOfReplaceableByContains
`format.indexOf("{1") >= 0` can be replaced with 'format.contains("{1")'
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
                return format;
            }
            if (format.indexOf("{0") >= 0 || format.indexOf("{1") >= 0
                        || format.indexOf("{2") >= 0 || format.indexOf("{3") >= 0) {
                return java.text.MessageFormat.format(format, parameters);
```

### IndexOfReplaceableByContains
`format.indexOf("{2") >= 0` can be replaced with 'format.contains("{2")'
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
            }
            if (format.indexOf("{0") >= 0 || format.indexOf("{1") >= 0
                        || format.indexOf("{2") >= 0 || format.indexOf("{3") >= 0) {
                return java.text.MessageFormat.format(format, parameters);
            }
```

### IndexOfReplaceableByContains
`format.indexOf("{3") >= 0` can be replaced with 'format.contains("{3")'
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
            }
            if (format.indexOf("{0") >= 0 || format.indexOf("{1") >= 0
                        || format.indexOf("{2") >= 0 || format.indexOf("{3") >= 0) {
                return java.text.MessageFormat.format(format, parameters);
            }
```

### IndexOfReplaceableByContains
`ei.getAddress().indexOf("local://") != -1` can be replaced with 'ei.getAddress().contains("local://")'
in `core/src/main/java/org/apache/cxf/endpoint/ServerImpl.java`
#### Snippet
```java
        //Of course its not an ideal situation here to use a hard-coded prefix. To be refactored.
        if (destinationFactory == null) {
            if (ei.getAddress() != null && ei.getAddress().indexOf("local://") != -1) {
                destinationFactory = bus.getExtension(DestinationFactoryManager.class)
                    .getDestinationFactoryForUri(ei.getAddress());
```

### IndexOfReplaceableByContains
`contentId.indexOf("://") == -1` can be replaced with '!contentId.contains("://")'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            // See please https://www.w3.org/TR/xop10/
            //
            if (contentId.indexOf("://") == -1) {
                return loadDataSource(contentId, atts);
            } else {
```

### IndexOfReplaceableByContains
`contentType.indexOf(s) != -1` can be replaced with 'contentType.contains(s)'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        contentType = contentType.toLowerCase();
        for (String s : types) {
            if (contentType.indexOf(s) != -1) {
                return true;
            }
```

### IndexOfReplaceableByContains
`requestMimeType.indexOf("type=") == -1` can be replaced with '!requestMimeType.contains("type=")'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentSerializer.java`
#### Snippet
```java
        // type is a required parameter for multipart/related only
        if (xopOrMultipartRelated
            && requestMimeType.indexOf("type=") == -1) {
            if (xop) {
                ct.append("; type=\"application/xop+xml\"");
```

### IndexOfReplaceableByContains
`ct.indexOf("fastinfoset") != -1` can be replaced with 'ct.contains("fastinfoset")'
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxInInterceptor.java`
#### Snippet
```java

        String ct = (String)message.get(Message.CONTENT_TYPE);
        if (ct != null && ct.indexOf("fastinfoset") != -1
            && message.getContent(InputStream.class) != null
            && message.getContent(XMLStreamReader.class) == null) {
```

### IndexOfReplaceableByContains
`contentType.indexOf("charset=") == -1` can be replaced with '!contentType.contains("charset=")'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
        // add the encoding information
        if (null != contentType) {
            if (enc != null && contentType.indexOf("charset=") == -1
                && !contentType.toLowerCase().contains("multipart/related")) {
                contentType = contentType + "; charset=" + enc;
```

### IndexOfReplaceableByContains
`result.indexOf("%2B") != -1` can be replaced with 'result.contains("%2B")'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            result = result.replace("+", "%20");
        }
        if (result.indexOf("%2B") != -1) {
            result = result.replace("%2B", "+");
        }
```

### IndexOfReplaceableByContains
`file.indexOf("junit") == -1` can be replaced with '!file.contains("junit")'
in `testutils/src/main/java/org/apache/cxf/testutil/common/TestUtil.java`
#### Snippet
```java
            for (URL url : urlLoader.getURLs()) {
                String file = url.getFile();
                if (file.indexOf("junit") == -1) {
                    classPath.append(url.toURI().getPort());
                    classPath.append(System.getProperty("path.separator"));
```

### IndexOfReplaceableByContains
`name.indexOf("!/") != -1` can be replaced with 'name.contains("!/")'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoaderHelper.java`
#### Snippet
```java

    private static boolean isJarReference(String name) {
        return name.indexOf("!/") != -1;
    }

```

### IndexOfReplaceableByContains
`s.indexOf(prefix) == -1` can be replaced with '!s.contains(prefix)'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/UriHandlerInit.java`
#### Snippet
```java
            s = prefix;
        } else {
            if (s.indexOf(prefix) == -1) {
                s = prefix + "|" + s;
            }
```

### IndexOfReplaceableByContains
`name.indexOf("!/") != -1` can be replaced with 'name.contains("!/")'
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
        }

        if (name.indexOf("!/") != -1) {
            return name.substring(0, name.indexOf("!/"));
        }
```

### IndexOfReplaceableByContains
`tmpPath.indexOf("!/") == -1` can be replaced with '!tmpPath.contains("!/")'
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java

        while (tmpPath.length() > 0) {
            if (tmpPath.indexOf("!/") == -1) {
                tokens.add(tmpPath);
                tmpPath = "";
```

### IndexOfReplaceableByContains
`contentType.indexOf("action=") != -1` can be replaced with 'contentType.contains("action=")'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
            contentType = ct.get(0);
        }
        if (contentType != null && contentType.indexOf("action=") != -1) {
            contentTypeAction = contentType.substring(contentType.indexOf("action=") + 7);
            if (contentTypeAction.indexOf(';') != -1) {
```

### IndexOfReplaceableByContains
`ct.indexOf("action=\"") == -1` can be replaced with '!ct.contains("action=\\"")'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapPreProtocolOutInterceptor.java`
#### Snippet
```java
            String ct = (String) message.get(Message.CONTENT_TYPE);

            if (ct.indexOf("action=\"") == -1) {
                ct = new StringBuilder().append(ct)
                    .append("; action=").append(action).toString();
```

### IndexOfReplaceableByContains
`ct.indexOf("start-info") == -1` can be replaced with '!ct.contains("start-info")'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java

            int start = ct.indexOf("action=");
            if (start == -1 && ct.indexOf("multipart/related") == 0 && ct.indexOf("start-info") == -1) {
                // the action property may not be found at the package's content-type for non-mtom multipart message
                // but skip searching if the start-info property is set
```

### IndexOfReplaceableByContains
`ex.getMessage().indexOf("Addressing") > -1` can be replaced with 'ex.getMessage().contains("Addressing")'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyVerificationInInterceptor.java`
#### Snippet
```java
            //To check if there is ws addressing policy violation and throw WSA specific
            //exception to pass jaxws2.2 tests
            if (ex.getMessage().indexOf("Addressing") > -1) {
                throw new Fault("A required header representing a Message Addressing Property "
                                    + "is not present", LOG)
```

### IndexOfReplaceableByContains
`contentType.indexOf("xml") >= 0` can be replaced with 'contentType.contains("xml")'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrettyLoggingFilter.java`
#### Snippet
```java
        return prettyLogging 
            && contentType != null 
            && contentType.indexOf("xml") >= 0
            && contentType.toLowerCase().indexOf("multipart/related") < 0
            && event.getPayload() != null
```

### IndexOfReplaceableByContains
`contentType.toLowerCase().indexOf("multipart/related") < 0` can be replaced with '!contentType.toLowerCase().contains("multipart/related")'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrettyLoggingFilter.java`
#### Snippet
```java
            && contentType != null 
            && contentType.indexOf("xml") >= 0
            && contentType.toLowerCase().indexOf("multipart/related") < 0
            && event.getPayload() != null
            && event.getPayload().length() > 0;
```

### IndexOfReplaceableByContains
`originalStr.indexOf(ManagementConstants.BUS_ID_PROP) != -1` can be replaced with 'originalStr.contains(ManagementConstants.BUS_ID_PROP)'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/InstrumentationManagerImpl.java`
#### Snippet
```java
              || persistentBusId.startsWith("${"))) {
            String originalStr = original.toString();
            if (originalStr.indexOf(ManagementConstants.BUS_ID_PROP) != -1) {
                String persistedStr =
                    originalStr.replaceFirst(ManagementConstants.BUS_ID_PROP + "=" + bus.getId(),
```

## RuleId[ruleID=StringEqualsCharSequence]
### StringEqualsCharSequence
`String.equals()` called with 'CharSequence' argument
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            if (IdentifierType.GETTER == type
                && !(buf.length() >= pos
                    && "get".equals(buf.subSequence(0, 3)))) {
                return false;
            } else if (IdentifierType.SETTER == type
```

### StringEqualsCharSequence
`String.equals()` called with 'CharSequence' argument
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
                return false;
            } else if (IdentifierType.SETTER == type
                && !(buf.length() >= pos && "set".equals(buf.subSequence(0, 3)))) {
                return false;
            } else {
```

## RuleId[ruleID=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
                (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.AUDIENCE_RESTRICTIONS, msg);
            if (audienceRestrictions != null) {
                for (String audienceRestriction : audienceRestrictions.split(",")) {
                    audiences.add(audienceRestriction);
                }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
                for (String s : ctx.getBeanDefinitionNames()) {
                    names.add(s);
                    for (String s2 : ctx.getAliases(s)) {
                        names.add(s2);
                    }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
    public static CachedContextAndSchemas getCachedContextAndSchemas(Class<?> ... cls) throws JAXBException {
        Set<Class<?>> classes = new HashSet<>();
        for (Class<?> c : cls) {
            classes.add(c);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        // add user extra class into jaxb context
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/cxf/common/util/ClasspathScanner.java`
#### Snippet
```java
        throws IOException, ClassNotFoundException {
        List<Class<? extends Annotation>> annotationList = new ArrayList<>(annotations.length);
        for (Class< ? extends Annotation > c : annotations) {
            annotationList.add(c);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `core/src/main/java/org/apache/cxf/common/util/ClasspathScanner.java`
#### Snippet
```java
        throws IOException, ClassNotFoundException {
        List<Class<? extends Annotation>> annotationList = new ArrayList<>(annotations.length);
        for (Class< ? extends Annotation > c : annotations) {
            annotationList.add(c);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyUtilities.java`
#### Snippet
```java
        ASSERTION_NAMES = new ArrayList<>();
        ASSERTION_NAMES.addAll(RM10AssertionBuilder.KNOWN_ELEMENTS);
        for (QName qn : RM12AssertionBuilder.KNOWN_ELEMENTS) {
            ASSERTION_NAMES.add(qn);
        }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        //add user extra class into jaxb context
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java

        if (null != serverArgs) {
            for (String s : serverArgs) {
                cmd.add(s);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java

        if (wsdlOptions != null) {
            for (WsdlOption wo : wsdlOptions) {
                effectiveWsdlOptions.add(wo);
            }
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java

        if (wsdlOptions != null) {
            for (WsdlOption wo : wsdlOptions) {
                effectiveWsdlOptions.add(wo);
            }
```

## RuleId[ruleID=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
public class CertConstraints {

    public enum Combinator { ANY, ALL };

    private static class DNConstraints {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/cxf/Bus.java`
#### Snippet
```java
public interface Bus extends InterceptorProvider {
    enum BusState {
        INITIAL, INITIALIZING, RUNNING, SHUTTING_DOWN, SHUTDOWN;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/cxf/service/model/MessageInfo.java`
#### Snippet
```java
    public enum Type {
        INPUT,
        OUTPUT;
    }

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        VARIABLE,
        CONSTANT
    };

    private static final char[] XML_NAME_PUNCTUATION_CHARS = new char[] {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/cxf/endpoint/ManagedEndpoint.java`
#### Snippet
```java
    protected final Server server;

    private enum State { CREATED, STARTED, STOPPED };
    private State state = State.CREATED;

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `core/src/main/java/org/apache/cxf/annotations/FactoryType.java`
#### Snippet
```java
        Pooled, //args[0] is the size of the pool
        PerRequest
    };

    final class DEFAULT implements Factory {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            return ASYNC_ONLY;
        }
    };

    volatile PoolingNHttpClientConnectionManager connectionManager;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            return ASYNC_ONLY;
        }
    };
    
    /**
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpoint.java`
#### Snippet
```java
    }

    public enum DeliveryModeType { PERSISTENT, NON_PERSISTENT };

    public enum MessageType {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXRSDataBinding.java`
#### Snippet
```java
        }

    };
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            return null;
        }
    };

    private static final LazyProviderClass DATA_SOURCE_PROVIDER_CLASS =
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/ReceivedToken.java`
#### Snippet
```java
    private Set<Principal> roles;

    public enum STATE { VALID, INVALID, CANCELLED, EXPIRED, NONE };

    public ReceivedToken(Object receivedToken) throws STSException {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLConstants.java`
#### Snippet
```java
        WSDL20,
        UNKNOWN
    };

    private WSDLConstants() {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        }

    };


```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java


    };
}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
        boolean write;
        boolean is;
    };


```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduitFactory.java`
#### Snippet
```java
            return ASYNC_ONLY;
        }
    };

    UseAsyncPolicy policy;
```

## RuleId[ruleID=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
                            .entrySet()
                            .stream()
                            .map(entry -> CastUtils.cast((Map<?, ?>)entry.getValue()))
                            .map(value -> CastUtils.cast((Map<?, ?>)value.get("content")))
                            .filter(Objects::nonNull)
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
                            .entrySet()
                            .stream()
                            .map(entry -> CastUtils.cast((Map<?, ?>)entry.getValue()))
                            .map(value -> CastUtils.cast((Map<?, ?>)value.get("content")))
                            .filter(Objects::nonNull)
```

### SimplifyStreamApiCallChains
'!Stream.anyMatch(...)' can be replaced with 'noneMatch()'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/UserInfoService.java`
#### Snippet
```java
        if (!oauth.getPermissions().stream()
            .map(OAuthPermission::getPermission)
            .anyMatch(OidcUtils.OPENID_SCOPE::equals)) {
            return Response.status(Status.UNAUTHORIZED).build();
        }
```

### SimplifyStreamApiCallChains
'!Stream.anyMatch(...)' can be replaced with 'noneMatch()'
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            return defaultExcludes.stream()
                .filter(ex -> !includes.stream()
                    .anyMatch(inc -> inc.matches(ex)))
                .collect(Collectors.toList());
        }
```

### SimplifyStreamApiCallChains
'Stream.noneMatch(x -\> !(...))' can be replaced with 'allMatch()'
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtTokenSecurityContext.java`
#### Snippet
```java
                }
            } else if (roleClaimValue instanceof List && ((List) roleClaimValue).stream()
                        .noneMatch(o -> !(o instanceof String))) {
                roles = new HashSet<>();
                ((List)roleClaimValue).stream().forEach(val -> {
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtTokenSecurityContext.java`
#### Snippet
```java
                        .noneMatch(o -> !(o instanceof String))) {
                roles = new HashSet<>();
                ((List)roleClaimValue).stream().forEach(val -> {
                    roles.add(new SimpleGroup(val.toString()));
                });
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
            final Set<String> sensitiveHeaderNames) {
        sensitiveHeaderNames.stream()
                .forEach(h -> {
                    headerMap.computeIfPresent(h, (key, value) -> MASKED_HEADER_VALUE);
                });
```

### SimplifyStreamApiCallChains
Can be replaced with 'min()'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
                .map(ProviderInfo::getProvider)
                .sorted(new ProviderFactory.ClassComparator(paramType))
                .findFirst()
                .orElse(null);
    }
```

## RuleId[ruleID=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/tomcat/AbstractTomcatServer.java`
#### Snippet
```java
            server.getHost().setDeployOnStartup(true);

            server.addWebapp(contextPath, getClass().getResource(resourcePath).toURI().getPath().toString());
            server.start();
        } catch (final Exception ex) {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/Beanspector.java`
#### Snippet
```java

        if (result.startsWith("is")) {
            result = result.substring(2, result.length());
        } else {
            result = result.substring(3, result.length());
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/Beanspector.java`
#### Snippet
```java
            result = result.substring(2, result.length());
        } else {
            result = result.substring(3, result.length());
        }
        return result;
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java

    private static String getService(String content) {
        return content.substring(content.indexOf(':') + 1, content.length());
    }

```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/helpers/NSDecl.java`
#### Snippet
```java
    public NSDecl(String pfx, String ur) {
        if (pfx == null) {
            this.prefix = "".intern();
        } else {
            this.prefix = pfx.intern();
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        java.io.PrintStream pout = new java.io.PrintStream(bout);
        e1.printStackTrace(pout);
        String str = new String(bout.toByteArray());
        Pattern pattern = Pattern.compile("(?<=There's\\sno\\sObjectFactory\\swith\\san\\s"
                                          + "@XmlElementDecl\\sfor\\sthe\\selement\\s\\{)\\S*(?=\\})");
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `core/src/main/java/org/apache/cxf/attachment/Rfc5987Util.java`
#### Snippet
```java
        }

        return new String(bos.toByteArray(), encoding);
    }
}
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String XML_NS = "http://www.w3.org/2000/xmlns/";
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String XML_NS = "http://www.w3.org/2000/xmlns/";
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String XML_NS = "http://www.w3.org/2000/xmlns/";
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
    private static final String[] DEF_PREFIXES = new String[] {
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };
```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };

```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };

```

### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        "ns1".intern(), "ns2".intern(), "ns3".intern(),
        "ns4".intern(), "ns5".intern(), "ns6".intern(),
        "ns7".intern(), "ns8".intern(), "ns9".intern()
    };

```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
                builder.append(result);
            } else {
                builder.append(result.substring(0, limit));
            }

```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
            builder.append(swriter.toString());
        } else {
            builder.append(swriter.toString().substring(0, limit));
        }
    }
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSURIParser.java`
#### Snippet
```java

    private String parseToEnd() {
        return uri.substring(pos, uri.length());
    }

```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriInfoImpl.java`
#### Snippet
```java
            List<String> objects = new ArrayList<>();
            List<String> uris = new LinkedList<>();
            StringBuilder sumPath = new StringBuilder("");
            for (MethodInvocationInfo invocation : stack) {
                List<String> templateObjects = invocation.getTemplateValues();
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java
        link = link.trim();
        if (link.length() > 1 && link.startsWith("<")) {
            int index = link.lastIndexOf('>', link.length());
            if (index != -1) {
                String uri = link.substring(1, index);
```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
            ByteArrayOutputStream bos = new ByteArrayOutputStream();
            IOUtils.copy(is, bos, 1024);
            return new String(bos.toByteArray(), encoding);
        } catch (Exception ex) {
            throw ExceptionUtils.toInternalServerErrorException(ex, null);
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
        }
        String localPart = namePattern.substring(idx + 1,
                                                 namePattern.length());
        if (localPart.contains("*")) {
            //wildcard pattern matching
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        implementor = i;
        this.bindingUri = bindingUri;
        wsdlLocation = wsdl == null ? null : new String(wsdl);
        serverFactory = new JaxWsServerFactoryBean();
        if (f != null) {
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    public void setWsdlLocation(String wsdlLocation) {
        if (wsdlLocation != null) {
            this.wsdlLocation = new String(wsdlLocation);
        } else {
            this.wsdlLocation = null;
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/jwt/DefaultJWTClaimsProvider.java`
#### Snippet
```java
                        claimValues = claim.getValues().get(0);
                    }
                    claims.setProperty(translateClaim(claim.getClaimType().toString()), claimValues);
                }
            }
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/CombinedClaimsAttributeStatementProvider.java`
#### Snippet
```java

            String namespace = uri.substring(0, lastSlash);
            String name = uri.substring(lastSlash + 1, uri.length());

            return new AttributeKey(namespace, null, name);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
            List<String> claims = new ArrayList<>();
            for (Claim claim : params.getRequestedPrimaryClaims()) {
                claims.add(claim.getClaimType().toString());
            }
            map.put(KEYS.CLAIMS_PRIMARY.name(), claims.toString());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
            List<String> claims = new ArrayList<>();
            for (Claim claim : params.getRequestedSecondaryClaims()) {
                claims.add(claim.getClaimType().toString());
            }
            map.put(KEYS.CLAIMS_SECONDARY.name(), claims.toString());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticClaimsHandler.java`
#### Snippet
```java
        ProcessedClaimCollection claimsColl = new ProcessedClaimCollection();
        for (Claim claim : claims) {
            if (getGlobalClaims().keySet().contains(claim.getClaimType().toString())) {
                ProcessedClaim c = new ProcessedClaim();
                c.setClaimType(claim.getClaimType());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticClaimsHandler.java`
#### Snippet
```java
                c.setClaimType(claim.getClaimType());
                c.setPrincipal(parameters.getPrincipal());
                c.addValue(getGlobalClaims().get(claim.getClaimType().toString()));
                claimsColl.add(c);
            } else {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsAttributeStatementProvider.java`
#### Snippet
```java

                String namespace = uri.substring(0, lastSlash);
                String name = uri.substring(lastSlash + 1, uri.length());

                attributeBean.setSimpleName(name);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticEndpointClaimsHandler.java`
#### Snippet
```java
        }
        for (Claim claim : claims) {
            if (endpointClaims.keySet().contains(claim.getClaimType().toString())) {
                ProcessedClaim c = new ProcessedClaim();
                c.setClaimType(claim.getClaimType());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticEndpointClaimsHandler.java`
#### Snippet
```java
                c.setClaimType(claim.getClaimType());
                c.setPrincipal(parameters.getPrincipal());
                c.addValue(endpointClaims.get(claim.getClaimType().toString()));
                claimsColl.add(c);
            } else {
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java
        boolean found = false;
        for (Claim claim: claims) {
            if (claim.getClaimType().toString().equals(this.groupURI)) {
                found = true;
                break;
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
            List<String> searchAttributeList = new ArrayList<>();
            for (Claim claim : claims) {
                String claimType = claim.getClaimType().toString();
                if (getClaimsLdapAttributeMapping().keySet().contains(claimType)) {
                    searchAttributeList.add(
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
        }
        for (ProcessedClaim c : processedClaims) {
            if (c.getClaimType() != null && processedClaimType.equals(c.getClaimType().toString())) {
                return c;
            }
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
            for (ProcessedClaim c : processedClaims) {
                String processedClaimType = (c.getClaimType() != null)
                    ? c.getClaimType().toString()
                    : "";
                String mappedProcessedClaimType = map.get(processedClaimType);
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        //string as the documentBaseLocation which would result in it being held onto strongly
        //from the definition.  With this, the String the definition holds onto would be unique
        url = new String(url);
        CatalogWSDLLocator catLocator = new CatalogWSDLLocator(url, bus);
        final ResourceManagerWSDLLocator wsdlLocator = new ResourceManagerWSDLLocator(url,
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
                if (src.getSystemId() != null) {
                    try {
                        doc.setDocumentURI(new String(src.getSystemId()));
                    } catch (Exception e) {
                        //ignore - probably not DOM level 3
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        // create a unique string so if its an interned string (like
        // from an annotation), caches will clear
        this.wsdlURL = new String(wsdlURL);
    }

```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                // create a unique string so if its an interned string (like
                // from an annotation), caches will clear
                wsdlURL = new String(wsdlURL);
            }
        }
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        String initOfMethodInWsdl = methodNameInWsdl.substring(0, 1);
        if (initOfMethodInClass.equalsIgnoreCase(initOfMethodInWsdl)
            && methodNameInClass.substring(1, methodNameInClass.length())
                .equals(methodNameInWsdl.substring(1, methodNameInWsdl.length()))) {
            ret = true;
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        if (initOfMethodInClass.equalsIgnoreCase(initOfMethodInWsdl)
            && methodNameInClass.substring(1, methodNameInClass.length())
                .equals(methodNameInWsdl.substring(1, methodNameInWsdl.length()))) {
            ret = true;
        }
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaOctetSequenceHandler.java`
#### Snippet
```java
        String result;
        if (isBase64Octets) {
            result = new String(DatatypeConverter.printBase64Binary(value));
        } else {
            result = new String(DatatypeConverter.printHexBinary(value));
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaOctetSequenceHandler.java`
#### Snippet
```java
            result = new String(DatatypeConverter.printBase64Binary(value));
        } else {
            result = new String(DatatypeConverter.printHexBinary(value));
        }
        return result;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaBindingHelper.java`
#### Snippet
```java
            String port = corbaAddr.substring(index + 1);

            props.put("yoko.orb.oa.endpoint", new String(protocol + " --host " + host + " --port " + port));
            // WHAT to do for non-yoko orb?
        } else if ("corbaname".equals(scheme)) {
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
    private static List<String> tokenizePathComponents(String path) {
        List<String> tokens = new LinkedList<>();
        String tmpPath = new String(path);

        while (tmpPath.length() > 0) {
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
        int pos = opathtrans.indexOf(opathinfo);
        //REVISIT may cache this value in requstHeaders?
        return new StringBuilder().append(opathtrans.substring(0, pos)).append(path).toString();
    }

```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpClientRequest.java`
#### Snippet
```java
            new DefaultFullHttpRequest(HttpVersion.HTTP_1_1,
                                       HttpMethod.valueOf(method),
                                       uri.getPath().toString(), content);
        // setup the default headers
        request.headers().set("Connection", "keep-alive");
```

## RuleId[ruleID=MethodOverloadsParentMethod]
### MethodOverloadsParentMethod
Method `remove()` overloads a compatible method of a superclass, when overriding might have been intended
in `core/src/main/java/org/apache/cxf/message/MessageContentsList.java`
#### Snippet
```java
     * @param key the key whose associated element is to be marked as removed.
     */
    public void remove(MessagePartInfo key) {
        put(key, REMOVED_MARKER);
    }
```

### MethodOverloadsParentMethod
Method `setTypeClass()` overloads a compatible method of a superclass, when overriding might have been intended
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapRefType.java`
#### Snippet
```java
    }

    public void setTypeClass(Class<?> typeClass) {
        baseType.setTypeClass(typeClass);
    }
```

## RuleId[ruleID=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java

    public String getId() {
        return null == id ? DEFAULT_BUS_ID + Integer.toString(Math.abs(this.hashCode())) : id;
    }

```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
            for (int i = 0; i < params.length; i++) {
                builder.append(" {");
                builder.append(Integer.toString(i));
                builder.append('}');
            }
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            }
        }
        return ATT_UUID + '-' + Integer.toString(COUNTER.incrementAndGet()) + '@'
            + URLEncoder.encode(cid, StandardCharsets.UTF_8.name());
    }
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
        map.put(KEYS.TIME.name(), new Date(event.getTimestamp()));
        map.put(KEYS.OPERATION.name(), event.getOperation());
        map.put(KEYS.DURATION.name(), String.valueOf(event.getDuration()) + "ms");

        if (event instanceof AbstractSTSFailureEvent) {
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
            PrintWriter fw = new PrintWriter(new FileWriter(f));
            for (List<String> args : wargs) {
                fw.println(Integer.toString(args.size()));
                for (String s : args) {
                    fw.println(s);
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
        String absoluteURI = di.getBaseURI();
        if (absoluteURI == null) {
            absoluteURI = Integer.toString(di.hashCode()) + ref.getURI();
        } else {
            absoluteURI = absoluteURI + ref.getURI();
```

### UnnecessaryCallToStringValueOf
Unnecessary `Integer.toString()` call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            PrintWriter fw = new PrintWriter(new FileWriter(f));
            for (List<String> args : wargs) {
                fw.println(Integer.toString(args.size()));
                for (String s : args) {
                    fw.println(s);
```

## RuleId[ruleID=JavaReflectionMemberAccess]
### JavaReflectionMemberAccess
Cannot resolve constructor with specified argument types
in `core/src/main/java/org/apache/cxf/staxutils/validation/W3CMultiSchemaFactory.java`
#### Snippet
```java

    public W3CMultiSchemaFactory() throws NoSuchMethodException {
        w3cSchemaConstructor = W3CSchema.class.getConstructor(XMLSchemaGrammar.class);
    }

```

### JavaReflectionMemberAccess
Cannot resolve field '_delegate'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/ServiceDelegateAccessor.java`
#### Snippet
```java
        } catch (Exception e) {
            try {
                Field delegateField = Service.class.getDeclaredField(DELEGATE_FIELD_NAME2);
                ReflectionUtil.setAccessible(delegateField);
                delegate = (ServiceImpl)delegateField.get(service);
```

### JavaReflectionMemberAccess
Cannot resolve field 'accessMode'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                        } catch (NoSuchFieldException nsfe) {
                            // IBM and OpenJ9 JDKs use a different field name
                            f = MethodHandles.Lookup.class.getDeclaredField("accessMode");
                            m.setAccessible(true);
                        }
```

## RuleId[ruleID=InstantiatingObjectToGetClassObject]
### InstantiatingObjectToGetClassObject
Instantiating object to get Class object
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
        if (refClass != null) {
            try {
                return refClass.getConstructor(QName.class, Type.class, new Annotation[0].getClass()) //NOPMD
                    .newInstance(n, cls, new Annotation[0]);
            } catch (Throwable e) {
```

### InstantiatingObjectToGetClassObject
Instantiating object to get Class object
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AbstractXOPType.java`
#### Snippet
```java
        // the appropriate data type. If there wasn't an attachment, it just returned the bytes.
        // Our subclass have to package them.
        if (thingRead.getClass() == (new byte[0]).getClass()) {
            return wrapBytes((byte[])thingRead, contentType);
        }
```

## RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `toByteArray()` overrides synchronized method
in `core/src/main/java/org/apache/cxf/helpers/LoadingByteArrayOutputStream.java`
#### Snippet
```java
    }

    public byte[] toByteArray() {
        if (count != buf.length) {
            buf = super.toByteArray();
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `setMessageObserver()` overrides synchronized method
in `core/src/main/java/org/apache/cxf/transport/AbstractDestination.java`
#### Snippet
```java
         * @param observer the observer to notify on receipt of incoming
         */
        public void setMessageObserver(MessageObserver observer) {
            // shouldn't be called for a back channel conduit
        }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `close()` overrides synchronized method
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/cache/CXFEHCacheReplayCache.java`
#### Snippet
```java

    @Override
    public void close() {
        super.close();

```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `setMessageObserver()` overrides synchronized method
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/BackChannelConduit.java`
#### Snippet
```java
     * @param observer the observer to notify on receipt of incoming
     */
    public void setMessageObserver(MessageObserver observer) {
        // shouldn't be called for a back channel conduit
    }
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `create()` overrides synchronized method
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/spring/ClientProxyFactoryBeanDefinitionParser.java`
#### Snippet
```java
            }
        }
        public Object create() {
            configured = true;
            return super.create();
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `create()` overrides synchronized method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spring/JaxWsProxyFactoryBeanDefinitionParser.java`
#### Snippet
```java
            }
        }
        public Object create() {
            configured = true;
            return super.create();
```

### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `create()` overrides synchronized method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java

    @Override
    public org.apache.cxf.service.Service create() {
        org.apache.cxf.service.Service s = super.create();

```

## RuleId[ruleID=CollectionContainsUrl]
### CollectionContainsUrl
Map `urlMap` may contain URL objects
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletContextResourceResolver.java`
#### Snippet
```java
public class ServletContextResourceResolver implements ResourceResolver {
    ServletContext servletContext;
    Map<String, URL> urlMap = new ConcurrentHashMap<>();

    public ServletContextResourceResolver(ServletContext sc) {
```

## RuleId[ruleID=ThreadRun]
### ThreadRun
Calls to `run()` should probably be replaced with 'start()'
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
                try {
                    Runtime.getRuntime().removeShutdownHook(shutdownHook);
                    shutdownHook.run();
                } catch (IllegalStateException ex) {
                    // The JVM is already shutting down so do nothing
```

## RuleId[ruleID=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalDestination.java`
#### Snippet
```java
            }

            protected void dispatchToClient(boolean empty) throws IOException {
                final MessageImpl m = new MessageImpl();
                localDestinationFactory.copy(message, m);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalConduit.java`
#### Snippet
```java
            dispatchToService(false);
        }
        protected void dispatchToService(boolean empty) throws IOException {
            final MessageImpl inMsg = new MessageImpl();
            transportFactory.copy(message, inMsg);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/cxf/common/logging/LogUtils.java`
#### Snippet
```java
     * Create a logger
     */
    protected static Logger createLogger(Class<?> cls,
                                         String name,
                                         String loggerName) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
     * @see org.apache.cxf.helpers.JavaUtils
     */
    protected static boolean isJavaKeyword(String word) {
        return JavaUtils.isJavaKeyword(word);
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
    }

    protected static void convertToSetOfQNames(List<String> set,
                                               Set<QName> elementsSet) {
        if (set != null) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
    }

    protected static void convertToQNamesMap(Map<String, String> map,
                                             QNamesMap elementsMap,
                                             Map<String, String> nsMap) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
    }

    protected static byte[] loadFile(String fileName) throws IOException {
        if (fileName == null) {
            return null;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    }
    
    protected void autoCloseWithHint(Class<?> cls, boolean autoCloseHint, boolean exception) {
        if (!entityBufferred && !JAXRSUtils.isStreamingOutType(cls)
            && (exception || MessageUtils.getContextualBoolean(outMessage, 
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    }

    protected void autoClose(Class<?> cls, boolean exception) {
        autoCloseWithHint(cls, false, exception);
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
    }

    protected static boolean isPrematching(Class<?> filterCls) {
        return AnnotationUtils.getClassAnnotation(filterCls, PreMatching.class) != null;
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
    }

    protected void injectApplicationIntoFeature(Feature feature) {
        if (application != null) {
            AbstractResourceInfo info = new AbstractResourceInfo(feature.getClass(),
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        if (handler != null) {
            handler.handleResponse(new Response<T>() {
                protected boolean cancelled;
                
                public Map<String, Object> getContext() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsClientCallback.java`
#### Snippet
```java
        if (handler != null) {
            handler.handleResponse(new Response<T>() {
                protected boolean cancelled;

                public Map<String, Object> getContext() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
     * Prevents instantiation.
     */
    protected RMContextUtils() {
    }

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java
    }

    protected EprMetaData getObjectReferenceBinding(Definition wsdlDef, QName bindingName) {
        EprMetaData info = new EprMetaData();
        Binding wsdlBinding = wsdlDef.getBinding(bindingName);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java


    protected static WadlOption generateWadlOptionFromFile(final File wadl,
                                                           final Option defaultOptions,
                                                           File defaultOutputDir)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/LogMessageFormatter.java`
#### Snippet
```java
        return b.toString();
    }
    protected static String localPart(QName name) {
        return name == null ? null : name.getLocalPart();
    }
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/LogMessageFormatter.java`
#### Snippet
```java
    }

    protected static void write(StringBuilder b, String key, String value) {
        if (value != null) {
            b.append("    ").append(key).append(": ").append(value).append('\n');
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java
    }

    protected static WsdlOption generateWsdlOptionFromFile(final File wsdl,
                                                           File defaultOutputDir)
        throws MojoExecutionException {
```

## RuleId[ruleID=RedundantEscapeInRegexReplacement]
### RedundantEscapeInRegexReplacement
Redundant escape of '''
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java

    public static String protectSingleQuotes(String value) {
        return value.replaceAll("'", "\\'");
    }

```

### RedundantEscapeInRegexReplacement
Redundant escape of '.'
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
                        if (null != className) {
                            final String name = n.charAt(0) != '*' ? n
                                    : "." + n.replaceAll("\\.", "\\."); //old wildcard
                            try {
                                Matcher matcher = Pattern.compile(name).matcher("");
```

## RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java

    private synchronized WSDiscoveryServiceImpl getService() {
        if (service == null) {
            service = bus.getExtension(WSDiscoveryServiceImpl.class);
            if (service == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java
    private synchronized WSDiscoveryServiceImpl getService() {
        if (service == null) {
            service = bus.getExtension(WSDiscoveryServiceImpl.class);
            if (service == null) {
                service = getStaticService();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java
        if (service == null) {
            service = bus.getExtension(WSDiscoveryServiceImpl.class);
            if (service == null) {
                service = getStaticService();
                bus.setExtension(service, WSDiscoveryServiceImpl.class);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java
            service = bus.getExtension(WSDiscoveryServiceImpl.class);
            if (service == null) {
                service = getStaticService();
                bus.setExtension(service, WSDiscoveryServiceImpl.class);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java
            if (service == null) {
                service = getStaticService();
                bus.setExtension(service, WSDiscoveryServiceImpl.class);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/listeners/WSDiscoveryServerListener.java`
#### Snippet
```java
            }
        }
        return service;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    }
    private synchronized void resetDispatch(String newad) {
        address = newad;
        dispatch = null;
        service = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    private synchronized void resetDispatch(String newad) {
        address = newad;
        dispatch = null;
        service = null;
        adHoc = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        address = newad;
        dispatch = null;
        service = null;
        adHoc = false;
        try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `adHoc` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        dispatch = null;
        service = null;
        adHoc = false;
        try {
            URI uri = new URI(address);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        adHoc = false;
        try {
            URI uri = new URI(address);
            if (StringUtils.isEmpty(uri.getHost())) {
                adHoc = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `adHoc` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            URI uri = new URI(address);
            if (StringUtils.isEmpty(uri.getHost())) {
                adHoc = true;
            } else {
                InetSocketAddress isa = new InetSocketAddress(uri.getHost(), uri.getPort());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `adHoc` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                InetSocketAddress isa = new InetSocketAddress(uri.getHost(), uri.getPort());
                if (isa.getAddress().isMulticastAddress()) {
                    adHoc = true;
                }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jaxbContext` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java

    private synchronized JAXBContext getJAXBContext() {
        if (jaxbContext == null) {
            try {
                jaxbContext = JAXBContextCache.getCachedContextAndSchemas(ObjectFactory.class).getContext();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jaxbContext` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        if (jaxbContext == null) {
            try {
                jaxbContext = JAXBContextCache.getCachedContextAndSchemas(ObjectFactory.class).getContext();
            } catch (JAXBException e) {
                throw new RuntimeException(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jaxbContext` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            }
        }
        return jaxbContext;
    }
    private synchronized ServiceImpl getService() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java

    public synchronized void close() throws IOException {
        if (dispatch != null) {
            ((Closeable)dispatch).close();
            dispatch = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    public synchronized void close() throws IOException {
        if (dispatch != null) {
            ((Closeable)dispatch).close();
            dispatch = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        if (dispatch != null) {
            ((Closeable)dispatch).close();
            dispatch = null;
        }
        service = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            dispatch = null;
        }
        service = null;
    }
    @SuppressWarnings("deprecation")
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
    }
    private synchronized ServiceImpl getService() {
        if (service == null) {
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
                service = new ServiceImpl(bus, null,
                                          version.getServiceName(),
                                          Service.class);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `version` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            try {
                service = new ServiceImpl(bus, null,
                                          version.getServiceName(),
                                          Service.class);
                service.addPort(version.getServiceName(),
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                                          version.getServiceName(),
                                          Service.class);
                service.addPort(version.getServiceName(),
                                soapVersion, address);
            } finally {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `version` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                                          version.getServiceName(),
                                          Service.class);
                service.addPort(version.getServiceName(),
                                soapVersion, address);
            } finally {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `soapVersion` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                                          Service.class);
                service.addPort(version.getServiceName(),
                                soapVersion, address);
            } finally {
                BusFactory.setThreadDefaultBus(b);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `address` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                                          Service.class);
                service.addPort(version.getServiceName(),
                                soapVersion, address);
            } finally {
                BusFactory.setThreadDefaultBus(b);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `service` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            }
        }
        return service;
    }
    private synchronized void resetDispatch(String newad) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java

    private synchronized Dispatch<Object> getDispatchInternal(boolean addSeq, String action) {
        if (dispatch == null) {
            AddressingFeature f = new AddressingFeature(true, true);
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        if (dispatch == null) {
            AddressingFeature f = new AddressingFeature(true, true);
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
            dispatch.getRequestContext().put("thread.local.request.context", Boolean.TRUE);
            version.addVersionTransformer(dispatch);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `version` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        if (dispatch == null) {
            AddressingFeature f = new AddressingFeature(true, true);
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
            dispatch.getRequestContext().put("thread.local.request.context", Boolean.TRUE);
            version.addVersionTransformer(dispatch);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            AddressingFeature f = new AddressingFeature(true, true);
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
            dispatch.getRequestContext().put("thread.local.request.context", Boolean.TRUE);
            version.addVersionTransformer(dispatch);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `version` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
            dispatch.getRequestContext().put("thread.local.request.context", Boolean.TRUE);
            version.addVersionTransformer(dispatch);
        }
        addAddressing(dispatch, addSeq, action);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            dispatch = getService().createDispatch(version.getServiceName(), getJAXBContext(), Service.Mode.PAYLOAD, f);
            dispatch.getRequestContext().put("thread.local.request.context", Boolean.TRUE);
            version.addVersionTransformer(dispatch);
        }
        addAddressing(dispatch, addSeq, action);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            version.addVersionTransformer(dispatch);
        }
        addAddressing(dispatch, addSeq, action);
        return dispatch;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `dispatch` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
        }
        addAddressing(dispatch, addSeq, action);
        return dispatch;
    }
    private void addAddressing(BindingProvider p, boolean addSeq, String action) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        String preferIPv4StackValue = System.getProperty("java.net.preferIPv4Stack");
        String preferIPv6AddressesValue = System.getProperty("java.net.preferIPv6Addresses");
        if (!started && client.isAdHoc()) {
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        String preferIPv4StackValue = System.getProperty("java.net.preferIPv4Stack");
        String preferIPv6AddressesValue = System.getProperty("java.net.preferIPv6Addresses");
        if (!started && client.isAdHoc()) {
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bus` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        String preferIPv6AddressesValue = System.getProperty("java.net.preferIPv6Addresses");
        if (!started && client.isAdHoc()) {
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
                udpEndpoint = new EndpointImpl(bus, new WSDiscoveryProvider());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
                udpEndpoint = new EndpointImpl(bus, new WSDiscoveryProvider());
                Map<String, Object> props = new HashMap<>();
                props.put("jaxws.provider.interpretNullAsOneway", "true");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bus` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            Bus b = BusFactory.getAndSetThreadDefaultBus(bus);
            try {
                udpEndpoint = new EndpointImpl(bus, new WSDiscoveryProvider());
                Map<String, Object> props = new HashMap<>();
                props.put("jaxws.provider.interpretNullAsOneway", "true");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                Map<String, Object> props = new HashMap<>();
                props.put("jaxws.provider.interpretNullAsOneway", "true");
                udpEndpoint.setProperties(props);
                if ("true".equals(preferIPv6AddressesValue) && "false".equals(preferIPv4StackValue)) {
                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                if ("true".equals(preferIPv6AddressesValue) && "false".equals(preferIPv4StackValue)) {
                    try {
                        udpEndpoint.publish("soap.udp://[FF02::C]:3702");
                        started = true;
                    } catch (Exception e) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                    try {
                        udpEndpoint.publish("soap.udp://[FF02::C]:3702");
                        started = true;
                    } catch (Exception e) {
                        LOG.log(Level.WARNING, "Could not start WS-Discovery Service with ipv6 address", e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                    }
                }
                if (!started) {
                    udpEndpoint.publish("soap.udp://239.255.255.250:3702");
                    started = true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                }
                if (!started) {
                    udpEndpoint.publish("soap.udp://239.255.255.250:3702");
                    started = true;
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                if (!started) {
                    udpEndpoint.publish("soap.udp://239.255.255.250:3702");
                    started = true;
                }
            } catch (RuntimeException ex) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bus` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                LOG.log(Level.WARNING, "Could not start WS-Discovery Service.", ex);
            } finally {
                if (b != bus) {
                    BusFactory.setThreadDefaultBus(b);
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        String address = (String)props.get("org.apache.cxf.service.ws-discovery.address");
        if (address != null) {
            client.setAddress(address);
        }
        if (udpEndpoint != null && !client.isAdHoc()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            client.setAddress(address);
        }
        if (udpEndpoint != null && !client.isAdHoc()) {
            udpEndpoint.stop();
            udpEndpoint = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            client.setAddress(address);
        }
        if (udpEndpoint != null && !client.isAdHoc()) {
            udpEndpoint.stop();
            udpEndpoint = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        }
        if (udpEndpoint != null && !client.isAdHoc()) {
            udpEndpoint.stop();
            udpEndpoint = null;
            started = false;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `udpEndpoint` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
        if (udpEndpoint != null && !client.isAdHoc()) {
            udpEndpoint.stop();
            udpEndpoint = null;
            started = false;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `started` accessed in synchronized context
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            udpEndpoint.stop();
            udpEndpoint = null;
            started = false;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java

    public static synchronized void addAuthenticator() {
        if (instance == null) {
            instance = new CXFAuthenticator();
            Authenticator wrapped = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java
    public static synchronized void addAuthenticator() {
        if (instance == null) {
            instance = new CXFAuthenticator();
            Authenticator wrapped = null;
            if (JavaUtils.isJava9Compatible()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `instance` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java
                }
                final Authenticator auth = (Authenticator)cls.getConstructor(Authenticator.class, Authenticator.class)
                    .newInstance(instance, wrapped);

                if (System.getSecurityManager() == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
        r.lock();
        try {
            synchronized (registry) {
                AbstractHTTPDestination d = registry.getDestinationForPath(endpointInfo.getAddress());
                if (d == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
        try {
            synchronized (registry) {
                AbstractHTTPDestination d = registry.getDestinationForPath(endpointInfo.getAddress());
                if (d == null) {
                    HttpDestinationFactory jettyFactory = bus.getExtension(HttpDestinationFactory.class);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
                    }

                    d = factory.createDestination(endpointInfo, bus, registry);
                    registry.addDestination(d);
                    configure(bus, d);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java

                    d = factory.createDestination(endpointInfo, bus, registry);
                    registry.addDestination(d);
                    configure(bus, d);
                    d.finalizeConfig();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `algorithm` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
        synchronized String generateAuth(String uri, String username, String password) {
            try {
                String digAlg = algorithm;
                if ("MD5-sess".equalsIgnoreCase(digAlg)) {
                    digAlg = "MD5";
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `realm` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                final MessageDigest digester = MessageDigest.getInstance(digAlg);
                String cnonce = createCnonce();
                String a1 = username + ':' + realm + ':' + password;
                if ("MD5-sess".equalsIgnoreCase(algorithm)) {
                    String tmp2 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `algorithm` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                String cnonce = createCnonce();
                String a1 = username + ':' + realm + ':' + password;
                if ("MD5-sess".equalsIgnoreCase(algorithm)) {
                    String tmp2 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
                    a1 = tmp2 + ':' + nonce + ':' + cnonce;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `charset` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                String a1 = username + ':' + realm + ':' + password;
                if ("MD5-sess".equalsIgnoreCase(algorithm)) {
                    String tmp2 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
                    a1 = tmp2 + ':' + nonce + ':' + cnonce;
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nonce` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                if ("MD5-sess".equalsIgnoreCase(algorithm)) {
                    String tmp2 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
                    a1 = tmp2 + ':' + nonce + ':' + cnonce;
                }
                String hasha1 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `charset` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                    a1 = tmp2 + ':' + nonce + ':' + cnonce;
                }
                String hasha1 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
                String a2 = method + ':' + uri;
                String hasha2 = StringUtils.toHexString(digester.digest(a2.getBytes(US_ASCII)));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `method` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                }
                String hasha1 = StringUtils.toHexString(digester.digest(a1.getBytes(charset)));
                String a2 = method + ':' + uri;
                String hasha2 = StringUtils.toHexString(digester.digest(a2.getBytes(US_ASCII)));
                final String serverDigestValue;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `qop` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                final String serverDigestValue;
                final String ncstring;
                if (qop == null) {
                    ncstring = null;
                    serverDigestValue = hasha1 + ':' + nonce + ':' + hasha2;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nonce` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                if (qop == null) {
                    ncstring = null;
                    serverDigestValue = hasha1 + ':' + nonce + ':' + hasha2;
                } else {
                    ncstring = StringUtils.toHexString(ByteBuffer.allocate(4).putInt(++nc).array());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nc` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                    serverDigestValue = hasha1 + ':' + nonce + ':' + hasha2;
                } else {
                    ncstring = StringUtils.toHexString(ByteBuffer.allocate(4).putInt(++nc).array());
                    serverDigestValue = hasha1 + ':' + nonce + ':' + ncstring + ':' + cnonce + ':'
                        + qop + ':' + hasha2;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nonce` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                } else {
                    ncstring = StringUtils.toHexString(ByteBuffer.allocate(4).putInt(++nc).array());
                    serverDigestValue = hasha1 + ':' + nonce + ':' + ncstring + ':' + cnonce + ':'
                        + qop + ':' + hasha2;
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `qop` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                    ncstring = StringUtils.toHexString(ByteBuffer.allocate(4).putInt(++nc).array());
                    serverDigestValue = hasha1 + ':' + nonce + ':' + ncstring + ':' + cnonce + ':'
                        + qop + ':' + hasha2;
                }
                String response = StringUtils.toHexString(digester.digest(serverDigestValue.getBytes(US_ASCII)));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `qop` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                String response = StringUtils.toHexString(digester.digest(serverDigestValue.getBytes(US_ASCII)));
                Map<String, String> outParams = new HashMap<>();
                if (qop != null) {
                    outParams.put("qop", "auth");
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `realm` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                    outParams.put("qop", "auth");
                }
                outParams.put("realm", realm);
                outParams.put("opaque", opaque);
                outParams.put("nonce", nonce);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `opaque` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                }
                outParams.put("realm", realm);
                outParams.put("opaque", opaque);
                outParams.put("nonce", nonce);
                outParams.put("uri", uri);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `nonce` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                outParams.put("realm", realm);
                outParams.put("opaque", opaque);
                outParams.put("nonce", nonce);
                outParams.put("uri", uri);
                outParams.put("username", username);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `algorithm` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                outParams.put("cnonce", cnonce);
                outParams.put("response", response);
                outParams.put("algorithm", algorithm);
                return new HttpAuthHeader(HttpAuthHeader.AUTH_TYPE_DIGEST, outParams).getFullHeader();
            } catch (Exception ex) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    protected void deactivate() {
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverPolicy` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java

    private synchronized HTTPServerPolicy calcServerPolicyInternal(Message m) {
        HTTPServerPolicy sp = serverPolicy;
        if (!serverPolicyCalced) {
            PolicyDataEngine pde = bus.getExtension(PolicyDataEngine.class);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `serverPolicy` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
                        new HTTPServerPolicy(), HTTPServerPolicy.class);
            }
            serverPolicy = sp;
            serverPolicyCalced = true;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    protected void activate() {
        synchronized (this) {
            if (registry != null) {
                registry.addDestination(this);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
        synchronized (this) {
            if (registry != null) {
                registry.addDestination(this);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    public void shutdown() {
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
        synchronized (this) {
            if (registry != null) {
                registry.removeDestination(path);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultAddress` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
        if (defaultAddress == null) {
            synchronized (this) {
                if (defaultAddress == null) {
                    if (fromEndpointReferenceType && getTarget().getAddress().getValue() != null) {
                        defaultAddress = new Address(this.getTarget().getAddress().getValue());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `fromEndpointReferenceType` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
            synchronized (this) {
                if (defaultAddress == null) {
                    if (fromEndpointReferenceType && getTarget().getAddress().getValue() != null) {
                        defaultAddress = new Address(this.getTarget().getAddress().getValue());
                    } else if (endpointInfo.getAddress() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultAddress` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                if (defaultAddress == null) {
                    if (fromEndpointReferenceType && getTarget().getAddress().getValue() != null) {
                        defaultAddress = new Address(this.getTarget().getAddress().getValue());
                    } else if (endpointInfo.getAddress() != null) {
                        defaultAddress = new Address(endpointInfo.getAddress());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultAddress` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                        defaultAddress = new Address(this.getTarget().getAddress().getValue());
                    } else if (endpointInfo.getAddress() != null) {
                        defaultAddress = new Address(endpointInfo.getAddress());
                    }
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultAddress` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
        if (defaultAddress == null) {
            synchronized (this) {
                if (defaultAddress == null) {
                    if (curAddr != null) {
                        defaultAddress = new Address(curAddr);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultAddress` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                if (defaultAddress == null) {
                    if (curAddr != null) {
                        defaultAddress = new Address(curAddr);
                    } else {
                        throw new URISyntaxException("<null>",
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastTlsHash` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java

        int hash = tlsClientParameters.hashCode();
        if (hash != lastTlsHash) {
            lastTlsHash = hash;
            socketFactory = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastTlsHash` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
        int hash = tlsClientParameters.hashCode();
        if (hash != lastTlsHash) {
            lastTlsHash = hash;
            socketFactory = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
        if (hash != lastTlsHash) {
            lastTlsHash = hash;
            socketFactory = null;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
        // tlsClientParameters.sslSocketFactory to allow runtime configuration change
        if (tlsClientParameters.isUseHttpsURLConnectionDefaultSslSocketFactory()) {
            socketFactory = HttpsURLConnection.getDefaultSSLSocketFactory();

        } else if (tlsClientParameters.getSSLSocketFactory() != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
            // with not-yet-commons-ssl.jar, or even just people who like doing their
            // own JSSE.
            socketFactory = tlsClientParameters.getSSLSocketFactory();

        } else if (socketFactory == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
            socketFactory = tlsClientParameters.getSSLSocketFactory();

        } else if (socketFactory == null) {

            final SSLContext ctx;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
            String protocol = tlsClientParameters.getSecureSocketProtocol() != null ? tlsClientParameters
                .getSecureSocketProtocol() : ctx.getProtocol();
            socketFactory = new SSLSocketFactoryWrapper(ctx.getSocketFactory(), cipherSuites,
                                                        protocol);
            //recalc the hashcode since some of the above MAY have changed the tlsClientParameters
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastTlsHash` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
                                                        protocol);
            //recalc the hashcode since some of the above MAY have changed the tlsClientParameters
            lastTlsHash = tlsClientParameters.hashCode();
        } else {
           // ssl socket factory already initialized, reuse it to benefit of keep alive
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
                Method setSSLSocketFactory = connection.getClass()
                    .getMethod("setSSLSocketFactory", getSSLSocketFactory.getReturnType());
                if (getSSLSocketFactory.getReturnType().isInstance(socketFactory)) {
                    setSSLSocketFactory.invoke(connection, socketFactory);
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
                    .getMethod("setSSLSocketFactory", getSSLSocketFactory.getReturnType());
                if (getSSLSocketFactory.getReturnType().isInstance(socketFactory)) {
                    setSSLSocketFactory.invoke(connection, socketFactory);
                } else {
                    //need to see if we can create one - mostly the weblogic case.   The
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `socketFactory` accessed in synchronized context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java
                        .getDeclaredConstructor(SSLSocketFactory.class);
                    ReflectionUtil.setAccessible(c);
                    setSSLSocketFactory.invoke(connection, c.newInstance(socketFactory));
                }
            } catch (Exception ex) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/CountingInputStream.java`
#### Snippet
```java

    public synchronized void reset() throws IOException {
        if (!in.markSupported()) {
            throw new IOException("Mark not supported");
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/CountingInputStream.java`
#### Snippet
```java
        }

        in.reset();
        count = mark;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `in` accessed in synchronized context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/CountingInputStream.java`
#### Snippet
```java

    public synchronized void mark(int readlimit) {
        in.mark(readlimit);
        mark = count;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `failoverStrategy` accessed in synchronized context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
     */
    public synchronized FailoverStrategy getStrategy()  {
        if (failoverStrategy == null) {
            failoverStrategy = new SequentialStrategy();
            getLogger().log(Level.INFO,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `failoverStrategy` accessed in synchronized context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
    public synchronized FailoverStrategy getStrategy()  {
        if (failoverStrategy == null) {
            failoverStrategy = new SequentialStrategy();
            getLogger().log(Level.INFO,
                            "USING_STRATEGY",
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `failoverStrategy` accessed in synchronized context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
            getLogger().log(Level.INFO,
                            "USING_STRATEGY",
                            new Object[] {failoverStrategy});
        }
        return failoverStrategy;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `failoverStrategy` accessed in synchronized context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
                            new Object[] {failoverStrategy});
        }
        return failoverStrategy;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `failoverStrategy` accessed in synchronized context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
        if (strategy != null) {
            getLogger().log(Level.INFO, "USING_STRATEGY", new Object[] {strategy});
            failoverStrategy = strategy;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
     */
    public static synchronized void setDefaultBus(Bus bus) {
        defaultBus = bus;
        BusHolder b = getThreadBusHolder(false);
        b.bus = bus;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
     */
    public static synchronized Bus getDefaultBus(boolean createIfNeeded) {
        if (defaultBus == null && createIfNeeded) {
            defaultBus = newInstance().createBus();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
    public static synchronized Bus getDefaultBus(boolean createIfNeeded) {
        if (defaultBus == null && createIfNeeded) {
            defaultBus = newInstance().createBus();
        }
        if (defaultBus == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
            defaultBus = newInstance().createBus();
        }
        if (defaultBus == null) {
            // never set up.
            return null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
            return null;
        }
        return defaultBus;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
            b.bus = bus;
        }
        if (defaultBus == null) {
            defaultBus = bus;
            return true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `defaultBus` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
        }
        if (defaultBus == null) {
            defaultBus = bus;
            return true;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `inShutdown` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java

    public synchronized void shutdown(boolean processRemainingTasks) {
        inShutdown = true;
        for (AutomaticWorkQueue q : namedQueues.values()) {
            if (q instanceof AutomaticWorkQueueImpl) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `namedQueues` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
    public synchronized void shutdown(boolean processRemainingTasks) {
        inShutdown = true;
        for (AutomaticWorkQueue q : namedQueues.values()) {
            if (q instanceof AutomaticWorkQueueImpl) {
                AutomaticWorkQueueImpl impl = (AutomaticWorkQueueImpl)q;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java

                        if (impl.getShareCount() == 0
                            && imanager != null
                            && imanager.getMBeanServer() != null) {
                            try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                        if (impl.getShareCount() == 0
                            && imanager != null
                            && imanager.getMBeanServer() != null) {
                            try {
                                imanager.unregister(new WorkQueueImplMBeanWrapper(impl, this));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                            && imanager.getMBeanServer() != null) {
                            try {
                                imanager.unregister(new WorkQueueImplMBeanWrapper(impl, this));
                            } catch (JMException jmex) {
                                LOG.log(Level.WARNING, jmex.getMessage(), jmex);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `inShutdown` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
    public void run() {
        synchronized (this) {
            while (!inShutdown) {
                try {
                    wait();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `namedQueues` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                }
            }
            for (AutomaticWorkQueue q : namedQueues.values()) {
                while (!q.isShutdown()) {
                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                synchronized (impl) {
                    if (impl.getShareCount() == 0
                        && imanager != null
                        && imanager.getMBeanServer() != null) {
                        try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                    if (impl.getShareCount() == 0
                        && imanager != null
                        && imanager.getMBeanServer() != null) {
                        try {
                            imanager.register(new WorkQueueImplMBeanWrapper((AutomaticWorkQueueImpl)q, this));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `imanager` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                        && imanager.getMBeanServer() != null) {
                        try {
                            imanager.register(new WorkQueueImplMBeanWrapper((AutomaticWorkQueueImpl)q, this));
                        } catch (JMException jmex) {
                            LOG.log(Level.WARNING, jmex.getMessage(), jmex);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clazz` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        }
        synchronized (this) {
            if (clazz == null) {
                clazz = tryClass(className, cl);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `clazz` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        synchronized (this) {
            if (clazz == null) {
                clazz = tryClass(className, cl);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `className` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        synchronized (this) {
            if (clazz == null) {
                clazz = tryClass(className, cl);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `intf` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        }
        synchronized (this) {
            if (intf == null) {
                intf = tryClass(interfaceName, cl);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `intf` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        synchronized (this) {
            if (intf == null) {
                intf = tryClass(interfaceName, cl);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `interfaceName` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        synchronized (this) {
            if (intf == null) {
                intf = tryClass(interfaceName, cl);
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `faults` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
     */
    public synchronized void addFault(FaultInfo fault) {
        if (faults == null) {
            faults = new ConcurrentHashMap<>(4, 0.75f, 2);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `faults` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
    public synchronized void addFault(FaultInfo fault) {
        if (faults == null) {
            faults = new ConcurrentHashMap<>(4, 0.75f, 2);
        }
        faults.put(fault.getFaultName(), fault);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `faults` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
            faults = new ConcurrentHashMap<>(4, 0.75f, 2);
        }
        faults.put(fault.getFaultName(), fault);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `createIfNotExist` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/SessionFactory.java`
#### Snippet
```java
            o = e.getSession().get(serv.getName().toString());
            if (o == null) {
                if (createIfNotExist) {
                    o = factory.create(e);
                    e.getSession().put(serv.getName().toString(), o);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `factory` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/SessionFactory.java`
#### Snippet
```java
            if (o == null) {
                if (createIfNotExist) {
                    o = factory.create(e);
                    e.getSession().put(serv.getName().toString(), o);
                } else {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bean` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/SingletonFactory.java`
#### Snippet
```java

    private synchronized void createBean(Exchange e) throws Throwable {
        if (bean == null) {
            bean = factory.create(e);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bean` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/SingletonFactory.java`
#### Snippet
```java
    private synchronized void createBean(Exchange e) throws Throwable {
        if (bean == null) {
            bean = factory.create(e);
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `factory` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/SingletonFactory.java`
#### Snippet
```java
    private synchronized void createBean(Exchange e) throws Throwable {
        if (bean == null) {
            bean = factory.create(e);
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `factory` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
    protected synchronized Object createObject(Exchange e) throws Throwable {
        //recheck the count/max stuff now that we're in a sync block
        if (factory == null
            || ((count >= max) && !createMore)) {
            return pool.take();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `count` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        //recheck the count/max stuff now that we're in a sync block
        if (factory == null
            || ((count >= max) && !createMore)) {
            return pool.take();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `max` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        //recheck the count/max stuff now that we're in a sync block
        if (factory == null
            || ((count >= max) && !createMore)) {
            return pool.take();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `createMore` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        //recheck the count/max stuff now that we're in a sync block
        if (factory == null
            || ((count >= max) && !createMore)) {
            return pool.take();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `pool` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        if (factory == null
            || ((count >= max) && !createMore)) {
            return pool.take();
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `count` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        }

        count++;
        return factory.create(e);
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `factory` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java

        count++;
        return factory.create(e);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
        throws IOException {
        Conduit c;
        synchronized (endpoint) {
            if (!conduits.isEmpty()) {
                c = findCompatibleConduit(message);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `conduits` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
        Conduit c;
        synchronized (endpoint) {
            if (!conduits.isEmpty()) {
                c = findCompatibleConduit(message);
                if (c != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
                }
            }
            EndpointInfo ei = endpoint.getEndpointInfo();
            String add = (String)message.get(Message.ENDPOINT_ADDRESS);
            String basePath = (String)message.get(Message.BASE_PATH);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `conduits` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
                getLogger().warning("MessageObserver not found");
            }
            conduits.add(c);
        }
        return c;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
     */
    public synchronized void setMessageObserver(MessageObserver observer) {
        if (observer != incomingObserver) {
            MessageObserver old = incomingObserver;
            // the observer switch must take place before activation or after deactivation
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
    public synchronized void setMessageObserver(MessageObserver observer) {
        if (observer != incomingObserver) {
            MessageObserver old = incomingObserver;
            // the observer switch must take place before activation or after deactivation
            if (observer != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
            // the observer switch must take place before activation or after deactivation
            if (observer != null) {
                incomingObserver = observer;
                final Logger logger = getLogger();
                if (logger.isLoggable(Level.FINE)) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
                        activate();
                    } catch (RuntimeException ex) {
                        incomingObserver = null;
                        throw ex;
                    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
                    deactivate();
                }
                incomingObserver = observer;
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `delayQueue` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    public synchronized void schedule(final Runnable work, final long delay) {
        if (delayQueue == null) {
            delayQueue = new DelayQueue<>();
            watchDog = new WatchDog(delayQueue);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `delayQueue` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    public synchronized void schedule(final Runnable work, final long delay) {
        if (delayQueue == null) {
            delayQueue = new DelayQueue<>();
            watchDog = new WatchDog(delayQueue);
            watchDog.setDaemon(true);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `watchDog` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
        if (delayQueue == null) {
            delayQueue = new DelayQueue<>();
            watchDog = new WatchDog(delayQueue);
            watchDog.setDaemon(true);
            watchDog.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `delayQueue` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
        if (delayQueue == null) {
            delayQueue = new DelayQueue<>();
            watchDog = new WatchDog(delayQueue);
            watchDog.setDaemon(true);
            watchDog.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `watchDog` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            delayQueue = new DelayQueue<>();
            watchDog = new WatchDog(delayQueue);
            watchDog.setDaemon(true);
            watchDog.start();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `watchDog` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            watchDog = new WatchDog(delayQueue);
            watchDog.setDaemon(true);
            watchDog.start();
        }
        delayQueue.put(new DelayedTaskWrapper(work, delay));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `delayQueue` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            watchDog.start();
        }
        delayQueue.put(new DelayedTaskWrapper(work, delay));
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    protected synchronized ThreadPoolExecutor getExecutor() {
        if (executor == null) {
            threadFactory = createThreadFactory(name);
            executor = new ThreadPoolExecutor(lowWaterMark,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `threadFactory` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    protected synchronized ThreadPoolExecutor getExecutor() {
        if (executor == null) {
            threadFactory = createThreadFactory(name);
            executor = new ThreadPoolExecutor(lowWaterMark,
                                              highWaterMark,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `name` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    protected synchronized ThreadPoolExecutor getExecutor() {
        if (executor == null) {
            threadFactory = createThreadFactory(name);
            executor = new ThreadPoolExecutor(lowWaterMark,
                                              highWaterMark,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
        if (executor == null) {
            threadFactory = createThreadFactory(name);
            executor = new ThreadPoolExecutor(lowWaterMark,
                                              highWaterMark,
                                              TimeUnit.MILLISECONDS.toMillis(dequeueTimeout),
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `maxQueueSize` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            if (LOG.isLoggable(Level.FINE)) {
                StringBuilder buf = new StringBuilder(128).append("Constructing automatic work queue with:\n")
                        .append("max queue size: ").append(maxQueueSize).append('\n')
                        .append("initialThreads: ").append(initialThreads).append('\n')
                        .append("lowWaterMark: ").append(lowWaterMark).append('\n')
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                StringBuilder buf = new StringBuilder(128).append("Constructing automatic work queue with:\n")
                        .append("max queue size: ").append(maxQueueSize).append('\n')
                        .append("initialThreads: ").append(initialThreads).append('\n')
                        .append("lowWaterMark: ").append(lowWaterMark).append('\n')
                        .append("highWaterMark: ").append(highWaterMark).append('\n');
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lowWaterMark` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                        .append("max queue size: ").append(maxQueueSize).append('\n')
                        .append("initialThreads: ").append(initialThreads).append('\n')
                        .append("lowWaterMark: ").append(lowWaterMark).append('\n')
                        .append("highWaterMark: ").append(highWaterMark).append('\n');
                LOG.fine(buf.toString());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `highWaterMark` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                        .append("initialThreads: ").append(initialThreads).append('\n')
                        .append("lowWaterMark: ").append(lowWaterMark).append('\n')
                        .append("highWaterMark: ").append(highWaterMark).append('\n');
                LOG.fine(buf.toString());
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            }

            if (initialThreads > highWaterMark) {
                initialThreads = highWaterMark;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `highWaterMark` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            }

            if (initialThreads > highWaterMark) {
                initialThreads = highWaterMark;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

            if (initialThreads > highWaterMark) {
                initialThreads = highWaterMark;
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `highWaterMark` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

            if (initialThreads > highWaterMark) {
                initialThreads = highWaterMark;
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            // the queue has filled up,
            // potentially causing problems with starting up under heavy load
            if (initialThreads < Integer.MAX_VALUE && initialThreads > 0) {
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            // the queue has filled up,
            // potentially causing problems with starting up under heavy load
            if (initialThreads < Integer.MAX_VALUE && initialThreads > 0) {
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            // potentially causing problems with starting up under heavy load
            if (initialThreads < Integer.MAX_VALUE && initialThreads > 0) {
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
                if (started < initialThreads) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            // potentially causing problems with starting up under heavy load
            if (initialThreads < Integer.MAX_VALUE && initialThreads > 0) {
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
                if (started < initialThreads) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            if (initialThreads < Integer.MAX_VALUE && initialThreads > 0) {
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
                if (started < initialThreads) {
                    LOG.log(Level.WARNING, "THREAD_START_FAILURE_MSG",
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                executor.setCorePoolSize(initialThreads);
                int started = executor.prestartAllCoreThreads();
                if (started < initialThreads) {
                    LOG.log(Level.WARNING, "THREAD_START_FAILURE_MSG",
                            new Object[] {started, initialThreads});
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialThreads` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                if (started < initialThreads) {
                    LOG.log(Level.WARNING, "THREAD_START_FAILURE_MSG",
                            new Object[] {started, initialThreads});
                }
                executor.setCorePoolSize(lowWaterMark);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                            new Object[] {started, initialThreads});
                }
                executor.setCorePoolSize(lowWaterMark);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lowWaterMark` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                            new Object[] {started, initialThreads});
                }
                executor.setCorePoolSize(lowWaterMark);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            try {
                l = ReflectionUtil.accessDeclaredField("mainLock", ThreadPoolExecutor.class,
                                                       executor, ReentrantLock.class);
                if (l == null) {
                    l = new ReentrantLock();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mainLock` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                l = new ReentrantLock();
            }
            mainLock = l;


```

### PublicFieldAccessedInSynchronizedContext
Non-private field `addWorkerMethod` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            try {
                //java 7
                addWorkerMethod = ThreadPoolExecutor.class.getDeclaredMethod("addWorker",
                                                                             Runnable.class, Boolean.TYPE);
                addWorkerArgs = new Object[] {null, Boolean.FALSE};
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `addWorkerArgs` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                addWorkerMethod = ThreadPoolExecutor.class.getDeclaredMethod("addWorker",
                                                                             Runnable.class, Boolean.TYPE);
                addWorkerArgs = new Object[] {null, Boolean.FALSE};
            } catch (Throwable t2) {
                //nothing we cando
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `executor` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            }
        }
        return executor;
    }
    private AWQThreadFactory createThreadFactory(final String nm) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `synchronousTimeout` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
    protected void waitResponse(Exchange exchange) throws IOException {
        synchronized (exchange) {
            long remaining = synchronousTimeout;
            Long o = PropertyUtils.getLong(exchange.getOutMessage(), SYNC_TIMEOUT);
            if (o != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `conduitSelector` accessed in synchronized context
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java

    public final synchronized void setConduitSelector(ConduitSelector selector) {
        conduitSelector = selector == null ? new UpfrontConduitSelector() : selector;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java

    public synchronized W3CEndpointReference getEpr() {
        if (endpoint != null) {
            if (endpointEpr == null) {
                endpointEpr = manager.getEpr(endpoint);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointEpr` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    public synchronized W3CEndpointReference getEpr() {
        if (endpoint != null) {
            if (endpointEpr == null) {
                endpointEpr = manager.getEpr(endpoint);
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointEpr` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
        if (endpoint != null) {
            if (endpointEpr == null) {
                endpointEpr = manager.getEpr(endpoint);
            }
            return endpointEpr;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `manager` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
        if (endpoint != null) {
            if (endpointEpr == null) {
                endpointEpr = manager.getEpr(endpoint);
            }
            return endpointEpr;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
        if (endpoint != null) {
            if (endpointEpr == null) {
                endpointEpr = manager.getEpr(endpoint);
            }
            return endpointEpr;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointEpr` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
                endpointEpr = manager.getEpr(endpoint);
            }
            return endpointEpr;
        }
        return null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java

    public synchronized void unregister() throws EndpointRegistrationException {
        if (endpoint != null) {
            manager.unregister(endpoint, this);
            endpointEpr = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `manager` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    public synchronized void unregister() throws EndpointRegistrationException {
        if (endpoint != null) {
            manager.unregister(endpoint, this);
            endpointEpr = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    public synchronized void unregister() throws EndpointRegistrationException {
        if (endpoint != null) {
            manager.unregister(endpoint, this);
            endpointEpr = null;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointEpr` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
        if (endpoint != null) {
            manager.unregister(endpoint, this);
            endpointEpr = null;
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    }
    public synchronized void register() throws EndpointRegistrationException {
        endpoint = manager.register(getAddress(), this, getWSDLLocation());
        endpointEpr = null;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `manager` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    }
    public synchronized void register() throws EndpointRegistrationException {
        endpoint = manager.register(getAddress(), this, getWSDLLocation());
        endpointEpr = null;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpointEpr` accessed in synchronized context
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    public synchronized void register() throws EndpointRegistrationException {
        endpoint = manager.register(getAddress(), this, getWSDLLocation());
        endpointEpr = null;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writeXsiType` accessed in synchronized context
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
            if (context == null) {
                context = new AegisContext();
                context.setWriteXsiTypes(writeXsiType);
                context.setReadXsiTypes(readXsiType);
                Set<java.lang.reflect.Type> rootClasses = new HashSet<>();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `readXsiType` accessed in synchronized context
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
                context = new AegisContext();
                context.setWriteXsiTypes(writeXsiType);
                context.setReadXsiTypes(readXsiType);
                Set<java.lang.reflect.Type> rootClasses = new HashSet<>();
                rootClasses.add(reflectionType);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `selectInterval` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                while (client.isRunning()) {
                    synchronized (this) {
                        sleep(selectInterval);
                        // make sure pending leases fail in a timely manner,
                        // not just when a connection becomes available
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionTTL` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                        connMgr.validatePendingRequests();

                        if (connectionTTL == 0
                            && connectionMaxIdle > 0 && System.currentTimeMillis() >= nextIdleCheck) {
                            nextIdleCheck += connectionMaxIdle;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionMaxIdle` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

                        if (connectionTTL == 0
                            && connectionMaxIdle > 0 && System.currentTimeMillis() >= nextIdleCheck) {
                            nextIdleCheck += connectionMaxIdle;
                            // close connections
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionMaxIdle` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                        if (connectionTTL == 0
                            && connectionMaxIdle > 0 && System.currentTimeMillis() >= nextIdleCheck) {
                            nextIdleCheck += connectionMaxIdle;
                            // close connections
                            // that have been idle longer than specified connectionMaxIdle
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionMaxIdle` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                            // close connections
                            // that have been idle longer than specified connectionMaxIdle
                            connMgr.closeIdleConnections(connectionMaxIdle, TimeUnit.MILLISECONDS);
                        }
                    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

    public synchronized void setupNIOClient(HTTPClientPolicy clientPolicy) throws IOReactorException {
        if (client != null) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `ioThreadCount` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

        IOReactorConfig config = IOReactorConfig.custom()
                .setIoThreadCount(ioThreadCount)
                .setSelectInterval(selectInterval)
                .setInterestOpQueued(interestOpQueued)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `selectInterval` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        IOReactorConfig config = IOReactorConfig.custom()
                .setIoThreadCount(ioThreadCount)
                .setSelectInterval(selectInterval)
                .setInterestOpQueued(interestOpQueued)
                .setSoLinger(soLinger)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `interestOpQueued` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .setIoThreadCount(ioThreadCount)
                .setSelectInterval(selectInterval)
                .setInterestOpQueued(interestOpQueued)
                .setSoLinger(soLinger)
                .setSoTimeout(soTimeout)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `soLinger` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .setSelectInterval(selectInterval)
                .setInterestOpQueued(interestOpQueued)
                .setSoLinger(soLinger)
                .setSoTimeout(soTimeout)
                .setSoKeepAlive(soKeepalive)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `soTimeout` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .setInterestOpQueued(interestOpQueued)
                .setSoLinger(soLinger)
                .setSoTimeout(soTimeout)
                .setSoKeepAlive(soKeepalive)
                .setTcpNoDelay(tcpNoDelay)
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `soKeepalive` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .setSoLinger(soLinger)
                .setSoTimeout(soTimeout)
                .setSoKeepAlive(soKeepalive)
                .setTcpNoDelay(tcpNoDelay)
                .build();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `tcpNoDelay` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .setSoTimeout(soTimeout)
                .setSoKeepAlive(soKeepalive)
                .setTcpNoDelay(tcpNoDelay)
                .build();

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

        DefaultConnectingIOReactor ioreactor = new DefaultConnectingIOReactor(config);
        connectionManager = new PoolingNHttpClientConnectionManager(
                ioreactor,
                connectionFactory,
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionTTL` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                DefaultSchemePortResolver.INSTANCE,
                SystemDefaultDnsResolver.INSTANCE,
                connectionTTL, TimeUnit.MILLISECONDS);

        connectionManager.setDefaultMaxPerRoute(maxPerRoute);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                connectionTTL, TimeUnit.MILLISECONDS);

        connectionManager.setDefaultMaxPerRoute(maxPerRoute);
        connectionManager.setMaxTotal(maxConnections);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `maxPerRoute` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                connectionTTL, TimeUnit.MILLISECONDS);

        connectionManager.setDefaultMaxPerRoute(maxPerRoute);
        connectionManager.setMaxTotal(maxConnections);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

        connectionManager.setDefaultMaxPerRoute(maxPerRoute);
        connectionManager.setMaxTotal(maxConnections);

        ConnectionConfig connectionConfig = ConnectionConfig.custom()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `maxConnections` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

        connectionManager.setDefaultMaxPerRoute(maxPerRoute);
        connectionManager.setMaxTotal(maxConnections);

        ConnectionConfig connectionConfig = ConnectionConfig.custom()
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                .build();

        connectionManager.setDefaultConnectionConfig(connectionConfig);

        RedirectStrategy redirectStrategy = new RedirectStrategy() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

        HttpAsyncClientBuilder httpAsyncClientBuilder = HttpAsyncClients.custom()
            .setConnectionManager(connectionManager)
            .setRedirectStrategy(redirectStrategy)
            .setDefaultCookieStore(new BasicCookieStore() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        adaptClientBuilder(httpAsyncClientBuilder);

        client = httpAsyncClientBuilder.build();
        // Start the client thread
        client.start();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        client = httpAsyncClientBuilder.build();
        // Start the client thread
        client.start();
        //Always start the idle checker thread to validate pending requests and
        //use the ConnectionMaxIdle to close the idle connection
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        //Always start the idle checker thread to validate pending requests and
        //use the ConnectionMaxIdle to close the idle connection
        new CloseIdleConnectionThread(connectionManager, client).start();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        //Always start the idle checker thread to validate pending requests and
        //use the ConnectionMaxIdle to close the idle connection
        new CloseIdleConnectionThread(connectionManager, client).start();
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    }
    private synchronized void resetVars() {
        client = null;
        connectionManager = null;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `connectionManager` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
    private synchronized void resetVars() {
        client = null;
        connectionManager = null;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

        protected synchronized HttpResponse getHttpResponse() throws IOException {
            while (httpResponse == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        protected synchronized HttpResponse getHttpResponse() throws IOException {
            while (httpResponse == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    }
                }
                if (httpResponse == null) {
                    outbuf.shutdown();
                    inbuf.shutdown();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `outbuf` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                }
                if (httpResponse == null) {
                    outbuf.shutdown();
                    inbuf.shutdown();
                    //outbuf = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `inbuf` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                if (httpResponse == null) {
                    outbuf.shutdown();
                    inbuf.shutdown();
                    //outbuf = null;
                    //inbuf = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    //inbuf = null;

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
                    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                }
            }
            return httpResponse;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

    public synchronized CloseableHttpAsyncClient getHttpAsyncClient() throws IOException {
        if (client == null) {
            client = factory.createClient(this);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
    public synchronized CloseableHttpAsyncClient getHttpAsyncClient() throws IOException {
        if (client == null) {
            client = factory.createClient(this);
        }
        if (client == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            client = factory.createClient(this);
        }
        if (client == null) {
            throw new IOException("HttpAsyncClient is null");
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `client` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            throw new IOException("HttpAsyncClient is null");
        }
        return client;
    }
    public AsyncHTTPConduitFactory getAsyncHTTPConduitFactory() {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslState` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            session = sslsession;
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
                sessionLock.notifyAll();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslURL` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
                sessionLock.notifyAll();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `url` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
                sessionLock.notifyAll();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            connect(true);
            synchronized (sessionLock) {
                if (session == null) {
                    try {
                        sessionLock.wait(csPolicy.getConnectionTimeout());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    }
                }
                if (session == null) {
                    throw new IOException("No SSLSession detected");
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        }
        protected synchronized void setException(Exception ex) {
            exception = ex;
            if (isAsync) {
                //got a response, need to start the response processing now
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        protected synchronized void setException(Exception ex) {
            exception = ex;
            if (isAsync) {
                //got a response, need to start the response processing now
                try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                try {
                    handleResponseOnWorkqueue(false, true);
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex2) {
                    ex2.printStackTrace();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        }
        protected synchronized void setHttpResponse(HttpResponse r) {
            httpResponse = r;
            if (isAsync) {
                //got a response, need to start the response processing now
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        protected synchronized void setHttpResponse(HttpResponse r) {
            httpResponse = r;
            if (isAsync) {
                //got a response, need to start the response processing now
                try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                try {
                    handleResponseOnWorkqueue(false, true);
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex) {
                    //ignore, we'll try again on the next consume;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastTlsHash` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

        int hash = tlsClientParameters.hashCode();
        if (hash == lastTlsHash && sslContext != null) {
            return sslContext;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslContext` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

        int hash = tlsClientParameters.hashCode();
        if (hash == lastTlsHash && sslContext != null) {
            return sslContext;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslContext` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        int hash = tlsClientParameters.hashCode();
        if (hash == lastTlsHash && sslContext != null) {
            return sslContext;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslContext` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        }

        sslContext = ctx;
        lastTlsHash = hash;
        sslState = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `lastTlsHash` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java

        sslContext = ctx;
        lastTlsHash = hash;
        sslState = null;
        sslURL = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslState` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        sslContext = ctx;
        lastTlsHash = hash;
        sslState = null;
        sslURL = null;
        session = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `sslURL` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        lastTlsHash = hash;
        sslState = null;
        sslURL = null;
        session = null;
        return ctx;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `session` accessed in synchronized context
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        sslState = null;
        sslURL = null;
        session = null;
        return ctx;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `url` accessed in synchronized context
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
                sessionLock.notifyAll();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mexEndpoint` accessed in synchronized context
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXInInterceptor.java`
#### Snippet
```java
    }
    private synchronized Endpoint createEndpoint(Message message) {
        if (mexEndpoint == null) {
            MEXJaxWsServerFactoryBean factory
                = new MEXJaxWsServerFactoryBean(message.getExchange().getBus());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mexEndpoint` accessed in synchronized context
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXInInterceptor.java`
#### Snippet
```java
                endpoint.getService().setInvoker(new JAXWSMethodInvoker(ep));

                mexEndpoint = endpoint;
            } catch (Exception ex) {
                throw new Fault(ex);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `mexEndpoint` accessed in synchronized context
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXInInterceptor.java`
#### Snippet
```java
            }
        }
        return mexEndpoint;
    }
}
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialized` accessed in synchronized context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java

        synchronized void loadClass() {
            if (!initialized) {
                try {
                    cls = ClassLoaderUtils.loadClass(className, ProviderFactory.class);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cls` accessed in synchronized context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            if (!initialized) {
                try {
                    cls = ClassLoaderUtils.loadClass(className, ProviderFactory.class);
                } catch (final Throwable ex) {
                    LOG.fine(className + " not available, skipping");
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `initialized` accessed in synchronized context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                    LOG.fine(className + " not available, skipping");
                }
                initialized = true;
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `doInit` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java

    private synchronized void initializeResourcesAndHandlerChain(Server server) {
        if (doInit) {
            try {
                injectResources(getServiceBean());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `doInit` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
            }
        }
        doInit = false;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `done` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
        }
        public synchronized void waitForDone() {
            while (!done) {
                try {
                    wait();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `r` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java

        public synchronized void handleResponse(Response<Object> res) {
            r = res;
            done = true;
            if (continuation != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `done` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
        public synchronized void handleResponse(Response<Object> res) {
            r = res;
            done = true;
            if (continuation != null) {
                continuation.resume();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `continuation` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
            r = res;
            done = true;
            if (continuation != null) {
                continuation.resume();
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `continuation` accessed in synchronized context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
            done = true;
            if (continuation != null) {
                continuation.resume();
            }
            notifyAll();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `databinding` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder.java`
#### Snippet
```java
        if (result == null) {
            synchronized (this) {
                result = databinding;
                if (result == null) {
                    result = new JAXBDataBinding(getContext());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `databinding` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder.java`
#### Snippet
```java
                if (result == null) {
                    result = new JAXBDataBinding(getContext());
                    databinding = result;
                }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `bus` accessed in synchronized context
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java

                if (async) {
                    WorkQueueManager queuem = bus.getExtension(WorkQueueManager.class);
                    WorkQueue queue = queuem.getNamedWorkQueue("udp-conduit");
                    if (queue == null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `incomingObserver` accessed in synchronized context
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java
                    queue.execute(() -> incomingObserver.onMessage(inMessage));
                } else {
                    incomingObserver.onMessage(inMessage);
                    if (!message.getExchange().isSynchronous() || multi) {
                        message.getExchange().setInMessage(null);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `acknowledgement` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/AbstractSequence.java`
#### Snippet
```java

    public synchronized boolean isAcknowledged(long m) {
        for (AcknowledgementRange r : acknowledgement.getAcknowledgementRange()) {
            if (m >= r.getLower().longValue() && r.getUpper().longValue() >= m) {
                return true;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `acknowledgement` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
            boolean done = false;
            int i = 0;
            for (; i < acknowledgement.getAcknowledgementRange().size(); i++) {
                AcknowledgementRange r = acknowledgement.getAcknowledgementRange().get(i);
                if (r.getLower().compareTo(messageNumber) <= 0
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `acknowledgement` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
            int i = 0;
            for (; i < acknowledgement.getAcknowledgementRange().size(); i++) {
                AcknowledgementRange r = acknowledgement.getAcknowledgementRange().get(i);
                if (r.getLower().compareTo(messageNumber) <= 0
                    && r.getUpper().compareTo(messageNumber) >= 0) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `acknowledgement` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                range.setUpper(messageNumber);
                updated = true;
                acknowledgement.getAcknowledgementRange().add(i, range);
                if (acknowledgement.getAcknowledgementRange().size() > 1) {

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `acknowledgement` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                updated = true;
                acknowledgement.getAcknowledgementRange().add(i, range);
                if (acknowledgement.getAcknowledgementRange().size() > 1) {

                    // acknowledge out-of-order at first opportunity
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `endpoint` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            public synchronized Conduit selectConduit(Message message) {
                final Conduit conduit;
                EndpointInfo endpointInfo = endpoint.getEndpointInfo();
                EndpointReferenceType original = endpointInfo.getTarget();
                try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `conduits` accessed in synchronized context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
                    endpointInfo.setAddress(original);
                }
                conduits.clear();
                return conduit;
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `inInterceptors` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    public synchronized void initialize(Service service) {

        inInterceptors.addIfAbsent(JAXBAttachmentSchemaValidationHack.INSTANCE);
        inFaultInterceptors.addIfAbsent(JAXBAttachmentSchemaValidationHack.INSTANCE);

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `inFaultInterceptors` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

        inInterceptors.addIfAbsent(JAXBAttachmentSchemaValidationHack.INSTANCE);
        inFaultInterceptors.addIfAbsent(JAXBAttachmentSchemaValidationHack.INSTANCE);

        // context is already set, don't redo it
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `context` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

        // context is already set, don't redo it
        if (context != null) {
            return;
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `contextClasses` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        }

        contextClasses = new LinkedHashSet<>();

        for (ServiceInfo serviceInfo : service.getServiceInfos()) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `contextClasses` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

        for (ServiceInfo serviceInfo : service.getServiceInfos()) {
            JAXBContextInitializer initializer = new JAXBContextInitializer(getBus(), serviceInfo, contextClasses,
                    typeRefs, this.getUnmarshallerProperties());
            initializer.walk();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `typeRefs` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        for (ServiceInfo serviceInfo : service.getServiceInfos()) {
            JAXBContextInitializer initializer = new JAXBContextInitializer(getBus(), serviceInfo, contextClasses,
                    typeRefs, this.getUnmarshallerProperties());
            initializer.walk();
            if (serviceInfo.getProperty("extra.class") != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `contextClasses` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            if (serviceInfo.getProperty("extra.class") != null) {
                Set<Class<?>> exClasses = serviceInfo.getProperty("extra.class", Set.class);
                contextClasses.addAll(exClasses);
            }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `contextClasses` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        final CachedContextAndSchemas cachedContextAndSchemas;
        try {
            cachedContextAndSchemas = createJAXBContextAndSchemas(contextClasses, tns);
        } catch (JAXBException e1) {
            throw new ServiceConstructionException(e1);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `contextClasses` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        final JAXBContext ctx = cachedContextAndSchemas.getContext();
        if (LOG.isLoggable(Level.FINE)) {
            LOG.log(Level.FINE, "CREATED_JAXB_CONTEXT", new Object[] {ctx, contextClasses});
        }
        setContext(ctx);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `context` accessed in synchronized context
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            }

            JAXBSchemaInitializer schemaInit = new JAXBSchemaInitializer(serviceInfo, col, context,
                                                                         this.qualifiedSchemas, tns);
            schemaInit.walk();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `extensionClass` accessed in synchronized context
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
            try {
                CachedContextAndSchemas ccs
                    = JAXBContextCache.getCachedContextAndSchemas(extensionClass);
                classes = ccs.getClasses();
                unmarshalContext = ccs.getContext();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `written` accessed in synchronized context
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            } finally {
                synchronized (this) {
                    written = true;
                    notifyAll();
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `written` accessed in synchronized context
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
        public ByteArrayOutputStream getResponseStream() throws Exception {
            synchronized (this) {
                while (!written) {
                    wait(10 * 1000);
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jcaBusFactory` accessed in synchronized context
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java

    protected synchronized void init(ClassLoader appserverClassLoader) throws ResourceException {
        if (jcaBusFactory == null) {
            jcaBusFactory = new JCABusFactory(this);
            jcaBusFactory.create(appserverClassLoader, getBootstrapContext());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jcaBusFactory` accessed in synchronized context
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
    protected synchronized void init(ClassLoader appserverClassLoader) throws ResourceException {
        if (jcaBusFactory == null) {
            jcaBusFactory = new JCABusFactory(this);
            jcaBusFactory.create(appserverClassLoader, getBootstrapContext());
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jcaBusFactory` accessed in synchronized context
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
        if (jcaBusFactory == null) {
            jcaBusFactory = new JCABusFactory(this);
            jcaBusFactory.create(appserverClassLoader, getBootstrapContext());
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `archives` accessed in synchronized context
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
    public static synchronized Map<?, ?> getJarContents(String path)
        throws MalformedURLException, IOException {
        if (!archives.containsKey(path)) {
            loadArchive(path);
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `archives` accessed in synchronized context
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java
        }

        return (Map<?, ?>)archives.get(path);
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writer` accessed in synchronized context
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrintWriterEventSender.java`
#### Snippet
```java
    @Override
    public void send(LogEvent event) {
        synchronized (writer) {
            writer.print(Instant.now().toString() + " - PrintWriterEventSender - ");
            writer.print(LogMessageFormatter.format(event));
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writer` accessed in synchronized context
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrintWriterEventSender.java`
#### Snippet
```java
    public void send(LogEvent event) {
        synchronized (writer) {
            writer.print(Instant.now().toString() + " - PrintWriterEventSender - ");
            writer.print(LogMessageFormatter.format(event));
            writer.flush();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writer` accessed in synchronized context
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrintWriterEventSender.java`
#### Snippet
```java
        synchronized (writer) {
            writer.print(Instant.now().toString() + " - PrintWriterEventSender - ");
            writer.print(LogMessageFormatter.format(event));
            writer.flush();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `writer` accessed in synchronized context
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrintWriterEventSender.java`
#### Snippet
```java
            writer.print(Instant.now().toString() + " - PrintWriterEventSender - ");
            writer.print(LogMessageFormatter.format(event));
            writer.flush();
        }
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                                    : di.getBaseURI())
                                   + "#" + uri;
                                Policy policy = registry.lookup(id);
                                if (policy == null) {
                                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `builder` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                                if (policy == null) {
                                    try {
                                        policy = builder.getPolicy(element);
                                        String fragement = "#" + uri;
                                        registry.register(fragement, policy);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                                        policy = builder.getPolicy(element);
                                        String fragement = "#" + uri;
                                        registry.register(fragement, policy);
                                        registry.register(id, policy);
                                    } catch (Exception policyEx) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `registry` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                                        String fragement = "#" + uri;
                                        registry.register(fragement, policy);
                                        registry.register(id, policy);
                                    } catch (Exception policyEx) {
                                        //ignore the policy can not be built
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `builder` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                synchronized (element.getOwnerDocument()) {
                    if (Constants.isPolicyElement(e.getElementType())) {
                        p = builder.getPolicy(element);
                    } else if (Constants.isPolicyRef(e.getElementType())) {
                        PolicyReference ref = builder.getPolicyReference(element);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `builder` accessed in synchronized context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                        p = builder.getPolicy(element);
                    } else if (Constants.isPolicyRef(e.getElementType())) {
                        PolicyReference ref = builder.getPolicyReference(element);
                        if (null != ref) {
                            p = resolveReference(ref, di);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `entity` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        setException(future.cause());
                    }
                    synchronized (entity) {
                        //ensure entity is write in main thread
                    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `entity` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    };
                    
                    synchronized (entity) {
                        Channel syncChannel = getChannel();
                        ChannelFuture channelFuture = syncChannel.writeAndFlush(entity);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `entity` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    synchronized (entity) {
                        Channel syncChannel = getChannel();
                        ChannelFuture channelFuture = syncChannel.writeAndFlush(entity);
                        channelFuture.addListener(listener);
                        outputStream.close();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `outputStream` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        ChannelFuture channelFuture = syncChannel.writeAndFlush(entity);
                        channelFuture.addListener(listener);
                        outputStream.close();
                    }
                }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

        protected synchronized HttpResponse getHttpResponse() throws IOException {
            while (httpResponse == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected synchronized HttpResponse getHttpResponse() throws IOException {
            while (httpResponse == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait(entity.getReceiveTimeout());
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `entity` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        wait(entity.getReceiveTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    }
                }
                if (httpResponse == null) {

                    if (exception != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                if (httpResponse == null) {

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
                    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                }
            }
            return httpResponse;
        }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

        protected synchronized void setChannel(Channel ch) {
            channel = ch;
            notifyAll();
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

        protected synchronized Channel getChannel() throws IOException {
            while (channel == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected synchronized Channel getChannel() throws IOException {
            while (channel == null) {
                if (exception == null) { //already have an exception, skip waiting
                    try {
                        // connection timeout
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `entity` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    try {
                        // connection timeout
                        wait(entity.getConnectionTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    }
                }
                if (channel == null) {

                    if (exception != null) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                if (channel == null) {

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    if (exception != null) {
                        if (exception instanceof IOException) {
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                            throw (IOException)exception;
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        }
                        if (exception instanceof RuntimeException) {
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                            throw (RuntimeException)exception;
                        }
                        throw new IOException(exception);
                    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `channel` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                }
            }
            return channel;

        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `httpResponse` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

        protected synchronized void setHttpResponse(HttpResponse r) {
            httpResponse = r;
            if (isAsync) {
                //got a response, need to start the response processing now
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected synchronized void setHttpResponse(HttpResponse r) {
            httpResponse = r;
            if (isAsync) {
                //got a response, need to start the response processing now
                try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                try {
                    handleResponseOnWorkqueue(false, true);
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex) {
                    //ignore, we'll try again on the next consume;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `exception` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java

        protected synchronized void setException(Throwable ex) {
            exception = ex;
            if (isAsync) {
                //got a response, need to start the response processing now
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected synchronized void setException(Throwable ex) {
            exception = ex;
            if (isAsync) {
                //got a response, need to start the response processing now
                try {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `isAsync` accessed in synchronized context
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                try {
                    handleResponseOnWorkqueue(false, true);
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex2) {
                    ex2.printStackTrace();
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cfg` accessed in synchronized context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            if (getResponseCode(exchange) == null) {
                try {
                    exchange.wait(cfg.getSynchronousTimeout());
                } catch (InterruptedException ex) {
                    // ignore
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `cfg` accessed in synchronized context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            synchronized (exchange) {
                while (exchange.get("IN_CHAIN_COMPLETE") == null) {
                    exchange.wait(cfg.getSynchronousTimeout());
                }
            }
```

## RuleId[ruleID=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `core/src/main/java/org/apache/cxf/databinding/source/mime/MimeSerializer.java`
#### Snippet
```java

    public void serialize(XmlSchemaObject schemaObject,
                          @SuppressWarnings("rawtypes") Class classOfType, Node domNode) {
        Map<Object, Object> metaInfoMap = schemaObject.getMetaInfoMap();
        MimeAttribute mimeType = (MimeAttribute)metaInfoMap.get(MimeAttribute.MIME_QNAME);
```

### RedundantSuppression
Redundant suppression
in `core/src/main/java/org/apache/cxf/interceptor/StaxOutInterceptor.java`
#### Snippet
```java

    @SuppressWarnings("resource")
    public void handleMessage(Message message) {
        OutputStream os = message.getContent(OutputStream.class);
        XMLStreamWriter xwriter = message.getContent(XMLStreamWriter.class);
```

### RedundantSuppression
Redundant suppression
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
    }

    public ManagedConnection matchManagedConnections(@SuppressWarnings("rawtypes") Set mcs, Subject subject,
            ConnectionRequestInfo reqInfo) throws ResourceException {

```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java

    @SuppressWarnings("resource") // Response that is built here shouldn't be closed here
    public Object getResponseObject() {
        Object obj = cont.getObject();
        if (!(obj instanceof Response) && !(obj instanceof Throwable)) {
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java

    @Override
    public UriBuilder path(@SuppressWarnings("rawtypes") Class resource) throws IllegalArgumentException {
        if (resource == null) {
            throw new IllegalArgumentException("resource is null");
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java

    @Override
    public UriBuilder path(@SuppressWarnings("rawtypes") Class resource, String method)
        throws IllegalArgumentException {
        if (resource == null) {
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java

    @SuppressWarnings("resource") // Response shouldn't be closed here
    private void processResponse(ServerProviderFactory providerFactory, Message message) {

        if (isResponseAlreadyHandled(message)) {
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected void setInterceptors(JAXRSServerFactoryBean bean, ServletConfig servletConfig,
                                   String paramName,
                                   String splitChar) throws ServletException {
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/binding/AbstractBindingImpl.java`
#### Snippet
```java
    }

    public void setHandlerChain(@SuppressWarnings("rawtypes") List<Handler> hc) {
        handlerChain = hc;
        if (handlerChain == null || handlerChain.isEmpty()) {
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/binding/http/HTTPBindingImpl.java`
#### Snippet
```java
    @SuppressWarnings("rawtypes")
    @Override
    public void setHandlerChain(List<Handler> hc) {
        super.setHandlerChain(hc);
        validate();
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
    @Override
    public ServiceDelegate createServiceDelegate(URL url, QName qname,
                                                 @SuppressWarnings("rawtypes") Class cls) {
        return new ServiceImpl(null, url, qname, cls);
    }
```

### RedundantSuppression
Redundant suppression
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
    public ServiceDelegate createServiceDelegate(URL wsdlDocumentLocation,
                                                 QName serviceName,
                                                 @SuppressWarnings("rawtypes") Class serviceClass,
                                                 WebServiceFeature ... features) {
        for (WebServiceFeature f : features) {
```

### RedundantSuppression
Redundant suppression
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
                SecurityEvent securityEvent = incomingSecurityEventList.get(i);
                if (securityEvent instanceof TokenSecurityEvent) {
                    @SuppressWarnings("unchecked")
                    TokenSecurityEvent<? extends SecurityToken> tokenSecurityEvent
                        = (TokenSecurityEvent<? extends SecurityToken>) securityEvent;
                    if (tokenSecurityEvent.getSecurityToken().getTokenUsages().contains(
                        SecurityTokenConstants.TokenUsage_Signature)
```

### RedundantSuppression
Redundant suppression
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/OutboundSseEventImpl.java`
#### Snippet
```java
        @Override
        @SuppressWarnings("rawtypes")
        public Builder data(Class newType, Object newData) {
            if (newType == null || newData == null) {
                throw new IllegalArgumentException("Parameters 'type' and 'data' must not be null.");
```

### RedundantSuppression
Redundant suppression
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/OutboundSseEventImpl.java`
#### Snippet
```java
        @Override
        @SuppressWarnings("rawtypes")
        public Builder data(GenericType newType, Object newData) {
            if (newType == null || newData == null) {
                throw new IllegalArgumentException("Parameters 'type' and 'data' must not be null.");
```

### RedundantSuppression
Redundant suppression
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/database/SubscriptionTicket.java`
#### Snippet
```java
     */
    public String getTargetURL() {
        @SuppressWarnings("unchecked")
        JAXBElement<EndpointReferenceType> el
            = (JAXBElement<EndpointReferenceType>)this.getDelivery().getContent().get(0);
        return el.getValue().getAddress().getValue().trim();
    }
```

### RedundantSuppression
Redundant suppression
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/database/SubscriptionTicket.java`
#### Snippet
```java

    public ReferenceParametersType getNotificationReferenceParams() {
        @SuppressWarnings("unchecked")
        JAXBElement<EndpointReferenceType> el
            = (JAXBElement<EndpointReferenceType>)this.getDelivery().getContent().get(0);
        return el.getValue().getReferenceParameters();
    }
```

### RedundantSuppression
Redundant suppression
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
        // check if there is any usable EPR in the Delivery part
        try {
            @SuppressWarnings("unchecked")
            JAXBElement<EndpointReferenceType> notifyTo
                = (JAXBElement<EndpointReferenceType>)request.getContent().get(0);
            if (!EPRInspectionTool.containsUsableEPR(notifyTo.getValue())) {
                throw new NoDeliveryMechanismEstablished();
```

### RedundantSuppression
Redundant suppression
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
     *   java.io.PrintWriter, jakarta.wsdl.Definition, jakarta.wsdl.extensions.ExtensionRegistry)
     */
    public void marshall(@SuppressWarnings("rawtypes") Class parent, QName qname,
                         ExtensibilityElement obj, PrintWriter pw,
                         final Definition wsdl, ExtensionRegistry registry) throws WSDLException {
```

### RedundantSuppression
Redundant suppression
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
     *   jakarta.wsdl.extensions.ExtensionRegistry)
     */
    public ExtensibilityElement unmarshall(@SuppressWarnings("rawtypes") Class parent,
                                           QName qname, Element element, Definition wsdl,
                                           ExtensionRegistry registry) throws WSDLException {
```

### RedundantSuppression
Redundant suppression
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaSerializer.java`
#### Snippet
```java
public class SchemaSerializer implements ExtensionSerializer {

    public void marshall(@SuppressWarnings("rawtypes") Class parentType,
                         QName elementType, ExtensibilityElement extension, PrintWriter pw,
                         Definition def, ExtensionRegistry extReg) throws WSDLException {
```

### RedundantSuppression
Redundant suppression
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/client/XKMSInvoker.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected X509Certificate parseLocateXKMSResponse(LocateResultType locateResultType, List<X509AppId> ids) {

        XKMSException exception = ExceptionMapper.fromResponse(locateResultType);
```

## RuleId[ruleID=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
        } catch (BusException e) {
            IOException ex = new IOException("Could not find destination factory for transport " + transId);
            ex.initCause(e);
            throw ex;
        }
```

## RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'jaxbNoEscapeHandler'
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    private static ClassLoader jaxbXjcLoader;
    private static volatile Optional<Object> jaxbMinimumEscapeHandler;
    private static volatile Optional<Object> jaxbNoEscapeHandler;

    static {
```

### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field 'jaxbMinimumEscapeHandler'
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    private static final Map<String, Class<?>> HOLDER_TYPES_MAP;
    private static ClassLoader jaxbXjcLoader;
    private static volatile Optional<Object> jaxbMinimumEscapeHandler;
    private static volatile Optional<Object> jaxbNoEscapeHandler;

```

### OptionalUsedAsFieldOrParameterType
`OptionalLong` used as type for field 'reconnectDelay'
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/InboundSseEventImpl.java`
#### Snippet
```java
        private String id;
        private String comment;
        private OptionalLong reconnectDelay = OptionalLong.empty();
        private String data;

```

## RuleId[ruleID=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
        String refId = ref.getURI();

        if (!refId.startsWith("#") || refId.length() <= 1) {
            throwFault("Only local Signature References are supported", null);
        }
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
        while (n > 0) {
            int c = (int)skip(n);
            if (c <= 0) {
                throw new EOFException();
            }
```

## RuleId[ruleID=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/ParseJavaDocMojo.java`
#### Snippet
```java
            
            f = AbstractJavadocMojo.class.getDeclaredField("session");
            System.out.println("========>" + session.getProjects());
            f.setAccessible(true);
            f.set(mojo, this.session);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            fileManager.close();
        } catch (IOException e) {
            System.err.print("[ERROR] IOException during compiling.");
            e.printStackTrace();
        }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                    errors.add(diagnostic.toString());
                    if (verbose) {
                        System.err.println(diagnostic.toString());
                    }
                    break;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                    warnings.add(diagnostic.toString());
                    if (verbose) {
                        System.err.println(diagnostic.toString());
                    }
                    break;
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java

            if (p.getErrorStream() != null) {
                StreamPrinter errorStreamPrinter = new StreamPrinter(p.getErrorStream(), "", System.out);
                errorStreamPrinter.start();
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java

            if (p.getInputStream() != null) {
                StreamPrinter infoStreamPrinter = new StreamPrinter(p.getInputStream(), "[INFO]", System.out);
                infoStreamPrinter.start();
            }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            return p.waitFor() == 0;
        } catch (SecurityException e) {
            System.err.println("[ERROR] SecurityException during exec() of compiler \"" + args[0] + "\".");
        } catch (InterruptedException e) {
            // ignore
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java

        } catch (IOException e) {
            System.err.print("[ERROR] IOException during exec() of compiler \"" + args[0] + "\"");
            System.err.println(". Check your path environment variable.");
        } finally {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
        } catch (IOException e) {
            System.err.print("[ERROR] IOException during exec() of compiler \"" + args[0] + "\"");
            System.err.println(". Check your path environment variable.");
        } finally {
            if (tmpFile != null && tmpFile.exists()) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                list.set(classpathIdx + 1, "@" + classpathTmpFile);
            } catch (IOException e) {
                System.err.print("[ERROR] can't write long classpath to @argfile");
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        XMLStreamWriter writer = null;
        try {
            writer = createXMLStreamWriter(System.out);
            copy(new DOMSource(node), writer);
            writer.flush();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
            writer = null;
        } else if ("<stdout>".equals(s)) {
            writer = new PrintWriter(System.out, true);
        } else if ("<stderr>".equals(s)) {
            writer = new PrintWriter(System.err, true);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
            writer = new PrintWriter(System.out, true);
        } else if ("<stderr>".equals(s)) {
            writer = new PrintWriter(System.err, true);
        } else {
            try {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
                    msgPrinted = true;
                    if (verbose) {
                        System.out.println("entering server");
                    }
                }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
                    fileSeen = true;
                    if (verbose) {
                        System.out.println("processing file " + f.getCanonicalPath());
                    }
                    ph.createAndPublish(f, epAddr, bOptSeen);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
                        fileSeen = true;
                        if (verbose) {
                            System.out.println("processing file " + file.getCanonicalPath());
                        }
                        ph.createAndPublish(file, epAddr, bOptSeen);
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
            app.start(args);
        } catch (Exception e) {
            System.err.println("error: " + e.getMessage());
            System.exit(1);
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
        }
        if (app.verbose) {
            System.out.println("server ready...");
        }
        Thread.sleep(5 * 60 * 1000);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
        Thread.sleep(5 * 60 * 1000);
        if (app.verbose) {
            System.out.println("server timed out, exiting");
        }
        System.exit(0);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/container-integration/webapp/src/main/java/demo/spring/HelloWorldImpl.java`
#### Snippet
```java

    public String sayHi(String text) {
        System.out.println("sayHi called");
        return "Hello " + text;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceWrappedImpl.java`
#### Snippet
```java
    public Book getBook(long bookid) throws BookNotFoundFault {
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("getBook -> " + me.getKey() + " : "
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceWrappedImpl.java`
#### Snippet
```java
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
        System.out.println("The Book's id " + bookid);
        Book b = books.get(bookid);

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceWrappedImpl.java`
#### Snippet
```java
    public BookServiceWrappedImpl() {
        Book book = createBook();
        System.out.println("Register the Book's id " + book.getId());
        books.put(book.getId(), book);
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java
        final Builder builder = SseEventSource.target(target).reconnectingEvery(1, TimeUnit.SECONDS);
        try (SseEventSource eventSource = builder.build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java
    
        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collectRaw(titles), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java

        try (SseEventSource eventSource = SseEventSource.target(target).build()) {
            eventSource.register(collect(books), System.out::println);
            eventSource.open();
            // Give the SSE stream some time to collect all events
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
        books.put(b.getId(), b);
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("updateBook -> " + me.getKey() + " : " + me.getValue());
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
    public Books getBooks() {
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("getBooks -> " + me.getKey() + " : " + me.getValue());
        }
        Books b = new Books();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
    public long addBook(Book b) {
        long id = ++currentId;
        System.out.println("addBook : " + b.getName());
        b.setId(id);
        books.put(id, b);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
        books.put(id, b);
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("addBook -> " + me.getKey() + " : "
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
    public BookServiceImpl() {
        Book book = createBook();
        System.out.println("Enregistre Book de id " + book.getId());
        books.put(book.getId(), book);
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
        books.remove(id);
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("deleteBook -> " + me.getKey() + " : " + me.getValue());
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
    public Book getAnotherBook(GetAnotherBook getAnotherBook) throws BookNotFoundFault {
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("getBook -> " + me.getKey() + " : "
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
        System.out.println("Book de id " + getAnotherBook.getId());
        Book b = books.get(getAnotherBook.getId());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
    public Book getBook(GetBook getBook) throws BookNotFoundFault {
        for (Map.Entry<Long, Book> me : books.entrySet()) {
            System.out.println("getBook -> " + me.getKey() + " : "
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/customer/book/BookServiceImpl.java`
#### Snippet
```java
                               + me.getValue().getName() + ", " + me.getValue().getId());
        }
        System.out.println("Book de id " + getBook.getId());
        Book b = books.get(getBook.getId());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
        if (verify(getLog())) {
            if (!inProcess) {
                System.out.println("server passed");
            }
        } else {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
    protected void ready() {
        if (!inProcess) {
            System.out.println("server ready");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
    public SOAPMessage invoke(final SOAPMessage request) {
        try {
            System.out.println("=== Received client request ===");

            // create the SOAPMessage
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            attachment.setContentId("mtom_xop.wsdl");
            message.addAttachmentPart(attachment);
            System.out
                .println("Adding attachment: " + attachment.getContentId() + ":" + attachment.getSize());

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
    public void start() throws Exception {
        try {
            System.out.println("running server");
            run();
            System.out.println("signal ready");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
            System.out.println("running server");
            run();
            System.out.println("signal ready");
            ready();

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
            //
            System.in.read();
            System.out.println("stopping bus");
            tearDown();
        } catch (Throwable ex) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
        } finally {
            if (verify(getLog())) {
                System.out.println("server passed");
            } else {
                System.out.println(ServerLauncher.SERVER_FAILED);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
                System.out.println("server passed");
            } else {
                System.out.println(ServerLauncher.SERVER_FAILED);
            }
            System.out.println("server stopped");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
                System.out.println(ServerLauncher.SERVER_FAILED);
            }
            System.out.println("server stopped");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java

    protected void startFailed() {
        System.out.println(ServerLauncher.SERVER_FAILED);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java

    public boolean handleFault(SOAPMessageContext smc) {
        System.out.println(this + " handleFault()");
        return true;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java

    public TrivialSOAPHandler() {
        System.out.println(this + " Construct");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java
    @PostConstruct
    public void init() {
        System.out.println(this + " PostConstruct");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java

    public boolean handleMessage(SOAPMessageContext smc) {
        System.out.println(this + " handleMessage(): " + greeting);
        return true;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java

    public void close(MessageContext messageContext) {
        System.out.println(this + " close()");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java
    @PreDestroy
    public void destroy() {
        System.out.println(this + " PreDestroy");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/EmbeddedJMSBrokerLauncher.java`
#### Snippet
```java
            s.start();
        } finally {
            System.out.println("done!");
        }
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
        }
        if (nExpectedIn != nIn) {
            System.out.println((nExpectedIn < nIn ? "excess" : "shortfall")
                               + " of " + Math.abs(nExpectedIn - nIn)
                               + " incoming messages");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
                               + " of " + Math.abs(nExpectedIn - nIn)
                               + " incoming messages");
            System.out.println("\nMessages actually received:\n");
            List<byte[]> inbound = inRecorder.getInboundMessages();
            for (byte[] b : inbound) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
            List<byte[]> inbound = inRecorder.getInboundMessages();
            for (byte[] b : inbound) {
                System.out.println(new String(b));
                System.out.println("----------------");
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
            for (byte[] b : inbound) {
                System.out.println(new String(b));
                System.out.println("----------------");
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
        }
        if (nExpectedOut != nOut) {
            System.out.println((nExpectedOut < nOut ? "excess" : "shortfall")
                               + " of " + Math.abs(nExpectedOut - nOut)
                               + " outgoing messages");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
                               + " of " + Math.abs(nExpectedOut - nOut)
                               + " outgoing messages");
            System.out.println("\nMessages actually sent:\n");
            List<byte[]> outbound = outRecorder.getOutboundMessages();
            for (byte[] b : outbound) {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
            List<byte[]> outbound = outRecorder.getOutboundMessages();
            for (byte[] b : outbound) {
                System.out.println(new String(b));
                System.out.println("----------------");
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/MessageRecorder.java`
#### Snippet
```java
            for (byte[] b : outbound) {
                System.out.println(new String(b));
                System.out.println("----------------");
            }
        }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public void greetMeOneWay(String requestType) {
        System.out.println("*********  greetMeOneWay: " + requestType);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_doc_lit/GreeterImplDoc.java`
#### Snippet
```java

    public String greetMe(String requestType) {
        System.out.println("Reached here :" + requestType);
        return "Hello " + requestType;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_doc_lit/GreeterImplDoc.java`
#### Snippet
```java

    public String sayHi() {
        System.out.println("Call sayHi here ");
        return "Bonjour";
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_doc_lit/GreeterImplDoc.java`
#### Snippet
```java

    public void greetMeOneWay(String requestType) {
        System.out.println("*********  greetMeOneWay: " + requestType);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
        return 0;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
    public void greetMeOneWay(String me) {
        incrementInvocationCount("greetMeOneWay");
        System.out.println("Hello there " + me);
        System.out.println("That was OneWay to say hello");
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
        incrementInvocationCount("greetMeOneWay");
        System.out.println("Hello there " + me);
        System.out.println("That was OneWay to say hello");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
    public void greetMeOneWay(String me) {
        incrementInvocationCount("greetMeOneWay");
        System.out.println("Hello there " + me);
        System.out.println("That was OneWay to say hello");
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
        incrementInvocationCount("greetMeOneWay");
        System.out.println("Hello there " + me);
        System.out.println("That was OneWay to say hello");
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
        return 0;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
        return 0;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
                    mutex.wait(1000L);
                    if (tc.isTimeoutExpired()) {
                        System.out.println("destroying server process");
                        process.destroy();
                        break;
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            LOG.fine("CMD: " + cmd);
            if (DEBUG) {
                System.err.print("CMD: " + cmd);
            }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            }
            if (serverLaunchFailed || !serverIsReady) {
                System.err.println(out.getServerOutput());
            }

```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
                        serverOutputAll.append(line);
                        if (DEBUG) {
                            System.err.print(line);
                        }
                        if (line.contains("server ready")) {
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
                                           AsyncHandler<GreetMeSometimeResponse> asyncHandler) {
        invocationCount++;
        System.err.println("In greetMeSometimeAsync 1");
        return null;
        /*not called */
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
        AsyncHandler<org.apache.hello_world_soap_http.types.TestDocLitFaultResponse> ah) {
        invocationCount++;
        System.err.println("In testDocLitFaultAsync 2");
        return null;
        /*not called */
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java

    public String testNillable(String nillElem, int intElem) {
        System.out.println("the testNillable is invoked");
        return nillElem;
    }
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType) {
        invocationCount++;
        System.err.println("In testDocLitFaultAsync 1");
        return null;
        /*not called */
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<GreetMeSometimeResponse> greetMeSometimeAsync(String requestType) {
        invocationCount++;
        System.err.println("In greetMeSometimeAsync 2");
        return null;
        /*not called */
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_mixedstyle/GreeterImplMixedStyle.java`
#### Snippet
```java

    public void greetMeOneWay(String requestType) {
        System.out.println("*********  greetMeOneWay: " + requestType);
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_mixedstyle/GreeterImplMixedStyle.java`
#### Snippet
```java

    public GreetMeResponse greetMe(GreetMe1 requestType) {
        System.out.println("Call greetMe here: " + requestType.getRequestType());
        GreetMeResponse response = new GreetMeResponse();
        response.setResponseType("Hello " + requestType.getRequestType() + version);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_mixedstyle/GreeterImplMixedStyle.java`
#### Snippet
```java

    public String sayHi() {
        System.out.println("Call sayHi here ");
        return "Bonjour" + version;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
            return invocationCount.get(method).intValue();
        }
        System.out.println("No invocation count for method: " + method);
        return 0;
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        printPolicyComponent(pc, buf, 0);
        System.out.println(buf.toString());
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
        }
        if ("<stdout>".equals(location)) {
            return new PrintWriterEventSender(System.out);
        } else if ("<stderr>".equals(location)) {
            return new PrintWriterEventSender(System.err);                
```

### SystemOutErr
Uses of `System.err` should probably be replaced with more robust logging
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
            return new PrintWriterEventSender(System.out);
        } else if ("<stderr>".equals(location)) {
            return new PrintWriterEventSender(System.err);                
        } else if (location.startsWith("file:")) {
            try {
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                endpointName = it.next();
                mbsc.invoke(endpointName, operation, new Object[0], new String[0]);
                System.out.println("invoke endpoint " + endpointName
                                   + " operation " + operation + " succeed!");
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                try {
                    Object o = mbsc.getAttribute(name, attrs[i].getName());
                    System.out.println("\t\t" + attrs[i].getName() + " = " + o);
                } catch (Exception e) {
                    e.printStackTrace();
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                                                          + ":type=Bus.Service.Endpoint,*");
            Set<ObjectName> endpointNames = CastUtils.cast(mbsc.queryNames(queryEndpointName, null));
            System.out.println("The endpoints are : ");
            for (ObjectName oName : endpointNames) {
                System.out.println(oName);
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
            System.out.println("The endpoints are : ");
            for (ObjectName oName : endpointNames) {
                System.out.println(oName);
                getManagedObjectAttributes(oName);
            }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java

    private static void printUsage() {
        System.out.println("Management Console for CXF Managed Endpoints");
        System.out.println("You can start and stop the endpoints which export as JMX managed objects");
        System.out.println("Usage: -o list ");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
    private static void printUsage() {
        System.out.println("Management Console for CXF Managed Endpoints");
        System.out.println("You can start and stop the endpoints which export as JMX managed objects");
        System.out.println("Usage: -o list ");
        System.out.println("       -o {start|stop|restart} -p PORTQNAME -s SERVICEQNAME ");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("Management Console for CXF Managed Endpoints");
        System.out.println("You can start and stop the endpoints which export as JMX managed objects");
        System.out.println("Usage: -o list ");
        System.out.println("       -o {start|stop|restart} -p PORTQNAME -s SERVICEQNAME ");
        System.out.println("Valid options:");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("You can start and stop the endpoints which export as JMX managed objects");
        System.out.println("Usage: -o list ");
        System.out.println("       -o {start|stop|restart} -p PORTQNAME -s SERVICEQNAME ");
        System.out.println("Valid options:");
        System.out.println(" -o [--operation] {list|start|stop|restart}  call the managed endpoint "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("Usage: -o list ");
        System.out.println("       -o {start|stop|restart} -p PORTQNAME -s SERVICEQNAME ");
        System.out.println("Valid options:");
        System.out.println(" -o [--operation] {list|start|stop|restart}  call the managed endpoint "
                        + "operation");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("       -o {start|stop|restart} -p PORTQNAME -s SERVICEQNAME ");
        System.out.println("Valid options:");
        System.out.println(" -o [--operation] {list|start|stop|restart}  call the managed endpoint "
                        + "operation");
        System.out.println("                          list: show all the managed endpoints' objectNames and");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println(" -o [--operation] {list|start|stop|restart}  call the managed endpoint "
                        + "operation");
        System.out.println("                          list: show all the managed endpoints' objectNames and");
        System.out.println("                                attributes");
        System.out.println("                          start: start the endpoint with the -p and -s "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                        + "operation");
        System.out.println("                          list: show all the managed endpoints' objectNames and");
        System.out.println("                                attributes");
        System.out.println("                          start: start the endpoint with the -p and -s "
                        + "arguments");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("                          list: show all the managed endpoints' objectNames and");
        System.out.println("                                attributes");
        System.out.println("                          start: start the endpoint with the -p and -s "
                        + "arguments");
        System.out.println("                          stop: stop the endpoint with the -p and -s arguments");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("                          start: start the endpoint with the -p and -s "
                        + "arguments");
        System.out.println("                          stop: stop the endpoint with the -p and -s arguments");
        System.out.println("                          restart: restart the endpoint with the -p and -s "
                        + "arguments");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                        + "arguments");
        System.out.println("                          stop: stop the endpoint with the -p and -s arguments");
        System.out.println("                          restart: restart the endpoint with the -p and -s "
                        + "arguments");

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                        + "arguments");

        System.out.println(" -p [--port] arg          ARG: the port Qname of the managed endpoint");
        System.out.println(" -s [--service] arg       ARG: the service Qname of the managed endpoint");
        System.out.println(" -j [--jmx] arg           ARG: the JMXServerURL for connecting to the mbean "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java

        System.out.println(" -p [--port] arg          ARG: the port Qname of the managed endpoint");
        System.out.println(" -s [--service] arg       ARG: the service Qname of the managed endpoint");
        System.out.println(" -j [--jmx] arg           ARG: the JMXServerURL for connecting to the mbean "
                           + "server");
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println(" -p [--port] arg          ARG: the port Qname of the managed endpoint");
        System.out.println(" -s [--service] arg       ARG: the service Qname of the managed endpoint");
        System.out.println(" -j [--jmx] arg           ARG: the JMXServerURL for connecting to the mbean "
                           + "server");
        System.out.println("                           if not using this option, the JMXServerURL will be "
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println(" -j [--jmx] arg           ARG: the JMXServerURL for connecting to the mbean "
                           + "server");
        System.out.println("                           if not using this option, the JMXServerURL will be "
                           + "set as");
        System.out.println("                           \"service:jmx:rmi:///jndi/rmi://localhost:1099/jmxrmi"
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
        System.out.println("                           if not using this option, the JMXServerURL will be "
                           + "set as");
        System.out.println("                           \"service:jmx:rmi:///jndi/rmi://localhost:1099/jmxrmi"
                        + "\"");

```

## RuleId[ruleID=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        }
        try {
            String path2 = URLDecoder.decode(path, "UTF-8");
            if (!path.equals(path2)) {
                decodedDestinations.put(path2, destination);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        destinations.remove(path);
        try {
            String path2 = URLDecoder.decode(path, "UTF-8");
            if (!path.equals(path2)) {
                decodedDestinations.remove(path2);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlRedirectBindingFilter.java`
#### Snippet
```java
            SamlRequestInfo info = createSamlRequestInfo(m);
            String urlEncodedRequest =
                URLEncoder.encode(info.getSamlRequest(), StandardCharsets.UTF_8.name());

            UriBuilder ub = UriBuilder.fromUri(getIdpServiceAddress());
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlRedirectBindingFilter.java`
#### Snippet
```java

        LOG.fine("Using Signature algorithm " + sigAlgo);
        ub.queryParam(SSOConstants.SIG_ALG, URLEncoder.encode(sigAlgo, StandardCharsets.UTF_8.name()));

        // Get the password
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlRedirectBindingFilter.java`
#### Snippet
```java
            SSOConstants.SAML_REQUEST + "=" + authnRequest + "&"
            + SSOConstants.RELAY_STATE + "=" + relayState + "&"
            + SSOConstants.SIG_ALG + "=" + URLEncoder.encode(sigAlgo, StandardCharsets.UTF_8.name());

        signature.update(requestToSign.getBytes(StandardCharsets.UTF_8));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlRedirectBindingFilter.java`
#### Snippet
```java
        }

        ub.queryParam(SSOConstants.SIGNATURE, URLEncoder.encode(encodedSignature, StandardCharsets.UTF_8.name()));

    }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
                                                     getStateTimeToLive());

        String relayState = URLEncoder.encode(UUID.randomUUID().toString(), StandardCharsets.UTF_8.name());
        getStateProvider().setRequestState(relayState, requestState);
        info.setRelayState(relayState);
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
            if (!SecurityActions.fileExists(uriFile, CXFPermissions.RESOLVE_URI)) {
                try {
                    URI urif = new URI(URLDecoder.decode(uriStr, "ASCII"));
                    if ("file".equals(urif.getScheme()) && urif.isAbsolute()) {
                        File f2 = new File(urif);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        }
        return ATT_UUID + '-' + Integer.toString(COUNTER.incrementAndGet()) + '@'
            + URLEncoder.encode(cid, StandardCharsets.UTF_8.name());
    }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        if (contentId.startsWith("cid:")) {
            try {
                contentId = URLDecoder.decode(contentId.substring(4), StandardCharsets.UTF_8.name());
            } catch (UnsupportedEncodingException ue) {
                contentId = contentId.substring(4);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            try {
                // urldecode
                id = URLDecoder.decode(id, StandardCharsets.UTF_8.name());
            } catch (UnsupportedEncodingException e) {
                //ignore, keep id as is
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `core/src/main/java/org/apache/cxf/attachment/AttachmentSerializer.java`
#### Snippet
```java
                    }
                } else {
                    writer.write(URLEncoder.encode(attachmentId, StandardCharsets.UTF_8.name()));
                }
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
        // Decode the path.
        try {
            uri = URLDecoder.decode(uri, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            try {
```

### CharsetObjectCanBeUsed
StandardCharsets.ISO_8859_1 can be used instead
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
        } catch (UnsupportedEncodingException e) {
            try {
                uri = URLDecoder.decode(uri, StandardCharsets.ISO_8859_1.name());
            } catch (UnsupportedEncodingException e1) {
                throw new Error();
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java

    private static String getDecodedAttachmentId(String attachmentId) throws IOException {
        return URLDecoder.decode(attachmentId, StandardCharsets.UTF_8.name());
    }

```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSURIParser.java`
#### Snippet
```java
    private static String urldecode(String s) {
        try {
            return URLDecoder.decode(s, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            throw new IllegalArgumentException("Encoding UTF-8 not supported");
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/UnsafeUriCharactersEncoder.java`
#### Snippet
```java
        try {
            // First check whether we actually need to encode
            byte[] bytes = s.getBytes("UTF8");
            for (int i = 0;;) {
                if (unsafeCharacters.get(bytes[i])) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java

                try {
                    decodedStart = URLDecoder.decode(start, "utf-8");
                } catch (UnsupportedEncodingException e) {
                    throw new WSDLQueryException(
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                        try {
                            start = getLocationURI(start, docBase);
                            decodedStart = URLDecoder.decode(start, "utf-8");
                        } catch (Exception e1) {
                            //ignore
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            //
            if (params.containsKey("wsdl")) {
                String wsdl = URLDecoder.decode(params.get("wsdl"), "utf-8");
                doc = writeWSDLDocument(message, mp, smp, wsdl, base, endpointInfo);
            } else if (params.get("xsd") != null) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                doc = writeWSDLDocument(message, mp, smp, wsdl, base, endpointInfo);
            } else if (params.get("xsd") != null) {
                String xsd = URLDecoder.decode(params.get("xsd"), "utf-8");
                doc = readXSDDocument(bus, xsd, smp, base);
                updateDoc(doc, base, mp, smp, message, xsd);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            // canonical representation of the import URL for lookup.
            try {
                decodedStart = URLDecoder.decode(start, "utf-8");
                decodedOrigLocation = URLDecoder.decode(origLocation, "utf-8");
            } catch (UnsupportedEncodingException e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            try {
                decodedStart = URLDecoder.decode(start, "utf-8");
                decodedOrigLocation = URLDecoder.decode(origLocation, "utf-8");
            } catch (UnsupportedEncodingException e) {
                throw new WSDLQueryException(
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                key = new URI(xsd).resolve(loc).toString();

                SchemaReference ref = smp.get(URLDecoder.decode(key, "utf-8"));
                if (ref == null) {
                    // if the result is not known, check if we can resolve it into something known
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    // if the result is not known, check if we can resolve it into something known
                    String resolved = resolveWithCatalogs(OASISCatalogManager.getCatalogManager(bus), key, base);
                    if (resolved != null  && smp.containsKey(URLDecoder.decode(resolved, "utf-8"))) {
                        // if it is resolvable, we can use it
                        return base + "?xsd=" + key.replace(" ", "%20");
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
           //ignore
        }
        SchemaReference ref = smp.get(URLDecoder.decode(key, "utf-8"));
        if (ref == null && resolvedXsd != null) {
            try {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            try {
                String key2 = new URI(resolvedXsd).resolve(loc).toString();
                SchemaReference ref2 = smp.get(URLDecoder.decode(key2, "utf-8"));
                if (ref2 == null) {
                    // if the result is not known, check if we can resolve it into something known
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    // if the result is not known, check if we can resolve it into something known
                    String resolved = resolveWithCatalogs(OASISCatalogManager.getCatalogManager(bus), key2, base);
                    if (resolved != null  && smp.containsKey(URLDecoder.decode(resolved, "utf-8"))) {
                        // if it is resolvable, we can use it
                        ref = smp.get(URLDecoder.decode(resolved, "utf-8"));
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    if (resolved != null  && smp.containsKey(URLDecoder.decode(resolved, "utf-8"))) {
                        // if it is resolvable, we can use it
                        ref = smp.get(URLDecoder.decode(resolved, "utf-8"));
                    }
                } else {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    }
                } else {
                    ref = smp.get(URLDecoder.decode(key2, "utf-8"));
                }
            } catch (URISyntaxException e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    //ignore
                }
                if (mp.containsKey(URLDecoder.decode(sl, "utf-8"))) {
                    el.setAttribute("location", base + "?wsdl=" + sl.replace(" ", "%20"));
                }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
        try (ByteArrayOutputStream bos = new ByteArrayOutputStream()) {
            FormUtils.writeMapToOutputStream(form.asMap(), bos, StandardCharsets.UTF_8.name(), false);
            return bos.toString(StandardCharsets.UTF_8.name());
        } catch (Exception ex) {
            // will not happen
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                                continue;
                            }
                            file = new File(URLDecoder.decode(url.getPath(), "utf-8"));
                        } catch (UnsupportedEncodingException uee) {
                            // ignored as utf-8 is supported
```

### CharsetObjectCanBeUsed
StandardCharsets.US_ASCII can be used instead
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
            rootPart = root;
            try {
                writer = new OutputStreamWriter(bout, "ASCII");
                writer.append("Content-Type: ").append(contentType).append("\r\n\r\n");
                writer.flush();
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/JsServiceFactoryBean.java`
#### Snippet
```java
        BusFactory.setDefaultBus(bus);
        String jsFileString = getClass().getResource(js).toURI().getPath();
        jsFileString = URLDecoder.decode(jsFileString, StandardCharsets.UTF_8.name());
        File file = new File(jsFileString);
        providerFactory.createAndPublish(file, address, isBaseAddr);
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
                int len = readShort();
                skipFully(len);
                cpool[i] = new String(buf, pos - len, len, "utf-8");
                s = new String(buf, pos - len, len, "utf-8");
            }
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
                skipFully(len);
                cpool[i] = new String(buf, pos - len, len, "utf-8");
                s = new String(buf, pos - len, len, "utf-8");
            }
            return s;
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
                    //TODO may want use string directly instead of converting it to byte[]
                    try {
                        byte[] bdata = message.getBytes("utf-8");
                        onWebSocketBinary(bdata, 0, bdata.length);
                    } catch (UnsupportedEncodingException e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
        public ByteArrayBuilder append(String s) {
            try {
                baos.write(s.getBytes("utf-8"));
            } catch (IOException e) {
                // ignore
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            MessageDigest cript = MessageDigest.getInstance("SHA-1");
            cript.reset();
            cript.update(doneFileName.getBytes("utf8"));
            doneFileName = new jakarta.xml.bind.annotation.adapters.HexBinaryAdapter().marshal(cript.digest());
        } catch (Exception e) {
```

## RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/BasicNameManager.java`
#### Snippet
```java
     */
    protected String transformURI(String uri) {
        return uri.replaceAll("http:/*", "").replace("uri:", "").replaceAll("[\\.:/-]", "_");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/BasicNameManager.java`
#### Snippet
```java
     */
    protected String transformURI(String uri) {
        return uri.replaceAll("http:/*", "").replace("uri:", "").replaceAll("[\\.:/-]", "_");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
    private String buildNamespaceList(String anyNamespaceSpec) {
        StringBuilder nslist = new StringBuilder();
        String[] namespaces = anyNamespaceSpec.split("\\s");
        nslist.append("[ ");
        for (int x = 0; x < namespaces.length; x++) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java

    public static String protectSingleQuotes(String value) {
        return value.replaceAll("'", "\\'");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletDestination.java`
#### Snippet
```java
        }
        if (address.startsWith("http")) {
            return URI.create(address.replaceAll(" ", "%20")).getPath();
        }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
        this.styleSheetPath = StringEscapeUtils.escapeHtml4(styleSheetPath);
        // Strip multiple forward slashes from the start of the styleSheePath to prevent CSS injection attacks
        this.styleSheetPath = this.styleSheetPath.replaceFirst("(/)+", "/");
        this.title = title;
        this.showForeignContexts = showForeignContexts;
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        if (values != null) {
            List<Pattern> list = new ArrayList<>();
            for (String value : values.split("\\s")) {
                if (!value.isEmpty()) {
                    list.add(Pattern.compile(value));
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
            sequence = sequence.trim();
            Map<String, String> map = new HashMap<>();
            for (String pair : sequence.split("\\s")) {
                if (!pair.isEmpty()) {
                    String[] value = pair.split("=");
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapCertificateRepo.java`
#### Snippet
```java

    protected String getDnForIdentifier(String id) {
        String escapedIdentifier = id.replaceAll("\\/", Matcher.quoteReplacement("\\/"));
        return String.format(ldapConfig.getServiceCertRDNTemplate(), escapedIdentifier) + "," + rootDN;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiCustomizer.java`
#### Snippet
```java

                    tag = Optional.of(new Tag());
                    tag.get().setName(cri.getURITemplate().getValue().replaceAll("/", "_"));
                    if (javadocProvider != null) {
                        tag.get().setDescription(javadocProvider.getClassDoc(cri));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
        return value;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String toSqlWildcardString(String value, boolean alwaysWildcard) {
        if (value.contains("\\")) {
            value = value.replaceAll("\\\\", "\\\\\\\\");
        }
        if (value.contains("_") && escapeUnderscoreChar()) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        }
        if (value.contains("_") && escapeUnderscoreChar()) {
            value = value.replaceAll("_", "\\\\_");
        }
        if (value.contains("%")) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        }
        if (value.contains("%")) {
            value = value.replaceAll("%", "\\\\%");
        }
        if (!containsWildcard(value)) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
            return alwaysWildcard ? "%" + value + "%" : value;
        }
        value = value.replaceAll("\\*", "%");
        return value;
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java

    public static Date dateFromStringWithDefaultFormats(String value) {
        value = value.replaceAll("Z$", "+0000");
        Date date = timestampFromString(new SimpleDateFormat(TIMESTAMP_WITH_TIMEZONE_Z), value);

```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/bus/spring/ControlledValidationXmlBeanDefinitionReader.java`
#### Snippet
```java
            throw new StaleFastinfosetException();
        }
        String fixmlPath = resUrl.getPath().replaceFirst("\\.xml$", ".fixml");
        String protocol = resUrl.getProtocol();
        // beware of the relative URL rules for jar:, which are surprising.
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/bus/spring/ControlledValidationXmlBeanDefinitionReader.java`
#### Snippet
```java
        // beware of the relative URL rules for jar:, which are surprising.
        if ("jar".equals(protocol)) {
            fixmlPath = fixmlPath.replaceFirst("^.*!", "");
        }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        PRIMITIVE_MAP.put(Double.TYPE, "D");

        NONPRIMITIVE_MAP.put(Byte.TYPE, Byte.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Boolean.TYPE, Boolean.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Long.TYPE, Long.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java

        NONPRIMITIVE_MAP.put(Byte.TYPE, Byte.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Boolean.TYPE, Boolean.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Long.TYPE, Long.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Integer.TYPE, Integer.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Byte.TYPE, Byte.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Boolean.TYPE, Boolean.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Long.TYPE, Long.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Integer.TYPE, Integer.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Short.TYPE, Short.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Boolean.TYPE, Boolean.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Long.TYPE, Long.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Integer.TYPE, Integer.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Short.TYPE, Short.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Character.TYPE, Character.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Long.TYPE, Long.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Integer.TYPE, Integer.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Short.TYPE, Short.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Character.TYPE, Character.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Float.TYPE, Float.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Integer.TYPE, Integer.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Short.TYPE, Short.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Character.TYPE, Character.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Float.TYPE, Float.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Double.TYPE, Double.class.getName().replaceAll("\\.", "/"));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Short.TYPE, Short.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Character.TYPE, Character.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Float.TYPE, Float.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Double.TYPE, Double.class.getName().replaceAll("\\.", "/"));
    }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        NONPRIMITIVE_MAP.put(Character.TYPE, Character.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Float.TYPE, Float.class.getName().replaceAll("\\.", "/"));
        NONPRIMITIVE_MAP.put(Double.TYPE, Double.class.getName().replaceAll("\\.", "/"));
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                                && SystemPropertyAction.getProperty("os.name")
                                    .toLowerCase().indexOf("windows") > -1) {
                                out.println('"' + args[i].replaceAll("/", "\\\\\\\\") + '"');
                            } else {
                                out.println('"' + args[i] + '"');
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                // if any classpath entry contains a whitespace char, 
                // enclose the entry in double quotes
                if (tok.matches(".*\\s+.*")) {
                    correctedEntries.add("\"" + tok + "\"");
                } else {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/attachment/AttachmentSerializer.java`
#### Snippet
```java
    // only the % encoded character to their equivalent US-ASCII characters. 
    private static String decode(String s, Charset charset) {
        return URLDecoder.decode(s.replaceAll("([^%])[+]", "$1%2B"), charset);
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
                        if (null != className) {
                            final String name = n.charAt(0) != '*' ? n
                                    : "." + n.replaceAll("\\.", "\\."); //old wildcard
                            try {
                                Matcher matcher = Pattern.compile(name).matcher("");
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java
        if (fork) {
            if (!StringUtils.isEmpty(additionalJvmArgs)) {
                String[] split = additionalJvmArgs.split("\\s+");
                args.addAll(Arrays.asList(split));
            }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
        bus = b;
        this.jmsConfig = jmsConfig;
        conduitId = UUID.randomUUID().toString().replaceAll("-", "");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/ExceptionClassGenerator.java`
#### Snippet
```java
    public Class<?> createExceptionClass(Class<?> bean) {
        String newClassName = bean.getName() + "_Exception";
        newClassName = newClassName.replaceAll("\\$", ".");
        newClassName = StringUtils.periodToSlashes(newClassName);

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java

    private String stripQuotes(String value) {
        return value.replaceAll(DOUBLE_QUOTE, "");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                String strValue = value.toString();
                if (encodeSlash) {
                    strValue = strValue.replaceAll("/", "%2F");
                }
                list.add(strValue);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                            val = HttpUtils.encodePartiallyEncoded(val, isQuery);
                            if (!isQuery) {
                                val = val.replaceAll("/", "%2F");
                            }
                        } else {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                            val = HttpUtils.encodePartiallyEncoded(val, isQuery);
                            if (!isQuery) {
                                val = val.replaceAll("/", "%2F");
                            }
                        } else {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        } else {
            segments = new ArrayList<>();
            path = path.replaceAll("/", "%2F");
            segments.add(new PathSegmentImpl(path, false));
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
                    }
                    if (encodePathSlashVars.contains(var.getName())) {
                        sval = sval.replaceAll("/", "%2F");
                    }
                    sb.append(sval);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
        int count = 1;
        String newClassName = wrapperType.getName() + "_WrapperTypeHelper" + count;
        newClassName = newClassName.replaceAll("\\$", ".");
        newClassName = StringUtils.periodToSlashes(newClassName);

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
                    count++;
                    newClassName = wrapperType.getName() + "_WrapperTypeHelper" + count;
                    newClassName = newClassName.replaceAll("\\$", ".");
                    newClassName = StringUtils.periodToSlashes(newClassName);
                    cls = findClass(StringUtils.slashesToPeriod(newClassName), wrapperType);
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

                try {
                    String newAcc = getAccessor.replaceFirst("get", "is");
                    getMethod = wrapperType.getMethod(newAcc, AbstractWrapperHelper.NO_CLASSES);
                } catch (NoSuchMethodException ex) {
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/AbstractHawkAccessTokenValidator.java`
#### Snippet
```java
        for (String pair : attributePairs) {
            String[] pairValues = pair.trim().split("=", 2);
            attributeMap.put(pairValues[0].trim(), pairValues[1].trim().replaceAll("\"", ""));
        }
        return attributeMap;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java

        String regex = this.groupNameGlobalFilter;
        regex = regex.replaceAll(ROLE, ".*");
        Pattern globalPattern = Pattern.compile(regex);

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java
        if (scope != null) {
            regex = this.groupNameScopedFilter;
            regex = regex.replaceAll(SCOPE, scope).replaceAll(ROLE, ".*");
            scopePattern = Pattern.compile(regex);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java
        if (scope != null) {
            regex = this.groupNameScopedFilter;
            regex = regex.replaceAll(SCOPE, scope).replaceAll(ROLE, ".*");
            scopePattern = Pattern.compile(regex);
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapGroupClaimsHandler.java`
#### Snippet
```java
                    role = group;
                } else {
                    role = parseRole(group, filter.replaceAll(SCOPE, scope));
                }
                filteredGroups.add(role);
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResolver.java`
#### Snippet
```java
    protected static String checkUiRoot(String urlStr, String swaggerUiVersion) {
        int swaggerUiIndex = urlStr.lastIndexOf("/swagger-ui-");
        if (swaggerUiIndex != -1 && urlStr.matches("^.*\\.jar!?/?$")) {
            String version = urlStr.substring(swaggerUiIndex + 12, urlStr.lastIndexOf(".jar"));
            if (swaggerUiVersion != null && !swaggerUiVersion.equals(version)) {
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
        List<X509Certificate> result = new ArrayList<>();
        File[] list = getX509Files();
        String[] sDnArray = subjectDn.split(SPLIT_REGEX);
        Arrays.sort(sDnArray);
        for (File certFile : list) {
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                LOG.debug("Searching for " + subjectDn + ". Checking cert "
                    + cert.getSubjectDN().getName() + ", " + cert.getSubjectX500Principal().getName());
                String[] csDnArray = cert.getSubjectDN().getName().split(SPLIT_REGEX);
                Arrays.sort(csDnArray);
                String[] csX500Array = cert.getSubjectX500Principal().getName().split(SPLIT_REGEX);
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                String[] csDnArray = cert.getSubjectDN().getName().split(SPLIT_REGEX);
                Arrays.sort(csDnArray);
                String[] csX500Array = cert.getSubjectX500Principal().getName().split(SPLIT_REGEX);
                Arrays.sort(csX500Array);
                if (arraysEqualsIgnoreCaseIgnoreWhiteSpace(sDnArray, csDnArray)
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
                                    final String sensitiveName,
                                    final Set<ReplacementPair> replacements) {
        final String matchPatternXML = matchPatternTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
        final String replacementXML = replacementTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
        replacements.add(new ReplacementPair(matchPatternXML, replacementXML));
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
                                    final Set<ReplacementPair> replacements) {
        final String matchPatternXML = matchPatternTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
        final String replacementXML = replacementTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
        replacements.add(new ReplacementPair(matchPatternXML, replacementXML));
    }
```

## RuleId[ruleID=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfoComparator.java`
#### Snippet
```java
    }

    public int compare(ClassResourceInfo cr1, ClassResourceInfo cr2) {
        int result;
        if (rc != null) {
```

## RuleId[ruleID=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.cxf.metrics.MetricsProvider\[\]' found
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/MetricsFeature.java`
#### Snippet
```java
                    Collection<?> coll = b.getBeansOfType(MetricsProvider.class);
                    if (coll != null) {
                        providers = coll.toArray(new MetricsProvider[]{});
                    }
                }
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
                list.add(idltowsdlOptions[x].getIDL());
                try {
                    IDLToWSDL.run(list.toArray(new String[0]));
                    doneFile.delete();
                    doneFile.createNewFile();
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/JwtTokenUtils.java`
#### Snippet
```java
        if (scope != null) {
            String[] scopes = scope instanceof String
                ? scope.toString().split(" ") : CastUtils.cast((List<?>)scope).toArray(new String[]{});
            List<OAuthPermission> perms = new LinkedList<>();
            for (String s : scopes) {
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/JwtAccessTokenValidator.java`
#### Snippet
```java
        if (scope != null) {
            String[] scopes = scope instanceof String
                ? scope.toString().split(" ") : CastUtils.cast((List<?>)scope).toArray(new String[]{});
            List<OAuthPermission> perms = new LinkedList<>();
            for (String s : scopes) {
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'java.lang.String\[\]' found
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
            LOG.config("get Filter " + propFile + "::" + (String)i.next());
        }
        return filtersProps.keySet().toArray(new String[filtersProps.keySet().size()]);
    }

```

## RuleId[ruleID=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `AbstractXACMLAuthorizingInterceptor()` of an abstract class should not be declared 'public'
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/AbstractXACMLAuthorizingInterceptor.java`
#### Snippet
```java
    private XACMLRequestBuilder requestBuilder = new DefaultXACMLRequestBuilder();

    public AbstractXACMLAuthorizingInterceptor() {
        super(Phase.PRE_INVOKE);
        org.apache.wss4j.common.saml.OpenSAMLUtil.initSamlEngine();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHTTPDestination()` of an abstract class should not be declared 'public'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
     * @throws IOException
     */
    public AbstractHTTPDestination(Bus b,
                                   DestinationRegistry registry,
                                   EndpointInfo ei,
```

### NonProtectedConstructorInAbstractClass
Constructor `HTTPConduit()` of an abstract class should not be declared 'public'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
     * @throws IOException
     */
    public HTTPConduit(Bus b, EndpointInfo ei) throws IOException {
        this(b,
             ei,
```

### NonProtectedConstructorInAbstractClass
Constructor `HTTPConduit()` of an abstract class should not be declared 'public'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
     * @throws IOException
     */
    public HTTPConduit(Bus b,
                       EndpointInfo ei,
                       EndpointReferenceType t) throws IOException {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractXmlSecOutInterceptor()` of an abstract class should not be declared 'public'
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
    }

    public AbstractXmlSecOutInterceptor() {
        super(Phase.WRITE);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMetricsInterceptor()` of an abstract class should not be declared 'public'
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/AbstractMetricsInterceptor.java`
#### Snippet
```java
    private static final String REST_METRICS_MAP = AbstractMetricsInterceptor.class.getName() + ".METRICS_MAP";
    MetricsProvider[] providers;
    public AbstractMetricsInterceptor(String phase, MetricsProvider[] p) {
        super(phase);
        providers = p;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractThresholdOutputStream()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/io/AbstractThresholdOutputStream.java`
#### Snippet
```java
    protected LoadingByteArrayOutputStream buffer;

    public AbstractThresholdOutputStream(int threshold) {
        this.threshold = threshold;
        if (threshold >= 0) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractUserInfo()` of an abstract class should not be declared 'public'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
    public AbstractUserInfo() {
    }
    public AbstractUserInfo(JwtClaims claims) {
        this(claims.asMap());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractUserInfo()` of an abstract class should not be declared 'public'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
        this(claims.asMap());
    }
    public AbstractUserInfo(Map<String, Object> claims) {
        super(claims);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractUserInfo()` of an abstract class should not be declared 'public'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
    private static final long serialVersionUID = 4554501320190745304L;

    public AbstractUserInfo() {
    }
    public AbstractUserInfo(JwtClaims claims) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPhaseInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
     * @param phase the interceptor's phase
     */
    public AbstractPhaseInterceptor(String phase) {
        this(null, phase, false);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPhaseInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
     * @param uniqueId
     */
    public AbstractPhaseInterceptor(String i, String p, boolean uniqueId) {
        if (i == null) {
            i = getClass().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPhaseInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
     * @param p the interceptor's phase
     */
    public AbstractPhaseInterceptor(String i, String p) {
        this(i, p, false);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPhaseInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
     * @param uniqueId true to have a unique ID generated
     */
    public AbstractPhaseInterceptor(String phase, boolean uniqueId) {
        this(null, phase, uniqueId);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/binding/AbstractBindingFactory.java`
#### Snippet
```java
    }

    public AbstractBindingFactory(Bus b, Collection<String> ns) {
        activationNamespaces = ns;
        bus = b;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/binding/AbstractBindingFactory.java`
#### Snippet
```java
        activationNamespaces = ns;
    }
    public AbstractBindingFactory(Bus b) {
        bus = b;
        registerWithBindingManager();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/binding/AbstractBindingFactory.java`
#### Snippet
```java
    }

    public AbstractBindingFactory(Collection<String> ns) {
        activationNamespaces = ns;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/binding/AbstractBindingFactory.java`
#### Snippet
```java
    protected Bus bus;

    public AbstractBindingFactory() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractXSLTInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/feature/transform/AbstractXSLTInterceptor.java`
#### Snippet
```java
    private final Templates xsltTemplate;

    public AbstractXSLTInterceptor(String phase, Class<?> before, Class<?> after, String xsltPath) {
        super(phase);
        if (before != null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConduitSelector()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java


    public AbstractConduitSelector() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConduitSelector()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
     * @param c specific conduit
     */
    public AbstractConduitSelector(Conduit c) {
        if (c != null) {
            conduits.add(c);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMultiplexDestination()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractMultiplexDestination.java`
#### Snippet
```java
                                                              "id");

    public AbstractMultiplexDestination(Bus b, EndpointReferenceType ref, EndpointInfo ei) {
        super(b, ref, ei);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBackChannelConduit()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractDestination.java`
#### Snippet
```java
    protected abstract class AbstractBackChannelConduit extends AbstractConduit {

        public AbstractBackChannelConduit() {
            super(getAnonymousEndpointReference());
        }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDestination()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractDestination.java`
#### Snippet
```java
    protected final Bus bus;

    public AbstractDestination(EndpointReferenceType ref,
                               EndpointInfo ei) {
        this(null, ref, ei);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDestination()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractDestination.java`
#### Snippet
```java
    }

    public AbstractDestination(Bus b,
                               EndpointReferenceType ref,
                               EndpointInfo ei) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTransportFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractTransportFactory.java`
#### Snippet
```java
    public AbstractTransportFactory() {
    }
    public AbstractTransportFactory(List<String> ids) {
        transportIds = ids;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTransportFactory()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractTransportFactory.java`
#### Snippet
```java
    private List<String> transportIds;

    public AbstractTransportFactory() {
    }
    public AbstractTransportFactory(List<String> ids) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDOMStreamReader()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java
     * @param frame
     */
    public AbstractDOMStreamReader(ElementFrame<T, I> frame) {
        this.frame = frame;
        frames.push(this.frame);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractConduit()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/transport/AbstractConduit.java`
#### Snippet
```java
    protected final EndpointReferenceType target;

    public AbstractConduit(EndpointReferenceType t) {
        target = t;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractValidationInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/validation/AbstractValidationInterceptor.java`
#### Snippet
```java
    private volatile BeanValidationProvider provider;

    public AbstractValidationInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInterceptorProvidingDataBinding()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/databinding/AbstractInterceptorProvidingDataBinding.java`
#### Snippet
```java
        = new ModCountCopyOnWriteArrayList<>();

    public AbstractInterceptorProvidingDataBinding() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractFaultChainInitiatorObserver()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractFaultChainInitiatorObserver.java`
#### Snippet
```java
    private ClassLoader loader;

    public AbstractFaultChainInitiatorObserver(Bus bus) {
        this.bus = bus;
        if (bus != null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInDatabindingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractInDatabindingInterceptor.java`
#### Snippet
```java


    public AbstractInDatabindingInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractInDatabindingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractInDatabindingInterceptor.java`
#### Snippet
```java
        super(phase);
    }
    public AbstractInDatabindingInterceptor(String i, String phase) {
        super(i, phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOutDatabindingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractOutDatabindingInterceptor.java`
#### Snippet
```java
    public static final String OUT_BUFFERING = "org.apache.cxf.output.buffering";

    public AbstractOutDatabindingInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOutDatabindingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractOutDatabindingInterceptor.java`
#### Snippet
```java
        super(phase);
    }
    public AbstractOutDatabindingInterceptor(String id, String phase) {
        super(id, phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLoggingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
    private List<String> binaryContentMediaTypes = BINARY_CONTENT_MEDIA_TYPES;

    public AbstractLoggingInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLoggingInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
        super(phase);
    }
    public AbstractLoggingInterceptor(String id, String phase) {
        super(id, phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSecurityContextInInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractSecurityContextInInterceptor.java`
#### Snippet
```java
        LogUtils.getL7dLogger(AbstractSecurityContextInInterceptor.class);

    public AbstractSecurityContextInInterceptor() {
        super(Phase.PRE_INVOKE);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAuthorizingInInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractAuthorizingInInterceptor.java`
#### Snippet
```java
        this(true);
    }
    public AbstractAuthorizingInInterceptor(boolean uniqueId) {
        super(null, Phase.PRE_INVOKE, uniqueId);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractAuthorizingInInterceptor()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractAuthorizingInInterceptor.java`
#### Snippet
```java
    private boolean allowAnonymousUsers = true;

    public AbstractAuthorizingInInterceptor() {
        this(true);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPullPoint()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractPullPoint.java`
#### Snippet
```java
    protected AbstractCreatePullPoint createPullPoint;

    public AbstractPullPoint(String name) {
        super(name);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEndpoint()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
    protected W3CEndpointReference endpointEpr;

    public AbstractEndpoint(String name) {
        this.name = name;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCreatePullPoint()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractCreatePullPoint.java`
#### Snippet
```java
    private Map<String, AbstractPullPoint> pullPoints;

    public AbstractCreatePullPoint(String name) {
        super(name);
        idGenerator = new IdGenerator();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPublisher()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractPublisher.java`
#### Snippet
```java
    protected List<TopicExpressionType> topic;

    public AbstractPublisher(String name) {
        super(name);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBeanDefinitionParser()` of an abstract class should not be declared 'public'
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
    private Set<Class<?>> classes;

    public AbstractBeanDefinitionParser() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubscription()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractSubscription.java`
#### Snippet
```java
    protected AbstractNotificationBroker broker;

    public AbstractSubscription(String name) {
        super(name);
        try {
```

### NonProtectedConstructorInAbstractClass
Constructor `JmsSubscription()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsSubscription.java`
#### Snippet
```java
    private Thread terminationThread;

    public JmsSubscription(String name) {
        super(name);
        topicConverter = new JmsTopicExpressionConverter();
```

### NonProtectedConstructorInAbstractClass
Constructor `JmsPublisher()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPublisher.java`
#### Snippet
```java
    }
    
    public JmsPublisher(String name, String notificationTopicName) {
        super(name);
        topicConverter = new JmsTopicExpressionConverter();
```

### NonProtectedConstructorInAbstractClass
Constructor `JmsPublisher()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPublisher.java`
#### Snippet
```java
    private final String notificationTopicName;

    public JmsPublisher(String name) {
        this(name, "activemq.notifications");
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `JmsNotificationBroker()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsNotificationBroker.java`
#### Snippet
```java
    }

    public JmsNotificationBroker(String name, ConnectionFactory connectionFactory) {
        super(name);
        this.connectionFactory = connectionFactory;
```

### NonProtectedConstructorInAbstractClass
Constructor `JmsNotificationBroker()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsNotificationBroker.java`
#### Snippet
```java
    private Connection connection;

    public JmsNotificationBroker(String name) {
        super(name);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractNotificationBroker()` of an abstract class should not be declared 'public'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractNotificationBroker.java`
#### Snippet
```java
    private Map<String, AbstractSubscription> subscriptions;

    public AbstractNotificationBroker(String name) {
        super(name);
        idGenerator = new IdGenerator();
```

### NonProtectedConstructorInAbstractClass
Constructor `WSTransferFault()` of an abstract class should not be declared 'public'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/WSTransferFault.java`
#### Snippet
```java
    private static final long serialVersionUID = -7603229392321418734L;

    public WSTransferFault(String reason, String detail, QName faultCode) {
        super(reason, faultCode);
        if (detail != null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `FragmentFault()` of an abstract class should not be declared 'public'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/faults/FragmentFault.java`
#### Snippet
```java
    private static final long serialVersionUID = 2111286624025926462L;

    public FragmentFault(String reason, String detail, QName faultCode) {
        super(reason, faultCode);
        if (detail != null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractUsernameTokenAuthenticatingInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractUsernameTokenAuthenticatingInterceptor.java`
#### Snippet
```java
    private boolean supportDigestPasswords;

    public AbstractUsernameTokenAuthenticatingInterceptor() {
        this(new HashMap<String, Object>());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractUsernameTokenAuthenticatingInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractUsernameTokenAuthenticatingInterceptor.java`
#### Snippet
```java
    }

    public AbstractUsernameTokenAuthenticatingInterceptor(Map<String, Object> properties) {
        super(properties);
        getAfter().add(PolicyBasedWSS4JInInterceptor.class.getName());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSS4JInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JInterceptor.java`
#### Snippet
```java
    private String id;

    public AbstractWSS4JInterceptor() {
        super();
        id = getClass().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTokenInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
        Collections.singleton(new QName(WSConstants.WSSE_NS, "Security"));

    public AbstractTokenInterceptor() {
        super(Phase.PRE_PROTOCOL);
        addAfter(PolicyBasedWSS4JOutInterceptor.class.getName());
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSS4JStaxInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
    }

    public AbstractWSS4JStaxInterceptor(Map<String, Object> properties) {
        super();
        id = getClass().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSS4JStaxInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
    private String id;

    public AbstractWSS4JStaxInterceptor(WSSSecurityProperties securityProperties) {
        super();
        id = getClass().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSS4JStaxInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
    }

    public AbstractWSS4JStaxInterceptor() {
        super();
        id = getClass().getName();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCommonBindingHandler()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractCommonBindingHandler.java`
#### Snippet
```java
    protected final SoapMessage message;

    public AbstractCommonBindingHandler(
        SoapMessage msg
    ) {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSTSClient()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
    protected TLSClientParameters tlsClientParameters;

    public AbstractSTSClient(Bus b) {
        bus = b;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractStaxBindingHandler()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
    private AbstractBinding binding;

    public AbstractStaxBindingHandler(
        WSSSecurityProperties properties,
        SoapMessage msg,
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingBuilder()` of an abstract class should not be declared 'public'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    private List<SupportingToken> sgndEndSuppTokList;

    public AbstractBindingBuilder(
                           WSSConfig config,
                           AbstractBinding binding,
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMessageListenerContainer()` of an abstract class should not be declared 'public'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/AbstractMessageListenerContainer.java`
#### Snippet
```java
    private boolean internalExecutor;

    public AbstractMessageListenerContainer() {
        super();
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBraveClientProvider()` of an abstract class should not be declared 'public'
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientProvider.java`
#### Snippet
```java
    private final HttpTracing brave;

    public AbstractBraveClientProvider(final HttpTracing brave) {
        this.brave = brave;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractResponseContextImpl()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AbstractResponseContextImpl.java`
#### Snippet
```java
    protected Message m;
    protected ResponseImpl r;
    public AbstractResponseContextImpl(ResponseImpl r, Message m) {
        this.m = m;
        this.r = r;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPropertiesImpl()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AbstractPropertiesImpl.java`
#### Snippet
```java
    protected Message m;
    private PropertyHolder holder;
    public AbstractPropertiesImpl(Message message) {
        holder = PropertyHolderFactory.getPropertyHolder(message);
        this.m = message;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractRequestContextImpl()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AbstractRequestContextImpl.java`
#### Snippet
```java
    protected HttpHeaders h;
    private boolean responseContext;
    public AbstractRequestContextImpl(Message message, boolean responseContext) {
        super(message);
        this.h = new HttpHeadersImpl(message);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJAXWSMethodInvoker()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
        "org.apache.cxf.ws.addressing.partial.response.sent";

    public AbstractJAXWSMethodInvoker(final Object bean) {
        super(new SingletonFactory(bean));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJAXWSMethodInvoker()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
    }

    public AbstractJAXWSMethodInvoker(Factory factory) {
        super(factory);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBindingImpl()` of an abstract class should not be declared 'public'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/binding/AbstractBindingImpl.java`
#### Snippet
```java
    private final JaxWsEndpointImpl endpoint;

    public AbstractBindingImpl(JaxWsEndpointImpl imp) {
        endpoint = imp;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractIdentityCache()` of an abstract class should not be declared 'public'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/AbstractIdentityCache.java`
#### Snippet
```java
    private MemoryIdentityCacheStatistics statistics;

    public AbstractIdentityCache(IdentityMapper identityMapper) {
        this(null, identityMapper);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractIdentityCache()` of an abstract class should not be declared 'public'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/AbstractIdentityCache.java`
#### Snippet
```java
    }

    public AbstractIdentityCache(Bus bus, IdentityMapper identityMapper) {
        this.identityMapper = identityMapper;
        this.bus = bus;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSTSEvent()` of an abstract class should not be declared 'public'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/AbstractSTSEvent.java`
#### Snippet
```java
    private final Object source;

    public AbstractSTSEvent(Object source, long duration) {
        this.duration = duration;
        this.timestamp = System.currentTimeMillis();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSTSFailureEvent()` of an abstract class should not be declared 'public'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/AbstractSTSFailureEvent.java`
#### Snippet
```java
    private Exception exception;

    public AbstractSTSFailureEvent(Object source, long duration, Exception ex) {
        super(source, duration);
        exception = ex;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSTSSuccessEvent()` of an abstract class should not be declared 'public'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/AbstractSTSSuccessEvent.java`
#### Snippet
```java
public abstract class AbstractSTSSuccessEvent extends AbstractSTSEvent {

    public AbstractSTSSuccessEvent(Object source, long duration) {
        super(source, duration);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSDLBindingFactory()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/binding/AbstractWSDLBindingFactory.java`
#### Snippet
```java
    }

    public AbstractWSDLBindingFactory(Bus b) {
        super(b);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSDLBindingFactory()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/binding/AbstractWSDLBindingFactory.java`
#### Snippet
```java
    }

    public AbstractWSDLBindingFactory(Collection<String> ns) {
        super(ns);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSDLBindingFactory()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/binding/AbstractWSDLBindingFactory.java`
#### Snippet
```java
                      implements WSDLBindingFactory {

    public AbstractWSDLBindingFactory() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWSDLBindingFactory()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/binding/AbstractWSDLBindingFactory.java`
#### Snippet
```java
    }

    public AbstractWSDLBindingFactory(Bus b, Collection<String> ns) {
        super(b, ns);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEndpointSelectionInterceptor()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/AbstractEndpointSelectionInterceptor.java`
#### Snippet
```java
        super(phase);
    }
    public AbstractEndpointSelectionInterceptor(String id, String phase) {
        super(id, phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEndpointSelectionInterceptor()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/AbstractEndpointSelectionInterceptor.java`
#### Snippet
```java
public abstract class AbstractEndpointSelectionInterceptor extends AbstractPhaseInterceptor<Message> {

    public AbstractEndpointSelectionInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractWrapperWSDLLocator()` of an abstract class should not be declared 'public'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/AbstractWrapperWSDLLocator.java`
#### Snippet
```java
    boolean fromParent;

    public AbstractWrapperWSDLLocator(String wsdlUrl,
                                      WSDLLocator parent) {
        this.wsdlUrl = wsdlUrl;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCorbaTypeListener()` of an abstract class should not be declared 'public'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/AbstractCorbaTypeListener.java`
#### Snippet
```java


    public AbstractCorbaTypeListener(CorbaObjectHandler h) {
        handler = h;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractManagedConnectionImpl()` of an abstract class should not be declared 'public'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionImpl.java`
#### Snippet
```java
    private final AbstractManagedConnectionFactoryImpl managedConnectionFactory;

    public AbstractManagedConnectionImpl(AbstractManagedConnectionFactoryImpl managedFactory,
        ConnectionRequestInfo crInfo,
        Subject sj) throws ResourceException {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractManagedConnectionFactoryImpl()` of an abstract class should not be declared 'public'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
    private PrintWriter printWriter;

    public AbstractManagedConnectionFactoryImpl() {
        super();
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractManagedConnectionFactoryImpl()` of an abstract class should not be declared 'public'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
    }

    public AbstractManagedConnectionFactoryImpl(Properties props) {
        super(props);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSoapInterceptor()` of an abstract class should not be declared 'public'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/AbstractSoapInterceptor.java`
#### Snippet
```java
        super(p);
    }
    public AbstractSoapInterceptor(String i, String p) {
        super(i, p);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSoapInterceptor()` of an abstract class should not be declared 'public'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/AbstractSoapInterceptor.java`
#### Snippet
```java
    implements SoapInterceptor {

    public AbstractSoapInterceptor(String p) {
        super(p);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPolicyInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AbstractPolicyInterceptor.java`
#### Snippet
```java
public abstract class AbstractPolicyInterceptor extends AbstractPhaseInterceptor<Message> {

    public AbstractPolicyInterceptor(String phase) {
        super(phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPolicyInterceptor()` of an abstract class should not be declared 'public'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AbstractPolicyInterceptor.java`
#### Snippet
```java
        super(phase);
    }
    public AbstractPolicyInterceptor(String id, String phase) {
        super(id, phase);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPolicyInterceptorProvider()` of an abstract class should not be declared 'public'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AbstractPolicyInterceptorProvider.java`
#### Snippet
```java
    private Collection<QName> assertionTypes;

    public AbstractPolicyInterceptorProvider(QName type) {
        this(Collections.singletonList(type));
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractPolicyInterceptorProvider()` of an abstract class should not be declared 'public'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AbstractPolicyInterceptorProvider.java`
#### Snippet
```java
    }

    public AbstractPolicyInterceptorProvider(Collection<QName> at) {
        assertionTypes = at;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractLoggingInterceptor()` of an abstract class should not be declared 'public'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
    protected Set<String> sensitiveProtocolHeaderNames = new HashSet<>();

    public AbstractLoggingInterceptor(String phase, LogEventSender sender) {
        super(phase);
        this.sender = sender;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractContentEncryptionCipherProperties()` of an abstract class should not be declared 'public'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractContentEncryptionCipherProperties.java`
#### Snippet
```java
    private final ContentAlgorithm algo;

    public AbstractContentEncryptionCipherProperties(ContentAlgorithm algo) {
        this.algo = algo;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `JoseHeaders()` of an abstract class should not be declared 'public'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    }

    public JoseHeaders(Map<String, Object> values) {
        super(values);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `JoseHeaders()` of an abstract class should not be declared 'public'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    }

    public JoseHeaders(JoseHeaders headers) {
        this(headers.asMap());
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `JoseHeaders()` of an abstract class should not be declared 'public'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    }

    public JoseHeaders(JoseType type) {
        init(type);
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `JoseHeaders()` of an abstract class should not be declared 'public'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    private static final long serialVersionUID = 1101185302425283553L;

    public JoseHeaders() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractOpenTracingClientProvider()` of an abstract class should not be declared 'public'
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingClientProvider.java`
#### Snippet
```java
    private final Tracer tracer;

    public AbstractOpenTracingClientProvider(final Tracer tracer) {
        this.tracer = tracer;
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMessageReader()` of an abstract class should not be declared 'public'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java
    private static final QName XSI_NIL = new QName(Constants.URI_2001_SCHEMA_XSI, "nil", "xsi");

    public AbstractMessageReader() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractMessageWriter()` of an abstract class should not be declared 'public'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java
    private boolean xsiTypeWritten;

    public AbstractMessageWriter() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AegisType()` of an abstract class should not be declared 'public'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AegisType.java`
#### Snippet
```java
    private boolean flatArray;

    public AegisType() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractXOPType()` of an abstract class should not be declared 'public'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AbstractXOPType.java`
#### Snippet
```java
     * @param expectedContentTypes whether to public xmime:expectedContentTypes.
     */
    public AbstractXOPType(boolean useXmimeBinaryType, String expectedContentTypes) {
        this.expectedContentTypes = expectedContentTypes;
        this.useXmimeBinaryType = useXmimeBinaryType;
```

### NonProtectedConstructorInAbstractClass
Constructor `WSEventingFault()` of an abstract class should not be declared 'public'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/WSEventingFault.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    public WSEventingFault(String reason, Element detail, QName faultCode) {
        super(reason, faultCode);
        if (detail != null) {
```

### NonProtectedConstructorInAbstractClass
Constructor `NotificatorService()` of an abstract class should not be declared 'public'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/NotificatorService.java`
#### Snippet
```java
    protected ExecutorService service;

    public NotificatorService() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractEventSource()` of an abstract class should not be declared 'public'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/eventsource/AbstractEventSource.java`
#### Snippet
```java
    protected WebServiceContext context;

    public AbstractEventSource() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractSubscriptionManager()` of an abstract class should not be declared 'public'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/manager/AbstractSubscriptionManager.java`
#### Snippet
```java
    protected WebServiceContext context;

    public AbstractSubscriptionManager() {
    }

```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractCodegenMojo()` of an abstract class should not be declared 'public'
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java


    public AbstractCodegenMojo() {
        super();
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractBodyWriter()` of an abstract class should not be declared 'public'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
    protected abstract static class AbstractBodyWriter extends AbstractOutDatabindingInterceptor {

        public AbstractBodyWriter() {
            super(Phase.WRITE);
        }
```

## RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            }
            throw ex;
        } catch (RuntimeException ex) {
            throw ex;
        } finally {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
                try {
                    subjectDNPatterns.add(Pattern.compile(constraint.trim()));
                } catch (PatternSyntaxException ex) {
                    throw ex;
                }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
                try {
                    subjectDNPatterns.add(Pattern.compile(constraint.trim()));
                } catch (PatternSyntaxException ex) {
                    throw ex;
                }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/Beanspector.java`
#### Snippet
```java
        } catch (InvocationTargetException e) {
            throw e.getCause();
        } catch (Exception e) {
            throw e;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/Beanspector.java`
#### Snippet
```java
                                       paramType.getName(), value.getClass().getName());
            throw new IllegalArgumentException(e.getMessage() + msg);
        } catch (Exception e) {
            throw e;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/WriterInterceptorContextImpl.java`
#### Snippet
```java
        try {
            next.aroundWriteTo(this);
        } catch (IOException ex) {
            throw ex;
        }
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
            try {
                return unmarshalFromReader(unmarshaller, reader, anns, mt);
            } catch (JAXBException e) {
                throw e;
            } finally {
```

### CaughtExceptionImmediatelyRethrown
Caught exception `ex` is immediately rethrown
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/JAXWSHttpSpiDestination.java`
#### Snippet
```java
            }
            throw ex;
        } catch (RuntimeException ex) {
            throw ex;
        } finally {
```

## RuleId[ruleID=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `cxfBus`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/cache/EHCacheXKMSClientCache.java`
#### Snippet
```java

        if (cxfBus == null) {
            cxfBus = BusFactory.getThreadDefaultBus(true);
        }
        if (cxfBus != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `schemaDefaultValue`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/ParticleInfo.java`
#### Snippet
```java
            }
            builder.append('\'');
            schemaDefaultValue = builder.toString();
        }
        return schemaDefaultValue;
```

### AssignmentToMethodParameter
Assignment to method parameter `out`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
        try {
            if (out instanceof HttpOutput) {
                out = new JettyOutputStream((HttpOutput)out);
            }
        } catch (Throwable t) {
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
                             HttpServletResponse resp) throws IOException {
        if (context == null) {
            context = servletContext;
        }
        Request baseRequest = (req instanceof Request)
```

### AssignmentToMethodParameter
Assignment to method parameter `doc`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java

            InTransformReader reader = new InTransformReader(domReader, inMap, null, false);
            doc = StaxUtils.read(reader);
            return new DOMSource(doc);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `doc`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                      "{" + WSDVersion.INSTANCE_1_1.getAddressingNamespace() + "}*");
            InTransformReader reader = new InTransformReader(domReader, inMap, null, false);
            doc = StaxUtils.read(reader);
            //System.out.println(StaxUtils.toString(doc));

```

### AssignmentToMethodParameter
Assignment to method parameter `element`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
            XmlSchemaElement nextElement = element.getRef().getTarget();
            assert nextElement != null;
            element = nextElement;
        }
        if (element.getSchemaType() == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java

        if (path.startsWith(lh)) {
            path = path.substring(lh.length());
        } else if (path.startsWith(lhs)) {
            path = path.substring(lhs.length());
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
            path = path.substring(lh.length());
        } else if (path.startsWith(lhs)) {
            path = path.substring(lhs.length());
        }
        if (!path.contains("://") && !path.startsWith("/")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        }
        if (!path.contains("://") && !path.startsWith("/")) {
            path = "/" + path;

        }
```

### AssignmentToMethodParameter
Assignment to method parameter `timeout`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Servlet3ContinuationProvider.java`
#### Snippet
```java
            if (isPending && timeout != 0) {
                long currentTimeout = context.getTimeout();
                timeout = currentTimeout + timeout;
            } else {
                isPending = true;
```

### AssignmentToMethodParameter
Assignment to method parameter `chunkThreshold`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java

        if (isChunking && chunkThreshold <= 0) {
            chunkThreshold = 0;
            connection.setChunkedStreamingMode(-1);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `registry`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
        super(transportIds);
        if (registry == null) {
            registry = new DestinationRegistryImpl();
        }
        this.registry = registry;
```

### AssignmentToMethodParameter
Assignment to method parameter `tlsClientParameters`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionFactory.java`
#### Snippet
```java

            if (tlsClientParameters == null) {
                tlsClientParameters = new TLSClientParameters();
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `location`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java
        try {
            if (r != null) {
                location = r.getURL().toExternalForm();
            }
        } catch (IOException e) {
```

### AssignmentToMethodParameter
Assignment to method parameter `specPath`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
                relPath += "/";
            }
            specPath = "api-docs?url=" + relPath + specPath;
        }
        if (!absoluteURL.endsWith("/")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `specPath`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
        }
        if (!absoluteURL.endsWith("/")) {
            specPath = "/" + specPath;
        }
        specPath = StringEscapeUtils.escapeHtml4(specPath);
```

### AssignmentToMethodParameter
Assignment to method parameter `specPath`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
            specPath = "/" + specPath;
        }
        specPath = StringEscapeUtils.escapeHtml4(specPath);
        writer.write("<br/><span class=\"field\">" + specName + " :</span> " + "<a href=\"" + absoluteURL
                 + specPath + "\">" + absoluteURL + specPath + "</a>");
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointAddress`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
            for (Map.Entry<String, String> entry : extMap.entrySet()) {
                if (endpointAddress.endsWith(entry.getKey())) {
                    endpointAddress = endpointAddress.substring(0, endpointAddress.length()
                                                                   - entry.getKey().length());
                    endpointAddress += entry.getValue();
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointAddress`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
                    endpointAddress = endpointAddress.substring(0, endpointAddress.length()
                                                                   - entry.getKey().length());
                    endpointAddress += entry.getValue();
                    return endpointAddress;
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `request`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
            return;
        }
        request = checkXForwardedHeaders(request);
        invoke(request, response);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `sequence`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
    protected static Map<String, String> parseMapSequence(String sequence) {
        if (sequence != null) {
            sequence = sequence.trim();
            Map<String, String> map = new HashMap<>();
            for (String pair : sequence.split("\\s")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
                                                                   Comparator<ProviderInfo<?>> comparator) {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus();
        }
        MicroProfileClientProviderFactory factory = new MicroProfileClientProviderFactory(bus, comparator);
```

### AssignmentToMethodParameter
Assignment to method parameter `methods`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/Validator.java`
#### Snippet
```java
                        try {
                            Class<?> computeClass = ClassLoaderUtils.loadClass(className, userType);
                            methods = Arrays.stream(computeClass.getDeclaredMethods())
                                                                .filter(m -> {
                                                                    int i = m.getModifiers();
```

### AssignmentToMethodParameter
Assignment to method parameter `location`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/RestClientBean.java`
#### Snippet
```java
    InputStream getInputStream(String location) {
        if (location.startsWith("classpath:")) {
            location = location.substring(10); // 10 == "classpath:".length()
            //TODO: replace getResources lookup with getResourceAsStream once 
            // https://github.com/arquillian/arquillian-container-weld/issues/59 is resolved
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSigOutInterceptor.java`
#### Snippet
```java
    public void setEnvelopeQName(QName name) {
        if (name.getPrefix().length() == 0) {
            name = new QName(name.getNamespaceURI(), name.getLocalPart(), DEFAULT_ENV_PREFIX);
        }
        this.envelopeQName = name;
```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlEncOutInterceptor.java`
#### Snippet
```java
        if (!(algo.startsWith(EncryptionConstants.EncryptionSpecNS)
            || algo.startsWith(EncryptionConstants.EncryptionSpec11NS))) {
            algo = EncryptionConstants.EncryptionSpecNS + algo;
        }
        encProps.setEncryptionSymmetricKeyAlgo(algo);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
    public void setEnvelopeQName(QName name) {
        if (name.getPrefix().length() == 0) {
            name = new QName(name.getNamespaceURI(), name.getLocalPart(), DEFAULT_ENV_PREFIX);
        }
        this.envelopeQName = name;
```

### AssignmentToMethodParameter
Assignment to method parameter `userName`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/RSSecurityUtils.java`
#### Snippet
```java
        if (crypto != null && StringUtils.isEmpty(userName)) {
            try {
                userName = crypto.getDefaultX509Identifier();
            } catch (WSSecurityException e1) {
                throw new Fault(e1);
```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
        if (!(algo.startsWith(EncryptionConstants.EncryptionSpecNS)
            || algo.startsWith(EncryptionConstants.EncryptionSpec11NS))) {
            algo = EncryptionConstants.EncryptionSpecNS + algo;
        }
        encryptionProperties.setEncryptionSymmetricKeyAlgo(algo);
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/EHCacheTokenReplayCache.java`
#### Snippet
```java
            throws IllegalAccessException, ClassNotFoundException, InstantiationException {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus(true);
        }
        URL configFileURL = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/state/EHCacheSPStateManager.java`
#### Snippet
```java
            throws IllegalAccessException, ClassNotFoundException, InstantiationException {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus(true);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/codahale/CodahaleMetricsProvider.java`
#### Snippet
```java
        // This can be replaced if really needed with pattern-based matching
        if (value.lastIndexOf(QUESTION_MARK) != -1) {
            value = value.replace(QUESTION_MARK, ESCAPED_QUESTION_MARK);
        }
        return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointAddress`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
                boolean startsWithSlash = pathInfo.startsWith(slash);
                if (endpointAddress.endsWith(slash)) {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo.substring(1) : pathInfo);
                } else {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo : (slash + pathInfo));
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointAddress`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo.substring(1) : pathInfo);
                } else {
                    endpointAddress = endpointAddress + (startsWithSlash ? pathInfo : (slash + pathInfo));
                }
                message.put(Message.ENDPOINT_ADDRESS, endpointAddress);
```

### AssignmentToMethodParameter
Assignment to method parameter `boi`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/AbstractMetricsInterceptor.java`
#### Snippet
```java
        } else {
            if (boi.isUnwrapped()) {
                boi = boi.getWrappedOperation();
            }
            metrics = boi.getProperty(MetricsContext.class.getName());
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPDestination.java`
#### Snippet
```java
                             HttpServletResponse resp) throws IOException {
        if (context == null) {
            context = servletContext;
        }
        HTTPServerPolicy sp = getServer();
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        if (this.tlsServerParameters != null && this.tlsServerParameters.getClientAuthentication() != null
            && this.tlsServerParameters.getClientAuthentication().isRequired()) {
            builder = builder.setSocketOption(Options.SSL_CLIENT_AUTH_MODE, SslClientAuthMode.REQUIRED);
        }
        if (this.tlsServerParameters != null && this.tlsServerParameters.getClientAuthentication() != null
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            && this.tlsServerParameters.getClientAuthentication().isWant()
            && !this.tlsServerParameters.getClientAuthentication().isRequired()) {
            builder = builder.setSocketOption(Options.SSL_CLIENT_AUTH_MODE, SslClientAuthMode.REQUESTED);
        }
        return builder;
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        if (this.threadingParameters != null) {
            if (this.threadingParameters.isWorkerIOThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_IO_THREADS,
                              this.threadingParameters.getWorkerIOThreads());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isMinThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_TASK_CORE_THREADS,
                              this.threadingParameters.getMinThreads());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isMaxThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_TASK_MAX_THREADS,
                              this.threadingParameters.getMaxThreads());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `builder`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isWorkerIONameSet()) {
                builder = builder.setWorkerOption(Options.WORKER_NAME,
                              this.threadingParameters.getWorkerIOName());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `loader`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/handlers/CxfUndertowLogHandler.java`
#### Snippet
```java
        try {
            if (loader == null) {
                loader = Undertow.class.getClassLoader();
            }
            Xnio xnio = Xnio.getInstance(loader);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
        if (index != -1) {
            String[] names = name.split("\\.");
            name = name.substring(index + 1);
            if (value != null && !InjectionUtils.isPrimitive(value.getClass())) {
                try {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
                    String nextPart = StringUtils.capitalize(names[1]);
                    Method m = value.getClass().getMethod("get" + nextPart, new Class[]{});
                    value = m.invoke(value, new Object[]{});
                } catch (Throwable ex) {
                    throw new RuntimeException();
```

### AssignmentToMethodParameter
Assignment to method parameter `rval`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
        if (rval.charAt(0) == '*') {
            starts = true;
            rval = rval.substring(1);
            if (rval.isEmpty()) {
                throw new SearchParseException("A single wildcard is not a valid search condition");
```

### AssignmentToMethodParameter
Assignment to method parameter `rval`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
        if (rval.charAt(rval.length() - 1) == '*') {
            ends = true;
            rval = rval.substring(0, rval.length() - 1);
        }
        if (starts || ends) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
        return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String toSqlWildcardString(String value, boolean alwaysWildcard) {
        if (value.contains("\\")) {
            value = value.replaceAll("\\\\", "\\\\\\\\");
        }
        if (value.contains("_") && escapeUnderscoreChar()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        }
        if (value.contains("_") && escapeUnderscoreChar()) {
            value = value.replaceAll("_", "\\\\_");
        }
        if (value.contains("%")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        }
        if (value.contains("%")) {
            value = value.replaceAll("%", "\\\\%");
        }
        if (!containsWildcard(value)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
            return alwaysWildcard ? "%" + value + "%" : value;
        }
        value = value.replaceAll("\\*", "%");
        return value;
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java

    public static Date dateFromStringWithDefaultFormats(String value) {
        value = value.replaceAll("Z$", "+0000");
        Date date = timestampFromString(new SimpleDateFormat(TIMESTAMP_WITH_TIMEZONE_Z), value);

```

### AssignmentToMethodParameter
Assignment to method parameter `lastCastedValue`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java

            if (lastTry) {
                lastCastedValue = lastCastedValue == null ? valueObject : lastCastedValue;
                return isCollection ? getCollectionSingleton(valueType, lastCastedValue) : lastCastedValue;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `lastCastedValue`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
                return isCollection ? getCollectionSingleton(valueType, lastCastedValue) : lastCastedValue;
            }
            lastCastedValue = valueObject;

            TypeInfo nextTypeInfo = new TypeInfo(valueObjectValue.getClass(), getterM.getGenericReturnType());
```

### AssignmentToMethodParameter
Assignment to method parameter `documentMetadata`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaLuceneContentExtractor.java`
#### Snippet
```java

        if (documentMetadata == null) {
            documentMetadata = defaultDocumentMetadata;
        }
        if (content.getContent() != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java

            if (context == null) {
                context = new ParseContext();
            }
            if (context.get(Parser.class) == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `handler`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
                // not ready to accept null handlers so lets retry with IgnoreContentHandler.
                if (handler == null) {
                    handler = new IgnoreContentHandler();
                    parser.parse(in, handler, metadata, context);
                } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
        if (index != -1) {
            String[] names = name.split("\\.");
            name = name.substring(index + 1);
            if (value != null && !InjectionUtils.isPrimitive(actualCls)) {
                try {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                    Method m = actualCls.getMethod("get" + nextPart, new Class[]{});
                    if (isCollection) {
                        value = ((Collection)value).iterator().next();
                        set.add(names[0]);
                    }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                        set.add(names[0]);
                    }
                    value = m.invoke(value, new Object[]{});
                    valueCls = value.getClass();
                    type = m.getGenericReturnType();
```

### AssignmentToMethodParameter
Assignment to method parameter `valueCls`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                    }
                    value = m.invoke(value, new Object[]{});
                    valueCls = value.getClass();
                    type = m.getGenericReturnType();
                } catch (Throwable ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                    value = m.invoke(value, new Object[]{});
                    valueCls = value.getClass();
                    type = m.getGenericReturnType();
                } catch (Throwable ex) {
                    throw new RuntimeException(ex);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
            set.add(name);
            Collection coll = (Collection)value;
            value = coll.isEmpty() ? null : coll.iterator().next();
            valueCls = actualCls;
            if (ps instanceof CollectionCheckStatement) {
```

### AssignmentToMethodParameter
Assignment to method parameter `valueCls`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
            Collection coll = (Collection)value;
            value = coll.isEmpty() ? null : coll.iterator().next();
            valueCls = actualCls;
            if (ps instanceof CollectionCheckStatement) {
                collInfo = ((CollectionCheckStatement)ps).getCollectionCheckInfo();
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/fiql/FiqlParser.java`
#### Snippet
```java
        TypeInfo typeInfo = getTypeInfo(setter, value);
        if (isDecodeQueryValues()) {
            value = UrlUtils.urlDecode(value);
        }
        Object object = parseType(originalName, null, null, setter, typeInfo, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java

    private Query buildSimpleQuery(ConditionType ct, String name, Object value) {
        name = super.getRealPropertyName(name);
        validatePropertyValue(name, value);

```

### AssignmentToMethodParameter
Assignment to method parameter `bytes`
in `rt/security/src/main/java/org/apache/cxf/rt/security/crypto/CryptoUtils.java`
#### Snippet
```java
        boolean compressionSupported = keyProps != null && keyProps.isCompressionSupported();
        if (compressionSupported && mode == Cipher.ENCRYPT_MODE) {
            bytes = CompressionUtils.deflate(bytes, true);
        }
        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/src/main/java/org/apache/cxf/io/ReaderInputStream.java`
#### Snippet
```java
                int c = Math.min(encoderOut.remaining(), len);
                encoderOut.get(b, off, c);
                off += c;
                len -= c;
                read += c;
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `core/src/main/java/org/apache/cxf/io/ReaderInputStream.java`
#### Snippet
```java
                encoderOut.get(b, off, c);
                off += c;
                len -= c;
                read += c;
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `core/src/main/java/org/apache/cxf/io/AbstractThresholdOutputStream.java`
#### Snippet
```java
            }
            buffer.write(b, off, space);
            len -= space;
            off += space;

```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/src/main/java/org/apache/cxf/io/AbstractThresholdOutputStream.java`
#### Snippet
```java
            buffer.write(b, off, space);
            len -= space;
            off += space;

            if (buffer.size() >= threshold) {
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java

            if (classLoader == null) {
                classLoader = Thread.currentThread().getContextClassLoader();
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java

                if (classLoader == null) {
                    classLoader = Thread.currentThread().getContextClassLoader();
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `className`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
        if (className == null) {
            ClassLoader loader = Thread.currentThread().getContextClassLoader();
            className = getBusFactoryClass(loader);
            if (className == null && loader != BusFactory.class.getClassLoader()) {
                className = getBusFactoryClass(BusFactory.class.getClassLoader());
```

### AssignmentToMethodParameter
Assignment to method parameter `className`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
            className = getBusFactoryClass(loader);
            if (className == null && loader != BusFactory.class.getClassLoader()) {
                className = getBusFactoryClass(BusFactory.class.getClassLoader());
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `className`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
        }
        if (className == null) {
            className = BusFactory.DEFAULT_BUS_FACTORY;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `l`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
        if (l == -1) {
            String s = SystemPropertyAction.getProperty(CachedConstants.MAX_SIZE_SYS_PROP, "-1");
            l = Long.parseLong(s);
        }
        defaultMaxSize = l;
```

### AssignmentToMethodParameter
Assignment to method parameter `out`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
    public void resetOut(OutputStream out, boolean copyOldContent) throws IOException {
        if (out == null) {
            out = new LoadingByteArrayOutputStream();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
    public static void setDefaultThreshold(int i) {
        if (i == -1) {
            i = SystemPropertyAction.getInteger(CachedConstants.THRESHOLD_SYS_PROP, -1);
            if (i <= 0) {
                i = 128 * 1024;
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
            i = SystemPropertyAction.getInteger(CachedConstants.THRESHOLD_SYS_PROP, -1);
            if (i <= 0) {
                i = 128 * 1024;
            } else {
                thresholdSysPropSet = true;
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
    public static void setDefaultCipherTransformation(String n) {
        if (n == null) {
            n = SystemPropertyAction.getPropertyOrNull(CachedConstants.CIPHER_TRANSFORMATION_SYS_PROP);
        }
        defaultCipherTransformation = n;
```

### AssignmentToMethodParameter
Assignment to method parameter `out`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
    public void resetOut(Writer out, boolean copyOldContent) throws IOException {
        if (out == null) {
            out = new LoadingCharArrayWriter();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `l`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
                s = System.getProperty("org.apache.cxf.io.CachedWriter.MaxSize", "-1");
            }
            l = Long.parseLong(s);
        }
        defaultMaxSize = l;
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
    public static void setDefaultCipherTransformation(String n) {
        if (n == null) {
            n = SystemPropertyAction.getPropertyOrNull(CachedConstants.CIPHER_TRANSFORMATION_SYS_PROP);
        }
        defaultCipherTransformation = n;
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
    public static void setDefaultThreshold(int i) {
        if (i == -1) {
            i = SystemPropertyAction.getInteger(CachedConstants.THRESHOLD_SYS_PROP, -1);
            if (i == -1) {
                // lookup the deprecated property
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
            if (i == -1) {
                // lookup the deprecated property
                i = SystemPropertyAction.getInteger("org.apache.cxf.io.CachedWriter.Threshold", -1);
            }
            if (i <= 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
            }
            if (i <= 0) {
                i = 64 * 1024;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `conduit`
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
        if (conduit == null) {
            Exchange exchange = message.getExchange();
            conduit = exchange != null ? exchange.getConduit(message) : null;
        }
        return conduit;
```

### AssignmentToMethodParameter
Assignment to method parameter `line`
in `core/src/main/java/org/apache/cxf/bus/extension/TextExtensionFragmentParser.java`
#### Snippet
```java

    private Extension getExtensionFromTextLine(String line) {
        line = line.trim();
        if (line.isEmpty() || line.charAt(0) == '#') {
            return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `namespaceURI`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
    private static String getNameSpaceUri(Node node, String content, String namespaceURI) {
        if (namespaceURI == null) {
            namespaceURI = node.lookupNamespaceURI(content.substring(0,
                                                                  content.indexOf(':')));
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `systemId`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                //REVIST - we need to get catalogs in here somehow  :-(
                if (systemId == null) {
                    systemId = publicId;
                }
                if (systemId != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
    public AbstractPhaseInterceptor(String i, String p, boolean uniqueId) {
        if (i == null) {
            i = getClass().getName();
        }
        if (uniqueId) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java
        }
        if (uniqueId) {
            i += System.identityHashCode(this);
        }
        id = i;
```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
        String postfix = "";
        while (cls.isArray()) {
            cls = cls.getComponentType();
            postfix = "Array";
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `args`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionInvokationHandler.java`
#### Snippet
```java
                }
                m = targetClass.getMethod(method.getName(), newParams);
                args = newArgs;
            }
            ReflectionUtil.setAccessible(m);
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            return PAD_SIZE0;
        case PAD_SIZE4:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            return PAD_SIZE4;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `core/src/main/java/org/apache/cxf/common/util/PrimitiveUtils.java`
#### Snippet
```java
            && value != null && value.isEmpty()) {
            //pass empty string to number type will result in Exception
            value = "0";
        }
        Object ret = value;
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            return PAD_SIZE4;
        case PAD_SIZE8:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p++] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            ob[p] = (byte)(b2 << 6 & 0xc0 | b3 & 0x3f);
```

### AssignmentToMethodParameter
Assignment to method parameter `p`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        case PAD_SIZE8:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p++] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            ob[p] = (byte)(b2 << 6 & 0xc0 | b3 & 0x3f);
            return PAD_SIZE8;
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
    public static byte[] decode(String id, boolean urlSafe) throws Base64Exception {
        if (urlSafe) {
            id = id.replace('-', '+').replace('_', '/');
            switch (id.length() % 4) {
            case 0:
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
                break;
            case 2:
                id += "==";
                break;
            case 3:
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
                break;
            case 3:
                id += "=";
                break;
            default:
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `core/src/main/java/org/apache/cxf/common/util/Base64OutputStream.java`
#### Snippet
```java
            theChunk = newArray(theChunk, 0, theChunk.length, b, off, len);
            lenToEncode = theChunk.length;
            off = 0;
        } else {
            theChunk = b;
```

### AssignmentToMethodParameter
Assignment to method parameter `nameSpaceURI`
in `core/src/main/java/org/apache/cxf/common/util/PackageUtils.java`
#### Snippet
```java
            urnScheme = "urn".equalsIgnoreCase(scheme);
            if ("http".equalsIgnoreCase(scheme) || urnScheme) {
                nameSpaceURI = nameSpaceURI.substring(idx + (urnScheme ? 1 : 3)); //
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `a`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java

        if (a.length < tmp.length) {
            a = (X[])java.lang.reflect.Array.
                newInstance(a.getClass().getComponentType(), tmp.length);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `command`
in `core/src/main/java/org/apache/cxf/common/logging/RegexLoggingFilter.java`
#### Snippet
```java
            while (m.find()) {
                int origLen = command.length();
                command = new StringBuilder(command)
                    .replace(m.start(group) + offset, m.end(group) + offset, replacement).toString();
                offset += command.length() - origLen;
```

### AssignmentToMethodParameter
Assignment to method parameter `command`
in `core/src/main/java/org/apache/cxf/common/logging/RegexLoggingFilter.java`
#### Snippet
```java
    public CharSequence filter(CharSequence command) {
        if (regPattern != null) {
            command = new ReplaceRegEx(regPattern, regGroup, regReplacement).filter(command);
        }
        for (ReplaceRegEx regex : regexs) {
```

### AssignmentToMethodParameter
Assignment to method parameter `command`
in `core/src/main/java/org/apache/cxf/common/logging/RegexLoggingFilter.java`
#### Snippet
```java
        }
        for (ReplaceRegEx regex : regexs) {
            command = regex.filter(command);
        }
        return command;
```

### AssignmentToMethodParameter
Assignment to method parameter `paramName`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            Multipart m = AnnotationUtils.getAnnotation(anns, Multipart.class);
            if (m != null) {
                paramName = m.value();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        }
        if (InjectionUtils.isSupportedCollectionOrArray(type)) {
            type = InjectionUtils.getActualType(genericType);
            sb.append(" repeating=\"true\"");
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        public void writeAttribute(String local, String value) throws XMLStreamException {
            if (SCHEMA_LOCATION.equals(local) && locsMap.containsKey(value)) {
                value = locsMap.get(value);
            }
            super.writeAttribute(local, value);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        }
        if (JAXB_DEFAULT_NAME.equals(name)) {
            name = StringUtils.uncapitalize(type.getSimpleName());
        }
        String prefix = getPrefix(namespace, clsMap);
```

### AssignmentToMethodParameter
Assignment to method parameter `hackAroundEmptyNamespaceIssue`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        if (StringUtils.isEmpty(ns)) {
            if (DOMUtils.getFirstElement(d.getDocumentElement()) == null) {
                hackAroundEmptyNamespaceIssue = true;
                return hackAroundEmptyNamespaceIssue;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            // create a copy of the dom so we
            // can modify it.
            d = copy(d);
            ns = tnsList.isEmpty() ? "" : tnsList.get(0);
            d.getDocumentElement().setAttribute("targetNamespace", ns);
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

        if (hackAroundEmptyNamespaceIssue) {
            d = doEmptyNamespaceHack(d);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `parentDocLoc`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        }
        int index = parentDocLoc.lastIndexOf('/');
        parentDocLoc = index == -1 ? parentDocLoc : parentDocLoc.substring(0, index + 1);

        index = parentRef.lastIndexOf('/');
```

### AssignmentToMethodParameter
Assignment to method parameter `parentRef`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

        index = parentRef.lastIndexOf('/');
        parentRef = index == -1 ? "" : parentRef.substring(0, index + 1);

        for (Element element : elements) {
```

### AssignmentToMethodParameter
Assignment to method parameter `resourceTagOpened`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        String path = ori.getURITemplate().getValue();
        if (!resourceTagOpened && openResource(path)) {
            resourceTagOpened = true;
            URITemplate template = ori.getClassResourceInfo().getURITemplate();
            if (template != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `resourceTagOpened`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        if (resourceTagOpened && !samePathOperationFollows) {
            endResourceTag(sb, ori);
            resourceTagOpened = false;
        }
        return resourceTagOpened;
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            // create a copy of the dom so we
            // can modify it.
            d = copy(d);
            el = DOMUtils.getFirstElement(d.getDocumentElement());
            while (el != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `enc`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
        int idx = enc.indexOf(';');
        if (idx != -1) {
            enc = enc.substring(0, idx);
        }
        // Charsets can be quoted. But it's quite certain that they can't have escaped quoted or
```

### AssignmentToMethodParameter
Assignment to method parameter `enc`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
        // Charsets can be quoted. But it's quite certain that they can't have escaped quoted or
        // anything like that.
        enc = enc.replace('"', ' ').replace('\'', ' ').trim();
        if (enc.isEmpty()) {
            return deflt;
```

### AssignmentToMethodParameter
Assignment to method parameter `pkg`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
            BufferedReader reader = new BufferedReader(new InputStreamReader(ins, StandardCharsets.UTF_8))) {
            if (!StringUtils.isEmpty(pkg)) {
                pkg += '.';
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `defaultNs`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
                // kind of a hack, but ObjectFactories may be created with empty
                // namespaces
                defaultNs = null;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `bufferSize`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
        }
        if (avail > bufferSize) {
            bufferSize = avail;
        }
        final byte[] buffer = new byte[bufferSize];
```

### AssignmentToMethodParameter
Assignment to method parameter `atLeast`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
            }
            output.write(buffer, 0, n);
            atLeast -= n;
            if (atLeast <= 0) {
                return;
```

### AssignmentToMethodParameter
Assignment to method parameter `atLeast`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
            }
            output.write(buffer, 0, n);
            atLeast -= n;
            if (atLeast <= 0) {
                return;
```

### AssignmentToMethodParameter
Assignment to method parameter `bufferSize`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
        int avail = input.available();
        if (avail > bufferSize) {
            bufferSize = avail;
        }
        Reader reader = charset == null ? new InputStreamReader(input, UTF8_CHARSET)
```

### AssignmentToMethodParameter
Assignment to method parameter `atLeast`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
                throw new IOException("0 bytes read in violation of InputStream.read(byte[])");
            }
            atLeast -= n;
            if (atLeast <= 0) {
                return;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java

        if (prefix == null) {
            prefix = "cxf";
        } else if (prefix.length() < 3) {
            prefix = prefix + "cxf";
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
            prefix = "cxf";
        } else if (prefix.length() < 3) {
            prefix = prefix + "cxf";
        }
        File result = Files.createTempFile(parent.toPath(), prefix, suffix).toFile();
```

### AssignmentToMethodParameter
Assignment to method parameter `node`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Element el;
        while (!(node instanceof Element)) {
            node = node.getParentNode();
        }
        el = (Element)node;
```

### AssignmentToMethodParameter
Assignment to method parameter `max`
in `core/src/main/java/org/apache/cxf/service/invoker/PooledFactory.java`
#### Snippet
```java
        this.factory = factory;
        if (max < 1) {
            max = 16;
        }
        pool = new ArrayBlockingQueue<>(max, true);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        if (legalIdentifier && conventionalIdentifier) {
            if (JAXBUtils.isJavaKeyword(name) && type == IdentifierType.VARIABLE) {
                name = normalizePackageNamePart(name);
            }
            if (!hasUnderscore || IdentifierType.CLASS != type) {
```

### AssignmentToMethodParameter
Assignment to method parameter `curUri`
in `core/src/main/java/org/apache/cxf/resource/ExtendedURIResolver.java`
#### Snippet
```java
                    // resolver finds any files which that file
                    // imports with locations relative to it.
                    curUri = currentResolver.getURI().toString();
                }
                if (currentResolver.isFile()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `curUri`
in `core/src/main/java/org/apache/cxf/resource/ExtendedURIResolver.java`
#### Snippet
```java
                }
                if (currentResolver.isFile()) {
                    curUri = currentResolver.getFile().getAbsoluteFile().toURI().toString();
                }
                InputStream in = currentResolver.getInputStream();
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `core/src/main/java/org/apache/cxf/service/invoker/AbstractInvoker.java`
#### Snippet
```java
        if (method != null && targetClass != null) {
            try {
                method = targetClass.getMethod(method.getName(), method.getParameterTypes());
            } catch (NoSuchMethodException ex) {
                // Perhaps the target class doesn't implement this method:
```

### AssignmentToMethodParameter
Assignment to method parameter `paramArray`
in `core/src/main/java/org/apache/cxf/service/invoker/AbstractInvoker.java`
#### Snippet
```java
    protected Object performInvocation(Exchange exchange, final Object serviceObject, Method m,
                                       Object[] paramArray) throws Exception {
        paramArray = insertExchange(m, paramArray, exchange);
        if (LOG.isLoggable(Level.FINER)) {
            LOG.log(Level.FINER, "INVOKING_METHOD", new Object[] {serviceObject,
```

### AssignmentToMethodParameter
Assignment to method parameter `destinationFactory`
in `core/src/main/java/org/apache/cxf/endpoint/ServerImpl.java`
#### Snippet
```java
        if (destinationFactory == null) {
            if (ei.getAddress() != null && ei.getAddress().indexOf("local://") != -1) {
                destinationFactory = bus.getExtension(DestinationFactoryManager.class)
                    .getDestinationFactoryForUri(ei.getAddress());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `destinationFactory`
in `core/src/main/java/org/apache/cxf/endpoint/ServerImpl.java`
#### Snippet
```java

            if (destinationFactory == null) {
                destinationFactory = bus.getExtension(DestinationFactoryManager.class)
                    .getDestinationFactory(ei.getTransportId());
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `core/src/main/java/org/apache/cxf/extension/RegistryImpl.java`
#### Snippet
```java
    protected RegistryImpl(Map<K, T> e) {
        if (null == e) {
            e = new ConcurrentHashMap<>(16, 0.75f, 4);
        } else if (!(e instanceof ConcurrentHashMap)) {
            e = new ConcurrentHashMap<>(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `core/src/main/java/org/apache/cxf/extension/RegistryImpl.java`
#### Snippet
```java
            e = new ConcurrentHashMap<>(16, 0.75f, 4);
        } else if (!(e instanceof ConcurrentHashMap)) {
            e = new ConcurrentHashMap<>(e);
        }
        entries = e;
```

### AssignmentToMethodParameter
Assignment to method parameter `uriStr`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
            }
        } catch (IOException e) {
            uriStr = uriStr.substring(i + 1);
            tryClasspath(uriStr);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `uriStr`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
        boolean isClasspathURL = false;
        if (uriStr.startsWith("classpath:")) {
            uriStr = uriStr.substring(10);
            isClasspathURL = true;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `ns`
in `core/src/main/java/org/apache/cxf/staxutils/transform/DelegatingNamespaceContext.java`
#### Snippet
```java
        }
        if (value != null) {
            ns = value;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `core/src/main/java/org/apache/cxf/transport/MultipleEndpointObserver.java`
#### Snippet
```java
                message.setExchange(exchange);
            }
            message = createMessage(message);
            message.setExchange(exchange);
            exchange.setInMessage(message);
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java

        if (sourceStart + length >= src.length) {
            length = src.length - sourceStart;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `curUri`
in `core/src/main/java/org/apache/cxf/transport/TransportURIResolver.java`
#### Snippet
```java
    public InputSource resolve(String curUri, String baseUri) {
        // Spaces must be encoded or URI.resolve() will choke
        curUri = curUri.replace(" ", "%20");

        InputSource is = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java
        //If the uri is related path or has no protocol prefix , we will set it to be http
        if (uri.startsWith("/") || uri.indexOf(':') < 0) {
            uri = "http://" + uri;
        }
        T factory = checkForURI(uri);
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `core/src/main/java/org/apache/cxf/attachment/Base64DecoderStream.java`
#### Snippet
```java
            decodedIndex += copyCount;
            decodedCount -= copyCount;
            offset += copyCount;
            length -= copyCount;
            readCharacters += copyCount;
```

### AssignmentToMethodParameter
Assignment to method parameter `length`
in `core/src/main/java/org/apache/cxf/attachment/Base64DecoderStream.java`
#### Snippet
```java
            decodedCount -= copyCount;
            offset += copyCount;
            length -= copyCount;
            readCharacters += copyCount;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `recordLoc`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                }
                e = (Element)parent.appendChild(e);
                recordLoc = addLocation(doc, e, reader, recordLoc);

                for (int ns = 0; ns < reader.getNamespaceCount(); ns++) {
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                                               + MAX_CHILD_ELEMENTS_VAL);
                }
                parent = e;
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                    return;
                }
                parent = stack.pop();
                if (parent instanceof Document || parent instanceof DocumentFragment) {
                    return;
```

### AssignmentToMethodParameter
Assignment to method parameter `recordLoc`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            case XMLStreamConstants.CHARACTERS:
                if (parent != null) {
                    recordLoc = addLocation(doc,
                                            parent.appendChild(doc.createTextNode(reader.getText())),
                                            reader, recordLoc);
```

### AssignmentToMethodParameter
Assignment to method parameter `recordLoc`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.CDATA:
                recordLoc = addLocation(doc,
                                        parent.appendChild(doc.createCDATASection(reader.getText())),
                                        reader, recordLoc);
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        throws XMLStreamException {
        if (prefix == null) {
            prefix = "";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                                           + MAX_CHILD_ELEMENTS_VAL);
            }
            parent = e;
            break;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                return parent;
            }
            parent = context.popFromStack();
            if (parent instanceof Document || parent instanceof DocumentFragment) {
                return parent;
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                                               + MAX_CHILD_ELEMENTS_VAL);
                }
                parent = e;
                break;
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                    return;
                }
                parent = context.popFromStack();
                if (parent instanceof Document || parent instanceof DocumentFragment) {
                    return;
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java

            if (e.getParentNode() instanceof Element) {
                e = (Element)e.getParentNode();
            } else if (StringUtils.isEmpty(prefix) && StringUtils.isEmpty(namespaceURI)) {
                //A document that probably doesn't have any namespace qualifies elements
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            }
            if (p != null) {
                reader = new DepthRestrictingStreamReader(reader, p);
            }
        } catch (ClassCastException cce) {
```

### AssignmentToMethodParameter
Assignment to method parameter `encoding`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            return in;
        }
        encoding = encoding.toLowerCase();

        // some encodings are just pass-throughs, with no real decoding.
```

### AssignmentToMethodParameter
Assignment to method parameter `contentId`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        if (contentId.startsWith("cid:")) {
            try {
                contentId = URLDecoder.decode(contentId.substring(4), StandardCharsets.UTF_8.name());
            } catch (UnsupportedEncodingException ue) {
                contentId = contentId.substring(4);
```

### AssignmentToMethodParameter
Assignment to method parameter `contentId`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                contentId = URLDecoder.decode(contentId.substring(4), StandardCharsets.UTF_8.name());
            } catch (UnsupportedEncodingException ue) {
                contentId = contentId.substring(4);
            }
            
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            if (id.startsWith("<")) {
                // strip <>
                id = id.substring(1, id.length() - 1);
            }
            // strip cid:
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            // strip cid:
            if (id.startsWith("cid:")) {
                id = id.substring(4);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            try {
                // urldecode
                id = URLDecoder.decode(id, StandardCharsets.UTF_8.name());
            } catch (UnsupportedEncodingException e) {
                //ignore, keep id as is
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        if (id == null) {
            //no Content-ID, set cxf default ID
            id =  BODY_ATTACHMENT_ID;
        }
        return id;
```

### AssignmentToMethodParameter
Assignment to method parameter `mimeType`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        }
        if (mimeType == null) {
            mimeType = "application/octet-stream";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            return false;
        }
        contentType = contentType.toLowerCase();
        for (String s : types) {
            if (contentType.indexOf(s) != -1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bufferSize`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentSerializer.java`
#### Snippet
```java
        }
        if (avail > bufferSize) {
            bufferSize = avail;
        }
        final byte[] buffer = new byte[bufferSize];
```

### AssignmentToMethodParameter
Assignment to method parameter `attachmentId`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentSerializer.java`
#### Snippet
```java

        if (attachmentId != null) {
            attachmentId = checkAngleBrackets(attachmentId);
            writer.write("Content-ID: <");
            
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            message.getExchange().put(Endpoint.class, endpoint);
        }
        message = endpoint.getBinding().createMessage(message);
        message.getExchange().setInMessage(message);
        message.put(Message.REQUESTOR_ROLE, Boolean.TRUE);
```

### AssignmentToMethodParameter
Assignment to method parameter `exchange`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            }
            if (exchange == null) {
                exchange = new ExchangeImpl();
            }
            exchange.setSynchronous(callback == null);
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            // on message
            if (context == null) {
                context = new HashMap<>();
            }
            Map<String, Object> reqContext = CastUtils.cast((Map<?, ?>)context.get(REQUEST_CONTEXT));
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java
            // can modify it.
            if (!alreadyWritable) {
                d = copy(d);
            }
            el = DOMUtils.getFirstElement(d.getDocumentElement());
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java
            // create a copy of the dom so we
            // can modify it.
            d = copy(d);
            copied = true;
            ns = serviceInfo.getInterface().getName().getNamespaceURI();
```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java

        if (hackAroundEmptyNamespaceIssue) {
            d = doEmptyNamespaceHack(d, copied);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `d`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java
        if (patchRequired) {
            if (!copied) {
                d = copy(d);
            }
            n = d.getDocumentElement().getFirstChild();
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataWriter.java`
#### Snippet
```java
            } else if (obj instanceof Node) {
                if (obj instanceof DocumentFragment) {
                    obj = org.apache.cxf.helpers.DOMUtils.getDomDocumentFragment((DocumentFragment)obj);
                }
                if (schema != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `nd`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataWriter.java`
#### Snippet
```java
                    return;
                } else if (nd instanceof DocumentFragment) {
                    nd = dw.getDocument().importNode(nd, true);
                    dw.getCurrentNode().appendChild(nd);
                    return;
```

### AssignmentToMethodParameter
Assignment to method parameter `nd`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataWriter.java`
#### Snippet
```java
                    return;
                } else if (nd instanceof DocumentFragment) {
                    nd = dw.getDocument().importNode(nd, true);
                    dw.getCurrentFragment().appendChild(nd);
                    return;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
    public Object read(final QName name, XMLStreamReader input, Class<?> type) {
        if (type == null) {
            type = preferred;
        }
        if (Source.class.equals(type) && message != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
            }
            if ("dom".equalsIgnoreCase(s)) {
                type = DOMSource.class;
            } else if ("stream".equalsIgnoreCase(s)) {
                type = StreamSource.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                type = DOMSource.class;
            } else if ("stream".equalsIgnoreCase(s)) {
                type = StreamSource.class;
            } else if ("sax".equalsIgnoreCase(s) || "cxf.stax".equalsIgnoreCase(s)) {
                type = SAXSource.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                type = StreamSource.class;
            } else if ("sax".equalsIgnoreCase(s) || "cxf.stax".equalsIgnoreCase(s)) {
                type = SAXSource.class;
            } else if ("stax".equals(s)) {
                try {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
            } else if ("stax".equals(s)) {
                try {
                    type = ClassLoaderUtils.loadClass("javax.xml.transform.stax.StAXSource", getClass());
                } catch (ClassNotFoundException e) {
                    type = SAXSource.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                    type = ClassLoaderUtils.loadClass("javax.xml.transform.stax.StAXSource", getClass());
                } catch (ClassNotFoundException e) {
                    type = SAXSource.class;
                }
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                }
            } else {
                type = DOMSource.class;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `input`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
            if (schema != null) {
                dom = validate(input);
                input = StaxUtils.createXMLStreamReader(dom);
            }
            if (type != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `input`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                    };
                } else if ("javax.xml.transform.stax.StAXSource".equals(type.getName())) {
                    input = resetForStreaming(input);
                    Object o = createStaxSource(input, type);
                    if (o != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractInDatabindingInterceptor.java`
#### Snippet
```java
            if (name.getNamespaceURI() == null || name.getNamespaceURI().isEmpty()) {
                // message part has same namespace with the message
                name = new QName(p.getMessageInfo().getName().getNamespaceURI(), name.getLocalPart());
            }
            if (name.equals(p.getConcreteName())) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                return;
            }
            uri = expected.getNamespaceURI();
            local = expected.getLocalPart();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `local`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
            }
            uri = expected.getNamespaceURI();
            local = expected.getLocalPart();
        }
        if (!attributesToElements) {
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        }
        if (replaceText != null) {
            text = replaceText;
            replaceText = null;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `local`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
            }
            uri = expected.getNamespaceURI();
            local = expected.getLocalPart();
        }
        if (!attributesToElements) {
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                // to that namespace. If the namespace is unbound, generate a new prefix and
                // write its declaration later.
                prefix = namespaceContext.getPrefix(expected.getNamespaceURI());
                if (prefix == null) {
                    prefix = namespaceContext.findUniquePrefix(expected.getNamespaceURI());
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                prefix = namespaceContext.getPrefix(expected.getNamespaceURI());
                if (prefix == null) {
                    prefix = namespaceContext.findUniquePrefix(expected.getNamespaceURI());
                }
            } else if (!prefix.isEmpty() && expected.getNamespaceURI().isEmpty()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
            } else if (!prefix.isEmpty() && expected.getNamespaceURI().isEmpty()) {
                // if the element is demoted to a unqualified element, use an empty prefix.
                prefix = "";
            }
            expected = new QName(expected.getNamespaceURI(), expected.getLocalPart(), prefix);
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        }

        uri = value != null ? value : uri;

        if (writtenUris.get(0).contains(uri)
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        } else {
            if (prefix.isEmpty()) {
                prefix = namespaceContext.findUniquePrefix(uri);
            }
            super.writeNamespace(prefix, uri);
```

### AssignmentToMethodParameter
Assignment to method parameter `text`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        }
        if (replaceText != null) {
            text = replaceText.toCharArray();
            replaceText = null;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        }

        uri = value != null ? value : uri;

        if (writtenUris.get(0).contains(uri) && namespaceContext.getPrefix(uri).isEmpty()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `os`
in `core/src/main/java/org/apache/cxf/interceptor/StaxOutInterceptor.java`
#### Snippet
```java
    private OutputStream setupOutputStream(OutputStream os) {
        if (!(os instanceof AbstractWrappedOutputStream)) {
            os = new AbstractWrappedOutputStream(os) { };
        }
        ((AbstractWrappedOutputStream)os).allowFlush(false);
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java

    protected void log(Logger logger, String message) {
        message = transform(message);
        if (writer != null) {
            writer.println(message);
```

### AssignmentToMethodParameter
Assignment to method parameter `firstLine`
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
        // The actual exception class of the cause might be unavailable at the
        // client -> use a standard throwable to represent the cause.
        firstLine = firstLine.substring(firstLine.indexOf(':') + 1).trim();
        Throwable res = null;
        if (firstLine.indexOf(':') != -1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `i`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
        }
        for (Object o : os) {
            i = hash(i, o);
        }
        return i;
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java
            return s.toCharArray();
        }
        s = s.substring(4);

        char[] b = new char[s.length() / 2];
```

### AssignmentToMethodParameter
Assignment to method parameter `writer`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
        if (outElementsMap != null || outDropElements != null
            || outAppendMap != null || attributesToElements) {
            writer = new OutTransformWriter(createNewWriterIfNeeded(writer, os, encoding), outElementsMap, outAppendMap,
                                            outDropElements, outAttributesMap, attributesToElements, defaultNamespace);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
        if (inElementsMap != null || inAppendMap != null || inDropElements != null
            || inAttributesMap != null) {
            reader = new InTransformReader(createNewReaderIfNeeded(reader, is),
                                           inElementsMap, inAppendMap, inDropElements,
                                           inAttributesMap, blockOriginalReader);
```

### AssignmentToMethodParameter
Assignment to method parameter `writer`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
        if (outElementsMap != null || outDropElements != null
            || outAppendMap != null || attributesToElements) {
            writer = new OutTransformWriter(createNewWriterIfNeeded(writer, os), outElementsMap, outAppendMap,
                                            outDropElements, null, attributesToElements, defaultNamespace);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `bn`
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java

        if (null == bn) {
            bn = getBeanName(beanInstance);
            if (null == bn) {
                return;
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
            }

            t = t.substring(i + 1);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `max`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPullPoint.java`
#### Snippet
```java
        try {
            if (max == 0) {
                max = 256;
            }
            initSession();
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStoreType`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
        } else {
            //Can default to JKS
            trustStoreType = SystemPropertyAction.getProperty("javax.net.ssl.trustStoreType");
            if (trustStoreType == null) {
                trustStoreType = def;
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStoreType`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            trustStoreType = SystemPropertyAction.getProperty("javax.net.ssl.trustStoreType");
            if (trustStoreType == null) {
                trustStoreType = def;
                logMsg = "TRUST_STORE_TYPE_NOT_SET";
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `keyPassword`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_PASSWORD_SET";
        } else {
            keyPassword =
                SystemPropertyAction.getProperty("javax.net.ssl.keyPassword");
            if (keyPassword == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `keyPassword`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
                SystemPropertyAction.getProperty("javax.net.ssl.keyPassword");
            if (keyPassword == null) {
                keyPassword =
                    SystemPropertyAction.getProperty("javax.net.ssl.keyStorePassword");
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStorePassword`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_STORE_PASSWORD_SET";
        } else {
            keyStorePassword =
                SystemPropertyAction.getProperty("javax.net.ssl.keyStorePassword");
            logMsg = keyStorePassword != null
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStoreLocation`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "TRUST_STORE_SET";
        } else {
            trustStoreLocation = SystemPropertyAction.getProperty("javax.net.ssl.trustStore");
            if (trustStoreLocation != null) {
                logMsg = "TRUST_STORE_SYSTEM_PROPERTY_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreLocation`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_STORE_SET";
        } else {
            keyStoreLocation = SystemPropertyAction.getProperty("javax.net.ssl.keyStore");
            if (keyStoreLocation != null) {
                logMsg = "KEY_STORE_SYSTEM_PROPERTY_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreLocation`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
                logMsg = "KEY_STORE_SYSTEM_PROPERTY_SET";
            } else {
                keyStoreLocation =
                    SystemPropertyAction.getProperty("user.home") + "/.keystore";
                logMsg = "KEY_STORE_NOT_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStoreMgrFactoryAlgorithm`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "TRUST_STORE_ALGORITHM_SET";
        } else {
            trustStoreMgrFactoryAlgorithm =
                TrustManagerFactory.getDefaultAlgorithm();
            logMsg = "TRUST_STORE_ALGORITHM_NOT_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStorePassword`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "TRUST_STORE_PASSWORD_SET";
        } else {
            trustStorePassword =
                SystemPropertyAction.getProperty("javax.net.ssl.trustStorePassword");
            logMsg = trustStorePassword != null
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreProvider`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_STORE_PROVIDER_SET";
        } else {
            keyStoreProvider = SystemPropertyAction.getProperty("javax.net.ssl.keyStoreProvider", null);
            if (keyStoreProvider == null) {
                logMsg = "KEY_STORE_PROVIDER_NOT_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreType`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_STORE_TYPE_SET";
        } else {
            keyStoreType = SystemPropertyAction.getProperty("javax.net.ssl.keyStoreType", null);
            if (keyStoreType == null) {
                keyStoreType = def;
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreType`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            keyStoreType = SystemPropertyAction.getProperty("javax.net.ssl.keyStoreType", null);
            if (keyStoreType == null) {
                keyStoreType = def;
                logMsg = "KEY_STORE_TYPE_NOT_SET";
            } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `trustStoreProvider`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "TRUST_STORE_PROVIDER_SET";
        } else {
            trustStoreProvider = SystemPropertyAction.getProperty("javax.net.ssl.trustStoreProvider", null);
            if (trustStoreProvider == null) {
                logMsg = "TRUST_STORE_PROVIDER_NOT_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `keyStoreMgrFactoryAlgorithm`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            logMsg = "KEY_STORE_ALGORITHM_SET";
        } else {
            keyStoreMgrFactoryAlgorithm =
                KeyManagerFactory.getDefaultAlgorithm();
            logMsg = "KEY_STORE_ALGORITHM_NOT_SET";
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java

        if (genericType == null) {
            genericType = plainClass;
        }
        return getClassContext(genericType);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java
        throws IOException {
        if (type == null) {
            type = obj.getClass();
        }
        if (genericType == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java
        }
        if (genericType == null) {
            genericType = type;
        }
        AegisContext context = getAegisContext(type, genericType);
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java

        if (genericType == null) {
            genericType = type;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java

        if (type == null) {
            type = messyCastToRawType(genericType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpDestination.java`
#### Snippet
```java
                             HttpServletResponse resp) throws IOException {
        if (context == null) {
            context = servletContext;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `registry`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpTransportFactory.java`
#### Snippet
```java
        super(DEFAULT_NAMESPACES);
        if (registry == null) {
            registry = new DestinationRegistryImpl();
        }
        this.registry = registry;
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
        // Decode the path.
        try {
            uri = URLDecoder.decode(uri, StandardCharsets.UTF_8.name());
        } catch (UnsupportedEncodingException e) {
            try {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
        } catch (UnsupportedEncodingException e) {
            try {
                uri = URLDecoder.decode(uri, StandardCharsets.ISO_8859_1.name());
            } catch (UnsupportedEncodingException e1) {
                throw new Error();
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java

        // Convert file separators.
        uri = uri.replace('/', File.separatorChar);

        // Simplistic dumb security check.
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                    ? actualObject.getClass() : cls;
                if (cls == genericType) {
                    genericType = actualClass;
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                                           String enc, OutputStream os) throws Exception {
        if (obj instanceof JAXBElement) {
            obj = ((JAXBElement<?>)obj).getValue();
        } else {
            obj = convertToJaxbElementIfNeeded(obj, cls, genericType);
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
            obj = ((JAXBElement<?>)obj).getValue();
        } else {
            obj = convertToJaxbElementIfNeeded(obj, cls, genericType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java

        if (obj instanceof JAXBElement && cls != JAXBElement.class) {
            cls = JAXBElement.class;
        }
        Marshaller ms = createMarshaller(obj, cls, genericType, enc);
```

### AssignmentToMethodParameter
Assignment to method parameter `actualObject`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                           Type genericType, String enc, OutputStream os) throws Exception {

        actualObject = convertToJaxbElementIfNeeded(actualObject, actualClass, genericType);
        if (actualObject instanceof JAXBElement && actualClass != JAXBElement.class) {
            actualClass = JAXBElement.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `actualClass`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        actualObject = convertToJaxbElementIfNeeded(actualObject, actualClass, genericType);
        if (actualObject instanceof JAXBElement && actualClass != JAXBElement.class) {
            actualClass = JAXBElement.class;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedOutputBuffer.java`
#### Snippet
```java
                    this.buffer.put(b, off, chunk);
                    remaining -= chunk;
                    off += chunk;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/SharedOutputBuffer.java`
#### Snippet
```java
                    buffer().put(b, off, chunk);
                    remaining -= chunk;
                    off += chunk;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `op`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/WebFaultInInterceptor.java`
#### Snippet
```java
    private MessagePartInfo getFaultMessagePart(QName qname, OperationInfo op) {
        if (op.isUnwrapped() && op instanceof UnwrappedOperationInfo) {
            op = ((UnwrappedOperationInfo)op).getWrappedOperation();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `st`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                    return ASYNC_ONLY;
                } else {
                    st = Boolean.parseBoolean(s);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `st`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                    return ASYNC_ONLY;
                } else {
                    st = Boolean.parseBoolean(s);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
    private static STSClient createSTSClient(Message message, String type) {
        if (type == null) {
            type = "";
        } else {
            type = "." + type + "-client";
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
            type = "";
        } else {
            type = "." + type + "-client";
        }
        STSClient client = new STSClient(message.getExchange().getBus());
```

### AssignmentToMethodParameter
Assignment to method parameter `transportId`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
            ConduitInitiatorManager cim = bus.getExtension(ConduitInitiatorManager.class);
            ConduitInitiator ci = cim.getConduitInitiatorForUri(location);
            transportId = ci.getTransportIds().get(0);
        }
        EndpointInfo ei = new EndpointInfo(si, transportId);
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
        }

        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java

        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
        if (s == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
        String action, SoapMessage message, RequestData data
    ) throws WSSecurityException {
        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
    ) throws WSSecurityException {
        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
        if (s == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
        }

        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
```

### AssignmentToMethodParameter
Assignment to method parameter `action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java

        action = addToAction(action, "Signature", true);
        action = addToAction(action, "Encrypt", true);
        Object s = SecurityUtils.getSecurityPropertyValue(SecurityConstants.SIGNATURE_CRYPTO, message);
        if (s == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `tokentype`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java

        if (tokentype == null) {
            tokentype = tokenType;
        }
        if (tokentype == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `tokentype`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
        }
        if (tokentype == null) {
            tokentype = namespace + "/RSTR/Status";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java

        if ("RequestSecurityTokenResponseCollection".equals(el.getLocalName())) {
            el = DOMUtils.getFirstElement(el);
        }
        if (!"RequestSecurityTokenResponse".equals(el.getLocalName())) {
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            throw new Fault("Unexpected element " + el.getLocalName(), LOG);
        }
        el = DOMUtils.getFirstElement(el);
        Element rst = null;
        Element rar = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
                }
            }
            el = DOMUtils.getNextElement(el);
        }
        Element rstDec = rst;
```

### AssignmentToMethodParameter
Assignment to method parameter `keyTypeToWrite`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
                writer.writeCharacters(STSUtils.getTokenTypeSCT(namespace));
                writer.writeEndElement();
                keyTypeToWrite = namespace + "/SymmetricKey";
            }
        } else if (keyTypeToWrite == null && sendKeyType) {
```

### AssignmentToMethodParameter
Assignment to method parameter `keyTypeToWrite`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            writer.writeCharacters(namespace + "/SymmetricKey");
            writer.writeEndElement();
            keyTypeToWrite = namespace + "/SymmetricKey";
        } else if (keyTypeToWrite != null) {
            writer.writeStartElement("wst", "KeyType", namespace);
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
            if (s == null) {
                //MS/WCF doesn't put a soap action out for this, must check the headers
                s = inProps.getAction().getValue();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java

    protected void addSupportingElement(Element el) {
        el = (Element)DOMUtils.getDomElement(el);
        if (lastSupportingTokenElement != null) {
            insertAfter(el, lastSupportingTokenElement);
```

### AssignmentToMethodParameter
Assignment to method parameter `child`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java

    protected void insertAfter(Element child, Element sib) {
        child = (Element)DOMUtils.getDomElement(child);
        if (sib.getNextSibling() == null) {
            secHeader.getSecurityHeaderElement().appendChild(child);
```

### AssignmentToMethodParameter
Assignment to method parameter `elementToProtect`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
        List<WSSecurityEngineResult> tokenResults
    ) {
        elementToProtect = (Element)DOMUtils.getDomElement(elementToProtect);
        for (WSSecurityEngineResult result : protResults) {
            List<WSDataRef> dataRefs =
```

### AssignmentToMethodParameter
Assignment to method parameter `replyToDestination`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
        if (replyToDestination == null && headers.isSetJMSReplyTo()) {
            String replyTo = headers.getJMSReplyTo();
            replyToDestination = jmsConfig.getReplyDestination(session, replyTo);
        }
        if (replyToDestination != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bindingInfo`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractWSDLBasedEndpointFactory.java`
#### Snippet
```java
        if (bindingInfo == null) {
            // SOAP nonsense
            bindingInfo = createBindingInfo();
            if (bindingInfo instanceof SoapBindingInfo
                && (((SoapBindingInfo) bindingInfo).getTransportURI() == null
```

### AssignmentToMethodParameter
Assignment to method parameter `msg`
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/client/NotificationBroker.java`
#### Snippet
```java
                DOMResult result = new DOMResult();
                context.createMarshaller().marshal(msg, result);
                msg = result.getNode();
                if (msg instanceof Document) {
                    msg = ((Document)msg).getDocumentElement();
```

### AssignmentToMethodParameter
Assignment to method parameter `msg`
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/client/NotificationBroker.java`
#### Snippet
```java
                msg = result.getNode();
                if (msg instanceof Document) {
                    msg = ((Document)msg).getDocumentElement();
                }
            } catch (JAXBException e) {
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
        if (result != null) {
            if (result instanceof MessageContentsList) {
                result = ((MessageContentsList)result).get(0);
            } else if (result instanceof List) {
                result = ((List<?>)result).get(0);
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
                result = ((MessageContentsList)result).get(0);
            } else if (result instanceof List) {
                result = ((List<?>)result).get(0);
            } else if (result.getClass().isArray()) {
                result = ((Object[])result)[0];
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
                result = ((List<?>)result).get(0);
            } else if (result.getClass().isArray()) {
                result = ((Object[])result)[0];
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
    }
    private static Object checkSubResultObject(Object result, String subResourcePath) {
        result = checkResultObject(result);
        if (result == null) {
            org.apache.cxf.common.i18n.Message errorM =
```

### AssignmentToMethodParameter
Assignment to method parameter `tag`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseBuilderImpl.java`
#### Snippet
```java
        final String doubleQuote = "\"";
        if (tag != null && !tag.startsWith(doubleQuote)) {
            tag = doubleQuote + tag + doubleQuote;
        }
        return setHeader(HttpHeaders.ETAG, tag);
```

### AssignmentToMethodParameter
Assignment to method parameter `loc`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseBuilderImpl.java`
#### Snippet
```java

                UriInfo ui = new UriInfoImpl(currentMessage.getExchange().getInMessage(), null);
                loc = ui.getBaseUriBuilder()
                        .path(loc.getRawPath())
                        .replaceQuery(loc.getRawQuery())
```

### AssignmentToMethodParameter
Assignment to method parameter `link`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
        if (!link.getUri().isAbsolute()) {
            URI requestURI = URI.create((String)outMessage.get(Message.REQUEST_URI));
            link = Link.fromLink(link).baseUri(requestURI).build();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `paramValue`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/NewCookieHeaderProvider.java`
#### Snippet
```java
        if (paramValue.startsWith(DOUBLE_QUOTE)
            && paramValue.endsWith(DOUBLE_QUOTE) && paramValue.length() > 1) {
            paramValue = paramValue.substring(1, paramValue.length() - 1);
        }
        return paramValue;
```

### AssignmentToMethodParameter
Assignment to method parameter `requestUri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerRequestContextImpl.java`
#### Snippet
```java
                requestUriString = "/";
            }
            requestUri = URI.create(requestUriString);

        }
```

### AssignmentToMethodParameter
Assignment to method parameter `provider`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurationImpl.java`
#### Snippet
```java
                return false;
            }
            provider = createProvider((Class<?>)provider);
        }
        if (isRegistered(provider)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `providerClass`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
                collectInterfaces(interfaces, anInterface);
            }
            providerClass = providerClass.getSuperclass();
        } while (providerClass != null && providerClass != Object.class);

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkHeaderProvider.java`
#### Snippet
```java
            throw new IllegalArgumentException("Link value can not be null");
        }
        value = value.trim();
        int closeIndex = value.indexOf('>');
        if (!value.startsWith("<") || closeIndex < 2) {
```

### AssignmentToMethodParameter
Assignment to method parameter `link`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java
    public Builder link(String link) {

        link = link.trim();
        if (link.length() > 1 && link.startsWith("<")) {
            int index = link.lastIndexOf('>', link.length());
```

### AssignmentToMethodParameter
Assignment to method parameter `link`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java
                ub = UriBuilder.fromUri(uri);
                if (index + 1 == link.length()) {
                    link = "";
                } else {
                    link = link.substring(index + 1);
```

### AssignmentToMethodParameter
Assignment to method parameter `link`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java
                    link = "";
                } else {
                    link = link.substring(index + 1);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `base`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
        try {
            Bus bus = message.getExchange().getBus();
            base = getPublishedEndpointURL(message, base, endpointInfo);
            //making sure this are existing map objects for the endpoint.
            Map<String, Definition> mp = getWSDLKeyDefinition(endpointInfo);
```

### AssignmentToMethodParameter
Assignment to method parameter `startLoc`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
        if (!(new URI(startLoc).isAbsolute())) {
            if (StringUtils.isEmpty(docBase)) {
                startLoc = new URI(".").resolve(startLoc).toString();
            } else {
                startLoc = new URI(docBase).resolve(startLoc).toString();
```

### AssignmentToMethodParameter
Assignment to method parameter `startLoc`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                startLoc = new URI(".").resolve(startLoc).toString();
            } else {
                startLoc = new URI(docBase).resolve(startLoc).toString();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `base`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
        }
        if (prop != null) {
            base = String.valueOf(prop);
        }
        return base;
```

### AssignmentToMethodParameter
Assignment to method parameter `epurl`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java

        if (endpointInfo.getProperty(PUBLISHED_ENDPOINT_URL) != null) {
            epurl = String.valueOf(
                endpointInfo.getProperty(PUBLISHED_ENDPOINT_URL));
            updatePublishedEndpointUrl(epurl, def, endpointInfo.getName());
```

### AssignmentToMethodParameter
Assignment to method parameter `map`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        }
        if (map == null) {
            map = new LinkedHashMap<>();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        if (index != -1) {
            this.scheme = uri.substring(0, index);
            uri = uri.substring(index + 1);
            if (uri.indexOf("//") == 0) {
                uri = uri.substring(2);
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            uri = uri.substring(index + 1);
            if (uri.indexOf("//") == 0) {
                uri = uri.substring(2);
                index = uri.indexOf('/');
                if (index != -1) {
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                    this.port = schemePair.length == 2 ? Integer.parseInt(schemePair[1]) : -1;

                    uri = uri.substring(index);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        if (index != -1) {
            rawQuery = uri.substring(index + 1);
            uri = uri.substring(0, index);
        }
        setPathAndMatrix(uri);
```

### AssignmentToMethodParameter
Assignment to method parameter `thePath`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            if ((scheme != null || host != null || userInfo != null)
                && thePath.length() != 0 && !(thePath.startsWith("/") || thePath.startsWith(";"))) {
                thePath = "/" + thePath;
            }
            try {
```

### AssignmentToMethodParameter
Assignment to method parameter `queryValue`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            // passed as part of replaceQuery are encoded as %20 while those passed as part
            // of quertyParam are encoded as '+'
            queryValue = queryValue.replace(" ", "%20");
        }
        query = JAXRSUtils.getStructuredParams(queryValue, "&", false, true);
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
        } else {
            segments = new ArrayList<>();
            path = path.replaceAll("/", "%2F");
            segments.add(new PathSegmentImpl(path, false));
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `instanceClass`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java

    public ClassResourceInfo getSubResource(Class<?> typedClass, Class<?> instanceClass, Object instance) {
        instanceClass = enableStatic && typedClass != Object.class ? typedClass : instanceClass;
        return getSubResource(typedClass, instanceClass, instance, enableStatic, JAXRSUtils.getCurrentMessage());
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `instanceClass`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java

    public ClassResourceInfo findResource(Class<?> typedClass, Class<?> instanceClass) {
        instanceClass = enableStatic ? typedClass : instanceClass;
        SubresourceKey key = new SubresourceKey(typedClass, instanceClass);
        return subResources.get(key);
```

### AssignmentToMethodParameter
Assignment to method parameter `theFields`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    private Map<Class<?>, List<Field>> addContextField(Map<Class<?>, List<Field>> theFields, Field f) {
        if (theFields == null) {
            theFields = new HashMap<>();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `theType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXBUtils.java`
#### Snippet
```java
        if (adapter != null) {
            if (adapter.type() != XmlJavaTypeAdapter.DEFAULT.class) {
                theType = adapter.type();
            } else {
                Type topAdapterType = adapter.value().getGenericSuperclass();
```

### AssignmentToMethodParameter
Assignment to method parameter `theType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXBUtils.java`
#### Snippet
```java
                if (types != null && types.length == 2) {
                    int index = boundType ? 1 : 0;
                    theType = InjectionUtils.getActualType(types[index]);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `pathValue`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
    public static URITemplate createTemplate(String pathValue) {
        if (pathValue == null) {
            pathValue = "/";
        } else if (!pathValue.startsWith("/")) {
            pathValue = "/" + pathValue;
```

### AssignmentToMethodParameter
Assignment to method parameter `pathValue`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
            pathValue = "/";
        } else if (!pathValue.startsWith("/")) {
            pathValue = "/" + pathValue;
        }
        return createExactTemplate(pathValue);
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
                    sb.append(segment);
                }
                uri = sb.toString();
                if (uri.isEmpty()) {
                    uri = SLASH;
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
                uri = sb.toString();
                if (uri.isEmpty()) {
                    uri = SLASH;
                }
                m = templateRegexPattern.matcher(uri);
```

### AssignmentToMethodParameter
Assignment to method parameter `uriChunk`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
            }
            if (CurlyBraceTokenizer.insideBraces(uriChunk)) {
                uriChunk = CurlyBraceTokenizer.stripBraces(uriChunk).trim();
                Matcher matcher = VARIABLE_PATTERN.matcher(uriChunk);
                if (matcher.matches()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `u`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            String base = request.getScheme() + "://" + serverAndPort;
            if (!absolute) {
                u = URI.create(base + u.toString());
            } else {
                int originalPort = u.getPort();
```

### AssignmentToMethodParameter
Assignment to method parameter `u`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
                    ? ANY_IP_ADDRESS : LOCAL_HOST_IP_ADDRESS;
                String replaceValue = originalPort == -1 ? hostValue : hostValue + ":" + originalPort;
                u = URI.create(u.toString().replace(replaceValue, serverAndPort));
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        int ind = path.indexOf(address);
        if (ind == -1 && address.equals(path + "/")) {
            path += "/";
            ind = 0;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        }
        if (ind == 0) {
            path = path.substring(address.length());
        }
        if (addSlash && !path.startsWith("/")) {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        }
        if (addSlash && !path.startsWith("/")) {
            path = "/" + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        boolean baseSlash = baseAddress.endsWith("/");
        if (pathSlash && baseSlash) {
            path = path.substring(1);
        } else if (!pathSlash && !baseSlash) {
            path = "/" + path;
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            path = path.substring(1);
        } else if (!pathSlash && !baseSlash) {
            path = "/" + path;
        }
        m.put(Message.REQUEST_URI, baseAddress + path);
```

### AssignmentToMethodParameter
Assignment to method parameter `sequence`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringConfigurationFactory.java`
#### Snippet
```java
    protected static Map<String, String> parseMapSequence(String sequence) {
        if (sequence != null) {
            sequence = sequence.trim();
            Map<String, String> map = new HashMap<>();
            String[] pairs = sequence.split(",");
```

### AssignmentToMethodParameter
Assignment to method parameter `appClassName`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
        }
        Map<String, List<String>> props = new HashMap<>();
        appClassName = getClassNameAndProperties(appClassName, props);
        Class<?> appClass = loadApplicationClass(appClassName);
        ApplicationInfo appInfo = (ApplicationInfo)createSingletonInstance(appClass, props, servletConfig);
```

### AssignmentToMethodParameter
Assignment to method parameter `sequence`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
    protected static Map<String, List<String>> parseMapListSequence(String sequence) {
        if (sequence != null) {
            sequence = sequence.trim();
            Map<String, List<String>> map = new HashMap<>();
            String[] pairs = sequence.split(" ");
```

### AssignmentToMethodParameter
Assignment to method parameter `catalogLocation`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/schemas/SchemaHandler.java`
#### Snippet
```java
            final OASISCatalogManager catalogResolver = OASISCatalogManager.getCatalogManager(bus);
            if (catalogResolver != null) {
                catalogLocation = catalogLocation == null
                    ? SchemaHandler.DEFAULT_CATALOG_LOCATION : catalogLocation;
                URL catalogURL = ResourceUtils.getResourceURL(catalogLocation, bus);
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        try (URIResolver resolver = new URIResolver(base, target)) {
            if (resolver.isResolved()) {
                target = resolver.getURI().toString();
            }
        } catch (Exception ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `classLoader`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java

        if (classLoader == null) {
            classLoader = Thread.currentThread().getContextClassLoader();
        }
        LOG.log(Level.FINE, "Creating client from WSDL " + wsdlUrl);
```

### AssignmentToMethodParameter
Assignment to method parameter `element`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
            return element;
        }
        element = (Element)cloneNode(element.getOwnerDocument(), element, true);
        List<Node> ns = new ArrayList<>();

```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
            if (locIdx != -1
                && index >= locIdx) {
                ++index;
            }
            return index;
```

### AssignmentToMethodParameter
Assignment to method parameter `result`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        for (String mName : methodNames) {
            try {
                result = evaluateFactoryMethod(value, cls, mName);
                if (result != null) {
                    factoryMethodEx = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `scope`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                return type;
            }
            scope = type.getRawType();
        }
        if (scope instanceof Class) {
```

### AssignmentToMethodParameter
Assignment to method parameter `paramType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        try {
            if (paramType.isInterface()) {
                paramType = org.apache.cxf.jaxrs.utils.JAXBUtils.getValueTypeFromAdapter(paramType,
                                                                                         paramType,
                                                                                         paramAnns);
```

### AssignmentToMethodParameter
Assignment to method parameter `paramCls`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            if (paramCls.isAssignableFrom(clsType)
                || clsType != Object.class && !clsType.isInterface() && clsType.isAssignableFrom(paramCls)) {
                paramCls = clsType;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
    public static void invokeLifeCycleMethod(Object instance, Method method) {
        if (method != null) {
            method = InjectionUtils.checkProxy(method, instance);
            try {
                method.invoke(instance, new Object[]{});
```

### AssignmentToMethodParameter
Assignment to method parameter `paramValues`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (paramValues == null || paramValues.size() == 1 && paramValues.get(0) == null) {
            if (defaultValue != null) {
                paramValues = Collections.singletonList(defaultValue);
            } else {
                if (paramType.isPrimitive()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `paramValues`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            } else {
                if (paramType.isPrimitive()) {
                    paramValues = Collections.singletonList(
                        boolean.class == paramType ? "false"
                            : char.class == paramType ? Character.toString('\u0000') : "0");
```

### AssignmentToMethodParameter
Assignment to method parameter `paramValues`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                            : char.class == paramType ? Character.toString('\u0000') : "0");
                } else if (InjectionUtils.isSupportedCollectionOrArray(paramType)) {
                    paramValues = Collections.emptyList();
                } else {
                    return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                return pClass.cast(new PathSegmentImpl(value, decoded));
            } else if (!MessageUtils.getContextualBoolean(message, IGNORE_MATRIX_PARAMETERS)) {
                value = new PathSegmentImpl(value, false).getPath();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        }

        value = decodeValue(value, decoded, pType);

        final Optional<ParamConverter<T>> converter = getParamConverter(pClass, genericType, paramAnns, message);
```

### AssignmentToMethodParameter
Assignment to method parameter `pClass`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (Boolean.class == pClass) {
            // allow == checks for Boolean object
            pClass = (Class<T>) Boolean.TYPE;
        }
        if (pClass.isPrimitive()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java

        if (type instanceof TypeVariable) {
            type = InjectionUtils.getSuperType(serviceCls, (TypeVariable<?>)type);
        } else if (type instanceof ParameterizedType) {
            ParameterizedType pt = (ParameterizedType)type;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                Type theType = InjectionUtils.getSuperType(serviceCls, typeVar);
                if (theType instanceof Class) {
                    type = new ParameterizedCollectionType((Class<?>)theType);
                } else {
                    type = processGenericTypeIfNeeded(serviceCls, paramCls, theType);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    type = new ParameterizedCollectionType((Class<?>)theType);
                } else {
                    type = processGenericTypeIfNeeded(serviceCls, paramCls, theType);
                    type = new ParameterizedCollectionType(type);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                } else {
                    type = processGenericTypeIfNeeded(serviceCls, paramCls, theType);
                    type = new ParameterizedCollectionType(type);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java

        if (type == null || type == Object.class) {
            type = paramCls;
        }
        return type;
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (!ParameterizedType.class.isAssignableFrom(genericType.getClass())) {
            if (genericType instanceof TypeVariable) {
                genericType = getType(((TypeVariable<?>)genericType).getBounds(), pos);
            } else if (genericType instanceof WildcardType) {
                WildcardType wildcardType = (WildcardType)genericType;
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    bounds = wildcardType.getUpperBounds();
                }
                genericType = getType(bounds, pos);
            } else if (genericType instanceof GenericArrayType) {
                genericType = ((GenericArrayType)genericType).getGenericComponentType();
```

### AssignmentToMethodParameter
Assignment to method parameter `genericType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                genericType = getType(bounds, pos);
            } else if (genericType instanceof GenericArrayType) {
                genericType = ((GenericArrayType)genericType).getGenericComponentType();
            }
            final Class<?> cls;
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
    public static ServerProviderFactory createInstance(Bus bus) {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus();
        }
        ServerProviderFactory factory = new ServerProviderFactory(bus);
```

### AssignmentToMethodParameter
Assignment to method parameter `is`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/BinaryDataProvider.java`
#### Snippet
```java
            if (InputStream.class.isAssignableFrom(clazz)) {
                if (DigestInputStream.class.isAssignableFrom(clazz)) {
                    is = new MessageDigestInputStream(is);
                }
                return clazz.cast(is);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
        boolean isCollection = false;
        if (InjectionUtils.isSupportedCollectionOrArray(type)) {
            type = InjectionUtils.getActualType(genericType);
            isCollection = true;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
                                                      Object.class,
                                                      genericType);
            type = InjectionUtils.getActualType(theType);
        }
        if (type == null
```

### AssignmentToMethodParameter
Assignment to method parameter `m`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
                                                      Type[] genericTypes) {
        if (m == null) {
            m = new MessageImpl();
        }
        @SuppressWarnings("unchecked")
```

### AssignmentToMethodParameter
Assignment to method parameter `anns`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
                mc.put("org.apache.cxf.multipart.embedded.input",
                       multipart.getDataHandler().getInputStream());
                anns = new Annotation[]{};
            }
            MessageBodyReader<T> r =
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
    public boolean isReadable(Class<?> type, Type genericType, Annotation[] anns, MediaType mt) {
        if (InjectionUtils.isSupportedCollectionOrArray(type)) {
            type = InjectionUtils.getActualType(genericType);
            if (type == null) {
                return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java

        if (InjectionUtils.isSupportedCollectionOrArray(type)) {
            type = InjectionUtils.getActualType(genericType);
            if (type == null) {
                return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        DocumentDepthProperties props = getDepthProperties();
        if (props != null && props.isEffective()) {
            reader = TransformUtils.createNewReaderIfNeeded(reader, is);
            reader = new DepthRestrictingStreamReader(reader, props);
        } else if (reader != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        if (props != null && props.isEffective()) {
            reader = TransformUtils.createNewReaderIfNeeded(reader, is);
            reader = new DepthRestrictingStreamReader(reader, props);
        } else if (reader != null) {
            reader = configureReaderRestrictions(reader);
```

### AssignmentToMethodParameter
Assignment to method parameter `reader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
            reader = new DepthRestrictingStreamReader(reader, props);
        } else if (reader != null) {
            reader = configureReaderRestrictions(reader);
        }
        return reader;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
    private static String getLocalName(String name, String clsName, boolean pluralName) {
        if (JAXB_DEFAULT_NAME.equals(name)) {
            name = clsName;
            if (name.length() > 1) {
                name = name.substring(0, 1).toLowerCase() + name.substring(1);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
            name = clsName;
            if (name.length() > 1) {
                name = name.substring(0, 1).toLowerCase() + name.substring(1);
            } else {
                name = name.toLowerCase();
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
                name = name.substring(0, 1).toLowerCase() + name.substring(1);
            } else {
                name = name.toLowerCase();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        }
        if (pluralName) {
            name += 's';
        }
        return name;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
                           String enc, OutputStream os, MediaType mt,
                           Annotation[] anns) throws Exception {
        obj = convertToJaxbElementIfNeeded(obj, cls, genericType);
        if (obj instanceof JAXBElement && cls != JAXBElement.class) {
            cls = JAXBElement.class;
```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
        obj = convertToJaxbElementIfNeeded(obj, cls, genericType);
        if (obj instanceof JAXBElement && cls != JAXBElement.class) {
            cls = JAXBElement.class;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
    //CHECKSTYLE:ON
        if (!(obj instanceof JAXBElement)) {
            obj = convertToJaxbElementIfNeeded(obj, cls, genericType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java

        if (obj instanceof JAXBElement && cls != JAXBElement.class) {
            cls = JAXBElement.class;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `mt`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
                List<MediaType> sorted =
                    JAXRSUtils.sortMediaTypes(JAXRSUtils.getMediaTypes(pm.value()), JAXRSUtils.MEDIA_TYPE_QS_PARAM);
                mt = JAXRSUtils.intersectMimeTypes(sorted, mt).get(0);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `mt`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
        if (mt.isWildcardType() || mt.isWildcardSubtype()) {
            if ("application".equals(mt.getType()) || mt.isWildcardType()) {
                mt = MediaType.APPLICATION_OCTET_STREAM_TYPE;
            } else {
                throw ExceptionUtils.toNotAcceptableException(null,  null);
```

### AssignmentToMethodParameter
Assignment to method parameter `anns`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                                                        Message m) {

        anns = anns != null ? anns : new Annotation[]{};
        for (ProviderInfo<ParamConverterProvider> pi : paramConverters) {
            injectContextValues(pi, m);
```

### AssignmentToMethodParameter
Assignment to method parameter `names`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            return Collections.emptyList();
        }
        names = names == null ? Collections.<String>emptySet() : names;

        MultivaluedMap<ProviderInfo<T>, String> map =
```

### AssignmentToMethodParameter
Assignment to method parameter `expectedType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                    }
                    if (expectedType.isArray() && !actualClass.isArray()) {
                        expectedType = expectedType.getComponentType();
                    }
                    if (actualClass.isAssignableFrom(expectedType) || actualClass == Object.class) {
```

### AssignmentToMethodParameter
Assignment to method parameter `protectedHttpHeaders`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JoseJaxrsUtils.java`
#### Snippet
```java
                                          Set<String> protectedHttpHeaders) {
        if (protectedHttpHeaders == null) {
            protectedHttpHeaders = DEFAULT_PROTECTED_HTTP_HEADERS;
        }
        for (String headerName : protectedHttpHeaders) {
```

### AssignmentToMethodParameter
Assignment to method parameter `protectedHttpHeaders`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JoseJaxrsUtils.java`
#### Snippet
```java
                                           Set<String> protectedHttpHeaders) {
        if (protectedHttpHeaders == null) {
            protectedHttpHeaders = DEFAULT_PROTECTED_HTTP_HEADERS;
        }
        Map<String, String> joseHttpHeaders = new HashMap<>();
```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
                System.arraycopy(cls, 0, cls2, 0, cls.length);
                cls2[cls.length] = seiClass;
                cls = cls2;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodDispatcher.java`
#### Snippet
```java
                        }
                    }
                    method = endpointClass.getMethod(method.getName(),
                                                     params);
                } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodDispatcher.java`
#### Snippet
```java
                                                     params);
                } else {
                    method = m2;
                }
                try {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodDispatcher.java`
#### Snippet
```java
    public BindingOperationInfo getBindingOperation(Method method, Endpoint endpoint) {
        try {
            method = getImplementationMethod(method);
        } catch (NoSuchMethodException e) {
            // ignore
```

### AssignmentToMethodParameter
Assignment to method parameter `params`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java
        try {
            if ((params == null || params.isEmpty()) && serviceObject instanceof Provider) {
                params = Collections.singletonList(null);
            }
            res = CastUtils.cast((List<?>)super.invoke(exchange, serviceObject, m, params));
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
            protected Throwable mapThrowable(Throwable t) {
                if (t instanceof Exception) {
                    t = mapException(null, oi, (Exception)t);
                }
                return t;
```

### AssignmentToMethodParameter
Assignment to method parameter `params`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
            Object[] newParams = new Object[params.length - 1];
            System.arraycopy(params, 0, newParams, 0, newParams.length);
            params = newParams;
        } else {
            handler = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `interfaceName`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
            try {
                Definition def = wsdlManager.getDefinition(wsdlDocumentLocation);
                interfaceName = def.getService(serviceName).getPort(portName.getLocalPart()).getBinding()
                    .getPortType().getQName();
            } catch (Exception e) {
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
            writer.writeStartElement(JAXWSAConstants.WSA_PREFIX, JAXWSAConstants.WSA_ADDRESS_NAME,
                                     JAXWSAConstants.NS_WSA);
            address = address == null ? "" : address;
            writer.writeCharacters(address);
            writer.writeEndElement();
```

### AssignmentToMethodParameter
Assignment to method parameter `hasOpName`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                        // Verification of the provided operation name failed.
                        // Resolve the operation name from the payload element.
                        hasOpName = false;
                        payloadOPMap = createPayloadEleOpNameMap(
                                client.getEndpoint().getBinding().getBindingInfo(), hasOpName);
```

### AssignmentToMethodParameter
Assignment to method parameter `opName`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                        .getOperation(dispatchedOpName);
                    if (dbop != null) {
                        opName = dispatchedOpName;
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                    return t;
                } else if (t instanceof Exception) {
                    t = mapException((Exception)t);
                }
                return t;
```

### AssignmentToMethodParameter
Assignment to method parameter `portName`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
            EndpointInfo ei = ServiceModelUtil.findBestEndpointInfo(portTypeName, service.getServiceInfos());
            if (ei != null) {
                portName = ei.getName();
            } else {
                portName = serviceFactory.getEndpointName();
```

### AssignmentToMethodParameter
Assignment to method parameter `portName`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                portName = ei.getName();
            } else {
                portName = serviceFactory.getEndpointName();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointReference`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    public <T> T getPort(EndpointReferenceType endpointReference, Class<T> type,
                         WebServiceFeature... features) {
        endpointReference = EndpointReferenceUtils.resolve(endpointReference, bus);
        QName serviceQName = EndpointReferenceUtils.getServiceName(endpointReference, bus);
        String portName = EndpointReferenceUtils.getPortName(endpointReference);
```

### AssignmentToMethodParameter
Assignment to method parameter `b`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    final void initialize(Bus b, URL url, WebServiceFeature ... f) {
        if (b == null) {
            b = BusFactory.getThreadDefaultBus(true);
        }
        serviceFeatures = f;
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                if (null != uri) {
                    try {
                        url = uri.toURL();
                    } catch (MalformedURLException e) {
                        LOG.log(Level.FINE, "resolve qname failed", serviceName);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
                   || "http://schemas.xmlsoap.org/wsdl/soap12/?mtom=true".contains(id);
        } else if ("##XML_HTTP".equals(name)) {
            name = "http://www.w3.org/2004/08/wsdl/http";
        }
        return name.contains(id);
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
            node = node.getNextSibling();
            if (cur instanceof Element) {
                el = (Element)cur;
                if (!namespace.equals(el.getNamespaceURI())) {
                    String xml = StaxUtils.toString(el);
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
        while (node != null) {
            if (node instanceof Element) {
                el = (Element)node;

                if (!namespace.equals(el.getNamespaceURI())) {
```

### AssignmentToMethodParameter
Assignment to method parameter `handlerChain`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java

        if (!outbound) {
            handlerChain = reverseHandlerChain(handlerChain);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `handlerChain`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java

        if (!outbound) {
            handlerChain = reverseHandlerChain(handlerChain);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java

    public final Object remove(Object key) {
        key = mapKey((String)key);
        scopes.remove(key);
        if (BindingProvider.PASSWORD_PROPERTY.equals(key)
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                writer.close();
                if (mode  == Service.Mode.MESSAGE) {
                    s = new DOMSource(m.getSOAPPart());
                } else {
                    s = new DOMSource(SAAJUtils.getBody(m).getFirstChild());
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                    s = new DOMSource(m.getSOAPPart());
                } else {
                    s = new DOMSource(SAAJUtils.getBody(m).getFirstChild());
                }
                W3CDOMStreamReader r = new W3CDOMStreamReader(DOMUtils.getFirstElement(SAAJUtils.getBody(m)));
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                        SOAPMessage msg = initSOAPMessage(null);
                        write(s, SAAJUtils.getBody(msg));
                        s = new DOMSource(msg.getSOAPPart());
                    } catch (Exception e) {
                        throw new Fault(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `c`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
                }
            }
            c = c.getSuperclass();
        }
        return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `ct`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
                return AttachmentUtil.getUniqueBoundaryValue();
            }
            ct = ct.substring(idx + 9);
            if (ct.charAt(0) == '"') {
                ct = ct.substring(1);
```

### AssignmentToMethodParameter
Assignment to method parameter `ct`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
            ct = ct.substring(idx + 9);
            if (ct.charAt(0) == '"') {
                ct = ct.substring(1);
                idx = ct.indexOf('"');
                return ct.substring(0, idx);
```

### AssignmentToMethodParameter
Assignment to method parameter `ct`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
                return "root";
            }
            ct = ct.substring(idx + 6);
            if (ct.charAt(0) == '"') {
                ct = ct.substring(1);
```

### AssignmentToMethodParameter
Assignment to method parameter `ct`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
            ct = ct.substring(idx + 6);
            if (ct.charAt(0) == '"') {
                ct = ct.substring(1);
                idx = ct.indexOf('"');
                return ct.substring(0, idx);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
                                   int curSize, String prefix, boolean input) {
        int partIndex = getPartIndex(method, paramNumber, input);
        method = getDeclaredMethod(method);
        WebParam param = getWebParam(method, paramNumber);
        String tns = null;
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public String getResponseWrapperPartName(OperationInfo op, Method method) {
        method = getDeclaredMethod(method);
        WebResult webResult = getWebResult(method);
        ResponseWrapper rw = method.getAnnotation(ResponseWrapper.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public QName getOutPartName(OperationInfo op, Method method, int paramNumber) {
        method = getDeclaredMethod(method);

        if (paramNumber >= 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public Boolean isHeader(Method method, int j) {
        method = getDeclaredMethod(method);
        if (j >= 0) {
            WebParam webParam = getWebParam(method, j);
```

### AssignmentToMethodParameter
Assignment to method parameter `m`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return getServiceFactory().getWrapped();
        }
        m = getDeclaredMethod(m);

        SOAPBinding ann = m.getAnnotation(SOAPBinding.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
                              int paramNumber, MessageInfo mi, String prefix, boolean isIn) {
        int partIndex = getPartIndex(method, paramNumber, isIn);
        method = getDeclaredMethod(method);
        WebParam param = getWebParam(method, paramNumber);
        String tns = mi.getName().getNamespaceURI();
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public QName getOperationName(InterfaceInfo intf, Method method) {
        method = getDeclaredMethod(method);

        WebMethod wm = method.getAnnotation(WebMethod.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public Boolean isOutParam(Method method, int j) {
        method = getDeclaredMethod(method);
        if (j == -1) {
            return !method.getReturnType().equals(void.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public String getAction(OperationInfo op, Method method) {
        method = getDeclaredMethod(method);
        WebMethod wm = method.getAnnotation(WebMethod.class);
        String action = "";
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public QName getOutParameterName(OperationInfo op, Method method, int paramNumber) {
        method = getDeclaredMethod(method);

        if (paramNumber >= 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public Boolean isInOutParam(Method method, int j) {
        method = getDeclaredMethod(method);
        if (j == -1) {
            return !method.getReturnType().equals(void.class);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public String getRequestWrapperPartName(OperationInfo op, Method method) {
        method = getDeclaredMethod(method);
        RequestWrapper rw = method.getAnnotation(RequestWrapper.class);
        if (rw != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        if (!method.getDeclaringClass().equals(endpointClass)) {
            try {
                method = endpointClass.getMethod(method.getName(), method.getParameterTypes());
            } catch (SecurityException e) {
                throw new ServiceConstructionException(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    @Override
    public Boolean hasOutMessage(Method method) {
        method = getDeclaredMethod(method);
        return !method.isAnnotationPresent(Oneway.class);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }

        method = getDeclaredMethod(method);

        WebParam webParam = getWebParam(method, j);
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPTransportFactory.java`
#### Snippet
```java
        LOG.log(Level.FINE, "Creating conduit for {0}", ei.getAddress());
        if (target == null) {
            target = createReference(ei);
        }
        return new UDPConduit(target, bus);
```

### AssignmentToMethodParameter
Assignment to method parameter `reference`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPTransportFactory.java`
#### Snippet
```java
        throws IOException {
        if (reference == null) {
            reference = createReference(ei);
        }
        return new UDPDestination(bus, reference, ei);
```

### AssignmentToMethodParameter
Assignment to method parameter `out`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
            exchange.setInMessage(m);
            m.setContent(InputStream.class, in);
            out = new UDPDestinationOutputStream(out);
            m.put(OutputStream.class, out);
            queue.execute(() -> getMessageObserver().onMessage(m));
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
                int nlen = buffer.remaining();
                buffer.put(b, off, nlen);
                len -= nlen;
                off += nlen;
                send();
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
                buffer.put(b, off, nlen);
                len -= nlen;
                off += nlen;
                send();
                buffer = IoBuffer.allocate((64 * 1024) - 42);
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java
                int nlen = buffer.remaining();
                buffer.put(b, off, nlen);
                len -= nlen;
                off += nlen;
                send();
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java
                buffer.put(b, off, nlen);
                len -= nlen;
                off += nlen;
                send();
                buffer = IoBuffer.allocate((64 * 1024) - 42);
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
    @Override
    protected void initializeWSDLOperation(InterfaceInfo intf, OperationInfo o, Method method) {
        method = ((JaxWsServiceConfiguration)jaxWsConfiguration).getDeclaredMethod(method);
        o.setProperty(Method.class.getName(), method);
        o.setProperty(METHOD, method);
```

### AssignmentToMethodParameter
Assignment to method parameter `continuation`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                    boolean isOneWay = message.getExchange().isOneWay();
                    message.getExchange().setOneWay(false);
                    continuation = p.getContinuation();
                    message.getExchange().setOneWay(isOneWay);
                    message.put(Continuation.class, continuation);
```

### AssignmentToMethodParameter
Assignment to method parameter `reference`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/InternalContextUtils.java`
#### Snippet
```java
            try {
                if (reference == null) {
                    reference = ContextUtils.getNoneEndpointReference();
                }
                Conduit backChannel = target.getBackChannel(inMessage);
```

### AssignmentToMethodParameter
Assignment to method parameter `sp`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    public void setSourcePolicy(SourcePolicyType sp) {
        if (null == sp) {
            sp = new SourcePolicyType();
        }
        if (sp.getSequenceTerminationPolicy() == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `message`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/DecoupledFaultHandler.java`
#### Snippet
```java
                //Just keep the wsa headers to remove the not understand headers
                if (exchange.getOutMessage() != null) {
                    message = (SoapMessage)exchange.getOutMessage();
                }
                Iterator<Header> iterator = message.getHeaders().iterator();
```

### AssignmentToMethodParameter
Assignment to method parameter `params`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
            acks.setAddress(attrURIType);
            client = createClient(bus, endpoint, protocol, c, acks);
            params = new Object[] {};
        } else {
            EndpointReferenceType replyTo = reliableEndpoint.getReplyTo();
```

### AssignmentToMethodParameter
Assignment to method parameter `reference`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
            try {
                if (reference == null) {
                    reference = ContextUtils.getNoneEndpointReference();
                }
                Conduit backChannel = target.getBackChannel(inMessage);
```

### AssignmentToMethodParameter
Assignment to method parameter `faultTo`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
        }
        if (faultTo == null) {
            faultTo = replyTo;
        }
        boolean anonReply = ContextUtils.isGenericAddress(replyTo);
```

### AssignmentToMethodParameter
Assignment to method parameter `maps`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
            if (maps == null) {
                Message m = message.getExchange().getOutMessage();
                maps = ContextUtils.retrieveMAPs(m, false, true, false);
                if (maps != null) {
                    Exchange ex = uncorrelatedExchanges.get(maps.getMessageID().getValue());
```

### AssignmentToMethodParameter
Assignment to method parameter `maps`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                //there is an Action header that points to a fault and no relatesTo.  Use the out map for the ID
                Message m = message.getExchange().getOutMessage();
                maps = ContextUtils.retrieveMAPs(m, false, true, false);
                if (maps != null) {
                    uncorrelatedExchanges.remove(maps.getMessageID().getValue());
```

### AssignmentToMethodParameter
Assignment to method parameter `source`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                //OK. we have extra namespaces.  We'll need to wrapper the reader
                //with a new one that will fake extra namespace events
                source = new DepthXMLStreamReader(source) {
                    public int getNamespaceCount() {
                        if (getDepth() == 0 && isStartElement()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `source`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            String type = reader.getAttributeValue(Constants.URI_2001_SCHEMA_XSI, "type");
            if (StringUtils.isEmpty(type)) {
                source = new AddXSITypeStreamReader(reader, typeQName);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `source`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            && !(part.getXmlSchema() instanceof XmlSchemaElement)) {
            //Validating RPC/Lit, make sure we don't try a root element name thing
            source = updateSourceWithXSIType(source, part.getTypeQName());
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `cls`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        }
        if (cls == null && mObj != null) {
            cls = mObj.getClass();
        }
        return new JAXBElement(elName, cls, mObj);
```

### AssignmentToMethodParameter
Assignment to method parameter `arg0`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                            int i = super.getNamespaceCount();
                            if (arg0 >= i) {
                                arg0 -= i;
                                return namespaces[arg0].getValue();
                            }
```

### AssignmentToMethodParameter
Assignment to method parameter `arg0`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                            int i = super.getNamespaceCount();
                            if (arg0 >= i) {
                                arg0 -= i;
                                return namespaces[arg0].getKey();
                            }
```

### AssignmentToMethodParameter
Assignment to method parameter `elValue`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        if (cls != null && cls.isArray() && elValue instanceof Collection) {
            Collection<?> col = (Collection<?>)elValue;
            elValue = col.toArray((Object[])Array.newInstance(cls.getComponentType(), col.size()));
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JCacheOAuthDataProvider.java`
#### Snippet
```java
    protected static CacheManager createCacheManager(String configFile, Bus bus) {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus(true);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        if (value != null && value.startsWith("\"")
            && value.endsWith("\"") && value.length() > 1) {
            value = value.substring(1,  value.length() - 1);
        }
        return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `m`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    private static Object createHttpHeaders(Message m, Class<?> ctxClass) {
        if (MessageUtils.isRequestor(m)) {
            m = m.getExchange() != null ? m.getExchange().getOutMessage() : m;
        }
        return HttpHeaders.class.isAssignableFrom(ctxClass) ? new HttpHeadersImpl(m)
```

### AssignmentToMethodParameter
Assignment to method parameter `q`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        }
        if (q.charAt(0) == '.') {
            q = '0' + q;
        }
        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        if (value != null) {
            if (decodePlus && value.contains("+")) {
                value = value.replace('+', ' ');
            }
            if (decode) {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }
            if (decode) {
                value = (";".equals(sep))
                    ? HttpUtils.pathDecode(value) : HttpUtils.urlDecode(value);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `m`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    private static UriInfo createUriInfo(Message m) {
        if (MessageUtils.isRequestor(m)) {
            m = m.getExchange() != null ? m.getExchange().getOutMessage() : m;
        }
        MultivaluedMap<String, String> templateParams =
```

### AssignmentToMethodParameter
Assignment to method parameter `stateString`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JoseSessionTokenProvider.java`
#### Snippet
```java
        }
        if (jws != null) {
            stateString = JwsUtils.sign(jws, stateString, null);
        }
        if (jwe != null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `stateString`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JoseSessionTokenProvider.java`
#### Snippet
```java
        }
        if (jwe != null) {
            stateString = jwe.encrypt(StringUtils.toBytesUTF8(stateString), null);
        }
        return stateString;
```

### AssignmentToMethodParameter
Assignment to method parameter `endpointAddress`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadataService.java`
#### Snippet
```java

    protected static String calculateEndpointAddress(String endpointAddress, String baseUri, String defRelAddress) {
        endpointAddress = endpointAddress != null ? endpointAddress : defRelAddress;
        if (isAbsoluteUri(endpointAddress)) {
            return endpointAddress;
```

### AssignmentToMethodParameter
Assignment to method parameter `requestedClaims`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        // Claims
        if (requestedClaims == null || requestedClaims.isEmpty()) {
            requestedClaims = defaultClaims;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `scriptLocation`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/JexlClaimsMapper.java`
#### Snippet
```java
        URL resource = ClassLoaderUtils.getResource(scriptLocation, this.getClass());
        if (resource != null) {
            scriptLocation = resource.getPath();
            if (LOG.isLoggable(Level.FINE)) {
                LOG.fine("Script found within Classpath: " + scriptLocation);
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
     */
    public ProcessedClaim wrapValues(ProcessedClaim processedClaim, String prefix, String suffix) {
        prefix = (prefix == null)
            ? ""
            : prefix;
```

### AssignmentToMethodParameter
Assignment to method parameter `suffix`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
            ? ""
            : prefix;
        suffix = (suffix == null)
            ? ""
            : suffix;
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
        } else if ("-a".equals(args[index])) {
            bOptSeen = false;
            if (++index == args.length) {
                throw new Exception(WRONG_ADDR_ERR);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `index`
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
        } else if ("-b".equals(args[index])) {
            bOptSeen = true;
            if (++index == args.length) {
                throw new Exception(WRONG_BASE_ERR);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `params`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java

        if (authorizationFilter != null) {
            params = authorizationFilter.process(params, userSubject, client);
        }
        // Validate the provided request URI, if any, against the ones Client provided
```

### AssignmentToMethodParameter
Assignment to method parameter `redirectUri`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
            }
        } else if (uris.size() == 1 && useRegisteredRedirectUriIfPossible) {
            redirectUri = uris.get(0);
        }
        if (redirectUri == null && uris.isEmpty() && !canRedirectUriBeEmpty(client)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `token`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/AbstractOperation.java`
#### Snippet
```java
                try {
                    tokenResponse = tokenValidator.validateToken(validatorParameters);
                    token = tokenResponse.getToken();
                    // The parsed principal/roles is set if available. It's up to other
                    // components to deal with the STATE of the validation
```

### AssignmentToMethodParameter
Assignment to method parameter `addMethodName`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
            if (names != null && names[paramNumber] != null) {
                paramName = names[paramNumber];
                addMethodName = false;
            } else {
                paramName = flow + currentSize;
```

### AssignmentToMethodParameter
Assignment to method parameter `method`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
        if (impl != null) {
            try {
                method = impl.getMethod(method.getName(), method.getParameterTypes());
            } catch (Exception e) {
                throw new ServiceConstructionException(e);
```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
            }

            n -= c;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        //string as the documentBaseLocation which would result in it being held onto strongly
        //from the definition.  With this, the String the definition holds onto would be unique
        url = new String(url);
        CatalogWSDLLocator catLocator = new CatalogWSDLLocator(url, bus);
        final ResourceManagerWSDLLocator wsdlLocator = new ResourceManagerWSDLLocator(url,
```

### AssignmentToMethodParameter
Assignment to method parameter `bop`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
        //bop might be a unwrapped, wrap it back so that we can get correct info
        if (bop != null && bop.isUnwrapped()) {
            bop = bop.getWrappedOperation();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `bop`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
                ? new QName("http://cxf.apache.org/jaxws/provider", "invoke")
                : xmlReader.getName();
            bop = getBindingOperationInfo(exchange, startQName, client);
        }
        return bop;
```

### AssignmentToMethodParameter
Assignment to method parameter `encodedUrl`
in `testutils/src/main/java/org/apache/cxf/testutil/common/EmbeddedJMSBrokerLauncher.java`
#### Snippet
```java
        try {
            if (encodedUrl == null) {
                encodedUrl = url;
            }
            if (bus == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `testutils/src/main/java/org/apache/cxf/testutil/common/EmbeddedJMSBrokerLauncher.java`
#### Snippet
```java
            }
            if (bus == null) {
                bus = BusFactory.getThreadDefaultBus();
            }
            Definition def = bus.getExtension(WSDLManager.class)
```

### AssignmentToMethodParameter
Assignment to method parameter `os`
in `testutils/src/main/java/org/apache/cxf/testutil/recorders/OutMessageRecorder.java`
#### Snippet
```java
            WriteOnCloseOutputStream cached = new WriteOnCloseOutputStream(os);
            message.setContent(OutputStream.class, cached);
            os = cached;
        }
        return (WriteOnCloseOutputStream) os;
```

### AssignmentToMethodParameter
Assignment to method parameter `resourcePath`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResourceLocator.java`
#### Snippet
```java
    public URL locate(String resourcePath) throws MalformedURLException {
        if (StringUtils.isEmpty(resourcePath) || "/".equals(resourcePath)) {
            resourcePath = "index.html";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `resourcePath`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResourceLocator.java`
#### Snippet
```java

        if (resourcePath.startsWith("/")) {
            resourcePath = resourcePath.substring(1);
        }
        URL ret;
```

### AssignmentToMethodParameter
Assignment to method parameter `urlStr`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResolver.java`
#### Snippet
```java
            }
            if (!urlStr.endsWith("/")) {
                urlStr = "jar:" + urlStr + "!/";
            }
            return urlStr + UI_RESOURCES_ROOT_START + version + "/";
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Type c = getHolderType(rawClass, type);
            if (c != null) {
                type = c;
                rawClass = getClass(type);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `rawClass`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            if (c != null) {
                type = c;
                rawClass = getClass(type);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `rawClass`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                Class<?> c = parameterizedTypes.get(gd).get(var.getName());
                if (c != null) {
                    rawClass = c;
                    type = c;
                    part.getMessageInfo().setProperty("parameterized", Boolean.TRUE);
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                if (c != null) {
                    rawClass = c;
                    type = c;
                    part.getMessageInfo().setProperty("parameterized", Boolean.TRUE);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `o`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                }
                setFaultClassInfo(o, method);
                o = o.getUnwrappedOperation();
            } else {
                LOG.warning(new Message("COULD_NOT_UNWRAP", LOG, o.getName(), method).toString());
```

### AssignmentToMethodParameter
Assignment to method parameter `location`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
        // so that we don't try re-create another ORB on the same host and port.
        if (CorbaUtils.isIOR(location)) {
            location = IOR_SHARED_KEY;
        }
        orb = CorbaBindingHelper.getAddressSpecificORB(location, props, orbArgs);
```

### AssignmentToMethodParameter
Assignment to method parameter `idlType`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
                }
                tc = tc.content_type();
                idlType = alias.getBasetype();
            }
        } catch (Throwable ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `idlType`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
                }
                tc = tc.content_type();
                idlType = alias.getBasetype();
            }
        } catch (Throwable ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `exClassName`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
                    Class<?> faultClass = faultMethod.getReturnType();
                    XmlType exType = faultClass.getAnnotation(XmlType.class);
                    exClassName = exType.name();
                    result = findRaisesType(exList, exClassName);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
        int pos = name.lastIndexOf('.');
        if (pos != -1) {
            name = name.substring(pos + 1);
        }
        return name;
```

### AssignmentToMethodParameter
Assignment to method parameter `obj`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            if (tc == null) {
                if (obj == null) {
                    obj = typeMap.getType(type.getLocalPart());
                    if (obj == null) {
                        throw new CorbaBindingException("Unable to locate object definition");
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `integration/jca/src/main/java/org/apache/cxf/jca/jarloader/JarLoader.java`
#### Snippet
```java

        if (index > -1) {
            name = name.substring(index);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `registry`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketTransportFactory.java`
#### Snippet
```java
        super(DEFAULT_NAMESPACES);
        if (registry == null) {
            registry = new DestinationRegistryImpl();
        }
        this.registry = registry;
```

### AssignmentToMethodParameter
Assignment to method parameter `registry`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
        // REVISIT other way of getting the registry of http so that the plain cxf servlet finds the
        // destination?
        registry = getDestinationRegistry(bus);

        // choose atmosphere if available, otherwise assume jetty is available
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/ByteBufferInputStream.java`
#### Snippet
```java
        }

        len = Math.min(len, buf.remaining());
        buf.get(bytes, off, len);
        return len;
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
                    byte[] headers = respid != null
                        ? WebSocketUtils.buildHeaderLine(WebSocketConstants.DEFAULT_RESPONSE_ID_KEY, respid) : null;
                    data = WebSocketUtils.buildResponse(headers, data, offset, length);
                    webSocketHolder.write(data, 0, data.length);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
                    byte[] headers = respid != null
                        ? WebSocketUtils.buildHeaderLine(WebSocketConstants.DEFAULT_RESPONSE_ID_KEY, respid) : null;
                    data = WebSocketUtils.buildResponse(headers, data, offset, length);
                    WebSockets.sendText(ByteBuffer.wrap(data), channel, null);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
        if (SoapJMSConstants.SOAP_JMS_SPECIFICIATION_TRANSPORTID.equals(s)
            || (address != null && address.startsWith("jms"))) {
            s = "http://cxf.apache.org/transports/jms";
        } else if (address != null && address.startsWith("soap.udp")) {
            s = "http://cxf.apache.org/transports/udp";
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
            s = "http://cxf.apache.org/transports/jms";
        } else if (address != null && address.startsWith("soap.udp")) {
            s = "http://cxf.apache.org/transports/udp";
        } else if (SOAP_11_HTTP_BINDING.equals(s)
            || SOAP_12_HTTP_BINDING.equals(s)
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
            || "http://schemas.xmlsoap.org/wsdl/soap/http/".equals(s)
            || "http://schemas.xmlsoap.org/wsdl/http/".equals(s)) {
            s = "http://cxf.apache.org/transports/http";
        }
        return s;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/saaj/SAAJUtils.java`
#### Snippet
```java
    public static Element adjustPrefix(Element e, String prefix) {
        if (prefix == null) {
            prefix = "";
        }
        try {
```

### AssignmentToMethodParameter
Assignment to method parameter `nextId`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
                pi.setProperty(HEADER, Boolean.TRUE);
                pi.setIndex(nextId);
                nextId++;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `attParts`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java

                if (attParts == null) {
                    attParts = new LinkedList<>();
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/Option.java`
#### Snippet
```java
    private <T> T setIfNull(T dest, T source) {
        if (dest == null) {
            dest = source;
        }
        return dest;
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
            val.add(el);
        }
        el = DOMUtils.getFirstElement(el);
        while (el != null) {
            findAllPolicyElementsOfLocalName(el, localName, val);
```

### AssignmentToMethodParameter
Assignment to method parameter `el`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
        while (el != null) {
            findAllPolicyElementsOfLocalName(el, localName, val);
            el = DOMUtils.getNextElement(el);
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
            } else {
                if (bus == null) {
                    bus = BusFactory.newInstance().createBus();
                    BusFactory.setThreadDefaultBus(bus);
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `uri`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
            reader = StaxUtils.createXMLStreamReader(src);
            Document doc = StaxUtils.read(reader);
            uri = getPolicyId(doc.getDocumentElement());
            if (StringUtils.isEmpty(uri)) {
                Attr att = doc.createAttributeNS(PolicyConstants.WSU_NAMESPACE_URI,
```

### AssignmentToMethodParameter
Assignment to method parameter `exts`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java

    private boolean isExistsPolicyReference(Object[] exts, String uri) {
        exts = exts == null ? new Object[0] : exts;
        for (Object o : exts) {
            if (o instanceof UnknownExtensibilityElement) {
```

### AssignmentToMethodParameter
Assignment to method parameter `exts`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
    }
    private boolean isExistsPolicy(Object[] exts, String uri) {
        exts = exts == null ? new Object[0] : exts;
        for (Object o : exts) {
            if (o instanceof UnknownExtensibilityElement) {
```

### AssignmentToMethodParameter
Assignment to method parameter `boi`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java

        if (boi.isUnwrapped()) {
            boi = boi.getUnwrappedOperation();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `assertor`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        policy = ep.getPolicy();
        if (ep instanceof EndpointPolicyImpl) {
            assertor = ((EndpointPolicyImpl)ep).getAssertor();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pc`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        if (Constants.TYPE_POLICY_REF == pc.getType()) {
            PolicyReference pr = (PolicyReference)pc;
            pc = pr.normalize(registry, false);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `bfi`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
            return epi;
        }
        bfi = mapToWrappedBindingFaultInfo(bfi);
        EffectivePolicy effectivePolicy = (EffectivePolicy)bfi.getProperty(POLICY_INFO_FAULT_SERVER);
        if (effectivePolicy == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `originalLogString`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
                }
            
                originalLogString = payload;
            }
        } catch (Exception ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `javaName`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java
            int index = javaName.indexOf('/');
            if (index != -1) {
                javaName = javaName.substring(0, index);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
            theChunk = newArray(theChunk, 0, theChunk.length, encryptedChunk, off, len);
            lenToEncode = theChunk.length;
            off = 0;
        } else {
            theChunk = encryptedChunk;
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
            int lenToCopy = remaining < len ? remaining : len;
            lastRawDataChunk = newArray(lastRawDataChunk, 0, lastRawDataChunk.length, b, off, lenToCopy);
            off = off + lenToCopy;
            len -= lenToCopy;
            if (lastRawDataChunk.length < blockSize) {
```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
            lastRawDataChunk = newArray(lastRawDataChunk, 0, lastRawDataChunk.length, b, off, lenToCopy);
            off = off + lenToCopy;
            len -= lenToCopy;
            if (lastRawDataChunk.length < blockSize) {
                return;
```

### AssignmentToMethodParameter
Assignment to method parameter `off`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
        int offset = 0;
        int chunkSize = blockSize > len ? blockSize : blockSize * (len / blockSize);
        for (; offset + chunkSize <= len; offset += chunkSize, off += chunkSize) {
            encryptAndWrite(b, off, chunkSize);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `headers`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/AbstractJwsSignatureProvider.java`
#### Snippet
```java
    protected JwsHeaders prepareHeaders(JwsHeaders headers) {
        if (headers == null) {
            headers = new JwsHeaders();
        }
        SignatureAlgorithm sigAlgo = headers.getSignatureAlgorithm();
```

### AssignmentToMethodParameter
Assignment to method parameter `required`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        if (keyEncryptionAlgorithm != null && AlgorithmUtils.PBES_HS_SET.contains(keyEncryptionAlgorithm)) {
            // We don't need to load the keystore properties for the PBES case
            required = false;
        }
        return KeyManagementUtils.loadStoreProperties(m, required,
```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                                                   KeyAlgorithm algo, KeyAlgorithm defaultAlgo) {
        if (algo == null) {
            algo = getKeyEncryptionAlgorithm(m, props, defaultAlgo);
        }
        return algo;
```

### AssignmentToMethodParameter
Assignment to method parameter `required`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        if (keyEncryptionAlgorithm != null && AlgorithmUtils.PBES_HS_SET.contains(keyEncryptionAlgorithm)) {
            // We don't need to load the keystore properties for the PBES case
            required = false;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                                                       ContentAlgorithm defaultAlgo) {
        if (algo == null) {
            algo = getContentEncryptionAlgorithm(m, props, defaultAlgo);
        }
        return algo;
```

### AssignmentToMethodParameter
Assignment to method parameter `headers`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            throw new JweException(JweException.Error.NO_ENCRYPTOR);
        }
        headers =
            prepareJweHeaders(keyEncryptionProvider != null ? keyEncryptionProvider.getAlgorithm().getJwaName() : null,
                contentEncryptionAlgo.getJwaName(), compression, headers);
```

### AssignmentToMethodParameter
Assignment to method parameter `headers`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                                                String compression,
                                                JweHeaders headers) {
        headers = headers != null ? headers : new JweHeaders();
        if (keyEncryptionAlgo != null) {
            headers.setKeyEncryptionAlgorithm(KeyAlgorithm.getAlgorithm(keyEncryptionAlgo));
```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                                                                       KeyAlgorithm algo) {
        if (algo == null) {
            algo = getDefaultPublicKeyAlgorithm(key);
        }
        if (key instanceof RSAPublicKey) {
```

### AssignmentToMethodParameter
Assignment to method parameter `theEncProvider`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtProducer.java`
#### Snippet
```java

        if (isJweRequired() && theEncProvider == null) {
            theEncProvider = getInitializedEncryptionProvider(jwt.getJweHeaders());
            if (theEncProvider == null) {
                throw new JwtException("Unable to encrypt JWT");
```

### AssignmentToMethodParameter
Assignment to method parameter `theSigProvider`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtProducer.java`
#### Snippet
```java
            } else {
                if (theSigProvider == null) {
                    theSigProvider = getInitializedSignatureProvider(jws.getJwsHeaders());
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
                                               SignatureAlgorithm defaultAlgo) {
        if (algo == null) {
            algo = getSignatureAlgorithm(m, props, defaultAlgo);
        }
        return algo;
```

### AssignmentToMethodParameter
Assignment to method parameter `algo`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        if (cert != null) {
            if (algo == null) {
                algo = getDefaultPublicKeyAlgorithm(cert.getPublicKey());
            }
            if (algo == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `theDecryptor`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtConsumer.java`
#### Snippet
```java

            if (theDecryptor == null) {
                theDecryptor = getInitializedDecryptionProvider(jwtConsumer.getHeaders());
            }
            if (theDecryptor == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `wrappedJwtToken`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtConsumer.java`
#### Snippet
```java

            JweDecryptionOutput decOutput = theDecryptor.decrypt(wrappedJwtToken);
            wrappedJwtToken = decOutput.getContentText();
            jweHeaders = decOutput.getHeaders();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `theSigVerifier`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtConsumer.java`
#### Snippet
```java
        if (isJwsRequired()) {
            if (theSigVerifier == null) {
                theSigVerifier = getInitializedSignatureVerifier(jwt);
            }
            if (theSigVerifier == null) {
```

### AssignmentToMethodParameter
Assignment to method parameter `writer`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JwtUtils.java`
#### Snippet
```java
    public static String claimsToJson(JwtClaims claims, JsonMapObjectReaderWriter writer) {
        if (writer == null) {
            writer = new JsonMapObjectReaderWriter();
        }
        return writer.toJson(claims);
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseProducer.java`
#### Snippet
```java
            }

            data = jws.signWith(theSigProvider);
            
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseProducer.java`
#### Snippet
```java
        }
        if (theEncProvider != null) {
            data = theEncProvider.encrypt(StringUtils.toBytesUTF8(data), jweHeaders);
        }
        return data;
```

### AssignmentToMethodParameter
Assignment to method parameter `data`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseConsumer.java`
#### Snippet
```java

            JweDecryptionOutput decOutput = theDecryptor.decrypt(data);
            data = decOutput.getContentText();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
        String typeWithoutParams = paramIndex == -1 ? contentType : contentType.substring(0, paramIndex);
        if (typeWithoutParams.indexOf('/') == -1) {
            contentType = "application/" + contentType;
        }
        return contentType;
```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
            String typeWithoutParams = paramIndex == -1 ? contentType : contentType.substring(0, paramIndex);
            if (typeWithoutParams.indexOf('/') == -1) {
                contentType = "application/" + contentType;
            }
        } else {
```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
            }
        } else {
            contentType = defaultType;
        }
        return contentType;
```

### AssignmentToMethodParameter
Assignment to method parameter `compactContent`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
    public static String[] getCompactParts(String compactContent) {
        if (compactContent.startsWith("\"") && compactContent.endsWith("\"")) {
            compactContent = compactContent.substring(1, compactContent.length() - 1);
        }
        return compactContent.split("\\.");
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/AbstractMessageResponseTimeInterceptor.java`
#### Snippet
```java
        // This can be replaced if really needed with pattern-based matching
        if (value.lastIndexOf(QUESTION_MARK) != -1) {
            value = value.replace(QUESTION_MARK, ESCAPED_QUESTION_MARK);
        }
        return value;
```

### AssignmentToMethodParameter
Assignment to method parameter `aegisType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisXMLStreamDataWriter.java`
#### Snippet
```java

        if (obj != null && aegisType == null) {
            aegisType = getContext().getTypeMapping().getType(obj.getClass());
            if (aegisType == null) {
                TypeCreator creator = getContext().getTypeMapping().getTypeCreator();
```

### AssignmentToMethodParameter
Assignment to method parameter `aegisType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisXMLStreamDataWriter.java`
#### Snippet
```java
            if (aegisType == null) {
                TypeCreator creator = getContext().getTypeMapping().getTypeCreator();
                aegisType = creator.createType(obj.getClass());
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `aegisType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisXMLStreamDataWriter.java`
#### Snippet
```java
        if (obj != null) {
            // look for overrides declared in the context.
            aegisType = TypeUtil.getWriteType(aegisContext, obj, aegisType);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `namespace`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/AttributeWriter.java`
#### Snippet
```java
        this.name = name;
        if (namespace == null) {
            namespace = ""; // otherwise, sjsxp is unhappy.
        }
        this.namespace = namespace;
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
        tm = globalContext.getTypeMapping();
        // don't use this for null!
        type = tm.getType(value.getClass());

        return type;
```

### AssignmentToMethodParameter
Assignment to method parameter `id`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AttachmentUtil.java`
#### Snippet
```java
        int i = id.indexOf("cid:");
        if (i != -1) {
            id = id.substring(4).trim();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `values`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
        if (values instanceof Collection) {
            Collection<?> c = (Collection<?>)values;
            values = c.toArray();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                              AegisType type, String name,
                              String ns) throws DatabindingException {
        type = TypeUtil.getWriteType(context.getGlobalContext(), value, type);
        MessageWriter cwriter;
        if (!type.isFlatArray()) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/StructType.java`
#### Snippet
```java

        // nested elements use unqualified names
        name = qualifyName(name);

        AegisType type = super.getElementType(name, beanTypeInfo, reader, context);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/StructType.java`
#### Snippet
```java
                                AegisType type, MessageWriter writer, Context context) {
        // Nested elements are unqualified
        name = new QName("", name.getLocalPart());

        MessageWriter cwriter = writer.getElementWriter(name);
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/StructType.java`
#### Snippet
```java

        // nested elements use unqualified names
        name = qualifyName(name);

        if (property instanceof SoapRef) {
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/StructType.java`
#### Snippet
```java
    protected BeanTypeInfo getBeanTypeInfoWithProperty(QName name) {
        // nested elements use unqualified names
        name = qualifyName(name);

        return super.getBeanTypeInfoWithProperty(name);
```

### AssignmentToMethodParameter
Assignment to method parameter `declare`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
            prefix = writer.getNamespaceContext().getPrefix(namespaceURI);
            if (prefix != null) {
                declare = false;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapArrayType.java`
#### Snippet
```java
            AegisType type) throws DatabindingException {

        type = TypeUtil.getWriteType(context.getGlobalContext(), value, type);

        MessageWriter cwriter = writer.getElementWriter(type.getSchemaType().getLocalPart(), "");
```

### AssignmentToMethodParameter
Assignment to method parameter `kType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/MapType.java`
#### Snippet
```java
                            AegisType kType, AegisType vType,
                            Map.Entry<?, ?> entry) throws DatabindingException {
        kType = TypeUtil.getWriteType(context.getGlobalContext(), entry.getKey(), kType);
        vType = TypeUtil.getWriteType(context.getGlobalContext(), entry.getValue(), vType);

```

### AssignmentToMethodParameter
Assignment to method parameter `vType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/MapType.java`
#### Snippet
```java
                            Map.Entry<?, ?> entry) throws DatabindingException {
        kType = TypeUtil.getWriteType(context.getGlobalContext(), entry.getKey(), kType);
        vType = TypeUtil.getWriteType(context.getGlobalContext(), entry.getValue(), vType);

        MessageWriter entryWriter = writer.getElementWriter(getEntryName());
```

### AssignmentToMethodParameter
Assignment to method parameter `typeClass`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
    private QName createQName(Class<?> typeClass, Type type, String name, String ns) {
        if (typeClass.isArray()) {
            typeClass = typeClass.getComponentType();
        }
        if (List.class.isAssignableFrom(typeClass)
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        if (List.class.isAssignableFrom(typeClass)
            && type instanceof ParameterizedType) {
            type = ((ParameterizedType)type).getActualTypeArguments()[0];
            if (type instanceof Class) {
                typeClass = (Class<?>)type;
```

### AssignmentToMethodParameter
Assignment to method parameter `typeClass`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
            type = ((ParameterizedType)type).getActualTypeArguments()[0];
            if (type instanceof Class) {
                typeClass = (Class<?>)type;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java

        if (name == null || name.length() == 0) {
            name = ServiceUtils.makeServiceNameFromClassName(typeClass);
        }
        //check from aegis type annotation
```

### AssignmentToMethodParameter
Assignment to method parameter `ns`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        //check from aegis type annotation
        if (ns == null || ns.length() == 0) {
            ns = annotationReader.getNamespace(typeClass);
        }
        // check jaxb package annotation
```

### AssignmentToMethodParameter
Assignment to method parameter `ns`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        // check jaxb package annotation
        if (ns == null || ns.length() == 0) {
            ns = annotationReader.getNamespace(typeClass.getPackage());
        }
        if (ns == null || ns.length() == 0) {
```

### AssignmentToMethodParameter
Assignment to method parameter `ns`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        }
        if (ns == null || ns.length() == 0) {
            ns = NamespaceHelper.makeNamespaceFromClassName(typeClass.getName(), "http");
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pm`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
    protected Type getComponentTypeForMap(Type genericType, Map<String, Type> pm, boolean key) {
        if (pm == null) {
            pm = new HashMap<>();
        }
        return findMapGenericTypes(genericType, pm, key);
```

### AssignmentToMethodParameter
Assignment to method parameter `st`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduitFactory.java`
#### Snippet
```java
                    return ASYNC_ONLY;
                } else {
                    st = Boolean.parseBoolean(s);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `elm`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/utils/FilteringUtil.java`
#### Snippet
```java
            XPath xPath = xPathFactory.newXPath();
            XPathExpression xPathExpression = xPath.compile(xPathString);
            elm = (Element)DOMUtils.getDomElement(elm);
            return (Boolean)xPathExpression.evaluate(elm, XPathConstants.BOOLEAN);
        } catch (XPathExpressionException ex) {
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
        } else {
            if (bus == null) {
                bus = BusFactory.newInstance().createBus();
                BusFactory.setThreadDefaultBus(bus);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
        } else {
            if (bus == null) {
                bus = BusFactory.newInstance().createBus();
                BusFactory.setThreadDefaultBus(bus);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
            if (s != null && s.startsWith("file:")) {
                if (s.contains("#")) {
                    s = s.substring(0, s.indexOf('#'));
                }
                try {
```

### AssignmentToMethodParameter
Assignment to method parameter `root`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        for (File f : files) {
            if (f.isDirectory()) {
                root = scanForResources(f, root);
            } else if (!f.getName().endsWith(".java")) {
                String n = f.getName();
```

### AssignmentToMethodParameter
Assignment to method parameter `root`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                }
                if (root == null) {
                    root = new Resource();
                }
                if (!root.getIncludes().contains(n)) {
```

### AssignmentToMethodParameter
Assignment to method parameter `dest`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/Option.java`
#### Snippet
```java
    private <T> T setIfNull(T dest, T source) {
        if (dest == null) {
            dest = source;
        }
        return dest;
```

### AssignmentToMethodParameter
Assignment to method parameter `bus`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
    public static ClientProviderFactory createInstance(Bus bus) {
        if (bus == null) {
            bus = BusFactory.getThreadDefaultBus();
        }
        ClientProviderFactory factory = new ClientProviderFactory(bus);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/InvocationBuilderImpl.java`
#### Snippet
```java
        HeaderDelegate<Object> hd = HttpUtils.getHeaderDelegate(rd, value);
        if (hd != null) {
            value = hd.toString(value);
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `address`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
        checkNull(address);
        if (address.isEmpty()) {
            address = "/";
        }
        return target(UriBuilder.fromUri(address));
```

### AssignmentToMethodParameter
Assignment to method parameter `context`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
                               Map<String, Object> context, boolean proxy) {
        if (context == null) {
            context = new HashMap<>();
        }
        Map<String, Object> reqContext = CastUtils.cast((Map<?, ?>)context.get(REQUEST_CONTEXT));
```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
                && !MediaType.APPLICATION_JSON.equals(contentType)
            || body instanceof byte[] && ((byte[])body).length == 0)) {
            body = null;
        }
        //CHECKSTYLE:ON
```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        m.put(Message.CONTENT_TYPE, ct);

        body = checkIfBodyEmpty(body, ct);
        setEmptyRequestPropertyIfNeeded(m, body);

```

### AssignmentToMethodParameter
Assignment to method parameter `exchange`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        m.setInterceptorChain(chain);

        exchange = createExchange(m, exchange);
        exchange.put(Message.REST_MESSAGE, Boolean.TRUE);
        exchange.setOneWay("true".equals(headers.getFirst(Message.ONE_WAY_REQUEST)));
```

### AssignmentToMethodParameter
Assignment to method parameter `exchange`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
    protected Exchange createExchange(Message m, Exchange exchange) {
        if (exchange == null) {
            exchange = new ExchangeImpl();
        }
        exchange.setSynchronous(true);
```

### AssignmentToMethodParameter
Assignment to method parameter `currentURI`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        if (address.startsWith(HTTP_SCHEME) && !address.equals(currentURI.toString())) {
            URI baseAddress = URI.create(address);
            currentURI = calculateNewRequestURI(baseAddress, currentURI, proxy);
            message.put(Message.ENDPOINT_ADDRESS, currentURI.toString());
            message.put(Message.REQUEST_URI, currentURI.toString());
```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            Entity<?> entity = (Entity<?>)body;
            setEntityHeaders(entity);
            body = entity.getEntity();
            requestClass = body.getClass();
            inType = body.getClass();
```

### AssignmentToMethodParameter
Assignment to method parameter `requestClass`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            setEntityHeaders(entity);
            body = entity.getEntity();
            requestClass = body.getClass();
            inType = body.getClass();
            inAnns = entity.getAnnotations();
```

### AssignmentToMethodParameter
Assignment to method parameter `inType`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            body = entity.getEntity();
            requestClass = body.getClass();
            inType = body.getClass();
            inAnns = entity.getAnnotations();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        if (body instanceof GenericEntity) {
            GenericEntity<?> genericEntity = (GenericEntity<?>)body;
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inType = genericEntity.getType();
```

### AssignmentToMethodParameter
Assignment to method parameter `requestClass`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            GenericEntity<?> genericEntity = (GenericEntity<?>)body;
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inType = genericEntity.getType();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `inType`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inType = genericEntity.getType();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            Entity<?> entity = (Entity<?>)body;
            setEntityHeaders(entity);
            body = entity.getEntity();
            requestClass = body.getClass();
            inGenericType = body.getClass();
```

### AssignmentToMethodParameter
Assignment to method parameter `requestClass`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            setEntityHeaders(entity);
            body = entity.getEntity();
            requestClass = body.getClass();
            inGenericType = body.getClass();
            inAnns = entity.getAnnotations();
```

### AssignmentToMethodParameter
Assignment to method parameter `inGenericType`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            body = entity.getEntity();
            requestClass = body.getClass();
            inGenericType = body.getClass();
            inAnns = entity.getAnnotations();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `body`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        if (body instanceof GenericEntity) {
            GenericEntity<?> genericEntity = (GenericEntity<?>)body;
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inGenericType = genericEntity.getType();
```

### AssignmentToMethodParameter
Assignment to method parameter `requestClass`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            GenericEntity<?> genericEntity = (GenericEntity<?>)body;
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inGenericType = genericEntity.getType();
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `inGenericType`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            body = genericEntity.getEntity();
            requestClass = genericEntity.getRawType();
            inGenericType = genericEntity.getType();
        }
        MultivaluedMap<String, String> headers = prepareHeaders(responseClass, body);
```

### AssignmentToMethodParameter
Assignment to method parameter `r`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            if (r == null) {
                try {
                    r = handleResponse(message.getExchange().getOutMessage(),
                                       cb.getResponseClass(),
                                       cb.getOutGenericType());
```

### AssignmentToMethodParameter
Assignment to method parameter `client`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
    public static ClientConfiguration getConfig(Object client) {
        if (client instanceof WebTargetImpl) {
            client = ((WebTargetImpl)client).getWebClient();
        } else if (client instanceof InvocationBuilderImpl) {
            client = ((InvocationBuilderImpl)client).getWebClient();
```

### AssignmentToMethodParameter
Assignment to method parameter `client`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            client = ((WebTargetImpl)client).getWebClient();
        } else if (client instanceof InvocationBuilderImpl) {
            client = ((InvocationBuilderImpl)client).getWebClient();
        }

```

## RuleId[ruleID=PrimitiveArrayArgumentToVariableArgMethod]
### PrimitiveArrayArgumentToVariableArgMethod
Confusing primitive array argument to varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java
                        //use defineClass as fallback
                        m = ReflectionUtil.getMethod(lookup.getClass(), "defineClass", byte[].class);
                        cls = (Class<?>)m.invoke(lookup, b);
                    }
                } else {
```

## RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `d`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletController.java`
#### Snippet
```java
            updateDestination(request, base, d);
        } else {
            synchronized (d) {
                updateDestination(request, base, d);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `dest`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                // no longer access the registry later
                AbstractHTTPDestination dest = destinationRegistry.getDestinationForPath(path);
                synchronized (dest) {
                    destinationRegistry.removeDestination(path);
                    dest.releaseRegistry();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
        if (endpoint != null) {
            EndpointInfo info = endpoint.getEndpointInfo();
            synchronized (info) {
                Map<Object, Crypto> o =
                    CastUtils.cast((Map<?, ?>)info.getProperty(CRYPTO_CACHE));
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ep`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/AbstractMetricsInterceptor.java`
#### Snippet
```java
        Object o = ep.get(MetricsContext.class.getName());
        if (o == null) {
            synchronized (ep) {
                o = createEndpointMetrics(m);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `boi`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/AbstractMetricsInterceptor.java`
#### Snippet
```java
            metrics = boi.getProperty(MetricsContext.class.getName());
            if (metrics == null) {
                synchronized (boi) {
                    metrics = createMetricsContextForOperation(m, boi);
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `e`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/interceptors/AbstractMetricsInterceptor.java`
#### Snippet
```java

    private Map<String, Object> getRestMetricsMap(Endpoint e) {
        synchronized (e) {
            Object mmo = e.get(REST_METRICS_MAP);
            if (mmo == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `impl`
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
                AutomaticWorkQueueImpl impl = (AutomaticWorkQueueImpl)q;
                if (impl.isShared()) {
                    synchronized (impl) {
                        impl.removeSharedUser();

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `impl`
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
            AutomaticWorkQueueImpl impl = (AutomaticWorkQueueImpl)q;
            if (impl.isShared()) {
                synchronized (impl) {
                    if (impl.getShareCount() == 0
                        && imanager != null
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `serviceInfo`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
        if (schema == null && !serviceInfo.hasProperty(Schema.class.getName() + ".CHECKED")) {
            try {
                synchronized (serviceInfo) {
                    return createSchema(serviceInfo, b);
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `loader`
in `core/src/main/java/org/apache/cxf/common/spi/ClassGeneratorClassLoader.java`
#### Snippet
```java
        }
        TypeHelperClassLoader loader = getOrCreateLoader(l);
        synchronized (loader) {
            Class<?> clz = loader.lookupDefinedClass(className);
            if (clz == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `loader`
in `core/src/main/java/org/apache/cxf/common/spi/ClassGeneratorClassLoader.java`
#### Snippet
```java
        }
        TypeHelperClassLoader loader = getOrCreateLoader(cls);
        synchronized (loader) {
            Class<?> clz = loader.lookupDefinedClass(className);
            if (clz == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ext`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            Class<?> cls = ext.getClassObject(loader);
            if (cls != null && type.isAssignableFrom(cls)) {
                synchronized (ext) {
                    if (ext.getLoadedObject() == null) {
                        loadAndRegister(ext);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ex`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
                Class<?> cls = ex.getClassObject(loader);
                if (cls != null && type.isAssignableFrom(cls)) {
                    synchronized (ex) {
                        if (ex.getLoadedObject() == null) {
                            loadAndRegister(ex);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ex`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            if (cls != null
                && type.isAssignableFrom(cls)) {
                synchronized (ex) {
                    if (listener.loadBean(ex.getName(), cls.asSubclass(type))) {
                        if (ex.getLoadedObject() == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `e`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
        }

        synchronized (e) {
            Object obj = e.load(loader, bus);
            if (obj == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ex`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            Class<?> cls = ex.getClassObject(loader);
            if (cls != null && type.isAssignableFrom(cls)) {
                synchronized (ex) {
                    ret.add(ex.getName());
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `e`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            Class<?> cls = e.getClassObject(loader);
            if (cls != null && type.isAssignableFrom(cls)) {
                synchronized (e) {
                    if (e.getLoadedObject() == null) {
                        loadAndRegister(e);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `e`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
                Class<?> cls = e.getClassObject(loader);
                if (cls != null && type.isAssignableFrom(cls)) {
                    synchronized (e) {
                        loadAndRegister(e);
                    }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `d`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        }

        synchronized (d) {
            col.read(d, systemId);
        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `sch`
in `core/src/main/java/org/apache/cxf/service/model/SchemaInfo.java`
#### Snippet
```java

        XmlSchema sch = getSchema();
        synchronized (sch) {
            XmlSchema schAgain = getSchema();
            // XML Schema blows up when the context is null as opposed to empty.
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `serv`
in `core/src/main/java/org/apache/cxf/service/invoker/SessionFactory.java`
#### Snippet
```java
        Service serv = e.getService();
        Object o;
        synchronized (serv) {
            o = e.getSession().get(serv.getName().toString());
            if (o == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `objectFactoryCache`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
                CachedClass cachedFactory = null;
                if (pkg != null && objectFactoryCache != null) {
                    synchronized (objectFactoryCache) {
                        cachedFactory = objectFactoryCache.get(pkg);
                    }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `objectFactoryCache`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            return;
        }
        synchronized (objectFactoryCache) {
            objectFactoryCache.put(objectFactoryPkg,
                                     new CachedClass(ofactory));
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `endpoint`
in `core/src/main/java/org/apache/cxf/staxutils/validation/Stax2ValidationUtils.java`
#### Snippet
```java
    private XMLValidationSchema getValidator(Endpoint endpoint, ServiceInfo serviceInfo)
            throws XMLStreamException {
        synchronized (endpoint) {
            XMLValidationSchema ret = (XMLValidationSchema) endpoint.get(KEY);
            if (ret == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `phaseChain`
in `core/src/main/java/org/apache/cxf/transport/ChainInitiationObserver.java`
#### Snippet
```java
                phaseChain = m.getInterceptorChain();
                // To make sure the phase chain is run by one thread once
                synchronized (phaseChain) {
                    if (phaseChain.getState() == InterceptorChain.State.PAUSED
                        || phaseChain.getState() == InterceptorChain.State.SUSPENDED) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `exchange`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java

    protected void waitResponse(Exchange exchange) throws IOException {
        synchronized (exchange) {
            long remaining = synchronousTimeout;
            Long o = PropertyUtils.getLong(exchange.getOutMessage(), SYNC_TIMEOUT);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `d`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java
        schema.setSystemId(systemId);
        XmlSchema xmlSchema;
        synchronized (d) {
            xmlSchema = col.read(d, systemId);
            schema.setSchema(xmlSchema);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factory`
in `core/src/main/java/org/apache/cxf/interceptor/StaxOutInterceptor.java`
#### Snippet
```java
                    }
                } else {
                    synchronized (factory) {
                        if (writer == null) {
                            os = setupOutputStream(os);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factory`
in `core/src/main/java/org/apache/cxf/interceptor/StaxInInterceptor.java`
#### Snippet
```java
                    }
                } else {
                    synchronized (factory) {
                        if (reader != null) {
                            xreader = factory.createXMLStreamReader(reader);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `c`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
            }

            synchronized (c) {
                System.setProperty("noprint", "true");

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenRetriever.java`
#### Snippet
```java
        }

        synchronized (client) {
            try {
                Map<String, Object> ctx = client.getRequestContext();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenRetriever.java`
#### Snippet
```java
        
        STSClient client = STSUtils.getClientWithIssuer(message, "sts", params.getIssuer());
        synchronized (client) {
            try {
                client.setMessage(message);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `c`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSStaxTokenValidator.java`
#### Snippet
```java

        STSClient c = STSUtils.getClient(message, "sts");
        synchronized (c) {
            System.setProperty("noprint", "true");
            try {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java

            EndpointInfo info = message.getExchange().getEndpoint().getEndpointInfo();
            synchronized (info) {
                info.setProperty(SecurityConstants.SIGNATURE_CRYPTO, signCrypto);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java

            EndpointInfo info = message.getExchange().getEndpoint().getEndpointInfo();
            synchronized (info) {
                info.setProperty(SecurityConstants.ENCRYPT_CRYPTO, encrCrypto);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
        if (replayCache == null && ep != null && ep.getEndpointInfo() != null) {
            EndpointInfo info = ep.getEndpointInfo();
            synchronized (info) {
                replayCache = (ReplayCache)info.getProperty(instanceKey);

```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
    protected TokenStore getTokenStore(SoapMessage message) {
        EndpointInfo info = message.getExchange().getEndpoint().getEndpointInfo();
        synchronized (info) {
            TokenStore tokenStore =
                (TokenStore)message.getContextualProperty(SecurityConstants.TOKEN_STORE_CACHE_INSTANCE);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
        if (callbackHandler != null) {
            EndpointInfo info = soapMessage.getExchange().getEndpoint().getEndpointInfo();
            synchronized (info) {
                info.setProperty(SecurityConstants.CALLBACK_HANDLER, callbackHandler);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenOutInterceptor.java`
#### Snippet
```java
            maps = (AddressingProperties)message.get("jakarta.xml.ws.addressing.context");
        }
        synchronized (client) {
            try {
                String s = SpnegoTokenInterceptorProvider.setupClient(client, message, aim);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
            }

            synchronized (client) {
                try {
                    SecureConversationTokenInterceptorProvider
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/KerberosTokenInterceptorProvider.java`
#### Snippet
```java
                    try {
                        KerberosClient client = KerberosUtils.getClient(message, "kerberos");
                        synchronized (client) {
                            tok = client.requestSecurityToken();
                        }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/TokenStoreUtils.java`
#### Snippet
```java
    public static TokenStore getTokenStore(Message message) throws TokenStoreException {
        EndpointInfo info = message.getExchange().getEndpoint().getEndpointInfo();
        synchronized (info) {
            TokenStore tokenStore =
                (TokenStore)message.getContextualProperty(SecurityConstants.TOKEN_STORE_CACHE_INSTANCE);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationOutInterceptor.java`
#### Snippet
```java
                .get("jakarta.xml.ws.addressing.context");
        }
        synchronized (client) {
            try {
                String s = SecureConversationTokenInterceptorProvider
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `client`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationOutInterceptor.java`
#### Snippet
```java
            return tok;
        }
        synchronized (client) {
            try {
                SecureConversationTokenInterceptorProvider.setupClient(client, message, aim, itok, true);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    protected final Map<Object, Crypto> getCryptoCache() {
        EndpointInfo info = message.getExchange().getEndpoint().getEndpointInfo();
        synchronized (info) {
            Map<Object, Crypto> o =
                CastUtils.cast((Map<?, ?>)message.getContextualProperty(CRYPTO_CACHE));
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `exchange`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java

        // Synchronize on exchange early to make sure we do not miss the notify
        synchronized (exchange) {
            String replyTo = headers.getJMSReplyTo();
            String jmsMessageID = sendMessage(request, outMessage,
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `exchange`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java

            if (exchange.isSynchronous()) {
                synchronized (exchange) {
                    exchange.put(CORRELATED, Boolean.TRUE);
                    exchange.notifyAll();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `d`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBindingFactory.java`
#### Snippet
```java

    public void addListener(Destination d, Endpoint e) {
        synchronized (d) {
            if (d.getMessageObserver() != null) {
                throw new ServiceConstructionException(new Message("ALREADY_RUNNING", LOG,
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `def`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java

    public void updateWSDLPublishedEndpointAddress(Definition def, EndpointInfo endpointInfo) {
        synchronized (def) {
            //writing a def is not threadsafe.  Sync on it to make sure
            //we don't get any ConcurrentModificationExceptions
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `def`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
        String epurl = base;

        synchronized (def) {
            //writing a def is not threadsafe.  Sync on it to make sure
            //we don't get any ConcurrentModificationExceptions
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `provider`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
        if (provider != null) {
            Object proxy = null;
            synchronized (provider) {
                try {
                    proxy = InjectionUtils.extractFieldValue(f, provider);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `provider`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
        if (provider != null) {
            Object proxy = null;
            synchronized (provider) {
                try {
                    proxy = InjectionUtils.extractFromMethod(provider,
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `instance`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return;
        }
        synchronized (instance) {
            for (Map.Entry<Class<?>, Method> entry : cri.getContextMethods().entrySet()) {
                Method method = entry.getValue();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `filters`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
            return filters;
        } else {
            synchronized (filters) {
                if (filters.get(0) != wadlGenerator) {
                    filters.add(0, wadlGenerator);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ei`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpHandlerImpl.java`
#### Snippet
```java
                String path = exchange.getHttpContext().getPath();
                if (ad != null && ad.equals(path)) {
                    synchronized (ei) {
                        String contextPath = exchange.getContextPath();
                        ei.setAddress(contextPath + path);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `source`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMCaptureOutInterceptor.java`
#### Snippet
```java

            // get the current sequence, requesting the creation of a new one if necessary
            synchronized (source) {
                final SourceSequence seq;
                if (isLastMessage) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ep`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    public void initializeInterceptorChain(Message msg) {
        Endpoint ep = msg.getExchange().getEndpoint();
        synchronized (ep) {
            if (ep.get(WSRM_RETRANSMIT_CHAIN) == null) {
                LOG.info("Setting retransmit chain from message");
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `endpoint`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
        RMEndpoint rme = reliableEndpoints.get(endpoint);
        if (null == rme) {
            synchronized (endpoint) {
                rme = reliableEndpoints.get(endpoint);
                if (rme != null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `info`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            return originalReplyTo;
        }
        synchronized (info) {
            EndpointInfo ei = info.getEndpointInfo();
            Destination dest = ei.getProperty(DECOUPLED_DESTINATION, Destination.class);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `correlatedExchange`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                uncorrelatedExchanges.remove(maps.getRelatesTo().getValue());
            if (correlatedExchange != null) {
                synchronized (correlatedExchange) {
                    message.setExchange(correlatedExchange);
                }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `service`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/TypeMapCache.java`
#### Snippet
```java
    public static CorbaTypeMap get(ServiceInfo service) {
        if (service != null) {
            synchronized (service) {
                CorbaTypeMap map = service.getProperty(KEY, CorbaTypeMap.class);
                if (map == null) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `rr`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            RequestResponse rr = uncorrelatedRequests.get(resp.getId());
            if (rr != null) {
                synchronized (rr) {
                    rr.setResponse(resp);
                    rr.notifyAll();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `rr`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
                String rid = entity.getId();
                RequestResponse rr = uncorrelatedRequests.get(rid);
                synchronized (rr) {
                    try {
                        long timetowait = entity.getReceiveTimeout();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `rr`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            RequestResponse rr = uncorrelatedRequests.get(resp.getId());
            if (rr != null) {
                synchronized (rr) {
                    rr.setResponse(resp);
                    rr.notifyAll();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `d`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
    @Override
    public synchronized void addListener(Destination d, Endpoint e) {
        synchronized (d) {
            MessageObserver mo = d.getMessageObserver();
            if (d.getAddress() != null
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `pe`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
            PolicyEngine pe = bus.getExtension(PolicyEngine.class);

            synchronized (pe) {
                pe.addPolicy(thePolicy);
            }
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `pe`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
        PolicyEngine pe = bus.getExtension(PolicyEngine.class);

        synchronized (pe) {
            pe.setEnabled(enabled);
            pe.setIgnoreUnknownAssertions(ignoreUnknownAssertions);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        }
        if (effectivePolicy == null) {
            synchronized (ei) {
                if (bfi != null) {
                    effectivePolicy = (EffectivePolicy)bfi.getProperty(POLICY_INFO_FAULT_CLIENT);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        EffectivePolicy effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_RESPONSE_CLIENT);
        if (effectivePolicy == null) {
            synchronized (ei) {
                effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_RESPONSE_CLIENT);
                if (null == effectivePolicy) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        EffectivePolicy effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_REQUEST_SERVER);
        if (effectivePolicy == null) {
            synchronized (ei) {
                effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_REQUEST_SERVER);
                if (null == effectivePolicy) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
                                                           ? POLICY_INFO_ENDPOINT_CLIENT : POLICY_INFO_ENDPOINT_SERVER);
        if (ep == null) {
            synchronized (ei) {
                ep = (EndpointPolicy)ei.getProperty(isRequestor
                    ? POLICY_INFO_ENDPOINT_CLIENT : POLICY_INFO_ENDPOINT_SERVER);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
            EffectivePolicy effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_RESPONSE_SERVER);
            if (effectivePolicy == null) {
                synchronized (ei) {
                    effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_RESPONSE_SERVER);
                    if (null == effectivePolicy) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        EffectivePolicy effectivePolicy = (EffectivePolicy)bfi.getProperty(POLICY_INFO_FAULT_SERVER);
        if (effectivePolicy == null) {
            synchronized (ei) {
                effectivePolicy = (EffectivePolicy)bfi.getProperty(POLICY_INFO_FAULT_SERVER);
                if (null == effectivePolicy) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `ei`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
        EffectivePolicy effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_REQUEST_CLIENT);
        if (effectivePolicy == null) {
            synchronized (ei) {
                effectivePolicy = (EffectivePolicy)boi.getProperty(POLICY_INFO_REQUEST_CLIENT);
                if (null == effectivePolicy) {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `di`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
        }

        synchronized (di) {
            if (di.getProperty("registeredPolicy") == null) {
                List<UnknownExtensibilityElement> diext = di.getExtensors(UnknownExtensibilityElement.class);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `exchange`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java

    protected void waitForResponseCode(Exchange exchange) {
        synchronized (exchange) {
            if (getResponseCode(exchange) == null) {
                try {
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `exchange`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        }
        if (ex == null && !exchange.isOneWay()) {
            synchronized (exchange) {
                while (exchange.get("IN_CHAIN_COMPLETE") == null) {
                    exchange.wait(cfg.getSynchronousTimeout());
```

## RuleId[ruleID=ReturnNull]
### ReturnNull
Return of `null`
in `rt/rs/description-common-openapi/src/main/java/org/apache/cxf/jaxrs/common/openapi/SyntheticServletConfig.java`
#### Snippet
```java
    @Override
    public String getServletName() {
        return null; /* no servlet available */
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/interceptor/XMLMessageInInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/server/ReactiveIOInvoker.java`
#### Snippet
```java
            return handleMaybe(inMessage, (Maybe<?>)result);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/description-common-openapi/src/main/java/org/apache/cxf/jaxrs/common/openapi/DefaultApplicationFactory.java`
#### Snippet
```java
        }
        
        return (appInfo == null) ? null : appInfo.getProvider();
    }
    
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/CryptoProviderUtils.java`
#### Snippet
```java
    public static String getCallbackPwd(String userName, int usage, CallbackHandler handler) {
        if (handler == null) {
            return null;
        }
        WSPasswordCallback[] cb = {
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/client/XKMSInvoker.java`
#### Snippet
```java
        if (!locateResultType.getUnverifiedKeyBinding().iterator().hasNext()) {
            LOG.warn("X509Certificate is not found in XKMS for id: " + ids);
            return null;
        }
        KeyInfoType keyInfo = locateResultType.getUnverifiedKeyBinding()
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/client/XKMSInvoker.java`
#### Snippet
```java
        if (!keyInfo.getContent().iterator().hasNext()) {
            LOG.warn("X509Certificate is not found in XKMS for id: " + ids);
            return null;
        }
        JAXBElement<X509DataType> x509Data = (JAXBElement<X509DataType>)keyInfo
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
        // This only applies if we've configured a local Crypto instance...
        if (fallbackCrypto == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
    private X509Certificate[] checkX509Cache(String key) {
        if (xkmsClientCache == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
            };
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            };
            disp.invokeAsync(new ObjectFactory().createResolve(rt), handler);
            return response.value == null ? null : response.value.getResolveMatch();
        }
        Object o = disp.invoke(new ObjectFactory().createResolve(rt));
```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
            o = ((JAXBElement)o).getValue();
        }
        return o == null ? null : ((ResolveMatchesType)o).getResolveMatch();
    }

```

### ReturnNull
Return of `null`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/ParticleInfo.java`
#### Snippet
```java
    private static String protectDefaultValue(String schemaDefaultValue) {
        if (schemaDefaultValue == null) {
            return null;
        }
        boolean leaveAlone = false;
```

### ReturnNull
Return of `null`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
            //
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
            return new URL(addr).getPath();
        } catch (MalformedURLException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        XmlSchemaContentModel model = type.getContentModel();
        if (model == null) {
            return null;
        }
        XmlSchemaContent content = model.getContent();
```

### ReturnNull
Return of `null`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        XmlSchemaContent content = model.getContent();
        if (content == null) {
            return null;
        }
        if (!(content instanceof XmlSchemaComplexContentExtension)) {
```

### ReturnNull
Return of `null`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        }
        if (!(content instanceof XmlSchemaComplexContentExtension)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        XmlSchemaParticle particle = ext.getParticle();
        if (particle == null) {
            return null;
        }
        final XmlSchemaSequence sequence;
```

### ReturnNull
Return of `null`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettySpringTypesFactory.java`
#### Snippet
```java
                                     Node data, Class<?> c) {
        if (u == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml/CXFMessageParser.java`
#### Snippet
```java
            return (String)message.get(property);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml/CXFMessageParser.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml/CXFMessageParser.java`
#### Snippet
```java
            return (QName)message.get(Message.WSDL_OPERATION);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml/CXFMessageParser.java`
#### Snippet
```java
            return (QName)message.get(Message.WSDL_SERVICE);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            return UUID.fromString(uri.getSchemeSpecificPart());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java

        if (consider.isEmpty()) {
            return null;
        }
        ProbeMatchesType pmt = new ProbeMatchesType();
```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                    .equals(doc.getDocumentElement().getNamespaceURI())) {
                    //not a proper ws-discovery message, ignore it
                    return null;
                }

```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                    ProbeMatchesType pmt = handleProbe((ProbeType)obj);
                    if (pmt == null) {
                        return null;
                    }
                    updateOutputAction("Matches");
```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                    ResolveMatchesType rmt = handleResolve((ResolveType)obj);
                    if (rmt == null) {
                        return null;
                    }
                    updateOutputAction("Matches");
```

### ReturnNull
Return of `null`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
            return bus.getExtension(InstrumentationManager.class).getMBeanServer();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/claims/SAMLSecurityContext.java`
#### Snippet
```java

    public javax.security.auth.Subject getSubject() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java
        //detect usage of user-defined proxy and avoid optimizations in that case
        if (!"sun.net.spi.DefaultProxySelector".equals(sel.getClass().getName())) {
            return null;
        }
        Iterator<Proxy> it = sel.select(uri).iterator();
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
    private PasswordAuthentication tryWithInternal(Authenticator a) throws Exception {
        if (a == null) {
            return null;
        }
        final Field[] fields;
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
            AccessController.doPrivileged((PrivilegedAction<Object>) () -> {
                method.setAccessible(true);
                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
    PasswordAuthentication tryWith(Authenticator a) throws Exception {
        if (a == null) {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        }
        if (ret != null && ret.getMessageObserver() == null) {
            return null;
        }
        return ret;
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ProxyFactory.java`
#### Snippet
```java

        // No proxy configured
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java

                    if ("basic".equals(getRequestingScheme()) || "digest".equals(getRequestingScheme())) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HttpServletRequestSnapshot.java`
#### Snippet
```java
            return headersMap.get(name).nextElement();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Servlet3ContinuationProvider.java`
#### Snippet
```java
    public Continuation getContinuation() {
        if (inMessage.getExchange().isOneWay()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/AbstractSpnegoAuthSupplier.java`
#### Snippet
```java
    public CallbackHandler getUsernamePasswordHandler(final String username, final String password) {
        if (StringUtils.isEmpty(username)) {
            return null;
        }
        return new NamePasswordCallbackHandler(username, password);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/AbstractSpnegoAuthSupplier.java`
#### Snippet
```java
            }
            LOG.log(Level.SEVERE, "initSecContext", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/AbstractSpnegoAuthSupplier.java`
#### Snippet
```java
                                   Message message) {
        if (!HttpAuthHeader.AUTH_TYPE_NEGOTIATE.equals(authPolicy.getAuthorizationType())) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/CustomAuthSupplier.java`
#### Snippet
```java
            return authPolicy.getAuthorizationType() + " " + authPolicy.getAuthorization();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DefaultBasicAuthSupplier.java`
#### Snippet
```java
                                      encodeBasicAuthWithIso8859);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                                   String fullHeader) {
        if (authPolicy == null || (authPolicy.getUserName() == null && authPolicy.getPassword() == null)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java
                                       authPolicy.getPassword());
            }
            return null;
        }
        HttpAuthHeader authHeader = new HttpAuthHeader(fullHeader);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/DigestAuthSupplier.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            // the request may not implement the Servlet3 API
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
                return cproviderFactory.retrieveFromContinuation(req);
            }
            return null;
        }
        return retrieveFromServlet3Async(req);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    private AuthorizationPolicy getAuthorizationPolicyFromMessage(String credentials, SecurityContext sc) {
        if (credentials == null || StringUtils.isEmpty(credentials.trim())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            return policy;
        }
        return null;
    }
    public static final class PrincipalAuthorizationPolicy extends AuthorizationPolicy {
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
    protected OutputStream flushHeaders(Message outMessage, boolean getStream) throws IOException {
        if (isResponseRedirected(outMessage)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            if (!StringUtils.isEmpty(ec)) {
                response.sendError(responseCode, ec);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            }
            Principal pp = getPrincipal();
            return pp != null ? pp.getName() : null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (!compatible(p1, p2)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/InsecureTrustManager.java`
#### Snippet
```java
        @Override
        public X509Certificate[] getAcceptedIssuers() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (!compatible(p1, p2)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
            return authorizationLines.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
        }
        if (list.isEmpty()) {
            return null;
        }
        return list.toArray(new String[0]);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletContextResourceResolver.java`
#### Snippet
```java
            return clz.cast(getAsStream(entryName));
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                        return location;
                    }
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
            return endpointInfo.getName().toString() + ".http-conduit";
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListJAASAuthenticator.java`
#### Snippet
```java
        } catch (FailedLoginException e) {
            LOG.log(Level.FINE, "Login failed ", e);
            return null;
        } catch (AccountException e) {
            LOG.log(Level.WARNING, "Account failure ",  e);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListJAASAuthenticator.java`
#### Snippet
```java
        } catch (AccountException e) {
            LOG.log(Level.WARNING, "Account failure ",  e);
            return null;
        } catch (GeneralSecurityException e) {
            LOG.log(Level.SEVERE, "General Security Exception ", e);
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListJAASAuthenticator.java`
#### Snippet
```java
        } catch (GeneralSecurityException e) {
            LOG.log(Level.SEVERE, "General Security Exception ", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixMatcher.java`
#### Snippet
```java
    public String getDomainRoot(final String domain, final DomainType expectedType) {
        if (domain == null) {
            return null;
        }
        if (domain.startsWith(".")) {
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixMatcher.java`
#### Snippet
```java
        }
        if (domain.startsWith(".")) {
            return null;
        }
        String domainName = null;
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/CharSequenceTranslator.java`
#### Snippet
```java
    public final String translate(final CharSequence input) {
        if (input == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java
    static String extractCN(final String subjectPrincipal) throws SSLException {
        if (subjectPrincipal == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java
                }
            }
            return null;
        } catch (InvalidNameException e) {
            throw new SSLException(subjectPrincipal + " is not a valid X500 distinguished name");
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
            // why are we throwing a busexception if the DF isn't found?
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
                return endpointAddress;
            }
            return null;
        }
        String address = basePath;
```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/FormattedServiceListWriter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java

    public ServletConfig getServletConfig() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java

    public String getServletInfo() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
            return list;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/Java2WADLMojo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/MPRestClientInvocationContextImpl.java`
#### Snippet
```java
    @Override
    public Constructor<?> getConstructor() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/MPRestClientInvocationContextImpl.java`
#### Snippet
```java
    @Override
    public Object getTimer() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/sse/SseMessageBodyReader.java`
#### Snippet
```java
            return new SseTypeSafeProcessor<Object>(new GenericType<Object>(typeArgument), publisher);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/sse/SseEventBuilder.java`
#### Snippet
```java
        private <T> T readData(Class<T> type, Type genericType, MediaType mediaType) {
            if (data == null) {
                return null;
            }
            try {
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlEncInInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
                    return c.get().getOptionalValue(configKeyPropName, Long.class).orElse(null);
                }
                return null;
            });
            return value == null ? OptionalLong.empty() : OptionalLong.of(value);
```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
                    return c.get().getValue(configKeyPropName, clazz);
                }
                return null;
            });
        }
```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
    public static <T> T getValue(String propertyName, Class<T> clazz) {
        Optional<Config> c = config();
        return c.isPresent() ? c.get().getValue(propertyName, clazz) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
                    return c.get().getOptionalValue(configKeyPropName, clazz).orElse(null);
                }
                return null;
            });
            return Optional.ofNullable(value);
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecInHandler.java`
#### Snippet
```java
    protected Document getDocument(Message message) {
        if (isServerGet(message)) {
            return null;
        }
        Integer responseCode = (Integer)message.get(Message.RESPONSE_CODE);
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecInHandler.java`
#### Snippet
```java
        Integer responseCode = (Integer)message.get(Message.RESPONSE_CODE);
        if (responseCode != null && responseCode != 200) {
            return null;
        }
        Document doc = null;
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecInHandler.java`
#### Snippet
```java
            return (Element)list.item(index);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
        Object body = getRequestBody(m);
        if (body == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
                                   MediaType.APPLICATION_XML_TYPE, m);
        if (!(providerObject instanceof JAXBElementProvider)) {
            return null;
        }
        JAXBElementProvider<Object> provider = (JAXBElementProvider<Object>)providerObject;
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
        MessageContentsList objs = MessageContentsList.getContentsList(message);
        if (objs == null || objs.isEmpty()) {
            return null;
        }
        return objs.get(0);
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/EncryptionUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
        }
        throwFault("Certificate is missing", null);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
            throwFault(ex.getMessage(), ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
            throwFault("Encrypted key can not be decrypted", ex);
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSigInInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
            throwFault("Signature Reference is not available", ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
        } catch (Exception ex) {
            throwFault("Crypto can not be loaded", ex);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlFormOutInterceptor.java`
#### Snippet
```java
        Object ct = message.get(Message.CONTENT_TYPE);
        if (ct == null || !MediaType.APPLICATION_FORM_URLENCODED.equalsIgnoreCase(ct.toString())) {
            return null;
        }
        MessageContentsList objs = MessageContentsList.getContentsList(message);
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlFormOutInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlFormInHandler.java`
#### Snippet
```java
            throwFault("Error reading the form", ex);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedInHandler.java`
#### Snippet
```java
            return (Element)list.item(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/authorization/SecurityContextProviderImpl.java`
#### Snippet
```java
        boolean allowUnsignedSamlPrincipals = Boolean.parseBoolean(allowUnsigned);
        if (!(wrapper.isSigned() || allowUnsignedSamlPrincipals)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
            return loadCryptoFromURL(url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
        Object o = org.apache.cxf.rt.security.utils.SecurityUtils.getSecurityPropertyValue(propKey, message);
        if (o == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SAMLUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SAMLUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/RSSecurityUtils.java`
#### Snippet
```java
            handler.handle(cb);
        } catch (Exception e) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
            return signatureToken.getX509Certificates()[0];
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/reactivestreams/src/main/java/org/apache/cxf/jaxrs/reactivestreams/server/AbstractReactiveInvoker.java`
#### Snippet
```java
            asyncResponse.resume(t);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/AbstractSamlInHandler.java`
#### Snippet
```java
            throwFault("Assertion can not be read as XML document", ex);
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/AbstractSamlInHandler.java`
#### Snippet
```java
    private Certificate[] getTLSCertificates(Message message) {
        TLSSessionInfo tlsInfo = message.get(TLSSessionInfo.class);
        return tlsInfo != null ? tlsInfo.getPeerCertificates() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/AbstractSamlInHandler.java`
#### Snippet
```java
            throwFault("Assertion can not be validated", ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractSSOSpHandler.java`
#### Snippet
```java
            if (sigProperties == null) {
                LOG.warning("Cannot load signature properties using: " + signaturePropertiesFile);
                return null;
            }
            try {
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractSSOSpHandler.java`
#### Snippet
```java
            } catch (WSSecurityException ex) {
                LOG.warning("Error in loading the signature Crypto object: " + ex.getMessage());
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractSSOSpHandler.java`
#### Snippet
```java
                if (callbackHandler == null) {
                    LOG.warning("Cannot load CallbackHandler using: " + callbackHandlerClass);
                    return null;
                }
            } catch (Exception ex) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractSSOSpHandler.java`
#### Snippet
```java
            } catch (Exception ex) {
                LOG.log(Level.FINE, "Error in loading callback handler", ex);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
            // former case
            reportTrace("MISSING_RESPONSE_STATE");
            return null;
        }
        String contextKey = securityContextCookie.getValue();
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
        if (responseState == null) {
            reportError("MISSING_RESPONSE_STATE");
            return null;
        }
        if (isStateExpired(responseState.getCreatedAt(), responseState.getExpiresAt())) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
            reportError("EXPIRED_RESPONSE_STATE");
            getStateProvider().removeResponseState(contextKey);
            return null;
        }
        String webAppContext = getWebAppContext(m);
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
            getStateProvider().removeResponseState(contextKey);
            reportError("INVALID_RESPONSE_STATE");
            return null;
        }
        if (responseState.getAssertion() == null) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
        if (responseState.getAssertion() == null) {
            reportError("INVALID_RESPONSE_STATE");
            return null;
        }
        return responseState;
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
    ) throws WSSecurityException {
        if (subject.getSubjectConfirmations() == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/RetryStrategy.java`
#### Snippet
```java
            alternates.remove(0);
        }
        return alternates.isEmpty() ? null : alternates.get(0);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
            return (Element)list.item(index);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/AbstractStaticFailoverStrategy.java`
#### Snippet
```java
        return alternateAddresses != null
               ? new ArrayList<>(alternateAddresses)
               : null;
    }

```

### ReturnNull
Return of `null`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/CircuitBreakerTargetSelector.java`
#### Snippet
```java
        if (circuits.isEmpty()) {
            LOG.log(Level.SEVERE, "No alternative addresses configured");
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
            return sr.getAttributes();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
            return attr;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/provider/jaxws/JaxwsFaultCodeProvider.java`
#### Snippet
```java
        
        if (fm == null) {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerMetricsProvider.java`
#### Snippet
```java
    @Override
    public MetricsContext createEndpointContext(Endpoint endpoint, boolean asClient, String clientId) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapCertificateRepo.java`
#### Snippet
```java
    protected X509Certificate getCert(Attribute attr) {
        if (attr == null) {
            return null;
        }
        byte[] data;
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapCertificateRepo.java`
#### Snippet
```java
        }
        if (data == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
            } catch (ClassNotFoundException ex) {
                LOG.warning("Cannot find specified computeValue class, " + className);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
                    throwException(t);
                }
                return null;
            }
        } else {
```

### ReturnNull
Return of `null`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
            return inProgress.get(key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalDestination.java`
#### Snippet
```java
            return new SynchronousConduit((LocalConduit)conduit);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/CxfUndertowServlet.java`
#### Snippet
```java
    @Override
    protected Bus getBus() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPDestination.java`
#### Snippet
```java
            return new URL(addr).getPath();
        } catch (MalformedURLException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowSpringTypesFactory.java`
#### Snippet
```java
                                     Node data, Class<?> c) {
        if (u == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
            InputStream is = ResourceUtils.getResourceStream(loc, bus);
            if (is == null) {
                return null;
            }
            return getUserApplicationFromStream(is, cfg);
```

### ReturnNull
Return of `null`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
            LOG.warning("Problem with processing a user model at " + loc);
        }
        return null;
    }
    public static UserApplication getUserApplicationFromStream(InputStream is) throws IOException {
```

### ReturnNull
Return of `null`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
            return String.join(",", list);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/handlers/CxfUndertowLogHandler.java`
#### Snippet
```java
        } catch (IOException ignore) {

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
            return getPrimitiveValue(getter, value);
        } catch (Throwable e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java

    public List<SearchCondition<T>> getSearchConditions() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
            return bus.getExtension(InstrumentationManager.class).getMBeanServer();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/DefaultParamConverterProvider.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/DefaultParamConverterProvider.java`
#### Snippet
```java
        @Override
        public String toString(final Date value) {
            return value != null ? DateTools.dateToString(value, Resolution.MILLISECOND) : null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/ParamConverterUtils.java`
#### Snippet
```java
        }

        return value == null ? null : value.toString();
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/InterfaceProxy.java`
#### Snippet
```java
        }
        map.put(property, args[0]);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractComplexCondition.java`
#### Snippet
```java

    public PrimitiveStatement getStatement() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractComplexCondition.java`
#### Snippet
```java

    public T getCondition() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/QueryContextProvider.java`
#### Snippet
```java
            SearchConditionVisitor<T, E> visitor = getVisitor();
            if (visitor == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/QueryContextProvider.java`
#### Snippet
```java
            SearchCondition<T> cond = searchContext.getCondition(originalExpression, beanClass);
            if (cond == null) {
                return null;
            }
            cond.accept(visitor);
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/QueryContextProvider.java`
#### Snippet
```java
            Object visitor = message.getContextualProperty(SearchUtils.SEARCH_VISITOR_PROPERTY);
            if (visitor == null) {
                return null;
            }
            //TODO: consider introducing SearchConditionVisitor.getBeanClass &&
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
        } catch (final ParseException ex) {
            LOG.log(Level.FINE, "Unable to parse date using format specification: " + formatter.toPattern(), ex);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java
            } catch (SearchParseException ex) {
                if (PropertyUtils.isTrue(message.getContextualProperty(BLOCK_SEARCH_EXCEPTION))) {
                    return null;
                }
                throw ex;
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaLuceneContentExtractor.java`
#### Snippet
```java

        if (content == null) {
            return null;
        }
        final Document document = new Document();
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
        TikaContent tc = extractMetadata(in);
        if (tc == null) {
            return null;
        }
        Metadata metadata = tc.getMetadata();
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
         */
        public String getContent() {
            return content instanceof ToTextContentHandler ? content.toString() : null;
        }
        /**
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
                               jakarta.ws.rs.core.MediaType mtHint, ParseContext context) {
        if (in == null) {
            return null;
        }
        final Metadata metadata = new Metadata();
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
            }
            if (parser == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
        }

        return null;
    }
    /**
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
            return Collections.unmodifiableList(scts);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
            return beanspector.swap(pojo).getValue(getter);
        } catch (Throwable e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
            return scts.get(0).getStatement();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
            return getters2values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/sql/SQLPrinterVisitor.java`
#### Snippet
```java
    public String getQuery() {
        StringBuilder sb = removeStringBuilder();
        return sb == null ? null : topBuilder.toString() + " WHERE " + sb.toString();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractUntypedSearchConditionVisitor.java`
#### Snippet
```java
    public String getQuery() {
        StringBuilder sb = removeStringBuilder();
        return sb == null ? null : sb.toString();
    }
}
```

### ReturnNull
Return of `null`
in `rt/security/src/main/java/org/apache/cxf/rt/security/utils/SecurityUtils.java`
#### Snippet
```java
            return (URL)o;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/security/src/main/java/org/apache/cxf/rt/security/utils/SecurityUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/fiql/FiqlParser.java`
#### Snippet
```java
                return new Comparison(name, operator, castedValue);
            }
            return null;
        }
        throw new SearchParseException("Not a comparison expression: " + expr);
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/client/FiqlSearchConditionBuilder.java`
#### Snippet
```java
        protected String toString(Object value) {
            if (value == null) {
                return null;
            }
            if (value.getClass() == Date.class) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcIdTokenProvider.java`
#### Snippet
```java
            };
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcUserInfoProvider.java`
#### Snippet
```java
            };
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
                DateTools.dateToString(new Date(), Resolution.MILLISECOND), minInclusive, maxInclusive);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
    public Query getQuery() {
        List<Query> queries = state.get().peek();
        return queries.isEmpty() ? null : queries.get(0);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java

    private< N > N getMin(final ConditionType type, final N value) {
        return type == ConditionType.LESS_THAN || type == ConditionType.LESS_OR_EQUALS ? null : value;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
    private< N > N getMax(final ConditionType type, final N value) {
        return type == ConditionType.GREATER_THAN || type == ConditionType.GREATER_OR_EQUALS
            ? null : value;
    }

```

### ReturnNull
Return of `null`
in `rt/security/src/main/java/org/apache/cxf/rt/security/crypto/CryptoUtils.java`
#### Snippet
```java
            return new IvParameterSpec(iv);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/security/src/main/java/org/apache/cxf/rt/security/crypto/CryptoUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public static Certificate loadCertificate(InputStream storeLocation, char[] storePassword, String alias,
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/IdTokenResponseFilter.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/ClaimsRequest.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/ClaimRequirement.java`
#### Snippet
```java
            return CastUtils.cast((List<?>)prop);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
        if (defaultBus == null) {
            // never set up.
            return null;
        }
        return defaultBus;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
                return orig;
            }
            return null;
        }
        BusHolder b = getThreadBusHolder(true);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
            }
        }
        return h == null || h.stale ? null : h.bus;
    }
    private static synchronized Bus createThreadBus() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java

    public List<CachedOutputStreamCallback> getCallbacks() {
        return callbacks == null ? null : Collections.unmodifiableList(callbacks);
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java

    public File getTempFile() {
        return tempFile != null && tempFile.exists() ? tempFile : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
                return new ByteArrayInputStream(((ByteArrayOutputStream) currentStream).toByteArray());
            } else {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
                                                    Class<T> annClass) {
        if (m == null) {
            return null;
        }
        return ReflectionUtil.getAnnotationForMethodOrContainingClass(m,  annClass);
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        List<String> headerOriginValues = getHeaderValues(CorsHeaderConstants.HEADER_ORIGIN, true);
        if (headerOriginValues == null || headerOriginValues.size() != 1) {
            return null;
        }
        String origin = headerOriginValues.get(0);
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            method = getResourceMethod(m, requestMethod);
            if (method == null) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            m.put(LOCAL_PREFLIGHT_ORIGIN, origin);
            m.put(LOCAL_PREFLIGHT_METHOD, method);
            return null; // let the resource method take all responsibility.
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            return (List<String>)obj;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            JAXRSUtils.selectResourceClass(resources, requestUri, m);
        if (matchedResources == null) {
            return null;
        }
        MultivaluedMap<String, String> values = new MetadataMap<>();
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        MultivaluedMap<String, String> values = new MetadataMap<>();
        OperationResourceInfo ori = findPreflightMethod(matchedResources, requestUri, httpMethod, values, m);
        return ori == null ? null : ori.getAnnotatedMethod();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            int ma = ann.maxAge();
            if (ma < 0) {
                return null;
            }
            return Integer.valueOf(ma);
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
                                    false);
        if (ori == null) {
            return null;
        }
        if (ori.isSubResourceLocator()) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
            ClassResourceInfo subcri = ori.getClassResourceInfo().getSubResource(cls, cls);
            if (subcri == null) {
                return null;
            }
            MultivaluedMap<String, String> newValues = new MetadataMap<>();
```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // 5.1.1 there has to be an origin
        if (headerOriginValues == null || headerOriginValues.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // 5.1.2 check all the origins
        if (!effectiveAllowOrigins(ann, headerOriginValues)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // note what kind of processing we're doing.
        m.getExchange().put(CrossOriginResourceSharingFilter.class.getName(), SIMPLE_REQUEST);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
            CastUtils.cast((Map<?, ?>)getProperty(OidcUtils.CLAIM_SOURCES_PROPERTY));
        if (names == null || sources == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
            return new UserAddress(map);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
            CastUtils.cast((Map<?, ?>)getProperty(OidcUtils.CLAIM_SOURCES_PROPERTY));
        if (names == null || sources == null) {
            return null;
        }
        DistributedClaims claims = new DistributedClaims();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/AddressingProperties.java`
#### Snippet
```java
    public AttributedURIType getTo() {
        if (toURIType == null) {
            return null != to ? to.getAddress() : null;
        }
        return toURIType;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java

    public List<CachedWriterCallback> getCallbacks() {
        return callbacks == null ? null : Collections.unmodifiableList(callbacks);
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
                return new CharArrayReader(lcaw.rawCharArray(), 0, lcaw.size());
            }
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java

    public File getTempFile() {
        return tempFile != null && tempFile.exists() ? tempFile : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
            }
            public MessageObserver getMessageObserver() {
                return null;
            }
            public void shutdown() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/ContextUtils.java`
#### Snippet
```java
                    return c;
                }
                return null;
            }
            public MessageObserver getMessageObserver() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContext.java`
#### Snippet
```java
                        return cpr;
                    }
                    return null;
                }
            });
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContext.java`
#### Snippet
```java
        } catch (AccessControlException ex) {
            //cannot read the user config file
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBusFactory.java`
#### Snippet
```java
            LOG.log(Level.FINEST, "Could not find NamespaceHandlerResolver",  t);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/OidcImplicitService.java`
#### Snippet
```java
            return processIdToken(state, idToken);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
            ? ContextJAXBUtils.getJAXBContext() : Names200408.WSA_NAMESPACE_NAME.equals(exposedURI) ? Names200408
                .getJAXBContext() : Names200403.WSA_NAMESPACE_NAME.equals(exposedURI) ? Names200403
                .getJAXBContext() : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
        }
        if (tns == null) {
            return null;
        }
        JAXBContext ctx = getExposedJAXBContext(tns);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
            ? EndpointReferenceType.class
                : Names200408.WSA_NAMESPACE_NAME.equals(exposedURI) ? Names200408.EPR_TYPE
                    : Names200403.WSA_NAMESPACE_NAME.equals(exposedURI) ? Names200403.EPR_TYPE : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
            return convert((org.apache.cxf.ws.addressing.v200403.EndpointReferenceType)exposed);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/managers/ServiceContractResolverRegistryImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
            // do nothing here
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/TextExtensionFragmentParser.java`
#### Snippet
```java
        line = line.trim();
        if (line.isEmpty() || line.charAt(0) == '#') {
            return null;
        }
        final Extension ext = new Extension(loader);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/TextExtensionFragmentParser.java`
#### Snippet
```java
        ext.setClassname(parts[0]);
        if (ext.getClassname() == null) {
            return null;
        }
        if (parts.length >= 2) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                        && node.getNodeName().contains("ServiceName")) {
                        Node item = node.getAttributes().getNamedItem("EndpointName");
                        return item != null ? item.getTextContent() : null;
                    }
                } else if (obj instanceof JAXBElement) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
            return a.getValue();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
    public static Schema getSchema(ServiceInfo serviceInfo, Bus b) {
        if (serviceInfo == null) {
            return null;
        }
        Schema schema = serviceInfo.getProperty(Schema.class.getName(), Schema.class);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
        MetadataType metadata = ref.getMetadata();
        if (metadata == null) {
            return null;
        }
        for (Object obj : metadata.getAny()) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
            LSInputImpl impl = null;
            if (done.contains(newId + ":" + namespaceURI)) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                    newId = url.toString();
                    if (done.contains(newId + ":" + namespaceURI)) {
                        return null;
                    }
                    if (schemas.containsKey(newId + ":" + namespaceURI)) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
        MetadataType metadata = ref.getMetadata();
        if (metadata == null) {
            return null;
        }
        for (Object obj : metadata.getAny()) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java

        if (null == wsdlLocation) {
            return null;
        }
        return wsdlLocation;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
    public Class<?> getClassObject(ClassLoader cl) {
        if (notFound) {
            return null;
        }
        if (clazz != null) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
        try {
            if (notFound) {
                return null;
            }
            try {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/phase/AbstractPhaseInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/spi/GeneratedClassClassLoader.java`
#### Snippet
```java
            LOG.fine("Failed to load class :" + e.toString());
        }
        return null;
    }
    public TypeHelperClassLoader getClassLoader() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/i18n/Exception.java`
#### Snippet
```java
            return message.getCode();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/i18n/Exception.java`
#### Snippet
```java
            return message.toString();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java
            return message.toString();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java
            return message.getCode();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/spi/ClassLoaderProxyService.java`
#### Snippet
```java
        } catch (Throwable e) {
            LOG.warning("NamespaceWrapper not found : " + e.toString());
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/spi/NamespaceClassGenerator.java`
#### Snippet
```java
        ASMHelper.ClassWriter cw = helper.createClassWriter();
        if (cw == null) {
            return null;
        }
        ASMHelper.FieldVisitor fv;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/spi/NamespaceClassGenerator.java`
#### Snippet
```java
        ASMHelper.ClassWriter cw = helper.createClassWriter();
        if (cw == null) {
            return null;
        }
        String superName = "org/eclipse/persistence/internal/oxm/record/namespaces/MapNamespacePrefixMapper";
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
        }
        if (el == null && type == null) {
            return null;
        }
        final QName typeName = type == null ? null : type.getQName();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/EscapeHandlerInvocationHandler.java`
#### Snippet
```java
                Writer writer = (Writer)args[4];
                writer.write("");
                return null;
            }
            result =  method.invoke(target, args);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/SystemPropertyAction.java`
#### Snippet
```java
        } catch (SecurityException ex) {
            LOG.log(Level.FINE, "SecurityException raised getting property " + name, ex);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
                    return extensionManager.getExtension(resourceName, resourceType);
                }
                return null;
            }
            public InputStream getAsStream(String name) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
            }
            public InputStream getAsStream(String name) {
                return null;
            }
        });
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
            if (missingExtensions.contains(extensionType)) {
                //already know we cannot find it
                return null;
            }
            ConfiguredBeanLocator loc = (ConfiguredBeanLocator)extensions.get(ConfiguredBeanLocator.class);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
        //record that it couldn't be found to avoid expensive searches again in the future
        missingExtensions.add(extensionType);
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java

        // otherwise use null so the default will be used
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionInvokationHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    public <T> T getBeanOfType(String name, Class<T> type) {
        if (name == null) {
            return null;
        }
        Extension ex = all.get(name);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            return type.cast(ex.getLoadedObject());
        }
        return null;
    }
    public <T> Collection<? extends T> getBeansOfType(Class<T> type) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    public <T> T getExtension(String name, Class<T> type) {
        if (name == null) {
            return null;
        }
        Extension e = all.get(name);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            return new char[0];
        } else if (l <= 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        //
        if ((l - o) < 4) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
            return a.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
            return ReflectionInvokationHandler.createProxyWrapper(newCw, ClassWriter.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
    public static String relativize(URI baseURI, URI toBeRelativizedURI) throws URISyntaxException {
        if (baseURI == null || toBeRelativizedURI == null) {
            return null;
        }
        if (baseURI.isAbsolute() ^ toBeRelativizedURI.isAbsolute()) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
        }
        if (baseURI.isAbsolute() ^ toBeRelativizedURI.isAbsolute()) {
            return null;
        }
        final String base = baseURI.getSchemeSpecificPart();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
    public static String getAbsoluteURI(final String arg) {
        if (arg == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
    public static String relativize(String base, String toBeRelativized) throws URISyntaxException {
        if (base == null || toBeRelativized == null) {
            return null;
        }
        return relativize(new URI(base), new URI(toBeRelativized));
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/PropertyUtils.java`
#### Snippet
```java
            return Integer.valueOf((String)o);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/PropertyUtils.java`
#### Snippet
```java
            return Long.valueOf(o.toString());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/PackageUtils.java`
#### Snippet
```java
    public static String getNamespace(String packageName) {
        if (packageName == null || packageName.isEmpty()) {
            return null;
        }
        final List<String> parts = Arrays.asList(packageName.split("\\."));
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/PackageUtils.java`
#### Snippet
```java
        List<String> tokens = tokenize(nameSpaceURI, "/:");
        if (tokens.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ProxyClassLoader.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        T[] tmp = data.get();
        if (tmp == null || tmp.length == 0) {
            return null;
        }
        return tmp[0];
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java

    public Comparator<? super T> comparator() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        T[] tmp = data.get();
        if (tmp == null || tmp.length == 0) {
            return null;
        }
        return tmp[tmp.length - 1];
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/CachedClass.java`
#### Snippet
```java

    public Class<?> getCachedClass() {
        return cachedClazz == null ? null : cachedClazz.get();
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
            return getAnnotationForInterface(intf2, annotationType);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return cls.getConstructor(args);
                } catch (SecurityException | NoSuchMethodException e) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return cls.getDeclaredConstructor(args);
                } catch (SecurityException | NoSuchMethodException e) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                return descriptors.toArray(new PropertyDescriptor[0]);
            }
            return null;
        }
        return beanInfo.getPropertyDescriptors();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return responseClass.cast(f.get(o));
                } catch (SecurityException | IllegalAccessException e) {
                    return null;
                } finally {
                    f.setAccessible(b);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return responseClass.cast(f.get(o));
                } catch (SecurityException | IllegalAccessException e) {
                    return null;
                } finally {
                    f.setAccessible(b);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return (Constructor<T>[])cls.getDeclaredConstructors();
                } catch (SecurityException e) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    return cls.getDeclaredField(name);
                } catch (SecurityException | NoSuchFieldException e) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/Base64UrlUtility.java`
#### Snippet
```java
            return new String(chunk);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/CollectionUtils.java`
#### Snippet
```java

        public T get(Object key) {
            return map != null ? map.get(key) : null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/CollectionUtils.java`
#### Snippet
```java

        public Enumeration<S> keys() {
            return map != null ? new IteratorToEnumeration<>(map.keySet().iterator()) : null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/CollectionUtils.java`
#### Snippet
```java

        public Enumeration<T> elements() {
            return map != null ? new IteratorToEnumeration<>(map.values().iterator()) : null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/CollectionUtils.java`
#### Snippet
```java

            public X nextElement() {
                return iter != null ? iter.next() : null;
            }
        }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/StringUtils.java`
#### Snippet
```java
    public static String getFirstFound(String contents, String regex) {
        if (isEmpty(regex) || isEmpty(contents)) {
            return null;
        }
        Pattern pattern = Pattern.compile(regex, Pattern.UNICODE_CASE);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/util/StringUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            throw ExceptionUtils.toBadRequestException(null, null);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            return getQNameFromParts(name, ns, type, clsMap);
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    private SchemaCollection getSchemaCollection(ResourceTypes resourceTypes, JAXBContext context) {
        if (context == null) {
            return null;
        }
        SchemaCollection xmlSchemaCollection = new SchemaCollection();
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        } catch (IOException e) {
            LOG.fine("No schema can be generated");
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            JAXBBeanInfo jaxbInfo = jaxbProxy == null ? null : JAXBUtils.getBeanInfo(jaxbProxy, type);
            if (jaxbInfo == null) {
                return null;
            }
            Object instance = type.getDeclaredConstructor().newInstance();
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (isFormParameter(params.get(i), getMethod(ori).getParameterTypes()[i], getMethod(ori)
                .getParameterAnnotations()[i])) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                return getQNameFromParts(qname.getLocalPart(), qname.getNamespaceURI(), type, clsMap);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    private QName getQNameFromParts(String name, String namespace, Class<?> type, Map<Class<?>, QName> clsMap) {
        if (namespace == null || JAXB_DEFAULT_NAMESPACE.equals(namespace) || namespace.length() == 0) {
            return null;
        }
        if (name == null || name.length() == 0) {
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        }
        if (name == null || name.length() == 0) {
            return null;
        }
        if (JAXB_DEFAULT_NAME.equals(name)) {
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                return new JaxbContextQNameResolver(proxy);
            }
            return null;
        } else if (externalQnamesMap != null) {
            return new SchemaQNameResolver(externalQnamesMap);
```

### ReturnNull
Return of `null`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            }
        }
        return null;
    }
    private Response finalizeExistingWadlResponse(Document wadlDoc, Message m, UriInfo ui, MediaType mt)
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
    @Override
    public Logger getParent() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/logging/LogUtils.java`
#### Snippet
```java
                public Object run() {
                    Thread.currentThread().setContextClassLoader(classLoader);
                    return null;
                }
            });
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
        XmlSchemaContentModel model = type.getContentModel();
        if (model == null) {
            return null;
        }
        XmlSchemaContent content = model.getContent();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
        XmlSchemaContent content = model.getContent();
        if (content == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java

        if (!(content instanceof XmlSchemaComplexContentExtension)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
        XmlSchemaContentModel model = type.getContentModel();
        if (model == null) {
            return null;
        }
        XmlSchemaContent content = model.getContent();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
        XmlSchemaContent content = model.getContent();
        if (content == null) {
            return null;
        }
        if (!(content instanceof XmlSchemaComplexContentExtension)) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
        }
        if (!(content instanceof XmlSchemaComplexContentExtension)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
            return new QName(schema.getTargetNamespace(), element.getName());
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/SchemaCollection.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/xmlschema/SchemaCollection.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
            return oasisCatalog;
        } catch (Throwable ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
    public String resolveURI(String uri) throws MalformedURLException, IOException {
        if (catalog == null) {
            return null;
        }
        return ((Catalog)catalog).resolveURI(uri);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }
    private static EntityResolver getResolver() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
    public String resolveSystem(String sys) throws MalformedURLException, IOException {
        if (catalog == null) {
            return null;
        }
        return ((Catalog)catalog).resolveSystem(sys);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/catalog/OASISCatalogManager.java`
#### Snippet
```java
    public String resolvePublic(String uri, String parent) throws MalformedURLException, IOException {
        if (resolver == null) {
            return null;
        }
        return ((Catalog)catalog).resolvePublic(uri, parent);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
    private static Field getField(Class<?> cls, String name) {
        if (cls == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
    private Object resolveResource(String resourceName, Class<?> type) {
        if (resourceManager == null) {
            return null;
        }
        return resourceManager.resolveResource(resourceName, type, resourceResolvers);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java

        try {
            return (url != null) ? url.openStream() : null;
        } catch (IOException e) {
            return null;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java
            return (url != null) ? url.openStream() : null;
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java
            }
            public URL nextElement() {
                return null;
            }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
    public static String findCharset(String contentType) {
        if (contentType == null) {
            return null;
        }
        int idx = contentType.indexOf("charset=");
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
            }
            if (charset.isEmpty()) {
                return null;
            }
            if (charset.charAt(0) == '\"') {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
            return charset;
        }
        return null;
    }
    public static String mapCharset(String enc) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
                newenc = Charset.forName(enc).name();
            } catch (IllegalCharsetNameException | UnsupportedCharsetException e) {
                return null;
            }
            String tmpenc = encodings.putIfAbsent(enc, newenc);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/NSStack.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/NSStack.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/ServiceUtils.java`
#### Snippet
```java
            return getSchemaValidationType(obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/ServiceUtils.java`
#### Snippet
```java
            return getSchemaValidationType(obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/MapNamespaceContext.java`
#### Snippet
```java

    public Iterator<String> getPrefixes(String namespaceURI) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/MapNamespaceContext.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
                return i.getSchemas();
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
    public static InputStream nullOrNotEmptyStream(InputStream is) throws IOException {
        if (isEmpty(is)) {
            return null;
        } else if (!(is instanceof PushbackInputStream)) {
            final byte[] bytes = new byte[1];
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
            }
            
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/XPathUtils.java`
#### Snippet
```java
            return xpath.evaluate(xpathExpression, node, type);
        } catch (Exception e) {
            return null;
        } finally {
            if (loader != null) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
    public static String stripPath(String name) {
        if (name == null) {
            return null;
        }
        int posUnix = name.lastIndexOf('/');
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/message/MessageContentsList.java`
#### Snippet
```java
        List<Object> o = CastUtils.cast(msg.getContent(List.class));
        if (o == null) {
            return null;
        }
        if (!(o instanceof MessageContentsList)) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/message/MessageImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/message/MessageImpl.java`
#### Snippet
```java
    public String getAttachmentMimeType() {
        //for sub class overriding
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/message/MessageUtils.java`
#### Snippet
```java
            return FaultMode.RUNTIME_FAULT;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/message/ExchangeImpl.java`
#### Snippet
```java
        return get(ConduitSelector.class) != null
               ? get(ConduitSelector.class).selectConduit(message)
               : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/ServiceImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        int ind2 = expandedQName.indexOf('}');
        if (ind2 <= ind1 + 1 || ind2 >= expandedQName.length() - 1) {
            return null;
        }
        String ns = expandedQName.substring(ind1 + 1, ind2);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        }
        if (n == null) {
            return null;
        }
        return n;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        NamedNodeMap attrs = element.getAttributes();
        if (attrs == null) {
            return null;
        }
        Node attN = attrs.getNamedItem(attName);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Node attN = attrs.getNamedItem(attName);
        if (attN == null) {
            return null;
        }
        return attN.getNodeValue();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            attr = element.getAttributeNodeNS(attName.getNamespaceURI(), attName.getLocalPart());
        }
        return attr == null ? null : attr.getValue();
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Node first = current.getNextSibling();
        if (first == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
    public static QName createQName(String qualifiedName, Node node) {
        if (qualifiedName == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Node first = parent.getFirstChild();
        if (first == null) {
            return null;
        }
        for (Node node = first; node != null; node = node.getNextSibling()) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        }
        if (n == null) {
            return null;
        }
        return (Element)n;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            } catch (NoSuchMethodException e) {
                //best effort to try, do nothing if NoSuchMethodException
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            nd = nd.getNextSibling();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
    public static String getRawContent(Node n) {
        if (n == null) {
            return null;
        }
        StringBuilder b = null;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
    public static Node getChild(Node parent, String name) {
        if (parent == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Node first = parent.getFirstChild();
        if (first == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        Attr node = e.getAttributeNode(attributeName);
        if (node == null) {
            return null;
        }
        return node.getValue();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/MessagePartInfo.java`
#### Snippet
```java
            return ((XmlSchemaElement)xmlSchema).getSchemaTypeName();
        }
        return null;
    }
    public void setTypeQName(QName qn) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/MessagePartInfo.java`
#### Snippet
```java
            return elementName;
        }
        return null;
    }
    public QName getTypeQName() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/BindingOperationInfo.java`
#### Snippet
```java
            return faults.get(name);
        }
        return null;
    }
    public Collection<BindingFaultInfo> getFaults() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/SchemaInfo.java`
#### Snippet
```java
            return schema.getElementByName(qname);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
            return faults.get(name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public void addEndpoint(EndpointInfo ep) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public void addBinding(BindingInfo binding) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/BindingInfo.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/BindingInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/BindingInfo.java`
#### Snippet
```java
    public DescriptionInfo getDescription() {
        if (service == null) {
            return null;
        }
        return service.getDescription();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/EndpointInfo.java`
#### Snippet
```java
    public InterfaceInfo getInterface() {
        if (service == null) {
            return null;
        }
        return service.getInterface();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/EndpointInfo.java`
#### Snippet
```java
    public DescriptionInfo getDescription() {
        if (service == null) {
            return null;
        }
        return service.getDescription();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/EndpointInfo.java`
#### Snippet
```java

    public String getAddress() {
        return (null != address && null != address.getAddress()) ? address.getAddress().getValue() : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractMessageContainer.java`
#### Snippet
```java
            return outOfBandParts.get(0);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractMessageContainer.java`
#### Snippet
```java
    public MessagePartInfo getMessagePart(int n) {
        if (n == -1) {
            return null;
        }
        List<MessagePartInfo> mpis = getMessageParts();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractMessageContainer.java`
#### Snippet
```java
        }
        List<MessagePartInfo> mpis = getMessageParts();
        return n < mpis.size() ? mpis.get(n) : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractMessageContainer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/InterfaceInfo.java`
#### Snippet
```java
    public DescriptionInfo getDescription() {
        if (service == null) {
            return null;
        }
        return service.getDescription();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/factory/SimpleMethodDispatcher.java`
#### Snippet
```java
        Map<BindingInfo, BindingOperationInfo> bops = infoMap.get(method);
        if (bops == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/factory/SimpleMethodDispatcher.java`
#### Snippet
```java
            OperationInfo o = methodToOp.get(method);
            if (o == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
        Endpoint ep = exchange.getEndpoint();
        if (ep == null) {
            return null;
        }
        BindingInfo service = ep.getEndpointInfo().getBinding();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
        Endpoint ep = exchange.getEndpoint();
        if (ep == null) {
            return null;
        }
        BindingInfo service = ep.getEndpointInfo().getBinding();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
        Endpoint ep = exchange.getEndpoint();
        if (ep == null) {
            return null;
        }
        BindingInfo service = ep.getEndpointInfo().getBinding();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/factory/AbstractServiceFactoryBean.java`
#### Snippet
```java
    }
    protected DataBinding createDefaultDataBinding() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
            return delegate.getExtensionAttribute(name);
        }
        return null == extensionAttributes ? null : extensionAttributes.get(name);
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
        }
        if (null == propertyMap.get()) {
            return null;
        }
        return propertyMap.get().get(name);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
        }
        if (null == propertyMap.get()) {
            return null;
        }
        return propertyMap.get().remove(name);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
        Object[] exts = extensors.get();
        if (exts == null) {
            return null;
        }
        List<T> list = new ArrayList<>(exts.length);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
        Object[] exts = extensors.get();
        if (exts == null) {
            return null;
        }
        for (int x = 0; x < exts.length; x++) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/model/AbstractPropertiesHolder.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public <T> List<T> getExtensors(Class<T> cls) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/NullConduitSelector.java`
#### Snippet
```java
     */
    public Conduit selectConduit(Message message) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            return ".internal";
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            return nameSpaceURIToPackage(new URI(namespaceURI));
        } catch (URISyntaxException ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        if (cls == Object.class || cls == String.class || cls.isPrimitive() || cls.isAnnotation()
            || "jakarta.xml.ws.Holder".equals(cls.getName())) {
            return null;
        } else if (cls.isInterface()
            || "jakarta.xml.ws.wsaddressing.W3CEndpointReference".equals(cls.getName())) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            cons = ReflectionUtil.getConstructor(cls);
            if (cons == null) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        Object o = context.getBeanInfo(cls);
        if (o == null) {
            return null;
        }
        if (o instanceof JAXBBeanInfo) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
                LOG.log(Level.WARNING, "Failed to create" + simpleClassName + " for unknown jaxb class:"
                    + cls);
                return null;
            }
            Class<?> handlerClass = ClassLoaderUtils.loadClass("org.glassfish.jaxb.core.marshaller."
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public static JAXBContext createContext(final Set<Class<?>> classes,
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ExtendedURIResolver.java`
#### Snippet
```java
            // so only store it away when everything else is done
        }
        return null;
        // return new InputSource(schemaLocation);
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/service/invoker/AbstractInvoker.java`
#### Snippet
```java

            if (exchange.isOneWay()) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/SinglePropertyResolver.java`
#### Snippet
```java
            return resourceType.cast(value);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/SinglePropertyResolver.java`
#### Snippet
```java

    public InputStream getAsStream(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ClasspathResolver.java`
#### Snippet
```java
    public <T> T resolve(String resourceName, Class<T> resourceType) {
        if (resourceName == null) {
            return null;
        }
        URL url = ClassLoader.getSystemResource(resourceName);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ClasspathResolver.java`
#### Snippet
```java
            return resourceType.cast(url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/PropertiesResolver.java`
#### Snippet
```java

    public InputStream getAsStream(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/PropertiesResolver.java`
#### Snippet
```java
    public <T> T resolve(String resourceName, Class<T> resourceType) {
        if (resourceName == null) {
            return null;
        }
        Object obj = properties.get(resourceName);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/PropertiesResolver.java`
#### Snippet
```java
            return resourceType.cast(properties.get(resourceName));
        }
        return null;


```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ObjectTypeResolver.java`
#### Snippet
```java

    public InputStream getAsStream(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ObjectTypeResolver.java`
#### Snippet
```java
            return resourceType.cast(value);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ClassLoaderResolver.java`
#### Snippet
```java
    public <T> T resolve(String resourceName, Class<T> resourceType) {
        if (resourceName == null || resourceType == null) {
            return null;
        }
        URL url = loader.getResource(resourceName);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/ClassLoaderResolver.java`
#### Snippet
```java
            return resourceType.cast(url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
            return path.replace("%20", " ");
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StreamWriterContentHandler.java`
#### Snippet
```java
        } else {
            //this is the case that namespaceURI is just empty, so NS_PREFIX is null
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
                return parent.getPrefix(namespaceURI);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
    public Object getProperty(String name) {
        //nothing
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public ErrorHandler getErrorHandler() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public DTDHandler getDTDHandler() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public Object getProperty(String name) throws SAXNotRecognizedException, SAXNotSupportedException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java

    public EntityResolver getEntityResolver() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CNamespaceContext.java`
#### Snippet
```java
    private String getPrefix(Element e, String uri) {
        if (e == null) {
            return null;
        }
        // check the outside namespace URI
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CNamespaceContext.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CNamespaceContext.java`
#### Snippet
```java
    private String getNamespaceURI(Element e, String name) {
        if (e == null) {
            return null;
        }
        // check the outside namespace URI
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CNamespaceContext.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamReader.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamReader.java`
#### Snippet
```java

    public String getEncoding() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamReader.java`
#### Snippet
```java

        if (at == null) {
            return null;
        }
        return at.getNodeValue();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/transform/QNamesMap.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/transform/DelegatingNamespaceContext.java`
#### Snippet
```java
    public String findUniquePrefix(String ns) {
        if (ns.isEmpty()) {
            return null;
        }
        String existingPrefix = getPrefix(ns);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/transform/DelegatingNamespaceContext.java`
#### Snippet
```java
    public String getPrefix(String ns) {
        if (ns.isEmpty()) {
            return null;
        }
        String value = nsMap.get(ns);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/transform/DelegatingNamespaceContext.java`
#### Snippet
```java
        String value = nsMap.get(ns);
        if (value != null && value.isEmpty()) {
            return null;
        }
        if (value != null) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamWriter.java`
#### Snippet
```java
            return (DocumentFragment)currentNode;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamWriter.java`
#### Snippet
```java

    public String getPrefix(String uri) throws XMLStreamException {
        return context == null ? null : context.getPrefix(uri);
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamWriter.java`
#### Snippet
```java
            return (Element)currentNode;
        }
        return null;
    }
    public DocumentFragment getCurrentFragment() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/validation/Stax2ValidationUtils.java`
#### Snippet
```java
            if (ret == null) {
                if (endpoint.containsKey(KEY)) {
                    return null;
                }
                Map<String, Source> sources = new TreeMap<>();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java

    public String getCharacterEncodingScheme() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java
    }
    public String getPublicId() {
        return null;
    }
    public Location getLocation() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java
     */
    public Object getProperty(String name) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java

    public String getVersion() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java

    public String getSystemId() {
        return null;
    }
    public String getPublicId() {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java
    public T findTransportForNamespace(final String namespace) {
        if (locator == null) {
            return null;
        }
        T factory = loadActivationNamespaces(namespace);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java
    public T findTransportForURI(String uri) {
        if (locator == null) {
            return null;
        }
        //If the uri is related path or has no protocol prefix , we will set it to be http
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/ByteDataSource.java`
#### Snippet
```java

    public OutputStream getOutputStream() throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDataSource.java`
#### Snippet
```java
            return delegate;
        } catch (IOException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
                }
            }
            return null;
        }
        public DataHandler put(String key, DataHandler value) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/ImageDataContentHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        String value = reader.getElementText();
        if (value == null) {
            return null;
        }
        value = value.trim();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/validation/AbstractValidationInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/annotations/FactoryType.java`
#### Snippet
```java
    final class DEFAULT implements Factory {
        public Object create(Exchange e) throws Throwable {
            return null;
        }
        public void release(Exchange e, Object o) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                                @Override
                                public DataHandler setValue(DataHandler value) {
                                    return null;
                                }
                            };
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            return v.get(0);
        }
        return null;
    }
    static String getHeaderValue(List<String> v, String delim) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
    static String getContentDispositionFileName(String cd) {
        if (StringUtils.isEmpty(cd)) {
            return null;
        }
        ContentDisposition c = new ContentDisposition(cd);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        boolean isXop, DataHandler handler, String elementNS, int threshold) {
        if (!isXop) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                File file = fds.getFile();
                if (file.length() < threshold) {
                    return null;
                }
            } else if (ds.getClass().getName().endsWith("ObjectDataSource")) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                if (o instanceof String
                    && ((String)o).length() < threshold) {
                    return null;
                } else if (o instanceof byte[] && ((byte[])o).length < threshold) {
                    return null;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                    return null;
                } else if (o instanceof byte[] && ((byte[])o).length < threshold) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            return String.join(delim, v);
        }
        return null;
    }
    static String getHeader(Map<String, List<String>> headers, String h) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                                                 byte[] data, int offset, int length, int threshold) {
        if (!isXop || length <= threshold) {
            return null;
        }
        if (mimeType == null) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/staxutils/transform/InTransformReader.java`
#### Snippet
```java
    public String getAttributeValue(String namespace, String localName) {
        if (!pushedBackEvents.isEmpty()) {
            return null;
        }
        checkAttributeIndexRange(-1);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
        // Use regex to get the boundary and return null if it's not found
        Matcher m = INPUT_STREAM_BOUNDARY_PATTERN.matcher(msg);
        return m.find() ? "--" + m.group(1) : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
        // Use regex to get the boundary and return null if it's not found
        Matcher m = CONTENT_TYPE_BOUNDARY_PATTERN.matcher(ct);
        return m.find() ? "--" + m.group(1) : null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
        cacheStreamedAttachments();
        if (closed) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
        int v = stream.read();
        if (v == -1) {
            return null;
        }
        stream.unread(v);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            return exchange.getInMessage().getContent(Exception.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            if (null != ep && null != ep.getEndpointInfo() && null == ep.getEndpointInfo().
                getProperty("org.apache.cxf.ws.addressing.MAPAggregator.decoupledDestination")) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
        }

        return null;
    }
    protected Exception getException(Exchange exchange) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java

            if (callback != null) {
                return null;
            }
            return processResult(message, exchange, oi, resContext);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/databinding/AbstractDataBinding.java`
#### Snippet
```java
            if (DOMUtils.getFirstElement(d.getDocumentElement()) == null) {
                hackAroundEmptyNamespaceIssue = true;
                return null;
            }
            // create a copy of the dom so we
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                        }
                        public OutputStream getOutputStream() throws IOException {
                            return null;
                        }
                    };
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }
    private XMLStreamReader resetForStreaming(XMLStreamReader input) throws XMLStreamException {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractInDatabindingInterceptor.java`
#### Snippet
```java
        XMLStreamReader xr = message.getContent(XMLStreamReader.class);
        if (xr == null) {
            return null;
        }
        if (xr instanceof DepthXMLStreamReader) {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/AnnotationInterceptors.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/AnnotationInterceptors.java`
#### Snippet
```java
        List<Interceptor> i = getAnnotationObject(t, Interceptor.class);
        if (i == null) {
            return null;
        }
        List<Interceptor<? extends Message>> m = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/StaxOutInterceptor.java`
#### Snippet
```java
            return xif;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/DefaultSecurityContext.java`
#### Snippet
```java
    private static Principal findPrincipal(String principalName, Subject subject) {
        if (subject == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/DefaultSecurityContext.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
    Logger getMessageLogger(Message message) {
        if (isLoggingDisabledNow(message)) {
            return null;
        }
        Endpoint ep = message.getExchange().getEndpoint();
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/StaxInInterceptor.java`
#### Snippet
```java
            return xif;
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
            // do nothing
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/JAASLoginInterceptor.java`
#### Snippet
```java
    private String getUsername(CallbackHandler handler) {
        if (handler == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/JAASLoginInterceptor.java`
#### Snippet
```java
            return usernameCallBack.getName();
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/JAASLoginInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/RolePrefixSecurityContextImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/callback/CallbackHandlerProviderUsernameToken.java`
#### Snippet
```java
        SecurityToken token = message.get(SecurityToken.class);
        if (!(token instanceof UsernameToken)) {
            return null;
        }
        UsernameToken ut = (UsernameToken)token;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/callback/CallbackHandlerTlsCert.java`
#### Snippet
```java
        TLSSessionInfo tlsSession = message.get(TLSSessionInfo.class);
        if (tlsSession == null) {
            return null;
        }
        Certificate cert = getCertificate(message);
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/callback/CallbackHandlerProviderAuthPol.java`
#### Snippet
```java
        AuthorizationPolicy policy = message.get(AuthorizationPolicy.class);
        if (policy == null) {
            return null;
        }
        return new NamePasswordCallbackHandler(policy.getUserName(), policy.getPassword());
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/interceptor/security/SecureAnnotationsInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java
        String callbackHandlerClass = kmc.getKeyPasswordCallbackHandler();
        if (callbackHandlerClass == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java

        if (kst == null) {
            return null;
        }
        final String type;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java

        if (pst == null) {
            return null;
        }
        String type;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/continuations/SuspendedInvocationException.java`
#### Snippet
```java
    public RuntimeException getRuntimeException() {
        Throwable ex = getCause();
        return ex instanceof RuntimeException ? (RuntimeException)ex : null;
    }
}
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractFactoryBeanDefinitionParser.java`
#### Snippet
```java

    protected String getDestroyMethod() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java

    public ObjectName getMBeanName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractEndpoint.java`
#### Snippet
```java
            return endpointEpr;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractCreatePullPoint.java`
#### Snippet
```java
            return new ObjectName("org.apache.cxf.service.wsn:type=CreatePullPoint");
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractSubscription.java`
#### Snippet
```java
            // Ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsTopicExpressionConverter.java`
#### Snippet
```java
            return createActiveMQTopicFromQName((QName) contentItem);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            return jvmCipherSuites.split(",");
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
        }
        if (defaultManagers.length == 0) {
            return null;
        }
        return defaultManagers;
```

### ReturnNull
Return of `null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
    protected static byte[] loadFile(String fileName) throws IOException {
        if (fileName == null) {
            return null;
        }
        Path path = FileSystems.getDefault().getPath(fileName);
```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsPublisher.java`
#### Snippet
```java
        } catch (Exception e) {
            LOGGER.log(Level.INFO, "Error while subscribing on-demand publisher", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractNotificationBroker.java`
#### Snippet
```java
            return new ObjectName("org.apache.cxf.service.wsn:type=WSNotificationBroker");
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java
            return (Class<T>)pType.getRawType();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpContextHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpDestination.java`
#### Snippet
```java
            return new URL(addr).getPath();
        } catch (MalformedURLException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngine.java`
#### Snippet
```java
        } catch (InterruptedException ex) {
            // do nothing here
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngine.java`
#### Snippet
```java
            return contextHandler.getNettyHttpHandler(url.getPath());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettySpringTypesFactory.java`
#### Snippet
```java
                                     Node data, Class<?> c) {
        if (u == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java

        if (contentType == null) {
            return null;
        }
        int start = contentType.indexOf("charset=");
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
        int start = contentType.indexOf("charset=");
        if (start < 0) {
            return null;
        }
        String encoding = contentType.substring(start + 8);
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java

        if (acceptLanguageHeader == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
            return foundCookie;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/util/Utils.java`
#### Snippet
```java
                || uri.contains("." + File.separator) || uri.startsWith(".")
                || uri.endsWith(".")) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpSession.java`
#### Snippet
```java
    @Override
    public Object getAttribute(String name) {
        return attributes != null ? attributes.get(name) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpSession.java`
#### Snippet
```java
    public ServletContext getServletContext() {
        // TODO do we need to support this
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpSession.java`
#### Snippet
```java
    public String[] getValueNames() {
        if (attributes == null) {
            return null;
        }
        return attributes.keySet().toArray(
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletContext.java`
#### Snippet
```java
    public String getInitParameter(String name) {
        if (this.initParameters == null) {
            return null;
        }
        return this.initParameters.get(name);
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletContext.java`
#### Snippet
```java
    @Override
    public Object getAttribute(String name) {
        return attributes != null ? attributes.get(name) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public String getRemoteUser() {
        // CXF Calls this method to cache the Request informaiton
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
        List<String> values = this.queryStringDecoder.parameters().get(name);
        if (values == null || values.isEmpty()) {
            return null;
        }
        return values.toArray(new String[0]);
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public String getRequestedSessionId() {
        // doesn't implement it yet
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
            return this.attributes.get(name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public Principal getUserPrincipal() {
        // CXF will call this method to setup user information for Authentication
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public HttpSession getSession(boolean create) {
        // doesn't implement it yet
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public String getPathTranslated() {
        // CXF Calls this method to cache the Request informaiton
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public String getParameter(String name) {
        String[] values = getParameterValues(name);
        return values != null ? values[0] : null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public HttpSession getSession() {
        // doesn't implement it yet
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
    public String getAuthType() {
        // CXF Calls this method to cache the Request informaiton
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageQName.java`
#### Snippet
```java
                    return resource.getAttribute("xmlns:" + prefix);
                }
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageQName.java`
#### Snippet
```java
                @Override
                public Node item(int i) {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageXPath10.java`
#### Snippet
```java
            if (checkResultConstraints(result)) {
                if (result.getLength() == 0) {
                    return null;
                }
                return result;
```

### ReturnNull
Return of `null`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageXPath10.java`
#### Snippet
```java
                    return resource.getAttribute("xmlns:" + prefix);
                }
                return null;
            }

```

### ReturnNull
Return of `null`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/DelegateContextAnnotatedType.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/utils/JSONUtils.java`
#### Snippet
```java
                public Iterator<String> getPrefixes(String ns) {
                    String prefix = getPrefix(ns);
                    return prefix == null ? null : Collections.singletonList(prefix).iterator();
                }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/utils/JSONUtils.java`
#### Snippet
```java
                        }
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
            return new QName(qname.getNamespaceURI(), qname.getLocalPart(), prefix);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        if (isPayloadEmpty(headers)) {
            if (AnnotationUtils.getAnnotation(anns, Nullable.class) != null) {
                return null;
            }
            reportEmptyContentLength();
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        }
        // unreachable
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
                        new Http2MultiplexHandler(createHttp2ChannelInitializer()));
                } else {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
            return new SslHandler(sslEngine);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/MutableHttpEntity.java`
#### Snippet
```java
    @Override
    public Supplier<List<? extends Header>> getTrailers() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/JAXRSCdiResourceExtension.java`
#### Snippet
```java
            return Class.forName(name);
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/WebFaultInInterceptor.java`
#### Snippet
```java

        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                                     EndpointReferenceType target) throws IOException {
        if (isShutdown) {
            return null;
        }
        return new AsyncHTTPConduit(bus, localInfo, target, this);
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            public HttpUriRequest getRedirect(HttpRequest request, HttpResponse response, HttpContext context)
                throws ProtocolException {
                return null;
            }
        };
```

### ReturnNull
Return of `null`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/ColocOutInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        protected HttpsURLConnectionInfo getHttpsURLConnectionInfo() throws IOException {
            if ("http".equals(outMessage.get("http.scheme"))) {
                return null;
            }
            connect(true);
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                                AsyncHTTPConduit.this.proxyAuthorizationPolicy.getPassword());
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        }
        if (list.isEmpty()) {
            return null;
        }
        return list.toArray(new String[0]);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AuthPolicyValidatingInterceptor.java`
#### Snippet
```java
    @Override
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
    static final TokenStore getTokenStore(Message message) throws TokenStoreException {
        if (message == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            EndpointReferenceType target) throws IOException {
        if (isShutdown) {
            return null;
        }
        return new AsyncHTTPConduit(bus, localInfo, target, this);
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            public URI getLocationURI(HttpRequest request, HttpResponse response, HttpContext context)
                    throws ProtocolException {
                return null;
            }
        };
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSSamlAssertionValidator.java`
#### Snippet
```java
                    ex);
            trustVerificationSucceeded = false;
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/DefaultSTSTokenCacher.java`
#### Snippet
```java
            throws TokenStoreException {
        if (delegationToken == null) {
            return null;
        }
        TokenStore tokenStore = TokenStoreUtils.getTokenStore(message);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/DefaultSTSTokenCacher.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/delegation/ReceivedTokenCallbackHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/delegation/ReceivedTokenCallbackHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
            el = DOMUtils.getNextElement(el);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenRetriever.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        protected HttpsURLConnectionInfo getHttpsURLConnectionInfo() throws IOException {
            if ("http".equals(outMessage.get("http.scheme"))) {
                return null;
            }
            connect(true);
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        }
        if (list.isEmpty()) {
            return null;
        }
        return list.toArray(new String[0]);
```

### ReturnNull
Return of `null`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                                AsyncHTTPConduit.this.proxyAuthorizationPolicy.getPassword().toCharArray());
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JInterceptor.java`
#### Snippet
```java
    public Object getProperty(Object msgContext, String key) {
        if (msgContext == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JInterceptor.java`
#### Snippet
```java

    public Set<URI> getRoles() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JInterceptor.java`
#### Snippet
```java
    }
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
        if (o != null) {
            if (!PropertyUtils.isTrue(o)) {
                return null;
            }
            specified = true;
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java

        if (!specified && MessageUtils.isRequestor(message)) {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
    public static PasswordEncryptor getPasswordEncryptor(Message message) {
        if (message == null) {
            return null;
        }
        PasswordEncryptor passwordEncryptor =
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
            }
        } catch (Exception ex) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
    public static X509Certificate getReqSigCert(List<WSHandlerResult> results) {
        if (results == null || results.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
    ) throws XMLSecurityException, TokenStoreException {
        if (securityToken == null) {
            return null;
        }
        SecurityToken existingToken = TokenStoreUtils.getTokenStore(message).getToken(securityToken.getId());
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
        }
        if (!create) {
            return null;
        }
        Document doc = DOMUtils.getEmptyDocument();
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
            if (handler == null) {
                policyNotAsserted(info, "No callback handler and no password available", message);
                return null;
            }
        } catch (Exception ex) {
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractTokenInterceptor.java`
#### Snippet
```java
        } catch (Exception ex) {
            policyNotAsserted(info, "No callback handler and no password available", message);
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultWSS4JSecurityContextCreator.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JOutInterceptor.java`
#### Snippet
```java
        getAdditionalInterceptors() {

            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/SamlTokenInterceptor.java`
#### Snippet
```java
        }
        if (handler == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/SamlTokenInterceptor.java`
#### Snippet
```java
        Object o = SecurityUtils.getSecurityPropertyValue(propKey, message);
        if (o == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/BinarySecurityTokenInterceptor.java`
#### Snippet
```java
        TokenStore tokenStore = getTokenStore(message);
        if (tokenStore == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/BinarySecurityTokenInterceptor.java`
#### Snippet
```java
            return tokenStore.getToken(id);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/BinarySecurityTokenInterceptor.java`
#### Snippet
```java
    protected AbstractToken assertTokens(SoapMessage message) {
        // Assert tokens here if required
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JOutInterceptor.java`
#### Snippet
```java

        public Collection<PhaseInterceptor<? extends org.apache.cxf.message.Message>> getAdditionalInterceptors() {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
    }
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
            return properties.get(key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java

    public Set<URI> getRoles() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
            return Loader.getTCL();
        } catch (Exception ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
            policyNotAsserted(token, "No username available", message);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
                                    String nonce,
                                    String created) throws SecurityException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/TokenStoreCallbackHandler.java`
#### Snippet
```java
            //REVISIT
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java
        Object o = message.getContextualProperty(validatorKey);
        if (o == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxActionInInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JInInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractCommonBindingHandler.java`
#### Snippet
```java
            return (Wss10)ai.getAssertion();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
        Element child = DOMUtils.getFirstElement(el);
        if (child == null) {
            return null;
        }
        QName elName = DOMUtils.getElementQName(child);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            return DOMUtils.getContent(child);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            }
        }
        return useEPRWSAAddrAsMEXLocation ? EndpointReferenceUtils.getAddress(ref) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            el = DOMUtils.getNextElement(el);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            return createCrypto(false);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
            unassertPolicy(recToken, e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxSymmetricBindingHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        IncludeTokenType includeToken = usernameToken.getIncludeTokenType();
        if (!isTokenRequired(includeToken)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        IncludeTokenType includeToken = token.getIncludeTokenType();
        if (!isTokenRequired(includeToken)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
            if (handler == null) {
                unassertPolicy(token, "No SAML CallbackHandler available");
                return null;
            }
            properties.setSamlCallbackHandler(handler);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        IncludeTokenType includeToken = token.getIncludeTokenType();
        if (!isTokenRequired(includeToken)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/X509TokenPolicyValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/PolicyUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/PolicyUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecurityVerificationOutInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
                return st;
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/NegotiationUtils.java`
#### Snippet
```java
        AssertionInfo ai = PolicyUtils.getFirstAssertionByLocalname(aim, SPConstants.TRUST_13);
        if (ai == null) {
            return null;
        }
        return (Trust13)ai.getAssertion();
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/NegotiationUtils.java`
#### Snippet
```java
        AssertionInfo ai = PolicyUtils.getFirstAssertionByLocalname(aim, SPConstants.TRUST_10);
        if (ai == null) {
            return null;
        }
        return (Trust10)ai.getAssertion();
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/NegotiationUtils.java`
#### Snippet
```java
            return binding.getAlgorithmSuite();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/KerberosTokenInterceptorProvider.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/KerberosTokenInterceptorProvider.java`
#### Snippet
```java
                LOG.fine(e.getMessage());
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/MemoryTokenStore.java`
#### Snippet
```java
            return cacheEntry.getSecurityToken();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/EHCacheTokenStore.java`
#### Snippet
```java
    public SecurityToken getToken(String identifier) {
        if (cache == null) {
            return null;
        }
        return cache.get(identifier);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/EHCacheTokenStore.java`
#### Snippet
```java
    public Collection<String> getTokenIdentifiers() {
        if (cache == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/SecurityToken.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/SecurityToken.java`
#### Snippet
```java
        Element child = DOMUtils.getFirstElement(str);
        if (child == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/SecurityToken.java`
#### Snippet
```java
            return child.getAttributeNS(null, "URI").substring(1);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
    private static Properties loadStoreProperties(Message m, String storeProp1, String storeProp2) {
        if (m == null) {
            return null;
        }
        Properties props = null;
```

### ReturnNull
Return of `null`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
    private static InputStream getResourceStream(String loc, Bus bus) throws Exception {
        URL url = getResourceURL(loc, bus);
        return url == null ? null : url.openStream();
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        Object o = SecurityUtils.getSecurityPropertyValue(propKey, message);
        if (o == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                    if (StringUtils.isEmpty(user)) {
                        unassertPolicy(token, "No configured " + type + " username detected");
                        return null;
                    }
                } catch (WSSecurityException e1) {
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                    + "attribute value matching endpoint port is defined as well as a "
                    + SecurityConstants.SIGNATURE_PROPERTIES + " element within it.");
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        assertToken(token);
        if (!isTokenRequired(token.getIncludeTokenType())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                } else {
                    unassertPolicy(token, "No password available");
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        }
        unassertPolicy(token, "No username available");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        assertToken(token);
        if (!isTokenRequired(token.getIncludeTokenType())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            } else {
                unassertPolicy(token, "No password available");
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        }
        unassertPolicy(token, "No username available");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        if (encrCert != null) {
            encrKeyBuilder.setUseThisCert(encrCert);
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        assertToken(token);
        if (!isTokenRequired(token.getIncludeTokenType())) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            if (handler == null) {
                unassertPolicy(token, "No SAML CallbackHandler available");
                return null;
            }
            SAMLUtil.doSAMLCallback(handler, samlCallback);
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                    unassertPolicy(info, "No callback handler and no password available");
                }
                return null;
            }
        } catch (Exception ex) {
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                unassertPolicy(info, "No callback handler and no password available");
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXUtils.java`
#### Snippet
```java

    public static List<Element> getPolicies(Server server, String id) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXUtils.java`
#### Snippet
```java

    public static Map<String, String> getPolicyLocations(Server server) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/SecurityContextFactory.java`
#### Snippet
```java
        }
        if (tempUserName == null) {
            return null;
        }
        final String jmsUserName = tempUserName;
```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            return id;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            //REVISIT
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSFactory.java`
#### Snippet
```java
        String conduitIdSt = jmsConfig.isUseConduitIdSelector() && conduitId != null ? conduitId : "";
        String correlationIdPrefix = staticSelectorPrefix + conduitIdSt;
        return correlationIdPrefix.isEmpty() ? null : "JMSCorrelationID LIKE '" + correlationIdPrefix + "%'";
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            unassertPolicy(recToken, e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java

        dkSign.clean();
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java

        sig.clean();
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            unassertPolicy(recToken, e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/server/ReactorInvoker.java`
#### Snippet
```java
            return asyncResponse;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSDestination.java`
#### Snippet
```java
        if (inMessage.getExchange().isOneWay()
            && !robust) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
            return ((Topic)replyTo).getTopicName();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSURIParser.java`
#### Snippet
```java
            return found;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfigFactory.java`
#### Snippet
```java
    private static TransactionManager getTransactionManagerFromJndi(String transactionManagerJndiName) {
        if (transactionManagerJndiName == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpointWSDLUtil.java`
#### Snippet
```java

        if (o == null) {
            return null;
        }
        if (cls.isInstance(o)) {
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpointWSDLUtil.java`
#### Snippet
```java
            return cls.cast(o);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/UnsafeUriCharactersEncoder.java`
#### Snippet
```java
        } catch (UnsupportedEncodingException e) {
            LOG.log(Level.SEVERE, "Can't encoding the uri: ", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/AbstractMessageListenerContainer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java

        if (ct == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
            return ct.substring(start, end);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
    private ConnectionFactory getConnectionFactoryFromJndi() {
        if (getJndiEnvironment() == null || getConnectionFactoryName() == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
            // in this case the correlation id will be set to
            // the message id later
            return null;
        } else {
            String prefix = (jmsConfig.isUseConduitIdSelector()) ? jmsConfig.getConduitSelectorPrefix()
```

### ReturnNull
Return of `null`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/continuations/JMSContinuationProvider.java`
#### Snippet
```java
        }
        if (m == null || m.getExchange() == null || m.getExchange().isOneWay()) {
            return null;
        }
        JMSContinuation cw = m.get(JMSContinuation.class);
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxy.java`
#### Snippet
```java
            return rawRet[0];
        }
        return null;
    }
    public Map<String, Object> getRequestContext() {
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientFactoryBean.java`
#### Snippet
```java
            return ci.getTransportIds().get(0);
        }
        return null;
    }
    @Override
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public Client create() {
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractServiceFactory.java`
#### Snippet
```java

    public File getOutputFile() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractServiceFactory.java`
#### Snippet
```java
            return ci.getTransportIds().get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractServiceFactory.java`
#### Snippet
```java
            return (WSDLEndpointFactory)destinationFactory;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractWSDLBasedEndpointFactory.java`
#### Snippet
```java
                return Boolean.FALSE;
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractWSDLBasedEndpointFactory.java`
#### Snippet
```java
                return ((SoapBindingConfiguration)getBindingConfig()).getStyle();
            }
            return null;
        }
        public Boolean isWrapped() {
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ServerFactoryBean.java`
#### Snippet
```java
     */
    public Server getServer() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ServerFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ServerFactoryBean.java`
#### Snippet
```java
            return (WSDLEndpointFactory)destinationFactory;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
        QName name = getEndpointName();
        if (name == null) {
            return null;
        }
        return name.toString() + ".client.proxyFactory";
```

### ReturnNull
Return of `null`
in `rt/rs/http-sci/src/main/java/org/apache/cxf/jaxrs/servlet/sci/JaxrsServletContainerInitializer.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveInterceptor.java`
#### Snippet
```java
        String safeGet(String key) {
            if (!message.containsKey(key)) {
                return null;
            }
            Object value = message.get(key);
```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveInterceptor.java`
#### Snippet
```java
            }
            Object value = message.get(key);
            return (value instanceof String) ? value.toString() : null;
        }
      
```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveInterceptor.java`
#### Snippet
```java
    @Override
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/internal/HttpServerAdapterFactory.java`
#### Snippet
```java
                }
    
                return null;
            }
    
```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/internal/HttpClientAdapterFactory.java`
#### Snippet
```java
                }
    
                return null;
            }
    
```

### ReturnNull
Return of `null`
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/util/WSNHelper.java`
#### Snippet
```java
            return DOMUtils.getContent(e).trim();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/client/Publisher.java`
#### Snippet
```java
    public W3CEndpointReference getEpr() {
        if (this.endpoint == null) {
            return null;
        }
        return this.endpoint.getEndpointReference(W3CEndpointReference.class);
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/server/ReactiveIOInvoker.java`
#### Snippet
```java
            return handleMaybe(inMessage, (Maybe<?>)result);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/InboundSseEventImpl.java`
#### Snippet
```java
    private <T> T read(Class<T> messageType, Type type, MediaType mediaType) {
        if (data == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/InboundSseEventProcessor.java`
#### Snippet
```java
            }

            return null;
        };
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServiceImpl.java`
#### Snippet
```java
        // (transport, binding, address etc) from WSDL's physical part.
        // not applicable to JAX-RS services.
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
            return asyncResponse;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
            return new MessageContentsList(r);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/ProtocolHeadersImpl.java`
#### Snippet
```java
    public String getRequestHeaderValue(String name) {
        List<String> values = getRequestHeader(name);
        return !values.isEmpty() ? values.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/MessageContextImpl.java`
#### Snippet
```java
            return resolverClazz.cast(getContext(resolveClazz, ContextResolver.class));
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/MessageContextImpl.java`
#### Snippet
```java
            return getContext(HttpServletRequest.class);
        } catch (Throwable t) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
            //do nothing
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/MultipartBody.java`
#### Snippet
```java

    public Attachment getRootAttachment() {
        return !atts.isEmpty() ? atts.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/MultipartBody.java`
#### Snippet
```java
            return att.getObject(cls);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/MultipartBody.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ReaderInterceptorContextImpl.java`
#### Snippet
```java
    public Object proceed() throws IOException {
        if (readers == null || readers.isEmpty()) {
            return null;
        }
        ReaderInterceptor next = readers.remove(0);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/PathSegmentImpl.java`
#### Snippet
```java
        int index = path.indexOf(';');
        if (index == -1) {
            return null;
        }
        return path.substring(index);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java

        if (ifNotModifiedSince == null || ifNotModifiedSince.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java

        if (ifModifiedSince == null || ifModifiedSince.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
        if (dateSince.before(lastModified)) {
            // request should continue
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java

        if (ifMatch == null || ifMatch.isEmpty()) {
            return date == null ? null : evaluateIfNotModifiedSince(date);
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
            for (String value : ifMatch) {
                if ("*".equals(value)) {
                    return null;
                }
                EntityTag requestTag = EntityTag.valueOf(value);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
                // must be a strong comparison
                if (!requestTag.isWeak() && !eTag.isWeak() && requestTag.equals(eTag)) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java

        if (ifNonMatch == null || ifNonMatch.isEmpty()) {
            return lastModified == null ? null : evaluateIfModifiedSince(lastModified);
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    public Link.Builder getLinkBuilder(String relation) {
        Link link = getLink(relation);
        return link == null ? null : Link.fromLink(link);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
                } else {
                    autoCloseWithHint(cls, autoCloseHint, false);
                    return null;
                }
            } catch (Exception ex) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    private List<String> toListOfStrings(List<Object> values) {
        if (values == null) {
            return null;
        }
        List<String> stringValues = new ArrayList<>(values.size());
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
            return separator;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
        List<String> values = headers.get(HttpHeaders.DATE);
        if (values == null || values.isEmpty() || StringUtils.isEmpty(values.get(0))) {
            return null;
        }
        return HttpUtils.getHttpDate(values.get(0));
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
    public Locale getLanguage() {
        List<String> values = getListValues(HttpHeaders.CONTENT_LANGUAGE);
        return values.isEmpty() ? null : HttpUtils.getLocale(values.get(0).trim());
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
    public MediaType getMediaType() {
        List<String> values = getListValues(HttpHeaders.CONTENT_TYPE);
        return values.isEmpty() ? null : JAXRSUtils.toMediaType(values.get(0));
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
        }
        K realKey = getMatchingKey(key);
        return realKey == null ? null : m.get(realKey);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
    public V getFirst(K key) {
        List<V> data = this.get(key);
        return data == null || data.isEmpty() ? null : data.get(0);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpServletRequestFilter.java`
#### Snippet
```java
    public String getParameter(String name) {
        String[] values = this.getParameterValues(name);
        return values == null || values.length == 0 ? null : values[0];
    }
    @Override
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResourceInfoImpl.java`
#### Snippet
```java
    @Override
    public Method getResourceMethod() {
        return ori == null ? null : ori.getMethodToInvoke();
    }
    @Override
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResourceInfoImpl.java`
#### Snippet
```java
    @Override
    public Class<?> getResourceClass() {
        return ori == null ? null : ori.getClassResourceInfo().getServiceClass();
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/SecurityContextImpl.java`
#### Snippet
```java
    public Principal getUserPrincipal() {
        org.apache.cxf.security.SecurityContext sc = getInternalSecurityContext();
        return sc == null ? null : sc.getUserPrincipal();
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/SecurityContextImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/PropertyHolderFactory.java`
#### Snippet
```java
        }
        public Object getProperty(String name) {
            return props == null ? null : props.get(name);
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RetryAfterHeaderProvider.java`
#### Snippet
```java
    public String toString(Duration value) {
        if (value == null) {
            return null;
        } else {
            return Long.toString(value.getSeconds());
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalProviders.java`
#### Snippet
```java
    private Providers getProvidersImpl() {
        Message m = JAXRSUtils.getCurrentMessage();
        return m != null ? new ProvidersImpl(JAXRSUtils.getContextMessage(m)) : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalProviders.java`
#### Snippet
```java
    public <T extends Throwable> ExceptionMapper<T> getExceptionMapper(Class<T> type) {
        Providers p = getCurrentProviders();
        return p != null ? p.getExceptionMapper(type) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalProviders.java`
#### Snippet
```java
                                                         MediaType mediaType) {
        Providers p = getCurrentProviders();
        return p != null ? p.getMessageBodyWriter(type, genericType, annotations, mediaType) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalProviders.java`
#### Snippet
```java
                                                         MediaType mediaType) {
        Providers p = getCurrentProviders();
        return p != null ? p.getMessageBodyReader(type, genericType, annotations, mediaType) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalProviders.java`
#### Snippet
```java
    public <T> ContextResolver<T> getContextResolver(Class<T> contextType, MediaType mediaType) {
        Providers p = getCurrentProviders();
        return p != null ? p.getContextResolver(contextType, mediaType) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RuntimeDelegateImpl.java`
#### Snippet
```java
            return type.cast(new VariantListBuilderImpl());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
        NodeList nodes = (NodeList)evaluate(expression, namespaces, XPathConstants.NODESET);
        if (nodes == null || nodes.getLength() == 0) {
            return null;
        }
        T[] values = (T[])Array.newInstance(cls, nodes.getLength());
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
    public <T> T getValue(String expression, Map<String, String> namespaces, Class<T> cls) {
        Object result = evaluate(expression, namespaces, XPathConstants.STRING);
        return result == null ? null : (T)InjectionUtils.convertStringToPrimitive(result.toString(), cls);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
    public URI getLink(String expression, Map<String, String> namespaces) {
        String value = getValue(expression, namespaces);
        return value == null ? null : URI.create(value);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
        String[] values = getValues(expression, namespaces);
        if (values == null) {
            return null;
        }
        URI[] uris = new URI[values.length];
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
        Object obj = evaluate(expression, namespaces, XPathConstants.NODE);
        if (obj == null) {
            return null;
        }
        if (obj instanceof Node) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
            String prefix = namespaces.get(namespace);
            if (prefix == null) {
                return null;
            }
            return Collections.singletonList(prefix).iterator();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public Providers getProviders() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getProviders() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public HttpServletRequest getHttpServletRequest() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getHttpServletRequest() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public Object get(Object key) {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.get(key) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public Object getContextualProperty(Object key) {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getContextualProperty(key) : null;
    }
    private MessageContext getCurrentMessageContext() {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public UriInfo getUriInfo() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getUriInfo() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public SecurityContext getSecurityContext() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getSecurityContext() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public ServletConfig getServletConfig() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getServletConfig() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public HttpHeaders getHttpHeaders() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getHttpHeaders() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public <T> T getContent(Class<T> format) {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getContent(format) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public HttpServletResponse getHttpServletResponse() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getHttpServletResponse() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public <T> T getContext(Class<T> contextClass) {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getContext(contextClass) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public ServletContext getServletContext() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getServletContext() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    private MessageContext getMessageContextImpl() {
        Message m = JAXRSUtils.getCurrentMessage();
        return m != null ? new MessageContextImpl(m) : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public Request getRequest() {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getRequest() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalMessageContext.java`
#### Snippet
```java
    public <T, E> T getResolver(Class<T> resolverClass, Class<E> resolveClazz) {
        MessageContext mc = getCurrentMessageContext();
        return mc != null ? mc.getResolver(resolverClass, resolveClazz) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalInvocationHandler.java`
#### Snippet
```java
            if (target == null) {
                if (m.getName().endsWith("toString")) {
                    return null;
                }
                Class<?> contextCls = m.getDeclaringClass();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalServletContext.java`
#### Snippet
```java

    private RequestDispatcher getDispatcher(RequestDispatcher rd) {
        return rd == null ? null : new RequestDispatcherImpl(rd);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalContextResolver.java`
#### Snippet
```java

    public T getContext(Class<?> type) {
        return get() != null ? get().getContext(type) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            return base + "?xsd=" + key.replace(" ", "%20");
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/Attachment.java`
#### Snippet
```java
        List<String> header = headers.get(name);
        if (header == null || header.isEmpty()) {
            return null;
        }
        StringBuilder sb = new StringBuilder();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/Attachment.java`
#### Snippet
```java
        String header = getHeader("Content-Disposition");

        return header == null ? null : new ContentDisposition(header);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/Attachment.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            }
        }
        return b.length() > 0 ? b.toString() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
        ClassDocs classDoc = getClassDocInternal(method.getDeclaringClass());
        if (classDoc == null) {
            return null;
        }
        MethodDocs mDocs = classDoc.getMethodDocs(method);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java

            if (operMarkerIndex == -1) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            MethodDocs doc = getOperationDocInternal(ori);
            if (doc == null) {
                return null;
            }
            return doc.getResponseInfo();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            ClassDocs doc = getClassDocInternal(cri.getServiceClass());
            if (doc == null) {
                return null;
            }
            return doc.getClassInfo();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            MethodDocs doc = getOperationDocInternal(ori);
            if (doc == null) {
                return null;
            }
            List<String> params = doc.getParamInfo();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
                return params.get(paramIndex);
            }
            return null;
        } catch (Exception ex) {
            // ignore
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            MethodDocs doc = getOperationDocInternal(ori);
            if (doc == null) {
                return null;
            }
            return doc.getMethodInfo();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ExceptionUtils.java`
#### Snippet
```java
    public static <T extends Throwable> Response convertFaultToResponse(T ex, Message currentMessage) {
        if (ex == null || currentMessage == null) {
            return null;
        }
        Message inMessage = currentMessage.getExchange().getInMessage();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
            return getConstructorProxyMap().get(serviceClass);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXBUtils.java`
#### Snippet
```java
                                                Map<String, Object> contextProperties) {
        if (classes == null || classes.isEmpty()) {
            return null;
        }
        org.apache.cxf.common.jaxb.JAXBUtils.scanPackages(classes, extraClass, null);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXBUtils.java`
#### Snippet
```java
            LOG.log(Level.SEVERE, "No JAXB context can be created", ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ParameterizedCollectionType.java`
#### Snippet
```java

    public Type getOwnerType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java

        public String getPattern() {
            return pattern != null ? pattern.pattern() : null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
            Variable newVariable = new Variable();
            if (uriChunk == null || "".equals(uriChunk)) {
                return null;
            }
            if (CurlyBraceTokenizer.insideBraces(uriChunk)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
    public static <A extends Annotation> A getClassAnnotation(Class<?> c, Class<A> aClass) {
        if (c == null) {
            return null;
        }
        A p = c.getAnnotation(aClass);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java

    public static <A extends Annotation> A getMethodAnnotation(Method m, Class<A> aClass) {
        return m == null ? null : m.getAnnotation(aClass);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
    private static Method getMethodOrNull(final Class<?> type, final String name) {
        if (type == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
            return type.getMethod(name);
        } catch (final NoSuchMethodException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
    public static <T> T getAnnotation(Annotation[] anns, Class<T> type) {
        if (anns == null) {
            return null;
        }
        for (Annotation a : anns) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
                    name, AnnotationUtils.class, Annotation.class);
        } catch (final ClassNotFoundException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java

        DefaultValue dv = AnnotationUtils.getAnnotation(anns, DefaultValue.class);
        return dv != null ? dv.value() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    public static Locale getLocale(String value) {
        if (value == null) {
            return null;
        }
        final String language;
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    public static Date getHttpDate(String value) {
        if (value == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            return Headers.getHttpDateFormat().parse(value);
        } catch (ParseException ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        try {
            RuntimeDelegate rd = RuntimeDelegate.getInstance();
            return rd instanceof RuntimeDelegateImpl ? null : rd;
        } catch (Throwable t) {
            return null;
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            return rd instanceof RuntimeDelegateImpl ? null : rd;
        } catch (Throwable t) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    public static String getHeaderString(List<String> values) {
        if (values == null) {
            return null;
        }
        StringBuilder sb = new StringBuilder();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static HeaderDelegate<Object> getHeaderDelegate(RuntimeDelegate rd, Object o) {
        return rd == null ? null : (HeaderDelegate<Object>)rd.createHeaderDelegate(o.getClass());
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/JAXRSServerFactoryBeanDefinitionParser.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
        List<Attachment> all = getAttachments(mc);
        List<Attachment> matching = getMatchingAttachments(id, mediaType, all);
        return matching.isEmpty() ? null : matching.get(0);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
                          new MultipartReadException(id.value(), id.type(), errorMsg.toString()), null);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
        }

        return !infos.isEmpty() ? infos.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
    protected static Set<String> parseSetProperty(String componentScanProp) {
        return !StringUtils.isEmpty(componentScanProp) 
            ? ClasspathScanner.parsePackages(componentScanProp) : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXRSDataBinding.java`
#### Snippet
```java
                return read(cls);
            } catch (Exception ex) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
    protected Application createApplicationInstance(String appClassName, ServletConfig servletConfig)
        throws ServletException {
        return null;
    }
    protected ApplicationInfo createApplicationInfo(String appClassName, ServletConfig servletConfig)
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/schemas/SchemaHandler.java`
#### Snippet
```java
                                    // ignore
                                }
                                return null;
                            }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/schemas/SchemaHandler.java`
#### Snippet
```java
            }
            if (sources.isEmpty()) {
                return null;
            }
            final OASISCatalogManager catalogResolver = OASISCatalogManager.getCatalogManager(bus);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/DataBindingProvider.java`
#### Snippet
```java
            DataReader<XMLStreamReader> dataReader = binding.createReader(XMLStreamReader.class);
            Object o = dataReader.read(null, reader, clazz);
            return o == null ? null : clazz.cast(o);
        } catch (Exception ex) {
            throw ExceptionUtils.toBadRequestException(ex, null);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBAttachmentMarshaller.java`
#### Snippet
```java
            return "cid:" + att.getId();
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBAttachmentMarshaller.java`
#### Snippet
```java
            return "cid:" + att.getId();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        }

        return addedToNotDone ? null : element;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                return new InputSource(systemId);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
    public Node cloneNode(Document document, Node node, boolean deep) throws DOMException {
        if (document == null || node == null) {
            return null;
        }
        int type = node.getNodeType();
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
            break;
        default:
            return null;
        }
        if (deep && type == Node.ELEMENT_NODE) {
```

### ReturnNull
Return of `null`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                return s;
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return first;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return ClassHelper.getRealClassFromClass(targetClass);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return getRawType(((GenericArrayType)genericType).getGenericComponentType());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                        Exchange exchange) {
        if (targetObject == null) {
            return null;
        }
        final Type type;
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    paramValues = Collections.emptyList();
                } else {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                        Message message) {
        if (value == null) {
            return null;
        }
        if (pType == ParameterType.PATH) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (Number.class.isAssignableFrom(pClass) && "".equals(value)) {
            //pass empty string to boxed number type will result in 404
            return null;
        }
        if (Boolean.class == pClass) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        } catch (ClassCastException ex) {
            reportServerError("WRONG_PARAMETER_TYPE", pClass.getName());
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java

        if (genericType == null) {
            return null;
        }
        if (genericType == Object.class) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return result;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (genericType == null
            || !ParameterizedType.class.isAssignableFrom(genericType.getClass())) {
            return null;
        }
        ParameterizedType paramType = (ParameterizedType)genericType;
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
    public static Field getDeclaredField(Class<?> cls, String fieldName) {
        if (cls == null || cls == Object.class) {
            return null;
        }
        Field f = ReflectionUtil.getDeclaredField(cls, fieldName);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            reportServerError("METHOD_INJECTION_FAILURE", method.getName(), logError);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                      f.getType().getName());
                }
                return null;
            }
        });
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                      f.getType().getName());
                }
                return null;
            }
        });
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyWriter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractResponseViewProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractResponseViewProvider.java`
#### Snippet
```java
            return resourceName;
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
        Object provider = createProvider(WADL_PROVIDER_NAME, bus);
        if (provider == null) {
            return null;
        }
        return new ProviderInfo<ContainerRequestFilter>((ContainerRequestFilter)provider, bus, true);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java
            String[] values = params.get(name);
            if (values == null || values.length == 0) {
                return null;
            }
            return values[0];
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
        try (InputStream is = ResourceUtils.getResourceStream(loc, bus)) {
            if (is == null) {
                return null;
            }
            return getUserResources(is);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
    public static Method findPostConstructMethod(Class<?> c, String name) {
        if (Object.class == c || null == c) {
            return null;
        }
        for (Method m : getDeclaredMethods(c)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
    public static InputStream getResourceStream(String loc, Bus bus) throws IOException {
        URL url = getResourceURL(loc, bus);
        return url == null ? null : url.openStream();
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java

        evaluateResourceClass(cri, enableStatic);
        return checkMethodDispatcher(cri) ? cri : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
    public static Method findPreDestroyMethod(Class<?> c, String name) {
        if (Object.class == c || null == c) {
            return null;
        }
        for (Method m : getDeclaredMethods(c)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
    private static ClassResourceInfo getAncestorWithSameServiceClass(ClassResourceInfo parent, Class<?> subClass) {
        if (parent == null) {
            return null;
        }
        if (parent.getServiceClass() == subClass) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java

        cri.setMethodDispatcher(md);
        return checkMethodDispatcher(cri) ? cri : null;

    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java

        });
        return cs.isEmpty() ? null : cs.get(0);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyReader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/FormEncodingProvider.java`
#### Snippet
```java
        throws IOException {
        if (is == null) {
            return null;
        }
        try {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
            return (ParamConverter<T>) new ZonedDateTimeConverter();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/SourceProvider.java`
#### Snippet
```java
            return mc.getContent(XMLStreamReader.class);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/springmvc/SpringViewResolverProvider.java`
#### Snippet
```java
            return resolveView(path);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/validation/ValidationUtils.java`
#### Snippet
```java
        final OperationResourceInfo ori = message.getExchange().get(OperationResourceInfo.class);
        if (ori == null) {
            return null;
        }
        if (!ori.getClassResourceInfo().isRoot()) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/validation/ValidationUtils.java`
#### Snippet
```java
            String error = "Service object is not a singleton, use a custom invoker to validate";
            LOG.warning(error);
            return null;
        }
        return resourceProvider.getInstance(message);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
    protected Class<?> getJaxbElementClass(Class<?> cls) {
        if (cls == Object.class) {
            return null;
        }
        if (jaxbElementClassNames.contains(cls.getName())) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
            return staxCls.cast(m.getContent(staxCls));
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
    protected JAXBContext getPackageContext(final Class<?> type, Type genericType) {
        if (type == null || type == JAXBElement.class) {
            return null;
        }
        final String packageName = PackageUtils.getPackageName(type);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java

        if (cls == JAXBElement.class) {
            return object != null ? ((JAXBElement<?>)object).getName() : null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        // deal with the typical default case first
        if (schema == null && schemaHandlers == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        }
        SchemaHandler handler = schemaHandlers.get(cls.getName());
        return handler != null ? handler.getSchema() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
            return CastUtils.cast((Collection<?>)mc.get(propertyName));
        }
        return null;
    }
    //CHECKSTYLE:OFF
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
        if (isPayloadEmpty(headers)) {
            if (AnnotationUtils.getAnnotation(anns, Nullable.class) != null) {
                return null;
            }
            reportEmptyContentLength();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
        }
        // unreachable
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return CastUtils.cast((List<?>)value);
        }
        return null;
    }
    public List<Map<String, Object>> getListMapProperty(String name) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return number instanceof Long ? (Long) number : Long.valueOf(number.longValue());
        }
        return null;
    }
    private Number getNumberProperty(String name, Function<String, Number> converter) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return new JsonMapObject(value);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return number instanceof Integer ? (Integer) number : Integer.valueOf(number.intValue());
        }
        return null;
    }
    public Long getLongProperty(String name) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return value.toString();
        }
        return null;
    }
    public List<String> getListStringProperty(String name) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return value instanceof Number ? (Number) value : converter.apply(value.toString());
        }
        return null;
    }
    public Boolean getBooleanProperty(String name) {
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return CastUtils.cast((Map<?, ?>)value);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
    
    public Map<String, Object> getUpdateCount() {
        return updateCount == null ? null : Collections.unmodifiableMap(updateCount);
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
            return value instanceof Boolean ? (Boolean)value : Boolean.valueOf(value.toString());
        }
        return null;
    }
    public String getStringProperty(String name) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtTokenSecurityContext.java`
#### Snippet
```java
    @Override
    public Subject getSubject() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            return interceptors;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        Class<?> contextCls = InjectionUtils.getActualType(contextType);
        if (contextCls == null) {
            return null;
        }
        for (ProviderInfo<ContextProvider<?>> cr : contextProviders) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            LOG.fine(message);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            return interceptors;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
            pi.clearThreadLocalProxies();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        Class<?> contextCls = InjectionUtils.getActualType(contextType);
        if (contextCls == null) {
            return null;
        }
        List<ContextResolver<T>> candidates = new LinkedList<>();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        }
        if (candidates.isEmpty()) {
            return null;
        } else if (candidates.size() == 1) {
            return candidates.get(0);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        boolean allowed = allowProp == null || PropertyUtils.isTrue(allowProp);
        if (!allowed) {
            return null;
        }
        boolean checkAll = PropertyUtils.isTrue(theBus.getProperty(PROVIDER_CACHE_CHECK_ALL));
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                }
            }
            return null;
        }
    };
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsContainerRequestFilter.java`
#### Snippet
```java
            };
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            LOG.warning("No template can be created : " + ex.getMessage());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
        if (templates == null) {
            if (supportJaxbOnly) {
                return null;
            }
            LOG.severe("No template is available");
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
                    }
                }
                return null;
            }
            return ann;
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            return ann;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
    protected Templates createTemplates(URL urlStream) {
        if (urlStream == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            LOG.warning("No template can be created : " + ex.getMessage());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJwtAuthenticationFilter.java`
#### Snippet
```java
            return new JwtTokenSecurityContext(jwt, roleClaim);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxwsResponseCallback.java`
#### Snippet
```java
            return callback.getResponseContext();
        } catch (Exception ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSProviderMethodDispatcher.java`
#### Snippet
```java

    public BindingOperationInfo getBindingOperation(Method m, Endpoint endpoint) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServerAsyncResponse.java`
#### Snippet
```java
     */
    public Map<String, Object> getContext() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
            return findSoapFaultException(ex.getCause());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
    private Message createResponseMessage(Exchange exchange) {
        if (exchange == null) {
            return null;
        }
        Message m = exchange.getOutMessage();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
            return paramAnno[index];
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
            } catch (Throwable t2) {
                //still didn't work, we'll just throw what we have
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/WrapperClassLoader.java`
#### Snippet
```java
        }
        //throw new ClassNotFoundException(origClassName);
        return null;
    }
    private String getPackageName(Method method) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
            value = Boolean.valueOf(valueStr);
        } else if (NULL_VALUE.equals(valueStr)) {
            return null;
        } else {
            try {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WebServiceContextResourceResolver.java`
#### Snippet
```java

    public final InputStream getAsStream(final String string) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WebServiceContextResourceResolver.java`
#### Snippet
```java
            return clz.cast(new WebServiceContextImpl());
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/binding/soap/SOAPBindingImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/binding/soap/SOAPBindingImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WebServiceContextImpl.java`
#### Snippet
```java
        SecurityContext ctx = (SecurityContext)getMessageContext().get(SecurityContext.class.getName());
        if (ctx == null) {
            return null;
        }
        return ctx.getUserPrincipal();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            StaxUtils.close(resultWriter);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            // ignore
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            Object[] ret = client.invokeWrapped(opName, holder.value);
            if (isOneWay || ret == null || ret.length == 0) {
                return null;
            }
            return (T)ret[0];
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            return (T)ret[0];
        } catch (IllegalEmptyResponseException ie) {
            return null;
        } catch (Exception ex) {
            throw mapException(ex);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    private PortInfoImpl getPortInfo(QName portName) {
        if (portName == null) {
            return null;
        }
        return portInfos.get(portName);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainBuilder.java`
#### Snippet
```java
    public List<Handler> buildHandlerChainFromConfiguration(List<PortComponentHandlerType> hc) {
        if (null == hc || hc.isEmpty()) {
            return null;
        }
        List<Handler> handlers = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainBuilder.java`
#### Snippet
```java

    private String trimString(String str) {
        return str != null ? str.trim() : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainBuilder.java`
#### Snippet
```java
    public List<Handler> buildHandlerChainFromConfiguration(PortComponentHandlerType hc) {
        if (null == hc) {
            return null;
        }
        return sortHandlers(buildHandlerChain(hc, getHandlerClassLoader()));
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/AnnotationHandlerChainBuilder.java`
#### Snippet
```java
    private HandlerChainAnnotation findHandlerChainAnnotation(Class<?> clz, boolean searchSEI) {
        if (clz == null) {
            return null;
        }
        if (LOG.isLoggable(Level.FINE)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
        Object ret = message.get(mappedkey);
        if (MessageContext.HTTP_REQUEST_METHOD.equals(key) && isRequestor()) {
            return null;
        }
        if (ret == null) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
    private Message createResponseMessage() {
        if (exchange == null || exchange.isOneWay()) {
            return null;
        }
        if (isResponse()) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
    private Object createAttachments(Map<String, Object> mc, String propertyName) {
        if (mc == null) {
            return null;
        }
        Collection<Attachment> attachments = CastUtils.cast((Collection<?>)mc.get(Message.ATTACHMENTS));
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
            return exchange.getBindingOperationInfo();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java

    public final Message getWrappedMessage() {
        return message instanceof Message ? (Message)message : null;
    }
    public final Map<String, Object> getWrappedMap() {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/AbstractJAXWSHandlerInterceptor.java`
#### Snippet
```java

    protected QName getOpQName(Exchange ex, Object data) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
            if (mp != null) {
                if (mp.isEmpty()) {
                    return null;
                }
                if (!isRequestor() && isOutbound() && MessageContext.HTTP_RESPONSE_HEADERS.equals(key)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
                }
                if (!isRequestor() && isOutbound() && MessageContext.HTTP_RESPONSE_HEADERS.equals(key)) {
                    return null;
                }
                if (isRequestor() && !isOutbound() && MessageContext.HTTP_REQUEST_HEADERS.equals(key)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
                }
                if (isRequestor() && !isOutbound() && MessageContext.HTTP_REQUEST_HEADERS.equals(key)) {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/InitParamResourceResolver.java`
#### Snippet
```java
    public InputStream getAsStream(String name) {
        // returning these as a stream does not make much sense
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageContextImpl.java`
#### Snippet
```java
            && o != null) {
            if (((Map<?, ?>)o).isEmpty()) {
                return null;
            }
            if (!isResponse() && isOutbound() && MessageContext.HTTP_RESPONSE_HEADERS.equals(key)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageContextImpl.java`
#### Snippet
```java
            }
            if (!isResponse() && isOutbound() && MessageContext.HTTP_RESPONSE_HEADERS.equals(key)) {
                return null;
            }
            if (isRequestor() && !isOutbound() && MessageContext.HTTP_REQUEST_HEADERS.equals(key)) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageContextImpl.java`
#### Snippet
```java
            }
            if (isRequestor() && !isOutbound() && MessageContext.HTTP_REQUEST_HEADERS.equals(key)) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
                if (isRequestor(message) && msg.getSOAPPart().getEnvelope().getBody() != null
                        && msg.getSOAPPart().getEnvelope().getBody().hasFault()) {
                    return null;
                }
            } catch (SOAPException e) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
            SOAPMessage msg = sm.getMessage();
            if (msg == null) {
                return null;
            }
            SOAPBody body = SAAJUtils.getBody(msg);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
            SOAPBody body = SAAJUtils.getBody(msg);
            if (body == null) {
                return null;
            }
            org.w3c.dom.Node nd = body.getFirstChild();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
            //ignore, nothing we can do
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalHandlerInInterceptor.java`
#### Snippet
```java
            return new QName(ns, ln);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/WebServiceProviderConfiguration.java`
#### Snippet
```java
            return wsdlLocation;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
            c = c.getSuperclass();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
    private static WebServiceProvider getWebServiceProviderAnnotation(Class<?> cls) {
        if (cls == null) {
            return null;
        }
        WebServiceProvider ann = cls.getAnnotation(WebServiceProvider.class);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
            return wsProviderAnnotation.wsdlLocation();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
        }
        public String getName() {
            return null;
        }
        public OutputStream getOutputStream() throws IOException {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeInInterceptor.java`
#### Snippet
```java
        }
        public OutputStream getOutputStream() throws IOException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java

                    public OutputStream getOutputStream() throws IOException {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    public QName getInPartName(OperationInfo op, Method method, int paramNumber) {
        if (paramNumber < 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    public Class<?> getResponseWrapper(Method selected) {
        if (this.responseMethodClassNotFoundCache.contains(selected)) {
            return null;
        }
        Class<?> cachedClass = responseMethodClassCache.get(selected);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }
        responseMethodClassNotFoundCache.add(selected);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return clsName;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return "result";
        }
        return null;
    }
    public String getFaultMessageName(OperationInfo op, Class<?> exClass, Class<?> beanClass) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return getWithReflection(WebFault.class, f, "messageName");
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return new QName(nm, lp);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            //ignore = possibly JAX-WS 2.1
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    public Class<?> getRequestWrapper(Method selected) {
        if (this.requestMethodClassNotFoundCache.contains(selected)) {
            return null;
        }
        Class<?> cachedClass = requestMethodClassCache.get(selected);
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }
        requestMethodClassNotFoundCache.add(selected);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return new QName(nm, lp);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return new QName(ns, name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return !(ann.parameterStyle().equals(ParameterStyle.BARE) || ann.style().equals(Style.RPC));
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        }
        if (parameter >= annotations.length) {
            return null;
        }
        for (int i = 0; i < annotations[parameter].length; i++) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }
    @Override
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return wsdlLocation;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return clsName;
        }
        return null;
    }
    public String getRequestWrapperClassName(Method selected) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return getWithReflection(RequestWrapper.class, rw, "partName");
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
                throw new ServiceConstructionException(e);
            } catch (NoSuchMethodException e) {
                return isWebMethod(method) ? method : null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
    public QName getInParameterName(OperationInfo op, Method method, int paramNumber) {
        if (paramNumber < 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletResponseAdapter.java`
#### Snippet
```java
    public String getHeader(String name) {
        List<String> headers = exchange.getResponseHeaders().get(name);
        return (headers != null && !headers.isEmpty()) ? headers.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java
    public Enumeration<String> getHeaders(String name) {
        List<String> list = exchange.getRequestHeaders().get(name);
        return list != null ? Collections.enumeration(list) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getRequestedSessionId() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getServerName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getServletPath() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public Cookie[] getCookies() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public ServletContext getServletContext() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getAuthType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public Locale getLocale() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getPathTranslated() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public HttpSession getSession(boolean create) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public String getRemoteUser() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/HttpServletRequestAdapter.java`
#### Snippet
```java

    public HttpSession getSession() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            //ignore - probably JAX-WS 2.1
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

    private List<ExtensibilityElement> getExtensors(Extensible extensibleInfo) {
        return (null != extensibleInfo) ? extensibleInfo.getExtensors(ExtensibilityElement.class) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private MTOMFeature getMTOMFeature() {
        if (wsFeatures == null) {
            return null;
        }
        for (WebServiceFeature feature : wsFeatures) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private AddressingFeature getAddressingFeature() {
        if (wsFeatures == null) {
            return null;
        }
        for (WebServiceFeature feature : wsFeatures) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private WSAddressingFeature getWSAddressingFeature() {
        if (features == null) {
            return null;
        }
        for (Feature f : features) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
    protected Conduit getInbuiltBackChannel(final Message inMessage) {
        if (inMessage.getExchange().isOneWay()) {
            return null;
        }
        return new AbstractBackChannelConduit() {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
        Class<?> cls = getServiceClass();
        if (cls.isInterface()) {
            return null;
        }
        return new JAXWSMethodInvoker(new SingletonFactory(getServiceClass()));
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }
    private void buildWSAActions(OperationInfo operation, Method method) {
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
                || java.rmi.RemoteException.class.isAssignableFrom(exClass)
                || "jakarta.xml.ws".equals(PackageUtils.getPackageName(exClass))) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
    protected Class<?> getProviderParameterType(Class<?> cls) {
        if (cls == null) {
            return null;
        }
        Type[] genericInterfaces = cls.getGenericInterfaces();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RM10Constants.java`
#### Snippet
```java

    public QName getSequenceClosedFaultCode() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RM10Constants.java`
#### Snippet
```java

    public QName getWSRMRequiredFaultCode() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
            //  TODO
            LOG.severe("No such sequence.");
            return null;
        }
        closedSeq.scheduleImmediateAcknowledgement();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
            return resList.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
        if (null == oi) {
            LOG.fine("No operation info.");
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    private Continuation getContinuation(Message message) {
        if (message == null) {
            return null;
        }
        return message.get(Continuation.class);
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/InternalContextUtils.java`
#### Snippet
```java
        public Conduit getBackChannel(Message inMessage) throws IOException {
            if (ContextUtils.isNoneAddress(reference)) {
                return null;
            }
            Bus bus = inMessage.getExchange().getBus();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/InternalContextUtils.java`
#### Snippet
```java
                return c;
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/InternalContextUtils.java`
#### Snippet
```java

        public MessageObserver getMessageObserver() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
            return (RMProperties)m.get(getRMPropertiesKey(false));
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder11Impl.java`
#### Snippet
```java

    public CloseSequenceType decodeSequenceTypeCloseSequence(Element elem) throws JAXBException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMUtils.java`
#### Snippet
```java
            return RM11Constants.INSTANCE;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java

    public Class<?> getTerminateSequenceResponseType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java
            return close;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java
            return close;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java

    public Class<?> getTerminateSequenceResponseType() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
    @ManagedAttribute(description = "Protocol Message Last Received", currencyTimeLimit = 60)
    public Date getLastControlMessage() {
        return endpoint.getLastControlMessage() == 0L ? null
            : new Date(endpoint.getLastControlMessage());
    }
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
    @ManagedAttribute(description = "Application Message Last Received", currencyTimeLimit = 60)
    public Date getLastApplicationMessage() {
        return endpoint.getLastApplicationMessage() == 0L ? null
            : new Date(endpoint.getLastApplicationMessage());
    }
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            return epr.getAddress().getValue();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ProtocolVariation.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502wsa15.OfferType convert200502wsa15(OfferType internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502wsa15.OfferType exposed =
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static Expires convert(org.apache.cxf.ws.rm.v200502.Expires exposed) {
        if (exposed == null) {
            return null;
        }
        Expires internal = new Expires();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502.AcceptType convert200502(AcceptType internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502.AcceptType exposed = RMUtils.getWSRM200502Factory().createAcceptType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static CreateSequenceType convert(org.apache.cxf.ws.rm.v200502.CreateSequenceType exposed) {
        if (exposed == null) {
            return null;
        }
        CreateSequenceType internal = new CreateSequenceType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502.OfferType convert200502(OfferType internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502.OfferType exposed = RMUtils.getWSRM200502Factory().createOfferType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static org.apache.cxf.ws.rm.v200502wsa15.Identifier convert200502wsa15(Identifier internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502wsa15.Identifier exposed =
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502wsa15.AcceptType convert200502wsa15(AcceptType internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502wsa15.AcceptType exposed =
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    convert(org.apache.cxf.ws.rm.v200502wsa15.CreateSequenceResponseType exposed) {
        if (exposed == null) {
            return null;
        }
        CreateSequenceResponseType internal = new CreateSequenceResponseType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static OfferType convert(org.apache.cxf.ws.rm.v200502.OfferType exposed) {
        if (exposed == null) {
            return null;
        }
        OfferType internal = new OfferType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static Identifier convert(org.apache.cxf.ws.rm.v200502.Identifier exposed) {
        if (exposed == null) {
            return null;
        }
        Identifier internal = new Identifier();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static Identifier convert(org.apache.cxf.ws.rm.v200502wsa15.Identifier exposed) {
        if (exposed == null) {
            return null;
        }
        Identifier internal = new Identifier();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static OfferType convert(org.apache.cxf.ws.rm.v200502wsa15.OfferType exposed) {
        if (exposed == null) {
            return null;
        }
        OfferType internal = new OfferType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static AcceptType convert(org.apache.cxf.ws.rm.v200502wsa15.AcceptType exposed) {
        if (exposed == null) {
            return null;
        }
        AcceptType internal = new AcceptType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502wsa15.Expires convert200502wsa15(Expires internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502wsa15.Expires exposed =
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static org.apache.cxf.ws.rm.v200502.Identifier convert200502(Identifier internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502.Identifier exposed = RMUtils.getWSRM200502Factory().createIdentifier();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static CreateSequenceType convert(org.apache.cxf.ws.rm.v200502wsa15.CreateSequenceType exposed) {
        if (exposed == null) {
            return null;
        }
        CreateSequenceType internal = new CreateSequenceType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static AcceptType convert(org.apache.cxf.ws.rm.v200502.AcceptType exposed) {
        if (exposed == null) {
            return null;
        }
        AcceptType internal = new AcceptType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    public static Expires convert(org.apache.cxf.ws.rm.v200502wsa15.Expires exposed) {
        if (exposed == null) {
            return null;
        }
        Expires internal = new Expires();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    convert(org.apache.cxf.ws.rm.v200502.CreateSequenceResponseType exposed) {
        if (exposed == null) {
            return null;
        }
        CreateSequenceResponseType internal = new CreateSequenceResponseType();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
    private static org.apache.cxf.ws.rm.v200502.Expires convert200502(Expires internal) {
        if (internal == null) {
            return null;
        }
        org.apache.cxf.ws.rm.v200502.Expires exposed = RMUtils.getWSRM200502Factory().createExpires();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
    Object getUsingAddressing(EndpointInfo endpointInfo) {
        if (null == endpointInfo) {
            return null;
        }
        List<ExtensibilityElement> exts = endpointInfo.getExtensors(ExtensibilityElement.class);
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
            return rme.getSource();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
        PhaseInterceptorChain pic = (PhaseInterceptorChain)ep.get(WSRM_RETRANSMIT_CHAIN);
        if (pic == null) {
            return null;
        }
        return pic.cloneChain();
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
            return rme.getDestination();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapInInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/DatatypeFactory.java`
#### Snippet
```java
            LogUtils.log(LOG, Level.SEVERE, "DATATYPE_FACTORY_INSTANTIATION_EXC", ex);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
                return new Date(next.getTime() - nextInterval / backoff);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassLoader.java`
#### Snippet
```java
                }
            } catch (Exception e) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassLoader.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBase.java`
#### Snippet
```java
        AbstractMessageContainer mi = mpi.getMessageInfo();
        if (mi == null || !isOutputMessage(mi)) {
            return null;
        }
        OperationInfo oi = mi.getOperation();
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBase.java`
#### Snippet
```java
        }
        OperationInfo oi = mi.getOperation();
        return oi != null ? (Annotation[])oi.getProperty("method.return.annotations") : null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBase.java`
#### Snippet
```java
            LOG.log(Level.INFO, "Could not create validation event handler", e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
        if (adapter == null && f.getGenericType() instanceof ParameterizedType
            && ((ParameterizedType)f.getGenericType()).getActualTypeArguments().length == 1) {
            return null;
        }
        Class<?> adapterType = (Class<?>)getTypeFromXmlAdapter(adapter);
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
            return cls.getDeclaredMethod(methodName, paramTypes);
        } catch (Exception e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
            return getDeclaredMethod(m.getDeclaringClass(), setterName, paramTypes);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
        throws InstantiationException, IllegalAccessException, InvocationTargetException,
               NoSuchMethodException, SecurityException {
        return adapterAnnotation != null ? adapterAnnotation.value().getDeclaredConstructor().newInstance() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/Utils.java`
#### Snippet
```java
        if (adapter == null && m.getGenericReturnType() instanceof ParameterizedType
            && ((ParameterizedType)m.getGenericReturnType()).getActualTypeArguments().length < 2) {
            return null;
        }
        Class<?> adapterType = (Class<?>)getTypeFromXmlAdapter(adapter);
```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
                return new Date(next.getTime() - nextInterval / backoff);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java

        if (cw == null) {
            return null;
        }
        int count = 1;
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
                }
            } catch (Exception e) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
            // ignore, we'll just fall down to reflection based
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/attachment/JAXBAttachmentMarshaller.java`
#### Snippet
```java
            return "cid:" + att.getId();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/attachment/JAXBAttachmentMarshaller.java`
#### Snippet
```java
            return "cid:" + att.getId();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
           //ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
            updateConnectionState(con, conex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
            updateConnectionState(con, conex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
            }
            ex.execute(r);
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
            throw new RMException(msg, ex);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            return Array.newInstance(ct, 0).getClass();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        Iterator<QName> itr = beanInfo.getTypeNames().iterator();
        if (!itr.hasNext()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/policy/AddressingAssertionBuilder.java`
#### Snippet
```java
                                          optional);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java

        public MessageObserver getMessageObserver() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
        public Conduit getBackChannel(Message inMessage) throws IOException {
            if (ContextUtils.isNoneAddress(reference)) {
                return null;
            }
            Bus bus = inMessage.getExchange().getBus();
```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
                return c;
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
            LOG.fine("action: " + action);
        }
        return action != null ? ContextUtils.getAttributedURI(action) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
            return ((QName)o).getLocalPart();
        }
        return o == null ? null : o.toString();
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
        BindingOperationInfo bop = message.getExchange().getBindingOperationInfo();
        if (bop == null || Boolean.TRUE.equals(bop.getProperty("operation.is.synthetic"))) {
            return null;
        }
        OperationInfo op = bop.getOperationInfo();
```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
    /**
```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                                             message);
        }
        return null;
    }
    /**
```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/AuthorizationUtils.java`
#### Snippet
```java
        }
        throwAuthorizationFailure(challenges, realm);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/AuthorizationUtils.java`
#### Snippet
```java
            return getBasicAuthParts(parts[1]);
        }
        return null;
    }
    public static String[] getBasicAuthParts(String basicAuthData) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/ClientTokenContextImpl.java`
#### Snippet
```java
            return (T)state;
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/MemoryClientTokenContextManager.java`
#### Snippet
```java
            return map.get(key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/BearerAuthSupplier.java`
#### Snippet
```java
                                   String fullHeader) {
        if (getClientAccessToken().getTokenKey() == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/BearerAuthSupplier.java`
#### Snippet
```java
            return createAuthorizationHeader();
        }
        return null;
    }
    private void refreshAccessTokenIfExpired(AuthorizationPolicy authPolicy) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
            return (X509Certificate)clientCerts[0];
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
    public static Object getElementValue(Object obj) {
        if (null == obj) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/crypto/ModelEncryptionSupport.java`
#### Snippet
```java

    public static String getStringPart(String str) {
        return " ".equals(str) ? null : str;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    protected String getNamespaceToUse(Service service) {
        if ("true".equals(service.get("org.apache.cxf.databinding.namespace"))) {
            return null;
        }
        final String tns;
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        } catch (Throwable t) {
            // Some error - probably a bad version of ASM or similar
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            return (DataWriter<T>)r;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/ClientCodeRequestFilter.java`
#### Snippet
```java
            return OAuthClientUtils.refreshAccessToken(accessTokenServiceClient, consumer, at);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACMTCodeDataProvider.java`
#### Snippet
```java
    @Override
    protected EntityTransaction beginIfNeeded(EntityManager em) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACMTCodeDataProvider.java`
#### Snippet
```java
                }
                em.refresh(refreshToken, LockModeType.PESSIMISTIC_WRITE, options);
                return null;
            });
        } catch (IllegalArgumentException e) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
            }
            em.persist(grant);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
        executeInTransaction(em -> {
            removeClientCodeGrants(c, em);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
            Client clientToRemove = em.getReference(Client.class, c.getClientId());
            em.remove(clientToRemove);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AuthorizationCodeGrantHandler.java`
#### Snippet
```java
            ((AuthorizationCodeDataProvider)getDataProvider()).removeCodeGrant(codeValue);
        if (grant == null) {
            return null;
        }
        // check it has not expired, the client ids are the same
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/OAuthClientUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/JwsJwksJwtAccessTokenValidator.java`
#### Snippet
```java
        Objects.requireNonNull(jwsHeaders.getKeyId());
        final JwkHolder jwkHolder = jsonWebKeys.computeIfAbsent(jwsHeaders.getKeyId(), keyId -> updateJwk(keyId));
        return jwkHolder != null ? jwkHolder.getJwsSignatureVerifier() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java
    protected String validateAudiences(List<String> audiences) {
        if (StringUtils.isEmpty(audiences) && audience == null) {
            return null;
        }
        if (audience != null) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java
        }
        if (!audienceIsEndpointAddress) {
            return null;
        }
        String requestPath = (String)PhaseInterceptorChain.getCurrentMessage().get(Message.REQUEST_URL);
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java
        }
        AuthorizationUtils.throwAuthorizationFailure(supportedSchemes, realm);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java

            public Principal getUserPrincipal() {
                return theSubject != null ? new SimplePrincipal(theSubject.getLogin()) : null;
            }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthRequestFilter.java`
#### Snippet
```java
        }
        AuthorizationUtils.throwAuthorizationFailure(supportedSchemes, realm);
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtProducer.java`
#### Snippet
```java
    protected JwsSignatureProvider getInitializedSignatureProvider(Client c) {
        if (c == null) {
            return null;
        }
        return super.getInitializedSignatureProvider(c.getClientSecret());
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthJoseJwtProducer.java`
#### Snippet
```java
            return OAuthUtils.getClientSecretSignatureProvider(clientSecret);
        }
        return null;
    }
    protected JweEncryptionProvider getInitializedEncryptionProvider(String clientSecret) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthJoseJwtProducer.java`
#### Snippet
```java
            return OAuthUtils.getClientSecretEncryptionProvider(clientSecret);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtConsumer.java`
#### Snippet
```java
    protected JweDecryptionProvider getInitializedDecryptionProvider(Client c) {
        if (c == null) {
            return null;
        }
        return super.getInitializedDecryptionProvider(c.getClientSecret());
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthJoseJwtConsumer.java`
#### Snippet
```java
            return OAuthUtils.getClientSecretDecryptionProvider(clientSecret);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthJoseJwtConsumer.java`
#### Snippet
```java
            return OAuthUtils.getClientSecretSignatureVerifier(clientSecret);
        }
        return null;
    }
    protected JweDecryptionProvider getInitializedDecryptionProvider(String clientSecret) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
            RefreshToken tokentoRemove = em.getReference(RefreshToken.class, rt.getTokenKey());
            em.remove(tokentoRemove);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
        executeInTransaction(em -> {
            em.merge(at);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
            Client clientToRemove = em.getReference(Client.class, c.getClientId());
            em.remove(clientToRemove);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
            ServerAccessToken tokenToRemove = em.getReference(at.getClass(), at.getTokenKey());
            em.remove(tokenToRemove);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
        executeInTransaction(em -> {
            em.remove(entity);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java

            em.persist(serverToken);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
                em.persist(client);
            }
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
        executeInTransaction(em -> {
            em.persist(entity);
            return null;
        });
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
                                  .setParameter("tokenKey", accessToken);
            if (query.getResultList().isEmpty()) {
                return null;
            }
            return query.getSingleResult();
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        }
        if (c == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            return template.getValue();
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    private static String splitMediaSubType(String type, boolean after) {
        int index = type.indexOf('+');
        return index == -1 ? null : after ? type.substring(index + 1) : type.substring(0, index);
    }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            return getUriTemplate(cri.getParent());
        } else {
            return null; /* should not happen */
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            return ProviderFactory.getInstance(m).createContextResolver(genericType, m);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            MultivaluedMap<String, String> values = new MetadataMap<>();
            return resources.get(0).getURITemplate().match(path, values)
                   ? Collections.singletonMap(resources.get(0), values) : null;
        }

```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        }

        return null;
    }
    public static OperationResourceInfo findTargetMethod(
```

### ReturnNull
Return of `null`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java

        if (!throwException) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadata.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractAccessTokenValidator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationService.java`
#### Snippet
```java

    private RedirectionBasedGrantService getService(String responseType) {
        return responseType == null ? null : servicesMap.get(responseType);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractOAuthService.java`
#### Snippet
```java
        }
        LOG.fine("No valid client found as the given clientId is null");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
            return c;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
    }
    protected String getCurrentClientSecret() {
        return messageContext != null ? (String)messageContext.get(OAuthConstants.CLIENT_SECRET) : null;
    }
    protected MultivaluedMap<String, String> getCurrentTokenRequestParams() {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java

    protected String getCurrentRequestedGrantType() {
        return messageContext != null ? (String)messageContext.get(OAuthConstants.GRANT_TYPE) : null;
    }
    protected String getCurrentClientSecret() {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
                                                   String grantType) throws OAuthServiceException {
        if (!isSupportPreauthorizedTokens()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
            return params;
        }
        return null;
    }
    protected RefreshToken updateExistingRefreshToken(RefreshToken rt, ServerAccessToken at) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/AbstractIdentityCache.java`
#### Snippet
```java
                                    + targetRealm + "'");
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
            // Do nothing
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
            return (X509Certificate)tlsInfo.getPeerCertificates()[0];
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java
        if (clientId == null) {
            reportInvalidRequestError("Client ID is null");
            return null;
        }
        Client client = null;
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java
            if (ex.getError() != null) {
                reportInvalidClient(ex.getError());
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/TokenProviderUtils.java`
#### Snippet
```java
                    } catch (XMLSecurityException ex) {
                        LOG.log(Level.FINE, ex.getMessage(), ex);
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/TokenProviderUtils.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/TokenProviderUtils.java`
#### Snippet
```java
            }
            LOG.fine("Participants element does not exist or could not be parsed");
            return null;
        } else if (participants instanceof JAXBElement<?>) {
            JAXBElement<?> jaxbElement = (JAXBElement<?>) participants;
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/TokenProviderUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/DefaultSubjectProvider.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/IdentityClaimsParser.java`
#### Snippet
```java
                        LOG.warning("Unsupported child element of ClaimValue element "
                                + valueNode.getLocalName());
                        return null;
                    }
                } else {
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/IdentityClaimsParser.java`
#### Snippet
```java
                } else {
                    LOG.warning("No child element of ClaimValue element available");
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/IdentityClaimsParser.java`
#### Snippet
```java

        LOG.fine("Found unknown element: " + claimLocalName + " " + claimNS);
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/CombinedClaimsAttributeStatementProvider.java`
#### Snippet
```java
        ProcessedClaimCollection retrievedClaims = ClaimsUtils.processClaims(providerParameters);
        if (retrievedClaims == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/CombinedClaimsAttributeStatementProvider.java`
#### Snippet
```java
        Iterator<ProcessedClaim> claimIterator = retrievedClaims.iterator();
        if (!claimIterator.hasNext()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsAttributeStatementProvider.java`
#### Snippet
```java
        ProcessedClaimCollection retrievedClaims = ClaimsUtils.processClaims(providerParameters);
        if (retrievedClaims == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsAttributeStatementProvider.java`
#### Snippet
```java
        Iterator<ProcessedClaim> claimIterator = retrievedClaims.iterator();
        if (!claimIterator.hasNext()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
                LOG.finest("Claim '" + claim.getClaimType() + "' is null");
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapUtils.java`
#### Snippet
```java
            return result.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapUtils.java`
#### Snippet
```java
                    return attrEnum.next().get();
                }
                return null;
            }
        };
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
    ) {
        if (primaryClaims == null && secondaryClaims == null) {
            return null;
        } else if (primaryClaims != null && secondaryClaims == null) {
            return retrieveClaimValues(primaryClaims, parameters);
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
    public ProcessedClaimCollection retrieveClaimValues(ClaimCollection claims, ClaimsParameters parameters) {
        if (claims == null || claims.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
    public ProcessedClaim get(ProcessedClaimCollection processedClaims, String processedClaimType) {
        if (processedClaimType == null || processedClaims == null) {
            return null;
        }
        for (ProcessedClaim c : processedClaims) {
```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/mapper/ClaimUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/server/ObservableInvoker.java`
#### Snippet
```java
            return asyncResponse;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/server/ObservableInvoker.java`
#### Snippet
```java
        //TODO: if it is a Cancelation exception => asyncResponse.cancel(); 
        asyncResponse.resume(t);
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
                                                                     subject);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSAEndpointReferenceUtils.java`
#### Snippet
```java
        }
        // should wsdl be parsed for an address now?
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/TokenIssueOperation.java`
#### Snippet
```java
        } catch (XMLSecurityException e) {
            LOG.log(Level.FINE, e.getMessage(), e);
            return null;
        }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/TokenIssueOperation.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/AbstractOperation.java`
#### Snippet
```java
        }
        LOG.fine("AppliesTo element does not exist or could not be parsed");
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/AbstractOperation.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/WSDLBasedServiceConfiguration.java`
#### Snippet
```java
    public String getStyle() {
        SOAPBinding sb = bi.getExtensor(SOAPBinding.class);
        return (sb == null) ? null : sb.getStyle();
    }
}
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
    public BindingOperation getBindingOperation(Binding binding, String operationName) {
        if (operationName == null) {
            return null;
        }
        List<BindingOperation> bindingOperations = CastUtils.cast(binding.getBindingOperations());
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
    public BindingOperation getBindingOperation(Definition def, String operationName) {
        if (operationName == null) {
            return null;
        }
        Iterator<Binding> ite = CastUtils.cast(def.getBindings().values().iterator());
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            throw new STSException(e.getMessage(), e, STSException.INVALID_REQUEST);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public String getRequestWrapperClassName(Method selected) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Type getHolderType(Class<?> cls, Type type) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getFaultMessageName(OperationInfo op, Class<?> exClass, Class<?> beanClass) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isWrapperPartNillable(MessagePartInfo mpi) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isAsync(final Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    public QName getInParameterName(final OperationInfo op, final Method method,
                                    final int paramNumber) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public String getResponseWrapperPartName(OperationInfo op, Method method) {
        return null;
    }
    public String getRequestWrapperPartName(OperationInfo op, Method method) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
     */
    public QName getOperationName(InterfaceInfo service, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public Long getWrapperPartMaxOccurs(MessagePartInfo mpi) {
        return null;
    }
    public Long getWrapperPartMinOccurs(MessagePartInfo mpi) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isOutParam(Method method, int j) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getServiceName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getFaultName(InterfaceInfo service, OperationInfo o, Class<?> exClass, Class<?> beanClass) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getEndpointName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getStyle() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getWsdlURL() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isHeader(Method method, int j) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    public QName getOutParameterName(final OperationInfo op, final Method method,
                                      final int paramNumber) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isWrapped(Method m) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isOperation(final Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public String getResponseWrapperClassName(Method selected) {
        return null;
    }
    public String getRequestWrapperClassName(Method selected) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getInterfaceName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isInParam(Method method, int j) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getOutputMessageName(final OperationInfo op, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getInputMessageName(final OperationInfo op, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isRPC(Method selected) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isWrapperPartQualified(MessagePartInfo mpi) {
        return null;
    }
    public Long getWrapperPartMaxOccurs(MessagePartInfo mpi) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getAction(OperationInfo op, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Class<?> getRequestWrapper(Method selected) {
        return null;
    }
    public String getResponseWrapperClassName(Method selected) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isInOutParam(Method method, int j) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public String getMEP(final Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean hasOutMessage(Method m) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getRequestWrapperName(OperationInfo op, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getResponseWrapperName(OperationInfo op, Method method) {
        return null;
    }
    public String getResponseWrapperPartName(OperationInfo op, Method method) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getOutPartName(final OperationInfo op, final Method method, final int paramNumber) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public QName getInPartName(final OperationInfo op, final Method method, final int paramNumber) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Class<?> getResponseWrapper(Method selected) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isWrapped() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public Long getWrapperPartMinOccurs(MessagePartInfo mpi) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java

    public Boolean isHolder(Class<?> cls, Type type) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/AbstractServiceConfiguration.java`
#### Snippet
```java
    }
    public String getRequestWrapperPartName(OperationInfo op, Method method) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ParamReader.java`
#### Snippet
```java
                return paramNames;
            }
            return null;
        }
        return null;
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ParamReader.java`
#### Snippet
```java
            return null;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ParamReader.java`
#### Snippet
```java
        int numParams = method.getParameterTypes().length;
        if (numParams == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ParamReader.java`
#### Snippet
```java
        // Don't worry about it if the class is a Java dynamic proxy
        if (Proxy.isProxyClass(c)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ParamReader.java`
#### Snippet
```java
            // log it and leave
            // log.info(Messages.getMessage("error00") + ":" + e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
        /** {@inheritDoc}*/
        public Reader getCharacterStream() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
        /** {@inheritDoc}*/
        public String getBaseURI() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
    void tryToParseSchemas(XmlSchemaCollection collection, DOMErrorHandler handler)
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
        /** {@inheritDoc}*/
        public String getPublicId() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
        /** {@inheritDoc}*/
        public InputStream getByteStream() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
            return Long.valueOf(0);
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
            return Long.MAX_VALUE;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
    public ServiceSchemaInfo getSchemasForDefinition(Definition wsdl) {
        if (disableSchemaCache) {
            return null;
        }
        synchronized (definitionsMap) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPAddress.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPBinding.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPFault.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPHeader.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPBody.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SOAPOperation.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java
            if (ns == null) {
                if (ignoreUnknownBindings) {
                    return null;
                }
                org.apache.cxf.common.i18n.Message msg = new
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            e.printStackTrace();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/testutil/handler/TrivialSOAPHandler.java`
#### Snippet
```java

    public Set<QName> getHeaders() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/testutil/common/TestUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public static Properties getAllPorts() {
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/authservice/AuthServiceImpl.java`
#### Snippet
```java
    public String[] getRolesAsArray(String sid) {
        if ("null".equals(sid)) {
            return null;
        }
        if ("0".equals(sid)) {
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Response<PingMeResponse> pingMeAsync() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Response<GreetMeResponse> greetMeAsync(String requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Response<SayHiResponse> sayHiAsync() {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Future<?> greetMeAsync(String requestType, AsyncHandler<GreetMeResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java

    public Future<?> pingMeAsync(AsyncHandler<PingMeResponse> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Response<PingMeResponse> pingMeAsync() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeResponse> greetMeAsync(String arg0) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Response<SayHiResponse> sayHiAsync() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public String sayHi() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeAsync(String arg0, AsyncHandler<GreetMeResponse> arg1) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> arg0) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/AbstractGreeterImpl.java`
#### Snippet
```java

    public Future<?> pingMeAsync(AsyncHandler<PingMeResponse> arg0) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/anonymous_complex_type/AnonymousComplexTypeImpl.java`
#### Snippet
```java
            return refResponse;
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/anonymous_complex_type/AnonymousComplexTypeImpl.java`
#### Snippet
```java
            return names;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/test/XPathAssert.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/test/XPathAssert.java`
#### Snippet
```java

        public Iterator<String> getPrefixes(String namespaceURI) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
    public Response<StopGreeterResponse> stopGreeterAsync(String requestType) {
        // never called
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
    public Future<?> startGreeterAsync(String requestType, AsyncHandler<StartGreeterResponse> asyncHandler) {
        // never called
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java

    public Future<?> setRobustInOnlyModeAsync(boolean in, AsyncHandler<?> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java

    public Response<?> setFaultLocationAsync(FaultLocation in) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
    public Future<?> stopGreeterAsync(String requestType, AsyncHandler<StopGreeterResponse> asyncHandler) {
        // never called
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java

    public Response<?> setRobustInOnlyModeAsync(boolean in) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java

    public Future<?> setFaultLocationAsync(FaultLocation in, AsyncHandler<?> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
    public Response<StartGreeterResponse> startGreeterAsync(String requestType) {
        // never called
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/RPCLitGreeterImpl.java`
#### Snippet
```java
        //System.out.println("Message received: " + me + "\n");
        if ("return null".equals(me)) {
            return null;
        }
        return "Hello " + me;
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_xml_http/bare/GreeterImpl.java`
#### Snippet
```java

    public String testTriPartNoOrder(String in3, MyComplexStructType in1, String in2) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_xml_http/bare/GreeterImpl.java`
#### Snippet
```java

    public String testTriPart(MyComplexStructType in1, String in3, String in2) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedProvider.java`
#### Snippet
```java

    public Source invoke(Source source) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DocLitBareGreeterImpl.java`
#### Snippet
```java
    public Response<BareDocumentResponse> testDocLitBareAsync(String in) {

        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DocLitBareGreeterImpl.java`
#### Snippet
```java
    public Future<?> testDocLitBareAsync(String in, AsyncHandler<BareDocumentResponse> asyncHandler) {

        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
                                       int intElem,
                                       AsyncHandler<TestNillableResponse> asyncHandler) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeAsync(String requestType, AsyncHandler<GreetMeResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    public Response<TestNillableResponse> testNillableAsync(String nillElem,
                                                            int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<BareDocumentResponse> testDocLitBareAsync(String in) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeResponse> greetMeAsync(String requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeSometimeResponse> greetMeSometimeAsync(String requestType) {
        return null;
        /* to be implemented */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public String testNillable(String nillElem, int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> testDocLitBareAsync(String in, AsyncHandler<BareDocumentResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<SayHiResponse> sayHiAsync() {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeLaterAsync(long requestType, AsyncHandler<GreetMeLaterResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    public Future<?>  greetMeSometimeAsync(String requestType,
                                           AsyncHandler<GreetMeSometimeResponse> asyncHandler) {
        return null;
        /* to be implemented */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeLaterResponse> greetMeLaterAsync(long requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType,
                                                                  AsyncHandler<TestDocLitFaultResponse> ah) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DocLitBareImpl.java`
#### Snippet
```java

    public String nillableParameter(BigDecimal theRequest) {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeLaterAsync(long requestType, AsyncHandler<GreetMeLaterResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
        invocationCount++;
        System.err.println("In greetMeSometimeAsync 1");
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
        invocationCount++;
        System.err.println("In testDocLitFaultAsync 2");
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<SayHiResponse> sayHiAsync() {
        invocationCount++;
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Future<?> greetMeAsync(String requestType, AsyncHandler<GreetMeResponse> asyncHandler) {
        invocationCount++;
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> asyncHandler) {
        invocationCount++;
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
        invocationCount++;
        System.err.println("In testDocLitFaultAsync 1");
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
                                       int intElem,
                                       AsyncHandler<TestNillableResponse> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
        invocationCount++;
        System.err.println("In greetMeSometimeAsync 2");
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<TestNillableResponse> testNillableAsync(String nillElem,
                                                            int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeLaterResponse> greetMeLaterAsync(long requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<GreetMeResponse> greetMeAsync(String requestType) {
        invocationCount++;
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/BaseGreeterImpl.java`
#### Snippet
```java
    public Response<BareDocumentResponse> testDocLitBareAsync(String bare) {
        invocationCount++;
        return null;
        /* not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java
    public Response<TestNillableResponse> testNillableAsync(String nillElem,
                                                            int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public String testNillable(String nillElem, int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeLaterAsync(long requestType, AsyncHandler<GreetMeLaterResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeLaterResponse> greetMeLaterAsync(long requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Future<?>  greetMeAsync(String requestType, AsyncHandler<GreetMeResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<BareDocumentResponse> testDocLitBareAsync(String bare) {
        return null;
        /* not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeSometimeResponse> greetMeSometimeAsync(String requestType) {
        return null;
        /* to be implemented" */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java
                                       int intElem,
                                       AsyncHandler<TestNillableResponse> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<SayHiResponse> sayHiAsync() {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Future<?> testDocLitBareAsync(String bare, AsyncHandler<BareDocumentResponse> ah) {
        return null;
        /* not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java
    public Future<?> testDocLitFaultAsync(String faultType,
                                          AsyncHandler<TestDocLitFaultResponse> ah) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeResponse> greetMeAsync(String requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/NotAnnotatedGreeterImpl.java`
#### Snippet
```java
    public Future<?>  greetMeSometimeAsync(String requestType,
                                           AsyncHandler<GreetMeSometimeResponse> asyncHandler) {
        return null;
        /* to be implemented */
    }
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-common/src/main/java/org/apache/cxf/xkms/handlers/Applications.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-common/src/main/java/org/apache/cxf/xkms/exception/ExceptionMapper.java`
#### Snippet
```java
        }
        if (major == ResultMajorEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_SUCCESS) {
            return null;
        }
        ResultMinorEnum minor = null;
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    public Response<TestNillableResponse> testNillableAsync(String nillElem,
                                                            int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeResponse> greetMeAsync(String requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<BareDocumentResponse> testDocLitBareAsync(String in) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<SayHiResponse> sayHiAsync() {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeLaterResponse> greetMeLaterAsync(long requestType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> testDocLitBareAsync(String in, AsyncHandler<BareDocumentResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeLaterAsync(long requestType, AsyncHandler<GreetMeLaterResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> greetMeAsync(String requestType, AsyncHandler<GreetMeResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Future<?> sayHiAsync(AsyncHandler<SayHiResponse> asyncHandler) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
                                       int intElem,
                                       AsyncHandler<TestNillableResponse> asyncHandler) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public String testNillable(String nillElem, int intElem) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType,
                                                                  AsyncHandler<TestDocLitFaultResponse> ah) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    public Future<?>  greetMeSometimeAsync(String requestType,
                                           AsyncHandler<GreetMeSometimeResponse> asyncHandler) {
        return null;
        /* to be implemented */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<GreetMeSometimeResponse> greetMeSometimeAsync(String requestType) {
        return null;
        /* to be implemented */
    }
```

### ReturnNull
Return of `null`
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java

    public Response<TestDocLitFaultResponse> testDocLitFaultAsync(String faultType) {
        return null;
        /*not called */
    }
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
    public List<ExtensibilityElement> getWSDL11Extensors(AbstractPropertiesHolder holder) {
        if (holder == null) {
            return null;
        }
        return holder.getExtensors(ExtensibilityElement.class);
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/utils/X509Utils.java`
#### Snippet
```java
            return certificate;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResolver.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResolver.java`
#### Snippet
```java
            String version = urlStr.substring(swaggerUiIndex + 12, urlStr.lastIndexOf(".jar"));
            if (swaggerUiVersion != null && !swaggerUiVersion.equals(version)) {
                return null;
            }
            if (!urlStr.endsWith("/")) {
```

### ReturnNull
Return of `null`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiResolver.java`
#### Snippet
```java
            return urlStr + UI_RESOURCES_ROOT_START + version + "/";
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/handlers/X509Locator.java`
#### Snippet
```java
            cert = findCertificate(keyIDs);
            if (cert == null) {
                return null;
            }
            UnverifiedKeyBindingType result = new UnverifiedKeyBindingType();
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/handlers/X509Locator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            return paramAnno[index];
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        Class<?> cls = getServiceClass();
        if (cls.isInterface()) {
            return null;
        }
        return new FactoryInvoker(new SingletonFactory(getServiceClass()));
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public boolean isWrapperPartNillable(MessagePartInfo mpi) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
    protected QName getInParameterName(final OperationInfo op, final Method method, final int paramNumber) {
        if (paramNumber == -1) {
            return null;
        }
        for (AbstractServiceConfiguration c : serviceConfigurations) {
```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
    protected QName getInPartName(final OperationInfo op, final Method method, final int paramNumber) {
        if (paramNumber == -1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java

    protected Set<Class<?>> getExtraClass() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
    protected Class<?> getBeanClass(Class<?> exClass) {
        if (java.rmi.RemoteException.class.isAssignableFrom(exClass)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        Class<?> beanClass = getBeanClass(exClass);
        if (beanClass == null) {
            return null;
        }
        String faultMsgName = null;
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaTypeMap.java`
#### Snippet
```java
        } catch (org.omg.CORBA.TypeCodePackage.BadKind ex) {
            // No id has been set.
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaTypeMap.java`
#### Snippet
```java

        if (repId == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaTypeMap.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/TypeMapCache.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
    }
    public MessageObserver getMessageObserver() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            return result.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            return result.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                LOG.warn(String.format("Certificate not found for endpoint %s, path %s", endpoint,
                                       certFile.getAbsolutePath()));
                return null;
            }
            InputStream input = Files.newInputStream(certFile.toPath());
```

### ReturnNull
Return of `null`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            LOG.warn(String.format("Cannot load certificate by endpoint: %s. Error: %s", endpoint,
                                   e.getMessage()), e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaEnumEventProducer.java`
#### Snippet
```java

    public List<Namespace> getNamespaces() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaEnumEventProducer.java`
#### Snippet
```java

    public List<Attribute> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveTypeEventProducer.java`
#### Snippet
```java

    public List<Namespace> getNamespaces() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveTypeEventProducer.java`
#### Snippet
```java

    public List<Attribute> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/ParameterEventProducer.java`
#### Snippet
```java

    public List<Attribute> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/ParameterEventProducer.java`
#### Snippet
```java

    public String getText() {
        return currentEventProducer != null ? currentEventProducer.getText() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/ParameterEventProducer.java`
#### Snippet
```java

    public QName getName() {
        return currentEventProducer != null ? currentEventProducer.getName() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/ParameterEventProducer.java`
#### Snippet
```java

    public String getLocalName() {
        return currentEventProducer != null ? currentEventProducer.getLocalName() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/ParameterEventProducer.java`
#### Snippet
```java

    public List<Namespace> getNamespaces() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java

        public List<Attribute> getAttributes() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java
            return attributes;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java

        public List<Namespace> getNamespaces() {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java

        public QName getName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java

        public String getLocalName() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaFixedEventProducer.java`
#### Snippet
```java

    public List<Attribute> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaFixedEventProducer.java`
#### Snippet
```java

    public List<Namespace> getNamespaces() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/AbstractNoStartEndEventProducer.java`
#### Snippet
```java

    public String getLocalName() {
        return currentEventProducer != null ? currentEventProducer.getLocalName() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/AbstractNoStartEndEventProducer.java`
#### Snippet
```java

    public String getText() {
        return currentEventProducer != null ? currentEventProducer.getText() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/AbstractNoStartEndEventProducer.java`
#### Snippet
```java

    public QName getName() {
        return currentEventProducer != null ? currentEventProducer.getName() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaOctetSequenceEventProducer.java`
#### Snippet
```java

    public List<Namespace> getNamespaces() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaOctetSequenceEventProducer.java`
#### Snippet
```java

    public List<Attribute> getAttributes() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

        public List<Namespace> getNamespaces() {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

        public List<Attribute> getAttributes() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

        public List<Attribute> getAttributes() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaUnionHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaObjectReferenceHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaObjectReferenceHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaObjectReferenceHelper.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getPITarget() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getCharacterEncodingScheme() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getPIData() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

            public String getSystemId() {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getNamespaceURI(String arg0) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getAttributeValue(String arg0, String arg1) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getVersion() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getEncoding() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java
        List<Namespace> namespaces = eventProducer.getNamespaces();
        if (namespaces == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java
            return ns.getNamespaceURI();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

            public String getPublicId() {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getPrefix() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public Object getProperty(String arg0) throws IllegalArgumentException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public NamespaceContext getNamespaceContext() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getAttributePrefix(int arg0) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java

    public String getElementText() throws XMLStreamException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java
        List<Namespace> namespaces = eventProducer.getNamespaces();
        if (namespaces == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamReader.java`
#### Snippet
```java
            return ns.getPrefix();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
                return parent.getPrefix(namespaceURI);
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    public java.lang.Object getProperty(java.lang.String name)
        throws java.lang.IllegalArgumentException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionImpl.java`
#### Snippet
```java
    public CXFTransaction getCXFTransaction() {
        //TODO should throw the exception
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java

    protected Object getBootstrapContext() {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java

    public Bus getBus() {
        return (jcaBusFactory != null) ? jcaBusFactory.getBus() : null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
            return context.getWorkManager();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoaderHelper.java`
#### Snippet
```java

        // failed to locate the resource
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            return typeMap.getType(idlType.getLocalPart());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
    private static String[] filters2FNFilters(String[] fs) {
        if (fs == null || fs.length == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
    public String[] getFilters() {
        if (filters == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
    private static String[] processFilters(String[] fs) {
        if (fs == null || fs.length == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
    public String[] getNegativeFilters() {
        if (negativeFilters == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBInvoker.java`
#### Snippet
```java
        if (ep == null) {
            LOG.log(Level.SEVERE, "Failed to obtain MessageEndpoint");
            return null;
        }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBInvoker.java`
#### Snippet
```java
        } catch (Exception e) {
            LOG.log(Level.SEVERE, "Failed to obtain service object " + targetJndiName, e);
            return null;
        } finally {
            if (epFromMessage == null) {
```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
                .getWorkManager();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
            return mc;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
            sendEvent(event);

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationWork.java`
#### Snippet
```java
    private List<String> getListOfString(String str) {
        if (str == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
                                      UndertowHTTPServerEngineFactory.class);
        } catch (Throwable t) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
                                                         registry, endpointInfo, undertowServerEngineFactory);
                }
                return null;
            }
            if (JETTY_AVAILABLE) {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
                                                     registry, endpointInfo, undertowServerEngineFactory);
            }
            return null;
        }
        // REVISIT other way of getting the registry of http so that the plain cxf servlet finds the
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
            // why are we throwing a busexception if the DF isn't found?
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
                                      JettyHTTPServerEngineFactory.class);
        } catch (Throwable t) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getHeaders({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeRedirectUrl({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
    public Collection<String> getHeaderNames() {
        LOG.log(Level.FINE, "getHeaderNames()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getHeader({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getHeaders({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getHeader({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
    public Locale getLocale() {
        LOG.log(Level.FINE, "getLocale");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeUrl({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeURL({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeRedirectURL({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeURL({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
    public String getCharacterEncoding() {
        LOG.log(Level.FINE, "getCharacterEncoding()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
    public String getCharacterEncoding() {
        LOG.log(Level.FINE, "getCharacterEncoding()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeRedirectUrl({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeRedirectURL({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
            LOG.log(Level.FINE, "encodeUrl({0})", url);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
    public Collection<String> getHeaderNames() {
        LOG.log(Level.FINE, "getHeaderNames()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletResponse.java`
#### Snippet
```java
    public Locale getLocale() {
        LOG.log(Level.FINE, "getLocale");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public HttpSession getSession(boolean create) {
        LOG.log(Level.FINE, "getSession");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public String getRealPath(String path) {
        LOG.log(Level.FINE, "getRealPath");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getParameterValues({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public String getQueryString() {
        LOG.log(Level.FINE, "getQueryString");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    @Override
    public AsyncContext getAsyncContext() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public AsyncContext startAsync(ServletRequest servletRequest, ServletResponse servletResponse) {
        LOG.log(Level.FINE, "startAsync");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public Map<String, String[]> getParameterMap() {
        LOG.log(Level.FINE, "getParameterMap()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public String getRemoteUser() {
        LOG.log(Level.FINE, "getRemoteUser");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public String getCharacterEncoding() {
        LOG.log(Level.FINE, "getCharacterEncoding()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public Cookie[] getCookies() {
        LOG.log(Level.FINE, "getCookies");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public Collection<Part> getParts() throws IOException, ServletException {
        LOG.log(Level.FINE, "getParts");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public HttpSession getSession() {
        LOG.log(Level.FINE, "getSession");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public Part getPart(String name) throws IOException, ServletException {
        LOG.log(Level.FINE, "getPart");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public String getRequestedSessionId() {
        LOG.log(Level.FINE, "getRequestedSessionId");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getParameter({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
        // some container may choose not to return this value
        if (opathtrans == null) {
            return null;
        }
        String opathinfo = webSocketHolder.getPathInfo();
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public AsyncContext startAsync() {
        LOG.log(Level.FINE, "startAsync");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public RequestDispatcher getRequestDispatcher(String path) {
        LOG.log(Level.FINE, "getRequestDispatcher");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
    public Enumeration<String> getParameterNames() {
        LOG.log(Level.FINE, "getParameterNames()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getProtocol() {
            return null;
        }
        public String getRemoteAddr() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public DispatcherType getDispatcherType() {
            return null;
        }
        public boolean isSecure() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getLocalName() {
            return null;
        }
        public int getLocalPort() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public Locale getLocale() {
            return null;
        }
        public Enumeration<Locale> getLocales() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public Principal getUserPrincipal() {
            return null;
        }
        public Object getAttribute(String name) {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public Enumeration<Locale> getLocales() {
            return null;
        }
        public String getProtocol() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public ServletContext getServletContext() {
            return null;
        }
        public int getServerPort() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getServerName() {
            return null;
        }
        public String getServletPath() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getAuthType() {
            return null;
        }
        public String getContextPath() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getRemoteHost() {
            return null;
        }
        public int getRemotePort() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getLocalAddr() {
            return null;
        }
        public String getLocalName() {
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        }
        public String getRemoteAddr() {
            return null;
        }
        public String getRemoteHost() {
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapMessage.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Enumeration<Locale> getLocales() {
        LOG.log(Level.FINE, "getLocales()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getQueryString() {
        LOG.log(Level.FINE, "getQueryString");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getCharacterEncoding() {
        LOG.log(Level.FINE, "getCharacterEncoding()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getContextPath -> " + null);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    @Override
    public String getServletPath() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getRemoteHost error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public ServletContext getServletContext() {
        LOG.log(Level.FINE, "getServletContext");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Locale getLocale() {
        LOG.log(Level.FINE, "getLocale()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getLocalName error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getRemoteUser() {
        LOG.log(Level.FINE, "getRemoteUser");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getPathInfo() {
        
        return null;
      
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketServletDestination.java`
#### Snippet
```java
                    @Override
                    public String getServletName() {
                        return null;
                    }
                    @Override
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Cookie[] getCookies() {
        LOG.log(Level.FINE, "getCookies");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketServletDestination.java`
#### Snippet
```java
                    @Override
                    public String getInitParameter(String name) {
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    @Override
    public String getPathTranslated() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketServletDestination.java`
#### Snippet
```java
                    @Override
                    public Enumeration<String> getInitParameterNames() {
                        return null;
                    }
                });
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Map<String, String[]> getParameterMap() {
        LOG.log(Level.FINE, "getParameterMap()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public HttpSession getSession() {
        LOG.log(Level.FINE, "getSession");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getParameterValues({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getRequestedSessionId() {
        LOG.log(Level.FINE, "getRequestedSessionId");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Collection<Part> getParts() throws IOException, ServletException {
        LOG.log(Level.FINE, "getParts");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Part getPart(String name) throws IOException, ServletException {
        LOG.log(Level.FINE, "getPart");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public DispatcherType getDispatcherType() {
        LOG.log(Level.FINE, "getDispatcherType()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public AsyncContext startAsync(ServletRequest servletRequest, ServletResponse servletResponse) {
        LOG.log(Level.FINE, "startAsync");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public RequestDispatcher getRequestDispatcher(String path) {
        LOG.log(Level.FINE, "getRequestDispatcher");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
            LOG.log(Level.FINE, "getParameter({0})", name);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            }
            if (sb.length() == 0) {
                return null;
            }
            return sb.toString();
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java

        private String gettext(Object o) {
            return o instanceof String ? (String)o : (o instanceof byte[] ? new String((byte[])o) : null);
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
        protected String getResponseMessage() throws IOException {
            //TODO return a generic message based on the status code
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
        @Override
        protected HttpsURLConnectionInfo getHttpsURLConnectionInfo() throws IOException {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public HttpSession getSession(boolean create) {
        LOG.log(Level.FINE, "getSession");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getProtocol error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Principal getUserPrincipal() {
        LOG.log(Level.FINE, "getUserPrincipal");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getRemoteAddr error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public String getRealPath(String path) {
        LOG.log(Level.FINE, "getRealPath");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public Enumeration<String> getParameterNames() {
        LOG.log(Level.FINE, "getParameterNames()");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    public AsyncContext startAsync() {
        LOG.log(Level.FINE, "startAsync");
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
    @Override
    public AsyncContext getAsyncContext() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getLocalAddr error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/WebSocketUndertowServletRequest.java`
#### Snippet
```java
        } catch (MalformedURLException e) {
            LOG.log(Level.FINE, "getScheme error", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
        // no characters found...this was either an eof or a null line.
        if (buffer.length() == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/Soap11.java`
#### Snippet
```java
    public QName getDateEncodingUnknown() {
        // There is no such fault code in soap11
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java

    private QName getRootSubCode() {
        return subCodes != null && !subCodes.isEmpty() ? subCodes.get(0) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/saaj/SAAJUtils.java`
#### Snippet
```java
        } catch (IllegalArgumentException ex) {
            //java9
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/model/SoapBindingInfo.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/EndpointSelectionInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapFault.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapBinding.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    public static jakarta.jws.soap.SOAPBinding.Use getSoapUse(String soapUse) {
        if ("".equals(soapUse)) {
            return null;
        } else if ("ENCODED".equalsIgnoreCase(soapUse)) {
            return jakarta.jws.soap.SOAPBinding.Use.ENCODED;
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapHeader.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    public static jakarta.jws.soap.SOAPBinding.Style getSoapStyle(String soapStyle) {
        if ("".equals(soapStyle)) {
            return null;
        } else if ("RPC".equalsIgnoreCase(soapStyle)) {
            return jakarta.jws.soap.SOAPBinding.Style.RPC;
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapBody.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapAddress.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            return getProxy(SoapOperation.class, obj);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/RPCInInterceptor.java`
#### Snippet
```java
            Endpoint ep = message.getExchange().getEndpoint();
            if (ep == null) {
                return null;
            }
            BindingInfo service = ep.getEndpointInfo().getBinding();
```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/saml/SamlOAuthValidator.java`
#### Snippet
```java
    private String getIssuer(SamlAssertionWrapper assertionW) {
        Issuer samlIssuer = assertionW.getSaml2().getIssuer();
        return samlIssuer == null ? null : samlIssuer.getValue();
    }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java
    public static String getSoapAction(Message m) {
        if (!(m instanceof SoapMessage)) {
            return null;
        }
        SoapMessage message = (SoapMessage)m;
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java

            if (ct == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/Saml2BearerGrantHandler.java`
#### Snippet
```java
    private Certificate[] getTLSCertificates(Message message) {
        TLSSessionInfo tlsInfo = message.get(TLSSessionInfo.class);
        return tlsInfo != null ? tlsInfo.getPeerCertificates() : null;
    }

```

### ReturnNull
Return of `null`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java

        if (wadl == null || !wadl.exists()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java
        }
        if (idx == -1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java
    private static WadlOption generateWsdlOptionFromArtifact(Artifact artifact, File outputDir) {
        if (!WADL_TYPE.equals(artifact.getType())) {
            return null;
        }
        WadlOption option = new WadlOption();
```

### ReturnNull
Return of `null`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
        if (msg == null) {
            if (done.contains(def)) {
                return null;
            }
            done.add(def);
```

### ReturnNull
Return of `null`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WadlOption.java`
#### Snippet
```java
    public File getDocumentFile(File baseDir) {
        if (wadl == null) {
            return null;
        }
        File file = null;
```

### ReturnNull
Return of `null`
in `rt/features/throttling/src/main/java/org/apache/cxf/throttling/SimpleThrottlingManager.java`
#### Snippet
```java
    public ThrottleResponse getThrottleResponse(String phase, Message m) {
        if (m.containsKey(THROTTLED_KEY)) {
            return null;
        }
        m.getExchange().put(ThrottlingCounter.class, counter);
```

### ReturnNull
Return of `null`
in `rt/features/throttling/src/main/java/org/apache/cxf/throttling/SimpleThrottlingManager.java`
#### Snippet
```java
            return this;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
            nd = nd.getNextSibling();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AbstractPolicyInterceptor.java`
#### Snippet
```java
            Throwable cause = ex.getCause();
            if (null == cause) {
                return null;
            }
            for (BindingFaultInfo b : boi.getFaults()) {
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java
            return new WrappedAssertor((org.apache.cxf.transport.Assertor)o);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/mtom/MTOMAssertionBuilder.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java

        if (null == src) {
            return null;
        }
        XMLStreamReader reader = null;
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
        } catch (XMLStreamException e) {
            LOG.log(Level.WARNING, e.getMessage());
            return null;
        } finally {
            try {
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
            Element element = loadPolicy(uri, defName);
            if (element == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
            return pb.getElement();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/WSPolicyFeature.java`
#### Snippet
```java
                    return builder.getPolicy(pb.getElement());
                }
                return null;
            }
        };
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
    Assertor initialisePolicy(EndpointInfo ei,
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/selector/FirstAlternativeSelector.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/LocalServiceModelReferenceResolver.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/LocalDocumentReferenceResolver.java`
#### Snippet
```java
    public Policy resolveReference(String uri, Element el) {
        if (el == null) {
            return null;
        }
        if (uri.equals(el.getAttributeNS(PolicyConstants.WSU_NAMESPACE_URI,
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/LocalDocumentReferenceResolver.java`
#### Snippet
```java
            el2 = DOMUtils.getNextElement(el2);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java
    Collection<Assertion> getAssertions(EffectivePolicy pc, boolean includeOptional) {
        if (pc == null || pc.getChosenAlternative() == null) {
            return null;
        }
        Collection<Assertion> assertions = new ArrayList<>();
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/RemoteReferenceResolver.java`
#### Snippet
```java
        InputSource is = resolver.resolve(documentURI, baseURI);
        if (null == is) {
            return null;
        }
        Document doc = null;
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/reference/RemoteReferenceResolver.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
    private LogEventSender createEventSender(String location) {
        if (StringUtils.isEmpty(location)) {
            return null;
        }
        if ("<stdout>".equals(location)) {
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/LogMessageFormatter.java`
#### Snippet
```java
    }
    protected static String localPart(QName name) {
        return name == null ? null : name.getLocalPart();
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
    private Extensible getMessageTypeInfo(QName name, DescriptionInfo di) {
        if (null == di) {
            return null;
        }
        Definition def = (Definition)di.getProperty("org.apache.cxf.wsdl11.WSDLServiceBuilder.DEFINITION");
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
        Definition def = (Definition)di.getProperty("org.apache.cxf.wsdl11.WSDLServiceBuilder.DEFINITION");
        if (null == def) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
    Policy getElementPolicy(AbstractDescriptionElement adh, boolean includeAttributes) {
        if (adh == null) {
            return null;
        }
        return getElementPolicy(adh, includeAttributes, adh.getDescription());
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                }
            }
            return null;
        }
        public <T> List<T> getExtensors(Class<T> cls) {
```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
    Policy getElementPolicy(Extensible ex, boolean includeAttributes, DescriptionInfo di) {
        if (null == ex || null == di) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
        public <T> List<T> getExtensors(Class<T> cls) {
            if (null == extensors) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java

        public Map<QName, Object> getExtensionAttributes() {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java

        public Object getExtensionAttribute(QName arg0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
        // Use regex to get the Content-Type and return null if it's not found
        Matcher m = CONTENT_TYPE_PATTERN.matcher(payload);
        return m.find() ? m.group(0) : null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
        // Use regex to get the boundary and return null if it's not found
        Matcher m = BOUNDARY_PATTERN.matcher(payload);
        return m.find() ? "--" + m.group(1) : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/KeyAlgorithm.java`
#### Snippet
```java
    public static KeyAlgorithm getAlgorithm(String algo) {
        if (algo == null) {
            return null;
        }
        if (KeyAlgorithm.DIRECT.getJwaName().equals(algo)) {
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/slf4j/Slf4jEventSender.java`
#### Snippet
```java

    private String localPart(QName name) {
        return name == null ? null : name.getLocalPart();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/ContentAlgorithm.java`
#### Snippet
```java
    public static ContentAlgorithm getAlgorithm(String algo) {
        if (algo == null) {
            return null;
        }
        return ContentAlgorithm.valueOf(algo.replace('-', '_')
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/SignatureAlgorithm.java`
#### Snippet
```java
    public static SignatureAlgorithm getAlgorithm(String algo) {
        if (algo == null) {
            return null;
        }
        if (AlgorithmUtils.NONE_TEXT_ALGO.equals(algo)) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonEncryptionEntry.java`
#### Snippet
```java
    }
    public byte[] getEncryptedKey() {
        return encodedEncryptedKey == null ? null : JoseUtils.decode(encodedEncryptedKey);
    }
    public String toJson() {
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
            return authPolicy.getUserName();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
        }
        if (principals.length() == 0) {
            return null;
        }
        return principals.toString();
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
    private static String safeGet(Message message, String key) {
        if (message == null || !message.containsKey(key)) {
            return null;
        }
        Object value = message.get(key);
```

### ReturnNull
Return of `null`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
        }
        Object value = message.get(key);
        return (value instanceof String) ? value.toString() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AesCbcContentEncryptionAlgorithm.java`
#### Snippet
```java
    @Override
    public byte[] getAdditionalAuthenticationData(String headersJson, byte[] aad) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/spring-boot/autoconfigure/src/main/java/org/apache/cxf/spring/boot/autoconfigure/micrometer/provider/SpringBasedTimedAnnotationProvider.java`
#### Snippet
```java
            }
            
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/WrappedKeyDecryptionAlgorithm.java`
#### Snippet
```java
    }
    protected AlgorithmParameterSpec getAlgorithmParameterSpec(JweDecryptionInput jweDecryptionInput) {
        return null;
    }
    protected byte[] getEncryptedContentEncryptionKey(JweDecryptionInput jweDecryptionInput) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweHeaders.java`
#### Snippet
```java
    public ContentAlgorithm getContentEncryptionAlgorithm() {
        Object prop = getHeader(JoseConstants.JWE_HEADER_CONTENT_ENC_ALGORITHM);
        return prop == null ? null : ContentAlgorithm.getAlgorithm(prop.toString());
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweHeaders.java`
#### Snippet
```java
    public KeyAlgorithm getKeyEncryptionAlgorithm() {
        String algo = super.getAlgorithm();
        return algo == null ? null : KeyAlgorithm.getAlgorithm(algo);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/EcdhHelper.java`
#### Snippet
```java
    }
    private byte[] toBytes(String str) {
        return str == null ? null : StringUtils.toBytesUTF8(str);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractWrapKeyEncryptionAlgorithm.java`
#### Snippet
```java
    }
    protected AlgorithmParameterSpec getAlgorithmParameterSpec(JweHeaders headers) {
        return null;
    }
    protected String checkAlgorithm(String algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractJweEncryption.java`
#### Snippet
```java

    protected AuthenticationTagProducer getAuthenticationTagProducer(JweEncryptionInternal state) {
        return null;
    }
    protected SecretKey createCekSecretKey(JweEncryptionInternal state) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractJweEncryption.java`
#### Snippet
```java
    public KeyAlgorithm getKeyAlgorithm() {
        KeyAlgorithm keyAlgo = getKeyEncryptionAlgo().getAlgorithm();
        return keyAlgo != null ? keyAlgo : null;
    }
    @Override
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/KeyOperation.java`
#### Snippet
```java
    public static KeyOperation getKeyOperation(String oper) {
        if (oper == null) {
            return null;
        }
        return valueOf(oper.toUpperCase());
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AesCbcHmacJweDecryption.java`
#### Snippet
```java
        @Override
        public byte[] getAdditionalAuthenticationData(String headersJson, byte[] aad) {
            return null;
        }
        @Override
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/KeyType.java`
#### Snippet
```java
    public static KeyType getKeyType(String type) {
        if (type == null) {
            return null;
        } else if (JsonWebKey.KEY_TYPE_OCTET.equals(type)) {
            return OCTET;
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/PublicKeyUse.java`
#### Snippet
```java
    public static PublicKeyUse getPublicKeyUse(String use) {
        if (use == null) {
            return null;
        }
        if (JsonWebKey.PUBLIC_KEY_USE_SIGN.equals(use)) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
            return keys;
        }
        return null;
    }
    public final void setKey(JsonWebKey key) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
        KeyType kt = KeyType.getKeyType(keyType);
        if (kt == null) {
            return null;
        }
        return getKeyTypeMap().get(kt);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKey.java`
#### Snippet
```java
        List<Object> ops = CastUtils.cast((List<?>)getProperty(KEY_OPERATIONS));
        if (ops == null) {
            return null;
        }
        List<KeyOperation> keyOps = new ArrayList<>(ops.size());
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKey.java`
#### Snippet
```java
    public PublicKeyUse getPublicKeyUse() {
        Object prop = getProperty(PUBLIC_KEY_USE);
        return prop == null ? null : PublicKeyUse.getPublicKeyUse(prop.toString());
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKey.java`
#### Snippet
```java
    public KeyType getKeyType() {
        Object prop = getProperty(KEY_TYPE);
        return prop == null ? null : KeyType.getKeyType(prop.toString());
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
            return JoseUtils.decode(value);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
    public String getIvText() {
        if (iv == null) {
            return null;
        }
        return new String(iv, StandardCharsets.UTF_8);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
    public String getCipherText() {
        if (cipherBytes == null) {
            return null;
        }
        return new String(cipherBytes, StandardCharsets.UTF_8);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
    public String getAadText() {
        if (aad == null) {
            return null;
        }
        return new String(aad, StandardCharsets.UTF_8);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonConsumer.java`
#### Snippet
```java
    public String getAuthTagText() {
        if (authTag == null) {
            return null;
        }
        return new String(authTag, StandardCharsets.UTF_8);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
    private String doGetJwsJsonSignedDocument(boolean detached) {
        if (signatures.isEmpty()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsHeaders.java`
#### Snippet
```java
    public SignatureAlgorithm getSignatureAlgorithm() {
        String algo = super.getAlgorithm();
        return algo == null ? null : SignatureAlgorithm.getAlgorithm(algo);
    }
    public void setPayloadEncodingStatus(Boolean status) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return new AesGcmWrapKeyDecryptionAlgorithm(key, algo);
        }
        return null;
    }
    public static ContentEncryptionProvider getContentEncryptionProvider(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return JwkUtils.toSecretKey(jwk);
        }
        return null;
    }
    private static ContentAlgorithm getContentAlgo(String algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return new AesGcmContentDecryptionAlgorithm(algorithm);
        }
        return null;
    }
    public static SecretKey getContentDecryptionSecretKey(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return new AesGcmWrapKeyEncryptionAlgorithm(key, algo);
        }
        return null;
    }
    public static KeyDecryptionProvider getKeyDecryptionProvider(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        }

        return null;
    }
    public static KeyDecryptionProvider getSecretKeyDecryptionProvider(SecretKey key, KeyAlgorithm algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return new AesCbcContentEncryptionAlgorithm(key, null, algorithm);
        }
        return null;
    }
    public static ContentEncryptionProvider getContentEncryptionProvider(ContentAlgorithm algorithm) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        Properties props = loadEncryptionOutProperties(required);
        if (props == null) {
            return null;
        }
        return loadEncryptionProvider(props, headers);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        Properties props = loadEncryptionInProperties(required);
        if (props == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                                                  JoseConstants.RSSEC_ENCRYPTION_KEY_ALGORITHM,
                                                  defaultAlgo == null ? null : defaultAlgo.getJwaName());
        return algo == null ? null : KeyAlgorithm.getAlgorithm(algo);
    }
    private static KeyAlgorithm getDefaultKeyAlgorithm(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return getContentEncryptionProvider(JwkUtils.toSecretKey(jwk), ctAlgo);
        }
        return null;
    }
    public static ContentEncryptionProvider getContentEncryptionProvider(SecretKey key,
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return KeyAlgorithm.ECDH_ES_A128KW;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            return KeyAlgorithm.ECDH_ES_A128KW;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
        }

        return null;
    }
    private static KeyAlgorithm getDefaultPublicKeyAlgorithm(PublicKey key) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        Properties props = loadSignatureOutProperties(required);
        if (props == null) {
            return null;
        }
        JwsSignatureProvider theSigProvider = loadSignatureProvider(props, headers);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            return SignatureAlgorithm.ES256;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            return SignatureAlgorithm.ES256;
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            return new HmacJwsSignatureVerifier(key, algo);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        }

        return null;
    }
    public static JwsSignatureVerifier getPublicKeySignatureVerifier(PublicKey key, SignatureAlgorithm algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            return new HmacJwsSignatureProvider(key, algo);
        }
        return null;
    }
    public static JwsSignatureVerifier getSignatureVerifier(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        }

        return null;
    }
    public static JwsSignatureVerifier getHmacSignatureVerifier(String encodedKey, SignatureAlgorithm algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        }

        return null;
    }
    public static JwsSignatureProvider getHmacSignatureProvider(String encodedKey, SignatureAlgorithm algo) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseType.java`
#### Snippet
```java
    public static JoseType getType(String type) {
        if (type == null) {
            return null;
        } else if (JoseConstants.TYPE_JOSE_JSON.equals(type)) {
            return JOSE_JSON;
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JwkUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
    public static RSAPublicKey toRSAPublicKey(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JwkUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JwkUtils.java`
#### Snippet
```java
            return (RSAPublicKey)chain.get(0).getPublicKey();
        }
        return null;
    }
    public static List<X509Certificate> toX509CertificateChain(JsonWebKey jwk) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JwtClaims.java`
#### Snippet
```java
            return audiences.get(0);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
    public static InputStream getResourceStream(String loc, Bus bus) throws IOException {
        URL url = getResourceURL(loc, bus);
        return url == null ? null : url.openStream();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
    public static URL getResourceURL(String loc, Bus bus) throws IOException {
        if (loc == null) {
            return null;
        }
        URL url;
```

### ReturnNull
Return of `null`
in `rt/management/src/main/java/org/apache/cxf/tracing/AbstractTracingProvider.java`
#### Snippet
```java
    private static String safeGet(Message message, String key) {
        if (!message.containsKey(key)) {
            return null;
        }
        Object value = message.get(key);
```

### ReturnNull
Return of `null`
in `rt/management/src/main/java/org/apache/cxf/tracing/AbstractTracingProvider.java`
#### Snippet
```java
        }
        Object value = message.get(key);
        return (value instanceof String) ? value.toString() : null;
    }
    
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    public JoseType getType() {
        Object prop = getHeader(JoseConstants.HEADER_TYPE);
        return prop == null ? null : JoseType.getType(prop.toString());
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseHeaders.java`
#### Snippet
```java
    public String getAlgorithm() {
        Object prop = getHeader(JoseConstants.HEADER_ALGORITHM);
        return prop == null ? null : prop.toString();
    }

```

### ReturnNull
Return of `null`
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java

        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
        if (mr == null) {
            // the class is not need to expose to jmx
            return null;
        }
        // Clazz get all the method which should be managemed
```

### ReturnNull
Return of `null`
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
            return methodName.substring(2);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingClientInterceptor.java`
#### Snippet
```java

    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingInterceptor.java`
#### Snippet
```java
    @Override
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisXMLStreamDataReader.java`
#### Snippet
```java
        if (elReader.isXsiNil()) {
            elReader.readToEnd();
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
            return certs;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
    public static X509Certificate[] toX509CertificateChainArray(List<String> base64EncodedChain) {
        List<X509Certificate> chain = toX509CertificateChain(base64EncodedChain);
        return chain == null ? null : chain.toArray(new X509Certificate[]{});
    }
    public static String getKeyAlgorithm(Message m, Properties props, String propName, String defaultAlg) {
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        KeyStore ks = loadPersistKeyStore(m, props);
        if (ks == null || thumbprint == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
                throw new JoseException();
            }
            return null;
        }
        final Properties props;
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementReader.java`
#### Snippet
```java

        if (!hasChildren) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementReader.java`
#### Snippet
```java
                if (isXsiNil()) {
                    readToEnd();
                    return null;
                }
                value = root.getElementText();
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            LOG.warning("xsi:type reading disabled, and no type available for "
                     + xsr.getName());
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            LOG.warning("xsi:type=\"" + schemaTypeName
                     + "\" was specified, but no corresponding AegisType was registered; no default.");
            return null;
        }
        LOG.warning("xsi:type was not specified for top-level element " + xsr.getName());
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
        }
        LOG.warning("xsi:type was not specified for top-level element " + xsr.getName());
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            LOG.warning("xsi:type=\"" + overrideName
                     + "\" was specified, but no corresponding AegisType was registered; no default.");
            return null;
        }
        if (baseType == null) {
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            throw new RuntimeException("Attempt to derive Class from reflection Type " + type);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
            return instance.getClass();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/SimpleParameterizedType.java`
#### Snippet
```java
    public Type getOwnerType() {
        // no nested types.
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AegisType.java`
#### Snippet
```java
     */
    public Set<AegisType> getDependencies() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/StreamDataSource.java`
#### Snippet
```java
    /** {@inheritDoc}*/
    public String getName() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/StreamDataSource.java`
#### Snippet
```java
    /** {@inheritDoc}*/
    public OutputStream getOutputStream() throws IOException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AbstractTypeCreator.java`
#### Snippet
```java

    public AegisType createEnumType(TypeClassInfo info) {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AttachmentUtil.java`
#### Snippet
```java

        if (attachments == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AttachmentUtil.java`
#### Snippet
```java
            return new AttachmentImpl(id, new DataHandler(source));
        } catch (MalformedURLException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
            return typeMapping.getType(clazz);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
            return typeMapping.getType(schemaTypeName);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BooleanType.java`
#### Snippet
```java
        if (reader.isXsiNil()) {
            reader.readToEnd();
            return null;
        }
        return Boolean.valueOf(reader.getValueAsBoolean());
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimeType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CalendarType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimezoneLessDateType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BigDecimalType.java`
#### Snippet
```java
        final String value = reader.getValue();

        return null == value ? null : new BigDecimal(value.trim());
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/SqlDateType.java`
#### Snippet
```java
        Date date = (Date)super.readObject(reader, context);
        if (date == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateTimeType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/URIType.java`
#### Snippet
```java
        final String value = reader.getValue();

        return null == value ? null : URI.create(value.trim());
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimestampType.java`
#### Snippet
```java

        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/XMLBeanTypeInfo.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BigIntegerType.java`
#### Snippet
```java
        final String value = reader.getValue();

        return null == value ? null : new BigInteger(value.trim());
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ObjectType.java`
#### Snippet
```java

        if (null == superclass || Object.class.equals(superclass)) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ObjectType.java`
#### Snippet
```java
            }

            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
    protected QName createQName(Element e, String value) {
        if (value == null || value.length() == 0) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        String spec = DOMUtils.getAttributeValueEmptyNull(parameter, "valueType");
        if (spec == null) {
            return null;
        }
        return getGenericParameterFromSpec(mapping, spec);
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        String componentSpec = DOMUtils.getAttributeValueEmptyNull(parameter, "componentType");
        if (componentSpec == null) {
            return null;
        }
        return getGenericParameterFromSpec(mapping, componentSpec);
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
    protected Document getDocument(Class<?> clazz) {
        if (clazz == null) {
            return null;
        }
        if (documents.containsKey(clazz.getName())) {
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            documents.put(clazz.getName(), null);
            LOG.finest("Mapping file : " + path + " not found.");
            return null;
        }
        LOG.finest("Found mapping file : " + path);
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            LOG.log(Level.SEVERE, "Error loading file " + path, e);
            documents.put(clazz.getName(), null);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            return createType((Class<?>)cType);
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java

        if (clazz == null) {
            return null;
        }
        Document doc = getDocument(clazz);
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        Document doc = getDocument(clazz);
        if (doc == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        // no name found, so no matches
        if (nodes.isEmpty()) {
            return null;
        }
        // if the method has no params, then more than one mapping is pointless
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        String spec = DOMUtils.getAttributeValueEmptyNull(parameter, "keyType");
        if (spec == null) {
            return null;
        }
        return getGenericParameterFromSpec(mapping, spec);
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            LOG.log(Level.SEVERE, "Error parsing Aegis file.", e); // can't happen due to
                                                        // above.
            return null;
        }
        return doc;
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
        writeProperty(attrName, args[0]);

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/EnumType.java`
#### Snippet
```java
    private Enum<?> matchValue(String value) {
        if (value == null) {
            return null;
        }
        @SuppressWarnings("rawtypes")
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/EnumType.java`
#### Snippet
```java
    private Object getValue(Object constant) {
        if (!(constant instanceof Enum<?>)) {
            return null;
        }
        Enum<?> enumConstant = (Enum<?>)constant;
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotatedTypeInfo.java`
#### Snippet
```java
            return getTypeClass().getDeclaredField(desc.getName());
        } catch (NoSuchFieldException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapEncodingUtil.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java

            if (desc == null) {
                return null;
            }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
    public static Boolean isNillable(Annotation[] anns) {
        if (anns == null) {
            return null;
        }
        return (Boolean)getAnnotationValue("nillable", // NOPMD
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            XmlEnumValue constantValueAnnotation = constantField.getAnnotation(XmlEnumValue.class);
            if (constantValueAnnotation == null) {
                return null;
            }
            return constantValueAnnotation.value();
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            return constantValueAnnotation.value();
        } catch (NoSuchFieldException e) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            || method.getParameterAnnotations().length <= index
            || method.getParameterAnnotations()[index] == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            }
        }
        return null;
    }
    @SafeVarargs
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
        // some poor class loader implementations may end not define Package elements
        if (element == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            || method.getParameterAnnotations().length <= index
            || method.getParameterAnnotations()[index] == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
    public static Integer getMinOccurs(Annotation[] anns) {
        if (anns == null) {
            return null;
        }
        String minOccurs = (String) getAnnotationValue("minOccurs",
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            return 1;
        }
        return null;
    }
    public boolean isExtensibleElements(AnnotatedElement element, boolean defaultValue) {
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/StructType.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
    public static QName createQName(NamespaceContext nc, String value) {
        if (value == null) {
            return null;
        }
        int index = value.indexOf(':');
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
    public static QName createQName(Element e, String value, String defaultNamespace) {
        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
        String value = reader.getElementText();
        if (value == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            return superType;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/W3CNamespaceContext.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/W3CNamespaceContext.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/date/XsDateTimeFormat.java`
#### Snippet
```java
            pParsePosition.setErrorIndex(offset);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMStreamWriter.java`
#### Snippet
```java

    public Object getProperty(String prop) throws IllegalArgumentException {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMNamespaceContext.java`
#### Snippet
```java
            return rawGetPrefix(element.getParentElement(), namespaceURI);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMNamespaceContext.java`
#### Snippet
```java
        Namespace namespace = element.getNamespace(prefix);
        if (namespace == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
    public static Class<? extends AegisType> castToAegisTypeClass(Class<?> c) {
        if (c == null) {
            return null;
        }
        if (AegisType.class.isAssignableFrom(c)) {
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
            return paramType; // take our chances.
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
    private Type findMapGenericTypes(Type cls, Map<String, Type> pm, boolean key) {
        if (cls == null) {
            return null;
        }
        if (cls instanceof ParameterizedType) {
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMStreamReader.java`
#### Snippet
```java

    public String getEncoding() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMStreamReader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/XMLStreamDataWriter.java`
#### Snippet
```java
    public Object getProperty(String key) {
        if (properties == null) {
            return null;
        }
        return properties.get(key);
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpClientPipelineFactory.java`
#### Snippet
```java
            return new SslHandler(sslEngine);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/ElementDataWriter.java`
#### Snippet
```java
    public Object getProperty(String key) {
        if (properties == null) {
            return null;
        }
        return properties.get(key);
```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java

        if (wsdl == null || !wsdl.exists()) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java
        }
        if (idx == -1) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java
            return option;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
    public File getWsdlFile(File baseDir) {
        if (wsdl == null) {
            return null;
        }
        File file = null;
```

### ReturnNull
Return of `null`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
        protected HttpsURLConnectionInfo getHttpsURLConnectionInfo() throws IOException {
            if ("http".equals(outMessage.get("http.scheme"))) {
                return null;
            }
            connect(true);
```

### ReturnNull
Return of `null`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/handlers/ReferenceParametersAddingHandler.java`
#### Snippet
```java
    @Override
    public Set<QName> getHeaders() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/handlers/SubscriptionReferenceParsingHandler.java`
#### Snippet
```java
    @Override
    public Set<QName> getHeaders() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/database/SubscriptionTicket.java`
#### Snippet
```java
            return (String)this.getFilter().getContent().get(0);
        } catch (NullPointerException ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/database/SubscriptionTicket.java`
#### Snippet
```java
            return this.getEndTo().getAddress().getValue();
        } catch (NullPointerException ex) {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/database/SubscriptionDatabaseImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
        // dependency as long as this null check is here.
        if (md == null) {
            return null;
        }
        SimpleMethodDispatcher smd = (SimpleMethodDispatcher)md;
```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
        Method m = getMethod(s, op);
        if (m == null) {
            return null;
        }

```

### ReturnNull
Return of `null`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
    public File getWsdlFile(GenericWsdlOption option, File baseDir) {
        if (option.getUri() == null) {
            return null;
        }
        File file = null;
```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProperties.java`
#### Snippet
```java

    static Integer getIntValue(Object o) {
        return o instanceof Integer ? (Integer)o : o instanceof String ? Integer.valueOf(o.toString()) : null;
    }
}
```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/Option.java`
#### Snippet
```java
    public URI[] getDependencyURIs(URI baseURI) {
        if (dependencies == null) {
            return null;
        }
        URI[] uris = new URI[dependencies.length];
```

### ReturnNull
Return of `null`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/Option.java`
#### Snippet
```java

    public String getWsdlLocation() {
        return isSetWsdlLocation() ? wsdlLocation : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientConfiguration.java`
#### Snippet
```java
    public HTTPConduit getHttpConduit() {
        Conduit conduit = getConduit();
        return conduit instanceof HTTPConduit ? (HTTPConduit)conduit : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientConfiguration.java`
#### Snippet
```java

    public Endpoint getEndpoint() {
        return conduitSelector == null ? null : conduitSelector.getEndpoint();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JaxrsClientCallback.java`
#### Snippet
```java
                return callback.getResponseContext();
            } catch (Exception ex) {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactoryBean.java`
#### Snippet
```java
                ? initialState.newState(URI.create(getAddress()), headers, null, getProperties()) : initialState;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactoryBean.java`
#### Snippet
```java
                        return (ParamConverter<T>) new UrlEncodingParamConverter(encodeClientParametersList);
                    }
                    return null;
                }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientRequestContextImpl.java`
#### Snippet
```java
    public Class<?> getEntityClass() {
        Object entity = getEntity();
        return entity == null ? null : entity.getClass();
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientRequestContextImpl.java`
#### Snippet
```java
        String requestURI = (String)m.get(Message.REQUEST_URI);
        if (requestURI  == null) {
            return null;
        }
        if (requestURI.startsWith("/")) {
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientRequestContextImpl.java`
#### Snippet
```java
    public MediaType getMediaType() {
        if (!hasEntity()) {
            return null;
        }
        Object mt = HttpUtils.getModifiableHeaders(m).getFirst(HttpHeaders.CONTENT_TYPE);
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientRequestContextImpl.java`
#### Snippet
```java
        MessageContentsList objs = MessageContentsList.getContentsList(m);
        if (objs == null || objs.isEmpty()) {
            return null;
        }
        return objs.get(0);
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
    }
    private static Integer getIntValue(Object o) {
        return o instanceof Integer ? (Integer)o : o instanceof String ? Integer.valueOf(o.toString()) : null;
    }
    private static Boolean getBooleanValue(Object o) {
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
    }
    private static Boolean getBooleanValue(Object o) {
        return o instanceof Boolean ? (Boolean)o : o instanceof String ? Boolean.valueOf(o.toString()) : null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
            }
        } else {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
        return o instanceof Long ? (Long)o
            : o instanceof String ? Long.valueOf(o.toString())
            : o instanceof Integer ? ((Integer)o).longValue() : null;
    }
    private static Integer getIntValue(Object o) {
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spring/JaxRsProxyClientConfiguration.java`
#### Snippet
```java

    protected Class<?> getServiceClass() {
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            return templatesMap;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
    protected String convertParamValue(Object pValue, Class<?> pClass, Annotation[] anns) {
        if (pValue == null && pClass == null) {
            return null;
        }
        ProviderFactory pf = ClientProviderFactory.getInstance(cfg.getEndpoint());
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
    protected static ParameterizedType findCallbackType(Class<?> cls) {
        if (cls == null || cls == Object.class) {
            return null;
        }
        for (Type c2 : cls.getGenericInterfaces()) {
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            exchange.put(Exception.class, new Exception(t));
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java

        List<?> result = exchange.get(List.class);
        return result != null ? result.toArray() : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        int status = r.getStatus();
        if ((status < 200 || status == 204) && r.getLength() <= 0 || (status >= 300 && status != 304)) {
            return null;
        }
        return ((ResponseImpl)r).doReadEntity(cls, type, anns);
```

### ReturnNull
Return of `null`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonConsumer.java`
#### Snippet
```java
            return producer.getJwsJsonSignedDocument();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            return (Client)object;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
    public static WebClient fromClientObject(Object object) {
        Client client = client(object);
        return client == null ? null : fromClient(client, false);
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        AbstractClient newClient = toAbstractClient(client);
        if (newClient == null) {
            return null;
        }
        return newClient.getState();
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        Method aMethod = ori.getAnnotatedMethod();
        return aMethod != null ? AnnotationUtils.getAnnotation(
            aMethod.getParameterAnnotations()[index], Multipart.class) : null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        List<String> headers = allHeaders.get(HttpHeaders.ACCEPT);
        if (headers == null || headers.isEmpty()) {
            return null;
        }
        return headers.stream().
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            return callback;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                    } catch (Exception ex) {
                                        // ignore
                                        return null;
                                    }
                                }).
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            checkResponse(method, r, outMessage);
            if (method.getReturnType() == Void.class || method.getReturnType() == Void.TYPE) {
                return null;
            }
            if (method.getReturnType() == Response.class
```

### ReturnNull
Return of `null`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        doRunInterceptorChain(outMessage);

        return null;
    }

```

## RuleId[ruleID=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `newPolicy` is redundant
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    public AuthorizationPolicy getEffectiveAuthPolicy(Message message) {
        AuthorizationPolicy authPolicy = getAuthorization();
        AuthorizationPolicy newPolicy = message.get(AuthorizationPolicy.class);
        AuthorizationPolicy effectivePolicy = newPolicy;
        if (effectivePolicy == null) {
```

### UnnecessaryLocalVariable
Local variable `instance` is redundant
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIUtils.java`
#### Snippet
```java
        Bean<?> bean = beanManager.getBeans(clazz).iterator().next();
        CreationalContext<?> ctx = beanManager.createCreationalContext(bean);
        Instance<T> instance = new Instance<>((T) beanManager.getReference(bean, clazz, ctx), 
            beanManager.isNormalScope(bean.getScope()) ? () -> { } : ctx::release);
        return instance;
```

### UnnecessaryLocalVariable
Local variable `keyNameSecurityToken` is redundant
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
        throws XMLSecurityException {
        X509Certificate[] certs;
        KeyNameSecurityToken keyNameSecurityToken = token;
        String keyName = keyNameSecurityToken.getKeyName();
        String alias = null;
```

### UnnecessaryLocalVariable
Local variable `signableObject` is redundant
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlPostBindingFilter.java`
#### Snippet
```java
        }

        SignableSAMLObject signableObject = authnRequest;
        signableObject.setSignature(signature);
        signableObject.releaseDOM();
```

### UnnecessaryLocalVariable
Local variable `samlResponseDecoded` is redundant
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractRequestAssertionConsumerHandler.java`
#### Snippet
```java
        }

        String samlResponseDecoded = samlResponse;
        /*
        // URL Decoding only applies for the re-direct binding
```

### UnnecessaryLocalVariable
Local variable `booleanQuery` is redundant
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
            BooleanQuery.Builder booleanBuilder = new BooleanQuery.Builder();
            booleanBuilder.add(createEqualsQuery(clazz, name, value), BooleanClause.Occur.MUST_NOT);
            BooleanQuery booleanQuery = booleanBuilder.build();
            query = booleanQuery;
            break;
```

### UnnecessaryLocalVariable
Local variable `methodName` is redundant
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    private void invokeSetterActivationNSMethod(Object target, Object value) {
        Class<?> clazz = target.getClass();
        String methodName = ACTIVATION_NAMESPACES_SETTER_METHOD_NAME;
        while (clazz != Object.class) {
            Method[] methods = clazz.getMethods();
```

### UnnecessaryLocalVariable
Local variable `element` is redundant
in `core/src/main/java/org/apache/cxf/common/xmlschema/SchemaCollection.java`
#### Snippet
```java

    private void addElementCrossImportsElement(XmlSchema schema, XmlSchemaElement item) {
        XmlSchemaElement element = item;
        XmlSchemaUtils.addImportIfNeeded(schema, element.getRef().getTargetQName());
        XmlSchemaUtils.addImportIfNeeded(schema, element.getSchemaTypeName());
```

### UnnecessaryLocalVariable
Local variable `ex` is redundant
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        } catch (PrivilegedActionException e2) {
            if (e2.getException() instanceof JAXBException) {
                JAXBException ex = (JAXBException) e2.getException();
                throw ex;
            } else {
```

### UnnecessaryLocalVariable
Local variable `zipOutput` is redundant
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java

            // gzip the result
            GZIPOutputStream zipOutput = new GZIPOutputStream(wrappedStream);
            wrappedStream = zipOutput;
        }
```

### UnnecessaryLocalVariable
Local variable `obj` is redundant
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
        try {
            Field field = cls.getField("TYPE");
            Object obj = cls;
            Object type = field.get(obj);
            if (type instanceof Class) {
```

### UnnecessaryLocalVariable
Local variable `cls` is redundant
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            throw new IllegalArgumentException("resource is null");
        }
        Class<?> cls = resource;
        Path ann = cls.getAnnotation(Path.class);
        if (ann == null) {
```

### UnnecessaryLocalVariable
Local variable `importNode` is redundant
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                                                           "import");
        for (Element elem : impElemList) {
            Node importNode = elem;
            ns.add(importNode);
        }
```

### UnnecessaryLocalVariable
Local variable `future` is redundant
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionOutputStream.java`
#### Snippet
```java
    private synchronized void write(IoBuffer buf) throws IOException {
        checkClosed();
        WriteFuture future = session.write(buf);
        lastWriteFuture = future;
    }
```

### UnnecessaryLocalVariable
Local variable `arrayCls` is redundant
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
                    && pt.getActualTypeArguments()[0] instanceof Class) {

                    Class<? extends Object> arrayCls =
                        Array.newInstance((Class<?>) pt.getActualTypeArguments()[0], 0).getClass();
                    clazz = arrayCls;
```

### UnnecessaryLocalVariable
Local variable `valueClass` is redundant
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            Method getMethod = null;
            Method setMethod = null;
            Class<?> valueClass = wrapperType;

            try {
```

### UnnecessaryLocalVariable
Local variable `sock` is redundant
in `testutils/src/main/java/org/apache/cxf/testutil/common/TestUtil.java`
#### Snippet
```java
                //will try again 100 ports up.   At this point, 100 ports is WAY
                //more than enough.  We can adjust later if needed.
                ServerSocket sock = new ServerSocket(pn);
                lock = sock;
                portNum = pn + 1;
```

### UnnecessaryLocalVariable
Local variable `c` is redundant
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaFixedAnyImplGenerator.java`
#### Snippet
```java

        byte[] b = cw.toByteArray();
        Class<?> c = loadClass("org.apache.cxf.binding.corba.utils.FixedAnyImpl",
                CorbaFixedAnyImplGenerator.class, b);
        return c;
```

### UnnecessaryLocalVariable
Local variable `re` is redundant
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/CXFInvocationHandlerBase.java`
#### Snippet
```java
        } else {
            //get the exception when call the method
            RuntimeException re = new RuntimeException("Unexpected exception from method " + targetMethod,
                                                      targetException);
            ret = re;
```

### UnnecessaryLocalVariable
Local variable `requestedDuration` is redundant
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
        if (DurationAndDateUtil.isDuration(requestedExpiration.getValue())) {
            // duration was requested
            javax.xml.datatype.Duration requestedDuration = DurationAndDateUtil
                    .parseDuration(requestedExpiration.getValue());
            javax.xml.datatype.Duration grantedDuration = requestedDuration;
```

### UnnecessaryLocalVariable
Local variable `grantedDuration` is redundant
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
            javax.xml.datatype.Duration requestedDuration = DurationAndDateUtil
                    .parseDuration(requestedExpiration.getValue());
            javax.xml.datatype.Duration grantedDuration = requestedDuration;
            LOG.info("[subscription=" + ticket.getUuid() + "] Granted renewal duration: " + grantedDuration.toString());
            grantedExpires = getDatabase().findById(uuid)
```

### UnnecessaryLocalVariable
Local variable `subHeaders` is redundant
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            }

            MultivaluedMap<String, String> subHeaders = paramHeaders;
            if (inheritHeaders) {
                subHeaders.putAll(headers);
```

## RuleId[ruleID=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsSubscription.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(2000); // check if should terminate every 2 sec
                } catch (InterruptedException e) {
                    LOGGER.log(Level.WARNING, "TerminationThread sleep interrupted", e);
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
                        store.destroyInactiveSessions();
                    }
                    Thread.sleep(5000);

                } catch (InterruptedException e) {
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                while (client.isRunning()) {
                    synchronized (this) {
                        sleep(selectInterval);
                        // make sure pending leases fail in a timely manner,
                        // not just when a connection becomes available
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
                while (client.getStatus() == IOReactorStatus.ACTIVE) {
                    synchronized (this) {
                        sleep(selectInterval);

                        if (connectionTTL == 0
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSDestination.java`
#### Snippet
```java
                }
                try {
                    Thread.sleep(jmsConfig.getRetryInterval());
                } catch (InterruptedException e2) {
                    shutdown = true;
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/PollingMessageListenerContainer.java`
#### Snippet
```java
                    handleException(e);
                    try {
                        Thread.sleep(1000);
                    } catch (InterruptedException e1) {
                        // Ignore
```

### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
                        initializeServantsFromProperties(loadProperties(propsFile.toURI().toURL()));
                    }
                    Thread.sleep(pollIntervalSeconds * 1000L);
                } catch (Exception e) {
                    LOG.info("MonitorThread: failed to initialiseServantsFromProperties "
```

## RuleId[ruleID=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Long.compare'
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
            long other = ((DelayedTaskWrapper)delayed).trigger;
            int returnValue;
            if (this.trigger < other) {
                returnValue = -1;
            } else if (this.trigger > other) {
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
        int l2 = t2.getLiteralChars().length();
        // descending order
        int result = l1 < l2 ? 1 : l1 > l2 ? -1 : 0;
        if (result == 0) {
            int g1 = t1.getVariables().size();
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
            int g2 = t2.getVariables().size();
            // descending order
            result = g1 < g2 ? 1 : g1 > g2 ? -1 : 0;
            if (result == 0) {
                int gCustom1 = t1.getCustomVariables().size();
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
                int gCustom1 = t1.getCustomVariables().size();
                int gCustom2 = t2.getCustomVariables().size();
                result = gCustom1 < gCustom2 ? 1 : gCustom1 > gCustom2 ? -1 : 0;
                if (result == 0) {
                    result = t1.getPatternValue().compareTo(t2.getPatternValue());
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
                int p1 = c1.getParameterTypes().length;
                int p2 = c2.getParameterTypes().length;
                return p1 > p2 ? -1 : p1 < p2 ? 1 : 0;
            }

```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }
        }
        return size1 == size2 ? 0 : size1 < size2 ? -1 : 1;
    }

```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }
        }
        return size1 == size2 ? 0 : size1 < size2 ? -1 : 1;
    }
    public static int compareMediaTypes(MediaType mt1, MediaType mt2) {
```

### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }
        }
        return size1 == size2 ? 0 : size1 < size2 ? -1 : 1;
    }

```

## RuleId[ruleID=WaitNotInLoop]
### WaitNotInLoop
Call to `wait()` is not in loop
in `core/src/main/java/org/apache/cxf/endpoint/ClientCallback.java`
#### Snippet
```java
        synchronized (this) {
            if (!delegate.isDone()) {
                wait();
            }
        }
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `core/src/main/java/org/apache/cxf/interceptor/OneWayProcessorInterceptor.java`
#### Snippet
```java
                        //Mostly just to make an attempt at keeping the ordering of the
                        //messages coming in from a client.  Not guaranteed though.
                        lock.wait(20L);
                    }
                } catch (RejectedExecutionException e) {
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                if (session == null) {
                    try {
                        sessionLock.wait(csPolicy.getConnectionTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                if (session == null) {
                    try {
                        sessionLock.wait(csPolicy.getConnectionTimeout());
                    } catch (InterruptedException e) {
                        throw new IOException(e);
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
                } else {
                    try {
                        exchange.wait(jmsConfig.getReceiveTimeout());
                    } catch (InterruptedException e) {
                        throw new JMSException("Interrupted while correlating " +  e.getMessage());
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionInputStream.java`
#### Snippet
```java
        if (exception == null && buf == null) {
            try {
                wait();
            } catch (InterruptedException e) {
                throw new IOException();
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
                        response = rr.getResponse();
                        if (response == null) {
                            rr.wait(timetowait);
                            response = rr.getResponse();
                        }
```

### WaitNotInLoop
Call to `wait()` is not in loop
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            if (getResponseCode(exchange) == null) {
                try {
                    exchange.wait(cfg.getSynchronousTimeout());
                } catch (InterruptedException ex) {
                    // ignore
```

## RuleId[ruleID=PointlessBooleanExpression]
### PointlessBooleanExpression
`hasComponents |= true` can be simplified to 'hasComponents=true'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            if (securityDefinitions != null && !securityDefinitions.isEmpty()) {
                securityDefinitions.forEach(components::addSecuritySchemes);
                hasComponents |= true;
            }

```

## RuleId[ruleID=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
            if (audienceRestrictions != null) {
                for (String audienceRestriction : audienceRestrictions.split(",")) {
                    audiences.add(audienceRestriction);
                }
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
                    names.add(s);
                    for (String s2 : ctx.getAliases(s)) {
                        names.add(s2);
                    }
                }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        Set<Class<?>> classes = new HashSet<>();
        for (Class<?> c : cls) {
            classes.add(c);
        }
        scanPackages(classes);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/cxf/common/util/ClasspathScanner.java`
#### Snippet
```java
        List<Class<? extends Annotation>> annotationList = new ArrayList<>(annotations.length);
        for (Class< ? extends Annotation > c : annotations) {
            annotationList.add(c);
        }
        return findClasses(basePackages, Collections.unmodifiableList(annotationList));
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `core/src/main/java/org/apache/cxf/common/util/ClasspathScanner.java`
#### Snippet
```java
        List<Class<? extends Annotation>> annotationList = new ArrayList<>(annotations.length);
        for (Class< ? extends Annotation > c : annotations) {
            annotationList.add(c);
        }
        return findClasses(parsePackages(basePackage),
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
            if (sl != null) {
                for (WSDataRef r : sl) {
                    signed.add(r);
                }
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
                if (sl != null) {
                    for (WSDataRef r : sl) {
                        encrypted.add(r);
                    }
                }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            if (supportingToken.getSignedParts() != null) {
                for (WSEncryptionPart signedPart : supportingToken.getSignedParts()) {
                    sigParts.add(signedPart);
                }
            }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfigFactory.java`
#### Snippet
```java
        }
        for (Map.Entry<String, String> ent : endpoint.getJndiParameters().entrySet()) {
            env.put(ent.getKey(), ent.getValue());
        }
        if (LOG.isLoggable(Level.FINE)) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyUtilities.java`
#### Snippet
```java
        ASSERTION_NAMES.addAll(RM10AssertionBuilder.KNOWN_ELEMENTS);
        for (QName qn : RM12AssertionBuilder.KNOWN_ELEMENTS) {
            ASSERTION_NAMES.add(qn);
        }
        ASSERTION_NAMES.add(RSPAssertionBuilder.CONFORMANT_QNAME);
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/AbstractIdentityCache.java`
#### Snippet
```java
    private void mergeMap(Map<String, String> to, Map<String, String> from) {
        for (Map.Entry<String, String> entry : from.entrySet()) {
            to.put(entry.getKey(), entry.getValue());
        }
        for (Map.Entry<String, String> entry : to.entrySet()) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/AbstractIdentityCache.java`
#### Snippet
```java
        }
        for (Map.Entry<String, String> entry : to.entrySet()) {
            from.put(entry.getKey(), entry.getValue());
        }
    }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
        Collection<PortType> ite = CastUtils.cast(def.getPortTypes().values());
        for (PortType portType : ite) {
            portTypes.add(portType);
        }
        return portTypes;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
            Collection<Part> parts = CastUtils.cast(output.getMessage().getParts().values());
            for (Part p : parts) {
                partsList.add(p);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/WSDLHelper.java`
#### Snippet
```java
            Collection<Part> parts = CastUtils.cast(input.getMessage().getParts().values());
            for (Part p : parts) {
                partsList.add(p);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
        if (null != serverArgs) {
            for (String s : serverArgs) {
                cmd.add(s);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaDSIServant.java`
#### Snippet
```java
            interfaces.add(bindType.getRepositoryID());
            for (Iterator<String> iter = bases.iterator(); iter.hasNext();) {
                interfaces.add(iter.next());
            }
        } catch (java.lang.Exception ex) {
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
        if (wsdlOptions != null) {
            for (WsdlOption wo : wsdlOptions) {
                effectiveWsdlOptions.add(wo);
            }
        }
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
        if (wsdlOptions != null) {
            for (WsdlOption wo : wsdlOptions) {
                effectiveWsdlOptions.add(wo);
            }
        }
```

## RuleId[ruleID=OverwrittenKey]
### OverwrittenKey
Duplicate Set element
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
    public SecurityTokenServiceProvider() throws Exception {
        Set<Class<?>> classes = new HashSet<>();
        classes.add(ObjectFactory.class);
        classes.add(org.apache.cxf.ws.security.sts.provider.model.wstrust14.ObjectFactory.class);

```

### OverwrittenKey
Duplicate Set element
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
        Set<Class<?>> classes = new HashSet<>();
        classes.add(ObjectFactory.class);
        classes.add(org.apache.cxf.ws.security.sts.provider.model.wstrust14.ObjectFactory.class);

        CachedContextAndSchemas cache =
```

### OverwrittenKey
Duplicate Set element
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/UsernameTokenValidator.java`
#### Snippet
```java
        try {
            Set<Class<?>> classes = new HashSet<>();
            classes.add(ObjectFactory.class);
            classes.add(org.apache.cxf.ws.security.sts.provider.model.wstrust14.ObjectFactory.class);

```

### OverwrittenKey
Duplicate Set element
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/UsernameTokenValidator.java`
#### Snippet
```java
            Set<Class<?>> classes = new HashSet<>();
            classes.add(ObjectFactory.class);
            classes.add(org.apache.cxf.ws.security.sts.provider.model.wstrust14.ObjectFactory.class);

            CachedContextAndSchemas cache =
```

## RuleId[ruleID=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `core/src/main/java/org/apache/cxf/helpers/NSDecl.java`
#### Snippet
```java
            return false;
        }
        return uri == ((NSDecl)obj).uri && prefix == ((NSDecl)obj).prefix; //NOPMD
    }

```

### StringEquality
String values are compared using `==`, not 'equals()'
in `core/src/main/java/org/apache/cxf/helpers/NSDecl.java`
#### Snippet
```java
            return false;
        }
        return uri == ((NSDecl)obj).uri && prefix == ((NSDecl)obj).prefix; //NOPMD
    }

```

## RuleId[ruleID=ConditionalBreakInInfiniteLoop]
### ConditionalBreakInInfiniteLoop
Conditional break inside loop
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
        boolean fileSeen = false;
        boolean msgPrinted = false;
        for (;;) {
            if (i == args.length) {
                break;
```

## RuleId[ruleID=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapUtils.java`
#### Snippet
```java
            public Object mapFromAttributes(Attributes attrs) throws NamingException {
                NamingEnumeration<? extends Attribute> attrEnum = attrs.getAll();
                while (attrEnum.hasMore()) {
                    return attrEnum.next().get();
                }
```

### LoopStatementsThatDontLoop
`while` statement does not loop
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
            Iterator<SchemaInfo> it = serviceInfo.getSchemas().iterator();
            while (it.hasNext()) {
                XmlSchema schema = it.next().getSchema();
                return isAttributeFormQualified(schema, uri);
```

### LoopStatementsThatDontLoop
`while` statement does not loop
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
            Iterator<SchemaInfo> it = serviceInfo.getSchemas().iterator();
            while (it.hasNext()) {
                XmlSchema schema = it.next().getSchema();
                return isElementFormQualified(schema, uri);
```

## RuleId[ruleID=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `a` of type 'X\[\]'
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
                newInstance(a.getClass().getComponentType(), tmp.length);
        }
        System.arraycopy(tmp, 0, a, 0, tmp.length);
        if (a.length > tmp.length) {
            a[tmp.length] = null;
```

### SuspiciousSystemArraycopy
`first` is not of an array type
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            Object mergedArray = Array.newInstance(InjectionUtils.getActualType(genericType),
                                                    firstLen + secondLen);
            System.arraycopy(first, 0, mergedArray, 0, firstLen);
            System.arraycopy(second, 0, mergedArray, firstLen, secondLen);
            return mergedArray;
```

### SuspiciousSystemArraycopy
`mergedArray` is not of an array type
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            Object mergedArray = Array.newInstance(InjectionUtils.getActualType(genericType),
                                                    firstLen + secondLen);
            System.arraycopy(first, 0, mergedArray, 0, firstLen);
            System.arraycopy(second, 0, mergedArray, firstLen, secondLen);
            return mergedArray;
```

### SuspiciousSystemArraycopy
`second` is not of an array type
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                                    firstLen + secondLen);
            System.arraycopy(first, 0, mergedArray, 0, firstLen);
            System.arraycopy(second, 0, mergedArray, firstLen, secondLen);
            return mergedArray;
        }
```

### SuspiciousSystemArraycopy
`mergedArray` is not of an array type
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                                                    firstLen + secondLen);
            System.arraycopy(first, 0, mergedArray, 0, firstLen);
            System.arraycopy(second, 0, mergedArray, firstLen, secondLen);
            return mergedArray;
        }
```

### SuspiciousSystemArraycopy
`data` is not of an array type
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            } else if (data instanceof byte[]) {
                entity = new byte[((byte[])data).length - pos];
                System.arraycopy(data, pos, entity, 0, ((byte[])entity).length);
            }
        }
```

### SuspiciousSystemArraycopy
`entity` is not of an array type
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            } else if (data instanceof byte[]) {
                entity = new byte[((byte[])data).length - pos];
                System.arraycopy(data, pos, entity, 0, ((byte[])entity).length);
            }
        }
```

## RuleId[ruleID=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `from()` declared in class 'java.util.Date' but referenced via subclass 'java.sql.Date'
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractSSOSpHandler.java`
#### Snippet
```java
            Instant expires = Instant.ofEpochMilli(System.currentTimeMillis() + stateTimeToLive);
            String cookieExpires =
                HttpUtils.getHttpDateFormat().format(Date.from(expires.atZone(ZoneOffset.UTC).toInstant()));
            contextCookie += ";Expires=" + cookieExpires;
        }
```

### StaticCallOnSubclass
Static method `init()` declared in class 'org.omg.CORBA.ORBSingleton' but referenced via subclass 'org.omg.CORBA.ORB'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java
    public TypeCode _type() {
        if (typeCode == null) {
            ORB orb = ORB.init();
            StructMember[] smBuf = new StructMember[2];
            TypeCode minortc = orb.get_primitive_tc(TCKind.tk_long);
```

## RuleId[ruleID=RedundantClassCall]
### RedundantClassCall
Redundant call to `cast()`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    public int getThreshold() {
        return Portable.class.cast(delegate).getThreshold();
    }

```

### RedundantClassCall
Redundant call to `cast()`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    public long getTimeout() {
        return Portable.class.cast(delegate).getTimeout();
    }

```

### RedundantClassCall
Redundant call to `cast()`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    public void setThreshold(int threshold) {
        Portable.class.cast(delegate).setThreshold(threshold);
    }

```

### RedundantClassCall
Redundant call to `cast()`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java

    public void setTimeout(long timeout) {
        Portable.class.cast(delegate).setTimeout(timeout);
    }

```

### RedundantClassCall
Redundant call to `cast()`
in `core/src/main/java/org/apache/cxf/bus/spring/BusWiringBeanFactoryPostProcessor.java`
#### Snippet
```java
            }
        }
        return Bus.class.cast(ctx.getBean(Bus.DEFAULT_BUS_ID, Bus.class));
    }
    public static Bus addBus(ApplicationContext ctx, String name) {
```

### RedundantClassCall
Redundant call to `isInstance()`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSDestination.java`
#### Snippet
```java
            this.jmsListener = createTargetDestinationListener();
        } catch (Exception e) {
            if (e.getCause() != null && InvalidClientIDException.class.isInstance(e.getCause())) {
                throw e;
            }
```

### RedundantClassCall
Redundant call to `isInstance()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java

        Method methodToInvoke;
        if (Proxy.class.isInstance(resourceObject)) {
            methodToInvoke = cri.getMethodDispatcher().getProxyMethod(resourceMethod);
            if (methodToInvoke == null) {
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
            entityStreamAvailable = entityStream != null;
        } else if (entity instanceof InputStream) {
            entityStream = InputStream.class.cast(entity);
        } else {
            Message inMessage = getResponseMessage();
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
            Reader reader = inMessage.getContent(Reader.class);
            if (reader != null) {
                entityStream = InputStream.class.cast(new ReaderInputStream(reader));
            }
        }
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
            try {
                if (entityBufferred) {
                    InputStream.class.cast(entity).reset();
                }

```

### RedundantClassCall
Redundant call to `isInstance()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
    @Override
    public C register(Object provider, int bindingPriority) {
        if (Instantiator.class.isInstance(provider)) {
            synchronized (this) {
                instantiator = Instantiator.class.cast(provider);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
        if (Instantiator.class.isInstance(provider)) {
            synchronized (this) {
                instantiator = Instantiator.class.cast(provider);
            }
            return configurable;
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
        Annotation b = getClassAnnotation(providerCls, PRIORITY_API);
        try {
            return b == null ? Priorities.USER : Integer.class.cast(PRIORITY_VALUE.invoke(b));
        } catch (final IllegalAccessException | InvocationTargetException e) {
            return Priorities.USER;
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return second;
        } else if (first instanceof Map) {
            Map.class.cast(first).putAll((Map<?, ?>) second);
            return first;
        }
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        if (o != null) {
            if (theValues instanceof Collection) {
                Collection.class.cast(theValues).add(o);
            } else if (theValues.getClass().getComponentType().isPrimitive()) {
                Array.set(theValues, index, o);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    Object paramValue;
                    if (setter instanceof Method) {
                        type = Method.class.cast(setter).getParameterTypes()[0];
                        genericType = Method.class.cast(setter).getGenericParameterTypes()[0];
                        paramValue = InjectionUtils.extractFromMethod(bean, (Method) getter);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    if (setter instanceof Method) {
                        type = Method.class.cast(setter).getParameterTypes()[0];
                        genericType = Method.class.cast(setter).getGenericParameterTypes()[0];
                        paramValue = InjectionUtils.extractFromMethod(bean, (Method) getter);
                    } else {
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                        paramValue = InjectionUtils.extractFromMethod(bean, (Method) getter);
                    } else {
                        type = Field.class.cast(setter).getType();
                        genericType = Field.class.cast(setter).getGenericType();
                        paramValue = InjectionUtils.extractFieldValue((Field) getter, bean);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    } else {
                        type = Field.class.cast(setter).getType();
                        genericType = Field.class.cast(setter).getGenericType();
                        paramValue = InjectionUtils.extractFieldValue((Field) getter, bean);
                    }
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            return second;
        } else if (first instanceof Collection) {
            Collection.class.cast(first).addAll((Collection<?>) second);
            return first;
        } else {
```

### RedundantClassCall
Redundant call to `isInstance()`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java

    public static Method checkProxy(Method methodToInvoke, Object resourceObject) {
        if (Proxy.class.isInstance(resourceObject)) {
            String methodToInvokeName = methodToInvoke.getName();
            Class<?>[] methodToInvokeTypes = methodToInvoke.getParameterTypes();
```

### RedundantClassCall
Redundant call to `isInstance()`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spring/EndpointDefinitionParser.java`
#### Snippet
```java
            DefaultListableBeanFactory dlbf = (DefaultListableBeanFactory)fact;
            for (BeanPostProcessor bpp : dlbf.getBeanPostProcessors()) {
                if (CommonAnnotationBeanPostProcessor.class.isInstance(bpp)) {
                    impl.getServerFactory().setBlockPostConstruct(true);
                    impl.getServerFactory().setBlockInjection(false);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassLoader.java`
#### Snippet
```java
        while (cls != null) {
            try {
                WrapperHelper helper = WrapperHelper.class.cast(cls.getDeclaredConstructor().newInstance());
                if (!helper.getSignature().equals(
                        WrapperHelperClassGenerator.computeSignature(setMethods, getMethods))) {
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
        while (cls != null) {
            try {
                WrapperHelper helper = WrapperHelper.class.cast(cls.getDeclaredConstructor().newInstance());
                if (!helper.getSignature().equals(computeSignature(setMethods, getMethods))) {
                    count++;
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
                Class<?> cl = loadClass(StringUtils.slashesToPeriod(newClassName), wrapperType, bt);
                Object o = cl.getDeclaredConstructor().newInstance();
                return WrapperHelper.class.cast(o);
            }
        } catch (Throwable e) {
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
        //  try to cast the resulting extension:
        try {
            return Schema.class.cast(extension);
        } catch (ClassCastException e) {
            throw new RuntimeException("Schema implementation problem", e);
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/DefaultProtocolInterceptor.java`
#### Snippet
```java

        if (writer instanceof AtmosphereInterceptorWriter) {
            AtmosphereInterceptorWriter.class.cast(writer).interceptor(interceptor, 0);
        }
    }
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java

            if (RxInvokerProvider.class.isAssignableFrom(providerCls)) {
                this.rxInvokerProvider = RxInvokerProvider.class.cast(provider.getProvider());
            }
        }
```

### RedundantClassCall
Redundant call to `cast()`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/Key.java`
#### Snippet
```java
        }

        final Key key = Key.class.cast(o);
        return !(accept != null ? !accept.equals(key.accept) : key.accept != null) && uri.equals(key.uri);

```

### RedundantClassCall
Redundant call to `cast()`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/validation/JAXRSClientBeanValidationFeature.java`
#### Snippet
```java

    public void setWrapInProcessingException(boolean wrapInProcessingException) {
        Portable.class.cast(getDelegate()).setWrapInProcessingException(wrapInProcessingException);
    }

```

## RuleId[ruleID=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        appendLine("var typePrefix = 'cxfjst" + anyTypePrefixCounter + "';");
        anyTypePrefixCounter++;
        appendLine("var typeAttr = 'xmlns:' + typePrefix + '=\\\''"
                   + " + anyHolder.namespaceURI + '\\\'';");
        appendLine("typeAttr = typeAttr + ' xsi:type=\\\'' + typePrefix + ':' "
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        anyTypePrefixCounter++;
        appendLine("var typeAttr = 'xmlns:' + typePrefix + '=\\\''"
                   + " + anyHolder.namespaceURI + '\\\'';");
        appendLine("typeAttr = typeAttr + ' xsi:type=\\\'' + typePrefix + ':' "
                   + "+ anyHolder.localName + '\\\'';");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
        appendLine("var typeAttr = 'xmlns:' + typePrefix + '=\\\''"
                   + " + anyHolder.namespaceURI + '\\\'';");
        appendLine("typeAttr = typeAttr + ' xsi:type=\\\'' + typePrefix + ':' "
                   + "+ anyHolder.localName + '\\\'';");
        endBlock();
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
                   + " + anyHolder.namespaceURI + '\\\'';");
        appendLine("typeAttr = typeAttr + ' xsi:type=\\\'' + typePrefix + ':' "
                   + "+ anyHolder.localName + '\\\'';");
        endBlock();
        startIf("anySerializer");
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java

    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java

    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
        return value;
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
        return value;
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchUtils.java`
#### Snippet
```java
    public static String duplicateSingleQuoteIfNeeded(String value) {
        if (value.indexOf('\'') != -1 && value.indexOf("\'\'") == -1) {
            value = value.replaceAll("\'", "\'\'");
        }
        return value;
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
                    description.append('#').append(opInfo.getName());
                }
                description.append("\' ");
            }
        }
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java

    // there are more of such characters, ex, '*' but '*' is not affected by UrlEncode
    private static final String PATH_RESERVED_CHARACTERS = "=@/:!$&\'(),;~";
    private static final String QUERY_RESERVED_CHARACTERS = "?/,";
    
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java
            } else if (o instanceof String) {
                if (ct == null) {
                    ct = "text/plain; charset=\'UTF-8\'";
                }
                dh = new DataHandler(new ByteDataSource(((String)o).getBytes(StandardCharsets.UTF_8), ct));
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java
            } else if (o instanceof String) {
                if (ct == null) {
                    ct = "text/plain; charset=\'UTF-8\'";
                }
                dh = new DataHandler(new ByteDataSource(((String)o).getBytes(StandardCharsets.UTF_8), ct));
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java
                if (sa != null && !sa.isEmpty()) {
                    String action = sa.get(0);
                    if (action.startsWith("\"") || action.startsWith("\'")) {
                        action = action.substring(1, action.length() - 1);
                    }
```

## RuleId[ruleID=ManualArrayCopy]
### ManualArrayCopy
Manual array copy
in `core/src/main/java/org/apache/cxf/staxutils/AbstractDOMStreamReader.java`
#### Snippet
```java
        }

        for (int i = 0; i < length; i++) {
            target[targetStart + i] = src[i + sourceStart];
        }
```

## RuleId[ruleID=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
    //CHECKSTYLE:OFF
    private InitialDirContext createInitialContext() throws NamingException {
        Hashtable<String, String> env = new Hashtable<>(5); //NOPMD
        env.put(javax.naming.Context.INITIAL_CONTEXT_FACTORY, "com.sun.jndi.ldap.LdapCtxFactory");
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
    //CHECKSTYLE:OFF
    private InitialDirContext createInitialContext() throws NamingException {
        Hashtable<String, String> env = new Hashtable<>(5); //NOPMD
        env.put(javax.naming.Context.INITIAL_CONTEXT_FACTORY, "com.sun.jndi.ldap.LdapCtxFactory");
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
    }
    public Dictionary<String, String> getProperties() {
        Dictionary<String, String> properties = new Hashtable<>();
        NumberFormat nf = NumberFormat.getIntegerInstance();
        properties.put("name", nf.format(getName()));
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java

    private InputStream createStreamContext(byte[] source, Node ctx) throws XMLEncryptionException {
        Vector<InputStream> v = new Vector<>(2); //NOPMD

        LoadingByteArrayOutputStream byteArrayOutputStream = new LoadingByteArrayOutputStream();
```

### ObsoleteCollection
Obsolete collection type `Vector<>` used
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java

    private InputStream createStreamContext(byte[] source, Node ctx) throws XMLEncryptionException {
        Vector<InputStream> v = new Vector<>(2); //NOPMD

        LoadingByteArrayOutputStream byteArrayOutputStream = new LoadingByteArrayOutputStream();
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java

    public static TypeCode getTypeCode(ORB orb, QName type, CorbaTypeMap typeMap) {
        Stack<QName> seenTypes = new Stack<>();
        return getTypeCode(orb, type, null, typeMap, seenTypes);
    }
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java

    public static TypeCode getTypeCode(ORB orb, QName type, CorbaTypeMap typeMap) {
        Stack<QName> seenTypes = new Stack<>();
        return getTypeCode(orb, type, null, typeMap, seenTypes);
    }
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                            Object obj,
                                            CorbaTypeMap typeMap,
                                            Stack<QName> seenTypes) {
        TypeCode tc = null;
        if (obj instanceof Anonarray) {
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                            Object obj,
                                            CorbaTypeMap typeMap,
                                            Stack<QName> seenTypes) {
        Union unionType = (Union)obj;

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                              Object obj,
                                              CorbaTypeMap typeMap,
                                              Stack<QName> seenTypes) {
        TypeCode tc = getAnonTypeCode(orb, type, obj, typeMap, seenTypes);

```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                       CorbaType obj,
                                       CorbaTypeMap typeMap,
                                       Stack<QName> seenTypes) {
        if (type == null) {
            throw new CorbaBindingException("corba:typemap type or elemtype information required"
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                       QName type,
                                       CorbaTypeMap typeMap,
                                       Stack<QName> seenTypes) {
        return getTypeCode(orb, type, null, typeMap, seenTypes);
    }
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                       CorbaType obj,
                                       CorbaTypeMap typeMap) {
        Stack<QName> seenTypes = new Stack<>();
        return getTypeCode(orb, type, obj, typeMap, seenTypes);
    }
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                       CorbaType obj,
                                       CorbaTypeMap typeMap) {
        Stack<QName> seenTypes = new Stack<>();
        return getTypeCode(orb, type, obj, typeMap, seenTypes);
    }
```

### ObsoleteCollection
Obsolete collection type `Vector<>` used
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/CollectionType.java`
#### Snippet
```java
            values = new TreeSet<>();
        } else if (userTypeClass.isAssignableFrom(Vector.class)) { //NOPMD
            values = new Vector<>(); //NOPMD
        } else if (userTypeClass.isAssignableFrom(Stack.class)) {
            values = new Stack<>();
```

### ObsoleteCollection
Obsolete collection type `Stack<>` used
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/CollectionType.java`
#### Snippet
```java
            values = new Vector<>(); //NOPMD
        } else if (userTypeClass.isAssignableFrom(Stack.class)) {
            values = new Stack<>();
        } else if (userTypeClass.isInterface()) {
            values = new ArrayList<>();
```

### ObsoleteCollection
Obsolete collection type `Hashtable<>` used
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/MapType.java`
#### Snippet
```java
            map = new HashMap<>();
        } else if (cls.equals(Hashtable.class)) { //NOPMD
            map = new Hashtable<>();
        } else if (cls.equals(ConcurrentMap.class)) {
            map = new ConcurrentHashMap<>();
```

## RuleId[ruleID=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
        inMessage.put(CertConstraints.class.getName(), certConstraints);
        inMessage.put(Message.IN_INTERCEPTORS,
                Arrays.asList(new Interceptor[] {CertConstraintsInterceptor.INSTANCE}));

    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    private static final String HTTP_GET_METHOD = "GET";
    private static final Set<String> KNOWN_HTTP_VERBS_WITH_NO_CONTENT =
        new HashSet<>(Arrays.asList(new String[]{"GET", "HEAD", "OPTIONS", "TRACE"}));

    private static final String AUTHORIZED_REDIRECTED_HTTP_VERBS = "http.redirect.allowed.verbs";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java
            String[] names = ctx.getBeanNamesForType(Bus.class);
            if (names == null || names.length == 0) {
                ctx2.setConfigLocations(new String[] {"classpath:/META-INF/cxf/cxf.xml",
                                                      location});
            } else {
                ctx2.setConfigLocations(new String[] {location});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java
                                                      location});
            } else {
                ctx2.setConfigLocations(new String[] {location});
            }
        } else {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java
            }
        } else {
            ctx2.setConfigLocations(new String[] {"classpath:/META-INF/cxf/cxf.xml",
                                                  location});
            createdContext = ctx2;
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
            if (servletPath == null) {
                try {
                    Method m = ServletContext.class.getMethod("getFilterRegistration", new Class[] {String.class});
                    Object registration = m.invoke(super.getServletContext(), new Object[]{filterName});
                    if (registration != null) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                try {
                    Method m = ServletContext.class.getMethod("getFilterRegistration", new Class[] {String.class});
                    Object registration = m.invoke(super.getServletContext(), new Object[]{filterName});
                    if (registration != null) {
                        m = registration.getClass().getMethod("getUrlPatternMappings", new Class[] {});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                    Object registration = m.invoke(super.getServletContext(), new Object[]{filterName});
                    if (registration != null) {
                        m = registration.getClass().getMethod("getUrlPatternMappings", new Class[] {});
                        Collection<String> mappings =
                            CastUtils.cast((Collection<?>)m.invoke(registration, new Object[]{}));
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
     */
    private static final List<String> KNOWN_HTTP_VERBS =
        Arrays.asList(new String[]{"POST", "GET", "PUT", "DELETE", "HEAD", "OPTIONS", "TRACE"});

    private static final String STATIC_RESOURCES_PARAMETER = "static-resources-list";
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
                try {
                    String nextPart = StringUtils.capitalize(names[1]);
                    Method m = value.getClass().getMethod("get" + nextPart, new Class[]{});
                    value = m.invoke(value, new Object[]{});
                } catch (Throwable ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
                    String nextPart = StringUtils.capitalize(names[1]);
                    Method m = value.getClass().getMethod("get" + nextPart, new Class[]{});
                    value = m.invoke(value, new Object[]{});
                } catch (Throwable ex) {
                    throw new RuntimeException();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
                    try {
                        Method setterM = valueType.getMethod("set" + getMethodNameSuffix(setter),
                                                             new Class[]{classType});
                        Object objectValue = !isCollection ? value : getCollectionSingleton(valueType, value);
                        setterM.invoke(ownerBean, new Object[]{objectValue});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
                                                             new Class[]{classType});
                        Object objectValue = !isCollection ? value : getCollectionSingleton(valueType, value);
                        setterM.invoke(ownerBean, new Object[]{objectValue});
                        castedValue = objectValue;
                    } catch (Throwable ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
        try {
            String nextPart = getMethodNameSuffix(names[1]);
            Method getterM = actualType.getMethod("get" + nextPart, new Class[]{});
            Class<?> returnType = getterM.getReturnType();
            boolean returnCollection = InjectionUtils.isSupportedCollectionOrArray(returnType);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
                nextObject = actualReturnType.getDeclaredConstructor().newInstance();
            }
            Method setterM = actualType.getMethod("set" + nextPart, new Class[]{returnType});
            final Object valueObjectValue;
            if (lastTry || !returnCollection) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
                valueObjectValue = getCollectionSingleton(collCls, nextObject);
            }
            setterM.invoke(valueObject, new Object[]{valueObjectValue});

            if (lastTry) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/security/src/main/java/org/apache/cxf/rt/security/SecurityConstants.java`
#### Snippet
```java

    static {
        Set<String> s = new HashSet<>(Arrays.asList(new String[] {
            USERNAME, PASSWORD, SIGNATURE_USERNAME, ENCRYPT_USERNAME,
            CALLBACK_HANDLER, SAML_CALLBACK_HANDLER, SIGNATURE_PROPERTIES,
            SIGNATURE_CRYPTO, ENCRYPT_PROPERTIES, ENCRYPT_CRYPTO, ENCRYPT_CERT,
            ENABLE_REVOCATION, SUBJECT_CERT_CONSTRAINTS, ENABLE_UNSIGNED_SAML_ASSERTION_PRINCIPAL,
            ENABLE_UT_NOPASSWORD_PRINCIPAL,
            AUDIENCE_RESTRICTION_VALIDATION, SAML_ROLE_ATTRIBUTENAME,
            ENABLE_UNSIGNED_SAML_ASSERTION_PRINCIPAL, SC_FROM_JAAS_SUBJECT,
            STS_TOKEN_USE_CERT_FOR_KEYINFO, STS_TOKEN_DO_CANCEL, CACHE_ISSUED_TOKEN_IN_ENDPOINT,
            DISABLE_STS_CLIENT_WSMEX_CALL_USING_EPR_ADDRESS, STS_TOKEN_CRYPTO,
            STS_TOKEN_PROPERTIES, STS_TOKEN_USERNAME, STS_TOKEN_ACT_AS, STS_TOKEN_ON_BEHALF_OF,
            STS_CLIENT, STS_APPLIES_TO, CACHE_ISSUED_TOKEN_IN_ENDPOINT, PREFER_WSMEX_OVER_STS_CLIENT_CONFIG,
            STS_TOKEN_IMMINENT_EXPIRY_VALUE, STS_TOKEN_CACHER_IMPL, AUDIENCE_RESTRICTIONS,
            STS_CHECK_FOR_RECURSIVE_CALL
        }));
        COMMON_PROPERTIES = Collections.unmodifiableSet(s);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                    String nextPart = StringUtils.capitalize(names[1]);

                    Method m = actualCls.getMethod("get" + nextPart, new Class[]{});
                    if (isCollection) {
                        value = ((Collection)value).iterator().next();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                        set.add(names[0]);
                    }
                    value = m.invoke(value, new Object[]{});
                    valueCls = value.getClass();
                    type = m.getGenericReturnType();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
                    if (paramType.isInstance(value)) {
                        try {
                            method.invoke(target, new Object[] {value});
                        } catch (Exception e) {
                            // do nothing here
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    private void setEnumOptions(StringBuilder sb, Class<?> enumClass) {
        try {
            Method m = enumClass.getMethod("values", new Class<?>[] {});
            Object[] values = (Object[])m.invoke(null, new Object[] {});
            m = enumClass.getMethod("name", new Class<?>[] {});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            Method m = enumClass.getMethod("values", new Class<?>[] {});
            Object[] values = (Object[])m.invoke(null, new Object[] {});
            m = enumClass.getMethod("name", new Class<?>[] {});
            for (Object o : values) {
                String str = (String)m.invoke(o, new Object[] {});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/src/main/java/org/apache/cxf/service/model/BindingInfo.java`
#### Snippet
```java
        if (operations.containsKey(operation.getName())) {
            throw new IllegalArgumentException(
                new Message("DUPLICATED.OPERATION.NAME", LOG, new Object[]{operation.getName()}).toString());
        }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
            XMLStreamReader filteredReader =
                StaxUtils.createFilteredReader(reader,
                                               new StaxStreamFilter(new QName[] {XOP}));

            StaxUtils.copy(filteredReader, nullWriter);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
     */
    private static final List<String> DEFAULT_CIPHERSUITE_FILTERS_EXCLUDE =
        Arrays.asList(new String[] {".*NULL.*",
                                    ".*anon.*",
                                    ".*EXPORT.*",
                                    ".*DES.*",
                                    ".*MD5",
                                    ".*CBC.*",
                                    ".*RC4.*"});

    private static volatile KeyManager[] defaultManagers;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/SecurityConstants.java`
#### Snippet
```java

    static {
        Set<String> s = new HashSet<>(Arrays.asList(new String[] {
            ACTOR, VALIDATE_TOKEN, ALWAYS_ENCRYPT_UT, IS_BSP_COMPLIANT, ENABLE_NONCE_CACHE,
            ENABLE_TIMESTAMP_CACHE, TIMESTAMP_TTL, TIMESTAMP_FUTURE_TTL,
            KERBEROS_CLIENT, SPNEGO_CLIENT_ACTION, KERBEROS_JAAS_CONTEXT_NAME, KERBEROS_SPN,
            NONCE_CACHE_INSTANCE, TIMESTAMP_CACHE_INSTANCE, CACHE_CONFIG_FILE,
            TOKEN_STORE_CACHE_INSTANCE, USERNAME_TOKEN_VALIDATOR, SAML1_TOKEN_VALIDATOR,
            SAML2_TOKEN_VALIDATOR, TIMESTAMP_TOKEN_VALIDATOR, SIGNATURE_TOKEN_VALIDATOR,
            BST_TOKEN_VALIDATOR, SCT_TOKEN_VALIDATOR, TOKEN, TOKEN_ID, SUBJECT_ROLE_CLASSIFIER,
            SUBJECT_ROLE_CLASSIFIER_TYPE, MUST_UNDERSTAND, ASYMMETRIC_SIGNATURE_ALGORITHM,
            PASSWORD_ENCRYPTOR_INSTANCE, ENABLE_SAML_ONE_TIME_USE_CACHE,
            SAML_ONE_TIME_USE_CACHE_INSTANCE, ENABLE_STREAMING_SECURITY, RETURN_SECURITY_ERROR,
            CACHE_IDENTIFIER, DELEGATED_CREDENTIAL, KERBEROS_USE_CREDENTIAL_DELEGATION,
            KERBEROS_IS_USERNAME_IN_SERVICENAME_FORM, KERBEROS_REQUEST_CREDENTIAL_DELEGATION,
            POLICY_VALIDATOR_MAP, STORE_BYTES_IN_ATTACHMENT, USE_ATTACHMENT_ENCRYPTION_CONTENT_ONLY_TRANSFORM,
            SYMMETRIC_SIGNATURE_ALGORITHM, SECURITY_CONTEXT_CREATOR, SECURITY_TOKEN_LIFETIME,
            DISABLE_REQ_CLIENT_CERT_CHECK, EXPAND_XOP_INCLUDE
        }));
        for (String commonProperty : COMMON_PROPERTIES) {
            s.add(commonProperty);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/WSSecurityPolicyLoader.java`
#### Snippet
```java
new QName[] {
            SP12Constants.INCLUDE_TIMESTAMP, SP11Constants.INCLUDE_TIMESTAMP,
            SP12Constants.ENCRYPT_SIGNATURE, SP11Constants.ENCRYPT_SIGNATURE,
            SP12Constants.PROTECT_TOKENS, SP11Constants.PROTECT_TOKENS,
            SP12Constants.ONLY_SIGN_ENTIRE_HEADERS_AND_BODY,
            SP11Constants.ONLY_SIGN_ENTIRE_HEADERS_AND_BODY,
            SP12Constants.WSS_X509_V1_TOKEN_10,
            SP12Constants.WSS_X509_V1_TOKEN_11,
            SP12Constants.WSS_X509_V3_TOKEN_10,
            SP12Constants.WSS_X509_V3_TOKEN_11,
            SP11Constants.WSS_X509_V1_TOKEN_10,
            SP11Constants.WSS_X509_V1_TOKEN_11,
            SP11Constants.WSS_X509_V3_TOKEN_10,
            SP11Constants.WSS_X509_V3_TOKEN_11,
            SP12Constants.WSS_X509_PKCS7_TOKEN_11,
            SP12Constants.WSS_X509_PKI_PATH_V1_TOKEN_11,
            SP11Constants.WSS_X509_PKCS7_TOKEN_11,
            SP11Constants.WSS_X509_PKI_PATH_V1_TOKEN_11,
            SP12Constants.REQUIRE_THUMBPRINT_REFERENCE,
            SP11Constants.REQUIRE_THUMBPRINT_REFERENCE,
            SP12Constants.REQUIRE_DERIVED_KEYS,
            SP11Constants.REQUIRE_DERIVED_KEYS,
            SP12Constants.REQUIRE_INTERNAL_REFERENCE,
            SP11Constants.REQUIRE_INTERNAL_REFERENCE,
            SP12Constants.REQUIRE_ISSUER_SERIAL_REFERENCE,
            SP11Constants.REQUIRE_ISSUER_SERIAL_REFERENCE,
            SP12Constants.REQUIRE_EMBEDDED_TOKEN_REFERENCE,
            SP11Constants.REQUIRE_EMBEDDED_TOKEN_REFERENCE,
            SP12Constants.ENCRYPT_BEFORE_SIGNING,
            SP11Constants.ENCRYPT_BEFORE_SIGNING,
            SP12Constants.SIGN_BEFORE_ENCRYPTING,
            SP11Constants.SIGN_BEFORE_ENCRYPTING,
            SP12Constants.REQUIRE_KEY_IDENTIFIER_REFERENCE,
            SP11Constants.REQUIRE_KEY_IDENTIFIER_REFERENCE,
            SP12Constants.PROTECT_TOKENS,
            SP11Constants.PROTECT_TOKENS,
            SP12Constants.RSA_KEY_VALUE,

            // Layout
            SP11Constants.LAX, SP11Constants.LAXTSFIRST, SP11Constants.LAXTSLAST, SP11Constants.STRICT,
            SP12Constants.LAX, SP12Constants.LAXTSFIRST, SP12Constants.LAXTSLAST, SP12Constants.STRICT,

            // UsernameToken
            SP11Constants.WSS_USERNAME_TOKEN10, SP12Constants.WSS_USERNAME_TOKEN10,
            SP11Constants.WSS_USERNAME_TOKEN11, SP12Constants.WSS_USERNAME_TOKEN11,
            SP12Constants.HASH_PASSWORD, SP12Constants.NO_PASSWORD,
            SP13Constants.CREATED, SP13Constants.NONCE,

            SP12Constants.REQUIRE_INTERNAL_REFERENCE, SP11Constants.REQUIRE_INTERNAL_REFERENCE,
            SP12Constants.REQUIRE_EXTERNAL_REFERNCE, SP11Constants.REQUIRE_EXTERNAL_REFERNCE,

            // Kerberos
            new QName(SP11Constants.SP_NS, "WssKerberosV5ApReqToken11"),
            new QName(SP12Constants.SP_NS, "WssKerberosV5ApReqToken11"),
            new QName(SP11Constants.SP_NS, "WssGssKerberosV5ApReqToken11"),
            new QName(SP12Constants.SP_NS, "WssGssKerberosV5ApReqToken11"),

            // Spnego
            SP12Constants.MUST_NOT_SEND_AMEND,
            SP12Constants.MUST_NOT_SEND_CANCEL,
            SP12Constants.MUST_NOT_SEND_RENEW,

            // Backwards compatibility thing
            new QName("http://schemas.microsoft.com/ws/2005/07/securitypolicy", SPConstants.MUST_NOT_SEND_CANCEL),

            // SCT
            SP12Constants.REQUIRE_EXTERNAL_URI_REFERENCE,
            SP12Constants.SC13_SECURITY_CONTEXT_TOKEN,
            SP11Constants.SC10_SECURITY_CONTEXT_TOKEN,

            // WSS10
            SP12Constants.MUST_SUPPORT_REF_KEY_IDENTIFIER, SP11Constants.MUST_SUPPORT_REF_KEY_IDENTIFIER,
            SP12Constants.MUST_SUPPORT_REF_ISSUER_SERIAL, SP11Constants.MUST_SUPPORT_REF_ISSUER_SERIAL,
            SP12Constants.MUST_SUPPORT_REF_EXTERNAL_URI, SP12Constants.MUST_SUPPORT_REF_EXTERNAL_URI,
            SP12Constants.MUST_SUPPORT_REF_EMBEDDED_TOKEN, SP11Constants.MUST_SUPPORT_REF_EMBEDDED_TOKEN,

            // WSS11
            SP12Constants.MUST_SUPPORT_REF_THUMBPRINT, SP11Constants.MUST_SUPPORT_REF_THUMBPRINT,
            SP12Constants.MUST_SUPPORT_REF_ENCRYPTED_KEY, SP11Constants.MUST_SUPPORT_REF_ENCRYPTED_KEY,
            SP12Constants.REQUIRE_SIGNATURE_CONFIRMATION, SP11Constants.REQUIRE_SIGNATURE_CONFIRMATION,

            // SAML
            new QName(SP11Constants.SP_NS, "WssSamlV11Token10"),
            new QName(SP12Constants.SP_NS, "WssSamlV11Token10"),
            new QName(SP11Constants.SP_NS, "WssSamlV11Token11"),
            new QName(SP12Constants.SP_NS, "WssSamlV11Token11"),
            new QName(SP11Constants.SP_NS, "WssSamlV20Token11"),
            new QName(SP12Constants.SP_NS, "WssSamlV20Token11"),

            // HTTPs
            SP12Constants.HTTP_BASIC_AUTHENTICATION,
            SP12Constants.HTTP_DIGEST_AUTHENTICATION,
            SP12Constants.REQUIRE_CLIENT_CERTIFICATE,

            // Trust13
            SP12Constants.MUST_SUPPORT_CLIENT_CHALLENGE, SP11Constants.MUST_SUPPORT_CLIENT_CHALLENGE,
            SP12Constants.MUST_SUPPORT_SERVER_CHALLENGE, SP11Constants.MUST_SUPPORT_SERVER_CHALLENGE,
            SP12Constants.REQUIRE_CLIENT_ENTROPY, SP11Constants.REQUIRE_CLIENT_ENTROPY,
            SP12Constants.REQUIRE_SERVER_ENTROPY, SP11Constants.REQUIRE_SERVER_ENTROPY,
            SP12Constants.MUST_SUPPORT_ISSUED_TOKENS, SP11Constants.MUST_SUPPORT_ISSUED_TOKENS,
            SP12Constants.REQUIRE_REQUEST_SECURITY_TOKEN_COLLECTION,
            SP12Constants.REQUIRE_APPLIES_TO,
            SP13Constants.SCOPE_POLICY_15,
            SP13Constants.MUST_SUPPORT_INTERACTIVE_CHALLENGE,

            // AlgorithmSuite misc
            new QName(SP11Constants.SP_NS, SPConstants.INCLUSIVE_C14N),
            new QName(SP12Constants.SP_NS, SPConstants.INCLUSIVE_C14N),
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
        String normalizedEncoding = HttpHeaderHelper.mapCharset(enc, StandardCharsets.UTF_8.name());
        if (normalizedEncoding == null) {
            String m = new org.apache.cxf.common.i18n.Message("INVALID_ENCODING_MSG", LOG, new Object[] {
                enc
            }).toString();
            LOG.log(Level.WARNING, m);
            throw new UnsupportedEncodingException(m);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        new HashSet<>(Arrays.asList(new String[]{"GET", "HEAD", "OPTIONS", "TRACE"}));
    private static final Set<String> KNOWN_HTTP_VERBS_WITH_NO_RESPONSE_CONTENT =
        new HashSet<>(Arrays.asList(new String[]{"HEAD", "OPTIONS"}));
    
    private static final Pattern HTTP_SCHEME_PATTERN = Pattern.compile("^(?i)(http|https)$");
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    
    private static final Set<String> KNOWN_HTTP_VERBS_WITH_NO_REQUEST_CONTENT =
        new HashSet<>(Arrays.asList(new String[]{"GET", "HEAD", "OPTIONS", "TRACE"}));
    private static final Set<String> KNOWN_HTTP_VERBS_WITH_NO_RESPONSE_CONTENT =
        new HashSet<>(Arrays.asList(new String[]{"HEAD", "OPTIONS"}));
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        } else {
            try {
                Constructor<?> c = cls.getConstructor(new Class<?>[]{String.class});
                return c.newInstance(new Object[]{value});
            } catch (Throwable ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            try {
                Constructor<?> c = cls.getConstructor(new Class<?>[]{String.class});
                return c.newInstance(new Object[]{value});
            } catch (Throwable ex) {
                // try valueOf
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        throws InvocationTargetException {
        try {
            Method m = pClass.getMethod(methodName, new Class<?>[]{String.class});
            if (Modifier.isStatic(m.getModifiers())) {
                return pClass.cast(m.invoke(null, new Object[]{value}));
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            Method m = pClass.getMethod(methodName, new Class<?>[]{String.class});
            if (Modifier.isStatic(m.getModifiers())) {
                return pClass.cast(m.invoke(null, new Object[]{value}));
            }
        } catch (NoSuchMethodException ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
    private static Object invokeValueOf(String value, Class<?> cls) {
        try {
            Method m = cls.getMethod("valueOf", new Class[]{String.class});
            return m.invoke(null, value);
        } catch (Exception ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        try {
            Method methodToInvoke = checkProxy(method, requestObject);
            methodToInvoke.invoke(requestObject, new Object[]{parameterValue});
        } catch (IllegalAccessException ex) {
            reportServerError("METHOD_ACCESS_FAILURE", method.getName());
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            method = InjectionUtils.checkProxy(method, instance);
            try {
                method.invoke(instance, new Object[]{});
            } catch (InvocationTargetException ex) {
                String msg = "Method " + method.getName() + " can not be invoked"
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        // check constructors accepting a single String value
        try {
            Constructor<?> c = cls.getConstructor(new Class<?>[]{String.class});
            result = c.newInstance(new Object[]{value});
        } catch (NoSuchMethodException ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        try {
            Constructor<?> c = cls.getConstructor(new Class<?>[]{String.class});
            result = c.newInstance(new Object[]{value});
        } catch (NoSuchMethodException ex) {
            // try valueOf
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/lifecycle/SingletonResourceProvider.java`
#### Snippet
```java
                ResourceUtils.createConstructorArguments(c, m, false, Collections.emptyMap());
            try {
                resourceInstance = values.length > 0 ? c.newInstance(values) : c.newInstance(new Object[]{});
            } catch (Exception ex) {
                throw new ServiceConstructionException(ex);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/lifecycle/PerRequestResourceProvider.java`
#### Snippet
```java
        Object[] values = ResourceUtils.createConstructorArguments(c, m, true, mapValues, params, anns, genericTypes);
        try {
            Object instance = values.length > 0 ? c.newInstance(values) : c.newInstance(new Object[]{});
            InjectionUtils.invokeLifeCycleMethod(instance, postConstructMethod);
            return instance;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/AttachmentInputInterceptor.java`
#### Snippet
```java

    private static final List<String> DEFAULT_TYPES =
        Arrays.asList(new String[]{"multipart/related",
                                   "multipart/mixed",
                                   "multipart/alternative",
                                   "multipart/form-data"});

    private List<String> types = DEFAULT_TYPES;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                                        Object pValue) {
        try {
            Method m = provider.getClass().getMethod(mName, new Class[]{pClass});
            m.invoke(provider, new Object[]{pValue});
            return true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        try {
            Method m = provider.getClass().getMethod(mName, new Class[]{pClass});
            m.invoke(provider, new Object[]{pValue});
            return true;
        } catch (Exception ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSProviderMethodDispatcher.java`
#### Snippet
```java
        try {
            invoke = ReflectionUtil.getMethod(implInfo.getImplementorClass(), "invoke", 
                                              new Class[] {implInfo.getProviderParameterType()});
            ReflectionUtil.setAccessible(invoke);
        } catch (Exception e1) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSProviderMethodDispatcher.java`
#### Snippet
```java
            //fall back to the raw Provider provided invoke method
            try {
                invoke = Provider.class.getMethod("invoke", new Class[] {Object.class});
            } catch (Exception e) {
                throw new ServiceConstructionException(e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
                //jaxContext = ContextUtils.getJAXBContext();
                JAXBContext context = JAXBContext
                    .newInstance(new Class[] {org.apache.cxf.ws.addressing.ObjectFactory.class});
                um = context.createUnmarshaller();
                return um.unmarshal(reader, EndpointReferenceType.class).getValue();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java

                    public Method run() throws Exception {
                        Method hasSwaRefMethod = ctx.getClass().getMethod("hasSwaRef", new Class[0]);
                        if (!hasSwaRefMethod.isAccessible()) {
                            hasSwaRefMethod.setAccessible(true);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
            Object faultInfo;
            try {
                Method method = cause.getClass().getMethod("getFaultInfo", new Class[0]);
                faultInfo = method.invoke(cause, new Object[0]);
            } catch (NoSuchMethodException e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
            try {
                Method method = cause.getClass().getMethod("getFaultInfo", new Class[0]);
                faultInfo = method.invoke(cause, new Object[0]);
            } catch (NoSuchMethodException e) {
                faultInfo = createFaultInfoBean(fault, cause);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
            }

            Method getFaultInfo = exClass.getMethod("getFaultInfo", new Class[0]);

            return getFaultInfo.getReturnType();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMCaptureOutInterceptor.java`
#### Snippet
```java
        ts.setIdentifier(identifier);
        MessageContentsList contents =
            new MessageContentsList(new Object[]{protocol.getCodec().convertToSend(ts)});
        msg.setContent(List.class, contents);

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/SoapFaultFactory.java`
#### Snippet
```java
        org.apache.cxf.common.i18n.Message msg
            = new org.apache.cxf.common.i18n.Message("SEQ_FAULT_MSG", LOG,
                new Object[] {sf.getReason(), sf.getFaultCode(), sf.getSubCode()});
        return msg.toString();
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
        try {
            Method m = ReflectionUtil.getDeclaredMethod(mapper.getClass(),
                                                        "setContextualNamespaceDecls", new Class<?>[]{String[].class});
            String[] args = new String[nsctxt.size() * 2];
            int ai = 0;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
            setter = method.getDeclaringClass()
                .getMethod("set" + method.getName().substring(beginIndex),
                           new Class[] {method.getReturnType()});
        } catch (Exception e) {
            //getter, but no setter
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        } else if (part.getXmlSchema() == null) {
            try {
                cls.getConstructor(new Class[] {String.class});
            } catch (Exception e) {
                try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            } catch (Exception e) {
                try {
                    cls.getConstructor(new Class[0]);
                } catch (Exception e2) {
                    //no String or default constructor, we cannot use it
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
        try {
            dataProviderContextMethod = provider.getClass().getMethod("setMessageContext",
                                                                          new Class[]{MessageContext.class});
        } catch (Throwable t) {
            // ignore
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
        if (dataProviderContextMethod != null) {
            try {
                dataProviderContextMethod.invoke(provider, new Object[]{context});
            } catch (Throwable t) {
                throw new RuntimeException(t);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                obj = cons.newInstance();
            } catch (NoSuchMethodException nse) {
                Constructor<?> cons = cls.getConstructor(new Class[] {String.class});
                obj = cons.newInstance(new Object[1]);
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                    try {
                        getMethod = valueClass.getMethod("is_return",
                                                          new Class[0]);
                    } catch (NoSuchMethodException ex2) {
                        //ignore for now
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
                    && method.getParameterTypes()[0].equals(typeClass)) {

                    mObj = method.invoke(objectFactory.getDeclaredConstructor().newInstance(), new Object[] {obj});
                }
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
            if (m != null) {
                try {
                    m.invoke(this, new Object[] {});
                } catch (IllegalAccessException e) {
                    pos = curPos;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
        //Some of them have a "write_fixed(BigDecimal, short, short)" method, we'll try that
        try {
            Method m = stream.getClass().getMethod("write_fixed", new Class[] {BigDecimal.class,
                                                                               Short.TYPE,
                                                                               Short.TYPE});
            m.invoke(stream, fixedHandler.getValue(), fixed, scale);
        } catch (Throwable e1) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                        Class<?> beanClass = e.getClass();
                        Constructor<?> constructor =
                            exClass.getConstructor(new Class[]{String.class, beanClass});

                        String repId = (message.getStreamableException()._type().id() != null)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                            ? message.getStreamableException()._type().id()
                                : "";
                        e = constructor.newInstance(new Object[]{repId, e});
                    } else {
                        // Get the Fault
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
            //Some of them have a "read_fixed(TypeCode)" method, we'll try that
            try {
                Method m = stream.getClass().getMethod("read_fixed", new Class[] {TypeCode.class});
                BigDecimal fixedValue =
                    (BigDecimal)m.invoke(stream, new Object[] {fixedHandler.getTypeCode()});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/InvocationHandlerFactory.java`
#### Snippet
```java
            try {
                Constructor<?> newHandlerConstructor = handlerChainTypes[i]
                    .getDeclaredConstructor(new Class[] {CXFInvocationHandlerData.class});
                newHandler = (CXFInvocationHandler)newHandlerConstructor.newInstance(new Object[] {data});
            } catch (Exception ex) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `integration/jca/src/main/java/org/apache/cxf/jca/servant/EJBInvoker.java`
#### Snippet
```java
                throw new IllegalArgumentException("home must be for a stateless session bean");
            }
            createMethod = home.getClass().getMethod("create", new Class[0]);
        } catch (Exception ex) {
            throw new IllegalArgumentException("Unable to initialize invoker: " + ex);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBMessageListenerImpl.java`
#### Snippet
```java
    public Object lookupTargetObject(String targetJndiName) throws Exception {
        Object home = new InitialContext().lookup(targetJndiName);
        Method method = home.getClass().getMethod("create", new Class[0]);
        return method.invoke(home, new Object[0]);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBMessageListenerImpl.java`
#### Snippet
```java
        Object home = new InitialContext().lookup(targetJndiName);
        Method method = home.getClass().getMethod("create", new Class[0]);
        return method.invoke(home, new Object[0]);
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationWork.java`
#### Snippet
```java
        try {
            Class<?> clazz = org.apache.cxf.jca.inbound.DispatchMDBMessageListener.class;
            method = clazz.getMethod(MESSAGE_LISTENER_METHOD, new Class[] {String.class});
        } catch (Exception ex) {
            LOG.severe("Failed to get method " + MESSAGE_LISTENER_METHOD
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createMalformedRequestURIFault(String requestURI) {
        String m = new org.apache.cxf.common.i18n.Message("MALFORMED_REQUESTURI", LOG,
                                                          new Object[] {
                                                              requestURI
                                                          }).toString();
        return createFault(SoapJMSConstants.getMalformedRequestURIQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createUnsupportedJMSMessageFormatFault(String messageFormat) {
        String m = new org.apache.cxf.common.i18n.Message("UNSUPPORTED_JMSMESSAGEFORMAT", LOG,
                                                          new Object[] {
                                                              messageFormat
                                                          }).toString();
        return createFault(SoapJMSConstants.getUnsupportedJMSMessageFormatQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createContentEncodingNotSupported(String contentEncoding) {
        String m = new org.apache.cxf.common.i18n.Message("CONTENT_ENCODING_NOT_SUPPORTED", LOG,
                                                          new Object[] {
                                                              contentEncoding
                                                          }).toString();
        return createFault(SoapJMSConstants.getContentTypeMismatchQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createContentTypeMismatchFault(String contentType) {
        String m = new org.apache.cxf.common.i18n.Message("CONTENTTYPE_MISMATCH", LOG,
                                                          new Object[] {
                                                              contentType
                                                          }).toString();
        return createFault(SoapJMSConstants.getContentTypeMismatchQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createUnrecognizedBindingVerionFault(String bindingVersion) {
        String m = new org.apache.cxf.common.i18n.Message("UNRECOGNIZED_BINDINGVERSION", LOG,
                                                          new Object[] {
                                                              bindingVersion
                                                          }).toString();
        return createFault(SoapJMSConstants.getUnrecognizedBindingVersionQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java
    public static JMSFault createMismatchedSoapActionFault(String soapAction) {
        String m = new org.apache.cxf.common.i18n.Message("MISMATCHED_SOAPACTION", LOG,
                                                          new Object[] {
                                                              soapAction
                                                          }).toString();
        return createFault(SoapJMSConstants.getMismatchedSoapActionQName(), m);
    }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpressionBuilder.java`
#### Snippet
```java
public class URIDomainExpressionBuilder implements DomainExpressionBuilder {
    private static final Collection<QName> SUPPORTED_TYPES = Collections.unmodifiableList(
        Arrays.asList(new QName[] {
            new QName("http://www.w3.org/ns/ws-policy", "URI"),
            new QName("http://schemas.xmlsoap.org/ws/2004/09/policy", "URI")
        }));

    @Override
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
        if (Object.class.getMethod("hashCode", (Class[]) null).equals(method)) {
            return proxyHashCode(proxy);
        } else if (Object.class.getMethod("equals", new Class[] {Object.class}).equals(method)) {
            return proxyEquals(proxy, args[0]);
        } else if (Object.class.getMethod("toString", (Class[]) null).equals(method)) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            private String errorMessage(SAXParseException exception) {
                return MessageFormat.format("{0} at {1} line {2} column {3}.",
                                            new Object[] {exception.getMessage(), path,
                                                          Integer.valueOf(exception.getLineNumber()),
                                                          Integer.valueOf(exception.getColumnNumber())});
            }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java

        try {
            ctr = clazz.getConstructor(new Class[] {
                String.class, Throwable.class
            });
            o = ctr.newInstance(new Object[] {
                fault.getMessage(), fault
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
                String.class, Throwable.class
            });
            o = ctr.newInstance(new Object[] {
                fault.getMessage(), fault
            });
        } catch (NoSuchMethodException e) {
            try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
        } catch (NoSuchMethodException e) {
            try {
                ctr = clazz.getConstructor(new Class[] {
                    String.class, Exception.class
                });
                o = ctr.newInstance(new Object[] {
                    fault.getMessage(), fault
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
                    String.class, Exception.class
                });
                o = ctr.newInstance(new Object[] {
                    fault.getMessage(), fault
                });
            } catch (NoSuchMethodException e1) {
                try {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            } catch (NoSuchMethodException e1) {
                try {
                    ctr = clazz.getConstructor(new Class[] {
                        String.class
                    });
                    o = ctr.newInstance(new Object[] {
                        fault.getMessage()
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
                        String.class
                    });
                    o = ctr.newInstance(new Object[] {
                        fault.getMessage()
                    });
                } catch (NoSuchMethodException e2) {
                    return clazz.getDeclaredConstructor().newInstance();
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            }

            return m.invoke(object, new Object[0]);
        } catch (Exception e) {
            throw new DatabindingException("Couldn't get property " + name + " from bean " + object, e);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            Class<?> propertyType = desc.getPropertyType();
            if ((property == null && !propertyType.isPrimitive()) || (property != null)) {
                m.invoke(object, new Object[] {
                    property
                });
            }
        } catch (DatabindingException e) {
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
        name = "set" + StringUtils.capitalize(name);

        return impl.getMethod(name, new Class[] {
            pd.getPropertyType()
        });
    }

```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
    static final Integer DEFAULT_MAX_RESPONSE_CONTENT_LENGTH = 1048576;
    private static final Set<String> KNOWN_HTTP_VERBS_WITH_NO_CONTENT =
            new HashSet<>(Arrays.asList(new String[]{"GET", "HEAD", "OPTIONS", "TRACE"}));
    
    final NettyHttpConduitFactory factory;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                getBeanGetter(requestBody.getClass(), varName, new Class<?>[] {}).ifPresent(getter -> {
                    try {
                        list.add(getter.invoke(requestBody, new Object[] {}));
                    } catch (Exception ex) {
                        // continue
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                map(getter -> {
                                    try {
                                        return getter.invoke(bean, new Object[] {});
                                    } catch (Exception ex) {
                                        // ignore
```

## RuleId[ruleID=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
            break;
        case GREATER_OR_EQUALS:
            query = createRangeQuery(clazz, name, value, ct);
            break;
        case LESS_THAN:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
            break;
        case LESS_THAN:
            query = createRangeQuery(clazz, name, value, ct);
            break;
        case LESS_OR_EQUALS:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
            break;
        case LESS_OR_EQUALS:
            query = createRangeQuery(clazz, name, value, ct);
            break;
        default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java
                    break;
                case XMLStreamConstants.DTD:
                    break;
                case XMLStreamConstants.END_DOCUMENT:
                    contentHandler.endDocument();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java
                case XMLStreamConstants.NAMESPACE:
                case XMLStreamConstants.NOTATION_DECLARATION:
                    break;
                case XMLStreamConstants.PROCESSING_INSTRUCTION:
                    break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxSource.java`
#### Snippet
```java
                    break;
                case XMLStreamConstants.PROCESSING_INSTRUCTION:
                    break;
                case XMLStreamConstants.START_DOCUMENT:
                    contentHandler.startDocument();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.NAMESPACE:
                break;
            case XMLStreamConstants.ATTRIBUTE:
                break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ATTRIBUTE:
                break;
            case XMLStreamConstants.CHARACTERS:
                if (parent != null) {
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.NAMESPACE:
                break;
            case XMLStreamConstants.ATTRIBUTE:
                break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ATTRIBUTE:
                break;
            case XMLStreamConstants.CHARACTERS:
                if (parent != null) {
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            case XMLStreamConstants.ATTRIBUTE:
            case XMLStreamConstants.NAMESPACE:
                break;
            default:
                break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_long:
            data = ((Integer)value).toString();
            break;
        case TCKind._tk_longlong:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_ulong:
            data = ((Long)value).toString();
            break;
        case TCKind._tk_ulonglong:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_long:
            obj = Integer.valueOf(data);
            break;
        case TCKind._tk_longlong:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_ulong:
            obj = Long.valueOf(data);
            break;
        case TCKind._tk_ulonglong:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
        case TCKind._tk_string:
        case TCKind._tk_wstring:
            obj = data;
            break;
        default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
                case XMLStreamConstants.ATTRIBUTE:
                case XMLStreamConstants.NAMESPACE:
                    break;
                default:
                    break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `rt/management/src/main/java/org/apache/cxf/management/counters/ResponseTimeCounter.java`
#### Snippet
```java
                    break;
                case RUNTIME_FAULT:
                    runtimeFaults.incrementAndGet();
                    break;
                case UNCHECKED_APPLICATION_FAULT:
```

## RuleId[ruleID=MisspelledEquals]
### MisspelledEquals
`equal()` method should probably be 'equals()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/HTTPClientAssertionBuilder.java`
#### Snippet
```java

        @Override
        public boolean equal(PolicyComponent policyComponent) {
            if (policyComponent == this) {
                return true;
```

### MisspelledEquals
`equal()` method should probably be 'equals()'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/HTTPServerAssertionBuilder.java`
#### Snippet
```java

        @Override
        public boolean equal(PolicyComponent policyComponent) {
            if (policyComponent == this) {
                return true;
```

### MisspelledEquals
`equal()` method should probably be 'equals()'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM10AssertionBuilder.java`
#### Snippet
```java

        @Override
        public boolean equal(PolicyComponent policyComponent) {
            if (policyComponent.getType() != Constants.TYPE_ASSERTION
                || !getName().equals(((Assertion)policyComponent).getName())) {
```

### MisspelledEquals
`equal()` method should probably be 'equals()'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/jaxb/JaxbAssertion.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public boolean equal(PolicyComponent policyComponent) {
        if (!super.equal(policyComponent)) {
            return false;
```

## RuleId[ruleID=RegExpSimplifiable]
### RegExpSimplifiable
`{1}` is redundant
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/InetAddressUtils.java`
#### Snippet
```java

    private static final String IPV4_BASIC_PATTERN_STRING =
        "(([1-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){1}" // initial first field, 1-255
        + "(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){2}" // following 2 fields, 0-255 followed by .
        + "([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])"; // final field, 0-255
```

### RegExpSimplifiable
`{1}` is redundant
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/InetAddressUtils.java`
#### Snippet
```java

    private static final Pattern IPV4_PATTERN =
        Pattern.compile("^" + IPV4_BASIC_PATTERN_STRING + "$");

    private static final Pattern IPV4_MAPPED_IPV6_PATTERN = // TODO does not allow for redundant leading zeros
```

### RegExpSimplifiable
`{1}` is redundant
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/InetAddressUtils.java`
#### Snippet
```java

    private static final Pattern IPV4_MAPPED_IPV6_PATTERN = // TODO does not allow for redundant leading zeros
            Pattern.compile("^::[fF]{4}:" + IPV4_BASIC_PATTERN_STRING + "$");

    private static final Pattern IPV6_STD_PATTERN =
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value= 
         "([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*(\|([\i-[:]][\c-[:]]*:)?(//)?([\i-[:]][\c-[:]]*|\*)((/|//)(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*|\*|[.]))*)*">
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/t-1.xsd`
#### Snippet
```java
      </xsd:annotation>
      <xsd:pattern value=
"(([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)(/([\i-[:]][\c-[:]]*:)?[\i-[:]][\c-[:]]*)*" >
      </xsd:pattern>
    </xsd:restriction>
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/2001/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `services/wsn/wsn-api/src/main/resources/org/apache/cxf/wsn/wsdl/2001/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpSimplifiable
`[\\p{ASCII}]` can be simplified to '\\p{ASCII}'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/fiql/FiqlParser.java`
#### Snippet
```java
        // pattern
        String comparators = GT + "|" + GE + "|" + LT + "|" + LE + "|" + EQ + "|" + NEQ;
        String s1 = "[\\p{ASCII}]+(" + comparators + ")";
        COMPARATORS_PATTERN = Pattern.compile(s1);

```

### RegExpSimplifiable
`[\\p{ASCII}]` can be simplified to '\\p{ASCII}'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/fiql/FiqlParser.java`
#### Snippet
```java
        COMPARATORS_PATTERN = Pattern.compile(s1);

        String s2 = "[\\p{ASCII}]+(" + comparators + "|" + "=" + ")";
        COMPARATORS_PATTERN_SINGLE_EQUALS = Pattern.compile(s2);
    }
```

### RegExpSimplifiable
`[\d]` can be simplified to '\\d'
in `testutils/src/main/resources/wsdl/schema_validation.wsdl`
#### Snippet
```java
                        	<simpleType>
                				<restriction base="string">
                	     			<pattern value="[\d]{4}" />
                				</restriction>
            				</simpleType>
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w-]+=\"[^\"]*\")|([\\w-]+=[\\w]+)|([\\w-]+))";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[\\w]` can be simplified to '\\w'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java

    private static final String COMPLEX_HEADER_EXPRESSION =
        "(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+))(;(([\\w]+=\"[^\"]*\")|([\\w]+=[\\w]+)|([\\w]+)))?";
    private static final Pattern COMPLEX_HEADER_PATTERN =
        Pattern.compile(COMPLEX_HEADER_EXPRESSION);
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RetryAfterHeaderProvider.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(RetryAfterHeaderProvider.class);
    private static final Pattern DELTA_SECONDS_PATTERN = Pattern.compile("^(\\d+)$");
    private static final Pattern RFC_PATTERN = Pattern.compile("^\\s*([^,\\s]+)\\s*[,].+$");
    private final Clock clock;
    
```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
     */
    private static final class Variable extends UriChunk {
        private static final Pattern VARIABLE_PATTERN = Pattern.compile("(\\w[-\\w\\.]*[ ]*)(\\:(.+))?");
        private String name;
        private Pattern pattern;
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiService.java`
#### Snippet
```java
    private static final String FAVICON = "favicon";
    private static final Map<String, String> DEFAULT_MEDIA_TYPES;
    private static final Pattern URL_PATTERN = Pattern.compile("url[:]\\s*[\"]([^\"]+)[\"][,]");

    static {
```

### RegExpSimplifiable
`[\"]` can be simplified to '"'
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiService.java`
#### Snippet
```java
    private static final String FAVICON = "favicon";
    private static final Map<String, String> DEFAULT_MEDIA_TYPES;
    private static final Pattern URL_PATTERN = Pattern.compile("url[:]\\s*[\"]([^\"]+)[\"][,]");

    static {
```

### RegExpSimplifiable
`[\"]` can be simplified to '"'
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiService.java`
#### Snippet
```java
    private static final String FAVICON = "favicon";
    private static final Map<String, String> DEFAULT_MEDIA_TYPES;
    private static final Pattern URL_PATTERN = Pattern.compile("url[:]\\s*[\"]([^\"]+)[\"][,]");

    static {
```

### RegExpSimplifiable
`[,]` can be simplified to ','
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiService.java`
#### Snippet
```java
    private static final String FAVICON = "favicon";
    private static final Map<String, String> DEFAULT_MEDIA_TYPES;
    private static final Pattern URL_PATTERN = Pattern.compile("url[:]\\s*[\"]([^\"]+)[\"][,]");

    static {
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `core/src/main/resources/schemas/wsdl/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

### RegExpSimplifiable
`[:]` can be simplified to ':'
in `core/src/main/resources/schemas/wsdl/XMLSchema.xsd`
#### Snippet
```java
    </xs:annotation>
    <xs:restriction base="xs:Name">
      <xs:pattern value="[\i-[:]][\c-[:]]*" id="NCName.pattern">
        <xs:annotation>
          <xs:documentation
```

## RuleId[ruleID=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `key.substring(0, key.length() - 4)`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java
        if (key.endsWith("From")) {
            ct = ConditionType.GREATER_OR_EQUALS;
            key = key.substring(0, key.length() - 4);
        } else if (key.endsWith("Till")) {
            ct = ConditionType.LESS_OR_EQUALS;
```

### DuplicateExpressions
Multiple occurrences of `key.substring(0, key.length() - 4)`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java
        } else if (key.endsWith("Till")) {
            ct = ConditionType.LESS_OR_EQUALS;
            key = key.substring(0, key.length() - 4);
        } else {
            ct = ConditionType.EQUALS;
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        switch (spad) {
        case PAD_SIZE0:
            ob[p] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            return PAD_SIZE0;
        case PAD_SIZE4:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            return PAD_SIZE0;
        case PAD_SIZE4:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            return PAD_SIZE4;
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        case PAD_SIZE4:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            return PAD_SIZE4;
        case PAD_SIZE8:
```

### DuplicateExpressions
Multiple occurrences of `b0 << 2 & 0xfc | b1 >> 4 & 0x3`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            return PAD_SIZE4;
        case PAD_SIZE8:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p++] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            ob[p] = (byte)(b2 << 6 & 0xc0 | b3 & 0x3f);
```

### DuplicateExpressions
Multiple occurrences of `b1 << 4 & 0xf0 | b2 >> 2 & 0xf`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        case PAD_SIZE8:
            ob[p++] = (byte)(b0 << 2 & 0xfc | b1 >> 4 & 0x3);
            ob[p++] = (byte)(b1 << 4 & 0xf0 | b2 >> 2 & 0xf);
            ob[p] = (byte)(b2 << 6 & 0xc0 | b3 & 0x3f);
            return PAD_SIZE8;
```

### DuplicateExpressions
Multiple occurrences of `Character.TYPE.equals(type) || Character.class.equals(type)`
in `core/src/main/java/org/apache/cxf/common/util/PrimitiveUtils.java`
#### Snippet
```java

    public static <T> Object read(String value, Class<T> type) {
        if (!(Character.TYPE.equals(type) || Character.class.equals(type))
            && value != null && value.isEmpty()) {
            //pass empty string to number type will result in Exception
```

### DuplicateExpressions
Multiple occurrences of `Character.TYPE.equals(type) || Character.class.equals(type)`
in `core/src/main/java/org/apache/cxf/common/util/PrimitiveUtils.java`
#### Snippet
```java
        } else if (Boolean.TYPE.equals(type) || Boolean.class.equals(type)) {
            ret = Boolean.valueOf(value);
        } else if ((Character.TYPE.equals(type) || Character.class.equals(type)) && value != null) {
            ret = value.charAt(0);
        }
```

### DuplicateExpressions
Multiple occurrences of `base.toString().startsWith("file:") ? base.toString().subst...`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
                            uri = base;
                        } else {
                            tryClasspath(base.toString().startsWith("file:")
                                         ? base.toString().substring(5) : base.toString());
                        }
                    } catch (Throwable th) {
```

### DuplicateExpressions
Multiple occurrences of `base.toString().startsWith("file:") ? base.toString().substring...`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
                        }
                    } catch (Throwable th) {
                        tryClasspath(base.toString().startsWith("file:")
                                     ? base.toString().substring(5) : base.toString());
                    }
                } else {
```

### DuplicateExpressions
Multiple occurrences of `base.toString().startsWith("file:") ? base.toString().substring(5) ...`
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
                    }
                } else {
                    tryClasspath(base.toString().startsWith("file:")
                                 ? base.toString().substring(5) : base.toString());
                }
            } else {
```

### DuplicateExpressions
Multiple occurrences of `subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2) && (subType1.charAt(0)...`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                    || subTypeAfterPlus2 == null && subTypeAfterPlus1.equals(subType2);
                if (!isCompatible && !nullPossible) {
                    isCompatible = subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2)
                        && (subType1.charAt(0) == '*' || subType2.charAt(0) == '*');
                }

```

### DuplicateExpressions
Multiple occurrences of `subType1.charAt(0) == '*' || subType2.charAt(0) == '*'`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                if (!isCompatible && !nullPossible) {
                    isCompatible = subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2)
                        && (subType1.charAt(0) == '*' || subType2.charAt(0) == '*');
                }

```

### DuplicateExpressions
Multiple occurrences of `subType1.charAt(subType1.length() - 1) == '*' || subType2.charAt(sub...`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                    if (!isCompatible && !nullPossible) {
                        isCompatible = subTypeBeforePlus1.equalsIgnoreCase(subTypeBeforePlus2)
                            && (subType1.charAt(subType1.length() - 1) == '*'
                                || subType2.charAt(subType2.length() - 1) == '*');
                    }
                }
```

### DuplicateExpressions
Multiple occurrences of `subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2) && (subType1.charAt(0) == ...`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            if (subTypeAfterPlus1 != null && subTypeAfterPlus2 != null) {

                isCompatible = subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2)
                    && (subType1.charAt(0) == '*' || subType2.charAt(0) == '*');

                if (!isCompatible) {
```

### DuplicateExpressions
Multiple occurrences of `subType1.charAt(0) == '*' || subType2.charAt(0) == '*'`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java

                isCompatible = subTypeAfterPlus1.equalsIgnoreCase(subTypeAfterPlus2)
                    && (subType1.charAt(0) == '*' || subType2.charAt(0) == '*');

                if (!isCompatible) {
```

### DuplicateExpressions
Multiple occurrences of `subType1.charAt(subType1.length() - 1) == '*' || subType2.charAt(subType...`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java

                    isCompatible = subTypeBeforePlus1 != null && subTypeBeforePlus1.equalsIgnoreCase(subTypeBeforePlus2)
                        && (subType1.charAt(subType1.length() - 1) == '*'
                            || subType2.charAt(subType2.length() - 1) == '*');
                }
            }
```

### DuplicateExpressions
Multiple occurrences of `STSConstants.SYMMETRIC_KEY_KEYTYPE.equals(keyType) || STSConstants.PUBLIC_KEY_KEYTYP...`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/DefaultSubjectProvider.java`
#### Snippet
```java
        if (WSS4JConstants.WSS_SAML_TOKEN_TYPE.equals(tokenType)
            || WSS4JConstants.SAML_NS.equals(tokenType)) {
            if (STSConstants.SYMMETRIC_KEY_KEYTYPE.equals(keyType)
                || STSConstants.PUBLIC_KEY_KEYTYPE.equals(keyType)) {
                return SAML1Constants.CONF_HOLDER_KEY;
            }
```

### DuplicateExpressions
Multiple occurrences of `STSConstants.SYMMETRIC_KEY_KEYTYPE.equals(keyType) || STSConstants.PUBLIC_KEY_KEYTYPE.eq...`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/DefaultSubjectProvider.java`
#### Snippet
```java
            return SAML1Constants.CONF_BEARER;
        }
        if (STSConstants.SYMMETRIC_KEY_KEYTYPE.equals(keyType)
            || STSConstants.PUBLIC_KEY_KEYTYPE.equals(keyType)) {
            return SAML2Constants.CONF_HOLDER_KEY;
        }
```

### DuplicateExpressions
Multiple occurrences of `Character.toLowerCase(methodName.charAt(firstCharacter)) + methodName.substri...`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
        if (methodName.length() >= firstCharacter + 2) {
            if (!Character.isUpperCase(methodName.charAt(firstCharacter + 1))) {
                return Character.toLowerCase(methodName.charAt(firstCharacter))
                       + methodName.substring(firstCharacter + 1);
            }
            return methodName.substring(3);
```

### DuplicateExpressions
Multiple occurrences of `Character.toLowerCase(methodName.charAt(firstCharacter)) + methodName.substring(first...`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
            return methodName.substring(3);
        }
        return Character.toLowerCase(methodName.charAt(firstCharacter))
               + methodName.substring(firstCharacter + 1);
    }

```

## RuleId[ruleID=EmptySynchronizedStatement]
### EmptySynchronizedStatement
Empty `synchronized` statement
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        setException(future.cause());
                    }
                    synchronized (entity) {
                        //ensure entity is write in main thread
                    }
```

## RuleId[ruleID=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlHeaderOutInterceptor.java`
#### Snippet
```java
            Map<String, List<String>> headers = getHeaders(message);

            StringBuilder builder = new StringBuilder();
            builder.append("SAML").append(' ').append(encodedToken);
            headers.put("Authorization",
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/Claim.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder builder = new StringBuilder(64);
        builder.append("Claim [values=");
        builder.append(values);
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImplMBeanWrapper.java`
#### Snippet
```java

        //Use default domain name of server
        objectName = new StringBuilder(ManagementConstants.DEFAULT_DOMAIN_NAME).append(':')
                .append(ManagementConstants.BUS_ID_PROP).append('=').append(wqManager.getBus().getId()).append(',')
                .append("WorkQueueManager=").append(NAME_VALUE)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/service/model/MessagePartInfo.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder("[MessagePartInfo name=")
            .append(getName())
            .append(", ConcreteName=")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/service/model/BindingOperationInfo.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder("[BindingOperationInfo: ")
            .append(getName() == null ? "" : getName())
            .append(']').toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
    @Override
    public String toString() {
        return new StringBuilder("[OperationInfo: ")
            .append(opName)
            .append(']').toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/endpoint/ManagedEndpoint.java`
#### Snippet
```java
        String instanceId = (String)endpoint.get(INSTANCE_ID);
        if (StringUtils.isEmpty(instanceId)) {
            instanceId = new StringBuilder().append(endpoint.hashCode()).toString();
        }
        // Added the instance id to make the ObjectName unique
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/staxutils/transform/ParsingEvent.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder().append("Event(").
            append(event).append(", ").append(name).append(", ").append(value).append(')').
            toString();
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder(super.toString())
                .append(" [queue size: ").append(getSize())
                .append(", max size: ").append(maxQueueSize)
```

### StringBufferReplaceableByString
`StringBuilder buf` can be replaced with 'String'
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

            if (LOG.isLoggable(Level.FINE)) {
                StringBuilder buf = new StringBuilder(128).append("Constructing automatic work queue with:\n")
                        .append("max queue size: ").append(maxQueueSize).append('\n')
                        .append("initialThreads: ").append(initialThreads).append('\n')
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java
                getLog().debug(e);

                StringBuilder msg = new StringBuilder(128);
                msg.append(e.getMessage()).append('\n');
                msg.append("Try to run this goal using the <fork>true</fork> and "
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/SignatureHeaderUtils.java`
#### Snippet
```java
        String digest = Base64.getEncoder().encodeToString(messageDigest.digest());

        StringBuilder sb = new StringBuilder(digestAlgorithmName.length() + 1 + digest.length());
        sb.append(digestAlgorithmName).append('=').append(digest);
        return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureCreator.java`
#### Snippet
```java
        Signature outputSignature = signer.sign(method, uri, SignatureHeaderUtils.mapHeaders(messageHeaders));

        StringBuilder sb = new StringBuilder(128);
        sb.append("keyId=\"");
        sb.append(outputSignature.getKeyId());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java
            String ct = (String) mpi.getProperty(Message.CONTENT_TYPE);

            String id = new StringBuilder().append(partName)
                .append('=')
                .append(UUID.randomUUID())
```

### StringBufferReplaceableByString
`StringBuilder state` can be replaced with 'String'
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JoseSessionTokenProvider.java`
#### Snippet
```java
    protected String convertStateToString(OAuthRedirectionState secData) {
        //TODO: make it simpler, convert it to JwtClaims -> JSON
        StringBuilder state = new StringBuilder();
        // 0: client id
        state.append(ModelEncryptionSupport.tokenizeString(secData.getClientId()));
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/realm/RelationshipResolver.java`
#### Snippet
```java

    private String generateKey(String sourceRealm, String targetRealm) {
        return new StringBuilder().append(sourceRealm).append('>').append(targetRealm).toString();

    }
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/DefaultConditionsProvider.java`
#### Snippet
```java
                long requestedLifetime = Duration.between(creationTime, expirationTime).getSeconds();
                if (requestedLifetime > getMaxLifetime()) {
                    StringBuilder sb = new StringBuilder();
                    sb.append("Requested lifetime [").append(requestedLifetime);
                    sb.append(" sec] exceed configured maximum lifetime [").append(getMaxLifetime());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/jwt/DefaultJWTClaimsProvider.java`
#### Snippet
```java
            long requestedLifetime = Duration.between(creationTime, expirationTime).getSeconds();
            if (requestedLifetime > getMaxLifetime()) {
                StringBuilder sb = new StringBuilder();
                sb.append("Requested lifetime [").append(requestedLifetime);
                sb.append(" sec] exceed configured maximum lifetime [").append(getMaxLifetime());
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ProcessedClaim.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder builder = new StringBuilder(128);
        builder.append("ProcessedClaim [");
        builder.append(super.toString());
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ProviderFactory.java`
#### Snippet
```java
                        providers.add(provider);
                    } catch (AbstractDOMProvider.JSDOMProviderException ex) {
                        StringBuilder msg = new StringBuilder(f.getPath());
                        msg.append(": ").append(ex.getMessage());
                        throw new Exception(msg.toString());
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ProviderFactory.java`
#### Snippet
```java
            String type = e4x ? "E4X" : "JavaScript";
            String base = isBase ? "base " : "";
            StringBuilder msg = new StringBuilder("creating a ");
            msg.append(modestr)
                .append(' ')
```

### StringBufferReplaceableByString
`StringBuilder b` can be replaced with 'String'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/EprMetaData.java`
#### Snippet
```java
        String ret = null;
        if (isValid()) {
            StringBuilder b = new StringBuilder();
            b.append('{');
            b.append(binding.getQName());
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/CXFConnectionSpec.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder(128)
            .append("serviceName=").append(serviceName)
            .append(",endpointName=").append(endpointName)
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `integration/jca/src/main/java/org/apache/cxf/connector/CXFConnectionParam.java`
#### Snippet
```java

    public String toString() {
        return new StringBuilder(256)
            .append("Interface [").append(getInterface()).append("] ")
            .append("PortName [").append(getPortName()).append("] ")
```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty/WebSocketVirtualServletRequest.java`
#### Snippet
```java
        int pos = opathtrans.indexOf(opathinfo);
        //REVISIT may cache this value in requstHeaders?
        return new StringBuilder().append(opathtrans.substring(0, pos)).append(path).toString();
    }

```

### StringBufferReplaceableByString
`StringBuilder` can be replaced with 'String'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapPreProtocolOutInterceptor.java`
#### Snippet
```java

            if (ct.indexOf("action=\"") == -1) {
                ct = new StringBuilder().append(ct)
                    .append("; action=").append(action).toString();
                message.put(Message.CONTENT_TYPE, ct);
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WadlOption.java`
#### Snippet
```java

    public String toString() {
        StringBuilder builder = new StringBuilder(64);
        builder.append("WADL: ").append(wadl).append('\n');
        builder.append("OutputDir: ").append(outputDir).append('\n');
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
        }
        if (StringUtils.isNotEmpty(err.getOutput()) && err.getOutput().contains("WADL2Java Error")) {
            StringBuilder msg = new StringBuilder();
            msg.append(err.getOutput());
            msg.append('\n');
```

### StringBufferReplaceableByString
`StringBuffer` can be replaced with 'String'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
            }
        }
        return new StringBuffer().append(httpMethod).append('[').append(path).append(']').toString();
    }

```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `rt/management/src/main/java/org/apache/cxf/management/counters/MessageHandlingTimeRecorder.java`
#### Snippet
```java
    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder(256);
        sb.append("MessageHandlingTimeRecorder@").append(Integer.toHexString(hashCode())).append('[');
        sb.append("beginTime: ").append(beginTime).append(", ");
```

### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
    ObjectName getEndpointObjectName()
        throws MalformedObjectNameException, NullPointerException {
        StringBuilder buffer = new StringBuilder(128)
            .append(ManagementConstants.DEFAULT_DOMAIN_NAME).append(":type=Bus.Service.Endpoint,")
            .append(ManagementConstants.SERVICE_NAME_PROP).append("=\"").append(serviceName).append("\",")
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java
        String value;
        if (prefix != null && prefix.length() > 0) {
            StringBuilder sb = new StringBuilder(prefix.length() + 1 + type.getLocalPart().length());
            sb.append(prefix);
            sb.append(':');
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java

        if (exitCode != 0) {
            StringBuilder msg = new StringBuilder("\nExit code: ");
            msg.append(exitCode);
            msg.append('\n');
```

### StringBufferReplaceableByString
`StringBuilder msg` can be replaced with 'String'
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        file.delete();
        if (b.toString().contains("WSDL2Java Error")) {
            StringBuilder msg = new StringBuilder();
            msg.append(b.toString());
            msg.append('\n');
```

## RuleId[ruleID=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcRpAuthenticationFilter.java`
#### Snippet
```java
    public void filter(ContainerRequestContext rc) {
        if (checkSecurityContext(rc)) {
            return;
        } else if (redirectUri != null) {
            final UriBuilder redirectBuilder;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java
        public void setApplicationContext(ApplicationContext applicationContext) {
            if (bus != null) {
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a constructor
in `core/src/main/java/org/apache/cxf/staxutils/validation/WoodstoxValidationImpl.java`
#### Snippet
```java
        } catch (Throwable e) {
            LOG.log(Level.FINE, "Problem initializing MSV validation", e);
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `core/src/main/java/org/apache/cxf/attachment/ImageDataContentHandler.java`
#### Snippet
```java
                out.flush();
                out.close();
                return;
            }
        } else if (obj instanceof byte[]) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweJsonContainerRequestFilter.java`
#### Snippet
```java
        } catch (JweException ex) {
            context.abortWith(JAXRSUtils.toResponse(400));
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
            }
        } else if (cause instanceof SOAPFaultException && ((SOAPFaultException)cause).getFault().hasDetail()) {
            return;
        } else if (f instanceof SoapFault && f.getCause() instanceof SOAPFaultException
                && ((SOAPFaultException)f.getCause()).getFault().hasDetail()) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
        } else if (f instanceof SoapFault && f.getCause() instanceof SOAPFaultException
                && ((SOAPFaultException)f.getCause()).getFault().hasDetail()) {
            return;
        } else {
            FaultMode mode = message.get(FaultMode.class);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
                //next was found, don't resume everything, just the next one
                c.resume();
                return;
            }
            //next wasn't found, nothing to resume, assume it will come in later...
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMInInterceptor.java`
#### Snippet
```java
                Proxy proxy = rme.getProxy();
                proxy.createSequenceResponse(csr, protocol);
                return;
            }
        }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
        } else if (claz.getName().startsWith("java.")
            || claz.getName().startsWith("jakarta.") || claz.getName().startsWith("javax.")) {
            return;
        } else {
            Class<?> cls = JAXBUtils.getValidClass(claz);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a constructor
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesXsdValidationImpl.java`
#### Snippet
```java
        } catch (Throwable e) {
            /* If the dependencies are missing ... */
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java
        // for non-soap binding, the extensiblilityElement could be null
        if (extensiblilityElement == null) {
            return;
        }
        // for (ExtensibilityElement element : extensiblilityElement) {
```

## RuleId[ruleID=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `matches &= foundOne`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                            }
                        }
                        matches &= foundOne;
                    }
                }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `matches &= foundOne`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                            }
                        }
                        matches &= foundOne;
                    }
                }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl() || p1.get...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl()
                          || p1.getCacheControl().equals(p2.getCacheControl());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getContentEncoding(), p2.getContentEncoding())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentEncoding(), p2.getContentEncoding());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getContentLocation(), p2.getContentLocation())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentLocation(), p2.getContentLocation());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getContentType(), p2.getContentType())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentType(), p2.getContentType());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getRedirectURL(), p2.getRedirectURL())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getRedirectURL(), p2.getRedirectURL());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getServerType(), p2.getServerType())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getServerType(), p2.getServerType());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= p1.isHonorKeepAlive() == p2.isHonorKeepAlive()`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isHonorKeepAlive() == p2.isHonorKeepAlive();
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= p1.isSuppressClientReceiveErrors() == p2.isSuppressClientReceiveErrors()`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isSuppressClientReceiveErrors() == p2.isSuppressClientReceiveErrors();
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= p1.isSuppressClientSendErrors() == p2.isSuppressClientSendErrors()`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isSuppressClientSendErrors() == p2.isSuppressClientSendErrors();
        }
        if (compatible) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getKeepAliveParameters(), p2.getKeepAliveParameters())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
        }
        if (compatible) {
            compatible &= StringUtils.compatible(p1.getKeepAliveParameters(), p2.getKeepAliveParameters());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= (p1.isHonorKeepAlive() == p2.isHonorKeepAlive()) && (p1.getCacheControl(...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
        boolean result = true;

        result &= (p1.isHonorKeepAlive() == p2.isHonorKeepAlive())
                  && (p1.getCacheControl() == null ? p2.getCacheControl() == null : p1.getCacheControl()
                      .equals(p2.getCacheControl()))
                  && StringUtils.equals(p1.getContentEncoding(), p2.getContentEncoding())
                  && StringUtils.equals(p1.getContentLocation(), p2.getContentLocation())
                  && StringUtils.equals(p1.getContentType(), p2.getContentType())
                  && StringUtils.equals(p1.getKeepAliveParameters(), p2.getKeepAliveParameters());
        if (!result) {
            return false;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= (p1.getReceiveTimeout() == p2.getReceiveTimeout()) && StringUtils.equals...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
            return false;
        }
        result &= (p1.getReceiveTimeout() == p2.getReceiveTimeout())
                  && StringUtils.equals(p1.getRedirectURL(), p2.getRedirectURL())
                  && StringUtils.equals(p1.getServerType(), p2.getServerType())
                  && (p1.isSuppressClientReceiveErrors() == p2.isSuppressClientReceiveErrors())
                  && (p1.isSuppressClientSendErrors() == p2.isSuppressClientSendErrors());

        return result;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getAccept(), p2.getAccept())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAccept(), p2.getAccept());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getAcceptEncoding(), p2.getAcceptEncoding())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAcceptEncoding(), p2.getAcceptEncoding());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getAcceptLanguage(), p2.getAcceptLanguage())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAcceptLanguage(), p2.getAcceptLanguage());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getBrowserType(), p2.getBrowserType())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getBrowserType(), p2.getBrowserType());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl() || p1.get...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl()
                          || p1.getCacheControl().equals(p2.getCacheControl());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getCookie(), p2.getCookie())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getCookie(), p2.getCookie());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getDecoupledEndpoint(), p2.getDecoupledEndpoint())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getDecoupledEndpoint(), p2.getDecoupledEndpoint());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getHost(), p2.getHost())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getHost(), p2.getHost());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getProxyServer(), p2.getProxyServer())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getProxyServer(), p2.getProxyServer());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetProxyServerPort() || !p2.isSetProxyServerPort() || ...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetProxyServerPort() || !p2.isSetProxyServerPort()
                          || p1.getProxyServerPort().equals(p2.getProxyServerPort());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetProxyServerType() || !p2.isSetProxyServerType() || ...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetProxyServerType() || !p2.isSetProxyServerType()
                          || p1.getProxyServerType().equals(p2.getProxyServerType());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= StringUtils.compatible(p1.getReferer(), p2.getReferer())`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getReferer(), p2.getReferer());
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetAllowChunking() || !p2.isSetAllowChunking() |...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetAllowChunking()
                || !p2.isSetAllowChunking()
                || p1.isAllowChunking() == p2.isAllowChunking();
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `compatible &= !p1.isSetAutoRedirect() || !p2.isSetAutoRedirect() || ...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetAutoRedirect()
                || !p2.isSetAutoRedirect()
                || p1.isAutoRedirect() == p2.isAutoRedirect();
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= (p1.isAllowChunking() == p2.isAllowChunking()) && (p1.isAutoRedirect() =...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }
        boolean result = true;
        result &= (p1.isAllowChunking() == p2.isAllowChunking())
                  && (p1.isAutoRedirect() == p2.isAutoRedirect())
                  && StringUtils.equals(p1.getAccept(), p2.getAccept())
                  && StringUtils.equals(p1.getAcceptEncoding(), p2.getAcceptEncoding())
                  && StringUtils.equals(p1.getAcceptLanguage(), p2.getAcceptLanguage())
                  && StringUtils.equals(p1.getBrowserType(), p2.getBrowserType());
        if (!result) {
            return false;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= (p1.getCacheControl() == null ? p2.getCacheControl() == null : p1.getCacheControl() ...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }

        result &= (p1.getCacheControl() == null ? p2.getCacheControl() == null : p1.getCacheControl()
            .equals(p2.getCacheControl()) && p1.getConnection().value().equals(p2.getConnection().value()))
                  && (p1.getConnectionTimeout() == p2.getConnectionTimeout())
                  && StringUtils.equals(p1.getContentType(), p2.getContentType())
                  && StringUtils.equals(p1.getCookie(), p2.getCookie())
                  && StringUtils.equals(p1.getDecoupledEndpoint(), p2.getDecoupledEndpoint())
                  && StringUtils.equals(p1.getHost(), p2.getHost());
        if (!result) {
            return false;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= StringUtils.equals(p1.getProxyServer(), p2.getProxyServer()) && (p1.isSe...`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }

        result &= StringUtils.equals(p1.getProxyServer(), p2.getProxyServer())
                  && (p1.isSetProxyServerPort() ? p1.getProxyServerPort().equals(p2.getProxyServerPort()) : !p2
                      .isSetProxyServerPort())
                  && p1.getProxyServerType().value().equals(p2.getProxyServerType().value())
                  && (p1.getConnectionRequestTimeout() == p2.getConnectionRequestTimeout())
                  && (p1.getReceiveTimeout() == p2.getReceiveTimeout())
                  && StringUtils.equals(p1.getReferer(), p2.getReferer());

        return result;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `hasComponents |= true`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            if (securityDefinitions != null && !securityDefinitions.isEmpty()) {
                securityDefinitions.forEach(components::addSecuritySchemes);
                hasComponents |= true;
            }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `hasAfters[phase] |= !afterList.isEmpty()`
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java

            }
            hasAfters[phase] |= !afterList.isEmpty();

            if (firstBefore == null
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `val |= add(t)`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        boolean val = false;
        for (T t : c) {
            val |= add(t);
        }
        return val;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `val |= remove(t)`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        boolean val = false;
        for (Object t : c) {
            val |= remove(t);
        }
        return val;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `val |= remove(t)`
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        for (T t : this) {
            if (!c.contains(t)) {
                val |= remove(t);
            }
        }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `legalIdentifier &= Character.isJavaIdentifierPart(buf.charAt(i))`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

        for (int i = 1; i < name.length() && legalIdentifier; i++) {
            legalIdentifier &= Character.isJavaIdentifierPart(buf.charAt(i));
            hasUnderscore |= '_' == buf.charAt(i);
        }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `hasUnderscore |= '_' == buf.charAt(i)`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        for (int i = 1; i < name.length() && legalIdentifier; i++) {
            legalIdentifier &= Character.isJavaIdentifierPart(buf.charAt(i));
            hasUnderscore |= '_' == buf.charAt(i);
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= sslSocketFactory == that.sslSocketFactory`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            TLSClientParameters that = (TLSClientParameters)o;
            boolean eq = disableCNCheck == that.disableCNCheck;
            eq &= sslSocketFactory == that.sslSocketFactory;
            eq &= sslContext == that.sslContext;
            eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFactory;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= sslContext == that.sslContext`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            boolean eq = disableCNCheck == that.disableCNCheck;
            eq &= sslSocketFactory == that.sslSocketFactory;
            eq &= sslContext == that.sslContext;
            eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFactory;
            eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerifier;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFact...`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= sslSocketFactory == that.sslSocketFactory;
            eq &= sslContext == that.sslContext;
            eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFactory;
            eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerifier;
            eq &= sslCacheTimeout == that.sslCacheTimeout;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerif...`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= sslContext == that.sslContext;
            eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFactory;
            eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerifier;
            eq &= sslCacheTimeout == that.sslCacheTimeout;
            eq &= secureRandom == that.secureRandom;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= sslCacheTimeout == that.sslCacheTimeout`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= useHttpsURLConnectionDefaultSslSocketFactory == that.useHttpsURLConnectionDefaultSslSocketFactory;
            eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerifier;
            eq &= sslCacheTimeout == that.sslCacheTimeout;
            eq &= secureRandom == that.secureRandom;
            eq &= Objects.equals(certAlias, that.certAlias);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= secureRandom == that.secureRandom`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= useHttpsURLConnectionDefaultHostnameVerifier == that.useHttpsURLConnectionDefaultHostnameVerifier;
            eq &= sslCacheTimeout == that.sslCacheTimeout;
            eq &= secureRandom == that.secureRandom;
            eq &= Objects.equals(certAlias, that.certAlias);
            eq &= Objects.equals(protocol, that.protocol);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.equals(certAlias, that.certAlias)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= sslCacheTimeout == that.sslCacheTimeout;
            eq &= secureRandom == that.secureRandom;
            eq &= Objects.equals(certAlias, that.certAlias);
            eq &= Objects.equals(protocol, that.protocol);
            eq &= Objects.equals(provider, that.provider);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.equals(protocol, that.protocol)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= secureRandom == that.secureRandom;
            eq &= Objects.equals(certAlias, that.certAlias);
            eq &= Objects.equals(protocol, that.protocol);
            eq &= Objects.equals(provider, that.provider);
            eq &= equals(ciphersuites, that.ciphersuites);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.equals(provider, that.provider)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= Objects.equals(certAlias, that.certAlias);
            eq &= Objects.equals(protocol, that.protocol);
            eq &= Objects.equals(provider, that.provider);
            eq &= equals(ciphersuites, that.ciphersuites);
            eq &= Objects.deepEquals(keyManagers, that.keyManagers);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= equals(ciphersuites, that.ciphersuites)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= Objects.equals(protocol, that.protocol);
            eq &= Objects.equals(provider, that.provider);
            eq &= equals(ciphersuites, that.ciphersuites);
            eq &= Objects.deepEquals(keyManagers, that.keyManagers);
            eq &= Objects.deepEquals(trustManagers, that.trustManagers);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.deepEquals(keyManagers, that.keyManagers)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= Objects.equals(provider, that.provider);
            eq &= equals(ciphersuites, that.ciphersuites);
            eq &= Objects.deepEquals(keyManagers, that.keyManagers);
            eq &= Objects.deepEquals(trustManagers, that.trustManagers);
            if (cipherSuiteFilters != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.deepEquals(trustManagers, that.trustManagers)`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            eq &= equals(ciphersuites, that.ciphersuites);
            eq &= Objects.deepEquals(keyManagers, that.keyManagers);
            eq &= Objects.deepEquals(trustManagers, that.trustManagers);
            if (cipherSuiteFilters != null) {
                if (that.cipherSuiteFilters != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= equals(cipherSuiteFilters.getExclude(), that.cipherSuiteFilters.getExclude())`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            if (cipherSuiteFilters != null) {
                if (that.cipherSuiteFilters != null) {
                    eq &= equals(cipherSuiteFilters.getExclude(), that.cipherSuiteFilters.getExclude());
                    eq &= equals(cipherSuiteFilters.getInclude(), that.cipherSuiteFilters.getInclude());
                } else {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= equals(cipherSuiteFilters.getInclude(), that.cipherSuiteFilters.getInclude())`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
                if (that.cipherSuiteFilters != null) {
                    eq &= equals(cipherSuiteFilters.getExclude(), that.cipherSuiteFilters.getExclude());
                    eq &= equals(cipherSuiteFilters.getInclude(), that.cipherSuiteFilters.getInclude());
                } else {
                    eq = false;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= that.cipherSuiteFilters == null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
                }
            } else {
                eq &= that.cipherSuiteFilters == null;
            }
            if (certConstraints != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.equals(certConstraints.getIssuerDNConstraints(), that...`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
            if (certConstraints != null) {
                if (that.certConstraints != null) {
                    eq &= Objects.equals(certConstraints.getIssuerDNConstraints(),
                                 that.certConstraints.getIssuerDNConstraints());
                    eq &= Objects.equals(certConstraints.getSubjectDNConstraints(),
                                 that.certConstraints.getSubjectDNConstraints());
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= Objects.equals(certConstraints.getSubjectDNConstraints(), tha...`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
                    eq &= Objects.equals(certConstraints.getIssuerDNConstraints(),
                                 that.certConstraints.getIssuerDNConstraints());
                    eq &= Objects.equals(certConstraints.getSubjectDNConstraints(),
                                 that.certConstraints.getSubjectDNConstraints());
                } else {
                    eq = false;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `eq &= that.certConstraints == null`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParameters.java`
#### Snippet
```java
                }
            } else {
                eq &= that.certConstraints == null;
            }
            return eq;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `setBus |= parseAttribute(element, node, ctx, bean)`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
            Attr node = (Attr) atts.item(i);

            setBus |= parseAttribute(element, node, ctx, bean);
        }
        return setBus;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != ioThreadCount`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        int i = ioThreadCount;
        ioThreadCount = getInt(s.get(THREAD_COUNT), Runtime.getRuntime().availableProcessors());
        changed |= i != ioThreadCount;

        long l = selectInterval;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= l != selectInterval`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        long l = selectInterval;
        selectInterval = getInt(s.get(SELECT_INTERVAL), 1000);
        changed |= l != selectInterval;

        i = soLinger;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != soLinger`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        i = soLinger;
        soLinger = getInt(s.get(SO_LINGER), -1);
        changed |= i != soLinger;

        i = soTimeout;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != soTimeout`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        i = soTimeout;
        soTimeout = getInt(s.get(SO_TIMEOUT), 0);
        changed |= i != soTimeout;

        boolean b = interestOpQueued;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= b != interestOpQueued`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        boolean b = interestOpQueued;
        interestOpQueued = getBoolean(s.get(INTEREST_OP_QUEUED), false);
        changed |= b != interestOpQueued;

        b = tcpNoDelay;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= b != tcpNoDelay`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        b = tcpNoDelay;
        tcpNoDelay = getBoolean(s.get(TCP_NODELAY), true);
        changed |= b != tcpNoDelay;

        b = soKeepalive;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= b != soKeepalive`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        b = soKeepalive;
        soKeepalive = getBoolean(s.get(SO_KEEPALIVE), false);
        changed |= b != soKeepalive;

        return changed;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != ioThreadCount`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        int i = ioThreadCount;
        ioThreadCount = getInt(s.get(THREAD_COUNT), Runtime.getRuntime().availableProcessors());
        changed |= i != ioThreadCount;

        long l = selectInterval;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= l != selectInterval`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        long l = selectInterval;
        selectInterval = getInt(s.get(SELECT_INTERVAL), 1000);
        changed |= l != selectInterval;

        i = soLinger;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != soLinger`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        i = soLinger;
        soLinger = getInt(s.get(SO_LINGER), -1);
        changed |= i != soLinger;

        i = soTimeout;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= i != soTimeout`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        i = soTimeout;
        soTimeout = getInt(s.get(SO_TIMEOUT), 0);
        changed |= i != soTimeout;

        boolean b = tcpNoDelay;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= b != tcpNoDelay`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        boolean b = tcpNoDelay;
        tcpNoDelay = getBoolean(s.get(TCP_NODELAY), true);
        changed |= b != tcpNoDelay;

        b = soKeepalive;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `changed |= b != soKeepalive`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        b = soKeepalive;
        soKeepalive = getBoolean(s.get(SO_KEEPALIVE), false);
        changed |= b != soKeepalive;

        return changed;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkInitiatorTokens(binding.getInitiatorToken(), ai, aim, hasDerivedKeys, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        boolean result = true;
        if (binding.getInitiatorToken() != null) {
            result &= checkInitiatorTokens(binding.getInitiatorToken(), ai, aim, hasDerivedKeys,
                                        signedResults, encryptedResults);
        }
        if (binding.getInitiatorSignatureToken() != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkInitiatorTokens(binding.getInitiatorSignatureToken(), ai, aim, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        }
        if (binding.getInitiatorSignatureToken() != null) {
            result &= checkInitiatorTokens(binding.getInitiatorSignatureToken(), ai, aim,
                                        hasDerivedKeys, signedResults, encryptedResults);
        }
        if (binding.getInitiatorEncryptionToken() != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkInitiatorTokens(binding.getInitiatorEncryptionToken(), ai, aim, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        }
        if (binding.getInitiatorEncryptionToken() != null) {
            result &= checkInitiatorTokens(binding.getInitiatorEncryptionToken(), ai, aim,
                                        hasDerivedKeys, signedResults, encryptedResults);
        }
        if (binding.getRecipientToken() != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkRecipientTokens(binding.getRecipientToken(), ai, aim, hasDerivedKeys, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        }
        if (binding.getRecipientToken() != null) {
            result &= checkRecipientTokens(binding.getRecipientToken(), ai, aim, hasDerivedKeys,
                                        signedResults, encryptedResults);
        }
        if (binding.getRecipientSignatureToken() != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkRecipientTokens(binding.getRecipientSignatureToken(), ai, aim, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        }
        if (binding.getRecipientSignatureToken() != null) {
            result &= checkRecipientTokens(binding.getRecipientSignatureToken(), ai, aim,
                                        hasDerivedKeys, signedResults, encryptedResults);
        }
        if (binding.getRecipientEncryptionToken() != null) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `result &= checkRecipientTokens(binding.getRecipientEncryptionToken(), ai, aim, ...`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        }
        if (binding.getRecipientEncryptionToken() != null) {
            result &= checkRecipientTokens(binding.getRecipientEncryptionToken(), ai, aim,
                                        hasDerivedKeys, signedResults, encryptedResults);
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `pass &= supportsAlternative(a, errors)`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionInfoMap.java`
#### Snippet
```java
        boolean pass = true;
        for (PolicyComponent a : alternative) {
            pass &= supportsAlternative(a, errors);
        }
        return pass;
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `pass &= supportsAlternative(p2, errors)`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionInfoMap.java`
#### Snippet
```java
                    List<Assertion> pc = alternatives.next();
                    for (Assertion p2 : pc) {
                        pass &= supportsAlternative(p2, errors);
                    }
                }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `b |= Boolean.getBoolean("com.sun.xml.ws.transport.local.LocalTransportPipe.dump")`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/LoggingBusLifecycleListener.java`
#### Snippet
```java
                b = Boolean.parseBoolean(prop);
                //treat these all the same
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.local.LocalTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.util.pipe.StandaloneTubeAssembler.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.client.HttpTransportPipe.dump");
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `b |= Boolean.getBoolean("com.sun.xml.ws.util.pipe.StandaloneTubeAssembler.dump")`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/LoggingBusLifecycleListener.java`
#### Snippet
```java
                //treat these all the same
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.local.LocalTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.util.pipe.StandaloneTubeAssembler.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.client.HttpTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.HttpAdapter.dump");
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.client.HttpTransportPipe.dump")`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/LoggingBusLifecycleListener.java`
#### Snippet
```java
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.local.LocalTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.util.pipe.StandaloneTubeAssembler.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.client.HttpTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.HttpAdapter.dump");
            }
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.HttpAdapter.dump")`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/LoggingBusLifecycleListener.java`
#### Snippet
```java
                b |= Boolean.getBoolean("com.sun.xml.ws.util.pipe.StandaloneTubeAssembler.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.client.HttpTransportPipe.dump");
                b |= Boolean.getBoolean("com.sun.xml.ws.transport.http.HttpAdapter.dump");
            }
        } catch (Throwable t) {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `needXmime |= type.usesXmime()`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            }
            writeTypeReference(name, element, type, root);
            needXmime |= type.usesXmime();
            needUtilityTypes |= type.usesUtilityTypes();

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `needUtilityTypes |= type.usesUtilityTypes()`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            writeTypeReference(name, element, type, root);
            needXmime |= type.usesXmime();
            needUtilityTypes |= type.usesUtilityTypes();

        }
```

## RuleId[ruleID=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
        Objects.requireNonNull(output, "The writer is required but null value was provided");
        final char[] buffer = new char[4096];
        int n = atLeast > buffer.length ? buffer.length : atLeast;
        n = input.read(buffer, 0, n);
        while (-1 != n) {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
                return;
            }
            n = atLeast > buffer.length ? buffer.length : atLeast;
            n = input.read(buffer, 0, n);
        }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
        Objects.requireNonNull(output, "The outputStream is required but null value was provided");
        final byte[] buffer = new byte[4096];
        int n = atLeast > buffer.length ? buffer.length : atLeast;
        n = input.read(buffer, 0, n);
        while (-1 != n) {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
                return;
            }
            n = atLeast > buffer.length ? buffer.length : atLeast;
            n = input.read(buffer, 0, n);
        }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
        Objects.requireNonNull(input, "The inputStream is required but null value was provided");
        final byte[] buffer = new byte[4096];
        int n = atLeast > buffer.length ? buffer.length : atLeast;
        n = input.read(buffer, 0, n);
        while (-1 != n) {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java
                return;
            }
            n = atLeast > buffer.length ? buffer.length : atLeast;
            n = input.read(buffer, 0, n);
        }
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

    public void setLowWaterMark(int lwm) {
        lowWaterMark = lwm < 0 ? 0 : lwm;
        if (executor != null) {
            notifyChangeListeners(new PropertyChangeEvent(this, "lowWaterMark",
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
                // if the content delimiter is present in the value, extract the content
                int d = value.indexOf('}');
                d = value.indexOf('=', d < 0 ? 0 : d);
                if (d > 0) {
                    text = value.substring(d + 1);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/IoSessionInputStream.java`
#### Snippet
```java
        }
        int readBytes;
        if (len > buf.remaining()) {
            readBytes = buf.remaining();
        } else {
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionInfoMap.java`
#### Snippet
```java

    public AssertionInfoMap(Collection<? extends Assertion> assertions) {
        super(assertions.size() < 6 ? 6 : assertions.size());
        for (Assertion a : assertions) {
            putAssertionInfo(a);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweOutputStream.java`
#### Snippet
```java
        if (lastRawDataChunk != null) {
            int remaining = blockSize - lastRawDataChunk.length;
            int lenToCopy = remaining < len ? remaining : len;
            lastRawDataChunk = newArray(lastRawDataChunk, 0, lastRawDataChunk.length, b, off, lenToCopy);
            off = off + lenToCopy;
```

## RuleId[ruleID=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `CXFService` has no concrete subclass
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/CXFService.java`
#### Snippet
```java
 *
 */
public abstract class CXFService extends Service {
    ServiceImpl impl;

```

### AbstractClassNeverImplemented
Abstract class `AbstractAuthorizationCodeDataProvider` has no concrete subclass
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AbstractAuthorizationCodeDataProvider.java`
#### Snippet
```java
 * Abstract AuthorizationCodeDataProvider implementation
 */
public abstract class AbstractAuthorizationCodeDataProvider
    extends AbstractOAuthDataProvider implements AuthorizationCodeDataProvider {

```

### AbstractClassNeverImplemented
Abstract class `AbstractCodeDataProvider` has no concrete subclass
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AbstractCodeDataProvider.java`
#### Snippet
```java
import org.apache.cxf.rs.security.oauth2.provider.OAuthServiceException;

public abstract class AbstractCodeDataProvider extends AbstractOAuthDataProvider
    implements AuthorizationCodeDataProvider {
    private long codeLifetime = 10 * 60;
```

## RuleId[ruleID=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/FlowableRxInvokerImpl.java`
#### Snippet
```java
    
    @Override
    public <T> Flowable<T> method(String name, Entity<?> entity, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/FlowableRxInvokerImpl.java`
#### Snippet
```java
    }
    
    private <T> Flowable<T> create(Supplier<T> supplier) {
        Flowable<T> flowable = Flowable.create(new FlowableOnSubscribe<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, Entity<?> entity, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/ObservableRxInvokerImpl.java`
#### Snippet
```java
    }
    
    private <T> Observable<T> create(Supplier<T> supplier) {
        Observable<T> observable = Observable.create(new ObservableOnSubscribe<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/ObservableRxInvokerImpl.java`
#### Snippet
```java
    
    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends JettyHTTPServerEngine`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    }

    public void setEnginesList(List<JettyHTTPServerEngine> enginesList) {
        for (JettyHTTPServerEngine engine : enginesList) {
            if (engine.getPort() == FALLBACK_THREADING_PARAMS_KEY) {
```

### BoundedWildcard
Can generalize to `? extends TLSServerParameters`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    }
    public JettyHTTPServerEngineFactory(Bus b,
                                        Map<String, TLSServerParameters> tls,
                                        Map<String, ThreadingParameters> threading) {
        tlsParametersMap.putAll(tls);
```

### BoundedWildcard
Can generalize to `? extends ThreadingParameters`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    public JettyHTTPServerEngineFactory(Bus b,
                                        Map<String, TLSServerParameters> tls,
                                        Map<String, ThreadingParameters> threading) {
        tlsParametersMap.putAll(tls);
        threadingParametersMap.putAll(threading);
```

### BoundedWildcard
Can generalize to `? extends Principal`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/claims/SAMLSecurityContext.java`
#### Snippet
```java
    public SAMLSecurityContext(
        Principal principal,
        Set<Principal> roles,
        ClaimCollection claims
    ) {
```

### BoundedWildcard
Can generalize to `? extends Principal`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/claims/SAMLSecurityContext.java`
#### Snippet
```java
    }

    public void setUserRoles(Set<Principal> userRoles) {
        this.roles = userRoles;
    }
```

### BoundedWildcard
Can generalize to `? super ParticleInfo`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
     * @param elements
     */
    private void getElementsForParts(MessageInfo message, List<ParticleInfo> elements) {
        for (MessagePartInfo mpi : message.getMessageParts()) {
            XmlSchemaElement element = null;
```

### BoundedWildcard
Can generalize to `? extends AttributeType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static ResourceType createResourceType(
        List<AttributeType> attributes,
        ResourceContentType resourceContent
    ) {
```

### BoundedWildcard
Can generalize to `? extends AttributeType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static EnvironmentType createEnvironmentType(
        List<AttributeType> attributes
    ) {
        if (environmentTypeBuilder == null) {
```

### BoundedWildcard
Can generalize to `? extends AttributeValueType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
        String dataType,
        String issuer,
        List<AttributeValueType> attributeValues
    ) {
        if (attributeTypeBuilder == null) {
```

### BoundedWildcard
Can generalize to `? extends AttributeType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static ActionType createActionType(
        List<AttributeType> attributes
    ) {
        if (actionTypeBuilder == null) {
```

### BoundedWildcard
Can generalize to `? extends SubjectType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static RequestType createRequestType(
        List<SubjectType> subjects,
        List<ResourceType> resources,
        ActionType action,
```

### BoundedWildcard
Can generalize to `? extends ResourceType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    public static RequestType createRequestType(
        List<SubjectType> subjects,
        List<ResourceType> resources,
        ActionType action,
        EnvironmentType environment
```

### BoundedWildcard
Can generalize to `? extends AttributeType`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/RequestComponentBuilder.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public static SubjectType createSubjectType(
        List<AttributeType> attributes,
        String subjectCategory
    ) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
     * enabled.
     */
    static String toString(Map<String, List<Object>> headers, boolean logSensitiveHeaders) {
        Map<String, List<Object>> filteredHeaders = new TreeMap<>(String.CASE_INSENSITIVE_ORDER);
        filteredHeaders.putAll(headers);
```

### BoundedWildcard
Can generalize to `? extends Cookie`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
     * @param sessionCookies
     */
    public void writeSessionCookies(Map<String, Cookie> sessionCookies) {
        List<String> cookies = null;
        for (String s : headers.keySet()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java

        private <T extends Exception> T mapException(String msg,
                                                     T ex, Class<T> cls) {
            T ex2;
            try {
```

### BoundedWildcard
Can generalize to `? super ProviderInfo`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/ContractComparator.java`
#### Snippet
```java

    ContractComparator(MicroProfileClientFactoryBean microProfileClientFactoryBean,
                       Comparator<ProviderInfo<?>> parent) {
        this.microProfileClientFactoryBean = microProfileClientFactoryBean;
        this.parent = parent;
```

### BoundedWildcard
Can generalize to `? extends InterceptorInvoker`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/MPRestClientInvocationContextImpl.java`
#### Snippet
```java
     */
    MPRestClientInvocationContextImpl(Object target, Method method, Object[] args, 
                                      List<InterceptorInvoker> interceptorInvokers, 
                                      Callable<Object> callable) {
        this.target = target;
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIFacade.java`
#### Snippet
```java
    }

    private static <T> Optional<T> nullableOptional(Callable<T> callable) {
        if (!CDI_AVAILABLE) {
            return Optional.empty();
```

### BoundedWildcard
Can generalize to `? super ProviderInfo`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
    }

    private MicroProfileClientProviderFactory(Bus bus, Comparator<ProviderInfo<?>> comparator) {
        super(bus);
        this.comparator = comparator;
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
    }

    private static Annotation[] merge(List<Annotation> methodBindings, List<Annotation> classBindings) {
        Set<Class<? extends Annotation>> types = methodBindings.stream()
                                                               .map(a -> a.annotationType())
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
    }

    private static Annotation[] merge(List<Annotation> methodBindings, List<Annotation> classBindings) {
        Set<Class<? extends Annotation>> types = methodBindings.stream()
                                                               .map(a -> a.annotationType())
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
    }

    private static List<Annotation> getBindings(Set<Annotation> annotations, BeanManager beanManager) {
        if (annotations == null || annotations.isEmpty()) {
            return Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
        }

        private boolean isEventInResults(Event event, List<SecurityEvent> incomingSecurityEventList) {
            for (SecurityEvent incomingEvent : incomingSecurityEventList) {
                if (event == incomingEvent.getSecurityEventType()) {
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
    }

    private SecurityToken getSignatureToken(List<SecurityEvent> incomingSecurityEventList)
        throws XMLSecurityException {
        if (incomingSecurityEventList != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/RequestAssertionConsumerFilter.java`
#### Snippet
```java

    protected boolean processParams(ContainerRequestContext ct,
                                 MultivaluedMap<String, String> params,
                                 boolean postBinding) {
        String encodedSamlResponse = params.getFirst(SSOConstants.SAML_RESPONSE);
```

### BoundedWildcard
Can generalize to `? super T`
in `rt/rs/extensions/reactivestreams/src/main/java/org/apache/cxf/jaxrs/reactivestreams/server/StreamingAsyncSubscriber.java`
#### Snippet
```java

        @Override
        public void writeTo(Writer<T> writer) throws IOException {

            while (!completed || !queue.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends AuthnContextClassRef`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlpRequestComponentBuilder.java`
#### Snippet
```java
    public static RequestedAuthnContext createRequestedAuthnCtxPolicy(
        AuthnContextComparisonTypeEnumeration comparison,
        List<AuthnContextClassRef> authnCtxClassRefList,
        List<AuthnContextDeclRef> authnCtxDeclRefList
    ) {
```

### BoundedWildcard
Can generalize to `? extends AuthnContextDeclRef`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlpRequestComponentBuilder.java`
#### Snippet
```java
        AuthnContextComparisonTypeEnumeration comparison,
        List<AuthnContextClassRef> authnCtxClassRefList,
        List<AuthnContextDeclRef> authnCtxDeclRefList
    ) {
        if (requestedAuthnCtxBuilder == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
    }

    public void setReplayCache(TokenReplayCache<String> replayCache) {
        this.replayCache = replayCache;
    }
```

### BoundedWildcard
Can generalize to `? extends AudienceRestriction`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java

    private boolean matchSaml2AudienceRestriction(
        String appliesTo, List<AudienceRestriction> audienceRestrictions
    ) {
        boolean oneMatchFound = false;
```

### BoundedWildcard
Can generalize to `? extends Endpoint`
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/AbstractStaticFailoverStrategy.java`
#### Snippet
```java
     * @return the selected endpoint or {@code null} if no alternate endpoint is available
     */
    public Endpoint selectAlternateEndpoint(List<Endpoint> alternates) {
        Endpoint selected = null;
        if (alternates != null && !alternates.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? super Annotation`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/provider/DefaultTimedAnnotationProvider.java`
#### Snippet
```java

    private static void findAllAnnotations(final AnnotatedElement annotatedElement,
                                           final Set<Annotation> foundAnnotations) {
        for (Annotation annotation : annotatedElement.getDeclaredAnnotations()) {
            if (!foundAnnotations.contains(annotation)) {
```

### BoundedWildcard
Can generalize to `? extends Iterable`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerMetricsContext.java`
#### Snippet
```java
    }

    private void stop(Timer.Sample timerSample, Supplier<Iterable<Tag>> tags, Timer.Builder builder) {
        timerSample.stop(builder.tags(tags.get()).register(this.registry));
    }
```

### BoundedWildcard
Can generalize to `? extends TagsCustomizer`
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerMetricsContext.java`
#### Snippet
```java
    MicrometerMetricsContext(MeterRegistry registry, TagsProvider tagsProvider,
                             TimedAnnotationProvider timedAnnotationProvider,
                             List<TagsCustomizer> tagsCustomizers, String metricName, boolean autoTimeRequests) {
        this.registry = registry;
        this.tagsProvider = tagsProvider;
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
     * @param annotations
     */
    private void applyBeanValidatorAnnotations(final Parameter parameter, final List<Annotation> annotations) {
        Map<String, Annotation> annos = new HashMap<>();
        if (annotations != null) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
    }

    public List<T> findAll(Collection<T> pojos) {
        List<T> result = new ArrayList<>();
        for (T pojo : pojos) {
```

### BoundedWildcard
Can generalize to `? extends UndertowHTTPServerEngine`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
    }

    public void setEnginesList(List<UndertowHTTPServerEngine> enginesList) {
        for (UndertowHTTPServerEngine engine : enginesList) {
            if (engine.getPort() == FALLBACK_THREADING_PARAMS_KEY) {
```

### BoundedWildcard
Can generalize to `? extends TLSServerParameters`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
    }
    public UndertowHTTPServerEngineFactory(Bus b,
                                        Map<String, TLSServerParameters> tls,
                                        Map<String, ThreadingParameters> threading) {
        tlsParametersMap.putAll(tls);
```

### BoundedWildcard
Can generalize to `? extends ThreadingParameters`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
    public UndertowHTTPServerEngineFactory(Bus b,
                                        Map<String, TLSServerParameters> tls,
                                        Map<String, ThreadingParameters> threading) {
        tlsParametersMap.putAll(tls);
        threadingParametersMap.putAll(threading);
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractComplexCondition.java`
#### Snippet
```java
    }

    public List<T> findAll(Collection<T> pojos) {
        List<T> result = new ArrayList<>();
        for (T pojo : pojos) {
```

### BoundedWildcard
Can generalize to `? extends SecurityScheme`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
        }

        private static Optional<Components> registerComponents(Map<String, SecurityScheme> securityDefinitions) {
            final Components components = new Components();

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java

    private <S extends Temporal> S convertTo(String value, String pattern, Boolean timezoneSupported,
            BiFunction<String, DateTimeFormatter, S> parser) throws ParseException {
        final DateTimeFormatter df = DateTimeFormatter.ofPattern(SearchUtils
            .getDateFormatOrDefault(contextProperties, pattern)
```

### BoundedWildcard
Can generalize to `? super DateTimeFormatter`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java

    private <S extends Temporal> S convertTo(String value, String pattern, Boolean timezoneSupported,
            BiFunction<String, DateTimeFormatter, S> parser) throws ParseException {
        final DateTimeFormatter df = DateTimeFormatter.ofPattern(SearchUtils
            .getDateFormatOrDefault(contextProperties, pattern)
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SearchContextImpl.java`
#### Snippet
```java

    private CompleteCondition getOrCondition(SearchConditionBuilder builder,
                                             Map.Entry<String, List<String>> entry) {
        String key = entry.getKey();
        final ConditionType ct;
```

### BoundedWildcard
Can generalize to `? extends Parser`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/tika/TikaContentExtractor.java`
#### Snippet
```java
     * @param parsers parser instances
     */
    public TikaContentExtractor(final List<Parser> parsers, Detector detector) {
        this.parsers = parsers;
        this.detector = detector;
```

### BoundedWildcard
Can generalize to `? extends TypeInfo`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
    private List<SearchCondition<T>> createConditions(Map<String, ConditionType> getters2operators,
                                                      Map<String, String> realGetters,
                                                      Map<String, TypeInfo> propertyTypeInfo,
                                                      ConditionType sharedType) {
        if (isBuiltIn(condition)) {
```

### BoundedWildcard
Can generalize to `? super T`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java
    }

    private Object getValue(Beanspector<T> beanspector, String getter, T pojo) {
        try {
            return beanspector.swap(pojo).getValue(getter);
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/SimpleSearchCondition.java`
#### Snippet
```java

    @Override
    public List<T> findAll(Collection<T> pojos) {
        List<T> result = new ArrayList<>();
        for (T pojo : pojos) {
```

### BoundedWildcard
Can generalize to `? extends SingularAttribute`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java
    }

    private List<Selection<?>> toSelectionsList(List<SingularAttribute<T, ?>> attributes, boolean setAlias) {
        List<Selection<?>> selections = new ArrayList<>(attributes.size());
        for (SingularAttribute<T, ?> attr : attributes) {
```

### BoundedWildcard
Can generalize to `? extends SingularAttribute`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java
    }

    public CriteriaQuery<E> orderBy(List<SingularAttribute<T, ?>> attributes, boolean asc) {
        CriteriaBuilder cb = getCriteriaBuilder();

```

### BoundedWildcard
Can generalize to `? extends ClaimBean`
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/interceptor/ClaimsAuthorizingInterceptor.java`
#### Snippet
```java
    }

    private static boolean isClaimOverridden(ClaimBean bean, List<ClaimBean> mClaims) {
        for (ClaimBean methodBean : mClaims) {
            if (bean.getClaim().getClaimType().equals(methodBean.getClaim().getClaimType())
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/interceptor/ClaimsAuthorizingInterceptor.java`
#### Snippet
```java
    }

    public void setClaims(Map<String, List<ClaimBean>> claimsMap) {
        claims.putAll(claimsMap);
    }
```

### BoundedWildcard
Can generalize to `? extends Query`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
    }

    private Query createCompositeQuery(List<Query> queries, boolean orCondition) {

        BooleanClause.Occur clause = orCondition
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClientCodeRequestFilter.java`
#### Snippet
```java
    @Override
    protected void setAdditionalCodeRequestParams(UriBuilder ub,
                                                  MultivaluedMap<String, String> redirectState,
                                                  MultivaluedMap<String, String> codeRequestState) {
        if (redirectState != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClientCodeRequestFilter.java`
#### Snippet
```java
    protected void setAdditionalCodeRequestParams(UriBuilder ub,
                                                  MultivaluedMap<String, String> redirectState,
                                                  MultivaluedMap<String, String> codeRequestState) {
        if (redirectState != null) {
            if (redirectState.getFirst(IdToken.NONCE_CLAIM) != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClientCodeRequestFilter.java`
#### Snippet
```java
    protected ClientTokenContext createTokenContext(ContainerRequestContext rc,
                                                    ClientAccessToken at,
                                                    MultivaluedMap<String, String> requestParams,
                                                    MultivaluedMap<String, String> state) {
        if (rc.getSecurityContext() instanceof OidcSecurityContext) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClientCodeRequestFilter.java`
#### Snippet
```java
    }

    private void validateIdToken(IdToken idToken, MultivaluedMap<String, String> state) {

        String nonce = state.getFirst(IdToken.NONCE_CLAIM);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/OidcConfigurationService.java`
#### Snippet
```java

    @Override
    protected void prepareConfigurationData(Map<String, Object> cfg, String baseUri) {
        super.prepareConfigurationData(cfg, baseUri);
        // UriInfo Endpoint
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java
    }
    public static void setStateClaimsProperty(OAuthRedirectionState state,
                                              MultivaluedMap<String, String> params) {
        String claims = params.getFirst(OidcUtils.CLAIMS_PARAM);
        if (claims != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java

    }
    public static List<String> getPromptValues(MultivaluedMap<String, String> params) {
        String prompt = params.getFirst(PROMPT_PARAMETER);
        if (prompt != null) {
```

### BoundedWildcard
Can generalize to `? extends Map`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
        setAddClaimSources(sources);
    }
    private void setAddClaimSources(Map<String, Map<String, String>> newSources) {
        Map<String, Map<String, String>> sources =
            CastUtils.cast((Map<?, ?>)getProperty(OidcUtils.CLAIM_SOURCES_PROPERTY));
```

### BoundedWildcard
Can generalize to `? super QName`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
     * @param from source map
     */
    private static void putAll(Map<QName, String> to, Map<QName, String> from) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
     * @param from source map
     */
    private static void putAll(Map<QName, String> to, Map<QName, String> from) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? extends QName`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
     * @param from source map
     */
    private static void putAll(Map<QName, String> to, Map<QName, String> from) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java

        @Override
        public void setOutInterceptors(List<Interceptor<? extends Message>> interceptors) {
            if (bus != null) {
                bus.getOutInterceptors().addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java

        @Override
        public void setInFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
            if (bus != null) {
                bus.getInFaultInterceptors().addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java

        @Override
        public void setOutFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
            if (bus != null) {
                bus.getOutFaultInterceptors().addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java

        @Override
        public void setInInterceptors(List<Interceptor<? extends Message>> interceptors) {
            if (bus != null) {
                bus.getInInterceptors().addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java


    public <T> boolean loadBeansOfType(Class<T> type,
                                       BeanLoaderListener<T> listener) {
        List<String> list = new ArrayList<>(Arrays.asList(context.getBeanNamesForType(type,
```

### BoundedWildcard
Can generalize to `? super T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java

    public <T> boolean loadBeansOfType(Class<T> type,
                                       BeanLoaderListener<T> listener) {
        List<String> list = new ArrayList<>(Arrays.asList(context.getBeanNamesForType(type,
                                                                                            false,
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
    }

    public <T> List<T> getOSGiServices(Class<T> type) {
        List<T> lst = new ArrayList<>();
        if (!osgi) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java

    /** {@inheritDoc}*/
    public <T> Collection<? extends T> getBeansOfType(Class<T> type) {
        Set<String> s = new LinkedHashSet<>(Arrays.asList(context.getBeanNamesForType(type,
                                                                                            false,
```

### BoundedWildcard
Can generalize to `? super T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        return lst;
    }
    private <T> void tryOSGI(Collection<T> lst, Class<T> type) {
        if (!osgi || bundleContext == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        return lst;
    }
    private <T> void tryOSGI(Collection<T> lst, Class<T> type) {
        if (!osgi || bundleContext == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends Phase`
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
    }

    public PhaseInterceptorChain(SortedSet<Phase> ps) {
        state = State.EXECUTING;
        isFineLogging = LOG.isLoggable(Level.FINE);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
    }

    public void add(Collection<Interceptor<? extends Message>> newhandlers, boolean force) {
        if (newhandlers == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
        return null;
    }
    public <T> Collection<? extends T> getBeansOfType(Class<T> type) {
        List<T> ret = new LinkedList<>();
        Extension ext = all.get(type.getName());
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
        return ret;
    }
    public <T> boolean loadBeansOfType(Class<T> type, BeanLoaderListener<T> listener) {
        boolean loaded = false;
        for (Extension ex : ordered) {
```

### BoundedWildcard
Can generalize to `? super Class`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
    public ExtensionManagerImpl(String[] resources,
                                ClassLoader cl,
                                Map<Class<?>, Object> initialExtensions,
                                ResourceManager rm,
                                Bus b) {
```

### BoundedWildcard
Can generalize to `? super JavaFileObject`
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java

    protected boolean internalJava6Compile(JavaCompiler compiler, JavaFileManager fileManager,
                                           DiagnosticListener<JavaFileObject> listener,
                                           Iterable<? extends JavaFileObject> fileList) {
        List<String> args = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
    }

    private void checkLongClasspath(String classpath, List<String> list, int classpathIdx) {
        if (isLongClasspath(classpath)) {
            try {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
    }

    protected void addArgs(List<String> list) {
        if (!StringUtils.isEmpty(encoding)) {
            list.add("-encoding");
```

### BoundedWildcard
Can generalize to `? extends File`
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
        return compileFiles(f.toArray(new String[0]));
    }
    public boolean compileFiles(List<File> files) {
        List<String> f = new ArrayList<>(files.size());
        for (File file : files) {
```

### BoundedWildcard
Can generalize to `? extends X`
in `core/src/main/java/org/apache/cxf/common/util/CollectionUtils.java`
#### Snippet
```java
            private final Iterator<X> iter;

            IteratorToEnumeration(Iterator<X> iter) {
                this.iter = iter;
            }
```

### BoundedWildcard
Can generalize to `? extends DocumentationProvider`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        docProviders.add(p);
    }
    public void setDocumentationProvider(List<DocumentationProvider> ps) {
        docProviders.addAll(ps);
    }
```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                                  Parameter pm,
                                  String parentName,
                                  List<Class<?>> parentBeanClasses,
                                  boolean isJson) {
        Map<Parameter, Class<?>> pms = InjectionUtils.getParametersFromBeanClass(type, pm.getType(), true);
```

### BoundedWildcard
Can generalize to `? extends OperationResourceInfo`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    protected List<OperationResourceInfo> sortOperationsByPath(Set<OperationResourceInfo> ops) {
        List<OperationResourceInfo> opsWithSamePath = new LinkedList<>(ops);
        Collections.sort(opsWithSamePath, new Comparator<OperationResourceInfo>() {
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

    protected void handleGrammars(StringBuilder sbApp, StringBuilder sbGrammars, SchemaWriter writer,
                                  Map<Class<?>, QName> clsMap) {
        if (writer == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    private String getPrefix(String ns, Map<Class<?>, QName> clsMap) {
        String prefix = null;
        int index = 0;
```

### BoundedWildcard
Can generalize to `? super ClassResourceInfo`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    protected void handleResource(StringBuilder sb, Set<Class<?>> jaxbTypes,
                                  ElementQNameResolver qnameResolver, Map<Class<?>, QName> clsMap,
                                  ClassResourceInfo cri, Set<ClassResourceInfo> visitedResources,
                                  boolean isJson) {
        visitedResources.add(cri);
```

### BoundedWildcard
Can generalize to `? extends Element`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    private void handleExistingDocRefs(List<Element> elements, String attrName, String parentDocLoc,
                                       String parentRef, Message m, UriInfo ui) {
        if (keepRelativeDocLinks) {
```

### BoundedWildcard
Can generalize to `? extends XmlSchemaAttributeOrGroupRef`
in `core/src/main/java/org/apache/cxf/common/xmlschema/SchemaCollection.java`
#### Snippet
```java
    }

    private void addCrossImportsAttributeList(XmlSchema schema, List<XmlSchemaAttributeOrGroupRef> list) {
        for (XmlSchemaAttributeOrGroupRef attr : list) {
            final QName ref;
```

### BoundedWildcard
Can generalize to `? super Method`
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java

    private void addAnnotatedMethods(Class<? extends Annotation> acls, Method[] methods,
        Collection<Method> annotatedMethods) {
        for (Method method : methods) {
            if (method.getAnnotation(acls) != null
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java
    }

    public static <T> Class<? extends T> loadClass(String className, Class<?> callingClass, Class<T> type)
        throws ClassNotFoundException {
        try {
```

### BoundedWildcard
Can generalize to `? extends List`
in `core/src/main/java/org/apache/cxf/helpers/HttpHeaderHelper.java`
#### Snippet
```java
    }

    public static List<String> getHeader(Map<String, List<String>> headerMap, String key) {
        return headerMap.get(getHeaderKey(key));
    }
```

### BoundedWildcard
Can generalize to `? super Class`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
    }

    public static void addPackage(Set<Class<?>>  classes, String pkg, ClassLoader loader) {
        try {
            classes.add(Class.forName(pkg + ".ObjectFactory", false, loader));
```

### BoundedWildcard
Can generalize to `? super Class`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
    // an ObjectFactory is pointed to by an jaxb @XmlElementDecl annotation
    // added this workaround method to load the jaxb needed ObjectFactory class
    private static boolean addJaxbObjectFactory(JAXBException e1, Set<Class<?>> classes) {
        boolean added = false;
        java.io.ByteArrayOutputStream bout = new java.io.ByteArrayOutputStream();
```

### BoundedWildcard
Can generalize to `? super Element`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
    }

    private static void findAllElementsByTagName(Element el, String tagName, List<Element> elementList) {

        if (tagName.equals(el.getTagName())) {
```

### BoundedWildcard
Can generalize to `? super Element`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java

    private static void findAllElementsByTagNameNS(Element el, String nameSpaceURI, String localName,
                                                   List<Element> elementList) {

        if (el.getNamespaceURI() != null && localName.equals(el.getLocalName())
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
     * @param prefixes
     */
    public static void getPrefixes(Element element, String namespaceUri, List<String> prefixes) {
        NamedNodeMap atts = element.getAttributes();
        for (int i = 0; i < atts.getLength(); i++) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/service/model/EndpointInfo.java`
#### Snippet
```java

    @Override
    public <T> T getTraversedExtensor(T defaultValue, Class<T> type) {
        T value = getExtensor(type);

```

### BoundedWildcard
Can generalize to `? extends ServiceInfo`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
    }

    public static EndpointInfo findBestEndpointInfo(QName qn, List<ServiceInfo> serviceInfos) {
        for (ServiceInfo serviceInfo : serviceInfos) {
            Collection<EndpointInfo> eps = serviceInfo.getEndpoints();
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        }

        public DefaultClassNameAllocator(Set<String> set) {
            typesClassNames = set;
        }
```

### BoundedWildcard
Can generalize to `? super Package`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    private static void addToObjectFactoryCache(Package objectFactoryPkg,
                                         Class<?> ofactory,
                                         Map<Package, CachedClass> objectFactoryCache) {
        if (objectFactoryPkg == null || objectFactoryCache == null) {
            return;
```

### BoundedWildcard
Can generalize to `? super CachedClass`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
    private static void addToObjectFactoryCache(Package objectFactoryPkg,
                                         Class<?> ofactory,
                                         Map<Package, CachedClass> objectFactoryCache) {
        if (objectFactoryPkg == null || objectFactoryCache == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends DOMResult`
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

    public static List<DOMResult> generateJaxbSchemas(
        JAXBContext context, final Map<String, DOMResult> builtIns) throws IOException {
        final List<DOMResult> results = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends ResourceResolver`
in `core/src/main/java/org/apache/cxf/resource/DefaultResourceManager.java`
#### Snippet
```java

    private <T> T findResource(String name, Class<T> type, boolean asStream,
                               List<ResourceResolver> resolvers) {
        if (!firstCalled) {
            onFirstResolve();
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/staxutils/validation/Stax2ValidationUtils.java`
#### Snippet
```java
    }

    private void addSchema(Map<String, Source> sources, XmlSchema schema, Element element)
            throws XMLStreamException {
        String schemaSystemId = schema.getSourceURI();
```

### BoundedWildcard
Can generalize to `? super Source`
in `core/src/main/java/org/apache/cxf/staxutils/validation/Stax2ValidationUtils.java`
#### Snippet
```java
    }

    private void addSchema(Map<String, Source> sources, XmlSchema schema, Element element)
            throws XMLStreamException {
        String schemaSystemId = schema.getSourceURI();
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java

    public TransportFinder(Bus b,
                           Map<String, T> m,
                           Set<String> l,
                           Class<T> c) {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/transport/TransportFinder.java`
#### Snippet
```java
    public TransportFinder(Bus b,
                           Map<String, T> m,
                           Set<String> l,
                           Class<T> c) {
        map = m;
```

### BoundedWildcard
Can generalize to `? extends DelayedTaskWrapper`
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
        AtomicBoolean shutdown = new AtomicBoolean(false);

        WatchDog(DelayQueue<DelayedTaskWrapper> queue) {
            delayQueue = queue;
        }
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `core/src/main/java/org/apache/cxf/attachment/LazyDataSource.java`
#### Snippet
```java
    private String id;

    public LazyDataSource(String id, Collection<Attachment> attachments) {
        super();
        this.id = id;
```

### BoundedWildcard
Can generalize to `? extends List`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        return null;
    }
    static String getHeader(Map<String, List<String>> headers, String h) {
        return getHeaderValue(headers.get(h));
    }
```

### BoundedWildcard
Can generalize to `? extends List`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        return getHeaderValue(headers.get(h));
    }
    static String getHeader(Map<String, List<String>> headers, String h, String delim) {
        return getHeaderValue(headers.get(h), delim);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/attachment/AttachmentDeserializer.java`
#### Snippet
```java
    }

    private void addHeaderLine(Map<String, List<String>> heads, StringBuilder line) {
        // null lines are a nop
        final int size = line.length();
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
                              BindingOperationInfo oi,
                              Object[] params,
                              Map<String, Object> context,
                              Exchange exchange) throws Exception {
        Bus origBus = BusFactory.getAndSetThreadDefaultBus(bus);
```

### BoundedWildcard
Can generalize to `? extends T`
in `core/src/main/java/org/apache/cxf/interceptor/AnnotationInterceptors.java`
#### Snippet
```java

    private <T> List<T> initializeAnnotationObjects(Annotation annotation,
                                             Class<T> type) {
        List<T> list = new ArrayList<>();
        for (String cn : getAnnotationObjectNames(annotation)) {
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractBasicInterceptorProvider.java`
#### Snippet
```java
    }

    public void setInFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
        inFault.clear();
        inFault.addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractBasicInterceptorProvider.java`
#### Snippet
```java
    }

    public void setOutFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
        outFault.clear();
        outFault.addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractBasicInterceptorProvider.java`
#### Snippet
```java
    }

    public void setOutInterceptors(List<Interceptor<? extends Message>> interceptors) {
        out.clear();
        out.addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/interceptor/AbstractBasicInterceptorProvider.java`
#### Snippet
```java
    }

    public void setInInterceptors(List<Interceptor<? extends Message>> interceptors) {
        in.clear();
        in.addAll(interceptors);
```

### BoundedWildcard
Can generalize to `? extends CallbackHandlerProvider`
in `core/src/main/java/org/apache/cxf/interceptor/security/JAASLoginInterceptor.java`
#### Snippet
```java
    }

    public void addCallbackHandlerProviders(List<CallbackHandlerProvider> callbackHandlerProviders2) {
        this.callbackHandlerProviders.addAll(callbackHandlerProviders2);
    }
```

### BoundedWildcard
Can generalize to `? extends CallbackHandlerProvider`
in `core/src/main/java/org/apache/cxf/interceptor/security/JAASLoginInterceptor.java`
#### Snippet
```java
    }

    public void setCallbackHandlerProviders(List<CallbackHandlerProvider> callbackHandlerProviders) {
        this.callbackHandlerProviders.clear();
        this.callbackHandlerProviders.addAll(callbackHandlerProviders);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `core/src/main/java/org/apache/cxf/interceptor/security/DelegatingAuthenticationInterceptor.java`
#### Snippet
```java
    }

    public void setSchemeHandlers(Map<String, Interceptor<Message>> handlers) {
        this.authenticationHandlers = handlers;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/interceptor/security/SecureAnnotationsInterceptor.java`
#### Snippet
```java
    }

    protected void findRoles(Class<?> cls, Map<String, String> rolesMap) {
        if (cls == null || cls == Object.class) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/interceptor/security/SecureAnnotationsInterceptor.java`
#### Snippet
```java
    }

    protected void findRoles(Class<?> cls, Map<String, String> rolesMap) {
        if (cls == null || cls == Object.class) {
            return;
```

### BoundedWildcard
Can generalize to `? super QName`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java

    protected static void convertToSetOfQNames(List<String> set,
                                               Set<QName> elementsSet) {
        if (set != null) {
            for (String entry : set) {
```

### BoundedWildcard
Can generalize to `? super QName`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java

    static void convertToMapOfElementProperties(Map<String, String> map,
                                                Map<QName, ElementProperty> elementsMap) {
        if (map != null) {
            for (Map.Entry<String, String> entry : map.entrySet()) {
```

### BoundedWildcard
Can generalize to `? super ElementProperty`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java

    static void convertToMapOfElementProperties(Map<String, String> map,
                                                Map<QName, ElementProperty> elementsMap) {
        if (map != null) {
            for (Map.Entry<String, String> entry : map.entrySet()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
    protected static void convertToQNamesMap(Map<String, String> map,
                                             QNamesMap elementsMap,
                                             Map<String, String> nsMap) {
        if (map != null) {
            for (Map.Entry<String, String> entry : map.entrySet()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `core/src/main/java/org/apache/cxf/staxutils/transform/TransformUtils.java`
#### Snippet
```java
    protected static void convertToQNamesMap(Map<String, String> map,
                                             QNamesMap elementsMap,
                                             Map<String, String> nsMap) {
        if (map != null) {
            for (Map.Entry<String, String> entry : map.entrySet()) {
```

### BoundedWildcard
Can generalize to `? super Pattern`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
    }

    private static void compileRegexPatterns(List<Pattern> patterns, List<String> regexes,
                                             boolean include, Logger log) {
        if (regexes != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/utils/PrefixCollectingXMLStreamWriter.java`
#### Snippet
```java

    public PrefixCollectingXMLStreamWriter(XMLStreamWriter target,
                                           Map<String, String> namespaces) {
        this.target = target;
        this.namespaces = namespaces;
```

### BoundedWildcard
Can generalize to `? extends NettyHttpServerEngine`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngineFactory.java`
#### Snippet
```java
    }

    public void setEnginesList(List<NettyHttpServerEngine> enginesList) {
        for (NettyHttpServerEngine engine : enginesList) {
            portMap.putIfAbsent(engine.getPort(), engine);
```

### BoundedWildcard
Can generalize to `? extends Lifecycle`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/PerRequestResourceProvider.java`
#### Snippet
```java
    private final Class<?> clazz;

    PerRequestResourceProvider(final Supplier<Lifecycle> lifecycle, final Class<?> clazz) {
        this.lifecycle = lifecycle;
        this.clazz = clazz;
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/DelegateContextAnnotatedType.java`
#### Snippet
```java
        }

        private Set<Annotation> processAnnotations(Set<Annotation> annotations) {
            Set<Annotation> resultAnnotations = new LinkedHashSet<>();
            for (Annotation a : annotations) {
```

### BoundedWildcard
Can generalize to `? extends ResourceTypeIdentifier`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/validationtransformation/ValidAndTransformHelper.java`
#### Snippet
```java
     */
    public static void validationAndTransformation(
            List<ResourceTypeIdentifier> resourceTypeIdentifiers,
            Representation newRepresentation,
            Representation oldRepresentation) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/utils/JSONUtils.java`
#### Snippet
```java
    }

    public static Configuration createConfiguration(ConcurrentHashMap<String, String> namespaceMap,
                                                    boolean writeXsiType,
                                                    boolean attributesAsElements,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/utils/JSONUtils.java`
#### Snippet
```java
    }

    public static Configuration createConfiguration(ConcurrentHashMap<String, String> namespaceMap,
                                                    boolean writeXsiType,
                                                    boolean attributesAsElements,
```

### BoundedWildcard
Can generalize to `? extends Feature`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/JAXRSCdiResourceExtension.java`
#### Snippet
```java
        }

        public void addFeatures(final Collection< Feature > others) {
            this.features.addAll(others);
        }
```

### BoundedWildcard
Can generalize to `? extends Bean`
in `integration/cdi/src/main/java/org/apache/cxf/cdi/JAXRSCdiResourceExtension.java`
#### Snippet
```java
     */
    private List< Object > loadBeans(final BeanManager beanManager, Collection<Class<?>> limitedClasses,
                                     Collection<Bean<?>> beans) {
        final List< Object > instances = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? extends Server`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/ColocOutInterceptor.java`
#### Snippet
```java
    }

    protected Server isColocated(List<Server> servers, Endpoint endpoint, BindingOperationInfo boi) {
        if (servers != null) {
            Service senderService = endpoint.getService();
```

### BoundedWildcard
Can generalize to `? super Node`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
    }

    private void walkDom(String pfx, Element element, Binder<Node> binder, Object parent) {
        try {
            Object o = binder.getJAXBNode(element);
```

### BoundedWildcard
Can generalize to `? extends FaultInfo`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/ColocUtil.java`
#### Snippet
```java
    }

    public static boolean isSameFaultInfo(Collection<FaultInfo> fil1,
                                          Collection<FaultInfo> fil2) {
        if ((fil1 == null && fil2 != null)
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenRetriever.java`
#### Snippet
```java
    }

    private static void mapSecurityProps(Message message, Map<String, Object> ctx) {
        for (String s : SecurityConstants.ALL_PROPERTIES) {
            Object v = message.getContextualProperty(s + ".it");
```

### BoundedWildcard
Can generalize to `? super org.apache.wss4j.common.ext.Attachment`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java

    private void loadAttachments(
        List<org.apache.wss4j.common.ext.Attachment> attachmentList,
        String attachmentId,
        boolean removeAttachments
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
    }

    private SubjectAndPrincipalSecurityToken getSubjectPrincipalToken(List<SecurityEvent> incomingSecurityEventList,
                                                                      Event desiredEvent,
                                                                      Message msg) throws XMLSecurityException {
```

### BoundedWildcard
Can generalize to `? extends WSHandlerResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java
     * Get the certificate that was used to sign the request
     */
    public static X509Certificate getReqSigCert(List<WSHandlerResult> results) {
        if (results == null || results.isEmpty()) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends org.apache.wss4j.policy.model.AlgorithmSuite`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AlgorithmSuiteTranslater.java`
#### Snippet
```java
     */
    private AlgorithmSuite translateAlgorithmSuites(
        List<org.apache.wss4j.policy.model.AlgorithmSuite> algorithmSuites
    ) {
        AlgorithmSuite algorithmSuite = null;
```

### BoundedWildcard
Can generalize to `? extends AbstractBinding`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AlgorithmSuiteTranslater.java`
#### Snippet
```java
     */
    private List<org.apache.wss4j.policy.model.AlgorithmSuite> getAlgorithmSuites(
        List<AbstractBinding> bindings
    ) {
        List<org.apache.wss4j.policy.model.AlgorithmSuite> algorithmSuites = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private void checkEncryptedUsernameToken(List<SecurityEvent> results) throws WSSecurityException {
        if (!encryptUsernameToken) {
            return;
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private void checkSignedUsernameToken(List<SecurityEvent> results) throws WSSecurityException {
        if (!signUsernameToken) {
            return;
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java

    private void checkSignedAddressing(
        List<SecurityEvent> results,
        AddressingProperties addressingProperties
    ) throws WSSecurityException {
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private void checkSignedBody(List<SecurityEvent> results) throws WSSecurityException {
        if (!signBody) {
            return;
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private void checkSignedTimestamp(List<SecurityEvent> results) throws WSSecurityException {
        if (!signTimestamp) {
            return;
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private boolean isUsernameToken(List<QName> qnames) {
        return qnames != null && qnames.size() == 4
            && isEnvelope(qnames.get(0))
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private boolean isBody(List<QName> qnames) {
        return qnames != null && qnames.size() == 2
            && isEnvelope(qnames.get(0))
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private boolean isTimestamp(List<QName> qnames) {
        return qnames != null
            && qnames.size() == 4
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private void checkEncryptedBody(List<SecurityEvent> results) throws WSSecurityException {
        if (!encryptBody) {
            return;
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private List<SecurityEvent> getEventFromResults(Event event, List<SecurityEvent> incomingSecurityEventList) {
        List<SecurityEvent> results = new ArrayList<>();
        for (SecurityEvent incomingEvent : incomingSecurityEventList) {
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private boolean isFaultTo(List<QName> qnames) {
        return qnames != null && qnames.size() == 3
            && isEnvelope(qnames.get(0))
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    }

    private boolean isReplyTo(List<QName> qnames) {
        return qnames != null && qnames.size() == 3
            && isEnvelope(qnames.get(0))
```

### BoundedWildcard
Can generalize to `? extends XPathExpression`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageChecker.java`
#### Snippet
```java
     * @param xpaths the XPath expressions to check for
     */
    public final void addXPaths(List<XPathExpression> xpaths) {
        if (xpaths != null) {
            this.xPaths.addAll(xpaths);
```

### BoundedWildcard
Can generalize to `? extends XPathExpression`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageChecker.java`
#### Snippet
```java
     * @param xpaths the XPath expressions to check for
     */
    public final void setXPaths(List<XPathExpression> xpaths) {
        this.xPaths.clear();
        if (xpaths != null) {
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxActionInInterceptor.java`
#### Snippet
```java
    }

    private boolean isEventInResults(Event event, List<SecurityEvent> incomingSecurityEventList) {
        for (SecurityEvent incomingEvent : incomingSecurityEventList) {
            if (event == incomingEvent.getSecurityEventType()) {
```

### BoundedWildcard
Can generalize to `? extends XMLSecurityConstants.Action`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxActionInInterceptor.java`
#### Snippet
```java
    private final List<XMLSecurityConstants.Action> inActions;

    public StaxActionInInterceptor(List<XMLSecurityConstants.Action> inActions) {
        super(Phase.PRE_PROTOCOL);
        this.inActions = inActions;
```

### BoundedWildcard
Can generalize to `? extends SecurityEvent`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxActionInInterceptor.java`
#### Snippet
```java
    }

    private SecurityEvent findEvent(Event event, List<SecurityEvent> incomingSecurityEventList) {
        for (SecurityEvent incomingEvent : incomingSecurityEventList) {
            if (event == incomingEvent.getSecurityEventType()) {
```

### BoundedWildcard
Can generalize to `? extends SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxAsymmetricBindingHandler.java`
#### Snippet
```java

    private void doEncryption(AbstractTokenWrapper recToken,
                                    List<SecurePart> encrParts) throws SOAPException {
        //Do encryption
        if (recToken != null && recToken.getToken() != null && !encrParts.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxAsymmetricBindingHandler.java`
#### Snippet
```java
    }

    private void doSignature(AbstractTokenWrapper wrapper, List<SecurePart> sigParts)
        throws WSSecurityException, SOAPException {

```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EndorsingTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            SupportingTokens binding = (SupportingTokens)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
    }

    public void setOutInterceptors(List<Interceptor<? extends Message>> interceptors) {
        getOutInterceptors().addAll(interceptors);
    }
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
    }

    public void setInFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
        getInFaultInterceptors().addAll(interceptors);
    }
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
    }

    public void setOutFaultInterceptors(List<Interceptor<? extends Message>> interceptors) {
        getOutFaultInterceptors().addAll(interceptors);
    }
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
    }

    public void setInInterceptors(List<Interceptor<? extends Message>> interceptors) {
        getInInterceptors().addAll(interceptors);
    }
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AlgorithmSuitePolicyValidator.java`
#### Snippet
```java

    private boolean validatePolicy(
        AssertionInfo ai, AlgorithmSuite algorithmPolicy, List<WSSecurityEngineResult> results
    ) {

```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AlgorithmSuitePolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            AlgorithmSuite algorithmSuite = (AlgorithmSuite)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends WSDataRef`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AlgorithmSuitePolicyValidator.java`
#### Snippet
```java
     */
    private boolean checkDataRefs(
        List<WSDataRef> dataRefs,
        AlgorithmSuite algorithmPolicy,
        AssertionInfo ai
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEndorsingEncryptedTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        // Tokens must be encrypted even if TLS is used unless we have a TransportBinding policy available
        if (isTLSInUse(parameters.getMessage())) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EndorsingEncryptedTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        // Tokens must be encrypted even if TLS is used unless we have a TransportBinding policy available
        if (isTLSInUse(parameters.getMessage())) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EncryptedTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        // Tokens must be encrypted even if TLS is used unless we have a TransportBinding policy available
        if (isTLSInUse(parameters.getMessage())) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            SupportingTokens binding = (SupportingTokens)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SecurityContextTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        List<WSSecurityEngineResult> sctResults =
            parameters.getResults().getActionResults().get(WSConstants.SCT);
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/WSS11PolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        List<WSSecurityEngineResult> scResults =
            parameters.getResults().getActionResults().get(WSConstants.SC);
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEncryptedTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        // Tokens must be encrypted even if TLS is used unless we have a TransportBinding policy available
        if (isTLSInUse(parameters.getMessage())) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SecuredPartsPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        //
        // SIGNED_PARTS and ENCRYPTED_PARTS only apply to non-Transport bindings
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/RequiredPartsPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        Element header = parameters.getSoapHeader();

```

### BoundedWildcard
Can generalize to `? extends SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxSymmetricBindingHandler.java`
#### Snippet
```java
    }

    private void doSignature(AbstractTokenWrapper wrapper, AbstractToken policyToken, List<SecurePart> sigParts)
        throws WSSecurityException, SOAPException {

```

### BoundedWildcard
Can generalize to `? extends SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxSymmetricBindingHandler.java`
#### Snippet
```java

    private void doEncryption(AbstractTokenWrapper recToken,
                              List<SecurePart> encrParts) throws SOAPException {
        //Do encryption
        if (recToken != null && recToken.getToken() != null) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
     * Note that results are stored in the reverse order.
     */
    private boolean isEncryptedBeforeSigned(List<WSSecurityEngineResult> results) {
        boolean encrypted = false;
        for (WSSecurityEngineResult result : results) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
    private WSSecurityEngineResult findCorrespondingToken(
        WSSecurityEngineResult signatureResult,
        List<WSSecurityEngineResult> results
    ) {
        // See what was used to sign this result
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
    protected boolean isTokenProtected(
        List<WSSecurityEngineResult> results,
        List<WSSecurityEngineResult> signedResults
    ) {
        for (WSSecurityEngineResult result : signedResults) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
     * Return true if the given Element was encrypted
     */
    private boolean isElementEncrypted(Element element, List<WSSecurityEngineResult> results) {
        for (WSSecurityEngineResult wser : results) {
            Integer actInt = (Integer)wser.get(WSSecurityEngineResult.TAG_ACTION);
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
     * Note that results are stored in the reverse order.
     */
    private boolean isSignedBeforeEncrypted(List<WSSecurityEngineResult> results) {
        boolean signed = false;
        for (WSSecurityEngineResult result : results) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
        boolean transportBinding,
        WSHandlerResult results,
        List<WSSecurityEngineResult> signedResults,
        Message message
    ) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractBindingPolicyValidator.java`
#### Snippet
```java
     */
    protected boolean validateEntireHeaderAndBodySignatures(
        List<WSSecurityEngineResult> signedResults
    ) {
        for (WSSecurityEngineResult signedResult : signedResults) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SymmetricBindingPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        boolean hasDerivedKeys =
            parameters.getResults().getActionResults().containsKey(WSConstants.DKT);
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java

    protected Map<AbstractToken, SecurePart> handleSupportingTokens(
        Collection<AssertionInfo> tokenAssertions,
        boolean signed,
        boolean endorse
```

### BoundedWildcard
Can generalize to `? super SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
    }

    protected void addSignatureConfirmation(List<SecurePart> sigParts) {
        Wss10 wss10 = getWss10();

```

### BoundedWildcard
Can generalize to `? super AbstractToken`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java

    protected void handleUsernameTokenSupportingToken(
         UsernameToken token, boolean endorse, boolean encryptedToken, Map<AbstractToken, SecurePart> ret
    ) throws Exception {
        if (endorse) {
```

### BoundedWildcard
Can generalize to `? super SecurePart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java

    protected void handleUsernameTokenSupportingToken(
         UsernameToken token, boolean endorse, boolean encryptedToken, Map<AbstractToken, SecurePart> ret
    ) throws Exception {
        if (endorse) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java

    private boolean validateStrictSignaturePlacement(
        List<WSSecurityEngineResult> results,
        List<WSSecurityEngineResult> signedResults
    ) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
    private boolean validateStrictSignaturePlacement(
        List<WSSecurityEngineResult> results,
        List<WSSecurityEngineResult> signedResults
    ) {
        // Go through each Signature and check any security header token is before the Signature
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
    private int findCorrespondingTokenIndex(
        WSSecurityEngineResult signatureResult,
        List<WSSecurityEngineResult> results
    ) {
        // See what was used to sign this result
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            Layout layout = (Layout)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
    }

    private boolean isEndorsingSignatureInCorrectPlace(List<WSSecurityEngineResult> results,
                                              WSSecurityEngineResult signedResult,
                                              Element protectedElement) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/LayoutPolicyValidator.java`
#### Snippet
```java
    private boolean checkSignatureIsSignedPlacement(
        List<WSSecurityEngineResult> results,
        List<WSSecurityEngineResult> signedResults
    ) {
        for (WSSecurityEngineResult signedResult : signedResults) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SamlTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            SamlToken samlToken = (SamlToken)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/KerberosTokenPolicyValidator.java`
#### Snippet
```java
    }

    private List<WSSecurityEngineResult> findKerberosResults(List<WSSecurityEngineResult> bstResults) {
        List<WSSecurityEngineResult> results = new ArrayList<>();
        if (bstResults != null) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/KerberosTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        List<WSSecurityEngineResult> kerberosResults =
            findKerberosResults(parameters.getResults().getActionResults().get(WSConstants.BST));
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/X509TokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        List<WSSecurityEngineResult> bstResults =
            parameters.getResults().getActionResults().get(WSConstants.BST);
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/X509TokenPolicyValidator.java`
#### Snippet
```java
    private boolean checkTokenType(
        TokenType tokenType,
        List<WSSecurityEngineResult> bstResults,
        List<WSSecurityEngineResult> signedResults
    ) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/X509TokenPolicyValidator.java`
#### Snippet
```java
        TokenType tokenType,
        List<WSSecurityEngineResult> bstResults,
        List<WSSecurityEngineResult> signedResults
    ) {
        if ((bstResults == null || bstResults.isEmpty()) && signedResults.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/RequiredElementsPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            RequiredElements rp = (RequiredElements)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/UsernameTokenPolicyValidator.java`
#### Snippet
```java
        org.apache.wss4j.policy.model.UsernameToken usernameTokenPolicy,
        AssertionInfo ai,
        List<WSSecurityEngineResult> utResults
    ) {
        for (WSSecurityEngineResult result : utResults) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/UsernameTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies. W
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            org.apache.wss4j.policy.model.UsernameToken usernameTokenPolicy =
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/TransportBindingPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            TransportBinding binding = (TransportBinding)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/ConcreteSupportingTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            SupportingTokens binding = (SupportingTokens)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEndorsingTokenPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        for (AssertionInfo ai : ais) {
            SupportingTokens binding = (SupportingTokens)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SecuredElementsPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {

        // XPathFactory and XPath are not thread-safe so we must recreate them
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/IssuedTokenPolicyValidator.java`
#### Snippet
```java
    private boolean validateBinarySecurityToken(PolicyValidatorParameters parameters,
                                                BinarySecurity binarySecurity,
                                                Collection<AssertionInfo> ais) {
        boolean asserted = true;
        for (AssertionInfo ai : ais) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/IssuedTokenPolicyValidator.java`
#### Snippet
```java
    private boolean validateSAMLToken(PolicyValidatorParameters parameters,
                                      SamlAssertionWrapper samlAssertion,
                                      Collection<AssertionInfo> ais) {
        boolean asserted = true;
        for (AssertionInfo ai : ais) {
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
     * Validate policies.
     */
    public void validatePolicies(PolicyValidatorParameters parameters, Collection<AssertionInfo> ais) {
        boolean hasDerivedKeys =
            parameters.getResults().getActionResults().containsKey(WSConstants.DKT);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationTokenInterceptorProvider.java`
#### Snippet
```java
    }

    private static void mapSecurityProps(Message message, Map<String, Object> ctx) {
        for (String s : SecurityConstants.ALL_PROPERTIES) {
            Object v = message.getContextualProperty(s + ".sct");
```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/HttpsTokenInterceptorProvider.java`
#### Snippet
```java
            }
        }
        private void assertHttps(AssertionInfoMap aim, Collection<AssertionInfo> ais, Message message) {
            for (AssertionInfo ai : ais) {
                HttpsToken token = (HttpsToken)ai.getAssertion();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoTokenInterceptorProvider.java`
#### Snippet
```java
    }

    private static void mapSecurityProps(Message message, Map<String, Object> ctx) {
        for (String s : SecurityConstants.ALL_PROPERTIES) {
            Object v = message.getContextualProperty(s);
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/DigestVerifier.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(DigestVerifier.class);

    public void inspectDigest(byte[] messageBody, Map<String, List<String>> responseHeaders) {
        LOG.fine("Starting digest verification");
        if (responseHeaders.containsKey("Digest")) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/SignatureHeaderUtils.java`
#### Snippet
```java
     * Add a date header at the current time using the ZoneOffset. Date format is http
     */
    public static void addDateHeader(Map<String, List<String>> messageHeaders, ZoneOffset zoneOffset) {
        String date = DateTimeFormatter.RFC_1123_DATE_TIME
                .format(LocalDateTime.now().atZone(Clock.system(zoneOffset).getZone()));
```

### BoundedWildcard
Can generalize to `? super List`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/SignatureHeaderUtils.java`
#### Snippet
```java
     * Add a date header at the current time using the ZoneOffset. Date format is http
     */
    public static void addDateHeader(Map<String, List<String>> messageHeaders, ZoneOffset zoneOffset) {
        String date = DateTimeFormatter.RFC_1123_DATE_TIME
                .format(LocalDateTime.now().atZone(Clock.system(zoneOffset).getZone()));
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/SignatureHeaderUtils.java`
#### Snippet
```java
    }

    public static void inspectMessageHeaders(Map<String, List<String>> messageHeaders) {
        Objects.requireNonNull(messageHeaders);

```

### BoundedWildcard
Can generalize to `? extends WSEncryptionPart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
     *             "Token"
     */
    public void handleEncryptedSignedHeaders(List<WSEncryptionPart> encryptedParts,
            List<WSEncryptionPart> signedParts) {

```

### BoundedWildcard
Can generalize to `? extends SupportingToken`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    }

    protected void doEndorsedSignatures(List<SupportingToken> tokenList,
                                        boolean isTokenProtection,
                                        boolean isSigProtect) {
```

### BoundedWildcard
Can generalize to `? super WSEncryptionPart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    }

    protected void addSignatureConfirmation(List<WSEncryptionPart> sigParts) {
        Wss10 wss10 = getWss10();

```

### BoundedWildcard
Can generalize to `? super SupportingToken`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java

    protected void handleUsernameTokenSupportingToken(
        UsernameToken token, boolean endorse, boolean encryptedToken, List<SupportingToken> ret
    ) throws WSSecurityException {
        if (endorse && isTokenRequired(token.getIncludeTokenType())) {
```

### BoundedWildcard
Can generalize to `? extends WSHandlerResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
     * @return
     */
    public static String getUsername(List<WSHandlerResult> results) {
        /*
         * Scan the results for a matching actor. Use results only if the
```

### BoundedWildcard
Can generalize to `? extends WSEncryptionPart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
     */
    protected List<WSEncryptionPart> getParts(boolean sign,
            boolean includeBody, List<WSEncryptionPart> parts,
            List<Element> found) throws SOAPException {

```

### BoundedWildcard
Can generalize to `? super Element`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    protected List<WSEncryptionPart> getParts(boolean sign,
            boolean includeBody, List<WSEncryptionPart> parts,
            List<Element> found) throws SOAPException {

        List<WSEncryptionPart> result = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends org.apache.wss4j.policy.model.XPath`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
     */
    protected List<WSEncryptionPart> getElements(String encryptionModifier,
            List<org.apache.wss4j.policy.model.XPath> xpaths,
            List<Element> found,
            boolean forceId) throws SOAPException {
```

### BoundedWildcard
Can generalize to `? super Element`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    protected List<WSEncryptionPart> getElements(String encryptionModifier,
            List<org.apache.wss4j.policy.model.XPath> xpaths,
            List<Element> found,
            boolean forceId) throws SOAPException {

```

### BoundedWildcard
Can generalize to `? extends AssertionInfo`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java

    private List<SupportingToken> handleSupportingTokens(
        Collection<AssertionInfo> tokensInfos,
        boolean endorse
    ) throws WSSecurityException {
```

### BoundedWildcard
Can generalize to `? extends SupportingToken`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    }

    protected void addSignatureParts(List<SupportingToken> tokenList, List<WSEncryptionPart> sigParts) {

        boolean useSTRTransform =
```

### BoundedWildcard
Can generalize to `? super WSEncryptionPart`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
    }

    protected void addSignatureParts(List<SupportingToken> tokenList, List<WSEncryptionPart> sigParts) {

        boolean useSTRTransform =
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/MessageVerifier.java`
#### Snippet
```java
    }

    private void inspectMultipleSignatureHeaders(Map<String, List<String>> responseHeaders) {
        if (responseHeaders.get("Signature").size() > 1) {
            throw new MultipleSignatureHeaderException("there are multiple signature headers in request");
```

### BoundedWildcard
Can generalize to `? extends WSDataRef`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java
    }

    private static boolean matchElement(Collection<WSDataRef> refs, CoverageScope scope, Element el) {
        if (el == null) {
            return false;
```

### BoundedWildcard
Can generalize to `? extends org.apache.cxf.message.Attachment`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java

    public static void checkAttachmentsCoverage(
        Collection<org.apache.cxf.message.Attachment> attachments,
        final Collection<WSDataRef> refs,
        CoverageType type,
```

### BoundedWildcard
Can generalize to `? extends WSDataRef`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java
    public static void checkAttachmentsCoverage(
        Collection<org.apache.cxf.message.Attachment> attachments,
        final Collection<WSDataRef> refs,
        CoverageType type,
        CoverageScope scope
```

### BoundedWildcard
Can generalize to `? extends WSDataRef`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java
     */
    public static void reconcileEncryptedSignedRefs(final Collection<WSDataRef> signedRefs,
            final Collection<WSDataRef> encryptedRefs) {

        final List<WSDataRef> encryptedSignedRefs = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     * Return true if a list of tokens were encrypted, false otherwise.
     */
    private boolean areTokensEncrypted(List<WSSecurityEngineResult> tokens,
                                       List<WSSecurityEngineResult> encryptedResults) {
        if (enforceEncryptedTokens) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
        Element elementToProtect,
        boolean content,
        List<WSSecurityEngineResult> protResults,
        List<WSSecurityEngineResult> tokenResults
    ) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     */
    private boolean checkSignatureIsSigned(List<WSSecurityEngineResult> tokenResults,
                                           List<WSSecurityEngineResult> signedResults) {
        for (WSSecurityEngineResult signedResult : signedResults) {
            List<WSDataRef> sl =
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     */
    private boolean checkTimestampIsSigned(List<WSSecurityEngineResult> tokenResults,
                                           List<WSSecurityEngineResult> signedResults,
                                           Element timestamp) {
        for (WSSecurityEngineResult signedResult : signedResults) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     * Return true if a list of tokens were signed, false otherwise.
     */
    private boolean areTokensSigned(List<WSSecurityEngineResult> tokens,
                                    List<WSSecurityEngineResult> signedResults,
                                    List<WSSecurityEngineResult> encryptedResults,
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     * Return true if a token was encrypted, false otherwise.
     */
    private boolean isTokenEncrypted(Element token, List<WSSecurityEngineResult> encryptedResults) {
        for (WSSecurityEngineResult result : encryptedResults) {
            List<WSDataRef> dataRefs =
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
    private boolean checkSignatureOrEncryptionResult(
        WSSecurityEngineResult result,
        List<WSSecurityEngineResult> tokenResult
    ) {
        // See what was used to sign/encrypt this result
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
     * Return true if a token was signed, false otherwise.
     */
    private boolean isTokenSigned(Element token, List<WSSecurityEngineResult> signedResults,
                                  List<WSSecurityEngineResult> encryptedResults) {
        for (WSSecurityEngineResult signedResult : signedResults) {
```

### BoundedWildcard
Can generalize to `? extends WSSecurityEngineResult`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java

    private boolean isEncryptedTokenSigned(Element token, WSDataRef signedRef,
                                           List<WSSecurityEngineResult> encryptedResults) {
        if (signedRef.getProtectedElement() != null
            && "EncryptedData".equals(signedRef.getProtectedElement().getLocalName())
```

### BoundedWildcard
Can generalize to `? extends R`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorUtils.java`
#### Snippet
```java

    }
    static <R> CompletableFuture<R> toCompletableFuture(Future<R> future, Executor executor) {
        Supplier<R> supplier = () -> {
            try {
```

### BoundedWildcard
Can generalize to `? extends Element`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
    }

    private void addAttachmentsForEncryption(boolean atEnd, Element refList, List<Element> attachments) {
        if (atEnd) {
            if (refList != null) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
    private static String getSoapAction(JMSMessageHeadersType messageProperties,
                                        org.apache.cxf.message.Message outMessage,
                                        Map<String, List<String>> headers) {
        String soapAction = null;

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpoint.java`
#### Snippet
```java
    }

    private void getJaxWsJmsProps(Map<String, Object> jaxwsProps, Map<String, Object> jmsProps) {
        if (jaxwsProps == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends Response`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorInvokerImpl.java`
#### Snippet
```java
    }

    private <R> Iterable<R> toIterable(Future<Response> futureResponse, Class<R> type) {
        try {
            Response response = futureResponse.get();
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flux<T> flux(String name, Class<T> responseType) {
        Future<Response> futureResponse = webClient.async().method(name);
        return Flux.fromStream(() -> 
```

### BoundedWildcard
Can generalize to `? extends R`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorInvokerImpl.java`
#### Snippet
```java
    }

    private <R> Mono<R> mono(Future<R> future) {
        return Mono.fromFuture(toCompletableFuture(future, executorService));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flux<T> flux(String name, Entity<?> entity, Class<T> responseType) {
        Future<Response> futureResponse = webClient.async().method(name, entity);
        return Flux.fromStream(() -> 
```

### BoundedWildcard
Can generalize to `? extends T`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveTracerContext.java`
#### Snippet
```java

    @Override
    public <T> Callable<T> wrap(final String description, final Traceable<T> traceable) {
        final Callable<T> callable = new Callable<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientProvider.java`
#### Snippet
```java
    }

    protected void stopTraceSpan(final TraceScopeHolder<TraceScope> holder, final int responseStatus) {
        if (holder == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientProvider.java`
#### Snippet
```java
    }
    
    protected void stopTraceSpan(final TraceScopeHolder<TraceScope> holder, final Throwable ex) {
        if (holder == null) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientProvider.java`
#### Snippet
```java
    }
    
    private <C> Setter<C, String> inject(final Map<String, List<String>> requestHeaders) {
        return (carrier, key, value) -> {
            if (!requestHeaders.containsKey(key)) {
```

### BoundedWildcard
Can generalize to `? super List`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveClientProvider.java`
#### Snippet
```java
    }
    
    private <C> Setter<C, String> inject(final Map<String, List<String>> requestHeaders) {
        return (carrier, key, value) -> {
            if (!requestHeaders.containsKey(key)) {
```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/AbstractBraveProvider.java`
#### Snippet
```java
                                 final Map<String, List<Object>> responseHeaders,
                                 final int responseStatus,
                                 final TraceScopeHolder<TraceScope> holder) {
        if (holder == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/interceptor/SseInterceptor.java`
#### Snippet
```java
    }
    
    private void setHeader(HttpServletResponse servletResponse, Map.Entry<String, List<String>> entry) {
        if (entry.getValue() != null) {
            servletResponse.setHeader(entry.getKey(), 
```

### BoundedWildcard
Can generalize to `? super InboundSseEvent`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/SseEventSourceImpl.java`
#### Snippet
```java
        }

        InboundSseEventListenerImpl(Consumer<InboundSseEvent> e, Consumer<Throwable> t, Runnable c) {
            this.onEvent = e;
            this.onError = t;
```

### BoundedWildcard
Can generalize to `? super Throwable`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/SseEventSourceImpl.java`
#### Snippet
```java
        }

        InboundSseEventListenerImpl(Consumer<InboundSseEvent> e, Consumer<Throwable> t, Runnable c) {
            this.onEvent = e;
            this.onError = t;
```

### BoundedWildcard
Can generalize to `? super OutboundSseEvent`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/SseEventSinkImpl.java`
#### Snippet
```java
     * @param bufferSize SSE events buffer size
     */
    public SseEventSinkImpl(final MessageBodyWriter<OutboundSseEvent> writer, 
            final AsyncResponse async, final AsyncContext ctx, final int bufferSize) {
        
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java
    }
    
    private <T> Flowable<T> create(Supplier<T> supplier) {
        Flowable<T> flowable = Flowable.create(new FlowableOnSubscribe<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java
    
    @Override
    public <T> Flowable<T> method(String name, Entity<?> entity, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Flowable<T> method(String name, Entity<?> entity, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, Class<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java
    
    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, GenericType<T> responseType) {
        return create(() -> syncInvoker.method(name, entity, responseType));
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java
    }
    
    private <T> Observable<T> create(Supplier<T> supplier) {
        Observable<T> observable = Observable.create(new ObservableOnSubscribe<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? extends UserResource`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServiceFactoryBean.java`
#### Snippet
```java
    }

    private Map<String, UserResource> userResourcesAsMap(List<UserResource> resources) {
        return resources.stream().collect(Collectors.toMap(UserResource::getName, Function.identity()));
    }
```

### BoundedWildcard
Can generalize to `? extends ReaderInterceptor`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ReaderInterceptorContextImpl.java`
#### Snippet
```java
                                        InputStream is,
                                        Message message,
                                        List<ReaderInterceptor> readers) {
        super(cls, type, anns, message);
        this.is = is;
```

### BoundedWildcard
Can generalize to `? extends ResourceProvider`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServerFactoryBean.java`
#### Snippet
```java
     * @param rps resource providers
     */
    public void setResourceProviders(List<ResourceProvider> rps) {
        for (ResourceProvider rp : rps) {
            Class<?> c = rp.getResourceClass();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MediaTypeHeaderProvider.java`
#### Snippet
```java
    }

    private static void addParameter(Map<String, String> parameters, String token) {
        int equalSign = token.indexOf('=');
        if (equalSign == -1) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MediaTypeHeaderProvider.java`
#### Snippet
```java
    }

    private static void addParameter(Map<String, String> parameters, String token) {
        int equalSign = token.indexOf('=');
        if (equalSign == -1) {
```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
    }

    private static List<Variant> sortAllCombinations(List<MediaType> mediaTypes,
                                                     List<Locale> langs,
                                                     List<String> encs) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java
    }

    private static void addFields(List<String> fields, String token) {
        int i = token.indexOf('=');
        if (i != -1) {
```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
    }

    private void sortMediaTypesUsingQualityFactor(List<MediaType> types) {
        if (types.size() > 1) {
            Collections.sort(types, new Comparator<MediaType>() {
```

### BoundedWildcard
Can generalize to `? extends V`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
    }

    private void addAllValues(K key, List<V> newValues) {
        if (newValues == null) {
            throw new NullPointerException("List is empty");
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestPreprocessor.java`
#### Snippet
```java

    private void handleMethod(Message m,
                              MultivaluedMap<String, String> queries,
                              HttpHeaders headers) {
        if (MessageUtils.getContextualBoolean(m, ALLOW_HTTP_METHOD_OVERRIDE, false)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestPreprocessor.java`
#### Snippet
```java
    }

    private void handleTypeQuery(Message m, MultivaluedMap<String, String> queries) {
        String type = queries.getFirst(ACCEPT_QUERY);
        if (type != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestPreprocessor.java`
#### Snippet
```java
    }

    private void handleCType(Message m, MultivaluedMap<String, String> queries) {
        String type = queries.getFirst(CTYPE_QUERY);
        if (type != null) {
```

### BoundedWildcard
Can generalize to `? extends WriterInterceptor`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/WriterInterceptorContextImpl.java`
#### Snippet
```java
                                        OutputStream os,
                                        Message message,
                                        List<WriterInterceptor> writers) {
    //CHECKSTYLE:ON
        super(cls, type, anns, message);
```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
     * But not the ones derived from those classes.
     */
    private static void collectInterfaces(Set<Class<?>> interfaces, Class<?> anInterface) {
        interfaces.add(anInterface);
        for (Class<?> superInterface : anInterface.getInterfaces()) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    public <T> T[] getNodes(String expression, Map<String, String> namespaces, Class<T> cls) {

        NodeList nodes = (NodeList)evaluate(expression, namespaces, XPathConstants.NODESET);
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/xml/XMLSource.java`
#### Snippet
```java
     * @return the array of instances representing the matching nodes
     */
    public <T> T[] getNodes(String expression, Class<T> cls) {
        return getNodes(expression, CastUtils.cast(Collections.emptyMap(), String.class, String.class), cls);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
    protected void updateDefinition(Bus bus,
                                    Definition def,
                                    Map<String, Definition> done,
                                    Map<String, SchemaReference> doneSchemas,
                                    String base,
```

### BoundedWildcard
Can generalize to `? super Definition`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
    protected void updateDefinition(Bus bus,
                                    Definition def,
                                    Map<String, Definition> done,
                                    Map<String, SchemaReference> doneSchemas,
                                    String base,
```

### BoundedWildcard
Can generalize to `? extends Definition`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
     */
    private Definition lookupDefinition(Bus bus,
                                        Map<String, Definition> mp,
                                        String wsdl,
                                        String base) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
    }

    protected String mapUri(Bus bus, String base, Map<String, SchemaReference> smp, 
                            String loc, String xsd, String resolvedXsd)
        throws UnsupportedEncodingException {
```

### BoundedWildcard
Can generalize to `? extends SchemaReference`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
    private SchemaReference lookupSchemaReference(Bus bus,
                                                  String xsd,
                                                  Map<String, SchemaReference> smp,
                                                  String base) {
        SchemaReference si = smp.get(xsd);
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java
    }

    public void setParameters(List<Parameter> ps) {
        params = ps;
    }
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java
    }

    public UserOperation(String methodName, String pathValue, List<Parameter> ps) {
        this.methodName = methodName;
        this.pathValue = pathValue;
```

### BoundedWildcard
Can generalize to `? extends UserOperation`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserResource.java`
#### Snippet
```java
    }

    public UserResource(String className, String pathValue, List<UserOperation> ops) {
        this.className = className;
        this.pathValue = pathValue;
```

### BoundedWildcard
Can generalize to `? extends UserOperation`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserResource.java`
#### Snippet
```java
    }

    public void setOperations(List<UserOperation> ops) {
        opers = ops;
    }
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    }

    private List<Field> getList(Map<Class<?>, List<Field>> fields) {
        List<Field> ret = fields == null ? null : fields.get(getServiceClass());
        if (ret != null) {
```

### BoundedWildcard
Can generalize to `? extends Map`>
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    }

    private <T> ThreadLocalProxy<?> getProxy(Map<Class<?>, Map<T, ThreadLocalProxy<?>>> proxies,
                                             T key) {

```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    }

    private <T, V> void addToMap(Map<Class<?>, Map<T, V>> proxyMap,
                                 T f,
                                 V proxy) {
```

### BoundedWildcard
Can generalize to `? extends Map`>
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java
    }

    private <T> void clearProxies(Map<Class<?>, Map<T, ThreadLocalProxy<?>>> tlps) {
        Map<T, ThreadLocalProxy<?>> proxies = tlps == null ? null : tlps.get(getServiceClass());
        if (proxies == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
    }

    public boolean match(String uri, MultivaluedMap<String, String> templateVariableToValue) {

        if (uri == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
    }

    public boolean match(String uri, MultivaluedMap<String, String> templateVariableToValue) {

        if (uri == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    }

    public static String getSetEncoding(MediaType mt, MultivaluedMap<String, Object> headers,
                                        String defaultEncoding) {
        String enc = getMediaTypeCharsetParameter(mt);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
    }

    public static boolean isPayloadEmpty(MultivaluedMap<String, String> headers) {
        if (headers != null) {
            String value = headers.getFirst(HttpHeaders.CONTENT_LENGTH);
```

### BoundedWildcard
Can generalize to `? extends SpringResourceFactory`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/JAXRSServerFactoryBeanDefinitionParser.java`
#### Snippet
```java
        }

        public void setTempResourceProviders(List<SpringResourceFactory> providers) {
            tempFactories = providers;
        }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void populateMapFromMultipart(MultivaluedMap<String, String> params,
                                                MultipartBody body,
                                                Message m,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void populateMapFromMultipart(MultivaluedMap<String, String> params,
                                                MultipartBody body,
                                                Message m,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void addPropertyToForm(MultivaluedMap<String, String> map, String name, Object value) {
        if (!"".equals(name)) {
            map.add(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void addPropertyToForm(MultivaluedMap<String, String> map, String name, Object value) {
        if (!"".equals(name)) {
            map.add(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super Form`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void writeForm(FormEncodingProvider<Form> provider,
                                 Form form, OutputStream os)
        throws Exception {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void populateMapFromString(MultivaluedMap<String, String> params,
                                             Message m,
                                             String postBody,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
    }

    public static void populateMapFromString(MultivaluedMap<String, String> params,
                                             Message m,
                                             String postBody,
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
    public static List<Attachment> getMatchingAttachments(String id,
                                                         String mediaType,
                                                         List<Attachment> infos) {

        List<Attachment> all = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
    }

    private static Map<String, Attachment> fromListToMap(List<Attachment> atts,
                                                         boolean preferContentDisposition) {
        Map<String, Attachment> map = new LinkedHashMap<>();
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/AttachmentUtils.java`
#### Snippet
```java
    public static Attachment getMultipart(Multipart id,
                                          MediaType mt,
                                          List<Attachment> infos) throws IOException {

        if (id != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
    }

    private String getClassNameAndProperties(String cName, Map<String, List<String>> props) {
        String theName = cName.trim();
        int ind = theName.indexOf('(');
```

### BoundedWildcard
Can generalize to `? super List`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
    }

    private String getClassNameAndProperties(String cName, Map<String, List<String>> props) {
        String theName = cName.trim();
        int ind = theName.indexOf('(');
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                                        OASISCatalogManager catalog,
                                        Map<String, Element> done,
                                        Map<String, Element> notDone) {
        List<Element> impElemList = DOMUtils.findAllElementsByTagNameNS(element,
                                                                        "http://www.w3.org/2001/XMLSchema",
```

### BoundedWildcard
Can generalize to `? super Element`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                                        OASISCatalogManager catalog,
                                        Map<String, Element> done,
                                        Map<String, Element> notDone) {
        List<Element> impElemList = DOMUtils.findAllElementsByTagNameNS(element,
                                                                        "http://www.w3.org/2001/XMLSchema",
```

### BoundedWildcard
Can generalize to `? super Attachment`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBAttachmentMarshaller.java`
#### Snippet
```java
    private boolean isXop;

    public JAXBAttachmentMarshaller(Collection<Attachment> attachments, Integer mtomThreshold) {
        super();
        if (mtomThreshold != null) {
```

### BoundedWildcard
Can generalize to `? extends ServiceInfo`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
    private void addSchemas(Options opts,
                            SchemaCompiler schemaCompiler,
                            List<ServiceInfo> serviceList,
                            SchemaCollection schemaCollection) {

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java

    public static void fillInValuesFromBean(Object bean, String baseName,
                                            MultivaluedMap<String, Object> values) {
        for (Method m : bean.getClass().getMethods()) {
            String methodName = m.getName();
```

### BoundedWildcard
Can generalize to `? extends MessageBodyWriter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyWriter.java`
#### Snippet
```java
    }

    public void setDelegatingWriters(List<MessageBodyWriter<T>> writers) {
        this.delegatingWriters = writers;
    }
```

### BoundedWildcard
Can generalize to `? super ProviderInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java

    private void addContainerRequestFilter(
        List<ProviderInfo<ContainerRequestFilter>> postMatchFilters,
        ProviderInfo<ContainerRequestFilter> p) {
        ContainerRequestFilter filter = p.getProvider();
```

### BoundedWildcard
Can generalize to `? extends ClassResourceInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
    }

    public void applyDynamicFeatures(List<ClassResourceInfo> list) {
        if (!dynamicFeatures.isEmpty()) {
            for (ClassResourceInfo cri : list) {
```

### BoundedWildcard
Can generalize to `? extends UserResource`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java

    public static ClassResourceInfo createServiceClassResourceInfo(
        Map<String, UserResource> resources, UserResource model,
        Class<?> sClass, boolean isRoot, boolean enableStatic, Bus bus) {
        if (model == null) {
```

### BoundedWildcard
Can generalize to `? extends ClassResourceInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
    }

    public static ResourceTypes getAllRequestResponseTypes(List<ClassResourceInfo> cris,
                                                           boolean jaxbOnly,
                                                           MessageBodyWriter<?> jaxbWriter) {
```

### BoundedWildcard
Can generalize to `? extends MessageBodyReader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyReader.java`
#### Snippet
```java
    }

    public void setDelegatingReaders(List<MessageBodyReader<T>> readers) {
        this.delegatingReaders = readers;
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/FormEncodingProvider.java`
#### Snippet
```java
    }

    private T getFormObject(Class<T> clazz, MultivaluedMap<String, String> params) {
        return clazz.cast(Form.class.isAssignableFrom(clazz) ? new Form(params) : params);
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/DataSourceProvider.java`
#### Snippet
```java

    private void setContentTypeIfNeeded(MediaType type,
        MultivaluedMap<String, Object> headers, String ct) {

        if (!StringUtils.isEmpty(ct) && !type.equals(JAXRSUtils.toMediaType(ct))) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/AttachmentOutputInterceptor.java`
#### Snippet
```java

    public AttachmentOutputInterceptor(String multipartType,
                                       Map<String, List<String>> headers) {
        this.multipartType = multipartType;
        this.rootHeaders = headers;
```

### BoundedWildcard
Can generalize to `? super T`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
        private Annotation[] anns;
        private MediaType contentType;
        MessageBodyWriterDataHandler(MessageBodyWriter<T> writer,
                                     T obj,
                                     Class<T> cls,
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
    }

    private Object getAttachmentCollection(Type t, List<Attachment> infos, Annotation[] anns) throws IOException {
        Class<?> actual = getActualType(t, 0);
        if (Attachment.class.isAssignableFrom(actual)) {
```

### BoundedWildcard
Can generalize to `? extends SchemaHandler`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
    }

    public void setSchemaHandlers(Map<String, SchemaHandler> handlers) {
        schemaHandlers = handlers;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
        return null;
    }
    private Number getNumberProperty(String name, Function<String, Number> converter) {
        Object value = getProperty(name);
        if (value != null) {
```

### BoundedWildcard
Can generalize to `? extends Number`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObject.java`
#### Snippet
```java
        return null;
    }
    private Number getNumberProperty(String name, Function<String, Number> converter) {
        Object value = getProperty(name);
        if (value != null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
    }

    private void setResponseDate(MultivaluedMap<String, Object> headers, boolean firstTry) {
        if (!firstTry || headers.containsKey(HttpHeaders.DATE)) {
            return;
```

### BoundedWildcard
Can generalize to `? extends WriterInterceptor`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
    }

    private boolean checkBufferingMode(Message m, List<WriterInterceptor> writers, boolean firstTry) {
        if (!firstTry) {
            return false;
```

### BoundedWildcard
Can generalize to `? extends ProviderInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected void setCommonProviders(List<ProviderInfo<? extends Object>> theProviders, RuntimeType type) {
        List<ProviderInfo<ReaderInterceptor>> readInts =
            new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? super ProviderInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java

        public NameKeyComparator(
            Comparator<ProviderInfo<?>> comparator, boolean ascending) {

            super(ascending);
```

### BoundedWildcard
Can generalize to `? super NameKey`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    protected static <T> void mapInterceptorFilters(Map<NameKey, ProviderInfo<T>> map,
                                                    List<ProviderInfo<T>> filters,
                                                    Class<?> providerCls,
```

### BoundedWildcard
Can generalize to `? super ProviderInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    protected static <T> void mapInterceptorFilters(Map<NameKey, ProviderInfo<T>> map,
                                                    List<ProviderInfo<T>> filters,
                                                    Class<?> providerCls,
```

### BoundedWildcard
Can generalize to `? extends ProviderInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java

    protected static <T> void mapInterceptorFilters(Map<NameKey, ProviderInfo<T>> map,
                                                    List<ProviderInfo<T>> filters,
                                                    Class<?> providerCls,
                                                    boolean ascending) {
```

### BoundedWildcard
Can generalize to `? extends MessageBodyWriter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    private boolean isWriteable(ProviderInfo<MessageBodyWriter<?>> pi,
                               Class<?> type,
                               Type genericType,
```

### BoundedWildcard
Can generalize to `? extends MessageBodyReader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java


    private <T> boolean matchesReaderMediaTypes(ProviderInfo<MessageBodyReader<?>> pi,
                                                MediaType mediaType) {
        MessageBodyReader<?> ep = pi.getProvider();
```

### BoundedWildcard
Can generalize to `? extends MessageBodyWriter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    private <T> boolean matchesWriterMediaTypes(ProviderInfo<MessageBodyWriter<?>> pi,
                                                MediaType mediaType) {
        MessageBodyWriter<?> ep = pi.getProvider();
```

### BoundedWildcard
Can generalize to `? extends MessageBodyReader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    private boolean isReadable(ProviderInfo<MessageBodyReader<?>> pi,
                               Class<?> type,
                               Type genericType,
```

### BoundedWildcard
Can generalize to `? extends JweHeaders`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJweJsonWriterProvider.java`
#### Snippet
```java
    protected List<JweEncryptionProvider> getInitializedEncryptionProviders(List<String> propLocs,
                                              JweHeaders sharedProtectedHeaders,
                                              List<JweHeaders> perRecipientUnprotectedHeaders) {
        if (encProviders != null) {
            return encProviders;
```

### BoundedWildcard
Can generalize to `? extends Attachment`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/multipart/JwsMultipartSignatureOutFilter.java`
#### Snippet
```java
    }
    @Override
    public void filter(List<Attachment> parts) {
        for (int i = 0; i < parts.size() - 1; i++) {
            Attachment dataPart = parts.get(i);
```

### BoundedWildcard
Can generalize to `? extends JwsHeaders`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJwsJsonWriterProvider.java`
#### Snippet
```java
    
    protected List<JwsSignatureProvider> getInitializedSigProviders(
        List<String> propLocs, List<JwsHeaders> protectedHeaders) {
        if (sigProviders != null) {
            return sigProviders;
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsProxyFactoryBean.java`
#### Snippet
```java
     * @param h a <code>List</code> of <code>Handler</code> objects
     */
    public void setHandlers(@SuppressWarnings("rawtypes") List<Handler> h) {
        handlers.clear();
        handlers.addAll(h);
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
        handlers.addAll(h);
    }
    public void addHandlers(@SuppressWarnings("rawtypes") List<Handler> h) {
        handlers.addAll(h);
    }
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
        return (JaxWsServiceFactoryBean)getServiceFactory();
    }
    public void setHandlers(@SuppressWarnings("rawtypes") List<Handler> h) {
        handlers.clear();
        handlers.addAll(h);
```

### BoundedWildcard
Can generalize to `? extends Annotation`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java

    private boolean addJAXBAnnotations(ASMHelper.FieldVisitor fv,
                                       List<Annotation> jaxbAnnos,
                                       String name) {
        ASMHelper.AnnotationVisitor av0;
```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
                                        Method method,
                                        boolean isRequest,
                                        Set<Class<?>> wrapperBeans,
                                        JaxWsServiceFactoryBean factory,
                                        InterfaceInfo interfaceInfo,
```

### BoundedWildcard
Can generalize to `? super T`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private QName calculateOpName(Holder<T> holder, QName opName, boolean hasOpName) throws XMLStreamException {
        boolean findDispatchOp = Boolean.TRUE.equals(getRequestContext().get("find.dispatch.operation"));

```

### BoundedWildcard
Can generalize to `? super Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/JavaeeHandlerChainBuilder.java`
#### Snippet
```java
    }

    void processHandlerElement(Element el, List<Handler> chain) {
        try {
            JAXBContext ctx = getContextForPortComponentHandlerType();
```

### BoundedWildcard
Can generalize to `? super Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/JakartaeeHandlerChainBuilder.java`
#### Snippet
```java
    }

    void processHandlerElement(Element el, List<Handler> chain) {
        try {
            JAXBContext ctx = getContextForPortComponentHandlerType();
```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedAttachments.java`
#### Snippet
```java
        private String key;

        WrappedAttachmentsIterator(Iterator<Map.Entry<String, DataHandler>> iterator) {
            this.iterator = iterator;
        }
```

### BoundedWildcard
Can generalize to `? extends PortComponentHandlerType`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainBuilder.java`
#### Snippet
```java
        return sortHandlers(buildHandlerChain(hc, getHandlerClassLoader()));
    }
    public List<Handler> buildHandlerChainFromConfiguration(List<PortComponentHandlerType> hc) {
        if (null == hc || hc.isEmpty()) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainBuilder.java`
#### Snippet
```java
     * @return sorted list of handlers
     */
    public List<Handler> sortHandlers(List<Handler> handlers) {

        final int size = handlers.size();
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
    }

    public HandlerChainInvoker(@SuppressWarnings("rawtypes") List<Handler> hc, boolean isOutbound) {
        if (LOG.isLoggable(Level.FINE)) {
            LOG.log(Level.FINE, "invoker for chain size: " + (hc != null ? hc.size() : 0));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
    }

    private <T extends Handler<?>> List<T> reverseHandlerChain(List<T> handlerChain) {
        List<T> reversedHandlerChain = new ArrayList<>();
        reversedHandlerChain.addAll(handlerChain);
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/AnnotationHandlerChainBuilder.java`
#### Snippet
```java
     * @return
     */
    public List<Handler> buildHandlerChainFromClass(Class<?> clz, List<Handler> existingHandlers,
                                                    QName portQName, QName serviceQName, String bindingID) {
        LOG.fine("building handler chain");
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
        return m;
    }
    private Object createAttachments(Map<String, Object> mc, String propertyName) {
        if (mc == null) {
            return null;
```

### BoundedWildcard
Can generalize to `? extends Handler`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        outFault = interceptors;
    }
    public void setHandlers(@SuppressWarnings("rawtypes") List<Handler> h) {
        handlers.clear();
        handlers.addAll(h);
```

### BoundedWildcard
Can generalize to `? super Interceptor`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        List<Interceptor<? extends Message>> other;
        DoubleAddInterceptorList(List<Interceptor<? extends Message>> a1,
                                 List<Interceptor<? extends Message>> a2) {
            orig = a1;
            other = a2;
```

### BoundedWildcard
Can generalize to `? extends WebServiceFeature`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

    public JaxWsEndpointImpl(Bus bus, Service s, EndpointInfo ei, JaxWsImplementorInfo implementorInfo,
                             List<WebServiceFeature> wf, List<? extends Feature> af, boolean isFromWsdl)
        throws EndpointException {
        super(bus, s, ei);
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
     * @param to target map
     */
    private static void putAll(Map<QName, String> from, Map<QName, String> to) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? super QName`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
     * @param to target map
     */
    private static void putAll(Map<QName, String> from, Map<QName, String> to) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/VersionTransformer.java`
#### Snippet
```java
     * @param to target map
     */
    private static void putAll(Map<QName, String> from, Map<QName, String> to) {
        if (from != null) {
            to.putAll(from);
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
    }

    Object getUsingAddressing(List<ExtensibilityElement> exts) {
        Object ua = null;
        if (exts != null) {
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java

    private static void addInterceptors(PhaseInterceptorChain chain,
                                        List<Interceptor<? extends Message>> il) {
        for (Interceptor<? extends Message> i : il) {
            final String iname = i.getClass().getSimpleName();
```

### BoundedWildcard
Can generalize to `? super Attachment`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/attachment/JAXBAttachmentMarshaller.java`
#### Snippet
```java
    private QName lastElementName;

    public JAXBAttachmentMarshaller(Collection<Attachment> attachments, Integer mtomThreshold) {
        super();
        if (mtomThreshold != null) {
```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java

    JAXBContextInitializer(Bus bus, ServiceInfo serviceInfo,
                           Set<Class<?>> classes,
                           Collection<Object> typeReferences,
                           Map<String, Object> unmarshallerProperties) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
                           Set<Class<?>> classes,
                           Collection<Object> typeReferences,
                           Map<String, Object> unmarshallerProperties) {
        super(serviceInfo);
        this.classes = classes;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/MemoryClientCodeStateManager.java`
#### Snippet
```java
    @Override
    public MultivaluedMap<String, String> fromRedirectState(MessageContext mc,
                                                            MultivaluedMap<String, String> redirectState) {
        String stateParam = redirectState.getFirst(OAuthConstants.STATE);
        String sessionToken = OAuthUtils.getSessionToken(mc, "state");
```

### BoundedWildcard
Can generalize to `? extends OAuthPermission`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
    }

    public static List<String> convertPermissionsToScopeList(List<OAuthPermission> perms) {
        return perms.stream().map(OAuthPermission::getPermission).collect(toList());
    }
```

### BoundedWildcard
Can generalize to `? extends OAuthPermission`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
    }

    public static String convertPermissionsToScope(List<OAuthPermission> perms) {
        StringBuilder sb = new StringBuilder();
        for (OAuthPermission perm : perms) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/JoseClientCodeStateManager.java`
#### Snippet
```java
    @Override
    public MultivaluedMap<String, String> toRedirectState(MessageContext mc,
                                                          MultivaluedMap<String, String> requestState) {
        JweEncryptionProvider theEncryptionProvider = getInitializedEncryptionProvider();
        JwsSignatureProvider theSigProvider = getInitializedSigProvider(theEncryptionProvider);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/JoseClientCodeStateManager.java`
#### Snippet
```java
    @Override
    public MultivaluedMap<String, String> toRedirectState(MessageContext mc,
                                                          MultivaluedMap<String, String> requestState) {
        JweEncryptionProvider theEncryptionProvider = getInitializedEncryptionProvider();
        JwsSignatureProvider theSigProvider = getInitializedSigProvider(theEncryptionProvider);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/JoseClientCodeStateManager.java`
#### Snippet
```java
    @Override
    public MultivaluedMap<String, String> fromRedirectState(MessageContext mc,
                                                            MultivaluedMap<String, String> redirectState) {

        String stateParam = redirectState.getFirst(OAuthConstants.STATE);
```

### BoundedWildcard
Can generalize to `? extends OAuthPermission`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/OAuthContext.java`
#### Snippet
```java
    public OAuthContext(UserSubject resourceOwnerSubject,
                        UserSubject clientSubject,
                        List<OAuthPermission> perms,
                        String tokenGrantType) {
        this.resourceOwnerSubject = resourceOwnerSubject;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/JwtBearerGrantHandler.java`
#### Snippet
```java

    @Override
    public ServerAccessToken createAccessToken(Client client, MultivaluedMap<String, String> params)
        throws OAuthServiceException {
        String assertion = params.getFirst(Constants.CLIENT_GRANT_ASSERTION_PARAM);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/AbstractJwtBearerGrant.java`
#### Snippet
```java
    }

    protected void addScope(MultivaluedMap<String, String> map) {
        if (scope != null) {
            map.putSingle(OAuthConstants.SCOPE, scope);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/AbstractJwtBearerGrant.java`
#### Snippet
```java
    }

    protected void addScope(MultivaluedMap<String, String> map) {
        if (scope != null) {
            map.putSingle(OAuthConstants.SCOPE, scope);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/ClientCodeRequestFilter.java`
#### Snippet
```java

    private void checkSecurityContextEnd(ContainerRequestContext rc,
                                         MultivaluedMap<String, String> requestParams) {
        SecurityContext sc = rc.getSecurityContext();
        if (sc == null || sc.getUserPrincipal() == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/ClientCodeRequestFilter.java`
#### Snippet
```java
    }

    protected void setCodeVerifier(UriBuilder ub, MultivaluedMap<String, String> redirectState) {
        if (codeVerifierTransformer != null) {
            String codeVerifier = redirectState.getFirst(OAuthConstants.AUTHORIZATION_CODE_VERIFIER);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/refresh/RefreshTokenGrantHandler.java`
#### Snippet
```java
    }

    public ServerAccessToken createAccessToken(Client client, MultivaluedMap<String, String> params)
        throws OAuthServiceException {
        String refreshToken = params.getFirst(OAuthConstants.REFRESH_TOKEN);
```

### BoundedWildcard
Can generalize to `? extends CodeVerifierTransformer`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AuthorizationCodeGrantHandler.java`
#### Snippet
```java
    }

    public void setCodeVerifierTransformers(List<CodeVerifierTransformer> codeVerifierTransformers) {
        if (codeVerifierTransformers == null) {
            this.codeVerifierTransformers = Collections.emptyList();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JCacheOAuthDataProvider.java`
#### Snippet
```java
    }

    protected static <V extends ServerAccessToken> V getToken(Cache<String, V> cache, String key) {
        V token = cache.get(key);
        if (token != null && isExpired(token)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthScopesFilter.java`
#### Snippet
```java
    }

    public void setConfidentialClientMethods(Set<String> confidentialClientMethods) {
        this.confidentialClientMethods = confidentialClientMethods;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthScopesFilter.java`
#### Snippet
```java
    }

    public void setScopesMatchAllMap(Map<String, Boolean> scopesMatchAllMap) {
        this.scopesMatchAllMap = scopesMatchAllMap;
    }
```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static String convertTypesToString(List<MediaType> types) {
        StringBuilder sb = new StringBuilder();
        for (MediaType type : types) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static String getCurrentPath(MultivaluedMap<String, String> values) {
        String path = values.getFirst(URITemplate.FINAL_MATCH_GROUP);
        return path == null ?  "/" : path;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static void addTemplateVarValues(List<String> values,
                                             MultivaluedMap<String, String> params,
                                             URITemplate template) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static void addStructuredPartToMap(MultivaluedMap<String, String> queries,
                                               String sep,
                                               String name,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static void addStructuredPartToMap(MultivaluedMap<String, String> queries,
                                               String sep,
                                               String name,
```

### BoundedWildcard
Can generalize to `? extends ClassResourceInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    public static Response createResponse(List<ClassResourceInfo> cris, Message msg,
                                          String responseMessage, int status, boolean addAllow) {
        ResponseBuilder rb = toResponseBuilder(status);
```

### BoundedWildcard
Can generalize to `? extends ClassResourceInfo`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java

    public static Map<ClassResourceInfo, MultivaluedMap<String, String>> selectResourceClass(
        List<ClassResourceInfo> resources, String path, Message message) {

        LOG.fine(() -> new org.apache.cxf.common.i18n.Message("START_CRI_MATCH",
```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static void intersectMimeTypes(List<MediaType> requiredMediaTypes, List<MediaType> userMediaTypes,
            MimeTypesIntersector intersector) {

```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    private static void intersectMimeTypes(List<MediaType> requiredMediaTypes, List<MediaType> userMediaTypes,
            MimeTypesIntersector intersector) {

```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    public static int compareSortedMediaTypes(List<MediaType> mts1, List<MediaType> mts2, String qs) {
        int size1 = mts1.size();
        int size2 = mts2.size();
```

### BoundedWildcard
Can generalize to `? extends MediaType`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    }

    public static int compareSortedMediaTypes(List<MediaType> mts1, List<MediaType> mts2, String qs) {
        int size1 = mts1.size();
        int size2 = mts2.size();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/AbstractGrantHandler.java`
#### Snippet
```java
    protected ServerAccessToken doCreateAccessToken(Client client,
                                                    UserSubject subject,
                                                    MultivaluedMap<String, String> params) {

        return doCreateAccessToken(client,
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/TokenIntrospectionService.java`
#### Snippet
```java
    @Produces({MediaType.APPLICATION_JSON })
    @Consumes(MediaType.APPLICATION_FORM_URLENCODED)
    public TokenIntrospection getTokenIntrospection(@Encoded MultivaluedMap<String, String> params) {
        checkSecurityContext();
        String tokenId = params.getFirst(OAuthConstants.TOKEN_ID);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadataService.java`
#### Snippet
```java
    }

    protected void prepareConfigurationData(Map<String, Object> cfg, String baseUri) {
        // Issuer
        cfg.put("issuer", buildIssuerUri(baseUri));
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationCodeGrantService.java`
#### Snippet
```java
    }

    protected OAuthRedirectionState recreateRedirectionStateFromParams(MultivaluedMap<String, String> params) {
        OAuthRedirectionState state = super.recreateRedirectionStateFromParams(params);
        state.setClientCodeChallenge(params.getFirst(OAuthConstants.AUTHORIZATION_CODE_CHALLENGE));
```

### BoundedWildcard
Can generalize to `? extends AccessTokenValidator`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractAccessTokenValidator.java`
#### Snippet
```java
    }

    public void setTokenValidators(List<AccessTokenValidator> validators) {
        tokenHandlers = validators;
        for (AccessTokenValidator handler : validators) {
```

### BoundedWildcard
Can generalize to `? extends RedirectionBasedGrantService`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationService.java`
#### Snippet
```java
    }

    public void setServices(List<RedirectionBasedGrantService> services) {
        for (RedirectionBasedGrantService service : services) {
            for (String responseType : service.getSupportedResponseTypes()) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationService.java`
#### Snippet
```java
    @Consumes("application/x-www-form-urlencoded")
    @Produces({"application/xhtml+xml", "text/html", "application/xml", "application/json" })
    public Response authorizePost(MultivaluedMap<String, String> params) {
        String responseType = params.getFirst(OAuthConstants.RESPONSE_TYPE);
        RedirectionBasedGrantService service = getService(responseType);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
     * Find the matching grant handler
     */
    protected AccessTokenGrantHandler findGrantHandler(MultivaluedMap<String, String> params) {
        String grantType = params.getFirst(OAuthConstants.GRANT_TYPE);

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
        }
    }
    protected void checkAudience(Client c, MultivaluedMap<String, String> params) {
        String audienceParam = params.getFirst(OAuthConstants.CLIENT_AUDIENCE);
        if (!OAuthUtils.validateAudience(audienceParam, c.getRegisteredAudiences())) {
```

### BoundedWildcard
Can generalize to `? extends AccessTokenGrantHandler`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
     * @param handlers the grant handlers
     */
    public void setGrantHandlers(List<AccessTokenGrantHandler> handlers) {
        grantHandlers = handlers;
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractOAuthService.java`
#### Snippet
```java
    }
    
    protected Client getValidClient(String clientId, String clientSecret, MultivaluedMap<String, String> params)
        throws OAuthServiceException {
        if (clientId != null) {
```

### BoundedWildcard
Can generalize to `? super OAuthPermission`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
    protected void convertSingleScopeToPermission(Client client,
                                                  String scope,
                                                  List<OAuthPermission> perms) {
        OAuthPermission permission = permissionMap.get(scope);
        if (permission == null) {
```

### BoundedWildcard
Can generalize to `? extends Client`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
        return false;
    }
    public void setClients(List<Client> clients) {
        for (Client c : clients) {
            setClient(c);
```

### BoundedWildcard
Can generalize to `? extends Relationship`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/realm/RelationshipResolver.java`
#### Snippet
```java


    public RelationshipResolver(List<Relationship> relationships) {
        relationshipMap = new HashMap<>();
        for (Relationship rel : relationships) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java
    }

    protected String retrieveClientId(MultivaluedMap<String, String> params) {
        String clientId = params.getFirst(OAuthConstants.CLIENT_ID);
        if (clientId == null) {
```

### BoundedWildcard
Can generalize to `? extends AudienceRestriction`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java

    private boolean matchSaml2AudienceRestriction(
        String appliesTo, List<AudienceRestriction> audienceRestrictions
    ) {
        boolean found = false;
```

### BoundedWildcard
Can generalize to `? extends AudienceRestrictionCondition`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java

    private boolean matchSaml1AudienceRestriction(
        String appliesTo, List<AudienceRestrictionCondition> restrConditions
    ) {
        boolean found = false;
```

### BoundedWildcard
Can generalize to `? super String`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
    }

    protected void handleEvent(TokenCancellerParametersSupport event, Map<String, Object> map) {
        TokenCancellerParameters params = event.getTokenParameters();
        HttpServletRequest req =
```

### BoundedWildcard
Can generalize to `? super String`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
    }

    protected void handleEvent(TokenValidatorParametersSupport event, Map<String, Object> map) {
        TokenValidatorParameters params = event.getTokenParameters();
        HttpServletRequest req =
```

### BoundedWildcard
Can generalize to `? super String`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
    }

    protected void handleEvent(TokenRenewerParametersSupport event, Map<String, Object> map) {
        TokenRenewerParameters params = event.getTokenParameters();
        HttpServletRequest req =
```

### BoundedWildcard
Can generalize to `? super String`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/map/EventMapper.java`
#### Snippet
```java
    }

    protected void handleEvent(TokenProviderParametersSupport event, Map<String, Object> map) {
        TokenProviderParameters params = event.getTokenParameters();
        try {
```

### BoundedWildcard
Can generalize to `? extends Attribute`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
    }

    protected ProcessedClaim processClaim(Claim claim, Map<String, Attribute> ldapAttributes, Principal principal) {
        String claimType = claim.getClaimType();
        String ldapAttribute = getClaimsLdapAttributeMapping().get(claimType);
```

### BoundedWildcard
Can generalize to `? extends Filter`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapUtils.java`
#### Snippet
```java
    public static List<String> getAttributeOfEntries(
        LdapTemplate ldapTemplate, String baseDN,
        String objectClass, List<Filter> filters,
        String searchAttribute) {

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java


    protected OAuthRedirectionState recreateRedirectionStateFromParams(MultivaluedMap<String, String> params) {
        OAuthRedirectionState state = new OAuthRedirectionState();
        state.setClientId(params.getFirst(OAuthConstants.CLIENT_ID));
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
    }

    protected Response createErrorResponse(MultivaluedMap<String, String> params,
                                           String redirectUri,
                                           String error) {
```

### BoundedWildcard
Can generalize to `? extends ClaimsParser`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
     */
    private static Claim parseChildClaimType(
        Element childClaimType, String dialect, List<ClaimsParser> claimsParsers
    ) {
        if (claimsParsers != null) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Class<T> responseType) {
        if (sc == null) {
            return Observable.from(wc.async().method(name, responseType));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, GenericType<T> responseType) {
        if (sc == null) {
            return Observable.from(wc.async().method(name, responseType));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, Class<T> responseType) {
        if (sc == null) {
            return Observable.from(wc.async().method(name, entity, responseType));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/client/ObservableRxInvokerImpl.java`
#### Snippet
```java

    @Override
    public <T> Observable<T> method(String name, Entity<?> entity, GenericType<T> responseType) {
        if (sc == null) {
            return Observable.from(wc.async().method(name, entity, responseType));
```

### BoundedWildcard
Can generalize to `? super XMLStreamReader`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java

    private void getPara(DepthXMLStreamReader xmlReader,
                         DataReader<XMLStreamReader> dr,
                         MessageContentsList parameters,
                         Iterator<MessagePartInfo> itr,
```

### BoundedWildcard
Can generalize to `? extends OperationInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
    }

    private void getBindingOperationForEmptyBody(Collection<OperationInfo> operations, Endpoint ep, Exchange exchange) {
        // TO DO : check duplicate operation with no input and also check if the action matches
        for (OperationInfo op : operations) {
```

### BoundedWildcard
Can generalize to `? super SchemaInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
    }

    private void extractSchema(Definition def, SchemaCollection schemaCol, List<SchemaInfo> schemaInfos) {
        Types typesElement = def.getTypes();
        if (typesElement != null) {
```

### BoundedWildcard
Can generalize to `? super Definition`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
    }

    private void parseImports(Definition def, List<Definition> defList) {
        List<Import> importList = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
    private final Bus bus;

    public SchemaUtil(final Bus b, final Map<String, Element> s) {
        this.bus = b;
        this.schemaList = s;
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SOAPBody getSoapBody(List<ExtensibilityElement> exts) {
        if (exts != null) {
            for (ExtensibilityElement ext : exts) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static List<SOAPHeader> getSoapHeaders(List<ExtensibilityElement> exts) {
        List<SOAPHeader> headers = new ArrayList<>();
        if (exts != null) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SOAPOperation getSoapOperation(List<ExtensibilityElement> exts) {
        if (exts != null) {
            for (ExtensibilityElement ext : exts) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SOAPBinding getSoapBinding(List<ExtensibilityElement> exts) {
        for (ExtensibilityElement ext : exts) {
            if (isSOAPBinding(ext)) {
```

### BoundedWildcard
Can generalize to `? super Definition`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java


    private void parseImports(Definition def, List<Definition> defList) {
        List<Import> importList = new ArrayList<>();

```

### BoundedWildcard
Can generalize to `? super String`
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java
    }
    
    private static Consumer<InboundSseEvent> collectRaw(final Collection<String> titles) {
        return event -> titles.add(event.readData(String.class, MediaType.TEXT_PLAIN_TYPE));
    }
```

### BoundedWildcard
Can generalize to `? super Book`
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/AbstractSseTest.java`
#### Snippet
```java
    }

    private static Consumer<InboundSseEvent> collect(final Collection<Book> books) {
        return event -> books.add(event.readData(Book.class, MediaType.APPLICATION_JSON_TYPE));
    }
```

### BoundedWildcard
Can generalize to `? super String`
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomImpl.java`
#### Snippet
```java
public class TestMtomImpl implements TestMtom {

    public void testXop(Holder<String> name, Holder<DataHandler> attachinfo) {
        if ("have name".equals(name.value) && attachinfo.value.getName() != null) {
            name.value = "return detail + " + attachinfo.value.getName();
```

### BoundedWildcard
Can generalize to `? extends DataHandler`
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomImpl.java`
#### Snippet
```java
public class TestMtomImpl implements TestMtom {

    public void testXop(Holder<String> name, Holder<DataHandler> attachinfo) {
        if ("have name".equals(name.value) && attachinfo.value.getName() != null) {
            name.value = "return detail + " + attachinfo.value.getName();
```

### BoundedWildcard
Can generalize to `? extends BindingFaultInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java

    protected void buildBindingFault(Definition def, BindingOperation bindingOperation,
                                   Collection<BindingFaultInfo> bindingFaultInfos) {
        for (BindingFaultInfo bindingFaultInfo : bindingFaultInfos) {
            final BindingFault bindingFault = def.createBindingFault();
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
    protected void addExtensibilityElements(Definition def,
                                            ElementExtensible elementExtensible,
                                            List<ExtensibilityElement> extensibilityElements) {
        if (extensibilityElements != null) {
            for (ExtensibilityElement element : extensibilityElements) {
```

### BoundedWildcard
Can generalize to `? extends ServiceInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
     * @param services the services.
     */
    public ServiceWSDLBuilder(Bus b, List<ServiceInfo> services) {
        this.services = services;
        bus = b;
```

### BoundedWildcard
Can generalize to `? extends BindingInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java

    protected void buildBinding(Definition definition,
                                Collection<BindingInfo> bindingInfos,
                                Collection<PortType> portTypes) {
        for (BindingInfo bindingInfo : bindingInfos) {
```

### BoundedWildcard
Can generalize to `? extends PortType`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
    protected void buildBinding(Definition definition,
                                Collection<BindingInfo> bindingInfos,
                                Collection<PortType> portTypes) {
        for (BindingInfo bindingInfo : bindingInfos) {
            final Binding binding = definition.createBinding();
```

### BoundedWildcard
Can generalize to `? extends OperationInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java

    protected void buildPortTypeOperation(PortType portType,
                                          Collection<OperationInfo> operationInfos,
                                          final Definition def) {
        for (OperationInfo operationInfo : operationInfos) {
```

### BoundedWildcard
Can generalize to `? extends BindingOperationInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java

    protected void buildBindingOperation(Definition def, Binding binding,
                                       Collection<BindingOperationInfo> bindingOperationInfos) {
        for (BindingOperationInfo bindingOperationInfo : bindingOperationInfos) {
            final BindingOperation bindingOperation = def.createBindingOperation();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
     */
    protected void buildTypesWithSchemaImports(final Collection<SchemaInfo> schemas,
                                               final Map<String, SchemaInfo> imports, final Definition def) {

        Types types = def.createTypes();
```

### BoundedWildcard
Can generalize to `? super SchemaInfo`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
     */
    protected void buildTypesWithSchemaImports(final Collection<SchemaInfo> schemas,
                                               final Map<String, SchemaInfo> imports, final Definition def) {

        Types types = def.createTypes();
```

### BoundedWildcard
Can generalize to `? extends X509Certificate`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/DateValidator.java`
#### Snippet
```java
    }

    public boolean isCertificateChainValid(List<X509Certificate> certificates) {
        if (certificates == null) {
            return false;
```

### BoundedWildcard
Can generalize to `? extends X509Certificate`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/TrustedAuthorityValidator.java`
#### Snippet
```java
    }

    private Set<TrustAnchor> asTrustAnchors(List<X509Certificate> trustedAuthorityCerts) {
        return trustedAuthorityCerts.stream()
                .map(cert -> new TrustAnchor(cert, null))
```

### BoundedWildcard
Can generalize to `? extends X509Certificate`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/TrustedAuthorityValidator.java`
#### Snippet
```java
     * @return the validity state of the certificate
     */
    boolean isCertificateChainValid(List<X509Certificate> certificates) {
        X509Certificate targetCert = certificates.get(0);
        X509CertSelector selector = new X509CertSelector();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }
    
    protected static void put(final String name, final Integer value, final Map<String, String> params) {
        if (value != null) {
            params.put(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }
    
    protected static void put(final String name, final Integer value, final Map<String, String> params) {
        if (value != null) {
            params.put(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }
    
    protected static void put(final String name, final String value, final Map<String, String> params) {
        if (!StringUtils.isEmpty(value)) {
            params.put(name, value);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }
    
    protected static void put(final String name, final String value, final Map<String, String> params) {
        if (!StringUtils.isEmpty(value)) {
            params.put(name, value);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }

    protected static void put(final String name, final Boolean value, final Map<String, String> params) {
        if (value != null) {
            params.put(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiConfig.java`
#### Snippet
```java
    }

    protected static void put(final String name, final Boolean value, final Map<String, String> params) {
        if (value != null) {
            params.put(name, value.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
    }

    private void setCorbaLocArgs(URI addressURI, List<String> orbArgs) {
        String schemeSpecificPart = addressURI.getSchemeSpecificPart();
        int keyIndex = schemeSpecificPart.indexOf('/');
```

### BoundedWildcard
Can generalize to `? extends TypeCode`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
    }

    public ExceptionList getExceptionList(Map<TypeCode, RaisesType> exceptions,
                                             CorbaMessage message,
                                             OperationType opType) {
```

### BoundedWildcard
Can generalize to `? super Long`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/InvocationHandlerFactory.java`
#### Snippet
```java


    private void loadProperties(Map<Long, String> handlersMap, URL url) throws IOException {
        Properties p = new Properties();
        p.load(url.openStream());
```

### BoundedWildcard
Can generalize to `? super String`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/InvocationHandlerFactory.java`
#### Snippet
```java


    private void loadProperties(Map<Long, String> handlersMap, URL url) throws IOException {
        Properties p = new Properties();
        p.load(url.openStream());
```

### BoundedWildcard
Can generalize to `? super String`
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationWork.java`
#### Snippet
```java
    public MDBActivationWork(MDBActivationSpec spec,
            MessageEndpointFactory endpointFactory,
            Map<String, InboundEndpoint> endpoints) {
        this.spec = spec;
        this.endpointFactory = endpointFactory;
```

### BoundedWildcard
Can generalize to `? super InboundEndpoint`
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationWork.java`
#### Snippet
```java
    public MDBActivationWork(MDBActivationSpec spec,
            MessageEndpointFactory endpointFactory,
            Map<String, InboundEndpoint> endpoints) {
        this.spec = spec;
        this.endpointFactory = endpointFactory;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketJettyDestination.java`
#### Snippet
```java
    }
    
    private static void addInitParameter(AtmosphereFramework fw, Map<String, String> initParams,
                                  String name, String value) {
        fw.addInitParameter(name,  value);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketJettyDestination.java`
#### Snippet
```java
    }
    
    private static void addInitParameter(AtmosphereFramework fw, Map<String, String> initParams,
                                  String name, String value) {
        fw.addInitParameter(name,  value);
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkSoapAction(SoapMessage message, Map<String, List<String>> headers) {
        JMSFault jmsFault = null;
        String soapAction = null;
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkContentType(SoapMessage message, Map<String, List<String>> headers) {
        List<String> ct = headers.get(SoapJMSConstants.CONTENTTYPE_FIELD);
        JMSFault jmsFault = null;
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkRequestURI(SoapMessage message, Map<String, List<String>> headers) {
        List<String> ru = headers.get(SoapJMSConstants.REQUESTURI_FIELD);
        JMSFault jmsFault = null;
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkJMSMessageFormat(SoapMessage message, Map<String, List<String>> headers) {
        List<String> mt = headers.get(SoapJMSConstants.JMS_MESSAGE_TYPE);
        if (mt != null && !mt.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkBindingVersion(SoapMessage message, Map<String, List<String>> headers) {
        List<String> bv = headers.get(SoapJMSConstants.BINDINGVERSION_FIELD);
        if (bv != null && !bv.isEmpty()) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/SoapJMSInInterceptor.java`
#### Snippet
```java
     * @param headers
     */
    private void checkContentEncoding(SoapMessage message, Map<String, List<String>> headers) {
        List<String> contentEncodingList = headers.get(SoapJMSConstants.CONTENTENCODING_FIELD);
        JMSFault jmsFault = null;
```

### BoundedWildcard
Can generalize to `? extends Endpoint`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/EndpointSelectionInterceptor.java`
#### Snippet
```java
    }

    protected Endpoint selectEndpoint(Message message, Set<Endpoint> eps) {
        SoapVersion sv = ((SoapMessage)message).getVersion();

```

### BoundedWildcard
Can generalize to `? extends XMLEvent`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/saaj/SAAJInInterceptor.java`
#### Snippet
```java
    }

    private static void applyEvents(List<XMLEvent> events, SOAPElement el) throws SOAPException {
        if (events != null) {
            for (XMLEvent ev : events) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static List<SoapHeader> getSoapHeaders(List<ExtensibilityElement> exts) {
        List<SoapHeader> headers = new ArrayList<>();
        if (exts != null) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SoapOperation getSoapOperation(List<ExtensibilityElement> exts) {
        if (exts != null) {
            for (ExtensibilityElement ext : exts) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SoapBody getSoapBody(List<ExtensibilityElement> exts) {
        if (exts != null) {
            for (ExtensibilityElement ext : exts) {
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
    }

    public static SoapBinding getSoapBinding(List<ExtensibilityElement> exts) {
        for (ExtensibilityElement ext : exts) {
            if (isSOAPBinding(ext)) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/AbstractSaml2BearerGrant.java`
#### Snippet
```java
    }

    protected void addScope(MultivaluedMap<String, String> map) {
        if (scope != null) {
            map.putSingle(OAuthConstants.SCOPE, scope);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/AbstractSaml2BearerGrant.java`
#### Snippet
```java
    }

    protected void addScope(MultivaluedMap<String, String> map) {
        if (scope != null) {
            map.putSingle(OAuthConstants.SCOPE, scope);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/Saml2BearerGrantHandler.java`
#### Snippet
```java
    }

    public ServerAccessToken createAccessToken(Client client, MultivaluedMap<String, String> params)
        throws OAuthServiceException {

```

### BoundedWildcard
Can generalize to `? super QName`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
        }
    }
    private void initServiceSideInfo(Set<QName> mustUnderstandQNames, SoapMessage soapMessage,
                    Set<URI> serviceRoles, Set<QName> paramHeaders) {

```

### BoundedWildcard
Can generalize to `? super URI`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
    }
    private void initServiceSideInfo(Set<QName> mustUnderstandQNames, SoapMessage soapMessage,
                    Set<URI> serviceRoles, Set<QName> paramHeaders) {

        if (paramHeaders != null) {
```

### BoundedWildcard
Can generalize to `? extends QName`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
    }
    private void initServiceSideInfo(Set<QName> mustUnderstandQNames, SoapMessage soapMessage,
                    Set<URI> serviceRoles, Set<QName> paramHeaders) {

        if (paramHeaders != null) {
```

### BoundedWildcard
Can generalize to `? extends Header`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
    }

    private void checkUnderstand(Set<Header> mustUnderstandHeaders,
                                    Set<QName> mustUnderstandQNames,
                                    Set<QName> notUnderstandHeaders) {
```

### BoundedWildcard
Can generalize to `? super QName`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
    private void checkUnderstand(Set<Header> mustUnderstandHeaders,
                                    Set<QName> mustUnderstandQNames,
                                    Set<QName> notUnderstandHeaders) {

        for (Header header : mustUnderstandHeaders) {
```

### BoundedWildcard
Can generalize to `? super Header`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
    }

    private void buildMustUnderstandHeaders(Set<Header> mustUnderstandHeaders,
                                            SoapMessage soapMessage,
                                            Set<URI> serviceRoles,
```

### BoundedWildcard
Can generalize to `? super Header`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/MustUnderstandInterceptor.java`
#### Snippet
```java
                                            SoapMessage soapMessage,
                                            Set<URI> serviceRoles,
                                            Set<Header> ultimateReceiverHeaders) {
        for (Header header : soapMessage.getHeaders()) {
            if (header instanceof SoapHeader && ((SoapHeader)header).isMustUnderstand()) {
```

### BoundedWildcard
Can generalize to `? super WadlOption`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WADL2JavaMojo.java`
#### Snippet
```java
    BuildContext buildContext;

    private void mergeOptions(List<WadlOption> effectiveOptions) {
        if (wadlOptions == null) {
            return;
```

### BoundedWildcard
Can generalize to `? super Definition`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
        return msg;
    }
    private javax.wsdl.Message findMessage(QName qn, Definition def, List<Definition> done) {
        javax.wsdl.Message msg = def.getMessage(qn);
        if (msg == null) {
```

### BoundedWildcard
Can generalize to `? super MessagePartInfo`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
    }

    private void addSoapBodyPart(MessageInfo msg, List<MessagePartInfo> bodyParts, String partName) {
        MessagePartInfo mpi = msg.getMessagePart(new QName(msg.getName().getNamespaceURI(),
                                                           partName));
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WadlOption.java`
#### Snippet
```java
    // TODO: the following 3 helpers can go to a superclass or common utility class
    //       to be used by WADL and WSDL Pptions
    private static void addIfNotNull(List<String> list, Object value, String key) {
        if (value != null) {
            list.add(key);
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WadlOption.java`
#### Snippet
```java
    }

    private static void addIfTrue(List<String> list, boolean expression, String key) {
        if (expression) {
            list.add(key);
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/WadlOption.java`
#### Snippet
```java
    }

    private static void addList(List<String> destList, String key, boolean keyAsOwnElement,
                                List<String> sourceList) {
        if (sourceList == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
    //CHECKSTYLE:ON

    private void addCurrentNamespaceDecls(XMLStreamReader xmlReader, Map<String, String> bodyNsMap) {
        for (int i = 0; i < xmlReader.getNamespaceCount(); i++) {
            String nsuri = xmlReader.getNamespaceURI(i);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
    //CHECKSTYLE:ON

    private void addCurrentNamespaceDecls(XMLStreamReader xmlReader, Map<String, String> bodyNsMap) {
        for (int i = 0; i < xmlReader.getNamespaceCount(); i++) {
            String nsuri = xmlReader.getNamespaceURI(i);
```

### BoundedWildcard
Can generalize to `? extends Interceptor`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyOutInterceptor.java`
#### Snippet
```java
    }

    private static void addInterceptors(List<Interceptor<? extends Message>> interceptors, Message msg) {
        for (Interceptor<? extends Message> oi : interceptors) {
            msg.getInterceptorChain().add(oi);
```

### BoundedWildcard
Can generalize to `? super Element`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyConstants.java`
#### Snippet
```java
        return ret;
    }
    public static void findAllPolicyElementsOfLocalName(Element el, String localName, List<Element> val) {
        QName qn = DOMUtils.getElementQName(el);
        if (localName.equals(qn.getLocalPart()) && Constants.isInPolicyNS(qn)) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyDataEngineImpl.java`
#### Snippet
```java
    }

    private <T> T getPolicy(List<T> policies, PolicyCalculator<T> intersector) {
        T compatible = null;
        for (T p : policies) {
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyDataEngineImpl.java`
#### Snippet
```java
    }

    private <T> List<T> getPoliciesFromMessage(QName name, Message message, Class<T> dataClass) {
        List<T> policies = new ArrayList<>();
        AssertionInfoMap amap = message.get(AssertionInfoMap.class);
```

### BoundedWildcard
Can generalize to `? extends PolicyAttachment`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/ServiceModelPolicyUpdater.java`
#### Snippet
```java
    }

    public void addPolicyAttachments(Collection<PolicyAttachment> attachments) {
        for (PolicyAttachment pa : attachments) {
            boolean policyUsed = false;
```

### BoundedWildcard
Can generalize to `? extends Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java
     * @return true iff candidate is equal to one of the assertions in the collection
     */
    public static boolean contains(Collection<Assertion> assertions, Assertion candidate) {
        for (Assertion a : assertions) {
            if (a.equal(candidate)) {
```

### BoundedWildcard
Can generalize to `? extends Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java
     */
    public static boolean contains(Collection<Assertion> assertions,
                                   Collection<Assertion> candidates) {
        if (null == candidates || candidates.isEmpty()) {
            return true;
```

### BoundedWildcard
Can generalize to `? extends WadlOption`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
    }

    protected void downloadRemoteDocs(List<WadlOption> effectiveOptions) throws MojoExecutionException {

        for (WadlOption option : effectiveOptions) {
```

### BoundedWildcard
Can generalize to `? extends WadlOption`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
    }

    protected void forkOnce(Set<URI> classPath, List<WadlOption> effectiveOptions)
        throws MojoExecutionException {
        List<WadlOption> toDo = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends List`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
    }

    private String[] createForkOnceArgs(List<List<String>> wargs) throws MojoExecutionException {
        try {
            File f = FileUtils.createTempFile("cxf-w2j", "args");
```

### BoundedWildcard
Can generalize to `? super URI`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java


    private void addPluginArtifact(Set<URI> artifactsPath) {
        //for Maven 2.x, the actual artifact isn't in the list....  need to try and find it
        URL url = getClass().getResource(getClass().getSimpleName() + ".class");
```

### BoundedWildcard
Can generalize to `? super Interceptor`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java

    void initialiseInterceptors(PolicyInterceptorProviderRegistry reg, PolicyEngine engine,
                                Set<Interceptor<? extends org.apache.cxf.message.Message>> out, Assertion a,
                                boolean useIn, boolean fault,
                                Message m) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/selector/BaseAlternativeSelector.java`
#### Snippet
```java

    protected boolean isCompatibleWithRequest(List<Assertion> alternative,
                                   List<List<Assertion>> request) {
        if (request == null) {
            return true;
```

### BoundedWildcard
Can generalize to `? extends Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/selector/BaseAlternativeSelector.java`
#### Snippet
```java
    }

    protected boolean isCompatible(List<Assertion> alternative, List<Assertion> r) {
        List<QName> rNames = new ArrayList<>(r.size());
        for (Assertion ra : r) {
```

### BoundedWildcard
Can generalize to `? extends Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/selector/BaseAlternativeSelector.java`
#### Snippet
```java
    }

    protected boolean isCompatible(List<Assertion> alternative, List<Assertion> r) {
        List<QName> rNames = new ArrayList<>(r.size());
        for (Assertion ra : r) {
```

### BoundedWildcard
Can generalize to `? super Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
    }

    private void addAll(Collection<Assertion> target, Collection<Assertion> l1) {
        for (Assertion l : l1) {
            if (!target.contains(l)) {
```

### BoundedWildcard
Can generalize to `? extends Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
    }

    private void addAll(Collection<Assertion> target, Collection<Assertion> l1) {
        for (Assertion l : l1) {
            if (!target.contains(l)) {
```

### BoundedWildcard
Can generalize to `? super Interceptor`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java

    void initializeInterceptors(PolicyInterceptorProviderRegistry reg,
                                Set<Interceptor<? extends Message>> out, Assertion a,
                                boolean fault, Message msg) {
        QName qn = a.getName();
```

### BoundedWildcard
Can generalize to `? super Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionInfoMap.java`
#### Snippet
```java
    }

    private static void getAssertions(PolicyOperator p, Collection<Assertion> assertions) {
        List<PolicyComponent> pcs = p.getPolicyComponents();
        for (PolicyComponent pc : pcs) {
```

### BoundedWildcard
Can generalize to `? super Assertion`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java

    void addAssertions(PolicyComponent pc, boolean includeOptional,
                       Collection<Assertion> assertions) {

        if (Constants.TYPE_ASSERTION == pc.getType()) {
```

### BoundedWildcard
Can generalize to `? extends PolicyProvider`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyEngineImpl.java`
#### Snippet
```java

    
    public void setPolicyProviders(Collection<PolicyProvider> p) {
        policyProviders = new CopyOnWriteArrayList<>(p);
    }
```

### BoundedWildcard
Can generalize to `? extends ReplacementPair`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
    }

    private String applyMasks(String originalLogString, Set<ReplacementPair> replacementPairs) {
        String resultString = originalLogString;
        for (final ReplacementPair replacementPair : replacementPairs) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java

    public void maskHeaders(
            final Map<String, String> headerMap,
            final Set<String> sensitiveHeaderNames) {
        sensitiveHeaderNames.stream()
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java

    public void maskHeaders(
            final Map<String, String> headerMap,
            final Set<String> sensitiveHeaderNames) {
        sensitiveHeaderNames.stream()
```

### BoundedWildcard
Can generalize to `? super ReplacementPair`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
                                    final String replacementTemplate,
                                    final String sensitiveName,
                                    final Set<ReplacementPair> replacements) {
        final String matchPatternXML = matchPatternTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
        final String replacementXML = replacementTemplate.replaceAll(ELEMENT_NAME_TEMPLATE, sensitiveName);
```

### BoundedWildcard
Can generalize to `? extends ExtensibilityElement`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
    private static class ExtensibleInfo implements Extensible {
        private List<ExtensibilityElement> extensors;
        ExtensibleInfo(List<ExtensibilityElement> e) {
            extensors = e;
        }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
            return null;
        }
        public <T> List<T> getExtensors(Class<T> cls) {
            if (null == extensors) {
                return null;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/slf4j/Slf4jEventSender.java`
#### Snippet
```java
    }

    private void put(Set<String> keys, String key, String value) {
        if (value != null) {
            MDC.put(key, value);
```

### BoundedWildcard
Can generalize to `? extends JwsJsonSignatureEntry`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
        }
    }
    static Boolean validateB64Status(List<JwsJsonSignatureEntry> signatures) {
        Set<Boolean> b64Set = new LinkedHashSet<>();
        for (JwsJsonSignatureEntry entry : signatures) {
```

### BoundedWildcard
Can generalize to `? extends JwsSignatureProvider`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
    }

    public String signWith(List<JwsSignatureProvider> signers) {
        for (JwsSignatureProvider signer : signers) {
            signWith(signer);
```

### BoundedWildcard
Can generalize to `? extends JwsSignature`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonOutputStream.java`
#### Snippet
```java
    public JwsJsonOutputStream(OutputStream out,
                               List<String> protectedHeaders,
                               List<JwsSignature> signatures) {
        super(out);
        this.protectedHeaders = protectedHeaders;
```

### BoundedWildcard
Can generalize to `? extends JwsJsonSignatureEntry`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java

    public static Map<SignatureAlgorithm, List<JwsJsonSignatureEntry>> getJwsJsonSignatureMap(
        List<JwsJsonSignatureEntry> signatures) {
        Map<SignatureAlgorithm, List<JwsJsonSignatureEntry>> map = new HashMap<>();
        for (JwsJsonSignatureEntry entry : signatures) {
```

### BoundedWildcard
Can generalize to `? extends JweEncryptionProvider`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonProducer.java`
#### Snippet
```java
        return new JweEncryptionInput(jsonHeaders, content, aad);
    }
    private String checkAndGetContentAlgorithm(List<JweEncryptionProvider> encryptors) {
        Set<String> set = new HashSet<>();
        for (JweEncryptionProvider encryptor : encryptors) {
```

### BoundedWildcard
Can generalize to `? extends JweHeaders`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweJsonProducer.java`
#### Snippet
```java
    }
    public String encryptWith(List<JweEncryptionProvider> encryptors,
                              List<JweHeaders> recipientUnprotected) {
        checkAndGetContentAlgorithm(encryptors);
        if (recipientUnprotected != null
```

### BoundedWildcard
Can generalize to `? extends T`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingContext.java`
#### Snippet
```java

    @Override
    public <T> Callable<T> wrap(final String description, final Traceable<T> traceable) {
        final Callable<T> callable = new Callable<T>() {
            @Override
```

### BoundedWildcard
Can generalize to `? super String`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/internal/TextMapInjectAdapter.java`
#### Snippet
```java
    private final Map<String, List<String>> headers;
    
    public TextMapInjectAdapter(final Map<String, List<String>> headers) {
        this.headers = headers;
    }
```

### BoundedWildcard
Can generalize to `? super List`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/internal/TextMapInjectAdapter.java`
#### Snippet
```java
    private final Map<String, List<String>> headers;
    
    public TextMapInjectAdapter(final Map<String, List<String>> headers) {
        this.headers = headers;
    }
```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingProvider.java`
#### Snippet
```java
                                 final Map<String, List<Object>> responseHeaders,
                                 final int responseStatus,
                                 final TraceScopeHolder<TraceScope> holder) {

        if (holder == null) {
```

### BoundedWildcard
Can generalize to `? extends List`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingProvider.java`
#### Snippet
```java
    }

    private String getFirstValueOrEmpty(Map.Entry<String, List<String>> entry) {
        final List<String> values = entry.getValue();

```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingClientProvider.java`
#### Snippet
```java
    }

    protected void stopTraceSpan(final TraceScopeHolder<TraceScope> holder, final int responseStatus) {
        if (holder == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends TraceScope`
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingClientProvider.java`
#### Snippet
```java
    }
    
    protected void stopTraceSpan(final TraceScopeHolder<TraceScope> holder, final Throwable ex) {
        if (holder == null) {
            return;
```

### BoundedWildcard
Can generalize to `? extends X509Certificate`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        validateCertificateChain(ks, inCerts, enableRevocation);
    }
    private static void validateCertificateChain(KeyStore ks, List<X509Certificate> inCerts, boolean enableRevocation) {
        // Initial chain validation, to be enhanced as needed
        try {
```

### BoundedWildcard
Can generalize to `? extends X509Certificate`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        return encodeX509CertificateChain(Arrays.asList(chain));
    }
    public static List<String> encodeX509CertificateChain(List<X509Certificate> chain) {
        List<String> encodedChain = new ArrayList<>(chain.size());
        for (X509Certificate cert : chain) {
```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    }

    private void addTypeToMappableClasses(Set<Class<?>> mappableClasses, java.lang.reflect.Type jtype) {
        if (jtype instanceof Class) {
            Class<?> jclass = (Class<?>) jtype;
```

### BoundedWildcard
Can generalize to `? extends Element`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/XMLBeanTypeInfo.java`
#### Snippet
```java
    private Map<QName, BeanTypePropertyInfo> name2PropertyInfo = new HashMap<>();

    public XMLBeanTypeInfo(Class<?> typeClass, List<Element> mappings, String defaultNS) {
        super(typeClass, defaultNS);

```

### BoundedWildcard
Can generalize to `? super Class`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java

    private void getInterfacePropertyDescriptors(Class<?> clazz, List<PropertyDescriptor> pds,
                                                 Set<Class<?>> classes) {
        if (classes.contains(clazz)) {
            return;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/stax/JDOMNamespaceContext.java`
#### Snippet
```java
    }

    static void rawGetPrefixes(Element element, String namespaceURI, List<String> prefixes) {
        if (element.getNamespaceURI().equals(namespaceURI)) {
            prefixes.add(element.getNamespacePrefix());
```

### BoundedWildcard
Can generalize to `? extends GenericWsdlOption`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
    }

    protected void mergeOptions(List<GenericWsdlOption> effectiveWsdlOptions) {
        File outputDirFile = getGeneratedTestRoot() == null
            ? getGeneratedSourceRoot() : getGeneratedTestRoot();
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
    }

    private static void addIfTrue(List<String> list, boolean expression, String key) {
        if (expression) {
            list.add(key);
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
    }

    private static void addIfNotNull(List<String> list, Object value, String key) {
        if (value != null) {
            list.add(key);
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
    }

    private static void addList(List<String> destList, String key, boolean keyAsOwnElement,
                                List<String> sourceList) {
        if (sourceList == null) {
```

### BoundedWildcard
Can generalize to `? super String`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
        }
    }
    private static void addEqualsArray(List<String> destList,
                                      String key,
                                      String[] sourceList) {
```

### BoundedWildcard
Can generalize to `? extends AegisType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
    }

    private void createSchemas(Service s, Set<AegisType> deps) {

        Map<String, Set<AegisType>> tns2Type = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? super AegisType`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
    }

    private void addDependencies(Set<AegisType> deps, AegisType type) {
        Set<AegisType> typeDeps = type.getDependencies();
        if (typeDeps != null) {
```

### BoundedWildcard
Can generalize to `? super File`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
        private final List<File> errorfiles;

        MavenToolErrorListener(List<File> errorfiles) {
            this.errorfiles = errorfiles;
        }
```

### BoundedWildcard
Can generalize to `? extends List`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
    }

    private String[] createForkOnceArgs(List<List<String>> wargs) throws MojoExecutionException {
        try {
            File f = FileUtils.createTempFile("cxf-w2j", "args");
```

### BoundedWildcard
Can generalize to `? extends GenericWsdlOption`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java


    protected void downloadRemoteWsdls(List<GenericWsdlOption> effectiveWsdlOptions)
        throws MojoExecutionException {

```

### BoundedWildcard
Can generalize to `? super URI`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                                    Bus bus, Set<URI> cp) throws MojoExecutionException;

    protected void addPluginArtifact(Set<URI> artifactsPath) {
        // for Maven 2.x, the actual artifact isn't in the list.... need to try and find it
        URL url = getClass().getResource(getClass().getSimpleName() + ".class");
```

### BoundedWildcard
Can generalize to `? extends GenericWsdlOption`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
    }

    protected void forkOnce(Set<URI> classPath, List<GenericWsdlOption> effectiveWsdlOptions)
        throws MojoExecutionException {
        List<GenericWsdlOption> toDo = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends Artifact`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
    }

    private Artifact findWsdlArtifact(Artifact targetArtifact, Collection<Artifact> artifactSet) {
        if (artifactSet != null && !artifactSet.isEmpty()) {
            for (Artifact pArtifact : artifactSet) {
```

### BoundedWildcard
Can generalize to `? extends Feature`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactory.java`
#### Snippet
```java
     */
    public static <T> T create(String baseAddress, Class<T> cls, List<?> providers,
                               List<Feature> features,
                               String configLocation) {
        JAXRSClientFactoryBean bean = getBean(baseAddress, cls, configLocation);
```

### BoundedWildcard
Can generalize to `? super Key`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientRequestFilter.java`
#### Snippet
```java
    }

    public CacheControlClientRequestFilter setCache(final Cache<Key, Entry> c) {
        this.cache = c;
        return this;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
    }

    private Map<String, String> computeCacheHeaders(final MultivaluedMap<String, String> responseHeaders) {
        final Map<String, String> cacheHeaders = new HashMap<>(2);

```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
    }

    private long computeExpiry(CacheControl cacheControl, MultivaluedMap<String, String> responseHeaders) {
        // if a max-age property is set then it overrides Expires
        long expiry = cacheControl.getMaxAge();
```

### BoundedWildcard
Can generalize to `? super Key`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
    }

    public CacheControlClientReaderInterceptor setCache(final Cache<Key, Entry> c) {
        this.cache = c;
        return this;
```

### BoundedWildcard
Can generalize to `? super Entry`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
    }

    public CacheControlClientReaderInterceptor setCache(final Cache<Key, Entry> c) {
        this.cache = c;
        return this;
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java

    protected void setContexts(Message message, Exchange exchange,
                               Map<String, Object> context, boolean proxy) {
        if (context == null) {
            context = new HashMap<>();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
    protected Message createMessage(Object body,
                                    String httpMethod,
                                    MultivaluedMap<String, String> headers,
                                    URI currentURI,
                                    Exchange exchange,
```

### BoundedWildcard
Can generalize to `? extends JwsSignatureVerifier`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonConsumer.java`
#### Snippet
```java
    }

    public List<JwsJsonSignatureEntry> verifyAndGetNonValidated(List<JwsSignatureVerifier> validators,
                                                                Map<String, Object> entryProps) {
        List<JwsJsonSignatureEntry> validatedSignatures = new LinkedList<>();
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
     * @return typed collection
     */
    public <T> Collection<? extends T> postAndGetCollection(Object body, Class<T> memberClass) {
        return invokeAndGetCollection(HttpMethod.POST, body, memberClass);
    }
```

### BoundedWildcard
Can generalize to `? extends T2`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
    public <T1, T2> Collection<? extends T2> postAndGetCollection(Object collection,
                                                                  Class<T1> memberClass,
                                                                  Class<T2> responseClass) {
        Response r = doInvoke(HttpMethod.POST, collection, new ParameterizedCollectionType(memberClass),
                              Collection.class, new ParameterizedCollectionType(responseClass));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
     */
    public <T> Collection<? extends T> invokeAndGetCollection(String httpMethod, Object body,
                                                    Class<T> memberClass) {
        Response r = doInvoke(httpMethod, body, null,
                              Collection.class, new ParameterizedCollectionType(memberClass));
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
     * @return typed collection
     */
    public <T> Collection<? extends T> getCollection(Class<T> memberClass) {
        return invokeAndGetCollection(HttpMethod.GET, null, memberClass);
    }
```

### BoundedWildcard
Can generalize to `? extends T`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
     */
    public <T> Collection<? extends T> postObjectGetCollection(Object body,
                                                                  Class<T> responseClass) {
        Response r = doInvoke(HttpMethod.POST, body, null, Collection.class,
                              new ParameterizedCollectionType(responseClass));
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
     * @return "true" @BeanParam object has at least one @FormParam, "false" otherwise
     */
    private boolean hasFormParams(Object[] params, List<Parameter> beanParams) {
        return beanParams
            .stream()
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                                      Object[] params,
                                                      MultivaluedMap<ParameterType, Parameter> map,
                                                      List<Parameter> beanParams) {

        MultivaluedMap<String, String> form = new MetadataMap<>();
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
    protected void handleCookies(Method m,
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
    protected void handleCookies(Method m,
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
        List<Parameter> cs = getParameters(map, ParameterType.COOKIE);
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                            Object[] params,
                                            MultivaluedMap<ParameterType, Parameter> map,
                                            List<Parameter> beanParams,
                                            OperationResourceInfo ori,
                                            int bodyIndex) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                                Field f,
                                                Class<? extends Annotation> annClass,
                                                Map<String, BeanPair> values) {
        boolean jaxrsParamAnnAvailable = false;
        Annotation fieldAnnotation = f.getAnnotation(annClass);
```

### BoundedWildcard
Can generalize to `? super BeanPair`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                                Field f,
                                                Class<? extends Annotation> annClass,
                                                Map<String, BeanPair> values) {
        boolean jaxrsParamAnnAvailable = false;
        Annotation fieldAnnotation = f.getAnnotation(annClass);
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
    protected void handleHeaders(Method m,
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
```

### BoundedWildcard
Can generalize to `? super String`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
    protected void handleHeaders(Method m,
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                               Object[] params,
                               MultivaluedMap<String, String> headers,
                               List<Parameter> beanParams,
                               MultivaluedMap<ParameterType, Parameter> map) {
        List<Parameter> hs = getParameters(map, ParameterType.HEADER);
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                Object[] params,
                                MultivaluedMap<ParameterType, Parameter> map,
                                List<Parameter> beanParams,
                                UriBuilder ub) {
        List<Parameter> mx = getParameters(map, ParameterType.MATRIX);
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                               Object[] params,
                               MultivaluedMap<ParameterType, Parameter> map,
                               List<Parameter> beanParams,
                               UriBuilder ub) {
        List<Parameter> qs = getParameters(map, ParameterType.QUERY);
```

## RuleId[ruleID=AnonymousHasLambdaAlternative]
### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
                Runtime.getRuntime().removeShutdownHook(shutdownHook);
            }
            shutdownHook = new Thread() {
                @Override
                public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
        if (addHook) {
            final File f2 = newTmpDir;
            Thread hook = new Thread() {
                @Override
                public void run() {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
    @Produces(MediaType.SERVER_SENT_EVENTS)
    public void headers(@Context SseEventSink sink) {
        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
    @Produces(MediaType.SERVER_SENT_EVENTS)
    public void filtered(@Context SseEventSink sink) {
        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
    public void forBookPOST(@Context SseEventSink sink, @PathParam("id") final String id,
            final String lastEventId) {
        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore.java`
#### Snippet
```java
            @HeaderParam(HttpHeaders.LAST_EVENT_ID_HEADER) @DefaultValue("0") final String lastEventId) {

        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore2.java`
#### Snippet
```java
    @Produces(MediaType.SERVER_SENT_EVENTS)
    public void filtered(@Context SseEventSink sink) {
        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore2.java`
#### Snippet
```java
    public void forBookPOST(@Context SseEventSink sink, @PathParam("id") final String id,
            final String lastEventId) {
        new Thread() {
            public void run() {
                try {
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `systests/rs-sse/rs-sse-base/src/main/java/org/apache/cxf/systest/jaxrs/sse/BookStore2.java`
#### Snippet
```java
            @HeaderParam(HttpHeaders.LAST_EVENT_ID_HEADER) @DefaultValue("0") final String lastEventId) {

        new Thread() {
            public void run() {
                try {
```

## RuleId[ruleID=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `core/src/main/java/org/apache/cxf/common/util/ReflectionInvokationHandler.java`
#### Snippet
```java
                    boolean found = true;
                    for (int x = 0; x < m2.getParameterTypes().length; x++) {
                        if (args[x] != null
                            && !m2.getParameterTypes()[x].isInstance(args[x])) {
                            found = false;
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `core/src/main/java/org/apache/cxf/interceptor/FaultOutInterceptor.java`
#### Snippet
```java
        boolean supportsDOM = false;
        for (Class<?> c : db.getSupportedWriterFormats()) {
            if (c.equals(Node.class)) {
                supportsDOM = true;
            }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
        boolean supportsDOM = false;
        for (Class<?> c : db.getSupportedReaderFormats()) {
            if (c.equals(Node.class)) {
                supportsDOM = true;
            }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
                    boolean hasOf = false;
                    for (Class<?> c : tmp) {
                        if (c.getPackage() == cls.getPackage()
                            && "ObjectFactory".equals(c.getSimpleName())) {
                            hasOf = true;
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                        for (ServiceInfo si : serviceList) {
                            for (SchemaInfo sci : si.getSchemas()) {
                                if (key != null && key.equals(sci.getSystemId())) {
                                    key = null;
                                }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsServerFactoryBean.java`
#### Snippet
```java
            boolean found = false;
            for (Class<?> c : cls) {
                if (c.equals(seiClass)) {
                    found = true;
                }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
            }
            for (Feature feature : allFeatures) {
                if (feature instanceof WSAddressingFeature) {
                    findDispatchOp = true;
                }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
                boolean found = false;
                for (Annotation t : anns) {
                    if (t == jta) {
                        found = true;
                    }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                    for (QName type : types) {
                        for (AssertionInfo assertInfo : aim.getAssertionInfo(type)) {
                            if (assertInfo.isAsserted()) {
                                missingWsaHeader = false;
                            }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
        if (features != null) {
            for (Feature f : features) {
                if (f.getClass().isAnnotationPresent(EvaluateAllEndpoints.class)) {
                    setEPName = false;
                }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
        } else {
            for (int i = 0; i < this.filters.length; i++) {
                if ("java.".equals(this.filters[i])) {
                    javaCovered = true;
                }
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
            for (int i = java.length(); i >= 0; i--) {
                for (int j = 0; j < this.negativeFilters.length; j++) {
                    if (negativeFilters[j].equals(java.substring(0, i))) {
                        javaCovered = false;
                    }
```

## RuleId[ruleID=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/resource/Resource.java`
#### Snippet
```java
        name = TransferConstants.NAME_RESOURCE)
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
public interface Resource {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/resourcefactory/ResourceFactory.java`
#### Snippet
```java
        name = TransferConstants.NAME_RESOURCE_FACTORY)
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
public interface ResourceFactory {
    @Action(
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MetadataExchange.java`
#### Snippet
```java

@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(required = true, enabled = true)
@XmlSeeAlso({
    org.apache.cxf.ws.mex.model._2004_09.ObjectFactory.class })
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXEndpoint.java`
#### Snippet
```java
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@BindingType(jakarta.xml.ws.soap.SOAPBinding.SOAP12HTTP_BINDING)
@Addressing(required = true, enabled = true)
@XmlSeeAlso({
    org.apache.cxf.ws.mex.model._2004_09.ObjectFactory.class })
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/client/EndToEndpoint.java`
#### Snippet
```java
@WebService
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
public interface EndToEndpoint {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/WrappedSink.java`
#### Snippet
```java
            name = EventingConstants.WRAPPED_SINK_PORT_TYPE)
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
public interface WrappedSink {

```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/manager/SubscriptionManagerEndpoint.java`
#### Snippet
```java
@WebService(targetNamespace = EventingConstants.EVENTING_2011_03_NAMESPACE)
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
@HandlerChain(file = "/subscription-reference-parsing-handler-chain.xml")
public interface SubscriptionManagerEndpoint {
```

### DefaultAnnotationParam
Redundant default parameter value assignment
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/eventsource/EventSourceEndpoint.java`
#### Snippet
```java
@WebService(targetNamespace = EventingConstants.EVENTING_2011_03_NAMESPACE)
@SOAPBinding(parameterStyle = SOAPBinding.ParameterStyle.BARE)
@Addressing(enabled = true, required = true)
public interface EventSourceEndpoint {

```

## RuleId[ruleID=WhileLoopSpinsOnField]
### WhileLoopSpinsOnField
`while` loop spins on field
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsSubscription.java`
#### Snippet
```java
    class TerminationThread extends Thread {
        public void run() {
            while (checkTermination) {
                XMLGregorianCalendar tt = getTerminationTime();
                if (tt != null && isSessionActive) {
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
        public void run() {

            while (!shouldStopWatching) {

                try {
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        public int copyFrom(InputStream in) throws IOException {
            int count = 0;
            while (buffer != null) {
                int pos = buffer.size();
                int i = in.read(buffer.getRawBytes(), pos,
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        public int copyFrom(InputStream in) throws IOException {
            int count = 0;
            while (buffer != null) {
                int pos = buffer.size();
                int i = in.read(buffer.getRawBytes(), pos,
```

### WhileLoopSpinsOnField
`while` loop spins on field
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Source.java`
#### Snippet
```java
            SourceSequence seq = getAssociatedSequence(i);
            while (seq == null) {
                while (!sequenceCreationNotified) {
                    try {
                        sequenceCreationCondition.await();
```

## RuleId[ruleID=RegExpEmptyAlternationBranch]
### RegExpEmptyAlternationBranch
Empty branch in alternation
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/crypto/ModelEncryptionSupport.java`
#### Snippet
```java
 */
public final class ModelEncryptionSupport {
    public static final String SEP = "|";
    private ModelEncryptionSupport() {
    }
```

## RuleId[ruleID=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/AtomFeedProvider.java`
#### Snippet
```java
                writer.close();
            } catch (XMLStreamException ex) {
                throw new IOException(ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/src/main/java/org/apache/cxf/feature/transform/XSLTInInterceptor.java`
#### Snippet
```java
                StaxUtils.close(xReader);
            } catch (XMLStreamException ex) {
                throw new Fault(ex);
            }
            try {
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParametersConfig.java`
#### Snippet
```java
                StaxUtils.close(data);
            } catch (XMLStreamException ex) {
                throw new RuntimeException(ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/src/main/java/org/apache/cxf/configuration/spring/JAXBBeanFactory.java`
#### Snippet
```java
                StaxUtils.close(data);
            } catch (XMLStreamException ex) {
                throw new RuntimeException(ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
                StaxUtils.close(data);
            } catch (XMLStreamException ex) {
                throw new RuntimeException(ex);
            }
            JAXBUtils.closeUnmarshaller(u);
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java
                StaxUtils.close(xmlStreamReader);
            } catch (XMLStreamException e) {
                throw ExceptionUtils.toBadRequestException(e, null);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                StaxUtils.close(reader);
            } catch (XMLStreamException e) {
                throw ExceptionUtils.toBadRequestException(e, null);
            }
            JAXBUtils.closeUnmarshaller(unmarshaller);
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
                StaxUtils.close(reader);
            } catch (final XMLStreamException ex) {
                throw new XMLEncryptionException(ex);
            }
        }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
                StaxUtils.close(reader);
            } catch (XMLStreamException ex) {
                throw new WSDLException(WSDLException.PARSER_ERROR, ex.getMessage(), ex);
            }
            JAXBUtils.closeUnmarshaller(u);
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
                    StaxUtils.close(xmlReader);
                } catch (XMLStreamException ex) {
                    throw new WSDLException(WSDLException.PARSER_ERROR, ex.getMessage(), ex);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
                    StaxUtils.close(xmlReader);
                } catch (XMLStreamException e) {
                    throw new SoapFault(new Message("XML_STREAM_EXC", LOG, e.getMessage()), e,
                                        message.getVersion().getSender());
                }
```

## RuleId[ruleID=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
            this.servletPath = servletPath;
            if ("/".equals(this.servletPath) && !customServletPath) {
                if (this.pathInfo == null) {
                    this.pathInfo = "/";
                    this.servletPath = "";
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfigFactory.java`
#### Snippet
```java

        boolean resolveUsingJndi = endpoint.getJmsVariant().contains(JMSEndpoint.JNDI);
        if (resolveUsingJndi) {
            // Setup Destination jndi destination resolver
            JndiHelper jt = new JndiHelper(JMSConfigFactory.getInitialContextEnv(endpoint));
```

### IfStatementWithIdenticalBranches
'if' statement can be collapsed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
            .getExtensors(ExtensibilityElement.class) : null;
        ua = getUsingAddressing(exts);
        if (null != ua) {
            return ua;
        }
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperClassGenerator.java`
#### Snippet
```java
            }

            if (method == null) {
                ASMHelper.Label l3 = asmhelper.createLabel();
                mv.visitLabel(l3);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/ExtensionClassGenerator.java`
#### Snippet
```java
        mv.visitEnd();

        if (hasAttributes) {
            mv = cw.visitMethod(Opcodes.ACC_PUBLIC, "getRequired", "()Ljava/lang/Boolean;", null, null);
            mv.visitCode();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/InvocationHandlerFactory.java`
#### Snippet
```java
            }

            if (last != null) {
                last.setNext(newHandler);
                last = newHandler;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingProvider.java`
#### Snippet
```java
        final Span activeSpan;
        final Scope scope;
        if (parent == null) {
            activeSpan = tracer.buildSpan(buildSpanDescription(uri.getPath(), method)).start(); 
            scope = tracer.scopeManager().activate(activeSpan);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/AbstractOpenTracingClientProvider.java`
#### Snippet
```java
        final Span activeSpan;
        final Scope scope;
        if (parent == null) {
            activeSpan = tracer.buildSpan(buildSpanDescription(uri.toString(), method)).start(); 
            scope = tracer.scopeManager().activate(activeSpan);
```

## RuleId[ruleID=SimplifyOptionalCallChains]
### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/provider/StandardTags.java`
#### Snippet
```java
        Optional<Integer> statusCode = ofNullable(response)
           .map(e -> MessageUtils.getReponseCodeFromMessage(response));
        if (!statusCode.isPresent()) {
            return OUTCOME_UNKNOWN;
        }
```

## RuleId[ruleID=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/SpnegoAuthSupplier.java`
#### Snippet
```java
                                    Message message,
                                    String fullHeader) {
        return super.getAuthorization(authPolicy, currentURI, message);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                    LoadingByteArrayOutputStream tmp = buffer;
                    buffer = null;
                    super.write(tmp.getRawBytes(), 0, tmp.size());
                }
                boolean exceptionSet = outMessage.getContent(Exception.class) != null;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                    super.close();
                } else if (cachedStream != null) {
                    super.flush();
                    cachedStream.getOut().close();
                    cachedStream.closeFlowthroughStream();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                HttpServletRequest httpRequest = (HttpServletRequest)request;
                if (controller.filter(new HttpServletRequestFilter(httpRequest,
                                                                   super.getServletName()),
                                      (HttpServletResponse)response)) {
                    return;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                try {
                    Method m = ServletContext.class.getMethod("getFilterRegistration", new Class[] {String.class});
                    Object registration = m.invoke(super.getServletContext(), new Object[]{filterName});
                    if (registration != null) {
                        m = registration.getClass().getMethod("getUrlPatternMappings", new Class[] {});
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                    pathInfo = getRequestURI();
                }
                String prefix = super.getContextPath() + this.getServletPath();
                if (pathInfo.startsWith(prefix)) {
                    pathInfo = pathInfo.substring(prefix.length());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        String method = request.getMethod();
        if (KNOWN_HTTP_VERBS.contains(method)) {
            super.service(request, response);
        } else {
            handleRequest(request, response);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        String theServletPath = customServletPath ? dispatcherServletPath : "/";

        ServletContext sc = super.getServletContext();
        RequestDispatcher rd = dispatcherServletName != null
            ? sc.getNamedDispatcher(dispatcherServletName)
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
        List<ProviderInfo<?>> theProviders =
                prepareProviders(custom, busGlobal, providers, null);
        super.setCommonProviders(theProviders, RuntimeType.CLIENT);
        for (ProviderInfo<?> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(getBus(), provider.getProvider());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientFactoryBean.java`
#### Snippet
```java
        this.executorService = (executorService == null) ? Utils.defaultExecutorService() : executorService; 
        this.secConfig = secConfig;
        super.setAddress(baseUri);
        super.setServiceClass(aClass);
        super.setProviderComparator(comparator);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientFactoryBean.java`
#### Snippet
```java
        this.secConfig = secConfig;
        super.setAddress(baseUri);
        super.setServiceClass(aClass);
        super.setProviderComparator(comparator);
        super.setProperties(this.configuration.getProperties());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientFactoryBean.java`
#### Snippet
```java
        super.setAddress(baseUri);
        super.setServiceClass(aClass);
        super.setProviderComparator(comparator);
        super.setProperties(this.configuration.getProperties());
        registeredProviders = new ArrayList<>();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientFactoryBean.java`
#### Snippet
```java
        super.setServiceClass(aClass);
        super.setProviderComparator(comparator);
        super.setProperties(this.configuration.getProperties());
        registeredProviders = new ArrayList<>();
        registeredProviders.addAll(processProviders());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientFactoryBean.java`
#### Snippet
```java
        registeredProviders.add(new ProviderInfo<>(new JsrJsonpProvider(), getBus(), false));
        registeredProviders.add(new ProviderInfo<>(new JsrJsonbProvider(), getBus(), false));
        super.setProviders(registeredProviders);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
        // SAML assertions may contain enveloped XML signatures so
        // makes sense to avoid having them signed in the detached mode
        super.addAfter(XmlSigOutInterceptor.class.getName());

        super.addBefore(XmlEncOutInterceptor.class.getName());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
        super.addAfter(XmlSigOutInterceptor.class.getName());

        super.addBefore(XmlEncOutInterceptor.class.getName());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
    public SamlEnvelopedOutInterceptor(boolean signLater) {
        if (signLater) {
            super.addBefore(XmlSigOutInterceptor.class.getName());
        } else {
            super.addAfter(XmlSigOutInterceptor.class.getName());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
            super.addBefore(XmlSigOutInterceptor.class.getName());
        } else {
            super.addAfter(XmlSigOutInterceptor.class.getName());
        }
        this.signLater = signLater;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java
        this.signLater = signLater;

        super.addBefore(XmlEncOutInterceptor.class.getName());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/reactivestreams/src/main/java/org/apache/cxf/jaxrs/reactivestreams/server/StreamingAsyncSubscriber.java`
#### Snippet
```java
    }
    private void resumeAsyncResponse() {
        super.resume(new StreamingResponseImpl());
    }
    @Override
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/reactivestreams/src/main/java/org/apache/cxf/jaxrs/reactivestreams/server/StreamingAsyncSubscriber.java`
#### Snippet
```java
        }
        queue.add(bean);
        super.requestNext();
    }
    private class StreamingResponseImpl implements StreamingResponse<T> {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
        List<String> methods = samlAssertion.getConfirmationMethods();
        if (methods == null || methods.isEmpty()) {
            if (super.getRequiredSubjectConfirmationMethod() != null) {
                LOG.warning("A required subject confirmation method was not present");
                throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
                throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
                                          "invalidSAMLsecurity");
            } else if (super.isRequireStandardSubjectConfirmationMethod()) {
                LOG.warning("A standard subject confirmation method was not present");
                throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java

            if (method != null) {
                if (method.equals(super.getRequiredSubjectConfirmationMethod())) {
                    requiredMethodFound = true;
                }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
                    || SAML1Constants.CONF_BEARER.equals(method)) {
                    standardMethodFound = true;
                    if (super.isRequireBearerSignature() && !signed && !signedResponse) {
                        LOG.warning("A Bearer Assertion was not signed");
                        throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
        }

        if (!requiredMethodFound && super.getRequiredSubjectConfirmationMethod() != null) {
            LOG.warning("A required subject confirmation method was not present");
            throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
        }

        if (!standardMethodFound && super.isRequireStandardSubjectConfirmationMethod()) {
            LOG.warning("A standard subject confirmation method was not present");
            throw new WSSecurityException(WSSecurityException.ErrorCode.FAILURE,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/circuitbreaker/CircuitBreakerFailoverFeature.java`
#### Snippet
```java
            if (this.targetSelector == null) {
                this.targetSelector = new CircuitBreakerTargetSelector(threshold, timeout,
                        super.getClientBootstrapAddress());
            }
            return this.targetSelector;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerClientMetricsContext.java`
#### Snippet
```java
    @Override
    protected void record(TimingContext timingContext, Exchange ex) {
        super.record(timingContext, ex, true);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerClientMetricsContext.java`
#### Snippet
```java
    @Override
    public void start(Exchange ex) {
        super.start(ex.getOutMessage(), ex);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerClientMetricsContext.java`
#### Snippet
```java
    @Override
    public void stop(long timeInNS, long inSize, long outSize, Exchange ex) {
        super.stop(ex.getOutMessage(), timeInNS, inSize, outSize, ex);
    }
    
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerServerMetricsContext.java`
#### Snippet
```java
    @Override
    public void start(Exchange ex) {
        super.start(ex.getInMessage(), ex);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerServerMetricsContext.java`
#### Snippet
```java
    @Override
    public void stop(long timeInNS, long inSize, long outSize, Exchange ex) {
        super.stop(ex.getInMessage(), timeInNS, inSize, outSize, ex);
    }
    
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/micrometer/MicrometerServerMetricsContext.java`
#### Snippet
```java
    @Override
    protected void record(TimingContext timingContext, Exchange ex) {
        super.record(timingContext, ex, false);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiCustomizedResource.java`
#### Snippet
```java
        }

        return super.getOpenApi(headers, config, app, uriInfo, type);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            if (ui.getPath().endsWith(APIDOCS_LISTING_PATH_JSON)) {
                try {
                    response = super.getOpenApi(mc.getHttpHeaders(), mc.getServletConfig(), app, ui, "json");
                } catch (Exception ex) {
                    throw new IOException(ex);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            } else if (ui.getPath().endsWith(APIDOCS_LISTING_PATH_YAML)) {
                try {
                    response = super.getOpenApi(mc.getHttpHeaders(), mc.getServletConfig(), app, ui, "yaml");
                } catch (Exception ex) {
                    throw new IOException(ex);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            if (ui.getPath().endsWith(APIDOCS_LISTING_PATH_JSON)) {
                try {
                    response = super.getOpenApi(app, mc.getServletConfig(), mc.getHttpHeaders(), ui, "json");
                } catch (Exception ex) {
                    throw new IOException(ex);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            } else if (ui.getPath().endsWith(APIDOCS_LISTING_PATH_YAML)) {
                try {
                    response = super.getOpenApi(app, mc.getServletConfig(), mc.getHttpHeaders(), ui, "yaml");
                } catch (Exception ex) {
                    throw new IOException(ex);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java

    public Long count() {
        if (super.getQueryClass() != Long.class) {
            throw new IllegalStateException("Query class needs to be of type Long");
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java
        CriteriaQuery<Long> countQuery = (CriteriaQuery<Long>)getCriteriaQuery();
        countQuery.select(getCriteriaBuilder().count(getRoot()));
        return super.getEntityManager().createQuery(countQuery).getSingleResult();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java

    private TypedQuery<E> getTypedQuery(CriteriaQuery<E> theCriteriaQuery) {
        return super.getEntityManager().createQuery(theCriteriaQuery);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
        validatePropertyValue(name, propertyValue);

        name = super.getRealPropertyName(name);
        ClassValue cv = getPrimitiveFieldClass(ps,
                                               name,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClaimsValidator.java`
#### Snippet
```java
            theJwsVerifier = JwsUtils.getSignatureVerifier(key, jwt.getJwsHeaders().getSignatureAlgorithm());
        } else {
            theJwsVerifier = super.getInitializedSignatureVerifier(jwt.getJwsHeaders());
        }
        if (theJwsVerifier == null) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
                strValue = strValue.toLowerCase();
            }
            boolean isWildCard = strValue.contains("*") || super.isWildcardStringMatch();

            String theContentsFieldName = getContentsFieldName(name);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java

    private Query buildSimpleQuery(ConditionType ct, String name, Object value) {
        name = super.getRealPropertyName(name);
        validatePropertyValue(name, value);

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/UserInfoService.java`
#### Snippet
```java
        userInfo.setSubject(idToken.getSubject());

        if (super.isJwsRequired()) {
            userInfo.setIssuer(idToken.getIssuer());
            userInfo.setAudience(idToken.getAudience());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/UserInfoService.java`
#### Snippet
```java
        final Object responseEntity;
        // UserInfo may be returned in a clear form as JSON
        if (super.isJwsRequired() || super.isJweRequired()) {
            Client client = null;
            if (oauthDataProvider != null) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/UserInfoService.java`
#### Snippet
```java
        final Object responseEntity;
        // UserInfo may be returned in a clear form as JSON
        if (super.isJwsRequired() || super.isJweRequired()) {
            Client client = null;
            if (oauthDataProvider != null) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/UserInfoService.java`
#### Snippet
```java
                client = oauthDataProvider.getClient(oauth.getClientId());
            }
            responseEntity = super.processJwt(new JwtToken(userInfo), client);
        } else {
            responseEntity = convertUserInfoToResponseEntity(userInfo);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/IdTokenResponseFilter.java`
#### Snippet
```java
            Properties props = JwsUtils.loadSignatureOutProperties(false);
            final SignatureAlgorithm sigAlgo;
            if (super.isSignWithClientSecret()) {
                sigAlgo = OAuthUtils.getClientSecretSignatureAlgorithm(props);
            } else {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/IdTokenResponseFilter.java`
#### Snippet
```java
        if (keyServiceClient != null) {
            List<String> opers = new LinkedList<>();
            if (super.isJwsRequired()) {
                opers.add(JsonWebKey.KEY_OPER_SIGN);
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/IdTokenResponseFilter.java`
#### Snippet
```java
                opers.add(JsonWebKey.KEY_OPER_SIGN);
            }
            if (super.isJweRequired()) {
                opers.add(JsonWebKey.KEY_OPER_ENCRYPT);
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/OidcAuthorizationCodeService.java`
#### Snippet
```java

    public void setSkipAuthorizationWithOidcScope(boolean skipAuthorizationWithOidcScope) {
        super.setScopesRequiringNoConsent(Collections.singletonList(OidcUtils.OPENID_SCOPE));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
        }
        public char[] rawCharArray() {
            return super.buf;
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/OidcImplicitService.java`
#### Snippet
```java

    public void setSkipAuthorizationWithOidcScope(boolean skipAuthorizationWithOidcScope) {
        super.setScopesRequiringNoConsent(Collections.singletonList(OidcUtils.OPENID_SCOPE));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/bus/resource/ResourceManagerImpl.java`
#### Snippet
```java
            bus = b;
            firstCalled = false;
            super.addResourceResolver(new ObjectTypeResolver(bus));
            if (null != bus) {
                bus.setExtension(this, ResourceManager.class);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/common/spi/ClassGeneratorClassLoader.java`
#### Snippet
```java
            if (name.endsWith("package-info")) {
                String s = name.substring(0, name.length() - 13);
                Package p = super.getPackage(s);
                if (p == null) {
                    definePackage(StringUtils.slashesToPeriod(s),
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/common/spi/GeneratedClassClassLoader.java`
#### Snippet
```java
            if (name.endsWith("package-info")) {
                String s = name.substring(0, name.length() - 13);
                Package p = super.getPackage(s);
                if (p == null) {
                    definePackage(StringUtils.slashesToPeriod(s),
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/common/util/MessageDigestInputStream.java`
#### Snippet
```java
    }
    public byte[] getDigestBytes() {
        return super.getMessageDigest().digest();
    }
    public String getBase64Digest() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/feature/transform/XSLTOutInterceptor.java`
#### Snippet
```java
        public void close() {
            try {
                super.flush();
                try (Reader transformedReader = XSLTUtils.transform(xsltTemplate, cachedWriter.getReader())) {
                    StaxUtils.copy(new StreamSource(transformedReader), origXWriter);                    
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/message/MessageContentsList.java`
#### Snippet
```java
     */
    public Object get(MessagePartInfo key) {
        return super.get(key.getIndex());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/message/ExchangeImpl.java`
#### Snippet
```java

    public <T> T remove(Class<T> key) {
        return key.cast(super.remove(key.getName()));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/message/ExchangeImpl.java`
#### Snippet
```java
        setMessageContextProperty(outFaultMessage, key, value);
        if (value == null) {
            return super.remove(key);
        }
        return super.put(key, value);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/message/ExchangeImpl.java`
#### Snippet
```java
    public <T> void put(Class<T> key, T value) {
        if (value == null) {
            super.remove((Object)key);
        } else if (key == Bus.class) {
            resetContextCaches();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/transform/IgnoreNamespacesWriter.java`
#### Snippet
```java

    public void writeStartElement(String prefix, String local, String uri) throws XMLStreamException {
        super.writeStartElement(local);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/transform/IgnoreNamespacesWriter.java`
#### Snippet
```java

    public void writeAttribute(String uri, String local, String value) throws XMLStreamException {
        super.writeAttribute(local, value);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/transform/IgnoreNamespacesWriter.java`
#### Snippet
```java
            return;
        }
        super.writeAttribute(local, value);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/transform/IgnoreNamespacesWriter.java`
#### Snippet
```java

    public void writeStartElement(String uri, String local) throws XMLStreamException {
        super.writeStartElement(local);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                                         boolean force, Message msg) {
            super(t);
            super.wrappedStream = orig;
            message = msg;
            if (force) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
        public void reload() {
            super.clear();
            super.putAll(requestContext.get(latestContextThread));
        }
        
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
        if (!attributesToElements) {
            if (!uri.isEmpty()) {
                super.writeAttribute(uri, local, value);
            } else {
                super.writeAttribute(local, value);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/interceptor/AbstractAttributedInterceptorProvider.java`
#### Snippet
```java
    public Object put(String s, Object o) {
        if (o == null) {
            return super.remove(s);
        }
        return super.put(s, o);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `core/src/main/java/org/apache/cxf/interceptor/security/SecureAnnotationsInterceptor.java`
#### Snippet
```java
            }
        }
        super.setMethodRolesMap(rolesMap);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
            config.setJsonNamespaceSeparator(namespaceSeparator);
        }
        if (!dropElementsInXmlStreamProp && super.outDropElements != null) {
            config.setIgnoredElements(outDropElements);
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
    @Context
    public void setMessageContext(MessageContext mc) {
        super.setContext(mc);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AuthPolicyValidatingInterceptor.java`
#### Snippet
```java

        try {
            super.validate(message);
        } catch (Exception ex) {
            throw new Fault(ex);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSStaxTokenValidator.java`
#### Snippet
```java
                    super.verify();
                } catch (XMLSecurityException ex) {
                    SamlAssertionWrapper assertion = super.getSamlAssertionWrapper();
                    Element tokenElement = assertion.getElement();
                    validateTokenToSTS(tokenElement, message);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java
        StaxStartBodyInterceptor() {
            super(Phase.READ);
            super.addAfter(StartBodyInterceptor.class.getName());
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JInInterceptor.java`
#### Snippet
```java
     */
    protected void setAlgorithmSuites(SoapMessage message, RequestData data) throws WSSecurityException {
        super.decodeAlgorithmSuite(data);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
        }

        return super.getPasswordEncryptor(requestData);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
                InternalProtectionToken(SPVersion version, Policy nestedPolicy) {
                    super(version, nestedPolicy);
                    super.setToken(secureConversationToken);
                }
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEndorsingEncryptedTokenPolicyValidator.java`
#### Snippet
```java
                PolicyUtils.getFirstAssertionByLocalname(parameters.getAssertionInfoMap(),
                                                         SPConstants.TRANSPORT_BINDING);
            super.setEnforceEncryptedTokens(transportAi == null);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EndorsingEncryptedTokenPolicyValidator.java`
#### Snippet
```java
                PolicyUtils.getFirstAssertionByLocalname(parameters.getAssertionInfoMap(),
                                                         SPConstants.TRANSPORT_BINDING);
            super.setEnforceEncryptedTokens(transportAi == null);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EncryptedTokenPolicyValidator.java`
#### Snippet
```java
                PolicyUtils.getFirstAssertionByLocalname(parameters.getAssertionInfoMap(),
                                                         SPConstants.TRANSPORT_BINDING);
            super.setEnforceEncryptedTokens(transportAi == null);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEncryptedTokenPolicyValidator.java`
#### Snippet
```java
                PolicyUtils.getFirstAssertionByLocalname(parameters.getAssertionInfoMap(),
                                                         SPConstants.TRANSPORT_BINDING);
            super.setEnforceEncryptedTokens(transportAi == null);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveClientStopInterceptor.java`
#### Snippet
```java
        }

        super.stopTraceSpan(holder, responseCode);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveStartInterceptor.java`
#### Snippet
```java
        final ParsedMessage parsed = new ParsedMessage(message);

        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(parsed.getHeaders(),
            parsed.getUri(), parsed.getHttpMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveStopInterceptor.java`
#### Snippet
```java
        }

        super.stopTraceSpan(requestHeaders, responseHeaders, responseCode, holder);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/jaxrs/BraveClientProvider.java`
#### Snippet
```java
    @Override
    public void filter(final ClientRequestContext requestContext) throws IOException {
        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(requestContext.getStringHeaders(),
            requestContext.getUri(), requestContext.getMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/jaxrs/BraveClientProvider.java`
#### Snippet
```java
        final TraceScopeHolder<TraceScope> holder =
            (TraceScopeHolder<TraceScope>)requestContext.getProperty(TRACE_SPAN);
        super.stopTraceSpan(holder, responseContext.getStatus());
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveClientStartInterceptor.java`
#### Snippet
```java
        final ParsedMessage parsed = new ParsedMessage(message);

        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(parsed.getHeaders(),
            parsed.getUri(), parsed.getHttpMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveClientStartInterceptor.java`
#### Snippet
```java
        
        final Exception ex = message.getContent(Exception.class);
        super.stopTraceSpan(holder, ex);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/jaxrs/BraveProvider.java`
#### Snippet
```java
    @Override
    public void filter(final ContainerRequestContext requestContext) throws IOException {
        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(requestContext.getHeaders(),
            requestContext.getUriInfo().getRequestUri(), requestContext.getMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/jaxrs/BraveProvider.java`
#### Snippet
```java
    public void filter(final ContainerRequestContext requestContext,
            final ContainerResponseContext responseContext) throws IOException {
        super.stopTraceSpan(requestContext.getHeaders(), responseContext.getHeaders(),
            responseContext.getStatus(), (TraceScopeHolder<TraceScope>)requestContext.getProperty(TRACE_SPAN));
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
                DestinationFactoryManager dfm = getBus().getExtension(DestinationFactoryManager.class);
                df = dfm.getDestinationFactoryForUri(getAddress());
                super.setDestinationFactory(df);
            }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerResponseContextImpl.java`
#### Snippet
```java
        return InjectionUtils.getGenericResponseType(invoked,
                                              serviceCls,
                                              super.r.getActualEntity(),
                                              getEntityClass(),
                                              super.m.getExchange());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerResponseContextImpl.java`
#### Snippet
```java
                                              super.r.getActualEntity(),
                                              getEntityClass(),
                                              super.m.getExchange());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerResponseContextImpl.java`
#### Snippet
```java
    @Override
    public Class<?> getEntityClass() {
        return InjectionUtils.getRawResponseClass(super.r.getActualEntity());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerResponseContextImpl.java`
#### Snippet
```java
    @Override
    public Annotation[] getEntityAnnotations() {
        return super.getResponseEntityAnnotations();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServerFactoryBean.java`
#### Snippet
```java
        setBeanInfo(factory);
        factory.setApplicationProvider(appProvider);
        super.setupFactory(factory, ep);
        return factory;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/WriterInterceptorContextImpl.java`
#### Snippet
```java
    public void setEntity(Object object) {
        entity = object;
        super.setType(entity != null ? entity.getClass() : null);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ApplicationInfo.java`
#### Snippet
```java

    public Map<String, Object> getProperties() {
        Map<String, Object> appProps = super.getProvider().getProperties();
        if (overridingProps.isEmpty()) {
            return appProps;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java
                }
            }
            List<Method> methods = super.getParameterMethods();
            for (Method m : methods) {
                if (m.getAnnotation(BeanParam.class) != null) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/ClassResourceInfo.java`
#### Snippet
```java
                }
            }
            List<Field> fields = super.getParameterFields();
            for (Field f : fields) {
                if (f.getAnnotation(BeanParam.class) != null) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/SpringJaxrsClassesScanServer.java`
#### Snippet
```java
    @Bean
    public Server jaxRsServer() {
        return super.createJaxRsServer();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/JAXRSServerFactoryBeanDefinitionParser.java`
#### Snippet
```java

        public void destroy() {
            Server server = super.getServer();
            if (server != null && server.isStarted()) {
                server.destroy();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/SpringComponentScanServer.java`
#### Snippet
```java
    @Bean
    public Server jaxRsServer() {
        return super.createJaxRsServer();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractJaxrsClassesScanServer.java`
#### Snippet
```java

            List<Object> jaxrsServices = JAXRSServerFactoryBeanDefinitionParser
                .createBeansFromDiscoveredClasses(super.applicationContext, classes.get(Path.class), null);
            List<Object> jaxrsProviders = JAXRSServerFactoryBeanDefinitionParser
                .createBeansFromDiscoveredClasses(super.applicationContext, classes.get(Provider.class), null);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractJaxrsClassesScanServer.java`
#### Snippet
```java
                .createBeansFromDiscoveredClasses(super.applicationContext, classes.get(Path.class), null);
            List<Object> jaxrsProviders = JAXRSServerFactoryBeanDefinitionParser
                .createBeansFromDiscoveredClasses(super.applicationContext, classes.get(Provider.class), null);

            factory.setServiceBeans(jaxrsServices);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
                
                List<Application> apps = CastUtils.cast(JAXRSServerFactoryBeanDefinitionParser
                    .createBeansFromDiscoveredClasses(super.applicationContext, 
                                                      appClasses.get(ApplicationPath.class), null));
                if (!apps.isEmpty()) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
            cxfFeatures.add((Feature)bean);
        } else if (ann.value() == org.apache.cxf.annotations.Provider.Type.InInterceptor) {
            super.getInInterceptors().add((Interceptor<?>)bean);
        } else if (ann.value() == org.apache.cxf.annotations.Provider.Type.OutInterceptor) {
            super.getOutInterceptors().add((Interceptor<?>)bean);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
            super.getInInterceptors().add((Interceptor<?>)bean);
        } else if (ann.value() == org.apache.cxf.annotations.Provider.Type.OutInterceptor) {
            super.getOutInterceptors().add((Interceptor<?>)bean);
        } else if (ann.value() == org.apache.cxf.annotations.Provider.Type.OutFaultInterceptor) {
            super.getOutFaultInterceptors().add((Interceptor<?>)bean);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
            super.getOutInterceptors().add((Interceptor<?>)bean);
        } else if (ann.value() == org.apache.cxf.annotations.Provider.Type.OutFaultInterceptor) {
            super.getOutFaultInterceptors().add((Interceptor<?>)bean);
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
            int i = super.next();
            if (i == XMLStreamConstants.START_ELEMENT) {
                QName qn = super.getName();
                isInclude = qn.equals(WSDLConstants.QNAME_SCHEMA_INCLUDE);
                isImport = qn.equals(WSDLConstants.QNAME_SCHEMA_IMPORT);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
            int i = super.nextTag();
            if (i == XMLStreamConstants.START_ELEMENT) {
                QName qn = super.getName();
                isInclude = qn.equals(WSDLConstants.QNAME_SCHEMA_INCLUDE);
                isImport = qn.equals(WSDLConstants.QNAME_SCHEMA_IMPORT);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
        List<ProviderInfo<? extends Object>> theProviders =
            prepareProviders(custom, busGlobal, allProviders.toArray(), application);
        super.setCommonProviders(theProviders, RuntimeType.SERVER);
        for (ProviderInfo<? extends Object> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(getBus(), provider.getProvider());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java
        private void doSetParameters(String name, String[] values) {
            if (saveParamsAsAttributes) {
                super.setAttribute(name, values);
            } else {
                params.put(name, values);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/security/KerberosAuthOutInterceptor.java`
#### Snippet
```java
    public void handleMessage(Message message) throws Fault {
        URI currentURI = getCurrentURI(message);
        String value = super.getAuthorization(getPolicy(),
                                              currentURI,
                                              message);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/validation/JAXRSBeanValidationOutInterceptor.java`
#### Snippet
```java
            || supportMultipleValidations) {
            try {
                super.handleMessage(message);
            } finally {
                message.put(OUT_VALIDATION_DONE, Boolean.TRUE);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
    @Context
    public void setMessageContext(MessageContext mc) {
        super.setContext(mc);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweJsonClientResponseFilter.java`
#### Snippet
```java
            res.getHeaders().putSingle("Content-Type", ct);
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), out.getHeaders());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweJsonClientResponseFilter.java`
#### Snippet
```java
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), out.getHeaders());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsJsonContainerRequestFilter.java`
#### Snippet
```java
            context.getHeaders().putSingle("Content-Type", ct);
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), sigEntry.getProtectedHeader());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsJsonContainerRequestFilter.java`
#### Snippet
```java
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), sigEntry.getProtectedHeader());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweContainerRequestFilter.java`
#### Snippet
```java
            context.getHeaders().putSingle("Content-Type", ct);
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), out.getHeaders());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweContainerRequestFilter.java`
#### Snippet
```java
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), out.getHeaders());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweClientResponseFilter.java`
#### Snippet
```java
            res.getHeaders().putSingle("Content-Type", ct);
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), out.getHeaders());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweClientResponseFilter.java`
#### Snippet
```java
        }
        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), out.getHeaders());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweJsonContainerRequestFilter.java`
#### Snippet
```java
                context.getHeaders().putSingle("Content-Type", ct);
            }
            if (super.isValidateHttpHeaders()) {
                super.validateHttpHeadersIfNeeded(context.getHeaders(), out.getHeaders());
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JweJsonContainerRequestFilter.java`
#### Snippet
```java
            }
            if (super.isValidateHttpHeaders()) {
                super.validateHttpHeadersIfNeeded(context.getHeaders(), out.getHeaders());
            }
        } catch (JweException ex) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtAuthenticationClientFilter.java`
#### Snippet
```java
    public void filter(ClientRequestContext requestContext) throws IOException {
        JwtToken jwt = getJwtToken(requestContext);
        if (jwt == null && super.isJweRequired()) {
            AuthorizationPolicy ap = JAXRSUtils.getCurrentMessage().getExchange()
                .getEndpoint().getEndpointInfo().getExtensor(AuthorizationPolicy.class);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtAuthenticationClientFilter.java`
#### Snippet
```java
            throw new JoseException("JWT token is not available");
        }
        String data = super.processJwt(jwt);
        requestContext.getHeaders().putSingle(HttpHeaders.AUTHORIZATION,
                                              authScheme + " " + data);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsClientResponseFilter.java`
#### Snippet
```java
        }

        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), p.getJwsHeaders());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsClientResponseFilter.java`
#### Snippet
```java

        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(res.getHeaders(), p.getJwsHeaders());
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsContainerRequestFilter.java`
#### Snippet
```java
        }

        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), p.getJwsHeaders());
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwsContainerRequestFilter.java`
#### Snippet
```java

        if (super.isValidateHttpHeaders()) {
            super.validateHttpHeadersIfNeeded(context.getHeaders(), p.getJwsHeaders());
        }
        
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJwtAuthenticationFilter.java`
#### Snippet
```java
    public void filter(ContainerRequestContext requestContext) throws IOException {
        String encodedJwtToken = getEncodedJwtToken(requestContext);
        JwtToken token = super.getJwtToken(encodedJwtToken);

        SecurityContext securityContext = configureSecurityContext(token);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsProxyFactoryBean.java`
#### Snippet
```java
            Service service = getServiceFactory().getService();
            if (needWrapperClassInterceptor(service.getServiceInfos().get(0))) {
                List<Interceptor<? extends Message>> in = super.getInInterceptors();
                List<Interceptor<? extends Message>> out = super.getOutInterceptors();
                in.add(new WrapperClassInInterceptor());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsProxyFactoryBean.java`
#### Snippet
```java
            if (needWrapperClassInterceptor(service.getServiceInfos().get(0))) {
                List<Interceptor<? extends Message>> in = super.getInInterceptors();
                List<Interceptor<? extends Message>> out = super.getOutInterceptors();
                in.add(new WrapperClassInInterceptor());
                in.add(new HolderInInterceptor());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        createJaxwsBinding();

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();

        boolean isProvider = implInfo != null && implInfo.isWebServiceProvider();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        handlerInterceptorsAdded = false;

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();

        in.remove(logicalInInterceptor);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        handlerInterceptorsAdded = true;

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        List<Interceptor<? extends Message>> in = super.getInInterceptors();
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        List<Interceptor<? extends Message>> out = super.getOutInterceptors();
        List<Interceptor<? extends Message>> outFault = super.getOutFaultInterceptors();
        List<Interceptor<? extends Message>> inFault = super.getInFaultInterceptors();

        in.add(logicalInInterceptor);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
        setJaxWsImplementorInfo(new JaxWsImplementorInfo(serviceClass));
        super.setServiceClass(getJaxWsImplementorInfo().getEndpointClass());
        super.setServiceType(getJaxWsImplementorInfo().getSEIType());
    }
    @Override
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMManagerBeanDefinitionParser.java`
#### Snippet
```java
        }

        super.parseChildElements(element, ctx, bean);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/ClientAccessToken.java`
#### Snippet
```java
    @Override
    public String toString() {
        if (OAuthConstants.BEARER_AUTHORIZATION_SCHEME.equalsIgnoreCase(super.getTokenType())) {
            return OAuthConstants.BEARER_AUTHORIZATION_SCHEME + " " + super.getTokenKey();
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/ClientAccessToken.java`
#### Snippet
```java
    public String toString() {
        if (OAuthConstants.BEARER_AUTHORIZATION_SCHEME.equalsIgnoreCase(super.getTokenType())) {
            return OAuthConstants.BEARER_AUTHORIZATION_SCHEME + " " + super.getTokenKey();
        }
        return super.toString();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/JwtBearerAuthHandler.java`
#### Snippet
```java
            message.put(Client.class, client);
        }
        JwtToken token = super.getJwtToken(assertion, client);

        String subjectName = (String)token.getClaim(JwtConstants.CLAIM_SUBJECT);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACMTCodeDataProvider.java`
#### Snippet
```java
            // lock RT for update
            lockRefreshTokenForUpdate(rt);
            return super.updateRefreshToken(rt, at);
        }
        return super.updateExistingRefreshToken(rt, at);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JwtRequestCodeFilter.java`
#### Snippet
```java
            return JwsUtils.getPublicKeySignatureVerifier(cert, SignatureAlgorithm.RS256);
        }
        return super.getInitializedSignatureVerifier(c.getClientSecret());
    }
    public void setIssuer(String issuer) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JwtRequestCodeFilter.java`
#### Snippet
```java

        if (requestToken != null) {
            JweDecryptionProvider theDecryptor = super.getInitializedDecryptionProvider(client.getClientSecret());
            JwsSignatureVerifier theSigVerifier = getInitializedSigVerifier(client);
            JwtToken jwt = getJwtToken(requestToken, theDecryptor, theSigVerifier);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/HawkAccessToken.java`
#### Snippet
```java
    private void setExtraParameters(HmacAlgorithm algo, String macKey) {
        String theKey = macKey == null ? HmacUtils.generateKey(algo.getJavaName()) : macKey;
        super.getParameters().put(OAuthConstants.HAWK_TOKEN_KEY,
                                  theKey);
        super.getParameters().put(OAuthConstants.HAWK_TOKEN_ALGORITHM,
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/HawkAccessToken.java`
#### Snippet
```java
        super.getParameters().put(OAuthConstants.HAWK_TOKEN_KEY,
                                  theKey);
        super.getParameters().put(OAuthConstants.HAWK_TOKEN_ALGORITHM,
                                  algo.getOAuthName());
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/HawkAccessToken.java`
#### Snippet
```java

    public String getMacAlgorithm() {
        return super.getParameters().get(OAuthConstants.HAWK_TOKEN_ALGORITHM);
    }
    private static Client checkClient(Client c) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/HawkAccessToken.java`
#### Snippet
```java

    public String getMacId() {
        return super.getTokenKey();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/tokens/hawk/HawkAccessToken.java`
#### Snippet
```java

    public String getMacKey() {
        return super.getParameters().get(OAuthConstants.HAWK_TOKEN_KEY);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtProducer.java`
#### Snippet
```java
            return null;
        }
        return super.getInitializedSignatureProvider(c.getClientSecret());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtProducer.java`
#### Snippet
```java
        }
        if (theEncryptionProvider == null && c != null && c.getClientSecret() != null) {
            theEncryptionProvider = super.getInitializedEncryptionProvider(c.getClientSecret());
        }
        return theEncryptionProvider;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/JwtAccessTokenValidator.java`
#### Snippet
```java
        throws OAuthServiceException {
        try {
            JwtToken token = super.getJwtToken(authSchemeData);
            return convertClaimsToValidation(token.getClaims());
        } catch (Exception ex) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtConsumer.java`
#### Snippet
```java
        }
        if (theSignatureVerifier == null && c != null && c.getClientSecret() != null) {
            theSignatureVerifier = super.getInitializedSignatureVerifier(c.getClientSecret());
        }
        return theSignatureVerifier;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServerJoseJwtConsumer.java`
#### Snippet
```java
            return null;
        }
        return super.getInitializedDecryptionProvider(c.getClientSecret());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JCacheOAuthDataProvider.java`
#### Snippet
```java
            JoseJwtConsumer theConsumer = jwtTokenConsumer == null ? new JoseJwtConsumer() : jwtTokenConsumer;
            token = JwtTokenUtils.createAccessTokenFromJwt(theConsumer, jose, this,
                                                                 super.getJwtAccessTokenClaimMap());
            if (isExpired(token)) {
                jwtAccessTokenCache.remove(key);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JCacheOAuthDataProvider.java`
#### Snippet
```java
            JoseJwtConsumer theConsumer = jwtTokenConsumer == null ? new JoseJwtConsumer() : jwtTokenConsumer;
            ServerAccessToken token = JwtTokenUtils.createAccessTokenFromJwt(theConsumer, jose, this,
                                                                                   super.getJwtAccessTokenClaimMap());

            if (isExpired(token)) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setClientUri(String clientUri) {
        super.setProperty(CLIENT_URI, clientUri);
    }
    public String getClientUri() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setGrantTypes(List<String> grantTypes) {
        super.setProperty(GRANT_TYPES, grantTypes);
    }
    public List<String> getGrantTypes() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setContacts(List<String> contacts) {
        super.setProperty(CONTACTS, contacts);
    }
    public List<String> getContacts() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java

    public void setResourceUris(List<String> redirectUris) {
        super.setProperty(RESOURCE_URIS, redirectUris);
    }
    public List<String> getResourceUris() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setLogoUri(String logoUri) {
        super.setProperty(LOGO_URI, logoUri);
    }
    public String getLogoUri() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java

    public void setRedirectUris(List<String> redirectUris) {
        super.setProperty(REDIRECT_URIS, redirectUris);
    }
    public List<String> getRedirectUris() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setTosUri(String tosUri) {
        super.setProperty(TOS_URI, tosUri);
    }
    public String getTosUri() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setClientName(String clientName) {
        super.setProperty(CLIENT_NAME, clientName);
    }
    public String getClientName() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setPolicyUri(String policyUri) {
        super.setProperty(POLICY_URI, policyUri);
    }
    public String getPolicyUri() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setTokenEndpointAuthMethod(String method) {
        super.setProperty(TOKEN_ENDPOINT_AUTH_METHOD, method);
    }
    public String getTokenEndpointAuthMethod() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setScope(String scope) {
        super.setProperty(SCOPE, scope);
    }
    public String getScope() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java
    }
    public void setApplicationType(String applicationType) {
        super.setProperty(APPLICATION_TYPE, applicationType);
    }
    public String getApplicationType() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistration.java`
#### Snippet
```java

    public void setResponseTypes(List<String> responseTypes) {
        super.setProperty(RESPONSE_TYPES, responseTypes);
    }
    public List<String> getResponseTypes() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadata.java`
#### Snippet
```java

    protected URL getURLProperty(String name) {
        String s = super.getStringProperty(name);
        if (null != s) {
            try {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadata.java`
#### Snippet
```java

    protected void setURLProperty(String name, URL url) {
        super.setProperty(name, url != null ? url.toString() : null);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenValidatorService.java`
#### Snippet
```java
        String authSchemeData = params.getFirst(OAuthConstants.AUTHORIZATION_SCHEME_DATA);
        try {
            return super.getAccessTokenValidation(authScheme, authSchemeData, params);
        } catch (NotAuthorizedException ex) {
            // at this point it does not mean that RS failed to authenticate but that the basic
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setClientSecretExpiresAt(Long expiresAt) {
        super.setProperty(CLIENT_SECRET_EXPIRES_AT, expiresAt);
    }
    public Long getClientSecretExpiresAt() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setClientSecret(String clientSecret) {
        super.setProperty(CLIENT_SECRET, clientSecret);
    }
    public String getClientSecret() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java

    public void setClientId(String clientId) {
        super.setProperty(CLIENT_ID, clientId);
    }
    public String getClientId() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setGrantTypes(List<String> grantTypes) {
        super.setProperty(ClientRegistration.GRANT_TYPES, grantTypes);
    }
    public List<String> getGrantTypes() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setRegistrationAccessToken(String at) {
        super.setProperty(REG_ACCESS_TOKEN, at);
    }
    public String getRegistrationAccessToken() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setClientIdIssuedAt(Long issuedAt) {
        super.setProperty(CLIENT_ID_ISSUED_AT, issuedAt);
    }
    public Long getClientIdIssuedAt() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/ClientRegistrationResponse.java`
#### Snippet
```java
    }
    public void setRegistrationClientUri(String at) {
        super.setProperty(REG_CLIENT_URI, at);
    }
    public String getRegistrationClientUri() {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
            checkAudience(client, params);
        } catch (OAuthServiceException ex) {
            return super.createErrorResponseFromBean(ex.getError());
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
            }
            // Lets try the well-known grant handlers
            if (super.getDataProvider() instanceof AuthorizationCodeDataProvider) {
                AuthorizationCodeGrantHandler handler = new AuthorizationCodeGrantHandler();
                if (handler.getSupportedGrantTypes().contains(grantType)) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
                AuthorizationCodeGrantHandler handler = new AuthorizationCodeGrantHandler();
                if (handler.getSupportedGrantTypes().contains(grantType)) {
                    handler.setDataProvider(super.getDataProvider());
                    return handler;
                }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/MemoryIdentityCache.java`
#### Snippet
```java
        StringBuilder buffer = new StringBuilder(128);
        buffer.append(ManagementConstants.DEFAULT_DOMAIN_NAME).append(':');
        if (super.getBus() != null) {
            buffer.append(
                ManagementConstants.BUS_ID_PROP).append('=').append(super.getBus().getId()).append(',');
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/MemoryIdentityCache.java`
#### Snippet
```java
        if (super.getBus() != null) {
            buffer.append(
                ManagementConstants.BUS_ID_PROP).append('=').append(super.getBus().getId()).append(',');
        }
        buffer.append(ManagementConstants.TYPE_PROP).append('=').append("MemoryIdentityCache").append(',');
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
    private URL getDefaultConfigFileURL() {
        URL url = null;
        if (super.getBus() != null) {
            ResourceManager rm = super.getBus().getExtension(ResourceManager.class);
            url = rm.resolveResource("sts-ehcache.xml", URL.class);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
        URL url = null;
        if (super.getBus() != null) {
            ResourceManager rm = super.getBus().getExtension(ResourceManager.class);
            url = rm.resolveResource("sts-ehcache.xml", URL.class);
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
            cacheManager.close();

            if (super.getBus() != null) {
                super.getBus().getExtension(BusLifeCycleManager.class).unregisterLifeCycleListener(this);
            }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java

            if (super.getBus() != null) {
                super.getBus().getExtension(BusLifeCycleManager.class).unregisterLifeCycleListener(this);
            }
        }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
        StringBuilder buffer = new StringBuilder(128);
        buffer.append(ManagementConstants.DEFAULT_DOMAIN_NAME).append(':');
        if (super.getBus() != null) {
            buffer.append(
                ManagementConstants.BUS_ID_PROP).append('=').append(super.getBus().getId()).append(',');
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java
        if (super.getBus() != null) {
            buffer.append(
                ManagementConstants.BUS_ID_PROP).append('=').append(super.getBus().getId()).append(',');
        }
        buffer.append(ManagementConstants.TYPE_PROP).append('=').append("EHCacheIdentityCache").append(',');
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractImplicitGrantService.java`
#### Snippet
```java
        AccessTokenRegistration reg = new AccessTokenRegistration();
        reg.setClient(client);
        reg.setGrantType(super.getSupportedGrantType());
        reg.setResponseType(state.getResponseType());
        reg.setSubject(userSubject);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/DefaultSAMLRoleParser.java`
#### Snippet
```java
    ) {
        if (subject != null && useJaasSubject) {
            return super.parseRolesFromSubject(principal, subject);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/jwt/DefaultJWTRoleParser.java`
#### Snippet
```java
    ) {
        if (subject != null && useJaasSubject) {
            return super.parseRolesFromSubject(principal, subject);
        }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
        UserSubject subject, String sessionToken) {
        if (sessionAuthenticityTokenProvider != null) {
            return sessionAuthenticityTokenProvider.getSessionState(super.getMessageContext(),
                                                                     sessionToken,
                                                                     subject);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
        for (int i = 1; i < count; i++) {
            int c = read();
            cpoolIndex[i] = super.pos;
            // constant pool tag
            switch (c) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/UndertowWebSocketDestination.java`
#### Snippet
```java
    public void invokeInternal(ServletConfig config, ServletContext context, HttpServletRequest req,
                               HttpServletResponse resp) throws IOException {
        super.invoke(config, context, req, resp);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapMessage.java`
#### Snippet
```java

    public final void setVersion(SoapVersion v) {
        super.put(SoapVersion.class, v);
        this.version = v;
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketJettyDestination.java`
#### Snippet
```java
    public void invokeInternal(ServletConfig config, ServletContext context, HttpServletRequest req,
                               HttpServletResponse resp) throws IOException {
        super.invoke(config, context, req, resp);
    }
    
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketUndertowDestination.java`
#### Snippet
```java
    public void invokeInternal(ServletConfig config, ServletContext context, HttpServletRequest req,
                               HttpServletResponse resp) throws IOException {
        super.invoke(config, context, req, resp);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java

    public void setActivationNamespaces(Collection<String> ans) {
        super.setTransportIds(new ArrayList<>(ans));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/auth/saml/Saml2BearerAuthHandler.java`
#### Snippet
```java
        // The common SAML assertion validation:
        // signature, subject confirmation, etc
        super.validateToken(message, wrapper);

        // This is specific to OAuth2 path
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyInterceptorProviderRegistryImpl.java`
#### Snippet
```java
            }
            providers.add(provider);
            super.register(qn, providers);
        }
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/spring/PolicyEngineBeanDefinitionParser.java`
#### Snippet
```java
public class PolicyEngineBeanDefinitionParser extends AbstractBeanDefinitionParser {
    protected void doParse(Element element, ParserContext ctx, BeanDefinitionBuilder bean) {
        super.addBusWiringAttribute(bean, BusWiringType.CONSTRUCTOR);
        super.doParse(element, ctx, bean);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweHeaders.java`
#### Snippet
```java

    public KeyAlgorithm getKeyEncryptionAlgorithm() {
        String algo = super.getAlgorithm();
        return algo == null ? null : KeyAlgorithm.getAlgorithm(algo);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweHeaders.java`
#### Snippet
```java

    public void setKeyEncryptionAlgorithm(KeyAlgorithm algo) {
        super.setAlgorithm(algo.getJwaName());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/EcdhDirectKeyDecryptionAlgorithm.java`
#### Snippet
```java
    @Override
    public byte[] getDecryptedContentEncryptionKey(JweDecryptionInput jweDecryptionInput) {
        super.validateKeyEncryptionKey(jweDecryptionInput);

        return getDecryptedContentEncryptionKeyFromHeaders(jweDecryptionInput.getJweHeaders(), privateKey);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java

    public List<JsonWebKey> getKeys() {
        List<?> list = (List<?>)super.getProperty(KEYS_PROPERTY);
        if (list != null && !list.isEmpty()) {
            Object first = list.get(0);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
                return CastUtils.cast(list);
            }
            List<Map<String, Object>> listOfMaps = super.getListMapProperty(KEYS_PROPERTY);
            List<JsonWebKey> keys = new ArrayList<>(listOfMaps.size());
            for (Map<String, Object> map : listOfMaps) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
    }
    public final void setKeys(List<JsonWebKey> keys) {
        super.setProperty(KEYS_PROPERTY, keys);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/EcDsaJwsSignatureVerifier.java`
#### Snippet
```java
    @Override
    public boolean verify(JwsHeaders headers, String unsignedText, byte[] signature) {
        final String algoName = super.getAlgorithm().getJwaName();
        if (SIGNATURE_LENGTH_MAP.get(algoName) != signature.length) {
            LOG.warning("Algorithm " + algoName + " signature length is " + SIGNATURE_LENGTH_MAP.get(algoName)
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsHeaders.java`
#### Snippet
```java
    }
    public void setPayloadEncodingStatus(Boolean status) {
        super.setProperty(JoseConstants.JWS_HEADER_B64_STATUS_HEADER, status);
        if (!status) {
            List<String> critical = this.getCritical();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsHeaders.java`
#### Snippet
```java

    public final void setSignatureAlgorithm(SignatureAlgorithm algo) {
        super.setAlgorithm(algo.getJwaName());
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsHeaders.java`
#### Snippet
```java
    }
    public Boolean getPayloadEncodingStatus() {
        return super.getBooleanProperty(JoseConstants.JWS_HEADER_B64_STATUS_HEADER);
    }
    private static SignatureAlgorithm getSignatureAlgorithm(Properties sigProps) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsHeaders.java`
#### Snippet
```java

    public SignatureAlgorithm getSignatureAlgorithm() {
        String algo = super.getAlgorithm();
        return algo == null ? null : SignatureAlgorithm.getAlgorithm(algo);
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/EcDsaJwsSignatureProvider.java`
#### Snippet
```java
    protected JwsSignature doCreateJwsSignature(Signature s) {
        return new EcDsaPrivateKeyJwsSignature(s,
            EcDsaJwsSignatureVerifier.SIGNATURE_LENGTH_MAP.get(super.getAlgorithm().getJwaName()));
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJwtCompactConsumer.java`
#### Snippet
```java
    public JwtToken getJwtToken() {
        if (token == null) {
            JwsHeaders theHeaders = super.getJwsHeaders();
            JwtClaims theClaims = new JwtClaims(getReader().fromJson(getDecodedJwsPayload()));
            token = new JwtToken(theHeaders, theClaims);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtProducer.java`
#### Snippet
```java
                                JweEncryptionProvider theEncProvider,
                                JwsSignatureProvider theSigProvider) {
        super.checkProcessRequirements();
        String data;

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwt/JoseJwtConsumer.java`
#### Snippet
```java
                                   JweDecryptionProvider theDecryptor,
                                   JwsSignatureVerifier theSigVerifier) {
        super.checkProcessRequirements();

        JweHeaders jweHeaders = new JweHeaders();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseProducer.java`
#### Snippet
```java
    
    public String processData(String data) {
        super.checkProcessRequirements();
        
        JweEncryptionProvider theEncProvider = null;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseConsumer.java`
#### Snippet
```java
    
    public String getData(String data) {
        super.checkProcessRequirements();

        if (isJweRequired()) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingClientStopInterceptor.java`
#### Snippet
```java
        }

        super.stopTraceSpan(holder, responseCode);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingStartInterceptor.java`
#### Snippet
```java
        final Map<String, List<String>> headers = CastUtils.cast((Map<?, ?>)message.get(Message.PROTOCOL_HEADERS));

        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(headers,
            getUri(message), (String)message.get(Message.HTTP_REQUEST_METHOD));

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingClientStartInterceptor.java`
#### Snippet
```java
        
        final Exception ex = message.getContent(Exception.class);
        super.stopTraceSpan(holder, ex);
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingClientStartInterceptor.java`
#### Snippet
```java
    public void handleMessage(Message message) throws Fault {
        final Map<String, List<String>> headers = CastUtils.cast((Map<?, ?>)message.get(Message.PROTOCOL_HEADERS));
        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(headers,
            getUri(message), (String)message.get(Message.HTTP_REQUEST_METHOD));

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingStopInterceptor.java`
#### Snippet
```java
        }

        super.stopTraceSpan(requestHeaders, responseHeaders, responseCode, holder);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/jaxrs/OpenTracingProvider.java`
#### Snippet
```java
    public void filter(final ContainerRequestContext requestContext,
            final ContainerResponseContext responseContext) throws IOException {
        super.stopTraceSpan(requestContext.getHeaders(), responseContext.getHeaders(),
            responseContext.getStatus(), (TraceScopeHolder<TraceScope>)requestContext.getProperty(TRACE_SPAN));
    }
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/jaxrs/OpenTracingProvider.java`
#### Snippet
```java
    @Override
    public void filter(final ContainerRequestContext requestContext) throws IOException {
        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(requestContext.getHeaders(),
            requestContext.getUriInfo().getRequestUri(), requestContext.getMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/jaxrs/OpenTracingClientProvider.java`
#### Snippet
```java
    @Override
    public void filter(final ClientRequestContext requestContext) throws IOException {
        final TraceScopeHolder<TraceScope> holder = super.startTraceSpan(requestContext.getStringHeaders(),
            requestContext.getUri(), requestContext.getMethod());

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/jaxrs/OpenTracingClientProvider.java`
#### Snippet
```java
        final TraceScopeHolder<TraceScope> holder =
            (TraceScopeHolder<TraceScope>)requestContext.getProperty(TRACE_SPAN);
        super.stopTraceSpan(holder, responseContext.getStatus());
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactoryBean.java`
#### Snippet
```java
        setupFactory(factory, ep);

        final Map<String, Object> theProperties = super.getProperties();
        final boolean encodeClientParameters = PropertyUtils.isTrue(theProperties, "url.encode.client.parameters");
        if (encodeClientParameters) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
        List<ProviderInfo<? extends Object>> theProviders =
            prepareProviders(custom, busGlobal, providers, null);
        super.setCommonProviders(theProviders, RuntimeType.CLIENT);
        for (ProviderInfo<? extends Object> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(getBus(), provider.getProvider());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spring/JaxRsProxyClientConfiguration.java`
#### Snippet
```java
    @Bean
    protected Client jaxRsProxyClient() {
        return super.createClient();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spring/JaxRsWebClientConfiguration.java`
#### Snippet
```java
    @Bean
    protected Client jaxRsWebClient() {
        return super.createClient();
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/validation/JAXRSClientBeanValidationFeature.java`
#### Snippet
```java
            JAXRSClientBeanValidationOutInterceptor out = new JAXRSClientBeanValidationOutInterceptor();
            out.setWrapInProcessingException(wrapInProcessingException);
            super.addInterceptor(interceptorProvider, out);
        }
        public void setWrapInProcessingException(boolean wrapInProcessingException) {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        // Enable the operation reporting by default
        if (prop == null || PropertyUtils.isTrue(prop)) {
            UriBuilder absPathUri = super.getCurrentBuilder().clone();
            absPathUri.replaceQuery(null);
            setPlainOperationNameProperty(m, httpMethod + ":" + absPathUri.build().toString());
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                return r;
            }
            if (PropertyUtils.isTrue(super.getConfiguration().getResponseContext().get(BUFFER_PROXY_RESPONSE))) {
                r.bufferEntity();
            }
```

## RuleId[ruleID=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/interceptor/ClaimsAuthorizingInterceptor.java`
#### Snippet
```java
            List<Object> matchingClaimValues = matchingClaim.getValues();
            if (claimBean.isMatchAll()
                && !matchingClaimValues.containsAll(claimValues)) {
                return false;
            }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcInvoker.java`
#### Snippet
```java
                throw new OAuthServiceException("Invalid id token subject");
            }
            if (!newIdToken.getAudiences().containsAll(currentIdToken.getAudiences())) {
                throw new OAuthServiceException("Invalid id token audience(s)");
            }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        }

        return actualOrigins.containsAll(origins);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java

    public boolean containsAll(Collection<?> arg0) {
        return attachments.containsAll(arg0);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureValidator.java`
#### Snippet
```java
            success = verifier.verify(method, uri, SignatureHeaderUtils.mapHeaders(messageHeaders));

            if (!signature.getHeaders().containsAll(requiredHeaders)) {
                LOG.warning("Not all of the required headers are signed");
                throw new InvalidDataToVerifySignatureException();
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java
            if (localValues == null
                || localValues.size() != mapValues.size()
                || !localValues.containsAll(mapValues)) {
                return false;
            }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        }
        public boolean containsAll(Collection<?> c) {
            return orig.containsAll(c);
        }
        public Interceptor<? extends Message> get(int index) {
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
            // the current request scopes
            if (!partialMatchScopeValidation) {
                return registeredScopes.containsAll(requestScopes);
            }
            for (String requestScope : requestScopes) {
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/OAuthUtils.java`
#### Snippet
```java
        return StringUtils.isEmpty(providedAudiences)
               && StringUtils.isEmpty(allowedAudiences)
               || allowedAudiences.containsAll(providedAudiences);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/OAuthScopesFilter.java`
#### Snippet
```java
        }

        if (!requestScopes.containsAll(methodScopes)) {
            LOG.warning("Scopes do not match");
            throw ExceptionUtils.toForbiddenException(null, null);
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java

    protected void checkRequestedScopes(Client client, List<String> requestedScopes) {
        if (requiredScopes != null && !requestedScopes.containsAll(requiredScopes)) {
            throw new OAuthServiceException("Required scopes are missing");
        }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
        } else {
            theNewScopes = convertScopeToPermissions(client, restrictedScopes);
            if (!oldRefreshToken.getScopes().containsAll(theNewScopes)) {
                throw new OAuthServiceException("Invalid scopes");
            }
```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
            alreadyAuthorizedPerms = preAuthorizedToken.getScopes();
            preAuthorizationComplete =
                OAuthUtils.convertPermissionsToScopeList(alreadyAuthorizedPerms).containsAll(requestedScope);
        }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/RedirectionBasedGrantService.java`
#### Snippet
```java
        return scopesRequiringNoConsent != null
               && requestedScope != null
               && scopesRequiringNoConsent.containsAll(requestedScope);
    }

```

### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            beanParamValues.putAll(getValuesFromBeanParam(params[p.getIndex()], PathParam.class));
        });
        if (!beanParamValues.isEmpty() && !methodVars.containsAll(beanParamValues.keySet())) {
            List<String> classVars = ori.getClassResourceInfo().getURITemplate().getVariables();
            classVars.forEach(classVar -> {
```

## RuleId[ruleID=InfiniteLoopStatement]
### InfiniteLoopStatement
`while` statement cannot complete without throwing an exception
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java
                        boolean found = false;
                        try {
                            while (true) {
                                socket.receive(p);
                                dataReceived(message, IoBuffer.wrap(bytes, 0, p.getLength()), false, true);
```

## RuleId[ruleID=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/Utils.java`
#### Snippet
```java
        if (System.getSecurityManager() != null) {
            return AccessController.doPrivileged((PrivilegedAction<ExecutorService>) () -> {
                return ForkJoinPool.commonPool();
            });
        } else {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
            c = null;
            LOG.log(Level.FINEST, ex, () -> {
                return "Caught ClassNotFoundException - expected if using MP Rest Client 1.0 APIs"; });
        }
        ASYNC_II_FACTORY_CLASS = c;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/RestClientBean.java`
#### Snippet
```java
        
        ConfigFacade.getOptionalValue(REST_TRUST_STORE_FORMAT, clientInterface, String.class).ifPresent(
            trustStoreLoc -> {
                initTrustStore(trustStoreLoc, builder);
            }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/RestClientBean.java`
#### Snippet
```java

        ConfigFacade.getOptionalValue(REST_KEY_STORE_FORMAT, clientInterface, String.class).ifPresent(
            keyStoreLoc -> {
                initKeyStore(keyStoreLoc, builder);
            }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
                                   .orElse(factoryCls.getDeclaredConstructor().newInstance());
                ProviderInfo<ClientHeadersFactory> pi = clientHeaderFactories.computeIfAbsent(factoryCls, k -> {
                    return new ProviderInfo<ClientHeadersFactory>(factory, m.getExchange().getBus(), true);
                });
                InjectionUtils.injectContexts(factory, pi, m);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
        Map<String, Annotation> annos = new HashMap<>();
        if (annotations != null) {
            annotations.forEach(annotation -> {
                annos.put(annotation.annotationType().getName(), annotation);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiCustomizer.java`
#### Snippet
```java
            Map<String, ClassResourceInfo> operations = new HashMap<>();
            Map<Pair<String, String>, OperationResourceInfo> methods = new HashMap<>();
            cris.forEach(cri -> {
                cri.getMethodDispatcher().getOperationResourceInfos().forEach(ori -> {
                    String normalizedPath = getNormalizedPath(
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
        }
        return AccessController.doPrivileged((PrivilegedAction<Boolean>) () -> {
            return file.exists();
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/SseBroadcasterImpl.java`
#### Snippet
```java
    public void close() {
        if (closed.compareAndSet(false, true)) {
            subscribers.forEach(subscriber -> {
                subscriber.close();
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
                final ClassLoader loader = AccessController.doPrivileged((PrivilegedAction<ClassLoader>) 
                    () -> {
                        return type.getClassLoader();
                    });
                if (loader != null && objectFactoryOrIndexAvailable(type)) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        try {
            return AccessController.doPrivileged((PrivilegedExceptionAction<JAXBContext>)() -> {
                return JAXBContext.newInstance(classes, cProperties);
            });
        } catch (PrivilegedActionException e) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtTokenSecurityContext.java`
#### Snippet
```java
                        .noneMatch(o -> !(o instanceof String))) {
                roles = new HashSet<>();
                ((List)roleClaimValue).stream().forEach(val -> {
                    roles.add(new SimpleGroup(val.toString()));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
            throws OAuthServiceException {
        return executeInTransaction(em -> {
            return getCodeGrants(c, subject, em);
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
    public ServerAuthorizationCodeGrant removeCodeGrant(final String code) throws OAuthServiceException {
        return executeInTransaction(em -> {
            return removeCodeGrant(code, em);
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
    protected RefreshToken getRefreshToken(final String refreshTokenKey) {
        return executeInTransaction(em -> {
            return em.find(RefreshToken.class, refreshTokenKey);
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
    public List<ServerAccessToken> getAccessTokens(final Client c, final UserSubject sub) {
        return executeInTransaction(em -> {
            return CastUtils.cast(getTokensQuery(c, sub, em).getResultList());
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
    public Client doGetClient(final String clientId) throws OAuthServiceException {
        return execute(em -> {
            return em.find(Client.class, clientId);
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
    public List<Client> getClients(final UserSubject resourceOwner) {
        return executeInTransaction(em -> {
            return getClientsQuery(resourceOwner, em).getResultList();
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/JPAOAuthDataProvider.java`
#### Snippet
```java
    public List<RefreshToken> getRefreshTokens(final Client c, final UserSubject sub) {
        return executeInTransaction(em ->  {
            return getRefreshTokensQuery(c, sub, em).getResultList();
        });
    }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/MaskSensitiveHelper.java`
#### Snippet
```java
            final Set<String> sensitiveHeaderNames) {
        sensitiveHeaderNames.stream()
                .forEach(h -> {
                    headerMap.computeIfPresent(h, (key, value) -> MASKED_HEADER_VALUE);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/JAXRSClientFactoryBean.java`
#### Snippet
```java
    protected void applyFeatures(AbstractClient client) {
        if (getFeatures() != null) {
            getFeatures().forEach(feature -> {
                feature.initialize(client.getConfiguration(), getBus());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
                exchange.put(MessageObserver.class, message -> {
                    if (!message.getExchange().containsKey(Executor.class.getName() + ".USING_SPECIFIED")) {
                        executor.execute(() -> {
                            observer.onMessage(message);
                        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            Object pValue = pValues[0];
            MultivaluedMap<String, Object> values = InjectionUtils.extractValuesFromBean(pValue, "");
            values.forEach((key, value) -> {
                value.forEach(v -> {
                    convertMatrixOrQueryToBuilder(ub, key, v, pt, anns);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            MultivaluedMap<String, Object> values = InjectionUtils.extractValuesFromBean(pValue, "");
            values.forEach((key, value) -> {
                value.forEach(v -> {
                    convertMatrixOrQueryToBuilder(ub, key, v, pt, anns);
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java

        List<Parameter> fm = getParameters(map, ParameterType.FORM);
        fm.forEach(p -> {
            addFormValue(form, p.getName(), params[p.getIndex()], getParamAnnotations(m, p));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        beanParams.stream().
                map(p -> getValuesFromBeanParam(params[p.getIndex()], FormParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        addFormValue(form, key, value.getValue(), value.getAnns());
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                map(p -> getValuesFromBeanParam(params[p.getIndex()], FormParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        addFormValue(form, key, value.getValue(), value.getAnns());
                    });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        cs.stream().
                filter(p -> params[p.getIndex()] != null).
                forEachOrdered(p -> {
                    headers.add(HttpHeaders.COOKIE,
                            p.getName() + '='
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        beanParams.stream().
                map(p -> getValuesFromBeanParam(params[p.getIndex()], CookieParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        if (value != null) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        List<Parameter> paramsList = getParameters(map, ParameterType.PATH);
        Map<String, BeanPair> beanParamValues = new HashMap<>(beanParams.size());
        beanParams.forEach(p -> {
            beanParamValues.putAll(getValuesFromBeanParam(params[p.getIndex()], PathParam.class));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            if (p.getName().isEmpty()) {
                MultivaluedMap<String, Object> values = InjectionUtils.extractValuesFromBean(params[p.getIndex()], "");
                methodVars.forEach(var -> {
                    list.addAll(values.get(var));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        hs.stream().
                filter(p -> params[p.getIndex()] != null).
                forEachOrdered(p -> {
                    headers.add(p.getName(), convertParamValue(params[p.getIndex()], getParamAnnotations(m, p)));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        beanParams.stream().
                map(p -> getValuesFromBeanParam(params[p.getIndex()], HeaderParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        if (value != null) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        mx.stream().
                filter(p -> params[p.getIndex()] != null).
                forEachOrdered(p -> {
                    addMatrixQueryParamsToBuilder(ub, p.getName(), ParameterType.MATRIX,
                            getParamAnnotations(m, p), params[p.getIndex()]);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        beanParams.stream().
                map(p -> getValuesFromBeanParam(params[p.getIndex()], MatrixParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        if (value != null) {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        qs.stream().
                filter(p -> params[p.getIndex()] != null).
                forEachOrdered(p -> {
                    addMatrixQueryParamsToBuilder(ub, p.getName(), ParameterType.QUERY,
                            getParamAnnotations(m, p), params[p.getIndex()]);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        beanParams.stream().
                map(p -> getValuesFromBeanParam(params[p.getIndex()], QueryParam.class)).
                forEachOrdered(values -> {
                    values.forEach((key, value) -> {
                        if (value != null) {
```

## RuleId[ruleID=UseOfPropertiesAsHashtable]
### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            Boolean fallback = value;
            if (value == null && properties != null) {
                fallback = PropertyUtils.isTrue(properties.get(PRETTY_PRINT_PROPERTY));
            }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/idp/OidcConfigurationService.java`
#### Snippet
```java
        if (sigProps != null && sigProps.containsKey(JoseConstants.RSSEC_SIGNATURE_ALGORITHM)) {
            cfg.put("id_token_signing_alg_values_supported",
                    Collections.singletonList(sigProps.get(JoseConstants.RSSEC_SIGNATURE_ALGORITHM)));
        }
        
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
        PrivateKeyPasswordProvider cb = null;
        if (props.containsKey(HTTPSignatureConstants.RSSEC_KEY_PSWD_PROVIDER)) {
            cb = (PrivateKeyPasswordProvider)props.get(HTTPSignatureConstants.RSSEC_KEY_PSWD_PROVIDER);
        } else if (m != null) {
            cb = (PrivateKeyPasswordProvider)m.getContextualProperty(HTTPSignatureConstants.RSSEC_KEY_PSWD_PROVIDER);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
        KeyStore keyStore = null;
        if (props.containsKey(HTTPSignatureConstants.RSSEC_KEY_STORE)) {
            keyStore = (KeyStore)props.get(HTTPSignatureConstants.RSSEC_KEY_STORE);
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
            }
            if (m != null) {
                Object keyStoreProp = m.getExchange().get(props.get(HTTPSignatureConstants.RSSEC_KEY_STORE_FILE));
                if (keyStoreProp != null && !(keyStoreProp instanceof KeyStore)) {
                    throw new SignatureException("Unexpected key store class: " + keyStoreProp.getClass().getName());
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/utils/KeyManagementUtils.java`
#### Snippet
```java
            keyStore = loadKeyStore(props, bus);
            if (m != null) {
                m.getExchange().put((String)props.get(HTTPSignatureConstants.RSSEC_KEY_STORE_FILE), keyStore);
            }
        }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.putAll()` on properties object
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
                }
            }
            System.getProperties().putAll(origProps);
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiSupport.java`
#### Snippet
```java
        Boolean theSupportSwaggerUI = isSupportSwaggerUi();
        if (theSupportSwaggerUI == null && props != null && props.containsKey(SUPPORT_UI_PROPERTY)) {
            theSupportSwaggerUI = PropertyUtils.isTrue(props.get(SUPPORT_UI_PROPERTY));
        }
        if (theSupportSwaggerUI == null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
            while (keys.hasMoreElements()) {
                String theJNDIName = (String)keys.nextElement();
                String value = (String)ejbServants.get(theJNDIName);
                EJBServantConfig config = new EJBServantConfig(theJNDIName, value);
                EJBEndpoint ejbEndpoint = new EJBEndpoint(config);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
                JoseUtils.checkBooleanProperty(headers, props, m, JoseConstants.RSSEC_ENCRYPTION_INCLUDE_KEY_ID);

            if (JoseConstants.HEADER_JSON_WEB_KEY.equals(props.get(JoseConstants.RSSEC_KEY_STORE_TYPE))) {
                JsonWebKey jwk = JwkUtils.loadJsonWebKey(m, props, KeyOperation.ENCRYPT);
                if (jwk != null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/JweUtils.java`
#### Snippet
```java
            }
        } else {
            if (JoseConstants.HEADER_JSON_WEB_KEY.equals(props.get(JoseConstants.RSSEC_KEY_STORE_TYPE))) {
                JsonWebKey jwk = JwkUtils.loadJsonWebKey(m, props, KeyOperation.DECRYPT);
                if (jwk == null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            JoseUtils.checkBooleanProperty(headers, props, m, JoseConstants.RSSEC_SIGNATURE_INCLUDE_KEY_ID);

        if (JoseConstants.HEADER_JSON_WEB_KEY.equals(props.get(JoseConstants.RSSEC_KEY_STORE_TYPE))) {
            JsonWebKey jwk = JwkUtils.loadJsonWebKey(m, props, KeyOperation.SIGN);
            if (jwk != null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        }

        if (JoseConstants.HEADER_JSON_WEB_KEY.equals(props.get(JoseConstants.RSSEC_KEY_STORE_TYPE))) {
            JsonWebKey jwk = JwkUtils.loadJsonWebKey(m, props, KeyOperation.VERIFY, inHeaderKid);
            if (jwk != null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
        }
        X509Certificate[] certs = null;
        if (PropertyUtils.isTrue(props.get(JoseConstants.RSSEC_SIGNATURE_INCLUDE_CERT))) {
            certs = KeyManagementUtils.loadX509CertificateOrChain(m, props);
        }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JwkUtils.java`
#### Snippet
```java
    }
    public static JsonWebKeys loadJwkSet(Message m, Properties props, PrivateKeyPasswordProvider cb) {
        String key = (String)props.get(JoseConstants.RSSEC_KEY_STORE_FILE);
        JsonWebKeys jwkSet = null;
        if (key != null && m != null) {
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/JoseUtils.java`
#### Snippet
```java
        }
        if (props.containsKey(propertyName)) {
            return PropertyUtils.isTrue(props.get(propertyName));
        }
        return MessageUtils.getContextualBoolean(m, propertyName, false);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        KeyStore keyStore = null;
        if (props.containsKey(JoseConstants.RSSEC_KEY_STORE)) {
            keyStore = (KeyStore)props.get(JoseConstants.RSSEC_KEY_STORE);
        }

```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
            }
            if (m != null) {
                Object keyStoreProp = m.getExchange().get(props.get(JoseConstants.RSSEC_KEY_STORE_FILE));
                if (keyStoreProp != null && !(keyStoreProp instanceof KeyStore)) {
                    throw new JwkException("Unexpected key store class: " + keyStoreProp.getClass().getName());
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
            keyStore = loadKeyStore(props, bus);
            if (m != null) {
                m.getExchange().put((String)props.get(JoseConstants.RSSEC_KEY_STORE_FILE), keyStore);
            }
        }
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
            if (propName != null) {
                if (props.containsKey(propName)) {
                    cb = (PrivateKeyPasswordProvider)props.get(propName);
                } else if (m != null) {
                    cb = (PrivateKeyPasswordProvider)m.getContextualProperty(propName);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.get()` on properties object
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
        if (cb == null) {
            if (props.containsKey(JoseConstants.RSSEC_KEY_PSWD_PROVIDER)) {
                cb = (PrivateKeyPasswordProvider)props.get(JoseConstants.RSSEC_KEY_PSWD_PROVIDER);
            } else if (m != null) {
                cb = (PrivateKeyPasswordProvider)m.getContextualProperty(JoseConstants.RSSEC_KEY_PSWD_PROVIDER);
```

### UseOfPropertiesAsHashtable
Call to `Hashtable.putAll()` on properties object
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlFeature.java`
#### Snippet
```java
    private Cache<Key, Entry> createCache(final Map<String, Object> properties) {
        final Properties props = new Properties();
        props.putAll(properties);

        final String prefix = this.getClass().getName() + ".";
```

## RuleId[ruleID=EmptyMethod]
### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/BusFactory.java`
#### Snippet
```java
    }

    protected void initializeBus(Bus bus) {
    }

```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
    }

    protected void loadAdditionalFeatures() {

    }
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/bus/managers/ConduitInitiatorManagerImpl.java`
#### Snippet
```java

    @PreDestroy
    public void shutdown() {
        // nothing to do
    }
```

### EmptyMethod
Method only calls its super
in `core/src/main/java/org/apache/cxf/interceptor/AbstractAttributedInterceptorProvider.java`
#### Snippet
```java

    @Override
    public int hashCode() {     //NOPMD
        return super.hashCode();
    }
```

### EmptyMethod
Empty method overrides empty method
in `core/src/main/java/org/apache/cxf/interceptor/AttachmentInInterceptor.java`
#### Snippet
```java

    @Override
    public void handleFault(Message messageParam) {
    }

```

### EmptyMethod
The method and all its derivables are empty
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
    }

    protected void onWrite() throws IOException {

    }
```

### EmptyMethod
All implementations of this method are empty
in `core/src/main/java/org/apache/cxf/io/CachedOutputStreamCallback.java`
#### Snippet
```java

    void onClose(CachedOutputStream os);
    void onFlush(CachedOutputStream os);

}
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
     * output stream ... etc.)
     */
    protected void doFlush() throws IOException {

    }
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
     * Perform any actions required after stream closure (close the other related stream etc.)
     */
    protected void postClose() throws IOException {

    }
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
    }

    protected void onWrite() throws IOException {

    }
```

### EmptyMethod
All implementations of this method are empty
in `core/src/main/java/org/apache/cxf/service/ServiceBuilder.java`
#### Snippet
```java
    File getOutputFile();

    void validate();
}

```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void begin(OperationInfo op) {
    }
    public void begin(UnwrappedOperationInfo op) {
    }
    public void begin(MessageInfo msg) {
```

### EmptyMethod
The method and all its derivables are empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void end(ServiceInfo service) {
    }
    public void end(InterfaceInfo intf) {
    }
    public void end(OperationInfo op) {
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void end(OperationInfo op) {
    }
    public void end(UnwrappedOperationInfo op) {
    }
    public void end(MessageInfo msg) {
```

### EmptyMethod
The method and all its derivables are empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void end(UnwrappedOperationInfo op) {
    }
    public void end(MessageInfo msg) {
    }
    public void end(MessagePartInfo part) {
```

### EmptyMethod
The method and all its derivables are empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void end(MessageInfo msg) {
    }
    public void end(MessagePartInfo part) {
    }
    public void end(FaultInfo fault) {
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void begin(EndpointInfo endpointInfo) {
    }
    public void end(EndpointInfo endpointInfo) {
    }
    public void begin(BindingInfo bindingInfo) {
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void end(EndpointInfo endpointInfo) {
    }
    public void begin(BindingInfo bindingInfo) {
    }
    public void end(BindingInfo bindingInfo) {
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/service/ServiceModelVisitor.java`
#### Snippet
```java
    public void begin(BindingInfo bindingInfo) {
    }
    public void end(BindingInfo bindingInfo) {
    }
}
```

### EmptyMethod
The method is empty
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
        }

        private void setupGZip() {

        }
```

### EmptyMethod
All implementations of this method are empty
in `integration/jca/src/main/java/org/apache/cxf/connector/Connection.java`
#### Snippet
```java
     * @throws ResourceException if an error occurs during close.
     */
    void close() throws ResourceException;

}
```

### EmptyMethod
All implementations of this method are empty
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
        throws ResourceException;

    public abstract void close() throws ResourceAdapterInternalException;

    protected abstract void validateReference(AbstractManagedConnectionImpl conn, Subject subject)
```

### EmptyMethod
Method only calls its super
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/AssociatedManagedConnectionFactoryImpl.java`
#### Snippet
```java
    //Explicit override these two methods,
    //otherwise when deploy rar to weblogic9.1, it would complaint about this.
    public int hashCode() {
        return super.hashCode();
    }
```

### EmptyMethod
The method is empty
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBMessageListenerImpl.java`
#### Snippet
```java
    //---------------- EJB Methods

    public void ejbCreate() {
    }

```

### EmptyMethod
Empty method overrides empty method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
         * output stream ... etc.)
         */
        public void doFlush() throws IOException {
            // do nothing here
        }
```

### EmptyMethod
Empty method overrides empty method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
         * output stream ... etc.)
         */
        public void doFlush() throws IOException {

            // do nothing here
```

### EmptyMethod
Empty method overrides empty method
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
         * Perform any actions required on stream closure (handle response etc.)
         */
        public void doClose() throws IOException {
        }

```

### EmptyMethod
The method is empty
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/OrbConfig.java`
#### Snippet
```java
    }

    public void addPOAPolicies(ORB orb,
                               String poaName,
                               POA parentPOA,
```

### EmptyMethod
The method is empty
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AbstractAegisIoImpl.java`
#### Snippet
```java

    /** {@inheritDoc}*/
    public void setSchema(Schema s) {
    }

```

### EmptyMethod
Empty method overrides empty method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutExceptionMapperInterceptor.java`
#### Snippet
```java
    }
    @Override
    public void handleFault(Message message) {
        //complete
    }
```

### EmptyMethod
Empty method overrides empty method
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
    }

    public void handleFault(Message message) {
        // complete
    }
```

### EmptyMethod
Method only calls its super
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        }

        public int hashCode() {
            return super.hashCode();
        }
```

### EmptyMethod
The method is empty
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
    }

    protected void configureSingleton(Object instance) {

    }
```

### EmptyMethod
The method is empty
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringConfigurationFactory.java`
#### Snippet
```java
    }

    protected void finalizeFactorySetup(JAXRSServerFactoryBean factory) {
        // complete
    }
```

### EmptyMethod
The method is empty
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        }

        public void info(SAXParseException arg0) {
            // ignore
        }
```

### EmptyMethod
The method is empty
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        }

        public void warning(SAXParseException arg0) {
            // ignore
        }
```

### EmptyMethod
Empty method overrides empty method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void begin(FaultInfo fault) {
    }

```

### EmptyMethod
Empty method overrides empty method
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

    @Override
    public void end(FaultInfo fault) {
    }

```

### EmptyMethod
The method is empty
in `rt/management/src/main/java/org/apache/cxf/management/jmx/InstrumentationManagerImpl.java`
#### Snippet
```java

    @Deprecated
    public void register() {
    }

```

### EmptyMethod
The method is empty
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
        }

        protected void handleAsyncFault(Message message) {
        }
    }
```

### EmptyMethod
The method is empty
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiCustomizer.java`
#### Snippet
```java
     * @see io.swagger.v3.oas.models.parameters.QueryParameter
     */
    protected void addParameters(final List<Parameter> parameters) {
        // does nothing by default
    }
```

### EmptyMethod
The method is empty
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJweDecryptingFilter.java`
#### Snippet
```java
    }

    protected void validateHeaders(JweHeaders headers) {
        // complete
    }
```

### EmptyMethod
The method is empty
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJweJsonDecryptingFilter.java`
#### Snippet
```java
    }

    protected void validateHeaders(JweHeaders headers) {
        // complete
    }
```

### EmptyMethod
The method is empty
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/DynamicRegistrationService.java`
#### Snippet
```java
    }

    protected void validateRequestUri(String uri, String appType, List<String> grantTypes) {
        // Web Clients using the OAuth Implicit Grant Type MUST only register URLs using the https scheme
        // as redirect_uris; they MUST NOT use localhost as the hostname. Native Clients MUST only register
```

### EmptyMethod
The method is empty
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClaimsValidator.java`
#### Snippet
```java
    }

    private void validateSelfIssuedProvider(JwtClaims claims, String clientId, boolean validateClaimsAlways) {
    }

```

### EmptyMethod
Method only calls its super
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java

    @PreDestroy
    public void close() {
        super.close();
    }
```

### EmptyMethod
The method is empty
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/AbstractXACMLAuthorizingInterceptor.java`
#### Snippet
```java
     * Handle any Obligations returned by the PDP
     */
    protected void handleObligations(
        RequestType request,
        Principal principal,
```

### EmptyMethod
The method is empty
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
    }

    public void createPortExtensors(EndpointInfo ei, Service service) {
        // TODO
    }
```

### EmptyMethod
The method is empty
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/AbstractSpnegoAuthSupplier.java`
#### Snippet
```java

    // Allow subclasses to decorate the Subject if required.
    protected void decorateSubject(Subject subject) {

    }
```

### EmptyMethod
The method is empty
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java

    //provide a hook to customize the builder
    protected void adaptClientBuilder(HttpAsyncClientBuilder httpAsyncClientBuilder) {
    }

```

### EmptyMethod
The method is empty
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/SharedOutputBuffer.java`
#### Snippet
```java
    }

    public void flush() throws IOException {
    }

```

### EmptyMethod
The method is empty
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java

    //provide a hook to customize the builder
    protected void adaptClientBuilder(HttpAsyncClientBuilder httpAsyncClientBuilder) {
    }

```

### EmptyMethod
The method is empty
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/SharedOutputBuffer.java`
#### Snippet
```java
    }

    public void flush() throws IOException {
    }

```

### EmptyMethod
The method is empty
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
                    checkKeyStore();
                }
                public void checkKeyStore() {
                    //we'll handle this later
                }
```

### EmptyMethod
The method is empty
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    }

    public void initComplete() {
        // do nothing here
    }
```

### EmptyMethod
The method is empty
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
    }

    public void preShutdown() {
        // do nothing here
        // just let server registry to call the server stop first
```

### EmptyMethod
The method is empty
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java


    public void initComplete() {
        // do nothing here
    }
```

### EmptyMethod
The method is empty
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
    }

    public void preShutdown() {
        // do nothing here
        // just let server registry to call the server stop first
```

### EmptyMethod
The method is empty
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java
     */
    @Deprecated
    public void setTransactionManager(Object transactionManager) {
    }

```

### EmptyMethod
The method is empty
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java

    @Deprecated
    public void setReconnectOnException(boolean reconnectOnException) {
        // Ignore. We always reconnect on exceptions
    }
```

### EmptyMethod
Method only calls its super
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java

    @Override
    public void shutdown() {
        super.shutdown();
    }
```

### EmptyMethod
The method is empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMInInterceptor.java`
#### Snippet
```java
    }

    void processDeliveryAssurance(RMProperties rmps) {

    }
```

### EmptyMethod
The method is empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    }

    public void stopServer(Server server) {
    }

```

### EmptyMethod
The method is empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    }

    public void clientDestroyed(Client client) {
    }

```

### EmptyMethod
All implementations of this method are empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RedeliveryQueue.java`
#### Snippet
```java
     * Initiate resends.
     */
    void start();

    /**
```

### EmptyMethod
The method is empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java
    //

    protected void beginTransaction() {
    }

```

### EmptyMethod
The method is empty
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
    }

    void stop() {

    }
```

### EmptyMethod
The method is empty
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/STSTokenOutInterceptor.java`
#### Snippet
```java

    // An extension point to allow custom processing of the token
    protected void processToken(Message message, SecurityToken tok) {

    }
```

### EmptyMethod
The method is empty
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JInterceptor.java`
#### Snippet
```java
    }

    public void postHandleMessage(SoapMessage message) throws Fault {
    }
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
```

### EmptyMethod
The method is empty
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
    }

    public void postHandleMessage(SoapMessage message) throws Fault {
    }
    public Collection<PhaseInterceptor<? extends Message>> getAdditionalInterceptors() {
```

### EmptyMethod
The method is empty
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DelegatingCallbackHandler.java`
#### Snippet
```java
    }

    protected void handleCallback(Callback c) throws IOException {
        // complete
    }
```

### EmptyMethod
The method is empty
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractPullPoint.java`
#### Snippet
```java
    }

    public void create(CreatePullPoint createPullPointRequest) throws UnableToCreatePullPointFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterImplBase.java`
#### Snippet
```java
    }

    public void pingMe() throws PingMeFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractTestServerBase.java`
#### Snippet
```java
    }

    public void setUp() throws Exception {
        // emtpy
    }
```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/header_test/TestHeaderImpl.java`
#### Snippet
```java
     * @param requestType
     */
    public void testHeader4(
        String requestType) {

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world/GreeterImpl.java`
#### Snippet
```java

    /** {@inheritDoc}*/
    public void pingMe() throws PingMeFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_doc_lit/MultiTransportGreeter.java`
#### Snippet
```java
    }

    public void greetMeOneWay(String requestType) {
        //System.out.println("*********  greetMeOneWay: " + requestType);
    }
```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
    }

    public void testDocLitFault(String faultType)  throws BadRecordLitFault, NoSuchCodeLitFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java

    @WebMethod(operationName = "PutLastTradedPrice")
    public void putLastTradedPriceAsync(
        @WebParam(name = "body", partName = "body")
        String body) {
```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java

    @WebMethod(operationName = "PutLastTradedPrice")
    public void putLastTradedPrice(
        @WebParam(name = "body", partName = "body")
        String body) {
```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
    }

    public void testDocLitFault(String faultType)  throws BadRecordLitFault, NoSuchCodeLitFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    }

    public void testDocLitFault(String faultType)  throws BadRecordLitFault, NoSuchCodeLitFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    }

    public void testDocLitFault(String faultType)  throws BadRecordLitFault, NoSuchCodeLitFault {
    }

```

### EmptyMethod
The method is empty
in `testutils/src/main/java/org/apache/hello_world_xml_http/mixed/GreeterImpl.java`
#### Snippet
```java
    }

    public void greetMeOneWay(String me) {
    }

```

## RuleId[ruleID=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ISTORE = 0;
    public int AALOAD = 0;
    public int ARRAYLENGTH = 0;
    public int IRETURN = 0;
    public int NEW = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int FCONST_0;
    public int DCONST_0;
    public int IF_ICMPLT = 0;
    public java.lang.Integer INTEGER;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int RETURN = 0;
    public int F_APPEND = 0;
    public int F_SAME = 0;
    public int F_SAME1 = 0;
    public int INVOKESPECIAL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int GETFIELD = 0;
    public int GETSTATIC = 0;
    public int ASTORE = 0;
    public int PUTFIELD = 0;
    public int PUTSTATIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ANEWARRAY = 0;
    public int DUP = 0;
    public int ATHROW = 0;
    public int INVOKEVIRTUAL = 0;
    public int GOTO = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int INVOKEINTERFACE = 0;
    public int GETFIELD = 0;
    public int GETSTATIC = 0;
    public int ASTORE = 0;
    public int PUTFIELD = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ATHROW = 0;
    public int INVOKEVIRTUAL = 0;
    public int GOTO = 0;
    public int POP = 0;
    public int ACONST_NULL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int DUP = 0;
    public int ATHROW = 0;
    public int INVOKEVIRTUAL = 0;
    public int GOTO = 0;
    public int POP = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_ABSTRACT = 0;
    public int ACC_INTERFACE = 0;
    public int ACC_SYNTHETIC = 0;
    public int ILOAD = 0;
    public int ISTORE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int NEW = 0;
    public int ANEWARRAY = 0;
    public int DUP = 0;
    public int ATHROW = 0;
    public int INVOKEVIRTUAL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ARRAYLENGTH = 0;
    public int IRETURN = 0;
    public int NEW = 0;
    public int ANEWARRAY = 0;
    public int DUP = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int F_SAME1 = 0;
    public int INVOKESPECIAL = 0;
    public int ACC_PUBLIC = 0;
    public int ACC_FINAL = 0;
    public int ACC_SUPER = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ALOAD = 0;
    public int IFNULL = 0;
    public int CHECKCAST = 0;
    public int INVOKEINTERFACE = 0;
    public int GETFIELD = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int V1_5 = 0;
    public int V1_6 = 0;
    public int V1_7 = 0;
    public int ACC_ABSTRACT = 0;
    public int ACC_INTERFACE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    //so we don't want them "final" or the compiler will optimize them out
    //to just "0" which we really don't want
    public int ARETURN = 0;
    public int ALOAD = 0;
    public int IFNULL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int AALOAD = 0;
    public int ARRAYLENGTH = 0;
    public int IRETURN = 0;
    public int NEW = 0;
    public int ANEWARRAY = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_FINAL = 0;
    public int ACC_SUPER = 0;
    public int ACC_PRIVATE = 0;
    public int ACC_STATIC = 0;
    public int V1_5 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int F_SAME = 0;
    public int F_SAME1 = 0;
    public int INVOKESPECIAL = 0;
    public int ACC_PUBLIC = 0;
    public int ACC_FINAL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_PUBLIC = 0;
    public int ACC_FINAL = 0;
    public int ACC_SUPER = 0;
    public int ACC_PRIVATE = 0;
    public int ACC_STATIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ASTORE = 0;
    public int PUTFIELD = 0;
    public int PUTSTATIC = 0;
    public int RETURN = 0;
    public int F_APPEND = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_SUPER = 0;
    public int ACC_PRIVATE = 0;
    public int ACC_STATIC = 0;
    public int V1_5 = 0;
    public int V1_6 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACONST_NULL = 0;
    public int IFNONNULL = 0;
    public int SIPUSH = 0;
    public int INVOKESTATIC = 0;
    public int ICONST_0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int IFNULL = 0;
    public int CHECKCAST = 0;
    public int INVOKEINTERFACE = 0;
    public int GETFIELD = 0;
    public int GETSTATIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int V1_6 = 0;
    public int V1_7 = 0;
    public int ACC_ABSTRACT = 0;
    public int ACC_INTERFACE = 0;
    public int ACC_SYNTHETIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ILOAD = 0;
    public int ISTORE = 0;
    public int AALOAD = 0;
    public int ARRAYLENGTH = 0;
    public int IRETURN = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_SYNTHETIC = 0;
    public int ILOAD = 0;
    public int ISTORE = 0;
    public int AALOAD = 0;
    public int ARRAYLENGTH = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int PUTFIELD = 0;
    public int PUTSTATIC = 0;
    public int RETURN = 0;
    public int F_APPEND = 0;
    public int F_SAME = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int PUTSTATIC = 0;
    public int RETURN = 0;
    public int F_APPEND = 0;
    public int F_SAME = 0;
    public int F_SAME1 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int GOTO = 0;
    public int POP = 0;
    public int ACONST_NULL = 0;
    public int IFNONNULL = 0;
    public int SIPUSH = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int POP = 0;
    public int ACONST_NULL = 0;
    public int IFNONNULL = 0;
    public int SIPUSH = 0;
    public int INVOKESTATIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int INVOKEVIRTUAL = 0;
    public int GOTO = 0;
    public int POP = 0;
    public int ACONST_NULL = 0;
    public int IFNONNULL = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_INTERFACE = 0;
    public int ACC_SYNTHETIC = 0;
    public int ILOAD = 0;
    public int ISTORE = 0;
    public int AALOAD = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int V1_7 = 0;
    public int ACC_ABSTRACT = 0;
    public int ACC_INTERFACE = 0;
    public int ACC_SYNTHETIC = 0;
    public int ILOAD = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int INVOKESPECIAL = 0;
    public int ACC_PUBLIC = 0;
    public int ACC_FINAL = 0;
    public int ACC_SUPER = 0;
    public int ACC_PRIVATE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    //to just "0" which we really don't want
    public int ARETURN = 0;
    public int ALOAD = 0;
    public int IFNULL = 0;
    public int CHECKCAST = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_PRIVATE = 0;
    public int ACC_STATIC = 0;
    public int V1_5 = 0;
    public int V1_6 = 0;
    public int V1_7 = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ARETURN = 0;
    public int ALOAD = 0;
    public int IFNULL = 0;
    public int CHECKCAST = 0;
    public int INVOKEINTERFACE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int IFNONNULL = 0;
    public int SIPUSH = 0;
    public int INVOKESTATIC = 0;
    public int ICONST_0;
    public int ICONST_1;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int IRETURN = 0;
    public int NEW = 0;
    public int ANEWARRAY = 0;
    public int DUP = 0;
    public int ATHROW = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int F_APPEND = 0;
    public int F_SAME = 0;
    public int F_SAME1 = 0;
    public int INVOKESPECIAL = 0;
    public int ACC_PUBLIC = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int CHECKCAST = 0;
    public int INVOKEINTERFACE = 0;
    public int GETFIELD = 0;
    public int GETSTATIC = 0;
    public int ASTORE = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int ACC_STATIC = 0;
    public int V1_5 = 0;
    public int V1_6 = 0;
    public int V1_7 = 0;
    public int ACC_ABSTRACT = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int GETSTATIC = 0;
    public int ASTORE = 0;
    public int PUTFIELD = 0;
    public int PUTSTATIC = 0;
    public int RETURN = 0;
```

## RuleId[ruleID=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Configurable`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientConfigurableImpl.java`
#### Snippet
```java
public class MicroProfileClientConfigurableImpl<C extends Configurable<C>>
        extends ConfigurableImpl<C>
    implements Configurable<C> {
    static final Class<?>[] CONTRACTS = new Class<?>[] {ClientRequestFilter.class,
        ClientResponseFilter.class, ReaderInterceptor.class, WriterInterceptor.class,
```

### RedundantImplements
Redundant interface declaration `Configurable`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/CxfTypeSafeClientBuilder.java`
#### Snippet
```java
import static org.apache.cxf.jaxrs.client.ClientProperties.HTTP_RECEIVE_TIMEOUT_PROP;

public class CxfTypeSafeClientBuilder implements RestClientBuilder, Configurable<RestClientBuilder> {
    private static final Map<ClassLoader, Collection<RestClientListener>> REST_CLIENT_LISTENERS = 
        new WeakHashMap<>();
```

### RedundantImplements
Redundant interface declaration `MessageObserver`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
public class ClientImpl
    extends AbstractBasicInterceptorProvider
    implements Client, Retryable, MessageObserver {

    public static final String THREAD_LOCAL_REQUEST_CONTEXT = "thread.local.request.context";
```

### RedundantImplements
Redundant interface declaration `Configurable`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/CXFHttpRequest.java`
#### Snippet
```java
import org.apache.hc.core5.http.HttpEntity;

public class CXFHttpRequest extends HttpUriRequestBase implements Configurable {
    private static final long serialVersionUID = 1L;
    
```

### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/ResourceCloser.java`
#### Snippet
```java
import jakarta.jms.Session;

public class ResourceCloser implements Closeable, AutoCloseable {
    private AbstractSequentialList<Object> resources;

```

### RedundantImplements
Redundant interface declaration `MessageBodyWriter`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementTypedProvider.java`
#### Snippet
```java
@SuppressWarnings("rawtypes")
public class JAXBElementTypedProvider extends JAXBElementProvider<JAXBElement>
    implements MessageBodyWriter<JAXBElement>, MessageBodyReader<JAXBElement> {

    public JAXBElement readFrom(Class<JAXBElement> type, Type genericType, Annotation[] anns, MediaType mt,
```

### RedundantImplements
Redundant interface declaration `MessageBodyReader`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementTypedProvider.java`
#### Snippet
```java
@SuppressWarnings("rawtypes")
public class JAXBElementTypedProvider extends JAXBElementProvider<JAXBElement>
    implements MessageBodyWriter<JAXBElement>, MessageBodyReader<JAXBElement> {

    public JAXBElement readFrom(Class<JAXBElement> type, Type genericType, Annotation[] anns, MediaType mt,
```

### RedundantImplements
Redundant interface declaration `AutoCloseable`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
    }

    static class DynamicClientImpl extends ClientImpl implements AutoCloseable {
        final ClassLoader cl;
        final ClassLoader orig;
```

### RedundantImplements
Redundant interface declaration `BindingProvider`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
import org.apache.cxf.ws.addressing.WSAddressingFeature;

public class DispatchImpl<T> implements Dispatch<T>, BindingProvider, Closeable {
    private static final Logger LOG = LogUtils.getL7dLogger(DispatchImpl.class);
    private static final String DISPATCH_NS = "http://cxf.apache.org/jaxws/dispatch";
```

### RedundantImplements
Redundant interface declaration `Invoker`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws22/JAXWS22Invoker.java`
#### Snippet
```java
 */
@Deprecated
public class JAXWS22Invoker extends JAXWSMethodInvoker implements Invoker {

    public JAXWS22Invoker(jakarta.xml.ws.spi.Invoker inv) {
```

### RedundantImplements
Redundant interface declaration `DestinationFactory`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/JAXWSHttpSpiTransportFactory.java`
#### Snippet
```java
import org.apache.cxf.transport.http.DestinationRegistryImpl;

public class JAXWSHttpSpiTransportFactory extends SoapTransportFactory implements DestinationFactory {

    private HttpContext context;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/OAuthAuthorizationData.java`
#### Snippet
```java
@XmlRootElement(name = "authorizationData",
                namespace = "http://org.apache.cxf.rs.security.oauth")
public class OAuthAuthorizationData extends OAuthRedirectionState implements Serializable {
    private static final long serialVersionUID = -7755998413495017637L;

```

### RedundantImplements
Redundant interface declaration `InterceptorProvider`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
@NoJSR250Annotations
public class JAXBDataBinding extends AbstractInterceptorProvidingDataBinding
    implements WrapperCapableDatabinding, InterceptorProvider {

    public static final String READER_VALIDATION_EVENT_HANDLER = "jaxb-reader-validation-event-handler";
```

### RedundantImplements
Redundant interface declaration `SubscriptionManagerInterfaceForEventSources`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManager.java`
#### Snippet
```java
public interface SubscriptionManager
        extends SubscriptionManagerInterfaceForManagers,
        SubscriptionManagerInterfaceForEventSources,
        SubscriptionManagerInterfaceForNotificators {

```

## RuleId[ruleID=InstanceofCatchParameter]
### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
            return tryWithInternal(a);
        } catch (final Throwable ex) {
            if (ex instanceof Exception) {
                throw (Exception)ex;
            } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/HttpsURLConnectionInfo.java`
#### Snippet
```java
            } catch (Exception e) {
                Throwable ex = e;
                if (e instanceof InvocationTargetException) {
                    ex = ((InvocationTargetException) e).getTargetException();
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `t`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
                    LOG.log(Level.FINEST, "Caught exception invoking compute method", t);
                }
                if (t instanceof InvocationTargetException) {
                    throw t.getCause();
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/SseEventSinkImpl.java`
#### Snippet
```java
                    // Very likely the connection is closed by the client (but we cannot
                    // detect if for sure, container-specific).
                    if (ex instanceof IOException) {
                        error = (IOException)ex;
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `re`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServerFactoryBean.java`
#### Snippet
```java
                    server.start();
                } catch (RuntimeException re) {
                    if (!(re instanceof ServiceConstructionException 
                        && re.getMessage().startsWith("There is an endpoint already running on"))) {
                        //avoid destroying another server on the same endpoint url
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
            throw e;
        } catch (Throwable e) {
            if (e instanceof UncheckedException && LOG.isLoggable(Level.FINE)) {
                LOG.log(Level.FINE, e.getLocalizedMessage(), e);
            }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
                if (responseExpected || isInbound()) {
                    //brain dead spec - if it's one way, swallow it
                    if (e instanceof SOAPFaultException) {
                        throw mapSoapFault((SOAPFaultException)e);
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
                if (responseExpected || outbound) {
                    setFault(e);
                    if (e instanceof SOAPFaultException) {
                        throw mapSoapFault((SOAPFaultException)e);
                    }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `exception`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerFaultOutInterceptor.java`
#### Snippet
```java
                SOAPFault soapFault = body.addFault();

                if (exception instanceof SOAPFaultException) {
                    SOAPFaultException sf = (SOAPFaultException)exception;
                    soapFault.setFaultString(sf.getFault().getFaultString());
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `exception`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerFaultOutInterceptor.java`
#### Snippet
```java
                        soapFault.addDetail().appendChild(nd);
                    }
                } else if (exception instanceof Fault) {
                    SoapFault sf = SoapFault.createFault((Fault)exception, message
                        .getVersion());
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AccessTokenService.java`
#### Snippet
```java
            // This is done to bypass a Check-Style
            // restriction on a number of return statements
            OAuthServiceException oauthEx = ex instanceof OAuthServiceException
                ? (OAuthServiceException)ex : new OAuthServiceException(ex);
            return handleException(oauthEx, OAuthConstants.INVALID_GRANT);
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                                              new Class[] {cls}, ih);
            } else {
                if (ex instanceof RuntimeException) {
                    throw (RuntimeException)ex;
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/DefaultProtocolInterceptor.java`
#### Snippet
```java
                        response = new WrappedAtmosphereResponse(r.getResponse(), request);
                    }
                    if (e instanceof InvalidPathException) {
                        response.setIntHeader(WebSocketUtils.SC_KEY, 400);
                    } else {
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `ex`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                                              new Class[] {cls}, ih);
            } else {
                if (ex instanceof RuntimeException) {
                    throw (RuntimeException)ex;
                }
```

### InstanceofCatchParameter
'instanceof' on 'catch' parameter `e`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
                buildContext.setValue("cxf.file.list." + doneFile.getName(), null);
                getLog().debug(e);
                if (e instanceof RuntimeException) {
                    throw (RuntimeException)e;
                }
```

## RuleId[ruleID=ExceptionNameDoesntEndWithException]
### ExceptionNameDoesntEndWithException
Exception class name `XMLFault` does not end with 'Exception'
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/XMLFault.java`
#### Snippet
```java
import org.apache.cxf.interceptor.Fault;

public class XMLFault extends Fault {

    public static final String XML_FAULT_PREFIX = "xfns";
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsupportedConstruct` does not end with 'Exception'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/UnsupportedConstruct.java`
#### Snippet
```java
 * construct that it cannot handle.
 */
public class UnsupportedConstruct extends RuntimeException {

    private static final long serialVersionUID = -5854990568839716895L;
```

### ExceptionNameDoesntEndWithException
Exception class name `XmlSchemaInvalidOperation` does not end with 'Exception'
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaInvalidOperation.java`
#### Snippet
```java
 * Very simple exception class used to detect internal errors.
 */
public class XmlSchemaInvalidOperation extends RuntimeException {

    private static final long serialVersionUID = 7007258111909011558L;
```

### ExceptionNameDoesntEndWithException
Exception class name `Fault` does not end with 'Exception'
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java
 * A Fault that occurs during invocation processing.
 */
public class Fault extends UncheckedException {
    public static final QName FAULT_CODE_CLIENT = new QName("http://cxf.apache.org/faultcode", "client");
    public static final QName FAULT_CODE_SERVER = new QName("http://cxf.apache.org/faultcode", "server");
```

### ExceptionNameDoesntEndWithException
Exception class name `UnknownDialect` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/UnknownDialect.java`
#### Snippet
```java
 * Definition of the UnknownDialect SOAPFault.
 */
public class UnknownDialect extends WSTransferFault {

    private static final long serialVersionUID = 7139243705016686015L;
```

### ExceptionNameDoesntEndWithException
Exception class name `UnknownResource` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/UnknownResource.java`
#### Snippet
```java
 * that is not known.
 */
public class UnknownResource extends WSTransferFault {

    private static final long serialVersionUID = 2925090710469446447L;
```

### ExceptionNameDoesntEndWithException
Exception class name `PutDenied` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/PutDenied.java`
#### Snippet
```java
 * Definition of the PutDenied SOAPFault.
 */
public class PutDenied extends WSTransferFault {

    private static final long serialVersionUID = -6644888926499946963L;
```

### ExceptionNameDoesntEndWithException
Exception class name `InvalidRepresentation` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/InvalidRepresentation.java`
#### Snippet
```java
 * Definition of the InvalidRepresentation SOAPFault.
 */
public class InvalidRepresentation extends WSTransferFault {

    private static final long serialVersionUID = 330259919571428100L;
```

### ExceptionNameDoesntEndWithException
Exception class name `InvalidExpression` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/faults/InvalidExpression.java`
#### Snippet
```java
 * Definition of the InvalidExpression SOAPFault.
 */
public class InvalidExpression extends FragmentFault {

    private static final long serialVersionUID = -1920756304737648952L;
```

### ExceptionNameDoesntEndWithException
Exception class name `WSTransferFault` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/shared/faults/WSTransferFault.java`
#### Snippet
```java
 * The parent for all WS-Transfer-specific faults.
 */
public abstract class WSTransferFault extends SoapFault {

    private static final long serialVersionUID = -7603229392321418734L;
```

### ExceptionNameDoesntEndWithException
Exception class name `FragmentFault` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/faults/FragmentFault.java`
#### Snippet
```java
 * The parent for all WS-Fragment-specific faults.
 */
public abstract class FragmentFault extends SoapFault {

    private static final long serialVersionUID = 2111286624025926462L;
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsupportedMode` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/faults/UnsupportedMode.java`
#### Snippet
```java
 * Definition of the UnsupportedMode SOAPFault.
 */
public class UnsupportedMode extends FragmentFault {

    private static final long serialVersionUID = -5824904523582078377L;
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsupportedLanguage` does not end with 'Exception'
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/faults/UnsupportedLanguage.java`
#### Snippet
```java
 * Definition of the UnsupportedLanguage SOAPFault.
 */
public class UnsupportedLanguage extends FragmentFault {

    private static final long serialVersionUID = 7627652802987243575L;
```

### ExceptionNameDoesntEndWithException
Exception class name `SequenceFault` does not end with 'Exception'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java
 * Holder for SequenceFault information.
 */
public class SequenceFault extends Exception {

    private static final long serialVersionUID = 1144842362378723203L;
```

### ExceptionNameDoesntEndWithException
Exception class name `CustomerNotFoundFault` does not end with 'Exception'
in `testutils/src/main/java/org/apache/cxf/customer/CustomerNotFoundFault.java`
#### Snippet
```java

@WebFault
public class CustomerNotFoundFault extends Exception {
    private static final long serialVersionUID = -2104569359859457879L;
    private CustomerNotFoundDetails details;
```

### ExceptionNameDoesntEndWithException
Exception class name `BookNotFoundFault` does not end with 'Exception'
in `testutils/src/main/java/org/apache/cxf/customer/book/BookNotFoundFault.java`
#### Snippet
```java

@WebFault
public class BookNotFoundFault extends Exception {
    private static final long serialVersionUID = 497738666759033678L;
    private BookNotFoundDetails details;
```

### ExceptionNameDoesntEndWithException
Exception class name `JMSFault` does not end with 'Exception'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java
 *
 */
public class JMSFault extends Exception {

    private static final long serialVersionUID = 8567771013189842711L;
```

### ExceptionNameDoesntEndWithException
Exception class name `SoapFault` does not end with 'Exception'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java
import org.apache.cxf.interceptor.Fault;

public class SoapFault extends Fault {
    public static final QName ATTACHMENT_IO = new QName(Soap12.SOAP_NAMESPACE, "AttachmentIOError");

```

### ExceptionNameDoesntEndWithException
Exception class name `NoDeliveryMechanismEstablished` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/NoDeliveryMechanismEstablished.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class NoDeliveryMechanismEstablished extends WSEventingFault {

    public static final String REASON = "No delivery mechanism specified.";
```

### ExceptionNameDoesntEndWithException
Exception class name `CannotProcessFilter` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/CannotProcessFilter.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class CannotProcessFilter extends WSEventingFault {

    public static final String REASON = "Cannot filter as requested.";
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsupportedExpirationValue` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/UnsupportedExpirationValue.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class UnsupportedExpirationValue extends WSEventingFault {

    public static final String REASON = "The expiration time requested is not within the min/max range.";
```

### ExceptionNameDoesntEndWithException
Exception class name `UnsupportedExpirationType` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/UnsupportedExpirationType.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class UnsupportedExpirationType extends WSEventingFault {

    public static final String REASON = "Only expiration durations are supported.";
```

### ExceptionNameDoesntEndWithException
Exception class name `DeliveryFormatRequestedUnavailable` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/DeliveryFormatRequestedUnavailable.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class DeliveryFormatRequestedUnavailable extends WSEventingFault {

    public static final String REASON = "The requested delivery format is not supported.";
```

### ExceptionNameDoesntEndWithException
Exception class name `UnusableEPR` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/UnusableEPR.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class UnusableEPR extends WSEventingFault {

    public static final String REASON = "An EPR in the Subscribe request message is unusable.";
```

### ExceptionNameDoesntEndWithException
Exception class name `EmptyFilter` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/EmptyFilter.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class EmptyFilter extends WSEventingFault {

    public static final String REASON = "The wse:Filter would result in zero notifications.";
```

### ExceptionNameDoesntEndWithException
Exception class name `WSEventingFault` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/WSEventingFault.java`
#### Snippet
```java
 */
@FaultAction(EventingConstants.ACTION_FAULT)
public abstract class WSEventingFault extends SoapFault {

    private static final long serialVersionUID = 1L;
```

### ExceptionNameDoesntEndWithException
Exception class name `UnknownSubscription` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/UnknownSubscription.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class UnknownSubscription extends WSEventingFault {

    public static final String REASON = "The subscription is not known.";
```

### ExceptionNameDoesntEndWithException
Exception class name `FilteringRequestedUnavailable` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/FilteringRequestedUnavailable.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class FilteringRequestedUnavailable extends WSEventingFault {

    public static final String REASON = "The requested filter dialect is not supported.";
```

### ExceptionNameDoesntEndWithException
Exception class name `EndToNotSupported` does not end with 'Exception'
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/faults/EndToNotSupported.java`
#### Snippet
```java
import org.apache.cxf.ws.eventing.shared.EventingConstants;

public class EndToNotSupported extends WSEventingFault {

    public static final String REASON = "wse:EndTo semantics is not supported.";
```

## RuleId[ruleID=ArrayEquality]
### ArrayEquality
Array objects are compared using `!=`, not 'Arrays.equals()'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/AbstractJweDecryption.java`
#### Snippet
```java
        }
        Arrays.fill(cek, (byte) 0);
        if (actualCek != cek) {
            Arrays.fill(actualCek, (byte) 0);
        }
```

## RuleId[ruleID=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing unmodifiableList
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
                                            .map(ProviderInfo::getProvider)
                                            .sorted(new ResponseExceptionMapperComparator())
                                            .collect(Collectors.toList()));
    }

```

## RuleId[ruleID=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
        }
        // Certificate was not found: return empty list
        return new X509Certificate[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        });

        return dest2.toArray(new AbstractDestination[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/CXFAuthenticator.java`
#### Snippet
```java
                    ClassLoader loader = AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                        public ClassLoader run() {
                            return new URLClassLoader(new URL[0], ClassLoader.getSystemClassLoader());
                        }
                    }, null);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/auth/AbstractSpnegoAuthSupplier.java`
#### Snippet
```java
        // immediately return a ticket based on this credential without attempting
        // to log on again
        final byte[] token = new byte[0];
        if (delegatedCred != null) {
            return context.initSecContext(token, 0, token.length);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
            return null;
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
            if (in == null) {
                // Create an empty stream to avoid NullPointerExceptions
                in = new ByteArrayInputStream(new byte[] {});
            }
            inMessage.setContent(InputStream.class, in);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                    Object registration = m.invoke(super.getServletContext(), new Object[]{filterName});
                    if (registration != null) {
                        m = registration.getClass().getMethod("getUrlPatternMappings", new Class[] {});
                        Collection<String> mappings =
                            CastUtils.cast((Collection<?>)m.invoke(registration, new Object[]{}));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
                        m = registration.getClass().getMethod("getUrlPatternMappings", new Class[] {});
                        Collection<String> mappings =
                            CastUtils.cast((Collection<?>)m.invoke(registration, new Object[]{}));
                        if (!mappings.isEmpty()) {
                            String mapping = mappings.iterator().next();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java
            }
        }
        return soapEndpoints.toArray(new AbstractDestination[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java
            }
        }
        return restfulDests.toArray(new AbstractDestination[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/MPRestClientInvocationContextImpl.java`
#### Snippet
```java
        this.target = target;
        this.method = method;
        this.args = args == null ? new Object[] {} : args;
        this.interceptorInvokers = interceptorInvokers;
        this.callable = callable;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
            }
        }
        return merged.toArray(new Annotation[] {});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java

        Object providerObject = pf.createMessageBodyWriter(body.getClass(),
                                   body.getClass(), new Annotation[]{},
                                   MediaType.APPLICATION_XML_TYPE, m);
        if (!(providerObject instanceof JAXBElementProvider)) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
        m.setContent(XMLStreamWriter.class, writer);
        provider.writeTo(body,
                         body.getClass(), new Annotation[]{},
                         MediaType.APPLICATION_XML_TYPE,
                         (MultivaluedMap<String, Object>)m.get(Message.PROTOCOL_HEADERS), null);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/MetricsFeature.java`
#### Snippet
```java
                    Collection<?> coll = b.getBeansOfType(MetricsProvider.class);
                    if (coll != null) {
                        providers = coll.toArray(new MetricsProvider[]{});
                    }
                }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
                try {
                    String nextPart = StringUtils.capitalize(names[1]);
                    Method m = value.getClass().getMethod("get" + nextPart, new Class[]{});
                    value = m.invoke(value, new Object[]{});
                } catch (Throwable ex) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
                    String nextPart = StringUtils.capitalize(names[1]);
                    Method m = value.getClass().getMethod("get" + nextPart, new Class[]{});
                    value = m.invoke(value, new Object[]{});
                } catch (Throwable ex) {
                    throw new RuntimeException();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/ParamConverterUtils.java`
#### Snippet
```java

        if (provider != null) {
            final ParamConverter< T > converter = provider.getConverter(type, null, new Annotation[0]);
            if (converter != null) {
                return converter.fromString(value);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/ParamConverterUtils.java`
#### Snippet
```java

        if (provider != null) {
            final ParamConverter< T > converter = provider.getConverter(type, null, new Annotation[0]);
            if (converter != null) {
                return converter.toString(value);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
        Message m = JAXRSUtils.getCurrentMessage();
        Object obj = InjectionUtils.createFromParameterHandler(value, valueType, valueType,
                                                               new Annotation[]{}, m);
        if (obj != null) {
            return obj;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
        try {
            String nextPart = getMethodNameSuffix(names[1]);
            Method getterM = actualType.getMethod("get" + nextPart, new Class[]{});
            Class<?> returnType = getterM.getReturnType();
            boolean returnCollection = InjectionUtils.isSupportedCollectionOrArray(returnType);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/AbstractSearchConditionParser.java`
#### Snippet
```java
        Message m = JAXRSUtils.getCurrentMessage();
        Temporal obj = InjectionUtils.createFromParameterHandler(value, valueType, valueType,
                                                               new Annotation[]{}, m);
        if (obj != null) {
            return obj;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                    String nextPart = StringUtils.capitalize(names[1]);

                    Method m = actualCls.getMethod("get" + nextPart, new Class[]{});
                    if (isCollection) {
                        value = ((Collection)value).iterator().next();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
                        set.add(names[0]);
                    }
                    value = m.invoke(value, new Object[]{});
                    valueCls = value.getClass();
                    type = m.getGenericReturnType();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/JPACriteriaQueryVisitor.java`
#### Snippet
```java

    private static Selection<?>[] toSelectionsArray(List<Selection<?>> selections) {
        return selections.toArray(new Selection[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
            }
            List<Predicate> predsList = predStack.pop();
            Predicate[] preds = predsList.toArray(new Predicate[0]);
            Predicate newPred;
            if (sc instanceof OrSearchCondition) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
        if (!criteriaFinalized) {
            List<Predicate> predsList = predStack.pop();
            cq.where(predsList.toArray(new Predicate[0]));
            criteriaFinalized = true;
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/security/src/main/java/org/apache/cxf/rt/security/crypto/CryptoUtils.java`
#### Snippet
```java
        try {
            Cipher c = initCipher(secretKey, keyProps, mode);
            byte[] result = new byte[0];
            int blockSize = keyProps != null ? keyProps.getBlockSize() : -1;
            if (secretKey instanceof SecretKey && blockSize == -1) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/io/DelegatingInputStream.java`
#### Snippet
```java
                    in = cache.getInputStream();
                } else {
                    in = new ByteArrayInputStream(new byte[0]);
                }
                cache.close();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContext.java`
#### Snippet
```java
                                 ApplicationContext parent,
                                 NamespaceHandlerResolver res) {
        super(new String[0], false, parent);
        cfgFileURLs = url;
        includeDefaults = include;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContext.java`
#### Snippet
```java
    public BusApplicationContext(String[] cf, boolean include,
                                 ApplicationContext parent, NamespaceHandlerResolver res) {
        super(new String[0], false, parent);
        cfgFiles = cf;
        includeDefaults = include;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java
        this.extensions.put(ResourceManager.class, resourceManager);

        extensionManager = new ExtensionManagerImpl(new String[0],
                                                    extensionClassLoader,
                                                    this.extensions,
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
                                     boolean urlSafe) {
        if (id != null && id.length == 0 && l == 0) {
            return new char[0];
        } else if (l <= 0) {
            return null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java

        if (id != null && id.length == 0 && l == 0) {
            return new byte[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        T[] tmp = data.get();
        if (tmp == null) {
            return new Object[0];
        }
        T[] tmp2 = newArray(tmp.length);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java
                    }
                }
                return descriptors.toArray(new PropertyDescriptor[0]);
            }
            return null;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
        Collections.addAll(list, files);

        return internalCompile(list.toArray(new String[0]), idx);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            f.add(file.getAbsolutePath());
        }
        return compileFiles(f.toArray(new String[0]));
    }
    public boolean compileFiles(List<File> files) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            f.add(file.getAbsolutePath());
        }
        return compileFiles(f.toArray(new String[0]));
    }
    public boolean compileFiles(String[] files) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/util/CglibProxyHelper.java`
#### Snippet
```java
            enhancer.setClassLoader(loader);
            enhancer.setSuperclass(superClass);
            enhancer.setInterfaces(theInterfaces.toArray(new Class<?>[0]));
            enhancer.setCallback(new MethodInterceptor() {

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                || XMLGregorianCalendar.class.isAssignableFrom(paramCls)
                || InjectionUtils.isSupportedCollectionOrArray(paramCls)) {
                doWriteParam(ori, sb, entry.getKey(), paramCls, paramCls, name, new Annotation[] {}, isJson);
            } else if (!parentBeanClasses.contains(paramCls)) {
                parentBeanClasses.add(paramCls);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                        XmlType root = cls.getAnnotation(XmlType.class);
                        if (root != null) {
                            QName typeName = theResolver.resolve(cls, new Annotation[] {},
                                                           Collections.<Class<?>, QName> emptyMap());
                            if (typeName != null && tns.equals(typeName.getNamespaceURI())) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                    if (supportJaxbSubstitutions) {
                        for (Map.Entry<Class<?>, Class<?>> entry : resourceTypes.getSubstitutions().entrySet()) {
                            QName typeName = theResolver.resolve(entry.getKey(), new Annotation[] {},
                                                                 Collections.<Class<?>, QName> emptyMap());
                            for (Element element : DOMUtils.findAllElementsByTagNameNS(doc.getDocumentElement(),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                                                                                       "element")) {
                                if (element.getAttribute("name").equals(typeName.getLocalPart())) {
                                    QName groupName = theResolver.resolve(entry.getValue(), new Annotation[] {},
                                                                         Collections.<Class<?>, QName> emptyMap());
                                    if (groupName != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                        QName colQName = entry.getValue();
                        if (colQName == null) {
                            colQName = theResolver.resolve(entry.getKey(), new Annotation[] {},
                                                Collections.<Class<?>, QName> emptyMap());
                            if (colQName != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                        }
                        if (tns.equals(colQName.getNamespaceURI())) {
                            QName typeName = theResolver.resolve(entry.getKey(), new Annotation[] {},
                                                                 Collections.<Class<?>, QName> emptyMap());
                            if (typeName != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                                     m.getGenericParameterTypes()[0],
                                     propertyName,
                                     new Annotation[]{},
                                     isJson);
                    } else if (f != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                                         f.getGenericType(),
                                         propertyName,
                                         new Annotation[]{},
                                         isJson);
                        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    private void setEnumOptions(StringBuilder sb, Class<?> enumClass) {
        try {
            Method m = enumClass.getMethod("values", new Class<?>[] {});
            Object[] values = (Object[])m.invoke(null, new Object[] {});
            m = enumClass.getMethod("name", new Class<?>[] {});
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        try {
            Method m = enumClass.getMethod("values", new Class<?>[] {});
            Object[] values = (Object[])m.invoke(null, new Object[] {});
            m = enumClass.getMethod("name", new Class<?>[] {});
            for (Object o : values) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            Method m = enumClass.getMethod("values", new Class<?>[] {});
            Object[] values = (Object[])m.invoke(null, new Object[] {});
            m = enumClass.getMethod("name", new Class<?>[] {});
            for (Object o : values) {
                String str = (String)m.invoke(o, new Object[] {});
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            m = enumClass.getMethod("name", new Class<?>[] {});
            for (Object o : values) {
                String str = (String)m.invoke(o, new Object[] {});
                sb.append("<option value=\"").append(str).append("\"/>");
            }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                }
            }
            return new Annotation[] {};
        }
        return opMethod.getDeclaredAnnotations();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java
        final List<URL> urlList, final ClassLoader parent
    ) {
        return getURLClassLoader(urlList.toArray(new URL[0]), parent);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
                        try {
                            return (JAXBContext)m.invoke(null,
                                     classes.toArray(new Class<?>[0]),
                                     typeRefs,
                                     map.get(pfx + "subclassReplacements"),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
        String[] list = d.list();
        if (list == null) {
            list = new String[0];
        }
        for (int i = 0; i < list.length; i++) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
                            JAXBContextCache.class);
                    Method m = factoryClass.getMethod("createContext", Class[].class, Map.class);
                    Object context = m.invoke(null, classes.toArray(new Class<?>[0]), map);
                    return (JAXBContext) context;
                }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
                if ("newInstance".equals(m.getName())
                    && m.getParameterTypes().length == 6) {
                    ctx = m.invoke(null, clses.toArray(new Class<?>[0]),
                                         typeRefs, null, null, true, null);

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/service/factory/AnnotationsFactoryBeanListener.java`
#### Snippet
```java
                addDocumentation(ep,
                                 WSDLDocumentation.Placement.SERVICE,
                                 docs.toArray(new WSDLDocumentation[0]));
            }
            addBindingOperationDocs(ep);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/service/invoker/AbstractInvoker.java`
#### Snippet
```java
        Object res;
        try {
            Object[] paramArray = new Object[]{};
            if (params != null) {
                paramArray = params.toArray();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            }

            CommandInfo[] allCommandArray = new CommandInfo[0];
            return cmdList.toArray(allCommandArray);
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            Collections.addAll(mimeTypeSet, mimeTypes);
            Collections.addAll(mimeTypeSet, defMimeTypes);
            String[] mimeArray = new String[0];
            return mimeTypeSet.toArray(mimeArray);
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
                                               Logger log,
                                               boolean exclude) {
        String[] cipherSuites = cipherSuitesList.toArray(new String[0]);
        if (log.isLoggable(Level.FINE)) {
            LogUtils.log(log, Level.FINE,
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
            } catch (Exception e) {
                log.log(Level.WARNING, "Default key managers cannot be initialized: " + e.getMessage(), e);
                defaultManagers = new KeyManager[0];
            }
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
        if (null == defaultManagers) {
            log.log(Level.FINER, "No default keystore {0}", location);
            defaultManagers = new KeyManager[0];
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpServletRequest.java`
#### Snippet
```java
            return null;
        }
        return values.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServletPipelineFactory.java`
#### Snippet
```java
                        tlsServerParameters.getCipherSuitesFilter(),
                        SSLContext.getDefault().getDefaultSSLParameters().getCipherSuites(),
                        Http2SecurityUtil.CIPHERS.toArray(new String[] {}),
                        LOG);
            
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java
            try {
                CommandInterfaceUtils.commandCommonMain();
                JavaToWS j2w = new JavaToWS(args.toArray(new String[0]));
                j2w.run();
            } catch (OutOfMemoryError e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/Java2WSMojo.java`
#### Snippet
```java
            }

            cmd.addArguments(args.toArray(new String[0]));
            if (classpathAsEnvVar && !StringUtils.isEmpty(cp)) {
                cmd.addEnvironment("CLASSPATH", cp);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            return null;
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSLoginModule.java`
#### Snippet
```java
        char[] tmpPassword = ((PasswordCallback) callbacks[1]).getPassword();
        if (tmpPassword == null) {
            tmpPassword = new char[0];
        }
        String password = new String(tmpPassword);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            return null;
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/TransportBindingHandler.java`
#### Snippet
```java
            return sig.getSignatureValue();
        }
        return new byte[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSMessageConverter.java`
#### Snippet
```java
            return streamMessage.readObject();
        } else {
            return new byte[]{};
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxy.java`
#### Snippet
```java
        Object[] params = args;
        if (null == params) {
            params = new Object[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/SseBroadcasterImpl.java`
#### Snippet
```java
        }
        
        return CompletableFuture.allOf(futures.toArray(new CompletableFuture[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/InboundSseEventImpl.java`
#### Snippet
```java
        }

        final Annotation[] annotations = new Annotation[0];
        final MultivaluedMap<String, String> headers = new MultivaluedHashMap<>(0);
        
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseBuilderImpl.java`
#### Snippet
```java
            return allow();
        }
        return allow(methods.toArray(new String[0]));
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java
                }
            }
            return values.toArray(new String[0]);
        }
        String separator = getSeparator();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    @Override
    public <T> T readEntity(Class<T> cls) throws ProcessingException, IllegalStateException {
        return readEntity(cls, new Annotation[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseImpl.java`
#### Snippet
```java
    @Override
    public <T> T readEntity(GenericType<T> genType) throws ProcessingException, IllegalStateException {
        return readEntity(genType, new Annotation[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpServletRequestFilter.java`
#### Snippet
```java
            map2.putAll(map1);
            for (Map.Entry<String, List<String>> e : formParams.entrySet()) {
                map2.put(e.getKey(), e.getValue().toArray(new String[]{}));
            }
            return Collections.unmodifiableMap(map2);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpServletRequestFilter.java`
#### Snippet
```java
            List<String> values = formParams.get(name);
            if (values != null) {
                value = values.toArray(new String[]{});
            }
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurationImpl.java`
#### Snippet
```java
        } else {
            register(o, AnnotationUtils.getBindingPriority(o.getClass()), 
                        ConfigurableImpl.getImplementedContracts(o, new Class<?>[]{}));
        }
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java
    @Override
    public Collection<Class<?>> register(Class<?> callback) throws NullPointerException {
        return register(callback, new Class<?>[]{}).get(callback);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java
    @Override
    public Collection<Class<?>> register(Object callback) throws NullPointerException {
        return register(callback, new Object[]{}).get(callback.getClass());
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurableImpl.java`
#### Snippet
```java
            .collect(Collectors.toList());

        return implementedContracts.toArray(new Class<?>[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/Attachment.java`
#### Snippet
```java
        if (providers != null) {
            MessageBodyReader<T> mbr =
                providers.getMessageBodyReader(cls, cls, new Annotation[]{}, getContentType());
            if (mbr != null) {
                try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/multipart/Attachment.java`
#### Snippet
```java
            if (mbr != null) {
                try {
                    return mbr.readFrom(cls, cls, new Annotation[]{}, getContentType(),
                                        headers, getDataHandler().getInputStream());
                } catch (Exception ex) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
        throws Exception {
        return provider.readFrom(Form.class, Form.class,
                              new Annotation[]{}, MediaType.APPLICATION_FORM_URLENCODED_TYPE,
                              new MetadataMap<String, String>(),
                              message.getContent(InputStream.class));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
                                 Form form, OutputStream os)
        throws Exception {
        provider.writeTo(form, Form.class, Form.class, new Annotation[]{},
                         MediaType.APPLICATION_FORM_URLENCODED_TYPE, new MetadataMap<String, Object>(), os);
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/schemas/SchemaHandler.java`
#### Snippet
```java
                }
            }
            return factory.newSchema(sources.toArray(new Source[0]));
        } catch (Exception ex) {
            throw new IllegalArgumentException("Failed to load XML schema : " + ex.getMessage(), ex);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        try {
            if (StringUtils.isEmpty(packageList)) {
                context = JAXBContext.newInstance(new Class[0], contextProperties);
            } else {
                context = JAXBContext.newInstance(packageList, cl, contextProperties);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
                URL[] urls = ((URLClassLoader)tcl).getURLs();
                if (urls == null) {
                    urls = new URL[0];
                }
                for (URL url : urls) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            method = InjectionUtils.checkProxy(method, instance);
            try {
                method.invoke(instance, new Object[]{});
            } catch (InvocationTargetException ex) {
                String msg = "Method " + method.getName() + " can not be invoked"
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
    }
    public static Object convertStringToPrimitive(String value, Class<?> cls) {
        return convertStringToPrimitive(value, cls, new Annotation[]{});
    }
    public static Object convertStringToPrimitive(String value, Class<?> cls, Annotation[] anns) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java

        public void setParameters(String name, List<String> values) {
            doSetParameters(name, values.toArray(new String[0]));
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/lifecycle/SingletonResourceProvider.java`
#### Snippet
```java
                ResourceUtils.createConstructorArguments(c, m, false, Collections.emptyMap());
            try {
                resourceInstance = values.length > 0 ? c.newInstance(values) : c.newInstance(new Object[]{});
            } catch (Exception ex) {
                throw new ServiceConstructionException(ex);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/lifecycle/PerRequestResourceProvider.java`
#### Snippet
```java
        Object[] values = ResourceUtils.createConstructorArguments(c, m, true, mapValues, params, anns, genericTypes);
        try {
            Object instance = values.length > 0 ? c.newInstance(values) : c.newInstance(new Object[]{});
            InjectionUtils.invokeLifeCycleMethod(instance, postConstructMethod);
            return instance;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
                mc.put("org.apache.cxf.multipart.embedded.input",
                       multipart.getDataHandler().getInputStream());
                anns = new Annotation[]{};
            }
            MessageBodyReader<T> r =
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
                Object value = entry.getValue();
                Attachment handler = createDataHandler(value, value.getClass(),
                                                       new Annotation[]{},
                                                       entry.getKey().toString(),
                                                       mt.toString(),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
            }
            dh = getHandlerForObject(att.getObject(),
                                     att.getObject().getClass(), new Annotation[]{},
                                     att.getContentType().toString(), id);
            return new Attachment(att.getContentId(), dh, att.getHeaders());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/MultipartProvider.java`
#### Snippet
```java
            Object value = objects.get(i);
            Attachment handler = createDataHandler(value,
                                           value.getClass(), new Annotation[]{},
                                           rootMediaType, rootMediaType, i);
            handlers.add(handler);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        }
        return newJAXBContextInstance(
            collectionContextClasses.toArray(new Class[0]), cProperties);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
    protected void marshal(Object obj, Class<?> cls, Type genericType,
                           String enc, OutputStream os, MediaType mt) throws Exception {
        marshal(obj, cls, genericType, enc, os, mt, new Annotation[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java

        Annotation[] annotations;
        Annotation[] staticAnns = ori != null ? ori.getOutAnnotations() : new Annotation[]{};
        Annotation[] responseAnns = response.getEntityAnnotations();
        if (responseAnns != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                                                        Message m) {

        anns = anns != null ? anns : new Annotation[]{};
        for (ProviderInfo<ParamConverterProvider> pi : paramConverters) {
            injectContextValues(pi, m);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                                               Class<?> commonBaseCls) {
        if (Object.class == cls) {
            return new Type[]{};
        }
        if (expectedClass != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                           && commonBaseCls.isAssignableFrom(actualType)
                           || expectedClass.isAssignableFrom(actualType)) {
                    return new Type[]{};
                }
            }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
        Object[] params = args;
        if (null == params) {
            params = new Object[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    @Override
    public <T> Dispatch<T> createDispatch(QName portName, Class<T> type, Mode mode) {
        return createDispatch(portName, type, mode, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    @Override
    public Dispatch<Object> createDispatch(QName portName, JAXBContext context, Mode mode) {
        return createDispatch(portName, context, mode, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java

    public <T> T getPort(Class<T> serviceEndpointInterface) {
        return getPort(serviceEndpointInterface, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java

    public <T> T getPort(QName portName, Class<T> serviceEndpointInterface) {
        return getPort(portName, serviceEndpointInterface, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java

    protected <T> T createPort(QName portName, EndpointReferenceType epr, Class<T> serviceEndpointInterface) {
        return createPort(portName, epr, serviceEndpointInterface, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
    public <T> T getPort(EndpointReferenceType endpointReference,
                            Class<T> type) {
        return getPort(endpointReference, type, new WebServiceFeature[]{});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedAttachments.java`
#### Snippet
```java

    public Object[] toArray() {
        return toArray(new Object[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
            header = msg.getSOAPPart().getEnvelope().getHeader();
            if (header == null || !header.hasChildNodes()) {
                return new Object[0];
            }
            List<Object> ret = new ArrayList<>();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
                }
            }
            return ret.toArray(new Object[0]);
        } catch (SOAPException | JAXBException e) {
            throw new WebServiceException(e);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAOutInterceptor.java`
#### Snippet
```java

                    public Method run() throws Exception {
                        Method hasSwaRefMethod = ctx.getClass().getMethod("hasSwaRef", new Class[0]);
                        if (!hasSwaRefMethod.isAccessible()) {
                            hasSwaRefMethod.setAccessible(true);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
            Object faultInfo;
            try {
                Method method = cause.getClass().getMethod("getFaultInfo", new Class[0]);
                faultInfo = method.invoke(cause, new Object[0]);
            } catch (NoSuchMethodException e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
            try {
                Method method = cause.getClass().getMethod("getFaultInfo", new Class[0]);
                faultInfo = method.invoke(cause, new Object[0]);
            } catch (NoSuchMethodException e) {
                faultInfo = createFaultInfoBean(fault, cause);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
            }

            Method getFaultInfo = exClass.getMethod("getFaultInfo", new Class[0]);

            return getFaultInfo.getReturnType();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
            Method futureMethod = ReflectionUtil
                .getDeclaredMethod(method.getDeclaringClass(), method.getName() + "Async",
                                   asyncHandlerParams.toArray(new Class<?>[0]));

            getMethodDispatcher().bind(op, method, responseMethod, futureMethod);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
        }

        return sps.toArray(new CompositeData[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            list.add(ds.getIdentifier().getValue());
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java

        List<Long> numbers = rq.getUndeliveredMessageNumbers(ds);
        return numbers.toArray(new Long[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
        }

        return sps.toArray(new CompositeData[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            }
        }
        return list.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            list.add(r.getUpper());
        }
        return list.toArray(new Long[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java

        List<Long> numbers = rq.getUnacknowledgedMessageNumbers(ss);
        return numbers.toArray(new Long[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            list.add(r.getUpper());
        }
        return list.toArray(new Long[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMManager.java`
#### Snippet
```java
            identifiers.add(RMUtils.getEndpointIdentifier(ep, manager.getBus()));
        }
        return identifiers.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBase.java`
#### Snippet
```java
            }
        }
        return annoList == null ? new Annotation[0] : annoList.toArray(new Annotation[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBase.java`
#### Snippet
```java
            }
        }
        return annoList == null ? new Annotation[0] : annoList.toArray(new Annotation[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
                list.add(idltowsdlOptions[x].getIDL());
                try {
                    IDLToWSDL.run(list.toArray(new String[0]));
                    doneFile.delete();
                    doneFile.createNewFile();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
        if (refClass != null) {
            try {
                return refClass.getConstructor(QName.class, Type.class, new Annotation[0].getClass()) //NOPMD
                    .newInstance(n, cls, new Annotation[0]);
            } catch (Throwable e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
            try {
                return refClass.getConstructor(QName.class, Type.class, new Annotation[0].getClass()) //NOPMD
                    .newInstance(n, cls, new Annotation[0]);
            } catch (Throwable e) {
                //ignore
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
                    list.add(wsdltoidlOptions[x].getWSDL());
                    try {
                        WSDLToIDL.run(list.toArray(new String[0]));
                        doneFile.delete();
                        doneFile.createNewFile();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
        OperationInfo oi = reliableEndpoint.getEndpoint(protocol).getEndpointInfo().getService()
            .getInterface().getOperation(constants.getAckRequestedOperationName());
        invoke(oi, protocol, new Object[] {});
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
            acks.setAddress(attrURIType);
            client = createClient(bus, endpoint, protocol, c, acks);
            params = new Object[] {};
        } else {
            EndpointReferenceType replyTo = reliableEndpoint.getReplyTo();
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
                invoke(oi, protocol, new Object[] {csr}, context, Level.FINER);
            } else {
                invoke(oi, protocol, new Object[] {}, context, Level.FINER);
            }
        } catch (Fault f) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            } catch (Exception e) {
                try {
                    cls.getConstructor(new Class[0]);
                } catch (Exception e2) {
                    //no String or default constructor, we cannot use it
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/utils/JwtTokenUtils.java`
#### Snippet
```java
        if (scope != null) {
            String[] scopes = scope instanceof String
                ? scope.toString().split(" ") : CastUtils.cast((List<?>)scope).toArray(new String[]{});
            List<OAuthPermission> perms = new LinkedList<>();
            for (String s : scopes) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            allOFMethods = objectFactory.getClass().getMethods();
        } else {
            allOFMethods = new Method[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                    try {
                        getMethod = valueClass.getMethod("is_return",
                                                          new Class[0]);
                    } catch (NoSuchMethodException ex2) {
                        //ignore for now
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

        return createWrapperHelper(getBus(), wrapperType,
                                 setMethods.toArray(new Method[0]),
                                 getMethods.toArray(new Method[0]),
                                 jaxbMethods.toArray(new Method[0]),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        return createWrapperHelper(getBus(), wrapperType,
                                 setMethods.toArray(new Method[0]),
                                 getMethods.toArray(new Method[0]),
                                 jaxbMethods.toArray(new Method[0]),
                                 fields.toArray(new Field[0]),
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                                 setMethods.toArray(new Method[0]),
                                 getMethods.toArray(new Method[0]),
                                 jaxbMethods.toArray(new Method[0]),
                                 fields.toArray(new Field[0]),
                                 objectFactory);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                                 getMethods.toArray(new Method[0]),
                                 jaxbMethods.toArray(new Method[0]),
                                 fields.toArray(new Field[0]),
                                 objectFactory);
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/JwtAccessTokenValidator.java`
#### Snippet
```java
        if (scope != null) {
            String[] scopes = scope instanceof String
                ? scope.toString().split(" ") : CastUtils.cast((List<?>)scope).toArray(new String[]{});
            List<OAuthPermission> perms = new LinkedList<>();
            for (String s : scopes) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }
            if (types != null) {
                values = !types.isEmpty() ? types.toArray(new String[0])
                                           : new String[]{"*/*"};
            }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
            }

            String[] searchAttributes = searchAttributeList.toArray(new String[0]);

            if (this.userBaseDn != null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
            Class<?> c = Class.forName(name);
            Object inputList = c.getConstructor(LSInput[].class, Integer.TYPE)
                .newInstance(inputs.toArray(new LSInput[0]), inputs.size());
            m.invoke(schemaLoader, inputList);
        } catch (InvocationTargetException e) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
            if (m != null) {
                try {
                    m.invoke(this, new Object[] {});
                } catch (IllegalAccessException e) {
                    pos = curPos;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `testutils/src/main/java/org/apache/cxf/authservice/AuthServiceImpl.java`
#### Snippet
```java
        }
        if ("0".equals(sid)) {
            return new String[0];
        }
        return new String[] {sid, sid + "-1"};
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java

        String inc = getIncludeExcludeString(includes);
        String ex = getIncludeExcludeString(exList.toArray(new String[0]));

        try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java

                WSDLValidator validator = new WSDLValidator(new ToolSpec(toolspecStream, false));
                validator.setArguments(list.toArray(new String[0]));
                ok = validator.executeForMaven();

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
                                                     + storageDir.getPath());
        }
        return certificateFiles.toArray(new File[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
        orbConfig.addPOAPolicies(orb, name, parentPOA, poaManager, policies);

        Policy[] policyList = policies.toArray(new Policy[0]);

        try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaBindingHelper.java`
#### Snippet
```java
            }
            List<String> orbArgs = config.getOrbArgs();
            defaultORB = ORB.init(orbArgs.toArray(new String[0]), props);
            if (defaultORB == null) {
                LOG.severe("Could not create instance of the ORB");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaBindingHelper.java`
#### Snippet
```java
            throw new CorbaBindingException("Unsupported address scheme type " + scheme);
        }
        ORB orb = ORB.init(orbArgs.toArray(new String[0]), props);
        orbList.put(getORBNameFromAddress(address), orb);

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaDSIServant.java`
#### Snippet
```java

    public String[] _all_interfaces(POA poa, byte[] objectId) {
        return interfaces.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java
     */
    public FireWallClassLoader(ClassLoader parent, String[] fs) {
        this(parent, fs, new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
        }

        return urlList.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `integration/jca/src/main/java/org/apache/cxf/jca/servant/EJBInvoker.java`
#### Snippet
```java
                throw new IllegalArgumentException("home must be for a stateless session bean");
            }
            createMethod = home.getClass().getMethod("create", new Class[0]);
        } catch (Exception ex) {
            throw new IllegalArgumentException("Unable to initialize invoker: " + ex);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBMessageListenerImpl.java`
#### Snippet
```java
    public Object lookupTargetObject(String targetJndiName) throws Exception {
        Object home = new InitialContext().lookup(targetJndiName);
        Method method = home.getClass().getMethod("create", new Class[0]);
        return method.invoke(home, new Object[0]);
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/DispatchMDBMessageListenerImpl.java`
#### Snippet
```java
        Object home = new InitialContext().lookup(targetJndiName);
        Method method = home.getClass().getMethod("create", new Class[0]);
        return method.invoke(home, new Object[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java

        String inc = joinWithComma(includes);
        String ex = joinWithComma(exList.toArray(new String[0]));

        try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java

    private static String[] readOptionsFromFile(File dir, String wsdlName) throws MojoExecutionException {
        String[] noOptions = new String[] {};
        List<File> files = FileUtils.getFiles(dir, wsdlName + WADL_OPTIONS);
        if (files.isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/Option.java`
#### Snippet
```java
     * Specifies JAXB binding files. Use spaces to separate multiple entries.
     */
    String[] bindingFiles = new String[0];

    /**
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
            List<String> list = option.generateCommandLine(outputDirFile, basedir, wadlURI, getLog()
                                                               .isDebugEnabled());
            String[] args = list.toArray(new String[0]);
            getLog().debug("Calling wadl2java with args: " + Arrays.toString(args));

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/primitive/PrimitiveAssertionBuilder.java`
#### Snippet
```java
    }
    public PrimitiveAssertionBuilder(Collection<QName> els) {
        knownElements = els.toArray(new QName[0]);
    }
    public PrimitiveAssertionBuilder(QName[] els) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/primitive/PrimitiveAssertionBuilder.java`
#### Snippet
```java
public class PrimitiveAssertionBuilder implements AssertionBuilder<Element> {

    private QName[] knownElements = {};

    public PrimitiveAssertionBuilder() {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/primitive/PrimitiveAssertionBuilder.java`
#### Snippet
```java

    public void setKnownElements(Collection<QName> k) {
        knownElements = k.toArray(new QName[0]);
    }
    public void setKnownElements(QName[] k) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/jaxb/JaxbAssertionBuilder.java`
#### Snippet
```java

    public QName[] getKnownElements() {
        return supportedTypes.toArray(new QName[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java

    private boolean isExistsPolicyReference(Object[] exts, String uri) {
        exts = exts == null ? new Object[0] : exts;
        for (Object o : exts) {
            if (o instanceof UnknownExtensibilityElement) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyAnnotationListener.java`
#### Snippet
```java
    }
    private boolean isExistsPolicy(Object[] exts, String uri) {
        exts = exts == null ? new Object[0] : exts;
        for (Object o : exts) {
            if (o instanceof UnknownExtensibilityElement) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwe/DirectKeyEncryptionAlgorithm.java`
#### Snippet
```java
    public byte[] getEncryptedContentEncryptionKey(JweHeaders headers, byte[] theCek) {
        checkKeyEncryptionAlgorithm(headers);
        return new byte[0];
    }
    protected void checkKeyEncryptionAlgorithm(JweHeaders headers) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsCompactConsumer.java`
#### Snippet
```java
    }
    public byte[] getDecodedSignature() {
        return encodedSignature.isEmpty() ? new byte[]{} : JoseUtils.decode(encodedSignature);
    }
    public JwsHeaders getJwsHeaders() {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
    public ManagedNotification[] getManagedNotifications(Class<?> clazz) {
        ManagedNotifications notificationsAnn = clazz.getAnnotation(ManagedNotifications.class);
        return null != notificationsAnn ? notificationsAnn.value() : new ManagedNotification[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
    public ManagedOperationParameter[] getManagedOperationParameters(Method method) {
        ManagedOperationParameters params = method.getAnnotation(ManagedOperationParameters.class);
        return params != null ? params.value() : new ManagedOperationParameter[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                // only deal with the first endpoint object which retrun from the list.
                endpointName = it.next();
                mbsc.invoke(endpointName, operation, new Object[0], new String[0]);
                System.out.println("invoke endpoint " + endpointName
                                   + " operation " + operation + " succeed!");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                // only deal with the first endpoint object which retrun from the list.
                endpointName = it.next();
                mbsc.invoke(endpointName, operation, new Object[0], new String[0]);
                System.out.println("invoke endpoint " + endpointName
                                   + " operation " + operation + " succeed!");
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
    public static X509Certificate[] toX509CertificateChainArray(List<String> base64EncodedChain) {
        List<X509Certificate> chain = toX509CertificateChain(base64EncodedChain);
        return chain == null ? null : chain.toArray(new X509Certificate[]{});
    }
    public static String getKeyAlgorithm(Message m, Properties props, String propName, String defaultAlg) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
            KeyStore keyStore = loadKeyStore(keyStoreLoc, null, keyStorePassword, bus);
            return CryptoUtils.loadPrivateKey(keyStore,
                                              keyPassword == null ? new char[]{} : keyPassword.toCharArray(),
                                              keyAlias);
        } catch (Exception ex) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/AbstractXOPType.java`
#### Snippet
```java
        // the appropriate data type. If there wasn't an attachment, it just returned the bytes.
        // Our subclass have to package them.
        if (thingRead.getClass() == (new byte[0]).getClass()) {
            return wrapBytes((byte[])thingRead, contentType);
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/Base64Type.java`
#### Snippet
```java
            if (reader.isEndElement()) {
                reader.next();
                return new byte[0];
            }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java
        getInterfacePropertyDescriptors(clazz, pds, new HashSet<Class<?>>());

        return pds.toArray(new PropertyDescriptor[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java

        if (descriptors == null) {
            descriptors = new PropertyDescriptor[0];
        }
        Arrays.sort(descriptors, new Comparator<PropertyDescriptor>() {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
            }

            return m.invoke(object, new Object[0]);
        } catch (Exception e) {
            throw new DatabindingException("Couldn't get property " + name + " from bean " + object, e);
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/WsdlUtilities.java`
#### Snippet
```java

        String inc = joinWithComma(includes);
        String ex = joinWithComma(exList.toArray(new String[0]));

        try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WsdlOption.java`
#### Snippet
```java
         * Until we figure out what this amounts to. I suspect it stays null for Javascript.
         */
        return new File[0];
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java

    private static String[] readOptionsFromFile(File dir, String wsdlName) throws MojoExecutionException {
        String[] noOptions = new String[] {};
        List<File> files = FileUtils.getFiles(dir, wsdlName + WSDL_OPTIONS);
        if (files.isEmpty()) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WSDL2JavaScriptMojo.java`
#### Snippet
```java
        List<String> list = wsdlOption.generateCommandLine(outputDirFile, basedir, wsdlURI, getLog()
            .isDebugEnabled());
        String[] args = list.toArray(new String[0]);
        getLog().debug("Calling wsdl2js with args: " + Arrays.toString(args));

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WSDL2JavaMojo.java`
#### Snippet
```java
            list.add(1, encoding);
        }
        String[] args = list.toArray(new String[0]);
        getLog().debug("Calling wsdl2java with args: " + Arrays.toString(args));

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientRequestContextImpl.java`
#### Snippet
```java
    public Annotation[] getEntityAnnotations() {
        Annotation[] anns = (Annotation[])m.get(Annotation.class.getName());
        return anns == null ? new Annotation[] {} : anns;
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
            }
        }
        return results.toArray(new String[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
            }

            Annotation[] anns = customAnns != null ? customAnns : new Annotation[]{};
            boolean isAssignable = requestClass != null && requestClass.isAssignableFrom(body.getClass());
            try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java

            Object entity = readBody(currentResponse, outMessage, responseClass, genericType,
                                     new Annotation[]{});

            if (entity == null) {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                list.add(convertParamValue(pair.getValue(), pair.getAnns()));
            } else if (requestBody != null) {
                getBeanGetter(requestBody.getClass(), varName, new Class<?>[] {}).ifPresent(getter -> {
                    try {
                        list.add(getter.invoke(requestBody, new Object[] {}));
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                getBeanGetter(requestBody.getClass(), varName, new Class<?>[] {}).ifPresent(getter -> {
                    try {
                        list.add(getter.invoke(requestBody, new Object[] {}));
                    } catch (Exception ex) {
                        // continue
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java

    protected static Annotation[] getMethodAnnotations(Method aMethod, int bodyIndex) {
        return aMethod == null || bodyIndex == -1 ? new Annotation[0]
            : aMethod.getParameterAnnotations()[bodyIndex];
    }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                    boolean beanParam = m.getAnnotation(BeanParam.class) != null;
                    if (methodAnnotation != null || beanParam) {
                        getBeanGetter(bean.getClass(), propertyName, new Class<?>[] {}).
                                map(getter -> {
                                    try {
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                map(getter -> {
                                    try {
                                        return getter.invoke(bean, new Object[] {});
                                    } catch (Exception ex) {
                                        // ignore
```

## RuleId[ruleID=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `url` of exception class
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPException.java`
#### Snippet
```java
    private int responseCode;
    private String responseMessage;
    private URL url;

    public HTTPException(int responseCode, String responseMessage, URL url) {
```

### NonFinalFieldOfException
Non-final field `responseMessage` of exception class
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPException.java`
#### Snippet
```java
    private static final long serialVersionUID = -6186825695409511895L;
    private int responseCode;
    private String responseMessage;
    private URL url;

```

### NonFinalFieldOfException
Non-final field `responseCode` of exception class
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPException.java`
#### Snippet
```java
public class HTTPException extends IOException {
    private static final long serialVersionUID = -6186825695409511895L;
    private int responseCode;
    private String responseMessage;
    private URL url;
```

### NonFinalFieldOfException
Non-final field `name` of exception class
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PropertyNotFoundException.java`
#### Snippet
```java
public class PropertyNotFoundException extends SearchParseException {
    private static final long serialVersionUID = -3262559013167020058L;
    private String name;
    private String value;
    public PropertyNotFoundException(String name, String value) {
```

### NonFinalFieldOfException
Non-final field `value` of exception class
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PropertyNotFoundException.java`
#### Snippet
```java
    private static final long serialVersionUID = -3262559013167020058L;
    private String name;
    private String value;
    public PropertyNotFoundException(String name, String value) {
        this.name = name;
```

### NonFinalFieldOfException
Non-final field `value` of exception class
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/PropertyValidationException.java`
#### Snippet
```java
    private static final long serialVersionUID = -3262559013167020058L;
    private String name;
    private String value;
    public PropertyValidationException(String name, String value) {
        this.name = name;
```

### NonFinalFieldOfException
Non-final field `name` of exception class
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/PropertyValidationException.java`
#### Snippet
```java
public class PropertyValidationException extends RuntimeException {
    private static final long serialVersionUID = -3262559013167020058L;
    private String name;
    private String value;
    public PropertyValidationException(String name, String value) {
```

### NonFinalFieldOfException
Non-final field `statusCode` of exception class
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java
     * response http header status code
     */
    private int statusCode = DEFAULT_HTTP_RESPONSE_CODE;

    public Fault(Message message, Throwable throwable) {
```

### NonFinalFieldOfException
Non-final field `messageString` of exception class
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java

    private Element detail;
    private String messageString;
    private QName code;
    private String lang;
```

### NonFinalFieldOfException
Non-final field `detail` of exception class
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java
    private static final long serialVersionUID = -1583932965031558864L;

    private Element detail;
    private String messageString;
    private QName code;
```

### NonFinalFieldOfException
Non-final field `code` of exception class
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java
    private Element detail;
    private String messageString;
    private QName code;
    private String lang;
    /**
```

### NonFinalFieldOfException
Non-final field `lang` of exception class
in `core/src/main/java/org/apache/cxf/interceptor/Fault.java`
#### Snippet
```java
    private String messageString;
    private QName code;
    private String lang;
    /**
     * response http header status code
```

### NonFinalFieldOfException
Non-final field `faultCode` of exception class
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/STSException.java`
#### Snippet
```java


    private QName faultCode;

    public STSException(String message) {
```

### NonFinalFieldOfException
Non-final field `expectedContentId` of exception class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/MultipartReadException.java`
#### Snippet
```java
public class MultipartReadException extends IOException {
    private static final long serialVersionUID = 4147731360800379534L;
    private String expectedContentId;
    private String expectedContentType;
    private String errorMessage;
```

### NonFinalFieldOfException
Non-final field `expectedContentType` of exception class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/MultipartReadException.java`
#### Snippet
```java
    private static final long serialVersionUID = 4147731360800379534L;
    private String expectedContentId;
    private String expectedContentType;
    private String errorMessage;
    public MultipartReadException(String expectedContentId,
```

### NonFinalFieldOfException
Non-final field `errorMessage` of exception class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/multipart/MultipartReadException.java`
#### Snippet
```java
    private String expectedContentId;
    private String expectedContentType;
    private String errorMessage;
    public MultipartReadException(String expectedContentId,
                                  String expectedContentType,
```

### NonFinalFieldOfException
Non-final field `detail` of exception class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java
    private boolean sender;
    private QName faultCode;
    private Object detail;
    private Element extraDetail;

```

### NonFinalFieldOfException
Non-final field `extraDetail` of exception class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java
    private QName faultCode;
    private Object detail;
    private Element extraDetail;

    public SequenceFault(String message) {
```

### NonFinalFieldOfException
Non-final field `faultCode` of exception class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java
    private static final long serialVersionUID = 1144842362378723203L;
    private boolean sender;
    private QName faultCode;
    private Object detail;
    private Element extraDetail;
```

### NonFinalFieldOfException
Non-final field `sender` of exception class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java

    private static final long serialVersionUID = 1144842362378723203L;
    private boolean sender;
    private QName faultCode;
    private Object detail;
```

### NonFinalFieldOfException
Non-final field `error` of exception class
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServiceException.java`
#### Snippet
```java

    private static final long serialVersionUID = 343738539234766320L;
    private OAuthError error;

    public OAuthServiceException() {
```

### NonFinalFieldOfException
Non-final field `details` of exception class
in `testutils/src/main/java/org/apache/cxf/customer/CustomerNotFoundFault.java`
#### Snippet
```java
public class CustomerNotFoundFault extends Exception {
    private static final long serialVersionUID = -2104569359859457879L;
    private CustomerNotFoundDetails details;

    public CustomerNotFoundFault(CustomerNotFoundDetails details) {
```

### NonFinalFieldOfException
Non-final field `details` of exception class
in `testutils/src/main/java/org/apache/cxf/customer/book/BookNotFoundFault.java`
#### Snippet
```java
public class BookNotFoundFault extends Exception {
    private static final long serialVersionUID = 497738666759033678L;
    private BookNotFoundDetails details;

    public BookNotFoundFault(BookNotFoundDetails details) {
```

### NonFinalFieldOfException
Non-final field `resultMinor` of exception class
in `services/xkms/xkms-common/src/main/java/org/apache/cxf/xkms/exception/XKMSException.java`
#### Snippet
```java

    private ResultMajorEnum resultMajor;
    private ResultMinorEnum resultMinor;

    public XKMSException(ResultMajorEnum resultMajor, ResultMinorEnum resultMinor) {
```

### NonFinalFieldOfException
Non-final field `resultMajor` of exception class
in `services/xkms/xkms-common/src/main/java/org/apache/cxf/xkms/exception/XKMSException.java`
#### Snippet
```java
    private static final long serialVersionUID = 7247415453067157299L;

    private ResultMajorEnum resultMajor;
    private ResultMinorEnum resultMinor;

```

### NonFinalFieldOfException
Non-final field `detail` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java
    private JMSFaultType jmsFaultType;
    private boolean sender;
    private Object detail;

    public JMSFault(String message) {
```

### NonFinalFieldOfException
Non-final field `sender` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java
    private static final long serialVersionUID = 8567771013189842711L;
    private JMSFaultType jmsFaultType;
    private boolean sender;
    private Object detail;

```

### NonFinalFieldOfException
Non-final field `jmsFaultType` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java

    private static final long serialVersionUID = 8567771013189842711L;
    private JMSFaultType jmsFaultType;
    private boolean sender;
    private Object detail;
```

### NonFinalFieldOfException
Non-final field `role` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java

    private List<QName> subCodes;
    private String role;
    private String node;
    private Map<String, String> namespaces = new HashMap<>();
```

### NonFinalFieldOfException
Non-final field `node` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java
    private List<QName> subCodes;
    private String role;
    private String node;
    private Map<String, String> namespaces = new HashMap<>();

```

### NonFinalFieldOfException
Non-final field `namespaces` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java
    private String role;
    private String node;
    private Map<String, String> namespaces = new HashMap<>();

    public SoapFault(Message message, Throwable throwable, QName faultCode) {
```

### NonFinalFieldOfException
Non-final field `subCodes` of exception class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapFault.java`
#### Snippet
```java
     */

    private List<QName> subCodes;
    private String role;
    private String node;
```

## RuleId[ruleID=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `FaultInfoException` ends with 'Exception'
in `core/src/main/java/org/apache/cxf/interceptor/FaultOutInterceptor.java`
#### Snippet
```java
     * marshal into a fault detail element
     */
    public interface FaultInfoException {
    }

```

## RuleId[ruleID=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/bus/spring/BusExtensionPostProcessor.java`
#### Snippet
```java
    public Object postProcessBeforeInitialization(Object bean, String beanId) {
        if (bean instanceof BusExtension && null != getBus()) {
            Class<? extends Object> cls = ((BusExtension)bean).getRegistrationType();
            registerExt(bean, cls);
        } else if (bean instanceof Bus && Bus.DEFAULT_BUS_ID.equals(beanId)) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/common/annotation/AnnotationProcessor.java`
#### Snippet
```java
    }

    private void processFields(AnnotationVisitor visitor, Class<? extends Object> targetClass) {
        if (targetClass.getSuperclass() != null) {
            processFields(visitor, targetClass.getSuperclass());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/common/annotation/AnnotationProcessor.java`
#### Snippet
```java


    private void processClass(AnnotationVisitor visitor, Class<? extends Object> targetClass) {
        if (targetClass.getSuperclass() != null) {
            processClass(visitor, targetClass.getSuperclass());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/common/annotation/AnnotationProcessor.java`
#### Snippet
```java


    private void processMethods(AnnotationVisitor visitor, Class<? extends Object> targetClass) {

        if (targetClass.getSuperclass() != null) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/message/AbstractWrappedMessage.java`
#### Snippet
```java
    }

    public void putAll(Map<? extends String, ? extends Object> t) {
        message.putAll(t);
    }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
        }
        @Override
        public void putAll(Map<? extends String, ? extends Object> m) {
            wrapped.putAll(m);
        }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
     * @param providers the entity providers
     */
    public void setProviders(List<? extends Object> providers) {
        List<Object> newBeans = new ArrayList<>();
        addToBeans(newBeans, providers);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriInfoImpl.java`
#### Snippet
```java
    }

    private static String createMatchedPath(String uri, List<? extends Object> vars, boolean decode) {
        String uriPath = UriBuilder.fromPath(uri).buildFromEncoded(vars.toArray()).getRawPath();
        uriPath = decode ? HttpUtils.pathDecode(uriPath) : uriPath;
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
    //CHECKSTYLE:OFF
    private String substituteMapped(String path,
                                    Map<String, ? extends Object> varValueMap,
                                    Map<String, Object> alreadyResolvedTs,
                                    Map<String, Object> alreadyResolvedTsPathEnc,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java

        Map<String, String> decodedMap = new HashMap<>(map.size());
        for (Map.Entry<String, ? extends Object> entry : map.entrySet()) {
            if (entry.getValue() == null) {
                throw new IllegalArgumentException("Value is null");
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
    }

    private URI doBuildFromMap(Map<String, ? extends Object> map, boolean fromEncoded,
                               boolean encodePathSlash)
        throws IllegalArgumentException, UriBuilderException {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
     * @return template with bound variables.
     */
    public String substitute(Map<String, ? extends Object> valuesMap,
                             Set<String> encodePathSlashVars,
                             boolean allowUnresolved) throws IllegalArgumentException {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
    }

    String substitute(Map<String, ? extends Object> valuesMap) throws IllegalArgumentException {
        return this.substitute(valuesMap, Collections.<String>emptySet(), false);
    }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/JAXRSServerFactoryBeanDefinitionParser.java`
#### Snippet
```java
        }
        @Override
        public void setProviders(List<? extends Object> beans) {
            super.setProviders(beans);
            this.providerBeansAvailable = true;
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/spring/AbstractSpringComponentScanServer.java`
#### Snippet
```java
        
    }
    private static void warnIfDuplicatesAvailable(List<? extends Object> providers) {
        Set<String> classNames = new HashSet<>();
        for (Object o : providers) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
        boolean isApplication = Application.class.isAssignableFrom(c.getDeclaringClass());
        try {
            final ProviderInfo<? extends Object> provider;
            if (c.getParameterTypes().length == 0) {
                if (isApplication) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/FormValidator.java`
#### Snippet
```java

public interface FormValidator {
    void validate(MultivaluedMap<String, ? extends Object> params);
}

```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
            new LinkedList<>();

        List<ProviderInfo<? extends Object>> theProviders =
            prepareProviders(custom, busGlobal, allProviders.toArray(), application);
        super.setCommonProviders(theProviders, RuntimeType.SERVER);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
            prepareProviders(custom, busGlobal, allProviders.toArray(), application);
        super.setCommonProviders(theProviders, RuntimeType.SERVER);
        for (ProviderInfo<? extends Object> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(getBus(), provider.getProvider());

```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    public static ProviderInfo<? extends Object> createProviderFromConstructor(Constructor<?> c,
                                                                 Map<Class<?>, Object> values,
                                                                 Bus theBus,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected void setCommonProviders(List<ProviderInfo<? extends Object>> theProviders, RuntimeType type) {
        List<ProviderInfo<ReaderInterceptor>> readInts =
            new LinkedList<>();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        List<ProviderInfo<WriterInterceptor>> writeInts =
            new LinkedList<>();
        for (ProviderInfo<? extends Object> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(bus, provider.getProvider());

```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
    }

    protected List<ProviderInfo<? extends Object>> prepareProviders(boolean custom,
                                                                    boolean busGlobal,
                                                                    Object[] providers,
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                                                                    Object[] providers,
                                                                    ProviderInfo<Application> application) {
        List<ProviderInfo<? extends Object>> theProviders =
            new ArrayList<>(providers.length);
        for (Object o : providers) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JoseJaxrsUtils.java`
#### Snippet
```java
        }
    }
    private static String getJoseHeaderValue(List<? extends Object> headerValues) {
        StringBuilder sb = new StringBuilder();
        for (Object o : headerValues) {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
    }

    public final void putAll(Map<? extends String, ? extends Object> t) {
        for (Map.Entry<? extends String, ? extends Object> s : t.entrySet()) {
            put(s.getKey(), s.getValue());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java

    public final void putAll(Map<? extends String, ? extends Object> t) {
        for (Map.Entry<? extends String, ? extends Object> s : t.entrySet()) {
            put(s.getKey(), s.getValue());
        }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/WrappedService.java`
#### Snippet
```java
    }

    public void putAll(Map<? extends String, ? extends Object> t) {
        wrappedService.putAll(t);
    }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/WrappedEndpoint.java`
#### Snippet
```java
    }

    public void putAll(Map<? extends String, ? extends Object> t) {
        wrappedEndpoint.putAll(t);
    }
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
                    && pt.getActualTypeArguments()[0] instanceof Class) {

                    Class<? extends Object> arrayCls =
                        Array.newInstance((Class<?>) pt.getActualTypeArguments()[0], 0).getClass();
                    clazz = arrayCls;
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
                    GenericArrayType gat = (GenericArrayType)pt.getActualTypeArguments()[0];
                    gat.getGenericComponentType();
                    Class<? extends Object> arrayCls =
                        Array.newInstance((Class<?>) gat.getGenericComponentType(), 0).getClass();
                    clazz = Array.newInstance(arrayCls, 0).getClass();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
    private String getJAASPrincipal() {
        StringBuilder principals = new StringBuilder();
        Iterator<? extends Object> principalIt = getJAASPrincipals();
        while (principalIt.hasNext()) {
            principals.append(principalIt.next());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
    }

    private Iterator<? extends Object> getJAASPrincipals() {
        Subject subject = Subject.getSubject(AccessController.getContext());
        return subject != null && subject.getPrincipals() != null
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
    @Override
    protected void setProviders(boolean custom, boolean busGlobal, Object... providers) {
        List<ProviderInfo<? extends Object>> theProviders =
            prepareProviders(custom, busGlobal, providers, null);
        super.setCommonProviders(theProviders, RuntimeType.CLIENT);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
            prepareProviders(custom, busGlobal, providers, null);
        super.setCommonProviders(theProviders, RuntimeType.CLIENT);
        for (ProviderInfo<? extends Object> provider : theProviders) {
            Class<?> providerCls = ClassHelper.getRealClass(getBus(), provider.getProvider());
            if (providerCls == Object.class) {
```

## RuleId[ruleID=ConstantValue]
### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/server/ReactiveIOInvoker.java`
#### Snippet
```java
        final AsyncResponseImpl asyncResponse = new AsyncResponseImpl(inMessage);
        Disposable d = single.subscribe(asyncResponse::resume, t -> handleThrowable(asyncResponse, t));
        if (d == null) {
            throw new IllegalStateException("Subscribe did not return a Disposable");
        }
```

### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/server/ReactiveIOInvoker.java`
#### Snippet
```java
        if (!isStreamingSubscriberUsed(f, asyncResponse, inMessage)) {
            Disposable d = subscribe(f, asyncResponse);
            if (d == null) {
                throw new IllegalStateException("Subscribe did not return a Disposable");
            }
```

### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx2/src/main/java/org/apache/cxf/jaxrs/rx2/server/ReactiveIOInvoker.java`
#### Snippet
```java
        final AsyncResponseImpl asyncResponse = new AsyncResponseImpl(inMessage);
        Disposable d = subscribe(obs, asyncResponse);
        if (d == null) {
            throw new IllegalStateException("Subscribe did not return a Disposable");
        }
```

### ConstantValue
Condition `certs.length > 0` is always `true`
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProvider.java`
#### Snippet
```java
        XKMSCacheToken cachedToken = null;
        // Try local cache first
        if (certs != null && certs.length > 0 && xkmsClientCache != null) {
            String key = certs[0].getSubjectX500Principal().getName();
            // Try by Subject DN and IssuerSerial
```

### ConstantValue
Condition `o instanceof ResolveMatchesType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                            o = ((JAXBElement)o).getValue();
                        }
                        if (o instanceof ResolveMatchesType) {
                            response.value = (ResolveMatchesType)o;
                        } else if (o instanceof HelloType) {
```

### ConstantValue
Condition `o instanceof HelloType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                        if (o instanceof ResolveMatchesType) {
                            response.value = (ResolveMatchesType)o;
                        } else if (o instanceof HelloType) {
                            HelloType h = (HelloType)o;
                            QName sn = version.getServiceName();
```

### ConstantValue
Condition `o instanceof ProbeMatchesType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                            o = ((JAXBElement)o).getValue();
                        }
                        if (o instanceof ProbeMatchesType) {
                            response.getProbeMatch().addAll(((ProbeMatchesType)o).getProbeMatch());
                        } else if (o instanceof HelloType) {
```

### ConstantValue
Condition `o instanceof HelloType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/WSDiscoveryClient.java`
#### Snippet
```java
                        if (o instanceof ProbeMatchesType) {
                            response.getProbeMatch().addAll(((ProbeMatchesType)o).getProbeMatch());
                        } else if (o instanceof HelloType) {
                            HelloType h = (HelloType)o;
                            QName sn = version.getServiceName();
```

### ConstantValue
Condition `obj instanceof ResolveType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                        ret = new JAXBSource(context, factory.createProbeMatches(pmt));
                    }
                } else if (obj instanceof ResolveType) {
                    ResolveMatchesType rmt = handleResolve((ResolveType)obj);
                    if (rmt == null) {
```

### ConstantValue
Condition `obj instanceof HelloType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                        ret = new JAXBSource(context, factory.createResolveMatches(rmt));
                    }
                } else if (obj instanceof HelloType) {
                    //check if it's a DiscoveryProxy
                    HelloType h = (HelloType)obj;
```

### ConstantValue
Condition `obj instanceof ByeType` is always `false`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                        }
                    }
                } else if (obj instanceof ByeType) {
                    ByeType h = (ByeType)obj;
                    if (h.getTypes().contains(WSDiscoveryClient.SERVICE_QNAME)
```

### ConstantValue
Value `ret` is always 'null'
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                e1.printStackTrace();
            }
            return ret;
        }

```

### ConstantValue
Condition `hostStr != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### ConstantValue
Condition `continuationsStr != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String continuationsStr = element.getAttribute("continuationsEnabled");
        if (continuationsStr != null && continuationsStr.length() > 0) {
            bean.addPropertyValue("continuationsEnabled", continuationsStr);
        }
```

### ConstantValue
Condition `maxIdleTimeStr != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxIdleTimeStr = element.getAttribute("maxIdleTime");
        if (maxIdleTimeStr != null && !"".equals(maxIdleTimeStr.trim())) {
            bean.addPropertyValue("maxIdleTime", maxIdleTimeStr);
        }
```

### ConstantValue
Condition `sendServerVersionStr != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String sendServerVersionStr = element.getAttribute("sendServerVersion");
        if (sendServerVersionStr != null && sendServerVersionStr.length() > 0) {
            bean.addPropertyValue("sendServerVersion", sendServerVersionStr);
        }
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### ConstantValue
Condition `compatible` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
        boolean compatible = true;

        if (compatible) {
            compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl()
                          || p1.getCacheControl().equals(p2.getCacheControl());
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl()
                          || p1.getCacheControl().equals(p2.getCacheControl());
        }
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentEncoding(), p2.getContentEncoding());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentLocation(), p2.getContentLocation());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getContentType(), p2.getContentType());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getRedirectURL(), p2.getRedirectURL());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getServerType(), p2.getServerType());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isHonorKeepAlive() == p2.isHonorKeepAlive();
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isSuppressClientReceiveErrors() == p2.isSuppressClientReceiveErrors();
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= p1.isSuppressClientSendErrors() == p2.isSuppressClientSendErrors();
        }
        if (compatible) {
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
        }
        if (compatible) {
            compatible &= StringUtils.compatible(p1.getKeepAliveParameters(), p2.getKeepAliveParameters());
        }

```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
        boolean result = true;

        result &= (p1.isHonorKeepAlive() == p2.isHonorKeepAlive())
                  && (p1.getCacheControl() == null ? p2.getCacheControl() == null : p1.getCacheControl()
                      .equals(p2.getCacheControl()))
```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ServerPolicyCalculator.java`
#### Snippet
```java
            return false;
        }
        result &= (p1.getReceiveTimeout() == p2.getReceiveTimeout())
                  && StringUtils.equals(p1.getRedirectURL(), p2.getRedirectURL())
                  && StringUtils.equals(p1.getServerType(), p2.getServerType())
```

### ConstantValue
Condition `compatible` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        boolean compatible = true;

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAccept(), p2.getAccept());
        }
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAccept(), p2.getAccept());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAcceptEncoding(), p2.getAcceptEncoding());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getAcceptLanguage(), p2.getAcceptLanguage());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getBrowserType(), p2.getBrowserType());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetCacheControl() || !p2.isSetCacheControl()
                          || p1.getCacheControl().equals(p2.getCacheControl());
        }
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getCookie(), p2.getCookie());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getDecoupledEndpoint(), p2.getDecoupledEndpoint());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getHost(), p2.getHost());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getProxyServer(), p2.getProxyServer());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetProxyServerPort() || !p2.isSetProxyServerPort()
                          || p1.getProxyServerPort().equals(p2.getProxyServerPort());
        }
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetProxyServerType() || !p2.isSetProxyServerType()
                          || p1.getProxyServerType().equals(p2.getProxyServerType());
        }
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= StringUtils.compatible(p1.getReferer(), p2.getReferer());
        }

```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetAllowChunking()
                || !p2.isSetAllowChunking()
                || p1.isAllowChunking() == p2.isAllowChunking();
```

### ConstantValue
Condition `compatible` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java

        if (compatible) {
            compatible &= !p1.isSetAutoRedirect()
                || !p2.isSetAutoRedirect()
                || p1.isAutoRedirect() == p2.isAutoRedirect();
```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }
        boolean result = true;
        result &= (p1.isAllowChunking() == p2.isAllowChunking())
                  && (p1.isAutoRedirect() == p2.isAutoRedirect())
                  && StringUtils.equals(p1.getAccept(), p2.getAccept())
```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }

        result &= (p1.getCacheControl() == null ? p2.getCacheControl() == null : p1.getCacheControl()
            .equals(p2.getCacheControl()) && p1.getConnection().value().equals(p2.getConnection().value()))
                  && (p1.getConnectionTimeout() == p2.getConnectionTimeout())
```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/impl/ClientPolicyCalculator.java`
#### Snippet
```java
        }

        result &= StringUtils.equals(p1.getProxyServer(), p2.getProxyServer())
                  && (p1.isSetProxyServerPort() ? p1.getProxyServerPort().equals(p2.getProxyServerPort()) : !p2
                      .isSetProxyServerPort())
```

### ConstantValue
Condition `classProperty != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java

        String classProperty = element.getAttribute("class");
        if (classProperty != null && !classProperty.isEmpty()) {
            try {
                Object obj =
```

### ConstantValue
Condition `beanref != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java
        }
        String beanref = element.getAttribute("bean");
        if (beanref != null && !beanref.isEmpty()) {
            if (classProperty != null && !classProperty.isEmpty()) {
                throw new IllegalArgumentException(
```

### ConstantValue
Condition `classProperty != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java
        String beanref = element.getAttribute("bean");
        if (beanref != null && !beanref.isEmpty()) {
            if (classProperty != null && !classProperty.isEmpty()) {
                throw new IllegalArgumentException(
                        "Element '" + elementName + "' cannot have both "
```

### ConstantValue
Condition `classProperty == null` is always `false`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java
            }
            bean.addPropertyReference(elementName, beanref);
        } else if (classProperty == null || classProperty.isEmpty()) {
            throw new IllegalArgumentException(
                    "Element '" + elementName
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/spring/HttpConduitBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### ConstantValue
Condition `!contentTypeSet` is always `true` when reached
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
            }
            if (!dropContentType) {
                String ct = emptyRequest && !contentTypeSet ? "*/*" : determineContentType();
                connection.setRequestProperty(HttpHeaderHelper.CONTENT_TYPE, ct);
            }
```

### ConstantValue
Value `contentTypeSet` is always 'false'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
            }
            if (!dropContentType) {
                String ct = emptyRequest && !contentTypeSet ? "*/*" : determineContentType();
                connection.setRequestProperty(HttpHeaderHelper.CONTENT_TYPE, ct);
            }
```

### ConstantValue
Value `socket` is always 'null'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/SSLSocketFactoryWrapper.java`
#### Snippet
```java
                         "PROBLEM_CREATING_OUTBOUND_REQUEST_SOCKET",
                         logParams);
            return socket;
        }

```

### ConstantValue
Condition `obj.getClass() == String.class` is always `true` when reached
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                Object obj = objs.get(0);
                return obj.getClass() != String.class
                    || (obj.getClass() == String.class && ((String)obj).length() > 0);
            }
        }
```

### ConstantValue
Condition `names == null` is always `false`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java
            ctx2.setParent(ctx);
            String[] names = ctx.getBeanNamesForType(Bus.class);
            if (names == null || names.length == 0) {
                ctx2.setConfigLocations(new String[] {"classpath:/META-INF/cxf/cxf.xml",
                                                      location});
```

### ConstantValue
Value `hostname` is always 'null'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java
    static String normaliseAddress(final String hostname) {
        if (hostname == null) {
            return hostname;
        }
        try {
```

### ConstantValue
Condition `subjectAlts != null` is always `true`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java
        final HostNameType hostType = determineHostFormat(host);
        final List<SubjectName> subjectAlts = getSubjectAltNames(cert);
        if (subjectAlts != null && !subjectAlts.isEmpty()) {
            switch (hostType) {
            case IPv4:
```

### ConstantValue
Value `originalHost` is always 'null'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
            // be returned (http://httpd.apache.org/docs/2.2/mod/mod_proxy.html). We don't need
            // more than 2 components.
            String outermostHost = originalHost != null ? (originalHost.split(",", 2)[0]).trim() : originalHost;
            newRequestUrl = calculateNewRequestUrl(request, 
                                                   originalProto, 
```

### ConstantValue
Condition `providers != null` is always `true`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/CxfTypeSafeClientBuilder.java`
#### Snippet
```java
        RegisterProvider[] providers = aClass.getAnnotationsByType(RegisterProvider.class);
        Configuration config = configImpl.getConfiguration();
        if (providers != null) {
            for (RegisterProvider provider : providers) {
                if (!config.isRegistered(provider.value())) {
```

### ConstantValue
Condition `urls != null` is always `true`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/RestClientBean.java`
#### Snippet
```java
            // in = ClassLoaderUtils.getResourceAsStream(location, clientInterface);
            List<URL> urls = ClassLoaderUtils.getResources(location, clientInterface);
            if (urls != null && !urls.isEmpty()) {
                try {
                    return urls.get(0).openStream();
```

### ConstantValue
Condition `list != null` is always `true`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecInHandler.java`
#### Snippet
```java
    protected Element getNode(Element parent, String ns, String name, int index) {
        NodeList list = parent.getElementsByTagNameNS(ns, name);
        if (list != null && list.getLength() >= index + 1) {
            return (Element)list.item(index);
        }
```

### ConstantValue
Condition `certConstraintsList != null` is always `true`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
        if (certConstraints != null) {
            String[] certConstraintsList = certConstraints.split(",");
            if (certConstraintsList != null) {
                subjectDNPatterns.clear();
                for (String certConstraint : certConstraintsList) {
```

### ConstantValue
Condition `certConstraintsList != null` is always `true`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
        if (certConstraints != null) {
            String[] certConstraintsList = certConstraints.split(",");
            if (certConstraintsList != null) {
                subjectDNPatterns.clear();
                for (String certConstraint : certConstraintsList) {
```

### ConstantValue
Condition `list != null` is always `true`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedInHandler.java`
#### Snippet
```java
    protected Element getNode(Element parent, String ns, String name) {
        NodeList list = parent.getElementsByTagNameNS(ns, name);
        if (list != null && list.getLength() == 1) {
            return (Element)list.item(0);
        }
```

### ConstantValue
Condition `list != null` is always `true`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
    private Element getNode(Element parent, String ns, String name, int index) {
        NodeList list = parent.getElementsByTagNameNS(ns, name);
        if (list != null && list.getLength() >= index + 1) {
            return (Element)list.item(index);
        }
```

### ConstantValue
Value `sc` is always 'null'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/ServletConfigProvider.java`
#### Snippet
```java
        }

        return sc;
    }
}
```

### ConstantValue
Condition `m.getExceptionTypes() != null` is always `true`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
                } else if (CompletionStage.class.isAssignableFrom(m.getReturnType())) {
                    throw new CompletionException(t);
                } else if (m.getExceptionTypes() != null) {
                    // its a checked exception, make sure its declared
                    for (Class<?> c : m.getExceptionTypes()) {
```

### ConstantValue
Condition `!min.inclusive()` is always `true`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
                schema.setMinimum(BigDecimal.valueOf(Double.valueOf(min.value())));
            } else {
                schema.setExclusiveMinimum(!min.inclusive());
            }
        }
```

### ConstantValue
Result of `min.inclusive()` is always 'false'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
                schema.setMinimum(BigDecimal.valueOf(Double.valueOf(min.value())));
            } else {
                schema.setExclusiveMinimum(!min.inclusive());
            }
        }
```

### ConstantValue
Condition `!max.inclusive()` is always `true`
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
                schema.setMaximum(BigDecimal.valueOf(Double.valueOf(max.value())));
            } else {
                schema.setExclusiveMaximum(!max.inclusive());
            }
        }
```

### ConstantValue
Result of `max.inclusive()` is always 'false'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
                schema.setMaximum(BigDecimal.valueOf(Double.valueOf(max.value())));
            } else {
                schema.setExclusiveMaximum(!max.inclusive());
            }
        }
```

### ConstantValue
Value `configuration` is always 'null'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiCustomizer.java`
#### Snippet
```java
    public OpenAPIConfiguration customize(final OpenAPIConfiguration configuration) {
        if (configuration == null) {
            return configuration;
        }

```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### ConstantValue
Condition `hostStr != null` is always `true`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### ConstantValue
Condition `continuationsStr != null` is always `true`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String continuationsStr = element.getAttribute("continuationsEnabled");
        if (continuationsStr != null && continuationsStr.length() > 0) {
            bean.addPropertyValue("continuationsEnabled", continuationsStr);
        }
```

### ConstantValue
Condition `maxIdleTimeStr != null` is always `true`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxIdleTimeStr = element.getAttribute("maxIdleTime");
        if (maxIdleTimeStr != null && !"".equals(maxIdleTimeStr.trim())) {
            bean.addPropertyValue("maxIdleTime", maxIdleTimeStr);
        }
```

### ConstantValue
Condition `ends` is always `true`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
            if (starts && !ends) {
                return lval.endsWith(rval);
            } else if (ends && !starts) {
                return lval.startsWith(rval);
            } else {
```

### ConstantValue
Condition `hasComponents` at the left side of assignment expression is always `false`. Can be simplified
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            if (securityDefinitions != null && !securityDefinitions.isEmpty()) {
                securityDefinitions.forEach(components::addSecuritySchemes);
                hasComponents |= true;
            }

```

### ConstantValue
Condition `operator == BinaryOperator.OR` is always `true`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/odata/ODataParser.java`
#### Snippet
```java
                    if (operator == BinaryOperator.AND) {
                        return new AndSearchCondition< T >(conditions);
                    } else if (operator == BinaryOperator.OR) {
                        return new OrSearchCondition< T >(conditions);
                    }
```

### ConstantValue
Condition `origAzp != null` is always `true`
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcInvoker.java`
#### Snippet
```java
            if (newAzp != null && origAzp == null
                || newAzp == null && origAzp != null
                || newAzp != null && origAzp != null && !newAzp.equals(origAzp)) {
                throw new OAuthServiceException("Invalid id token authorized party");
            }
```

### ConstantValue
Value `minInclusive` is always 'false'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java

            if (type == ConditionType.LESS_THAN || type == ConditionType.LESS_OR_EQUALS) {
                return TermRangeQuery.newStringRange(name, null, luceneDateValue, minInclusive, maxInclusive);
            }
            return TermRangeQuery.newStringRange(name, luceneDateValue,
```

### ConstantValue
Condition `headerOriginValues == null` is always `false`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // 5.2.1 -- must have origin, must have one origin.
        List<String> headerOriginValues = getHeaderValues(CorsHeaderConstants.HEADER_ORIGIN, true);
        if (headerOriginValues == null || headerOriginValues.size() != 1) {
            return null;
        }
```

### ConstantValue
Condition `requestMethodValues == null` is always `false`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // we should reject parse errors but we cannot.
        List<String> requestMethodValues = getHeaderValues(CorsHeaderConstants.HEADER_AC_REQUEST_METHOD, false);
        if (requestMethodValues == null || requestMethodValues.size() != 1) {
            return createPreflightResponse(m, false);
        }
```

### ConstantValue
Condition `headerOriginValues == null` is always `false`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        List<String> headerOriginValues = getHeaderValues(CorsHeaderConstants.HEADER_ORIGIN, true);
        // 5.1.1 there has to be an origin
        if (headerOriginValues == null || headerOriginValues.isEmpty()) {
            return null;
        }
```

### ConstantValue
Value `sources` is always 'null'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/common/AbstractUserInfo.java`
#### Snippet
```java
            CastUtils.cast((Map<?, ?>)getProperty(OidcUtils.CLAIM_SOURCES_PROPERTY));
        if (sources == null) {
            setProperty(OidcUtils.CLAIM_SOURCES_PROPERTY, sources);
        } else {
            sources.putAll(newSources);
```

### ConstantValue
Condition `names != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
            if (resourceName == null) {
                String[] names = context.getBeanNamesForType(resourceType);
                if (names != null && names.length > 0) {
                    resource = resourceType.cast(context.getBean(names[0], resourceType));
                }
```

### ConstantValue
Condition `r != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
            } else if (URL.class.isAssignableFrom(resourceType)) {
                Resource r = context.getResource(resourceName);
                if (r != null && r.exists()) {
                    r.getInputStream().close(); //checks to see if the URL really can resolve
                    return resourceType.cast(r.getURL());
```

### ConstantValue
Condition `r != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
    public InputStream getAsStream(String name) {
        Resource r = context.getResource(name);
        if (r != null && r.exists()) {
            try {
                return r.getInputStream();
```

### ConstantValue
Condition `r != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
        }
        r = context.getResource("/" + name);
        if (r != null && r.exists()) {
            try {
                return r.getInputStream();
```

### ConstantValue
Condition `bean != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/Jsr250BeanPostProcessor.java`
#### Snippet
```java
        */

        if (bean != null
            && injectable(bean, beanId)) {
            new ResourceInjector(getResourceManager(bean)).inject(bean);
```

### ConstantValue
Condition `bean != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/Jsr250BeanPostProcessor.java`
#### Snippet
```java
            return;
        }
        if (bean != null
            && injectable(bean, beanId)) {
            new ResourceInjector(getResourceManager(bean)).destroy(bean);
```

### ConstantValue
Condition `bean != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/Jsr250BeanPostProcessor.java`
#### Snippet
```java
            return bean;
        }
        if (bean != null
            && injectable(bean, beanId)) {
            new ResourceInjector(getResourceManager(bean)).construct(bean);
```

### ConstantValue
Condition `c2 != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        for (Class<?> c : cls.getInterfaces()) {
            Class<?> c2 = findContextClass(c);
            if (c2 != null) {
                return c2;
            }
```

### ConstantValue
Condition `c2 != null` is always `true`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        }
        Class<?> c2 = findContextClass(cls.getSuperclass());
        if (c2 != null) {
            return c2;
        }
```

### ConstantValue
Condition `obj instanceof ServiceNameType` is always `false`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                        return ((ServiceNameType)val).getEndpointName();
                    }
                } else if (obj instanceof ServiceNameType) {
                    return ((ServiceNameType)obj).getEndpointName();
                }
```

### ConstantValue
Condition `obj instanceof AttributedQNameType` is always `false`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                    return ((AttributedQNameType)val).getValue();
                }
            } else if (obj instanceof AttributedQNameType) {
                return ((AttributedQNameType)obj).getValue();
            }
```

### ConstantValue
Condition `obj instanceof ServiceNameType` is always `false`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                    return ((ServiceNameType)val).getValue();
                }
            } else if (obj instanceof ServiceNameType) {
                return ((ServiceNameType)obj).getValue();
            }
```

### ConstantValue
Condition `obj instanceof ServiceNameType` is always `false`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
                        ((ServiceNameType)val).setEndpointName(portName);
                    }
                } else if (obj instanceof ServiceNameType) {
                    ((ServiceNameType)obj).setEndpointName(portName);
                }
```

### ConstantValue
Condition `args != null` is always `true` when reached
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
                    obj = con.newInstance(b);
                    return obj;
                } else if (b != null && args != null) {
                    try {
                        obj = cls.getConstructor(Bus.class, Object[].class).newInstance(b, args);
```

### ConstantValue
Condition `resolver == null` is always `true`
in `core/src/main/java/org/apache/cxf/common/util/SpringClasspathScanner.java`
#### Snippet
```java
            resolver = SpringOsgiUtil.getResolver(loader);
        }*/
        if (resolver == null) {
            resolver = loader != null
                ? new PathMatchingResourcePatternResolver(loader) : new PathMatchingResourcePatternResolver();
```

### ConstantValue
Condition `super.add(element)` is always `true`
in `core/src/main/java/org/apache/cxf/common/util/ModCountCopyOnWriteArrayList.java`
#### Snippet
```java
    @Override
    public boolean add(T element) {
        if (super.add(element)) {
            modCount.incrementAndGet();
            return true;
```

### ConstantValue
Condition `targetClass != null` is always `true`
in `core/src/main/java/org/apache/cxf/common/util/SpringClassUnwrapper.java`
#### Snippet
```java
                if (target == null) {
                    Class<?> targetClass = AopUtils.getTargetClass(o);
                    if (targetClass != null) {
                        return getRealClassFromClass(targetClass);
                    }
```

### ConstantValue
Value `value` is always 'null'
in `core/src/main/java/org/apache/cxf/common/util/XmlSchemaPrimitiveUtils.java`
#### Snippet
```java
    public static String getSchemaRepresentation(Class<?> type, String xsdPrefix) {
        String value = XML_SCHEMA_PRIMITIVE_MAP.get(type);
        return value == null ? value : xsdPrefix + ":" + value;
    }

```

### ConstantValue
Value `isJson` is always 'false'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                    if (isFormParameter(params.get(i), getMethod(ori).getParameterTypes()[i], getMethod(ori)
                        .getParameterAnnotations()[i])) {
                        writeParam(sb, params.get(i), ori, isJson);
                    }
                }
```

### ConstantValue
Condition `tnsDecl != null` is always `true`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                    String tnsDecl =
                        doc.getDocumentElement().getAttribute("xmlns:tns");
                    tnsPrefix = tnsDecl != null && tnsDecl.equals(tns) ? "tns:" : "";
                } else {
                    tnsPrefix += ":";
```

### ConstantValue
Condition `path.length() > 0` is always `true`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    protected String getPath(String path) {
        String thePath;
        if (ignoreForwardSlash && path.startsWith("/") && path.length() > 0) {
            thePath = path.substring(1);
        } else {
```

### ConstantValue
Condition `!path.isEmpty()` is always `true` when reached
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (stylesheetReference != null || !docLocationMap.isEmpty()) {
                String path = ui.getPath(false);
                if (path.startsWith("/") && !path.isEmpty()) {
                    path = path.substring(1);
                }
```

### ConstantValue
Result of `path.isEmpty()` is always 'false'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (stylesheetReference != null || !docLocationMap.isEmpty()) {
                String path = ui.getPath(false);
                if (path.startsWith("/") && !path.isEmpty()) {
                    path = path.substring(1);
                }
```

### ConstantValue
Condition `sub2` is always `true` when reached
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                URITemplate ut2 = op2.getURITemplate();
                int result = ut1.getValue().compareTo(ut2.getValue());
                if (result == 0 && !(sub1 && sub2)) {
                    result = op1.getHttpMethod().compareTo(op2.getHttpMethod());
                }
```

### ConstantValue
Value `hackAroundEmptyNamespaceIssue` is always 'true'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (DOMUtils.getFirstElement(d.getDocumentElement()) == null) {
                hackAroundEmptyNamespaceIssue = true;
                return hackAroundEmptyNamespaceIssue;
            }
            // create a copy of the dom so we
```

### ConstantValue
Condition `annotation instanceof Resources` is always `true`
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
        if (annotation instanceof Resource) {
            injectResourceClassLevel((Resource)annotation);
        } else if (annotation instanceof Resources) {
            Resources resources = (Resources)annotation;
            for (Resource resource : resources.value()) {
```

### ConstantValue
Value `rec` is always 'true'
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
            for (File file : files) {
                if (file.isDirectory() && rec) {
                    getFilesRecurseUsingSuffix(file, suffix, rec, fileList);
                } else {
                    if (file.getName().endsWith(suffix) && file.getName().length() > suffixLength) {
```

### ConstantValue
Condition `name != null` is always `true` when reached
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            String name = node.getNodeName();
            if (ns.equals(node.getNodeValue())
                && (name != null && (XMLNAMESPACE.equals(name) || name.startsWith(XMLNAMESPACE + ":")))) {
                return node.getLocalName();
            }
```

### ConstantValue
Condition `name != null` is always `true` when reached
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
            String name = node.getNodeName();
            if (namespaceUri.equals(node.getNodeValue())
                && (name != null && (XMLNAMESPACE.equals(name) || name.startsWith(XMLNAMESPACE + ":")))) {
                prefixes.add(node.getPrefix());
            }
```

### ConstantValue
Value `mpi` is always 'null'
in `core/src/main/java/org/apache/cxf/service/model/AbstractMessageContainer.java`
#### Snippet
```java
            }
        }
        return mpi;
    }

```

### ConstantValue
Condition `legalIdentifier` at the left side of assignment expression is always `true`. Can be simplified
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

        for (int i = 1; i < name.length() && legalIdentifier; i++) {
            legalIdentifier &= Character.isJavaIdentifierPart(buf.charAt(i));
            hasUnderscore |= '_' == buf.charAt(i);
        }
```

### ConstantValue
Condition `old != null` is always `true`
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
                }
            } else {
                if (old != null) {
                    final Logger logger = getLogger();
                    if (logger.isLoggable(Level.FINE)) {
```

### ConstantValue
Value `observer` is always 'null'
in `core/src/main/java/org/apache/cxf/transport/AbstractObservable.java`
#### Snippet
```java
                    deactivate();
                }
                incomingObserver = observer;
            }
        }
```

### ConstantValue
Condition `"".equals(path)` is always `false` when reached
in `core/src/main/java/org/apache/cxf/transport/HttpUriMapper.java`
#### Snippet
```java
            servletMap = path.substring(idx);
        }
        if ("".equals(servletMap) || "".equals(path)) {
            servletMap = "/";
        }
```

### ConstantValue
Condition `xGzipEnabled` is always `true` when reached
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                        permitted = UseGzip.YES;
                        message.put(GZIP_ENCODING_KEY, "gzip");
                    } else if (identityEnabled && xGzipEnabled) {
                        permitted = UseGzip.YES;
                        message.put(GZIP_ENCODING_KEY, "x-gzip");
```

### ConstantValue
Condition `!identityEnabled` is always `true`
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                        permitted = UseGzip.YES;
                        message.put(GZIP_ENCODING_KEY, "x-gzip");
                    } else if (!identityEnabled && gzipEnabled) {
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "gzip");
```

### ConstantValue
Value `identityEnabled` is always 'false'
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                        permitted = UseGzip.YES;
                        message.put(GZIP_ENCODING_KEY, "x-gzip");
                    } else if (!identityEnabled && gzipEnabled) {
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "gzip");
```

### ConstantValue
Condition `!identityEnabled` is always `true`
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "gzip");
                    } else if (!identityEnabled && xGzipEnabled) {
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "x-gzip");
```

### ConstantValue
Value `identityEnabled` is always 'false'
in `core/src/main/java/org/apache/cxf/transport/common/gzip/GZIPOutInterceptor.java`
#### Snippet
```java
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "gzip");
                    } else if (!identityEnabled && xGzipEnabled) {
                        permitted = UseGzip.FORCE;
                        message.put(GZIP_ENCODING_KEY, "x-gzip");
```

### ConstantValue
Condition `task != null` is always `true`
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                while (!shutdown.get()) {
                    task = delayQueue.take();
                    if (task != null) {
                        try {
                            execute(task);
```

### ConstantValue
Result of `attachments.isEmpty()` is always 'false'
in `core/src/main/java/org/apache/cxf/attachment/LazyAttachmentCollection.java`
#### Snippet
```java
            return !iterator().hasNext();
        }
        return attachments.isEmpty();
    }

```

### ConstantValue
Condition `localName == null` is always `false`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            localName = e.getNodeName();

            if (localName == null) {
                throw new IllegalStateException("Element's local name cannot be null!");
            }
```

### ConstantValue
Value `isXop` is always 'true'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            att.setHeader("Content-Disposition", "attachment;name=\"" + file + "\"");
        }
        att.setXOP(isXop);
        return att;
    }
```

### ConstantValue
Value `isXop` is always 'true'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        }
        AttachmentImpl att = new AttachmentImpl(id, handler);
        att.setXOP(isXop);
        return att;
    }
```

### ConstantValue
Condition `replaceContent` is always `true` when reached
in `core/src/main/java/org/apache/cxf/staxutils/transform/InTransformReader.java`
#### Snippet
```java
                // handle one of the four append modes
                handleAppendMode(expected, appendProp);
            } else if (null != appendProp && replaceContent) {
                replaceText = appendProp.getText();
                if (doDebug) {
```

### ConstantValue
Condition `selectedFaultInfoClass != null` is always `true` when reached
in `core/src/main/java/org/apache/cxf/interceptor/FaultOutInterceptor.java`
#### Snippet
```java
            Class<?> c = (Class<?>)faultInfo.getProperty(Class.class.getName());
            if (c != null && c.isAssignableFrom(class1) && (selectedFaultInfo == null
                || (selectedFaultInfoClass != null && selectedFaultInfoClass.isAssignableFrom(c)))) {
                selectedFaultInfo = faultInfo;
                selectedFaultInfoClass = c;
```

### ConstantValue
Condition `replaceContent` is always `true` when reached
in `core/src/main/java/org/apache/cxf/staxutils/transform/OutTransformWriter.java`
#### Snippet
```java
                }
            }
        } else if (null != appendProp && replaceContent) {
            //
            replaceText = appendProp.getText();
```

### ConstantValue
Condition `ps != null` is always `true`
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractSecurityContextInInterceptor.java`
#### Snippet
```java
    protected Principal getPrincipal(Principal originalPrincipal, Subject subject) {
        Principal[] ps = subject.getPrincipals().toArray(new Principal[subject.getPrincipals().size()]);
        if (ps != null && ps.length > 0 
            && !DefaultSecurityContext.isGroupPrincipal(ps[0])) {
            return ps[0];
```

### ConstantValue
Condition `cause != null` is always `true`
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
                    StackTraceElement[] stackTraceElement = new StackTraceElement[stackTraceList.size()];
                    e.setStackTrace(stackTraceList.toArray(stackTraceElement));
                } else if (cause != null
                    && cause.getMessage() != null
                    && cause.getMessage().startsWith(e.getClass().getName())) {
```

### ConstantValue
Condition `cause != null` is always `true`
in `core/src/main/java/org/apache/cxf/interceptor/ClientFaultConverter.java`
#### Snippet
```java
                        e.initCause(cause.getCause());
                    }
                } else if (cause != null) {
                    e.initCause(cause);
                }
```

### ConstantValue
Condition `!first` is always `false`
in `core/src/main/java/org/apache/cxf/interceptor/security/SimpleAuthorizingInterceptor.java`
#### Snippet
```java
        boolean first = true;
        for (Class<?> cls : method.getParameterTypes()) {
            if (!first) {
                b.append(", ");
                first = false;
```

### ConstantValue
Value `first` is always 'true'
in `core/src/main/java/org/apache/cxf/interceptor/security/SimpleAuthorizingInterceptor.java`
#### Snippet
```java
        boolean first = true;
        for (Class<?> cls : method.getParameterTypes()) {
            if (!first) {
                b.append(", ");
                first = false;
```

### ConstantValue
Condition `iparams != null` is always `true`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParametersConfig.java`
#### Snippet
```java
            ret.setCertAlias(params.getCertAlias());
        }
        if (iparams != null && iparams.isSetKeyManagersRef() && !usingDefaults) {
            ret.setKeyManagers(iparams.getKeyManagersRef());
        }
```

### ConstantValue
Condition `iparams != null` is always `true`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSClientParametersConfig.java`
#### Snippet
```java
            ret.setKeyManagers(iparams.getKeyManagersRef());
        }
        if (iparams != null && iparams.isSetTrustManagersRef() && !usingDefaults) {
            ret.setTrustManagers(iparams.getTrustManagersRef());
        }
```

### ConstantValue
Condition `createdFromAPI != null` is always `true`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
        }

        if (createdFromAPI != null && Boolean.parseBoolean(createdFromAPI)) {
            return id + getSuffix();
        }
```

### ConstantValue
Condition `id == null` is always `false`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
            if ("ref".equals(name)) {
                id = first.getAttribute("bean");
                if (id == null) {
                    throw new IllegalStateException("<ref> elements must have a \"bean\" attribute!");
                }
```

### ConstantValue
Condition `null == id` is always `false`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
        String id = elem.getAttribute(BeanDefinitionParserDelegate.ID_ATTRIBUTE);

        if (null == id || "".equals(id)) {
            String names = elem.getAttribute("name");
            if (null != names) {
```

### ConstantValue
Condition `null != names` is always `true`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
        if (null == id || "".equals(id)) {
            String names = elem.getAttribute("name");
            if (null != names) {
                StringTokenizer st =
                    new StringTokenizer(names, BeanDefinitionParserDelegate.MULTI_VALUE_ATTRIBUTE_DELIMITERS);
```

### ConstantValue
Condition `f instanceof QueryExpressionType` is always `false`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractSubscription.java`
#### Snippet
```java
                    }
                    topic = (TopicExpressionType) f;
                } else if (f instanceof QueryExpressionType) {
                    if (e != null && e.getName().equals(QNAME_PRODUCER_PROPERTIES)) {
                        InvalidProducerPropertiesExpressionFaultType fault =
```

### ConstantValue
Condition `location != null` is always `true`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
        String keyStoreType = getKeystoreType(null, log);

        if (location != null) {
            File file = new File(location);
            try (InputStream is = FileUtils.exists(file) ? Files.newInputStream(file.toPath())
```

### ConstantValue
Condition `hostStr != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### ConstantValue
Condition `readIdleTimeStr != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String readIdleTimeStr = element.getAttribute("readIdleTime");
        if (readIdleTimeStr != null && !"".equals(readIdleTimeStr.trim())) {
            bean.addPropertyValue("readIdleTime", readIdleTimeStr);
        }
```

### ConstantValue
Condition `writeIdleTimeStr != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String writeIdleTimeStr = element.getAttribute("writeIdleTime");
        if (writeIdleTimeStr != null && !"".equals(writeIdleTimeStr.trim())) {
            bean.addPropertyValue("writeIdleTime", writeIdleTimeStr);
        }
```

### ConstantValue
Condition `maxChunkContentSizeStr != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxChunkContentSizeStr = element.getAttribute("maxChunkContentSize");
        if (maxChunkContentSizeStr != null && !"".equals(maxChunkContentSizeStr.trim())) {
            bean.addPropertyValue("maxChunkContentSize", maxChunkContentSizeStr);
        }
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

            if ("keyManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("keyManagersRef", ref);
                } else {
```

### ConstantValue
Condition `ref != null` is always `true`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }
            } else if ("trustManagers".equals(ename)) {
                if (ref != null && ref.length() > 0) {
                    paramsbean.addPropertyReference("trustManagersRef", ref);
                } else {
```

### ConstantValue
Value `writeNullAsString` is always 'false'
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        }
        if (!writeNullAsString) {
            config.setWriteNullAsString(writeNullAsString);
        }
        boolean ignoreEmpty = getBooleanJsonProperty(IGNORE_EMPTY_JSON_ARRAY_VALUES_PROPERTY, ignoreEmptyArrayValues);
```

### ConstantValue
Value `ignoreEmpty` is always 'true'
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        boolean ignoreEmpty = getBooleanJsonProperty(IGNORE_EMPTY_JSON_ARRAY_VALUES_PROPERTY, ignoreEmptyArrayValues);
        if (ignoreEmpty) {
            config.setIgnoreEmptyArrayValues(ignoreEmpty);
        }

```

### ConstantValue
Value `escapeForwardSlashesAlways` is always 'true'
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java

        if (escapeForwardSlashesAlways) {
            config.setEscapeForwardSlashAlways(escapeForwardSlashesAlways);
        }

```

### ConstantValue
Condition `dropRootInJsonStream` is always `true`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
            if (theArrayKeys == null) {
                theArrayKeys = new LinkedList<>();
            } else if (dropRootInJsonStream) {
                theArrayKeys = new LinkedList<>(theArrayKeys);
            }
```

### ConstantValue
Condition `attrName == null` is always `false`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/FragmentDialect.java`
#### Snippet
```java
                            FragmentDialectConstants.FRAGMENT_ATTR_NODE_NAME_ATTR);
                    String attrValue = node.getTextContent();
                    if (attrName == null) {
                        throw new SoapFault("wsf:AttributeNode@name is not present.", getSoapVersion().getSender());
                    }
```

### ConstantValue
Condition `changed` at the left side of assignment expression is always `false`. Can be simplified
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        int i = ioThreadCount;
        ioThreadCount = getInt(s.get(THREAD_COUNT), Runtime.getRuntime().availableProcessors());
        changed |= i != ioThreadCount;

        long l = selectInterval;
```

### ConstantValue
Value `b` is always 'true'
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                return true;
            }
            return b;
        }

```

### ConstantValue
Condition `!useOnBehalfOf` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
                    returnedToken = c.requestSecurityToken();
                    c.setOnBehalfOf(null);
                } else if (useIssueBinding && !useOnBehalfOf && credential.getUsernametoken() != null) {
                    c.getProperties().put(SecurityConstants.USERNAME,
                                          credential.getUsernametoken().getName());
```

### ConstantValue
Value `useOnBehalfOf` is always 'false'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenValidator.java`
#### Snippet
```java
                    returnedToken = c.requestSecurityToken();
                    c.setOnBehalfOf(null);
                } else if (useIssueBinding && !useOnBehalfOf && credential.getUsernametoken() != null) {
                    c.getProperties().put(SecurityConstants.USERNAME,
                                          credential.getUsernametoken().getName());
```

### ConstantValue
Condition `mi2 != null` is always `true` when reached
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/ColocUtil.java`
#### Snippet
```java
        }

        if (mi1 != null && mi2 != null) {
            List<MessagePartInfo> mpil1 = mi1.getMessageParts();
            List<MessagePartInfo> mpil2 = mi2.getMessageParts();
```

### ConstantValue
Condition `fil2 != null` is always `true` when reached
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/ColocUtil.java`
#### Snippet
```java
        }

        if (fil1 != null && fil2 != null) {
            if (fil1.size() != fil2.size()) {
                return false;
```

### ConstantValue
Condition `changed` at the left side of assignment expression is always `false`. Can be simplified
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduitFactory.java`
#### Snippet
```java
        int i = ioThreadCount;
        ioThreadCount = getInt(s.get(THREAD_COUNT), Runtime.getRuntime().availableProcessors());
        changed |= i != ioThreadCount;

        long l = selectInterval;
```

### ConstantValue
Condition `key == null` is always `false`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSTokenRetriever.java`
#### Snippet
```java

                String key = appliesTo;
                if (!enableAppliesTo || key == null || key.isEmpty()) {
                    key = ASSOCIATED_TOKEN;
                }
```

### ConstantValue
Value `b` is always 'true'
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                return true;
            }
            return b;
        }

```

### ConstantValue
Condition `id == null` is always `false`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
        if ((id == null || "".equals(id)) && rst != null) {
            id = rst.getAttributeNS(WSS4JConstants.WSU_NS, "Id");
            if (id == null || "".equals(id)) {
                QName elName = DOMUtils.getElementQName(rst);
                if (elName.equals(new QName(WSS4JConstants.SAML2_NS, "EncryptedAssertion"))) {
```

### ConstantValue
Condition `epName != null` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            serviceName = sName;
            final QName epName = EndpointReferenceUtils.getPortQName(ref, bus);
            if (epName != null) {
                endpointName = epName;
            }
```

### ConstantValue
Value `attached` is always 'false'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
            // Add the BST to the security header if required
            if (!attached && isTokenRequired(sigToken.getIncludeTokenType())) {
                WSSecSignature sig = getSignatureBuilder(sigToken, attached, false);
                sig.appendBSTElementToHeader();
                sig.clean();
```

### ConstantValue
Condition `attributeStatements == null` is always `false`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
            return false;
        }
```

### ConstantValue
Condition `attribute.getAttributeValues() != null` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
                String desiredRole = attributeNamespace.relativize(claimURI).toString();
                if (attribute.getAttributeName().equals(desiredRole)
                    && attribute.getAttributeValues() != null && !attribute.getAttributeValues().isEmpty()) {
                    return true;
                }
```

### ConstantValue
Condition `result` at the left side of assignment expression is always `true`. Can be simplified
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
        boolean result = true;
        if (binding.getInitiatorToken() != null) {
            result &= checkInitiatorTokens(binding.getInitiatorToken(), ai, aim, hasDerivedKeys,
                                        signedResults, encryptedResults);
        }
```

### ConstantValue
Condition `!foundSCT` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
                        IncludeTokenType inclusion = token.getIncludeTokenType();
                        if (foundSCT || token.isOptional()
                                || (!foundSCT && inclusion == IncludeTokenType.INCLUDE_TOKEN_NEVER)) {
                            inf.setAsserted(true);
                        } else {
```

### ConstantValue
Value `foundSCT` is always 'false'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
                        IncludeTokenType inclusion = token.getIncludeTokenType();
                        if (foundSCT || token.isOptional()
                                || (!foundSCT && inclusion == IncludeTokenType.INCLUDE_TOKEN_NEVER)) {
                            inf.setAsserted(true);
                        } else {
```

### ConstantValue
Value `optional` is always 'true'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/NegotiationUtils.java`
#### Snippet
```java
        } else if (optional) {
            return new PrimitiveAssertion(assertion.getName(),
                                          optional);
        }
        return assertion;
```

### ConstantValue
Condition `context != null` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenInInterceptor.java`
#### Snippet
```java

            String context = requestEl.getAttributeNS(null, "Context");
            if (context != null && !"".equals(context)) {
                writer.writeAttribute("Context", context);
            }
```

### ConstantValue
Condition `cacheManagerClass != null` is always `true`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/TokenStoreFactory.java`
#### Snippet
```java
        try {
            Class<?> cacheManagerClass = Class.forName("org.ehcache.CacheManager");
            if (cacheManagerClass != null) {
                ehCacheInstalled = true;
            }
```

### ConstantValue
Value `sign` is always 'false'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        if (!sign) {
            // Handle content encrypted elements
            result.addAll(this.getElements("Content", contentXpaths, found, sign));
        }

```

### ConstantValue
Value `enableRevocation` is always 'true'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            X509Certificate[] certs = crypto.getX509Certificates(cryptoType);
            if (certs != null && certs.length > 0) {
                crypto.verifyTrust(certs, enableRevocation, null, null);
            }
        }
```

### ConstantValue
Value `messageBody` is always 'null'
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/filters/AbstractSignatureInFilter.java`
#### Snippet
```java
        byte[] messageBody = null;
        if (!enabled) {
            return messageBody;
        }

```

### ConstantValue
Value `server` is always 'null'
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ServerFactoryBean.java`
#### Snippet
```java
                Endpoint ep = createEndpoint();

                getServiceFactory().sendEvent(FactoryBeanListener.Event.PRE_SERVER_CREATE, server, serviceBean,
                                              serviceBean == null
                                              ? getServiceClass() == null
```

### ConstantValue
Condition `nl != null` is always `true`
in `services/wsn/wsn-api/src/main/java/org/apache/cxf/wsn/util/WSNHelper.java`
#### Snippet
```java
        ref.writeTo(new DOMResult(element));
        NodeList nl = element.getElementsByTagNameNS("http://www.w3.org/2005/08/addressing", "Address");
        if (nl != null && nl.getLength() > 0) {
            Element e = (Element) nl.item(0);
            return DOMUtils.getContent(e).trim();
```

### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/server/ReactiveIOInvoker.java`
#### Snippet
```java
        final AsyncResponseImpl asyncResponse = new AsyncResponseImpl(inMessage);
        Disposable d = subscribe(obs, asyncResponse);
        if (d == null) {
            throw new IllegalStateException("Subscribe did not return a Disposable");
        }
```

### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/server/ReactiveIOInvoker.java`
#### Snippet
```java
        final AsyncResponseImpl asyncResponse = new AsyncResponseImpl(inMessage);
        Disposable d = single.subscribe(asyncResponse::resume, t -> handleThrowable(asyncResponse, t));
        if (d == null) {
            throw new IllegalStateException("Subscribe did not return a Disposable");
        }
```

### ConstantValue
Condition `d == null` is always `false`
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/server/ReactiveIOInvoker.java`
#### Snippet
```java
        if (!isStreamingSubscriberUsed(f, asyncResponse, inMessage)) {
            Disposable d = subscribe(f, asyncResponse);
            if (d == null) {
                throw new IllegalStateException("Subscribe did not return a Disposable");
            }
```

### ConstantValue
Condition `payloadClass == null` is always `true` when reached
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/OutboundSseEventBodyWriter.java`
#### Snippet
```java
            }

            if (payloadType == null && payloadClass == null) {
                payloadType = Object.class;
                payloadClass = Object.class;
```

### ConstantValue
Condition `headers != null` is always `true`
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/interceptor/SseInterceptor.java`
#### Snippet
```java
                        
                        final MultivaluedMap<String, String> headers = responseImpl.getStringHeaders();
                        if (headers != null) {
                            for (Map.Entry<String, List<String>> entry: headers.entrySet()) {
                                setHeader(servletResponse, entry);
```

### ConstantValue
Condition `i == token.length() + 1` is always `false`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CacheControlHeaderProvider.java`
#### Snippet
```java
        int i = token.indexOf('=');
        if (i != -1) {
            String f = i == token.length() + 1 ? "" : token.substring(i + 1);
            if (f.length() < 2 || !f.startsWith("\"") || !f.endsWith("\"")) {
                return;
```

### ConstantValue
Value `name` is always 'null'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurationImpl.java`
#### Snippet
```java
    public void setProperty(String name, Object value) {
        if (name == null) {
            props.remove(name);
        } else {
            props.put(name, value);
```

### ConstantValue
Condition `i == theToken.length() + 1` is always `false`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CookieHeaderProvider.java`
#### Snippet
```java
                if (i != -1) {
                    name = theToken.substring(0, i);
                    value = i == theToken.length()  + 1 ? ""
                        : NewCookieHeaderProvider.stripQuotes(theToken.substring(i + 1));
                }
```

### ConstantValue
Condition `value == null` is always `false` when reached
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/CookieHeaderProvider.java`
#### Snippet
```java
        }

        if (name == null || value == null) {
            throw new IllegalArgumentException("Cookie is malformed : " + c);
        }
```

### ConstantValue
Condition `formatter != null` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RetryAfterHeaderProvider.java`
#### Snippet
```java
        } else {
            final DateFormat formatter = tryFormatter(value);
            if (formatter != null) {
                try {
                    final Date date = formatter.parse(value);
```

### ConstantValue
Value `weak` is always 'true'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/EntityTagHeaderProvider.java`
#### Snippet
```java
                tag = trimmed.substring(2);
            } else {
                return new EntityTag("", weak);
            }
        }  else {
```

### ConstantValue
Value `value` is always 'null'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/LinkBuilderImpl.java`
#### Snippet
```java
    private void checkNotNull(String value) {
        if (value == null) {
            throw new IllegalArgumentException(value);
        }
    }
```

### ConstantValue
Condition `xsd == null` is always `true` when reached
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
                    }
                }
            } else if (!absoluteLocUri && xsd == null) { // WSDL request
                key = new URI(".").resolve(loc).toString();
            }
```

### ConstantValue
Value `isQuery` is always 'true'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                        boolean templateValue = val.startsWith("{") && val.endsWith("}");
                        if (!templateValue) {
                            val = HttpUtils.encodePartiallyEncoded(val, isQuery);
                            if (!isQuery) {
                                val = val.replaceAll("/", "%2F");
```

### ConstantValue
Condition `!isQuery` is always `false`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                        if (!templateValue) {
                            val = HttpUtils.encodePartiallyEncoded(val, isQuery);
                            if (!isQuery) {
                                val = val.replaceAll("/", "%2F");
                            }
```

### ConstantValue
Value `isQuery` is always 'true'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                        if (!templateValue) {
                            val = HttpUtils.encodePartiallyEncoded(val, isQuery);
                            if (!isQuery) {
                                val = val.replaceAll("/", "%2F");
                            }
```

### ConstantValue
Value `isQuery` is always 'true'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
                            }
                        } else {
                            val = URITemplate.createExactTemplate(val).encodeLiteralCharacters(isQuery);
                        }

```

### ConstantValue
Condition `e2HttpMethod != null` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/OperationResourceInfoComparatorBase.java`
#### Snippet
```java
        }

        if (result == 0 && e1HttpMethod != null && e2HttpMethod != null) {
            boolean e1IsDefault = DefaultMethod.class.getSimpleName().equals(e1HttpMethod);
            boolean e2IsDefault = DefaultMethod.class.getSimpleName().equals(e2HttpMethod);
```

### ConstantValue
Condition `index < requestAddress.length()` is always `true` when reached
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        if (m.get(Message.QUERY_STRING) == null) {
            int index = requestAddress.lastIndexOf('?');
            if (index > 0 && index < requestAddress.length()) {
                m.put(Message.QUERY_STRING, requestAddress.substring(index + 1));
                requestAddress = requestAddress.substring(0, index);
```

### ConstantValue
Condition `properties != null` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
                parseMapSequence(servletConfig.getInitParameter(PROPERTIES_PARAM)),
                String.class, Object.class);
        if (properties != null) {
            bean.getProperties(true).putAll(properties);
        }
```

### ConstantValue
Value `result` is always 'null'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
            // The converter was applied and returned null value, acceptable
            // outcome for non-primitive type.
            return pClass.cast(result);
        }

```

### ConstantValue
Value `result` is always 'null'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                ? new String[] {"fromString", "fromValue", "valueOf"}
                : new String[] {"valueOf", "fromString"};
            result = evaluateFactoryMethods(value, pType, result, cls, methodNames);
        }

```

### ConstantValue
Value `enableStatic` is always 'true'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
                        ClassResourceInfo ancestor = getAncestorWithSameServiceClass(cri, subClass);
                        subCri = ancestor != null ? ancestor
                                : createClassResourceInfo(subClass, subClass, cri, false, enableStatic,
                                cri.getBus());
                    }
```

### ConstantValue
Condition `roleClaimValue instanceof List` is always `true`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/JwtTokenSecurityContext.java`
#### Snippet
```java
                    roles.add(new SimpleGroup(r));
                }
            } else if (roleClaimValue instanceof List && ((List) roleClaimValue).stream()
                        .noneMatch(o -> !(o instanceof String))) {
                roles = new HashSet<>();
```

### ConstantValue
Condition `types2.length > 0` is always `true` when reached
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        if (types1.length == 0 && types2.length == 0) {
            return 0;
        } else if (types1.length == 0 && types2.length > 0) {
            return 1;
        } else if (types1.length > 0 && types2.length == 0) {
```

### ConstantValue
Condition `types1.length > 0` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
        } else if (types1.length == 0 && types2.length > 0) {
            return 1;
        } else if (types1.length > 0 && types2.length == 0) {
            return -1;
        }
```

### ConstantValue
Condition `clz.getPackage().getAnnotations() != null` is always `true` when reached
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
        av0.visitEnd();

        if (clz.getPackage() != null && clz.getPackage().getAnnotations() != null) {
            for (Annotation ann : clz.getPackage().getAnnotations()) {
                if (ann instanceof XmlJavaTypeAdapters) {
```

### ConstantValue
Value `hasOpName` is always 'false'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                        hasOpName = false;
                        payloadOPMap = createPayloadEleOpNameMap(
                                client.getEndpoint().getBinding().getBindingInfo(), hasOpName);
                    }
                }
```

### ConstantValue
Condition `m == Service.Mode.MESSAGE` is always `true`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                                    m,
                                    "SOAP/HTTP");
            } else if (m == Service.Mode.MESSAGE) {
                SAAJOutInterceptor saajOut = new SAAJOutInterceptor();
                client.getOutInterceptors().add(saajOut);
```

### ConstantValue
Condition `f != null` is always `true`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
        List<WebServiceFeature> f = getAllFeatures(features);
        proxyFac.initFeatures();
        if (f != null) {
            serviceFactory.setWsFeatures(f);
        }
```

### ConstantValue
Condition `e instanceof SOAP12Address` is always `false`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof AddressType) {
                        address = ((AddressType)e).getLocation();
                    } else if (e instanceof SOAP12Address) {
                        address = ((SOAP12Address)e).getLocationURI();
                    } else if (e instanceof SOAPAddress) {
```

### ConstantValue
Value `e` is always 'null'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof AddressType) {
                        address = ((AddressType)e).getLocation();
                    } else if (e instanceof SOAP12Address) {
                        address = ((SOAP12Address)e).getLocationURI();
                    } else if (e instanceof SOAPAddress) {
```

### ConstantValue
Condition `e instanceof SOAPAddress` is always `false`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof SOAP12Address) {
                        address = ((SOAP12Address)e).getLocationURI();
                    } else if (e instanceof SOAPAddress) {
                        address = ((SOAPAddress)e).getLocationURI();
                    } else if (e instanceof HTTPAddress) {
```

### ConstantValue
Value `e` is always 'null'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof SOAP12Address) {
                        address = ((SOAP12Address)e).getLocationURI();
                    } else if (e instanceof SOAPAddress) {
                        address = ((SOAPAddress)e).getLocationURI();
                    } else if (e instanceof HTTPAddress) {
```

### ConstantValue
Condition `e instanceof HTTPAddress` is always `false`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof SOAPAddress) {
                        address = ((SOAPAddress)e).getLocationURI();
                    } else if (e instanceof HTTPAddress) {
                        address = ((HTTPAddress)e).getLocationURI();
                    }
```

### ConstantValue
Value `e` is always 'null'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    } else if (e instanceof SOAPAddress) {
                        address = ((SOAPAddress)e).getLocationURI();
                    } else if (e instanceof HTTPAddress) {
                        address = ((HTTPAddress)e).getLocationURI();
                    }
```

### ConstantValue
Condition `result` is always `false`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
        boolean result = false;
        while (st.hasMoreTokens() && !result) {
            result = result || singleProtocolMatches(st.nextToken(), id);
        }
        return result;
```

### ConstantValue
Condition `continueProcessing` is always `true` when reached
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
        try {
            int index = invokedHandlers.size() - 2;
            while (index >= 0 && continueProcessing) {
                Handler<? extends MessageContext> h = invokedHandlers.get(index);
                if (h instanceof LogicalHandler) {
```

### ConstantValue
Condition `!wm.exclude()` is always `true` when reached
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return Boolean.FALSE;
        }
        if ((wm != null && !wm.exclude())
            || (implInfo.getSEIClass() != null
                && cls.isInterface()
```

### ConstantValue
Result of `wm.exclude()` is always 'false'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
            return Boolean.FALSE;
        }
        if ((wm != null && !wm.exclude())
            || (implInfo.getSEIClass() != null
                && cls.isInterface()
```

### ConstantValue
Condition `ei != null` is always `true`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
        }

        if (ei != null) {
            if (!isFromWsdl) {
                buildWsdlExtensibilities(ei.getBinding());
```

### ConstantValue
Condition `ifcs != null` is always `true`
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
        if (ret == null) {
            Enumeration<NetworkInterface> ifcs = NetworkInterface.getNetworkInterfaces();
            if (ifcs != null) {
                List<NetworkInterface> possibles = new ArrayList<>();
                while (ifcs.hasMoreElements()) {
```

### ConstantValue
Condition `addressing != null` is always `true` when reached
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
            inputAction = computeAction(operation, "Request");
        }
        if (action == null && addressing != null) {
            operation.getInput().addExtensionAttribute(JAXWSAConstants.WSAM_ACTION_QNAME,
                                                       inputAction);
```

### ConstantValue
Condition `detail instanceof Identifier` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java
        if (detail instanceof Element) {
            flt.getAny().add(detail);
        } else if (detail instanceof Identifier) {
            flt.getAny().add(VersionTransformer.convert200502wsa15((Identifier)detail));
        } else if (detail instanceof SequenceAcknowledgement) {
```

### ConstantValue
Condition `detail instanceof SequenceAcknowledgement` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10AImpl.java`
#### Snippet
```java
        } else if (detail instanceof Identifier) {
            flt.getAny().add(VersionTransformer.convert200502wsa15((Identifier)detail));
        } else if (detail instanceof SequenceAcknowledgement) {
            flt.getAny().add(VersionTransformer.convert200502wsa15((SequenceAcknowledgement)detail));
        }
```

### ConstantValue
Condition `detail instanceof Identifier` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java
        if (detail instanceof Element) {
            flt.getAny().add(detail);
        } else if (detail instanceof Identifier) {
            flt.getAny().add(VersionTransformer.convert200502((Identifier)detail));
        } else if (detail instanceof SequenceAcknowledgement) {
```

### ConstantValue
Condition `detail instanceof SequenceAcknowledgement` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/EncoderDecoder10Impl.java`
#### Snippet
```java
        } else if (detail instanceof Identifier) {
            flt.getAny().add(VersionTransformer.convert200502((Identifier)detail));
        } else if (detail instanceof SequenceAcknowledgement) {
            flt.getAny().add(VersionTransformer.convert200502((SequenceAcknowledgement)detail));
        }
```

### ConstantValue
Condition `null != rmps` is always `true`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMInInterceptor.java`
#### Snippet
```java
        assertReliability(message);
        if (isApplicationMessage) {
            if (null != rmps) {
                processAcknowledgments(rme, rmps, protocol);
                processAcknowledgmentRequests(destination, message);
```

### ConstantValue
Value `ua` is always 'null'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
            return ua;
        }
        return ua;
    }

```

### ConstantValue
Condition `bus == null` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMManagerBeanDefinitionParser.java`
#### Snippet
```java

        String bus = element.getAttribute("bus");
        if (bus == null || "".equals(bus)) {
            addBusWiringAttribute(bean, BusWiringType.PROPERTY);
        } else {
```

### ConstantValue
Condition `b == null` is always `false`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyUtilities.java`
#### Snippet
```java
        }
        if (a == null) {
            return b == null;
        } else if (b == null) {
            return false;
```

### ConstantValue
Value `je` is always 'null'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBWrapperHelper.java`
#### Snippet
```java
        if ("jakarta.xml.bind.JAXBElement".equals(method.getReturnType().getCanonicalName())) {
            JAXBElement<?> je = (JAXBElement<?>)method.invoke(in);
            return je == null ? je : je.getValue();
        }
        return method.invoke(in);
```

### ConstantValue
Condition `typeClass == null` is always `false`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
        }
        Class<?> typeClass = part.getTypeClass();
        if (typeClass == null) {
            return;
        }
```

### ConstantValue
Condition `honorJaxbAnnotation` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
                    JAXBEncoderDecoder.marshall(createMarshaller(obj, part), obj, part, output);
                    onCompleteMarshalling();
                } else if (honorJaxbAnnotation && anns.length > 0) {
                    //RpcLit will use the JAXB Bridge to marshall part message when it is
                    //annotated with @XmlList,@XmlAttachmentRef,@XmlJavaTypeAdapter
```

### ConstantValue
Condition `honorJaxbAnnotation && anns.length > 0` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
                    JAXBEncoderDecoder.marshall(createMarshaller(obj, part), obj, part, output);
                    onCompleteMarshalling();
                } else if (honorJaxbAnnotation && anns.length > 0) {
                    //RpcLit will use the JAXB Bridge to marshall part message when it is
                    //annotated with @XmlList,@XmlAttachmentRef,@XmlJavaTypeAdapter
```

### ConstantValue
Condition `anns.length > 0` is always `true` when reached
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
                    JAXBEncoderDecoder.marshall(createMarshaller(obj, part), obj, part, output);
                    onCompleteMarshalling();
                } else if (honorJaxbAnnotation && anns.length > 0) {
                    //RpcLit will use the JAXB Bridge to marshall part message when it is
                    //annotated with @XmlList,@XmlAttachmentRef,@XmlJavaTypeAdapter
```

### ConstantValue
Condition `cl2.isArray()` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBContextInitializer.java`
#### Snippet
```java
                    && ((Class<?>)genericType).isArray()) {
                    Class<?> cl2 = (Class<?>)genericType;
                    if (cl2.isArray()
                        && !Byte.TYPE.equals(cl2.getComponentType())) {
                        genericType = cl2.getComponentType();
```

### ConstantValue
Value `continueProcessing` is always 'true'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            }
        }
        return continueProcessing;
    }

```

### ConstantValue
Condition `reference != null` is always `true`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
        EndpointReferenceType reference =
            EndpointReferenceUtils.getEndpointReference(replyToAddress);
        if (reference != null) {
            String decoupledAddress = reference.getAddress().getValue();
            LOG.info("creating decoupled endpoint: " + decoupledAddress);
```

### ConstantValue
Condition `value instanceof EndpointReferenceType` is always `false`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
        } else if (value instanceof AttributedURIType) {
            text = ((AttributedURIType)value).getValue();
        } else if (value instanceof EndpointReferenceType) {
            text = ((EndpointReferenceType)value).getAddress() != null
                   ? ((EndpointReferenceType)value).getAddress().getValue()
```

### ConstantValue
Condition `value instanceof RelatesToType` is always `false`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                   ? ((EndpointReferenceType)value).getAddress().getValue()
                   : "null";
        } else if (value instanceof RelatesToType) {
            text = ((RelatesToType)value).getValue();
        }
```

### ConstantValue
Condition `null != headerElement.getAttribute(IS_REFERENCE_PARAM_ATTR_NAME)` is always `true`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                                                       unmarshaller));
                            }
                        } else if (null != headerElement.getAttribute(IS_REFERENCE_PARAM_ATTR_NAME)) {
                            if (null == referenceParameterHeaders) {
                                referenceParameterHeaders = new HashSet<>();
```

### ConstantValue
Condition `m2Index == -1` is always `true` when reached
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                            return 1;
                        }
                        if (m1Index != -1 && m2Index == -1) {
                            return -1;
                        }
```

### ConstantValue
Condition `clazz.isArray()` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java

                Object obj = unmarshall(u, source, elName, null, unwrap);
                if (clazz.isArray() && obj instanceof List) {
                    return ((List<?>)obj).toArray((Object[])Array.newInstance(clazz.getComponentType(),
                                                                           ((List<?>)obj).size()));
```

### ConstantValue
Condition `part != null` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            if (null != elName) {

                if (part != null && part.getXmlSchema() instanceof XmlSchemaElement) {

                    XmlSchemaElement el = (XmlSchemaElement)part.getXmlSchema();
```

### ConstantValue
Condition `method.getParameterTypes() != null` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java

            for (Method method : allMethods) {
                if (method.getParameterTypes() != null && method.getParameterTypes().length == 1
                    && (setAccessor.equals(method.getName())
                        || setAccessor2.equals(method.getName()))) {
```

### ConstantValue
Condition `cachedContextAndSchemas != null` is always `true`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
                                                                         this.qualifiedSchemas, tns);
            schemaInit.walk();
            if (cachedContextAndSchemas != null && !schemasFromCache) {
                cachedContextAndSchemas.setSchemas(schemas);
            }
```

### ConstantValue
Condition `grant != null` is always `true`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/DefaultEncryptingCodeDataProvider.java`
#### Snippet
```java

        ServerAuthorizationCodeGrant grant = ModelEncryptionSupport.decryptCodeGrant(this, code, key);
        if (grant != null) {
            grants.remove(code);
        }
```

### ConstantValue
Condition `resourceOwnerSubject != null` is always `true` when reached
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
            return em.createQuery("SELECT c FROM ServerAuthorizationCodeGrant c",
                    ServerAuthorizationCodeGrant.class);
        } else if (c == null && resourceOwnerSubject != null) {
            return em.createQuery(
                    "SELECT c FROM ServerAuthorizationCodeGrant"
```

### ConstantValue
Condition `c != null` is always `true`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
                            + " WHERE s.login = :login", ServerAuthorizationCodeGrant.class)
                    .setParameter("login", resourceOwnerSubject.getLogin());
        } else if (c != null && resourceOwnerSubject == null) {
            return em.createQuery(
                    "SELECT code FROM ServerAuthorizationCodeGrant code"
```

### ConstantValue
Condition `clientCodeVerifier != null` is always `true` when reached
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AuthorizationCodeGrantHandler.java`
#### Snippet
```java
            return c.isConfidential() || !expectCodeVerifierForPublicClients;
        } else if (clientCodeChallenge != null && clientCodeVerifier == null
            || clientCodeChallenge == null && clientCodeVerifier != null) {
            return false;
        } else {
```

### ConstantValue
Condition `index < part.length()` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                } else {
                    name = part.substring(0, index);
                    value = index < part.length() ? part.substring(index + 1) : "";
                }
                if (valueIsCollection) {
```

### ConstantValue
Condition `len > 0` is always `true`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java

            int len = thePath.length();
            if (len > 0 && thePath.charAt(len - 1) == '/') {
                String value = ignoreLastSlash ? "" : "/";
                segments.add(new PathSegmentImpl(value, false));
```

### ConstantValue
Condition `parameter instanceof Element` is always `false`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/ActAsAttributeStatementProvider.java`
#### Snippet
```java
                ((UsernameTokenType)parameter).getUsername().getValue()
            );
        } else if (parameter instanceof Element) {
            SamlAssertionWrapper wrapper = new SamlAssertionWrapper((Element)parameter);
            SAMLTokenPrincipal principal = new SAMLTokenPrincipalImpl(wrapper);
```

### ConstantValue
Condition `attributeStatements == null` is always `false`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
            if (LOG.isLoggable(Level.FINEST)) {
                LOG.finest("No attribute statements found");
```

### ConstantValue
Condition `secondaryClaims != null` is always `true`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
        } else if (primaryClaims != null && secondaryClaims == null) {
            return retrieveClaimValues(primaryClaims, parameters);
        } else if (secondaryClaims != null && primaryClaims == null) {
            return retrieveClaimValues(secondaryClaims, parameters);
        }
```

### ConstantValue
Condition `verbose` is always `true`
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
                if (verbose && !msgPrinted) {
                    msgPrinted = true;
                    if (verbose) {
                        System.out.println("entering server");
                    }
```

### ConstantValue
Condition `m != null` is always `true`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/ExtensionClassGenerator.java`
#### Snippet
```java
        try {
            Method m = cls.getDeclaredMethod("getOtherAttributes");
            if (m != null && m.getReturnType() == Map.class){
                hasAttributes = true;
            }
```

### ConstantValue
Condition `dialectAttr != null` is always `true`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
        try {
            dialectAttr = claimsElement.getAttributeNS(null, "Dialect");
            if (dialectAttr != null && !"".equals(dialectAttr)) {
                requestedClaims.setDialect(new URI(dialectAttr));
            }
```

### ConstantValue
Condition `obj instanceof SecurityTokenReferenceType` is always `false` when reached
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
                }
            } else if (SecurityTokenReferenceType.class == useKeyJaxb.getDeclaredType()
                || obj instanceof SecurityTokenReferenceType) {
                SecurityTokenReferenceType strType =
                    SecurityTokenReferenceType.class.cast(useKeyJaxb.getValue());
```

### ConstantValue
Condition `x509CertData != null` is always `true`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
                        Constants.SignatureSpecNS, Constants._TAG_X509CERTIFICATE
                    );
                if (x509CertData != null && x509CertData.getLength() > 0) {
                    try {
                        x509 = Base64Utility.decode(x509CertData.item(0).getTextContent().trim());
```

### ConstantValue
Condition `targetToken instanceof SecurityTokenReferenceType` is always `false`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
                return true;
            }
        } else if (targetToken instanceof SecurityTokenReferenceType) {
            return true;
        }
```

### ConstantValue
Condition `targetToken instanceof SecurityTokenReferenceType` is always `false`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
            }
            referenceURI = refList.item(0).getNodeValue();
        } else if (targetToken instanceof SecurityTokenReferenceType) {
            Iterator<?> iterator = ((SecurityTokenReferenceType) targetToken).getAny().iterator();
            while (iterator.hasNext()) {
```

### ConstantValue
Condition `m.getName() != null` is always `true`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/XercesSchemaValidationUtils.java`
#### Snippet
```java
    Method findMethod(Object o, String name) {
        for (Method m : o.getClass().getMethods()) {
            if (m.getName() != null && m.getName().equals(name)) {
                m.setAccessible(true);
                return m;
```

### ConstantValue
Condition `nsField != null` is always `true`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/NSManager.java`
#### Snippet
```java
                    String prefix = (String) field.get(clz);
                    Field nsField = clz.getField("NS_" + prefix.toUpperCase());
                    if (nsField != null) {
                        cache.put((String)nsField.get(clz), prefix);
                    }
```

### ConstantValue
Condition `!synth` is always `true`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
            if (p.getConcreteName() != null
                && !elName.equals(p.getConcreteName())
                && !synth) {
                throw new Fault("UNEXPECTED_ELEMENT", LOG, null, elName,
                                p.getConcreteName());
```

### ConstantValue
Value `synth` is always 'false'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
            if (p.getConcreteName() != null
                && !elName.equals(p.getConcreteName())
                && !synth) {
                throw new Fault("UNEXPECTED_ELEMENT", LOG, null, elName,
                                p.getConcreteName());
```

### ConstantValue
Condition `!synth` is always `true` when reached
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
        } else {
            if (!(elName.equals(p.getName()) || elName.equals(p.getConcreteName()))
                && !synth) {
                throw new Fault("UNEXPECTED_ELEMENT", LOG, null, elName,
                                p.getConcreteName());
```

### ConstantValue
Value `synth` is always 'false'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
        } else {
            if (!(elName.equals(p.getName()) || elName.equals(p.getConcreteName()))
                && !synth) {
                throw new Fault("UNEXPECTED_ELEMENT", LOG, null, elName,
                                p.getConcreteName());
```

### ConstantValue
Value `unrestrictedPoliciesInstalled` is always 'false'
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            unrestrictedPoliciesInstalled = true;
        } catch (Exception e) {
            return unrestrictedPoliciesInstalled;
        }
        return unrestrictedPoliciesInstalled;
```

### ConstantValue
Value `unrestrictedPoliciesInstalled` is always 'true'
in `testutils/src/main/java/org/apache/cxf/test/TestUtilities.java`
#### Snippet
```java
            return unrestrictedPoliciesInstalled;
        }
        return unrestrictedPoliciesInstalled;
    }
}
```

### ConstantValue
Value `ok` is always 'true'
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
        }

        return ok;
    }

```

### ConstantValue
Value `ok` is always 'true'
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
        }

        return ok;
    }

```

### ConstantValue
Value `ok` is always 'true'
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
        }

        return ok;
    }

```

### ConstantValue
Value `attr` is always 'null'
in `services/xkms/xkms-common/src/main/java/org/apache/cxf/xkms/handlers/DnUtils.java`
#### Snippet
```java
            return attr.trim();
        }
        return attr;
    }

```

### ConstantValue
Condition `certificates == null` is always `false`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/DirectTrustValidator.java`
#### Snippet
```java
            if (keyUsages.contains(KeyUsageEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_SIGNATURE)) {
                List<X509Certificate> certificates = ValidateRequestParser.parse(request);
                if (certificates == null || certificates.isEmpty()) {
                    status.setStatusValue(KeyBindingEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_INDETERMINATE);
                    status.getIndeterminateReason().add("http://www.cxf.apache.org/2002/03/xkms#RequestNotSupported");
```

### ConstantValue
Condition `certificates == null` is always `false`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/DateValidator.java`
#### Snippet
```java
        StatusType status = new StatusType();
        List<X509Certificate> certificates = ValidateRequestParser.parse(request);
        if (certificates == null || certificates.isEmpty()) {
            status.setStatusValue(KeyBindingEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_INDETERMINATE);
            status.getIndeterminateReason().add("http://www.cxf.apache.org/2002/03/xkms#RequestNotSupported");
```

### ConstantValue
Condition `certificates == null` is always `false`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/validator/TrustedAuthorityValidator.java`
#### Snippet
```java
        StatusType status = new StatusType();
        List<X509Certificate> certificates = ValidateRequestParser.parse(request);
        if (certificates == null || certificates.isEmpty()) {
            status.setStatusValue(KeyBindingEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_INDETERMINATE);
            status.getIndeterminateReason().add("http://www.cxf.apache.org/2002/03/xkms#RequestNotSupported");
```

### ConstantValue
Value `nillable` is always 'true'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                el.setMaxOccurs(max);
                if (nillable) {
                    el.setNillable(nillable);
                }
                seq.getItems().add(el);
```

### ConstantValue
Condition `isOut` is always `true` when reached
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            initializeParameter(part, paramType, genericType);
            part.setIndex(i + 1);
        } else if (isIn && isOut) {
            QName name = getInPartName(o, method, i);
            part = o.getInput().getMessagePart(name);
```

### ConstantValue
Condition `outMsg == null` is always `false`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                }
                if (isOutParam(method, j)) {
                    if (outMsg == null) {
                        outMsg = op.createMessage(createOutputMessageName(op, method),
                                                  MessageInfo.Type.OUTPUT);
```

### ConstantValue
Condition `anns != null` is always `true`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
    protected void checkServiceClassAnnotations(Class<?> sc) {
        Annotation[] anns = serviceClass.getAnnotations();
        if (anns != null) {
            for (Annotation ann : anns) {
                String pkg = ann.annotationType().getPackage().getName();
```

### ConstantValue
Condition `fixedType instanceof Anonfixed` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaFixedHandler.java`
#### Snippet
```java
            digits = ((Fixed)fixedType).getDigits();
            scale = ((Fixed)fixedType).getScale();
        } else if (fixedType instanceof Anonfixed) {
            digits = ((Anonfixed)fixedType).getDigits();
            scale = ((Anonfixed)fixedType).getScale();
```

### ConstantValue
Condition `ind < path.length()` is always `true` when reached
in `rt/rs/description-swagger-ui/src/main/java/org/apache/cxf/jaxrs/swagger/ui/SwaggerUiService.java`
#### Snippet
```java
            String mediaType = null;
            int ind = path.lastIndexOf('.');
            if (ind != -1 && ind < path.length()) {
                String resourceExt = path.substring(ind + 1);
                if (mediaTypes != null && mediaTypes.containsKey(resourceExt)) {
```

### ConstantValue
Value `arrayType` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaArrayListener.java`
#### Snippet
```java
            arrayElementType = anonType.getElemtype();
        } else {
            Array type = (Array) arrayType;
            arrayElementType = type.getElemtype();
        }
```

### ConstantValue
Value `type` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaStructListener.java`
#### Snippet
```java
                if (type instanceof Abstractsequence) {
                    wrapped = ((Abstractsequence)type).isWrapped();
                } else if (type instanceof Abstractanonsequence) {
                    wrapped = ((Abstractanonsequence)type).isWrapped();
                }
```

### ConstantValue
Value `seqType` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaSequenceListener.java`
#### Snippet
```java
            elementName = anonSeqType.getElemname();
        } else {
            Sequence type = (Sequence) seqType;
            seqElementType = type.getElemtype();
            elementName = type.getElemname();
```

### ConstantValue
Condition `old != null` is always `true`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
                }
            } else {
                if (old != null) {
                    deactivate();
                }
```

### ConstantValue
Condition `i >= Integer.MIN_VALUE` is always `true`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaUnionHandler.java`
#### Snippet
```java
                label = String.valueOf(Integer.MAX_VALUE);
            }
            for (int i = Integer.MAX_VALUE; i >= Integer.MIN_VALUE; i--) {
                if (!labels.contains(String.valueOf(i))) {
                    label = String.valueOf(i);
```

### ConstantValue
Value `baseType` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
            elementName = arrayType.getElemname();
        } else {
            Anonarray anonArrayType = (Anonarray)baseType;
            arrayElementType = anonArrayType.getElemtype();
            arrayBound = anonArrayType.getBound();
```

### ConstantValue
Value `baseType` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
            elementName = seqType.getElemname();
        } else {
            Anonsequence seqType = (Anonsequence)baseType;
            seqElementType = seqType.getElemtype();
            seqBound = seqType.getBound();
```

### ConstantValue
Value `seqType` is always 'null'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
            seqElementType = anonSeqType.getElemtype();
        } else {
            Sequence type = (Sequence) seqType;
            seqElementType = type.getElemtype();
        }
```

### ConstantValue
Condition `faultMethod == null` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java

        Method faultMethod = ex.getClass().getMethod("getFaultInfo");
        if (faultMethod == null) {
            return;
        }
```

### ConstantValue
Condition `faultMethod != null` is always `true`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
            try {
                Method faultMethod = ex.getClass().getMethod("getFaultInfo");
                if (faultMethod != null) {
                    Class<?> faultClass = faultMethod.getReturnType();
                    XmlType exType = faultClass.getAnnotation(XmlType.class);
```

### ConstantValue
Condition `obj instanceof Anonfixed` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            tc = orb.create_array_tc((int) anonArrayType.getBound(),
                                     getTypeCode(orb, anonArrayType.getElemtype(), typeMap, seenTypes));
        } else if (obj instanceof Anonfixed) {
            Anonfixed anonFixedType = (Anonfixed) obj;
            tc = orb.create_fixed_tc((short) anonFixedType.getDigits(), (short) anonFixedType.getScale());
```

### ConstantValue
Condition `obj instanceof Anonsequence` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            Anonfixed anonFixedType = (Anonfixed) obj;
            tc = orb.create_fixed_tc((short) anonFixedType.getDigits(), (short) anonFixedType.getScale());
        } else if (obj instanceof Anonsequence) {
            Anonsequence anonSeqType = (Anonsequence)obj;
            tc = orb.create_sequence_tc((int) anonSeqType.getBound(),
```

### ConstantValue
Condition `obj instanceof Anonstring` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            tc = orb.create_sequence_tc((int) anonSeqType.getBound(),
                                        getTypeCode(orb, anonSeqType.getElemtype(), typeMap, seenTypes));
        } else if (obj instanceof Anonstring) {
            Anonstring anonStringType = (Anonstring)obj;
            tc = orb.create_string_tc((int)anonStringType.getBound());
```

### ConstantValue
Condition `obj instanceof Anonwstring` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            Anonstring anonStringType = (Anonstring)obj;
            tc = orb.create_string_tc((int)anonStringType.getBound());
        } else if (obj instanceof Anonwstring) {
            Anonwstring anonWStringType = (Anonwstring)obj;
            tc = orb.create_wstring_tc((int)anonWStringType.getBound());
```

### ConstantValue
Condition `obj instanceof Array` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                         getTypeCodeName(aliasType.getName()),
                                         getTypeCode(orb, aliasType.getBasetype(), typeMap, seenTypes));
            } else if (obj instanceof Array) {
                Array arrayType = (Array)obj;
                tc = orb.create_array_tc((int) arrayType.getBound(),
```

### ConstantValue
Condition `obj instanceof Enum` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                tc = orb.create_array_tc((int) arrayType.getBound(),
                                         getTypeCode(orb, arrayType.getElemtype(), typeMap, seenTypes));
            } else if (obj instanceof Enum) {
                Enum enumType = (Enum)obj;
                String name = enumType.getName();
```

### ConstantValue
Condition `obj instanceof Exception` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                name = getTypeCodeName(name);
                tc = orb.create_enum_tc(enumType.getRepositoryID(), name, members);
            } else if (obj instanceof Exception) {
                Exception exceptType = (Exception)obj;

```

### ConstantValue
Condition `obj instanceof Fixed` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                String name = getTypeCodeName(exceptType.getName());
                tc = orb.create_exception_tc(exceptType.getRepositoryID(), name, members);
            } else if (obj instanceof Fixed) {
                Fixed fixedType = (Fixed) obj;
                tc = orb.create_fixed_tc((short) fixedType.getDigits(), (short) fixedType.getScale());
```

### ConstantValue
Condition `obj instanceof org.apache.cxf.binding.corba.wsdl.Object` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                Fixed fixedType = (Fixed) obj;
                tc = orb.create_fixed_tc((short) fixedType.getDigits(), (short) fixedType.getScale());
            } else if (obj instanceof org.apache.cxf.binding.corba.wsdl.Object) {
                org.apache.cxf.binding.corba.wsdl.Object objType =
                    (org.apache.cxf.binding.corba.wsdl.Object)obj;
```

### ConstantValue
Condition `obj instanceof Sequence` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                                                 getTypeCodeName(objType.getName()));
                }
            } else if (obj instanceof Sequence) {
                Sequence seqType = (Sequence)obj;
                tc = orb.create_sequence_tc((int) seqType.getBound(),
```

### ConstantValue
Condition `obj instanceof Struct` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                tc = orb.create_sequence_tc((int) seqType.getBound(),
                                            getTypeCode(orb, seqType.getElemtype(), typeMap, seenTypes));
            } else if (obj instanceof Struct) {
                Struct structType = (Struct)obj;

```

### ConstantValue
Condition `obj instanceof Union` is always `false`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                    seenTypes.pop();
                }
            } else if (obj instanceof Union) {
                tc = getUnionTypeCode(orb, obj, typeMap, seenTypes);
            }
```

### ConstantValue
Condition `o != null` is always `true`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapActionInInterceptor.java`
#### Snippet
```java
                }
            }
            if (o != null) {
                boi.getOperationInfo().getInput().setProperty(CALCULATED_WSA_ACTION, o);
            }
```

### ConstantValue
Value `mu` is always 'true'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java
                boolean mu = soapHeader.isMustUnderstand();
                if (mu) {
                    String mul = soapVersion.getAttrValueMustUnderstand(mu);
                    super.writeAttribute(soapPrefix,
                                   soapVersion.getNamespace(),
```

### ConstantValue
Value `endedHeader` is always 'false'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java
        BindingOperationInfo bop = exchange.getBindingOperationInfo();
        if (bop == null) {
            return endedHeader;
        }

```

### ConstantValue
Value `endedHeader` is always 'false'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java

        if (bmi == null) {
            return endedHeader;
        }

```

### ConstantValue
Value `endedHeader` is always 'false'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java
            MessageContentsList objs = MessageContentsList.getContentsList(message);
            if (objs == null) {
                return endedHeader;
            }
            SoapVersion soapVersion = message.getVersion();
```

### ConstantValue
Value `endedHeader` is always 'false'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java
            List<SoapHeaderInfo> headers = bmi.getExtensors(SoapHeaderInfo.class);
            if (headers == null) {
                return endedHeader;
            }

```

### ConstantValue
Condition `bindingFiles != null` is always `true`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/OptionLoader.java`
#### Snippet
```java

        List<File> bindingFiles = FileUtils.getFiles(wadl.getParentFile(), wadlName + WADL_BINDINGS);
        if (bindingFiles != null) {
            for (File binding : bindingFiles) {
                wadlOption.addBindingFile(binding);
```

### ConstantValue
Condition `headers != null` is always `true`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
        List<SoapHeader> headers =
            SOAPBindingUtil.getSoapHeaders(bmsg.getExtensors(ExtensibilityElement.class));
        if (headers != null) {
            for (SoapHeader header : headers) {
                SoapHeaderInfo headerInfo = new SoapHeaderInfo();
```

### ConstantValue
Condition `file != null` is always `true`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java
        }

        if (file != null) {
            file.delete();
        }
```

### ConstantValue
Condition `fault` is always `true` when reached
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        } else if (!useIn && !fault) {
            i = reg.getOutInterceptorsForAssertion(qn);
        } else if (useIn && fault) {
            i = reg.getInFaultInterceptorsForAssertion(qn);
        } else if (!useIn && fault) {
```

### ConstantValue
Condition `!useIn` is always `true`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        } else if (useIn && fault) {
            i = reg.getInFaultInterceptorsForAssertion(qn);
        } else if (!useIn && fault) {
            i = reg.getOutFaultInterceptorsForAssertion(qn);
        }
```

### ConstantValue
Condition `!useIn && fault` is always `true`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        } else if (useIn && fault) {
            i = reg.getInFaultInterceptorsForAssertion(qn);
        } else if (!useIn && fault) {
            i = reg.getOutFaultInterceptorsForAssertion(qn);
        }
```

### ConstantValue
Value `useIn` is always 'false'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        } else if (useIn && fault) {
            i = reg.getInFaultInterceptorsForAssertion(qn);
        } else if (!useIn && fault) {
            i = reg.getOutFaultInterceptorsForAssertion(qn);
        }
```

### ConstantValue
Condition `fault` is always `true` when reached
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        } else if (useIn && fault) {
            i = reg.getInFaultInterceptorsForAssertion(qn);
        } else if (!useIn && fault) {
            i = reg.getOutFaultInterceptorsForAssertion(qn);
        }
```

### ConstantValue
Condition `null == is` is always `false`
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/ExternalAttachmentProvider.java`
#### Snippet
```java
        final Document doc;
        try (InputStream is = location.getInputStream()) {
            if (null == is) {
                throw new PolicyException(
                                          new org.apache.cxf.common.i18n.Message(
```

### ConstantValue
Condition `certs != null` is always `true`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsUtils.java`
#### Snippet
```java
            } else {
                X509Certificate[] certs = KeyManagementUtils.loadX509CertificateOrChain(m, props);
                if (certs != null && certs.length > 0) {
                    theVerifier = getPublicKeySignatureVerifier(certs[0], signatureAlgo);
                }
```

### ConstantValue
Condition `certs != null` is always `true`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
    public static String loadDigestAndEncodeX509Certificate(Message m, Properties props, String digestAlgo) {
        X509Certificate[] certs = loadX509CertificateOrChain(m, props);
        if (certs != null && certs.length > 0) {
            try {
                byte[] digest =
```

### ConstantValue
Condition `ks == null` is always `false`
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/common/KeyManagementUtils.java`
#### Snippet
```java
                                                               Properties props) {
        KeyStore ks = loadPersistKeyStore(m, props);
        if (ks == null || thumbprint == null) {
            return null;
        }
```

### ConstantValue
Condition `localName == null` is always `false` when reached
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        String ns = DOMUtils.getNamespace(e, prefix);

        if (ns == null || localName == null) {
            throw new DatabindingException("Invalid QName in mapping: " + value);
        }
```

### ConstantValue
Condition `method.getParameterAnnotations() == null` is always `false`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            Class<? extends Annotation>... annotations) {

        if (method.getParameterAnnotations() == null
            || method.getParameterAnnotations().length <= index
            || method.getParameterAnnotations()[index] == null) {
```

### ConstantValue
Condition `method.getParameterAnnotations() == null` is always `false`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java

    private static Annotation getAnnotation(Method method, int index, Class<? extends Annotation> type) {
        if (method.getParameterAnnotations() == null
            || method.getParameterAnnotations().length <= index
            || method.getParameterAnnotations()[index] == null) {
```

### ConstantValue
Condition `localName == null` is always `false`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
        }

        if (localName == null) {
            throw new DatabindingException("Invalid QName in mapping: " + value);
        }
```

### ConstantValue
Condition `localName == null` is always `false` when reached
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
        String ns = reader.getNamespaceURI(prefix);

        if (ns == null || localName == null) {
            throw new DatabindingException("Invalid QName in mapping: " + value);
        }
```

### ConstantValue
Condition `type == null` is always `false`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
                AegisType type = getType(inf, name);

                if (type == null) {
                    throw new DatabindingException("Couldn't find type for " + value.getClass()
                                                   + " for property " + name);
```

### ConstantValue
Condition `m.getParameterAnnotations() != null` is always `true`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/Java5TypeCreator.java`
#### Snippet
```java
        }

        if (m.getParameterAnnotations() != null && m.getAnnotations().length > 0) {
            info.setAnnotations(m.getAnnotations());
        }
```

### ConstantValue
Condition `bindingFiles != null` is always `true`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOptionLoader.java`
#### Snippet
```java

        List<File> bindingFiles = FileUtils.getFiles(wsdl.getParentFile(), wsdlName + WSDL_BINDINGS);
        if (bindingFiles != null) {
            for (File binding : bindingFiles) {
                wsdlOption.addBindingFile(binding);
```

### ConstantValue
Value `isNil` is always 'true'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
                Boolean b = AnnotationReader.isNillable(anns);
                if (b != null || (miValue != 0 && isNil)) {
                    part.setProperty("nillable", b == null ? isNil : b);
                }
                /*
```

### ConstantValue
Condition `wsdlArtifact != null` is always `true`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                                                                        wsdlA.getClassifier());
            wsdlArtifact = resolveRemoteWsdlArtifact(wsdlArtifact);
            if (wsdlArtifact != null) {
                File supposedFile = wsdlArtifact.getFile();
                if (!supposedFile.exists() || !supposedFile.isFile()) {
```

### ConstantValue
Condition `version != null` is always `true`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            if (!java9 && line.contains("java.version")) {
                String version = line.trim().substring("java.version = ".length());
                if (version != null) {
                    if (version.indexOf('.') > 0) {
                        version = version.substring(0, version.indexOf('.'));
```

### ConstantValue
Condition `String.valueOf(proxy.getPort()) != null` is always `true`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                System.setProperty(HTTP_PROXY_HOST, proxy.getHost());
            }
            if (String.valueOf(proxy.getPort()) != null) {
                System.setProperty(HTTP_PROXY_PORT, String.valueOf(proxy.getPort()));
            }
```

### ConstantValue
Value `value` is always 'null'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/InvocationBuilderImpl.java`
#### Snippet
```java
        // If value is null then all current headers of the same name should be removed
        if (value == null) {
            webClient.replaceHeader(name, value);
        } else {
            webClient.header(name, value);
```

### ConstantValue
Method reference result is always 'true'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
                                    }
                                }).
                                filter(Objects::nonNull).
                                ifPresent(value -> {
                                    if (methodAnnotation != null) {
```

### ConstantValue
Condition `t == null` is always `true`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
            }

            if ((t == null) && (m.getReturnType() == Response.class) && (m.getExceptionTypes().length == 0)) {
                return;
            }
```

## RuleId[ruleID=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
    }

    public static MicroProfileClientProviderFactory getInstance(Message m) {
        Endpoint e = m.getExchange().getEndpoint();
        return getInstance(e);
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ServerProviderFactory.java`
#### Snippet
```java
    }

    public static ServerProviderFactory getInstance(Message m) {
        Endpoint e = m.getExchange().getEndpoint();
        return (ServerProviderFactory)e.get(SERVER_FACTORY_NAME);
```

### MethodOverridesStaticMethod
Method `newInstance()` tries to override a static method of a superclass
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/endpoint/dynamic/JaxWsDynamicClientFactory.java`
#### Snippet
```java
     * @return the new instance
     */
    public static JaxWsDynamicClientFactory newInstance(Bus b) {
        return new JaxWsDynamicClientFactory(b);
    }
```

### MethodOverridesStaticMethod
Method `newInstance()` tries to override a static method of a superclass
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/endpoint/dynamic/JaxWsDynamicClientFactory.java`
#### Snippet
```java
     * @see CXFBusFactory#getDefaultBus()
     */
    public static JaxWsDynamicClientFactory newInstance() {
        Bus bus = BusFactory.getThreadDefaultBus();
        return new JaxWsDynamicClientFactory(bus);
```

### MethodOverridesStaticMethod
Method `getInstance()` tries to override a static method of a superclass
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProviderFactory.java`
#### Snippet
```java
    }

    public static ClientProviderFactory getInstance(Message m) {
        return getInstance(m.getExchange().getEndpoint());
    }
```

## RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
        code.append("//\n");

        code.append("function "
                    +  opFunctionGlobalName
                    + "("  + responseCallbackParams
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingOutInterceptor.java`
#### Snippet
```java
                //buffer.append("Outbound Message:\n");
                if (cos.size() >= lim) {
                    buffer.getMessage().append("(message truncated to " + lim + " bytes)\n");
                    truncated = true;
                }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingOutInterceptor.java`
#### Snippet
```java
            } else {
                buffer.getMessage().append("Outbound Message (saved to tmp file):\n");
                buffer.getMessage().append("Filename: " + cos.getTempFile().getAbsolutePath() + "\n");
                if (cos.size() >= lim) {
                    buffer.getMessage().append("(message truncated to " + lim + " bytes)\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingOutInterceptor.java`
#### Snippet
```java
                buffer.getMessage().append("Filename: " + cos.getTempFile().getAbsolutePath() + "\n");
                if (cos.size() >= lim) {
                    buffer.getMessage().append("(message truncated to " + lim + " bytes)\n");
                    truncated = true;
                }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingOutInterceptor.java`
#### Snippet
```java
            LoggingMessage buffer = setupBuffer(message);
            if (count >= lim) {
                buffer.getMessage().append("(message truncated to " + lim + " bytes)\n");
            }
            StringWriter w2 = out2;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingInInterceptor.java`
#### Snippet
```java
                //large thing on disk...
                buffer.getMessage().append("\nMessage (saved to tmp file):\n");
                buffer.getMessage().append("Filename: " + bos.getTempFile().getAbsolutePath() + "\n");
            }
            boolean truncated = false;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingInInterceptor.java`
#### Snippet
```java
            boolean truncated = false;
            if (bos.size() > limit && limit != -1) {
                buffer.getMessage().append("(message truncated to " + limit + " bytes)\n");
                truncated = true;
            }
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingInInterceptor.java`
#### Snippet
```java
                //large thing on disk...
                buffer.getMessage().append("\nMessage (saved to tmp file):\n");
                buffer.getMessage().append("Filename: " + writer.getTempFile().getAbsolutePath() + "\n");
            }
            if (writer.size() > limit && limit != -1) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `core/src/main/java/org/apache/cxf/interceptor/LoggingInInterceptor.java`
#### Snippet
```java
            }
            if (writer.size() > limit && limit != -1) {
                buffer.getMessage().append("(message truncated to " + limit + " bytes)\n");
            }
            writer.writeCacheTo(buffer.getPayload(), limit);
```

## RuleId[ruleID=IOResource]
### IOResource
'OutputStreamWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptGetInterceptor.java`
#### Snippet
```java
                .getXmlSchemaCollection());
            try {
                OutputStreamWriter writer = new OutputStreamWriter(os, UTF_8);
                for (SchemaInfo schema : schemata) {
                    SchemaJavascriptBuilder builder = new SchemaJavascriptBuilder(serviceInfo
```

### IOResource
'JettyOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
        try {
            if (out instanceof HttpOutput) {
                out = new JettyOutputStream((HttpOutput)out);
            }
        } catch (Throwable t) {
```

### IOResource
'PushbackInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ChunkedUtil.java`
#### Snippet
```java
        try {
            PushbackInputStream pin =
                new PushbackInputStream(connection.getInputStream());
            int c = pin.read();
            if (c != -1) {
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java
        final List<String> rules = new ArrayList<>();
        final List<String> exceptions = new ArrayList<>();
        final BufferedReader r = new BufferedReader(reader);

        String line;
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java
        final List<PublicSuffixList> result = new ArrayList<>(2);

        final BufferedReader r = new BufferedReader(reader);

        DomainType domainType = null;
```

### IOResource
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `core/src/main/java/org/apache/cxf/common/util/StreamPrinter.java`
#### Snippet
```java
            PrintWriter pw = null;
            if (os != null) {
                pw = new PrintWriter(os);
            }
            InputStreamReader isr = new InputStreamReader(is);
```

### IOResource
'BufferedReader' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `core/src/main/java/org/apache/cxf/common/util/StreamPrinter.java`
#### Snippet
```java
            }
            InputStreamReader isr = new InputStreamReader(is);
            BufferedReader br = new BufferedReader(isr);
            String line = br.readLine();
            while (line != null) {
```

### IOResource
'AbstractWrappedOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `core/src/main/java/org/apache/cxf/interceptor/StaxOutInterceptor.java`
#### Snippet
```java
    private OutputStream setupOutputStream(OutputStream os) {
        if (!(os instanceof AbstractWrappedOutputStream)) {
            os = new AbstractWrappedOutputStream(os) { };
        }
        ((AbstractWrappedOutputStream)os).allowFlush(false);
```

### IOResource
'PushbackInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    try {
                        PushbackInputStream pin =
                            new PushbackInputStream(getInputStream());
                        int c = pin.read();
                        if (c != -1) {
```

### IOResource
'PushbackInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                    try {
                        PushbackInputStream pin =
                            new PushbackInputStream(getInputStream());
                        int c = pin.read();
                        if (c != -1) {
```

### IOResource
'MessageDigestInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/BinaryDataProvider.java`
#### Snippet
```java
            if (InputStream.class.isAssignableFrom(clazz)) {
                if (DigestInputStream.class.isAssignableFrom(clazz)) {
                    is = new MessageDigestInputStream(is);
                }
                return clazz.cast(is);
```

### IOResource
'UDPDestinationOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
            exchange.setInMessage(m);
            m.setContent(InputStream.class, in);
            out = new UDPDestinationOutputStream(out);
            m.put(OutputStream.class, out);
            queue.execute(() -> getMessageObserver().onMessage(m));
```

### IOResource
'InputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            byte[] data = new byte[fileSize *  count];
            for (int x = 0; x < count; x++) {
                this.getClass().getResourceAsStream("/wsdl/mtom_xop.wsdl").read(data,
                                                                                fileSize * x,
                                                                                fileSize);
```

### IOResource
'PushbackInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    try {
                        PushbackInputStream pin =
                                new PushbackInputStream(getInputStream());
                        int c = pin.read();
                        if (c != -1) {
```

## RuleId[ruleID=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Collection `atSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
    public void writeStartElement(String pfx, String name, String ns) throws XMLStreamException {
```

### RedundantOperationOnEmptyContainer
Collection `nsSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
    public void writeStartElement(String pfx, String name, String ns) throws XMLStreamException {
```

### RedundantOperationOnEmptyContainer
Collection `atSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
    public void writeStartElement(String name, String ns) throws XMLStreamException {
```

### RedundantOperationOnEmptyContainer
Collection `nsSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
    public void writeStartElement(String name, String ns) throws XMLStreamException {
```

### RedundantOperationOnEmptyContainer
Collection `l` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
                if (pfx == null) {
                    l = Collections.emptyList();
                    return l.iterator();
                }
                return Collections.singleton(pfx).iterator();
```

### RedundantOperationOnEmptyContainer
Collection `atSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }

```

### RedundantOperationOnEmptyContainer
Collection `nsSet` is always empty
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }

```

## RuleId[ruleID=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
        Optional<Long> opt =
            c.isPresent() ? c.get().getOptionalValue(propName, Long.class) : Optional.empty();
        return opt.isPresent() ? OptionalLong.of(opt.get()) : OptionalLong.empty();
    }

```

### OptionalIsPresent
Can be replaced with single expression in functional style
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/config/ConfigFacade.java`
#### Snippet
```java
    public static <T> T getValue(String propertyName, Class<T> clazz) {
        Optional<Config> c = config();
        return c.isPresent() ? c.get().getValue(propertyName, clazz) : null;
    }

```

## RuleId[ruleID=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

        // add user extra class into jaxb context
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
```

### RedundantLengthCheck
Redundant array length check
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
        throws JAXBException {
        //add user extra class into jaxb context
        if (extraClass != null && extraClass.length > 0) {
            for (Class<?> clz : extraClass) {
                classes.add(clz);
```

## RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `Names200408` has only 'static' members, and lacks a 'private' constructor
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
     * Holder for 2004/08 Names
     */
    public static class Names200408 {
        public static final String WSA_NAMESPACE_NAME =
            "http://schemas.xmlsoap.org/ws/2004/08/addressing";
```

### UtilityClassWithoutPrivateConstructor
Class `Names200403` has only 'static' members, and lacks a 'private' constructor
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
     * Holder for 2004/03 Names
     */
    public static class Names200403 {
        public static final String WSA_NAMESPACE_NAME = "http://schemas.xmlsoap.org/ws/2004/03/addressing";
        public static final String WSA_ANONYMOUS_ADDRESS = WSA_NAMESPACE_NAME + "/role/anonymous";
```

### UtilityClassWithoutPrivateConstructor
Class `RMContextUtils` has only 'static' members, and lacks a 'private' constructor
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
 */

public final class RMContextUtils {

    /**
```

## RuleId[ruleID=DataFlowIssue]
### DataFlowIssue
Condition `addr instanceof AddressType` is redundant and can be replaced with a null check
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/wsdl11/HttpAddressPlugin.java`
#### Snippet
```java
                                                              WSDLConstants.QNAME_XMLHTTP_BINDING_ADDRESS);

        if (addr instanceof AddressType) {
            ((AddressType)addr).setLocation(address);
        }
```

### DataFlowIssue
Condition `addr instanceof HTTPAddress` is redundant and can be replaced with a null check
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/wsdl11/HttpAddressPlugin.java`
#### Snippet
```java
        }

        if (addr instanceof HTTPAddress) {
            ((HTTPAddress)addr).setLocationURI(address);
        }
```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/tomcat/AbstractTomcatServer.java`
#### Snippet
```java
            server.getHost().setDeployOnStartup(true);

            server.addWebapp(contextPath, getClass().getResource(resourcePath).toURI().getPath().toString());
            server.start();
        } catch (final Exception ex) {
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/ParticleInfo.java`
#### Snippet
```java
            XmlSchemaElement element = (XmlSchemaElement)particle;
            QName elementQName = XmlSchemaUtils.getElementQualifiedName(element, currentSchema);
            String elementNamespaceURI = elementQName.getNamespaceURI();

            boolean elementNoNamespace = "".equals(elementNamespaceURI);
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
        XmlSchemaElement element = (XmlSchemaElement)itemInfo.getParticle();
        QName elementQName = XmlSchemaUtils.getElementQualifiedName(element, xmlSchema);
        String elementNamespaceURI = elementQName.getNamespaceURI();
        boolean elementNoNamespace = "".equals(elementNamespaceURI);
        XmlSchema elementSchema = null;
```

### DataFlowIssue
Method invocation `setHandled` may produce `NullPointerException`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
            resp.sendRedirect(sp.getRedirectURL());
            resp.flushBuffer();
            baseRequest.setHandled(true);
            return;
        }
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                        for (String hts : ht.getScopes().getValue()) {
                            UUID htuuid = toUUID(hts);
                            if (!htuuid.equals(psuuid)) {
                                foundOne = true;
                            }
```

### DataFlowIssue
Variable is already assigned to this value
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                //LDAP not supported
                if (!pt.getScopes().getValue().isEmpty()) {
                    matches = false;
                }
            } else if ("/strcmp0".equals(mb)) {
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java

                    for (XMLObject o : atr.getAttributeValues()) {
                        String attrValue = o.getDOM().getTextContent();
                        claim.getValues().add(attrValue);
                    }
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java

                    for (XMLObject o : atr.getAttributeValues()) {
                        String attrValue = o.getDOM().getTextContent();
                        claim.getValues().add(attrValue);
                    }
```

### DataFlowIssue
Method invocation `getConstructorArgumentValues` may produce `NullPointerException`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
            Element elem = DOMUtils.getFirstElement(element);
```

### DataFlowIssue
Argument `outputMessage` might be null
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java

        if (nonVoidOutput) {
            createResponseDeserializer(outputMessage);
        }
    }
```

### DataFlowIssue
Argument `mpi.getElementQName()` might be null
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
                element = (XmlSchemaElement)mpi.getXmlSchema();
                if (element == null) {
                    element = XmlSchemaUtils.findElementByRefName(xmlSchemaCollection, mpi.getElementQName(),
                                                                  serviceInfo.getTargetNamespace());
                }
```

### DataFlowIssue
Argument `inputWrapperPartInfo.getElementQName()` might be null
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
                inputWrapperElement =
                    XmlSchemaUtils.findElementByRefName(xmlSchemaCollection,
                                                        inputWrapperPartInfo.getElementQName(),
                                                        serviceInfo.getTargetNamespace());
            }
```

### DataFlowIssue
Argument `outputWrapperPartInfo.getElementQName()` might be null
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/service/ServiceJavascriptBuilder.java`
#### Snippet
```java
                outputWrapperElement =
                    XmlSchemaUtils.findElementByRefName(xmlSchemaCollection,
                                                        outputWrapperPartInfo.getElementQName(),
                                                        serviceInfo.getTargetNamespace());
            }
```

### DataFlowIssue
Method invocation `addHandler` may produce `NullPointerException`
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
                            securityHandler = (SecurityHandler)h;
                        } else {
                            handlerCollection.addHandler(h);
                        }
                    }
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
                }
            } catch (Exception e) {
                pauth = null;
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
                pauth = tryWith(cxfauth);
            } catch (Exception e1) {
                pauth = null;
            }
        }
```

### DataFlowIssue
Method invocation `endsWith` may produce `NullPointerException`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            String address = EndpointReferenceUtils.getAddress(reference);
            ref = EndpointReferenceUtils.duplicate(reference);
            if (address.endsWith("/")) {
                EndpointReferenceUtils.setAddress(ref, address + id);
            } else {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
        if (this.authSupplier.requiresRequestCaching()) {
            needToCacheRequest = true;
            isChunking = false;
            LOG.log(Level.FINE,
                    "Auth Supplier, but no Preemptive User Pass or Digest auth (nonce may be stale)"
```

### DataFlowIssue
Method invocation `endsWith` may produce `NullPointerException`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java

        // REVISIT: is this really correct?
        if (null != pathInfo && !result.endsWith(pathInfo)) {
            result = result + pathInfo;
        }
```

### DataFlowIssue
Unboxing of `type` may produce `NullPointerException`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java
            for (final List<?> entry : entries) {
                final Integer type = entry.size() >= 2 ? (Integer) entry.get(0) : null;
                if (type == SubjectName.DNS || type == SubjectName.IP) {
                    final Object o = entry.get(1);
                    if (o instanceof String) {
```

### DataFlowIssue
Argument `outputFile` might be null
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/Java2WADLMojo.java`
#### Snippet
```java

        try {
            FileUtils.mkDir(new File(outputFile).getParentFile());
            try (BufferedWriter writer = new BufferedWriter(new FileWriter(outputFile))) {
                writer.write(wadl);
```

### DataFlowIssue
Passing `null` argument to `Optional`
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIFacade.java`
#### Snippet
```java
        } catch (Throwable t) {
            t.printStackTrace();
            return Optional.ofNullable(null);
        }
    }
```

### DataFlowIssue
Argument `beanManager` might be null
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
        BeanManager beanManager = (BeanManager) beanManagerObject;
        creationalContext = beanManager != null ? beanManager.createCreationalContext(null) : null;
        interceptorInvokers = initInterceptorInvokers(beanManager, creationalContext, restClient);
    }

```

### DataFlowIssue
Variable is already assigned to this value
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecInHandler.java`
#### Snippet
```java
        StringBuilder log = new StringBuilder(error);
        if (ex != null) {
            log = log.append(" - ").append(ex.getMessage());
        }
        LOG.warning(log.toString());
```

### DataFlowIssue
Argument `encKeyElement` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java


        Element cipherValue = getNode(encKeyElement, ENC_NS, "CipherValue", 0);
        if (cipherValue == null) {
            throwFault("CipherValue element is not available", null);
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
        }
        try {
            return decryptSymmetricKey(cipherValue.getTextContent().trim(),
                                       cert,
                                       crypto,
```

### DataFlowIssue
Method invocation `getAttribute` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
            throwFault("EncryptionMethod element is not available", null);
        }
        return encMethod.getAttribute("Algorithm");
    }

```

### DataFlowIssue
Method invocation `destroy` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
            // Clean the private key from memory now that we're finished with it
            try {
                key.destroy();
            } catch (DestroyFailedException ex) {
                // ignore
```

### DataFlowIssue
Argument `decryptedPayload` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlEncInHandler.java`
#### Snippet
```java
        Document payloadDoc = null;
        try {
            payloadDoc = StaxUtils.read(new InputStreamReader(new ByteArrayInputStream(decryptedPayload),
                                               StandardCharsets.UTF_8));
        } catch (Exception ex) {
```

### DataFlowIssue
Method invocation `getLength` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
        String c14TransformExpected = sigProps != null ? sigProps.getSignatureC14nTransform() : null;
        boolean envelopedConfirmed = false;
        for (int i = 0; i < transforms.getLength(); i++) {
            try {
                Transform tr = transforms.item(i);
```

### DataFlowIssue
Argument `signatureElement` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
        Reference ref = null;
        try {
            XMLSignature signature = new XMLSignature(signatureElement, "", true);

            if (sigProps != null) {
```

### DataFlowIssue
Argument `ref` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
        if (removeSignature) {
            if (!isEnveloping(root)) {
                Element signedEl = getSignedElement(root, ref);
                signedEl.removeAttribute("ID");
                root.removeChild(signatureElement);
```

### DataFlowIssue
Argument `objectNode` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSigInHandler.java`
#### Snippet
```java
            throwFault("Object envelope is not available", null);
        }
        Element node = DOMUtils.getFirstElement(objectNode);
        if (node == null) {
            throwFault("No signed data is found", null);
```

### DataFlowIssue
Argument `incomingSecurityEventList` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
            if (signatureRequired) {
                Event requiredEvent = SecurityEventConstants.SignatureValue;
                if (!isEventInResults(requiredEvent, incomingSecurityEventList)) {
                    LOG.warning("The request was not signed");
                    XMLSecurityException ex =
```

### DataFlowIssue
Argument `incomingSecurityEventList` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecInInterceptor.java`
#### Snippet
```java
            if (encryptionRequired) {
                boolean foundEncryptionPart =
                    isEventInResults(SecurityEventConstants.EncryptedElement, incomingSecurityEventList);
                if (!foundEncryptionPart) {
                    LOG.warning("The request was not encrypted");
```

### DataFlowIssue
Argument `DOMUtils.convertStringToQName(expandedName, DEFAULT_ENV_PREFIX)` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSigOutInterceptor.java`
#### Snippet
```java

    public void setEnvelopeName(String expandedName) {
        setEnvelopeQName(DOMUtils.convertStringToQName(expandedName, DEFAULT_ENV_PREFIX));
    }

```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlHeaderInHandler.java`
#### Snippet
```java
        }

        String[] parts = values.get(0).split(" ");
        if (parts.length != 2) {
            throwFault("Authorization header is malformed", null);
```

### DataFlowIssue
Method invocation `asMap` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlFormInHandler.java`
#### Snippet
```java

        Form form = readFormData(message);
        MultivaluedMap<String, String> formData = form.asMap();
        String assertion = formData.getFirst(SAML_ELEMENT);

```

### DataFlowIssue
Method invocation `getDocumentElement` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedInHandler.java`
#### Snippet
```java
            throwFault("No payload is available", null);
        }
        Element samlElement = getNode(doc.getDocumentElement(),
                SAML2_NS, SAML_ASSERTION);
        if (samlElement == null) {
```

### DataFlowIssue
Argument `DOMUtils.convertStringToQName(expandedName, DEFAULT_ENV_PREFIX)` might be null
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlEnvelopedOutInterceptor.java`
#### Snippet
```java

    public void setEnvelopeName(String expandedName) {
        setEnvelopeQName(DOMUtils.convertStringToQName(expandedName, DEFAULT_ENV_PREFIX));
    }

```

### DataFlowIssue
Method invocation `writeStartDocument` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
        if (MessageUtils.getContextualBoolean(message, StaxOutInterceptor.FORCE_START_DOCUMENT, false)) {
            try {
                newXMLStreamWriter.writeStartDocument(encoding, "1.0");
            } catch (XMLStreamException e) {
                throw new Fault(e);
```

### DataFlowIssue
Method invocation `getOwnerDocument` may produce `NullPointerException`
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/AbstractSamlInHandler.java`
#### Snippet
```java

                Signature sig = assertion.getSignature();
                WSDocInfo docInfo = new WSDocInfo(sig.getDOM().getOwnerDocument());
                data.setWsDocInfo(docInfo);

```

### DataFlowIssue
Dereference of `methods` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SamlSSOAssertionValidator.java`
#### Snippet
```java
        boolean requiredMethodFound = false;
        boolean standardMethodFound = false;
        for (String method : methods) {
            if (OpenSAMLUtil.isMethodHolderOfKey(method)) {
                if (samlAssertion.getSubjectKeyInfo() == null) {
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractServiceProviderFilter.java`
#### Snippet
```java
    protected void setSecurityContext(Message m, SamlAssertionWrapper assertionWrapper) {
        Subject subject = SAMLUtils.getSubject(m, assertionWrapper);
        final String name = subject.getName();

        if (name != null) {
```

### DataFlowIssue
Method invocation `cloneNode` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java

        Element assertionElement = validAssertion.getDOM();
        Element clonedAssertionElement = (Element)assertionElement.cloneNode(true);
        validatorResponse.setAssertionElement(clonedAssertionElement);
        validatorResponse.setAssertion(DOM2Writer.nodeToString(clonedAssertionElement));
```

### DataFlowIssue
Method invocation `getOwnerDocument` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
        validateResponseSignature(samlResponse, sigCrypto, callbackHandler);

        Document doc = samlResponse.getDOM().getOwnerDocument();
        // Decrypt any encrypted Assertions and add them to the Response (note that this will break any
        // signature on the Response)
```

### DataFlowIssue
Method invocation `getAttributeNodeNS` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        // Required to make IdResolver happy in OpenSAML
        Attr idAttr = samlResponse.getDOM().getAttributeNodeNS(null, "ID");
        if (idAttr != null) {
            samlResponse.getDOM().setIdAttributeNode(idAttr, true);
```

### DataFlowIssue
Argument `samlResponse.getSignature()` might be null
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        validateResponseSignature(
            samlResponse.getSignature(), samlResponse.getDOM().getOwnerDocument(),
            sigCrypto, callbackHandler
        );
```

### DataFlowIssue
Method invocation `getAttributeNodeNS` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        // Required to make IdResolver happy in OpenSAML
        Attr idAttr = samlResponse.getDOM().getAttributeNodeNS(null, "ID");
        if (idAttr != null) {
            samlResponse.getDOM().setIdAttributeNode(idAttr, true);
```

### DataFlowIssue
Argument `samlResponse.getSignature()` might be null
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        validateResponseSignature(
            samlResponse.getSignature(), samlResponse.getDOM().getOwnerDocument(),
            sigCrypto, callbackHandler
        );
```

### DataFlowIssue
Argument `assertion.getDOM()` might be null
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
        Element encryptedDataDOM = encryptedData.getDOM();

        Element encKeyElement = getNode(assertion.getDOM(), WSS4JConstants.ENC_NS, "EncryptedKey", 0);
        if (encKeyElement == null) {
            encKeyElement = getNode(encryptedDataDOM, WSS4JConstants.ENC_NS, "EncryptedKey", 0);
```

### DataFlowIssue
Argument `encryptedDataDOM` might be null
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
        Element encKeyElement = getNode(assertion.getDOM(), WSS4JConstants.ENC_NS, "EncryptedKey", 0);
        if (encKeyElement == null) {
            encKeyElement = getNode(encryptedDataDOM, WSS4JConstants.ENC_NS, "EncryptedKey", 0);
        }
        if (encKeyElement == null) {
```

### DataFlowIssue
Method invocation `setIdAttributeNS` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
        if (assertion.isSigned()) {
            if (assertion.getSaml1() != null) {
                assertion.getSaml1().getDOM().setIdAttributeNS(null, "AssertionID", true);
            } else {
                assertion.getSaml2().getDOM().setIdAttributeNS(null, "ID", true);
```

### DataFlowIssue
Method invocation `setIdAttributeNS` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
                assertion.getSaml1().getDOM().setIdAttributeNS(null, "AssertionID", true);
            } else {
                assertion.getSaml2().getDOM().setIdAttributeNS(null, "ID", true);
            }

```

### DataFlowIssue
Method invocation `getOwnerDocument` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
            try {
                Signature sig = assertion.getSignature();
                WSDocInfo docInfo = new WSDocInfo(sig.getDOM().getOwnerDocument());
                requestData.setWsDocInfo(docInfo);

```

### DataFlowIssue
Method invocation `getOwnerDocument` may produce `NullPointerException`
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
            SamlAssertionWrapper wrapper = new SamlAssertionWrapper(assertion);
            validateAssertion(
                wrapper, sigCrypto, callbackHandler, samlResponse.getDOM().getOwnerDocument(),
                samlResponse.isSigned()
            );
```

### DataFlowIssue
Argument `InjectionUtils.getRawType(pt)` might be null
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
        if (genericReturnType instanceof ParameterizedType) {
            final ParameterizedType pt = (ParameterizedType)genericReturnType;
            if (CompletionStage.class.isAssignableFrom(InjectionUtils.getRawType(pt))) {
                final Type[] actualTypeArguments = pt.getActualTypeArguments();
                if (actualTypeArguments.length > 0 && actualTypeArguments[0] instanceof ParameterizedType) {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
                this.sslContext = createSSLContext();
            }
            result = result.addHttpsListener(getPort(), getHost(), this.sslContext);
        } else {
            result = result.addHttpListener(getPort(), getHost());
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            result = result.addHttpsListener(getPort(), getHost(), this.sslContext);
        } else {
            result = result.addHttpListener(getPort(), getHost());
        }
        path = Handlers.path(new NotFoundHandler());
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java

        if (url.getPath().length() == 0) {
            result = result.setHandler(Handlers.trace(undertowHTTPHandler));
        } else {
            if (undertowHTTPHandler.isContextMatchExact()) {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }

            result = result.setHandler(wrapHandler(new HttpContinueReadHandler(path)));
        }

```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        if (this.tlsServerParameters != null && this.tlsServerParameters.getClientAuthentication() != null
            && this.tlsServerParameters.getClientAuthentication().isRequired()) {
            builder = builder.setSocketOption(Options.SSL_CLIENT_AUTH_MODE, SslClientAuthMode.REQUIRED);
        }
        if (this.tlsServerParameters != null && this.tlsServerParameters.getClientAuthentication() != null
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            && this.tlsServerParameters.getClientAuthentication().isWant()
            && !this.tlsServerParameters.getClientAuthentication().isRequired()) {
            builder = builder.setSocketOption(Options.SSL_CLIENT_AUTH_MODE, SslClientAuthMode.REQUESTED);
        }
        return builder;
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
        if (this.threadingParameters != null) {
            if (this.threadingParameters.isWorkerIOThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_IO_THREADS,
                              this.threadingParameters.getWorkerIOThreads());
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isMinThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_TASK_CORE_THREADS,
                              this.threadingParameters.getMinThreads());
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isMaxThreadsSet()) {
                builder = builder.setWorkerOption(Options.WORKER_TASK_MAX_THREADS,
                              this.threadingParameters.getMaxThreads());
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            }
            if (this.threadingParameters.isWorkerIONameSet()) {
                builder = builder.setWorkerOption(Options.WORKER_NAME,
                              this.threadingParameters.getWorkerIOName());
            }
```

### DataFlowIssue
Method invocation `getConstructorArgumentValues` may produce `NullPointerException`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
            Element elem = DOMUtils.getFirstElement(element);
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/PrimitiveSearchCondition.java`
#### Snippet
```java
        if (cond == ConditionType.EQUALS || cond == ConditionType.NOT_EQUALS) {
            if (rval == null) {
                compares = true;
            } else if (lval == null) {
                compares = false;
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
        }
        if (sc.getStatement() != null) {
            predStack.peek().add(buildPredicate(sc.getStatement()));
        } else {
            predStack.push(new ArrayList<>());
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/jpa/AbstractJPATypedQueryVisitor.java`
#### Snippet
```java
                newPred = builder.and(preds);
            }
            predStack.peek().add(newPred);
        }
    }
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
        if (statement != null) {
            if (statement.getProperty() != null) {
                state.get().peek().add(buildSimpleQuery(sc.getConditionType(),
                                         statement.getProperty(),
                                         statement.getValue()));
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
            boolean orCondition = sc.getConditionType() == ConditionType.OR;
            List<Query> queries = state.get().pop();
            state.get().peek().add(createCompositeQuery(queries, orCondition));
        }
    }
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/lucene/LuceneQueryVisitor.java`
#### Snippet
```java
    public Query getQuery() {
        List<Query> queries = state.get().peek();
        return queries.isEmpty() ? null : queries.get(0);
    }

```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
        // 5.2.8 max-age lives in the output filter.
        if (effectiveMaxAge(ann) != null) {
            m.getExchange().put(CorsHeaderConstants.HEADER_AC_MAX_AGE, effectiveMaxAge(ann).toString());
        }

```

### DataFlowIssue
Method invocation `addResourceResolver` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBus.java`
#### Snippet
```java

        ResourceManager m = getExtension(ResourceManager.class);
        m.addResourceResolver(new BusApplicationContextResourceResolver(applicationContext));

        setExtension(applicationContext, ApplicationContext.class);
```

### DataFlowIssue
Method invocation `createUnmarshaller` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/ws/addressing/VersionTransformer.java`
#### Snippet
```java
        }
        JAXBContext ctx = getExposedJAXBContext(tns);
        Unmarshaller um = ctx.createUnmarshaller();
        um.setEventHandler(null);
        try {
```

### DataFlowIssue
Argument `resourceName` might be null
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContextResourceResolver.java`
#### Snippet
```java
                return resourceType.cast(context.getClassLoader());
            } else if (URL.class.isAssignableFrom(resourceType)) {
                Resource r = context.getResource(resourceName);
                if (r != null && r.exists()) {
                    r.getInputStream().close(); //checks to see if the URL really can resolve
```

### DataFlowIssue
Argument `v.getValue()` might be null
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java
        for (PropertyValue v : src.getBeanDefinition().getPropertyValues().getPropertyValues()) {
            if (!"bus".equals(v.getName())) {
                def.getPropertyValues().addPropertyValue(v.getName(), v.getValue());
            }
            src.getBeanDefinition().getPropertyValues().removePropertyValue(v);
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `core/src/main/java/org/apache/cxf/bus/spring/BusDefinitionParser.java`
#### Snippet
```java
            addBusWiringAttribute(bean, BusWiringType.PROPERTY, bus, ctx);
            bean.getRawBeanDefinition().setAttribute(WIRE_BUS_CREATE,
                                                     resolveId(element, null, ctx));
            bean.addConstructorArgValue(bus);
        }
```

### DataFlowIssue
Method invocation `asSubclass` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        for (String s : list) {
            Class<?> beanType = context.getType(s);
            Class<? extends T> t = beanType.asSubclass(type);
            if (listener.loadBean(s, t)) {
                Object o = context.getBean(s);
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
    public static QName getPortQName(EndpointReferenceType ref, Bus bus) {
        QName serviceName = getServiceName(ref, bus);
        return new QName(serviceName.getNamespaceURI(), getPortName(ref));
    }

```

### DataFlowIssue
Argument `getPortName(ref)` might be null
in `core/src/main/java/org/apache/cxf/ws/addressing/EndpointReferenceUtils.java`
#### Snippet
```java
    public static QName getPortQName(EndpointReferenceType ref, Bus bus) {
        QName serviceName = getServiceName(ref, bus);
        return new QName(serviceName.getNamespaceURI(), getPortName(ref));
    }

```

### DataFlowIssue
Method invocation `getBeansOfType` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerBus.java`
#### Snippet
```java

        Collection<? extends BusCreationListener> ls = getExtension(ConfiguredBeanLocator.class)
            .getBeansOfType(BusCreationListener.class);
        for (BusCreationListener l : ls) {
            l.busCreated(this);
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
                        lastAfter = ih2;
                    }
                    if (!force && cmpId.equals(id)) {
                        // interceptor is already in chain
                        return;
```

### DataFlowIssue
Argument `cd` might be null
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
    public static String encode(byte[] id, boolean urlSafe) {
        char[] cd = encodeChunk(id, 0, id.length, urlSafe);
        return new String(cd, 0, cd.length);
    }

```

### DataFlowIssue
Argument `decodeChunk(cd, 0, cd.length)` might be null
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        try {
            char[] cd = id.toCharArray();
            ostream.write(decodeChunk(cd, 0, cd.length));
        } catch (IOException ioe) {
            throw new Base64Exception(new Message("BASE64_DECODE_IOEXCEPTION", LOG), ioe);
```

### DataFlowIssue
Array access `id[rindex]` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        final char[] base64Table = urlSafe ? BCS_URL_SAFE : BCS;
        while (rest >= 3) {
            int i = ((id[rindex] & 0xff) << 16)
                    + ((id[rindex + 1] & 0xff) << 8)
                    + (id[rindex + 2] & 0xff);
```

### DataFlowIssue
Array access `id[rindex]` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java

        if (rest == 1) {
            int i = id[rindex] & 0xff;
            out[windex++] = base64Table[i >> 2];
            out[windex++] = base64Table[(i << 4) & 0x3f];
```

### DataFlowIssue
Array access `id[rindex]` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
            }
        } else if (rest == 2) {
            int i = ((id[rindex] & 0xff) << 8) + (id[rindex + 1] & 0xff);
            out[windex++] = base64Table[i >> 10];
            out[windex++] = base64Table[(i >> 4) & 0x3f];
```

### DataFlowIssue
Argument `decodeChunk(id, o, l)` might be null
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java

        try {
            ostream.write(decodeChunk(id, o, l));
        } catch (Exception e) {
            LOG.warning("Invalid base64 encoded string : " + new String(id));
```

### DataFlowIssue
Array access `id[l - 1]` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/common/util/Base64Utility.java`
#### Snippet
```java
        // 1 or 2 octets.
        //
        if (id[l - 1] == PAD) {
            // TT== means last 4 chars encode 8 bits (ie subtract 2)
            // TTT= means last 4 chars encode 16 bits (ie subtract 1)
```

### DataFlowIssue
Method invocation `getFile` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
            boolean classpathSetted = !StringUtils.isEmpty(javaClasspath);
            if (!classpathSetted) {
                File f = new File(getClass().getClassLoader().getResource(".").getFile());
                f = new File(f, "../lib");
                if (f.exists() && f.isDirectory()) {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                break;
            default:
                ap = null;
            }
            if (ap != null) {
```

### DataFlowIssue
Method invocation `getPrefix` may produce `NullPointerException`
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
            if (name != null) {
                QName qname = DOMUtils.convertStringToQName(name.value(), name.prefix());
                if (qname.getPrefix().length() > 0) {
                    return qname;
                }
```

### DataFlowIssue
Argument `extAttrs` might be null
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
            // and now process our sequence.
            List<XmlSchemaAttributeOrGroupRef> extAttrs = getContentAttributes(type);
            results.addAll(extAttrs);
            return results;
        }
```

### DataFlowIssue
Argument `ins` might be null
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        }
        try (InputStream ins = loader.getResourceAsStream('/' + pkg.replace('.', '/') + "/jaxb.index");
            BufferedReader reader = new BufferedReader(new InputStreamReader(ins, StandardCharsets.UTF_8))) {
            if (!StringUtils.isEmpty(pkg)) {
                pkg += '.';
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
        final String tns;
        if (messagePartInfo.isElement()) {
            tns = messagePartInfo.getElementQName().getNamespaceURI();
        } else {
            tns = messagePartInfo.getTypeQName().getNamespaceURI();
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/service/model/ServiceModelUtil.java`
#### Snippet
```java
            tns = messagePartInfo.getElementQName().getNamespaceURI();
        } else {
            tns = messagePartInfo.getTypeQName().getNamespaceURI();
        }
        for (SchemaInfo schema : serviceInfo.getSchemas()) {
```

### DataFlowIssue
Argument `JAXBUtils.getParamClass(compiler, "setClassNameAllocator")` might be null
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
            Object allocator = ReflectionInvokationHandler
                .createProxyWrapper(new DefaultClassNameAllocator(allocatorSet),
                                JAXBUtils.getParamClass(compiler, "setClassNameAllocator"));

            compiler.setClassNameAllocator(allocator);
```

### DataFlowIssue
Argument `getFilePathFromUri(baseUriStr)` might be null
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java

                if (!baseFile.exists() && baseUriStr.startsWith("file:")) {
                    baseFile = new File(getFilePathFromUri(baseUriStr));
                }

```

### DataFlowIssue
Method invocation `setChildElements` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
            indentWithSpaces();
            CurrentElement elem = elems.peek();
            elem.setChildElements(true);
        }
        if (prefix == null && namespaceURI == null) {
```

### DataFlowIssue
Argument `group` might be null
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                            //the security manager will allow us to go.   Use that one.
                        }
                        return new ThreadGroup(group, nm + "-workqueue");
                    }
                }
```

### DataFlowIssue
Dereference of `decodedChars` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/attachment/Base64DecoderStream.java`
#### Snippet
```java
                throw new IOException(e);
            }
            decodedCount = decodedChars.length;
            return true;
        }
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                    e = doc.createElementNS(reader.getNamespaceURI(), reader.getLocalName());
                }
                e = (Element)parent.appendChild(e);
                recordLoc = addLocation(doc, e, reader, recordLoc);

```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            case XMLStreamConstants.CDATA:
                recordLoc = addLocation(doc,
                                        parent.appendChild(doc.createCDATASection(reader.getText())),
                                        reader, recordLoc);
                break;
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.PROCESSING_INSTRUCTION:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            default:
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                    e = doc.createElementNS(reader.getNamespaceURI(), reader.getLocalName());
                }
                e = (Element)parent.appendChild(e);
                if (context.isRecordLoc()) {
                    context.setRecordLoc(addLocation(doc, e, reader.getLocation(), context.isRecordLoc()));
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
            case XMLStreamConstants.CDATA:
                context.setRecordLoc(addLocation(doc,
                                        parent.appendChild(doc.createCDATASection(reader.getText())),
                                        reader.getLocation(), context.isRecordLoc()));
                break;
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.PROCESSING_INSTRUCTION:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
```

### DataFlowIssue
Method invocation `appendChild` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                break;
            case XMLStreamConstants.ENTITY_REFERENCE:
                parent.appendChild(doc.createProcessingInstruction(reader.getPITarget(), reader.getPIData()));
                break;
            default:
```

### DataFlowIssue
Argument `sysId` might be null
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
        } else {
            try {
                final URL url = new URL(sysId);
                final InputStream is = url.openStream();
                final StreamSource ss = new StreamSource(is, sysId);
```

### DataFlowIssue
Argument `bean` might be null
in `core/src/main/java/org/apache/cxf/validation/BeanValidationProvider.java`
#### Snippet
```java
            return;
        }
        final Set<ConstraintViolation< T > > violations = doValidateBean(validator, bean);
        if (!violations.isEmpty()) {
            throw new ResponseConstraintViolationException(violations);
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
            if (null != exchange) {
                Integer responseCode = (Integer)exchange.get(Message.RESPONSE_CODE);
                resContext.put(MessageContext.HTTP_RESPONSE_CODE, responseCode);
                resContext.put(org.apache.cxf.message.Message.RESPONSE_CODE, responseCode);
                setResponseContext(resContext);
```

### DataFlowIssue
Method invocation `getBytes` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                    String cid = DOMUtils.getAttribute(include, "href");
                    //set the fake base64Binary to validate instead of reading the attachment from message
                    parentNode.setTextContent(jakarta.xml.bind.DatatypeConverter.printBase64Binary(cid.getBytes()));
                }
            }
```

### DataFlowIssue
Method invocation `getUserPrincipal` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractSecurityContextInInterceptor.java`
#### Snippet
```java
        }

        Principal principal = getPrincipal(context.getUserPrincipal(), subject);
        SecurityContext sc = createSecurityContext(principal, subject);
        message.put(SecurityContext.class, sc);
```

### DataFlowIssue
Argument `subject` might be null
in `core/src/main/java/org/apache/cxf/interceptor/security/AbstractSecurityContextInInterceptor.java`
#### Snippet
```java
        }

        Principal principal = getPrincipal(context.getUserPrincipal(), subject);
        SecurityContext sc = createSecurityContext(principal, subject);
        message.put(SecurityContext.class, sc);
```

### DataFlowIssue
Method invocation `getExtension` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java
        if (is == null) {
            Bus bus = BusFactory.getThreadDefaultBus(true);
            ResourceManager rm = bus.getExtension(ResourceManager.class);
            if (rm != null) {
                is = rm.getResourceAsStream(resource);
```

### DataFlowIssue
Argument `keyStore` might be null
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java

        if (enableRevocation) {
            PKIXBuilderParameters param = new PKIXBuilderParameters(keyStore, new X509CertSelector());
            param.setRevocationEnabled(true);

```

### DataFlowIssue
Argument `keyPass` might be null
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java
            MultiKeyPasswordKeyManager manager
                = new MultiKeyPasswordKeyManager(keyStore, alias,
                                             new String(keyPass));
            return new KeyManager[]{manager};
        }
```

### DataFlowIssue
Method invocation `getBeanDefinition` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
            } else if ("bean".equals(name)) {
                BeanDefinitionHolder bdh = ctx.getDelegate().parseBeanDefinitionElement(first);
                child = bdh.getBeanDefinition();
                bean.addPropertyValue(propertyName, child);
                return;
```

### DataFlowIssue
Method invocation `getExtension` may produce `NullPointerException`
in `core/src/main/java/org/apache/cxf/configuration/jsse/SSLUtils.java`
#### Snippet
```java
        if (is == null) {
            Bus bus = BusFactory.getThreadDefaultBus(true);
            ResourceManager rm = bus.getExtension(ResourceManager.class);
            if (rm != null) {
                is = rm.getResourceAsStream(resource);
```

### DataFlowIssue
Method invocation `cast` may produce `NullPointerException`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AegisElementProvider.java`
#### Snippet
```java
        try {
            xmlStreamReader = createStreamReader(typeToRead, is);
            return type.cast(aegisReader.read(xmlStreamReader, typeToRead));
        } catch (Exception e) {
            throw ExceptionUtils.toBadRequestException(e, null);
```

### DataFlowIssue
Method invocation `getConstructorArgumentValues` may produce `NullPointerException`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java
        ValueHolder busValue = ctx.getContainingBeanDefinition()
            .getConstructorArgumentValues().getArgumentValue(0, Bus.class);
        bean.addPropertyValue("bus", busValue.getValue());
        try {
            Element elem = DOMUtils.getFirstElement(element);
```

### DataFlowIssue
Method invocation `getVersion` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/manager/MemoryResourceManager.java`
#### Snippet
```java
        WrappedMessageContext wmc = (WrappedMessageContext) context.getMessageContext();
        SoapMessage message = (SoapMessage) wmc.getWrappedMessage();
        return message.getVersion();
    }
}
```

### DataFlowIssue
Method invocation `getVersion` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/validationtransformation/XSLTResourceTransformer.java`
#### Snippet
```java
        WrappedMessageContext wmc = (WrappedMessageContext) context.getMessageContext();
        SoapMessage message = (SoapMessage) wmc.getWrappedMessage();
        return message.getVersion();
    }

```

### DataFlowIssue
Method invocation `getContextualProperty` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/resource/ResourceLocal.java`
#### Snippet
```java
        AddressingProperties addrProps = (AddressingProperties) ((WrappedMessageContext) context
                .getMessageContext()).getWrappedMessage()
                .getContextualProperty(JAXWSAConstants.ADDRESSING_PROPERTIES_INBOUND);
        ReferenceParametersType refParams = addrProps
                .getToEndpointReference()
```

### DataFlowIssue
Method invocation `getContextualProperty` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/resource/ResourceLocal.java`
#### Snippet
```java
        AddressingProperties addrProps = (AddressingProperties) ((WrappedMessageContext) context
                .getMessageContext()).getWrappedMessage()
                .getContextualProperty(JAXWSAConstants.ADDRESSING_PROPERTIES_INBOUND);
        ReferenceParametersType refParams = addrProps
                .getToEndpointReference()
```

### DataFlowIssue
Method invocation `getContextualProperty` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/resource/ResourceLocal.java`
#### Snippet
```java
        AddressingProperties addrProps = (AddressingProperties) ((WrappedMessageContext) context
                .getMessageContext()).getWrappedMessage()
                .getContextualProperty(JAXWSAConstants.ADDRESSING_PROPERTIES_INBOUND);
        ReferenceParametersType refParams = addrProps
                .getToEndpointReference()
```

### DataFlowIssue
Method invocation `getVersion` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/validationtransformation/XSDResourceTypeIdentifier.java`
#### Snippet
```java
        WrappedMessageContext wmc = (WrappedMessageContext) context.getMessageContext();
        SoapMessage message = (SoapMessage) wmc.getWrappedMessage();
        return message.getVersion();
    }
}
```

### DataFlowIssue
Method invocation `getVersion` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/validationtransformation/XSDResourceValidator.java`
#### Snippet
```java
        WrappedMessageContext wmc = (WrappedMessageContext) context.getMessageContext();
        SoapMessage message = (SoapMessage) wmc.getWrappedMessage();
        return message.getVersion();
    }
}
```

### DataFlowIssue
Method invocation `getVersion` may produce `NullPointerException`
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/FragmentDialect.java`
#### Snippet
```java
        WrappedMessageContext wmc = (WrappedMessageContext) context.getMessageContext();
        SoapMessage message = (SoapMessage) wmc.getWrappedMessage();
        return message.getVersion();
    }
}
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/WebFaultInInterceptor.java`
#### Snippet
```java
                final String ns;
                if (mpi.isElement()) {
                    ns = mpi.getElementQName().getNamespaceURI();
                } else {
                    ns = mpi.getTypeQName().getNamespaceURI();
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/WebFaultInInterceptor.java`
#### Snippet
```java
                    ns = mpi.getElementQName().getNamespaceURI();
                } else {
                    ns = mpi.getTypeQName().getNamespaceURI();
                }
                if (qname.getLocalPart().equals(mpi.getConcreteName().getLocalPart())
```

### DataFlowIssue
Method invocation `getLocalName` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSClient.java`
#### Snippet
```java
            el = DOMUtils.getFirstElement(el);
        }
        if (!"RequestSecurityTokenResponse".equals(el.getLocalName())) {
            throw new Fault("Unexpected element " + el.getLocalName(), LOG);
        }
```

### DataFlowIssue
Argument `epr` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
            }

            if (preferWSMex && findMEXLocation(epr) != null) {
                // WS-MEX call. So now either get the WS-MEX specific STSClient or else create one
                STSClient wsMexClient =
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSStaxTokenValidator.java`
#### Snippet
```java
        if (alwaysValidateToSts) {
            Element tokenElement =
                convertToDOM(username.getValue(), passwordType.getValue(),
                             passwordType.getType(), usernameTokenType.getId());
            validateTokenToSTS(tokenElement, message);
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSStaxTokenValidator.java`
#### Snippet
```java
            } catch (WSSecurityException ex) {
                Element tokenElement =
                    convertToDOM(username.getValue(), passwordType.getValue(),
                                 passwordType.getType(), usernameTokenType.getId());
                validateTokenToSTS(tokenElement, message);
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
                    securedAttachment.setHeader(entry.getKey(), entry.getValue());
                }
                attachments.add(securedAttachment);

            } else if (callback instanceof AttachmentRemovalCallback) {
```

### DataFlowIssue
Method invocation `getAsymmetricSignature` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AlgorithmSuiteTranslater.java`
#### Snippet
```java
            }

            algorithmSuite.addSignatureMethod(algorithmSuiteType.getAsymmetricSignature());
            algorithmSuite.addSignatureMethod(algorithmSuiteType.getSymmetricSignature());
            algorithmSuite.addC14nAlgorithm(cxfAlgorithmSuite.getC14n().getValue());
```

### DataFlowIssue
Argument `incomingSecurityEventList` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
                // We only insist on the Timestamp being signed if it is actually present in the message
                List<SecurityEvent> timestampResults =
                    getEventFromResults(WSSecurityEventConstants.TIMESTAMP, incomingSecurityEventList);
                if (!timestampResults.isEmpty()) {
                    checkSignedTimestamp(results);
```

### DataFlowIssue
Argument `incomingSecurityEventList` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
                // present in the message
                List<SecurityEvent> usernameTokenResults =
                    getEventFromResults(WSSecurityEventConstants.USERNAME_TOKEN, incomingSecurityEventList);
                if (!usernameTokenResults.isEmpty()) {
                    if (signUsernameToken) {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/SamlTokenInterceptor.java`
#### Snippet
```java
                    .loadClass((String)o, this.getClass()).getDeclaredConstructor().newInstance();
            } catch (Exception e) {
                handler = null;
            }
        }
```

### DataFlowIssue
Method invocation `getNonce` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
            }

            if (tok.isNonce() && princ.getNonce() == null) {
                ai.setNotAsserted("No Nonce");
            } else {
```

### DataFlowIssue
Argument `el` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
                //which only checks for Document as parent, not DocumentFragment
                Element element = ctx.getOwnerDocument().createElementNS("dummy", "dummy");
                XMLStreamWriter writer = new SAAJStreamWriter((SOAPEnvelope)el, element);
                return appendNewChild(reader, wrapped, contextDocument, writer, element);
            }
```

### DataFlowIssue
Method invocation `getLocalName` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            el = DOMUtils.getFirstElement(el);
        }
        if (!"RequestSecurityTokenResponse".equals(el.getLocalName())) {
            throw new Fault("Unexpected element " + el.getLocalName(), LOG);
        }
```

### DataFlowIssue
Argument `child` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
        if (rpt != null) {
            Element child = DOMUtils.getFirstElement(rpt);
            QName childQname = DOMUtils.getElementQName(child);
            if (childQname.equals(new QName(namespace, "BinarySecret"))) {
                // First check for the binary secret
```

### DataFlowIssue
Method invocation `clean` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AsymmetricBindingHandler.java`
#### Snippet
```java
                    }

                    encr.clean();
                }
                assertTokenWrapper(encToken);
```

### DataFlowIssue
Argument `attribute.getAttributeNamespace()` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
            for (org.opensaml.saml.saml1.core.Attribute attribute : attributes) {

                URI attributeNamespace = URI.create(attribute.getAttributeNamespace());
                String desiredRole = attributeNamespace.relativize(claimURI).toString();
                if (attribute.getAttributeName().equals(desiredRole)
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
                URI attributeNamespace = URI.create(attribute.getAttributeNamespace());
                String desiredRole = attributeNamespace.relativize(claimURI).toString();
                if (attribute.getAttributeName().equals(desiredRole)
                    && attribute.getAttributeValues() != null && !attribute.getAttributeValues().isEmpty()) {
                    return true;
```

### DataFlowIssue
Method invocation `getToken` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
        // Get the kerberos token from the element
        byte[] data = null;
        if (secToken.getToken() != null) {
            String text = XMLUtils.getElementText(secToken.getToken());
            if (text != null) {
```

### DataFlowIssue
Method invocation `getLocalName` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/custom/AlgorithmSuiteBuilder.java`
#### Snippet
```java
            }
            if (algorithmSuiteName == null) {
                algorithmSuiteName = DOMUtils.getFirstElement(nestedPolicyElement).getLocalName();
            }
            throw new IllegalArgumentException(
```

### DataFlowIssue
Method invocation `getId` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
                    TokenStore tokenStore = TokenStoreUtils.getTokenStore(m2);
                    if (tokenStore != null) {
                        tokenStore.remove(tok.getId());
                    }
                    m2.put(SecurityConstants.TOKEN, null);
```

### DataFlowIssue
Method invocation `getAlgorithmSuite` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java

                AbstractBinding origBinding = PolicyUtils.getSecurityBinding(aim);
                bindingPolicyAll.addPolicyComponent(origBinding.getAlgorithmSuite());
                bindingPolicyAll.addPolicyComponent(new ProtectionToken(SPConstants.SPVersion.SP12, sctPolicy));
                bindingPolicyAll.addAssertion(
```

### DataFlowIssue
Method invocation `getLocalName` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/STSInvoker.java`
#### Snippet
```java
        Element childElement = DOMUtils.getFirstElement(el);
        final String uri;
        if ("SecurityContextToken".equals(childElement.getLocalName())) {
            SecurityContextToken sct = new SecurityContextToken(childElement);
            uri = sct.getIdentifier();
```

### DataFlowIssue
Method invocation `getToken` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                    unassertPolicy(token, "Could not find IssuedToken");
                }
                Element clone = cloneElement(secToken.getToken());
                secToken.setToken(clone);
                addSupportingElement(clone);
```

### DataFlowIssue
Argument `soapBody` might be null
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        if (includeBody && !found.contains(soapBody)) {
            found.add(soapBody);
            final String id = this.addWsuIdToElement(soapBody);
            if (sign) {
                WSEncryptionPart bodyPart = new WSEncryptionPart(id, "Element");
```

### DataFlowIssue
Method invocation `setNotAsserted` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            if (binding.getLayout().getLayoutType() == LayoutType.LaxTsLast) {
                if (timestamp == null) {
                    ai.setNotAsserted(SPConstants.LAYOUT_LAX_TIMESTAMP_LAST + " requires a timestamp");
                } else {
                    ai.setAsserted(true);
```

### DataFlowIssue
Method invocation `setAsserted` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                    ai.setNotAsserted(SPConstants.LAYOUT_LAX_TIMESTAMP_LAST + " requires a timestamp");
                } else {
                    ai.setAsserted(true);
                    assertPolicy(
                        new QName(binding.getLayout().getName().getNamespaceURI(),
```

### DataFlowIssue
Method invocation `setNotAsserted` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            } else if (binding.getLayout().getLayoutType() == LayoutType.LaxTsFirst) {
                if (timestamp == null) {
                    ai.setNotAsserted(SPConstants.LAYOUT_LAX_TIMESTAMP_FIRST + " requires a timestamp");
                } else {
                    addTopDownElement(timestampEl.getElement());
```

### DataFlowIssue
Method invocation `setAsserted` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                } else {
                    addTopDownElement(timestampEl.getElement());
                    ai.setAsserted(true);
                    assertPolicy(
                         new QName(binding.getLayout().getName().getNamespaceURI(),
```

### DataFlowIssue
Dereference of `policies` may produce `NullPointerException`
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXEndpoint.java`
#### Snippet
```java
        } else if ("http://schemas.xmlsoap.org/ws/2004/09/policy".equals(dialect)) {
            List<Element> policies = MEXUtils.getPolicies(server, id);
            for (Element el : policies) {
                org.apache.cxf.ws.mex.model._2004_09.MetadataSection sect
                    = new org.apache.cxf.ws.mex.model._2004_09.MetadataSection();
```

### DataFlowIssue
Method invocation `clean` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                }

                encr.clean();
            }
        } catch (Exception e) {
```

### DataFlowIssue
Casting `encr` to `WSSecDKEncrypt` may produce `ClassCastException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java

                        if (encryptionToken.getDerivedKeys() == DerivedKeys.RequireDerivedKeys) {
                            secondRefList = ((WSSecDKEncrypt)encr).encryptForExternalRef(null, secondEncrParts);
                        } else {
                            //Encrypt, get hold of the ref list and add it
```

### DataFlowIssue
Method invocation `encryptForExternalRef` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java

                        if (encryptionToken.getDerivedKeys() == DerivedKeys.RequireDerivedKeys) {
                            secondRefList = ((WSSecDKEncrypt)encr).encryptForExternalRef(null, secondEncrParts);
                        } else {
                            //Encrypt, get hold of the ref list and add it
```

### DataFlowIssue
Casting `encr` to `WSSecEncrypt` may produce `ClassCastException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                        } else {
                            //Encrypt, get hold of the ref list and add it
                            secondRefList = ((WSSecEncrypt)encr).encryptForRef(null, secondEncrParts, symmetricKey);
                        }
                        if (secondRefList != null) {
```

### DataFlowIssue
Method invocation `encryptForRef` may produce `NullPointerException`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                        } else {
                            //Encrypt, get hold of the ref list and add it
                            secondRefList = ((WSSecEncrypt)encr).encryptForRef(null, secondEncrParts, symmetricKey);
                        }
                        if (secondRefList != null) {
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSUtil.java`
#### Snippet
```java
            jakarta.jms.Message replyMessage = consumer.receive(receiveTimeout);
            if (replyMessage == null) {
                if ((boolean)exchange.get(JMSUtil.JMS_IGNORE_TIMEOUT)) {
                    throw new RuntimeException("Timeout receiving message with correlationId "
                                           + correlationId);
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/TypeClassInitializer.java`
#### Snippet
```java
                    while (i.hasNext()) {
                        JType jt = i.next();
                        if (jt.name().equalsIgnoreCase(part.getElementQName().getLocalPart())) {
                            jType = jt;
                        }
```

### DataFlowIssue
Argument `paramValue` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/NewCookieHeaderProvider.java`
#### Snippet
```java

            if (paramName.equalsIgnoreCase(MAX_AGE)) {
                maxAge = Integer.parseInt(paramValue);
            } else if (paramName.equalsIgnoreCase(PATH)) {
                path = paramValue;
```

### DataFlowIssue
Argument `paramValue` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/NewCookieHeaderProvider.java`
#### Snippet
```java
                httpOnly = true;
            } else if (paramName.equalsIgnoreCase(VERSION)) {
                version = Integer.parseInt(paramValue);
            } else if (paramValue != null) {
                name = paramName;
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/WSDLGetUtils.java`
#### Snippet
```java
            for (Map.Entry<String, SchemaReference> e : doneSchemas.entrySet()) {
                if ((e.getValue().getReferencedSchema().getElement() == imp.getReferencedSchema().getElement())
                    && schemaLocationURI.equals(e.getKey())) {
                    doneSchemas.put(schemaLocationURI, imp);
                    imp.setSchemaLocationURI(e.getKey());
```

### DataFlowIssue
Immutable object is modified
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
            theMap.put(var, oval);
            if (!isQuery && (isPathEncVar || encodePathSlash)) {
                pathEncodeVars.add(var);
            }
        }
```

### DataFlowIssue
Method invocation `getReturnType` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXRSDataBinding.java`
#### Snippet
```java
                MultivaluedMap<String, Object> headers = getWriteHeaders(message);
                xmlWriter.writeTo(obj,
                                 method.getReturnType(),
                                 method.getGenericReturnType(),
                                 method.getAnnotations(),
```

### DataFlowIssue
Method invocation `getGenericParameterTypes` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXRSDataBinding.java`
#### Snippet
```java

            return reader.readFrom(cls,
                                      method.getGenericParameterTypes()[0],
                                      method.getParameterTypes()[0].getAnnotations(),
                                      MediaType.APPLICATION_ATOM_XML_TYPE,
```

### DataFlowIssue
Argument `JAXBUtils.getParamClass(compiler, "setErrorListener")` might be null
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java
        Object elForRun = ReflectionInvokationHandler
            .createProxyWrapper(listener,
                                JAXBUtils.getParamClass(compiler, "setErrorListener"));

        compiler.setErrorListener(elForRun);
```

### DataFlowIssue
Argument `rawType` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
    private static boolean isSupportedMap(Type genericType) {
        Class<?> rawType = getRawType(genericType);
        if (Map.class.isAssignableFrom(rawType) && genericType instanceof ParameterizedType) {
            ParameterizedType paramType = (ParameterizedType) genericType;
            if (paramType.getActualTypeArguments().length == 2) {
```

### DataFlowIssue
Argument `DOMUtils.getChild(f.getDetail(), Node.ELEMENT_NODE)` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSDefaultFaultOutInterceptor.java`
#### Snippet
```java
            if (f.getDetail() != null) {
                StaxUtils.writeStartElement(writer, prefix, "detail", "http://cxf.apache.org/bindings/xformat");
                StaxUtils.writeNode(DOMUtils.getChild(f.getDetail(), Node.ELEMENT_NODE),
                                    writer, false);
                writer.writeEndElement();
```

### DataFlowIssue
Argument `ori` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSInInterceptor.java`
#### Snippet
```java
            ori = JAXRSUtils.findTargetMethod(matchedResources, message,
                      httpMethod, matchedValues, requestContentType, acceptContentTypes, true, true);
            setExchangeProperties(message, exchange, ori, matchedValues, resources.size());
        } catch (WebApplicationException ex) {
            if (JAXRSUtils.noResourceMethodForOptions(ex.getResponse(), httpMethod)) {
```

### DataFlowIssue
Method invocation `getCollectionOrArray` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
            }
            if (isCollection) {
                response = ((CollectionWrapper)response).getCollectionOrArray(
                                 unmarshaller, theType, type, genericType,
                                 org.apache.cxf.jaxrs.utils.JAXBUtils.getAdapter(theGenericType, anns));
```

### DataFlowIssue
Argument `actualType` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                } else if (commonBaseCls != null && commonBaseCls != Object.class
                           && commonBaseCls.isAssignableFrom(expectedClass)
                           && commonBaseCls.isAssignableFrom(actualType)
                           || expectedClass.isAssignableFrom(actualType)) {
                    return new Type[]{};
```

### DataFlowIssue
Argument `actualType` might be null
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/ProviderFactory.java`
#### Snippet
```java
                           && commonBaseCls.isAssignableFrom(expectedClass)
                           && commonBaseCls.isAssignableFrom(actualType)
                           || expectedClass.isAssignableFrom(actualType)) {
                    return new Type[]{};
                }
```

### DataFlowIssue
Method invocation `getTemplates` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
        } catch (TransformerConfigurationException ex) {
            TemplatesImpl ti = (TemplatesImpl)t;
            th = factory.newTransformerHandler(ti.getTemplates());
            this.trySettingProperties(th, ti);
        }
```

### DataFlowIssue
Method invocation `newTransformer` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
                Source reader = new StaxSource(StaxUtils.createXMLStreamReader(is));
                DOMResult dom = new DOMResult();
                t.newTransformer().transform(reader, dom);
                return unmarshaller.unmarshal(dom.getNode());
            }
```

### DataFlowIssue
Method invocation `getTemplates` may produce `NullPointerException`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            } catch (TransformerConfigurationException ex) {
                TemplatesImpl ti = (TemplatesImpl)t;
                filter = factory.newXMLFilter(ti.getTemplates());
                trySettingProperties(filter, ti);
            }
```

### DataFlowIssue
Method invocation `createJwsSignature` may produce `NullPointerException`
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/multipart/AbstractJwsMultipartSignatureFilter.java`
#### Snippet
```java
        JwsSignatureProvider theSigProvider = sigProvider != null ? sigProvider
            : JwsUtils.loadSignatureProvider(headers, true);
        JwsSignature jwsSignature = theSigProvider.createJwsSignature(headers);
        
        String base64UrlEncodedHeaders = Base64UrlUtility.encode(writer.toJson(headers));
```

### DataFlowIssue
Method invocation `getExchange` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WebServiceContextImpl.java`
#### Snippet
```java
        WrappedMessageContext ctx = (WrappedMessageContext)getMessageContext();
        org.apache.cxf.message.Message msg = ctx.getWrappedMessage();
        Endpoint ep = msg.getExchange().getEndpoint();

        W3CEndpointReferenceBuilder builder = new W3CEndpointReferenceBuilder();
```

### DataFlowIssue
Argument `SAAJUtils.getBody(soapMessage)` might be null
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
        try {
            // we only care about the first element node, not text nodes
            Element element = DOMUtils.getFirstElement(SAAJUtils.getBody(soapMessage));
            if (element != null) {
                return DOMUtils.getElementQName(element);
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                            payloadElementMap.put(op.toString(), qn);
                        } else {
                            payloadElementMap.put(qn.toString(), op);
                        }
                    }
```

### DataFlowIssue
Condition `e instanceof AddressType` is redundant and can be replaced with a null check
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    if (e instanceof SoapAddress) {
                        address = ((SoapAddress)e).getLocationURI();
                    } else if (e instanceof AddressType) {
                        address = ((AddressType)e).getLocation();
                    } else if (e instanceof SOAP12Address) {
```

### DataFlowIssue
Method invocation `setContent` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
    private void setFaultMessage(MessageContext mc, Exception exception) {
        Message msg = ((WrappedMessageContext)mc).getWrappedMessage();
        msg.setContent(Exception.class, exception);
        msg.removeContent(XMLStreamReader.class);
        msg.removeContent(Source.class);
```

### DataFlowIssue
Method invocation `addFault` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java

            SOAPBody body = SAAJUtils.getBody(soapMessage);
            SOAPFault soapFault = body.addFault();

            if (exception instanceof SOAPFaultException) {
```

### DataFlowIssue
Casting `msg` to `SoapMessage` may produce `ClassCastException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
                }
            } else if (exception instanceof Fault) {
                SoapFault sf = SoapFault.createFault((Fault)exception, ((SoapMessage)msg).getVersion());
                soapFault.setFaultString(sf.getReason());
                SAAJUtils.setFaultCode(soapFault, sf.getFaultCode());
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
            if (MessageContext.INBOUND_MESSAGE_ATTACHMENTS.equals(key)) {
                if (isRequestor() && isOutbound()) {
                    ret = null;
                } else if (isOutbound()) {
                    ret = createAttachments(reqMessage,
```

### DataFlowIssue
Method invocation `getContent` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java
    public SOAPMessage getMessage() {
        SOAPMessage message;
        if (getWrappedMessage().getContent(Object.class) instanceof SOAPMessage) {
            message = (SOAPMessage)getWrappedMessage().getContent(Object.class);
        } else {
```

### DataFlowIssue
Method invocation `getContent` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPMessageContextImpl.java`
#### Snippet
```java

    public void setMessage(SOAPMessage message) {
        if (getWrappedMessage().getContent(Object.class) instanceof SOAPMessage) {
            getWrappedMessage().setContent(Object.class, message);
        } else {
```

### DataFlowIssue
Method invocation `getExchange` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
        Source source;

        Service.Mode mode = msgContext.getWrappedMessage().getExchange().get(Service.Mode.class);

        if (mode != null) {
```

### DataFlowIssue
Method invocation `getFirstChild` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                if (msg != null) {
                    try {
                        Node node = SAAJUtils.getBody(msg).getFirstChild();
                        while (node != null && !(node instanceof Element))  {
                            node = node.getNextSibling();
```

### DataFlowIssue
Method invocation `getContextualProperty` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
        Message message = msgContext.getWrappedMessage();
        Service.Mode mode = (Service.Mode)msgContext.getWrappedMessage()
            .getContextualProperty(Service.Mode.class.getName());
        SOAPMessage m = message.getContent(SOAPMessage.class);
        if (m != null && !MessageUtils.isOutbound(message)) {
```

### DataFlowIssue
Method invocation `removeContents` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
        if (m != null && !MessageUtils.isOutbound(message)) {
            try {
                SAAJUtils.getBody(m).removeContents();
                W3CDOMStreamWriter writer = new W3CDOMStreamWriter(SAAJUtils.getBody(m));
                StaxUtils.copy(s, writer);
```

### DataFlowIssue
Argument `SAAJUtils.getBody(m)` might be null
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
            try {
                SAAJUtils.getBody(m).removeContents();
                W3CDOMStreamWriter writer = new W3CDOMStreamWriter(SAAJUtils.getBody(m));
                StaxUtils.copy(s, writer);
                writer.flush();
```

### DataFlowIssue
Method invocation `getFirstChild` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                    s = new DOMSource(m.getSOAPPart());
                } else {
                    s = new DOMSource(SAAJUtils.getBody(m).getFirstChild());
                }
                W3CDOMStreamReader r = new W3CDOMStreamReader(DOMUtils.getFirstElement(SAAJUtils.getBody(m)));
```

### DataFlowIssue
Argument `SAAJUtils.getBody(m)` might be null
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                    s = new DOMSource(SAAJUtils.getBody(m).getFirstChild());
                }
                W3CDOMStreamReader r = new W3CDOMStreamReader(DOMUtils.getFirstElement(SAAJUtils.getBody(m)));
                message.setContent(XMLStreamReader.class, r);
            } catch (Exception e) {
```

### DataFlowIssue
Method invocation `getContent` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
        Source source = null;
        Message message = msgContext.getWrappedMessage();
        Source obj = message.getContent(Source.class);
        if (message instanceof SoapMessage) {
            // StreamSource may only be used once, need to make a copy
```

### DataFlowIssue
Method invocation `getFirstChild` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
                    InputStream in = cos.getInputStream();
                    SOAPMessage msg = initSOAPMessage(in);
                    source = new DOMSource(SAAJUtils.getBody(msg).getFirstChild());
                    in.close();
                } catch (Exception e) {
```

### DataFlowIssue
Argument `name` might be null
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsImplementorInfo.java`
#### Snippet
```java
        }

        return new QName(namespace, name);
    }

```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
                final String ns;
                if (mpi.isElement()) {
                    ns = mpi.getElementQName().getNamespaceURI();
                } else {
                    ns = mpi.getTypeQName().getNamespaceURI();
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WebFaultOutInterceptor.java`
#### Snippet
```java
                    ns = mpi.getElementQName().getNamespaceURI();
                } else {
                    ns = mpi.getTypeQName().getNamespaceURI();
                }
                if (qname.getLocalPart().equals(mpi.getConcreteName().getLocalPart())
```

### DataFlowIssue
Method invocation `hasValue` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/WrapperClassOutInterceptor.java`
#### Snippet
```java
                    if (Boolean.TRUE.equals(p.getProperty(ReflectionServiceFactoryBean.HEADER))) {
                        MessagePartInfo mpi = wrappedMsgInfo.getMessagePart(p.getName());
                        if (objs.hasValue(p)) {
                            newObjs.put(mpi, objs.get(p));
                        }
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAInInterceptor.java`
#### Snippet
```java
                    }

                    inObjects.put(mpi, o);
                    found = true;
                    break;
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/SwAInInterceptor.java`
#### Snippet
```java

            if (!found) {
                inObjects.put(mpi, null);
            }
        }
```

### DataFlowIssue
Method invocation `set` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/HolderOutInterceptor.java`
#### Snippet
```java
                        Object o = inObjects.get(x);
                        if (o instanceof Holder) {
                            outObjects.set(x + 1, o);
                        }
                    }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/HolderOutInterceptor.java`
#### Snippet
```java
            for (MessagePartInfo part : parts) {
                if (part.getIndex() > 0 && part.getTypeClass() != null) {
                    Holder<?> holder = (Holder<?>)outObjects.get(part);
                    outObjects.put(part, holder.value);
                }
```

### DataFlowIssue
Argument `outObjects` might be null
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/HolderOutInterceptor.java`
#### Snippet
```java
            }
        } else {
            List<Object> holders = new ArrayList<>(outObjects);
            for (int x = 0; x < outObjects.size(); x++) {
                Object o = outObjects.get(x);
```

### DataFlowIssue
Method invocation `getFirstChild` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
            try {
                Node body = SAAJUtils.getBody(soapMessage);
                Node nd = body.getFirstChild();
                while (nd != null) {
                    if (nd instanceof SOAPFault) {
```

### DataFlowIssue
Method invocation `getFirstChild` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
                    nd = DOMUtils.getDomElement(nd);
                    frag.appendChild(nd);
                    nd = SAAJUtils.getBody(soapMessage).getFirstChild();
                }

```

### DataFlowIssue
Method invocation `getLocalName` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
    private void validatePossibleFault(Message message, BindingOperationInfo bop, Node ds) {
        Element el = DOMUtils.getFirstElement(ds);
        if (!"Fault".equals(el.getLocalName())) {
            return;
        }
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/HolderInInterceptor.java`
#### Snippet
```java
                    Holder<Object> holder = (Holder<Object>)outHolders.get(part.getIndex() - 1);
                    if (holder != null) {
                        holder.value = inObjects.get(part);
                        inObjects.put(part, holder);
                    }
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Servant.java`
#### Snippet
```java
            Duration effectiveDuration = ex.getValue();
            // PT0S represents 0 second and the shortest duration but in ws-rm, considered the longest
            if (DatatypeFactory.PT0S.equals(effectiveDuration)
                || (!DatatypeFactory.PT0S.equals(supportedDuration)
                    &&  supportedDuration.isShorterThan(effectiveDuration)))  {
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    public void acknowledge(Message message) throws SequenceFault {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, false);
        SequenceType st = rmps.getSequence();
        long messageNumber = st.getMessageNumber();
        LOG.fine("Acknowledging message: " + messageNumber);
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMCaptureOutInterceptor.java`
#### Snippet
```java

                RMProperties rmps = RMContextUtils.retrieveRMProperties(message, true);
                SequenceType sequence = rmps.getSequence();
                Long number = sequence.getMessageNumber();
                Identifier sid = sequence.getIdentifier();
```

### DataFlowIssue
Method invocation `getConstants` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMCaptureOutInterceptor.java`
#### Snippet
```java
        boolean isApplicationMessage = !RMContextUtils.isRMProtocolMessage(action);
        boolean isPartialResponse = MessageUtils.isPartialResponse(msg);
        RMConstants constants = protocol.getConstants();
        boolean isLastMessage = RM10Constants.CLOSE_SEQUENCE_ACTION.equals(action);

```

### DataFlowIssue
Method invocation `getAcksRequested` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Destination.java`
#### Snippet
```java
        // TODO
        Collection<AckRequestedType> ars = RMContextUtils.retrieveRMProperties(message, false)
            .getAcksRequested();
        if (null == ars) {
            return;
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Destination.java`
#### Snippet
```java
    public void acknowledge(Message message) throws SequenceFault, RMException {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, false);
        SequenceType sequenceType = rmps.getSequence();
        if (null == sequenceType) {
            return;
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Destination.java`
#### Snippet
```java
    void releaseDeliveringStatus(Message message) {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, false);
        SequenceType sequenceType = rmps.getSequence();
        if (null != sequenceType) {
            DestinationSequence seq = getSequence(sequenceType.getIdentifier());
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Destination.java`
#### Snippet
```java

    void processingComplete(Message message) {
        SequenceType sequenceType = RMContextUtils.retrieveRMProperties(message, false).getSequence();
        if (null == sequenceType) {
            return;
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMInInterceptor.java`
#### Snippet
```java

        // get the wsa and wsrm namespaces from the message
        String rmUri = rmps.getNamespaceURI();
        String addrUri = maps.getNamespaceURI();

```

### DataFlowIssue
Argument `protocol` might be null
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMInInterceptor.java`
#### Snippet
```java
                Object csr = servant.createSequence(message);
                Proxy proxy = rme.getProxy();
                proxy.createSequenceResponse(csr, protocol);
                return;
            }
```

### DataFlowIssue
Method invocation `getConstants` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMOutInterceptor.java`
#### Snippet
```java
        boolean isApplicationMessage = !RMContextUtils.isRMProtocolMessage(action);
        boolean isPartialResponse = MessageUtils.isPartialResponse(msg);
        RMConstants constants = protocol.getConstants();
        RMProperties rmpsOut = RMContextUtils.retrieveRMProperties(msg, true);

```

### DataFlowIssue
Method invocation `getAcks` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMOutInterceptor.java`
#### Snippet
```java
            assert null != to;
            addAcknowledgements(destination, rmpsOut, inSeqId, to);
            if (isPartialResponse && rmpsOut.getAcks() != null && rmpsOut.getAcks().size() > 0) {
                setAction(maps, constants.getSequenceAckAction());
                msg.remove(Message.EMPTY_PARTIAL_RESPONSE_MESSAGE);
```

### DataFlowIssue
Method invocation `getPortName` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
        ei.setAddress(aei.getAddress());

        ei.setName(RMUtils.getConstants(protocol.getWSRMNamespace()).getPortName());
        ei.setBinding(si.getBinding(bindingQName));

```

### DataFlowIssue
Method invocation `openStream` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
                javax.xml.transform.Source ad = new StreamSource(RMEndpoint.class
                                             .getResource("/schemas/wsdl/addressing.xsd")
                                             .openStream(),
                                             "http://schemas.xmlsoap.org/ws/2004/08/addressing");
                javax.xml.transform.Source rm = new StreamSource(RMEndpoint.class
```

### DataFlowIssue
Method invocation `openStream` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMEndpoint.java`
#### Snippet
```java
                javax.xml.transform.Source rm = new StreamSource(RMEndpoint.class
                                                                 .getResource("/schemas/wsdl/wsrm.xsd")
                                                                 .openStream());

                javax.xml.transform.Source[] schemas = new javax.xml.transform.Source[] {ad, rm};
```

### DataFlowIssue
Method invocation `size` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
            return;
        }
        LOG.log(Level.FINE, "Number of source sequences: {0}", sss.size());
        LOG.log(Level.FINE, "Number of destination sequences: {0}", dss.size());

```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapOutInterceptor.java`
#### Snippet
```java
            RMProperties rmps = RMContextUtils.retrieveRMProperties(inmsg, false);
            AddressingProperties maps = RMContextUtils.retrieveMAPs(inmsg, false, false);
            ProtocolVariation protocol = ProtocolVariation.findVariant(rmps.getNamespaceURI(),
                maps.getNamespaceURI());
            Header header = protocol.getCodec().buildHeaderFault(sf);
```

### DataFlowIssue
Method invocation `getCodec` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapOutInterceptor.java`
#### Snippet
```java
            ProtocolVariation protocol = ProtocolVariation.findVariant(rmps.getNamespaceURI(),
                maps.getNamespaceURI());
            Header header = protocol.getCodec().buildHeaderFault(sf);
            List<Header> headers = message.getHeaders();
            headers.add(header);
```

### DataFlowIssue
Method invocation `getCodec` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapOutInterceptor.java`
#### Snippet
```java
            List<Header> headers = message.getHeaders();
            int startSize = headers.size();
            protocol.getCodec().buildHeaders(rmps, headers);
            if (startSize != headers.size() && MessageUtils.isPartialResponse(message)) {
                // make sure the response is returned as HTTP 200 and not 202
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/SoapFaultFactory.java`
#### Snippet
```java
            RMProperties rmps = RMContextUtils.retrieveRMProperties(msg, false);
            AddressingProperties maps = RMContextUtils.retrieveMAPs(msg, false, false);
            EncoderDecoder codec = ProtocolVariation.findVariant(rmps.getNamespaceURI(),
                maps.getNamespaceURI()).getCodec();
            setDetail(fault, detail, codec);
```

### DataFlowIssue
Method invocation `getCodec` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/SoapFaultFactory.java`
#### Snippet
```java
            AddressingProperties maps = RMContextUtils.retrieveMAPs(msg, false, false);
            EncoderDecoder codec = ProtocolVariation.findVariant(rmps.getNamespaceURI(),
                maps.getNamespaceURI()).getCodec();
            setDetail(fault, detail, codec);
        } catch (Exception ex) {
```

### DataFlowIssue
Method invocation `getEffectiveConfiguration` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapInInterceptor.java`
#### Snippet
```java
                            AddressingProperties maps = ContextUtils.retrieveMAPs(message, false, false, false);
                            if (maps == null) {
                                RMConfiguration config = getManager(message).getEffectiveConfiguration(message);
                                wsauri = config.getAddressingNamespace();
                            } else {
```

### DataFlowIssue
Method invocation `exposeAs` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapInInterceptor.java`
#### Snippet
```java
        }
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, false);
        rmps.exposeAs(consts.getWSRMNamespace());
        ProtocolVariation protocol =
            ProtocolVariation.findVariant(consts.getWSRMNamespace(), maps.getNamespaceURI());
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RMSoapInInterceptor.java`
#### Snippet
```java
        rmps.exposeAs(consts.getWSRMNamespace());
        ProtocolVariation protocol =
            ProtocolVariation.findVariant(consts.getWSRMNamespace(), maps.getNamespaceURI());

        LOG.info("Updating service model info in exchange");
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    protected void purgeDelivered(RedeliverCandidate candidate) {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(candidate.getMessage(), false);
        SequenceType st = rmps.getSequence();
        Identifier sid = st.getIdentifier();
        String key = sid.getValue();
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
                undeliveredCount--;
            }
            if (sequenceCandidates.isEmpty()) {
                candidates.remove(sid.getValue());
            }
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
                RMProperties properties = RMContextUtils.retrieveRMProperties(candidate.getMessage(),
                                                                              false);
                SequenceType st = properties.getSequence();
                undelivered.add(st.getMessageNumber());
            }
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
                RMProperties properties = RMContextUtils.retrieveRMProperties(candidate.getMessage(),
                                                                              false);
                SequenceType st = properties.getSequence();
                cp.put(st.getMessageNumber(), candidate);
            }
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
    protected RedeliverCandidate cacheUndelivered(Message message) {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, false);
        SequenceType st = rmps.getSequence();
        Identifier sid = st.getIdentifier();
        String key = sid.getValue();
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RedeliveryQueueImpl.java`
#### Snippet
```java
                RMProperties properties = RMContextUtils.retrieveRMProperties(candidate.getMessage(),
                                                                              false);
                SequenceType st = properties.getSequence();
                if (num == st.getMessageNumber()) {
                    return candidate;
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            public void resend(Message message, boolean requestAcknowledge) {
                RMProperties properties = RMContextUtils.retrieveRMProperties(message, true);
                SequenceType st = properties.getSequence();
                if (st != null) {
                    LOG.log(Level.INFO, "RESEND_MSG", st.getMessageNumber());
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
    protected ResendCandidate cacheUnacknowledged(Message message) {
        RMProperties rmps = RMContextUtils.retrieveRMProperties(message, true);
        SequenceType st = rmps.getSequence();
        Identifier sid = st.getIdentifier();
        String key = sid.getValue();
```

### DataFlowIssue
Method invocation `getSequence` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/RetransmissionQueueImpl.java`
#### Snippet
```java
            if (LOG.isLoggable(Level.INFO)) {
                RMProperties rmps = RMContextUtils.retrieveRMProperties(message, true);
                SequenceType seq = rmps.getSequence();
                LOG.log(Level.INFO, "Retransmitted message " + seq.getMessageNumber() + " in sequence "
                    + seq.getIdentifier().getValue());
```

### DataFlowIssue
Dereference of `dir.listFiles()` may produce `NullPointerException`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/persistence/jdbc/RMTxStore.java`
#### Snippet
```java

    private static void recursiveDelete(File dir, boolean now) {
        for (File f : dir.listFiles()) {
            if (f.isDirectory()) {
                recursiveDelete(f, now);
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        SchemaInfo schemaInfo = null;
        for (SchemaInfo s : serviceInfo.getSchemas()) {
            if (s.getNamespaceURI().equals(part.getElementQName().getNamespaceURI())) {
                schemaInfo = s;
                break;
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        XmlSchema faultBeanSchema = null;
        if (xmlTypeAnno != null && !StringUtils.isEmpty(xmlTypeAnno.namespace())
            && !xmlTypeAnno.namespace().equals(part.getElementQName().getNamespaceURI())) {
            respectXmlTypeNS = true;
            NamespaceMap nsMap = new NamespaceMap();
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
        if (schemaInfo == null) {
            NamespaceMap nsMap = new NamespaceMap();
            nsMap.add(WSDLConstants.CONVENTIONAL_TNS_PREFIX, part.getElementQName().getNamespaceURI());
            nsMap.add(WSDLConstants.NP_SCHEMA_XSD, WSDLConstants.NS_SCHEMA_XSD);
            schemaInfo = createSchemaIfNeeded(part.getElementQName().getNamespaceURI(), nsMap);
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
                                             QName typeName, SchemaInfo schemaInfo) {
        XmlSchemaElement el = new XmlSchemaElement(schema, true);
        el.setName(part.getElementQName().getLocalPart());
        el.setNillable(true);
        el.setSchemaTypeName(typeName);
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
                throw new Fault(new Message("NO_BEAN_INFO", LOG, cls.getName()));
            }
            SchemaInfo schemaInfo = serviceInfo.getSchema(part.getElementQName().getNamespaceURI());
            if (schemaInfo != null
                && !isExistSchemaElement(schemaInfo.getSchema(), part.getElementQName())) {
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java

            //not mappable in JAXBContext directly, we'll have to do it manually :-(
            SchemaInfo schemaInfo = serviceInfo.getSchema(part.getElementQName().getNamespaceURI());
            if (schemaInfo == null
                || isExistSchemaElement(schemaInfo.getSchema(), part.getElementQName())) {
```

### DataFlowIssue
Method invocation `isAddressingRequired` may produce `NullPointerException`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
            if (!ContextUtils.isOutbound(message) && ContextUtils.isRequestor(message)
                && getWSAddressingFeature(message) != null
                && !getWSAddressingFeature(message).isAddressingRequired()) {
                assertAddressing(message);
            }
```

### DataFlowIssue
Method invocation `isAddressingRequired` may produce `NullPointerException`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                && ContextUtils.isRequestor(message)
                && getWSAddressingFeature(message) != null
                && getWSAddressingFeature(message).isAddressingRequired()) {
                boolean missingWsaHeader = false;
                AssertionInfoMap aim = message.get(AssertionInfoMap.class);
```

### DataFlowIssue
Method invocation `createUnmarshaller` may produce `NullPointerException`
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/soap/MAPCodec.java`
#### Snippet
```java
                                    org.apache.cxf.ws.addressing.VersionTransformer.getExposedJAXBContext(headerURI);
                                unmarshaller =
                                    jaxbContext.createUnmarshaller();
                                unmarshaller.setEventHandler(null);
                            }
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        QName qn = part.getElementQName();
        try {
            writer.writeStartElement("ns1", qn.getLocalPart(), qn.getNamespaceURI());
            Class<?> cls = part.getTypeClass();
            XmlAccessType accessType = Utils.getXmlAccessType(cls);
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        try {
            QName qn = part.getElementQName();
            if (!qn.equals(reader.getName())) {
                throw new Fault(new Message("ELEMENT_NAME_MISMATCH", LOG, qn, reader.getName()));
            }
```

### DataFlowIssue
Method invocation `isPrimitive` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        Object o = ret;
                        if (!isList(type)) {
                            if (compType.isPrimitive()) {
                                o = java.lang.reflect.Array.newInstance(compType, ret.size());
                                for (int x = 0; x < ret.size(); x++) {
```

### DataFlowIssue
Method invocation `getGenericReturnType` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        m = Utils.getMethod(cls, accessType, "is" + s);
                    }
                    Type type = m.getGenericReturnType();
                    Object o;
                    if (JAXBSchemaInitializer.isArray(type)) {
```

### DataFlowIssue
Method invocation `isPrimitive` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        o = ret;
                        if (!isList(type)) {
                            if (compType.isPrimitive()) {
                                o = java.lang.reflect.Array.newInstance(compType, ret.size());
                                for (int x = 0; x < ret.size(); x++) {
```

### DataFlowIssue
Method invocation `getElementQName` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        Class<?> clazz = part != null ? part.getTypeClass() : null;
        try {
            writeObject(marshaller, source, newJAXBElement(part.getElementQName(), clazz, null));
        } catch (JAXBException e) {
            throw new Fault(new Message("MARSHAL_ERROR", LOG, e.getMessage()), e);
```

### DataFlowIssue
Argument `part` might be null
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java

        Object o = unmarshall(u, source, elName, clazz, unwrap);
        if (o != null && o.getClass().isArray() && isList(part)) {
            List<Object> ret = createList(part);
            Collections.addAll(ret, (Object[])o);
```

### DataFlowIssue
Argument `cls` might be null
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            cls = mObj.getClass();
        }
        return new JAXBElement(elName, cls, mObj);
    }

```

### DataFlowIssue
Method invocation `isArray` may produce `NullPointerException`
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                    XmlSchemaElement el = (XmlSchemaElement)part.getXmlSchema();

                    if (mObj.getClass().isArray()
                        && el.getSchemaType() instanceof XmlSchemaSimpleType
                        && ((XmlSchemaSimpleType)el.getSchemaType()).
```

### DataFlowIssue
Argument `codeVerifierTransformers` might be null
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/AuthorizationCodeGrantHandler.java`
#### Snippet
```java
            this.codeVerifierTransformers = Collections.emptyList();
        }
        this.codeVerifierTransformers = new ArrayList<>(codeVerifierTransformers);
    }

```

### DataFlowIssue
Argument `localAccessToken` might be null
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractAccessTokenValidator.java`
#### Snippet
```java
                        Collections.singleton(authScheme), realm);
                }
                accessTokenV = new AccessTokenValidation(localAccessToken);
            }
        }
```

### DataFlowIssue
Method invocation `getTokenIssuedAt` may produce `NullPointerException`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractAccessTokenValidator.java`
#### Snippet
```java
        }
        // Check if token is still valid
        if (OAuthUtils.isExpired(accessTokenV.getTokenIssuedAt(), accessTokenV.getTokenLifetime())) {
            if (localAccessToken != null) {
                removeAccessToken(localAccessToken);
```

### DataFlowIssue
Argument `xmlConfigURL` might be null
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/EHCacheIdentityCache.java`
#### Snippet
```java

        URL xmlConfigURL = configFileURL != null ? configFileURL : getDefaultConfigFileURL();
        Configuration xmlConfig = new XmlConfiguration(xmlConfigURL);
        cacheManager = CacheManagerBuilder.newCacheManager(xmlConfig);

```

### DataFlowIssue
Method invocation `getAny` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        if ("jwt".equals(tokenType)) {
            // Discard the wrapper here
            return Response.ok(((Element)requestedToken.getAny()).getTextContent()).build();
        }
        // Base-64 encode the token + return it
```

### DataFlowIssue
Method invocation `getAny` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        try {
            String encodedToken =
                encodeToken(DOM2Writer.nodeToString((Element)requestedToken.getAny()));
            return Response.ok(encodedToken).build();
        } catch (Exception ex) {
```

### DataFlowIssue
Method invocation `getAny` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java

        RequestedSecurityTokenType requestedToken = getRequestedSecurityToken(response);
        return Response.ok(requestedToken.getAny()).build();
    }

```

### DataFlowIssue
Method invocation `getAny` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java

        // Discard the XML Wrapper + create a new JSON Wrapper
        String token = ((Element)requestedToken.getAny()).getTextContent();
        return Response.ok(new JSONWrapper(token)).build();
    }
```

### DataFlowIssue
Argument `cert` might be null
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java

        if (subjectDn != null
            && !subjectDn.equals(OAuthUtils.getSubjectDnFromTLSCertificates(cert))) {
            LOG.warning("Client \"" + client.getClientId() + "\" can not be bound to the TLS certificate");
            reportInvalidClient();
```

### DataFlowIssue
Argument `cert` might be null
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java
        String issuerDn = client.getProperties().get(OAuthConstants.TLS_CLIENT_AUTH_ISSUER_DN);
        if (issuerDn != null
            && !issuerDn.equals(OAuthUtils.getIssuerDnFromTLSCertificates(cert))) {
            LOG.warning("Client \"" + client.getClientId() + "\" can not be bound to the TLS certificate");
            reportInvalidClient();
```

### DataFlowIssue
Argument `cert` might be null
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AbstractTokenService.java`
#### Snippet
```java
        if (OAuthUtils.isMutualTls(sc, tlsSessionInfo)) {
            X509Certificate cert = OAuthUtils.getRootTLSCertificate(tlsSessionInfo);
            String subjectDn = OAuthUtils.getSubjectDnFromTLSCertificates(cert);
            if (!StringUtils.isEmpty(subjectDn)) {
                client = getClient(subjectDn, params);
```

### DataFlowIssue
Method invocation `getNotBefore` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
                validTill = renewedAssertion.getSaml2().getConditions().getNotOnOrAfter();
            } else {
                validFrom = renewedAssertion.getSaml1().getConditions().getNotBefore();
                validTill = renewedAssertion.getSaml1().getConditions().getNotOnOrAfter();
            }
```

### DataFlowIssue
Method invocation `getAudienceRestrictionConditions` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            if (assertion.getSaml1() != null) {
                List<AudienceRestrictionCondition> restrConditions =
                    assertion.getSaml1().getConditions().getAudienceRestrictionConditions();
                if (!matchSaml1AudienceRestriction(appliesToAddress, restrConditions)) {
                    LOG.log(Level.WARNING, "The AppliesTo address does not match the Audience Restriction");
```

### DataFlowIssue
Method invocation `getNotOnOrAfter` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            return assertion.getSaml2().getConditions().getNotOnOrAfter();
        }
        return assertion.getSaml1().getConditions().getNotOnOrAfter();
    }

```

### DataFlowIssue
Method invocation `getNotBefore` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java
                validTill = assertion.getSaml2().getConditions().getNotOnOrAfter();
            } else {
                validFrom = assertion.getSaml1().getConditions().getNotBefore();
                validTill = assertion.getSaml1().getConditions().getNotOnOrAfter();
            }
```

### DataFlowIssue
Method invocation `getAudienceRestrictionConditions` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
        if (assertion.getSaml1() != null) {
            for (AudienceRestrictionCondition restriction
                : assertion.getSaml1().getConditions().getAudienceRestrictionConditions()) {
                for (org.opensaml.saml.saml1.core.Audience audience : restriction.getAudiences()) {
                    addresses.add(audience.getUri());
```

### DataFlowIssue
Method invocation `getNotBefore` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/SAMLTokenValidator.java`
#### Snippet
```java
            issueInstant = assertion.getSaml2().getIssueInstant();
        } else {
            validFrom = assertion.getSaml1().getConditions().getNotBefore();
            validTill = assertion.getSaml1().getConditions().getNotOnOrAfter();
            issueInstant = assertion.getSaml1().getIssueInstant();
```

### DataFlowIssue
Method invocation `getDOM` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/SAMLTokenValidator.java`
#### Snippet
```java
            SAMLKeyInfo samlKeyInfo =
                SAMLUtil.getCredentialFromKeyInfo(
                    keyInfo.getDOM(), new WSSSAMLKeyInfoProcessor(requestData), sigCrypto
                );
            assertion.verifySignature(samlKeyInfo);
```

### DataFlowIssue
Argument `attribute.getAttributeName()` might be null
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
                ProcessedClaim c = new ProcessedClaim();
                c.setIssuer(assertion.getIssuer());
                c.setClaimType(URI.create(attribute.getAttributeName()));
                try {
                    c.setClaimType(new URI(attribute.getAttributeName()));
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
                for (XMLObject attributeValue : attribute.getAttributeValues()) {
                    Element attributeValueElement = attributeValue.getDOM();
                    String value = attributeValueElement.getTextContent();
                    if (LOG.isLoggable(Level.FINEST)) {
                        LOG.finest(" [" + value + "]");
```

### DataFlowIssue
Method invocation `getTextContent` may produce `NullPointerException`
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
                for (XMLObject attributeValue : attribute.getAttributeValues()) {
                    Element attributeValueElement = attributeValue.getDOM();
                    String value = attributeValueElement.getTextContent();
                    if (LOG.isLoggable(Level.FINEST)) {
                        LOG.finest(" [" + value + "]");
```

### DataFlowIssue
Dereference of `flist` may produce `NullPointerException`
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/ServerApp.java`
#### Snippet
```java
                } else if (f.isDirectory()) {
                    File[] flist = f.listFiles(jsFilter);
                    for (File file : flist) {
                        fileSeen = true;
                        if (verbose) {
```

### DataFlowIssue
Method invocation `toURI` may produce `NullPointerException`
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/JsServiceFactoryBean.java`
#### Snippet
```java
    public void create() throws Exception {
        BusFactory.setDefaultBus(bus);
        String jsFileString = getClass().getResource(js).toURI().getPath();
        jsFileString = URLDecoder.decode(jsFileString, StandardCharsets.UTF_8.name());
        File file = new File(jsFileString);
```

### DataFlowIssue
Argument `svcNm` might be null
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/AbstractDOMProvider.java`
#### Snippet
```java
        factory.setWsdlLocation(wsdlLoc);
        factory.setBindingId(binding);
        factory.setServiceName(new QName(tgtNmspc, svcNm));
        factory.setEndpointName(new QName(tgtNmspc, portNm));
        ep = new EndpointImpl(bus, this, factory);
```

### DataFlowIssue
Argument `portNm` might be null
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/AbstractDOMProvider.java`
#### Snippet
```java
        factory.setBindingId(binding);
        factory.setServiceName(new QName(tgtNmspc, svcNm));
        factory.setEndpointName(new QName(tgtNmspc, portNm));
        ep = new EndpointImpl(bus, this, factory);
        ep.publish(addr);
```

### DataFlowIssue
Argument `ops` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/BareInInterceptor.java`
#### Snippet
```java
                p = msgInfo.getMessageParts().get(paramNum);
            } else {
                p = findMessagePart(exchange, ops, elName, client, paramNum, message);
            }

```

### DataFlowIssue
Variable is already assigned to this value
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/PartialWSDLProcessor.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            portType = null;
        }
        return portType != null;
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/PartialWSDLProcessor.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            binding = null;
        }
        return binding != null;
```

### DataFlowIssue
Argument `p` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/interceptors/DocLiteralInInterceptor.java`
#### Snippet
```java
                        parameters.add(o);
                    } else {
                        parameters.put(p, o);
                    }

```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            if (isSOAPBinding(obj)) {
                SOAPBinding soapBinding = getSoapBinding(obj);
                bindingStyle = soapBinding.getStyle();
                if (bindingStyle == null) {
                    bindingStyle = "";
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                if (isSOAPOperation(obj)) {
                    SOAPOperation soapOperation = getSoapOperation(obj);
                    String style = soapOperation.getStyle();
                    if (style == null) {
                        style = "";
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        for (Object obj : binding.getExtensibilityElements()) {
            if (isSOAPBinding(obj)) {
                return getSoapBinding(obj).getStyle();
            }
        }
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            for (Object obj : bop.getExtensibilityElements()) {
                if (isSOAPOperation(obj)) {
                    style = getSoapOperation(obj).getStyle();
                    break;
                }
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java
            if (inputEl == null) {
                passedRule = false;
            } else if (!opInfo.getName().getLocalPart().equals(inputElementName.getLocalPart())) {
                passedRule = relaxed;
            }
```

### DataFlowIssue
Method invocation `read` may produce `NullPointerException`
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            int fileSize = 0;
            try (InputStream pre = this.getClass().getResourceAsStream("/wsdl/mtom_xop.wsdl")) {
                for (int i = pre.read(); i != -1; i = pre.read()) {
                    fileSize++;
                }
```

### DataFlowIssue
Method invocation `read` may produce `NullPointerException`
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java
            byte[] data = new byte[fileSize *  count];
            for (int x = 0; x < count; x++) {
                this.getClass().getResourceAsStream("/wsdl/mtom_xop.wsdl").read(data,
                                                                                fileSize * x,
                                                                                fileSize);
```

### DataFlowIssue
Argument `cfgResource` might be null
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
            System.clearProperty(Configurer.USER_CFG_FILE_PROPERTY_NAME);
            if (null != original) {
                System.setProperty(Configurer.USER_CFG_FILE_PROPERTY_NAME, cfgResource);
            }
        }
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
        }
        // expose only running server ports
        String simpleName = className.substring(className.lastIndexOf('.') + 1);
        int idx = simpleName.indexOf('$');
        if (-1 != idx) {
```

### DataFlowIssue
Argument `os` might be null
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            }

            try (PrintStream ps = new PrintStream(os)) {
                StringBuilder serverOutput = new StringBuilder();
                for (int ch = in.read(); ch != -1; ch = in.read()) {
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
            if (messagePartInfo.isElement()) {
                messagePart.setElementName(messagePartInfo.getElementQName());
                addNamespace(messagePartInfo.getElementQName().getNamespaceURI(), def);
            } else if (messagePartInfo.getTypeQName() != null) {
                messagePart.setTypeName(messagePartInfo.getTypeQName());
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
                    }
                }
                definition = orig;
            }

```

### DataFlowIssue
Method invocation `getRespondWith` will produce `NullPointerException`
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/handlers/X509Register.java`
#### Snippet
```java
            return false;
        }
        List<String> respondWithList = request.getRespondWith();
        if ((respondWithList != null) && !(respondWithList.isEmpty())) {
            return respondWithList.contains((Object)RespondWithEnum.HTTP_WWW_W_3_ORG_2002_03_XKMS_X_509_CERT);
```

### DataFlowIssue
Method invocation `getConstructor` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            }
            try {
                return cls.getConstructor(Boolean.TYPE, Map.class)
                    .newInstance(this.isQualifyWrapperSchema(), this.getProperties());
            } catch (NoSuchMethodException nsme) {
```

### DataFlowIssue
Argument `schema` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                    el.setSchemaType(oldEl.getSchemaType());
                    if (oldEl.getSchemaTypeName() != null) {
                        addImport(schema, oldEl.getSchemaTypeName().getNamespaceURI());
                    }
                }
```

### DataFlowIssue
Argument `schema` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            mpi.setConcreteName(qname);
            if (mpi.getTypeQName() != null) {
                addImport(schema, mpi.getTypeQName().getNamespaceURI());
            }

```

### DataFlowIssue
Argument `wrapperBeanName` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                        XmlSchemaElement e = null;
                        for (SchemaInfo s : serviceInfo.getSchemas()) {
                            e = s.getElementByQName(wrapperBeanName);
                            if (e != null) {
                                fmpi.setXmlSchema(e);
```

### DataFlowIssue
Argument `wrapperBeanName` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                        if (e == null) {
                            createWrappedSchema(serviceInfo, op.getInput(), op.getUnwrappedOperation()
                                .getInput(), wrapperBeanName);
                        }
                    }
```

### DataFlowIssue
Argument `wrapperBeanName` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                        XmlSchemaElement e = null;
                        for (SchemaInfo s : serviceInfo.getSchemas()) {
                            e = s.getElementByQName(wrapperBeanName);
                            if (e != null) {
                                break;
```

### DataFlowIssue
Argument `wrapperBeanName` might be null
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                        if (e == null) {
                            createWrappedSchema(serviceInfo, op.getOutput(), op.getUnwrappedOperation()
                                .getOutput(), wrapperBeanName);
                        }
                    }
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            Map<Class<?>, Boolean> jaxbAnnoMap = getJaxbAnnoMap(mpi);
            if (mpi.isElement()) {
                addImport(schema, mpi.getElementQName().getNamespaceURI());
                XmlSchemaUtils.setElementRefName(el, mpi.getElementQName());
            } else {
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java

    protected void checkForElement(ServiceInfo serviceInfo, MessagePartInfo mpi) {
        SchemaInfo si = getOrCreateSchema(serviceInfo, mpi.getElementQName().getNamespaceURI(),
                                          getQualifyWrapperSchema());
        XmlSchemaElement e = si.getSchema().getElementByName(mpi.getElementQName().getLocalPart());
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                    final String ns;
                    if (mpi.isElement()) {
                        ns = mpi.getElementQName().getNamespaceURI();
                    } else {
                        ns = mpi.getTypeQName().getNamespaceURI();
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                        ns = mpi.getElementQName().getNamespaceURI();
                    } else {
                        ns = mpi.getTypeQName().getNamespaceURI();
                    }
                    if (mpi.getConcreteName().getLocalPart().equals(name.getLocalPart())
```

### DataFlowIssue
Argument `storageDir.listFiles()` might be null
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
        List<File> certificateFiles = new ArrayList<>();
        try {
            Collections.addAll(certificateFiles, storageDir.listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, TRUSTED_CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CAS_PATH).listFiles());
```

### DataFlowIssue
Argument `new File(storageDir, TRUSTED_CAS_PATH).listFiles()` might be null
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
        try {
            Collections.addAll(certificateFiles, storageDir.listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, TRUSTED_CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CRLS_PATH).listFiles());
```

### DataFlowIssue
Argument `new File(storageDir, CAS_PATH).listFiles()` might be null
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            Collections.addAll(certificateFiles, storageDir.listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, TRUSTED_CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CRLS_PATH).listFiles());
        } catch (NullPointerException e) {
```

### DataFlowIssue
Argument `new File(storageDir, CRLS_PATH).listFiles()` might be null
in `services/xkms/xkms-x509-handlers/src/main/java/org/apache/cxf/xkms/x509/repo/file/FileCertificateRepo.java`
#### Snippet
```java
            Collections.addAll(certificateFiles, new File(storageDir, TRUSTED_CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CAS_PATH).listFiles());
            Collections.addAll(certificateFiles, new File(storageDir, CRLS_PATH).listFiles());
        } catch (NullPointerException e) {
            //
```

### DataFlowIssue
Argument `containedType` might be null
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java

        CorbaTypeEventProducer containedProducer =
            CorbaHandlerUtils.getTypeEventProducer(containedType, serviceInfo, orb);
        if (containedProducer instanceof AbstractStartEndEventProducer) {
            iterator = ((AbstractStartEndEventProducer)containedProducer).getNestedTypes();
```

### DataFlowIssue
Condition `arrayType instanceof Anonarray` is redundant and can be replaced with a null check
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaArrayListener.java`
#### Snippet
```java
        serviceInfo = sInfo;
        Object arrayType = handler.getType();
        if (arrayType instanceof Anonarray) {
            Anonarray anonType = (Anonarray) arrayType;
            arrayElementType = anonType.getElemtype();
```

### DataFlowIssue
Method invocation `getElemtype` will produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaArrayListener.java`
#### Snippet
```java
        } else {
            Array type = (Array) arrayType;
            arrayElementType = type.getElemtype();
        }
    }
```

### DataFlowIssue
Method invocation `getElemtype` will produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaSequenceListener.java`
#### Snippet
```java
        } else {
            Sequence type = (Sequence) seqType;
            seqElementType = type.getElemtype();
            elementName = type.getElemname();
        }
```

### DataFlowIssue
Method invocation `isValid` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java
            }

            if (eprInfo.isValid()) {
                LOG.log(Level.FINE, "Epr metadata " + eprInfo);
                // Create the meta data producer and add its child producers.
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        default:
            result = null;
        }

```

### DataFlowIssue
Argument `prefix` might be null
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
            setPrefix(prefix, namespaceURI);
        }
        QName name = new QName(namespaceURI, localName, prefix);
        elements.push(name);
        if (!skipWrap) {
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
        Element faultElement = faultDoc.createElement("detail");
        Element partElement =
            faultDoc.createElementNS(partInfoName.getNamespaceURI(), partInfoName.getLocalPart());

        Element faultDataElement = (Element) faultData.getFirstChild();
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
            MessagePartInfo partInfo = fault.getMessageParts().get(0);
            if (partInfo.isElement()
                && partInfo.getElementQName().getLocalPart().equals(faultName.getLocalPart())) {
                return fault;
            } else if (partInfo.getTypeQName().getLocalPart().equals(faultName.getLocalPart())) {
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                && partInfo.getElementQName().getLocalPart().equals(faultName.getLocalPart())) {
                return fault;
            } else if (partInfo.getTypeQName().getLocalPart().equals(faultName.getLocalPart())) {
                return fault;
            }
```

### DataFlowIssue
Casting `schemaType` to `XmlSchemaElement` may produce `ClassCastException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
            el = (XmlSchemaElement) seq.getItems().get(0);
        } else {
            el = (XmlSchemaElement) schemaType;
        }
        return el;
```

### DataFlowIssue
Method invocation `getElemtype` will produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
        } else {
            Anonarray anonArrayType = (Anonarray)baseType;
            arrayElementType = anonArrayType.getElemtype();
            arrayBound = anonArrayType.getBound();
            elementName = anonArrayType.getElemname();
```

### DataFlowIssue
Method invocation `getElemtype` will produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
        } else {
            Anonsequence seqType = (Anonsequence)baseType;
            seqElementType = seqType.getElemtype();
            seqBound = seqType.getBound();
            elementName = seqType.getElemname();
```

### DataFlowIssue
Method invocation `getElemtype` will produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
        } else {
            Sequence type = (Sequence) seqType;
            seqElementType = type.getElemtype();
        }
        return CorbaUtils.isPrimitiveIdlType(seqElementType);
```

### DataFlowIssue
Argument `type` might be null
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
                break;
            case TCKind._tk_sequence:
                if (isOctets(type)) {
                    handler = new CorbaOctetSequenceHandler(name, idlType, tc, type);
                } else {
```

### DataFlowIssue
Argument `type` might be null
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaHandlerUtils.java`
#### Snippet
```java
                break;
            case TCKind._tk_sequence:
                if (isOctets(type)) {
                    handler = new CorbaOctetSequenceHandler(name, idlType, tc, type);
                    result = new CorbaOctetSequenceListener(handler);
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
            MessagePartInfo partInfo = getFaultMessagePartInfo(opInfo, new QName("", exClassName));
            if (partInfo != null) {
                exClassName = partInfo.getTypeQName().getLocalPart();
            }

```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
            MessagePartInfo partInfo = fault.getMessageParts().get(0);
            if (partInfo.isElement()
                && partInfo.getElementQName().getLocalPart().equals(faultName.getLocalPart())) {
                return partInfo;
            } else if (partInfo.getTypeQName().getLocalPart().equals(faultName.getLocalPart())) {
```

### DataFlowIssue
Method invocation `getLocalPart` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java
                && partInfo.getElementQName().getLocalPart().equals(faultName.getLocalPart())) {
                return partInfo;
            } else if (partInfo.getTypeQName().getLocalPart().equals(faultName.getLocalPart())) {
                return partInfo;
            }
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
        MessagePartInfo part = msgInfo.getMessageParts().get(0);
        if (part.isElement()) {
            return part.getElementQName().getNamespaceURI();
        }
        return part.getName().getNamespaceURI();
```

### DataFlowIssue
Method invocation `getEnumerator` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                            member.label.create_output_stream();
                        Enum enumVal = (Enum)getCorbaType(unionType.getDiscriminator(), typeMap);
                        List<Enumerator> enumerators = enumVal.getEnumerator();
                        for (int i = 0; i < enumerators.size(); ++i) {
                            Enumerator e = enumerators.get(i);
```

### DataFlowIssue
Method invocation `getNamespaceURI` may produce `NullPointerException`
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamInInterceptor.java`
#### Snippet
```java
        MessagePartInfo part = msgInfo.getMessageParts().get(0);
        if (part.isElement()) {
            return part.getElementQName().getNamespaceURI();
        }
        return part.getName().getNamespaceURI();
```

### DataFlowIssue
Variable is already assigned to this value
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
                result = true;
            } catch (Exception thrown) {
                result = false;
            }
        }
```

### DataFlowIssue
Argument `response` might be null
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
                    invoke(null, null, request, response);
                } catch (InvalidPathException ex) {
                    reportErrorStatus(session, 400, response);
                } catch (Exception e) {
                    LOG.log(Level.WARNING, "Failed to invoke service", e);
```

### DataFlowIssue
Argument `response` might be null
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
                } catch (Exception e) {
                    LOG.log(Level.WARNING, "Failed to invoke service", e);
                    reportErrorStatus(session, 500, response);
                }
            }
```

### DataFlowIssue
Method invocation `getPart` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/HeaderUtil.java`
#### Snippet
```java
                if (SOAPBindingUtil.isSOAPHeader(ext)) {
                    SoapHeader header = SOAPBindingUtil.getSoapHeader(ext);
                    String pn = header.getPart();
                    for (MessagePartInfo mpi : mps) {
                        if (pn.equals(mpi.getName().getLocalPart())) {
```

### DataFlowIssue
Method invocation `indexOf` may produce `NullPointerException`
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
            // read the request line
            line = readLine(in);
            del = line.indexOf(' ');
            if (del < 0) {
                throw new IOException("invalid request: " + line);
```

### DataFlowIssue
Method invocation `getLocationURI` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java

                    info.addExtensor(sa);
                    info.setAddress(sa.getLocationURI());
                    if (isJMSSpecAddress(sa.getLocationURI())) {
                        info.setTransportId(SoapJMSConstants.SOAP_JMS_SPECIFICIATION_TRANSPORTID);
```

### DataFlowIssue
Casting `source` to `Element` may produce `ClassCastException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapHeaderInterceptor.java`
#### Snippet
```java
                            object = getNodeDataReader(message).read(mpi, source);
                        } else {
                            W3CDOMStreamReader reader = new W3CDOMStreamReader((Element)source);
                            try {
                                reader.nextTag(); //advance into the first tag
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        for (Object obj : binding.getExtensibilityElements()) {
            if (isSOAPBinding(obj)) {
                return getSoapBinding(obj).getStyle();
            }
        }
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            if (isSOAPBinding(obj)) {
                SoapBinding soapBinding = getSoapBinding(obj);
                bindingStyle = soapBinding.getStyle();
                if (bindingStyle == null) {
                    bindingStyle = "";
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                if (isSOAPOperation(obj)) {
                    SoapOperation soapOperation = getSoapOperation(obj);
                    String style = soapOperation.getStyle();
                    if (style == null) {
                        style = "";
```

### DataFlowIssue
Method invocation `getStyle` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            for (Object obj : bop.getExtensibilityElements()) {
                if (isSOAPOperation(obj)) {
                    style = getSoapOperation(obj).getStyle();
                    break;
                }
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/auth/saml/Saml2BearerAuthHandler.java`
#### Snippet
```java
        // Introduce SAMLOAuth2Validator to be reused between auth and grant handlers
        Subject subject = SAMLUtils.getSubject(message, wrapper);
        if (subject.getName() == null) {
            throw ExceptionUtils.toNotAuthorizedException(null, null);
        }
```

### DataFlowIssue
Method invocation `getOwnerDocument` may produce `NullPointerException`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/Saml2BearerGrantHandler.java`
#### Snippet
```java

                Signature sig = assertion.getSignature();
                WSDocInfo docInfo = new WSDocInfo(sig.getDOM().getOwnerDocument());
                data.setWsDocInfo(docInfo);
                KeyInfo keyInfo = sig.getKeyInfo();
```

### DataFlowIssue
Method invocation `getDOM` may produce `NullPointerException`
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/Saml2BearerGrantHandler.java`
#### Snippet
```java
                SAMLKeyInfo samlKeyInfo =
                    SAMLUtil.getCredentialFromKeyInfo(
                        keyInfo.getDOM(), new WSSSAMLKeyInfoProcessor(data),
                        data.getSigVerCrypto()
                    );
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
        if (isInput) {
            minfo = unwrapped.getInput();
            type = MessageInfo.Type.INPUT;
            if (minfo != null) {
                for (MessagePartInfo part : minfo.getMessageParts()) {
```

### DataFlowIssue
Variable is already assigned to this value
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
        } else {
            minfo = unwrapped.getOutput();
            type = MessageInfo.Type.OUTPUT;
            if (minfo != null) {
                for (MessagePartInfo part : minfo.getMessageParts()) {
```

### DataFlowIssue
Method invocation `getParts` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
            } else if (SOAPBindingUtil.isSOAPBody(content)) {
                SoapBody sb = SOAPBindingUtil.getSoapBody(content);
                if (sb.getParts() != null && sb.getParts().size() == 1) {
                    partName = (String) sb.getParts().get(0);
                }
```

### DataFlowIssue
Method invocation `getUse` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java

                SoapHeaderInfo headerInfo = new SoapHeaderInfo();
                headerInfo.setUse(header.getUse());

                if (StringUtils.isEmpty(header.getPart())) {
```

### DataFlowIssue
Method invocation `getTransportURI` may produce `NullPointerException`
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java


        bi.setTransportURI(wSoapBinding.getTransportURI());
        bi.setStyle(wSoapBinding.getStyle());

```

### DataFlowIssue
Method invocation `getProtocol` may produce `NullPointerException`
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/AbstractCodeGeneratorMojo.java`
#### Snippet
```java

        try {
            if ("jar".equals(url.getProtocol())) {
                String s = url.getPath();
                if (s.contains("!")) {
```

### DataFlowIssue
Argument `boundary` might be null
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
            if (!this.logBinary && findBoundary(originalLogString) != null) {
                String boundary = findBoundary(originalLogString);
                String[] parts = originalLogString.split(Pattern.quote(boundary));
                String payload = "";
                for (String str : parts) {
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/AbstractLoggingInterceptor.java`
#### Snippet
```java
                    if (findContentType(str) != null
                        && eventMapper.isBinaryContent(
                           findContentType(str).substring("Content-Type:".length()).trim())) {
                        payload = payload + "\r\n" + CONTENT_SUPPRESSED + "\r\n";
                    } else {
```

### DataFlowIssue
Argument `JwsUtils.getHmacSignatureProvider(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
    }
    public String signWith(byte[] key, SignatureAlgorithm algo) {
        return signWith(JwsUtils.getHmacSignatureProvider(key, algo));
    }
    public String signWith(JwsSignatureProvider signer,
```

### DataFlowIssue
Argument `JwsUtils.getPrivateKeySignatureProvider(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
    }
    public String signWith(PrivateKey key, SignatureAlgorithm algo) {
        return signWith(JwsUtils.getPrivateKeySignatureProvider(key, algo));
    }
    public String signWith(byte[] key, SignatureAlgorithm algo) {
```

### DataFlowIssue
Argument `JwsUtils.getHmacSignatureVerifier(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsCompactConsumer.java`
#### Snippet
```java
    }
    public boolean verifySignatureWith(byte[] key, SignatureAlgorithm algo) {
        return verifySignatureWith(JwsUtils.getHmacSignatureVerifier(key, algo));
    }
    public boolean validateCriticalHeaders() {
```

### DataFlowIssue
Argument `JwsUtils.getPublicKeySignatureVerifier(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsCompactConsumer.java`
#### Snippet
```java
    }
    public boolean verifySignatureWith(PublicKey key, SignatureAlgorithm algo) {
        return verifySignatureWith(JwsUtils.getPublicKeySignatureVerifier(key, algo));
    }
    public boolean verifySignatureWith(byte[] key, SignatureAlgorithm algo) {
```

### DataFlowIssue
Method invocation `getPackage` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AbstractTypeCreator.java`
#### Snippet
```java
                // than create nonsense.
                Class<?> typeClass = TypeUtil.getTypeRelatedClass(info.getType());
                if (typeClass.getPackage().getName().startsWith("java")) {
                    name = tm.getTypeQName(typeClass);
                }
```

### DataFlowIssue
Argument `javaClass` might be null
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AbstractTypeCreator.java`
#### Snippet
```java
        } else if (info.getAegisTypeClass() != null) {
            result = createUserType(info);
        } else if (isArray(javaClass)) {
            result = createArrayType(info);
        } else if (isMap(javaClass)) {
```

### DataFlowIssue
Method invocation `getSimpleName` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AbstractTypeCreator.java`
#### Snippet
```java

        Class<?> cls = TypeUtil.getTypeRelatedClass(info.getType());
        name += cls.getSimpleName();

        // TODO: Get namespace from XML?
```

### DataFlowIssue
Method invocation `equals` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
                    // if it doesn't match, then we can definitely rule out
                    // this result
                    && !DOMUtils.getAttributeValueEmptyNull(match, "class").equals(parameterType.getName())) {

                    iterator.remove();
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            }

            String defaultNS = NamespaceHelper.makeNamespaceFromClassName(relatedClass.getName(), "http");
            QName name = null;
            if (typeNameAtt != null) {
```

### DataFlowIssue
Argument `relatedClass` might be null
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        Class<?> relatedClass = TypeUtil.getTypeRelatedClass(info.getType());
        // we only mess with the generic issues for list and map
        if (Collection.class.isAssignableFrom(relatedClass)) {
            Type componentType = getComponentType(mapping, parameter);
            if (componentType != null) { // there is actually XML config.
```

### DataFlowIssue
Method invocation `readObject` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/MapType.java`
#### Snippet
```java
                                                              context.getGlobalContext(),
                                                              getKeyType());
                            key = kType.readObject(evReader, context);
                        } else if (evReader.getName().equals(getValueName())) {
                            AegisType vType = TypeUtil.getReadType(evReader.getXMLStreamReader(),
```

### DataFlowIssue
Method invocation `readObject` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/collection/MapType.java`
#### Snippet
```java
                                                              context.getGlobalContext(),
                                                              getValueType());
                            value = vType.readObject(evReader, context);
                        } else {
                            readToEnd(evReader);
```

### DataFlowIssue
Method invocation `getXmlSchema` may produce `NullPointerException`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/XMLStreamDataWriter.java`
#### Snippet
```java
        try {
            if (obj == null) {
                if (part.getXmlSchema() instanceof XmlSchemaElement
                    && ((XmlSchemaElement)part.getXmlSchema()).getMinOccurs() == 0) {
                    //skip writing minOccurs=0 stuff if obj is null
```

### DataFlowIssue
Method invocation `getCxfResponseCallback` may produce `NullPointerException`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpClientHandler.java`
#### Snippet
```java
        if (cause instanceof ReadTimeoutException) {
            final NettyHttpClientRequest request = sendedQueue.poll();
            request.getCxfResponseCallback().error(new IOException(cause));
        } else {
            cause.printStackTrace();
```

### DataFlowIssue
Method invocation `setResponse` may produce `NullPointerException`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpClientHandler.java`
#### Snippet
```java
                HttpResponse response = (HttpResponse)msg;
                NettyHttpClientRequest request = sendedQueue.poll();
                request.setResponse(response);
                // calling the callback here
                request.getCxfResponseCallback().responseReceived(response);
```

### DataFlowIssue
Method invocation `getContextualProperty` may produce `NullPointerException`
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/manager/AbstractSubscriptionManager.java`
#### Snippet
```java
    protected String retrieveSubscriptionUUID() {
        Object uuid = ((WrappedMessageContext)context.getMessageContext()).getWrappedMessage()
                .getContextualProperty("uuid");
        if (uuid == null) {
            throw new UnknownSubscription();
```

### DataFlowIssue
Method invocation `getProtocol` may produce `NullPointerException`
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java

        try {
            if ("jar".equals(url.getProtocol())) {
                String s = url.getPath();
                if (s.contains("!")) {
```

### DataFlowIssue
Casting `cachedEntity` to `InputStream` may produce `ClassCastException`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
                cachedEntity = bytesEntity.isFromStream() ? new ByteArrayInputStream(bytes) : bytes;
                if (cacheResponseInputStream) {
                    InputStream is = bytesEntity.isFromStream() ? (InputStream)cachedEntity
                        : new ByteArrayInputStream((byte[])cachedEntity);
                    context.setInputStream(is);
```

### DataFlowIssue
Casting `cachedEntity` to `byte[]` may produce `ClassCastException`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/cache/CacheControlClientReaderInterceptor.java`
#### Snippet
```java
                if (cacheResponseInputStream) {
                    InputStream is = bytesEntity.isFromStream() ? (InputStream)cachedEntity
                        : new ByteArrayInputStream((byte[])cachedEntity);
                    context.setInputStream(is);
                    return context.proceed();
```

### DataFlowIssue
Argument `JwsUtils.getHmacSignatureVerifier(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonConsumer.java`
#### Snippet
```java
    }
    public boolean verifySignatureWith(byte[] key, SignatureAlgorithm algo, Map<String, Object> entryProps) {
        return verifySignatureWith(JwsUtils.getHmacSignatureVerifier(key, algo), entryProps);
    }
    public boolean verifySignatureWith(JsonWebKey key) {
```

### DataFlowIssue
Argument `JwsUtils.getPublicKeySignatureVerifier(key, algo)` might be null
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonConsumer.java`
#### Snippet
```java
    }
    public boolean verifySignatureWith(PublicKey key, SignatureAlgorithm algo, Map<String, Object> entryProps) {
        return verifySignatureWith(JwsUtils.getPublicKeySignatureVerifier(key, algo), entryProps);
    }
    public boolean verifySignatureWith(byte[] key, SignatureAlgorithm algo) {
```

### DataFlowIssue
Method invocation `setConfiguration` may produce `NullPointerException`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        AbstractClient newClient = toAbstractClient(toClient);
        AbstractClient oldClient = toAbstractClient(fromClient);
        newClient.setConfiguration(oldClient.getConfiguration());
    }

```

### DataFlowIssue
Method invocation `getConfiguration` may produce `NullPointerException`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
        AbstractClient newClient = toAbstractClient(toClient);
        AbstractClient oldClient = toAbstractClient(fromClient);
        newClient.setConfiguration(oldClient.getConfiguration());
    }

```

### DataFlowIssue
Argument `ori` might be null
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/ClientProxyImpl.java`
#### Snippet
```java
        }

        MultivaluedMap<ParameterType, Parameter> types = getParametersInfo(m, params, ori);
        List<Parameter> beanParamsList = getParameters(types, ParameterType.BEAN);

```

## RuleId[ruleID=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'ENABLE_HTTP2_PROP' is still used
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
     */
    @Deprecated
    public static final String ENABLE_HTTP2_PROP = "org.apache.cxf.transports.http_undertow.EnableHttp2";
    
    public static final String ENABLE_RECORD_REQUEST_START_TIME_PROP = 
```

### DeprecatedIsStillUsed
Deprecated member 'LoggingMessage' is still used
in `core/src/main/java/org/apache/cxf/interceptor/LoggingMessage.java`
#### Snippet
```java
 */
@Deprecated
public final class LoggingMessage {
    public static final String ID_KEY = LoggingMessage.class.getName() + ".ID";
    private static final AtomicInteger ID = new AtomicInteger();
```

### DeprecatedIsStillUsed
Deprecated member 'NettyHttpServerEngine' is still used
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngine.java`
#### Snippet
```java

    @Deprecated
    public NettyHttpServerEngine(String host, int port) {
        this(host, port, null);
    }
```

### DeprecatedIsStillUsed
Deprecated member 'getJwsJsonSignedDocument' is still used
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonProducer.java`
#### Snippet
```java
    }
    @Deprecated
    public String getJwsJsonSignedDocument(boolean detached) {
        return doGetJwsJsonSignedDocument(detached);
    }
```

## RuleId[ruleID=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/ReferencingAuthenticator.java`
#### Snippet
```java
        } else {
            fields = AccessController.doPrivileged(
                    (PrivilegedAction<Field[]>) () -> Authenticator.class.getDeclaredFields());

        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/Utils.java`
#### Snippet
```java
        if (System.getSecurityManager() != null) {
            return AccessController.doPrivileged((PrivilegedAction<ExecutorService>) () -> {
                return ForkJoinPool.commonPool();
            });
        } else {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIFacade.java`
#### Snippet
```java
    public static Optional<Object> getBeanManager() {
        try {
            return nullableOptional(() -> CDIUtils.getCurrentBeanManager());
        } catch (Throwable t) {
            t.printStackTrace();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java

    static Comparator<ProviderInfo<?>> createComparator(MicroProfileClientFactoryBean bean) {
        Comparator<ProviderInfo<?>> parent = (o1, o2) -> compareCustomStatus(o1, o2);
        return new ContractComparator(bean, parent);
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIInterceptorWrapperImpl.java`
#### Snippet
```java
    private static Annotation[] merge(List<Annotation> methodBindings, List<Annotation> classBindings) {
        Set<Class<? extends Annotation>> types = methodBindings.stream()
                                                               .map(a -> a.annotationType())
                                                               .collect(Collectors.toSet());
        List<Annotation> merged = new ArrayList<>(methodBindings);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `core/src/main/java/org/apache/cxf/helpers/FileUtils.java`
#### Snippet
```java
        }
        return AccessController.doPrivileged((PrivilegedAction<Boolean>) () -> {
            return file.exists();
        });
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java

            WSSSecurityProperties secProps = createSecurityProperties();
            secProps.setDocumentCreator(() -> DOMUtils.createDocument());
            translateProperties(soapMessage, secProps);
            configureCallbackHandler(soapMessage, secProps);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxOutInterceptor.java`
#### Snippet
```java
        try {
            WSSSecurityProperties secProps = createSecurityProperties();
            secProps.setDocumentCreator(() -> DOMUtils.createDocument());
            translateProperties(mc, secProps);
            configureCallbackHandler(mc, secProps);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSDestination.java`
#### Snippet
```java
            if (!jmsConfig.isOneSessionPerConnection()) {
                // If first connect fails we will try to establish the connection in the background
                new Thread(() -> restartConnection()).start();
            }
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/SseBroadcasterImpl.java`
#### Snippet
```java
        if (closed.compareAndSet(false, true)) {
            subscribers.forEach(subscriber -> {
                subscriber.close();
            });
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
                final ClassLoader loader = AccessController.doPrivileged((PrivilegedAction<ClassLoader>) 
                    () -> {
                        return type.getClassLoader();
                    });
                if (loader != null && objectFactoryOrIndexAvailable(type)) {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/filters/JwsJwksJwtAccessTokenValidator.java`
#### Snippet
```java
    protected JwsSignatureVerifier getInitializedSignatureVerifier(JwsHeaders jwsHeaders) {
        Objects.requireNonNull(jwsHeaders.getKeyId());
        final JwkHolder jwkHolder = jsonWebKeys.computeIfAbsent(jwsHeaders.getKeyId(), keyId -> updateJwk(keyId));
        return jwkHolder != null ? jwkHolder.getJwsSignatureVerifier() : null;
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                                                   convertTypesToString(acceptContentTypes),
                                                   convertTypesToString(ori.getProduceTypes()));
        return () -> errorMsg.toString();
    }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        if (!"OPTIONS".equalsIgnoreCase(httpMethod)) {
            Level logLevel = getExceptionLogLevel(message, ClientErrorException.class);
            LOG.log(logLevel == null ? Level.FINE : logLevel, () -> errorMsg.toString());
        }
        Response response =
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `rt/rs/extensions/rx/src/main/java/org/apache/cxf/jaxrs/rx/server/ObservableInvoker.java`
#### Snippet
```java
            final Observable<?> obs = (Observable<?>)result;
            final AsyncResponseImpl asyncResponse = new AsyncResponseImpl(inMessage);
            obs.subscribe(v -> asyncResponse.resume(v), t -> handleThrowable(asyncResponse, t));
            return asyncResponse;
        }
```

## RuleId[ruleID=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'createdContext' in a Serializable class
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFServlet.java`
#### Snippet
```java

    private boolean busCreated;
    private XmlWebApplicationContext createdContext;

    public CXFServlet() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'controller' in a Serializable class
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java
    private DestinationRegistry destinationRegistry;
    private boolean globalRegistry;
    private ServletController controller;
    private ClassLoader loader;
    private boolean loadBus = true;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'subject' in a Serializable class
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/authorization/SubjectPrincipal.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private Subject subject;
    public SubjectPrincipal(String principalName, Subject subject) {
        super(principalName);
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'contents' in a Serializable class
in `core/src/main/java/org/apache/cxf/message/MessageImpl.java`
#### Snippet
```java

    // array of Class<T>/T pairs for contents
    private Object[] contents = new Object[20];
    private int index;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'bindingOp' in a Serializable class
in `core/src/main/java/org/apache/cxf/message/ExchangeImpl.java`
#### Snippet
```java
    private Service service;
    private Binding binding;
    private BindingOperationInfo bindingOp;

    public ExchangeImpl() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'endpointInfo' in a Serializable class
in `core/src/main/java/org/apache/cxf/endpoint/EndpointImpl.java`
#### Snippet
```java
    private Service service;
    private Binding binding;
    private EndpointInfo endpointInfo;
    private Executor executor;
    private Bus bus;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'out' in a Serializable class
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/CXFHttpRequest.java`
#### Snippet
```java
    
    private HttpEntity entity;
    private AsyncWrappedOutputStream out;
    private RequestConfig config;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'config' in a Serializable class
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/CXFHttpRequest.java`
#### Snippet
```java
    private HttpEntity entity;
    private AsyncWrappedOutputStream out;
    private RequestConfig config;

    public CXFHttpRequest(String method, URI uri) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'application' in a Serializable class
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java

    private ClassLoader classLoader;
    private Application application;

    public CXFNonSpringJaxrsServlet() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'executorProvider' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsClientEndpointImpl.java`
#### Snippet
```java

    private static final long serialVersionUID = 3059035442836604053L;
    private ServiceImpl executorProvider;

    public JaxWsClientEndpointImpl(Bus bus, Service s, EndpointInfo ei, ServiceImpl si,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'logicalOutInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private SOAPHandlerInterceptor soapHandlerInterceptor;
    private LogicalHandlerInInterceptor logicalInInterceptor;
    private LogicalHandlerOutInterceptor logicalOutInterceptor;
    private LogicalHandlerFaultOutInterceptor logicalFaultOutInterceptor;
    private SOAPHandlerFaultOutInterceptor soapFaultOutInterceptor;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'logicalInInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    //interceptors added/removed to chains as needed
    private SOAPHandlerInterceptor soapHandlerInterceptor;
    private LogicalHandlerInInterceptor logicalInInterceptor;
    private LogicalHandlerOutInterceptor logicalOutInterceptor;
    private LogicalHandlerFaultOutInterceptor logicalFaultOutInterceptor;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'logicalFaultInInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private LogicalHandlerFaultOutInterceptor logicalFaultOutInterceptor;
    private SOAPHandlerFaultOutInterceptor soapFaultOutInterceptor;
    private LogicalHandlerFaultInInterceptor logicalFaultInInterceptor;
    private SOAPHandlerFaultInInterceptor soapFaultInInterceptor;
    private boolean handlerInterceptorsAdded;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'soapFaultInInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private SOAPHandlerFaultOutInterceptor soapFaultOutInterceptor;
    private LogicalHandlerFaultInInterceptor logicalFaultInInterceptor;
    private SOAPHandlerFaultInInterceptor soapFaultInInterceptor;
    private boolean handlerInterceptorsAdded;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'implInfo' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

    private Binding jaxwsBinding;
    private JaxWsImplementorInfo implInfo;
    private List<WebServiceFeature> wsFeatures;
    private List<Feature> features;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'soapFaultOutInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private LogicalHandlerOutInterceptor logicalOutInterceptor;
    private LogicalHandlerFaultOutInterceptor logicalFaultOutInterceptor;
    private SOAPHandlerFaultOutInterceptor soapFaultOutInterceptor;
    private LogicalHandlerFaultInInterceptor logicalFaultInInterceptor;
    private SOAPHandlerFaultInInterceptor soapFaultInInterceptor;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'logicalFaultOutInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
    private LogicalHandlerInInterceptor logicalInInterceptor;
    private LogicalHandlerOutInterceptor logicalOutInterceptor;
    private LogicalHandlerFaultOutInterceptor logicalFaultOutInterceptor;
    private SOAPHandlerFaultOutInterceptor soapFaultOutInterceptor;
    private LogicalHandlerFaultInInterceptor logicalFaultInInterceptor;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'soapHandlerInterceptor' in a Serializable class
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java

    //interceptors added/removed to chains as needed
    private SOAPHandlerInterceptor soapHandlerInterceptor;
    private LogicalHandlerInInterceptor logicalInInterceptor;
    private LogicalHandlerOutInterceptor logicalOutInterceptor;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'detail' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/SequenceFault.java`
#### Snippet
```java
    private boolean sender;
    private QName faultCode;
    private Object detail;
    private Element extraDetail;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmInSoap' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private RMCaptureOutInterceptor rmCaptureOut = new RMCaptureOutInterceptor();
    private RMSoapOutInterceptor rmOutSoap = new RMSoapOutInterceptor();
    private RMSoapInInterceptor rmInSoap = new RMSoapInInterceptor();
    private RMDeliveryInterceptor rmDelivery = new RMDeliveryInterceptor();

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmOutSoap' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private RMCaptureInInterceptor rmCaptureIn = new RMCaptureInInterceptor();
    private RMCaptureOutInterceptor rmCaptureOut = new RMCaptureOutInterceptor();
    private RMSoapOutInterceptor rmOutSoap = new RMSoapOutInterceptor();
    private RMSoapInInterceptor rmInSoap = new RMSoapInInterceptor();
    private RMDeliveryInterceptor rmDelivery = new RMDeliveryInterceptor();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmCaptureOut' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private RMOutInterceptor rmOut = new RMOutInterceptor();
    private RMCaptureInInterceptor rmCaptureIn = new RMCaptureInInterceptor();
    private RMCaptureOutInterceptor rmCaptureOut = new RMCaptureOutInterceptor();
    private RMSoapOutInterceptor rmOutSoap = new RMSoapOutInterceptor();
    private RMSoapInInterceptor rmInSoap = new RMSoapInInterceptor();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmCaptureIn' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private RMInInterceptor rmIn = new RMInInterceptor();
    private RMOutInterceptor rmOut = new RMOutInterceptor();
    private RMCaptureInInterceptor rmCaptureIn = new RMCaptureInInterceptor();
    private RMCaptureOutInterceptor rmCaptureOut = new RMCaptureOutInterceptor();
    private RMSoapOutInterceptor rmOutSoap = new RMSoapOutInterceptor();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmIn' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private static final long serialVersionUID = -9134254448692671780L;
    private static final Collection<QName> ASSERTION_TYPES;
    private RMInInterceptor rmIn = new RMInInterceptor();
    private RMOutInterceptor rmOut = new RMOutInterceptor();
    private RMCaptureInInterceptor rmCaptureIn = new RMCaptureInInterceptor();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmDelivery' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private RMSoapOutInterceptor rmOutSoap = new RMSoapOutInterceptor();
    private RMSoapInInterceptor rmInSoap = new RMSoapInInterceptor();
    private RMDeliveryInterceptor rmDelivery = new RMDeliveryInterceptor();

    static {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'rmOut' in a Serializable class
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RMPolicyInterceptorProvider.java`
#### Snippet
```java
    private static final Collection<QName> ASSERTION_TYPES;
    private RMInInterceptor rmIn = new RMInInterceptor();
    private RMOutInterceptor rmOut = new RMOutInterceptor();
    private RMCaptureInInterceptor rmCaptureIn = new RMCaptureInInterceptor();
    private RMCaptureOutInterceptor rmCaptureOut = new RMCaptureOutInterceptor();
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'token' in a Serializable class
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/JwtUserSubject.java`
#### Snippet
```java
public class JwtUserSubject extends UserSubject {
    private static final long serialVersionUID = -1135272749329239037L;
    private JwtToken token;
    public JwtUserSubject(String user, JwtToken token) {
        super(user);
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'joseProducer' in a Serializable class
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JwtRequestCodeGrant.java`
#### Snippet
```java
public class JwtRequestCodeGrant extends AuthorizationCodeGrant {
    private static final long serialVersionUID = -3738825769770411453L;
    private OAuthJoseJwtProducer joseProducer = new OAuthJoseJwtProducer();
    private String clientSecret;
    private String issuer;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'error' in a Serializable class
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/OAuthServiceException.java`
#### Snippet
```java

    private static final long serialVersionUID = 343738539234766320L;
    private OAuthError error;

    public OAuthServiceException() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'details' in a Serializable class
in `testutils/src/main/java/org/apache/cxf/customer/CustomerNotFoundFault.java`
#### Snippet
```java
public class CustomerNotFoundFault extends Exception {
    private static final long serialVersionUID = -2104569359859457879L;
    private CustomerNotFoundDetails details;

    public CustomerNotFoundFault(CustomerNotFoundDetails details) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'details' in a Serializable class
in `testutils/src/main/java/org/apache/cxf/customer/book/BookNotFoundFault.java`
#### Snippet
```java
public class BookNotFoundFault extends Exception {
    private static final long serialVersionUID = 497738666759033678L;
    private BookNotFoundDetails details;

    public BookNotFoundFault(BookNotFoundDetails details) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'jcaBusFactory' in a Serializable class
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java
    private static final ResourceBundle BUNDLE = BundleUtils.getBundle(ManagedConnectionFactoryImpl.class);

    protected JCABusFactory jcaBusFactory;

    public ManagedConnectionFactoryImpl() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'printWriter' in a Serializable class
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(AbstractManagedConnectionFactoryImpl.class);

    private PrintWriter printWriter;

    public AbstractManagedConnectionFactoryImpl() {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'printWriter' in a Serializable class
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionFactoryImpl.java`
#### Snippet
```java

    private String busConfigURL;
    private PrintWriter printWriter;
    private ResourceAdapter resourceAdapter;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'detail' in a Serializable class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java
    private JMSFaultType jmsFaultType;
    private boolean sender;
    private Object detail;

    public JMSFault(String message) {
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'jmsFaultType' in a Serializable class
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFault.java`
#### Snippet
```java

    private static final long serialVersionUID = 8567771013189842711L;
    private JMSFaultType jmsFaultType;
    private boolean sender;
    private Object detail;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'interceptor' in a Serializable class
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/IgnorablePolicyInterceptorProvider.java`
#### Snippet
```java
    private static final Logger LOG = LogUtils.getL7dLogger(IgnorablePolicyInterceptorProvider.class);

    private IgnorableAssertionsInterceptor interceptor = new IgnorableAssertionsInterceptor();

    /**
```

## RuleId[ruleID=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/claims/SAMLSecurityContext.java`
#### Snippet
```java
        }
        Set<Principal> retRoles = new HashSet<>(roles);
        if (principal != null && retRoles.contains(principal)) {
            retRoles.remove(principal);
        }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
            List<String> protocols = new LinkedList<>(Arrays.asList("TLSv1", "TLSv1.1", "TLSv1.2", "SSLv3"));
            for (String excludedProtocol : tlsServerParameters.getExcludeProtocols()) {
                if (protocols.contains(excludedProtocol)) {
                    protocols.remove(excludedProtocol);
                }
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java

                                if (Modifier.isAbstract(cls.getModifiers())
                                    && resourceTypes.getSubstitutions().values().contains(cls)) {
                                    newElement.setAttribute("abstract", "true");
                                }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `core/src/main/java/org/apache/cxf/resource/DefaultResourceManager.java`
#### Snippet
```java

    public final void removeResourceResolver(ResourceResolver resolver) {
        if (registeredResolvers.contains(resolver)) {
            registeredResolvers.remove(resolver);
        }
```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
    public void writeEndElement() throws XMLStreamException {
        addEvent(factory.createEndElement(lastStart,
                                          Collections.<javax.xml.stream.events.Namespace>emptyList().iterator()));
        curContext = contexts.pop();
        lastStart = elNames.pop();
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap11Expression2)) {
                xPaths.remove(soap11Expression2);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression2);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap12Expression);
            }
            if (xPaths.contains(soap12Expression2)) {
                xPaths.remove(soap12Expression2);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
            }
        } else {
            if (xPaths.contains(soap11Expression)) {
                xPaths.remove(soap11Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultCryptoCoverageChecker.java`
#### Snippet
```java
                xPaths.remove(soap11Expression);
            }
            if (xPaths.contains(soap12Expression)) {
                xPaths.remove(soap12Expression);
            }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
            List<WSSConstants.Action> actionList = properties.getActions();
            if (actionList.contains(WSSConstants.SAML_TOKEN_SIGNED)
                && actionList.contains(XMLSecurityConstants.SIGNATURE)) {
                actionList.remove(XMLSecurityConstants.SIGNATURE);
            }
```

### RedundantCollectionOperation
Unnecessary 'Arrays.asList()' call
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/WSSecurityPolicyLoader.java`
#### Snippet
```java

        //add generic assertions for these known things to prevent warnings
        List<QName> others = Arrays.asList(new QName[] {
            SP12Constants.INCLUDE_TIMESTAMP, SP11Constants.INCLUDE_TIMESTAMP,
            SP12Constants.ENCRYPT_SIGNATURE, SP11Constants.ENCRYPT_SIGNATURE,
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/MemoryTokenStore.java`
#### Snippet
```java

    public void remove(String identifier) {
        if (!StringUtils.isEmpty(identifier) && tokens.containsKey(identifier)) {
            tokens.remove(identifier);
        }
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/cache/HazelCastTokenStore.java`
#### Snippet
```java

    public void remove(String identifier) {
        if (!StringUtils.isEmpty(identifier) && getCacheMap().containsKey(identifier)) {
            getCacheMap().remove(identifier);
        }
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticClaimsHandler.java`
#### Snippet
```java
        ProcessedClaimCollection claimsColl = new ProcessedClaimCollection();
        for (Claim claim : claims) {
            if (getGlobalClaims().keySet().contains(claim.getClaimType().toString())) {
                ProcessedClaim c = new ProcessedClaim();
                c.setClaimType(claim.getClaimType());
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/StaticEndpointClaimsHandler.java`
#### Snippet
```java
        }
        for (Claim claim : claims) {
            if (endpointClaims.keySet().contains(claim.getClaimType().toString())) {
                ProcessedClaim c = new ProcessedClaim();
                c.setClaimType(claim.getClaimType());
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/LdapClaimsHandler.java`
#### Snippet
```java
            for (Claim claim : claims) {
                String claimType = claim.getClaimType().toString();
                if (getClaimsLdapAttributeMapping().keySet().contains(claimType)) {
                    searchAttributeList.add(
                        getClaimsLdapAttributeMapping().get(claimType)
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
        Map<?, ?> namespaces = definition.getNamespaces();
        if (isSOAP12
            && !namespaces.values().contains(WSDLConstants.NS_SOAP12)) {
            definition.addNamespace("soap12", WSDLConstants.NS_SOAP12);
        } else if (!namespaces.values().contains(WSDLConstants.NS_SOAP11)) {
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
            && !namespaces.values().contains(WSDLConstants.NS_SOAP12)) {
            definition.addNamespace("soap12", WSDLConstants.NS_SOAP12);
        } else if (!namespaces.values().contains(WSDLConstants.NS_SOAP11)) {
            definition.addNamespace("soap", WSDLConstants.NS_SOAP11);
        }
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java

    public static boolean isPrimitiveTypeCode(TypeCode tc) {
        return PRIMITIVE_TYPECODES.values().contains(tc.kind());
    }

```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        Map<?, ?> namespaces = definition.getNamespaces();
        if (isSOAP12
            && !namespaces.values().contains(WSDLConstants.NS_SOAP12)) {
            definition.addNamespace("soap12", WSDLConstants.NS_SOAP12);
        } else if (!namespaces.values().contains(WSDLConstants.NS_SOAP11)) {
```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
            && !namespaces.values().contains(WSDLConstants.NS_SOAP12)) {
            definition.addNamespace("soap12", WSDLConstants.NS_SOAP12);
        } else if (!namespaces.values().contains(WSDLConstants.NS_SOAP11)) {
            definition.addNamespace("soap", WSDLConstants.NS_SOAP11);
        }
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
        if (additional != null) {
            for (AegisType t : additional) {
                if (!deps.contains(t)) {
                    deps.add(t);
                }
```

## RuleId[ruleID=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `core/src/main/java/org/apache/cxf/annotations/EndpointProperties.java`
#### Snippet
```java
    /**
     * The properties
     * @return the list of properties to set
     */
    EndpointProperty[] value();
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/validationtransformation/ResourceTypeIdentifier.java`
#### Snippet
```java

    /**
     * Returns true if the representation corresponds to type, which object represents.
     * @param representation
     * @return
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/UserSubject.java`
#### Snippet
```java
     * Get the list of additional user subject properties
     *
     * @return the list of properties
     */
    @ElementCollection(fetch = FetchType.EAGER)
```

### MismatchedJavadocCode
Method is specified to return list but the return type is map
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/common/Client.java`
#### Snippet
```java
    /**
     * Get the list of additional client properties
     * @return the list of properties
     */
    @ElementCollection(fetch = FetchType.EAGER)
```

## RuleId[ruleID=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java

            }
        } catch (Throwable t) {

        }
```

### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
                mergeHeaders(headersFactoryClass, headers);
            }
        } catch (Throwable t) {
            throwException(t);
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/proxy/MicroProfileClientProxyImpl.java`
#### Snippet
```java
            try {
                m = computeClass.getMethod(methodName);
            } catch (NoSuchMethodException expected2) { }
        }

```

### CatchMayIgnoreException
Empty `catch` block
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/code/JPACodeDataProvider.java`
#### Snippet
```java
        try {
            em.remove(grant);
        } catch (EntityNotFoundException e) {
        }
        return grant;
```

### CatchMayIgnoreException
Empty `catch` block
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadata.java`
#### Snippet
```java
            try {
                return new URL(s);
            } catch (MalformedURLException e) {
            }
        }
```

## RuleId[ruleID=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                value = buffer[initialI + 2];
                if ((byte) value != boundary[0]) {
                    i++;
                    continue;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                }
                needUnread0d0a = true;
                i += 2; //i after this points to boundary[0] element
            } else if (value != boundary[0]) {
                continue;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                    return initialI - off;
                }
                value = buffer[++i];
                boundaryIndex++;
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                // read the end of line character
                if (initialI != off) {
                    i = 1000000000;
                }
                if (initialI - off != 0
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
                    // Last mime boundary should have a succeeding "--"
                    // as we are on it, read the terminating CRLF
                    i += 2;
                    //last mime boundary
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
            if (value != -1) { //pushing back first byte of boundary
                // Stream might have ended
                i--;
            }
            if (needUnread0d0a) { //Pushing all,  returning 13
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
            }
            if (needUnread0d0a) { //Pushing all,  returning 13
                i = i - boundaryIndex;
                i--; //for 10
//                value = 13;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
            if (needUnread0d0a) { //Pushing all,  returning 13
                i = i - boundaryIndex;
                i--; //for 10
//                value = 13;
            } else {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
//                value = 13;
            } else {
                i = i - boundaryIndex;
                i++;
//                value = boundary[0];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `core/src/main/java/org/apache/cxf/attachment/MimeBodyPartInputStream.java`
#### Snippet
```java
            } else {
                i = i - boundaryIndex;
                i++;
//                value = boundary[0];
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
        for (int x = 0; x < args.length; x++) {
            if ("-brokerUrl".equals(args[x])) {
                activeMqUrl = args[++x];
            } else if ("-userName".equals(args[x])) {
                userName = args[++x];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
                activeMqUrl = args[++x];
            } else if ("-userName".equals(args[x])) {
                userName = args[++x];
            } else if ("-password".equals(args[x])) {
                password = args[++x];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
                userName = args[++x];
            } else if ("-password".equals(args[x])) {
                password = args[++x];
            } else if ("-rootUrl".equals(args[x])) {
                rootURL = args[++x];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
                password = args[++x];
            } else if ("-rootUrl".equals(args[x])) {
                rootURL = args[++x];
            } else if ("-jmxEnable".equals(args[x])) {
                jmxEnable = Boolean.valueOf(args[++x]);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
                rootURL = args[++x];
            } else if ("-jmxEnable".equals(args[x])) {
                jmxEnable = Boolean.valueOf(args[++x]);
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
                readJsonObjectAsSettable(nextMap, newJson);
                values.put(name, nextMap.map);
                i = closingIndex + 1;
            } else if (json.charAt(sepIndex + j) == ARRAY_START) {
                int closingIndex = getClosingIndex(json, ARRAY_START, ARRAY_END, sepIndex + j);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
                String newJson = json.substring(sepIndex + j + 1, closingIndex);
                values.put(name, internalFromJsonAsList(name, newJson));
                i = closingIndex + 1;
            } else {
                int commaIndex = getCommaIndex(json, sepIndex + j);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
                Object value = readPrimitiveValue(name, json, sepIndex + j, commaIndex);
                values.put(name, value);
                i = commaIndex + 1;
            }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
                readJsonObjectAsSettable(nextMap, json.substring(i + 1, closingIndex));
                values.add(nextMap.map);
                i = closingIndex + 1;
            } else {
                int commaIndex = getCommaIndex(json, i);
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
                Object value = readPrimitiveValue(name, json, i, commaIndex);
                values.add(value);
                i = commaIndex;
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
            case '*':
                buf.insert(x, '.');
                x++;
                break;
            case '.':
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `x`
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/BaseHandlerChainBuilder.java`
#### Snippet
```java
            case ')':
                buf.insert(x, '\\');
                x++;
                break;
            default:
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ClassReader.java`
#### Snippet
```java
                // see jvm spec section 4.4.5 - double and long cpool
                // entries occupy two "slots" in the cpool table.
                i++;
                readInt(); // value
                break;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                String arg = args[i];
                if ("--port".equals(arg) || "-p".equals(arg)) {
                    portName = args[++i];
                } else if ("--service".equals(arg) || "-s".equals(arg)) {
                    serviceName = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                    portName = args[++i];
                } else if ("--service".equals(arg) || "-s".equals(arg)) {
                    serviceName = args[++i];
                } else if ("--jmx".equals(arg) || "-j".equals(arg)) {
                    jmxServerURL = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                    serviceName = args[++i];
                } else if ("--jmx".equals(arg) || "-j".equals(arg)) {
                    jmxServerURL = args[++i];
                } else if ("--operation".equals(arg) || "-o".equals(arg)) {
                    operationName = args[++i];
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                    jmxServerURL = args[++i];
                } else if ("--operation".equals(arg) || "-o".equals(arg)) {
                    operationName = args[++i];
                    // it is the key option
                    result = true;
```

## RuleId[ruleID=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/SamlRequestComponentBuilder.java`
#### Snippet
```java
                SAMLProfileConstants.SAML20XACMLPROTOCOL_PREFIX
            );
        authzQuery.setID("_" + UUID.randomUUID().toString());
        authzQuery.setVersion(SAMLVersion.VERSION_20);
        authzQuery.setIssueInstant(Instant.now());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/AbstractXmlSecOutInterceptor.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSigOutInterceptor.java`
#### Snippet
```java
        }

        String id = "_" + UUID.randomUUID().toString();
        String referenceId = "#" + id;

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlFormOutInterceptor.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SamlHeaderOutInterceptor.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SAMLUtils.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SAMLUtils.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/SAMLUtils.java`
#### Snippet
```java
            ex.printStackTrace(new PrintWriter(sw));
            LOG.warning(sw.toString());
            throw new Fault(new RuntimeException(ex.getMessage() + ", stacktrace: " + sw.toString()));
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/LoadDistributorTargetSelector.java`
#### Snippet
```java
            } catch (NullPointerException ex) {
                getLogger().fine("Strategy " + getStrategy().getClass()
                        + " cannot handle a null argument to getAlternateAddresses: " + ex.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/sql/SQLPrinterVisitor.java`
#### Snippet
```java
    public String getQuery() {
        StringBuilder sb = removeStringBuilder();
        return sb == null ? null : topBuilder.toString() + " WHERE " + sb.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/io/CachedOutputStream.java`
#### Snippet
```java
            return fileInputStream;
        } catch (FileNotFoundException e) {
            throw new IOException("Cached file was deleted, " + e.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/io/CachedWriter.java`
#### Snippet
```java
            return new InputStreamReader(fileInputStream, StandardCharsets.UTF_8);
        } catch (FileNotFoundException e) {
            throw new IOException("Cached file was deleted, " + e.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/common/spi/GeneratedClassClassLoader.java`
#### Snippet
```java
            return ClassLoaderUtils.loadClass(className, callingClass);
        } catch (ClassNotFoundException e) {
            LOG.fine("Failed to load class :" + e.toString());
        }
        return null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/common/spi/ClassLoaderProxyService.java`
#### Snippet
```java
            return cls.getConstructor(Map.class).newInstance(map);
        } catch (Throwable e) {
            LOG.warning("NamespaceWrapper not found : " + e.toString());
            return null;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                    errors.add(diagnostic.toString());
                    if (verbose) {
                        System.err.println(diagnostic.toString());
                    }
                    break;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
                    warnings.add(diagnostic.toString());
                    if (verbose) {
                        System.err.println(diagnostic.toString());
                    }
                    break;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        sbMain.append('>');
        handleApplicationDocs(sbMain);
        sbMain.append(sbGrammars.toString());
        sbMain.append(sbResources.toString());
        sbMain.append("</application>");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        handleApplicationDocs(sbMain);
        sbMain.append(sbGrammars.toString());
        sbMain.append(sbResources.toString());
        sbMain.append("</application>");
        return sbMain;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
                StringWriter writer = new StringWriter();
                xs.write(writer);
                sb.append(writer.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        }

        logger.log(Level.FINE, "Created classes: " + sb.toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/resource/URIResolver.java`
#### Snippet
```java
            URI u = new URI(archiveEntry).resolve(uriStr);

            tryArchive(archiveBase + u.toString());

            if (is != null) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        UUID result = new UUID(mostSigBits, leastSigBits);

        return "uuid:" + result.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `core/src/main/java/org/apache/cxf/interceptor/AbstractLoggingInterceptor.java`
#### Snippet
```java
        String result = swriter.toString();
        if (result.length() < limit || limit == -1) {
            builder.append(swriter.toString());
        } else {
            builder.append(swriter.toString().substring(0, limit));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/java2ws-plugin/src/main/java/org/apache/cxf/maven_plugin/ClassLoaderSwitcher.java`
#### Snippet
```java
        String newCp = buf.toString();

        log.debug("Classpath: " + urlList.toString());

        origProps = (Properties)System.getProperties().clone();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        message.put(USE_ASYNC, Boolean.TRUE);
        if (LOG.isLoggable(Level.FINE)) {
            LOG.fine("Asynchronous connection to " + uri.toString() + " has been set up");
        }
        message.put("http.scheme", uri.getScheme());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        message.put(USE_ASYNC, Boolean.TRUE);
        if (LOG.isLoggable(Level.FINE)) {
            LOG.fine("Asynchronous connection to " + uri.toString() + " has been set up");
        }
        message.put("http.scheme", uri.getScheme());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
            return null;
        }
        return name.toString() + ".client.proxyFactory";
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveFeature.java`
#### Snippet
```java

        public Portable() {
            this("cxf-svc-" + UUID.randomUUID().toString());
        }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveFeature.java`
#### Snippet
```java
public class BraveFeature extends DelegatingFeature<BraveFeature.Portable> {
    public BraveFeature() {
        this("cxf-svc-" + UUID.randomUUID().toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/jaxrs/BraveFeature.java`
#### Snippet
```java

    public BraveFeature() {
        this("cxf-svc-" + UUID.randomUUID().toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
            String base = request.getScheme() + "://" + serverAndPort;
            if (!absolute) {
                u = URI.create(base + u.toString());
            } else {
                int originalPort = u.getPort();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/DynamicClientFactory.java`
#### Snippet
```java

        // our hashcode + timestamp ought to be enough.
        String stem = toString() + "-" + System.currentTimeMillis();
        File src = new File(tmpdir, stem + "-src");
        if (!src.mkdir()) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractResponseViewProvider.java`
#### Snippet
```java
                    sb.append(locationPrefix);
                }
                sb.append(resourcePathProp.toString());
                if (resourceExtension != null) {
                    sb.append(resourceExtension);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractResponseViewProvider.java`
#### Snippet
```java
            } catch (Exception e) {
                LOG.log(Level.SEVERE, String.format("Error forwarding to error page '%s': %s",
                        errorView, e.toString()),
                        e);
                handleInternalViewRenderingException(exception);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/BinaryDataProvider.java`
#### Snippet
```java
                    + " providers is not recommended - use InputStream or custom File reader");
                // create a temp file, delete on exit
                File f = FileUtils.createTempFile("File" + UUID.randomUUID().toString(),
                                                  "jaxrs",
                                                  null,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
                W3CDOMStreamWriter writer = new W3CDOMStreamWriter();
                writer.writeStartElement("wsp", "PolicyReference", URI_POLICY_NS);
                writer.writeAttribute("URI", "#" + polRefId.toString());
                writer.writeEndElement();
                Element pr = writer.getDocument().getDocumentElement();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/AuthorizationMetadataService.java`
#### Snippet
```java
                        uri.getPath(), uri.getQuery(), uri.getFragment());
            } catch (URISyntaxException e) {
                throw new IllegalArgumentException("Invalid URI " + uri + " : " + e.toString(), e);
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/event/LoggerPatternLayoutLogback.java`
#### Snippet
```java
            line.append(item).append(';');
        }
        return line.toString() + System.getProperty("line.separator");
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java
                    if (LOG.isLoggable(Level.FINE)) {
                        LOG.fine(
                            "AttributeStatements " + statementBean.toString()
                            + " returned by AttributeStatementProvider "
                            + statementProvider.getClass().getName()
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java
                    if (LOG.isLoggable(Level.FINE)) {
                        LOG.fine(
                            "AuthenticationStatement " + statementBean.toString()
                            + " returned by AuthenticationStatementProvider "
                            + statementProvider.getClass().getName()
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/SAMLTokenProvider.java`
#### Snippet
```java
                    if (LOG.isLoggable(Level.FINE)) {
                        LOG.fine(
                            "AuthDecisionStatement " + statementBean.toString()
                            + " returned by AuthDecisionStatementProvider "
                            + statementProvider.getClass().getName()
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            throw new CorbaBindingException("Failed to getAddressWithId, reason:" + e.toString(), e);
        }
        return ref;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoader.java`
#### Snippet
```java
                // we should find everything we look for but if we don't our
                // parent can when we throw cnf below
                LOG.fine("findClass: " + name + ": " + ex.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/common/ClassLoaderSwitcher.java`
#### Snippet
```java
        String newCp = buf.toString();

        log.debug("Classpath: " + urlList.toString());

        origProps = (Properties)System.getProperties().clone();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyVerificationOutInterceptor.java`
#### Snippet
```java
                     + "this request was satisfied.  However, this exception will not result in "
                     + "a fault.  The exception raised is: "
                     + e.toString());
            return;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyUtils.java`
#### Snippet
```java
        StringBuilder buf = new StringBuilder();
        printPolicyComponent(pc, buf, 0);
        System.out.println(buf.toString());
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
                    Object nameProperty = message.getExchange().get("org.apache.cxf.resource.operation.name");
                    if (nameProperty != null) {
                        operationName = "\"" + nameProperty.toString() + "\"";
                    }
                }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
        }

        return protocol + "://" + sb.toString();
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapArrayType.java`
#### Snippet
```java
                        + getDimensions() + " dimensions, but arrayType has "
                        + arrayTypeInfo.getTotalDimensions() + " dimensions: "
                        + arrayTypeInfo.toString());
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/ClassLoaderSwitcher.java`
#### Snippet
```java
        String newCp = buf.toString();

        log.debug("Classpath: " + urlList.toString());

        origProps = (Properties)System.getProperties().clone();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/utils/FilteringUtil.java`
#### Snippet
```java
            return true;
        } catch (SOAPException e) {
            LOG.severe("SOAPException in runFilterOnMessage: " + e.toString());
            return false;
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
        builder.append("Packagenames: ").append(packagenames).append('\n');
        if (wsdlArtifact != null) {
            builder.append("Artifact: ").append(wsdlArtifact.toString()).append('\n');
        }
        builder.append('\n');
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
        if (b.toString().contains("WSDL2Java Error")) {
            StringBuilder msg = new StringBuilder();
            msg.append(b.toString());
            msg.append('\n');
            msg.append("Command line was: ").append(cmdLine).append('\n').append('\n');
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
                if (!supposedFile.exists() || !supposedFile.isFile()) {
                    getLog().info("Apparent Maven bug: wsdl artifact 'resolved' to "
                                      + supposedFile.getAbsolutePath() + " for " + wsdlArtifact.toString());
                    continue;
                }
```

## RuleId[ruleID=SetReplaceableByEnumSet]
### SetReplaceableByEnumSet
`HashSet<>` can be replaced with 'EnumSet'
in `rt/rs/security/jose-parent/jose-jaxrs/src/main/java/org/apache/cxf/rs/security/jose/jaxrs/AbstractJweJsonWriterProvider.java`
#### Snippet
```java
        // If only one then save it in the shared protected headers as opposed to
        // per-recipient specific not protected ones
        Set<KeyAlgorithm> keyAlgos = new HashSet<>();
        
        List<KeyEncryptionProvider> keyProviders = new LinkedList<>();
```

## RuleId[ruleID=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `HTTPClientPolicyAssertion` may be 'static'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/HTTPClientAssertionBuilder.java`
#### Snippet
```java
    }

    class HTTPClientPolicyAssertion extends JaxbAssertion<HTTPClientPolicy> {
        HTTPClientPolicyAssertion() {
            super(new ClientPolicyCalculator().getDataClassName(), false);
```

### InnerClassMayBeStatic
Inner class `HTTPServerPolicyAssertion` may be 'static'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/policy/HTTPServerAssertionBuilder.java`
#### Snippet
```java
    }

    class HTTPServerPolicyAssertion extends JaxbAssertion<HTTPServerPolicy> {
        HTTPServerPolicyAssertion() {
            super(new ServerPolicyCalculator().getDataClassName(), false);
```

### InnerClassMayBeStatic
Inner class `ResponseExceptionMapperComparator` may be 'static'
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/MicroProfileClientProviderFactory.java`
#### Snippet
```java
    }

    private class ResponseExceptionMapperComparator implements Comparator<ResponseExceptionMapper<?>> {

        @Override
```

### InnerClassMayBeStatic
Inner class `XmlSecStaxOutInterceptorInternal` may be 'static'
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlSecOutInterceptor.java`
#### Snippet
```java
    }

    final class XmlSecStaxOutInterceptorInternal extends AbstractPhaseInterceptor<Message> {
        XmlSecStaxOutInterceptorInternal() {
            super(Phase.PRE_STREAM_ENDING);
```

### InnerClassMayBeStatic
Inner class `ClassValue` may be 'static'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/visitor/AbstractSearchConditionVisitor.java`
#### Snippet
```java
    }

    protected class ClassValue {
        private Class<?> cls;
        private Object value;
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `core/src/main/java/org/apache/cxf/common/spi/ClassLoaderProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends ClassLoaderProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new NamespaceClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `core/src/main/java/org/apache/cxf/common/spi/ClassLoaderProxyService.java`
#### Snippet
```java
        }
    }
    public class LoadFirst extends ClassLoaderProxyService {
        public LoadFirst(Bus bus) {
            super(new GeneratedNamespaceClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `SchemaCollectionWriter` may be 'static'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    private class SchemaCollectionWriter implements SchemaWriter {

        private final SchemaCollection coll;
```

### InnerClassMayBeStatic
Inner class `StringSchemaWriter` may be 'static'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    private class StringSchemaWriter implements SchemaWriter {

        private final List<String> theSchemas;
```

### InnerClassMayBeStatic
Inner class `ExternalSchemaWriter` may be 'static'
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    }

    private class ExternalSchemaWriter implements SchemaWriter {

        private final List<URI> links;
```

### InnerClassMayBeStatic
Inner class `IllegalEmptyResponseException` may be 'static'
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java

    
    public class IllegalEmptyResponseException extends IllegalStateException {
        private static final long serialVersionUID = 1L;

```

### InnerClassMayBeStatic
Inner class `AttachmentOutEndingInterceptor` may be 'static'
in `core/src/main/java/org/apache/cxf/interceptor/AttachmentOutInterceptor.java`
#### Snippet
```java
    }

    public class AttachmentOutEndingInterceptor extends AbstractPhaseInterceptor<Message> {
        public AttachmentOutEndingInterceptor() {
            super(Phase.PRE_STREAM_ENDING);
```

### InnerClassMayBeStatic
Inner class `DelegateAnnotatedField` may be 'static'
in `integration/cdi/src/main/java/org/apache/cxf/cdi/DelegateContextAnnotatedType.java`
#### Snippet
```java
    }

    private final class DelegateAnnotatedField<Y> implements AnnotatedField<Y> {
        private final AnnotatedField<Y> original;
        private final Set<Annotation> annotationSet;
```

### InnerClassMayBeStatic
Inner class `StaxStartBodyInterceptor` may be 'static'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java
     * then WS-Security processing does not happen correctly).
     */
    private class StaxStartBodyInterceptor extends AbstractSoapInterceptor {

        StaxStartBodyInterceptor() {
```

### InnerClassMayBeStatic
Inner class `WSS4JStaxOutInterceptorInternal` may be 'static'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxOutInterceptor.java`
#### Snippet
```java
    }

    final class WSS4JStaxOutInterceptorInternal extends AbstractPhaseInterceptor<Message> {
        WSS4JStaxOutInterceptorInternal() {
            super(Phase.PRE_STREAM_ENDING);
```

### InnerClassMayBeStatic
Inner class `SecureConversationSTSInvoker` may be 'static'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationInInterceptor.java`
#### Snippet
```java
    }

    public class SecureConversationSTSInvoker extends STSInvoker {

        void doIssue(
```

### InnerClassMayBeStatic
Inner class `SpnegoSTSInvoker` may be 'static'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenInInterceptor.java`
#### Snippet
```java
    }

    public class SpnegoSTSInvoker extends STSInvoker {

        void doIssue(
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/ExceptionClassCreatorProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends ExceptionClassCreatorProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new ExceptionClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/frontend/simple/src/main/java/org/apache/cxf/endpoint/dynamic/ExceptionClassCreatorProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends ExceptionClassCreatorProxyService {
        public LoadFirst(Bus bus) {
            super(new ExceptionClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `WrappedType` may be 'static'
in `rt/rs/extensions/reactor/src/main/java/org/apache/cxf/jaxrs/reactor/client/ReactorInvokerImpl.java`
#### Snippet
```java
    }

    private class WrappedType<R> implements ParameterizedType {
        private final Class<R> rClass;

```

### InnerClassMayBeStatic
Inner class `InboundSseEventListenerImpl` may be 'static'
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/SseEventSourceImpl.java`
#### Snippet
```java
    }
    
    private class InboundSseEventListenerImpl implements InboundSseEventListener {
        private final Consumer<InboundSseEvent> onEvent;
        private final Consumer<Throwable> onError;
```

### InnerClassMayBeStatic
Inner class `LocalDateConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class LocalDateConverter implements ParamConverter<LocalDate> {
        @Override
        public LocalDate fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `LocalTimeConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class LocalTimeConverter implements ParamConverter<LocalTime> {
        @Override
        public LocalTime fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `OffsetDateTimeConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class OffsetDateTimeConverter implements ParamConverter<OffsetDateTime> {
        @Override
        public OffsetDateTime fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `LocalDateTimeConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class LocalDateTimeConverter implements ParamConverter<LocalDateTime> {
        @Override
        public LocalDateTime fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `ZonedDateTimeConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class ZonedDateTimeConverter implements ParamConverter<ZonedDateTime> {
        @Override
        public ZonedDateTime fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `OffsetTimeConverter` may be 'static'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JavaTimeTypesParamConverterProvider.java`
#### Snippet
```java
    }

    public class OffsetTimeConverter implements ParamConverter<OffsetTime> {
        @Override
        public OffsetTime fromString(String value) {
```

### InnerClassMayBeStatic
Inner class `JaxwsServerHandler` may be 'static'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/AbstractJAXWSMethodInvoker.java`
#### Snippet
```java
    }

    class JaxwsServerHandler implements AsyncHandler<Object> {
        Response<Object> r;
        Continuation continuation;
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/WrapperClassCreatorProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends WrapperClassCreatorProxyService {
        public GenerateJustInTime(final Bus bus) {
            super(new WrapperClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/WrapperClassCreatorProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends WrapperClassCreatorProxyService {
        public LoadFirst(Bus bus) {
            super(new WrapperClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `StringBuilderOutput` may be 'static'
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
        Output append(char ch);
    }
    private class StringBuilderOutput implements Output {
        private StringBuilder sb;
        StringBuilderOutput(StringBuilder sb) {
```

### InnerClassMayBeStatic
Inner class `StreamOutput` may be 'static'
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java

    }
    private class StreamOutput implements Output {
        private OutputStream os;
        StreamOutput(OutputStream os) {
```

### InnerClassMayBeStatic
Inner class `DoubleAddInterceptorList` may be 'static'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    }

    class DoubleAddInterceptorList implements List<Interceptor<? extends Message>> {
        List<Interceptor<? extends Message>> orig;
        List<Interceptor<? extends Message>> other;
```

### InnerClassMayBeStatic
Inner class `DefaultSequenceIdentifierGenerator` may be 'static'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
    }

    class DefaultSequenceIdentifierGenerator implements SequenceIdentifierGenerator {

        public Identifier generateSequenceIdentifier() {
```

### InnerClassMayBeStatic
Inner class `RMPolicyAssertion` may be 'static'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM10AssertionBuilder.java`
#### Snippet
```java
    }

    class RMPolicyAssertion extends JaxbAssertion<RMAssertion> {
        RMPolicyAssertion() {
            super(RM10Constants.WSRMP_RMASSERTION_QNAME, false);
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/FactoryClassProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends FactoryClassProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new FactoryClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/FactoryClassProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends FactoryClassProxyService {
        public LoadFirst(Bus bus) {
            super(new FactoryClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends WrapperHelperProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new WrapperHelperClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/WrapperHelperProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends WrapperHelperProxyService {
        public LoadFirst(Bus bus) {
            super(new WrapperHelperClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/ExtensionClassCreatorProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends ExtensionClassCreatorProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new ExtensionClassGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/ExtensionClassCreatorProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends ExtensionClassCreatorProxyService {
        public LoadFirst(Bus bus) {
            super(new ExtensionClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `GreeterImpl` may be 'static'
in `testutils/src/main/java/org/apache/cxf/greeter_control/ControlImpl.java`
#### Snippet
```java
            endpointInterface = "org.apache.cxf.greeter_control.Greeter",
            targetNamespace = "http://cxf.apache.org/greeter_control")
    class GreeterImpl extends AbstractGreeterImpl {
    }

```

### InnerClassMayBeStatic
Inner class `CorbaOutputStream` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
    }

    private class CorbaOutputStream extends CachedOutputStream {

        /**
```

### InnerClassMayBeStatic
Inner class `CorbaSimpleAnyContainedTypeEventProducer` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaAnyEventProducer.java`
#### Snippet
```java


    class CorbaSimpleAnyContainedTypeEventProducer implements CorbaTypeEventProducer {
        int state;
        int[] states = {XMLStreamConstants.CHARACTERS};
```

### InnerClassMayBeStatic
Inner class `CorbaServiceNameEventProducer` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

    // An event producer to handle the production of the ServiceName XML data.
    class CorbaServiceNameEventProducer implements CorbaTypeEventProducer {
        int state;
        int[] states = {XMLStreamConstants.START_ELEMENT,
```

### InnerClassMayBeStatic
Inner class `CorbaAddressEventProducer` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

    // An event producer to handle the production of the Address XML data.
    class CorbaAddressEventProducer implements CorbaTypeEventProducer {
        int state;

```

### InnerClassMayBeStatic
Inner class `CorbaInterfaceNameEventProducer` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceEventProducer.java`
#### Snippet
```java

    // An event producer to handle the production of the InterfaceName XML data.
    class CorbaInterfaceNameEventProducer implements CorbaTypeEventProducer {
        int state;
        int[] states = {XMLStreamConstants.START_ELEMENT,
```

### InnerClassMayBeStatic
Inner class `GenerateJustInTime` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaFixedAnyImplClassCreatorProxyService.java`
#### Snippet
```java
        }
    }
    public class GenerateJustInTime extends CorbaFixedAnyImplClassCreatorProxyService {
        public GenerateJustInTime(Bus bus) {
            super(new CorbaFixedAnyImplGenerator(bus));
```

### InnerClassMayBeStatic
Inner class `LoadFirst` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaFixedAnyImplClassCreatorProxyService.java`
#### Snippet
```java
    }

    public class LoadFirst extends CorbaFixedAnyImplClassCreatorProxyService {
        public LoadFirst(Bus bus) {
            super(new CorbaFixedAnyImplClassLoader(bus));
```

### InnerClassMayBeStatic
Inner class `CorbaNamespaceContext` may be 'static'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java


    public class CorbaNamespaceContext implements NamespaceContext {

        private Map<String, String> map;
```

### InnerClassMayBeStatic
Inner class `WorkImpl` may be 'static'
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/WorkManagerThreadPool.java`
#### Snippet
```java
    }

    public class WorkImpl implements Work {

        private Runnable job;
```

### InnerClassMayBeStatic
Inner class `HandlerUrlConnection` may be 'static'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/Handler.java`
#### Snippet
```java
    }

    class HandlerUrlConnection extends URLConnection {
        HandlerUrlConnection(URL someUrl) {
            super(someUrl);
```

### InnerClassMayBeStatic
Inner class `Jetty11WebSocketHolder` may be 'static'
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
    }

    class Jetty11WebSocketHolder implements WebSocketServletHolder {
        final Session session;
        Jetty11WebSocketHolder(Session s) {
```

### InnerClassMayBeStatic
Inner class `SoapOutEndingInterceptor` may be 'static'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapOutInterceptor.java`
#### Snippet
```java
    }

    public class SoapOutEndingInterceptor extends AbstractSoapInterceptor {
        public SoapOutEndingInterceptor() {
            super(SoapOutEndingInterceptor.class.getName(), Phase.WRITE_ENDING);
```

### InnerClassMayBeStatic
Inner class `PublicKeyJwsVerificationSignature` may be 'static'
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/PublicKeyJwsSignatureVerifier.java`
#### Snippet
```java
    }

    private class PublicKeyJwsVerificationSignature implements JwsVerificationSignature {
        private Signature sig;
        PublicKeyJwsVerificationSignature(Signature sig) {
```

### InnerClassMayBeStatic
Inner class `ResponseTimeMessageInvokerEndingInteceptor` may be 'static'
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/ResponseTimeMessageInvokerInterceptor.java`
#### Snippet
```java
    }

    class ResponseTimeMessageInvokerEndingInteceptor extends AbstractMessageResponseTimeInterceptor {
        ResponseTimeMessageInvokerEndingInteceptor() {
            super(Phase.INVOKE);
```

## RuleId[ruleID=SuspiciousMethodCalls]
### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/interceptor/WSS4JBasicAuthValidator.java`
#### Snippet
```java
    protected void validate(Message message) throws WSSecurityException {

        AuthorizationPolicy policy = message.get(AuthorizationPolicy.class);
        if (policy == null || policy.getUserName() == null || policy.getPassword() == null) {
            String name = null;
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    public HTTPClientPolicy getClient(Message message) {
        ClientPolicyCalculator cpc = new ClientPolicyCalculator();
        HTTPClientPolicy pol = message.get(HTTPClientPolicy.class);
        updateClientPolicy(message);
        if (pol != null) {
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
    public AuthorizationPolicy getEffectiveAuthPolicy(Message message) {
        AuthorizationPolicy authPolicy = getAuthorization();
        AuthorizationPolicy newPolicy = message.get(AuthorizationPolicy.class);
        AuthorizationPolicy effectivePolicy = newPolicy;
        if (effectivePolicy == null) {
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `core/src/main/java/org/apache/cxf/interceptor/security/callback/CallbackHandlerProviderAuthPol.java`
#### Snippet
```java
    @Override
    public CallbackHandler create(Message message) {
        AuthorizationPolicy policy = message.get(AuthorizationPolicy.class);
        if (policy == null) {
            return null;
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AuthPolicyValidatingInterceptor.java`
#### Snippet
```java
    public void handleMessage(Message message) throws Fault {

        AuthorizationPolicy policy = message.get(AuthorizationPolicy.class);
        if (policy == null || policy.getUserName() == null || policy.getPassword() == null) {
            String name = null;
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/HttpsTokenInterceptorProvider.java`
#### Snippet
```java
        ) throws XMLSecurityException {
            List<SecurityEvent> securityEvents = getSecurityEventList(message);
            AuthorizationPolicy policy = message.get(AuthorizationPolicy.class);

            for (AssertionInfo ai : ais) {
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/SecurityContextImpl.java`
#### Snippet
```java

    public String getAuthenticationScheme() {
        if (m.get(AuthorizationPolicy.class) != null) {
            return SecurityContext.BASIC_AUTH;
        }
```

### SuspiciousMethodCalls
'Message' may not contain keys of type 'Class'
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
        }

        AuthorizationPolicy authPolicy = message.get(AuthorizationPolicy.class);
        if (authPolicy != null) {
            return authPolicy.getUserName();
```

## RuleId[ruleID=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/XMLBindingFactory.java`
#### Snippet
```java
public class XMLBindingFactory extends AbstractBindingFactory {
    public static final Collection<String> DEFAULT_NAMESPACES
        = Collections.unmodifiableList(Arrays.asList(
            "http://cxf.apache.org/bindings/xformat",
            "http://www.w3.org/2004/08/wsdl/http",
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java

    public static final List<String> DEFAULT_NAMESPACES
        = Collections.unmodifiableList(Arrays.asList(
            "http://cxf.apache.org/transports/http",
            "http://cxf.apache.org/transports/http/configuration",
```

### Java9CollectionFactory
Can be replaced with 'Map.ofEntries()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/EntityArrays.java`
#### Snippet
```java
        initialMap.put("\u00FE", "&thorn;"); // þ - lowercase thorn, Icelandic
        initialMap.put("\u00FF", "&yuml;"); // ÿ - lowercase y, umlaut
        ISO8859_1_ESCAPE = Collections.unmodifiableMap(initialMap);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.ofEntries()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/EntityArrays.java`
#### Snippet
```java
        // <!-- rsaquo is proposed but not yet ISO standardized -->
        initialMap.put("\u20AC", "&euro;"); // -- euro sign, U+20AC NEW -->
        HTML40_EXTENDED_ESCAPE = Collections.unmodifiableMap(initialMap);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/EntityArrays.java`
#### Snippet
```java
        initialMap.put("<", "&lt;");    // < - less-than
        initialMap.put(">", "&gt;");    // > - greater-than
        BASIC_ESCAPE = Collections.unmodifiableMap(initialMap);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/EntityArrays.java`
#### Snippet
```java
        final Map<CharSequence, CharSequence> initialMap = new HashMap<>();
        initialMap.put("'", "&apos;"); // XML apostrophe
        APOS_ESCAPE = Collections.unmodifiableMap(initialMap);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/commons_text/EntityArrays.java`
#### Snippet
```java
        initialMap.put("\f", "\\f");
        initialMap.put("\r", "\\r");
        JAVA_CTRL_CHARS_ESCAPE = Collections.unmodifiableMap(initialMap);
    }

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.ADDRESS_CLAIM));
    public static final List<String> PHONE_CLAIMS =
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.PHONE_CLAIM));

    public static final String CLAIMS_PARAM = "claims";
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java

    public static final List<String> PROFILE_CLAIMS =
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.NAME_CLAIM,
                                                   AbstractUserInfo.FAMILY_NAME_CLAIM,
                                                   AbstractUserInfo.GIVEN_NAME_CLAIM,
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java
                                                   AbstractUserInfo.EMAIL_VERIFIED_CLAIM));
    public static final List<String> ADDRESS_CLAIMS =
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.ADDRESS_CLAIM));
    public static final List<String> PHONE_CLAIMS =
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.PHONE_CLAIM));
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/utils/OidcUtils.java`
#### Snippet
```java
                                                   AbstractUserInfo.UPDATED_AT_CLAIM));
    public static final List<String> EMAIL_CLAIMS =
        Collections.unmodifiableList(Arrays.asList(AbstractUserInfo.EMAIL_CLAIM,
                                                   AbstractUserInfo.EMAIL_VERIFIED_CLAIM));
    public static final List<String> ADDRESS_CLAIMS =
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `core/src/main/java/org/apache/cxf/ws/addressing/Names.java`
#### Snippet
```java
        headers.add(WSA_MESSAGEID_QNAME);
        headers.add(WSA_RELATESTO_QNAME);
        HEADERS = Collections.unmodifiableSet(headers);
    }

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpTransportFactory.java`
#### Snippet
```java
public class NettyHttpTransportFactory extends AbstractTransportFactory implements DestinationFactory {

    public static final List<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays
        .asList("http://cxf.apache.org/transports/http/netty/server"));

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHttpTransportFactory.java`
#### Snippet
```java
public class AsyncHttpTransportFactory extends AbstractTransportFactory implements ConduitInitiator {

    public static final List<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays
        .asList("http://cxf.apache.org/transports/http/http-client"));

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHttpTransportFactory.java`
#### Snippet
```java
public class AsyncHttpTransportFactory extends AbstractTransportFactory implements ConduitInitiator {

    public static final List<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays
        .asList("http://cxf.apache.org/transports/http/http-client"));

```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSTransportFactory.java`
#### Snippet
```java

    public static final List<String> DEFAULT_NAMESPACES
        = Collections.unmodifiableList(Arrays.asList(
            "http://cxf.apache.org/transports/jms",
            "http://cxf.apache.org/transports/jms/configuration"
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBindingFactory.java`
#### Snippet
```java
    }
    public JAXRSBindingFactory(Bus b) {
        super(b, Collections.unmodifiableList(Arrays.asList(
                                                            JAXRS_BINDING_ID)));
    }
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java
        chars.add('r');
        chars.add('t');
        ESCAPED_CHARS = Collections.unmodifiableSet(chars);
    }

```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RM10Constants.java`
#### Snippet
```java
        headers.add(new QName(NAMESPACE_URI, SEQUENCE_ACK_NAME));
        headers.add(new QName(NAMESPACE_URI, ACK_REQUESTED_NAME));
        HEADERS = Collections.unmodifiableSet(headers);
    }

```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RM11Constants.java`
#### Snippet
```java
        headers.add(new QName(NAMESPACE_URI, SEQUENCE_ACK_NAME));
        headers.add(new QName(NAMESPACE_URI, ACK_REQUESTED_NAME));
        HEADERS = Collections.unmodifiableSet(headers);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        tmpClaimTypeMap.put("upn", CLAIM_TYPE_NS + "/claims/upn");
        tmpClaimTypeMap.put("nameidentifier", CLAIM_TYPE_NS + "/claims/nameidentifier");
        DEFAULT_CLAIM_TYPE_MAP = Collections.unmodifiableMap(tmpClaimTypeMap);

        Map<String, String> tmpTokenTypeMap = new HashMap<>();
```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/rest/RESTSecurityTokenServiceImpl.java`
#### Snippet
```java
        tmpTokenTypeMap.put("jwt", JWTTokenProvider.JWT_TOKEN_TYPE);
        tmpTokenTypeMap.put("sct", STSUtils.TOKEN_TYPE_SCT_05_12);
        DEFAULT_TOKEN_TYPE_MAP = Collections.unmodifiableMap(tmpTokenTypeMap);

        DEFAULT_KEY_TYPE_MAP.put("SymmetricKey", STSConstants.SYMMETRIC_KEY_KEYTYPE);
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketTransportFactory.java`
#### Snippet
```java

    public static final List<String> DEFAULT_NAMESPACES
        = Collections.unmodifiableList(Arrays.asList(
            "http://cxf.apache.org/transports/websocket",
            "http://cxf.apache.org/transports/websocket/configuration"
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
            "http://www.w3.org/2003/05/soap/bindings/HTTP/"));
    public static final Set<String> DEFAULT_PREFIXES
        = Collections.unmodifiableSet(new HashSet<>(Arrays.asList(
            "soap.udp"
        )));
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java
    public static final String TRANSPORT_ID = "http://schemas.xmlsoap.org/soap/";

    public static final List<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays.asList(
            "http://schemas.xmlsoap.org/soap/",
            "http://schemas.xmlsoap.org/wsdl/soap/",
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
@NoJSR250Annotations(unlessNull = { "bus" })
public class SoapBindingFactory extends AbstractWSDLBindingFactory {
    public static final Collection<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays.asList(
        "http://schemas.xmlsoap.org/soap/",
        "http://schemas.xmlsoap.org/wsdl/soap/",
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/external/URIDomainExpressionBuilder.java`
#### Snippet
```java

public class URIDomainExpressionBuilder implements DomainExpressionBuilder {
    private static final Collection<QName> SUPPORTED_TYPES = Collections.unmodifiableList(
        Arrays.asList(new QName[] {
            new QName("http://www.w3.org/ns/ws-policy", "URI"),
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/DefaultLogEventMapper.java`
#### Snippet
```java
        mediaTypes.add("image/gif");
        mediaTypes.add("image/bmp");
        DEFAULT_BINARY_CONTENT_MEDIA_TYPES = Collections.unmodifiableSet(mediaTypes);
    }
    private static final String MULTIPART_CONTENT_MEDIA_TYPE = "multipart";
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> EC_SHA_SIGN_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(ES_SHA_256_ALGO,
                                                                ES_SHA_384_ALGO,
                                                                ES_SHA_512_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> AES_GCM_KW_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(A128GCMKW_ALGO,
                                                                A192GCMKW_ALGO,
                                                                A256GCMKW_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> RSA_CEK_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(RSA_OAEP_ALGO,
                                                                RSA_OAEP_256_ALGO,
                                                                RSA1_5_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> AES_KW_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(A128KW_ALGO,
                                                                A192KW_ALGO,
                                                                A256KW_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> RSA_SHA_SIGN_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(RS_SHA_256_ALGO,
                                                                RS_SHA_384_ALGO,
                                                                RS_SHA_512_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> AES_GCM_CEK_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(A128GCM_ALGO,
                                                                A192GCM_ALGO,
                                                                A256GCM_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> ECDH_ES_WRAP_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(ECDH_ES_A128KW_ALGO,
                                                                ECDH_ES_A192KW_ALGO,
                                                                ECDH_ES_A256KW_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> PBES_HS_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(PBES2_HS256_A128KW_ALGO,
                                                                PBES2_HS384_A192KW_ALGO,
                                                                PBES2_HS512_A256KW_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> RSA_SHA_PS_SIGN_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(PS_SHA_256_ALGO,
                                                                PS_SHA_384_ALGO,
                                                                PS_SHA_512_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> ACBC_HS_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(A128CBC_HS256_ALGO,
                                                                A192CBC_HS384_ALGO,
                                                                A256CBC_HS512_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwa/AlgorithmUtils.java`
#### Snippet
```java

    public static final Set<String> HMAC_SIGN_SET =
        Collections.unmodifiableSet(new HashSet<>(Arrays.asList(HMAC_SHA_256_ALGO,
                                                                HMAC_SHA_384_ALGO,
                                                                HMAC_SHA_512_ALGO)));
```

### Java9CollectionFactory
Can be replaced with 'List.of()' call
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpTransportFactory.java`
#### Snippet
```java
public class NettyHttpTransportFactory extends AbstractTransportFactory implements ConduitInitiator {

    public static final List<String> DEFAULT_NAMESPACES = Collections.unmodifiableList(Arrays
        .asList("http://cxf.apache.org/transports/http/netty/client"));

```

## RuleId[ruleID=NestedSynchronizedStatement]
### NestedSynchronizedStatement
Nested `synchronized` statement
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/sse/SseSubscription.java`
#### Snippet
```java
        requested.addAndGet(n);
        synchronized (buffer) {
            synchronized (delivered) {
                InboundSseEvent bufferedEvent;
                while (delivered.get() < requested.get()
```

### NestedSynchronizedStatement
Nested `synchronized` statement
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
                    for (UnknownExtensibilityElement e : diext) {
                        final Element element = e.getElement();
                        synchronized (element.getOwnerDocument()) {
                            String uri = element.getAttributeNS(
                                PolicyConstants.WSU_NAMESPACE_URI,
```

## RuleId[ruleID=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/AttributeInfo.java`
#### Snippet
```java
            XmlSchemaAttribute attribute = (XmlSchemaAttribute)annotated;
            String attributeNamespaceURI = attribute.getQName().getNamespaceURI();
            boolean attributeNoNamespace = "".equals(attributeNamespaceURI);

            XmlSchema attributeSchema = null;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/NamespacePrefixAccumulator.java`
#### Snippet
```java

    private String getPrefix(String namespaceURI) {
        if ("".equals(namespaceURI)) {
            throw new RuntimeException("Prefix requested for default namespace.");
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/NamespacePrefixAccumulator.java`
#### Snippet
```java

    public void collect(String prefix, String uri) {
        if (!("".equals(uri)) && !prefixes.contains(prefix)) {
            attributes.append("xmlns:").append(prefix).append("='").append(uri).append("' ");
            prefixes.add(prefix);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/ParticleInfo.java`
#### Snippet
```java
            String elementNamespaceURI = elementQName.getNamespaceURI();

            boolean elementNoNamespace = "".equals(elementNamespaceURI);

            XmlSchema elementSchema = null;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
        QName elementQName = XmlSchemaUtils.getElementQualifiedName(element, xmlSchema);
        String elementNamespaceURI = elementQName.getNamespaceURI();
        boolean elementNoNamespace = "".equals(elementNamespaceURI);
        XmlSchema elementSchema = null;
        if (!elementNoNamespace) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
        String namespaceList = "[]";

        if (anyNamespaceSpec == null || "##any".equals(anyNamespaceSpec) || "".equals(anyNamespaceSpec)) {
            matchType = "org_apache_cxf_any_ns_matcher.ANY";
        } else if ("##other".equals(anyNamespaceSpec)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxIdleTimeStr = element.getAttribute("maxIdleTime");
        if (maxIdleTimeStr != null && !"".equals(maxIdleTimeStr.trim())) {
            bean.addPropertyValue("maxIdleTime", maxIdleTimeStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/Validator.java`
#### Snippet
```java
            headerNames.add(name);

            if (name == null || "".equals(name)) {
                throwException("CLIENT_HEADER_NO_NAME", userType.getName());
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/sse/SsePublisher.java`
#### Snippet
```java
                        } else if (line.startsWith(":")) {
                            builder.comment(removeSpace(line.substring(1)));
                        } else if ("".equals(line)) {
                            InboundSseEvent event = builder.build();
                            for (SseSubscription subscription : subscriptions) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/EHCacheTokenReplayCache.java`
#### Snippet
```java
     */
    public void putId(String identifier, Instant expiry) {
        if (identifier == null || "".equals(identifier)) {
            return;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/state/EHCacheSPStateManager.java`
#### Snippet
```java

    public void setResponseState(String securityContextKey, ResponseState state) {
        if (securityContextKey == null || "".equals(securityContextKey)) {
            return;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/state/EHCacheSPStateManager.java`
#### Snippet
```java

    public void setRequestState(String relayState, RequestState state) {
        if (relayState == null || "".equals(relayState)) {
            return;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxIdleTimeStr = element.getAttribute("maxIdleTime");
        if (maxIdleTimeStr != null && !"".equals(maxIdleTimeStr.trim())) {
            bean.addPropertyValue("maxIdleTime", maxIdleTimeStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/extensions/search/src/main/java/org/apache/cxf/jaxrs/ext/search/fiql/FiqlParser.java`
#### Snippet
```java
            String operator = m.group(1);
            String value = expr.substring(m.end(1));
            if ("".equals(value)) {
                throw new SearchParseException("Not a comparison expression: " + expr);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/bus/spring/BusApplicationContext.java`
#### Snippet
```java
                        String line = rd.readLine();
                        while (line != null) {
                            if (!"".equals(line)) {
                                resources.add(resolver.getResource(line));
                            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
                                                         String referencingURI) {
        String uri = name.getNamespaceURI();
        if ("".equals(uri)) {
            uri = referencingURI;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/common/xmlschema/XmlSchemaUtils.java`
#### Snippet
```java
    public static void addImportIfNeeded(XmlSchema schema, String namespaceUri) {
        // no need to import nothing or the XSD schema, or the schema we are fixing.
        if ("".equals(namespaceUri)
            || Constants.URI_2001_SCHEMA_XSD.equals(namespaceUri)
            || schema.getTargetNamespace().equals(namespaceUri)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java

    private void injectResourceClassLevel(Resource res) {
        if (res.name() == null || "".equals(res.name())) {
            LOG.log(Level.INFO, "RESOURCE_NAME_NOT_SPECIFIED", target.getClass().getName());
            return;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
    private String getFieldNameForResource(Resource res, Field field) {
        assert res != null;
        if (res.name() == null || "".equals(res.name())) {
            return field.getDeclaringClass().getCanonicalName() + "/" + field.getName();
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        }

        if (null != authority && !"".equals(authority)) {
            if ("urn".equals(scheme)) {
                packageName.append(authority);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/transport/HttpUriMapper.java`
#### Snippet
```java
            servletMap = path.substring(idx);
        }
        if ("".equals(servletMap) || "".equals(path)) {
            servletMap = "/";
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/transport/HttpUriMapper.java`
#### Snippet
```java
            servletMap = path.substring(idx);
        }
        if ("".equals(servletMap) || "".equals(path)) {
            servletMap = "/";
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/staxutils/StaxUtils.java`
#### Snippet
```java
                }
            } else {
                if ("xmlns".equals(name) && "".equals(attrPrefix)) {
                    writer.writeDefaultNamespace(attr.getNodeValue());
                    writer.setDefaultNamespace(attr.getNodeValue());
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java
        String id = elem.getAttribute(BeanDefinitionParserDelegate.ID_ATTRIBUTE);

        if (null == id || "".equals(id)) {
            String names = elem.getAttribute("name");
            if (null != names) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String hostStr = element.getAttribute("host");
        if (hostStr != null && !"".equals(hostStr.trim())) {
            bean.addPropertyValue("host", hostStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String readIdleTimeStr = element.getAttribute("readIdleTime");
        if (readIdleTimeStr != null && !"".equals(readIdleTimeStr.trim())) {
            bean.addPropertyValue("readIdleTime", readIdleTimeStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String writeIdleTimeStr = element.getAttribute("writeIdleTime");
        if (writeIdleTimeStr != null && !"".equals(writeIdleTimeStr.trim())) {
            bean.addPropertyValue("writeIdleTime", writeIdleTimeStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java

        String maxChunkContentSizeStr = element.getAttribute("maxChunkContentSize");
        if (maxChunkContentSizeStr != null && !"".equals(maxChunkContentSizeStr.trim())) {
            bean.addPropertyValue("maxChunkContentSize", maxChunkContentSizeStr);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/DefaultSTSTokenCacher.java`
#### Snippet
```java
                && WSS4JConstants.WSSE_NS.equals(token.getNamespaceURI())) {
                String text = XMLUtils.getElementText(token);
                if (text != null && !"".equals(text)) {
                    try {
                        MessageDigest digest = MessageDigest.getInstance("SHA-256");
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
                    String roleClassifier =
                        (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER);
                    if (roleClassifier != null && !"".equals(roleClassifier)) {
                        String roleClassifierType =
                            (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER_TYPE);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
                        String roleClassifierType =
                            (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER_TYPE);
                        if (roleClassifierType == null || "".equals(roleClassifierType)) {
                            roleClassifierType = "prefix";
                        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultWSS4JSecurityContextCreator.java`
#### Snippet
```java
            String roleClassifier =
                (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER);
            if (roleClassifier != null && !"".equals(roleClassifier)) {
                String roleClassifierType =
                    (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER_TYPE);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/DefaultWSS4JSecurityContextCreator.java`
#### Snippet
```java
                String roleClassifierType =
                    (String)msg.getContextualProperty(SecurityConstants.SUBJECT_ROLE_CLASSIFIER_TYPE);
                if (roleClassifierType == null || "".equals(roleClassifierType)) {
                    roleClassifierType = "prefix";
                }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
        String certConstraints =
            (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.SUBJECT_CERT_CONSTRAINTS, msg);
        if (certConstraints != null && !"".equals(certConstraints)) {
            String certConstraintsSeparator =
                (String)SecurityUtils.getSecurityPropertyValue(SecurityConstants.CERT_CONSTRAINTS_SEPARATOR, msg);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JInInterceptor.java`
#### Snippet
```java
            action = checkSymmetricBinding(aim, action, message, data);
            Collection<AssertionInfo> ais = aim.get(SP12Constants.TRANSPORT_BINDING);
            if ("".equals(action) || (ais != null && !ais.isEmpty())) {
                action = checkDefaultBinding(action, message, data);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
                id = rst.getAttributeNS(null, "ID");
            }
            if (id == null || "".equals(id)) {
                id = this.getIDFromSTR(rst);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            }
        }
        if ((id == null || "".equals(id)) && rar != null) {
            id = this.getIDFromSTR(rar);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            id = this.getIDFromSTR(rar);
        }
        if ((id == null || "".equals(id)) && rur != null) {
            id = this.getIDFromSTR(rur);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
            id = this.getIDFromSTR(rur);
        }
        if ((id == null || "".equals(id)) && rst != null) {
            id = rst.getAttributeNS(WSS4JConstants.WSU_NS, "Id");
            if (id == null || "".equals(id)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/AbstractSTSClient.java`
#### Snippet
```java
        if ((id == null || "".equals(id)) && rst != null) {
            id = rst.getAttributeNS(WSS4JConstants.WSU_NS, "Id");
            if (id == null || "".equals(id)) {
                QName elName = DOMUtils.getElementQName(rst);
                if (elName.equals(new QName(WSS4JConstants.SAML2_NS, "EncryptedAssertion"))) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
                String claimTypeOptional = claimType.getAttributeNS(null, "Optional");

                if (("".equals(claimTypeOptional) || !Boolean.parseBoolean(claimTypeOptional))
                    && !findClaimInAssertion(assertion, URI.create(claimTypeUri))) {
                    return false;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
                @Override
                public Key getSecretKey(String algorithmURI) throws XMLSecurityException {
                    if (secToken.getSecret() != null && algorithmURI != null && !"".equals(algorithmURI)) {
                        return KeyUtils.prepareSecretKey(algorithmURI, secToken.getSecret());
                    }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
                @Override
                public Key getSecretKey(String algorithmURI) throws XMLSecurityException {
                    if (secret != null && algorithmURI != null && !"".equals(algorithmURI)) {
                        return KeyUtils.prepareSecretKey(algorithmURI, secret);
                    }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractStaxBindingHandler.java`
#### Snippet
```java
                    if (secret != null) {
                        String jceAlg = JCEMapper.getJCEKeyAlgorithmFromURI(algorithmURI);
                        if (jceAlg == null || "".equals(jceAlg)) {
                            jceAlg = "HmacSHA1";
                        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SpnegoContextTokenInInterceptor.java`
#### Snippet
```java

            String context = requestEl.getAttributeNS(null, "Context");
            if (context != null && !"".equals(context)) {
                writer.writeAttribute("Context", context);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/SecureConversationOutInterceptor.java`
#### Snippet
```java
                String tokenType = tok.getTokenType();
                tok.setTokenType(tokenType);
                if (tokenType == null || "".equals(tokenType)) {
                    tok.setTokenType(WSS4JConstants.WSC_SCT);
                }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
        String encrUser = (String)SecurityUtils.getSecurityPropertyValue(key, message);

        if (crypto != null && (encrUser == null || "".equals(encrUser))) {
            try {
                encrUser = crypto.getDefaultX509Identifier();
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            }
        }
        if (encrUser == null || "".equals(encrUser)) {
            unassertPolicy(token, "A " + (sign ? "signature" : "encryption") + " username needs to be declared.");
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                        || WSS4JConstants.SAML_NS.equals(tokenType);
                    String id = token.getId();
                    if (id == null || "".equals(id)) {
                        if (saml1) {
                            id = token.getToken().getAttributeNS(null, "AssertionID");
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/AbstractWSDLBasedEndpointFactory.java`
#### Snippet
```java
        }

        if (publishedEndpointUrl != null && !"".equals(publishedEndpointUrl)) {
            ei.setProperty("publishedEndpointUrl", publishedEndpointUrl);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/UriBuilderImpl.java`
#### Snippet
```java
    @Override
    public UriBuilder host(String theHost) throws IllegalArgumentException {
        if ("".equals(theHost)) {
            throw new IllegalArgumentException("Host cannot be empty");
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java

    public void setVerb(String name) {
        if (!"".equals(name)) {
            httpMethodName = name;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java

    public void setName(String name) {
        if (!"".equals(name)) {
            methodName = name;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java

    public void setPath(String path) {
        if (!"".equals(path)) {
            pathValue = path;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java

    public void setConsumes(String types) {
        if (!"".equals(types)) {
            consumesTypes = types;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/UserOperation.java`
#### Snippet
```java

    public void setProduces(String types) {
        if (!"".equals(types)) {
            producesTypes = types;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java

        public static Literal create(String uriChunk) {
            if (uriChunk == null || "".equals(uriChunk)) {
                throw new IllegalArgumentException("uriChunk is empty");
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
         */
        public static UriChunk createUriChunk(String uriChunk) {
            if (uriChunk == null || "".equals(uriChunk)) {
                throw new IllegalArgumentException("uriChunk is empty");
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/URITemplate.java`
#### Snippet
```java
        public static Variable create(String uriChunk) {
            Variable newVariable = new Variable();
            if (uriChunk == null || "".equals(uriChunk)) {
                return null;
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java

    public static void addPropertyToForm(MultivaluedMap<String, String> map, String name, Object value) {
        if (!"".equals(name)) {
            map.add(name, value.toString());
        } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
                    continue;
                }
                if (!"".equals(baseName)) {
                    propertyName = baseName + '.' + propertyName;
                }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        }

        if (Number.class.isAssignableFrom(pClass) && "".equals(value)) {
            //pass empty string to boxed number type will result in 404
            return null;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractJAXBProvider.java`
#### Snippet
```java
        String name = getLocalName(localName, cls.getSimpleName(), plural);
        String namespace = getNamespace(ns);
        if ("".equals(namespace)) {
            namespace = getPackageNamespace(cls);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/AnnotationHandlerChainBuilder.java`
#### Snippet
```java

        public void validate() {
            if (null == ann.file() || "".equals(ann.file())) {
                throw new WebServiceException(BUNDLE.getString("ANNOTATION_WITHOUT_URL_EXC"));
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/spring/RMManagerBeanDefinitionParser.java`
#### Snippet
```java

        String bus = element.getAttribute("bus");
        if (bus == null || "".equals(bus)) {
            addBusWiringAttribute(bean, BusWiringType.PROPERTY);
        } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
                    action = (String) message.get(SoapBindingConstants.SOAP_ACTION);
                }
                if (action == null || "".equals(action)) {
                    MessageInfo msgInfo =
                        ContextUtils.isRequestor(message)
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
                                           boolean decoded) {
    //CHECKSTYLE:ON
        if ("".equals(parameterName)) {
            return InjectionUtils.handleBean(paramType, paramAnns, values, ParameterType.PATH, m, decoded);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        }

        if ("".equals(key)) {
            return InjectionUtils.handleBean(pClass, paramAnns, params, ParameterType.FORM, m, false);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
            }

            if ("".equals(key)) {
                return InjectionUtils.handleBean(pClass, paramAnns, params, ParameterType.MATRIX, m, false);
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        MultivaluedMap<String, String> queryMap = new UriInfoImpl(m, null).getQueryParameters(decode);

        if ("".equals(queryName)) {
            return InjectionUtils.handleBean(paramType, paramAnns, queryMap, ParameterType.QUERY, m, false);
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/AbstractSAMLTokenProvider.java`
#### Snippet
```java

        // If alias not defined, get the default of the SignatureCrypto
        if ((alias == null || "".equals(alias)) && (signatureCrypto != null)) {
            alias = signatureCrypto.getDefaultX509Identifier();
            if (LOG.isLoggable(Level.FINE)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/jwt/JWTTokenProvider.java`
#### Snippet
```java

            // If alias not defined, get the default of the SignatureCrypto
            if ((alias == null || "".equals(alias)) && (signatureCrypto != null)) {
                alias = signatureCrypto.getDefaultX509Identifier();
                if (LOG.isLoggable(Level.FINE)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/DefaultSubjectRoleParser.java`
#### Snippet
```java
    public Set<Principal> parseRolesFromSubject(Principal principal, Subject subject) {
        if (subject != null) {
            if (roleClassifier != null && !"".equals(roleClassifier)) {
                RolePrefixSecurityContextImpl securityContext =
                    new RolePrefixSecurityContextImpl(subject, roleClassifier, roleClassifierType);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/jwt/JWTTokenValidator.java`
#### Snippet
```java

        String token = ((Element)validateTarget.getToken()).getTextContent();
        if (token == null || "".equals(token)) {
            return response;
        }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
        try {
            dialectAttr = claimsType.getDialect();
            if (dialectAttr != null && !"".equals(dialectAttr)) {
                requestedClaims.setDialect(new URI(dialectAttr));
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/request/RequestParser.java`
#### Snippet
```java
        try {
            dialectAttr = claimsElement.getAttributeNS(null, "Dialect");
            if (dialectAttr != null && !"".equals(dialectAttr)) {
                requestedClaims.setDialect(new URI(dialectAttr));
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;

                    } else if (!"".equals(bindingStyle) && "".equals(previousOpStyle)
                               && bindingStyle.equalsIgnoreCase(style)
                               || bindingStyle.equalsIgnoreCase(previousOpStyle)
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;

                    } else if (!"".equals(bindingStyle) && "".equals(previousOpStyle)
                               && bindingStyle.equalsIgnoreCase(style)
                               || bindingStyle.equalsIgnoreCase(previousOpStyle)
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
            validator.walk();
            String validationComplaints = validator.getComplaints();
            if (!"".equals(validationComplaints)) {
                if (isValidate()) {
                    LOG.warning(validationComplaints);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoaderHelper.java`
#### Snippet
```java
        String nestedDir = path.substring(path.lastIndexOf("!/") + 2);

        return !"".equals(nestedDir) && nestedDir.endsWith("/");
    }
}
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `integration/jca/src/main/java/org/apache/cxf/jca/servant/EJBEndpoint.java`
#### Snippet
```java

    private static boolean isNotNull(String value) {
        return value != null && !"".equals(value.trim());
    }

```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java

    public static jakarta.jws.soap.SOAPBinding.Use getSoapUse(String soapUse) {
        if ("".equals(soapUse)) {
            return null;
        } else if ("ENCODED".equalsIgnoreCase(soapUse)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java

    public static jakarta.jws.soap.SOAPBinding.Style getSoapStyle(String soapStyle) {
        if ("".equals(soapStyle)) {
            return null;
        } else if ("RPC".equalsIgnoreCase(soapStyle)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                    }

                    if ("".equals(bindingStyle) && "".equals(previousOpStyle) || "".equals(bindingStyle)
                        && previousOpStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;

                    } else if (!"".equals(bindingStyle) && "".equals(previousOpStyle)
                               && bindingStyle.equalsIgnoreCase(style)
                               || bindingStyle.equalsIgnoreCase(previousOpStyle)
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;

                    } else if (!"".equals(bindingStyle) && "".equals(previousOpStyle)
                               && bindingStyle.equalsIgnoreCase(style)
                               || bindingStyle.equalsIgnoreCase(previousOpStyle)
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                               && bindingStyle.equalsIgnoreCase(style)) {
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
        for (int i = 0; i < xmlReader.getNamespaceCount(); i++) {
            String nsuri = xmlReader.getNamespaceURI(i);
            if ("".equals(nsuri)) {
                bodyNsMap.remove("");
            } else {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
        String ns = xmlReader.getNamespaceURI();
        String lcname = xmlReader.getLocalName();
        if (ns == null || "".equals(ns)) {
            throw new SoapFault(new Message("NO_NAMESPACE", LOG, lcname),
                                Soap11.getInstance().getVersionMismatch());
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/management/src/main/java/org/apache/cxf/management/jmx/InstrumentationManagerImpl.java`
#### Snippet
```java
        ObjectName persisted = original;
        if (!(persistentBusId == null
              || "".equals(persistentBusId)
              || persistentBusId.startsWith("${"))) {
            String originalStr = original.toString();
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementWriter.java`
#### Snippet
```java
                String ns2 = writer.getNamespaceContext().getNamespaceURI(hint);
                // if the hint is "" (the default) and the context does
                if (ns2 == null && !"".equals(hint)) {
                    pfx = hint;
                } else if (ns.equals(ns2)) {
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotatedTypeInfo.java`
#### Snippet
```java

        String explicitNamespace = annotationReader.getNamespace(pd.getReadMethod());
        boolean mustQualify = null != explicitNamespace && !"".equals(explicitNamespace);

        String name = pd.getName();
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/NamespaceHelper.java`
#### Snippet
```java
        // it is OK to have both namespace URI and prefix be empty.
        if (prefix == null) {
            if ("".equals(namespaceURI)) {
                return "";
            }
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WsdlOption.java`
#### Snippet
```java
    public List<String> generateCommandLine(File outputDirFile, URI basedir, URI wsdlURI, boolean debug) {
        List<String> options = new ArrayList<>();
        if (wsdlVersion != null && !"".equals(wsdlVersion)) {
            options.add("-wv");
            options.add(wsdlVersion);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/AbstractClient.java`
#### Snippet
```java
                                               + "with matrix and query parameters only");
        }
        if (!"".equals(paramName)) {
            if (pValues != null && pValues.length > 0) {
                for (Object pValue : pValues) {
```

## RuleId[ruleID=UnnecessaryBoxing]
### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
    public void serverStarted(Server server) {
        Object o = getProperty(server, "ws-discovery-disable");
        if (o == Boolean.TRUE || Boolean.valueOf((String)o)) {
            return;
        }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/SamlRequestComponentBuilder.java`
#### Snippet
```java
        authzQuery.setVersion(SAMLVersion.VERSION_20);
        authzQuery.setIssueInstant(Instant.now());
        authzQuery.setInputContextOnly(Boolean.valueOf(inputContextOnly));
        authzQuery.setReturnContext(Boolean.valueOf(returnContext));

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/SamlRequestComponentBuilder.java`
#### Snippet
```java
        authzQuery.setIssueInstant(Instant.now());
        authzQuery.setInputContextOnly(Boolean.valueOf(inputContextOnly));
        authzQuery.setReturnContext(Boolean.valueOf(returnContext));

        if (issuerValue != null) {
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
                .getPropertyOrNull("org.apache.cxf.transports.http_jetty.DontClosePort");
        }
        return !Boolean.valueOf(s);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java

            if (getMaxIdleTime() > 0) {
                result.setIdleTimeout(Long.valueOf(getMaxIdleTime()));
            }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
    static {
        DEFAULT_USE_REFLECTION =
            Boolean.valueOf(SystemPropertyAction.getProperty(HTTPURL_CONNECTION_METHOD_REFLECTION, "false"));
        SET_REASON_PHRASE = 
            Boolean.valueOf(SystemPropertyAction.getProperty(SET_REASON_PHRASE_NOT_NULL, "false"));
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
            Boolean.valueOf(SystemPropertyAction.getProperty(HTTPURL_CONNECTION_METHOD_REFLECTION, "false"));
        SET_REASON_PHRASE = 
            Boolean.valueOf(SystemPropertyAction.getProperty(SET_REASON_PHRASE_NOT_NULL, "false"));
    }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletController.java`
#### Snippet
```java
        String hideServiceList = servletConfig.getInitParameter("hide-service-list-page");
        if (!StringUtils.isEmpty(hideServiceList)) {
            this.isHideServiceList = Boolean.valueOf(hideServiceList);
        }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletController.java`
#### Snippet
```java
        String authServiceListPage = servletConfig.getInitParameter("service-list-page-authenticate");
        if (!StringUtils.isEmpty(authServiceListPage)) {
            this.isAuthServiceListPage = Boolean.valueOf(authServiceListPage);
        }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/ServletController.java`
#### Snippet
```java
        String isDisableAddressUpdates = servletConfig.getInitParameter("disable-address-updates");
        if (!StringUtils.isEmpty(isDisableAddressUpdates)) {
            this.disableAddressUpdates = Boolean.valueOf(isDisableAddressUpdates);
        }
        String isForcedBaseAddress = servletConfig.getInitParameter("base-address");
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/CXFNonSpringServlet.java`
#### Snippet
```java

    protected void registerServletContextResolver(ServletConfig sc) {
        if (Boolean.valueOf(sc.getInitParameter(IGNORE_SERVLET_CONTEXT_RESOLVER))) {
            return;
        }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java
        String showAllContexts = servletConfig.getInitParameter("service-list-all-contexts");
        if (!StringUtils.isEmpty(showAllContexts)) {
            this.showForeignContexts = Boolean.valueOf(showAllContexts);
        }
    }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/servicelist/ServiceListGeneratorServlet.java`
#### Snippet
```java
                        HttpServletResponse response) throws ServletException, IOException {
        final Object isAuthServiceList = request.getAttribute(ServletController.AUTH_SERVICE_LIST);
        if (isAuthServiceList != null && Boolean.valueOf(isAuthServiceList.toString())) {
            String authServiceListRealm = (String)request.getAttribute(ServletController.AUTH_SERVICE_LIST_REALM);
            ServiceListJAASAuthenticator authenticator = new ServiceListJAASAuthenticator();
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        staticWelcomeFile = servletConfig.getInitParameter(STATIC_WELCOME_FILE_PARAMETER);
        redirectList = parseListSequence(servletConfig.getInitParameter(REDIRECTS_PARAMETER));
        redirectQueryCheck = Boolean.valueOf(servletConfig.getInitParameter(REDIRECT_QUERY_CHECK_PARAMETER));
        dispatcherServletName = servletConfig.getInitParameter(REDIRECT_SERVLET_NAME_PARAMETER);
        dispatcherServletPath = servletConfig.getInitParameter(REDIRECT_SERVLET_PATH_PARAMETER);
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
        dispatcherServletPath = servletConfig.getInitParameter(REDIRECT_SERVLET_PATH_PARAMETER);
        redirectAttributes = parseMapSequence(servletConfig.getInitParameter(REDIRECT_ATTRIBUTES_PARAMETER));
        useXForwardedHeaders = Boolean.valueOf(servletConfig.getInitParameter(USE_X_FORWARDED_HEADERS_PARAMETER));
    }

```

### UnnecessaryBoxing
Redundant boxing, `Double.parseDouble()` call can be used instead
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
            DecimalMin min = (DecimalMin) annos.get(DecimalMin.class.getName());
            if (min.inclusive()) {
                schema.setMinimum(BigDecimal.valueOf(Double.valueOf(min.value())));
            } else {
                schema.setExclusiveMinimum(!min.inclusive());
```

### UnnecessaryBoxing
Redundant boxing, `Double.parseDouble()` call can be used instead
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/JaxRs2Extension.java`
#### Snippet
```java
            DecimalMax max = (DecimalMax) annos.get(DecimalMax.class.getName());
            if (max.inclusive()) {
                schema.setMaximum(BigDecimal.valueOf(Double.valueOf(max.value())));
            } else {
                schema.setExclusiveMaximum(!max.inclusive());
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
                .getPropertyOrNull("org.apache.cxf.transports.http_undertow.DontClosePort");
        }
        return !Boolean.valueOf(s);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/rs/security/cors/src/main/java/org/apache/cxf/rs/security/cors/CrossOriginResourceSharingFilter.java`
#### Snippet
```java
                return null;
            }
            return Integer.valueOf(ma);
        }
        return maxAge;
```

### UnnecessaryBoxing
Unnecessary boxing
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
                int i = cwClass.getField("COMPUTE_MAXS").getInt(null);
                i |= cwClass.getField("COMPUTE_FRAMES").getInt(null);
                newCw = cons.newInstance(Integer.valueOf(i));
            } catch (Throwable e1) {
                // ignore
```

### UnnecessaryBoxing
Unnecessary boxing
in `core/src/main/java/org/apache/cxf/common/logging/JDKBugHacks.java`
#### Snippet
```java
                            //something already set it, move on
                            method = clazz.getDeclaredMethod("requestLatency", Long.TYPE);
                            method.invoke(null, Long.valueOf(36000000));
                        }
                    }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
            } else {
                try {
                    final boolean followUrls = Boolean.valueOf(SystemPropertyAction
                        .getProperty(ATTACHMENT_XOP_FOLLOW_URLS_PROPERTY, "false"));
                    if (followUrls) {
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/Service.java`
#### Snippet
```java
                rootURL = args[++x];
            } else if ("-jmxEnable".equals(args[x])) {
                jmxEnable = Boolean.valueOf(args[++x]);
            }
        }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/coloc/src/main/java/org/apache/cxf/binding/coloc/WebFaultInInterceptor.java`
#### Snippet
```java
        Exception ex = message.getContent(Exception.class);
        if (ex != null) {
            message.put(Message.RESPONSE_CODE, Integer.valueOf(500));
        }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/trust/STSUtils.java`
#### Snippet
```java
                // Only use WS-MEX here if the pre-configured STSClient has no location/wsdllocation
                boolean useEPRWSAAddrAsMEXLocation =
                    !Boolean.valueOf((String)SecurityUtils.getSecurityPropertyValue(
                        SecurityConstants.DISABLE_STS_CLIENT_WSMEX_CALL_USING_EPR_ADDRESS, message));

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
        String bspCompliant = (String)msg.getContextualProperty(SecurityConstants.IS_BSP_COMPLIANT);
        if (bspCompliant != null) {
            securityProperties.setDisableBSPEnforcement(!Boolean.valueOf(bspCompliant));
        }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AbstractWSS4JStaxInterceptor.java`
#### Snippet
```java
                                                           msg);
        if (validateSAMLSubjectConf != null) {
            securityProperties.setValidateSamlSubjectConfirmation(Boolean.valueOf(validateSAMLSubjectConf));
        }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
    private void read(Message message) throws JMSException {
        setJMSCorrelationID(message.getJMSCorrelationID());
        setJMSDeliveryMode(Integer.valueOf(message.getJMSDeliveryMode()));
        setJMSExpiration(Long.valueOf(message.getJMSExpiration()));
        setJMSMessageID(message.getJMSMessageID());
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
        setJMSCorrelationID(message.getJMSCorrelationID());
        setJMSDeliveryMode(Integer.valueOf(message.getJMSDeliveryMode()));
        setJMSExpiration(Long.valueOf(message.getJMSExpiration()));
        setJMSMessageID(message.getJMSMessageID());
        setJMSPriority(Integer.valueOf(message.getJMSPriority()));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
        setJMSExpiration(Long.valueOf(message.getJMSExpiration()));
        setJMSMessageID(message.getJMSMessageID());
        setJMSPriority(Integer.valueOf(message.getJMSPriority()));
        setJMSRedelivered(Boolean.valueOf(message.getJMSRedelivered()));
        setJMSTimeStamp(Long.valueOf(message.getJMSTimestamp()));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
        setJMSMessageID(message.getJMSMessageID());
        setJMSPriority(Integer.valueOf(message.getJMSPriority()));
        setJMSRedelivered(Boolean.valueOf(message.getJMSRedelivered()));
        setJMSTimeStamp(Long.valueOf(message.getJMSTimestamp()));
        setJMSType(message.getJMSType());
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageHeadersType.java`
#### Snippet
```java
        setJMSPriority(Integer.valueOf(message.getJMSPriority()));
        setJMSRedelivered(Boolean.valueOf(message.getJMSRedelivered()));
        setJMSTimeStamp(Long.valueOf(message.getJMSTimestamp()));
        setJMSType(message.getJMSType());
        setSOAPJMSTargetService(message.getStringProperty(JMSSpecConstants.TARGETSERVICE_FIELD));
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpoint.java`
#### Snippet
```java
    }
    public void setSessionTransacted(String sessionTransacted) {
        this.sessionTransacted = Boolean.valueOf(sessionTransacted);
    }
    public String getConduitIdSelectorPrefix() {
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpoint.java`
#### Snippet
```java

    public void setUseConduitIdSelector(String useConduitIdSelectorSt) {
        this.useConduitIdSelector = Boolean.valueOf(useConduitIdSelectorSt);
    }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/rs/sse/src/main/java/org/apache/cxf/jaxrs/sse/client/SseEventSourceImpl.java`
#### Snippet
```java
                return (Boolean)value;
            } else if (value != null) {
                return Boolean.valueOf(value.toString());
            }
        }
```

### UnnecessaryBoxing
Redundant boxing, `Double.parseDouble()` call can be used instead
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/doc/JavaDocProvider.java`
#### Snippet
```java
    }
    public void setJavaDocsBuiltByVersion(String version) {
        javaDocsBuiltByVersion = Double.valueOf(version);
    }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
        String param = servletConfig.getInitParameter(STATIC_SUB_RESOLUTION_PARAM);
        if (param != null) {
            return Boolean.valueOf(param.trim());
        }
        return false;
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
            Element paramEl = paramEls.get(i);
            Parameter p = new Parameter(paramEl.getAttribute("type"), i, paramEl.getAttribute("name"));
            p.setEncoded(Boolean.valueOf(paramEl.getAttribute("encoded")));
            p.setDefaultValue(paramEl.getAttribute("defaultValue"));
            String pClass = paramEl.getAttribute("class");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/PrimitiveTextProvider.java`
#### Snippet
```java
        if (type == Character.class) {
            char character = string.charAt(0);
            return type.cast(Character.valueOf(character));
        }
        return InjectionUtils.handleParameter(
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
        Object ignoreWritersProp = exchange.get(JAXRSUtils.IGNORE_MESSAGE_WRITERS);
        boolean ignoreWriters =
            ignoreWritersProp != null && Boolean.valueOf(ignoreWritersProp.toString());
        if (ignoreWriters) {
            writeResponseToStream(message.getContent(OutputStream.class), entity);
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/rs/extensions/json-basic/src/main/java/org/apache/cxf/jaxrs/json/basic/JsonMapObjectReaderWriter.java`
#### Snippet
```java

    private boolean isEscapedChar(char c) {
        return ESCAPED_CHARS.contains(Character.valueOf(c));
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
            LOG.log(Level.FINE,
                    "Shutting down RMManager with {0} remaining endpoints.",
                    new Object[] {Integer.valueOf(reliableEndpoints.size())});
            for (RMEndpoint rme : reliableEndpoints.values()) {
                rme.shutdown();
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/policy/AddressingAssertionBuilder.java`
#### Snippet
```java
        Attr attribute = PolicyConstants.findOptionalAttribute(elem);
        if (attribute != null) {
            optional = Boolean.valueOf(attribute.getValue());
        }
        if (MetadataConstants.ADDRESSING_ASSERTION_QNAME.equals(qn)
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
    public <T> DataWriter<T> createWriter(Class<T> c) {

        Integer mtomThresholdInt = Integer.valueOf(getMtomThreshold());
        if (c == XMLStreamWriter.class) {
            DataWriterImpl<XMLStreamWriter> r
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/DynamicRegistrationService.java`
#### Snippet
```java
            response.setClientSecret(client.getClientSecret());
            // TODO: consider making Client secret time limited
            response.setClientSecretExpiresAt(Long.valueOf(0));
        }
        response.setClientIdIssuedAt(client.getRegisteredAt());
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            (String)props.get(STSConstants.TOKEN_RENEWING_ALLOW_AFTER_EXPIRY);

        if (isAllowRenewal == null || !Boolean.valueOf(isAllowRenewal)) {
            LOG.log(Level.WARNING, "The token is not allowed to be renewed");
            throw new STSException("The token is not allowed to be renewed", STSException.REQUEST_FAILED);
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
        if (tokenToRenew.getState() == STATE.EXPIRED) {
            if (!allowRenewalAfterExpiry || isAllowRenewalAfterExpiry == null
                || !Boolean.valueOf(isAllowRenewalAfterExpiry)) {
                LOG.log(Level.WARNING, "Renewal after expiry is not allowed");
                throw new STSException(
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java

    static {
        WSDL_INDENT_MAP.put(Definition.class, Integer.valueOf(DEFAULT_INDENT_LEVEL));
        WSDL_INDENT_MAP.put(Binding.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(BindingFault.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
    static {
        WSDL_INDENT_MAP.put(Definition.class, Integer.valueOf(DEFAULT_INDENT_LEVEL));
        WSDL_INDENT_MAP.put(Binding.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(BindingFault.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingInput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(Definition.class, Integer.valueOf(DEFAULT_INDENT_LEVEL));
        WSDL_INDENT_MAP.put(Binding.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(BindingFault.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingInput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOutput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(Binding.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(BindingFault.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingInput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOutput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOperation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(BindingFault.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingInput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOutput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOperation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Message.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(BindingInput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOutput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOperation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Message.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Operation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(BindingOutput.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(BindingOperation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Message.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Operation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Port.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(BindingOperation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Message.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Operation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Port.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Service.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(Message.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Operation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Port.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Service.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Types.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(Operation.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Port.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Service.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Types.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
        WSDL_INDENT_MAP.put(Port.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 3));
        WSDL_INDENT_MAP.put(Service.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
        WSDL_INDENT_MAP.put(Types.class, Integer.valueOf(DEFAULT_INDENT_LEVEL * 2));
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/DefaultServiceConfiguration.java`
#### Snippet
```java
        // set to 0.
        if (mpi.getTypeClass() != null && !mpi.getTypeClass().isPrimitive()) {
            return Long.valueOf(0);
        }
        return null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `testutils/src/main/java/org/apache/cxf/ordered_param_holder/OrderedParamHolderImpl.java`
#### Snippet
```java

    public void orderedParamHolder(Holder<ComplexStruct> part3, Holder<Integer> part2, Holder<String> part1) {
        part2.value = Integer.valueOf(part2.value.intValue() + 1);
        part1.value = "return " + part1.value;
        part3.value.elem1 = "return " + part3.value.elem1;
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            // This means that we can directly write out the chars in the normal
            // range 0-127 even when using UTF-8
            obj = Character.valueOf((char)(byteValue.byteValue() < 0
                                         ? byteValue.byteValue() + UNSIGNED_MAX
                                         : byteValue.byteValue()));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
        case TCKind._tk_wchar:
            // A wide char is mapped to a string, we need it as a character
            obj = Character.valueOf(data.charAt(0));
            break;
        case TCKind._tk_octet:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
        switch (primitive.getTypeCode().kind().value()) {
        case TCKind._tk_boolean:
            primitive.setValue(Boolean.valueOf(a.extract_boolean()));
            break;
        case TCKind._tk_char:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_char:
            primitive.setValue(Character.valueOf(a.extract_char()));
            break;
        case TCKind._tk_wchar:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_wchar:
            primitive.setValue(Character.valueOf(a.extract_wchar()));
            break;
        case TCKind._tk_octet:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_octet:
            primitive.setValue(Byte.valueOf(a.extract_octet()));
            break;
        case TCKind._tk_short:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_short:
            primitive.setValue(Short.valueOf(a.extract_short()));
            break;
        case TCKind._tk_ushort:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_ushort:
            primitive.setValue(Integer.valueOf(a.extract_ushort()));
            break;
        case TCKind._tk_long:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_long:
            primitive.setValue(Integer.valueOf(a.extract_long()));
            break;
        case TCKind._tk_ulong:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_longlong:
            primitive.setValue(Long.valueOf(a.extract_longlong()));
            break;
        case TCKind._tk_ulonglong:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_float:
            primitive.setValue(Float.valueOf(a.extract_float()));
            break;
        case TCKind._tk_double:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaAnyHelper.java`
#### Snippet
```java
            break;
        case TCKind._tk_double:
            primitive.setValue(Double.valueOf(a.extract_double()));
            break;
        case TCKind._tk_string:
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Double readDouble() throws CorbaBindingException {
        try {
            return Double.valueOf(stream.read_double());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read double");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Short readShort() throws CorbaBindingException {
        try {
            return Short.valueOf(stream.read_short());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read short");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Character readWChar() throws CorbaBindingException {
        try {
            return Character.valueOf(stream.read_wchar());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read wide character");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Character readChar() throws CorbaBindingException {
        try {
            return Character.valueOf(stream.read_char());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read character");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Float readFloat() throws CorbaBindingException {
        try {
            return Float.valueOf(stream.read_float());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read float");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Long readLongLong() throws CorbaBindingException {
        try {
            return Long.valueOf(stream.read_longlong());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read long long");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
    public Byte readOctet() throws CorbaBindingException {
        try {
            return Byte.valueOf(stream.read_octet());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read octet");
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java

    public Integer readLong() throws CorbaBindingException {
        Integer result = Integer.valueOf(stream.read_long());
        try {
            return result;
```

### UnnecessaryBoxing
Unnecessary boxing
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/handlers/ObjectMethodInvocationHandler.java`
#### Snippet
```java
            InvocationHandler rhsHandler = Proxy.getInvocationHandler(rhs);
            if (rhsHandler instanceof CXFInvocationHandler) {
                ret = Boolean.valueOf(getData().getTarget() == ((CXFInvocationHandler)rhsHandler).getData()
                    .getTarget());
            }
```

### UnnecessaryBoxing
Unnecessary boxing
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/AbstractManagedConnectionFactoryImpl.java`
#### Snippet
```java

        LOG.log(Level.FINE, "MATCHING_CONNECTIONS",
            new Object[] {Integer.valueOf(aMCSet.size()), crInfo, subject});

        for (Iterator iterator = aMCSet.iterator(); iterator.hasNext();) {
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/saaj/SAAJInInterceptor.java`
#### Snippet
```java

            shead.setActor(act);
            shead.setMustUnderstand(Boolean.valueOf(mu) || "1".equals(mu));
            Header oldHdr = message.getHeader(
                    new QName(elem.getNamespaceURI(), elem.getLocalName()));
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/ReadHeadersInterceptor.java`
#### Snippet
```java
                                shead.setActor(act);
                            }
                            shead.setMustUnderstand(Boolean.valueOf(mu) || "1".equals(mu));
                            // mark header as inbound header.(for distinguishing between the direction to
                            // avoid piggybacking of headers from request->server->response.
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/mtom/MTOMAssertionBuilder.java`
#### Snippet
```java
        Attr attribute = PolicyConstants.findOptionalAttribute(elem);
        if (attribute != null) {
            optional = Boolean.valueOf(attribute.getValue());
        }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/builder/jaxb/JaxbAssertionBuilder.java`
#### Snippet
```java
                           Constants.Q_ELEM_OPTIONAL_ATTR.getNamespaceURI(),
                           Constants.Q_ELEM_OPTIONAL_ATTR.getLocalPart());
        if (Boolean.valueOf(value)) {
            optional = true;
        }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `rt/management/src/main/java/org/apache/cxf/management/jmx/InstrumentationManagerImpl.java`
#### Snippet
```java
            return (Boolean)v;
        }
        return v != null ? Boolean.valueOf(v.toString()) : dflt;
    }
}
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ByteType.java`
#### Snippet
```java
        int intValue = ((Number)object).intValue();
        byte byteValue = (byte)(intValue & 0xff);
        writer.writeValueAsByte(Byte.valueOf(byteValue));
    }
}
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BooleanType.java`
#### Snippet
```java
            return null;
        }
        return Boolean.valueOf(reader.getValueAsBoolean());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DoubleType.java`
#### Snippet
```java
            writer.writeValueAsDouble((Double)object);
        } else {
            writer.writeValueAsDouble(Double.valueOf(((Number)object).doubleValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DoubleType.java`
#### Snippet
```java
    @Override
    public Object readObject(MessageReader reader, Context context) {
        return Double.valueOf(reader.getValueAsDouble());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/FloatType.java`
#### Snippet
```java
    @Override
    public Object readObject(MessageReader reader, Context context) {
        return Float.valueOf(reader.getValueAsFloat());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/FloatType.java`
#### Snippet
```java
            writer.writeValueAsFloat((Float)object);
        } else {
            writer.writeValueAsFloat(Float.valueOf(((Number)object).floatValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/IntType.java`
#### Snippet
```java
    @Override
    public Object readObject(MessageReader reader, Context context) {
        return Integer.valueOf(reader.getValueAsInt());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/IntType.java`
#### Snippet
```java
            writer.writeValueAsInt((Integer)object);
        } else {
            writer.writeValueAsInt(Integer.valueOf(((Number)object).intValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ShortType.java`
#### Snippet
```java
            writer.writeValueAsShort((Short)object);
        } else {
            writer.writeValueAsShort(Short.valueOf(((Number)object).shortValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CharacterType.java`
#### Snippet
```java
    @Override
    public Object readObject(MessageReader reader, Context context) {
        return Character.valueOf(reader.getValueAsCharacter());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CharacterAsStringType.java`
#### Snippet
```java
        throws DatabindingException {
        Character charObject = (Character) object;
        intType.writeObject(Integer.valueOf(charObject.charValue()), writer, context);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CharacterAsStringType.java`
#### Snippet
```java
    public Object readObject(MessageReader reader, Context context) throws DatabindingException {
        Integer readInteger = (Integer)intType.readObject(reader, context);
        return Character.valueOf((char)readInteger.intValue());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/LongType.java`
#### Snippet
```java
    @Override
    public Object readObject(MessageReader reader, Context context) {
        return Long.valueOf(reader.getValueAsLong());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/LongType.java`
#### Snippet
```java
            writer.writeValueAsLong((Long)object);
        } else {
            writer.writeValueAsLong(Long.valueOf(((Number)object).longValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                int[] objects = (int[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Integer.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Long.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                long[] objects = (long[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Long.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Short.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                short[] objects = (short[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Short.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Double.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                double[] objects = (double[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Double.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Float.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                float[] objects = (float[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Float.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Byte.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                byte[] objects = (byte[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Byte.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Boolean.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                boolean[] objects = (boolean[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Boolean.valueOf(objects[i]), writer, context, type, name, ns);
                }
            } else if (Character.TYPE.equals(arrayType)) {
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
                char[] objects = (char[])values;
                for (i = 0, n = objects.length; i < n; i++) {
                    writeValue(Character.valueOf(objects[i]), writer, context, type, name, ns);
                }
            }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
                return MessageFormat.format("{0} at {1} line {2} column {3}.",
                                            new Object[] {exception.getMessage(), path,
                                                          Integer.valueOf(exception.getLineNumber()),
                                                          Integer.valueOf(exception.getColumnNumber())});
            }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
                                            new Object[] {exception.getMessage(), path,
                                                          Integer.valueOf(exception.getLineNumber()),
                                                          Integer.valueOf(exception.getColumnNumber())});
            }

```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
        if (prop == null && method.getReturnType().isPrimitive()) {
            if (method.getReturnType() == int.class) {
                prop = Integer.valueOf(0);
            } else if (method.getReturnType() == boolean.class) {
                prop = Boolean.FALSE;
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
                prop = Boolean.FALSE;
            } else if (method.getReturnType() == long.class) {
                prop = Long.valueOf(0);
            } else if (method.getReturnType() == double.class) {
                prop = Double.valueOf(0);
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
                prop = Long.valueOf(0);
            } else if (method.getReturnType() == double.class) {
                prop = Double.valueOf(0);
            } else if (method.getReturnType() == short.class) {
                prop = Short.valueOf((short)0);
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
                prop = Double.valueOf(0);
            } else if (method.getReturnType() == short.class) {
                prop = Short.valueOf((short)0);
            } else if (method.getReturnType() == byte.class) {
                prop = Byte.valueOf((byte)0);
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
                prop = Short.valueOf((short)0);
            } else if (method.getReturnType() == byte.class) {
                prop = Byte.valueOf((byte)0);
            } else if (method.getReturnType() == char.class) {
                prop = Character.valueOf((char)0);
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java
                prop = Byte.valueOf((byte)0);
            } else if (method.getReturnType() == char.class) {
                prop = Character.valueOf((char)0);
            }
        }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/InterfaceInvocationHandler.java`
#### Snippet
```java

    protected Integer proxyHashCode(Object proxy) {
        return Integer.valueOf(System.identityHashCode(proxy));
    }

```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java

                    try {
                        if (Integer.valueOf(version) >= 9) {
                            java9 = true;
                        }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/UrlEncodingParamConverter.java`
#### Snippet
```java
            for (String ch : chars) {
                // this may need to be tuned though this should cover URI reserved chars
                encodeClientParametersList.add(Character.valueOf(ch.charAt(0)));
            }
        }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
    private static final Map<String, Integer> ATTR_TYPES = new HashMap<>(32);
    static {
        ATTR_TYPES.put("CDATA", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("cdata", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("ID", Integer.valueOf(Attribute.ID_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
    static {
        ATTR_TYPES.put("CDATA", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("cdata", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("ID", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("id", Integer.valueOf(Attribute.ID_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("CDATA", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("cdata", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("ID", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("id", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("IDREF", Integer.valueOf(Attribute.IDREF_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("cdata", Integer.valueOf(Attribute.CDATA_TYPE));
        ATTR_TYPES.put("ID", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("id", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("IDREF", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("idref", Integer.valueOf(Attribute.IDREF_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("ID", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("id", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("IDREF", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("idref", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("IDREFS", Integer.valueOf(Attribute.IDREFS_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("id", Integer.valueOf(Attribute.ID_TYPE));
        ATTR_TYPES.put("IDREF", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("idref", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("IDREFS", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("idrefs", Integer.valueOf(Attribute.IDREFS_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("IDREF", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("idref", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("IDREFS", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("idrefs", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("ENTITY", Integer.valueOf(Attribute.ENTITY_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("idref", Integer.valueOf(Attribute.IDREF_TYPE));
        ATTR_TYPES.put("IDREFS", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("idrefs", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("ENTITY", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("entity", Integer.valueOf(Attribute.ENTITY_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("IDREFS", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("idrefs", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("ENTITY", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("entity", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("ENTITIES", Integer.valueOf(Attribute.ENTITIES_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("idrefs", Integer.valueOf(Attribute.IDREFS_TYPE));
        ATTR_TYPES.put("ENTITY", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("entity", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("ENTITIES", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("entities", Integer.valueOf(Attribute.ENTITIES_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("ENTITY", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("entity", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("ENTITIES", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("entities", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("NMTOKEN", Integer.valueOf(Attribute.NMTOKEN_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("entity", Integer.valueOf(Attribute.ENTITY_TYPE));
        ATTR_TYPES.put("ENTITIES", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("entities", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("NMTOKEN", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("nmtoken", Integer.valueOf(Attribute.NMTOKEN_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("ENTITIES", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("entities", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("NMTOKEN", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("nmtoken", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("NMTOKENS", Integer.valueOf(Attribute.NMTOKENS_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("entities", Integer.valueOf(Attribute.ENTITIES_TYPE));
        ATTR_TYPES.put("NMTOKEN", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("nmtoken", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("NMTOKENS", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("nmtokens", Integer.valueOf(Attribute.NMTOKENS_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("NMTOKEN", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("nmtoken", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("NMTOKENS", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("nmtokens", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("NOTATION", Integer.valueOf(Attribute.NOTATION_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("nmtoken", Integer.valueOf(Attribute.NMTOKEN_TYPE));
        ATTR_TYPES.put("NMTOKENS", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("nmtokens", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("NOTATION", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("notation", Integer.valueOf(Attribute.NOTATION_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("NMTOKENS", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("nmtokens", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("NOTATION", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("notation", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("ENUMERATED", Integer.valueOf(Attribute.ENUMERATED_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("nmtokens", Integer.valueOf(Attribute.NMTOKENS_TYPE));
        ATTR_TYPES.put("NOTATION", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("notation", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("ENUMERATED", Integer.valueOf(Attribute.ENUMERATED_TYPE));
        ATTR_TYPES.put("enumerated", Integer.valueOf(Attribute.ENUMERATED_TYPE));
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("NOTATION", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("notation", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("ENUMERATED", Integer.valueOf(Attribute.ENUMERATED_TYPE));
        ATTR_TYPES.put("enumerated", Integer.valueOf(Attribute.ENUMERATED_TYPE));
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
        ATTR_TYPES.put("notation", Integer.valueOf(Attribute.NOTATION_TYPE));
        ATTR_TYPES.put("ENUMERATED", Integer.valueOf(Attribute.ENUMERATED_TYPE));
        ATTR_TYPES.put("enumerated", Integer.valueOf(Attribute.ENUMERATED_TYPE));
    }

```

## RuleId[ruleID=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `registry`
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
        r.lock();
        try {
            synchronized (registry) {
                AbstractHTTPDestination d = registry.getDestinationForPath(endpointInfo.getAddress());
                if (d == null) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `endpoint`
in `core/src/main/java/org/apache/cxf/endpoint/AbstractConduitSelector.java`
#### Snippet
```java
        throws IOException {
        Conduit c;
        synchronized (endpoint) {
            if (!conduits.isEmpty()) {
                c = findCompatibleConduit(message);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `m.matcher`
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
                if (matchers != null) {
                    for (MatcherHolder m : matchers) {
                        synchronized (m.matcher) {
                            m.matcher.reset(bn);
                            if (m.matcher.matches()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `producerSession`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPullPoint.java`
#### Snippet
```java
            StringWriter writer = new StringWriter();
            jaxbContext.createMarshaller().marshal(notify, writer);
            synchronized (producerSession) {
                Message message = producerSession.createTextMessage(writer.toString());
                producer.send(message);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `consumerSession`
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/jms/JmsPullPoint.java`
#### Snippet
```java
            for (int i = 0; i < max; i++) {
                final Message msg;
                synchronized (consumerSession) {
                    msg = consumer.receiveNoWait();
                }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `classContexts`
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java

    private AegisContext getClassContext(Type reflectionType) {
        synchronized (classContexts) {
            AegisContext context = classContexts.get(reflectionType);
            if (context == null) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `sessionLock`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
        public void setSSLSession(SSLSession sslsession) {
            session = sslsession;
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `sessionLock`
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            }
            connect(true);
            synchronized (sessionLock) {
                if (session == null) {
                    try {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `sessionLock`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            }
            connect(true);
            synchronized (sessionLock) {
                if (session == null) {
                    try {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `sessionLock`
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
        public void setSSLSession(SSLSession sslsession) {
            session = sslsession;
            synchronized (sessionLock) {
                sslState = sslsession.getLocalPrincipal();
                sslURL = url;
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `tokens`
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/tokenstore/MemoryTokenStore.java`
#### Snippet
```java
    protected void processTokenExpiry() {
        Instant current = Instant.now();
        synchronized (tokens) {
            for (Map.Entry<String, CacheEntry> entry : tokens.entrySet()) {
                if (entry.getValue().getExpiry().isBefore(current)) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `bus`
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java

        Object property;
        synchronized (bus) {
            property = bus.getProperty(prop);
            if (property == null && create) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/MessageCountingCallback.java`
#### Snippet
```java
    public boolean waitComplete(long timeout) {
        long start = System.currentTimeMillis();
        synchronized (lock) {
            while (countOutstanding > 0) {
                long remain = 0;
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/MessageCountingCallback.java`
#### Snippet
```java
    @Override
    public void messageAcknowledged(String seqId, long msgNum) {
        synchronized (lock) {
            countOutstanding--;
            if (countOutstanding == 0) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `lock`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/MessageCountingCallback.java`
#### Snippet
```java
    @Override
    public void messageAccepted(String seqId, long msgNum) {
        synchronized (lock) {
            countOutstanding++;
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `deliveringMessageNumbers`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java

    void removeDeliveringMessageNumber(long mn) {
        synchronized (deliveringMessageNumbers) {
            deliveringMessageNumbers.remove(mn);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `deliveringMessageNumbers`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/DestinationSequence.java`
#### Snippet
```java
    }
    void addDeliveringMessageNumber(long mn) {
        synchronized (deliveringMessageNumbers) {
            deliveringMessageNumbers.add(mn);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `reliableEndpoints`
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMManager.java`
#### Snippet
```java
        RMEndpoint rme = createReliableEndpoint(endpoint);
        rme.initialise(getConfiguration(), conduit, null, null, null);
        synchronized (reliableEndpoints) {
            reliableEndpoints.put(endpoint, rme);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `tokenSupplier`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/client/CodeAuthSupplier.java`
#### Snippet
```java
                                   String fullHeader) {
        if (code != null) {
            synchronized (tokenSupplier) {
                if (tokenSupplier.getClientAccessToken().getTokenKey() == null) {
                    WebClient wc = tokenSupplier.createAccessTokenServiceClient();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `refreshTokenLock`
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/provider/AbstractOAuthDataProvider.java`
#### Snippet
```java
    }
    protected RefreshToken updateExistingRefreshToken(RefreshToken rt, ServerAccessToken at) {
        synchronized (refreshTokenLock) {
            return updateRefreshToken(rt, at);
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `servantsCache`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java

    private void deregisterServants(Bus aBus) {
        synchronized (servantsCache) {
            for (Server servant : servantsCache) {
                //REVISIT: seems using server.stop() doesn't release resource properly.
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `servantsCache`
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
                Server servant = ejbEndpoint.publish();

                synchronized (servantsCache) {
                    if (servant != null) {
                        servantsCache.add(servant);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `listeners`
in `integration/jca/src/main/java/org/apache/cxf/jca/outbound/ManagedConnectionImpl.java`
#### Snippet
```java

    private void sendEvent(final ConnectionEvent coEvent) {
        synchronized (listeners) {
            Iterator<ConnectionEventListener> iterator = listeners.iterator();
            while (iterator.hasNext()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `writer`
in `rt/features/logging/src/main/java/org/apache/cxf/ext/logging/event/PrintWriterEventSender.java`
#### Snippet
```java
    @Override
    public void send(LogEvent event) {
        synchronized (writer) {
            writer.print(Instant.now().toString() + " - PrintWriterEventSender - ");
            writer.print(LogMessageFormatter.format(event));
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimeType.java`
#### Snippet
```java
        Calendar c = Calendar.getInstance();
        c.setTime((Time)object);
        synchronized (format) {
            writer.writeValue(format.format(c));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimeType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                Calendar c = (Calendar)format.parseObject(value.trim());
                return new Time(c.getTimeInMillis()); //NOPMD
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateType.java`
#### Snippet
```java
        Calendar c = Calendar.getInstance();
        c.setTime((Date)object);
        synchronized (format) {
            writer.writeValue(format.format(c));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                return ((Calendar)format.parseObject(value.trim())).getTime();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CalendarType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                return format.parseObject(value.trim());
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimezoneLessDateType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                return ((Calendar)format.parseObject(value.trim())).getTime();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/CalendarType.java`
#### Snippet
```java
    @Override
    public void writeObject(Object object, MessageWriter writer, Context context) {
        synchronized (format) {
            writer.writeValue(format.format(object));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimezoneLessDateType.java`
#### Snippet
```java
            c.setTime((Date) object);
        }
        synchronized (format) {
            writer.writeValue(format.format(c));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateTimeType.java`
#### Snippet
```java
        Calendar c = Calendar.getInstance();
        c.setTime((Date)object);
        synchronized (format) {
            writer.writeValue(format.format(c));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/DateTimeType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                return ((Calendar)format.parseObject(value.trim())).getTime();
            }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimestampType.java`
#### Snippet
```java

        try {
            synchronized (format) {
                Calendar c = (Calendar)format.parseObject(value.trim());
                return new Timestamp(c.getTimeInMillis()); //NOPMD
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `format`
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/TimestampType.java`
#### Snippet
```java
        Calendar c = Calendar.getInstance();
        c.setTime((Timestamp)object);
        synchronized (format) {
            writer.writeValue(format.format(c));
        }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `entity`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                        setException(future.cause());
                    }
                    synchronized (entity) {
                        //ensure entity is write in main thread
                    }
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `entity`
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    };
                    
                    synchronized (entity) {
                        Channel syncChannel = getChannel();
                        ChannelFuture channelFuture = syncChannel.writeAndFlush(entity);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `baseClients`
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/spec/ClientImpl.java`
#### Snippet
```java
    public void close() {
        if (!closed) {
            synchronized (baseClients) {
                for (WebClient wc : baseClients) {
                    wc.close();
```

## RuleId[ruleID=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'issuer != null' covered by subsequent condition 'SELF_ISSUED_ISSUER.equals(...)'
in `rt/rs/security/sso/oidc/src/main/java/org/apache/cxf/rs/security/oidc/rp/OidcClaimsValidator.java`
#### Snippet
```java
        }
        if (supportSelfIssuedProvider && issuerId == null
            && issuer != null && SELF_ISSUED_ISSUER.equals(issuer)) {
            validateSelfIssuedProvider(claims, clientId, validateClaimsAlways);
        } else {
```

### ConditionCoveredByFurtherCondition
Condition 'pre == null' covered by subsequent condition '!"xmlns".equals(...)'
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java

    protected boolean isAttribute(String pre, String name) {
        return !"xmlns".equals(name) && (pre == null || !"xmlns".equals(pre))
            && !"abstract".equals(name) && !"lazy-init".equals(name) && !"id".equals(name);
    }
```

### ConditionCoveredByFurtherCondition
Condition 'token instanceof SecureConversationToken' covered by subsequent condition 'token instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxTransportBindingHandler.java`
#### Snippet
```java
            AlgorithmSuiteType algType = tbinding.getAlgorithmSuite().getAlgorithmSuiteType();
            properties.setSignatureDigestAlgorithm(algType.getDigest());
        } else if (token instanceof SecureConversationToken
            || token instanceof SecurityContextToken || token instanceof SpnegoContextToken) {
            SecurityToken securityToken = getSecurityToken();
```

### ConditionCoveredByFurtherCondition
Condition 'token instanceof SecureConversationToken' covered by subsequent condition 'token instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/TransportBindingHandler.java`
#### Snippet
```java

        if (token instanceof IssuedToken
            || token instanceof SecureConversationToken
            || token instanceof SecurityContextToken
            || token instanceof KerberosToken
```

### ConditionCoveredByFurtherCondition
Condition 'encryptionToken instanceof SecureConversationToken' covered by subsequent condition 'encryptionToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxSymmetricBindingHandler.java`
#### Snippet
```java
                    tokenId = WSS4JUtils.parseAndStoreStreamingSecurityToken(securityToken, message);
                }
            } else if (encryptionToken instanceof SecureConversationToken
                || encryptionToken instanceof SecurityContextToken
                || encryptionToken instanceof SpnegoContextToken) {
```

### ConditionCoveredByFurtherCondition
Condition 'sigToken instanceof SecureConversationToken' covered by subsequent condition 'sigToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/StaxSymmetricBindingHandler.java`
#### Snippet
```java
                        sigTokId = WSS4JUtils.parseAndStoreStreamingSecurityToken(securityToken, message);
                    }
                } else if (sigToken instanceof SecureConversationToken
                    || sigToken instanceof SecurityContextToken
                    || sigToken instanceof SpnegoContextToken) {
```

### ConditionCoveredByFurtherCondition
Condition 'token instanceof SecureConversationToken' covered by subsequent condition 'token instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
                );
            } else if (token instanceof IssuedToken
                    || token instanceof SecureConversationToken
                    || token instanceof SecurityContextToken
                    || token instanceof KerberosToken
```

### ConditionCoveredByFurtherCondition
Condition 'sigToken instanceof SecureConversationToken' covered by subsequent condition 'sigToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            SecurityToken sigTok = null;
            if (sigToken != null) {
                if (sigToken instanceof SecureConversationToken
                    || sigToken instanceof SecurityContextToken
                    || sigToken instanceof IssuedToken
```

### ConditionCoveredByFurtherCondition
Condition 'encrToken instanceof SecureConversationToken' covered by subsequent condition 'encrToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                encrTokId = encrTok.getWsuId();
                if (encrTokId == null
                    && (encrToken instanceof SecureConversationToken
                        || encrToken instanceof SecurityContextToken)) {
                    encr.setEncKeyIdDirectId(true);
```

### ConditionCoveredByFurtherCondition
Condition 'policyToken instanceof SecureConversationToken' covered by subsequent condition 'policyToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            dkSign.setStrElem(tokenRef.getElement());
        } else {
            if ((!attached && !isRequestor()) || policyToken instanceof SecureConversationToken
                || policyToken instanceof SecurityContextToken) {
                dkSign.setTokenIdDirectId(true);
```

### ConditionCoveredByFurtherCondition
Condition 'encryptionToken instanceof SecureConversationToken' covered by subsequent condition 'encryptionToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                if (encryptionToken instanceof IssuedToken
                    || encryptionToken instanceof KerberosToken
                    || encryptionToken instanceof SecureConversationToken
                    || encryptionToken instanceof SecurityContextToken
                    || encryptionToken instanceof SpnegoContextToken) {
```

### ConditionCoveredByFurtherCondition
Condition 'policyToken instanceof SecureConversationToken' covered by subsequent condition 'policyToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
            sigTokId = tok.getWsuId();
            if (sigTokId == null) {
                if (policyToken instanceof SecureConversationToken
                    || policyToken instanceof SecurityContextToken) {
                    sig.setKeyIdentifierType(WSConstants.CUSTOM_SYMM_SIGNING_DIRECT);
```

### ConditionCoveredByFurtherCondition
Condition 'encrToken instanceof SecureConversationToken' covered by subsequent condition 'encrToken instanceof SecurityContextToken'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/SymmetricBindingHandler.java`
#### Snippet
```java
                    String id = encrTok.getWsuId();
                    if (id == null
                        && (encrToken instanceof SecureConversationToken
                            || encrToken instanceof SecurityContextToken)) {
                        dkEncr.setTokenIdDirectId(true);
```

### ConditionCoveredByFurtherCondition
Condition 'nap instanceof PolicyContainingPrimitiveAssertion' covered by subsequent condition 'nap instanceof org.apache.neethi.builders.PrimitiveAssertion'
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/policy/AddressingAssertionBuilder.java`
#### Snippet
```java
                }
            }.build(elem, factory);
            if (!(nap instanceof PolicyContainingPrimitiveAssertion
                    || nap instanceof org.apache.neethi.builders.PrimitiveAssertion)) {
                // this happens when neethi fails to recognize the specified addressing policy element
```

### ConditionCoveredByFurtherCondition
Condition 'elementType != null' covered by subsequent condition '"boolean".equalsIgnoreCase(...)'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBDataBinding.java`
#### Snippet
```java
            Field elField = getElField(partName, valueClass);
            if (getMethod == null
                && elementType != null
                && "boolean".equalsIgnoreCase(elementType)
                && (elField == null
```

### ConditionCoveredByFurtherCondition
Condition 'decodedAssertionType == null' covered by subsequent condition '!Constants.CLIENT_AUTH_JWT_BEARER.equals(...)'
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/grants/jwt/JwtBearerAuthHandler.java`
#### Snippet
```java
        String assertionType = formData.getFirst(Constants.CLIENT_AUTH_ASSERTION_TYPE);
        String decodedAssertionType = assertionType != null ? HttpUtils.urlDecode(assertionType) : null;
        if (decodedAssertionType == null || !Constants.CLIENT_AUTH_JWT_BEARER.equals(decodedAssertionType)) {
            throw ExceptionUtils.toNotAuthorizedException(null, null);
        }
```

### ConditionCoveredByFurtherCondition
Condition '!OAuthConstants.TOKEN_ENDPOINT_AUTH_NONE.equals(...)' covered by subsequent condition 'OAuthConstants.TOKEN_ENDPOINT_AUTH_BASIC.equals(...) \|\| OAuthConstants.TOKEN_ENDPOINT_AUTH_POST.equa ...'
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/DynamicRegistrationService.java`
#### Snippet
```java
        }

        return !OAuthConstants.TOKEN_ENDPOINT_AUTH_NONE.equals(tokenEndpointAuthMethod)
            && (OAuthConstants.TOKEN_ENDPOINT_AUTH_BASIC.equals(tokenEndpointAuthMethod)
                || OAuthConstants.TOKEN_ENDPOINT_AUTH_POST.equals(tokenEndpointAuthMethod));
```

### ConditionCoveredByFurtherCondition
Condition 'decodedAssertionType == null' covered by subsequent condition '!Constants.CLIENT_AUTH_SAML2_BEARER.equals(...)'
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/auth/saml/Saml2BearerAuthHandler.java`
#### Snippet
```java
        String assertionType = formData.getFirst(Constants.CLIENT_AUTH_ASSERTION_TYPE);
        String decodedAssertionType = assertionType != null ? HttpUtils.urlDecode(assertionType) : null;
        if (decodedAssertionType == null || !Constants.CLIENT_AUTH_SAML2_BEARER.equals(decodedAssertionType)) {
            throw ExceptionUtils.toNotAuthorizedException(null, null);
        }
```

### ConditionCoveredByFurtherCondition
Condition 'head != null' covered by subsequent condition 'HttpHeaderHelper.CHUNKED.equalsIgnoreCase(...)'
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                }
                head = httpResponse.headers().get(HttpHeaderHelper.TRANSFER_ENCODING);
                boolean isChunked = head != null &&  HttpHeaderHelper.CHUNKED.equalsIgnoreCase(head);
                head = httpResponse.headers().get(HttpHeaderHelper.CONNECTION);
                boolean isEofTerminated = head != null &&  HttpHeaderHelper.CLOSE.equalsIgnoreCase(head);
```

### ConditionCoveredByFurtherCondition
Condition 'head != null' covered by subsequent condition 'HttpHeaderHelper.CLOSE.equalsIgnoreCase(...)'
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                boolean isChunked = head != null &&  HttpHeaderHelper.CHUNKED.equalsIgnoreCase(head);
                head = httpResponse.headers().get(HttpHeaderHelper.CONNECTION);
                boolean isEofTerminated = head != null &&  HttpHeaderHelper.CLOSE.equalsIgnoreCase(head);
                if (cli > 0) {
                    in = getInputStream();
```

## RuleId[ruleID=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/wsdl11/XMLWSDLExtensionLoader.java`
#### Snippet
```java

    public void registerExtensors(WSDLManager manager) {
        createExtensor(manager, javax.wsdl.BindingInput.class,
                       org.apache.cxf.bindings.xformat.XMLBindingMessageFormat.class);
        createExtensor(manager, javax.wsdl.BindingOutput.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/wsdl11/XMLWSDLExtensionLoader.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.BindingInput.class,
                       org.apache.cxf.bindings.xformat.XMLBindingMessageFormat.class);
        createExtensor(manager, javax.wsdl.BindingOutput.class,
                       org.apache.cxf.bindings.xformat.XMLBindingMessageFormat.class);
        createExtensor(manager, javax.wsdl.Binding.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/wsdl11/XMLWSDLExtensionLoader.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.BindingOutput.class,
                       org.apache.cxf.bindings.xformat.XMLBindingMessageFormat.class);
        createExtensor(manager, javax.wsdl.Binding.class,
                       org.apache.cxf.bindings.xformat.XMLFormatBinding.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/bindings/xml/src/main/java/org/apache/cxf/binding/xml/interceptor/XMLFaultOutInterceptor.java`
#### Snippet
```java

        Fault f = (Fault) message.getContent(Exception.class);
        message.put(org.apache.cxf.message.Message.RESPONSE_CODE, f.getStatusCode());
        NSStack nsStack = new NSStack();
        nsStack.push();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProviderFactory.java`
#### Snippet
```java
    public Crypto create(Message message) {
        Object crypto = SecurityUtils
            .getSecurityPropertyValue(org.apache.cxf.rt.security.SecurityConstants.SIGNATURE_CRYPTO, message);
        if (crypto instanceof Crypto) {
            try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-client/src/main/java/org/apache/cxf/xkms/crypto/provider/XkmsCryptoProviderFactory.java`
#### Snippet
```java
        Properties keystoreProps = CryptoProviderUtils
            .loadKeystoreProperties(message,
                                    org.apache.cxf.rt.security.SecurityConstants.SIGNATURE_PROPERTIES);
        try {
            Crypto defaultCrypto = CryptoFactory.getInstance(keystoreProps);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPDestination.java`
#### Snippet
```java
     * @param ei the endpoint info of the destination
     * @param serverEngineFactory the serverEngineFactory which could be used to create ServerEngine
     * @throws java.io.IOException
     */
    public JettyHTTPDestination(
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
            // Defaults
            if (audiences.isEmpty()) {
                if (msg.get(org.apache.cxf.message.Message.REQUEST_URL) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URL));
                } else if (msg.get(org.apache.cxf.message.Message.REQUEST_URI) != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
            if (audiences.isEmpty()) {
                if (msg.get(org.apache.cxf.message.Message.REQUEST_URL) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URL));
                } else if (msg.get(org.apache.cxf.message.Message.REQUEST_URI) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URI));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
                if (msg.get(org.apache.cxf.message.Message.REQUEST_URL) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URL));
                } else if (msg.get(org.apache.cxf.message.Message.REQUEST_URI) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URI));
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/utils/SAMLUtils.java`
#### Snippet
```java
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URL));
                } else if (msg.get(org.apache.cxf.message.Message.REQUEST_URI) != null) {
                    audiences.add((String)msg.get(org.apache.cxf.message.Message.REQUEST_URI));
                }

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.security.auth` is unnecessary, and can be replaced with an import
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/claims/SAMLSecurityContext.java`
#### Snippet
```java
    }

    public javax.security.auth.Subject getSubject() {
        return null;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/spring/JettyHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }

                elem = org.apache.cxf.helpers.DOMUtils.getNextElement(elem);
            }
        } catch (Exception e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wss4j.common.saml` is unnecessary and can be removed
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/xacml2/AbstractXACMLAuthorizingInterceptor.java`
#### Snippet
```java
    public AbstractXACMLAuthorizingInterceptor() {
        super(Phase.PRE_INVOKE);
        org.apache.wss4j.common.saml.OpenSAMLUtil.initSamlEngine();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.jetty.server` is unnecessary and can be removed
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
            connectionFactories.add(httpFactory);

            result = new org.eclipse.jetty.server.ServerConnector(server);

            if (tlsServerParameters != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPWSDLExtensionLoader.java`
#### Snippet
```java
        WSDLManager manager = b.getExtension(WSDLManager.class);

        createExtensor(b, manager, javax.wsdl.Port.class,
                       org.apache.cxf.transports.http.configuration.HTTPClientPolicy.class);
        createExtensor(b, manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPWSDLExtensionLoader.java`
#### Snippet
```java
        createExtensor(b, manager, javax.wsdl.Port.class,
                       org.apache.cxf.transports.http.configuration.HTTPClientPolicy.class);
        createExtensor(b, manager, javax.wsdl.Port.class,
                       org.apache.cxf.transports.http.configuration.HTTPServerPolicy.class);
        createExtensor(b, manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPWSDLExtensionLoader.java`
#### Snippet
```java
        createExtensor(b, manager, javax.wsdl.Port.class,
                       org.apache.cxf.transports.http.configuration.HTTPServerPolicy.class);
        createExtensor(b, manager, javax.wsdl.Port.class,
                       AddressType.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java
    private static Proxy chooseProxy(URI uri) {
        ProxySelector sel = java.security.AccessController
            .doPrivileged(new java.security.PrivilegedAction<ProxySelector>() {
                @Override
                public ProxySelector run() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java

    private static Proxy chooseProxy(URI uri) {
        ProxySelector sel = java.security.AccessController
            .doPrivileged(new java.security.PrivilegedAction<ProxySelector>() {
                @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HttpServletRequestSnapshot.java`
#### Snippet
```java
    private Enumeration<String> requestHeaderNames;
    private Map<String, Enumeration<String>> headersMap =
        new java.util.concurrent.ConcurrentHashMap<>();
    private String requestedSessionId;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
                connection.connect();
                try {
                    java.lang.reflect.Field f = ReflectionUtil.getDeclaredField(HttpURLConnection.class, "method");
                    ReflectionUtil.setAccessible(f).set(connection, "POST");
                    cout = connection.getOutputStream();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
        try {
            connection.setRequestMethod(httpRequestMethod);
        } catch (java.net.ProtocolException ex) {
            boolean b = MessageUtils.getContextualBoolean(message,
                                                          HTTPURL_CONNECTION_METHOD_REFLECTION,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
            if (b) {
                try {
                    java.lang.reflect.Field f = ReflectionUtil.getDeclaredField(HttpURLConnection.class, "method");
                    if (connection instanceof HttpsURLConnection) {
                        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/URLConnectionHTTPConduit.java`
#### Snippet
```java
                    if (connection instanceof HttpsURLConnection) {
                        try {
                            java.lang.reflect.Field f2 = ReflectionUtil.getDeclaredField(connection.getClass(),
                                                                                         "delegate");
                            Object c = ReflectionUtil.setAccessible(f2).get(connection);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
                    if (jettyFactory == null && addr != null && addr.startsWith("http")) {
                        String m =
                            new org.apache.cxf.common.i18n.Message("NO_HTTP_DESTINATION_FACTORY_FOUND",
                                                                   LOG).toString();
                        LOG.log(Level.SEVERE, m);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPTransportFactory.java`
#### Snippet
```java
                this.registry = newRegistry;
            } else {
                String m = new org.apache.cxf.common.i18n.Message("CANNOT_CHANGE_REGISTRY_ALREADY_IN_USE",
                                                                  LOG).toString();
                LOG.log(Level.SEVERE, m);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security.cert` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
            (String) request.getAttribute(SSL_CIPHER_SUITE_ATTRIBUTE);
        if (cipherSuite != null) {
            final java.security.cert.Certificate[] certs =
                (java.security.cert.Certificate[]) request.getAttribute(SSL_PEER_CERT_CHAIN_ATTRIBUTE);
            message.put(TLSSessionInfo.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security.cert` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/AbstractHTTPDestination.java`
#### Snippet
```java
        if (cipherSuite != null) {
            final java.security.cert.Certificate[] certs =
                (java.security.cert.Certificate[]) request.getAttribute(SSL_PEER_CERT_CHAIN_ATTRIBUTE);
            message.put(TLSSessionInfo.class,
                        new TLSSessionInfo(cipherSuite,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AllowAllHostnameVerifier.java`
#### Snippet
```java
 * Allow all hostnames. This is only suitable for use in testing, and NOT in production!
 */
class AllowAllHostnameVerifier implements javax.net.ssl.HostnameVerifier {

    private static final Logger LOG = LogUtils.getL7dLogger(AllowAllHostnameVerifier.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseClientAlias(java.lang.String[], java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseClientAlias(java.lang.String[], java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseClientAlias(java.lang.String[], java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /**
     * @see javax.net.ssl.X509KeyManager#chooseClientAlias(java.lang.String[], java.security.Principal[],
     *      java.net.Socket)
     */
    public String chooseClientAlias(String[] keyType, Principal[] issuers, Socket socket) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getCertificateChain(java.lang.String)
     */
    public X509Certificate[] getCertificateChain(String alias) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getCertificateChain(java.lang.String)
     */
    public X509Certificate[] getCertificateChain(String alias) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509ExtendedKeyManager#chooseEngineServerAlias(java.lang.String,
     *      java.security.Principal[], jakarta.net.ssl.SSLEngine)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509ExtendedKeyManager#chooseEngineServerAlias(java.lang.String,
     *      java.security.Principal[], jakarta.net.ssl.SSLEngine)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /**
     * @see javax.net.ssl.X509ExtendedKeyManager#chooseEngineServerAlias(java.lang.String,
     *      java.security.Principal[], jakarta.net.ssl.SSLEngine)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getServerAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getServerAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getServerAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getServerAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getServerAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getServerAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getClientAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getClientAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getClientAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getClientAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getClientAliases(java.lang.String, java.security.Principal[])
     */
    public String[] getClientAliases(String keyType, Principal[] issuers) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509ExtendedKeyManager#chooseEngineClientAlias(String[], Principal[], SSLEngine)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getPrivateKey(java.lang.String)
     */
    public PrivateKey getPrivateKey(String alias) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#getPrivateKey(java.lang.String)
     */
    public PrivateKey getPrivateKey(String alias) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseServerAlias(java.lang.String, java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseServerAlias(java.lang.String, java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /* ------------------------------------------------------------ */
    /**
     * @see javax.net.ssl.X509KeyManager#chooseServerAlias(java.lang.String, java.security.Principal[],
     *      java.net.Socket)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/AliasedX509ExtendedKeyManager.java`
#### Snippet
```java
    /**
     * @see javax.net.ssl.X509KeyManager#chooseServerAlias(java.lang.String, java.security.Principal[],
     *      java.net.Socket)
     */
    public String chooseServerAlias(String keyType, Principal[] issuers, Socket socket) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraintsJaxBUtils.java`
#### Snippet
```java
            return constraints.getRegularExpression();
        }
        return java.util.Collections.emptyList();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraintsJaxBUtils.java`
#### Snippet
```java
            return constraints.getRegularExpression();
        }
        return java.util.Collections.emptyList();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
     */
    public CertConstraints(
        final java.util.List<String> subjectConstraints,
        final Combinator subjectConstraintsCombinator,
        final java.util.List<String> issuerConstraints,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
        final java.util.List<String> subjectConstraints,
        final Combinator subjectConstraintsCombinator,
        final java.util.List<String> issuerConstraints,
        final Combinator issuerConstraintsCombinator
    ) throws java.util.regex.PatternSyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
        final java.util.List<String> issuerConstraints,
        final Combinator issuerConstraintsCombinator
    ) throws java.util.regex.PatternSyntaxException {
        this.subjectDNConstraints =
            new DNConstraints(subjectConstraints, subjectConstraintsCombinator);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.security.auth.x500` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
         */
        final boolean matches(
            final javax.security.auth.x500.X500Principal dn
        ) {
            boolean atLeastOnePatternMatches = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security.cert` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
     */
    public boolean matches(
        final java.security.cert.X509Certificate cert
    ) {
        return
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
         */
        DNConstraints(
            final java.util.List<String> patterns,
            final Combinator patternCombinator
        ) throws java.util.regex.PatternSyntaxException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/CertConstraints.java`
#### Snippet
```java
            final java.util.List<String> patterns,
            final Combinator patternCombinator
        ) throws java.util.regex.PatternSyntaxException {
            if (patterns == null) {
                combinator = Combinator.ALL;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java
     *
     * @param reader the data reader. The caller is responsible for closing the reader.
     * @throws java.io.IOException on error while reading from list
     */
    public PublicSuffixList parse(final Reader reader) throws IOException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/PublicSuffixListParser.java`
#### Snippet
```java
     *
     * @param reader the data reader. The caller is responsible for closing the reader.
     * @throws java.io.IOException on error while reading from list
     *
     * @since 4.5
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.net.ssl` is unnecessary and can be removed
in `rt/transports/http/src/main/java/org/apache/cxf/transport/https/httpclient/DefaultHostnameVerifier.java`
#### Snippet
```java

/**
 * Default {@link javax.net.ssl.HostnameVerifier} implementation.
 * Copied from httpclient.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/transports/http/src/main/java/org/apache/cxf/transport/servlet/AbstractHTTPServlet.java`
#### Snippet
```java
                is.close();
            } catch (IOException ex) {
                String message = new org.apache.cxf.common.i18n.Message("STATIC_RESOURCES_MAP_LOAD_FAILURE",
                                                                        BUNDLE).toString();
                LOG.warning(message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.maven.project` is unnecessary, and can be replaced with an import
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/ParseJavaDocMojo.java`
#### Snippet
```java
     * @component
     */
    private org.apache.maven.project.ProjectBuilder mavenProjectBuilder;

    
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/xml/XmlEncOutInterceptor.java`
#### Snippet
```java
        } else if (keyIdType.equals(RSSecurityUtils.X509_ISSUER_SERIAL)) {
            String issuer = remoteCert.getIssuerDN().getName();
            java.math.BigInteger serialNumber = remoteCert.getSerialNumber();
            DOMX509IssuerSerial domIssuerSerial =
                new DOMX509IssuerSerial(
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary, and can be replaced with an import
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
    public Crypto loadCrypto(String cryptoResource) throws IOException, WSSecurityException {
        URL url =
            org.apache.cxf.rt.security.utils.SecurityUtils.loadResource(cryptoResource);
        if (url != null) {
            return loadCryptoFromURL(url);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary, and can be replaced with an import
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
        throws IOException, WSSecurityException {
        Crypto crypto =
            (Crypto)org.apache.cxf.rt.security.utils.SecurityUtils.getSecurityPropertyValue(cryptoKey, message);
        if (crypto != null) {
            return crypto;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary, and can be replaced with an import
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
        }

        Object o = org.apache.cxf.rt.security.utils.SecurityUtils.getSecurityPropertyValue(propKey, message);
        if (o == null) {
            return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary, and can be replaced with an import
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/common/CryptoLoader.java`
#### Snippet
```java
        }

        URL url = org.apache.cxf.rt.security.utils.SecurityUtils.loadResource(message, propResourceName);

        if (url != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/EHCacheTokenReplayCache.java`
#### Snippet
```java
    public static final String CACHE_KEY = "cxf.samlp.replay.cache";

    private static final org.slf4j.Logger LOG =
            org.slf4j.LoggerFactory.getLogger(EHCacheReplayCache.class);
    private static final String DEFAULT_CONFIG_URL = "/cxf-samlp-ehcache.xml";
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/EHCacheTokenReplayCache.java`
#### Snippet
```java

    private static final org.slf4j.Logger LOG =
            org.slf4j.LoggerFactory.getLogger(EHCacheReplayCache.class);
    private static final String DEFAULT_CONFIG_URL = "/cxf-samlp-ehcache.xml";

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary and can be removed
in `rt/rs/security/xml/src/main/java/org/apache/cxf/rs/security/saml/AbstractSamlInHandler.java`
#### Snippet
```java
                boolean enableRevocation = false;
                String enableRevocationStr =
                    (String)org.apache.cxf.rt.security.utils.SecurityUtils.getSecurityPropertyValue(
                        SecurityConstants.ENABLE_REVOCATION, message);
                if (enableRevocationStr != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/LogoutService.java`
#### Snippet
```java

    protected void reportError(String code) {
        org.apache.cxf.common.i18n.Message errorMsg =
            new org.apache.cxf.common.i18n.Message(code, BUNDLE);
        LOG.warning(errorMsg.toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/LogoutService.java`
#### Snippet
```java
    protected void reportError(String code) {
        org.apache.cxf.common.i18n.Message errorMsg =
            new org.apache.cxf.common.i18n.Message(code, BUNDLE);
        LOG.warning(errorMsg.toString());
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/state/EHCacheSPStateManager.java`
#### Snippet
```java
    public static final String RESPONSE_CACHE_KEY = "cxf.samlp.response.state.cache";
    private static final String DEFAULT_CONFIG_URL = "/cxf-samlp-ehcache.xml";
    private static final org.slf4j.Logger LOG =
            org.slf4j.LoggerFactory.getLogger(EHCacheSPStateManager.class);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/state/EHCacheSPStateManager.java`
#### Snippet
```java
    private static final String DEFAULT_CONFIG_URL = "/cxf-samlp-ehcache.xml";
    private static final org.slf4j.Logger LOG =
            org.slf4j.LoggerFactory.getLogger(EHCacheSPStateManager.class);

    private final Cache<String, RequestState> requestCache;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
     * Validate the Subject (of an Authentication Statement).
     */
    private org.opensaml.saml.saml2.core.SubjectConfirmation validateAuthenticationSubject(
        org.opensaml.saml.saml2.core.Subject subject, String id, boolean postBinding
    ) throws WSSecurityException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
     */
    private org.opensaml.saml.saml2.core.SubjectConfirmation validateAuthenticationSubject(
        org.opensaml.saml.saml2.core.Subject subject, String id, boolean postBinding
    ) throws WSSecurityException {
        if (subject.getSubjectConfirmations() == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
        }

        org.opensaml.saml.saml2.core.SubjectConfirmation validSubjectConf = null;
        // We need to find a Bearer Subject Confirmation method
        for (org.opensaml.saml.saml2.core.SubjectConfirmation subjectConf
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
        org.opensaml.saml.saml2.core.SubjectConfirmation validSubjectConf = null;
        // We need to find a Bearer Subject Confirmation method
        for (org.opensaml.saml.saml2.core.SubjectConfirmation subjectConf
            : subject.getSubjectConfirmations()) {
            if (SAML2Constants.CONF_BEARER.equals(subjectConf.getMethod())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
     */
    private void validateSubjectConfirmation(
        org.opensaml.saml.saml2.core.SubjectConfirmationData subjectConfData, String id, boolean postBinding
    ) throws WSSecurityException {
        if (subjectConfData == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
                if (audienceRestriction.getAudiences() != null) {
                    boolean matchFound = false;
                    for (org.opensaml.saml.saml2.core.Audience audience : audienceRestriction.getAudiences()) {
                        if (appliesTo.equals(audience.getAudienceURI())) {
                            matchFound = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
     */
    public SSOValidatorResponse validateSamlResponse(
        org.opensaml.saml.saml2.core.Response samlResponse,
        boolean postBinding
    ) throws WSSecurityException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java

        // Validate Assertions
        org.opensaml.saml.saml2.core.Assertion validAssertion = null;
        Instant sessionNotOnOrAfter = null;
        for (org.opensaml.saml.saml2.core.Assertion assertion : samlResponse.getAssertions()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
        org.opensaml.saml.saml2.core.Assertion validAssertion = null;
        Instant sessionNotOnOrAfter = null;
        for (org.opensaml.saml.saml2.core.Assertion assertion : samlResponse.getAssertions()) {
            // Check the Issuer
            if (assertion.getIssuer() == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
            if (assertion.getAuthnStatements() != null
                && !assertion.getAuthnStatements().isEmpty()) {
                org.opensaml.saml.saml2.core.Subject subject = assertion.getSubject();
                org.opensaml.saml.saml2.core.SubjectConfirmation subjectConf =
                    validateAuthenticationSubject(subject, assertion.getID(), postBinding);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
                && !assertion.getAuthnStatements().isEmpty()) {
                org.opensaml.saml.saml2.core.Subject subject = assertion.getSubject();
                org.opensaml.saml.saml2.core.SubjectConfirmation subjectConf =
                    validateAuthenticationSubject(subject, assertion.getID(), postBinding);
                if (subjectConf != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java
     * Validate the Issuer (if it exists)
     */
    private void validateIssuer(org.opensaml.saml.saml2.core.Issuer issuer) throws WSSecurityException {
        if (issuer == null) {
            return;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLSSOResponseValidator.java`
#### Snippet
```java

    private void validateAudienceRestrictionCondition(
        org.opensaml.saml.saml2.core.Conditions conditions
    ) throws WSSecurityException {
        if (conditions == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractRequestAssertionConsumerHandler.java`
#### Snippet
```java

    protected void reportError(String code) {
        org.apache.cxf.common.i18n.Message errorMsg =
            new org.apache.cxf.common.i18n.Message(code, BUNDLE);
        LOG.warning(errorMsg.toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/AbstractRequestAssertionConsumerHandler.java`
#### Snippet
```java
    protected void reportError(String code) {
        org.apache.cxf.common.i18n.Message errorMsg =
            new org.apache.cxf.common.i18n.Message(code, BUNDLE);
        LOG.warning(errorMsg.toString());
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
     */
    public void validateSamlResponse(
        org.opensaml.saml.saml2.core.Response samlResponse,
        Crypto sigCrypto,
        CallbackHandler callbackHandler
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
        // Decrypt any encrypted Assertions and add them to the Response (note that this will break any
        // signature on the Response)
        for (org.opensaml.saml.saml2.core.EncryptedAssertion assertion : samlResponse.getEncryptedAssertions()) {

            Element decAssertion = decryptAssertion(assertion, sigCrypto, callbackHandler);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        // Validate Assertions
        for (org.opensaml.saml.saml2.core.Assertion assertion : samlResponse.getAssertions()) {
            SamlAssertionWrapper wrapper = new SamlAssertionWrapper(assertion);
            validateAssertion(wrapper, sigCrypto, callbackHandler, doc, samlResponse.isSigned());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
     */
    private void validateResponseSignature(
        org.opensaml.saml.saml2.core.Response samlResponse,
        Crypto sigCrypto,
        CallbackHandler callbackHandler
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
     */
    private void validateResponseSignature(
        org.opensaml.saml.saml1.core.Response samlResponse,
        Crypto sigCrypto,
        CallbackHandler callbackHandler
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

    private Element decryptAssertion(
        org.opensaml.saml.saml2.core.EncryptedAssertion assertion, Crypto sigCrypto, CallbackHandler callbackHandler
    ) throws WSSecurityException {
        EncryptedData encryptedData = assertion.getEncryptedData();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java
     */
    public void validateSamlResponse(
        org.opensaml.saml.saml1.core.Response samlResponse,
        Crypto sigCrypto,
        CallbackHandler callbackHandler
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/sso/saml/src/main/java/org/apache/cxf/rs/security/saml/sso/SAMLProtocolResponseValidator.java`
#### Snippet
```java

        // Validate Assertions
        for (org.opensaml.saml.saml1.core.Assertion assertion : samlResponse.getAssertions()) {
            SamlAssertionWrapper wrapper = new SamlAssertionWrapper(assertion);
            validateAssertion(
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.logging` is unnecessary and can be removed
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/LoadDistributorTargetSelector.java`
#### Snippet
```java

    @Override
    protected java.util.logging.Logger getLogger() {
        return LOG;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.metrics` is unnecessary and can be removed
in `rt/features/metrics/src/main/java/org/apache/cxf/metrics/MetricsFeature.java`
#### Snippet
```java
                try {
                    Class<?> cls = ClassLoaderUtils.loadClass("org.apache.cxf.metrics.codahale.CodahaleMetricsProvider",
                            org.apache.cxf.metrics.MetricsFeature.class);
                    Constructor<?> c = cls.getConstructor(Bus.class);
                    providers = new MetricsProvider[] {(MetricsProvider)c.newInstance(bus)};
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
    private InitialDirContext createInitialContext() throws NamingException {
        Hashtable<String, String> env = new Hashtable<>(5); //NOPMD
        env.put(javax.naming.Context.INITIAL_CONTEXT_FACTORY, "com.sun.jndi.ldap.LdapCtxFactory");
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
        env.put(javax.naming.Context.SECURITY_AUTHENTICATION, SECURITY_AUTHENTICATION);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
        Hashtable<String, String> env = new Hashtable<>(5); //NOPMD
        env.put(javax.naming.Context.INITIAL_CONTEXT_FACTORY, "com.sun.jndi.ldap.LdapCtxFactory");
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
        env.put(javax.naming.Context.SECURITY_AUTHENTICATION, SECURITY_AUTHENTICATION);
        env.put(javax.naming.Context.SECURITY_PRINCIPAL, bindDN);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
        env.put(javax.naming.Context.INITIAL_CONTEXT_FACTORY, "com.sun.jndi.ldap.LdapCtxFactory");
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
        env.put(javax.naming.Context.SECURITY_AUTHENTICATION, SECURITY_AUTHENTICATION);
        env.put(javax.naming.Context.SECURITY_PRINCIPAL, bindDN);
        env.put(javax.naming.Context.SECURITY_CREDENTIALS, bindPassword);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
        env.put(javax.naming.Context.PROVIDER_URL, ldapuri);
        env.put(javax.naming.Context.SECURITY_AUTHENTICATION, SECURITY_AUTHENTICATION);
        env.put(javax.naming.Context.SECURITY_PRINCIPAL, bindDN);
        env.put(javax.naming.Context.SECURITY_CREDENTIALS, bindPassword);
        return new InitialLdapContext(env, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `services/xkms/xkms-x509-repo-ldap/src/main/java/org/apache/cxf/xkms/x509/repo/ldap/LdapSearch.java`
#### Snippet
```java
        env.put(javax.naming.Context.SECURITY_AUTHENTICATION, SECURITY_AUTHENTICATION);
        env.put(javax.naming.Context.SECURITY_PRINCIPAL, bindDN);
        env.put(javax.naming.Context.SECURITY_CREDENTIALS, bindPassword);
        return new InitialLdapContext(env, null);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/features/clustering/src/main/java/org/apache/cxf/clustering/FailoverTargetSelector.java`
#### Snippet
```java
        boolean failover = false;
        while (curr != null) {
            failover = curr instanceof java.io.IOException;
            curr = curr.getCause();
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPDestination.java`
#### Snippet
```java
     * @param ei the endpoint info of the destination
     * @param serverEngineFactory the serverEngineFactory which could be used to create ServerEngine
     * @throws java.io.IOException
     */
    public UndertowHTTPDestination(Bus bus,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
        OPENAPI_TYPE_MAP.put("byte", byte.class);
        OPENAPI_TYPE_MAP.put("boolean", boolean.class);
        OPENAPI_TYPE_MAP.put("date", java.util.Date.class);
        OPENAPI_TYPE_MAP.put("dateTime", java.util.Date.class);
        OPENAPI_TYPE_MAP.put("password", String.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
        OPENAPI_TYPE_MAP.put("boolean", boolean.class);
        OPENAPI_TYPE_MAP.put("date", java.util.Date.class);
        OPENAPI_TYPE_MAP.put("dateTime", java.util.Date.class);
        OPENAPI_TYPE_MAP.put("password", String.class);
        OPENAPI_TYPE_MAP.put("binary", java.io.InputStream.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/parse/OpenApiParseUtils.java`
#### Snippet
```java
        OPENAPI_TYPE_MAP.put("dateTime", java.util.Date.class);
        OPENAPI_TYPE_MAP.put("password", String.class);
        OPENAPI_TYPE_MAP.put("binary", java.io.InputStream.class);
    }
    private OpenApiParseUtils() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport.http_undertow` is unnecessary and can be removed
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngine.java`
#### Snippet
```java
    private int maxIdleTime = 200000;

    private org.apache.cxf.transport.http_undertow.ThreadingParameters threadingParameters;

    private List<CXFUndertowHttpHandler> handlers;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/spring/UndertowHTTPServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                    bean.addPropertyValue("handlers", handlers);
                }
                elem = org.apache.cxf.helpers.DOMUtils.getNextElement(elem);
            }
        } catch (Exception e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/ClaimCollection.java`
#### Snippet
```java
 * This holds a collection of Claim Objects.
 */
public class ClaimCollection extends java.util.ArrayList<Claim> {

    private static final long serialVersionUID = -4543840943290756510L;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.claims` is unnecessary, and can be replaced with an import
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/interceptor/ClaimsAuthorizingInterceptor.java`
#### Snippet
```java
    protected boolean authorize(ClaimsSecurityContext sc, Method method) {
        List<ClaimBean> list = claims.get(method.getName());
        org.apache.cxf.rt.security.claims.ClaimCollection actualClaims = sc.getClaims();

        for (ClaimBean claimBean : list) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.bus` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBusFactory.java`
#### Snippet
```java
            if (context == null && userCfgFile == null && cfgFiles == null && sysCfgFileUrl == null
                && (r == null || !exists) && includeDefaults) {
                return new org.apache.cxf.bus.CXFBusFactory().createBus();
            }
            return finishCreatingBus(createApplicationContext(cfgFiles, includeDefaults));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.feature` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/bus/spring/NamespaceHandler.java`
#### Snippet
```java
                                     new BusDefinitionParser());
        registerBeanDefinitionParser("logging",
                                     new SimpleBeanDefinitionParser(org.apache.cxf.feature.LoggingFeature.class));
        registerBeanDefinitionParser("fastinfoset",
                                     new SimpleBeanDefinitionParser(FastInfosetFeature.class));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.logging` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/bus/managers/WorkQueueManagerImpl.java`
#### Snippet
```java
            }
        }
        for (java.util.logging.Handler h : LOG.getHandlers())  {
            h.flush();
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.phase` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptor.java`
#### Snippet
```java
 * interceptors that must be placed before and after the interceptor.
 *
 * @see org.apache.cxf.phase.PhaseInterceptorChain
 */
public interface PhaseInterceptor<T extends Message> extends Interceptor<T> {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java
    public UncheckedException(ResourceBundle bundle, String msg, Throwable t, Object ... params) {
        super(t);
        message = new org.apache.cxf.common.i18n.Message(msg,
                                                         bundle,
                                                         params);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java
    public UncheckedException(Logger log, String msg, Throwable t, Object ... params) {
        super(t);
        message = new org.apache.cxf.common.i18n.Message(msg,
                                                         log,
                                                         params);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java


public class UncheckedException extends java.lang.RuntimeException {

    private static final long serialVersionUID = 1L;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java
    }
    public UncheckedException(ResourceBundle bundle, String msg, Object ... params) {
        message = new org.apache.cxf.common.i18n.Message(msg,
                                                         bundle,
                                                         params);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/i18n/UncheckedException.java`
#### Snippet
```java

    public UncheckedException(Logger log, String msg, Object ... params) {
        message = new org.apache.cxf.common.i18n.Message(msg,
                                                         log,
                                                         params);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/i18n/Message.java`
#### Snippet
```java
    }

    private void writeObject(java.io.ObjectOutputStream out)
        throws IOException {
        out.writeUTF(toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/i18n/Message.java`
#### Snippet
```java
        out.writeUTF(toString());
    }
    private void readObject(java.io.ObjectInputStream in)
        throws IOException, ClassNotFoundException {
        code = in.readUTF();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.beans` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
        String namespace = xre == null ? "##default" : xre.namespace();
        if ("##default".equals(name)) {
            name = java.beans.Introspector.decapitalize(cls.getSimpleName());
        }
        if ("##default".equals(namespace) && cls.getPackage() != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.beans` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/SchemaCollectionContextProxy.java`
#### Snippet
```java
        String tns = xtype == null ? "##default" : xtype.namespace();
        if ("##default".equals(tn)) {
            tn = java.beans.Introspector.decapitalize(cls.getSimpleName());
        }
        if ("##default".equals(tns) || StringUtils.isEmpty(tns)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelper.java`
#### Snippet
```java
public interface ASMHelper {
    String getClassCode(Class<?> cl);
    String getClassCode(java.lang.reflect.Type type);
    ClassWriter createClassWriter();
    ASMType getType(String type);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/MessageDigestInputStream.java`
#### Snippet
```java
import java.security.NoSuchAlgorithmException;

public class MessageDigestInputStream extends java.security.DigestInputStream {
    public static final String ALGO_SHA_256 = "SHA-256";

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        return "L" + StringUtils.periodToSlashes(cl.getName()) + ";";
    }
    public String getClassCode(java.lang.reflect.Type type) {
        if (type instanceof Class) {
            return getClassCode((Class<?>)type);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
        } else if (type instanceof TypeVariable) {
            TypeVariable<?> tv = (TypeVariable<?>)type;
            java.lang.reflect.Type[] bounds = tv.getBounds();
            if (bounds != null && bounds.length == 1) {
                return getClassCode(bounds[0]);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
                a.append('<');

                for (java.lang.reflect.Type t : pt.getActualTypeArguments()) {
                    a.append(getClassCode(t));
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
            WildcardType wt = (WildcardType)type;
            StringBuilder a = new StringBuilder();
            java.lang.reflect.Type[] lowBounds = wt.getLowerBounds();
            java.lang.reflect.Type[] upBounds = wt.getUpperBounds();
            for (java.lang.reflect.Type t : upBounds) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
            StringBuilder a = new StringBuilder();
            java.lang.reflect.Type[] lowBounds = wt.getLowerBounds();
            java.lang.reflect.Type[] upBounds = wt.getUpperBounds();
            for (java.lang.reflect.Type t : upBounds) {
                a.append('+');
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
            java.lang.reflect.Type[] lowBounds = wt.getLowerBounds();
            java.lang.reflect.Type[] upBounds = wt.getUpperBounds();
            for (java.lang.reflect.Type t : upBounds) {
                a.append('+');
                a.append(getClassCode(t));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/ASMHelperImpl.java`
#### Snippet
```java
                a.append(getClassCode(t));
            }
            for (java.lang.reflect.Type t : lowBounds) {
                a.append('-');
                a.append(getClassCode(t));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java
        if (tmp == null) {
            if (a.length != 0) {
                return (X[])java.lang.reflect.Array.
                    newInstance(a.getClass().getComponentType(), 0);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/SortedArraySet.java`
#### Snippet
```java

        if (a.length < tmp.length) {
            a = (X[])java.lang.reflect.Array.
                newInstance(a.getClass().getComponentType(), tmp.length);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/util/OpcodesProxy.java`
#### Snippet
```java
    public int DCONST_0;
    public int IF_ICMPLT = 0;
    public java.lang.Integer INTEGER;

    public OpcodesProxy(ASMHelper helper) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/XmlSchemaPrimitiveUtils.java`
#### Snippet
```java
        registerPrimitiveClasses("string", String.class);

        registerPrimitiveClasses("dateTime", java.sql.Date.class,
                                 java.util.Date.class,
                                 Calendar.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/XmlSchemaPrimitiveUtils.java`
#### Snippet
```java

        registerPrimitiveClasses("dateTime", java.sql.Date.class,
                                 java.util.Date.class,
                                 Calendar.class,
                                 java.sql.Timestamp.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/XmlSchemaPrimitiveUtils.java`
#### Snippet
```java
                                 java.util.Date.class,
                                 Calendar.class,
                                 java.sql.Timestamp.class);
        registerPrimitiveClasses("time", java.sql.Time.class);
        // add more as needed
```

### UnnecessaryFullyQualifiedName
Qualifier `java.sql` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/XmlSchemaPrimitiveUtils.java`
#### Snippet
```java
                                 Calendar.class,
                                 java.sql.Timestamp.class);
        registerPrimitiveClasses("time", java.sql.Time.class);
        // add more as needed
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/util/CglibProxyHelper.java`
#### Snippet
```java
    @Override
    protected Object getProxyInternal(ClassLoader loader, Class<?>[] interfaces,
                                      final java.lang.reflect.InvocationHandler h) {

        Class<?> superClass = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/logging/Slf4jLogger.java`
#### Snippet
```java
    public Slf4jLogger(String name, String resourceBundleName) {
        super(name, resourceBundleName);
        logger = org.slf4j.LoggerFactory.getLogger(name);
        if (logger instanceof LocationAwareLogger) {
            locationAwareLogger = (LocationAwareLogger) logger;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/logging/Slf4jLogger.java`
#### Snippet
```java
    private static final String FQCN = AbstractDelegatingLogger.class.getName();

    private final org.slf4j.Logger logger;
    private LocationAwareLogger locationAwareLogger;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/logging/JDKBugHacks.java`
#### Snippet
```java
                // to the web application class loader.
                if (!skipHack("org.apache.cxf.JDKBugHacks.securityProviders")) {
                    java.security.Security.getProviders();
                }

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary and can be removed
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        InputStream is = ResourceUtils.getResourceStream(stylesheetReference, m.getExchange().getBus());
        TransformerFactory transformerFactory = TransformerFactory.newInstance();
        transformerFactory.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
        try {
            transformerFactory.setAttribute(XMLConstants.ACCESS_EXTERNAL_DTD, "");
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary and can be removed
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
        StringWriter stringWriter = new StringWriter();
        TransformerFactory transformerFactory = TransformerFactory.newInstance();
        transformerFactory.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
        try {
            transformerFactory.setAttribute(XMLConstants.ACCESS_EXTERNAL_DTD, "");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.text` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
            if (format.indexOf("{0") >= 0 || format.indexOf("{1") >= 0
                        || format.indexOf("{2") >= 0 || format.indexOf("{3") >= 0) {
                return java.text.MessageFormat.format(format, parameters);
            }
            return format;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/logging/AbstractDelegatingLogger.java`
#### Snippet
```java
     *            the name of the resource bundle to load, cannot be null
     * @return the loaded resource bundle.
     * @throws java.util.MissingResourceException
     *             If the specified resource bundle can not be loaded.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/feature/transform/AbstractXSLTInterceptor.java`
#### Snippet
```java
            TransformerFactory transformerFactory = TransformerFactory.newInstance();
            try {
                transformerFactory.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
            } catch (javax.xml.transform.TransformerConfigurationException ex) {
                //
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/feature/transform/AbstractXSLTInterceptor.java`
#### Snippet
```java
            try {
                transformerFactory.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
            } catch (javax.xml.transform.TransformerConfigurationException ex) {
                //
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
    private static boolean addJaxbObjectFactory(JAXBException e1, Set<Class<?>> classes) {
        boolean added = false;
        java.io.ByteArrayOutputStream bout = new java.io.ByteArrayOutputStream();
        java.io.PrintStream pout = new java.io.PrintStream(bout);
        e1.printStackTrace(pout);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
    private static boolean addJaxbObjectFactory(JAXBException e1, Set<Class<?>> classes) {
        boolean added = false;
        java.io.ByteArrayOutputStream bout = new java.io.ByteArrayOutputStream();
        java.io.PrintStream pout = new java.io.PrintStream(bout);
        e1.printStackTrace(pout);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        boolean added = false;
        java.io.ByteArrayOutputStream bout = new java.io.ByteArrayOutputStream();
        java.io.PrintStream pout = new java.io.PrintStream(bout);
        e1.printStackTrace(pout);
        String str = new String(bout.toByteArray());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        boolean added = false;
        java.io.ByteArrayOutputStream bout = new java.io.ByteArrayOutputStream();
        java.io.PrintStream pout = new java.io.PrintStream(bout);
        e1.printStackTrace(pout);
        String str = new String(bout.toByteArray());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
        Pattern pattern = Pattern.compile("(?<=There's\\sno\\sObjectFactory\\swith\\san\\s"
                                          + "@XmlElementDecl\\sfor\\sthe\\selement\\s\\{)\\S*(?=\\})");
        java.util.regex.Matcher matcher = pattern.matcher(str);
        while (matcher.find()) {
            String pkgName = JAXBUtils.namespaceURIToPackage(matcher.group());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio.charset` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/helpers/IOUtils.java`
#### Snippet
```java

public final class IOUtils {
    public static final Charset UTF8_CHARSET = java.nio.charset.StandardCharsets.UTF_8;
    public static final int DEFAULT_BUFFER_SIZE = 1024 * 4;

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/helpers/XPathUtils.java`
#### Snippet
```java
        try {
            xpathFactory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        DocumentBuilderFactory f = DocumentBuilderFactory.newInstance();
        f.setNamespaceAware(true);
        f.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
        f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
        return f.newDocumentBuilder();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.annotations` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
                return;
            }
            addLoggingSupport(ep, bus, cls.getAnnotation(org.apache.cxf.annotations.Logging.class));
            break;
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.annotations` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
            Server server = (Server)args[0];
            Bus bus = factory.getBus();
            addLoggingSupport(server.getEndpoint(), bus, cls.getAnnotation(org.apache.cxf.annotations.Logging.class));
            break;
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.annotations` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/OldLoggingFactoryBeanListener.java`
#### Snippet
```java

    @SuppressWarnings("deprecation")
    private void addLoggingSupport(Endpoint endpoint, Bus bus, org.apache.cxf.annotations.Logging annotation) {
        if (annotation != null) {
            org.apache.cxf.feature.LoggingFeature lf = new org.apache.cxf.feature.LoggingFeature(annotation);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.feature` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
    private void addLoggingSupport(Endpoint endpoint, Bus bus, org.apache.cxf.annotations.Logging annotation) {
        if (annotation != null) {
            org.apache.cxf.feature.LoggingFeature lf = new org.apache.cxf.feature.LoggingFeature(annotation);
            LOG.warning("Deprecated logging interceptors being used, switch to cxf-rt-ext-logging based logging.");
            lf.initialize(endpoint, bus);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.feature` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/OldLoggingFactoryBeanListener.java`
#### Snippet
```java
    private void addLoggingSupport(Endpoint endpoint, Bus bus, org.apache.cxf.annotations.Logging annotation) {
        if (annotation != null) {
            org.apache.cxf.feature.LoggingFeature lf = new org.apache.cxf.feature.LoggingFeature(annotation);
            LOG.warning("Deprecated logging interceptors being used, switch to cxf-rt-ext-logging based logging.");
            lf.initialize(endpoint, bus);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.service.invoker` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/service/factory/SimpleMethodDispatcher.java`
#### Snippet
```java

public class SimpleMethodDispatcher
    implements org.apache.cxf.service.invoker.MethodDispatcher  {

    private Map<Method, Map<BindingInfo, BindingOperationInfo>> infoMap =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
     * @param word the word to check.
     * @return true if the word is a keyword.
     * @see org.apache.cxf.helpers.JavaUtils
     */
    protected static boolean isJavaKeyword(String word) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java

        HOLDER_TYPES_MAP = new HashMap<>();
        HOLDER_TYPES_MAP.put("int", java.lang.Integer.class);
        HOLDER_TYPES_MAP.put("long", java.lang.Long.class);
        HOLDER_TYPES_MAP.put("short", java.lang.Short.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP = new HashMap<>();
        HOLDER_TYPES_MAP.put("int", java.lang.Integer.class);
        HOLDER_TYPES_MAP.put("long", java.lang.Long.class);
        HOLDER_TYPES_MAP.put("short", java.lang.Short.class);
        HOLDER_TYPES_MAP.put("float", java.lang.Float.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP.put("int", java.lang.Integer.class);
        HOLDER_TYPES_MAP.put("long", java.lang.Long.class);
        HOLDER_TYPES_MAP.put("short", java.lang.Short.class);
        HOLDER_TYPES_MAP.put("float", java.lang.Float.class);
        HOLDER_TYPES_MAP.put("double", java.lang.Double.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP.put("long", java.lang.Long.class);
        HOLDER_TYPES_MAP.put("short", java.lang.Short.class);
        HOLDER_TYPES_MAP.put("float", java.lang.Float.class);
        HOLDER_TYPES_MAP.put("double", java.lang.Double.class);
        HOLDER_TYPES_MAP.put("boolean", java.lang.Boolean.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP.put("short", java.lang.Short.class);
        HOLDER_TYPES_MAP.put("float", java.lang.Float.class);
        HOLDER_TYPES_MAP.put("double", java.lang.Double.class);
        HOLDER_TYPES_MAP.put("boolean", java.lang.Boolean.class);
        HOLDER_TYPES_MAP.put("byte", java.lang.Byte.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP.put("float", java.lang.Float.class);
        HOLDER_TYPES_MAP.put("double", java.lang.Double.class);
        HOLDER_TYPES_MAP.put("boolean", java.lang.Boolean.class);
        HOLDER_TYPES_MAP.put("byte", java.lang.Byte.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        HOLDER_TYPES_MAP.put("double", java.lang.Double.class);
        HOLDER_TYPES_MAP.put("boolean", java.lang.Boolean.class);
        HOLDER_TYPES_MAP.put("byte", java.lang.Byte.class);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        Package p = cls.getPackage();
        if (p != null) {
            jakarta.xml.bind.annotation.XmlSchema schemaAnn =
                p.getAnnotation(jakarta.xml.bind.annotation.XmlSchema.class);
            if (schemaAnn != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        if (p != null) {
            jakarta.xml.bind.annotation.XmlSchema schemaAnn =
                p.getAnnotation(jakarta.xml.bind.annotation.XmlSchema.class);
            if (schemaAnn != null) {
                return schemaAnn.namespace();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.resource` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/resource/ExtendedURIResolver.java`
#### Snippet
```java

    public ExtendedURIResolver() {
        currentResolver = new org.apache.cxf.resource.URIResolver();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.resource` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/resource/ExtendedURIResolver.java`
#### Snippet
```java
public class ExtendedURIResolver {

    protected org.apache.cxf.resource.URIResolver currentResolver;
    protected String lastestImportUri;
    protected final Deque<InputStream> resourceOpened = new ArrayDeque<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/service/invoker/AbstractInvoker.java`
#### Snippet
```java
     *
     * @param object the object to check
     * @see java.lang.reflect.Proxy#isProxyClass
     */
    public static boolean isJdkDynamicProxy(Object object) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
    public void writeEndElement() throws XMLStreamException {
        addEvent(factory.createEndElement(lastStart,
                                          Collections.<javax.xml.stream.events.Namespace>emptyList().iterator()));
        curContext = contexts.pop();
        lastStart = elNames.pop();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        curContext = new NSContext(curContext);
        lastStart = new QName(ns, name);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        lastStart = new QName(ns, name);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        curContext = new NSContext(curContext);
        lastStart = new QName(name);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        lastStart = new QName(name);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        curContext = new NSContext(curContext);
        lastStart = new QName(ns, name, pfx);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream.events` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/staxutils/CachingXmlEventWriter.java`
#### Snippet
```java
        lastStart = new QName(ns, name, pfx);
        Set<javax.xml.stream.events.Attribute> atSet = Collections.emptySet();
        Set<javax.xml.stream.events.Namespace> nsSet = Collections.emptySet();
        addEvent(factory.createStartElement(lastStart, atSet.iterator(), nsSet.iterator()));
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartDocument(java.lang.String encoding, java.lang.String version)
        throws XMLStreamException {
        baseWriter.writeStartDocument(encoding, version);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartDocument(java.lang.String encoding, java.lang.String version)
        throws XMLStreamException {
        baseWriter.writeStartDocument(encoding, version);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeProcessingInstruction(java.lang.String target, java.lang.String data)
        throws XMLStreamException {
        baseWriter.writeProcessingInstruction(target, data);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeProcessingInstruction(java.lang.String target, java.lang.String data)
        throws XMLStreamException {
        baseWriter.writeProcessingInstruction(target, data);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeNamespace(java.lang.String prefix, java.lang.String namespaceURI)
        throws XMLStreamException {
        baseWriter.writeNamespace(prefix, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeNamespace(java.lang.String prefix, java.lang.String namespaceURI)
        throws XMLStreamException {
        baseWriter.writeNamespace(prefix, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartDocument(java.lang.String version) throws XMLStreamException {
        baseWriter.writeStartDocument(version);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java


    public java.lang.String getPrefix(java.lang.String uri) throws XMLStreamException {
        return baseWriter.getPrefix(uri);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java


    public java.lang.String getPrefix(java.lang.String uri) throws XMLStreamException {
        return baseWriter.getPrefix(uri);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java

    public void writeAttribute(java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
        baseWriter.writeAttribute(namespaceURI, localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    public void writeAttribute(java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
        baseWriter.writeAttribute(namespaceURI, localName, value);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String localName) throws XMLStreamException {
        baseWriter.writeEmptyElement(localName);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeDefaultNamespace(java.lang.String namespaceURI) throws XMLStreamException {
        baseWriter.writeDefaultNamespace(namespaceURI);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeCharacters(java.lang.String text) throws XMLStreamException {
        baseWriter.writeCharacters(text);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String prefix,
                        java.lang.String namespaceURI,
                        java.lang.String localName,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java

    public void writeAttribute(java.lang.String prefix,
                        java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    public void writeAttribute(java.lang.String prefix,
                        java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
        baseWriter.writeAttribute(prefix, namespaceURI, localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
                        java.lang.String namespaceURI,
                        java.lang.String localName,
                        java.lang.String value) throws XMLStreamException {
        baseWriter.writeAttribute(prefix, namespaceURI, localName, value);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String localName) throws XMLStreamException {
        writeStartElement(null, localName, null);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeComment(java.lang.String data) throws XMLStreamException {
        baseWriter.writeComment(data);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeDTD(java.lang.String dtd) throws XMLStreamException {
        baseWriter.writeDTD(dtd);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void setPrefix(java.lang.String prefix, java.lang.String uri)
        throws XMLStreamException {
        baseWriter.setPrefix(prefix, uri);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void setPrefix(java.lang.String prefix, java.lang.String uri)
        throws XMLStreamException {
        baseWriter.setPrefix(prefix, uri);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeProcessingInstruction(java.lang.String target)
        throws XMLStreamException {
        baseWriter.writeProcessingInstruction(target);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void setDefaultNamespace(java.lang.String uri) throws XMLStreamException {
        baseWriter.setDefaultNamespace(uri);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeCData(java.lang.String data) throws XMLStreamException {
        baseWriter.writeCData(data);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String namespaceURI, java.lang.String localName)
        throws XMLStreamException {
        writeStartElement(null, localName, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String namespaceURI, java.lang.String localName)
        throws XMLStreamException {
        writeStartElement(null, localName, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java

    public void writeStartElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
        QName currElemName = new QName(namespaceURI, localName);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    public void writeStartElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
        QName currElemName = new QName(namespaceURI, localName);
        if (elems.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java

    public void writeEmptyElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
        baseWriter.writeEmptyElement(prefix, localName, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    public void writeEmptyElement(java.lang.String prefix,
                           java.lang.String localName,
                           java.lang.String namespaceURI) throws XMLStreamException {
        baseWriter.writeEmptyElement(prefix, localName, namespaceURI);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeEntityRef(java.lang.String name) throws XMLStreamException {
        baseWriter.writeEntityRef(name);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String localName, java.lang.String value)
        throws XMLStreamException {
        baseWriter.writeAttribute(localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String localName, java.lang.String value)
        throws XMLStreamException {
        baseWriter.writeAttribute(localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public Object getProperty(java.lang.String name) {
        return baseWriter.getProperty(name);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String namespaceURI, java.lang.String localName)
        throws XMLStreamException {
        baseWriter.writeEmptyElement(localName, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/PrettyPrintXMLStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String namespaceURI, java.lang.String localName)
        throws XMLStreamException {
        baseWriter.writeEmptyElement(localName, namespaceURI);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamWriter.java`
#### Snippet
```java
        ((W3CNamespaceContext)context).setElement(element);
        if (appendedChildNode != null) {
            currentNode = org.apache.cxf.helpers.DOMUtils.getDomElement(appendedChildNode);
        } else {
            currentNode = element;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/transport/AbstractMultiplexDestination.java`
#### Snippet
```java
     * @param id the id to encode in the new reference
     * @return the new reference with the id encoded as a reference parameter
     * @see org.apache.cxf.transport.MultiplexDestination#getAddressWithId(java.lang.String)

     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/transport/AbstractMultiplexDestination.java`
#### Snippet
```java
     * @param id the id to encode in the new reference
     * @return the new reference with the id encoded as a reference parameter
     * @see org.apache.cxf.transport.MultiplexDestination#getAddressWithId(java.lang.String)

     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/transport/AbstractMultiplexDestination.java`
#### Snippet
```java
     * @param contextMap the current invocation or message context
     * @return the id from the reference parameters of the  ws-a-to address or null if not found
     * @see org.apache.cxf.transport.MultiplexDestination#getId(java.util.Map)
     */
    public String getId(Map<String, Object> contextMap) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/transport/AbstractMultiplexDestination.java`
#### Snippet
```java
     * @param contextMap the current invocation or message context
     * @return the id from the reference parameters of the  ws-a-to address or null if not found
     * @see org.apache.cxf.transport.MultiplexDestination#getId(java.util.Map)
     */
    public String getId(Map<String, Object> contextMap) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.databinding` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/databinding/source/SourceDataBinding.java`
#### Snippet
```java
 * A simple databinding implementation which reads and writes Source objects.
 */
public class SourceDataBinding extends org.apache.cxf.databinding.AbstractDataBinding {

    public static final String PREFERRED_FORMAT = "source-preferred-format";
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
                Integer responseCode = (Integer)exchange.get(Message.RESPONSE_CODE);
                resContext.put(MessageContext.HTTP_RESPONSE_CODE, responseCode);
                resContext.put(org.apache.cxf.message.Message.RESPONSE_CODE, responseCode);
                setResponseContext(resContext);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataWriter.java`
#### Snippet
```java
            } else if (obj instanceof Node) {
                if (obj instanceof DocumentFragment) {
                    obj = org.apache.cxf.helpers.DOMUtils.getDomDocumentFragment((DocumentFragment)obj);
                }
                if (schema != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataWriter.java`
#### Snippet
```java
                    while (ch != null) {
                        nd.removeChild(ch);
                        dw.getCurrentNode().appendChild(org.apache.cxf.helpers.DOMUtils.getDomElement(ch));
                        ch = nd.getFirstChild();
                    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `core/src/main/java/org/apache/cxf/interceptor/AttachmentOutInterceptor.java`
#### Snippet
```java
        serializer.setXop(mtomEnabled);
        String contentTransferEncoding = (String)message.getContextualProperty(
                                            org.apache.cxf.message.Message.CONTENT_TRANSFER_ENCODING);
        if (contentTransferEncoding != null) {
            serializer.setContentTransferEncoding(contentTransferEncoding);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/databinding/source/XMLStreamDataReader.java`
#### Snippet
```java
                    String cid = DOMUtils.getAttribute(include, "href");
                    //set the fake base64Binary to validate instead of reading the attachment from message
                    parentNode.setTextContent(jakarta.xml.bind.DatatypeConverter.printBase64Binary(cid.getBytes()));
                }
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/interceptor/security/NamePasswordCallbackHandler.java`
#### Snippet
```java
                pwCallback.setPassword(password == null ? null : password.toCharArray());
            } else if (!invokePasswordCallback(callback)) {
                org.apache.cxf.common.i18n.Message errorMsg =
                    new org.apache.cxf.common.i18n.Message("UNSUPPORTED_CALLBACK_TYPE",
                                                           BUNDLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/interceptor/security/NamePasswordCallbackHandler.java`
#### Snippet
```java
            } else if (!invokePasswordCallback(callback)) {
                org.apache.cxf.common.i18n.Message errorMsg =
                    new org.apache.cxf.common.i18n.Message("UNSUPPORTED_CALLBACK_TYPE",
                                                           BUNDLE,
                                                           callbacks[i].getClass().getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/interceptor/security/NameDigestPasswordCallbackHandler.java`
#### Snippet
```java
                pwCallback.setPassword(password == null ? null : password.toCharArray());
            } else if (!invokePasswordCallback(callback)) {
                org.apache.cxf.common.i18n.Message errorMsg =
                    new org.apache.cxf.common.i18n.Message("UNSUPPORTED_CALLBACK_TYPE",
                                                           BUNDLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/interceptor/security/NameDigestPasswordCallbackHandler.java`
#### Snippet
```java
            } else if (!invokePasswordCallback(callback)) {
                org.apache.cxf.common.i18n.Message errorMsg =
                    new org.apache.cxf.common.i18n.Message("UNSUPPORTED_CALLBACK_TYPE",
                                                           BUNDLE,
                                                           callbacks[i].getClass().getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/configuration/jsse/TLSParameterJaxBUtils.java`
#### Snippet
```java

            return fac.getKeyManagers();
        } catch (java.security.UnrecoverableKeyException uke) {
            //jsse has the restriction that different key in keystore
            //cannot has different password, use MultiKeyPasswordKeyManager
```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.beans.factory.xml` is unnecessary, and can be replaced with an import
in `core/src/main/java/org/apache/cxf/configuration/spring/AbstractBeanDefinitionParser.java`
#### Snippet
```java

public abstract class AbstractBeanDefinitionParser
    extends org.springframework.beans.factory.xml.AbstractSingleBeanDefinitionParser {
    public static final String WIRE_BUS_ATTRIBUTE = AbstractBeanDefinitionParser.class.getName() + ".wireBus";
    public static final String WIRE_BUS_NAME = AbstractBeanDefinitionParser.class.getName() + ".wireBusName";
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.soap` is unnecessary, and can be replaced with an import
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsCreatePullPoint.java`
#### Snippet
```java
            serviceName = "CreatePullPointService",
            portName = "CreatePullPointPort")
@BindingType(jakarta.xml.ws.soap.SOAPBinding.SOAP12HTTP_BINDING)
public class JaxwsCreatePullPoint extends JmsCreatePullPoint
    implements JaxwsCreatePullPointMBean {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.soap` is unnecessary, and can be replaced with an import
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsPublisher.java`
#### Snippet
```java
            serviceName = "PublisherRegistrationManagerService",
            portName = "PublisherRegistrationManagerPort")
@BindingType(jakarta.xml.ws.soap.SOAPBinding.SOAP12HTTP_BINDING)
public class JaxwsPublisher extends JmsPublisher {

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.namespace` is unnecessary and can be removed
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/AbstractNotificationBroker.java`
#### Snippet
```java
                  name = "GetResourceProperty",
                  targetNamespace = "http://docs.oasis-open.org/wsrf/rp-2")
        javax.xml.namespace.QName getResourcePropertyRequest
    ) throws ResourceUnavailableFault, ResourceUnknownFault, InvalidResourcePropertyQNameFault {

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.soap` is unnecessary, and can be replaced with an import
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsNotificationBroker.java`
#### Snippet
```java
            serviceName = "NotificationBrokerService",
            portName = "NotificationBrokerPort")
@BindingType(jakarta.xml.ws.soap.SOAPBinding.SOAP12HTTP_BINDING)
public class JaxwsNotificationBroker extends JmsNotificationBroker
    implements JaxwsNotificationBrokerMBean {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
                context.setWriteXsiTypes(writeXsiType);
                context.setReadXsiTypes(readXsiType);
                Set<java.lang.reflect.Type> rootClasses = new HashSet<>();
                rootClasses.add(reflectionType);
                context.setRootClasses(rootClasses);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/aegis/AbstractAegisProvider.java`
#### Snippet
```java
    implements MessageBodyReader<T>, MessageBodyWriter<T> {

    private static Map<java.lang.reflect.Type, AegisContext> classContexts
        = new WeakHashMap<>();

```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    public void writeTo(org.dom4j.Document doc, Class<?> cls,
                        Type type, Annotation[] anns, MediaType mt,
                        MultivaluedMap<String, Object> headers, OutputStream os)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j.io` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
            XMLWriter writer;
            if (MessageUtils.getContextualBoolean(getCurrentMessage(), SUPPRESS_XML_DECLARATION, false)) {
                OutputFormat format = new org.dom4j.io.OutputFormat();
                format.setSuppressDeclaration(true);
                writer = new org.dom4j.io.XMLWriter(os, format);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j.io` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
                OutputFormat format = new org.dom4j.io.OutputFormat();
                format.setSuppressDeclaration(true);
                writer = new org.dom4j.io.XMLWriter(os, format);
            } else {
                writer = new org.dom4j.io.XMLWriter(os);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j.io` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
                writer = new org.dom4j.io.XMLWriter(os, format);
            } else {
                writer = new org.dom4j.io.XMLWriter(os);
            }
            writer.write(doc);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
            writer.flush();
        } else {
            org.w3c.dom.Document domDoc = convertToDOM(doc);

            MessageBodyWriter<org.w3c.dom.Document> writer =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
            org.w3c.dom.Document domDoc = convertToDOM(doc);

            MessageBodyWriter<org.w3c.dom.Document> writer =
                providers.getMessageBodyWriter(DOM_DOC_CLS, DOM_DOC_CLS, anns, mt);
            if (writer == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    public boolean isWriteable(Class<?> cls, Type type,
                               Annotation[] anns, MediaType mt) {
        return org.dom4j.Document.class.isAssignableFrom(cls);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    implements MessageBodyReader<org.dom4j.Document>, MessageBodyWriter<org.dom4j.Document> {
    public static final String SUPPRESS_XML_DECLARATION = "suppress.xml.declaration";
    private static final Class<org.w3c.dom.Document> DOM_DOC_CLS =
        org.w3c.dom.Document.class;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    public static final String SUPPRESS_XML_DECLARATION = "suppress.xml.declaration";
    private static final Class<org.w3c.dom.Document> DOM_DOC_CLS =
        org.w3c.dom.Document.class;

    private Providers providers;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    private org.w3c.dom.Document convertToDOM(org.dom4j.Document doc) {
        String xml = doc.asXML();
        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    private org.w3c.dom.Document convertToDOM(org.dom4j.Document doc) {
        String xml = doc.asXML();
        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    public org.dom4j.Document readFrom(Class<org.dom4j.Document> cls, Type type,
                    Annotation[] anns, MediaType mt,
                    MultivaluedMap<String, String> headers, InputStream is)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    public org.dom4j.Document readFrom(Class<org.dom4j.Document> cls, Type type,
                    Annotation[] anns, MediaType mt,
                    MultivaluedMap<String, String> headers, InputStream is)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
                    MultivaluedMap<String, String> headers, InputStream is)
        throws IOException, WebApplicationException {
        MessageBodyReader<org.w3c.dom.Document> reader =
            providers.getMessageBodyReader(DOM_DOC_CLS, DOM_DOC_CLS, anns, mt);
        if (reader == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
            throw ExceptionUtils.toNotSupportedException(null, null);
        }
        org.w3c.dom.Document domDoc =
            reader.readFrom(DOM_DOC_CLS, DOM_DOC_CLS, anns, mt, headers, is);
        return new org.dom4j.io.DOMReader().read(domDoc);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j.io` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
        org.w3c.dom.Document domDoc =
            reader.readFrom(DOM_DOC_CLS, DOM_DOC_CLS, anns, mt, headers, is);
        return new org.dom4j.io.DOMReader().read(domDoc);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
@Consumes({"application/xml", "application/*+xml", "text/xml", "application/json", "application/*+json" })
public class DOM4JProvider extends AbstractConfigurableProvider
    implements MessageBodyReader<org.dom4j.Document>, MessageBodyWriter<org.dom4j.Document> {
    public static final String SUPPRESS_XML_DECLARATION = "suppress.xml.declaration";
    private static final Class<org.w3c.dom.Document> DOM_DOC_CLS =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
@Consumes({"application/xml", "application/*+xml", "text/xml", "application/json", "application/*+json" })
public class DOM4JProvider extends AbstractConfigurableProvider
    implements MessageBodyReader<org.dom4j.Document>, MessageBodyWriter<org.dom4j.Document> {
    public static final String SUPPRESS_XML_DECLARATION = "suppress.xml.declaration";
    private static final Class<org.w3c.dom.Document> DOM_DOC_CLS =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    public boolean isReadable(Class<?> cls, Type type, Annotation[] anns,
                    MediaType mt) {
        return org.dom4j.Document.class.isAssignableFrom(cls);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.dom4j` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/dom4j/DOM4JProvider.java`
#### Snippet
```java
    }

    public long getSize(org.dom4j.Document doc, Class<?> cls, Type type,
        Annotation[] anns, MediaType mt) {
        return -1;
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util` is unnecessary, and can be replaced with an import
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpDestinationFactory.java`
#### Snippet
```java

    public NettyHttpDestinationFactory() {
        io.netty.util.Version.identify();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpDestination.java`
#### Snippet
```java
     * @param ei                  the endpoint info of the destination
     * @param serverEngineFactory the serverEngineFactory which could be used to create ServerEngine
     * @throws java.io.IOException
     */
    public NettyHttpDestination(Bus b, DestinationRegistry registry,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyHttpSession.java`
#### Snippet
```java
    @SuppressWarnings("deprecation")
    @Override
    public jakarta.servlet.http.HttpSessionContext getSessionContext() {
        throw new IllegalStateException(
                "As of Version 2.1, this method is deprecated and has no replacement.");
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/spring/NettyHttpServerEngineBeanDefinitionParser.java`
#### Snippet
```java
                }

                elem = org.apache.cxf.helpers.DOMUtils.getNextElement(elem);
            }
        } catch (Exception e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletResponse.java`
#### Snippet
```java

    @Override
    public void addCookie(jakarta.servlet.http.Cookie cookie) {
        //TODO Do we need to implement it ?
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletContext.java`
#### Snippet
```java

    @Override
    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName, 
            Class<? extends Filter> filterClass) {
        throw new IllegalStateException("Method 'addFilter' not yet implemented!");
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletContext.java`
#### Snippet
```java

    @Override
    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName, String className) {
        throw new IllegalStateException("Method 'addFilter' not yet implemented!");
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/servlet/NettyServletContext.java`
#### Snippet
```java

    @Override
    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName, Filter filter) {
        throw new IllegalStateException("Method 'addFilter' not yet implemented!");
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/transfer/src/main/java/org/apache/cxf/ws/transfer/dialect/fragment/language/FragmentDialectLanguageXPath10.java`
#### Snippet
```java
        try {
            xpathFactory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary, and can be replaced with an import
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java

        QName qname = actualClass == Document.class
            ? org.apache.cxf.helpers.DOMUtils.getElementQName(((Document)actualObject).getDocumentElement())
            : getQName(actualClass, genericType, actualObject);
        if (qname != null && ignoreNamespaces && (isCollection || dropRootInXmlNeeded)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.utils` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
        if (firstObj != null) {
            XmlJavaTypeAdapter adapter =
                org.apache.cxf.jaxrs.utils.JAXBUtils.getAdapter(firstObj.getClass(), anns);
            marshalCollectionMember(JAXBUtils.useAdapter(firstObj, adapter, true),
                                    actualClass, genericType, encoding, os);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.utils` is unnecessary and can be removed
in `rt/rs/extensions/providers/src/main/java/org/apache/cxf/jaxrs/provider/json/JSONProvider.java`
#### Snippet
```java
                response = ((CollectionWrapper)response).getCollectionOrArray(
                               unmarshaller, theType, type, genericType,
                               org.apache.cxf.jaxrs.utils.JAXBUtils.getAdapter(theGenericType, anns));
            } else {
                response = checkAdapter(response, type, anns, false);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.ext` is unnecessary and can be removed
in `integration/cdi/src/main/java/org/apache/cxf/cdi/JAXRSCdiResourceExtension.java`
#### Snippet
```java
     *
     * It also has a side effect of capturing the context object type, in case no
     * {@link org.apache.cxf.jaxrs.ext.ContextClassProvider} was registered for the type.
     *
     * @param processAnnotatedType the annotated type being investigated
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/STSException.java`
#### Snippet
```java
     * A map of Fault Code to Fault Strings
     */
    private static final java.util.Map<QName, String> FAULT_CODE_MAP =
            new java.util.HashMap<>();

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/STSException.java`
#### Snippet
```java
     */
    private static final java.util.Map<QName, String> FAULT_CODE_MAP =
            new java.util.HashMap<>();

    static {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.configuration.jsse` is unnecessary and can be removed
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            KeyManager[] keyManagers = tlsClientParameters.getKeyManagers();
            if (keyManagers == null) {
                keyManagers = org.apache.cxf.configuration.jsse.SSLUtils.getDefaultKeyStoreManagers(LOG);
            }
            KeyManager[] configuredKeyManagers =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.configuration.jsse` is unnecessary and can be removed
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            TrustManager[] trustManagers = tlsClientParameters.getTrustManagers();
            if (trustManagers == null) {
                trustManagers = org.apache.cxf.configuration.jsse.SSLUtils.getDefaultTrustStoreManagers(LOG);
            }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.security` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JTokenConverter.java`
#### Snippet
```java
                nonce = Base64Utility.encode(utp.getNonce());
            }
            msg.put(org.apache.cxf.common.security.SecurityToken.class,
                    new UsernameToken(utp.getName(),
                                      utp.getPassword(),
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.configuration.jsse` is unnecessary and can be removed
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            KeyManager[] keyManagers = tlsClientParameters.getKeyManagers();
            if (keyManagers == null) {
                keyManagers = org.apache.cxf.configuration.jsse.SSLUtils.getDefaultKeyStoreManagers(LOG);
            }
            KeyManager[] configuredKeyManagers =
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.configuration.jsse` is unnecessary and can be removed
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            TrustManager[] trustManagers = tlsClientParameters.getTrustManagers();
            if (trustManagers == null) {
                trustManagers = org.apache.cxf.configuration.jsse.SSLUtils.getDefaultTrustStoreManagers(LOG);
            }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.attachment` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
                AttachmentResultCallback attachmentResultCallback = (AttachmentResultCallback) callback;

                org.apache.cxf.attachment.AttachmentImpl securedAttachment =
                    new org.apache.cxf.attachment.AttachmentImpl(
                        attachmentResultCallback.getAttachmentId(),
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.attachment` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java

                org.apache.cxf.attachment.AttachmentImpl securedAttachment =
                    new org.apache.cxf.attachment.AttachmentImpl(
                        attachmentResultCallback.getAttachmentId(),
                        new DataHandler(
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
                    // Calling LazyAttachmentCollection.size() here to force it to load the attachments
                    if (attachments != null && attachments.size() > 0) {  // NOPMD
                        for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                            iterator.hasNext();) {
                            org.apache.cxf.message.Attachment attachment = iterator.next();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
                        for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                            iterator.hasNext();) {
                            org.apache.cxf.message.Attachment attachment = iterator.next();

                            if (attachmentId.equals(attachment.getId())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
    }

    public AttachmentCallbackHandler(Collection<org.apache.cxf.message.Attachment> attachments) {
        this.attachments = attachments;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
public class AttachmentCallbackHandler implements CallbackHandler {

    private final Collection<org.apache.cxf.message.Attachment> attachments;

    public AttachmentCallbackHandler(Message message) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
        // Calling LazyAttachmentCollection.size() here to force it to load the attachments
        if (attachments != null && attachments.size() > 0) { // NOPMD
            for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                iterator.hasNext();) {
                org.apache.cxf.message.Attachment attachment = iterator.next();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/AttachmentCallbackHandler.java`
#### Snippet
```java
            for (Iterator<org.apache.cxf.message.Attachment> iterator = attachments.iterator();
                iterator.hasNext();) {
                org.apache.cxf.message.Attachment attachment = iterator.next();

                if (attachmentId != null
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.namespace` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
    private SoapFault createSoapFault(SoapVersion version, WSSecurityException e) {
        SoapFault fault;
        javax.xml.namespace.QName faultCode = e.getFaultCode();
        if (version.getVersion() == 1.1 && faultCode != null) {
            fault = new SoapFault(e.getMessage(), e, faultCode);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.namespace` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JUtils.java`
#### Snippet
```java

        final String errorMessage;
        final javax.xml.namespace.QName faultCode;

        boolean returnSecurityError =
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.namespace` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxCryptoCoverageChecker.java`
#### Snippet
```java
    private SoapFault createSoapFault(SoapVersion version, WSSecurityException e) {
        SoapFault fault;
        javax.xml.namespace.QName faultCode = e.getFaultCode();
        if (version.getVersion() == 1.1 && faultCode != null) {
            fault = new SoapFault(e.getMessage(), e, faultCode);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageChecker.java`
#### Snippet
```java
        try {
            factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wss4j.policy` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JOutInterceptor.java`
#### Snippet
```java
            if (binding == null && isRequestor(message)) {
                Policy policy = new Policy();
                binding = new TransportBinding(org.apache.wss4j.policy.SPConstants.SPVersion.SP11,
                                                 policy);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JStaxInInterceptor.java`
#### Snippet
```java
        }
        String actor = (String)msg.getContextualProperty(SecurityConstants.ACTOR);
        final Collection<org.apache.cxf.message.Attachment> attachments = msg.getAttachments();
        int attachmentCount = 0;
        if (attachments != null && !attachments.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wss4j.common.bsp` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
                                                                  boolean allowNamespaceQualifiedPWDTypes)
        throws WSSecurityException, Base64DecodingException {
        BSPEnforcer bspEnforcer = new org.apache.wss4j.common.bsp.BSPEnforcer(!bspCompliant);
        org.apache.wss4j.dom.message.token.UsernameToken ut =
            new org.apache.wss4j.dom.message.token.UsernameToken(tokenElement, allowNamespaceQualifiedPWDTypes,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.wss4j.policy.model` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/UsernameTokenInterceptor.java`
#### Snippet
```java
     */
    private boolean isNonEndorsingSupportingToken(
        org.apache.wss4j.policy.model.UsernameToken usernameTokenPolicy
    ) {
        AbstractSecurityAssertion supportingToken = usernameTokenPolicy.getParentAssertion();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxOutInterceptor.java`
#### Snippet
```java
        //must turn off mtom when using WS-Sec so binary is inlined so it can
        //be properly signed/encrypted/etc...
        String mtomKey = org.apache.cxf.message.Message.MTOM_ENABLED;
        if (Boolean.TRUE.equals(mc.get(mtomKey))) {
            LOG.warning("MTOM will be disabled as the WSS4JOutInterceptor.mtomEnabled property"
```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.sr` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
        try {
            NamespaceContext nsctx = reader.getNamespaceContext();
            if (nsctx instanceof com.ctc.wstx.sr.InputElementStack) {
                com.ctc.wstx.sr.InputElementStack ies = (com.ctc.wstx.sr.InputElementStack)nsctx;
                com.ctc.wstx.util.InternCache ic = com.ctc.wstx.util.InternCache.getInstance();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.sr` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
            NamespaceContext nsctx = reader.getNamespaceContext();
            if (nsctx instanceof com.ctc.wstx.sr.InputElementStack) {
                com.ctc.wstx.sr.InputElementStack ies = (com.ctc.wstx.sr.InputElementStack)nsctx;
                com.ctc.wstx.util.InternCache ic = com.ctc.wstx.util.InternCache.getInstance();

```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.sr` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
            NamespaceContext nsctx = reader.getNamespaceContext();
            if (nsctx instanceof com.ctc.wstx.sr.InputElementStack) {
                com.ctc.wstx.sr.InputElementStack ies = (com.ctc.wstx.sr.InputElementStack)nsctx;
                com.ctc.wstx.util.InternCache ic = com.ctc.wstx.util.InternCache.getInstance();

```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.util` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
            if (nsctx instanceof com.ctc.wstx.sr.InputElementStack) {
                com.ctc.wstx.sr.InputElementStack ies = (com.ctc.wstx.sr.InputElementStack)nsctx;
                com.ctc.wstx.util.InternCache ic = com.ctc.wstx.util.InternCache.getInstance();

                Map<String, String> storedNamespaces = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.util` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
            if (nsctx instanceof com.ctc.wstx.sr.InputElementStack) {
                com.ctc.wstx.sr.InputElementStack ies = (com.ctc.wstx.sr.InputElementStack)nsctx;
                com.ctc.wstx.util.InternCache ic = com.ctc.wstx.util.InternCache.getInstance();

                Map<String, String> storedNamespaces = new HashMap<>();
```

### UnnecessaryFullyQualifiedName
Qualifier `com.ctc.wstx.api` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
                try {
                    factory.setProperty("com.ctc.wstx.fragmentMode",
                                        com.ctc.wstx.api.WstxInputProperties.PARSING_MODE_FRAGMENT);
                    factory.setProperty(org.codehaus.stax2.XMLInputFactory2.P_REPORT_PROLOG_WHITESPACE, Boolean.TRUE);
                    validFactory = true;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.codehaus.stax2` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSerializer.java`
#### Snippet
```java
                    factory.setProperty("com.ctc.wstx.fragmentMode",
                                        com.ctc.wstx.api.WstxInputProperties.PARSING_MODE_FRAGMENT);
                    factory.setProperty(org.codehaus.stax2.XMLInputFactory2.P_REPORT_PROLOG_WHITESPACE, Boolean.TRUE);
                    validFactory = true;
                } catch (Throwable t) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
    }

    private boolean findClaimInAssertion(org.opensaml.saml.saml1.core.Assertion assertion, URI claimURI) {
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java

    private boolean findClaimInAssertion(org.opensaml.saml.saml1.core.Assertion assertion, URI claimURI) {
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
        }

        for (org.opensaml.saml.saml1.core.AttributeStatement statement : attributeStatements) {

            List<org.opensaml.saml.saml1.core.Attribute> attributes = statement.getAttributes();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
        for (org.opensaml.saml.saml1.core.AttributeStatement statement : attributeStatements) {

            List<org.opensaml.saml.saml1.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml1.core.Attribute attribute : attributes) {

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java

            List<org.opensaml.saml.saml1.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml1.core.Attribute attribute : attributes) {

                URI attributeNamespace = URI.create(attribute.getAttributeNamespace());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
    }

    private boolean findClaimInAssertion(org.opensaml.saml.saml2.core.Assertion assertion, URI claimURI) {
        List<org.opensaml.saml.saml2.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java

    private boolean findClaimInAssertion(org.opensaml.saml.saml2.core.Assertion assertion, URI claimURI) {
        List<org.opensaml.saml.saml2.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
        }

        for (org.opensaml.saml.saml2.core.AttributeStatement statement : attributeStatements) {

            List<org.opensaml.saml.saml2.core.Attribute> attributes = statement.getAttributes();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java
        for (org.opensaml.saml.saml2.core.AttributeStatement statement : attributeStatements) {

            List<org.opensaml.saml.saml2.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml2.core.Attribute attribute : attributes) {

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/DefaultClaimsPolicyValidator.java`
#### Snippet
```java

            List<org.opensaml.saml.saml2.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml2.core.Attribute attribute : attributes) {

                if (attribute.getName().equals(claimURI.toString())
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/RequiredElementsPolicyValidator.java`
#### Snippet
```java
                try {
                    factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
                } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
                    // ignore
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SecuredElementsPolicyValidator.java`
#### Snippet
```java
        try {
            factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.binding` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/policy/interceptors/NegotiationUtils.java`
#### Snippet
```java
            ex.put(Endpoint.class, endpoint);
            ex.put(Service.class, endpoint.getService());
            ex.put(org.apache.cxf.binding.Binding.class, endpoint.getBinding());
            ex.remove(BindingOperationInfo.class);
            message.put(MAPAggregator.ACTION_VERIFIED, Boolean.TRUE);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureValidator.java`
#### Snippet
```java
        Key key = keyProvider.getKey(signature.getKeyId());

        java.security.Provider provider =
            securityProvider != null ? securityProvider.getProvider(signature.getKeyId()) : null;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureValidator.java`
#### Snippet
```java
                             Key key,
                             Signature signature,
                             java.security.Provider provider,
                             String method,
                             String uri,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.tomitribe.auth.signatures` is unnecessary, and can be replaced with an import
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureCreator.java`
#### Snippet
```java

        final Signature signature = new Signature(keyId, signatureAlgorithmName, null, headers);
        final org.tomitribe.auth.signatures.Signer signer =
                new org.tomitribe.auth.signatures.Signer(keyProvider.getKey(keyId), signature);
        Signature outputSignature = signer.sign(method, uri, SignatureHeaderUtils.mapHeaders(messageHeaders));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.tomitribe.auth.signatures` is unnecessary, and can be replaced with an import
in `rt/rs/security/http-signature/src/main/java/org/apache/cxf/rs/security/httpsignature/TomitribeSignatureCreator.java`
#### Snippet
```java
        final Signature signature = new Signature(keyId, signatureAlgorithmName, null, headers);
        final org.tomitribe.auth.signatures.Signer signer =
                new org.tomitribe.auth.signatures.Signer(keyProvider.getKey(keyId), signature);
        Signature outputSignature = signer.sign(method, uri, SignatureHeaderUtils.mapHeaders(messageHeaders));

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            try {
                factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
            } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
                // ignore
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.binding` is unnecessary, and can be replaced with an import
in `rt/ws/mex/src/main/java/org/apache/cxf/ws/mex/MEXInInterceptor.java`
#### Snippet
```java
            ex.put(Endpoint.class, endpoint);
            ex.put(Service.class, endpoint.getService());
            ex.put(org.apache.cxf.binding.Binding.class, endpoint.getBinding());
            ex.put(BindingOperationInfo.class,
                   endpoint.getBinding().getBindingInfo()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java

    public static void checkAttachmentsCoverage(
        Collection<org.apache.cxf.message.Attachment> attachments,
        final Collection<WSDataRef> refs,
        CoverageType type,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java
        if (attachments != null) {
            // For each matching attachment, check for a ref that covers it.
            for (org.apache.cxf.message.Attachment attachment : attachments) {
                boolean matched = false;

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/CryptoCoverageUtil.java`
#### Snippet
```java
        try {
            factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AbstractSupportingTokenPolicyValidator.java`
#### Snippet
```java
            try {
                factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
            } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
                // ignore
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport.jms.uri` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfigFactory.java`
#### Snippet
```java

        int deliveryMode = endpoint.getDeliveryMode()
            == org.apache.cxf.transport.jms.uri.JMSEndpoint.DeliveryModeType.PERSISTENT
            ? DeliveryMode.PERSISTENT : DeliveryMode.NON_PERSISTENT;
        jmsConfig.setDeliveryMode(deliveryMode);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport.jms.uri` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/uri/JMSEndpointWSDLUtil.java`
#### Snippet
```java
        if (deliveryModeType != null) {
            String deliveryMode = deliveryModeType.getValue().trim();
            endpoint.setDeliveryMode(org.apache.cxf.transport.jms.uri.JMSEndpoint.DeliveryModeType
                                     .valueOf(deliveryMode));
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSMessageUtils.java`
#### Snippet
```java
    }

    public static String getMessageType(final jakarta.jms.Message request) {
        final String msgType;
        if (request instanceof TextMessage) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSMessageConverter.java`
#### Snippet
```java
 * Converts jms messages to Objects and vice a versa.
 * <p>
 * String <=> {@link jakarta.jms.TextMessage}
 * byte[] <=> {@link jakarta.jms.BytesMessage}
 * Serializable object <=> {@link jakarta.jms.ObjectMessage}
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSMessageConverter.java`
#### Snippet
```java
 * <p>
 * String <=> {@link jakarta.jms.TextMessage}
 * byte[] <=> {@link jakarta.jms.BytesMessage}
 * Serializable object <=> {@link jakarta.jms.ObjectMessage}
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSMessageConverter.java`
#### Snippet
```java
 * String <=> {@link jakarta.jms.TextMessage}
 * byte[] <=> {@link jakarta.jms.BytesMessage}
 * Serializable object <=> {@link jakarta.jms.ObjectMessage}
 */
public class JMSMessageConverter {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary, and can be replaced with an import
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSSender.java`
#### Snippet
```java

    public void sendMessage(Session session, Destination targetDest,
                            jakarta.jms.Message message) throws JMSException {
        MessageProducer producer = null;
        try { // NOPMD - UseTryWithResources
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
            }
            exchange.setInMessage(inMessage);
            Object responseCode = inMessage.get(org.apache.cxf.message.Message.RESPONSE_CODE);
            exchange.put(org.apache.cxf.message.Message.RESPONSE_CODE, responseCode);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.nio` is unnecessary, and can be replaced with an import
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSUtil.java`
#### Snippet
```java

    public static String createCorrelationId(final String prefix, long sequenceNum) {
        return prefix + StringUtils.toHexString(java.nio.ByteBuffer.allocate(Long.BYTES).putLong(sequenceNum).array());
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
            exchange.setInMessage(inMessage);
            Object responseCode = inMessage.get(org.apache.cxf.message.Message.RESPONSE_CODE);
            exchange.put(org.apache.cxf.message.Message.RESPONSE_CODE, responseCode);

            if (exchange.isSynchronous()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConduit.java`
#### Snippet
```java
    private void assertIsNotTextMessageAndMtom(final Message outMessage) {
        boolean isTextPayload = JMSConstants.TEXT_MESSAGE_TYPE.equals(jmsConfig.getMessageType());
        if (isTextPayload && MessageUtils.getContextualBoolean(outMessage, org.apache.cxf.message.Message.MTOM_ENABLED)
            && outMessage.getAttachments() != null && outMessage.getAttachments().size() > 0) {
            org.apache.cxf.common.i18n.Message msg =
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSUtil.java`
#### Snippet
```java
                exchange.put(JMS_MESSAGE_CONSUMER, consumer);
            }
            jakarta.jms.Message replyMessage = consumer.receive(receiveTimeout);
            if (replyMessage == null) {
                if ((boolean)exchange.get(JMSUtil.JMS_IGNORE_TIMEOUT)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jms` is unnecessary and can be removed
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/util/JMSUtil.java`
#### Snippet
```java
            MessageConsumer consumer = closer.register(session.createConsumer(replyToDestination, messageSelector,
                                                 pubSubNoLocal));
            jakarta.jms.Message replyMessage = consumer.receive(receiveTimeout);
            if (replyMessage == null) {
                throw new RuntimeException("Timeout receiving message with correlationId "
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/wsdl11/JMSWSDLExtensionLoader.java`
#### Snippet
```java
        WSDLManager manager = b.getExtension(WSDLManager.class);
        for (Class<?> extensor : EXTENSORS) {
            addExtensions(manager, javax.wsdl.Binding.class, extensor);
            addExtensions(manager, javax.wsdl.Port.class, extensor);
            addExtensions(manager, javax.wsdl.Service.class, extensor);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/wsdl11/JMSWSDLExtensionLoader.java`
#### Snippet
```java
        for (Class<?> extensor : EXTENSORS) {
            addExtensions(manager, javax.wsdl.Binding.class, extensor);
            addExtensions(manager, javax.wsdl.Port.class, extensor);
            addExtensions(manager, javax.wsdl.Service.class, extensor);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/wsdl11/JMSWSDLExtensionLoader.java`
#### Snippet
```java
            addExtensions(manager, javax.wsdl.Binding.class, extensor);
            addExtensions(manager, javax.wsdl.Port.class, extensor);
            addExtensions(manager, javax.wsdl.Service.class, extensor);
        }
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java
    }

    public org.apache.cxf.message.Message createMessage() {
        return createMessage(new MessageImpl());
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java
    }

    public org.apache.cxf.message.Message createMessage(org.apache.cxf.message.Message m) {
        if (!m.containsKey(org.apache.cxf.message.Message.CONTENT_TYPE)) {

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java
    }

    public org.apache.cxf.message.Message createMessage(org.apache.cxf.message.Message m) {
        if (!m.containsKey(org.apache.cxf.message.Message.CONTENT_TYPE)) {

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java

    public org.apache.cxf.message.Message createMessage(org.apache.cxf.message.Message m) {
        if (!m.containsKey(org.apache.cxf.message.Message.CONTENT_TYPE)) {

            String ct = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java
            Exchange exchange = m.getExchange();
            if (exchange != null) {
                ct = (String)exchange.get(org.apache.cxf.message.Message.CONTENT_TYPE);
            }
            m.put(org.apache.cxf.message.Message.CONTENT_TYPE, ct);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSBinding.java`
#### Snippet
```java
                ct = (String)exchange.get(org.apache.cxf.message.Message.CONTENT_TYPE);
            }
            m.put(org.apache.cxf.message.Message.CONTENT_TYPE, ct);
        }
        return m;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/ext/MessageContextImpl.java`
#### Snippet
```java
        if (message == null) {
            Endpoint ep = m.getExchange().getEndpoint();
            message = new org.apache.cxf.message.MessageImpl();
            message.setExchange(m.getExchange());
            message = ep.getBinding().createMessage(message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
        }
        if (list.isEmpty()) {
            org.apache.cxf.common.i18n.Message msg =
                new org.apache.cxf.common.i18n.Message("NO_RESOURCES_AVAILABLE",
                                                       BUNDLE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
        if (list.isEmpty()) {
            org.apache.cxf.common.i18n.Message msg =
                new org.apache.cxf.common.i18n.Message("NO_RESOURCES_AVAILABLE",
                                                       BUNDLE);
            LOG.severe(msg.toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.endpoint` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSServerFactoryBean.java`
#### Snippet
```java
 * </pre>
 * This will start a server for you and register it with the ServerManager.  Note
 * you should explicitly close the {@link org.apache.cxf.endpoint.Server} created
 * when finished with it:
 * <pre>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/RequestImpl.java`
#### Snippet
```java
                    sb.append(varyValues.get(i).toString());
                }
                ((jakarta.servlet.http.HttpServletResponse)httpResponse)
                    .setHeader(HttpHeaders.VARY, sb.toString());
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerRequestContextImpl.java`
#### Snippet
```java
        Object servletRequest = m.get("HTTP.REQUEST");
        if (servletRequest != null) {
            ((jakarta.servlet.http.HttpServletRequest)servletRequest)
                .setAttribute(ENDPOINT_ADDRESS_PROPERTY, baseUri.toString());
            
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ContainerRequestContextImpl.java`
#### Snippet
```java
            // The base URI and request URI should be treated differently
            if (requestUri.isAbsolute() && baseUri.resolve("/").compareTo(requestUri.resolve("/")) != 0) {
                ((jakarta.servlet.http.HttpServletRequest)servletRequest)
                    .setAttribute(ENDPOINT_URI_PROPERTY, requestUri.resolve("/"));
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalServletContext.java`
#### Snippet
```java
    }

    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName, Filter filter)
        throws IllegalArgumentException, IllegalStateException {
        return get().addFilter(filterName, filter);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalServletContext.java`
#### Snippet
```java
    }

    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName, String className)
        throws IllegalArgumentException, IllegalStateException {
        return get().addFilter(filterName, className);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/tl/ThreadLocalServletContext.java`
#### Snippet
```java
    }

    public jakarta.servlet.FilterRegistration.Dynamic addFilter(String filterName,
                                                              Class<? extends Filter> filterClass)
        throws IllegalArgumentException, IllegalStateException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.classloader` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/AnnotationUtils.java`
#### Snippet
```java
    private static Class<? extends Annotation> loadClassOrNull(final String name) {
        try {
            return org.apache.cxf.common.classloader.ClassLoaderUtils.loadClass(
                    name, AnnotationUtils.class, Annotation.class);
        } catch (final ClassNotFoundException e) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/HttpUtils.java`
#### Snippet
```java
        
        if (servletRequest != null) {
            final Object property = ((jakarta.servlet.http.HttpServletRequest)servletRequest)
                .getAttribute("org.apache.cxf.transport.endpoint.uri");
            if (property != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.impl` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFJaxrsRuntimeDelegateServlet.java`
#### Snippet
```java
    static {
        // Ensure that the correct JAX-RS implementation is loaded
        RuntimeDelegate runtimeDelegate = new org.apache.cxf.jaxrs.impl.RuntimeDelegateImpl();
        RuntimeDelegate.setInstance(runtimeDelegate);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/FormUtils.java`
#### Snippet
```java
                                             String enc,
                                             boolean decode,
                                             jakarta.servlet.http.HttpServletRequest request) {
        if (!StringUtils.isEmpty(postBody)) {
            populateMapFromString(params, m, postBody, enc, decode);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBAttachmentUnmarshaller.java`
#### Snippet
```java
            bos.close();
        } catch (IOException e) {
            throw new Fault(new org.apache.cxf.common.i18n.Message("ATTACHMENT_READ_ERROR", LOG), e);
        }
        return bos.toByteArray();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/AbstractConfigurableProvider.java`
#### Snippet
```java
    }
    protected void reportEmptyContentLength() throws NoContentException {
        String message = new org.apache.cxf.common.i18n.Message("EMPTY_BODY", BUNDLE).toString();
        LOG.warning(message);
        throw new NoContentException(message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.utils` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/InjectionUtils.java`
#### Snippet
```java
        try {
            if (paramType.isInterface()) {
                paramType = org.apache.cxf.jaxrs.utils.JAXBUtils.getValueTypeFromAdapter(paramType,
                                                                                         paramType,
                                                                                         paramAnns);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyWriter.java`
#### Snippet
```java

        if (w == null) {
            org.apache.cxf.common.i18n.Message message =
                new org.apache.cxf.common.i18n.Message("NO_MSG_WRITER", BUNDLE, type);
            LOG.severe(message.toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyWriter.java`
#### Snippet
```java
        if (w == null) {
            org.apache.cxf.common.i18n.Message message =
                new org.apache.cxf.common.i18n.Message("NO_MSG_WRITER", BUNDLE, type);
            LOG.severe(message.toString());
            throw ExceptionUtils.toInternalServerErrorException(null, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java
        if (rd == null) {
            String message =
                new org.apache.cxf.common.i18n.Message("RESOURCE_PATH_NOT_FOUND",
                                                       BUNDLE, path).toString();
            LOG.severe(message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java
            if (sc == null) {
                String message =
                    new org.apache.cxf.common.i18n.Message("RESOURCE_DISPATCH_NOT_FOUND",
                                                           BUNDLE, servletContextPath).toString();
                LOG.severe(message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/RequestDispatcherProvider.java`
#### Snippet
```java
        if (LOG.isLoggable(level)) {
            String message =
                new org.apache.cxf.common.i18n.Message("RESPONSE_REDIRECTED_TO",
                    BUNDLE, o.getClass().getName(), attributeName, path).toString();
            LOG.log(level, message);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyReader.java`
#### Snippet
```java

        if (r == null) {
            org.apache.cxf.common.i18n.Message message =
                new org.apache.cxf.common.i18n.Message("NO_MSG_READER", BUNDLE, type);
            LOG.severe(message.toString());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/CachingMessageBodyReader.java`
#### Snippet
```java
        if (r == null) {
            org.apache.cxf.common.i18n.Message message =
                new org.apache.cxf.common.i18n.Message("NO_MSG_READER", BUNDLE, type);
            LOG.severe(message.toString());
            throw ExceptionUtils.toNotAcceptableException(null, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/FormEncodingProvider.java`
#### Snippet
```java
                                                enc,
                                                decode,
                                                (jakarta.servlet.http.HttpServletRequest)servletRequest);
            }
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/PrimitiveTextProvider.java`
#### Snippet
```java
    private static boolean isSupported(Class<?> type, MediaType mt) {
        boolean isPrimitive = InjectionUtils.isPrimitiveOnly(type);
        return (isPrimitive || Enum.class.isAssignableFrom(type) || java.net.URI.class == type
                || java.net.URL.class == type) && mt.isCompatible(MediaType.TEXT_PLAIN_TYPE);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/PrimitiveTextProvider.java`
#### Snippet
```java
        boolean isPrimitive = InjectionUtils.isPrimitiveOnly(type);
        return (isPrimitive || Enum.class.isAssignableFrom(type) || java.net.URI.class == type
                || java.net.URL.class == type) && mt.isCompatible(MediaType.TEXT_PLAIN_TYPE);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/springmvc/SpringViewResolverProvider.java`
#### Snippet
```java
        Level level = isLogRedirects() ? Level.INFO : Level.FINE;
        if (LOG.isLoggable(level)) {
            String message = new org.apache.cxf.common.i18n.Message("RESPONSE_REDIRECTED_TO",
                    BUNDLE, o.getClass().getName(),
                    attributeName, view).toString();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSDefaultFaultOutInterceptor.java`
#### Snippet
```java

        new StaxOutInterceptor().handleMessage(message);
        message.put(org.apache.cxf.message.Message.RESPONSE_CODE, f.getStatusCode());
        NSStack nsStack = new NSStack();
        nsStack.push();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.utils` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
        if (firstObj != null) {
            XmlJavaTypeAdapter adapter =
                org.apache.cxf.jaxrs.utils.JAXBUtils.getAdapter(firstObj.getClass(), anns);
            marshalCollectionMember(JAXBUtils.useAdapter(firstObj, adapter, true),
                                    actualClass, genericType, enc, os, anns, m,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jaxrs.utils` is unnecessary and can be removed
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/JAXBElementProvider.java`
#### Snippet
```java
                response = ((CollectionWrapper)response).getCollectionOrArray(
                                 unmarshaller, theType, type, genericType,
                                 org.apache.cxf.jaxrs.utils.JAXBUtils.getAdapter(theGenericType, anns));
            } else {
                response = checkAdapter(response, type, anns, false);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.jaxb` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/provider/XSLTJaxbProvider.java`
#### Snippet
```java
            return;
        }
        org.apache.cxf.common.jaxb.JAXBUtils.setMinimumEscapeHandler(ms);
        TransformerHandler th;
        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.service.invoker` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSProviderMethodDispatcher.java`
#### Snippet
```java

public class JAXWSProviderMethodDispatcher
    implements org.apache.cxf.service.invoker.MethodDispatcher {

    Method invoke;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServerAsyncResponse.java`
#### Snippet
```java
 *
 */
public class ServerAsyncResponse<T> implements jakarta.xml.ws.Response<T> {
    T value;
    boolean done;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.spi` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java
        super(factory);
    }
    public JAXWSMethodInvoker(jakarta.xml.ws.spi.Invoker i) {
        super(null);
        invoker = i;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java
        exchange.setOneWay(true);
        exchange.setOutMessage(null);
        jakarta.servlet.http.HttpServletResponse httpresp =
            (jakarta.servlet.http.HttpServletResponse)exchange.getInMessage().
                get("HTTP.RESPONSE");
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java
        exchange.setOutMessage(null);
        jakarta.servlet.http.HttpServletResponse httpresp =
            (jakarta.servlet.http.HttpServletResponse)exchange.getInMessage().
                get("HTTP.RESPONSE");
        if (httpresp != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.spi` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JAXWSMethodInvoker.java`
#### Snippet
```java
    public static final String COPY_SOAP_HEADERS_BY_FAULT = "org.apache.cxf.fault.copySoapHeaders";

    jakarta.xml.ws.spi.Invoker invoker;


```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.concurrent` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java

    private List<Annotation> getJaxbAnnos(MessagePartInfo mpi) {
        List<Annotation> list = new java.util.concurrent.CopyOnWriteArrayList<>();
        Annotation[] anns = getMethodParameterAnnotations(mpi);
        if (anns != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
                ParameterizedType ptype = (ParameterizedType)genericType;

                java.lang.reflect.Type[] types = ptype.getActualTypeArguments();
                if (types.length > 0) {
                    if (types[0] instanceof Class) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation.adapters` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/WrapperClassGenerator.java`
#### Snippet
```java
            av.visit("value", helper.getType(helper.getClassCode(adapter.value())));
        }
        if (adapter.type() != jakarta.xml.bind.annotation.adapters.XmlJavaTypeAdapter.DEFAULT.class) {
            av.visit("type", helper.getType(helper.getClassCode(adapter.type())));
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.frontend` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
import org.apache.cxf.service.model.BindingOperationInfo;

public class JaxWsClientProxy extends org.apache.cxf.frontend.ClientProxy implements
    BindingProvider {

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.spi` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/spi/ProviderImpl.java`
#### Snippet
```java
import org.apache.cxf.wsdl.WSDLManager;

public class ProviderImpl extends jakarta.xml.ws.spi.Provider {
    public static final String JAXWS_PROVIDER = ProviderImpl.class.getName();
    protected static final Logger LOG = LogUtils.getL7dLogger(ProviderImpl.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
        if (findDispatchOp && !payloadOPMap.isEmpty()) {
            QName payloadElementName = null;
            if (holder.value instanceof javax.xml.transform.Source) {
                XMLStreamReader reader = null;
                try {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.transform` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java
                XMLStreamReader reader = null;
                try {
                    reader = StaxUtils.createXMLStreamReader((javax.xml.transform.Source)holder.value);
                    Document document = StaxUtils.read(reader);
                    createdSource = new StaxSource(StaxUtils.createXMLStreamReader(document));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/DispatchImpl.java`
#### Snippet
```java

    private Map<String, QName> createPayloadEleOpNameMap(BindingInfo bindingInfo, boolean reverseMapping) {
        Map<String, QName> payloadElementMap = new java.util.HashMap<>();
        // assume a document binding style, which is default according to W3C spec on WSDL
        String bindingStyle = "document";
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/ServiceImpl.java`
#### Snippet
```java
                    throw new WebServiceException(msg.toString());
                }
                if (!seiClass.isAnnotationPresent(jakarta.jws.WebService.class)) {
                    Message msg = new Message("SEI_NO_WEBSERVICE_ANNOTATION", BUNDLE,
                                              seiClass.getCanonicalName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
            if (m == null) {
                Endpoint ep = exchange.getEndpoint();
                m = new org.apache.cxf.message.MessageImpl();
                m.setExchange(exchange);
                m = ep.getBinding().createMessage(m);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/context/WrappedMessageContext.java`
#### Snippet
```java
            if (m == null) {
                Endpoint ep = exchange.getEndpoint();
                m = new org.apache.cxf.message.MessageImpl();
                m.setExchange(exchange);
                m = ep.getBinding().createMessage(m);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://java.sun.com/xml/ns/javaee",
        elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.jaxws.handler.types;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/package-info.java`
#### Snippet
```java
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://java.sun.com/xml/ns/javaee",
        elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.jaxws.handler.types;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/ParamValueType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/ParamValueType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/ParamValueType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/ParamValueType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/ParamValueType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
        this.id = id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * Sets the value of the value property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
        this.value = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java

    @XmlValue
    protected java.lang.String value;
    @XmlAttribute
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * Gets the value of the value property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdStringType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
        return value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DisplayNameType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DisplayNameType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DisplayNameType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DisplayNameType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DisplayNameType.java`
#### Snippet
```java

    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * Sets the value of the value property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
        this.value = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * Gets the value of the value property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
        return value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
    @XmlValue
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    protected java.lang.String value;
    @XmlAttribute
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/CString.java`
#### Snippet
```java
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
        this.id = id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DescriptionType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DescriptionType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DescriptionType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DescriptionType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/DescriptionType.java`
#### Snippet
```java

    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdQNameType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdQNameType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdQNameType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdQNameType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/XsdQNameType.java`
#### Snippet
```java
     * @param id allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String id) {
        this.id = id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/PortComponentHandlerType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/PortComponentHandlerType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/PortComponentHandlerType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/PortComponentHandlerType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/PortComponentHandlerType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;
    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
    protected java.lang.String id;
    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/types/IconType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdQNameType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
            }
            org.w3c.dom.Node nd = body.getFirstChild();
            while (nd != null && !(nd instanceof org.w3c.dom.Element)) {
                nd = nd.getNextSibling();
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdQNameType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
        this.id = v;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdQNameType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/package-info.java`
#### Snippet
```java
 */

@jakarta.xml.bind.annotation.XmlSchema(namespace = "https://jakarta.ee/xml/ns/jakartaee",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED
)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/package-info.java`
#### Snippet
```java

@jakarta.xml.bind.annotation.XmlSchema(namespace = "https://jakarta.ee/xml/ns/jakartaee",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED
)
package org.apache.cxf.jaxws.handler.jakartaee;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdQNameType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdQNameType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DescriptionType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DescriptionType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DescriptionType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/ParamValueType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DescriptionType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DescriptionType.java`
#### Snippet
```java

    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/ParamValueType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/ParamValueType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/ParamValueType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/ParamValueType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * Gets the value of the value property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
        return value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
        this.id = v;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * Sets the value of the value property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
        this.value = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/XsdStringType.java`
#### Snippet
```java

    @XmlValue
    protected java.lang.String value;
    @XmlAttribute
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DisplayNameType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DisplayNameType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DisplayNameType.java`
#### Snippet
```java

    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DisplayNameType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
    @XmlValue
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    protected java.lang.String value;
    @XmlAttribute
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * Gets the value of the value property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getValue() {
        return value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String v) {
        this.id = v;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * Sets the value of the value property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/CString.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setValue(java.lang.String value) {
        this.value = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/DisplayNameType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * Gets the value of the lang property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getLang() {
        return lang;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * Sets the value of the lang property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setLang(java.lang.String value) {
        this.lang = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;
    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/IconType.java`
#### Snippet
```java
    protected java.lang.String id;
    @XmlAttribute(namespace = "http://www.w3.org/XML/1998/namespace")
    protected java.lang.String lang;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/PortComponentHandlerType.java`
#### Snippet
```java
     * Sets the value of the id property.
     *
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/PortComponentHandlerType.java`
#### Snippet
```java
     * @param value allowed object is {@link java.lang.String }
     */
    public void setId(java.lang.String value) {
        this.id = value;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/PortComponentHandlerType.java`
#### Snippet
```java
     * Gets the value of the id property.
     *
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/PortComponentHandlerType.java`
#### Snippet
```java
     * @return possible object is {@link java.lang.String }
     */
    public java.lang.String getId() {
        return id;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/jakartaee/PortComponentHandlerType.java`
#### Snippet
```java
    @XmlJavaTypeAdapter(CollapsedStringAdapter.class)
    @XmlID
    protected java.lang.String id;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.helpers` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/logical/LogicalMessageImpl.java`
#### Snippet
```java
            if (s instanceof DOMSource) {
                DOMSource ds = (DOMSource)s;
                ds.setNode(org.apache.cxf.helpers.DOMUtils.getDomElement(ds.getNode()));
                Node parent = ds.getNode().getParentNode();
                Node next = ds.getNode().getNextSibling();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
import org.apache.cxf.wsdl11.WSDLServiceBuilder;

public class EndpointImpl extends jakarta.xml.ws.Endpoint
    implements InterceptorProvider, Configurable, AutoCloseable {
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.endpoint` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
                server = serverFactory.create();

                org.apache.cxf.endpoint.Endpoint endpoint = getEndpoint();
                if (in != null) {
                    endpoint.getInInterceptors().addAll(in);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws.spi.http` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
        return endpointContext;
    }
    public void publish(jakarta.xml.ws.spi.http.HttpContext context) {
        ServerFactoryBean sf = getServerFactory();
        if (sf.getDestinationFactory() == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.endpoint` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
    }

    org.apache.cxf.endpoint.Endpoint getEndpoint() {
        return getServer(null).getEndpoint();
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
                wsdlLocation = properties.get(Message.WSDL_DESCRIPTION).toString();
            }
            if (properties.containsKey(jakarta.xml.ws.Endpoint.WSDL_PORT)) {
                endpointName = (QName)properties.get(jakarta.xml.ws.Endpoint.WSDL_PORT);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
            }
            if (properties.containsKey(jakarta.xml.ws.Endpoint.WSDL_PORT)) {
                endpointName = (QName)properties.get(jakarta.xml.ws.Endpoint.WSDL_PORT);
            }
            if (properties.containsKey(jakarta.xml.ws.Endpoint.WSDL_SERVICE)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
                endpointName = (QName)properties.get(jakarta.xml.ws.Endpoint.WSDL_PORT);
            }
            if (properties.containsKey(jakarta.xml.ws.Endpoint.WSDL_SERVICE)) {
                serviceName = (QName)properties.get(jakarta.xml.ws.Endpoint.WSDL_SERVICE);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/EndpointImpl.java`
#### Snippet
```java
            }
            if (properties.containsKey(jakarta.xml.ws.Endpoint.WSDL_SERVICE)) {
                serviceName = (QName)properties.get(jakarta.xml.ws.Endpoint.WSDL_SERVICE);
            }
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.binding.soap` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
            }
            try {
                if (message instanceof org.apache.cxf.binding.soap.SoapMessage) {
                    org.apache.cxf.binding.soap.SoapMessage cxfSoapMessage =
                            (org.apache.cxf.binding.soap.SoapMessage)message;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.binding.soap` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
            try {
                if (message instanceof org.apache.cxf.binding.soap.SoapMessage) {
                    org.apache.cxf.binding.soap.SoapMessage cxfSoapMessage =
                            (org.apache.cxf.binding.soap.SoapMessage)message;
                    String cxfNamespace = cxfSoapMessage.getVersion().getNamespace();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.binding.soap` is unnecessary and can be removed
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/interceptors/MessageModeOutInterceptor.java`
#### Snippet
```java
                if (message instanceof org.apache.cxf.binding.soap.SoapMessage) {
                    org.apache.cxf.binding.soap.SoapMessage cxfSoapMessage =
                            (org.apache.cxf.binding.soap.SoapMessage)message;
                    String cxfNamespace = cxfSoapMessage.getVersion().getNamespace();
                    SOAPHeader soapHeader = soapMessage.getSOAPHeader();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/WrappedService.java`
#### Snippet
```java
    }

    public Set<java.util.Map.Entry<String, Object>> entrySet() {
        return wrappedService.entrySet();
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
                        for (InterfaceAddress ia : ni.getInterfaceAddresses()) {
                            // Ignore any virtual interfaces created by/for a VPN connection.
                            if (ia != null && ia.getAddress() instanceof java.net.Inet4Address
                                    && !ia.getAddress().isLoopbackAddress()
                                    && !ni.getDisplayName().startsWith("vnic")) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPConduit.java`
#### Snippet
```java
                                found = true;
                            }
                        } catch (java.net.SocketTimeoutException ex) {
                            if (!found) {
                                throw ex;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.rmi` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
    protected Class<?> getBeanClass(Class<?> exClass) {
        try {
            if (java.rmi.ServerException.class.isAssignableFrom(exClass)
                || java.rmi.RemoteException.class.isAssignableFrom(exClass)
                || "jakarta.xml.ws".equals(PackageUtils.getPackageName(exClass))) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.rmi` is unnecessary, and can be replaced with an import
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceFactoryBean.java`
#### Snippet
```java
        try {
            if (java.rmi.ServerException.class.isAssignableFrom(exClass)
                || java.rmi.RemoteException.class.isAssignableFrom(exClass)
                || "jakarta.xml.ws".equals(PackageUtils.getPackageName(exClass))) {
                return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
     */
    public static String generateUUID() {
        return org.apache.cxf.ws.addressing.ContextUtils.generateUUID();
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
                                                    boolean isProviderContext,
                                                    boolean isOutbound) {
        return org.apache.cxf.ws.addressing.ContextUtils.retrieveMAPs(message, isProviderContext, isOutbound);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMContextUtils.java`
#### Snippet
```java
    public static void storeMAPs(AddressingProperties maps, Message message, boolean isOutbound,
                                 boolean isRequestor) {
        org.apache.cxf.ws.addressing.ContextUtils.storeMAPs(maps, message, isOutbound, isRequestor);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMUtils.java`
#### Snippet
```java

    public static org.apache.cxf.ws.addressing.EndpointReferenceType createAnonymousReference() {
        return createReference(org.apache.cxf.ws.addressing.Names.WSA_ANONYMOUS_ADDRESS);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/RMUtils.java`
#### Snippet
```java

    public static org.apache.cxf.ws.addressing.EndpointReferenceType createNoneReference() {
        return createReference(org.apache.cxf.ws.addressing.Names.WSA_NONE_ADDRESS);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.microprofile.openapi.models.info` is unnecessary, and can be replaced with an import
in `rt/rs/description-microprofile-openapi/src/main/java/org/apache/cxf/jaxrs/microprofile/openapi/OpenApiFeature.java`
#### Snippet
```java
         * The info will be used only if there is no @OpenAPIDefinition annotation is present.
         */
        private org.eclipse.microprofile.openapi.models.info.Info getInfo(final Properties properties) {
            org.eclipse.microprofile.openapi.models.info.Info info = new InfoImpl()
                    .title(getOrFallback(getTitle(), properties, TITLE_PROPERTY))
```

### UnnecessaryFullyQualifiedName
Qualifier `org.eclipse.microprofile.openapi.models.info` is unnecessary, and can be replaced with an import
in `rt/rs/description-microprofile-openapi/src/main/java/org/apache/cxf/jaxrs/microprofile/openapi/OpenApiFeature.java`
#### Snippet
```java
         */
        private org.eclipse.microprofile.openapi.models.info.Info getInfo(final Properties properties) {
            org.eclipse.microprofile.openapi.models.info.Info info = new InfoImpl()
                    .title(getOrFallback(getTitle(), properties, TITLE_PROPERTY))
                    .version(getOrFallback(getVersion(), properties, VERSION_PROPERTY))
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/MC11AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#getKnownElements()
     */
    public QName[] getKnownElements() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/MC11AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/MC11AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/MC11AssertionBuilder.java`
#### Snippet
```java
    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
    public Assertion build(Element elem, AssertionBuilderFactory factory) throws IllegalArgumentException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RSPAssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#getKnownElements()
     */
    public QName[] getKnownElements() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RSPAssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RSPAssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RSPAssertionBuilder.java`
#### Snippet
```java
    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
    public Assertion build(Element elem, AssertionBuilderFactory factory) throws IllegalArgumentException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM12AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM12AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM12AssertionBuilder.java`
#### Snippet
```java
    /**
     * @see org.apache.neethi.builders.AssertionBuilder#build(org.w3c.dom.Element,
     *  org.apache.neethi.AssertionBuilderFactory)
     */
    public Assertion build(Element elem, AssertionBuilderFactory factory) throws IllegalArgumentException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.neethi.builders` is unnecessary and can be removed
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/policy/RM12AssertionBuilder.java`
#### Snippet
```java

    /**
     * @see org.apache.neethi.builders.AssertionBuilder#getKnownElements()
     */
    public QName[] getKnownElements() {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502/package-info.java`
#### Snippet
```java
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://schemas.xmlsoap.org/ws/2005/02/rm",
                                               prefix = "wsrm") })
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502/package-info.java`
#### Snippet
```java
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://schemas.xmlsoap.org/ws/2005/02/rm",
                                               prefix = "wsrm") })
package org.apache.cxf.ws.rm.v200502;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200702/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://docs.oasis-open.org/ws-rx/wsrm/200702",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200702/package-info.java`
#### Snippet
```java
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://docs.oasis-open.org/ws-rx/wsrm/200702",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://docs.oasis-open.org/ws-rx/wsrm/200702",
                                               prefix = "wsrm") })
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200702/package-info.java`
#### Snippet
```java
    namespace = "http://docs.oasis-open.org/ws-rx/wsrm/200702",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://docs.oasis-open.org/ws-rx/wsrm/200702",
                                               prefix = "wsrm") })
package org.apache.cxf.ws.rm.v200702;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502wsa15/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502wsa15/package-info.java`
#### Snippet
```java
@jakarta.xml.bind.annotation.XmlSchema(
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://schemas.xmlsoap.org/ws/2005/02/rm",
                                               prefix = "wsrm") })
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/v200502wsa15/package-info.java`
#### Snippet
```java
    namespace = "http://schemas.xmlsoap.org/ws/2005/02/rm",
    elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED,
    xmlns = { @jakarta.xml.bind.annotation.XmlNs(namespaceURI = "http://schemas.xmlsoap.org/ws/2005/02/rm",
                                               prefix = "wsrm") })
package org.apache.cxf.ws.rm.v200502wsa15;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataReaderImpl.java`
#### Snippet
```java
            }
            return um;
        } catch (jakarta.xml.bind.UnmarshalException ex) {
            throw new Fault(new Message("UNMARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage()), ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/attachment/JAXBAttachmentUnmarshaller.java`
#### Snippet
```java
            bos.close();
        } catch (IOException e) {
            throw new Fault(new org.apache.cxf.common.i18n.Message("ATTACHMENT_READ_ERROR", LOG), e);
        }
        return bos.toByteArray();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/io/DataWriterImpl.java`
#### Snippet
```java
                                                            (JAXBAttachmentMarshaller)atmarsh));
            }
        } catch (jakarta.xml.bind.MarshalException ex) {
            Message faultMessage = new Message("MARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage());
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/AddressingWSDLExtensionLoader.java`
#### Snippet
```java
        WSDLManager manager = b.getExtension(WSDLManager.class);

        createExtensor(manager, javax.wsdl.Binding.class,
                       org.apache.cxf.ws.addressing.wsdl.UsingAddressing.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
            if (!m.isAnnotationPresent(XmlTransient.class)
                && m.getDeclaringClass().equals(Throwable.class)) {
                JAXBBeanInfo beanInfo = getBeanInfo(java.lang.String.class);
                XmlSchemaElement exEle = new XmlSchemaElement(schema, false);
                exEle.setName("message");
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
                                            + inMessage.getExchange().get(Message.RESPONSE_CODE));
                    partialResponse.setContent(Exception.class, exception);
                    partialResponse.put(org.apache.cxf.message.Message.PROTOCOL_HEADERS,
                                        inMessage.get(Message.PROTOCOL_HEADERS));
                    partialResponse.put(org.apache.cxf.message.Message.ENCODING,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
                    partialResponse.put(org.apache.cxf.message.Message.PROTOCOL_HEADERS,
                                        inMessage.get(Message.PROTOCOL_HEADERS));
                    partialResponse.put(org.apache.cxf.message.Message.ENCODING,
                                        inMessage.get(Message.ENCODING));
                    partialResponse.put(ContextUtils.ACTION, inMessage.get(ContextUtils.ACTION));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        if (!isList(type)) {
                            if (compType.isPrimitive()) {
                                o = java.lang.reflect.Array.newInstance(compType, ret.size());
                                for (int x = 0; x < ret.size(); x++) {
                                    Array.set(o, x, ret.get(x));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        if (!isList(type)) {
                            if (compType.isPrimitive()) {
                                o = java.lang.reflect.Array.newInstance(compType, ret.size());
                                for (int x = 0; x < ret.size(); x++) {
                                    Array.set(o, x, ret.get(x));
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                //the namespace is missing when marshal the xsd:QName type
                //to the OutputStream directly
                java.io.StringWriter sw = new java.io.StringWriter();
                StreamResult s1 = new StreamResult(sw);
                bridge.marshal(elValue, s1);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                //the namespace is missing when marshal the xsd:QName type
                //to the OutputStream directly
                java.io.StringWriter sw = new java.io.StringWriter();
                StreamResult s1 = new StreamResult(sw);
                bridge.marshal(elValue, s1);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                throw new Fault(new Message("UNKNOWN_SOURCE", LOG, source.getClass().getName()));
            }
        } catch (jakarta.xml.bind.MarshalException ex) {
            Message faultMessage = new Message("MARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage());
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                throw (Fault)ex;
            }
            if (ex instanceof jakarta.xml.bind.UnmarshalException) {
                jakarta.xml.bind.UnmarshalException unmarshalEx = (jakarta.xml.bind.UnmarshalException)ex;
                if (unmarshalEx.getLinkedException() != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            }
            if (ex instanceof jakarta.xml.bind.UnmarshalException) {
                jakarta.xml.bind.UnmarshalException unmarshalEx = (jakarta.xml.bind.UnmarshalException)ex;
                if (unmarshalEx.getLinkedException() != null) {
                    throw new Fault(new Message("UNMARSHAL_ERROR", LOG,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            }
            if (ex instanceof jakarta.xml.bind.UnmarshalException) {
                jakarta.xml.bind.UnmarshalException unmarshalEx = (jakarta.xml.bind.UnmarshalException)ex;
                if (unmarshalEx.getLinkedException() != null) {
                    throw new Fault(new Message("UNMARSHAL_ERROR", LOG,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                throw new Fault(new Message("UNKNOWN_SOURCE", LOG, source.getClass().getName()));
            }
        } catch (jakarta.xml.bind.MarshalException ex) {
            Message faultMessage = new Message("MARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage());
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        } catch (Fault ex) {
            throw ex;
        } catch (jakarta.xml.bind.MarshalException ex) {
            throw new Fault(new Message("UNMARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage()), ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
                        o = createSet(part, ret);
                    } else if (clazz.getComponentType().isPrimitive()) {
                        o = java.lang.reflect.Array.newInstance(clazz.getComponentType(), ret.size());
                        for (int x = 0; x < ret.size(); x++) {
                            Array.set(o, x, ret.get(x));
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            marshaller.setProperty(Marshaller.JAXB_FRAGMENT, true);
            marshaller.setProperty(Marshaller.JAXB_FORMATTED_OUTPUT, false);
        } catch (jakarta.xml.bind.PropertyException e) {
            // intentionally empty.
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind` is unnecessary, and can be replaced with an import
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
        } catch (Fault ex) {
            throw ex;
        } catch (jakarta.xml.bind.MarshalException ex) {
            Message faultMessage = new Message("MARSHAL_ERROR", LOG, ex.getLinkedException()
                .getMessage());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security.cert` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/realm/CertConstraintsParser.java`
#### Snippet
```java
     */
    public boolean matches(
        final java.security.cert.X509Certificate cert
    ) {
        if (!subjectDNPatterns.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.xml.security.stax.securityToken` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/TokenProviderUtils.java`
#### Snippet
```java
                    || WSSecurityEventConstants.SignedElement
                        == incomingEvent.getSecurityEventType()) {
                    org.apache.xml.security.stax.securityToken.SecurityToken token =
                        ((AbstractSecuredElementSecurityEvent)incomingEvent).getSecurityToken();
                    try {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/ActAsAttributeStatementProvider.java`
#### Snippet
```java
            // Check for other ActAs attributes here + add them in
            if (wrapper.getSaml2() != null) {
                for (org.opensaml.saml.saml2.core.AttributeStatement attributeStatement
                    : wrapper.getSaml2().getAttributeStatements()) {
                    for (org.opensaml.saml.saml2.core.Attribute attribute : attributeStatement.getAttributes()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java

        if (assertion.getSaml1() != null) {
            org.opensaml.saml.saml1.core.Assertion saml1Assertion = assertion.getSaml1();
            saml1Assertion.setIssueInstant(Instant.now());

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            saml1Assertion.setIssueInstant(Instant.now());

            org.opensaml.saml.saml1.core.Conditions saml1Conditions =
                SAML1ComponentBuilder.createSamlv1Conditions(conditions);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            saml1Assertion.setConditions(saml1Conditions);
        } else {
            org.opensaml.saml.saml2.core.Assertion saml2Assertion = assertion.getSaml2();
            saml2Assertion.setIssueInstant(Instant.now());

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            saml2Assertion.setIssueInstant(Instant.now());

            org.opensaml.saml.saml2.core.Conditions saml2Conditions =
                SAML2ComponentBuilder.createConditions(conditions);

```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
    private String createNewId(SamlAssertionWrapper assertion) {
        if (assertion.getSaml1() != null) {
            org.opensaml.saml.saml1.core.Assertion saml1Assertion = assertion.getSaml1();
            String oldId = saml1Assertion.getID();
            saml1Assertion.setID(IDGenerator.generateID("_"));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/renewer/SAMLTokenRenewer.java`
#### Snippet
```java
            return oldId;
        }
        org.opensaml.saml.saml2.core.Assertion saml2Assertion = assertion.getSaml2();
        String oldId = saml2Assertion.getID();
        saml2Assertion.setID(IDGenerator.generateID("_"));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/ActAsAttributeStatementProvider.java`
#### Snippet
```java
                for (org.opensaml.saml.saml2.core.AttributeStatement attributeStatement
                    : wrapper.getSaml2().getAttributeStatements()) {
                    for (org.opensaml.saml.saml2.core.Attribute attribute : attributeStatement.getAttributes()) {
                        if ("ActAs".equals(attribute.getName())) {
                            for (XMLObject attributeValue : attribute.getAttributeValues()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/ActAsAttributeStatementProvider.java`
#### Snippet
```java
                }
            } else if (wrapper.getSaml1() != null) {
                for (org.opensaml.saml.saml1.core.AttributeStatement attributeStatement
                    : wrapper.getSaml1().getAttributeStatements()) {
                    for (org.opensaml.saml.saml1.core.Attribute attribute : attributeStatement.getAttributes()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/provider/ActAsAttributeStatementProvider.java`
#### Snippet
```java
                for (org.opensaml.saml.saml1.core.AttributeStatement attributeStatement
                    : wrapper.getSaml1().getAttributeStatements()) {
                    for (org.opensaml.saml.saml1.core.Attribute attribute : attributeStatement.getAttributes()) {
                        if ("ActAs".equals(attribute.getAttributeName())) {
                            for (XMLObject attributeValue : attribute.getAttributeValues()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ProcessedClaimCollection.java`
#### Snippet
```java
 * This holds a collection of Claims that have been processed by a ClaimsHandler implementation
 */
public class ProcessedClaimCollection extends java.util.ArrayList<ProcessedClaim> {

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/jwt/JWTTokenValidator.java`
#### Snippet
```java
        if (token instanceof Element) {
            Element tokenEl = (Element)token;
            if (tokenEl.getFirstChild().getNodeType() == org.w3c.dom.Node.TEXT_NODE) {
                try {
                    JwsJwtCompactConsumer jwtConsumer = new JwsJwtCompactConsumer(tokenEl.getTextContent());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
            for (AudienceRestrictionCondition restriction
                : assertion.getSaml1().getConditions().getAudienceRestrictionConditions()) {
                for (org.opensaml.saml.saml1.core.Audience audience : restriction.getAudiences()) {
                    addresses.add(audience.getUri());
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
            }
        } else if (assertion.getSaml2() != null) {
            for (org.opensaml.saml.saml2.core.AudienceRestriction restriction
                : assertion.getSaml2().getConditions().getAudienceRestrictions()) {
                for (org.opensaml.saml.saml2.core.Audience audience : restriction.getAudiences()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/delegation/SAMLDelegationHandler.java`
#### Snippet
```java
            for (org.opensaml.saml.saml2.core.AudienceRestriction restriction
                : assertion.getSaml2().getConditions().getAudienceRestrictions()) {
                for (org.opensaml.saml.saml2.core.Audience audience : restriction.getAudiences()) {
                    addresses.add(audience.getAudienceURI());
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java


    protected List<ProcessedClaim> parseClaimsInAssertion(org.opensaml.saml.saml1.core.Assertion assertion) {
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java

    protected List<ProcessedClaim> parseClaimsInAssertion(org.opensaml.saml.saml1.core.Assertion assertion) {
        List<org.opensaml.saml.saml1.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
        ProcessedClaimCollection collection = new ProcessedClaimCollection();

        for (org.opensaml.saml.saml1.core.AttributeStatement statement : attributeStatements) {
            if (LOG.isLoggable(Level.FINEST)) {
                LOG.finest("parsing statement: " + statement.getElementQName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
            }

            List<org.opensaml.saml.saml1.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml1.core.Attribute attribute : attributes) {
                if (LOG.isLoggable(Level.FINEST)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml1.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java

            List<org.opensaml.saml.saml1.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml1.core.Attribute attribute : attributes) {
                if (LOG.isLoggable(Level.FINEST)) {
                    LOG.finest("parsing attribute: " + attribute.getAttributeName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
    }

    protected List<ProcessedClaim> parseClaimsInAssertion(org.opensaml.saml.saml2.core.Assertion assertion) {
        List<org.opensaml.saml.saml2.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java

    protected List<ProcessedClaim> parseClaimsInAssertion(org.opensaml.saml.saml2.core.Assertion assertion) {
        List<org.opensaml.saml.saml2.core.AttributeStatement> attributeStatements =
            assertion.getAttributeStatements();
        if (attributeStatements == null || attributeStatements.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
        List<ProcessedClaim> collection = new ArrayList<>();

        for (org.opensaml.saml.saml2.core.AttributeStatement statement : attributeStatements) {
            if (LOG.isLoggable(Level.FINEST)) {
                LOG.finest("parsing statement: " + statement.getElementQName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
                LOG.finest("parsing statement: " + statement.getElementQName());
            }
            List<org.opensaml.saml.saml2.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml2.core.Attribute attribute : attributes) {
                if (LOG.isLoggable(Level.FINEST)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/claims/ClaimsManager.java`
#### Snippet
```java
            }
            List<org.opensaml.saml.saml2.core.Attribute> attributes = statement.getAttributes();
            for (org.opensaml.saml.saml2.core.Attribute attribute : attributes) {
                if (LOG.isLoggable(Level.FINEST)) {
                    LOG.finest("parsing attribute: " + attribute.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.addressing` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/interceptor/SCTInInterceptor.java`
#### Snippet
```java
            && (s.startsWith(STSUtils.WST_NS_05_02)
                || s.startsWith(STSUtils.WST_NS_05_12))) {
            message.put(org.apache.cxf.ws.addressing.MAPAggregator.ACTION_VERIFIED, Boolean.TRUE);
        }
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.xml.security.stax.securityToken` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/TokenIssueOperation.java`
#### Snippet
```java
        // Now try steaming results
        try {
            org.apache.xml.security.stax.securityToken.SecurityToken securityToken =
                findInboundSecurityToken(WSSecurityEventConstants.SAML_TOKEN, messageContext);
            if (securityToken instanceof SamlSecurityToken
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.xml.security.stax.securityToken` is unnecessary, and can be replaced with an import
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/operation/AbstractOperation.java`
#### Snippet
```java
    }

    protected static org.apache.xml.security.stax.securityToken.SecurityToken
    findInboundSecurityToken(SecurityEventConstants.Event event,
                             Map<String, Object> messageContext) throws XMLSecurityException {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.mozilla.javascript.xmlimpl` is unnecessary, and can be replaced with an import
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/AbstractDOMProvider.java`
#### Snippet
```java
            }
            if (jsResp instanceof XMLObject) {
                jsResp = org.mozilla.javascript.xmlimpl.XMLLibImpl.toDomNode(jsResp);
            }
            if (jsResp instanceof Node) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
            }

            javax.xml.stream.XMLOutputFactory fact = javax.xml.stream.XMLOutputFactory.newInstance();
            XMLStreamWriter writer =
                new PrettyPrintXMLStreamWriter(fact.createXMLStreamWriter(pw), 2, getIndentLevel(parent));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/JAXBExtensionHelper.java`
#### Snippet
```java
            }

            javax.xml.stream.XMLOutputFactory fact = javax.xml.stream.XMLOutputFactory.newInstance();
            XMLStreamWriter writer =
                new PrettyPrintXMLStreamWriter(fact.createXMLStreamWriter(pw), 2, getIndentLevel(parent));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/PartialWSDLProcessor.java`
#### Snippet
```java
        return address;
    }
    public static javax.wsdl.Service doAppendService(Definition wsdlDefinition,
                                                     String existPortName, ExtensionRegistry
                                                     extReg, Binding binding) throws Exception {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/PartialWSDLProcessor.java`
#### Snippet
```java
                                                     String existPortName, ExtensionRegistry
                                                     extReg, Binding binding) throws Exception {
        javax.wsdl.Service wsdlService = wsdlDefinition.createService();
        wsdlService.setQName(new QName(wsdlDefinition.getTargetNamespace(), existPortName + serviceName));
        Port port = wsdlDefinition.createPort();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        // register types that are not of wsdl4j's default attribute type QName
        QName qn = new QName("http://www.w3.org/2006/05/addressing/wsdl", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        QName qn = new QName("http://www.w3.org/2006/05/addressing/wsdl", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2007/05/addressing/metadata", "Action");
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2007/05/addressing/metadata", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2007/05/addressing/metadata", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        qn = new QName("http://www.w3.org/2007/05/addressing/metadata", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2005/02/addressing/wsdl", "Action");
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2005/02/addressing/wsdl", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
        qn = new QName("http://www.w3.org/2005/02/addressing/wsdl", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        qn = new QName("http://www.w3.org/2005/02/addressing/wsdl", "Action");
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLManagerImpl.java`
#### Snippet
```java
        extreg.registerExtensionAttributeType(javax.wsdl.Input.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Output.class, qn, AttributeExtensible.STRING_TYPE);
        extreg.registerExtensionAttributeType(javax.wsdl.Fault.class, qn, AttributeExtensible.STRING_TYPE);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
            int schemaCount = 1;
            for (Object obj : typesElement.getExtensibilityElements()) {
                org.w3c.dom.Element schemaElem = null;
                if (obj instanceof Schema) {
                    Schema schema = (Schema)obj;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
                    schemaElem = schema.getElement();
                } else if (obj instanceof UnknownExtensibilityElement) {
                    org.w3c.dom.Element elem = ((UnknownExtensibilityElement)obj).getElement();
                    synchronized (elem.getOwnerDocument()) {
                        if ("schema".equals(elem.getLocalName())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
                                    Attr attr =
                                        schemaElem.getOwnerDocument()
                                            .createAttributeNS(javax.xml.XMLConstants.XMLNS_ATTRIBUTE_NS_URI,
                                                               "xmlns");
                                    attr.setValue(ns);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SchemaUtil.java`
#### Snippet
```java
                                }
                            } else if (!schemaElem.hasAttribute("xmlns:" + prefix)) {
                                String namespace = javax.xml.XMLConstants.XMLNS_ATTRIBUTE_NS_URI;
                                Attr attr =
                                    schemaElem.getOwnerDocument().createAttributeNS(namespace,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/package-info.java`
#### Snippet
```java
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java

        List<ServiceInfo> serviceList = new ArrayList<>();
        for (java.util.Iterator<QName> ite = CastUtils.cast(d.getServices().keySet().iterator()); ite
            .hasNext();) {
            QName qn = ite.next();
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl.extensions` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/WSDLServiceBuilder.java`
#### Snippet
```java

    private void copyExtensionAttributes(AbstractPropertiesHolder info,
                                         javax.wsdl.extensions.AttributeExtensible ae) {
        Map<QName, Object> attrs = CastUtils.cast(ae.getExtensionAttributes());
        if (!attrs.isEmpty()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/bare/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.bare;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/bare/package-info.java`
#### Snippet
```java
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.bare;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.springframework.core.io` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/test/AbstractCXFSpringTest.java`
#### Snippet
```java
    /**
     * Return an array of resource specifications.
     * @see org.springframework.core.io.DefaultResourceLoader for the syntax.
     * @return array of resource specifications.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/book/package-info.java`
#### Snippet
```java


@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://book.acme.com",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.book;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/book/package-info.java`
#### Snippet
```java

@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://book.acme.com",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.book;

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/wraped/package-info.java`
#### Snippet
```java
 * under the License.
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.wraped;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/customer/wraped/package-info.java`
#### Snippet
```java
 */
@jakarta.xml.bind.annotation.XmlSchema(namespace = "http://cxf.apache.org/jra",
                                     elementFormDefault = jakarta.xml.bind.annotation.XmlNsForm.QUALIFIED)
package org.apache.cxf.customer.wraped;

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/authservice/AuthService.java`
#### Snippet
```java
    );

    java.lang.String[] getRolesAsArray(String sid);

    @WebMethod(operationName = "authenticate1")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/authservice/AuthService.java`
#### Snippet
```java
public interface AuthService {

    java.util.List<java.lang.String> getRoles(
                                              String sid
    );
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/authservice/AuthService.java`
#### Snippet
```java
public interface AuthService {

    java.util.List<java.lang.String> getRoles(
                                              String sid
    );
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterNoWsdl.java`
#### Snippet
```java
    @WebResult(targetNamespace = "http://cxf.apache.org/greeter_control/types", name = "responseType")
    @WebMethod(operationName = "greetMe")
    java.lang.String greetMe(
        @WebParam(targetNamespace = "http://cxf.apache.org/greeter_control/types", name = "requestType")
        java.lang.String requestType
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterNoWsdl.java`
#### Snippet
```java
    java.lang.String greetMe(
        @WebParam(targetNamespace = "http://cxf.apache.org/greeter_control/types", name = "requestType")
        java.lang.String requestType
    );

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterNoWsdl.java`
#### Snippet
```java
    @WebResult(targetNamespace = "http://cxf.apache.org/greeter_control/types", name = "responseType")
    @WebMethod(operationName = "sayHi")
    java.lang.String sayHi();

    @ResponseWrapper(targetNamespace = "http://cxf.apache.org/greeter_control/types",
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/cxf/greeter_control/GreeterNoWsdl.java`
#### Snippet
```java
    void greetMeOneWay(
        @WebParam(targetNamespace = "http://cxf.apache.org/greeter_control/types", name = "requestType")
        java.lang.String requestType
    );

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/cxf/test/XPathAssert.java`
#### Snippet
```java
        try {
            xpathFactory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/mixed/GreeterImpl.java`
#### Snippet
```java
import org.apache.hello_world_xml_http.mixed.types.SayHiResponse;

@jakarta.jws.WebService(serviceName = "XMLService",
                      portName = "XMLPort",
                      endpointInterface = "org.apache.hello_world_xml_http.mixed.Greeter",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/mixed/GreeterImpl.java`
#### Snippet
```java
                      wsdlLocation = "testutils/hello_world_xml_mixed.wsdl")

@jakarta.xml.ws.BindingType(value = "http://cxf.apache.org/bindings/xformat")

public class GreeterImpl implements Greeter {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/wrapped/GreeterImpl.java`
#### Snippet
```java
import org.apache.hello_world_xml_http.wrapped.types.FaultDetail;

@jakarta.jws.WebService(serviceName = "XMLService",
                      portName = "XMLPort",
                      endpointInterface = "org.apache.hello_world_xml_http.wrapped.Greeter",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/wrapped/GreeterImpl.java`
#### Snippet
```java
                      wsdlLocation = "testutils/hello_world_xml_wrapped.wsdl")

@jakarta.xml.ws.BindingType(value = "http://cxf.apache.org/bindings/xformat")

public class GreeterImpl implements Greeter {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/GreeterEndpointInterface.java`
#### Snippet
```java
package org.apache.hello_world_soap_http;

@jakarta.jws.WebService(name = "Greeter",
                      serviceName = "SOAPService",
                      targetNamespace = "http://apache.org/hello_world_soap_http",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/HelloImpl.java`
#### Snippet
```java
 */
package org.apache.hello_world_soap_http;
@jakarta.jws.WebService(name = "Hello",
                      serviceName = "HelloService",
                      portName = "HelloPort",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/bare/GreeterImpl.java`
#### Snippet
```java
import org.apache.hello_world_xml_http.bare.types.MyComplexStructType;

@jakarta.jws.WebService(serviceName = "XMLService",
                portName = "XMLPort",
                endpointInterface = "org.apache.hello_world_xml_http.bare.Greeter",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/bare/GreeterImpl.java`
#### Snippet
```java
                targetNamespace = "http://apache.org/hello_world_xml_http/bare")

@jakarta.xml.ws.BindingType(value = "http://cxf.apache.org/bindings/xformat")

public class GreeterImpl implements Greeter {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/wrapped/GreeterFaultImpl.java`
#### Snippet
```java


@jakarta.jws.WebService(serviceName = "XMLService",
                      portName = "XMLFaultPort",
                      endpointInterface = "org.apache.hello_world_xml_http.wrapped.Greeter",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.ws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_xml_http/wrapped/GreeterFaultImpl.java`
#### Snippet
```java
                      wsdlLocation = "testutils/hello_world_xml_wrapped.wsdl")

@jakarta.xml.ws.BindingType(value = "http://cxf.apache.org/bindings/xformat")

public class GreeterFaultImpl implements Greeter {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/HelloWrongAnnotation.java`
#### Snippet
```java
import jakarta.jws.soap.SOAPBinding;

@jakarta.jws.WebService(name = "HelloWrongAnnotation",
                      serviceName = "HelloService",
                      portName = "HelloPort",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterNoOverloadImpl.java`
#### Snippet
```java
import jakarta.xml.ws.WebServiceContext;

@jakarta.jws.WebService(name = "Greeter", serviceName = "SOAPService",
                      targetNamespace = "http://apache.org/hello_world_soap_http")
@HandlerChain(name = "TestHandlerChain", file = "handlers.xml")
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws.soap` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/hello_world_soap_http/Hello.java`
#### Snippet
```java

@WebService(name = "Hello", targetNamespace = "http://apache.org/hello_world_soap_http")
@SOAPBinding(style = jakarta.jws.soap.SOAPBinding.Style.RPC, use = jakarta.jws.soap.SOAPBinding.Use.LITERAL)
public interface Hello {
    @WebMethod(operationName = "sayHi")
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws.soap` is unnecessary and can be removed
in `testutils/src/main/java/org/apache/hello_world_soap_http/Hello.java`
#### Snippet
```java

@WebService(name = "Hello", targetNamespace = "http://apache.org/hello_world_soap_http")
@SOAPBinding(style = jakarta.jws.soap.SOAPBinding.Style.RPC, use = jakarta.jws.soap.SOAPBinding.Use.LITERAL)
public interface Hello {
    @WebMethod(operationName = "sayHi")
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnnotatedGreeterImpl.java`
#### Snippet
```java
import jakarta.xml.ws.WebServiceContext;

@jakarta.jws.WebService(name = "Greeter", serviceName = "SOAPService",
                      targetNamespace = "http://apache.org/hello_world_soap_http")
@HandlerChain(name = "TestHandlerChain", file = "handlers.xml")
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java


@jakarta.jws.WebService(endpointInterface = "org.apache.hello_world_soap_http.Greeter",
                      wsdlLocation = "tetutils/hello_world.wsdl")
public class AnotherDerivedGreeterImpl implements GreeterEndpointInterface {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/AnotherDerivedGreeterImpl.java`
#### Snippet
```java
    }

    @jakarta.jws.WebMethod(operationName = "sayHi")
    /*
     * @jakarta.jws.WebResult(name="responseType",
```

### UnnecessaryFullyQualifiedName
Qualifier `org.codehaus.plexus.util` is unnecessary, and can be replaced with an import
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java
            Collections.addAll(exList, excludes);
        }
        Collections.addAll(exList, org.codehaus.plexus.util.FileUtils.getDefaultExcludes());

        String inc = getIncludeExcludeString(includes);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.codehaus.plexus.util` is unnecessary, and can be replaced with an import
in `maven-plugins/wsdl-validator-plugin/src/main/java/org/apache/cxf/maven_plugin/WSDLValidatorMojo.java`
#### Snippet
```java

        try {
            return org.codehaus.plexus.util.FileUtils.getFiles(dir, inc, ex);
        } catch (IOException exc) {
            throw new MojoExecutionException(exc.getMessage(), exc);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java
    }

    @jakarta.jws.WebMethod(operationName = "sayHi")
    /*
     * @jakarta.jws.WebResult(name="responseType",
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `testutils/src/main/java/org/apache/hello_world_soap_http/DerivedGreeterImpl.java`
#### Snippet
```java


@jakarta.jws.WebService(name = "DerivedGreeter", serviceName = "DerivedGreeterService",
                      targetNamespace = "http://apache.org/hello_world_soap_http")
public class DerivedGreeterImpl implements Greeter {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary and can be removed
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
        ExtensibilityElement extension;
        try {
            extension = reg.createExtension(javax.wsdl.Types.class, WSDLConstants.QNAME_SCHEMA);
        } catch (WSDLException e) {
            throw new RuntimeException("Problem creating schema implementation", e);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.service.factory` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
 */
//CHECKSTYLE:OFF:NCSS    -   This class is just huge and complex
public class ReflectionServiceFactoryBean extends org.apache.cxf.service.factory.AbstractServiceFactoryBean {

    public static final String ENDPOINT_CLASS = "endpoint.class";
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java
                    Type type = (Type)mpi.getProperty(GENERIC_TYPE);

                    if (!(type instanceof java.lang.reflect.ParameterizedType)
                        && el.getSchemaTypeName() == null && el.getSchemaType() == null) {
                        max = Long.MAX_VALUE;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.rmi` is unnecessary, and can be replaced with an import
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl/service/factory/ReflectionServiceFactoryBean.java`
#### Snippet
```java

    protected Class<?> getBeanClass(Class<?> exClass) {
        if (java.rmi.RemoteException.class.isAssignableFrom(exClass)) {
            return null;
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA.TypeCodePackage` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaTypeMap.java`
#### Snippet
```java
        try {
            repId = tc.id();
        } catch (org.omg.CORBA.TypeCodePackage.BadKind ex) {
            // No id has been set.
            return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
                    if (arguments != null) {
                        for (int i = 0; i < arguments.length; ++i) {
                            if (list.item(i).flags() != org.omg.CORBA.ARG_IN.value) {
                                arguments[i].getObject().setIntoAny(list.item(i).value(),
                                                                    arguments[i], true);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaServerConduit.java`
#### Snippet
```java
            }

        } catch (java.lang.Exception ex) {
            throw new CorbaBindingException("Exception during buildRequestResult", ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaBindingFactory.java`
#### Snippet
```java

    public Set<String> getUriPrefixes() {
        Set<String> uriPrefixes = new java.util.HashSet<>();
        uriPrefixes.add("IOR");
        uriPrefixes.add("ior");
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java

    void registerCXFExtensors(WSDLManager manager) {
        createExtensor(manager, javax.wsdl.Binding.class,
                             org.apache.cxf.binding.corba.wsdl.BindingType.class);
        createExtensor(manager, javax.wsdl.BindingOperation.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.Binding.class,
                             org.apache.cxf.binding.corba.wsdl.BindingType.class);
        createExtensor(manager, javax.wsdl.BindingOperation.class,
                             org.apache.cxf.binding.corba.wsdl.OperationType.class);
        createExtensor(manager, javax.wsdl.Definition.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.BindingOperation.class,
                             org.apache.cxf.binding.corba.wsdl.OperationType.class);
        createExtensor(manager, javax.wsdl.Definition.class,
                             org.apache.cxf.binding.corba.wsdl.TypeMappingType.class);
        createExtensor(manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.Definition.class,
                             org.apache.cxf.binding.corba.wsdl.TypeMappingType.class);
        createExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.AddressType.class);
        createExtensor(manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.AddressType.class);
        createExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.PolicyType.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
    }
    void registerYokoCompatibleExtensors(WSDLManager manager) {
        createCompatExtensor(manager, javax.wsdl.Binding.class,
                             org.apache.cxf.binding.corba.wsdl.BindingType.class);
        createCompatExtensor(manager, javax.wsdl.BindingOperation.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createCompatExtensor(manager, javax.wsdl.Binding.class,
                             org.apache.cxf.binding.corba.wsdl.BindingType.class);
        createCompatExtensor(manager, javax.wsdl.BindingOperation.class,
                             org.apache.cxf.binding.corba.wsdl.OperationType.class);
        createCompatExtensor(manager, javax.wsdl.Definition.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createCompatExtensor(manager, javax.wsdl.BindingOperation.class,
                             org.apache.cxf.binding.corba.wsdl.OperationType.class);
        createCompatExtensor(manager, javax.wsdl.Definition.class,
                             org.apache.cxf.binding.corba.wsdl.TypeMappingType.class);
        createCompatExtensor(manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createCompatExtensor(manager, javax.wsdl.Definition.class,
                             org.apache.cxf.binding.corba.wsdl.TypeMappingType.class);
        createCompatExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.AddressType.class);
        createCompatExtensor(manager, javax.wsdl.Port.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.wsdl` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/wsdl/WSDLExtensionRegister.java`
#### Snippet
```java
        createCompatExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.AddressType.class);
        createCompatExtensor(manager, javax.wsdl.Port.class,
                             org.apache.cxf.binding.corba.wsdl.PolicyType.class);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
        }
        case TCKind._tk_ulonglong:
            data = java.math.BigInteger.valueOf(any.extract_ulonglong()).toString();
            break;
        case TCKind._tk_float:
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_ulonglong:
            data = ((java.math.BigInteger)value).toString();
            break;
        case TCKind._tk_float:
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
                break;
            case TCKind._tk_ulonglong:
                any.insert_ulonglong(((java.math.BigInteger)value).longValue());
                break;
            case TCKind._tk_float:
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaPrimitiveHandler.java`
#### Snippet
```java
            break;
        case TCKind._tk_ulonglong:
            obj = new java.math.BigInteger(data);
            break;
        case TCKind._tk_float:
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
    public ORB getORB(List<String> orbArgs,
                      String location,
                      java.util.Properties props) {
        // See if an ORB has already been created for the given address. If so,
        // we'll simply use it
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java

    public void activate() {
        java.util.Properties props = new java.util.Properties();
        Properties configSpecifiedOrbProperties = orbConfig.getOrbProperties();
        props.putAll(configSpecifiedOrbProperties);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java

    public void activate() {
        java.util.Properties props = new java.util.Properties();
        Properties configSpecifiedOrbProperties = orbConfig.getOrbProperties();
        props.putAll(configSpecifiedOrbProperties);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
        try {
            addressURI = new URI(location);
        } catch (java.net.URISyntaxException ex) {
            throw new CorbaBindingException(
                    "Unable to create ORB with address " + address);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.PortableServer.POAPackage` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
            try {
                bindingPOA = rootPOA.find_POA(poaName, false);
            } catch (org.omg.PortableServer.POAPackage.AdapterNonExistent ex) {
                // do nothing
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.PortableServer.POAPackage` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaDestination.java`
#### Snippet
```java
                try {
                    bindingPOA.activate_object_with_id(objectId, servant);
                } catch (org.omg.PortableServer.POAPackage.ObjectAlreadyActive ex) {
                    if (!isPersistent) {
                        throw new CorbaBindingException("Object "
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/CorbaObjectReferenceListener.java`
#### Snippet
```java
            try {
                ref = CorbaUtils.importObjectReference(orb, text);
            } catch (org.omg.CORBA.BAD_PARAM ex) {
                throw new RuntimeException("Unable to resolve CORBA object with address " + text);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/HandlerIterator.java`
#### Snippet
```java
        while (next == null && count < params.length) {
            int mode = params[count].getMode();
            int value = org.omg.CORBA.ARG_OUT.value;
            if (isServer) {
                value = org.omg.CORBA.ARG_IN.value;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/HandlerIterator.java`
#### Snippet
```java
            int value = org.omg.CORBA.ARG_OUT.value;
            if (isServer) {
                value = org.omg.CORBA.ARG_IN.value;
            }
            if (mode == value
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/HandlerIterator.java`
#### Snippet
```java
            }
            if (mode == value
                || mode == org.omg.CORBA.ARG_INOUT.value) {
                next = params[count].getObject();
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/types/HandlerIterator.java`
#### Snippet
```java
            }
            if (!isServer
                && mode == org.omg.CORBA.ARG_INOUT.value
                && next instanceof CorbaPrimitiveHandler) {
                CorbaPrimitiveHandler prim = (CorbaPrimitiveHandler)next;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
            Any returnAny = CorbaAnyHelper.createAny(orb, message.getExchange().getBus());
            retVal.getObject().setIntoAny(returnAny, retVal, false);
            ret = orb.create_named_value(retVal.getName(), returnAny, org.omg.CORBA.ARG_OUT.value);
        } else {
            // for some reason, some ORBs do not like to
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
            // have a null NamedValue return value. Create this 'empty'
            // one if a void return type is used.
            ret = orb.create_named_value("return", orb.create_any(), org.omg.CORBA.ARG_OUT.value);
        }
        return ret;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
    public Request getRequest(CorbaMessage message,
                                 String opName,
                                 org.omg.CORBA.NVList nvlist,
                                 org.omg.CORBA.NamedValue ret,
                                 org.omg.CORBA.ExceptionList exList)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
                                 String opName,
                                 org.omg.CORBA.NVList nvlist,
                                 org.omg.CORBA.NamedValue ret,
                                 org.omg.CORBA.ExceptionList exList)
        throws Exception {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
                                 org.omg.CORBA.NVList nvlist,
                                 org.omg.CORBA.NamedValue ret,
                                 org.omg.CORBA.ExceptionList exList)
        throws Exception {
        Request request = null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/CorbaConduit.java`
#### Snippet
```java
            }

        } catch (java.lang.Exception ex) {
            LOG.log(Level.SEVERE, "Could not resolve target object");
            throw new CorbaBindingException(ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/OrbConfig.java`
#### Snippet
```java
            Method narrowMethod =
                bootMgrHelperClass.getMethod("narrow", org.omg.CORBA.Object.class);
            java.lang.Object bootMgr = narrowMethod.invoke(null,
                                                           orb.resolve_initial_references("BootManager"));
            Method addBindingMethod =
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/OrbConfig.java`
#### Snippet
```java
                bootMgrClass.getMethod("add_binding", byte[].class, org.omg.CORBA.Object.class);
            addBindingMethod.invoke(bootMgr, key.getBytes(), object);
        } catch (ClassNotFoundException | java.lang.reflect.InvocationTargetException ex) {
            //Not supported by the orb. skip it.
        } catch (java.lang.Exception ex) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/OrbConfig.java`
#### Snippet
```java
        } catch (ClassNotFoundException | java.lang.reflect.InvocationTargetException ex) {
            //Not supported by the orb. skip it.
        } catch (java.lang.Exception ex) {
            throw new CorbaBindingException(ex.getMessage(), ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamableImpl.java`
#### Snippet
```java
            CorbaObjectReader reader = new CorbaObjectReader(istream);
            reader.read(value);
        } catch (java.lang.Exception ex) {
            throw new CorbaBindingException("Error reading streamable value", ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamableImpl.java`
#### Snippet
```java
        typecode = obj.getTypeCode();

        mode = org.omg.CORBA.ARG_OUT.value;
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamableImpl.java`
#### Snippet
```java
            CorbaObjectWriter writer = new CorbaObjectWriter(ostream);
            writer.write(value);
        } catch (java.lang.Exception ex) {
            throw new CorbaBindingException("Error writing streamable value", ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeProcessingInstruction(java.lang.String target,
                                           java.lang.String data)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeProcessingInstruction(java.lang.String target,
                                           java.lang.String data)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String localName)
        throws XMLStreamException {
        writeStartElement(localName);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeComment(java.lang.String data)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void setDefaultNamespace(java.lang.String uri)
        throws XMLStreamException {
        defaultNS = uri;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeCharacters(java.lang.String text)
        throws XMLStreamException {
        currentTypeListener.processCharacters(text);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeAttribute(java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
        writeAttribute("", defaultNS, localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeEmptyElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    public void writeEmptyElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
        throws XMLStreamException {
        writeStartElement(namespaceURI, localName, prefix);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeAttribute(java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    public void writeAttribute(java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
        writeAttribute("", namespaceURI, localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void setPrefix(java.lang.String prefix,
                          java.lang.String uri)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void setPrefix(java.lang.String prefix,
                          java.lang.String uri)
        throws XMLStreamException {
        ctx.setPrefix(prefix, uri);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeStartElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    public void writeStartElement(java.lang.String prefix,
                                  java.lang.String localName,
                                  java.lang.String namespaceURI)
        throws XMLStreamException {
        ctx.push();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartDocument(java.lang.String version)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeProcessingInstruction(java.lang.String target)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeDTD(java.lang.String dtd)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeEmptyElement(java.lang.String namespaceURI,
                                  java.lang.String localName)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeEmptyElement(java.lang.String namespaceURI,
                                  java.lang.String localName)
        throws XMLStreamException {
        writeStartElement(namespaceURI, localName);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public java.lang.String getPrefix(java.lang.String uri)
        throws XMLStreamException {
        return ctx.getPrefix(uri);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public java.lang.String getPrefix(java.lang.String uri)
        throws XMLStreamException {
        return ctx.getPrefix(uri);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeAttribute(java.lang.String prefix,
                               java.lang.String namespaceURI,
                               java.lang.String localName,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaDSIServant.java`
#### Snippet
```java
                interfaces.add(iter.next());
            }
        } catch (java.lang.Exception ex) {
            LOG.log(Level.SEVERE, "Couldn't initialize the corba DSI servant");
            throw new CorbaBindingException(ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeAttribute(java.lang.String prefix,
                               java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    public void writeAttribute(java.lang.String prefix,
                               java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
                               java.lang.String namespaceURI,
                               java.lang.String localName,
                               java.lang.String value)
        throws XMLStreamException {
        currentTypeListener.processWriteAttribute(prefix, namespaceURI, localName, value);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartElement(java.lang.String localName)
        throws XMLStreamException {
        writeStartElement("", localName, defaultNS);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeStartDocument(java.lang.String encoding,
                                   java.lang.String version)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeStartDocument(java.lang.String encoding,
                                   java.lang.String version)
        throws XMLStreamException {
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeCData(java.lang.String data)
        throws XMLStreamException {
        throw new XMLStreamException("Not yet implemented");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeDefaultNamespace(java.lang.String namespaceURI)
        throws XMLStreamException {
        defaultNS = namespaceURI;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeNamespace(java.lang.String prefix,
                               java.lang.String namespaceURI)
        throws XMLStreamException {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public void writeNamespace(java.lang.String prefix,
                               java.lang.String namespaceURI)
        throws XMLStreamException {
        if (currentTypeListener != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public void writeEntityRef(java.lang.String name)
        throws XMLStreamException {
        throw new XMLStreamException("Not yet implemented");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public java.lang.Object getProperty(java.lang.String name)
        throws java.lang.IllegalArgumentException {
        return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java
    }

    public java.lang.Object getProperty(java.lang.String name)
        throws java.lang.IllegalArgumentException {
        return null;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaStreamWriter.java`
#### Snippet
```java

    public java.lang.Object getProperty(java.lang.String name)
        throws java.lang.IllegalArgumentException {
        return null;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java
    }

    public void writeULongLong(java.math.BigInteger l) throws CorbaBindingException {
        if (l == null) {
            stream.write_ulonglong(0);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutInterceptor.java`
#### Snippet
```java
    public void handleMessage(Message msg) {
        CorbaMessage message = (CorbaMessage) msg;
        orb = (org.omg.CORBA.ORB) message.get(CorbaConstants.ORB);
        Exchange exchange = message.getExchange();
        service = exchange.getEndpoint().getEndpointInfo().getService();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutInterceptor.java`
#### Snippet
```java
public class CorbaStreamOutInterceptor extends AbstractPhaseInterceptor<Message> {

    private org.omg.CORBA.ORB orb;
    private CorbaTypeMap typeMap;
    private ServiceInfo service;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                ServiceInfo service = message.getExchange().getEndpoint().getEndpointInfo().getService();

                org.omg.CORBA.ORB orb = (org.omg.CORBA.ORB) message.get(CorbaConstants.ORB);
                if (orb == null) {
                    orb = message.getExchange().get(org.omg.CORBA.ORB.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                ServiceInfo service = message.getExchange().getEndpoint().getEndpointInfo().getService();

                org.omg.CORBA.ORB orb = (org.omg.CORBA.ORB) message.get(CorbaConstants.ORB);
                if (orb == null) {
                    orb = message.getExchange().get(org.omg.CORBA.ORB.class);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                org.omg.CORBA.ORB orb = (org.omg.CORBA.ORB) message.get(CorbaConstants.ORB);
                if (orb == null) {
                    orb = message.getExchange().get(org.omg.CORBA.ORB.class);
                }
                QName elName = new QName("", exStreamable.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultInInterceptor.java`
#### Snippet
```java
                message.setContent(Exception.class, e);
            }
        } catch (java.lang.Exception ex) {
            LOG.log(Level.SEVERE, "CORBA unmarshalFault exception", ex);
            throw new CorbaBindingException("CORBA unmarshalFault exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA.portable` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java

public final class SystemExceptionHelper
    implements org.omg.CORBA.portable.Streamable {

    private static final int BAD_CONTEXT = 0;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA.portable` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java
    //CHECKSTYLE:OFF
    //NCSS is to high for this due to the massive switch statement
    public static SystemException read(org.omg.CORBA.portable.InputStream in) {

        String id = in.read_string();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java
        String id = in.read_string();
        int minor = in.read_ulong();
        org.omg.CORBA.CompletionStatus status = org.omg.CORBA.CompletionStatus.from_int(in.read_ulong());

        int n = binarySearch(IDS, id);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/SystemExceptionHelper.java`
#### Snippet
```java
        String id = in.read_string();
        int minor = in.read_ulong();
        org.omg.CORBA.CompletionStatus status = org.omg.CORBA.CompletionStatus.from_int(in.read_ulong());

        int n = binarySearch(IDS, id);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamFaultOutInterceptor.java`
#### Snippet
```java

    protected void setUserExceptionFromFaultDetail(CorbaMessage message,
                                                   org.w3c.dom.Element faultDetail,
                                                   RaisesType exType,
                                                   OperationInfo opInfo,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
            l &= 0xffffffffL;
            return l;
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read unsigned long");
            throw new CorbaBindingException("CorbaObjectReader: readULong MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
                ((CorbaAnyHandler)handler).setTypeMap(((CorbaAnyHandler)template).getTypeMap());
            }
        } catch (java.lang.Exception ex) {
            throw new CorbaBindingException("Unable to instantiate sequence element", ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Short.valueOf(stream.read_short());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read short");
            throw new CorbaBindingException("CorbaObjectReader: readShort MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Character.valueOf(stream.read_wchar());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read wide character");
            throw new CorbaBindingException("CorbaObjectReader: readWChar MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Float.valueOf(stream.read_float());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read float");
            throw new CorbaBindingException("CorbaObjectReader: readFloat MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.math` is unnecessary and can be removed
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        long scale = fixedHandler.getScale();
        try {
            java.math.BigDecimal fixedValue = stream.read_fixed().movePointLeft((int)scale);
            fixedHandler.setValue(fixedValue);
        } catch (NO_IMPLEMENT ex) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
            }
            return result;
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read unsigned short");
            throw new CorbaBindingException("CorbaObjectReader: readUShort MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return stream.read_any();
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read any");
            throw new CorbaBindingException("CorbaObjectReader: readAny MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Double.valueOf(stream.read_double());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read double");
            throw new CorbaBindingException("CorbaObjectReader: readDouble MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Character.valueOf(stream.read_char());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read character");
            throw new CorbaBindingException("CorbaObjectReader: readChar MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return stream.read_string();
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read string");
            throw new CorbaBindingException("CorbaObjectReader: readString MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return convertLongToULong(stream.read_ulonglong());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read unsigned long long");
            throw new CorbaBindingException("CorbaObjectReader: readULongLong MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Long.valueOf(stream.read_longlong());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read long long");
            throw new CorbaBindingException("CorbaObjectReader: readLongLong MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return Byte.valueOf(stream.read_octet());
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read octet");
            throw new CorbaBindingException("CorbaObjectReader: readOctet MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return stream.read_boolean();
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read boolean");
            throw new CorbaBindingException("CorbaObjectReader: readBoolean MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return result;
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read long");
            throw new CorbaBindingException("CorbaObjectReader: readLong MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectReader.java`
#### Snippet
```java
        try {
            return stream.read_wstring();
        } catch (org.omg.CORBA.MARSHAL ex) {
            LOG.log(Level.SEVERE, "CorbaObjectReader: could not read wide string");
            throw new CorbaBindingException("CorbaObjectReader: readWString MARSHAL exception", ex);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
    public void handleMessage(Message msg) {
        CorbaMessage message = (CorbaMessage) msg;
        orb = (org.omg.CORBA.ORB) message.get(CorbaConstants.ORB);
        Exchange exchange = message.getExchange();
        BindingOperationInfo boi = exchange.getBindingOperationInfo();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
            ModeType paramMode = param.getMode();
            if ("in".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_IN.value);
            } else if ("inout".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_INOUT.value);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
                streamable.setMode(org.omg.CORBA.ARG_IN.value);
            } else if ("inout".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_INOUT.value);
            } // default mode is out
            message.addStreamableArgument(streamable);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
public class CorbaStreamOutEndingInterceptor extends AbstractPhaseInterceptor<Message> {

    private org.omg.CORBA.ORB orb;
    private CorbaTypeMap typeMap;
    private ServiceInfo service;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
            ModeType paramMode = param.getMode();
            if ("in".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_IN.value);
            } else if ("inout".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_INOUT.value);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
                streamable.setMode(org.omg.CORBA.ARG_IN.value);
            } else if ("inout".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_INOUT.value);
            } else if ("out".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_OUT.value);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamOutEndingInterceptor.java`
#### Snippet
```java
                streamable.setMode(org.omg.CORBA.ARG_INOUT.value);
            } else if ("out".equals(paramMode.value())) {
                streamable.setMode(org.omg.CORBA.ARG_OUT.value);
            }
            message.addStreamableArgument(streamable);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.security.auth` is unnecessary and can be removed
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/ManagedConnectionFactoryImpl.java`
#### Snippet
```java


    protected void validateReference(AbstractManagedConnectionImpl conn, javax.security.auth.Subject subj) {
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/Handler.java`
#### Snippet
```java
        }

        public InputStream getInputStream() throws java.io.IOException {
            byte[] bytes = PlugInClassLoaderHelper.getResourceAsBytes(url.getPath());

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/Handler.java`
#### Snippet
```java

            if (bytes != null) {
                return new java.io.ByteArrayInputStream(bytes);
            }
            throw new java.io.IOException(url.getPath() + " not found");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/Handler.java`
#### Snippet
```java
                return new java.io.ByteArrayInputStream(bytes);
            }
            throw new java.io.IOException(url.getPath() + " not found");
        }
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/PlugInClassLoaderHelper.java`
#### Snippet
```java
            try {
                return JarLoader.getBytesFromInputStream(new URL(name).openStream());
            } catch (java.net.MalformedURLException mue) {
                throw new IOException(mue.getMessage());
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/Handler.java`
#### Snippet
```java
            }

            public InputStream getInputStream() throws java.io.IOException {
                LOG.fine("getInputStream, path=" + url.getPath());
                InputStream is = Handler.class.getClassLoader().getResourceAsStream(url.getPath());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/Handler.java`
#### Snippet
```java
                InputStream is = Handler.class.getClassLoader().getResourceAsStream(url.getPath());
                if (is == null) {
                    throw new java.io.IOException(url.getPath()
                                                  + " Not Found by getResourceAsStream(), ClassLoader="
                                                  + Handler.class.getClassLoader());
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.resource.spi` is unnecessary and can be removed
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationSpec.java`
#### Snippet
```java

/**
 * MDBActivationSpec is an {@link jakarta.resource.spi.ActivationSpec} that
 * activates a CXF service endpoint facade.  All resource locations are
 * relative to the message driven bean jar.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
        org.omg.CORBA.Object result;

        java.io.File file = new java.io.File(url);
        if (!file.exists()) {
            throw new RuntimeException("Could not find file " + url + " to read the object reference");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
        org.omg.CORBA.Object result;

        java.io.File file = new java.io.File(url);
        if (!file.exists()) {
            throw new RuntimeException("Could not find file " + url + " to read the object reference");
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            throw new RuntimeException("Could not find file " + url + " to read the object reference");
        }
        try (java.io.BufferedReader reader = new java.io.BufferedReader(new java.io.FileReader(file))) {
            String ior = reader.readLine();
            if (ior == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            throw new RuntimeException("Could not find file " + url + " to read the object reference");
        }
        try (java.io.BufferedReader reader = new java.io.BufferedReader(new java.io.FileReader(file))) {
            String ior = reader.readLine();
            if (ior == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            throw new RuntimeException("Could not find file " + url + " to read the object reference");
        }
        try (java.io.BufferedReader reader = new java.io.BufferedReader(new java.io.FileReader(file))) {
            String ior = reader.readLine();
            if (ior == null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
            }
            result = orb.string_to_object(ior.trim());
        } catch (java.io.IOException ex) {
            throw new RuntimeException(ex);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA.portable` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaUtils.java`
#### Snippet
```java
                        break;
                    case TCKind._tk_enum:
                        org.omg.CORBA.portable.OutputStream out =
                            member.label.create_output_stream();
                        Enum enumVal = (Enum)getCorbaType(unionType.getDiscriminator(), typeMap);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `integration/jca/src/main/java/org/apache/cxf/jca/core/classloader/FireWallClassLoader.java`
#### Snippet
```java


    public java.net.URL getResource(String name) {
        if (negativeFNFilters != null) {
            for (int i = 0; i < negativeFNFilters.length; i++) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamInInterceptor.java`
#### Snippet
```java
                Any value = CorbaAnyHelper.createAny(orb, corbaMsg.getExchange().getBus());
                if ("in".equals(paramMode.value())) {
                    streamables[i].setMode(org.omg.CORBA.ARG_IN.value);
                    streamables[i].getObject().setIntoAny(value, streamables[i], false);
                } else if ("out".equals(paramMode.value())) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamInInterceptor.java`
#### Snippet
```java
                    streamables[i].getObject().setIntoAny(value, streamables[i], false);
                } else if ("out".equals(paramMode.value())) {
                    streamables[i].setMode(org.omg.CORBA.ARG_OUT.value);
                    streamables[i].getObject().setIntoAny(value, streamables[i], true);
                } else {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.omg.CORBA` is unnecessary, and can be replaced with an import
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/interceptors/CorbaStreamInInterceptor.java`
#### Snippet
```java
                    streamables[i].getObject().setIntoAny(value, streamables[i], true);
                } else {
                    streamables[i].setMode(org.omg.CORBA.ARG_INOUT.value);
                    streamables[i].getObject().setIntoAny(value, streamables[i], false);
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.jca.inbound` is unnecessary and can be removed
in `integration/jca/src/main/java/org/apache/cxf/jca/inbound/MDBActivationWork.java`
#### Snippet
```java

        try {
            Class<?> clazz = org.apache.cxf.jca.inbound.DispatchMDBMessageListener.class;
            method = clazz.getMethod(MESSAGE_LISTENER_METHOD, new Class[] {String.class});
        } catch (Exception ex) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `integration/jca/src/main/java/org/apache/cxf/connector/CXFConnectionParam.java`
#### Snippet
```java


    public boolean equals(java.lang.Object other) {
        boolean result = false;
        if (other instanceof CXFConnectionRequestInfo) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            Response r = getResponse();
            //REVISIT
            return new java.io.ByteArrayInputStream(r.getTextEntity().getBytes());
        }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
            Response r = getResponse();
            //REVISIT
            return new java.io.ByteArrayInputStream(r.getTextEntity().getBytes());
        }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createMalformedRequestURIFault(String requestURI) {
        String m = new org.apache.cxf.common.i18n.Message("MALFORMED_REQUESTURI", LOG,
                                                          new Object[] {
                                                              requestURI
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createMissingContentTypeFault() {
        String m = new org.apache.cxf.common.i18n.Message("MISSING_CONTENTTYPE", LOG).toString();
        return createFault(SoapJMSConstants.getMissingContentTypeQName(), m);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createUnsupportedJMSMessageFormatFault(String messageFormat) {
        String m = new org.apache.cxf.common.i18n.Message("UNSUPPORTED_JMSMESSAGEFORMAT", LOG,
                                                          new Object[] {
                                                              messageFormat
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createContentEncodingNotSupported(String contentEncoding) {
        String m = new org.apache.cxf.common.i18n.Message("CONTENT_ENCODING_NOT_SUPPORTED", LOG,
                                                          new Object[] {
                                                              contentEncoding
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createContentTypeMismatchFault(String contentType) {
        String m = new org.apache.cxf.common.i18n.Message("CONTENTTYPE_MISMATCH", LOG,
                                                          new Object[] {
                                                              contentType
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createUnrecognizedBindingVerionFault(String bindingVersion) {
        String m = new org.apache.cxf.common.i18n.Message("UNRECOGNIZED_BINDINGVERSION", LOG,
                                                          new Object[] {
                                                              bindingVersion
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createMissingRequestURIFault() {
        String m = new org.apache.cxf.common.i18n.Message("MISSING_REQUESTURI", LOG).toString();
        return createFault(SoapJMSConstants.getMissingRequestURIQName(), m);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createMissingSoapActionFault() {
        String m = new org.apache.cxf.common.i18n.Message("MISSING_SOAPACTION", LOG).toString();
        return createFault(SoapJMSConstants.getMissingSoapActionQName(), m);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createTargetServiceNotAllowedInRequestURIFault() {
        String m = new org.apache.cxf.common.i18n.Message(
                                                          "TARGET_SERVICE_NOT_ALLOWED_IN_REQUESTURI",
                                                          LOG).toString();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/jms/interceptor/JMSFaultFactory.java`
#### Snippet
```java

    public static JMSFault createMismatchedSoapActionFault(String soapAction) {
        String m = new org.apache.cxf.common.i18n.Message("MISMATCHED_SOAPACTION", LOG,
                                                          new Object[] {
                                                              soapAction
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/Soap12FaultOutInterceptor.java`
#### Snippet
```java
            XMLStreamWriter writer = message.getContent(XMLStreamWriter.class);
            Fault f = (Fault)message.getContent(Exception.class);
            message.put(org.apache.cxf.message.Message.RESPONSE_CODE, f.getStatusCode());

            SoapFault fault = SoapFault.createFault(f, message.getVersion());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/Soap12FaultOutInterceptor.java`
#### Snippet
```java
    public void handleMessage(SoapMessage message) throws Fault {
        Fault f = (Fault) message.getContent(Exception.class);
        message.put(org.apache.cxf.message.Message.RESPONSE_CODE, f.getStatusCode());
        if (message.getVersion() == Soap11.getInstance()) {
            message.getInterceptorChain().add(Soap11FaultOutInterceptorInternal.INSTANCE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary, and can be replaced with an import
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/Soap11FaultOutInterceptor.java`
#### Snippet
```java
    public void handleMessage(SoapMessage message) throws Fault {
        Fault f = (Fault) message.getContent(Exception.class);
        message.put(org.apache.cxf.message.Message.RESPONSE_CODE, f.getStatusCode());
        if (message.getVersion() == Soap11.getInstance()) {
            message.getInterceptorChain().add(Soap11FaultOutInterceptorInternal.INSTANCE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.opensaml.saml.saml2.core` is unnecessary, and can be replaced with an import
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/saml/SamlOAuthValidator.java`
#### Snippet
```java
    private boolean validateAuthenticationSubject(Message m,
                                                  Conditions cs,
                                                  org.opensaml.saml.saml2.core.Subject subject) {
        // We need to find a Bearer Subject Confirmation method
        boolean bearerSubjectConfFound = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.rt.security.utils` is unnecessary, and can be replaced with an import
in `rt/rs/security/oauth-parent/oauth2-saml/src/main/java/org/apache/cxf/rs/security/oauth2/grants/saml/Saml2BearerGrantHandler.java`
#### Snippet
```java
                boolean enableRevocation = false;
                String enableRevocationStr =
                    (String)org.apache.cxf.rt.security.utils.SecurityUtils.getSecurityPropertyValue(
                        SecurityConstants.ENABLE_REVOCATION, message);
                if (enableRevocationStr != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `maven-plugins/wadl2java-plugin/src/main/java/org/apache/cxf/maven_plugin/wadlto/Option.java`
#### Snippet
```java
        }
        int len = l1.length + l2.length;
        T[] ret = (T[])java.lang.reflect.Array.newInstance(cls, len);
        System.arraycopy(l1, 0, ret, 0, l1.length);
        System.arraycopy(l2, 0, ret, l1.length, l2.length);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.policy` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyBuilderImpl.java`
#### Snippet
```java
                factory = reg;
            }
            org.apache.cxf.ws.policy.PolicyEngine e
                = bus.getExtension(org.apache.cxf.ws.policy.PolicyEngine.class);
            if (e != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.ws.policy` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/PolicyBuilderImpl.java`
#### Snippet
```java
            }
            org.apache.cxf.ws.policy.PolicyEngine e
                = bus.getExtension(org.apache.cxf.ws.policy.PolicyEngine.class);
            if (e != null) {
                this.setPolicyRegistry(e.getRegistry());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
    }

    void setInterceptors(List<Interceptor<? extends org.apache.cxf.message.Message>> out) {
        interceptors = out;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
            PolicyInterceptorProviderRegistry reg
                = ((PolicyEngineImpl)engine).getBus().getExtension(PolicyInterceptorProviderRegistry.class);
            Set<Interceptor<? extends org.apache.cxf.message.Message>> out
                = new LinkedHashSet<>();
            for (Assertion a : getChosenAlternative()) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
                initialiseInterceptors(reg, engine, out, a, useIn, fault, m);
            }
            setInterceptors(new ArrayList<Interceptor<? extends  org.apache.cxf.message.Message>>(out));
        }
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
    }

    public List<Interceptor<? extends org.apache.cxf.message.Message>> getInterceptors() {
        return interceptors;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java

    void initialiseInterceptors(PolicyInterceptorProviderRegistry reg, PolicyEngine engine,
                                Set<Interceptor<? extends org.apache.cxf.message.Message>> out, Assertion a,
                                boolean useIn, boolean fault,
                                Message m) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
        QName qn = a.getName();

        List<Interceptor<? extends org.apache.cxf.message.Message>> i = null;
        if (useIn && !fault) {
            i = reg.getInInterceptorsForAssertion(qn);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EffectivePolicyImpl.java`
#### Snippet
```java
    protected Policy policy;
    protected Collection<Assertion> chosenAlternative;
    protected List<Interceptor<? extends org.apache.cxf.message.Message>> interceptors;

    public EffectivePolicyImpl() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/EndpointPolicyImpl.java`
#### Snippet
```java
                                boolean fault, Message msg) {
        QName qn = a.getName();
        List<Interceptor<? extends org.apache.cxf.message.Message>> i
            = fault ? reg.getInFaultInterceptorsForAssertion(qn)
            : reg.getInInterceptorsForAssertion(qn);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.common.i18n` is unnecessary and can be removed
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/AssertionInfoMap.java`
#### Snippet
```java
            for (AssertionInfo ai : ais) {
                if (!ai.isAsserted()) {
                    throw new PolicyException(new org.apache.cxf.common.i18n.Message(
                        "NOT_ASSERTED_EXC", BUNDLE, ai.getAssertion().getName()));
                }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
    private void addPropertiesFrom(ExchangeData exchange, Message message) {
        for (Map.Entry<String, Object> entry : message.entrySet()) {
            if (entry.getKey().equals(org.apache.cxf.message.Message.ENCODING)) {
                exchange.setEncoding((String)entry.getValue());
            } else if (entry.getKey().equals(org.apache.cxf.message.Message.REQUEST_URI)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
            if (entry.getKey().equals(org.apache.cxf.message.Message.ENCODING)) {
                exchange.setEncoding((String)entry.getValue());
            } else if (entry.getKey().equals(org.apache.cxf.message.Message.REQUEST_URI)) {
                exchange.setUri((String)entry.getValue());
            } else if (entry.getKey().equals(org.apache.cxf.message.Message.PROTOCOL_HEADERS)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.message` is unnecessary and can be removed
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
            } else if (entry.getKey().equals(org.apache.cxf.message.Message.REQUEST_URI)) {
                exchange.setUri((String)entry.getValue());
            } else if (entry.getKey().equals(org.apache.cxf.message.Message.PROTOCOL_HEADERS)) {

                if (entry.getValue() instanceof Map) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisElementDataWriter.java`
#### Snippet
```java

    public void write(Object obj, QName elementName, boolean optional, Element output,
                      java.lang.reflect.Type objectType) throws Exception {
        W3CDOMStreamWriter swriter = new W3CDOMStreamWriter(output);
        writer.write(obj, elementName, optional, swriter, objectType);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisWriter.java`
#### Snippet
```java
               boolean optional,
               SinkT output,
               java.lang.reflect.Type objectType) throws Exception;
}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisXMLStreamDataWriter.java`
#### Snippet
```java
     */
    public void write(Object obj, QName elementName, boolean optional, XMLStreamWriter output,
                      java.lang.reflect.Type objectType) throws Exception {
        AegisType aegisType = null;
        if (objectType instanceof Class) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageReader#getValueAsBoolean()
     */
    public boolean getValueAsBoolean() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageReader#getValueAsFloat()
     */
    public float getValueAsFloat() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageReader#getValueAsLong()
     */
    public long getValueAsLong() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageReader#getValueAsDouble()
     */
    public double getValueAsDouble() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageReader.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageReader#getValueAsCharacter()
     */
    public char getValueAsCharacter() {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsCharacter(java.lang.Character)
     */
    public void writeValueAsCharacter(Character char1) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsCharacter(java.lang.Character)
     */
    public void writeValueAsCharacter(Character char1) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsDouble(java.lang.Double)
     */
    public void writeValueAsDouble(Double d) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsDouble(java.lang.Double)
     */
    public void writeValueAsDouble(Double d) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsLong(java.lang.Long)
     */
    public void writeValueAsLong(Long l) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsLong(java.lang.Long)
     */
    public void writeValueAsLong(Long l) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsBoolean(boolean)
     */
    public void writeValueAsBoolean(boolean b) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsFloat(java.lang.Float)
     */
    public void writeValueAsFloat(Float f) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsFloat(java.lang.Float)
     */
    public void writeValueAsFloat(Float f) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsInt(java.lang.Integer)
     */
    public void writeValueAsInt(Integer i) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/AbstractMessageWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValueAsInt(java.lang.Integer)
     */
    public void writeValueAsInt(Integer i) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeMapping.java`
#### Snippet
```java
/**
 * Abstraction for the map between Java types (represented as
 * {@link java.lang.reflect.Type} and Aegis types.
 */
public interface TypeMapping {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
    /**
     * Allow writing of collections when the type of the collection object is known via
     * an {@link java.lang.reflect.Type} object.
     * @param globalContext the context
     * @param value the object to write.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/TypeUtil.java`
#### Snippet
```java
    public static AegisType getWriteTypeStandalone(AegisContext globalContext,
                                              Object value,
                                              java.lang.reflect.Type reflectType) {
        if (reflectType == null) {
            return getWriteTypeStandalone(globalContext, value, (AegisType)null);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#getWriter(java.lang.String)
     */
    public MessageWriter getElementWriter(String nm) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#getWriter(java.lang.String)
     */
    public MessageWriter getElementWriter(String nm) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValue(java.lang.Object)
     */
    public void writeValue(Object value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/xml/stax/ElementWriter.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.xml.MessageWriter#writeValue(java.lang.Object)
     */
    public void writeValue(Object value) {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AegisType.java`
#### Snippet
```java

    /**
     * @see java.lang.Object#equals(java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/AegisType.java`
#### Snippet
```java

    /**
     * @see java.lang.Object#equals(java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
/**
 * The implementation of the Aegis type map. It maintains a map from
 * Java types {@link java.lang.reflect.Type} and AegisType objects,
 * also indexed by the XML Schema QName of each type.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        new CharacterAsStringType());

        defaultRegister(tm, defaultNillable, javax.xml.datatype.Duration.class, Constants.XSD_DURATION,
                            new org.apache.cxf.aegis.type.java5.DurationType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java

        defaultRegister(tm, defaultNillable, javax.xml.datatype.Duration.class, Constants.XSD_DURATION,
                            new org.apache.cxf.aegis.type.java5.DurationType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATE,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.Duration.class, Constants.XSD_DURATION,
                            new org.apache.cxf.aegis.type.java5.DurationType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATE,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATE,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_TIME,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_DATE,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_TIME,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_TIME,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DAY,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.activation` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/DataSourceType.java`
#### Snippet
```java

/**
 * Binding for {@link jakarta.activation.DataSource}.
 */
public class DataSourceType extends AbstractXOPType {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.activation` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/mtom/DataHandlerType.java`
#### Snippet
```java

/**
 * Binding for {@link jakarta.activation.DataHandler}. This assumes that the DataHandler will always
 * contain a {@link jakarta.activation.DataSource}, not data in the Object.
 */
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_TIME,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTH,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_DAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTHDAY,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_MONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTHDAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_MONTHDAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEAR,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_MONTHDAY,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEAR,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEAR,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEARMONTH,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_YEAR,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEARMONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_YEARMONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATETIME,
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
                        Constants.XSD_YEARMONTH,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATETIME,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.java5` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/DefaultTypeMapping.java`
#### Snippet
```java
        defaultRegister(tm, defaultNillable, javax.xml.datatype.XMLGregorianCalendar.class,
                        Constants.XSD_DATETIME,
                            new org.apache.cxf.aegis.type.java5.XMLGregorianCalendarType());
        return tm;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    }

    public AegisWriter<org.w3c.dom.Element> createDomElementWriter() {
        return new AegisElementDataWriter(this);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    }

    private void addTypeToMappableClasses(Set<Class<?>> mappableClasses, java.lang.reflect.Type jtype) {
        if (jtype instanceof Class) {
            Class<?> jclass = (Class<?>) jtype;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
            mappableClasses.add(jclass);
        } else if (jtype instanceof ParameterizedType) {
            for (java.lang.reflect.Type t2 : ((ParameterizedType)jtype).getActualTypeArguments()) {
                addTypeToMappableClasses(mappableClasses, t2);
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java

    private Set<String> rootClassNames;
    private Set<java.lang.reflect.Type> rootClasses;
    private Set<QName> rootTypeQNames;
    // this type mapping is the front of the chain of delegating type mappings.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type.basic` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
     * string containing a class name, Aegis will create proxy objects of that class name.
     *
     * @see org.apache.cxf.aegis.type.basic.BeanType
     * @return
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    private Set<Class<?>> rootMappableClasses() {
        Set<Class<?>> mappableClasses = new HashSet<>();
        for (java.lang.reflect.Type jtype : rootClasses) {
            addTypeToMappableClasses(mappableClasses, jtype);
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    }

    public Set<java.lang.reflect.Type> getRootClasses() {
        return rootClasses;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
    }

    public AegisReader<org.w3c.dom.Element> createDomElementReader() {
        return new AegisElementDataReader(this);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
     * @param rootClasses
     */
    public void setRootClasses(Set<java.lang.reflect.Type> rootClasses) {
        this.rootClasses = rootClasses;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/AegisContext.java`
#### Snippet
```java
         * and registering them.
         */
        for (java.lang.reflect.Type reflectType : rootClasses) {
            if (!(reflectType instanceof Class)) {
                // if it's not a Class, it can't be mapped from Class to type in the mapping.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BooleanType.java`
#### Snippet
```java

/**
 * Aegis type for {@link java.lang.Boolean}. These can be null.
 */
public class BooleanType extends AegisType {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java

    /**
     * @see org.apache.cxf.aegis.type.AegisType#getDependencies()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/ArrayType.java`
#### Snippet
```java
     * We need to write a complex type schema for Beans, so return true.
     *
     * @see org.apache.cxf.aegis.type.AegisType#isComplex()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.w3c.dom` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            throw new RuntimeException("Unable to create a document builder, e");
        }
        org.w3c.dom.Document doc;
        documentBuilder.setErrorHandler(new ErrorHandler() {

```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
        AEGIS_DOCUMENT_BUILDER_FACTORY.setNamespaceAware(true);
        try {
            AEGIS_DOCUMENT_BUILDER_FACTORY.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
            AEGIS_DOCUMENT_BUILDER_FACTORY.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
        } catch (javax.xml.parsers.ParserConfigurationException ex) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.parsers` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/XMLTypeCreator.java`
#### Snippet
```java
            AEGIS_DOCUMENT_BUILDER_FACTORY.setFeature(javax.xml.XMLConstants.FEATURE_SECURE_PROCESSING, true);
            AEGIS_DOCUMENT_BUILDER_FACTORY.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
        } catch (javax.xml.parsers.ParserConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java

public class AnnotationReader {
    private static final Class<? extends Annotation> WEB_PARAM = jakarta.jws.WebParam.class;
    private static final Class<? extends Annotation> WEB_RESULT = jakarta.jws.WebResult.class;
    private static final Class<? extends Annotation> XML_ATTRIBUTE =
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            jakarta.xml.bind.annotation.XmlType.class;
    private static final Class<? extends Annotation> XML_TRANSIENT =
            jakarta.xml.bind.annotation.XmlTransient.class;

    public boolean isIgnored(AnnotatedElement element) {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.jws` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
public class AnnotationReader {
    private static final Class<? extends Annotation> WEB_PARAM = jakarta.jws.WebParam.class;
    private static final Class<? extends Annotation> WEB_RESULT = jakarta.jws.WebResult.class;
    private static final Class<? extends Annotation> XML_ATTRIBUTE =
            jakarta.xml.bind.annotation.XmlAttribute.class;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation` is unnecessary, and can be replaced with an import
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
            jakarta.xml.bind.annotation.XmlElement.class;
    private static final Class<? extends Annotation> XML_SCHEMA =
            jakarta.xml.bind.annotation.XmlSchema.class;
    private static final Class<? extends Annotation> XML_TYPE =
            jakarta.xml.bind.annotation.XmlType.class;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanType.java`
#### Snippet
```java
     * We need to write a complex type schema for Beans, so return true.
     *
     * @see org.apache.cxf.aegis.type.AegisType#isComplex()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.text` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/util/date/XsDateTimeFormat.java`
#### Snippet
```java
/**
 * <p>
 * An instance of {@link java.text.Format}, which may be used to parse and
 * format <code>xs:dateTime</code> values.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis.type` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapArrayType.java`
#### Snippet
```java
     * We need to write a complex type schema for Beans, so return true.
     *
     * @see org.apache.cxf.aegis.type.AegisType#isComplex()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `io.netty.util` is unnecessary, and can be replaced with an import
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduitFactory.java`
#### Snippet
```java
    UseAsyncPolicy policy;
    public NettyHttpConduitFactory() {
        io.netty.util.Version.identify();
        Object st = SystemPropertyAction.getPropertyOrNull(USE_POLICY);
        policy = UseAsyncPolicy.getPolicy(st);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.codehaus.plexus.util` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/WsdlUtilities.java`
#### Snippet
```java
            Collections.addAll(exList, excludes);
        }
        Collections.addAll(exList, org.codehaus.plexus.util.FileUtils.getDefaultExcludes());

        String inc = joinWithComma(includes);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.codehaus.plexus.util` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/WsdlUtilities.java`
#### Snippet
```java

        try {
            List<?> newfiles = org.codehaus.plexus.util.FileUtils.getFiles(dir, inc, ex);
            return CastUtils.cast(newfiles);
        } catch (IOException exc) {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.maven_plugin` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2js/WsdlOption.java`
#### Snippet
```java
 * An option for javascript generation.
 */
public class WsdlOption extends Option implements org.apache.cxf.maven_plugin.GenericWsdlOption {

    private String wsdl;
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.xpath` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/shared/utils/FilteringUtil.java`
#### Snippet
```java
        try {
            xPathFactory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, Boolean.TRUE);
        } catch (javax.xml.xpath.XPathFactoryConfigurationException ex) {
            // ignore
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.maven_plugin` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/WsdlOption.java`
#### Snippet
```java
import org.apache.maven.plugin.MojoExecutionException;

public class WsdlOption extends Option implements org.apache.cxf.maven_plugin.GenericWsdlOption {

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.transport.https` is unnecessary, and can be replaced with an import
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
            connect(true);

            HostnameVerifier verifier = org.apache.cxf.transport.https.SSLUtils
                .getHostnameVerifier(findTLSClientParameters());

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.soap` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/FilteringInterceptor.java`
#### Snippet
```java
            return;
        }
        jakarta.xml.soap.SOAPMessage msg = message.getContent(jakarta.xml.soap.SOAPMessage.class);
        if (!FilteringUtil.runFilterOnMessage(msg, filter)) {
            message.getInterceptorChain().abort();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.soap` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/FilteringInterceptor.java`
#### Snippet
```java
            return;
        }
        jakarta.xml.soap.SOAPMessage msg = message.getContent(jakarta.xml.soap.SOAPMessage.class);
        if (!FilteringUtil.runFilterOnMessage(msg, filter)) {
            message.getInterceptorChain().abort();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.cxf.aegis` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
 * based on the SEI and implementation.
 *
 * @see org.apache.cxf.aegis.AegisContext
 */
public class AegisDatabinding extends AbstractDataBinding {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
            Boolean bestEffort = request.isBestEffort();
            if (bestEffort != null && bestEffort) {
                if (expirationTypeValue instanceof javax.xml.datatype.Duration) {
                    granted = grantExpirationFor((javax.xml.datatype.Duration)expirationTypeValue);
                } else if (expirationTypeValue instanceof XMLGregorianCalendar) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
            if (bestEffort != null && bestEffort) {
                if (expirationTypeValue instanceof javax.xml.datatype.Duration) {
                    granted = grantExpirationFor((javax.xml.datatype.Duration)expirationTypeValue);
                } else if (expirationTypeValue instanceof XMLGregorianCalendar) {
                    granted = grantExpirationFor((XMLGregorianCalendar)expirationTypeValue);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
                // client did not specify BestEffort granting, so we must either follow their wish
                // or throw a UnsupportedExpirationValue fault
                if (expirationTypeValue instanceof javax.xml.datatype.Duration) {
                    try {
                        if (DurationAndDateUtil.isPT0S((javax.xml.datatype.Duration)expirationTypeValue)) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
                if (expirationTypeValue instanceof javax.xml.datatype.Duration) {
                    try {
                        if (DurationAndDateUtil.isPT0S((javax.xml.datatype.Duration)expirationTypeValue)) {
                            ticket.setNonExpiring(true);
                        }
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
                        }
                        granted = DatatypeFactory.newInstance().newXMLGregorianCalendar(new GregorianCalendar());
                        granted.add((javax.xml.datatype.Duration)expirationTypeValue);
                    } catch (DatatypeConfigurationException e) {
                        throw new Error(e);
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
     * the client specified a duration in the request and did specify BestEffort=true.
     */
    public XMLGregorianCalendar grantExpirationFor(javax.xml.datatype.Duration requested) {
        XMLGregorianCalendar granted;
        try {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
        if (DurationAndDateUtil.isDuration(requestedExpiration.getValue())) {
            // duration was requested
            javax.xml.datatype.Duration requestedDuration = DurationAndDateUtil
                    .parseDuration(requestedExpiration.getValue());
            javax.xml.datatype.Duration grantedDuration = requestedDuration;
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.datatype` is unnecessary, and can be replaced with an import
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/manager/SubscriptionManagerImpl.java`
#### Snippet
```java
            javax.xml.datatype.Duration requestedDuration = DurationAndDateUtil
                    .parseDuration(requestedExpiration.getValue());
            javax.xml.datatype.Duration grantedDuration = requestedDuration;
            LOG.info("[subscription=" + ticket.getUuid() + "] Granted renewal duration: " + grantedDuration.toString());
            grantedExpires = getDatabase().findById(uuid)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.xml.bind.annotation.adapters` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            cript.reset();
            cript.update(doneFileName.getBytes("utf8"));
            doneFileName = new jakarta.xml.bind.annotation.adapters.HexBinaryAdapter().marshal(cript.digest());
        } catch (Exception e) {
            //ignore, we'll try and fake it based on the wsdl
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/wsdl2java/Option.java`
#### Snippet
```java
        }
        int len = l1.length + l2.length;
        T[] ret = (T[])java.lang.reflect.Array.newInstance(cls, len);
        System.arraycopy(l1, 0, ret, 0, l1.length);
        System.arraycopy(l2, 0, ret, l1.length, l2.length);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jdom` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java

    /**
     * Returns the current {@link org.jdom.JDOMFactory} in use, if one has been
     * previously set with {@link #setFactory}, otherwise null.
     *
```

### UnnecessaryFullyQualifiedName
Qualifier `org.jdom` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java

/**
 * Builds a JDOM {@link org.jdom.Document org.jdom.Document} using a
 * {@link javax.xml.stream.XMLStreamReader}.
 *
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.xml.stream` is unnecessary and can be removed
in `rt/databinding/aegis/src/main/generated/org/apache/cxf/aegis/util/jdom/StaxBuilder.java`
#### Snippet
```java
/**
 * Builds a JDOM {@link org.jdom.Document org.jdom.Document} using a
 * {@link javax.xml.stream.XMLStreamReader}.
 *
 * @author Tatu Saloranta
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
    private ExecutorService lookUpExecutorService() {
        try {
            javax.naming.InitialContext ic = new javax.naming.InitialContext();
            Object execService = ic.lookup("java:comp/DefaultManagedExecutorService");
            if (execService != null) {
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary, and can be replaced with an import
in `rt/rs/client/src/main/java/org/apache/cxf/jaxrs/client/WebClient.java`
#### Snippet
```java
    private ExecutorService lookUpExecutorService() {
        try {
            javax.naming.InitialContext ic = new javax.naming.InitialContext();
            Object execService = ic.lookup("java:comp/DefaultManagedExecutorService");
            if (execService != null) {
```

## RuleId[ruleID=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
    private static final byte[] CRLF = "\r\n".getBytes();
    private static final byte[] COLSP = ": ".getBytes();
    private static final Pattern CR_OR_LF = Pattern.compile("\\r|\\n");

    private WebSocketUtils() {
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `core/src/main/resources/schemas/wsdl/soap-encoding.xsd`
#### Snippet
```java
   <xs:simpleType>
     <xs:restriction base='xs:boolean'>
	   <xs:pattern value='0|1' />
	 </xs:restriction>
   </xs:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `maven-plugins/codegen-plugin/src/it/jaxb-xjc-runtime-sources/Cxf7998Service-1.0.0.wsdl`
#### Snippet
```java
			<xs:simpleType name="boolean">
				<xs:restriction base="xs:boolean">
					<xs:pattern value="0|1"/>
				</xs:restriction>
			</xs:simpleType>
```

## RuleId[ruleID=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/tomcat/AbstractTomcatServer.java`
#### Snippet
```java
            server.start();
        } catch (final Exception ex) {
            ex.printStackTrace();
            fail(ex.getMessage());
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `systests/cdi/base/src/main/java/org/apache/cxf/systests/cdi/base/jetty/AbstractJettyServer.java`
#### Snippet
```java
            server.start();
        } catch (final Exception ex) {
            ex.printStackTrace();
            fail(ex.getMessage());
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                context = JAXBContextCache.getCachedContextAndSchemas(ObjectFactory.class).getContext();
            } catch (JAXBException e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                            client = new WSDiscoveryClient(h.getXAddrs().get(0));
                        } catch (IOException e) {
                            e.printStackTrace();
                        }
                    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
                            }
                        } catch (IOException e) {
                            e.printStackTrace();
                        }
                    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            } catch (JAXBException e) {
                // TODO Auto-generated catch block
                e.printStackTrace();
            } catch (XMLStreamException e1) {
                // TODO Auto-generated catch block
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `services/ws-discovery/ws-discovery-api/src/main/java/org/apache/cxf/ws/discovery/internal/WSDiscoveryServiceImpl.java`
#### Snippet
```java
            } catch (XMLStreamException e1) {
                // TODO Auto-generated catch block
                e1.printStackTrace();
            }
            return ret;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngineFactory.java`
#### Snippet
```java
            } catch (Exception e) {
                // TODO Auto-generated catch block
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/rs/microprofile-client/src/main/java/org/apache/cxf/microprofile/client/cdi/CDIFacade.java`
#### Snippet
```java
            return nullableOptional(() -> CDIUtils.getCurrentBeanManager());
        } catch (Throwable t) {
            t.printStackTrace();
            return Optional.ofNullable(null);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/CxfUndertowServlet.java`
#### Snippet
```java
            undertowHTTPDestination.doService(request.getServletContext(), request, response);
        } catch (IOException e) {
            e.printStackTrace();
            throw new ServletException(e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPHandler.java`
#### Snippet
```java

        } catch (Throwable t) {
            t.printStackTrace();
            if (undertowExchange.isResponseChannelAvailable()) {
                undertowExchange.setStatusCode(500);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-undertow/src/main/java/org/apache/cxf/transport/http_undertow/UndertowHTTPServerEngineFactory.java`
#### Snippet
```java
                ref.stop();
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/security/src/main/java/org/apache/cxf/rt/security/claims/Claim.java`
#### Snippet
```java
            super.clone(); // Checkstyle requires this call
        } catch (CloneNotSupportedException e) {
            e.printStackTrace();
        }
        return new Claim(this);
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        } catch (NoSuchMethodException e) {
            //not using OSGi apparently
            e.printStackTrace();
        } catch (Throwable e) {
            //ignore
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBeanLocator.java`
#### Snippet
```java
        } catch (Throwable e) {
            //ignore
            e.printStackTrace();
            LOG.log(Level.FINE, "Could not get services for " + type.getName(), e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/URIParserUtil.java`
#### Snippet
```java
                url = file.toURI().toURL();
            } catch (MalformedURLException e) {
                e.printStackTrace();
            }
            if (url != null) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/util/Compiler.java`
#### Snippet
```java
        } catch (IOException e) {
            System.err.print("[ERROR] IOException during compiling.");
            e.printStackTrace();
        }
        return ret;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/common/injection/ResourceInjector.java`
#### Snippet
```java
            }
        } catch (IllegalAccessException e) {
            e.printStackTrace();
            LOG.severe("FAILED_TO_INJECT_FIELD");
        } finally {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamReader.java`
#### Snippet
```java
            return StaxUtils.toString(document);
        } catch (Throwable t) {
            t.printStackTrace();
            return super.toString();
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/staxutils/W3CDOMStreamWriter.java`
#### Snippet
```java
            return StaxUtils.toString(document);
        } catch (Throwable t) {
            t.printStackTrace();
            return super.toString();
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/databinding/AbstractWrapperHelper.java`
#### Snippet
```java
            return wrapperObject;
        } catch (Exception ex) {
            ex.printStackTrace();
            throw new Fault(ex.getCause());
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `core/src/main/java/org/apache/cxf/interceptor/security/SecureAnnotationsInterceptor.java`
#### Snippet
```java
            annotationClassName = name;
        } catch (Throwable ex) {
            ex.printStackTrace();
            throw new IllegalArgumentException("Annotation class " + name + " is not available");
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-netty/netty-server/src/main/java/org/apache/cxf/transport/http/netty/server/NettyHttpServerEngineFactory.java`
#### Snippet
```java
            } catch (Exception e) {
                // TODO Auto-generated catch block
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduitFactory.java`
#### Snippet
```java
            client.close();
        } catch (IOException e1) {
            e1.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex2) {
                    ex2.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
                } catch (GeneralSecurityException e) {
                    // TODO Auto-generated catch block
                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
            OPERATION_METHODS.put(WSTRUST_REQUESTTYPE_REQUESTCOLLECTION, m);
        } catch (Exception ex) {
            ex.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/sts/provider/SecurityTokenServiceProvider.java`
#### Snippet
```java
            operationMap.put(WSTRUST_REQUESTTYPE_ISSUE, issueSingleOperation);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex2) {
                    ex2.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/AbstractJAXRSFactoryBean.java`
#### Snippet
```java
            return bi;
        } catch (BusException ex) {
            ex.printStackTrace();
            //do nothing
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ExceptionUtils.java`
#### Snippet
```java
                } catch (Throwable mapperEx) {
                    inMessage.getExchange().put(JAXRSUtils.EXCEPTION_FROM_MAPPER, "true");
                    mapperEx.printStackTrace();
                    return Response.serverError().build();
                }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
            return isApplication ? provider : instance;
        } catch (InstantiationException ex) {
            ex.printStackTrace();
            throw new ServletException("Resource class " + cls.getName()
                                       + " can not be instantiated");
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
                                       + " can not be instantiated");
        } catch (IllegalAccessException ex) {
            ex.printStackTrace();
            throw new ServletException("Resource class " + cls.getName()
                                       + " can not be instantiated due to IllegalAccessException");
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
                                       + " can not be instantiated due to IllegalAccessException");
        } catch (InvocationTargetException ex) {
            ex.printStackTrace();
            throw new ServletException("Resource class " + cls.getName()
                                       + " can not be instantiated due to InvocationTargetException");
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/interceptor/JAXRSOutInterceptor.java`
#### Snippet
```java
        } catch (Exception ex) {
            LOG.severe("Problem with writing the data to the output stream");
            ex.printStackTrace();
            throw new RuntimeException(ex);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/JaxWsClientProxy.java`
#### Snippet
```java
                return exception;
            } catch (SOAPException e) {
                e.printStackTrace();
                return new WebServiceException(ex);
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/HandlerChainInvoker.java`
#### Snippet
```java
            }
        } catch (SOAPException e) {
            e.printStackTrace();
            // do nothing
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerFaultOutInterceptor.java`
#### Snippet
```java
            } catch (SOAPException e) {
                // do nothing
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerFaultInInterceptor.java`
#### Snippet
```java
            xmlReader.nextTag(); // move past body tag
        } catch (SOAPException | XMLStreamException e) {
            e.printStackTrace();
        }
        return xmlReader;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
                    SAAJInInterceptor.replaceHeaders(msg, message);
                } catch (SOAPException e) {
                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/soap/SOAPHandlerInterceptor.java`
#### Snippet
```java
            xmlReader.nextTag(); // move past body tag
        } catch (SOAPException | XMLStreamException e) {
            e.printStackTrace();
        }
        return xmlReader;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/JAXWSHttpSpiDestination.java`
#### Snippet
```java
            serviceRequest(req, resp);
        } catch (Exception e) {
            e.printStackTrace();
        } finally {
            if (origBus != bus) {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsEndpointImpl.java`
#### Snippet
```java
            } catch (Exception e) {
                //ignore
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/udp/src/main/java/org/apache/cxf/transport/udp/UDPDestination.java`
#### Snippet
```java
                    queue.execute(() -> getMessageObserver().onMessage(m));
                } catch (IOException ex) {
                    ex.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/ManagedRMEndpoint.java`
#### Snippet
```java
            proxy.lastMessage(ss);
        } catch (RMException e) {
            e.printStackTrace();
            throw new JMException("Error closing sequence: " + e.getMessage());
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/soap/SoapFaultFactory.java`
#### Snippet
```java
        } catch (Exception ex) {
            LogUtils.log(LOG, Level.SEVERE, "MARSHAL_FAULT_DETAIL_EXC", ex);
            ex.printStackTrace();
        }
        return fault;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/IDLToWSDLPlugin.java`
#### Snippet
```java
                    doneFile.createNewFile();
                } catch (Throwable e) {
                    e.printStackTrace();
                    throw new MojoExecutionException(e.getMessage(), e);
                }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `maven-plugins/corba/src/main/java/org/apache/cxf/maven_plugin/corba/maven/plugins/WSDLToIDLPlugin.java`
#### Snippet
```java
                        doneFile.createNewFile();
                    } catch (Throwable e) {
                        e.printStackTrace();
                        throw new MojoExecutionException(e.getMessage(), e);
                    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/MAPAggregatorImpl.java`
#### Snippet
```java
                observer.onMessage(inMessage);
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/AbstractDOMProvider.java`
#### Snippet
```java
                    inDoc = cx.newObject(scope, "XML", args);
                } catch (Exception ex) {
                    ex.printStackTrace();
                }
            } else {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/frontend/js/src/main/java/org/apache/cxf/js/rhino/AbstractDOMProvider.java`
#### Snippet
```java
            }
        } catch (Exception ex) {
            ex.printStackTrace();
        } finally {
            Context.exit();
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/mtom_xop/TestMtomProviderImpl.java`
#### Snippet
```java

        } catch (Exception e) {
            e.printStackTrace();
        }
        return null;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/EmbeddedJMSBrokerLauncher.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
            launchers.add(0, sl);
        } catch (IOException ex) {
            ex.printStackTrace();
            fail("failed to launch server " + clz);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
            launchers.add(0, sl);
        } catch (IOException ex) {
            ex.printStackTrace();
            fail("failed to launch server " + base);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
                sl.signalStop();
            } catch (IOException ex) {
                ex.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
                passed = passed && sl.stopServer();
            } catch (IOException ex) {
                ex.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/AbstractClientServerTestBase.java`
#### Snippet
```java
            launchers.add(0, sl);
        } catch (IOException ex) {
            ex.printStackTrace();
            fail("failed to launch server " + clz);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
                serverIsReady = true;
            } catch (Throwable ex) {
                ex.printStackTrace();
                serverLaunchFailed = true;
            } finally {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
                        }
                    } catch (InterruptedException e) {
                        e.printStackTrace();
                    }
                }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
                return inProcessServer.stopInProcess();
            } catch (Exception ex) {
                ex.printStackTrace();
                throw new IOException(ex.getMessage());
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            ret = process.exitValue();
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        return ret;
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            } catch (IOException ex) {
                if (!ex.getMessage().contains("Stream closed")) {
                    ex.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `testutils/src/main/java/org/apache/cxf/testutil/common/ServerLauncher.java`
#### Snippet
```java
            } catch (IOException ex) {
                if (!ex.getMessage().contains("Stream closed")) {
                    ex.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/CXFWorkAdapter.java`
#### Snippet
```java
        LOG.severe("root cause is:" + e.getException().getMessage());

        e.getException().printStackTrace();
    }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/WorkManagerThreadPool.java`
#### Snippet
```java
            return true;
        } catch (WorkException e) {
            e.printStackTrace();
            return false;
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/jca/src/main/java/org/apache/cxf/jca/cxf/JCABusFactory.java`
#### Snippet
```java
            }
        } catch (Exception e) {
            e.printStackTrace();
            throw new ResourceException(new Message("FAIL_TO_START_EJB_SERVANTS", BUNDLE).toString(), e);
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/jca/src/main/java/org/apache/cxf/jca/core/resourceadapter/ResourceAdapterInternalException.java`
#### Snippet
```java
            if (null != LOGGER.getLevel()
                 && LOGGER.getLevel().intValue() < Level.INFO.intValue()) {
                cause.printStackTrace();
            }
            LOGGER.warning(cause.toString());
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
            } catch (Throwable t) {
                // log
                t.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketDestinationFactory.java`
#### Snippet
```java
            } catch (Throwable t) {
                // log
                t.printStackTrace();
            }
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/UndertowWebSocketDestination.java`
#### Snippet
```java
                        handleNormalRequest(request, response);
                    } catch (Exception ex) {
                        ex.printStackTrace();
                    }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
                    } catch (UnsupportedEncodingException e) {
                        // TODO Auto-generated catch block
                        e.printStackTrace();
                    }
                }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        } catch (IOException e) {
            // TODO Auto-generated catch block
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketServletDestination.java`
#### Snippet
```java
            } catch (ServletException e) {
                // TODO Auto-generated catch block
                e.printStackTrace();
            }
        } else {
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketUndertowDestination.java`
#### Snippet
```java
                            channel.resumeReceives();
                        } catch (Exception e) {
                            e.printStackTrace();
                        }
                    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapTransportFactory.java`
#### Snippet
```java

        } catch (WSDLException e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/saaj/SAAJUtils.java`
#### Snippet
```java
                        method.invoke(e, "xmlns:" + s);
                    } catch (Exception ex) {
                        ex.printStackTrace();
                    }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SoapBindingFactory.java`
#### Snippet
```java
            createSoapBinding(info);
        } catch (WSDLException e) {
            e.printStackTrace();
        }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
                } catch (Throwable e) {

                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/interceptor/PersistOutInterceptor.java`
#### Snippet
```java
            } catch (Exception e) {
                // TODO: handle exception
                e.printStackTrace();

            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/management/src/main/java/org/apache/cxf/management/utils/ManagementConsole.java`
#### Snippet
```java
                    System.out.println("\t\t" + attrs[i].getName() + " = " + o);
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpClientHandler.java`
#### Snippet
```java
            request.getCxfResponseCallback().error(new IOException(cause));
        } else {
            cause.printStackTrace();
            ctx.close();
        }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/transports/http-netty/netty-client/src/main/java/org/apache/cxf/transport/http/netty/client/NettyHttpConduit.java`
#### Snippet
```java
                    isAsync = false; // don't trigger another start on next block. :-)
                } catch (Exception ex2) {
                    ex2.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/SubscriptionEndNotificationTask.java`
#### Snippet
```java

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rt/ws/eventing/src/main/java/org/apache/cxf/ws/eventing/backend/notification/EventSinkInterfaceNotificationTask.java`
#### Snippet
```java
            method.invoke(proxy, param);
        } catch (Throwable e) {
            e.printStackTrace();
        }
    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `maven-plugins/codegen-plugin/src/main/java/org/apache/cxf/maven_plugin/AbstractCodegenMojo.java`
#### Snippet
```java
            CommandLineUtils.executeCommandLine(cmd, null, consumer);
        } catch (Exception e2) {
            e2.printStackTrace();
        }

```

## RuleId[ruleID=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/JavascriptUtils.java`
#### Snippet
```java
    public String getDefaultValueForSimpleType(XmlSchemaType type) {
        String val = DEFAULT_VALUE_FOR_SIMPLE_TYPE.get(type.getName());
        if (val == null) { // ints and such return the appropriate 0.
            return "''";
        }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
            SessionHandler sh = configureSession();

            if (securityHandler != null) {
                //use the securityHander which already wrap the jetty http handler
                sh.setHandler(securityHandler);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/transports/http-jetty/src/main/java/org/apache/cxf/transport/http_jetty/JettyHTTPServerEngine.java`
#### Snippet
```java
        } else {
            // otherwise, just the one.
            if (securityHandler != null) {
                //use the securityHander which already wrap the jetty http handler
                context.setHandler(securityHandler);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/rs/description-openapi-v3/src/main/java/org/apache/cxf/jaxrs/openapi/OpenApiFeature.java`
#### Snippet
```java
            }

            if (fallback == null) {
                return false;
            }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/src/main/java/org/apache/cxf/helpers/NSDecl.java`
#### Snippet
```java

    public NSDecl(String pfx, String ur) {
        if (pfx == null) {
            this.prefix = "".intern();
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBContextCache.java`
#### Snippet
```java
                        if (key != null && key.containsAll(classes)) {
                            cachedContextAndSchemasInternalMap = k.getValue();
                            if (defaultNs != null) {
                                cachedContextAndSchemasInternal = cachedContextAndSchemasInternalMap.get(defaultNs);
                            } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
                URI uri = new URI(ns);
                String host = uri.getHost();
                if (host != null) {
                    cid = host;
                } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsCreatePullPoint.java`
#### Snippet
```java
                                EndpointManager epManager) {
        super(name, connectionFactory);
        if (epManager == null) {
            manager = new JaxwsEndpointManager();
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `services/wsn/wsn-core/src/main/java/org/apache/cxf/wsn/services/JaxwsNotificationBroker.java`
#### Snippet
```java
                                   EndpointManager epManager) {
        super(name, connectionFactory);
        if (epManager == null) {
            manager = new JaxwsEndpointManager();
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            || WSS4JConstants.SAML2_NS.equals(tokenType)) {
            sig.setCustomTokenValueType(WSS4JConstants.WSS_SAML2_KI_VALUE_TYPE);
        } else if (tokenType != null) {
            sig.setCustomTokenValueType(tokenType);
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/JMSConfiguration.java`
#### Snippet
```java

    public String getConduitSelectorPrefix() {
        if (conduitSelectorPrefix == null) {
            return "";
        }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/JAXRSInvoker.java`
#### Snippet
```java
        } catch (WebApplicationException ex) {
            responseList = checkExchangeForResponse(exchange);
            if (responseList != null) {
                return responseList;
            }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            for (int x = 0; x < source.getNamespaceCount(); x++) {
                String pfx = source.getNamespacePrefix(x);
                if (pfx == null) {
                    nsMap.remove("");
                } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java

    public void writeWString(String s) throws CorbaBindingException {
        if (s == null) {
            stream.write_wstring("");
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/runtime/CorbaObjectWriter.java`
#### Snippet
```java

    public void writeString(String s) throws CorbaBindingException {
        if (s == null) {
            stream.write_string("");
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `rt/ws/policy/src/main/java/org/apache/cxf/ws/policy/attachment/wsdl11/Wsdl11AttachmentPolicyProvider.java`
#### Snippet
```java
        String uri = ref.getURI().substring(1);
        String absoluteURI = di.getBaseURI();
        if (absoluteURI == null) {
            absoluteURI = Integer.toString(di.hashCode()) + ref.getURI();
        } else {
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
                XmlType.class);

        if (extensibleAttributes == null) {
            return defaultValue;
        }
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/java5/AnnotationReader.java`
#### Snippet
```java
                XmlType.class);

        if (extensibleElements == null) {
            return defaultValue;
        }
```

## RuleId[ruleID=Anonymous2MethodRef]
### Anonymous2MethodRef
Anonymous new java.security.PrivilegedAction() can be replaced with method reference
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Address.java`
#### Snippet
```java
    private static Proxy chooseProxy(URI uri) {
        ProxySelector sel = java.security.AccessController
            .doPrivileged(new java.security.PrivilegedAction<ProxySelector>() {
                @Override
                public ProxySelector run() {
```

### Anonymous2MethodRef
Anonymous new ApplicationListener() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBus.java`
#### Snippet
```java
        ctx = (AbstractApplicationContext)applicationContext;
        @SuppressWarnings("rawtypes")
        ApplicationListener listener = new ApplicationListener() {
            public void onApplicationEvent(ApplicationEvent event) {
                SpringBus.this.onApplicationEvent(event);
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBusFactory.java`
#### Snippet
```java
            if (r != null) {
                exists = AccessController
                    .doPrivileged(new PrivilegedAction<Boolean>() {
                        public Boolean run() {
                            return r.exists();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/i18n/BundleUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedExceptionAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/bus/extension/ExtensionManagerImpl.java`
#### Snippet
```java
        while (urls.hasMoreElements()) {
            final URL url = urls.nextElement();
            try (InputStream is = AccessController.doPrivileged(new PrivilegedExceptionAction<InputStream>() {
                    public InputStream run() throws Exception {
                        return url.openStream();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/util/ProxyClassLoaderCache.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java

    public static Field[] getDeclaredFields(final Class<?> cls) {
        return AccessController.doPrivileged(new PrivilegedAction<Field[]>() {
            public Field[] run() {
                return cls.getDeclaredFields();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/util/ReflectionUtil.java`
#### Snippet
```java

    public static Method[] getDeclaredMethods(final Class<?> cls) {
        return AccessController.doPrivileged(new PrivilegedAction<Method[]>() {
            public Method[] run() {
                return cls.getDeclaredMethods();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/logging/LogUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/classloader/ClassLoaderUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/helpers/XPathUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/helpers/DOMUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/common/jaxb/JAXBUtils.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                @Override
                public ClassLoader run() {
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `core/src/main/java/org/apache/cxf/resource/SecurityActions.java`
#### Snippet
```java
        }
        sm.checkPermission(permission);
        return AccessController.doPrivileged(new PrivilegedAction<Boolean>() {
            public Boolean run() {
                return file.exists();
```

### Anonymous2MethodRef
Anonymous new CXFResponseCallback() can be replaced with method reference
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            }

            CXFResponseCallback responseCallback = new CXFResponseCallback() {
                @Override
                public void responseReceived(HttpResponse response) {
```

### Anonymous2MethodRef
Anonymous new CXFResponseCallback() can be replaced with method reference
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            }

            CXFResponseCallback responseCallback = new CXFResponseCallback() {
                @Override
                public void responseReceived(HttpResponse response) {
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `rt/frontend/simple/src/main/java/org/apache/cxf/frontend/ClientProxyFactoryBean.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new Comparator() can be replaced with method reference
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
    private void sortMediaTypesUsingQualityFactor(List<MediaType> types) {
        if (types.size() > 1) {
            Collections.sort(types, new Comparator<MediaType>() {

                public int compare(MediaType mt1, MediaType mt2) {
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java

    private static Method[] getDeclaredMethods(final Class<?> c) {
        return AccessController.doPrivileged(new PrivilegedAction<Method[]>() {
            @Override
            public Method[] run() {
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/handler/AnnotationHandlerChainBuilder.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

### Anonymous2MethodRef
Anonymous new PrivilegedAction() can be replaced with method reference
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
        final SecurityManager sm = System.getSecurityManager();
        if (sm != null) {
            return AccessController.doPrivileged(new PrivilegedAction<ClassLoader>() {
                public ClassLoader run() {
                    return clazz.getClassLoader();
```

## RuleId[ruleID=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/Headers.java`
#### Snippet
```java
            String mappedName = HttpHeaderHelper.getHeaderKey(fname);
            List<String> values = headers.get(mappedName);
            if (values == null) {
                values = new ArrayList<>();
                headers.put(mappedName, values);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/src/main/java/org/apache/cxf/interceptor/FIStaxOutInterceptor.java`
#### Snippet
```java
                = CastUtils.cast((Map<?, ?>)message.get(Message.PROTOCOL_HEADERS));
            List<String> accepts = headers.get("Accept");
            if (accepts == null) {
                accepts = new ArrayList<>();
                headers.put("Accept", accepts);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `core/src/main/java/org/apache/cxf/configuration/spring/ConfigurerImpl.java`
#### Snippet
```java
                                Matcher matcher = Pattern.compile(name).matcher("");
                                List<MatcherHolder> m = wildCardBeanDefinitions.get(className);
                                if (m == null) {
                                    m = new ArrayList<>();
                                    wildCardBeanDefinitions.put(className, m);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/transports/http-hc/src/main/java/org/apache/cxf/transport/http/asyncclient/AsyncHTTPConduit.java`
#### Snippet
```java
            for (Header header : headers) {
                List<String> s = h.headerMap().get(header.getName());
                if (s == null) {
                    s = new ArrayList<>(1);
                    h.headerMap().put(header.getName(), s);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/transports/http-hc5/src/main/java/org/apache/cxf/transport/http/asyncclient/hc5/AsyncHTTPConduit.java`
#### Snippet
```java
            for (Header header : headers) {
                List<String> s = h.headerMap().get(header.getName());
                if (s == null) {
                    s = new ArrayList<>(1);
                    h.headerMap().put(header.getName(), s);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ConfigurationImpl.java`
#### Snippet
```java

        Map<Class<?>, Integer> metadata = providers.get(provider);
        if (metadata == null) {
            metadata = new HashMap<>();
            providers.put(provider, metadata);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/AsyncResponseImpl.java`
#### Snippet
```java
            Class<?> callbackCls = allCallbacks[i].getClass();
            Collection<Class<?>> knownCallbacks = map.get(callbackCls);
            if (knownCallbacks == null) {
                knownCallbacks = new HashSet<>();
                map.put(callbackCls, knownCallbacks);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/model/AbstractResourceInfo.java`
#### Snippet
```java

        List<Field> fields = theFields.get(serviceClass);
        if (fields == null) {
            fields = new ArrayList<>();
            theFields.put(serviceClass, fields);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/servlet/CXFNonSpringJaxrsServlet.java`
#### Snippet
```java
                }
                List<String> list = map.get(key);
                if (list == null) {
                    list = new LinkedList<>();
                    map.put(key, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/jaxws/support/JaxWsServiceConfiguration.java`
#### Snippet
```java
        // we could really use a centralized location for this.
        Annotation[][] annotations = methodAnnotationCache.get(method);
        if (annotations == null) {
            annotations = method.getParameterAnnotations();
            methodAnnotationCache.put(method, annotations);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/ServiceWSDLBuilder.java`
#### Snippet
```java
        }
        String prefix = ns2prefix.get(ns);
        if (prefix == null) {
            prefix = getNewPrefix();
            ns2prefix.put(ns, prefix);
```

### Java8MapApi
Can be replaced with single 'Map.merge' method call
in `rt/bindings/corba/src/main/java/org/apache/cxf/binding/corba/utils/CorbaBindingHelper.java`
#### Snippet
```java
        Integer count = orbUseCount.get(getORBNameFromAddress(address));

        if (count == null) {
            orbUseCount.put(getORBNameFromAddress(address), 1);
        } else {
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/interceptor/SoapPreProtocolOutInterceptor.java`
#### Snippet
```java
     */
    private void ensureMimeHeaders(SoapMessage message) {
        if (message.get(MIME_HEADERS) == null) {
            message.put(MIME_HEADERS, new HashMap<String, List<String>>());
        }
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/features/throttling/src/main/java/org/apache/cxf/throttling/ThrottlingResponseInterceptor.java`
#### Snippet
```java
            for (Map.Entry<String, String> e : rsp.getResponseHeaders().entrySet()) {
                List<String> r = headers.get(e.getKey());
                if (r == null) {
                    r = new ArrayList<>();
                    headers.put(e.getKey(), r);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
                for (KeyOperation op : ops) {
                    List<JsonWebKey> list = map.get(op);
                    if (list == null) {
                        list = new LinkedList<>();
                        map.put(op, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jwk/JsonWebKeys.java`
#### Snippet
```java
            if (type != null) {
                List<JsonWebKey> list = map.get(type);
                if (list == null) {
                    list = new LinkedList<>();
                    map.put(type, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/encoded/SoapRefRegistry.java`
#### Snippet
```java
        } else {
            List<SoapRef> list = unresolvedRefs.get(id);
            if (list == null) {
                list = new ArrayList<>();
                unresolvedRefs.put(id, list);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
            String ns = t.getSchemaType().getNamespaceURI();
            Set<AegisType> types = tns2Type.get(ns);
            if (types == null) {
                types = new HashSet<>();
                tns2Type.put(ns, types);
```

## RuleId[ruleID=Convert2Lambda]
### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/DestinationRegistryImpl.java`
#### Snippet
```java
        List<AbstractHTTPDestination> dest2 = new LinkedList<>(
                getDestinations());
        Collections.sort(dest2, new Comparator<AbstractHTTPDestination>() {
            public int compare(AbstractHTTPDestination o1, AbstractHTTPDestination o2) {
                InterfaceInfo i1 = o1.getEndpointInfo().getInterface();
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java

        protected void handleResponseOnWorkqueue(boolean allowCurrentThread, boolean forceWQ) throws IOException {
            Runnable runnable = new Runnable() {
                public void run() {
                    try {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java
                        final Executor ex2 = ex;
                        final Runnable origRunnable = runnable;
                        runnable = new Runnable() {
                            public void run() {
                                outMessage.getExchange().put(Executor.class.getName()
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalDestination.java`
#### Snippet
```java
                }

                final Runnable receiver = new Runnable() {
                    public void run() {
                        if (exchange != null) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/local/src/main/java/org/apache/cxf/transport/local/LocalConduit.java`
#### Snippet
```java
            inMsg.put(IN_CONDUIT, conduit);

            final Runnable receiver = new Runnable() {
                public void run() {
                    ExchangeImpl ex = new ExchangeImpl();
```

### Convert2Lambda
Anonymous new ApplicationListener() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/bus/spring/SpringBus.java`
#### Snippet
```java
        ctx = (AbstractApplicationContext)applicationContext;
        @SuppressWarnings("rawtypes")
        ApplicationListener listener = new ApplicationListener() {
            public void onApplicationEvent(ApplicationEvent event) {
                SpringBus.this.onApplicationEvent(event);
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/rs/description/src/main/java/org/apache/cxf/jaxrs/model/wadl/WadlGenerator.java`
#### Snippet
```java
    protected List<OperationResourceInfo> sortOperationsByPath(Set<OperationResourceInfo> ops) {
        List<OperationResourceInfo> opsWithSamePath = new LinkedList<>(ops);
        Collections.sort(opsWithSamePath, new Comparator<OperationResourceInfo>() {

            @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java

        final ClassLoader loader = Thread.currentThread().getContextClassLoader();
        Runnable r = new Runnable() {
            public void run() {
                ClassLoaderHolder orig = ClassLoaderUtils.setThreadContextClassloader(loader);
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/workqueue/AutomaticWorkQueueImpl.java`
#### Snippet
```java
                group = new ThreadGroup(group.getParent(), name + "-workqueue");
            }
            Runnable wrapped = new Runnable() {
                public void run() {
                    approxThreadCount.incrementAndGet();
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/endpoint/ClientImpl.java`
#### Snippet
```java
                        .containsKey(Executor.class.getName() + ".USING_SPECIFIED")) {

                        executor.execute(new Runnable() {
                            public void run() {
                                ClientImpl.this.onMessage(message);
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/interceptor/OneWayProcessorInterceptor.java`
#### Snippet
```java
                    synchronized (lock) {
                        message.getExchange().getBus().getExtension(WorkQueueManager.class)
                            .getAutomaticWorkQueue().execute(new Runnable() {
                                public void run() {
                                    synchronized (lock) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `core/src/main/java/org/apache/cxf/interceptor/ServiceInvokerInterceptor.java`
#### Snippet
```java
        final Invoker invoker = service.getInvoker();

        Runnable invocation = new Runnable() {

            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/jms/src/main/java/org/apache/cxf/transport/jms/continuations/JMSContinuation.java`
#### Snippet
```java

    protected void createTimerTask(long timeout) {
        workQueue.schedule(new Runnable() {
            public void run() {
                synchronized (JMSContinuation.this) {
```

### Convert2Lambda
Anonymous new Callable() can be replaced with lambda
in `integration/tracing/tracing-brave/src/main/java/org/apache/cxf/tracing/brave/BraveTracerContext.java`
#### Snippet
```java
    @Override
    public <T> Callable<T> wrap(final String description, final Traceable<T> traceable) {
        final Callable<T> callable = new Callable<T>() {
            @Override
            public T call() throws Exception {
```

### Convert2Lambda
Anonymous new FlowableOnSubscribe() can be replaced with lambda
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/FlowableRxInvokerImpl.java`
#### Snippet
```java
    
    private <T> Flowable<T> create(Supplier<T> supplier) {
        Flowable<T> flowable = Flowable.create(new FlowableOnSubscribe<T>() {
            @Override
            public void subscribe(FlowableEmitter<T> emitter) throws Exception {
```

### Convert2Lambda
Anonymous new ObservableOnSubscribe() can be replaced with lambda
in `rt/rs/extensions/rx3/src/main/java/org/apache/cxf/jaxrs/rx3/client/ObservableRxInvokerImpl.java`
#### Snippet
```java
    
    private <T> Observable<T> create(Supplier<T> supplier) {
        Observable<T> observable = Observable.create(new ObservableOnSubscribe<T>() {
            @Override
            public void subscribe(ObservableEmitter<T> emitter) throws Exception {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/HttpHeadersImpl.java`
#### Snippet
```java
    private void sortMediaTypesUsingQualityFactor(List<MediaType> types) {
        if (types.size() > 1) {
            Collections.sort(types, new Comparator<MediaType>() {

                public int compare(MediaType mt1, MediaType mt2) {
```

### Convert2Lambda
Anonymous new Comparator\>() can be replaced with lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/ResourceUtils.java`
#### Snippet
```java
            }
        }
        Collections.sort(cs, new Comparator<Constructor<?>>() {

            public int compare(Constructor<?> c1, Constructor<?> c2) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/ws/rm/src/main/java/org/apache/cxf/ws/rm/Proxy.java`
#### Snippet
```java
        if (isServer) {
            LOG.fine("sending CreateSequenceRequest from server side");
            Runnable r = new Runnable() {
                public void run() {
                    try {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
    private void sortItems(final XmlSchemaSequence seq, final String[] propertyOrder) {
        final List<String> propList = Arrays.asList(propertyOrder);
        Collections.sort(seq.getItems(), new Comparator<XmlSchemaSequenceMember>() {
            public int compare(XmlSchemaSequenceMember o1, XmlSchemaSequenceMember o2) {
                XmlSchemaElement element1 = (XmlSchemaElement)o1;
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBSchemaInitializer.java`
#### Snippet
```java
    //sort to Alphabetical order
    private void sort(final XmlSchemaSequence seq) {
        Collections.sort(seq.getItems(), new Comparator<XmlSchemaSequenceMember>() {
            public int compare(XmlSchemaSequenceMember o1, XmlSchemaSequenceMember o2) {
                XmlSchemaElement element1 = (XmlSchemaElement)o1;
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/ws/addr/src/main/java/org/apache/cxf/ws/addressing/impl/InternalContextUtils.java`
#### Snippet
```java
                        try {
                            // ... and resume on executor thread
                            getExecutor(inMessage).execute(new Runnable() {
                                public void run() {
                                    inMessage.getInterceptorChain().resume();
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            XmlAccessorOrder xmlAccessorOrder = cls.getAnnotation(XmlAccessorOrder.class);
            if (xmlAccessorOrder != null && xmlAccessorOrder.value().equals(XmlAccessOrder.ALPHABETICAL)) {
                Collections.sort(combinedMembers, new Comparator<Member>() {
                    public int compare(Member m1, Member m2) {
                        return m1.getName().compareTo(m2.getName());
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/databinding/jaxb/src/main/java/org/apache/cxf/jaxb/JAXBEncoderDecoder.java`
#### Snippet
```java
            if (xmlType != null && xmlType.propOrder().length > 1 && !xmlType.propOrder()[0].isEmpty()) {
                final List<String> orderList = Arrays.asList(xmlType.propOrder());
                Collections.sort(combinedMembers, new Comparator<Member>() {
                    public int compare(Member m1, Member m2) {
                        String m1Name = getName(m1);
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
        List<MediaType> all = intersectMimeTypes(acceptTypes, producesTypes, true, checkDistance);
        if (all.size() > 1) {
            all.sort(new Comparator<MediaType>() {

                public int compare(MediaType mt1, MediaType mt2) {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/utils/JAXRSUtils.java`
#### Snippet
```java
    public static List<MediaType> sortMediaTypes(List<MediaType> types, final String qs) {
        if (types.size() > 1) {
            Collections.sort(types, new Comparator<MediaType>() {

                public int compare(MediaType mt1, MediaType mt2) {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/undertow/UndertowWebSocketDestination.java`
#### Snippet
```java

        private void handleReceivedMessage(WebSocketChannel channel, Object message, HttpServerExchange exchange) {
            executor.execute(new Runnable() {

                @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/jetty11/Jetty11WebSocketDestination.java`
#### Snippet
```java
        // may corrupt the byte array after this method is returned (i.e., before the data is returned in
        // the executor's thread.
        executeServiceTask(new Runnable() {
            @Override
            public void run() {
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/atmosphere/AtmosphereWebSocketUndertowDestination.java`
#### Snippet
```java

        private void handleReceivedMessage(WebSocketChannel channel, Object message, HttpServerExchange exchange) {
            executor.execute(new Runnable() {

                @Override
```

### Convert2Lambda
Anonymous new Runnable() can be replaced with lambda
in `rt/rs/security/jose-parent/jose/src/main/java/org/apache/cxf/rs/security/jose/jws/JwsJsonOutputStream.java`
#### Snippet
```java
        //TODO: Review if it is at least theoretically possible that a given b[] region
        // can be modified in a subsequent write which might affect the signature calculation
        executor.execute(new Runnable() {
            public void run() {
                for (JwsSignature signature : signatures) {
```

### Convert2Lambda
Anonymous new Callable() can be replaced with lambda
in `integration/tracing/tracing-opentracing/src/main/java/org/apache/cxf/tracing/opentracing/OpenTracingContext.java`
#### Snippet
```java
    @Override
    public <T> Callable<T> wrap(final String description, final Traceable<T> traceable) {
        final Callable<T> callable = new Callable<T>() {
            @Override
            public T call() throws Exception {
```

### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/type/basic/BeanTypeInfo.java`
#### Snippet
```java
            descriptors = new PropertyDescriptor[0];
        }
        Arrays.sort(descriptors, new Comparator<PropertyDescriptor>() {
            public int compare(PropertyDescriptor o1, PropertyDescriptor o2) {
                return o1.getName().compareTo(o2.getName());
```

## RuleId[ruleID=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
                } catch (UnsupportedConstruct usc) {
                    LOG.warning(usc.toString());
                    continue; // it could be empty, but the style checker
                    // would complain.
                }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/javascript/javascript-rt/src/main/java/org/apache/cxf/javascript/types/SchemaJavascriptBuilder.java`
#### Snippet
```java
            } catch (UnsupportedConstruct usc) {
                LOG.warning(usc.getMessage());
                continue; // it could be empty, but the style checker
                // would complain.
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `core/src/main/java/org/apache/cxf/interceptor/security/NamePasswordCallbackHandler.java`
#### Snippet
```java
            Callback callback = callbacks[i];
            if (handleCallback(callback)) {
                continue;
            } else if (callback instanceof NameCallback) {
                ((NameCallback) callback).setName(username);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `core/src/main/java/org/apache/cxf/interceptor/security/NameDigestPasswordCallbackHandler.java`
#### Snippet
```java
            Callback callback = callbacks[i];
            if (handleCallback(callback)) {
                continue;
            } else if (callback instanceof NameCallback) {
                ((NameCallback) callback).setName(username);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/SamlTokenInterceptor.java`
#### Snippet
```java
                                if (!DOMSAMLUtil.checkSenderVouches(assertionWrapper, tlsCerts, null, null)) {
                                    ai.setNotAsserted("Assertion fails sender-vouches requirements");
                                    continue;
                                }
                            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/EncryptedTokenPolicyValidator.java`
#### Snippet
```java
                        "The received token does not match the encrypted supporting token requirement"
                    );
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedTokenPolicyValidator.java`
#### Snippet
```java
                        "The received token does not match the signed supporting token requirement"
                    );
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SecurityContextTokenPolicyValidator.java`
#### Snippet
```java
                    "The received token does not match the token inclusion requirement"
                );
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/WSS11PolicyValidator.java`
#### Snippet
```java
                    "Signature Confirmation policy validation failed"
                );
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SignedEncryptedTokenPolicyValidator.java`
#### Snippet
```java
                        "The received token does not match the signed encrypted supporting token requirement"
                    );
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SymmetricBindingPolicyValidator.java`
#### Snippet
```java
            if (!checkTokens(binding, ai, parameters.getAssertionInfoMap(), hasDerivedKeys,
                             parameters.getSignedResults(), parameters.getEncryptedResults())) {
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/SamlTokenPolicyValidator.java`
#### Snippet
```java
                                                        parameters.getSignedResults())) {
                        ai.setNotAsserted("Assertion fails sender-vouches requirements");
                        continue;
                    }
                }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/KerberosTokenPolicyValidator.java`
#### Snippet
```java
                    asserted = false;
                    ai.setNotAsserted("An incorrect Kerberos Token Type is detected");
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/X509TokenPolicyValidator.java`
#### Snippet
```java
            if (!checkTokenType(x509TokenPolicy.getTokenType(), bstResults, parameters.getSignedResults())) {
                ai.setNotAsserted("An incorrect X.509 Token Type is detected");
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/UsernameTokenPolicyValidator.java`
#### Snippet
```java

            if (!checkTokens(usernameTokenPolicy, ai, parameters.getUsernameTokenResults())) {
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/ConcreteSupportingTokenPolicyValidator.java`
#### Snippet
```java
                        "The received token does not match the supporting token requirement"
                    );
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/IssuedTokenPolicyValidator.java`
#### Snippet
```java
                asserted = false;
                ai.setNotAsserted("Error in validating the IssuedToken policy");
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/IssuedTokenPolicyValidator.java`
#### Snippet
```java
                asserted = false;
                ai.setNotAsserted("Assertion fails holder-of-key requirements");
                continue;
            }

```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyvalidators/AsymmetricBindingPolicyValidator.java`
#### Snippet
```java
            if (!checkTokens(binding, ai, parameters.getAssertionInfoMap(), hasDerivedKeys,
                             parameters.getSignedResults(), parameters.getEncryptedResults())) {
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MediaTypeHeaderProvider.java`
#### Snippet
```java
                return false;
            default:
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/wsdl/src/main/java/org/apache/cxf/wsdl11/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
                        return true;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/ahc/AhcWebSocketConduit.java`
#### Snippet
```java
                    break;
                } else if (c == '\r') {
                    continue;
                } else {
                    sb.append((char)c);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/transports/websocket/src/main/java/org/apache/cxf/transport/websocket/WebSocketUtils.java`
#### Snippet
```java
                //just ignore the CR.  The next character SHOULD be an NL.  If not, we're
                //just going to discard this
                continue;
            } else {
                // just add to the buffer
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/bindings/soap/src/main/java/org/apache/cxf/binding/soap/SOAPBindingUtil.java`
#### Snippet
```java
                        previousOpStyle = style;
                    } else if (!"".equals(bindingStyle) && "".equals(style) && "".equals(previousOpStyle)) {
                        continue;
                    } else {
                        return true;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/management/src/main/java/org/apache/cxf/management/jmx/export/runtime/ModelMBeanAssembler.java`
#### Snippet
```java
                return methods[i];
            }
            continue;

        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
            if (col.getXmlSchemas().length > 1) {
                // someone has already filled in the types
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `rt/databinding/aegis/src/main/java/org/apache/cxf/aegis/databinding/AegisDatabinding.java`
#### Snippet
```java
                        Message message = new Message("MAPPED_CLASS_NOT_FOUND", LOG, className, key);
                        LOG.warning(message.toString());
                        continue;
                    }
                }
```

## RuleId[ruleID=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field 'cachedStream'
in `rt/transports/http/src/main/java/org/apache/cxf/transport/http/HTTPConduit.java`
#### Snippet
```java

        // This construction makes extending the HTTPConduit more easier
        protected WrappedOutputStream(WrappedOutputStream wos) {
            super(wos.threshold);
            this.outMessage = wos.outMessage;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'notFound'
in `core/src/main/java/org/apache/cxf/bus/extension/Extension.java`
#### Snippet
```java
    }

    public Extension(Extension ext) {
        className = ext.className;
        interfaceName = ext.interfaceName;
```

### CopyConstructorMissesField
Copy constructor does not copy fields 'next' and 'prev'
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java
            phaseIdx = p;
        }
        InterceptorHolder(InterceptorHolder p) {
            interceptor = p.interceptor;
            phaseIdx = p.phaseIdx;
```

### CopyConstructorMissesField
Copy constructor does not copy 5 fields
in `core/src/main/java/org/apache/cxf/phase/PhaseInterceptorChain.java`
#### Snippet
```java


    private PhaseInterceptorChain(PhaseInterceptorChain src) {
        isFineLogging = LOG.isLoggable(Level.FINE);

```

### CopyConstructorMissesField
Copy constructor does not copy 7 fields
in `core/src/main/java/org/apache/cxf/service/model/OperationInfo.java`
#### Snippet
```java
        setName(n);
    }
    OperationInfo(OperationInfo op) {
        intf = op.getInterface();
        setName(op.getName());
```

### CopyConstructorMissesField
Copy constructor does not copy field 'annotations'
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/ResponseBuilderImpl.java`
#### Snippet
```java
    }

    private ResponseBuilderImpl(ResponseBuilderImpl copy) {
        status = copy.status;
        statusSet = copy.statusSet;
```

## RuleId[ruleID=ThrowableNotThrown]
### ThrowableNotThrown
Result of `toNotAuthorizedException()` not thrown
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/TokenIntrospectionService.java`
#### Snippet
```java
        if (!sc.isSecure() && blockUnsecureRequests) {
            LOG.warning("Unsecure HTTP, Transport Layer Security is recommended");
            ExceptionUtils.toNotAuthorizedException(null,  null);
        }
        if (sc.getUserPrincipal() == null && blockUnauthorizedRequests) {
```

### ThrowableNotThrown
Result of `toNotAuthorizedException()` not thrown
in `rt/rs/security/oauth-parent/oauth2/src/main/java/org/apache/cxf/rs/security/oauth2/services/TokenIntrospectionService.java`
#### Snippet
```java
        if (sc.getUserPrincipal() == null && blockUnauthorizedRequests) {
            LOG.warning("Authenticated Principal is not available");
            ExceptionUtils.toNotAuthorizedException(null, null);
        }

```

## RuleId[ruleID=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `core/src/main/java/org/apache/cxf/message/AbstractWrappedMessage.java`
#### Snippet
```java
    }

    public boolean equals(Object o) {
        return message.equals(o);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `core/src/main/java/org/apache/cxf/staxutils/DepthXMLStreamReader.java`
#### Snippet
```java
    }

    public boolean equals(Object arg0) {
        return reader.equals(arg0);
    }
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `rt/frontend/jaxrs/src/main/java/org/apache/cxf/jaxrs/impl/MetadataMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o) {
        return m.equals(o);
    }
```

## RuleId[ruleID=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'p' to 'WSUsernameTokenPrincipalImpl'
in `rt/security-saml/src/main/java/org/apache/cxf/rt/security/saml/interceptor/WSS4JBasicAuthValidator.java`
#### Snippet
```java
        } else {
            Principal p = new WSUsernameTokenPrincipalImpl(policy.getUserName(), false);
            ((WSUsernameTokenPrincipalImpl)p).setPassword(policy.getPassword());
            sc = createSecurityContext(p);
        }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'o' to 'Artifact'
in `maven-plugins/java2wadl-plugin/src/main/java/org/apache/cxf/maven_plugin/javatowadl/ParseJavaDocMojo.java`
#### Snippet
```java
            for (Object o : this.mavenProject.getPluginArtifacts()) {
                if (o instanceof Artifact) {
                    Artifact artifact = (Artifact)o;
                    if ("cxf-java2wadl-plugin".equals(artifact.getArtifactId())) {
                        docletArtifact.setGroupId(artifact.getGroupId());
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'source' to 'AttachmentDataSource'
in `core/src/main/java/org/apache/cxf/attachment/AttachmentUtil.java`
#### Snippet
```java
        DataSource source = new AttachmentDataSource(ct, ins);
        if (!StringUtils.isEmpty(fileName)) {
            ((AttachmentDataSource)source).setName(FileUtils.stripPath(fileName));
        }
        att.setDataHandler(new DataHandler(source));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'receivedAssertion' to 'SamlAssertionWrapper'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/StaxSecurityContextInInterceptor.java`
#### Snippet
```java
                        if (receivedAssertion != null) {
                            ClaimCollection claims =
                                SAMLUtils.getClaims((SamlAssertionWrapper)receivedAssertion);
                            Set<Principal> roles =
                                SAMLUtils.parseRolesFromClaims(claims, roleAttributeName, null);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'provider' to 'Provider'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/PolicyBasedWSS4JOutInterceptor.java`
#### Snippet
```java
            try {
                if (useCustomProvider) {
                    ThreadLocalSecurityProvider.setProvider((Provider)provider);
                }
                handleMessageInternal(message);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'provider' to 'Provider'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JOutInterceptor.java`
#### Snippet
```java
            try {
                if (useCustomProvider) {
                    ThreadLocalSecurityProvider.setProvider((Provider)provider);
                }
                handleMessageInternal(message);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'provider' to 'Provider'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxInInterceptor.java`
#### Snippet
```java
                    public int next() throws XMLStreamException {
                        try {
                            ThreadLocalSecurityProvider.setProvider((Provider)provider);
                            return super.next();
                        } finally {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'provider' to 'Provider'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JStaxOutInterceptor.java`
#### Snippet
```java
            try {
                if (useCustomProvider) {
                    ThreadLocalSecurityProvider.setProvider((Provider)provider);
                }
                handleMessageInternal(message);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'provider' to 'Provider'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/WSS4JInInterceptor.java`
#### Snippet
```java
        try {
            if (useCustomProvider) {
                ThreadLocalSecurityProvider.setProvider((Provider)provider);
            }
            handleMessageInternal(msg);
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'bstToken' to 'X509Security'
in `rt/ws/security/src/main/java/org/apache/cxf/ws/security/wss4j/policyhandlers/AbstractBindingBuilder.java`
#### Snippet
```java
            X509Certificate encCert = getEncryptCert(crypto, encrUser);
            BinarySecurity bstToken = new X509Security(saaj.getSOAPPart());
            ((X509Security)bstToken).setX509Certificate(encCert);
            bstToken.addWSUNamespace();
            bstToken.setID(wssConfig.getIdAllocator().createSecureId("X509-", encCert));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'inMessage' to 'MessageImpl'
in `rt/frontend/jaxws/src/main/java/org/apache/cxf/transport/http_jaxws_spi/JAXWSHttpSpiDestination.java`
#### Snippet
```java
        setupMessage(inMessage, null, req.getServletContext(), req, resp);

        ((MessageImpl)inMessage).setDestination(this);

        exchange.setSession(new HTTPSession(req));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'binarySecurity' to 'X509Security'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/X509TokenValidator.java`
#### Snippet
```java
                if (x509Data.containsCertificate()) {
                    X509Certificate cert = x509Data.itemCertificate(0).getX509Certificate();
                    ((X509Security)binarySecurity).setX509Certificate(cert);
                }
            } catch (XMLSecurityException ex) {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'binarySecurity' to 'X509Security'
in `services/sts/sts-core/src/main/java/org/apache/cxf/sts/token/validator/X509TokenValidator.java`
#### Snippet
```java
            credential.setBinarySecurityToken(binarySecurity);
            if (crypto != null) {
                X509Certificate cert = ((X509Security)binarySecurity).getX509Certificate(crypto);
                credential.setCertificates(new X509Certificate[]{cert});
            }
```

