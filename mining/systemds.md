# systemds 
 
# Bad smells
I found 5848 bad smells with 891 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| UnusedAssignment | 1211 | false |
| DuplicatedCode | 496 | false |
| UnnecessaryModifier | 367 | true |
| FieldMayBeFinal | 361 | false |
| ConstantValue | 355 | false |
| StringConcatenationInsideStringBufferAppend | 335 | false |
| DataFlowIssue | 331 | false |
| UnnecessaryLocalVariable | 194 | true |
| UnnecessaryToStringCall | 189 | true |
| RedundantCast | 154 | false |
| IntegerMultiplicationImplicitCastToLong | 145 | false |
| StringBufferReplaceableByString | 123 | false |
| CommentedOutCode | 112 | false |
| JavadocReference | 92 | false |
| PointlessArithmeticExpression | 83 | false |
| CStyleArrayDeclaration | 69 | false |
| DuplicateBranchesInSwitch | 63 | false |
| FinalPrivateMethod | 60 | false |
| ConditionCoveredByFurtherCondition | 56 | false |
| SimplifiableConditionalExpression | 51 | false |
| ProtectedMemberInFinalClass | 47 | true |
| UnnecessaryReturn | 43 | true |
| StringOperationCanBeSimplified | 40 | false |
| ArraysAsListWithZeroOrOneArgument | 36 | false |
| TrivialIf | 33 | false |
| RedundantMethodOverride | 29 | false |
| NullableProblems | 28 | false |
| UnnecessaryCallToStringValueOf | 28 | false |
| EmptyStatementBody | 26 | false |
| FinalStaticMethod | 26 | false |
| ManualMinMaxCalculation | 26 | false |
| FinalMethodInFinalClass | 25 | false |
| IgnoreResultOfCall | 24 | false |
| UnnecessarySemicolon | 23 | false |
| JavadocDeclaration | 22 | false |
| UseBulkOperation | 22 | false |
| FieldCanBeLocal | 21 | false |
| SwitchStatementWithTooFewBranches | 19 | false |
| SynchronizeOnNonFinalField | 19 | false |
| DuplicateExpressions | 17 | false |
| InnerClassMayBeStatic | 17 | true |
| SynchronizationOnLocalVariableOrMethodParameter | 16 | false |
| UnnecessaryStringEscape | 16 | true |
| UnnecessaryUnicodeEscape | 15 | false |
| WrapperTypeMayBePrimitive | 13 | false |
| ManualArrayToCollectionCopy | 13 | false |
| SwitchStatementWithConfusingDeclaration | 13 | false |
| RefusedBequest | 13 | false |
| MismatchedCollectionQueryUpdate | 12 | false |
| PointlessBitwiseExpression | 12 | false |
| DoubleNegation | 12 | false |
| IfStatementWithIdenticalBranches | 12 | false |
| CopyConstructorMissesField | 12 | false |
| SimplifyStreamApiCallChains | 11 | false |
| DanglingJavadoc | 11 | false |
| IntegerDivisionInFloatingPointContext | 11 | false |
| UnpredictableBigDecimalConstructorCall | 10 | false |
| PointlessBooleanExpression | 10 | true |
| RedundantCollectionOperation | 10 | false |
| CatchMayIgnoreException | 10 | false |
| UnnecessaryContinue | 10 | false |
| TrivialStringConcatenation | 9 | false |
| SuspiciousToArrayCall | 9 | false |
| ManualArrayCopy | 9 | false |
| SuspiciousIndentAfterControlStatement | 9 | false |
| DuplicateCondition | 8 | false |
| ConditionalExpressionWithIdenticalBranches | 8 | false |
| OptionalGetWithoutIsPresent | 8 | false |
| UNUSED_IMPORT | 7 | false |
| RegExpSimplifiable | 6 | false |
| FuseStreamOperations | 6 | false |
| UNCHECKED_WARNING | 6 | false |
| CastCanBeRemovedNarrowingVariableType | 6 | false |
| ExplicitArrayFilling | 5 | false |
| StringConcatenationInLoops | 5 | false |
| NonStrictComparisonCanBeEquality | 5 | true |
| CharsetObjectCanBeUsed | 5 | false |
| LongLiteralsEndingWithLowercaseL | 5 | false |
| DuplicateThrows | 4 | false |
| ComparatorMethodParameterNotUsed | 4 | false |
| JavadocLinkAsPlainText | 4 | false |
| ParameterCanBeLocal | 4 | false |
| MismatchedStringCase | 3 | false |
| UnaryPlus | 3 | false |
| CaughtExceptionImmediatelyRethrown | 3 | false |
| ThrowFromFinallyBlock | 3 | false |
| PointlessNullCheck | 3 | false |
| MismatchedJavadocCode | 3 | false |
| CollectionAddAllCanBeReplacedWithConstructor | 3 | false |
| SuspiciousMethodCalls | 3 | false |
| MismatchedArrayReadWrite | 2 | false |
| ArrayHashCode | 2 | false |
| StringEquality | 2 | false |
| RedundantArrayCreation | 2 | true |
| SuspiciousListRemoveInLoop | 2 | false |
| IfStatementMissingBreakInLoop | 2 | false |
| ExcessiveRangeCheck | 2 | false |
| ToArrayCallWithZeroLengthArrayArgument | 1 | true |
| LoopConditionNotUpdatedInsideLoop | 1 | false |
| RegExpRedundantEscape | 1 | false |
| AssignmentUsedAsCondition | 1 | false |
| InstantiatingObjectToGetClassObject | 1 | false |
| ShiftOutOfRange | 1 | false |
| OptionalUsedAsFieldOrParameterType | 1 | false |
| BusyWait | 1 | false |
| ExternalizableWithoutPublicNoArgConstructor | 1 | false |
| NonFinalFieldInEnum | 1 | false |
| RedundantStreamOptionalCall | 1 | false |
| SimplifyOptionalCallChains | 1 | false |
| SlowListContainsAll | 1 | false |
| TypeParameterExtendsObject | 1 | false |
| RedundantOperationOnEmptyContainer | 1 | false |
| RedundantLengthCheck | 1 | false |
| BoxingBoxedValue | 1 | false |
| EqualsWhichDoesntCheckParameterClass | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new CPOperand\[CPOpInputs.size()\]'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
		}
		return(CPOpInputs != null ? LineageItemUtils.getLineage(ec, 
			CPOpInputs.toArray(new CPOperand[CPOpInputs.size()])) : null);
	}
	
```

## RuleId[id=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_rowLowerBound instanceof ConstIdentifier && ( _rowLowerBound instanceof IntIdentifier || _rowLowerBound instanceof DoubleIdentifier )){
			
			Long rowLB_1_1 = -1L;
			if (_rowLowerBound instanceof IntIdentifier) 
				rowLB_1_1 = ((IntIdentifier)_rowLowerBound).getValue();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		// check 1 < indexed row lower-bound < rows in IndexedIdentifier 
		// (assuming row dims available for upper bound)
		Long rowLB_1 = -1L;
		if (isConst_rowLowerBound) {
				
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_rowUpperBound instanceof ConstIdentifier && ( _rowUpperBound instanceof IntIdentifier || _rowUpperBound instanceof DoubleIdentifier )){
			
			Long rowUB_1_1 = -1L;
			if (_rowUpperBound instanceof IntIdentifier) 
				rowUB_1_1 = ((IntIdentifier)_rowUpperBound).getValue();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_colLowerBound instanceof ConstIdentifier && ( _colLowerBound instanceof IntIdentifier || _colLowerBound instanceof DoubleIdentifier )){
			
			Long colLB_1_1 = -1L;
			if (_colLowerBound instanceof IntIdentifier) 
				colLB_1_1 = ((IntIdentifier)_colLowerBound).getValue();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		// check 1 < indexed column lower-bound < columns in IndexedIdentifier 
		// (assuming column dims available for upper bound)
		Long colLB_1 = -1L;
		if (isConst_colLowerBound) {
				
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_colUpperBound instanceof ConstIdentifier && ( _colUpperBound instanceof IntIdentifier || _colUpperBound instanceof DoubleIdentifier )){
			
			Long colUB_1_1 = -1L;
			if (_colUpperBound instanceof IntIdentifier) 
				colUB_1_1 = ((IntIdentifier)_colUpperBound).getValue();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		HashSet<Candidate> C = new HashSet<>(); 
		HashSet<Candidate> C2 = new HashSet<>(); 
		Integer sCount = 0; //object for call by ref 
		rDetermineCandidates(pfs.getBody(), C, sCount);
		if( LOG.isTraceEnabled() )
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/compress/readers/ReaderColumnSelectionSparse.java`
#### Snippet
```java
		while(skip < _colIndexes.size() && j < alen) {
			if(_colIndexes.get(skip) == aix[j]) {
				final Double v = avals[j];
				boolean isNan = Double.isNaN(v);
				if(isNan) {
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/instructions/cp/StringInitCPInstruction.java`
#### Snippet
```java
		for( int i=0; i<len; i++ ){
			String sval = st.nextToken();
			Double dval = Double.parseDouble(sval);
			int rix = (int) (i / _clen);
			int cix = (int) (i % _clen);
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java

		// read in names
		Boolean names = in.readBoolean();
		if(names) {
			_names = new ArrayList<>();
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
		if(average && distinctValues.size() > quantileIndex) {
			Double[] distinctsSorted = distinctValues.stream().flatMap(Stream::of).sorted().toArray(Double[]::new);
			Double medianSum = Double.sum(distinctsSorted[quantileIndex-1], distinctsSorted[quantileIndex]);
			return (T) medianSum;
		}
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
				
		//Step 2) output blocks 
		Long tmpIx = -1L;
		FrameBlock tmpBlock = new FrameBlock(_schema);
		
```

### WrapperTypeMayBePrimitive
Type may be primitive
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public static MatrixBlock toMatrixBlock(JavaPairRDD<MatrixIndexes,MatrixBlock> rdd, int rlen, int clen, int blen, long nnz) {
		long t0 = DMLScript.STATISTICS ? System.nanoTime() : 0;
		final Boolean singleBlock = rlen <= blen && clen <= blen ;
		final MatrixBlock out = singleBlock ? toMatrixBlockSingleBlock(rdd, rlen, clen) : toMatrixBlockMultiBlock(rdd, rlen, clen, blen, nnz);

```

## RuleId[id=MismatchedArrayReadWrite]
### MismatchedArrayReadWrite
Contents of array `count` are written to, but never read
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
		double[] c = out.getDenseBlockValues();
		Arrays.fill(c, initialVal);
		int[] count = new int[n];
		
		//note: sequential scan algorithm for both sparse-safe and -unsafe
```

### MismatchedArrayReadWrite
Contents of array `aligned` are written to, but never read
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		
		int[] curk = new int[blocksizeI];
		boolean[] aligned = (four&&!scalar) ? new boolean[blocksizeI] : null;
		
		//blocked execution over row blocks
```

## RuleId[id=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/api/ConfigurableAPI.java`
#### Snippet
```java
	 *            property value
	 */
	public void setConfigProperty(String propertyName, String propertyValue);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/api/ConfigurableAPI.java`
#### Snippet
```java
	 * Reset configuration settings to default settings.
	 */
	public void resetConfig();
	
	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/hops/estim/SparsityEstimator.java`
#### Snippet
```java
	};
	
	public static enum OpCode {
		MM, 
		MULT, PLUS, EQZERO, NEQZERO,
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `src/main/java/org/apache/sysds/hops/fedplanner/FTypes.java`
#### Snippet
```java
		private final FReplication _repType;

		private FType(FPartitioning ptype, FReplication rtype) {
			_partType = ptype;
			_repType = rtype;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `src/main/java/org/apache/sysds/common/Types.java`
#### Snippet
```java
	
	/** Common type for both function statement blocks and function program blocks **/
	public static interface FunctionBlock {
		public FunctionBlock cloneFunctionBlock();
	} 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/common/Types.java`
#### Snippet
```java
	/** Common type for both function statement blocks and function program blocks **/
	public static interface FunctionBlock {
		public FunctionBlock cloneFunctionBlock();
	} 
}
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `src/main/java/org/apache/sysds/common/Types.java`
#### Snippet
```java
		private final boolean _validOuter;
		
		private OpOp2(boolean outer) {
			_validOuter = outer;
		}
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `src/main/java/org/apache/sysds/parser/BuiltinConstant.java`
#### Snippet
```java
	private DoubleIdentifier _id;

	private BuiltinConstant(double d) {
		this._id = new DoubleIdentifier(d);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setText(String text);
	
	public void setFilename(String filename);

	public int getBeginLine();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setBeginLine(int beginLine);

	public void setBeginColumn(int beginColumn);

	public void setEndLine(int endLine);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public int getBeginColumn();

	public int getEndLine();

	public int getEndColumn();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public String getFilename();

	public static ParseInfo ctxAndFilenameToParseInfo(ParserRuleContext ctx, String fname) {
		ParseInfo pi = new ParseInfo() {
			private int beginLine;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
public interface ParseInfo {

	public void setBeginLine(int beginLine);

	public void setBeginColumn(int beginColumn);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public int getEndColumn();

	public String getText();
	
	public String getFilename();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setEndColumn(int endColumn);

	public void setText(String text);
	
	public void setFilename(String filename);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setEndLine(int endLine);

	public void setEndColumn(int endColumn);

	public void setText(String text);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public String getText();
	
	public String getFilename();

	public static ParseInfo ctxAndFilenameToParseInfo(ParserRuleContext ctx, String fname) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setBeginColumn(int beginColumn);

	public void setEndLine(int endLine);

	public void setEndColumn(int endColumn);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public int getBeginLine();

	public int getBeginColumn();

	public int getEndLine();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public int getEndLine();

	public int getEndColumn();

	public String getText();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/parser/ParseInfo.java`
#### Snippet
```java
	public void setFilename(String filename);

	public int getBeginLine();

	public int getBeginColumn();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/data/Block.java`
#### Snippet
```java
	 * @return value of cell at position (r,c)
	 */
	public abstract double get(int r, int c);
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `src/main/java/org/apache/sysds/runtime/data/Block.java`
#### Snippet
```java
	 * @return value of cell at position (r,c)
	 */
	public abstract double get(int r, int c);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java
	 * @return matrix block
	 */
	public static MatrixBlock convertToMatrixBlock(FrameBlock frame) {
		final int m = frame.getNumRows();
		final int n = frame.getNumColumns();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java

public interface MatrixBlockFromFrame {
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java

public interface MatrixBlockFromFrame {
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/MatrixBlockFromFrame.java`
#### Snippet
```java

public interface MatrixBlockFromFrame {
	public static final Log LOG = LogFactory.getLog(MatrixBlockFromFrame.class.getName());

	public static final int blocksizeIJ = 32;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static ValueType isType(double val) {
		if(val == 1.0d || val == 0.0d)
			return ValueType.BOOLEAN;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static ValueType isIntType(final String val, final int len) {
		if(len <= 22) {
			if(simpleIntMatch(val, len)) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java


	public static boolean isDefault(String v, ValueType t) {
		if(v == null)
			return true;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern dotSplitPattern = Pattern.compile("\\.");

	public static Array<?>[] add(Array<?>[] ar, Array<?> e) {
		if(ar == null)
			return new Array[] {e};
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");

	public static Array<?>[] add(Array<?>[] ar, Array<?> e) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");

	public static Array<?>[] add(Array<?>[] ar, Array<?> e) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");

	public static Array<?>[] add(Array<?>[] ar, Array<?> e) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java

public interface FrameUtil {
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java

public interface FrameUtil {
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java

public interface FrameUtil {
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static ValueType isFloatType(final String val, final int len) {

		if(len <= 25 && floatPattern.matcher(val).matches()) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static FrameBlock mergeSchema(FrameBlock temp1, FrameBlock temp2) {
		String[] rowTemp1 = IteratorFactory.getStringRowIterator(temp1).next();
		String[] rowTemp2 = IteratorFactory.getStringRowIterator(temp2).next();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	 * @return ValueType subject to restriction
	 */
	public static ValueType isType(String val, ValueType minType) {
		if(val == null)
			return ValueType.UNKNOWN;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static ValueType isType(double val, ValueType min) {
		switch(min) {
			case BOOLEAN:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	}

	public static ValueType isType(String val) {
		return isType(val, ValueType.BOOLEAN);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Pattern booleanPattern = Pattern
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
		.compile("([tT]((rue)|(RUE))?|[fF]((alse)|(ALSE))?|0\\.0+|1\\.0+|0|1)");
	public static final Pattern integerFloatPattern = Pattern.compile("[-+]?\\d+(\\.0+)?");
	public static final Pattern floatPattern = Pattern.compile("[-+]?[0-9]*\\.?[0-9]*([eE][-+]?[0-9]+)?");

	public static final Pattern dotSplitPattern = Pattern.compile("\\.");
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb, int rl, int ru, int[] cols) {
		return new StringRowIterator(fb, rl, ru, cols);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb, int[] cols) {
		return new StringRowIterator(fb, 0, fb.getNumRows(), cols);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, int colID) {
		return new ObjectRowIterator(fb, 0, fb.getNumRows(), new int[] {colID});
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, int rl, int ru, int[] cols) {
		return new ObjectRowIterator(fb, rl, ru, cols);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, ValueType[] schema) {
		return new ObjectRowIterator(fb, 0, fb.getNumRows(), schema);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, int rl, int ru) {
		return new ObjectRowIterator(fb, rl, ru);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb, int colID) {
		return new StringRowIterator(fb, 0, fb.getNumRows(), new int[] {colID});
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, int[] cols) {
		return new ObjectRowIterator(fb, 0, fb.getNumRows(), cols);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb) {
		return new ObjectRowIterator(fb, 0, fb.getNumRows());
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb, int rl, int ru, int colID) {
		return new StringRowIterator(fb, rl, ru, new int[] {colID});
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb, int rl, int ru) {
		return new StringRowIterator(fb, rl, ru);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return string array iterator
	 */
	public static RowIterator<String> getStringRowIterator(FrameBlock fb) {
		return new StringRowIterator(fb, 0, fb.getNumRows());
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/IteratorFactory.java`
#### Snippet
```java
	 * @return object array iterator
	 */
	public static RowIterator<Object> getObjectRowIterator(FrameBlock fb, int rl, int ru, int colID) {
		return new ObjectRowIterator(fb, rl, ru, new int[] {colID});
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Array<?> read(DataInput in, int nRow) throws IOException {
		final FrameArrayType v = FrameArrayType.values()[in.readByte()];
		Array<?> arr;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static ABooleanArray allocateBoolean(int nRow) {
		if(nRow > bitSetSwitchPoint)
			return new BitSetArray(nRow);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Object defaultNullValue(ValueType v) {
		return parseString(null, v);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static CharArray create(char[] col) {
		return new CharArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Object parseString(String s, ValueType v) {
		switch(v) {
			case BOOLEAN:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static long getInMemorySize(ValueType type, int _numRows) {
		switch(type) {
			case BOOLEAN:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static BooleanArray create(boolean[] col) {
		return new BooleanArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static IntegerArray create(int[] col) {
		return new IntegerArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static <T> OptionalArray<T> create(T[] col) {
		return new OptionalArray<T>(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static FloatArray create(float[] col) {
		return new FloatArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public static <C> Array<C> set(Array<?> target, Array<?> src, int rl, int ru, int rlen) {
		if(target == null) {
			if(src.getFrameArrayType() == FrameArrayType.OPTIONAL)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Array<?> allocateOptional(ValueType v, int nRow) {
		switch(v) {
			case BOOLEAN:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	public final static int bitSetSwitchPoint = 64;

	public enum FrameArrayType {
		STRING, BOOLEAN, BITSET, INT32, INT64, FP32, FP64, CHARACTER, OPTIONAL;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Array<?> allocate(ValueType v, int nRow, String val) {
		Array<?> a = allocate(v, nRow);
		a.fill(val);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static DoubleArray create(double[] col) {
		return new DoubleArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static BitSetArray create(BitSet col, int size) {
		return new BitSetArray(col, size);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
public interface ArrayFactory {

	public final static int bitSetSwitchPoint = 64;

	public enum FrameArrayType {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
public interface ArrayFactory {

	public final static int bitSetSwitchPoint = 64;

	public enum FrameArrayType {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
public interface ArrayFactory {

	public final static int bitSetSwitchPoint = 64;

	public enum FrameArrayType {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static StringArray create(String[] col) {
		return new StringArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	 */
	@SuppressWarnings("unchecked")
	public static <C> Array<C> append(Array<?> a, Array<?> b) {

		// get common highest datatype.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static LongArray create(long[] col) {
		return new LongArray(col);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
	}

	public static Array<?> allocate(ValueType v, int nRow) {
		switch(v) {
			case BOOLEAN:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlockDataOutput.java`
#### Snippet
```java
	 * @throws IOException if IOException occurs
	 */
	public void writeDoubleArray(int len, double[] varr) 
		throws IOException;
	
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlockDataOutput.java`
#### Snippet
```java
	 * @throws IOException if IOException occurs
	 */
	public void writeSparseRows(int rlen, SparseBlock rows) 
		throws IOException;
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlockDataInput.java`
#### Snippet
```java
	 * @throws IOException if IOException occurs
	 */
	public long readDoubleArray(int len, double[] varr) 
		throws IOException;
	
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlockDataInput.java`
#### Snippet
```java
	 * @throws IOException if IOExcepton occurs
	 */
	public long readSparseRows(int rlen, long nnz, SparseBlock rows) 
		throws IOException;
}
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNRotate180.java`
#### Snippet
```java
public class LibMatrixDNNRotate180
{
	public static interface Rotate180Worker {
		public void execute(int inputN, int outputN);
		public static Rotate180Worker getWorker(MatrixBlock in, MatrixBlock out,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNRotate180.java`
#### Snippet
```java
	public static interface Rotate180Worker {
		public void execute(int inputN, int outputN);
		public static Rotate180Worker getWorker(MatrixBlock in, MatrixBlock out,
			DnnParameters params, boolean zeroOutSparseOutput, boolean trans) {
			return in.isInSparseFormat() ?
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNRotate180.java`
#### Snippet
```java
{
	public static interface Rotate180Worker {
		public void execute(int inputN, int outputN);
		public static Rotate180Worker getWorker(MatrixBlock in, MatrixBlock out,
			DnnParameters params, boolean zeroOutSparseOutput, boolean trans) {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNN.java`
#### Snippet
```java
	
	protected static final Log LOG =  LogFactory.getLog(LibMatrixDNN.class.getName());
	public static enum PoolingType {
		MAX, AVG
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusolverDngeqrf_bufferSize(cusolverDnHandle handle, int m, int n, Pointer A, int lda, int[] Lwork);
	public int cusolverDngeqrf(cusolverDnHandle handle, int m, int n, Pointer A, int lda, Pointer TAU, Pointer Workspace, int Lwork, Pointer devInfo);
	public int cusolverDnormqr(cusolverDnHandle handle, int side, int trans, int m, int n, int k, Pointer A, int lda, Pointer tau, Pointer C, int ldc, Pointer work, int lwork, Pointer devInfo);
	public int cusparsecsrgeam(cusparseHandle handle, int m, int n, jcuda.Pointer alpha, cusparseMatDescr descrA, int nnzA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer beta, cusparseMatDescr descrB, int nnzB, jcuda.Pointer csrValB, jcuda.Pointer csrRowPtrB, jcuda.Pointer csrColIndB, cusparseMatDescr descrC, jcuda.Pointer csrValC, jcuda.Pointer csrRowPtrC, jcuda.Pointer csrColIndC);
	public int cusparsecsr2dense(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer A, int lda) ;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int	cusparsecsrmv(cusparseHandle handle, int transA, int m, int n, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
			jcuda.Pointer x, jcuda.Pointer beta, jcuda.Pointer y);
	public int	cusparsecsrmm2(cusparseHandle handle, int transa, int transb, int m, int n, int k, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
			jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasdot(cublasHandle handle, int n, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy, jcuda.Pointer result);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int	cublasgeam(cublasHandle handle, int transa, int transb, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, 
			int lda, jcuda.Pointer beta, jcuda.Pointer B, int ldb, jcuda.Pointer C, int ldc);
	public int	cusparsecsrmv(cusparseHandle handle, int transA, int m, int n, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
			jcuda.Pointer x, jcuda.Pointer beta, jcuda.Pointer y);
	public int	cusparsecsrmm2(cusparseHandle handle, int transa, int transb, int m, int n, int k, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublastrsm(cublasHandle handle, int side, int uplo, int trans, int diag, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb);
	public int cusolverDngeqrf_bufferSize(cusolverDnHandle handle, int m, int n, Pointer A, int lda, int[] Lwork);
	public int cusolverDngeqrf(cusolverDnHandle handle, int m, int n, Pointer A, int lda, Pointer TAU, Pointer Workspace, int Lwork, Pointer devInfo);
	public int cusolverDnormqr(cusolverDnHandle handle, int side, int trans, int m, int n, int k, Pointer A, int lda, Pointer tau, Pointer C, int ldc, Pointer work, int lwork, Pointer devInfo);
	public int cusparsecsrgeam(cusparseHandle handle, int m, int n, jcuda.Pointer alpha, cusparseMatDescr descrA, int nnzA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer beta, cusparseMatDescr descrB, int nnzB, jcuda.Pointer csrValB, jcuda.Pointer csrRowPtrB, jcuda.Pointer csrColIndB, cusparseMatDescr descrC, jcuda.Pointer csrValC, jcuda.Pointer csrRowPtrC, jcuda.Pointer csrColIndC);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
			cusparseMatDescr descrB, int nnzB, Pointer csrValB, Pointer csrRowPtrB, Pointer csrColIndB, 
			cusparseMatDescr descrC, Pointer csrValC, Pointer csrRowPtrC, Pointer csrColIndC);
	public int	cublasgeam(cublasHandle handle, int transa, int transb, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, 
			int lda, jcuda.Pointer beta, jcuda.Pointer B, int ldb, jcuda.Pointer C, int ldc);
	public int	cusparsecsrmv(cusparseHandle handle, int transA, int m, int n, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusolverDnormqr(cusolverDnHandle handle, int side, int trans, int m, int n, int k, Pointer A, int lda, Pointer tau, Pointer C, int ldc, Pointer work, int lwork, Pointer devInfo);
	public int cusparsecsrgeam(cusparseHandle handle, int m, int n, jcuda.Pointer alpha, cusparseMatDescr descrA, int nnzA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer beta, cusparseMatDescr descrB, int nnzB, jcuda.Pointer csrValB, jcuda.Pointer csrRowPtrB, jcuda.Pointer csrColIndB, cusparseMatDescr descrC, jcuda.Pointer csrValC, jcuda.Pointer csrRowPtrC, jcuda.Pointer csrColIndC);
	public int cusparsecsr2dense(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer A, int lda) ;
	public int cusparsedense2csr(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRow, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA);
	public int cusparsennz(cusparseHandle handle, int dirA, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRowCol, jcuda.Pointer nnzTotalDevHostPtr);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublassyrk(cublasHandle handle, int uplo, int trans, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasaxpy(cublasHandle handle, int n, jcuda.Pointer alpha, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy);
	public int cublastrsm(cublasHandle handle, int side, int uplo, int trans, int diag, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb);
	public int cusolverDngeqrf_bufferSize(cusolverDnHandle handle, int m, int n, Pointer A, int lda, int[] Lwork);
	public int cusolverDngeqrf(cusolverDnHandle handle, int m, int n, Pointer A, int lda, Pointer TAU, Pointer Workspace, int Lwork, Pointer devInfo);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublasgemm(cublasHandle handle, int transa, int transb, int m, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cusparsecsr2csc(cusparseHandle handle, int m, int n, int nnz, jcuda.Pointer csrVal, jcuda.Pointer csrRowPtr, jcuda.Pointer csrColInd, jcuda.Pointer cscVal, jcuda.Pointer cscRowInd, jcuda.Pointer cscColPtr, int copyValues, int idxBase);
	public int cublassyrk(cublasHandle handle, int uplo, int trans, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasaxpy(cublasHandle handle, int n, jcuda.Pointer alpha, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy);
	public int cublastrsm(cublasHandle handle, int side, int uplo, int trans, int diag, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
 */
public interface CudaSupportFunctions {
	public static boolean PERFORM_CONVERSION_ON_DEVICE = true;
	public int cusparsecsrgemm(cusparseHandle handle, int transA, int transB, int m, int n, int k, 
			cusparseMatDescr descrA, int nnzA, Pointer csrValA, Pointer csrRowPtrA, Pointer csrColIndA, 
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
 */
public interface CudaSupportFunctions {
	public static boolean PERFORM_CONVERSION_ON_DEVICE = true;
	public int cusparsecsrgemm(cusparseHandle handle, int transA, int transB, int m, int n, int k, 
			cusparseMatDescr descrA, int nnzA, Pointer csrValA, Pointer csrRowPtrA, Pointer csrColIndA, 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusparsedense2csr(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRow, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA);
	public int cusparsennz(cusparseHandle handle, int dirA, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRowCol, jcuda.Pointer nnzTotalDevHostPtr);
	public void deviceToHost(GPUContext gCtx, Pointer src, double [] dest, String instName, boolean isEviction);
	public void hostToDevice(GPUContext gCtx, double [] src,  Pointer dest, String instName);
	
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublasgemv(cublasHandle handle, int trans, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer x, int incx, jcuda.Pointer beta, jcuda.Pointer y, int incy);
	public int cublasgemm(cublasHandle handle, int transa, int transb, int m, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cusparsecsr2csc(cusparseHandle handle, int m, int n, int nnz, jcuda.Pointer csrVal, jcuda.Pointer csrRowPtr, jcuda.Pointer csrColInd, jcuda.Pointer cscVal, jcuda.Pointer cscRowInd, jcuda.Pointer cscColPtr, int copyValues, int idxBase);
	public int cublassyrk(cublasHandle handle, int uplo, int trans, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasaxpy(cublasHandle handle, int n, jcuda.Pointer alpha, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublasaxpy(cublasHandle handle, int n, jcuda.Pointer alpha, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy);
	public int cublastrsm(cublasHandle handle, int side, int uplo, int trans, int diag, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb);
	public int cusolverDngeqrf_bufferSize(cusolverDnHandle handle, int m, int n, Pointer A, int lda, int[] Lwork);
	public int cusolverDngeqrf(cusolverDnHandle handle, int m, int n, Pointer A, int lda, Pointer TAU, Pointer Workspace, int Lwork, Pointer devInfo);
	public int cusolverDnormqr(cusolverDnHandle handle, int side, int trans, int m, int n, int k, Pointer A, int lda, Pointer tau, Pointer C, int ldc, Pointer work, int lwork, Pointer devInfo);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
public interface CudaSupportFunctions {
	public static boolean PERFORM_CONVERSION_ON_DEVICE = true;
	public int cusparsecsrgemm(cusparseHandle handle, int transA, int transB, int m, int n, int k, 
			cusparseMatDescr descrA, int nnzA, Pointer csrValA, Pointer csrRowPtrA, Pointer csrColIndA, 
			cusparseMatDescr descrB, int nnzB, Pointer csrValB, Pointer csrRowPtrB, Pointer csrColIndB, 
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
			jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasdot(cublasHandle handle, int n, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy, jcuda.Pointer result);
	public int cublasgemv(cublasHandle handle, int trans, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer x, int incx, jcuda.Pointer beta, jcuda.Pointer y, int incy);
	public int cublasgemm(cublasHandle handle, int transa, int transb, int m, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cusparsecsr2csc(cusparseHandle handle, int m, int n, int nnz, jcuda.Pointer csrVal, jcuda.Pointer csrRowPtr, jcuda.Pointer csrColInd, jcuda.Pointer cscVal, jcuda.Pointer cscRowInd, jcuda.Pointer cscColPtr, int copyValues, int idxBase);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cublasdot(cublasHandle handle, int n, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy, jcuda.Pointer result);
	public int cublasgemv(cublasHandle handle, int trans, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer x, int incx, jcuda.Pointer beta, jcuda.Pointer y, int incy);
	public int cublasgemm(cublasHandle handle, int transa, int transb, int m, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cusparsecsr2csc(cusparseHandle handle, int m, int n, int nnz, jcuda.Pointer csrVal, jcuda.Pointer csrRowPtr, jcuda.Pointer csrColInd, jcuda.Pointer cscVal, jcuda.Pointer cscRowInd, jcuda.Pointer cscColPtr, int copyValues, int idxBase);
	public int cublassyrk(cublasHandle handle, int uplo, int trans, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusparsennz(cusparseHandle handle, int dirA, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRowCol, jcuda.Pointer nnzTotalDevHostPtr);
	public void deviceToHost(GPUContext gCtx, Pointer src, double [] dest, String instName, boolean isEviction);
	public void hostToDevice(GPUContext gCtx, double [] src,  Pointer dest, String instName);
	
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusparsecsr2csc(cusparseHandle handle, int m, int n, int nnz, jcuda.Pointer csrVal, jcuda.Pointer csrRowPtr, jcuda.Pointer csrColInd, jcuda.Pointer cscVal, jcuda.Pointer cscRowInd, jcuda.Pointer cscColPtr, int copyValues, int idxBase);
	public int cublassyrk(cublasHandle handle, int uplo, int trans, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasaxpy(cublasHandle handle, int n, jcuda.Pointer alpha, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy);
	public int cublastrsm(cublasHandle handle, int side, int uplo, int trans, int diag, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb);
	public int cusolverDngeqrf_bufferSize(cusolverDnHandle handle, int m, int n, Pointer A, int lda, int[] Lwork);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int	cusparsecsrmm2(cusparseHandle handle, int transa, int transb, int m, int n, int k, int nnz, jcuda.Pointer alpha, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, 
			jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
	public int cublasdot(cublasHandle handle, int n, jcuda.Pointer x, int incx, jcuda.Pointer y, int incy, jcuda.Pointer result);
	public int cublasgemv(cublasHandle handle, int trans, int m, int n, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer x, int incx, jcuda.Pointer beta, jcuda.Pointer y, int incy);
	public int cublasgemm(cublasHandle handle, int transa, int transb, int m, int n, int k, jcuda.Pointer alpha, jcuda.Pointer A, int lda, jcuda.Pointer B, int ldb, jcuda.Pointer beta, jcuda.Pointer C, int ldc);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusparsecsrgeam(cusparseHandle handle, int m, int n, jcuda.Pointer alpha, cusparseMatDescr descrA, int nnzA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer beta, cusparseMatDescr descrB, int nnzB, jcuda.Pointer csrValB, jcuda.Pointer csrRowPtrB, jcuda.Pointer csrColIndB, cusparseMatDescr descrC, jcuda.Pointer csrValC, jcuda.Pointer csrRowPtrC, jcuda.Pointer csrColIndC);
	public int cusparsecsr2dense(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer A, int lda) ;
	public int cusparsedense2csr(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRow, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA);
	public int cusparsennz(cusparseHandle handle, int dirA, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRowCol, jcuda.Pointer nnzTotalDevHostPtr);
	public void deviceToHost(GPUContext gCtx, Pointer src, double [] dest, String instName, boolean isEviction);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusparsecsr2dense(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer A, int lda) ;
	public int cusparsedense2csr(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRow, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA);
	public int cusparsennz(cusparseHandle handle, int dirA, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRowCol, jcuda.Pointer nnzTotalDevHostPtr);
	public void deviceToHost(GPUContext gCtx, Pointer src, double [] dest, String instName, boolean isEviction);
	public void hostToDevice(GPUContext gCtx, double [] src,  Pointer dest, String instName);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/CudaSupportFunctions.java`
#### Snippet
```java
	public int cusolverDngeqrf(cusolverDnHandle handle, int m, int n, Pointer A, int lda, Pointer TAU, Pointer Workspace, int Lwork, Pointer devInfo);
	public int cusolverDnormqr(cusolverDnHandle handle, int side, int trans, int m, int n, int k, Pointer A, int lda, Pointer tau, Pointer C, int ldc, Pointer work, int lwork, Pointer devInfo);
	public int cusparsecsrgeam(cusparseHandle handle, int m, int n, jcuda.Pointer alpha, cusparseMatDescr descrA, int nnzA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer beta, cusparseMatDescr descrB, int nnzB, jcuda.Pointer csrValB, jcuda.Pointer csrRowPtrB, jcuda.Pointer csrColIndB, cusparseMatDescr descrC, jcuda.Pointer csrValC, jcuda.Pointer csrRowPtrC, jcuda.Pointer csrColIndC);
	public int cusparsecsr2dense(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA, jcuda.Pointer A, int lda) ;
	public int cusparsedense2csr(cusparseHandle handle, int m, int n, cusparseMatDescr descrA, jcuda.Pointer A, int lda, jcuda.Pointer nnzPerRow, jcuda.Pointer csrValA, jcuda.Pointer csrRowPtrA, jcuda.Pointer csrColIndA);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/Converter.java`
#### Snippet
```java
{
	public void convert(K1 k1, V1 v1);
	public void setBlockSize(int rl, int cl);
	public boolean hasNext();
	public Pair<K2, V2> next();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/Converter.java`
#### Snippet
```java
	public void setBlockSize(int rl, int cl);
	public boolean hasNext();
	public Pair<K2, V2> next();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/Converter.java`
#### Snippet
```java
K2 extends Writable, V2 extends Writable> 
{
	public void convert(K1 k1, V1 v1);
	public void setBlockSize(int rl, int cl);
	public boolean hasNext();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/matrix/data/Converter.java`
#### Snippet
```java
	public void convert(K1 k1, V1 v1);
	public void setBlockSize(int rl, int cl);
	public boolean hasNext();
	public Pair<K2, V2> next();
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/lineage/LineageTraceable.java`
#### Snippet
```java
	 * @return pairs of (output variable name, output lineage item)
	 */
	public default Pair<String,LineageItem>[] getLineageItems(ExecutionContext ec) {
		throw new DMLRuntimeException("Unsupported call for instruction with single output.");
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/lineage/LineageTraceable.java`
#### Snippet
```java
	 * @return pair of (output variable name, output lineage item)
	 */
	public Pair<String,LineageItem> getLineageItem(ExecutionContext ec);
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/lineage/LineageTraceable.java`
#### Snippet
```java
	 * @return true iff instruction has a single output
	 */
	public default boolean hasSingleLineage() {
		return true;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorFactory.java`
#### Snippet
```java
	 * @return A cost estimator
	 */
	public static ACostEstimate create(CompressionSettings cs, CostEstimatorBuilder costBuilder, int nRows, int nCols,
		double sparsity) {
		switch(cs.costComputationType) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorFactory.java`
#### Snippet
```java
public interface CostEstimatorFactory {

	public enum CostType {
		MEMORY, W_TREE, HYBRID_W_TREE, DISTINCT, AUTO;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/plan/IPlanEncode.java`
#### Snippet
```java
	 * @param in The bock
	 */
	public void expandPlan(MatrixBlock in);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/plan/IPlanEncode.java`
#### Snippet
```java
	 * @return A Compressed Matrix block with the safe scheme applied.
	 */
	public CompressedMatrixBlock encode(MatrixBlock in);

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserJackknifeEstimator.java`
#### Snippet
```java
public interface ShlosserJackknifeEstimator {

	final static double SHLOSSER_JACKKNIFE_ALPHA = 0.975;

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserJackknifeEstimator.java`
#### Snippet
```java
public interface ShlosserJackknifeEstimator {

	final static double SHLOSSER_JACKKNIFE_ALPHA = 0.975;

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserJackknifeEstimator.java`
#### Snippet
```java
	 * @return an estimation of number of distinct values.
	 */
	public static int distinctCount(int numVals, int[] frequencies, int[] freqCounts, int nRows, int sampleSize) {

		final CriticalValue cv = computeCriticalValue(sampleSize);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserJackknifeEstimator.java`
#### Snippet
```java
	 * avoid recomputing it in each call.
	 */
	public static class CriticalValue {
		public final double uniformityCriticalValue;
		public final int usedSampleSize;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserJackknifeEstimator.java`
#### Snippet
```java
	 * avoid recomputing it in each call.
	 */
	public static class CriticalValue {
		public final double uniformityCriticalValue;
		public final int usedSampleSize;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserEstimator.java`
#### Snippet
```java
	 * @return an estimation of number of distinct values.
	 */
	public static int distinctCount(long numVals, int[] freqCounts, long nRows, long sampleSize) {

		if(freqCounts[0] == 0) // early abort
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java
	 * @return A new CompressionSizeEstimator used to extract information of column groups
	 */
	public static AComEst createEstimator(MatrixBlock data, CompressionSettings cs, int k) {
		final int nRows = cs.transposed ? data.getNumColumns() : data.getNumRows();
		final int nCols = cs.transposed ? data.getNumRows() : data.getNumColumns();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java
	 * @return A new CompressionSizeEstimator used to extract information of column groups
	 */
	public static AComEst createEstimator(MatrixBlock data, CompressionSettings cs, int sampleSize,
		int k) {
		final int nRows = cs.transposed ? data.getNumColumns() : data.getNumRows();
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java

public interface ComEstFactory {
	static final Log LOG = LogFactory.getLog(ComEstFactory.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java

public interface ComEstFactory {
	static final Log LOG = LogFactory.getLog(ComEstFactory.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SmoothedJackknifeEstimator.java`
#### Snippet
```java
	 * @return Estimate of the number of distinct values
	 */
	public static int distinctCount(int numVals, int[] freqCounts, int nRows, int sampleSize) {

		int d = numVals;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return The combined encoding
	 */
	public IEncode combineNoResize(IEncode e);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return A EstimationFactors object
	 */
	public EstimationFactors extractFacts(int nRows, double tupleSparsity, double matrixSparsity,
		CompressionSettings cs);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return is dense
	 */
	public abstract boolean isDense();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return is dense
	 */
	public abstract boolean isDense();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return The number of unique values.
	 */
	public int getUnique();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
public interface IEncode {

	public static final Log LOG = LogFactory.getLog(IEncode.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
public interface IEncode {

	public static final Log LOG = LogFactory.getLog(IEncode.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
public interface IEncode {

	public static final Log LOG = LogFactory.getLog(IEncode.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/IEncode.java`
#### Snippet
```java
	 * @return The combined encoding
	 */
	public IEncode combine(IEncode e);

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
public interface SampleEstimatorFactory {

	static final Log LOG = LogFactory.getLog(SampleEstimatorFactory.class.getName());

	public enum EstimationType {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
public interface SampleEstimatorFactory {

	static final Log LOG = LogFactory.getLog(SampleEstimatorFactory.class.getName());

	public enum EstimationType {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
	 * @return A estimated number of unique values
	 */
	public static int distinctCount(int[] frequencies, int nRows, int sampleSize, EstimationType type,
		HashMap<Integer, Double> solveCache) {
		if(frequencies == null || frequencies.length == 0)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
	 * @return A estimated number of unique values
	 */
	public static int distinctCount(int[] frequencies, int nRows, int sampleSize, EstimationType type) {
		return distinctCount(frequencies, nRows, sampleSize, type, null);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(SampleEstimatorFactory.class.getName());

	public enum EstimationType {
		HassAndStokes, ShlosserEstimator, //
		ShlosserJackknifeEstimator, SmoothedJackknifeEstimator,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	 * @return An estimation of distinct elements in the population.
	 */
	public static int distinctCount(int numVals, int[] freqCounts, int nRows, int sampleSize,
		HashMap<Integer, Double> solveCache) {

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	}

	public static class MethodOfMomentsFunction implements UnivariateFunction {
		private final int _nj;
		private final double _q;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	}

	public static class MethodOfMomentsFunction implements UnivariateFunction {
		private final int _nj;
		private final double _q;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	// static final Log LOG = LogFactory.getLog(HassAndStokes.class.getName());

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	// static final Log LOG = LogFactory.getLog(HassAndStokes.class.getName());

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	// static final Log LOG = LogFactory.getLog(HassAndStokes.class.getName());

	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
	public static final int MAX_SOLVE_CACHE_SIZE = 64 * 1024; // global 2MB cache
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	public static final double HAAS_AND_STOKES_ALPHA1 = 0.9; // 0.9 recommended in paper
	public static final double HAAS_AND_STOKES_ALPHA2 = 30; // 30 recommended in paper;
	public static final int HAAS_AND_STOKES_UJ2A_C = 50; // 50 recommend in paper
	public static final boolean HAAS_AND_STOKES_UJ2A_CUT2 = true; // cut frequency in half
	public static final boolean HAAS_AND_STOKES_UJ2A_SOLVE = true; // true recommended
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static MatrixBlock matrixBlockFromDenseArray(double[] values, int nCol, boolean check) {
		final int nRow = values.length / nCol;
		DenseBlock dictV = new DenseBlockFP64(new int[] {nRow, nCol}, values);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static MatrixBlock extractValues(double[] v, IColIndex colIndexes) {
		MatrixBlock rowVector = new MatrixBlock(1, colIndexes.size(), false);
		for(int i = 0; i < colIndexes.size(); i++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static int[] genColsIndices(final int numCols) {
		final int[] colIndices = new int[numCols];
		for(int i = 0; i < numCols; i++)
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
public interface Util {

	static final Log LOG = LogFactory.getLog(Util.class.getName());

	public static int[] combine(int[] lhs, int[] rhs) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
public interface Util {

	static final Log LOG = LogFactory.getLog(Util.class.getName());

	public static int[] combine(int[] lhs, int[] rhs) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static MatrixBlock matrixBlockFromDenseArray(double[] values, int nCol) {
		return matrixBlockFromDenseArray(values, nCol, true);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static int[] genColsIndicesOffset(final int numCols, final int start) {
		final int[] colIndices = new int[numCols];
		for(int i = 0, j = start; i < numCols; i++, j++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static int[] genColsIndices(final int cl, final int cu) {
		final int[] colIndices = new int[cu - cl];
		for(int i = 0, j = cl; j < cu; i++, j++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	}

	public static int getPow2(int x) {
		int v = UtilFunctions.nextIntPow2(x + 1);
		return Math.max(v, 4);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/utils/Util.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(Util.class.getName());

	public static int[] combine(int[] lhs, int[] rhs) {
		int[] joined = new int[lhs.length + rhs.length];
		int lp = 0;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	 * @return An encoded format of the information of this column.
	 */
	public static IEncode createFromMatrixBlock(MatrixBlock m, boolean transposed, int rowCol) {
		if(m.isEmpty())
			return new EmptyEncoding();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	}

	public static SparseEncoding createSparse(AMapToData map, AOffset off, int nRows) {
		return new SparseEncoding(map, off, nRows);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	 * @return A delta encoded encoding.
	 */
	public static IEncode createFromMatrixBlockDelta(MatrixBlock m, boolean transposed, IColIndex rowCols, int sampleSize) {
		throw new NotImplementedException();
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	}

	public static IEncode create(AMapToData d){
		return new DenseEncoding(d);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	}

	public static IEncode create(AMapToData d, AOffset i, int nRow){
		return new SparseEncoding(d, i, nRow);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	}

	public static IEncode create(ColGroupEmpty c){
		return new EmptyEncoding();
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	 * @return An encoded format of the information of the columns.
	 */
	public static IEncode createFromMatrixBlock(MatrixBlock m, boolean transposed, IColIndex rowCols) {
		if(m.isEmpty())
			return new EmptyEncoding();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	}

	public static IEncode create(ColGroupConst c){
		return new ConstEncoding(-1);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
public interface EncodingFactory {

	public static final Log LOG = LogFactory.getLog(EncodingFactory.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
public interface EncodingFactory {

	public static final Log LOG = LogFactory.getLog(EncodingFactory.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
public interface EncodingFactory {

	public static final Log LOG = LogFactory.getLog(EncodingFactory.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	 * @return A delta encoded encoding.
	 */
	public static IEncode createFromMatrixBlockDelta(MatrixBlock m, boolean transposed, IColIndex rowCols) {
		throw new NotImplementedException();
		// final int sampleSize = transposed ? m.getNumColumns() : m.getNumRows();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCoderFactory.java`
#### Snippet
```java
	 * The Valid coCoding techniques
	 */
	public enum PartitionerType {
		BIN_PACKING, STATIC, PRIORITY_QUE, GREEDY, AUTO;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCoderFactory.java`
#### Snippet
```java
	 * @return The estimated (hopefully) best groups of ColGroups.
	 */
	public static CompressedSizeInfo findCoCodesByPartitioning(AComEst est, CompressedSizeInfo colInfos,
		int k, ACostEstimate costEstimator, CompressionSettings cs) {

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
public interface ColGroupIO {

	static final Log LOG = LogFactory.getLog(ColGroupIO.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
public interface ColGroupIO {

	static final Log LOG = LogFactory.getLog(ColGroupIO.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
	 * @throws IOException Throws IO Exception if the out refuses to write.
	 */
	public static void writeGroups(DataOutput out, Collection<AColGroup> colGroups) throws IOException {
		// Write out how many ColGroups to save.
		out.writeInt(colGroups.size());
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
	 * @return The exact disk size required for writing the compressed matrix.
	 */
	public static long getExactSizeOnDisk(List<AColGroup> colGroups) {
		long ret = 4; // int for number of colGroups.
		for(AColGroup grp : colGroups)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
	 * @throws IOException Throws IO Exception if the in refuses to read data.
	 */
	public static List<AColGroup> readGroups(DataInput in, int nRows) throws IOException {

		// Read in how many colGroups there are
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
	}

	public static AColGroup readColGroup(DataInput in, int nRows) throws IOException {
		final ColGroupType ctype = ColGroupType.values()[in.readByte()];
		switch(ctype) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupIO.java`
#### Snippet
```java
	}

	public static double[] readDoubleArray(int length, DataInput in) throws IOException {
		double[] ret = new double[length];
		for(int i = 0; i < length; i++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AOffsetsGroup.java`
#### Snippet
```java
public interface AOffsetsGroup {

	public AOffset getOffsets();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeCONST(int nrColumns, boolean contiguousColumns, double tupleSparsity,
		boolean lossy) {
		long size = estimateInMemorySizeGroup(nrColumns, contiguousColumns);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeOLE(int nrColumns, boolean contiguousColumns, int nrValues, int offsetLength,
		int nrRows, double tupleSparsity, boolean lossy) {
		nrColumns = nrColumns > 0 ? nrColumns : 1;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeRLE(int nrColumns, boolean contiguousColumns, int nrValues, int nrRuns,
		int nrRows, double tupleSparsity, boolean lossy) {
		// Correct low number of runs if very large input.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeLinearFunctional(int nrColumns, boolean contiguousColumns) {
		long size = 0;
		// Since the Object is a col group the overhead from the Memory Size group is added
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java

public interface ColGroupSizes {
	static final Log LOG = LogFactory.getLog(ColGroupSizes.class.getName());

	public static long estimateInMemorySizeGroup(int nrColumns, boolean contiguousColumns) {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java

public interface ColGroupSizes {
	static final Log LOG = LogFactory.getLog(ColGroupSizes.class.getName());

	public static long estimateInMemorySizeGroup(int nrColumns, boolean contiguousColumns) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeSDC(int nrColumns, boolean contiguousColumns, int nrValues, int nrRows,
		int largestOff, double tupleSparsity, boolean largestOffZero, boolean lossy) {
		long size = estimateInMemorySizeGroupValue(nrColumns, contiguousColumns, nrValues, tupleSparsity, lossy);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeOffset(int nrColumns, boolean contiguousColumns, int nrValues, int pointers,
		int offsetLength, double tupleSparsity, boolean lossy) {
		long size = estimateInMemorySizeGroupValue(nrColumns, contiguousColumns, nrValues, tupleSparsity, lossy);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeUncompressed(int nrRows, boolean contiguousColumns, int nrColumns,
		double sparsity) {
		long size = 0;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeDDC(int nrCols, boolean contiguousColumns, int numTuples, int dataLength,
		double tupleSparsity, boolean lossy) {
		long size = estimateInMemorySizeGroupValue(nrCols, contiguousColumns, numTuples, tupleSparsity, lossy);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeGroupValue(int nrColumns, boolean contiguousColumns, int nrValues,
		double tupleSparsity, boolean lossy) {
		long size = estimateInMemorySizeGroup(nrColumns, contiguousColumns);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(ColGroupSizes.class.getName());

	public static long estimateInMemorySizeGroup(int nrColumns, boolean contiguousColumns) {
		long size = 16; // Object header
		size += ColIndexFactory.estimateMemoryCost(nrColumns, contiguousColumns);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
	}

	public static long estimateInMemorySizeEMPTY(int nrColumns, boolean contiguousColumns) {
		return estimateInMemorySizeGroup(nrColumns, contiguousColumns);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/IContainDefaultTuple.java`
#### Snippet
```java

public interface IContainDefaultTuple {
	public double[] getDefaultTuple(); 
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/IContainADictionary.java`
#### Snippet
```java

public interface IContainADictionary {
	public ADictionary getDictionary();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/IMapToDataGroup.java`
#### Snippet
```java

public interface IMapToDataGroup {
	public AMapToData getMapToData();
}

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java
	 * Protected such that outside the ColGroup package it should be unknown which specific subtype is used.
	 */
	protected static enum ColGroupType {
		UNCOMPRESSED, RLE, OLE, DDC, CONST, EMPTY, SDC, SDCSingle, SDCSingleZeros, SDCZeros, SDCFOR, DDCFOR, DeltaDDC,
		LinearFunctional;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java

	/** Public super types of compression ColGroups supported */
	public static enum CompressionType {
		UNCOMPRESSED, RLE, OLE, DDC, CONST, EMPTY, SDC, SDCFOR, DDCFOR, DeltaDDC, LinearFunctional;

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	 * @return Double array with most common values.
	 */
	public static double[] extractMostCommonValueInColumns(MatrixBlock mb) {
		final int nCol = mb.getNumColumns();
		final int nVal = mb.getNumRows();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static void outerProduct(final double[] leftRowSum, final double[] rightColumnSum,
		final IColIndex colIdxRight, final double[] result, final int nColR, final int rl, final int ru) {
		for(int row = rl; row < ru; row++) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static void addMatrixToResult(MatrixBlock tmp, MatrixBlock result, IColIndex colIndexes, int rl, int ru) {
		if(tmp.isEmpty())
			return;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static void outerProduct(final double[] leftRowSum, final SparseBlock rightColSum,
		final IColIndex colIdxRight, final double[] result, final int nColR, final int rl, final int ru) {
		final int alen = rightColSum.size(0);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static double[] reorderDefault(double[] vals, int[] reordering){
		double[] ret = new double[vals.length];
		for(int i = 0; i < vals.length; i++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static double[] createReference(int nCol, double val) {
		double[] reference = new double[nCol];
		for(int i = 0; i < nCol; i++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static double refSumSq(double[] reference) {
		double ret = 0;
		for(double d : reference)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static double[] unaryOperator(UnaryOperator op, double[] reference) {
		final double[] newRef = new double[reference.length];
		for(int i = 0; i < reference.length; i++)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static void outerProduct(final double[] leftRowSum, final double[] rightColumnSum, final double[] result,
		int rl, int ru) {
		for(int row = rl; row < ru; row++) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static boolean allZero(double[] in) {
		for(double v : in)
			if(v != 0)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	 * @param result    The result matrix block to move values to
	 */
	public static void copyValuesColGroupMatrixBlocks(AColGroup lhs, AColGroup rhs, MatrixBlock tmpResult,
		MatrixBlock result) {
		final double[] resV = result.getDenseBlockValues();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	 * @return The result as a double array.
	 */
	public static double[] binaryDefRowLeft(BinaryOperator op, double[] v, IColIndex colIndexes) {
		final ValueFunction fn = op.fn;
		final int len = colIndexes.size();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static double refSum(double[] reference) {
		double ret = 0;
		for(double d : reference)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	}

	public static boolean containsInfOrNan(double pattern, double[] reference) {
		if(Double.isNaN(pattern)) {
			for(double d : reference)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	 * @return The result as a double array.
	 */
	public static double[] binaryDefRowRight(BinaryOperator op, double[] v, IColIndex colIndexes) {
		final ValueFunction fn = op.fn;
		final int len = colIndexes.size();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java
	 * @return A updated scheme 
	 */
	public ICLAScheme update(MatrixBlock data);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java

	/** Logging access for the CLA Scheme encoders */
	public final Log LOG = LogFactory.getLog(ICLAScheme.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java

	/** Logging access for the CLA Scheme encoders */
	public final Log LOG = LogFactory.getLog(ICLAScheme.class.getName());

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java
	 * @return A updated scheme 
	 */
	public ICLAScheme update(MatrixBlock data, IColIndex columns);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java
	 * @return A compressed column group forced to use the scheme provided.
	 */
	public AColGroup encode(MatrixBlock data);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ICLAScheme.java`
#### Snippet
```java
	 * @return A compressed column group forced to use the scheme provided.
	 */
	public AColGroup encode(MatrixBlock data, IColIndex columns);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IIterate.java`
#### Snippet
```java
	 * @return the next index.
	 */
	public boolean hasNext();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IIterate.java`
#### Snippet
```java
	 * @return The index currently pointed at
	 */
	public int i();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IIterate.java`
#### Snippet
```java
	 * @return the value pointing at.
	 */
	public int v();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IIterate.java`
#### Snippet
```java
	 * @return the index.
	 */
	public int next();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return if this array contains both a and b
	 */
	public boolean containsStrict(IColIndex a, IColIndex b);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The combined array
	 */
	public IColIndex combine(IColIndex other);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java

	@Override
	public int hashCode();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java

	@Override
	public boolean equals(Object other);

	public boolean equals(IColIndex other);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return If the Columns are contiguous.
	 */
	public boolean isContiguous();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return the new instance of an index.
	 */
	public IColIndex shift(int i);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return the column index at the index.
	 */
	public int get(int i);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
public interface IColIndex {

	public static enum ColIndexType {
		SINGLE, TWO, ARRAY, RANGE, UNKNOWN;
	}
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
public interface IColIndex {

	public static enum ColIndexType {
		SINGLE, TWO, ARRAY, RANGE, UNKNOWN;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return A iterator for the indexes contained.
	 */
	public IIterate iterator();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The memory size of this object
	 */
	public long estimateInMemorySize();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return A Reordered index.
	 */
	public int[] getReorderingIndex();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	public int findIndex(int i);

	public SliceResult slice(int l, int u);

	@Override
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The exact disk representation size
	 */
	public long getExactSizeOnDisk();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The index of the value.
	 */
	public int findIndex(int i);

	public SliceResult slice(int l, int u);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java

	/** A Class for slice results containing indexes for the slicing of dictionaries, and the resulting column index */
	public static class SliceResult {
		/** Start index to slice inside the dictionary */
		public final int idStart;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java

	/** A Class for slice results containing indexes for the slicing of dictionaries, and the resulting column index */
	public static class SliceResult {
		/** Start index to slice inside the dictionary */
		public final int idStart;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The sorted instance of this column index.
	 */
	public IColIndex sort();

	/** A Class for slice results containing indexes for the slicing of dictionaries, and the resulting column index */
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	public boolean equals(Object other);

	public boolean equals(IColIndex other);

	@Override
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return The size of the array
	 */
	public int size();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return If the index is sorted
	 */
	public boolean isSorted();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @return if the other arrays contain values from this array
	 */
	public boolean contains(IColIndex a, IColIndex b);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
	 * @throws IOException IO exceptions in case of for instance not enough disk space
	 */
	public void write(DataOutput out) throws IOException;

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex createI(int... indexes) {
		return create(indexes);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(ColIndexFactory.class.getName());

	public static IColIndex read(DataInput in) throws IOException {
		final ColIndexType t = ColIndexType.values()[in.readByte()];
		switch(t) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex combine(AColGroup a, AColGroup b) {
		return combine(a.getColIndices(), b.getColIndices());
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex combine(IColIndex a, IColIndex b) {
		final int numCols = a.size() + b.size();
		final int[] resCols = new int[numCols];
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex create(int l, int u) {
		if(u - l <= 0)
			throw new DMLRuntimeException("Invalid range: " + l + " " + u);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java

public interface ColIndexFactory {
	public static final Log LOG = LogFactory.getLog(ColIndexFactory.class.getName());

	public static IColIndex read(DataInput in) throws IOException {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java

public interface ColIndexFactory {
	public static final Log LOG = LogFactory.getLog(ColIndexFactory.class.getName());

	public static IColIndex read(DataInput in) throws IOException {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java

public interface ColIndexFactory {
	public static final Log LOG = LogFactory.getLog(ColIndexFactory.class.getName());

	public static IColIndex read(DataInput in) throws IOException {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex combine(List<AColGroup> gs) {
		int numCols = 0;
		for(AColGroup g : gs)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex create(IntArrayList indexes) {
		final int s = indexes.size();
		if(s <= 0)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static long estimateMemoryCost(int nCol, boolean contiguous) {
		if(nCol == 1)
			return SingleIndex.estimateInMemorySizeStatic();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex create(int nCol) {
		if(nCol <= 0)
			throw new DMLRuntimeException("Invalid size of index columns must be above 0");
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	 * @return A column index mapping.
	 */
	public static IColIndex getColumnMapping(IColIndex comb, IColIndex a) {

		// naive scan entire a, there are options that make this faster depending on what comb look like
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java
	}

	public static IColIndex create(int[] indexes) {
		if(indexes.length <= 0)
			throw new DMLRuntimeException("Invalid length to create index from : " + indexes.length);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java

public interface MapToFactory {
	static final Log LOG = LogFactory.getLog(MapToFactory.class.getName());

	public enum MAP_TYPE {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java

public interface MapToFactory {
	static final Log LOG = LogFactory.getLog(MapToFactory.class.getName());

	public enum MAP_TYPE {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(MapToFactory.class.getName());

	public enum MAP_TYPE {
		ZERO, BIT, UBYTE, BYTE, CHAR, CHAR_BYTE, INT;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static AMapToData readIn(DataInput in) throws IOException {
		MAP_TYPE t = MAP_TYPE.values()[in.readByte()];
		switch(t) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static AMapToData create(int size, ABitmap ubm) {
		if(ubm == null)
			return null;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static long estimateInMemorySize(int size, int numTuples) {
		if(numTuples <= 1)
			return MapToZero.getInMemorySize(size);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static AMapToData create(int size, MAP_TYPE t) {
		switch(t) {
			case ZERO:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static AMapToData create(int size, int[] values, int nUnique) {
		AMapToData _data = create(size, nUnique);
		_data.copyInt(values);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	 * @return A new allocated mapToData with the specified type.
	 */
	public static AMapToData resizeForce(AMapToData d, MAP_TYPE t) {
		final int size = d.size();
		final int numTuples = d.getUnique();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	 * @return A new map
	 */
	public static AMapToData create(final int size, final int numTuples) {
		if(numTuples <= 1)
			return new MapToZero(size);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	 * @return The returned hopefully reduced map.
	 */
	public static AMapToData resize(AMapToData d, int numTuples) {
		return d.resize(numTuples);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java
	}

	public static AMapToData create(int size, boolean zeros, IntArrayList[] values) {
		AMapToData _data = create(size, values.length + (zeros ? 1 : 0));

```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/ADictionary.java`
#### Snippet
```java
	protected static final Log LOG = LogFactory.getLog(ADictionary.class.getName());

	public static enum DictType {
		Delta, Dict, MatrixBlock, UInt8, Identity, IdentitySlice;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/functional/LinearRegression.java`
#### Snippet
```java
public interface LinearRegression {

	public static double[] regressMatrixBlock(MatrixBlock rawBlock, IColIndex colIndexes, boolean transposed) {
		final int nRows = transposed ? rawBlock.getNumColumns() : rawBlock.getNumRows();
		final int nCol = colIndexes.size();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	 * @return A combined dictionary
	 */
	public static ADictionary combineFullDictionaries(ADictionary a, int nca, ADictionary b, int ncb) {
		final int ra = a.getNumberOfValues(nca);
		final int rb = b.getNumberOfValues(ncb);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(DblArrayCountHashMap map, int nCols, boolean addZeroTuple, double sparsity) {
		try {
			final ArrayList<DArrCounts> vals = map.extractValues();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineConstSparseSparseRet(double[] tua, ADictionary b, int ncb) {
		final int nca = tua.length;
		final int rb = b.getNumberOfValues(ncb);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(DictionaryFactory.class.getName());

	public enum Type {
		FP64_DICT, MATRIX_BLOCK_DICT, INT8_DICT, IDENTITY, IDENTITY_SLICE,
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(ABitmap ubm, double sparsity, boolean withZeroTuple) {
		return (withZeroTuple) ? createWithAppendedZeroTuple(ubm, sparsity) : create(ubm, sparsity);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(DoubleCountHashMap map) {
		final double[] resValues = map.getDictionary();
		return Dictionary.create(resValues);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary read(DataInput in) throws IOException {
		Type type = Type.values()[in.readByte()];
		switch(type) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineDictionariesSparse(AColGroupCompressed a, AColGroupCompressed b) {
		CompressionType ac = a.getCompType();
		CompressionType bc = b.getCompType();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(ABitmap ubm) {
		return create(ubm, 1.0);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineDictionaries(AColGroupCompressed a, AColGroupCompressed b) {
		if(a instanceof ColGroupEmpty && b instanceof ColGroupEmpty)
			return null; // null return is handled elsewhere.
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary createWithAppendedZeroTuple(ABitmap ubm, double sparsity) {
		final int nVals = ubm.getNumValues();
		final int nRows = nVals + 1;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static long getInMemorySize(int nrValues, int nrColumns, double tupleSparsity, boolean lossy) {
		if(lossy)
			return QDictionary.getInMemorySize(nrValues * nrColumns);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineSparseConstSparseRet(ADictionary a, int nca, double[] tub) {
		final int ncb = tub.length;
		final int ra = a.getNumberOfValues(nca);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineSDC(ADictionary a, double[] tua, ADictionary b, double[] tub) {
		final int nca = tua.length;
		final int ncb = tub.length;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary combineSDCRight(ADictionary a, int nca, ADictionary b, double[] tub) {
		final int ncb = tub.length;
		final int ra = a.getNumberOfValues(nca);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(ABitmap ubm, int defaultIndex, double[] defaultTuple, double sparsity,
		boolean addZero) {
		final int nCol = ubm.getNumColumns();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
	}

	public static ADictionary create(ABitmap ubm, double sparsity) {
		final int nCol = ubm.getNumColumns();
		if(ubm instanceof Bitmap)
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java

public interface DictionaryFactory {
	static final Log LOG = LogFactory.getLog(DictionaryFactory.class.getName());

	public enum Type {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java

public interface DictionaryFactory {
	static final Log LOG = LogFactory.getLog(DictionaryFactory.class.getName());

	public enum Type {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	}

	public static AInsertionSorter create(int numRows, IntArrayList[] offsets, SORT_TYPE st) {
		return create(getEndLength(offsets), numRows, offsets, st);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	}

	public static AInsertionSorter create(int endLength, int numRows, IntArrayList[] offsets, SORT_TYPE st) {
		switch(st) {
			case MERGE:
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	}

	public static AInsertionSorter createNegative(int endLength, int numRows, IntArrayList[] offsets, int negativeIndex,
		SORT_TYPE st) {
		switch(st) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java

public interface InsertionSorterFactory {
	static final Log LOG = LogFactory.getLog(InsertionSorterFactory.class.getName());

	public enum SORT_TYPE {
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java

public interface InsertionSorterFactory {
	static final Log LOG = LogFactory.getLog(InsertionSorterFactory.class.getName());

	public enum SORT_TYPE {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	}

	public static AInsertionSorter createNegative(int numRows, IntArrayList[] offsets, int negativeIndex, SORT_TYPE st) {
		return createNegative(numRows - offsets[negativeIndex].size(), numRows, offsets, negativeIndex, st);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	static final Log LOG = LogFactory.getLog(InsertionSorterFactory.class.getName());

	public enum SORT_TYPE {
		MERGE, MATERIALIZE;
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	final static Log LOG = LogFactory.getLog(EncoderFactory.class.getName());

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, int clen, FrameBlock meta) {
		return createEncoder(spec, colnames, UtilFunctions.nCopies(clen, ValueType.STRING), meta);
	}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, ValueType[] schema,
		FrameBlock meta) {
		return createEncoder(spec, colnames, schema, meta, -1, -1);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, ValueType[] schema, FrameBlock meta,
		int minCol, int maxCol) {
		MultiColumnEncoder encoder;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static ColumnEncoder createInstance(int type) {
		EncoderType etype = EncoderType.values()[type];
		switch(etype) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, ValueType[] schema, int clen,
		FrameBlock meta) {
		ValueType[] lschema = (schema == null) ? UtilFunctions.nCopies(clen, ValueType.STRING) : schema;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static int getEncoderType(ColumnEncoder columnEncoder) {
		//TODO replace with columnEncoder.getType().ordinal
		//(which requires a cleanup of all type handling)
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java
	}

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, int clen, FrameBlock meta,
		int minCol, int maxCol) {
		return createEncoder(spec, colnames, UtilFunctions.nCopies(clen, ValueType.STRING), meta, minCol, maxCol);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java

public interface EncoderFactory {
	final static Log LOG = LogFactory.getLog(EncoderFactory.class.getName());

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, int clen, FrameBlock meta) {
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderFactory.java`
#### Snippet
```java

public interface EncoderFactory {
	final static Log LOG = LogFactory.getLog(EncoderFactory.class.getName());

	public static MultiColumnEncoder createEncoder(String spec, String[] colnames, int clen, FrameBlock meta) {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * @throws jcuda.CudaException if unable to allocate
	 */
	public void allocate(Pointer devPtr, long size) throws jcuda.CudaException;
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * @return the available memory in bytes
	 */
	public long getAvailableMemory();
	
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * @throws jcuda.CudaException if error occurs
	 */
	public void free(Pointer devPtr) throws jcuda.CudaException;
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * @return true if there is enough available memory to allocate a pointer of the given size 
	 */
	public boolean canAllocate(long size);
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
{

	public int getNumRows();

	public int getNumColumns();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * if the given cache block is not amenable.
	 */
	public void toShallowSerializeBlock();
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru, int cl, int cu, T block);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return exact serialized size in bytes of cache block
	 */
	public long getExactSerializedSize();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(IndexRange ixrange, T ret);
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @param appendOnly ?
	 */
	public void merge(T that, boolean appendOnly);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru, int cl, int cu, boolean deep);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * Free unnecessarily allocated empty block.
	 */
	public void compactEmptyBlock();

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return double value at the passed row and column
	 */
	public double getDoubleNaN(int r, int c);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru, boolean deep);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return in-memory size in bytes of cache block
	 */
	public long getInMemorySize();
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return double value at the passed row and column
	 */
	public double getDouble(int r, int c);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return true if shallow serialized
	 */
	public boolean isShallowSerialize(boolean inclConvert);
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return true if shallow serialized
	 */
	public boolean isShallowSerialize();
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	public int getNumRows();

	public int getNumColumns();
	
	public DataCharacteristics getDataCharacteristics();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru, int cl, int cu, boolean deep, T block);
	
	
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru, int cl, int cu);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return sub-block of cache block
	 */
	public CacheBlock<T> slice(int rl, int ru);

	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	 * @return string of the value at the passed row and column
	 */
	public String getString(int r, int c);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlock.java`
#### Snippet
```java
	public int getNumColumns();
	
	public DataCharacteristics getDataCharacteristics();
	
	/**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `src/main/java/org/apache/sysds/runtime/functionobjects/TernaryValueFunction.java`
#### Snippet
```java
	
	public interface ValueFunctionWithConstant {
		public BinaryOperator setOp2Constant(double cnt);
	}
}
```

### UnnecessaryModifier
Modifier `private` is redundant for enum constructors
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      private final int value;

      private ValueType(int value) {
        this.value = value;
      }
```

## RuleId[id=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`1 * 100 * 99` can be replaced with '100 \* 99'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			final long DEFAULT_SIZE = 36 + 2 *
					(100 * 100 * AVERAGE_CHARS_PER_VALUE // Length for digits
					+ 1 * 100 * 99                       // Length for separator chars
					+ 1* 100);                           // Length for line separator chars
			
```

### PointlessArithmeticExpression
`1* 100` can be replaced with '100'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
					(100 * 100 * AVERAGE_CHARS_PER_VALUE // Length for digits
					+ 1 * 100 * 99                       // Length for separator chars
					+ 1* 100);                           // Length for line separator chars
			
			try {
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
			//unrolled 8-block (for better instruction-level parallelism)
			for( int i = bn; i < len; i+=8, bi+=8, ci+=8 ) {
				c[ci+0] |= b[bi+0]; c[ci+1] |= b[bi+1];
				c[ci+2] |= b[bi+2]; c[ci+3] |= b[bi+3];
				c[ci+4] |= b[bi+4]; c[ci+5] |= b[bi+5];
```

### PointlessArithmeticExpression
`bi+0` can be replaced with 'bi'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
			//unrolled 8-block (for better instruction-level parallelism)
			for( int i = bn; i < len; i+=8, bi+=8, ci+=8 ) {
				c[ci+0] |= b[bi+0]; c[ci+1] |= b[bi+1];
				c[ci+2] |= b[bi+2]; c[ci+3] |= b[bi+3];
				c[ci+4] |= b[bi+4]; c[ci+5] |= b[bi+5];
```

### PointlessArithmeticExpression
`1L*1024*1024*1024` can be replaced with '1024 \* 1024 \* 1024'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	//reason: single-threaded text read at 20MB/s, 1GB input -> 50s (should exploit parallelism)
	//note that we scale this threshold up by the degree of available parallelism
	private static final long CP_REBLOCK_THRESHOLD_SIZE = 1L*1024*1024*1024; 
	private static final long CP_CSV_REBLOCK_UNKNOWN_THRESHOLD_SIZE = CP_REBLOCK_THRESHOLD_SIZE;
	
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static void longToBa( final long val, byte[] ba, final int off ) {
		//shift and mask out 8 bytes
		ba[ off+0 ] = (byte)((val >>> 56) & 0xFF);
		ba[ off+1 ] = (byte)((val >>> 48) & 0xFF);
		ba[ off+2 ] = (byte)((val >>> 40) & 0xFF);
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static void intToBa( final int val, byte[] ba, final int off ) {
		//shift and mask out 4 bytes
		ba[ off+0 ] = (byte)((val >>> 24) & 0xFF);
		ba[ off+1 ] = (byte)((val >>> 16) & 0xFF);
		ba[ off+2 ] = (byte)((val >>>  8) & 0xFF);
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static long baToLong( byte[] ba, final int off ) {
		//shift and add 8 bytes into single long
		return ((long)(ba[off+0] & 0xFF) << 56)
			+  ((long)(ba[off+1] & 0xFF) << 48)
			+  ((long)(ba[off+2] & 0xFF) << 40)
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static int baToShort( byte[] ba, final int off ) {
		//shift and add 2 bytes into single int
		return ((ba[off+0] & 0xFF) << 8)
			+  ((ba[off+1] & 0xFF) << 0);
	}
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static void shortToBa( final int val, byte[] ba, final int off ) {
		//shift and mask out 2 bytes
		ba[ off+0 ] = (byte)((val >>>  8) & 0xFF);
		ba[ off+1 ] = (byte)((val >>>  0) & 0xFF);
	}
```

### PointlessArithmeticExpression
`off+0` can be replaced with 'off'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static int baToInt( byte[] ba, final int off ) {
		//shift and add 4 bytes into single int
		return ((ba[off+0] & 0xFF) << 24)
			+  ((ba[off+1] & 0xFF) << 16)
			+  ((ba[off+2] & 0xFF) <<  8)
```

### PointlessArithmeticExpression
`1 * getLength()` can be replaced with 'getLength()'
in `src/main/java/org/apache/sysds/runtime/data/TensorBlock.java`
#### Snippet
```java
					size += getLength() / 2 + getLength() % 2;
				case UINT8:
					size += 1 * getLength(); break;
				case INT32:
				case FP32:
```

### PointlessArithmeticExpression
`1 * getNumColumns()` can be replaced with 'getNumColumns()'
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		long size = 4 + 4 + 1;

		size += 1 * getNumColumns(); // column schema
		// column sizes
		final boolean isDefaultMeta = isColNamesDefault() && isColumnMetadataDefault();
```

### PointlessArithmeticExpression
`1 * 1000 * 1000` can be replaced with '1000 \* 1000'
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java

	/** Buffer size variable: 1M elements, size of default matrix block */
	public static final int BUFFER_SIZE = 1 * 1000 * 1000;

	/** The schema of the data frame as an ordered list of value types */
```

### PointlessArithmeticExpression
`1 * sizeOfDataType` can be replaced with 'sizeOfDataType'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuMatMult.java`
#### Snippet
```java
			// rest of
			// infrastructure can treat it uniformly.
			cudaMemcpy(C, Pointer.to(result), 1 * sizeOfDataType, cudaMemcpyHostToDevice);
		} else if (param.m == 1) {
			// Vector-matrix multiply
```

### PointlessArithmeticExpression
`n * 1 * sizeOfDataType` can be replaced with 'n \* sizeOfDataType'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		// There is an avoidable copy happening here
		MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, instName, outputName, in1.getNumColumns(), 1);
		cudaMemcpy(out.getGPUObject(gCtx).getDensePointer(), bTobj.getDensePointer(), n * 1 * sizeOfDataType, cudaMemcpyDeviceToDevice);

		gCtx.cudaFreeHelper(instName, work, DMLScript.EAGER_CUDA_FREE);
```

### PointlessArithmeticExpression
`aix+0` can be replaced with 'aix'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//compute rest (not aligned to 8-blocks)
		for( int j=0; j<bn; j++, aix++, cix+=n2 )
			c[ cix ] = a[ aix+0 ];
		//unrolled 8-blocks
		for( int j=bn; j<len; j+=8, aix+=8, cix+=8*n2 ) {
```

### PointlessArithmeticExpression
`0*n2` can be replaced with '0'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//unrolled 8-blocks
		for( int j=bn; j<len; j+=8, aix+=8, cix+=8*n2 ) {
			c[ cix + 0*n2 ] = a[ aix+0 ];
			c[ cix + 1*n2 ] = a[ aix+1 ];
			c[ cix + 2*n2 ] = a[ aix+2 ];
```

### PointlessArithmeticExpression
`aix+0` can be replaced with 'aix'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//unrolled 8-blocks
		for( int j=bn; j<len; j+=8, aix+=8, cix+=8*n2 ) {
			c[ cix + 0*n2 ] = a[ aix+0 ];
			c[ cix + 1*n2 ] = a[ aix+1 ];
			c[ cix + 2*n2 ] = a[ aix+2 ];
```

### PointlessArithmeticExpression
`1*n2` can be replaced with 'n2'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		for( int j=bn; j<len; j+=8, aix+=8, cix+=8*n2 ) {
			c[ cix + 0*n2 ] = a[ aix+0 ];
			c[ cix + 1*n2 ] = a[ aix+1 ];
			c[ cix + 2*n2 ] = a[ aix+2 ];
			c[ cix + 3*n2 ] = a[ aix+3 ];
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		for( int i=ai+bn; i<ai+len; i+=8 )
		{
			c[ aix[ i+0 ] ] ++;
			c[ aix[ i+1 ] ] ++;
			c[ aix[ i+2 ] ] ++;
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		for( int i=bn; i<len; i+=8 )
		{
			c[ ai[ i+0 ] ] ++;
			c[ ai[ i+1 ] ] ++;
			c[ ai[ i+2 ] ] ++;
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction level parallelism)
		for( int i=ai+bn; i<ai+len; i+=8 ) {
			c[ aix[ i+0 ] ] ++;
			c[ aix[ i+1 ] ] ++;
			c[ aix[ i+2 ] ] ++;
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction level parallelism)
		for( int i=bn; i<len; i+=8 ) {
			c[i+0] += a[ai+i+0]!=0 ? 1 : 0;
			c[i+1] += a[ai+i+1]!=0 ? 1 : 0;
			c[i+2] += a[ai+i+2]!=0 ? 1 : 0;
```

### PointlessArithmeticExpression
`ai+i+0` can be replaced with 'ai + i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction level parallelism)
		for( int i=bn; i<len; i+=8 ) {
			c[i+0] += a[ai+i+0]!=0 ? 1 : 0;
			c[i+1] += a[ai+i+1]!=0 ? 1 : 0;
			c[i+2] += a[ai+i+2]!=0 ? 1 : 0;
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction level parallelism)
		for( int i=ai+bn; i<ai+len; i+=8 ) {
			c[ ci+aix[ i+0 ] ] --;
			c[ ci+aix[ i+1 ] ] --;
			c[ ci+aix[ i+2 ] ] --;
```

### PointlessArithmeticExpression
`1.0 * (lsparse ? r1 * c1 * s1 : r1 * c1)` can be replaced with '(lsparse ? r1 \* c1 \* s1 : r1 \* c1)'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				boolean lsparse = MatrixBlock.evalSparseFormatInMemory(r1, c1, nnz1);
				//if (inst.getOpcode().equalsIgnoreCase("rightIndex"))
					nflops = 1.0 * (lsparse ? r1 * c1 * s1 : r1 * c1); //FIXME
				break;
			}
```

### PointlessArithmeticExpression
`1.0 * ((lsparse ? r1*c1*s1 : r1*c1) + (rsparse ? r2*c2*s2 : r2*c2))` can be replaced with '((lsparse ? r1\*c1\*s1 : r1\*c1) + (rsparse ? r2\*c2\*s2 : r2\*c2))'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				double s2 = OptimizerUtils.getSparsity(r2, c2, nnz2);
				boolean rsparse = MatrixBlock.evalSparseFormatInMemory(r2, c2, nnz2);
				nflops = 1.0 * ((lsparse ? r1*c1*s1 : r1*c1) + (rsparse ? r2*c2*s2 : r2*c2));
				break;
			}
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
		for( int i = ai+bn; i < ai+len; i+=8 ) {
			//read 64B cacheline of a, compute cval' = sum(a) + cval
			val += a[ i+0 ] + a[ i+1 ] + a[ i+2 ] + a[ i+3 ]
			     + a[ i+4 ] + a[ i+5 ] + a[ i+6 ] + a[ i+7 ];
		}
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = a * b
			//write back 64B cacheline of c = c'
			c[ ci+0 ] = a[ ai+0 ] * b[ bi+0 ];
			c[ ci+1 ] = a[ ai+1 ] * b[ bi+1 ];
			c[ ci+2 ] = a[ ai+2 ] * b[ bi+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = a * b
			//write back 64B cacheline of c = c'
			c[ ci+0 ] = a[ ai+0 ] * b[ bi+0 ];
			c[ ci+1 ] = a[ ai+1 ] * b[ bi+1 ];
			c[ ci+2 ] = a[ ai+2 ] * b[ bi+2 ];
```

### PointlessArithmeticExpression
`bi+0` can be replaced with 'bi'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = a * b
			//write back 64B cacheline of c = c'
			c[ ci+0 ] = a[ ai+0 ] * b[ bi+0 ];
			c[ ci+1 ] = a[ ai+1 ] * b[ bi+1 ];
			c[ ci+2 ] = a[ ai+2 ] * b[ bi+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a[ ai+0 ];
			c[ ci+1 ] += a[ ai+1 ];
			c[ ci+2 ] += a[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a[ ai+0 ];
			c[ ci+1 ] += a[ ai+1 ];
			c[ ci+2 ] += a[ ai+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] *= a[ ai+0 ];
			c[ ci+1 ] *= a[ ai+1 ];
			c[ ci+2 ] *= a[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] *= a[ ai+0 ];
			c[ ci+1 ] *= a[ ai+1 ];
			c[ ci+2 ] *= a[ ai+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better instruction-level parallelism)
		for( int j = ci+bn; j < ci+len; j+=8) {
			c[ j+0 ] += aval; c[ j+1 ] += aval; 
			c[ j+2 ] += aval; c[ j+3 ] += aval;
			c[ j+4 ] += aval; c[ j+5 ] += aval;
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B of b via 'gather'
			//compute cval' = sum(a * b) + cval
			val += a[ i+0 ] * b[ bi+aix[i+0] ]
			     + a[ i+1 ] * b[ bi+aix[i+1] ]
			     + a[ i+2 ] * b[ bi+aix[i+2] ]
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B of b via 'gather'
			//compute cval' = sum(a * b) + cval
			val += a[ i+0 ] * b[ bi+aix[i+0] ]
			     + a[ i+1 ] * b[ bi+aix[i+1] ]
			     + a[ i+2 ] * b[ bi+aix[i+2] ]
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better ILP)
		for( int j = bn; j < len; j+=8, ai+=8, ci+=8) {
			c[ ci+0 ] += a[ ai+0 ] + bval;
			c[ ci+1 ] += a[ ai+1 ] + bval;
			c[ ci+2 ] += a[ ai+2 ] + bval;
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better ILP)
		for( int j = bn; j < len; j+=8, ai+=8, ci+=8) {
			c[ ci+0 ] += a[ ai+0 ] + bval;
			c[ ci+1 ] += a[ ai+1 ] + bval;
			c[ ci+2 ] += a[ ai+2 ] + bval;
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ];
```

### PointlessArithmeticExpression
`bi1+0` can be replaced with 'bi1'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ];
```

### PointlessArithmeticExpression
`bi2+0` can be replaced with 'bi2'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ];
```

### PointlessArithmeticExpression
`bi3+0` can be replaced with 'bi3'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ];
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B cachelines of a and b
			//compute cval' = sum(a * b) + cval
			val += a[ i+0 ] * b[ i+0 ]
			     + a[ i+1 ] * b[ i+1 ]
			     + a[ i+2 ] * b[ i+2 ]
```

### PointlessArithmeticExpression
`i+0` can be replaced with 'i'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B cachelines of a and b
			//compute cval' = sum(a * b) + cval
			val += a[ i+0 ] * b[ i+0 ]
			     + a[ i+1 ] * b[ i+1 ]
			     + a[ i+2 ] * b[ i+2 ]
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] -= a[ ai+0 ];
			c[ ci+1 ] -= a[ ai+1 ];
			c[ ci+2 ] -= a[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c * a
			//write back 64B cacheline of c = c'
			c[ ci+0 ] -= a[ ai+0 ];
			c[ ci+1 ] -= a[ ai+1 ];
			c[ ci+2 ] -= a[ ai+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] = aval * b[ bi+0 ];
			c[ ci+1 ] = aval * b[ bi+1 ];
			c[ ci+2 ] = aval * b[ bi+2 ];
```

### PointlessArithmeticExpression
`bi+0` can be replaced with 'bi'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] = aval * b[ bi+0 ];
			c[ ci+1 ] = aval * b[ bi+1 ];
			c[ ci+2 ] = aval * b[ bi+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B of c = c' via 'scatter'
			c[ ci+bix[j+0] ] += aval * b[ j+0 ];
			c[ ci+bix[j+1] ] += aval * b[ j+1 ];
			c[ ci+bix[j+2] ] += aval * b[ j+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B of c = c' via 'scatter'
			c[ ci+bix[j+0] ] += aval * b[ j+0 ];
			c[ ci+bix[j+1] ] += aval * b[ j+1 ];
			c[ ci+bix[j+2] ] += aval * b[ j+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better instruction-level parallelism)
		for( int j = bn; j < len; j+=8, ci+=8) {
			c[ ci+0 ] *= aval; c[ ci+1 ] *= aval;
			c[ ci+2 ] *= aval; c[ ci+3 ] *= aval;
			c[ ci+4 ] *= aval; c[ ci+5 ] *= aval;
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ] + aval4 * b[ bi4+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ] + aval4 * b[ bi4+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ] + aval4 * b[ bi4+2 ];
```

### PointlessArithmeticExpression
`bi1+0` can be replaced with 'bi1'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ] + aval4 * b[ bi4+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ] + aval4 * b[ bi4+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ] + aval4 * b[ bi4+2 ];
```

### PointlessArithmeticExpression
`bi2+0` can be replaced with 'bi2'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ] + aval4 * b[ bi4+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ] + aval4 * b[ bi4+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ] + aval4 * b[ bi4+2 ];
```

### PointlessArithmeticExpression
`bi3+0` can be replaced with 'bi3'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ] + aval4 * b[ bi4+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ] + aval4 * b[ bi4+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ] + aval4 * b[ bi4+2 ];
```

### PointlessArithmeticExpression
`bi4+0` can be replaced with 'bi4'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ] + aval3 * b[ bi3+0 ] + aval4 * b[ bi4+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ] + aval3 * b[ bi3+1 ] + aval4 * b[ bi4+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ] + aval3 * b[ bi3+2 ] + aval4 * b[ bi4+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction-level parallelism)
		for( int j = bi+bn; j < bi+len; j+=8 ) {
			c[ ci+bix[j+0] ] = a[ ai+bix[j+0] ] * b[ j+0 ];
			c[ ci+bix[j+1] ] = a[ ai+bix[j+1] ] * b[ j+1 ];
			c[ ci+bix[j+2] ] = a[ ai+bix[j+2] ] * b[ j+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction-level parallelism)
		for( int j = bi+bn; j < bi+len; j+=8 ) {
			c[ ci+bix[j+0] ] = a[ ai+bix[j+0] ] * b[ j+0 ];
			c[ ci+bix[j+1] ] = a[ ai+bix[j+1] ] * b[ j+1 ];
			c[ ci+bix[j+2] ] = a[ ai+bix[j+2] ] * b[ j+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block (for better instruction-level parallelism)
		for( int j = bi+bn; j < bi+len; j+=8 ) {
			c[ ci+bix[j+0] ] = a[ ai+bix[j+0] ] * b[ j+0 ];
			c[ ci+bix[j+1] ] = a[ ai+bix[j+1] ] * b[ j+1 ];
			c[ ci+bix[j+2] ] = a[ ai+bix[j+2] ] * b[ j+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B cachelines of a and b
			//compute cval' = sum(a * b) + cval
			val += a[ ai+0 ] * b[ bi+0 ]
			     + a[ ai+1 ] * b[ bi+1 ]
			     + a[ ai+2 ] * b[ bi+2 ]
```

### PointlessArithmeticExpression
`bi+0` can be replaced with 'bi'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//read 64B cachelines of a and b
			//compute cval' = sum(a * b) + cval
			val += a[ ai+0 ] * b[ bi+0 ]
			     + a[ ai+1 ] * b[ bi+1 ]
			     + a[ ai+2 ] * b[ bi+2 ]
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better instruction-level parallelism)
		for( int j = ai+bn; j < ai+alen; j+=8 ) {
			c[ ci+aix[j+0] ] += a[ j+0 ];
			c[ ci+aix[j+1] ] += a[ j+1 ];
			c[ ci+aix[j+2] ] += a[ j+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//unrolled 8-block  (for better instruction-level parallelism)
		for( int j = ai+bn; j < ai+alen; j+=8 ) {
			c[ ci+aix[j+0] ] += a[ j+0 ];
			c[ ci+aix[j+1] ] += a[ j+1 ];
			c[ ci+aix[j+2] ] += a[ j+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c + a1 + a2 + a3 + a4
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a1[ ai+0 ] + a2[ ai+0 ] + a3[ ai+0 ] + a4[ ai+0 ];
			c[ ci+1 ] += a1[ ai+1 ] + a2[ ai+1 ] + a3[ ai+1 ] + a4[ ai+1 ];
			c[ ci+2 ] += a1[ ai+2 ] + a2[ ai+2 ] + a3[ ai+2 ] + a4[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c + a1 + a2 + a3 + a4
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a1[ ai+0 ] + a2[ ai+0 ] + a3[ ai+0 ] + a4[ ai+0 ];
			c[ ci+1 ] += a1[ ai+1 ] + a2[ ai+1 ] + a3[ ai+1 ] + a4[ ai+1 ];
			c[ ci+2 ] += a1[ ai+2 ] + a2[ ai+2 ] + a3[ ai+2 ] + a4[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c + a1 + a2 + a3 + a4
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a1[ ai+0 ] + a2[ ai+0 ] + a3[ ai+0 ] + a4[ ai+0 ];
			c[ ci+1 ] += a1[ ai+1 ] + a2[ ai+1 ] + a3[ ai+1 ] + a4[ ai+1 ];
			c[ ci+2 ] += a1[ ai+2 ] + a2[ ai+2 ] + a3[ ai+2 ] + a4[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c + a1 + a2 + a3 + a4
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a1[ ai+0 ] + a2[ ai+0 ] + a3[ ai+0 ] + a4[ ai+0 ];
			c[ ci+1 ] += a1[ ai+1 ] + a2[ ai+1 ] + a3[ ai+1 ] + a4[ ai+1 ];
			c[ ci+2 ] += a1[ ai+2 ] + a2[ ai+2 ] + a3[ ai+2 ] + a4[ ai+2 ];
```

### PointlessArithmeticExpression
`ai+0` can be replaced with 'ai'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = c + a1 + a2 + a3 + a4
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += a1[ ai+0 ] + a2[ ai+0 ] + a3[ ai+0 ] + a4[ ai+0 ];
			c[ ci+1 ] += a1[ ai+1 ] + a2[ ai+1 ] + a3[ ai+1 ] + a4[ ai+1 ];
			c[ ci+2 ] += a1[ ai+2 ] + a2[ ai+2 ] + a3[ ai+2 ] + a4[ ai+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B of c = c' via 'scatter'
			c[ ci+bix[j+0] ] += aval * b[ j+0 ];
			c[ ci+bix[j+1] ] += aval * b[ j+1 ];
			c[ ci+bix[j+2] ] += aval * b[ j+2 ];
```

### PointlessArithmeticExpression
`j+0` can be replaced with 'j'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B of c = c' via 'scatter'
			c[ ci+bix[j+0] ] += aval * b[ j+0 ];
			c[ ci+bix[j+1] ] += aval * b[ j+1 ];
			c[ ci+bix[j+2] ] += aval * b[ j+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ];
```

### PointlessArithmeticExpression
`bi1+0` can be replaced with 'bi1'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ];
```

### PointlessArithmeticExpression
`bi2+0` can be replaced with 'bi2'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval_1 * b_1 + aval_2 * b_2 + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval1 * b[ bi1+0 ] + aval2 * b[ bi2+0 ];
			c[ ci+1 ] += aval1 * b[ bi1+1 ] + aval2 * b[ bi2+1 ];
			c[ ci+2 ] += aval1 * b[ bi1+2 ] + aval2 * b[ bi2+2 ];
```

### PointlessArithmeticExpression
`ci+0` can be replaced with 'ci'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval * b[ bi+0 ];
			c[ ci+1 ] += aval * b[ bi+1 ];
			c[ ci+2 ] += aval * b[ bi+2 ];
```

### PointlessArithmeticExpression
`bi+0` can be replaced with 'bi'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//compute c' = aval * b + c
			//write back 64B cacheline of c = c'
			c[ ci+0 ] += aval * b[ bi+0 ];
			c[ ci+1 ] += aval * b[ bi+1 ];
			c[ ci+2 ] += aval * b[ bi+2 ];
```

### PointlessArithmeticExpression
`_value.length() * 1` can be replaced with '_value.length()'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/StringObject.java`
#### Snippet
```java
	@Override
	public int getSize() {
		return 16 + _value.length() * 1;
	}

```

### PointlessArithmeticExpression
`1*1024*1024` can be replaced with '1024 \* 1024'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/StringObject.java`
#### Snippet
```java
	private static final long serialVersionUID = 2464839775369002455L;

	private static final int MAX_STRING_SIZE = 1*1024*1024; //1MB
	
	private final String _value;
```

### PointlessArithmeticExpression
`1.0 * OptimizationWrapper.PAR_FACTOR_INFRASTRUCTURE` can be replaced with 'OptimizationWrapper.PAR_FACTOR_INFRASTRUCTURE'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
	
	public static final double PAR_K_FACTOR        = OptimizationWrapper.PAR_FACTOR_INFRASTRUCTURE; 
	public static final double PAR_K_MR_FACTOR     = 1.0 * OptimizationWrapper.PAR_FACTOR_INFRASTRUCTURE; 
	
	//problem and infrastructure properties
```

### PointlessArithmeticExpression
`1 * getColumnNamesList().size()` can be replaced with 'getColumnNamesList().size()'
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getColumnNamesList().size();
      }
      {
```

### PointlessArithmeticExpression
`1 * getColumnDataList().size()` can be replaced with 'getColumnDataList().size()'
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        }
        size += dataSize;
        size += 1 * getColumnDataList().size();
      }
      for (int i = 0; i < columnSchema_.size(); i++) {
```

## RuleId[id=LoopConditionNotUpdatedInsideLoop]
### LoopConditionNotUpdatedInsideLoop
Condition '!_stopped' is not updated inside loop
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalParWorker.java`
#### Snippet
```java
		Task lTask = null; 
		try {
			while( !_stopped ) {
				//dequeue the next task (abort on NO_MORE_TASKS or error)
				try {
```

## RuleId[id=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/UnaryOp.java`
#### Snippet
```java
	{
		Hop input = getInput().get(0);
		if ( getDataType() == DataType.SCALAR )  {
			//do nothing always known
		}
```

### EmptyStatementBody
`else` statement has empty body
in `src/main/java/org/apache/sysds/hops/DataOp.java`
#### Snippet
```java
			setDim2(ncol);
		}
		else { //READ
			//do nothing; dimensions updated via set output params
		}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		Hop input3 = getInput().get(2);

		if ( getDataType() == DataType.SCALAR ) 
		{
			//do nothing always known
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteHoistLoopInvariantOperations.java`
#### Snippet
```java
				collectOperations(csb, candInputs, invariantOps);
		}
		else if( sb instanceof IfStatementBlock ) {
			//note: for now we do not pull loop-invariant code out of
			//if statement blocks because these operations are conditionally
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCommonSubexpressionElimination.java`
#### Snippet
```java
					Hop h2 = hop.getParent().get(j);
					
					if( h1==h2 ) {
						//do nothing, note: we should not remove redundant parent links
						//(otherwise rewrites would need to take this property into account) 
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteBlockSizeAndReblock.java`
#### Snippet
```java
				else if( dop.getOp() == OpOpData.PERSISTENTWRITE )
				{
					if (dop.getBlocksize() == -1)
					{
						// if this dataop is for cell output, then no reblock is needed 
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
				rule_GPUKernels(roots, hi, descendFirst); //see below
			
			if(roots != null) {
				//hi = batchNormTrain(roots, hop, hi, i);
			}
```

### EmptyStatementBody
`else` statement has empty body
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
					hop.refreshColsParameterInformation(hop.getInput().get(2), vars, memo);
				}
			} else {
				//TODO tensor rewrite
			}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			removeUpdatedScalars(vars, fpb.getStatementBlock());
		}
		else if (  pb instanceof FunctionProgramBlock ) //includes ExternalFunctionProgramBlock and ExternalFunctionProgramBlockCP
		{
			//do nothing
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/ParserWrapper.java`
#### Snippet
```java
		if (resPath.startsWith(".")) {
			resPath = resPath.substring(1);
		} else if (resPath.startsWith("\\")) {
			// do nothing
		} else if (!resPath.startsWith("/")) {
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
		checkAndSetDimensions(output, conditional);
		if (getOpCode() == Expression.BinaryOp.MATMULT) {
			if ((getLeft().getOutput().getDataType() != DataType.MATRIX) || (getRight().getOutput().getDataType() != DataType.MATRIX)) {
		// remove exception for now
		//		throw new LanguageException(
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			}
			// no work to perform for PathStatement or ImportStatement
			else if (current instanceof PathStatement){}
			else if (current instanceof ImportStatement){}
			else {
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			// no work to perform for PathStatement or ImportStatement
			else if (current instanceof PathStatement){}
			else if (current instanceof ImportStatement){}
			else {
				raiseValidateError("cannot process statement of type " + current.getClass().getSimpleName(), conditional);
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			}
		}
		if (target == null) {
			// function has no return value
		}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
				}
			}
			else if (fnameStr.equals(Statement.GAGG_FN_COUNT) 
					|| fnameStr.equals(Statement.GAGG_FN_SUM) 
					|| fnameStr.equals(Statement.GAGG_FN_MEAN)
```

### EmptyStatementBody
`while` statement has empty body
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCell.java`
#### Snippet
```java
			
			// skip until end-of-comments
			while( (value = br.readLine())!=null && value.charAt(0) == '%' ) {
				//do nothing just skip comments
			}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
							DataIdentifier dat2 = write;
							if( c._dat == dat2 ) continue; //skip self-check
							if( runEqualsCheck(c._dat, dat2) ) {
								//intra-iteration output dependencies (same index function) are OK
							}
```

### EmptyStatementBody
`while` statement has empty body
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		
		if( ix >= 0 ){ //match, scan to next val
			while( value==bv[ix++] && ix<bv.length ) {}
			ix += (value==bv[bv.length-1])?1:0;
		}
```

### EmptyStatementBody
`else` statement has empty body
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
						quickSetValue(r, 0, newMaxIndex);
						quickSetValue(r, 1, newMaxValue);
					} else {
						// Other return value ==> current answer is best
					}
```

### EmptyStatementBody
`else` statement has empty body
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
							quickSetValue(r, 0, newMaxIndex);
							cor.quickSetValue(r, 0, newMaxValue);
						} else {
							// Other return value ==> current answer is best
						}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
				counter.incCMM(count * d);
		}
		else if(o instanceof OpMetadata) {
			// ignore it
		}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			if (dt == DataType.MATRIX || dt == DataType.FRAME || dt == DataType.LIST) {
				DataCharacteristics mc = new MatrixCharacteristics();
				if (parts.length == 6) {
					// do nothing
				}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			else if (dt == DataType.TENSOR) {
				TensorCharacteristics tc = new TensorCharacteristics(new long[]{1, 1}, 0);
				if (parts.length == 6) {
					// do nothing
				}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			}
		}
		else if (  pb instanceof FunctionProgramBlock ) {
			//do nothing
		}
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			if ( h.getForcedExecType() != Types.ExecType.SPARK) {
				double mem = _cost.getLeafNodeEstimate(TestMeasure.MEMORY_USAGE, n, Types.ExecType.CP);
				if (mem >= OptimizerUtils.DEFAULT_SIZE) {
					// memory estimate for worst case scenario.
					// optimistically ignoring this
```

### EmptyStatementBody
`if` statement has empty body
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
      private void maybeForceBuilderInitialization() {
        if (com.google.protobuf.GeneratedMessageV3
                .alwaysUseFieldBuilders) {
        }
```

## RuleId[id=CommentedOutCode]
### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
			LOG.error("Mutually exclusive options were selected. " + e.getMessage());
			//TODO fix null default options
			//HelpFormatter formatter = new HelpFormatter();
			//formatter.printHelp( "systemds", DMLOptions.defaultOptions.options );
			return false;
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
			LOG.error("Parsing Exception " + e.getMessage());
			//TODO fix null default options
			//HelpFormatter formatter = new HelpFormatter();
			//formatter.printHelp( "systemds", DMLOptions.defaultOptions.options );
			return false;
```

### CommentedOutCode
Commented out code (6 lines)
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
					&& (!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT))) {
				// uncomment if we support arrays of doubles for matrices
				// if (dataType instanceof ArrayType) {
				// ArrayType arrayType = (ArrayType) dataType;
				// if (arrayType.elementType() == DataTypes.DoubleType) {
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/UnaryOp.java`
#### Snippet
```java
		if( isMetadataOperation() ) {
			_memEstimate = OptimizerUtils.INT_SIZE;
			//_outputMemEstimate = OptimizerUtils.INT_SIZE;
			//_processingMemEstimate = 0;
		}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case LOG_NZ:
					ret = sp1; break;
				//case EQUAL: //doesnt work on worstcase estimates, but on 
				//	ret = 1-Math.abs(sp1-sp2); break;
				default:
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
				ret = mbytes / DEFAULT_MBS_HDFSWRITE_BINARYBLOCK_DENSE;
		}
		//if( LOG.isDebugEnabled() )
		//	LOG.debug("Costs[export] = "+ret+"s, "+mbytes+" MB ("+dm+","+dn+","+ds+").");
		
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		double costs = ltime + etime + wtime;
		
		//if( LOG.isDebugEnabled() )
		//	LOG.debug("Costs CP instruction = "+costs);
		
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
			ret = mbytes / DEFAULT_MBS_HDFSWRITE_BINARYBLOCK_DENSE;
		
		//if( LOG.isDebugEnabled() )
		//	LOG.debug("Costs[export] = "+ret+"s, "+mbytes+" MB ("+dm+","+dn+","+ds+").");
		
```

### CommentedOutCode
Commented out code (14 lines)
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		double sparsity = (nnz == 0) ? 0 : 1;
		double ret;
		/*
		if( isMatrixMultiply() ) {	
			if( nnz < 0 ){
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
		}
		
		//protected abstract BitsetMatrix diag();
		
		//protected abstract BitsetMatrix reshape(int rows, int cols, boolean byrow);
```

### CommentedOutCode
Commented out code (11 lines)
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
				//make class available and maintain hits
				if(cla != null) {
//					if(CodegenUtils.getNativeOpData(cla.getName()) != null) {
//						if(tmp.getValue().getVarname() == null) {
//							tmp.getValue().setVarName(cla.getName());
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
			tmp = tmp.replace("%IN"+(j+1)+"v%", varj+"vals");
			tmp = tmp.replace("%IN"+(j+1)+"i%", varj+"ix");
//			tmp = tmp.replace("%IN"+(j+1)+"%", 
//				varj.startsWith("b") ? varj + ".values(rix)" : varj );
			tmp = tmp.replace("%IN"+(j+1)+"%",
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeRow.java`
#### Snippet
```java
//	private static final String TEMPLATE_ROWAGG_OUT_CUDA  = "\t\tif(threadIdx.x == 0){\n\t\t\t*(c.vals(rix)) = %IN%;\n//printf(\"rix=%d TMP7=%f TMP8=%f %IN%=%f\\n\",rix, TMP7, TMP8,%IN%);\n}\n";
private static final String TEMPLATE_ROWAGG_OUT_CUDA  = "\t\tif(threadIdx.x == 0){\n\t\t\t*(c.vals(rix)) = %IN%;\n\t\t}\n";
//	private static final String TEMPLATE_FULLAGG_OUT_CUDA =
//		"\t\tif(threadIdx.x == 0) {\n\t\t\tT old = atomicAdd(c.vals(0), %IN%);\n//\t\t\tprintf(\"bid=%d full_agg add %f to %f\\n\",blockIdx.x, %IN%, old);\n\t\t}\n";
	private static final String TEMPLATE_FULLAGG_OUT_CUDA =
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java

		// remove empty lines
//		if(!api.isJava())
//			tmpDense = tmpDense.replaceAll("(?m)^[ \t]*\r?\n", "");

```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCommonSubexpressionElimination.java`
#### Snippet
```java
						
						//remove redundant h2 from parent list
						//hop.getParent().remove(j);
						//j--;
					}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteBlockSizeAndReblock.java`
#### Snippet
```java
		{
			// TODO: following two lines are commented, and the subsequent hack is used instead!
			//set_rows_per_block(GLOBAL_BLOCKSIZE);
			//set_cols_per_block(GLOBAL_BLOCKSIZE);
			
```

### CommentedOutCode
Commented out code (8 lines)
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			// Valid with uncompressed outputs
			boolean compUCOut = false;
			// // tsmm
			// compUCOut |= (current instanceof AggBinaryOp && current.getDim2() <= current.getBlocksize() &&
			// ((AggBinaryOp) current).checkTransposeSelf() == MMTSJType.LEFT);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/lops/TernaryAggregate.java`
#### Snippet
```java
	
	//NOTE: currently only used for ta+*
	//private AggOp _aggOp = null;
	//private Binary.OperationTypes _binOp = null;
	private Direction _direction;
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/lops/TernaryAggregate.java`
#### Snippet
```java
		super(Lop.Type.TernaryAggregate, dt, vt);
		
		//_aggOp = aggOp;
		//_binOp = binOp;
		
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				hnew = HopRewriteUtils.createNary(OpOpN.PLUS, mms);
				//cleanup parent references from rbind left/right
				//HopRewriteUtils.removeAllChildReferences(hi.getInput().get(0).getInput().get(0));
				//HopRewriteUtils.removeAllChildReferences(hi.getInput().get(1));
				branch = 2;
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			}

			// long t1 = System.nanoTime();
			// System.out.println("R is finished (in "+((double)t1-t0)/1000000000+" sec)");
			System.out.println("Done installing packages required for running integration tests.");
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java

				// generate an instruction that creates a symbol table entry for the new variable
				//String createInst = prepareVariableInstruction("createvar", node);
				//out.addPreInstruction(CPInstructionParser.parseSingleInstruction(createInst));
				int blen = (int) oparams.getBlocksize();
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/parser/ImportStatement.java`
#### Snippet
```java
{
		
	/*
	// indicates the imported list of function names 
	private ArrayList<String> _importVarList;
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
			if ((getLeft().getOutput().getDataType() != DataType.MATRIX) || (getRight().getOutput().getDataType() != DataType.MATRIX)) {
		// remove exception for now
		//		throw new LanguageException(
		//				"Matrix multiplication not supported for scalars",
		//				LanguageException.LanguageErrorCodes.INVALID_PARAMETERS);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/parser/IterablePredicate.java`
#### Snippet
```java
	@Override
	public Expression rewriteExpression(String prefix) {
		//DataIdentifier newIterVar = (DataIdentifier)_iterVar.rewriteExpression(prefix);
		//return new IterablePredicate(newIterVar, _from, _to, _increment);
		throw new LanguageException(this.printErrorLocation() + "rewriteExpression not supported for IterablePredicate");
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/WhileStatementBlock.java`
#### Snippet
```java
				//handle sparsity change
				//NOTE: nnz not propagated via validate, and hence, we conservatively assume that nnz have been changed.
				//long startVersionNNZ 	= startVersion.getNnz();
				//long endVersionNNZ    = endVersion.getNnz(); 
				//boolean nnzUnchanged  = (startVersionNNZ == endVersionNNZ);
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
					}
					//NOTE: nnz not propagated via validate, and hence, we conservatively assume that nnz have been changed.
					//if( origVersion.getNnz() == recVersion.getNnz() ){
					//	updatedNnz = recVersion.getNnz();
					//}
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
				//handle sparsity change
				//NOTE: nnz not propagated via validate, and hence, we conservatively assume that nnz have been changed.
				//long startVersionNNZ 	= startVersion.getNnz();
				//long endVersionNNZ    = endVersion.getNnz(); 
				//boolean nnzUnchanged  = (startVersionNNZ == endVersionNNZ);
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					_kill.addVariable(var, _updated.getVariable(var));

					//if (!_read.containsVariable(var)) {
					//	_kill.addVariable(var, _updated.getVariable(var));
					//}
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java

			//validate correct value types
			/*if (getVarParam(RAND_DATA) != null && (getVarParam(RAND_DATA) instanceof BooleanIdentifier)){
				raiseValidateError("for tensor statement " + RAND_DATA + " has incorrect value type", conditional);
			}*/
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		// Process the case where user provides double values to rows or cols
		// TODO convert double Matrix to long Matrix
		/*if (paramName.equals(RAND_DIMS) && paramValue instanceof DoubleIdentifier) {
			paramValue = new IntIdentifier((long) ((DoubleIdentifier) paramValue).getValue(), this);
		} else if (paramName.equals(RAND_COLS) && paramValue instanceof DoubleIdentifier) {
```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONL.java`
#### Snippet
```java
package org.apache.sysds.runtime.io;

// import static org.apache.sysds.runtime.io.FrameReader.LOG;
// import static org.apache.sysds.runtime.io.FrameReader.checkValidInputFile;
// import static org.apache.sysds.runtime.io.FrameReader.createOutputFrameBlock;
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/io/MatrixWriterFactory.java`
#### Snippet
```java
				//requirement of writing out a single file

				//if( OptimizerUtils.PARALLEL_CP_WRITE_TEXTFORMATS )
				//	writer = new WriterMatrixMarketParallel();
				writer = new WriterMatrixMarket();
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/util/BinaryBlockRecordReader.java`
#### Snippet
```java
		//long t0 = System.nanoTime();		
		boolean ret = super.next(key, value);		
		//long t1 = System.nanoTime();
		
		//_time+=(t1-t0);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
	// see https://hadoop.apache.org/docs/current/hadoop-project-dist/hadoop-common/DeprecatedProperties.html
	public static final String DFS_BLOCKSIZE = "dfs.blocksize"; // hdfs-default.xml
	// public static final String DFS_DATANODE_DATA_DIR; // hdfs-default.xml - currently not used
	// public static final String DFS_METRICS_SESSION_ID; // N/A - currently not used
	public static final String DFS_PERMISSIONS_ENABLED = "dfs.permissions.enabled"; // hdfs-default.xml
```

### CommentedOutCode
Commented out code (10 lines)
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java

	public static double[] convertStringToDoubleArray(String[] original) {
//		double[] ret = new double[original.length];
//		for (int i = 0; i < original.length; i++) {
//			try {
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibCommonsMath.java`
#### Snippet
```java
		BlockRealMatrix vectorInput = DataConverter.convertToBlockRealMatrix(in2);
		
		/*LUDecompositionImpl ludecompose = new LUDecompositionImpl(matrixInput);
		DecompositionSolver lusolver = ludecompose.getSolver();
		RealMatrix solutionMatrix = lusolver.solve(vectorInput);*/
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
				if((str.charAt(i) == '-') && (i == 0))
					continue;
//				if((str.charAt(i) == '.') && (sz > 1))
//					continue;
				return false;
```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
			// until a sparse specific kernel is written.
			in1.getGPUObject(gCtx).sparseToDense(instName);
			// long nnz = in1.getNnz();
			// assert nnz > 0 : "Internal Error - number of non zeroes set to " + nnz + " in Aggregate Binary for GPU";
			// MatrixObject out = ec.getSparseMatrixOutputForGPUInstruction(output, nnz);
```

### CommentedOutCode
Commented out code (17 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		return (int) reduceAll(gCtx, null, "compute_nnz", densePtr, length);
		// This is extremely slow
//		cusparseMatDescr matDescr = CSRPointer.getDefaultCuSparseMatrixDescriptor();
//		cusparseHandle cusparseHandle = gCtx.getCusparseHandle();
//		if(_TMP_NNZ_ROW_PTR == null) {
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
			aggregateBinaryMatrixDenseGeneric(in, aggVal, aggCorr);
		
		//System.out.println("agg ("+in.rlen+","+in.clen+","+in.nonZeros+","+in.sparse+"), ("+naggVal+","+saggVal+"), ("+naggCorr+","+saggCorr+") -> " +
		//	"("+aggVal.nonZeros+","+aggVal.sparse+"), ("+aggCorr.nonZeros+","+aggCorr.sparse+") in "+time.stop()+"ms.");
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/lineage/LineageGPUCacheEviction.java`
#### Snippet
```java
			ret = new MatrixBlock(rows, cols, nnz, sparseBlock);
		}*/
		//mat.acquireModify(tmp);
		//mat.release();
		return ret;
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/runtime/lineage/LineageParser.java`
#### Snippet
```java
		String opcode = tokens[0];

		/*if (opcode.startsWith(LineageItemUtils.LPLACEHOLDER)) {
			// Convert this to a leaf node (creation type)
			String data = opcode;
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
	
	protected static Comparator<LineageCacheEntry> LineageCacheComparator = (e1, e2) -> {
		/*return e1.score == e2.score ?
			Long.compare(e1._key.getId(), e2._key.getId()) :
			e1.score < e2.score ? -1 : 1;
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
	}

	/*public static void setSpill(boolean toSpill) {
		_allowSpill = toSpill;
		// NOTE: _allowSpill only enables/disables disk spilling, but has 
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettingsBuilder.java`
#### Snippet
```java
	// private double samplePower = 0.6;
	private double samplePower = 0.65;
	// private double samplePower = 0.68;
	// private double samplePower = 0.7;
	private boolean allowSharedDictionary = false;
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlockFactory.java`
#### Snippet
```java
		LOG.warn("Not Implemented Recompress yet");
		return new ImmutablePair<>(cmb, null);
		// _stats.originalSize = cmb.getInMemorySize();
		// CompressedMatrixBlock combined = CLALibCombineGroups.combine(cmb, k);
		// CompressedMatrixBlock squashed = CLALibSquash.squash(combined, k);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMWDiv "+wt.toString()+" k="+k+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}	
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMWDiv "+wt.toString()+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                 "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMWu "+wt.toString()+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                 "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			parallelMatrixMult(m1, m2, ret, k, ultraSparse, sparse, tm2, m1Perm);

		//System.out.println("MM "+k+" ("+m1.isInSparseFormat()+","+m1.getNumRows()+","+m1.getNumColumns()+","+m1.getNonZeros()+")x" +
		//		"("+m2.isInSparseFormat()+","+m2.getNumRows()+","+m2.getNumColumns()+","+m2.getNonZeros()+") in "+time.stop());
	
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();

		//System.out.println("MMWSig "+wt.toString()+" k="+k+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                   "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop() + ".");
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMChain "+ct.toString()+" k="+k+" ("+mX.isInSparseFormat()+","+mX.getNumRows()+","+mX.getNumColumns()+","+mX.getNonZeros()+")x" +
		//		              "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();

		//System.out.println("MMWu "+wt.toString()+" k="+k+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                   "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop() + ".");
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMWSig "+wt.toString()+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                 "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		}

		//System.out.println("PMM Seq ("+pm1.isInSparseFormat()+","+pm1.getNumRows()+","+pm1.getNumColumns()+","+pm1.getNonZeros()+")x" +
		//                  "("+m2.isInSparseFormat()+","+m2.getNumRows()+","+m2.getNumColumns()+","+m2.getNonZeros()+") in "+time.stop());
	}	
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		}
		
		//System.out.println("MMWCe "+wt.toString()+" k="+k+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                 "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			addMatrixMultWSLossNoWeightCorrection(mU, mV, ret, k);
		
		//System.out.println("MMWSLoss "+wt.toString()+" k="+k+" ("+mX.isInSparseFormat()+","+mX.getNumRows()+","+mX.getNumColumns()+","+mX.getNonZeros()+")x" +
		//                   "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		}
		
		// System.out.println("PMM Par ("+pm1.isInSparseFormat()+","+pm1.getNumRows()+","+pm1.getNumColumns()+","+pm1.getNonZeros()+")x" +
		//                   "("+m2.isInSparseFormat()+","+m2.getNumRows()+","+m2.getNumColumns()+","+m2.getNonZeros()+") in "+time.stop());
	}	
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		ret.examSparsity();
		
		//System.out.println("MMChain "+ct.toString()+" ("+mX.isInSparseFormat()+","+mX.getNumRows()+","+mX.getNumColumns()+","+mX.getNonZeros()+")x" +
		//		             "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			matrixMultWCeMMGeneric(mW, mU, mV, eps, ret, wt, 0, mW.rlen);
		
		//System.out.println("MMWCe "+wt.toString()+" ("+mW.isInSparseFormat()+","+mW.getNumRows()+","+mW.getNumColumns()+","+mW.getNonZeros()+")x" +
		//                 "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			addMatrixMultWSLossNoWeightCorrection(mU, mV, ret, 1);
		
		//System.out.println("MMWSLoss " +wt.toString()+ " ("+mX.isInSparseFormat()+","+mX.getNumRows()+","+mX.getNumColumns()+","+mX.getNonZeros()+")x" +
		//                  "("+mV.isInSparseFormat()+","+mV.getNumRows()+","+mV.getNumColumns()+","+mV.getNonZeros()+") in "+time.stop());
	}
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java
			// Decide if we should use the cached decompressed Version, or we should decompress the full matrix, or we
			// should decompress blocks.
			// final double denseSize = MatrixBlock.estimateSizeDenseInMemory(r, c);
			// final double localMaxMemory = InfrastructureAnalyzer.getLocalMaxMemory();

```

### CommentedOutCode
Commented out code (6 lines)
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java
				return inputMatrix.getCachedDecompressed().aggregateUnaryOperations(op, result, blen, indexesIn, inCP);

			// else if(colGroups.size() > 5 && denseSize <= localMaxMemory / 2) {
			// MatrixBlock uc = inputMatrix.getUncompressed(
			// op.indexFn.getClass().getSimpleName() + " " + op.aggOp.increOp.fn.getClass().getSimpleName()
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
	}

	// private static int indexFor(int h, int length) {
	// return h & (length - 1);
	// }
```

### CommentedOutCode
Commented out code (18 lines)
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstSample.java`
#### Snippet
```java

		// process frequency maps.
		// if(sampleFacts.frequencies != null) {
		// for(int freq : sampleFacts.frequencies) {
		// double dFreq = freq;
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
	public static IEncode createFromMatrixBlockDelta(MatrixBlock m, boolean transposed, IColIndex rowCols) {
		throw new NotImplementedException();
		// final int sampleSize = transposed ? m.getNumColumns() : m.getNumRows();
		// return createFromMatrixBlockDelta(m, transposed, rowCols, sampleSize);
	}
```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
public class CoCodeBinPacking extends AColumnCoCoder {

	// private static final boolean FIRST_FIT_DEC = true;
	// private static final int MAX_COL_FIRST_FIT = 16384;
	// private static final int MAX_COL_PER_GROUP = 1024;
```

### CommentedOutCode
Commented out code (26 lines)
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	 * @return The Partitioned column groups
	 */
	// private List<CompressedSizeInfoColGroup> partitionColumns(List<CompressedSizeInfoColGroup> currentGroups) {

	// 	List<CompressedSizeInfoColGroup> bins = new ArrayList<>();
```

### CommentedOutCode
Commented out code (26 lines)
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	 * @return The currently selected groups, packed together in bins.
	 */
	// private List<CompressedSizeInfoColGroup> packFirstFit(List<CompressedSizeInfoColGroup> currentGroups) {
	// 	List<CompressedSizeInfoColGroup> bins = new ArrayList<>();
	// 	double[] binWeights = new double[16];
```

### CommentedOutCode
Commented out code (70 lines)
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	 * @return The cocoded columns
	 */
	// private CompressedSizeInfo getCoCodingGroupsBruteForce(CompressedSizeInfo bins, int k) {

	// 	List<CompressedSizeInfoColGroup> finalGroups = new ArrayList<>();
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
		// colInfos.setInfo(partitionColumns(newGroups));
		// Cocode compare all in bins
		// getCoCodingGroupsBruteForce(colInfos, k);

		// colInfos.getInfo().addAll(constantGroups);
```

### CommentedOutCode
Commented out code (21 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
		throw new NotImplementedException();
		// NOTE: zeros handled once for all column groups outside
		// final int blksz = CompressionSettings.BITMAP_BLOCK_SZ;
		// final int numVals = getNumValues();
		// final double[] values = _dict.getValues();
```

### CommentedOutCode
Commented out code (53 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	protected void computeRowSums(double[] c, int rl, int ru, double[] preAgg) {
		throw new NotImplementedException();
		// final int blksz = CompressionSettings.BITMAP_BLOCK_SZ;
		// final int numVals = getNumValues();

```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	protected void computeColMxx(double[] c, Builtin builtin) {
		throw new NotImplementedException();
		// if(isZero())
		// for(int x = 0; x < _colIndexes.size(); x++)
		// c[_colIndexes[x]] = builtin.execute(c[_colIndexes[x]], 0);
```

### CommentedOutCode
Commented out code (32 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	}

	// @Override
	// public AColGroup binaryRowOp(BinaryOperator op, double[] v, boolean sparseSafe, boolean left) {
	// sparseSafe = sparseSafe || !_zeros;
```

### CommentedOutCode
Commented out code (29 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	}

	// @Override
	// public void countNonZerosPerRow(int[] rnnz, int rl, int ru) {
	// final int blksz = CompressionSettings.BITMAP_BLOCK_SZ;
```

### CommentedOutCode
Commented out code (14 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	}

	// private int skipScanVal(int k, int rl) {
	// final int blksz = CompressionSettings.BITMAP_BLOCK_SZ;

```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	}

	// @Override
	// public void leftMultByAColGroup(AColGroup lhs, MatrixBlock result, int nRows) {
	// throw new NotImplementedException();
```

### CommentedOutCode
Commented out code (31 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
		double[] values) {
		throw new NotImplementedException();
		// final int blksz = CompressionSettings.BITMAP_BLOCK_SZ;
		// final int numCols = getNumCols();
		// final int numVals = getNumValues();
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	public boolean containsValue(double pattern) {
		throw new NotImplementedException();
		// if(pattern == 0 && isZero())
		// return true;
		// return _dict.containsValue(pattern);
```

### CommentedOutCode
Commented out code (7 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
				+ " Ideally implement a modification such that OLE becomes SDC group when materializing Zero tuples");

		// ADictionary rvalues = _dict.applyScalarOp(op, val0, getNumCols());
		// char[] lbitmap = genOffsetBitmap(loff, loff.length);
		// char[] rbitmaps = Arrays.copyOf(_data, _data.length + lbitmap.length);
```

### CommentedOutCode
Commented out code (45 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDeltaDDC.java`
#### Snippet
```java
public class ColGroupDeltaDDC  { // extends ColGroupDDC

// 	private static final long serialVersionUID = -1045556313148564147L;

// 	/** Constructor for serialization */
```

### CommentedOutCode
Commented out code (6 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java
	public double getValue(int i) {
		throw new NotImplementedException();
		// final int nCol = nRowCol;
		// final int row = i / nCol;
		// if(row > nRowCol)
```

### CommentedOutCode
Commented out code (6 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/QDictionary.java`
#### Snippet
```java
	public ADictionary rexpandCols(int max, boolean ignore, boolean cast, int nCol) {
		throw new NotImplementedException();
		// byte[] newDictValues = new byte[_values.length * max];
		// for(int i = 0, offset = 0; i < _values.length; i++, offset += max) {
		// int val = _values[i] - 1;
```

### CommentedOutCode
Commented out code (6 lines)
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionary.java`
#### Snippet
```java
	public double[] getValues() {
		throw new DMLCompressionException("Invalid to materialize identity Matrix Please Implement alternative");
		// LOG.warn("Should not call getValues on Identity Dictionary");
		// double[] ret = new double[nRowCol * nRowCol];
		// for(int i = 0; i < nRowCol; i++) {
```

### CommentedOutCode
Commented out code (8 lines)
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java


	/*protected void applySparse(CacheBlock in, MatrixBlock out, int outputCol, int rowStart, int blk){
		int index = _colID - 1;
		for(int r = rowStart; r < getEndIndex(in.getNumRows(), rowStart, blk); r++) {
```

### CommentedOutCode
Commented out code (5 lines)
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java
	}

	/*protected void applyDense(CacheBlock in, MatrixBlock out, int outputCol, int rowStart, int blk){
		for(int i = rowStart; i < getEndIndex(in.getNumRows(), rowStart, blk); i++) {
			out.quickSetValue(i, outputCol, getCode(in, i));
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderUDF.java`
#### Snippet
```java
		//obtain result and in-place write back
		MatrixBlock ret = ((MatrixObject)ec.getCacheableData("O")).acquireReadAndRelease();
		//out.leftIndexingOperations(ret, 0, in.getNumRows()-1, _colID-1, _colID-1, ret, UpdateType.INPLACE);
		//out.leftIndexingOperations(ret, 0, in.getNumRows()-1, outputCol, outputCol+_domainSize-1, ret, UpdateType.INPLACE);
		//out.copy(0, in.getNumRows()-1, _colID-1, _colID-1, ret, true);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/transform/encode/MultiColumnEncoder.java`
#### Snippet
```java
		}

		//_columnEncoders.stream().parallel().forEach(columnEncoder ->
		//		columnEncoder.getMetaData(meta));
		if(_legacyOmit != null)
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DeCompressionCPInstruction.java`
#### Snippet
```java
		MatrixBlock out = (in instanceof CompressedMatrixBlock) ? 
			((CompressedMatrixBlock)in).decompress(OptimizerUtils.getConstrainedNumThreads(-1)): in;
		// MatrixBlock out = (in instanceof CompressedMatrixBlock) ? ((CompressedMatrixBlock) in)
		// 	.squeeze(OptimizerUtils.getConstrainedNumThreads(-1)) : in;
		ec.releaseMatrixInput(input1.getName());
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			if( lobj.getLength() != 1 || !(lobj.getData(0) instanceof ListObject) )
				ec.setVariable(output.getName(), lobj);
//				throw new RuntimeException("as.list() expects a list input with one nested list: "
//					+ "length(list)="+lobj.getLength()+", dt(list[0])="+lobj.getData(0).getDataType() );
			else ec.setVariable(output.getName(), lobj.getData(0));
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
		}
		else {
			//throw new DMLRuntimeException("rmfilevar w/ true is not expected! " + instString);
			//cleanDataOnHDFS(pb, input1.getName());
			cleanDataOnHDFS( m );
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
	private void deriveNewOutputFedMapping(MatrixObject in, MatrixObject out, FederatedRequest fr1){
		//Get agg type
		//if ( !(instOpcode.equals("uack+") || instOpcode.equals("uark+")) )
		//	throw new DMLRuntimeException("Operation " + instOpcode + " is unknown to FOUT processing");
		boolean isColAgg = ((AggregateUnaryOperator) _optr).isColAggregate();
```

### CommentedOutCode
Commented out code (7 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
		//   and set FType to PART
		if ( (inFtype.isRowPartitioned() && isColAgg) || (inFtype.isColPartitioned() && !isColAgg) ){
			/*for ( FederatedRange range : inputFedMapCopy.getFederatedRanges() ){
				range.setBeginDim(0,0);
				range.setBeginDim(1,0);
```

### CommentedOutCode
Commented out code (93 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryEviction.java`
#### Snippet
```java
		// CPU instruction is ended, and 3) No non-live entries left in the cache.
		long t0 =  DMLScript.STATISTICS ? System.nanoTime() : 0;
		/*while (!LineageGPUCacheEviction.isGPUCacheEmpty())
		{
			if (LineageCacheConfig.STOPBACKGROUNDEVICTION)
```

### CommentedOutCode
Commented out code (8 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUContextPool.java`
#### Snippet
```java
		GPUContext.LOG.info("Initial GPU memory: " + initialGPUMemBudget());

		//int[] device = {-1};
		//cudaGetDevice(device);
		//cudaDeviceProp prop = getGPUProperties(device[0]);
```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
		cudaMemcpy(r.rowPtr, Pointer.to(rowPtr), getIntSizeOf(rows + 1), cudaMemcpyHostToDevice);
		cudaMemcpy(r.colInd, Pointer.to(colInd), getIntSizeOf(nnz), cudaMemcpyHostToDevice);
		//if (DMLScript.STATISTICS)
		//	GPUStatistics.cudaToDevTime.add(System.nanoTime() - t0);
		//if (DMLScript.STATISTICS)
```

### CommentedOutCode
Commented out code (8 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
				// TODO MULTI_BLOCK
				throw new DMLRuntimeException("Multi block spark aggregations are not supported for tensors yet.");
				/*
				//in case of multi-block aggregation, we always keep the correction
				out = out.mapToPair(new RDDUTensorAggFunction(auop, dc.getBlocksize(), dc.getBlocksize()));
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java
			//execute actual reshape operation
			//LibTensorReorg.reshape()
//			List<IndexedTensorBlock> out = LibTensorReorg
//					.reshape(in, _mcIn, _mcOut, _byrow, _outputEmptyBlocks);
//			// TODO create iterator
```

### CommentedOutCode
Commented out code (9 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
	}
	
//	private Pointer transpose(ExecutionContext ec, MatrixObject X) throws DMLRuntimeException {
//		GPUContext gCtx = ec.getGPUContext(0);
//		String instructionName = getExtendedOpcode();
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
			in1 = RDDAggregateUtils.mergeByKey(in1.flatMapToPair(new ExtractBlockForBinaryReblock(mcRdd, mcOut)));
			// TODO: Inject checkpoint to avoid doing this repeated for validation set
//			sec.setRDDHandleForVariable(name, in1);
//			sec.setMetaData(name, new MatrixDimensionsMetaData(mcOut));
		}
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
			case LIBSVM: {
				// TODO: implement for libsvm
				//				FileFormatPropertiesCSV props = (formatProperties!=null) ?(FileFormatPropertiesCSV) formatProperties : null;
				//				JavaRDD<String> out = FrameRDDConverterUtils.binaryBlockToCsv(in1, mc, props, true);
				//				customSaveTextFile(out, fname, false);
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
				HDFSTool.mergeIntoSingleFile(randFName, fname); // Faster version :)

				// rdd.coalesce(1, true).saveAsTextFile(randFName);
				// MapReduceTool.copyFileOnHDFS(randFName + "/part-00000", fname);
			} catch (IOException e) {
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/IDHandler.java`
#### Snippet
```java

		//*-endian version 
		//long value = ((long)part1)*(long)Math.pow(2, 32);
		//value += part2;

```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
			for( int i=0; i<stats.get(Stat.PARFOR_NUMTHREADS).size(); i++ )
			{
				//sb.append(" Optimization = "+stats.get(Stat.STAT_PARSE_T).get(i)+"ms\n");
				//sb.append(" Optimization = "+stats.get(Stat.STAT_OPT_T).get(i)+"ms\n");
				
```

### CommentedOutCode
Commented out code (2 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
				node.setExecType(ExecType.CP);
				//exec operations
				//CPInstruction cpinst = (CPInstruction) inst;
				//node.addParam(ParamType.OPTYPE,cpinst.getCPInstructionType().toString());
				break;
```

### CommentedOutCode
Commented out code (4 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/services/WorkerService.java`
#### Snippet
```java
			if (stats.heavyHitters != null) {
				CompletableFuture.runAsync(() -> {
					// entityRepository.removeAllEntitiesByField(Constants.ENTITY_WORKER_ID_COL, id, HeavyHitterModel.class);

					// for (var heavyHitterEntity : stats.heavyHitters) {
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
			sb.append(displayFedSerializationReuseStats());

			//sb.append(displayFedTransfer());
			//sb.append(displayCPUUsage());
			//sb.append(displayMemoryUsage());
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				// TODO implement hadoop read write for tensor
				throw new DMLRuntimeException("Tensor can not yet be written or read to hadoopFile");
				/*rdd = sc.hadoopFile(to.getFileName(), inputInfo.inputFormatClass, inputInfo.inputKeyClass, inputInfo.inputValueClass);
				rdd = SparkUtils.copyBinaryBlockTensor((JavaPairRDD<TensorIndexes, HomogTensor>) rdd); //cp is workaround for read bug
				fromFile = true;*/
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java

			//keep rdd handle for future operations on it
			//RDDObject rddhandle = new RDDObject(rdd);
			//rddhandle.setHDFSFile(true);
			//to.setRDDHandle(rddhandle);
```

### CommentedOutCode
Commented out code (3 lines)
in `src/main/java/org/apache/sysds/runtime/functionobjects/OffsetColumnIndex.java`
#### Snippet
```java
	public static OffsetColumnIndex getOffsetColumnIndexFnObject(int offset) {
		return new OffsetColumnIndex(offset);
		//if ( singleObj == null )
		//	singleObj = new OffsetColumnIndex(offset);
		//return singleObj;
```

## RuleId[id=CStyleArrayDeclaration]
### CStyleArrayDeclaration
C-style array declaration of local variable `lineageTypes`
in `src/main/java/org/apache/sysds/api/DMLOptions.java`
#### Snippet
```java
		if (line.hasOption("lineage")){
			dmlOptions.lineage = true;
			String lineageTypes[] = line.getOptionValues("lineage");
			if (lineageTypes != null) {
				for (String lineageType : lineageTypes) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `supd`
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
	public List<FunctionOp> getParamservPseudoFunctionCalls() {
		try {
			String supd[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("upd")).getStringValue());
			String sagg[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("agg")).getStringValue());
			String sval[] = getParameterHop("val") == null ? null :
```

### CStyleArrayDeclaration
C-style array declaration of local variable `sagg`
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
		try {
			String supd[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("upd")).getStringValue());
			String sagg[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("agg")).getStringValue());
			String sval[] = getParameterHop("val") == null ? null :
				DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("val")).getStringValue());
```

### CStyleArrayDeclaration
C-style array declaration of local variable `sval`
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			String supd[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("upd")).getStringValue());
			String sagg[] = DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("agg")).getStringValue());
			String sval[] = getParameterHop("val") == null ? null :
				DMLProgram.splitFunctionKey(((LiteralOp)getParameterHop("val")).getStringValue());
			Hop model = getParameterHop("model");
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/util/AutoDiff.java`
#### Snippet
```java
				}
				else if(inst instanceof VariableCPInstruction && ((VariableCPInstruction) inst).isCreateVariable()) {
					String parts[] = InstructionUtils.getInstructionPartsWithValueType(inst.toString());
					Types.DataType dt = Types.DataType.valueOf(parts[4]);
					Types.ValueType vt = dt == Types.DataType.MATRIX ? Types.ValueType.FP64 : Types.ValueType.STRING;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `sbs`
in `src/main/java/org/apache/sysds/runtime/util/DependencyThreadPool.java`
#### Snippet
```java
			}
		}
		StringBuilder sbs[] = new StringBuilder[depth];
		String offsets[] = new String[depth];
		for (Map.Entry<DependencyTask<?>, Integer> entry : levelMap.entrySet()) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `offsets`
in `src/main/java/org/apache/sysds/runtime/util/DependencyThreadPool.java`
#### Snippet
```java
		}
		StringBuilder sbs[] = new StringBuilder[depth];
		String offsets[] = new String[depth];
		for (Map.Entry<DependencyTask<?>, Integer> entry : levelMap.entrySet()) {
			int level = entry.getValue();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `contents`
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
		OutputStream out = dstFS.create(dstFile);
		try {
			FileStatus contents[] = srcFS.listStatus(srcDir);
			Arrays.sort(contents);
			for (int i = 0; i < contents.length; i++) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `output`
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static String[] getTimestamp(String[] values)
	{
		String output[] = new String[values.length];
		for (int i = 0; i< values.length; i++){
			//skip null/blank entries
```

### CStyleArrayDeclaration
C-style array declaration of local variable `sizeInBytesArray`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
				pad_h, pad_w, stride_h, stride_w, P, Q);
		int[] algos = {-1};
		long sizeInBytesArray[] = {Math.min(workspaceLimit, MAX_WORKSPACE_LIMIT_BYTES)};
		jcuda.jcudnn.JCudnn.cudnnGetConvolutionForwardAlgorithm(LibMatrixCuDNN.getCudnnHandle(gCtx), 
				ret.nchwTensorDesc, ret.filterDesc, ret.convDesc, ret.nkpqTensorDesc,
```

### CStyleArrayDeclaration
C-style array declaration of local variable `padding`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	private LibMatrixCuDNNConvolutionAlgorithm(GPUContext gCtx, String instName, int N, int C, int H, int W, int K, int R, int S, 
			int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q) {
		int padding[] = {pad_h, pad_w};
		int strides[] = {stride_h, stride_w};
		convDesc = allocateConvolutionDescriptor(padding, strides);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `strides`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
			int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q) {
		int padding[] = {pad_h, pad_w};
		int strides[] = {stride_h, stride_w};
		convDesc = allocateConvolutionDescriptor(padding, strides);
		this.gCtx = gCtx;
```

### CStyleArrayDeclaration
C-style array declaration of parameter `padding`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	}
	
	private static cudnnConvolutionDescriptor allocateConvolutionDescriptor(int padding [], int strides []) {
		cudnnConvolutionDescriptor convDesc = new cudnnConvolutionDescriptor();
		cudnnCreateConvolutionDescriptor(convDesc);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `strides`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	}
	
	private static cudnnConvolutionDescriptor allocateConvolutionDescriptor(int padding [], int strides []) {
		cudnnConvolutionDescriptor convDesc = new cudnnConvolutionDescriptor();
		cudnnCreateConvolutionDescriptor(convDesc);
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uariminGt(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ixMin = 1;
		if(value <= bv[0] || value > bv[bv.length-1]) 
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uariminNe(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ixMin = 1;
		if( bv[0] != value ) 
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uarimaxNe(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ixMax = bv.length;
		if( bv[bv.length-1] == value ) 
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uariminEq(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ixMin = 1;
		if(value == bv[0])
```

### CStyleArrayDeclaration
C-style array declaration of parameter `vmb`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @return array of minimum row indices
	 */
	public static int[] prepareRowIndicesMin(int iCols, double vmb[], BinaryOperator bOp)
	{
		int[] vixCumSum = null;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `dvix`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
				bPrimeCumSum = true;
			
			double dvix[] = new double[vix.length];
			if (bPrimeCumSum)
				for (int i = 0; i< vix.length; i++)
```

### CStyleArrayDeclaration
C-style array declaration of parameter `vmb`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	}

	public static int[] prepareRowIndices(int iCols, double vmb[], BinaryOperator bOp, AggregateUnaryOperator uaggOp) {
		return (isRowIndexMax(uaggOp)?prepareRowIndicesMax(iCols, vmb, bOp):prepareRowIndicesMin(iCols, vmb, bOp));
	}
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uarimaxEq(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ix = Arrays.binarySearch(bv, value);
		int ixMax = bv.length;
```

### CStyleArrayDeclaration
C-style array declaration of parameter `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @param bOp binary operator
	 */
	private static int uarimaxGt(double value, double[] bv, int bvi[], BinaryOperator bOp) {
		int ixMax = bv.length;
		
```

### CStyleArrayDeclaration
C-style array declaration of parameter `vmb`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * @return array of maximum row indices
	 */
	public static int[] prepareRowIndicesMax(int iCols, double vmb[], BinaryOperator bOp)
	{
		int[] vixCumSum = null;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `dvix`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
				bPrimeCumSum = true;
			
			double dvix[] = new double[vix.length];
			if (bPrimeCumSum)
				for (int i = 0; i< vix.length; i++)
```

### CStyleArrayDeclaration
C-style array declaration of local variable `b`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
		int rlen = m1.rlen;
		int clen = ret.clen;
		double b[] = DataConverter.convertToDoubleVector(m2);
		if(!ret.isAllocated())
			ret.allocateDenseBlock();
```

### CStyleArrayDeclaration
C-style array declaration of local variable `bv`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java

	public MatrixBlock  uaggouterchainOperations(MatrixBlock mbLeft, MatrixBlock mbRight, MatrixBlock mbOut, BinaryOperator bOp, AggregateUnaryOperator uaggOp) {
		double bv[] = DataConverter.convertToDoubleVector(mbRight);
		int bvi[] = null;
		
```

### CStyleArrayDeclaration
C-style array declaration of local variable `bvi`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
	public MatrixBlock  uaggouterchainOperations(MatrixBlock mbLeft, MatrixBlock mbRight, MatrixBlock mbOut, BinaryOperator bOp, AggregateUnaryOperator uaggOp) {
		double bv[] = DataConverter.convertToDoubleVector(mbRight);
		int bvi[] = null;
		
		//process instruction
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
				} else if (inst instanceof VariableCPInstruction
						&& ((VariableCPInstruction) inst).isCreateVariable()) {
					String parts[] = InstructionUtils.getInstructionPartsWithValueType(inst.toString());
					DataType dt = DataType.valueOf(parts[4]);
					ValueType vt = dt == DataType.MATRIX ? ValueType.FP64 : ValueType.STRING;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
				} else if (inst instanceof VariableCPInstruction
						&& ((VariableCPInstruction) inst).isCreateVariable()) {
					String parts[] = InstructionUtils.getInstructionPartsWithValueType(inst.toString());
					DataType dt = DataType.valueOf(parts[4]);
					ValueType vt = dt == DataType.MATRIX ? ValueType.FP64 : ValueType.STRING;
```

### CStyleArrayDeclaration
C-style array declaration of parameter `f`
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
	}

	private static double getDuj2aEstimate(double q, int f[], int n, int dn, double gammaDuj1, int N,
		HashMap<Integer, Double> solveCache) {
		// Computes the "un-smoothed second-order jackknife estimator" with additional stabilization procedure, which
```

### CStyleArrayDeclaration
C-style array declaration of local variable `codes`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderFeatureHash.java`
#### Snippet
```java
		// hash a block of rows
		int endInd = getEndIndex(in.getNumRows(), startInd, blkSize);
		double codes[] = new double[endInd-startInd];
		if( in instanceof FrameBlock) {
			Array<?> a = ((FrameBlock) in).getColumn(_colID-1);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rptr`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java
					// Manually fill the column-indexes and values array
					SparseBlockCSR csrblock = (SparseBlockCSR)out.getSparseBlock();
					int rptr[] = csrblock.rowPointers();
					csrblock.indexes()[rptr[ii]+index] = outputCol;
					csrblock.values()[rptr[ii]+index] = codes[ii-rowStart];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `vals`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
		}
		int rowEnd = getEndIndex(in.getNumRows(), rowStart, blk);
		double vals[] = new double[rowEnd -rowStart];
		for (int i=rowStart; i<rowEnd; i++)
			vals[i-rowStart] = in.getDouble(i, outputCol);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rptr`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
			else { //csr
				SparseBlockCSR csrblock = (SparseBlockCSR)out.getSparseBlock();
				int rptr[] = csrblock.rowPointers();
				double val = csrblock.values()[rptr[r]+index];
				if(Double.isNaN(val)){
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rptr`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderPassThrough.java`
#### Snippet
```java
					// Manually fill the column-indexes and values array
					SparseBlockCSR csrblock = (SparseBlockCSR)out.getSparseBlock();
					int rptr[] = csrblock.rowPointers();
					csrblock.indexes()[rptr[ii]+index] = outputCol;
					csrblock.values()[rptr[ii]+index] = codes[ii-rowStart];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `codes`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderRecode.java`
#### Snippet
```java
		// lookup for a block of rows
		int endInd = getEndIndex(in.getNumRows(), startInd, blkSize);
		double codes[] = new double[endInd-startInd];
		for (int i=startInd; i<endInd; i++) {
			String key = in.getString(i, _colID-1);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java

	public static UaggOuterChainCPInstruction parseInstruction(String str) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `sparts`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			int lrows = (int) ec.getScalarInput(rows).getLongValue();
			int lcols = (int) ec.getScalarInput(cols).getLongValue();
			String sparts[] = schema.split(DataExpression.DELIM_NA_STRING_SEP);
			ValueType[] vt = sparts[0].equals(DataExpression.DEFAULT_SCHEMAPARAM) ?
				UtilFunctions.nCopies(lcols, ValueType.STRING) : (sparts.length == 1 && lcols > 1) ?
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMFEDInstruction.java`
#### Snippet
```java

	public static MMFEDInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReblockFEDInstruction.java`
#### Snippet
```java

	public static ReblockFEDInstruction parseInstruction(String str) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `free`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CudaMemoryAllocator.java`
#### Snippet
```java
	@Override
	public long getAvailableMemory() {
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `total`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CudaMemoryAllocator.java`
#### Snippet
```java
	public long getAvailableMemory() {
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
		return (long) (free[0] * DMLScript.GPU_MEMORY_UTILIZATION_FACTOR);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `free`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/UnifiedMemoryAllocator.java`
#### Snippet
```java
	public long getAvailableMemory() {
		if(maxAvailableMemory < 0 || gpuUtilizationFactor != DMLScript.GPU_MEMORY_UTILIZATION_FACTOR) {
			long free[] = { 0 };
			long total[] = { 0 };
			cudaMemGetInfo(free, total);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `total`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/UnifiedMemoryAllocator.java`
#### Snippet
```java
		if(maxAvailableMemory < 0 || gpuUtilizationFactor != DMLScript.GPU_MEMORY_UTILIZATION_FACTOR) {
			long free[] = { 0 };
			long total[] = { 0 };
			cudaMemGetInfo(free, total);
			maxAvailableMemory = (long) (total[0] * DMLScript.GPU_MEMORY_UTILIZATION_FACTOR);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `maxBlockDimX`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/ExecutionConfig.java`
#### Snippet
```java
			CUdevice device = new CUdevice();
			JCudaKernels.checkResult(jcuda.driver.JCudaDriver.cuDeviceGet(device, deviceNumber));
			int maxBlockDimX[] = { 0 };
			jcuda.driver.JCudaDriver
					.cuDeviceGetAttribute(maxBlockDimX, CUdevice_attribute.CU_DEVICE_ATTRIBUTE_MAX_BLOCK_DIM_X, device);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `deviceCountArray`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUContextPool.java`
#### Snippet
```java
		cuInit(0); // Initialize the driver

		int deviceCountArray[] = { 0 };
		cuDeviceGetCount(deviceCountArray);        // Obtain the number of devices
		deviceCount = deviceCountArray[0];
```

### CStyleArrayDeclaration
C-style array declaration of local variable `buffer`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java
			if (in != null) {
				out = new ByteArrayOutputStream();
				byte buffer[] = new byte[8192];
				while (true) {
					int read = in.read(buffer);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `baseArray`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
			C.nnz = CnnzArray[0];
		} else {
			int baseArray[] = { 0 };
			cudaMemcpy(Pointer.to(CnnzArray), C.rowPtr.withByteOffset(getIntSizeOf(m)), getIntSizeOf(1),
					cudaMemcpyDeviceToHost);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `baseArray`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
			C.nnz = CnnzArray[0];
		} else {
			int baseArray[] = { 0 };
			cudaMemcpy(Pointer.to(CnnzArray), C.rowPtr.withByteOffset(getIntSizeOf(m)), getIntSizeOf(1),
					cudaMemcpyDeviceToHost);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `rowPtr`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		
		if (tmp.isInSparseFormat()) {
			int rowPtr[] = null;
			int colInd[] = null;
			double[] values = null;
```

### CStyleArrayDeclaration
C-style array declaration of local variable `colInd`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		if (tmp.isInSparseFormat()) {
			int rowPtr[] = null;
			int colInd[] = null;
			double[] values = null;
			
```

### CStyleArrayDeclaration
C-style array declaration of local variable `free`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		ret.append(String.format("%-35s%-15s%-15s%-15s\n", "All pointers", 
				"-", allPointers.size(), byteCountToDisplaySize(totalMemoryAllocated)));
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `total`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
				"-", allPointers.size(), byteCountToDisplaySize(totalMemoryAllocated)));
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
		ret.append(String.format("%-35s%-15s%-15s%-15s\n", "Free mem (from cudaMemGetInfo)", 
```

### CStyleArrayDeclaration
C-style array declaration of local variable `free`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
					+ ". Supported values are cuda, unified_memory.");
		}
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `total`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		}
		long free[] = { 0 };
		long total[] = { 0 };
		cudaMemGetInfo(free, total);
		if(free[0] < WARN_UTILIZATION_FACTOR*total[0]) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
			throw new DMLRuntimeException("Incorrect opcode for LIBSVMReblockSPInstruction:" + opcode);

		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);

		CPOperand in = new CPOperand(parts[1]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java

	public static ReblockSPInstruction parseInstruction(String str) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java

	public static RmmSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java

	public static PmmSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = InstructionUtils.getOpCode(str);
		if ( opcode.equalsIgnoreCase(PMMJ.OPCODE)) {
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TsmmSPInstruction.java`
#### Snippet
```java

	public static TsmmSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];
		//check supported opcode 
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java

	public static Tsmm2SPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];
		//check supported opcode 
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java

	public static MapmmSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java

	public static PMapmmSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java

	public static UaggOuterChainSPInstruction parseInstruction( String str ) {
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];

```

### CStyleArrayDeclaration
C-style array declaration of local variable `schemaValues`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		
		//prepare input arguments
		String schemaValues[] = schema.split(DataExpression.DELIM_NA_STRING_SEP);
		ValueType[] vt = (schemaValues[0].equals(DataExpression.DEFAULT_SCHEMAPARAM)) ?
			UtilFunctions.nCopies((int)lcols, ValueType.STRING) :
```

### CStyleArrayDeclaration
C-style array declaration of local variable `parts`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
		// [csvrblk, pREADmissing_val_maps·MATRIX·DOUBLE, _mVar37·MATRIX·DOUBLE,
		// 1000, 1000, false, ,, true, 0.0]
		String parts[] = InstructionUtils.getInstructionPartsWithValueType(str);

		CPOperand in = new CPOperand(parts[1]);
```

### CStyleArrayDeclaration
C-style array declaration of local variable `bytes`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java
			Checksum checksum = new Adler32();
			if (ob instanceof ScalarObject) {
				byte bytes[] = ((ScalarObject)ob).getStringValue().getBytes();
				checksum.update(bytes, 0, bytes.length);
				_checksums.add(checksum.getValue());
```

### CStyleArrayDeclaration
C-style array declaration of local variable `bytes`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java
					DataOutput dout = new CacheDataOutput(new byte[(int)cbsize]);
					cb.write(dout);
					byte bytes[] = ((CacheDataOutput) dout).getBytes();
					checksum.update(bytes, 0, bytes.length);
					_checksums.add(checksum.getValue());
```

## RuleId[id=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/services/StatisticsService.java`
#### Snippet
```java
		Future<FederatedResponse> result = null;

		final Pattern pattern = Pattern.compile("(.*://)?([A-Za-z0-9\\-\\.]+)(:[0-9]+)?(.*)");
		final Matcher matcher = pattern.matcher(address);

```

## RuleId[id=FinalMethodInFinalClass]
### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteElementwiseMultChainOptimization.java`
#### Snippet
```java

		@Override
		public final int compare(final Hop o1, final Hop o2) {
			final int c = Integer.compare(orderDataType[o1.getDataType().ordinal()], orderDataType[o2.getDataType().ordinal()]);
			if (c != 0) return c;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/SingletonLookupHashMap.java`
#### Snippet
```java
	}

	public static final SingletonLookupHashMap getMap() {
		return singleton;
	}
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/Bitmap.java`
#### Snippet
```java

	@Override
	public final int getNumNonZerosInOffset(int idx) {
		// all values are non zero therefore since this bitmap type contains one column
		// always return 1.
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/Bitmap.java`
#### Snippet
```java
	 * @return Dictionary of distinct values
	 */
	public final double[] getValues() {
		return _values;
	}
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Row_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Frame)
        org.apache.sysds.protobuf.SysdsProtos.FrameOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Frame_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    private byte memoizedIsInitialized = -1;
    @java.lang.Override
    public final boolean isInitialized() {
      byte isInitialized = memoizedIsInitialized;
      if (isInitialized == 1) return true;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Frame_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Schema)
        org.apache.sysds.protobuf.SysdsProtos.SchemaOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Schema_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
      @java.lang.Override
      public final Builder setUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.setUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Schema_descriptor;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final Builder mergeUnknownFields(
          final com.google.protobuf.UnknownFieldSet unknownFields) {
        return super.mergeUnknownFields(unknownFields);
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

    @java.lang.Override
    public final com.google.protobuf.UnknownFieldSet
    getUnknownFields() {
      return this.unknownFields;
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      @java.lang.Override
      public final boolean isInitialized() {
        return true;
      }
```

### FinalMethodInFinalClass
Method declared `final` in 'final' class
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Row)
        org.apache.sysds.protobuf.SysdsProtos.RowOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Row_descriptor;
```

## RuleId[id=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSV.java`
#### Snippet
```java
	}

	private final static boolean containsMetaTag(Text val) {
		if(val.charAt(0) == '#')
			return val.find(TfUtils.TXMTD_MVPREFIX) > -1//
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
	}

	private static final ValueType getHighest(ValueType state, ValueType c) {

		switch(state) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processRightDense(final int rl, final int ru) {
			final DenseBlock rv = _ret.getDenseBlock();
			final int cols = _ret.getNumColumns();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processRight(final int rl, final int ru) {
			// all exec should have ret on left side
			if(_m2.isInSparseFormat())
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processRightSparse(final int rl, final int ru) {
			final DenseBlock rv = _ret.getDenseBlock();
			final int cols = _ret.getNumColumns();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processLeft(final int rl, final int ru) {
			// all exec should have ret on right side
			if(_m2.isInSparseFormat())
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processLeftSparse(final int rl, final int ru) {
			final DenseBlock rv = _ret.getDenseBlock();
			final int cols = _ret.getNumColumns();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processLeftDense(final int rl, final int ru) {
			final DenseBlock rv = _ret.getDenseBlock();
			final int cols = _ret.getNumColumns();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processBlock(final int rl, final int ru, final List<AColGroup> groups, final AIterator[] its) {
			// unsafe decompress, since we count nonzeros afterwards.
			final DenseBlock db = _ret.getDenseBlock();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processBlock(final int rl, final int ru, final List<AColGroup> groups, final AIterator[] its) {
			// unsafe decompress, since we count nonzeros afterwards.
			final DenseBlock db = _ret.getDenseBlock();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
		}

		private final void processDense(final int rl, final int ru) {
			int offset = rl * _m1.getNumColumns();
			final double[] _retDense = _ret.getDenseBlockValues();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
	}

	private final int hashIndex(final double key) {
		// Option 1 ... conflict on 1 vs -1
		final long bits = Double.doubleToLongBits(key);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/DenseEncoding.java`
#### Snippet
```java
	}

	private final DenseEncoding combineSparseHashMap(final AMapToData ret) {
		final int size = ret.size();
		final Map<Integer, Integer> m = new HashMap<>(size);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/DenseEncoding.java`
#### Snippet
```java
	}

	private final DenseEncoding combineSparseMapToData(final AMapToData ret, final int maxUnique, final int nVl) {
		final int size = ret.size();
		final AMapToData m = MapToFactory.create(maxUnique, maxUnique + nVl);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AMorphingMMColGroup.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockCommonVector(DenseBlock db, int rl, int ru, int offR, int offC,
		double[] common) {
		for(int i = rl, offT = rl + offR; i < ru; i++, offT++) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AMorphingMMColGroup.java`
#### Snippet
```java
	}

	private final void decompressToSparseBlockCommonVector(SparseBlock sb, int rl, int ru, int offR, int offC,
		double[] common) {
		final int nCol = _colIndexes.size();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggSingleRowSparse(final SparseBlock sb, final double[] resV,
		final int offRet, final int r, final int cu, final AIterator it) {
		if(sb.isEmpty(r))
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggSingleRow(MatrixBlock mb, MatrixBlock result, int r, int cl, int cu,
		AIterator it) {
		final double[] resV = result.getDenseBlockValues();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggRows(MatrixBlock mb, MatrixBlock result, int rl, int ru, int cl, int cu,
		AIterator it) {
		final double[] resV = result.getDenseBlockValues();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggRowsDense(MatrixBlock mb, double[] resV, int nCols, int rl, int ru,
		int cl, int cu, AIterator it) {
		final DenseBlock db = mb.getDenseBlock();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggRowsSparse(SparseBlock sb, double[] resV, int nCols, int rl, int ru,
		int cl, int cu, AIterator it) {
		for(int r = rl; r < ru; r++) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
	}

	private final void leftMultByMatrixNoPreAggSingleRowDense(double[] mV, int off, double[] resV, int offRet, int r,
		int cl, int cu, AIterator it) {
		final int last = _indexes.getOffsetToLast();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupCompressed.java`
#### Snippet
```java
	}

	private final void prod(IndexFunction idx, double[] c, int nRows, int rl, int ru, double[] preAgg) {
		if(idx instanceof ReduceAll)
			computeProduct(c, nRows);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupCompressed.java`
#### Snippet
```java
	}

	private final void sumSq(IndexFunction idx, double[] c, int nRows, int rl, int ru, double[] preAgg) {
		if(idx instanceof ReduceAll)
			computeSumSq(c, nRows);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupCompressed.java`
#### Snippet
```java
	}

	private final void builtin(AggregateUnaryOperator op, double[] c, int nRows, int rl, int ru, double[] preAgg) {
		Builtin bop = (Builtin) op.aggOp.increOp.fn;
		BuiltinCode bopC = bop.getBuiltinCode();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupCompressed.java`
#### Snippet
```java
	}

	private final void sum(IndexFunction idx, double[] c, int nRows, int rl, int ru, double[] preAgg) {
		if(idx instanceof ReduceAll)
			computeSum(c, nRows);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockSparseDictionaryPost(DenseBlock db, int rl, int ru, int offR, int offC,
		SparseBlock sb, AIterator it, int last) {
		while(true) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockSparseDictionaryPre(DenseBlock db, int rl, int ru, int offR, int offC,
		SparseBlock sb, AIterator it) {
		while(it.isNotOver(ru)) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictioanryPostAllCols(DenseBlock db, int rl, int ru, int offR,
		double[] values, AIterator it) {
		final int lastOff = _indexes.getOffsetToLast();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictionaryPostGeneric(DenseBlock db, int rl, int ru, int offR,
		int offC, double[] values, AIterator it) {
		final int lastOff = _indexes.getOffsetToLast();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictionaryPostSingleColContiguous(DenseBlock db, int rl, int ru,
		int offR, int offC, double[] values, AIterator it) {
		final int lastOff = _indexes.getOffsetToLast() + offR;
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockAllColumnsContiguous(final DenseBlock db, final int rl, final int ru) {
		final double[] c = db.values(0);
		final int nCol = _colIndexes.size();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
	}

	private final void addToCommonSparse(double[] constV, SparseBlock sb) {

		final int alen = sb.size(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
	}

	private final void addToCommonDense(double[] constV, double[] values) {
		for(int i = 0; i < _colIndexes.size(); i++)
			constV[_colIndexes.get(i)] += values[i];
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictGeneric(DenseBlock db, int rl, int ru, int offR, int offC,
		double[] values) {
		final int nCol = _colIndexes.size();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictNoOff(DenseBlock db, int rl, int ru, double[] values) {
		final int nCol = _colIndexes.size();
		final int nColU = db.getDim(1);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictAllColumnsContiguous(DenseBlock db, int rl, int ru, int offR,
		double[] values) {
		final double[] c = db.values(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictNoColOffset(DenseBlock db, int rl, int ru, int offR,
		double[] values) {
		final int nCol = _colIndexes.size();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictSingleColContiguous(DenseBlock db, int rl, int ru, int offR,
		int offC, double[] values) {
		final double[] c = db.values(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockDenseDictSingleColOutContiguous(DenseBlock db, int rl, int ru, int offR,
		int offC, double[] values) {
		final double[] c = db.values(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
	}

	private final void preAggregateSparseMapRowBellowEnd(SparseBlock sb, double[] preAV, int r, int nVal,
		AMapToData data, AIterator it) {
		int apos = sb.pos(r);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
	}

	private final void preAggregateSparseMapRowEnd(SparseBlock sb, double[] preAV, int r, int nVal, AMapToData data,
		AIterator it) {
		int apos = sb.pos(r);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockSparseDictionaryPre(DenseBlock db, int rl, int ru, int offR, int offC,
		SparseBlock sb, AIterator it) {
		final int apos = sb.pos(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
	}

	private final void decompressToDenseBlockSparseDictionaryPost(DenseBlock db, int rl, int ru, int offR, int offC,
		SparseBlock sb, AIterator it, int last) {
		final int apos = sb.pos(0);
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	}

	private final void computeRowProductDenseRLE(double[] c, int rl, int ru, double[] preAgg) {
		final int numVals = getNumValues();

```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	}

	private final void computeRowProductSparseRLE(double[] c, int rl, int ru, double[] preAgg) {
		final int numVals = getNumValues();
		// waste full but works
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	}

	private final void computeRowMxxDenseRLE(double[] c, Builtin builtin, int rl, int ru, double[] preAgg) {
		final int numVals = getNumValues();
		for(int k = 0; k < numVals; k++) {
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	}

	private final void computeRowMxxSparseRLE(double[] c, Builtin builtin, int rl, int ru, double[] preAgg) {
		final int numVals = getNumValues();
		// waste full but works
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	}

	private final void preAggregateSparseSingleRow(final SparseBlock sb, final double[] preAV, final int r) {
		if(sb.isEmpty(r))
			return;
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	}

	private final void preAggregateSDCZ_SDCZSingleCol(AMapToData tm, double[] td, AOffset tof, AOffset of, double[] dv) {
		final AOffsetIterator itThat = tof.getOffsetIterator();
		final AOffsetIterator itThis = of.getOffsetIterator();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	}

	private final void preAggregateSparseMultiRow(final SparseBlock sb, final double[] preAV, final int rl,
		final int ru) {
		final int unique = getUnique();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
	}

	private final void addToOffsets(double[] v, int sf, int st, int nCol, int rep) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
			v[j] += _values[i] * rep;
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
	}

	private final void addToOffsets(final double[] v, final int sf, final int st, final int nCol) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
			v[j] += _values[i];
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparse(final SparseBlock sb, final double[] v, final int fr, final int st,
		final int nCol, final int rep) {
		if(sb.isEmpty(fr))
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparse(final SparseBlock sb, final double[] v, final int fr, final int st,
		final int nCol) {
		if(sb.isEmpty(fr))
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntryDense(final double[] thisV, final double[] v, final int sf, final int st,
		final int nCol, final int rep) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparseCSR(final SparseBlockCSR sb, final double[] v, final int fr, final int st,
		final int nCol, final int[] aix, final double[] avals) {

```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private final void productAllRowsToDouble(double[] ret, int nCol) {
		if(_data.isInSparseFormat()) {
			SparseBlock sb = _data.getSparseBlock();
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntryDense(final double[] thisV, final double[] v, final int sf, final int st,
		final int nCol) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
```

### FinalPrivateMethod
'private' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private final void productAllRowsToDoubleWithReference(double[] ret, int nCol, double[] reference) {
		final int nRow = _data.getNumRows();
		if(_data.isInSparseFormat()) {
```

## RuleId[id=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/utils/MemoryEstimates.java`
#### Snippet
```java
	 * @return The memory estimate in bytes
	 */
	public static final double longArrayCost(long length) {
		// exactly the same size as a double array
		return doubleArrayCost(length);
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/utils/MemoryEstimates.java`
#### Snippet
```java
	 * @return The memory estimate in bytes
	 */
	public static final double objectArrayCost(long length) {
		double size = 0;
		size += 8d; // reference to array
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/utils/MemoryEstimates.java`
#### Snippet
```java
	 * @return The array memory cost
	 */
	public static final double stringArrayCost(String[] strings) {
		long size = 0;
		for(int i = 0; i < strings.length; i++)
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/utils/MemoryEstimates.java`
#### Snippet
```java
	 * @return The memory estimate in bytes
	 */
	public static final double doubleArrayCost(long length) {
		double size = 0;
		size += 8; // _values double array reference
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSV.java`
#### Snippet
```java
	}

	private final static boolean containsMetaTag(Text val) {
		if(val.charAt(0) == '#')
			return val.find(TfUtils.TXMTD_MVPREFIX) > -1//
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
	}

	private static final ValueType getHighest(ValueType state, ValueType c) {

		switch(state) {
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/SingletonLookupHashMap.java`
#### Snippet
```java
	}

	public static final SingletonLookupHashMap getMap() {
		return singleton;
	}
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
	}

	protected static final void computeRowSums(double[] c, int rl, int ru, double[] preAgg, AOffset indexes, int nRows) {
		int r = rl;
		final AIterator it = indexes.getIterator(rl);
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
	}

	protected static final void computeRowProduct(double[] c, int rl, int ru, AOffset indexes, int nRows, double def,
		double norm) {
		int r = rl;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
	}

	protected static final void computeRowMxx(double[] c, Builtin builtin, int rl, int ru, AOffset indexes, int nRows,
		double def, double norm) {
		int r = rl;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
	}

	protected static final void computeRowProduct(double[] c, int rl, int ru, double[] preAgg, AMapToData data,
		AOffset indexes, int nRows) {
		int r = rl;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
	}

	protected static final void computeRowSums(double[] c, int rl, int ru, double[] preAgg, AMapToData data,
		AOffset indexes, int nRows) {
		int r = rl;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
	}

	protected static final void computeRowMxx(double[] c, Builtin builtin, int rl, int ru, double[] preAgg,
		AMapToData data, AOffset indexes, int nRows, double def) {
		int r = rl;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
	}

	protected static final void computeRowSums(double[] c, int rl, int ru, double[] preAgg, AMapToData data,
		AOffset indexes, int nRows) {
		final AIterator it = indexes.getIterator(rl);
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparse(final SparseBlock sb, final double[] v, final int fr, final int st,
		final int nCol, final int rep) {
		if(sb.isEmpty(fr))
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparse(final SparseBlock sb, final double[] v, final int fr, final int st,
		final int nCol) {
		if(sb.isEmpty(fr))
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntryDense(final double[] thisV, final double[] v, final int sf, final int st,
		final int nCol, final int rep) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntrySparseCSR(final SparseBlockCSR sb, final double[] v, final int fr, final int st,
		final int nCol, final int[] aix, final double[] avals) {

```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	}

	private static final void addToEntryDense(final double[] thisV, final double[] v, final int sf, final int st,
		final int nCol) {
		for(int i = sf, j = st; i < sf + nCol; i++, j++)
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Frame_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Schema)
        org.apache.sysds.protobuf.SysdsProtos.SchemaOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Schema_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Row_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      }
    }
    public static final com.google.protobuf.Descriptors.Descriptor
        getDescriptor() {
      return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Schema_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        return getDescriptor();
      }
      public static final com.google.protobuf.Descriptors.EnumDescriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.Schema.getDescriptor().getEnumTypes().get(0);
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Frame)
        org.apache.sysds.protobuf.SysdsProtos.FrameOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Frame_descriptor;
```

### FinalStaticMethod
'static' method declared `final`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        // @@protoc_insertion_point(builder_implements:sysds.Row)
        org.apache.sysds.protobuf.SysdsProtos.RowOrBuilder {
      public static final com.google.protobuf.Descriptors.Descriptor
          getDescriptor() {
        return org.apache.sysds.protobuf.SysdsProtos.internal_static_sysds_Row_descriptor;
```

## RuleId[id=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		return "" + _op;
	}
	
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/util/DnnUtils.java`
#### Snippet
```java
		try {
			padX2 = Long.parseLong(heightPadding)*2;
			return "" + getP(Long.parseLong(H), Long.parseLong(R), Long.parseLong(verticalStride), Long.parseLong(heightPadding));
		} catch(Exception e) {
			  if(padX2 == -1) 			return "((" + H + " + 2*" + heightPadding + " - " + R + ") / " + verticalStride + "+ 1)";
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
		String[] ret = new String[size()];
		for(int i = 0; i < size(); i++)
			ret[i] = _data[i] + "";
		return new StringArray(ret);
	}
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	private String pair(char[] d, int off) {
		if((int) _data[off + 1] == 1)
			return ((int) _data[off]) + "";
		else
			return ((int) _data[off]) + "-" + ((int) _data[off + 1]);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	private String pair(char[] d, int off, int sum) {
		if((int) _data[off + 1] == 1)
			return ((int) _data[off] + sum) + "";
		else
			return ((int) _data[off] + sum) + "-" + ((int) _data[off + 1]);
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CPInstruction.java`
#### Snippet
```java

			if (val.getDataType() == DataType.SCALAR)
				replacement = "" + ((ScalarObject) val).getStringValue();
			return replacement;
		} else {
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/functionobjects/ParameterizedBuiltin.java`
#### Snippet
```java
		case CHISQ:
			if ( params.get("df") == null ) {
				throw new DMLRuntimeException("" +
						"Degrees of freedom must be specified for chi-squared distribution " +
						"(e.g., q=qchisq(0.5, df=20); p=pchisq(target=q, df=1.2))");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/functionobjects/ParameterizedBuiltin.java`
#### Snippet
```java
		case F:
			if ( params.get("df1") == null || params.get("df2") == null ) {
				throw new DMLRuntimeException("" +
						"Degrees of freedom must be specified for F distribution " +
						"(e.g., q = qf(target=0.5, df1=20, df2=30); p=pf(target=q, df1=20, df2=30))");
```

### TrivialStringConcatenation
Empty string used in concatenation
in `src/main/java/org/apache/sysds/runtime/functionobjects/ParameterizedBuiltin.java`
#### Snippet
```java
		case T:
			if ( params.get("df") == null ) {
				throw new DMLRuntimeException("" +
						"Degrees of freedom is needed to compute probabilities from t distribution " +
						"(e.g., q = qt(target=0.5, df=10); p = pt(target=q, df=10))");
```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> Future<T> submit(Callable<T> task) {
		return _pool.submit(task);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public Future<?> submit(Runnable task) {
		return _pool.submit(task);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> List<Future<T>> invokeAll(Collection<? extends Callable<T>> tasks) throws InterruptedException {
		return _pool.invokeAll(tasks);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public boolean awaitTermination(long timeout, TimeUnit unit) throws InterruptedException {
		throw new NotImplementedException();
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> T invokeAny(Collection<? extends Callable<T>> tasks) throws InterruptedException, ExecutionException {
		throw new NotImplementedException();
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> Future<T> submit(Runnable task, T result) {
		return _pool.submit(task, result);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> List<Future<T>> invokeAll(Collection<? extends Callable<T>> tasks, long timeout, TimeUnit unit)
			throws InterruptedException {
		return _pool.invokeAll(tasks, timeout, unit);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> List<Future<T>> invokeAll(Collection<? extends Callable<T>> tasks, long timeout, TimeUnit unit)
			throws InterruptedException {
		return _pool.invokeAll(tasks, timeout, unit);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java
	
	@Override
	public void execute(Runnable command) {
		_pool.execute(command);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> T invokeAny(Collection<? extends Callable<T>> tasks, long timeout, TimeUnit unit)
			throws InterruptedException, ExecutionException, TimeoutException {
		throw new NotImplementedException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java

	@Override
	public <T> T invokeAny(Collection<? extends Callable<T>> tasks, long timeout, TimeUnit unit)
			throws InterruptedException, ExecutionException, TimeoutException {
		throw new NotImplementedException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/ByteBufferDataInput.java`
#### Snippet
```java
	
	@Override
	public void readFully(byte[] b) throws IOException {
		_buff.get(b);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/ByteBufferDataInput.java`
#### Snippet
```java

	@Override
	public void readFully(byte[] b, int off, int len) throws IOException {
		_buff.get(b, off, len);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataInputStream.java`
#### Snippet
```java

	@Override
	public void readFully(byte[] b, int off, int len) throws IOException {
		if (len < 0)
			throw new IndexOutOfBoundsException();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataInputStream.java`
#### Snippet
```java

	@Override
	public void readFully(byte[] b) throws IOException {
		readFully(b, 0, b.length);
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataOutputStream.java`
#### Snippet
```java

	@Override
	public void write(byte[] b, int off, int len) 
		throws IOException 
	{
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataOutputStream.java`
#### Snippet
```java

	@Override
	public void writeBytes(String s) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataOutputStream.java`
#### Snippet
```java

	@Override
	public void writeChars(String s) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java

		@Override
		public int compareTo(DIListEntry o) {
			return compare(this, o);
		}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java

	@Override
	public int compareTo(ColumnEncoder o) {
		return Integer.compare(getEncoderType(this), getEncoderType(o));
	}
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ConvertMatrixBlockToIJVLines.java`
#### Snippet
```java
		Iterable<String> ret = new Iterable<String>() {
			@Override
			public Iterator<String> iterator() {
				return new Iterator<String>() {
					
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataInput.java`
#### Snippet
```java

	@Override
	public void readFully(byte[] b, int off, int len) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataInput.java`
#### Snippet
```java

	@Override
	public void readFully(byte[] b) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java

	@Override
	public void writeChars(String s) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java

	@Override
	public void write(byte[] b, int off, int len) throws IOException {
		System.arraycopy(b, off, _buff, _count, len);
		_count += len;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java

	@Override
	public void writeBytes(String s) throws IOException {
		throw new IOException("Not supported.");
	}
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java

	@Override
	public void write(byte[] b) throws IOException {
		System.arraycopy(b, 0, _buff, _count, b.length);
		_count += b.length;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRange.java`
#### Snippet
```java
	
	@Override
	public int compareTo(FederatedRange o) {
		for (int i = 0; i < _beginDims.length; i++) {
			if ( _beginDims[i] < o._beginDims[i])
```

## RuleId[id=ExplicitArrayFilling]
### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
	{
		Types.ValueType[] schema = new Types.ValueType[cols];
		for(int i = 0; i < cols; i++) {
			schema[i] = Types.ValueType.FP64;
		}
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupEmpty.java`
#### Snippet
```java
	@Override
	protected void computeColProduct(double[] c, int nRows) {
		for(int i = 0; i < c.length; i++)
			c[i] = 0;
	}
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupEmpty.java`
#### Snippet
```java
	@Override
	protected void computeRowProduct(double[] c, int rl, int ru, double[] preAgg) {
		for(int i = 0; i < c.length; i++)
			c[i] = 0;
	}
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
	public static double[] createReference(int nCol, double val) {
		double[] reference = new double[nCol];
		for(int i = 0; i < nCol; i++)
			reference[i] = val;
		return reference;
```

### ExplicitArrayFilling
Can be replaced with single 'Arrays.fill()' method call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
		final int numCols = cols.size();
		double[] values = new double[numCols];
		for(int i = 0; i < numCols; i++)
			values[i] = value;
		return create(cols, values);
```

## RuleId[id=DuplicateCondition]
### DuplicateCondition
Duplicate condition `inst instanceof ComputationFEDInstruction`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java

	public static boolean isOutputFederated(Instruction inst, Data data) {
		if (!(inst instanceof ComputationFEDInstruction))
			return false;
		// return true if the output matrixobject is federated
```

### DuplicateCondition
Duplicate condition `inst instanceof ComputationFEDInstruction`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
			return false;
		// return true if the output matrixobject is federated
		if (inst instanceof ComputationFEDInstruction)
			if (data instanceof MatrixObject && ((MatrixObject) data).isFederated())
				return true;
```

### DuplicateCondition
Duplicate condition `this == DDCFOR`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java

		public boolean isDense() {
			return this == DDC || this == CONST || this == DDCFOR || this == DDCFOR;
		}

```

### DuplicateCondition
Duplicate condition `this == DDCFOR`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java

		public boolean isDense() {
			return this == DDC || this == CONST || this == DDCFOR || this == DDCFOR;
		}

```

### DuplicateCondition
Duplicate condition `group.getNumRows() != target.getNumRows()`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroupNWeights.java`
#### Snippet
```java
		
		//sanity check matching block dimensions
		if(group.getNumRows() != target.getNumRows() || group.getNumRows()!=target.getNumRows()) {
			throw new Exception("The blocksize for group/target/weight blocks are mismatched: " + group.getNumRows()  + ", " + target.getNumRows() + ", " + weight.getNumRows());
		}
```

### DuplicateCondition
Duplicate condition `group.getNumRows()!=target.getNumRows()`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroupNWeights.java`
#### Snippet
```java
		
		//sanity check matching block dimensions
		if(group.getNumRows() != target.getNumRows() || group.getNumRows()!=target.getNumRows()) {
			throw new Exception("The blocksize for group/target/weight blocks are mismatched: " + group.getNumRows()  + ", " + target.getNumRows() + ", " + weight.getNumRows());
		}
```

### DuplicateCondition
Duplicate condition `out_blen%in_blen==0`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractBlockForBinaryReblock.java`
#### Snippet
```java
		final long out_startColBlockIndex = UtilFunctions.computeBlockIndex(startColGlobalCellIndex, out_blen);
		final long out_endColBlockIndex = UtilFunctions.computeBlockIndex(endColGlobalCellIndex, out_blen);
		final boolean aligned = out_blen%in_blen==0 && out_blen%in_blen==0; //e.g, 1K -> 2K
		
		ArrayList<Tuple2<MatrixIndexes, MatrixBlock>> retVal = new ArrayList<>();
```

### DuplicateCondition
Duplicate condition `out_blen%in_blen==0`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractBlockForBinaryReblock.java`
#### Snippet
```java
		final long out_startColBlockIndex = UtilFunctions.computeBlockIndex(startColGlobalCellIndex, out_blen);
		final long out_endColBlockIndex = UtilFunctions.computeBlockIndex(endColGlobalCellIndex, out_blen);
		final boolean aligned = out_blen%in_blen==0 && out_blen%in_blen==0; //e.g, 1K -> 2K
		
		ArrayList<Tuple2<MatrixIndexes, MatrixBlock>> retVal = new ArrayList<>();
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
					File parent = out_file.getParentFile();
					if (parent != null) {
						parent.mkdirs();
						parent.deleteOnExit();
					}
```

### IgnoreResultOfCall
Result of `BufferedInputStream.skip()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
		try {
			bis.reset();
			bis.skip(offset);
			bis.read(signature);
		}
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
			bis.reset();
			bis.skip(offset);
			bis.read(signature);
		}
		catch(IOException e) {
```

### IgnoreResultOfCall
Result of `BufferedInputStream.skip()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
			byte[] b = new byte[superBlockHeaderSize];
			bis.reset();
			bis.skip((int) fileLocation);
			bis.read(b);
			header.put(b);
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
			bis.reset();
			bis.skip((int) fileLocation);
			bis.read(b);
			header.put(b);
		}
```

### IgnoreResultOfCall
Result of `BufferedInputStream.skip()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
			byte[] hb = new byte[nextSectionSize];
			bis.reset();
			bis.skip(address);
			bis.read(hb);
			header.put(hb);
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5Superblock.java`
#### Snippet
```java
			bis.reset();
			bis.skip(address);
			bis.read(hb);
			header.put(hb);
			address += nextSectionSize;
```

### IgnoreResultOfCall
Result of `BufferedInputStream.skip()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5RootObject.java`
#### Snippet
```java
			byte[] b = new byte[length];
			bufferedInputStream.reset();
			bufferedInputStream.skip(address);
			bufferedInputStream.read(b);
			bb.put(b);
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5RootObject.java`
#### Snippet
```java
			bufferedInputStream.reset();
			bufferedInputStream.skip(address);
			bufferedInputStream.read(b);
			bb.put(b);
		}
```

### IgnoreResultOfCall
Result of `BufferedInputStream.skip()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5RootObject.java`
#### Snippet
```java
			byte[] b = new byte[length];
			bufferedInputStream.reset();
			bufferedInputStream.skip(address);
			bufferedInputStream.read(b);
			bb.put(b);
```

### IgnoreResultOfCall
Result of `FilterInputStream.read()` is ignored
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5RootObject.java`
#### Snippet
```java
			bufferedInputStream.reset();
			bufferedInputStream.skip(address);
			bufferedInputStream.read(b);
			bb.put(b);
		}
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		File f =  new File(dir);
		if( !f.exists() )
			f.mkdirs();
		
		return dir;
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		
		//delete file/dir itself
		dir.delete();
	}
	
```

### IgnoreResultOfCall
Result of `File.setExecutable()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		short sO = (short)(c[2]-48); 
		
		file.setExecutable( (sU&1)==1, (sO&1)==0 );
		file.setWritable(   (sU&2)==2, (sO&2)==0 );
		file.setReadable(   (sU&4)==4, (sO&4)==0 );
```

### IgnoreResultOfCall
Result of `File.setWritable()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		
		file.setExecutable( (sU&1)==1, (sO&1)==0 );
		file.setWritable(   (sU&2)==2, (sO&2)==0 );
		file.setReadable(   (sU&4)==4, (sO&4)==0 );
	}
```

### IgnoreResultOfCall
Result of `File.setReadable()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		file.setExecutable( (sU&1)==1, (sO&1)==0 );
		file.setWritable(   (sU&2)==2, (sO&2)==0 );
		file.setReadable(   (sU&4)==4, (sO&4)==0 );
	}

```

### IgnoreResultOfCall
Result of `FileChannel.write()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
			channel = FileChannel.open(path, StandardOpenOption.CREATE, 
				StandardOpenOption.TRUNCATE_EXISTING, StandardOpenOption.WRITE);
			channel.write(ByteBuffer.wrap(data));
		}
		finally {
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		{
			if( fileOnly ) //delete single file
				fdir.delete();
			else //recursively delete entire directory
				rDelete(fdir);	
```

### IgnoreResultOfCall
Result of `Throwable.getMessage()` is ignored
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			date = curr.parse(dateString);
		}
		catch(Exception e) { e.getMessage();}

		Date newDate;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
			File ftmp = new File(_workingDir+"/"+name.replace(".", "/")+".java");
			if( !ftmp.getParentFile().exists() )
				ftmp.getParentFile().mkdirs();
			LocalFileUtils.writeTextFile(ftmp, src);

```

### IgnoreResultOfCall
Result of `String.replaceAll()` is ignored
in `src/main/java/org/apache/sysds/runtime/lineage/LineageParser.java`
#### Snippet
```java
	
	protected static void parseLineageTraceDedup(String str) {
		str.replaceAll("\r\n", "\n");
		String[] allPatches = str.split("\n\n");
		for (String patch : allPatches) {
```

### IgnoreResultOfCall
Result of `OffsetFactory.getNoZero()` is ignored
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetChar.java`
#### Snippet
```java
			offsets[i] = in.readChar();

		OffsetFactory.getNoZero(offsets);
		return new OffsetChar(offsets, offsetToFirst, offsetToLast, OffsetFactory.getNoZero(offsets));
	}
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
				for( File f : files )
					if( f.getName().startsWith(cacheEvictionLocalFilePrefix) )
						f.delete();
				if( withDir )
					fdir.delete(); //deletes dir only if empty
```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
						f.delete();
				if( withDir )
					fdir.delete(); //deletes dir only if empty
			}
		}
```

## RuleId[id=RedundantMethodOverride]
### RedundantMethodOverride
Method `resetNoFill()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockLFP32.java`
#### Snippet
```java

	@Override
	public void resetNoFill(int rlen, int[] odims){
		throw new NotImplementedException();
	}
```

### RedundantMethodOverride
Method `pos()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockString.java`
#### Snippet
```java

	@Override
	public int pos(int r, int c) {
		return r * _odims[0] + c;
	}
```

### RedundantMethodOverride
Method `pos()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockString.java`
#### Snippet
```java

	@Override
	public int pos(int r) {
		return r * _odims[0];
	}
```

### RedundantMethodOverride
Method `pos()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockFP64.java`
#### Snippet
```java

	@Override
	public int pos(int r, int c) {
		return r * _odims[0] + c;
	}
```

### RedundantMethodOverride
Method `pos()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockFP64.java`
#### Snippet
```java

	@Override
	public int pos(int r) {
		return r * _odims[0];
	}
```

### RedundantMethodOverride
Method `hashCode()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedPair.java`
#### Snippet
```java
	
	@Override
	public int hashCode() {
		throw new RuntimeException("hashCode() should never be called on instances of this class.");
	}
```

### RedundantMethodOverride
Method `hashCode()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedCell.java`
#### Snippet
```java

	@Override
	public int hashCode() {
		throw new RuntimeException("hashCode() should never be called on instances of this class.");
	}
```

### RedundantMethodOverride
Method `sparseScalarOperationsInPlace()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedCell.java`
#### Snippet
```java

	@Override
	public void sparseScalarOperationsInPlace(ScalarOperator op) {
		value=op.executeScalar(value);
	}
```

### RedundantMethodOverride
Method `denseScalarOperationsInPlace()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedCell.java`
#### Snippet
```java

	@Override
	public void denseScalarOperationsInPlace(ScalarOperator op) {
		value=op.executeScalar(value);
	}
```

### RedundantMethodOverride
Method `sparseUnaryOperationsInPlace()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedCell.java`
#### Snippet
```java

	@Override
	public void sparseUnaryOperationsInPlace(UnaryOperator op) {
		value=op.fn.execute(value);
	}
```

### RedundantMethodOverride
Method `getValue()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/matrix/data/WeightedCell.java`
#### Snippet
```java

	@Override
	public double getValue() {
		return value;
	}
```

### RedundantMethodOverride
Method `getColIndices()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ConstScheme.java`
#### Snippet
```java

	@Override
	protected IColIndex getColIndices() {
		return cols;
	}
```

### RedundantMethodOverride
Method `value()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetChar.java`
#### Snippet
```java

		@Override
		public int value() {
			return offset;
		}
```

### RedundantMethodOverride
Method `value()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetChar.java`
#### Snippet
```java

		@Override
		public int value() {
			return offset;
		}
```

### RedundantMethodOverride
Method `initialValue()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
	private ThreadLocal<OffsetCache> cacheRow = new ThreadLocal<OffsetCache>() {
		@Override
		protected OffsetCache initialValue() {
			return null;
		}
```

### RedundantMethodOverride
Method `getSparsity()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public double getSparsity() {
		return 1.0d / (double)nRowCol;
	}
```

### RedundantMethodOverride
Method `colSum()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public void colSum(double[] c, int[] counts, IColIndex colIndexes) {
		for(int i = 0; i < colIndexes.size(); i++) {
			// very nice...
```

### RedundantMethodOverride
Method `replaceWithReference()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public ADictionary replaceWithReference(double pattern, double replace, double[] reference) {
		if(containsValueWithReference(pattern, reference))
			return getMBDict().replaceWithReference(pattern, replace, reference);
```

### RedundantMethodOverride
Method `colProduct()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public void colProduct(double[] res, int[] counts, IColIndex colIndexes) {
		for(int i = 0; i < colIndexes.size(); i++) {
			res[colIndexes.get(i)] = 0;
```

### RedundantMethodOverride
Method `productAllRowsToDouble()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public double[] productAllRowsToDouble(int nCol) {
		return new double[nRowCol];
	}
```

### RedundantMethodOverride
Method `productAllRowsToDoubleWithDefault()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public double[] productAllRowsToDoubleWithDefault(double[] defaultTuple) {
		return new double[nRowCol];
	}
```

### RedundantMethodOverride
Method `scaleTuples()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public ADictionary scaleTuples(int[] scaling, int nCol) {
		return getMBDict().scaleTuples(scaling, nCol);
	}
```

### RedundantMethodOverride
Method `getString()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public String getString(int colIndexes) {
		return "IdentityMatrix of size: " + nRowCol;
	}
```

### RedundantMethodOverride
Method `containsValue()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public boolean containsValue(double pattern) {
		return pattern == 0.0 || pattern == 1.0;
	}
```

### RedundantMethodOverride
Method `toString()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public String toString() {
		return "IdentityMatrix of size: " + nRowCol;
	}
```

### RedundantMethodOverride
Method `initColList()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderOmit.java`
#### Snippet
```java
	}

	public int initColList(int[] colList) {
		_colList = colList;
		return _colList.length;
```

### RedundantMethodOverride
Method `isApplicable()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/transform/encode/EncoderOmit.java`
#### Snippet
```java
	 * @return true if at least one column to encode
	 */
	public boolean isApplicable() {
		return(_colList != null && _colList.length > 0);
	}
```

### RedundantMethodOverride
Method `getOperator()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/instructions/cp/BinaryCPInstruction.java`
#### Snippet
```java
	
	
	public Operator getOperator() {
		return _optr;
	}
```

### RedundantMethodOverride
Method `getBlocksize()` is identical to its super method
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
	}

	public int getBlocksize() {
		return getDataCharacteristics().getBlocksize();
	}
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
dims.length \* 4: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
		if( fmt == FileFormat.TEXT )
			// nnz * (8 bytes for number + each dimension with an expected String length of 3 and one space)
			return nnz * (8 + dims.length * 4); // very simple estimation. example:100 100 1.345678
		throw new DMLRuntimeException("Tensor output format not implemented.");
	}
```

### IntegerMultiplicationImplicitCastToLong
8\*k: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
		//create sort tasks (increase number of tasks for better load balance)
		ArrayList<SortRowsTask> tasks = new ArrayList<>();
		int k2 = (int) Math.min(8*k, rlen); 
		int blklen = (int)(Math.ceil((double)rlen/k2));
		for( int i=0; i<k2 & i*blklen<rlen; i++ )
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
		int k2 = (int) Math.min(8*k, rlen); 
		int blklen = (int)(Math.ceil((double)rlen/k2));
		for( int i=0; i<k2 & i*blklen<rlen; i++ )
			tasks.add(new SortRowsTask(dest, i*blklen, Math.min((i+1)*blklen, (int)rlen)));
		
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
			ArrayList<WriteFileTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / numThreads);
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, fileSystem, src, i*blklen, (int)Math.min((i+1)*blklen, rlen), schemaMap));
```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, fileSystem, src, i*blklen, (int)Math.min((i+1)*blklen, rlen), schemaMap));
			}

```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fileSystem instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++)
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fileSystem,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
			ArrayList<WriteFileTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / numThreads);
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen), csvprops));
```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen), csvprops));
			}

```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
			ArrayList<WriteTextTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / numThreads);
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteTextTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen)));
```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteTextTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen)));
			}

```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i \* blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/ReaderHDF5Parallel.java`
#### Snippet
```java
			rlen = src.getNumRows();
			int blklen = (int) Math.ceil((double) rlen / _numThreads);
			for(int i = 0; i < _numThreads & i * blklen < rlen; i++) {
				int rl = i * blklen;
				int ru = (int) Math.min((i + 1) * blklen, rlen);
```

### IntegerMultiplicationImplicitCastToLong
(i + 1) \* blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/ReaderHDF5Parallel.java`
#### Snippet
```java
			for(int i = 0; i < _numThreads & i * blklen < rlen; i++) {
				int rl = i * blklen;
				int ru = (int) Math.min((i + 1) * blklen, rlen);
				BufferedInputStream bis = new BufferedInputStream(fs.open(path), bufferSize);

```

### IntegerMultiplicationImplicitCastToLong
i \* blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
			for (int i = 0; i < numThreads & i * blklen < rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i * blklen, Math.min((i + 1) * blklen, rlen), blen));
			}
			
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
			ArrayList<WriteFileTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / blen / numThreads) * blen;
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, Math.min((i+1)*blklen, rlen), blen));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, Math.min((i+1)*blklen, rlen), blen));
			}

```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, Math.min((i+1)*blklen, rlen), blen));
			}

```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
			ArrayList<WriteFileTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / blen / numThreads) * blen;
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, Math.min((i+1)*blklen, (int)rlen), blen));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
			ArrayList<WriteFileTask> tasks = new ArrayList<>();
			int blklen = (int)Math.ceil((double)rlen / numThreads);
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen)));
```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, (int)Math.min((i+1)*blklen, rlen)));
			}

```

### IntegerMultiplicationImplicitCastToLong
i\*blklen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
```

### IntegerMultiplicationImplicitCastToLong
i \* newBlockSize: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockLBool.java`
#### Snippet
```java
		if (_blen == newBlockSize && dataLength <= capacity()) {
			for (int i = 0; i < numBlocks; i++) {
				int toIndex = (int)Math.min(newBlockSize, dataLength - i * newBlockSize) * _odims[0];
				_blocks[i].set(0, toIndex, bv);
				// Clear old data so we can use cardinality for computeNnz
```

### IntegerMultiplicationImplicitCastToLong
4 \* (1 + _dims.length): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/data/TensorBlock.java`
#### Snippet
```java
	public long getExactSerializedSize() {
		// header size (_basic, _dims.length + _dims[*], type)
		long size = 1 + 4 * (1 + _dims.length) + 1;
		if (isAllocated()) {
			if (_basic) {
```

### IntegerMultiplicationImplicitCastToLong
bi \* newBlockSize: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockLDRB.java`
#### Snippet
```java
			IntStream.range(0, numBlocks)
					.forEach((bi) -> {
						int toIndex = (int)Math.min(newBlockSize, dataLength - bi * newBlockSize) * _odims[0];
						fillBlock(bi, 0, toIndex, v);
					});
```

### IntegerMultiplicationImplicitCastToLong
mb.getNumRows() \* mb.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/util/Py4jConverterUtils.java`
#### Snippet
```java
		}

		long limit = mb.getNumRows() * mb.getNumColumns();
		int times = Double.SIZE / Byte.SIZE;
		if(limit > Integer.MAX_VALUE / times)
```

### IntegerMultiplicationImplicitCastToLong
8 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
	@Override
	public long getExactSerializedSize() {
		return 1 + 8 * _data.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
8 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
	@Override
	public long getExactSerializedSize() {
		return 1 + 8 * _data.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
2 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
	@Override
	public long getExactSerializedSize() {
		return 1 + 2 * _data.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
4 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
	@Override
	public long getExactSerializedSize() {
		return 1 + 4 * _data.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
_data.length \* 8: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BitSetArray.java`
#### Snippet
```java
	public long getExactSerializedSize() {
		long size = 1 + 4 + 4;
		size += _data.length * 8;
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
4 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
	@Override
	public long getExactSerializedSize() {
		return 1 + 4 * _data.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
row \<\< 16: integer shift implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixValue.java`
#### Snippet
```java
		@Override
		public int hashCode() {
			return UtilFunctions.longHashCode((row << 16) + column);
		}
	
```

### IntegerMultiplicationImplicitCastToLong
(iix-1)\*blen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	@SuppressWarnings("rawtypes")
	public static ArrayList performSlice(IndexRange ixrange, int blen, int iix, int jix, FrameBlock in) {
		Pair<Long, FrameBlock> lfp = new Pair<>(Long.valueOf(((iix-1)*blen)+1), in);
		ArrayList<Pair<Long, FrameBlock>> outlist = performSlice(lfp, ixrange, blen);
	
```

### IntegerMultiplicationImplicitCastToLong
blockrows\*blockcols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		double log1mp = Math.log(1-sparsity);
		long idx = 0;  // takes values in range [1, blen*blen] (both ends including)
		long blocksize = blockrows*blockcols;
		while(idx < blocksize) {
			//compute skip to next index
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;

```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;

```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;

```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, PoolingType poolingType, double intermediateMemoryBudget) {
		long CHW = C*H*W; long CPQ = C*P*Q;  
		long NCHW = N*CHW; long NCPQ = N*CPQ; 

```

### IntegerMultiplicationImplicitCastToLong
C\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, PoolingType poolingType, double intermediateMemoryBudget) {
		long CHW = C*H*W; long CPQ = C*P*Q;  
		long NCHW = N*CHW; long NCPQ = N*CPQ; 

```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int K, int R, int S, int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q, double intermediateMemoryBudget) {

		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		long NPQ = N*P*Q;
```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int K, int R, int S, int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q, double intermediateMemoryBudget) {

		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		long NPQ = N*P*Q;
```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int K, int R, int S, int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q, double intermediateMemoryBudget) {

		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		long NPQ = N*P*Q;
```

### IntegerMultiplicationImplicitCastToLong
N\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		long NPQ = N*P*Q;
		
		boolean isSparseFilter = isInSparseFormat(gCtx, filter);
```

### IntegerMultiplicationImplicitCastToLong
T\*D: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			ec.releaseMatrixOutputForGPUInstruction(dcxName);
			
			Pointer smlDx = getDenseOutputPointer(ec, gCtx, instName, dxName, N, T*D);
			LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_dinput",
					ExecutionConfig.getConfigForSimpleVectorOperations(N*T*D),
```

### IntegerMultiplicationImplicitCastToLong
4\*M: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_dweight",
					ExecutionConfig.getConfigForSimpleVectorOperations((D+M+2)*(4*M)),
					getDenseOutputPointer(ec, gCtx, instName, dwName, D+M, 4*M), 
					getDenseOutputPointer(ec, gCtx, instName, dbName, 1, 4*M), cudnnDwPointer, D, M);
			gCtx.cudaFreeHelper(instName, cudnnDwPointer, DMLScript.EAGER_CUDA_FREE);
```

### IntegerMultiplicationImplicitCastToLong
4\*M: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
					ExecutionConfig.getConfigForSimpleVectorOperations((D+M+2)*(4*M)),
					getDenseOutputPointer(ec, gCtx, instName, dwName, D+M, 4*M), 
					getDenseOutputPointer(ec, gCtx, instName, dbName, 1, 4*M), cudnnDwPointer, D, M);
			gCtx.cudaFreeHelper(instName, cudnnDwPointer, DMLScript.EAGER_CUDA_FREE);
			ec.releaseMatrixOutputForGPUInstruction(dwName);
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		
```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		
```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, double intermediateMemoryBudget) {
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
		
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, PoolingType poolingType, double intermediateMemoryBudget) {
		long CHW = C*H*W; long CPQ = C*P*Q;  
		long NCHW = N*CHW; long NCPQ = N*CPQ; 

```

### IntegerMultiplicationImplicitCastToLong
C\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			int S, int pad_h, int pad_w, int stride_h, int stride_w, int P,
			int Q, PoolingType poolingType, double intermediateMemoryBudget) {
		long CHW = C*H*W; long CPQ = C*P*Q;  
		long NCHW = N*CHW; long NCPQ = N*CPQ; 

```

### IntegerMultiplicationImplicitCastToLong
T\*M: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		if(return_sequences) {
			gCtx.cudaFreeHelper(instName, hyPointer, DMLScript.EAGER_CUDA_FREE);
			Pointer sysdsYPointer = getDenseOutputPointer(ec, gCtx, instName, outputName, N, T*M);
			LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_output",
					ExecutionConfig.getConfigForSimpleVectorOperations(N*T*M),
```

### IntegerMultiplicationImplicitCastToLong
1 \<\< k: integer shift implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinct/CountDistinctFunctionSketch.java`
#### Snippet
```java

	private long extractRightKBitsFromIndex(long n, int startingIndex, int k) {
		long kMask = (1 << k) - 1;
		return kMask & (n >> startingIndex);
	}
```

### IntegerMultiplicationImplicitCastToLong
m \* n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
			
			//compute non-zero input values
			long nnz = lsparseSafe ? 0 : m * n;
			for(int bi=0; bi<dc.numBlocks(); bi++) {
				int blen = dc.blockSize(bi);
```

### IntegerMultiplicationImplicitCastToLong
out.getNumRows()\*out.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofRowwise.java`
#### Snippet
```java
		out.setNumRows(out.getNumColumns());
		out.setNumColumns(rlen);
		out.setNonZeros(out.getNumRows()*out.getNumColumns());
	}
	
```

### IntegerMultiplicationImplicitCastToLong
(ru-rl+1) \* (cu-cl+1): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		//compute nnz of output (not maintained due to native calls)
		dest.setNonZeros((getNonZeros() == getLength()) ? 
			(ru-rl+1) * (cu-cl+1) : dest.recomputeNonZeros());
	}
	
```

### IntegerMultiplicationImplicitCastToLong
m \* n \* (d1 != 0 ? 1 : 0): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			}
			
			final boolean sparseOutput = evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));

```

### IntegerMultiplicationImplicitCastToLong
m \* n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			
			final boolean sparseOutput = evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));

			if(m2 instanceof CompressedMatrixBlock)
```

### IntegerMultiplicationImplicitCastToLong
m \* n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			
			final boolean sparseOutput = evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));

			if(m2 instanceof CompressedMatrixBlock)
```

### IntegerMultiplicationImplicitCastToLong
rlen\*n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		int n = Builtin.isBuiltinCode(op.fn, BuiltinCode.CUMSUMPROD) ? 1 : clen;
		if( ret == null )
			ret = new MatrixBlock(rlen, n, sp, sp ? nonZeros : rlen*n);
		else
			ret.reset(rlen, n, sp);
```

### IntegerMultiplicationImplicitCastToLong
a.getNumRows()\*a.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
		//correction for min/max
		if( (_aggOp == AggOp.MIN || _aggOp == AggOp.MAX) && sparseSafe 
			&& a.getNonZeros()<a.getNumRows()*a.getNumColumns() )
			ret = getAggFunction().execute(ret, 0); //unseen 0 might be max or min value
		
```

### IntegerMultiplicationImplicitCastToLong
ret.clen\*8\*k: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//fall back to sequential version if necessary
		if( k <= 1 || in1.nonZeros+in2.nonZeros < PAR_NUMCELL_THRESHOLD1 || in1.rlen <= k/2 
			|| (!(op.indexFn instanceof ReduceCol) &&  ret.clen*8*k > PAR_INTERMEDIATE_SIZE_THRESHOLD) ) {
			return aggregateTernary(in1, in2, in3, ret, op);
		}
```

### IntegerMultiplicationImplicitCastToLong
out.clen\*8\*k: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		boolean sharedTP = (InfrastructureAnalyzer.getLocalParallelism() == k);
		return k > 1 && out.isThreadSafe() && in.rlen > (sharedTP ? k/8 : k/2)
			&& (uaop.indexFn instanceof ReduceCol || out.clen*8*k < PAR_INTERMEDIATE_SIZE_THRESHOLD) //size
			&& in.nonZeros > (sharedTP ? PAR_NUMCELL_THRESHOLD2 : PAR_NUMCELL_THRESHOLD1);
	}
```

### IntegerMultiplicationImplicitCastToLong
(ru-rl)\*n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//note: before aggregate computation in order to
		//exploit 0 sum (noop) and better numerical stability
		count += (ru-rl)*n - a.size(rl, ru);
		
		//compute aggregate mean
```

### IntegerMultiplicationImplicitCastToLong
out.clen\*8\*k: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//fall back to sequential if necessary or agg not supported
		if( k <= 1 || (long)in.rlen*in.clen < PAR_NUMCELL_THRESHOLD1 || in.rlen <= k
			|| out.clen*8*k > PAR_INTERMEDIATE_SIZE_THRESHOLD || uaop == null || !out.isThreadSafe()) {
			return cumaggregateUnaryMatrix(in, out, uop);
		}
```

### IntegerMultiplicationImplicitCastToLong
m \* n \* (d1 != 0 ? 1 : 0): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibTernaryOp.java`
#### Snippet
```java
		}
		else {
			final boolean sparseOutput = MatrixBlock.evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : m1.getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));
			ret.reset(m, n, sparseOutput);
```

### IntegerMultiplicationImplicitCastToLong
m \* n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibTernaryOp.java`
#### Snippet
```java
		else {
			final boolean sparseOutput = MatrixBlock.evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : m1.getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));
			ret.reset(m, n, sparseOutput);
			final MatrixBlock thisUncompressed = m1.getUncompressed("Ternary Operation not supported");
```

### IntegerMultiplicationImplicitCastToLong
m \* n: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibTernaryOp.java`
#### Snippet
```java
		else {
			final boolean sparseOutput = MatrixBlock.evalSparseFormatInMemory(m, n, (s1 ? m * n * (d1 != 0 ? 1 : 0) : m1.getNonZeros()) +
				Math.min(s2 ? m * n : m2.getNonZeros(), s3 ? m * n : m3.getNonZeros()));
			ret.reset(m, n, sparseOutput);
			final MatrixBlock thisUncompressed = m1.getUncompressed("Ternary Operation not supported");
```

### IntegerMultiplicationImplicitCastToLong
numRowsOutput \* numColumnsOutput: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibLeftMultBy.java`
#### Snippet
```java
		final int numColumnsOutput = m1.getNumColumns();
		if(ret == null)
			ret = new MatrixBlock(numRowsOutput, numColumnsOutput, false, numRowsOutput * numColumnsOutput);
		else if(!(ret.getNumColumns() == numColumnsOutput && ret.getNumRows() == numRowsOutput && ret.isAllocated()))
			ret.reset(numRowsOutput, numColumnsOutput, false, numRowsOutput * numColumnsOutput);
```

### IntegerMultiplicationImplicitCastToLong
numRowsOutput \* numColumnsOutput: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibLeftMultBy.java`
#### Snippet
```java
			ret = new MatrixBlock(numRowsOutput, numColumnsOutput, false, numRowsOutput * numColumnsOutput);
		else if(!(ret.getNumColumns() == numColumnsOutput && ret.getNumRows() == numRowsOutput && ret.isAllocated()))
			ret.reset(numRowsOutput, numColumnsOutput, false, numRowsOutput * numColumnsOutput);
		ret.allocateDenseBlock();
		return ret;
```

### IntegerMultiplicationImplicitCastToLong
clen \* rlen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
	public long recomputeNonZeros() {
		if(isOverlapping())
			nonZeros = clen * rlen;
		else {
			long nnz = 0;
```

### IntegerMultiplicationImplicitCastToLong
m1.getNumColumns() \* m1.getNumRows(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java

		ret.allocateColGroupList(newColGroups);
		ret.setNonZeros(m1.getNumColumns() * m1.getNumRows());
		return ret;
	}
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
		long ret = super.getExactSizeOnDisk();
		ret += _data.getExactSizeOnDisk();
		ret += 8 * _colIndexes.size(); // reference values.
		return ret;
	}
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
		long size = super.estimateInMemorySize();
		size += _data.getInMemorySize();
		size += 8 * _colIndexes.size();
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
offsetsInt.length \* cols.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java
		MatrixBlock sub = new MatrixBlock(offsetsInt.length, cols.size(), false);
		sub.allocateDenseBlock();
		sub.setNonZeros(offsetsInt.length * cols.size());
		double[] subV = sub.getDenseBlockValues();

```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
		long size = super.estimateInMemorySize();
		size += _indexes.getInMemorySize();
		size += 8 * _colIndexes.size();
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
		long ret = super.getExactSizeOnDisk();
		ret += _indexes.getExactSizeOnDisk();
		ret += 8 * _colIndexes.size(); // _default tuple values.
		return ret;
	}
```

### IntegerMultiplicationImplicitCastToLong
nColL \* nVal: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
			final int nVal = this.getNumValues();
			// allocation
			preAggFLOPS += nColL * nVal;
			// preAgg
			preAggFLOPS += nColL * commonDim; // worst case but okay
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		ret += _data.getExactSizeOnDisk();
		ret += _indexes.getExactSizeOnDisk();
		ret += 8 * _colIndexes.size(); // _default tuple values.
		return ret;
	}
```

### IntegerMultiplicationImplicitCastToLong
nColR \* nColL \* nVal: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
			preAggFLOPS += nColL * commonDim; // worst case but okay
			// multiply
			preAggFLOPS += nColR * nColL * nVal;
		}
		else {
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		size += _indexes.getInMemorySize();
		size += _data.getInMemorySize();
		size += 8 * _colIndexes.size();
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
nColR \* nVal: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
			final int nVal = lg.getNumValues();
			// allocation
			preAggFLOPS += nColR * nVal;
			// preAgg
			preAggFLOPS += nColR * commonDim; // worst case but okay
```

### IntegerMultiplicationImplicitCastToLong
nColR \* nColL \* nVal: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
			preAggFLOPS += nColR * commonDim; // worst case but okay
			// multiply
			preAggFLOPS += nColR * nColL * nVal;
		}

```

### IntegerMultiplicationImplicitCastToLong
4 \* _ptr.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupOffset.java`
#### Snippet
```java
		// actual bitmaps
		ret += 4; // _ptr list length
		ret += 4 * _ptr.length;
		ret += 4; // _data list length
		ret += 2 * _data.length;
```

### IntegerMultiplicationImplicitCastToLong
2 \* _data.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupOffset.java`
#### Snippet
```java
		ret += 4 * _ptr.length;
		ret += 4; // _data list length
		ret += 2 * _data.length;
		ret += 1; // boolean

```

### IntegerMultiplicationImplicitCastToLong
_data.getNumColumns() \* nColR: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
				System.arraycopy(rightV, offRight, sbr, offSubBlock, nColR);
			}
			subBlockRight.setNonZeros(_data.getNumColumns() * nColR);
		}
		MatrixBlock out = new MatrixBlock(_data.getNumRows(), nColR, false);
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCFOR.java`
#### Snippet
```java
		ret += _data.getExactSizeOnDisk();
		ret += _indexes.getExactSizeOnDisk();
		ret += 8 * _colIndexes.size(); // reference values.
		return ret;
	}
```

### IntegerMultiplicationImplicitCastToLong
8 \* _colIndexes.size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCFOR.java`
#### Snippet
```java
		size += _indexes.getInMemorySize();
		size += _data.getInMemorySize();
		size += 8 * _colIndexes.size();
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
offsets.length \* 2: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetChar.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 4 + 4 + offsets.length * 2;
	}

```

### IntegerMultiplicationImplicitCastToLong
4 \* cols.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 4 * cols.length;
	}

```

### IntegerMultiplicationImplicitCastToLong
_data_c.length \* 3: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToCharPByte.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 4 + _data_c.length * 3;
	}

```

### IntegerMultiplicationImplicitCastToLong
_data.length \* 4: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToInt.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 4 + _data.length * 4;
	}

```

### IntegerMultiplicationImplicitCastToLong
_data.toLongArray().length \* 8: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToBit.java`
#### Snippet
```java
	public long getExactSizeOnDisk() {
		long size = 1 + 4 + 4; // base variables
		size += _data.toLongArray().length * 8;
		return size;
	}
```

### IntegerMultiplicationImplicitCastToLong
_data.length \* 2: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToChar.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 4 + _data.length * 2;
	}

```

### IntegerMultiplicationImplicitCastToLong
8 \* size(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
	@Override
	public long getExactSizeOnDisk() {
		return 1 + 4 + 8 * size();
	}

```

### IntegerMultiplicationImplicitCastToLong
_data.getNumColumns() \* _data.getNumRows(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		if(_data.isInSparseFormat())
			ret[0] = 0; // if we are sparse there is a zero
		else if(_data.getNonZeros() < _data.getNumColumns() * _data.getNumRows())
			ret[0] = 0; // if the number of zeros are not equal number of cells.
		else {
```

### IntegerMultiplicationImplicitCastToLong
_data.getNumColumns() \* _data.getNumRows(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		if(_data.isInSparseFormat())
			ret[0] = 0; // if we are sparse there is a zero
		else if(_data.getNonZeros() < _data.getNumColumns() * _data.getNumRows())
			ret[0] = 0; // if the number of zeros are not equal number of cells.
		else {
```

### IntegerMultiplicationImplicitCastToLong
countThis \* counts\[i\]: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
							countThis++;

					nnz += countThis * counts[i];
				}
			}
```

### IntegerMultiplicationImplicitCastToLong
countThisTuple \* counts\[i\]: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
					if(values[off++] + reference[j] != 0)
						countThisTuple++;
				nnz += countThisTuple * counts[i];
			}
		}
```

### IntegerMultiplicationImplicitCastToLong
sb.size(i) \* counts\[i\]: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			for(int i = 0; i < counts.length; i++)
				if(!sb.isEmpty(i))
					nnz += sb.size(i) * counts[i];
		}
		else {
```

### IntegerMultiplicationImplicitCastToLong
countThisTuple \* counts\[i\]: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
						countThisTuple++;
				}
				nnz += countThisTuple * counts[i];
			}
		}
```

### IntegerMultiplicationImplicitCastToLong
inputRows\*inputCols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CtableCPInstruction.java`
#### Snippet
```java
			int inputRows = matBlock1.getNumRows();
			int inputCols = matBlock1.getNumColumns();
			boolean sparse = MatrixBlock.evalSparseFormatInMemory(outputDim1, outputDim2, inputRows*inputCols);
			//only create result block if dense; it is important not to aggregate on sparse result
			//blocks because it would implicitly turn the O(N) algorithm into O(N log N). 
```

### IntegerMultiplicationImplicitCastToLong
matBlock.getNumRows()\*matBlock.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in2.getName());
			if(filter.isEmpty() || matBlock.isEmpty()) {
```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			else {
				boolean sparse = matBlock.isUltraSparse(false) && params.bias == null
					&& matBlock.getInMemorySize() < MatrixBlock.estimateSizeDenseInMemory(N, K*P*Q);
				outputBlock = new MatrixBlock(N, K*P*Q, sparse).allocateBlock();
				if(params.enableNative && matBlock.isInSparseFormat())
```

### IntegerMultiplicationImplicitCastToLong
matBlock.getNumRows()\*matBlock.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d_bias_add")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in3.getName());
			MatrixBlock bias = ec.getMatrixInput(_in2.getName());
```

### IntegerMultiplicationImplicitCastToLong
filter.getNumRows()\*filter.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
	// This increases the number of native calls. For example:the cases where filter is sparse but input is dense
	private static boolean isFilterSparse(MatrixBlock filter) {
		long numElems = filter.getNumRows()*filter.getNumColumns();
		// if filter is less than 10 MB in dense format (which handles almost all the cases).
		// In fact, using threshold of 1 MB is still sufficient for common CNNs.
```

### IntegerMultiplicationImplicitCastToLong
rows \* cols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReshapeFEDInstruction.java`
#### Snippet
```java
				throw new DMLRuntimeException("Federated Rshape: " 
					+ "Federated input expected, but invoked w/ " + mo1.isFederated());
			if(mo1.getNumColumns() * mo1.getNumRows() != rows * cols)
				throw new DMLRuntimeException("Reshape matrix requires consistent numbers of input/output cells (" 
					+ mo1.getNumRows() + ":" + mo1.getNumColumns() + ", " + rows + ":" + cols + ").");
```

### IntegerMultiplicationImplicitCastToLong
rows\*cols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixReshapeGPUInstruction.java`
#### Snippet
```java
		GPUContext gCtx = ec.getGPUContext(0); 
		MatrixObject mat = getMatrixInputForGPUInstruction(ec, _input1.getName());
		if(rows*cols != mat.getNumRows()*mat.getNumColumns()) {
			throw new DMLRuntimeException("Incorrect number of rows and cols in rshape instruction");
		}
```

### IntegerMultiplicationImplicitCastToLong
rows\*cols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixReshapeGPUInstruction.java`
#### Snippet
```java
			// byrow = FALSE uses a custom kernel to perform rshape
			LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("colwise_reshape", 
				ExecutionConfig.getConfigForSimpleVectorOperations(LibMatrixCUDA.toInt(rows*cols)),
				inPtr, outPtr, LibMatrixCUDA.toInt(rows*cols), 
				LibMatrixCUDA.toInt(mat.getNumRows()), LibMatrixCUDA.toInt(mat.getNumColumns()),
```

### IntegerMultiplicationImplicitCastToLong
rows\*cols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixReshapeGPUInstruction.java`
#### Snippet
```java
			LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("colwise_reshape", 
				ExecutionConfig.getConfigForSimpleVectorOperations(LibMatrixCUDA.toInt(rows*cols)),
				inPtr, outPtr, LibMatrixCUDA.toInt(rows*cols), 
				LibMatrixCUDA.toInt(mat.getNumRows()), LibMatrixCUDA.toInt(mat.getNumColumns()),
				rows, cols);
```

### IntegerMultiplicationImplicitCastToLong
shadowPointer.length\*LibMatrixCUDA.sizeOfDataType: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/ShadowBuffer.java`
#### Snippet
```java
	public void moveToDevice() {
		long start = DMLScript.STATISTICS ? System.nanoTime() : 0;
		long numBytes = shadowPointer.length*LibMatrixCUDA.sizeOfDataType;
		gpuObj.jcudaDenseMatrixPtr = gpuObj.getGPUContext().allocate(null, numBytes, false);
		cudaMemcpy(gpuObj.jcudaDenseMatrixPtr, Pointer.to(shadowPointer), numBytes, jcuda.runtime.cudaMemcpyKind.cudaMemcpyHostToDevice);
```

### IntegerMultiplicationImplicitCastToLong
4\*M: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		Pointer sysdsWPointer = LibMatrixCuDNN.getDensePointerForCuDNN(gCtx, W, instructionName, D+M, 4*M);
		Pointer sysdsBiasPointer = LibMatrixCuDNN.getDensePointerForCuDNN(gCtx, bias, instructionName, 1, 4*M);
		Pointer cudnnWPointer = gCtx.allocate(instructionName, (D+M+2)*(4*M)*LibMatrixCUDA.sizeOfDataType, false);
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_weight",
				ExecutionConfig.getConfigForSimpleVectorOperations((D+M+2)*(4*M)),
```

### IntegerMultiplicationImplicitCastToLong
N\*T\*D: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		long numColsX = X.getNumColumns();
		int T = toInt(numColsX/ D); // since X:(N, T*D) ... seqLength
		Pointer cudnnInput = gCtx.allocate(instructionName, (N*T*D)*LibMatrixCUDA.sizeOfDataType, false);
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_input",
				ExecutionConfig.getConfigForSimpleVectorOperations(N*T*D),
```

### IntegerMultiplicationImplicitCastToLong
4\*M: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		Pointer sysdsBiasPointer = LibMatrixCuDNN.getDensePointerForCuDNN(gCtx, bias, instructionName, 1, 4*M);
		// TODO: find out if memset is necessary
		Pointer cudnnWPointer = gCtx.allocate(instructionName, (D+M+2)*(4*M)*LibMatrixCUDA.sizeOfDataType, true);
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_weight",
				ExecutionConfig.getConfigForSimpleVectorOperations((D+M+2)*(4*M)),
```

### IntegerMultiplicationImplicitCastToLong
N\*T\*D: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		long numColsX = X.getNumColumns();
		int T = toInt(numColsX/ D); // since X:(N, T*D) ... seqLength
		Pointer cudnnInput = gCtx.allocate(instructionName, (N*T*D)*LibMatrixCUDA.sizeOfDataType, false);
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_input",
				ExecutionConfig.getConfigForSimpleVectorOperations(N*T*D),
```

### IntegerMultiplicationImplicitCastToLong
C\*HW: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		int C = (int) ec.getScalarInput(_input2).getLongValue();
		int HW = (int) ec.getScalarInput(_input3).getLongValue();
		if(C*HW != input.getNumColumns()) {
			throw new DMLRuntimeException("Expected rows*cols" + C + "*" + HW + " to be equal to number of columns of input " + input.getNumColumns());
		}
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject filter = getMatrixInputForGPUInstruction(ec, _input2.getName());

			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
```

### IntegerMultiplicationImplicitCastToLong
K \* P \* Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, K * P * Q);
			
			LibMatrixCuDNN.conv2d(ec.getGPUContext(0), getExtendedOpcode(), image, filter, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject filter = getMatrixInputForGPUInstruction(ec, _input3.getName());

			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
```

### IntegerMultiplicationImplicitCastToLong
K \* P \* Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, K * P * Q);
			
			LibMatrixCuDNN.conv2dBiasAdd(ec.getGPUContext(0), getExtendedOpcode(), image, bias, filter, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject dout = getMatrixInputForGPUInstruction(ec, _input2.getName());

			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d_backward_filter");
			if(dout.getNumRows() != N || dout.getNumColumns() != K*P*Q) 
```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d_backward_filter");
			if(dout.getNumRows() != N || dout.getNumColumns() != K*P*Q) 
				throw new DMLRuntimeException("Incorrect dimensions for dout in conv2d_backward_filter: " + 
						dout.getNumRows() + " != " +  N + " || " + dout.getNumColumns() + " != " + K*P*Q);
```

### IntegerMultiplicationImplicitCastToLong
C \* R \* S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
						dout.getNumRows() + " != " +  N + " || " + dout.getNumColumns() + " != " + K*P*Q);
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), K, C * R * S);
			
			LibMatrixCuDNN.conv2dBackwardFilter(ec.getGPUContext(0), getExtendedOpcode(), image, dout, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
C\*R\*S: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject dout = getMatrixInputForGPUInstruction(ec, _input2.getName());

			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in convolution_backward_data");
			if(dout.getNumRows() != N || dout.getNumColumns() != K*P*Q) 
```

### IntegerMultiplicationImplicitCastToLong
K\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in convolution_backward_data");
			if(dout.getNumRows() != N || dout.getNumColumns() != K*P*Q) 
				throw new DMLRuntimeException("Incorrect dimensions for dout in conv2d_backward_data: " + 
						dout.getNumRows() + " != " +  N + " || " + dout.getNumColumns() + " != " + K*P*Q);
```

### IntegerMultiplicationImplicitCastToLong
C \* H \* W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
						dout.getNumRows() + " != " +  N + " || " + dout.getNumColumns() + " != " + K*P*Q);
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, C * H * W);
			
			LibMatrixCuDNN.conv2dBackwardData(ec.getGPUContext(0), getExtendedOpcode(), filter, dout, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject image = getMatrixInputForGPUInstruction(ec, _input1.getName());

			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in maxpooling: " + 
						image.getNumRows() + " != " +  N + " || " + image.getNumColumns() + " != " + C*H*W);
```

### IntegerMultiplicationImplicitCastToLong
C \* P \* Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
						image.getNumRows() + " != " +  N + " || " + image.getNumColumns() + " != " + C*H*W);
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, C * P * Q);
			PoolingType poolType = instOpcode.equalsIgnoreCase("maxpooling") ? PoolingType.MAX : PoolingType.AVG;
			LibMatrixCuDNN.pooling(ec.getGPUContext(0), getExtendedOpcode(), image, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
C\*P\*Q: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject dout = getMatrixInputForGPUInstruction(ec, _input2.getName());
			MatrixObject maxPoolOutput = _input3 != null ? getMatrixInputForGPUInstruction(ec, _input3.getName()) : null;
			if(dout.getNumRows() != N || dout.getNumColumns() != C*P*Q) 
				throw new DMLRuntimeException("Incorrect dimensions for dout in maxpooling_backward");
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
```

### IntegerMultiplicationImplicitCastToLong
C\*H\*W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(dout.getNumRows() != N || dout.getNumColumns() != C*P*Q) 
				throw new DMLRuntimeException("Incorrect dimensions for dout in maxpooling_backward");
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in maxpooling_backward: " + 
						image.getNumRows() + " != " +  N + " || " + image.getNumColumns() + " != " + K*P*Q);
```

### IntegerMultiplicationImplicitCastToLong
C \* H \* W: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
						image.getNumRows() + " != " +  N + " || " + image.getNumColumns() + " != " + K*P*Q);
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, C * H * W);
			PoolingType poolType = instOpcode.equalsIgnoreCase("maxpooling_backward") ? PoolingType.MAX : PoolingType.AVG;
			LibMatrixCuDNN.poolingBackward(ec.getGPUContext(0), getExtendedOpcode(), image, dout, maxPoolOutput, out, N, C, H, W,
```

### IntegerMultiplicationImplicitCastToLong
rows\*cols: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		String instName = getExtendedOpcode();
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("update_nesterov_x", 
				ExecutionConfig.getConfigForSimpleVectorOperations(LibMatrixCUDA.toInt(rows*cols)),
				LibMatrixCUDA.getDensePointer(gCtx, input, instName), 
				LibMatrixCUDA.getDensePointer(gCtx, v, instName),
```

### IntegerMultiplicationImplicitCastToLong
NUM_ROWBLOCKS\*mc1.getBlocksize(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
			//create broadcast for rdd partition
			JavaPairRDD<MatrixIndexes,MatrixBlock> rdd = in1
					.filter(new IsBlockInRange(i+1, i+NUM_ROWBLOCKS*mc1.getBlocksize(), 1, mc1.getCols(), mc1))
					.mapToPair(new PMapMMRebaseBlocksFunction(i/mc1.getBlocksize()));
			
```

### IntegerMultiplicationImplicitCastToLong
NUM_ROWBLOCKS\*mc1.getBlocksize(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
					.mapToPair(new PMapMMRebaseBlocksFunction(i/mc1.getBlocksize()));
			
			int rlen = (int)Math.min(mc1.getRows()-i, NUM_ROWBLOCKS*mc1.getBlocksize());
			PartitionedBlock<MatrixBlock> pmb = SparkExecutionContext.toPartitionedMatrixBlock(rdd, rlen, (int)mc1.getCols(), mc1.getBlocksize(), -1L);
			Broadcast<PartitionedBlock<MatrixBlock>> bpmb = sec.getSparkContext().broadcast(pmb);
```

### IntegerMultiplicationImplicitCastToLong
blocksize \* tDims.length: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		long[] longDims = new long[tDims.length];
		long totalSize = 1;
		long hdfsBlkSize = blocksize * tDims.length;
		for (int i = 0; i < tDims.length; i++) {
			longDims[i] = tDims[i];
```

### IntegerMultiplicationImplicitCastToLong
(i+1)\*_blen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
			MatrixBlock out = new MatrixBlock(1,(int)_clen, tmpBlks[0].isInSparseFormat());
			for( int i=0; i<_ncblks; i++ )
				out.copy(0, 0, i*_blen, (int)Math.min((i+1)*_blen, _clen)-1, tmpBlks[i], false);
			out.recomputeNonZeros();
			//output row block
```

### IntegerMultiplicationImplicitCastToLong
cix\*_blen: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
				for( int cix=1, pix=_isVector?0:off; cix<=ncblks; cix++ ) {
					int lclen = UtilFunctions.computeBlockSize(_clen, cix, _blen);
					int cu = (int) Math.min(_clen, cix*_blen) + (_isVector?0:off);
					//allocate sparse row once (avoid re-allocations)
					if( mb[cix-1].isInSparseFormat() ) {
```

### IntegerMultiplicationImplicitCastToLong
_data.getNumRows()\*_data.getNumColumns(): integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/FrameObject.java`
#### Snippet
```java
		DataCharacteristics dc = _metaData.getDataCharacteristics();
		dc.setDimension( _data.getNumRows(),_data.getNumColumns() );
		dc.setNonZeros(_data.getNumRows()*_data.getNumColumns());
		
		//update schema information
```

### IntegerMultiplicationImplicitCastToLong
2\*cpk: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			//MR if local par cannot be exploited due to mem constraints (this implies that we work on large data)
			//(the factor of 2 is to account for hyper-threading and in order prevent too eager remote parfor)
			if( 2*cpk < _lk && 2*cpk < _N && 2*cpk < _rk ) //incl conditional partitioning
			{
				n.setExecType( REMOTE ); //remote parfor
```

### IntegerMultiplicationImplicitCastToLong
(rblkID - 1) \* OptimizerUtils.DEFAULT_BLOCKSIZE: integer multiplication implicitly cast to long
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionSparkScheme.java`
#### Snippet
```java
			int workerID = (int) indicator.getValue((int) r, 0);
			MatrixBlock rowMB = ParamservUtils.sliceMatrixBlock(mb, r + 1, r + 1);
			long shiftedPosition = r + (rblkID - 1) * OptimizerUtils.DEFAULT_BLOCKSIZE;
			return new Tuple2<>(workerID, new Tuple2<>(shiftedPosition, rowMB));
		}).collect(Collectors.toList());
```

## RuleId[id=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import java.util.Iterator;`
in `src/main/java/org/apache/sysds/parser/dml/DmlParser.java`
#### Snippet
```java
import org.antlr.v4.runtime.tree.*;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.FederatedPSScheme;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.common.Types.ExecType;
import org.apache.sysds.hops.recompile.Recompiler;
import org.apache.sysds.parser.Statement.FederatedPSScheme;
import org.apache.sysds.parser.Statement.PSFrequency;
import org.apache.sysds.parser.Statement.PSModeType;
```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.PSFrequency;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.hops.recompile.Recompiler;
import org.apache.sysds.parser.Statement.FederatedPSScheme;
import org.apache.sysds.parser.Statement.PSFrequency;
import org.apache.sysds.parser.Statement.PSModeType;
import org.apache.sysds.parser.Statement.PSRuntimeBalancing;
```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.PSModeType;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.parser.Statement.FederatedPSScheme;
import org.apache.sysds.parser.Statement.PSFrequency;
import org.apache.sysds.parser.Statement.PSModeType;
import org.apache.sysds.parser.Statement.PSRuntimeBalancing;
import org.apache.sysds.parser.Statement.PSScheme;
```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.PSRuntimeBalancing;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.parser.Statement.PSFrequency;
import org.apache.sysds.parser.Statement.PSModeType;
import org.apache.sysds.parser.Statement.PSRuntimeBalancing;
import org.apache.sysds.parser.Statement.PSScheme;
import org.apache.sysds.parser.Statement.PSUpdateType;
```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.PSScheme;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.parser.Statement.PSModeType;
import org.apache.sysds.parser.Statement.PSRuntimeBalancing;
import org.apache.sysds.parser.Statement.PSScheme;
import org.apache.sysds.parser.Statement.PSUpdateType;
import org.apache.sysds.runtime.DMLRuntimeException;
```

### UNUSED_IMPORT
Unused import `import org.apache.sysds.parser.Statement.PSUpdateType;`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java
import org.apache.sysds.parser.Statement.PSRuntimeBalancing;
import org.apache.sysds.parser.Statement.PSScheme;
import org.apache.sysds.parser.Statement.PSUpdateType;
import org.apache.sysds.runtime.DMLRuntimeException;
import org.apache.sysds.runtime.controlprogram.LocalVariableMap;
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `nodesWithPrefetch` are updated, but never queried
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddPrefetchLop.java`
#### Snippet
```java
			return List.of(sb);

		ArrayList<Lop> nodesWithPrefetch = new ArrayList<>();
		//Find the Spark nodes with all CP outputs
		for (Lop l : lops) {
```

### MismatchedCollectionQueryUpdate
Contents of collection `nodesWithBroadcast` are updated, but never queried
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddBroadcastLop.java`
#### Snippet
```java
			return List.of(sb);

		ArrayList<Lop> nodesWithBroadcast = new ArrayList<>();
		for (Lop l : lops) {
			nodesWithBroadcast.add(l);
```

### MismatchedCollectionQueryUpdate
Contents of collection `columnMetadata` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameLibRemoveEmpty.java`
#### Snippet
```java
	private FrameBlock removeEmptyColumns() {

		List<ColumnMetadata> columnMetadata = new ArrayList<>();

		final String[] colNames = in.getColumnNames(false);
```

### MismatchedCollectionQueryUpdate
Contents of collection `_CUDA_op_src` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java

	private static ConcurrentHashMap<String, Integer> _CUDA_op_IDs = new ConcurrentHashMap<>();
	private static ConcurrentHashMap<Integer, String> _CUDA_op_src = new ConcurrentHashMap<>();

	//javac-specific working directory for src/class files
```

### MismatchedCollectionQueryUpdate
Contents of collection `newMap` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/instructions/fed/VariableFEDInstruction.java`
#### Snippet
```java
		MatrixObject out = ec.getMatrixObject(_in.getOutput());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr1.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
```

### MismatchedCollectionQueryUpdate
Contents of collection `newMap` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/instructions/fed/VariableFEDInstruction.java`
#### Snippet
```java
		out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr1.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Map.Entry<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
```

### MismatchedCollectionQueryUpdate
Contents of collection `newMap` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CastFEDInstruction.java`
#### Snippet
```java
		out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr2.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Map.Entry<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
```

### MismatchedCollectionQueryUpdate
Contents of collection `newMap` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CastFEDInstruction.java`
#### Snippet
```java
		MatrixObject out = ec.getMatrixObject(output);
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr1.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
```

### MismatchedCollectionQueryUpdate
Contents of collection `frC` are queried, but never updated
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSigmoidFEDInstruction.java`
#### Snippet
```java
				output, new CPOperand[] {input1, input2, input3}, varNewIn);

			ArrayList<FederatedRequest> frC = new ArrayList<>();
			FederatedRequest[] frAll = ArrayUtils.addAll(new FederatedRequest[]{frB, frComp},
				frC.toArray(new FederatedRequest[0]));
```

### MismatchedCollectionQueryUpdate
Contents of collection `frC` are queried, but never updated
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWUMMFEDInstruction.java`
#### Snippet
```java
				new CPOperand[]{input1, input2, input3}, varNewIn);

			ArrayList<FederatedRequest> frC = new ArrayList<>();
			FederatedRequest[] frAll = ArrayUtils.addAll(new FederatedRequest[]{frB, frComp},
				frC.toArray(new FederatedRequest[0]));
```

### MismatchedCollectionQueryUpdate
Contents of collection `kernels` are queried, but never updated
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java

	private final static String ptxFileName = "/cuda/kernels/SystemDS.ptx";
	private HashMap<String, CUfunction> kernels = new HashMap<>();
	private CUmodule module;

```

### MismatchedCollectionQueryUpdate
Contents of collection `tmp` are updated, but never queried
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
			VariableCPInstruction.prepareRemoveInstruction(id).toString());
		request.setTID(tid);
		List<Future<FederatedResponse>> tmp = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> fd : _fedMap)
			tmp.add(fd.getValue().executeFederatedOperation(request));
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`val >>> 0` can be replaced with 'val'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		ba[ off+5 ] = (byte)((val >>> 16) & 0xFF);
		ba[ off+6 ] = (byte)((val >>>  8) & 0xFF);
		ba[ off+7 ] = (byte)((val >>>  0) & 0xFF);
	}
	
```

### PointlessBitwiseExpression
`val >>> 0` can be replaced with 'val'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		ba[ off+1 ] = (byte)((val >>> 16) & 0xFF);
		ba[ off+2 ] = (byte)((val >>>  8) & 0xFF);
		ba[ off+3 ] = (byte)((val >>>  0) & 0xFF);
	}

```

### PointlessBitwiseExpression
`(long)(ba[off+7] & 0xFF) << 0` can be replaced with '(long)(ba\[off+7\] \& 0xFF)'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
			+  ((long)(ba[off+5] & 0xFF) << 16)
			+  ((long)(ba[off+6] & 0xFF) <<  8)
			+  ((long)(ba[off+7] & 0xFF) <<  0);
	}
	
```

### PointlessBitwiseExpression
`(ba[off+1] & 0xFF) << 0` can be replaced with '(ba\[off+1\] \& 0xFF)'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		//shift and add 2 bytes into single int
		return ((ba[off+0] & 0xFF) << 8)
			+  ((ba[off+1] & 0xFF) << 0);
	}

```

### PointlessBitwiseExpression
`val >>> 0` can be replaced with 'val'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		//shift and mask out 2 bytes
		ba[ off+0 ] = (byte)((val >>>  8) & 0xFF);
		ba[ off+1 ] = (byte)((val >>>  0) & 0xFF);
	}

```

### PointlessBitwiseExpression
`(ba[off+3] & 0xFF) << 0` can be replaced with '(ba\[off+3\] \& 0xFF)'
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
			+  ((ba[off+1] & 0xFF) << 16)
			+  ((ba[off+2] & 0xFF) <<  8)
			+  ((ba[off+3] & 0xFF) <<  0);
	}

```

### PointlessBitwiseExpression
`c >> 0` can be replaced with 'c'
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataOutputStream.java`
#### Snippet
```java
				_buff[_count++] = (byte) (0xE0 | ((c >> 12) & 0x0F));
				_buff[_count++] = (byte) (0x80 | ((c >>  6) & 0x3F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
			else { //2 byte range and null
```

### PointlessBitwiseExpression
`c >> 0` can be replaced with 'c'
in `src/main/java/org/apache/sysds/runtime/util/FastBufferedDataOutputStream.java`
#### Snippet
```java
			else { //2 byte range and null
				_buff[_count++] = (byte) (0xC0 | ((c >>  6) & 0x1F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
		}
```

### PointlessBitwiseExpression
`(-1L) ^ ((-1L) << (_size - 64))` can be replaced with '\~((-1L) \<\< (_size - 64))'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToBit.java`
#### Snippet
```java
			if(_size != 64 && _data.get(_size - 1)) {
				// make last run go over into the last elements.
				long mask = ~((-1L) ^ ((-1L) << (_size - 64)));
				l |= mask;
			}
```

### PointlessBitwiseExpression
`(-1L) ^ ((-1L) << (_size - 64))` can be replaced with '\~((-1L) \<\< (_size - 64))'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToBit.java`
#### Snippet
```java
			// handle if we are not aligned with 64.
			l = (_size % 64 != 0 && _data.get(_size - 1)) ? _longs[idx] |
				(~((-1L) ^ ((-1L) << (_size - 64)))) : _longs[idx];
			shift1 = (l << 1) | ((_longs[idx - 1] & lastMask) >>> 63);

```

### PointlessBitwiseExpression
`c >> 0` can be replaced with 'c'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java
				_buff[_count++] = (byte) (0xE0 | ((c >> 12) & 0x0F));
				_buff[_count++] = (byte) (0x80 | ((c >>  6) & 0x3F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
			else { //2 byte range and null
```

### PointlessBitwiseExpression
`c >> 0` can be replaced with 'c'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java
			else { //2 byte range and null
				_buff[_count++] = (byte) (0xC0 | ((c >>  6) & 0x1F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
		}
```

## RuleId[id=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java

	private FederatedResponse executeCommand(FederatedRequest request, ExecutionContextMap ecm, EventStageModel eventStage)
		throws DMLPrivacyException, FederatedWorkerHandlerException, Exception {
		final RequestType method = request.getType();
		FederatedResponse result = null;
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java

	private FederatedResponse executeCommand(FederatedRequest request, ExecutionContextMap ecm, EventStageModel eventStage)
		throws DMLPrivacyException, FederatedWorkerHandlerException, Exception {
		final RequestType method = request.getType();
		FederatedResponse result = null;
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java

	private FederatedResponse createResponse(FederatedRequest[] requests, String remoteHost)
		throws DMLPrivacyException, FederatedWorkerHandlerException, Exception {
			
		FederatedResponse response = null; // last response
```

### DuplicateThrows
There is a more general exception, 'java.lang.Exception', in the throws list already.
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java

	private FederatedResponse createResponse(FederatedRequest[] requests, String remoteHost)
		throws DMLPrivacyException, FederatedWorkerHandlerException, Exception {
			
		FederatedResponse response = null; // last response
```

## RuleId[id=StringConcatenationInLoops]
### StringConcatenationInLoops
String concatenation `+=` in loop
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeMultiAgg.java`
#### Snippet
```java
		String aggList = "";
		for( AggOp aggOp : _aggOps ) {
			aggList += !aggList.isEmpty() ? "," : "";
			aggList += "AggOp."+aggOp.name();
		}
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5BufferBuilder.java`
#### Snippet
```java
			if(value % 2L != 0) {
				bits.set(index);
				a += "1";
			}
			else
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5BufferBuilder.java`
#### Snippet
```java
			}
			else
				a += "0";
			++index;
			value = value >>> 1;
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			String key = "";
			for(int index : DEBUG_MEMORY_LEAK_STACKTRACE_DEPTH) {
				key += getCallerInfo(ptrInfo.stackTraceElements, index);
			}
			if(frequency.containsKey(key)) {
```

### StringConcatenationInLoops
String concatenation `+=` in loop
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
		String retVal = lines[0];
		for(int i = 1; i < lines.length-1; i++) {
			retVal += "|" + lines[i];
		}
		String twoSpaces = "  ";
```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `_out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/jmlc/ResultVariables.java`
#### Snippet
```java
public class ResultVariables 
{
	private HashMap<String, Data> _out = null;
	
	public ResultVariables() {
```

### UnusedAssignment
Variable `properties` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/ProjectInfo.java`
#### Snippet
```java
public class ProjectInfo {

	SortedMap<String, String> properties = null;
	static ProjectInfo projectInfo = null;

```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/jmlc/Connection.java`
#### Snippet
```java
		throws IOException
	{
		FrameBlock ret = null;
	
		//sanity check input format
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/jmlc/Connection.java`
#### Snippet
```java
		throws IOException
	{
		MatrixBlock ret = null;

		//sanity check input format
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/jmlc/Connection.java`
#### Snippet
```java
			
			//core script reading
			String tmp = null;
			while ((tmp = in.readLine()) != null) {
				sb.append( tmp );
```

### UnusedAssignment
Variable `rtprog` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/jmlc/Connection.java`
#### Snippet
```java
		
		//simplified compilation chain
		Program rtprog = null;
		try {
			//parsing
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
				
				//core script reading
				String tmp = null;
				while ((tmp = in.readLine()) != null) {
					sb.append( tmp );
```

### UnusedAssignment
Variable `dmlOptions` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
		ExplainType oldexplain  = EXPLAIN;     //keep old explain

		DMLOptions dmlOptions = null;
		
		try{
```

### UnusedAssignment
Variable `_bmap` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/CompilerConfig.java`
#### Snippet
```java
	public static boolean FLAG_PARREADWRITE_BINARY = true;
	
	private HashMap<ConfigType, Boolean> _bmap = null;
	private HashMap<ConfigType, Integer> _imap = null;
	
```

### UnusedAssignment
Variable `_imap` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/CompilerConfig.java`
#### Snippet
```java
	
	private HashMap<ConfigType, Boolean> _bmap = null;
	private HashMap<ConfigType, Integer> _imap = null;
	
	public CompilerConfig() {
```

### UnusedAssignment
Variable `_dmlconf` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/ConfigurationManager.java`
#### Snippet
```java

	/** Global DML configuration (read or defaults) */
	private static DMLConfig _dmlconf = null; 

	/** Local DML configuration for thread-local config updates */
```

### UnusedAssignment
Variable `_cconf` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/ConfigurationManager.java`
#### Snippet
```java

	/** Global compiler configuration (defaults) */
	private static CompilerConfig _cconf = null;

	/** Local compiler configuration for thead-local config updates */
```

### UnusedAssignment
Variable `_rJob` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/ConfigurationManager.java`
#### Snippet
```java

	/** Global cached job conf for read-only operations */
	private static JobConf _rJob = null; 

	/** Global DML configuration (read or defaults) */
```

### UnusedAssignment
Variable `val` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/LiteralOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		String val = null;
		switch (getValueType()) {
			case FP64:
```

### UnusedAssignment
Variable `l` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/LiteralOp.java`
#### Snippet
```java
		try 
		{
			Lop l = null;

			switch (getValueType()) {
```

### UnusedAssignment
Variable `ret` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/LiteralOp.java`
#### Snippet
```java
	protected double computeOutputMemEstimate( long dim1, long dim2, long nnz )
	{		
		double ret = 0;
		
		switch( getValueType() ) {
```

### UnusedAssignment
Variable `str` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
				sb.append(key);
				sb.append(": ");
				String str = null;
				if (object instanceof MatrixBlock) {
					MatrixBlock mb = (MatrixBlock) object;
```

### UnusedAssignment
Variable `minimumRecommendedSparkVersion` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	 */
	public static void verifySparkVersionSupported(SparkSession spark) {
		String minimumRecommendedSparkVersion = null;
		try {
			// If this is being called using the SystemDS jar file,
```

### UnusedAssignment
Variable `_memo` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/MemoTable.java`
#### Snippet
```java
public class MemoTable 
{
	private HashMap<Long, DataCharacteristics> _memo = null;
	
	public MemoTable() {
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/MemoTable.java`
#### Snippet
```java
			return null;
		
		DataCharacteristics ret = null;
			
		long dim1 = input.getDim1();
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	public synchronized String serializeDMLConfig() 
	{
		String ret = null;
		try {
			Transformer transformer = TransformerFactory.newInstance().newTransformer();
```

### UnusedAssignment
Variable `_defaultVals` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	
	//configuration default values
	private static HashMap<String, String> _defaultVals = null;

	private String _fileName = null;
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	
	public static DMLConfig parseDMLConfig( String content ) {
		DMLConfig ret = null;
		try {
			DocumentBuilder builder = DocumentBuilderFactory.newInstance().newDocumentBuilder();
```

### UnusedAssignment
Variable `domTree` initializer `null` is redundant
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
		try {
			DocumentBuilder builder = DocumentBuilderFactory.newInstance().newDocumentBuilder();
			Document domTree = null;
			domTree = builder.parse( new ByteArrayInputStream(content.getBytes("utf-8")) );
			Element root = domTree.getDocumentElement();
```

### UnusedAssignment
Variable `config` initializer `new DMLConfig()` is redundant
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	{
		// Always start with the internal defaults
		DMLConfig config = new DMLConfig();

		// Merge in any specified or default configs if available
```

### UnusedAssignment
Variable `sb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		List<String> list = new ArrayList<>();

		StringBuilder sb = null;
		for (int i = 0; i < rows; i++) {
			sb = new StringBuilder();
```

### UnusedAssignment
The value `new StringBuilder()` assigned to `sb` is never used
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		StringBuilder sb = null;
		for (int i = 0; i < rows; i++) {
			sb = new StringBuilder();
			for (int j = 0; j < cols; j++) {
				if (fb.get(i, j) != null) {
```

### UnusedAssignment
Variable `sb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
			if (mb.isInSparseFormat()) {
				Iterator<IJV> iter = mb.getSparseBlockIterator();
				StringBuilder sb = null;
				while (iter.hasNext()) {
					IJV cell = iter.next();
```

### UnusedAssignment
Variable `sb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
				}
			} else {
				StringBuilder sb = null;
				for (int i = 0; i < rows; i++) {
					sb = new StringBuilder();
```

### UnusedAssignment
The value `new StringBuilder()` assigned to `sb` is never used
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
				StringBuilder sb = null;
				for (int i = 0; i < rows; i++) {
					sb = new StringBuilder();
					for (int j = 0; j < cols; j++) {
						sb = new StringBuilder();
```

### UnusedAssignment
Variable `mf` initializer `null` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java

		StructField[] fields = schema.fields();
		MatrixFormat mf = null;
		if (hasID) {
			if (fields[1].dataType() instanceof VectorUDT) {
```

### UnusedAssignment
Variable `sparsity` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/UnaryOp.java`
#### Snippet
```java
	protected double computeOutputMemEstimate( long dim1, long dim2, long nnz )
	{
		double sparsity = -1;
		if (isGPUEnabled()) {
			sparsity = 1.0; // Output is always dense (for now) on the GPU
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/UnaryOp.java`
#### Snippet
```java
		try {
			Hop input = getInput().get(0);
			Lop ret = null;
			switch(_op){
				case COMPRESS:
```

### UnusedAssignment
Variable `numberOfChars` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}
				
				long numberOfChars = -1;
				
				if (sparsePrint){
```

### UnusedAssignment
Variable `lDim1` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}

				long lDim1 = 0, lDim2 = 0;
				if( margin.equals("rows") ) {
					lDim1 = (dcSelect == null ||  !dcSelect.nnzKnown() ) ? dc.getRows(): dcSelect.getNonZeros();
```

### UnusedAssignment
Variable `lDim2` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}

				long lDim1 = 0, lDim2 = 0;
				if( margin.equals("rows") ) {
					lDim1 = (dcSelect == null ||  !dcSelect.nnzKnown() ) ? dc.getRows(): dcSelect.getNonZeros();
```

### UnusedAssignment
Variable `emptyInd` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			//in order to reuse of operator selection decisions
			BinaryOp ppred0 = null;
			Hop emptyInd = null;
			
			if(selectHop == null) {
```

### UnusedAssignment
Variable `l` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/DataOp.java`
#### Snippet
```java

		ExecType et = optFindExecType();
		Lop l = null;
		
		// construct lops for all input parameters
```

### UnusedAssignment
Variable `agg1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggUnaryOp.java`
#### Snippet
```java
			if ( et == ExecType.CP || et == ExecType.GPU || et == ExecType.FED )
			{
				Lop agg1 = null; 
				if( isTernaryAggregateRewriteApplicable() ) {
					agg1 = constructLopsTernaryAggregateRewrite(et);
```

### UnusedAssignment
Variable `sparsity` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/AggUnaryOp.java`
#### Snippet
```java
	protected double computeOutputMemEstimate( long dim1, long dim2, long nnz )
	{
		double sparsity = -1;
		if (isGPUEnabled()) {
			// The GPU version (for the time being) only does dense outputs
```

### UnusedAssignment
Variable `ret` initializer `new MatrixCharacteristics()` is redundant
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
	{
		// [numRows, numCols, NNZ] 
		DataCharacteristics ret = new MatrixCharacteristics();
		
		if(op == OpOpDnn.BIASADD || op == OpOpDnn.BIASMULT || op == OpOpDnn.BATCH_NORM2D_TEST ||
```

### UnusedAssignment
Variable `lhsInputLop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		// ---------------------------------------------------------------
		// Deal with fused operators and contruct lhsInputLop/optionalRhsInputLop
		Lop lhsInputLop = null; Lop optionalRhsInputLop = null;
		ArrayList<Hop> inputsOfPotentiallyFusedOp = inputs;
		
```

### UnusedAssignment
Variable `compressionInstruction` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
			ExecType et = getExecutionModeForCompression();

			Lop compressionInstruction = null;
			try{
				if( requiresCompression() ){
```

### UnusedAssignment
Variable `ret` initializer `Double.MAX_VALUE` is redundant
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java

	public double computeBoundsInformation( Hop input ) {
		double ret = Double.MAX_VALUE;
		try {
			ret = OptimizerUtils.rEvalSimpleDoubleExpression(input, new HashMap<Long, Double>());
```

### UnusedAssignment
Variable `reblock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
		{
			Lop input = getLops();
			Lop reblock = null;
			
			try
```

### UnusedAssignment
Variable `offset` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java

	public static Lop createOffsetLop( Hop hop, boolean repCols ) {
		Lop offset = null;
		if( ConfigurationManager.isDynamicRecompilation() && hop.dimsKnown() ) {
			// If dynamic recompilation is enabled and dims are known, we can replace the ncol with 
```

### UnusedAssignment
Variable `ret` initializer `Double.MAX_VALUE` is redundant
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
	
	public static double computeBoundsInformation( Hop input, LocalVariableMap vars, HashMap<Long, Double> memo ) {
		double ret = Double.MAX_VALUE;
		try {
			ret = OptimizerUtils.rEvalSimpleDoubleExpression(input, memo, vars);
```

### UnusedAssignment
Variable `pick_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
		// For INTERQUANTILE: 2nd argument is always a scalar

		PickByCount.OperationTypes pick_op = null;
		if(op == OpOp2.QUANTILE)
			pick_op = PickByCount.OperationTypes.VALUEPICK;
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
	public static Lop constructSPAppendLop( Hop left, Hop right, DataType dt, ValueType vt, boolean cbind, Hop current ) 
	{
		Lop ret = null;
		
		Lop offset = createOffsetLop( left, cbind ); //offset 1st input
```

### UnusedAssignment
Variable `append` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
		}
		
		Lop append = null;
		if( dt1==DataType.MATRIX || dt1==DataType.FRAME )
		{
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java


			Lop tmp = null;
			if( ot != null ) {
				tmp = new Unary(getInput(0).constructLops(), getInput(1).constructLops(),
```

### UnusedAssignment
Variable `binary` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
			else if ( et == ExecType.CP || et == ExecType.GPU || et == ExecType.FED )
			{
				Lop binary = null;
				
				boolean isLeftXGt = (getInput().get(0) instanceof BinaryOp) && ((BinaryOp) getInput().get(0)).getOp() == OpOp2.GREATER;
```

### UnusedAssignment
Variable `binary` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
					mbin = FORCED_BINARY_METHOD;
				
				Lop  binary = null;
				if( mbin == MMBinaryMethod.MR_BINARY_UAGG_CHAIN ) {
					AggUnaryOp uRight = (AggUnaryOp)right;
```

### UnusedAssignment
Variable `ret` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
	protected double computeOutputMemEstimate( long dim1, long dim2, long nnz )
	{		
		double ret = 0;
		

```

### UnusedAssignment
Variable `lnnz1` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
			{
				case MAP:
					long ldim1, ldim2, lnnz1 = -1;
					ldim1 = (input1.rowsKnown()) ? input1.getDim1() : ((input2.getDim1()>=1)?input2.getDim1():-1);
					ldim2 = (input1.colsKnown()) ? input1.getDim2() : ((input2.getDim2()>=1)?input2.getDim2():-1);
```

### UnusedAssignment
Variable `subSize` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
			// unless second input is single cell / row vector / column vector
			// use worst-case memory estimate for second input (it cannot be larger than overall matrix)
			double subSize = -1;	
			if( _rowLowerEqualsUpper && _colLowerEqualsUpper )
				subSize = OptimizerUtils.estimateSize(1, 1);
```

### UnusedAssignment
Variable `_inmem` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/cost/VarStats.java`
#### Snippet
```java
{
	DataCharacteristics _dc;
	boolean _inmem = false;
	
	public VarStats( long rlen, long clen, int blen, long nnz, boolean inmem ) {
```

### UnusedAssignment
Variable `ret` initializer `true` is redundant
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
	public static boolean isValidCPMatrixSize( long rows, long cols, double sparsity )
	{
		boolean ret = true;
		
		//the current CP runtime implementation has several limitations:
```

### UnusedAssignment
Variable `vs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
		{
			Data dat = vars.get(varname);
			VarStats vs = null;
			if( dat instanceof MatrixObject ) //matrix
			{
```

### UnusedAssignment
Variable `rows` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorLayeredGraph.java`
#### Snippet
```java
			
			//step 1: create node arrays for rows/cols
			Node[] rows = null, cols = null;
			if( _nodes.size() == 0 ) {
				rows = new Node[m];
```

### UnusedAssignment
Variable `cols` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorLayeredGraph.java`
#### Snippet
```java
			
			//step 1: create node arrays for rows/cols
			Node[] rows = null, cols = null;
			if( _nodes.size() == 0 ) {
				rows = new Node[m];
```

### UnusedAssignment
Variable `ret` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		double mbytes = bytes / (1024*1024);
		
		double ret = -1;
		
		FileFormat fmt = FileFormat.safeValueOf(format);
```

### UnusedAssignment
Variable `ret` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		double mbytes = bytes / (1024*1024);
		
		double ret = -1;
		if( sparse )
			ret = mbytes / DEFAULT_MBS_HDFSWRITE_BINARYBLOCK_SPARSE;
```

### UnusedAssignment
Variable `sortCosts` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
					if( optype.equals("sort") ){
						//note: mergesort since comparator used
						double sortCosts = 0;
						if( onlyLeft )
							sortCosts = DEFAULT_NFLOP_CP * d1m + d1m;
```

### UnusedAssignment
Variable `s` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorSampleRa.java`
#### Snippet
```java
			Integer[] y = Ci.stream().sorted(Comparator.comparing(a -> h2[a])).toArray(Integer[]::new);

			int s = 0;
			int sHat = 0;
			for(int t=0; t<y.length; t++) {
```

### UnusedAssignment
Variable `matmultCP` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
	private void constructCPLopsMM(ExecType et) 
	{
		Lop matmultCP = null;
		String cla = ConfigurationManager.getDMLConfig().getTextValue("sysds.compressed.linalg");
		if (et == ExecType.GPU) {
```

### UnusedAssignment
Variable `footprint` initializer `0` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		double m3m2OutSize = OptimizerUtils.estimateSize(m1_rows, Math.min(m2_cols, m2_blen)); //output per m2 block if m1 in cache
	
		double footprint = 0;
		if( pmm )
		{
```

### UnusedAssignment
Variable `mapmult` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
	private void constructSparkLopsMapMM(MMultMethod method)
	{
		Lop mapmult = null;
		if( isLeftTransposeRewriteApplicable(false) ) 
		{
```

### UnusedAssignment
Variable `mapmmchain` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
	private void constructCPLopsMMChain( ChainType chain )
	{
		MapMultChain mapmmchain = null;
		if( chain == ChainType.XtXv ) {
			Hop hX = getInput().get(0).getInput().get(0);
```

### UnusedAssignment
Variable `mapmmchain` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java

	private void constructSparkLopsMapMMChain(ChainType chain) {
		MapMultChain mapmmchain = null;
		if( chain == ChainType.XtXv ) {
			Hop hX = getInput().get(0).getInput().get(0);
```

### UnusedAssignment
Variable `nrow` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		
		Lop lpmInput = pmInput.constructLops();
		Hop nrow = null;
		double mestPM = OptimizerUtils.estimateSize(pmInput.getDim1(), 1);
		ExecType etVect = (mestPM>OptimizerUtils.getLocalMemBudget())?ExecType.SPARK:ExecType.CP;
```

### UnusedAssignment
The value `GeneratorAPI.JAVA` assigned to `generator` is never used
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
		if(DMLScript.getGlobalExecMode() == ExecMode.SPARK) {
			LOG.warn("Not loading native codegen library in SPARK execution mode!\n");
			generator = GeneratorAPI.JAVA;
			return;
		}
```

### UnusedAssignment
Variable `_root` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
{
	private HashMap<Pair<Long,Long>,NodeLink> _matPoints = null;
	private NodeLink _root = null;
	
	private InterestingPoint[] _searchSpace;
```

### UnusedAssignment
Variable `_matPoints` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
public class ReachabilityGraph 
{
	private HashMap<Pair<Long,Long>,NodeLink> _matPoints = null;
	private NodeLink _root = null;
	
```

### UnusedAssignment
Variable `_outputs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeMultiAgg.java`
#### Snippet
```java
	private static final String TEMPLATE_OUT_MAX   = "    c[%IX%] = Math.max(c[%IX%], %IN%);\n";
	
	private ArrayList<CNode> _outputs = null;
	private ArrayList<AggOp> _aggOps = null;
	private ArrayList<Hop> _roots = null;
```

### UnusedAssignment
Variable `_generated` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNode.java`
#### Snippet
```java
	protected CNode _output = null; 
	protected boolean _visited = false;
	protected boolean _generated = false;
	protected String _genVar = null;
	protected long _rows = -1;
```

### UnusedAssignment
Variable `_inputs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNode.java`
#### Snippet
```java
	
	protected final long _ID; 
	protected ArrayList<CNode> _inputs = null; 
	protected CNode _output = null; 
	protected boolean _visited = false;
```

### UnusedAssignment
Variable `plan` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
	
	private static boolean[] getPlan(PartitionSignature pKey) {
		boolean[] plan = null;
		synchronized( _planCache ) {
			plan = _planCache.get(pKey);
```

### UnusedAssignment
Variable `_rmBranches` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriteStatus.java`
#### Snippet
```java
{
	//status of applied rewrites
	private boolean _rmBranches = false; //removed branches
	private int _blkSize = -1;
	private boolean _injectCheckpoints = false;
```

### UnusedAssignment
Variable `_injectCheckpoints` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriteStatus.java`
#### Snippet
```java
	private boolean _rmBranches = false; //removed branches
	private int _blkSize = -1;
	private boolean _injectCheckpoints = false;
	
	//current context
```

### UnusedAssignment
Variable `_inParforCtx` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriteStatus.java`
#### Snippet
```java
	
	//current context
	private boolean _inParforCtx = false;
	private LocalVariableMap _vars = null;
	
```

### UnusedAssignment
Variable `_checkCtx` initializer `false` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteInjectSparkLoopCheckpointing.java`
#### Snippet
```java
public class RewriteInjectSparkLoopCheckpointing extends StatementBlockRewriteRule
{
	private boolean _checkCtx = false;
	
	public RewriteInjectSparkLoopCheckpointing(boolean checkParForContext) {
```

### UnusedAssignment
Variable `_outputs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
{
	private HashSet<String> _inputs = null;
	private HashSet<String> _outputs = null;
	private HashMap<String,MetaData> _inputsMeta = null;
	
```

### UnusedAssignment
Variable `_inputs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
public class RewriteRemovePersistentReadWrite extends HopRewriteRule
{
	private HashSet<String> _inputs = null;
	private HashSet<String> _outputs = null;
	private HashMap<String,MetaData> _inputsMeta = null;
```

### UnusedAssignment
Variable `_inputsMeta` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
	private HashSet<String> _inputs = null;
	private HashSet<String> _outputs = null;
	private HashMap<String,MetaData> _inputsMeta = null;
	
	public RewriteRemovePersistentReadWrite( String[] in, String[] out ) {
```

### UnusedAssignment
Variable `lhsTimesOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		double expectedOneMinusMu = OptimizerUtils.rEvalSimpleDoubleExpression(getFirstInput(rhsTimesOp), new HashMap<>());
		Hop plusOp = rhsTimesOp.getParent().get(0); 
		Hop lhsTimesOp = null;
		if(plusOp.getInput().get(0) == rhsTimesOp) {
			lhsTimesOp = plusOp.getInput().get(1); 
```

### UnusedAssignment
Variable `lhsTimesOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		// Check (1-mu)*mean
		Hop plusOp = rhsTimesOp.getParent().get(0); 
		Hop lhsTimesOp = null;
		if(plusOp.getInput().get(0) == rhsTimesOp) {
			lhsTimesOp = plusOp.getInput().get(1); 
```

### UnusedAssignment
Variable `lhsTimesOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		double expectedOneMinusMu = OptimizerUtils.rEvalSimpleDoubleExpression(getFirstInput(rhsTimesOp), new HashMap<>());
		Hop plusOp = rhsTimesOp.getParent().get(0); 
		Hop lhsTimesOp = null;
		if(plusOp.getInput().get(0) == rhsTimesOp) {
			lhsTimesOp = plusOp.getInput().get(1); 
```

### UnusedAssignment
Variable `cdata2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
			//special case for cbind with zeros
			CNode cdata1 = tmp.get(hop.getInput().get(0).getHopID());
			CNode cdata2 = null;
			if( HopRewriteUtils.isDataGenOpWithConstantValue(hop.getInput().get(1)) ) {
				cdata2 = TemplateUtils.createCNodeData(HopRewriteUtils
```

### UnusedAssignment
Variable `_sbRuleSet` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
	
	private ArrayList<HopRewriteRule> _dagRuleSet = null;
	private ArrayList<StatementBlockRewriteRule> _sbRuleSet = null;

	static {
```

### UnusedAssignment
Variable `_dagRuleSet` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
	private static final boolean CHECK = false;
	
	private ArrayList<HopRewriteRule> _dagRuleSet = null;
	private ArrayList<StatementBlockRewriteRule> _sbRuleSet = null;

```

### UnusedAssignment
Variable `varname` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
						c, getFirstTransientWriteParent(c).getName()) : false;
					
					String varname = null;
					long rlen = c.getDim1();
					long clen = c.getDim2();
```

### UnusedAssignment
Variable `_cacheType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/GroupedAggregateM.java`
#### Snippet
```java
	
	private HashMap<String, Lop> _inputParams;
	private CacheType _cacheType = null;
		
	public GroupedAggregateM(HashMap<String, Lop> inputParameterLops, 
```

### UnusedAssignment
Variable `_wsigmoidType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSigmoid.java`
#### Snippet
```java
	}
	
	private WSigmoidType _wsigmoidType = null;
	
	public WeightedSigmoid(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, WSigmoidType wt, ExecType et) {
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/Append.java`
#### Snippet
```java
	public static final String OPCODE = "append";

	private boolean _cbind = true;
	private ExecType _et;

```

### UnusedAssignment
Variable `_tRewrite` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/MMZip.java`
#### Snippet
```java
public class MMZip extends Lop 
{
	private boolean _tRewrite = true;
	
	public MMZip(Lop input1, Lop input2, DataType dt, ValueType vt, boolean tRewrite, ExecType et) 
```

### UnusedAssignment
Variable `_numThreads` initializer `1` is redundant
in `src/main/java/org/apache/sysds/lops/MMTSJ.java`
#### Snippet
```java
	private MMTSJType _type = null;
	private boolean _multiPass = false;
	private int _numThreads = 1;

	public MMTSJ(Lop input1, DataType dt, ValueType vt, ExecType et, MMTSJType type) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/MMTSJ.java`
#### Snippet
```java
	}
	
	private MMTSJType _type = null;
	private boolean _multiPass = false;
	private int _numThreads = 1;
```

### UnusedAssignment
Variable `_multiPass` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/MMTSJ.java`
#### Snippet
```java
	
	private MMTSJType _type = null;
	private boolean _multiPass = false;
	private int _numThreads = 1;

```

### UnusedAssignment
Variable `value` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/LiteralReplacement.java`
#### Snippet
```java
	private static long getIntValueDataLiteral(Hop hop, LocalVariableMap vars)
	{
		long value = -1;
		
		try 
```

### UnusedAssignment
Variable `val` initializer `Double.MAX_VALUE` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/LiteralReplacement.java`
#### Snippet
```java
		
		//compute required unary aggregate 
		double val = Double.MAX_VALUE;
		switch( auop.getOp() ) {
			case SUM: 
```

### UnusedAssignment
Variable `lit` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/LiteralReplacement.java`
#### Snippet
```java
			{
				Hop c = hop.getInput().get(i);
				Hop lit = null;
				
				//conditional apply of literal replacements
```

### UnusedAssignment
Variable `gen` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
				{
					//create fused data gen operator
					DataGenOp gen = null;
					if( bop.getOp()==OpOp2.MULT )
						gen = HopRewriteUtils.copyDataGenOp(inputGen, sval, 0);
```

### UnusedAssignment
The value `true` assigned to `applied` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
						HopRewriteUtils.cleanupUnreferenced(bop, left);
						hi = binary;
						applied= true;
						
						LOG.debug("Applied fuseBinarySubDAGToUnaryOperation-max0b");
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSigmoidR.java`
#### Snippet
```java

	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
	public WeightedSigmoidR(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, WSigmoidType wt, boolean cacheU, boolean cacheV, ExecType et) {
```

### UnusedAssignment
Variable `_wsType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSigmoidR.java`
#### Snippet
```java
	public static final String OPCODE = "redwsigmoid";
	
	private WSigmoidType _wsType = null;

	private boolean _cacheU = false;
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSigmoidR.java`
#### Snippet
```java
	private WSigmoidType _wsType = null;

	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
```

### UnusedAssignment
Variable `UniqueLopID` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/LopProperties.java`
#### Snippet
```java
{
	// static variable to assign an unique ID to every lop that is created
	private static IDSequence UniqueLopID = null;
	
	static{
```

### UnusedAssignment
Variable `_weightsType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedDivMMR.java`
#### Snippet
```java
	public static final String OPCODE = "redwdivmm";
	
	private WDivMMType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedDivMMR.java`
#### Snippet
```java
	
	private WDivMMType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedDivMMR.java`
#### Snippet
```java
	private WDivMMType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
	public WeightedDivMMR(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WDivMMType wt, boolean cacheU, boolean cacheV, ExecType et)  {
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/AppendGAlignedSP.java`
#### Snippet
```java
	public static final String OPCODE = "galignedappend";

	private boolean _cbind = true;
	
	public AppendGAlignedSP(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, boolean cbind) 
```

### UnusedAssignment
Variable `_type` initializer `MMCJType.AGG` is redundant
in `src/main/java/org/apache/sysds/lops/MMCJ.java`
#### Snippet
```java
	//optional attribute for mr exec type
	private boolean _outputEmptyBlocks = true;
	private MMCJType _type = MMCJType.AGG;
	
	//optional attribute for spark exec type
```

### UnusedAssignment
Variable `_wcemmType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedCrossEntropy.java`
#### Snippet
```java
	}
	
	private WCeMMType _wcemmType = null;
	
	public WeightedCrossEntropy(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WCeMMType wt, ExecType et) {
```

### UnusedAssignment
Variable `op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/Ctable.java`
#### Snippet
```java
	public static OperationTypes getOperationType(String opcode)
	{
		OperationTypes op = null;
		
		if( opcode.equals("ctabletransform") )
```

### UnusedAssignment
Variable `_numThreads` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/lops/TernaryAggregate.java`
#### Snippet
```java
	
	//optional attribute for cp
	private int _numThreads = -1;

	public TernaryAggregate(Lop input1, Lop input2, Lop input3, AggOp aggOp, OpOp2 binOp, Direction direction, DataType dt, ValueType vt, ExecType et, int k ) 
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/UAggOuterChain.java`
#### Snippet
```java

	//outer operation
	private AggOp _uaggOp = null;
	private Direction _uaggDir = null;
	//inner operation
```

### UnusedAssignment
Variable `_uaggDir` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/UAggOuterChain.java`
#### Snippet
```java
	//outer operation
	private AggOp _uaggOp = null;
	private Direction _uaggDir = null;
	//inner operation
	private OpOp2 _binOp = null;
```

### UnusedAssignment
Variable `_binOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/UAggOuterChain.java`
#### Snippet
```java
	private Direction _uaggDir = null;
	//inner operation
	private OpOp2 _binOp = null;
	
	/**
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/AppendM.java`
#### Snippet
```java
	}
	
	private boolean _cbind = true;
	
	public AppendM(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, boolean cbind, boolean partitioned, ExecType et) 
```

### UnusedAssignment
Variable `_aggtype` initializer `SparkAggType.MULTI_BLOCK` is redundant
in `src/main/java/org/apache/sysds/lops/MapMult.java`
#### Snippet
```java
	
	//optional attribute for spark exec type
	private SparkAggType _aggtype = SparkAggType.MULTI_BLOCK;

	/**
```

### UnusedAssignment
Variable `_outputEmptyBlocks` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/MapMult.java`
#### Snippet
```java
	
	private CacheType _cacheType = null;
	private boolean _outputEmptyBlocks = true;
	
	//optional attribute for spark exec type
```

### UnusedAssignment
Variable `_cacheType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/MapMult.java`
#### Snippet
```java
	}
	
	private CacheType _cacheType = null;
	private boolean _outputEmptyBlocks = true;
	
```

### UnusedAssignment
Variable `_weightsType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedDivMM.java`
#### Snippet
```java
	}
	
	private WDivMMType _weightsType = null;
	
	public WeightedDivMM(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WDivMMType wt, ExecType et) {
```

### UnusedAssignment
Variable `_vectorType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/BinaryM.java`
#### Snippet
```java
	
	private OpOp2 _operation;
	private VectorType _vectorType = null; 
	
	/**
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedCrossEntropyR.java`
#### Snippet
```java
	private WCeMMType _wcemmType = null;

	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedCrossEntropyR.java`
#### Snippet
```java

	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
	public WeightedCrossEntropyR(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WCeMMType wt, boolean cacheU, boolean cacheV, ExecType et) {
```

### UnusedAssignment
Variable `_wcemmType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedCrossEntropyR.java`
#### Snippet
```java
	public static final String OPCODE = "redwcemm";
	
	private WCeMMType _wcemmType = null;

	private boolean _cacheU = false;
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/AppendR.java`
#### Snippet
```java
	public static final String OPCODE = "rappend";
	
	private boolean _cbind = true;
	
	public AppendR(Lop input1, Lop input2, DataType dt, ValueType vt, boolean cbind, ExecType et) 
```

### UnusedAssignment
Variable `lps` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	protected OutputParameters outParams = null;

	protected LopProperties lps = null;

	/**
```

### UnusedAssignment
Variable `outParams` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	 */

	protected OutputParameters outParams = null;

	protected LopProperties lps = null;
```

### UnusedAssignment
Variable `_chainType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/MapMultChain.java`
#### Snippet
```java
	}
	
	private ChainType _chainType = null;
	private int _numThreads = 1;
	
```

### UnusedAssignment
Variable `_weightsType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSquaredLoss.java`
#### Snippet
```java
	}
	
	private WeightsType _weightsType = null;
	
	public WeightedSquaredLoss(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WeightsType wt, ExecType et) {
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	public static ArrayList<Instruction> recompileHopsDagInstructions( StatementBlock sb, ArrayList<Hop> hops ) 
	{
		ArrayList<Instruction> newInst = null;

		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `tmp` initializer `bpb.getInstructions()` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			//would be invalid with permutation matrix mult across multiple dags)
			if( sb != null ) {
				ArrayList<Instruction> tmp = bpb.getInstructions();
				tmp = Recompiler.recompileHopsDag2Forced(sb, sb.getHops(), tid, et);
				bpb.setInstructions( tmp );
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			RecompileStatus status, boolean inplace, boolean replaceLit, long tid ) 
	{
		ArrayList<Instruction> newInst = null;

		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			ExecutionContext ec, RecompileStatus status, boolean inplace, boolean replaceLit, long tid ) 
	{
		ArrayList<Instruction> newInst = null;

		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `tmp` initializer `bpb.getInstructions()` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			StatementBlock sb = pb.getStatementBlock();
			BasicProgramBlock bpb = (BasicProgramBlock) pb;
			ArrayList<Instruction> tmp = bpb.getInstructions();
			if( sb == null ) 
				return;
```

### UnusedAssignment
Variable `_uop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMM.java`
#### Snippet
```java
	
	private WUMMType _wummType = null;
	private OpOp1 _uop = null;
	private int _numThreads = 1;
	
```

### UnusedAssignment
Variable `dat1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			Data ifVar = callVarsIf.get(varname);
			Data elseVar = callVarsElse.get(varname);
			Data dat1 = null, dat2 = null;
			
			if( ifVar!=null && elseVar!=null ){ // both branches exists
```

### UnusedAssignment
Variable `_wummType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMM.java`
#### Snippet
```java
	}
	
	private WUMMType _wummType = null;
	private OpOp1 _uop = null;
	private int _numThreads = 1;
```

### UnusedAssignment
Variable `dat2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			Data ifVar = callVarsIf.get(varname);
			Data elseVar = callVarsElse.get(varname);
			Data dat1 = null, dat2 = null;
			
			if( ifVar!=null && elseVar!=null ){ // both branches exists
```

### UnusedAssignment
Variable `dat1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			DataCharacteristics ifVar = callStatusIf.getTWriteStats().get(varname);
			DataCharacteristics elseVar = callStatusElse.getTWriteStats().get(varname);
			DataCharacteristics dat1 = null, dat2 = null;
			
			if( ifVar!=null && elseVar!=null ){ // both branches exists
```

### UnusedAssignment
Variable `dat2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			DataCharacteristics ifVar = callStatusIf.getTWriteStats().get(varname);
			DataCharacteristics elseVar = callStatusElse.getTWriteStats().get(varname);
			DataCharacteristics dat1 = null, dat2 = null;
			
			if( ifVar!=null && elseVar!=null ){ // both branches exists
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	public static ArrayList<Instruction> recompileHopsDagInstructions( Hop hop )
	{
		ArrayList<Instruction> newInst = null;

		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	public static ArrayList<Instruction> recompileHopsDag2Forced( Hop hop, long tid, ExecType et ) 
	{
		ArrayList<Instruction> newInst = null;

		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	 */
	public static Hop deepCopyHopsDag( Hop hops ) {
		Hop ret = null;
		
		try {
```

### UnusedAssignment
Variable `newInst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	public static ArrayList<Instruction> recompileHopsDag2Forced( StatementBlock sb, ArrayList<Hop> hops, long tid, ExecType et )
	{
		ArrayList<Instruction> newInst = null;
		
		//need for synchronization as we do temp changes in shared hops/lops
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/MatMultCP.java`
#### Snippet
```java
	@Override
	public String getInstructions(String input1, String input2, String output) {
		String ret = null;
		if(!useTranspose) {
			ret = InstructionUtils.concatOperands(
```

### UnusedAssignment
Variable `_numThreads` initializer `1` is redundant
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java

	//cp-specific parameters
	private int _numThreads = 1;
	
	public ParameterizedBuiltin(HashMap<String, Lop> paramLops, ParamBuiltinOp op, DataType dt, ValueType vt, ExecType et) {
```

### UnusedAssignment
Variable `_cacheType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/PMMJ.java`
#### Snippet
```java
	}
	
	private CacheType _cacheType = null;
	private int _numThreads = 1;
	
```

### UnusedAssignment
Variable `_weightsType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSquaredLossR.java`
#### Snippet
```java
	public static final String OPCODE = "redwsloss";
	
	private WeightsType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSquaredLossR.java`
#### Snippet
```java
	
	private WeightsType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedSquaredLossR.java`
#### Snippet
```java
	private WeightsType _weightsType = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
	public WeightedSquaredLossR(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, WeightsType wt, boolean cacheU, boolean cacheV, ExecType et) {
```

### UnusedAssignment
Variable `_async` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/Checkpoint.java`
#### Snippet
```java

	private StorageLevel _storageLevel;
	private boolean _async = false;
	

```

### UnusedAssignment
Variable `_binOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/BinaryUAggChain.java`
#### Snippet
```java

	//outer operation
	private OpOp2 _binOp = null;
	//inner operation
	private AggOp _uaggOp = null;
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/BinaryUAggChain.java`
#### Snippet
```java
	private OpOp2 _binOp = null;
	//inner operation
	private AggOp _uaggOp = null;
	private Direction _uaggDir = null;
	
```

### UnusedAssignment
Variable `_uaggDir` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/BinaryUAggChain.java`
#### Snippet
```java
	//inner operation
	private AggOp _uaggOp = null;
	private Direction _uaggDir = null;
	
	
```

### UnusedAssignment
Variable `_uop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMMR.java`
#### Snippet
```java
	
	private WUMMType _wummType = null;
	private OpOp1 _uop = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
```

### UnusedAssignment
Variable `_wummType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMMR.java`
#### Snippet
```java
	public static final String OPCODE = "redwumm";
	
	private WUMMType _wummType = null;
	private OpOp1 _uop = null;
	private boolean _cacheU = false;
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMMR.java`
#### Snippet
```java
	private OpOp1 _uop = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
	public WeightedUnaryMMR(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, WUMMType wt, OpOp1 op, boolean cacheU, boolean cacheV, ExecType et) {
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMMR.java`
#### Snippet
```java
	private WUMMType _wummType = null;
	private OpOp1 _uop = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
	
```

### UnusedAssignment
Variable `_outputEmptyBlocks` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/ReBlock.java`
#### Snippet
```java
	public static final String OPCODE = "rblk"; 
	
	private boolean _outputEmptyBlocks = true;
	
	private int _blocksize;
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/lops/AppendG.java`
#### Snippet
```java
	public static final String OPCODE = "gappend";
	
	private boolean _cbind = true;
	
	public AppendG(Lop input1, Lop input2, Lop input3, Lop input4, DataType dt, ValueType vt, boolean cbind, ExecType et) 
```

### UnusedAssignment
Variable `fmt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/Data.java`
#### Snippet
```java
		if ( _op.isWrite() ) {
			sb.append( OPERAND_DELIMITOR );
			FileFormat fmt = null;
			if ( getDataType() == DataType.MATRIX || getDataType() == DataType.FRAME )
				fmt = oparams.getFormat();
```

### UnusedAssignment
Variable `_numThreads` initializer `1` is redundant
in `src/main/java/org/apache/sysds/lops/Unary.java`
#### Snippet
```java
	
	//cp-specific parameters
	private int _numThreads = 1;
	private boolean _inplace = false;

```

### UnusedAssignment
Variable `_lopSBRuleSet` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/rewrite/LopRewriter.java`
#### Snippet
```java
public class LopRewriter
{
	private ArrayList<LopRewriteRule> _lopSBRuleSet = null;

	public LopRewriter() {
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
	
	public static Hop createValueHop( Hop hop, boolean row ) {
		Hop ret = null;
		if( row ){
			ret = hop.rowsKnown() ? new LiteralOp(hop.getDim1()) : 
```

### UnusedAssignment
The value `true` assigned to `applied` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				
				hi = bop;
				applied = true;
				
				LOG.debug("Applied fuseLeftIndexingChainToAppend2 (line "+hi.getBeginLine()+")");
```

### UnusedAssignment
The value `true` assigned to `appliedPattern` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
					ValueType.FP64, OpOp4.WSLOSS, X, U, V, W, false);
				HopRewriteUtils.setOutputParametersForScalar(hnew);
				appliedPattern = true;
				
				LOG.debug("Applied simplifyWeightedSquaredLoss4"+uvIndex+" (line "+hi.getBeginLine()+")");
```

### UnusedAssignment
Variable `hnew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				&& input.getDim1()>=1 && input.getDim2() >= 1 )
			{
				Hop hnew = null;
				if( uhi.getDirection() == Direction.RowCol ) 
					hnew = new LiteralOp(0.0);
```

### UnusedAssignment
The value `true` assigned to `appliedPattern` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				hnew.refreshSizeInformation();
				
				appliedPattern = true;
				LOG.debug("Applied simplifyWeightedUnaryMM2 (line "+hi.getBeginLine()+")");	
			}
```

### UnusedAssignment
The value `true` assigned to `appliedPattern` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				hnew.setBlocksize(W.getBlocksize());
				hnew.refreshSizeInformation();
				appliedPattern = true;
				LOG.debug("Applied simplifyWeightedDivMM7 (line "+hi.getBeginLine()+")");
			}
```

### UnusedAssignment
The value `true` assigned to `appliedPattern` is never used
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
					hnew.refreshSizeInformation();
					
					appliedPattern = true;
					LOG.debug("Applied simplifyWeightedSigmoid4 (line "+hi.getBeginLine()+")");	
				}
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
	private static Process runCommand(String cmd) throws IOException, InterruptedException {
		Process child = Runtime.getRuntime().exec(cmd);     
		int c = 0;

		while ((c = child.getInputStream().read()) != -1) {
```

### UnusedAssignment
Variable `final_v` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/compile/linearization/ILinearize.java`
#### Snippet
```java
	private static List<Lop> doMaxParallelizeSort(List<Lop> v)
	{
		List<Lop> final_v = null;
		// Fallback to default depth-first if all operators are CP
		if (v.stream().anyMatch(ILinearize::isDistributedOp)) {
```

### UnusedAssignment
Variable `opcode` initializer `null` is redundant
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
	public static String getCPHeavyHitterCode( Instruction inst )
	{
		String opcode = null;
		
		if( inst instanceof SPInstruction ) {
```

### UnusedAssignment
Variable `job_id` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
	private static final Log LOG = LogFactory.getLog(Dag.class.getName());

	private static IDSequence job_id = null;
	private static IDSequence var_index = null;
	
```

### UnusedAssignment
Variable `var_index` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java

	private static IDSequence job_id = null;
	private static IDSequence var_index = null;
	
	private String scratch = "";
```

### UnusedAssignment
Variable `doRmVar` initializer `false` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
		HashMap<String, Lop> var_deletionsLineNum =  new HashMap<>();
		
		boolean doRmVar = false;

		for (int i = 0; i < execNodes.size(); i++) {
```

### UnusedAssignment
Variable `inst_string` initializer `""` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
				}

				String inst_string = "";

				// Lops with arbitrary number of inputs (ParameterizedBuiltin, GroupedAggregate, DataGen)
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
				
				if ( op.isWrite() ) {
					NodeOutput out = null;
						out = setupNodeOutputs(node, ExecType.CP, false, false);
						if ( dnode.getDataType() == DataType.SCALAR ) {
```

### UnusedAssignment
The value `false` assigned to `doRmVar` is never used
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
			if(doRmVar)
				processConsumersForInputs(node, inst, deleteInst);
			doRmVar = false;
		}
		
```

### UnusedAssignment
Variable `nodes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java

	// list of all nodes in the dag
	private ArrayList<Lop> nodes = null;

	static {
```

### UnusedAssignment
Variable `dmlScriptStr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParserWrapper.java`
#### Snippet
```java
			throws IOException, LanguageException
	{
		String dmlScriptStr = null;
		
		//read DML script from file
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParserWrapper.java`
#### Snippet
```java
			
			//core script reading
			String tmp = null;
			while ((tmp = in.readLine()) != null) {
				sb.append( tmp );
```

### UnusedAssignment
Variable `blk` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParserWrapper.java`
#### Snippet
```java
	 */
	public static StatementBlock getStatementBlock(Statement current) {
		StatementBlock blk = null;
		if(current instanceof ParForStatement) {
			blk = new ParForStatementBlock();
```

### UnusedAssignment
Variable `recVersion` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
			else {
				// CASE: defined only if branch
				DataIdentifier recVersion = null;
				if (ifVersion != null){
					// add reconciled version (deep copy of ifVersion, cast as DataIdentifier)
```

### UnusedAssignment
Variable `sourceExpr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java

		if (stmt instanceof AssignmentStatement || stmt instanceof MultiAssignmentStatement){
			Expression sourceExpr = null;
			if (stmt instanceof AssignmentStatement) {
				AssignmentStatement astmt = (AssignmentStatement)stmt;
```

### UnusedAssignment
Variable `intid` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					bife.raiseValidateError("Undefined Variable (" + id.getName() + ") used in statement", false, LanguageErrorCodes.INVALID_PARAMETERS);
				}
				IntIdentifier intid = null;
				if (bife.getOpCode() == Builtins.NROW) {
					intid = new IntIdentifier((currVal instanceof IndexedIdentifier)
```

### UnusedAssignment
Variable `_updateInPlaceVars` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
	HashMap<String,ConstIdentifier> _constVarsOut;

	private ArrayList<String> _updateInPlaceVars = null;
	private boolean _requiresRecompile = false;
	private boolean _splitDag = false;
```

### UnusedAssignment
Variable `sourceExpr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		// for regular stmt, check if this is a function call stmt block
		if (stmt instanceof AssignmentStatement || stmt instanceof MultiAssignmentStatement){
			Expression sourceExpr = null;
			if (stmt instanceof AssignmentStatement)
				sourceExpr = ((AssignmentStatement)stmt).getSource();
```

### UnusedAssignment
Variable `_rowLowerEqualsUpper` initializer `false` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
	private boolean _rowLowerEqualsUpper = false, _colLowerEqualsUpper = false;
	
	// 	for IndexedIdentifier, dim1 and dim2 will ultimately be the dimensions of the indexed region and NOT the dims of what is being indexed
```

### UnusedAssignment
Variable `_colLowerEqualsUpper` initializer `false` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
	private boolean _rowLowerEqualsUpper = false, _colLowerEqualsUpper = false;
	
	// 	for IndexedIdentifier, dim1 and dim2 will ultimately be the dimensions of the indexed region and NOT the dims of what is being indexed
```

### UnusedAssignment
Variable `_colLowerBound` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores the expressions containing the ranges for the 
	private Expression 	_rowLowerBound = null, _rowUpperBound = null, _colLowerBound = null, _colUpperBound = null;
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
```

### UnusedAssignment
Variable `rowLB_1_1` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_rowLowerBound instanceof ConstIdentifier && ( _rowLowerBound instanceof IntIdentifier || _rowLowerBound instanceof DoubleIdentifier )){
			
			Long rowLB_1_1 = -1L;
			if (_rowLowerBound instanceof IntIdentifier) 
				rowLB_1_1 = ((IntIdentifier)_rowLowerBound).getValue();
```

### UnusedAssignment
Variable `tempRowLB` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
					
					// test constant propogation			
					long tempRowLB = -1L;
					boolean validRowLB = true;
					if (constValue instanceof IntIdentifier) 
```

### UnusedAssignment
Variable `rowUB_1_1` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_rowUpperBound instanceof ConstIdentifier && ( _rowUpperBound instanceof IntIdentifier || _rowUpperBound instanceof DoubleIdentifier )){
			
			Long rowUB_1_1 = -1L;
			if (_rowUpperBound instanceof IntIdentifier) 
				rowUB_1_1 = ((IntIdentifier)_rowUpperBound).getValue();
```

### UnusedAssignment
Variable `tempRowUB` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				else {						
					// test constant propogation			
					long tempRowUB = -1L;
					boolean validRowUB = true;
					if (constValue instanceof IntIdentifier) 
```

### UnusedAssignment
Variable `colLB_1_1` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_colLowerBound instanceof ConstIdentifier && ( _colLowerBound instanceof IntIdentifier || _colLowerBound instanceof DoubleIdentifier )){
			
			Long colLB_1_1 = -1L;
			if (_colLowerBound instanceof IntIdentifier) 
				colLB_1_1 = ((IntIdentifier)_colLowerBound).getValue();
```

### UnusedAssignment
Variable `tempColLB` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
					
					// test constant propogation			
					long tempColLB = -1L;
					boolean validColLB = true;
					if (constValue instanceof IntIdentifier) 
```

### UnusedAssignment
Variable `colUB_1_1` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		if (_colUpperBound instanceof ConstIdentifier && ( _colUpperBound instanceof IntIdentifier || _colUpperBound instanceof DoubleIdentifier )){
			
			Long colUB_1_1 = -1L;
			if (_colUpperBound instanceof IntIdentifier) 
				colUB_1_1 = ((IntIdentifier)_colUpperBound).getValue();
```

### UnusedAssignment
Variable `tempColUB` initializer `-1L` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				else {						
					// test constant propogation			
					long tempColUB = -1L;
					boolean validColUB = true;
					if (constValue instanceof IntIdentifier) 
```

### UnusedAssignment
Variable `_rowUpperBound` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores the expressions containing the ranges for the 
	private Expression 	_rowLowerBound = null, _rowUpperBound = null, _colLowerBound = null, _colUpperBound = null;
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
```

### UnusedAssignment
Variable `_colUpperBound` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores the expressions containing the ranges for the 
	private Expression 	_rowLowerBound = null, _rowUpperBound = null, _colLowerBound = null, _colUpperBound = null;
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
```

### UnusedAssignment
Variable `_rowLowerBound` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
	
	// stores the expressions containing the ranges for the 
	private Expression 	_rowLowerBound = null, _rowUpperBound = null, _colLowerBound = null, _colUpperBound = null;
	
	// stores whether row / col indices have same value (thus selecting either (1 X n) row-vector OR (n X 1) col-vector)
```

### UnusedAssignment
Variable `dmlPgm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DMLParserWrapper.java`
#### Snippet
```java
	 */
	public DMLProgram doParse(String fileName, String dmlScript, String sourceNamespace, Map<String,String> argVals) {
		DMLProgram dmlPgm = null;
		
		CharStream in;
```

### UnusedAssignment
Variable `ast` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DMLParserWrapper.java`
#### Snippet
```java
		}

		ProgramrootContext ast = null;
		CustomErrorListener errorListener = new CustomErrorListener();
		
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
		private MatrixBlock _dest = null;
		private int _rl = -1;
		private int _ru = -1;
		
		public SortRowsTask(MatrixBlock dest, int rl, int ru) {
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
	{
		private MatrixBlock _dest = null;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_dest` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
	private static class SortRowsTask implements Callable<Object> 
	{
		private MatrixBlock _dest = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_localFS` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderBinaryBlock.java`
#### Snippet
```java
public class ReaderBinaryBlock extends MatrixReader
{
	protected boolean _localFS = false;
	
	public ReaderBinaryBlock( boolean localFS )
```

### UnusedAssignment
Variable `_props` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSV.java`
#### Snippet
```java
	public static final int BLOCKSIZE_J = 32; //32 cells (typically ~512B, should be less than write buffer of 1KB)
	
	protected FileFormatPropertiesCSV _props = null;
	
	public FrameWriterTextCSV( FileFormatPropertiesCSV props ) {
```

### UnusedAssignment
The value `true` assigned to `shouldReadMTD` is never used
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			if (isCSV){
				// there should be no MTD file for delimited file format
				shouldReadMTD = true;
				
				// Handle valid ParamNames.
```

### UnusedAssignment
Variable `isLIBSVM` initializer `false` is redundant
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			}

			boolean isLIBSVM = false;
			isLIBSVM = (formatTypeString != null && formatTypeString.equalsIgnoreCase(FileFormat.LIBSVM.toString()));
			if (isLIBSVM) {
```

### UnusedAssignment
The value `true` assigned to `shouldReadMTD` is never used
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			if (isLIBSVM) {
				 // Handle libsvm file format
				shouldReadMTD = true;
				
				// only allow IO_FILENAME, READROWPARAM, READCOLPARAM   
```

### UnusedAssignment
Variable `nnz` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				// set number non-zeros
				Expression ennz = getVarParam("nnz");
				long nnz = -1;
				if( ennz != null ) {
					nnz = Long.valueOf(ennz.toString());
```

### UnusedAssignment
Variable `defExpr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
	private void handleCSVDefaultParam(String param, ValueType vt, boolean conditional) {
		if (getVarParam(param) == null) {
			Expression defExpr = null;
			switch(vt) {
				case BOOLEAN:
```

### UnusedAssignment
Variable `_replication` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlock.java`
#### Snippet
```java

public class WriterBinaryBlock extends MatrixWriter {
	protected int _replication = -1;

	public WriterBinaryBlock(int replication) {
```

### UnusedAssignment
Variable `block` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlock.java`
#### Snippet
```java
						int maxCol = (blockCol * blen + blen < src.getNumColumns()) ? blen : src.getNumColumns() -
							blockCol * blen;
						MatrixBlock block = null;

						if(blockRow == blockCol) { // block on diagonal
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
```

### UnusedAssignment
Variable `_csvprops` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
		private int _rl = -1;
		private int _ru = -1;
		private FileFormatPropertiesCSV _csvprops = null;
		
		public WriteFileTask(Path path, JobConf job, FileSystem fs, FrameBlock src, int rl, int ru, FileFormatPropertiesCSV csvprops) {
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
		private FileFormatPropertiesCSV _csvprops = null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
		private FileFormatPropertiesCSV _csvprops = null;
		
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCell.java`
#### Snippet
```java
		throws IOException
	{
		String value = null;
		FastStringTokenizer st = new FastStringTokenizer(' ');
		int row = -1;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCell.java`
#### Snippet
```java
		
		boolean sparse = dest.isInSparseFormat();
		String value = null;
		IJV cell = new IJV();
		long nnz = 0;
```

### UnusedAssignment
Variable `incr` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					long low = Integer.MIN_VALUE;
					long up = Integer.MAX_VALUE;
					long incr = -1;
					
					if( ip.getFromExpr()instanceof IntIdentifier)
```

### UnusedAssignment
Variable `dpf` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		boolean isSpark = OptimizerUtils.isSparkExecutionMode();
		int blksz = ConfigurationManager.getBlocksize();
		PartitionFormat dpf = null;
		
		//1) get all bounds expressions for index access
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		 */

		LinearFunction out = null;
		
		if( ! (dat instanceof IndexedIdentifier ) ) //happens if matrix is now used as scalar
```

### UnusedAssignment
Variable `_resultVars` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
	private final long _PID;
	private VariableSet          _vsParent   = null;
	private ArrayList<ResultVar> _resultVars = null;
	private Bounds               _bounds     = null;
	
```

### UnusedAssignment
Variable `ret` initializer `true` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			
		//general case function comparison
		boolean ret = true; //true if equal index functions
		LinearFunction f1 = getLinearFunction(dat1);
		LinearFunction f2 = getLinearFunction(dat2);
```

### UnusedAssignment
Variable `_numThreads` initializer `1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCellParallel.java`
#### Snippet
```java
	private static final long MIN_FILESIZE_MM = 8L * 1024; //8KB
	
	private int _numThreads = 1;
	
	public ReaderTextCellParallel(FileFormat fmt) {
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;
		
		public WriteTextTask(Path path, JobConf job, FileSystem fs, MatrixBlock src, int rl, int ru) {
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
	{
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
	private class WriteTextTask implements Callable<Object> 
	{
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
	{
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;

		public WriteMMTask(Path path, JobConf job, FileSystem fs, MatrixBlock src, int rl, int ru) {
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private Path _path =null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
	private class WriteMMTask implements Callable<Object> 
	{
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
```

### UnusedAssignment
The value changed at `start++` is never used
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
					start++; start++; // Increment index for K and C
					long R = (long) getDoubleValue(_args[start++]);
					long S = (long) getDoubleValue(_args[start++]);
					
					if(this.getOpCode() == Builtins.CONV2D_BACKWARD_FILTER) {
```

### UnusedAssignment
Variable `_args` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
public class BuiltinFunctionExpression extends DataIdentifier 
{
	protected Expression[] _args = null;
	private Builtins _opcode;

```

### UnusedAssignment
Variable `index` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixWriter.java`
#### Snippet
```java

	public static MatrixBlock getMatrixBlockForReuse( MatrixBlock[] blocks, int rows, int cols, int blen ) {
		int index = -1;
		if( rows==blen && cols==blen )
			index = 0;
```

### UnusedAssignment
Variable `estNNZ` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixWriter.java`
#### Snippet
```java
		MatrixBlock[] blocks = new MatrixBlock[4];
		double sparsity = ((double)nonZeros)/(rlen*clen);
		long estNNZ = -1;
		
		//full block 
```

### UnusedAssignment
Variable `target` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}

		DataIdentifier target = null;
		if(ctx.targetList.dataInfo.expr instanceof DataIdentifier) {
			target = (DataIdentifier) ctx.targetList.dataInfo.expr;
```

### UnusedAssignment
Variable `source` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		if(ctx.targetList.dataInfo.expr instanceof DataIdentifier) {
			target = (DataIdentifier) ctx.targetList.dataInfo.expr;
			Expression source = null;
			if(ctx.commandLineParam.dataInfo.expr != null) {
				// Since commandline parameter is set
```

### UnusedAssignment
Variable `argVals` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	protected final String currentFile;
	protected String _workingDir = DEF_WORK_DIR;
	protected Map<String,String> argVals = null;
	protected String sourceNamespace = null;
	// Map namespaces to full paths as defined only from source statements in this script (i.e., currentFile)
```

### UnusedAssignment
Variable `functionName` initializer `""` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		String splitStr = Pattern.quote(namespaceResolutionOp());
		String [] fnNames = fullyQualifiedFunctionName.split(splitStr);
		String functionName = "";
		String namespace = "";
		if(fnNames.length == 1) {
```

### UnusedAssignment
Variable `namespace` initializer `""` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		String [] fnNames = fullyQualifiedFunctionName.split(splitStr);
		String functionName = "";
		String namespace = "";
		if(fnNames.length == 1) {
			namespace = DMLProgram.DEFAULT_NAMESPACE;
```

### UnusedAssignment
Variable `sourceNamespace` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	protected String _workingDir = DEF_WORK_DIR;
	protected Map<String,String> argVals = null;
	protected String sourceNamespace = null;
	// Map namespaces to full paths as defined only from source statements in this script (i.e., currentFile)
	protected HashMap<String, String> sources;
```

### UnusedAssignment
Variable `target` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}

		DataIdentifier target = null;
		if(dataInfo.expr instanceof DataIdentifier) {
			target = (DataIdentifier) dataInfo.expr;
```

### UnusedAssignment
Variable `be` initializer `new BinaryExpression(bop)` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		if(left.expr != null && right.expr != null) {
			Expression.BinaryOp bop = Expression.getBinaryOp(op);
			BinaryExpression be = new BinaryExpression(bop);
			be = new BinaryExpression(bop);
			be.setLeft(left.expr);
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private long _rl = -1;
		private long _ru = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private long _rl = -1;
		private long _ru = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
		private MatrixBlock _src = null;
		private long _rl = -1;
		private long _ru = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private MatrixBlock _src = null;
		private long _rl = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
		private long _rl = -1;
		private long _ru = -1;
		private int _blen = -1;
		
		public WriteFileTask(Path path, JobConf job, FileSystem fs, MatrixBlock src, long rl, long ru, int blen) {
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVM.java`
#### Snippet
```java
	{
		SparseRowVector vect = new SparseRowVector(1024);
		String value = null;
		int row = rowPos.intValue();
		long lnnz = 0;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
		private int _blen = -1;
		private int _rl = -1;
		private int _ru = -1;
		
		public WriteFileTask(Path path, JobConf job, FileSystem fs, FrameBlock src, int rl, int ru, int blen) {
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _blen = -1;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
		private FrameBlock _src = null;
		private int _blen = -1;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _blen = -1;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `writer` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixWriterFactory.java`
#### Snippet
```java
	public static MatrixWriter createMatrixWriter(FileFormat fmt, int replication, FileFormatProperties props)
	{
		MatrixWriter writer = null;

		switch(fmt) {
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
	{
		boolean sparse = dest.isInSparseFormat();
		String value = null;
		int row = rowPos.intValue();
		double cellValue = 0;
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
		String value = null;
		int row = rowPos.intValue();
		double cellValue = 0;
		long lnnz = 0;
		
```

### UnusedAssignment
Variable `emptyValuesFound` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
		
		// Read the data
		boolean emptyValuesFound = false;
		try
		{
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
		int nrow = -1;
		int ncol = -1;
		String value = null;
		
		String cellStr = null;
```

### UnusedAssignment
Variable `cellStr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
		String value = null;
		
		String cellStr = null;
		for(int fileNo=0; fileNo<files.size(); fileNo++)
		{
```

### UnusedAssignment
The value `br.readLine()` assigned to `value` is never used
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
				}
				
				while ( (value = br.readLine()) != null ) {
					nrow++;
				}
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
		protected long parse(RecordReader<LongWritable, Text> reader, LongWritable key, Text value) throws IOException {
			DenseBlock a = _dest.getDenseBlock();
			double cellValue = 0;
			long nnz = 0;
			boolean noFillEmpty = false;
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
		protected long parse(RecordReader<LongWritable, Text> reader, LongWritable key, Text value) throws IOException {
			boolean noFillEmpty = false;
			double cellValue = 0;
			final SparseBlock sb = _dest.getSparseBlock();
			long nnz = 0;
```

### UnusedAssignment
Variable `offsetPerSplit` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
	private static class SplitOffsetInfos {
		// offset & length info per split
		private int[] offsetPerSplit = null;
		private int[] lenghtPerSplit = null;

```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
			final SparseBlock sb = _dest.getSparseBlock();
			long nnz = 0;
			double cellValue = 0;
			while(reader.next(key, value)) {
				_col = 0;
```

### UnusedAssignment
Variable `nnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
				_row = _offsets.getOffsetPerSplit(_splitCount);

				long nnz = 0;
				try {
					nnz = parse(reader, key, value);
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
			final SparseBlock sb = _dest.getSparseBlock();
			long nnz = 0;
			double cellValue = 0;
			boolean noFillEmpty = false;
			while(reader.next(key, value)) {
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
		protected long parse(RecordReader<LongWritable, Text> reader, LongWritable key, Text value) throws IOException {
			DenseBlock a = _dest.getDenseBlock();
			double cellValue = 0;
			boolean noFillEmpty = false;
			long nnz = 0;
```

### UnusedAssignment
Variable `lenghtPerSplit` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
		// offset & length info per split
		private int[] offsetPerSplit = null;
		private int[] lenghtPerSplit = null;

		public SplitOffsetInfos(int numSplits) {
```

### UnusedAssignment
Variable `_isFirstSplit` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
		private FrameBlock _dest = null;
		private int _offset = -1;
		private boolean _isFirstSplit = false;
		
		
```

### UnusedAssignment
Variable `_split` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
	private class ReadRowsTask implements Callable<Object> 
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
		private int _offset = -1;
```

### UnusedAssignment
Variable `_informat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
```

### UnusedAssignment
Variable `_dest` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
		private int _offset = -1;
		private boolean _isFirstSplit = false;
```

### UnusedAssignment
Variable `_offset` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FrameBlock _dest = null;
		private int _offset = -1;
		private boolean _isFirstSplit = false;
		
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixReaderFactory.java`
#### Snippet
```java
			throw new DMLRuntimeException("Failed to create matrix reader with empty properties.");

		MatrixReader reader = null;
		FileFormat fmt = props.fmt;
		boolean par = ConfigurationManager.getCompilerConfigFlag(ConfigType.PARALLEL_CP_READ_TEXTFORMATS);
```

### UnusedAssignment
Variable `reader` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/MatrixReaderFactory.java`
#### Snippet
```java
	private static final Log LOG = LogFactory.getLog(MatrixReaderFactory.class.getName());
	public static MatrixReader createMatrixReader(FileFormat fmt) {
		MatrixReader reader = null;
		boolean par = ConfigurationManager.getCompilerConfigFlag(ConfigType.PARALLEL_CP_READ_TEXTFORMATS);
		boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
```

### UnusedAssignment
Variable `named` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ListReader.java`
#### Snippet
```java
		MetaDataAll meta = new MetaDataAll(fname+".mtd", false, true);
		int numObjs = (int) meta.getDim1();
		boolean named = false;
		
		Data[] data = null;
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ListReader.java`
#### Snippet
```java
		boolean named = false;
		
		Data[] data = null;
		String[] names = null;
		try {
```

### UnusedAssignment
Variable `names` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ListReader.java`
#### Snippet
```java
		
		Data[] data = null;
		String[] names = null;
		try {
			// read all meta data files
```

### UnusedAssignment
Variable `dat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ListReader.java`
#### Snippet
```java
				DataCharacteristics dc = lmeta.getDataCharacteristics();
				FileFormat fmt = lmeta.getFileFormat();
				Data dat = null;
				switch( lmeta.getDataType() ) {
					case MATRIX:
```

### UnusedAssignment
Variable `fmt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesMM.java`
#### Snippet
```java
		//check format, field, and 
		String format = st.nextToken();
		MMFormat fmt = null;
		switch( format ) {
			//case "array": fmt = MMFormat.ARRAY; break;
```

### UnusedAssignment
Variable `f` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesMM.java`
#### Snippet
```java
		}
		String field = st.nextToken();
		MMField f = null;
		switch( field ) {
			case "real": f = MMField.REAL; break;
```

### UnusedAssignment
Variable `s` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesMM.java`
#### Snippet
```java
		}
		String symmetry = st.nextToken();
		MMSymmetry s = null;
		switch( symmetry ) {
			case "general": s = MMSymmetry.GENERAL; break;
```

### UnusedAssignment
Variable `currBuiltinOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		}
		
		Hop currBuiltinOp = null;
		target = (target == null) ? createTarget(source) : target;
		
```

### UnusedAssignment
Variable `currBop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		} 
		else {
			Hop currBop = null;
			OpOp2 op = null;

```

### UnusedAssignment
Variable `op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		else {
			Hop currBop = null;
			OpOp2 op = null;

			if (source.getOpCode() == Expression.BooleanOp.LOGICALAND) {
```

### UnusedAssignment
Variable `currBuiltinOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		
		// Construct Hop for current builtin function expression based on its type
		Hop currBuiltinOp = null;
		switch (source.getOpCode()) {
			case TRANSFORMENCODE:
```

### UnusedAssignment
Variable `currBop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		target.setValueType(source.getOutput().getValueType());
		
		Hop currBop = null;
		switch( source.getOpCode() ) {
			case PLUS:
```

### UnusedAssignment
Variable `rowUpperHops` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			processExpression(ix.getColLowerBound(),null, hops) : new LiteralOp(1);
		
		Hop rowUpperHops = null, colUpperHops = null;
		if (ix.getRowUpperBound() != null)
			rowUpperHops = processExpression(ix.getRowUpperBound(),null,hops);
```

### UnusedAssignment
Variable `colUpperHops` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			processExpression(ix.getColLowerBound(),null, hops) : new LiteralOp(1);
		
		Hop rowUpperHops = null, colUpperHops = null;
		if (ix.getRowUpperBound() != null)
			rowUpperHops = processExpression(ix.getRowUpperBound(),null,hops);
```

### UnusedAssignment
Variable `read` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					
					DataIdentifier var = fsb.liveIn().getVariable(varName);
					DataOp read = null;
					if (var == null) {
						throw new ParseException("variable '" + varName + "' is not available for iterable predicate");
```

### UnusedAssignment
Variable `currBuiltinOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		
		// Construct Hop for current builtin function expression based on its type
		Hop currBuiltinOp = null;
		switch (source.getOpCode()) {
			case QR:
```

### UnusedAssignment
Variable `pHop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		// -- construct hops for all input parameters
		// -- store them in hashmap so that their "name"s are maintained
		Hop pHop = null;
		for ( String paramName : source.getVarParams().keySet() ) {
			pHop = processExpression(source.getVarParam(paramName), null, hops);
```

### UnusedAssignment
Variable `currBuiltinOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		}
		
		Hop currBuiltinOp = null;

		if (target == null) {
```

### UnusedAssignment
Variable `cp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		
		// set conditional predicate
		ConditionalPredicate cp = null;
		
		if (passedSB instanceof WhileStatementBlock){
```

### UnusedAssignment
Variable `read` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			DataIdentifier var = passedSB.liveIn().getVariables().get(varName);
			
			DataOp read = null;
			
			if (var == null) {
```

### UnusedAssignment
Variable `pHop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		// -- construct hops for all input parameters
		// -- store them in hashmap so that their "name"s are maintained
		Hop pHop = null; 
		for ( String paramName : source.getVarParams().keySet() ) {
			pHop = processExpression(source.getVarParam(paramName), null, hops);
```

### UnusedAssignment
Variable `currBuiltinOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		}
		
		Hop currBuiltinOp = null;

		if (target == null) {
```

### UnusedAssignment
Variable `numImgIndex` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		ret.add(first);
		Expression[] allExpr = source.getAllExpr();
		int numImgIndex = -1;
		if(skip == 1) {
			numImgIndex = 5;
```

### UnusedAssignment
Variable `currBop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		Hop right = processExpression(source.getRight(), null, hops);

		Hop currBop = null;

		if (target == null) {
```

### UnusedAssignment
Variable `dag` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
	
	public ProgramBlock createRuntimeProgramBlock(Program prog, StatementBlock sb, DMLConfig config) {
		Dag<Lop> dag = null;
		Dag<Lop> pred_dag = null;

```

### UnusedAssignment
Variable `pred_dag` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
	public ProgramBlock createRuntimeProgramBlock(Program prog, StatementBlock sb, DMLConfig config) {
		Dag<Lop> dag = null;
		Dag<Lop> pred_dag = null;

		ArrayList<Instruction> instruct;
```

### UnusedAssignment
Variable `pred_instruct` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java

		ArrayList<Instruction> instruct;
		ArrayList<Instruction> pred_instruct = null;
		
		ProgramBlock retPB = null;
```

### UnusedAssignment
Variable `retPB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		ArrayList<Instruction> pred_instruct = null;
		
		ProgramBlock retPB = null;
		
		// process While Statement - add runtime program blocks to program
```

### UnusedAssignment
Variable `rtpb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java

			// create for program block
			ForProgramBlock rtpb = null;
			IterablePredicate iterPred = fsb.getIterPredicate();
			
```

### UnusedAssignment
Variable `rtpb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			FunctionStatementBlock fsb = (FunctionStatementBlock)sb;
			FunctionStatement fstmt = (FunctionStatement)fsb.getStatement(0);
			FunctionProgramBlock rtpb = null;
			
	
```

### UnusedAssignment
Variable `writer` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterFactory.java`
#### Snippet
```java

	public static TensorWriter createTensorWriter(FileFormat fmt) {
		TensorWriter writer = null;
		if (fmt == FileFormat.TEXT) {
			writer = new TensorWriterTextCell();
```

### UnusedAssignment
Variable `_dest` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCellParallel.java`
#### Snippet
```java
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
		
		public ReadTask( InputSplit split, TextInputFormat informat, JobConf job, FrameBlock dest ) {
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCellParallel.java`
#### Snippet
```java
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
		
```

### UnusedAssignment
Variable `_informat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCellParallel.java`
#### Snippet
```java
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private FrameBlock _dest = null;
```

### UnusedAssignment
Variable `_split` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCellParallel.java`
#### Snippet
```java
	public class ReadTask implements Callable<Object> 
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
```

### UnusedAssignment
The value `from` assigned to `to` is never used
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		}
		else if(isEmptyMatch(str, from, delim, dLen, len))
			return to = from; // empty string
		else // default: unquoted non-empty
			to = str.indexOf(delim, fromP1);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		throws IOException
	{
		Path[] ret = null;
		
		//Note on object stores: Since the object store file system implementations 
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		throws IOException
	{
		Path[] ret = null;
		if( fs.getFileStatus(file).isDirectory()
			|| IOUtilFunctions.isObjectStoreFileScheme(file) )
```

### UnusedAssignment
Variable `exists` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		retVal[0] = new String("");
		retVal[1] = new String("");
		boolean exists = false;
		
		try {
```

### UnusedAssignment
Variable `len` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
			ByteArrayOutputStream bos = new ByteArrayOutputStream();
			byte[] buff = new byte[LocalFileUtils.BUFFER_SIZE];
			for( int len=0; (len=input.read(buff))!=-1; )
				bos.write(buff, 0, len);
			return bos.toString("UTF-8");
```

### UnusedAssignment
Variable `to` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
			return new String[] {""};

		int from = 0, to = 0;
		final int len = str.length();
		final int delimLen = delim.length();
```

### UnusedAssignment
Variable `_dest` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderBinaryBlockParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _dest = null;
		
		public ReadFileTask(Path path, JobConf job, FileSystem fs, FrameBlock dest) {
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderBinaryBlockParallel.java`
#### Snippet
```java
	private class ReadFileTask implements Callable<Object> 
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderBinaryBlockParallel.java`
#### Snippet
```java
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _dest = null;
		
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderBinaryBlockParallel.java`
#### Snippet
```java
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _dest = null;
```

### UnusedAssignment
Variable `_split` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
	private static class CountRowsTask implements Callable<Object>
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
```

### UnusedAssignment
Variable `_numThreads` initializer `1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
public class ReaderTextLIBSVMParallel extends MatrixReader {
	private static FileFormatPropertiesLIBSVM _props;
	private int _numThreads = 1;
	private SplitOffsetInfos _offsets = null;

```

### UnusedAssignment
Variable `_dest` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private MatrixBlock _dest = null;
		private long _clen = -1;
		private int _splitCount = 0;
```

### UnusedAssignment
Variable `_nrows` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private boolean _rc = true;
		private String _errMsg = null;
		private int _nrows = -1;

		public CountRowsTask(InputSplit split, TextInputFormat informat, JobConf job) {
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private MatrixBlock _dest = null;
		private long _clen = -1;
		private int _splitCount = 0;

```

### UnusedAssignment
Variable `_informat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private InputSplit _split = null;
		private SplitOffsetInfos _splitoffsets = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private MatrixBlock _dest = null;
```

### UnusedAssignment
Variable `_informat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
	{
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private boolean _rc = true;
```

### UnusedAssignment
Variable `lenghtPerSplit` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		// offset & length info per split
		private int[] offsetPerSplit = null;
		private int[] lenghtPerSplit = null;

		public SplitOffsetInfos(int numSplits) {
```

### UnusedAssignment
Variable `_rc` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private int _splitCount = 0;

		private boolean _rc = true;
		private Exception _exception = null;
		private long _nnz;
```

### UnusedAssignment
Variable `_splitoffsets` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
	{
		private InputSplit _split = null;
		private SplitOffsetInfos _splitoffsets = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private SplitOffsetInfos _splitoffsets = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private MatrixBlock _dest = null;
		private long _clen = -1;
```

### UnusedAssignment
Variable `offsetPerSplit` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
	private static class SplitOffsetInfos {
		// offset & length info per split
		private int[] offsetPerSplit = null;
		private int[] lenghtPerSplit = null;

```

### UnusedAssignment
Variable `_splitCount` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private MatrixBlock _dest = null;
		private long _clen = -1;
		private int _splitCount = 0;

		private boolean _rc = true;
```

### UnusedAssignment
Variable `_split` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
	private static class LIBSVMReadTask implements Callable<Object>
	{
		private InputSplit _split = null;
		private SplitOffsetInfos _splitoffsets = null;
		private TextInputFormat _informat = null;
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
		private InputSplit _split = null;
		private TextInputFormat _informat = null;
		private JobConf _job = null;
		private boolean _rc = true;
		private String _errMsg = null;
```

### UnusedAssignment
Variable `_path` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
		
		public WriteFileTask(Path path, JobConf job, FileSystem fs, FrameBlock src, int rl, int ru) {
```

### UnusedAssignment
Variable `_fs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_src` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
		private FileSystem _fs = null;
		private FrameBlock _src = null;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
	{
		private Path _path = null;
		private JobConf _job = null;
		private FileSystem _fs = null;
		private FrameBlock _src = null;
```

### UnusedAssignment
Variable `_props` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCSV.java`
#### Snippet
```java
	public static final int BLOCKSIZE_J = 32; //32 cells (typically ~512B, should be less than write buffer of 1KB)
	
	protected FileFormatPropertiesCSV _props = null;
	
	public WriterTextCSV( FileFormatPropertiesCSV props ) {
```

### UnusedAssignment
Variable `values` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseRowVector.java`
#### Snippet
```java
	private int maxNzs = Integer.MAX_VALUE;
	private int size = 0;
	private double[] values = null;
	private int[] indexes = null;
	
```

### UnusedAssignment
Variable `indexes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseRowVector.java`
#### Snippet
```java
	private int size = 0;
	private double[] values = null;
	private int[] indexes = null;
	
	public SparseRowVector() {
```

### UnusedAssignment
Variable `_rows` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockMCSR.java`
#### Snippet
```java
	private static final long serialVersionUID = -4743624499258436199L;
	
	private SparseRow[] _rows = null;
	
	/**
```

### UnusedAssignment
Variable `_ptr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
	private static final long serialVersionUID = 1922673868466164244L;

	private int[] _ptr = null;       //row pointer array (size: rlen+1)
	private int[] _indexes = null;   //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
```

### UnusedAssignment
Variable `_values` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
	private int[] _ptr = null;       //row pointer array (size: rlen+1)
	private int[] _indexes = null;   //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
	
```

### UnusedAssignment
Variable `_indexes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java

	private int[] _ptr = null;       //row pointer array (size: rlen+1)
	private int[] _indexes = null;   //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
```

### UnusedAssignment
Variable `k` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
			int rlen = size(r) - tshift1;
			//copy lhs right
			int k = -1;
			for( k=rpos+rlen-1; k>=rpos && _indexes[k]>=cu; k-- ) {
				_indexes[pos] = _indexes[k];
```

### UnusedAssignment
Variable `k` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
			int rlen = size(r) - tshift1;
			//copy lhs right
			int k = -1;
			for( k=rpos+rlen-1; k>=rpos && _indexes[k]>=cu; k-- ) {
				_indexes[pos] = _indexes[k];
```

### UnusedAssignment
Variable `_size` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
	private int[] _indexes = null;   //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
	
	public SparseBlockCSR(int rlen) {
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	private static final long serialVersionUID = 7223478015917668745L;

	private int _rlen = -1;
	private int[] _rindexes = null;  //row index array (size: >=nnz)
	private int[] _cindexes = null;  //column index array (size: >=nnz)
```

### UnusedAssignment
Variable `_pos` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	private class SparseBlockCOOIterator implements Iterator<IJV>
	{
		private int _pos = 0; //current nnz position
		private int _len = 0; //upper nnz position (exclusive)
		private IJV retijv = new IJV(); //reuse output tuple
```

### UnusedAssignment
Variable `_rindexes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java

	private int _rlen = -1;
	private int[] _rindexes = null;  //row index array (size: >=nnz)
	private int[] _cindexes = null;  //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
```

### UnusedAssignment
Variable `_values` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	private int[] _rindexes = null;  //row index array (size: >=nnz)
	private int[] _cindexes = null;  //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
	
```

### UnusedAssignment
Variable `_len` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	{
		private int _pos = 0; //current nnz position
		private int _len = 0; //upper nnz position (exclusive)
		private IJV retijv = new IJV(); //reuse output tuple

```

### UnusedAssignment
Variable `_size` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	private int[] _cindexes = null;  //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
	
	public SparseBlockCOO(int rlen) {
```

### UnusedAssignment
Variable `_cindexes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
	private int _rlen = -1;
	private int[] _rindexes = null;  //row index array (size: >=nnz)
	private int[] _cindexes = null;  //column index array (size: >=nnz)
	private double[] _values = null; //value array (size: >=nnz)
	private int _size = 0;           //actual number of nnz
```

### UnusedAssignment
Variable `_curRow` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlock.java`
#### Snippet
```java
	{
		private int _rlen = 0; //row upper
		private int _curRow = -1; //current row
		private int _curColIx = -1; //current col index pos
		private int[] _curIndexes = null; //current col indexes
```

### UnusedAssignment
Variable `_rlen` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlock.java`
#### Snippet
```java
	private class SparseBlockIterator implements Iterator<IJV>
	{
		private int _rlen = 0; //row upper
		private int _curRow = -1; //current row
		private int _curColIx = -1; //current col index pos
```

### UnusedAssignment
Variable `_del` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/FastStringTokenizer.java`
#### Snippet
```java
	private static final long serialVersionUID = 4051436015710778611L;
	private String _string = null;
	private char   _del    = 0;
	private int    _pos    = -1;

```

### UnusedAssignment
Variable `naStrings` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
							|| key.toString().equalsIgnoreCase(DataExpression.PRIVACY)
							|| key.toString().equalsIgnoreCase(DataExpression.FINE_GRAINED_PRIVACY)) {
							String naStrings = null;
							if ( val instanceof String) {
								naStrings = val.toString();
```

### UnusedAssignment
Variable `size` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LongLongDoubleHashMap.java`
#### Snippet
```java
	private final EntryType type;
	private ADoubleEntry[] data = null;
	private int size = -1;
	
	public LongLongDoubleHashMap() {
```

### UnusedAssignment
Variable `value` initializer `Double.MAX_VALUE` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LongLongDoubleHashMap.java`
#### Snippet
```java

	public static abstract class ADoubleEntry {
		public double value = Double.MAX_VALUE;
		public ADoubleEntry next = null;
		public ADoubleEntry(double val) {
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LongLongDoubleHashMap.java`
#### Snippet
```java
	
	private final EntryType type;
	private ADoubleEntry[] data = null;
	private int size = -1;
	
```

### UnusedAssignment
Variable `next` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LongLongDoubleHashMap.java`
#### Snippet
```java
	public static abstract class ADoubleEntry {
		public double value = Double.MAX_VALUE;
		public ADoubleEntry next = null;
		public ADoubleEntry(double val) {
			value = val;
```

### UnusedAssignment
Variable `bufferSize` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		FileInputStream fis = new FileInputStream(fname);
		FastBufferedDataInputStream in = new FastBufferedDataInputStream(fis, BUFFER_SIZE);
		int bufferSize = 0;
		
		try {
```

### UnusedAssignment
Variable `_seq` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
	
	//unique IDs per JVM for tmp files
	private static IDSequence _seq = null;
	private static String _workingDir = null;
	
```

### UnusedAssignment
Variable `dirRoot` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
	{
		//create local tmp dir if not existing
		String dirRoot = null;
		DMLConfig conf = ConfigurationManager.getDMLConfig();
		if( conf != null ) 
```

### UnusedAssignment
Variable `val` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
		pred[0] = data[0];

		double val = 0;

		ArrayList<Double> not_missing = new ArrayList<>();
```

### UnusedAssignment
Variable `val` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
		pred.add(s[0] + b[0]);

		double val = 0;

		ArrayList<Double> not_missing = new ArrayList<>();
```

### UnusedAssignment
Variable `combined` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java

		for (int i = 0; i < freq; i++) {
			double combined = 0;
			if (i + freq < trend.size()) {
				combined = (season_tmp.get(i) + season_tmp.get(i + freq)) / 2;
```

### UnusedAssignment
Variable `val` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
		pred.add(s[0] + b[0] + c[0]);

		double val = 0;

		ArrayList<Double> not_missing = new ArrayList<>();
```

### UnusedAssignment
Variable `pattern` initializer `""` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
	{
		int row_idx = -1;
		String pattern = "";
		String[] attr = (String[]) col;
		int numRows = frameBlock.getNumRows();
```

### UnusedAssignment
Variable `key` initializer `""` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
			Object c = frame.getColumnData(idx);
			String[] attr = (String[]) c;
			String key = "";
			for (int i = 0;  i < numRows; i++) {
				key = (attr[i] == null) ? "NULL": attr[i];
```

### UnusedAssignment
Variable `_mean` initializer `Double.NaN` is redundant
in `src/main/java/org/apache/sysds/runtime/util/PoissonPRNGenerator.java`
#### Snippet
```java
{
	private PoissonDistribution _pdist = null;
	private double _mean = Double.NaN;
	private long seed;
	
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/Py4jConverterUtils.java`
#### Snippet
```java

	public static byte[] convertMBtoPy4JDenseArr(MatrixBlock mb) {
		byte[] ret = null;
		if(mb.isInSparseFormat()) {
			mb.sparseToDense();
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
	
	public static Object readObjectFromHDFSFile(String filename, ValueType vt) throws IOException {
		String line = null;
		try( BufferedReader br = setupInputFile(filename) ) {
			line = br.readLine();
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
		try( BufferedReader br = setupInputFile(filename) ) {
			// handle multi-line strings in the HDFS file
			String line = null;
			while ( (line = br.readLine()) != null ) {
				sb.append(line);
```

### UnusedAssignment
Variable `ret` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
	{
		FileSystem fs = IOUtilFunctions.getFileSystem(path);
		long ret = 0; //in bytes
		if( fs.getFileStatus(path).isDirectory() )
			ret = fs.getContentSummary(path).getLength();
```

### UnusedAssignment
Variable `obj` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
	public static ScalarObject readScalarObjectFromHDFSFile(String fname, ValueType vt) {
		try {
			Object obj = null;
			switch( vt ) {
				case INT64:   obj = readIntegerFromHDFSFile(fname); break;
```

### UnusedAssignment
Variable `r` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
		if(len == 0)
			return ValueType.UNKNOWN;
		ValueType r = null;
		switch(minType) {
			case UNKNOWN:
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java

		//core matrix reading
		MatrixBlock ret = null;
		try {
			MatrixReader reader = MatrixReaderFactory.createMatrixReader(prop);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
	public static MatrixBlock[] convertToMatrixBlockPartitions( MatrixBlock mb, boolean colwise )
	{
		MatrixBlock[] ret = null;
		int rows = mb.getNumRows();
		int cols = mb.getNumColumns();
```

### UnusedAssignment
Variable `ret` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static long parseToLong( String str )
	{
		long ret = -1;
		if( str.contains(".") )
			ret = toLong( Double.parseDouble(str) );
```

### UnusedAssignment
Variable `ret` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static int parseToInt( String str )
	{
		int ret = -1;
		if( str.contains(".") )
			ret = toInt( Double.parseDouble(str) );
```

### UnusedAssignment
Variable `_curPos` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/iterators/RowIterator.java`
#### Snippet
```java
	protected final T[] _curRow;
	protected final int _maxPos;
	protected int _curPos = -1;

	protected RowIterator(FrameBlock fb, int rl, int ru) {
```

### UnusedAssignment
Variable `_del` initializer `""` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/FastStringTokenizer.java`
#### Snippet
```java
	private static final long serialVersionUID = -4698672725609750097L;
	private String _string = null;
	private String _del = "";
	private int _pos = -1;
	private int _index = 0;
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static WhileStatementBlock createWhileStatementBlockCopy( WhileStatementBlock sb, boolean forceDeepCopy ) 
	{
		WhileStatementBlock ret = null;
		
		try
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static ForStatementBlock createForStatementBlockCopy( ForStatementBlock sb, boolean forceDeepCopy ) 
	{
		ForStatementBlock ret = null;
		
		try
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static IfStatementBlock createIfStatementBlockCopy( IfStatementBlock sb, boolean forceDeepCopy ) 
	{
		IfStatementBlock ret = null;
		
		try
```

### UnusedAssignment
Variable `inst` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static Instruction cloneInstruction( Instruction oInst, long pid, boolean plain, boolean cpFunctions ) 
	{
		Instruction inst = null;
		String tmpString = oInst.toString();
		
```

### UnusedAssignment
Variable `pb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java

	private static ProgramBlock rParseProgramBlock( String in, Program prog, int id ) {
		ProgramBlock pb = null;
		if( in.startsWith(PB_WHILE) )
			pb = rParseWhileProgramBlock( in, prog, id );
```

### UnusedAssignment
Variable `tmpPB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java

	public static ParForProgramBlock createDeepCopyParForProgramBlock(ParForProgramBlock pfpb, long pid, int IDPrefix, Program prog, Set<String> fnStack, Set<String> fnCreated, boolean plain, boolean forceDeepCopy) {
		ParForProgramBlock tmpPB = null;
		
		if( IDPrefix == -1 ) //still on master node
```

### UnusedAssignment
Variable `dat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		ValueType valuetype = ValueType.valueOf( st.nextToken() );
		String valString = st.hasMoreTokens() ? st.nextToken() : "";
		Data dat = null;
		switch( datatype )
		{
```

### UnusedAssignment
Variable `_str` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	private static class HierarchyAwareStringTokenizer //extends StringTokenizer
	{
		private String _str = null;
		private String _del = null;
		private int    _off = -1;
```

### UnusedAssignment
Variable `copy` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	
		//create deep copy
		FunctionProgramBlock copy = null;
		ArrayList<DataIdentifier> tmp1 = new ArrayList<>();
		ArrayList<DataIdentifier> tmp2 = new ArrayList<>();
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		DataType datatype = dat.getDataType();
		ValueType valuetype = dat.getValueType();
		String value = null;
		String[] metaData = null;
		String[] listData = null;
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	
	public static StatementBlock rCreateDeepCopyStatementBlock(StatementBlock sb) {
		StatementBlock ret = null;
		if( sb instanceof IfStatementBlock ) {
			IfStatementBlock orig = (IfStatementBlock) sb;
```

### UnusedAssignment
Variable `_off` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		private String _str = null;
		private String _del = null;
		private int    _off = -1;
		
		public HierarchyAwareStringTokenizer( String in, String delim ) {
```

### UnusedAssignment
Variable `_del` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	{
		private String _str = null;
		private String _del = null;
		private int    _off = -1;
		
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static StatementBlock createStatementBlockCopy( StatementBlock sb, long pid, boolean plain, boolean forceDeepCopy )
	{
		StatementBlock ret = null;
		
		try
```

### UnusedAssignment
Variable `token` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		public String nextToken() {
			int nextDelim = determineNextSameLevelIndexOf(_str, _del);
			String token = null;
			if(nextDelim < 0) {
				nextDelim = _str.length();
```

### UnusedAssignment
Variable `copy1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		
		//create deep copy
		FunctionProgramBlock copy1 = null;
		if( !fnStack.contains(fnameNewKey) ) {
			fnStack.add(fnameNewKey);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java

	private static LocalVariableMap parseVariables(String in) {
		LocalVariableMap ret = null;
		if( in.length()> VARS_BEGIN.length() + VARS_END.length()) {
			String varStr = in.substring( VARS_BEGIN.length(),in.length() - VARS_END.length()).trim();
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			MutableInt rowPos, long rlen, long clen, int blen) throws IOException {

			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
			int col = 0;
			long lnnz = 0;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			MutableInt rowPos, long rlen, long clen, int blen) throws IOException {

			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
			int col = 0;
			long lnnz = 0;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
		protected long readMatrixFromInputStream(InputStream is, String srcInfo, MatrixBlock dest,
			MutableInt rowPos, long rlen, long clen, int blen) throws IOException {
			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
```

### UnusedAssignment
Variable `cellValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			String value = null;
			int row = rowPos.intValue();
			double cellValue = 0;
			int col = 0;
			long lnnz = 0;
```

### UnusedAssignment
Variable `copy` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/ValueTrimFormat.java`
#### Snippet
```java
	// Get a copy of value
	public ValueTrimFormat getACopy() {
		ValueTrimFormat copy = null;
		if(valueType.isNumeric()) {
			copy = new ValueTrimFormat(colIndex, valueType, getDoubleActualValue());
```

### UnusedAssignment
Variable `col` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/MatrixGenerateReader.java`
#### Snippet
```java
			int row = rowPos.intValue();
			double cellValue = 0;
			int col = 0;
			long lnnz = 0;

```

### UnusedAssignment
Variable `col` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/FrameGenerateReader.java`
#### Snippet
```java
			Text value = new Text();
			int row = rl;
			int col = 0;

			// Read the data
```

### UnusedAssignment
Variable `nchwTensorDesc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	public Pointer workSpace = new Pointer();
	public long sizeInBytes = 0;
	cudnnTensorDescriptor nchwTensorDesc = null;
	cudnnTensorDescriptor nkpqTensorDesc = null;
	cudnnFilterDescriptor filterDesc = null;
```

### UnusedAssignment
Variable `convDesc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	cudnnTensorDescriptor nkpqTensorDesc = null;
	cudnnFilterDescriptor filterDesc = null;
	cudnnConvolutionDescriptor convDesc = null;
	GPUContext gCtx = null; String instName = null;
	
```

### UnusedAssignment
Variable `gCtx` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	cudnnFilterDescriptor filterDesc = null;
	cudnnConvolutionDescriptor convDesc = null;
	GPUContext gCtx = null; String instName = null;
	
	private LibMatrixCuDNNConvolutionAlgorithm(GPUContext gCtx, String instName, int N, int C, int H, int W, int K, int R, int S, 
```

### UnusedAssignment
Variable `filterDesc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	cudnnTensorDescriptor nchwTensorDesc = null;
	cudnnTensorDescriptor nkpqTensorDesc = null;
	cudnnFilterDescriptor filterDesc = null;
	cudnnConvolutionDescriptor convDesc = null;
	GPUContext gCtx = null; String instName = null;
```

### UnusedAssignment
Variable `nkpqTensorDesc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	public long sizeInBytes = 0;
	cudnnTensorDescriptor nchwTensorDesc = null;
	cudnnTensorDescriptor nkpqTensorDesc = null;
	cudnnFilterDescriptor filterDesc = null;
	cudnnConvolutionDescriptor convDesc = null;
```

### UnusedAssignment
Variable `instName` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNConvolutionAlgorithm.java`
#### Snippet
```java
	cudnnFilterDescriptor filterDesc = null;
	cudnnConvolutionDescriptor convDesc = null;
	GPUContext gCtx = null; String instName = null;
	
	private LibMatrixCuDNNConvolutionAlgorithm(GPUContext gCtx, String instName, int N, int C, int H, int W, int K, int R, int S, 
```

### UnusedAssignment
Variable `spair` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/RawRow.java`
#### Snippet
```java
		int currentNumericLastIndex = numericLastIndex;
		int currentRawLastIndex = rawLastIndex;
		Pair<Integer, Integer> spair = null;
		Pair<Integer, Integer> epair = null;
		ValueTrimFormat snode = vtfs.get(0);
```

### UnusedAssignment
Variable `outputArray` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNIm2Col.java`
#### Snippet
```java
		}
		
		double [] outputArray = null;
		if (!params.output.isInSparseFormat())
			outputArray = params.output.getDenseBlockValues();
```

### UnusedAssignment
Variable `_del` initializer `""` is redundant
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
		private static final long serialVersionUID = -4698672725609750097L;
		private String _string = null;
		private String _del = "";
		private int _pos = -1;
		private int _count = 0;
```

### UnusedAssignment
Variable `outRow` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	public static void startAggregation(MatrixValue valueOut, MatrixValue correction, AggregateOperator op, 
			int rlen, int clen, boolean sparseHint, boolean embeddedCorrection) {
		int outRow=0, outCol=0, corRow=0, corCol=0;
		if(!embeddedCorrection || op.existsCorrection())
		{
```

### UnusedAssignment
Variable `outCol` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	public static void startAggregation(MatrixValue valueOut, MatrixValue correction, AggregateOperator op, 
			int rlen, int clen, boolean sparseHint, boolean embeddedCorrection) {
		int outRow=0, outCol=0, corRow=0, corCol=0;
		if(!embeddedCorrection || op.existsCorrection())
		{
```

### UnusedAssignment
Variable `corRow` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	public static void startAggregation(MatrixValue valueOut, MatrixValue correction, AggregateOperator op, 
			int rlen, int clen, boolean sparseHint, boolean embeddedCorrection) {
		int outRow=0, outCol=0, corRow=0, corCol=0;
		if(!embeddedCorrection || op.existsCorrection())
		{
```

### UnusedAssignment
Variable `corCol` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	public static void startAggregation(MatrixValue valueOut, MatrixValue correction, AggregateOperator op, 
			int rlen, int clen, boolean sparseHint, boolean embeddedCorrection) {
		int outRow=0, outCol=0, corRow=0, corCol=0;
		if(!embeddedCorrection || op.existsCorrection())
		{
```

### UnusedAssignment
Variable `_bSeed` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private MatrixBlock _out = null;
		private RandomMatrixGenerator _rgen = new RandomMatrixGenerator();
		private long _bSeed = 0;
		private long[] _seeds = null;
		
```

### UnusedAssignment
Variable `rgen` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
	public static RandomMatrixGenerator createRandomMatrixGenerator(String pdfStr, int r, int c, int blen, double sp, double min, double max, String distParams) {
		RandomMatrixGenerator.PDF pdf = RandomMatrixGenerator.PDF.valueOf(pdfStr.toUpperCase());
		RandomMatrixGenerator rgen = null;
		switch (pdf) {
			case UNIFORM:
```

### UnusedAssignment
Variable `mean` initializer `Double.NaN` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
				break;
			case POISSON:
				double mean = Double.NaN;
				try {
					mean = Double.parseDouble(distParams);
```

### UnusedAssignment
Variable `_out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private int _cl = -1;
		private int _cu = -1;
		private MatrixBlock _out = null;
		private RandomMatrixGenerator _rgen = new RandomMatrixGenerator();
		private long _bSeed = 0;
```

### UnusedAssignment
Variable `_cu` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
		private MatrixBlock _out = null;
		private RandomMatrixGenerator _rgen = new RandomMatrixGenerator();
```

### UnusedAssignment
Variable `_seeds` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private RandomMatrixGenerator _rgen = new RandomMatrixGenerator();
		private long _bSeed = 0;
		private long[] _seeds = null;
		
		public RandTask(int rl, int ru, int cl, int cu, MatrixBlock out, RandomMatrixGenerator rgen, long bSeed, long[] seeds) {
```

### UnusedAssignment
Variable `_cl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
		private MatrixBlock _out = null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
	{
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
	private static class RandTask implements Callable<Long> 
	{
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
```

### UnusedAssignment
Variable `output` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuMatMult.java`
#### Snippet
```java
			boolean isLeftTransposed, boolean isRightTransposed) {
		// t(C) = t(B) %*% t(A)
		Pointer output = null;
		if (outRLen != 1 && outCLen != 1) {
			output = gCtx.allocate(instName, outRLen * outCLen * sizeOfDataType, false);
```

### UnusedAssignment
Variable `blkOut` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixSketch.java`
#### Snippet
```java
		int clen = blkIn.getNumColumns();

		MatrixBlock blkOut = null;
		switch (dir) {
			case RowCol:
```

### UnusedAssignment
Variable `nnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
			DenseBlock a = m1.getDenseBlock();
			double[] cvals = ret.getDenseBlockValues();
			long nnz = 0;
			if( a.isContiguous() ) {
				nnz = NativeHelper.tsmm(a.valuesAt(0),
```

### UnusedAssignment
Variable `rnnModeVal` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNRnnAlgorithm.java`
#### Snippet
```java
	
	private static int getCuDNNRnnMode(String rnnMode) throws DMLRuntimeException {
		int rnnModeVal = -1;
		if(rnnMode.equalsIgnoreCase("rnn_relu")) {
			rnnModeVal = jcuda.jcudnn.cudnnRNNMode.CUDNN_RNN_RELU;
```

### UnusedAssignment
Variable `ret` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNRnnAlgorithm.java`
#### Snippet
```java
	@SuppressWarnings("unused")
	private static int getNumLinearLayers(String rnnMode) throws DMLRuntimeException {
		int ret = 0;
		if(rnnMode.equalsIgnoreCase("rnn_relu") || rnnMode.equalsIgnoreCase("rnn_tanh")) {
			ret = 2;
```

### UnusedAssignment
Variable `_maxCol` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/CTableMap.java`
#### Snippet
```java
	private final LongLongDoubleHashMap _map;
	private long _maxRow = -1;
	private long _maxCol = -1;
	
	public CTableMap() {
```

### UnusedAssignment
Variable `_maxRow` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/CTableMap.java`
#### Snippet
```java
{
	private final LongLongDoubleHashMap _map;
	private long _maxRow = -1;
	private long _maxCol = -1;
	
```

### UnusedAssignment
Variable `im2colPointer` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
	private static Pointer denseIm2col(GPUContext gCtx, String instName, MatrixObject image, boolean isSparseImage, long N, long C, long H, long W,
			int R, int S, int pad_h, int pad_w, int stride_h, int stride_w, int P, int Q) {
		Pointer im2colPointer = null;
		if(isSparseImage) {
			CSRPointer inPointer = getSparsePointer(gCtx, image, instName);
```

### UnusedAssignment
Variable `eigendecompose` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibCommonsMath.java`
#### Snippet
```java
		}
		
		EigenDecomposition eigendecompose = null;
		try {
			Array2DRowRealMatrix matrixInput = DataConverter.convertToArray2DRowRealMatrix(in);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
	 */
	private static cudnnTensorDescriptor allocateNCHWDescriptors(GPUContext gCtx, int N, int C, long CHW, MatrixObject [] input, MatrixObject [] output) throws DMLRuntimeException {
		cudnnTensorDescriptor ret  = null; // Return any one
		if(CHW > ((long)Integer.MAX_VALUE)*C) {
			throw new DMLRuntimeException("image size (height*width) should be less than " + Integer.MAX_VALUE);
```

### UnusedAssignment
Variable `H` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			throw new DMLRuntimeException("image size (height*width) should be less than " + Integer.MAX_VALUE);
		}
		int H = -1; int W = -1;
		int HW = (int) (CHW / C);
		H = HW; W = 1; // If not known
```

### UnusedAssignment
Variable `W` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			throw new DMLRuntimeException("image size (height*width) should be less than " + Integer.MAX_VALUE);
		}
		int H = -1; int W = -1;
		int HW = (int) (CHW / C);
		H = HW; W = 1; // If not known
```

### UnusedAssignment
Variable `cnt` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	private static int sumRowSumGtLeColSumLtGe(double value, double[] bv, BinaryOperator bOp) {
		int ix = Arrays.binarySearch(bv, value);
		int cnt = 0;
		if( ix >= 0 ){ //match, scan to next val
			while( ix > 0 && value==bv[ix-1]) --ix;
```

### UnusedAssignment
Variable `cnt` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	private static int sumRowSumLtGeColSumGtLe(double value, double[] bv, BinaryOperator bOp) {
		int ix = Arrays.binarySearch(bv, value);
		int cnt = 0;
		
		if( ix >= 0 ){ //match, scan to next val
```

### UnusedAssignment
Variable `nnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java

		//execute binary cell operations
		long nnz = 0;
		if(op.sparseSafe || isSparseSafeDivide(op, m2))
			nnz = safeBinary(m1, m2, ret, op, atype, 0, m1.rlen);
```

### UnusedAssignment
Variable `lnnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
		int m = m1.rlen;
		int n = m1.clen;
		long lnnz = 0;
		
		if( m1.sparse ) //SPARSE MATRIX
```

### UnusedAssignment
Variable `lnnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
			|| op.fn instanceof Power2 || Builtin.isBuiltinCode(op.fn, BuiltinCode.MAX)
			|| Builtin.isBuiltinCode(op.fn, BuiltinCode.MIN));
		long lnnz = 0;
		
		if( m1.sparse ) //SPARSE <- SPARSE
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java

	public static SpoofOperator createInstance(Class<?> cla) {
		SpoofOperator ret = null;

		try {
```

### UnusedAssignment
Variable `kernel` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		int retClen = cu - cl + 1;
		
		String kernel = null;
		// Note: row-wise parallelization scheme iterates over input rows in single thread 
		// whereas nnz parallelization scheme iterates over number of output rows in single thread.
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

			// Allocated the dense output matrix
			MatrixObject out = null;
			try {
				out = getDenseMatrixOutputForGPUInstruction(ec, instName, outputName, outRLen, outCLen);
```

### UnusedAssignment
Variable `duration` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		Timing time = new Timing(false);
		double duration = 0;
		if(LOG.isTraceEnabled()) { time.start(); }

```

### UnusedAssignment
Variable `reductionDirection` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		// Convert Reduction direction to a number
		int reductionDirection = -1;
		if (indexFn instanceof ReduceAll){
			reductionDirection = REDUCTION_ALL;
```

### UnusedAssignment
Variable `opIndex` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		// Convert function type to a number
		int opIndex = -1;
		if (aggOp.increOp.fn instanceof KahanPlus) {
			opIndex = OP_PLUS;
```

### UnusedAssignment
Variable `duration` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		if (blocks_y > 1) {
			Timing time = new Timing(false);
			double duration = 0;
			double alloc_duration = 0;
			double total_duration = 0;
```

### UnusedAssignment
Variable `duration` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

			Timing time = new Timing(true);
			double duration = 0;

			getCudaKernels(gCtx).launchKernel(kernelFunction + "_down_sweep",
```

### UnusedAssignment
Variable `duration` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		Timing time = new Timing(false);
		double duration = 0;
		if(LOG.isTraceEnabled()) { time.start(); }

```

### UnusedAssignment
Variable `estnnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		
		//account for matrix vector and vector/vector
		long estnnz = 0;
		if( atype == BinaryAccessType.OUTER_VECTOR_VECTOR )
		{
```

### UnusedAssignment
Variable `cix` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
		
		final int blocksizeIJ = 16; //u/v block (max at typical L2 size) 
		int cix = 0;
		//blocked execution
		for( int bi = rl; bi < ru; bi+=blocksizeIJ )
```

### UnusedAssignment
Variable `_hash` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
	private LineageItem[] _inputs;
	private long _height = 0; //distance leaf to node
	private int _hash = 0;
	private LineageItem _dedupPatch;
	private final BooleanArray32 _specialValueBits;  // TODO: Move this to a new subclass
```

### UnusedAssignment
Variable `_height` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
	private final String _data;
	private LineageItem[] _inputs;
	private long _height = 0; //distance leaf to node
	private int _hash = 0;
	private LineageItem _dedupPatch;
```

### UnusedAssignment
Variable `_cnt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private int _rl = -1;
		private int _ru = -1;
		private int[] _cnt = null;

		protected TransposeTask(MatrixBlock in, MatrixBlock out, boolean row, int rl, int ru, int[] cnt) {
```

### UnusedAssignment
Variable `values` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				int[] rptr = new int[rows+1];
				int[] indexes = new int[(int)a.size()];
				double[] values = null;
				rptr[0] = 0;
				
```

### UnusedAssignment
Variable `_col` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	{
		private MatrixBlock _mb = null;
		private int _col = -1;
		
		public AscRowComparator( MatrixBlock mb, int col )
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	public static MatrixBlock transposeInPlace(MatrixBlock in, int k){
		// Timing time = new Timing(true);
		MatrixBlock out = null;
		if(in.isEmpty()) 
			out = new MatrixBlock(in.getNumColumns(), in.getNumRows(), true);
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	{
		private MatrixBlock _in = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	{
		private MatrixBlock _in = null;
		private MatrixBlock _out = null;
		private boolean _row = false;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private boolean _row = false;
		private int _rl = -1;
		private int _ru = -1;
		private int[] _cnt = null;

```

### UnusedAssignment
Variable `_col` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	{
		private MatrixBlock _mb = null;
		private int _col = -1;
		
		public DescRowComparator( MatrixBlock mb, int col )
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private MatrixBlock _out = null;
		private boolean _row = false;
		private int _rl = -1;
		private int _ru = -1;
		private int[] _cnt = null;
```

### UnusedAssignment
Variable `_in` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static class CountNnzTask implements Callable<int[]>
	{
		private MatrixBlock _in = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private MatrixBlock _in = null;
		private int _rl = -1;
		private int _ru = -1;

		protected CountNnzTask(MatrixBlock in, int rl, int ru) {
```

### UnusedAssignment
Variable `cix` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			if( SPARSE_OUTPUTS_IN_CSR ) {
				int[] rptr = new int[in.rlen+1];
				int[] cix = null;
				double[] vals = null;
				//case a: fully dense vector
```

### UnusedAssignment
Variable `vals` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				int[] rptr = new int[in.rlen+1];
				int[] cix = null;
				double[] vals = null;
				//case a: fully dense vector
				if( rlen == in.nonZeros && !in.sparse ) {
```

### UnusedAssignment
Variable `flags` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//Step 1: scan block and determine non-empty columns 
		//(we optimized for cache-friendly behavior and hence don't do early abort)
		boolean[] flags = null; 
		
		if (select == null) 
```

### UnusedAssignment
Variable `_row` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private MatrixBlock _in = null;
		private MatrixBlock _out = null;
		private boolean _row = false;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static class AscRowComparator implements Comparator<Integer> 
	{
		private MatrixBlock _mb = null;
		private int _col = -1;
		
```

### UnusedAssignment
Variable `_mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static class DescRowComparator implements Comparator<Integer> 
	{
		private MatrixBlock _mb = null;
		private int _col = -1;
		
```

### UnusedAssignment
Variable `_in` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static class TransposeTask implements Callable<Object>
	{
		private MatrixBlock _in = null;
		private MatrixBlock _out = null;
		private boolean _row = false;
```

### UnusedAssignment
Variable `flags` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		final int m = in.rlen;
		final int n = in.clen;
		boolean[] flags = null;
		int rlen2 = 0; 
		
```

### UnusedAssignment
Variable `mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
		LineageCacheEntry e = cache.get(key);
		long t0 = System.nanoTime();
		MatrixBlock mb = null;
		// Read from local FS
		try {
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;

		protected PartialAggTask( MatrixBlock in, MatrixBlock ret, AggType aggtype, AggregateUnaryOperator uaop, int rl, int ru ) {
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_uaop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_aggtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_uop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private UnaryOperator _uop = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private UnaryOperator _uop = null;
		private int _rl = -1;
		private int _ru = -1;

		protected CumAggTask( MatrixBlock in, double[] agg, MatrixBlock ret, AggType aggtype, UnaryOperator uop, int rl, int ru ) {
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;

		protected RowAggTask( MatrixBlock in, MatrixBlock ret, AggType aggtype, AggregateUnaryOperator uaop, int rl, int ru )
```

### UnusedAssignment
The value `alen` assigned to `count` is never used
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
			int alen = (int) a.size(rl, ru);
			mean(a.values(rl), a.pos(rl), alen, count, kbuff, kmean);
			count += alen;
		}
		else {
```

### UnusedAssignment
Variable `_aggtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private double[] _agg = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private UnaryOperator _uop = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	{
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private final MatrixBlock _in2;
		private final MatrixBlock _in3;
		private MatrixBlock _ret = null;
		private final IndexFunction _ixFn;
		private final int _rl;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _target  = null;
		private MatrixBlock _weights  = null;
		private MatrixBlock _ret  = null;
		private int _numGroups = -1;
		private Operator _op = null;
```

### UnusedAssignment
Variable `_numGroups` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _weights  = null;
		private MatrixBlock _ret  = null;
		private int _numGroups = -1;
		private Operator _op = null;
		private int _cl = -1;
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _ret  = null;
		private int _numGroups = -1;
		private Operator _op = null;
		private int _cl = -1;
		private int _cu = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private AggType _aggtype = null;
		private UnaryOperator _uop = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_in` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static class CumAggTask implements Callable<Long> 
	{
		private MatrixBlock _in  = null;
		private double[] _agg = null;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	{
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
```

### UnusedAssignment
Variable `_cl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private int _numGroups = -1;
		private Operator _op = null;
		private int _cl = -1;
		private int _cu = -1;

```

### UnusedAssignment
Variable `_in` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static class PartialAggTask extends AggTask 
	{
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
```

### UnusedAssignment
Variable `_cu` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private Operator _op = null;
		private int _cl = -1;
		private int _cu = -1;

		protected GrpAggTask( MatrixBlock groups, MatrixBlock target, MatrixBlock weights, MatrixBlock ret, int numGroups, Operator op, int cl, int cu ) {
```

### UnusedAssignment
Variable `_groups` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static class GrpAggTask extends AggTask 
	{
		private MatrixBlock _groups  = null;
		private MatrixBlock _target  = null;
		private MatrixBlock _weights  = null;
```

### UnusedAssignment
Variable `_uaop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_in` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static class RowAggTask extends AggTask 
	{
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
```

### UnusedAssignment
Variable `_weights` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _groups  = null;
		private MatrixBlock _target  = null;
		private MatrixBlock _weights  = null;
		private MatrixBlock _ret  = null;
		private int _numGroups = -1;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _in  = null;
		private double[] _agg = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private UnaryOperator _uop = null;
```

### UnusedAssignment
Variable `_agg` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	{
		private MatrixBlock _in  = null;
		private double[] _agg = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
```

### UnusedAssignment
Variable `val` initializer `Double.NaN` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//handle all full aggregates over matrices with zero rows or columns
		if( ixFn instanceof ReduceAll && (in.getNumRows() == 0 || in.getNumColumns() == 0) ) {
			double val = Double.NaN;
			switch( optype ) {
				case PROD:         val = 1; break;
```

### UnusedAssignment
Variable `_aggtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		private MatrixBlock _in  = null;
		private MatrixBlock _ret = null;
		private AggType _aggtype = null;
		private AggregateUnaryOperator _uaop = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_target` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	{
		private MatrixBlock _groups  = null;
		private MatrixBlock _target  = null;
		private MatrixBlock _weights  = null;
		private MatrixBlock _ret  = null;
```

### UnusedAssignment
The value `null` assigned to `li` is never used
in `src/main/java/org/apache/sysds/runtime/lineage/LineageParser.java`
#### Snippet
```java
		
		for (String line : str.split("\\r?\\n")) {
			li = null;
			Map<String, String> tokens = lineageTraceTokenizer.tokenize(line);
			Long id = Long.valueOf(tokens.get("id"));
```

### UnusedAssignment
Variable `datasize` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
			if (boundLI != null) {
				Data data = ec.getVariable(boundVarName);
				long datasize = 0;
				if (data instanceof MatrixObject)
					datasize = (((MatrixObject)data).acquireReadAndRelease()).getInMemorySize();
```

### UnusedAssignment
Variable `datasize` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
		// Gather the size of this intermediate
		Data data = ec.getVariable(((ComputationCPInstruction) inst).output);
		long datasize = 0;
		if (data instanceof MatrixObject)
			datasize = (((MatrixObject)data).acquireReadAndRelease()).getInMemorySize();
```

### UnusedAssignment
Variable `_outfile` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEntry.java`
#### Snippet
```java
	protected LineageCacheEntry _nextEntry;
	protected LineageItem _origItem;
	private String _outfile = null;
	protected double score;
	protected GPUPointer _gpuPointer;
```

### UnusedAssignment
Variable `REUSE_OPCODES` initializer `new String[] {}` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
	};

	private static String[] REUSE_OPCODES  = new String[] {};
	private static String[] CHKPOINT_OPCODES  = new String[] {};

```

### UnusedAssignment
Variable `CHKPOINT_OPCODES` initializer `new String[] {}` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java

	private static String[] REUSE_OPCODES  = new String[] {};
	private static String[] CHKPOINT_OPCODES  = new String[] {};

	public enum ReuseCacheType {
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		LineageItem li = LineageItemUtils.getSerializedFedResponseLineageItem(objLI);

		LineageCacheEntry e = null;
		synchronized(_cache) {
			if(LineageCache.probe(li)) {
```

### UnusedAssignment
Variable `liList` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		//TODO: Replace with generic type

		List<MutablePair<LineageItem, LineageCacheEntry>> liList = null;
		LineageItem instLI = (cinst != null) ? cinst.getLineageItem(ec).getValue()
			: (cfinst != null) ? cfinst.getLineageItem(ec).getValue()
```

### UnusedAssignment
Variable `reuse` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		//TODO: reuse only those UDFs which are part of reusable instructions
		
		boolean reuse = false;
		List<Long> outIds = udf.getOutputIds();
		HashMap<String, Data> udfOutputs = new HashMap<>();
```

### UnusedAssignment
Variable `outValue` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		if (e != null) {
			String outName = String.valueOf(outIds.get(0));
			Data outValue = null;
			//convert to matrix object
			if (e.isMatrixValue()) {
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			return false;

		LineageCacheEntry e = null;
		synchronized(_cache) {
			if(LineageCache.probe(li)) {
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java

	public static LineageCacheEntry getEntry(LineageItem key) {
		LineageCacheEntry e = null;
		synchronized( _cache ) {
			e = getIntern(key);
```

### UnusedAssignment
Variable `e` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
	
	public static MatrixBlock getMatrix(LineageItem key) {
		LineageCacheEntry e = null;
		synchronized( _cache ) {
			e = getIntern(key);
```

### UnusedAssignment
Variable `lossy` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettingsBuilder.java`
#### Snippet
```java
	private String transposeInput;
	private int seed = -1;
	private boolean lossy = false;
	private EnumSet<CompressionType> validCompressions;
	private boolean sortValuesByLength = true;
```

### UnusedAssignment
Variable `seed` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettingsBuilder.java`
#### Snippet
```java
	private boolean allowSharedDictionary = false;
	private String transposeInput;
	private int seed = -1;
	private boolean lossy = false;
	private EnumSet<CompressionType> validCompressions;
```

### UnusedAssignment
The value `false` assigned to `transposeLeft` is never used
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMatrixMult.java`
#### Snippet
```java
			if(!(m1 instanceof CompressedMatrixBlock) && transposeLeft) {
				m1 = LibMatrixReorg.transpose(m1, k);
				transposeLeft = false;
			}
			else if(!(m2 instanceof CompressedMatrixBlock) && transposeRight) {
```

### UnusedAssignment
The value `false` assigned to `transposeRight` is never used
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMatrixMult.java`
#### Snippet
```java
			else if(!(m2 instanceof CompressedMatrixBlock) && transposeRight) {
				m2 = LibMatrixReorg.transpose(m2, k);
				transposeRight = false;
			}
		}
```

### UnusedAssignment
The value `HopRewriteUtils.createTransientRead("deltaX", cachedRI)` assigned to `lastRow` is never used
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			MatrixBlock cachedRI = inCache.get("deltaX");
			lrwec.setVariable("deltaX", toMatrixObject(cachedRI));
			lastRow = HopRewriteUtils.createTransientRead("deltaX", cachedRI);
		}
		lastRow = HopRewriteUtils.createIndexingOp(leftMatrix, new LiteralOp(moL.getNumRows()), 
```

### UnusedAssignment
Variable `old_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
	
	private static LineageItem reduceColByOne(LineageItem cu) {
		String old_data = null;
		try {
			String data = cu.getData();  //xx·SCALAR·INT64·true
```

### UnusedAssignment
The value `HopRewriteUtils.createTransientRead("deltaY", cachedRI)` assigned to `lastCol` is never used
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			MatrixBlock cachedRI = inCache.get("deltaY");
			lrwec.setVariable("deltaY", toMatrixObject(cachedRI));
			lastCol = HopRewriteUtils.createTransientRead("deltaY", cachedRI);
		}
		lastCol = HopRewriteUtils.createIndexingOp(rightMatrix, new LiteralOp(1), new LiteralOp(moR.getNumRows()), 
```

### UnusedAssignment
Variable `_mW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mW = null;
		private MatrixBlock _ret = null;
		private WeightsType _wt = null;
```

### UnusedAssignment
Variable `_pm1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultPermuteTask implements Callable<Object> 
	{
		private MatrixBlock _pm1  = null;
		private MatrixBlock _m2 = null;
		private MatrixBlock _ret1 = null;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _m3  = null;
		private ChainType _ct = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private ValueFunction _fn = null;
		private int _rl = -1;
		private int _ru = -1;
		
		protected MatrixMultWuTask(MatrixBlock mW, MatrixBlock mU, MatrixBlock mV, MatrixBlock ret, WUMMType wt, ValueFunction fn, int rl, int ru) {
```

### UnusedAssignment
Variable `_mW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultWSigmoidTask implements Callable<Long> 
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private WeightsType _wt = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_mV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WSigmoidType _wt = null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret2 = null;
		private int _rl = -1;
		private int _ru = -1;

		protected MatrixMultPermuteTask( MatrixBlock pm1, MatrixBlock m2, MatrixBlock ret1, MatrixBlock ret2, int rl, int ru)
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private WUMMType _wt = null;
		private ValueFunction _fn = null;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_mW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultWCeTask implements Callable<Double>
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private WeightsType _wt = null;
		private int _rl = -1;
		private int _ru = -1;

		protected MatrixMultWSLossTask(MatrixBlock mX, MatrixBlock mU, MatrixBlock mV, MatrixBlock mW, WeightsType wt, int rl, int ru) {
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mV = null;
		private MatrixBlock _mX = null;
		private MatrixBlock _ret = null;
		private WDivMMType _wt = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_mU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `_mX` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultWSLossTask implements Callable<Double>
	{
		private MatrixBlock _mX = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WUMMType _wt = null;
		private ValueFunction _fn = null;
```

### UnusedAssignment
Variable `_m3` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _m1  = null;
		private MatrixBlock _m2  = null;
		private MatrixBlock _m3  = null;
		private ChainType _ct = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret1 = null;
		private MatrixBlock _ret2 = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
The value `m1.clen` assigned to `m` is never used
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				//direct computation with IJK (no dependencies/branches in inner loop)
				//see preprocessMatrixMultTransposeSelf m1<-tmpBlock
				m = m1.clen;
				
				//algorithm: scan rows, foreach row self join (KIJ)
```

### UnusedAssignment
Variable `_wt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WSigmoidType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private WSigmoidType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
		
		protected MatrixMultWSigmoidTask(MatrixBlock mW, MatrixBlock mU, MatrixBlock mV, MatrixBlock ret, WSigmoidType wt, int rl, int ru) {
```

### UnusedAssignment
Variable `_mV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WUMMType _wt = null;
```

### UnusedAssignment
Variable `_mU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _mX = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mW = null;
```

### UnusedAssignment
Variable `_wt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mW = null;
		private MatrixBlock _ret = null;
		private WeightsType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_fn` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private WUMMType _wt = null;
		private ValueFunction _fn = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private WSigmoidType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
		
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private final MatrixBlock _m1;
		private final MatrixBlock _m2;
		private MatrixBlock _ret = null;
		private final boolean _tm2; //transposed m2
		private final boolean _pm2r; //par over m2 rows
```

### UnusedAssignment
Variable `_mV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mX = null;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `_wt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mX = null;
		private MatrixBlock _ret = null;
		private WDivMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_mW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultWDivTask implements Callable<Long> 
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
```

### UnusedAssignment
Variable `_m2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _m1  = null;
		private MatrixBlock _m2  = null;
		private MatrixBlock _m3  = null;
		private ChainType _ct = null;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mV = null;
		private double _eps = 0.0;
		private MatrixBlock _ret = null;
		private WCeMMType _wt = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private ChainType _ct = null;
		private int _rl = -1;
		private int _ru = -1;

		protected MatrixMultChainTask( MatrixBlock mX, MatrixBlock mV, MatrixBlock mW, ChainType ct, int rl, int ru ) {
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private WCeMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;

		protected MatrixMultWCeTask(MatrixBlock mW, MatrixBlock mU, MatrixBlock mV, double eps, WCeMMType wt, int rl, int ru) {
```

### UnusedAssignment
Variable `_cl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
		
```

### UnusedAssignment
Variable `_mX` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mX = null;
		private MatrixBlock _ret = null;
		private WDivMMType _wt = null;
```

### UnusedAssignment
Variable `_mW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultWuTask implements Callable<Long> 
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
```

### UnusedAssignment
Variable `_mU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `_wt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WUMMType _wt = null;
		private ValueFunction _fn = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private WCeMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;

```

### UnusedAssignment
Variable `_eps` initializer `0.0` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private double _eps = 0.0;
		private MatrixBlock _ret = null;
		private WCeMMType _wt = null;
```

### UnusedAssignment
Variable `_m1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	private static class MatrixMultChainTask implements Callable<double[]> 
	{
		private MatrixBlock _m1  = null;
		private MatrixBlock _m2  = null;
		private MatrixBlock _m3  = null;
```

### UnusedAssignment
Variable `_ret2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _m2 = null;
		private MatrixBlock _ret1 = null;
		private MatrixBlock _ret2 = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_ct` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _m2  = null;
		private MatrixBlock _m3  = null;
		private ChainType _ct = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _ret = null;
		private WSigmoidType _wt = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_cu` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
		
		protected MatrixMultWDivTask(MatrixBlock mW, MatrixBlock mU, MatrixBlock mV, MatrixBlock mX, MatrixBlock ret, WDivMMType wt, int rl, int ru, int cl, int cu) {
```

### UnusedAssignment
Variable `_ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mV = null;
		private MatrixBlock _mW = null;
		private MatrixBlock _ret = null;
		private WeightsType _wt = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_ret1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _pm1  = null;
		private MatrixBlock _m2 = null;
		private MatrixBlock _ret1 = null;
		private MatrixBlock _ret2 = null;
		private int _rl = -1;
```

### UnusedAssignment
Variable `_ru` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private WDivMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
		private int _cu = -1;
```

### UnusedAssignment
Variable `_rl` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _ret = null;
		private WDivMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
		private int _cl = -1;
```

### UnusedAssignment
Variable `_wt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private double _eps = 0.0;
		private MatrixBlock _ret = null;
		private WCeMMType _wt = null;
		private int _rl = -1;
		private int _ru = -1;
```

### UnusedAssignment
Variable `_mU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mX = null;
```

### UnusedAssignment
Variable `_m2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _pm1  = null;
		private MatrixBlock _m2 = null;
		private MatrixBlock _ret1 = null;
		private MatrixBlock _ret2 = null;
```

### UnusedAssignment
Variable `_mU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
	{
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private double _eps = 0.0;
```

### UnusedAssignment
Variable `_mV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mX = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private MatrixBlock _mW = null;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `_mV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		private MatrixBlock _mW = null;
		private MatrixBlock _mU = null;
		private MatrixBlock _mV = null;
		private double _eps = 0.0;
		private MatrixBlock _ret = null;
```

### UnusedAssignment
Variable `p1` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/ShlosserEstimator.java`
#### Snippet
```java
		final double oneMinusQ = 1 - q;

		double numberSum = 0, denomSum = 0, p1 = 0;

		int i = 0;
```

### UnusedAssignment
Variable `d` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java
		// compute gamma based on Duj1
		double gamma = getGammaSquared(duj1, freqCounts, sampleSize, nRows);
		double d = -1;

		// core hybrid estimator based on gamma
```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/IntArrayList.java`
#### Snippet
```java
	private static final int RESIZE_FACTOR = 2;

	private int[] _data = null;
	private int _size;

```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java

	private static CompressedMatrixBlock setupCompressedReturnMatrixBlock(CompressedMatrixBlock m1, MatrixValue result) {
		CompressedMatrixBlock ret = null;
		if(result == null || !(result instanceof CompressedMatrixBlock))
			ret = new CompressedMatrixBlock(m1.getNumRows(), m1.getNumColumns());
```

### UnusedAssignment
Variable `nnz` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java

		final int k = op.getNumThreads();
		long nnz = 0;

		if(k <= 1)
```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java

	protected int _size = -1;
	private Bucket[] _data = null;

	public DblArrayCountHashMap(int init_capacity, int cols) {
```

### UnusedAssignment
Variable `_size` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java
	// public static int hashMissCount = 0;

	protected int _size = -1;
	private Bucket[] _data = null;

```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java

	protected int _size = -1;
	private Bucket[] _data = null;

	public DoubleCountHashMap(int init_capacity) {
```

### UnusedAssignment
Variable `_size` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
	protected static final float LOAD_FACTOR = 0.80f;

	protected int _size = -1;
	private Bucket[] _data = null;

```

### UnusedAssignment
Variable `numRuns` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstSample.java`
#### Snippet
```java
		// naive approach.
		final double nRunsInSample = sampleFacts.numRuns;
		double numRuns = 0;

		// process frequency maps.
```

### UnusedAssignment
Variable `next` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
		public double key = Double.MAX_VALUE;
		public IntArrayList value = null;
		public DIListEntry next = null;

		public DIListEntry(double ekey, IntArrayList evalue) {
```

### UnusedAssignment
Variable `key` initializer `Double.MAX_VALUE` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java

	public static class DIListEntry implements Comparator<DIListEntry>, Comparable<DIListEntry> {
		public double key = Double.MAX_VALUE;
		public IntArrayList value = null;
		public DIListEntry next = null;
```

### UnusedAssignment
Variable `_size` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
	protected static final int RESIZE_FACTOR = 2;
	protected static final float LOAD_FACTOR = 0.50f;
	protected int _size = -1;
	private DIListEntry[] _data = null;
	public static int hashMissCount = 0;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
	public static class DIListEntry implements Comparator<DIListEntry>, Comparable<DIListEntry> {
		public double key = Double.MAX_VALUE;
		public IntArrayList value = null;
		public DIListEntry next = null;

```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
	protected static final float LOAD_FACTOR = 0.50f;
	protected int _size = -1;
	private DIListEntry[] _data = null;
	public static int hashMissCount = 0;

```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java
	protected int _size = -1;

	protected DArrayIListEntry[] _data = null;

	public DblArrayIntListHashMap() {
```

### UnusedAssignment
Variable `_size` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java
	public static int hashMissCount = 0;

	protected int _size = -1;

	protected DArrayIListEntry[] _data = null;
```

### UnusedAssignment
Variable `cellVals` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/BitmapEncoder.java`
#### Snippet
```java
		final ReaderColumnSelection reader = ReaderColumnSelection.createReader(rawBlock, colIndices, transposed);

		DblArray cellVals = null;
		while((cellVals = reader.nextRow()) != null)
			map.appendValue(cellVals, reader.getCurrentRowIndex());
```

### UnusedAssignment
Variable `l` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodePriorityQue.java`
#### Snippet
```java

		List<CompressedSizeInfoColGroup> ret = new ArrayList<>();
		CompressedSizeInfoColGroup l = null;
		l = que.poll();
		int groupNr = ret.size() + que.size();
```

### UnusedAssignment
Variable `cellVals` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java
		DblArrayCountHashMap map = new DblArrayCountHashMap(mapStartSize, subCols.size());

		DblArray cellVals = null;
		AMapToData data = MapToFactory.create(offsetsInt.length, 257);

```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
			final AIterator itThis = _indexes.getIterator();
			final int blen = that._ptr[k + 1];
			for(int apos = that._ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + that._data[apos];
				re = rs + that._data[apos + 1];
```

### UnusedAssignment
Variable `initialized` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/ConstScheme.java`
#### Snippet
```java

	final double[] vals;
	boolean initialized = false;

	private ConstScheme(IColIndex cols, double[] vals, boolean initialized) {
```

### UnusedAssignment
Variable `d` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/DDCSchemeMC.java`
#### Snippet
```java
		final ReaderColumnSelection r = ReaderColumnSelection.createReader(mbDict, //
			ColIndexFactory.create(dictCols), false, 0, dictRows);
		DblArray d = null;
		while((d = r.nextRow()) != null)
			map.increment(d);
```

### UnusedAssignment
Variable `d` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/DDCSchemeMC.java`
#### Snippet
```java
		final ReaderColumnSelection reader = ReaderColumnSelection.createReader(//
			data, columns, false, 0, nRow);
		DblArray d = null;
		int r = 0;
		while((d = reader.nextRow()) != null) {
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
			final AOffsetIterator itThis = _indexes.getOffsetIterator();
			final int blen = that._ptr[k + 1];
			for(int apos = that._ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + that._data[apos];
				re = rs + that._data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				int i = sbApos;
				final int blen = _ptr[k + 1];
				for(int apos = _ptr[k], rs = 0, re = 0; apos < blen && i < sbAlen; apos += 2) {
					rs = re + _data[apos];
					re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final skipPair tmp = skipScanVal(k, cl);
			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = 0, re = tmp.astart; apos < blen; apos += 2) {
				rs = re + _data[apos];
				re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final double[] sbAval = sb.values(k);
			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final AOffsetIterator itThat = that._indexes.getOffsetIterator();
			final int blen = _ptr[k + 1];
			for(int apos = _ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + _data[apos];
				re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			}
			final int blen = _ptr[k + 1];
			for(int apos = _ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + _data[apos];
				re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `trs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
					final int tblen = that._ptr[tk + 1];
					int tapos = skip[tk];
					int trs = 0, tre = skipV[tk];
					for(; tapos < tblen; tapos += 2) {
						trs = tre + that._data[tapos];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final double[] sbAval = sb.values(k);
			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final AIterator itThat = that._indexes.getIterator();
			final int blen = _ptr[k + 1];
			for(int apos = _ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + _data[apos];
				re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `curRunStartOff` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			int blen = len(k);

			int curRunStartOff = 0;
			int curRunEnd = 0;
			for(int bix = 0; bix < blen; bix += 2) {
```

### UnusedAssignment
Variable `curRunLen` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

		// Length of the most recent run of 1's
		int curRunLen = 0;

		// Current encoding is as follows:
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				final int offR = (r - rl) * nv + k;
				int i = sbApos;
				for(int apos = _ptr[k], rs = 0, re = 0; apos < blen; apos += 2) { // for each run
					rs = re + _data[apos];
					re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				// rs is runStart and re is runEnd

				for(int apos = tmp.apos, rs = 0, re = tmp.astart; apos < blen; apos += 2) {
					rs = re + _data[apos];
					re = rs + _data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `tmp.astart` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

			// rs is runStart and re is runEnd
			for(int apos = tmp.apos, rs = tmp.astart, re = tmp.astart; apos < blen; apos += 2) {
				// for each run find new start and end
				rs = re + _data[apos];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				// rs is runStart and re is runEnd
				int apos = tmp.apos;
				int rs = 0;
				int re = tmp.astart;
				for(; apos < blen; apos += 2) {
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		for(int k = 0; k < ptr.length - 1; k++) { // for each run in RLE
			final int blen = ptr[k + 1];
			for(int apos = ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + data[apos];
				re = rs + data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		for(int k = 0; k < ret.length / nCol; k++) { // for each run in RLE
			final int blen = ptr[k + 1];
			for(int apos = ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + data[apos];
				re = rs + data[apos + 1];
```

### UnusedAssignment
Variable `rs` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		for(int k = 0; k < ret.length; k++) { // for each run in RLE
			final int blen = ptr[k + 1];
			for(int apos = ptr[k], rs = 0, re = 0; apos < blen; apos += 2) {
				rs = re + data[apos];
				re = rs + data[apos + 1];
```

### UnusedAssignment
Variable `vA` initializer `a.get(pA)` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/MergeSort.java`
#### Snippet
```java
		// From here on currentFill is no longer needed.
		int pN = _indexes.length - 1; // Pointer new
		int vA = a.get(pA);
		// Pointer to last index
		int vM = _numRows - 1;
```

### UnusedAssignment
Variable `_map` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfOffsetMap.java`
#### Snippet
```java
	private static final long serialVersionUID = 8949124761287236703L;

	private HashMap<Long, Long> _map = null;
	private long _rmRows = -1;
	
```

### UnusedAssignment
Variable `_rmRows` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfOffsetMap.java`
#### Snippet
```java

	private HashMap<Long, Long> _map = null;
	private long _rmRows = -1;
	
	public TfOffsetMap( List<Pair<Long,Long>> rmRows ) {
```

### UnusedAssignment
Variable `decoder` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/decode/DecoderFactory.java`
#### Snippet
```java
		FrameBlock meta, int clen, int minCol, int maxCol)
	{
		Decoder decoder = null;
		
		try {
```

### UnusedAssignment
Variable `specRecodeIDs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
			throw new IOException("Missing transform specification.");
		
		List<Integer> specRecodeIDs = null;
		
		try {
```

### UnusedAssignment
Variable `attrs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
		if( spec.containsKey(group) ) {
			//parse attribute-array or plain array of IDs
			JSONArray attrs = null;
			if( spec.get(group) instanceof JSONObject ) {
				attrs = (JSONArray) ((JSONObject)spec.get(group)).get(TfUtils.JSON_ATTRS);
```

### UnusedAssignment
Variable `mcsr` initializer `MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java

	protected void applySparse(CacheBlock<?> in, MatrixBlock out, int outputCol, int rowStart, int blk){
		boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
		mcsr = false; //force CSR for transformencode
		int index = _colID - 1;
```

### UnusedAssignment
Variable `mcsr` initializer `MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
					" and not MatrixBlock");
		}
		boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
		mcsr = false; //force CSR for transformencode
		ArrayList<Integer> sparseRowsWZeros = null;
```

### UnusedAssignment
Variable `mcsr` initializer `MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderPassThrough.java`
#### Snippet
```java
		//Set<Integer> sparseRowsWZeros = null;
		ArrayList<Integer> sparseRowsWZeros = null;
		boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
		mcsr = false; //force CSR for transformencode
		int index = _colID - 1;
```

### UnusedAssignment
Variable `tokenizer` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/tokenize/TokenizerFactory.java`
#### Snippet
```java

	public static Tokenizer createTokenizer(String spec, int maxTokens) {
		Tokenizer tokenizer = null;

		try {
```

### UnusedAssignment
Variable `mbLeft` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java
				|| !LibMatrixOuterAgg.isSupportedUaggOp(_uaggOp, _bOp));

		MatrixBlock mbLeft = null, mbRight = null, mbOut = null;
		//get the main data input
		if( rightCached ) { 
```

### UnusedAssignment
Variable `mbRight` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java
				|| !LibMatrixOuterAgg.isSupportedUaggOp(_uaggOp, _bOp));

		MatrixBlock mbLeft = null, mbRight = null, mbOut = null;
		//get the main data input
		if( rightCached ) { 
```

### UnusedAssignment
Variable `outBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixBuiltinNaryCPInstruction.java`
#### Snippet
```java
		List<ScalarObject> scalars = ec.getScalarInputs(inputs);
		
		MatrixBlock outBlock = null;
		if( "cbind".equals(getOpcode()) || "rbind".equals(getOpcode()) ) {
			boolean cbind = "cbind".equals(getOpcode());
```

### UnusedAssignment
Variable `retBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryMatrixCPInstruction.java`
#### Snippet
```java
	public void processInstruction(ExecutionContext ec) {
		MatrixBlock inBlock = ec.getMatrixInput(input1.getName());
		MatrixBlock retBlock = null;
		
		if(LibCommonsMath.isSupportedUnaryOperation(getOpcode())) {
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FrameIndexingCPInstruction.java`
#### Snippet
```java
		else if ( opcode.equalsIgnoreCase(LeftIndex.OPCODE)) {
			FrameBlock lin = ec.getFrameInput(input1.getName());
			FrameBlock out = null;
			
			if(input2.getDataType() == DataType.FRAME) { //FRAME<-FRAME
```

### UnusedAssignment
Variable `covobj` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CovarianceCPInstruction.java`
#### Snippet
```java
		String output_name = output.getName(); 
		COVOperator cov_op = (COVOperator)_optr;
		CM_COV_Object covobj = null;
		
		if ( input3 == null ) {
```

### UnusedAssignment
Variable `sores` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/BinaryScalarScalarCPInstruction.java`
#### Snippet
```java
		String opcode = getOpcode();
		BinaryOperator dop = (BinaryOperator) _optr;
		ScalarObject sores = null;
		
		//compare output value, incl implicit type promotion if necessary
```

### UnusedAssignment
Variable `parts` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/SPInstructionParser.java`
#### Snippet
```java
			return null;

		String [] parts = null;
		switch(sptype)
		{
```

### UnusedAssignment
The value `null` assigned to `in2` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
		case 3:
			in1.split(parts[1]);
			in2 = null;
			in3 = null;
			break;
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
			in1.split(parts[1]);
			in2 = null;
			in3 = null;
			break;
		case 4:
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
			in1.split(parts[1]);
			in2.split(parts[2]);
			in3 = null;
			break;
		case 5:
```

### UnusedAssignment
Variable `opcode` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
		
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = null;
		ValueFunction func = null;
		
```

### UnusedAssignment
Variable `func` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = null;
		ValueFunction func = null;
		
		//print or stop or cumulative aggregates
```

### UnusedAssignment
Variable `sores` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryScalarCPInstruction.java`
#### Snippet
```java
	public void processInstruction(ExecutionContext ec) {
		String opcode = getOpcode();
		ScalarObject sores = null;
		ScalarObject so = null;
		
```

### UnusedAssignment
Variable `so` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryScalarCPInstruction.java`
#### Snippet
```java
		String opcode = getOpcode();
		ScalarObject sores = null;
		ScalarObject so = null;
		
		//get the scalar input 
```

### UnusedAssignment
Variable `cmobj` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CentralMomentCPInstruction.java`
#### Snippet
```java
			cm_op = cm_op.setCMAggOp((int)order.getLongValue());
		
		CM_COV_Object cmobj = null; 
		if (input3 == null ) {
			cmobj = matBlock.cmOperations(cm_op);
```

### UnusedAssignment
The value `null` assigned to `quantiles` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantilePickCPInstruction.java`
#### Snippet
```java
						MatrixBlock quantiles = ec.getMatrixInput(input2.getName());
						MatrixBlock resultBlock = matBlock.pickValues(quantiles, new MatrixBlock());
						quantiles = null;
						ec.releaseMatrixInput(input2.getName());
						ec.setMatrixOutput(output.getName(), resultBlock);
```

### UnusedAssignment
Variable `func` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java

		// determine the appropriate value function
		ValueFunction func = null;
		if(opcode.equalsIgnoreCase("cdf")) {
			if(paramsMap.get("dist") == null)
```

### UnusedAssignment
Variable `sores` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
	public void processInstruction(ExecutionContext ec) {
		String opcode = getOpcode();
		ScalarObject sores = null;
		if(opcode.equalsIgnoreCase("cdf")) {
			SimpleOperator op = (SimpleOperator) _optr;
```

### UnusedAssignment
The value `groups = weights = null` assigned to `target` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
			ec.setMatrixOutput(output.getName(), soresBlock);
			// release locks
			target = groups = weights = null;
			ec.releaseMatrixInput(params.get(Statement.GAGG_TARGET));
			ec.releaseMatrixInput(params.get(Statement.GAGG_GROUPS));
```

### UnusedAssignment
The value `weights = null` assigned to `groups` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
			ec.setMatrixOutput(output.getName(), soresBlock);
			// release locks
			target = groups = weights = null;
			ec.releaseMatrixInput(params.get(Statement.GAGG_TARGET));
			ec.releaseMatrixInput(params.get(Statement.GAGG_GROUPS));
```

### UnusedAssignment
The value `null` assigned to `weights` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
			ec.setMatrixOutput(output.getName(), soresBlock);
			// release locks
			target = groups = weights = null;
			ec.releaseMatrixInput(params.get(Statement.GAGG_TARGET));
			ec.releaseMatrixInput(params.get(Statement.GAGG_GROUPS));
```

### UnusedAssignment
Variable `meta` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java

			// execute transform meta data read
			FrameBlock meta = null;
			try {
				meta = TfMetaUtils.readTransformMetaDataFromFile(spec, path, delim);
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java

			// get input matrix/frame and convert to string
			String out = null;

			Data cacheData = ec.getVariable(getParam("target"));
```

### UnusedAssignment
Variable `list` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
			List<String> names = new ArrayList<>(params.keySet());

			ListObject list = null;
			if (DMLScript.LINEAGE) {
				CPOperand[] listOperands = names.stream().map(n -> ec.containsVariable(params.get(n)) 
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListAppendRemoveCPInstruction.java`
#### Snippet
```java
			Data dat2 = ec.getVariable(input2);
			LineageItem li = DMLScript.LINEAGE ? ec.getLineage().get(input2) : null;
			ListObject tmp = null;
			if( dat2 instanceof ListObject && ((ListObject)dat2).getLength() == 1 ) {
				//add unfolded elements for lists of size 1 (e.g., named)
```

### UnusedAssignment
Variable `resultBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixIndexingCPInstruction.java`
#### Snippet
```java
		if( opcode.equalsIgnoreCase(RightIndex.OPCODE) )
		{
			MatrixBlock resultBlock = null;
			
			if( mo.isPartitioned() ) //via data partitioning
```

### UnusedAssignment
Variable `resultBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixIndexingCPInstruction.java`
#### Snippet
```java
			
			MatrixBlock matBlock = ec.getMatrixInput(input1.getName());
			MatrixBlock resultBlock = null;
			
			if(input2.getDataType() == DataType.MATRIX) { //MATRIX<-MATRIX
```

### UnusedAssignment
Variable `op` initializer `AggregateOperationTypes.INVALID` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java
	
	public static Operator parseGroupedAggOperator(String fn, String other) {
		AggregateOperationTypes op = AggregateOperationTypes.INVALID;
		if ( fn.equalsIgnoreCase("centralmoment") )
			// in case of CM, we also need to pass "order"
```

### UnusedAssignment
Variable `aggun` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java

	public static AggregateUnaryOperator parseAggregateUnaryRowIndexOperator(String opcode, int numOutputs, int numThreads) {
		AggregateUnaryOperator aggun = null;
		AggregateOperator agg = null;
		if (opcode.equalsIgnoreCase("uarimax") )
```

### UnusedAssignment
The value `null` assigned to `in2` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantileSortCPInstruction.java`
#### Snippet
```java
			case 4:
				in1.split(parts[1]);
				in2 = null;
				break;
			case 5:
```

### UnusedAssignment
Variable `in2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantileSortCPInstruction.java`
#### Snippet
```java
	public static QuantileSortCPInstruction parseInstruction ( String str ) {
		CPOperand in1 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand in2 = null;
		CPOperand out = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		
```

### UnusedAssignment
Variable `wtBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CtableCPInstruction.java`
#### Snippet
```java
	public void processInstruction(ExecutionContext ec) {
		MatrixBlock matBlock1 = ec.getMatrixInput(input1.getName());
		MatrixBlock matBlock2=null, wtBlock=null;
		double cst1, cst2;
		
```

### UnusedAssignment
Variable `outputBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		MatrixBlock input = ec.getMatrixInput(input1.getName());
		MatrixBlock bias = ec.getMatrixInput(_in2.getName());
		MatrixBlock outputBlock = null;
		
		if(bias.getNumColumns() != 1) {
```

### UnusedAssignment
Variable `outputBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		
		// acquire inputs
		MatrixBlock outputBlock = null;
		MatrixBlock matBlock = instOpcode.equalsIgnoreCase("avgpooling_backward") ? null : ec.getMatrixInput(input1.getName());
		int pad_h = getScalarInput(ec, _padding, 0);
```

### UnusedAssignment
Variable `outputBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		MatrixBlock input = ec.getMatrixInput(input1.getName());
		MatrixBlock bias = ec.getMatrixInput(_in2.getName());
		MatrixBlock outputBlock = null;
		
		if(bias.getNumColumns() != 1) {
```

### UnusedAssignment
Variable `qop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryFEDInstruction.java`
#### Snippet
```java
		checkDataTypes(DataType.MATRIX, in1, in2, in3);

		QuaternaryOperator qop = null;
		if(addInput4 == 1) // wcemm, wsloss, wdivmm
		{
```

### UnusedAssignment
Variable `mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/EvalNaryCPInstruction.java`
#### Snippet
```java
			Data newOutput = ec.getVariable(output);
			if (!(newOutput instanceof MatrixObject)) {
				MatrixBlock mb = null;
				if (newOutput instanceof ScalarObject) {
					//convert scalar to matrix
```

### UnusedAssignment
Variable `frB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSLossFEDInstruction.java`
#### Snippet
```java
			FederationMap fedMap = X.getFedMapping();
			ArrayList<FederatedRequest[]> frSliced = new ArrayList<>(); // FederatedRequests of broadcastSliced
			FederatedRequest frB = null; // FederatedRequest for broadcast
			long[] varNewIn = new long[qop.hasFourInputs() ? 4 : 3];
			varNewIn[0] = fedMap.getID();
```

### UnusedAssignment
Variable `_fedOut` initializer `FederatedOutput.NONE` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/FEDInstruction.java`
#### Snippet
```java
	protected final FEDType _fedType;
	protected long _tid = -1; //main
	protected FederatedOutput _fedOut = FederatedOutput.NONE;

	protected FEDInstruction(FEDType type, String opcode, String istr) {
```

### UnusedAssignment
Variable `frB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSigmoidFEDInstruction.java`
#### Snippet
```java
			FederationMap fedMap = X.getFedMapping();
			FederatedRequest[] frSliced = null;
			FederatedRequest frB = null; // FederatedRequest for broadcast
			long[] varNewIn = new long[3];
			varNewIn[0] = fedMap.getID();
```

### UnusedAssignment
The value `null` assigned to `in2` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantileSortFEDInstruction.java`
#### Snippet
```java
			case 4:
				in1.split(parts[1]);
				in2 = null;
				break;
			case 5:
```

### UnusedAssignment
Variable `in2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantileSortFEDInstruction.java`
#### Snippet
```java
	public static QuantileSortFEDInstruction parseInstruction ( String str , boolean hasFedOut) {
		CPOperand in1 = new CPOperand("", Types.ValueType.UNKNOWN, Types.DataType.UNKNOWN);
		CPOperand in2 = null;
		CPOperand out = new CPOperand("", Types.ValueType.UNKNOWN, Types.DataType.UNKNOWN);

```

### UnusedAssignment
Variable `meanTmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CovarianceFEDInstruction.java`
#### Snippet
```java
		meanInstr = meanInstr.replace((var == 0 ? parts[2] : parts[3]) + "°", "");
		meanInstr = meanInstr.replace(parts[4], parts[4].replace("FP64", "STRING°16"));
		Future<FederatedResponse>[] meanTmp = null;

		//create federated commands for aggregation
```

### UnusedAssignment
Variable `frB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWUMMFEDInstruction.java`
#### Snippet
```java
			FederationMap fedMap = X.getFedMapping();
			FederatedRequest[] frSliced = null; // FederatedRequest for broadcastSliced
			FederatedRequest frB = null; // FederatedRequest for broadcast
			long[] varNewIn = new long[3];
			varNewIn[0] = fedMap.getID();
```

### UnusedAssignment
Variable `fr2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/BinaryMatrixMatrixFEDInstruction.java`
#### Snippet
```java

		//execute federated operation on mo1 or mo2
		FederatedRequest fr2 = null;
		if( mo2.isFederatedExcept(FType.BROADCAST) ) {
			if(mo1.isFederated() && mo1.getFedMapping().isAligned(mo2.getFedMapping(),
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AppendFEDInstruction.java`
#### Snippet
```java
			
			//execute federated operations and set output
			Future<FederatedResponse>[] ret = null;
			if(isSpark) {
				FederatedRequest tmp = new FederatedRequest(RequestType.PUT_VAR,
```

### UnusedAssignment
Variable `success` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java

			if ( getInput3().getName().equalsIgnoreCase("binaryblock") ) {
				boolean success = false;
				success = ((CacheableData)object).moveData(getInput2().getName(), getInput3().getName());
				if (!success) {
```

### UnusedAssignment
Variable `fmtProperties` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
				// 13 inputs: createvar corresponding to WRITE -- includes properties hasHeader, delim, and sparse
				// 14 inputs: createvar corresponding to READ -- includes properties hasHeader, delim, fill, and fillValue
				FileFormatProperties fmtProperties = null;
				int curPos = 11;
				if ( parts.length == 14+extSchema ) {
```

### UnusedAssignment
Variable `fmtProperties` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
				// 13 inputs: createvar corresponding to WRITE -- includes properties delim, index delim and sparse
				// 12 inputs: createvar corresponding to READ -- includes properties delim, index delim, and sparse
				FileFormatProperties fmtProperties = null;
				int curPos = 11;
				if(parts.length == 12 + extSchema) {
```

### UnusedAssignment
Variable `fprops` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			in3 = new CPOperand(parts[3]);

			FileFormatProperties fprops = null;
			if ( in3.getName().equalsIgnoreCase("csv") ) {
				boolean hasHeader = Boolean.parseBoolean(parts[4]);
```

### UnusedAssignment
The value `null` assigned to `in2` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryFEDInstruction.java`
#### Snippet
```java
			case 3:
				in1.split(parts[1]);
				in2 = null;
				in3 = null;
				break;
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryFEDInstruction.java`
#### Snippet
```java
				in1.split(parts[1]);
				in2 = null;
				in3 = null;
				break;
			case 4:
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryFEDInstruction.java`
#### Snippet
```java
				in1.split(parts[1]);
				in2.split(parts[2]);
				in3 = null;
				break;
			case 5:
```

### UnusedAssignment
Variable `spoofType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java

		Class<?> scla = _op.getClass().getSuperclass();
		SpoofFEDType spoofType = null;
		if(scla == SpoofCellwise.class)
			spoofType = new SpoofFEDCellwise(_op, _output, fedMap.getType());
```

### UnusedAssignment
Variable `aggop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/AggregateUnaryGPUInstruction.java`
#### Snippet
```java
		// This follows logic similar to AggregateUnaryCPInstruction.
		// nrow, ncol & length should either read or refresh metadata
		Operator aggop = null;
		if (opcode.equalsIgnoreCase("nrow") || opcode.equalsIgnoreCase("ncol") || opcode.equalsIgnoreCase("length")) {
			throw new DMLRuntimeException("nrow, ncol & length should not be compiled as GPU instructions!");
```

### UnusedAssignment
Variable `opcode` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/BuiltinUnaryGPUInstruction.java`
#### Snippet
```java
		
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = null;
		
		//print or stop or cumulative aggregates
```

### UnusedAssignment
Variable `s` initializer `new MatrixBlock()` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
			(!marginRow && mo.getFedMapping().getType().isRowPartitioned()));

		MatrixBlock s = new MatrixBlock();
		if(select == null && isNotAligned) {
			List<MatrixBlock> colSums = new ArrayList<>();
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MMTSJGPUInstruction.java`
#### Snippet
```java

public class MMTSJGPUInstruction extends GPUInstruction {
	private MMTSJType _type = null;

	/**
```

### UnusedAssignment
Variable `aggun` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
		CPOperand out = new CPOperand(parts[2]);

		AggregateUnaryOperator aggun = null;
		if(opcode.equalsIgnoreCase("uarimax") || opcode.equalsIgnoreCase("uarimin"))
			if(InstructionUtils.getExecType(str) == ExecType.SPARK)
```

### UnusedAssignment
Variable `fedOut` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
			aggun = InstructionUtils.parseBasicAggregateUnaryOperator(opcode);

		FederatedOutput fedOut = null;
		if ( parts.length == 5 && !parts[4].equals("uarimin") && !parts[4].equals("uarimax") )
			fedOut = FederatedOutput.valueOf(parts[4]);
```

### UnusedAssignment
Variable `constant` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixAxpyGPUInstruction.java`
#### Snippet
```java

public class MatrixMatrixAxpyGPUInstruction extends ArithmeticBinaryGPUInstruction {
	CPOperand constant = null;
	int multiplier = 1;

```

### UnusedAssignment
Variable `multiplier` initializer `1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixAxpyGPUInstruction.java`
#### Snippet
```java
public class MatrixMatrixAxpyGPUInstruction extends ArithmeticBinaryGPUInstruction {
	CPOperand constant = null;
	int multiplier = 1;

	private MatrixMatrixAxpyGPUInstruction(Operator op, CPOperand in1, CPOperand constant, int multiplier,
```

### UnusedAssignment
Variable `_requiresLabelUpdate` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/GPUInstruction.java`
#### Snippet
```java
	protected GPUINSTRUCTION_TYPE _gputype;

	protected boolean _requiresLabelUpdate = false;

	protected GPUInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, String opcode, String istr) {
```

### UnusedAssignment
Variable `inputs` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BuiltinNarySPInstruction.java`
#### Snippet
```java
		String opcode = parts[0];
		CPOperand output = new CPOperand(parts[parts.length - 1]);
		CPOperand[] inputs = null;
		inputs = new CPOperand[parts.length - 2];
		for (int i = 1; i < parts.length-1; i++)
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		private MatrixIndexes _ix = null;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5352374590399929673L;
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		private MatrixIndexes _ix = null;
```

### UnusedAssignment
Variable `_aggtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java

public class AggregateUnarySPInstruction extends UnarySPInstruction {
	private SparkAggType _aggtype = null;
	private AggregateOperator _aop = null;

```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -968274963539513423L;

		private AggregateUnaryOperator _op = null;

		public RDDUTensorAggValueFunction(AggregateUnaryOperator op)
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		
		public RDDUAggFunction( AggregateUnaryOperator op, int blen ) {
```

### UnusedAssignment
The value `blen` assigned to `_blen` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		public RDDUAggFunction2( AggregateUnaryOperator op, int blen ) {
			_op = op;
			_blen = blen;
			_blen = blen;
		}
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 2672082409287856038L;
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 2672082409287856038L;
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -6258769067791011763L;

		private AggregateUnaryOperator _op = null;

		public RDDUTensorAggFunction2( AggregateUnaryOperator op ) {
```

### UnusedAssignment
Variable `_aop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
public class AggregateUnarySPInstruction extends UnarySPInstruction {
	private SparkAggType _aggtype = null;
	private AggregateOperator _aop = null;

	protected AggregateUnarySPInstruction(SPType type, AggregateUnaryOperator auop, AggregateOperator aop, CPOperand in,
```

### UnusedAssignment
Variable `_ix` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		private MatrixIndexes _ix = null;
		
		public RDDUAggValueFunction( AggregateUnaryOperator op, int blen ) {
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		
		private AggregateUnaryOperator _op = null;
		private int _blen = -1;
		
		public RDDUAggFunction2( AggregateUnaryOperator op, int blen ) {
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
		TernaryOperator op = (TernaryOperator) _optr;
		
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		if( input1.isMatrix() && !input2.isMatrix() && !input3.isMatrix() )
			out = in1.mapValues(new TernaryFunctionMSS(op, m1, m2, m3));
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantileSortSPInstruction.java`
#### Snippet
```java
		DataCharacteristics mc = sec.getDataCharacteristics(input1.getName());
		
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		long clen = -1;
		if( !weighted ) { //W/O WEIGHTS (default)
```

### UnusedAssignment
Variable `clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantileSortSPInstruction.java`
#### Snippet
```java
		
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		long clen = -1;
		if( !weighted ) { //W/O WEIGHTS (default)
			out = RDDSortUtils.sortByVal(in, mc.getRows(), mc.getBlocksize());
```

### UnusedAssignment
Variable `in2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantileSortSPInstruction.java`
#### Snippet
```java
	public static QuantileSortSPInstruction parseInstruction ( String str ) {
		CPOperand in1 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand in2 = null;
		CPOperand out = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		
```

### UnusedAssignment
Variable `_offset` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendMSPInstruction.java`
#### Snippet
```java

public abstract class AppendMSPInstruction extends AppendSPInstruction {
	protected CPOperand _offset = null;

	protected AppendMSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand offset, CPOperand out,
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes,MatrixBlock> in1 = sec.getBinaryMatrixBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> in2 = sec.getBinaryMatrixBlockRDDHandleForVariable( input2.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		
		// General case: This one needs shifting and merging and hence has huge performance hit.
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGAlignedSPInstruction.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes,MatrixBlock> in1 = sec.getBinaryMatrixBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> in2 = sec.getBinaryMatrixBlockRDDHandleForVariable( input2.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		
		// Simple changing of matrix indexes of RHS
```

### UnusedAssignment
Variable `mat` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
	 * Enclosing {@link MatrixObject} instance
	 */
	MatrixObject mat = null;
	
	/**
```

### UnusedAssignment
Variable `GPUSize` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
	// Method to find the estimated size of this GPU Object in the device
	public long getSizeOnDevice() {
		long GPUSize = 0;
		long rlen = mat.getNumRows();
		long clen = mat.getNumColumns();
```

### UnusedAssignment
Variable `nnzPerRowPtr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
			Pointer densePtr, int rows, int cols) {
		cusparseMatDescr matDescr = CSRPointer.getDefaultCuSparseMatrixDescriptor();
		Pointer nnzPerRowPtr = null;
		Pointer nnzTotalDevHostPtr = null;

```

### UnusedAssignment
Variable `nnzTotalDevHostPtr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		cusparseMatDescr matDescr = CSRPointer.getDefaultCuSparseMatrixDescriptor();
		Pointer nnzPerRowPtr = null;
		Pointer nnzTotalDevHostPtr = null;

		nnzPerRowPtr = gCtx.allocate(null, getIntSizeOf(rows), false);
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		if(isDensePointerNull())
			sparse = true;
		MatrixBlock tmp = null;
		long start = DMLScript.STATISTICS ? System.nanoTime() : 0;
		if (!isDensePointerNull()) {
```

### UnusedAssignment
Variable `csrBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
				// CSR is the preferred format for cuSparse GEMM
				// Converts MCSR and COO to CSR
				SparseBlockCSR csrBlock = null;
				long t0 = 0;
				if (block instanceof SparseBlockCSR) {
```

### UnusedAssignment
Variable `nnzPerRowPtr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
				int rows = toIntExact(mat.getNumRows());
				int cols = toIntExact(mat.getNumColumns());
				Pointer nnzPerRowPtr = null;
				Pointer nnzTotalDevHostPtr = null;
				nnzPerRowPtr = gCtx.allocate(instName, getIntSizeOf(rows), false);
```

### UnusedAssignment
Variable `nnzTotalDevHostPtr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
				int cols = toIntExact(mat.getNumColumns());
				Pointer nnzPerRowPtr = null;
				Pointer nnzTotalDevHostPtr = null;
				nnzPerRowPtr = gCtx.allocate(instName, getIntSizeOf(rows), false);
				nnzTotalDevHostPtr = gCtx.allocate(instName, getIntSizeOf(1), false);
```

### UnusedAssignment
Variable `value_in2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
				outlist.add(first);
				
				MatrixBlock value_in2 = null;
				if( _cbind ) {
					value_in2 = _pm.getBlock((int)ix.getRowIndex(), 1);
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
		
		//execute map-append operations (partitioning preserving if #in-blocks = #out-blocks)
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		if( preservesPartitioning(mc1, mc2, _cbind) ) {
			out = in1.mapPartitionsToPair(
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java

		private PartitionedBroadcast<MatrixBlock> _pm = null;
		private boolean _cbind = true;
		private long _lastBlockColIndex = -1;
		
```

### UnusedAssignment
Variable `_pm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5767240739761027220L;

		private PartitionedBroadcast<MatrixBlock> _pm = null;
		private boolean _cbind = true;
		private long _lastBlockColIndex = -1;
```

### UnusedAssignment
Variable `_lastBlockColIndex` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
		private PartitionedBroadcast<MatrixBlock> _pm = null;
		private boolean _cbind = true;
		private long _lastBlockColIndex = -1;
		
		public MapSideAppendPartitionFunction(PartitionedBroadcast<MatrixBlock> binput, boolean cbind, long offset, int blen)  
```

### UnusedAssignment
Variable `_expr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernaryFrameScalarSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5850400295183766400L;

		private String _expr = null;
		private long _margin = -1;

```

### UnusedAssignment
Variable `_margin` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernaryFrameScalarSPInstruction.java`
#### Snippet
```java

		private String _expr = null;
		private long _margin = -1;

		public RDDStringProcessing(String expr, long margin) {
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		}
		else {
			long size = 0;
			synchronized(allPointers) {
				if (!allPointers.containsKey(toFree)) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CentralMomentSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8949715516574052497L;
		
		private CMOperator _op = null;
		
		public RDDCMWeightsFunction( CMOperator op ) {
```

### UnusedAssignment
Variable `cmobj` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CentralMomentSPInstruction.java`
#### Snippet
```java
				
		//process central moment instruction
		CM_COV_Object cmobj = null; 
		if( input3 == null ) //w/o weights
		{
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CentralMomentSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 3272260751983866544L;
		
		private CMOperator _op = null;
		
		public RDDCMReduceFunction( CMOperator op ) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CentralMomentSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 2293839116041610644L;
		
		private CMOperator _op = null;
		
		public RDDCMFunction( CMOperator op ) {
```

### UnusedAssignment
The value `null` assigned to `in2` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UnarySPInstruction.java`
#### Snippet
```java
		case 3:
			in1.split(parts[1]);
			in2 = null;
			in3 = null;
			break;
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UnarySPInstruction.java`
#### Snippet
```java
			in1.split(parts[1]);
			in2 = null;
			in3 = null;
			break;
		case 4:
```

### UnusedAssignment
The value `null` assigned to `in3` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UnarySPInstruction.java`
#### Snippet
```java
			in1.split(parts[1]);
			in2.split(parts[2]);
			in3 = null;
			break;
		case 5:
```

### UnusedAssignment
Variable `_pm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendMSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3997051891171313830L;

		private PartitionedBroadcast<FrameBlock> _pm = null;
		
		public MapSideAppendPartitionFunction(PartitionedBroadcast<FrameBlock> binput)  
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendMSPInstruction.java`
#### Snippet
```java
		
		//execute map-append operations (partitioning preserving if keys for blocks not changing)
		JavaPairRDD<Long,FrameBlock> out = null;
		if( preservesPartitioning(_cbind) ) {
			out = in1.mapPartitionsToPair(
```

### UnusedAssignment
The value `br` assigned to `_blen` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
		String delim, String indexDelim, String instr) {
		super(SPType.LIBSVMReblock, op, in, out, opcode, instr);
		_blen = br;
		_blen = bc;
		_delim = delim;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		DataCharacteristics mcIn, DataCharacteristics mcOut, IndexRange ixrange, SparkAggType aggType)
	{
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		
		if( isPartitioningPreservingRightIndexing(mcIn, ixrange) ) {
```

### UnusedAssignment
Variable `_ixrange` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		
		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		
		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
			PartitionedBroadcast<MatrixBlock> broadcastIn2 = null;
			JavaPairRDD<MatrixIndexes,MatrixBlock> in2 = null;
			JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
			
			//update and check output dimensions
```

### UnusedAssignment
Variable `_filterFlags` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -9114299718258329951L;

		private HashSet<Integer> _filterFlags = null;

		public PartitionPruningFunction(HashSet<Integer> flags) {
```

### UnusedAssignment
Variable `_ixrange` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5724800998701216440L;
		
		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
		
		public ZeroOutLHS(boolean complement, IndexRange range, DataCharacteristics mcLeft) {
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
		
		public SliceRHSForLeftIndexing(IndexRange ixrange, DataCharacteristics mcLeft) {
```

### UnusedAssignment
Variable `_complement` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3581795160948484261L;
		
		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
```

### UnusedAssignment
Variable `csvInstruction` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			// HACK ALERT: Until we introduces the rewrite to insert csvrblock for non-persistent read
			// throw new DMLRuntimeException("CSVInputInfo is not supported for ReblockSPInstruction");
			CSVReblockSPInstruction csvInstruction = null;
			boolean hasHeader = false;
			String delim = ",";
```

### UnusedAssignment
Variable `csvInstruction` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			// HACK ALERT: Until we introduces the rewrite to insert csvrblock for non-persistent read
			// throw new DMLRuntimeException("CSVInputInfo is not supported for ReblockSPInstruction");
			CSVReblockSPInstruction csvInstruction = null;
			boolean hasHeader = false;
			String delim = ",";
```

### UnusedAssignment
The value `br` assigned to `blen` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			String opcode, String instr) {
		super(SPType.Reblock, op, in, out, opcode, instr);
		blen = br;
		blen = bc;
		outputEmptyBlocks = emptyBlocks;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java
		
		private long _len = -1;
		private long _blen = -1;
		private boolean _left = false;
		
```

### UnusedAssignment
Variable `_left` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java
		private long _len = -1;
		private long _blen = -1;
		private boolean _left = false;
		
		public RmmReplicateFunction(long len, long blen, boolean left)
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -5772410117511730911L;
		
		private AggregateBinaryOperator _op = null;
		
		public RmmMultiplyFunction()
```

### UnusedAssignment
Variable `_len` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 3577072668341033932L;
		
		private long _len = -1;
		private long _blen = -1;
		private boolean _left = false;
```

### UnusedAssignment
Variable `_pmV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 8197406787010296291L;

		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		
		public RDDMapMMChainFunction( PartitionedBroadcast<MatrixBlock> bV) {
```

### UnusedAssignment
Variable `_pmV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -7926980450209760212L;

		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private PartitionedBroadcast<MatrixBlock> _pmW = null;
		private ChainType _chainType = null;
```

### UnusedAssignment
Variable `_chainType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private PartitionedBroadcast<MatrixBlock> _pmW = null;
		private ChainType _chainType = null;
		
		public RDDMapMMChainFunction2( PartitionedBroadcast<MatrixBlock> bV, PartitionedBroadcast<MatrixBlock> bW, ChainType chain) {
```

### UnusedAssignment
Variable `_chainType` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java

public class MapmmChainSPInstruction extends SPInstruction {
	private ChainType _chainType = null;
	private CPOperand _input1 = null;
	private CPOperand _input2 = null;
```

### UnusedAssignment
Variable `_input2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
	private ChainType _chainType = null;
	private CPOperand _input1 = null;
	private CPOperand _input2 = null;
	private CPOperand _input3 = null;
	private CPOperand _output = null;
```

### UnusedAssignment
Variable `_input1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
public class MapmmChainSPInstruction extends SPInstruction {
	private ChainType _chainType = null;
	private CPOperand _input1 = null;
	private CPOperand _input2 = null;
	private CPOperand _input3 = null;
```

### UnusedAssignment
Variable `_pmW` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java

		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private PartitionedBroadcast<MatrixBlock> _pmW = null;
		private ChainType _chainType = null;
		
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
		
		//execute mapmmchain (guaranteed to have single output block)
		MatrixBlock out = null;
		if( _chainType == ChainType.XtXv ) {
			JavaRDD<MatrixBlock> tmp = inX.values().map(new RDDMapMMChainFunction(inV));
```

### UnusedAssignment
Variable `_output` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmChainSPInstruction.java`
#### Snippet
```java
	private CPOperand _input2 = null;
	private CPOperand _input3 = null;
	private CPOperand _output = null;

	private MapmmChainSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, ChainType type, String opcode, String istr) {
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendRSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -6763904972560309095L;

		private boolean _cbind = true;
		
		public ReduceSideAppendFunction(boolean cbind) {
```

### UnusedAssignment
Variable `_abop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ZipmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -6669267794926220287L;
		
		private AggregateBinaryOperator _abop = null;
		private ReorgOperator _rop = null;
		private boolean _tRewrite = true;
```

### UnusedAssignment
Variable `_tRewrite` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ZipmmSPInstruction.java`
#### Snippet
```java
		private AggregateBinaryOperator _abop = null;
		private ReorgOperator _rop = null;
		private boolean _tRewrite = true;
		
		public ZipMultiplyFunction(boolean tRewrite)
```

### UnusedAssignment
Variable `_tRewrite` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ZipmmSPInstruction.java`
#### Snippet
```java
public class ZipmmSPInstruction extends BinarySPInstruction {
	// internal flag to apply left-transpose rewrite or not
	private boolean _tRewrite = true;

	private ZipmmSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, boolean tRewrite,
```

### UnusedAssignment
Variable `_rop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ZipmmSPInstruction.java`
#### Snippet
```java
		
		private AggregateBinaryOperator _abop = null;
		private ReorgOperator _rop = null;
		private boolean _tRewrite = true;
		
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java

		JavaPairRDD<MatrixIndexes,MatrixBlock> in = sec.getBinaryMatrixBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<MatrixIndexes, MatrixBlock> out = null;
		
		DataCharacteristics inMc = sec.getDataCharacteristics( input1.getName() );
```

### UnusedAssignment
Variable `_pmV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
		protected QuaternaryOperator _qop = null;
		protected PartitionedBroadcast<MatrixBlock> _pmU = null;
		protected PartitionedBroadcast<MatrixBlock> _pmV = null;
		
		public RDDQuaternaryBaseFunction( QuaternaryOperator qop, PartitionedBroadcast<MatrixBlock> bcU, PartitionedBroadcast<MatrixBlock> bcV ) {
```

### UnusedAssignment
Variable `_pmU` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
		
		protected QuaternaryOperator _qop = null;
		protected PartitionedBroadcast<MatrixBlock> _pmU = null;
		protected PartitionedBroadcast<MatrixBlock> _pmV = null;
		
```

### UnusedAssignment
Variable `_qop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3175397651350954930L;
		
		protected QuaternaryOperator _qop = null;
		protected PartitionedBroadcast<MatrixBlock> _pmU = null;
		protected PartitionedBroadcast<MatrixBlock> _pmV = null;
```

### UnusedAssignment
Variable `_cacheU` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
public class QuaternarySPInstruction extends ComputationSPInstruction {
	private CPOperand _input4 = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;

```

### UnusedAssignment
Variable `_input4` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java

public class QuaternarySPInstruction extends ComputationSPInstruction {
	private CPOperand _input4 = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;
```

### UnusedAssignment
Variable `_cacheV` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
	private CPOperand _input4 = null;
	private boolean _cacheU = false;
	private boolean _cacheV = false;

	private QuaternarySPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand in3, CPOperand in4,
```

### UnusedAssignment
Variable `_intermediateMemoryBudget` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
	private ArrayList<CPOperand> _stride = new ArrayList<>();
	private ArrayList<CPOperand> _padding = new ArrayList<>();
	private double _intermediateMemoryBudget = 0;
	
	public DnnGPUInstruction(CPOperand in1, CPOperand in2, CPOperand out, String opcode, String istr, double intermediateMemoryBudget) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java

public class QuantilePickSPInstruction extends BinarySPInstruction {
	private OperationTypes _type = null;

	private QuantilePickSPInstruction(Operator op, CPOperand in, CPOperand out, OperationTypes type, boolean inmem,
```

### UnusedAssignment
Variable `_cbind` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendRSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -97824903649667646L;

		private boolean _cbind = true;
				
		public ReduceSideColumnsFunction(boolean cbind) {
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendRSPInstruction.java`
#### Snippet
```java
		JavaPairRDD<Long,FrameBlock> in1 = sec.getFrameBinaryBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<Long,FrameBlock> in2 = sec.getFrameBinaryBlockRDDHandleForVariable( input2.getName() );
		JavaPairRDD<Long,FrameBlock> out = null;
		long leftRows = sec.getDataCharacteristics(input1.getName()).getRows();
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CovarianceSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 1945166819152577077L;
		
		private COVOperator _op = null;
		
		public RDDCOVWeightsFunction( COVOperator op ) {
```

### UnusedAssignment
Variable `cmobj` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CovarianceSPInstruction.java`
#### Snippet
```java
		
		//process central moment instruction
		CM_COV_Object cmobj = null; 
		if( input3 == null ) { //w/o weights
			cmobj = in1.join( in2 )
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CovarianceSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -9088449969750217519L;
		
		private COVOperator _op = null;
		
		public RDDCOVFunction( COVOperator op ) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CovarianceSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 1118102911706607118L;
		
		private COVOperator _op = null;
		
		public RDDCOVReduceFunction( COVOperator op ) {
```

### UnusedAssignment
Variable `in3` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CovarianceSPInstruction.java`
#### Snippet
```java
		CPOperand in1 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand in2 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand in3 = null;
		CPOperand out = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);

```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinUaggChainSPInstruction.java`
#### Snippet
```java
	// operators
	private BinaryOperator _bOp = null;
	private AggregateUnaryOperator _uaggOp = null;

	private BinUaggChainSPInstruction(CPOperand in, CPOperand out, BinaryOperator bop, AggregateUnaryOperator uaggop,
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinUaggChainSPInstruction.java`
#### Snippet
```java
		
		private BinaryOperator _bOp = null;
		private AggregateUnaryOperator _uaggOp = null;
		
		public RDDBinUaggChainFunction(BinaryOperator bop, AggregateUnaryOperator uaggop) {
```

### UnusedAssignment
Variable `_bOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinUaggChainSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 886065328623752520L;
		
		private BinaryOperator _bOp = null;
		private AggregateUnaryOperator _uaggOp = null;
		
```

### UnusedAssignment
Variable `_bOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinUaggChainSPInstruction.java`
#### Snippet
```java
public class BinUaggChainSPInstruction extends UnarySPInstruction {
	// operators
	private BinaryOperator _bOp = null;
	private AggregateUnaryOperator _uaggOp = null;

```

### UnusedAssignment
Variable `in3` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
			sec.getBinaryMatrixBlockRDDHandleForVariable( input2.getName() );
		
		JavaPairRDD<MatrixIndexes,MatrixBlock> in3 = null;
		double s2 = -1, s3 = -1; //scalars
		
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
			SparkUtils.getNumPreferredPartitions(mcOut, _outputEmptyBlocks));
		
		JavaPairRDD<MatrixIndexes, MatrixBlock> out = null;
		switch(ctableOp) {
			case CTABLE_TRANSFORM: //(VECTOR)
```

### UnusedAssignment
Variable `_pmV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -1696560050436469140L;
		
		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private long _rlen = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java
		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private long _rlen = -1;
		private int _blen = -1;
		
		public RDDPMMFunction( CacheType type, PartitionedBroadcast<MatrixBlock> binput, long rlen, int blen ) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java

public class PmmSPInstruction extends BinarySPInstruction {
	private CacheType _type = null;
	private CPOperand _nrow = null;

```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java
		
		private PartitionedBroadcast<MatrixBlock> _pmV = null;
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_nrow` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PmmSPInstruction.java`
#### Snippet
```java
public class PmmSPInstruction extends BinarySPInstruction {
	private CacheType _type = null;
	private CPOperand _nrow = null;

	private PmmSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, CPOperand nrow, CacheType type,
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TsmmSPInstruction.java`
#### Snippet
```java

public class TsmmSPInstruction extends UnarySPInstruction {
	private MMTSJType _type = null;

	private TsmmSPInstruction(Operator op, CPOperand in1, CPOperand out, MMTSJType type, String opcode, String istr) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TsmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 2935770425858019666L;
		
		private MMTSJType _type = null;
		
		public RDDTSMMFunction( MMTSJType type ) {
```

### UnusedAssignment
Variable `_outputDim` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
		private int _outputDim = -1;
		private int _blen = -1; 
		
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
		
		public RDDTSMM2Function( Broadcast<PartitionedBlock<MatrixBlock>> pb, MMTSJType type ) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
		private int _outputDim = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private AggregateBinaryOperator _op = null;
		private int _outputDim = -1;
		private int _blen = -1; 
		
		public RDDTSMM2ExtFunction( Broadcast<PartitionedBlock<MatrixBlock>> pb, MMTSJType type, int outputDim, int blen ) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
		private int _outputDim = -1;
		private int _blen = -1; 
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java

public class Tsmm2SPInstruction extends UnarySPInstruction {
	private MMTSJType _type = null;

	private Tsmm2SPInstruction(Operator op, CPOperand in1, CPOperand out, MMTSJType type, String opcode, String istr) {
```

### UnusedAssignment
Variable `_pb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 3284059592407517911L;
		
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
```

### UnusedAssignment
Variable `_pb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 2935770425858019666L;
		
		private Broadcast<PartitionedBlock<MatrixBlock>> _pb = null;
		private MMTSJType _type = null;
		private AggregateBinaryOperator _op = null;
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3858454295795680100L;
		
		private MMTSJType _type = null;
		
		public ShiftTSMMIndexesFunction( MMTSJType type ) {
```

### UnusedAssignment
Variable `cdop` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
        AggBinaryOp.SparkAggType aggtype = AggBinaryOp.SparkAggType.valueOf(parts[3]);

        CountDistinctOperator cdop = null;
        if (opcode.equals("uacd")) {
            cdop = new CountDistinctOperator(CountDistinctOperatorTypes.COUNT, Types.Direction.RowCol,
```

### UnusedAssignment
Variable `_outputEmpty` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
	
	private CacheType _type = null;
	private boolean _outputEmpty = true;
	private SparkAggType _aggtype;

```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
		else //MULTI_BLOCK or NONE
		{
			JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
			if( requiresFlatMapFunction(type, mcBc) ) {
				if( requiresRepartitioning(type, mcRdd, mcBc, in1.getNumPartitions()) ) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
	private static final Log LOG = LogFactory.getLog(MapmmSPInstruction.class.getName());
	
	private CacheType _type = null;
	private boolean _outputEmpty = true;
	private SparkAggType _aggtype;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CastSPInstruction.java`
#### Snippet
```java
		JavaPairRDD<?,?> in = sec.getRDDHandleForVariable(input1.getName(), FileFormat.BINARY, -1, true);
		DataCharacteristics mcIn = sec.getDataCharacteristics( input1.getName() );
		JavaPairRDD<?,?> out = null;
		
		//convert frame-matrix / matrix-frame and set output
```

### UnusedAssignment
Variable `_offset` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 98051757210704132L;

		private int _offset = -1;
		
		public PMapMMRebaseBlocksFunction(int offset){
```

### UnusedAssignment
Variable `_pbc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java

		private AggregateBinaryOperator _op = null;
		private Broadcast<PartitionedBlock<MatrixBlock>> _pbc = null;
		private long _offset = -1;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -4520080421816885321L;

		private AggregateBinaryOperator _op = null;
		private Broadcast<PartitionedBlock<MatrixBlock>> _pbc = null;
		private long _offset = -1;
```

### UnusedAssignment
Variable `_offset` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
		private AggregateBinaryOperator _op = null;
		private Broadcast<PartitionedBlock<MatrixBlock>> _pbc = null;
		private long _offset = -1;
		
		public PMapMMFunction( Broadcast<PartitionedBlock<MatrixBlock>> binput, long offset ) {
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
public class UaggOuterChainSPInstruction extends BinarySPInstruction {
	// operators
	private AggregateUnaryOperator _uaggOp = null;
	private AggregateOperator _aggOp = null;
	private BinaryOperator _bOp = null;
```

### UnusedAssignment
Variable `_aggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
	// operators
	private AggregateUnaryOperator _uaggOp = null;
	private AggregateOperator _aggOp = null;
	private BinaryOperator _bOp = null;

```

### UnusedAssignment
Variable `_bv` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 8197406787010296291L;

		private Broadcast<double[]> _bv = null;
		private Broadcast<int[]> _bvi = null;
		private BinaryOperator _bOp = null;
```

### UnusedAssignment
Variable `_bOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
	private AggregateUnaryOperator _uaggOp = null;
	private AggregateOperator _aggOp = null;
	private BinaryOperator _bOp = null;

	private UaggOuterChainSPInstruction(BinaryOperator bop, AggregateUnaryOperator uaggop, AggregateOperator aggop,
```

### UnusedAssignment
Variable `_bOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private AggregateUnaryOperator _uaggOp = null;
		private AggregateOperator _aggOp = null;
		private BinaryOperator _bOp = null;

		private int _blen;
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private Broadcast<int[]> _bvi = null;
		private BinaryOperator _bOp = null;
		private AggregateUnaryOperator _uaggOp = null;
		
		public RDDMapUAggOuterChainFunction(Broadcast<double[]> bv, Broadcast<int[]> bvi, BinaryOperator bOp, AggregateUnaryOperator uaggOp)
```

### UnusedAssignment
Variable `_uaggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		
		// Operators
		private AggregateUnaryOperator _uaggOp = null;
		private AggregateOperator _aggOp = null;
		private BinaryOperator _bOp = null;
```

### UnusedAssignment
Variable `_aggOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		// Operators
		private AggregateUnaryOperator _uaggOp = null;
		private AggregateOperator _aggOp = null;
		private BinaryOperator _bOp = null;

```

### UnusedAssignment
Variable `_bOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private Broadcast<double[]> _bv = null;
		private Broadcast<int[]> _bvi = null;
		private BinaryOperator _bOp = null;
		private AggregateUnaryOperator _uaggOp = null;
		
```

### UnusedAssignment
Variable `_bvi` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java

		private Broadcast<double[]> _bv = null;
		private Broadcast<int[]> _bvi = null;
		private BinaryOperator _bOp = null;
		private AggregateUnaryOperator _uaggOp = null;
```

### UnusedAssignment
Variable `_pbc` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 8197406787010296291L;

		private PartitionedBroadcast<MatrixBlock> _pbc = null;
		
		// Operators
```

### UnusedAssignment
Variable `_tmpVal1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		
		//reused intermediates  
		private MatrixValue _tmpVal1 = null;
		private MatrixValue _tmpVal2 = null;

```

### UnusedAssignment
Variable `_tmpVal2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		//reused intermediates  
		private MatrixValue _tmpVal1 = null;
		private MatrixValue _tmpVal2 = null;

		public RDDMapGenUAggOuterChainFunction(PartitionedBroadcast<MatrixBlock> binput, AggregateUnaryOperator uaggOp, AggregateOperator aggOp, BinaryOperator bOp, 
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		
		//execute UAggOuterChain instruction
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;

		if (LibMatrixOuterAgg.isSupportedUaggOp(_uaggOp, _bOp))
```

### UnusedAssignment
Variable `func` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

			// determine the appropriate value function
			ValueFunction func = null;
			if(opcode.equalsIgnoreCase("groupedagg")) {
				// check for mandatory arguments
```

### UnusedAssignment
Variable `val` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		@Override
		public MatrixCell call(WeightedCell kv) throws Exception {
			double val = -1;
			if(op instanceof CMOperator) {
				AggregateOperationTypes agg = ((CMOperator) op).aggOpType;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

		private int _clen = -1;
		private int _blen = -1;

		public RDDTransformDecodeExpandFunction(int clen, int blen) {
```

### UnusedAssignment
Variable `_tokenizer` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8788298032616522019L;

		private Tokenizer _tokenizer = null;
		
		public RDDTokenizeFunction(Tokenizer tokenizer, int blen) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

		private Decoder _decoder = null;
		private int _blen = -1;

		public RDDTransformDecodeFunction(Decoder decoder, int blen) {
```

### UnusedAssignment
Variable `_off` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private final long _blen;

		private PartitionedBroadcast<MatrixBlock> _off = null;

		public RDDRemoveEmptyFunctionInMem(boolean rmRows, long len, long blen, PartitionedBroadcast<MatrixBlock> off) {
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8187400248076127598L;

		private int _clen = -1;
		private int _blen = -1;

```

### UnusedAssignment
Variable `_ngroups` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;
		private int _blen = -1;

```

### UnusedAssignment
Variable `_omitColList` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 3450977356721057440L;

		private int[] _omitColList = null;

		public RDDTransformApplyOffsetFunction(String spec, String[] colnames) {
```

### UnusedAssignment
Variable `_pbm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 6795402640178679851L;

		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private Operator _op = null;
		private int _ngroups = -1;
		private int _blen = -1;

		public RDDMapGroupedAggFunction(PartitionedBroadcast<MatrixBlock> pbm, Operator op, int ngroups, int blen) {
```

### UnusedAssignment
Variable `weights` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			JavaPairRDD<MatrixIndexes, MatrixBlock> groups = broadcastGroups ? null : sec
				.getBinaryMatrixBlockRDDHandleForVariable(groupsVar);
			JavaPairRDD<MatrixIndexes, MatrixBlock> weights = null;

			DataCharacteristics mc1 = sec.getDataCharacteristics(params.get(Statement.GAGG_TARGET));
```

### UnusedAssignment
Variable `groupWeightedCells` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			DataCharacteristics mcOut = sec.getDataCharacteristics(output.getName());

			JavaPairRDD<MatrixIndexes, WeightedCell> groupWeightedCells = null;

			// Step 1: First extract groupWeightedCells from group, target and weights
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

			// Step 3: Now perform grouped aggregate operation (either on combiner side or reducer side)
			JavaPairRDD<MatrixIndexes, MatrixCell> out = null;
			if(_optr instanceof CMOperator && ((CMOperator) _optr).isPartialAggregateOperator() ||
				_optr instanceof AggregateOperator) {
```

### UnusedAssignment
Variable `_ngroups` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;

		public RDDMapGroupedAggFunction2(PartitionedBroadcast<MatrixBlock> pbm, Operator op, int ngroups) {
```

### UnusedAssignment
Variable `_omap` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

		private Broadcast<MultiColumnEncoder> _bencoder = null;
		private Broadcast<TfOffsetMap> _omap = null;

		public RDDTransformApplyFunction(Broadcast<MultiColumnEncoder> bencoder, Broadcast<TfOffsetMap> omap) {
```

### UnusedAssignment
Variable `_bencoder` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5759813006068230916L;

		private Broadcast<MultiColumnEncoder> _bencoder = null;
		private Broadcast<TfOffsetMap> _omap = null;

```

### UnusedAssignment
Variable `_pbm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -6820599604299797661L;

		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;
```

### UnusedAssignment
Variable `_decoder` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -4797324742568170756L;

		private Decoder _decoder = null;
		private int _blen = -1;

```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java

		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		private Operator _op = null;
		private int _ngroups = -1;

```

### UnusedAssignment
Variable `opcode` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinarySPInstruction.java`
#### Snippet
```java
		CPOperand in2 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand out = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		String opcode = null;
		boolean isBroadcast = false;
		VectorType vtype = null;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinarySPInstruction.java`
#### Snippet
```java
		
		//execute map binary operation
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		if( isOuter ) {
			out = in1.flatMapToPair(new OuterVectorBinaryOpFunction(bop, in2));
```

### UnusedAssignment
Variable `outputBlock` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
		
		private MatrixBlock processRectangularBlock(MatrixBlock matBlock) throws Exception {
			MatrixBlock outputBlock = null;
			if(instOpcode.equalsIgnoreCase("conv2d")) {
				MatrixBlock filter = filterBroadcast.getValue();
```

### UnusedAssignment
Variable `instOpcode` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
		Broadcast<MatrixBlock> biasBroadcast = null;
		DnnParameters params = null;
		String instOpcode = null; boolean enableNative;
		long numRows = 0;
		public RDDConv2dMapMMFunction(Broadcast<MatrixBlock> filterBroadcast, 
```

### UnusedAssignment
Variable `numRows` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
		DnnParameters params = null;
		String instOpcode = null; boolean enableNative;
		long numRows = 0;
		public RDDConv2dMapMMFunction(Broadcast<MatrixBlock> filterBroadcast, 
				DnnParameters params, String instOpcode, Broadcast<MatrixBlock> biasBroadcast, long numRows, boolean enableNativeBLAS) {
```

### UnusedAssignment
Variable `biasBroadcast` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -2106155380020232155L;
		Broadcast<MatrixBlock> filterBroadcast = null;
		Broadcast<MatrixBlock> biasBroadcast = null;
		DnnParameters params = null;
		String instOpcode = null; boolean enableNative;
```

### UnusedAssignment
Variable `params` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
		Broadcast<MatrixBlock> filterBroadcast = null;
		Broadcast<MatrixBlock> biasBroadcast = null;
		DnnParameters params = null;
		String instOpcode = null; boolean enableNative;
		long numRows = 0;
```

### UnusedAssignment
Variable `filterBroadcast` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
	// PairFunction<Tuple2<MatrixIndexes,MatrixBlock>, MatrixIndexes, MatrixBlock> {
		private static final long serialVersionUID = -2106155380020232155L;
		Broadcast<MatrixBlock> filterBroadcast = null;
		Broadcast<MatrixBlock> biasBroadcast = null;
		DnnParameters params = null;
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UnaryMatrixSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3128192099832877491L;
		
		private UnaryOperator _op = null;
		
		public RDDMatrixBuiltinUnaryOp(UnaryOperator u_op) {
```

### UnusedAssignment
Variable `featureLength` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryFrameMatrixSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5850400295183766400L;

		private PartitionedBroadcast<MatrixBlock> featureLength = null;

		public DropInvalidLengths(PartitionedBroadcast<MatrixBlock> fl) {
```

### UnusedAssignment
Variable `joined` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CumulativeOffsetSPInstruction.java`
#### Snippet
```java
		//get and join inputs
		JavaPairRDD<MatrixIndexes,MatrixBlock> inData = sec.getBinaryMatrixBlockRDDHandleForVariable(input1.getName());
		JavaPairRDD<MatrixIndexes,Tuple2<MatrixBlock,MatrixBlock>> joined = null;
		boolean broadcast = _broadcast && !SparkUtils.isHashPartitioned(inData);
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CumulativeOffsetSPInstruction.java`
#### Snippet
```java
		
		private double _initValue = 0;
		private int _blen = -1;
		private long _lastRowBlockIndex;
		
```

### UnusedAssignment
Variable `_initValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CumulativeOffsetSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8407407527406576965L;
		
		private double _initValue = 0;
		private int _blen = -1;
		private long _lastRowBlockIndex;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryFrameFrameSPInstruction.java`
#### Snippet
```java
		// Get input RDDs
		JavaPairRDD<Long, FrameBlock> in1 = sec.getFrameBinaryBlockRDDHandleForVariable(input1.getName());
		JavaPairRDD<Long, FrameBlock> out = null;
		
		if(getOpcode().equals("dropInvalidType")) {
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateTernarySPInstruction.java`
#### Snippet
```java
		//execute aggregate ternary operation
		AggregateTernaryOperator aggop = (AggregateTernaryOperator) _optr;
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		if( in3 != null ) { //3 inputs
			out = in1.join( in2 ).join( in3 )
```

### UnusedAssignment
Variable `_vtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryMatrixBVectorSPInstruction.java`
#### Snippet
```java

public class BinaryMatrixBVectorSPInstruction extends BinaryMatrixMatrixSPInstruction {
	private VectorType _vtype = null;

	protected BinaryMatrixBVectorSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out,
```

### UnusedAssignment
Variable `_level` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CheckpointSPInstruction.java`
#### Snippet
```java
public class CheckpointSPInstruction extends UnarySPInstruction {
	// default storage level
	private StorageLevel _level = null;

	private CheckpointSPInstruction(Operator op, CPOperand in, CPOperand out, StorageLevel level, String opcode, String istr) {
```

### UnusedAssignment
Variable `_value` initializer `Long.MIN_VALUE` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -3739727823287550826L;

		private long _value = Long.MIN_VALUE;

		public MaxLongAccumulator(long value) {
```

### UnusedAssignment
Variable `input3` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
	public CPOperand input1 = null;
	private CPOperand input2 = null;
	private CPOperand input3 = null;
	private CPOperand input4 = null;
	private FileFormatProperties formatProperties;
```

### UnusedAssignment
Variable `input1` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java

public class WriteSPInstruction extends SPInstruction implements LineageTraceable {
	public CPOperand input1 = null;
	private CPOperand input2 = null;
	private CPOperand input3 = null;
```

### UnusedAssignment
Variable `input2` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
public class WriteSPInstruction extends SPInstruction implements LineageTraceable {
	public CPOperand input1 = null;
	private CPOperand input2 = null;
	private CPOperand input3 = null;
	private CPOperand input4 = null;
```

### UnusedAssignment
Variable `zeroBlk` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
			// Global index of row (1-based)
			long lGblStartRow = ((kv._1.longValue()-1)/_blen)*_blen+1;
			FrameBlock zeroBlk = null;
			int iMaxRowsToCopy = 0;
			
```

### UnusedAssignment
Variable `iMaxRowsToCopy` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
			long lGblStartRow = ((kv._1.longValue()-1)/_blen)*_blen+1;
			FrameBlock zeroBlk = null;
			int iMaxRowsToCopy = 0;
			
			// Starting local location (0-based) of target block where to start copy. 
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java

		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private IndexRange _ixrange = null;
		private int _blen = -1;
		private long _rlen = -1;
		
		public ZeroOutLHS(boolean complement, IndexRange range, DataCharacteristics mcLeft) {
```

### UnusedAssignment
Variable `_complement` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -2672267231152496854L;

		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_ixrange` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java

		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
		private long _rlen = -1;
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
			//execute right indexing operation (partitioning-preserving if possible)
			JavaPairRDD<Long,FrameBlock> in1 = sec.getFrameBinaryBlockRDDHandleForVariable( input1.getName() );
			JavaPairRDD<Long,FrameBlock> out = null;
			if( isPartitioningPreservingRightIndexing(mcIn, ixrange) ) {
				out = in1.mapPartitionsToPair(
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
			PartitionedBroadcast<FrameBlock> broadcastIn2 = null;
			JavaPairRDD<Long,FrameBlock> in2 = null;
			JavaPairRDD<Long,FrameBlock> out = null;
			
			//update and check output dimensions
```

### UnusedAssignment
Variable `_ixrange` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java

		private PartitionedBroadcast<FrameBlock> _binput;
		private IndexRange _ixrange = null;
		
		public LeftIndexPartitionFunction(PartitionedBroadcast<FrameBlock> binput, IndexRange ixrange, DataCharacteristics mc)
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private boolean _complement = false;
		private IndexRange _ixrange = null;
		private int _blen = -1;
		private long _rlen = -1;
		
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
		
		public SliceRHSForLeftIndexing(IndexRange ixrange, DataCharacteristics mcLeft) {
```

### UnusedAssignment
Variable `_ixrange` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5724800998701216440L;

		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private IndexRange _ixrange = null; 
		private int _blen = -1;
		private long _rlen = -1;
		private long _clen = -1;
		
```

### UnusedAssignment
Variable `seedsRDD` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

		//step 2: seed generation
		JavaPairRDD<Long, Long> seedsRDD = null;
		Well1024a bigrand = LibMatrixDatagen.setupSeedsForRand(lSeed);
		double totalSize = OptimizerUtils.estimatePartitionedSizeExactSparsity( lrows, lcols, -1, 1);
```

### UnusedAssignment
Variable `r` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
	private static class AttachRandom implements PairFunction<Double, Double, Double> {
		private static final long serialVersionUID = -7508858192367406554L;
		Random r = null;
		AttachRandom() {
			r = new Random();
```

### UnusedAssignment
Variable `seed` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
	private final double sparsity;
	private final String pdf, pdfParams, frame_data, schema;
	private long seed = 0;
	private final String dir;
	private final CPOperand seq_from, seq_to, seq_incr;
```

### UnusedAssignment
Variable `offsetsRDD` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		
		//step 1: offset generation 
		JavaRDD<Double> offsetsRDD = null;
		long nnz = UtilFunctions.getSeqLength(lfrom, lto, lincr);
		double totalSize = OptimizerUtils.estimatePartitionedSizeExactSparsity( nnz, 1, blocksize,
```

### UnusedAssignment
Variable `_method` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
	private static final long INMEMORY_NUMBLOCKS_THRESHOLD = 1024 * 1024;

	private OpOpDG _method = null;
	private final CPOperand rows, cols, dims;
	private int blocksize;
```

### UnusedAssignment
Variable `seedsRDD` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

		//step 3: seed generation
		JavaPairRDD<MatrixIndexes, Long> seedsRDD = null;
		Well1024a bigrand = LibMatrixDatagen.setupSeedsForRand(lSeed);
		double totalSize = OptimizerUtils.estimatePartitionedSizeExactSparsity( lrows, lcols, blocksize,
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			//long seed = kv._2;

			FrameBlock out = null;
			if(_data.equals("")) {
				//TODO fix hard-coded seed
```

### UnusedAssignment
The value `br` assigned to `_blen` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
			String delim, boolean fill, double fillValue, String opcode, String instr, Set<String> naStrings) {
		super(SPType.CSVReblock, op, in, out, opcode, instr);
		_blen = br;
		_blen = bc;
		_hasHeader = hasHeader;
```

### UnusedAssignment
Variable `_lineageCached` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/LineageObject.java`
#### Snippet
```java
	protected int _numRef = -1;
	protected int _maxNumRef = -1;
	protected boolean _lineageCached = false;
	protected final List<LineageObject> _childs;
	
```

### UnusedAssignment
Variable `_numRef` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/LineageObject.java`
#### Snippet
```java
{
	//basic lineage information
	protected int _numRef = -1;
	protected int _maxNumRef = -1;
	protected boolean _lineageCached = false;
```

### UnusedAssignment
Variable `_rddHandle` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/RDDObject.java`
#### Snippet
```java
public class RDDObject extends LineageObject
{
	private JavaPairRDD<?,?> _rddHandle = null;
	
	//meta data on origin of given rdd handle
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/SpoofSPInstruction.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes, MatrixBlock[]> in = createJoinedInputRDD(
			sec, _in, bcVect, (_class.getSuperclass() == SpoofOuterProduct.class));
		JavaPairRDD<MatrixIndexes, MatrixBlock> out = null;

		//create lists of input broadcasts and scalars
```

### UnusedAssignment
Variable `_ops` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/SpoofSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5978731867787952513L;

		private AggOp[] _ops = null;

		public MultiAggAggregateFunction( AggOp[] ops ) {
```

### UnusedAssignment
Variable `_consumed` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/LazyIterableIterator.java`
#### Snippet
```java
{
	private Iterator<T> _iterator = null;
	private boolean _consumed = false;

	public LazyIterableIterator(Iterator<T> in) 
```

### UnusedAssignment
Variable `_iterator` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/LazyIterableIterator.java`
#### Snippet
```java
public abstract class LazyIterableIterator<T> implements Iterable<T>, Iterator<T>
{
	private Iterator<T> _iterator = null;
	private boolean _consumed = false;

```

### UnusedAssignment
Variable `_ncparts` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/BlockPartitioner.java`
#### Snippet
```java
	
	private int _numParts = -1;
	private int _ncparts = -1;
	private long _rbPerPart = -1;
	private long _cbPerPart = -1;
```

### UnusedAssignment
Variable `_numParts` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/BlockPartitioner.java`
#### Snippet
```java
	private static final long serialVersionUID = 3207938407732880324L;
	
	private int _numParts = -1;
	private int _ncparts = -1;
	private long _rbPerPart = -1;
```

### UnusedAssignment
Variable `_cbPerPart` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/BlockPartitioner.java`
#### Snippet
```java
	private int _ncparts = -1;
	private long _rbPerPart = -1;
	private long _cbPerPart = -1;
	
	public BlockPartitioner(DataCharacteristics mc, int numParts)
```

### UnusedAssignment
Variable `_rbPerPart` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/BlockPartitioner.java`
#### Snippet
```java
	private int _numParts = -1;
	private int _ncparts = -1;
	private long _rbPerPart = -1;
	private long _cbPerPart = -1;
	
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	private long _rlen = -1;
	private long _clen = -1;
	private int _blen = -1;
	
	private ValueType[] _schema;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	private FrameCell[] _buff = null;
	
	private long _rlen = -1;
	private long _clen = -1;
	private int _blen = -1;
```

### UnusedAssignment
Variable `_buff` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	private int _count = -1;
	
	private FrameCell[] _buff = null;
	
	private long _rlen = -1;
```

### UnusedAssignment
The value `Math.max((int)(_bufflen/_clen), 1)` assigned to `_blen` is never used
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
		_rlen = rlen;
		_clen = clen;
		_blen = Math.max((int)(_bufflen/_clen), 1);
		_blen = (int)clen;
		
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	
	private long _rlen = -1;
	private long _clen = -1;
	private int _blen = -1;
	
```

### UnusedAssignment
Variable `_count` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	
	private int _bufflen = -1;
	private int _count = -1;
	
	private FrameCell[] _buff = null;
```

### UnusedAssignment
Variable `_bufflen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	public static final int DEFAULT_BUFFER_SIZE = 5000000;
	
	private int _bufflen = -1;
	private int _count = -1;
	
```

### UnusedAssignment
Variable `_indexes` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/IndexedMatrixValue.java`
#### Snippet
```java
	private static final long serialVersionUID = 6723389820806752110L;

	private MatrixIndexes _indexes = null;
	private MatrixValue   _value = null;
	
```

### UnusedAssignment
Variable `_deep` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 7807210434431147007L;
		
		private boolean _deep = false;
		
		public MergeBlocksFunction(boolean deep) {
```

### UnusedAssignment
Variable `_mcIn` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
		
		private ReorgOperator _reorgOp = null;
		private DataCharacteristics _mcIn = null;
		
		public RDDDiagV2MFunction(DataCharacteristics mcIn) {
```

### UnusedAssignment
Variable `_reorgOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 31065772250744103L;
		
		private ReorgOperator _reorgOp = null;
		private DataCharacteristics _mcIn = null;
		
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
		//get input rdd handle
		JavaPairRDD<MatrixIndexes,MatrixBlock> in1 = sec.getBinaryMatrixBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		DataCharacteristics mcIn = sec.getDataCharacteristics(input1.getName());
		
```

### UnusedAssignment
Variable `_mcIn` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 1183373828539843938L;
		
		private DataCharacteristics _mcIn = null;
		
		public RDDRevFunction(DataCharacteristics mcIn) {
```

### UnusedAssignment
Variable `_deepCopy` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyFrameBlockFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 612972882700587381L;
	
	private boolean _deepCopy = true;
	
	public CopyFrameBlockFunction() {
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/OuterVectorBinaryOpFunction.java`
#### Snippet
```java
		@Override
		public Tuple2<MatrixIndexes, MatrixBlock> next() {
			Tuple2<MatrixIndexes,MatrixBlock> ret = null;
			try {
				//produce next output tuple
```

### UnusedAssignment
Variable `_currBlk` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/OuterVectorBinaryOpFunction.java`
#### Snippet
```java
	private class OuterVectorBinaryOpIterator implements Iterable<Tuple2<MatrixIndexes, MatrixBlock>>, Iterator<Tuple2<MatrixIndexes, MatrixBlock>>
	{
		private Tuple2<MatrixIndexes, MatrixBlock> _currBlk = null;
		private int _currPos = -1;
		
```

### UnusedAssignment
Variable `_currPos` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/OuterVectorBinaryOpFunction.java`
#### Snippet
```java
	{
		private Tuple2<MatrixIndexes, MatrixBlock> _currBlk = null;
		private int _currPos = -1;
		
		public OuterVectorBinaryOpIterator(Tuple2<MatrixIndexes, MatrixBlock> in) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/AggregateDropCorrectionFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -5573656897943638857L;
	
	private AggregateOperator _op = null;
	
	public AggregateDropCorrectionFunction(AggregateOperator op)
```

### UnusedAssignment
Variable `_deepCopy` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyMatrixBlockPairFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -196553327495233360L;

	private boolean _deepCopy = true;
	
	public CopyMatrixBlockPairFunction() {
```

### UnusedAssignment
Variable `block` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyMatrixBlockPairFunction.java`
#### Snippet
```java
			if( _deepCopy ) {
				MatrixIndexes ix = new MatrixIndexes(arg._1());
				MatrixBlock block = null;
				//always create deep copies in more memory-efficient CSR representation 
				//if block is already in sparse format
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		private int _bufflen = -1;

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `cell` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
			Iterator<?> iter = (Iterator<?>) arg0;
			while( iter.hasNext() ) {
				Tuple2<MatrixIndexes, MatrixCell> cell = null;
				switch(converter) {
					case MATRIXENTRY_TO_MATRIXCELL:
```

### UnusedAssignment
Variable `helper` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		private static final long serialVersionUID = 4907483236186747224L;

		private IJVToBinaryBlockFunctionHelper helper = null;
		public MatrixEntryToBinaryBlockFunction(DataCharacteristics mc) {
			helper = new IJVToBinaryBlockFunctionHelper(mc);
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;

```

### UnusedAssignment
Variable `_bufflen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		//internal buffer size (aligned w/ default matrix block size)
		private static final int BUFFER_SIZE = 4 * 1000 * 1000; //4M elements (32MB)
		private int _bufflen = -1;

		private long _rlen = -1;
```

### UnusedAssignment
Variable `_aNnz` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ComputeBinaryBlockNnzFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -8396410450821999936L;
	
	private LongAccumulator _aNnz = null;
	
	public ComputeBinaryBlockNnzFunction( LongAccumulator aNnz ) {
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;

		public IJVToBinaryBlockFunctionHelper(DataCharacteristics mc) {
```

### UnusedAssignment
Variable `_deepCopy` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyFrameBlockPairFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -4686652006382558021L;
	
	private boolean _deepCopy = true;
	
	public CopyFrameBlockPairFunction() {
```

### UnusedAssignment
Variable `_deepCopy` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyMatrixBlockFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 966409324406154236L;

	private boolean _deepCopy = true;
	
	public CopyMatrixBlockFunction() {
```

### UnusedAssignment
Variable `_indexFnObject` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReorgMapFunction.java`
#### Snippet
```java
	
	private ReorgOperator _reorgOp = null;
	private IndexFunction _indexFnObject = null;
	
	public ReorgMapFunction(String opcode) {
```

### UnusedAssignment
Variable `_reorgOp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReorgMapFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 31065772250744103L;
	
	private ReorgOperator _reorgOp = null;
	private IndexFunction _indexFnObject = null;
	
```

### UnusedAssignment
Variable `_stype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CreateSparseBlockFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -4503367283351708178L;
	
	private SparseBlock.Type _stype = null;
	
	public CreateSparseBlockFunction( SparseBlock.Type stype ) {
```

### UnusedAssignment
Variable `_deep` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private AggregateOperator _op = null;
		private MatrixBlock _corr = null;
		private boolean _deep = false;
		
		public SumSingleBlockFunction(boolean deep) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 389422125491172011L;
		
		private AggregateOperator _op = null;	
		
		public MergeAggBlockValueFunction(AggregateOperator aop)
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 1737038715965862222L;
		
		private AggregateOperator _op = null;
		private MatrixBlock _corr = null;
		private boolean _deep = false;
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -3672377410407066396L;

		private AggregateOperator _op = null;
		private MatrixBlock _corr = null;
		
```

### UnusedAssignment
Variable `_deep` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
	{		
		private static final long serialVersionUID = -8881019027250258850L;
		private boolean _deep = false;
		
		@SuppressWarnings("unused")
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5665180309149919945L;

		private AggregateOperator _op = null;

		public AggregateSingleTensorBlockFunction( AggregateOperator op ) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 4803711632648880797L;
		
		private AggregateOperator _op = null;
		
		public MergeAggBlockCombinerFunction(AggregateOperator aop)
```

### UnusedAssignment
Variable `_ngroups` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroup.java`
#### Snippet
```java

	protected long _blen = -1; 
	protected long _ngroups = -1; 
	protected Operator _op = null;
	
```

### UnusedAssignment
Variable `_pbm` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroup.java`
#### Snippet
```java
		private static final long serialVersionUID = 5709955602290131093L;
		
		private PartitionedBroadcast<MatrixBlock> _pbm = null;
		
		public ExtractGroupBroadcast( PartitionedBroadcast<MatrixBlock> pbm, long blen, long ngroups, Operator op ) {
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroup.java`
#### Snippet
```java
	protected long _blen = -1; 
	protected long _ngroups = -1; 
	protected Operator _op = null;
	
	public ExtractGroup( long blen, long ngroups, Operator op ) {
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroup.java`
#### Snippet
```java
	private static final long serialVersionUID = -7059358143841229966L;

	protected long _blen = -1; 
	protected long _ngroups = -1; 
	protected Operator _op = null;
```

### UnusedAssignment
Variable `_maxRowsPerBlock` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private boolean _containsID = false;
		private int _colVect = -1;
		private int _maxRowsPerBlock = -1;
		
		public DataFrameToBinaryBlockFunction(DataCharacteristics mc, String[] colnames,
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 2269315691094111843L;

		private long _clen = -1;
		private String[] _colnames = null;
		private ValueType[] _schema = null;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 6205071301074768437L;

		private int _blen = -1;
		private long _clen = -1;
		private int _maxRowsPerBlock = -1;
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -1976803898174960086L;

		private long _clen = -1;
		private boolean _hasHeader = false;
		private String _delim = null;
```

### UnusedAssignment
Variable `_containsID` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private String[] _colnames = null;
		private ValueType[] _schema = null;
		private boolean _containsID = false;
		private int _colVect = -1;
		private int _maxRowsPerBlock = -1;
```

### UnusedAssignment
Variable `_schema` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -6736256507697511070L;

		private ValueType[] _schema = null;
		private String _delim = null; 
		
```

### UnusedAssignment
Variable `_colVect` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private ValueType[] _schema = null;
		private boolean _containsID = false;
		private int _colVect = -1;
		private int _maxRowsPerBlock = -1;
		
```

### UnusedAssignment
Variable `_sparse` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private int _maxRowsPerBlock = -1;
		private boolean _sparse = false;
		
		public MatrixFrameReblockFunction(DataCharacteristics dc) {
```

### UnusedAssignment
Variable `_schema` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private String[] _colnames = null;
		private ValueType[] _schema = null;
		private boolean _containsID = false;
		private int _colVect = -1;
```

### UnusedAssignment
Variable `_maxRowsPerBlock` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private int _blen = -1;
		private long _clen = -1;
		private int _maxRowsPerBlock = -1;
		private boolean _sparse = false;
		
```

### UnusedAssignment
Variable `_hasHeader` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		private long _clen = -1;
		private boolean _hasHeader = false;
		private String _delim = null;
		private int _maxRowsPerBlock = -1; 
```

### UnusedAssignment
Variable `_delim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private boolean _hasHeader = false;
		private String _delim = null;
		private int _maxRowsPerBlock = -1; 
		private ValueType[] _schema = null;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 3716019666116660815L;

		private int _blen = -1;
			
		public MatrixToFrameBlockFunction(DataCharacteristics mc) {
```

### UnusedAssignment
Variable `_pmV` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/MatrixVectorBinaryOpPartitionFunction.java`
#### Snippet
```java
	
	private BinaryOperator _op = null;
	private PartitionedBroadcast<MatrixBlock> _pmV = null;
	private VectorType _vtype = null;
	
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		protected int _bufflen = -1;
		protected long _rlen = -1;
		protected long _clen = -1;
		
```

### UnusedAssignment
Variable `_op` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/MatrixVectorBinaryOpPartitionFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 9096091404578628534L;
	
	private BinaryOperator _op = null;
	private PartitionedBroadcast<MatrixBlock> _pmV = null;
	private VectorType _vtype = null;
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		private int _blen = -1;
		private long _clen = -1;
		private int _maxRowsPerBlock = -1;
		private boolean _sparse = false;
```

### UnusedAssignment
Variable `_vtype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/MatrixVectorBinaryOpPartitionFunction.java`
#### Snippet
```java
	private BinaryOperator _op = null;
	private PartitionedBroadcast<MatrixBlock> _pmV = null;
	private VectorType _vtype = null;
	
	public MatrixVectorBinaryOpPartitionFunction( BinaryOperator op, PartitionedBroadcast<MatrixBlock> binput, VectorType vtype ) 
```

### UnusedAssignment
Variable `_maxRowsPerBlock` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private boolean _hasHeader = false;
		private String _delim = null;
		private int _maxRowsPerBlock = -1; 
		private ValueType[] _schema = null;
		private String[] _colnames = null;
```

### UnusedAssignment
Variable `_bufflen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -729614449626680946L;

		protected int _bufflen = -1;
		protected long _rlen = -1;
		protected long _clen = -1;
```

### UnusedAssignment
Variable `_colnames` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		private long _clen = -1;
		private String[] _colnames = null;
		private ValueType[] _schema = null;
		private boolean _containsID = false;
```

### UnusedAssignment
Variable `_delim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		private ValueType[] _schema = null;
		private String _delim = null; 
		
		public RowGenerator(ValueType[] schema, String delim) {
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		protected int _bufflen = -1;
		protected long _rlen = -1;
		protected long _clen = -1;
		
		protected CellToBinaryBlockFunction(DataCharacteristics mc)
```

### UnusedAssignment
Variable `_schema` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private String _delim = null;
		private int _maxRowsPerBlock = -1; 
		private ValueType[] _schema = null;
		private String[] _colnames = null;
		private List<String> _mvMeta = null; //missing value meta data
```

### UnusedAssignment
Variable `_props` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 8020608184930291069L;

		private FileFormatPropertiesCSV _props = null;
		
		public BinaryBlockToCSVFunction(FileFormatPropertiesCSV props) {
```

### UnusedAssignment
Variable `_schema` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
	{
		private static final long serialVersionUID = -2042208027876880588L;
		ValueType[] _schema = null;
		
		protected TextToBinaryBlockFunction(DataCharacteristics mc, ValueType[] schema ) {
```

### UnusedAssignment
Variable `dt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		int col = 1;
		for (ValueType schema : fschema) {
			DataType dt = null;
			switch(schema) {
				case STRING:  dt = DataTypes.StringType; break;
```

### UnusedAssignment
Variable `mcNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cpfile/MatrixIndexingCPFileInstruction.java`
#### Snippet
```java
				//create output matrix object
				MatrixObject mobj = new MatrixObject(mo.getValueType(), pfname );
				DataCharacteristics mcNew = null;
				switch( mo.getPartitionFormat() ) {
					case ROW_WISE:
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 9113122668214965797L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -3976735381580482118L;
		
		private int _blen = -1;
		
		public ExtractDoubleValuesWithIndexFunction(int blen)
```

### UnusedAssignment
Variable `_inIter` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private class ShuffleMatrixIterator implements Iterable<Tuple2<MatrixIndexes, RowMatrixBlock>>, Iterator<Tuple2<MatrixIndexes, RowMatrixBlock>>
		{
			private Iterator<Tuple2<MatrixIndexes, Tuple2<MatrixBlock, MatrixBlock>>> _inIter = null;
			private Tuple2<MatrixIndexes, Tuple2<MatrixBlock, MatrixBlock>> _currBlk = null;
			private int _currPos = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction5(long rlen, int blen)
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -8638434373377180192L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ShuffleMatrixBlockRowsFunction(long rlen, int blen)
```

### UnusedAssignment
Variable `_inIter` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private class ShuffleMatrixIterator implements Iterable<Tuple2<MatrixIndexes, RowMatrixBlock>>, Iterator<Tuple2<MatrixIndexes, RowMatrixBlock>>
		{
			private Iterator<Tuple2<MatrixIndexes, MatrixBlock>> _inIter = null;
			private Tuple2<MatrixIndexes, MatrixBlock> _currBlk = null;
			private int _currPos = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction3(long rlen, int blen)
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;

		private Broadcast<PartitionedBlock<MatrixBlock>> _pmb = null;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 9113122668214965797L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction2(long rlen, int blen) {
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5351649694631911694L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction4(long rlen, int blen)
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 6357994683868091724L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction(long rlen, int blen) {
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		
		private long _rlen = -1;
		private int _blen = -1;
		
		public ConvertToBinaryBlockFunction6(long rlen, int blen)
```

### UnusedAssignment
Variable `_pmb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private int _blen = -1;

		private Broadcast<PartitionedBlock<MatrixBlock>> _pmb = null;
		
		public ShuffleMatrixBlockRowsInMemFunction(long rlen, int blen, Broadcast<PartitionedBlock<MatrixBlock>> pmb)
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 6885207719329119646L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5000298196472931653L;
		
		private long _rlen = -1;
		private int _blen = -1;
		
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 6885207719329119646L; 
		
		private long _rlen = -1;
		private int _blen = -1;

```

### UnusedAssignment
Variable `_incr` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ForProgramBlock.java`
#### Snippet
```java
		private long _cur = -1;
		private long _to = -1;
		private long _incr = -1;
		private boolean _inuse = false;
		
```

### UnusedAssignment
Variable `_to` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ForProgramBlock.java`
#### Snippet
```java
	{
		private long _cur = -1;
		private long _to = -1;
		private long _incr = -1;
		private boolean _inuse = false;
```

### UnusedAssignment
Variable `_cur` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ForProgramBlock.java`
#### Snippet
```java
	protected static class SequenceIterator implements Iterator<IntObject>, Iterable<IntObject>
	{
		private long _cur = -1;
		private long _to = -1;
		private long _incr = -1;
```

### UnusedAssignment
Variable `dict` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/Program.java`
#### Snippet
```java
	public synchronized void removeFunctionProgramBlock(String namespace, String fname) {
		namespace = getSafeNamespace(namespace);
		FunctionDictionary<?> dict = null;
		if( _namespaces.containsKey(namespace) ){
			dict = _namespaces.get(namespace);
```

### UnusedAssignment
Variable `_constraint` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoringCmin.java`
#### Snippet
```java
{
	
	protected long _constraint = -1;
	
	public TaskPartitionerFactoringCmin( long taskSize, int numThreads, long constraint, String iterVarName, IntObject fromVal, IntObject toVal, IntObject incrVal ) 
```

### UnusedAssignment
Variable `_n` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitioner.java`
#### Snippet
```java
	//instance variables
	protected PDataPartitionFormat _format = null;
	protected int _n = -1; //blocksize if applicable
	protected boolean _allowBinarycell = true;
	
```

### UnusedAssignment
Variable `_format` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitioner.java`
#### Snippet
```java
	
	//instance variables
	protected PDataPartitionFormat _format = null;
	protected int _n = -1; //blocksize if applicable
	protected boolean _allowBinarycell = true;
```

### UnusedAssignment
Variable `_constraint` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoringCmax.java`
#### Snippet
```java
{
	
	protected long _constraint = -1;
	
	public TaskPartitionerFactoringCmax( long taskSize, int numThreads, long constraint, String iterVarName, IntObject fromVal, IntObject toVal, IntObject incrVal ) 
```

### UnusedAssignment
Variable `foNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeFrameLocalMemory.java`
#### Snippet
```java
	public FrameObject executeSerialMerge() 
	{
		FrameObject foNew = null; //always create new matrix object (required for nested parallelism)
		
		if( LOG.isTraceEnabled() )
```

### UnusedAssignment
Variable `lTask` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalParWorker.java`
#### Snippet
```java
		
		// continuous execution (execute tasks until (1) stopped or (2) no more tasks)
		Task lTask = null; 
		try {
			while( !_stopped ) {
```

### UnusedAssignment
Variable `_incrVal` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
	protected IntObject      _fromVal      = null;
	protected IntObject      _toVal        = null;
	protected IntObject      _incrVal      = null;
	protected long           _numIter      = -1;
	
```

### UnusedAssignment
Variable `_toVal` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
	protected String  		 _iterVarName  = null;
	protected IntObject      _fromVal      = null;
	protected IntObject      _toVal        = null;
	protected IntObject      _incrVal      = null;
	protected long           _numIter      = -1;
```

### UnusedAssignment
Variable `_numIter` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
	protected IntObject      _toVal        = null;
	protected IntObject      _incrVal      = null;
	protected long           _numIter      = -1;
	
	protected TaskPartitioner( long taskSize, String iterVarName, IntObject fromVal, IntObject toVal, IntObject incrVal ) 
```

### UnusedAssignment
Variable `_iterVarName` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
{	
	protected long           _taskSize     = -1;	
	protected String  		 _iterVarName  = null;
	protected IntObject      _fromVal      = null;
	protected IntObject      _toVal        = null;
```

### UnusedAssignment
Variable `_fromVal` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
	protected long           _taskSize     = -1;	
	protected String  		 _iterVarName  = null;
	protected IntObject      _fromVal      = null;
	protected IntObject      _toVal        = null;
	protected IntObject      _incrVal      = null;
```

### UnusedAssignment
Variable `_taskSize` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitioner.java`
#### Snippet
```java
public abstract class TaskPartitioner 
{	
	protected long           _taskSize     = -1;	
	protected String  		 _iterVarName  = null;
	protected IntObject      _fromVal      = null;
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
		boolean withCompare = (compare!=null);

		RDDObject ret = null;
		
		//determine degree of parallelism
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
			
			//Step 2a: merge with compare
			JavaPairRDD<MatrixIndexes, MatrixBlock> out = null;
			if( withCompare ) {
				JavaPairRDD<MatrixIndexes, MatrixBlock> compareRdd = (JavaPairRDD<MatrixIndexes, MatrixBlock>) 
```

### UnusedAssignment
Variable `_ec` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
	private static final long serialVersionUID = -6924566953903424820L;
	
	private ExecutionContext _ec = null;
	private int  _numMappers = -1;
	private int  _numReducers = -1;
```

### UnusedAssignment
Variable `_numMappers` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
	
	private ExecutionContext _ec = null;
	private int  _numMappers = -1;
	private int  _numReducers = -1;
	
```

### UnusedAssignment
Variable `moNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
	public MatrixObject executeParallelMerge(int par) 
	{
		MatrixObject moNew = null; //always create new matrix object (required for nested parallelism)

		if( LOG.isTraceEnabled() )
```

### UnusedAssignment
Variable `_numReducers` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
	private ExecutionContext _ec = null;
	private int  _numMappers = -1;
	private int  _numReducers = -1;
	
	public ResultMergeRemoteSpark(MatrixObject out, MatrixObject[] in, String outputFilename, boolean accum,
```

### UnusedAssignment
Variable `partition` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteDPParForSparkWorker.java`
#### Snippet
```java
		throws IOException 
	{
		MatrixBlock partition = null;

		//reset reuse block, keep configured representation
```

### UnusedAssignment
Variable `_caching` initializer `true` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteParForSparkWorker.java`
#### Snippet
```java
	private final HashMap<String, byte[]> _clsMap;
	private boolean _initialized = false;
	private boolean _caching = true;
	private final boolean _cleanCache;
	private final Map<String,String> _lineage;
```

### UnusedAssignment
Variable `_initialized` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteParForSparkWorker.java`
#### Snippet
```java
	private final boolean _isLocal;
	private final HashMap<String, byte[]> _clsMap;
	private boolean _initialized = false;
	private boolean _caching = true;
	private final boolean _cleanCache;
```

### UnusedAssignment
Variable `_closedInput` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalTaskQueue.java`
#### Snippet
```java
	
	private LinkedList<T>  _data        = null;
	private boolean 	   _closedInput = false; 
	private static final Log LOG = LogFactory.getLog(LocalTaskQueue.class.getName());
	
```

### UnusedAssignment
Variable `_data` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalTaskQueue.java`
#### Snippet
```java
	public static final Object NO_MORE_TASKS = null; //object to signal NO_MORE_TASKS
	
	private LinkedList<T>  _data        = null;
	private boolean 	   _closedInput = false; 
	private static final Log LOG = LogFactory.getLog(LocalTaskQueue.class.getName());
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
```

### UnusedAssignment
Variable `_props` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		implements FlatMapFunction<Tuple2<MatrixIndexes, MatrixBlock>, String> {
		private static final long serialVersionUID = 1891768410987528573L;
		private FileFormatPropertiesLIBSVM _props = null;

		public BinaryBlockToLIBSVMFunction(FileFormatPropertiesLIBSVM props) {
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		protected long _rlen = -1;
		protected long _clen = -1;
		protected int _blen = -1;

```

### UnusedAssignment
Variable `_sparsity` initializer `1.0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _header = false;
```

### UnusedAssignment
Variable `_props` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 1891768410987528573L;

		private FileFormatPropertiesCSV _props = null;

		public BinaryBlockToCSVFunction(FileFormatPropertiesCSV props) {
```

### UnusedAssignment
Variable `_sparsity` initializer `1.0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _containsID;
```

### UnusedAssignment
Variable `_indexDelim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private LongAccumulator _aNnz = null;
		private String _delim = null;
		private String _indexDelim = null;

		public LIBSVMAnalysisFunction(LongAccumulator aNnz, String delim, String indexDelim) {
```

### UnusedAssignment
Variable `_aNnz` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5705371332119770215L;

		private LongAccumulator _aNnz = null;
		private boolean _containsID;
		private boolean _isVector;
```

### UnusedAssignment
Variable `_sparse` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _header = false;
		private String _delim = null;
```

### UnusedAssignment
Variable `_delim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private LongAccumulator _aNnz = null;
		private String _delim = null;
		private String _indexDelim = null;

```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 653447740362447236L;

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_fillValue` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private String _delim = null;
		private boolean _fill = false;
		private double _fillValue = 0;
		private Set<String> _naStrings;

```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		protected int _bufflen = -1;

		protected long _rlen = -1;
		protected long _clen = -1;
		protected int _blen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 7192024840710093114L;

		private int _blen = -1;

		public SliceBinaryBlockToRowsFunction(int blen) {
```

### UnusedAssignment
Variable `_delim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private boolean _sparse = false;
		private boolean _header = false;
		private String _delim = null;
		private boolean _fill = false;
		private double _fillValue = 0;
```

### UnusedAssignment
Variable `_aNnz` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 2310303223289674477L;

		private LongAccumulator _aNnz = null;
		private String _delim = null;
		private String _indexDelim = null;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		protected long _rlen = -1;
		protected long _clen = -1;
		protected int _blen = -1;

		protected CellToBinaryBlockFunction(DataCharacteristics mc)
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private long _clen = -1;
		private int _blen = -1;
		private int _ncblks = -1;

```

### UnusedAssignment
Variable `_fill` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private boolean _header = false;
		private String _delim = null;
		private boolean _fill = false;
		private double _fillValue = 0;
		private Set<String> _naStrings;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
```

### UnusedAssignment
Variable `_sparse` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _containsID;
		private boolean _isVector;
```

### UnusedAssignment
Variable `_header` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _header = false;
		private String _delim = null;
		private boolean _fill = false;
```

### UnusedAssignment
Variable `_clen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -7879603125149650097L;

		private long _clen = -1;
		private int _blen = -1;
		private int _ncblks = -1;
```

### UnusedAssignment
Variable `_bufflen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		//internal buffer size (aligned w/ default matrix block size)
		protected static final int BUFFER_SIZE = 4 * 1000 * 1000; //4M elements (32MB)
		protected int _bufflen = -1;

		protected long _rlen = -1;
```

### UnusedAssignment
Variable `_rlen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -4948430402942717043L;

		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
```

### UnusedAssignment
Variable `_blen` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private long _rlen = -1;
		private long _clen = -1;
		private int _blen = -1;
		private double _sparsity = 1.0;
		private boolean _sparse = false;
```

### UnusedAssignment
Variable `_index` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 7438855241666363433L;

		private int _index = -1;

		public DataFrameExtractIDFunction(int index) {
```

### UnusedAssignment
Variable `_ncblks` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private long _clen = -1;
		private int _blen = -1;
		private int _ncblks = -1;

		public ConcatenateBlocksFunction(long clen, int blen) {
```

### UnusedAssignment
Variable `K` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long N = _numIter;    // total number of iterations
		long R = N;           // remaining number of iterations
		long K = -1;          // next _numThreads task sizes
		TaskType type = null; // type of iterations: range tasks (similar to run-length encoding) make only sense if taskSize>3
		
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long R = N;           // remaining number of iterations
		long K = -1;          // next _numThreads task sizes
		TaskType type = null; // type of iterations: range tasks (similar to run-length encoding) make only sense if taskSize>3
		
		for( long i = lFrom; i<=lTo;  )
```

### UnusedAssignment
Variable `_numThreads` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
{
	
	private int _numThreads = -1;
	
	public TaskPartitionerFactoring( long taskSize, int numThreads, String iterVarName, IntObject fromVal, IntObject toVal, IntObject incrVal ) 
```

### UnusedAssignment
Variable `K` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long N = _numIter;     // total number of iterations
		long R = N;            // remaining number of iterations
		long K = -1;           //next _numThreads task sizes	
	    TaskType type = null;  // type of iterations: range tasks (similar to run-length encoding) make only sense if taskSize>3
		
```

### UnusedAssignment
Variable `type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long R = N;            // remaining number of iterations
		long K = -1;           //next _numThreads task sizes	
	    TaskType type = null;  // type of iterations: range tasks (similar to run-length encoding) make only sense if taskSize>3
		
		try
```

### UnusedAssignment
Variable `_map` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/CostEstimatorHops.java`
#### Snippet
```java
	public static final double DEFAULT_MEM_SP = 20*1024*1024;
	
	private OptTreePlanMappingAbstract _map = null;
	
	public CostEstimatorHops( OptTreePlanMappingAbstract map ) {
```

### UnusedAssignment
Variable `moNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
	@Override
	public MatrixObject executeSerialMerge() {
		MatrixObject moNew = null; //always create new matrix object (required for nested parallelism)
		
		if( LOG.isTraceEnabled() )
```

### UnusedAssignment
Variable `_seq` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
	
	//internal comparison matrix
	private IDSequence _seq = null;
	
	public ResultMergeLocalFile( MatrixObject out, MatrixObject[] in, String outputFilename, boolean accum )
```

### UnusedAssignment
Variable `valueStr` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
			BufferedWriter out = new BufferedWriter(new OutputStreamWriter(fs.create(path,true)));		
			
			String valueStr = null;
			
			try
```

### UnusedAssignment
Variable `moNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalMemory.java`
#### Snippet
```java
	public MatrixObject executeParallelMerge( int par ) 
	{
		MatrixObject moNew = null; //always create new matrix object (required for nested parallelism)
		
		if( LOG.isTraceEnabled() )
```

### UnusedAssignment
Variable `_outMB` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalMemory.java`
#### Snippet
```java
	{
		private MatrixObject _inMO  = null;
		private MatrixBlock  _outMB = null;
		
		public ResultMergeWorker(MatrixObject inMO, MatrixBlock outMB)
```

### UnusedAssignment
Variable `moNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalMemory.java`
#### Snippet
```java
	public MatrixObject executeSerialMerge() 
	{
		MatrixObject moNew = null; //always create new matrix object (required for nested parallelism)
		
		if( LOG.isTraceEnabled() )
```

### UnusedAssignment
Variable `_inMO` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalMemory.java`
#### Snippet
```java
	private class ResultMergeWorker implements Runnable
	{
		private MatrixObject _inMO  = null;
		private MatrixBlock  _outMB = null;
		
```

### UnusedAssignment
Variable `_numEvaluatedPlans` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/Optimizer.java`
#### Snippet
```java
{
	protected long _numTotalPlans     = -1;
	protected long _numEvaluatedPlans = -1;
	
	public enum PlanInputType {
```

### UnusedAssignment
Variable `_numTotalPlans` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/Optimizer.java`
#### Snippet
```java
public abstract class Optimizer 
{
	protected long _numTotalPlans     = -1;
	protected long _numEvaluatedPlans = -1;
	
```

### UnusedAssignment
Variable `_root` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTree.java`
#### Snippet
```java
	//actual tree
	private PlanInputType _type = null;
	private OptNode       _root = null;
	
	public OptTree( int ck, double cm, OptNode node ) {
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTree.java`
#### Snippet
```java
	
	//actual tree
	private PlanInputType _type = null;
	private OptNode       _root = null;
	
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/CostEstimator.java`
#### Snippet
```java
		{
			//aggreagtion methods for different program block types and measure types
			String tmp = null;
			double N = -1;
			switch ( measure )
```

### UnusedAssignment
Variable `N` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/CostEstimator.java`
#### Snippet
```java
			//aggreagtion methods for different program block types and measure types
			String tmp = null;
			double N = -1;
			switch ( measure )
			{
```

### UnusedAssignment
Variable `_job` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	private abstract class DataPartitionerWorker implements Runnable
	{
		private JobConf _job = null;
		private String _fnameNew = null;
		private String _fnameStaging = null;
```

### UnusedAssignment
Variable `_fnamesPartitions` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
		private String _fnameNew = null;
		private String _fnameStaging = null;
		private String[] _fnamesPartitions = null;
		private int _start = -1;
		private int _end = -1;
```

### UnusedAssignment
Variable `_seq` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	private static final boolean PARALLEL = true; 
	
	private IDSequence _seq = null;
	private MatrixBlock _reuseBlk = null;
	
```

### UnusedAssignment
Variable `_end` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
		private String[] _fnamesPartitions = null;
		private int _start = -1;
		private int _end = -1;
		
		public DataPartitionerWorker(JobConf job, String fnameNew, String fnameStaging, String[] fnamesPartitions, int start, int end)
```

### UnusedAssignment
Variable `_par` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	private MatrixBlock _reuseBlk = null;
	
	private int _par = -1;
	
	/**
```

### UnusedAssignment
Variable `_fnameNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	{
		private JobConf _job = null;
		private String _fnameNew = null;
		private String _fnameStaging = null;
		private String[] _fnamesPartitions = null;
```

### UnusedAssignment
Variable `_fnameStaging` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
		private JobConf _job = null;
		private String _fnameNew = null;
		private String _fnameStaging = null;
		private String[] _fnamesPartitions = null;
		private int _start = -1;
```

### UnusedAssignment
Variable `tmp` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			{
				long key2 = getKey2FromFileName(fnameBlock);
				MatrixBlock tmp = null;
				if( threadsafe )
					tmp = LocalFileUtils.readMatrixBlockFromLocal(lpdir+"/"+fnameBlock);
```

### UnusedAssignment
Variable `_start` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
		private String _fnameStaging = null;
		private String[] _fnamesPartitions = null;
		private int _start = -1;
		private int _end = -1;
		
```

### UnusedAssignment
Variable `_monitorReport` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	protected long _ID = -1;
	protected int _IDPrefix = -1;
	protected boolean _monitorReport = false;
	protected int _numRuns = -1;
	
```

### UnusedAssignment
Variable `_numRuns` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	protected int _IDPrefix = -1;
	protected boolean _monitorReport = false;
	protected int _numRuns = -1;
	
	// local parworker data
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
						break;
				
					CacheableData<?> out = null;
					synchronized( _ec.getVariables() ){
						out = _ec.getCacheableData(var._name);
```

### UnusedAssignment
Variable `pw` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	private LocalParWorker createParallelWorker(long pwID, LocalTaskQueue<Task> queue, ExecutionContext ec, int index)
	{
		LocalParWorker pw = null; 
		
		try
```

### UnusedAssignment
Variable `cpChildBlocks` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		{
			//create deep copies of required elements child blocks
			ArrayList<ProgramBlock> cpChildBlocks = null;
			HashSet<String> fnNames = new HashSet<>();
			if( USE_PB_CACHE )
```

### UnusedAssignment
Variable `iterVar` initializer `new IntObject(from.getLongValue())` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		
		// initialize iter var to form value
		IntObject iterVar = new IntObject(from.getLongValue());
		
		///////
```

### UnusedAssignment
Variable `_taskSize` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	protected int _numThreads = -1;
	protected boolean _fixedDOP = false; //guard for numThreads
	protected long _taskSize = -1;
	protected PTaskPartitioner _taskPartitioner;
	protected PDataPartitioner _dataPartitioner;
```

### UnusedAssignment
Variable `_numThreads` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	protected final boolean _monitor;
	protected final Level _optLogLevel;
	protected int _numThreads = -1;
	protected boolean _fixedDOP = false; //guard for numThreads
	protected long _taskSize = -1;
```

### UnusedAssignment
Variable `numCreatedTasks` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
			TaskPartitioner partitioner = createTaskPartitioner(from, to, incr);
			long numIterations = partitioner.getNumIterations();
			long numCreatedTasks = -1;
			if( USE_STREAMING_TASK_CREATION )
			{
```

### UnusedAssignment
Variable `_sparsity` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java
	private long _dim4 = -1; //cols right
	
	private double     _sparsity  = -1; //sparsity left 
	private DataFormat _df        = null; //data format left
	
```

### UnusedAssignment
Variable `_dim1` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java

	//operation characteristics
	private long _dim1 = -1; //rows left
	private long _dim2 = -1; //cols left
	private long _dim3 = -1; //rows right
```

### UnusedAssignment
Variable `_dim3` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java
	private long _dim1 = -1; //rows left
	private long _dim2 = -1; //cols left
	private long _dim3 = -1; //rows right
	private long _dim4 = -1; //cols right
	
```

### UnusedAssignment
Variable `_dim4` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java
	private long _dim2 = -1; //cols left
	private long _dim3 = -1; //rows right
	private long _dim4 = -1; //cols right
	
	private double     _sparsity  = -1; //sparsity left 
```

### UnusedAssignment
Variable `_dim2` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java
	//operation characteristics
	private long _dim1 = -1; //rows left
	private long _dim2 = -1; //cols left
	private long _dim3 = -1; //rows right
	private long _dim4 = -1; //cols right
```

### UnusedAssignment
Variable `_df` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNodeStatistics.java`
#### Snippet
```java
	
	private double     _sparsity  = -1; //sparsity left 
	private DataFormat _df        = null; //data format left
	
	
```

### UnusedAssignment
Variable `ret` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerConstrained.java`
#### Snippet
```java
	protected boolean rewriteSetExecutionStategy(OptNode n, double M0, double M, double M2, double M3, boolean flagLIX)
	{
		boolean ret = false;

		// constraint awareness
```

### UnusedAssignment
Variable `tree` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizationWrapper.java`
#### Snippet
```java
		LOG.trace("ParFOR Opt: Created optimizer ("+otype+","+opt.getPlanInputType()+","+opt.getCostModelType());
		
		OptTree tree = null;
		
		//recompile parfor body 
```

### UnusedAssignment
Variable `javaOptsNew` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/InfrastructureAnalyzer.java`
#### Snippet
```java
	public static void setMaxMemoryOpt(JobConf job, String key, long bytes) {
		String javaOptsOld = job.get( key );
		String javaOptsNew = null;
		String[] tokens = javaOptsOld.split(" "); //account also for no ' '
		StringBuilder sb = new StringBuilder();
```

### UnusedAssignment
Variable `ret` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/ProgramRecompiler.java`
#### Snippet
```java
		try
		{
			boolean ret = false;
			hop.resetVisitStatus();
			
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
		FastStringTokenizer st = new FastStringTokenizer(' ');
		try(BufferedReader in = new BufferedReader(new InputStreamReader(fis))) {
			String value = null;
			if( sparse ) {
				while( (value=in.readLine())!=null ) {
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
		throws NumberFormatException, IOException
	{
		String value = null;
		FastStringTokenizer st = new FastStringTokenizer(' ');
		while( (value=in.readLine())!=null )
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
		map.clear();
		
		String value = null;
		int len = 0;
		FastStringTokenizer st = new FastStringTokenizer(' ');
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
		LinkedList<Cell> buffer = new LinkedList<>();
		try(BufferedReader in = new BufferedReader(new InputStreamReader(fis)))  {
			String value = null;
			FastStringTokenizer st = new FastStringTokenizer(' '); 
			while( (value=in.readLine())!=null ) {
```

### UnusedAssignment
Variable `uuid` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/IDHandler.java`
#### Snippet
```java
	 */
	public static String createDistributedUniqueID() {
		String uuid = null;

		try {
```

### UnusedAssignment
Variable `_ntype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
	//node configuration 
	private long                      _id      = -1;
	private NodeType                  _ntype   = null;
	private ExecType                  _etype   = null;
	private int                       _k       = -1;
```

### UnusedAssignment
Variable `_etype` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
	private long                      _id      = -1;
	private NodeType                  _ntype   = null;
	private ExecType                  _etype   = null;
	private int                       _k       = -1;
	private HashMap<ParamType,String> _params  = null;
```

### UnusedAssignment
Variable `_k` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
	private NodeType                  _ntype   = null;
	private ExecType                  _etype   = null;
	private int                       _k       = -1;
	private HashMap<ParamType,String> _params  = null;
	
```

### UnusedAssignment
Variable `pbParent` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
	
	public static void replaceProgramBlock(OptNode parent, OptNode n, ProgramBlock pbOld, ProgramBlock pbNew, OptTreePlanMappingRuntime rtMap) {
		ProgramBlock pbParent = null;
		pbParent = (ProgramBlock) rtMap.getMappedObject( parent.getID() );
		
```

### UnusedAssignment
Variable `pbParent` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java

	public static void replaceProgramBlock(OptNode parent, OptNode n, ProgramBlock pbOld, ProgramBlock pbNew, OptTreePlanMappingAbstract hlMap) {
		ProgramBlock pbParent = null;
		if( parent.getNodeType()==NodeType.FUNCCALL ) {
			FunctionOp fop = (FunctionOp) hlMap.getMappedHop(parent.getID());
```

### UnusedAssignment
Variable `tree` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
		ParForProgramBlock pfpb, OptTreePlanMappingAbstract hlMap, Set<String> memo, ExecutionContext ec ) 
	{
		OptTree tree = null;
		OptNode root = null;
		
```

### UnusedAssignment
Variable `root` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
	{
		OptTree tree = null;
		OptNode root = null;
		
		try {
```

### UnusedAssignment
Variable `tb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/TensorObject.java`
#### Snippet
```java
	@Override
	protected TensorBlock readBlobFromCache(String fname) throws IOException {
		TensorBlock tb = null;
		if (OptimizerUtils.isUMMEnabled())
			tb = (TensorBlock) UnifiedMemoryManager.readBlock(fname, false);
```

### UnusedAssignment
Variable `_cb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheMaintenanceService.java`
#### Snippet
```java
	private static class DataSerializerTask implements Runnable {
		private ByteBuffer _bbuff = null;
		private CacheBlock<?> _cb = null;

		public DataSerializerTask(ByteBuffer bbuff, CacheBlock<?> cb) {
```

### UnusedAssignment
Variable `_bbuff` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheMaintenanceService.java`
#### Snippet
```java

	private static class DataSerializerTask implements Runnable {
		private ByteBuffer _bbuff = null;
		private CacheBlock<?> _cb = null;

```

### UnusedAssignment
Variable `_fname` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheMaintenanceService.java`
#### Snippet
```java

	private static class FileCleanerTask implements Runnable {
		private String _fname = null;

		public FileCleanerTask( String fname ) {
```

### UnusedAssignment
Variable `cb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
		throws IOException
	{
		CacheBlock<?> cb = null;
		ByteBuffer ldata = null;

```

### UnusedAssignment
Variable `ldata` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
	{
		CacheBlock<?> cb = null;
		ByteBuffer ldata = null;

		//probe write buffer
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/ByteBuffer.java`
#### Snippet
```java
		throws IOException
	{
		CacheBlock<?> ret = null;
		
		if( !_shallow ) { //sparse matrix / string frame
```

### UnusedAssignment
Variable `cb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
		throws IOException
	{
		CacheBlock<?> cb = null;
		ByteBuffer ldata = null;
		
```

### UnusedAssignment
Variable `ldata` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
	{
		CacheBlock<?> cb = null;
		ByteBuffer ldata = null;
		
		//probe write buffer
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/MatrixObjectFuture.java`
#### Snippet
```java
				UnifiedMemoryManager.pin(this);

			MatrixBlock out = null;
			long t1 = System.nanoTime();
			out = _futureData.get();
```

### UnusedAssignment
Variable `fb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/FrameObject.java`
#### Snippet
```java
	@Override
	protected FrameBlock readBlobFromCache(String fname) throws IOException {
		FrameBlock fb = null;
		if (OptimizerUtils.isUMMEnabled())
			fb = (FrameBlock) UnifiedMemoryManager.readBlock(fname, false);
```

### UnusedAssignment
Variable `fb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/FrameObject.java`
#### Snippet
```java
			UtilFunctions.nCopies(clen>=1 ? (int)clen : 1, ValueType.STRING);
		
		FrameBlock fb = null;
		try  {
			//prevent unnecessary collect through rdd checkpoint
```

### UnusedAssignment
Variable `mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
		DataCharacteristics mc = iimd.getDataCharacteristics();
		FileFormat fmt = iimd.getFileFormat();
		MatrixBlock mb = null;
		try {
			// prevent unnecessary collect through rdd checkpoint (unless lineage cached)
```

### UnusedAssignment
Variable `rows` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
			{
				// get rows and cols
				long rows = -1;
				long cols = -1;
				switch(_partitionFormat) {
```

### UnusedAssignment
Variable `cols` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
				// get rows and cols
				long rows = -1;
				long cols = -1;
				switch(_partitionFormat) {
					case ROW_WISE:
```

### UnusedAssignment
Variable `mb` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
	@Override
	protected MatrixBlock readBlobFromCache(String fname) throws IOException {
		MatrixBlock mb = null;
		if (OptimizerUtils.isUMMEnabled())
			mb = (MatrixBlock) UnifiedMemoryManager.readBlock(fname, true);
```

### UnusedAssignment
Variable `dim1` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public MatrixObject allocateGPUMatrixObject(String varName, long numRows, long numCols) {
		MatrixObject mo = getMatrixObject(varName);
		long dim1 = -1; long dim2 = -1;

		try {
```

### UnusedAssignment
Variable `dim2` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public MatrixObject allocateGPUMatrixObject(String varName, long numRows, long numCols) {
		MatrixObject mo = getMatrixObject(varName);
		long dim1 = -1; long dim2 = -1;

		try {
```

### UnusedAssignment
Variable `_prog` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java

	//program reference (e.g., function repository)
	protected Program _prog = null;
	
	//symbol table
```

### UnusedAssignment
Variable `_seq` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	
	/** Global sequence for generating unique ids. */
	private static IDSequence _seq = null;

	// Global eviction path and prefix (prefix used for isolation purposes)
```

### UnusedAssignment
Variable `_gpuObjects` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	private RDDObject _rddHandle = null; //RDD handle
	private BroadcastObject<T> _bcHandle = null; //Broadcast handle
	protected HashMap<GPUContext, GPUObject> _gpuObjects = null; //Per GPUContext object allocated on GPU

	private LineageItem _lineage = null;
```

### UnusedAssignment
Variable `_cacheStatus` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	
	/** The cache status of the data blob (whether it can be or is evicted, etc. */
	private CacheStatus _cacheStatus = null;
	
	/** Cache for actual data, evicted by garbage collector. */
```

### UnusedAssignment
Variable `_numReadThreads` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	
	// additional private flags and meta data
	private int     _numReadThreads = 0;   //number of threads for read from HDFS
	private boolean _cleanupFlag = true;   //flag if obj unpinned (cleanup enabled)	
	private String  _cacheFileName = null; //local eviction file name
```

### UnusedAssignment
Variable `_ID` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
public class FederationMap {

	private long _ID = -1;
	private final List<Pair<FederatedRange, FederatedData>> _fedMap;
	private FType _type;
```

### UnusedAssignment
Variable `_linItem` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedResponse.java`
#### Snippet
```java
	private Map<PrivacyLevel,LongAdder> checkedConstraints;

	private transient LineageItem _linItem = null; // not included in serialized object
	
	public FederatedResponse(ResponseType status) {
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
		try {
			MatrixBlock prev = (MatrixBlock) ffr[0].get().getData()[0];
			int size = 0;
			for(int i = 1; i < ffr.length; i++) {
				MatrixBlock next = (MatrixBlock) ffr[i].get().getData()[0];
```

### UnusedAssignment
Variable `result` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
		throws DMLPrivacyException, FederatedWorkerHandlerException, Exception {
		final RequestType method = request.getType();
		FederatedResponse result = null;

		eventStage.startTime = LocalDateTime.now();
```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
		// wrap transferred cache block into cacheable data
		Data data;
		long size = 0;
		if(v instanceof CacheBlock) {
			var block = ExecutionContext.createCacheableData((CacheBlock<?>) v);
```

### UnusedAssignment
Variable `cd` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
	private CacheableData<?> readDataNoReuse(String filename, DataType dataType,
		MatrixCharacteristics mc) {
		CacheableData<?> cd = null;

		switch(dataType) {
```

### UnusedAssignment
Variable `fmt` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
		}

		FileFormat fmt = null;
		boolean header = false;
		String delim = null;
```

### UnusedAssignment
Variable `header` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java

		FileFormat fmt = null;
		boolean header = false;
		String delim = null;
		FileSystem fs = null;
```

### UnusedAssignment
Variable `delim` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
		FileFormat fmt = null;
		boolean header = false;
		String delim = null;
		FileSystem fs = null;
		MetaDataAll mtd;
```

### UnusedAssignment
Variable `kMax` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		else // ExecType.MR/ExecType.SPARK
		{
			int kMax = -1;
			if( flagNested ) {
				//determine remote max parallelism constraint
```

### UnusedAssignment
Variable `var` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
	
	private static String rGetVarFromExpression(Hop current) {
		String var = null;
		for( Hop c : current.getInput() ) {
			var = rGetVarFromExpression(c);
```

### UnusedAssignment
Variable `ret` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		
		PResultMerge REMOTE = PResultMerge.REMOTE_SPARK;
		PResultMerge ret = null;
		
		//investigate details of current parfor node
```

### UnusedAssignment
Variable `W` initializer `-1` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
	protected long estimateNumTasks( PTaskPartitioner tp, long N, int k )
	{
		long W = -1;
		
		switch( tp )
```

### UnusedAssignment
Variable `memoryUsage` initializer `0.0` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

		double cpuUsage = osMXBean.getSystemLoadAverage();
		double memoryUsage = 0.0;

		double maxMemory = (double)memoryMXBean.getHeapMemoryUsage().getMax() / 1073741824;
```

### UnusedAssignment
Variable `rdd` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		InputOutputInfo inputInfo2 = InputOutputInfo.get(DataType.FRAME, fmt); 
		JavaSparkContext sc = getSparkContext();
		JavaPairRDD<?,?> rdd = null;
		//CASE 1: rdd already existing (reuse if checkpoint or trigger
		//pending rdd operations if not yet cached but prevent to re-evaluate
```

### UnusedAssignment
Variable `_legacyVersion` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java

		//meta configurations
		private boolean _legacyVersion = false; //spark version <1.6
		private boolean _confOnly = false; //infrastructure info based on config

```

### UnusedAssignment
Variable `list` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			int blen, int numParts, boolean inclEmpty) {
		long t0 = DMLScript.STATISTICS ? System.nanoTime() : 0;
		List<Tuple2<MatrixIndexes,MatrixBlock>> list = null;

		if( src.getNumRows() <= blen && src.getNumColumns() <= blen ) {
```

### UnusedAssignment
Variable `rdd` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java

		JavaSparkContext sc = getSparkContext();
		JavaPairRDD<?,?> rdd = null;
		InputOutputInfo inputInfo = InputOutputInfo.get(DataType.MATRIX, fmt);
		//CASE 1: rdd already existing (reuse if checkpoint or trigger
```

### UnusedAssignment
Variable `out` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		long t0 = DMLScript.STATISTICS ? System.nanoTime() : 0;

		MatrixBlock out = null;

		//determine target sparse/dense representation
```

### UnusedAssignment
Variable `_confOnly` initializer `false` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		//meta configurations
		private boolean _legacyVersion = false; //spark version <1.6
		private boolean _confOnly = false; //infrastructure info based on config

		//memory management configurations
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
		T resultModel = null;

		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> Long createEntity(T model) {

		PreparedStatement st = null;
		long id = -1L;

```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> List<T> getAllEntities(Class<T> type) {
		List<T> resultModels = new ArrayList<>();
		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> void updateEntity(T model) {

		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> void removeAllEntitiesByField(String fieldName, Object value, Class<T> type) {

		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> void removeEntity(Long id, Class<T> type) {

		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `st` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java
	public <T extends BaseModel> List<T> getAllEntitiesByField(String fieldName, Object value, Class<T> type, int rowCount) {
		List<T> resultModels = new ArrayList<>();
		PreparedStatement st = null;

		try (var db = DriverManager.getConnection(DB_CONNECTION)) {
```

### UnusedAssignment
Variable `db` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/repositories/DerbyRepository.java`
#### Snippet
```java

	private void createMonitoringDatabase() {
		Connection db = null;
		try {
			// Creates only if DB doesn't exist
```

### UnusedAssignment
Variable `_plus` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/Mean.java`
#### Snippet
```java
	private static Mean singleObj = null;
	
	private KahanPlus _plus = null; 
	
	private Mean() {
```

### UnusedAssignment
Variable `_plus` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/COV.java`
#### Snippet
```java
	private static COV singleObj = null;
	
	private KahanPlus _plus = null; 
	
	public static COV getCOMFnObject() {
```

### UnusedAssignment
Variable `_intdiv` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/Modulus.java`
#### Snippet
```java

	private static Modulus singleObj = null;
	private IntegerDivide _intdiv = null;
	
	private Modulus() {
```

### UnusedAssignment
Variable `kplus` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/KahanPlusSq.java`
#### Snippet
```java

	private static KahanPlusSq singleObj = null;
	private KahanPlus kplus = null;

	private KahanPlusSq() {
```

### UnusedAssignment
Variable `distFunction` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/ParameterizedBuiltin.java`
#### Snippet
```java
		}
		
		AbstractRealDistribution distFunction = null;
		
		switch(dcode) {
```

### UnusedAssignment
Variable `ret` initializer `Double.NaN` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/ParameterizedBuiltin.java`
#### Snippet
```java
		}
		
		double ret = Double.NaN;
		if(inverse) {
			// inverse cdf
```

### UnusedAssignment
Variable `_type` initializer `null` is redundant
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
	private static final long serialVersionUID = 9177194651533064123L;

	private AggregateOperationTypes _type = null;
	
	//helper function objects for specific types
```

## RuleId[id=MismatchedStringCase]
### MismatchedStringCase
Switch branch is unreachable: the label contains a lowercase symbol while the selector is uppercase-only
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java

							switch(key.toString().toUpperCase()) {
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
```

### MismatchedStringCase
Switch branch is unreachable: the label contains a lowercase symbol while the selector is uppercase-only
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
							switch(key.toString().toUpperCase()) {
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
							}
```

### MismatchedStringCase
Switch branch is unreachable: the label contains a lowercase symbol while the selector is uppercase-only
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
							}

```

## RuleId[id=DoubleNegation]
### DoubleNegation
Double negation in `!(compress != CompressConfig.FALSE)`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
			.getDMLConfig().getTextValue(DMLConfig.COMPRESSED_LINALG).toUpperCase());
		if( (DMLScript.getGlobalExecMode() == ExecMode.SINGLE_NODE
			&& !(compress != CompressConfig.FALSE) )
			|| !HopRewriteUtils.isLastLevelStatementBlock(sb) )
			return Arrays.asList(sb);
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000004) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureColumnSchemaIsMutable() {
        if (!((bitField0_ & 0x00000004) != 0)) {
          columnSchema_ = new java.util.ArrayList<org.apache.sysds.protobuf.SysdsProtos.Schema>(columnSchema_);
          bitField0_ |= 0x00000004;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureRowsIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          rows_ = new java.util.ArrayList<org.apache.sysds.protobuf.SysdsProtos.Row>(rows_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
              break;
            case 10: {
              if (!((mutable_bitField0_ & 0x00000001) != 0)) {
                rows_ = new java.util.ArrayList<org.apache.sysds.protobuf.SysdsProtos.Row>();
                mutable_bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
        java.util.Collections.emptyList();
      private void ensureValueTypeIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          valueType_ = new java.util.ArrayList<java.lang.Integer>(valueType_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
            case 8: {
              int rawValue = input.readEnum();
              if (!((mutable_bitField0_ & 0x00000001) != 0)) {
                valueType_ = new java.util.ArrayList<java.lang.Integer>();
                mutable_bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
              while(input.getBytesUntilLimit() > 0) {
                int rawValue = input.readEnum();
                if (!((mutable_bitField0_ & 0x00000001) != 0)) {
                  valueType_ = new java.util.ArrayList<java.lang.Integer>();
                  mutable_bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000002) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList columnData_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureColumnDataIsMutable() {
        if (!((bitField0_ & 0x00000002) != 0)) {
          columnData_ = new com.google.protobuf.LazyStringArrayList(columnData_);
          bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      private com.google.protobuf.LazyStringList columnNames_ = com.google.protobuf.LazyStringArrayList.EMPTY;
      private void ensureColumnNamesIsMutable() {
        if (!((bitField0_ & 0x00000001) != 0)) {
          columnNames_ = new com.google.protobuf.LazyStringArrayList(columnNames_);
          bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000001) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
            case 10: {
              java.lang.String s = input.readStringRequireUtf8();
              if (!((mutable_bitField0_ & 0x00000001) != 0)) {
                columnNames_ = new com.google.protobuf.LazyStringArrayList();
                mutable_bitField0_ |= 0x00000001;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000002) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
            case 18: {
              java.lang.String s = input.readStringRequireUtf8();
              if (!((mutable_bitField0_ & 0x00000002) != 0)) {
                columnData_ = new com.google.protobuf.LazyStringArrayList();
                mutable_bitField0_ |= 0x00000002;
```

### DoubleNegation
Double negation in `!((mutable_bitField0_ & 0x00000004) != 0)`
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
            }
            case 26: {
              if (!((mutable_bitField0_ & 0x00000004) != 0)) {
                columnSchema_ = new java.util.ArrayList<org.apache.sysds.protobuf.SysdsProtos.Schema>();
                mutable_bitField0_ |= 0x00000004;
```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `scriptFromFile`
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptFactory.java`
#### Snippet
```java
	 * Creates a DML or PYDML Script object based on a file in the local file
	 * system. To create a Script object from a local file or HDFS, please use
	 * {@link scriptFromFile(String, ScriptType)}.
	 *
	 * @param localScriptFile
```

### JavadocReference
Cannot resolve symbol `ScriptType`
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptFactory.java`
#### Snippet
```java
	 * Creates a DML or PYDML Script object based on a file in the local file
	 * system. To create a Script object from a local file or HDFS, please use
	 * {@link scriptFromFile(String, ScriptType)}.
	 *
	 * @param localScriptFile
```

### JavadocReference
Cannot resolve symbol `scriptType`
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptFactory.java`
#### Snippet
```java
	 * @param localScriptFile
	 *            The local DML or PYDML file
	 * @param scriptType
	 *            {@code ScriptType.DML} or {@code ScriptType.PYDML}
	 * @return DML or PYDML Script object
```

### JavadocReference
Cannot resolve symbol `scriptType`
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptFactory.java`
#### Snippet
```java
	 * @param inputStream
	 *            input stream to DML or PYDML
	 * @param scriptType
	 *            {@code ScriptType.DML} or {@code ScriptType.PYDML}
	 * @return DML or PYDML Script object
```

### JavadocReference
Cannot resolve symbol `scriptType`
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptFactory.java`
#### Snippet
```java
	 * @param scriptFilePath
	 *            path to DML or PYDML script file (local or HDFS)
	 * @param scriptType
	 *            {@code ScriptType.DML} or {@code ScriptType.PYDML}
	 * @return DML or PYDML Script object
```

### JavadocReference
Cannot resolve symbol `sc`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
	 * config.
	 *
	 * @param sc
	 *            SparkContext object.
	 */
```

### JavadocReference
Cannot resolve symbol `init(boolean)`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
	/**
	 * Whether or not the default ScriptExecutor should be initialized before
	 * execution. See {@link ScriptExecutor#init(boolean)}.
	 */
	private boolean initBeforeExecution = true;
```

### JavadocReference
Cannot resolve symbol `rhsTimesOps`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
	 * Checks for the expression ema_mean_upd = mu*ema_mean + (1-mu)*mean
	 * 
	 * @param rhsTimesOps hop representing BinaryOp of expression (1-mu)*mean
	 * @return true if expression matched
	 */
```

### JavadocReference
Cannot resolve symbol `rhsTimesOps`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
	 * Checks and returns the matched hops for expression ema_mean_upd = mu*ema_mean + (1-mu)*mean  
	 * 
	 * @param rhsTimesOps hop representing BinaryOp of expression (1-mu)*mean 
	 * @param mu value of mu
	 * @return an array [ema_mean_upd, ema_mean] if expression matched, else null
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code BooleanOrExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code MatrixMulExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code DataIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ConstDoubleIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code MultDivExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code BuiltinFunctionExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ModIntDivExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code BooleanOrExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code MultiIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ConstStringIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code UnaryExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ConstIntIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code AtomicExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code BooleanAndExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code AtomicExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code MatrixMulExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code DataIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ConstStringIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code BooleanNotExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code MultiIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ConstDoubleIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code MultDivExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code BuiltinFunctionExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code PowerExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ModIntDivExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ConstTrueExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code RelationalExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code RelationalExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code ConstFalseExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code AddSubExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code BooleanNotExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code AddSubExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code UnaryExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ConstIntIdExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Exit a parse tree produced by the {@code BooleanAndExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code PowerExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ConstFalseExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Symbol `expression` is inaccessible from here
in `src/main/java/org/apache/sysds/parser/dml/DmlListener.java`
#### Snippet
```java
	/**
	 * Enter a parse tree produced by the {@code ConstTrueExpression}
	 * labeled alternative in {@link DmlParser#expression}.
	 * @param ctx the parse tree
	 */
```

### JavadocReference
Cannot resolve symbol `a`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixEquals.java`
#### Snippet
```java
	 * Compare metadata, and return true if metadata is different
	 * 
	 * @param a MatrixBlock a
	 * @param b MatrixBlock b
	 * @return If the metadata was comparable
```

### JavadocReference
Cannot resolve symbol `b`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixEquals.java`
#### Snippet
```java
	 * 
	 * @param a MatrixBlock a
	 * @param b MatrixBlock b
	 * @return If the metadata was comparable
	 */
```

### JavadocReference
Cannot resolve symbol `dim5`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
	 * @param dim3 input2 number of rows
	 * @param dim4 input2 number of columns
	 * @param dim5 output number of rows
	 * @param dim6 output number of columns
	 */
```

### JavadocReference
Cannot resolve symbol `dim6`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
	 * @param dim4 input2 number of columns
	 * @param dim5 output number of rows
	 * @param dim6 output number of columns
	 */
	private static void throwCuDNNDimensionError(long dim1, long dim2, long dim3, long dim4) {
```

### JavadocReference
Cannot resolve symbol `in`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * UAgg colSums for LessThan and GreaterThanEqual operator
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
```

### JavadocReference
Cannot resolve symbol `out`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
	 * @param bOp binary operator
```

### JavadocReference
Cannot resolve symbol `in`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * UAgg colSums for GreaterThan and LessThanEqual operator
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
```

### JavadocReference
Cannot resolve symbol `out`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
	 * @param bOp binary operator
```

### JavadocReference
Cannot resolve symbol `in`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * UAgg colSums for Equal and NotEqual operator
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
```

### JavadocReference
Cannot resolve symbol `out`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	 * 
	 * @param in input matrix block
	 * @param out output matrix block
	 * @param bv ?
	 * @param bOp binary operator
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * @param gCtx            a valid {@link GPUContext}
	 * @param kernelFunction 	name of the kernel function to invoke
	 * @param in							{@link Pointer} to input matrix in device memory (size - rows * columns)
	 * @param out							{@link Pointer} to output matrix in device memory (size - 1 * cols)
	 * @param rows						number of rows in input matrix
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * @param kernelFunction 	name of the kernel function to invoke
	 * @param in							{@link Pointer} to input matrix in device memory (size - rows * columns)
	 * @param out							{@link Pointer} to output matrix in device memory (size - 1 * cols)
	 * @param rows						number of rows in input matrix
	 * @param cols						number of columns in input matrix
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * @param gCtx   a valid {@link GPUContext}
	 * @param kernelFunction 	name of the kernel function to invoke
	 * @param in							{@link Pointer} to matrix in device memory
	 * @param n								size of array
	 * @return	the reduced value
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * @param gCtx            a valid {@link GPUContext}
	 * @param kernelFunction 	name of the kernel function to invoke
	 * @param in							{@link Pointer} to input matrix in device memory (size - rows * columns)
	 * @param out							{@link Pointer} to output matrix in device memory (size - rows * 1)
	 * @param rows						number of rows in input matrix
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * @param kernelFunction 	name of the kernel function to invoke
	 * @param in							{@link Pointer} to input matrix in device memory (size - rows * columns)
	 * @param out							{@link Pointer} to output matrix in device memory (size - rows * 1)
	 * @param rows						number of rows in input matrix
	 * @param cols						number of columns in input matrix
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * Helper method to launch binary scalar-matrix arithmetic/relational operations CUDA kernel.
	 * This method is isolated to be taken advantage of from other operations
	 * as it accepts JCuda {@link Pointer} instances instead of {@link MatrixObject} instances.
	 *
	 * @param gCtx     a valid {@link GPUContext}
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/privacy/PrivacyConstraint.java`
#### Snippet
```java
	 * Get privacy constraints and put them into JSON object. 
	 * @param json JSON object in which the privacy constraints are put
	 * @throws JSONException in case of errors in putting into JSON object
	 */
	public void toJson(JSONObject json) throws JSONException {
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/privacy/propagation/PrivacyPropagator.java`
#### Snippet
```java
	 * @param mtd metadata
	 * @return privacy constraint parsed from metadata object
	 * @throws JSONException during parsing of metadata
	 */
	public static PrivacyConstraint parseAndReturnPrivacyConstraint(JSONObject mtd)
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/privacy/propagation/PrivacyPropagator.java`
#### Snippet
```java
	 * @param mtd metadata object
	 * @return data object with privacy constraint set
	 * @throws JSONException during parsing of metadata
	 */
	public static Data parseAndSetPrivacyConstraint(Data cd, JSONObject mtd)
```

### JavadocReference
Cannot resolve symbol `minimumSampleSize`
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java
	 * @param nCols             The number of columns
	 * @param sparsity          The sparsity of the input
	 * @param minimumSampleSize The minimum sample size
	 * @param maxSampleSize     The maximum sample size
	 * @return The sample size to use.
```

### JavadocReference
Cannot resolve symbol `ret`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	 * @param tm  Map of other side
	 * @param td  Dictionary to take values from (other side dictionary)
	 * @param ret The output dictionary to aggregate into
	 */
	protected void preAggregateDDC_DDCSingleCol(AMapToData tm, double[] td, double[] v) {
```

### JavadocReference
Cannot resolve symbol `ret`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	 * @param tm   Map of other side
	 * @param td   Dictionary to take values from (other side dictionary)
	 * @param ret  The output dictionary to aggregate into
	 * @param nCol The number of columns
	 */
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	 * @param group attribute name in json class
	 * @return list of column ids
	 * @throws JSONException if JSONException occurs
	 */
	public static int[] parseJsonIDList(JSONObject spec, String[] colnames, String group)
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	 * @param parsedSpec parsed specifications
	 * @return K value
	 * @throws JSONException if JSONException occurs
	 */
	public static long getK(JSONObject parsedSpec) throws JSONException {
```

### JavadocReference
Cannot resolve symbol `JSONException`
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	 * @param maxCol end of columns to ignore (1-based, exclusive, if -1 not used)
	 * @return list of column ids
	 * @throws JSONException if JSONException occurs
	 */
	public static int[] parseJsonIDList(JSONObject spec, String[] colnames, String group, int minCol, int maxCol)
```

### JavadocReference
Symbol `deviceNum` is inaccessible from here
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMatrixMemoryManager.java`
#### Snippet
```java
	
	/**
	 * list of allocated {@link GPUObject} instances allocated on {@link GPUContext#deviceNum} GPU
	 * These are matrices allocated on the GPU on which rmvar hasn't been called yet.
	 * If a {@link GPUObject} has more than one lock on it, it cannot be freed
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CudaMemoryAllocator.java`
#### Snippet
```java
	 * 
	 * @param devPtr Device pointer to memory to free
	 * @throws jcuda.CudaException if error occurs
	 */
	@Override
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CudaMemoryAllocator.java`
#### Snippet
```java
	 * @param devPtr Pointer to allocated device memory
	 * @param size size in bytes
	 * @throws jcuda.CudaException if unable to allocate
	 */
	@Override
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/UnifiedMemoryAllocator.java`
#### Snippet
```java
	 * 
	 * @param devPtr Device pointer to memory to free
	 * @throws jcuda.CudaException if error occurs
	 */
	@Override
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/UnifiedMemoryAllocator.java`
#### Snippet
```java
	 * @param devPtr Pointer to allocated device memory
	 * @param size size in bytes
	 * @throws jcuda.CudaException if unable to allocate
	 */
	@Override
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * @param devPtr Pointer to allocated device memory
	 * @param size size in bytes
	 * @throws jcuda.CudaException if unable to allocate
	 */
	public void allocate(Pointer devPtr, long size) throws jcuda.CudaException;
```

### JavadocReference
Cannot resolve symbol `jcuda.CudaException`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryAllocator.java`
#### Snippet
```java
	 * 
	 * @param devPtr Device pointer to memory to free
	 * @throws jcuda.CudaException if error occurs
	 */
	public void free(Pointer devPtr) throws jcuda.CudaException;
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 * The caller is responsible for calling "free" on the returned Pointer object
	 *
	 * @param cusparseHandle a valid {@link cusparseHandle}
	 * @param cublasHandle   a valid {@link cublasHandle}
	 * @param rows           number of rows in this CSR matrix
```

### JavadocReference
Cannot resolve symbol `cublasHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param cusparseHandle a valid {@link cusparseHandle}
	 * @param cublasHandle   a valid {@link cublasHandle}
	 * @param rows           number of rows in this CSR matrix
	 * @param cols           number of columns in this CSR matrix
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 * @param cols           number of columns in this CSR matrix
	 * @param instName          name of the invoking instruction to record{@link Statistics}.
	 * @return A {@link Pointer} to the allocated dense matrix (in column-major format)
	 */
	public Pointer toColumnMajorDenseMatrix(cusparseHandle cusparseHandle, cublasHandle cublasHandle, int rows,
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param A      Sparse Matrix A on GPU
	 * @param transA op - whether A is transposed
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param A      Sparse Matrix A on GPU
	 * @param B      Sparse Matrix B on GPU
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java

	/**
	 * Calls cudaFree lazily on the allocated {@link Pointer} instances
	 *
	 */
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param A      Sparse Matrix A on GPU
	 * @param transA 'T' if A is to be transposed, 'N' otherwise
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java

	/**
	 * Calls cudaFree lazily or eagerly on the allocated {@link Pointer} instances
	 *
	 * @param eager whether to do eager or lazy cudaFrees
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param A      Sparse Matrix A on GPU
	 * @param B      Sparse Matrix B on GPU
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param C      Output matrix
	 * @param rowsC  number of rows in C
```

### JavadocReference
Cannot resolve symbol `cusparseHandle`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 *
	 * @param gCtx   a valid {@link GPUContext}
	 * @param handle a valid {@link cusparseHandle}
	 * @param C      Output sparse matrix on GPU
	 */
```

### JavadocReference
Cannot resolve symbol `Pointer`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUContext.java`
#### Snippet
```java
	 * @param instructionName name of the instruction for which to record per instruction free time, null if you do not
	 *                        want to record
	 * @param toFree          {@link Pointer} instance to be freed
	 * @param eager           true if to be done eagerly
	 */
```

### JavadocReference
Cannot resolve symbol `R`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionFederatedScheme.java`
#### Snippet
```java
	 *
	 * @param m the input matrix object
	 * @param R the permutation matrix for subsampling
	 */
	static void subsampleTo(MatrixObject m, MatrixBlock S) {
```

## RuleId[id=AssignmentUsedAsCondition]
### AssignmentUsedAsCondition
Assignment `flags[i] = !a.isEmpty(i)` used as condition
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				SparseBlock a = in.sparseBlock;
				for ( int i=0; i < m; i++ )
					rlen2 += (flags[i] = !a.isEmpty(i)) ? 1 : 0;
			}
			else { //DENSE
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassForwardFunctionCalls.java`
#### Snippet
```java
		//build phase: second
		HashSet<String> probe = new HashSet<>();
		for( String s : second )
			probe.add(s);
		//probe phase: first
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
	
	public CNodeNary( CNode[] inputs, NaryType type ) {
		for( CNode in : inputs )
			_inputs.add(in);
		_type = type;
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
		//store input and output names
		_inputs = new HashSet<>();
		for( String var : in )
			_inputs.add( var );
		_outputs = new HashSet<>();
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
			_inputs.add( var );
		_outputs = new HashSet<>();
		for( String var : out )
			_outputs.add( var );
		
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
		_dagRuleSet = new ArrayList<>();
		_sbRuleSet = new ArrayList<>();
		for( StatementBlockRewriteRule rewrite : rewrites )
			_sbRuleSet.add( rewrite );
	}
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
		//initialize HOP DAG rewrite ruleSet (with fixed rewrite order)
		_dagRuleSet = new ArrayList<>();
		for( HopRewriteRule rewrite : rewrites )
			_dagRuleSet.add( rewrite );
		_sbRuleSet = new ArrayList<>();
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		if(naS.length > 0  && !(naS.length ==1 && naS[0].isEmpty())){
			this.naStrings = new HashSet<>();
			for(String s: naS)
				this.naStrings.add(s);
		}
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
	
	public static <T> void addAll(Collection<T> a, T[] b) {
		for( T item : b )
			a.add(item);
	}
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
	public static <T> ArrayList<T> asArrayList(T... inputs) {
		ArrayList<T> ret = new ArrayList<>();
		for( T list : inputs )
			ret.add(list);
		return ret;
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
		Set<T> ret = new HashSet<>();
		for( T[] input : inputs )
			for( T element : input )
				ret.add(element);
		return ret;
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
	public static <T> Set<T> asSet(T... inputs) {
		Set<T> ret = new HashSet<>();
		for( T element : inputs )
			ret.add(element);
		return ret;
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
		ArrayList<Double> start_data = new ArrayList<>();

		for (int i = 0; i < l; i++) {
			start_data.add(data[i]);
		}
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
		if(naS.length > 0  && !(naS.length ==1 && naS[0].isEmpty())){
			naStrings = new HashSet<>();
			for(String s: naS)
				naStrings.add(s);
		}
```

## RuleId[id=UnaryPlus]
### UnaryPlus
Unary `+` operator
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ cudaAllocSuccessCount.longValue() + "/"
				+ cudaAllocFailedCount.longValue() + "/" +
				+ cudaAllocReuseCount.longValue() +") / "
				+ cudaDeAllocCount.longValue() + " / "
				+ cudaMemSet0Count.longValue() + ".\n");
```

### UnaryPlus
Unary `+` operator
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ cudaEvictCount.longValue() + "("
				+ cudaFromDevToShadowCount.longValue() + "/" + 
				+ cudaEvictSizeCount.longValue() + ").\n");
		sb.append("GPU conversion time  (sparseConv / sp2dense / dense2sp):\t"
				+ String.format("%.3f", cudaSparseConversionTime.longValue()*1e-9) + " / "
```

### UnaryPlus
Unary `+` operator
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		}
		else {
			raiseValidateError("column indices are length " + + colIndices.size() + " -- should be either 1 or 2");
		}
		
```

## RuleId[id=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/conf/CompilerConfig.java`
#### Snippet
```java
		
		//federated runtime conversion
		FEDERATED_RUNTIME;
	}
	
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/hops/estim/SparsityEstimator.java`
#### Snippet
```java
		MULT, PLUS, EQZERO, NEQZERO,
		CBIND, RBIND, 
		TRANS, DIAG, RESHAPE;
	}
	
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/hops/codegen/SpoofFusedOp.java`
#### Snippet
```java
		COLUMN_RANK_DIMS,  // left wdivmm, row mm
		COLUMN_RANK_DIMS_T,
		VECT_CONST2;
	}
	
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/lops/PMMJ.java`
#### Snippet
```java
	public enum CacheType {
		LEFT,
		LEFT_PART;
	}
	
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java

							switch(key.toString().toUpperCase()) {
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
							switch(key.toString().toUpperCase()) {
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
							}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
								case DataExpression.DELIM_HAS_HEADER_ROW: ;
								case DataExpression.DELIM_FILL: ;
								case DataExpression.DELIM_SPARSE: ;
							}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java

	public enum FrameArrayType {
		STRING, BOOLEAN, BITSET, INT32, INT64, FP32, FP64, CHARACTER, OPTIONAL;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofRowwise.java`
#### Snippet
```java
			}
		}
	};
	
	
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/io/CompressedWriteBlock.java`
#### Snippet
```java

	private enum CONTENT {
		Comp, MB;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorFactory.java`
#### Snippet
```java

	public enum CostType {
		MEMORY, W_TREE, HYBRID_W_TREE, DISTINCT, AUTO;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCoderFactory.java`
#### Snippet
```java
	 */
	public enum PartitionerType {
		BIN_PACKING, STATIC, PRIORITY_QUE, GREEDY, AUTO;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java
	protected static enum ColGroupType {
		UNCOMPRESSED, RLE, OLE, DDC, CONST, EMPTY, SDC, SDCSingle, SDCSingleZeros, SDCZeros, SDCFOR, DDCFOR, DeltaDDC,
		LinearFunctional;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java

	public static enum ColIndexType {
		SINGLE, TWO, ARRAY, RANGE, UNKNOWN;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToFactory.java`
#### Snippet
```java

	public enum MAP_TYPE {
		ZERO, BIT, UBYTE, BYTE, CHAR, CHAR_BYTE, INT;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				final int id = i + rl;
				c[id] = builtin.execute(c[id], 0);
				;
			}
	}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/ADictionary.java`
#### Snippet
```java

	public static enum DictType {
		Delta, Dict, MatrixBlock, UInt8, Identity, IdentitySlice;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java

	public enum SORT_TYPE {
		MERGE, MATERIALIZE;
	}

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/transform/decode/DecoderFactory.java`
#### Snippet
```java
		PassThrough,
		Recode,
	};
	
	public static Decoder createDecoder(String spec, String[] colnames, ValueType[] schema, FrameBlock meta) {
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
			} else {
				res = new MatrixBlock(_len, _slice[1], 0.0);
				res.copy(_slice[0], _slice[1]-1, 0, mb.getNumColumns() - 1, tmp, false);;
			}
			MatrixObject mout = ExecutionContext.createMatrixObject(res);
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/Stat.java`
#### Snippet
```java
	PARWRK_ITER_T,
	PARWRK_TASK_T,
	PARWRK_EXEC_T;
	

```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		if (!workerFederatedRequests.containsKey(request.type)) {
			workerFederatedRequests.put(request.type, request);
		};

		workerFederatedRequests.get(request.type).count++;
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/HEParamServer.java`
#### Snippet
```java
				summands[i] = (CiphertextMatrix) encrypted_models.get(i).getData(matrix_idx);
			}
			result[matrix_idx] = _seal_server.accumulateCiphertexts(summands);;
		});
		if (tAgg != null) {
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
Can be replaced with 'List.contains()'
in `src/main/java/org/apache/sysds/hops/FunctionOp.java`
#### Snippet
```java
	public boolean containsOutput(String varname) {
		return Arrays.stream(getOutputVariableNames())
			.anyMatch(outName -> outName.equals(varname));
	}
	
```

### SimplifyStreamApiCallChains
'Stream.allMatch(x -\> !(...))' can be replaced with 'noneMatch()'
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassEliminateDeadCode.java`
#### Snippet
```java
			&& fgraph.isSideEffectFreeFunction(((FunctionOp)hop).getFunctionKey())
			&& Arrays.stream(((FunctionOp) hop).getOutputVariableNames())
				.allMatch(var -> !varNames.contains(var));
	}
	
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassEliminateDeadCode.java`
#### Snippet
```java
			Set<String> usedVars = new HashSet<>();
			FunctionStatement fstmt = (FunctionStatement) fsb.getStatement(0);
			fstmt.getOutputParams().stream().forEach(d -> usedVars.add(d.getName()));
			
			// backward pass over function to track used and remove unused vars
```

### SimplifyStreamApiCallChains
Can be replaced with 'Arrays.stream()'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorMatrixHistogram.java`
#### Snippet
```java
		public long getNonZeros() {
			return getRows() < getCols() ?
				IntStream.range(0, getRows()).mapToLong(i-> rNnz[i]).sum() :
				IntStream.range(0, getCols()).mapToLong(i-> cNnz[i]).sum();
		}
```

### SimplifyStreamApiCallChains
Can be replaced with 'Arrays.stream()'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorMatrixHistogram.java`
#### Snippet
```java
			return getRows() < getCols() ?
				IntStream.range(0, getRows()).mapToLong(i-> rNnz[i]).sum() :
				IntStream.range(0, getCols()).mapToLong(i-> cNnz[i]).sum();
		}
		
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
						//(even if fname is a known literal, the target function might call other functions)
						pb.getProgram().getFunctionProgramBlocks().keySet().stream()
							.forEach(s -> cand.add(s));
					}
				}
```

### SimplifyStreamApiCallChains
'Stream.allMatch(x -\> !(...))' can be replaced with 'noneMatch()'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
	
	public static boolean isAllDense(MatrixBlock... mb) {
		return Arrays.stream(mb).allMatch(m -> !m.sparse);
	}
	
```

### SimplifyStreamApiCallChains
'Arrays.asList().stream()' can be replaced with 'Stream.of()'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/TernaryFEDInstruction.java`
#### Snippet
```java

		long matrixInputsCount = Arrays.asList(mo1, mo2, mo3)
			.stream().filter(Objects::nonNull).count();

		if(matrixInputsCount == 3)
```

### SimplifyStreamApiCallChains
'!IntStream.anyMatch(...)' can be replaced with 'noneMatch()'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/SpoofSPInstruction.java`
#### Snippet
```java

		//ensure there is at least one RDD input, with awareness for scalars
		if( !IntStream.range(0, ret.length).anyMatch(i -> inputs[i].isMatrix() && !ret[i]) )
			ret[0] = false;

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
				.map(in -> getListObject(in.getName())).collect(Collectors.toList());
			for (ListObject lo : lolist)
				getMatricesFromList(lo).stream().forEach(mo -> mo.release());
		}
	}
```

### SimplifyStreamApiCallChains
'!Stream.anyMatch(...)' can be replaced with 'noneMatch()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			Hop h = _plan.getMappedHop(n.getID());
			if( h instanceof LeftIndexingOp && ResultVar.contains(retVars, h.getInput().get(0).getName() )
				&& !retVars.stream().anyMatch(rvar -> rvar._isAccum) )
				ret &= (h.getParent().size()==1 
					&& h.getParent().get(0).getName().equals(h.getInput().get(0).getName()));
```

## RuleId[id=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/hops/IndexingOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		String s = new String("");
		s += OPSTRING;
		return s;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/hops/UnaryOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		String s = new String("");
		s += "u(" + _op.toString().toLowerCase() + ")";
		return s;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				if(    marginHop instanceof LiteralOp 
						&& "cols".equals(((LiteralOp)marginHop).getStringValue()) )
					margin = new String("cols");
				
				DataCharacteristics dcSelect = null;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/hops/DataOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		String s = new String("");
		s += _op.toString();
		s += " "+getName();
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
	@Override
	public String getOpString() {
		String s = new String("");
		s += OPSTRING;
		return s;
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		return InstructionUtils.concatOperands(
			getExecType().name(), "sample",
			_inputParams.get(DataExpression.RAND_MAX.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_ROWS.toString()).prepScalarInputOperand(et),
			_inputParams.get(DataExpression.RAND_PDF.toString()).prepScalarLabel(),
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			getExecType().name(), "sample",
			_inputParams.get(DataExpression.RAND_MAX.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_ROWS.toString()).prepScalarInputOperand(et),
			_inputParams.get(DataExpression.RAND_PDF.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_SEED.toString()).prepScalarLabel(),
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			_inputParams.get(DataExpression.RAND_MAX.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_ROWS.toString()).prepScalarInputOperand(et),
			_inputParams.get(DataExpression.RAND_PDF.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_SEED.toString()).prepScalarLabel(),
			String.valueOf(getOutputParameters().getBlocksize()),
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			_inputParams.get(DataExpression.RAND_ROWS.toString()).prepScalarInputOperand(et),
			_inputParams.get(DataExpression.RAND_PDF.toString()).prepScalarLabel(),
			_inputParams.get(DataExpression.RAND_SEED.toString()).prepScalarLabel(),
			String.valueOf(getOutputParameters().getBlocksize()),
			prepOutputOperand(output));
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			String.valueOf(getOutputParameters().getNumCols()),
			String.valueOf(getOutputParameters().getBlocksize()),
			_inputParams.get(Statement.SEQ_FROM.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_TO.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_INCR.toString()).prepScalarInputOperand(et),
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			String.valueOf(getOutputParameters().getBlocksize()),
			_inputParams.get(Statement.SEQ_FROM.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_TO.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_INCR.toString()).prepScalarInputOperand(et),
			prepOutputOperand(output));
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
			_inputParams.get(Statement.SEQ_FROM.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_TO.toString()).prepScalarInputOperand(et),
			_inputParams.get(Statement.SEQ_INCR.toString()).prepScalarInputOperand(et),
			prepOutputOperand(output));
	}
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	 */
	private String prepOperand(String label) {
		StringBuilder sb = new StringBuilder("");
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
		boolean isLiteral = (isData && ((Data)this).isLiteral());
		
		StringBuilder sb = new StringBuilder("");
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringOperationCanBeSimplified
Unnecessary empty string argument
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
		boolean isLiteral = (isData && ((Data)this).isLiteral());
		
		StringBuilder sb = new StringBuilder("");
		if ( et == ExecType.CP || et == ExecType.SPARK || et == ExecType.GPU || (isData && isLiteral)) {
			sb.append(label);
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `src/main/java/org/apache/sysds/utils/DataAugmentation.java`
#### Snippet
```java
		return str.substring(0, i) + str.charAt(j)
			+ str.substring(i + 1, j) + str.charAt(i)
			+ str.substring(j + 1, str.length());
	}
}
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCell.java`
#### Snippet
```java
			if( sparse ) { //SPARSE<-value
				while( (value=br.readLine())!=null ) {
					cell = parseCell(value.toString(), st, cell, mmProps);
					appendCell(cell, dest, mmProps);
				}
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCell.java`
#### Snippet
```java
				DenseBlock a = dest.getDenseBlock();
				while( (value=br.readLine())!=null ) {
					cell = parseCell(value.toString(), st, cell, mmProps);
					nnz += appendCell(cell, a, mmProps);
				}
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVM.java`
#### Snippet
```java
		try( BufferedReader br = new BufferedReader(new InputStreamReader(is)) ) {
			while( (value=br.readLine())!=null ) { //for each line
				String rowStr = value.toString().trim();
				lnnz += ReaderTextLIBSVM.parseLibsvmRow(rowStr, vect, 
						(int) clen, _props.getDelim(), _props.getIndexDelim());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
				while( (value=br.readLine())!=null ) //foreach line
				{
					String cellStr = value.toString().trim();
					emptyValuesFound = false;
					String[] parts = IOUtilFunctions.split(cellStr, delim);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
				DenseBlock a = dest.getDenseBlock();
				while( (value=br.readLine())!=null ) { //foreach line
					String cellStr = value.toString().trim();
					emptyValuesFound = false;
					String[] parts = IOUtilFunctions.split(cellStr, delim);
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSV.java`
#### Snippet
```java
						br.readLine(); //ignore header
					if( (value = br.readLine()) != null ) {
						cellStr = value.toString().trim();
						ncol = StringUtils.countMatches(cellStr, delim) + 1;
						nrow = 1;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
	public static String[] readMatrixMarketHeader(String filename) {
		String[] retVal = new String[2];
		retVal[0] = new String("");
		retVal[1] = new String("");
		boolean exists = false;
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		String[] retVal = new String[2];
		retVal[0] = new String("");
		retVal[1] = new String("");
		boolean exists = false;
		
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
		int pos = str.indexOf(delim);
		return new String[]{str.substring(0, pos),
			str.substring(pos+1, str.length())};
	}
	
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
				IOUtilFunctions.getFileSystem(path).open(path))))
			{
				String headerLine = new String("");
				if (in.ready())
					headerLine = in.readLine();
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/util/PorterStemmer.java`
#### Snippet
```java
		while (it.hasNext() && (stemmed == null)) {
			Entry<String,String> pair = it.next();
			stemmed = replacer(word, pair.getKey().toString(), pair.getValue(), mCount);
			it.remove();
		}
```

### StringOperationCanBeSimplified
`toUpperCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		for (int i = 0; i< values.length; i++){
			//skip null/blank entries
			if (values[i] == null || values[i].trim().isEmpty() || values[i].toUpperCase().equals("NULL")
				|| values[i].equals("0")) continue;
			String currentFormat = getDateFormat(values[i]);
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024;
		else 
			return Long.parseLong(arg.substring(0,arg.length()));
	}
	
```

### StringOperationCanBeSimplified
`toUpperCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			for (int j = 0; j < values.length; j++) {
				//skip null/blank entries
				if (values[j] == null || values[j].trim().isEmpty() || values[j].toUpperCase().equals("NULL") ||
					values[j].equals("0")) continue;
				String tmp = values[j];
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		if(i == size()) // if all null return empty boolean.
			return ArrayFactory.allocateBoolean(size());
		else if(firstNN.toLowerCase().equals("true") || firstNN.toLowerCase().equals("false"))
			return changeTypeBooleanStandard();
		else if(firstNN.equals("0") || firstNN.equals("1"))
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		if(i == size()) // if all null return empty boolean.
			return ArrayFactory.allocateBoolean(size());
		else if(firstNN.toLowerCase().equals("true") || firstNN.toLowerCase().equals("false"))
			return changeTypeBooleanStandard();
		else if(firstNN.equals("0") || firstNN.equals("1"))
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		else if(firstNN.equals("0.0") || firstNN.equals("1.0"))
			return changeTypeBooleanFloat();
		else if(firstNN.toLowerCase().equals("t") || firstNN.toLowerCase().equals("f"))
			return changeTypeBooleanCharacter();
		else
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		else if(firstNN.equals("0.0") || firstNN.equals("1.0"))
			return changeTypeBooleanFloat();
		else if(firstNN.toLowerCase().equals("t") || firstNN.toLowerCase().equals("f"))
			return changeTypeBooleanCharacter();
		else
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java
		final double estC = ce.getCost(est);
		final double actC = ce.getCost(act, nRow);
		final String retType = act.getClass().getSimpleName().toString();
		final String cols = est.getColumns().toString();
		final String wanted = est.getBestCompressionType().toString();
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java

			StringBuilder sb = new StringBuilder();
			sb.append(instString.substring(0,iPos+1)); // It takes first part before file name.
			// This will replace 'pattern' with 'replace' string from file name.
			sb.append(ProgramConverter.saveReplaceFilenameThreadID(instString.substring(iPos+1, iPos2+1), pattern, replace));
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
	public static String getStartLineFromSparkDebugInfo(String line) {
		// To remove: (2)  -- Assumption: At max, 9 RDDs as input to transformation/action
		String withoutPrefix = line.substring(4, line.length());
		// To remove: [Disk Memory Deserialized 1x Replicated]
		return  withoutPrefix.split(":")[0]; // Return 'MapPartitionsRDD[51] at mapToPair at ReorgSPInstruction.java'
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

				//parse input ijv triple
				st.reset( strVal.toString() ); //reinit tokenizer
				long row = st.nextLong();
				long col = st.nextLong();
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		if(_prog != null)
			sb.append("\nProgram: " + _prog.toString());
```

### StringOperationCanBeSimplified
Call to `toString()` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		sb.append(" " + _dataType);
		sb.append(" " + _address.toString());
```

## RuleId[id=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/lops/CSVReBlock.java`
#### Snippet
```java
			String.valueOf(DataExpression.DEFAULT_DELIM_FILL_VALUE));
		Lop naStrings = dataInput.getNamedInputLop(DataExpression.DELIM_NA_STRINGS,
			String.valueOf(DataExpression.DEFAULT_NA_STRINGS));
		
		if (headerLop.isVariable())
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		
		StringBuilder sb = new StringBuilder();
		sb.append(String.valueOf(ex1a));
		sb.append(',');
		sb.append(String.valueOf(ex1b));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		sb.append(String.valueOf(ex1a));
		sb.append(',');
		sb.append(String.valueOf(ex1b));
		sb.append(',');
		sb.append(String.valueOf(ex2a));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		sb.append(String.valueOf(ex1b));
		sb.append(',');
		sb.append(String.valueOf(ex2a));
		sb.append(',');
		sb.append(String.valueOf(ex2b));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		sb.append(String.valueOf(ex2a));
		sb.append(',');
		sb.append(String.valueOf(ex2b));
		return sb.toString();
	}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		}
		catch(Exception ex) {
			LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub1)+"'.", ex);
			out = null; //let dependency analysis fail
		}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		}
		catch(Exception ex) {
			LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub1)+"'.", ex);
			out = null; //let dependency analysis fail
		}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		}
		catch(Exception ex) {
			LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub1)+"'.", ex);
			out = null; //let dependency analysis fail
		}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			catch(Exception ex)
			{
				LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub2)+"'.", ex);
				out = null; //let dependency analysis fail
			}
```

### UnnecessaryCallToStringValueOf
Unnecessary `Double.toString()` call
in `src/main/java/org/apache/sysds/runtime/data/SparseRow.java`
#### Snippet
```java
				sb.append(String.format("%"+rowDigits+"d:%d", indexes[i], (long)values[i]));
			else
				sb.append(String.format("%"+rowDigits+"d:%s", indexes[i], Double.toString(values[i])));
			if(i + 1 < s)
				sb.append(", ");
```

### UnnecessaryCallToStringValueOf
Unnecessary `Double.toString()` call
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
						sb.append(String.format("%"+rowDigits+"d:%d", _indexes[j], (long)_values[j]));
					else
						sb.append(String.format("%"+rowDigits+"d:%s", _indexes[j], Double.toString(_values[j])));
					if(j + 1 < pos + len)
						sb.append(", ");
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
			for( int j=0; j<colLength; j++ ) {
				if( row[j]==null )
					sb.append(String.valueOf(row[j]));
				else if( fb.getSchema()[j] == ValueType.FP64 )
					sb.append(dfFormat(df, (Double)row[j]));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
			int pos = ArrayUtils.indexOf(inputs, tmp);
			LineageItem li = new LineageItem(LPLACEHOLDER+pos,
				"Create"+String.valueOf(root.getHopID()));
			operands.put(tmp.getHopID(), li);
			return;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
			int tl = String.valueOf(op.getRight()*1e-3).indexOf(".");
			if (op != null && op.getRight() > 0)
				sb.append(String.valueOf(i) 
					+ String.format("%"+(4-String.valueOf(i).length())+"s", "") // 4-length(i) spaces
					+ op.getLeft() 
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
		DMLScript.LINEAGE_ESTIMATE = true;
		for (int i=0; i<outputs.size(); i++) {
			String opcode = name + "%" + String.valueOf(i+1);
			LineageItem li = new LineageItem(opcode, liInputs);
			String boundVarName = outputs.get(i).getName();
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
		boolean allOutputsCached = true;
		for (int i=0; i<outputs.size(); i++) {
			String opcode = name + "%" + String.valueOf(i+1);
			LineageItem li = new LineageItem(opcode, liInputs);
			if (!_cache.containsKey(li))
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupUtils.java`
#### Snippet
```java
				continue;
			// Wrap the inputs with order-preserving placeholders.
			LineageItem phInput = new LineageItem(ph+String.valueOf(i), new LineageItem[] {liinputs[i]});
			_tmpLineage.set(input, phInput);
			i++;
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				MultiReturnBuiltinCPInstruction mrInst = (MultiReturnBuiltinCPInstruction)inst;
				for (int i=0; i<mrInst.getNumOutputs(); i++) {
					String opcode = instLI.getOpcode() + String.valueOf(i);
					LineageItem li = new LineageItem(opcode, instLI.getInputs());
					Data value = ec.getVariable(mrInst.getOutput(i));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		boolean AllOutputsCacheable = true;
		for (int i=0; i<outputs.size(); i++) {
			String opcode = name + String.valueOf(i+1);
			LineageItem li = new LineageItem(opcode, liInputs);
			String boundVarName = outputs.get(i).getName();
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			MultiReturnBuiltinCPInstruction mrInst = (MultiReturnBuiltinCPInstruction)inst;
			for (int i=0; i<mrInst.getNumOutputs(); i++) {
				String opcode = instLI.getOpcode() + String.valueOf(i);
				liList.add(MutablePair.of(new LineageItem(opcode, instLI.getInputs()), null));
			}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		HashMap<String, LineageItem> funcLIs = new HashMap<>();
		for (int i=0; i<numOutputs; i++) {
			String opcode = name + String.valueOf(i+1);
			LineageItem li = new LineageItem(opcode, liInputs);
			// set _distLeaf2Node for this special lineage item to 1
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java

	public String getInstructionName() {
		return String.valueOf(_etype) + Lop.OPERAND_DELIMITOR + getParam(ParamType.OPSTRING);
	}

```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
					linst[j] = linst[j].replace(
						Lop.OPERAND_DELIMITOR + varOldIn[i].getName() + Lop.DATATYPE_PREFIX,
						Lop.OPERAND_DELIMITOR + String.valueOf(varNewIn[i]) + Lop.DATATYPE_PREFIX);
					// handle parameterized builtin functions
					linst[j] = linst[j].replace("=" + varOldIn[i].getName(), "=" + String.valueOf(varNewIn[i]));
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
						Lop.OPERAND_DELIMITOR + String.valueOf(varNewIn[i]) + Lop.DATATYPE_PREFIX);
					// handle parameterized builtin functions
					linst[j] = linst[j].replace("=" + varOldIn[i].getName(), "=" + String.valueOf(varNewIn[i]));
				}
			}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
				linst[j] = linst[j].replace(
					Lop.OPERAND_DELIMITOR + varOldOut.getName() + Lop.DATATYPE_PREFIX,
					Lop.OPERAND_DELIMITOR + String.valueOf(outputId) + Lop.DATATYPE_PREFIX);
			}

```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
				linst[j] = linst[j].replace(
					Lop.OPERAND_DELIMITOR + varOldOut.getName() + Lop.DATATYPE_PREFIX,
					Lop.OPERAND_DELIMITOR + String.valueOf(id) + Lop.DATATYPE_PREFIX);

				if(varOldIn[i] != null) {
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
					linst[j] = linst[j].replace(
						Lop.OPERAND_DELIMITOR + varOldIn[i].getName() + Lop.DATATYPE_PREFIX,
						Lop.OPERAND_DELIMITOR + String.valueOf(varNewIn[i]) + Lop.DATATYPE_PREFIX);
					linst[j] = linst[j].replace("=" + varOldIn[i].getName(), "=" + String.valueOf(varNewIn[i])); //parameterized
				}
```

### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
						Lop.OPERAND_DELIMITOR + varOldIn[i].getName() + Lop.DATATYPE_PREFIX,
						Lop.OPERAND_DELIMITOR + String.valueOf(varNewIn[i]) + Lop.DATATYPE_PREFIX);
					linst[j] = linst[j].replace("=" + varOldIn[i].getName(), "=" + String.valueOf(varNewIn[i])); //parameterized
				}
			}
```

## RuleId[id=InstantiatingObjectToGetClassObject]
### InstantiatingObjectToGetClassObject
Instantiating object to get Class object
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	@SuppressWarnings("rawtypes")
	public static final Class[] COMPLEX_DATA_TYPES = { JavaRDD.class, RDD.class, Dataset.class, Matrix.class,
			Frame.class, (new double[][] {}).getClass(), MatrixBlock.class, URL.class };

	/**
```

## RuleId[id=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {Integer.MIN_VALUE..-1, 2147483584..Integer.MAX_VALUE}
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToBit.java`
#### Snippet
```java
			if(_size != 64 && _data.get(_size - 1)) {
				// make last run go over into the last elements.
				long mask = ~((-1L) ^ ((-1L) << (_size - 64)));
				l |= mask;
			}
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `docs/api/java/stylesheet.css`
#### Snippet
```java
    background-color:#4D7A97;
    color:#FFFFFF;
    float:left;
    padding:0;
    width:100%;
    clear:right;
    height:2.8em;
    padding-top:10px;
    overflow:hidden;
    font-size:12px; 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/api/DMLOptions.java`
#### Snippet
```java
		options.addOption(configOpt);
		options.addOption(cleanOpt);
		options.addOption(statsOpt);
		options.addOption(fedStatsOpt);
		options.addOption(memOpt);
		options.addOption(explainOpt);
		options.addOption(execOpt);
		options.addOption(gpuOpt);
		options.addOption(debugOpt);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		JavaPairRDD<LongWritable, Text> javaPairRDD = javaRDD.mapToPair(new ConvertStringToLongTextPair());
		MatrixCharacteristics mc = (frameMetadata != null) ? frameMetadata.asMatrixCharacteristics()
				: new MatrixCharacteristics();
		JavaPairRDD<LongWritable, Text> javaPairRDDText = javaPairRDD.mapToPair(new CopyTextInputFunction());

		FrameObject frameObject = new FrameObject(OptimizerUtils.getUniqueTempFileName(),
				new MetaDataFormat(mc, FileFormat.BINARY),
				frameMetadata.getFrameSchema().getSchema().toArray(new ValueType[0]));
		JavaPairRDD<Long, FrameBlock> rdd;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		Object o = value;
		boolean supported = false;
		for (Class<?> clazz : ALL_SUPPORTED_DATA_TYPES) {
			if (o.getClass().equals(clazz)) {
				supported = true;
				break;
			} else if (clazz.isAssignableFrom(o.getClass())) {
				supported = true;
				break;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
			int wix = (chain == ChainType.XtwXv) ? 0 : 1;
			int vix = (chain == ChainType.XtwXv) ? 1 : 0;
			Hop hX = getInput().get(0).getInput().get(0);
			Hop hw = getInput().get(1).getInput().get(wix);
			Hop hv = getInput().get(1).getInput().get(vix).getInput().get(1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		long m1_nrb = (long) Math.ceil((double)m1_rows/m1_blen); // number of row blocks in m1
		long m2_ncb = (long) Math.ceil((double)m2_cols/m2_blen); // number of column blocks in m2

		// TODO: we must factor in the "sparsity"
		double m1_size = m1_rows * m1_cols;
		double m2_size = m2_rows * m2_cols;
		double result_size = m1_rows * m2_cols;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/AggUnaryOp.java`
#### Snippet
```java
					setLineNumbers(transform1);
					setLops(transform1);
				
					if (getDataType() == DataType.SCALAR) {
						UnaryCP unary1 = new UnaryCP(transform1,
							OpOp1.CAST_AS_SCALAR, getDataType(), getValueType());
						unary1.getOutputParameters().setDimensions(0, 0, 0, -1);
						setLineNumbers(unary1);
						setLops(unary1);
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
		int k = OptimizerUtils.getConstrainedNumThreads(_maxNumThreads);
		SortKeys sort = SortKeys.constructSortByValueLop(
				getInput().get(0).constructLops(), 
				getInput().get(1).constructLops(), 
				SortKeys.OperationTypes.WithWeights, 
				getInput().get(0).getDataType(), getInput().get(0).getValueType(), et, k);
		sort.getOutputParameters().setDimensions(
				getInput().get(0).getDim1(),
				getInput().get(0).getDim2(), 
				getInput().get(0).getBlocksize(), 
				getInput().get(0).getNnz());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
		int k = OptimizerUtils.getConstrainedNumThreads(_maxNumThreads);
		SortKeys sort = SortKeys.constructSortByValueLop(
			getInput().get(0).constructLops(), 
			SortKeys.OperationTypes.WithoutWeights, 
			DataType.MATRIX, ValueType.FP64, et, k );
		sort.getOutputParameters().setDimensions(
			getInput().get(0).getDim1(),
			getInput().get(0).getDim2(),
			getInput().get(0).getBlocksize(),
			getInput().get(0).getNnz());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/DataOp.java`
#### Snippet
```java
		if( ret ) {
			for( Entry<String,Integer> e : _paramIndexMap.entrySet() ) {
				String key1 = e.getKey();
				int pos1 = e.getValue();
				int pos2 = that2._paramIndexMap.get(key1);
				ret &= (   that2.getInput().get(pos2)!=null
					    && getInput().get(pos1) == that2.getInput().get(pos2) );
			}
		}
		
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
			_cachedParams.H = (_cachedParams.H < 0) ? parentParam.P : _cachedParams.H;
			_cachedParams.W = (_cachedParams.W < 0) ? parentParam.Q : _cachedParams.W;
			if(LOG.isDebugEnabled()) {
				LOG.debug("Inferring [C,H,W] from maxpool parent: [" + prevC + "," + prevH + "," + prevW + "]-> [" + _cachedParams.C + "," + _cachedParams.H + "," + _cachedParams.W + "]");
			}
			if(THROW_ERROR_IF_INFERRED_SHAPE_MISMATCH) {
				throwExceptionIfNotEqual(prevC, _cachedParams.C, "C");
				throwExceptionIfNotEqual(prevH, _cachedParams.H, "H");
				throwExceptionIfNotEqual(prevW, _cachedParams.W, "W");
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
			if( lval != Double.MAX_VALUE )
			{
				switch( uroot.getOp() )
				{
					case SQRT:  ret = Math.sqrt(lval); break;
					case ROUND: ret = Math.round(lval); break;
					case CEIL:  ret = Math.ceil(lval); break;
					case FLOOR: ret = Math.floor(lval); break;
					case CAST_AS_BOOLEAN: ret = (lval!=0)? 1 : 0; break;
					case CAST_AS_INT: ret = UtilFunctions.toLong(lval); break;
					case CAST_AS_DOUBLE: ret = lval; break;
					default: ret = Double.MAX_VALUE;
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
		if( lret!=Double.MAX_VALUE && rret!=Double.MAX_VALUE ) //if known
		{
			switch( broot.getOp() )
			{
				case PLUS:	ret = lret + rret; break;
				case MINUS:	ret = lret - rret; break;
				case MULT:  ret = lret * rret; break;
				case DIV:   ret = lret / rret; break;
				case MIN:   ret = Math.min(lret, rret); break;
				case MAX:   ret = Math.max(lret, rret); break;
				case POW:   ret = Math.pow(lret, rret); break; 
				//special mod / inddiv for runtime consistency
				case MODULUS: ret = Modulus.getFnObject().execute(lret, rret); break;
				case INTDIV:  ret = IntegerDivide.getFnObject().execute(lret, rret); break; 
				default: ret = Double.MAX_VALUE;
			}
		}
		
		valMemo.put(root.getHopID(), ret);
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/QuaternaryOp.java`
#### Snippet
```java
		double memBudgetExec = SparkExecutionContext.getBroadcastMemoryBudget();
		double memBudgetLocal = OptimizerUtils.getLocalMemBudget();

		Hop X = getInput().get(0);
		Hop U = getInput().get(1);
		Hop V = getInput().get(2);
		Hop W = getInput().get(3);
		
		//MR operator selection, part1
		double m1Size = OptimizerUtils.estimateSize(U.getDim1(), U.getDim2()); //size U
		double m2Size = OptimizerUtils.estimateSize(V.getDim1(), V.getDim2()); //size V
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/QuaternaryOp.java`
#### Snippet
```java
		double memBudgetExec = SparkExecutionContext.getBroadcastMemoryBudget();
		double memBudgetLocal = OptimizerUtils.getLocalMemBudget();

		Hop X = getInput().get(0);
		Hop U = getInput().get(1);
		Hop V = getInput().get(2);
		
		//MR operator selection, part1
		double m1Size = OptimizerUtils.estimateSize(U.getDim1(), U.getDim2()); //size U
		double m2Size = OptimizerUtils.estimateSize(V.getDim1(), V.getDim2()); //size V
		boolean isMapWsig = (m1Size+m2Size < memBudgetExec
				&& 2*m1Size<memBudgetLocal && 2*m2Size<memBudgetLocal); 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
						if(    left instanceof BinaryOp && ((BinaryOp)left).getOp()==OpOp2.MULT
							&& left.getInput().get(0) instanceof BinaryOp && ((BinaryOp)left.getInput().get(0)).getOp()==OpOp2.NOTEQUAL
							&& left.getInput().get(0).getInput().get(1) instanceof LiteralOp && HopRewriteUtils.getDoubleValue((LiteralOp)left.getInput().get(0).getInput().get(1))==0 
							&& left.getInput().get(0).getInput().get(0) == right ) //relies on CSE
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeBinary.java`
#### Snippet
```java
			return this == VECT_DIV_SCALAR || this == VECT_MULT_SCALAR 
				|| this == VECT_MINUS_SCALAR || this == VECT_PLUS_SCALAR
				|| this == VECT_POW_SCALAR 
				|| this == VECT_MIN_SCALAR || this == VECT_MAX_SCALAR
				|| this == VECT_EQUAL_SCALAR || this == VECT_NOTEQUAL_SCALAR
				|| this == VECT_LESS_SCALAR || this == VECT_LESSEQUAL_SCALAR
				|| this == VECT_GREATER_SCALAR || this == VECT_GREATEREQUAL_SCALAR
				|| this == VECT_CBIND
				|| this == VECT_XOR_SCALAR || this == VECT_BITWAND_SCALAR;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeUnary.java`
#### Snippet
```java
			return this == VECT_EXP || this == VECT_POW2
				|| this == VECT_MULT2 || this == VECT_SQRT
				|| this == VECT_LOG || this == VECT_ABS
				|| this == VECT_ROUND || this == VECT_CEIL
				|| this == VECT_FLOOR || this == VECT_SIGN
				|| this == VECT_SIN || this == VECT_COS || this == VECT_TAN
				|| this == VECT_ASIN || this == VECT_ACOS || this == VECT_ATAN
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeBinary.java`
#### Snippet
```java
		boolean is_supported = (api == GeneratorAPI.CUDA || api == GeneratorAPI.JAVA);
		
		// ToDo: support these
		if(api == GeneratorAPI.CUDA)
			is_supported = !_type.isNotSupportedBySpoofCUDA();
		
		int i = 0;
		while(is_supported && i < _inputs.size()) {
			CNode in = _inputs.get(i++);
			is_supported = in.isSupported(api);
		}
		return  is_supported;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeMultiAgg.java`
#### Snippet
```java
		boolean is_supported = (api == GeneratorAPI.JAVA);
		int i = 0;
		while(is_supported && i < _inputs.size()) {
			CNode in = _inputs.get(i++);
			is_supported = in.isSupported(api);
		}
		return  is_supported;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
				long P = DnnUtils.getP(H, R, 1, 0);
				long Q = DnnUtils.getQ(W, S, 1, 0);
				_rows = _inputs.get(0)._rows; //N
				_cols =  C * P * Q;
				_dataType = DataType.MATRIX;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelection.java`
#### Snippet
```java
		if( memo.contains(current.getHopID()) ) {
			HashSet<MemoTableEntry> rmSet = new HashSet<>();
			List<MemoTableEntry> hopP = memo.get(current.getHopID());
			for( MemoTableEntry e1 : hopP )
				for( MemoTableEntry e2 : hopP )
					if( e1 != e2 && e1.subsumes(e2) )
						rmSet.add(e2);
			memo.remove(current, rmSet);
		}
		
		//step 2: select plan for current path
		MemoTableEntry best = null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		HashSet<Long> refHops = new HashSet<>();
		for( Entry<Long, List<MemoTableEntry>> e : memo.getPlans().entrySet() )
			if( !e.getValue().isEmpty() ) {
				Hop hop = memo.getHopRefs().get(e.getKey());
				for( Hop c : hop.getInput() )
					refHops.add(c.getHopID());
			}
		
		//find all full aggregations (the fact that they are in the same partition guarantees 
		//that they also have common subexpressions, also full aggregations are by def root nodes)
		ArrayList<Long> fullAggs = new ArrayList<>();
		for( Long hopID : R ) {
			Hop root = memo.getHopRefs().get(hopID);
			if( !refHops.contains(hopID) && isMultiAggregateRoot(root) )
				fullAggs.add(hopID);
		}
		if( LOG.isTraceEnabled() ) {
			LOG.trace("Found within-partition ua(RC) aggregations: " +
				Arrays.toString(fullAggs.toArray(new Long[0])));
		}
		
		//construct and add multiagg template plans (w/ max 3 aggregations)
		for( int i=0; i<fullAggs.size(); i+=3 ) {
			int ito = Math.min(i+3, fullAggs.size());
			if( ito-i >= 2 ) {
				MemoTableEntry me = new MemoTableEntry(TemplateType.MAGG,
					fullAggs.get(i), fullAggs.get(i+1), ((ito-i)==3)?fullAggs.get(i+2):-1, ito-i);
				if( isValidMultiAggregate(memo, me) ) {
					for( int j=i; j<ito; j++ ) {
						memo.add(memo.getHopRefs().get(fullAggs.get(j)), me);
						if( LOG.isTraceEnabled() )
							LOG.trace("Added multiagg plan: "+fullAggs.get(j)+" "+me);
					}
				}
				else if( LOG.isTraceEnabled() ) {
					LOG.trace("Removed invalid multiagg plan: "+me);
				}
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
HashSet<Long> fullAggs = new HashSet<>();
		Hop.resetVisitStatus(roots);
		for( Hop hop : roots )
			rCollectFullAggregates(hop, fullAggs);
		Hop.resetVisitStatus(roots);

		//remove operators with assigned multi-agg plans
		fullAggs.removeIf(p -> memo.contains(p, TemplateType.MAGG));
	
		//check applicability for further analysis
		if( fullAggs.size() <= 1 )
			return;
	
		if( LOG.isTraceEnabled() ) {
			LOG.trace("Found across-partition ua(RC) aggregations: " +
				Arrays.toString(fullAggs.toArray(new Long[0])));
		}
		
		//collect information for all candidates 
		//(subsumed aggregations, and inputs to fused operators) 
		List<AggregateInfo> aggInfos = new ArrayList<>();
		for( Long hopID : fullAggs ) {
			Hop aggHop = memo.getHopRefs().get(hopID);
			AggregateInfo tmp = new AggregateInfo(aggHop);
			for( int i=0; i<aggHop.getInput().size(); i++ ) {
				Hop c = HopRewriteUtils.isMatrixMultiply(aggHop) && i==0 ? 
					aggHop.getInput().get(0).getInput().get(0) : aggHop.getInput().get(i);
				rExtractAggregateInfo(memo, c, tmp, TemplateType.CELL);
			}
			if( tmp._fusedInputs.isEmpty() ) {
				if( HopRewriteUtils.isMatrixMultiply(aggHop) ) {
					tmp.addFusedInput(aggHop.getInput().get(0).getInput().get(0).getHopID());
					tmp.addFusedInput(aggHop.getInput().get(1).getHopID());
				}
				else	
					tmp.addFusedInput(aggHop.getInput().get(0).getHopID());
			}
			aggInfos.add(tmp);	
		}
		
		if( LOG.isTraceEnabled() ) {
			LOG.trace("Extracted across-partition ua(RC) aggregation info: ");
			for( AggregateInfo info : aggInfos )
				LOG.trace(info);
		}
		
		//sort aggregations by num dependencies to simplify merging
		//clusters of aggregations with parallel dependencies
		aggInfos = aggInfos.stream()
			.sorted(Comparator.comparing(a -> a._inputAggs.size()))
			.collect(Collectors.toList());
		
		//greedy grouping of multi-agg candidates
		boolean converged = false;
		while( !converged ) {
			AggregateInfo merged = null;
			for( int i=0; i<aggInfos.size(); i++ ) {
				AggregateInfo current = aggInfos.get(i);
				for( int j=i+1; j<aggInfos.size(); j++ ) {
					AggregateInfo that = aggInfos.get(j);
					if( current.isMergable(that) ) {
						merged = current.merge(that);
						aggInfos.remove(j); j--;
					}
				}
			}
			converged = (merged == null);
		}
		
		if( LOG.isTraceEnabled() ) {
			LOG.trace("Merged across-partition ua(RC) aggregation info: ");
			for( AggregateInfo info : aggInfos )
				LOG.trace(info);
		}
		
		//construct and add multiagg template plans (w/ max 3 aggregations)
		for( AggregateInfo info : aggInfos ) {
			if( info._aggregates.size()<=1 )
				continue;
			Long[] aggs = info._aggregates.keySet().toArray(new Long[0]);
			MemoTableEntry me = new MemoTableEntry(TemplateType.MAGG,
				aggs[0], aggs[1], (aggs.length>2)?aggs[2]:-1, aggs.length);
			for( int i=0; i<aggs.length; i++ ) {
				memo.add(memo.getHopRefs().get(aggs[i]), me);
				addBestPlan(aggs[i], me);
				if( LOG.isTraceEnabled() )
					LOG.trace("Added multiagg* plan: "+aggs[i]+" "+me);
				
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		boolean ret = true;
		Hop refSize = memo.getHopRefs().get(me.input1).getInput().get(0);
		for( int i=1; ret && i<3; i++ ) {
			if( me.isPlanRef(i) )
				ret &= HopRewriteUtils.isEqualSize(refSize, 
					memo.getHopRefs().get(me.input(i)).getInput().get(0));
		}
		
		//ensure that aggregates are independent of each other, i.e.,
		//they to not have potentially transitive parent child references
		for( int i=0; ret && i<3; i++ ) 
			if( me.isPlanRef(i) ) {
				HashSet<Long> probe = new HashSet<>();
				for( int j=0; j<3; j++ )
					if( i != j )
						probe.add(me.input(j));
				ret &= rCheckMultiAggregate(memo.getHopRefs().get(me.input(i)), probe);
			}
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		if( isMultiAggregateRoot(current) )
			aggInfo.addInputAggregate(current.getHopID());
		
		//recursively process children
		MemoTableEntry me = (type!=null) ? memo.getBest(current.getHopID()) : null;
		for( int i=0; i<current.getInput().size(); i++ ) {
			Hop c = current.getInput().get(i);
			if( me != null && me.isPlanRef(i) )
				rExtractAggregateInfo(memo, c, aggInfo, type);
			else {
				if( type != null && c.getDataType().isMatrix()  ) //add fused input
					aggInfo.addFusedInput(c.getHopID());
				rExtractAggregateInfo(memo, c, aggInfo, null);
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				List<MemoTableEntry> excludeList = memo.get(hopID, TemplateType.ROW); 
				memo.remove(memo.getHopRefs().get(hopID), new HashSet<>(excludeList));
				if( LOG.isTraceEnabled() ) {
					LOG.trace("Removed row memo table entries w/o aggregation: "
						+ Arrays.toString(excludeList.toArray(new MemoTableEntry[0])));
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			if( memo.countEntries(hopID, TemplateType.OUTER) == 2 ) {
				List<MemoTableEntry> entries = memo.get(hopID, TemplateType.OUTER);
				MemoTableEntry me1 = entries.get(0);
				MemoTableEntry me2 = entries.get(1);
				MemoTableEntry rmEntry = TemplateOuterProduct.dropAlternativePlan(memo, me1, me2);
				if( rmEntry != null ) {
					memo.remove(memo.getHopRefs().get(hopID), Collections.singleton(rmEntry));
					memo.getPlansExcludeListed().remove(rmEntry.input(rmEntry.getPlanRefIndex()));
					if( LOG.isTraceEnabled() )
						LOG.trace("Removed dominated outer product memo table entry: " + rmEntry);
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			switch( ((UnaryOp)current).getOp() ) {
				case ABS:
				case ROUND:
				case CEIL:
				case FLOOR:
				case SIGN:    costs = 1; break; 
				case SPROP:
				case SQRT:    costs = 2; break;
				case EXP:     costs = 18; break;
				case SIGMOID: costs = 21; break;
				case LOG:
				case LOG_NZ:  costs = 32; break;
				case NCOL:
				case NROW:
				case PRINT:
				case ASSERT:
				case CAST_AS_BOOLEAN:
				case CAST_AS_DOUBLE:
				case CAST_AS_INT:
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
				case TAN:     costs = 42; break;
				case ASIN:    costs = 93; break;
				case ACOS:    costs = 103; break;
				case ATAN:    costs = 40; break;
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
				case CUMSUM:
				case CUMMIN:
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
					LOG.warn("Cost model not "
						+ "implemented yet for: "+((UnaryOp)current).getOp());
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			switch( ((BinaryOp)current).getOp() ) {
				case MULT: 
				case PLUS:
				case MINUS:
				case MIN:
				case MAX: 
				case AND:
				case OR:
				case EQUAL:
				case NOTEQUAL:
				case LESS:
				case LESSEQUAL:
				case GREATER:
				case GREATEREQUAL:
				case CBIND:
				case RBIND:   costs = 1; break;
				case INTDIV:  costs = 6; break;
				case MODULUS: costs = 8; break;
				case DIV:     costs = 22; break;
				case LOG:
				case LOG_NZ:  costs = 32; break;
				case POW:     costs = (HopRewriteUtils.isLiteralOfValue(
						current.getInput().get(1), 2) ? 1 : 16); break;
				case MINUS_NZ:
				case MINUS1_MULT: costs = 2; break;
				case MOMENT:
					int type = (int) (current.getInput().get(1) instanceof LiteralOp ? 
						HopRewriteUtils.getIntValueSafe((LiteralOp)current.getInput().get(1)) : 2);
					switch( type ) {
						case 0: costs = 1; break; //count
						case 1: costs = 8; break; //mean
						case 2: costs = 16; break; //cm2
						case 3: costs = 31; break; //cm3
						case 4: costs = 51; break; //cm4
						case 5: costs = 16; break; //variance
					}
					break;
				case COV: costs = 23; break;
				default:
					LOG.warn("Cost model not "
						+ "implemented yet for: "+((BinaryOp)current).getOp());
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
					switch( type ) {
						case 0: costs = 2; break; //count
						case 1: costs = 9; break; //mean
						case 2: costs = 17; break; //cm2
						case 3: costs = 32; break; //cm3
						case 4: costs = 52; break; //cm4
						case 5: costs = 17; break; //variance
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			switch(((AggUnaryOp)current).getOp()) {
			case SUM:    costs = 4; break; 
			case SUM_SQ: costs = 5; break;
			case MIN:
			case MAX:    costs = 1; break;
			default:
				LOG.warn("Cost model not "
					+ "implemented yet for: "+((AggUnaryOp)current).getOp());			
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			boolean ret = _aggregates.size()<3 
				&& _aggregates.size()+that._aggregates.size()<=3;
			for( Long hopID : that._aggregates.keySet() )
				ret &= !_inputAggs.contains(hopID);
			for( Long hopID : _aggregates.keySet() )
				ret &= !that._inputAggs.contains(hopID);
			//check partial shared reads
			ret &= CollectionUtils.containsAny(_fusedInputs, that._fusedInputs);
			//check consistent sizes (result correctness)
			Hop in1 = _aggregates.values().iterator().next();
			Hop in2 = that._aggregates.values().iterator().next();
			return ret && HopRewriteUtils.isEqualSize(
				in1.getInput().get(HopRewriteUtils.isMatrixMultiply(in1)?1:0),
				in2.getInput().get(HopRewriteUtils.isMatrixMultiply(in2)?1:0));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
			cdata1 = TemplateUtils.wrapLookupIfNecessary(cdata1, hop.getInput().get(0));
			cdata2 = TemplateUtils.wrapLookupIfNecessary(cdata2, hop.getInput().get(1));
			cdata3 = TemplateUtils.wrapLookupIfNecessary(cdata3, hop.getInput().get(2));
			
			//construct ternary cnode, primitive operation derived from OpOp3
			out = new CNodeTernary(cdata1, cdata2, cdata3, 
				TernaryType.valueOf(top.getOp().name()));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
			CNode cdata1 = tmp.get(((ParameterizedBuiltinOp)hop).getTargetHop().getHopID());
			cdata1 = TemplateUtils.wrapLookupIfNecessary(cdata1, hop.getInput().get(0));
			
			CNode cdata2 = tmp.get(((ParameterizedBuiltinOp)hop).getParameterHop("pattern").getHopID());
			CNode cdata3 = tmp.get(((ParameterizedBuiltinOp)hop).getParameterHop("replacement").getHopID());
			TernaryType ttype = (cdata2.isLiteral() && cdata2.getVarname().equals("Double.NaN")) ? 
				TernaryType.REPLACE_NAN : TernaryType.REPLACE;
			out = new CNodeTernary(cdata1, cdata2, cdata3, ttype);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
			CNodeData from = TemplateUtils.getLiteral(tmp.get(((DataGenOp)hop).getParam(Statement.SEQ_FROM).getHopID()));
			CNodeData to = TemplateUtils.getLiteral(tmp.get(((DataGenOp)hop).getParam(Statement.SEQ_TO).getHopID()));
			CNodeData incr = TemplateUtils.getLiteral(tmp.get(((DataGenOp)hop).getParam(Statement.SEQ_INCR).getHopID()));
			if( Double.parseDouble(from.getVarname()) > Double.parseDouble(to.getVarname())
				&& Double.parseDouble(incr.getVarname()) > 0 ) {
				incr = TemplateUtils.createCNodeData(new LiteralOp("-"+incr.getVarname()), true);
			}
			out = new CNodeBinary(from, incr, BinType.SEQ_RIX);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateOuterProduct.java`
#### Snippet
```java
		HashSet<Hop> inHops = new HashSet<>();
		HashMap<String,Hop> inHops2 = new HashMap<>();
		HashMap<Long, CNode> tmp = new HashMap<>();
		hop.resetVisitStatus();
		rConstructCplan(hop, memo, tmp, inHops, inHops2, compileLiterals);
		hop.resetVisitStatus();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
			|| (HopRewriteUtils.isDnn(hop, OpOpDnn.BIASADD, OpOpDnn.BIASMULT)
				&& hop.getInput().get(0).dimsKnown() && hop.getInput().get(1).dimsKnown()
				&& hop.getInput().get(0).getDim2()>1)
			|| (HopRewriteUtils.isDnn(hop, OpOpDnn.MAX_POOL, OpOpDnn.AVG_POOL, OpOpDnn.CONV2D)
				&& hop.getInput().get(0).dimsKnown() && ((DnnOp)hop).isStride1Pad0()
				&& hop.getInput().get(1).dimsKnown() && hop.getInput().get(1)!=input) //for conv2d
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
			vs[0] = stats.get( binst.input1.getName() );
			vs[1] = stats.get( binst.input2.getName() );
			vs[2] = stats.get( binst.output.getName() );
			
			if( vs[0] == null ) //scalar input,
				vs[0] = _scalarStats;
			if( vs[1] == null ) //scalar input,
				vs[1] = _scalarStats;
			if( vs[2] == null ) //scalar output
				vs[2] = _scalarStats;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		boolean sparse = MatrixBlock.evalSparseFormatOnDisk(dm, dn, (long)(ds*dm*dn));
		double ret = ((double)MatrixBlock.estimateSizeOnDisk(dm, dn, (long)(ds*dm*dn))) / (1024*1024);
		
		if( sparse )
			ret /= DEFAULT_MBS_HDFSREAD_BINARYBLOCK_SPARSE;
		else //dense
			ret /= DEFAULT_MBS_HDFSREAD_BINARYBLOCK_DENSE;
		
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		boolean sparse = MatrixBlock.evalSparseFormatOnDisk(dm, dn, (long)(ds*dm*dn));
		
		double bytes = MatrixBlock.estimateSizeOnDisk(dm, dn, (long)(ds*dm*dn));
		double mbytes = bytes / (1024*1024);
		
		double ret = -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/estim/EstimationUtils.java`
#### Snippet
```java
				if( a.isEmpty(i) ) continue;
				int alen = a.size(i);
				int apos = a.pos(i);
				int[] aix = a.indexes(i);
				double[] avals = a.values(i);
				
				//compute number of aggregated non-zeros for input row
				int nnz1 = (int) Math.min(UtilFunctions.computeNnz(a, aix, apos, alen), n);
				boolean ldense = nnz1 > n / 128;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/estim/EstimationUtils.java`
#### Snippet
```java
				for( int k=apos; k<apos+alen; k++ ) {
					if( a.isEmpty(aix[k]) ) continue;
					int blen = a.size(aix[k]);
					int bpos = a.pos(aix[k]);
					int[] bix = a.indexes(aix[k]);
					double aval = avals[k];
					double[] bvals = a.values(aix[k]);
					if( ldense ) { //dense aggregation
						for( int j=bpos; j<bpos+blen; j++ )
							tmpD[bix[j]] += aval * bvals[j];
					}
					else { //sparse aggregation
						for( int j=bpos; j<bpos+blen; j++ )
							tmpS.add(bix[j], aval * bvals[j]);
					}
				}
				retNnz += !ldense ? tmpS.size() :
					UtilFunctions.computeNnz(tmpD, 0, n);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBasicAvg.java`
#### Snippet
```java
		DataCharacteristics dc1 = !root.getLeft().isLeaf() ?
			estim(root.getLeft()) : root.getLeft().getDataCharacteristics();
		DataCharacteristics dc2 = root.getRight()==null ? null :
			!root.getRight().isLeaf() ? estim(root.getRight()) : 
			root.getRight().getDataCharacteristics();
		return root.setDataCharacteristics(
				estimIntern(dc1, dc2, root.getOp()));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/estim/EstimatorMatrixHistogram.java`
#### Snippet
```java
			final double scaler = IntStream.range(0, h1.getCols())
				.mapToDouble(j -> (double)h1.cNnz[j] * h2.cNnz[j])
				.sum() / h1.getNonZeros() / h2.getNonZeros();
			final double scalec = IntStream.range(0, h1.getRows())
				.mapToDouble(j -> (double)h1.rNnz[j] * h2.rNnz[j])
				.sum() / h1.getNonZeros() / h2.getNonZeros();
			int rMaxNnz = 0, cMaxNnz = 0;
			Random rn = new Random();
			int[] rNnz = new int[h1.getRows()];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/ipa/FunctionCallSizeInfo.java`
#### Snippet
```java
		for( Entry<String, Set<Integer>> e : _fSafeLiterals.entrySet() ) {
			sb.append("--");
			sb.append(e.getKey());
			sb.append(": ");
			for( Integer pos : e.getValue() ) {
				sb.append(pos);
				sb.append(":");
				sb.append(_fgraph.getFunctionCalls(e.getKey())
					.get(0).getInput().get(pos).getName());
				sb.append(" ");
			}
			sb.append("\n");
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassRemoveUnnecessaryCheckpoints.java`
#### Snippet
```java
			Set<String> cands = new HashSet<>(chkpointCand.keySet());
			for( String cand : cands )
				if( sb.variablesRead().containsVariable(cand) 
					&& !sb.variablesUpdated().containsVariable(cand) ) 
				{	
					//note: variableRead might include false positives due to meta 
					//data operations like nrow(X) or operations removed by rewrites 
					//double check hops on basic blocks; otherwise worst-case
					boolean skipRemove = false;
					if( sb.getHops() !=null ) {
						Hop.resetVisitStatus(sb.getHops());
						skipRemove = true;
						for( Hop root : sb.getHops() )
							skipRemove &= !HopRewriteUtils.rContainsRead(root, cand, false);
					}					
					if( !skipRemove )
						chkpointCand.remove(cand);
				}
			
			//prune candidates (updated in conditional control flow)
			Set<String> cands2 = new HashSet<>(chkpointCand.keySet());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/recompile/LiteralReplacement.java`
#### Snippet
```java
			IndexingOp rix = (IndexingOp)c.getInput().get(0);
			Hop data = rix.getInput().get(0);
			Hop rl = rix.getInput().get(1);
			Hop ru = rix.getInput().get(2);
			Hop cl = rix.getInput().get(3);
			Hop cu = rix.getInput().get(4);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
					long ldim1 = mc.getRows(), ldim2 = mc.getCols(), lnnz = mc.getNonZeros();
					//handle row dimension change in body
					if( mcOld.getRows() != mc.getRows() ) {
						ldim1=-1; //unknown
						requiresRecompile = true;
					}
					//handle column dimension change in body
					if( mcOld.getCols() != mc.getCols() ) {
						ldim2=-1; //unknown
						requiresRecompile = true;
					}
					//handle sparsity change
					if( mcOld.getNonZeros() != mc.getNonZeros() ) {
						lnnz=-1; //unknown		
						requiresRecompile = true;
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			if( ifVar!=null && elseVar!=null ){ // both branches exists
				dat1 = ifVar;
				dat2 = elseVar;
			}
			else if( ifVar!=null && elseVar==null ){ //only if
				dat1 = origVar;
				dat2 = ifVar;
			}
			else { //only else
				dat1 = origVar;
				dat2 = elseVar;
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		if( isb == null || isb.getPredicateHops() == null )
			return;
		Hop hops = isb.getPredicateHops();
		ArrayList<Instruction> tmp = recompileHopsDag(
			hops, vars, status, status.isInPlace(), false, status.getTID());
		ipb.setPredicate( tmp );
		if( ParForProgramBlock.RESET_RECOMPILATION_FLAGs && status.isReset() ) {
			Hop.resetRecompilationFlag(hops, ExecType.CP, status.getReset());
			isb.updatePredicateRecompilationFlag();
		}
		status.trackRecompile(isb.requiresPredicateRecompilation());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		switch( op.getValueType() ) {
			case FP64:    return op.getDoubleValue() != 0; 
			case INT64:   return op.getLongValue()   != 0;
			case BOOLEAN: return op.getBooleanValue();
			default: throw new HopsException("Invalid boolean value: "+op.getValueType());
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		Hop rows = input.rowsKnown() ? new LiteralOp(input.getDim1()) : 
			new UnaryOp("tmprows", DataType.SCALAR, ValueType.INT64, OpOp1.NROW, input);
		Hop cols = input.colsKnown() ? new LiteralOp(input.getDim2()) :
			new UnaryOp("tmpcols", DataType.SCALAR, ValueType.INT64, OpOp1.NCOL, input);
		Hop val = new LiteralOp(value);
		
		HashMap<String, Hop> params = new HashMap<>();
		params.put(DataExpression.RAND_ROWS, rows);
		params.put(DataExpression.RAND_COLS, cols);
		params.put(DataExpression.RAND_MIN, val);
		params.put(DataExpression.RAND_MAX, val);
		params.put(DataExpression.RAND_PDF, new LiteralOp(DataExpression.RAND_PDF_UNIFORM));
		params.put(DataExpression.RAND_LAMBDA, new LiteralOp(-1.0));
		params.put(DataExpression.RAND_SPARSITY, new LiteralOp(1.0));
		params.put(DataExpression.RAND_SEED, new LiteralOp(DataGenOp.UNSPECIFIED_SEED) );
		
		//note internal refresh size information
		Hop datagen = new DataGenOp(OpOpDG.RAND, new DataIdentifier("tmp"), params);
		datagen.setBlocksize(input.getBlocksize());
		copyLineNumbers(input, datagen);
		
		if( value==0 )
			datagen.setNnz(0);
			
		return datagen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		Hop val = new LiteralOp(value);
		
		HashMap<String, Hop> params = new HashMap<>();
		params.put(DataExpression.RAND_ROWS, rows);
		params.put(DataExpression.RAND_COLS, cols);
		params.put(DataExpression.RAND_MIN, val);
		params.put(DataExpression.RAND_MAX, val);
		params.put(DataExpression.RAND_PDF, new LiteralOp(DataExpression.RAND_PDF_UNIFORM));
		params.put(DataExpression.RAND_LAMBDA,new LiteralOp(-1.0));
		params.put(DataExpression.RAND_SPARSITY, new LiteralOp(1.0));		
		params.put(DataExpression.RAND_SEED, new LiteralOp(DataGenOp.UNSPECIFIED_SEED) );
		
		//note internal refresh size information
		Hop datagen = new DataGenOp(OpOpDG.RAND, new DataIdentifier("tmp"), params);
		datagen.setBlocksize(colInput.getBlocksize());
		copyLineNumbers(rowInput, datagen);
		
		if( value==0 )
			datagen.setNnz(0);
		
		return datagen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		params.put(DataExpression.RAND_MIN, val);
		params.put(DataExpression.RAND_MAX, val);
		params.put(DataExpression.RAND_PDF, new LiteralOp(DataExpression.RAND_PDF_UNIFORM));
		params.put(DataExpression.RAND_LAMBDA, new LiteralOp(-1.0));
		params.put(DataExpression.RAND_SPARSITY, new LiteralOp(1.0));		
		params.put(DataExpression.RAND_SEED, new LiteralOp(DataGenOp.UNSPECIFIED_SEED) );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
				DataGenOp inputGen = (DataGenOp)left;
				Hop pdf = inputGen.getInput(DataExpression.RAND_PDF);
				Hop min = inputGen.getInput(DataExpression.RAND_MIN);
				Hop max = inputGen.getInput(DataExpression.RAND_MAX);
				double sval = ((LiteralOp)right).getDoubleValue();
				boolean pdfUniform = pdf instanceof LiteralOp 
					&& DataExpression.RAND_PDF_UNIFORM.equals(((LiteralOp)pdf).getStringValue());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
					Hop leftC1 = left.getInput().get(0);
					Hop leftC2 = left.getInput().get(1);
					
					if( leftC1.getDataType()==DataType.MATRIX && leftC2.getDataType()==DataType.MATRIX &&
						(right == leftC1 || right == leftC2) && leftC1 !=leftC2 ){ //any mult order
						X = right;
						Y = ( right == leftC1 ) ? leftC2 : leftC1;
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
						LiteralOp literal = new LiteralOp(1);
						BinaryOp plus = HopRewriteUtils.createBinary(Y, literal, bop.getOp());
						BinaryOp mult = HopRewriteUtils.createBinary(plus, X, OpOp2.MULT);
						HopRewriteUtils.replaceChildReference(parent, hi, mult, pos);
						HopRewriteUtils.cleanupUnreferenced(hi, left);
						hi = mult;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
					IndexingOp ix = (IndexingOp)right.getInput().get(0);
					if( ix.isRowLowerEqualsUpper() && ix.getInput().get(1) instanceof DataOp
						&& ix.getInput().get(1).getName().equals(itervar) ){
						leftScalar = true;
						rowIx = true;
					}
					else if( ix.isColLowerEqualsUpper() && ix.getInput().get(3) instanceof DataOp
						&& ix.getInput().get(3).getName().equals(itervar) ){
						leftScalar = true;
						rowIx = false;
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
			IndexingOp rix = (IndexingOp) lix.getInput().get(1);
			int index1 = rowIx ? 2 : 4;
			int index2 = rowIx ? 3 : 5;
			//modify left indexing bounds
			HopRewriteUtils.replaceChildReference(lix, lix.getInput().get(index1), from, index1);
			HopRewriteUtils.replaceChildReference(lix, lix.getInput().get(index2), to, index2);
			//modify right indexing
			HopRewriteUtils.replaceChildReference(rix, rix.getInput().get(index1-1), from, index1-1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
			int index1 = rowIx ? 2 : 4;
			int index2 = rowIx ? 3 : 5;
			//modify left indexing bounds
			HopRewriteUtils.replaceChildReference(lix, lix.getInput().get(index1),from, index1);
			HopRewriteUtils.replaceChildReference(lix, lix.getInput().get(index2),to, index2);
			//modify both right indexing
			HopRewriteUtils.replaceChildReference(rix0, rix0.getInput().get(index1-1), from, index1-1);
			HopRewriteUtils.replaceChildReference(rix0, rix0.getInput().get(index2-1), to, index2-1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		double expectedOneMinusMu = OptimizerUtils.rEvalSimpleDoubleExpression(getFirstInput(rhsTimesOp), new HashMap<>());
		Hop plusOp = rhsTimesOp.getParent().get(0); 
		Hop lhsTimesOp = null;
		if(plusOp.getInput().get(0) == rhsTimesOp) {
			lhsTimesOp = plusOp.getInput().get(1); 
		}
		else {
			lhsTimesOp = plusOp.getInput().get(0);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
					if(getFirstInput(var) instanceof LiteralOp) {
						eps = OptimizerUtils.rEvalSimpleDoubleExpression(getFirstInput(var), new HashMap<>());
						var = getSecondInput(var);
					}
					else {
						eps = OptimizerUtils.rEvalSimpleDoubleExpression(getSecondInput(var), new HashMap<>());
						var = getFirstInput(var);
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteIndexingVectorization.java`
#### Snippet
```java
					HopRewriteUtils.setOutputParameters(newRix, -1, -1, input.getBlocksize(), -1);
					newRix.refreshSizeInformation();
					//reset visit status of copied hops (otherwise hidden by left indexing)
					for( Hop c : newRix.getInput() )
						c.resetVisitStatus();
					
					//rewrite bottom left indexing operator
					HopRewriteUtils.removeChildReference(current, input); //input data
					HopRewriteUtils.addChildReference(current, newRix, 0);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteIndexingVectorization.java`
#### Snippet
```java
					ArrayList<Hop> ihop0parents = (ArrayList<Hop>) ihop0.getParent().clone();
					ArrayList<Integer> ihop0parentsPos = new ArrayList<>();
					for( Hop parent : ihop0parents ) {
						int posp = HopRewriteUtils.getChildReferencePos(parent, ihop0);
						HopRewriteUtils.removeChildReferenceByPos(parent, ihop0, posp); //input data
						ihop0parentsPos.add(posp);
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteIndexingVectorization.java`
#### Snippet
```java
					HopRewriteUtils.setOutputParameters(newLix, -1, -1, input.getBlocksize(), -1);
					newLix.refreshSizeInformation();
					//reset visit status of copied hops (otherwise hidden by left indexing)
					for( Hop c : newLix.getInput() )
						c.resetVisitStatus();
					
					for( int i=0; i<ihop0parentsPos.size(); i++ ) {
						Hop parent = ihop0parents.get(i);
						int posp = ihop0parentsPos.get(i);
						HopRewriteUtils.addChildReference(parent, newLix, posp);
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
				StatementBlock sb1 = new StatementBlock();
				sb1.setDMLProg(sb.getDMLProg());
				sb1.setParseInfo(sb);
				sb1.setLiveIn(new VariableSet());
				sb1.setLiveOut(new VariableSet());
				
				//move data-dependent ops incl transient writes to new statement block
				//(and replace original hop with transient read)
				ArrayList<Hop> sb1hops = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/CSVReBlock.java`
#### Snippet
```java
		sb.append( OPERAND_DELIMITOR );
		if(naStrings instanceof Nary){
			Nary naLops = (Nary) naStrings;
			for(Lop na : naLops.getInputs()){
				sb.append(((Data)na).getStringValue());
				sb.append(DataExpression.DELIM_NA_STRING_SEP);
			}
		} else if (naStrings instanceof Data){
			sb.append(((Data)naStrings).getStringValue());
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/CentralMoment.java`
#### Snippet
```java
		if( getExecType() == ExecType.CP || getExecType() == ExecType.FED ) {
			sb.append(OPERAND_DELIMITOR);
			sb.append(_numThreads);
			if ( getExecType() == ExecType.FED ){
				sb.append(OPERAND_DELIMITOR);
				sb.append(_fedOutput);
			}
		}
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/Compression.java`
#### Snippet
```java
		StringBuilder sb = InstructionUtils.getStringBuilder();
		sb.append(getExecType());
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(OPCODE);
		sb.append(OPERAND_DELIMITOR);
		sb.append(getInputs().get(0).prepInputOperand(input1));
		sb.append(OPERAND_DELIMITOR);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/Data.java`
#### Snippet
```java
				Data headerLop = (Data) getNamedInputLop(DataExpression.DELIM_HAS_HEADER_ROW);
				Data delimLop = (Data) getNamedInputLop(DataExpression.DELIM_DELIMITER);
				Data sparseLop = (Data) getNamedInputLop(DataExpression.DELIM_SPARSE);

				if (headerLop.isVariable())
					throw new LopsException(this.printErrorLocation()
							+ "Parameter " + DataExpression.DELIM_HAS_HEADER_ROW
							+ " must be a literal for a seq operation.");
				if (delimLop.isVariable())
					throw new LopsException(this.printErrorLocation()
							+ "Parameter " + DataExpression.DELIM_DELIMITER
							+ " must be a literal for a seq operation.");
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/Data.java`
#### Snippet
```java
				Data delimLop = (Data) getNamedInputLop(DataExpression.DELIM_DELIMITER);
				Data indexDelimLop = (Data) getNamedInputLop(DataExpression.LIBSVM_INDEX_DELIM);
				Data sparseLop = (Data) getNamedInputLop(DataExpression.DELIM_SPARSE);

				if(delimLop.isVariable())
					throw new LopsException(
						this.printErrorLocation() + "Parameter " + DataExpression.DELIM_DELIMITER + " must be a literal for a seq operation.");

				if(indexDelimLop.isVariable())
					throw new LopsException(
						this.printErrorLocation() + "Parameter " + DataExpression.LIBSVM_INDEX_DELIM + " must be a literal for a seq operation.");
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		if (iLop != null) {
			sb.append(iLop.prepScalarInputOperand(getExecType()));
			sb.append(OPERAND_DELIMITOR);
		}
		else {
			iLop = _inputParams.get(DataExpression.RAND_ROWS);
			sb.append(iLop.prepScalarInputOperand(getExecType()));
			sb.append(OPERAND_DELIMITOR);
			
			iLop = _inputParams.get(DataExpression.RAND_COLS);
			sb.append(iLop.prepScalarInputOperand(getExecType()));
			sb.append(OPERAND_DELIMITOR);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		if ( iLop != null ) {
			if(iLop instanceof Nary) {
				for(Lop lop : iLop.getInputs()) {
					sb.append(((Data)lop).getStringValue());
					sb.append(DataExpression.DELIM_NA_STRING_SEP);
				}
			}
			else if(iLop instanceof Data) {
				sb.append(((Data)iLop).getStringValue());
			}
		}

		sb.append(OPERAND_DELIMITOR);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/DnnTransform.java`
#### Snippet
```java
		sb.append( OPERAND_DELIMITOR );
		sb.append( this.prepOutputOperand(output));
		
		//append degree of parallelism
		if( getExecType()==ExecType.CP ) {
			sb.append( OPERAND_DELIMITOR );
			sb.append( numThreads );
		}
		
		sb.append( OPERAND_DELIMITOR );
		sb.append( intermediateMemBudget );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/RightIndex.java`
#### Snippet
```java
		addInput(inputMatrix);
		addInput(rowL);
		addInput(rowU);
		addInput(colL);
		addInput(colU);
		
		inputMatrix.addOutput(this);
		rowL.addOutput(this);
		rowU.addOutput(this);
		colL.addOutput(this);
		colU.addOutput(this);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/LeftIndex.java`
#### Snippet
```java
		StringBuilder sb = InstructionUtils.getStringBuilder();
		sb.append( getExecType() );
		sb.append( OPERAND_DELIMITOR );
		
		sb.append( getOpcode() );
		sb.append( OPERAND_DELIMITOR );
		
		sb.append( getInputs().get(0).prepInputOperand(lhsInput));
		sb.append( OPERAND_DELIMITOR );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
		sb.append(Lop.DATATYPE_PREFIX);
		sb.append(getDataType());
		sb.append(Lop.VALUETYPE_PREFIX);
		sb.append(getValueType());
		sb.append(Lop.LITERAL_PREFIX);
		sb.append(isLiteral);
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/MMTSJ.java`
#### Snippet
```java
		if( getExecType()==ExecType.CP || getExecType()==ExecType.FED ) {
			sb.append( OPERAND_DELIMITOR );
			sb.append( _numThreads );
			if ( getExecType()==ExecType.FED ){
				sb.append( OPERAND_DELIMITOR );
				sb.append( _fedOutput.name() );
			}
		}
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/MapMultChain.java`
#### Snippet
```java
		StringBuilder sb = InstructionUtils.getStringBuilder();
		
		sb.append(getExecType());
		sb.append(Lop.OPERAND_DELIMITOR);
		
		if( getExecType()==ExecType.CP )
			sb.append(OPCODE_CP);
		else
			sb.append(OPCODE);
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(0).prepInputOperand(input1));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(1).prepInputOperand(input2));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/MapMultChain.java`
#### Snippet
```java
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(prepOutputOperand(output));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(_chainType);
		
		//append degree of parallelism for matrix multiplications
		if( getExecType()==ExecType.CP ) {
			sb.append( OPERAND_DELIMITOR );
			sb.append( _numThreads );
		}
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/WeightedSigmoid.java`
#### Snippet
```java
		sb.append( prepOutputOperand(output));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(_wsigmoidType);
		
		//append degree of parallelism
		if( getExecType()==ExecType.CP ) {
			sb.append( OPERAND_DELIMITOR );
			sb.append( _numThreads );
		}
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/PMMJ.java`
#### Snippet
```java
		sb.append( getInputs().get(0).prepInputOperand(input_index1));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(1).prepInputOperand(input_index2));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(2).prepInputOperand(input_index3));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( prepOutputOperand(output_index));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/WeightedDivMM.java`
#### Snippet
```java
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(0).prepInputOperand(input1));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(1).prepInputOperand(input2));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(2).prepInputOperand(input3));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(3).prepInputOperand(input4));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( prepOutputOperand(output));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(_weightsType);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/WeightedUnaryMM.java`
#### Snippet
```java
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(0).prepInputOperand(input1));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(1).prepInputOperand(input2));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( getInputs().get(2).prepInputOperand(input3));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append( prepOutputOperand(output));
		
		sb.append(Lop.OPERAND_DELIMITOR);
		sb.append(_wummType);
		
		//append degree of parallelism
		if( getExecType()==ExecType.CP ) {
			sb.append( OPERAND_DELIMITOR );
			sb.append( _numThreads );
		}
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddChkpointInLoop.java`
#### Snippet
```java
				List<Lop> oldOuts = new ArrayList<>(l.getOutputs());
				// Construct a chkpoint lop that takes this Spark node as an input
				Lop checkpoint = new Checkpoint(l, l.getDataType(), l.getValueType(),
					Checkpoint.getDefaultStorageLevelString(), false);
				for (Lop out : oldOuts) {
					//Rewire l -> out to l -> checkpoint -> out
					checkpoint.addOutput(out);
					out.replaceInput(l, checkpoint);
					l.removeOutput(out);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
		if( !conditional ) {
			if( _left instanceof DataIdentifier && constVars.containsKey(((DataIdentifier) _left).getName()) )
				_left = constVars.get(((DataIdentifier) _left).getName());
			if( _right instanceof DataIdentifier && constVars.containsKey(((DataIdentifier) _right).getName()) )
				_right = constVars.get(((DataIdentifier) _right).getName());
		}
		
		String outputName = getTempName();
		DataIdentifier output = new DataIdentifier(outputName);
		output.setParseInfo(this);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
		String leftString;
		String rightString;
		if (_left instanceof StringIdentifier) {
			leftString = "\"" + _left.toString() + "\"";
		} else {
			leftString = _left.toString();
		}
		if (_right instanceof StringIdentifier) {
			rightString = "\"" + _right.toString() + "\"";
		} else {
			rightString = _right.toString();
		}
		return "(" + leftString + " " + _opcode.toString() + " "
				+ rightString + ")";
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
		_outputs = new Identifier[stmt.getTargetList().size()];
		int count = 0;
		for (DataIdentifier outParam: stmt.getTargetList()){
			DataIdentifier tmp = new DataIdentifier(outParam);
			tmp.setParseInfo(this);
			_outputs[count++] = tmp;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			DataIdentifier dx = (DataIdentifier) getOutputs()[0];
			DataIdentifier dw = (DataIdentifier) getOutputs()[1];
			DataIdentifier db = (DataIdentifier) getOutputs()[2];
			DataIdentifier dout0 = (DataIdentifier) getOutputs()[3];
			DataIdentifier dc0 = (DataIdentifier) getOutputs()[4];
			
			setDimensions(dx, getFirstExpr());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			else if( (!allowsMV && expr1.getOutput().getDim1() != expr2.getOutput().getDim1())
				  || (allowsMV && expr1.getOutput().getDim1() != expr2.getOutput().getDim1() && expr2.getOutput().getDim1() != 1)
				  || (!allowsMV && expr1.getOutput().getDim2() != expr2.getOutput().getDim2()) 
				  || (allowsMV && expr1.getOutput().getDim2() != expr2.getOutput().getDim2() && expr2.getOutput().getDim2() != 1) ) 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		if (paramName.equals(RAND_ROWS) && paramValue instanceof DoubleIdentifier) {
			paramValue = new IntIdentifier((long) ((DoubleIdentifier) paramValue).getValue(), this);
		} else if (paramName.equals(RAND_COLS) && paramValue instanceof DoubleIdentifier) {
			paramValue = new IntIdentifier((long) ((DoubleIdentifier) paramValue).getValue(), this);
		}
		// add the parameter to expression list
		paramValue.setParseInfo(this);
		addVarParam(paramName,paramValue);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		if (DMLScript.VALIDATOR_IGNORE_ISSUES && (value == null)) {
			return;
		}
		_varParams.put(name, value);
		
		// if required, initialize values
		setFilename(value.getFilename());
		if (getBeginLine() == 0) setBeginLine(value.getBeginLine());
		if (getBeginColumn() == 0) setBeginColumn(value.getBeginColumn());
		if (getEndLine() == 0) setEndLine(value.getEndLine());
		if (getEndColumn() == 0) setEndColumn(value.getEndColumn());
		if (getText() == null) setText(value.getText());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		if (conditional) {// warning if conditional
			String fullMsg = this.printWarningLocation() + msg;
			LOG.warn(fullMsg);
		} else {// error and exception if unconditional
			String fullMsg = this.printErrorLocation() + msg;
			if (errorCode != null)
				throw new LanguageException(fullMsg, errorCode);
			else
				throw new LanguageException(fullMsg);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		setBeginLine(ctx.start.getLine());
		setBeginColumn(ctx.start.getCharPositionInLine());
		setEndLine(ctx.stop.getLine());
		setEndColumn(ctx.stop.getCharPositionInLine());
		// preserve whitespace if possible
		if ((ctx.start != null) && (ctx.stop != null) && (ctx.start.getStartIndex() != -1)
				&& (ctx.stop.getStopIndex() != -1) && (ctx.start.getStartIndex() <= ctx.stop.getStopIndex())
				&& (ctx.start.getInputStream() != null)) {
			String text = ctx.start.getInputStream()
					.getText(Interval.of(ctx.start.getStartIndex(), ctx.stop.getStopIndex()));
			if (text != null) {
				text = text.trim();
			}
			setText(text);
		} else {
			String text = ctx.getText();
			if (text != null) {
				text = text.trim();
			}
			setText(text);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		String file = _filename;
		if (file == null) {
			file = "";
		} else {
			file = file + " ";
		}
		if (getText() != null) {
			return "ERROR: " + file + "[line " + _beginLine + ":" + _beginColumn + "] -> " + getText() + " -- ";
		} else {
			return "ERROR: " + file + "[line " + _beginLine + ":" + _beginColumn + "] -- ";
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		String file = _filename;
		if (file == null) {
			file = "";
		} else {
			file = file + " ";
		}
		if (getText() != null) {
			return "WARNING: " + file + "[line " + _beginLine + ":" + _beginColumn + "] -> " + getText() + " -- ";
		} else {
			return "WARNING: " + file + "[line " + _beginLine + ":" + _beginColumn + "] -- ";
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		VariableSet origVarsBeforeBody = new VariableSet();
		for (String key : ids.getVariableNames()){
			DataIdentifier origId = ids.getVariable(key);
			DataIdentifier copyId = new DataIdentifier(origId);
			origVarsBeforeBody.addVariable(key, copyId);
		}
		
		//////////////////////////////////////////////////////////////////////////////
		// FIRST PASS: process the predicate / statement blocks in the body of the for statement
		///////////////////////////////////////////////////////////////////////////////
		
		//remove updated vars from constants
		for( String var : _updated.getVariableNames() )
			if( constVars.containsKey( var ) )
				constVars.remove( var );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		_dmlProg = dmlProg;
		for(StatementBlock sb : body) {
			ids = sb.validate(dmlProg, ids, constVars, true);
			constVars = sb.getConstOut();
		}
		
		if (!body.isEmpty()){
			_constVarsIn.putAll(body.get(0).getConstIn());
			_constVarsOut.putAll(body.get(body.size()-1).getConstOut());
		}
		
		// for each updated variable 
		boolean revalidationRequired = false;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
				long startVersionDim1 = (startVersion instanceof IndexedIdentifier)   ? ((IndexedIdentifier)startVersion).getOrigDim1() : startVersion.getDim1(); 
				long endVersionDim1 = (endVersion instanceof IndexedIdentifier) ? ((IndexedIdentifier)endVersion).getOrigDim1() : endVersion.getDim1(); 
				long startVersionDim2 = (startVersion instanceof IndexedIdentifier)   ? ((IndexedIdentifier)startVersion).getOrigDim2() : startVersion.getDim2(); 
				long endVersionDim2 = (endVersion instanceof IndexedIdentifier) ? ((IndexedIdentifier)endVersion).getOrigDim2() : endVersion.getDim2(); 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
			body = fs.getBody();
			
			//validate body
			_dmlProg = dmlProg;
			for(StatementBlock sb : body) {
				ids = sb.validate(dmlProg, ids, constVars, true);
				constVars = sb.getConstOut();
			}
			if (!body.isEmpty()){
				_constVarsIn.putAll(body.get(0).getConstIn());
				_constVarsOut.putAll(body.get(body.size()-1).getConstOut());
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		for( StatementBlock sb : fstmt.getBody())
		{
			current = sb.initializeforwardLV(current);	
			
			// for each generated variable in this block, check variable not killed
			// in prior statement block in while stmt blody
			for (String varName : sb._gen.getVariableNames()){
				
				// IF the variable is NOT set in the while loop PRIOR to this stmt block, 
				// THEN needs to be generated
				if (!_kill.getVariableNames().contains(varName)){
					_gen.addVariable(varName, sb._gen.getVariable(varName));	
				}
			}
			
			_read.addVariables(sb._read);
			_updated.addVariables(sb._updated);
		
			// only add kill variables for statement blocks guaranteed to execute
			if (!(sb instanceof WhileStatementBlock) && !(sb instanceof ForStatementBlock) ){
				_kill.addVariables(sb._kill);
			}	
		}
		
		// set preliminary "warn" set -- variables that if used later may cause runtime error
		// if the loop is not executed
		// warnSet = (updated MINUS (updatedIfBody INTERSECT updatedElseBody)) MINUS current
		for (String varName : _updated.getVariableNames()){
			if (!activeInPassed.containsVariable(varName)) {
				_warnSet.addVariable(varName, _updated.getVariable(varName));
			}
		}
		
		// activeOut includes variables from passed live in and updated in the while body
		_liveOut = new VariableSet();
		_liveOut.addVariables(current);
		_liveOut.addVariables(_updated);
		return _liveOut;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
			ifCurrent = sb.initializeforwardLV(ifCurrent);
				
			// for each generated variable in this block, check variable not killed
			// (assigned value) in prior statement block in ifstmt blody
			for (String varName : sb._gen.getVariableNames()){
				
				// IF the variable is NOT set in the while loop PRIOR to this stmt block, 
				// THEN needs to be generated
				if (!killIfBody.getVariableNames().contains(varName)){
					genIfBody.addVariable(varName, sb._gen.getVariable(varName));	
				}
			}
				
			readIfBody.addVariables(sb._read);
			updatedIfBody.addVariables(sb._updated);
			
			// only add kill variables for statement blocks guaranteed to execute
			if (!(sb instanceof WhileStatementBlock) && !(sb instanceof ForStatementBlock) ){
				killIfBody.addVariables(sb._kill);
			}	
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/WhileStatementBlock.java`
#### Snippet
```java
		for (String varName : _updated.getVariableNames()){
			if (!activeInPassed.containsVariable(varName)) {
				_warnSet.addVariable(varName, _updated.getVariable(varName));
			}
		}
		
		// activeOut includes variables from passed live in and updated in the while body
		_liveOut = new VariableSet();
		_liveOut.addVariables(current);
		_liveOut.addVariables(_updated);
		return _liveOut;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		VariableSet lo = new VariableSet();
		lo.addVariables(loPassed);
		
		// calls analyze for each statement block in while stmt body
		int numBlocks = fstmt.getBody().size();
		for (int i = numBlocks - 1; i >= 0; i--){
			lo = fstmt.getBody().get(i).analyze(lo);
		}	
		
		VariableSet loReturn = new VariableSet();
		loReturn.addVariables(lo);
		return loReturn;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		VariableSet candidateLO = new VariableSet();
		candidateLO.addVariables(loPassed);
		candidateLO.addVariables(_gen);
		candidateLO.addVariables(predVars);
		
		VariableSet origLiveOut = new VariableSet();
		origLiveOut.addVariables(_liveOut);
		origLiveOut.addVariables(predVars);
		origLiveOut.addVariables(_gen);
		
		_liveOut = new VariableSet();
	 	for (String name : candidateLO.getVariableNames()){
	 		if (origLiveOut.containsVariable(name)){
	 			_liveOut.addVariable(name, candidateLO.getVariable(name));
	 		}
	 	}
	 	
		initializebackwardLV(_liveOut);
		
		// set final warnSet: remove variables NOT in live out
		VariableSet finalWarnSet = new VariableSet();
		for (String varName : _warnSet.getVariableNames()){
			if (_liveOut.containsVariable(varName)){
				finalWarnSet.addVariable(varName,_warnSet.getVariable(varName));
			}
		}
		_warnSet = finalWarnSet;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
	 	_liveOut = new VariableSet();
	 	for (String name : candidateLO.getVariableNames()){
	 		if (origLiveOut.containsVariable(name)){
	 			_liveOut.addVariable(name, candidateLO.getVariable(name));
	 		}
	 	}
	
		initializebackwardLV(_liveOut);
		
		// set final warnSet: remove variables NOT in live out
		VariableSet finalWarnSet = new VariableSet();
		for (String varName : _warnSet.getVariableNames()){
			if (_liveOut.containsVariable(varName)){
				finalWarnSet.addVariable(varName,_warnSet.getVariable(varName));
			}
		}
		_warnSet = finalWarnSet;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/FunctionStatementBlock.java`
#### Snippet
```java
		VariableSet current = new VariableSet();
		current.addVariables(activeInPassed);
		
		for( StatementBlock sb : fstmt.getBody() )
		{
			current = sb.initializeforwardLV(current);	
			
			// for each generated variable in this block, check variable not killed
			// in prior statement block in while stmt blody
			for (String varName : sb._gen.getVariableNames()){
				
				// IF the variable is NOT set in the while loop PRIOR to this stmt block, 
				// THEN needs to be generated
				if (!_kill.getVariableNames().contains(varName)){
					_gen.addVariable(varName, sb._gen.getVariable(varName));
				}
			}
			
			_read.addVariables(sb._read);
			_updated.addVariables(sb._updated);
		
			// only add kill variables for statement blocks guaranteed to execute
			if (!(sb instanceof WhileStatementBlock) && !(sb instanceof ForStatementBlock) ){
				_kill.addVariables(sb._kill);
			}	
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		candidateLO.addVariables(loPassed);
		//candidateLO.addVariables(_gen);

		VariableSet origLiveOut = new VariableSet();
		origLiveOut.addVariables(_liveOut);

		_liveOut = new VariableSet();
	 	for (String name : candidateLO.getVariableNames()){
	 		if (origLiveOut.containsVariable(name)){
	 			_liveOut.addVariable(name, candidateLO.getVariable(name));
	 		}
	 	}

		initializebackwardLV(_liveOut);

		_liveIn = new VariableSet();
		_liveIn.addVariables(_liveOut);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/FunctionStatementBlock.java`
#### Snippet
```java
		VariableSet origLiveOut = new VariableSet();
		origLiveOut.addVariables(_liveOut);
		
		_liveOut = new VariableSet();
	 	for (String name : candidateLO.getVariableNames()){
	 		if (origLiveOut.containsVariable(name)){
	 			_liveOut.addVariable(name, candidateLO.getVariable(name));
	 		}
	 	}
	 	
		initializebackwardLV(_liveOut);
		
		// Cannot remove kill variables
		_liveIn = new VariableSet();
		_liveIn.addVariables(liPassed);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		else if (isConst_rowLowerBound && isConst_rowUpperBound) {
			if (_rowLowerBound instanceof IntIdentifier && _rowUpperBound instanceof IntIdentifier)
				updatedRowDim = ((IntIdentifier)_rowUpperBound).getValue() - ((IntIdentifier)_rowLowerBound).getValue() + 1;
			
			else if (_rowLowerBound instanceof DoubleIdentifier && _rowUpperBound instanceof DoubleIdentifier)
				updatedRowDim = UtilFunctions.toLong( ((DoubleIdentifier)_rowUpperBound).getValue() - ((DoubleIdentifier)_rowLowerBound).getValue() + 1);
			
			else if (_rowLowerBound instanceof IntIdentifier && _rowUpperBound instanceof DoubleIdentifier)
				updatedRowDim = UtilFunctions.toLong( ((DoubleIdentifier)_rowUpperBound).getValue() - ((IntIdentifier)_rowLowerBound).getValue() + 1);
			
			else if (_rowLowerBound instanceof DoubleIdentifier && _rowUpperBound instanceof IntIdentifier)
				updatedRowDim = UtilFunctions.toLong( ((IntIdentifier)_rowUpperBound).getValue() - ((DoubleIdentifier)_rowLowerBound).getValue() + 1);
			
		}
		
		// CASE: row dimension is unknown --> assign -1
		else{ 
			updatedRowDim = -1;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
			if (_rowLowerBound != null && _rowUpperBound != null){
				if (_rowLowerBound.toString().equals(_rowUpperBound.toString()))
					retVal += _rowLowerBound.toString();
				else 
					retVal += _rowLowerBound.toString() + ":" + _rowUpperBound.toString();
			}
			else {
				if (_rowLowerBound != null || _rowUpperBound != null){
					if (_rowLowerBound != null)
						retVal += _rowLowerBound.toString();
					
					retVal += ":";
					
					if (_rowUpperBound != null)
						retVal += _rowUpperBound.toString();
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		long lgcd = f1._b[0];
		for( int i=1; i<f1._b.length; i++ )
			lgcd = determineGCD( lgcd, f1._b[i] );
		for( int i=0; i<f2._b.length; i++ )
			lgcd = determineGCD( lgcd, f2._b[i] );
		
		if( (Math.abs(f1._a-f2._a) % lgcd) != 0 ) { //if GCD divides the intercepts
			//no integer solution exists -> no dependency
			ret = false;
		}
		
		LOG.trace("PARFOR: GCD result: "+ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			boolean ixid = (dat1 instanceof IndexedIdentifier && dat2 instanceof IndexedIdentifier); 
			boolean ignoreRow = ixid && isRowIgnorable((IndexedIdentifier)dat1, (IndexedIdentifier)dat2);
			boolean ignoreCol = ixid && isColumnIgnorable((IndexedIdentifier)dat1, (IndexedIdentifier)dat2);
	
			LinearFunction f1p = null, f2p = null;
			if( ignoreRow ) {
				f1p = getColLinearFunction(dat1);
				f2p = getColLinearFunction(dat2);
			}
			if( ignoreCol ) {
				f1p = getRowLinearFunction(dat1);
				f2p = getRowLinearFunction(dat2);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				if( f1._b.length>i )
					lmax += (f1._b[i]>0) ? f1._b[i]*upper : f1._b[i]*lower;
				if( f2._b.length>i )
					lmax -= (f2._b[i]>0) ? f2._b[i]*lower : f2._b[i]*upper; 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
		HashMap<String, Expression> varParams = getVarParams();

		// "data" is the only parameter that is allowed to be unnamed
		if (varParams.containsKey(null)) {
			varParams.put("data", varParams.remove(null));
		}

		// Validate the number of parameters
		String fname = getOpCode().getName();
		String usageMessage = "function " + fname + " takes at least 1 and at most 2 parameters";
		if (varParams.size() < 1) {
			raiseValidateError("Too few parameters: " + usageMessage, conditional);
		}

		if (varParams.size() > 2) {
			raiseValidateError("Too many parameters: " + usageMessage, conditional);
		}

		// Check parameter names are valid
		Set<String> validParameterNames = CollectionUtils.asSet("data", "dir");
		checkInvalidParameters(getOpCode(), varParams, validParameterNames);

		// Check parameter expression data types match expected
		checkDataType(false, fname, "data", DataType.MATRIX, conditional);
		checkDataValueType(false, fname, "data", DataType.MATRIX, ValueType.FP64, conditional);

		// We need the dimensions of the input matrix to determine the output matrix characteristics
		// Validate data parameter, lookup previously defined var or resolve expression
		Identifier dataId = varParams.get("data").getOutput();
		if (dataId == null) {
			raiseValidateError("Cannot parse input parameter \"data\" to function " + fname, conditional);
		}

		checkStringParam(true, fname, "dir", conditional);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
		checkInvalidParameters(getOpCode(), varParams, validParameterNames);

		// Check parameter expression data types match expected
		checkDataType(false, fname, "data", DataType.MATRIX, conditional);
		checkDataValueType(false, fname, "data", DataType.MATRIX, ValueType.FP64, conditional);

		// We need the dimensions of the input matrix to determine the output matrix characteristics
		// Validate data parameter, lookup previously defined var or resolve expression
		Identifier dataId = varParams.get("data").getOutput();
		if (dataId == null) {
			raiseValidateError("Cannot parse input parameter \"data\" to function " + fname, conditional);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		ArrayList<String> inputs = _liveIn != null && _read != null ? new ArrayList<>() : null;
		if (_liveIn != null && _read != null) {
			for (String varName : _read.getVariables().keySet()) {
				if (_liveIn.containsVariable(varName))
					inputs.add(varName);
			}
		}
		return inputs;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		Expression incrementExpr = null; //1/-1
		if(ctx.iterPred.info.increment != null) {
			incrementExpr = ctx.iterPred.info.increment;
		}
		IterablePredicate predicate = new IterablePredicate(ctx, iterVar, ctx.iterPred.info.from, ctx.iterPred.info.to,
				incrementExpr, parForParamValues, currentFile);
		forStmt.setPredicate(predicate);

		if(ctx.body.size() > 0) {
			for(StatementContext stmtCtx : ctx.body)
				forStmt.addStatementBlock(getStatementBlock(stmtCtx.info.stmt));
			forStmt.mergeStatementBlocks();
		}
		ctx.info.stmt = forStmt;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			DataIdentifier dataId = new DataIdentifier(paramCtx.paramName.getText());
			String dataType = (paramCtx.paramType == null || paramCtx.paramType.dataType() == null
				|| paramCtx.paramType.dataType().getText() == null || paramCtx.paramType.dataType().getText().isEmpty()) ?
				"scalar" : paramCtx.paramType.dataType().getText();
			String valueType = paramCtx.paramType.valueType().getText();
			
			//check and assign data type
			checkValidDataType(dataType, paramCtx.start);
			if( !setDataAndValueType(dataId, dataType, valueType, paramCtx.start, false, true) )
				return null;
			retVal.add(dataId);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
		MatrixBlock a = inputs.get(0);
		if(a instanceof CompressedMatrixBlock)
			a = CompressedMatrixBlock.getUncompressed(a);

		SideInput[] b = prepInputMatrices(inputs);
		double[] scalars = prepInputScalars(scalarObjects);
		final int m = a.getNumRows();
		final int n = a.getNumColumns();
		
		//sparse safe check
		boolean sparseSafe = isSparseSafe() || (b.length == 0
				&& genexec( 0, b, scalars, m, n, 0, 0 ) == 0);
		
		long inputSize = sparseSafe ? 
			getTotalInputNnz(inputs) : getTotalInputSize(inputs);
		if( inputSize < PAR_NUMCELL_THRESHOLD ) {
			k = 1; //serial execution
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
				ExecutorService pool = CommonThreadPool.get(k);
				ArrayList<ParAggTask> tasks = new ArrayList<>();
				int nk = UtilFunctions.roundToNext(Math.min(8*k,m/32), k);
				int blklen = (int)(Math.ceil((double)m/nk));
				for( int i=0; i<nk & i*blklen<m; i++ )
					tasks.add(new ParAggTask(a, b, scalars, m, n, sparseSafe, i*blklen, Math.min((i+1)*blklen, m)));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
				double[] avals = a.values(i);
				int aix = a.pos(i);
				for( int j=0; j<n; j++ ) {
					double aval = avals[aix+j];
					if( aval != 0 || !sparseSafe)
						kplus.execute2(kbuff, genexec(aval, b, scalars, m, n, rix+i, i, j));
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
				double[] avals = a.values(i);
				int aix = a.pos(i);
				for( int j=0; j<n; j++ ) {
					double aval = avals[aix + j];
					if( aval != 0 || !sparseSafe)
						tmp = vfun.execute(tmp, genexec(aval, b, scalars, m, n, rix+i, i, j));
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
				int apos = sblock.pos(i);
				int alen = sblock.size(i);
				int[] aix = sblock.indexes(i);
				double[] avals = sblock.values(i);
				double[] cvals = c.values(i);
				int cix = c.pos(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
			int lastj = -1;
			//handle non-empty rows
			if( sblock != null && !sblock.isEmpty(i) ) {
				int apos = sblock.pos(i);
				int alen = sblock.size(i);
				int[] aix = sblock.indexes(i);
				double[] avals = sblock.values(i);
				for(int k=apos; k<apos+alen; k++) {
					//process zeros before current non-zero
					if( !sparseSafe )
						for(int j=lastj+1; j<aix[k]; j++)
							kplus.execute2(kbuff, genexec(0, b, scalars, m, n, rix+i, i, j));
					//process current non-zero
					lastj = aix[k];
					kplus.execute2(kbuff, genexec(avals[k], b, scalars, m, n, rix+i, i, lastj));
				}
			}
			//process empty rows or remaining zeros
			if( !sparseSafe )
				for(int j=lastj+1; j<n; j++)
					kplus.execute2(kbuff, genexec(0, b, scalars, m, n, rix+i, i, j));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofCellwise.java`
#### Snippet
```java
			int lastj = -1;
			//handle non-empty rows
			if( sblock != null && !sblock.isEmpty(i) ) {
				int apos = sblock.pos(i);
				int alen = sblock.size(i);
				int[] aix = sblock.indexes(i);
				double[] avals = sblock.values(i);
				for(int k=apos; k<apos+alen; k++) {
					//process zeros before current non-zero
					if( !sparseSafe )
						for(int j=lastj+1; j<aix[k]; j++)
							tmp = vfun.execute(tmp, genexec(0, b, scalars, m, n, rix+i, i, j));
					//process current non-zero
					lastj = aix[k];
					tmp = vfun.execute( tmp, genexec(avals[k], b, scalars, m, n, rix+i, i, lastj));
				}
			}
			//process empty rows or remaining zeros
			if( !sparseSafe )
				for(int j=lastj+1; j<n; j++)
					tmp = vfun.execute(tmp, genexec(0, b, scalars, m, n, rix+i, i, j));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
		DenseBlock[] ab = getDenseMatrices(prepInputMatrices(inputs, 1, 2, true, false));
		SideInput[] b = prepInputMatrices(inputs, 3, false);
		double[] scalars = prepInputScalars(scalarObjects);
		
		//core sequential execute
		final int m = inputs.get(0).getNumRows();
		final int n = inputs.get(0).getNumColumns();
		final int k = inputs.get(1).getNumColumns(); // rank
		
		MatrixBlock a = inputs.get(0);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
		if( !a.isInSparseFormat() )
			executeCellwiseDense(a.getDenseBlock(), ab[0], ab[1], b, scalars, out.getDenseBlock(), m, n, k, _outerProductType, 0, m, 0, n);
		else
			executeCellwiseSparse(a.getSparseBlock(), ab[0], ab[1], b, scalars, out, m, n, k, a.getNonZeros(), _outerProductType, 0, m, 0, n);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
		DenseBlock[] ab = getDenseMatrices(prepInputMatrices(inputs, 1, 2, true, false));
		SideInput[] b = prepInputMatrices(inputs, 3, false);
		double[] scalars = prepInputScalars(scalarObjects);
		
		//core sequential execute
		final int m = inputs.get(0).getNumRows();
		final int n = inputs.get(0).getNumColumns();
		final int k = inputs.get(1).getNumColumns(); // rank
		final long nnz = inputs.get(0).getNonZeros();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
		if( inputs==null || inputs.size() < 3 || out==null )
			throw new RuntimeException("Invalid input arguments.");
		
		//check empty result
		if( (_outerProductType == OutProdType.LEFT_OUTER_PRODUCT && inputs.get(1).isEmptyBlock(false)) //U is empty
			|| (_outerProductType == OutProdType.RIGHT_OUTER_PRODUCT &&  inputs.get(2).isEmptyBlock(false)) //V is empty
			|| inputs.get(0).isEmptyBlock(false) ) {  //X is empty
			out.examSparsity(); //turn empty dense into sparse
			return out;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofOuterProduct.java`
#### Snippet
```java
				if( sblock.isEmpty(i) ) continue;
				
				int wpos = sblock.pos(i);
				int wlen = sblock.size(i);
				int[] wix = sblock.indexes(i);
				double[] wvals = sblock.values(i);
				double[] uvals = u.values(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofRowwise.java`
#### Snippet
```java
		if( inputs==null || inputs.size() < 1 || out==null )
			throw new RuntimeException("Invalid input arguments.");
		
		//result allocation and preparations
		final int m = inputs.get(0).getNumRows();
		final int n = inputs.get(0).getNumColumns();
		final int n2 = _type.isConstDim2(_constDim2) ? (int)_constDim2 : 
			_type.isRowTypeB1() || hasMatrixSideInput(inputs) ?
			getMinColsMatrixSideInputs(inputs) : -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/BitmapEncoder.java`
#### Snippet
```java
				if(sb.isEmpty(r))
					continue;
				final int apos = sb.pos(r);
				final int alen = sb.size(r) + apos;
				final int[] aix = sb.indexes(r);
				final int idx = Arrays.binarySearch(aix, apos, alen, colIndex);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AMorphingMMColGroup.java`
#### Snippet
```java
		LOG.warn("Should never call decompress on morphing group instead extract common values and combine all commons");
		double[] cv = new double[_colIndexes.get(_colIndexes.size() - 1) + 1];
		AColGroup b = extractCommon(cv);
		b.decompressToSparseBlock(ret, rl, ru, offR, offC);
		decompressToSparseBlockCommonVector(ret, rl, ru, offR, offC, cv);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AMorphingMMColGroup.java`
#### Snippet
```java
				final int of = _colIndexes.get(r);
				if(sb.isEmpty(of))
					continue;
				final int apos = sb.pos(of);
				final int alen = sb.size(of) + apos;
				final int[] aix = sb.indexes(of);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
			if(sb.isEmpty(r))
				continue;
			final int apos = sb.pos(r);
			final int alen = sb.size(r) + apos;
			final int[] aix = sb.indexes(r);
			final double[] aval = sb.values(r);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
		for(int i = rl, offT = rl + offR; i < ru; i++, offT++) {
			final double[] c = db.values(offT);
			final int off = db.pos(offT) + offC;
			for(int j = apos; j < alen; j++)
				c[off + _colIndexes.get(aix[j])] += avals[j];
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
			if(sb.isEmpty(row))
				continue;
			final double[] c = db.values(offT);
			final int off = db.pos(offT) + offC;
			final int apos = sb.pos(row);
			final int alen = sb.size(row) + apos;
			final int[] aix = sb.indexes(row);
			final double[] avals = sb.values(row);
			for(int col = apos; col < alen; col++)
				c[_colIndexes.get(aix[col]) + off] += avals[col];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
			final double[] c = db.values(offT);
			final int off = db.pos(offT) + offC;
			final int apos = sb.pos(vr);
			final int alen = sb.size(vr) + apos;
			final int[] aix = sb.indexes(vr);
			final double[] aval = sb.values(vr);
			for(int j = apos; j < alen; j++)
				c[off + _colIndexes.get(aix[j])] += aval[j];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
			final int apos = sb.pos(vr);
			final int alen = sb.size(vr) + apos;
			final int[] aix = sb.indexes(vr);
			final double[] aval = sb.values(vr);
			for(int j = apos; j < alen; j++)
				ret.append(offT, offC + _colIndexes.get(aix[j]), aval[j]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java
			final SparseBlock sb = in.getSparseBlock();
			if(sb.isEmpty(col))
				// It should never be empty here.
				return;

			final int apos = sb.pos(col);
			final int alen = sb.size(col) + apos;
			final int[] aix = sb.indexes(col);
			final double[] aval = sb.values(col);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		if(sb.isEmpty(r))
			return;

		final int apos = sb.pos(r);
		final int alen = sb.size(r) + apos;
		final int[] aix = sb.indexes(r);
		final double[] avals = sb.values(r);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
					for(int rix = rsc, offT = rsc + offR; rix < ru; rix++, offT++) {
						final double[] c = db.values(offT);
						final int off = db.pos(offT) + offC;
						for(int j = 0; j < nCol; j++)
							c[off + _colIndexes.get(j)] += values[rowIndex + j];
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			final int blen = _ptr[k + 1]; // 2 short to handle last differently
			final skipPair tmp = skipScanVal(k, rl);
			final int sbApos = sb.pos(k);
			final int sbAlen = sb.size(k) + sbApos;
			final int[] sbAix = sb.indexes(k);
			final double[] sbAval = sb.values(k);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
					for(int r = rl; r < ru; r++) {
						final int off = (r - rl) * nv + k;
						final int offI = nCol * r;
						for(int rix = rsc + offI; rix < cu + offI; rix++) {
							preAgg[off] += mV[rix];
						}
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				rs = re + _data[apos];
				re = rs + _data[apos + 1];
				// if index is later than run continue
				if(itThat.value() >= re || rs == re || rs > finalOff)
					continue;
				// while lower than run iterate through
				while(itThat.value() < rs && itThat.value() != finalOff)
					itThat.next();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		final int count = _numRows - _data.size();
		for(int x = 0; x < _colIndexes.size(); x++) {
			double v = c[_colIndexes.get(x)];
			c[_colIndexes.get(x)] = v != 0 ? v * Math.pow(_defaultTuple[x], count) : 0;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		final double[] newDefaultTuple = new double[_defaultTuple.length];
		for(int i = 0; i < _defaultTuple.length; i++)
			newDefaultTuple[i] = op.fn.execute(v[_colIndexes.get(i)], _defaultTuple[i]);
		final ADictionary newDict = _dict.binOpLeft(op, v, _colIndexes);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		final double[] newDefaultTuple = new double[_defaultTuple.length];
		for(int i = 0; i < _defaultTuple.length; i++)
			newDefaultTuple[i] = op.fn.execute(_defaultTuple[i], v[_colIndexes.get(i)]);
		final ADictionary newDict = _dict.binOpRight(op, v, _colIndexes);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		if(that instanceof ColGroupSDCZeros) {
			ColGroupSDCZeros th = (ColGroupSDCZeros) that;
			return th._indexes == _indexes && th._data == _data;
		}
		else if(that instanceof ColGroupSDC) {
			ColGroupSDC th = (ColGroupSDC) that;
			return th._indexes == _indexes && th._data == _data;
		}
		else
			return false;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Default: "));
		sb.append(Arrays.toString(_defaultTuple));
		sb.append(String.format("\n%15s", "Indexes: "));
		sb.append(_indexes.toString());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCFOR.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Indexes: "));
		sb.append(_indexes.toString());
		sb.append(String.format("\n%15s", "Data: "));
		sb.append(_data);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
		if(that instanceof ColGroupSDCSingleZeros) {
			ColGroupSDCSingleZeros th = (ColGroupSDCSingleZeros) that;
			return th._indexes == _indexes;
		}
		else if(that instanceof ColGroupSDCSingle) {
			ColGroupSDCSingle th = (ColGroupSDCSingle) that;
			return th._indexes == _indexes;
		}
		else
			return false;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);

		final int last = _indexes.getOffsetToLast();
		if(ru > last)
			decompressToDenseBlockSparseDictionaryPost(db, rl, ru, offR, offC, sb, it, last);
		else
			decompressToDenseBlockSparseDictionaryPre(db, rl, ru, offR, offC, sb, it);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
			for(int r = rl; r < ru; r++) {
				if(sb.isEmpty(r))
					continue;
				final int off = r - rl;
				int apos = aOffs[off]; // current offset
				final int alen = sb.size(r) + sb.pos(r);
				final int[] aix = sb.indexes(r);
				while(apos < alen && aix[apos] < i)// increment all pointers to offset
					apos++;

				if(apos < alen && aix[apos] == i)
					preAgg[off] += sb.values(r)[apos];
				aOffs[off] = apos;
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
				rs = re + that._data[apos];
				re = rs + that._data[apos + 1];
				// if index is later than run continue
				if(itThis.value() >= re || rs == re || rs > finalOff)
					continue;
				// while lower than run iterate through
				while(itThis.value() < rs && itThis.value() != finalOff)
					itThis.next();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
			final int idx = offR + it.value();
			final double[] c = db.values(idx);
			final int off = db.pos(idx) + offC;
			final int offDict = _data.getIndex(it.getDataIndex()) * nCol;
			for(int j = 0; j < nCol; j++)
				c[off + _colIndexes.get(j)] += values[offDict + j];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
			final int off = db.pos(idx) + offC;
			final int apos = sb.pos(dictIndex);
			final int alen = sb.size(dictIndex) + apos;
			final double[] avals = sb.values(dictIndex);
			final int[] aix = sb.indexes(dictIndex);
			for(int j = apos; j < alen; j++)
				c[off + _colIndexes.get(aix[j])] += avals[j];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
				final int apos = sb.pos(dictIndex);
				final int alen = sb.size(dictIndex) + apos;
				final double[] avals = sb.values(dictIndex);
				final int[] aix = sb.indexes(dictIndex);
				for(int j = apos; j < alen; j++)
					ret.append(row, _colIndexes.get(aix[j]) + offC, avals[j]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
				final int row = offR + it.value();
				final int dx = it.getDataIndex();
				final int offDict = _data.getIndex(dx) * nCol;
				for(int j = 0; j < nCol; j++)
					ret.append(row, _colIndexes.get(j) + offC, values[offDict + j]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
					final int apos = dsb.pos(c);
					final int alen = dsb.size(c) + apos;
					final int[] aix = dsb.indexes(c);
					final double[] aval = dsb.values(c);
					for(int i = apos; i < alen; i++)
						retV[offR + _colIndexes.get(aix[i])] += v * aval[i];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		MatrixBlock ret = new MatrixBlock(_data.getNumRows(), _data.getNumColumns(), _data.getNonZeros());
		// TODO add sparse optmization
		for(int r = 0; r < _data.getNumRows(); r++)
			for(int c = 0; c < _data.getNumColumns(); c++)
				ret.quickSetValue(r, c, _data.quickGetValue(r, reordering[c]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictLibMatrixMult.java`
#### Snippet
```java
			if(left.isEmpty(i) || right.isEmpty(i))
				continue;
			final int leftAPos = left.pos(i);
			final int leftAlen = left.size(i) + leftAPos;
			final int[] leftAix = left.indexes(i);
			final double[] leftVals = left.values(i);
			final int rightAPos = right.pos(i);
			final int rightAlen = right.size(i) + rightAPos;
			final int[] rightAix = right.indexes(i);
			final double[] rightVals = right.values(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictLibMatrixMult.java`
#### Snippet
```java
				if(vl != 0) {
					for(int k = apos; k < alen; k++) {
						final double vr = rightVals[k];
						final int colOut = colsRight.get(aix[k]);
						addToUpperTriangle(resCols, rowOut, colOut, resV, vl * vr);
					}
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int i = 0; i < nRows; i++) {
			final int off = i * nCol;
			res[i] = _values[off];
			for(int j = off + 1; j < off + nCol; j++)
				res[i] = fn.execute(res[i], _values[j]);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		if(nrColumns == 1)
			return getValues(); // shallow copy of values

		// pre-aggregate value tuple
		final int numVals = getNumberOfValues(nrColumns);
		double[] ret = new double[numVals];
		for(int k = 0; k < numVals; k++)
			ret[k] = sumRow(k, nrColumns);

		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		int orgOffset = idxStart;
		int targetOffset = 0;
		for(int v = 0; v < numberTuples; v++) {
			for(int c = 0; c < tupleLengthAfter; c++, orgOffset++, targetOffset++) {
				newDictValues[targetOffset] = _values[orgOffset];
			}
			orgOffset += previousNumberOfColumns - idxEnd + idxStart;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int i = 0; i < nRow; i++) {
			long rowCount = 0;
			final int off = i * nCol;
			for(int j = off; j < off + nCol; j++) {
				if(_values[j] != 0)
					rowCount++;
			}
			nnz += rowCount * counts[i];
		}
		return nnz;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		addToOffsets(v, f1 * nCol, t1 * nCol, nCol);
		addToOffsets(v, f2 * nCol, t2 * nCol, nCol);
		addToOffsets(v, f3 * nCol, t3 * nCol, nCol);
		addToOffsets(v, f4 * nCol, t4 * nCol, nCol);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		final double[] scaledValues = new double[_values.length];
		int off = 0;
		for(int tuple = 0; tuple < _values.length / nCol; tuple++) {
			final int scale = scaling[tuple];
			for(int v = 0; v < nCol; v++) {
				scaledValues[off] = _values[off] * scale;
				off++;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int k = 0, off = 0; k < numVals * colIndexes.size(); k += colIndexes.size(), off += aggregateColumns.size()) {
			for(int h = 0; h < colIndexes.size(); h++) {
				int idb = colIndexes.get(h) * cut;
				double v = _values[k + h];
				if(v != 0)
					for(int i = 0; i < aggregateColumns.size(); i++)
						ret[off + i] += v * b[idb + aggregateColumns.get(i)];
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		BigDecimal tmp = BigDecimal.ONE;
		for(int i = 0; i < len; i++) {
			for(int j = i * nCol; j < (i + 1) * nCol; j++) {
				double v = _values[j];
				if(v == 0) {
					ret[0] = 0;
					return;
				}
				tmp = tmp.multiply(new BigDecimal(v).pow(counts[i], cont), cont);
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int i = 0; i < len; i++) {
			for(int j = i * nCol; j < (i + 1) * nCol; j++) {
				double v = _values[j];
				if(v == 0) {
					ret[0] = 0;
					return;
				}
				tmp = tmp.multiply(new BigDecimal(v).pow(counts[i], cont), cont);
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int x = 0; x < def.length; x++)
			tmp = tmp.multiply(new BigDecimal(def[x]).pow(defCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		int off = 0;
		for(int i = 0; i < len; i++) {
			for(int j = 0; j < nCol; j++) {
				final double v = _values[off++] + reference[j];
				if(v == 0) {
					ret[0] = 0;
					return;
				}
				tmp = tmp.multiply(new BigDecimal(v).pow(counts[i], cont), cont);
			}
		}
		for(int x = 0; x < reference.length; x++)
			tmp = tmp.multiply(new BigDecimal(reference[x]).pow(refCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		for(int i = 0; i < nRow; i++) {
			for(int j = 0; j < nCol; j++) {
				final double v = values[off++] + reference[j];
				if(v == 0) {
					ret[0] = 0;
					return;
				}
				tmp = tmp.multiply(new BigDecimal(v).pow(counts[i], cont), cont);
			}
		}
		for(int x = 0; x < reference.length; x++)
			tmp = tmp.multiply(new BigDecimal(reference[x]).pow(refCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/DictionaryFactory.java`
#### Snippet
```java
			for(int i = 0; i < nVals; i++) {
				final double[] tuple = mcbm.getValues(i);
				for(int col = 0; col < nCol; col++)
					sb.append(i, col, tuple[col]);
			}
			m.recomputeNonZeros();
			m.examSparsity(true);
			return MatrixBlockDictionary.create(m);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			SparseBlock sb = _data.getSparseBlock();
			for(int i = 0; i < _data.getNumRows(); i++) {
				if(!sb.isEmpty(i)) {
					final int apos = sb.pos(i);
					final int alen = sb.size(i) + apos;
					final double[] avals = sb.values(i);
					ret[i] = avals[apos];
					for(int j = apos + 1; j < alen; j++)
						ret[i] = fn.execute(ret[i], avals[j]);

					if(sb.size(i) < _data.getNumColumns())
						ret[i] = fn.execute(ret[i], 0);
				}
				else
					ret[i] = fn.execute(ret[i], 0);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			double[] values = _data.getDenseBlockValues();
			int off = 0;
			for(int k = 0; k < _data.getNumRows(); k++) {
				ret[k] = values[off++];
				for(int j = 1; j < _data.getNumColumns(); j++)
					ret[k] = fn.execute(ret[k], values[off++]);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				if(sb.isEmpty(i))
					continue;
				final int apos = sb.pos(i);
				final int alen = sb.size(i) + apos;
				final double[] avals = sb.values(i);
				final int[] aix = sb.indexes(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		final int nCol = _data.getNumColumns();
		final int nRow = _data.getNumRows();
		final MatrixBlock ret = new MatrixBlock(nRow, nCol, false);
		ret.allocateDenseBlock();
		final double[] retV = ret.getDenseBlockValues();
		int off = 0;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		if(_data.isInSparseFormat()) {
			final SparseBlock sb = _data.getSparseBlock();
			for(int i = 0; i < nRow; i++) {
				if(sb.isEmpty(i))
					for(int j = 0; j < nCol; j++)
						retV[off++] = op.fn.execute(v[j], 0);
				else {
					final int apos = sb.pos(i);
					final int alen = sb.size(i) + apos;
					final int[] aix = sb.indexes(i);
					final double[] avals = sb.values(i);
					int j = 0;
					for(int k = apos; j < nCol && k < alen; j++) {
						final double vx = aix[k] == j ? avals[k++] : 0;
						retV[off++] = op.fn.execute(v[j], vx);
					}
					for(; j < nCol; j++)
						retV[off++] = op.fn.execute(v[j], 0);
				}
			}
		}
		else {
			final double[] values = _data.getDenseBlockValues();
			for(int i = 0; i < nRow; i++) {
				for(int j = 0; j < nCol; j++) {
					retV[off] = op.fn.execute(v[j], values[off]);
					off++;
				}
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			final double[] values = _data.getDenseBlockValues();
			for(int i = 0; i < nRow; i++) {
				for(int j = 0; j < nCol; j++) {
					retV[off] = op.fn.execute(values[off], v[j]);
					off++;
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		final int nCol = _data.getNumColumns();
		final int nRow = _data.getNumRows();

		final MatrixBlock ret = new MatrixBlock(nRow + 1, nCol, false);
		ret.allocateDenseBlock();
		final double[] retV = ret.getDenseBlockValues();

		int off = 0;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				if(!sb.isEmpty(i)) {
					final int apos = sb.pos(i);
					final int alen = sb.size(i) + apos;
					final double[] avals = sb.values(i);
					for(int j = apos; j < alen; j++) {
						ret[i] += avals[j];
					}
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
					final int apos = sb.pos(i);
					final int alen = sb.size(i) + apos;
					final int[] aix = sb.indexes(i);
					final double[] avals = sb.values(i);
					int k = apos;
					int j = 0;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
					final int count = counts[i];
					final int apos = sb.pos(i);
					final int alen = sb.size(i) + apos;
					final int[] aix = sb.indexes(i);
					final double[] avals = sb.values(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				if(sb.isEmpty(i))
					continue;
				final int count = counts[i];
				final int apos = sb.pos(i);
				final int alen = sb.size(i) + apos;
				final int[] aix = sb.indexes(i);
				final double[] avals = sb.values(i);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				addToEntrySparseCSR(csr, v, f1, t1 * nCol, nCol, aix, avals);
				addToEntrySparseCSR(csr, v, f2, t2 * nCol, nCol, aix, avals);
				addToEntrySparseCSR(csr, v, f3, t3 * nCol, nCol, aix, avals);
				addToEntrySparseCSR(csr, v, f4, t4 * nCol, nCol, aix, avals);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			addToEntryDense(thisV, v, f1 * nCol, t1 * nCol, nCol);
			addToEntryDense(thisV, v, f2 * nCol, t2 * nCol, nCol);
			addToEntryDense(thisV, v, f3 * nCol, t3 * nCol, nCol);
			addToEntryDense(thisV, v, f4 * nCol, t4 * nCol, nCol);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			final double[] values = _data.getDenseBlockValues();
			BigDecimal tmp = BigDecimal.ONE;
			int off = 0;
			for(int i = 0; i < nRow; i++) {
				for(int j = 0; j < nCol; j++) {
					final double v = values[off++];
					tmp = tmp.multiply(new BigDecimal(v).pow(counts[i], cont), cont);
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
		final int sr = other.size();
		final int sl = size();
		final int[] ret = new int[sr + sl];
		int pl = 0;
		int pr = 0;
		int i = 0;
		while(pl < sl && pr < sr) {
			final int vl = get(pl);
			final int vr = other.get(pr);
			if(vl < vr) {
				ret[i++] = vl;
				pl++;
			}
			else {
				ret[i++] = vr;
				pr++;
			}
		}
		while(pl < sl)
			ret[i++] = get(pl++);
		while(pr < sr)
			ret[i++] = other.get(pr++);
		return ColIndexFactory.create(ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
			final int start = c + offR;
			final int end = c + offE;
			int nValOff = 0;
			for(int off = start; off < end; off += nCol) {
				preAV[id1 + nValOff] += mV[off];
				preAV[id2 + nValOff] += mV[off + 1];
				preAV[id3 + nValOff] += mV[off + 2];
				preAV[id4 + nValOff] += mV[off + 3];
				preAV[id5 + nValOff] += mV[off + 4];
				preAV[id6 + nValOff] += mV[off + 5];
				preAV[id7 + nValOff] += mV[off + 6];
				preAV[id8 + nValOff] += mV[off + 7];
				nValOff += nVal;
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		while(i < tSize && tv < v) { // this is at final
			itThat.next();
			i++;
			tv = itThat.value();
			if(tv == v) {
				final int fr = tm.getIndex(i);
				final int to = m.getIndex(j);
				td.addToEntry(dv, fr, to, nCol);
				return;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToChar.java`
#### Snippet
```java
		final int h = (cu - cl) % 8;
		off += cl;
		for(int rc = cl; rc < cl + h; rc++, off++)
			preAV[_data[rc]] += mV[off];
		for(int rc = cl + h; rc < cu; rc += 8, off += 8) {
			preAV[_data[rc]] += mV[off];
			preAV[_data[rc + 1]] += mV[off + 1];
			preAV[_data[rc + 2]] += mV[off + 2];
			preAV[_data[rc + 3]] += mV[off + 3];
			preAV[_data[rc + 4]] += mV[off + 4];
			preAV[_data[rc + 5]] += mV[off + 5];
			preAV[_data[rc + 6]] += mV[off + 6];
			preAV[_data[rc + 7]] += mV[off + 7];
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToChar.java`
#### Snippet
```java
		final int h = (cu - cl) % 8;
		preAggregateDenseMultiRowContiguousBy1(mV, nCol, nVal, preAV, rl, ru, cl, cl + h);
		final int offR = nCol * rl;
		final int offE = nCol * ru;
		for(int c = cl + h; c < cu; c += 8) {
			final int id1 = _data[c], id2 = _data[c + 1], id3 = _data[c + 2], id4 = _data[c + 3], id5 = _data[c + 4],
				id6 = _data[c + 5], id7 = _data[c + 6], id8 = _data[c + 7];

			final int start = c + offR;
			final int end = c + offE;
			int nValOff = 0;
			for(int off = start; off < end; off += nCol) {
				preAV[id1 + nValOff] += mV[off];
				preAV[id2 + nValOff] += mV[off + 1];
				preAV[id3 + nValOff] += mV[off + 2];
				preAV[id4 + nValOff] += mV[off + 3];
				preAV[id5 + nValOff] += mV[off + 4];
				preAV[id6 + nValOff] += mV[off + 5];
				preAV[id7 + nValOff] += mV[off + 6];
				preAV[id8 + nValOff] += mV[off + 7];
				nValOff += nVal;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		int v = it.value();
		while(v < last) {
			if(aix[apos] == v) {
				preAV[data.getIndex(it.getDataIndex())] += avals[apos++];
				v = it.next();
			}
			else if(aix[apos] < v)
				apos++;
			else
				v = it.next();
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		while(apos < alen) {
			if(aix[apos] == v) {
				preAV[data.getIndex(it.getDataIndex())] += avals[apos++];
				v = it.next();
			}
			else if(aix[apos] < v)
				apos++;
			else
				v = it.next();
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
			for(int r = rl; r < ru; r++) {
				if(sb.isEmpty(r))
					continue;
				final int off = r - rl;
				int apos = aOffs[off]; // current offset
				final int alen = sb.size(r) + sb.pos(r);
				final int[] aix = sb.indexes(r);
				while(apos < alen && aix[apos] < i)// increment all pointers to offset
					apos++;

				if(apos < alen && aix[apos] == i)
					preAV[off * nVal + data.getIndex(it.getDataIndex())] += sb.values(r)[apos];
				aOffs[off] = apos;
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetByte.java`
#### Snippet
```java
				final int div = offFirst / OffsetByte.maxV;
				final int mod = offFirst % OffsetByte.maxV;
				if(mod == 0) {
					p += div - 1; // skip values
					ret[p++] = (byte) OffsetByte.maxV;
				}
				else {
					p += div; // skip values
					ret[p++] = (byte) (mod);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/DenseEncoding.java`
#### Snippet
```java
		if(map == other.map) // same object
			return this; // unlikely to happen but cheap to compute

		final AMapToData lm = map;
		final AMapToData rm = other.map;

		final int nVL = lm.getUnique();
		final int nVR = rm.getUnique();
		final int size = map.size();
		final int maxUnique = nVL * nVR;

		final AMapToData ret = MapToFactory.create(size, maxUnique);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/SparseEncoding.java`
#### Snippet
```java
		final int fl = off.getOffsetToLast();
		final int fr = e.off.getOffsetToLast();
		final AIterator itl = off.getIterator();
		final AIterator itr = e.off.getIterator();

		final int nVl = getUnique();
		final int nVr = e.getUnique();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/SparseEncoding.java`
#### Snippet
```java
		final AMapToData retMap = MapToFactory.create(nRows, (nVl + 1) * (nVr + 1));
		int il = itl.value();
		// parse through one side set all values into the dense.
		while(il < fl) {
			retMap.set(il, map.getIndex(itl.getDataIndex()) + 1);
			il = itl.next();
		}
		retMap.set(fl, map.getIndex(itl.getDataIndex()) + 1);

		int ir = itr.value();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibRightMultBy.java`
#### Snippet
```java
		final int rl = m1.getNumRows();
		final int cr = that.getNumColumns();
		final int rr = that.getNumRows(); // shared dim
		final List<AColGroup> colGroups = m1.getColGroups();
		final List<AColGroup> retCg = new ArrayList<>();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibRightMultBy.java`
#### Snippet
```java
		double[] constV = shouldFilter ? new double[rr] : null;
		final List<AColGroup> filteredGroups = CLALibUtils.filterGroups(colGroups, constV);
		if(colGroups == filteredGroups)
			constV = null;

		if(k == 1)
			RMMSingle(filteredGroups, that, retCg);
		else
			RMMParallel(filteredGroups, that, retCg, k);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibStack.java`
#### Snippet
```java
			final MatrixBlock b = m.get(lookup);
			if(!(b instanceof CompressedMatrixBlock)) {
				LOG.warn("Found uncompressed matrix in Map of matrices, this is not"
					+ " supported in combine therefore falling back to decompression");
				return combineViaDecompression(m, rlen, clen, blen, k);
			}
			final CompressedMatrixBlock cmb = (CompressedMatrixBlock) b;
			if(cmb.isOverlapping()) {
				LOG.warn("Not supporting overlapping combine yet falling back to decompression");
				return combineViaDecompression(m, rlen, clen, blen, k);
			}
			final List<AColGroup> gs = cmb.getColGroups();
			final int off = bc * blen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibTernaryOp.java`
#### Snippet
```java
		final double d1 = s1 ? m1.quickGetValue(0, 0) : Double.NaN;
		final double d2 = s2 ? m2.quickGetValue(0, 0) : Double.NaN;
		final double d3 = s3 ? m3.quickGetValue(0, 0) : Double.NaN;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
		Bucket l = _data[ix];
		if(l == null)
			_data[ix] = new Bucket(ent);
		else {
			while(l != null)
				l = l.n;
			Bucket ob = _data[ix];
			_data[ix] = new Bucket(ent);
			_data[ix].n = ob;
		}
		_size++;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java
		if(l == null)
			_data[ix] = new Bucket(ent);
		else {
			while(l != null)
				l = l.n;
			Bucket ob = _data[ix];
			_data[ix] = new Bucket(ent);
			_data[ix].n = ob;
		}
		_size++;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java
		final int[] counts = new int[_size];
		int i = 0;
		for(Bucket e : _data)
			while(e != null) {
				counts[i] = e.v.count;
				e.v.count = i++;
				e = e.n;
			}

		return counts;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java
		int i = 0;
		for(Bucket e : _data)
			while(e != null) {
				counts[i] = e.v.count;
				e.v.count = i++;
				e = e.n;
			}
		return counts;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayCountHashMap.java`
#### Snippet
```java
		if(_data.length > Integer.MAX_VALUE / RESIZE_FACTOR)
			return;

		// resize data array and copy existing contents
		Bucket[] olddata = _data;
		_data = new Bucket[_data.length * RESIZE_FACTOR];
		_size = 0;

		// rehash all entries
		for(Bucket e : olddata) {
			while(e != null) {
				appendValue(e.v);
				e = e.n;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java
		int hash = key.hashCode();
		int ix = indexFor(hash, _data.length);

		// find entry

		while(_data[ix] != null && !_data[ix].keyEquals(key)) {
			hash = Integer.hashCode(hash + 1); // hash of hash
			ix = indexFor(hash, _data.length);
			hashMissCount++;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName() + this.hashCode());
		for(int i = 0; i < _data.length; i++)
			if(_data[i] != null)
				sb.append(", " + _data[i]);
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/ForProgramBlock.java`
#### Snippet
```java
		ScalarObject from = executePredicateInstructions( 1, _fromInstructions, ec, false);
		ScalarObject to   = executePredicateInstructions( 2, _toInstructions, ec, false);
		ScalarObject incr = (_incrementInstructions == null || _incrementInstructions.isEmpty()) ? 
			new IntObject((from.getLongValue()<=to.getLongValue()) ? 1 : -1) :
			executePredicateInstructions( 3, _incrementInstructions, ec, false);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/ForProgramBlock.java`
#### Snippet
```java
				if (DMLScript.LINEAGE_DEDUP)
					// create a new dedup map, if needed, to trace this iteration
					ec.getLineage().createDedupPatch(this, ec);
				
				//execute all child blocks
				for (int i = 0; i < _childBlocks.size(); i++)
					_childBlocks.get(i).execute(ec);
				
				if (DMLScript.LINEAGE_DEDUP) {
					LineageDedupUtils.replaceLineage(ec);
					// hook the dedup map to the main lineage trace
					ec.getLineage().traceCurrentDedupPath(this, ec);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		ParForBody body = new ParForBody(_childBlocks, _resultVars, ec);
		HashMap<String, byte[]> clsMap = new HashMap<>();
		String program = ProgramConverter.serializeParForBody(body, clsMap);
		
		if( _monitor ) 
			StatisticMonitor.putPFStat(_ID, Stat.PARFOR_INIT_PARWRK_T, time.stop());
		
		// Step 2) create tasks 
		TaskPartitioner partitioner = createTaskPartitioner(from, to, incr);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					Stream<Object> tmp = Arrays.stream(results).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (dat instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					String fname = constructResultMergeFileName();
					ResultMerge<?> rm = createResultMerge(_resultMerge, out, in, fname, var._isAccum, ec);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		String scratchSpaceLoc = ConfigurationManager.getScratchSpace();
		String fname = PARFOR_MR_RESULTMERGE_FNAME;
		fname = fname.replaceAll("%ID%", String.valueOf(_ID)); //replace workerID
		fname = fname.replaceAll("%VAR%", String.valueOf(_resultVarsIDSeq.getNextID()));
		
		StringBuilder sb = new StringBuilder();
		sb.append(scratchSpaceLoc);
		sb.append(Lop.FILE_SEPARATOR);
		sb.append(Lop.PROCESS_PREFIX);
		sb.append(DMLScript.getUUID());
		sb.append(fname);
		
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheDataOutput.java`
#### Snippet
```java
			else if( c>=0x0800 ) { //3 byte range
				_buff[_count++] = (byte) (0xE0 | ((c >> 12) & 0x0F));
				_buff[_count++] = (byte) (0x80 | ((c >>  6) & 0x3F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
			else { //2 byte range and null
				_buff[_count++] = (byte) (0xC0 | ((c >>  6) & 0x1F));
				_buff[_count++] = (byte) (0x80 | ((c >>  0) & 0x3F));
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheStatistics.java`
#### Snippet
```java
		_numHitsMem.reset();
		_numHitsLin.reset();
		_numHitsFSBuff.reset();
		_numHitsFS.reset();
		_numHitsHDFS.reset();
		
		_numWritesBPool.reset();
		_numWritesFS.reset();
		_numWritesHDFS.reset();
		_numWritesLin.reset();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsMem.longValue());
		sb.append("/");
		sb.append(_numHitsLin.longValue());
		sb.append("/");
		sb.append(_numHitsFSBuff.longValue());
		sb.append("/");
		sb.append(_numHitsFS.longValue());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
		if( DMLScript.USE_ACCELERATOR && _gpuObjects != null ) {
			boolean copiedFromGPU = false;
			for (Map.Entry<GPUContext, GPUObject> kv : _gpuObjects.entrySet()) {
				GPUObject gObj = kv.getValue();
				if (gObj != null && copiedFromGPU && gObj.isDirty())
					throw new DMLRuntimeException("Internal Error : Inconsistent internal state, A copy of this CacheableData was dirty on more than 1 GPU");
				else if (gObj != null) {
					copiedFromGPU = gObj.acquireHostRead(null);
					if( _data == null )
						getCache();
				}
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
					String ftmp = entry.getKey();
					ByteBuffer tmp = entry.getValue();
					
					if( tmp != null ) {
						//wait for pending serialization
						tmp.checkSerialized();
						
						//evict matrix
						tmp.evictBuffer(ftmp);
						tmp.freeMemory();
						_size -= tmp.getSize();
						numEvicted++;
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
		boolean requiresDelete = true;
		
		synchronized( _mQueue )
		{
			//remove queue entry 
			ByteBuffer ldata = _mQueue.remove(fname);
			if( ldata != null ) {
				_size -= ldata.getSize();
				requiresDelete = false;
				ldata.freeMemory(); //cleanup
			}
		}
		
		//delete from FS if required
		if( requiresDelete )
			_fClean.deleteFile(fname);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				if (numParts > 1) {
					Arrays.parallelSetAll(ret, i -> createPartitionedBroadcast(pmb, numPerPart, i));
				} else { //single partition
					ret[0] = getSparkContext().broadcast(pmb);
					if (!isLocalMaster())
						pmb.clearBlocks();
				}
				
				bret = new PartitionedBroadcast<>(ret, mo.getDataCharacteristics());
				// create the broadcast handle if the matrix or frame has never been broadcasted
				if (mo.getBroadcastHandle() == null) {
					mo.setBroadcastHandle(new BroadcastObject<MatrixBlock>());
				}
				mo.getBroadcastHandle().setPartitionedBroadcast(bret,
					OptimizerUtils.estimatePartitionedSizeExactSparsity(mo.getDataCharacteristics()));
				CacheableData.addBroadcastSize(mo.getBroadcastHandle().getSize());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
			int numLines = wrappedInstruction.length;
			
			for(int wrapIter = 0; wrapIter < numLines; wrapIter++) {
				String instStr = (wrapIter < wrappedInstruction.length) ? wrappedInstruction[wrapIter] : "";
				if(wrapIter == 0) {
					sb.append(String.format(
						" %" + maxNumLen + "d  %-" + maxInstLen + "s  %" + maxTimeSLen + "s  %" 
						+ maxCountLen + "d", (counter + 1), instStr, timeSString, count));
				}
				else {
					sb.append(String.format(
						" %" + maxNumLen + "s  %-" + maxInstLen + "s  %" + maxTimeSLen + "s  %" 
						+ maxCountLen + "s", "", instStr, "", ""));
				}
				sb.append("\n");
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
				if(varOldIn[i] != null) {
					linst[j] = linst[j].replace(
						Lop.OPERAND_DELIMITOR + varOldIn[i].getName() + Lop.DATATYPE_PREFIX,
						Lop.OPERAND_DELIMITOR + String.valueOf(varNewIn[i]) + Lop.DATATYPE_PREFIX);
					linst[j] = linst[j].replace("=" + varOldIn[i].getName(), "=" + String.valueOf(varNewIn[i])); //parameterized
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/models/StatisticsModel.java`
#### Snippet
```java
		if (utilization != null) {
			utilizationStr = utilization.stream()
					.map(UtilizationModel::toString)
					.collect(Collectors.joining(","));
		}

		if (traffic != null) {
			trafficStr = traffic.stream()
					.map(TrafficModel::toString)
					.collect(Collectors.joining(","));
		}

		if (events != null) {
			eventsStr = events.stream()
					.map(EventModel::toString)
					.collect(Collectors.joining(","));
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
				ListObject model = pullModel();
				ListObject gradients = computeGradientsForNBatches(model, _numBatchesPerNbatch, localStartBatchNum, true);

				Timing tAgg = DMLScript.STATISTICS ? new Timing(true) : null;
				weightAndPushGradients(gradients);
				if (tAgg != null) {
					ParamServStatistics.accFedAggregation((long)tAgg.stop());
				}

				ParamservUtils.cleanupListObject(model);
				ParamservUtils.cleanupListObject(gradients);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
			boolean opt = !ec.getProgram().containsFunctionProgramBlock(namespace, gradientsFunc, false);
			FunctionProgramBlock func = ec.getProgram().getFunctionProgramBlock(namespace, gradientsFunc, opt);
			ArrayList<DataIdentifier> inputs = func.getInputParams();
			ArrayList<DataIdentifier> outputs = func.getOutputParams();
			CPOperand[] boundInputs = inputs.stream()
					.map(input -> new CPOperand(input.getName(), input.getValueType(), input.getDataType()))
					.toArray(CPOperand[]::new);
			ArrayList<String> outputNames = outputs.stream().map(DataIdentifier::getName)
					.collect(Collectors.toCollection(ArrayList::new));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/PSWorker.java`
#### Snippet
```java
		String[] cfn = DMLProgram.splitFunctionKey(updFunc);
		String ns = cfn[0];
		String fname = cfn[1];
		boolean opt = !ec.getProgram().containsFunctionProgramBlock(ns, fname, false);
		FunctionProgramBlock func = ec.getProgram().getFunctionProgramBlock(ns, fname, opt);
		ArrayList<DataIdentifier> inputs = func.getInputParams();
		ArrayList<DataIdentifier> outputs = func.getOutputParams();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamServer.java`
#### Snippet
```java
		String ns = cfn[0];
		String fname = cfn[1];
		boolean opt = !ec.getProgram().containsFunctionProgramBlock(ns, fname, false);
		FunctionProgramBlock func = ec.getProgram().getFunctionProgramBlock(ns, fname, opt);
		ArrayList<DataIdentifier> inputs = func.getInputParams();
		ArrayList<DataIdentifier> outputs = func.getOutputParams();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
		if (accGradients == null)
			return ParamservUtils.copyList(gradients, cleanup);
		IntStream range = IntStream.range(0, accGradients.getLength());
		(par ? range.parallel() : range).forEach(i -> {
			MatrixBlock mb1 = ((MatrixObject) accGradients.getData().get(i)).acquireReadAndRelease();
			MatrixBlock mb2 = ((MatrixObject) gradients.getData().get(i)).acquireReadAndRelease();
			mb1.binaryOperationsInPlace(new BinaryOperator(Plus.getPlusFnObject()), mb2);
		});
		if (cleanup)
			ParamservUtils.cleanupListObject(gradients);
		return accGradients;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DCSparkScheme.java`
#### Snippet
```java
		List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pfs = nonShuffledPartition(rblkID, features);
		List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pls = nonShuffledPartition(rblkID, labels);
		return new Result(pfs, pls);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			FileInputFormat.addInputPath(job, path);
			TextInputFormat informat = new TextInputFormat();
			informat.configure(job);
			InputSplit[] splits = informat.getSplits(job, 1);
			
			LinkedList<Cell> buffer = new LinkedList<>();
			LongWritable key = new LongWritable();
			Text value = new Text();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
						long row_offset = (key.getRowIndex()-1)*blen;
						long col_offset = (key.getColumnIndex()-1)*blen;
						long rows = value.getNumRows();
						long cols = value.getNumColumns();
						
						//bound check per block
						if( row_offset + rows < 1 || row_offset + rows > rlen || col_offset + cols<1 || col_offset + cols > clen )
						{
							throw new IOException("Matrix block ["+(row_offset+1)+":"+(row_offset+rows)+","+(col_offset+1)+":"+(col_offset+cols)+"] " +
									              "out of overall matrix range [1:"+rlen+",1:"+clen+"].");
						}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerRemoteSparkMapper.java`
#### Snippet
```java
		long col_offset = (key2.getColumnIndex()-1)*_blen;
		long rows = value2.getNumRows();
		long cols = value2.getNumColumns();
		
		//bound check per block
		if( row_offset + rows < 1 || row_offset + rows > _rlen || col_offset + cols<1 || col_offset + cols > _clen )
		{
			throw new IOException("Matrix block ["+(row_offset+1)+":"+(row_offset+rows)+","+(col_offset+1)+":"+(col_offset+cols)+"] " +
					              "out of overall matrix range [1:"+_rlen+",1:"+_clen+"].");
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
				for( Cell c : tmp )
				{
					sb.append(c.getRow());
					sb.append(' ');
					sb.append(c.getCol());
					sb.append(' ');
					sb.append(c.getValue());
					sb.append('\n');
					out.write( sb.toString() );
					sb.setLength(0);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ParWorker.java`
#### Snippet
```java
			_ec.setVariable(lVarName, indexVal);
			if (DMLScript.LINEAGE) {
				Lineage li = _ec.getLineage();
				li.set(lVarName, li.getOrCreate(new CPOperand(indexVal)));
			}
			
			// for each program block
			for (ProgramBlock pb : _childBlocks)
				pb.execute(_ec);
					
			_numIters++;
			
			if( _monitor )
				StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_ITER_T, time1.stop());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteDPParForSparkWorker.java`
#### Snippet
```java
		_workerID = ID;
		
		//initialize codegen class cache (before program parsing)
		for( Entry<String, byte[]> e : _clsMap.entrySet() )
			CodegenUtils.getClassSync(e.getKey(), e.getValue());
	
		//parse and setup parfor body program
		ParForBody body = ProgramConverter.parseParForBody(_prog, (int)_workerID, true);
		_childBlocks = body.getChildBlocks();
		_ec          = body.getEc();
		_resultVars  = body.getResultVariables();
		_numTasks    = 0;
		_numIters    = 0;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
					JobConf tmpJob = new JobConf(ConfigurationManager.getCachedJobConf());
					Path tmpPath = new Path(in.getFileName());
					FileInputFormat.addInputPath(tmpJob, tmpPath);
					TextInputFormat informat = new TextInputFormat();
					informat.configure(tmpJob);
					InputSplit[] splits = informat.getSplits(tmpJob, 1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long lFrom = _fromVal.getLongValue();
		long lTo = _toVal.getLongValue();
		long lIncr = _incrVal.getLongValue();
		int P = _numThreads;  // number of parallel workers
		long N = _numIter;    // total number of iterations
		long R = N;           // remaining number of iterations
		long K = -1;          // next _numThreads task sizes
		TaskType type = null; // type of iterations: range tasks (similar to run-length encoding) make only sense if taskSize>3
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
				if( type == TaskType.SET ) {
					//value based tasks
					for( long k=0; k<K && i<=lTo; k++, i+=lIncr )
						lTask.addIteration(new IntObject(i));
				}
				else {
					//determine end of task
					long to = Math.min( i+(K-1)*lIncr, lTo );
					
					//range based tasks
					lTask.addIteration(new IntObject(i));     //from
					lTask.addIteration(new IntObject(to));    //to
					lTask.addIteration(new IntObject(lIncr)); //increment
					i = to + lIncr;
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFixedsize.java`
#### Snippet
```java
			int corr = (lfnp1-- > 0)? 1:0; //correction for static partitioner
			
			// add <tasksize> iterations to task 
			// (last task might have less)
			if( type == TaskType.SET ) 
			{
				//value based tasks
				for( long j=0; j<_taskSize+corr && i<=lTo; j++, i+=lIncr )
					lTask.addIteration(new IntObject(i));
			}
			else 
			{
				//determine end of task
				long to = Math.min( i+(_taskSize-1+corr)*lIncr, lTo );
				
				//range based tasks
				lTask.addIteration(new IntObject(i));     //from
				lTask.addIteration(new IntObject(to));    //to
				lTask.addIteration(new IntObject(lIncr)); //increment
				
				i = to + lIncr;
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			int len = ws.getBody().size();
			for( int i=0; i<wpb.getChildBlocks().size() && i<len; i++ ) {
				ProgramBlock lpb = wpb.getChildBlocks().get(i);
				StatementBlock lsb = ws.getBody().get(i);
				node.addChild( rCreateAbstractOptNode(lsb, lpb, vars, false, hlMap, memo) );
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			fsb.getFromHops().resetVisitStatus();
			fsb.getToHops().resetVisitStatus();
			if( fsb.getIncrementHops()!=null )
				fsb.getIncrementHops().resetVisitStatus();
			node.addChilds( rCreateAbstractOptNodes( fsb.getFromHops(), vars, hlMap, memo ) );
			node.addChilds( rCreateAbstractOptNodes( fsb.getToHops(), vars, hlMap, memo ) );
			if( fsb.getIncrementHops()!=null )
				node.addChilds( rCreateAbstractOptNodes( fsb.getIncrementHops(), vars, hlMap, memo ) );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
		if( pbParent instanceof IfProgramBlock ) {
			IfProgramBlock ipb = (IfProgramBlock) pbParent;
			replaceProgramBlock( ipb.getChildBlocksIfBody(), pbOld, pbNew );
			replaceProgramBlock( ipb.getChildBlocksElseBody(), pbOld, pbNew );
		}
		else if( pbParent instanceof WhileProgramBlock ) {
			WhileProgramBlock wpb = (WhileProgramBlock) pbParent;
			replaceProgramBlock( wpb.getChildBlocks(), pbOld, pbNew );
		}
		else if( pbParent instanceof ForProgramBlock || pbParent instanceof ParForProgramBlock ) {
			ForProgramBlock fpb = (ForProgramBlock) pbParent;
			replaceProgramBlock( fpb.getChildBlocks(), pbOld, pbNew );
		}
		else if( pbParent instanceof FunctionProgramBlock ) {
			FunctionProgramBlock fpb = (FunctionProgramBlock) pbParent;
			replaceProgramBlock( fpb.getChildBlocks(), pbOld, pbNew );
		}
		else
			throw new DMLRuntimeException("Optimizer doesn't support "+pbParent.getClass().getName());
		
		//update repository
		hlMap.replaceMapping(pbNew, n);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreePlanChecker.java`
#### Snippet
```java
			for( int i=0; i<wpb.getChildBlocks().size(); i++ ) {
				ProgramBlock pbc = wpb.getChildBlocks().get(i);
				StatementBlock sbc = wstmt.getBody().get(i);
				checkProgramCorrectness(pbc, sbc, fnStack);
			}
			checkLinksProgramStatementBlock(wpb, wsb);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerConstrained.java`
#### Snippet
```java
			if( rIsAccessByIterationVariable(pn, moVarname, pfpb.getIterVar()) &&
			   ((moDpf==PartitionFormat.ROW_WISE && mo.getNumRows()==_N ) ||
				(moDpf==PartitionFormat.COLUMN_WISE && mo.getNumColumns()==_N) ||
				(moDpf._dpf==PDataPartitionFormat.ROW_BLOCK_WISE_N && mo.getNumRows()<=_N*moDpf._N)||
				(moDpf._dpf==PDataPartitionFormat.COLUMN_BLOCK_WISE_N && mo.getNumColumns()<=_N*moDpf._N)) )
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
						Program prog = pfpb.getProgram();
						ForProgramBlock fpb = ProgramConverter.createShallowCopyForProgramBlock(pfpb, prog);

						//replace parfor with for, and update objectmapping
						OptTreeConverter.replaceProgramBlock(n, sub, pfpb, fpb, _plan.getAbstractPlanMapping());
						//update link to statement block
						fpb.setStatementBlock(pfsb);
							
						//update node
						sub.setNodeType(NodeType.FOR);
						sub.setK(1);
						
						count++;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/ProgramRecompiler.java`
#### Snippet
```java
			int len = ws.getBody().size(); //robustness for potentially added problem blocks
			for( int i=0; i<wpb.getChildBlocks().size() && i<len; i++ )
			{
				ProgramBlock lpb = wpb.getChildBlocks().get(i);
				StatementBlock lsb = ws.getBody().get(i);
				rFindAndRecompileIndexingHOP(lsb,lpb,var,ec, force);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
		if( _disabled )
			return; // do nothing
		
		//check if parfor exists
		if( !_pfstats.containsKey(id) )
			_pfstats.put(id, new HashMap<Stat,LinkedList<Double>>());
		HashMap<Stat,LinkedList<Double>> allstats = _pfstats.get(id);
		
		//check if stat type exists
		if( !allstats.containsKey(type) )
			allstats.put(type, new LinkedList<Double>());
		LinkedList<Double> stats = allstats.get(type);
		
		//add new stat
		stats.addLast(s);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
			st.reset( value ); //reset tokenizer
			long row1 = st.nextLong();
			long row2 = st.nextLong();
			
			int id = (int)row1/blen;
			if( !map.containsKey(id) )
				map.put(id, new HashMap<Long,Long>());
			
			map.get(id).put(row1, row2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/DenseBlockLDRB.java`
#### Snippet
```java
				double[] other = db.valuesAt(bix);
				int blen = db.blockSize(bix) * db._odims[0];
				int bix2 = (int)(globalPos / bsize);
				int off2 = (int)(globalPos % bsize);
				int blen2 = size(bix2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		for( int i=0; i<_size; i++ ) {
			if( _values[i] == 0)
				throw new RuntimeException("The values array should not contain zeros."
						+ " The " + i + "th value is "+_values[i]);
		}

		//5. a capacity that is no larger than nnz times the resize factor
		int capacity = _values.length;
		if( capacity > nnz*RESIZE_FACTOR1 ) {
			throw new RuntimeException("Capacity is larger than the nnz times a resize factor."
					+ " Current size: "+capacity+ ", while Expected size:"+nnz*RESIZE_FACTOR1);
		}

		return true;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		long nnz = 0;
		for(int i=rl; i<ru; i++)
			if( !isEmpty(i) ) {
				int start = internPosFIndexGTE(i, cl);
				int end = internPosFIndexGTE(i, cu);
				nnz += (start!=-1) ? (end-start) : 0;
			}
		return nnz;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		int start = internPosFIndexGTE(r,cl);
		if( start < 0 ) //nothing to delete 
			return;

		int len = size(r);
		int end = internPosFIndexGTE(r, cu);
		if( end < 0 ) //delete all remaining
			end = start+len;
		
		//overlapping array copy (shift rhs values left)
		System.arraycopy(_rindexes, end, _rindexes, start, _size-end);
		System.arraycopy(_cindexes, end, _cindexes, start, _size-end);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		int pos = pos(r);
		int len = size(r);
		
		SparseRowVector row = new SparseRowVector(len);
		System.arraycopy(_cindexes, pos, row.indexes(), 0, len);
		System.arraycopy(_values, pos, row.values(), 0, len);
		row.setSize(len);
		
		return row;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		int pos = pos(r);
		int len = size(r);
		
		//search for existing col index in [pos,pos+len)
		int index = Arrays.binarySearch(_cindexes, pos, pos+len, c);
		if( index >= 0  )
			return (index < pos+len) ? index : -1;
		
		//search lt col index (see binary search)
		index = Math.abs( index+1 );
		return (index-1 >= pos) ? index-1 : -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		int pos = pos(r);
		int len = size(r);
		
		//search for existing col index
		int index = Arrays.binarySearch(_cindexes, pos, pos+len, c);
		if( index >= 0  )
			return (index < pos+len) ? index : -1;
		
		//search gt col index (see binary search)
		index = Math.abs( index+1 );
		return (index < pos+len) ? index : -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		int pos = pos(r);
		int len = size(r);
		
		//search for existing col index
		int index = Arrays.binarySearch(_cindexes, pos, pos+len, c);
		if( index >= 0  )
			return (index+1 < pos+len) ? index+1 : -1;
		
		//search gt col index (see binary search)
		index = Math.abs( index+1 );
		return (index < pos+len) ? index : -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		System.arraycopy(_rindexes, ix, _rindexes, ix+1, _size-ix);
		System.arraycopy(_cindexes, ix, _cindexes, ix+1, _size-ix);
		System.arraycopy(_values, ix, _values, ix+1, _size-ix);
		
		//insert new value
		insert(ix, r, c, v);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
		int lsize = _size+vlen;
		if( _values.length < lsize )
			resize(lsize);
		int index = internPosFIndexGT(r, cl);
		int index2 = (index>0)?index:pos(r+1);
		shiftRightByN(index2, vlen);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
		if( _values.length < nnz )
			resize(nnz);
		
		//step 3: insert and overwrite index range
		//total shift can be negative or positive and w/ internal skew
		
		//step 3a: forward pass: compact (delete index range)
		int pos = pos(rl);
		for( int r=rl; r<ru; r++ ) {
			int rpos = pos(r);
			int rlen = size(r);
			_ptr[r] = pos;
			for( int k=rpos; k<rpos+rlen; k++ )
				if( _indexes[k]<cl || cu<=_indexes[k] ) {
					_indexes[pos] = _indexes[k];
					_values[pos++] = _values[k];
				}
		}
		shiftLeftByN(pos(ru), pos(ru)-pos);
		decrPtr(ru, pos(ru)-pos);
		
		//step 3b: backward pass: merge (insert index range)
		int tshift1 = nnz - _size; //always non-negative
		if( v == null || tshift1==0 ) //early abort
			return;
		shiftRightByN(pos(ru), tshift1);
		incrPtr(ru, tshift1);
		pos = pos(ru)-1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCSR.java`
#### Snippet
```java
			int rpos = pos(r);
			int rlen = size(r) - tshift1;
			//copy lhs right
			int k = -1;
			for( k=rpos+rlen-1; k>=rpos && _indexes[k]>=cu; k-- ) {
				_indexes[pos] = _indexes[k];
				_values[pos--] = _values[k];
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
		final int[] colIdx = new int[nRowCol];
		final double[] vals = new double[nRowCol];
		int nnz = nRowCol;
		
		for(int i = 0; i < nRowCol; i++){
			rowPtr[i] = i;
			colIdx[i] = i;
			vals[i] = 1;
		}
		rowPtr[nRowCol] = nRowCol; // add last index for row pointers.
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/data/SparseRowVector.java`
#### Snippet
```java
		if( size == 0 ) return -1;
		
		//search for existing col index
		int index = Arrays.binarySearch(indexes, 0, size, col);
		if( index >= 0 )
			return (index < size) ? index : -1;
		
		//search lt col index (see binary search)
		index = Math.abs( index+1 );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		if(_nRow == 0) {
			ensureAllocateMeta();
			_coldata = new Array[_schema.length];
			for(int j = 0; j < _schema.length; j++) {
				_coldata[j] = ArrayFactory.allocate(_schema[j], 1);
				_coldata[j].set(0, row[j]);
			}
		}
		else {
			for(int j = 0; j < row.length; j++)
				_coldata[j].append(row[j]);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
		final ValueType ta = a.getValueType();
		final ValueType tb = b.getValueType();
		final ValueType tc = ValueType.getHighestCommonType(ta, tb);

		Array<C> ac = (Array<C>) (ta != tc ? a.changeType(tc) : a);
		Array<C> bc = (Array<C>) (tb != tc ? b.changeType(tc) : b);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BitSetArray.java`
#### Snippet
```java
		retBS.set(0, this._size - 1, this);
		if(other instanceof OptionalArray) {
			retBS.set(this._size, endSize - 1, ((OptionalArray<Boolean>) other)._a);
			return OptionalArray.appendOther((OptionalArray<Boolean>) other, retBS);
		}
		else {
			retBS.set(this._size, endSize - 1, other);
			return retBS;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
		BitSet ret = new BitSet(size());
		for(int i = 0; i < size(); i++) {
			if(_data[i] != 0 && _data[i] != 1)
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret.set(i, _data[i] == 0 ? false : true);
		}
		return new BitSetArray(ret, size());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
		boolean[] ret = new boolean[size()];
		for(int i = 0; i < size(); i++) {
			if(_data[i] != 0 && _data[i] != 1)
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder(_data.length * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
		boolean[] ret = new boolean[size()];
		for(int i = 0; i < size(); i++) {
			if(_data[i] < 0 || _data[i] > 1)
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameFromMatrixBlock.java`
#### Snippet
```java
				int bimin = Math.min(bi + blocksizeIJ, m);
				int bjmin = Math.min(bj + blocksizeIJ, n);
				for(int i = bi, aix = bi * n; i < bimin; i++, aix += n)
					for(int j = bj; j < bjmin; j++)
						c[j][i] = a[aix + j];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameFromMatrixBlock.java`
#### Snippet
```java
				int bimin = Math.min(bi + blocksizeIJ, m);
				int bjmin = Math.min(bj + blocksizeIJ, n);
				for(int i = bi; i < bimin; i++) {
					double[] avals = a.values(i);
					int apos = a.pos(i);
					for(int j = bj; j < bjmin; j++)
						c[j][i] = avals[apos + j];
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameFromMatrixBlock.java`
#### Snippet
```java
				int bimin = Math.min(bi + blocksizeIJ, m);
				int bjmin = Math.min(bj + blocksizeIJ, n);
				for(int i = bi, aix = bi * n; i < bimin; i++, aix += n)
					for(int j = bj; j < bjmin; j++)
						c[j].set(i, a[aix + j]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w= cm1.w + 1;
				double d=in2-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, d/w);
				double t1=cm1.w/w*d;
				double lt1=t1*d;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w = cm1.w + 1;
				double d=in2-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, d/w);
				double t1=cm1.w/w*d;
				double t2=-1/cm1.w;
				double lt1=t1*d;
				double lt2=Math.pow(t1, 3)*(1.0-Math.pow(t2, 2));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double f2=1.0/w;
				_buff2.set(cm1.m2);
				_buff2=(KahanObject) _plus.execute(_buff2, lt1);
				_buff3.set(cm1.m3);
				_buff3=(KahanObject) _plus.execute(_buff3, lt2-3*cm1.m2._sum*f2*d);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				cm1.m4=(KahanObject) _plus.execute(cm1.m4, 6*cm1.m2._sum*Math.pow(-f2*d, 2) + lt3-4*cm1.m3._sum*f2*d);
				cm1.m2.set(_buff2);
				cm1.m3.set(_buff3);
				cm1.w=w;
				break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w = cm1.w + w2;
				double d=in2-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, w2*d/w);
				double t1=cm1.w*w2/w*d;
				double lt1=t1*d;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w = cm1.w + w2;
				double d=in2-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, w2*d/w);
				double t1=cm1.w*w2/w*d;
				double t2=-1/cm1.w;
				double lt1=t1*d;
				double lt2=Math.pow(t1, 3)*(1/Math.pow(w2, 2)-Math.pow(t2, 2));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double f2=w2/w;
				_buff2.set(cm1.m2);
				_buff2=(KahanObject) _plus.execute(_buff2, lt1);
				_buff3.set(cm1.m3);
				_buff3=(KahanObject) _plus.execute(_buff3, lt2-3*cm1.m2._sum*f2*d);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w = cm1.w + cm2.w;
				double d=cm2.mean._sum-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, cm2.w*d/w);
				double t1=cm1.w*cm2.w/w*d;
				double lt1=t1*d;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double w = cm1.w + cm2.w;
				double d=cm2.mean._sum-cm1.mean._sum;
				cm1.mean=(KahanObject) _plus.execute(cm1.mean, cm2.w*d/w);
				double t1=cm1.w*cm2.w/w*d;
				double t2=-1/cm1.w;
				double lt1=t1*d;
				double lt2=Math.pow(t1, 3)*(1/Math.pow(cm2.w, 2)-Math.pow(t2, 2));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CM.java`
#### Snippet
```java
				double f1=cm1.w/w;
				double f2=cm2.w/w;
				_buff2.set(cm1.m2);
				_buff2=(KahanObject) _plus.execute(_buff2, cm2.m2._sum, cm2.m2._correction);
				_buff2=(KahanObject) _plus.execute(_buff2, lt1);
				_buff3.set(cm1.m3);
				_buff3=(KahanObject) _plus.execute(_buff3, cm2.m3._sum, cm2.m3._correction);
				_buff3=(KahanObject) _plus.execute(_buff3, 3*(-f2*cm1.m2._sum+f1*cm2.m2._sum)*d + lt2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/functionobjects/CTable.java`
#### Snippet
```java
		if ( Double.isNaN(v1) || Double.isNaN(v2) || Double.isNaN(w) ) {
			return;
		}
		
		// safe casts to long for consistent behavior with indexing
		long row = UtilFunctions.toLong( v1 );
		long col = UtilFunctions.toLong( v2 );
		
		// skip this entry as it does not fall within specified output dimensions
		if( ignoreZeros && row == 0 && col == 0 ) {
			return;
		}
		
		//check for incorrect ctable inputs
		if( row <= 0 || col <= 0 ) {
			throw new DMLRuntimeException("Erroneous input while computing the contingency table (one of the value <= zero): "+v1+" "+v2);
		} 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateTernaryFEDInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand out = new CPOperand(parts[4]);
			int numThreads = Integer.parseInt(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CentralMomentCPInstruction.java`
#### Snippet
```java
		InstructionUtils.checkNumFields(str, 4, 5); //w/o opcode
		CPOperand in1 = new CPOperand(parts[1]); //data
		CPOperand in2 = new CPOperand(parts[2]); //scalar
		CPOperand in3 = (parts.length==5) ? null : new CPOperand(parts[3]); //weights
		CPOperand out = new CPOperand(parts[parts.length-2]);
		int numThreads = Integer.parseInt(parts[parts.length-1]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ComputationCPInstruction.java`
#### Snippet
```java
		if( DMLScript.getGlobalExecMode() == ExecMode.SINGLE_NODE
			&& !CacheableData.isCachingActive() )
			return true;
		double memIn1 = (in1 != null) ? in1.getInMemorySize() : 0;
		double memIn2 = (in2 != null) ? in2.getInMemorySize() : 0;
		double memReq = out.isInSparseFormat() ? 
			MatrixBlock.estimateSizeDenseInMemory(out.getNumRows(), out.getNumColumns()) :
			MatrixBlock.estimateSizeSparseInMemory(out.getNumRows(), out.getNumColumns(), out.getSparsity());
		//guarded if mem requirements smaller than input sizes
		return ( memReq < memIn1 + memIn2
			+ OptimizerUtils.SAFE_REP_CHANGE_THRES ); //8MB
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CtableCPInstruction.java`
#### Snippet
```java
		boolean isExpand = opcode.equalsIgnoreCase("ctableexpand");
		
		//handle operands
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand in3 = new CPOperand(parts[3]);
		
		//handle known dimension information
		String[] dim1Fields = parts[4].split(Instruction.LITERAL_PREFIX);
		String[] dim2Fields = parts[5].split(Instruction.LITERAL_PREFIX);

		CPOperand out = new CPOperand(parts[6]);
		boolean ignoreZeros = Boolean.parseBoolean(parts[7]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CtableFEDInstruction.java`
#### Snippet
```java
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand in3 = new CPOperand(parts[3]);
		// handle known dimension information
		String[] dim1Fields = parts[4].split(Instruction.LITERAL_PREFIX);
		String[] dim2Fields = parts[5].split(Instruction.LITERAL_PREFIX);
		CPOperand out = new CPOperand(parts[6]);
		boolean ignoreZeros = Boolean.parseBoolean(parts[7]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			int missing; // number of missing params (row & cols or dims)
			CPOperand rows = null, cols = null, dims = null;
			if(s.length == 12) {
				missing = 1;
				rows = new CPOperand(s[1]);
				cols = new CPOperand(s[2]);
			}
			else {
				missing = 2;
				dims = new CPOperand(s[1]);
			}
			int blen = Integer.parseInt(s[4 - missing]);
			double sparsity = !s[7 - missing].contains(Lop.VARIABLE_NAME_PLACEHOLDER) ? Double
				.parseDouble(s[7 - missing]) : -1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand out = new CPOperand(parts[14]);
		
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[2]));
			stride.add(new CPOperand(parts[3]));
			padding.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			input_shape.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			filter_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			CPOperand out = new CPOperand(parts[14]);

			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[2]));
			stride.add(new CPOperand(parts[3]));
			padding.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			input_shape.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			filter_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[2]));
			stride.add(new CPOperand(parts[3]));
			padding.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			input_shape.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			filter_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand out = new CPOperand(parts[15]);
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[3]));
			stride.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			CPOperand out = new CPOperand(parts[15]);

			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[3]));
			stride.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[3]));
			stride.add(new CPOperand(parts[4]));
			padding.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			input_shape.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			filter_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			CPOperand in = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand out = new CPOperand(parts[16]);

			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[4]));
			stride.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			padding.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			input_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
			filter_shape.add(new CPOperand(parts[15]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand out = new CPOperand(parts[16]);
		
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[4]));
			stride.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			padding.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			input_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
			filter_shape.add(new CPOperand(parts[15]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
			ArrayList<CPOperand> stride = new ArrayList<>();
			ArrayList<CPOperand> padding = new ArrayList<>();
			ArrayList<CPOperand> input_shape = new ArrayList<>();
			ArrayList<CPOperand> filter_shape = new ArrayList<>();
			stride.add(new CPOperand(parts[4]));
			stride.add(new CPOperand(parts[5]));
			padding.add(new CPOperand(parts[6]));
			padding.add(new CPOperand(parts[7]));
			input_shape.add(new CPOperand(parts[8]));
			input_shape.add(new CPOperand(parts[9]));
			input_shape.add(new CPOperand(parts[10]));
			input_shape.add(new CPOperand(parts[11]));
			filter_shape.add(new CPOperand(parts[12]));
			filter_shape.add(new CPOperand(parts[13]));
			filter_shape.add(new CPOperand(parts[14]));
			filter_shape.add(new CPOperand(parts[15]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			InstructionUtils.checkNumFields(parts, 13);
			CPOperand in1 = new CPOperand(parts[1]); // image
			CPOperand in2 = new CPOperand(parts[2]); // scale
			CPOperand in3 = new CPOperand(parts[3]); // bias
			CPOperand in4 = new CPOperand(parts[4]); // runningMean
			CPOperand in5 = new CPOperand(parts[5]); // runningVar
			CPOperand in6 = new CPOperand(parts[6]); // mode
			CPOperand in7 = new CPOperand(parts[7]); // epsilon
			CPOperand in8 = new CPOperand(parts[8]); // exponentialAverageFactor
			CPOperand out = new CPOperand(parts[9]);  // ret
			CPOperand out2 = new CPOperand(parts[10]); // retRunningMean
			CPOperand out3 = new CPOperand(parts[11]); // retRunningVar
			CPOperand out4 = new CPOperand(parts[12]); // resultSaveMean
			CPOperand out5 = new CPOperand(parts[13]); // resultSaveInvVariance
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand in4 = new CPOperand(parts[4]);
			CPOperand in5 = new CPOperand(parts[5]);
			CPOperand in6 = new CPOperand(parts[6]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
			InstructionUtils.checkNumFields(parts, 9);
			CPOperand in1 = new CPOperand(parts[1]); // image
			CPOperand in2 = new CPOperand(parts[2]); // dout
			CPOperand in3 = new CPOperand(parts[3]); // scale
			CPOperand in4 = new CPOperand(parts[4]); // epsilon
			CPOperand in5 = new CPOperand(parts[5]); // resultSaveMean
			CPOperand in6 = new CPOperand(parts[6]); // resultSaveInvVariance
			CPOperand out = new CPOperand(parts[7]);  // dX
			CPOperand out2 = new CPOperand(parts[8]); // dScale
			CPOperand out3 = new CPOperand(parts[9]); // dBias
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		int pad_h = getScalarInput(ec, _padding, 0);
		int pad_w = getScalarInput(ec, _padding, 1);
		int stride_h = getScalarInput(ec, _stride, 0);
		int stride_w = getScalarInput(ec, _stride, 1);

		int N = getScalarInput(ec, _input_shape, 0);
		int C = getScalarInput(ec, _input_shape, 1);
		int H = getScalarInput(ec, _input_shape, 2);
		int W = getScalarInput(ec, _input_shape, 3);

		int K = getScalarInput(ec, _filter_shape, 0);
		
		int R = getScalarInput(ec, _filter_shape, 2);
		int S = getScalarInput(ec, _filter_shape, 3);
		int P = (int) DnnUtils.getP(H, R, stride_h, pad_h);
		int Q = (int) DnnUtils.getQ(W, S, stride_w, pad_w);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/DnnSPInstruction.java`
#### Snippet
```java
			int C = getScalarInput(ec, _input_shape, 1);
			int H = getScalarInput(ec, _input_shape, 2);
			int W = getScalarInput(ec, _input_shape, 3);

			int K = getScalarInput(ec, _filter_shape, 0);
			int R = getScalarInput(ec, _filter_shape, 2);
			int S = getScalarInput(ec, _filter_shape, 3);
			int P = (int) DnnUtils.getP(H, R, stride_h, pad_h);
			int Q = (int) DnnUtils.getQ(W, S, stride_w, pad_w);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/IndexingCPInstruction.java`
#### Snippet
```java
				CPOperand in, rl, ru, cl, cu, out;
				in = new CPOperand(parts[1]);
				rl = new CPOperand(parts[2]);
				ru = new CPOperand(parts[3]);
				cl = new CPOperand(parts[4]);
				cu = new CPOperand(parts[5]);
				out = new CPOperand(parts[6]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/IndexingCPInstruction.java`
#### Snippet
```java
				CPOperand lhsInput, rhsInput, rl, ru, cl, cu, out;
				lhsInput = new CPOperand(parts[1]);
				rhsInput = new CPOperand(parts[2]);
				rl = new CPOperand(parts[3]);
				ru = new CPOperand(parts[4]);
				cl = new CPOperand(parts[5]);
				cu = new CPOperand(parts[6]);
				out = new CPOperand(parts[7]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
					switch (valueType) {
						case INT64:     so = new IntObject(Long.parseLong(value)); break;
						case FP64:  	so = new DoubleObject(Double.parseDouble(value)); break;
						case BOOLEAN: 	so = new BooleanObject(Boolean.parseBoolean(value)); break;
						case STRING:  	so = new StringObject(value); break;
						default:
							throw new DMLRuntimeException("Unable to parse valuetype " + valueType);
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MultiReturnBuiltinCPInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			outputs.add ( new CPOperand(parts[2], ValueType.FP64, DataType.MATRIX) );
			outputs.add ( new CPOperand(parts[3], ValueType.FP64, DataType.MATRIX) );
			
			return new MultiReturnBuiltinCPInstruction(null, in1, outputs, opcode, str);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MultiReturnBuiltinCPInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			
			// one input and three outputs
			outputs.add ( new CPOperand(parts[2], ValueType.FP64, DataType.MATRIX) );
			outputs.add ( new CPOperand(parts[3], ValueType.FP64, DataType.MATRIX) );
			outputs.add ( new CPOperand(parts[4], ValueType.FP64, DataType.MATRIX) );
			
			return new MultiReturnBuiltinCPInstruction(null, in1, outputs, opcode, str);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/PMMJCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		InstructionUtils.checkNumFields ( parts, 5 );
		String opcode = parts[0];
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand in3 = new CPOperand(parts[3]);
		CPOperand out = new CPOperand(parts[4]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
		LinkedHashMap<String, String> paramMap = new LinkedHashMap<>();
		// all parameters are of form <name=value>
		String[] parts;
		for(int i = 1; i <= params.length - 2; i++) {
			parts = params[i].split(Lop.NAME_VALUE_SEPARATOR);
			paramMap.put(parts[0], parts[1]);
		}

		return paramMap;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
			String fnStr = paramsMap.get("fn");
			if(fnStr == null)
				throw new DMLRuntimeException("Function parameter is missing in groupedAggregate.");
			if(fnStr.equalsIgnoreCase("centralmoment")) {
				if(paramsMap.get("order") == null)
					throw new DMLRuntimeException(
						"Mandatory \"order\" must be specified when fn=\"centralmoment\" in groupedAggregate.");
			}

			Operator op = InstructionUtils.parseGroupedAggOperator(fnStr, paramsMap.get("order"));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantilePickCPInstruction.java`
#### Snippet
```java
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand out = new CPOperand(parts[3]);
			OperationTypes ptype = OperationTypes.valueOf(parts[4]);
			boolean inmem = Boolean.parseBoolean(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantileSortCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(instr);

		out.split(parts[parts.length-2]);

		switch(parts.length) {
			case 4:
				in1.split(parts[1]);
				in2 = null;
				break;
			case 5:
				in1.split(parts[1]);
				in2.split(parts[2]);
				break;
			default:
				throw new DMLRuntimeException("Unexpected number of operands in the instruction: " + instr);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuantileSortCPInstruction.java`
#### Snippet
```java
		CPOperand in1 = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		CPOperand in2 = null;
		CPOperand out = new CPOperand("", ValueType.UNKNOWN, DataType.UNKNOWN);
		
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode = parts[0];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/QuaternaryCPInstruction.java`
#### Snippet
```java
			InstructionUtils.checkNumFields ( parts, 7 );
			
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand in4 = new CPOperand(parts[4]);
			CPOperand out = new CPOperand(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ReorgCPInstruction.java`
#### Snippet
```java
			InstructionUtils.checkNumFields(str, 5,6);
			in.split(parts[1]);
			out.split(parts[5]);
			CPOperand col = new CPOperand(parts[2]);
			CPOperand desc = new CPOperand(parts[3]);
			CPOperand ixret = new CPOperand(parts[4]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ReshapeCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		InstructionUtils.checkNumFields( parts, 6 );
		String opcode = parts[0];
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand in3 = new CPOperand(parts[3]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReshapeFEDInstruction.java`
#### Snippet
```java
		String opcode = parts[0];
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand in3 = new CPOperand(parts[3]);
		CPOperand in4 = new CPOperand(parts[4]);
		CPOperand in5 = new CPOperand(parts[5]);
		CPOperand out = new CPOperand(parts[6]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java
		CPOperand conn, user, pass, query, out;
		conn = new CPOperand(parts[1]);
		user = new CPOperand(parts[2]);
		pass = new CPOperand(parts[3]);
		query = new CPOperand(parts[4]);
		out = new CPOperand(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/TernaryCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		String opcode=parts[0];
		CPOperand operand1 = new CPOperand(parts[1]);
		CPOperand operand2 = new CPOperand(parts[2]);
		CPOperand operand3 = new CPOperand(parts[3]);
		CPOperand outOperand = new CPOperand(parts[4]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java
			AggregateUnaryOperator uaggop = InstructionUtils.parseBasicAggregateUnaryOperator(parts[1]);
			BinaryOperator bop = InstructionUtils.parseBinaryOperator(parts[2]);

			CPOperand in1 = new CPOperand(parts[3]);
			CPOperand in2 = new CPOperand(parts[4]);
			CPOperand out = new CPOperand(parts[5]);
					
			//derive aggregation operator from unary operator
			String aopcode = InstructionUtils.deriveAggregateOperatorOpcode(parts[1]);
			CorrectionLocationType corrLoc = InstructionUtils.deriveAggregateOperatorCorrectionLocation(parts[1]);
			AggregateOperator aop = InstructionUtils.parseAggregateOperator(aopcode, corrLoc.toString());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UnaryCPInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(instr);
		
		// first part is the opcode, last part is the output, middle parts are input operands
		String opcode = parts[0];
		out.split(parts[parts.length-1]);
		
		switch(parts.length) {
		case 3:
			in1.split(parts[1]);
			in2 = null;
			in3 = null;
			break;
		case 4:
			in1.split(parts[1]);
			in2.split(parts[2]);
			in3 = null;
			break;
		case 5:
			in1.split(parts[1]);
			in2.split(parts[2]);
			in3.split(parts[3]);
			break;
		default:
			throw new DMLRuntimeException("Unexpected number of operands in the instruction: " + instr);
		}
		return opcode;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateBinaryFEDInstruction.java`
#### Snippet
```java
		long callInstID = fr[fr.length - 1].getID();
		FederatedRequest frG = new FederatedRequest(RequestType.GET_VAR, callInstID);
		FederatedRequest frC = fedMap.cleanup(getTID(), callInstID);
		//execute federated operations and aggregate
		Future<FederatedResponse>[] ffr;
		if(frSliced != null)
			ffr = fedMap.execute(getTID(), frSliced, ArrayUtils.addAll(fr, frG, frC));
		else
			ffr = fedMap.execute(getTID(), ArrayUtils.addAll(fr, frG, frC));

		MatrixBlock ret;
		if ( aggAdd )
			ret = FederationUtils.aggAdd(ffr);
		else
			ret = FederationUtils.bind(ffr, false);
		ec.setMatrixOutput(output.getName(), ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateTernaryFEDInstruction.java`
#### Snippet
```java
			if(output.getDataType().isScalar()) {
				double sum = 0;
				for(Future<FederatedResponse> fr : tmp)
					try {
						sum += ((ScalarObject) fr.get().getData()[0]).getDoubleValue();
					}
					catch(Exception e) {
						throw new DMLRuntimeException("Federated Get data failed with exception on TernaryFedInstruction", e);
					}
				ec.setScalarOutput(output.getName(), new DoubleObject(sum));
			}
			else {
				throw new DMLRuntimeException("Not Implemented Federated Ternary Variation");
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
		if((map.getType() == FType.COL && aop.isColAggregate()) ||
			(map.getType() == FType.ROW && aop.isRowAggregate()))
			fr1 = new FederatedRequest(RequestType.PUT_VAR, id, new MatrixCharacteristics(-1, -1), in.getDataType());
		else
			fr1 = new FederatedRequest(RequestType.PUT_VAR, id, dc, in.getDataType());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/BinaryFEDInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(instr);
		InstructionUtils.checkNumFields(parts, 4);
		String opcode = parts[0];
		in1.split(parts[1]);
		in2.split(parts[2]);
		in3.split(parts[3]);
		out.split(parts[4]);
		return opcode;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CastFEDInstruction.java`
#### Snippet
```java
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr1.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
			FederatedData nf = new FederatedData(Types.DataType.MATRIX,
				om.getAddress(), om.getFilepath(), om.getVarID());
			newMap.add(Pair.of(pair.getKey(), nf));
		}
		out.setFedMapping(outMap);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CastFEDInstruction.java`
#### Snippet
```java
		out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr2.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
		for(Map.Entry<FederatedRange, FederatedData> pair : outMap.getMap()) {
			FederatedData om = pair.getValue();
			FederatedData nf = new FederatedData(Types.DataType.FRAME,
				om.getAddress(), om.getFilepath(), om.getVarID());
			newMap.add(Pair.of(pair.getKey(), nf));
		}
		ValueType[] schema = new ValueType[(int) mo1.getDataCharacteristics().getCols()];
		Arrays.fill(schema, ValueType.FP64);
		out.setSchema(schema);
		out.setFedMapping(outMap);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CentralMomentFEDInstruction.java`
#### Snippet
```java
		Optional<CM_COV_Object> res = globalCmobj.stream()
				.reduce((arg0, arg1) -> (CM_COV_Object) finalCm_op.fn.execute(arg0, arg1));
		try {
			ec.setScalarOutput(output.getName(), new DoubleObject(res.get().getRequiredResult(finalCm_op)));
		}
		catch (Exception e) {
			throw new DMLRuntimeException(e);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CovarianceFEDInstruction.java`
#### Snippet
```java
		Future<FederatedResponse>[] meanTmp1 = processMean(mo1, 0);
		Future<FederatedResponse>[] meanTmp2 = processMean(mo2, 1);

		ImmutableTriple<Double[], Double[], Double[]> res = getResponses(covTmp, meanTmp1, meanTmp2);

		double result = aggCov(res.left, res.middle, res.right, mo1.getFedMapping().getFederatedRanges());
		ec.setVariable(output.getName(), new DoubleObject(result));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType( str );
		InstructionUtils.checkNumFields(parts, 5);
		String opcode = parts[0];
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand out = new CPOperand(parts[3]);
		double init = Double.parseDouble(parts[4]);
		boolean broadcast = Boolean.parseBoolean(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		MatrixBlock a = new MatrixBlock(tmp.length, 1, 0.0);
		a.copy(1, a.getNumRows()-1, 0, 0,
			prod.unaryOperations(new UnaryOperator(Builtin.getBuiltinFnObject("ucumk+*")), new MatrixBlock())
				.slice(0, prod.getNumRows()-2), true);

		// compute  B11 = B11 + B12 ⊙ a
		MatrixBlock B = firstValues.slice(0, firstValues.getNumRows()-1,1, 1)
			.binaryOperations(InstructionUtils.parseBinaryOperator("*"), a, new MatrixBlock());
		return B.binaryOperationsInPlace(InstructionUtils.parseBinaryOperator("+"), firstValues.slice(0,firstValues.getNumRows()-1,0,0));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		MatrixBlock condition = new MatrixBlock((int) mo1.getNumRows(), (int) mo1.getNumColumns(), 1.0);
		MatrixBlock mb2 = new MatrixBlock((int) mo1.getNumRows(), (int) mo1.getNumColumns(), 0.0);

		for(int i = 0; i < scalingValues.getNumRows()-1; i++) {
			int step = (int) mo1.getFedMapping().getFederatedRanges()[i + 1].getBeginDims()[0];
			condition.setValue(step, 0, 0.0);
			mb2.setValue(step, 0, scalingValues.getValue(i + 1, 0));
		}

		MatrixObject cond = ExecutionContext.createMatrixObject(condition);
		long condID = FederationUtils.getNextFedDataID();
		ec.setVariable(String.valueOf(condID), cond);

		MatrixObject mo2 = ExecutionContext.createMatrixObject(mb2);
		long varID2 = FederationUtils.getNextFedDataID();
		ec.setVariable(String.valueOf(varID2), mo2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		String ternaryInstString = InstructionUtils.constructTernaryString(instString, opCond, input1, op2, output);

		FederatedRequest[] fr1 = mo1.getFedMapping().broadcastSliced(cond, false);
		FederatedRequest[] fr2 = mo1.getFedMapping().broadcastSliced(mo2, false);
		FederatedRequest fr3 = FederationUtils.callInstruction(ternaryInstString, output,
			new CPOperand[] {input1, opCond, op2}, new long[] {mo1.getFedMapping().getID(), fr1[0].getID(), fr2[0].getID()});
		//TODO perf no need to execute here, we can piggyback the requests onto the final cumagg
		mo1.getFedMapping().execute(getTID(), true, fr1, fr2, fr3);

		out.setFedMapping(mo1.getFedMapping().copyWithNewID(fr3.getID()));

		ec.removeVariable(opCond.getName());
		ec.removeVariable(op2.getName());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		MatrixBlock mb2 = new MatrixBlock((int) mo1.getNumRows(), (int) mo1.getNumColumns(), init);
		for(int i = 1; i < scalingValues.getNumRows(); i++) {
			int step = (int) mo1.getFedMapping().getFederatedRanges()[i].getBeginDims()[0];
			mb2.copy(step, step, 0, (int)(mo1.getNumColumns()-1), scalingValues.slice(i, i), true);
		}

		MatrixObject mo2 = ExecutionContext.createMatrixObject(mb2);
		long varID2 = FederationUtils.getNextFedDataID();
		ec.setVariable(String.valueOf(varID2), mo2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java
			FederatedRange range = e.getKey();
			long rs = range.getBeginDims()[0], re = range.getEndDims()[0],
				cs = range.getBeginDims()[1], ce = range.getEndDims()[1];
			long rsn = (ixrange.rowStart >= rs) ? (ixrange.rowStart - rs) : 0;
			long ren = (ixrange.rowEnd >= rs && ixrange.rowEnd < re) ? (ixrange.rowEnd - rs) : (re - rs - 1);
			long csn = (ixrange.colStart >= cs) ? (ixrange.colStart - cs) : 0;
			long cen = (ixrange.colEnd >= cs && ixrange.colEnd < ce) ? (ixrange.colEnd - cs) : (ce - cs - 1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
		if(addresses.getLength() * 2 != ranges.getLength())
			throw new DMLRuntimeException("Federated read needs twice the amount of addresses as ranges " + "(begin and end): addresses=" + addresses.getLength() + " ranges=" + ranges.getLength());

		//check for duplicate addresses (would lead to overwrite with common variable names)
		// TODO relax requirement by using different execution contexts per federated data?
		Set<String> addCheck = new HashSet<>();
		for( Data dat : addresses.getData() )
			if( dat instanceof StringObject ) {
				String address = ((StringObject) dat).getStringValue();
				if(addCheck.contains(address))
					LOG.warn("Federated data contains address duplicates: " + addresses);
				addCheck.add(address);
			}

		Types.DataType fedDataType;
		if(type.equalsIgnoreCase(FED_MATRIX_IDENTIFIER))
			fedDataType = Types.DataType.MATRIX;
		else if(type.equalsIgnoreCase(FED_FRAME_IDENTIFIER))
			fedDataType = Types.DataType.FRAME;
		else
			throw new DMLRuntimeException("type \"" + type + "\" non valid federated type");

		long[] usedDims = new long[] {0, 0};
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
				if(DMLScript.FED_STATISTICS)
					// register the federated worker for federated statistics creation
					FederatedStatistics.registerFedWorker(host, port);

				// get beginning and end of data ranges
				List<Data> rangesData = ranges.getData();
				Data beginData = rangesData.get(i * 2);
				Data endData = rangesData.get(i * 2 + 1);
				if(beginData.getDataType() != Types.DataType.LIST || endData.getDataType() != Types.DataType.LIST)
					throw new DMLRuntimeException("Federated read ranges (lower, upper) have to be lists of dimensions");
				List<Data> beginDimsData = ((ListObject) beginData).getData();
				List<Data> endDimsData = ((ListObject) endData).getData();

				// fill begin and end dims
				long[] beginDims = new long[beginDimsData.size()];
				long[] endDims = new long[beginDims.length];
				for(int d = 0; d < beginDims.length; d++) {
					beginDims[d] = ((ScalarObject) beginDimsData.get(d)).getLongValue();
					endDims[d] = ((ScalarObject) endDimsData.get(d)).getLongValue();
				}

				usedDims[0] = Math.max(usedDims[0], endDims[0]);
				usedDims[1] = Math.max(usedDims[1], endDims[1]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
			URL address = new URL("http://" + input);
			String host = address.getHost();
			if(host.length() == 0)
				throw new IllegalArgumentException("Missing Host name for federated address");
			// The current system does not support ipv6, only ipv4.
			// TODO: Support IPV6 address for Federated communication
			String ipRegex = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$";
			if(host.matches("^\\d+\\.\\d+\\.\\d+\\.\\d+$") && !host.matches(ipRegex))
				throw new IllegalArgumentException("Input Host address looks like an IP address but is outside range");
			int port = address.getPort();
			if(port == -1)
				port = DMLConfig.DEFAULT_FEDERATED_PORT;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
				long[] beginDims = range.getBeginDims();
				long[] endDims = range.getEndDims();
				long[] dims = output.getDataCharacteristics().getDims();
				for(int i = 0; i < dims.length; i++) {
					dims[i] = endDims[i] - beginDims[i];
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMChainFEDInstruction.java`
#### Snippet
```java
				FederatedRequest fr3 = new FederatedRequest(RequestType.GET_VAR, fr2.getID());
				FederatedRequest fr4 = mo1.getFedMapping().cleanup(getTID(), fr2.getID());

				//execute federated operations and aggregate
				Future<FederatedResponse>[] tmp = mo1.getFedMapping().execute(getTID(), fr1, fr2, fr3, fr4);
				MatrixBlock ret = FederationUtils.aggAdd(tmp);
				ec.setMatrixOutput(output.getName(), ret);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
			BinaryOperator plus = InstructionUtils.parseBinaryOperator("+");
			BinaryOperator greater = InstructionUtils.parseBinaryOperator(">");
			s = colSums.get(0);
			for(int i = 1; i < colSums.size(); i++)
				s = s.binaryOperationsInPlace(plus, colSums.get(i));
			s = s.binaryOperationsInPlace(greater, new MatrixBlock(s.getNumRows(), s.getNumColumns(), 0.0));
			select = MatrixLineagePair.of(ExecutionContext.createMatrixObject(s), null);

			long varID = FederationUtils.getNextFedDataID();
			ec.setVariable(String.valueOf(varID), select.getMO());
			params.put("select", String.valueOf(varID));
			// construct new string
			String[] oldString = InstructionUtils.getInstructionParts(instString);
			String[] newString = new String[oldString.length + 1];
			newString[2] = "select=" + varID;
			System.arraycopy(oldString, 0, newString, 0, 2);
			System.arraycopy(oldString, 2, newString, 3, newString.length - 3);
			instString = instString.replace(InstructionUtils.concatOperands(oldString),
				InstructionUtils.concatOperands(newString));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		if(select == null) {
			FederatedRequest fr1 = FederationUtils.callInstruction(instString,
				output,
				new CPOperand[] {getTargetOperand()},
				new long[] {mo.getFedMapping().getID()});
			mo.getFedMapping().execute(getTID(), true, fr1);
			out.setFedMapping(mo.getFedMapping().copyWithNewID(fr1.getID()));
		}
		else if(!isNotAligned) {
			// construct commands: broadcast , fed rmempty, clean broadcast
			FederatedRequest[] fr1 = mo.getFedMapping().broadcastSliced(select, !marginRow);
			FederatedRequest fr2 = FederationUtils.callInstruction(instString,
				output,
				new CPOperand[] {getTargetOperand(),
					new CPOperand(params.get("select"), ValueType.FP64, DataType.MATRIX)},
				new long[] {mo.getFedMapping().getID(), fr1[0].getID()});

			// execute federated operations and set output
			mo.getFedMapping().execute(getTID(), true, fr1, fr2);
			out.setFedMapping(mo.getFedMapping().copyWithNewID(fr2.getID()));
		}
		else {
			// construct commands: broadcast , fed rmempty, clean broadcast
			FederatedRequest fr1 = mo.getFedMapping().broadcast(select);
			FederatedRequest fr2 = FederationUtils.callInstruction(instString,
				output,
				new CPOperand[] {getTargetOperand(),
					new CPOperand(params.get("select"), ValueType.FP64, DataType.MATRIX)},
				new long[] {mo.getFedMapping().getID(), fr1.getID()});

			// execute federated operations and set output
			mo.getFedMapping().execute(getTID(), true, fr1, fr2);
			out.setFedMapping(mo.getFedMapping().copyWithNewID(fr2.getID()));
		}

		// new ranges
		Map<FederatedRange, int[]> dcs = new HashMap<>();
		Map<FederatedRange, int[]> finalDcs1 = dcs;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
			out.getFedMapping().getFederatedRanges()[i].setBeginDim(0,
				(out.getFedMapping().getFederatedRanges()[i].getBeginDims()[0] == 0 ||
					i == 0) ? 0 : out.getFedMapping().getFederatedRanges()[i - 1].getEndDims()[0]);

			out.getFedMapping().getFederatedRanges()[i].setEndDim(0,
				out.getFedMapping().getFederatedRanges()[i].getBeginDims()[0] + newRange[0]);

			out.getFedMapping().getFederatedRanges()[i].setBeginDim(1,
				(out.getFedMapping().getFederatedRanges()[i].getBeginDims()[1] == 0 ||
					i == 0) ? 0 : out.getFedMapping().getFederatedRanges()[i - 1].getEndDims()[1]);

			out.getFedMapping().getFederatedRanges()[i].setEndDim(1,
				out.getFedMapping().getFederatedRanges()[i].getBeginDims()[1] + newRange[1]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
		List<double[]> minMax = new ArrayList<>();
		newFedMap.mapParallel(varID, (range, data) -> {
			try {
				FederatedResponse response = data.executeFederatedOperation(new FederatedRequest(
					FederatedRequest.RequestType.EXEC_UDF, -1,
					new QuantilePickFEDInstruction.MinMax(data.getVarID()))).get();
				if(!response.isSuccessful())
					response.throwExceptionFromResponse();
				double[] rangeMinMax = (double[]) response.getData()[0];
				minMax.add(rangeMinMax);

				return null;
			}
			catch(Exception e) {
				throw new DMLRuntimeException(e);
			}
		});
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryFEDInstruction.java`
#### Snippet
```java
		QuaternaryOperator qop = (QuaternaryOperator) instr.getOperator();
		if(qop.wtype1 != null)
			return QuaternaryWSLossFEDInstruction.parseInstruction(instr);
		else if(qop.wtype2 != null)
			return QuaternaryWSigmoidFEDInstruction.parseInstruction(instr);
		else if(qop.wtype3 != null)
			return QuaternaryWDivMMFEDInstruction.parseInstruction(instr);
		else if(qop.wtype4 != null)
			return QuaternaryWCeMMFEDInstruction.parseInstruction(instr);
		else if(qop.wtype5 != null)
			return QuaternaryWUMMFEDInstruction.parseInstruction(instr);
		// unreachable
		return null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
				if(U.isFederated(FType.ROW) && fedMap.isAligned(U.getFedMapping(), AlignType.ROW)) {
					// U federated and aligned
					varNewIn[1] = U.getFedMapping().getID();
				}
				else {
					FederatedRequest[] tmpFrS = fedMap.broadcastSliced(U, false);
					varNewIn[1] = tmpFrS[0].getID();
					frSliced.add(tmpFrS);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
				if(V.isFederated() && fedMap.isAligned(V.getFedMapping(), AlignType.COL, AlignType.COL_T)) {
					// V federated and aligned
					varNewIn[2] = V.getFedMapping().getID();
				}
				else {
					FederatedRequest[] tmpFrS = fedMap.broadcastSliced(V, true);
					varNewIn[2] = tmpFrS[0].getID();
					frSliced.add(tmpFrS);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSigmoidFEDInstruction.java`
#### Snippet
```java
				if(U.isFederated(FType.ROW) && fedMap.isAligned(U.getFedMapping(), AlignType.ROW)) {
					// U federated and aligned
					varNewIn[1] = U.getFedMapping().getID();
				}
				else {
					frSliced = fedMap.broadcastSliced(U, false);
					varNewIn[1] = frSliced[0].getID();
				}
				frB = fedMap.broadcast(V);
				varNewIn[2] = frB.getID();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSigmoidFEDInstruction.java`
#### Snippet
```java
				frB = fedMap.broadcast(U);
				varNewIn[1] = frB.getID();
				if(V.isFederated() && fedMap.isAligned(V.getFedMapping(), AlignType.COL, AlignType.COL_T)) {
					// V federated and aligned
					varNewIn[2] = V.getFedMapping().getID();
				}
				else {
					frSliced = fedMap.broadcastSliced(V, true);
					varNewIn[2] = frSliced[0].getID();
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSigmoidFEDInstruction.java`
#### Snippet
```java
			FederatedRequest frComp = FederationUtils.callInstruction(instString,
				output, new CPOperand[] {input1, input2, input3}, varNewIn);

			ArrayList<FederatedRequest> frC = new ArrayList<>();
			FederatedRequest[] frAll = ArrayUtils.addAll(new FederatedRequest[]{frB, frComp},
				frC.toArray(new FederatedRequest[0]));

			// execute federated instructions
			if(frSliced == null)
				fedMap.execute(getTID(), true, frAll);
			else
				fedMap.execute(getTID(), true, frSliced, frAll);

			// derive output federated mapping
			MatrixObject out = ec.getMatrixObject(output);
			out.setFedMapping(fedMap.copyWithNewID(frComp.getID()));
			setOutputDataCharacteristics(X, U.getMO(), V.getMO(), ec);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReblockFEDInstruction.java`
#### Snippet
```java
		CacheableData<?> obj = ec.getCacheableData(input1.getName());
		DataCharacteristics mc = ec.getDataCharacteristics(input1.getName());
		DataCharacteristics mcOut = ec.getDataCharacteristics(output.getName());
		mcOut.set(mc.getRows(), mc.getCols(), blen, mc.getNonZeros());

		//get the source format from the meta data
		MetaDataFormat iimd = (MetaDataFormat) obj.getMetaData();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
			long id = FederationUtils.getNextFedDataID();
			FederatedRequest fr = new FederatedRequest(FederatedRequest.RequestType.PUT_VAR, id, new MatrixCharacteristics(-1, -1), mo1.getDataType());

			FederatedRequest fr1 = FederationUtils.callInstruction(instString, output, id, new CPOperand[] {input1},
				new long[] {mo1.getFedMapping().getID()}, isSpark ? Types.ExecType.SPARK : Types.ExecType.CP, true);
			mo1.getFedMapping().execute(getTID(), true, fr, fr1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
		Class<?> scla = inst.getOperatorClass().getSuperclass();
		if(((scla == SpoofCellwise.class || scla == SpoofMultiAggregate.class || scla == SpoofOuterProduct.class)
			&& SpoofFEDInstruction.isFederated(ec, inst.getInputs(), scla))
			|| (scla == SpoofRowwise.class && SpoofFEDInstruction.isFederated(ec, FType.ROW, inst.getInputs(), scla))) {
			return SpoofFEDInstruction.parseInstruction(inst);
		}
		return null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/ArithmeticBinaryGPUInstruction.java`
#### Snippet
```java
		String[] parts = InstructionUtils.getInstructionPartsWithValueType(str);
		InstructionUtils.checkNumFields ( parts, 3 );
		
		String opcode = parts[0];
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand in2 = new CPOperand(parts[2]);
		CPOperand out = new CPOperand(parts[3]);
		
		DataType dt1 = in1.getDataType();
		DataType dt2 = in2.getDataType();
		DataType dt3 = out.getDataType();
	 
		Operator operator = (dt1 != dt2) ?
				InstructionUtils.parseScalarBinaryOperator(opcode, (dt1 == DataType.SCALAR)) : 
				InstructionUtils.parseBinaryOperator(opcode);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		GPUStatistics.incrementNoOfExecutedGPUInst();
		MatrixObject image = getMatrixInputForGPUInstruction(ec, _input1.getName());
		MatrixObject scale = getMatrixInputForGPUInstruction(ec, _input2.getName());
		MatrixObject bias = getMatrixInputForGPUInstruction(ec, _input3.getName());
		MatrixObject runningMean = getMatrixInputForGPUInstruction(ec, _input4.getName());
		MatrixObject runningVar = getMatrixInputForGPUInstruction(ec, _input5.getName());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			double exponentialAverageFactor = 1-ec.getScalarInput(_input8).getDoubleValue();
			MatrixObject retRunningMean = getDenseMatrixOutputForGPUInstruction(ec, _output2.getName(), runningMean.getNumRows(), runningMean.getNumColumns());
			MatrixObject retRunningVar = getDenseMatrixOutputForGPUInstruction(ec, _output3.getName(), runningVar.getNumRows(), runningVar.getNumColumns());
			MatrixObject resultSaveMean = getDenseMatrixOutputForGPUInstruction(ec, _output4.getName(), runningMean.getNumRows(), runningMean.getNumColumns());
			MatrixObject resultSaveInvVariance = getDenseMatrixOutputForGPUInstruction(ec, _output5.getName(), runningVar.getNumRows(), runningVar.getNumColumns());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		ec.releaseMatrixInputForGPUInstruction(_input1.getName());
		ec.releaseMatrixInputForGPUInstruction(_input2.getName());
		ec.releaseMatrixInputForGPUInstruction(_input3.getName());
		ec.releaseMatrixInputForGPUInstruction(_input4.getName());
		ec.releaseMatrixInputForGPUInstruction(_input5.getName());
		ec.releaseMatrixOutputForGPUInstruction(_output.getName());
		ec.releaseMatrixOutputForGPUInstruction(_output2.getName());
		ec.releaseMatrixOutputForGPUInstruction(_output3.getName());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		GPUStatistics.incrementNoOfExecutedGPUInst();
		GPUContext gCtx = ec.getGPUContext(0);
		String instructionName = getExtendedOpcode();
		
		MatrixObject out0 = getMatrixInputForGPUInstruction(ec, _input4.getName());
		int M = toInt(out0.getNumColumns()); // hiddenSize .. since out0: (N, M)
		Pointer out0Pointer =  LibMatrixCUDA.getDensePointer(gCtx, out0, instructionName);
		
		MatrixObject W = getMatrixInputForGPUInstruction(ec, _input2.getName());
		MatrixObject bias = getMatrixInputForGPUInstruction(ec, _input3.getName());
		long numRowsW = W.getNumRows();
		int D = toInt(numRowsW) - M; // since W:(D+M, 4M) ... numFeatures 
		Pointer sysdsWPointer = LibMatrixCuDNN.getDensePointerForCuDNN(gCtx, W, instructionName, D+M, 4*M);
		Pointer sysdsBiasPointer = LibMatrixCuDNN.getDensePointerForCuDNN(gCtx, bias, instructionName, 1, 4*M);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
		MatrixObject X = getMatrixInputForGPUInstruction(ec, _input1.getName());
		Pointer xPointer = LibMatrixCUDA.getDensePointer(gCtx, X, instructionName); 
		int N = toInt(X.getNumRows()); // batchSize .. since X:(N, T*D)
		long numColsX = X.getNumColumns();
		int T = toInt(numColsX/ D); // since X:(N, T*D) ... seqLength
		Pointer cudnnInput = gCtx.allocate(instructionName, (N*T*D)*LibMatrixCUDA.sizeOfDataType, false);
		LibMatrixCUDA.getCudaKernels(gCtx).launchKernel("prepare_lstm_input",
				ExecutionConfig.getConfigForSimpleVectorOperations(N*T*D),
				xPointer, cudnnInput, N, D, T*D, N*T*D);
		ec.releaseMatrixInputForGPUInstruction(_input1.getName());
		
		Pointer c0Pointer = LibMatrixCUDA.getDensePointer(gCtx, getMatrixInputForGPUInstruction(ec, _input5.getName()), instructionName);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			MatrixObject image = getMatrixInputForGPUInstruction(ec, _input1.getName());
			MatrixObject filter = getMatrixInputForGPUInstruction(ec, _input2.getName());

			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, K * P * Q);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
			if(image.getNumRows() != N || image.getNumColumns() != C*H*W) 
				throw new DMLRuntimeException("Incorrect dimensions for image in conv2d");
			if(filter.getNumRows() != K || filter.getNumColumns() != C*R*S) 
				throw new DMLRuntimeException("Incorrect dimensions for filter in conv2d");
			
			MatrixObject out = getDenseMatrixOutputForGPUInstruction(ec, _output.getName(), N, K * P * Q);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		long rlen1 = in1.getNumRows();
		long clen1 = in1.getNumColumns();
		long rlen2 = in2.getNumRows();
		long clen2 = in2.getNumColumns();

		// Assume ordinary binary op
		long rlen = rlen1;
		long clen = clen1;

		// Outer binary op ( [100,1] + [1,100] or [100,100] + [100,1]
		if (rlen1 != rlen2 || clen1 != clen2){
			rlen = rlen1 > rlen2 ? rlen1 : rlen2;
			clen = clen1 > clen2 ? clen1 : clen2;
		}

		ec.setMetaData(_output.getName(), (int)rlen, (int)clen);
		
		BinaryOperator bop = (BinaryOperator) _optr;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java
		CPOperand in1 = new CPOperand(parts[1]);
		CPOperand rows = new CPOperand(parts[2]);
		CPOperand cols = new CPOperand(parts[3]);
		//TODO handle dims for tensors parts[4]
		CPOperand byRow = new CPOperand(parts[5]);
		CPOperand out = new CPOperand(parts[6]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/ScalarMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		GPUStatistics.incrementNoOfExecutedGPUInst();
		
		CPOperand mat = ( _input1.getDataType() == DataType.MATRIX ) ? _input1 : _input2;
		CPOperand scalar = ( _input1.getDataType() == DataType.MATRIX ) ? _input2 : _input1;
		MatrixObject in1 = getMatrixInputForGPUInstruction(ec, mat.getName());
		ScalarObject constant = ec.getScalarInput(scalar);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/ScalarMatrixBuiltinGPUInstruction.java`
#### Snippet
```java
		CPOperand mat = (_input1.getDataType() == DataType.MATRIX) ? _input1 : _input2;
		CPOperand scalar = (_input1.getDataType() == DataType.MATRIX) ? _input2 : _input1;
		MatrixObject in1 = getMatrixInputForGPUInstruction(ec, mat.getName());
		ScalarObject constant = ec.getScalarInput(scalar);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
		if (CnnzArray[0] != -1) {
			C.nnz = CnnzArray[0];
		} else {
			int baseArray[] = { 0 };
			cudaMemcpy(Pointer.to(CnnzArray), C.rowPtr.withByteOffset(getIntSizeOf(m)), getIntSizeOf(1),
					cudaMemcpyDeviceToHost);
			cudaMemcpy(Pointer.to(baseArray), C.rowPtr, getIntSizeOf(1), cudaMemcpyDeviceToHost);
			C.nnz = CnnzArray[0] - baseArray[0];
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		if(LOG.isTraceEnabled()) {
			LOG.trace("GPU : allocateSparseMatrixOnDevice, on " + this + ", GPUContext=" + getGPUContext());
		}
		if(isAllocated()) 
			throw new DMLRuntimeException("Internal error - trying to allocated sparse matrix to a GPUObject that is already allocated");
		long rows = mat.getNumRows();
		long nnz = mat.getNnz();
		if(rows <= 0)
			throw new DMLRuntimeException("Internal error - invalid number of rows when allocating sparse matrix");
		if(nnz < 0)
			throw new DMLRuntimeException("Internal error - invalid number of non zeroes when allocating a sparse matrix");
		CSRPointer tmp = CSRPointer.allocateEmpty(getGPUContext(), nnz, rows, initialize);
		setSparseMatrixCudaPointer(tmp);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGAlignedSPInstruction.java`
#### Snippet
```java
		DataCharacteristics mc1 = sec.getDataCharacteristics(input1.getName());
		
		JavaPairRDD<MatrixIndexes,MatrixBlock> in1 = sec.getBinaryMatrixBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> in2 = sec.getBinaryMatrixBlockRDDHandleForVariable( input2.getName() );
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
		DataCharacteristics mcIn = sec.getDataCharacteristics(input1.getName());
		DataCharacteristics mcOut = sec.getDataCharacteristics(output.getName());

		mcOut.set(mcIn.getRows(), mcIn.getCols(), _blen);

		//check for in-memory reblock (w/ lazy spark context, potential for latency reduction)
		if( Recompiler.checkCPReblock(sec, input1.getName()) ) {
			if( input1.getDataType().isMatrix() || input1.getDataType().isFrame() ) {
				Recompiler.executeInMemoryReblock(sec, input1.getName(), output.getName());
			}
			Statistics.decrementNoOfExecutedSPInst();
			return;
		}
		
		//execute matrix/frame csvreblock 
		JavaPairRDD<?,?> out = null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		SparkExecutionContext sec = (SparkExecutionContext)ec;
		String opcode = getOpcode();
		
		//get indexing range
		long rl = ec.getScalarInput(rowLower).getLongValue();
		long ru = ec.getScalarInput(rowUpper).getLongValue();
		long cl = ec.getScalarInput(colLower).getLongValue();
		long cu = ec.getScalarInput(colUpper).getLongValue();
		IndexRange ixrange = new IndexRange(rl, ru, cl, cu);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
			DataCharacteristics mcOut = sec.getDataCharacteristics(output.getName());
			DataCharacteristics mcLeft = ec.getDataCharacteristics(input1.getName());
			mcOut.set(mcLeft.getRows(), mcLeft.getCols(), mcLeft.getBlocksize(), mcLeft.getBlocksize());
			checkValidOutputDimensions(mcOut);
			
			//note: always frame rhs, scalars are preprocessed via cast to 1x1 frame
			DataCharacteristics mcRight = ec.getDataCharacteristics(input2.getName());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			mcOut.setDimension(mcIn.getRows() - ((omap != null) ? omap.getNumRmRows() : 0),
				(int) encoder.getNumOutCols());
			Broadcast<MultiColumnEncoder> bmeta = sec.getSparkContext().broadcast(encoder);
			Broadcast<TfOffsetMap> bomap = (omap != null) ? sec.getSparkContext().broadcast(omap) : null;

			// execute transform apply
			JavaPairRDD<Long, FrameBlock> tmp = in.mapToPair(new RDDTransformApplyFunction(bmeta, bomap));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			if( isRed )
				InstructionUtils.checkNumFields ( parts, 8 );
			else
				InstructionUtils.checkNumFields ( parts, 6 );
			
			CPOperand in1 = new CPOperand(parts[1]);
			CPOperand in2 = new CPOperand(parts[2]);
			CPOperand in3 = new CPOperand(parts[3]);
			CPOperand in4 = new CPOperand(parts[4]);
			CPOperand out = new CPOperand(parts[5]);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			CSVReblockSPInstruction csvInstruction = null;
			boolean hasHeader = false;
			String delim = ",";
			boolean fill = false;
			double fillValue = 0;
			Set<String> naStrings = null;
			if(mo.getFileFormatProperties() instanceof FileFormatPropertiesCSV
			   && mo.getFileFormatProperties() != null )
			{
				FileFormatPropertiesCSV props = (FileFormatPropertiesCSV) mo.getFileFormatProperties();
				hasHeader = props.hasHeader();
				delim = props.getDelim();
				fill = props.isFill();
				fillValue = props.getFillValue();
				naStrings = props.getNAStrings();
			}

			csvInstruction = new CSVReblockSPInstruction(null, input1, output, mcOut.getBlocksize(), mcOut.getBlocksize(), hasHeader, delim, fill, fillValue, "csvrblk", instString, naStrings);
			csvInstruction.processInstruction(sec);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			String delim = IOUtilFunctions.LIBSVM_DELIM;
			String indexDelim = IOUtilFunctions.LIBSVM_INDEX_DELIM;
			if(mo.getFileFormatProperties() instanceof FileFormatPropertiesLIBSVM && mo
				.getFileFormatProperties() != null) {
				FileFormatPropertiesLIBSVM props = (FileFormatPropertiesLIBSVM) mo.getFileFormatProperties();
				delim = props.getDelim();
				indexDelim = props.getIndexDelim();
			}

			LIBSVMReblockSPInstruction libsvmInstruction = new LIBSVMReblockSPInstruction(null, input1, output,
				mcOut.getBlocksize(), mcOut.getBlocksize(), "libsvmblk", delim, indexDelim, instString);
			libsvmInstruction.processInstruction(sec);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RmmSPInstruction.java`
#### Snippet
```java
				long i = ixIn.getRowIndex();
				long k = ixIn.getColumnIndex();
				for( long j=1; j<=numBlocks; j++ ) {
					TripleIndexes tmptix = new TripleIndexes(i, j, k);
					ret.add( new Tuple2<>(tmptix, blkIn) );
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/Tsmm2SPInstruction.java`
#### Snippet
```java
				MatrixBlock mbin2 = _pb.getValue().getBlock( //lookup broadcast block
						(int)(_type.isLeft()?ixin.getRowIndex():1), 
						(int)(_type.isLeft()?1:ixin.getColumnIndex()));
				MatrixBlock mbin2t = transpose(mbin2, new MatrixBlock()); //prep for transpose rewrite mm
				
				MatrixBlock out2 = OperationsOnMatrixValues.matMult( //mm
						_type.isLeft() ? mbin2t : mbin, _type.isLeft() ? mbin : mbin2t, new MatrixBlock(), _op);
				MatrixIndexes ixout2 = _type.isLeft() ? new MatrixIndexes(2,1) : new MatrixIndexes(1,2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ZipmmSPInstruction.java`
#### Snippet
```java
			JavaRDD<MatrixBlock> out = in1.join(in2).values()     // join over original indexes
				.map(new ZipMultiplyFunction(_tRewrite));  // compute block multiplications, incl t(y)

			//single-block aggregation (guaranteed by zipmm blocksize constraint)
			MatrixBlock out2 = RDDAggregateUtils.sumStable(out);

			//final transpose of result (for t(t(y)%*%X))), if transpose rewrite
			if(_tRewrite) {
				ReorgOperator rop = new ReorgOperator(SwapIndex.getSwapIndexFnObject());
				out2 = out2.reorgOperations(rop, new MatrixBlock(), 0, 0, 0);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/PartitionedBlock.java`
#### Snippet
```java
		int nrblks = getNumRowBlocks();
		int ncblks = getNumColumnBlocks();
		if( rowIndex <= 0 || rowIndex > nrblks || colIndex <= 0 || colIndex > ncblks ) {
			throw new DMLRuntimeException("Block indexes ["+rowIndex+","+colIndex+"] out of range ["+nrblks+","+ncblks+"]");
		}
		
		//get the requested frame/matrix block
		int rix = rowIndex - 1;
		int cix = colIndex - 1;
		int ix = rix*ncblks+cix - _offset;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes,MatrixBlock> input = in;

		//fast path without, general case with shuffle
		if( mcIn.getCols()>mcIn.getBlocksize() ) {
			//create row partitioned matrix
			input = input
					.flatMapToPair(new SliceBinaryBlockToRowsFunction(mcIn.getBlocksize()))
					.groupByKey()
					.mapToPair(new ConcatenateBlocksFunction(mcIn.getCols(), mcIn.getBlocksize()));
		}

		//sort if required (on blocks/rows)
		if( strict ) {
			input = input.sortByKey(true);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
				long rix = UtilFunctions.computeBlockIndex(rowix, _blen);
				int pos = UtilFunctions.computeCellInBlock(rowix, _blen);

				//create new blocks for entire row
				if( ix[0] == null || ix[0].getRowIndex() != rix ) {
					if( ix[0] !=null )
						flushBlocksToList(ix, mb, ret);
					long len = UtilFunctions.computeBlockSize(_rlen, rix, _blen);
					createBlocks(rowix, (int)len, ix, mb);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
				if( ix[0] == null || ix[0].getRowIndex() != rix ) {
					if( ix[0] !=null )
						flushBlocksToList(ix, mb, ret);
					long len = UtilFunctions.computeBlockSize(_rlen, rix, _blen);
					createBlocks(rowix, (int)len, ix, mb);
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
			long rix = UtilFunctions.computeBlockIndex(rowix, _blen);
			int ncblks = (int)Math.ceil((double)_clen/_blen);

			//create all column blocks (assume dense since csv is dense text format)
			for( int cix=1; cix<=ncblks; cix++ ) {
				int lclen = UtilFunctions.computeBlockSize(_clen, cix, _blen);
				ix[cix-1] = new MatrixIndexes(rix, cix);
				mb[cix-1] = new MatrixBlock(lrlen, lclen, _sparse, (int)(lrlen*lclen*_sparsity));
				mb[cix-1].allocateBlock();
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		JavaPairRDD<ValueIndexPair, Double> dvals = val
			.flatMapToPair(new ExtractDoubleValuesWithIndexFunction(blen));
		
		//sort (creates sorted range per partition)
		long hdfsBlocksize = InfrastructureAnalyzer.getHDFSBlockSize();
		int numPartitions = (int)Math.ceil(((double)rlen*16)/hdfsBlocksize);
		JavaRDD<ValueIndexPair> sdvals = dvals
			.sortByKey(new IndexComparator(asc), true, numPartitions)
			.keys(); //workaround for index comparator
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
				long valix = val._2 + 1;
				long rix = UtilFunctions.computeBlockIndex(valix, _blen);
				int pos = UtilFunctions.computeCellInBlock(valix, _blen);
				
				if( ix == null || ix.getRowIndex() != rix )
				{
					if( ix !=null )
						ret.add(new Tuple2<>(ix,mb));
					long len = UtilFunctions.computeBlockSize(_rlen, rix, _blen);
					ix = new MatrixIndexes(rix,1);
					mb = new MatrixBlock((int)len, 1, false);	
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
				long rix = UtilFunctions.computeBlockIndex(valix, _blen);
				int pos = UtilFunctions.computeCellInBlock(valix, _blen);
				
				if( ix == null || ix.getRowIndex() != rix )
				{
					if( ix !=null )
						ret.add(new Tuple2<>(ix,mb));
					long len = UtilFunctions.computeBlockSize(_rlen, rix, _blen);
					ix = new MatrixIndexes(rix,1);
					mb = new MatrixBlock((int)len, 1, false);	
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
					long valix = (long) mbTargetIndex.getValue(_currPos, 0);
					long rix = UtilFunctions.computeBlockIndex(valix, _blen);
					int pos = UtilFunctions.computeCellInBlock(valix, _blen);
					int len = UtilFunctions.computeBlockSize(_rlen, rix, _blen);
					MatrixIndexes lix = new MatrixIndexes(rix,ixmap.getColumnIndex());
					MatrixBlock tmp = data.slice(_currPos, _currPos);
					_currPos++;
					
					//handle end of block situations
					if( _currPos == data.getNumRows() ){
						_currBlk = null;
					}
					
					return new Tuple2<>(lix, new RowMatrixBlock(len, pos, tmp));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSVParallel.java`
#### Snippet
```java
			ArrayList<CountRowsTask> tasks = new ArrayList<>();
			for( int i=0; i<splits.length; i++ )
				tasks.add(new CountRowsTask(splits[i], informat, job, _props.hasHeader()&& i==0, ncol));
			List<Future<Integer>> cret = pool.invokeAll(tasks);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
			List<Future<Object>> rt = pool.invokeAll(tasks);	
			pool.shutdown();
			
			//check for exceptions 
			for( Future<Object> task : rt )
				task.get();
			
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONL.java`
#### Snippet
```java
		JobConf job = new JobConf(ConfigurationManager.getCachedJobConf());
		Path path = new Path( fname );

		//if the file already exists on HDFS, remove it.
		HDFSTool.deleteFileIfExistOnHDFS( fname );

		//validity check frame dimensions
		if( src.getNumRows() != rlen || src.getNumColumns() != clen ) {
			throw new IOException("Frame dimensions mismatch with metadata: " +
				src.getNumRows()+"x"+src.getNumColumns()+" vs "+rlen+"x"+clen+".");
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderBinaryBlock.java`
#### Snippet
```java
					if( value.isEmptyBlock(false) )
						continue;
					
					int row_offset = (int)(key.getRowIndex()-1)*blen;
					int col_offset = (int)(key.getColumnIndex()-1)*blen;
					
					int rows = value.getNumRows();
					int cols = value.getNumColumns();
					
					//bound check per block
					if( row_offset + rows < 0 || row_offset + rows > rlen || col_offset + cols<0 || col_offset + cols > clen )
					{
						throw new IOException("Matrix block ["+(row_offset+1)+":"+(row_offset+rows)+","+(col_offset+1)+":"+(col_offset+cols)+"] " +
								              "out of overall matrix range [1:"+rlen+",1:"+clen+"].");
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderBinaryBlock.java`
#### Snippet
```java
					int row_offset = (int)(key.getRowIndex()-1)*blen;
					int col_offset = (int)(key.getColumnIndex()-1)*blen;
					int rows = value.getNumRows();
					int cols = value.getNumColumns();
					
					//bound check per block
					if( row_offset + rows < 0 || row_offset + rows > rlen || col_offset + cols<0 || col_offset + cols > clen )
					{
						throw new IOException("Matrix block ["+(row_offset+1)+":"+(row_offset+rows)+","+(col_offset+1)+":"+(col_offset+cols)+"] " +
								              "out of overall matrix range [1:"+rlen+",1:"+clen+"].");
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderHDF5.java`
#### Snippet
```java
		MatrixBlock ret = null;
		if(rlen >= 0 && clen >= 0) //otherwise allocated on read
			ret = createOutputMatrixBlock(rlen, clen, (int) rlen, estnnz, true, false);

		//prepare file access
		JobConf job = new JobConf(ConfigurationManager.getCachedJobConf());
		Path path = new Path(fname);
		FileSystem fs = IOUtilFunctions.getFileSystem(path, job);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderHDF5.java`
#### Snippet
```java
		ArrayList<Path> files = new ArrayList<>();
		if(fs.getFileStatus(path).isDirectory()) {
			for(FileStatus stat : fs.listStatus(path, IOUtilFunctions.hiddenFileFilter))
				files.add(stat.getPath());
			Collections.sort(files);
		}
		else
			files.add(path);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderHDF5Parallel.java`
#### Snippet
```java
		_job = new JobConf(ConfigurationManager.getCachedJobConf());

		Path path = new Path(fname);
		FileSystem fs = IOUtilFunctions.getFileSystem(path, _job);

		FileInputFormat.addInputPath(_job, path);
		TextInputFormat informat = new TextInputFormat();
		informat.configure(_job);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCSVParallel.java`
#### Snippet
```java
		if((rlen != -1 && _rLen != rlen) || (clen != -1 && _cLen != clen)) {
			String msg = "Read matrix dimensions differ from meta data: [" + _rLen + "x" + _cLen + "] vs. [" + rlen
				+ "x" + clen + "].";
			if(rlen < _rLen || clen < _cLen) {
				// a) specified matrix dimensions too small
				throw new DMLRuntimeException(msg);
			}
			else {
				// b) specified matrix dimensions too large -> padding and warning
				LOG.warn(msg);
				_rLen = (int) rlen;
				_cLen = (int) clen;
			}
		}

		// allocate target matrix block based on given size;
		// need to allocate sparse as well since lock-free insert into target
		long estnnz2 = (estnnz < 0) ? (long) _rLen * _cLen : estnnz;
		return createOutputMatrixBlock(_rLen, _cLen, blen, estnnz2, true, true);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/TensorReaderBinaryBlock.java`
#### Snippet
```java
			try (SequenceFile.Reader reader = new SequenceFile.Reader(job, SequenceFile.Reader.file(lpath))) {
				while (reader.next(key, value)) {
					if (value.isEmpty(false))
						continue;
					int[] lower = new int[dims.length];
					int[] upper = new int[lower.length];
					UtilFunctions.getBlockBounds(key, value.getLongDims(), blen, lower, upper);
					ret.copy(lower, upper, value);
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
			int blklen = (int) Math.ceil((double) rlen / blen / numThreads) * blen;
			for (int i = 0; i < numThreads & i * blklen < rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i * blklen, Math.min((i + 1) * blklen, rlen), blen));
			}
			
			//wait until all tasks have been executed
			List<Future<Object>> rt = pool.invokeAll(tasks);
			pool.shutdown();
			
			//check for exceptions
			for (Future<Object> task : rt)
				task.get();
			
			// delete crc files if written to local file system
			if( fs instanceof LocalFileSystem ) {
				for (int i = 0; i < numThreads & i * blklen < rlen; i++)
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
							new Path(path, IOUtilFunctions.getPartFileName(i)));
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/WriterBinaryBlockParallel.java`
#### Snippet
```java
			for(int i=0; i<numThreads & i*blklen<rlen; i++) {
				Path newPath = new Path(path, IOUtilFunctions.getPartFileName(i));
				tasks.add(new WriteFileTask(newPath, job, fs, src, i*blklen, Math.min((i+1)*blklen, rlen), blen));
			}

			//wait until all tasks have been executed
			List<Future<Object>> rt = pool.invokeAll(tasks);	
			pool.shutdown();
			
			//check for exceptions 
			for( Future<Object> task : rt )
				task.get();
			
			// delete crc files if written to local file system
			if (fs instanceof LocalFileSystem) {
				for(int i=0; i<numThreads & i*blklen<rlen; i++) 
					IOUtilFunctions.deleteCrcFilesFromLocalFileSystem(fs,
						new Path(path, IOUtilFunctions.getPartFileName(i)));
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/WriterHDF5.java`
#### Snippet
```java
		if(src.getNumRows() != rlen || src.getNumColumns() != clen)
			throw new IOException("Matrix dimensions mismatch with metadata: " + src.getNumRows() + "x" + src
				.getNumColumns() + " vs " + rlen + "x" + clen + ".");
		if(rlen == 0 || clen == 0)
			throw new IOException(
				"Write of matrices with zero rows or columns not supported (" + rlen + "x" + clen + ").");

		//prepare file access
		JobConf job = new JobConf(ConfigurationManager.getCachedJobConf());
		Path path = new Path(fname);
		FileSystem fs = IOUtilFunctions.getFileSystem(path, job);

		//if the file already exists on HDFS, remove it.
		HDFSTool.deleteFileIfExistOnHDFS(fname);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java
		if( src.getNumRows() != rlen || src.getNumColumns() != clen ) {
			throw new IOException("Matrix dimensions mismatch with metadata: "+src.getNumRows()+"x"+src.getNumColumns()+" vs "+rlen+"x"+clen+".");
		}
				
		//prepare file access
		JobConf job = new JobConf(ConfigurationManager.getCachedJobConf());
		Path path = new Path( fname );
		FileSystem fs = IOUtilFunctions.getFileSystem(path, job);
		
		//if the file already exists on HDFS, remove it.
		HDFSTool.deleteFileIfExistOnHDFS( fname );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java
			if( sparse ) //SPARSE
			{			   
				Iterator<IJV> iter = src.getSparseBlockIterator(rl, ru);
				while( iter.hasNext() )
				{
					IJV cell = iter.next();

					sb.append(cell.getI()+1);
					sb.append(' ');
					sb.append(cell.getJ()+1);
					sb.append(' ');
					sb.append(cell.getV());
					sb.append('\n');
					br.write( sb.toString() ); //same as append
					sb.setLength(0); 
				}
			}
			else //DENSE
			{
				for( int i=rl; i<ru; i++ )
				{
					String rowIndex = Integer.toString(i+1);					
					for( int j=0; j<clen; j++ )
					{
						double lvalue = src.getValueDenseUnsafe(i, j);
						if( lvalue != 0 ) //for nnz
						{
							sb.append(rowIndex);
							sb.append(' ');
							sb.append(j+1);
							sb.append(' ');
							sb.append(lvalue);
							sb.append('\n');
							br.write( sb.toString() ); //same as append
							sb.setLength(0); 
						}
					}
				}
			}
	
			//handle empty result
			if ( src.isEmptyBlock(false) && rl==0 )
				br.write(IOUtilFunctions.EMPTY_TEXT_LINE);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCSV.java`
#### Snippet
```java
			OutputStream out = fs.create(tmp, true);
			out.write(sb.toString().getBytes());
			sb.setLength(0);
			
			// copy rest of the data from firstpart
			InputStream in = null;
			try {
				in = fs.open(firstpart);
				IOUtils.copyBytes(in, out, conf, true);
			} finally {
				IOUtilFunctions.closeSilently(in);
				IOUtilFunctions.closeSilently(out);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5RootObject.java`
#### Snippet
```java
		ByteBuffer bb = ByteBuffer.allocate(length);
		try {
			byte[] b = new byte[length];
			bufferedInputStream.reset();
			bufferedInputStream.skip(address);
			bufferedInputStream.read(b);
			bb.put(b);
		}
		catch(IOException e) {
			throw new H5RuntimeException(e);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/iogen/FrameGenerateReader.java`
#### Snippet
```java
		int rows = 0;
		String value;
		for(int fileNo = 0; fileNo < files.size(); fileNo++) {
			BufferedReader br = new BufferedReader(new InputStreamReader(fs.open(files.get(fileNo))));
			try {
				// Row Regular
				if(_props.getRowPattern().equals(CustomProperties.GRPattern.Regular)) {
					// TODO: check the file has header?
					while(br.readLine() != null)
						rows++;
				}
				// Row Irregular
				else {
					FastStringTokenizer st = new FastStringTokenizer(_props.getDelim());
					while((value = br.readLine()) != null) {
						st.reset(value);
						int row = st.nextInt();
						rows = Math.max(rows, row);
					}
					rows++;
				}
			}
			finally {
				IOUtilFunctions.closeSilently(br);
			}
		}
		return rows;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/iogen/FrameGenerateReader.java`
#### Snippet
```java
			String cellValue;
			fastStringTokenizerDelim.setNaStrings(_props.getNaStrings());

			// create record reader
			RecordReader<LongWritable, Text> reader = informat.getRecordReader(split, job, Reporter.NULL);
			LongWritable key = new LongWritable();
			Text value = new Text();
			int row = rl;
			int col = 0;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/iogen/RawRow.java`
#### Snippet
```java
			char ch = raw.charAt(i);
			if(ch == 'E' || ch == 'e' || ch == '+' || ch == '-') {
				sb.append(ch);
			}
			else if(!flagD && ch == '.') {
				sb.append(ch);
				flagD = true;
			}
			else if(Character.isDigit(ch))
				sb.append(ch);
			else
				break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/iogen/RawRow.java`
#### Snippet
```java
		if(result.getKey() != -1) {
			if(update) {
				for(int i = resultNumeric.getKey() - 1; i >= 0; i--) {
					if(numericPositions.get(i) >= result.getKey())
						numericReserved.set(i);
					else
						break;
				}

				for(int i = resultNumeric.getKey() + 1; i < numericPositions.size(); i++) {
					if(numericPositions.get(i) <= result.getKey() + result.getValue()) {
						numericReserved.set(i);
						numericLastIndex = i;
					}
					else
						break;
				}
				numericReserved.set(resultNumeric.getKey(), resultNumeric.getKey() + resultNumeric.getValue(), true);
				reserved.set(result.getKey(), result.getKey() + result.getValue(), true);
			}
			else {
				for(int i = resultNumeric.getKey() + 1; i < numericPositions.size(); i++) {
					if(numericPositions.get(i) <= result.getKey() + result.getValue()) {
						numericLastIndex = i;
					}
					else
						break;
				}
			}
			numericLastIndex = Math.max(numericLastIndex, resultNumeric.getKey() + resultNumeric.getValue());
			rawLastIndex = result.getKey() + result.getValue();
		}
		return result;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/iogen/RawRow.java`
#### Snippet
```java
		int[] start = {numericLastIndex, 0};
		int[] end = {size, numericLastIndex};
		int psize = (forward || rawLastIndex == 0) ? 1 : 2;

		for(int p = 0; p < psize; p++) {
			for(int i = start[p]; i < end[p]; ) {
				// skip all reserved indexes
				for(int j = i; j < end[p]; j++) {
					if(numericReserved.get(j))
						i++;
					else
						break;
				}
				sIndex = i;
				// Extract unreserved position
				for(int j = i; j < end[p]; j++) {
					if(!numericReserved.get(j))
						i++;
					else
						break;
				}
				eIndex = i;
				if(sIndex < eIndex)
					result.add(new Pair<>(sIndex, eIndex - 1));
			}
		}
		return result;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		ComputationCPInstruction cinst = inst instanceof ComputationCPInstruction ? (ComputationCPInstruction)inst : null;
		ComputationFEDInstruction cfinst = inst instanceof ComputationFEDInstruction ? (ComputationFEDInstruction)inst : null;
		ComputationSPInstruction cspinst = inst instanceof ComputationSPInstruction ? (ComputationSPInstruction)inst : null;
		GPUInstruction gpuinst = inst instanceof GPUInstruction ? (GPUInstruction)inst : null;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				MatrixObject mo = ec.getMatrixObject(((ComputationCPInstruction)inst).input1);
				long r = mo.getNumRows();
				long c = mo.getNumColumns();
				long nnz = mo.getNnz();
				double s = OptimizerUtils.getSparsity(r, c, nnz);
				boolean sparse = MatrixBlock.evalSparseFormatInMemory(r, c, nnz);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				MatrixObject mo1 = ec.getMatrixObject(((ComputationCPInstruction)inst).input1);
				MatrixObject mo2 = ec.getMatrixObject(((ComputationCPInstruction)inst).input2);
				long r1 = mo1.getNumRows();
				long c1 = mo1.getNumColumns();
				long nnz1 = mo1.getNnz();
				double s1 = OptimizerUtils.getSparsity(r1, c1, nnz1);
				boolean lsparse = MatrixBlock.evalSparseFormatInMemory(r1, c1, nnz1);
				long r2 = mo2.getNumRows();
				long c2 = mo2.getNumColumns();
				long nnz2 = mo2.getNnz();
				double s2 = OptimizerUtils.getSparsity(r2, c2, nnz2);
				boolean rsparse = MatrixBlock.evalSparseFormatInMemory(r2, c2, nnz2);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
			if (!fedinst.input1.isMatrix() || !fedinst.input2.isMatrix())
				return false;
			long c1 = ec.getMatrixObject(fedinst.input1).getNumColumns();
			long c2 = ec.getMatrixObject(fedinst.input2).getNumColumns();
			return(c1 == 1 || c2 == 1);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
		if (LineageCacheConfig.isTimeBased()) { 
			if (weightedQueue.remove(entry)) {
				entry.updateTimestamp();
				weightedQueue.add(entry);
			}
		}
		// Scale score of the sought entry after every cache hit
		// FIXME: avoid when called from partial reuse methods
		if (LineageCacheConfig.isCostNsize()) {
			if (weightedQueue.remove(entry)) {
				entry.updateScore(true);
				weightedQueue.add(entry);
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsMem.longValue());
		sb.append("/");
		sb.append(_numHitsFS.longValue());
		sb.append("/");
		sb.append(_numHitsDel.longValue());
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)_ctimeFSRead.longValue())/1000000000)); //in sec
		sb.append("/");
		sb.append(String.format("%.3f", ((double)_ctimeFSWrite.longValue())/1000000000)); //in sec
		return sb.toString();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupBlock.java`
#### Snippet
```java
		ipb.setLineageDedupPathPos(_numPaths++);
		ArrayList<Long> rep = new ArrayList<>();
		int pathKey = 1 << (_numPaths-1);
		for (long p : paths) {
			long pathIndex = p | pathKey;
			rep.add(pathIndex);
		}
		_numDistinctPaths.addAll(rep);
		for (ProgramBlock pb : ipb.getChildBlocksIfBody())
			numPathsInPB(pb, ec, rep);
		for (ProgramBlock pb : ipb.getChildBlocksElseBody())
			numPathsInPB(pb, ec, paths);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
Instruction inst = InstructionParser.parseSingleInstruction(item.getData());
				
				if (inst instanceof DataGenCPInstruction) {
					DataGenCPInstruction rand = (DataGenCPInstruction) inst;
					HashMap<String, Hop> params = new HashMap<>();
					if( rand.getOpcode().equals("rand") ) {
						if( rand.output.getDataType() == DataType.TENSOR)
							params.put(DataExpression.RAND_DIMS, new LiteralOp(rand.getDims()));
						else {
							params.put(DataExpression.RAND_ROWS, new LiteralOp(rand.getRows()));
							params.put(DataExpression.RAND_COLS, new LiteralOp(rand.getCols()));
						}
						params.put(DataExpression.RAND_MIN, new LiteralOp(rand.getMinValue()));
						params.put(DataExpression.RAND_MAX, new LiteralOp(rand.getMaxValue()));
						params.put(DataExpression.RAND_PDF, new LiteralOp(rand.getPdf()));
						params.put(DataExpression.RAND_LAMBDA, new LiteralOp(rand.getPdfParams()));
						params.put(DataExpression.RAND_SPARSITY, new LiteralOp(rand.getSparsity()));
						params.put(DataExpression.RAND_SEED, new LiteralOp(rand.getSeed()));
					}
					else if( rand.getOpcode().equals("seq") ) {
						params.put(Statement.SEQ_FROM, new LiteralOp(rand.getFrom()));
						params.put(Statement.SEQ_TO, new LiteralOp(rand.getTo()));
						params.put(Statement.SEQ_INCR, new LiteralOp(rand.getIncr()));
					}
					Hop datagen = new DataGenOp(OpOpDG.valueOf(rand.getOpcode().toUpperCase()),
						new DataIdentifier("tmp"), params);
					datagen.setBlocksize(rand.getBlocksize());
					operands.put(item.getId(), datagen);
				} else if (inst instanceof VariableCPInstruction
						&& ((VariableCPInstruction) inst).isCreateVariable()) {
					String parts[] = InstructionUtils.getInstructionPartsWithValueType(inst.toString());
					DataType dt = DataType.valueOf(parts[4]);
					ValueType vt = dt == DataType.MATRIX ? ValueType.FP64 : ValueType.STRING;
					HashMap<String, Hop> params = new HashMap<>();
					params.put(DataExpression.IO_FILENAME, new LiteralOp(parts[2]));
					params.put(DataExpression.READROWPARAM, new LiteralOp(Long.parseLong(parts[6])));
					params.put(DataExpression.READCOLPARAM, new LiteralOp(Long.parseLong(parts[7])));
					params.put(DataExpression.READNNZPARAM, new LiteralOp(Long.parseLong(parts[8])));
					params.put(DataExpression.FORMAT_TYPE, new LiteralOp(parts[5]));
					DataOp pread = new DataOp(parts[1].substring(5), dt, vt, OpOpData.PERSISTENTREAD, params);
					pread.setFileName(parts[2]);
					operands.put(item.getId(), pread);
				}
				else if  (inst instanceof RandSPInstruction) {
					RandSPInstruction rand = (RandSPInstruction) inst;
					HashMap<String, Hop> params = new HashMap<>();
					if (rand.output.getDataType() == DataType.TENSOR)
						params.put(DataExpression.RAND_DIMS, new LiteralOp(rand.getDims()));
					else {
						params.put(DataExpression.RAND_ROWS, new LiteralOp(rand.getRows()));
						params.put(DataExpression.RAND_COLS, new LiteralOp(rand.getCols()));
					}
					params.put(DataExpression.RAND_MIN, new LiteralOp(rand.getMinValue()));
					params.put(DataExpression.RAND_MAX, new LiteralOp(rand.getMaxValue()));
					params.put(DataExpression.RAND_PDF, new LiteralOp(rand.getPdf()));
					params.put(DataExpression.RAND_LAMBDA, new LiteralOp(rand.getPdfParams()));
					params.put(DataExpression.RAND_SPARSITY, new LiteralOp(rand.getSparsity()));
					params.put(DataExpression.RAND_SEED, new LiteralOp(rand.getSeed()));
					Hop datagen = new DataGenOp(OpOpDG.RAND, new DataIdentifier("tmp"), params);
					datagen.setBlocksize(rand.getBlocksize());
					operands.put(item.getId(), datagen);
				}
				break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
if (inst instanceof DataGenCPInstruction) {
					DataGenCPInstruction rand = (DataGenCPInstruction) inst;
					HashMap<String, Hop> params = new HashMap<>();
					if( rand.getOpcode().equals("rand") ) {
						if( rand.output.getDataType() == DataType.TENSOR)
							params.put(DataExpression.RAND_DIMS, new LiteralOp(rand.getDims()));
						else {
							params.put(DataExpression.RAND_ROWS, new LiteralOp(rand.getRows()));
							params.put(DataExpression.RAND_COLS, new LiteralOp(rand.getCols()));
						}
						params.put(DataExpression.RAND_MIN, new LiteralOp(rand.getMinValue()));
						params.put(DataExpression.RAND_MAX, new LiteralOp(rand.getMaxValue()));
						params.put(DataExpression.RAND_PDF, new LiteralOp(rand.getPdf()));
						params.put(DataExpression.RAND_LAMBDA, new LiteralOp(rand.getPdfParams()));
						params.put(DataExpression.RAND_SPARSITY, new LiteralOp(rand.getSparsity()));
						params.put(DataExpression.RAND_SEED, new LiteralOp(rand.getSeed()));
					}
					else if( rand.getOpcode().equals("seq") ) {
						params.put(Statement.SEQ_FROM, new LiteralOp(rand.getFrom()));
						params.put(Statement.SEQ_TO, new LiteralOp(rand.getTo()));
						params.put(Statement.SEQ_INCR, new LiteralOp(rand.getIncr()));
					}
					Hop datagen = new DataGenOp(OpOpDG.valueOf(rand.getOpcode().toUpperCase()),
						new DataIdentifier("tmp"), params);
					datagen.setBlocksize(rand.getBlocksize());
					operands.put(item.getId(), datagen);
				} else if (inst instanceof VariableCPInstruction
						&& ((VariableCPInstruction) inst).isCreateVariable()) {
					String parts[] = InstructionUtils.getInstructionPartsWithValueType(inst.toString());
					DataType dt = DataType.valueOf(parts[4]);
					ValueType vt = dt == DataType.MATRIX ? ValueType.FP64 : ValueType.STRING;
					HashMap<String, Hop> params = new HashMap<>();
					params.put(DataExpression.IO_FILENAME, new LiteralOp(parts[2]));
					params.put(DataExpression.READROWPARAM, new LiteralOp(Long.parseLong(parts[6])));
					params.put(DataExpression.READCOLPARAM, new LiteralOp(Long.parseLong(parts[7])));
					params.put(DataExpression.READNNZPARAM, new LiteralOp(Long.parseLong(parts[8])));
					params.put(DataExpression.FORMAT_TYPE, new LiteralOp(parts[5]));
					DataOp pread = new DataOp(parts[1].substring(5), dt, vt, OpOpData.PERSISTENTREAD, params);
					pread.setFileName(parts[2]);
					operands.put(item.getId(), pread);
				}
				else if  (inst instanceof RandSPInstruction) {
					RandSPInstruction rand = (RandSPInstruction) inst;
					HashMap<String, Hop> params = new HashMap<>();
					if (rand.output.getDataType() == DataType.TENSOR)
						params.put(DataExpression.RAND_DIMS, new LiteralOp(rand.getDims()));
					else {
						params.put(DataExpression.RAND_ROWS, new LiteralOp(rand.getRows()));
						params.put(DataExpression.RAND_COLS, new LiteralOp(rand.getCols()));
					}
					params.put(DataExpression.RAND_MIN, new LiteralOp(rand.getMinValue()));
					params.put(DataExpression.RAND_MAX, new LiteralOp(rand.getMaxValue()));
					params.put(DataExpression.RAND_PDF, new LiteralOp(rand.getPdf()));
					params.put(DataExpression.RAND_LAMBDA, new LiteralOp(rand.getPdfParams()));
					params.put(DataExpression.RAND_SPARSITY, new LiteralOp(rand.getSparsity()));
					params.put(DataExpression.RAND_SEED, new LiteralOp(rand.getSeed()));
					Hop datagen = new DataGenOp(OpOpDG.RAND, new DataIdentifier("tmp"), params);
					datagen.setBlocksize(rand.getBlocksize());
					operands.put(item.getId(), datagen);
				}
				break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/AutoDiff.java`
#### Snippet
```java
					HashMap<String, Hop> params = new HashMap<>();
					params.put(DataExpression.IO_FILENAME, new LiteralOp(parts[2]));
					params.put(DataExpression.READROWPARAM, new LiteralOp(Long.parseLong(parts[6])));
					params.put(DataExpression.READCOLPARAM, new LiteralOp(Long.parseLong(parts[7])));
					params.put(DataExpression.READNNZPARAM, new LiteralOp(Long.parseLong(parts[8])));
					params.put(DataExpression.FORMAT_TYPE, new LiteralOp(parts[5]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
				String[] parts = item.getOpcode().split(LineageDedupUtils.DEDUP_DELIM); //e.g. dedup_R_SB13_0
				String name = parts[2] + parts[1] + parts[3];  //loopId + outVar + pathId
				List<Hop> finputs = Arrays.stream(item.getInputs())
						.map(inp -> operands.get(inp.getId())).collect(Collectors.toList());
				String[] inputNames = new String[item.getInputs().length];
				for (int i=0; i<item.getInputs().length; i++)
					inputNames[i] = LPLACEHOLDER + i;  //e.g. IN#0, IN#1
				Hop funcOp = new FunctionOp(FunctionType.DML, DMLProgram.DEFAULT_NAMESPACE, 
						name, inputNames, finputs, new String[] {parts[1]}, false);

				// Cut the Hop dag after function calls 
				partDagRoots.put(parts[1], funcOp);
				// Compile the dag and save
				constructBasicBlock(partDagRoots, parts[1], prog);

				// Construct a Hop dag for the function body from the dedup patch, and compile
				Hop output = constructHopsDedupPatch(parts, inputNames, finputs, prog);
				// Create a TRead on the function o/p as a leaf for the next Hop dag
				// Use the function body root/return hop to propagate right data type
				operands.put(item.getId(), HopRewriteUtils.createTransientRead(parts[1], output));
				break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
							Hop input = operands.get(item.getInputs()[0].getId());
							Hop aggunary = InstructionUtils.isUnaryMetadata(item.getOpcode()) ?
								HopRewriteUtils.createUnary(input, OpOp1.valueOfByOpcode(item.getOpcode())) :
								HopRewriteUtils.createAggUnaryOp(input, item.getOpcode());
							operands.put(item.getId(), aggunary);
							break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
							Hop input1 = operands.get(item.getInputs()[0].getId());
							Hop input2 = operands.get(item.getInputs()[1].getId());
							Hop input3 = operands.get(item.getInputs()[2].getId());
							Hop aggternary = HopRewriteUtils.createSum(
								HopRewriteUtils.createBinary(
								HopRewriteUtils.createBinary(input1, input2, OpOp2.MULT),
								input3, OpOp2.MULT));
							operands.put(item.getId(), aggternary);
							break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
							ArrayList<Hop> inputs = new ArrayList<>();
							for(int i=0; i<5; i++)
								inputs.add(operands.get(item.getInputs()[i].getId()));
							operands.put(item.getId(), HopRewriteUtils.createReorg(inputs, ReOrgOp.RESHAPE));
							break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
							String opcode = ("^2".equals(item.getOpcode()) 
								|| "*2".equals(item.getOpcode())) ? 
								item.getOpcode().substring(0, 1) : item.getOpcode();
							Hop input1 = operands.get(item.getInputs()[0].getId());
							Hop input2 = operands.get(item.getInputs()[1].getId());
							Hop binary = HopRewriteUtils.createBinary(input1, input2, opcode);
							operands.put(item.getId(), binary);
							break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
							if( item.getInputs().length==3 )
								operands.put(item.getId(), HopRewriteUtils.createTernary(
									operands.get(item.getInputs()[0].getId()),
									operands.get(item.getInputs()[1].getId()),
									operands.get(item.getInputs()[2].getId()), OpOp3.CTABLE));
							else if( item.getInputs().length==5 )
								operands.put(item.getId(), HopRewriteUtils.createTernary(
									operands.get(item.getInputs()[0].getId()),
									operands.get(item.getInputs()[1].getId()),
									operands.get(item.getInputs()[2].getId()),
									operands.get(item.getInputs()[3].getId()),
									operands.get(item.getInputs()[4].getId()), OpOp3.CTABLE));
							break;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
				else if( stype != null ) {
					switch(stype) {
						case Reblock: {
							Hop input = operands.get(item.getInputs()[0].getId());
							input.setBlocksize(ConfigurationManager.getBlocksize());
							input.setRequiresReblock(true);
							operands.put(item.getId(), input);
							break;
						}
						case Checkpoint: {
							Hop input = operands.get(item.getInputs()[0].getId());
							operands.put(item.getId(), input);
							break;
						}
						case MatrixIndexing: {
							operands.put(item.getId(), constructIndexingOp(item, operands));
							break;
						}
						case GAppend: {
							operands.put(item.getId(), HopRewriteUtils.createBinary(
								operands.get(item.getInputs()[0].getId()),
								operands.get(item.getInputs()[1].getId()), OpOp2.CBIND));
							break;
						}
						default:
							throw new DMLRuntimeException("Unsupported instruction "
								+ "type: " + stype.name() + " (" + item.getOpcode() + ").");
					}
				}
				else
					throw new DMLRuntimeException("Unsupported instruction: " + item.getOpcode());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
				if (item.getData().startsWith(LPLACEHOLDER)) {
					long phId = Long.parseLong(item.getData().substring(3));
					Hop input = operands.get(phId);
					operands.remove(phId);
					// Replace the placeholders with TReads
					operands.put(item.getId(), input); // order preserving
					break;
				}
				Instruction inst = InstructionParser.parseSingleInstruction(item.getData());
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		MatrixObject cachedEntry = toMatrixObject(inCache.get("lastMatrix"));
		lrwec.setVariable("cachedEntry", cachedEntry);
		DataOp lastRes = HopRewriteUtils.createTransientRead("cachedEntry", cachedEntry);
		// Create rightIndex op to find the last matrix
		// TODO: For now assumption is that a single column is being appended in a loop
		//       Need to go down the lineage to find number of columns are being appended.
		MatrixObject mo = ec.getMatrixObject(((ComputationCPInstruction)curr).input1);
		lrwec.setVariable("oldMatrix", mo);
		DataOp newMatrix = HopRewriteUtils.createTransientRead("oldMatrix", mo);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		MatrixBlock cachedEntry = inCache.get("lastMatrix");
		MatrixObject newmo = toMatrixObject(cachedEntry);
		lrwec.setVariable("cachedEntry", newmo);
		DataOp lastRes = HopRewriteUtils.createTransientRead("cachedEntry", cachedEntry);
		MatrixObject mo = ec.getMatrixObject(((ComputationCPInstruction)curr).input1);
		lrwec.setVariable("oldMatrix", mo);
		DataOp newMatrix = HopRewriteUtils.createTransientRead("oldMatrix", mo);
		
		// pull out the newly added column(2nd last) from the input matrix
		Hop lastCol;
		// Use deltaX from cache, or create rightIndex
		if (inCache.containsKey("deltaX")) {
			MatrixBlock cachedRI = inCache.get("deltaX");
			lrwec.setVariable("deltaX", toMatrixObject(cachedRI));
			lastCol = HopRewriteUtils.createTransientRead("deltaX", cachedRI);
		}
		else
			lastCol = HopRewriteUtils.createIndexingOp(newMatrix, new LiteralOp(1), new LiteralOp(mo.getNumRows()), 
				new LiteralOp(mo.getNumColumns()-1), new LiteralOp(mo.getNumColumns()-1));
		// apply t(lastCol) on i/p matrix to get the result vectors.
		ReorgOp tlastCol = HopRewriteUtils.createTranspose(lastCol);
		AggBinaryOp newCol = HopRewriteUtils.createMatrixMultiply(tlastCol, newMatrix);
		ReorgOp tnewCol = HopRewriteUtils.createTranspose(newCol);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		ReorgOp tlastCol = HopRewriteUtils.createTranspose(lastCol);
		AggBinaryOp newCol = HopRewriteUtils.createMatrixMultiply(tlastCol, newMatrix);
		ReorgOp tnewCol = HopRewriteUtils.createTranspose(newCol);
		
		// Push the result row & column inside the cashed block as 2nd last row and col respectively.
		IndexingOp topLeft = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(1), new LiteralOp(newmo.getNumRows()-1), 
			new LiteralOp(1), new LiteralOp(newmo.getNumColumns()-1));
		IndexingOp topRight = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(1), new LiteralOp(newmo.getNumRows()-1), 
			new LiteralOp(newmo.getNumColumns()), new LiteralOp(newmo.getNumColumns()));
		IndexingOp bottomLeft = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(newmo.getNumRows()), 
			new LiteralOp(newmo.getNumRows()), new LiteralOp(1), new LiteralOp(newmo.getNumColumns()-1));
		IndexingOp bottomRight = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(newmo.getNumRows()), 
			new LiteralOp(newmo.getNumRows()), new LiteralOp(newmo.getNumColumns()), new LiteralOp(newmo.getNumColumns()));
		IndexingOp topCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(1), new LiteralOp(mo.getNumColumns()-2), 
			new LiteralOp(1), new LiteralOp(1));
		IndexingOp bottomCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(mo.getNumColumns()), 
			new LiteralOp(mo.getNumColumns()), new LiteralOp(1), new LiteralOp(1));
		NaryOp rowOne = HopRewriteUtils.createNary(OpOpN.CBIND, topLeft, topCol, topRight);
		NaryOp rowTwo = HopRewriteUtils.createNary(OpOpN.CBIND, bottomLeft, bottomCol, bottomRight);
		NaryOp lrwHop = HopRewriteUtils.createNary(OpOpN.RBIND, rowOne, newCol, rowTwo);
		DataOp lrwWrite = HopRewriteUtils.createTransientWrite(LR_VAR, lrwHop);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		IndexingOp topLeft = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(1), new LiteralOp(newmo.getNumRows()-1), 
			new LiteralOp(1), new LiteralOp(newmo.getNumColumns()-1));
		IndexingOp topRight = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(1), new LiteralOp(newmo.getNumRows()-1), 
			new LiteralOp(newmo.getNumColumns()), new LiteralOp(newmo.getNumColumns()));
		IndexingOp bottomLeft = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(newmo.getNumRows()), 
			new LiteralOp(newmo.getNumRows()), new LiteralOp(1), new LiteralOp(newmo.getNumColumns()-1));
		IndexingOp bottomRight = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(newmo.getNumRows()), 
			new LiteralOp(newmo.getNumRows()), new LiteralOp(newmo.getNumColumns()), new LiteralOp(newmo.getNumColumns()));
		IndexingOp topCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(1), new LiteralOp(mo.getNumColumns()-2), 
			new LiteralOp(1), new LiteralOp(1));
		IndexingOp bottomCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(mo.getNumColumns()), 
			new LiteralOp(mo.getNumColumns()), new LiteralOp(1), new LiteralOp(1));
		NaryOp rowOne = HopRewriteUtils.createNary(OpOpN.CBIND, topLeft, topCol, topRight);
		NaryOp rowTwo = HopRewriteUtils.createNary(OpOpN.CBIND, bottomLeft, bottomCol, bottomRight);
		NaryOp lrwHop = HopRewriteUtils.createNary(OpOpN.RBIND, rowOne, newCol, rowTwo);
		DataOp lrwWrite = HopRewriteUtils.createTransientWrite(LR_VAR, lrwHop);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		IndexingOp bottomRight = HopRewriteUtils.createIndexingOp(lastRes, new LiteralOp(newmo.getNumRows()), 
			new LiteralOp(newmo.getNumRows()), new LiteralOp(newmo.getNumColumns()), new LiteralOp(newmo.getNumColumns()));
		IndexingOp topCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(1), new LiteralOp(mo.getNumColumns()-2), 
			new LiteralOp(1), new LiteralOp(1));
		IndexingOp bottomCol = HopRewriteUtils.createIndexingOp(tnewCol, new LiteralOp(mo.getNumColumns()), 
			new LiteralOp(mo.getNumColumns()), new LiteralOp(1), new LiteralOp(1));
		NaryOp rowOne = HopRewriteUtils.createNary(OpOpN.CBIND, topLeft, topCol, topRight);
		NaryOp rowTwo = HopRewriteUtils.createNary(OpOpN.CBIND, bottomLeft, bottomCol, bottomRight);
		NaryOp lrwHop = HopRewriteUtils.createNary(OpOpN.RBIND, rowOne, newCol, rowTwo);
		DataOp lrwWrite = HopRewriteUtils.createTransientWrite(LR_VAR, lrwHop);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		MatrixBlock cachedEntry = inCache.get("lastMatrix");
		lrwec.setVariable("cachedEntry", toMatrixObject(cachedEntry));
		DataOp lastRes = HopRewriteUtils.createTransientRead("cachedEntry", cachedEntry);
		//TODO: support for block of rows
		MatrixObject moL = ec.getMatrixObject(((ComputationCPInstruction)curr).input1);
		lrwec.setVariable("leftMatrix", moL);
		DataOp leftMatrix = HopRewriteUtils.createTransientRead("leftMatrix", moL);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		BinaryOp lrwHop= HopRewriteUtils.createBinary(lastRes, rowTwo, OpOp2.CBIND);
		DataOp lrwWrite = HopRewriteUtils.createTransientWrite(LR_VAR, lrwHop);

		// generate runtime instructions
		if (LOG.isDebugEnabled())
			LOG.debug("LINEAGE REWRITE rewriteElementMulCbind APPLIED");
		ArrayList<Instruction> inst = genInst(lrwWrite, lrwec);
		_disableReuse = true;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
					LineageItem tmp = new LineageItem("cbind", new LineageItem[] {L2appin1, source.getInputs()[1]});
					LineageItem toProbe = new LineageItem(curr.getOpcode(), new LineageItem[] {tmp});
					if (LineageCache.probe(toProbe)) { 
						inCache.put("lastMatrix", LineageCache.getMatrix(toProbe));
						_computeTime = LineageCache.getEntry(toProbe)._computeTime;
					}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
				LineageItem leftSource = left.getInputs()[0]; //left inpur of rbind = X
				// create ba+* lineage on top of the input of last append
				LineageItem tmp = new LineageItem(curr.getOpcode(), new LineageItem[] {leftSource, right});
				if (LineageCache.probe(tmp)) {
					inCache.put("lastMatrix", LineageCache.getMatrix(tmp));
					_computeTime = LineageCache.getEntry(tmp)._computeTime;
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
				LineageItem leftSource = left.getInputs()[0]; //left inpur of rbind = X
				LineageItem rightSource = right.getInputs()[0]; //right inpur of rbind = Y 
				// create * lineage on top of the input of last append
				LineageItem tmp = new LineageItem(curr.getOpcode(), new LineageItem[] {leftSource, rightSource});
				if (LineageCache.probe(tmp)) {
					inCache.put("lastMatrix", LineageCache.getMatrix(tmp));
					_computeTime = LineageCache.getEntry(tmp)._computeTime;
				}
				// look for the appended rows in cache
				if (LineageCache.probe(left.getInputs()[1]))
					inCache.put("deltaX", LineageCache.getMatrix(left.getInputs()[1]));
				if (LineageCache.probe(right.getInputs()[1]))
					inCache.put("deltaY", LineageCache.getMatrix(right.getInputs()[1]));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibCommonsMath.java`
#### Snippet
```java
			int k = i;
			double p = eValues[i];
			for(int j = i + 1; j < n; j++) {
				if(eValues[j] < p) {
					k = j;
					p = eValues[j];
				}
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
		for(int r=0; r<rlen; r++)
			for(int c=0; c<clen; c++) {
				double thisvalue = m1ret.quickGetValue(r, c);
				double thatvalue = m2.quickGetValue(r, c);
				double resultvalue = op.fn.execute(thisvalue, thatvalue);
				m1ret.quickSetValue(r, c, resultvalue);
			}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		long CHW = C*H*W; long KPQ = K*P*Q; long CRS = C*R*S; 
		long NCHW = N*CHW; long NKPQ = N*KPQ; long KCRS = K*CRS;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		boolean isSparseFilter = isInSparseFormat(gCtx, filter);
		long filterNnz = getNnz(gCtx, instName, filter, RECOMPUTE_DENSE_NNZ);
		if(filterNnz == 0) {
			return; // since filter is empty
		}
		boolean isSparseImage = isInSparseFormat(gCtx, image);
		long imageNnz = getNnz(gCtx, instName, image, RECOMPUTE_DENSE_NNZ);
		if(imageNnz == 0) {
			return; // since image is empty
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		int N = toInt(image.getNumRows());
		int C = toInt(scale.getNumRows());
		long CHW = image.getNumColumns();
		validateBatchNormalizationDimensions(scale, bias, runningMean, runningVar, C);

		// Allocate descriptors
		cudnnTensorDescriptor nCHWDescriptor = allocateNCHWDescriptors(gCtx, N, C, CHW,
				new MatrixObject[] {image},  new MatrixObject[] {ret});
		cudnnTensorDescriptor scaleTensorDesc = allocateTensorDescriptor(1, C, 1, 1);

		// Get underlying dense pointer
		Pointer imagePtr = getDensePointerForCuDNN(gCtx, image, instName);
		Pointer retPtr = getDensePointerForCuDNN(gCtx, ret, instName);
		Pointer biasPtr = getDensePointerForCuDNN(gCtx, bias, instName);
		Pointer scalePtr = getDensePointerForCuDNN(gCtx, scale, instName);
		Pointer runningMeanPtr = getDensePointerForCuDNN(gCtx, runningMean, instName);
		Pointer runningVarPtr = getDensePointerForCuDNN(gCtx, runningVar, instName);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
		cudnnTensorDescriptor scaleTensorDesc = allocateTensorDescriptor(1, C, 1, 1);

		// Get underlying dense pointer
		Pointer imagePtr = getDensePointerForCuDNN(gCtx, image, instName);
		Pointer doutPtr = getDensePointerForCuDNN(gCtx, dout, instName);
		Pointer scalePtr = getDensePointerForCuDNN(gCtx, scale, instName);
		Pointer dXPtr = getDensePointerForCuDNN(gCtx, dX, instName);
		Pointer dScalePtr = getDensePointerForCuDNN(gCtx, dScale, instName);
		Pointer dBiasPtr = getDensePointerForCuDNN(gCtx, dBias, instName);
		
		Pointer resultSaveMeanPtr = getDensePointerForCuDNN(gCtx, resultSaveMean, instName);
		Pointer resultSaveInvVariancePtr = getDensePointerForCuDNN(gCtx, resultSaveInvVariance, instName);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNRnnAlgorithm.java`
#### Snippet
```java
		rnnDesc = null;
		if(hxDesc != null)
			cudnnDestroyTensorDescriptor(hxDesc);
		hxDesc = null;
		if(dhxDesc != null)
			cudnnDestroyTensorDescriptor(dhxDesc);
		dhxDesc = null;
		if(hyDesc != null)
			cudnnDestroyTensorDescriptor(hyDesc);
		hyDesc = null;
		if(dhyDesc != null)
			cudnnDestroyTensorDescriptor(dhyDesc);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNNRnnAlgorithm.java`
#### Snippet
```java
		if(xDesc != null) {
			for(cudnnTensorDescriptor dsc : xDesc) {
				cudnnDestroyTensorDescriptor(dsc);
			}
			xDesc = null;
		}
		if(dxDesc != null) {
			for(cudnnTensorDescriptor dsc : dxDesc) {
				cudnnDestroyTensorDescriptor(dsc);
			}
			dxDesc = null;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNN.java`
#### Snippet
```java
		int index = 0;
		for(int n = 0; n < N; n++) {
			for(int k = 0; k < K; k++) {
				double biasVal = bias[k];
				for(int pq = 0; pq < PQ; pq++, index++)
					a[index] *= biasVal;
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNConv2d.java`
#### Snippet
```java
		ArrayList<Callable<Long>> ret = new ArrayList<>();
		// Try to create as many tasks as threads. 
		// Creating more tasks will help in tail, but would have additional overhead of maintaining the intermediate
		// data structures such as im2col blocks.
		int k = OptimizerUtils.getConstrainedNumThreads(params.numThreads);
		int taskSize = (int)(Math.ceil((double)params.N / k));
		
		boolean isEmptyDenseInput = (!params.input1.isInSparseFormat() && params.input1.denseBlock == null) || 
			(!params.input2.isInSparseFormat() && params.input2.denseBlock == null);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNConv2d.java`
#### Snippet
```java
			int PQ = _params.P*_params.Q, K = _params.K, CRS = _params.C*_params.R*_params.S;
			MatrixBlock dout = _params.input2;
			MatrixBlock im2ColOutBlock = new MatrixBlock(CRS, PQ, _params.input1.sparse).allocateBlock();
			LibMatrixDNNIm2Col.preallocateSparseOutput(_params.input1, im2ColOutBlock);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
			doutArray = params.input2.getDenseBlockValues();
			output = params.output;
			C = params.C; CHW = params.C*params.H*params.W; HW = params.H*params.W;
			P = params.P; Q = params.Q; CPQ = params.C*params.P*params.Q;
			PQ = params.P*params.Q;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
			output = params.output;
			C = params.C;
			CHW = params.C*params.H*params.W; HW = params.H*params.W;
			P = params.P; Q = params.Q; 
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				int apos = sblock.pos(n);
				int alen = sblock.size(n);
				int[] aix = sblock.indexes(n);
				double[] avals = sblock.values(n);
				//find channel start and end, w/ robustness for non-existing entries
				int cpos = (c==0) ? 0 : sblock.posFIndexGTE(n, c*HW);
				int cpos2 = (c+1==C) ? alen : sblock.posFIndexGTE(n, (c+1)*HW);
				cpos = (cpos>=0) ? cpos : alen;
				cpos2 = (cpos2>=0) ? cpos2 : alen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
			int cpos = (c==0) ? 0 : sblock.posFIndexGTE(n, c*PQ);
			int cpos2 = (c+1==C) ? alen : sblock.posFIndexGTE(n, (c+1)*PQ);
			cpos = (cpos>=0) ? cpos : alen;
			cpos2 = (cpos2>=0) ? cpos2 : alen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNRelu.java`
#### Snippet
```java
			if( b.isEmpty(i) ) continue;
			int bpos = b.pos(i), blen = b.size(i);
			int[] bix = b.indexes(i);
			double[] bvals = b.values(i), avals = a.values(i);
			int aix = a.pos(i);
			c.allocate(i, blen);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		int rows = rgen._rows;
		int cols = rgen._cols;
		int blen = rgen._blocksize;
		double sparsity = rgen._sparsity;
		
		if(out instanceof CompressedMatrixBlock)
			throw new DMLRuntimeException("Invalid to use compressed matrix block as output");

		// sanity check valid dimensions and sparsity
		checkMatrixDimensionsAndSparsity(rows, cols, sparsity);
		
		/*
		 * Setup min and max for distributions other than "uniform". Min and Max
		 * are set up in such a way that the usual logic of
		 * (max-min)*prng.nextDouble() is still valid. This is done primarily to
		 * share the same code across different distributions.
		 */
		double min = rgen._pdf == RandomMatrixGenerator.PDF.UNIFORM ? rgen._min : 0;
		double max = rgen._pdf == RandomMatrixGenerator.PDF.UNIFORM ? rgen._max : 1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
			double dvix[] = new double[vix.length];
			if (bPrimeCumSum)
				for (int i = 0; i< vix.length; i++)
					dvix[vix.length-1-i] = vix[i];
			else
				for (int i = 0; i< vix.length; i++)
					dvix[i] = vix[i];
			
			MatrixBlock mbix = DataConverter.convertToMatrixBlock(dvix, true);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
			MatrixBlock mbResult = mbix.unaryOperations(u_op, new MatrixBlock());
			
			vixCumSum = DataConverter.convertToIntVector(mbResult);  
			if (bPrimeCumSum)
				for (int i = 0; i< (vixCumSum.length+1)/2; i++) {
					int iTemp = vixCumSum[vixCumSum.length-1-i];
					vixCumSum[vixCumSum.length-1-i] = vixCumSum[i];
					vixCumSum[i] = iTemp;
				}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		out.allocateDenseBlock(true);
		if(agg0 != 0.0) {
			out.getDenseBlock().set(0, 1, 0, out.clen, agg0);
			out.setNonZeros(out.getNumColumns());
		}
		
		if( in.isEmptyBlock(false) )
			return;
		
		SparseBlock sblock = in.getSparseBlock();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		int ixMax = bv.length;
		if(value < bv[0] || value >= bv[bv.length-1]) 
			return ixMax;
		int ix = Arrays.binarySearch(bv, value);
		if(ix < 0)
			ix = Math.abs(ix)-2;
		ixMax = bvi[ix]+1; 
		return ixMax;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		if(value < bv[0] || value >= bv[bv.length-1]) 
			return ixMin;
		int ix = Arrays.binarySearch(bv, value);
		if (ix < 0) 
			ix = Math.abs(ix)-2;
		ixMin = bvi[ix]+1; 
		return ixMin;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
		long cellIndexOverlapTop = Math.max(cellIndexTopRow, ixrange.rowStart);
		long cellIndexOverlapBottom = Math.min(cellIndexBottomRow, ixrange.rowEnd);
		long cellIndexOverlapLeft = Math.max(cellIndexLeftCol, ixrange.colStart);
		long cellIndexOverlapRight = Math.min(cellIndexRightCol, ixrange.colEnd);
		
		//check if block is outside the indexing range
		if(cellIndexOverlapTop>cellIndexOverlapBottom || cellIndexOverlapLeft>cellIndexOverlapRight) {
			return  outlist;
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
			long D = blkIn.getNonZeros() + 1;

			/**
			 * To ensure that the likelihood to hash to the same value we need O(D^2) positions to hash to assign. If the
			 * value is higher than int (which is the area we hash to) then use Integer Max value as largest hashing space.
			 */
			long tmp = D * D;
			int M = (tmp > (long) Integer.MAX_VALUE) ? Integer.MAX_VALUE : (int) tmp;
			/**
			 * The estimator is asymptotically unbiased as k becomes large, but memory usage also scales with k. Furthermore k
			 * value must be within range: D >> k >> 0
			 */
			int k = D > 64 ? 64 : (int) D;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
		for(int i = 0; i < nHashes0; ++i) {
			double val;
			if(op.getDirection().isRow() || op.getDirection().isRowCol()) {
				val = matrix0.getValue(idx, i);
			}
			else { // op.getDirection().isCol()
				val = matrix0.getValue(i, idx);
			}
			hashUnion.add(val);
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java
		boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
		mcsr = false; //force CSR for transformencode
		int index = _colID - 1;
		// Apply loop tiling to exploit CPU caches
		double[] codes = getCodeCol(in, rowStart, blk);
		int rowEnd = getEndIndex(in.getNumRows(), rowStart, blk);
		int B = 32; //tile size
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
		if(_colID == -1)
			return;
		for(ColumnEncoder columnEncoder : columnEncoders) {
			int distinct = -1;
			if(columnEncoder instanceof ColumnEncoderRecode) {
				ColumnEncoderRecode columnEncoderRecode = (ColumnEncoderRecode) columnEncoder;
				distinct = columnEncoderRecode.getNumDistinctValues();
			}
			else if(columnEncoder instanceof ColumnEncoderBin) {
				distinct = ((ColumnEncoderBin) columnEncoder)._numBin;
			}
			else if(columnEncoder instanceof ColumnEncoderFeatureHash){
				distinct = (int) ((ColumnEncoderFeatureHash) columnEncoder).getK();
			}

			if(distinct != -1) {
				_domainSize = distinct;
				LOG.debug("DummyCoder for column: " + _colID + " has domain size: " + _domainSize);
			}
		}
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
		final int colId = c._colID;
		final Array<?> a = in.getColumn(colId - 1);
		final boolean containsNull = a.containsNull();
		final List<ColumnEncoder> r = c.getEncoders();
		final ColumnEncoderBin b = (ColumnEncoderBin) r.get(0);
		b.build(in);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
		MatrixBlock incrementing = new MatrixBlock(domain + (containsNull ? 1 : 0), 1, false);
		for(int i = 0; i < domain; i++)
			incrementing.quickSetValue(i, 0, i + 1);
		if(containsNull)
			incrementing.quickSetValue(domain, 0, Double.NaN);

		ADictionary d = MatrixBlockDictionary.create(incrementing);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
		int colId = c._colID;
		Array<?> a = in.getColumn(colId - 1);
		ColumnEncoderFeatureHash CEHash = (ColumnEncoderFeatureHash) c.getEncoders().get(0);
		int domain = (int) CEHash.getK();
		boolean nulls = a.containsNull();
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/AutoDiff.java`
#### Snippet
```java
						if(rand.output.getDataType() == Types.DataType.TENSOR)
							params.put(DataExpression.RAND_DIMS, new LiteralOp(rand.getDims()));
						else {
							params.put(DataExpression.RAND_ROWS, new LiteralOp(rand.getRows()));
							params.put(DataExpression.RAND_COLS, new LiteralOp(rand.getCols()));
						}
						params.put(DataExpression.RAND_MIN, new LiteralOp(rand.getMinValue()));
						params.put(DataExpression.RAND_MAX, new LiteralOp(rand.getMaxValue()));
						params.put(DataExpression.RAND_PDF, new LiteralOp(rand.getPdf()));
						params.put(DataExpression.RAND_LAMBDA, new LiteralOp(rand.getPdfParams()));
						params.put(DataExpression.RAND_SPARSITY, new LiteralOp(rand.getSparsity()));
						params.put(DataExpression.RAND_SEED, new LiteralOp(rand.getSeed()));
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		if( mb.isEmptyBlock(false) )
			return ret;
		if( mb.isInSparseFormat() ) {
			Iterator<IJV> iter = mb.getSparseBlockIterator();
			while( iter.hasNext() ) {
				IJV cell = iter.next();
				ret[cell.getI()*cols+cell.getJ()] = (int)cell.getV();
			}
		}
		else {
			//memcopy row major representation if at least 1 non-zero
			for( int i=0, cix=0; i<rows; i++ )
				for( int j=0; j<cols; j++, cix++ )
					ret[cix] = (int)(mb.getValueDenseUnsafe(i, j));
		}
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		StringBuffer sb = new StringBuffer();

		// Setup number of rows and columns to print
		int rlen = mb.getNumRows();
		int clen = mb.getNumColumns();
		int rowLength = rlen;
		int colLength = clen;
		if (rowsToPrint >= 0)
			rowLength = rowsToPrint < rlen ? rowsToPrint : rlen;
		if (colsToPrint >= 0)
			colLength = colsToPrint < clen ? colsToPrint : clen;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java
		for (int i = 0; i < not_missing.size(); i++) {
			sum += Math.pow(not_missing.get(i) - not_missing_pred.get(i), 2);
		}

		double rmse = Math.sqrt(sum / n_size);
		Double[] content = new Double[pred.size()];
		return new Container(pred.toArray(content), rmse);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		DataInput in = !(ret instanceof MatrixBlock) ? 
			new DataInputStream(new BufferedInputStream(fis, BUFFER_SIZE)) :
			new FastBufferedDataInputStream(fis, BUFFER_SIZE);		
		try {
			ret.readFields(in);
		}
		finally {
			IOUtilFunctions.closeSilently((InputStream)in);
			IOUtilFunctions.closeSilently(fis);
		}
			
		return ret;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		tmpPB.setFromInstructions( createDeepCopyInstructionSet(fpb.getFromInstructions(), pid, IDPrefix, prog, fnStack, fnCreated, plain, true) );
		tmpPB.setToInstructions( createDeepCopyInstructionSet(fpb.getToInstructions(), pid, IDPrefix, prog, fnStack, fnCreated, plain, true) );
		tmpPB.setIncrementInstructions( createDeepCopyInstructionSet(fpb.getIncrementInstructions(), pid, IDPrefix, prog, fnStack, fnCreated, plain, true) );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
				ret.setDMLProg(sb.getDMLProg());
				ret.setParseInfo(sb);
				ret.setLiveIn( sb.liveIn() );
				ret.setLiveOut( sb.liveOut() );
				ret.setUpdatedVariables( sb.variablesUpdated() );
				ret.setReadVariables( sb.variablesRead() );
				ret.setUpdateInPlaceVars( sb.getUpdateInPlaceVars() );
				
				//shallow copy child statements
				ret.setStatements( sb.getStatements() );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		StringBuilder builder = new StringBuilder();
		builder.append(PSBODY_BEGIN);
		builder.append(NEWLINE);

		//handle DMLScript UUID (propagate original uuid for writing to scratch space)
		builder.append(DMLScript.getUUID());
		builder.append(COMPONENTS_DELIM);
		builder.append(NEWLINE);

		//handle DML config
		builder.append(ConfigurationManager.getDMLConfig().serializeDMLConfig());
		builder.append(COMPONENTS_DELIM);
		builder.append(NEWLINE);

		//handle additional configurations
		builder.append(CONF_STATS + "=" + DMLScript.STATISTICS);
		builder.append(COMPONENTS_DELIM);
		builder.append(NEWLINE);

		//handle program
		builder.append(PROG_BEGIN);
		builder.append(NEWLINE);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		sb.append( COMPONENTS_DELIM );
		
		//handle execution context
		//note: this includes also the symbol table (serialize only the top-level variable map,
		//      (symbol tables for nested/child blocks are created at parse time, on the remote side)
		sb.append(EC_BEGIN);
		sb.append( serializeExecutionContext(ec) );
		sb.append(EC_END);
		sb.append( NEWLINE );
		sb.append( COMPONENTS_DELIM );
		sb.append( NEWLINE );
		
		//handle program blocks
		sb.append(PBS_BEGIN);
		sb.append( NEWLINE );
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
			sb.append( COMPONENTS_DELIM );
			sb.append(INST_BEGIN);
			sb.append( serializeInstructions( fpb.getFromInstructions(), clsMap ) );
			sb.append(INST_END);
			sb.append( COMPONENTS_DELIM );
			sb.append(INST_BEGIN);
			sb.append( serializeInstructions(fpb.getToInstructions(), clsMap) );
			sb.append(INST_END);
			sb.append( COMPONENTS_DELIM );
			sb.append(INST_BEGIN);
			sb.append( serializeInstructions(fpb.getIncrementInstructions(), clsMap) );
			sb.append(INST_END);
			sb.append( COMPONENTS_DELIM );
			sb.append(PBS_BEGIN);
			sb.append( rSerializeProgramBlocks(fpb.getChildBlocks(), clsMap) );
			sb.append(PBS_END);
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
        int length = end - start;
        if (length < 7) {
            for (int i = start + 1; i < end; i++) {
                for (int j = i; j > start && values[j - 1] > values[j]; j--) {
                    tempVal = values[j];
                    values[j] = values[j - 1];
                    values[j - 1] = tempVal;
                    tempIx = indexes[j];
                    indexes[j] = indexes[j - 1];
                    indexes[j - 1] = tempIx;
                }
            }
            return;
        }
        int middle = (start + end) / 2;
        if (length > 7) {
            int bottom = start;
            int top = end - 1;
            if (length > 40) {
                length /= 8;
                bottom = med3(values, bottom, bottom + length, bottom
                        + (2 * length));
                middle = med3(values, middle - length, middle, middle + length);
                top = med3(values, top - (2 * length), top - length, top);
            }
            middle = med3(values, bottom, middle, top);
        }
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
        if (length < 7) {
            for (int i = start + 1; i < end; i++) {
                for (int j = i; j > start && indexes[j - 1] > indexes[j]; j--) {
                    tempIx = indexes[j];
                    indexes[j] = indexes[j - 1];
                    indexes[j - 1] = tempIx;
                    tempVal = values[j];
                    values[j] = values[j - 1];
                    values[j - 1] = tempVal;
                }
            }
            return;
        }
        int middle = (start + end) / 2;
        if (length > 7) {
            int bottom = start;
            int top = end - 1;
            if (length > 40) {
                length /= 8;
                bottom = med3(indexes, bottom, bottom + length, bottom
                        + (2 * length));
                middle = med3(indexes, middle - length, middle, middle + length);
                top = med3(indexes, top - (2 * length), top - length, top);
            }
            middle = med3(indexes, bottom, middle, top);
        }
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
                    tempIx = indexes[j];
                    indexes[j] = indexes[j - 1];
                    indexes[j - 1] = tempIx;
                    tempIx2 = indexes2[j];
                    indexes2[j] = indexes2[j - 1];
                    indexes2[j - 1] = tempIx2;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
        int middle = (start + end) / 2;
        if (length > 7) {
            int bottom = start;
            int top = end - 1;
            if (length > 40) {
                length /= 8;
                bottom = med3(indexes, bottom, bottom + length, bottom
                        + (2 * length));
                middle = med3(indexes, middle - length, middle, middle + length);
                top = med3(indexes, top - (2 * length), top - length, top);
            }
            middle = med3(indexes, bottom, middle, top);
        }
        int partionValue = indexes[middle];
        int a, b, c, d;
        a = b = start;
        c = d = end - 1;
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
int a, b, c, d;
        a = b = start;
        c = d = end - 1;
        while (true) {
            while (b <= c && values[b] <= partionValue) {
                if (values[b] == partionValue) {
                    tempVal = values[a];
                    values[a] = values[b];
                    values[b] = tempVal;
                    tempIx = indexes[a];
                    indexes[a++] = indexes[b];
                    indexes[b] = tempIx;
                }
                b++;
            }
            while (c >= b && values[c] >= partionValue) {
                if (values[c] == partionValue) {
                    tempVal = values[c];
                    values[c] = values[d];
                    values[d] = tempVal;
                    tempIx = indexes[c];
                    indexes[c] = indexes[d];
                    indexes[d--] = tempIx;
                }
                c--;
            }
            if (b > c) {
                break;
            }
            tempVal = values[b];
            values[b] = values[c];
            values[c] = tempVal;
            tempIx = indexes[b];
            indexes[b++] = indexes[c];
            indexes[c--] = tempIx;
        }
        length = a - start < b - a ? a - start : b - a;
        int l = start;
        int h = b - length;
        while (length-- > 0) {
            tempVal = values[l];
            values[l] = values[h];
            values[h] = tempVal;
            tempIx = indexes[l];
            indexes[l++] = indexes[h];
            indexes[h++] = tempIx;
        }
        length = d - c < end - 1 - d ? d - c : end - 1 - d;
        l = b;
        h = end - length;
        while (length-- > 0) {
            tempVal = values[l];
            values[l] = values[h];
            values[h] = tempVal;
            tempIx = indexes[l];
            indexes[l++] = indexes[h];
            indexes[h++] = tempIx;
        }
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
while (true) {
            while (b <= c && indexes[b] <= partionValue) {
                if (indexes[b] == partionValue) {
                    tempIx = indexes[a];
                    indexes[a] = indexes[b];
                    indexes[b] = tempIx;
                    tempVal = values[a];
                    values[a++] = values[b];
                    values[b] = tempVal;
                }
                b++;
            }
            while (c >= b && indexes[c] >= partionValue) {
                if (indexes[c] == partionValue) {
                    tempIx = indexes[c];
                    indexes[c] = indexes[d];
                    indexes[d] = tempIx;
                    tempVal = values[c];
                    values[c] = values[d];
                    values[d--] = tempVal;
                }
                c--;
            }
            if (b > c) {
                break;
            }
            tempIx = indexes[b];
            indexes[b] = indexes[c];
            indexes[c] = tempIx;
            tempVal = values[b];
            values[b++] = values[c];
            values[c--] = tempVal;
        }
        length = a - start < b - a ? a - start : b - a;
        int l = start;
        int h = b - length;
        while (length-- > 0) {
            tempIx = indexes[l];
            indexes[l] = indexes[h];
            indexes[h] = tempIx;
            tempVal = values[l];
            values[l++] = values[h];
            values[h++] = tempVal;
        }
        length = d - c < end - 1 - d ? d - c : end - 1 - d;
        l = b;
        h = end - length;
        while (length-- > 0) {
            tempIx = indexes[l];
            indexes[l] = indexes[h];
            indexes[h] = tempIx;
            tempVal = values[l];
            values[l++] = values[h];
            values[h++] = tempVal;
        }
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
                    tempIx = indexes[a];
                    indexes[a] = indexes[b];
                    indexes[b] = tempIx;
                    tempIx2 = indexes2[a];
                    indexes2[a] = indexes2[b];
                    indexes2[b] = tempIx2;
                    tempVal = values[a];
                    values[a++] = values[b];
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
        while (length-- > 0) {
            tempIx = indexes[l];
            indexes[l] = indexes[h];
            indexes[h] = tempIx;
            tempIx2 = indexes2[l];
            indexes2[l] = indexes2[h];
            indexes2[h] = tempIx2;
            tempVal = values[l];
            values[l++] = values[h];
            values[h++] = tempVal;
        }
```

### DuplicatedCode
Duplicated code
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
		cudaLibrariesInitTime = 0;
		cudaAllocTime.reset();
		cudaDeAllocTime.reset();
		cudaMemSet0Time.reset();
		cudaMemSet0Count.reset();
		cudaToDevTime.reset();
		cudaFromDevTime.reset();
		cudaFromShadowToHostTime.reset();
		cudaFromShadowToDevTime.reset();
		cudaFromDevToShadowTime.reset();
```

## RuleId[id=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/io/WriterCompressed.java`
#### Snippet
```java
public final class WriterCompressed extends MatrixWriter {

	protected static final Log LOG = LogFactory.getLog(ReaderCompressed.class.getName());

	public static WriterCompressed create(FileFormatProperties props) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java

public final class CLALibUtils {
	protected static final Log LOG = LogFactory.getLog(CLALibUtils.class.getName());

	private CLALibUtils() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	}

	protected static void filterGroupsAndSplitPreAgg(List<AColGroup> groups, double[] constV,
		List<AColGroup> noPreAggGroups, List<APreAgg> preAggGroups) {
		for(AColGroup g : groups) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	 * @return The Filtered list of Column groups containing no SDC Groups but only SDCZero groups.
	 */
	protected static List<AColGroup> filterGroups(List<AColGroup> groups, double[] constV) {
		if(constV == null)
			return groups;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	 * @return A Boolean saying there is SDC groups or Constant groups.
	 */
	protected static boolean shouldPreFilter(List<AColGroup> groups) {
		for(AColGroup g : groups)
			if(g instanceof AMorphingMMColGroup || g instanceof ColGroupConst || g instanceof ColGroupEmpty || g.isEmpty())
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	}

	protected static void addEmptyColumn(List<AColGroup> colGroups, int nCols) {

		// early abort loop
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	}

	protected static void splitPreAgg(List<AColGroup> groups, List<AColGroup> noPreAggGroups,
		List<APreAgg> preAggGroups) {
		for(AColGroup g : groups) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
	}

	protected static double[] getColSum(List<AColGroup> groups, int nCols, int nRows) {
		return AColGroup.colSum(groups, new double[nCols], nRows);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUnary.java`
#### Snippet
```java

public final class CLALibUnary {
	protected static final Log LOG = LogFactory.getLog(CLALibUnary.class.getName());

	private CLALibUnary() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCombineGroups.java`
#### Snippet
```java
 */
public final class CLALibCombineGroups {
	protected static final Log LOG = LogFactory.getLog(CLALibCombineGroups.class.getName());

	private CLALibCombineGroups() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibRexpand.java`
#### Snippet
```java

public final class CLALibRexpand {
	protected static final Log LOG = LogFactory.getLog(CLALibRexpand.class.getName());

	private CLALibRexpand(){
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibSlice.java`
#### Snippet
```java

public final class CLALibSlice {
	protected static final Log LOG = LogFactory.getLog(CLALibSlice.class.getName());

	private CLALibSlice() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibStack.java`
#### Snippet
```java

public final class CLALibStack {
	protected static final Log LOG = LogFactory.getLog(CLALibStack.class.getName());

	private CLALibStack() {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
	}

	protected static void decompressDenseMultiThread(MatrixBlock ret, List<AColGroup> groups, double[] constV, int k,
		boolean overlapping) {
		final int nRows = ret.getNumRows();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
	}

	protected static void decompressDenseMultiThread(MatrixBlock ret, List<AColGroup> groups, double[] constV,
		double eps, int k, boolean overlapping) {
		final int nRows = ret.getNumRows();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
	}

	protected ACostEstimate create(boolean isInSpark) {
		return new ComputationCostEstimator(counter);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
	protected static final Log LOG = LogFactory.getLog(CostEstimatorBuilder.class.getName());

	protected final InstructionTypeCounter counter;

	public CostEstimatorBuilder(WTreeRoot root) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
	}

	protected ACostEstimate createHybrid() {
		return new HybridCostEstimator(counter);
	}
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
	private static final long serialVersionUID = 14L;

	protected static final Log LOG = LogFactory.getLog(CostEstimatorBuilder.class.getName());

	protected final InstructionTypeCounter counter;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java
	}

	protected static MatrixBlock genTmpReduceAllOrRow(MatrixBlock ret, AggregateUnaryOperator op) {
		final int c = ret.getNumColumns();
		MatrixBlock tmp = new MatrixBlock(1, c, false);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
	}

	protected static void decompressToSubBlock(final int rl, final int ru, final DenseBlock db,
		final List<AColGroup> groups, final AIterator[] its) {
		for(int i = 0; i < groups.size(); i++) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
	}

	protected static CompressedMatrixBlock binaryMVPlusStack(CompressedMatrixBlock m1, MatrixBlock m2,
		CompressedMatrixBlock ret, BinaryOperator op, boolean left) {
		final List<AColGroup> oldColGroups = m1.getColGroups();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
	}

	protected static AIterator[] getIterators(final List<AColGroup> groups, final int rl) {
		final AIterator[] its = new AIterator[groups.size()];
		for(int i = 0; i < groups.size(); i++) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/MultiColBitmap.java`
#### Snippet
```java
	 * @param rows         The number of rows encoded
	 */
	protected MultiColBitmap(IntArrayList[] offsetsLists, double[][] values, int rows) {
		super(offsetsLists, rows);
		_values = values;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/Bitmap.java`
#### Snippet
```java
	 * @param rows         The number of rows encoded
	 */
	protected Bitmap(IntArrayList[] offsetsLists, double[] values, int rows) {
		super(offsetsLists, rows);
		_values = values;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetFactory.java`
#### Snippet
```java
	}

	protected static boolean getNoZero(byte[] off) {
		boolean noZero = true;
		for(byte b : off)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetFactory.java`
#### Snippet
```java
	}

	protected static boolean getNoZero(char[] off) {
		boolean noZero = true;
		for(char b : off)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetFactory.java`
#### Snippet
```java
	}

	protected static boolean getNoOverHalf(byte[] off) {
		boolean noOverHalf = true;
		for(byte b : off)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		public final AOffset offsetSlice;

		protected OffsetSliceInfo(int l, int u, AOffset off) {
			this.lIndex = l;
			this.uIndex = u;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/transform/TfUtils.java`
#### Snippet
```java
		ORDINAL,
		DUMMYCODED;
		protected byte toID() { 
			switch(this) {
				case SCALE: return 1;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FrameAppendCPInstruction.java`
#### Snippet
```java
public final class FrameAppendCPInstruction extends AppendCPInstruction {

	protected FrameAppendCPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, AppendType type,
		String opcode, String istr) {
		super(op, in1, in2, out, type, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixAppendCPInstruction.java`
#### Snippet
```java
public final class MatrixAppendCPInstruction extends AppendCPInstruction {

	protected MatrixAppendCPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, AppendType type,
		String opcode, String istr) {
		super(op, in1, in2, out, type, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FrameIndexingCPInstruction.java`
#### Snippet
```java
public final class FrameIndexingCPInstruction extends IndexingCPInstruction {

	protected FrameIndexingCPInstruction(CPOperand in, CPOperand rl, CPOperand ru, CPOperand cl,
			CPOperand cu, CPOperand out, String opcode, String istr) {
		super(in, rl, ru, cl, cu, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FrameIndexingCPInstruction.java`
#### Snippet
```java
	}

	protected FrameIndexingCPInstruction(CPOperand lhsInput, CPOperand rhsInput, CPOperand rl,
			CPOperand ru, CPOperand cl, CPOperand cu, CPOperand out, String opcode, String istr) {
		super(lhsInput, rhsInput, rl, ru, cl, cu, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ScalarAppendCPInstruction.java`
#### Snippet
```java
public final class ScalarAppendCPInstruction extends AppendCPInstruction {

	protected ScalarAppendCPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out,
			AppendType type, String opcode, String istr) {
		super(op, in1, in2, out, type, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListAppendRemoveCPInstruction.java`
#### Snippet
```java
	private CPOperand output2 = null;
	
	protected ListAppendRemoveCPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out,
			AppendType type, String opcode, String istr) {
		super(op, in1, in2, out, type, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixIndexingCPInstruction.java`
#### Snippet
```java
	}

	protected MatrixIndexingCPInstruction(CPOperand lhsInput, CPOperand rhsInput, CPOperand rl,
			CPOperand ru, CPOperand cl, CPOperand cu, CPOperand out, String opcode, String istr) {
		super(lhsInput, rhsInput, rl, ru, cl, cu, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListIndexingCPInstruction.java`
#### Snippet
```java
	}

	protected ListIndexingCPInstruction(CPOperand lhsInput, CPOperand rhsInput, CPOperand rl,
			CPOperand ru, CPOperand cl, CPOperand cu, CPOperand out, String opcode, String istr) {
		super(lhsInput, rhsInput, rl, ru, cl, cu, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListIndexingCPInstruction.java`
#### Snippet
```java
public final class ListIndexingCPInstruction extends IndexingCPInstruction {

	protected ListIndexingCPInstruction(CPOperand in, CPOperand rl, CPOperand ru, CPOperand cl,
			CPOperand cu, CPOperand out, String opcode, String istr) {
		super(in, rl, ru, cl, cu, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java

public final class IndexingFEDInstruction extends UnaryFEDInstruction {
	protected final CPOperand rowLower, rowUpper, colLower, colUpper;

	protected IndexingFEDInstruction(CPOperand in, CPOperand rl, CPOperand ru, CPOperand cl, CPOperand cu,
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java
	protected final CPOperand rowLower, rowUpper, colLower, colUpper;

	protected IndexingFEDInstruction(CPOperand in, CPOperand rl, CPOperand ru, CPOperand cl, CPOperand cu,
		CPOperand out, String opcode, String istr) {
		super(FEDInstruction.FEDType.MatrixIndexing, null, in, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java
	}

	protected IndexRange getIndexRange(ExecutionContext ec) {
		return new IndexRange( // rl, ru, cl, ru
			(int) (ec.getScalarInput(rowLower).getLongValue() - 1),
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java
	}

	protected IndexingFEDInstruction(CPOperand lhsInput, CPOperand rhsInput, CPOperand rl, CPOperand ru, CPOperand cl,
		CPOperand cu, CPOperand out, String opcode, String istr) {
		super(FEDInstruction.FEDType.MatrixIndexing, null, lhsInput, rhsInput, out, opcode, istr);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionSparkScheme.java`
#### Snippet
```java
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels;

		protected Result(List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pFeatures, List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels) {
			this.pFeatures = pFeatures;
			this.pLabels = pLabels;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionSparkScheme.java`
#### Snippet
```java

	protected final class Result {
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pFeatures; // WorkerID => (rowID, matrix)
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionSparkScheme.java`
#### Snippet
```java
	protected final class Result {
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pFeatures; // WorkerID => (rowID, matrix)
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels;

		protected Result(List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pFeatures, List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels) {
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
	private LocalParamServer _server;

	protected PSRpcHandler(LocalParamServer server) {
		_server = server;
	}
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/api/PythonDMLScript.java`
#### Snippet
```java
		}
		catch(Py4JNetworkException p4e) {
			/**
			 * This sometimes happens when the startup is using a port already in use. In this case we handle it in python
			 * therefore use logging framework. and terminate program.
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	private static final long CP_CSV_REBLOCK_UNKNOWN_THRESHOLD_SIZE = CP_REBLOCK_THRESHOLD_SIZE;
	
	/** Local reused rewriter for dynamic rewrites during recompile */

	/** Local DML configuration for thread-local config updates */
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
			long D = blkIn.getNonZeros() + 1;

			/**
			 * To ensure that the likelihood to hash to the same value we need O(D^2) positions to hash to assign. If the
			 * value is higher than int (which is the area we hash to) then use Integer Max value as largest hashing space.
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
			long tmp = D * D;
			int M = (tmp > (long) Integer.MAX_VALUE) ? Integer.MAX_VALUE : (int) tmp;
			/**
			 * The estimator is asymptotically unbiased as k becomes large, but memory usage also scales with k. Furthermore k
			 * value must be within range: D >> k >> 0
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibSlice.java`
#### Snippet
```java

	private static MatrixBlock sliceInternal(CompressedMatrixBlock cmb, int rl, int ru, int cl, int cu, boolean deep) {
		/**
		 * In the case where an internal matrix is sliced out, then first slice out the columns to an compressed
		 * intermediate. Then call slice recursively, to do the row slice. Since we do not copy the index structure but
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibLeftMultBy.java`
#### Snippet
```java
		int off, int skip, double[] rowSum) {

		/** The column block size for preAggregating column groups */
		final int colBZ = 1024;
		// The number of rows to process together
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	}

	/**
	 * Partition columns such that we make sure that the columns we analyze further actually have the potential of
	 * compressing together.
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	// }

	/**
	 * Pack into the bins such that no bin exceed the Bin Capacity. This ensure that each bin only contains columns that
	 * are amenable to compression. And potentially CoCoding.
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodeBinPacking.java`
#### Snippet
```java
	// }

	/**
	 * This methods verifies the coCoded bins actually are the best combinations within each individual Group based on
	 * the sample.
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AMorphingMMColGroup.java`
#### Snippet
```java
		ADictionary preAgg);

	/** extract common value from group and return non morphing group */
	/**
	 * extract common value from group and return non morphing group
```

### DanglingJavadoc
Dangling Javadoc comment
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
	protected final HashMap<Pointer, PointerInfo> allPointers = new HashMap<>();
	
	/*****************************************************************************************/
	

```

## RuleId[id=SwitchStatementWithConfusingDeclaration]
### SwitchStatementWithConfusingDeclaration
Local variable `sparsity` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
				//   worst case #nnz in C = m => sparsity = 1/m
				// for ctable_histogram also one dimension is known
				double sparsity = OptimizerUtils.getSparsity(dim1, dim2, (nnz<=dim1)?nnz:dim1); 
				return OptimizerUtils.estimateSizeExactSparsity(dim1, dim2, sparsity);
				
```

### SwitchStatementWithConfusingDeclaration
Local variable `rowsLong` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			}
				
			long rowsLong = -1L, colsLong = -1L;
			
			Expression rowsExpr = getVarParam(RAND_ROWS);
```

### SwitchStatementWithConfusingDeclaration
Local variable `colsLong` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			}
				
			long rowsLong = -1L, colsLong = -1L;
			
			Expression rowsExpr = getVarParam(RAND_ROWS);
```

### SwitchStatementWithConfusingDeclaration
Local variable `rowsExpr` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			long rowsLong = -1L, colsLong = -1L;
			
			Expression rowsExpr = getVarParam(RAND_ROWS);
			Expression colsExpr = getVarParam(RAND_COLS);
			if (!isTensorOperation) {
```

### SwitchStatementWithConfusingDeclaration
Local variable `colsExpr` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			
			Expression rowsExpr = getVarParam(RAND_ROWS);
			Expression colsExpr = getVarParam(RAND_COLS);
			if (!isTensorOperation) {
				///////////////////////////////////////////////////////////////////
```

### SwitchStatementWithConfusingDeclaration
Local variable `exp` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			
			//validate correct value types
			Expression exp = getVarParam(SQL_CONN);
			if( !(exp instanceof StringIdentifier) && exp instanceof Identifier ) {
				raiseValidateError("for tensor statement " + SQL_CONN + " has incorrect value type", conditional);
```

### SwitchStatementWithConfusingDeclaration
Local variable `method` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		case RAND:
			// We limit RAND_MIN, RAND_MAX, RAND_SPARSITY, RAND_SEED, and RAND_PDF to be constants
			OpOpDG method = (paramHops.get(DataExpression.RAND_MIN).getValueType()==ValueType.STRING &&
					target.getDataType() == DataType.MATRIX) ? OpOpDG.SINIT : OpOpDG.RAND;
			currBuiltinOp = new DataGenOp(method, target, paramHops);
```

### SwitchStatementWithConfusingDeclaration
Local variable `resWithCorrection` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java
		switch(op.aggOp.correction) {
			case LASTCOLUMN:
				MatrixBlock resWithCorrection = new MatrixBlock(ret.getNumRows(), ret.getNumColumns() + 1, false);
				resWithCorrection.allocateDenseBlock();
				for(int i = 0; i < ret.getNumRows(); i++)
```

### SwitchStatementWithConfusingDeclaration
Local variable `rows` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
					break;
				case MATRIX:
					long rows = (long) in.readObject();
					long cols = (long) in.readObject();
					int blockSize = (int) in.readObject();
```

### SwitchStatementWithConfusingDeclaration
Local variable `cols` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
				case MATRIX:
					long rows = (long) in.readObject();
					long cols = (long) in.readObject();
					int blockSize = (int) in.readObject();
					long nonZeros = (long) in.readObject();
```

### SwitchStatementWithConfusingDeclaration
Local variable `blockSize` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
					long rows = (long) in.readObject();
					long cols = (long) in.readObject();
					int blockSize = (int) in.readObject();
					long nonZeros = (long) in.readObject();
					Types.FileFormat fileFormat = (Types.FileFormat) in.readObject();
```

### SwitchStatementWithConfusingDeclaration
Local variable `nonZeros` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
					long cols = (long) in.readObject();
					int blockSize = (int) in.readObject();
					long nonZeros = (long) in.readObject();
					Types.FileFormat fileFormat = (Types.FileFormat) in.readObject();

```

### SwitchStatementWithConfusingDeclaration
Local variable `dc` declared in one 'switch' branch and used in another
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
					MatrixObject mo = (MatrixObject) d;
					MetaDataFormat md = (MetaDataFormat) mo.getMetaData();
					DataCharacteristics dc = md.getDataCharacteristics();

					out.writeObject(dc.getRows());
```

## RuleId[id=TrivialIf]
### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/FunctionOp.java`
#### Snippet
```java
	@Override
	public boolean isGPUEnabled() {
		if(getFunctionName().equalsIgnoreCase("lstm") || getFunctionName().equalsIgnoreCase("lstm_backward") ||  
			getFunctionName().equalsIgnoreCase("batch_norm2d") || getFunctionName().equalsIgnoreCase("batch_norm2d_backward") ||
			getFunctionName().equalsIgnoreCase("batch_norm2d_train") || getFunctionName().equalsIgnoreCase("batch_norm2d_test")) 
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
	@Override
	public boolean isGPUEnabled() {
		if(!DMLScript.USE_ACCELERATOR)
			return false;
		return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
	private boolean isOuterProduct() {
		if ( getInput().get(0).isVector() && getInput().get(1).isVector() ) {
			if ( getInput().get(0).getDim1() == 1 && getInput().get(0).getDim1() > 1
					&& getInput().get(1).getDim1() > 1 && getInput().get(1).getDim2() == 1 )
				return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		
		//right side cached (no agg if left has just one column block)
		if(  method == MMultMethod.MAPMM_R && getInput().get(0).getDim2() >= 0 //known num columns
			&& getInput().get(0).getDim2() <= getInput().get(0).getBlocksize() ) 
		{
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/SpoofFusedOp.java`
#### Snippet
```java
	@Override
	public boolean isGPUEnabled() {
		if(_api == GeneratorAPI.CUDA)
			return true;
		else
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		}
		
		if(expectedOneMinusMu == (1-mu) && plusOp.getParent() != null && plusOp.getParent().size() == 1 &&  
			isBinarySMMult(lhsTimesOp) && OptimizerUtils.rEvalSimpleDoubleExpression(getFirstInput(lhsTimesOp), new HashMap<>()) == mu) {
			return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
		}
		
		if(plusOp.getParent() != null && plusOp.getParent().size() == 1 && isBinarySMMult(lhsTimesOp)) {
			return true;
		}
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/lops/OperatorOrderingUtils.java`
#### Snippet
```java
			return true;
		//TODO: Handle internal builtins (e.g. eigen)
		if (lop instanceof FunctionCallCP &&
			((FunctionCallCP) lop).getFnamespace().equalsIgnoreCase(DMLProgram.INTERNAL_NAMESPACE)) {
			return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
	
	public static boolean isTsmm(Hop input) {
		if( isMatrixMultiply(input) && isTransposeOfItself(
			input.getInput().get(0), input.getInput().get(1)) )
			return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/parser/AssignmentStatement.java`
#### Snippet
```java
		if (_source instanceof FunctionCallIdentifier)
			return true;
		if (_source.toString().contains(Builtins.TIME.toString()))
			return true;
		
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/parser/DataIdentifier.java`
#### Snippet
```java
		if(!(this.getDim1() == target.getDim1()))
			return false;
		if(!(this.getDim2() == target.getDim2()))
			return false;
		return true;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					}
				}
				if (!rIsInlineableFunction(fblock, dmlProg))
					return false;
			}
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java

				//check if function can be inlined
				if( rIsInlineableFunction(fblock, dmlProg) ) {
					return true;
				}
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				
				boolean isMatrix = false;
				if ( dataTypeString == null || dataTypeString.equalsIgnoreCase(Statement.MATRIX_DATA_TYPE))
						isMatrix = true;
				
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/io/BinaryBlockSerialization.java`
#### Snippet
```java
		boolean ret = false;
		
		if( //arg0 == MatrixIndexes.class || //binary block key
			arg0 == MatrixBlock.class  ) //binary block 
		{
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			return false;
		}
		else if(cl > bCUpperIndex || cu < bCLowerIndex) {
			return false;
		}
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static boolean isInFrameBlockRange( Long ix, int blen, long rl, long ru )
	{
		if(rl > ix+blen-1 || ru < ix)
			return false;
		else
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/matrix/data/DnnParameters.java`
#### Snippet
```java
	
	public boolean compare(DnnParameters that) {
		if(this.N == that.N && this.C == that.C && this.H == that.H && this.W == that.W
				&& this.K == that.K && this.R == that.R && this.S == that.S && this.stride_h == that.stride_h
				 && this.stride_w == that.stride_w  && this.pad_h == that.pad_h
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	
			boolean bPrimeCumSum = false;
			if(bOp.fn instanceof GreaterThan || bOp.fn instanceof GreaterThanEquals )
				bPrimeCumSum = true;
			
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		boolean bSupported = false;
		
		if(isCompareOperator(bOp)
			&& 
				(uaggOp.aggOp.increOp.fn instanceof KahanPlus					// Kahanplus
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
	
			boolean bPrimeCumSum = false;
			if(bOp.fn instanceof LessThan || bOp.fn instanceof LessThanEquals )
				bPrimeCumSum = true;
			
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
		// return true if the output matrixobject is federated
		if (inst instanceof ComputationFEDInstruction)
			if (data instanceof MatrixObject && ((MatrixObject) data).isFederated())
				return true;
		return false;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			ec.getMatrixObject(in2.getName()).hasBroadcastHandle()))
			return false;
		if (in3 != null && (!ec.getMatrixObject(in3.getName()).hasRDDHandle() ||
			ec.getMatrixObject(in3.getName()).hasBroadcastHandle()))
			return false;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/privacy/propagation/PrivacyPropagator.java`
#### Snippet
```java
		else if (privacyConstraint1 != null)
			return privacyConstraint1;
		else if (privacyConstraint2 != null)
			return privacyConstraint2;
		return null;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/instructions/cp/BinaryMatrixMatrixCPInstruction.java`
#### Snippet
```java
			else {
				((BinaryOperator) op).setNumThreads(Integer.parseInt(parts[parts.length - 2]));
				if(parts[parts.length - 1].equals("InPlace"))
					inplace = true;
				else
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		}
		boolean sparse = false;
		if(isDensePointerNull())
			sparse = true;
		MatrixBlock tmp = null;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public boolean isFederated(CPOperand input, FType type) {
		Data data = getVariable(input);
		if(data instanceof CacheableData && ((CacheableData<?>) data).isFederated(type))
			return true;
		return false;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public boolean isFederated(CPOperand input) {
		Data data = getVariable(input);
		if(data instanceof CacheableData && ((CacheableData<?>) data).isFederated())
			return true;
		return false;
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		
		//check for colwise overwrite
		if(   (inpColL.getName().equals(iterVarname) && inpColU.getName().equals(iterVarname))
		   && inpRowL instanceof LiteralOp && HopRewriteUtils.getDoubleValueSafe((LiteralOp)inpRowL)==1
		   && inpRowU instanceof LiteralOp && HopRewriteUtils.getDoubleValueSafe((LiteralOp)inpRowU)==mo.getNumRows() )
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
		// and recompile the program block
		try {
			if(forceExecTypeCP)
				rAssignParallelismAndRecompile(pbs, k, true, forceExecTypeCP);
			else
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      if (!getRowsList()
          .equals(other.getRowsList())) return false;
      if (!unknownFields.equals(other.unknownFields)) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java

      if (!valueType_.equals(other.valueType_)) return false;
      if (!unknownFields.equals(other.unknownFields)) return false;
      return true;
    }
```

### TrivialIf
`if` statement can be simplified
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
      if (!getColumnSchemaList()
          .equals(other.getColumnSchemaList())) return false;
      if (!unknownFields.equals(other.unknownFields)) return false;
      return true;
    }
```

## RuleId[id=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for parameter 'fedType'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
	}

	public static MatrixBlock aggMinMax(Future<FederatedResponse>[] ffr, boolean isMin, boolean isScalar, Optional<FType> fedType) {
		try {
			if (!fedType.isPresent() || fedType.get() == FType.OTHER) {
```

## RuleId[id=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	public static String getCPHeavyHittersMem(int num) {
		if ((_cpMemObjs.size() <= 0) || (num <= 0))
			return "-";

```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	public static String getHeavyHitters(int num) {
		if (num <= 0 || _instStats.size() <= 0)
			return "-";

```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java

			final AOffset offsets = OffsetFactory.createOffset(sb.indexes(sbRow), apos, alen);
			if(entries.length <= 1)
				return ColGroupSDCSingleZeros.create(cols, nRow, Dictionary.create(dict), offsets, counts);
			else {
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ColIndexFactory.java`
#### Snippet
```java

	public static IColIndex create(int[] indexes) {
		if(indexes.length <= 0)
			throw new DMLRuntimeException("Invalid length to create index from : " + indexes.length);
		if(indexes.length == 1)
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerConstrained.java`
#### Snippet
```java

			//partitioned matrix
			if( partitionedMatrices.size()<=0 ) {
				LOG.debug(getOptMode()+" OPT: unable to force 'set fused data partitioning and execution' - result="+false );
				return;
```

## RuleId[id=CharsetObjectCanBeUsed]
### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
			DocumentBuilder builder = DocumentBuilderFactory.newInstance().newDocumentBuilder();
			Document domTree = null;
			domTree = builder.parse( new ByteArrayInputStream(content.getBytes("utf-8")) );
			Element root = domTree.getDocumentElement();
			ret = new DMLConfig( root );
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
				DocumentBuilder builder = getDocumentBuilder();
				String configString = "<root><" + paramName + ">"+paramValue+"</" + paramName + "></root>";
				_document = builder.parse(new ByteArrayInputStream(configString.getBytes("UTF-8")));
				_xmlRoot = _document.getDocumentElement();
			} catch (Exception e) {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `src/main/java/org/apache/sysds/runtime/io/IOUtilFunctions.java`
#### Snippet
```java
			for( int len=0; (len=input.read(buff))!=-1; )
				bos.write(buff, 0, len);
			return bos.toString("UTF-8");
		}
		finally {
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
						IOUtils.copyBytes(in, out, conf, false);
						if (addString!=null)
							out.write(addString.getBytes("UTF-8"));
					} finally {
						in.close();
```

### CharsetObjectCanBeUsed
StandardCharsets.UTF_8 can be used instead
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
				throw new IOException("Recode map for column '"+name+"' (id="+colID+") not existing.");
			
			InputStream is = new ByteArrayInputStream(map.getBytes("UTF-8"));
			BufferedReader br = new BufferedReader(new InputStreamReader(is));
			Pair<String,String> pair = new Pair<>();
```

## RuleId[id=ComparatorMethodParameterNotUsed]
### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `src/main/java/org/apache/sysds/lops/compile/LopComparator.java`
#### Snippet
```java
	
	@Override
	public int compare(N o1, N o2) {
		if (o1.getLevel() < o2.getLevel())
			return -1; // o1 is less than o2
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		}
		//find format with the most occurences in values -> dominant format
		String dominantFormat = format_matches.entrySet().stream().max((entry1, entry2) -> entry1.getValue() > entry2.getValue() ? 1 : -1).get().getKey();
		for (int i = 0; i< values.length; i++){
			//skip null/blank entries
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderBin.java`
#### Snippet
```java
		@Override
		public int compareTo(ArrayContainer o) {
			return this.arr[this.index] < o.arr[o.index] ? -1 : 1;
		}
	}
```

### ComparatorMethodParameterNotUsed
Comparator does not return 0 for equal elements
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPULazyCudaFreeMemoryManager.java`
#### Snippet
```java
	synchronized public Pointer getRmvarPointerMinSize(String opcode, long minSize) throws DMLRuntimeException {
		Optional<Long> toClear = rmvarGPUPointers.entrySet().stream().filter(e -> e.getValue().size() > 0).map(e -> e.getKey())
				.filter(size -> size >= minSize).min((s1, s2) -> s1 < s2 ? -1 : 1);
		if(toClear.isPresent()) {
			Pointer A = remove(rmvarGPUPointers, toClear.get()); // remove from rmvarGPUPointers as you are not calling cudaFree
```

## RuleId[id=SuspiciousToArrayCall]
### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.hops.codegen.opt.ReachabilityGraph.CutSet\[\]' found
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
		_cutSets = cutSets.stream()
				.sorted(Comparator.comparing(p -> p.getRight()))
				.map(p -> p.getLeft()).toArray(CutSet[]::new);
		
		//created sorted order of materialization points
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.caching.MatrixObject\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					Stream<Object> tmp = Arrays.stream(results).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (dat instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					String fname = constructResultMergeFileName();
					ResultMerge<?> rm = createResultMerge(_resultMerge, out, in, fname, var._isAccum, ec);
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.caching.FrameObject\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					Stream<Object> tmp = Arrays.stream(results).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (dat instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					String fname = constructResultMergeFileName();
					ResultMerge<?> rm = createResultMerge(_resultMerge, out, in, fname, var._isAccum, ec);
```

### SuspiciousToArrayCall
Array of type 'org.apache.sysds.runtime.instructions.cp.Data\[\]' expected, 'org.apache.sysds.runtime.instructions.cp.ListObject\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					ListObject newList = new ListObject(oldList);
					ListObject[] in = Arrays.stream(results).map(vars -> 
						vars.get(var._name)).toArray(ListObject[]::new);
					
					//merge modified list entries into result
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.caching.MatrixObject\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					Stream<Object> tmp = Arrays.stream(_refVars).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (out instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					
					String fname = constructResultMergeFileName();
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.caching.FrameObject\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					Stream<Object> tmp = Arrays.stream(_refVars).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (out instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					
					String fname = constructResultMergeFileName();
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.federated.FederatedRange\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java

	public FederatedRange[] getFederatedRanges() {
		return _fedMap.stream().map(e -> e.getKey()).toArray(FederatedRange[]::new);
	}

```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.federated.FederatedRange\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
	public Future<FederatedResponse>[] execute(long tid, boolean wait, FederatedRequest[] frSlices1, FederatedRequest[] frSlices2, FederatedRequest... fr) {
		return execute(tid, wait,
			_fedMap.stream().map(e->e.getKey()).toArray(FederatedRange[]::new),
			null, frSlices1, frSlices2, fr);
	}
```

### SuspiciousToArrayCall
Array of type 'java.lang.Object\[\]' expected, 'org.apache.sysds.runtime.controlprogram.federated.FederatedData\[\]' found
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java

	public FederatedData[] getFederatedData() {
		return _fedMap.stream().map(e -> e.getValue()).toArray(FederatedData[]::new);
	}

```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `sparkExecutionContext.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		try {
			@SuppressWarnings("unchecked")
			JavaPairRDD<Long, FrameBlock> binaryBlocks = (JavaPairRDD<Long, FrameBlock>)
				sparkExecutionContext.getRDDHandleForFrameObject(frameObject, FileFormat.BINARY);
			return binaryBlocks;
```

### RedundantCast
Casting `sparkExecutionContext.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		try {
			@SuppressWarnings("unchecked")
			JavaPairRDD<MatrixIndexes, MatrixBlock> binaryBlocks = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
				sparkExecutionContext.getRDDHandleForMatrixObject(matrixObject, FileFormat.BINARY);
			return binaryBlocks;
```

### RedundantCast
Casting `sparkExecutionContext.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		try {
			@SuppressWarnings("unchecked")
			JavaPairRDD<Long, FrameBlock> binaryBlockFrame = (JavaPairRDD<Long, FrameBlock>)
				sparkExecutionContext.getRDDHandleForFrameObject(frameObject, FileFormat.BINARY);
			DataCharacteristics mc = frameObject.getDataCharacteristics();
```

### RedundantCast
Casting `sparkExecutionContext.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		try {
			@SuppressWarnings("unchecked")
			JavaPairRDD<MatrixIndexes, MatrixBlock> binaryBlocks = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
				sparkExecutionContext.getRDDHandleForMatrixObject(matrixObject, FileFormat.BINARY);
			DataCharacteristics mc = matrixObject.getDataCharacteristics();
```

### RedundantCast
Casting `l` to `Data` is redundant
in `src/main/java/org/apache/sysds/hops/DataOp.java`
#### Snippet
```java
				l = new Data(_op, getInput().get(0).constructLops(), inputLops, getName(), null, 
					getDataType(), getValueType(), getFileFormat());
				((Data)l).setExecType(et);
				setOutputDimensions(l);
				break;
```

### RedundantCast
Casting `getInput(...)` to `ReorgOp` is redundant
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		else if( isCTableReshapeRewriteApplicable(et, ternaryOp) ) {
			//table - reshape --> table
			inputLops[0] = ((ReorgOp)getInput(0)).getInput(0).constructLops();
			inputLops[1] = ((ReorgOp)getInput(1)).getInput(0).constructLops();
		}
```

### RedundantCast
Casting `getInput(...)` to `ReorgOp` is redundant
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
			//table - reshape --> table
			inputLops[0] = ((ReorgOp)getInput(0)).getInput(0).constructLops();
			inputLops[1] = ((ReorgOp)getInput(1)).getInput(0).constructLops();
		}
		
```

### RedundantCast
Casting `root` to `DataOp` is redundant
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassReplaceEvalFunctionCalls.java`
#### Snippet
```java
			{
				Hop eval = root.getInput(0);
				String outvar = ((DataOp)root).getName();
				
				//get function name and namespace
```

### RedundantCast
Casting `inst` to `VariableCPInstruction` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		double wtime = 0;
		//double wtime = getFSWriteTime( vs[2]._rlen, vs[2]._clen, (vs[2]._nnz<0)? 1.0:(double)vs[2]._nnz/vs[2]._rlen/vs[2]._clen );
		if( inst instanceof VariableCPInstruction && ((VariableCPInstruction)inst).getOpcode().equals("write") )
			wtime += getHDFSWriteTime(vs[2].getRows(), vs[2].getCols(), vs[2].getSparsity(), ((VariableCPInstruction)inst).getInput3().getName() );
		
```

### RedundantCast
Casting `getInput(...)` to `DataOp` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
	{
		int index = left ? 0 : 1;
		return !(getInput(index) instanceof DataOp && ((DataOp)getInput(index)).requiresCheckpoint())
			&& (!HopRewriteUtils.isTransposeOperation(getInput(index))
				|| (left && !isLeftTransposeRewriteApplicable(true)))
```

### RedundantCast
Casting `cnode` to `CNodeData` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeTpl.java`
#### Snippet
```java
		for(int i=startIndex, sPos=0, mPos=0; i < inputs.size(); i++) {
			CNode cnode = inputs.get(i);
			if( cnode instanceof CNodeData && ((CNodeData)cnode).isLiteral() )
				continue;
			newNames.put(((CNodeData)cnode).getHopID(), cnode.getDataType().isScalar() ?
```

### RedundantCast
Casting `node` to `CNodeData` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateUtils.java`
#### Snippet
```java
	
	public static CNodeData getLiteral(CNode node) {
		return ((CNodeData) node).isLiteral() ? (CNodeData)node :
			createCNodeData(new LiteralOp(node.getVarname()), true);
	}
```

### RedundantCast
Casting `node` to `CNodeData` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateUtils.java`
#### Snippet
```java
	
	public static boolean isLiteral(CNode node) {
		return node instanceof CNodeData && ((CNodeData)node).isLiteral();
	}
	
```

### RedundantCast
Casting `node` to `CNodeData` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateUtils.java`
#### Snippet
```java
	
	public static boolean isLiteral(CNode node, String val) {
		return isLiteral(node) && ((CNodeData)node).getVarname().equals(val);
	}
	
```

### RedundantCast
Casting `dat` to `CacheableData` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
				Data dat = vars.get(varname);
				if( dat != null && dat instanceof CacheableData<?> )
					_inputsMeta.put(varname, ((CacheableData<?>)dat).getMetaData());
			}
		}
```

### RedundantCast
Casting `inputParametersLops.get(...)` to `Data` is redundant
in `src/main/java/org/apache/sysds/lops/Data.java`
#### Snippet
```java
				&& inputParametersLops.get(DataExpression.IO_FILENAME) instanceof Data )
			{
				OutputParameters outParams = ((Data)inputParametersLops.get(DataExpression.IO_FILENAME)).getOutputParameters();
				String fName = outParams.getLabel();
				this.getOutputParameters().setFile_name(fName);
```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		DataOp tread = new DataOp(name, DataType.MATRIX, ValueType.FP64, OpOpData.TRANSIENTREAD,
			null, mo.getNumRows(), mo.getNumColumns(), mo.getNnz(), UpdateType.COPY,
			(int)mo.getBlocksize());
		tread.setVisited();
		copyLineNumbers(mo, tread);
```

### RedundantCast
Casting `getOutput()` to `IndexedIdentifier` is redundant
in `src/main/java/org/apache/sysds/parser/Identifier.java`
#### Snippet
```java
				if( getOutput().getDataType() == DataType.LIST ) {
					int dim1 = (((IndexedIdentifier)getOutput()).getRowUpperBound() == null) ? 1 : - 1;
					((IndexedIdentifier)getOutput()).setDimensions(dim1, 1);
				} 
				else { //default
```

### RedundantCast
Casting `getOutput()` to `IndexedIdentifier` is redundant
in `src/main/java/org/apache/sysds/parser/Identifier.java`
#### Snippet
```java
				else { //default
					IndexPair updatedIndices = ((IndexedIdentifier)getOutput()).calculateIndexedDimensions(ids, constVars, conditional);
					((IndexedIdentifier)getOutput()).setDimensions(updatedIndices._row, updatedIndices._col);
				}
			}
```

### RedundantCast
Casting `node` to `Data` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
			if (node.isDataExecLocation()
					&& ((Data) node).getOperationType().isRead()
					&& ((Data) node).getDataType() == DataType.SCALAR 
					&& node.getOutputParameters().getFile_name() == null ) {
				markedNodes.add(node);
```

### RedundantCast
Casting `node` to `Data` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
					&& ((Data) node).getOperationType().isTransient()
					&& ((Data) node).getOperationType().isRead()
					&& ((Data) node).getDataType() == DataType.MATRIX) {
				// "node" is considered as updated ONLY IF the old value is not used any more
				// So, make sure that this READ node does not feed into any (transient/persistent) WRITE
```

### RedundantCast
Casting `node` to `Data` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
					&& ((Data) node).getOperationType().isTransient()
					&& ((Data) node).getOperationType().isWrite()
					&& ((Data) node).getDataType() == DataType.MATRIX
					&& labelNodeMapping.containsKey(node.getOutputParameters().getLabel()) // check to make sure corresponding (i.e., with the same label/name) transient read is present
					&& !labelNodeMapping.containsValue(node.getInputs().get(0)) ){ // check to avoid cases where transient read feeds into a transient write
```

### RedundantCast
Casting `target` to `IndexedIdentifier` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				}
			}
			((IndexedIdentifier)target).setDimensions(targetSize._row, targetSize._col);
		}
		
```

### RedundantCast
Casting `currBlock` to `WhileStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			// recurse to children function statement blocks
			if (currBlock instanceof WhileStatementBlock){
				WhileStatement wstmt = (WhileStatement)((WhileStatementBlock)currBlock).getStatement(0);
				wstmt.setBody(mergeFunctionCalls(wstmt.getBody(),dmlProg));
			}
```

### RedundantCast
Casting `currBlock` to `ForStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java

			else if (currBlock instanceof ForStatementBlock){
				ForStatement fstmt = (ForStatement)((ForStatementBlock)currBlock).getStatement(0);
				fstmt.setBody(mergeFunctionCalls(fstmt.getBody(),dmlProg));
			}
```

### RedundantCast
Casting `currBlock` to `IfStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java

			else if (currBlock instanceof IfStatementBlock){
				IfStatement ifstmt = (IfStatement)((IfStatementBlock)currBlock).getStatement(0);
				ifstmt.setIfBody(mergeFunctionCalls(ifstmt.getIfBody(),dmlProg));
				ifstmt.setElseBody(mergeFunctionCalls(ifstmt.getElseBody(),dmlProg));
```

### RedundantCast
Casting `currBlock` to `FunctionStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java

			else if (currBlock instanceof FunctionStatementBlock){
				FunctionStatement functStmt = (FunctionStatement)((FunctionStatementBlock)currBlock).getStatement(0);
				functStmt.setBody(mergeFunctionCalls(functStmt.getBody(),dmlProg));
			}
```

### RedundantCast
Casting `expr.getLeft()` to `ConstIdentifier` is redundant
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		}
		else if (expr.getLeft() instanceof ConstIdentifier){
			filename = ((ConstIdentifier)expr.getLeft()).toString()+ filename;
		}
		else if (expr.getLeft() instanceof DataIdentifier 
```

### RedundantCast
Casting `expr.getRight()` to `ConstIdentifier` is redundant
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		// DRB: CHANGE
		else if (expr.getRight() instanceof ConstIdentifier){
			filename = filename + ((ConstIdentifier)expr.getRight()).toString();
		}
		else if (expr.getRight() instanceof DataIdentifier 
```

### RedundantCast
Casting `passedSB` to `WhileStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		
		if (passedSB instanceof WhileStatementBlock){
			WhileStatement ws = (WhileStatement) ((WhileStatementBlock)passedSB).getStatement(0);
			cp = ws.getConditionalPredicate();
		} 
```

### RedundantCast
Casting `dat` to `CacheableData` is redundant
in `src/main/java/org/apache/sysds/runtime/io/ListReader.java`
#### Snippet
```java
				
				if(dat instanceof CacheableData<?>) {
					((CacheableData<?>)dat).setMetaData(new MetaDataFormat(dc, fmt));
					((CacheableData<?>)dat).enableCleanup(false); // disable delete
				}
```

### RedundantCast
Casting `passedSB` to `IfStatementBlock` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		} 
		else if (passedSB instanceof IfStatementBlock) {
			IfStatement ws = (IfStatement) ((IfStatementBlock)passedSB).getStatement(0);
			cp = ws.getConditionalPredicate();
		}
```

### RedundantCast
Casting `currBuiltinOp` to `DataOp` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			((DataOp)currBuiltinOp).setInputBlocksize(target.getBlocksize());
		else if ( source.getOpCode() == DataExpression.DataOp.WRITE ) {
			((DataOp)currBuiltinOp).setPrivacy(hops.get(target.getName()).getPrivacy());
			if( source.getVarParam(DataExpression.ROWBLOCKCOUNTPARAM) != null )
				currBuiltinOp.setBlocksize(Integer.parseInt(
```

### RedundantCast
Casting `_rows[r]` to `SparseRowVector` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockMCSR.java`
#### Snippet
```java
			  && _rows[r].size() * SparseBlock.RESIZE_FACTOR1 
				  < ((SparseRowVector)_rows[r]).capacity() ) {
			  ((SparseRowVector)_rows[r]).compact();
		  }
		  else if(_rows[r] instanceof SparseRowScalar) {
```

### RedundantCast
Casting `nrows` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockMCSR.java`
#### Snippet
```java
		//If nnz < rows we have guaranteed also empty rows.
		double size = 16; //object
		size += MemoryEstimates.objectArrayCost((long)nrows); //references
		long sparseRowSize = 16; // object
		sparseRowSize += 4*4; // 3 integers + padding
```

### RedundantCast
Casting `block` to `TensorBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/data/TensorBlock.java`
#### Snippet
```java
		if( !(block instanceof TensorBlock) )
			throw new RuntimeException("TensorBlock.slice(int,int,int,int,CacheBlock) CacheBlock was no TensorBlock");
		TensorBlock tb = (TensorBlock) block;
		int[] dims = new int[_dims.length];
		dims[0] = ru - rl + 1;
```

### RedundantCast
Casting `dat.acquireRead()` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
				sb.append("\nMatrix:\n");
				MatrixObject dat = (MatrixObject) x;
				MatrixBlock matrix = (MatrixBlock) dat.acquireRead();
				sb.append(DataConverter.toString(matrix, sparse, separator, lineSeparator, rows, cols, decimal));
				dat.release();
```

### RedundantCast
Casting `dat.acquireRead()` to `TensorBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
				sb.append("\n");
				TensorObject dat = (TensorObject) x;
				TensorBlock tensor = (TensorBlock) dat.acquireRead();
				sb.append(DataConverter.toString(tensor, sparse, separator,
					lineSeparator, "[", "]", rows, cols, decimal));
```

### RedundantCast
Casting `dat.acquireRead()` to `FrameBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
				sb.append("\n");
				FrameObject dat = (FrameObject) x;
				FrameBlock frame = (FrameBlock) dat.acquireRead();
				sb.append(DataConverter.toString(frame, sparse, separator, lineSeparator, rows, cols, decimal));
				dat.release();
```

### RedundantCast
Casting `other.get()` to `long[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
		final long[] ret = new long[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((long[]) other.get(), 0, ret, this._size, other.size());
		if(other instanceof OptionalArray)
			return OptionalArray.appendOther((OptionalArray<Long>) other, new LongArray(ret));
```

### RedundantCast
Casting `value.get()` to `long[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
	@Override
	public void set(int rl, int ru, Array<Long> value, int rlSrc) {
		System.arraycopy((long[]) value.get(), rlSrc, _data, rl, ru - rl + 1);
	}

```

### RedundantCast
Casting `other.get()` to `double[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
		final double[] ret = new double[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((double[]) other.get(), 0, ret, this._size, other.size());
		if(other instanceof OptionalArray)
			return OptionalArray.appendOther((OptionalArray<Double>) other, new DoubleArray(ret));
```

### RedundantCast
Casting `value.get()` to `boolean[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BooleanArray.java`
#### Snippet
```java
	public void set(int rl, int ru, Array<Boolean> value, int rlSrc) {
		if(value instanceof BooleanArray)
			System.arraycopy((boolean[]) value.get(), rlSrc, _data, rl, ru - rl + 1);
		else
			for(int i = rl, off = rlSrc; i <= ru; i++, off++)
```

### RedundantCast
Casting `other.get()` to `char[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
		final char[] ret = new char[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((char[]) other.get(), 0, ret, this._size, other.size());
		if(other instanceof OptionalArray)
			return OptionalArray.appendOther((OptionalArray<Character>) other, new CharArray(ret));
```

### RedundantCast
Casting `_data[i]` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
		int[] ret = new int[size()];
		for(int i = 0; i < size(); i++)
			ret[i] = (int) _data[i];
		return new IntegerArray(ret);
	}
```

### RedundantCast
Casting `_data[i]` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
		final BitSet ret = new BitSet(size());
		for(int i = 0; i < size(); i++) {
			final int di = (int) _data[i];
			if(di != 0 && di != 1)
				throw new DMLRuntimeException("Unable to change to boolean from char array because of value:" //
```

### RedundantCast
Casting `_data[i]` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/CharArray.java`
#### Snippet
```java
		final boolean[] ret = new boolean[size()];
		for(int i = 0; i < size(); i++) {
			final int di = (int) _data[i];
			if(di != 0 && di != 1)
				throw new DMLRuntimeException("Unable to change to boolean from char array because of value:" //
```

### RedundantCast
Casting `_data[i]` to `double` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
		double[] ret = new double[size()];
		for(int i = 0; i < size(); i++)
			ret[i] = (double) _data[i];
		return new DoubleArray(ret);
	}
```

### RedundantCast
Casting `other.get()` to `int[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
		final int[] ret = new int[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((int[]) other.get(), 0, ret, this._size, other.size());
		if(other instanceof OptionalArray)
			return OptionalArray.appendOther((OptionalArray<Integer>) other, new IntegerArray(ret));
```

### RedundantCast
Casting `s.charAt(...)` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		int javaIdx = idx - 1;
		if (javaIdx >= 0 && javaIdx < strlen) {
			c = (int)s.charAt(javaIdx);
		}
		return c;
```

### RedundantCast
Casting `MemoryEstimates.stringCost(...)` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ArrayFactory.java`
#### Snippet
```java
				// cannot be known since strings have dynamic length
				// lets assume something large to make it somewhat safe.
				return Array.baseMemoryCost() + (long) MemoryEstimates.stringCost(12) * _numRows;
			case CHARACTER:
				return Array.baseMemoryCost() + (long) MemoryEstimates.charArrayCost(_numRows);
```

### RedundantCast
Casting `_data[i]` to `double` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
		double[] ret = new double[size()];
		for(int i = 0; i < size(); i++)
			ret[i] = (double) _data[i];
		return new DoubleArray(ret);
	}
```

### RedundantCast
Casting `other.get()` to `float[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
		final float[] ret = new float[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((float[]) other.get(), 0, ret, this._size, other.size());
		if(other instanceof OptionalArray)
			return OptionalArray.appendOther((OptionalArray<Float>) other, new FloatArray(ret));
```

### RedundantCast
Casting `other.get()` to `String[]` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		final String[] ret = new String[endSize];
		System.arraycopy(_data, 0, ret, 0, this._size);
		System.arraycopy((String[]) other.get(), 0, ret, this._size, other.size());
		return new StringArray(ret);
	}
```

### RedundantCast
Casting `ret` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		// Output matrix will have the same sparsity as that of the input matrix.
		// (assuming a uniform distribution of non-zeros in the input)
		MatrixBlock result=checkType((MatrixBlock)ret);
		long estnnz= (long) ((double)this.nonZeros/rlen/clen*(ru-rl+1)*(cu-cl+1));
		boolean result_sparsity = this.sparse && MatrixBlock.evalSparseFormatInMemory(ru-rl+1, cu-cl+1, estnnz);
```

### RedundantCast
Casting `outlist.get(...).getValue()` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		//case 1: copy lhs and rhs to output
		if( cbind && clen==blen || !cbind && rlen==blen ) {
			((MatrixBlock) outlist.get(0).getValue()).copy(this);
			((MatrixBlock) outlist.get(1).getValue()).copy(m2);
		}
```

### RedundantCast
Casting `outlist.get(...).getValue()` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		if( cbind && clen==blen || !cbind && rlen==blen ) {
			((MatrixBlock) outlist.get(0).getValue()).copy(this);
			((MatrixBlock) outlist.get(1).getValue()).copy(m2);
		}
		//case 2: append part of rhs to lhs, append to 2nd output if necessary
```

### RedundantCast
Casting `tmpIMV.getValue()` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
					IndexedMatrixValue tmpIMV = new IndexedMatrixValue(new MatrixIndexes(),new MatrixBlock());
					tmpIMV.getIndexes().setIndexes(tmpIx);
					((MatrixBlock)tmpIMV.getValue()).reset((int)Math.min(blen, rlen-((brix-1)*blen)), (int)clen);
					out.put(tmpIMV.getIndexes(), tmpIMV);
				}
```

### RedundantCast
Casting `tmpIMV.getValue()` to `MatrixBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
					IndexedMatrixValue tmpIMV = new IndexedMatrixValue(new MatrixIndexes(),new MatrixBlock());
					tmpIMV.getIndexes().setIndexes(tmpIx);
					((MatrixBlock)tmpIMV.getValue()).reset((int)rlen,(int)Math.min(blen, clen-((bcix-1)*blen)));
					out.put(tmpIMV.getIndexes(), tmpIMV);
				}
```

### RedundantCast
Casting `udf` to `LineageTraceable` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
		if (!(udf instanceof LineageTraceable))
			throw new DMLRuntimeException("Unknown Federated UDF (" + udf.getClass().getSimpleName() + ") traced.");
		LineageTraceable ludf = (LineageTraceable) udf;
		if (ludf.hasSingleLineage()) {
			Pair<String, LineageItem> item = udf.getLineageItem(ec);
```

### RedundantCast
Casting `inst` to `ParameterizedBuiltinCPInstruction` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			case ParameterizedBuiltin:  //groupedagg (sum, count)
			{
				String opcode = ((ParameterizedBuiltinCPInstruction)inst).getOpcode();
				HashMap<String, String> params = ((ParameterizedBuiltinCPInstruction)inst).getParameterMap();
				long r1 = ec.getMatrixObject(params.get(Statement.GAGG_TARGET)).getNumRows();
```

### RedundantCast
Casting `colGroup` to `AColGroupValue` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlockFactory.java`
#### Snippet
```java
										+ colGroup.estimateInMemorySize()
										+ ((colGroup instanceof AColGroupValue) ? "  numValues :"
											+ ((AColGroupValue) colGroup).getNumValues() : "")
										+ "  colIndexes : " + colGroup.getColIndices());
								}
```

### RedundantCast
Casting `ac` to `AColGroupCompressed` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCombineGroups.java`
#### Snippet
```java
			SparseEncoding sed = (SparseEncoding) ce;
			ADictionary cd = DictionaryFactory.combineDictionariesSparse(ac, bc);
			double[] defaultTuple = constructDefaultTuple((AColGroupCompressed) ac, (AColGroupCompressed) bc);
			return ColGroupSDC.create(combinedColumns, sed.getNumRows(), cd, defaultTuple, sed.getOffsets(), sed.getMap(),
				null);
```

### RedundantCast
Casting `bc` to `AColGroupCompressed` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCombineGroups.java`
#### Snippet
```java
			SparseEncoding sed = (SparseEncoding) ce;
			ADictionary cd = DictionaryFactory.combineDictionariesSparse(ac, bc);
			double[] defaultTuple = constructDefaultTuple((AColGroupCompressed) ac, (AColGroupCompressed) bc);
			return ColGroupSDC.create(combinedColumns, sed.getNumRows(), cd, defaultTuple, sed.getOffsets(), sed.getMap(),
				null);
```

### RedundantCast
Casting `m1.getNumRows()` to `double` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java

	private static void divideByNumberOfCellsForMeanCols(CompressedMatrixBlock m1, MatrixBlock ret) {
		double div = (double) m1.getNumRows();

		if(ret.isInSparseFormat()) {
```

### RedundantCast
Casting `Math.max(...)` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstFactory.java`
#### Snippet
```java

		// adhere to maximum sample size.
		sampleSize = (int) Math.max(minSampleSize, Math.min(sampleSize, maxSampleSize));

		return sampleSize;
```

### RedundantCast
Casting `def` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
			else {
				double[] retDef = new double[max];
				retDef[((int) def) - 1] = 1;
				return ColGroupConst.create(retDef);
			}
```

### RedundantCast
Casting `def` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
			else {
				double[] retDef = new double[max];
				retDef[((int) def) - 1] = 1;
				return ColGroupDDCFOR.create(outCols, d, _data, getCachedCounts(), retDef);
			}
```

### RedundantCast
Casting `other` to `ColGroupUncompressed` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
		LOG.warn("Inefficient multiplication with uncompressed column group");
		final int nCols = result.getNumColumns();
		final MatrixBlock otherMBT = LibMatrixReorg.transpose(((ColGroupUncompressed) other).getData());
		final int nRows = otherMBT.getNumRows();
		final MatrixBlock tmp = new MatrixBlock(nRows, nCols, false);
```

### RedundantCast
Casting `def` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
			else {
				double[] retDef = new double[max];
				retDef[((int) def) - 1] = 1;
				return ColGroupSDCSingle.create(ColIndexFactory.create(max), nRows, Dictionary.create(new double[max]),
					retDef, indexes, null);
```

### RedundantCast
Casting `Math.min(...)` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
		int lMRows = lg.numRowsToMultiply();
		int rMRows = this.numRowsToMultiply();
		long commonDim = (long) Math.min(lMRows, rMRows);
		long directFLOPS = commonDim * nColL * nColR * 2; // times 2 for first add then multiply

```

### RedundantCast
Casting `def` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
			else {
				double[] retDef = new double[max];
				retDef[((int) def) - 1] = 1;
				return ColGroupSDC.create(outCols, nRows, d, retDef, indexes, data, counts);
			}
```

### RedundantCast
Casting `g` to `ColGroupSDC` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCFOR.java`
#### Snippet
```java
	public static AColGroup sparsifyFOR(ColGroupSDC g) {
		// subtract default.
		final double[] constV = ((ColGroupSDC) g)._defaultTuple;
		final AColGroupValue clg = (AColGroupValue) g.subtractDefaultTuple();
		return create(g.getColIndices(), g._numRows, clg._dict, g._indexes, g._data, g.getCachedCounts(), constV);
```

### RedundantCast
Casting `Character.MAX_VALUE` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetChar.java`
#### Snippet
```java

	private static final long serialVersionUID = -1192266421395964882L;
	protected static final int maxV = (int) Character.MAX_VALUE;

	private final char[] offsets;
```

### RedundantCast
Casting `(_values[valOff + i] * _values[valOff + i])` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/QDictionary.java`
#### Snippet
```java
		double res = 0.0;
		for(int i = 0; i < nrColumns; i++)
			res += (int) (_values[valOff + i] * _values[valOff + i]) * _scale * _scale;
		return res;
	}
```

### RedundantCast
Casting `X.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryFEDInstruction.java`
#### Snippet
```java
			.getNumColumns() : (U.getNumColumns() == V.getNumRows() ? V.getNumColumns() : V.getNumRows());
		MatrixObject out = ec.getMatrixObject(output);
		out.getDataCharacteristics().set(rows, cols, (int) X.getBlocksize());
	}
}
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/VariableFEDInstruction.java`
#### Snippet
```java
		// Construct output local.
		FrameObject out = ec.getFrameObject(_in.getOutput());
		out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr1.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CastFEDInstruction.java`
#### Snippet
```java
		// Construct output local.
		FrameObject out = ec.getFrameObject(output);
		out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
		FederationMap outMap = mo1.getFedMapping().copyWithNewID(fr2.getID());
		List<Pair<FederatedRange, FederatedData>> newMap = new ArrayList<>();
```

### RedundantCast
Casting `inst` to `MMChainCPInstruction` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMChainFEDInstruction.java`
#### Snippet
```java

	public static MMChainFEDInstruction parseInstruction(MMChainCPInstruction inst, ExecutionContext ec) {
		MMChainCPInstruction linst = (MMChainCPInstruction) inst;
		MatrixObject mo = ec.getMatrixObject(linst.input1);
		if( mo.isFederated(FType.ROW) )
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/TsmmFEDInstruction.java`
#### Snippet
```java
		MatrixObject out = ec.getMatrixObject(output);
		out.getDataCharacteristics()
			.set(mo1.getNumColumns(), mo1.getNumColumns(), (int) mo1.getBlocksize());
		FederationMap outputFedMap = mo1.getFedMapping()
			.copyWithNewIDAndRange(mo1.getNumColumns(), mo1.getNumColumns(), fr1.getID(), outFType);
```

### RedundantCast
Casting `X.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
		}
		out.setFedMapping(outFedMap);
		out.getDataCharacteristics().set(rows, cols, (int) X.getBlocksize());
	}
}
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMFEDInstruction.java`
#### Snippet
```java
		long outputID, ExecutionContext ec){
		MatrixObject out = ec.getMatrixObject(output);
		out.getDataCharacteristics().set(mo1.getNumRows(), mo2.getNumColumns(), (int)mo1.getBlocksize());
		FederationMap outputFedMap = federationMap
			.copyWithNewIDAndRange(mo1.getNumRows(), mo2.getNumColumns(), outputID);
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMFEDInstruction.java`
#### Snippet
```java
		long outputID, ExecutionContext ec){
		MatrixObject out = ec.getMatrixObject(output);
		out.getDataCharacteristics().set(mo1.getNumRows(), mo2.getNumColumns(), (int)mo1.getBlocksize());
		out.setFedMapping(federationMap.copyWithNewID(outputID, mo2.getNumColumns()));
	}
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReshapeFEDInstruction.java`
#### Snippet
```java
			//derive output federated mapping
			MatrixObject out = ec.getMatrixObject(output);
			out.getDataCharacteristics().set(rows, cols, (int) mo1.getBlocksize(), mo1.getNnz());
			out.setFedMapping(reshapedFedMap.copyWithNewID(fr1[0].getID()));
		}
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/BinaryMatrixMatrixFEDInstruction.java`
#### Snippet
```java
	private void setOutputFedMappingPart(MatrixObject mo1, MatrixObject mo2, long outputID, ExecutionContext ec){
		MatrixObject out = ec.getMatrixObject(output);
		out.getDataCharacteristics().set(mo1.getNumRows(), mo2.getNumColumns(), (int)mo1.getBlocksize());
		FederationMap outputFedMap = mo1.getFedMapping()
			.copyWithNewIDAndRange(mo1.getNumRows(), mo2.getNumColumns(), outputID);
```

### RedundantCast
Casting `ec.getScalarInput(...)` to `BooleanObject` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
	private void processRemoveVariableAndFileInstruction(ExecutionContext ec){
		// Remove the variable from HashMap _variables, and possibly delete the data on disk.
		boolean del = ( (BooleanObject) ec.getScalarInput(getInput2().getName(), getInput2().getValueType(), true) ).getBooleanValue();
		MatrixObject m = (MatrixObject) ec.removeVariable(getInput1().getName());

```

### RedundantCast
Casting `out.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryMatrixFEDInstruction.java`
#### Snippet
```java
		// modify fed ranges since ucumk+* output is always nx1
		if(getOpcode().equalsIgnoreCase("ucumk+*")) {
			out.getDataCharacteristics().set(out.getNumRows(), 1L, (int) out.getBlocksize());
			for(int i = 0; i < out.getFedMapping().getFederatedRanges().length; i++)
				out.getFedMapping().getFederatedRanges()[i].setEndDim(1, 1);
```

### RedundantCast
Casting `out.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryMatrixFEDInstruction.java`
#### Snippet
```java
				out.getFedMapping().getFederatedRanges()[i].setEndDim(1, 1);
		} else {
			out.getDataCharacteristics().set(out.getNumRows(), out.getNumColumns(), (int) out.getBlocksize());
		}
	}
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
				//drive output federated mapping
				MatrixObject out = ec.getMatrixObject(output);
				out.getDataCharacteristics().set(mo1.getNumColumns(), mo1.getNumRows(), (int) mo1.getBlocksize(), mo1.getNnz());
				out.setFedMapping(mo1.getFedMapping().copyWithNewID(fr1.getID()).transpose());
			} else {
```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
			//derive output federated mapping
			MatrixObject out = ec.getMatrixObject(output);
			out.getDataCharacteristics().set(mo1.getNumRows(), mo1.getNumColumns(), (int) mo1.getBlocksize(), mo1.getNnz());
			out.setFedMapping(mo1.getFedMapping().copyWithNewID(fr1.getID()));

```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
			rdiag.getDataCharacteristics()
				.set(diagFedMap.getMaxIndexInRange(0), diagFedMap.getMaxIndexInRange(1),
					(int) mo1.getBlocksize());
			rdiag.setFedMapping(diagFedMap);
			optionalForceLocal(rdiag);
```

### RedundantCast
Casting `out.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
		// modify fed ranges since ucumk+* output is always nx1
		if(getOpcode().equalsIgnoreCase("bcumoff+*")) {
			out.getDataCharacteristics().set(out.getNumRows(), 1L, (int) out.getBlocksize());
			for(int i = 0; i < out.getFedMapping().getFederatedRanges().length; i++)
				out.getFedMapping().getFederatedRanges()[i].setEndDim(1, 1);
```

### RedundantCast
Casting `out.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CumulativeOffsetFEDInstruction.java`
#### Snippet
```java
				out.getFedMapping().getFederatedRanges()[i].setEndDim(1, 1);
		} else {
			out.getDataCharacteristics().set(out.getNumRows(), out.getNumColumns(), (int) out.getBlocksize());
		}
	}
```

### RedundantCast
Casting `inst` to `BinarySPInstruction` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/BinaryFEDInstruction.java`
#### Snippet
```java
		else if(inst instanceof AppendGAlignedSPInstruction || inst instanceof AppendGSPInstruction ||
			inst instanceof AppendMSPInstruction || inst instanceof AppendRSPInstruction) {
			BinarySPInstruction ainstruction = (BinarySPInstruction) inst;
			Data data1 = ec.getVariable(ainstruction.input1);
			Data data2 = ec.getVariable(ainstruction.input2);
```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		out.getDataCharacteristics().set(out.getFedMapping().getMaxIndexInRange(0),
			out.getFedMapping().getMaxIndexInRange(1),
			(int) mo.getBlocksize());
	}

```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		out.getDataCharacteristics().set(out.getFedMapping().getMaxIndexInRange(0),
			out.getFedMapping().getMaxIndexInRange(1),
			(int) mo.getBlocksize());
	}

```

### RedundantCast
Casting `mo1.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CtableFEDInstruction.java`
#### Snippet
```java

		// set output
		out.getDataCharacteristics().set(d1, d2, (int) mo1.getBlocksize(), mo1.getNnz());
		out.setFedMapping(fedMap);

```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
	protected JavaPairRDD<Long, FrameBlock> processFrameLIBSVMReblockInstruction(
		SparkExecutionContext sec, DataCharacteristics mcOut, ValueType[] schema) {  //TODO
		JavaPairRDD<LongWritable, Text> in = (JavaPairRDD<LongWritable, Text>) sec
			.getRDDHandleForFrameObject(sec.getFrameObject(input1), FileFormat.LIBSVM);

```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
		//get input rdd (needs to be longwritable/text for consistency with meta data, in case of
		//serialization issues create longwritableser/textser as serializable wrappers
		JavaPairRDD<LongWritable, Text> in = (JavaPairRDD<LongWritable, Text>) sec
			.getRDDHandleForMatrixObject(sec.getMatrixObject(input1), FileFormat.LIBSVM);

```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
		if(fmt == FileFormat.TEXT) {
			//get the input textcell rdd
			JavaPairRDD<LongWritable, Text> lines = (JavaPairRDD<LongWritable, Text>)
				sec.getRDDHandleForFrameObject(fo, fmt);

```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java

			//get the input textcell rdd
			JavaPairRDD<LongWritable, Text> lines = (JavaPairRDD<LongWritable, Text>)
				sec.getRDDHandleForMatrixObject(mo, fmt);

```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
		else if(fmt == FileFormat.BINARY && mc.getBlocksize() <= 0) {
			//BINARY BLOCK <- BINARY CELL (e.g., after grouped aggregate)
			JavaPairRDD<MatrixIndexes, MatrixCell> binaryCells = (JavaPairRDD<MatrixIndexes, MatrixCell>) sec.getRDDHandleForMatrixObject(mo, FileFormat.BINARY);
			JavaPairRDD<MatrixIndexes, MatrixBlock> out = RDDConverterUtils.binaryCellToBinaryBlock(sec.getSparkContext(), binaryCells, mcOut, outputEmptyBlocks);

```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
		}
		else if(fmt == FileFormat.COMPRESSED){
			JavaPairRDD<MatrixIndexes, MatrixBlock> in1 = (JavaPairRDD<MatrixIndexes, MatrixBlock>) sec
				.getRDDHandleForMatrixObject(mo, FileFormat.COMPRESSED);
			JavaPairRDD<MatrixIndexes, MatrixBlock> out = RDDConverterUtils.binaryBlockToBinaryBlock(in1, mc, mcOut);
```

### RedundantCast
Casting `in` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CastSPInstruction.java`
#### Snippet
```java
			mcOut.setBlocksize(ConfigurationManager.getBlocksize());
			out = FrameRDDConverterUtils.binaryBlockToMatrixBlock(
				(JavaPairRDD<Long, FrameBlock>)in, mcIn, mcOut);
		}
		else if( opcode.equals(OpOp1.CAST_AS_FRAME.toString()) ) {
```

### RedundantCast
Casting `in` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CastSPInstruction.java`
#### Snippet
```java
		else if( opcode.equals(OpOp1.CAST_AS_FRAME.toString()) ) {
			out = FrameRDDConverterUtils.matrixBlockToBinaryBlockLongIndex(sec.getSparkContext(), 
				(JavaPairRDD<MatrixIndexes, MatrixBlock>)in, mcIn);
		}
		else {
```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			// get input RDD data
			FrameObject fo = sec.getFrameObject(params.get("target"));
			JavaPairRDD<Long, FrameBlock> in = (JavaPairRDD<Long, FrameBlock>) sec.getRDDHandleForFrameObject(fo,
				FileFormat.BINARY);
			DataCharacteristics mc = sec.getDataCharacteristics(params.get("target"));
```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			// get input RDD and meta data
			FrameObject fo = sec.getFrameObject(params.get("target"));
			JavaPairRDD<Long, FrameBlock> in = (JavaPairRDD<Long, FrameBlock>) sec.getRDDHandleForFrameObject(fo,
				FileFormat.BINARY);
			FrameBlock meta = sec.getFrameInput(params.get("meta"));
```

### RedundantCast
Casting `encoder.getNumOutCols()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
				.createEncoder(params.get("spec"), colnames, fo.getSchema(), (int) fo.getNumColumns(), meta);
			mcOut.setDimension(mcIn.getRows() - ((omap != null) ? omap.getNumRmRows() : 0),
				(int)encoder.getNumOutCols());
			Broadcast<MultiColumnEncoder> bmeta = sec.getSparkContext().broadcast(encoder);
			Broadcast<TfOffsetMap> bomap = (omap != null) ? sec.getSparkContext().broadcast(omap) : null;
```

### RedundantCast
Casting `in` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CheckpointSPInstruction.java`
#### Snippet
```java
				if( input1.getDataType() == DataType.MATRIX )
					out = SparkUtils.copyBinaryBlockMatrix(
						(JavaPairRDD<MatrixIndexes,MatrixBlock>)in, false);
				else if( input1.getDataType() == DataType.FRAME)
					out = ((JavaPairRDD<Long,FrameBlock>)in)
```

### RedundantCast
Casting `out` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CheckpointSPInstruction.java`
#### Snippet
```java
			//otherwise their nnz would never be evaluated due to lazy evaluation in spark
			if( input1.isMatrix() && mcIn.dimsKnown() && !mcIn.dimsKnown(true) ) {
				mcIn.setNonZeros(SparkUtils.getNonZeros((JavaPairRDD<MatrixIndexes,MatrixBlock>)out));
			}
		}
```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
			FrameObject fo = sec.getFrameObject(input1.getName());
			FrameObject fometa = sec.getFrameObject(_outputs.get(1).getName());
			JavaPairRDD<Long, FrameBlock> in = (JavaPairRDD<Long, FrameBlock>) sec.getRDDHandleForFrameObject(fo,
				FileFormat.BINARY);
			String spec = ec.getScalarInput(input2).getStringValue();
```

### RedundantCast
Casting `encoder.getNumOutCols()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
				.createEncoder(spec, colnames, fo.getSchema(), (int) fo.getNumColumns(), meta);
			mcOut.setDimension(mcIn.getRows() - ((omap != null) ? omap.getNumRmRows() : 0),
				(int) encoder.getNumOutCols());
			Broadcast<MultiColumnEncoder> bmeta = sec.getSparkContext().broadcast(encoder);
			Broadcast<TfOffsetMap> bomap = (omap != null) ? sec.getSparkContext().broadcast(omap) : null;
```

### RedundantCast
Casting `sec.getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
		//get input rdd (needs to be longwritable/text for consistency with meta data, in case of
		//serialization issues create longwritableser/textser as serializable wrappers
		JavaPairRDD<LongWritable, Text> in = (JavaPairRDD<LongWritable, Text>) 
			sec.getRDDHandleForFrameObject(sec.getFrameObject(input1), FileFormat.CSV);
		
```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
		//get input rdd (needs to be longwritable/text for consistency with meta data, in case of
		//serialization issues create longwritableser/textser as serializable wrappers
		JavaPairRDD<LongWritable, Text> in = (JavaPairRDD<LongWritable, Text>)
			sec.getRDDHandleForMatrixObject(sec.getMatrixObject(input1), FileFormat.CSV);
		
```

### RedundantCast
Casting `allBlks.get(...).getValue()` to `T` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/PartitionedBroadcast.java`
#### Snippet
```java
		T ret = (T) allBlks.get(0).getValue();
		for(int i=1; i<allBlks.size(); i++)
			ret.merge((T)allBlks.get(i).getValue(), false);
		return ret;
	}
```

### RedundantCast
Casting `numRowsPerBlock` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java

	public static void copyRowBlocks(MatrixBlock mb, int rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
		copyRowBlocks(mb, (long)rowIndex, ret, (long)numRowsPerBlock, (long)rlen, (long)clen);
	}
	public static void copyRowBlocks(MatrixBlock mb, long rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
```

### RedundantCast
Casting `rlen` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java

	public static void copyRowBlocks(MatrixBlock mb, int rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
		copyRowBlocks(mb, (long)rowIndex, ret, (long)numRowsPerBlock, (long)rlen, (long)clen);
	}
	public static void copyRowBlocks(MatrixBlock mb, long rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
```

### RedundantCast
Casting `numRowsPerBlock` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
	}
	public static void copyRowBlocks(MatrixBlock mb, long rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
		copyRowBlocks(mb, rowIndex, ret, (long)numRowsPerBlock, (long)rlen, (long)clen);
	}
	public static void copyRowBlocks(MatrixBlock mb, int rowIndex, MatrixBlock ret, long numRowsPerBlock, long rlen, long clen) {
```

### RedundantCast
Casting `rlen` to `long` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
	}
	public static void copyRowBlocks(MatrixBlock mb, long rowIndex, MatrixBlock ret, int numRowsPerBlock, int rlen, int clen) {
		copyRowBlocks(mb, rowIndex, ret, (long)numRowsPerBlock, (long)rlen, (long)clen);
	}
	public static void copyRowBlocks(MatrixBlock mb, int rowIndex, MatrixBlock ret, long numRowsPerBlock, long rlen, long clen) {
```

### RedundantCast
Casting `mo.getRDDHandle().getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	public static long getNonZeros(MatrixObject mo) {
		return getNonZeros((JavaPairRDD<MatrixIndexes, MatrixBlock>)mo.getRDDHandle().getRDD());
	}
	
```

### RedundantCast
Casting `((RDDObject)mo.getRDDHandle().getLineageChilds().get(...)).getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteDPParForSpark.java`
#### Snippet
```java
			JavaPairRDD<MatrixIndexes,MatrixBlock> in = sec.getBinaryMatrixBlockRDDHandleForVariable(matrixvar);
			if( mo.getRDDHandle().isCheckpointRDD() && !SparkExecutionContext.isRDDCached(in.id()) )
				in = (JavaPairRDD<MatrixIndexes,MatrixBlock>)((RDDObject)
						mo.getRDDHandle().getLineageChilds().get(0)).getRDD();
			
```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
			JavaPairRDD<MatrixIndexes, MatrixBlock> out = null;
			if( withCompare ) {
				JavaPairRDD<MatrixIndexes, MatrixBlock> compareRdd = (JavaPairRDD<MatrixIndexes, MatrixBlock>) 
						sec.getRDDHandleForMatrixObject(compare, FileFormat.BINARY);
				
```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerRemoteSpark.java`
#### Snippet
```java
			HDFSTool.deleteFileIfExistOnHDFS(fnameNew);
			//get input rdd
			JavaPairRDD<MatrixIndexes, MatrixBlock> inRdd = (JavaPairRDD<MatrixIndexes, MatrixBlock>) 
					sec.getRDDHandleForMatrixObject(in, FileFormat.BINARY);
			
```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/TensorObject.java`
#### Snippet
```java
		// TODO correct blocksize;
		// TODO read from RDD
		return SparkExecutionContext.toTensorBlock((JavaPairRDD<TensorIndexes, TensorBlock>) rdd.getRDD(), tc);
	}
	
```

### RedundantCast
Casting `null` to `Program` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContextFactory.java`
#### Snippet
```java
public class ExecutionContextFactory {
	public static ExecutionContext createContext() {
		return createContext(true, DMLScript.LINEAGE,(Program) null );
	}
	
```

### RedundantCast
Casting `clen` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/FrameObject.java`
#### Snippet
```java
		//handle missing schema if necessary
		ValueType[] lschema = (_schema!=null) ? _schema : 
			UtilFunctions.nCopies(clen>=1 ? (int)clen : 1, ValueType.STRING);
		
		FrameBlock fb = null;
```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
		if( oldMetaData == null || !(oldMetaData instanceof MetaDataFormat) )
			throw new DMLRuntimeException("Metadata not available");
		MatrixCharacteristics mc = new MatrixCharacteristics(nrows, ncols, (int) mo.getBlocksize());
		mo.setMetaData(new MetaDataFormat(mc, ((MetaDataFormat)oldMetaData).getFileFormat()));
	}
```

### RedundantCast
Casting `linst[j]` to `Object` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
			}

			fr[j] = new FederatedRequest(RequestType.EXEC_INST, outputId, (Object) linst[j]);
		}
		return fr;
```

### RedundantCast
Casting `linst[j]` to `Object` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
				}
			}
			fr[j] = new FederatedRequest(RequestType.EXEC_INST, id, (Object) linst[j]);
		}
		return fr;
```

### RedundantCast
Casting `Math.max(...)` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		//to avoid internal SIGFPE and allocation buffer issues w/ MKL and OpenBlas)
		return NativeHelper.isNativeLibraryLoaded() ?
			(int) Math.max(opsK / tmpK, 1) :
			(int) Math.max(Math.round((double)opsK / tmpK), 1);
	}
```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/SparkParamservUtils.java`
#### Snippet
```java
		Timing tSetup = DMLScript.STATISTICS ? new Timing(true) : null;
		// Get input RDD
		JavaPairRDD<MatrixIndexes, MatrixBlock> featuresRDD = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
			sec.getRDDHandleForMatrixObject(features, FileFormat.BINARY);
		JavaPairRDD<MatrixIndexes, MatrixBlock> labelsRDD = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
```

### RedundantCast
Casting `sec.getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/SparkParamservUtils.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes, MatrixBlock> featuresRDD = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
			sec.getRDDHandleForMatrixObject(features, FileFormat.BINARY);
		JavaPairRDD<MatrixIndexes, MatrixBlock> labelsRDD = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
			sec.getRDDHandleForMatrixObject(labels, FileFormat.BINARY);

```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public static MatrixBlock toMatrixBlock(RDDObject rdd, int rlen, int clen, long nnz) {
		return toMatrixBlock(
				(JavaPairRDD<MatrixIndexes, MatrixCell>) rdd.getRDD(),
				rlen, clen, nnz);
	}
```

### RedundantCast
Casting `getRDDHandleForFrameObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<Long,FrameBlock> getFrameBinaryBlockRDDHandleForVariable( String varname ) {
		FrameObject fo = getFrameObject(varname);
		JavaPairRDD<Long,FrameBlock> out = (JavaPairRDD<Long,FrameBlock>)
			getRDDHandleForFrameObject(fo, FileFormat.BINARY);
		return out;
```

### RedundantCast
Casting `getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java

		//get input rdd and default storage level
		JavaPairRDD<MatrixIndexes,MatrixBlock> in = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
			getRDDHandleForMatrixObject(mo, FileFormat.BINARY);

```

### RedundantCast
Casting `((RDDObject)mo.getRDDHandle().getLineageChilds().get(...)).getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		if( mo.getRDDHandle().allowsShortCircuitRead()
			&& isRDDMarkedForCaching(in.id()) && !isRDDCached(in.id()) ) {
			in = (JavaPairRDD<MatrixIndexes,MatrixBlock>)
				((RDDObject)mo.getRDDHandle().getLineageChilds().get(0)).getRDD();

```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public static long writeMatrixRDDtoHDFS( RDDObject rdd, String path, FileFormat fmt )
	{
		JavaPairRDD<MatrixIndexes,MatrixBlock> lrdd = (JavaPairRDD<MatrixIndexes, MatrixBlock>) rdd.getRDD();
		InputOutputInfo oinfo = InputOutputInfo.get(DataType.MATRIX, fmt);
		
```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	@SuppressWarnings("unchecked")
	public static FrameBlock toFrameBlock(RDDObject rdd, ValueType[] schema, int rlen, int clen) {
		JavaPairRDD<Long,FrameBlock> lrdd = (JavaPairRDD<Long,FrameBlock>) rdd.getRDD();
		return toFrameBlock(lrdd, schema, rlen, clen);
	}
```

### RedundantCast
Casting `getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			return;

		JavaPairRDD<MatrixIndexes,MatrixBlock> in = (JavaPairRDD<MatrixIndexes, MatrixBlock>)
			getRDDHandleForMatrixObject(mo, FileFormat.BINARY);

```

### RedundantCast
Casting `rdd` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
					mo.exportData();
				rdd = sc.hadoopFile( mo.getFileName(), inputInfo.inputFormatClass, inputInfo.keyClass, inputInfo.valueClass);
				rdd = SparkUtils.copyBinaryBlockMatrix((JavaPairRDD<MatrixIndexes, MatrixBlock>)rdd); //cp is workaround for read bug
				fromFile = true;
			}
```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			else { //default case
				MatrixBlock mb = mo.acquireRead(); //pin matrix in memory
				rdd = toMatrixJavaPairRDD(sc, mb, (int)mo.getBlocksize(), numParts, inclEmpty);
				mo.release(); //unpin matrix
				_parRDDs.registerRDD(rdd.id(), OptimizerUtils.estimatePartitionedSizeExactSparsity(dc), true);
```

### RedundantCast
Casting `rdd` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				//recordreader returns; the javadoc explicitly recommend to copy all key/value pairs
				// cp is workaround for read bug
				rdd = SparkUtils.copyBinaryBlockMatrix((JavaPairRDD<MatrixIndexes, MatrixBlock>)rdd); 
			else if(fmt == FileFormat.COMPRESSED)
				rdd = ((JavaPairRDD<MatrixIndexes, CompressedWriteBlock>) rdd).mapValues(new CompressUnwrap());
```

### RedundantCast
Casting `getRDDHandleForTensorObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<TensorIndexes, TensorBlock> getBinaryTensorBlockRDDHandleForVariable(String varname, int numParts, boolean inclEmpty ) {
		TensorObject to = getTensorObject(varname);
		return (JavaPairRDD<TensorIndexes, TensorBlock>)
				getRDDHandleForTensorObject(to, FileFormat.BINARY, numParts, inclEmpty);
	}
```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public static void writeFrameRDDtoHDFS( RDDObject rdd, String path, FileFormat fmt)
	{
		JavaPairRDD<?, FrameBlock> lrdd = (JavaPairRDD<Long, FrameBlock>) rdd.getRDD();
		InputOutputInfo oinfo = InputOutputInfo.get(DataType.FRAME, fmt);
		
```

### RedundantCast
Casting `getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<MatrixIndexes,MatrixBlock> getBinaryMatrixBlockRDDHandleForVariable(String varname ) {
		MatrixObject mo = getMatrixObject(varname);
		return (JavaPairRDD<MatrixIndexes,MatrixBlock>)
			getRDDHandleForMatrixObject(mo, FileFormat.BINARY, -1, true);
	}
```

### RedundantCast
Casting `getRDDHandleForMatrixObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<MatrixIndexes,MatrixBlock> getBinaryMatrixBlockRDDHandleForVariable(String varname, int numParts, boolean inclEmpty ) {
		MatrixObject mo = getMatrixObject(varname);
		return (JavaPairRDD<MatrixIndexes,MatrixBlock>)
			getRDDHandleForMatrixObject(mo, FileFormat.BINARY, numParts, inclEmpty);
	}
```

### RedundantCast
Casting `getRDDHandleForTensorObject(...)` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<TensorIndexes, TensorBlock> getBinaryTensorBlockRDDHandleForVariable(String varname ) {
		TensorObject to = getTensorObject(varname);
		return (JavaPairRDD<TensorIndexes, TensorBlock>)
				getRDDHandleForTensorObject(to, FileFormat.BINARY, -1, true);
	}
```

### RedundantCast
Casting `rdd.getRDD()` to `JavaPairRDD` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public static MatrixBlock toMatrixBlock(RDDObject rdd, int rlen, int clen, int blen, long nnz) {
		return toMatrixBlock(
			(JavaPairRDD<MatrixIndexes, MatrixBlock>) rdd.getRDD(),
			rlen, clen, blen, nnz);
	}
```

### RedundantCast
Casting `mo.getBlocksize()` to `int` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java

				//obtain meta data for matrix
				int blen = (int) mo.getBlocksize();

				//create partitioned matrix block and release memory consumed by input
```

### RedundantCast
Casting `pb` to `ForProgramBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
				}
			} else if (pb instanceof ForProgramBlock) {
				recompiled |= rAssignParallelismAndRecompile(((ForProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
			} else if (pb instanceof WhileProgramBlock) {
				recompiled |= rAssignParallelismAndRecompile(((WhileProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
```

### RedundantCast
Casting `pb` to `WhileProgramBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
				recompiled |= rAssignParallelismAndRecompile(((ForProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
			} else if (pb instanceof WhileProgramBlock) {
				recompiled |= rAssignParallelismAndRecompile(((WhileProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
			} else if (pb instanceof FunctionProgramBlock) {
				recompiled |= rAssignParallelismAndRecompile(((FunctionProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
```

### RedundantCast
Casting `pb` to `FunctionProgramBlock` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
				recompiled |= rAssignParallelismAndRecompile(((WhileProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
			} else if (pb instanceof FunctionProgramBlock) {
				recompiled |= rAssignParallelismAndRecompile(((FunctionProgramBlock) pb).getChildBlocks(), k, recompiled, forceExecTypeCP);
			} else if (pb instanceof IfProgramBlock) {
				IfProgramBlock ipb = (IfProgramBlock) pb;
```

### RedundantCast
Casting `pFeatures.get(...).getFedMapping().getFederatedData()[0]` to `FederatedData` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/BalanceToAvgFederatedScheme.java`
#### Snippet
```java
		for(int i = 0; i < pFeatures.size(); i++) {
			// Works, because the map contains a single entry
			FederatedData featuresData = (FederatedData) pFeatures.get(i).getFedMapping().getFederatedData()[0];
			FederatedData labelsData = (FederatedData) pLabels.get(i).getFedMapping().getFederatedData()[0];

```

### RedundantCast
Casting `pLabels.get(...).getFedMapping().getFederatedData()[0]` to `FederatedData` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/BalanceToAvgFederatedScheme.java`
#### Snippet
```java
			// Works, because the map contains a single entry
			FederatedData featuresData = (FederatedData) pFeatures.get(i).getFedMapping().getFederatedData()[0];
			FederatedData labelsData = (FederatedData) pLabels.get(i).getFedMapping().getFederatedData()[0];

			Future<FederatedResponse> udfResponse = featuresData.executeFederatedOperation(new FederatedRequest(FederatedRequest.RequestType.EXEC_UDF,
```

## RuleId[id=JavadocDeclaration]
### JavadocDeclaration
`@throws` tag description is missing
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	/**
	 * Method to parse configuration
	 * @throws ParserConfigurationException
	 * @throws SAXException
	 * @throws IOException
```

### JavadocDeclaration
`@throws` tag description is missing
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	 * Method to parse configuration
	 * @throws ParserConfigurationException
	 * @throws SAXException
	 * @throws IOException
	 */
```

### JavadocDeclaration
`@throws` tag description is missing
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	 * @throws ParserConfigurationException
	 * @throws SAXException
	 * @throws IOException
	 */
	private void parseConfig () throws ParserConfigurationException, SAXException, IOException 
```

### JavadocDeclaration
Duplicate @param tag for parameter 'blen'
in `src/main/java/org/apache/sysds/runtime/io/ReaderBinaryBlock.java`
#### Snippet
```java
	 * @param clen number of columns
	 * @param blen number of rows in block
	 * @param blen number of columns in block
	 * @throws IOException if IOException occurs
	 */
```

### JavadocDeclaration
Wrong tag `returnA`
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/Array.java`
#### Snippet
```java
	 * Change type to a boolean array
	 * 
	 * @returnA Boolean type of array
	 */
	protected abstract Array<Boolean> changeTypeBoolean();
```

### JavadocDeclaration
`@param rows` tag description is missing
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * Given the number of rows and columns, returns
	 * whether this is a row vector, column vector or neither.
	 * @param rows
	 * @param cols
	 * @return 1 for column vector, 2 for row vector, 0 for neither
```

### JavadocDeclaration
`@param cols` tag description is missing
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	 * whether this is a row vector, column vector or neither.
	 * @param rows
	 * @param cols
	 * @return 1 for column vector, 2 for row vector, 0 for neither
	 */
```

### JavadocDeclaration
`@param opcode` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSLossFEDInstruction.java`
#### Snippet
```java
	 * @param in4 W
	 * @param out Z
	 * @param opcode
	 * @param instruction_str
	 */
```

### JavadocDeclaration
`@param instruction_str` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWSLossFEDInstruction.java`
#### Snippet
```java
	 * @param out Z
	 * @param opcode
	 * @param instruction_str
	 */
	protected QuaternaryWSLossFEDInstruction(Operator operator, CPOperand in1, CPOperand in2, CPOperand in3,
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * and many more
	 *
	 * @param operator        Weighted Div Matrix Multiplication Federated Instruction.
	 * @param in1             X
	 * @param in2             U
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 *
	 * @param operator        Weighted Div Matrix Multiplication Federated Instruction.
	 * @param in1             X
	 * @param in2             U
	 * @param in3             V
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param operator        Weighted Div Matrix Multiplication Federated Instruction.
	 * @param in1             X
	 * @param in2             U
	 * @param in3             V
	 * @param in4             W (=epsilon or MX matrix)
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param in1             X
	 * @param in2             U
	 * @param in3             V
	 * @param in4             W (=epsilon or MX matrix)
	 * @param out             The Federated Result Z
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param in2             U
	 * @param in3             V
	 * @param in4             W (=epsilon or MX matrix)
	 * @param out             The Federated Result Z
	 * @param opcode          ...
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param in3             V
	 * @param in4             W (=epsilon or MX matrix)
	 * @param out             The Federated Result Z
	 * @param opcode          ...
	 * @param instruction_str ...
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param in4             W (=epsilon or MX matrix)
	 * @param out             The Federated Result Z
	 * @param opcode          ...
	 * @param instruction_str ...
	*/
```

### JavadocDeclaration
Tag `param` is not allowed here
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	 * @param out             The Federated Result Z
	 * @param opcode          ...
	 * @param instruction_str ...
	*/

```

### JavadocDeclaration
`@param data` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CtableFEDInstruction.java`
#### Snippet
```java
		 *
		 * @param ec the execution context
		 * @param data
		 * @return FederatedResponse with status SUCCESS and an empty object
		 */
```

### JavadocDeclaration
`@param ptxFileName` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java
	 * Reads the ptx file from resource
	 *
	 * @param ptxFileName
	 * @return
	 */
```

### JavadocDeclaration
`@return` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java
	 *
	 * @param ptxFileName
	 * @return
	 */
	private static Pointer initKernels(String ptxFileName) {
```

### JavadocDeclaration
`@return` tag description is missing
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
	 * @param min minimum value
	 * @param max maximum value
	 * @return
	 */
	private static boolean isMemAvail(long lrows, long lcols, double sparsity, double min, double max) {
```

### JavadocDeclaration
Duplicate @return tag
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
       * @param index The index of the value to return.
       * @return The enum numeric value on the wire of valueType at the given index.
       * @return This builder for chaining.
       */
      public Builder setValueTypeValue(
```

## RuleId[id=FieldMayBeFinal]
### FieldMayBeFinal
Field `outputVariables` may be 'final'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	 * The output variables.
	 */
	private Set<String> outputVariables = new LinkedHashSet<>();
	/**
	 * The symbol table containing the data associated with variables.
```

### FieldMayBeFinal
Field `symbolTable` may be 'final'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	 * The symbol table containing the data associated with variables.
	 */
	private LocalVariableMap symbolTable = new LocalVariableMap();
	/**
	 * The ScriptExecutor which is used to define the execution of the script.
```

### FieldMayBeFinal
Field `inputMetadata` may be 'final'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	 * The input matrix or frame metadata if present.
	 */
	private Map<String, Metadata> inputMetadata = new LinkedHashMap<>();
	/**
	 * The output variables.
```

### FieldMayBeFinal
Field `inputVariables` may be 'final'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	 * The input variables.
	 */
	private Set<String> inputVariables = new LinkedHashSet<>();
	/**
	 * The input matrix or frame metadata if present.
```

### FieldMayBeFinal
Field `internalProxy` may be 'final'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
	 * Contains cleanup methods used by MLContextProxy.
	 */
	private InternalProxy internalProxy = new InternalProxy();

	/**
```

### FieldMayBeFinal
Field `_seqHopID` may be 'final'
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java

	// static variable to assign an unique ID to every hop that is created
	private static IDSequence _seqHopID = new IDSequence();
	
	protected final long _ID;
```

### FieldMayBeFinal
Field `EXACT_META_DATA_OPS` may be 'final'
in `src/main/java/org/apache/sysds/hops/estim/SparsityEstimator.java`
#### Snippet
```java
	public static final int MIN_PAR_THRESHOLD = 10 * 1024;
	
	private static OpCode[] EXACT_META_DATA_OPS = new OpCode[] {
		OpCode.EQZERO, OpCode.NEQZERO, OpCode.CBIND,
		OpCode.RBIND, OpCode.TRANS, OpCode.DIAG, OpCode.RESHAPE
```

### FieldMayBeFinal
Field `_input` may be 'final'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorLayeredGraph.java`
#### Snippet
```java
		
		private static class Node {
			private List<Node> _input = new ArrayList<>();
			private double[] _rvect;
			
```

### FieldMayBeFinal
Field `indexes` may be 'final'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorSampleRa.java`
#### Snippet
```java
	
	private static class AdjacencyLists {
		private ArrayList<Integer>[] indexes;
		
		@SuppressWarnings("unchecked")
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
	
	public static class BitsetMatrix2 extends BitsetMatrix {
		private BitSet[] _data;

		public BitsetMatrix2(int rlen, int clen) {
```

### FieldMayBeFinal
Field `rewriteCSE` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
	private static final PlanCache planCache = new PlanCache(PLAN_CACHE_SIZE);
	
	private static ProgramRewriter rewriteCSE = new ProgramRewriter(
		new RewriteCommonSubexpressionElimination(true),
		new RewriteRemoveUnnecessaryCasts());
```

### FieldMayBeFinal
Field `_cutSets` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
	
	private InterestingPoint[] _searchSpace;
	private CutSet[] _cutSets;
	
	public ReachabilityGraph(PlanPartition part, CPlanMemoTable memo) {
```

### FieldMayBeFinal
Field `_ID` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
		
		private ArrayList<NodeLink> _inputs = new ArrayList<>();
		private long _ID;
		private InterestingPoint _p;
		
```

### FieldMayBeFinal
Field `_searchSpace` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
	private NodeLink _root = null;
	
	private InterestingPoint[] _searchSpace;
	private CutSet[] _cutSets;
	
```

### FieldMayBeFinal
Field `_p` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
		private ArrayList<NodeLink> _inputs = new ArrayList<>();
		private long _ID;
		private InterestingPoint _p;
		
		private NodeLink(InterestingPoint p) {
```

### FieldMayBeFinal
Field `_inputs` may be 'final'
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
		private static final IDSequence _seqID = new IDSequence();
		
		private ArrayList<NodeLink> _inputs = new ArrayList<>();
		private long _ID;
		private InterestingPoint _p;
```

### FieldMayBeFinal
Field `rewriter` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMergeBlockSequence.java`
#### Snippet
```java
public class RewriteMergeBlockSequence extends StatementBlockRewriteRule
{
	private ProgramRewriter rewriter = new ProgramRewriter(
		new RewriteCommonSubexpressionElimination(true));
	
```

### FieldMayBeFinal
Field `_seq` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/StatementBlockRewriteRule.java`
#### Snippet
```java
	private static final String SB_CUT_PREFIX = "_sbcvar";
	private static final String FUN_CUT_PREFIX = "_funvar";
	private static IDSequence _seq = new IDSequence();
	
	public static String createCutVarName(boolean fun) {
```

### FieldMayBeFinal
Field `procFn` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		private boolean nonApplicable = false;

		private HashSet<String> procFn = new HashSet<>();
		private HashSet<String> compMtx = new HashSet<>();

```

### FieldMayBeFinal
Field `compMtx` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java

		private HashSet<String> procFn = new HashSet<>();
		private HashSet<String> compMtx = new HashSet<>();

		private ProbeStatus(long hopID, DMLProgram p) {
```

### FieldMayBeFinal
Field `name` may be 'final'
in `src/main/java/org/apache/sysds/hops/fedplanner/FederatedCompilationTimer.java`
#### Snippet
```java
		private long stopTime;
		private long duration;
		private String name;

		public TimeEntry(String name){
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/lops/CumulativeOffsetBinary.java`
#### Snippet
```java
public class CumulativeOffsetBinary extends Lop 
{
	private AggOp _op;
	private double _initValue = 0;
	private boolean _broadcast = false;
```

### FieldMayBeFinal
Field `_inputParams` may be 'final'
in `src/main/java/org/apache/sysds/lops/GroupedAggregateM.java`
#### Snippet
```java
	}
	
	private HashMap<String, Lop> _inputParams;
	private CacheType _cacheType = null;
		
```

### FieldMayBeFinal
Field `_et` may be 'final'
in `src/main/java/org/apache/sysds/lops/Append.java`
#### Snippet
```java

	private boolean _cbind = true;
	private ExecType _et;

	public Append(Lop input1, Lop input2, Lop input3, DataType dt, ValueType vt, boolean cbind, ExecType et)
```

### FieldMayBeFinal
Field `_type` may be 'final'
in `src/main/java/org/apache/sysds/lops/Federated.java`
#### Snippet
```java

public class Federated extends Lop {
	private Lop _type, _addresses, _ranges, _localObject;
	
	public Federated(HashMap<String, Lop> inputLops, DataType dataType, ValueType valueType) {
```

### FieldMayBeFinal
Field `_ranges` may be 'final'
in `src/main/java/org/apache/sysds/lops/Federated.java`
#### Snippet
```java

public class Federated extends Lop {
	private Lop _type, _addresses, _ranges, _localObject;
	
	public Federated(HashMap<String, Lop> inputLops, DataType dataType, ValueType valueType) {
```

### FieldMayBeFinal
Field `_addresses` may be 'final'
in `src/main/java/org/apache/sysds/lops/Federated.java`
#### Snippet
```java

public class Federated extends Lop {
	private Lop _type, _addresses, _ranges, _localObject;
	
	public Federated(HashMap<String, Lop> inputLops, DataType dataType, ValueType valueType) {
```

### FieldMayBeFinal
Field `_blocksize` may be 'final'
in `src/main/java/org/apache/sysds/lops/CSVReBlock.java`
#### Snippet
```java
	public static final String OPCODE = "csvrblk"; 
	
	private int _blocksize;

	public CSVReBlock(Lop input, int blen, DataType dt, ValueType vt, ExecType et)
```

### FieldMayBeFinal
Field `_inputParams` may be 'final'
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
	private String baseDir;
	
	private HashMap<String, Lop> _inputParams;
	private final OpOpDG _op;
	
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/lops/CumulativePartialAggregate.java`
#### Snippet
```java
public class CumulativePartialAggregate extends Lop 
{
	private AggOp _op;
	
	public CumulativePartialAggregate(Lop input, DataType dt, ValueType vt, AggOp op, ExecType et) {
```

### FieldMayBeFinal
Field `_direction` may be 'final'
in `src/main/java/org/apache/sysds/lops/TernaryAggregate.java`
#### Snippet
```java
	//private AggOp _aggOp = null;
	//private Binary.OperationTypes _binOp = null;
	private Direction _direction;
	
	//optional attribute for cp
```

### FieldMayBeFinal
Field `_inputParams` may be 'final'
in `src/main/java/org/apache/sysds/lops/Sql.java`
#### Snippet
```java

public class Sql extends Lop {
	private HashMap<String, Lop> _inputParams;
	
	public Sql(HashMap<String, Lop> inputParametersLops, DataType dt, ValueType vt) {
```

### FieldMayBeFinal
Field `_operation` may be 'final'
in `src/main/java/org/apache/sysds/lops/BinaryM.java`
#### Snippet
```java
	}
	
	private OpOp2 _operation;
	private VectorType _vectorType = null; 
	
```

### FieldMayBeFinal
Field `operationType` may be 'final'
in `src/main/java/org/apache/sysds/lops/Nary.java`
#### Snippet
```java
 */
public class Nary extends Lop {
	private OpOpN operationType;

	public Nary(OpOpN operationType, DataType dt, ValueType vt, Lop[] inputLops, ExecType et)
```

### FieldMayBeFinal
Field `_type` may be 'final'
in `src/main/java/org/apache/sysds/lops/LeftIndex.java`
#### Snippet
```java
	public static final String OPCODE = "leftIndex";
	
	private LixCacheType _type;

	public LeftIndex(
```

### FieldMayBeFinal
Field `_lopRewriter` may be 'final'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	};

	private static ThreadLocal<LopRewriter> _lopRewriter = new ThreadLocal<LopRewriter>() {
		@Override protected LopRewriter initialValue() {return new LopRewriter();}
	};
```

### FieldMayBeFinal
Field `_rewriter` may be 'final'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java

	/** Local DML configuration for thread-local config updates */
	private static ThreadLocal<ProgramRewriter> _rewriter = new ThreadLocal<ProgramRewriter>() {
		@Override protected ProgramRewriter initialValue() { return new ProgramRewriter(false, true); }
	};
```

### FieldMayBeFinal
Field `_memoHop` may be 'final'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	
	// additional reused objects to avoid repeated, incremental reallocation on deepCopyDags
	private static ThreadLocal<HashMap<Long,Hop>> _memoHop = new ThreadLocal<HashMap<Long,Hop>>() {
		@Override protected HashMap<Long,Hop> initialValue() { return new HashMap<>(); }
		@Override public HashMap<Long,Hop> get() { var tmp = super.get(); tmp.clear(); return tmp; }
```

### FieldMayBeFinal
Field `_operation` may be 'final'
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java
public class ParameterizedBuiltin extends Lop 
{
	private ParamBuiltinOp _operation;
	private HashMap<String, Lop> _inputParams;
	private boolean _bRmEmptyBC;
```

### FieldMayBeFinal
Field `_inputParams` may be 'final'
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java
{
	private ParamBuiltinOp _operation;
	private HashMap<String, Lop> _inputParams;
	private boolean _bRmEmptyBC;

```

### FieldMayBeFinal
Field `_blocksize` may be 'final'
in `src/main/java/org/apache/sysds/lops/ReBlock.java`
#### Snippet
```java
	private boolean _outputEmptyBlocks = true;
	
	private int _blocksize;

	public ReBlock(Lop input, int blen, DataType dt, ValueType vt, boolean outputEmptyBlocks, ExecType et)
```

### FieldMayBeFinal
Field `_outputNames` may be 'final'
in `src/main/java/org/apache/sysds/lops/FunctionCallCP.java`
#### Snippet
```java
	private String _fname;
	private String[] _inputNames;
	private String[] _outputNames;
	private ArrayList<Lop> _outputLops = null;
	private boolean _opt;
```

### FieldMayBeFinal
Field `_inputNames` may be 'final'
in `src/main/java/org/apache/sysds/lops/FunctionCallCP.java`
#### Snippet
```java
	private String _fnamespace;
	private String _fname;
	private String[] _inputNames;
	private String[] _outputNames;
	private ArrayList<Lop> _outputLops = null;
```

### FieldMayBeFinal
Field `_fname` may be 'final'
in `src/main/java/org/apache/sysds/lops/FunctionCallCP.java`
#### Snippet
```java
{	
	private String _fnamespace;
	private String _fname;
	private String[] _inputNames;
	private String[] _outputNames;
```

### FieldMayBeFinal
Field `_fnamespace` may be 'final'
in `src/main/java/org/apache/sysds/lops/FunctionCallCP.java`
#### Snippet
```java
public class FunctionCallCP extends Lop
{	
	private String _fnamespace;
	private String _fname;
	private String[] _inputNames;
```

### FieldMayBeFinal
Field `_inputParams` may be 'final'
in `src/main/java/org/apache/sysds/lops/Data.java`
#### Snippet
```java
	private final OpOpData _op;
	private final boolean literal_var;
	private HashMap<String, Lop> _inputParams;

	/**
```

### FieldMayBeFinal
Field `_order` may be 'final'
in `src/main/java/org/apache/sysds/lops/compile/linearization/CostBasedLinearize.java`
#### Snippet
```java
	private static class Order
	{
		private List<Lop> _order;
		private double _pinnedMemEstimate;
		private double _bufferpoolEstimate;
```

### FieldMayBeFinal
Field `LOOKUP_VALID_EMPTY_UNARY` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	
	//valid unary operation types for empty (sparse-safe) operations (not all operations apply)
	private static OpOp1[] LOOKUP_VALID_EMPTY_UNARY = new OpOp1[]{OpOp1.ABS, OpOp1.SIN, OpOp1.TAN, OpOp1.SQRT, OpOp1.ROUND, OpOp1.CUMSUM}; 
	
	//valid pseudo-sparse-safe binary operators for wdivmm 
```

### FieldMayBeFinal
Field `LOOKUP_VALID_WUMM_UNARY` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	
	//valid unary and binary operators for wumm
	private static OpOp1[] LOOKUP_VALID_WUMM_UNARY = new OpOp1[]{OpOp1.ABS, OpOp1.ROUND, OpOp1.CEIL, OpOp1.FLOOR, OpOp1.EXP, OpOp1.LOG, OpOp1.SQRT,  OpOp1.SIGMOID, OpOp1.SPROP}; 
	private static OpOp2[] LOOKUP_VALID_WUMM_BINARY = new OpOp2[]{OpOp2.MULT, OpOp2.POW}; 
	
```

### FieldMayBeFinal
Field `LOOKUP_VALID_ROW_COL_AGGREGATE` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
{
	//valid aggregation operation types for rowOp to Op conversions (not all operations apply)
	private static AggOp[] LOOKUP_VALID_ROW_COL_AGGREGATE = new AggOp[]{AggOp.SUM, AggOp.SUM_SQ, AggOp.MIN, AggOp.MAX, AggOp.MEAN, AggOp.VAR};
	
	//valid aggregation operation types for empty (sparse-safe) operations (not all operations apply)
```

### FieldMayBeFinal
Field `LOOKUP_VALID_UNNECESSARY_AGGREGATE` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	//AggOp.MEAN currently not due to missing count/corrections
	private static AggOp[] LOOKUP_VALID_EMPTY_AGGREGATE = new AggOp[]{AggOp.SUM, AggOp.SUM_SQ, AggOp.MIN, AggOp.MAX, AggOp.PROD, AggOp.TRACE};
	private static AggOp[] LOOKUP_VALID_UNNECESSARY_AGGREGATE = new AggOp[]{AggOp.SUM, AggOp.MIN, AggOp.MAX, AggOp.PROD, AggOp.TRACE};
	
	//valid unary operation types for empty (sparse-safe) operations (not all operations apply)
```

### FieldMayBeFinal
Field `LOOKUP_VALID_WDIVMM_BINARY` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	
	//valid pseudo-sparse-safe binary operators for wdivmm 
	private static OpOp2[] LOOKUP_VALID_WDIVMM_BINARY = new OpOp2[]{OpOp2.MULT, OpOp2.DIV}; 
	
	//valid unary and binary operators for wumm
```

### FieldMayBeFinal
Field `LOOKUP_VALID_EMPTY_AGGREGATE` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	//valid aggregation operation types for empty (sparse-safe) operations (not all operations apply)
	//AggOp.MEAN currently not due to missing count/corrections
	private static AggOp[] LOOKUP_VALID_EMPTY_AGGREGATE = new AggOp[]{AggOp.SUM, AggOp.SUM_SQ, AggOp.MIN, AggOp.MAX, AggOp.PROD, AggOp.TRACE};
	private static AggOp[] LOOKUP_VALID_UNNECESSARY_AGGREGATE = new AggOp[]{AggOp.SUM, AggOp.MIN, AggOp.MAX, AggOp.PROD, AggOp.TRACE};
	
```

### FieldMayBeFinal
Field `LOOKUP_VALID_WUMM_BINARY` may be 'final'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	//valid unary and binary operators for wumm
	private static OpOp1[] LOOKUP_VALID_WUMM_UNARY = new OpOp1[]{OpOp1.ABS, OpOp1.ROUND, OpOp1.CEIL, OpOp1.FLOOR, OpOp1.EXP, OpOp1.LOG, OpOp1.SQRT,  OpOp1.SIGMOID, OpOp1.SPROP}; 
	private static OpOp2[] LOOKUP_VALID_WUMM_BINARY = new OpOp2[]{OpOp2.MULT, OpOp2.POW}; 
	
	@Override
```

### FieldMayBeFinal
Field `_RS` may be 'final'
in `src/main/java/org/apache/sysds/utils/ParameterBuilder.java`
#### Snippet
```java
	
	/** String used to replace variables in scripts */
	private static String _RS = "\\$\\$";

	/**
```

### FieldMayBeFinal
Field `_cpInstMiscCount` may be 'final'
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
	// provide a breakdown of how time was spent in that instruction
	private static HashMap<String, HashMap<String, Long>> _cpInstMiscTime = new HashMap<> ();
	private static HashMap<String, HashMap<String, Long>> _cpInstMiscCount = new HashMap<> ();

	/**
```

### FieldMayBeFinal
Field `_cpInstMiscTime` may be 'final'
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
	// Used to record events in a CP Heavy Hitter instruction and
	// provide a breakdown of how time was spent in that instruction
	private static HashMap<String, HashMap<String, Long>> _cpInstMiscTime = new HashMap<> ();
	private static HashMap<String, HashMap<String, Long>> _cpInstMiscCount = new HashMap<> ();

```

### FieldMayBeFinal
Field `numConv2dBwdFilterCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numConv2dBwdDataCalls = new LongAdder();
	private static LongAdder conv2dBwdDataTime = new LongAdder();
	private static LongAdder numConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder conv2dBwdFilterTime = new LongAdder();
	private static LongAdder numSparseConv2dCalls = new LongAdder();
```

### FieldMayBeFinal
Field `numSparseConv2dBwdDataCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numSparseConv2dCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdDataCalls = new LongAdder();

	public static void incrementFailuresCounter() {
```

### FieldMayBeFinal
Field `libMatrixMultTime` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numFailures = new LongAdder();
	private static LongAdder numLibMatrixMultCalls = new LongAdder();
	private static LongAdder libMatrixMultTime = new LongAdder();
	private static LongAdder numConv2dCalls = new LongAdder();
	private static LongAdder conv2dTime = new LongAdder();
```

### FieldMayBeFinal
Field `numSparseConv2dBwdFilterCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder conv2dBwdFilterTime = new LongAdder();
	private static LongAdder numSparseConv2dCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdDataCalls = new LongAdder();

```

### FieldMayBeFinal
Field `conv2dBwdDataTime` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder conv2dTime = new LongAdder();
	private static LongAdder numConv2dBwdDataCalls = new LongAdder();
	private static LongAdder conv2dBwdDataTime = new LongAdder();
	private static LongAdder numConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder conv2dBwdFilterTime = new LongAdder();
```

### FieldMayBeFinal
Field `conv2dBwdFilterTime` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder conv2dBwdDataTime = new LongAdder();
	private static LongAdder numConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder conv2dBwdFilterTime = new LongAdder();
	private static LongAdder numSparseConv2dCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdFilterCalls = new LongAdder();
```

### FieldMayBeFinal
Field `numConv2dBwdDataCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numConv2dCalls = new LongAdder();
	private static LongAdder conv2dTime = new LongAdder();
	private static LongAdder numConv2dBwdDataCalls = new LongAdder();
	private static LongAdder conv2dBwdDataTime = new LongAdder();
	private static LongAdder numConv2dBwdFilterCalls = new LongAdder();
```

### FieldMayBeFinal
Field `conv2dTime` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder libMatrixMultTime = new LongAdder();
	private static LongAdder numConv2dCalls = new LongAdder();
	private static LongAdder conv2dTime = new LongAdder();
	private static LongAdder numConv2dBwdDataCalls = new LongAdder();
	private static LongAdder conv2dBwdDataTime = new LongAdder();
```

### FieldMayBeFinal
Field `numConv2dCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numLibMatrixMultCalls = new LongAdder();
	private static LongAdder libMatrixMultTime = new LongAdder();
	private static LongAdder numConv2dCalls = new LongAdder();
	private static LongAdder conv2dTime = new LongAdder();
	private static LongAdder numConv2dBwdDataCalls = new LongAdder();
```

### FieldMayBeFinal
Field `numLibMatrixMultCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
public class NativeStatistics {
	private static LongAdder numFailures = new LongAdder();
	private static LongAdder numLibMatrixMultCalls = new LongAdder();
	private static LongAdder libMatrixMultTime = new LongAdder();
	private static LongAdder numConv2dCalls = new LongAdder();
```

### FieldMayBeFinal
Field `numSparseConv2dCalls` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
	private static LongAdder numConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder conv2dBwdFilterTime = new LongAdder();
	private static LongAdder numSparseConv2dCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdFilterCalls = new LongAdder();
	private static LongAdder numSparseConv2dBwdDataCalls = new LongAdder();
```

### FieldMayBeFinal
Field `numFailures` may be 'final'
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java

public class NativeStatistics {
	private static LongAdder numFailures = new LongAdder();
	private static LongAdder numLibMatrixMultCalls = new LongAdder();
	private static LongAdder libMatrixMultTime = new LongAdder();
```

### FieldMayBeFinal
Field `_funs` may be 'final'
in `src/main/java/org/apache/sysds/parser/FunctionDictionary.java`
#### Snippet
```java
public class FunctionDictionary<T extends FunctionBlock> {
	/** optimized functions **/
	private Map<String, T> _funs;
	
	/** optional unoptimized functions (no sizes/literals propagated), e.g., for eval **/
```

### FieldMayBeFinal
Field `_pathValue` may be 'final'
in `src/main/java/org/apache/sysds/parser/PathStatement.java`
#### Snippet
```java
{
	// Set of file system paths to packages
	private String _pathValue;
	
	public PathStatement(String pv){
```

### FieldMayBeFinal
Field `_opcode` may be 'final'
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java
	private Expression _left;
	private Expression _right;
	private RelationalOp _opcode;
	
	public RelationalExpression(RelationalOp bop) {
```

### FieldMayBeFinal
Field `_variables` may be 'final'
in `src/main/java/org/apache/sysds/parser/VariableSet.java`
#### Snippet
```java
public class VariableSet 
{
	private HashMap<String,DataIdentifier> _variables;
	
	public VariableSet() {
```

### FieldMayBeFinal
Field `_id` may be 'final'
in `src/main/java/org/apache/sysds/parser/BuiltinConstant.java`
#### Snippet
```java
	pi(Math.PI);
	
	private DoubleIdentifier _id;

	private BuiltinConstant(double d) {
```

### FieldMayBeFinal
Field `_namespaces` may be 'final'
in `src/main/java/org/apache/sysds/parser/DMLProgram.java`
#### Snippet
```java
	
	private ArrayList<StatementBlock> _blocks;
	private Map<String, FunctionDictionary<FunctionStatementBlock>> _namespaces;
	private boolean _containsRemoteParfor;
	
```

### FieldMayBeFinal
Field `_opcode` may be 'final'
in `src/main/java/org/apache/sysds/parser/BooleanExpression.java`
#### Snippet
```java
	private Expression _left;
	private Expression _right;
	private BooleanOp _opcode;
	
	public BooleanExpression(BooleanOp bop){
```

### FieldMayBeFinal
Field `_opcode` may be 'final'
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
	private Expression _left;
	private Expression _right;
	private BinaryOp _opcode;
	
	@Override
```

### FieldMayBeFinal
Field `_targetList` may be 'final'
in `src/main/java/org/apache/sysds/parser/AssignmentStatement.java`
#### Snippet
```java
public class AssignmentStatement extends Statement
{
	private ArrayList<DataIdentifier> _targetList;
	private Expression _source;
	private boolean _isAccum; //+=
```

### FieldMayBeFinal
Field `_targetList` may be 'final'
in `src/main/java/org/apache/sysds/parser/MultiAssignmentStatement.java`
#### Snippet
```java
public class MultiAssignmentStatement extends Statement
{
	private ArrayList<DataIdentifier> _targetList;
	private Expression _source;
	
```

### FieldMayBeFinal
Field `_seqSBID` may be 'final'
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
	protected static final Log LOG = LogFactory.getLog(StatementBlock.class.getName());
	protected static IDSequence _seq = new IDSequence();
	private static IDSequence _seqSBID = new IDSequence();
	protected final long _ID;
	protected final String _name;
```

### FieldMayBeFinal
Field `_varParams` may be 'final'
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
	//parameters if needed, such as for named lists
	private Builtins _opcode;
	private LinkedHashMap<String,Expression> _varParams;
	
	public static final String TF_FN_PARAM_DATA = "target";
```

### FieldMayBeFinal
Field `_lowerRowBound` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
		private FileSystem _fileSystem;
		private FrameBlock _src;
		private int _lowerRowBound;
		private int _upperRowBound;
		Map<String, Integer> _schemaMap;
```

### FieldMayBeFinal
Field `_src` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
		private Path _path;
		private FileSystem _fileSystem;
		private FrameBlock _src;
		private int _lowerRowBound;
		private int _upperRowBound;
```

### FieldMayBeFinal
Field `_fileSystem` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
	private class WriteFileTask implements Callable<Object> {
		private Path _path;
		private FileSystem _fileSystem;
		private FrameBlock _src;
		private int _lowerRowBound;
```

### FieldMayBeFinal
Field `_upperRowBound` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java
		private FrameBlock _src;
		private int _lowerRowBound;
		private int _upperRowBound;
		Map<String, Integer> _schemaMap;

```

### FieldMayBeFinal
Field `_path` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterJSONLParallel.java`
#### Snippet
```java

	private class WriteFileTask implements Callable<Object> {
		private Path _path;
		private FileSystem _fileSystem;
		private FrameBlock _src;
```

### FieldMayBeFinal
Field `csvDefaults` may be 'final'
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
	public static final String  DEFAULT_SCHEMAPARAM = "NULL";
	public static final String DEFAULT_LIBSVM_INDEX_DELIM = ":";
	private static Map<String, Object> csvDefaults;
	static {
		csvDefaults = new HashMap<>();
```

### FieldMayBeFinal
Field `_jobConf` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java
		private InputSplit _split;
		private TextInputFormat _inputFormat;
		private JobConf _jobConf;
		private FrameBlock _dest;
		Map<String, Integer> _schemaMap;
```

### FieldMayBeFinal
Field `_split` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java

	private class ReadRowsTask implements Callable<Object>{
		private InputSplit _split;
		private TextInputFormat _inputFormat;
		private JobConf _jobConf;
```

### FieldMayBeFinal
Field `_inputFormat` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java
	private class ReadRowsTask implements Callable<Object>{
		private InputSplit _split;
		private TextInputFormat _inputFormat;
		private JobConf _jobConf;
		private FrameBlock _dest;
```

### FieldMayBeFinal
Field `_offset` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java
		private FrameBlock _dest;
		Map<String, Integer> _schemaMap;
		private int _offset;

		public ReadRowsTask(InputSplit split, TextInputFormat inputFormat, JobConf jobConf,
```

### FieldMayBeFinal
Field `_dest` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java
		private TextInputFormat _inputFormat;
		private JobConf _jobConf;
		private FrameBlock _dest;
		Map<String, Integer> _schemaMap;
		private int _offset;
```

### FieldMayBeFinal
Field `datasetName` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesHDF5.java`
#### Snippet
```java
	private static final long serialVersionUID = 8646275033790103030L;

	private String datasetName;

	public FileFormatPropertiesHDF5() {
```

### FieldMayBeFinal
Field `_paramDefaults` may be 'final'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
	
	//default external parameter values
	private static HashMap<String, String> _paramDefaults;
	private static HashMap<String, String> _paramDefaults2; //for constrained opt
	
```

### FieldMayBeFinal
Field `_paramNames` may be 'final'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
	
	//external parameter names 
	private static HashSet<String> _paramNames;
	public static final String CHECK            = "check";       //run loop dependency analysis
	public static final String PAR              = "par";         //number of parallel workers
```

### FieldMayBeFinal
Field `_paramDefaults2` may be 'final'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
	//default external parameter values
	private static HashMap<String, String> _paramDefaults;
	private static HashMap<String, String> _paramDefaults2; //for constrained opt
	
	//internal parameter values
```

### FieldMayBeFinal
Field `_vals` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCellParallel.java`
#### Snippet
```java
		private int[] _rlen;
		private int[] _clen;
		private double[] _vals;
		private int _pos;
		
```

### FieldMayBeFinal
Field `_clen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCellParallel.java`
#### Snippet
```java
		
		private int[] _rlen;
		private int[] _clen;
		private double[] _vals;
		private int _pos;
```

### FieldMayBeFinal
Field `_rlen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCellParallel.java`
#### Snippet
```java
		public static final int CAPACITY = 100*1024; //100K elements 
		
		private int[] _rlen;
		private int[] _clen;
		private double[] _vals;
```

### FieldMayBeFinal
Field `_ru` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterTextCellParallel.java`
#### Snippet
```java
		private Path _path;
		private int _rl;
		private int _ru;
		
		public WriteTextTask(Path path, FileSystem fs, TensorBlock src, int rl, int ru) {
```

### FieldMayBeFinal
Field `_path` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterTextCellParallel.java`
#### Snippet
```java
		private FileSystem _fs;
		private TensorBlock _src;
		private Path _path;
		private int _rl;
		private int _ru;
```

### FieldMayBeFinal
Field `_src` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterTextCellParallel.java`
#### Snippet
```java
	private static class WriteTextTask implements Callable<Object> {
		private FileSystem _fs;
		private TensorBlock _src;
		private Path _path;
		private int _rl;
```

### FieldMayBeFinal
Field `_fs` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterTextCellParallel.java`
#### Snippet
```java
	
	private static class WriteTextTask implements Callable<Object> {
		private FileSystem _fs;
		private TensorBlock _src;
		private Path _path;
```

### FieldMayBeFinal
Field `_rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterTextCellParallel.java`
#### Snippet
```java
		private TensorBlock _src;
		private Path _path;
		private int _rl;
		private int _ru;
		
```

### FieldMayBeFinal
Field `_opcode` may be 'final'
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
{
	protected Expression[] _args = null;
	private Builtins _opcode;

	public BuiltinFunctionExpression(ParserRuleContext ctx, Builtins bifop, ArrayList<ParameterExpression> args, String fname) {
```

### FieldMayBeFinal
Field `_job` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
	public static class WriteFileTask implements Callable<Object> {
		private Path _path;
		private JobConf _job;
		private FileSystem _fs;
		private TensorBlock _src;
```

### FieldMayBeFinal
Field `_path` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
	
	public static class WriteFileTask implements Callable<Object> {
		private Path _path;
		private JobConf _job;
		private FileSystem _fs;
```

### FieldMayBeFinal
Field `_ru` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
		private TensorBlock _src;
		private long _rl;
		private long _ru;
		private int _blen;
		
```

### FieldMayBeFinal
Field `_rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
		private FileSystem _fs;
		private TensorBlock _src;
		private long _rl;
		private long _ru;
		private int _blen;
```

### FieldMayBeFinal
Field `_fs` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
		private Path _path;
		private JobConf _job;
		private FileSystem _fs;
		private TensorBlock _src;
		private long _rl;
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
		private long _rl;
		private long _ru;
		private int _blen;
		
		public WriteFileTask(Path path, JobConf job, FileSystem fs, TensorBlock src, long rl, long ru, int blen) {
```

### FieldMayBeFinal
Field `_src` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorWriterBinaryBlockParallel.java`
#### Snippet
```java
		private JobConf _job;
		private FileSystem _fs;
		private TensorBlock _src;
		private long _rl;
		private long _ru;
```

### FieldMayBeFinal
Field `_numThreads` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/TensorReaderTextCellParallel.java`
#### Snippet
```java

public class TensorReaderTextCellParallel extends TensorReaderTextCell {
	private int _numThreads = OptimizerUtils.getParallelTextReadParallelism();
	
	@Override
```

### FieldMayBeFinal
Field `_dmlProg` may be 'final'
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
{
	private static final Log LOG = LogFactory.getLog(DMLTranslator.class.getName());
	private DMLProgram _dmlProg;
	
	public DMLTranslator(DMLProgram dmlp) {
```

### FieldMayBeFinal
Field `byteOrder` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5BufferBuilder.java`
#### Snippet
```java
	private final ByteArrayOutputStream byteArrayOutputStream;
	private final DataOutputStream dataOutputStream;
	private ByteOrder byteOrder = LITTLE_ENDIAN;

	public H5BufferBuilder() {
```

### FieldMayBeFinal
Field `doubleDataType` may be 'final'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/message/H5DataTypeMessage.java`
#### Snippet
```java
public class H5DataTypeMessage extends H5Message {

	private H5DoubleDataType doubleDataType;

	public H5DataTypeMessage(H5RootObject rootObject, BitSet flags, ByteBuffer bb) {
```

### FieldMayBeFinal
Field `_uaop` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/LibTensorAgg.java`
#### Snippet
```java
		private BasicTensorBlock _ret;
		private AggType _aggtype;
		private AggregateUnaryOperator _uaop;
		private int _rl;
		private int _ru;
```

### FieldMayBeFinal
Field `_aggtype` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/LibTensorAgg.java`
#### Snippet
```java
		private BasicTensorBlock _in;
		private BasicTensorBlock _ret;
		private AggType _aggtype;
		private AggregateUnaryOperator _uaop;
		private int _rl;
```

### FieldMayBeFinal
Field `_ru` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/LibTensorAgg.java`
#### Snippet
```java
		private AggregateUnaryOperator _uaop;
		private int _rl;
		private int _ru;

		protected PartialAggTask(BasicTensorBlock in, BasicTensorBlock ret, AggType aggtype, AggregateUnaryOperator uaop, int rl, int ru) {
```

### FieldMayBeFinal
Field `_rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/LibTensorAgg.java`
#### Snippet
```java
		private AggType _aggtype;
		private AggregateUnaryOperator _uaop;
		private int _rl;
		private int _ru;

```

### FieldMayBeFinal
Field `_in` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/LibTensorAgg.java`
#### Snippet
```java

	private static class PartialAggTask extends AggTask {
		private BasicTensorBlock _in;
		private BasicTensorBlock _ret;
		private AggType _aggtype;
```

### FieldMayBeFinal
Field `_indexes` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/IndexedTensorBlock.java`
#### Snippet
```java
	private static final long serialVersionUID = -6227311506740546446L;

	private TensorIndexes _indexes;
	private TensorBlock _block = null;

```

### FieldMayBeFinal
Field `retijv` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockCOO.java`
#### Snippet
```java
		private int _pos = 0; //current nnz position
		private int _len = 0; //upper nnz position (exclusive)
		private IJV retijv = new IJV(); //reuse output tuple

		protected SparseBlockCOOIterator(int posrl, int posru) {
```

### FieldMayBeFinal
Field `retijv` may be 'final'
in `src/main/java/org/apache/sysds/runtime/data/SparseBlock.java`
#### Snippet
```java
		private double[] _curValues = null; //current col values
 		private boolean _noNext = false; //end indicator		
		private IJV retijv = new IJV(); //reuse output tuple

		protected SparseBlockIterator(int ru) {
```

### FieldMayBeFinal
Field `sampleRaw` may be 'final'
in `src/main/java/org/apache/sysds/runtime/iogen/SampleProperties.java`
#### Snippet
```java
	protected static final Log LOG = LogFactory.getLog(CustomProperties.class.getName());

	private String sampleRaw;
	private MatrixBlock sampleMatrix;
	private FrameBlock sampleFrame;
```

### FieldMayBeFinal
Field `valueType` may be 'final'
in `src/main/java/org/apache/sysds/runtime/iogen/ValueTrimFormat.java`
#### Snippet
```java
	private char[] N; // array of none zero chars. Example: value = 0.00012345, N = [1,2,3,4,5]
	private String NString;
	private Types.ValueType valueType;

	public ValueTrimFormat(int actualValue) {
```

### FieldMayBeFinal
Field `sampleMatrix` may be 'final'
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
	public static class MatrixReaderMapping extends ReaderMapping {

		private MatrixBlock sampleMatrix;

		public MatrixReaderMapping(String raw, MatrixBlock matrix) throws Exception {
```

### FieldMayBeFinal
Field `schema` may be 'final'
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java

		private FrameBlock sampleFrame;
		private Types.ValueType[] schema;

		public FrameReaderMapping(String raw, FrameBlock frame) throws Exception {
```

### FieldMayBeFinal
Field `sampleFrame` may be 'final'
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
	public static class FrameReaderMapping extends ReaderMapping {

		private FrameBlock sampleFrame;
		private Types.ValueType[] schema;

```

### FieldMayBeFinal
Field `value` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/BinaryBlockToTextCellConverter.java`
#### Snippet
```java
	private int bcolumn;
	
	private Text value=new Text();
	private Pair<NullWritable, Text> pair=new Pair<>(NullWritable.get(), value);
	
```

### FieldMayBeFinal
Field `startIndexes` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/BinaryBlockToTextCellConverter.java`
#### Snippet
```java
	private boolean sparse=true;
	private int thisBlockWidth=0;
	private MatrixIndexes startIndexes=new MatrixIndexes();
	private boolean hasValue=false;
	private int brow;
```

### FieldMayBeFinal
Field `pair` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/BinaryBlockToTextCellConverter.java`
#### Snippet
```java
	
	private Text value=new Text();
	private Pair<NullWritable, Text> pair=new Pair<>(NullWritable.get(), value);
	
	private void reset()
```

### FieldMayBeFinal
Field `st` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/TextToBinaryCellConverter.java`
#### Snippet
```java
	private MatrixCell value = new MatrixCell();
	private Pair<MatrixIndexes, MatrixCell> pair = new Pair<>(indexes, value);
	private FastStringTokenizer st = new FastStringTokenizer(' '); 
	private boolean hasValue = false;
	private boolean toIgnore = false;
```

### FieldMayBeFinal
Field `pair` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/TextToBinaryCellConverter.java`
#### Snippet
```java
	private MatrixIndexes indexes = new MatrixIndexes();
	private MatrixCell value = new MatrixCell();
	private Pair<MatrixIndexes, MatrixCell> pair = new Pair<>(indexes, value);
	private FastStringTokenizer st = new FastStringTokenizer(' '); 
	private boolean hasValue = false;
```

### FieldMayBeFinal
Field `value` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/TextToBinaryCellConverter.java`
#### Snippet
```java
{
	private MatrixIndexes indexes = new MatrixIndexes();
	private MatrixCell value = new MatrixCell();
	private Pair<MatrixIndexes, MatrixCell> pair = new Pair<>(indexes, value);
	private FastStringTokenizer st = new FastStringTokenizer(' '); 
```

### FieldMayBeFinal
Field `indexes` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/TextToBinaryCellConverter.java`
#### Snippet
```java
implements Converter<LongWritable, Text, MatrixIndexes, MatrixCell>
{
	private MatrixIndexes indexes = new MatrixIndexes();
	private MatrixCell value = new MatrixCell();
	private Pair<MatrixIndexes, MatrixCell> pair = new Pair<>(indexes, value);
```

### FieldMayBeFinal
Field `_rgen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		private int _cu = -1;
		private MatrixBlock _out = null;
		private RandomMatrixGenerator _rgen = new RandomMatrixGenerator();
		private long _bSeed = 0;
		private long[] _seeds = null;
```

### FieldMayBeFinal
Field `_seqRandInput` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
	private static final long PAR_NUMCELL_THRESHOLD = 512*1024; //Min 500k elements
	
	private static IDSequence _seqRandInput = new IDSequence(); 
	
	private LibMatrixDatagen() {
```

### FieldMayBeFinal
Field `cm` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/CM_N_COVCell.java`
#### Snippet
```java
public class CM_N_COVCell extends MatrixValue
{
	private CM_COV_Object cm=new CM_COV_Object();
	
	@Override
```

### FieldMayBeFinal
Field `outBuff` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
	private static ThreadLocal<FloatBuffer> biasBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> filterBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> outBuff = new ThreadLocal<>();
	
	// We could encapsulate heuristics in this function
```

### FieldMayBeFinal
Field `biasBuff` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
	//   to ensure there are no additional copies created by the transfer over jni
	private static ThreadLocal<FloatBuffer> inBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> biasBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> filterBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> outBuff = new ThreadLocal<>();
```

### FieldMayBeFinal
Field `inBuff` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
	//   preallocated direct buffers (with thread-local reuse and resizing on demand)
	//   to ensure there are no additional copies created by the transfer over jni
	private static ThreadLocal<FloatBuffer> inBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> biasBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> filterBuff = new ThreadLocal<>();
```

### FieldMayBeFinal
Field `filterBuff` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
	private static ThreadLocal<FloatBuffer> inBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> biasBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> filterBuff = new ThreadLocal<>();
	private static ThreadLocal<FloatBuffer> outBuff = new ThreadLocal<>();
	
```

### FieldMayBeFinal
Field `outputArray` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
		private final int _rl, _ru; 
		private final DnnParameters _params;
		private double [] outputArray;
		private final int C, P, Q, W, H, CPQ, PQ;
		private final PoolingType _poolingType;
```

### FieldMayBeFinal
Field `smallestHashes` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java

	private Set<Double> containedSet;
	private PriorityQueue<Double> smallestHashes;
	private int k;

```

### FieldMayBeFinal
Field `containedSet` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java
	private static final Log LOG = LogFactory.getLog(SmallestPriorityQueue.class.getName());

	private Set<Double> containedSet;
	private PriorityQueue<Double> smallestHashes;
	private int k;
```

### FieldMayBeFinal
Field `k` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java
	private Set<Double> containedSet;
	private PriorityQueue<Double> smallestHashes;
	private int k;

	public SmallestPriorityQueue(int k) {
```

### FieldMayBeFinal
Field `_src` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java

	//janino-specific map of source code transfer/recompile on-demand
	private static ConcurrentHashMap<String, String> _src = new ConcurrentHashMap<>();

	private static ConcurrentHashMap<String, Integer> _CUDA_op_IDs = new ConcurrentHashMap<>();
```

### FieldMayBeFinal
Field `_cache` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java

	//cache to reuse compiled and loaded classes
	private static ConcurrentHashMap<String, Class<?>> _cache = new ConcurrentHashMap<>();

	//janino-specific map of source code transfer/recompile on-demand
```

### FieldMayBeFinal
Field `_CUDA_op_IDs` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
	private static ConcurrentHashMap<String, String> _src = new ConcurrentHashMap<>();

	private static ConcurrentHashMap<String, Integer> _CUDA_op_IDs = new ConcurrentHashMap<>();
	private static ConcurrentHashMap<Integer, String> _CUDA_op_src = new ConcurrentHashMap<>();

```

### FieldMayBeFinal
Field `_CUDA_op_src` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java

	private static ConcurrentHashMap<String, Integer> _CUDA_op_IDs = new ConcurrentHashMap<>();
	private static ConcurrentHashMap<Integer, String> _CUDA_op_src = new ConcurrentHashMap<>();

	//javac-specific working directory for src/class files
```

### FieldMayBeFinal
Field `_idSeq` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java

public class LineageItem {
	private static IDSequence _idSeq = new IDSequence();
	
	private final long _id;
```

### FieldMayBeFinal
Field `_visited` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
	
	//TODO replace with thread local concurrent hashmap per worker
	private Map<Long, Boolean> _visited = new ConcurrentHashMap<>();
	
	public enum LineageItemType {Literal, Creation, Instruction, Dedup}
```

### FieldMayBeFinal
Field `_codegentraces` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCodegenItem.java`
#### Snippet
```java

public class LineageCodegenItem {
	private static Map<String, LineageItem> _codegentraces = new HashMap<>();

	public static void reset() {
```

### FieldMayBeFinal
Field `_distinctPaths` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupBlock.java`
#### Snippet
```java

public class LineageDedupBlock {
	private Map<Long, LineageMap> _distinctPaths = new HashMap<>();
	private BitSet _path = new BitSet();
	private int _numPaths = 0;
```

### FieldMayBeFinal
Field `_numDistinctPaths` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupBlock.java`
#### Snippet
```java
	
	private long _activePath = -1;
	private ArrayList<Long> _numDistinctPaths = new ArrayList<>();
	
	public LineageMap getActiveMap() {
```

### FieldMayBeFinal
Field `_path` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupBlock.java`
#### Snippet
```java
public class LineageDedupBlock {
	private Map<Long, LineageMap> _distinctPaths = new HashMap<>();
	private BitSet _path = new BitSet();
	private int _numPaths = 0;
	
```

### FieldMayBeFinal
Field `cu` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private int ru;
		private int cl;
		private int cu;

		protected TransposeDenseToSparseTask(DenseBlock a, SparseRowVector[] rows, int rl, int ru, int cl, int cu) {
```

### FieldMayBeFinal
Field `rows` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static class TransposeDenseToSparseTask implements Callable<Object> {
		private DenseBlock a;
		private SparseRowVector[] rows;
		private int rl;
		private int ru;
```

### FieldMayBeFinal
Field `rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private DenseBlock a;
		private SparseRowVector[] rows;
		private int rl;
		private int ru;
		private int cl;
```

### FieldMayBeFinal
Field `cl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private int rl;
		private int ru;
		private int cl;
		private int cu;

```

### FieldMayBeFinal
Field `ru` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		private SparseRowVector[] rows;
		private int rl;
		private int ru;
		private int cl;
		private int cu;
```

### FieldMayBeFinal
Field `a` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java

	private static class TransposeDenseToSparseTask implements Callable<Object> {
		private DenseBlock a;
		private SparseRowVector[] rows;
		private int rl;
```

### FieldMayBeFinal
Field `memPool` may be 'final'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java

	/** Thread local temporary double array.. */
	private static ThreadLocal<double[]> memPool = new ThreadLocal<double[]>() {
        @Override
        protected double[] initialValue() {
```

### FieldMayBeFinal
Field `weightedQueue` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageSparkCacheEviction.java`
#### Snippet
```java
	private static long SPARK_STORAGE_LIMIT = 0; //60% (upper limit of Spark unified memory)
	private static long _sparkStorageSize = 0; //current size
	private static TreeSet<LineageCacheEntry> weightedQueue = new TreeSet<>(LineageCacheConfig.LineageCacheComparator);

	protected static void resetEviction() {
```

### FieldMayBeFinal
Field `weightedQueue` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
	protected static final Map<LineageItem, Integer> _removelist = new HashMap<>();
	private static String _outdir = null;
	private static TreeSet<LineageCacheEntry> weightedQueue = new TreeSet<>(LineageCacheConfig.LineageCacheComparator);
	
	protected static void resetEviction() {
```

### FieldMayBeFinal
Field `GPUCacheEntries` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageGPUCacheEviction.java`
#### Snippet
```java

	// All cached pointers mapped to the corresponding lineage cache entries
	private static HashMap<Pointer, LineageCacheEntry> GPUCacheEntries = new HashMap<>();

	protected static void resetEviction() {
```

### FieldMayBeFinal
Field `freeQueues` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageGPUCacheEviction.java`
#### Snippet
```java

	// Weighted queue of freed pointers.
	private static HashMap<Long, TreeSet<LineageCacheEntry>> freeQueues = new HashMap<>();

	// Pointers and live counts associated
```

### FieldMayBeFinal
Field `livePointers` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageGPUCacheEviction.java`
#### Snippet
```java

	// Pointers and live counts associated
	private static HashMap<Pointer, Integer> livePointers = new HashMap<>();

	// All cached pointers mapped to the corresponding lineage cache entries
```

### FieldMayBeFinal
Field `INSTCOUNT` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
public class LineageEstimatorStatistics {
	private static final LongAdder _ctimeSaved      = new LongAdder(); //in nano sec
	private static int INSTCOUNT = 10;
	
	public static void reset() {
```

### FieldMayBeFinal
Field `savedOPComparator` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
	private static final double CACHE_FRAC = 0.05; //5% of JVM
	protected static long CACHE_LIMIT;
	private static Comparator<MutableTriple<String, Long, Double>> savedOPComparator = (op1, op2) -> {
		return op1.getRight() == op2.getRight() ? 0 : op1.getRight() < op2.getRight() ? 1 : -1;  
	};
```

### FieldMayBeFinal
Field `_pointer` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEntry.java`
#### Snippet
```java

	static class GPUPointer {
		private Pointer _pointer;
		private long _allocatedSize; //bytes
		private MetaData _metadata;
```

### FieldMayBeFinal
Field `_allocatedSize` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEntry.java`
#### Snippet
```java
	static class GPUPointer {
		private Pointer _pointer;
		private long _allocatedSize; //bytes
		private MetaData _metadata;

```

### FieldMayBeFinal
Field `_metadata` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEntry.java`
#### Snippet
```java
		private Pointer _pointer;
		private long _allocatedSize; //bytes
		private MetaData _metadata;

		public GPUPointer(Pointer pointer, long size, MetaData metadata) {
```

### FieldMayBeFinal
Field `_numDistinctPaths` may be 'final'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupUtils.java`
#### Snippet
```java
	private static Lineage _tmpLineage = null;
	private static Lineage _mainLineage = null;
	private static ArrayList<Long> _numDistinctPaths = new ArrayList<>();
	private static long _maxNumPaths = 0;
	private static int _numPaths = 0;
```

### FieldMayBeFinal
Field `singleton` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/SingletonLookupHashMap.java`
#### Snippet
```java
public final class SingletonLookupHashMap {
	// Shared singleton map
	private static SingletonLookupHashMap singleton = new SingletonLookupHashMap();

	private final HashMap<Integer, Object> map;
```

### FieldMayBeFinal
Field `samplePower` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettingsBuilder.java`
#### Snippet
```java
	private double samplingRatio;
	// private double samplePower = 0.6;
	private double samplePower = 0.65;
	// private double samplePower = 0.68;
	// private double samplePower = 0.7;
```

### FieldMayBeFinal
Field `_len1` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
		private final double[][] _data;
		private int _pos;
		private int _len1;
		private int _len2;
		
```

### FieldMayBeFinal
Field `mod` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
{
	private static IntegerDivide intDiv = IntegerDivide.getFnObject();
	private static Modulus mod = Modulus.getFnObject();
	private static BitwAnd bwAnd = BitwAnd.getBitwAndFnObject();
	
```

### FieldMayBeFinal
Field `_len2` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
		private int _pos;
		private int _len1;
		private int _len2;
		
		public VectorBuffer(int num, int len1, int len2) {
```

### FieldMayBeFinal
Field `memPool` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
	//global pool of reusable vectors, individual operations set up their own thread-local
	//ring buffers of reusable vectors with specific number of vectors and vector sizes 
	private static ThreadLocal<VectorBuffer> memPool = new ThreadLocal<VectorBuffer>() {
		@Override protected VectorBuffer initialValue() { return new VectorBuffer(0,0,0); }
	};
```

### FieldMayBeFinal
Field `bwAnd` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
	private static IntegerDivide intDiv = IntegerDivide.getFnObject();
	private static Modulus mod = Modulus.getFnObject();
	private static BitwAnd bwAnd = BitwAnd.getBitwAndFnObject();
	
	//global pool of reusable vectors, individual operations set up their own thread-local
```

### FieldMayBeFinal
Field `intDiv` may be 'final'
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
public class LibSpoofPrimitives 
{
	private static IntegerDivide intDiv = IntegerDivide.getFnObject();
	private static Modulus mod = Modulus.getFnObject();
	private static BitwAnd bwAnd = BitwAnd.getBitwAndFnObject();
```

### FieldMayBeFinal
Field `_arr` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArray.java`
#### Snippet
```java
 */
public class DblArray {
	private double[] _arr;
	private int _hash;

```

### FieldMayBeFinal
Field `_values` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/MultiColBitmap.java`
#### Snippet
```java

	/** Distinct tuples that appear in the columnGroup */
	private double[][] _values;

	/**
```

### FieldMayBeFinal
Field `_values` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/Bitmap.java`
#### Snippet
```java

	/** Distinct values contained in the bitmap */
	private double[] _values;

	/**
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/readers/ReaderColumnSelectionDenseMultiBlockTransposed.java`
#### Snippet
```java

public class ReaderColumnSelectionDenseMultiBlockTransposed extends ReaderColumnSelection {
	private DenseBlock _data;

	protected ReaderColumnSelectionDenseMultiBlockTransposed(MatrixBlock data, IColIndex colIndices, int rl, int ru) {
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/readers/ReaderColumnSelectionDenseMultiBlock.java`
#### Snippet
```java

public class ReaderColumnSelectionDenseMultiBlock extends ReaderColumnSelection {
	private DenseBlock _data;

	protected ReaderColumnSelectionDenseMultiBlock(MatrixBlock data, IColIndex colIndices, int rl, int ru) {
```

### FieldMayBeFinal
Field `cacheRow` may be 'final'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java

	/** Thread local cache for a single recently used Iterator, this is used for cache blocking */
	private ThreadLocal<OffsetCache> cacheRow = new ThreadLocal<OffsetCache>() {
		@Override
		protected OffsetCache initialValue() {
```

### FieldMayBeFinal
Field `_NAstrings` may be 'final'
in `src/main/java/org/apache/sysds/runtime/transform/TfUtils.java`
#### Snippet
```java
		+ Lop.INSTRUCTION_DELIMITOR +Lop.DATATYPE_PREFIX;
	
	private String[] _NAstrings = null;
	
	public String[] getNAStrings() { return _NAstrings; }
```

### FieldMayBeFinal
Field `subTokens` may be 'final'
in `src/main/java/org/apache/sysds/runtime/transform/tokenize/Token.java`
#### Snippet
```java
	}

	private List<SubToken> subTokens;

	private Token(int subListSize){
```

### FieldMayBeFinal
Field `_user` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java

public class SqlCPInstruction extends CPInstruction {
	private CPOperand _conn, _user, _pass, _query, _output;
	
	public SqlCPInstruction(CPOperand conn, CPOperand user, CPOperand pass, CPOperand query, CPOperand out,
```

### FieldMayBeFinal
Field `_output` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java

public class SqlCPInstruction extends CPInstruction {
	private CPOperand _conn, _user, _pass, _query, _output;
	
	public SqlCPInstruction(CPOperand conn, CPOperand user, CPOperand pass, CPOperand query, CPOperand out,
```

### FieldMayBeFinal
Field `_conn` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java

public class SqlCPInstruction extends CPInstruction {
	private CPOperand _conn, _user, _pass, _query, _output;
	
	public SqlCPInstruction(CPOperand conn, CPOperand user, CPOperand pass, CPOperand query, CPOperand out,
```

### FieldMayBeFinal
Field `_query` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java

public class SqlCPInstruction extends CPInstruction {
	private CPOperand _conn, _user, _pass, _query, _output;
	
	public SqlCPInstruction(CPOperand conn, CPOperand user, CPOperand pass, CPOperand query, CPOperand out,
```

### FieldMayBeFinal
Field `_pass` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/SqlCPInstruction.java`
#### Snippet
```java

public class SqlCPInstruction extends CPInstruction {
	private CPOperand _conn, _user, _pass, _query, _output;
	
	public SqlCPInstruction(CPOperand conn, CPOperand user, CPOperand pass, CPOperand query, CPOperand out,
```

### FieldMayBeFinal
Field `_strBuilders` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java

	//thread-local string builders for instruction concatenation (avoid allocation)
	private static ThreadLocal<StringBuilder> _strBuilders = new ThreadLocal<StringBuilder>() {
		@Override
		protected StringBuilder initialValue() { 
```

### FieldMayBeFinal
Field `method` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
public class DataGenCPInstruction extends UnaryCPInstruction {
	private static final Log LOG = LogFactory.getLog(DataGenCPInstruction.class.getName());
	private OpOpDG method;

	private final CPOperand rows, cols, dims;
```

### FieldMayBeFinal
Field `_qop` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryWDivMMFEDInstruction.java`
#### Snippet
```java
	*/

	private QuaternaryOperator _qop;

	protected QuaternaryWDivMMFEDInstruction(QuaternaryOperator operator,
```

### FieldMayBeFinal
Field `blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReblockFEDInstruction.java`
#### Snippet
```java

public class ReblockFEDInstruction extends UnaryFEDInstruction {
	private int blen;

	private ReblockFEDInstruction(Operator op, CPOperand in, CPOperand out, int blen, boolean emptyBlocks,
```

### FieldMayBeFinal
Field `_addresses` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
	public static final String FED_FRAME_IDENTIFIER = "frame";

	private CPOperand _type, _addresses, _ranges, _localObject, _output;

	public InitFEDInstruction(CPOperand type, CPOperand addresses, CPOperand ranges, CPOperand out, String opcode,
```

### FieldMayBeFinal
Field `_type` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
	public static final String FED_FRAME_IDENTIFIER = "frame";

	private CPOperand _type, _addresses, _ranges, _localObject, _output;

	public InitFEDInstruction(CPOperand type, CPOperand addresses, CPOperand ranges, CPOperand out, String opcode,
```

### FieldMayBeFinal
Field `_ranges` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
	public static final String FED_FRAME_IDENTIFIER = "frame";

	private CPOperand _type, _addresses, _ranges, _localObject, _output;

	public InitFEDInstruction(CPOperand type, CPOperand addresses, CPOperand ranges, CPOperand out, String opcode,
```

### FieldMayBeFinal
Field `_output` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
	public static final String FED_FRAME_IDENTIFIER = "frame";

	private CPOperand _type, _addresses, _ranges, _localObject, _output;

	public InitFEDInstruction(CPOperand type, CPOperand addresses, CPOperand ranges, CPOperand out, String opcode,
```

### FieldMayBeFinal
Field `_inputs` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
		private final SpoofOuterProduct _op;
		private final OutProdType _outProdType;
		private CPOperand[] _inputs;

		SpoofFEDOuterProduct(SpoofOperator op, CPOperand out, FType fedType, CPOperand[] inputs) {
```

### FieldMayBeFinal
Field `_arity` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/BuiltinBinaryGPUInstruction.java`
#### Snippet
```java
public abstract class BuiltinBinaryGPUInstruction extends GPUInstruction {
	@SuppressWarnings("unused")
	private int _arity;

	protected BuiltinBinaryGPUInstruction(Operator op, CPOperand input1, CPOperand input2, CPOperand output,
```

### FieldMayBeFinal
Field `_isLeftTransposed` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/AggregateBinaryGPUInstruction.java`
#### Snippet
```java

public class AggregateBinaryGPUInstruction extends GPUInstruction {
	private boolean _isLeftTransposed;
	private boolean _isRightTransposed;

```

### FieldMayBeFinal
Field `_isRightTransposed` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/AggregateBinaryGPUInstruction.java`
#### Snippet
```java
public class AggregateBinaryGPUInstruction extends GPUInstruction {
	private boolean _isLeftTransposed;
	private boolean _isRightTransposed;

	private AggregateBinaryGPUInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, String opcode,
```

### FieldMayBeFinal
Field `rmvarGPUPointers` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPULazyCudaFreeMemoryManager.java`
#### Snippet
```java
	 * Map of free blocks allocate on GPU. maps size_of_block -> pointer on GPU
	 */
	private HashMap<Long, Set<Pointer>> rmvarGPUPointers = new HashMap<>();
	
	/**
```

### FieldMayBeFinal
Field `maxBlockDimForDevice` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/ExecutionConfig.java`
#### Snippet
```java
	public CUstream stream = null;

	private static HashMap<Integer, Integer> maxBlockDimForDevice = new HashMap<>();

	/**
```

### FieldMayBeFinal
Field `kernels` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java

	private final static String ptxFileName = "/cuda/kernels/SystemDS.ptx";
	private HashMap<String, CUfunction> kernels = new HashMap<>();
	private CUmodule module;

```

### FieldMayBeFinal
Field `module` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/JCudaKernels.java`
#### Snippet
```java
	private final static String ptxFileName = "/cuda/kernels/SystemDS.ptx";
	private HashMap<String, CUfunction> kernels = new HashMap<>();
	private CUmodule module;

	/**
```

### FieldMayBeFinal
Field `output` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BuiltinNarySPInstruction.java`
#### Snippet
```java
{
	private CPOperand[] inputs;
	private CPOperand output;
	
	protected BuiltinNarySPInstruction(CPOperand[] in, CPOperand out, String opcode, String istr) {
```

### FieldMayBeFinal
Field `inputs` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BuiltinNarySPInstruction.java`
#### Snippet
```java
public class BuiltinNarySPInstruction extends SPInstruction implements LineageTraceable
{
	private CPOperand[] inputs;
	private CPOperand output;
	
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private long _startIx; 
		private int _shiftBy; 
		private int _blen;
		private long _outlen;
		
```

### FieldMayBeFinal
Field `_startIx` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		
		private boolean _cbind;
		private long _startIx; 
		private int _shiftBy; 
		private int _blen;
```

### FieldMayBeFinal
Field `_cbind` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 3524189212798209172L;
		
		private boolean _cbind;
		private long _startIx; 
		private int _shiftBy; 
```

### FieldMayBeFinal
Field `_lastIxLeft` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		
		private boolean _cbind;
		private long _lastIxLeft;
		private int _blen;
		
```

### FieldMayBeFinal
Field `_cbind` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 848955582909209400L;
		
		private boolean _cbind;
		private long _lastIxLeft;
		private int _blen;
```

### FieldMayBeFinal
Field `_outlen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private int _shiftBy; 
		private int _blen;
		private long _outlen;
		
		public ShiftMatrix(DataCharacteristics mc1, DataCharacteristics mc2, boolean cbind) {
```

### FieldMayBeFinal
Field `_shiftBy` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private boolean _cbind;
		private long _startIx; 
		private int _shiftBy; 
		private int _blen;
		private long _outlen;
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private boolean _cbind;
		private long _lastIxLeft;
		private int _blen;
		
		public MergeWithShiftedBlocks(DataCharacteristics mc1, DataCharacteristics mc2, boolean cbind)
```

### FieldMayBeFinal
Field `_cbind` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGAlignedSPInstruction.java`
#### Snippet
```java
		
		private long _shiftBy;
		private boolean _cbind;
		
		public ShiftColumnIndex(long shiftBy, boolean cbind) {
```

### FieldMayBeFinal
Field `_shiftBy` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGAlignedSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -5185023611319654242L;
		
		private long _shiftBy;
		private boolean _cbind;
		
```

### FieldMayBeFinal
Field `sizeInBytes` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
	 */
	static class PointerInfo {
		private long sizeInBytes;
		private StackTraceElement[] stackTraceElements;
		public PointerInfo(long sizeInBytes) {
```

### FieldMayBeFinal
Field `_indexDelim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
	private int _blen;
	private String _delim;
	private String _indexDelim;

	protected LIBSVMReblockSPInstruction(Operator op, CPOperand in, CPOperand out, int br, int bc, String opcode,
```

### FieldMayBeFinal
Field `_delim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/LIBSVMReblockSPInstruction.java`
#### Snippet
```java
public class LIBSVMReblockSPInstruction extends UnarySPInstruction {
	private int _blen;
	private String _delim;
	private String _indexDelim;

```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		
		private IndexRange _ixrange;
		private int _blen; 
		
		public SliceBlock2(IndexRange ixrange, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_ixrange` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 7481889252529447770L;
		
		private IndexRange _ixrange;
		private int _blen; 
		
```

### FieldMayBeFinal
Field `_ixrange` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8111291718258309968L;
		
		private IndexRange _ixrange;
		private int _blen; 
		
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		
		private IndexRange _ixrange;
		private int _blen; 
		
		public SliceBlockPartitionFunction(IndexRange ixrange, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `outputEmptyBlocks` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
public class ReblockSPInstruction extends UnarySPInstruction {
	private int blen;
	private boolean outputEmptyBlocks;

	private ReblockSPInstruction(Operator op, CPOperand in, CPOperand out, int br, int bc, boolean emptyBlocks,
```

### FieldMayBeFinal
Field `_output` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
	private CPOperand _input7;
	private CPOperand _input8;
	private CPOperand _output;
	private CPOperand _output2;
	private CPOperand _output3;
```

### FieldMayBeFinal
Field `_input1` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java

public class DnnGPUInstruction extends GPUInstruction {
	private CPOperand _input1;
	private CPOperand _input2;
	private CPOperand _input3;
```

### FieldMayBeFinal
Field `_input2` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/DnnGPUInstruction.java`
#### Snippet
```java
public class DnnGPUInstruction extends GPUInstruction {
	private CPOperand _input1;
	private CPOperand _input2;
	private CPOperand _input3;
	private CPOperand _input4;
```

### FieldMayBeFinal
Field `_minRowIndex` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java

		//boundary keys (inclusive)
		private long _minRowIndex;
		private long _maxRowIndex;
		
```

### FieldMayBeFinal
Field `_maxRowIndex` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java
		//boundary keys (inclusive)
		private long _minRowIndex;
		private long _maxRowIndex;
		
		public FilterFunction(long key25, long key75, int blen) {
```

### FieldMayBeFinal
Field `_minRowIndex` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java
		
		//boundary keys (inclusive)
		private long _minRowIndex;
		private long _maxRowIndex;
		private int _minPos;
```

### FieldMayBeFinal
Field `_maxRowIndex` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java
		//boundary keys (inclusive)
		private long _minRowIndex;
		private long _maxRowIndex;
		private int _minPos;
		private int _maxPos;
```

### FieldMayBeFinal
Field `_maxPos` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java
		private long _maxRowIndex;
		private int _minPos;
		private int _maxPos;
		
		public ExtractAndSumFunction(long key25, long key75, int blen)
```

### FieldMayBeFinal
Field `_minPos` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuantilePickSPInstruction.java`
#### Snippet
```java
		private long _minRowIndex;
		private long _maxRowIndex;
		private int _minPos;
		private int _maxPos;
		
```

### FieldMayBeFinal
Field `_rows` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendRSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5850400295183766409L;

		private long _rows;
				
		public ReduceSideAppendAlignFunction(long rows) {
```

### FieldMayBeFinal
Field `_offset` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameAppendRSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 1723795153048336791L;

		private long _offset;
				
		public ReduceSideAppendRowsFunction(long offset) {
```

### FieldMayBeFinal
Field `_isExpand` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
	private boolean _dim1Literal;
	private boolean _dim2Literal;
	private boolean _isExpand;
	private final boolean _ignoreZeros;
	private final boolean _outputEmptyBlocks;
```

### FieldMayBeFinal
Field `_dim1Literal` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
	private String _outDim1;
	private String _outDim2;
	private boolean _dim1Literal;
	private boolean _dim2Literal;
	private boolean _isExpand;
```

### FieldMayBeFinal
Field `_outDim2` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
public class CtableSPInstruction extends ComputationSPInstruction {
	private String _outDim1;
	private String _outDim2;
	private boolean _dim1Literal;
	private boolean _dim2Literal;
```

### FieldMayBeFinal
Field `_dim2Literal` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
	private String _outDim2;
	private boolean _dim1Literal;
	private boolean _dim2Literal;
	private boolean _isExpand;
	private final boolean _ignoreZeros;
```

### FieldMayBeFinal
Field `_outDim1` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java

public class CtableSPInstruction extends ComputationSPInstruction {
	private String _outDim1;
	private String _outDim2;
	private boolean _dim1Literal;
```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
    private static class AggregateUnarySketchCreateFunction implements Function<Tuple2<MatrixIndexes, MatrixBlock>, CorrMatrixBlock> {
        private static final long serialVersionUID = 7295176181965491548L;
        private CountDistinctOperator op;

        public AggregateUnarySketchCreateFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
    {
        private static final long serialVersionUID = 172215143740379070L;
        private CountDistinctOperator op;

        public AggregateUnarySketchMergeCombinerFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
public class AggregateUnarySketchSPInstruction extends UnarySPInstruction {
    private AggBinaryOp.SparkAggType aggtype;
    private CountDistinctOperator op;

    protected AggregateUnarySketchSPInstruction(Operator op, CPOperand in, CPOperand out, AggBinaryOp.SparkAggType aggtype, String opcode, String instr) {
```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
    {
        private static final long serialVersionUID = -7006864809860460549L;
        private CountDistinctOperator op;

        public AggregateUnarySketchMergeValueFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java

        private static final long serialVersionUID = -3456633769452405482L;
        private CountDistinctOperator _op;

        public RowColGroupingFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
    {
        private static final long serialVersionUID = 7504873483231717138L;
        private CountDistinctOperator op;

        public CalculateAggregateSketchFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `aggtype` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java

public class AggregateUnarySketchSPInstruction extends UnarySPInstruction {
    private AggBinaryOp.SparkAggType aggtype;
    private CountDistinctOperator op;

```

### FieldMayBeFinal
Field `op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
    private static class AggregateUnarySketchUnionAllFunction implements Function2<CorrMatrixBlock, CorrMatrixBlock, CorrMatrixBlock> {
        private static final long serialVersionUID = -3799519241499062936L;
        private CountDistinctOperator op;

        public AggregateUnarySketchUnionAllFunction(CountDistinctOperator op) {
```

### FieldMayBeFinal
Field `_aggtype` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
	private CacheType _type = null;
	private boolean _outputEmpty = true;
	private SparkAggType _aggtype;

	private MapmmSPInstruction(Operator op, CPOperand in1, CPOperand in2, CPOperand out, CacheType type,
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		private BinaryOperator _bOp = null;

		private int _blen;
		
		//reused intermediates  
```

### FieldMayBeFinal
Field `_lastRowBlockIndex` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CumulativeOffsetSPInstruction.java`
#### Snippet
```java
		private double _initValue = 0;
		private int _blen = -1;
		private long _lastRowBlockIndex;
		
		public RDDCumSplitFunction( double initValue, long rlen, int blen )
```

### FieldMayBeFinal
Field `schema_frame` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryFrameFrameSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5850400295183766402L;

		private FrameBlock schema_frame;

		public valueSwapBySchema(FrameBlock fb_name ) {
```

### FieldMayBeFinal
Field `schema_frame` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryFrameFrameSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 5850400295183766400L;

		private FrameBlock schema_frame;

		public isCorrectbySchema(FrameBlock fb_name ) {
```

### FieldMayBeFinal
Field `_encoder` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 6336375833412029279L;

		private MultiColumnEncoder _encoder;

		public TransformEncodeBuildFunction(MultiColumnEncoder encoder) {
```

### FieldMayBeFinal
Field `_encoder` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 6336375833412029279L;

		private EncoderMVImpute _encoder;

		public TransformEncodeBuild2Function(EncoderMVImpute encoder) {
```

### FieldMayBeFinal
Field `_encoder` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 702100641492347459L;

		private EncoderMVImpute _encoder;

		public TransformEncodeGroup2Function(EncoderMVImpute encoder) {
```

### FieldMayBeFinal
Field `_binput` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -911940376947364915L;

		private PartitionedBroadcast<FrameBlock> _binput;
		private IndexRange _ixrange = null;
		
```

### FieldMayBeFinal
Field `_ixrange` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -1655390518299307588L;
		
		private IndexRange _ixrange;
		
		public SliceBlockPartitionFunction(IndexRange ixrange, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_ixrange` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/FrameIndexingSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -5270171193018691692L;
		
		private IndexRange _ixrange;
		
		public SliceBlock(IndexRange ixrange, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_vt` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -512119897654170462L;

		private ValueType _vt;
		private int[] _dims;
		private int _blen;
```

### FieldMayBeFinal
Field `_maxValue` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _frac;
		private boolean _replace;
		private long _maxValue, _partitionSize; 

		GenerateSampleBlock(boolean replace, double frac, long max, long psize)
```

### FieldMayBeFinal
Field `_pdfParams` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _max; 
		private String _pdf; 
		private String _pdfParams;
		
		public GenerateRandomBlock(long rlen, long clen, int blen, double sparsity, double min, double max, String pdf, String pdfParams) {
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private long _rlen; 
		private long _clen;
		private int _blen;
		private double _sparsity; 
		private double _min; 
```

### FieldMayBeFinal
Field `_min` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private int _blen;
		private double _sparsity;
		private String _min;
		private String _max;
		private String _pdf;
```

### FieldMayBeFinal
Field `_pdf` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _min; 
		private double _max; 
		private String _pdf; 
		private String _pdfParams;
		
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private ValueType _vt;
		private int[] _dims;
		private int _blen;
		private double _sparsity;
		private String _min;
```

### FieldMayBeFinal
Field `_min` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private int _blen;
		private double _sparsity; 
		private double _min; 
		private double _max; 
		private String _pdf; 
```

### FieldMayBeFinal
Field `_replace` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -8211490954143527232L;
		private double _frac;
		private boolean _replace;
		private long _maxValue, _partitionSize; 

```

### FieldMayBeFinal
Field `_fillValue` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
	private String _delim;
	private boolean _fill;
	private double _fillValue;
	private Set<String> _naStrings;

```

### FieldMayBeFinal
Field `_sparsity` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private long _clen;
		private int _blen;
		private double _sparsity; 
		private double _min; 
		private double _max; 
```

### FieldMayBeFinal
Field `_naStrings` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
	private boolean _fill;
	private double _fillValue;
	private Set<String> _naStrings;

	protected CSVReblockSPInstruction(Operator op, CPOperand in, CPOperand out, int br, int bc, boolean hasHeader,
```

### FieldMayBeFinal
Field `_sparsity` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private int[] _dims;
		private int _blen;
		private double _sparsity;
		private String _min;
		private String _max;
```

### FieldMayBeFinal
Field `_hasHeader` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
	
	private int _blen;
	private boolean _hasHeader;
	private String _delim;
	private boolean _fill;
```

### FieldMayBeFinal
Field `_clen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		
		private long _rlen; 
		private long _clen;
		private int _blen;
		private double _sparsity; 
```

### FieldMayBeFinal
Field `_rlen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 1616346120426470173L;
		
		private long _rlen; 
		private long _clen;
		private int _blen;
```

### FieldMayBeFinal
Field `_delim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
	private int _blen;
	private boolean _hasHeader;
	private String _delim;
	private boolean _fill;
	private double _fillValue;
```

### FieldMayBeFinal
Field `_max` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _sparsity;
		private String _min;
		private String _max;
		private String _pdf;
		private String _pdfParams;
```

### FieldMayBeFinal
Field `_fill` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
	private boolean _hasHeader;
	private String _delim;
	private boolean _fill;
	private double _fillValue;
	private Set<String> _naStrings;
```

### FieldMayBeFinal
Field `_dims` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

		private ValueType _vt;
		private int[] _dims;
		private int _blen;
		private double _sparsity;
```

### FieldMayBeFinal
Field `_partitionSize` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _frac;
		private boolean _replace;
		private long _maxValue, _partitionSize; 

		GenerateSampleBlock(boolean replace, double frac, long max, long psize)
```

### FieldMayBeFinal
Field `_pdfParams` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private String _max;
		private String _pdf;
		private String _pdfParams;

		public GenerateRandomTensorBlock(ValueType vt, int[] dims, int blen,
```

### FieldMayBeFinal
Field `_frac` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
	{
		private static final long serialVersionUID = -8211490954143527232L;
		private double _frac;
		private boolean _replace;
		private long _maxValue, _partitionSize; 
```

### FieldMayBeFinal
Field `_pdf` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private String _min;
		private String _max;
		private String _pdf;
		private String _pdfParams;

```

### FieldMayBeFinal
Field `_max` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		private double _sparsity; 
		private double _min; 
		private double _max; 
		private String _pdf; 
		private String _pdfParams;
```

### FieldMayBeFinal
Field `_schema` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	private int _blen = -1;
	
	private ValueType[] _schema;

	public FrameReblockBuffer( int buffersize,  long rlen, long clen, ValueType[] schema )
```

### FieldMayBeFinal
Field `_col` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = -1472164797288449559L;
		
		private int _col;
		
		public ExtractColumn(int col) {
```

### FieldMayBeFinal
Field `_pmV` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/OuterVectorBinaryOpFunction.java`
#### Snippet
```java
	
	private BinaryOperator _op;
	private PartitionedBroadcast<MatrixBlock> _pmV;
	
	public OuterVectorBinaryOpFunction( BinaryOperator op, PartitionedBroadcast<MatrixBlock> binput ) 
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/OuterVectorBinaryOpFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 1730704346934726826L;
	
	private BinaryOperator _op;
	private PartitionedBroadcast<MatrixBlock> _pmV;
	
```

### FieldMayBeFinal
Field `_ptV` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/TensorTensorBinaryOpPartitionFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 8029096658247920867L;
	private BinaryOperator _op;
	private PartitionedBroadcast<TensorBlock> _ptV;
	private boolean[] _replicateDim;

```

### FieldMayBeFinal
Field `_replicateDim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/TensorTensorBinaryOpPartitionFunction.java`
#### Snippet
```java
	private BinaryOperator _op;
	private PartitionedBroadcast<TensorBlock> _ptV;
	private boolean[] _replicateDim;

	public TensorTensorBinaryOpPartitionFunction(BinaryOperator op, PartitionedBroadcast<TensorBlock> binput,
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/TensorTensorBinaryOpPartitionFunction.java`
#### Snippet
```java
public class TensorTensorBinaryOpPartitionFunction implements PairFlatMapFunction<Iterator<Tuple2<TensorIndexes, TensorBlock>>, TensorIndexes, TensorBlock> {
	private static final long serialVersionUID = 8029096658247920867L;
	private BinaryOperator _op;
	private PartitionedBroadcast<TensorBlock> _ptV;
	private boolean[] _replicateDim;
```

### FieldMayBeFinal
Field `_rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/IsBlockInRange.java`
#### Snippet
```java
	private static final long serialVersionUID = 5849687296021280540L;
	
	private long _rl; long _ru; long _cl; long _cu;
	private int _blen;
	
```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/IsBlockInRange.java`
#### Snippet
```java
	
	private long _rl; long _ru; long _cl; long _cu;
	private int _blen;
	
	public IsBlockInRange(long rl, long ru, long cl, long cu, DataCharacteristics mc) {
```

### FieldMayBeFinal
Field `_ru` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/IsFrameBlockInRange.java`
#### Snippet
```java
	private static final long serialVersionUID = 4433918122474769296L;

	private long _rl, _ru;
	
	public IsFrameBlockInRange(long rl, long ru, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_rl` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/IsFrameBlockInRange.java`
#### Snippet
```java
	private static final long serialVersionUID = 4433918122474769296L;

	private long _rl, _ru;
	
	public IsFrameBlockInRange(long rl, long ru, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_newBlen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReblockTensorFunction.java`
#### Snippet
```java
	
	private int _numDims;
	private long _newBlen;
	
	public ReblockTensorFunction(int numDims, long newBlen) {
```

### FieldMayBeFinal
Field `_numDims` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReblockTensorFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 9118830682358813489L;
	
	private int _numDims;
	private long _newBlen;
	
```

### FieldMayBeFinal
Field `_deepCopy` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyTensorBlockPairFunction.java`
#### Snippet
```java

	private static final long serialVersionUID = 605514365345997070L;
	private boolean _deepCopy;

	public CopyTensorBlockPairFunction() {
```

### FieldMayBeFinal
Field `_bop` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/MatrixMatrixBinaryOpFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -2683276102742977900L;

	private BinaryOperator _bop;

	public MatrixMatrixBinaryOpFunction(BinaryOperator op) {
```

### FieldMayBeFinal
Field `_deepCopy` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/CopyTensorBlockFunction.java`
#### Snippet
```java
public class CopyTensorBlockFunction implements Function<BasicTensorBlock, BasicTensorBlock> {
	private static final long serialVersionUID = 707987326466592670L;
	private boolean _deepCopy;

	public CopyTensorBlockFunction() {
```

### FieldMayBeFinal
Field `_byDim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReplicateTensorFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 7181347334827684965L;

	private int _byDim;
	private long _numReplicas;

```

### FieldMayBeFinal
Field `_numReplicas` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReplicateTensorFunction.java`
#### Snippet
```java

	private int _byDim;
	private long _numReplicas;

	public ReplicateTensorFunction(int byDim, long numReplicas) {
```

### FieldMayBeFinal
Field `_bop` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/TensorTensorBinaryOpFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 4204525225937988112L;

	private BinaryOperator _bop;

	public TensorTensorBinaryOpFunction(BinaryOperator op) {
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 7664941774566119853L;
		
		private AggregateOperator _op = new AggregateOperator(0, KahanPlus.getKahanPlusFnObject(), CorrectionLocationType.NONE);
		private final boolean _deep;

```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 3703543699467085539L;
		
		private AggregateOperator _op = new AggregateOperator(0, KahanPlus.getKahanPlusFnObject(), CorrectionLocationType.NONE);
		
		private final boolean _deep;
```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/MatrixScalarUnaryFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = 7246757780621114500L;
	
	private ScalarOperator _op;
	
	public MatrixScalarUnaryFunction(ScalarOperator sc_op) {
```

### FieldMayBeFinal
Field `_numReplicas` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReplicateVectorFunction.java`
#### Snippet
```java
	
	private boolean _byRow; 
	private long _numReplicas;
	
	public ReplicateVectorFunction(boolean byRow, long numReplicas) 
```

### FieldMayBeFinal
Field `_byRow` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ReplicateVectorFunction.java`
#### Snippet
```java
	private static final long serialVersionUID = -1505557561471236851L;
	
	private boolean _byRow; 
	private long _numReplicas;
	
```

### FieldMayBeFinal
Field `_mcOut` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		
		private DataCharacteristics _mcIn;
		private DataCharacteristics _mcOut;

		public BinaryBlockToMatrixBlockFunction(DataCharacteristics mcIn, DataCharacteristics mcOut) {
```

### FieldMayBeFinal
Field `_naStrings` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private List<String> _mvMeta = null; //missing value meta data
		private List<String> _ndMeta = null; //num distinct meta data
		private Set<String> _naStrings;
		
		public CSVToBinaryBlockFunction(DataCharacteristics mc, ValueType[] schema, boolean hasHeader, String delim, Set<String> naStrings) {
```

### FieldMayBeFinal
Field `_mcIn` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -2654986510471835933L;
		
		private DataCharacteristics _mcIn;
		private DataCharacteristics _mcOut;

```

### FieldMayBeFinal
Field `_op` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/PerformGroupByAggInCombiner.java`
#### Snippet
```java
	private static final long serialVersionUID = -813530414567786509L;
	
	private Operator _op;
	
	public PerformGroupByAggInCombiner(Operator op) {
```

### FieldMayBeFinal
Field `_asc` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5154839870549241343L;
		
		private boolean _asc;
		public IndexComparator(boolean asc) {
			_asc = asc;
```

### FieldMayBeFinal
Field `_asc` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDSortUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 5531987863790922691L;
		
		private boolean _asc;
		public IndexComparator2(boolean asc) {
			_asc = asc;
```

### FieldMayBeFinal
Field `_containsID` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private double _sparsity = 1.0;
		private boolean _sparse = false;
		private boolean _containsID;
		private boolean _isVector;

```

### FieldMayBeFinal
Field `_clen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = 4441184411670316972L;

		private int _clen;
		private int _blen;
		private boolean _toVector;
```

### FieldMayBeFinal
Field `_delim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
	{
		private static final long serialVersionUID = -4948430402942717043L;
		private String _delim;
		private String _indexDelim;

```

### FieldMayBeFinal
Field `_blen` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private int _clen;
		private int _blen;
		private boolean _toVector;

```

### FieldMayBeFinal
Field `_indexDelim` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private static final long serialVersionUID = -4948430402942717043L;
		private String _delim;
		private String _indexDelim;

		protected LIBSVMToBinaryBlockFunction(DataCharacteristics mc, String delim, String indexDelim) {
```

### FieldMayBeFinal
Field `_toVector` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private int _clen;
		private int _blen;
		private boolean _toVector;

		public ConvertRowBlocksToRows(int clen, int blen, boolean toVector) {
```

### FieldMayBeFinal
Field `_isVector` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private LongAccumulator _aNnz = null;
		private boolean _containsID;
		private boolean _isVector;

		public DataFrameAnalysisFunction( LongAccumulator aNnz, boolean containsID, boolean isVector) {
```

### FieldMayBeFinal
Field `_containsID` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		private LongAccumulator _aNnz = null;
		private boolean _containsID;
		private boolean _isVector;

```

### FieldMayBeFinal
Field `_isVector` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private boolean _sparse = false;
		private boolean _containsID;
		private boolean _isVector;

		public DataFrameToBinaryBlockFunction(DataCharacteristics mc, boolean sparse, boolean containsID, boolean isVector) {
```

### FieldMayBeFinal
Field `_naStrings` may be 'final'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
		private boolean _fill = false;
		private double _fillValue = 0;
		private Set<String> _naStrings;

		public CSVToBinaryBlockFunction(DataCharacteristics mc, boolean sparse, boolean hasHeader, String delim, boolean fill, double fillValue, Set<String> naStrings)
```

### FieldMayBeFinal
Field `_id_rtprog` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreePlanMappingRuntime.java`
#### Snippet
```java
public class OptTreePlanMappingRuntime extends OptTreePlanMapping
{
	private Map<Long, Object> _id_rtprog;

	public OptTreePlanMappingRuntime() {
```

### FieldMayBeFinal
Field `_dpf` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerRemoteSparkMapper.java`
#### Snippet
```java
	private final long _clen;
	private final long _blen;
	private PDataPartitionFormat _dpf;
	private final long _n;
	
```

### FieldMayBeFinal
Field `_id_hlprog` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreePlanMappingAbstract.java`
#### Snippet
```java
	private DMLProgram _prog;
	private Program _rtprog;
	private Map<Long, Object> _id_hlprog;
	private Map<Long, Object> _id_rtprog;
	
```

### FieldMayBeFinal
Field `_id_rtprog` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreePlanMappingAbstract.java`
#### Snippet
```java
	private Program _rtprog;
	private Map<Long, Object> _id_hlprog;
	private Map<Long, Object> _id_rtprog;
	
	public OptTreePlanMappingAbstract( ) {
```

### FieldMayBeFinal
Field `_ck` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTree.java`
#### Snippet
```java

	//global contraints 
	private int     _ck;  //max constraint degree of parallelism
	private double  _cm;  //max constraint memory consumption
	
```

### FieldMayBeFinal
Field `_cm` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTree.java`
#### Snippet
```java
	//global contraints 
	private int     _ck;  //max constraint degree of parallelism
	private double  _cm;  //max constraint memory consumption
	
	//actual tree
```

### FieldMayBeFinal
Field `_pfstats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
	
	private static HashMap<Long,Long>                              _mapPwPf;       //mapping parfor to parworkers
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pfstats;       //parfor statistics
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pwstats;       //parworker statistics
	
```

### FieldMayBeFinal
Field `_mapPwPf` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
{	
	
	private static HashMap<Long,Long>                              _mapPwPf;       //mapping parfor to parworkers
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pfstats;       //parfor statistics
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pwstats;       //parworker statistics
```

### FieldMayBeFinal
Field `_pwstats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
	private static HashMap<Long,Long>                              _mapPwPf;       //mapping parfor to parworkers
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pfstats;       //parfor statistics
	private static HashMap<Long, HashMap<Stat,LinkedList<Double>>> _pwstats;       //parworker statistics
	
	private static boolean _disabled;
```

### FieldMayBeFinal
Field `_rmap` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedReadCache.java`
#### Snippet
```java
	private static final Logger LOG = Logger.getLogger(FederatedReadCache.class);

	private Map<String, ReadCacheEntry> _rmap = new ConcurrentHashMap<>();

	/**
```

### FieldMayBeFinal
Field `compressRunFrequency` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkloadAnalyzer.java`
#### Snippet
```java

	/** Frequency value for how many instructions before we do a pass for compression */
	private static int compressRunFrequency = 10;

	/** Instruction maps to interesting variables */
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java
	private long _id;
	private long _tid;
	private List<Object> _data;
	private boolean _checkPrivacy;
	private List<Long> _checksums;
```

### FieldMayBeFinal
Field `_id` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java

	private RequestType _method;
	private long _id;
	private long _tid;
	private List<Object> _data;
```

### FieldMayBeFinal
Field `_pid` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java
	private boolean _checkPrivacy;
	private List<Long> _checksums;
	private long _pid;
	private String _lineageTrace; // the serialized lineage trace of a put object

```

### FieldMayBeFinal
Field `_method` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedRequest.java`
#### Snippet
```java
	}

	private RequestType _method;
	private long _id;
	private long _tid;
```

### FieldMayBeFinal
Field `networkTimer` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorker.java`
#### Snippet
```java
	private final FederatedWorkloadAnalyzer _fan;
	private final boolean _debug;
	private Timing networkTimer = new Timing();

	public FederatedWorker(int port, boolean debug) {
```

### FieldMayBeFinal
Field `sizePinned` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	 * Current state of pinned variables, required for guarded collect.
	 */
	private static ThreadLocal<Long> sizePinned = new ThreadLocal<Long>() {
		@Override protected Long initialValue() { return 0L; }
	};
```

### FieldMayBeFinal
Field `_data` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedResponse.java`
#### Snippet
```java
	
	private ResponseType _status;
	private Object[] _data;
	private Map<PrivacyLevel,LongAdder> checkedConstraints;

```

### FieldMayBeFinal
Field `sslInstance` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java

	/** A Singleton constructed SSL context, that only is assigned if ssl is enabled. */
	private static SslContextMan sslInstance = null;

	private final Types.DataType _dataType;
```

### FieldMayBeFinal
Field `cacheStats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		}

		private CacheStatsCollection cacheStats = new CacheStatsCollection();
		public double jitCompileTime = 0;
		public UtilizationModel utilization = new UtilizationModel(0.0, 0.0);
```

### FieldMayBeFinal
Field `gcStats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		public double jitCompileTime = 0;
		public UtilizationModel utilization = new UtilizationModel(0.0, 0.0);
		private GCStatsCollection gcStats = new GCStatsCollection();
		private LineageCacheStatsCollection linCacheStats = new LineageCacheStatsCollection();
		private MultiTenantStatsCollection mtStats = new MultiTenantStatsCollection();
```

### FieldMayBeFinal
Field `mtStats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		private GCStatsCollection gcStats = new GCStatsCollection();
		private LineageCacheStatsCollection linCacheStats = new LineageCacheStatsCollection();
		private MultiTenantStatsCollection mtStats = new MultiTenantStatsCollection();
		public HashMap<String, Pair<Long, Double>> heavyHitters = new HashMap<>();
		public List<TrafficModel> coordinatorsTrafficBytes = new ArrayList<>();
```

### FieldMayBeFinal
Field `linCacheStats` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		public UtilizationModel utilization = new UtilizationModel(0.0, 0.0);
		private GCStatsCollection gcStats = new GCStatsCollection();
		private LineageCacheStatsCollection linCacheStats = new LineageCacheStatsCollection();
		private MultiTenantStatsCollection mtStats = new MultiTenantStatsCollection();
		public HashMap<String, Pair<Long, Double>> heavyHitters = new HashMap<>();
```

### FieldMayBeFinal
Field `_fedWorkerAddresses` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
public class FederatedStatistics {
	// stats of the federated worker on the coordinator site
	private static Set<Pair<String, Integer>> _fedWorkerAddresses = new HashSet<>();
	private static final LongAdder readCount = new LongAdder();
	private static final LongAdder putCount = new LongAdder();
```

### FieldMayBeFinal
Field `_scheme` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/LocalDataPartitioner.java`
#### Snippet
```java
public class LocalDataPartitioner {

	private DataPartitionLocalScheme _scheme;

	public LocalDataPartitioner(Statement.PSScheme scheme) {
```

### FieldMayBeFinal
Field `_rdds` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		private final long _limit;
		private long _size;
		private HashMap<Integer, Long> _rdds;

		public MemoryManagerParRDDs(double fractionMem) {
```

### FieldMayBeFinal
Field `_server` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
public final class PSRpcHandler extends RpcHandler {

	private LocalParamServer _server;

	protected PSRpcHandler(LocalParamServer server) {
```

### FieldMayBeFinal
Field `_numBatchesPerNbatch` may be 'final'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
	private final PSRuntimeBalancing _runtimeBalancing;
	private int _numBatchesPerEpoch;
	private int _numBatchesPerNbatch;
	private int _possibleBatchesPerLocalEpoch;
	private final boolean _weighting;
```

### FieldMayBeFinal
Field `descriptor` may be 'final'
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
  }
  private static  com.google.protobuf.Descriptors.FileDescriptor
      descriptor;
  static {
    java.lang.String[] descriptorData = {
```

## RuleId[id=CaughtExceptionImmediatelyRethrown]
### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `src/main/java/org/apache/sysds/api/ScriptExecutorUtils.java`
#### Snippet
```java
			}
			rtprog.execute(ec);
		} catch (Throwable e) {
			throw e;
		} finally { // ensure cleanup/shutdown
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
				LOG.error("Custom config file " + configPath + " not found.");
				throw fnfe;
			} catch (ParseException e) {
				throw e;
			}
```

### CaughtExceptionImmediatelyRethrown
Caught exception `e` is immediately rethrown
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
						 "customize any settings, please supply a `SystemDS-config.xml` file.");
				config = new DMLConfig();
			} catch (ParseException e) {
				throw e;
			}
```

## RuleId[id=UnnecessaryUnicodeEscape]
### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b7` can be replaced with '·'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	public static final String INSTRUCTION_DELIMITOR = "\u2021";
	public static final String OPERAND_DELIMITOR = "\u00b0"; 
	public static final String VALUETYPE_PREFIX = "\u00b7" ; 
	public static final String DATATYPE_PREFIX = VALUETYPE_PREFIX; 
	public static final String LITERAL_PREFIX = VALUETYPE_PREFIX; 
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b0` can be replaced with '°'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	//special delimiters w/ extended ASCII characters to avoid collisions 
	public static final String INSTRUCTION_DELIMITOR = "\u2021";
	public static final String OPERAND_DELIMITOR = "\u00b0"; 
	public static final String VALUETYPE_PREFIX = "\u00b7" ; 
	public static final String DATATYPE_PREFIX = VALUETYPE_PREFIX; 
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u2021` can be replaced with '‡'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	
	//special delimiters w/ extended ASCII characters to avoid collisions 
	public static final String INSTRUCTION_DELIMITOR = "\u2021";
	public static final String OPERAND_DELIMITOR = "\u00b0"; 
	public static final String VALUETYPE_PREFIX = "\u00b7" ; 
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b6` can be replaced with '¶'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	public static final String DATATYPE_PREFIX = VALUETYPE_PREFIX; 
	public static final String LITERAL_PREFIX = VALUETYPE_PREFIX; 
	public static final String VARIABLE_NAME_PLACEHOLDER = "\u00b6"; 
	
	public static final String NAME_VALUE_SEPARATOR = "="; // e.g., used in parameterized builtins
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b7` can be replaced with '·'
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
	//public static final String DELIM_RECODE = "recode";
	public static final String DELIM_NA_STRINGS = "naStrings";
	public static final String DELIM_NA_STRING_SEP = "\u00b7";
	// Parameter names relevant to reading/writing delimited index/libsvmv files
	public static final String LIBSVM_INDEX_DELIM = "indSep";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u00b7` can be replaced with '·'
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameUtil.java`
#### Snippet
```java
	public static final Log LOG = LogFactory.getLog(FrameUtil.class.getName());

	public static final String SCHEMA_SEPARATOR = "\u00b7";

	public static final Pattern booleanPattern = Pattern
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u221E` can be replaced with '∞'
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
	/**
	 * Convenience method to print NaN & Infinity compliant with how as.scalar prints them.
	 * {@link DecimalFormat} prints NaN as \uFFFD and Infinity as \u221E
	 * http://docs.oracle.com/javase/6/docs/api/java/text/DecimalFormat.html
	 * @param df	The {@link DecimalFormat} instance, constructed with the appropriate options
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u23ac` can be replaced with '⎬'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
	public static final String LEVELOUT          = "\u23ac"; //variant of right curly bracket; "\u007d"; //"}";
	public static final String EMPTY             = "null";
	public static final String DASH              = "-";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u007d` can be replaced with '}'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
	public static final String LEVELOUT          = "\u23ac"; //variant of right curly bracket; "\u007d"; //"}";
	public static final String EMPTY             = "null";
	public static final String DASH              = "-";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u236e` can be replaced with '⍮'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	//use escaped unicodes for separators in order to prevent string conflict
	public static final String NEWLINE           = "\n"; //System.lineSeparator();
	public static final String COMPONENTS_DELIM  = "\u236e"; //semicolon w/ bar; ";";
	public static final String ELEMENT_DELIM     = "\u236a"; //comma w/ bar; ",";
	public static final String ELEMENT_DELIM2    = ",";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u236a` can be replaced with '⍪'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String NEWLINE           = "\n"; //System.lineSeparator();
	public static final String COMPONENTS_DELIM  = "\u236e"; //semicolon w/ bar; ";";
	public static final String ELEMENT_DELIM     = "\u236a"; //comma w/ bar; ",";
	public static final String ELEMENT_DELIM2    = ",";
	public static final String DATA_FIELD_DELIM  = "\u007c"; //"|";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u003d` can be replaced with '='
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String ELEMENT_DELIM2    = ",";
	public static final String DATA_FIELD_DELIM  = "\u007c"; //"|";
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
	public static final String LEVELOUT          = "\u23ac"; //variant of right curly bracket; "\u007d"; //"}";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u23a8` can be replaced with '⎨'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String DATA_FIELD_DELIM  = "\u007c"; //"|";
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
	public static final String LEVELOUT          = "\u23ac"; //variant of right curly bracket; "\u007d"; //"}";
	public static final String EMPTY             = "null";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u007b` can be replaced with '{'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String DATA_FIELD_DELIM  = "\u007c"; //"|";
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
	public static final String LEVELOUT          = "\u23ac"; //variant of right curly bracket; "\u007d"; //"}";
	public static final String EMPTY             = "null";
```

### UnnecessaryUnicodeEscape
Unicode escape sequence `\u007c` can be replaced with '\|'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	public static final String ELEMENT_DELIM     = "\u236a"; //comma w/ bar; ",";
	public static final String ELEMENT_DELIM2    = ",";
	public static final String DATA_FIELD_DELIM  = "\u007c"; //"|";
	public static final String KEY_VALUE_DELIM   = "\u003d"; //"=";
	public static final String LEVELIN           = "\u23a8"; //variant of left curly bracket; "\u007b"; //"{";
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hops`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		//need for synchronization as we do temp changes in shared hops/lops
		//however, we create deep copies for most dags to allow for concurrent recompile
		synchronized( hops ) {
			//always in place, no stats update/rewrites
			newInst = recompile(sb, hops, (LocalVariableMap)null, null, true, false, false, false, false, null, 0);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `pbs`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
	 */
	public static void recompileProgramBlockHierarchy2Forced( ArrayList<ProgramBlock> pbs, long tid, HashSet<String> fnStack, ExecType et ) {
		synchronized( pbs ) {
			for( ProgramBlock pb : pbs )
				rRecompileProgramBlock2Forced(pb, tid, fnStack, et);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hop`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		if( hop == null )
			return false;
		synchronized( hop ) {
			hop.resetVisitStatus();
			return rRequiresRecompile(hop);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hop`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java

		//need for synchronization as we do temp changes in shared hops/lops
		synchronized( hop ) {
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
				vars, status, inplace, replaceLit, true, false, true, null, tid);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hops`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		//need for synchronization as we do temp changes in shared hops/lops
		//however, we create deep copies for most dags to allow for concurrent recompile
		synchronized( hops ) {
			newInst = recompile(sb, hops, ec, status, inplace, replaceLit, true, false, false, null, tid);
		}
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hops`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		if( hops == null )
			return false;
		synchronized( hops ) {
			Hop.resetVisitStatus(hops);
			return hops.stream()
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `pbs`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		// (preserve variables for potential second pass, otherwise corrupted stats)
		RecompileStatus status1 = new RecompileStatus(tid, true, resetRecompile, false);
		synchronized( pbs ) {
			for( ProgramBlock pb : pbs )
				rRecompileProgramBlock(pb, vars, status1);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hop`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java

		//need for synchronization as we do temp changes in shared hops/lops
		synchronized( hop ) {
			//always in place, no stats update/rewrites
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hop`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java

		//need for synchronization as we do temp changes in shared hops/lops
		synchronized( hop ) {
			//always in place, no stats update/rewrites, but forced exec type
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `hops`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		//need for synchronization as we do temp changes in shared hops/lops
		//however, we create deep copies for most dags to allow for concurrent recompile
		synchronized( hops ) {
			//always in place, no stats update/rewrites, but forced exec type
			newInst = recompile(sb, hops, (LocalVariableMap)null, null, true, false, false, true, false, et, tid);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `hops`
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
				//deep copy hops dag for concurrent recompile
				ArrayList<Hop> hops = sb.getHops();
				synchronized(hops) { // guard concurrent recompile
					hops = Recompiler.deepCopyHopsDag( hops );
				}
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `solveCache`
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java

		if(solveCache.containsKey(nj))
			synchronized(solveCache) {
				return solveCache.get(nj);
			}
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `solveCache`
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/HassAndStokes.java`
#### Snippet
```java

		if(solveCache.size() < MAX_SOLVE_CACHE_SIZE)
			synchronized(solveCache) {
				solveCache.put(nj, est);
			}
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `mb`
in `src/main/java/org/apache/sysds/runtime/controlprogram/ProgramBlock.java`
#### Snippet
```java
					boolean sparse1 = mb.isInSparseFormat();
					long nnz1 = mb.getNonZeros();
					synchronized(mb) { // potential state change
						mb.recomputeNonZeros();
						mb.examSparsity();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `ecm`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLocalData.java`
#### Snippet
```java
		long pid = Long.valueOf(IDHandler.obtainProcessID());
		ExecutionContextMap ecm = _flt.getECM(FederatedLookupTable.NOHOST, pid);
		synchronized(ecm) {
			ecm.get(-1).setVariable(Long.toString(id), _data);
		}
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `mo`
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		PartitionedBroadcast<MatrixBlock> bret = null;

		synchronized (mo) {  //synchronize with the async. broadcast thread
			long t0 = DMLScript.STATISTICS ? System.nanoTime() : 0;
			//reuse existing broadcast handle
```

## RuleId[id=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `explainType` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/ScriptExecutor.java`
#### Snippet
```java
			DMLScript.EXPLAIN = ExplainType.NONE;
		} else {
			ExplainType explainType = explainLevel.getExplainType();
			DMLScript.EXPLAIN = explainType;
		}
```

### UnnecessaryLocalVariable
Local variable `results` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
			}

			MLResults results = scriptExecutor.execute(script);

			return results;
```

### UnnecessaryLocalVariable
Local variable `projectInfo` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
	public ProjectInfo info() {
		try {
			ProjectInfo projectInfo = ProjectInfo.getProjectInfo();
			return projectInfo;
		} catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `fileName` is redundant
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
		if( isFile )
		{
			String fileName = scriptOrFilename;
			//read DML script from file
			if(fileName == null)
```

### UnnecessaryLocalVariable
Local variable `scriptString` is redundant
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
		else
		{
			String scriptString = scriptOrFilename;
			//parse given script string 
			if(scriptString == null)
```

### UnnecessaryLocalVariable
Local variable `sc` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	public static SparkContext getSparkContextFromProxy() {
		MLContext activeMLContext = MLContextProxy.getActiveMLContextForAPI();
		SparkContext sc = getSparkContext(activeMLContext);
		return sc;
	}
```

### UnnecessaryLocalVariable
Local variable `name` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	 */
	public static Data convertInputType(String parameterName, Object parameterValue, Metadata metadata) {
		String name = parameterName;
		Object value = parameterValue;
		boolean hasMetadata = (metadata != null) ? true : false;
```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	public static Data convertInputType(String parameterName, Object parameterValue, Metadata metadata) {
		String name = parameterName;
		Object value = parameterValue;
		boolean hasMetadata = (metadata != null) ? true : false;
		boolean hasMatrixMetadata = hasMetadata && (metadata instanceof MatrixMetadata) ? true : false;
```

### UnnecessaryLocalVariable
Local variable `jsc` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	public static JavaSparkContext getJavaSparkContextFromProxy() {
		MLContext activeMLContext = MLContextProxy.getActiveMLContextForAPI();
		JavaSparkContext jsc = getJavaSparkContext(activeMLContext);
		return jsc;
	}
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		}

		Object o = value;
		boolean supported = false;
		for (Class<?> clazz : ALL_SUPPORTED_DATA_TYPES) {
```

### UnnecessaryLocalVariable
Local variable `o` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		}

		Object o = parameterValue;
		boolean supported = false;
		for (Class<?> clazz : BASIC_DATA_TYPES) {
```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			}
			Node node = nodes.item(0);
			String value = node.getTextContent();
			return value;
		} catch (Exception e) {
```

### UnnecessaryLocalVariable
Local variable `interOutput` is redundant
in `src/main/java/org/apache/sysds/hops/FunctionOp.java`
#### Snippet
```java
			}
			else if ( getFunctionName().equalsIgnoreCase("svd")) {
				double interOutput = OptimizerUtils.estimateSizeExactSparsity(1, getInput().get(0).getDim2(), 1.0);
				return interOutput;
			}
```

### UnnecessaryLocalVariable
Local variable `matrixBlock` is redundant
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
			MatrixMetadata matrixMetadata) {
		try {
			MatrixBlock matrixBlock = SparkExecutionContext.toMatrixBlock(binaryBlocks,
					matrixMetadata.getNumRows().intValue(), matrixMetadata.getNumColumns().intValue(),
					matrixMetadata.getBlocksize(), matrixMetadata.getNumNonZeros());
```

### UnnecessaryLocalVariable
Local variable `input` is redundant
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				throw new HopsException("Parameter 'margin' must be a literal argument.");
			
			Hop input = targetHop;
			long rlen = input.getDim1();
			long clen = input.getDim2();
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/hops/AggUnaryOp.java`
#### Snippet
```java
	public boolean isTransposeSafe()
	{
		boolean ret = (_direction == Direction.RowCol) && //full aggregate
		              (_op == AggOp.SUM || _op == AggOp.SUM_SQ || //valid aggregration functions
		               _op == AggOp.MIN || _op == AggOp.MAX ||
```

### UnnecessaryLocalVariable
Local variable `interOutput` is redundant
in `src/main/java/org/apache/sysds/hops/BinaryOp.java`
#### Snippet
```java
				// x=solve(A,b) relies on QR decomposition of A, which is done using Apache commons-math
				// matrix of size same as the first input
				double interOutput = OptimizerUtils
						.estimateSizeExactSparsity(getInput().get(0).getDim1(), getInput().get(0).getDim2(), 1.0);
				return interOutput;
```

### UnnecessaryLocalVariable
Local variable `memTensor` is redundant
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
		// TODO estimate exact size
		long doubleSize = UtilFunctions.prod(dims) * 8;
		double memTensor = doubleSize;
		double memPTensor = doubleSize;
		//check if both output matrix and partitioned matrix fit into local mem budget
```

### UnnecessaryLocalVariable
Local variable `memPTensor` is redundant
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
		long doubleSize = UtilFunctions.prod(dims) * 8;
		double memTensor = doubleSize;
		double memPTensor = doubleSize;
		//check if both output matrix and partitioned matrix fit into local mem budget
		return (memPinned + memTensor + memPTensor < getLocalMemBudget())
```

### UnnecessaryLocalVariable
Local variable `costs` is redundant
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
		
		//total costs
		double costs = ltime + etime + wtime;
		
		//if( LOG.isDebugEnabled() )
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		
		//result transpose (dimensions set outside)
		Lop out = new Transform(mult, ReOrgOp.TRANS, getDataType(), getValueType(), ExecType.CP);
		
		return out;
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
		ExecType outTransposeExecType = ( _federatedOutput == FederatedOutput.FOUT ) ?
			ExecType.FED : ExecType.CP;
		Lop out = new Transform(mult, ReOrgOp.TRANS, getDataType(), getValueType(), outTransposeExecType, k);

		return out;
```

### UnnecessaryLocalVariable
Local variable `cpmm_costs` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
			numReducersCPMM); //available reducers
		// CPMM total costs
		double cpmm_costs =  (cpmm_shuffle1+cpmm_io1)/cpmm_nred1  //cpmm phase1
			+(cpmm_shuffle2+cpmm_io2)/cpmm_nred2; //cpmm phase2
		
```

### UnnecessaryLocalVariable
Local variable `rmm_costs` is redundant
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
				                    numReducersRMM); //available reducers
		// RMM total costs
		double rmm_costs = (rmm_shuffle + rmm_io) / rmm_nred;
		
		// return total costs
```

### UnnecessaryLocalVariable
Local variable `cd` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
			BitsetMatrix2 bsb = (BitsetMatrix2) bsb2;
			BitsetMatrix2 bsc = (BitsetMatrix2) bsc2;
			final int cd = _clen;
			final int n = bsb._clen;
			// matrix multiply with IKJ schedule and pure OR ops in inner loop
```

### UnnecessaryLocalVariable
Local variable `n` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
			BitsetMatrix2 bsc = (BitsetMatrix2) bsc2;
			final int cd = _clen;
			final int n = bsb._clen;
			// matrix multiply with IKJ schedule and pure OR ops in inner loop
			long lnnz = 0;
```

### UnnecessaryLocalVariable
Local variable `b` is redundant
in `src/main/java/org/apache/sysds/hops/estim/EstimatorBitsetMM.java`
#### Snippet
```java
			BitsetMatrix1 bsb = (BitsetMatrix1) bsb2;
			BitsetMatrix1 bsc = (BitsetMatrix1) bsc2;
			final long[] b = bsb._data;
			final long[] c = bsc._data;
			final int cd = _clen;
```

### UnnecessaryLocalVariable
Local variable `type` is redundant
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
		DMLConfig conf = ConfigurationManager.getDMLConfig();
		String optimizer = conf.getTextValue(DMLConfig.CODEGEN_OPTIMIZER);
		PlanSelector type = PlanSelector.valueOf(optimizer.toUpperCase());
		PLAN_SEL_POLICY = type;
	}
```

### UnnecessaryLocalVariable
Local variable `dcOld` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			if( dat1!=null  && dat2!=null  )
			{
				DataCharacteristics dcOld = dat1;
				DataCharacteristics dc = dat2;
				
```

### UnnecessaryLocalVariable
Local variable `dc` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			{
				DataCharacteristics dcOld = dat1;
				DataCharacteristics dc = dat2;
				
				if( dcOld.getRows() != dc.getRows()
```

### UnnecessaryLocalVariable
Local variable `dcOld` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			if( dat1 != null && dat2!=null )
			{
				DataCharacteristics dcOld = dat1;
				DataCharacteristics dc = dat2;
					
```

### UnnecessaryLocalVariable
Local variable `dc` is redundant
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			{
				DataCharacteristics dcOld = dat1;
				DataCharacteristics dc = dat2;
					
				if( dcOld.getRows() != dc.getRows()
```

### UnnecessaryLocalVariable
Local variable `X` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				&& HopRewriteUtils.isSingleBlock(right.getInput().get(0).getInput().get(0),true)) //BLOCKSIZE CONSTRAINT
			{
				Hop X = left; 
				Hop U = right.getInput().get(0).getInput().get(0);
				Hop V = right.getInput().get(0).getInput().get(1);
```

### UnnecessaryLocalVariable
Local variable `X` is redundant
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
				&& HopRewriteUtils.isSingleBlock(right.getInput().get(0).getInput().get(0).getInput().get(0),true))
			{
				Hop X = left; 
				Hop U = right.getInput().get(0).getInput().get(0).getInput().get(0);
				Hop V = right.getInput().get(0).getInput().get(0).getInput().get(1);
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `src/main/java/org/apache/sysds/utils/JSONHelper.java`
#### Snippet
```java
		try { 
			if(reader != null) {
				JSONObject result = new JSONObject(reader); 
				return result;
			} else {
```

### UnnecessaryLocalVariable
Local variable `nodes` is redundant
in `src/main/java/org/apache/sysds/lops/compile/linearization/ILinearize.java`
#### Snippet
```java
		// + append root nodes in order of their original definition
		// (which also preserves the original order of prints)
		List<Lop> nodes = Stream
			.concat(v.stream().filter(l -> !l.getOutputs().isEmpty()).sorted(Comparator.comparing(l -> l.getID())),
				v.stream().filter(l -> l.getOutputs().isEmpty()))
```

### UnnecessaryLocalVariable
Local variable `nodes` is redundant
in `src/main/java/org/apache/sysds/lops/compile/linearization/ILinearize.java`
#### Snippet
```java

	private static List<Lop> doBreadthFirstSort(List<Lop> v) {
		List<Lop> nodes = v.stream().sorted(Comparator.comparing(Lop::getLevel)).collect(Collectors.toList());

		return nodes;
```

### UnnecessaryLocalVariable
Local variable `strScript` is redundant
in `src/main/java/org/apache/sysds/utils/ParameterBuilder.java`
#### Snippet
```java
	public static void setVariablesInScript(String strScriptPathName, HashMap<String, String> variables) {
		try {
			String strScript = strScriptPathName;
			String strTmpScript = strScript + "t";

```

### UnnecessaryLocalVariable
Local variable `tmp2` is redundant
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
		
		//step 2: create packed rmvar instructions: rmvar m1, rmvar m2 -> rmvar m1 m2
		ArrayList<Instruction> tmp2 = createPackedRmvarInstructions(tmp1);
		
		return tmp2;
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `src/main/java/org/apache/sysds/parser/AssignmentStatement.java`
#### Snippet
```java
		Expression newSource = _source.rewriteExpression(prefix);
		// create rewritten assignment statement (deep copy)
		AssignmentStatement retVal = new AssignmentStatement(newTarget, newSource, this);
		return retVal;
	}
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
			newVarParams.put(key, newExpr);
		}
		ParameterizedBuiltinFunctionExpression retVal = 
			new ParameterizedBuiltinFunctionExpression(_opcode, newVarParams, this);
		return retVal;
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
			varParams.put(pexpr.getName(), pexpr.getExpr());
		
		ParameterizedBuiltinFunctionExpression retVal = 
			new ParameterizedBuiltinFunctionExpression(ctx, pbifop,varParams, fileName);
		return retVal;
```

### UnnecessaryLocalVariable
Local variable `dat2` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
							}
							
							DataIdentifier dat2 = write;
							if( c._dat == dat2 ) continue; //skip self-check
							if( runEqualsCheck(c._dat, dat2) ) {
```

### UnnecessaryLocalVariable
Local variable `dat2` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					{
						if( !c._var.equals( read.getName() ) ) continue;
						DataIdentifier dat2 = read;
						DataType dat2dt = _vsParent.getVariables().get(read.getName()).getDataType();
						
```

### UnnecessaryLocalVariable
Local variable `idat` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		// note: using dat.hashCode can be different for same functions, 
		// hence, we use a custom String ID
		IndexedIdentifier idat = dat;
		Expression ex1a = idat.getRowLowerBound();
		Expression ex1b = idat.getRowUpperBound();
```

### UnnecessaryLocalVariable
Local variable `sub1a` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			else //range indexing
			{
				Expression sub1a = sub1;
				Expression sub1b = idat.getRowUpperBound();
				
```

### UnnecessaryLocalVariable
Local variable `sub2a` is redundant
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				else //range indexing
				{
					Expression sub2a = sub2;
					Expression sub2b = idat.getColUpperBound();
					
```

### UnnecessaryLocalVariable
Local variable `retVal` is redundant
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			newArgs[i] = _args[i].rewriteExpression(prefix);
		}
		BuiltinFunctionExpression retVal = new BuiltinFunctionExpression(this._opcode, newArgs, this);
		return retVal;
	}
```

### UnnecessaryLocalVariable
Local variable `result` is redundant
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
	@Override
	public VariableSet variablesUpdated() {
		VariableSet result = new VariableSet();
		// result.addVariables(_first.variablesUpdated());
		return result;
```

### UnnecessaryLocalVariable
Local variable `text` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		// Otherwise it is a string literal (optionally enclosed within single or double quotes)
		String val = "";
		String text = varValue;
		if(	(text.startsWith("\"") && text.endsWith("\"")) || (text.startsWith("\'") && text.endsWith("\'"))) {
			if(text.length() > 2) {
```

### UnnecessaryLocalVariable
Local variable `filePath` is redundant
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	public void exitPathStatement(PathStatementContext ctx) {
		PathStatement stmt = new PathStatement(ctx.pathValue.getText());
		String filePath = UtilFunctions.unquote(ctx.pathValue.getText());
		_workingDir = filePath;
		ctx.info.stmt = stmt;
```

### UnnecessaryLocalVariable
Local variable `fcall` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
				
				// Create the hop for current function call
				FunctionOp fcall = new FunctionOp(ftype, nameSpace, source.getOpCode().toString(), null, inputs, outputNames, outputs);
				currBuiltinOp = fcall;
				
```

### UnnecessaryLocalVariable
Local variable `currBuiltinOp` is redundant
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					listHops[idx++] = processExpression(ex, null, hops);
				}
				Hop currBuiltinOp = HopRewriteUtils.createNary(OpOpN.LIST,listHops );
				return currBuiltinOp;
			}
```

### UnnecessaryLocalVariable
Local variable `contiguousDataset` is redundant
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5.java`
#### Snippet
```java

			final H5ObjectHeader header = new H5ObjectHeader(rootObject, symbolTableEntry.getObjectHeaderAddress());
			final H5ContiguousDataset contiguousDataset = new H5ContiguousDataset(rootObject, header);
			return contiguousDataset;

```

### UnnecessaryLocalVariable
Local variable `message` is redundant
in `src/main/java/org/apache/sysds/runtime/io/hdf5/message/H5Message.java`
#### Snippet
```java
		// Create a new buffer holding this header data
		final ByteBuffer headerData = Utils.createSubBuffer(bb, dataSize);
		final H5Message message = readMessage(rootObject, headerData, messageType, flags);
		return message;
	}
```

### UnnecessaryLocalVariable
Local variable `nnz` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
		final int[] colIdx = new int[nRowCol];
		final double[] vals = new double[nRowCol];
		int nnz = nRowCol;
		
		for(int i = 0; i < nRowCol; i++){
```

### UnnecessaryLocalVariable
Local variable `nnz` is redundant
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
		final int[] colIdx = new int[nRowCol];
		final double[] vals = new double[nRowCol];
		int nnz = nRowCol;
		
		for(int i = 0; i < nRowCol; i++){
```

### UnnecessaryLocalVariable
Local variable `tb` is redundant
in `src/main/java/org/apache/sysds/runtime/data/TensorBlock.java`
#### Snippet
```java
		if( !(block instanceof TensorBlock) )
			throw new RuntimeException("TensorBlock.slice(int,int,int,int,CacheBlock) CacheBlock was no TensorBlock");
		TensorBlock tb = (TensorBlock) block;
		int[] dims = new int[_dims.length];
		dims[0] = ru - rl + 1;
```

### UnnecessaryLocalVariable
Local variable `new_block` is redundant
in `src/main/java/org/apache/sysds/runtime/util/EMAUtils.java`
#### Snippet
```java

		Double[][] values = new Double[][]{};
		FrameBlock new_block = generateBlock(rows, cols, data_list.toArray(values));
		return new_block;
	}
```

### UnnecessaryLocalVariable
Local variable `disguisedVal` is redundant
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
	public static FrameBlock syntacticalPatternDiscovery(FrameBlock frame, double threshold, String disguised_value) {
		// Preparation
		String disguisedVal = disguised_value;
		int numCols = frame.getNumColumns();
		int numRows = frame.getNumRows();
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/util/BinaryBlockRecordReader.java`
#### Snippet
```java
	{
		//long t0 = System.nanoTime();		
		boolean ret = super.next(key, value);		
		//long t1 = System.nanoTime();
		
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameLibRemoveEmpty.java`
#### Snippet
```java

	private FrameBlock apply() {
		FrameBlock ret = rows ? removeEmptyRows() : removeEmptyColumns();
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `br` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
		Path path = new Path(filename);
		FileSystem fs = IOUtilFunctions.getFileSystem(path);
		BufferedWriter br=new BufferedWriter(new OutputStreamWriter(fs.create(path,true)));
		return br;
	}
```

### UnnecessaryLocalVariable
Local variable `br` is redundant
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
        Path path = new Path(filename);
        FileSystem fs = IOUtilFunctions.getFileSystem(path);
		BufferedReader br=new BufferedReader(new InputStreamReader(fs.open(path)));		
        return br;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameLibAppend.java`
#### Snippet
```java
	 */
	public static FrameBlock append(FrameBlock a, FrameBlock b, boolean cbind) {
		FrameBlock ret = cbind ? appendCbind(a, b): appendRbind(a, b);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `string_array` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static String[] getSplittedStringAsArray (String input) {
		//Frame f = new Frame();
		String[] string_array = input.split("'[ ]*,[ ]*'");
		return string_array;//.subList(0,2);
	}
```

### UnnecessaryLocalVariable
Local variable `date_formats` is redundant
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static String[] getDominantDateFormat (String[] values) {
		String[] output = new String[values.length];
		Map<String, String> date_formats = DATE_FORMATS;

		Map<String, Integer> format_matches = new HashMap<String, Integer>();
```

### UnnecessaryLocalVariable
Local variable `name` is redundant
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		//prepare data for serialization
		String name = key;
		DataType datatype = dat.getDataType();
		ValueType valuetype = dat.getValueType();
```

### UnnecessaryLocalVariable
Local variable `tmp` is redundant
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
						bestIdx = probColList.get(0);
					}
					String tmp = dataValue;
					this.set(j, i, this.get(j, bestIdx));
					this.set(j, bestIdx, tmp);
```

### UnnecessaryLocalVariable
Local variable `cellIndexTopRow` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
		
		// Get Block indexes (rows and columns boundaries)
		long cellIndexTopRow = index;
		long cellIndexBottomRow = index+block.getNumRows()-1;
		long cellIndexLeftCol = 1;
```

### UnnecessaryLocalVariable
Local variable `outlist` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
	public static ArrayList performSlice(IndexRange ixrange, int blen, int iix, int jix, FrameBlock in) {
		Pair<Long, FrameBlock> lfp = new Pair<>(Long.valueOf(((iix-1)*blen)+1), in);
		ArrayList<Pair<Long, FrameBlock>> outlist = performSlice(lfp, ixrange, blen);
	
		return outlist;
```

### UnnecessaryLocalVariable
Local variable `lhs_cl` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
			long lhs_rl = Math.max((leftRowIndex-1)*blenLeft+1, start_lhs_globalRowIndex);
			long lhs_ru = Math.min(leftRowIndex*blenLeft, end_lhs_globalRowIndex);
			long lhs_cl = start_lhs_globalColIndex;
			long lhs_cu = end_lhs_globalColIndex;
			
```

### UnnecessaryLocalVariable
Local variable `lhs_cu` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
			long lhs_ru = Math.min(leftRowIndex*blenLeft, end_lhs_globalRowIndex);
			long lhs_cl = start_lhs_globalColIndex;
			long lhs_cu = end_lhs_globalColIndex;
			
			int lhs_lrl = UtilFunctions.computeCellInBlock(lhs_rl, blenLeft);
```

### UnnecessaryLocalVariable
Local variable `lblen` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
			FrameBlock slicedRHSBlk = fb.slice(rhs_lrl, rhs_lru, rhs_lcl, rhs_lcu, new FrameBlock());
			
			int lblen = blenLeft;
			
			ValueType[] schemaPartialLeft = UtilFunctions.nCopies(lhs_lcl, ValueType.STRING);
```

### UnnecessaryLocalVariable
Local variable `dstTensorDesc` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
				LOG.trace("GPU : performCuDNNReLU" + ", GPUContext=" + gCtx);
			}
			cudnnTensorDescriptor dstTensorDesc = srcTensorDesc;

			Pointer srcData = getDensePointerForCuDNN(gCtx, in, instName);
```

### UnnecessaryLocalVariable
Local variable `blkOut` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
		}
		else if(op.getDirection().isRow()) {
			MatrixBlock blkOut = blkIn;
			MatrixBlock blkOutCorr = new MatrixBlock(blkIn.getNumRows(), 3, false);
			// (nHashes, k, D) row matrix
```

### UnnecessaryLocalVariable
Local variable `blkOut` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/KMVSketch.java`
#### Snippet
```java
		}
		else if(op.getDirection().isCol()) {
			MatrixBlock blkOut = blkIn;
			// (nHashes, k, D) row matrix
			MatrixBlock blkOutCorr = new MatrixBlock(blkIn.getNumColumns(), 3, false);
```

### UnnecessaryLocalVariable
Local variable `skipEmpty` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
	private static void safeBinaryMVGeneric(MatrixBlock m1, MatrixBlock m2, MatrixBlock ret, BinaryOperator op) {
		boolean isMultiply = (op.fn instanceof Multiply);
		boolean skipEmpty = (isMultiply);
		int rlen = m1.rlen;
		int clen = m1.clen;
```

### UnnecessaryLocalVariable
Local variable `skipEmpty` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
	private static long safeBinaryMVDense(MatrixBlock m1, MatrixBlock m2, MatrixBlock ret, BinaryOperator op, int rl, int ru) {
		boolean isMultiply = (op.fn instanceof Multiply);
		boolean skipEmpty = (isMultiply);
		BinaryAccessType atype = getBinaryAccessType(m1, m2);
		int clen = m1.clen;
```

### UnnecessaryLocalVariable
Local variable `isMultiply` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java

	private static void safeBinaryMVSparseDenseRow(MatrixBlock m1, MatrixBlock m2, MatrixBlock ret, BinaryOperator op) {
		boolean isMultiply = (op.fn instanceof Multiply);
		boolean skipEmpty = (isMultiply);
		int rlen = m1.rlen;
```

### UnnecessaryLocalVariable
Local variable `skipEmpty` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
	private static void safeBinaryMVSparseDenseRow(MatrixBlock m1, MatrixBlock m2, MatrixBlock ret, BinaryOperator op) {
		boolean isMultiply = (op.fn instanceof Multiply);
		boolean skipEmpty = (isMultiply);
		int rlen = m1.rlen;
		int clen = m1.clen;
```

### UnnecessaryLocalVariable
Local variable `ldc` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		int lda = toInt(isLeftTransposed ? m : k);
		int ldc = m;

		if(!left.getGPUObject(gCtx).isAllocated())
```

### UnnecessaryLocalVariable
Local variable `blocks` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		final int MAX_THREADS = getMaxThreads(gCtx);
		int threads = (cols < MAX_THREADS *2) ? nextPow2((cols + 1)/ 2) : MAX_THREADS;
		int blocks = rows;
		int sharedMemSize = threads * sizeOfDataType;
		if (threads <= WARP_SIZE){
```

### UnnecessaryLocalVariable
Local variable `isEmpty1` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
	private static boolean isSparseAndEmpty(GPUContext gCtx, MatrixObject in1) {
		boolean isSparse1 = isInSparseFormat(gCtx, in1);
		boolean isEmpty1 = isSparse1 && in1.getGPUObject(gCtx).getJcudaSparseMatrixPtr().nnz == 0;
		return isEmpty1;
	}
```

### UnnecessaryLocalVariable
Local variable `ldb` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		// Dense-Dense dgeam
		int lda = toInt(numColsA);
		int ldb = lda;
		int m = toInt(numRowsA);
		int n = lda;
```

### UnnecessaryLocalVariable
Local variable `n` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		int ldb = lda;
		int m = toInt(numRowsA);
		int n = lda;
		int ldc = m;
		cudaSupportFunctions.cublasgeam(getCublasHandle(gCtx), CUBLAS_OP_T, CUBLAS_OP_T, m, n, one(), A, lda, zero(), A, ldb, C, ldc);
```

### UnnecessaryLocalVariable
Local variable `ldc` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		int m = toInt(numRowsA);
		int n = lda;
		int ldc = m;
		cudaSupportFunctions.cublasgeam(getCublasHandle(gCtx), CUBLAS_OP_T, CUBLAS_OP_T, m, n, one(), A, lda, zero(), A, ldb, C, ldc);
	}
```

### UnnecessaryLocalVariable
Local variable `src` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		//     improved the performance of zeroout.
		
		MatrixBlock src = rhsMatrix;

		if(rl==ru && cl==cu) { //specific case: cell update
```

### UnnecessaryLocalVariable
Local variable `v2` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		MatrixBlock that2 = checkType(that2Val);
		CTable ctable = CTable.getCTableFnObject();
		double v2 = scalarThat;
		
		//sparse-unsafe ctable execution
```

### UnnecessaryLocalVariable
Local variable `w` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java

		CTable ctable = CTable.getCTableFnObject();
		double w = scalarThat2;
		
		if( ignoreZeros //SPARSE-SAFE & SPARSE INPUTS
```

### UnnecessaryLocalVariable
Local variable `w` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
	{	
		CTable ctable = CTable.getCTableFnObject();
		double w = scalarThat;
		int offset = (int) ((ix1.getRowIndex()-1)*blen); 
		
```

### UnnecessaryLocalVariable
Local variable `v2` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
	{
		CTable ctable = CTable.getCTableFnObject();
		double v2 = scalarThat;
		double w = scalarThat2;
		
```

### UnnecessaryLocalVariable
Local variable `w` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		CTable ctable = CTable.getCTableFnObject();
		double v2 = scalarThat;
		double w = scalarThat2;
		
		//sparse-unsafe ctable execution
```

### UnnecessaryLocalVariable
Local variable `w` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		MatrixBlock that = checkType(thatMatrix);
		CTable ctable = CTable.getCTableFnObject();
		double w = thatScalar;
		
		//prepare allocation of CSR sparse block
```

### UnnecessaryLocalVariable
Local variable `rlen` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		if( rmRows ) //margin = "rows"
		{
			long rlen = len; //max dimensionality
			long clen = linData.getNumColumns();
			
```

### UnnecessaryLocalVariable
Local variable `clen` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		{
			long rlen = linData.getNumRows();
			long clen = len;
			
			for( int i=0; i<linOffset.getNumColumns(); i++ ) {
```

### UnnecessaryLocalVariable
Local variable `rlen` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static MatrixBlock rexpandRows(MatrixBlock in, MatrixBlock ret, int max, boolean cast, boolean ignore) {
		//set meta data
		final int rlen = max;
		final int clen = in.rlen;
		final long nnz = in.nonZeros;
```

### UnnecessaryLocalVariable
Local variable `clen` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//set meta data
		final int rlen = in.rlen;
		final int clen = max;
		final long nnz = in.nonZeros;
		boolean sp = MatrixBlock.evalSparseFormatInMemory(rlen, clen, nnz);
```

### UnnecessaryLocalVariable
Local variable `disksize` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
		long nnz = mb.getNonZeros();
		double s = OptimizerUtils.getSparsity(r, c, nnz);
		double disksize = ((double)MatrixBlock.estimateSizeOnDisk(r, c, (long)(s*r*c))) / (1024*1024);
		return disksize;
	}
```

### UnnecessaryLocalVariable
Local variable `limit` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
	protected static void setCacheLimit(double fraction) {
		long maxMem = InfrastructureAnalyzer.getLocalMaxMemory();
		long limit = (long)(fraction * maxMem);
		CACHE_LIMIT = limit;
	}
```

### UnnecessaryLocalVariable
Local variable `ludf` is redundant
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
		if (!(udf instanceof LineageTraceable))
			throw new DMLRuntimeException("Unknown Federated UDF (" + udf.getClass().getSimpleName() + ") traced.");
		LineageTraceable ludf = (LineageTraceable) udf;
		if (ludf.hasSingleLineage()) {
			Pair<String, LineageItem> item = udf.getLineageItem(ec);
```

### UnnecessaryLocalVariable
Local variable `count` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static void s_uavar(SparseBlock a, DenseBlock c, int n, CM_COV_Object cbuff, CM cm, int rl, int ru) {
		// compute and store count of empty cells before aggregation
		int count = (ru-rl)*n - (int)a.size(rl, ru);
		cbuff.w = count;
		// calculate aggregated variance (only using non-empty cells)
```

### UnnecessaryLocalVariable
Local variable `count` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
			cbuff.reset(); // reset buffer for each row
			// compute and store count of empty cells in this row before aggregation
			int count = (a.isEmpty(i)) ? n : n-a.size(i);
			cbuff.w = count;
			if (!a.isEmpty(i))
```

### UnnecessaryLocalVariable
Local variable `cix1` is redundant
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
			//unrolled 4-block (for fewer L1-dcache loads)
			for( int i=bn, cix=ci+bn*len2; i < len1; i+=4, cix+=4*len2 ) {
				final int cix1=cix, cix2=cix+len2, cix3=cix+2*len2, cix4=cix+3*len2;
				final double aval1=a[ai+i], aval2=a[ai+i+1], aval3=a[ai+i+2], aval4=a[ai+i+3];
				for( int j=0; j<len2; j++ ) {
```

### UnnecessaryLocalVariable
Local variable `mx` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
					int[]  tbi  = new int[ blocksizeK ];
					
					final int mx = ru;
					final int cdx = m;
					final int nx = n;
```

### UnnecessaryLocalVariable
Local variable `cdx` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
					
					final int mx = ru;
					final int cdx = m;
					final int nx = n;
					
```

### UnnecessaryLocalVariable
Local variable `nx` is redundant
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
					final int mx = ru;
					final int cdx = m;
					final int nx = n;
					
					//blocked execution
```

### UnnecessaryLocalVariable
Local variable `allocationCost` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/cost/ComputationCostEstimator.java`
#### Snippet
```java
	private double rightMultCost(double nVals, double nCols, double sparsity) {
		final double preMultiplicationCost = sparsity * nCols * nVals;
		final double allocationCost = nVals;
		return rightMultCost(preMultiplicationCost, allocationCost);
	}
```

### UnnecessaryLocalVariable
Local variable `rl` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
		final SparseBlock sb = ret.getSparseBlock();
		for(int i = 0; i < rlen; i += blklen) {
			final int rl = i;
			final int ru = Math.min(i + blklen, rlen);
			for(AColGroup grp : filteredGroups)
```

### UnnecessaryLocalVariable
Local variable `rl` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
		int blklen, double[] constV, double eps, long nonZeros, boolean overlapping) {
		for(int i = 0; i < rlen; i += blklen) {
			final int rl = i;
			final int ru = Math.min(i + blklen, rlen);
			for(AColGroup grp : filteredGroups)
```

### UnnecessaryLocalVariable
Local variable `eps` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
					min = v;
			}
			final double eps = (max + 1e-4 - min) * 1e-10;
			return eps;
		}
```

### UnnecessaryLocalVariable
Local variable `d` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SmoothedJackknifeEstimator.java`
#### Snippet
```java
	public static int distinctCount(int numVals, int[] freqCounts, int nRows, int sampleSize) {

		int d = numVals;
		double f1 = freqCounts[0];
		int Nn = nRows * sampleSize;
```

### UnnecessaryLocalVariable
Local variable `high` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/sample/SampleEstimatorFactory.java`
#### Snippet
```java
		final int low = Math.max(frequencies.length, est);
		// upper: The number of rows minus the observed uniques total count, plus the observed number of uniques.
		final int high = Math.min(low, nRows - sampleSize + frequencies.length);
		return high;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstSample.java`
#### Snippet
```java
		for(int i = 0; i < sampleRows.length; i++)
			select.appendValue(sampleRows[i], 0, 1);
		MatrixBlock ret = _data.removeEmptyOperations(new MatrixBlock(), !_cs.transposed, true, select);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `off` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
			throw new NotImplementedException("Not Implemented non contiguous dense matrix encoding for sample");
		final DoubleCountHashMap map = new DoubleCountHashMap(16);
		final int off = col;
		final int nCol = m.getNumColumns();
		final int nRow = m.getNumRows();
```

### UnnecessaryLocalVariable
Local variable `que` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodePriorityQue.java`
#### Snippet
```java
	private static Queue<CompressedSizeInfoColGroup> getQue(int size, ACostEstimate cEst) {
		Comparator<CompressedSizeInfoColGroup> comp = Comparator.comparing(x -> cEst.getCost(x));
		Queue<CompressedSizeInfoColGroup> que = new PriorityQueue<>(size, comp);
		return que;
	}
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSizes.java`
#### Snippet
```java
		nrColumns = nrColumns > 0 ? nrColumns : 1;
		offsetLength += (nrRows / CompressionSettings.BITMAP_BLOCK_SZ) * 2;
		long size = estimateInMemorySizeOffset(nrColumns, contiguousColumns, nrValues, nrValues + 1, offsetLength,
			tupleSparsity, lossy);
		return size;
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
	public CM_COV_Object centralMoment(CMOperator op, int nRows) {
		// should be guaranteed to be one column therefore only one reference value.
		CM_COV_Object ret = _dict.centralMomentWithReference(op.fn, getCounts(), _reference[0], nRows);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java
	 */
	public final AColGroup sliceColumns(int cl, int cu) {
		AColGroup ret = (cu - cl == 1) ? sliceColumn(cl) : sliceMultiColumns(cl, cu);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `m` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		mb = new MatrixBlock(_numRows, colIndexes.size(), rawBlock.isInSparseFormat());

		final int m = _numRows;
		final int n = colIndexes.size();

```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
				retArr[j] = cols[i] - l;

		SliceResult ret = new SliceResult(s, e, ColIndexFactory.create(retArr));

		return ret;
```

### UnnecessaryLocalVariable
Local variable `sortedIndices` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
	@Override
	public int[] getReorderingIndex() {
		int[] sortedIndices = IntStream.range(0, cols.length).boxed()//
			.sorted((i, j) -> Integer.valueOf(cols[i]).compareTo(cols[j]))//
			.mapToInt(ele -> ele).toArray();
```

### UnnecessaryLocalVariable
Local variable `otV` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
			if(other instanceof ArrayIndex) {
				ArrayIndex ot = (ArrayIndex) other;
				int[] otV = ot.cols;
				return Arrays.equals(cols, otV);
			}
```

### UnnecessaryLocalVariable
Local variable `len` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/RangeIndex.java`
#### Snippet
```java

	private static boolean isValidRange(final int[] indexes, final int length) {
		int len = length;
		int first = indexes[0];
		int last = indexes[length - 1];
```

### UnnecessaryLocalVariable
Local variable `tbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToCharPByte.java`
#### Snippet
```java
		if(t instanceof MapToCharPByte) {
			MapToCharPByte tb = (MapToCharPByte) t;
			char[] tbb = tb._data_c;
			byte[] tbbb = tb._data_b;
			final int newSize = _data_c.length + t.size();
```

### UnnecessaryLocalVariable
Local variable `tbbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToCharPByte.java`
#### Snippet
```java
			MapToCharPByte tb = (MapToCharPByte) t;
			char[] tbb = tb._data_c;
			byte[] tbbb = tb._data_b;
			final int newSize = _data_c.length + t.size();
			final int newDistinct = Math.max(getUnique(), t.getUnique());
```

### UnnecessaryLocalVariable
Local variable `tbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToByte.java`
#### Snippet
```java
		if(t instanceof MapToByte) {
			final MapToByte tb = (MapToByte) t;
			final byte[] tbb = tb._data;
			final int newSize = _data.length + t.size();
			final int newDistinct = Math.max(getUnique(), t.getUnique());
```

### UnnecessaryLocalVariable
Local variable `tbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToInt.java`
#### Snippet
```java
		if(t instanceof MapToInt) {
			MapToInt tb = (MapToInt) t;
			int[] tbb = tb._data;
			final int newSize = _data.length + t.size();
			final int newDistinct = Math.max(getUnique(), t.getUnique());
```

### UnnecessaryLocalVariable
Local variable `tbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToBit.java`
#### Snippet
```java
		if(t instanceof MapToBit) {
			MapToBit tb = (MapToBit) t;
			BitSet tbb = tb._data;
			final int newSize = _size + t.size();
			BitSet ret = new BitSet(newSize);
```

### UnnecessaryLocalVariable
Local variable `CMi` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java

		final char CM = Character.MAX_VALUE;
		final int CMi = CM;
		final char c0 = (char) 0;

```

### UnnecessaryLocalVariable
Local variable `tbb` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToChar.java`
#### Snippet
```java
		if(t instanceof MapToChar) {
			MapToChar tb = (MapToChar) t;
			char[] tbb = tb._data;
			final int newSize = _data.length + t.size();
			final int newDistinct = Math.max(getUnique(), t.getUnique());
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
		ADictionary d = new IdentityDictionary(colIndexes.size(), containsNull);
		AMapToData m = binEncode(a, b, containsNull);
		AColGroup ret = ColGroupDDC.create(colIndexes, d, m, null);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
		AMapToData m = binEncode(a, b, containsNull);

		AColGroup ret = ColGroupDDC.create(colIndexes, d, m, null);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java

		AMapToData m = createHashMappingAMapToData(a, domain, nulls);
		AColGroup ret = ColGroupDDC.create(colIndexes, d, m, null);
		return ret;
	}
```

### UnnecessaryLocalVariable
Local variable `finalIndex` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		final double[] ret = new double[numVals + 1];

		final int finalIndex = numVals;
		for(int i = 0; i < nCol; i++)
			ret[finalIndex] += reference[i];
```

### UnnecessaryLocalVariable
Local variable `finalIndex` is redundant
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		final double[] ret = new double[numVals + 1];

		final int finalIndex = numVals;
		for(int i = 0; i < nCol; i++)
			ret[finalIndex] += reference[i] * reference[i];
```

### UnnecessaryLocalVariable
Local variable `li` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java

	public LineageItem getLineageItem(int ix) {
		LineageItem li = _lineage != null ? _lineage.get(ix):null;
		return li;
	}
```

### UnnecessaryLocalVariable
Local variable `linst` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMChainFEDInstruction.java`
#### Snippet
```java

	public static MMChainFEDInstruction parseInstruction(MMChainCPInstruction inst, ExecutionContext ec) {
		MMChainCPInstruction linst = (MMChainCPInstruction) inst;
		MatrixObject mo = ec.getMatrixObject(linst.input1);
		if( mo.isFederated(FType.ROW) )
```

### UnnecessaryLocalVariable
Local variable `ainstruction` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/fed/BinaryFEDInstruction.java`
#### Snippet
```java
		else if(inst instanceof AppendGAlignedSPInstruction || inst instanceof AppendGSPInstruction ||
			inst instanceof AppendMSPInstruction || inst instanceof AppendRSPInstruction) {
			BinarySPInstruction ainstruction = (BinarySPInstruction) inst;
			Data data1 = ec.getVariable(ainstruction.input1);
			Data data2 = ec.getVariable(ainstruction.input2);
```

### UnnecessaryLocalVariable
Local variable `clen` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MMTSJGPUInstruction.java`
#### Snippet
```java
		boolean isLeftTransposed = ( _type == MMTSJType.LEFT);
		int rlen = (int) (isLeftTransposed? mat.getNumColumns() : mat.getNumRows());
		int clen = rlen;
		//execute operations 
		ec.setMetaData(_output.getName(), rlen, clen);
```

### UnnecessaryLocalVariable
Local variable `r` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 */
	public static void copyPtrToHost(CSRPointer src, int rows, long nnz, int[] rowPtr, int[] colInd) {
		CSRPointer r = src;
		cudaMemcpy(Pointer.to(rowPtr), r.rowPtr, getIntSizeOf(rows + 1), cudaMemcpyDeviceToHost);
		cudaMemcpy(Pointer.to(colInd), r.colInd, getIntSizeOf(nnz), cudaMemcpyDeviceToHost);
```

### UnnecessaryLocalVariable
Local variable `r` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
	 */
	public static void copyToDevice(GPUContext gCtx, CSRPointer dest, int rows, long nnz, int[] rowPtr, int[] colInd, double[] values) {
		CSRPointer r = dest;
		r.nnz = nnz;
		if(rows < 0) throw new DMLRuntimeException("Incorrect input parameter: rows=" + rows);
```

### UnnecessaryLocalVariable
Local variable `tot` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/CSRPointer.java`
#### Snippet
```java
		// 	cusparseIndexBase_t IndexBase;
		// } cusparseMatDescr_t;
		long tot = sizeofValArray + sizeofRowPtrArray + sizeofColIndArray + sizeofDescr;
		return tot;
	}
```

### UnnecessaryLocalVariable
Local variable `in` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		//get input
		// TODO support DataTensor
		JavaPairRDD<TensorIndexes, TensorBlock> in = sec.getBinaryTensorBlockRDDHandleForVariable( input1.getName() );
		JavaPairRDD<TensorIndexes, TensorBlock> out = in;

```

### UnnecessaryLocalVariable
Local variable `lda` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		int n = toIntExact(mat.getNumRows());
		int m = toIntExact(mat.getNumColumns());
		int lda = n;
		int ldc = m;
		if (!isAllocated()) {
```

### UnnecessaryLocalVariable
Local variable `ldc` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		int m = toIntExact(mat.getNumColumns());
		int lda = n;
		int ldc = m;
		if (!isAllocated()) {
			throw new DMLRuntimeException("Error in converting column major to row major : data is not allocated");
```

### UnnecessaryLocalVariable
Local variable `eitherAllocated` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java

	public boolean isAllocated() {
		boolean eitherAllocated = shadowBuffer.isBuffered() || !isDensePointerNull() || getJcudaSparseMatrixPtr() != null;
		return eitherAllocated;
	}
```

### UnnecessaryLocalVariable
Local variable `A` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		Pointer alpha = LibMatrixCUDA.one();
		Pointer beta = LibMatrixCUDA.zero();
		Pointer A = densePtr;
		Pointer C = gCtx.allocate(null, m * getDatatypeSizeOf(n), false);

```

### UnnecessaryLocalVariable
Local variable `lda` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		int m = toIntExact(mat.getNumRows());
		int n = toIntExact(mat.getNumColumns());
		int lda = n;
		int ldc = m;
		if (!isAllocated()) {
```

### UnnecessaryLocalVariable
Local variable `ldc` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		int n = toIntExact(mat.getNumColumns());
		int lda = n;
		int ldc = m;
		if (!isAllocated()) {
			throw new DMLRuntimeException("Error in converting row major to column major : data is not allocated");
```

### UnnecessaryLocalVariable
Local variable `isEmptyAndSparseAndAllocated` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
	public boolean isSparseAndEmpty() {
		boolean isSparseAndAllocated = isAllocated() && LibMatrixCUDA.isInSparseFormat(getGPUContext(), mat);
		boolean isEmptyAndSparseAndAllocated = isSparseAndAllocated && getJcudaSparseMatrixPtr().nnz == 0;
		return isEmptyAndSparseAndAllocated;
	}
```

### UnnecessaryLocalVariable
Local variable `fb` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernaryFrameScalarSPInstruction.java`
#### Snippet
```java
		@Override
		public FrameBlock call(FrameBlock arg0) throws Exception {
			FrameBlock fb =  arg0.map(_expr, _margin);
			return fb;
		}
```

### UnnecessaryLocalVariable
Local variable `mbout` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		
		//collect output without shuffle to avoid side-effects with custom PartitionPruningRDD
		MatrixBlock mbout = SparkExecutionContext.toMatrixBlock(out, (int)mcOut.getRows(), 
				(int)mcOut.getCols(), mcOut.getBlocksize(), -1);
		return mbout;
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinUaggChainSPInstruction.java`
#### Snippet
```java
			
			// perform binary operation
			MatrixBlock ret = arg0.binaryOperations(_bOp, out1);

			return ret;
```

### UnnecessaryLocalVariable
Local variable `blkOut` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java

            MatrixIndexes idxOut = new MatrixIndexes();
            MatrixBlock blkOut = blkIn;  // Do not create sketch yet
            this._op.indexFn.execute(idxIn, idxOut);

```

### UnnecessaryLocalVariable
Local variable `in` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java

        //get input
        JavaPairRDD<MatrixIndexes, MatrixBlock> in = sec.getBinaryMatrixBlockRDDHandleForVariable(input1.getName());
        JavaPairRDD<MatrixIndexes, MatrixBlock> out = in;

```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySketchSPInstruction.java`
#### Snippet
```java
        //get input
        JavaPairRDD<MatrixIndexes, MatrixBlock> in = sec.getBinaryMatrixBlockRDDHandleForVariable(input1.getName());
        JavaPairRDD<MatrixIndexes, MatrixBlock> out = in;

        // dir = RowCol and (dim1() > 1000 || dim2() > 1000)
```

### UnnecessaryLocalVariable
Local variable `out2` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
			//execute mapmm and aggregation if necessary and put output into symbol table
			JavaRDD<MatrixBlock> out = _in1.map(new RDDMapMMFunction2(_type, _in2));
			MatrixBlock out2 = RDDAggregateUtils.sumStable(out);
			return out2;
		}
```

### UnnecessaryLocalVariable
Local variable `fb` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinaryFrameMatrixSPInstruction.java`
#### Snippet
```java
		@Override public FrameBlock call(FrameBlock frameBlock) throws Exception {
			int idx = (int)featureLength.getNumRows();
			FrameBlock fb = frameBlock.invalidByLength(featureLength.getBlock(1, idx));
			return fb;
		}
```

### UnnecessaryLocalVariable
Local variable `in4` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
			FileFormatProperties formatProperties = new FileFormatPropertiesCSV(hasHeader, delim, sparse);
			inst.setFormatProperties(formatProperties);
			CPOperand in4 = new CPOperand(parts[8]);
			inst.input4 = in4;
		}
```

### UnnecessaryLocalVariable
Local variable `in4` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
			FileFormatProperties formatProperties = new FileFormatPropertiesLIBSVM(delim, indexDelim, sparse);
			inst.setFormatProperties(formatProperties);
			CPOperand in4 = new CPOperand(parts[8]);
			inst.input4 = in4;
		}
```

### UnnecessaryLocalVariable
Local variable `in4` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
			FileFormatProperties ffp = new FileFormatProperties();

			CPOperand in4 = new CPOperand(parts[5]);
			inst.input4 = in4;
			inst.setFormatProperties(ffp);
```

### UnnecessaryLocalVariable
Local variable `data` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
		long lrows = sec.getScalarInput(rows).getLongValue();
		long lcols = sec.getScalarInput(cols).getLongValue();
		String data = frame_data;

		//step 1: generate pseudo-random seed (because not specified)
```

### UnnecessaryLocalVariable
Local variable `tmpblk` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/SpoofSPInstruction.java`
#### Snippet
```java
			for( long i=1; i<=numBlocks; i++ ) {
				MatrixIndexes tmpix = new MatrixIndexes(i, j);
				MatrixBlock tmpblk = blkIn;
				ret.add( new Tuple2<>(tmpix, tmpblk) );
			}
```

### UnnecessaryLocalVariable
Local variable `row` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
					}
				}
				Row row = RowFactory.create(fieldsArr.toArray());
				return row;
			}
```

### UnnecessaryLocalVariable
Local variable `outDF` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		// output DF
		JavaRDD<Row> newRows = inputDF.rdd().toJavaRDD().zipWithIndex().map(new StringToVector());
		Dataset<Row> outDF = sparkSession.createDataFrame(newRows.rdd(), DataTypes.createStructType(newSchema));
		return outDF;
	}
```

### UnnecessaryLocalVariable
Local variable `ret` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
	{
		// compute dimensions and nnz in single pass
		DataCharacteristics ret = input
				.map(new AnalyzeCellDataCharacteristics())
				.reduce(new AggregateDataCharacteristics());
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
			//copy row into existing target block
			MatrixBlock row = arg1.getValue();
			MatrixBlock out = arg0; //in-place update
			out.copy(arg1.getRow(), arg1.getRow(), 0, row.getNumColumns()-1, row, true);
			out.examSparsity();
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		
		//strip-off correction blocks from
		JavaPairRDD<MatrixIndexes, MatrixBlock> out =
			tmp.mapValues( new ExtractMatrixBlock() );
		
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		
		//strip-off correction blocks from
		JavaPairRDD<MatrixIndexes, Double> out =
				tmp.mapValues( new ExtractDoubleCell() );
		
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDAggregateUtils.java`
#### Snippet
```java
		
		//strip-off correction blocks from
		JavaPairRDD<MatrixIndexes, MatrixBlock> out =  
				tmp.mapValues( new ExtractMatrixBlock() );
		
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java

		//convert csv rdd to binary block rdd (w/ partial blocks)
		JavaPairRDD<Long, FrameBlock> out = prepinput.mapPartitionsToPair(
				new CSVToBinaryBlockFunction(mc, schema, hasHeader, delim, naStrings));
		
```

### UnnecessaryLocalVariable
Local variable `compare` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeFrameLocalMemory.java`
#### Snippet
```java
			
			//create compare matrix if required (existing data in result)
			FrameBlock compare = outFB;
			int rlen = compare.getNumRows();
			int clen = compare.getNumColumns();
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		//convert binary block to libsvm (from blocks/rows)
		JavaRDD<String> out = input.flatMap(new BinaryBlockToLIBSVMFunction(props));

		return out;
```

### UnnecessaryLocalVariable
Local variable `pointrdd` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
	{
		//convert indexed binary block input to collection of labeled points
		JavaRDD<LabeledPoint> pointrdd = in
				.values()
				.flatMap(new PrepareBinaryBlockFunction());
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java

		//convert binary block to csv (from blocks/rows)
		JavaRDD<String> out = input.flatMap(new BinaryBlockToCSVFunction(props));

		return out;
```

### UnnecessaryLocalVariable
Local variable `N` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		long lIncr = _incrVal.getLongValue();
		int P = _numThreads;  // number of parallel workers
		long N = _numIter;    // total number of iterations
		long R = N;           // remaining number of iterations
		long K = -1;          // next _numThreads task sizes
```

### UnnecessaryLocalVariable
Local variable `N` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/TaskPartitionerFactoring.java`
#### Snippet
```java
		
		int P = _numThreads;   // number of parallel workers
		long N = _numIter;     // total number of iterations
		long R = N;            // remaining number of iterations
		long K = -1;           //next _numThreads task sizes	
```

### UnnecessaryLocalVariable
Local variable `key` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	private static long getKeyFromFilePath( String dir ) {
		String[] dirparts = dir.split("/");
		long key = Long.parseLong( dirparts[dirparts.length-1] );
		return key;
	}
```

### UnnecessaryLocalVariable
Local variable `numRed` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					int numMap = Math.max(_numThreads,
						SparkExecutionContext.getDefaultParallelism(true));
					int numRed = numMap; //equal map/reduce
					rm = new ResultMergeRemoteSpark( (MatrixObject)out,
						(MatrixObject[])in, fname, accum, ec, numMap, numRed );
```

### UnnecessaryLocalVariable
Local variable `brVars` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		Set<String> retVars = resultVars.stream()
			.map(v -> v._name).collect(Collectors.toSet());
		Set<String> brVars = inputs.keySet().stream()
			.filter(v -> !retVars.contains(v))
			.filter(v -> ec.getVariable(v).getDataType().isMatrixOrFrame())
```

### UnnecessaryLocalVariable
Local variable `numCreatedTasks` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		String resultFile = constructResultFileName();
		long numIterations = partitioner.getNumIterations();
		long numCreatedTasks = numIterations;//partitioner.createTasks().size();
		
		if( _monitor )
```

### UnnecessaryLocalVariable
Local variable `rewriter` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizationWrapper.java`
#### Snippet
```java
		sbRewrites.add( new RewriteRemoveUnnecessaryBranches() );
		
		ProgramRewriter rewriter = new ProgramRewriter( hRewrites, sbRewrites );
		
		return rewriter;
```

### UnnecessaryLocalVariable
Local variable `in` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/StagingFileUtils.java`
#### Snippet
```java
	{
		FileInputStream fis = new FileInputStream( name );
		BufferedReader in = new BufferedReader(new InputStreamReader(fis));	
		return in;
	}
```

### UnnecessaryLocalVariable
Local variable `pid` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/IDHandler.java`
#### Snippet
```java
		//get process id
		String pname = ManagementFactory.getRuntimeMXBean().getName(); //pid@hostname
		String pid = pname.split("@")[0];
		// TODO: change this as soon as we switch to a java version >= 9
		// import java.lang.ProcessHandle;
```

### UnnecessaryLocalVariable
Local variable `intVal` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/util/IDHandler.java`
#### Snippet
```java
	public static int extractIntID( String taskID ) {
		int maxlen = (int)(Math.log10(Integer.MAX_VALUE));
		int intVal = (int)extractID( taskID, maxlen );
		return intVal;
	}
```

### UnnecessaryLocalVariable
Local variable `fr` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
				ru.getLiteralLineageItem(), cl.getLiteralLineageItem(), cu.getLiteralLineageItem()});
		}
		FederatedRequest fr = new FederatedRequest(RequestType.PUT_VAR, li, id,
			cb.slice(ix[0], ix[1], ix[2], ix[3]));
		return fr;
```

### UnnecessaryLocalVariable
Local variable `linst` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java

	public static FederatedRequest[] callInstruction(String[] inst, CPOperand varOldOut, long outputId, CPOperand[] varOldIn, long[] varNewIn, ExecType type) {
		String[] linst = inst;
		FederatedRequest[] fr = new FederatedRequest[inst.length];
		for(int j=0; j<inst.length; j++) {
```

### UnnecessaryLocalVariable
Local variable `linst` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
	public static FederatedRequest[] callInstruction(String[] inst, CPOperand varOldOut, CPOperand[] varOldIn, long[] varNewIn) {
		long id = getNextFedDataID();
		String[] linst = inst;
		FederatedRequest[] fr = new FederatedRequest[inst.length];
		for(int j=0; j<inst.length; j++) {
```

### UnnecessaryLocalVariable
Local variable `final_averagingFactor` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamServer.java`
#### Snippet
```java

		if( _averagingFactor != 1) {
			double final_averagingFactor = _averagingFactor;
			params.getData().parallelStream().forEach((matrix) -> {
				MatrixObject matrixObject = (MatrixObject) matrix;
```

### UnnecessaryLocalVariable
Local variable `out` is redundant
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
	public JavaPairRDD<Long,FrameBlock> getFrameBinaryBlockRDDHandleForVariable( String varname ) {
		FrameObject fo = getFrameObject(varname);
		JavaPairRDD<Long,FrameBlock> out = (JavaPairRDD<Long,FrameBlock>)
			getRDDHandleForFrameObject(fo, FileFormat.BINARY);
		return out;
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

### UnnecessaryLocalVariable
Local variable `builder` is redundant
in `src/main/java/org/apache/sysds/protobuf/SysdsProtos.java`
#### Snippet
```java
    protected Builder newBuilderForType(
        com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      Builder builder = new Builder(parent);
      return builder;
    }
```

## RuleId[id=UnpredictableBigDecimalConstructorCall]
### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java

		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
	}

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		}
		for(int x = 0; x < def.length; x++)
			tmp = tmp.multiply(new BigDecimal(def[x]).pow(defCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
	}

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		}
		for(int x = 0; x < reference.length; x++)
			tmp = tmp.multiply(new BigDecimal(reference[x]).pow(refCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
	}

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			}
			for(int x = 0; x < def.length; x++)
				tmp = tmp.multiply(new BigDecimal(def[x]).pow(defCount, cont), cont);
			if(Math.abs(tmp.doubleValue()) == 0)
				ret[0] = 0;
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				ret[0] = 0;
			else if(!Double.isInfinite(ret[0]))
				ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
		}
	}
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
		}
		for(int x = 0; x < reference.length; x++)
			tmp = tmp.multiply(new BigDecimal(reference[x]).pow(refCount, cont), cont);
		if(Math.abs(tmp.doubleValue()) == 0)
			ret[0] = 0;
```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			ret[0] = 0;
		else if(!Double.isInfinite(ret[0]))
			ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
	}

```

### UnpredictableBigDecimalConstructorCall
Unpredictable `new BigDecimal()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
				ret[0] = 0;
			else if(!Double.isInfinite(ret[0]))
				ret[0] = new BigDecimal(ret[0]).multiply(tmp, MathContext.DECIMAL128).doubleValue();
		}
	}
```

## RuleId[id=BusyWait]
### BusyWait
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/ByteBuffer.java`
#### Snippet
```java
		//robust checking until serialized
		while( !_serialized ) {
			try{Thread.sleep(1);} catch(Exception e) {}
		}
	}
```

## RuleId[id=ArrayHashCode]
### ArrayHashCode
`hashCode()` called on array should probably be 'Arrays.hashCode()'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/PlaintextMatrix.java`
#### Snippet
```java
	@Override
	public String getDebugName() {
		return "PlaintextMatrix " + getData().hashCode();
	}
}
```

### ArrayHashCode
`hashCode()` called on array should probably be 'Arrays.hashCode()'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CiphertextMatrix.java`
#### Snippet
```java
    @Override
    public String getDebugName() {
        return "CiphertextMatrix " + getData().hashCode();
    }
}
```

## RuleId[id=ArraysAsListWithZeroOrOneArgument]
### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
		tpl.setCellType(TemplateUtils.getCellType(hop));
		tpl.setAggOp(TemplateUtils.getAggOp(hop));
		tpl.setSparseSafe(isSparseSafe(Arrays.asList(hop), sinHops[0], 
			Arrays.asList(tpl.getOutput()), Arrays.asList(tpl.getAggOp()), false));
		tpl.setContainsSeq(rContainsSeq(tpl.getOutput(), new HashSet<>()));
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
		tpl.setAggOp(TemplateUtils.getAggOp(hop));
		tpl.setSparseSafe(isSparseSafe(Arrays.asList(hop), sinHops[0], 
			Arrays.asList(tpl.getOutput()), Arrays.asList(tpl.getAggOp()), false));
		tpl.setContainsSeq(rContainsSeq(tpl.getOutput(), new HashSet<>()));
		tpl.setRequiresCastDtm(hop instanceof AggBinaryOp);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
		tpl.setAggOp(TemplateUtils.getAggOp(hop));
		tpl.setSparseSafe(isSparseSafe(Arrays.asList(hop), sinHops[0], 
			Arrays.asList(tpl.getOutput()), Arrays.asList(tpl.getAggOp()), false));
		tpl.setContainsSeq(rContainsSeq(tpl.getOutput(), new HashSet<>()));
		tpl.setRequiresCastDtm(hop instanceof AggBinaryOp);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMergeBlockSequence.java`
#### Snippet
```java
	public List<StatementBlock> rewriteStatementBlock(StatementBlock sb, 
			ProgramRewriteStatus state) {
		return Arrays.asList(sb);
	}
	
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteHoistLoopInvariantOperations.java`
#### Snippet
```java
		//early abort if possible
		if( sb == null || !HopRewriteUtils.isLoopStatementBlock(sb) )
			return Arrays.asList(sb); //rewrite only applies to loops
		
		//step 1: determine read-only variables
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteHoistLoopInvariantOperations.java`
#### Snippet
```java
		
		//step 3: create new statement block for all temporary intermediates
		return invariantOps.isEmpty() ? Arrays.asList(sb) :
			Arrays.asList(createStatementBlock(sb, invariantOps), sb);
	}
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteInjectSparkLoopCheckpointing.java`
#### Snippet
```java
		if( !OptimizerUtils.isSparkExecutionMode() ) {
			// nothing to do here, return original statement block
			return Arrays.asList(sb);
		}
		
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/MarkForLineageReuse.java`
#### Snippet
```java
	{
		if (!HopRewriteUtils.isLoopStatementBlock(sb) || LineageCacheConfig.ReuseCacheType.isNone())
			return Arrays.asList(sb);    //early abort

		if (sb instanceof ForStatementBlock) {
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/MarkForLineageReuse.java`
#### Snippet
```java
		if (sb instanceof ForStatementBlock) {
			ForStatement fstmt = (ForStatement)sb.getStatement(0);
			Set<String> loopVar = new HashSet<>(Arrays.asList(fstmt.getIterablePredicate().getIterVar().getName()));
			HashSet<String> deproots = new HashSet<>();
			rUnmarkLoopDepVarsSB(fstmt.getBody(), deproots, loopVar);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/MarkForLineageReuse.java`
#### Snippet
```java
			rUnmarkLoopDepVarsSB(wstmt.getBody(), deproots, loopVar);
		}
		return Arrays.asList(sb);
	}
	
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
		if( DMLScript.getGlobalExecMode() == ExecMode.SPARK ) {
			// nothing to do here, return original statement block
			return Arrays.asList(sb);
		}
		
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
			
		//return modified statement block
		return Arrays.asList(sb);
	}
	
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagUnknownCSVRead.java`
#### Snippet
```java
		if( DMLScript.getGlobalExecMode() == ExecMode.SINGLE_NODE
			|| !HopRewriteUtils.isLastLevelStatementBlock(sb) )
			return Arrays.asList(sb);
		
		ArrayList<StatementBlock> ret = new ArrayList<>();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
		//if no rewrite applied sb is the original for loop otherwise a last level statement block
		//that includes the equivalent vectorized operations.
		return Arrays.asList(sb);
	}
	
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
			&& !(compress != CompressConfig.FALSE) )
			|| !HopRewriteUtils.isLastLevelStatementBlock(sb) )
			return Arrays.asList(sb);
		
		ArrayList<StatementBlock> ret = new ArrayList<>();
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		// check for inapplicable statement blocks
		if(!HopRewriteUtils.isLastLevelStatementBlock(sb) || sb.getHops() == null)
			return Arrays.asList(sb);

		// parse compression config
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			Hop.resetVisitStatus(sb.getHops());
		}
		return Arrays.asList(sb);
	}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		//need for synchronization as we do temp changes in shared hops/lops
		synchronized( hop ) {
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
				vars, status, inplace, replaceLit, true, false, true, null, tid);
		}
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		synchronized( hop ) {
			//always in place, no stats update/rewrites
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
				(LocalVariableMap)null, null, true, false, false, false, true, null, 0);
		}
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		synchronized( hop ) {
			//always in place, no stats update/rewrites, but forced exec type
			newInst = recompile(null, new ArrayList<>(Arrays.asList(hop)),
				(LocalVariableMap)null, null, true, false, false, true, true, et, tid);
		}
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddPrefetchLop.java`
#### Snippet
```java
		}
		//New node is added inplace in the Lop DAG
		return Arrays.asList(sb);
	}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddBroadcastLop.java`
#### Snippet
```java
		}
		// New node is added inplace in the Lop DAG
		return Arrays.asList(sb);
	}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/lops/compile/linearization/CostBasedLinearize.java`
#### Snippet
```java
		public Order(Lop lop) {
			// Initiate the memory estimates for the first operator
			this(Arrays.asList(lop), lop.getOutputMemoryEstimate(), 0, lop.getComputeEstimate());
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		//most statements will be returned unchanged, while expressions with
		//function calls are split into potentially many statements
		List<Statement> ret = tmp.isEmpty() ? Arrays.asList(stmt) : tmp;
		if( !tmp.isEmpty() ) {
			for( Statement ltmp : tmp )
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				return createStatementBlocks(current, tmp);
		}
		return Arrays.asList(current);
	}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
				//prepare input source code
				Iterable<? extends JavaFileObject> sources = fileManager
						.getJavaFileObjectsFromFiles(Arrays.asList(ftmp));

				//prepare class path
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRecomputeUtils.java`
#### Snippet
```java
		for (int i=0; i<inpHops.size(); i++)
			funcInputs.add(new DataIdentifier(inputs[i], inpHops.get(i).getDataType(), inpHops.get(i).getValueType()));
		List<DataIdentifier> funcOutput = new ArrayList<>(Arrays.asList(new DataIdentifier(outname)));
		// TODO: multi-return function
		FunctionProgramBlock fpb = new FunctionProgramBlock(prog, funcInputs, funcOutput);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
	 */
	public ListObject remove(int pos) {
		ListObject ret = new ListObject(Arrays.asList(_data.get(pos)),
				null, _lineage != null?Arrays.asList(_lineage.get(pos)):null);
		_data.remove(pos);
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
	public ListObject remove(int pos) {
		ListObject ret = new ListObject(Arrays.asList(_data.get(pos)),
				null, _lineage != null?Arrays.asList(_lineage.get(pos)):null);
		_data.remove(pos);
		if (_lineage != null)
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		@Override
		public List<Long> getOutputIds() {
			return new ArrayList<>(Arrays.asList(_outputID));
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
		@Override
		public List<Long> getOutputIds() {
			return new ArrayList<>(Arrays.asList(_outputID));
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
		@Override
		public List<Long> getOutputIds() {
			return new ArrayList<>(Arrays.asList(_outputID));
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		@Override
		public List<Long> getOutputIds() {
			return new ArrayList<>(Arrays.asList(_outputID));
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		@Override
		public List<Long> getOutputIds() {
			return new ArrayList<>(Arrays.asList(_outputID));
		}

```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/RecomputeNnzFunction.java`
#### Snippet
```java
		while( iter.hasNext() )
			nnz += iter.next().getNonZeros();
		return Arrays.asList(nnz).iterator();
	}
}
```

### ArraysAsListWithZeroOrOneArgument
Call to `asList()` with only one argument
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
			// and sets it in the ExecutionContext of the parfor
			if (DMLScript.USE_ACCELERATOR){
				cpEc.setGPUContexts(Arrays.asList(ec.getGPUContext(index)));
			}
			
```

## RuleId[id=PointlessBooleanExpression]
### PointlessBooleanExpression
`ret |= true` can be simplified to 'ret=true'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
		for( Hop h : hops )
			if( h instanceof FunctionOp )
				ret |= true;
		
		return ret;
```

### PointlessBooleanExpression
`DMLScript.VALIDATOR_IGNORE_ISSUES == true` can be simplified to 'DMLScript.VALIDATOR_IGNORE_ISSUES'
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	protected void setPrintStatement(ParserRuleContext ctx, String functionName,
			ArrayList<ParameterExpression> paramExpression, StatementInfo thisinfo) {
		if (DMLScript.VALIDATOR_IGNORE_ISSUES == true) { // create dummy print statement
			try {
				thisinfo.stmt = new PrintStatement(ctx, functionName, currentFile);
```

### PointlessBooleanExpression
`((Boolean)in).booleanValue() == false` can be simplified to '!((Boolean)in).booleanValue()'
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			else if(in instanceof Long && ((Integer)in).intValue() == 0)
				return null;
			else if(in instanceof Boolean && ((Boolean)in).booleanValue() == false)
				return null;
			else if(in instanceof String && ((String)in).trim().length() == 0)
```

### PointlessBooleanExpression
`isLeftTransposed == true` can be simplified to 'isLeftTransposed'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
			ec.allocateGPUMatrixObject(outputName, outRLen, outCLen);

			if (in1 == in2 && isLeftTransposed == true && isLeftTransposed == isRightTransposed) {
				// Special case for transpose

```

### PointlessBooleanExpression
`ret[off + _data[boff + bix + i]] &= false` can be simplified to 'ret\[off + _data\[boff + bix + i\]\]=false'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
				slen = _data[boff + bix];
				for(int i = 1; i <= slen; i++) {
					ret[off + _data[boff + bix + i]] &= false;
				}
			}
```

### PointlessBooleanExpression
`contains == false` can be simplified to '!contains'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
				break;
			}
		if(contains == false)
			return null;
		else
```

### PointlessBooleanExpression
`ret |= true` can be simplified to 'ret=true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
				&& h.getInput().get(0).getName().equals(var) )
			{
				ret |= true;
			}
		}
```

### PointlessBooleanExpression
`_confOnly &= true` does not modify value of '_confOnly \&= true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				_numExecutors = numExecutors;
				_defaultPar = (defaultPar>1) ? defaultPar : numExecutors * numCoresPerExec;
				_confOnly &= true;
			}
			else if( DMLScript.USE_LOCAL_SPARK_CONFIG ) {
```

### PointlessBooleanExpression
`_confOnly &= true` does not modify value of '_confOnly \&= true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				_numExecutors = 1;
				_defaultPar = 2;
				_confOnly &= true;
			}
			else {
```

### PointlessBooleanExpression
`_confOnly &= false` can be simplified to '_confOnly=false'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				_numExecutors = Math.max(jsc.sc().getExecutorMemoryStatus().size() - 1, 1);
				_defaultPar = jsc.defaultParallelism();
				_confOnly &= false; //implies env info refresh w/ spark context
			}
		}
```

## RuleId[id=IntegerDivisionInFloatingPointContext]
### IntegerDivisionInFloatingPointContext
`(2+2) * (d1m * d1n) /2`: integer division in floating-point context
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
					//(mmchain essentially two matrix-vector muliplications)
					if( !leftSparse  )
						return (2+2) * (d1m * d1n) /2;
					else 
						return (2+2) * (d1m * d1n * d1s) /2;
```

### IntegerDivisionInFloatingPointContext
`1/(R*S)`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
			a[aix[k]] = avals[k];
		LibMatrixDNNPooling.poolingDenseStride1Pad0(PoolingType.AVG,
			0, 1/(R*S), a, c, rix, rix+1, 0, 0, C, P, Q, R, S, H, W);
		return c;
	}
```

### IntegerDivisionInFloatingPointContext
`1/(R*S)`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/codegen/LibSpoofPrimitives.java`
#### Snippet
```java
		double[] c = allocVector(C*P*Q, true);
		LibMatrixDNNPooling.poolingDenseStride1Pad0(PoolingType.AVG,
			0, 1/(R*S), a, c, rix, rix+1, ai, 0, C, P, Q, R, S, H, W);
		return c;
	} 
```

### IntegerDivisionInFloatingPointContext
`estimatedNonZerosInSample / sampleSize`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstSample.java`
#### Snippet
```java
			final SparseBlock resb = res.getSparseBlock();
			final SparseBlockMCSR resbmcsr = (SparseBlockMCSR) resb;
			final int estimatedNrDoublesEachRow = (int) Math.max(4, Math.ceil(estimatedNonZerosInSample / sampleSize));
			for(int col = 0; col < resRows; col++)
				resbmcsr.allocate(col, estimatedNrDoublesEachRow);
```

### IntegerDivisionInFloatingPointContext
`Math.min(mb.estimateSizeDenseInMemory(), mb.estimateSizeSparseInMemory()) / mb.estimateSizeInMem...`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
			LOG.debug(String.format("Compressed transform encode size:           %16d", mb.estimateSizeInMemory()));

			double ratio = Math.min(mb.estimateSizeDenseInMemory(), mb.estimateSizeSparseInMemory()) /
				mb.estimateSizeInMemory();
			double denseRatio = mb.estimateSizeDenseInMemory() / mb.estimateSizeInMemory();
			LOG.debug(String.format("Compression ratio: %10.3f", ratio));
```

### IntegerDivisionInFloatingPointContext
`mb.estimateSizeDenseInMemory() / mb.estimateSizeInMemory()`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/transform/encode/CompressedEncode.java`
#### Snippet
```java
			double ratio = Math.min(mb.estimateSizeDenseInMemory(), mb.estimateSizeSparseInMemory()) /
				mb.estimateSizeInMemory();
			double denseRatio = mb.estimateSizeDenseInMemory() / mb.estimateSizeInMemory();
			LOG.debug(String.format("Compression ratio: %10.3f", ratio));
			LOG.debug(String.format("Dense ratio:       %10.3f", denseRatio));
```

### IntegerDivisionInFloatingPointContext
`matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns())`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in2.getName());
			if(filter.isEmpty() || matBlock.isEmpty()) {
```

### IntegerDivisionInFloatingPointContext
`matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns())`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d_bias_add")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in3.getName());
			MatrixBlock bias = ec.getMatrixInput(_in2.getName());
```

### IntegerDivisionInFloatingPointContext
`size/InfrastructureAnalyzer.getHDFSBlockSize()`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
				Math.max(mc.getRows(),1), mc.getBlocksize()), Math.min(Math.max(mc.getCols(),1), mc.getBlocksize()));
		int par = (int) Math.min(4*Math.max(SparkExecutionContext.getDefaultParallelism(true),
				Math.ceil(size/InfrastructureAnalyzer.getHDFSBlockSize())), mc.getNumBlocks());
		long pNumBlocks = (long)Math.ceil((double)mc.getNumBlocks()/par);
		
```

### IntegerDivisionInFloatingPointContext
`InfrastructureAnalyzer.getLocalMaxMemory()/_rk`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			_rk  = InfrastructureAnalyzer.getLocalParallelism();
			_rk2 = InfrastructureAnalyzer.getLocalParallelism();
			_rm  = InfrastructureAnalyzer.getLocalMaxMemory()/_rk;
			_rm2  = InfrastructureAnalyzer.getLocalMaxMemory()/_rk2;
		}
```

### IntegerDivisionInFloatingPointContext
`InfrastructureAnalyzer.getLocalMaxMemory()/_rk2`: integer division in floating-point context
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			_rk2 = InfrastructureAnalyzer.getLocalParallelism();
			_rm  = InfrastructureAnalyzer.getLocalMaxMemory()/_rk;
			_rm2  = InfrastructureAnalyzer.getLocalMaxMemory()/_rk2;
		}
		
```

## RuleId[id=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassForwardFunctionCalls.java`
#### Snippet
```java
		HashSet<String> probe = new HashSet<>();
		for( String s : second )
			probe.add(s);
		//probe phase: first
		return Arrays.stream(first).allMatch(s -> probe.contains(s));
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
	public CNodeNary( CNode[] inputs, NaryType type ) {
		for( CNode in : inputs )
			_inputs.add(in);
		_type = type;
		setOutputDims();
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
		_inputs = new HashSet<>();
		for( String var : in )
			_inputs.add( var );
		_outputs = new HashSet<>();
		for( String var : out )
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
		_outputs = new HashSet<>();
		for( String var : out )
			_outputs.add( var );
		
		//store input meta data
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMatrixMultChainOptimization.java`
#### Snippet
```java
		mmOperators.add(hop);
		for( Hop hi : hop.getInput() )
			mmChain.add(hi);

		// expand each Hop in mmChain to find the entire matrix multiplication
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
		_sbRuleSet = new ArrayList<>();
		for( StatementBlockRewriteRule rewrite : rewrites )
			_sbRuleSet.add( rewrite );
	}
	
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/hops/rewrite/ProgramRewriter.java`
#### Snippet
```java
		_dagRuleSet = new ArrayList<>();
		for( HopRewriteRule rewrite : rewrites )
			_dagRuleSet.add( rewrite );
		_sbRuleSet = new ArrayList<>();
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `src/main/java/org/apache/sysds/parser/DMLProgram.java`
#### Snippet
```java
		for( FunctionDictionary<FunctionStatementBlock> dict : _namespaces.values() )
			for( Entry<String, FunctionStatementBlock> e : dict.getFunctions().entrySet() )
				ret.put(e.getKey(), e.getValue());
		return ret;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
			this.naStrings = new HashSet<>();
			for(String s: naS)
				this.naStrings.add(s);
		}
		if(LOG.isDebugEnabled())
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			ArrayList<Instruction> pInst = pred_dag.getJobs(null, config);
			for (Instruction i : pInst ) {
				pred_instruct.add(i);
			}
			
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			ArrayList<Instruction> pInst = pred_dag.getJobs(null, config);
			for (Instruction i : pInst ) {
				pred_instruct.add(i);
			}
			
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
	public static <T> void addAll(Collection<T> a, T[] b) {
		for( T item : b )
			a.add(item);
	}

```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
		ArrayList<T> ret = new ArrayList<>();
		for( T list : inputs )
			ret.add(list);
		return ret;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
		for( T[] input : inputs )
			for( T element : input )
				ret.add(element);
		return ret;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/util/CollectionUtils.java`
#### Snippet
```java
		Set<T> ret = new HashSet<>();
		for( T element : inputs )
			ret.add(element);
		return ret;
	}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
						//(even if fname is a known literal, the target function might call other functions)
						pb.getProgram().getFunctionProgramBlocks().keySet().stream()
							.forEach(s -> cand.add(s));
					}
				}
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
		this._colGroups = new ArrayList<>(that.getColGroups().size());
		for(AColGroup cg : that._colGroups)
			_colGroups.add(cg);

		overlappingColGroups = that.overlappingColGroups;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodePriorityQue.java`
#### Snippet
```java

		for(CompressedSizeInfoColGroup g : que)
			ret.add(g);

		return ret;
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/EvalNaryCPInstruction.java`
#### Snippet
```java
			HashSet<String> probe = new HashSet<>();
			for( String var : fArgNames )
				probe.add(var);
			for( String var : loNames )
				if( !probe.contains(var) )
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
		List<Pair<FederatedRange, FederatedData>> fedMapping = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> e : workers)
			fedMapping.add(e);
		List<Pair<FederatedData, Future<FederatedResponse>>> idResponses = new ArrayList<>();
		long id = FederationUtils.getNextFedDataID();
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/InitFEDInstruction.java`
#### Snippet
```java
		List<Pair<FederatedRange, FederatedData>> fedMapping = new ArrayList<>();
		for(Pair<FederatedRange, FederatedData> e : workers)
			fedMapping.add(e);
		// we want to wait for the futures with the response containing varIDs and the schemas of the frames
		// on the distributed workers. We need the FederatedData, the starting column of the sub frame (for the schema)
```

### UseBulkOperation
Iteration can be replaced with bulk 'Collection.addAll()' call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CSVReblockSPInstruction.java`
#### Snippet
```java
			naStrings = new HashSet<>();
			for(String s: naS)
				naStrings.add(s);
		}

```

## RuleId[id=StringEquality]
### StringEquality
String values are compared using `!=`, not 'equals()'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixBuiltinGPUInstruction.java`
#### Snippet
```java
		String opcode = getOpcode();
		MatrixObject mat = getMatrixInputForGPUInstruction(ec, _input1.getName());
		if(opcode != "ucumk+*")
			ec.setMetaData(_output.getName(), mat.getNumRows(), mat.getNumColumns());

```

### StringEquality
String values are compared using `!=`, not 'equals()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedResponse.java`
#### Snippet
```java
		}
		String errorMessage = getErrorMessage();
		if (getErrorMessage() != "No readable error message")
			throw new DMLRuntimeException(errorMessage);
		else
```

## RuleId[id=ExternalizableWithoutPublicNoArgConstructor]
### ExternalizableWithoutPublicNoArgConstructor
Externalizable class `DecoderDummycode` has no 'public' no-arg constructor
in `src/main/java/org/apache/sysds/runtime/transform/decode/DecoderDummycode.java`
#### Snippet
```java
 *  
 */
public class DecoderDummycode extends Decoder
{
	private static final long serialVersionUID = 4758831042891032129L;
```

## RuleId[id=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		String val = "";
		String text = varValue;
		if(	(text.startsWith("\"") && text.endsWith("\"")) || (text.startsWith("\'") && text.endsWith("\'"))) {
			if(text.length() > 2) {
				val = extractStringInQuotes(text, true);
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		String val = "";
		String text = varValue;
		if(	(text.startsWith("\"") && text.endsWith("\"")) || (text.startsWith("\'") && text.endsWith("\'"))) {
			if(text.length() > 2) {
				val = extractStringInQuotes(text, true);
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		if(inQuotes) {
			if(	(text.startsWith("\"") && text.endsWith("\"")) ||
				(text.startsWith("\'") && text.endsWith("\'"))) {
				if(text.length() > 2) {
					val = text.substring(1, text.length()-1)
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		if(inQuotes) {
			if(	(text.startsWith("\"") && text.endsWith("\"")) ||
				(text.startsWith("\'") && text.endsWith("\'"))) {
				if(text.length() > 2) {
					val = text.substring(1, text.length()-1)
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
						.replace("\\\"","\"");
				}
				else if(text.equals("\"\"") || text.equals("\'\'")) {
					val = "";
				}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
						.replace("\\\"","\"");
				}
				else if(text.equals("\"\"") || text.equals("\'\'")) {
					val = "";
				}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		catch(Exception e) {
			notifyErrorListeners("cannot parse the int value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		catch(Exception e) {
			notifyErrorListeners("cannot parse the int value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		catch(Exception e) {
			notifyErrorListeners("cannot parse the float value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		catch(Exception e) {
			notifyErrorListeners("cannot parse the float value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	public void exitIterablePredicateSeqExpression(IterablePredicateSeqExpressionContext ctx) {
		if(!ctx.ID().getText().equals("seq")) {
			notifyErrorListeners("incorrect function:\'" + ctx.ID().getText() + "\'. expected \'seq\'", ctx.start);
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	public void exitIterablePredicateSeqExpression(IterablePredicateSeqExpressionContext ctx) {
		if(!ctx.ID().getText().equals("seq")) {
			notifyErrorListeners("incorrect function:\'" + ctx.ID().getText() + "\'. expected \'seq\'", ctx.start);
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	public void exitIterablePredicateSeqExpression(IterablePredicateSeqExpressionContext ctx) {
		if(!ctx.ID().getText().equals("seq")) {
			notifyErrorListeners("incorrect function:\'" + ctx.ID().getText() + "\'. expected \'seq\'", ctx.start);
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	public void exitIterablePredicateSeqExpression(IterablePredicateSeqExpressionContext ctx) {
		if(!ctx.ID().getText().equals("seq")) {
			notifyErrorListeners("incorrect function:\'" + ctx.ID().getText() + "\'. expected \'seq\'", ctx.start);
			return;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		else if(helpBool && valueType.equals("bool")) {
			notifyErrorListeners("invalid valuetype " + valueType + " (Quickfix: use \'boolean\' instead)", start);
			return false;
		}
```

### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		else if(helpBool && valueType.equals("bool")) {
			notifyErrorListeners("invalid valuetype " + valueType + " (Quickfix: use \'boolean\' instead)", start);
			return false;
		}
```

## RuleId[id=ManualArrayCopy]
### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
		_opcode = bifop;
		_args = new Expression[args.length];
		for (int i = 0; i < args.length; i++) {
			_args[i] = args[i];
		}
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
		_opcode = bifop;
		_args = new Expression[args.length];
		for(int i=0; i < args.length; i++) {
			_args[i] = args[i];
		}
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
			// this shallow copy does not only avoid an array copy, but
			// also allows for bi-directional reuses of recodemaps
			for(int j = cl; j <= cu; j++)
				ret._coldata[j - cl] = _coldata[j];
		}
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibCommonsMath.java`
#### Snippet
```java
		double[] check = in.getDenseBlockValues();
		double[] eval = new double[m];
		for(int i = 0; i < m; i++)
			eval[i] = check[i*m+i];
		
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/MapToInt.java`
#### Snippet
```java
	@Override
	public void copyInt(int[] d) {
		for(int i = 0; i < _data.length; i++)
			_data[i] = d[i];
	}
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java
		String[] parts = str.split(Instruction.OPERAND_DELIM, -1);
		String[] ret = new String[parts.length-1];
		for (int i=0; i<parts.length-1; i++) //strip-off the thread count
			ret[i] = parts[i];
		return concatOperands(ret);
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java
		String[] ret = new String[parts.length-1]; // stripping-off the exectype
		ret[0] = parts[1]; // opcode
		for( int i=1; i<parts.length; i++ )
			ret[i-1] = parts[i];
		
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/instructions/fed/TernaryFEDInstruction.java`
#### Snippet
```java
	private static FederatedRequest[] collectRequests(FederatedRequest[] fedRequests, FederatedRequest fedRequest1){
		FederatedRequest[] allRequests = new FederatedRequest[fedRequests.length + 1];
		for ( int i = 0; i < fedRequests.length; i++ )
			allRequests[i] = fedRequests[i];
		allRequests[allRequests.length-1] = fedRequest1;
```

### ManualArrayCopy
Manual array copy
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java
		StructField[] oldSchema = df.schema().fields();
		StructField[] newSchema = new StructField[oldSchema.length + 1];
		for(int i = 0; i < oldSchema.length; i++) {
			newSchema[i] = oldSchema[i];
		}
```

## RuleId[id=LongLiteralsEndingWithLowercaseL]
### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
							String id = INTERAL_FN_INDEX_COL+_idSeqfn.getNextID();
							tmpOut = new LinearFunction(0, 1L, id); 
							_bounds._lower.put(id, 1l);
							_bounds._upper.put(id, _vsParent.getVariable(idat._name).getDim2()); //col dim
							_bounds._increment.put(id, 1L);	
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		public Long call(){
			SortUtils.sortByValue(_start, _end, _values, _indexes);
			return 1l;
		}

```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			int middle = _start + _blockSize;
			// Early return if edge case
			if (middle == _end) return 1l;
			
			// Pointer left side merge.
```

### LongLiteralsEndingWithLowercaseL
'long' literal `1l` ends with lowercase 'l'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			}

			return 1l;
		}
	}
```

### LongLiteralsEndingWithLowercaseL
'long' literal `0l` ends with lowercase 'l'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		}
		else if(getJcudaSparseMatrixPtr() != null && isSparseAndEmpty()) {
			mat.acquireModify(new MatrixBlock((int)mat.getNumRows(), (int)mat.getNumColumns(), 0l)); // empty block
			mat.release();
			return;
```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/api/jmlc/PreparedScript.java`
#### Snippet
```java
	
	@Override
	public Object clone() {
		return clone(true);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/conf/CompilerConfig.java`
#### Snippet
```java
	
	@Override
	public CompilerConfig clone() {
		return new CompilerConfig(this);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
	
	@Override
	public DMLConfig clone() {
		DMLConfig conf = new DMLConfig();
		conf._fileName = _fileName;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/hops/cost/VarStats.java`
#### Snippet
```java
	
	@Override
	public Object clone() {
		return new VarStats(_dc.getRows(), _dc.getCols(),
			_dc.getBlocksize(), _dc.getNonZeros(), _inmem );
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/hops/recompile/RecompileStatus.java`
#### Snippet
```java

	@Override
	public Object clone() {
		RecompileStatus ret = new RecompileStatus(
			_tid, _inplace, _reset, _initialCodegen);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataFormat.java`
#### Snippet
```java
	
	@Override
	public Object clone() {
		if (_dc instanceof MatrixCharacteristics)
			return new MetaDataFormat(new MatrixCharacteristics(_dc), _fmt);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/meta/MetaData.java`
#### Snippet
```java
	
	@Override
	public Object clone() {
		if (_dc instanceof MatrixCharacteristics)
			return new MetaData(new MatrixCharacteristics(_dc));
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/OffsetByte.java`
#### Snippet
```java

		@Override
		public final IterateByteOffsetNoOverHalf clone() {
			return new IterateByteOffsetNoOverHalf(index, offset);
		}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/IdentityDictionarySlice.java`
#### Snippet
```java

	@Override
	public ADictionary clone() {
		return new IdentityDictionarySlice(nRowCol, l, u);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java

	@Override
	public Object clone() {
		GPUObject me = this;
		GPUObject that = new GPUObject(me.gpuContext, me.mat);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/LocalVariableMap.java`
#### Snippet
```java
		
	@Override
	public Object clone() {
		return new LocalVariableMap(this);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/Program.java`
#### Snippet
```java
	
	@Override
	public Object clone() {
		return clone(true);
	}
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java

	@Override
	public FederationMap clone() {
		return copyWithNewID(getID());
	}
```

## RuleId[id=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			
			if( HopRewriteUtils.containsTransposeOperation(X.getParent()) 
				&& !HopRewriteUtils.isValidOp(binary.getOp(), new OpOp2[]{OpOp2.MOMENT, OpOp2.QUANTILE})) 
			{
				//clear existing wiring
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderUDF.java`
#### Snippet
```java
		var fun = new EvalNaryCPInstruction(null, "eval", "",
			new CPOperand("O", ValueType.FP64, DataType.MATRIX),
			new CPOperand[] {
				new CPOperand(_fName, ValueType.STRING, DataType.SCALAR, true),
				new CPOperand("I", ValueType.UNKNOWN, DataType.LIST)});
		fun.processInstruction(ec);

```

## RuleId[id=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/api/mlcontext/MLContext.java`
#### Snippet
```java
				return ExplainType.NONE;
			case HOPS:
				return ExplainType.HOPS;
			case RUNTIME:
				return ExplainType.RUNTIME;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/LiteralOp.java`
#### Snippet
```java
				return value_string;
			case CHARACTER:
				return value_string;
			case UNKNOWN:
				//do nothing (return null)
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
				break;
			}
			case CONV2D_BACKWARD_DATA: {
				setDim1(getDim("N"));
				setDim2(getDim("CHW"));
				setNnz(-1); // cannot infer stats
				break;
			}
			case CONV2D_BACKWARD_FILTER: {
				setDim1(getDim("K"));
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CAST_AS_INT:
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
				case TAN:     costs = 42; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case ACOS:    costs = 103; break;
				case ATAN:    costs = 40; break;
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case ATAN:    costs = 40; break;
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
				case CUMSUM:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
				case CUMSUM:
				case CUMMIN:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CUMMIN:
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
					LOG.warn("Cost model not "
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
						case 3: costs = 31; break; //cm3
						case 4: costs = 51; break; //cm4
						case 5: costs = 16; break; //variance
					}
					break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
						case 3: costs = 32; break; //cm3
						case 4: costs = 52; break; //cm4
						case 5: costs = 17; break; //variance
					}
					break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
					break;
				case DIV:
					ret = 1.0; // worst case estimate
					break;
				case LESS:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case EQUAL:
				case NOTEQUAL:
					ret = 1.0; // purely data-dependent operations, and hence worse-case estimate
					break;
				//MIN, MAX, AND, OR, LOG, POW
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
							case 3: xcm=31; break; //cm3
							case 4: xcm=51; break; //cm4
							case 5: xcm=16; break; //variance
						}
						return (leftSparse) ? xcm * (d1m * d1s + 1) : xcm * d1m;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
							case 4: xga=31; break; //cm3
							case 5: xga=51; break; //cm4
							case 6: xga=16; break; //variance
						}						
						return 2 * d1m + xga * d1m; //scan for min/max, groupedagg
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
				return false;
			case PMM:
				return false;
			case MM:
				return true;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CAST_AS_INT:
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
				case TAN:     costs = 42; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case ACOS:    costs = 103; break;
				case ATAN:    costs = 40; break;
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case ATAN:    costs = 40; break;
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
				case CUMSUM:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case SINH:    costs = 93; break; // TODO:
				case COSH:    costs = 103; break;
				case TANH:    costs = 40; break;
				case CUMSUM:
				case CUMMIN:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CUMMIN:
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
					LOG.warn("Cost model not "
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
						case 3: costs = 31; break; //cm3
						case 4: costs = 51; break; //cm4
						case 5: costs = 16; break; //variance
					}
					break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
						case 3: costs = 32; break; //cm3
						case 4: costs = 52; break; //cm4
						case 5: costs = 17; break; //variance
					}
					break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		case FRAME_ROW_REPLICATE:
		case APPLY_SCHEMA:
			currBuiltinOp = new BinaryOp(target.getName(), target.getDataType(),
				target.getValueType(), OpOp2.valueOf(source.getOpCode().name()), expr, expr2);
			break;
		case MAP:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		case DETECTSCHEMA:
		case COLNAMES:
			currBuiltinOp = new UnaryOp(target.getName(), target.getDataType(),
				target.getValueType(), OpOp1.valueOf(source.getOpCode().name()), expr);
			break;
			
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/Utils.java`
#### Snippet
```java
			case 6:
			case 7:
				return readArbitraryLengthBytesAsUnsignedLong(buffer, length);
			case 8:
				long value = buffer.getLong();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/Utils.java`
#### Snippet
```java
			case 6:
			case 7:
				return readArbitraryLengthBytesAsUnsignedInt(buffer, length);
			case 8:
				// Throws if the long can't be converted safely
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/message/H5Message.java`
#### Snippet
```java
			case H5Constants.OBJECT_MODIFICATION_TIME_MESSAGE:
				// Data Size
				bb.writeShort((short) 8);
				break;
			case H5Constants.DATA_LAYOUT_MESSAGE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/io/hdf5/message/H5Message.java`
#### Snippet
```java
			case H5Constants.DATA_LAYOUT_MESSAGE:
				// Data Size
				bb.writeShort((short) 24);
				break;
			default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/Array.java`
#### Snippet
```java
				return changeTypeLong();
			case STRING:
				return changeTypeString();
			case CHARACTER:
				return changeTypeCharacter();
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
		switch(a.getValueType()) {
			case BOOLEAN:
				return new OptionalArray<Boolean>((Array<Boolean>) a, n);
			case FP32:
				return new OptionalArray<Float>((Array<Float>) a, n);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
				return new OptionalArray<Boolean>((Array<Boolean>) a, n);
			case FP32:
				return new OptionalArray<Float>((Array<Float>) a, n);
			case FP64:
				return new OptionalArray<Double>((Array<Double>) a, n);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
				return new OptionalArray<Float>((Array<Float>) a, n);
			case FP64:
				return new OptionalArray<Double>((Array<Double>) a, n);
			case UINT8:
			case INT32:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
			case UINT8:
			case INT32:
				return new OptionalArray<Integer>((Array<Integer>) a, n);
			case INT64:
				return new OptionalArray<Long>((Array<Long>) a, n);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
				return new OptionalArray<Integer>((Array<Integer>) a, n);
			case INT64:
				return new OptionalArray<Long>((Array<Long>) a, n);
			case CHARACTER:
			default:
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				return DDCScheme.create(columns);
			case DDCFOR:
				break;
			case DeltaDDC:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case DeltaDDC:
				break;
			case EMPTY:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case EMPTY:
				break;
			case LinearFunctional:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case LinearFunctional:
				break;
			case OLE:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case OLE:
				break;
			case RLE:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case RLE:
				break;
			case SDC:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case SDC:
				break;
			case SDCFOR:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case SDCFOR:
				break;
			case UNCOMPRESSED:
				break;
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/SchemeFactory.java`
#### Snippet
```java
				break;
			case UNCOMPRESSED:
				break;
			default:
				break;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java

		case Read:
			in1 = new CPOperand(parts[1]);
			in2 = new CPOperand(parts[2]);
			break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						break;
					case MIN:
						val = kv.getValue();
						break;
					case MAX:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						break;
					case MAX:
						val = kv.getValue();
						break;
					case CM2:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						break;
					case CM3:
						val = kv.getValue() / kv.getWeight();
						break;
					case CM4:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						break;
					case CM4:
						val = kv.getValue() / kv.getWeight();
						break;
					case VARIANCE:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						break;
					case VARIANCE:
						val = kv.getValue() / kv.getWeight();
						break;
					default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
			switch( _dpf ) {
				case ROW_WISE: return mc.getCols();
				case ROW_BLOCK_WISE: return mc.getCols();
				case ROW_BLOCK_WISE_N: return mc.getCols();
				case COLUMN_WISE: return 1;
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
				case ROW_WISE: return mc.getCols();
				case ROW_BLOCK_WISE: return mc.getCols();
				case ROW_BLOCK_WISE_N: return mc.getCols();
				case COLUMN_WISE: return 1;
				case COLUMN_BLOCK_WISE: return mc.getBlocksize();
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
				case ROW_BLOCK_WISE_N: return _N;
				case COLUMN_WISE: return mc.getRows();
				case COLUMN_BLOCK_WISE: return mc.getRows();
				case COLUMN_BLOCK_WISE_N: return mc.getRows();
				default:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
				case COLUMN_WISE: return mc.getRows();
				case COLUMN_BLOCK_WISE: return mc.getRows();
				case COLUMN_BLOCK_WISE_N: return mc.getRows();
				default:
					throw new RuntimeException("Unsupported partition format: "+_dpf);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlockFactory.java`
#### Snippet
```java
		switch (code) {
			case 0: return new ArrayList<Pair<MatrixIndexes, MatrixBlock>>();
			case 1: return new ArrayList<Pair<Long, FrameBlock>>();
			case 2: return new ArrayList<Pair<TensorIndexes, TensorBlock>>();
		}
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheBlockFactory.java`
#### Snippet
```java
			case 0: return new ArrayList<Pair<MatrixIndexes, MatrixBlock>>();
			case 1: return new ArrayList<Pair<Long, FrameBlock>>();
			case 2: return new ArrayList<Pair<TensorIndexes, TensorBlock>>();
		}
		throw new RuntimeException("Unsupported cache block type: "+code);
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
				// TODO: create execution mode for parfor loop
				case FED:
					node.setExecType(ExecType.CP); break;
				default:
					throw new DMLRuntimeException("Unsupported optnode exec type: "+et);
```

### DuplicateBranchesInSwitch
Branch in 'switch' is a duplicate of the default branch
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		{
			case NAIVE:
			case FIXED:            W = N; break; 
			case STATIC:           W = N / k; break;
			case FACTORING:
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
			
			case ISNA: return Double.isNaN(in) ? 1 : 0;
			case ISNAN: return Double.isNaN(in) ? 1 : 0;
			case ISINF: return Double.isInfinite(in) ? 1 : 0;
			
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
			return null;
		case PRINTF:
			if (!DMLScript.suppressPrint2Stdout())
				System.out.println(in1);
			return null;
		case STOP:
			throw new DMLScriptException(in1);
```

## RuleId[id=NonFinalFieldInEnum]
### NonFinalFieldInEnum
Non-final field `_id` in enum 'BuiltinConstant'
in `src/main/java/org/apache/sysds/parser/BuiltinConstant.java`
#### Snippet
```java
	pi(Math.PI);
	
	private DoubleIdentifier _id;

	private BuiltinConstant(double d) {
```

## RuleId[id=RegExpSimplifiable]
### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static String[] getSplittedStringAsArray (String input) {
		//Frame f = new Frame();
		String[] string_array = input.split("'[ ]*,[ ]*'");
		return string_array;//.subList(0,2);
	}
```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static String[] getSplittedStringAsArray (String input) {
		//Frame f = new Frame();
		String[] string_array = input.split("'[ ]*,[ ]*'");
		return string_array;//.subList(0,2);
	}
```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		  Pattern p = Pattern.compile(", None,");
		  Matcher m = p.matcher(sb);
		  string_array = m.replaceAll(", 'None',").split("'[ ]*,[ ]*'");
		
		  // remove apostrophe in first and last string element
```

### RegExpSimplifiable
`[ ]` can be simplified to ' '
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		  Pattern p = Pattern.compile(", None,");
		  Matcher m = p.matcher(sb);
		  string_array = m.replaceAll(", 'None',").split("'[ ]*,[ ]*'");
		
		  // remove apostrophe in first and last string element
```

### RegExpSimplifiable
`[/]` can be simplified to '/'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/FederatedMonitoringServerHandler.java`
#### Snippet
```java

	private Long parseId(final String uri) {
		final Pattern pattern = Pattern.compile("^[/][a-z]+[/]");
		final Matcher matcher = pattern.matcher(uri);

```

### RegExpSimplifiable
`[/]` can be simplified to '/'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/monitoring/FederatedMonitoringServerHandler.java`
#### Snippet
```java

	private Long parseId(final String uri) {
		final Pattern pattern = Pattern.compile("^[/][a-z]+[/]");
		final Matcher matcher = pattern.matcher(uri);

```

## RuleId[id=DuplicateExpressions]
### DuplicateExpressions
Multiple occurrences of `(leftSparse) ? d1m * d1n * d1s : d1m * d1n`
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
				case Append: //opcodes: append
					return DEFAULT_NFLOP_CP * 
					       (((leftSparse) ? d1m * d1n * d1s : d1m * d1n ) +
					        ((rightSparse) ? d2m * d2n * d2s : d2m * d2n ));
				
```

### DuplicateExpressions
Multiple occurrences of `(rightSparse) ? d2m * d2n * d2s : d2m * d2n`
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
					return DEFAULT_NFLOP_CP * 
					       (((leftSparse) ? d1m * d1n * d1s : d1m * d1n ) +
					        ((rightSparse) ? d2m * d2n * d2s : d2m * d2n ));
				
				case Variable: //opcodes: assignvar, cpvar, rmvar, rmfilevar, assignvarwithfile, attachfiletovar, valuepick, iqsize, read, write, createvar, setfilename, castAsMatrix
```

### DuplicateExpressions
Multiple occurrences of `(leftSparse)? d1m*d1n*d1s : d1m*d1n`
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
				case MatrixIndexing: //opcodes: rightIndex, leftIndex
					if( optype.equals(LeftIndex.OPCODE) ){
						return DEFAULT_NFLOP_CP * ((leftSparse)? d1m*d1n*d1s : d1m*d1n)
						       + 2 * DEFAULT_NFLOP_CP * ((rightSparse)? d2m*d2n*d2s : d2m*d2n );
					}
```

### DuplicateExpressions
Multiple occurrences of `(rightSparse)? d2m*d2n*d2s : d2m*d2n`
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
					if( optype.equals(LeftIndex.OPCODE) ){
						return DEFAULT_NFLOP_CP * ((leftSparse)? d1m*d1n*d1s : d1m*d1n)
						       + 2 * DEFAULT_NFLOP_CP * ((rightSparse)? d2m*d2n*d2s : d2m*d2n );
					}
					else if( optype.equals(RightIndex.OPCODE) ){
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(arg.substring(0,arg.length()-1))`
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
	public static long parseMemorySize(String arg) {
		if ( arg.endsWith("g") || arg.endsWith("G") )
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024 * 1024 * 1024;
		else if ( arg.endsWith("m") || arg.endsWith("M") )
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024 * 1024;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(arg.substring(0,arg.length()-1))`
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024 * 1024 * 1024;
		else if ( arg.endsWith("m") || arg.endsWith("M") )
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024 * 1024;
		else if( arg.endsWith("k") || arg.endsWith("K") )
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024;
```

### DuplicateExpressions
Multiple occurrences of `Long.parseLong(arg.substring(0,arg.length()-1))`
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024 * 1024;
		else if( arg.endsWith("k") || arg.endsWith("K") )
			return Long.parseLong(arg.substring(0,arg.length()-1)) * 1024;
		else 
			return Long.parseLong(arg.substring(0,arg.length()));
```

### DuplicateExpressions
Multiple occurrences of `Math.min((i+1)*blklen, parnb)`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
			for( int i=0; i<k & i*blklen<parnb; i++ ) {
				int rl = parcol ? 0 : i*blklen; 
				int ru = parcol ? nrb : Math.min((i+1)*blklen, parnb);
				int cl = parcol ? i*blklen : 0; 
				int cu = parcol ? Math.min((i+1)*blklen, parnb) : ncb;
```

### DuplicateExpressions
Multiple occurrences of `Math.min((i+1)*blklen, parnb)`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
				int ru = parcol ? nrb : Math.min((i+1)*blklen, parnb);
				int cl = parcol ? i*blklen : 0; 
				int cu = parcol ? Math.min((i+1)*blklen, parnb) : ncb;
				long[] lseeds = sliceSeedsForCP(seeds, rl, ru, cl, cu, nrb, ncb);
				tasks.add(new RandTask(rl, ru, cl, cu, out, rgen, bSeed, lseeds) );
```

### DuplicateExpressions
Multiple occurrences of `Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1)`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			
			//for load balancing: degree of parallelism such that ~128MB per partition
			int numPartitions = (int) Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1);
			
			//create offset rdd
```

### DuplicateExpressions
Multiple occurrences of `Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1)`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			
			//for load balancing: degree of parallelism such that ~128MB per partition
			int numPartitions = (int) Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1);
			
			//create seeds rdd 
```

### DuplicateExpressions
Multiple occurrences of `Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1)`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

			//for load balancing: degree of parallelism such that ~128MB per partition
			int numPartitions = (int) Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1);

			//create seeds rdd
```

### DuplicateExpressions
Multiple occurrences of `Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1)`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

			//for load balancing: degree of parallelism such that ~128MB per partition
			int numPartitions = (int) Math.max(Math.min(totalSize/hdfsBlkSize, numBlocks), 1);

			//create seeds rdd
```

### DuplicateExpressions
Multiple occurrences of `Math.log(in1)/Math.log(in2)`
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
			case LOG:
				//faster in Math
				return Math.log(in1)/Math.log(in2);
			case LOG_NZ:
				//faster in Math
```

### DuplicateExpressions
Multiple occurrences of `Math.log(in1)/Math.log(in2)`
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
			case LOG_NZ:
				//faster in Math
				return (in1==0) ? 0 : Math.log(in1)/Math.log(in2);
	
			default:
```

### DuplicateExpressions
Multiple occurrences of `Math.log(in1)/Math.log(in2)`
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
				// *** END HACK ***
			case LOG://faster in Math
				return (Math.log(in1)/Math.log(in2)); 
			case LOG_NZ: //faster in Math
				return (in1==0) ? 0 : (Math.log(in1)/Math.log(in2));
```

### DuplicateExpressions
Multiple occurrences of `Math.log(in1)/Math.log(in2)`
in `src/main/java/org/apache/sysds/runtime/functionobjects/Builtin.java`
#### Snippet
```java
				return (Math.log(in1)/Math.log(in2)); 
			case LOG_NZ: //faster in Math
				return (in1==0) ? 0 : (Math.log(in1)/Math.log(in2));
			default:
				throw new DMLRuntimeException("Builtin.execute(): Unknown operation: " + bFunc);
```

## RuleId[id=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	 */
	public String info() {
		StringBuilder sb = new StringBuilder();

		sb.append(MLContextUtil.displayInputs("Inputs", inputs, symbolTable));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();

		sb.append(MLContextUtil.displayInputs("Inputs", inputs, symbolTable));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
	{
		//create dml-script-specific suffix
		StringBuilder sb = new StringBuilder();
		sb.append(Lop.FILE_SEPARATOR);
		sb.append(Lop.PROCESS_PREFIX);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	 */
	public static String displaySymbolTable(String name, LocalVariableMap symbolTable) {
		StringBuilder sb = new StringBuilder();
		sb.append(name);
		sb.append(":\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
	 */
	public static String displayOutputs(String name, Set<String> outputNames, LocalVariableMap symbolTable) {
		StringBuilder sb = new StringBuilder();
		sb.append(name);
		sb.append(":\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/api/mlcontext/MLResults.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(MLContextUtil.displayOutputs(script.getOutputVariables(), symbolTable));
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/cost/VarStats.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("VarStats: [");
		sb.append("rlen = ");
```

### StringBufferReplaceableByString
`StringBuilder builder` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/cost/FederatedCost.java`
#### Snippet
```java
	@Override
	public String toString(){
		StringBuilder builder = new StringBuilder();
		builder.append(" computeCost: ");
		builder.append(_computeCost);
```

### StringBufferReplaceableByString
`StringBuilder strB` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/cost/HopRel.java`
#### Snippet
```java
	@Override
	public String toString(){
		StringBuilder strB = new StringBuilder();
		strB.append(this.getClass().getSimpleName());
		strB.append(" {HopID: ");
```

### StringBufferReplaceableByString
`StringBuilder costInfo` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/cost/FederatedCostEstimator.java`
#### Snippet
```java
	private static String getCostInfo(Hop root){
		String sep = System.getProperty("line.separator");
		StringBuilder costInfo = new StringBuilder();
		costInfo
			.append(root).append(sep)
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeRow.java`
#### Snippet
```java
	@Override
	public String getTemplateInfo() {
		StringBuilder sb = new StringBuilder();
		sb.append("SPOOF ROWAGGREGATE [type=");
		sb.append(_type.name());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
	@Override
	public String getTemplateInfo() {
		StringBuilder sb = new StringBuilder();
		sb.append("SPOOF CELLWISE [type=");
		sb.append(_type.name());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeOuterProduct.java`
#### Snippet
```java
	@Override
	public String getTemplateInfo() {
		StringBuilder sb = new StringBuilder();
		sb.append("SPOOF OUTERPRODUCT [type=");
		sb.append(_type.name());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeMultiAgg.java`
#### Snippet
```java
	@Override
	public String getTemplateInfo() {
		StringBuilder sb = new StringBuilder();
		sb.append("SPOOF MULTIAGG [aggOps=");
		sb.append(Arrays.toString(_aggOps.toArray(new AggOp[0])));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append("Compressed ProbeStatus : hopID =" + startHopID);
			sb.append("\n CLA Ops         : " + numberCompressedOpsExecuted);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/hops/fedplanner/FederatedCompilationTimer.java`
#### Snippet
```java
			long privPropagationTime = privProcessTime.getDuration()-totalFetchTime;
			long basicCompileTime = getBasicCompileTime();
			StringBuilder sb = new StringBuilder();
			sb.append("Basic Compilation Time:\t\t").append(nanoToSeconds(basicCompileTime)).append("\n");
			sb.append("Total Privacy Fetch Time:\t").append(nanoToSeconds(totalFetchTime)).append("\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("rows=" + getNumRows() + Lop.VALUETYPE_PREFIX);
		sb.append("cols=" + getNumCols() + Lop.VALUETYPE_PREFIX);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/Federated.java`
#### Snippet
```java
	@Override
	public String getInstructions(String type, String addresses, String ranges, String output) {
		StringBuilder sb = new StringBuilder("FED");
		sb.append(OPERAND_DELIMITOR);
		sb.append("fedinit");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(_op.toString());
		sb.append(" ; num_rows=" + getOutputParameters().getNumRows());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	 */
	private String prepOperand(String label) {
		StringBuilder sb = new StringBuilder("");
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
		boolean isLiteral = (isData && ((Data)this).isLiteral());
		
		StringBuilder sb = new StringBuilder("");
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	 */
	public String prepOperand(String label, DataType dt, ValueType vt, boolean literal) {
		StringBuilder sb = new StringBuilder();
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/Lop.java`
#### Snippet
```java
	 */
	public String prepOperand(String label, DataType dt, ValueType vt) {
		StringBuilder sb = new StringBuilder();
		sb.append(label);
		sb.append(Lop.DATATYPE_PREFIX);
```

### StringBufferReplaceableByString
`StringBuffer sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
	 */
	public static String getStringForCudaTimers() {
		StringBuffer sb = new StringBuffer();
		sb.append("CUDA/CuLibraries init time:\t" + String.format("%.3f", cudaInitTime*1e-9) + "/"
				+ String.format("%.3f", cudaLibrariesInitTime*1e-9) + " sec.\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/stats/RecompileStatistics.java`
#### Snippet
```java

	public static String displayStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append("HOP DAGs recompiled (PRED, SB):\t" + getRecompiledPredDAGs() + "/" + getRecompiledSBDAGs() + ".\n");
		sb.append("HOP DAGs recompile time:\t" + String.format("%.3f", ((double)getRecompileTime())/1000000000) + " sec.\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/stats/ParamServStatistics.java`
#### Snippet
```java

	public static String displayFloStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("PS fed network time (cum):\t%.3f secs.\n", fedNetworkTime.doubleValue() / 1000));
		sb.append(String.format("PS fed agg time:\t%.3f secs.\n", fedAggregation.doubleValue() / 1000));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/stats/ParamServStatistics.java`
#### Snippet
```java

	private static String displayFedPSStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("PS fed data partitioning time:\t%.3f secs.\n", fedDataPartitioningTime.doubleValue() / 1000));
		sb.append(String.format("PS fed comm time (cum):\t\t%.3f secs.\n", fedCommunicationTime.doubleValue() / 1000));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
	public static String displayStatistics() {
		if( optCount.longValue() > 0 ){
			StringBuilder sb = new StringBuilder();
			sb.append("ParFor loops optimized:\t\t" + getOptCount() + ".\n");
			sb.append("ParFor optimize time:\t\t" + String.format("%.3f", ((double)getOptTime())/1000) + " sec.\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java

	public static String displayStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append("Codegen compile (DAG,CP,JC):\t" + getDAGCompile() + "/"
				+ getCPlanCompile() + "/" + getClassCompile() + ".\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/parser/PathStatement.java`
#### Snippet
```java
	@Override
	public String toString(){
		 StringBuilder sb = new StringBuilder();
		 sb.append(Statement.SETWD + "(");
		 sb.append(_pathValue);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/common/Builtins.java`
#### Snippet
```java

	public static String getFilePath(String name) {
		StringBuilder sb = new StringBuilder();
		sb.append(BUILTIN_DIR);
		sb.append(name);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
	
	private static String prepareAssignVarInstruction(Lop input, Lop node) {
		StringBuilder sb = new StringBuilder();
		
		sb.append(ExecType.CP);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

	private static String explainInstructions( Instruction inst, int level ) {
		StringBuilder sb = new StringBuilder();
		sb.append( createOffset(level) );
		sb.append( explainGenericInstruction(inst, level) );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/parser/MultiAssignmentStatement.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(Arrays.toString(_targetList.toArray()));
		sb.append(" = ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesHDF5.java`
#### Snippet
```java

	@Override public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(" datasetName " + datasetName);
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		Expression ex2b = idat.getColUpperBound();
		
		StringBuilder sb = new StringBuilder();
		sb.append(String.valueOf(ex1a));
		sb.append(',');
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java

		// write out the header first 
		StringBuilder  sb = new StringBuilder();
		sb.append ("%%MatrixMarket matrix coordinate real general\n");

```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(" delim " + delim);
		sb.append(" indexDelim " + indexDelim);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
	@Override
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append("header " +header);
		sb.append(" delim " + delim);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/io/ReaderWriterFederated.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append(_dataType);
			sb.append(" ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		if( _workingDir == null )
			createWorkingDirectory();
		StringBuilder sb = new StringBuilder();
		sb.append( _workingDir );
		sb.append( Lop.FILE_SEPARATOR );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		if( _workingDir == null )
			createWorkingDirectory();
		StringBuilder sb = new StringBuilder();
		sb.append( _workingDir );
		sb.append( Lop.FILE_SEPARATOR );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		
		//create process specific sub tmp dir
		StringBuilder sb = new StringBuilder();
		sb.append( dirRoot );
		sb.append(Lop.FILE_SEPARATOR);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		Program prog = pbs.get( 0 ).getProgram();
		
		StringBuilder sb = new StringBuilder();
		sb.append( PARFORBODY_BEGIN );
		sb.append( NEWLINE );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java

	private static String serializeVariables (LocalVariableMap vars) {
		StringBuilder sb = new StringBuilder();
		sb.append(VARS_BEGIN);
		sb.append( vars.serialize() );
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
	private static String serializeDataIdentifier( DataIdentifier dat ) {
		// SCHEMA: <name>|<datatype>|<valuetype>
		StringBuilder sb = new StringBuilder();
		sb.append(dat.getName());
		sb.append(DATA_FIELD_DELIM);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder("PRNG[");
		sb.append("pdf = "+_pdf.name()+", ");
		sb.append("rows = "+_rows+", ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/matrix/operators/AggregateUnaryOperator.java`
#### Snippet
```java
	@Override
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append("(");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/matrix/operators/AggregateOperator.java`
#### Snippet
```java
	@Override
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("(");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
	
	public static LineageItem createScalarLineageItem(LiteralOp lop) {
		StringBuilder sb = new StringBuilder(lop.getName());
		sb.append(Instruction.VALUETYPE_PREFIX);
		sb.append(lop.getDataType().toString());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
	public static String displaySize() {
		//size of all cached reusable intermediates/size of reused intermediates/cache size
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)LineageEstimator._totReusableSize)/(1024*1024))); //in MB
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
	
	public static String displayLineageEstimates() {
		StringBuilder sb = new StringBuilder();
		sb.append("Compute Time (Elapsed/Saved): \t" + displayComputeTime() + " sec.\n");
		sb.append("Space Used (C/R/L): \t\t" + displaySize() + " MB.\n"); // total cached/reused/cache limit
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
	
	public static String displayComputeTime() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)Statistics.getRunTime())*1e-9)); //in sec
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayMultiLevelHits() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsInst.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayHits() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsMem.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayWtrites() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numWritesMem.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displaySparkHits() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsSparkActions.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayRewrites() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numRewrites.longValue());
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java
	
	public static String displayFSTime() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)_ctimeFSRead.longValue())/1000000000)); //in sec
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayGpuEvictTime() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)_evtimeGpu.longValue())/1000000000)); //in sec
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayGpuStats() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsGpu.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java
	}
	public static String displayComputeTime() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)_ctimeSaved.longValue())/1000000000)); //in sec
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displaySparkPersist() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numRddPersist.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheStatistics.java`
#### Snippet
```java

	public static String displayGpuPointerStats() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numRecycleGpu.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/cost/ComputationCostEstimator.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(" : ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("CostVector: ");
		sb.append(counter);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfoColGroup.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("cols: " + _cols);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EmptyEncoding.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/ConstEncoding.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/ComEstSample.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(" sampleSize: ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/DenseEncoding.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append("[" + key + ", ");
			sb.append(value + ", ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/SparseEncoding.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s%5d", "Pointers:", this._ptr.length));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDCFOR.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s ", "Data: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupLinearFunctional.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", " Intercepts: " + Arrays.toString(getIntercepts())));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingle.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Default: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDC.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Default: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroupValue.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s%s", "Values: ", _dict.getClass().getSimpleName()));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%15s", "Type: "));
		sb.append(this.getClass().getSimpleName());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java

	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Indexes: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AIterator.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append(" v:" + value() + " d:" + getDataIndex() + " o:" + getOffsetsIndex());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupConst.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Values: " + _dict.getClass().getSimpleName()));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCFOR.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Indexes: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupDDC.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Data: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/scheme/DDCScheme.java`
#### Snippet
```java
	@Override
	public final String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("\nCols: ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/SingleIndex.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("[");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append("sliceInfo: ");
			sb.append(lIndex);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append(String.format("\n%15s", "Indexes: "));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/TwoIndex.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("[");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/IColIndex.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder(50);
			sb.append("SliceResult:[");
			sb.append(idStart);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/ArrayIndex.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append(Arrays.toString(cols));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/indexes/RangeIndex.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("[");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/compress/workload/AWTreeNode.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("\n---------------------------Workload Tree:---------------------------------------\n");
		sb.append(this.explain(0));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderFeatureHash.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderPassThrough.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderRecode.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderBin.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(getClass().getSimpleName());
		sb.append(": ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AppendFEDInstruction.java`
#### Snippet
```java
		// check input dimensions
		if(_cbind && mo1.getNumRows() != mo2.getNumRows()) {
			StringBuilder sb = new StringBuilder();
			sb.append("Append-cbind is not possible for federated input matrices ");
			sb.append(input1.getName()).append(" and ").append(input2.getName());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AppendFEDInstruction.java`
#### Snippet
```java
		}
		else if(!_cbind && mo1.getNumColumns() != mo2.getNumColumns()) {
			StringBuilder sb = new StringBuilder();
			sb.append("Append-rbind is not possible for federated input matrices ");
			sb.append(input1.getName()).append(" and ").append(input2.getName());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			int iPos2 = StringUtils.indexOf(instString, Lop.OPERAND_DELIMITOR, iPos+1);

			StringBuilder sb = new StringBuilder();
			sb.append(instString.substring(0,iPos+1)); // It takes first part before file name.
			// This will replace 'pattern' with 'replace' string from file name.
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CompressionSPInstruction.java`
#### Snippet
```java
					sparkSizeIn = info.memSize();
			}
			StringBuilder sb = new StringBuilder();
			sb.append("Spark Compression Instruction sizes:");
			sb.append(String.format("\nSBCompress: InSize:       %16d", sparkSizeIn));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTree.java`
#### Snippet
```java
	public String explain( boolean withDetails )
	{
		StringBuilder sb = new StringBuilder();
		sb.append("\n");
		sb.append("----------------------------\n");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		fname = fname.replaceAll("%VAR%", String.valueOf(_resultVarsIDSeq.getNextID()));
		
		StringBuilder sb = new StringBuilder();
		sb.append(scratchSpaceLoc);
		sb.append(Lop.FILE_SEPARATOR);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
		fname = fname.replaceAll("%VAR%", String.valueOf(_dpVarsIDSeq.getNextID()));
		
		StringBuilder sb = new StringBuilder();
		sb.append(scratchSpaceLoc);
		sb.append(Lop.FILE_SEPARATOR);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
	private String constructResultFileName() {
		String scratchSpaceLoc = ConfigurationManager.getScratchSpace();
		StringBuilder sb = new StringBuilder();
		sb.append(scratchSpaceLoc);
		sb.append(Lop.FILE_SEPARATOR);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/ProgramRecompiler.java`
#### Snippet
```java
	protected static ArrayList<Instruction> createNestedParallelismToInstructionSet(String iterVar, String offset) {
		//create instruction string
		StringBuilder sb = new StringBuilder("CP"+Lop.OPERAND_DELIMITOR+"+"+Lop.OPERAND_DELIMITOR);
		sb.append(iterVar);
		sb.append(Lop.DATATYPE_PREFIX+"SCALAR"+Lop.VALUETYPE_PREFIX+"INT"+Lop.OPERAND_DELIMITOR);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheStatistics.java`
#### Snippet
```java
	
	public static String displayTime() {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("%.3f", ((double)_ctimeAcquireR.longValue())/1000000000)); //in sec
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheStatistics.java`
#### Snippet
```java
	
	public static String displayHits() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numHitsMem.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheStatistics.java`
#### Snippet
```java
	
	public static String displayWrites() {
		StringBuilder sb = new StringBuilder();
		sb.append(_numWritesLin.longValue());
		sb.append("/");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/ExecutionContextMap.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(super.toString());
		sb.append("\nMain EC: ");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLookupTable.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append(_pid);
			sb.append("@");
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	protected String getCacheFilePathAndName () {
		if( _cacheFileName==null ) {
			StringBuilder sb = new StringBuilder();
			sb.append(CacheableData.cacheEvictionLocalFilePath);
			sb.append(CacheableData.cacheEvictionLocalFilePrefix);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("Fed Map: " + _type);
		sb.append("\t ID:" + _ID);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		sb.append(" " + _dataType);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

	private static String displayGCStats(GCStatsCollection gcsc) {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("Total JVM GC count:\t\t%d.\n", gcsc.gcCount));
		sb.append(String.format("Total JVM GC time:\t\t%.3f sec.\n", gcsc.gcTime));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

	public static String displayStatistics(FedStatsCollection fedStats, int numHeavyHitters) {
		StringBuilder sb = new StringBuilder();
		sb.append("SystemDS Federated Statistics:\n");
		sb.append(displayCacheStats(fedStats.cacheStats));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

	private static String displayLinCacheStats(LineageCacheStatsCollection lcsc) {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("LinCache hits (Mem/FS/Del):\t%d/%d/%d.\n",
			lcsc.numHitsMem, lcsc.numHitsFS, lcsc.numHitsDel));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

	private static String displayCacheStats(CacheStatsCollection csc) {
		StringBuilder sb = new StringBuilder();
		sb.append(String.format("Cache hits (Mem/Li/WB/FS/HDFS):\t%d/%d/%d/%d/%d.\n",
			csc.memHits, csc.linHits, csc.fsBuffHits, csc.fsHits, csc.hdfsHits));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
	public static String displayFedWorkerStats() {
		if( readCount.longValue() > 0){ 
			StringBuilder sb = new StringBuilder();
			sb.append(displayFedLookupTableStats());
			sb.append(displayFedReuseReadStats());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java

	private static String displayMultiTenantStats(MultiTenantStatsCollection mtsc) {
		StringBuilder sb = new StringBuilder();
		sb.append(displayFedLookupTableStats(mtsc.fLTGetCount, mtsc.fLTEntryCount, mtsc.fLTGetTime));
		sb.append(displayFedReuseReadStats(mtsc.reuseReadHits, mtsc.reuseReadBytes));
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		@Override
		public String toString() {
			StringBuilder sb = new StringBuilder("SparkClusterConfig: \n");
			sb.append("-- legacyVersion    = " + _legacyVersion + " ("+getSparkVersionString()+")\n" );
			sb.append("-- confOnly         = " + _confOnly + "\n");
```

## RuleId[id=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		
		if(parentOp == null)
			return;
		else if(parentOp.getOp() == OpOpDnn.MAX_POOL || parentOp.getOp() == OpOpDnn.AVG_POOL) {
			DnnParameters parentParam = parentOp.parseInput();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMatrixMultChainOptimization.java`
#### Snippet
```java
		//core mmchain optimization (potentially overridden)
		if( mmChain.size() == 2 ) 
			return; //nothing to optimize
		else
			optimizeMMChain(hop, mmChain, mmOperators, state);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java
			   ||expr1.getOutput().getDim2() == -1 || expr2.getOutput().getDim2() == -1 ) 
			{
				return;
			}
			else if( (!allowsMV && expr1.getOutput().getDim1() != expr2.getOutput().getDim1())
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			   ||expr1.getOutput().getDim2() == -1 || expr2.getOutput().getDim2() == -1 ) 
			{
				return;
			}
			else if( (!allowsMV && expr1.getOutput().getDim1() != expr2.getOutput().getDim1())
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		catch(Exception e) {
			notifyErrorListeners("cannot parse the int value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			} catch (LanguageException e) {
				notifyErrorListeners("invalid assignment for ifdef function", ctx.targetList.start);
				return;
			}

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		else {
			notifyErrorListeners("incorrect lvalue in ifdef function ", ctx.targetList.start);
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			// TODO: extract more meaningful info from this exception.
			notifyErrorListeners("invalid function call", ctx.start);
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		catch(Exception e) {
			notifyErrorListeners("cannot parse the float value: \'" +  ctx.getText() + "\'", ctx.getStart());
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		catch(Exception e) {
			notifyErrorListeners("cannot set the indices", ctx.start);
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				// TODO: extract more meaningful info from this exception.
				notifyErrorListeners("invalid assignment: " + e.getMessage(), lhsStart);
				return;
			}
		}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		else {
			notifyErrorListeners("incorrect lvalue in assignment statement", lhsStart);
			return;
		}
	}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		if (numParams == 0) {
			notifyErrorListeners(functionName + "() must have more than 0 parameters", ctx.start);
			return;
		} else if (numParams == 1) {
			Expression expr = paramExpression.get(0).getExpr();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			} catch (LanguageException e) {
				notifyErrorListeners("cannot process " + functionName + "() function", ctx.start);
				return;
			}
		} else if (numParams > 1) {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			} catch (LanguageException e) {
				notifyErrorListeners("cannot process " + functionName + "() function", ctx.start);
				return;
			}
		}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
					appendRow(tmp);
			}
			return;
		}
		else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupBlock.java`
#### Snippet
```java
			numPathsInIfPB((IfProgramBlock)pb, ec, paths);
		else if (pb instanceof BasicProgramBlock)
			return;
		else
			throw new DMLRuntimeException("Only BasicProgramBlocks or "
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheEviction.java`
#### Snippet
```java
			}
			// Keep them in cache.
			return;
		}
		// All are set to be deleted.
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupUtils.java`
#### Snippet
```java
			numPathsInIfPB((IfProgramBlock)pb, ec, paths);
		else if (pb instanceof BasicProgramBlock)
			return;
		else
			return;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupUtils.java`
#### Snippet
```java
			return;
		else
			return;
	}
	
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		}
		
		return;
	}
	
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
		}
		else
			return;
	}
}
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ASDCZero.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(cl);
		if(it == null)
			return;
		else if(it.value() > cu)
			_indexes.cacheIterator(it, cu); // cache this iterator.
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/APreAgg.java`
#### Snippet
```java
	public final void tsmmAColGroup(AColGroup other, MatrixBlock result) {
		if(other instanceof ColGroupEmpty)
			return;
		else if(other instanceof APreAgg)
			tsmmAPreAgg((APreAgg) other, result);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java
		final double[] resV = result.getDenseBlockValues();
		if(tmpResult.isEmpty())
			return;
		else if(tmpResult.isInSparseFormat()) {
			SparseBlock sb = tmpResult.getSparseBlock();
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
		final AIterator it = indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() > ru)
			indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		final AIterator it = getIterator(cl);
		if(it == null)
			return;
		else if(it.offset > cu)
			cacheIterator(it, cu); // cache this iterator.
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final int last = _indexes.getOffsetToLast();
		if(it == null || it.value() >= ru || rl > last)
			return;
		else if(ru > _indexes.getOffsetToLast())
			decompressToDenseBlockDenseDictionaryPost(db, rl, ru, offR, offC, values, it);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final int nCol = m.getNumColumns();
		if(it == null)
			return;
		else if(it.value() > cu)
			_indexes.cacheIterator(it, cu);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() > ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final int last = _indexes.getOffsetToLast();
		if(it == null)
			return;
		else if(it.value() >= ru)
			_indexes.cacheIterator(it, ru);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
		final AIterator it = _indexes.getIterator(rl);
		if(it == null)
			return;
		else if(it.value() >= ru)
			return;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
			return;
		else if(it.value() >= ru)
			return;
		// _indexes.cacheIterator(it, ru);
		else {
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
	public void copy(AMapToData d) {
		if(d.nUnique == 1)
			return;
		else if(d instanceof MapToBit)
			copyBit((MapToBit) d);
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/compress/workload/WorkloadAnalyzer.java`
#### Snippet
```java
	private static void getCandidates(StatementBlock sb, DMLProgram prog, List<Hop> cands, Set<String> fStack) {
		if(sb == null)
			return;
		else if(sb instanceof FunctionStatementBlock) {
			FunctionStatementBlock fsb = (FunctionStatementBlock) sb;
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
	@Override
	public void allocateMetaData(FrameBlock meta) {
		return;
	}

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderUDF.java`
#### Snippet
```java
	public void allocateMetaData(FrameBlock meta) {
		// do nothing
		return;
	}

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderPassThrough.java`
#### Snippet
```java
	public void allocateMetaData(FrameBlock meta) {
		// do nothing
		return;
	}

```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java
			csvInstruction = new CSVReblockSPInstruction(null, input1, output, mcOut.getBlocksize(), mcOut.getBlocksize(), hasHeader, delim, fill, fillValue, "csvrblk", instString, naStrings);
			csvInstruction.processInstruction(sec);
			return;
		}
		else if(fmt == FileFormat.BINARY && mc.getBlocksize() <= 0) {
```

## RuleId[id=SimplifiableConditionalExpression]
### SimplifiableConditionalExpression
`(cconf!=null) ? cconf.getBool(key) : false` can be simplified to 'cconf!=null \&\& cconf.getBool(key)'
in `src/main/java/org/apache/sysds/conf/ConfigurationManager.java`
#### Snippet
```java
	public static boolean getCompilerConfigFlag(ConfigType key) {
		CompilerConfig cconf = getCompilerConfig();
		return (cconf!=null) ? cconf.getBool(key) : false;
	}
	
```

### SimplifiableConditionalExpression
`(metadata != null) ? true : false` can be simplified to 'metadata != null'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		String name = parameterName;
		Object value = parameterValue;
		boolean hasMetadata = (metadata != null) ? true : false;
		boolean hasMatrixMetadata = hasMetadata && (metadata instanceof MatrixMetadata) ? true : false;
		boolean hasFrameMetadata = hasMetadata && (metadata instanceof FrameMetadata) ? true : false;
```

### SimplifiableConditionalExpression
`hasMetadata && (metadata instanceof MatrixMetadata) ? true : false` can be simplified to 'hasMetadata \&\& (metadata instanceof MatrixMetadata)'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		Object value = parameterValue;
		boolean hasMetadata = (metadata != null) ? true : false;
		boolean hasMatrixMetadata = hasMetadata && (metadata instanceof MatrixMetadata) ? true : false;
		boolean hasFrameMetadata = hasMetadata && (metadata instanceof FrameMetadata) ? true : false;
		if (name == null) {
```

### SimplifiableConditionalExpression
`hasMetadata && (metadata instanceof FrameMetadata) ? true : false` can be simplified to 'hasMetadata \&\& (metadata instanceof FrameMetadata)'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		boolean hasMetadata = (metadata != null) ? true : false;
		boolean hasMatrixMetadata = hasMetadata && (metadata instanceof MatrixMetadata) ? true : false;
		boolean hasFrameMetadata = hasMetadata && (metadata instanceof FrameMetadata) ? true : false;
		if (name == null) {
			throw new MLContextException("Input parameter name is null");
```

### SimplifiableConditionalExpression
`(margin instanceof LiteralOp) ?
((LiteralOp)margin).getStringValue().equals("cols") : false` can be simplified to 'margin instanceof LiteralOp \&\& ((LiteralOp)margin).getStringValue().equals("cols")'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
		Hop input = getInput().get(0);
		Hop margin = getParameterHop("margin");
		boolean col = (margin instanceof LiteralOp) ?
			((LiteralOp)margin).getStringValue().equals("cols") : false;
		
```

### SimplifiableConditionalExpression
`(includeNnz) ? _dc.nnzKnown() : true` can be simplified to '!includeNnz \|\| _dc.nnzKnown()'
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
	public boolean dimsKnown(boolean includeNnz) {
		return rowsKnown() && colsKnown()
			&& (_dataType.isScalar() || ((includeNnz) ? _dc.nnzKnown() : true));
	}

```

### SimplifiableConditionalExpression
`(getInput().size() == 6) ?
HopRewriteUtils.getBooleanValue((LiteralOp)getInput(5)) : true` can be simplified to 'getInput().size()!=6 \|\| HopRewriteUtils.getBooleanValue((LiteralOp)getInput(5))'
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
			Ctable.OperationTypes.CTABLE_EXPAND_SCALAR_WEIGHT : ternaryOpOrig;
		boolean ignoreZeros = false;
		boolean outputEmptyBlocks = (getInput().size() == 6) ?
			HopRewriteUtils.getBooleanValue((LiteralOp)getInput(5)) : true;
		
```

### SimplifiableConditionalExpression
`(e1.isPlanRef(i) && !e2.isPlanRef(i)) ?
(matPoints!=null && !InterestingPoint.isMatPoint(
matPoints, hopID, e1.input(i)))
|| hop.getInput().get(i).getParent().size()==1 : true` can be simplified to '!e1.isPlanRef(i) \|\| e2.isPlanRef(i) \|\| (matPoints!=null \&\& !InterestingPoint.isMatPoint( matPoints, hopID, e1.input(i))) \|\| hop.getInput().get(i).getParent().size()==1'
in `src/main/java/org/apache/sysds/hops/codegen/template/CPlanMemoTable.java`
#### Snippet
```java
						boolean rmSafe = true; 
						for( int i=0; i<=2; i++ ) {
							rmSafe &= (e1.isPlanRef(i) && !e2.isPlanRef(i)) ?
								(matPoints!=null && !InterestingPoint.isMatPoint(
									matPoints, hopID, e1.input(i)))
```

### SimplifiableConditionalExpression
`_checkCtx ? !status.isInParforContext() : true` can be simplified to '!_checkCtx \|\| !status.isInParforContext()'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteInjectSparkLoopCheckpointing.java`
#### Snippet
```java
		//optimization because otherwise we would prevent remote parfor)
		if( (sb instanceof WhileStatementBlock || sb instanceof ForStatementBlock)  //incl parfor 
		    && (_checkCtx ? !status.isInParforContext() : true)  )
		{
			//step 1: determine checkpointing candidates
```

### SimplifiableConditionalExpression
`hasTWrites ? HopRewriteUtils.rHasSimpleReadChain(
c, getFirstTransientWriteParent(c).getName()) : false` can be simplified to 'hasTWrites \&\& HopRewriteUtils.rHasSimpleReadChain( c, getFirstTransientWriteParent(c).getName())'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
					//lead to invalid reordering if variable consumers are not feeding into the candidate op.
					boolean hasTWrites = hasTransientWriteParents(c);
					boolean moveTWrite = hasTWrites ? HopRewriteUtils.rHasSimpleReadChain(
						c, getFirstTransientWriteParent(c).getName()) : false;
					
```

### SimplifiableConditionalExpression
`compCOut ? false : compUCOut` can be simplified to '!compCOut \&\& compUCOut'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			// Compressed Output possible through overlapping matrix.if the operation is right Matrix Multiply
			compCOut |= (current instanceof AggBinaryOp) && isCompressed(current.getInput().get(0));
			compUCOut = compCOut ? false : compUCOut;

			// Compressed Output if the operation is column bind.
```

### SimplifiableConditionalExpression
`getThirdExpr() != null ? isConstant(getThirdExpr()) : true` can be simplified to 'getThirdExpr()==null \|\| isConstant(getThirdExpr())'
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			// check if dimensions can be inferred
			long dim1=-1, dim2=1;
			if ( isConstant(getFirstExpr()) && isConstant(getSecondExpr()) && (getThirdExpr() != null ? isConstant(getThirdExpr()) : true) ) {
				double from, to, incr;
				try {
```

### SimplifiableConditionalExpression
`exists ? HDFSTool.isDirectory(filename) : false` can be simplified to 'exists \&\& HDFSTool.isDirectory(filename)'
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
		JSONObject retVal = new JSONObject();
		boolean exists = HDFSTool.existsFileOnHDFS(filename);
		boolean isDir = exists ? HDFSTool.isDirectory(filename) : false;

		// CASE: filename is a directory -- process as a directory
```

### SimplifiableConditionalExpression
`in.isInSparseFormat() ? false : !in.isAllocated() ? true :
isSorted(0, in.getNumRows()*in.getNumColumns(), in.getDenseBlockValues())` can be simplified to '!in.isInSparseFormat() \&\& (!in.isAllocated() ? true : isSorted(0, in.getNumRows()\*in.getNumColumns(), in.getDenseBlockValues()))'
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
	
	public static boolean isSorted(MatrixBlock in) {
		return in.isInSparseFormat() ? false : !in.isAllocated() ? true :
			isSorted(0, in.getNumRows()*in.getNumColumns(), in.getDenseBlockValues());
	}
```

### SimplifiableConditionalExpression
`!in.isAllocated() ? true :
isSorted(0, in.getNumRows()*in.getNumColumns(), in.getDenseBlockValues())` can be simplified to '!in.isAllocated() \|\| isSorted(0, in.getNumRows()\*in.getNumColumns(), in.getDenseBlockValues())'
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
	
	public static boolean isSorted(MatrixBlock in) {
		return in.isInSparseFormat() ? false : !in.isAllocated() ? true :
			isSorted(0, in.getNumRows()*in.getNumColumns(), in.getDenseBlockValues());
	}
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret.set(i, _data[i] == 0 ? false : true);
		}
		return new BitSetArray(ret, size());
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret.set(i, _data[i] == 0 ? false : true);
		}
		return new BitSetArray(ret, size());
```

### SimplifiableConditionalExpression
`that instanceof ColumnMetadata ? equals((ColumnMetadata) that) : false` can be simplified to 'that instanceof ColumnMetadata \&\& equals((ColumnMetadata) that)'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ColumnMetadata.java`
#### Snippet
```java
	@Override
	public boolean equals(Object that) {
		return that instanceof ColumnMetadata ? equals((ColumnMetadata) that) : false;
	}

```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret.set(i, _data[i] == 0 ? false : true);
		}
		return new BitSetArray(ret, size());
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret.set(i, _data[i] == 0 ? false : true);
		}
		return new BitSetArray(ret, size());
```

### SimplifiableConditionalExpression
`_data[i] == 0 ? false : true` can be simplified to '_data\[i\]!=0'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
				throw new DMLRuntimeException(
					"Unable to change to Boolean from Integer array because of value:" + _data[i]);
			ret[i] = _data[i] == 0 ? false : true;
		}
		return new BooleanArray(ret);
```

### SimplifiableConditionalExpression
`qop.wtype3.isBasic()?X.isInSparseFormat():false` can be simplified to 'qop.wtype3.isBasic() \&\& X.isInSparseFormat()'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		else if( qop.wtype3 != null ) {
			DataCharacteristics mc = qop.wtype3.computeOutputCharacteristics(X.rlen, X.clen, U.clen);
			R.reset( (int)mc.getRows(), (int)mc.getCols(), qop.wtype3.isBasic()?X.isInSparseFormat():false);
		}
		
```

### SimplifiableConditionalExpression
`(val == 0) ? sp : false` can be simplified to 'val == 0 \&\& sp'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		rlen = rl;
		clen = cl;
		sparse = (val == 0) ? sp : false;
		nonZeros = (val == 0) ? 0 : (long)rl*cl;
		estimatedNNzsPerRow = (estnnz < 0 || !sparse) ? -1 :
```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}
	
```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}
	
```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			}
		}
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			}
		}
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm result is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			}
		}
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("indexSource") ? true : false` can be simplified to 'inCache.containsKey("indexSource")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the input to rightIndex is found
		return inCache.containsKey("indexSource") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
		}
		// return true only if the last tsmm is found
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`inCache.containsKey("lastMatrix") ? true : false` can be simplified to 'inCache.containsKey("lastMatrix")'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageRewriteReuse.java`
#### Snippet
```java
			}
		}
		return inCache.containsKey("lastMatrix") ? true : false;
	}

```

### SimplifiableConditionalExpression
`wt.isBasic()?mW.sparse:false` can be simplified to 'wt.isBasic() \&\& mW.sparse'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java

		//pre-processing
		ret.sparse = wt.isBasic()?mW.sparse:false;
		ret.allocateBlock();

```

### SimplifiableConditionalExpression
`wt.isBasic()?mW.sparse:false` can be simplified to 'wt.isBasic() \&\& mW.sparse'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java

		//pre-processing
		ret.sparse = wt.isBasic()?mW.sparse:false;
		ret.allocateBlock();
		
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[7]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[7\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
			
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[8]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[8\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
			
			return new QuaternarySPInstruction(new QuaternaryOperator(wtype), in1, in2, in3, in4, out, cacheU, cacheV, opcode, str);	
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[7]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[7\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
			
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[8]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[8\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
			
			return new QuaternarySPInstruction(new QuaternaryOperator(wtype, uopcode), in1, in2, in3, null, out, cacheU, cacheV, opcode, str);	
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[7]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[7\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
		
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[8]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[8\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[7]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[8]) : true;
		
			final WDivMMType wt = WDivMMType.valueOf(parts[6]);
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[6 + addInput4]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[6 + addInput4\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[6 + addInput4]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[7 + addInput4]) : true;
		
```

### SimplifiableConditionalExpression
`isRed ? Boolean.parseBoolean(parts[7 + addInput4]) : true` can be simplified to '!isRed \|\| Boolean.parseBoolean(parts\[7 + addInput4\])'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
			//in mappers always through distcache, in reducers through distcache/shuffle
			boolean cacheU = isRed ? Boolean.parseBoolean(parts[6 + addInput4]) : true;
			boolean cacheV = isRed ? Boolean.parseBoolean(parts[7 + addInput4]) : true;
		
			if( opcode.endsWith("wsigmoid") )
```

### SimplifiableConditionalExpression
`(dis.readByte() != 0) ? true : false` can be simplified to 'dis.readByte() != 0'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/CorrMatrixBlock.java`
#### Snippet
```java
		throws IOException 
	{
		boolean corrExists = (dis.readByte() != 0) ? true : false;
		_value = new MatrixBlock();
		_value.readFields(dis);
```

### SimplifiableConditionalExpression
`(rec != null) ?
Boolean.parseBoolean(rec) : false` can be simplified to 'rec != null \&\& Boolean.parseBoolean(rec)'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
	public boolean isRecursive() {
		String rec = getParam(ParamType.RECURSIVE_CALL);
		return (rec != null) ?
			Boolean.parseBoolean(rec) : false;
	}
```

### SimplifiableConditionalExpression
`writeStatus.booleanValue() ?
CACHING_WRITE_CACHE_ON_READ : true` can be simplified to '!writeStatus.booleanValue() \|\| CACHING_WRITE_CACHE_ON_READ'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
					
					//mark for initial local write (prevent repeated execution of rdd operations)
					_requiresLocalWrite = writeStatus.booleanValue() ? 
						CACHING_WRITE_CACHE_ON_READ : true;
				}
```

## RuleId[id=ManualMinMaxCalculation]
### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
					specifiedRows = ((LiteralOp)rowsHop).getLongValue();
				}
				numRows = numRows < specifiedRows ? numRows : specifiedRows;
				if (colsHop != null && colsHop instanceof LiteralOp){
					specifiedCols = ((LiteralOp)colsHop).getLongValue();
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
					specifiedCols = ((LiteralOp)colsHop).getLongValue();
				}
				numCols = numCols < specifiedCols ? numCols : specifiedCols;
				
				if (sparsePrintHOP != null && sparsePrintHOP instanceof LiteralOp){
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
			if(val1 < 0 || val2 < 0) return OptimizerUtils.DEFAULT_SIZE;
			double ret = val1 + val2;
			if(ret >= OptimizerUtils.DEFAULT_SIZE) return OptimizerUtils.DEFAULT_SIZE;
			else return ret;
		}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
			if(val1 < 0 || val2 < 0) return OptimizerUtils.DEFAULT_SIZE;
			double ret = Math.max(val1, val2);
			if(ret >= OptimizerUtils.DEFAULT_SIZE) return OptimizerUtils.DEFAULT_SIZE;
			else return ret;
		}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
					long outputDim1 = HopRewriteUtils.getIntValueSafe((LiteralOp)getInput().get(3));
					long outputDim2 = HopRewriteUtils.getIntValueSafe((LiteralOp)getInput().get(4));
					long outputNNZ = ( outputDim1*outputDim2 > outputDim1 ? outputDim1 : outputDim1*outputDim2 );
					setDim1(outputDim1);
					setDim2(outputDim2);
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
				//   worst case #nnz in C = m => sparsity = 1/m
				// for ctable_histogram also one dimension is known
				double sparsity = OptimizerUtils.getSparsity(dim1, dim2, (nnz<=dim1)?nnz:dim1); 
				return OptimizerUtils.estimateSizeExactSparsity(dim1, dim2, sparsity);
				
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            values[c--] = tempVal;
        }
        length = a - start < b - a ? a - start : b - a;
        int l = start;
        int h = b - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            values[h++] = tempVal;
        }
        length = d - c < end - 1 - d ? d - c : end - 1 - d;
        l = b;
        h = end - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            values[c--] = tempVal;
        }
        length = a - start < b - a ? a - start : b - a;
        int l = start;
        int h = b - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            values[h++] = tempVal;
        }
        length = d - c < end - 1 - d ? d - c : end - 1 - d;
        l = b;
        h = end - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            indexes[c--] = tempIx;
        }
        length = a - start < b - a ? a - start : b - a;
        int l = start;
        int h = b - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
            indexes[h++] = tempIx;
        }
        length = d - c < end - 1 - d ? d - c : end - 1 - d;
        l = b;
        h = end - length;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		int colLength = clen;
		if (rowsToPrint >= 0)
			rowLength = rowsToPrint < rlen ? rowsToPrint : rlen;
		if (colsToPrint >= 0)
			colLength = colsToPrint < clen ? colsToPrint : clen;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
			rowLength = rowsToPrint < rlen ? rowsToPrint : rlen;
		if (colsToPrint >= 0)
			colLength = colsToPrint < clen ? colsToPrint : clen;

		//print frame header
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		int colLength = clen;
		if (rowsToPrint >= 0)
			rowLength = rowsToPrint < rlen ? rowsToPrint : rlen;
		if (colsToPrint >= 0)
			colLength = colsToPrint < clen ? colsToPrint : clen;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
			rowLength = rowsToPrint < rlen ? rowsToPrint : rlen;
		if (colsToPrint >= 0)
			colLength = colsToPrint < clen ? colsToPrint : clen;

		DecimalFormat df = new DecimalFormat();
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
				continue;
			int l = _data[i].length();
			minLength = minLength < l ? minLength : l;
			maxLength = maxLength > l ? maxLength : l;

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
			int l = _data[i].length();
			minLength = minLength < l ? minLength : l;
			maxLength = maxLength > l ? maxLength : l;

		}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		for( int i=ai; i<ai+len; i++ ) {
			minindex = (a[i]<=minval) ? i-ai : minindex;
			minval = (a[i]<=minval) ? a[i] : minval;
		}
		//note: robustness for all-NaN rows
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		for( int i=ai; i<ai+len; i++ ) {
			maxindex = (a[i]>=maxval) ? i-ai : maxindex;
			maxval = (a[i]>=maxval) ? a[i] : maxval;
		}
		//note: robustness for all-NaN rows
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		for(int i = 0; i < size(); i++) {
			int v = getIndex(i);
			m = v > m ? v : m;
		}
		return m;
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixRelationalBinaryGPUInstruction.java`
#### Snippet
```java
		// Outer binary op ( [100,1] + [1,100] or [100,100] + [100,1]
		if (rlen1 != rlen2 || clen1 != clen2){
			rlen = rlen1 > rlen2 ? rlen1 : rlen2;
			clen = clen1 > clen2 ? clen1 : clen2;
		}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixRelationalBinaryGPUInstruction.java`
#### Snippet
```java
		if (rlen1 != rlen2 || clen1 != clen2){
			rlen = rlen1 > rlen2 ? rlen1 : rlen2;
			clen = clen1 > clen2 ? clen1 : clen2;
		}

```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		// Outer binary op ( [100,1] + [1,100] or [100,100] + [100,1]
		if (rlen1 != rlen2 || clen1 != clen2){
			rlen = rlen1 > rlen2 ? rlen1 : rlen2;
			clen = clen1 > clen2 ? clen1 : clen2;
		}
```

### ManualMinMaxCalculation
Can be replaced with 'Math.max()' call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/MatrixMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		if (rlen1 != rlen2 || clen1 != clen2){
			rlen = rlen1 > rlen2 ? rlen1 : rlen2;
			clen = clen1 > clen2 ? clen1 : clen2;
		}

```

### ManualMinMaxCalculation
Can be replaced with 'Math.min()' call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
					//constrain max parfor parallelism by problem size
					int tmpN = Integer.parseInt(c.getParam(ParamType.NUM_ITERATIONS));
					int tmpK = (tmpN<parforK)? tmpN : parforK;
					
					//set parfor degree of parallelism
```

## RuleId[id=SuspiciousListRemoveInLoop]
### SuspiciousListRemoveInLoop
Suspicious 'List.remove()' in loop
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemoveUnnecessaryCasts.java`
#### Snippet
```java
						{
							//rehang cast input as child of cast consumer
							pin.remove( j ); //remove cast ref
							pin.add(j, in); //add ref to cast input
							in.getParent().remove(hop); //remove cast from cast input parents
```

### SuspiciousListRemoveInLoop
Suspicious 'List.remove()' in loop
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
					&& ((CPInstruction)inst).getCPInstructionType() == CPType.Variable 
					&& ((VariableCPInstruction)inst).isRemoveVariable(varName) ) {
				deleteInst.remove(i);
			}
		}
```

## RuleId[id=JavadocLinkAsPlainText]
### JavadocLinkAsPlainText
Link specified as plain text
in `src/main/java/org/apache/sysds/lops/compile/linearization/ILinearize.java`
#### Snippet
```java
 * A interface for the linearization algorithms that order the DAG nodes into a sequence of instructions to execute.
 *
 * https://en.wikipedia.org/wiki/Linearizability#Linearization_points
 */
public class ILinearize {
```

### JavadocLinkAsPlainText
Link specified as plain text
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
	 * Convenience method to print NaN & Infinity compliant with how as.scalar prints them.
	 * {@link DecimalFormat} prints NaN as \uFFFD and Infinity as \u221E
	 * http://docs.oracle.com/javase/6/docs/api/java/text/DecimalFormat.html
	 * @param df	The {@link DecimalFormat} instance, constructed with the appropriate options
	 * @param value	The double value to print
```

### JavadocLinkAsPlainText
Link specified as plain text
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibCommonsMath.java`
#### Snippet
```java
	/**
	 * Function to perform the Lanczos algorithm and then computes the Eigendecomposition.
	 * Caution: Lanczos is not numerically stable (see https://en.wikipedia.org/wiki/Lanczos_algorithm)
	 * Input must be a symmetric (and square) matrix.
	 *
```

### JavadocLinkAsPlainText
Link specified as plain text
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	 * Using C2R & R2C algorithm from PPOP 2014.
	 * 
	 * https://dl.acm.org/doi/pdf/10.1145/2692916.2555253
	 * 
	 * @param in The matrix to transpose in place.
```

## RuleId[id=FieldCanBeLocal]
### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/hops/estim/EstimatorSampleRa.java`
#### Snippet
```java
	private final double _sampleFrac; //sample fraction (0,1]
	private final double _eps; //target error
	private final double _delta; //probability of error
	private final int _k; //k-minimum hash values
	
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/hops/estim/EstimatorSampleRa.java`
#### Snippet
```java
	private final int _runs;
	private final double _sampleFrac; //sample fraction (0,1]
	private final double _eps; //target error
	private final double _delta; //probability of error
	private final int _k; //k-minimum hash values
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/hops/fedplanner/FederatedCompilationTimer.java`
#### Snippet
```java
	public static class TimeEntry {
		private final long startTime;
		private long stopTime;
		private long duration;
		private String name;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/utils/ParameterBuilder.java`
#### Snippet
```java
	
	/** String used to replace variables in scripts */
	private static String _RS = "\\$\\$";

	/**
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5ContiguousDataset.java`
#### Snippet
```java
	private final H5DataTypeMessage dataTypeMessage;
	@SuppressWarnings("unused")
	private final H5DataSpaceMessage dataSpaceMessage;

	public H5ContiguousDataset(H5RootObject rootObject, H5ObjectHeader objectHeader) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/io/hdf5/H5GroupSymbolTableNode.java`
#### Snippet
```java
	private final short numberOfEntries;
	private final H5SymbolTableEntry[] symbolTableEntries;
	private final int headerSize = 8;
	private final H5RootObject rootObject;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/iogen/GenerateReader.java`
#### Snippet
```java
	public static class GenerateReaderMatrix extends GenerateReader {

		private MatrixReader matrixReader;

		public GenerateReaderMatrix(SampleProperties sampleProperties) throws Exception {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/iogen/GenerateReader.java`
#### Snippet
```java
	public static class GenerateReaderFrame extends GenerateReader {

		private FrameReader frameReader;

		public GenerateReaderFrame(SampleProperties sampleProperties) throws Exception {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
public class LineageEstimatorStatistics {
	private static final LongAdder _ctimeSaved      = new LongAdder(); //in nano sec
	private static int INSTCOUNT = 10;
	
	public static void reset() {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettingsBuilder.java`
#### Snippet
```java
	private double samplingRatio;
	// private double samplePower = 0.6;
	private double samplePower = 0.65;
	// private double samplePower = 0.68;
	// private double samplePower = 0.7;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/fed/TsmmFEDInstruction.java`
#### Snippet
```java
	private final MMTSJType _type;
	@SuppressWarnings("unused")
	private final int _numThreads;
	
	public TsmmFEDInstruction(CPOperand in, CPOperand out, MMTSJType type, int k, String opcode, String istr, FederatedOutput fedOut) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
	// OUTER PRODUCT TEMPLATE
	private static class SpoofFEDOuterProduct extends SpoofFEDType {
		private final SpoofOuterProduct _op;
		private final OutProdType _outProdType;
		private CPOperand[] _inputs;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
	// ROWWISE TEMPLATE
	private static class SpoofFEDRowwise extends SpoofFEDType {
		private final SpoofRowwise _op;
		private final RowType _rowType;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/BuiltinBinaryGPUInstruction.java`
#### Snippet
```java
public abstract class BuiltinBinaryGPUInstruction extends GPUInstruction {
	@SuppressWarnings("unused")
	private int _arity;

	protected BuiltinBinaryGPUInstruction(Operator op, CPOperand input1, CPOperand input2, CPOperand output,
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		private boolean _cbind;
		private long _lastIxLeft;
		private int _blen;
		
		public MergeWithShiftedBlocks(DataCharacteristics mc1, DataCharacteristics mc2, boolean cbind)
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java
		private final DataCharacteristics _mcIn;
		private final DataCharacteristics _mcOut;
		private final boolean _byrow;
		private final boolean _outputEmptyBlocks;

```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java

		private final DataCharacteristics _mcIn;
		private final DataCharacteristics _mcOut;
		private final boolean _byrow;
		private final boolean _outputEmptyBlocks;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java
		private final DataCharacteristics _mcOut;
		private final boolean _byrow;
		private final boolean _outputEmptyBlocks;

		public RDDTensorReshapeFunction(DataCharacteristics mcIn, DataCharacteristics mcOut, boolean byrow, boolean outputEmptyBlocks) {
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixReshapeSPInstruction.java`
#### Snippet
```java
		private static final long serialVersionUID = 8030648988828223639L;

		private final DataCharacteristics _mcIn;
		private final DataCharacteristics _mcOut;
		private final boolean _byrow;
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
	
	private long _rlen = -1;
	private long _clen = -1;
	private int _blen = -1;
	
```

### FieldCanBeLocal
Field can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkloadAnalyzer.java`
#### Snippet
```java

	/** Frequency value for how many instructions before we do a pass for compression */
	private static int compressRunFrequency = 10;

	/** Instruction maps to interesting variables */
```

## RuleId[id=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					boolean paramsOkay = true;
					for (String key : os.getSource().getVarParams().keySet()){
						if (! (key.equals(DataExpression.IO_FILENAME) || key.equals(DataExpression.FORMAT_TYPE)))
							paramsOkay = false;
					}
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
		for(int r = 0; r < nrows && result; r++) {
			for(int c = 0; c < ncols && result; c++) {
				if(mapRow[r][c] != -1 && mapRow[r][c] != rValue + r) {
					result = false;
				}
```

## RuleId[id=RedundantStreamOptionalCall]
### RedundantStreamOptionalCall
Redundant 'flatMap()' call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java

		if(average && distinctValues.size() > quantileIndex) {
			Double[] distinctsSorted = distinctValues.stream().flatMap(Stream::of).sorted().toArray(Double[]::new);
			Double medianSum = Double.sum(distinctsSorted[quantileIndex-1], distinctsSorted[quantileIndex]);
			return (T) medianSum;
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
					HDFSTool.deleteFileIfExistOnHDFS( randFName );
				} catch (IOException e) {
					throw new DMLRuntimeException("Cannot merge the output into single file: " + e.getMessage());
				}
			}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
						callback.onSuccess(response.serialize());
					} catch (IOException e) {
						throw new DMLRuntimeException("PSRpcHandler: some error occrred when wrapping the rpc response.", e);
					}
				}
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
						callback.onSuccess(response.serialize());
					} catch (IOException e) {
						throw new DMLRuntimeException("PSRpcHandler: some error occrred when wrapping the rpc response.", e);
					}
				}
```

## RuleId[id=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
					double v = cell.getV();
					if (i > prevCellRow) {
						if (sb == null) {
							sb = new StringBuilder();
						} else {
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java

		iLop = _inputParams.get(DataExpression.RAND_DIMS);
		if (iLop != null) {
			sb.append(iLop.prepScalarInputOperand(getExecType()));
			sb.append(OPERAND_DELIMITOR);
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		
		Lop iLop = _inputParams.get(DataExpression.RAND_DIMS);
		if (iLop != null) {
			sb.append(iLop.prepScalarInputOperand(getExecType()));
			sb.append(OPERAND_DELIMITOR);
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `src/main/java/org/apache/sysds/runtime/util/HDFSTool.java`
#### Snippet
```java
			// it is a directory
			FileStatus[] stats = fs.listStatus(dir);
			if (stats != null) {
				for (FileStatus stat : stats) {
					if (stat.getLen() > 0)
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/matrix/data/OperationsOnMatrixValues.java`
#### Snippet
```java
					break;
				case LASTCOLUMN:
					if(op.increOp.fn instanceof Builtin 
					   && ( ((Builtin)(op.increOp.fn)).bFunc == Builtin.BuiltinCode.MAXINDEX 
					        || ((Builtin)(op.increOp.fn)).bFunc == Builtin.BuiltinCode.MININDEX) )
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			return;

		if( !sparse ) { //DENSE 
			//allocate on demand (w/o overwriting nnz)
			allocateDenseBlock(false);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
	private static void d_uacm( DenseBlock a, DenseBlock c, int n, int rl, int ru, long nnz ) {
		double[] lc = c.set(1).values(0); // guaranteed single row
		if(nnz < (double) a.getDim(0) * n * 0.90) { // 90% sparsity.
			// process with early termination if a zero is hit in all columns.
			final int blockz = 32;
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUnary.java`
#### Snippet
```java
			if(Builtin.isBuiltinCode(op.fn, BuiltinCode.ISINF, BuiltinCode.ISNA, BuiltinCode.ISNAN))
				return new MatrixBlock(r, c, 0);
			if(op.fn instanceof Builtin) {
				String message = "Unary Op not supported Overlapping builtin: " + ((Builtin) (op.fn)).getBuiltinCode();
				return m.getUncompressed(message, op.getNumThreads()).unaryOperations(op, null);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMMChain.java`
#### Snippet
```java
		final List<AColGroup> groups = x.getColGroups();
		final boolean shouldFilter = CLALibUtils.shouldPreFilter(groups);
		if(shouldFilter) {
			final int nCol = x.getNumColumns();
			final double[] constV = new double[nCol];
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUtils.java`
#### Snippet
```java

	public static boolean containsInfOrNan(double pattern, double[] reference) {
		if(Double.isNaN(pattern)) {
			for(double d : reference)
				if(Double.isNaN(d))
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/AggregateBinaryCPInstruction.java`
#### Snippet
```java
		MatrixBlock ret;

		if(c1) {
			CompressedMatrixBlock main = (CompressedMatrixBlock) matBlock1;
			ret = main.aggregateBinaryOperations(matBlock1, matBlock2, new MatrixBlock(), ab_op, transposeLeft,
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
		}
		if(shadowBuffer.isBuffered()) {
			if(isEviction) {
				// If already copied to shadow buffer as part of previous eviction, do nothing.
				return;
```

## RuleId[id=SimplifyOptionalCallChains]
### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
	public static MatrixBlock aggMinMax(Future<FederatedResponse>[] ffr, boolean isMin, boolean isScalar, Optional<FType> fedType) {
		try {
			if (!fedType.isPresent() || fedType.get() == FType.OTHER) {
				double res = isMin ? Double.MAX_VALUE : -Double.MAX_VALUE;
				for (Future<FederatedResponse> fr : ffr) {
```

## RuleId[id=SlowListContainsAll]
### SlowListContainsAll
Call to 'list.containsAll(collection)' may have poor performance
in `src/main/java/org/apache/sysds/lops/compile/linearization/ILinearize.java`
#### Snippet
```java
			// Skip if one of the lop's outputs is not in the result list yet (will be added once the output lop is
			// traversed), but only if any of the output lops is bound to be added to the result at a later stage
			if(result.contains(e.getKey()) || (!result.containsAll(e.getKey().getOutputs()) &&
				remaining.stream().anyMatch(l -> e.getKey().getOutputs().contains(l))))
				continue;
```

## RuleId[id=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing ArrayList
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
	private static ArrayList<Expression> getFunctionDefaults(List<TypedArgAssignContext> ctx) {
		return new ArrayList<>(ctx.stream().map(arg -> 
			(arg.paramVal!=null)?arg.paramVal.info.expr:null).collect(Collectors.toList()));
	}

```

### FuseStreamOperations
Stream may be extended replacing ArrayList
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					//prepare function input names and inputs
					List<String> inputNames = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> e.getName()).collect(Collectors.toList()));
					List<Hop> finputs = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> processExpression(e.getExpr(), null, ids)).collect(Collectors.toList()));
```

### FuseStreamOperations
Stream may be extended replacing ArrayList
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
						.map(e -> e.getName()).collect(Collectors.toList()));
					List<Hop> finputs = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> processExpression(e.getExpr(), null, ids)).collect(Collectors.toList()));
					
					//append default expression for missing arguments
```

### FuseStreamOperations
Stream may be extended replacing ArrayList
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					//prepare function input names and inputs
					List<String> inputNames = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> e.getName()).collect(Collectors.toList()));
					List<Hop> finputs = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> processExpression(e.getExpr(), null, ids)).collect(Collectors.toList()));
```

### FuseStreamOperations
Stream may be extended replacing ArrayList
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
						.map(e -> e.getName()).collect(Collectors.toList()));
					List<Hop> finputs = new ArrayList<>(fci.getParamExprs().stream()
						.map(e -> processExpression(e.getExpr(), null, ids)).collect(Collectors.toList()));
					
					//use function signature to obtain names for unnamed args
```

### FuseStreamOperations
Stream may be extended replacing 'sort'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			synchronized(matrixMemoryManager.gpuObjects) {
				List<GPUObject> unlockedGPUObjects = matrixMemoryManager.gpuObjects.stream()
						.filter(gpuObj -> !gpuObj.isLocked()).collect(Collectors.toList());
				Collections.sort(unlockedGPUObjects, new EvictionPolicyBasedComparator(size));
				while(A == null && unlockedGPUObjects.size() > 0) {
```

## RuleId[id=ConditionalExpressionWithIdenticalBranches]
### ConditionalExpressionWithIdenticalBranches
Conditional expression `dim1 ? h.getRows() : h.getRows()` with identical branches
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
	public static long getMaxInputDim(DataCharacteristics[] dc, boolean dim1) {
		return Arrays.stream(dc).mapToLong(
			h -> (dim1 ? h.getRows() : h.getRows())).max().orElse(-1);
	}
	
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `cbind ? mc1.getBlocksize() : mc1.getBlocksize()` with identical branches
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AppendGSPInstruction.java`
#### Snippet
```java
		{
			_cbind = cbind;
			_blen = cbind ? mc1.getBlocksize() : mc1.getBlocksize();
			_lastIxLeft = (long) Math.ceil((double)(cbind ? mc1.getCols():mc1.getRows()) / _blen);			
		}
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `cbind ? blen : blen` with identical branches
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
			//check for boundary block
			_lastBlockColIndex = Math.max((long)Math.ceil(
				(double)offset/(cbind ? blen : blen)),1);
		}

```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `cbind ? blen : blen` with identical branches
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixAppendMSPInstruction.java`
#### Snippet
```java
			//check for boundary block
			_lastBlockColIndex = Math.max((long)Math.ceil(
				(double)offset/(cbind ? blen : blen)),1);
		}
		
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `isRow ? in_blen : in_blen` with identical branches
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractBlockForBinaryReblock.java`
#### Snippet
```java
		//determine dimension and block sizes
		long len = isRow ? rlen : clen;
		int blen = isIn ? (isRow ? in_blen : in_blen) : (isRow ? out_blen : out_blen);
		
		//compute 1-based global cell index in block
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `isRow ? out_blen : out_blen` with identical branches
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractBlockForBinaryReblock.java`
#### Snippet
```java
		//determine dimension and block sizes
		long len = isRow ? rlen : clen;
		int blen = isIn ? (isRow ? in_blen : in_blen) : (isRow ? out_blen : out_blen);
		
		//compute 1-based global cell index in block
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `(dat instanceof MatrixObject) ? tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::...` with identical branches
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					CacheableData<?> out = (CacheableData<?>) dat;
					Stream<Object> tmp = Arrays.stream(results).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (dat instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					String fname = constructResultMergeFileName();
					ResultMerge<?> rm = createResultMerge(_resultMerge, out, in, fname, var._isAccum, ec);
```

### ConditionalExpressionWithIdenticalBranches
Conditional expression `(out instanceof MatrixObject) ? tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::...` with identical branches
in `src/main/java/org/apache/sysds/runtime/controlprogram/ParForProgramBlock.java`
#### Snippet
```java
					
					Stream<Object> tmp = Arrays.stream(_refVars).map(vars -> vars.get(var._name));
					CacheableData<?>[] in = (out instanceof MatrixObject) ?
						tmp.toArray(MatrixObject[]::new) : tmp.toArray(FrameObject[]::new);
					
					String fname = constructResultMergeFileName();
```

## RuleId[id=PointlessNullCheck]
### PointlessNullCheck
Unnecessary 'null' check before 'isLoopStatementBlock()' call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteHoistLoopInvariantOperations.java`
#### Snippet
```java
	public List<StatementBlock> rewriteStatementBlock(StatementBlock sb, ProgramRewriteStatus state) {
		//early abort if possible
		if( sb == null || !HopRewriteUtils.isLoopStatementBlock(sb) )
			return Arrays.asList(sb); //rewrite only applies to loops
		
```

### PointlessNullCheck
Unnecessary 'null' check before 'isBinaryMSAdd()' call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
                            varPlusEps = var.getParent().get(0);
                    }
					if(varPlusEps != null && isBinaryMSAdd(varPlusEps, eps) && isOneBySqrt(varPlusEps)) {
						
						Hop cache_var = varPlusEps.getParent().get(0).getParent().get(0);
```

### PointlessNullCheck
Unnecessary 'null' check before 'parseBoolean()' call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
			String rec = getParam(ParamType.RECURSIVE_CALL);
			String fname = getParam(ParamType.OPSTRING);
			if( rec != null && Boolean.parseBoolean(rec) ) 
				stack.add(fname); //collect
			else if( stack.contains(fname) )
```

## RuleId[id=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			throw new MLContextException("Type (" + object.getClass() + ") not a recognized basic type");
		}
		Class<? extends Object> clazz = object.getClass();
		if (clazz.equals(Integer.class)) {
			return Statement.INT_VALUE_TYPE;
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			e -> e.getOutput().getDataType().isScalar()) ? DataType.SCALAR : DataType.MATRIX;
		Expression firstM = dt.isMatrix() ? Arrays.stream(getAllExpr()).filter(
			e -> e.getOutput().getDataType().isMatrix()).findFirst().get() : null;
		ValueType vt = dt.isMatrix() ? ValueType.FP64 : ValueType.INT64;
		for( Expression e : getAllExpr() ) {
```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/util/DependencyThreadPool.java`
#### Snippet
```java
					if (missing)
						continue;
					int maxParentLevel = Arrays.stream(parentLevels).max().getAsInt();
					levelMap.put(dt, maxParentLevel+1);
					if (maxParentLevel+1 == depth)
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		}
		//find format with the most occurences in values -> dominant format
		String dominantFormat = format_matches.entrySet().stream().max((entry1, entry2) -> entry1.getValue() > entry2.getValue() ? 1 : -1).get().getKey();
		for (int i = 0; i< values.length; i++){
			//skip null/blank entries
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderComposite.java`
#### Snippet
```java
	public int getDomainSize() {
		return _columnEncoders.stream()//
			.map(ColumnEncoder::getDomainSize).reduce(Integer::max).get();
	}

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/transform/tokenize/Tokenizer.java`
#### Snippet
```java
				List<DependencyTask<?>> taskList = tokenizerApplier.getApplyTasks(this.internalRepresentation, out);
				lastRow = pool.submitAllAndWait(taskList)//
					.stream().map(x -> (Integer) x).max((x,y) -> Integer.compare(x, y)).get();
			}
			catch(ExecutionException | InterruptedException e) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/transform/tokenize/Tokenizer.java`
#### Snippet
```java
				List<DependencyTask<?>> tokenizeTasks = getTokenizeTasks(in, out, pool);
				int lastRow = pool.submitAllAndWait(tokenizeTasks).stream()//
					.map(s -> s == null? 0 :(Integer)s).max((x,y) -> Integer.compare(x, y)).get();
				if(lastRow != out.getNumRows()){
					out = out.slice(0, lastRow - 1, 0, out.getNumColumns() - 1, null);
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CovarianceFEDInstruction.java`
#### Snippet
```java
		Optional<CM_COV_Object> res = globalCmobj.stream().reduce((arg0, arg1) -> (CM_COV_Object) cop.fn.execute(arg0, arg1));
		try {
			ec.setScalarOutput(output.getName(), new DoubleObject(res.get().getRequiredResult(cop)));
		}
		catch(Exception e) {
```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CentralMomentFEDInstruction.java`
#### Snippet
```java
				.reduce((arg0, arg1) -> (CM_COV_Object) finalCm_op.fn.execute(arg0, arg1));
		try {
			ec.setScalarOutput(output.getName(), new DoubleObject(res.get().getRequiredResult(finalCm_op)));
		}
		catch (Exception e) {
```

## RuleId[id=ConstantValue]
### ConstantValue
Condition `obj instanceof Tuple2` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
				Tuple3<String, Object, MatrixMetadata> t3 = (Tuple3<String, Object, MatrixMetadata>) obj;
				in(t3._1(), t3._2(), t3._3());
			} else if (obj instanceof Tuple2) {
				@SuppressWarnings("unchecked")
				Tuple2<String, Object> t2 = (Tuple2<String, Object>) obj;
```

### ConstantValue
Condition `value instanceof RDD` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			}

		} else if (value instanceof RDD<?>) {
			@SuppressWarnings("unchecked")
			RDD<String> rdd = (RDD<String>) value;
```

### ConstantValue
Condition `value instanceof MatrixBlock` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
				return MLContextConversionUtil.rddStringCSVToFrameObject(rdd);
			}
		} else if (value instanceof MatrixBlock) {
			MatrixBlock matrixBlock = (MatrixBlock) value;
			return MLContextConversionUtil.matrixBlockToMatrixObject(name, matrixBlock, (MatrixMetadata) metadata);
```

### ConstantValue
Condition `value instanceof FrameBlock` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			MatrixBlock matrixBlock = (MatrixBlock) value;
			return MLContextConversionUtil.matrixBlockToMatrixObject(name, matrixBlock, (MatrixMetadata) metadata);
		} else if (value instanceof FrameBlock) {
			FrameBlock frameBlock = (FrameBlock) value;
			return MLContextConversionUtil.frameBlockToFrameObject(name, frameBlock, (FrameMetadata) metadata);
```

### ConstantValue
Condition `value instanceof Dataset` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			FrameBlock frameBlock = (FrameBlock) value;
			return MLContextConversionUtil.frameBlockToFrameObject(name, frameBlock, (FrameMetadata) metadata);
		} else if (value instanceof Dataset<?>) {
			@SuppressWarnings("unchecked")
			Dataset<Row> dataFrame = (Dataset<Row>) value;
```

### ConstantValue
Condition `value instanceof Matrix` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
				return MLContextConversionUtil.dataFrameToFrameObject(dataFrame);
			}
		} else if (value instanceof Matrix) {
			Matrix matrix = (Matrix) value;
			if ((matrix.hasBinaryBlocks()) && (!matrix.hasMatrixObject())) {
```

### ConstantValue
Condition `value instanceof Frame` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			}
			return matrix.toMatrixObject();
		} else if (value instanceof Frame) {
			Frame frame = (Frame) value;
			if ((frame.hasBinaryBlocks()) && (!frame.hasFrameObject())) {
```

### ConstantValue
Condition `value instanceof URL` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			double[][] doubleMatrix = (double[][]) value;
			return MLContextConversionUtil.doubleMatrixToMatrixObject(name, doubleMatrix, (MatrixMetadata) metadata);
		} else if (value instanceof URL) {
			URL url = (URL) value;
			return MLContextConversionUtil.urlToMatrixObject(url, (MatrixMetadata) metadata);
```

### ConstantValue
Condition `value instanceof Integer` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			URL url = (URL) value;
			return MLContextConversionUtil.urlToMatrixObject(url, (MatrixMetadata) metadata);
		} else if (value instanceof Integer) {
			return new IntObject((Integer) value);
		} else if (value instanceof Double) {
```

### ConstantValue
Condition `value instanceof Double` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		} else if (value instanceof Integer) {
			return new IntObject((Integer) value);
		} else if (value instanceof Double) {
			return new DoubleObject((Double) value);
		} else if (value instanceof String) {
```

### ConstantValue
Condition `value instanceof String` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		} else if (value instanceof Double) {
			return new DoubleObject((Double) value);
		} else if (value instanceof String) {
			return new StringObject((String) value);
		} else if (value instanceof Boolean) {
```

### ConstantValue
Condition `value instanceof Boolean` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		} else if (value instanceof String) {
			return new StringObject((String) value);
		} else if (value instanceof Boolean) {
			return new BooleanObject((Boolean) value);
		}
```

### ConstantValue
Condition `object instanceof RDD` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
				if (object instanceof JavaRDD<?>) {
					type = "JavaRDD";
				} else if (object instanceof RDD<?>) {
					type = "RDD";
				}
```

### ConstantValue
Condition `!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT)` is always `true`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			if ((dataType != DataTypes.DoubleType) && (dataType != DataTypes.IntegerType)
					&& (dataType != DataTypes.LongType) && (!(dataType instanceof org.apache.spark.ml.linalg.VectorUDT))
					&& (!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT))) {
				// uncomment if we support arrays of doubles for matrices
				// if (dataType instanceof ArrayType) {
```

### ConstantValue
Value `dataType` is always 'null'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			if ((dataType != DataTypes.DoubleType) && (dataType != DataTypes.IntegerType)
					&& (dataType != DataTypes.LongType) && (!(dataType instanceof org.apache.spark.ml.linalg.VectorUDT))
					&& (!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT))) {
				// uncomment if we support arrays of doubles for matrices
				// if (dataType instanceof ArrayType) {
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/NaryOp.java`
#### Snippet
```java
			&& getInput().size() == that2.getInput().size());
		for( int i=0; i<getInput().size() && ret; i++ )
			ret &= (getInput().get(i) == that2.getInput().get(i));
		return ret;
	}
```

### ConstantValue
Condition `list != null` is always `true`
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
		String textVal = null;
		NodeList list = element.getElementsByTagName(tagName);
		if (list != null && list.getLength() > 0) {
			Element elem = (Element) list.item(0);
			textVal = elem.getFirstChild().getNodeValue();
```

### ConstantValue
Condition `list != null` is always `true`
in `src/main/java/org/apache/sysds/conf/DMLConfig.java`
#### Snippet
```java
		if(_xmlRoot != null) {
			NodeList list = _xmlRoot.getElementsByTagName(paramName);
			if (list != null && list.getLength() > 0) {
				Element elem = (Element) list.item(0);
				elem.getFirstChild().setNodeValue(paramValue);
```

### ConstantValue
Condition `mf == null` is always `false`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		}

		if (mf == null) {
			throw new MLContextException("DataFrame format not recognized as an accepted SystemDS MatrixFormat");
		}
```

### ConstantValue
Condition `imageHeightHop == filterHeightHop` is always `true`
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		}
		
		if(imageHeightHop == filterHeightHop && _cachedParams.R < 0 && _cachedParams.H > 0) {
			// Unknown R, but known H and both are equal
			// This happens for one-dimensional conv2d where H=R and H can be inferred from the parent hop
```

### ConstantValue
Value `imageHeightHop` is always 'null'
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		}
		
		if(imageHeightHop == filterHeightHop && _cachedParams.R < 0 && _cachedParams.H > 0) {
			// Unknown R, but known H and both are equal
			// This happens for one-dimensional conv2d where H=R and H can be inferred from the parent hop
```

### ConstantValue
Value `filterHeightHop` is always 'null'
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		}
		
		if(imageHeightHop == filterHeightHop && _cachedParams.R < 0 && _cachedParams.H > 0) {
			// Unknown R, but known H and both are equal
			// This happens for one-dimensional conv2d where H=R and H can be inferred from the parent hop
```

### ConstantValue
Condition `cpIntermediates.size() > 0` is always `false` when reached
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		}
		
		if(gpuIntermediates.size() > 0 || cpIntermediates.size() > 0)
			return computeIntermediateMemEstimateHelper(gpuIntermediates, cpIntermediates);
		else
```

### ConstantValue
Result of `cpIntermediates.size()` is always '0'
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
		}
		
		if(gpuIntermediates.size() > 0 || cpIntermediates.size() > 0)
			return computeIntermediateMemEstimateHelper(gpuIntermediates, cpIntermediates);
		else
```

### ConstantValue
Condition `lret!=Double.MAX_VALUE` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
				}
				//exploit min constraint to propagate 
				else if( broot.getOp()==OpOp2.MIN && (lret!=Double.MAX_VALUE || rret!=Double.MAX_VALUE) )
				{
					ret = Math.min(lret, rret);
```

### ConstantValue
Condition `lret!=Double.MAX_VALUE || rret!=Double.MAX_VALUE` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
				}
				//exploit min constraint to propagate 
				else if( broot.getOp()==OpOp2.MIN && (lret!=Double.MAX_VALUE || rret!=Double.MAX_VALUE) )
				{
					ret = Math.min(lret, rret);
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		//early abort if rewrite globally not allowed
		if( !ALLOW_CTABLE_SEQUENCE_REWRITES )
			return ret;
		
		try
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
						if( dgop.getOp() == OpOpDG.SEQ ){
							Hop incr = dgop.getInput().get(dgop.getParamIndex(Statement.SEQ_INCR));
							ret |= (incr instanceof LiteralOp && HopRewriteUtils.getDoubleValue((LiteralOp)incr)==1)
								   || dgop.getIncrementValue()==1.0; //set by recompiler;
						}
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		//early abort if rewrite globally not allowed
		if( !ALLOW_CTABLE_SEQUENCE_REWRITES || _op!=OpOp3.CTABLE )
			return ret;
		
		try
```

### ConstantValue
Value `ret` is always 'null'
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		}
		
		return ret;
	}
	
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/TernaryOp.java`
#### Snippet
```java
		ret &= (_dimInputsPresent == that2._dimInputsPresent);
		if( ret && _dimInputsPresent ){
			ret &= getInput().get(3) == that2.getInput().get(3)
				&& getInput().get(4) == that2.getInput().get(4);
		}
```

### ConstantValue
Value `callsND` is always 'true'
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassFlagNonDeterminism.java`
#### Snippet
```java
					Hop.resetVisitStatus(sb.getHops());
					if (callsND)
						sb.setNondeterministic(callsND);
				}
			}
```

### ConstantValue
Condition `InterProceduralAnalysis.INLINING_MAX_NUM_OPS > 0` is always `true`
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassInlineFunctions.java`
#### Snippet
```java
	@Override
	public boolean isApplicable(FunctionCallGraph fgraph) {
		return InterProceduralAnalysis.INLINING_MAX_NUM_OPS > 0;
	}
	
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/QuaternaryOp.java`
#### Snippet
```java
		//check for 4th argument if same size (see above)
		if( ret && getInput().size()==4 ) 
			ret &= (getInput().get(3) == that2.getInput().get(3));
		
		//compare specific parameters
```

### ConstantValue
Condition `m2_dim1 >= 1` is always `true`
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
			
		// broadcast-based left indexing w/o shuffle for small left/right inputs
		if( m2_dim1 >= 1 && m2_dim2 >= 1 && m2_dim1 >= 1 && m2_dim2 >= 1 ) { //lhs/rhs known
			boolean isAligned = (rhsDt == DataType.MATRIX) &&
					((m1_dim1 == m2_dim1 && m1_dim2 <= m1_blen) || (m1_dim2 == m2_dim2 && m1_dim1 <= m1_blen));
```

### ConstantValue
Condition `m2_dim2 >= 1` is always `true`
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
			
		// broadcast-based left indexing w/o shuffle for small left/right inputs
		if( m2_dim1 >= 1 && m2_dim2 >= 1 && m2_dim1 >= 1 && m2_dim2 >= 1 ) { //lhs/rhs known
			boolean isAligned = (rhsDt == DataType.MATRIX) &&
					((m1_dim1 == m2_dim1 && m1_dim2 <= m1_blen) || (m1_dim2 == m2_dim2 && m1_dim1 <= m1_blen));
```

### ConstantValue
Condition `InterProceduralAnalysis.APPLY_STATIC_REWRITES || InterProceduralAnalysis.APPLY_DYNAMIC_REWRITES` is always `true`
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassApplyStaticAndDynamicHopRewrites.java`
#### Snippet
```java
	@SuppressWarnings("unused")
	public boolean isApplicable(FunctionCallGraph fgraph) {
		return InterProceduralAnalysis.APPLY_STATIC_REWRITES
			|| InterProceduralAnalysis.APPLY_DYNAMIC_REWRITES;
	}
	
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/hops/ipa/FunctionCallGraph.java`
#### Snippet
```java
		boolean ret = false;
		if( hop.isVisited() )
			return ret;
		
		//recursively process all child nodes
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/hops/ipa/FunctionCallGraph.java`
#### Snippet
```java
			//prevent redundant call edges
			if( lfset.contains(lfkey) || fop.getFunctionNamespace().equals(DMLProgram.INTERNAL_NAMESPACE) )
				return ret;

			if( !_fGraph.containsKey(lfkey) )
```

### ConstantValue
Condition `map != null` is always `true`
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassCompressionWorkloadAnalysis.java`
#### Snippet
```java
		}

		return map != null;
	}
}
```

### ConstantValue
Condition `lastSizes != null` is always `false` when reached
in `src/main/java/org/apache/sysds/hops/ipa/InterProceduralAnalysis.java`
#### Snippet
```java
			//early abort without functions or on reached fixpoint
			if( _fgraph.getReachableFunctions().isEmpty() 
				|| (lastSizes != null && lastSizes.equals(fcallSizes)) ) {
				if( LOG.isDebugEnabled() )
					LOG.debug("IPA: Early abort after " + (i+1) + "/" + repetitions
```

### ConstantValue
Condition `lastSizes != null && lastSizes.equals(fcallSizes)` is always `false` when reached
in `src/main/java/org/apache/sysds/hops/ipa/InterProceduralAnalysis.java`
#### Snippet
```java
			//early abort without functions or on reached fixpoint
			if( _fgraph.getReachableFunctions().isEmpty() 
				|| (lastSizes != null && lastSizes.equals(fcallSizes)) ) {
				if( LOG.isDebugEnabled() )
					LOG.debug("IPA: Early abort after " + (i+1) + "/" + repetitions
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/ipa/InterProceduralAnalysis.java`
#### Snippet
```java
			//compare output
			MatrixObject mo2 = (MatrixObject)callVars.get(fstmt.getOutputParams().get(0).getName());
			ret &= mo.getNumRows() == mo2.getNumRows() && mo.getNumColumns() == mo2.getNumColumns();
		
			//reset function (note: mo might have been replaced)
```

### ConstantValue
Condition `worstcase` is always `true`
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case MAX:
				case OR:
					ret = worstcase ? Math.min(1, sp1 + sp2) :
						sp1 + sp2 - sp1 * sp2; break;
				case MULT:
```

### ConstantValue
Condition `worstcase` is always `true`
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case MULT:
				case AND:
					ret = worstcase ? Math.min(sp1, sp2) :
						sp1 * sp2; break;
				case DIV:
```

### ConstantValue
Value `recursive` is always 'true'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
			if( recursive )
				for (ProgramBlock pb2 : tmp.getChildBlocks())
					ret += rGetTimeEstimate(pb2, stats, memoFunc, recursive);
			ret *= DEFAULT_NUMITER;
		}
```

### ConstantValue
Value `recursive` is always 'true'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
			if( recursive ) {
				for( ProgramBlock pb2 : tmp.getChildBlocksIfBody() )
					ret += rGetTimeEstimate(pb2, stats, memoFunc, recursive);
				if( tmp.getChildBlocksElseBody()!=null )
					for( ProgramBlock pb2 : tmp.getChildBlocksElseBody() ){
```

### ConstantValue
Value `recursive` is always 'true'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
				if( tmp.getChildBlocksElseBody()!=null )
					for( ProgramBlock pb2 : tmp.getChildBlocksElseBody() ){
						ret += rGetTimeEstimate(pb2, stats, memoFunc, recursive);
						ret /= 2; //weighted sum
					}
```

### ConstantValue
Value `recursive` is always 'true'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
			if( recursive )
				for( ProgramBlock pb2 : tmp.getChildBlocks() )
					ret += rGetTimeEstimate(pb2, stats, memoFunc, recursive);
			
			ret *= getNumIterations(tmp);
```

### ConstantValue
Value `recursive` is always 'true'
in `src/main/java/org/apache/sysds/hops/cost/CostEstimator.java`
#### Snippet
```java
			if( recursive )
				for( ProgramBlock pb2 : tmp.getChildBlocks() )
					ret += rGetTimeEstimate(pb2, stats, memoFunc, recursive);
		}
		else if( pb instanceof BasicProgramBlock ) 
```

### ConstantValue
Condition `rightSparse` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
						if( !leftSparse && !rightSparse )
							return 2 * (d1m * d1n * ((d2n>1)?d1s:1.0) * d2n) /2;
						else if( !leftSparse && rightSparse )
							return 2 * (d1m * d1n * d1s * d2n * d2s) /2;
						else if( leftSparse && !rightSparse )
```

### ConstantValue
Condition `leftSparse` is always `true`
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
						else if( !leftSparse && rightSparse )
							return 2 * (d1m * d1n * d1s * d2n * d2s) /2;
						else if( leftSparse && !rightSparse )
							return 2 * (d1m * d1n * d1s * d2n) /2;
						else //leftSparse && rightSparse
```

### ConstantValue
Condition `m1_rows>=0` is always `true`
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
			if( chainType!=ChainType.NONE && m1_rows >=0 && m1_rows <= m1_blen && m2_cols==1 )
			{
				if( chainType==ChainType.XtXv && m1_rows>=0 && m2_cols>=0 
					&& OptimizerUtils.estimateSize(m1_rows, m2_cols ) < memBudgetExec )
				{
```

### ConstantValue
Condition `m2_cols>=0` is always `true`
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
			if( chainType!=ChainType.NONE && m1_rows >=0 && m1_rows <= m1_blen && m2_cols==1 )
			{
				if( chainType==ChainType.XtXv && m1_rows>=0 && m2_cols>=0 
					&& OptimizerUtils.estimateSize(m1_rows, m2_cols ) < memBudgetExec )
				{
```

### ConstantValue
Condition `m1_rows>=0` is always `true`
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
				}
				else if( (chainType==ChainType.XtwXv || chainType==ChainType.XtXvy ) 
					&& m1_rows>=0 && m2_cols>=0 && m1_cols>=0
					&&   OptimizerUtils.estimateSize(m1_rows, m2_cols) 
					   + OptimizerUtils.estimateSize(m1_cols, m2_cols) < memBudgetExec
```

### ConstantValue
Condition `m2_cols>=0` is always `true`
in `src/main/java/org/apache/sysds/hops/AggBinaryOp.java`
#### Snippet
```java
				}
				else if( (chainType==ChainType.XtwXv || chainType==ChainType.XtXvy ) 
					&& m1_rows>=0 && m2_cols>=0 && m1_cols>=0
					&&   OptimizerUtils.estimateSize(m1_rows, m2_cols) 
					   + OptimizerUtils.estimateSize(m1_cols, m2_cols) < memBudgetExec
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeTpl.java`
#### Snippet
```java
		//process childs recursively
		for( int i=0; ret && i<current1.getInput().size(); i++ )
			ret &= equalInputReferences(
					current1.getInput().get(i), current2.getInput().get(i), input1, input2);
		
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeTpl.java`
#### Snippet
```java
		
		if( ret && current1 instanceof CNodeData ) {
			ret &= current2 instanceof CNodeData
				&& indexOf(input1, (CNodeData)current1)
				== indexOf(input2, (CNodeData)current2);
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeTpl.java`
#### Snippet
```java
		boolean ret = (current1.size() == current2.size());
		for( int i=0; ret && i<current1.size(); i++ )
			ret &= equalInputReferences(current1.get(i), current2.get(i), input1, input2);
		return ret;
	}
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
	public static Hop optimize( Hop root, boolean recompile ) {
		if( root == null )
			return root;
		return optimize(new ArrayList<>(
			Collections.singleton(root)), recompile).get(0);
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
				ret = true;
			else
				ret |= rHasLookupRC1(tmp, mainInput, includeRC1);
		}
		return ret;
```

### ConstantValue
Value `roots` is always 'null'
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java
	public static ArrayList<Hop> generateCodeFromHopDAGs(ArrayList<Hop> roots) {
		if( roots == null )
			return roots;

		ArrayList<Hop> optimized = SpoofCompiler.optimize(roots, false);
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
			boolean ret = (_inputs.size() == that._inputs.size());
			for( int i=0; i<_inputs.size() && ret; i++ )
				ret &= (_inputs.get(i)._ID == that._inputs.get(i)._ID);
			return ret;
		}
```

### ConstantValue
Condition `!current.isEmpty()` is always `true`
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
			}
		}
		if( !current.isEmpty() )
			candCS.add(current);
		
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
		boolean ret = true;
		for(int i=0; i<cs.posCut.length && ret; i++)
			ret &= plan[cs.posCut[i]];
		return ret;
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNode.java`
#### Snippet
```java
		boolean ret = _inputs.size() == cthat._inputs.size();
		for( int i=0; i<_inputs.size() && ret; i++ )
			ret &= _inputs.get(i).equals(cthat._inputs.get(i));
		return ret 
			&& (_output == cthat._output || _output.equals(cthat._output))
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		for( int i=1; ret && i<3; i++ ) {
			if( me.isPlanRef(i) )
				ret &= HopRewriteUtils.isEqualSize(refSize, 
					memo.getHopRefs().get(me.input(i)).getInput().get(0));
		}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
					if( i != j )
						probe.add(me.input(j));
				ret &= rCheckMultiAggregate(memo.getHopRefs().get(me.input(i)), probe);
			}
		return ret;
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		boolean ret = true;
		for( int i=0; ret && i<3; i++ )
			ret &= (!M.contains(me.input(i)) || !plan[M.indexOf(me.input(i))]);
		return ret;
	}
```

### ConstantValue
Condition `bestPlan == null` is always `false`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
			
			//cost comparisons
			if( bestPlan == null || C < bestC ) {
				bestC = C;
				bestPlan = plan;
```

### ConstantValue
Condition `bestPlan==null` is always `false`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
		
		//copy best plan w/o fixed offset plan
		return (bestPlan==null) ? new boolean[Mlen] :
			Arrays.copyOfRange(bestPlan, off, bestPlan.length);
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
		for( int i=1; ret && i<3; i++ ) {
			if( me.isPlanRef(i) )
				ret &= HopRewriteUtils.isEqualSize(refSize, 
					memo.getHopRefs().get(me.input(i)).getInput().get(0));
		}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
					if( i != j )
						probe.add(me.input(j));
				ret &= rCheckMultiAggregate(memo.getHopRefs().get(me.input(i)), probe);
			}
		return ret;
```

### ConstantValue
Condition `applyInner` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
				for( int i=0; i<3 & applyInner; i++ )
					if( me.isPlanRef(i) )
						applyInner &= !memo.contains(me.input(i), TemplateType.ROW);
				
				if( applyLeaf || applyInner ) {
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
			Hop root = (roots.get(i) instanceof AggUnaryOp || roots.get(i) 
				instanceof AggBinaryOp) ? roots.get(i).getInput().get(0) : roots.get(i);
			ret &= (HopRewriteUtils.isBinarySparseSafe(root) 
					&& root.getInput().contains(mainInput))
				|| (HopRewriteUtils.isBinary(root, OpOp2.DIV) 
```

### ConstantValue
Condition `inputsAvail` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMatrixMultChainOptimizationSparse.java`
#### Snippet
```java
		
		for( int i=0; i<chain.size(); i++ ) {
			inputsAvail &= HopRewriteUtils.isData(chain.get(0), OpOpData.TRANSIENTREAD);
			if( inputsAvail )
				sketchArray[i] = new MMNode(getMatrix(chain.get(i).getName(), vars));
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemoveDanglingParentReferences.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		
		//note: the predicate can have an arbitrary root node
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateUtils.java`
#### Snippet
```java
			&& checkContainment(current.getInput(), probe, true);
		for( int i=0; i<current.getInput().size() && !ret; i++ )
			ret |= rContainsOuterProduct(current.getInput().get(i), probe);
		current.setVisited();
		return ret;
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateUtils.java`
#### Snippet
```java
		ret |= HopRewriteUtils.isOuterProductLikeMM(current);
		for( int i=0; i<current.getInput().size() && !ret; i++ )
			ret |= rContainsOuterProduct(current.getInput().get(i));
		current.setVisited();
		return ret;
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemoveUnnecessaryCasts.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		rule_RemoveUnnecessaryCasts( root );
		return root;
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		rule_RemovePersistentDataOp( root );
		return root;
```

### ConstantValue
Value `descendFirst` is always 'true'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
			//process childs recursively first (to allow roll-up)
			if( descendFirst )
				rule_GPUKernels(roots, hi, descendFirst); //see below
			
			if(roots != null) {
```

### ConstantValue
Value `descendFirst` is always 'false'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
	
			if( !descendFirst )
				rule_GPUKernels(roots, hi, descendFirst);
		}

```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		
		//one pass rewrite-descend (rewrite created pattern)
```

### ConstantValue
Value `roots` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
	public ArrayList<Hop> rewriteHopDAGs(ArrayList<Hop> roots, ProgramRewriteStatus state) {
		if( roots == null )
			return roots;

		//one pass rewrite-descend (rewrite created pattern)
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
			
			if( sb instanceof WhileStatementBlock || sb instanceof ForStatementBlock ) {
				ret &= sb.getUpdateInPlaceVars().contains(varname);
			}
			else if( sb instanceof IfStatementBlock ) {
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
				IfStatementBlock isb = (IfStatementBlock) sb;
				IfStatement istmt = (IfStatement)isb.getStatement(0);
				ret &= rIsApplicableForUpdateInPlace(istmt.getIfBody(), varname);
				if( ret && istmt.getElseBody() != null )
					ret &= rIsApplicableForUpdateInPlace(istmt.getElseBody(), varname);	
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
				ret &= rIsApplicableForUpdateInPlace(istmt.getIfBody(), varname);
				if( ret && istmt.getElseBody() != null )
					ret &= rIsApplicableForUpdateInPlace(istmt.getElseBody(), varname);	
			}
			else {
```

### ConstantValue
Condition `sb instanceof ForStatementBlock` is always `true`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteMarkLoopVariablesUpdateInPlace.java`
#### Snippet
```java
							candidates.add(varname);
					}
					else if( sb instanceof ForStatementBlock ) {
						ForStatement wstmt = (ForStatement) sb.getStatement(0);
						if( rIsApplicableForUpdateInPlace(wstmt.getBody(), varname) )
```

### ConstantValue
Value `roots` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteIndexingVectorization.java`
#### Snippet
```java
	public ArrayList<Hop> rewriteHopDAGs(ArrayList<Hop> roots, ProgramRewriteStatus state) {
		if( roots == null )
			return roots;
		for( Hop h : roots )
			rule_IndexingVectorization( h );
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteIndexingVectorization.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		rule_IndexingVectorization( root );
		return root;
```

### ConstantValue
Condition `satisfies` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		boolean satisfies = false;
		if(satisfiesSizeConstraintsForCompression(hop))
			satisfies |= HopRewriteUtils.isData(hop, OpOpData.PERSISTENTREAD);

		return satisfies;
```

### ConstantValue
Condition `funcParamMap != null` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/fedplanner/PrivacyConstraintLoader.java`
#### Snippet
```java
					String funcName = ((FunctionOp) sbHop).getFunctionName();
					Map<String, Hop> funcParamMap = FederatedPlannerUtils.getParamMap((FunctionOp) sbHop);
					if ( paramMap != null && funcParamMap != null)
						funcParamMap.putAll(paramMap);
					paramMap = funcParamMap;
```

### ConstantValue
Condition `hnew != null` is always `true`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java

				//modify dag if nnz is called on the output of removeEmpty
				if( hnew != null ){
					HopRewriteUtils.replaceChildReference(parent, hi, hnew, pos);
					hi = hnew;
```

### ConstantValue
Value `descendFirst` is always 'true'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			//process childs recursively first (to allow roll-up)
			if( descendFirst )
				rule_AlgebraicSimplification(hi, descendFirst); //see below
			
			//apply actual simplification rewrites (of childs incl checks)
```

### ConstantValue
Value `descendFirst` is always 'false'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			//process childs recursively after rewrites (to investigate pattern newly created by rewrites)
			if( !descendFirst )
				rule_AlgebraicSimplification(hi, descendFirst);
		}

```

### ConstantValue
Value `roots` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
	{
		if( roots == null )
			return roots;

		//one pass rewrite-descend (rewrite created pattern)
```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
	{
		if( root == null )
			return root;
		
		//one pass rewrite-descend (rewrite created pattern)
```

### ConstantValue
Condition `funcParamMap != null` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/fedplanner/FederatedPlannerCostbased.java`
#### Snippet
```java
					String funcNamespace = ((FunctionOp) sbHop).getFunctionNamespace();
					Map<String, Hop> funcParamMap = FederatedPlannerUtils.getParamMap((FunctionOp) sbHop);
					if ( paramMap != null && funcParamMap != null)
						funcParamMap.putAll(paramMap);
					paramMap = funcParamMap;
```

### ConstantValue
Condition `constant!=null` is always `true`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
					{
						ScalarObject constant = HopRewriteUtils.getScalarObject((LiteralOp)hop.getInput().get(0));
						if( constant!=null )
							vars.put(varName, constant);
					}
```

### ConstantValue
Condition `constant!=null` is always `true`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
					{
						ScalarObject constant = HopRewriteUtils.getScalarObject((LiteralOp)hop.getInput().get(0));
						if( constant!=null )
							vars.put(varName, constant);
					}
```

### ConstantValue
Condition `elseVar==null` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
				dat2 = elseVar;
			}
			else if( ifVar!=null && elseVar==null ){ //only if
				dat1 = origVar;
				dat2 = ifVar;
```

### ConstantValue
Condition `dat1 instanceof MatrixObject` is always `true`
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			{
				//handle matrices
				if( dat1 instanceof MatrixObject && dat2 instanceof MatrixObject )
				{
					MatrixObject moOld = (MatrixObject) dat1;
```

### ConstantValue
Condition `elseVar==null` is always `true` when reached
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
				dat2 = elseVar;
			}
			else if( ifVar!=null && elseVar==null ){ //only if
				dat1 = origVar;
				dat2 = ifVar;
```

### ConstantValue
Condition `input3!=null` is always `false`
in `src/main/java/org/apache/sysds/lops/CentralMoment.java`
#### Snippet
```java
				getExecType().toString(), "cm",
				getInputs().get(0).prepInputOperand(input1),
				getInputs().get((input3!=null)?2:1).prepScalarInputOperand(getExecType()),
				prepOutputOperand(output));
		}
```

### ConstantValue
Condition `input3!=null` is always `true`
in `src/main/java/org/apache/sysds/lops/CentralMoment.java`
#### Snippet
```java
				getInputs().get(0).prepInputOperand(input1),
				getInputs().get(1).prepInputOperand(input2),
				getInputs().get((input3!=null)?2:1).prepScalarInputOperand(getExecType()),
				prepOutputOperand(output));
		}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		boolean ret = false;
		for( int i=0; i<current.getInput().size() && !ret; i++ )
			ret |= rContainsInput(current.getInput().get(i), probe, memo);
		ret |= (current == probe);
		memo.add(current.getHopID());
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/hops/rewrite/HopRewriteUtils.java`
#### Snippet
```java
		boolean ret = hop1.dimsKnown();
		for( int i=0; i<hops.length && ret; i++ )
			ret &= isEqualSize(hop1, hops[i]);
		return ret;	
	}
```

### ConstantValue
Condition `applyRight` is always `true`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
					hi = left;
				}
				else if ( applyRight ) {
					Hop input = right.getInput().get(0);
					HopRewriteUtils.removeChildReferenceByPos(hi, right, 1);
```

### ConstantValue
Value `descendFirst` is always 'true'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
			//process childs recursively first (to allow roll-up)
			if( descendFirst )
				rule_AlgebraicSimplification(hi, descendFirst); //see below
			
			//apply actual simplification rewrites (of childs incl checks)
```

### ConstantValue
Value `descendFirst` is always 'false'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
			//process childs recursively after rewrites (to investigate pattern newly created by rewrites)
			if( !descendFirst )
				rule_AlgebraicSimplification(hi, descendFirst);
		}

```

### ConstantValue
Value `root` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	public Hop rewriteHopDAG(Hop root, ProgramRewriteStatus state) {
		if( root == null )
			return root;
		//one pass rewrite-descend (rewrite created pattern)
		rule_AlgebraicSimplification( root, false );
```

### ConstantValue
Value `roots` is always 'null'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationDynamic.java`
#### Snippet
```java
	public ArrayList<Hop> rewriteHopDAGs(ArrayList<Hop> roots, ProgramRewriteStatus state) {
		if( roots == null )
			return roots;

		//one pass rewrite-descend (rewrite created pattern)
```

### ConstantValue
Condition `_funsOrig != null` is always `true`
in `src/main/java/org/apache/sysds/parser/FunctionDictionary.java`
#### Snippet
```java
		//obtain optimized or unoptimized function (null if not available)
		return opt ? _funs.get(fname) : 
			(_funsOrig != null) ? _funsOrig.get(fname) : null;
	}
	
```

### ConstantValue
Condition `_right instanceof BooleanIdentifier` is always `true`
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java
					else
						this.setLeft(new IntIdentifier(0, _left));
				} else if (_right instanceof BooleanIdentifier) {
					if (((BooleanIdentifier) _right).getValue())
						this.setRight(new IntIdentifier(1, _right));
```

### ConstantValue
Condition `v2 != ValueType.UNKNOWN` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		if (v1 == v2)
			return v1;
		if (v1 == ValueType.UNKNOWN && v2 != ValueType.UNKNOWN)
			return v2;
		if (v1 != ValueType.UNKNOWN && v2 == ValueType.UNKNOWN)
```

### ConstantValue
Condition `v1 != ValueType.UNKNOWN` is always `true`
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
		if (v1 == ValueType.UNKNOWN && v2 != ValueType.UNKNOWN)
			return v2;
		if (v1 != ValueType.UNKNOWN && v2 == ValueType.UNKNOWN)
			return v1;

```

### ConstantValue
Condition `inst instanceof FunctionCallCPInstruction` is always `false`
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
		if( inst instanceof SPInstruction ) {
			opcode = "SP_"+InstructionUtils.getOpCode(inst.toString());
			if( inst instanceof FunctionCallCPInstruction ) {
				FunctionCallCPInstruction extfunct = (FunctionCallCPInstruction)inst;
				opcode = extfunct.getFunctionName();
```

### ConstantValue
Condition `wrapIter < wrappedInstruction.length` is always `true`
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			
			for(int wrapIter = 0; wrapIter < numLines; wrapIter++) {
				String instStr = (wrapIter < wrappedInstruction.length) ? wrappedInstruction[wrapIter] : "";
				if(wrapIter == 0) {
					// Display instruction count
```

### ConstantValue
Condition `_opcode == Expression.BooleanOp.CONDITIONALOR` is always `true`
in `src/main/java/org/apache/sysds/parser/BooleanExpression.java`
#### Snippet
```java
			if (_opcode == Expression.BooleanOp.CONDITIONALAND) {
				raiseValidateError("Conditional AND (&&) not supported.", false);
			} else if (_opcode == Expression.BooleanOp.CONDITIONALOR) {
				raiseValidateError("Conditional OR (||) not supported.", false);
			}
```

### ConstantValue
Condition `!sizeUnchanged || !nnzUnchanged` is always `true`
in `src/main/java/org/apache/sysds/parser/WhileStatementBlock.java`
#### Snippet
```java
				
				// IF size has changed -- 
				if (!sizeUnchanged || !nnzUnchanged){
					revalidationRequired = true;
					DataIdentifier recVersion = new DataIdentifier(endVersion);
```

### ConstantValue
Condition `!sizeUnchanged || !nnzUnchanged` is always `true`
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
				
				// IF size has changed -- 
				if (!sizeUnchanged || !nnzUnchanged){
					revalidationRequired = true;
					DataIdentifier recVersion = new DataIdentifier(endVersion);
```

### ConstantValue
Condition `source instanceof DataIdentifier` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		//User-defined, and Multi-return Builtin function expressions
		if (!(source instanceof DataIdentifier)
				|| (source instanceof DataIdentifier && !((DataIdentifier)source).multipleReturns()) ) {
			source.raiseValidateError("can only use user-defined functions with multi-assignment statement", conditional);
		}
```

### ConstantValue
Condition `source instanceof BuiltinFunctionExpression || source instanceof ParameterizedBuiltinFunctionExpress...` is always `true`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			}
		}
		else if ( source instanceof BuiltinFunctionExpression || source instanceof ParameterizedBuiltinFunctionExpression ) {
			Identifier[] outputs = source.getOutputs();
			for (int j=0; j < targetList.size(); j++) {
```

### ConstantValue
Condition `source instanceof ParameterizedBuiltinFunctionExpression` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			}
		}
		else if ( source instanceof BuiltinFunctionExpression || source instanceof ParameterizedBuiltinFunctionExpression ) {
			Identifier[] outputs = source.getOutputs();
			for (int j=0; j < targetList.size(); j++) {
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					FunctionCallIdentifier fcall = (FunctionCallIdentifier) as.getSource();
					FunctionStatementBlock fblock2 = prog.getFunctionStatementBlock(fcall.getNamespace(), fcall.getName());
					ret &= rIsInlineableFunction(fblock2, prog);
					if( as.getSource().toString().contains(DataExpression.FORMAT_TYPE + "=" + FileFormat.CSV.toString())
						&& as.getSource().toString().contains("read"))
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
						FunctionCallIdentifier fcall = (FunctionCallIdentifier) ((MultiAssignmentStatement)s).getSource();
						FunctionStatementBlock fblock2 = prog.getFunctionStatementBlock(fcall.getNamespace(), fcall.getName());
						ret &= rIsInlineableFunction(fblock2, prog);
						if( !ret ) return false;
					}
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		}
	
		return ret;
	}

```

### ConstantValue
Condition `current instanceof MultiAssignmentStatement` is always `true`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				}
			}
			else if (current instanceof MultiAssignmentStatement) {
				if (fstmt.getOutputParams().size() == 0) {
					MultiAssignmentStatement mas = (MultiAssignmentStatement) current;
```

### ConstantValue
Condition `colwise` is always `true`
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
			if ( numGroups instanceof ConstIdentifier) {
				long ngroups = ((ConstIdentifier)numGroups).getLongValue();
				if ( colwise ) {
					outputDim1 = ngroups;
					outputDim2 = matrix ? exprTarget.getOutput().getDim2() : 1;
```

### ConstantValue
Condition `tryOptimizedParsing` is always `false`
in `src/main/java/org/apache/sysds/parser/dml/DMLParserWrapper.java`
#### Snippet
```java
			boolean tryOptimizedParsing = false; // For now no optimization, since it is not able to parse integer value. 
	
			if(tryOptimizedParsing) {
				// Try faster and simpler SLL
				antlr4Parser.getInterpreter().setPredictionMode(PredictionMode.SLL);
```

### ConstantValue
Condition `blen > 0` is always `true`
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
			//create synchronization points for MCSR (start row per block row)
			if( sblock instanceof SparseBlockMCSR && clen > blen      //multiple col blocks 
				&& clen >= 0 && blen > 0 && rlen >= 0 && blen > 0 ) {  //all dims known
				//note: allocate w/ min 2 nnz to ensure allocated row object because
				//adaptive change from scalar to row could cause synchronization issues
```

### ConstantValue
Condition `(dim1 != null) || (dim2 != null)` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					if (dim1 != null && dim2 != null){
						getOutput().setDimensions(dim1, dim2);
					} else if (!isCSV && ((dim1 != null) || (dim2 != null))) {
						raiseValidateError("Partial dimension information in read statement", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
					}
```

### ConstantValue
Condition `dim1 != null` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					if (dim1 != null && dim2 != null){
						getOutput().setDimensions(dim1, dim2);
					} else if (!isCSV && ((dim1 != null) || (dim2 != null))) {
						raiseValidateError("Partial dimension information in read statement", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
					}
```

### ConstantValue
Condition `passedParamExprs.size() == 0` is always `false`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		}
		
		ParameterExpression pexpr = (passedParamExprs.size() == 0) ? null : passedParamExprs.get(0);
		
		if ( (pexpr != null) &&  (!(pexpr.getName() == null) || (pexpr.getName() != null && pexpr.getName().equalsIgnoreCase(DataExpression.IO_FILENAME)))){
```

### ConstantValue
Condition `temp instanceof JSONArray` is always `false`
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONL.java`
#### Snippet
```java
				} else if (temp instanceof JSONObject) {
					temp = ((JSONObject) temp).get(split);
				} else if (temp instanceof JSONArray) {
					throw new IOException("Cannot traverse JSON Array in a meaningful manner " + split);
				} else {
```

### ConstantValue
Condition `s instanceof ParForStatement` is always `false` when reached
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			
			for( Statement s : sb._statements ) {
				if( s instanceof ForStatement || s instanceof ParForStatement )
					rConsolidateResultVars(((ForStatement)s).getBody(), vars);
				else if( s instanceof WhileStatement ) 
```

### ConstantValue
Value `hop` is always 'null'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
	
	private static Hop resetVisitStatus(Hop hop) {
		return hop == null ? hop :
			hop.resetVisitStatus();
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			boolean ret = ( _b.length == f2._b.length );
			for( int i=0; i<_b.length && ret; i++ ) {
				ret &= (_b[i] == f2._b[i] );
				//note robustness for null var names 
				String var1 = String.valueOf(_vars[i]);
```

### ConstantValue
Value `lFlag` is always 'false'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				if( !lFlag )
					if( fs.getBody() != null )
						rDetermineBounds(fs.getBody(), lFlag);
			}
			else if( s instanceof ForStatement ) {
```

### ConstantValue
Condition `sub1a instanceof IntIdentifier` is always `true`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					_bounds._increment.put(id, 1L);
				}
				else if( sub1a instanceof IntIdentifier && sub1b instanceof IntIdentifier ) {
					_bounds._lower.put(id, ((IntIdentifier)sub1a).getValue());
					_bounds._upper.put(id, ((IntIdentifier)sub1b).getValue()); 
```

### ConstantValue
Condition `sub1a instanceof IntIdentifier && sub1b instanceof IntIdentifier` is always `true`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					_bounds._increment.put(id, 1L);
				}
				else if( sub1a instanceof IntIdentifier && sub1b instanceof IntIdentifier ) {
					_bounds._lower.put(id, ((IntIdentifier)sub1a).getValue());
					_bounds._upper.put(id, ((IntIdentifier)sub1b).getValue()); 
```

### ConstantValue
Condition `sub1b instanceof IntIdentifier` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					_bounds._increment.put(id, 1L);
				}
				else if( sub1a instanceof IntIdentifier && sub1b instanceof IntIdentifier ) {
					_bounds._lower.put(id, ((IntIdentifier)sub1a).getValue());
					_bounds._upper.put(id, ((IntIdentifier)sub1b).getValue()); 
```

### ConstantValue
Condition `sub2a instanceof IntIdentifier` is always `true`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
						_bounds._increment.put(id, 1L);					
					}
					else if( sub2a instanceof IntIdentifier && sub2b instanceof IntIdentifier )
					{
						_bounds._lower.put(id, ((IntIdentifier)sub2a).getValue());
```

### ConstantValue
Condition `sub2a instanceof IntIdentifier && sub2b instanceof IntIdentifier` is always `true`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
						_bounds._increment.put(id, 1L);					
					}
					else if( sub2a instanceof IntIdentifier && sub2b instanceof IntIdentifier )
					{
						_bounds._lower.put(id, ((IntIdentifier)sub2a).getValue());
```

### ConstantValue
Condition `sub2b instanceof IntIdentifier` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
						_bounds._increment.put(id, 1L);					
					}
					else if( sub2a instanceof IntIdentifier && sub2b instanceof IntIdentifier )
					{
						_bounds._lower.put(id, ((IntIdentifier)sub2a).getValue());
```

### ConstantValue
Condition `dat2 instanceof IndexedIdentifier` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		{
			//NOTE: cases both and none negligible already covered (constant check, general case) 
			boolean ixid = (dat1 instanceof IndexedIdentifier && dat2 instanceof IndexedIdentifier); 
			boolean ignoreRow = ixid && isRowIgnorable((IndexedIdentifier)dat1, (IndexedIdentifier)dat2);
			boolean ignoreCol = ixid && isColumnIgnorable((IndexedIdentifier)dat1, (IndexedIdentifier)dat2);
```

### ConstantValue
Value `prog` is always 'null'
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			catch (ParseException e) {
				notifyErrorListeners(e.getMessage(), ctx.start);
				return prog;
			}
			if(prog == null) {
```

### ConstantValue
Value `prog` is always 'null'
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			if(prog == null) {
				notifyErrorListeners("One or more errors found during importing a program from file " + filePath, ctx.start);
				return prog;
			}
		}
```

### ConstantValue
Condition `!isRowUpper` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				rowIndices.add(rowUpper.expr);
			}
			else if(isRowLower && !isRowUpper) {
				// only row set
				rowIndices.add(rowLower.expr);
```

### ConstantValue
Condition `!isColUpper` is always `true` when reached
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				colIndices.add(colUpper.expr);
			}
			else if(isColLower && !isColUpper) {
				colIndices.add(colLower.expr);
			}
```

### ConstantValue
Value `parForParamValues` is always 'null'
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
		}
		IterablePredicate predicate = new IterablePredicate(ctx, iterVar, ctx.iterPred.info.from, ctx.iterPred.info.to,
				incrementExpr, parForParamValues, currentFile);
		forStmt.setPredicate(predicate);

```

### ConstantValue
Condition `numParams > 1` is always `true`
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				return;
			}
		} else if (numParams > 1) {
			if ("stop".equals(functionName)) {
				notifyErrorListeners("stop() function cannot have more than 1 parameter", ctx.start);
```

### ConstantValue
Condition `mtdx` is always `true`
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCSV.java`
#### Snippet
```java
						for(int j = 0; j < dest.getNumColumns(); j++)
							dest.getColumnMetadata(j).setMvValue(parts[j + 1]);
					else if(mtdx)
						for(int j = 0; j < dest.getNumColumns(); j++)
							dest.getColumnMetadata(j).setNumDistinct(Long.parseLong(parts[j + 1]));
```

### ConstantValue
Value `fWriteRead` is always 'true'
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			for (int i = 0; i < dmlp.getNumStatementBlocks(); i++) {
				StatementBlock sb = dmlp.getStatementBlock(i);
				vs = sb.validate(dmlp, vs, constVars, fWriteRead);
				constVars = sb.getConstOut();
			}
```

### ConstantValue
Condition `passedSB instanceof IfStatementBlock` is always `true`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		if (passedSB instanceof WhileStatementBlock)
			((WhileStatementBlock)passedSB).setPredicateHops(predicateHops);
		else if (passedSB instanceof IfStatementBlock)
			((IfStatementBlock)passedSB).setPredicateHops(predicateHops);
	}
```

### ConstantValue
Condition `val instanceof JSONArray` is always `false`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
								naStrings = val.toString();
							}
							else if (val instanceof JSONArray) {
								StringBuilder sb = new StringBuilder();
								JSONArray valarr = (JSONArray)val;
```

### ConstantValue
Condition `val instanceof JSONObject` is always `false`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
								naStrings = sb.toString();
							}
							else if ( val instanceof JSONObject ){
								JSONObject valJsonObject = (JSONObject)val;
								naStrings = valJsonObject.toString();
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
		boolean ret = true;
		for( int i=start+1; i<end && ret; i++ )
			ret &= (values[i-1]<values[i]);
		return ret;
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/util/SortUtils.java`
#### Snippet
```java
		boolean ret = true;
		for( int i=start+1; i<end && ret; i++ )
			ret &= (indexes[i-1]<indexes[i]);
		return ret;
	}
```

### ConstantValue
Value `v` is always 'true'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BooleanArray.java`
#### Snippet
```java
				final boolean v = value.get(i);
				if(v)
					_data[i] = v;
			}
		}
```

### ConstantValue
Value `v` is always 'true'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BooleanArray.java`
#### Snippet
```java
			boolean v = UtilFunctions.objectToBoolean(vt, value.get(i));
			if(v)
				_data[i] = v;
		}
	}
```

### ConstantValue
Value `strReturn` is always 'null'
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		String strReturn = objectToString(in); 
		if( strReturn == null )
			return strReturn;
		else if (ignoreNull){
			if(in instanceof Double && ((Double)in).doubleValue() == 0.0)
```

### ConstantValue
Condition `in instanceof Long && ((Integer)in).intValue() == 0` is always `false`
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			else if(in instanceof Long && ((Long)in).longValue() == 0)
				return null;
			else if(in instanceof Long && ((Integer)in).intValue() == 0)
				return null;
			else if(in instanceof Boolean && ((Boolean)in).booleanValue() == false)
```

### ConstantValue
Condition `((Integer)in).intValue() == 0` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			else if(in instanceof Long && ((Long)in).longValue() == 0)
				return null;
			else if(in instanceof Long && ((Integer)in).intValue() == 0)
				return null;
			else if(in instanceof Boolean && ((Boolean)in).booleanValue() == false)
```

### ConstantValue
Condition `dateCol < 0` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
		}

		if (maxMatches <= 0 || dateCol < 0){
			//ERROR - no date column found
			System.out.println("No date column in the dataset");
```

### ConstantValue
Value `v` is always 'true'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BitSetArray.java`
#### Snippet
```java
			boolean v = UtilFunctions.objectToBoolean(vt, value.get(i));
			if(v)
				set(i, v);
		}
	}
```

### ConstantValue
Value `v` is always 'true'
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BitSetArray.java`
#### Snippet
```java
				final boolean v = value.get(i);
				if(v)
					set(i, v);
			}
		}
```

### ConstantValue
Condition `pb instanceof ParForProgramBlock` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
				rFindSerializationCandidates(wpb.getChildBlocks(), cand);
			}
			else if ( pb instanceof ForProgramBlock || pb instanceof ParForProgramBlock ) {
				ForProgramBlock fpb = (ForProgramBlock) pb; 
				rFindSerializationCandidates(fpb.getChildBlocks(), cand);
```

### ConstantValue
Value `isUpperTriangular` is always 'false'
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
				// Lower Triangular
				isUpperTriangular = false;
				transferSampleTriangular(isUpperTriangular);
				mapped = isRR ? findMapping() : findMapping() && verifyRISymmetricMapping(isUpperTriangular);

```

### ConstantValue
Value `isUpperTriangular` is always 'false'
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
				// Lower Triangular
				isUpperTriangular = false;
				transferSampleTriangular(isUpperTriangular);
				mapped = isRR ? findMapping() : findMapping() && verifyRISymmetricMapping(isUpperTriangular);

```

### ConstantValue
Condition `minDelimLength == -1` is always `true`
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
				Pair<HashSet<String>, Integer> pair = row.getDelimsSet();
				delims.addAll(pair.getKey());
				minDelimLength = minDelimLength == -1 ? pair.getValue() : Math.min(minDelimLength, pair.getValue());
			}
		}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		boolean ret = true;
		for(int j = 0; j < getNumColumns() && ret; j++)
			ret &= isColumnMetadataDefault(j);
		return ret;
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		boolean ret = true;
		for(int j = 0; j < _schema.length && ret; j++)
			ret &= _coldata[j].isShallowSerialize();
		return ret;
	}
```

### ConstantValue
Value `tmp` is always 'null'
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
			for(int i = 0; i < getNumRows(); i++) {
				Object tmp = get(i, j);
				set(i, j, (tmp == null) ? tmp : UtilFunctions.objectToObject(_schema[j], fun.apply(tmp.toString())));
			}
	}
```

### ConstantValue
Condition `!(dataValue == null)` is always `true`
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
							// compute distance between sample and invalid value
							double simScore = 0;
							if(!(dataValue == null) && !(dataValue2 == null))
								simScore = StringUtils.getLevenshteinDistance(dataValue, dataValue2);
							if(simScore < minSimScore) {
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		boolean ret = (_colnames != null);
		for(int j = 0; j < getNumColumns() && ret; j++)
			ret &= isColNameDefault(j);
		return ret;
	}
```

### ConstantValue
Condition `min==0.0 && max==0.0` is always `false`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		// if invoked from CP: estimated NNZ is for entire matrix (nnz=0, if 0 initialized)
		// if invoked from MR: estimated NNZ is for one block
		final long estnnz = (long) Math.ceil((min==0.0 && max==0.0) ? 0 : sparsity*rows*cols);
		boolean lsparse = MatrixBlock.evalSparseFormatInMemory( rows, cols, estnnz );
		out.reset(rows, cols, lsparse, estnnz);
```

### ConstantValue
Condition `max==0.0` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDatagen.java`
#### Snippet
```java
		// if invoked from CP: estimated NNZ is for entire matrix (nnz=0, if 0 initialized)
		// if invoked from MR: estimated NNZ is for one block
		final long estnnz = (long) Math.ceil((min==0.0 && max==0.0) ? 0 : sparsity*rows*cols);
		boolean lsparse = MatrixBlock.evalSparseFormatInMemory( rows, cols, estnnz );
		out.reset(rows, cols, lsparse, estnnz);
```

### ConstantValue
Condition `!isM2Sparse` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuMatMult.java`
#### Snippet
```java
			denseSparseMatMult(getCusparseHandle(gCtx), instName, C, A, B, params);
			// -------------------------------------------------------------------------------------
		} else if (isM1Sparse && !isM2Sparse) {
			// -------------------------------------------------------------------------------------
			// sparse-dense matrix multiplication
```

### ConstantValue
Condition `ret == null` is always `false`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixNative.java`
#### Snippet
```java
			{
				// allocate output
				if(ret == null)
					ret = new MatrixBlock(m1.rlen, m2.clen, false);
				else 
```

### ConstantValue
Condition `sparse2` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				if( !sparse1 && !sparse2 )
					ret.add(new PoolingBackwardDenseDense(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( !sparse1 && sparse2 )
					ret.add(new PoolingBackwardDenseSparse(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && !sparse2 ) 
```

### ConstantValue
Condition `sparse1` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				else if( !sparse1 && sparse2 )
					ret.add(new PoolingBackwardDenseSparse(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && !sparse2 ) 
					ret.add(new PoolingBackwardSparseDense(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && sparse2 )
```

### ConstantValue
Condition `sparse1` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				else if( sparse1 && !sparse2 ) 
					ret.add(new PoolingBackwardSparseDense(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && sparse2 )
					ret.add(new PoolingBackwardSparseSparse(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
			}
```

### ConstantValue
Condition `sparse1 && sparse2` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				else if( sparse1 && !sparse2 ) 
					ret.add(new PoolingBackwardSparseDense(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && sparse2 )
					ret.add(new PoolingBackwardSparseSparse(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
			}
```

### ConstantValue
Condition `sparse2` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				else if( sparse1 && !sparse2 ) 
					ret.add(new PoolingBackwardSparseDense(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
				else if( sparse1 && sparse2 )
					ret.add(new PoolingBackwardSparseSparse(i*taskSize, Math.min((i+1)*taskSize, params.N), params, performReluBackward));
			}
```

### ConstantValue
Condition `!m1.sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
			else if( m1.sparse ) //SPARSE m1
				safeBinaryMVSparse(m1, m2, ret, op);
			else if( !m1.sparse && !m2.sparse && ret.sparse && op.fn instanceof Multiply
				&& atype == BinaryAccessType.MATRIX_COL_VECTOR
				&& (long)m1.rlen * m2.clen < Integer.MAX_VALUE )
```

### ConstantValue
Condition `ret.sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
			else if( m1.sparse ) //SPARSE m1
				safeBinaryMVSparse(m1, m2, ret, op);
			else if( !m1.sparse && !m2.sparse && ret.sparse && op.fn instanceof Multiply
				&& atype == BinaryAccessType.MATRIX_COL_VECTOR
				&& (long)m1.rlen * m2.clen < Integer.MAX_VALUE )
```

### ConstantValue
Condition `a != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
					
				if( isMultiply && v2==1 ) { //ROW COPY
					if( a != null && !a.isEmpty(i)  )
						ret.appendRow(i, a.get(i));
				}
```

### ConstantValue
Value `isRightTransposed` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		else if(isEmpty1 && clenB != 1 && rlenB != 1) {
			// C = empty_in1 op in2 ==> becomes ==> C = 0.0 op in2
			matrixScalarArithmetic(ec, gCtx, instName, in2, outputName, isRightTransposed, new LeftScalarOperator(op.fn, 0.0));
		}
		// Check for M1 * M2 when M2 is empty; if M1 is a vector then fallback to general case
```

### ConstantValue
Value `isLeftTransposed` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		else if(isEmpty2 && clenA != 1 && rlenA != 1) {
			// C = in1 op empty_in2 ==> becomes ==> C = in1 op 0.0
			matrixScalarArithmetic(ec, gCtx, instName, in1, outputName, isLeftTransposed, new RightScalarOperator(op.fn, 0.0));
		}
		else {
```

### ConstantValue
Condition `reductionDirection == -1` is always `false`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
			throw new DMLRuntimeException("Internal Error - Invalid index function type, only reducing along rows, columns, diagonals or all elements is supported in Aggregate Unary operations");
		}
		if(reductionDirection == -1)
			throw new DMLRuntimeException("Internal Error - Incorrect type of reduction direction set for aggregate unary GPU instruction");

```

### ConstantValue
Condition `opIndex == -1` is always `false`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
			throw new DMLRuntimeException("Internal Error - Aggregate operator has invalid Value function");
		}
		if(opIndex == -1)
			throw new DMLRuntimeException("Internal Error - Incorrect type of operation set for aggregate unary GPU instruction");

```

### ConstantValue
Value `isLeftTransposed` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
			ec.allocateGPUMatrixObject(outputName, outRLen, outCLen);

			if (in1 == in2 && isLeftTransposed == true && isLeftTransposed == isRightTransposed) {
				// Special case for transpose

```

### ConstantValue
Condition `op.fn instanceof Minus` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
				beta = 1.0;
			}
			else if(op.fn instanceof Minus) {
				alpha = 1.0;
				beta = -1.0;
```

### ConstantValue
Condition `inputs==null` is always `false`
in `src/main/java/org/apache/sysds/runtime/codegen/SpoofRowwise.java`
#### Snippet
```java
		
		//sanity check
		if( inputs==null || inputs.size() < 1 || out==null )
			throw new RuntimeException("Invalid input arguments.");
		
```

### ConstantValue
Condition `this.getNumColumns() != that.getNumColumns()` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			throw new DMLRuntimeException("Covariance can be computed only on 1-dimensional column matrices."); 
		}
		if ( this.getNumRows() != that.getNumRows() || this.getNumColumns() != that.getNumColumns()) {
			throw new DMLRuntimeException("Covariance: Mismatching input matrix dimensions - " +
					"["+this.getNumRows()+","+this.getNumColumns() +"] != [" 
```

### ConstantValue
Condition `this.getNumColumns() != weights.getNumColumns()` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
					+ that.getNumRows() + "," + that.getNumColumns() +"]");
		}
		if ( this.getNumRows() != weights.getNumRows() || this.getNumColumns() != weights.getNumColumns()) {
			throw new DMLRuntimeException("Covariance: Mismatching dimensions between input and weight matrices - " +
					"["+this.getNumRows()+","+this.getNumColumns() +"] != [" 
```

### ConstantValue
Condition `sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		boolean sparseDst = evalSparseFormatOnDisk();
		return !sparse || !sparseDst
			|| (sparse && sparseBlock instanceof SparseBlockCSR)
			|| (sparse && sparseBlock instanceof SparseBlockMCSR
				&& getInMemorySize() / MAX_SHALLOW_SERIALIZE_OVERHEAD 
```

### ConstantValue
Condition `sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		return !sparse || !sparseDst
			|| (sparse && sparseBlock instanceof SparseBlockCSR)
			|| (sparse && sparseBlock instanceof SparseBlockMCSR
				&& getInMemorySize() / MAX_SHALLOW_SERIALIZE_OVERHEAD 
				<= getExactSerializedSize())
```

### ConstantValue
Value `sparseDst` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				&& nonZeros < Integer.MAX_VALUE //CSR constraint
				&& inclConvert && CONVERT_MCSR_TO_CSR_ON_DEEP_SERIALIZE
				&& !isUltraSparseSerialize(sparseDst));
	}
	
```

### ConstantValue
Condition `!that.sparse` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		if(sparse && that.sparse)
			copySparseToSparse(that);
		else if(sparse && !that.sparse)
			copyDenseToSparse(that);
		else if(!sparse && that.sparse)
```

### ConstantValue
Condition `!sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		else if(sparse && !that.sparse)
			copyDenseToSparse(that);
		else if(!sparse && that.sparse)
			copySparseToDense(that);
		else
```

### ConstantValue
Condition `isPM` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		boolean isPM = (sblock != null);
		for( int i=0; i<rlen & isPM; i++ )
			isPM &= sblock.isEmpty(i) || sblock.size(i) == 1;
		return isPM;
	}
```

### ConstantValue
Value `ignoreZeros` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				for( int j=0; j<alen; j++ )
					ctable.execute(avals[apos+j], bvals[bpos+j], 
						w, ignoreZeros, resultMap, resultBlock);
			}
		}
```

### ConstantValue
Condition `this.getNumColumns() != weights.getNumColumns()` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			throw new DMLRuntimeException("Central Moment can be computed only on 1-dimensional column matrices.");
		}
		if ( this.getNumRows() != weights.getNumRows() || this.getNumColumns() != weights.getNumColumns()) {
			throw new DMLRuntimeException("Covariance: Mismatching dimensions between input and weight matrices - " +
					"["+this.getNumRows()+","+this.getNumColumns() +"] != [" 
```

### ConstantValue
Condition `aggOp.correction==CorrectionLocationType.LASTCOLUMN` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				if(aggOp.correction==CorrectionLocationType.LASTROW)//extra row
					corRow++;
				else if(aggOp.correction==CorrectionLocationType.LASTCOLUMN)
					corCol++;
				else
```

### ConstantValue
Condition `!src.sparse` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		if(sparse && src.sparse)
			copySparseToSparse(rl, ru, cl, cu, src, awareDestNZ);
		else if(sparse && !src.sparse)
			copyDenseToSparse(rl, ru, cl, cu, src, awareDestNZ);
		else if(!sparse && src.sparse)
```

### ConstantValue
Condition `!sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		else if(sparse && !src.sparse)
			copyDenseToSparse(rl, ru, cl, cu, src, awareDestNZ);
		else if(!sparse && src.sparse)
			copySparseToDense(rl, ru, cl, cu, src, awareDestNZ);
		else
```

### ConstantValue
Condition `this.getNumColumns() != that.getNumColumns()` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			throw new DMLRuntimeException("Covariance can be computed only on 1-dimensional column matrices."); 
		}
		if ( this.getNumRows() != that.getNumRows() || this.getNumColumns() != that.getNumColumns()) {
			throw new DMLRuntimeException("Covariance: Mismatching input matrix dimensions - " +
					"["+this.getNumRows()+","+this.getNumColumns() +"] != [" 
```

### ConstantValue
Condition `pool != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
	public Future<MatrixBlock> allocateBlockAsync() {
		ExecutorService pool = CommonThreadPool.get(InfrastructureAnalyzer.getLocalParallelism());
		return (pool != null) ? pool.submit(() -> allocateBlock()) : //async
			ConcurrentUtils.constantFuture(allocateBlock()); //fallback sync
	}
```

### ConstantValue
Condition `ret` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
			if (!ret) break;

			if (ret && li1._inputs != null && li1._inputs.length == li2._inputs.length || li1.isDedup() || li2.isDedup())
				for (int i=0; i<li1._inputs.length; i++) {
					// Find the i'th inputs. If the input is a non-leaf placeholder, read the inputs to it,
```

### ConstantValue
Condition `ret` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
			ret &= (li1.hashCode() == li2.hashCode());
			if (!ret) break;
			if (ret && li1._inputs != null && li1._inputs.length == li2._inputs.length)
				for (int i=0; i<li1._inputs.length; i++) {
					s1.push(li1.getInputs()[i]);
```

### ConstantValue
Condition `ret` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItem.java`
#### Snippet
```java
			// Do not match the hash codes, as the hash of a dedup patch node doesn't represent the whole dag.
			if (!ret) break;
			if (ret && li1._inputs != null && li1._inputs.length == li2._inputs.length)
				for (int i=0; i<li1._inputs.length; i++) {
					LineageItem in1 = li1.getInputs()[i];
```

### ConstantValue
Value `value` is always 'true'
in `src/main/java/org/apache/sysds/runtime/lineage/Lineage.java`
#### Snippet
```java
	public void setDedupPathBranch(int pos, boolean value) {
		if( _activeDedupBlock != null && value )
			_activeDedupBlock.setPathBranch(pos, value);
	}
	
```

### ConstantValue
Value `rowwise` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		if( rowwise ) {
			if( mcIn.getCols() == 1 ) {
				MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), row_offset, 0, mcIn, mcOut, rowwise);
				MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), max_row_offset, 0, mcIn, mcOut, rowwise);
				createRowwiseIndexes(first, last, mcOut.getNumColBlocks(), ret);
```

### ConstantValue
Value `rowwise` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			if( mcIn.getCols() == 1 ) {
				MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), row_offset, 0, mcIn, mcOut, rowwise);
				MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), max_row_offset, 0, mcIn, mcOut, rowwise);
				createRowwiseIndexes(first, last, mcOut.getNumColBlocks(), ret);
			}
```

### ConstantValue
Value `rowwise` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			}
			else if( in.getNonZeros()<in.getNumRows() && !outputEmpty ) {
				createNonZeroIndexes(mcIn, mcOut, in, row_offset, col_offset, rowwise, ret);
			}
			else {
```

### ConstantValue
Value `rowwise` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			else {
				for( long i=row_offset; i<max_row_offset+1; i++ ) {
					MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), i, col_offset, mcIn, mcOut, rowwise);
					MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), i, max_col_offset, mcIn, mcOut, rowwise);
					createRowwiseIndexes(first, last, mcOut.getNumColBlocks(), ret);
```

### ConstantValue
Value `rowwise` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				for( long i=row_offset; i<max_row_offset+1; i++ ) {
					MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), i, col_offset, mcIn, mcOut, rowwise);
					MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), i, max_col_offset, mcIn, mcOut, rowwise);
					createRowwiseIndexes(first, last, mcOut.getNumColBlocks(), ret);
				}
```

### ConstantValue
Value `rowwise` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		else{ //colwise
			if( mcIn.getRows() == 1 ) {
				MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), 0, col_offset, mcIn, mcOut, rowwise);
				MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), 0, max_col_offset, mcIn, mcOut, rowwise);
				createColwiseIndexes(first, last, mcOut.getNumRowBlocks(), ret);
```

### ConstantValue
Value `rowwise` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			if( mcIn.getRows() == 1 ) {
				MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), 0, col_offset, mcIn, mcOut, rowwise);
				MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), 0, max_col_offset, mcIn, mcOut, rowwise);
				createColwiseIndexes(first, last, mcOut.getNumRowBlocks(), ret);
			}
```

### ConstantValue
Value `rowwise` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			}
			else if( in.getNonZeros()<in.getNumColumns() && !outputEmpty ) {
				createNonZeroIndexes(mcIn, mcOut, in, row_offset, col_offset, rowwise, ret);
			}
			else {
```

### ConstantValue
Value `rowwise` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			else {
				for( long j=col_offset; j<max_col_offset+1; j++ ) {
					MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), row_offset, j, mcIn, mcOut, rowwise);
					MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), max_row_offset, j, mcIn, mcOut, rowwise);
					createColwiseIndexes(first, last, mcOut.getNumRowBlocks(), ret);
```

### ConstantValue
Value `rowwise` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				for( long j=col_offset; j<max_col_offset+1; j++ ) {
					MatrixIndexes first = computeResultBlockIndex(new MatrixIndexes(), row_offset, j, mcIn, mcOut, rowwise);
					MatrixIndexes last = computeResultBlockIndex(new MatrixIndexes(), max_row_offset, j, mcIn, mcOut, rowwise);
					createColwiseIndexes(first, last, mcOut.getNumRowBlocks(), ret);
				}
```

### ConstantValue
Condition `!in.sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
					}
			}
			else if( !in.sparse && !ret.sparse ) { //DENSE <- DENSE
				ret.allocateDenseBlock();
				DenseBlock a = in.getDenseBlock();
```

### ConstantValue
Result of `0*n2` is always '0'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
		//unrolled 8-blocks
		for( int j=bn; j<len; j+=8, aix+=8, cix+=8*n2 ) {
			c[ cix + 0*n2 ] = a[ aix+0 ];
			c[ cix + 1*n2 ] = a[ aix+1 ];
			c[ cix + 2*n2 ] = a[ aix+2 ];
```

### ConstantValue
Condition `pointrIndex < 0` is always `false`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			int pointrIndex = _end - middle - 1;
			while (positionToAssign >= _start && pointrIndex >= 0) {
				if( pointrIndex < 0 
					|| ( pointlIndex >= _start && _values[pointlIndex] > rhsCopyV[pointrIndex]) )
				{
```

### ConstantValue
Condition `!in.sparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
				}
		}
		else if( !in.sparse && !ret.sparse )  //DENSE <- DENSE
		{
			ret.allocateDenseBlock();
```

### ConstantValue
Condition `!(udf instanceof LineageTraceable)` is always `false`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
			return;

		if (!(udf instanceof LineageTraceable))
			throw new DMLRuntimeException("Unknown Federated UDF (" + udf.getClass().getSimpleName() + ") traced.");
		LineageTraceable ludf = (LineageTraceable) udf;
```

### ConstantValue
Condition `op instanceof AggregateOperator` is always `true`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
		//Aggregate operator for sum (via kahan sum)
		//note: support for row/column vectors and dense/sparse
		else if( op instanceof AggregateOperator ) {
			AggregateOperator aggop = (AggregateOperator) op;
			groupedAggregateKahanPlus(groups, target, weights, result, numGroups, aggop, 0, target.clen);
```

### ConstantValue
Condition `alen` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
					final int apos = a.pos(i);
					double tmp = product(a.values(i), apos, alen);
					lc[i] = tmp * ((alen<n) ? 0 : 1);
				}
			}
```

### ConstantValue
Condition `op != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
			MutableTriple<String, Long, Double> op = LineageEstimator.computeSavingInst.poll();
			int tl = String.valueOf(op.getRight()*1e-3).indexOf(".");
			if (op != null && op.getRight() > 0)
				sb.append(String.valueOf(i) 
					+ String.format("%"+(4-String.valueOf(i).length())+"s", "") // 4-length(i) spaces
```

### ConstantValue
Condition `inst instanceof ComputationFEDInstruction` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCacheConfig.java`
#### Snippet
```java
			return false;
		// return true if the output matrixobject is federated
		if (inst instanceof ComputationFEDInstruction)
			if (data instanceof MatrixObject && ((MatrixObject) data).isFederated())
				return true;
```

### ConstantValue
Condition `data instanceof ScalarObject` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			if (data instanceof MatrixObject)
				entry.setValue(mb, computetime);
			else if (data instanceof ScalarObject)
				entry.setValue((ScalarObject)data, computetime);

```

### ConstantValue
Condition `data instanceof ScalarObject` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				if (data instanceof MatrixObject)
					centry.setValue(mb, computetime);
				else if (data instanceof ScalarObject)
					centry.setValue((ScalarObject)data, computetime);

```

### ConstantValue
Condition `rsparse` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				if( !lsparse && !rsparse )
					nflops = 2 * (r1 * c1 * ((c2>1)?s1:1.0) * c2) /2;
				else if( !lsparse && rsparse )
					nflops = 2 * (r1 * c1 * s1 * c2 * s2) /2;
				else if( lsparse && !rsparse )
```

### ConstantValue
Condition `lsparse` is always `true`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				else if( !lsparse && rsparse )
					nflops = 2 * (r1 * c1 * s1 * c2 * s2) /2;
				else if( lsparse && !rsparse )
					nflops = 2 * (r1 * c1 * s1 * c2) /2;
				else //lsparse && rsparse
```

### ConstantValue
Value `transposeLeft` is always 'false'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMatrixMult.java`
#### Snippet
```java
			// compressed overlapping output.
			LOG.warn("Matrix decompression from multiplying two compressed matrices.");
			return matrixMultiply(m1, CompressedMatrixBlock.getUncompressed(m2), ret, k, transposeLeft, transposeRight);
		}
		else if(transposeLeft && !transposeRight) {
```

### ConstantValue
Value `transposeRight` is always 'false'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMatrixMult.java`
#### Snippet
```java
			// compressed overlapping output.
			LOG.warn("Matrix decompression from multiplying two compressed matrices.");
			return matrixMultiply(m1, CompressedMatrixBlock.getUncompressed(m2), ret, k, transposeLeft, transposeRight);
		}
		else if(transposeLeft && !transposeRight) {
```

### ConstantValue
Condition `transposeRight` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMatrixMult.java`
#### Snippet
```java

		}
		else if(!transposeLeft && transposeRight) {
			throw new DMLCompressionException("Not Implemented compressed Matrix Mult to produce larger matrix");
			// worst situation since it blows up the result matrix in number of rows in
```

### ConstantValue
Value `rows` is always 'true'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibRexpand.java`
#### Snippet
```java
		boolean ignore, int k) {
		if(rows)
			return in.getUncompressed("Rexpand in rows direction (one hot encode)").rexpandOperations(ret, max, rows, cast,
				ignore, k);
		else
```

### ConstantValue
Value `m1.sparse` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		if( !leftTranspose && m1.sparse && rlen > 1) { //X%*%t(X) SPARSE MATRIX
			//directly via LibMatrixReorg in order to prevent sparsity change
			MatrixBlock tmpBlock = new MatrixBlock(clen, rlen, m1.sparse);
			LibMatrixReorg.reorg(m1, tmpBlock, new ReorgOperator(SwapIndex.getSwapIndexFnObject()));
			ret = tmpBlock;
```

### ConstantValue
Value `scalar` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
							else if( four ) { //left/right 
								if (scalar)
									wdivmm(wvals[wix+j], eps, uvals, v.values(j), cvals, uix, v.pos(j), left, scalar, cd);
								else
									wdivmm(wvals[wix+j], xvals[wix+j], uvals, v.values(j), cvals, uix, v.pos(j), left, scalar, cd);
```

### ConstantValue
Value `scalar` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
									wdivmm(wvals[wix+j], eps, uvals, v.values(j), cvals, uix, v.pos(j), left, scalar, cd);
								else
									wdivmm(wvals[wix+j], xvals[wix+j], uvals, v.values(j), cvals, uix, v.pos(j), left, scalar, cd);
							}
							else //left/right minus/default
```

### ConstantValue
Value `scalar` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
								double[] cvals = c.values(left ? wix[k] : i);
								wdivmm(wval[k], xvals[k], uvals, v.values(wix[k]),
									cvals, uix, v.pos(wix[k]), left, scalar, cd);
							}
						}
```

### ConstantValue
Value `scalar` is always 'true'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
								if (scalar)
									wdivmm(wval[k], eps, uvals, v.values(wix[k]),
										cvals, uix, v.pos(wix[k]), left, scalar, cd);
								else
									wdivmm(wval[k], x.get(i, wix[k]), uvals,
```

### ConstantValue
Value `scalar` is always 'false'
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
								else
									wdivmm(wval[k], x.get(i, wix[k]), uvals,
										v.values(wix[k]), cvals, uix, v.pos(wix[k]), left, scalar, cd);
							}
						}
```

### ConstantValue
Condition `constV != null` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibLeftMultBy.java`
#### Snippet
```java

			// add the correction layer for the subtracted common values.
			if(rowSums != null && constV != null) {
				if(ret.isEmpty())
					ret.allocateDenseBlock();
```

### ConstantValue
Value `ret` is always 'null'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
		}

		return ret;
	}

```

### ConstantValue
Value `mbSliced` is always 'null'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
			}

			cmb = (CompressedMatrixBlock) mbSliced;
			decompress(cmb, 1);
		}
```

### ConstantValue
Value `cmb` is always 'null'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java

			cmb = (CompressedMatrixBlock) mbSliced;
			decompress(cmb, 1);
		}

```

### ConstantValue
Condition `c != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibScalar.java`
#### Snippet
```java
				newColGroups.add(grp.scalarOperation(new RightScalarOperator(Multiply.getMultiplyFnObject(), -1)));
		}
		if(c != null)
			newColGroups.add(c);
		return newColGroups;
```

### ConstantValue
Condition `index >= 0` is always `true`
in `src/main/java/org/apache/sysds/runtime/compress/estim/encoding/EncodingFactory.java`
#### Snippet
```java
			if(index >= 0) {
				final double v = sb.values(r)[index];
				if(index >= 0 && !Double.isNaN(v))
					d.set(off++, map.get(v));
			}
```

### ConstantValue
Result of `ret.size()` is always '0'
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodePriorityQue.java`
#### Snippet
```java
		CompressedSizeInfoColGroup l = null;
		l = que.poll();
		int groupNr = ret.size() + que.size();
		while(que.peek() != null && groupNr >= minNumGroups) {
			CompressedSizeInfoColGroup r = que.peek();
```

### ConstantValue
Condition `numZeros < largestOffset` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupFactory.java`
#### Snippet
```java
		}
		else if((nVal == 2 && numZeros == 0) // case 1 : two distinct non zero values
			|| (nVal == 1 && numZeros < largestOffset) // case 2: 1 non zero value more frequent than zero.
		) {
			double[] defaultTuple = new double[colIndexes.size()];
```

### ConstantValue
Condition `this == DDCFOR` is always `false`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/AColGroup.java`
#### Snippet
```java

		public boolean isDense() {
			return this == DDC || this == CONST || this == DDCFOR || this == DDCFOR;
		}

```

### ConstantValue
Condition `itThis.value() != finalOff` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupSDCSingleZeros.java`
#### Snippet
```java
					continue;
				// while lower than run iterate through
				while(itThis.value() < rs && itThis.value() != finalOff)
					itThis.next();
				// process inside run
```

### ConstantValue
Condition `itThat.value() != finalOff` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
					continue;
				// while lower than run iterate through
				while(itThat.value() < rs && itThat.value() != finalOff)
					itThat.next();
				// process inside run
```

### ConstantValue
Condition `(trs >= rs && trs < re) || // inside low (tre <= re && tre > rs) || // inside high (tr...` is always `true`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
						else if(trs >= re) // if we are past run break.
							break;
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
							final int crs = Math.max(rs, trs); // common largest run start
							final int cre = Math.min(re, tre); // common smallest run end
```

### ConstantValue
Condition `trs < re` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
						else if(trs >= re) // if we are past run break.
							break;
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
```

### ConstantValue
Condition `tre > rs` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
							break;
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
							final int crs = Math.max(rs, trs); // common largest run start
```

### ConstantValue
Condition `trs <= rs` is always `true`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
							final int crs = Math.max(rs, trs); // common largest run start
							final int cre = Math.min(re, tre); // common smallest run end
```

### ConstantValue
Condition `trs <= rs && tre > re` is always `true`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
							final int crs = Math.max(rs, trs); // common largest run start
							final int cre = Math.min(re, tre); // common smallest run end
```

### ConstantValue
Condition `tre > re` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
						else if((trs >= rs && trs < re) || // inside low
							(tre <= re && tre > rs) || // inside high
							(trs <= rs && tre > re)) { // encapsulate
							final int crs = Math.max(rs, trs); // common largest run start
							final int cre = Math.min(re, tre); // common smallest run end
```

### ConstantValue
Condition `i < _values.length` is always `false`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int i = 0; i < _values.length; i++)
			retVals[i] = fn.execute(v[colIndexes.get(i % lenV)], _values[i]);
		for(int i = _values.length; i < _values.length; i++)
			retVals[i] = fn.execute(v[colIndexes.get(i % lenV)], 0);

```

### ConstantValue
Condition `i < _values.length` is always `false`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/Dictionary.java`
#### Snippet
```java
		for(int i = 0; i < _values.length; i++)
			retVals[i] = fn.execute(_values[i], v[colIndexes.get(i % lenV)]);
		for(int i = _values.length; i < _values.length; i++)
			retVals[i] = fn.execute(0, v[colIndexes.get(i % lenV)]);

```

### ConstantValue
Condition `mcsr` is always `false`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoder.java`
#### Snippet
```java
			int lim = Math.min(i+B, rowEnd);
			for (int ii=i; ii<lim; ii++) {
				if (mcsr) {
					SparseRowVector row = (SparseRowVector) out.getSparseBlock().get(ii);
					row.values()[index] = codes[ii-rowStart];
```

### ConstantValue
Condition `mcsr` is always `false`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderDummycode.java`
#### Snippet
```java
			// indexes = [0,2] ===> indexes = [0,3]
			// values = [1,2] values = [1,1]
			if (mcsr) {
				double val = out.getSparseBlock().get(r).values()[index];
				if(Double.isNaN(val)){
```

### ConstantValue
Condition `mcsr` is always `false`
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderPassThrough.java`
#### Snippet
```java
					sparseRowsWZeros.add(ii);
				}
				if (mcsr) {
					SparseRowVector row = (SparseRowVector) out.getSparseBlock().get(ii);
					row.values()[index] = v;
```

### ConstantValue
Value `mbOut` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java
		}
		
		mbOut = mbLeft.uaggouterchainOperations(mbLeft, mbRight, mbOut, _bOp, _uaggOp);

		//release locks
```

### ConstantValue
Result of `emptyRowNNZ * counts[i]` is always '0'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
			for(int i = 0; i < counts.length; i++) {
				if(sb.isEmpty(i))
					nnz += emptyRowNNZ * counts[i];
				else {
					int countThis = 0;
```

### ConstantValue
Condition `mcsr` is always `false`
in `src/main/java/org/apache/sysds/runtime/transform/encode/MultiColumnEncoder.java`
#### Snippet
```java
			//boolean mcsr = MatrixBlock.DEFAULT_SPARSEBLOCK == SparseBlock.Type.MCSR;
			boolean mcsr = false; //force CSR for transformencode
			if (mcsr) {
				output.allocateBlock();
				SparseBlock block = output.getSparseBlock();
```

### ConstantValue
Condition `opcode.equals(WeightedSquaredLoss.OPCODE_CP)` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuaternaryFEDInstruction.java`
#### Snippet
```java
				return new QuaternaryWDivMMFEDInstruction(qop, in1, in2, in3, in4, out, opcode, str);
			}
			else if(opcode.equals(WeightedSquaredLoss.OPCODE_CP)) {
				final WeightsType weights_type = WeightsType.valueOf(parts[6]);
				if(weights_type.hasFourInputs())
```

### ConstantValue
Condition `rowLength == schemaLength` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
						outF.appendRow(data);
				}
				else if(data.length > 1 && rowLength == schemaLength)
				{
					int beg = 0;
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			int k = Integer.parseInt(s[11 - missing]);

			return new DataGenCPInstruction(op, method, null, out, rows, cols, dims, blen, s[5 - missing],
				s[6 - missing], sparsity, seed, pdf, pdfParams, k, opcode, str);
		}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			CPOperand incr = new CPOperand(s[6]);

			return new DataGenCPInstruction(op, method, null, out, null, null, null, blen, from, to, incr, opcode, str);
		}
		else if(method == OpOpDG.FRAMEINIT) {
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			CPOperand cols = new CPOperand(s[3]);
			String valueType = s[4];
			return new DataGenCPInstruction(op, method, out, rows, cols, data, valueType, opcode, str);
		}
		else if(method == OpOpDG.SAMPLE) {
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
			int blen = Integer.parseInt(s[5]);

			return new DataGenCPInstruction(op, method, null,
				out, rows, cols, null, blen, s[1], replace, seed, opcode, str);
		}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
		}
		else if(method == OpOpDG.TIME) {
			return new DataGenCPInstruction(op, method, out, opcode, str);
		}
		else
```

### ConstantValue
Value `method` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
		}
		else
			throw new DMLRuntimeException("Unrecognized data generation method: " + method);
	}

```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReblockFEDInstruction.java`
#### Snippet
```java

		Operator op = null; // no operator for ReblockFEDInstruction
		return new ReblockFEDInstruction(op, in, out, blen, outputEmptyBlocks, opcode, str);
	}

```

### ConstantValue
Condition `parts.length >= 10` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
					// do nothing
				}
				else if (parts.length >= 10) {
					// matrix characteristics
					mc.setDimension(Long.parseLong(parts[6]), Long.parseLong(parts[7]));
```

### ConstantValue
Condition `parts.length >= 10` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
					// do nothing
				}
				else if (parts.length >= 10) {
					// TODO correct sizes
					tc.setDim(0, Long.parseLong(parts[6]));
```

### ConstantValue
Value `out` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
		case RemoveVariable:
			VariableCPInstruction rminst = new VariableCPInstruction(
				getVariableOperationCode(opcode), null, null, null, out, opcode, str);
			for( int i=1; i<parts.length; i++ )
				rminst.addInput(new CPOperand(parts[i], ValueType.UNKNOWN, DataType.SCALAR));
```

### ConstantValue
Value `out` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			}
			VariableCPInstruction inst = new VariableCPInstruction(
				getVariableOperationCode(opcode), in1, in2, in3, out, null, fprops, null, null, opcode, str);
			inst.addInput(in4);

```

### ConstantValue
Condition `isColAgg` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
		//   then set row dimension to output and col dimension to in col split
		//   and set FType to COL
		if ( inFtype.isColPartitioned() && isColAgg ){
			for ( FederatedRange range : inputFedMapCopy.getFederatedRanges() )
				range.setEndDim(0,out.getNumRows());
```

### ConstantValue
Condition `isTransposed` is always `false`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/ScalarMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		
		boolean isTransposed = false;
		int rlen = isTransposed ? (int) in1.getNumColumns() : (int) in1.getNumRows();
		int clen = isTransposed ? (int) in1.getNumRows() : (int) in1.getNumColumns();
		
```

### ConstantValue
Condition `isTransposed` is always `false`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/ScalarMatrixArithmeticGPUInstruction.java`
#### Snippet
```java
		boolean isTransposed = false;
		int rlen = isTransposed ? (int) in1.getNumColumns() : (int) in1.getNumRows();
		int clen = isTransposed ? (int) in1.getNumRows() : (int) in1.getNumColumns();
		
		ec.setMetaData(_output.getName(), rlen, clen);
```

### ConstantValue
Condition `retVal` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/instructions/fed/CtableFEDInstruction.java`
#### Snippet
```java
			entry = iter.next();
			// previous end dimension must be less than current begin dimension (no overlaps of ranges)
			retVal &= (prevEndDim < entry.getKey());
			prevEndDim = entry.getValue();
		}
```

### ConstantValue
Value `writeLock` is always 'true'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
	public void addReadLock() {
		if(writeLock)
			throw new DMLRuntimeException("Attempting to add a read lock when writeLock="+ writeLock);
		else
			readLocks.increment();
```

### ConstantValue
Value `writeLock` is always 'true'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUObject.java`
#### Snippet
```java
			throw new DMLRuntimeException("Attempting to add a write lock when readLocks="+ readLocks.longValue());
		else if(writeLock)
			throw new DMLRuntimeException("Attempting to add a write lock when writeLock="+ writeLock);
		else
			writeLock = true;
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReblockSPInstruction.java`
#### Snippet
```java

		Operator op = null; // no operator for ReblockSPInstruction
		return new ReblockSPInstruction(op, in, out, blen, blen, outputEmptyBlocks, opcode, str);
	}

```

### ConstantValue
Condition `inW != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
				out = in.join(inU).join(inV)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
			else if( inU != null && inV == null && inW != null )
				out = in.join(inU).join(inW)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
```

### ConstantValue
Condition `inW != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
				out = in.join(inU).join(inW)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
			else if( inU == null && inV != null && inW != null )
				out = in.join(inV).join(inW)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
```

### ConstantValue
Condition `inV == null` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
				out = in.join(inV).join(inW)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
			else if( inU == null && inV == null && inW == null ) {
				out = in.mapPartitionsToPair(new RDDQuaternaryFunction1(qop, bc1, bc2), false);
			}
```

### ConstantValue
Condition `inW == null` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/QuaternarySPInstruction.java`
#### Snippet
```java
				out = in.join(inV).join(inW)
				        .mapToPair(new RDDQuaternaryFunction3(qop, bc1, bc2));
			else if( inU == null && inV == null && inW == null ) {
				out = in.mapPartitionsToPair(new RDDQuaternaryFunction1(qop, bc1, bc2), false);
			}
```

### ConstantValue
Value `block` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
					case CTABLE_TRANSFORM: {
						OperationsOnMatrixValues.performCtable(ix, mb[0], ix,
							mb[1], ix, mb[2], map, block, null);
						break;
					}
```

### ConstantValue
Value `block` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
					case CTABLE_TRANSFORM_SCALAR_WEIGHT: {
						// 3rd input is a scalar
						mb[0].ctableOperations(null, mb[1], _scalar_input3, _ignoreZeros, map, block);
						break;
					}
```

### ConstantValue
Value `block` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
					case CTABLE_TRANSFORM_HISTOGRAM: {
						OperationsOnMatrixValues.performCtable(ix, mb[0],
							_scalar_input2, _scalar_input3, map, block, null);
						break;
					}
```

### ConstantValue
Value `block` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
						// 2nd and 3rd inputs are scalars
						OperationsOnMatrixValues.performCtable(ix, mb[0],
							_scalar_input2, ix, mb[1], map, block, null);
						break;
					}
```

### ConstantValue
Condition `dt2 == DataType.MATRIX` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BinarySPInstruction.java`
#### Snippet
```java
					return new BinaryMatrixMatrixSPInstruction(operator, in1, in2, out, opcode, str);
			}
			else if(dt1 == DataType.FRAME && dt2 == DataType.MATRIX)
				return  new BinaryFrameMatrixSPInstruction(operator, in1, in2, out, opcode, str);
			else
```

### ConstantValue
Condition `baEncoder != null` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MultiReturnParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
						max = Math.max(max, value);
					}
					assert baEncoder != null;
					baEncoder.computeBins(min, max);
				}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
				s[11 - missing] : null;
			
			return new RandSPInstruction(op, method, null, out, rows, cols, dims,
				blen, s[5 - missing], s[6 - missing], sparsity, seed, dir, pdf, pdfParams, opcode, str);
		}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			
			CPOperand in = null;
			return new RandSPInstruction(op, method, in, out, null,
				null, null, blen, from, to, incr, opcode, str);
		}
```

### ConstantValue
Value `in` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			
			CPOperand in = null;
			return new RandSPInstruction(op, method, in, out, null,
				null, null, blen, from, to, incr, opcode, str);
		}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			int blen = Integer.parseInt(s[5]);
			
			return new RandSPInstruction(op, method, null, out, rows, cols,
				null, blen, max, replace, seed, opcode, str);
		}
```

### ConstantValue
Value `op` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
			CPOperand cols = new CPOperand(s[3]);
			String valueType = s[4];
			return new RandSPInstruction(op, method, out, rows, cols, data, valueType, opcode, str);
		}

```

### ConstantValue
Value `method` is always 'null'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java

		else 
			throw new DMLRuntimeException("Unrecognized data generation method: " + method);
	}
	
```

### ConstantValue
Condition `rowLength == _schema.length` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
						out.appendRow(data);
				}
				else if(data.length > 1 && rowLength == _schema.length)
				{
					out = new FrameBlock(_schema);
```

### ConstantValue
Condition `parts.length > 5` is always `true`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ReorgSPInstruction.java`
#### Snippet
```java
			boolean bSortIndInMem = false;
			
			if(parts.length > 5)
				bSortIndInMem = Boolean.parseBoolean(parts[6]);
			
```

### ConstantValue
Value `deep` is always 'true'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
			return in.mapValues(new CopyMatrixBlockFunction(false));
		else //requires key access, so use mappartitions
			return in.mapPartitionsToPair(new CopyMatrixBlockPairFunction(deep), true);
	}

```

### ConstantValue
Value `deep` is always 'true'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/SparkUtils.java`
#### Snippet
```java
			return in.mapValues(new CopyTensorBlockFunction(false));
		else //requires key access, so use mappartitions
			return in.mapPartitionsToPair(new CopyTensorBlockPairFunction(deep), true);
	}
	
```

### ConstantValue
Condition `group.getNumRows()!=target.getNumRows()` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractGroupNWeights.java`
#### Snippet
```java
		
		//sanity check matching block dimensions
		if(group.getNumRows() != target.getNumRows() || group.getNumRows()!=target.getNumRows()) {
			throw new Exception("The blocksize for group/target/weight blocks are mismatched: " + group.getNumRows()  + ", " + target.getNumRows() + ", " + weight.getNumRows());
		}
```

### ConstantValue
Condition `withCompare` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeRemoteSpark.java`
#### Snippet
```java
			for(int i=0; i<paths.length; i++)
				ret.addLineageChild(inputs[i].getRDDHandle());
			if( withCompare )
				ret.addLineageChild(compare.getRDDHandle());
		}
```

### ConstantValue
Value `isVector` is always 'false'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
			return ((Vector) vect).numNonzeros();
		else
			return countNnz(vect, isVector, off, ((Row)vect).length());
	}

```

### ConstantValue
Condition `vect instanceof SparseVector` is always `false`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
					lnnz += (vec.apply(i) != 0) ? 1 : 0;
			}
			else if( vect instanceof SparseVector ) {
				SparseVector vec = (SparseVector) vect;
				int alen = vec.numActives();
```

### ConstantValue
Value `flagRecompMR` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerConstrained.java`
#### Snippet
```java
			if( flagRecompMR ){
				//rewrite 5: set operations exec type
				rewriteSetOperationsExecType( pn, flagRecompMR );
				M1 = _cost.getEstimate(TestMeasure.MEMORY_USAGE, pn); //reestimate
			}
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/ProgramRecompiler.java`
#### Snippet
```java
		
		if( hop.isVisited() )
			return ret;
		
		ArrayList<Hop> in = hop.getInput();
```

### ConstantValue
Value `ret` is always 'false'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/ProgramRecompiler.java`
#### Snippet
```java
		
		if( hop.isVisited() )
			return ret;
		
		ArrayList<Hop> in = hop.getInput();
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
			for( OptNode n : _childs ) {
				if( !ret ) break; //early abort if already false
				ret &= n.isCPOnly();
			}
		return ret;
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
		if( !isLeaf() )
			for( int i=0; i<_childs.size() && !ret; i++ )
				ret |= _childs.get(i).hasNestedParallelism( flagNested );
		return ret;
	}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptNode.java`
#### Snippet
```java
			if( n._ntype.isLoop() )
				flagNested = true;
			ret |= n.hasNestedPartitionReads( flagNested );
		}
		return ret;
```

### ConstantValue
Condition `pbParent instanceof ParForProgramBlock` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			replaceProgramBlock( wpb.getChildBlocks(), pbOld, pbNew );
		}
		else if( pbParent instanceof ForProgramBlock || pbParent instanceof ParForProgramBlock ) {
			ForProgramBlock fpb = (ForProgramBlock) pbParent;
			replaceProgramBlock( fpb.getChildBlocks(), pbOld, pbNew );
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			WhileProgramBlock tmp = (WhileProgramBlock)pb;
			ret = containsSparkInstruction(tmp.getPredicate(), true);
			if( ret ) return ret;
			for (ProgramBlock pb2 : tmp.getChildBlocks()) {
				ret = rContainsSparkInstruction(pb2, inclFunctions);
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			for (ProgramBlock pb2 : tmp.getChildBlocks()) {
				ret = rContainsSparkInstruction(pb2, inclFunctions);
				if( ret ) return ret;
			}
		}
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			IfProgramBlock tmp = (IfProgramBlock)pb;
			ret = containsSparkInstruction(tmp.getPredicate(), true);
			if( ret ) return ret;
			for( ProgramBlock pb2 : tmp.getChildBlocksIfBody() ){
				ret = rContainsSparkInstruction(pb2, inclFunctions);
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			for( ProgramBlock pb2 : tmp.getChildBlocksIfBody() ){
				ret = rContainsSparkInstruction(pb2, inclFunctions);
				if( ret ) return ret;
			}
			for( ProgramBlock pb2 : tmp.getChildBlocksElseBody() ){
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			for( ProgramBlock pb2 : tmp.getChildBlocksElseBody() ){
				ret = rContainsSparkInstruction(pb2, inclFunctions);
				if( ret ) return ret;
			}
		}
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			ret |= containsSparkInstruction(tmp.getToInstructions(), true);
			ret |= containsSparkInstruction(tmp.getIncrementInstructions(), true);
			if( ret ) return ret;
			for( ProgramBlock pb2 : tmp.getChildBlocks() ){
				ret = rContainsSparkInstruction(pb2, inclFunctions);
```

### ConstantValue
Value `ret` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			for( ProgramBlock pb2 : tmp.getChildBlocks() ){
				ret = rContainsSparkInstruction(pb2, inclFunctions);
				if( ret ) return ret;
			}
		}
```

### ConstantValue
Condition `pbParent instanceof ParForProgramBlock` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
			replaceProgramBlock( wpb.getChildBlocks(), pbOld, pbNew );
		}
		else if( pbParent instanceof ForProgramBlock || pbParent instanceof ParForProgramBlock ) {
			ForProgramBlock fpb = (ForProgramBlock) pbParent;
			replaceProgramBlock( fpb.getChildBlocks(), pbOld, pbNew );
```

### ConstantValue
Condition `CacheableData.CACHING_ASYNC_FILECLEANUP || CacheableData.CACHING_ASYNC_SERIALIZE` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheMaintenanceService.java`
#### Snippet
```java
	@SuppressWarnings("unused")
	public boolean isAsync() {
		return CacheableData.CACHING_ASYNC_FILECLEANUP
			|| CacheableData.CACHING_ASYNC_SERIALIZE;
	}

```

### ConstantValue
Condition `fb == null` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/FrameObject.java`
#### Snippet
```java
		
		//sanity check correct output
		if( fb == null )
			throw new IOException("Unable to load frame from rdd.");
		
```

### ConstantValue
Condition `ecm == null` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLookupTable.java`
#### Snippet
```java
		ExecutionContextMap ecm = _lookup_table.computeIfAbsent(funCID,
			k -> createNewECM());
		if(ecm == null) {
			LOG.error("Computing federated execution context map failed. "
				+ "No valid resolution for " + funCID.toString() + " found.");
```

### ConstantValue
Condition `!federatedWrite` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
					else if( getRDDHandle() != null )
						_data = readBlobFromRDD( getRDDHandle(), new MutableBoolean() );
					else if(!federatedWrite)
						_data = readBlobFromFederated( getFedMapping() );
					setDirty(false);
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
		for(AlignType at : alignTypes) {
			if(at.isFullType())
				ret |= isAligned(that, at.isTransposed());
			else
				ret |= isAligned(that, at.isTransposed(), at.isRowType(), at.isColType());
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
				ret |= isAligned(that, at.isTransposed());
			else
				ret |= isAligned(that, at.isTransposed(), at.isRowType(), at.isColType());
			if(ret) // early stopping - alignment already found
				break;
```

### ConstantValue
Condition `remoteAddress instanceof SocketAddress` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
			host = ((InetSocketAddress) remoteAddress).getHostString();
		}
		else if(remoteAddress instanceof SocketAddress) {
			host = remoteAddress.toString().split(":")[0].split("/")[1];
		}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			{
				if( !ret ) break; //early abort if already false
				ret &= isCPOnlyPossible(c, memBudget);
			}
		return ret;
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		boolean ret = true;
		for( OptNode n : nlist ) {
			ret &= isResultPartitionable(n, resultVars, vars, iterVarname);
			if(!ret) //early abort
				break;
```

### ConstantValue
Value `flagRecompMR` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			if( flagRecompMR ){
				//rewrite 5: set operations exec type
				rewriteSetOperationsExecType( pn, flagRecompMR );
				M1 = _cost.getEstimate(TestMeasure.MEMORY_USAGE, pn); //reestimate
			}
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `false`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		if( !ret && !n.isLeaf() )
			for( OptNode c : n.getChilds() ) {
				ret |= rContainsNode(c, parfor);
				if( ret ) break; //early abort
			}
```

### ConstantValue
Condition `isCPOnlyPossible` is always `true` when reached
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			}
			//MR if MR operations in local, but CP only in remote (less overall MR jobs)
			else if( !isCPOnly && isCPOnlyPossible )
			{
				n.setExecType( REMOTE ); //remote parfor
```

### ConstantValue
Condition `ret` at the left side of assignment expression is always `true`. Can be simplified
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			}
			
			ret &= (   (inMatrix!=null && inMatrix.equals(varName)) 
				    && (indexAccess!=null && indexAccess.equals(iterVarname)));
		}
```

### ConstantValue
Condition `apply` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		
		//modify the runtime plan (apply true if at least one candidate)
		if( apply )
			pfpb.enableColocatedPartitionedMatrix( varname );
		
```

### ConstantValue
Condition `apply` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		
		_numEvaluatedPlans++;
		LOG.debug(getOptMode()+" OPT: rewrite 'enable data colocation' - result="+apply+((apply)?" ("+varname+")":"") );
	}

```

### ConstantValue
Condition `wrapIter < wrappedInstruction.length` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
			
			for(int wrapIter = 0; wrapIter < numLines; wrapIter++) {
				String instStr = (wrapIter < wrappedInstruction.length) ? wrappedInstruction[wrapIter] : "";
				if(wrapIter == 0) {
					sb.append(String.format(
```

### ConstantValue
Condition `j < batchIter` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/LocalPSWorker.java`
#### Snippet
```java
			try {
				for(int j = 0; j < batchIter; j++) {
					boolean localUpdate = j < batchIter;
					if( j % _nbatches == 0 )
						model = pullModel();
```

### ConstantValue
Condition `!localUpdate` is always `false`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/LocalPSWorker.java`
#### Snippet
```java
					ListObject accGradientsPrev = accGradients.get();
					accGradients = _tpool
						.submit(() -> ParamservUtils.accrueGradients(accGradientsPrev, gradients, false, !localUpdate));
					// Update the local model with gradients
					if(localUpdate | _modelAvg)
```

### ConstantValue
Condition `localUpdate` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/LocalPSWorker.java`
#### Snippet
```java
						.submit(() -> ParamservUtils.accrueGradients(accGradientsPrev, gradients, false, !localUpdate));
					// Update the local model with gradients
					if(localUpdate | _modelAvg)
						model = updateModel(model, gradients, i, j, batchIter);
					accNumBatches(1);
```

### ConstantValue
Condition `localUpdate | _modelAvg` is always `true`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/LocalPSWorker.java`
#### Snippet
```java
						.submit(() -> ParamservUtils.accrueGradients(accGradientsPrev, gradients, false, !localUpdate));
					// Update the local model with gradients
					if(localUpdate | _modelAvg)
						model = updateModel(model, gradients, i, j, batchIter);
					accNumBatches(1);
```

### ConstantValue
Value `forceExecTypeCP` is always 'true'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
		try {
			if(forceExecTypeCP)
				rAssignParallelismAndRecompile(pbs, k, true, forceExecTypeCP);
			else
				rAssignParallelismAndRecompile(pbs, k, false, forceExecTypeCP);
```

### ConstantValue
Value `forceExecTypeCP` is always 'false'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/ParamservUtils.java`
#### Snippet
```java
				rAssignParallelismAndRecompile(pbs, k, true, forceExecTypeCP);
			else
				rAssignParallelismAndRecompile(pbs, k, false, forceExecTypeCP);
		} catch (IOException e) {
			throw new DMLRuntimeException(e);
```

### ConstantValue
Condition `in2==-0.0` is always `false` when reached
in `src/main/java/org/apache/sysds/runtime/functionobjects/Modulus.java`
#### Snippet
```java
	@Override
	public double execute(double in1, double in2) {
		if( in2==0.0 || in2==-0.0 )
			return Double.NaN;
		return in1 - _intdiv.execute(in1, in2)*in2;
```

## RuleId[id=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/api/mlcontext/ProjectInfo.java`
#### Snippet
```java
		Set<String> keySet = properties.keySet();
		for (String key : keySet) {
			sb.append(key + ": " + properties.get(key) + "\n");
		}
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
			if (inValue instanceof String) {
				String quotedString = MLContextUtil.quotedString((String) inValue);
				sb.append(" = " + quotedString + ";\n");
			} else if (MLContextUtil.isBasicType(inValue)) {
				sb.append(" = read('', data_type='scalar', value_type='" + MLContextUtil.getBasicTypeString(inValue)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
				sb.append(" = " + quotedString + ";\n");
			} else if (MLContextUtil.isBasicType(inValue)) {
				sb.append(" = read('', data_type='scalar', value_type='" + MLContextUtil.getBasicTypeString(inValue)
						+ "');\n");
			} else if (MLContextUtil.doesSymbolTableContainFrameObject(symbolTable, in)) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/cost/FederatedCostEstimator.java`
#### Snippet
```java
			.append(" Has federated output: ").append(root.hasFederatedOutput())
			.append(root.getText()).append(sep)
			.append("Pure computeCost: " + ComputeCost.getHOPComputeCost(root))
			.append(" Dim1: " + root.getDim1() + " Dim2: " + root.getDim2()).append(sep)
			.append(root.getFederatedCost().toString()).append(sep);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/cost/FederatedCostEstimator.java`
#### Snippet
```java
			.append(root.getText()).append(sep)
			.append("Pure computeCost: " + ComputeCost.getHOPComputeCost(root))
			.append(" Dim1: " + root.getDim1() + " Dim2: " + root.getDim2()).append(sep)
			.append(root.getFederatedCost().toString()).append(sep);
		return costInfo.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
				String tmp = in.explain(visited);
				if( !tmp.isEmpty() )
					sb.append(tmp + "\n");
				if( inputs.length() > 0 )
					inputs.append(",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
			}
			//add node itself
			sb.append(_ID+" ("+inputs+") "+((_p!=null)?_p:"null"));
			visited.add(_ID);
			
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
				case VECT_CBIND:
					StringBuilder sb = new StringBuilder();
					sb.append("    double[] %TMP% = LibSpoofPrimitives.allocVector("+len+", true); //nary cbind\n");
					for( int i=0, off=0; i<inputs.size(); i++ ) {
						CNode input = inputs.get(i);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
						}
						else { //e.g., col vectors -> scalars
							sb.append("    %TMP%["+off+"] = "+varj+";\n");
							off ++;
						}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
		sb.append("SPOOF CELLWISE [type=");
		sb.append(_type.name());
		sb.append(", aggOp="+((_aggOp!=null) ? _aggOp.name() : "null"));
		sb.append(", sparseSafe="+_sparseSafe);
		sb.append(", castdtm="+_requiresCastdtm);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
		sb.append(_type.name());
		sb.append(", aggOp="+((_aggOp!=null) ? _aggOp.name() : "null"));
		sb.append(", sparseSafe="+_sparseSafe);
		sb.append(", castdtm="+_requiresCastdtm);
		sb.append(", mc="+_multipleConsumers);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
		sb.append(", aggOp="+((_aggOp!=null) ? _aggOp.name() : "null"));
		sb.append(", sparseSafe="+_sparseSafe);
		sb.append(", castdtm="+_requiresCastdtm);
		sb.append(", mc="+_multipleConsumers);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
		sb.append(", sparseSafe="+_sparseSafe);
		sb.append(", castdtm="+_requiresCastdtm);
		sb.append(", mc="+_multipleConsumers);
		sb.append("]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeOuterProduct.java`
#### Snippet
```java
		sb.append("SPOOF OUTERPRODUCT [type=");
		sb.append(_type.name());
		sb.append(", to="+_transposeOutput);
		sb.append("]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/template/CPlanMemoTable.java`
#### Snippet
```java
		sb.append("----------------------------------\n");
		for( Entry<Long, List<MemoTableEntry>> e : _plans.entrySet() ) {
			sb.append(e.getKey() + " "+_hopRefs.get(e.getKey()).getOpString()+": ");
			sb.append(Arrays.toString(e.getValue().toArray(new MemoTableEntry[0]))+"\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/template/CPlanMemoTable.java`
#### Snippet
```java
		for( Entry<Long, List<MemoTableEntry>> e : _plans.entrySet() ) {
			sb.append(e.getKey() + " "+_hopRefs.get(e.getKey()).getOpString()+": ");
			sb.append(Arrays.toString(e.getValue().toArray(new MemoTableEntry[0]))+"\n");
		}
		sb.append("----------------------------------\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/codegen/template/CPlanMemoTable.java`
#### Snippet
```java
		sb.append("----------------------------------\n");
		sb.append("ExcludeListed Plans: ");
		sb.append(Arrays.toString(_plansExcludeList.toArray(new Long[0]))+"\n");
		sb.append("----------------------------------\n");
		return sb.toString();	
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append("Compressed ProbeStatus : hopID =" + startHopID);
			sb.append("\n CLA Ops         : " + numberCompressedOpsExecuted);
			sb.append("\n Decompress Ops  : " + numberDecompressedOpsExecuted);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			StringBuilder sb = new StringBuilder();
			sb.append("Compressed ProbeStatus : hopID =" + startHopID);
			sb.append("\n CLA Ops         : " + numberCompressedOpsExecuted);
			sb.append("\n Decompress Ops  : " + numberDecompressedOpsExecuted);
			sb.append("\n Inefficient Ops : " + inefficientSupportedOpsExecuted);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			sb.append("Compressed ProbeStatus : hopID =" + startHopID);
			sb.append("\n CLA Ops         : " + numberCompressedOpsExecuted);
			sb.append("\n Decompress Ops  : " + numberDecompressedOpsExecuted);
			sb.append("\n Inefficient Ops : " + inefficientSupportedOpsExecuted);
			sb.append("\n foundStart " + foundStart + " , inLoop :" + usedInLoop + " , condUpdate : " + condUpdate
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			sb.append("\n CLA Ops         : " + numberCompressedOpsExecuted);
			sb.append("\n Decompress Ops  : " + numberDecompressedOpsExecuted);
			sb.append("\n Inefficient Ops : " + inefficientSupportedOpsExecuted);
			sb.append("\n foundStart " + foundStart + " , inLoop :" + usedInLoop + " , condUpdate : " + condUpdate
				+ " , nonApplicable : " + nonApplicable);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			sb.append("\n Decompress Ops  : " + numberDecompressedOpsExecuted);
			sb.append("\n Inefficient Ops : " + inefficientSupportedOpsExecuted);
			sb.append("\n foundStart " + foundStart + " , inLoop :" + usedInLoop + " , condUpdate : " + condUpdate
				+ " , nonApplicable : " + nonApplicable);
			sb.append("\n compressed Matrix: " + compMtx);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
			sb.append("\n foundStart " + foundStart + " , inLoop :" + usedInLoop + " , condUpdate : " + condUpdate
				+ " , nonApplicable : " + nonApplicable);
			sb.append("\n compressed Matrix: " + compMtx);
			sb.append("\n Prog Fn " + procFn);
			return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
				+ " , nonApplicable : " + nonApplicable);
			sb.append("\n compressed Matrix: " + compMtx);
			sb.append("\n Prog Fn " + procFn);
			return sb.toString();
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("rows=" + getNumRows() + Lop.VALUETYPE_PREFIX);
		sb.append("cols=" + getNumCols() + Lop.VALUETYPE_PREFIX);
		sb.append("nnz=" + getNnz() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("rows=" + getNumRows() + Lop.VALUETYPE_PREFIX);
		sb.append("cols=" + getNumCols() + Lop.VALUETYPE_PREFIX);
		sb.append("nnz=" + getNnz() + Lop.VALUETYPE_PREFIX);
		sb.append("updateInPlace=" + getUpdateType().toString() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("rows=" + getNumRows() + Lop.VALUETYPE_PREFIX);
		sb.append("cols=" + getNumCols() + Lop.VALUETYPE_PREFIX);
		sb.append("nnz=" + getNnz() + Lop.VALUETYPE_PREFIX);
		sb.append("updateInPlace=" + getUpdateType().toString() + Lop.VALUETYPE_PREFIX);
		sb.append("blocksize=" + getBlocksize() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("cols=" + getNumCols() + Lop.VALUETYPE_PREFIX);
		sb.append("nnz=" + getNnz() + Lop.VALUETYPE_PREFIX);
		sb.append("updateInPlace=" + getUpdateType().toString() + Lop.VALUETYPE_PREFIX);
		sb.append("blocksize=" + getBlocksize() + Lop.VALUETYPE_PREFIX);
		sb.append("isBlockedRepresentation=" + isBlocked() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("nnz=" + getNnz() + Lop.VALUETYPE_PREFIX);
		sb.append("updateInPlace=" + getUpdateType().toString() + Lop.VALUETYPE_PREFIX);
		sb.append("blocksize=" + getBlocksize() + Lop.VALUETYPE_PREFIX);
		sb.append("isBlockedRepresentation=" + isBlocked() + Lop.VALUETYPE_PREFIX);
		sb.append("format=" + getFormat() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("updateInPlace=" + getUpdateType().toString() + Lop.VALUETYPE_PREFIX);
		sb.append("blocksize=" + getBlocksize() + Lop.VALUETYPE_PREFIX);
		sb.append("isBlockedRepresentation=" + isBlocked() + Lop.VALUETYPE_PREFIX);
		sb.append("format=" + getFormat() + Lop.VALUETYPE_PREFIX);
		sb.append("label=" + getLabel() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("blocksize=" + getBlocksize() + Lop.VALUETYPE_PREFIX);
		sb.append("isBlockedRepresentation=" + isBlocked() + Lop.VALUETYPE_PREFIX);
		sb.append("format=" + getFormat() + Lop.VALUETYPE_PREFIX);
		sb.append("label=" + getLabel() + Lop.VALUETYPE_PREFIX);
		sb.append("filename=" + getFile_name() + Lop.VALUETYPE_PREFIX);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("isBlockedRepresentation=" + isBlocked() + Lop.VALUETYPE_PREFIX);
		sb.append("format=" + getFormat() + Lop.VALUETYPE_PREFIX);
		sb.append("label=" + getLabel() + Lop.VALUETYPE_PREFIX);
		sb.append("filename=" + getFile_name() + Lop.VALUETYPE_PREFIX);
		sb.append("compressedSize=" + getCompressedSize());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("format=" + getFormat() + Lop.VALUETYPE_PREFIX);
		sb.append("label=" + getLabel() + Lop.VALUETYPE_PREFIX);
		sb.append("filename=" + getFile_name() + Lop.VALUETYPE_PREFIX);
		sb.append("compressedSize=" + getCompressedSize());
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/OutputParameters.java`
#### Snippet
```java
		sb.append("label=" + getLabel() + Lop.VALUETYPE_PREFIX);
		sb.append("filename=" + getFile_name() + Lop.VALUETYPE_PREFIX);
		sb.append("compressedSize=" + getCompressedSize());
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(_op.toString());
		sb.append(" ; num_rows=" + getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + getOutputParameters().getNumCols());
		sb.append(" ; nnz=" + getOutputParameters().getNnz());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(_op.toString());
		sb.append(" ; num_rows=" + getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + getOutputParameters().getNumCols());
		sb.append(" ; nnz=" + getOutputParameters().getNnz());
		sb.append(" ; blocksize=" + getOutputParameters().getBlocksize());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(" ; num_rows=" + getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + getOutputParameters().getNumCols());
		sb.append(" ; nnz=" + getOutputParameters().getNnz());
		sb.append(" ; blocksize=" + getOutputParameters().getBlocksize());
		sb.append(" ; format=" + getOutputParameters().getFormat());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(" ; num_cols=" + getOutputParameters().getNumCols());
		sb.append(" ; nnz=" + getOutputParameters().getNnz());
		sb.append(" ; blocksize=" + getOutputParameters().getBlocksize());
		sb.append(" ; format=" + getOutputParameters().getFormat());
		sb.append(" ; blocked=" + getOutputParameters().isBlocked());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(" ; nnz=" + getOutputParameters().getNnz());
		sb.append(" ; blocksize=" + getOutputParameters().getBlocksize());
		sb.append(" ; format=" + getOutputParameters().getFormat());
		sb.append(" ; blocked=" + getOutputParameters().isBlocked());
		sb.append(" ; dir=" + baseDir);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(" ; blocksize=" + getOutputParameters().getBlocksize());
		sb.append(" ; format=" + getOutputParameters().getFormat());
		sb.append(" ; blocked=" + getOutputParameters().isBlocked());
		sb.append(" ; dir=" + baseDir);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/DataGen.java`
#### Snippet
```java
		sb.append(" ; format=" + getOutputParameters().getFormat());
		sb.append(" ; blocked=" + getOutputParameters().isBlocked());
		sb.append(" ; dir=" + baseDir);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java
			sb.append(") ");

		sb.append(" ; num_rows=" + this.getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + this.getOutputParameters().getNumCols());
		sb.append(" ; format=" + this.getOutputParameters().getFormat());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java

		sb.append(" ; num_rows=" + this.getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + this.getOutputParameters().getNumCols());
		sb.append(" ; format=" + this.getOutputParameters().getFormat());
		sb.append(" ; blocked=" + this.getOutputParameters().isBlocked());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java
		sb.append(" ; num_rows=" + this.getOutputParameters().getNumRows());
		sb.append(" ; num_cols=" + this.getOutputParameters().getNumCols());
		sb.append(" ; format=" + this.getOutputParameters().getFormat());
		sb.append(" ; blocked=" + this.getOutputParameters().isBlocked());
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/lops/ParameterizedBuiltin.java`
#### Snippet
```java
		sb.append(" ; num_cols=" + this.getOutputParameters().getNumCols());
		sb.append(" ; format=" + this.getOutputParameters().getFormat());
		sb.append(" ; blocked=" + this.getOutputParameters().isBlocked());
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			e.printStackTrace();
			StringBuilder errorMessage = new StringBuilder();
			errorMessage.append("failed to run script " + executionFile);
			errorMessage.append("\nexception: " + e.toString());
			errorMessage.append("\nmessage: " + e.getMessage());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			StringBuilder errorMessage = new StringBuilder();
			errorMessage.append("failed to run script " + executionFile);
			errorMessage.append("\nexception: " + e.toString());
			errorMessage.append("\nmessage: " + e.getMessage());
			errorMessage.append("\nstack trace:");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			errorMessage.append("failed to run script " + executionFile);
			errorMessage.append("\nexception: " + e.toString());
			errorMessage.append("\nmessage: " + e.getMessage());
			errorMessage.append("\nstack trace:");
			for (StackTraceElement ste : e.getStackTrace()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			errorMessage.append("\nstack trace:");
			for (StackTraceElement ste : e.getStackTrace()) {
				errorMessage.append("\n>" + ste);
			}
			fail(errorMessage.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
	public static String getStringForCudaTimers() {
		StringBuffer sb = new StringBuffer();
		sb.append("CUDA/CuLibraries init time:\t" + String.format("%.3f", cudaInitTime*1e-9) + "/"
				+ String.format("%.3f", cudaLibrariesInitTime*1e-9) + " sec.\n");
		sb.append("Number of executed GPU inst:\t" + getNoOfExecutedGPUInst() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
		sb.append("CUDA/CuLibraries init time:\t" + String.format("%.3f", cudaInitTime*1e-9) + "/"
				+ String.format("%.3f", cudaLibrariesInitTime*1e-9) + " sec.\n");
		sb.append("Number of executed GPU inst:\t" + getNoOfExecutedGPUInst() + ".\n");
		// cudaSparseConversionCount
		sb.append("GPU mem alloc time  (alloc(success/fail) / dealloc / set0):\t"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
		sb.append("Number of executed GPU inst:\t" + getNoOfExecutedGPUInst() + ".\n");
		// cudaSparseConversionCount
		sb.append("GPU mem alloc time  (alloc(success/fail) / dealloc / set0):\t"
				+ String.format("%.3f", cudaAllocTime.longValue()*1e-9) + "("
				+ String.format("%.3f", cudaAllocSuccessTime.longValue()*1e-9) + "/"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ String.format("%.3f", cudaDeAllocTime.longValue()*1e-9) + " / "
				+ String.format("%.3f", cudaMemSet0Time.longValue()*1e-9) + " sec.\n");
		sb.append("GPU mem alloc count (alloc(success/fail/reuse) / dealloc / set0):\t"
				+ cudaAllocCount.longValue() + "("
				+ cudaAllocSuccessCount.longValue() + "/"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ cudaDeAllocCount.longValue() + " / "
				+ cudaMemSet0Count.longValue() + ".\n");
		sb.append("GPU mem tx time  (toDev(d2f/s2d) / fromDev(f2d/s2h) / evict(d2s/size)):\t"
				+ String.format("%.3f", cudaToDevTime.longValue()*1e-9) + "("
				+ String.format("%.3f", cudaDouble2FloatTime.longValue()*1e-9)+ "/"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ String.format("%.3f", cudaFromDevToShadowTime.longValue()*1e-9) + "/"
				+ String.format("%.3f", cudaEvictSizeTime.longValue()*1e-9) + ") sec.\n");
		sb.append("GPU mem tx count (toDev(d2f/s2d) / fromDev(f2d/s2h) / evict(d2s/size)):\t"
				+ cudaToDevCount.longValue() + "("
				+ cudaDouble2FloatCount.longValue() + "/" 
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ cudaFromDevToShadowCount.longValue() + "/" + 
				+ cudaEvictSizeCount.longValue() + ").\n");
		sb.append("GPU conversion time  (sparseConv / sp2dense / dense2sp):\t"
				+ String.format("%.3f", cudaSparseConversionTime.longValue()*1e-9) + " / "
				+ String.format("%.3f", cudaSparseToDenseTime.longValue()*1e-9) + " / "
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				+ String.format("%.3f", cudaSparseToDenseTime.longValue()*1e-9) + " / "
				+ String.format("%.3f", cudaDenseToSparseTime.longValue()*1e-9) + " sec.\n");
		sb.append("GPU conversion count (sparseConv / sp2dense / dense2sp):\t"
				+ cudaSparseConversionCount.longValue() + " / "
				+ cudaSparseToDenseCount.longValue() + " / "
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/utils/GPUStatistics.java`
#### Snippet
```java
				Long miscTimerTime = e.getValue();
				Long miscCount = miscCountMap.get(miscTimerName);
				sb.append(miscTimerName + "[" + String.format("%.3f", (double) miscTimerTime / 1000000000.0) + "s," + miscCount + "]");
				if (miscTimeIter.hasNext())
					sb.append(", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/RecompileStatistics.java`
#### Snippet
```java
	public static String displayStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append("HOP DAGs recompiled (PRED, SB):\t" + getRecompiledPredDAGs() + "/" + getRecompiledSBDAGs() + ".\n");
		sb.append("HOP DAGs recompile time:\t" + String.format("%.3f", ((double)getRecompileTime())/1000000000) + " sec.\n");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/RecompileStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("HOP DAGs recompiled (PRED, SB):\t" + getRecompiledPredDAGs() + "/" + getRecompiledSBDAGs() + ".\n");
		sb.append("HOP DAGs recompile time:\t" + String.format("%.3f", ((double)getRecompileTime())/1000000000) + " sec.\n");
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/SparkStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		String lazy = SparkExecutionContext.isLazySparkContextCreation() ? "(lazy)" : "(eager)";
		sb.append("Spark ctx create time "+lazy+":\t"+
				String.format("%.3f", ctxCreateTime*1e-9)  + " sec.\n" ); // nanoSec --> sec
		sb.append("Spark trans counts (par,bc,col):" +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/SparkStatistics.java`
#### Snippet
```java
		sb.append("Spark ctx create time "+lazy+":\t"+
				String.format("%.3f", ctxCreateTime*1e-9)  + " sec.\n" ); // nanoSec --> sec
		sb.append("Spark trans counts (par,bc,col):" +
				String.format("%d/%d/%d.\n", parallelizeCount.longValue(),
						broadcastCount.longValue(), collectCount.longValue()));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/SparkStatistics.java`
#### Snippet
```java
				String.format("%d/%d/%d.\n", parallelizeCount.longValue(),
						broadcastCount.longValue(), collectCount.longValue()));
		sb.append("Spark trans times (par,bc,col):\t" +
				String.format("%.3f/%.3f/%.3f secs.\n",
						parallelizeTime.longValue()*1e-9,
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/SparkStatistics.java`
#### Snippet
```java
						broadcastTime.longValue()*1e-9,
						collectTime.longValue()*1e-9));
		sb.append("Spark async. count (pf,bc,op): \t" +
				String.format("%d/%d/%d.\n", getAsyncPrefetchCount(), getAsyncBroadcastCount(), getAsyncSparkOpCount()));
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		String blas = NativeHelper.getCurrentBLAS();
		sb.append("Native " + blas + " calls (dense mult/conv/bwdF/bwdD):\t" + numLibMatrixMultCalls.longValue()  + "/"
			+ numConv2dCalls.longValue() + "/" + numConv2dBwdFilterCalls.longValue()
			+ "/" + numConv2dBwdDataCalls.longValue() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
			+ numConv2dCalls.longValue() + "/" + numConv2dBwdFilterCalls.longValue()
			+ "/" + numConv2dBwdDataCalls.longValue() + ".\n");
		sb.append("Native " + blas + " calls (sparse conv/bwdF/bwdD):\t"
			+ numSparseConv2dCalls.longValue() + "/" + numSparseConv2dBwdFilterCalls.longValue()
			+ "/" + numSparseConv2dBwdDataCalls.longValue() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/NativeStatistics.java`
#### Snippet
```java
			+ numSparseConv2dCalls.longValue() + "/" + numSparseConv2dBwdFilterCalls.longValue()
			+ "/" + numSparseConv2dBwdDataCalls.longValue() + ".\n");
		sb.append("Native " + blas + " times (dense mult/conv/bwdF/bwdD):\t"
			+ String.format("%.3f", libMatrixMultTime.longValue()*1e-9) + "/"
			+ String.format("%.3f", conv2dTime.longValue()*1e-9) + "/"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
		if( optCount.longValue() > 0 ){
			StringBuilder sb = new StringBuilder();
			sb.append("ParFor loops optimized:\t\t" + getOptCount() + ".\n");
			sb.append("ParFor optimize time:\t\t" + String.format("%.3f", ((double)getOptTime())/1000) + " sec.\n");
			sb.append("ParFor initialize time:\t\t" + String.format("%.3f", ((double)getInitTime())/1000) + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
			StringBuilder sb = new StringBuilder();
			sb.append("ParFor loops optimized:\t\t" + getOptCount() + ".\n");
			sb.append("ParFor optimize time:\t\t" + String.format("%.3f", ((double)getOptTime())/1000) + " sec.\n");
			sb.append("ParFor initialize time:\t\t" + String.format("%.3f", ((double)getInitTime())/1000) + " sec.\n");
			sb.append("ParFor result merge time:\t" + String.format("%.3f", ((double)getMergeTime())/1000) + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
			sb.append("ParFor loops optimized:\t\t" + getOptCount() + ".\n");
			sb.append("ParFor optimize time:\t\t" + String.format("%.3f", ((double)getOptTime())/1000) + " sec.\n");
			sb.append("ParFor initialize time:\t\t" + String.format("%.3f", ((double)getInitTime())/1000) + " sec.\n");
			sb.append("ParFor result merge time:\t" + String.format("%.3f", ((double)getMergeTime())/1000) + " sec.\n");
			sb.append("ParFor total update in-place:\t" + Statistics.getTotalUIPVar() + "/"
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
			sb.append("ParFor optimize time:\t\t" + String.format("%.3f", ((double)getOptTime())/1000) + " sec.\n");
			sb.append("ParFor initialize time:\t\t" + String.format("%.3f", ((double)getInitTime())/1000) + " sec.\n");
			sb.append("ParFor result merge time:\t" + String.format("%.3f", ((double)getMergeTime())/1000) + " sec.\n");
			sb.append("ParFor total update in-place:\t" + Statistics.getTotalUIPVar() + "/"
				+ Statistics.getTotalLixUIP() + "/" + Statistics.getTotalLix() + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/ParForStatistics.java`
#### Snippet
```java
			sb.append("ParFor initialize time:\t\t" + String.format("%.3f", ((double)getInitTime())/1000) + " sec.\n");
			sb.append("ParFor result merge time:\t" + String.format("%.3f", ((double)getMergeTime())/1000) + " sec.\n");
			sb.append("ParFor total update in-place:\t" + Statistics.getTotalUIPVar() + "/"
				+ Statistics.getTotalLixUIP() + "/" + Statistics.getTotalLix() + "\n");
			return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java
	public static String displayStatistics() {
		StringBuilder sb = new StringBuilder();
		sb.append("Codegen compile (DAG,CP,JC):\t" + getDAGCompile() + "/"
				+ getCPlanCompile() + "/" + getClassCompile() + ".\n");
		sb.append("Codegen enum (ALLt/p,EVALt/p):\t" + getEnumAll() + "/" +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java
		sb.append("Codegen compile (DAG,CP,JC):\t" + getDAGCompile() + "/"
				+ getCPlanCompile() + "/" + getClassCompile() + ".\n");
		sb.append("Codegen enum (ALLt/p,EVALt/p):\t" + getEnumAll() + "/" +
				getEnumAllP() + "/" + getEnumEval() + "/" + getEnumEvalP() + ".\n");
		sb.append("Codegen compile times (DAG,JC):\t" + String.format("%.3f", (double)getCompileTime()/1000000000) + "/" +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java
		sb.append("Codegen enum (ALLt/p,EVALt/p):\t" + getEnumAll() + "/" +
				getEnumAllP() + "/" + getEnumEval() + "/" + getEnumEvalP() + ".\n");
		sb.append("Codegen compile times (DAG,JC):\t" + String.format("%.3f", (double)getCompileTime()/1000000000) + "/" +
				String.format("%.3f", (double)getClassCompileTime()/1000000000)  + " sec.\n");
		sb.append("Codegen enum plan cache hits:\t" + getPlanCacheHits() + "/" + getPlanCacheTotal() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java
		sb.append("Codegen compile times (DAG,JC):\t" + String.format("%.3f", (double)getCompileTime()/1000000000) + "/" +
				String.format("%.3f", (double)getClassCompileTime()/1000000000)  + " sec.\n");
		sb.append("Codegen enum plan cache hits:\t" + getPlanCacheHits() + "/" + getPlanCacheTotal() + ".\n");
		sb.append("Codegen op plan cache hits:\t" + getOpCacheHits() + "/" + getOpCacheTotal() + ".\n");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/stats/CodegenStatistics.java`
#### Snippet
```java
				String.format("%.3f", (double)getClassCompileTime()/1000000000)  + " sec.\n");
		sb.append("Codegen enum plan cache hits:\t" + getPlanCacheHits() + "/" + getPlanCacheTotal() + ".\n");
		sb.append("Codegen op plan cache hits:\t" + getOpCacheHits() + "/" + getOpCacheTotal() + ".\n");
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/PrintStatement.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(_type + "(");
		if ((_type == PRINTTYPE.PRINT) || (_type == PRINTTYPE.STOP) || (_type == PRINTTYPE.ASSERT)) {
			Expression expression = expressions.get(0);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/DMLProgram.java`
#### Snippet
```java
		for (String namespaceKey : this.getNamespaces().keySet()){
			
			sb.append("NAMESPACE = " + namespaceKey + "\n");
			FunctionDictionary<FunctionStatementBlock> dict = getNamespaces().get(namespaceKey);
			
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
		sb.append("SystemDS Statistics:\n");
		if( DMLScript.STATISTICS ) {
			sb.append("Total elapsed time:\t\t" + String.format("%.3f", (getCompileTime()+getRunTime())*1e-9) + " sec.\n"); // nanoSec --> sec
			sb.append("Total compilation time:\t\t" + String.format("%.3f", getCompileTime()*1e-9) + " sec.\n"); // nanoSec --> sec
			sb.append(FederatedCompilationTimer.getStringRepresentation());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
		if( DMLScript.STATISTICS ) {
			sb.append("Total elapsed time:\t\t" + String.format("%.3f", (getCompileTime()+getRunTime())*1e-9) + " sec.\n"); // nanoSec --> sec
			sb.append("Total compilation time:\t\t" + String.format("%.3f", getCompileTime()*1e-9) + " sec.\n"); // nanoSec --> sec
			sb.append(FederatedCompilationTimer.getStringRepresentation());
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			sb.append(FederatedCompilationTimer.getStringRepresentation());
		}
		sb.append("Total execution time:\t\t" + String.format("%.3f", getRunTime()*1e-9) + " sec.\n"); // nanoSec --> sec
		if( OptimizerUtils.isSparkExecutionMode() ) {
			if( DMLScript.STATISTICS ) //moved into stats on Shiv's request
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
		if( OptimizerUtils.isSparkExecutionMode() ) {
			if( DMLScript.STATISTICS ) //moved into stats on Shiv's request
				sb.append("Number of compiled Spark inst:\t" + getNoOfCompiledSPInst() + ".\n");
			sb.append("Number of executed Spark inst:\t" + getNoOfExecutedSPInst() + ".\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			if( DMLScript.STATISTICS ) //moved into stats on Shiv's request
				sb.append("Number of compiled Spark inst:\t" + getNoOfCompiledSPInst() + ".\n");
			sb.append("Number of executed Spark inst:\t" + getNoOfExecutedSPInst() + ".\n");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java

			if(recomputeNNZTime != 0 || examSparsityTime != 0 || allocateDoubleArrTime != 0) {
				sb.append("MatrixBlock times (recomputeNNZ/examSparsity/allocateDoubleArr):\t" + String.format("%.3f", recomputeNNZTime*1e-9) + "/" +
						String.format("%.3f", examSparsityTime*1e-9) + "/" + String.format("%.3f", allocateDoubleArrTime*1e-9)  + ".\n");
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			}

			sb.append("Cache hits (Mem/Li/WB/FS/HDFS):\t" + CacheStatistics.displayHits() + ".\n");
			sb.append("Cache writes (Li/WB/FS/HDFS):\t" + CacheStatistics.displayWrites() + ".\n");
			sb.append("Cache times (ACQr/m, RLS, EXP):\t" + CacheStatistics.displayTime() + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java

			sb.append("Cache hits (Mem/Li/WB/FS/HDFS):\t" + CacheStatistics.displayHits() + ".\n");
			sb.append("Cache writes (Li/WB/FS/HDFS):\t" + CacheStatistics.displayWrites() + ".\n");
			sb.append("Cache times (ACQr/m, RLS, EXP):\t" + CacheStatistics.displayTime() + " sec.\n");
			if (DMLScript.JMLC_MEM_STATISTICS)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			sb.append("Cache hits (Mem/Li/WB/FS/HDFS):\t" + CacheStatistics.displayHits() + ".\n");
			sb.append("Cache writes (Li/WB/FS/HDFS):\t" + CacheStatistics.displayWrites() + ".\n");
			sb.append("Cache times (ACQr/m, RLS, EXP):\t" + CacheStatistics.displayTime() + " sec.\n");
			if (DMLScript.JMLC_MEM_STATISTICS)
				sb.append("Max size of live objects:\t" + byteCountToDisplaySize(getSizeofPinnedObjects()) + " ("  + getNumPinnedObjects() + " total objects)" + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			sb.append("Cache times (ACQr/m, RLS, EXP):\t" + CacheStatistics.displayTime() + " sec.\n");
			if (DMLScript.JMLC_MEM_STATISTICS)
				sb.append("Max size of live objects:\t" + byteCountToDisplaySize(getSizeofPinnedObjects()) + " ("  + getNumPinnedObjects() + " total objects)" + "\n");

			sb.append(RecompileStatistics.displayStatistics());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java

			if( getFunRecompiles()>0 ) {
				sb.append("Functions recompiled:\t\t" + getFunRecompiles() + ".\n");
				sb.append("Functions recompile time:\t" + String.format("%.3f", ((double)getFunRecompileTime())/1000000000) + " sec.\n");
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			if( getFunRecompiles()>0 ) {
				sb.append("Functions recompiled:\t\t" + getFunRecompiles() + ".\n");
				sb.append("Functions recompile time:\t" + String.format("%.3f", ((double)getFunRecompileTime())/1000000000) + " sec.\n");
			}
			if (DMLScript.LINEAGE && !ReuseCacheType.isNone()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			}
			if (DMLScript.LINEAGE && !ReuseCacheType.isNone()) {
				sb.append("LinCache hits (Mem/FS/Del): \t" + LineageCacheStatistics.displayHits() + ".\n");
				sb.append("LinCache MultiLevel (Ins/SB/Fn):" + LineageCacheStatistics.displayMultiLevelHits() + ".\n");
				if (LineageCacheStatistics.ifGpuStats()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			if (DMLScript.LINEAGE && !ReuseCacheType.isNone()) {
				sb.append("LinCache hits (Mem/FS/Del): \t" + LineageCacheStatistics.displayHits() + ".\n");
				sb.append("LinCache MultiLevel (Ins/SB/Fn):" + LineageCacheStatistics.displayMultiLevelHits() + ".\n");
				if (LineageCacheStatistics.ifGpuStats()) {
					sb.append("LinCache GPU (Hit/Async/Sync): \t" + LineageCacheStatistics.displayGpuStats() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				sb.append("LinCache MultiLevel (Ins/SB/Fn):" + LineageCacheStatistics.displayMultiLevelHits() + ".\n");
				if (LineageCacheStatistics.ifGpuStats()) {
					sb.append("LinCache GPU (Hit/Async/Sync): \t" + LineageCacheStatistics.displayGpuStats() + ".\n");
					sb.append("LinCache GPU (Recyc/Del): \t" + LineageCacheStatistics.displayGpuPointerStats() + ".\n");
					sb.append("LinCache GPU evict time: \t" + LineageCacheStatistics.displayGpuEvictTime() + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				if (LineageCacheStatistics.ifGpuStats()) {
					sb.append("LinCache GPU (Hit/Async/Sync): \t" + LineageCacheStatistics.displayGpuStats() + ".\n");
					sb.append("LinCache GPU (Recyc/Del): \t" + LineageCacheStatistics.displayGpuPointerStats() + ".\n");
					sb.append("LinCache GPU evict time: \t" + LineageCacheStatistics.displayGpuEvictTime() + " sec.\n");
				}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
					sb.append("LinCache GPU (Hit/Async/Sync): \t" + LineageCacheStatistics.displayGpuStats() + ".\n");
					sb.append("LinCache GPU (Recyc/Del): \t" + LineageCacheStatistics.displayGpuPointerStats() + ".\n");
					sb.append("LinCache GPU evict time: \t" + LineageCacheStatistics.displayGpuEvictTime() + " sec.\n");
				}
				if (LineageCacheStatistics.ifSparkStats()) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				}
				if (LineageCacheStatistics.ifSparkStats()) {
					sb.append("LinCache Spark (Col/Loc/Dist): \t" + LineageCacheStatistics.displaySparkHits() + ".\n");
					sb.append("LinCache Spark (Per/Unper): \t" + LineageCacheStatistics.displaySparkPersist() + ".\n");
				}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				if (LineageCacheStatistics.ifSparkStats()) {
					sb.append("LinCache Spark (Col/Loc/Dist): \t" + LineageCacheStatistics.displaySparkHits() + ".\n");
					sb.append("LinCache Spark (Per/Unper): \t" + LineageCacheStatistics.displaySparkPersist() + ".\n");
				}
				sb.append("LinCache writes (Mem/FS/Del): \t" + LineageCacheStatistics.displayWtrites() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
					sb.append("LinCache Spark (Per/Unper): \t" + LineageCacheStatistics.displaySparkPersist() + ".\n");
				}
				sb.append("LinCache writes (Mem/FS/Del): \t" + LineageCacheStatistics.displayWtrites() + ".\n");
				sb.append("LinCache FStimes (Rd/Wr): \t" + LineageCacheStatistics.displayFSTime() + " sec.\n");
				sb.append("LinCache Computetime (S/M): \t" + LineageCacheStatistics.displayComputeTime() + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				}
				sb.append("LinCache writes (Mem/FS/Del): \t" + LineageCacheStatistics.displayWtrites() + ".\n");
				sb.append("LinCache FStimes (Rd/Wr): \t" + LineageCacheStatistics.displayFSTime() + " sec.\n");
				sb.append("LinCache Computetime (S/M): \t" + LineageCacheStatistics.displayComputeTime() + " sec.\n");
				sb.append("LinCache Rewrites:    \t\t" + LineageCacheStatistics.displayRewrites() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				sb.append("LinCache writes (Mem/FS/Del): \t" + LineageCacheStatistics.displayWtrites() + ".\n");
				sb.append("LinCache FStimes (Rd/Wr): \t" + LineageCacheStatistics.displayFSTime() + " sec.\n");
				sb.append("LinCache Computetime (S/M): \t" + LineageCacheStatistics.displayComputeTime() + " sec.\n");
				sb.append("LinCache Rewrites:    \t\t" + LineageCacheStatistics.displayRewrites() + ".\n");
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
				sb.append("LinCache FStimes (Rd/Wr): \t" + LineageCacheStatistics.displayFSTime() + " sec.\n");
				sb.append("LinCache Computetime (S/M): \t" + LineageCacheStatistics.displayComputeTime() + " sec.\n");
				sb.append("LinCache Rewrites:    \t\t" + LineageCacheStatistics.displayRewrites() + ".\n");
			}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			}

			sb.append("Total JIT compile time:\t\t" + ((double)getJITCompileTime())/1000 + " sec.\n");
			sb.append("Total JVM GC count:\t\t" + getJVMgcCount() + ".\n");
			sb.append("Total JVM GC time:\t\t" + ((double)getJVMgcTime())/1000 + " sec.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java

			sb.append("Total JIT compile time:\t\t" + ((double)getJITCompileTime())/1000 + " sec.\n");
			sb.append("Total JVM GC count:\t\t" + getJVMgcCount() + ".\n");
			sb.append("Total JVM GC time:\t\t" + ((double)getJVMgcTime())/1000 + " sec.\n");
			sb.append("Heavy hitter instructions:\n" + getHeavyHitters(maxHeavyHitters));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			sb.append("Total JIT compile time:\t\t" + ((double)getJITCompileTime())/1000 + " sec.\n");
			sb.append("Total JVM GC count:\t\t" + getJVMgcCount() + ".\n");
			sb.append("Total JVM GC time:\t\t" + ((double)getJVMgcTime())/1000 + " sec.\n");
			sb.append("Heavy hitter instructions:\n" + getHeavyHitters(maxHeavyHitters));
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			sb.append("Total JVM GC count:\t\t" + getJVMgcCount() + ".\n");
			sb.append("Total JVM GC time:\t\t" + ((double)getJVMgcTime())/1000 + " sec.\n");
			sb.append("Heavy hitter instructions:\n" + getHeavyHitters(maxHeavyHitters));
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Statistics.java`
#### Snippet
```java
			String numStr = String.format("  %-" + numPadLen + "s", ix);
			String objNameStr = String.format("  %-" + maxNameLength + "s ", objName);
			res.append(numStr + objNameStr + String.format("  %s", objSize) + "\n");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/FunctionStatement.java`
#### Snippet
```java
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append(_name + " = ");
		
		sb.append("function ( ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/ImportStatement.java`
#### Snippet
```java
		 StringBuilder sb = new StringBuilder();
		 sb.append(Statement.SOURCE + "(");
		 sb.append(this._filePath + ")");
		 
		 if (this._namespace != null){
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/ImportStatement.java`
#### Snippet
```java
		 
		 if (this._namespace != null){
			 sb.append(" AS " + this._namespace);
		 }
		 sb.append(";");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		//create header
		sb.append("\nPROGRAM ( size CP/"+(sparkExec?"SP":"MR")+" = ");
		sb.append(counts.numCPInst);
		sb.append("/");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				FunctionProgramBlock fpb = e.getValue();
				//explain optimized function
				sb.append("----FUNCTION "+fkey+" [recompile="+fpb.isRecompileOnce()+"]\n");
				for( ProgramBlock pb : fpb.getChildBlocks() )
					sb.append( explainProgramBlock(pb,3) );
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				if( rtprog.containsFunctionProgramBlock(fkey, false) ) {
					FunctionProgramBlock fpb2 = rtprog.getFunctionProgramBlock(fkey, false);
					sb.append("----FUNCTION "+fkey+" (unoptimized) [recompile="+fpb2.isRecompileOnce()+"]\n");
					for( ProgramBlock pb : fpb2.getChildBlocks() )
						sb.append( explainProgramBlock(pb,3) );
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
		//hop id
		if( SHOW_DATA_DEPENDENCIES )
			sb.append("("+hop.getHopID()+") ");

		//operation string
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		//matrix characteristics
		sb.append(" [" + hop.getDim1() + ","
				+ hop.getDim2() + ","
				+ hop.getBlocksize() + ","
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		if (hop.getUpdateType().isInPlace())
			sb.append("," + hop.getUpdateType().toString().toLowerCase());

		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		//memory estimates
		sb.append(" [" + showMem(hop.getInputMemEstimate(), false) + ","
				+ showMem(hop.getIntermediateMemEstimate(), false) + ","
				+ showMem(hop.getOutputMemEstimate(), false) + " -> "
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
		//exec type
		if (hop.getExecType() != null)
			sb.append(", " + hop.getExecType());

		if ( hop.getFederatedOutput() != FederatedOutput.NONE )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
		//hop id
		if( SHOW_DATA_DEPENDENCIES )
			sb.append("("+cnode.getID()+") ");

		//operation string
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			sb.append(offset);
			if( wsb != null && !wsb.getUpdateInPlaceVars().isEmpty() )
				sb.append("WHILE (lines "+wpb.getBeginLine()+"-"+wpb.getEndLine()+") [in-place="+wsb.getUpdateInPlaceVars().toString()+"]\n");
			else
				sb.append("WHILE (lines "+wpb.getBeginLine()+"-"+wpb.getEndLine()+")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				sb.append("WHILE (lines "+wpb.getBeginLine()+"-"+wpb.getEndLine()+") [in-place="+wsb.getUpdateInPlaceVars().toString()+"]\n");
			else
				sb.append("WHILE (lines "+wpb.getBeginLine()+"-"+wpb.getEndLine()+")\n");
			sb.append(explainInstructions(wpb.getPredicate(), level+1));
			for( ProgramBlock pbc : wpb.getChildBlocks() )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			IfProgramBlock ipb = (IfProgramBlock) pb;
			sb.append(offset);
			sb.append("IF (lines "+ipb.getBeginLine()+"-"+ipb.getEndLine()+")\n");
			sb.append(explainInstructions(ipb.getPredicate(), level+1));
			for( ProgramBlock pbc : ipb.getChildBlocksIfBody() )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			sb.append(offset);
			if( pb instanceof ParForProgramBlock )
				sb.append("PARFOR (lines "+fpb.getBeginLine()+"-"+fpb.getEndLine()+")\n");
			else {
				if( fsb != null && !fsb.getUpdateInPlaceVars().isEmpty() )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			else {
				if( fsb != null && !fsb.getUpdateInPlaceVars().isEmpty() )
					sb.append("FOR (lines "+fpb.getBeginLine()+"-"+fpb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					sb.append("FOR (lines "+fpb.getBeginLine()+"-"+fpb.getEndLine()+")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
					sb.append("FOR (lines "+fpb.getBeginLine()+"-"+fpb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					sb.append("FOR (lines "+fpb.getBeginLine()+"-"+fpb.getEndLine()+")\n");
			}
			sb.append(explainInstructions(fpb.getFromInstructions(), level+1));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			sb.append(offset);
			if( pb.getStatementBlock()!=null )
				sb.append("GENERIC (lines "+pb.getBeginLine()+"-"+pb.getEndLine()+") [recompile="+pb.getStatementBlock().requiresRecompilation()+"]\n");
			else
				sb.append("GENERIC (lines "+pb.getBeginLine()+"-"+pb.getEndLine()+") \n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				sb.append("GENERIC (lines "+pb.getBeginLine()+"-"+pb.getEndLine()+") [recompile="+pb.getStatementBlock().requiresRecompilation()+"]\n");
			else
				sb.append("GENERIC (lines "+pb.getBeginLine()+"-"+pb.getEndLine()+") \n");
			sb.append(explainInstructions(bpb.getInstructions(), level+1));
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			builder.append(offset);
			if( !wsb.getUpdateInPlaceVars().isEmpty() )
				builder.append("WHILE (lines "+wsb.getBeginLine()+"-"+wsb.getEndLine()+") [in-place="+wsb.getUpdateInPlaceVars().toString()+"]\n");
			else
				builder.append("WHILE (lines "+wsb.getBeginLine()+"-"+wsb.getEndLine()+")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				builder.append("WHILE (lines "+wsb.getBeginLine()+"-"+wsb.getEndLine()+") [in-place="+wsb.getUpdateInPlaceVars().toString()+"]\n");
			else
				builder.append("WHILE (lines "+wsb.getBeginLine()+"-"+wsb.getEndLine()+")\n");
			builder.append(explainHop(wsb.getPredicateHops(), level+1));

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			IfStatementBlock ifsb = (IfStatementBlock) sb;
			builder.append(offset);
			builder.append("IF (lines "+ifsb.getBeginLine()+"-"+ifsb.getEndLine()+")\n");
			builder.append(explainHop(ifsb.getPredicateHops(), level+1));

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			if (sb instanceof ParForStatementBlock) {
				if( !fsb.getUpdateInPlaceVars().isEmpty() )
					builder.append("PARFOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					builder.append("PARFOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
					builder.append("PARFOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					builder.append("PARFOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+")\n");
			}
			else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			else {
				if( !fsb.getUpdateInPlaceVars().isEmpty() )
					builder.append("FOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					builder.append("FOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+")\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
					builder.append("FOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+") [in-place="+fsb.getUpdateInPlaceVars().toString()+"]\n");
				else
					builder.append("FOR (lines "+fsb.getBeginLine()+"-"+fsb.getEndLine()+")\n");
			}
			if (fsb.getFromHops() != null)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			// For generic StatementBlock
			builder.append(offset);
			builder.append("GENERIC (lines "+sb.getBeginLine()+"-"+sb.getEndLine()+") [recompile=" + sb.requiresRecompilation() + "]\n");
			ArrayList<Hop> hopsDAG = sb.getHops();
			if( hopsDAG != null && !hopsDAG.isEmpty() ) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			for( String cfkey : cfkeys ) {
				if( fstack.contains(cfkey) && fgraph.isRecursiveFunction(cfkey) )
					builder.append(offset + "--" + cfkey + " (recursive)\n");
				else {
					fstack.add(cfkey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				else {
					fstack.add(cfkey);
					builder.append(offset + "--" + cfkey + "\n");
					builder.append(explainFunctionCallGraph(fgraph, fstack, cfkey, level+1));
					fstack.remove(cfkey);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
					FunctionStatement fstmt = (FunctionStatement) fsb.getStatement(0);
					String fkey = DMLProgram.constructFunctionKey(namespace, fname);
					sb.append("----FUNCTION " + fkey + " [recompile="+fsb.isRecompileOnce()+"]\n");
					for (StatementBlock current : fstmt.getBody())
						sb.append(explainStatementBlock(current, 3));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
		//create template header
		sb.append("\n----------------------------------------\n");
		sb.append("CPLAN: "+cplan.getTemplateInfo()+"\n");
		sb.append("--inputs: "+Arrays.toString(cplan.getInputNames())+"\n");
		sb.append("----------------------------------------\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
		sb.append("\n----------------------------------------\n");
		sb.append("CPLAN: "+cplan.getTemplateInfo()+"\n");
		sb.append("--inputs: "+Arrays.toString(cplan.getInputNames())+"\n");
		sb.append("----------------------------------------\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/OutputStatement.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(Statement.OUTPUTSTATEMENT + "(");
		sb.append("id=" + _id.toString());
		for (String key : _paramsExpr.getVarParams().keySet()) {
			sb.append(", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/FunctionCallIdentifier.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		if (_namespace != null && _namespace.length() > 0 && !_namespace.equals(DMLProgram.DEFAULT_NAMESPACE))
			sb.append(_namespace + "::");
		sb.append(_name);
		sb.append(" ( ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			sb.append("\n");
		}
		if (_liveOut != null) sb.append("liveout " + _liveOut.toString() + "\n");
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		}
		if (_liveOut != null) sb.append("liveout " + _liveOut.toString() + "\n");
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		if (_liveOut != null) sb.append("liveout " + _liveOut.toString() + "\n");
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
		if (_read != null && !_read.getVariables().isEmpty()) sb.append("read " + _read.toString()+ "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
		if (_read != null && !_read.getVariables().isEmpty()) sb.append("read " + _read.toString()+ "\n");
		if (_updated != null && !_updated.getVariables().isEmpty()) sb.append("updated " + _updated.toString()+ "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
		if (_read != null && !_read.getVariables().isEmpty()) sb.append("read " + _read.toString()+ "\n");
		if (_updated != null && !_updated.getVariables().isEmpty()) sb.append("updated " + _updated.toString()+ "\n");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
		if (_read != null && !_read.getVariables().isEmpty()) sb.append("read " + _read.toString()+ "\n");
		if (_updated != null && !_updated.getVariables().isEmpty()) sb.append("updated " + _updated.toString()+ "\n");
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java

		 for (String key : _varParams.keySet()){
			 sb.append("," + key + "=" + _varParams.get(key));
		 }
		sb.append(" )");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/parser/dml/CustomErrorListener.java`
#### Snippet
```java
			sb.append("\nThe following parse issue was encountered:\n");
		else
			sb.append("\nThe following " + parseIssues.size() + " parse issues were encountered:\n");
		int count = 1;
		for (ParseIssue parseIssue : parseIssues) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSV.java`
#### Snippet
```java
				//append meta data
				if( !src.isColumnMetadataDefault() ) {
					sb.append(TfUtils.TXMTD_MVPREFIX + delim);
					for( int j=0; j<cols; j++ )
						sb.append(src.getColumnMetadata(j).getMvValue() + ((j<cols-1)?delim:""));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSV.java`
#### Snippet
```java
					sb.append(TfUtils.TXMTD_MVPREFIX + delim);
					for( int j=0; j<cols; j++ )
						sb.append(src.getColumnMetadata(j).getMvValue() + ((j<cols-1)?delim:""));
					sb.append("\n");
					sb.append(TfUtils.TXMTD_NDPREFIX + delim);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSV.java`
#### Snippet
```java
						sb.append(src.getColumnMetadata(j).getMvValue() + ((j<cols-1)?delim:""));
					sb.append("\n");
					sb.append(TfUtils.TXMTD_NDPREFIX + delim);
					for( int j=0; j<cols; j++ )
						sb.append(src.getColumnMetadata(j).getNumDistinct() + ((j<cols-1)?delim:""));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSV.java`
#### Snippet
```java
					sb.append(TfUtils.TXMTD_NDPREFIX + delim);
					for( int j=0; j<cols; j++ )
						sb.append(src.getColumnMetadata(j).getNumDistinct() + ((j<cols-1)?delim:""));
					sb.append("\n");
				}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesHDF5.java`
#### Snippet
```java
	@Override public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(" datasetName " + datasetName);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java
			
				// output number of rows, number of columns and number of nnz
				sb.append (rlen + " " + clen + " " + nnz + "\n");
				br.write( sb.toString());
				sb.setLength(0);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java

		// output number of rows, number of columns and number of nnz
		sb.append (rlen + " " + clen + " " + nnz + "\n");
		out.write (sb.toString().getBytes());

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCell.java`
#### Snippet
```java
				for( int j=0; j<cols; j++ )
					if( !src.isColumnMetadataDefault(j) ) {
						sb.append("-1 " + (j+1) + " " + src.getColumnMetadata(j).getNumDistinct() + "\n");
						sb.append("-2 " + (j+1) + " " + src.getColumnMetadata(j).getMvValue() + "\n");
						br.write( sb.toString() );
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCell.java`
#### Snippet
```java
					if( !src.isColumnMetadataDefault(j) ) {
						sb.append("-1 " + (j+1) + " " + src.getColumnMetadata(j).getNumDistinct() + "\n");
						sb.append("-2 " + (j+1) + " " + src.getColumnMetadata(j).getMvValue() + "\n");
						br.write( sb.toString() );
						sb.setLength(0);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(" delim " + delim);
		sb.append(" indexDelim " + indexDelim);
		sb.append(" sparse " + sparse);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(" delim " + delim);
		sb.append(" indexDelim " + indexDelim);
		sb.append(" sparse " + sparse);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
		sb.append(" delim " + delim);
		sb.append(" indexDelim " + indexDelim);
		sb.append(" sparse " + sparse);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append("header " +header);
		sb.append(" delim " + delim);
		sb.append(" fill " + fill);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("header " +header);
		sb.append(" delim " + delim);
		sb.append(" fill " + fill);
		sb.append(" fillValue " + fillValue);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		sb.append("header " +header);
		sb.append(" delim " + delim);
		sb.append(" fill " + fill);
		sb.append(" fillValue " + fillValue);
		sb.append(" naStrings " + naStrings);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		sb.append(" delim " + delim);
		sb.append(" fill " + fill);
		sb.append(" fillValue " + fillValue);
		sb.append(" naStrings " + naStrings);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		sb.append(" fill " + fill);
		sb.append(" fillValue " + fillValue);
		sb.append(" naStrings " + naStrings);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCSV.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		for (int i = 0; i < clen; i++) {
			sb.append("C" + (i + 1));
			if (i < clen - 1)
				sb.append(_props.getDelim());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCSV.java`
#### Snippet
```java
					for( int j=bj; j < Math.min(clen,bj+BLOCKSIZE_J); j++) 
					{
						sb.append("C"+ (j+1));
						if ( j < clen-1 )
							sb.append(delim);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/util/DependencyThreadPool.java`
#### Snippet
```java
			}
			sbs[level].append(offsets[level]);
			sbs[level].append(entry.getKey().toString()+"\n");
		}
		System.out.println("EXPlAIN (TASK-GRAPH):");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		//print frame header
		sb.append("# FRAME: ");
		sb.append("nrow = " + fb.getNumRows() + ", ");
		sb.append("ncol = " + fb.getNumColumns() + lineseparator);

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuffer.append()` call
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
		sb.append("# FRAME: ");
		sb.append("nrow = " + fb.getNumRows() + ", ");
		sb.append("ncol = " + fb.getNumColumns() + lineseparator);

		//print column names
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_data.length * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/LongArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_data.length * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BooleanArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_data.length * 2 + 10);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append((_data[i] ? 1 : 0) + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BooleanArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append((_data[i] ? 1 : 0) + ",");
		sb.append(_data[_size - 1] ? 1 : 0);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/ColumnMetadata.java`
#### Snippet
```java
		}
		if(_mvValue != null)
			sb.append("--" + _mvValue);

		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_data.length * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/IntegerArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_size + 2);
		sb.append(super.toString() + "<" + _a.getClass().getSimpleName() + ">:[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(get(i) + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/OptionalArray.java`
#### Snippet
```java
		sb.append(super.toString() + "<" + _a.getClass().getSimpleName() + ">:[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(get(i) + ",");
		sb.append(get(_size - 1));
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/BitSetArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_size + 10);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size; i++)
			sb.append((get(i) ? 1 : 0));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_data.length * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/FloatArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java
		
		//handle additional configurations
		sb.append( CONF_STATS + "=" + DMLScript.STATISTICS );
		sb.append( COMPONENTS_DELIM );
		sb.append( NEWLINE );
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/util/ProgramConverter.java`
#### Snippet
```java

		//handle additional configurations
		builder.append(CONF_STATS + "=" + DMLScript.STATISTICS);
		builder.append(COMPONENTS_DELIM);
		builder.append(NEWLINE);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder(_size * 5 + 2);
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		sb.append(super.toString() + ":[");
		for(int i = 0; i < _size - 1; i++)
			sb.append(_data[i] + ",");
		sb.append(_data[_size - 1]);
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		sb.append("import org.apache.sysds.runtime.frame.data.FrameBlock.FrameMapFunction;\n");
		sb.append("import java.util.Arrays;\n");
		sb.append("public class " + cname + " extends FrameMapFunction {\n");
		if(margin != 0) {
			sb.append("public String[] apply(String[] " + varname[0].trim() + ") {\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		sb.append("public class " + cname + " extends FrameMapFunction {\n");
		if(margin != 0) {
			sb.append("public String[] apply(String[] " + varname[0].trim() + ") {\n");
			sb.append("  return UtilFunctions.toStringArray(" + expr + "); }}\n");
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		if(margin != 0) {
			sb.append("public String[] apply(String[] " + varname[0].trim() + ") {\n");
			sb.append("  return UtilFunctions.toStringArray(" + expr + "); }}\n");
		}
		else {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		else {
			if(varname.length == 1) {
				sb.append("public String apply(String " + varname[0].trim() + ") {\n");
				sb.append("  return String.valueOf(" + expr + "); }}\n");
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
			if(varname.length == 1) {
				sb.append("public String apply(String " + varname[0].trim() + ") {\n");
				sb.append("  return String.valueOf(" + expr + "); }}\n");
			}
			else if(varname.length == 2) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
			}
			else if(varname.length == 2) {
				sb.append("public String apply(String " + varname[0].trim() + ", String " + varname[1].trim() + ") {\n");
				sb.append("  return String.valueOf(" + expr + "); }}\n");
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
			else if(varname.length == 2) {
				sb.append("public String apply(String " + varname[0].trim() + ", String " + varname[1].trim() + ") {\n");
				sb.append("  return String.valueOf(" + expr + "); }}\n");
			}
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder("PRNG[");
		sb.append("pdf = "+_pdf.name()+", ");
		sb.append("rows = "+_rows+", ");
		sb.append("cols = "+_cols+", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder("PRNG[");
		sb.append("pdf = "+_pdf.name()+", ");
		sb.append("rows = "+_rows+", ");
		sb.append("cols = "+_cols+", ");
		sb.append("blen = "+_blocksize+", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		sb.append("pdf = "+_pdf.name()+", ");
		sb.append("rows = "+_rows+", ");
		sb.append("cols = "+_cols+", ");
		sb.append("blen = "+_blocksize+", ");
		sb.append("sparsity = "+_sparsity+", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		sb.append("rows = "+_rows+", ");
		sb.append("cols = "+_cols+", ");
		sb.append("blen = "+_blocksize+", ");
		sb.append("sparsity = "+_sparsity+", ");
		sb.append("min = "+_min+", ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		sb.append("cols = "+_cols+", ");
		sb.append("blen = "+_blocksize+", ");
		sb.append("sparsity = "+_sparsity+", ");
		sb.append("min = "+_min+", ");
		sb.append("max = "+_max+"]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		sb.append("blen = "+_blocksize+", ");
		sb.append("sparsity = "+_sparsity+", ");
		sb.append("min = "+_min+", ");
		sb.append("max = "+_max+"]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/RandomMatrixGenerator.java`
#### Snippet
```java
		sb.append("sparsity = "+_sparsity+", ");
		sb.append("min = "+_min+", ");
		sb.append("max = "+_max+"]");
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
		while (scanner.hasNextLine()) {
			String line = scanner.nextLine();
			sb.append(line_count++ + ": " + line + System.lineSeparator());
		}
		scanner.close();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
			int tl = String.valueOf(op.getRight()*1e-3).indexOf(".");
			if (op != null && op.getRight() > 0)
				sb.append(String.valueOf(i) 
					+ String.format("%"+(4-String.valueOf(i).length())+"s", "") // 4-length(i) spaces
					+ op.getLeft() 
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
	public static String displayLineageEstimates() {
		StringBuilder sb = new StringBuilder();
		sb.append("Compute Time (Elapsed/Saved): \t" + displayComputeTime() + " sec.\n");
		sb.append("Space Used (C/R/L): \t\t" + displaySize() + " MB.\n"); // total cached/reused/cache limit
		sb.append("Cache Full Timestamp: \t\t" + LineageEstimator.computeCacheFullTime() + "% instructions.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("Compute Time (Elapsed/Saved): \t" + displayComputeTime() + " sec.\n");
		sb.append("Space Used (C/R/L): \t\t" + displaySize() + " MB.\n"); // total cached/reused/cache limit
		sb.append("Cache Full Timestamp: \t\t" + LineageEstimator.computeCacheFullTime() + "% instructions.\n");
		sb.append(displayReusableInsts());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
		sb.append("Compute Time (Elapsed/Saved): \t" + displayComputeTime() + " sec.\n");
		sb.append("Space Used (C/R/L): \t\t" + displaySize() + " MB.\n"); // total cached/reused/cache limit
		sb.append("Cache Full Timestamp: \t\t" + LineageEstimator.computeCacheFullTime() + "% instructions.\n");
		sb.append(displayReusableInsts());
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("CompressionSettings: ");
		sb.append("\t Valid Compressions: " + validCompressions);
		sb.append("\t Share dict: " + allowSharedDictionary);
		sb.append("\t Partitioner: " + columnPartitioner);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		sb.append("CompressionSettings: ");
		sb.append("\t Valid Compressions: " + validCompressions);
		sb.append("\t Share dict: " + allowSharedDictionary);
		sb.append("\t Partitioner: " + columnPartitioner);
		sb.append("\t Lossy: " + lossy);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		sb.append("\t Valid Compressions: " + validCompressions);
		sb.append("\t Share dict: " + allowSharedDictionary);
		sb.append("\t Partitioner: " + columnPartitioner);
		sb.append("\t Lossy: " + lossy);
		sb.append("\t Cost Computation Type: " + costComputationType);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		sb.append("\t Share dict: " + allowSharedDictionary);
		sb.append("\t Partitioner: " + columnPartitioner);
		sb.append("\t Lossy: " + lossy);
		sb.append("\t Cost Computation Type: " + costComputationType);
		if(samplingRatio < 1.0)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		sb.append("\t Partitioner: " + columnPartitioner);
		sb.append("\t Lossy: " + lossy);
		sb.append("\t Cost Computation Type: " + costComputationType);
		if(samplingRatio < 1.0)
			sb.append("\t Estimation Type: " + estimationType);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionSettings.java`
#### Snippet
```java
		sb.append("\t Cost Computation Type: " + costComputationType);
		if(samplingRatio < 1.0)
			sb.append("\t Estimation Type: " + estimationType);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("\nCompressionStatistics:");
		sb.append("\nDense Size            : " + denseSize);
		sb.append("\nOriginal Size         : " + originalSize);
		sb.append("\nCompressed Size       : " + compressedSize);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		sb.append("\nCompressionStatistics:");
		sb.append("\nDense Size            : " + denseSize);
		sb.append("\nOriginal Size         : " + originalSize);
		sb.append("\nCompressed Size       : " + compressedSize);
		sb.append("\nCompressionRatio      : " + getRatio());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		sb.append("\nDense Size            : " + denseSize);
		sb.append("\nOriginal Size         : " + originalSize);
		sb.append("\nCompressed Size       : " + compressedSize);
		sb.append("\nCompressionRatio      : " + getRatio());
		sb.append("\nDenseCompressionRatio : " + getDenseRatio());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		sb.append("\nOriginal Size         : " + originalSize);
		sb.append("\nCompressed Size       : " + compressedSize);
		sb.append("\nCompressionRatio      : " + getRatio());
		sb.append("\nDenseCompressionRatio : " + getDenseRatio());
	
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		sb.append("\nCompressed Size       : " + compressedSize);
		sb.append("\nCompressionRatio      : " + getRatio());
		sb.append("\nDenseCompressionRatio : " + getDenseRatio());
	
		if(colGroupCounts != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
	
		if(colGroupCounts != null) {
			sb.append("\nCompressionTypes      : " + getGroupsTypesString());
			sb.append("\nCompressionGroupSizes : " + getGroupsSizesString());
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java
		if(colGroupCounts != null) {
			sb.append("\nCompressionTypes      : " + getGroupsTypesString());
			sb.append("\nCompressionGroupSizes : " + getGroupsSizesString());
		}
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java

		for(String ctKey : colGroupCounts.keySet())
			sb.append(ctKey + ":" + colGroupCounts.get(ctKey)[0] + " ");

		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressionStatistics.java`
#### Snippet
```java

		for(String ctKey : colGroupCounts.keySet())
			sb.append(ctKey + ":" + colGroupCounts.get(ctKey)[1] + " ");

		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlockFactory.java`
#### Snippet
```java
		sb.append(cg.get(0).getNumCols());
		for(int id = 1; id < cg.size(); id++)
			sb.append(", " + cg.get(id).getNumCols());
		sb.append("]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("CompressedMatrixBlock:");
		sb.append("\nCols:" + getNumColumns() + " Rows:" + getNumRows() + " Overlapping: " + isOverlapping() + " nnz: "
			+ nonZeros);
		if(_colGroups != null)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
		if(_colGroups != null)
			for(AColGroup cg : _colGroups) {
				sb.append("\n" + cg);
			}
		else
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("rows:" + numRows);
		sb.append(" num Offsets:" + numOffs);
		if(largestOff != -1)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("rows:" + numRows);
		sb.append(" num Offsets:" + numOffs);
		if(largestOff != -1)
			sb.append(" LargestOffset:" + largestOff);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		sb.append(" num Offsets:" + numOffs);
		if(largestOff != -1)
			sb.append(" LargestOffset:" + largestOff);
		sb.append(" num Singles:" + numSingle);
		if(numRuns != numOffs)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		if(largestOff != -1)
			sb.append(" LargestOffset:" + largestOff);
		sb.append(" num Singles:" + numSingle);
		if(numRuns != numOffs)
			sb.append(" num Runs: " + numRuns);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		sb.append(" num Singles:" + numSingle);
		if(numRuns != numOffs)
			sb.append(" num Runs: " + numRuns);
		sb.append(" num Unique Vals:" + numVals);
		sb.append(" overallSparsity:" + overAllSparsity);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		if(numRuns != numOffs)
			sb.append(" num Runs: " + numRuns);
		sb.append(" num Unique Vals:" + numVals);
		sb.append(" overallSparsity:" + overAllSparsity);
		sb.append(" tupleSparsity:" + tupleSparsity);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
			sb.append(" num Runs: " + numRuns);
		sb.append(" num Unique Vals:" + numVals);
		sb.append(" overallSparsity:" + overAllSparsity);
		sb.append(" tupleSparsity:" + tupleSparsity);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/EstimationFactors.java`
#### Snippet
```java
		sb.append(" num Unique Vals:" + numVals);
		sb.append(" overallSparsity:" + overAllSparsity);
		sb.append(" tupleSparsity:" + tupleSparsity);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfo.java`
#### Snippet
```java
		sb.append(compressionInfo.get(0).getNumVals());
		for(int i = 1; i < compressionInfo.size(); i++)
			sb.append(", " + compressionInfo.get(i).getNumVals());
		sb.append("]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfo.java`
#### Snippet
```java
		// sb.append("num Rows" + numRows + " NumCols" + numCols );
		for(CompressedSizeInfoColGroup g : compressionInfo)
			sb.append("\n" + g.toString());
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfo.java`
#### Snippet
```java
		sb.append(compressionInfo.get(0).getColumns().size());
		for(int i = 1; i < compressionInfo.size(); i++)
			sb.append(", " + compressionInfo.get(i).getColumns().size());
		sb.append("]");
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/plan/NaivePlanEncode.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append(" Parallelization: " + k);
		sb.append(" Overlapping: " + overlapping);
		sb.append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/plan/NaivePlanEncode.java`
#### Snippet
```java
		sb.append(this.getClass().getSimpleName());
		sb.append(" Parallelization: " + k);
		sb.append(" Overlapping: " + overlapping);
		sb.append("\n");
		for(int i = 0; i < schemes.length; i++) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfoColGroup.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("cols: " + _cols);
		sb.append(String.format(" common: %4.3f", getMostCommonFraction()));
		sb.append(" Sizes: ");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/estim/CompressedSizeInfoColGroup.java`
#### Snippet
```java
		sb.append(" Sizes: ");
		sb.append(_sizes);
		sb.append(" facts: " + _facts);
		// sb.append("\n" + _map);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/IntArrayList.java`
#### Snippet
```java
		int i = 0;
		for(; i < _size - 1; i++)
			sb.append(_data[i] + ",");

		sb.append(_data[i] + "]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/IntArrayList.java`
#### Snippet
```java
			sb.append(_data[i] + ",");

		sb.append(_data[i] + "]");

		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName() + this.hashCode());
		for(int i = 0; i < _data.length; i++)
			if(_data[i] != null)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
		for(int i = 0; i < _data.length; i++)
			if(_data[i] != null)
				sb.append(", " + _data[i]);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/ABitmap.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append("  zeros:  " + _numZeros);
		sb.append("\nOffsets:" + Arrays.toString(_offsetsLists));
		addToString(sb);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/ABitmap.java`
#### Snippet
```java
		sb.append(this.getClass().getSimpleName());
		sb.append("  zeros:  " + _numZeros);
		sb.append("\nOffsets:" + Arrays.toString(_offsetsLists));
		addToString(sb);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/MultiColBitmap.java`
#### Snippet
```java
		sb.append("\nValues:");
		for(double[] vv : _values)
			sb.append("\n" + Arrays.toString(vv));
	}
}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/bitmap/Bitmap.java`
#### Snippet
```java
	@Override
	protected void addToString(StringBuilder sb) {
		sb.append("\nValues: " + Arrays.toString(_values));
	}
}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName() + this.hashCode());
		sb.append("   " + _size);
		for(int i = 0; i < _data.length; i++) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
		public String toString() {
			StringBuilder sb = new StringBuilder();
			sb.append("[" + key + ", ");
			sb.append(value + ", ");
			sb.append(next + "]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName() + this.hashCode());
		sb.append("   " + _size);
		for(int i = 0; i < _data.length; i++) {
			DArrayIListEntry ent = _data[i];
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
			StringBuilder sb = new StringBuilder();
			sb.append("[" + key + ", ");
			sb.append(value + ", ");
			sb.append(next + "]");
			return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DblArrayIntListHashMap.java`
#### Snippet
```java

				sb.append("\n");
				sb.append("id:" + i);
				sb.append("[");
				sb.append(ent);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
			sb.append("[" + key + ", ");
			sb.append(value + ", ");
			sb.append(next + "]");
			return sb.toString();
		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName() + this.hashCode());
		for(int i = 0; i < _data.length; i++)
			if(_data[i] != null)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleIntListHashMap.java`
#### Snippet
```java
		for(int i = 0; i < _data.length; i++)
			if(_data[i] != null)
				sb.append(", " + _data[i]);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		sb.append(super.toString());
		sb.append("\n");
		sb.append(" numCols : " + _data.getNumColumns());
		sb.append(" numRows : " + _data.getNumRows());
		sb.append(" nonZeros: " + _data.getNonZeros());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		sb.append("\n");
		sb.append(" numCols : " + _data.getNumColumns());
		sb.append(" numRows : " + _data.getNumRows());
		sb.append(" nonZeros: " + _data.getNonZeros());
		sb.append(" Sparse : " + _data.isInSparseFormat());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		sb.append(" numCols : " + _data.getNumColumns());
		sb.append(" numRows : " + _data.getNumRows());
		sb.append(" nonZeros: " + _data.getNonZeros());
		sb.append(" Sparse : " + _data.isInSparseFormat());

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		sb.append(" numRows : " + _data.getNumRows());
		sb.append(" nonZeros: " + _data.getNonZeros());
		sb.append(" Sparse : " + _data.isInSparseFormat());

		sb.append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AIterator.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName());
		sb.append(" v:" + value() + " d:" + getDataIndex() + " o:" + getOffsetsIndex());
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			if(_ptr[p] == i) {
				if(_ptr[p] + 2 == _ptr[p + 1])
					sb.append("}, {" + pair(_data, i));
				else

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
				else

					sb.append("},\n {" + pair(_data, i));
				p++;
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			}
			else
				sb.append(", " + pair(_data, i));

		}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			if(_ptr[p] == i) {
				sum = 0;
				sb.append("},\n {" + pair(_data, i, sum));
				sum += _data[i] + _data[i + 1];
				p++;
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
			}
			else {
				sb.append(", " + pair(_data, i, sum));
				sum += _data[i] + _data[i + 1];
			}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
		sb.append("\nActual: ");
		for(char c : _data) {
			sb.append((int) c + ", ");
		}

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/workload/WTreeRoot.java`
#### Snippet
```java
	
		// append node summary
		sb.append("ROOT : " + _root.toString());
		sb.append("\n");

```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/mapping/AMapToData.java`
#### Snippet
```java
		sb.append("[");
		for(int i = 0; i < sz - 1; i++)
			sb.append(getIndex(i) + ", ");
		sb.append(getIndex(sz - 1));
		sb.append("]");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderBin.java`
#### Snippet
```java
		sb.append(": ");
		sb.append(_colID);
		sb.append(" --- Method: " + _binMethod + " num Bin: " + _numBin);
		sb.append("\n---- BinMin: " + Arrays.toString(_binMins));
		sb.append("\n---- BinMax: " + Arrays.toString(_binMaxs));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderBin.java`
#### Snippet
```java
		sb.append(_colID);
		sb.append(" --- Method: " + _binMethod + " num Bin: " + _numBin);
		sb.append("\n---- BinMin: " + Arrays.toString(_binMins));
		sb.append("\n---- BinMax: " + Arrays.toString(_binMaxs));
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/transform/encode/ColumnEncoderBin.java`
#### Snippet
```java
		sb.append(" --- Method: " + _binMethod + " num Bin: " + _numBin);
		sb.append("\n---- BinMin: " + Arrays.toString(_binMins));
		sb.append("\n---- BinMax: " + Arrays.toString(_binMaxs));
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		}
		for(Entry<String, Integer> kv : frequency.entrySet()) {
			sb.append(">>" + kv.getKey() + " => " + kv.getValue() + "\n");
		}
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
				if( _props.hasHeader() ) {
					for(int j = 1; j <= blk.getNumColumns(); j++) {
						sb.append(blk.getColumnNames()[j] 
							+ ((j<blk.getNumColumns()-1)?_props.getDelim():""));
					}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
				}
				if( !blk.isColumnMetadataDefault() ) {
					sb.append(TfUtils.TXMTD_MVPREFIX + _props.getDelim());
					for( int j=0; j<blk.getNumColumns(); j++ )
						sb.append(blk.getColumnMetadata(j).getMvValue() + ((j<blk.getNumColumns()-1)?_props.getDelim():""));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
					sb.append(TfUtils.TXMTD_MVPREFIX + _props.getDelim());
					for( int j=0; j<blk.getNumColumns(); j++ )
						sb.append(blk.getColumnMetadata(j).getMvValue() + ((j<blk.getNumColumns()-1)?_props.getDelim():""));
					ret.add(sb.toString());
					sb.setLength(0); //reset
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
					ret.add(sb.toString());
					sb.setLength(0); //reset
					sb.append(TfUtils.TXMTD_NDPREFIX + _props.getDelim());
					for( int j=0; j<blk.getNumColumns(); j++ )
						sb.append(blk.getColumnMetadata(j).getNumDistinct() + ((j<blk.getNumColumns()-1)?_props.getDelim():""));
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
					sb.append(TfUtils.TXMTD_NDPREFIX + _props.getDelim());
					for( int j=0; j<blk.getNumColumns(); j++ )
						sb.append(blk.getColumnMetadata(j).getNumDistinct() + ((j<blk.getNumColumns()-1)?_props.getDelim():""));
					ret.add(sb.toString());
					sb.setLength(0); //reset		
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
					if(j != 1)
						sb.append(_props.getDelim());
					sb.append("C" + j);
				}
				ret.add(sb.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
			sb.append("\n");
			sb.append("##############################################\n");
			sb.append("## ParFOR (ID="+ pfid +") Execution Statistics:\n");
			HashMap<Stat,LinkedList<Double>> stats = _pfstats.get(pfid); 
			
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				//sb.append(" Optimization = "+stats.get(Stat.STAT_OPT_T).get(i)+"ms\n");
				
				sb.append(" Run #"+i+"\n");
				sb.append("  Num Threads      = "+(int)(double)stats.get(Stat.PARFOR_NUMTHREADS).get(i)+"\n");
				sb.append("  TaskSize         = "+(int)(double)stats.get(Stat.PARFOR_TASKSIZE).get(i)+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				
				sb.append(" Run #"+i+"\n");
				sb.append("  Num Threads      = "+(int)(double)stats.get(Stat.PARFOR_NUMTHREADS).get(i)+"\n");
				sb.append("  TaskSize         = "+(int)(double)stats.get(Stat.PARFOR_TASKSIZE).get(i)+"\n");
				sb.append("  Task Partitioner = "+PTaskPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_TASKPARTITIONER).get(i)]+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append(" Run #"+i+"\n");
				sb.append("  Num Threads      = "+(int)(double)stats.get(Stat.PARFOR_NUMTHREADS).get(i)+"\n");
				sb.append("  TaskSize         = "+(int)(double)stats.get(Stat.PARFOR_TASKSIZE).get(i)+"\n");
				sb.append("  Task Partitioner = "+PTaskPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_TASKPARTITIONER).get(i)]+"\n");
				sb.append("  Data Partitioner = "+PDataPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_DATAPARTITIONER).get(i)]+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Num Threads      = "+(int)(double)stats.get(Stat.PARFOR_NUMTHREADS).get(i)+"\n");
				sb.append("  TaskSize         = "+(int)(double)stats.get(Stat.PARFOR_TASKSIZE).get(i)+"\n");
				sb.append("  Task Partitioner = "+PTaskPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_TASKPARTITIONER).get(i)]+"\n");
				sb.append("  Data Partitioner = "+PDataPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_DATAPARTITIONER).get(i)]+"\n");
				sb.append("  Exec Mode        = "+PExecMode.values()[(int)(double)stats.get(Stat.PARFOR_EXECMODE).get(i)]+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  TaskSize         = "+(int)(double)stats.get(Stat.PARFOR_TASKSIZE).get(i)+"\n");
				sb.append("  Task Partitioner = "+PTaskPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_TASKPARTITIONER).get(i)]+"\n");
				sb.append("  Data Partitioner = "+PDataPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_DATAPARTITIONER).get(i)]+"\n");
				sb.append("  Exec Mode        = "+PExecMode.values()[(int)(double)stats.get(Stat.PARFOR_EXECMODE).get(i)]+"\n");
				sb.append("  Num Tasks        = "+(int)(double)stats.get(Stat.PARFOR_NUMTASKS).get(i)+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Task Partitioner = "+PTaskPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_TASKPARTITIONER).get(i)]+"\n");
				sb.append("  Data Partitioner = "+PDataPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_DATAPARTITIONER).get(i)]+"\n");
				sb.append("  Exec Mode        = "+PExecMode.values()[(int)(double)stats.get(Stat.PARFOR_EXECMODE).get(i)]+"\n");
				sb.append("  Num Tasks        = "+(int)(double)stats.get(Stat.PARFOR_NUMTASKS).get(i)+"\n");
				sb.append("  Num Iterations   = "+(int)(double)stats.get(Stat.PARFOR_NUMITERS).get(i)+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Data Partitioner = "+PDataPartitioner.values()[(int)(double)stats.get(Stat.PARFOR_DATAPARTITIONER).get(i)]+"\n");
				sb.append("  Exec Mode        = "+PExecMode.values()[(int)(double)stats.get(Stat.PARFOR_EXECMODE).get(i)]+"\n");
				sb.append("  Num Tasks        = "+(int)(double)stats.get(Stat.PARFOR_NUMTASKS).get(i)+"\n");
				sb.append("  Num Iterations   = "+(int)(double)stats.get(Stat.PARFOR_NUMITERS).get(i)+"\n");
				
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Exec Mode        = "+PExecMode.values()[(int)(double)stats.get(Stat.PARFOR_EXECMODE).get(i)]+"\n");
				sb.append("  Num Tasks        = "+(int)(double)stats.get(Stat.PARFOR_NUMTASKS).get(i)+"\n");
				sb.append("  Num Iterations   = "+(int)(double)stats.get(Stat.PARFOR_NUMITERS).get(i)+"\n");
				
				if( stats.containsKey(Stat.OPT_OPTIMIZER) )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				if( stats.containsKey(Stat.OPT_OPTIMIZER) )
				{
					sb.append("  Optimizer               = "+POptMode.values()[(int)(double)stats.get(Stat.OPT_OPTIMIZER).get(i)]+"\n");
					sb.append("  Opt Num Total Plans     = "+(int)(double)stats.get(Stat.OPT_NUMTPLANS).get(i)+"\n");
					sb.append("  Opt Num Evaluated Plans = "+(int)(double)stats.get(Stat.OPT_NUMEPLANS).get(i)+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				{
					sb.append("  Optimizer               = "+POptMode.values()[(int)(double)stats.get(Stat.OPT_OPTIMIZER).get(i)]+"\n");
					sb.append("  Opt Num Total Plans     = "+(int)(double)stats.get(Stat.OPT_NUMTPLANS).get(i)+"\n");
					sb.append("  Opt Num Evaluated Plans = "+(int)(double)stats.get(Stat.OPT_NUMEPLANS).get(i)+"\n");
					sb.append("  Time INIT OPTIM   = "+stats.get(Stat.OPT_T).get(i)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
					sb.append("  Optimizer               = "+POptMode.values()[(int)(double)stats.get(Stat.OPT_OPTIMIZER).get(i)]+"\n");
					sb.append("  Opt Num Total Plans     = "+(int)(double)stats.get(Stat.OPT_NUMTPLANS).get(i)+"\n");
					sb.append("  Opt Num Evaluated Plans = "+(int)(double)stats.get(Stat.OPT_NUMEPLANS).get(i)+"\n");
					sb.append("  Time INIT OPTIM   = "+stats.get(Stat.OPT_T).get(i)+"ms\n");
				}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
					sb.append("  Opt Num Total Plans     = "+(int)(double)stats.get(Stat.OPT_NUMTPLANS).get(i)+"\n");
					sb.append("  Opt Num Evaluated Plans = "+(int)(double)stats.get(Stat.OPT_NUMEPLANS).get(i)+"\n");
					sb.append("  Time INIT OPTIM   = "+stats.get(Stat.OPT_T).get(i)+"ms\n");
				}
				
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				}
				
				sb.append("  Time INIT DATA    = "+stats.get(Stat.PARFOR_INIT_DATA_T).get(i)+"ms\n");
				sb.append("  Time INIT PARWRK  = "+stats.get(Stat.PARFOR_INIT_PARWRK_T).get(i)+"ms\n");
				sb.append("  Time INIT TASKS   = "+stats.get(Stat.PARFOR_INIT_TASKS_T).get(i)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				
				sb.append("  Time INIT DATA    = "+stats.get(Stat.PARFOR_INIT_DATA_T).get(i)+"ms\n");
				sb.append("  Time INIT PARWRK  = "+stats.get(Stat.PARFOR_INIT_PARWRK_T).get(i)+"ms\n");
				sb.append("  Time INIT TASKS   = "+stats.get(Stat.PARFOR_INIT_TASKS_T).get(i)+"ms\n");
				sb.append("  Time WAIT EXEC    = "+stats.get(Stat.PARFOR_WAIT_EXEC_T).get(i)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Time INIT DATA    = "+stats.get(Stat.PARFOR_INIT_DATA_T).get(i)+"ms\n");
				sb.append("  Time INIT PARWRK  = "+stats.get(Stat.PARFOR_INIT_PARWRK_T).get(i)+"ms\n");
				sb.append("  Time INIT TASKS   = "+stats.get(Stat.PARFOR_INIT_TASKS_T).get(i)+"ms\n");
				sb.append("  Time WAIT EXEC    = "+stats.get(Stat.PARFOR_WAIT_EXEC_T).get(i)+"ms\n");
				sb.append("  Time WAIT RESULT  = "+stats.get(Stat.PARFOR_WAIT_RESULTS_T).get(i)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Time INIT PARWRK  = "+stats.get(Stat.PARFOR_INIT_PARWRK_T).get(i)+"ms\n");
				sb.append("  Time INIT TASKS   = "+stats.get(Stat.PARFOR_INIT_TASKS_T).get(i)+"ms\n");
				sb.append("  Time WAIT EXEC    = "+stats.get(Stat.PARFOR_WAIT_EXEC_T).get(i)+"ms\n");
				sb.append("  Time WAIT RESULT  = "+stats.get(Stat.PARFOR_WAIT_RESULTS_T).get(i)+"ms\n");
				
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
				sb.append("  Time INIT TASKS   = "+stats.get(Stat.PARFOR_INIT_TASKS_T).get(i)+"ms\n");
				sb.append("  Time WAIT EXEC    = "+stats.get(Stat.PARFOR_WAIT_EXEC_T).get(i)+"ms\n");
				sb.append("  Time WAIT RESULT  = "+stats.get(Stat.PARFOR_WAIT_RESULTS_T).get(i)+"ms\n");
				
				//foreach parworker of this parfor
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
						
						sb.append("   ------------------------\n");
						sb.append("   --- ParWorker #"+count2+" (ID="+ pid +") Execution Statistics:\n");						
						sb.append("       Num Tasks = "+ntasks+"\n");
						sb.append("       Num Iters = "+niters+"\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
						sb.append("   ------------------------\n");
						sb.append("   --- ParWorker #"+count2+" (ID="+ pid +") Execution Statistics:\n");						
						sb.append("       Num Tasks = "+ntasks+"\n");
						sb.append("       Num Iters = "+niters+"\n");
						sb.append("       Time EXEC = "+stats2.get(Stat.PARWRK_EXEC_T).get(0)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
						sb.append("   --- ParWorker #"+count2+" (ID="+ pid +") Execution Statistics:\n");						
						sb.append("       Num Tasks = "+ntasks+"\n");
						sb.append("       Num Iters = "+niters+"\n");
						sb.append("       Time EXEC = "+stats2.get(Stat.PARWRK_EXEC_T).get(0)+"ms\n");
						
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
						sb.append("       Num Tasks = "+ntasks+"\n");
						sb.append("       Num Iters = "+niters+"\n");
						sb.append("       Time EXEC = "+stats2.get(Stat.PARWRK_EXEC_T).get(0)+"ms\n");
						
						LinkedList<Double> taskexec = stats2.get(Stat.PARWRK_TASK_T);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
							int ltasksize=(int)(double)tasksize.get(k1);
							
							sb.append("        Task #"+(k1+1)+": \n");
							sb.append("         Task Size = "+ltasksize+"\n");
							sb.append("         Time EXEC = "+taskexec.get(k1)+"ms\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
							
							sb.append("        Task #"+(k1+1)+": \n");
							sb.append("         Task Size = "+ltasksize+"\n");
							sb.append("         Time EXEC = "+taskexec.get(k1)+"ms\n");
							
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
							sb.append("        Task #"+(k1+1)+": \n");
							sb.append("         Task Size = "+ltasksize+"\n");
							sb.append("         Time EXEC = "+taskexec.get(k1)+"ms\n");
							
							for( int k2=0; k2<ltasksize; k2++ )
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/stat/StatisticMonitor.java`
#### Snippet
```java
							for( int k2=0; k2<ltasksize; k2++ )
							{
								sb.append("          Iteration #"+(k2+1)+": Time EXEC = "+iterexec.get(count3)+"ms\n");
								count3++;
							}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
		if(_partitioned){
			sb.append("\n");
			sb.append("partitioned:" + _partitionFormat );
			sb.append(", partitionSize: " + _partitionSize);
			sb.append(", partitionCacheName:" + _partitionCacheName);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
			sb.append("\n");
			sb.append("partitioned:" + _partitionFormat );
			sb.append(", partitionSize: " + _partitionSize);
			sb.append(", partitionCacheName:" + _partitionCacheName);
			sb.append(_partitionInMemory);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
			sb.append("partitioned:" + _partitionFormat );
			sb.append(", partitionSize: " + _partitionSize);
			sb.append(", partitionCacheName:" + _partitionCacheName);
			sb.append(_partitionInMemory);
			sb.append("\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
		str.append(getClass().getSimpleName());
		str.append(": file:");
		str.append(_hdfsFileName + ", ");

		if (_metaData != null) {
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java

		if(isCompressed())
			str.append( ", Compressed " + _compressedSize );

		if(_data != null)
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java

		if(_data != null)
			str.append(";\nData:" + _data);
		return str.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
		sb.append(this.getClass().getSimpleName().toString());
		if(_prog != null)
			sb.append("\nProgram: " + _prog.toString());
		if(_variables != null)
			sb.append("\nLocalVariableMap: " + _variables.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
			sb.append("\nProgram: " + _prog.toString());
		if(_variables != null)
			sb.append("\nLocalVariableMap: " + _variables.toString());
		if(_lineage != null)
			sb.append("\nLineage: " + _lineage.toString());
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
			sb.append("\nLocalVariableMap: " + _variables.toString());
		if(_lineage != null)
			sb.append("\nLineage: " + _lineage.toString());
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("Fed Map: " + _type);
		sb.append("\t ID:" + _ID);
		sb.append("\n" + _fedMap);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append("Fed Map: " + _type);
		sb.append("\t ID:" + _ID);
		sb.append("\n" + _fedMap);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java
		sb.append("Fed Map: " + _type);
		sb.append("\t ID:" + _ID);
		sb.append("\n" + _fedMap);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		sb.append(" " + _dataType);
		sb.append(" " + _address.toString());
		sb.append(":" + _filepath);
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
		sb.append(this.getClass().getSimpleName().toString());
		sb.append(" " + _dataType);
		sb.append(" " + _address.toString());
		sb.append(":" + _filepath);
		return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
		sb.append(" " + _dataType);
		sb.append(" " + _address.toString());
		sb.append(":" + _filepath);
		return sb.toString();
	}
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
		if( readCount.longValue() > 0){ // only if there happened something on the federated worker
			StringBuilder sb = new StringBuilder();
			sb.append("Federated I/O (Read, Put, Get):\t" +
				readCount.longValue() + "/" +
				putCount.longValue() + "/" +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
				putCount.longValue() + "/" +
				getCount.longValue() + ".\n");
			sb.append("Federated Execute (Inst, UDF):\t" +
				executeInstructionCount.longValue() + "/" +
				executeUDFCount.longValue() + ".\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
				executeUDFCount.longValue() + ".\n");
			if(getTotalFedTransferCount() > 0)
				sb.append("Fed Put Count (Sc/Li/Ma/Fr/MC):\t" +
					transferredScalarCount.longValue() + "/" +
					transferredListCount.longValue() + "/" +
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
					transferredMatCharCount.longValue() + ".\n");
			if(transferredMatrixBytes.longValue() > 0 || transferredFrameBytes.longValue() > 0)
				sb.append("Fed Put Bytes (Mat/Frame):\t" +
					transferredMatrixBytes.longValue() + "/" +
					transferredFrameBytes.longValue() + " Bytes.\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedStatistics.java`
#### Snippet
```java
					transferredMatrixBytes.longValue() + "/" +
					transferredFrameBytes.longValue() + " Bytes.\n");
			sb.append("Federated prefetch count:\t" +
				asyncPrefetchCount.longValue() + ".\n");
			return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
		public String toString() {
			StringBuilder sb = new StringBuilder("SparkClusterConfig: \n");
			sb.append("-- legacyVersion    = " + _legacyVersion + " ("+getSparkVersionString()+")\n" );
			sb.append("-- confOnly         = " + _confOnly + "\n");
			sb.append("-- numExecutors     = " + _numExecutors + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			StringBuilder sb = new StringBuilder("SparkClusterConfig: \n");
			sb.append("-- legacyVersion    = " + _legacyVersion + " ("+getSparkVersionString()+")\n" );
			sb.append("-- confOnly         = " + _confOnly + "\n");
			sb.append("-- numExecutors     = " + _numExecutors + "\n");
			sb.append("-- defaultPar       = " + _defaultPar + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- legacyVersion    = " + _legacyVersion + " ("+getSparkVersionString()+")\n" );
			sb.append("-- confOnly         = " + _confOnly + "\n");
			sb.append("-- numExecutors     = " + _numExecutors + "\n");
			sb.append("-- defaultPar       = " + _defaultPar + "\n");
			sb.append("-- memExecutor      = " + _memExecutor + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- confOnly         = " + _confOnly + "\n");
			sb.append("-- numExecutors     = " + _numExecutors + "\n");
			sb.append("-- defaultPar       = " + _defaultPar + "\n");
			sb.append("-- memExecutor      = " + _memExecutor + "\n");
			sb.append("-- memDataMinFrac   = " + _memDataMinFrac + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- numExecutors     = " + _numExecutors + "\n");
			sb.append("-- defaultPar       = " + _defaultPar + "\n");
			sb.append("-- memExecutor      = " + _memExecutor + "\n");
			sb.append("-- memDataMinFrac   = " + _memDataMinFrac + "\n");
			sb.append("-- memDataMaxFrac   = " + _memDataMaxFrac + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- defaultPar       = " + _defaultPar + "\n");
			sb.append("-- memExecutor      = " + _memExecutor + "\n");
			sb.append("-- memDataMinFrac   = " + _memDataMinFrac + "\n");
			sb.append("-- memDataMaxFrac   = " + _memDataMaxFrac + "\n");
			sb.append("-- memBroadcastFrac = " + _memBroadcastFrac + "\n");
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- memExecutor      = " + _memExecutor + "\n");
			sb.append("-- memDataMinFrac   = " + _memDataMinFrac + "\n");
			sb.append("-- memDataMaxFrac   = " + _memDataMaxFrac + "\n");
			sb.append("-- memBroadcastFrac = " + _memBroadcastFrac + "\n");
			return sb.toString();
```

### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
			sb.append("-- memDataMinFrac   = " + _memDataMinFrac + "\n");
			sb.append("-- memDataMaxFrac   = " + _memDataMaxFrac + "\n");
			sb.append("-- memBroadcastFrac = " + _memBroadcastFrac + "\n");
			return sb.toString();
		}
```

## RuleId[id=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Collection `new ArrayList`() is always empty
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		//check for existing sparse block: return empty list
		if( sparseBlock==null )
			return new ArrayList<IJV>().iterator();
		
		//get iterator over sparse block
```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked assignment: 'java.util.PriorityQueue' to 'java.util.PriorityQueue\>'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
		return op1.getRight() == op2.getRight() ? 0 : op1.getRight() < op2.getRight() ? 1 : -1;  
	};
	protected static PriorityQueue<MutableTriple<String, Long, Double>> computeSavingInst = new PriorityQueue<>(savedOPComparator);
	
	static {
```

### UNCHECKED_WARNING
Unchecked call to 'PriorityQueue(Comparator)' as a member of raw type 'java.util.PriorityQueue'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimator.java`
#### Snippet
```java
		return op1.getRight() == op2.getRight() ? 0 : op1.getRight() < op2.getRight() ? 1 : -1;  
	};
	protected static PriorityQueue<MutableTriple<String, Long, Double>> computeSavingInst = new PriorityQueue<>(savedOPComparator);
	
	static {
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.HashSet' to 'java.util.Set'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		// Then clean up remaining allocated GPU pointers 
		synchronized(allPointers) {
			Set<Pointer> remainingPtr = new HashSet<>(allPointers.keySet());
			for(Pointer toFree : remainingPtr) {
				guardedCudaFree(toFree); // cleans up allocatedGPUPointers and rmvarGPUPointers as well
```

### UNCHECKED_WARNING
Unchecked call to 'HashSet(Collection)' as a member of raw type 'java.util.HashSet'
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		// Then clean up remaining allocated GPU pointers 
		synchronized(allPointers) {
			Set<Pointer> remainingPtr = new HashSet<>(allPointers.keySet());
			for(Pointer toFree : remainingPtr) {
				guardedCudaFree(toFree); // cleans up allocatedGPUPointers and rmvarGPUPointers as well
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.ArrayList' to 'java.util.List\>'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java

	public FederationMap transpose() {
		List<Pair<FederatedRange, FederatedData>> tmp = new ArrayList<>(_fedMap);
		_fedMap.clear();
		for(Pair<FederatedRange, FederatedData> e : tmp) {
```

### UNCHECKED_WARNING
Unchecked call to 'ArrayList(Collection)' as a member of raw type 'java.util.ArrayList'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationMap.java`
#### Snippet
```java

	public FederationMap transpose() {
		List<Pair<FederatedRange, FederatedData>> tmp = new ArrayList<>(_fedMap);
		_fedMap.clear();
		for(Pair<FederatedRange, FederatedData> e : tmp) {
```

## RuleId[id=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `src/main/java/org/apache/sysds/api/DMLOptions.java`
#### Snippet
```java
			dmlOptions.federatedCompilation = true;
			String[] fedCompSpecs = line.getOptionValues("federatedCompilation");
			if ( fedCompSpecs != null && fedCompSpecs.length > 0 ){
				for ( String spec : fedCompSpecs ){
					String[] specPair = spec.split("=");
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Method invocation `getGPUContexts` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/api/ScriptExecutorUtils.java`
#### Snippet
```java
			throw e;
		} finally { // ensure cleanup/shutdown
			if (DMLScript.USE_ACCELERATOR && !ec.getGPUContexts().isEmpty()) {
				// -----------------------------------------------------------------
				// The below code pulls the output variables on the GPU to the host. This is required especially when:
```

### DataFlowIssue
Casting `metadata` to `MatrixMetadata` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
		} else if (value instanceof double[][]) {
			double[][] doubleMatrix = (double[][]) value;
			return MLContextConversionUtil.doubleMatrixToMatrixObject(name, doubleMatrix, (MatrixMetadata) metadata);
		} else if (value instanceof URL) {
			URL url = (URL) value;
```

### DataFlowIssue
Method invocation `append` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
						prevCellRow = i;
					} else if (i == prevCellRow) {
						sb.append(",");
						sb.append(v);
					}
```

### DataFlowIssue
Method invocation `getFrameSchema` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		FrameObject frameObject = new FrameObject(OptimizerUtils.getUniqueTempFileName(),
				new MetaDataFormat(mc, FileFormat.BINARY),
				frameMetadata.getFrameSchema().getSchema().toArray(new ValueType[0]));
		JavaPairRDD<Long, FrameBlock> rdd;
		
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
	 */
	public static FrameObject javaRDDStringCSVToFrameObject(JavaRDD<String> javaRDD) {
		return javaRDDStringCSVToFrameObject(javaRDD, null);
	}

```

### DataFlowIssue
Method invocation `getFrameSchema` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
		FrameObject frameObject = new FrameObject(OptimizerUtils.getUniqueTempFileName(),
				new MetaDataFormat(mc, FileFormat.BINARY),
				frameMetadata.getFrameSchema().getSchema().toArray(new ValueType[0]));
		JavaPairRDD<Long, FrameBlock> rdd;
		try {
```

### DataFlowIssue
Method invocation `getFrameSchema` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
			MetaDataFormat mtd = new MetaDataFormat(mc, FileFormat.BINARY);
			FrameObject frameObject = new FrameObject(OptimizerUtils.getUniqueTempFileName(), mtd,
				frameMetadata.getFrameSchema().getSchema().toArray(new ValueType[0]));
			frameObject.acquireModify(frameBlock);
			frameObject.release();
```

### DataFlowIssue
Argument `grp_agg` might be null
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			}
			
			setLineNumbers(grp_agg);
			setLops(grp_agg);
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.error("Failed to compute bounds information.", ex);
			ret = Double.MAX_VALUE;
		}
		return ret;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
						case MIN:   ret = Math.min(lret, rret); break;
						case MAX:   ret = Math.max(lret, rret); break;
						default:    ret = Long.MAX_VALUE;
					}
				}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
				&& 2 * _outputMemEstimate < OptimizerUtils.getLocalMemBudget()
				|| _etypeForced == ExecType.CP )
				et = ExecType.CP;
			else 
				et = ExecType.SPARK;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.error("Failed to compute size information.", ex);
			ret = -1;
		}
		return ret;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.error("Failed to compute size information.", ex);
			ret = -1;
		}

```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/Hop.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.error("Failed to compute bounds information.", ex);
			ret = Double.MAX_VALUE;
		}
		return ret;
```

### DataFlowIssue
Argument `wtype` might be null
in `src/main/java/org/apache/sysds/hops/QuaternaryOp.java`
#### Snippet
```java
						constructCPLopsWeightedDivMM(wtype);
					else if( et == ExecType.SPARK )
						constructSparkLopsWeightedDivMM(wtype);
					else
						throw new HopsException("Unsupported quaternaryop-wdivmm exec type: "+et);
```

### DataFlowIssue
Casting `o` to `FunctionCallSizeInfo` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/hops/ipa/FunctionCallSizeInfo.java`
#### Snippet
```java
		if( o instanceof FunctionCallSizeInfo )
			return false;
		FunctionCallSizeInfo that = (FunctionCallSizeInfo)o;
		return _fgraph == that._fgraph
			&& _fcand.equals(that._fcand)
```

### DataFlowIssue
Dereference of `that` will produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/ipa/FunctionCallSizeInfo.java`
#### Snippet
```java
			return false;
		FunctionCallSizeInfo that = (FunctionCallSizeInfo)o;
		return _fgraph == that._fgraph
			&& _fcand.equals(that._fcand)
			&& _fcandUnary.equals(that._fcandUnary)
```

### DataFlowIssue
Method invocation `getOutputParams` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassReplaceEvalFunctionCalls.java`
#### Snippet
```java
					continue;
				}
				if( fstmt.getOutputParams().size() != 1 || !fstmt.getOutputParams().get(0).getDataType().isMatrix() ) {
					LOG.warn("IPA: eval("+fnamespace+"::"+fname+") "
						+ "applicable for replacement, but function output is not a matrix.");
```

### DataFlowIssue
Method invocation `rewriteProgram` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassRewriteFederatedPlan.java`
#### Snippet
```java

		// run planner rewrite with forced federated exec types
		planner.getPlanner().rewriteProgram(prog, fgraph, fcallSizes);
	}
}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CEIL:
				case FLOOR:
				case SIGN:    costs = 1; break;
				case SPROP:
				case SQRT:    costs = 2; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CAST_AS_INT:
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case CUMMIN:
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case GREATEREQUAL:
				case CBIND:
				case RBIND:   costs = 1; break;
				case INTDIV:  costs = 6; break;
				case MODULUS: costs = 8; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
						HopRewriteUtils.getIntValueSafe((LiteralOp)currentHop.getInput().get(1)) : 2);
					switch( type ) {
						case 0: costs = 1; break; //count
						case 1: costs = 8; break; //mean
						case 2: costs = 16; break; //cm2
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
		}
		else if( currentHop instanceof ParameterizedBuiltinOp) {
			costs = 1;
		}
		else if( currentHop instanceof IndexingOp) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
		}
		else if( currentHop instanceof IndexingOp) {
			costs = 1;
		}
		else if( currentHop instanceof ReorgOp) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
		}
		else if( currentHop instanceof ReorgOp) {
			costs = 1;
		}
		else if( currentHop instanceof DnnOp) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/ComputeCost.java`
#### Snippet
```java
				case SUM_SQ: costs = 5; break;
				case MIN:
				case MAX:    costs = 1; break;
				default:
					LOG.warn("Cost model not "
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/ipa/InterProceduralAnalysis.java`
#### Snippet
```java
			for (StatementBlock sbi : istmt.getElseBody())
				propagateStatisticsAcrossBlock(sbi, callVarsElse, fcallSizes, fnStack, replaceScalars);
			callVars = Recompiler.reconcileUpdatedCallVarsIf(oldCallVars, callVars, callVarsElse, isb);
			//remove updated constant scalars
			Recompiler.removeUpdatedScalars(callVars, sb);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
					case CAST_AS_INT: ret = UtilFunctions.toLong(lval); break;
					case CAST_AS_DOUBLE: ret = lval; break;
					default: ret = Double.MAX_VALUE;
				}
			}
```

### DataFlowIssue
Casting `p` to `DataOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				||(p instanceof AggBinaryOp && allowsToFilterEmptyBlockOutputs(p) )
				||(HopRewriteUtils.isReorg(p, ReOrgOp.RESHAPE, ReOrgOp.TRANS) && allowsToFilterEmptyBlockOutputs(p) )
				||(HopRewriteUtils.isData(p, OpOpData.PERSISTENTWRITE) && ((DataOp)p).getFileFormat()==FileFormat.TEXT))
				&& !(p instanceof FunctionOp || (p instanceof DataOp && ((DataOp)p).getFileFormat()!=FileFormat.TEXT) ); //no function call or transient write
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case MODULUS: ret = Modulus.getFnObject().execute(lret, rret); break;
				case INTDIV:  ret = IntegerDivide.getFnObject().execute(lret, rret); break; 
				default: ret = Double.MAX_VALUE;
			}
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				//	ret = 1-Math.abs(sp1-sp2); break;
				default:
					ret = 1.0;
			}
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
					break;
				case DIV:
					ret = 1.0; // worst case estimate
					break;
				case LESS:
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case EQUAL:
				case NOTEQUAL:
					ret = 1.0; // purely data-dependent operations, and hence worse-case estimate
					break;
				//MIN, MAX, AND, OR, LOG, POW
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				//MIN, MAX, AND, OR, LOG, POW
				default:
					ret = 1.0;
			}
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
					case CAST_AS_INT: ret = UtilFunctions.toLong(lval); break;
					case CAST_AS_DOUBLE: ret = lval; break;
					default: ret = Double.MAX_VALUE;
				}
			}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
		String sparkenv = System.getenv().get("SPARK_ENV_LOADED");
		if( sparkenv != null && sparkenv.equals("1") )
			ret = ExecMode.HYBRID;
		
		return ret;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				case MODULUS: ret = Modulus.getFnObject().execute(lret, rret); break;
				case INTDIV:  ret = IntegerDivide.getFnObject().execute(lret, rret); break; 
				default: ret = Double.MAX_VALUE;
			}
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
						double xcm = 1;
						switch( Integer.parseInt(args[0]) ) {
							case 0: xcm=1; break; //count
							case 1: xcm=8; break; //mean
							case 2: xcm=16; break; //cm2
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/cost/CostEstimatorStaticRuntime.java`
#### Snippet
```java
						switch( Integer.parseInt(args[0]) ) {
							case 0: xga=4; break; //sum, see uk+
							case 1: xga=1; break; //count, see cm
							case 2: xga=8; break; //mean
							case 3: xga=16; break; //cm2
```

### DataFlowIssue
Casting `cnode` to `CNodeData` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeTpl.java`
#### Snippet
```java
			if( cnode instanceof CNodeData && ((CNodeData)cnode).isLiteral() )
				continue;
			newNames.put(((CNodeData)cnode).getHopID(), cnode.getDataType().isScalar() ?
				"scalars["+ mPos++ +"]" : "b["+ sPos++ +"]");
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/SpoofCompiler.java`
#### Snippet
```java

				//generate final hop dag
				ret = constructModifiedHopDag(roots, cplans, clas);
				
				//run common subexpression elimination and other rewrites
```

### DataFlowIssue
Method invocation `replace` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeMultiAgg.java`
#### Snippet
```java
			String varName = (out instanceof CNodeData && ((CNodeData)out).getHopID()==
				((CNodeData)_inputs.get(0)).getHopID()) ? "a" : out.getVarname(); 
			tmpOut = tmpOut.replace("%IN%", varName);
			tmpOut = tmpOut.replace("%IX%", String.valueOf(i));
			sb.append(tmpOut);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CEIL:
				case FLOOR:
				case SIGN:    costs = 1; break; 
				case SPROP:
				case SQRT:    costs = 2; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CAST_AS_INT:
				case CAST_AS_MATRIX:
				case CAST_AS_SCALAR: costs = 1; break;
				case SIN:     costs = 18; break;
				case COS:     costs = 22; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case CUMMIN:
				case CUMMAX:
				case CUMPROD: costs = 1; break;
				case CUMSUMPROD: costs = 2; break;
				default:
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				case GREATEREQUAL:
				case CBIND:
				case RBIND:   costs = 1; break;
				case INTDIV:  costs = 6; break;
				case MODULUS: costs = 8; break;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
						HopRewriteUtils.getIntValueSafe((LiteralOp)current.getInput().get(1)) : 2);
					switch( type ) {
						case 0: costs = 1; break; //count
						case 1: costs = 8; break; //mean
						case 2: costs = 16; break; //cm2
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		}
		else if( current instanceof ParameterizedBuiltinOp ) {
			costs = 1;
		}
		else if( current instanceof IndexingOp ) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		}
		else if( current instanceof IndexingOp ) {
			costs = 1;
		}
		else if( current instanceof ReorgOp ) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		}
		else if( current instanceof ReorgOp ) {
			costs = 1;
		}
		else if( current instanceof AggBinaryOp ) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
			case SUM_SQ: costs = 5; break;
			case MIN:
			case MAX:    costs = 1; break;
			default:
				LOG.warn("Cost model not "
```

### DataFlowIssue
Dereference of `costVect` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
		//add compute costs of current operator to costs vector 
		if( partition.contains(current.getHopID()) )
			costVect.computeCosts += computeCosts.get(current.getHopID());
		
		//process children recursively
```

### DataFlowIssue
Method invocation `addInputSize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBased.java`
#### Snippet
```java
				costs += rGetPlanCosts(memo, c, visited, partition, M, plan, computeCosts, costVect, best.type);
			else if( best!=null && isImplicitlyFused(current, i, best.type) )
				costVect.addInputSize(c.getInput().get(0).getHopID(), Math.max(c.getDim1(),1)*Math.max(c.getDim2(),1));
			else { //include children and I/O costs
				costs += rGetPlanCosts(memo, c, visited, partition, M, plan, computeCosts, null, null);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
						&& hasNoRefToMatPoint(currentHopId, me, matPoints, plan)
						&& BasicPlanComparator.icompare(me, best)<0 ? me : best;
				opened = true;
			}
			else {
```

### DataFlowIssue
Dereference of `costVect` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
		//add compute costs of current operator to costs vector
		if( computeCosts.containsKey(currentHopId) )
			costVect.computeCosts += computeCosts.get(currentHopId);
		
		//process children recursively
```

### DataFlowIssue
Method invocation `addInputSize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/codegen/opt/PlanSelectionFuseCostBasedV2.java`
#### Snippet
```java
						plan, computeCosts, costVect, best.type, costBound-costs);
			else if( best!=null && isImplicitlyFused(current, i, best.type) )
				costVect.addInputSize(c.getInput().get(0).getHopID(), getSize(c));
			else { //include children and I/O costs
				if( part.getPartition().contains(c.getHopID()) )
```

### DataFlowIssue
Casting `hop` to `DnnOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
				&& hop.getInput().get(0).getDim2()>1)
			|| (HopRewriteUtils.isDnn(hop, OpOpDnn.MAX_POOL, OpOpDnn.AVG_POOL, OpOpDnn.CONV2D)
				&& hop.getInput().get(0).dimsKnown() && ((DnnOp)hop).isStride1Pad0()
				&& hop.getInput().get(1).dimsKnown() && hop.getInput().get(1)!=input) //for conv2d
			|| isPartOfValidCumAggChain(hop) //cum* with transpose
```

### DataFlowIssue
Casting `hop` to `DnnOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
				&& hop.getInput().get(0).getDim2()>1 )
			|| (HopRewriteUtils.isDnn(hop, OpOpDnn.MAX_POOL, OpOpDnn.AVG_POOL, OpOpDnn.CONV2D)
				&& hop.getInput().get(0).dimsKnown() && ((DnnOp)hop).isStride1Pad0()
				&& hop.getInput().get(1).dimsKnown() && hop.getInput().get(1)!=input) //for conv2d
			|| (HopRewriteUtils.isDataGenOpWithLiteralInputs(input, OpOpDG.SEQ)
```

### DataFlowIssue
Casting `hop` to `DnnOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
				&& hop.getInput().get(0).getDim2()>1)
			|| (HopRewriteUtils.isDnn(hop, OpOpDnn.MAX_POOL, OpOpDnn.AVG_POOL, OpOpDnn.CONV2D)
				&& hop.getInput().get(0).dimsKnown() && ((DnnOp)hop).isStride1Pad0()
				&& hop.getInput().get(1).dimsKnown()); //for conv2d
	}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
						&& ix.getInput().get(3).getName().equals(itervar) ){
						leftScalar = true;
						rowIx = false;
					}
				}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
						&& ix.getInput().get(3).getName().equals(itervar) ){
						rightScalar = true;
						rowIx = false;
					}
				}
```

### DataFlowIssue
The call to 'check' always fails, according to its method contracts
in `src/main/java/org/apache/sysds/hops/rewrite/HopDagValidator.java`
#### Snippet
```java
			String parentIDs = hop.getParent().stream()
					.map(h -> Long.toString(h.getHopID())).collect(Collectors.joining(", "));
			check(false, hop, parentIDs, seen);
		}
		if (seen) return; // we saw the Hop previously, no need to re-validate
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
					boolean hasTWrites = hasTransientWriteParents(c);
					boolean moveTWrite = hasTWrites ? HopRewriteUtils.rHasSimpleReadChain(
						c, getFirstTransientWriteParent(c).getName()) : false;
					
					String varname = null;
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteSplitDagDataDependentOperators.java`
#### Snippet
```java
					{
						Hop twrite = getFirstTransientWriteParent(c);
						varname = twrite.getName();
						
						//create new transient read
```

### DataFlowIssue
Argument `metadataObject` might be null
in `src/main/java/org/apache/sysds/hops/fedplanner/PrivacyConstraintLoader.java`
#### Snippet
```java
				try(BufferedReader br = new BufferedReader(new InputStreamReader(fs.open(path)))) {
					JSONObject metadataObject = JSONHelper.parse(br);
					privacyConstraint = PrivacyPropagator.parseAndReturnPrivacyConstraint(metadataObject);
				}
			}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
					bop.setOp(OpOp2.MINUS);
					HopRewriteUtils.replaceChildReference(bop, left, new LiteralOp(0), 0);
					hi = bop;

					LOG.debug("Applied removeUnnecessaryBinaryOperation4 (line "+bop.getBeginLine()+")");
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
					HopRewriteUtils.removeChildReferenceByPos(bop, right, 1);
					HopRewriteUtils.addChildReference(bop, new LiteralOp(0), 0);
					hi = bop;
					
					LOG.debug("Applied removeUnnecessaryBinaryOperation5 (line "+bop.getBeginLine()+")");
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			hi = simplifyBinaryToUnaryOperation(hop, hi, i);     //e.g., X*X -> X^2 (pow2), X+X -> X*2, (X>0)-(X<0) -> sign(X)
			hi = canonicalizeMatrixMultScalarAdd(hi);            //e.g., eps+U%*%t(V) -> U%*%t(V)+eps, U%*%t(V)-eps -> U%*%t(V)+(-eps) 
			hi = simplifyCTableWithConstMatrixInputs(hi);        //e.g., table(X, matrix(1,...)) -> table(X, 1)
			hi = removeUnnecessaryCTable(hop, hi, i);            //e.g., sum(table(X, 1)) -> nrow(X) and sum(table(1, Y)) -> nrow(Y) and sum(table(X, Y)) -> nrow(X)
			hi = simplifyReverseOperation(hop, hi, i);           //e.g., table(seq(1,nrow(X),1),seq(nrow(X),1,-1)) %*% X -> rev(X)
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			hi = simplifyReverseOperation(hop, hi, i);           //e.g., table(seq(1,nrow(X),1),seq(nrow(X),1,-1)) %*% X -> rev(X)
			if(OptimizerUtils.ALLOW_OPERATOR_FUSION)
				hi = simplifyMultiBinaryToBinaryOperation(hi);       //e.g., 1-X*Y -> X 1-* Y
			hi = simplifyDistributiveBinaryOperation(hop, hi, i);//e.g., (X-Y*X) -> (1-Y)*X
			hi = simplifyBushyBinaryOperation(hop, hi, i);       //e.g., (X*(Y*(Z%*%v))) -> (X*Y)*(Z%*%v)
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			hi = simplifyUnaryAggReorgOperation(hop, hi, i);     //e.g., sum(t(X)) -> sum(X)
			hi = removeUnnecessaryAggregates(hi);                //e.g., sum(rowSums(X)) -> sum(X)
			hi = simplifyBinaryMatrixScalarOperation(hop, hi, i);//e.g., as.scalar(X*s) -> as.scalar(X)*s;
			hi = pushdownUnaryAggTransposeOperation(hop, hi, i); //e.g., colSums(t(X)) -> t(rowSums(X))
			hi = pushdownCSETransposeScalarOperation(hop, hi, i);//e.g., a=t(X), b=t(X^2) -> a=t(X), b=t(X)^2 for CSE t(X)
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteAlgebraicSimplificationStatic.java`
#### Snippet
```java
			hi = simplifyOuterSeqExpand(hop, hi, i);             //e.g., outer(v, seq(1,m), "==") -> rexpand(v, max=m, dir=row, ignore=true, cast=false)
			hi = simplifyBinaryComparisonChain(hop, hi, i);      //e.g., outer(v1,v2,"==")==1 -> outer(v1,v2,"=="), outer(v1,v2,"==")==0 -> outer(v1,v2,"!="), 
			hi = simplifyCumsumColOrFullAggregates(hi);          //e.g., colSums(cumsum(X)) -> cumSums(X*seq(nrow(X),1))
			hi = simplifyCumsumReverse(hop, hi, i);              //e.g., rev(cumsum(rev(X))) -> X + colSums(X) - cumsum(X)

```

### DataFlowIssue
Argument `sb` might be null
in `src/main/java/org/apache/sysds/lops/rewrite/RewriteAddChkpointInLoop.java`
#### Snippet
```java

		if (sb == null || !HopRewriteUtils.isLastLevelLoopStatementBlock(sb))
			return List.of(sb);
		// TODO: support If-Else block inside loop. Consumers inside branches.

```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/utils/DataAugmentation.java`
#### Snippet
```java

		FrameBlock res = preprocessing(input, numerics, strings, swappable);
		res = typos(res, strings, pTypo);
		res = miss(res, pMiss, pDrop);
		res = outlier(res, numerics, pOut, 0.5, 3);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/utils/DataAugmentation.java`
#### Snippet
```java
		FrameBlock res = preprocessing(input, numerics, strings, swappable);
		res = typos(res, strings, pTypo);
		res = miss(res, pMiss, pDrop);
		res = outlier(res, numerics, pOut, 0.5, 3);
		
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/utils/DataAugmentation.java`
#### Snippet
```java
		res = typos(res, strings, pTypo);
		res = miss(res, pMiss, pDrop);
		res = outlier(res, numerics, pOut, 0.5, 3);
		
		return res;
```

### DataFlowIssue
Method invocation `validateExpression` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java

		//recursive validate
		_left.validateExpression(ids, constVars, conditional);
		if( _right !=null )
			_right.validateExpression(ids, constVars, conditional);
```

### DataFlowIssue
Dereference of `lValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/common/Types.java`
#### Snippet
```java
			//until we have completely changed the external types
			String lValue = (value != null) ? value.toUpperCase() : null;
			switch(lValue) {
				case "FP32":     return FP32;
				case "FP64":
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
		for (int i = 0; i < execNodes.size(); i++) {
			Lop node = execNodes.get(i);
			doRmVar = false;

			// mark input scalar read nodes for deletion
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
							// processing is same for both transient and persistent scalar writes 
							writeInst.addAll(out.getLastInstructions());
							doRmVar = false;
						}
						else {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
							if ( dnode.getOperationType().isTransient() ) {
								deleteInst.addAll(out.getLastInstructions());
								doRmVar = false;
							}
							else {
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
				Instruction createvarInst = VariableCPInstruction.prepCreatevarInstruction(
					oparams.getLabel(), oparams.getFile_name(), true, node.getDataType(),
					getOutputFileFormat(node, false).toString(),
					new MatrixCharacteristics(oparams.getNumRows(), oparams.getNumCols(), blen, oparams.getNnz()),
					oparams.getUpdateType());
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
						Instruction createvarInst = VariableCPInstruction.prepCreatevarInstruction(
							fnOutParams.getLabel(), getFilePath() + fnOutParams.getLabel(), 
							true, fnOut.getDataType(), getOutputFileFormat(fnOut, false).toString(),
							new MatrixCharacteristics(fnOutParams.getNumRows(), fnOutParams.getNumCols(), (int)fnOutParams.getBlocksize(), fnOutParams.getNnz()),
							oparams.getUpdateType());
```

### DataFlowIssue
Method invocation `getStatement` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/FunctionCallIdentifier.java`
#### Snippet
```java
		
		// Step 6: validate default parameters (after block assignment)
		FunctionStatement fstmt = (FunctionStatement)fblock.getStatement(0);
		for( Expression expDef : fstmt.getInputDefaults() ) {
			if( expDef != null )
```

### DataFlowIssue
Method invocation `getDataType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/FunctionStatementBlock.java`
#### Snippet
```java
			}
			
			if (curr.getDataType() != DataType.UNKNOWN && !curr.getDataType().equals(returnValue.getDataType()) ){
				raiseValidateError("for function " + fstmt.getName() + ", return variable " + curr.getName() + " data type of " + curr.getDataType() + " does not match data type in function signature of " + returnValue.getDataType(), conditional);
			}
```

### DataFlowIssue
Method invocation `getOutput` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
			else if( origVersion !=null ) //only if branch exists
			{
				if (!ifVersion.getOutput().getDataType().equals(origVersion.getOutput().getDataType())){
					raiseValidateError("IfStatementBlock has unsupported conditional data type change of variable '"+updatedVar+"' in if branch.", conditional);
				}
```

### DataFlowIssue
Dereference of `target` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			
			if( MLContextProxy.isActive() )
				MLContextProxy.setAppropriateVarsForRead(source, target._name);
			
			source.validateExpression(ids.getVariables(), currConstVars, conditional);
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				if (bife.getOpCode() == Builtins.NROW) {
					intid = new IntIdentifier((currVal instanceof IndexedIdentifier)
							? ((IndexedIdentifier) currVal).getOrigDim1() : currVal.getDim1(), bife);
				} else {
					intid = new IntIdentifier((currVal instanceof IndexedIdentifier)
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				} else {
					intid = new IntIdentifier((currVal instanceof IndexedIdentifier)
							? ((IndexedIdentifier) currVal).getOrigDim2() : currVal.getDim2(), bife);
				}
				
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				//	--> const prop NOT possible
				if (intid.getValue() != -1)
					currConstVars.put(target.getName(), intid);
			}
		}
```

### DataFlowIssue
Method invocation `getStatement` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			if( fblock == null )
				fcall.raiseValidateError("function " + fcall.getName() + " is undefined in namespace " + fcall.getNamespace(), false);
			FunctionStatement fstmt = (FunctionStatement)fblock.getStatement(0);

			// recursive inlining (no memo required because update-inplace of function statement blocks, so no redundant inlining)
```

### DataFlowIssue
Method invocation `getOutput` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
			raiseValidateError(String.format("Function %s should provide parameter '%s'", fname, pname), conditional);
		}
		if (!(param.getOutput().getDataType().isScalar() && param.getOutput().getValueType().equals(ValueType.STRING))) {
			raiseValidateError(
					String.format("Function %s should provide a string value for %s parameter.", fname, pname),
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
						"1 x " + exprTarget.getOutput().getDim2() +", groups dims: 1 x " + exprGroups.getOutput().getDim2() + ".", conditional);
				}
				colwise = true;
			}
			else {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		// CASE: row dimension is unknown --> assign -1
		else{ 
			updatedRowDim = -1;
		}
		
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		// CASE: column dimension is unknown --> assign -1
		else{ 
			updatedColDim = -1;
		}
		
```

### DataFlowIssue
Unboxing of `dim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					Long dim1 = (getVarParam(READROWPARAM) == null) ? null : Long.valueOf( getVarParam(READROWPARAM).toString());
					Long dim2 = (getVarParam(READCOLPARAM) == null) ? null : Long.valueOf( getVarParam(READCOLPARAM).toString());
					if ( !isCSV && (dim1 < 0 || dim2 < 0) && ConfigurationManager
							.getCompilerConfig().getBool(ConfigType.REJECT_READ_WRITE_UNKNOWNS) ) {
						raiseValidateError("Invalid dimension information in read statement", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
```

### DataFlowIssue
Unboxing of `dim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					Long dim1 = (getVarParam(READROWPARAM) == null) ? null : Long.valueOf( getVarParam(READROWPARAM).toString());
					Long dim2 = (getVarParam(READCOLPARAM) == null) ? null : Long.valueOf( getVarParam(READCOLPARAM).toString());
					if ( !isCSV && (dim1 < 0 || dim2 < 0) && ConfigurationManager
							.getCompilerConfig().getBool(ConfigType.REJECT_READ_WRITE_UNKNOWNS) ) {
						raiseValidateError("Invalid dimension information in read statement", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
```

### DataFlowIssue
Unboxing of `dim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				if(isLIBSVM) {
					Long dim2 = (getVarParam(READCOLPARAM) == null) ? null : Long.valueOf(getVarParam(READCOLPARAM).toString());
					if(dim2 < 0 && ConfigurationManager.getCompilerConfig()
							.getBool(ConfigType.REJECT_READ_WRITE_UNKNOWNS)) {
						raiseValidateError("Invalid dimension information in read statement", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				}
				//_output.setProperties(targetAsSeen);
				((IndexedIdentifier) getOutput()).setOriginalDimensions(targetAsSeen.getDim1(), targetAsSeen.getDim2());
				//((IndexedIdentifier) getOutput()).setOriginalDimensions(getOutput().getDim1(), getOutput().getDim2());
			}
```

### DataFlowIssue
Casting `exp` to `StringIdentifier` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			
			getOutput().setFileFormat(FileFormat.BINARY);
			StringIdentifier fedType = (StringIdentifier) exp;
			if(fedType.getValue().equalsIgnoreCase(FED_MATRIX_IDENTIFIER)) {
				getOutput().setDataType(DataType.MATRIX);
```

### DataFlowIssue
Method invocation `getValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			getOutput().setFileFormat(FileFormat.BINARY);
			StringIdentifier fedType = (StringIdentifier) exp;
			if(fedType.getValue().equalsIgnoreCase(FED_MATRIX_IDENTIFIER)) {
				getOutput().setDataType(DataType.MATRIX);
				// TODO value type for federated object
```

### DataFlowIssue
Method invocation `trim` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextCell.java`
#### Snippet
```java
			// the first line after comments is the one w/ matrix dimensions
			// validate (rlen clen nnz)
			String[] fields = value.trim().split("\\s+"); 
			long mm_rlen = Long.parseLong(fields[0]);
			long mm_clen = Long.parseLong(fields[1]);
```

### DataFlowIssue
Method invocation `scale` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				return new LinearFunction(0, cvalR,((DataIdentifier)l)._name);
			else if( cvalL != null && r instanceof BinaryExpression )
				return rParseBinaryExpression((BinaryExpression)r).scale(cvalL);
			else if( cvalR != null && l instanceof BinaryExpression )
				return rParseBinaryExpression((BinaryExpression)l).scale(cvalR);
```

### DataFlowIssue
Method invocation `scale` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				return rParseBinaryExpression((BinaryExpression)r).scale(cvalL);
			else if( cvalR != null && l instanceof BinaryExpression )
				return rParseBinaryExpression((BinaryExpression)l).scale(cvalR);
		}
		return null; //let dependency analysis fail
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub1)+"'.", ex);
			out = null; //let dependency analysis fail
		}
		
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		catch(Exception ex) {
			LOG.debug("PARFOR: Unable to parse MATRIX subscript expression for '"+String.valueOf(sub1)+"'.", ex);
			out = null; //let dependency analysis fail
		}
		
```

### DataFlowIssue
Method invocation `scale` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				return new LinearFunction(0, cvalR, l.getName());
			else if( cvalL != null && r instanceof org.apache.sysds.hops.BinaryOp )
				return rParseBinaryExpression(r).scale(cvalL);
			else if( cvalR != null && l instanceof org.apache.sysds.hops.BinaryOp )
				return rParseBinaryExpression(l).scale(cvalR);
```

### DataFlowIssue
Method invocation `scale` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				return rParseBinaryExpression(r).scale(cvalL);
			else if( cvalR != null && l instanceof org.apache.sysds.hops.BinaryOp )
				return rParseBinaryExpression(l).scale(cvalR);
		}
		return null; //let dependency analysis fail
```

### DataFlowIssue
Method invocation `hasNonIndexVariables` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				
				if( !CONSERVATIVE_CHECK )
					if(out.hasNonIndexVariables())
					{
						String id = INTERAL_FN_INDEX_ROW+_idSeqfn.getNextID();
```

### DataFlowIssue
Method invocation `scale` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			long colDim = _vsParent.getVariable(idat._name).getDim2();
			if( colDim >= 0 ) {
				out.scale( colDim );
			}
			else {
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(1, id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim1(), 1);
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			output.setDimensions(0, 0);
			output.setBlocksize(0);
			switch (id.getValueType()) {
				case STRING: // TODO think about what we want to get when we sum tensor of strings
				case CHARACTER: // TODO here also for Characters.
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			output.setDimensions(0, 0);
			output.setBlocksize(0);
			output.setValueType(id.getValueType());
			break;
		
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				checkMatrixParam(getFirstExpr());
				output.setDataType(DataType.SCALAR);
				output.setValueType(id.getValueType());
				output.setDimensions(0, 0);
				output.setBlocksize(0);
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			boolean cumSP = getOpCode() == Builtins.CUMSUMPROD;
			if( cumSP && id.getDim2() > 2 )
				raiseValidateError("Cumsumprod only supported over two-column matrices", conditional);
			
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim1(), cumSP ? 1 : id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			output.setDimensions(0, 0);
			output.setBlocksize(0);
			output.setValueType((id.getValueType()!=ValueType.UNKNOWN 
				|| id.getDataType()==DataType.LIST) ? id.getValueType() : ValueType.FP64);
			break;
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				DataType.SCALAR, DataType.FRAME, DataType.LIST);
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim1(), id.getDim2());
			if( getFirstExpr().getOutput().getDataType()==DataType.SCALAR )
				output.setDimensions(1, 1); //correction scalars
```

### DataFlowIssue
Method invocation `getBlocksize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			output.setDataType(DataType.LIST);
			output.setDimensions(-1, 1);
			output.setBlocksize(id.getBlocksize());
			output.setValueType(ValueType.UNKNOWN);
			break;
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixFrameParam(getFirstExpr());
			output.setDataType(DataType.FRAME);
			output.setDimensions(1, id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(ValueType.STRING);
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			DataType.SCALAR, DataType.MATRIX, DataType.LIST);
			output.setDataType(DataType.FRAME);
			output.setDimensions(id.getDim1(), id.getDim2());
			if( getFirstExpr().getOutput().getDataType()==DataType.SCALAR )
				output.setDimensions(1, 1); //correction scalars
```

### DataFlowIssue
Method invocation `getDataType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			}
			
			output.setDataType(id.getDataType());
			output.setValueType(id.getValueType());
			
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim2(), id.getDim1());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim1(), id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			if( id.getDim2() != -1 ) { //type known
				if ( id.getDim2() == 1 ) 
				{
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			}

			output.setValueType(id.getValueType());
			// output dimensions are unknown
			output.setDimensions(-1, -1);
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java

			// Output is a scalar
			output.setValueType(id.getValueType());
			output.setDimensions(0, 0);
			output.setBlocksize(0);
```

### DataFlowIssue
Method invocation `getDataType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkNumParameters(1);
			checkMatrixScalarParam(getFirstExpr());
			output.setDataType(id.getDataType());
			output.setDimensions(id.getDim1(), id.getDim2());
			output.setBlocksize (id.getBlocksize());
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java

			// Output is a scalar
			output.setValueType(id.getValueType());
			output.setDimensions(0, 0);
			output.setBlocksize(0);
```

### DataFlowIssue
Method invocation `getBlocksize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			else
				output.setDimensions(-1, 1);
 			setBlocksize(id.getBlocksize());
 			
			break;
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkScalarParam(getThirdExpr());
			checkValueTypeParam(getThirdExpr(), ValueType.STRING);
			if( id.getDim2() > 1 || id2.getDim1()>1 ) {
				raiseValidateError("Outer vector operations require a common dimension of one: " +
					id.getDim1()+"x"+id.getDim2()+" o "+id2.getDim1()+"x"+id2.getDim2()+".", false);
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixFrameParam(getSecondExpr());
			output.setDataType(DataType.FRAME);
			output.setDimensions(id.getDim1(), id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(ValueType.STRING);
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixFrameParam(getSecondExpr());
			output.setDataType(DataType.FRAME);
			output.setDimensions(id.getDim1(), id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				checkMatrixFrameParam(getSecondExpr());
				output.setDataType(DataType.FRAME);
				output.setDimensions(id.getDim1(), id.getDim2());
				output.setBlocksize (id.getBlocksize());
				break;
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			output.setDataType(DataType.FRAME);
			if(_args[1].getText().contains("jaccardSim")) {
				output.setDimensions(id.getDim1(), id.getDim1());
				output.setValueType(ValueType.FP64);
			}
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			}
			else {
				output.setDimensions(id.getDim1(), id.getDim2());
				output.setValueType(ValueType.STRING);
			}
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				checkMatrixParam(getFirstExpr());
				output.setDataType(DataType.MATRIX);
				output.setDimensions(id.getDim1(), id.getDim2());
				output.setBlocksize (id.getBlocksize());
				output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				checkMatrixParam(getFirstExpr());
				output.setDataType(DataType.MATRIX);
				output.setDimensions(id.getDim1(), id.getDim2());
				output.setBlocksize (id.getBlocksize());
				output.setValueType(id.getValueType());
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(id.getDim1(), 1);
			output.setBlocksize (id.getBlocksize());
			output.setValueType(ValueType.INT64);
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			checkMatrixParam(getFirstExpr());
			output.setDataType(DataType.MATRIX);
			output.setDimensions(1, id.getDim2());
			output.setBlocksize (id.getBlocksize());
			output.setValueType(ValueType.INT64);
```

### DataFlowIssue
Method invocation `getDataType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				//unary operations
				if( getSecondExpr() == null ) {
					output.setDataType(id.getDataType());
					output.setValueType((output.getDataType()==DataType.SCALAR
						&& getOpCode()==Builtins.ABS)?id.getValueType():ValueType.FP64 );
```

### DataFlowIssue
Method invocation `getOutput` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
		output.setDataType(dt);
		output.setValueType(vt);
		output.setDimensions(dt.isMatrix() ? firstM.getOutput().getDim1() : 0,
			dt.isMatrix() ? firstM.getOutput().getDim2() : 0);
		output.setBlocksize (dt.isMatrix() ? firstM.getOutput().getBlocksize() : 0);
```

### DataFlowIssue
Method invocation `getOutput` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
		output.setDimensions(dt.isMatrix() ? firstM.getOutput().getDim1() : 0,
			dt.isMatrix() ? firstM.getOutput().getDim2() : 0);
		output.setBlocksize (dt.isMatrix() ? firstM.getOutput().getBlocksize() : 0);
	}
	
```

### DataFlowIssue
Method invocation `valueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				|| paramCtx.paramType.dataType().getText() == null || paramCtx.paramType.dataType().getText().isEmpty()) ?
				"scalar" : paramCtx.paramType.dataType().getText();
			String valueType = paramCtx.paramType.valueType().getText();
			
			//check and assign data type
```

### DataFlowIssue
Method invocation `valueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
				|| paramCtx.paramType.dataType().getText() == null || paramCtx.paramType.dataType().getText().isEmpty()) ?
				"scalar" : paramCtx.paramType.dataType().getText();
			String valueType = paramCtx.paramType.valueType().getText();
			
			//check and assign data type
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			// If the dimensions are available at compile time, then create a LiteralOp (constant propagation)
			// Else create a UnaryOp so that a control program instruction is generated
			currBuiltinOp = (expr.getDim1()==-1) ? new UnaryOp(target.getName(), target.getDataType(),
				target.getValueType(), OpOp1.NROW, expr) : new LiteralOp(expr.getDim1());
			break;
```

### DataFlowIssue
Method invocation `getDim2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			// If the dimensions are available at compile time, then create a LiteralOp (constant propagation)
			// Else create a UnaryOp so that a control program instruction is generated
			currBuiltinOp = (expr.getDim2()==-1) ? new UnaryOp(target.getName(), target.getDataType(),
				target.getValueType(), OpOp1.NCOL, expr) : new LiteralOp(expr.getDim2());
			break;
```

### DataFlowIssue
Method invocation `getDim1` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			// If the dimensions are available at compile time, then create a LiteralOp (constant propagation)
			// Else create a UnaryOp so that a control program instruction is generated
			currBuiltinOp = (expr.getDim1()==-1 || expr.getDim2()==-1) ? new UnaryOp(target.getName(), target.getDataType(),
				target.getValueType(), OpOp1.LENGTH, expr) : new LiteralOp(expr.getDim1()*expr.getDim2());
			break;
```

### DataFlowIssue
Method invocation `getValueType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			{
				// check if the third argument is "replace" or "seed"
				if ( expr3.getValueType() == ValueType.BOOLEAN ) 
				{
					tmpparams.put(DataExpression.RAND_PDF, expr3);
```

### DataFlowIssue
Argument `expr` might be null
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			currBuiltinOp = new DnnOp(target.getName(), DataType.MATRIX, target.getValueType(),
				OpOpDnn.valueOf(source.getOpCode().name()), inHops1);
			setBlockSizeAndRefreshSizeInfo(expr, currBuiltinOp);
			break;
		}
```

### DataFlowIssue
Argument `expr` might be null
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			currBuiltinOp = new DnnOp(target.getName(), DataType.MATRIX, target.getValueType(),
				OpOpDnn.valueOf(source.getOpCode().name()), getALHopsForPoolingForwardIM2COL(expr, source, 1, hops));
			setBlockSizeAndRefreshSizeInfo(expr, currBuiltinOp);
			break;
		}
```

### DataFlowIssue
Argument `expr` might be null
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			currBuiltinOp = new DnnOp(target.getName(), DataType.MATRIX, target.getValueType(),
				OpOpDnn.valueOf(source.getOpCode().name()), getALHopsForConvOpPoolingCOL2IM(expr, source, 1, hops));
			setBlockSizeAndRefreshSizeInfo(expr, currBuiltinOp);
			break;
		}
```

### DataFlowIssue
Argument `expr` might be null
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			currBuiltinOp = new DnnOp(target.getName(), DataType.MATRIX, target.getValueType(),
				OpOpDnn.valueOf(source.getOpCode().name()), getALHopsForConvOp(expr, source, 1, hops));
			setBlockSizeAndRefreshSizeInfo(expr, currBuiltinOp);
			break;
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					String dirString = dirOp.getStringValue().toUpperCase();
					if (dirString.equals(Direction.RowCol.toString())) {
						dir = Direction.RowCol;
						dataType = DataType.SCALAR;
					} else if (dirString.equals(Direction.Row.toString())) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					if (dirString.equals(Direction.RowCol.toString())) {
						dir = Direction.RowCol;
						dataType = DataType.SCALAR;
					} else if (dirString.equals(Direction.Row.toString())) {
						dir = Direction.Row;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
					String dirString = dirOp.getStringValue().toUpperCase();
					if (dirString.equals(Direction.RowCol.toString())) {
						dir = Direction.RowCol;
					} else if (dirString.equals(Direction.Row.toString())) {
						dir = Direction.Row;
```

### DataFlowIssue
Casting `currBuiltinOp` to `DataOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		setIdentifierParams(currBuiltinOp, source.getOutput());
		if( source.getOpCode()==DataExpression.DataOp.READ )
			((DataOp)currBuiltinOp).setInputBlocksize(target.getBlocksize());
		else if ( source.getOpCode() == DataExpression.DataOp.WRITE ) {
			((DataOp)currBuiltinOp).setPrivacy(hops.get(target.getName()).getPrivacy());
```

### DataFlowIssue
Casting `currBuiltinOp` to `DataOp` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			((DataOp)currBuiltinOp).setInputBlocksize(target.getBlocksize());
		else if ( source.getOpCode() == DataExpression.DataOp.WRITE ) {
			((DataOp)currBuiltinOp).setPrivacy(hops.get(target.getName()).getPrivacy());
			if( source.getVarParam(DataExpression.ROWBLOCKCOUNTPARAM) != null )
				currBuiltinOp.setBlocksize(Integer.parseInt(
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/io/ReaderTextLIBSVMParallel.java`
#### Snippet
```java
			_dest = dest;
			_clen = clen;
			_rc = true;
			_splitCount = splitCount;
		}
```

### DataFlowIssue
Condition `block instanceof TensorBlock` is redundant and can be replaced with a null check
in `src/main/java/org/apache/sysds/runtime/data/TensorBlock.java`
#### Snippet
```java
	@Override
	public TensorBlock slice(int rl, int ru, int cl, int cu, boolean deep, TensorBlock block) {
		if( !(block instanceof TensorBlock) )
			throw new RuntimeException("TensorBlock.slice(int,int,int,int,CacheBlock) CacheBlock was no TensorBlock");
		TensorBlock tb = (TensorBlock) block;
```

### DataFlowIssue
Method invocation `entrySet` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
				{
					JSONObject childObj = JSONHelper.parse(br);
					for( Object obj : childObj.entrySet() ){
						@SuppressWarnings("unchecked") Map.Entry<Object,Object> e = (Map.Entry<Object, Object>) obj;
						Object key = e.getKey();
```

### DataFlowIssue
`null` is returned by the method declared as @NotNull
in `src/main/java/org/apache/sysds/runtime/util/CommonThreadPool.java`
#### Snippet
```java
	public List<Runnable> shutdownNow() {
		return ( _pool != shared ) ?
			_pool.shutdownNow() : null;
	}

```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		{
			File[] files = dir.listFiles();
			for( File f : files )
				count += rcDelete( f );	
		}
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/util/LocalFileUtils.java`
#### Snippet
```java
		{
			File[] files = dir.listFiles();
			for( File f : files )
				rDelete( f );	
		}
```

### DataFlowIssue
Unboxing of `v` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
			map.put(encoded_value, nr_occurrences);
		else
			map.compute(encoded_value, (k, v) -> v + nr_occurrences);
	}

```

### DataFlowIssue
Unboxing of `v` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
			maps.get(idx).put(key, 1);
		else
			maps.get(idx).compute(key, (k, v) -> v + 1);
	}

```

### DataFlowIssue
Dereference of `DataConverter.convertVectorToIndexList(select)` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/frame/data/lib/FrameLibRemoveEmpty.java`
#### Snippet
```java
				return removeEmptyColumnsEmptyReturn();
			else {
				for(int i : DataConverter.convertVectorToIndexList(select)) {
					ret.appendColumn(in.getColumn(i));
					columnMetadata.add(new ColumnMetadata(in.getColumnMetadata(i)));
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/util/DependencyThreadPool.java`
#### Snippet
```java
		int i = 0;
		for(Future<Future<?>> ff : futures) {
			if(dtasks.get(i) instanceof DependencyWrapperTask) {
				for(Future<Future<?>> f : ((DependencyWrapperTask<?>) dtasks.get(i)).getWrappedTaskFuture()) {
					res.add(f.get().get());
```

### DataFlowIssue
Casting `in` to `Integer` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			else if(in instanceof Long && ((Long)in).longValue() == 0)
				return null;
			else if(in instanceof Long && ((Integer)in).intValue() == 0)
				return null;
			else if(in instanceof Boolean && ((Boolean)in).booleanValue() == false)
```

### DataFlowIssue
Method invocation `toLowerCase` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		if(i == size()) // if all null return empty boolean.
			return ArrayFactory.allocateBoolean(size());
		else if(firstNN.toLowerCase().equals("true") || firstNN.toLowerCase().equals("false"))
			return changeTypeBooleanStandard();
		else if(firstNN.equals("0") || firstNN.equals("1"))
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/iogen/RawRow.java`
#### Snippet
```java
					continue;

				sb = sb.reverse();
				startPos -= sb.length();
				sb.append(ntfString);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNIm2Col.java`
#### Snippet
```java
			
			// Get individual zero-based c,h,w indexes from zero-based 'chw'
			ix = LibMatrixDNNHelper.computeTensorIndexes(chw, H, W, ix);
			
			if( simple )
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNIm2Col.java`
#### Snippet
```java
				for(int i = 0; i < input.getNumRows(); i++) {
					if( sblock.isEmpty(i) ) continue;
					ix = LibMatrixDNNHelper.computeTensorIndexes(i, params.P, params.Q, ix);
					int tmpP = ix.ix2*params.stride_h - params.pad_h;
					int tmpQ = ix.ix3*params.stride_w - params.pad_w;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNIm2Col.java`
#### Snippet
```java
					double[] avals = sblock.values(i);
					for(int j = apos; j < apos+alen; j++) {
						ix = LibMatrixDNNHelper.computeTensorIndexes(aix[j], params.R, params.S, ix);
						int h = tmpP + ix.ix2;
						int w = tmpQ + ix.ix3;
```

### DataFlowIssue
Method invocation `length` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/iogen/ReaderMapping.java`
#### Snippet
```java
				stringToken = pair.getKey();
		}
		if(stringToken.length() == 0)
			stringToken = rowDelims.get(0);
		String uniqueDelimiter = null;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNRotate180.java`
#### Snippet
```java
			double[] avals = sblock.values(inputN);
			for(int j = apos; j < apos+alen; j++) {
				ix = LibMatrixDNNHelper.computeTensorIndexes(aix[j], params.P, params.Q, ix);
				if( trans )
					out.appendValue(ix.ix1, outputOffset + ix.ix2*params.Q + ix.ix3, avals[j]);
```

### DataFlowIssue
Array access `locks[j]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
		Object[] locks = _columnLocks.get();
		for(int j = cl; j <= cu; j++) {
			synchronized(locks[j]) { // synchronize on the column.
				_coldata[j] = ArrayFactory.set(_coldata[j], src._coldata[j - cl], rl, ru, _nRow);
			}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/CTableMap.java`
#### Snippet
```java
	public CTableMap(EntryType type) {
		_map = new LongLongDoubleHashMap(type);
		_maxRow = -1;
		_maxCol = -1;
	}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/CTableMap.java`
#### Snippet
```java
		_map = new LongLongDoubleHashMap(type);
		_maxRow = -1;
		_maxCol = -1;
	}
	
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
				double[] avals = sblock.values(n);
				for(int j = apos; j < apos+alen; j++) {
					ix = LibMatrixDNNHelper.computeTensorIndexes(aix[j], P, Q, ix);
					int c = ix.ix1;
					int p = ix.ix2;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNPooling.java`
#### Snippet
```java
					double[] avals = sblock.values(n);
					for(int j = apos; j < apos+alen; j++) {
						ix = LibMatrixDNNHelper.computeTensorIndexes(aix[j], P, Q, ix);
						final int inputOffset = n*CHW + ix.ix1*HW;
						int maxIndex = getMaxIndex(ix.ix2, ix.ix3,
```

### DataFlowIssue
Unboxing of `smallestHashes.peek()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java
				containedSet.add(v);
			}
			else if(v < smallestHashes.peek()) {
				LOG.trace(smallestHashes.peek() + " -- " + v);
				smallestHashes.add(v);
```

### DataFlowIssue
Unboxing of `smallestHashes.poll()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java
				smallestHashes.add(v);
				containedSet.add(v);
				double largest = smallestHashes.poll();
				containedSet.remove(largest);
			}
```

### DataFlowIssue
Unboxing of `smallestHashes.poll()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java

	public double poll() {
		return smallestHashes.poll();
	}

```

### DataFlowIssue
Unboxing of `smallestHashes.peek()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/sketch/countdistinctapprox/SmallestPriorityQueue.java`
#### Snippet
```java

	public double peek() {
		return smallestHashes.peek();
	}

```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/operators/CMOperator.java`
#### Snippet
```java
			case CM4:
			case VARIANCE: 
				ret = false; break;	
			
			default:
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixBincell.java`
#### Snippet
```java
			SparseBlock lsblock = m1.sparseBlock;
			for(int r=rl; r<ru; r++) {
				if( lsblock.isEmpty(r) ) continue;
				appendLeftForSparseBinary(op, lsblock.values(r), lsblock.indexes(r), 
					lsblock.pos(r), lsblock.size(r), 0, r, ret);
```

### DataFlowIssue
Argument `stream` might be null
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
					CodegenUtils.class.getClassLoader());
			stream = classLoader.getResourceAsStream(classAsPath);
			return IOUtils.toByteArray(stream);
		}
		catch (IOException e) {
```

### DataFlowIssue
Arguments of 'max' are the same. Calling this method with the same arguments is meaningless
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java

		int maxRows = Math.max(rowsA, rowsB);
		int maxCols = Math.max(colsA, colsB);

		getCudaKernels(gCtx).launchKernel("rbind",
```

### DataFlowIssue
Arguments of 'max' are the same. Calling this method with the same arguments is meaningless
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		Pointer B = getDensePointer(gCtx, in2, instName);

		int maxRows = toInt(Math.max(rowsA, rowsB));
		int maxCols = toInt(Math.max(colsA, colsB));

```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			ret = LibMatrixAgg.aggregateTernary(m1, m2, m3, ret, op, op.getNumThreads());
		else
			ret = LibMatrixAgg.aggregateTernary(m1, m2, m3, ret, op);
		
		if(op.aggOp.existsCorrection() && inCP)
```

### DataFlowIssue
Array access `cnt[j]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		if( Arrays.stream(inputs).allMatch(m -> m.sparse || m.isEmpty()) ) {
			for( int j=0; j<n; j++ ) {
				if( cnt[j]!=inputs.length )
					c[cix+j] = fn.execute(c[cix+j], 0);
			}
```

### DataFlowIssue
Method invocation `quickGetValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			for ( int i=0; i<rlen; i++ ) {
				d = quickGetValue(i,0);
				w = wts.quickGetValue(i,0);
				if ( d != 0 ) {
					tdw.quickSetValue(ind, 0, d);
```

### DataFlowIssue
Condition `in instanceof DataInputBuffer` is redundant and can be replaced with a null check
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
			nonZeros = mbin.readSparseRows(rlen, nonZeros, sparseBlock);
		}
		else if( in instanceof DataInputBuffer && HDFSTool.USE_BINARYBLOCK_SERIALIZATION ) {
			//workaround because sequencefile.reader.next(key, value) does not yet support serialization framework
			DataInputBuffer din = (DataInputBuffer)in;
```

### DataFlowIssue
Method invocation `appendValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				int c=(int) range.colStart;
				for(; c<Math.min(colCut, range.colEnd+1); c++)
					topleft.appendValue(r+blen-rowCut, c+blen-colCut, a[i+c]);
				for(; c<=range.colEnd; c++)
					topright.appendValue(r+blen-rowCut, c-colCut, a[i+c]);
```

### DataFlowIssue
Method invocation `appendValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
					topleft.appendValue(r+blen-rowCut, c+blen-colCut, a[i+c]);
				for(; c<=range.colEnd; c++)
					topright.appendValue(r+blen-rowCut, c-colCut, a[i+c]);
				i+=clen;
			}
```

### DataFlowIssue
Method invocation `appendValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				int c=(int) range.colStart;
				for(; c<Math.min(colCut, range.colEnd+1); c++)
					bottomleft.appendValue(r-rowCut, c+blen-colCut, a[i+c]);
				for(; c<=range.colEnd; c++)
					bottomright.appendValue(r-rowCut, c-colCut, a[i+c]);
```

### DataFlowIssue
Method invocation `appendValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
					bottomleft.appendValue(r-rowCut, c+blen-colCut, a[i+c]);
				for(; c<=range.colEnd; c++)
					bottomright.appendValue(r-rowCut, c-colCut, a[i+c]);
				i+=clen;
			}
```

### DataFlowIssue
Condition `in instanceof DataInputBuffer` is redundant and can be replaced with a null check
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
				nnz += mbin.readDoubleArray(a.size(i), a.valuesAt(i));
		}
		else if( in instanceof DataInputBuffer && HDFSTool.USE_BINARYBLOCK_SERIALIZATION ) {
			//workaround because sequencefile.reader.next(key, value) does not yet support serialization framework
			DataInputBuffer din = (DataInputBuffer)in;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
				break;
			default:
				isND = false;
				break;
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
				case MIN_INDEX:
				case MAX_INDEX:
				default:           val = Double.NaN; break;
			}
			out.quickSetValue(0, 0, val);
```

### DataFlowIssue
Method invocation `quickGetValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
					fn.execute(ret,
						in1.quickGetValue(i,0),
						in2.quickGetValue(i,0),
						in3.quickGetValue(i,0));
				}
```

### DataFlowIssue
Dereference of `in2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixAgg.java`
#### Snippet
```java
				double[] a = in1.getDenseBlockValues();
				
				if( !in2.sparse && !in3.sparse ) {
					double[] w = in3.getDenseBlockValues();
					if(in2.denseBlock!=null) {
```

### DataFlowIssue
Unboxing of `v` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageGPUCacheEviction.java`
#### Snippet
```java
	public static void decrementLiveCount(Pointer ptr) {
		// Decrement and move to the free list if the live count becomes 0
		if(livePointers.compute(ptr, (k, v) -> v==1 ? null : v-1) == null) {
			long size = getPointerSize(ptr);
			if (!freeQueues.containsKey(size))
```

### DataFlowIssue
Method invocation `getRight` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageEstimatorStatistics.java`
#### Snippet
```java
		for (int i=1; i<=instCount; i++) {
			MutableTriple<String, Long, Double> op = LineageEstimator.computeSavingInst.poll();
			int tl = String.valueOf(op.getRight()*1e-3).indexOf(".");
			if (op != null && op.getRight() > 0)
				sb.append(String.valueOf(i) 
```

### DataFlowIssue
Array access `liinputs[i]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageDedupUtils.java`
#### Snippet
```java
				continue;
			// Wrap the inputs with order-preserving placeholders.
			LineageItem phInput = new LineageItem(ph+String.valueOf(i), new LineageItem[] {liinputs[i]});
			_tmpLineage.set(input, phInput);
			i++;
```

### DataFlowIssue
Argument `liData` might be null
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java

			if (liGPUObj == null)
				putValueCPU(inst, liData, computetime);
			else
				putValueGPU(liGPUObj, instLI, computetime);
```

### DataFlowIssue
Dereference of `sBytes` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			if (DMLScript.STATISTICS) { // increment statistics
				LineageCacheStatistics.incrementSavedComputeTime(e._computeTime);
				FederatedStatistics.aggFedSerializationReuse(sBytes.length);
			}

```

### DataFlowIssue
Method invocation `getLineageItem` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			: (cfinst != null) ? cfinst.getLineageItem(ec).getValue()
			: (cspinst != null) ? cspinst.getLineageItem(ec).getValue()
			: gpuinst.getLineageItem(ec).getValue();
		if (inst instanceof MultiReturnBuiltinCPInstruction) {
			liList = new ArrayList<>();
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
		}
		else
			reuse = false;
		
		if (reuse) {
```

### DataFlowIssue
Method invocation `acquireReadAndRelease` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java

			synchronized( _cache ) {
				putIntern(item, DataType.MATRIX, mo.acquireReadAndRelease(), null, computetime);
			}
		}
```

### DataFlowIssue
Casting `data` to `ScalarObject` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
			MatrixBlock mb = (data instanceof MatrixObject) ? 
					((MatrixObject)data).acquireReadAndRelease() : null;
			long size = mb != null ? mb.getInMemorySize() : ((ScalarObject)data).getSize();

			//remove the placeholder if the entry is bigger than the cache.
```

### DataFlowIssue
Casting `data` to `ScalarObject` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/runtime/lineage/LineageCache.java`
#### Snippet
```java
				MatrixBlock mb = (data instanceof MatrixObject) ? 
						((MatrixObject)data).acquireReadAndRelease() : null;
				long size = mb != null ? mb.getInMemorySize() : ((ScalarObject)data).getSize();

				//remove the placeholder if the entry is bigger than the cache.
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibMMChain.java`
#### Snippet
```java

		// Morph the columns to effecient types for the operation.
		x = filterColGroups(x);

		// Allow overlapping intermediate if the intermediate is guaranteed not to be overlapping.
```

### DataFlowIssue
Array access `w[i]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			double val = (b == null) ? 0 :
				dotProduct(avals, b, aix, 0, cd);
			val *= (weights) ? w[i] : 1;
			val -= (weights2) ? w[i] : 0;
			vectMultiplyAdd(val, avals, c, aix, 0, cd);
```

### DataFlowIssue
Array access `w[i]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				dotProduct(avals, b, aix, 0, cd);
			val *= (weights) ? w[i] : 1;
			val -= (weights2) ? w[i] : 0;
			vectMultiplyAdd(val, avals, c, aix, 0, cd);
		}
```

### DataFlowIssue
Array access `w[i]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
		//row-wise mmchain computation
		for( int i=rl; i < ru; i++ ) {
			if( a.isEmpty(i) || (weights && w[i]==0) )
				continue;
			int apos = a.pos(i);
```

### DataFlowIssue
Array access `w[i]` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			//multiply/subtract weights, if required
			val *= (weights) ? w[i] : 1;
			val -= (weights2) ? w[i] : 0;
			
			//compute 2nd matrix vector and aggregate
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				if( wt==WeightsType.POST ) {
					for( int i=bi; i<bimin; i++ ) {
						double[] wvals = w.values(i), xvals = x.values(i), uvals = u.values(i);
						int xix = x.pos(i), uix = u.pos(i);
						for( int j=bj; j<bjmin; j++ ) {
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				else if( wt==WeightsType.PRE ) {
					for( int i=bi; i<bimin; i++ ) {
						double[] wvals = w.values(i), xvals = x.values(i), uvals = u.values(i);
						int xix = x.pos(i), uix = u.pos(i);
						for( int j=bj; j<bjmin; j++ ) {
```

### DataFlowIssue
Method invocation `values` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				for( int i=bi; i<bimin; i++ ) {
					double[] wvals = w.values(i), uvals = u.values(i);
					double[] xvals = four ? x.values(i) : null; 
					int wix = w.pos(i), uix = u.pos(i);
					for( int j=bj; j<bjmin; j++ )
```

### DataFlowIssue
Argument `x` might be null
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			if( four && !scalar )
				for( int i=bi; i<bimin; i++ )
					aligned[i-bi] = w.isAligned(i-bi, x);
			
			//blocked execution over column blocks
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
			// approach: iterate over W, point-wise in order to exploit sparsity
			for( int i=rl; i<ru; i++ ) {
				if( w.isEmpty(i) ) continue;
				int wpos = w.pos(i);
				int wlen = w.size(i);
```

### DataFlowIssue
Method invocation `quickGetValue` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixMult.java`
#### Snippet
```java
				for( int j=0; j<n; j++ ) {
					double xij = mX.quickGetValue(i, j);
					double wij = mW.quickGetValue(i, j);
					double uvij = 0;
					if( wij != 0 )
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibLeftMultBy.java`
#### Snippet
```java
			return prepareEmptyReturnMatrix(right, left, ret, false);
		ret = prepareReturnMatrix(right, left, ret, false);
		ret = LMM(right.getColGroups(), left, ret, k, right.isOverlapping());
		return ret;
	}
```

### DataFlowIssue
Argument `that` might be null
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
		MatrixBlock that = thatValue == null ? null : (MatrixBlock) thatValue;
		MatrixBlock ret = result == null ? null : (MatrixBlock) result;
		return CLALibBinaryCellOp.binaryOperationsLeft(op, this, that, ret);
	}

```

### DataFlowIssue
Argument `that` might be null
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java
		MatrixBlock that = thatValue == null ? null : (MatrixBlock) thatValue;
		MatrixBlock ret = result == null ? null : (MatrixBlock) result;
		return CLALibBinaryCellOp.binaryOperationsRight(op, this, that, ret);
	}

```

### DataFlowIssue
Condition `mbSliced instanceof MatrixBlock` is redundant and can be replaced with a null check
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibDecompress.java`
#### Snippet
```java
				Math.min(Math.abs(rowOffset), 0), Math.min(cmb.getNumRows(), ret.getNumRows() - rowOffset) - 1, // Rows
				Math.min(Math.abs(colOffset), 0), Math.min(cmb.getNumColumns(), ret.getNumColumns() - colOffset) - 1); // Cols
			if(mbSliced instanceof MatrixBlock) {
				mbSliced.putInto(ret, rowOffset, colOffset, false);
				return;
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibCompAgg.java`
#### Snippet
```java
			result = addCorrection(result, op);
			if(op.aggOp.increOp.fn instanceof Mean)
				result = addCellCount(result, op, r, c);
		}
		return result;
```

### DataFlowIssue
Argument `c` might be null
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibScalar.java`
#### Snippet
```java
			final ColGroupConst c = (v0 != 0) ? constOverlap(m1, v0) : null;
			boolean isMinus = sop instanceof LeftScalarOperator && sop.fn instanceof Minus;
			List<AColGroup> newColGroups = isMinus ? copyGroupsAndMultMinus(m1, sop, c, ret) : copyGroups(m1, sop, c, ret);
			ret.allocateColGroupList(newColGroups);
			ret.setOverlapping(true);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/compress/readers/ReaderColumnSelectionSparseTransposed.java`
#### Snippet
```java
		_rl = mr;
		if(allDone)
			_rl = _ru;
	}
}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/compress/readers/ReaderColumnSelectionSparseTransposed.java`
#### Snippet
```java

		if(_rl == _ru) { // if done return null
			_rl = _ru;
			return null;
		}
```

### DataFlowIssue
Argument `l` might be null
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCodePriorityQue.java`
#### Snippet
```java
		while(que.peek() != null && groupNr >= minNumGroups) {
			CompressedSizeInfoColGroup r = que.peek();
			CompressedSizeInfoColGroup g = sEst.combine(l, r);
			if(g != null) {
				double costOfJoin = cEst.getCost(g);
```

### DataFlowIssue
Dereference of `skip` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
		final OffsetCacheV2[] skip = skipList.get();
		int idx = 0;
		while(idx < skip.length && skip[idx] != null && skip[idx].row <= row)
			idx++;

```

### DataFlowIssue
Result of 'max' is the same as the second argument making the call meaningless
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
					itThat.next();
				// process inside run
				for(int rix = Math.max(rs, itThat.value()); rix < re; rix = itThat.value()) { // nice skip inside runs
					that._dict.addToEntry(v, 0, k, nCol);
					if(itThat.value() == finalOff) // break if final.
```

### DataFlowIssue
Argument `aopcode` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/cp/UaggOuterChainCPInstruction.java`
#### Snippet
```java
			String aopcode = InstructionUtils.deriveAggregateOperatorOpcode(parts[1]);
			CorrectionLocationType corrLoc = InstructionUtils.deriveAggregateOperatorCorrectionLocation(parts[1]);
			AggregateOperator aop = InstructionUtils.parseAggregateOperator(aopcode, corrLoc.toString());

			return new UaggOuterChainCPInstruction(bop, uaggop, aop, in1, in2, out, opcode, str);
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/cp/BinaryMatrixMatrixCPInstruction.java`
#### Snippet
```java

		if(inplace && !(compressedLeft || compressedRight)) {
			inBlock1 = LibMatrixBincell.bincellOpInPlace(inBlock1, inBlock2, (BinaryOperator) _optr);
			// Release the memory occupied by input matrices
			ec.releaseMatrixInput(input1.getName(), input2.getName());
```

### DataFlowIssue
Method invocation `examSparsity` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/BinaryMatrixMatrixCPInstruction.java`
#### Snippet
```java
			// Ensure right dense/sparse output representation (guarded by released input memory)
			if(checkGuardedRepresentationChange(inBlock1, inBlock2, retBlock))
				retBlock.examSparsity();
		}

```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java

		if (DMLScript.STATISTICS)
			ParamServStatistics.accSetupTime((long) tSetup.stop());

		MatrixObject features = sec.getMatrixObject(getParam(PS_FEATURES));
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java

		if (DMLScript.STATISTICS)
			ParamServStatistics.accSetupTime((long) tSetup.stop());

		try {
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParamservBuiltinCPInstruction.java`
#### Snippet
```java

		if (DMLScript.STATISTICS)
			ParamServStatistics.accSetupTime((long) tSetup.stop());

		if (LOG.isDebugEnabled()) {
```

### DataFlowIssue
Casting `val` to `ScalarObject` may produce `ClassCastException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CPInstruction.java`
#### Snippet
```java

			if (val.getDataType() == DataType.SCALAR)
				replacement = "" + ((ScalarObject) val).getStringValue();
			return replacement;
		} else {
```

### DataFlowIssue
Argument `agg` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/InstructionUtils.java`
#### Snippet
```java
				numOutputs == 1 ? CorrectionLocationType.LASTCOLUMN : CorrectionLocationType.NONE);

		aggun = new AggregateUnaryOperator(agg, ReduceCol.getReduceColFnObject(), numThreads);
		return aggun;
	}
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		if (instOpcode.equalsIgnoreCase("maxpooling") || instOpcode.equalsIgnoreCase("relu_maxpooling") ||
			instOpcode.equalsIgnoreCase("avgpooling")) {
			if(matBlock.isEmpty()) {
				outputBlock = new MatrixBlock(N, C*P*Q, true);
			}
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
				instOpcode.equalsIgnoreCase("avgpooling_backward")) {
			MatrixBlock dout = ec.getMatrixInput(_in2.getName());
			boolean isEmpty = instOpcode.equalsIgnoreCase("avgpooling_backward") ? dout.isEmpty() : (matBlock.isEmpty() || dout.isEmpty());
			if(isEmpty) {
				outputBlock = new MatrixBlock(N, C*H*W, true);
```

### DataFlowIssue
Method invocation `getNonZeros` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in2.getName());
			if(filter.isEmpty() || matBlock.isEmpty()) {
```

### DataFlowIssue
Method invocation `getNonZeros` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		}
		else if (instOpcode.equalsIgnoreCase("conv2d_bias_add")) {
			resetNumThreads(params, C*R*S, P*Q, matBlock.getNonZeros() / (matBlock.getNumRows()*matBlock.getNumColumns()));
			MatrixBlock filter = ec.getMatrixInput(_in3.getName());
			MatrixBlock bias = ec.getMatrixInput(_in2.getName());
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		else if (instOpcode.equalsIgnoreCase("conv2d_backward_filter")) {
			MatrixBlock dout = ec.getMatrixInput(_in2.getName());
			if(dout.isEmpty() || matBlock.isEmpty()) {
				outputBlock = new MatrixBlock(K, C*R*S, true);
			}
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DnnCPInstruction.java`
#### Snippet
```java
		else if (instOpcode.equalsIgnoreCase("conv2d_backward_data")) {
			MatrixBlock dout = ec.getMatrixInput(_in2.getName());
			if(dout.isEmpty() || matBlock.isEmpty()) {
				outputBlock = new MatrixBlock(N, C * H * W, true);
			}
```

### DataFlowIssue
Method invocation `add` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/EvalNaryCPInstruction.java`
#### Snippet
```java
			sortedData.add(in.getData(name));
			if (DMLScript.LINEAGE)
				sortedLI.add(in.getLineageItem(name));
		}
		return new ListObject(sortedData, new ArrayList<>(fArgNames), sortedLI);
```

### DataFlowIssue
Method invocation `acquireModify` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/EvalNaryCPInstruction.java`
#### Snippet
```java
						+ " (valid: matrix/frame/scalar; where frames or scalars are converted to output matrices)");
				}
				outputMO.acquireModify(mb);
				outputMO.release();
				ec.setVariable(output.getName(), outputMO);
```

### DataFlowIssue
Method invocation `isFederated` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MMChainFEDInstruction.java`
#### Snippet
```java
		FederatedRequest fr1 = mo1.getFedMapping().broadcast(mo2);

		if(_type.isWeighted() && mo3.isFederated()
			&& mo1.getFedMapping().isAligned(mo3.getFedMapping(), AlignType.ROW)) {
				//construct commands: execute, get and aggregate, cleanup
```

### DataFlowIssue
Argument `naStrings` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
					if ( parts.length == 16+extSchema )
						naStrings = parts[curPos+4];
					fmtProperties = new FileFormatPropertiesCSV(hasHeader, delim, fill, fillValue, naStrings) ;
				}
				return new VariableCPInstruction(VariableOperationCode.CreateVariable,
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
				return null;
			});
			globalEncoder = finalGlobalEncoder;
			colNames = finalColNames;
		}
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
			});
			globalEncoder = finalGlobalEncoder;
			colNames = finalColNames;
		}

```

### DataFlowIssue
Dereference of `bucketWithQ` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
				int bucket2Size = bucketFrequencies[i];
				if (bucket2Size != 0) {
					bucketWithQ = new ImmutablePair<>(bucketWithQ.left, tmpBinLeft + bucketRange);
					bucketWithQSize += bucket2Size;
					break;
```

### DataFlowIssue
Casting `ret` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java

		} else
			result = (Double) ret;

		result = average ? result / 2 : (isIQM ? ((result + q25Part*q25Val - q75Part*q75Val) / (vectorLength * 0.5)) : result);
```

### DataFlowIssue
Casting `hist` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java

			if(_type == OperationTypes.IQM) {
				q25Right = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q25Right;
				q25Left = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q25Left;
				q75Right = i == 1 ? hist instanceof ImmutablePair ? ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q75Right;
```

### DataFlowIssue
Casting `hist` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
			if(_type == OperationTypes.IQM) {
				q25Right = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q25Right;
				q25Left = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q25Left;
				q75Right = i == 1 ? hist instanceof ImmutablePair ? ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q75Right;
				q75Left = i == 1 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q75Left;
```

### DataFlowIssue
Casting `hist` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
				q25Right = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q25Right;
				q25Left = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q25Left;
				q75Right = i == 1 ? hist instanceof ImmutablePair ? ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q75Right;
				q75Left = i == 1 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q75Left;
			} else {
```

### DataFlowIssue
Casting `hist` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
				q25Left = i == 0 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q25Left;
				q75Right = i == 1 ? hist instanceof ImmutablePair ? ((ImmutablePair<Double, Double>)hist).right : (Double) hist : q75Right;
				q75Left = i == 1 ? hist instanceof ImmutablePair ?  ((ImmutablePair<Double, Double>)hist).left : (Double) hist : q75Left;
			} else {
				if(hist instanceof ImmutablePair)
```

### DataFlowIssue
Casting `hist` to `Double` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java
					retBuckets.put(i, hist); // set value if returned double instead of bin
				else
					out.setValue(i, 0, (Double) hist);
			}
		}
```

### DataFlowIssue
Method invocation `getNumRows` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/QuantilePickFEDInstruction.java`
#### Snippet
```java

		// Add min to the result
		MatrixBlock res = new MatrixBlock(quantileValues.getNumRows() + 1, 1, false);
		res.setValue(0,0, globalMin);
		res.copy(1, quantileValues.getNumRows(), 0, 0,  quantileValues,false);
```

### DataFlowIssue
Casting `inst` to `CentralMomentSPInstruction` will produce `ClassCastException` for any non-null value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/UnaryFEDInstruction.java`
#### Snippet
```java
			if(mo1.isFederatedExcept(FType.BROADCAST)) {
				if(inst.getOpcode().equalsIgnoreCase("cm"))
					return CentralMomentFEDInstruction.parseInstruction((CentralMomentSPInstruction) inst);
				else if(inst.getOpcode().equalsIgnoreCase("qsort")) {
					if(mo1.getFedMapping().getFederatedRanges().length == 1)
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
		SpoofFEDType spoofType = null;
		if(scla == SpoofCellwise.class)
			spoofType = new SpoofFEDCellwise(_op, _output, fedMap.getType());
		else if(scla == SpoofRowwise.class)
			spoofType = new SpoofFEDRowwise(_op, _output, fedMap.getType());
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
			spoofType = new SpoofFEDCellwise(_op, _output, fedMap.getType());
		else if(scla == SpoofRowwise.class)
			spoofType = new SpoofFEDRowwise(_op, _output, fedMap.getType());
		else if(scla == SpoofMultiAggregate.class)
			spoofType = new SpoofFEDMultiAgg(_op, _output, fedMap.getType());
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
			spoofType = new SpoofFEDRowwise(_op, _output, fedMap.getType());
		else if(scla == SpoofMultiAggregate.class)
			spoofType = new SpoofFEDMultiAgg(_op, _output, fedMap.getType());
		else if(scla == SpoofOuterProduct.class)
			spoofType = new SpoofFEDOuterProduct(_op, _output, fedMap.getType(), _inputs);
```

### DataFlowIssue
Method invocation `getType` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/fed/SpoofFEDInstruction.java`
#### Snippet
```java
			spoofType = new SpoofFEDMultiAgg(_op, _output, fedMap.getType());
		else if(scla == SpoofOuterProduct.class)
			spoofType = new SpoofFEDOuterProduct(_op, _output, fedMap.getType(), _inputs);
		else
			throw new DMLRuntimeException("Federated code generation only supported" +
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
			return null;
		});
		dcs = finalDcs1;
		out.getDataCharacteristics().set(mo.getDataCharacteristics());
		for(int i = 0; i < mo.getFedMapping().getFederatedRanges().length; i++) {
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		});

		dcs = finalDcs1;
		out.getDataCharacteristics().set(mo.getDataCharacteristics());
		int len = marginRow ? mo.getSchema().length : (int) (mo.isFederated(FType.ROW) ? s
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMatrixMemoryManager.java`
#### Snippet
```java
				ret = gpuManager.allPointers.get(gpuObj.getDensePointer()).getSizeInBytes();
			else
				ret = 0; // evicted hence no contiguous memory on GPU
		}
		else if(gpuObj.getJcudaSparseMatrixPtr() != null) {
```

### DataFlowIssue
Argument `out` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BuiltinNarySPInstruction.java`
#### Snippet
```java
			//aggregate partially overlapping blocks w/ single shuffle
			int numPartOut = SparkUtils.getNumPreferredPartitions(mcOut);
			out = RDDAggregateUtils.mergeByKey(out, numPartOut, false);
		}
		else if( ArrayUtils.contains(new String[]{"nmin","nmax","n+"}, getOpcode()) ) {
```

### DataFlowIssue
Method invocation `mapValues` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/BuiltinNarySPInstruction.java`
#### Snippet
```java
			
			//compute nary min/max (partitioning-preserving)
			out = in.mapValues(new MinMaxAddFunction(getOpcode(), scalars));
		}
		
```

### DataFlowIssue
Variable is already assigned to this value
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
			_op = op;
			_blen = blen;
			_blen = blen;
		}
		
```

### DataFlowIssue
Argument `aopcode` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/spark/AggregateUnarySPInstruction.java`
#### Snippet
```java
		
		AggregateUnaryOperator aggun = InstructionUtils.parseBasicAggregateUnaryOperator(opcode);
		AggregateOperator aop = InstructionUtils.parseAggregateOperator(aopcode, corrLoc.toString());
		return new AggregateUnarySPInstruction(SPType.AggregateUnary, aggun, aop, in1, out, aggtype, opcode, str);
	}
```

### DataFlowIssue
Method invocation `mapValues` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
		JavaPairRDD<MatrixIndexes,MatrixBlock> out = null;
		if( input1.isMatrix() && !input2.isMatrix() && !input3.isMatrix() )
			out = in1.mapValues(new TernaryFunctionMSS(op, m1, m2, m3));
		else if( !input1.isMatrix() && input2.isMatrix() && !input3.isMatrix() )
			out = in2.mapValues(new TernaryFunctionSMS(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `mapValues` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in1.mapValues(new TernaryFunctionMSS(op, m1, m2, m3));
		else if( !input1.isMatrix() && input2.isMatrix() && !input3.isMatrix() )
			out = in2.mapValues(new TernaryFunctionSMS(op, m1, m2, m3));
		else if( !input1.isMatrix() && !input2.isMatrix() && input3.isMatrix() )
			out = in3.mapValues(new TernaryFunctionSSM(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `mapValues` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in2.mapValues(new TernaryFunctionSMS(op, m1, m2, m3));
		else if( !input1.isMatrix() && !input2.isMatrix() && input3.isMatrix() )
			out = in3.mapValues(new TernaryFunctionSSM(op, m1, m2, m3));
		else if( input1.isMatrix() && input2.isMatrix() && !input3.isMatrix() )
			out = in1.join(in2).mapValues(new TernaryFunctionMMS(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `join` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in3.mapValues(new TernaryFunctionSSM(op, m1, m2, m3));
		else if( input1.isMatrix() && input2.isMatrix() && !input3.isMatrix() )
			out = in1.join(in2).mapValues(new TernaryFunctionMMS(op, m1, m2, m3));
		else if( input1.isMatrix() && !input2.isMatrix() && input3.isMatrix() )
			out = in1.join(in3).mapValues(new TernaryFunctionMSM(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `join` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in1.join(in2).mapValues(new TernaryFunctionMMS(op, m1, m2, m3));
		else if( input1.isMatrix() && !input2.isMatrix() && input3.isMatrix() )
			out = in1.join(in3).mapValues(new TernaryFunctionMSM(op, m1, m2, m3));
		else if( !input1.isMatrix() && input2.isMatrix() && input3.isMatrix() )
			out = in2.join(in3).mapValues(new TernaryFunctionSMM(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `join` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in1.join(in3).mapValues(new TernaryFunctionMSM(op, m1, m2, m3));
		else if( !input1.isMatrix() && input2.isMatrix() && input3.isMatrix() )
			out = in2.join(in3).mapValues(new TernaryFunctionSMM(op, m1, m2, m3));
		else // all matrices
			out = in1.join(in2).join(in3).mapValues(new TernaryFunctionMMM(op, m1, m2, m3));
```

### DataFlowIssue
Method invocation `join` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/TernarySPInstruction.java`
#### Snippet
```java
			out = in2.join(in3).mapValues(new TernaryFunctionSMM(op, m1, m2, m3));
		else // all matrices
			out = in1.join(in2).join(in3).mapValues(new TernaryFunctionMMM(op, m1, m2, m3));
		
		//set output RDD
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CentralMomentSPInstruction.java`
#### Snippet
```java
		try {
			if ( in3 == null ) {
				cmOrder = Integer.parseInt(in2.getName());
			}
			else {
```

### DataFlowIssue
Argument `in2` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
			//creating cells to avoid unnecessary caching, repeated joins, etc.
			dim1 = (long) RDDAggregateUtils.max(in1);
			dim2 = ctableOp.hasSecondInput() ? (long) RDDAggregateUtils.max(in2) :
				sec.getScalarInput(input3).getLongValue();
		}
```

### DataFlowIssue
Method invocation `isRight` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MapmmSPInstruction.java`
#### Snippet
```java
			if( numParts2 > numParts ) { //flip required
				type = type.getFlipped();
				rddVar = type.isRight() ? input1.getName() : input2.getName();
				bcastVar = type.isRight() ? input2.getName() : input1.getName();
				mcRdd = sec.getDataCharacteristics(rddVar);
```

### DataFlowIssue
Method invocation `persist` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/PMapmmSPInstruction.java`
#### Snippet
```java
		
		//cache final result
		out = out.persist(pmapmmStorageLevel);
		out.count();
		
```

### DataFlowIssue
Argument `aopcode` might be null
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
			String aopcode = InstructionUtils.deriveAggregateOperatorOpcode(parts[1]);
			CorrectionLocationType corrLoc = InstructionUtils.deriveAggregateOperatorCorrectionLocation(parts[1]);
			AggregateOperator aop = InstructionUtils.parseAggregateOperator(aopcode, corrLoc.toString());

			return new UaggOuterChainSPInstruction(bop, uaggop, aop, in1, in2, out, opcode, str);
```

### DataFlowIssue
Method invocation `join` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ParameterizedBuiltinSPInstruction.java`
#### Snippet
```java
				}

				groupWeightedCells = groups.join(target).join(weights).flatMapToPair(new ExtractGroupNWeights());
			}
			else // input vector or matrix
```

### DataFlowIssue
Method invocation `persist` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CheckpointSPInstruction.java`
#### Snippet
```java
			
			//actual checkpoint into given storage level
			out = out.persist( _level );
			
			//trigger nnz computation for all cached (i.e., read-only) datasets 
```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java

			if( !mc.nnzKnown() )
				mc.setNonZeros( aNnz.value() );
		}
		else if( fmt == FileFormat.CSV ) {
```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java

			if( !mc.nnzKnown() )
				mc.setNonZeros(aNnz.value().longValue());
		}
		else if( fmt == FileFormat.BINARY ) {
```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java

			if( !mc.nnzKnown() ) //update nnz
				mc.setNonZeros(aNnz.value().longValue());
			if( nonDefaultBlen )
				mcOut = new MatrixCharacteristics(mc).setBlocksize(blen);
```

### DataFlowIssue
Method invocation `value` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java

			if( !mc.nnzKnown() )
				mc.setNonZeros(aNnz.value().longValue());
		}
		else {
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ParWorker.java`
#### Snippet
```java
			
			if( _monitor )
				StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_ITER_T, time1.stop());
		}

```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ParWorker.java`
#### Snippet
```java
		if( _monitor ) {
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_TASKSIZE, task.size());
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_TASK_T, time2.stop());
		}
	}
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ParWorker.java`
#### Snippet
```java
			
			if( _monitor )
				StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_ITER_T, time1.stop());
		}

```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ParWorker.java`
#### Snippet
```java
		if( _monitor ) {
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_TASKSIZE, task.size());
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_TASK_T, time2.stop());
		}
	}
```

### DataFlowIssue
Method invocation `apply` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteDPParForSpark.java`
#### Snippet
```java
				else { //dense
					for( int j=0; j<_clen; j++ )
						mb.appendValue(0, j, vect.apply(j));
				}
			}
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/RemoteDPParForSpark.java`
#### Snippet
```java
				Row row = (Row) obj;
				for( int j=off; j<off+_clen; j++ )
					mb.appendValue(0, j-off, UtilFunctions.getDouble(row.get(j)));
			}
			mb.examSparsity();
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalParWorker.java`
#### Snippet
```java
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_NUMTASKS, _numTasks);
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_NUMITERS, _numIters);
			StatisticMonitor.putPWStat(_workerID, Stat.PARWRK_EXEC_T, time1.stop());
		}
	}
```

### DataFlowIssue
Method invocation `apply` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtils.java`
#### Snippet
```java
						else { //dense
							for( int j=0; j<lclen; j++ )
								mb[cix-1].appendValue(pos, j, vect.apply(pix++));
						}
					}
```

### DataFlowIssue
Dereference of `lnames2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							//copy only values that are different from the original
							String[] lnames2 = dir2.list();
							if( lnames2.length != 1 ) //there should be exactly 1 compare block
								throw new DMLRuntimeException("Unable to merge results because multiple compare blocks found.");
							mb = StagingFileUtils.readCellList2BlockFromLocal( dir2+"/"+lnames2[0], blen );
```

### DataFlowIssue
Dereference of `dir.list()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							boolean appendOnly = mb.isInSparseFormat();
							DenseBlock compare = DataConverter.convertToDenseBlock(mb, false);
							for( String lname : dir.list() ) {
								MatrixBlock tmp = StagingFileUtils.readCellList2BlockFromLocal(  dir+"/"+lname, blen );
								mergeWithComp(mb, tmp, compare);
```

### DataFlowIssue
Dereference of `dir.list()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							//copy all non-zeros from all workers
							boolean appendOnly = false;
							for( String lname : dir.list() ) {
								if( mb == null ) {
									mb = StagingFileUtils.readCellList2BlockFromLocal( dir+"/"+lname, blen );
```

### DataFlowIssue
Method invocation `examSparsity` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							
							//change sparsity if required after 
							mb.examSparsity(); 
						}
					}
```

### DataFlowIssue
Dereference of `lnames2` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							//copy only values that are different from the original
							String[] lnames2 = dir2.list();
							if( lnames2.length != 1 ) //there should be exactly 1 compare block
								throw new DMLRuntimeException("Unable to merge results because multiple compare blocks found.");
							mb = LocalFileUtils.readMatrixBlockFromLocal( dir2+"/"+lnames2[0] );
```

### DataFlowIssue
Dereference of `dir.list()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							boolean appendOnly = mb.isInSparseFormat();
							DenseBlock compare = DataConverter.convertToDenseBlock(mb, false);
							for( String lname : dir.list() ) {
								MatrixBlock tmp = LocalFileUtils.readMatrixBlockFromLocal( dir+"/"+lname );
								mergeWithComp(mb, tmp, compare);
```

### DataFlowIssue
Dereference of `dir.list()` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							//copy all non-zeros from all workers
							boolean appendOnly = false;
							for( String lname : dir.list() ) {
								if( mb == null ) {
									mb = LocalFileUtils.readMatrixBlockFromLocal( dir+"/"+lname );
```

### DataFlowIssue
Method invocation `examSparsity` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
							
							//change sparsity if required after 
							mb.examSparsity(); 
						}
					}
```

### DataFlowIssue
Dereference of `fnameBlocks` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			
			String[] fnameBlocks = new File( lpdir ).list();
			for( String fnameBlock : fnameBlocks  )
			{
				LinkedList<Cell> tmp = StagingFileUtils.readCellListFromLocal(lpdir+"/"+fnameBlock);
```

### DataFlowIssue
Dereference of `fnameBlocks` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			
			String[] fnameBlocks = new File( lpdir ).list();
			for( String fnameBlock : fnameBlocks  )
			{
				LinkedList<Cell> tmp = StagingFileUtils.readCellListFromLocal(lpdir+"/"+fnameBlock);
```

### DataFlowIssue
Dereference of `fnamesPartitions` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			if(PARALLEL) 
			{
				int len = Math.min(fnamesPartitions.length, _par);
				Thread[] threads = new Thread[len];
				for( int i=0;i<len;i++ )
```

### DataFlowIssue
Dereference of `fnamesPartitions` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			else
			{
				for( String pdir : fnamesPartitions  )
					writeTextCellFileToHDFS( job, fnameNew, fnameStaging+"/"+pdir );	
			}
```

### DataFlowIssue
Dereference of `fnamesPartitions` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			if(PARALLEL) 
			{
				int len = Math.min(fnamesPartitions.length, _par);
				Thread[] threads = new Thread[len];
				for( int i=0;i<len;i++ )
```

### DataFlowIssue
Dereference of `fnamesPartitions` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
			}
			else {
				for( String pdir : fnamesPartitions  )
					writeBinaryBlockSequenceFileToHDFS( job, fnameNew, fnameStaging+"/"+pdir, false );
			}
```

### DataFlowIssue
Dereference of `fnameBlocks` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
		try {	
			String[] fnameBlocks = new File( lpdir ).list();
			for( String fnameBlock : fnameBlocks  )
			{
				long key2 = getKey2FromFileName(fnameBlock);
```

### DataFlowIssue
Method invocation `checkAndCleanupLeafNodes` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
		
		//final cleanup
		node.checkAndCleanupLeafNodes(); //NOTE: required because this function is also used to create subtrees
		
		return node;
```

### DataFlowIssue
Dereference of `files` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
			if( fdir.exists()){ //just for robustness
				File[] files = fdir.listFiles();
				for( File f : files )
					if( f.getName().startsWith(cacheEvictionLocalFilePrefix) )
						f.delete();
```

### DataFlowIssue
Method invocation `scalarOperations` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
			ScalarOperator sop2 = InstructionUtils.parseScalarBinaryOperator("/", false);
			sop2 = sop2.setConstant(size);
			return ret.scalarOperations(sop2, new MatrixBlock());
		}
		catch(Exception ex) {
```

### DataFlowIssue
Method invocation `getDataSize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
		if(v instanceof CacheBlock) {
			var block = ExecutionContext.createCacheableData((CacheBlock<?>) v);
			size = block.getDataSize();
			data = block;
		}
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/SparkParamservUtils.java`
#### Snippet
```java

		if (DMLScript.STATISTICS)
			ParamServStatistics.accSetupTime((long) tSetup.stop());
		return result;
	}
```

### DataFlowIssue
Variable update does nothing
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				_numExecutors = numExecutors;
				_defaultPar = (defaultPar>1) ? defaultPar : numExecutors * numCoresPerExec;
				_confOnly &= true;
			}
			else if( DMLScript.USE_LOCAL_SPARK_CONFIG ) {
```

### DataFlowIssue
Variable update does nothing
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/SparkExecutionContext.java`
#### Snippet
```java
				_numExecutors = 1;
				_defaultPar = 2;
				_confOnly &= true;
			}
			else {
```

### DataFlowIssue
Method invocation `serialize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
				} finally {
					try {
						callback.onSuccess(response.serialize());
					} catch (IOException e) {
						throw new DMLRuntimeException("PSRpcHandler: some error occrred when wrapping the rpc response.", e);
```

### DataFlowIssue
Method invocation `serialize` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/rpc/PSRpcHandler.java`
#### Snippet
```java
				} finally {
					try {
						callback.onSuccess(response.serialize());
					} catch (IOException e) {
						throw new DMLRuntimeException("PSRpcHandler: some error occrred when wrapping the rpc response.", e);
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
			Object[] responseData = udfResponse.get().getData();
			if(DMLScript.STATISTICS) {
				long total = (long) tFedCommunication.stop();
				long workerComputing = ((DoubleObject) responseData[1]).getLongValue();
				ParamServStatistics.accFedWorkerComputing(workerComputing);
```

### DataFlowIssue
Method invocation `stop` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
		catch(Exception e) {
			if(DMLScript.STATISTICS)
				tFedCommunication.stop();
			throw new DMLRuntimeException("FederatedLocalPSThread: failed to execute UDF" + e.getMessage(), e);
		}
```

### DataFlowIssue
Method invocation `processInstruction` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
				if(_localUpdate && (batchCounter < _numBatchesToCompute - 1 | modelAvg) ) {
					// Invoke the aggregate function
					aggregationInstruction.processInstruction(ec);
					// Get the new model
					model = ec.getListObject(aggregationOutput.getName());
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/FederatedPSControlThread.java`
#### Snippet
```java
					aggregationInstruction.processInstruction(ec);
					// Get the new model
					model = ec.getListObject(aggregationOutput.getName());
					// Set new model in execution context
					ec.setVariable(Statement.PS_MODEL, model);
```

## RuleId[id=BoxingBoxedValue]
### BoxingBoxedValue
Boxing of already boxed `key`
in `src/main/java/org/apache/sysds/runtime/instructions/spark/data/FrameReblockBuffer.java`
#### Snippet
```java
		
		//output block
		out.add(new Pair<>(Long.valueOf(key), value));
	}
	
```

## RuleId[id=ParameterCanBeLocal]
### ParameterCanBeLocal
Parameter can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
	 * @return a boolean frameBlock
	 */
	public FrameBlock binaryOperations(BinaryOperator bop, FrameBlock that, FrameBlock out) {
		if(getNumColumns() != that.getNumColumns() && getNumRows() != that.getNumColumns())
			throw new DMLRuntimeException("Frame dimension mismatch " + getNumRows() + " * " + getNumColumns() + " != "
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibStack.java`
#### Snippet
```java
	}

	private static long findCLength(Map<MatrixIndexes, MatrixBlock> m, MatrixBlock b) {
		final MatrixIndexes lookup = new MatrixIndexes(1, 1);
		long cols = 0;
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibStack.java`
#### Snippet
```java
	}

	private static long findRLength(Map<MatrixIndexes, MatrixBlock> m, MatrixBlock b) {
		final MatrixIndexes lookup = new MatrixIndexes(1, 1);
		long rows = 0;
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java

	private static MatrixBlock binaryOperationsEmpty(BinaryOperator op, CompressedMatrixBlock m1, MatrixBlock that,
		MatrixBlock result) {
		final int m1Col = m1.getNumColumns();
		final int m1Row = m1.getNumRows();
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassRemoveConstantBinaryOps.java`
#### Snippet
```java
			//pruning updated variables
			for( String var : sb.variablesUpdated().getVariableNames() )
				if( mOnes.containsKey( var ) )
					mOnes.remove( var );
			
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
				TemplateUtils.rFlipVectorLookups(out);
			//maintain input hops
			if( out instanceof CNodeData && !inHops.contains(hop.getInput().get(0)) )
				inHops.add(hop.getInput().get(0));
		}
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateCell.java`
#### Snippet
```java
				CNode cdata1 = TemplateUtils.skipTranspose(tmp.get(hop.getInput().get(0).getHopID()), 
						hop.getInput().get(0), tmp, compileLiterals);
				if( cdata1 instanceof CNodeData && !inHops.contains(hop.getInput().get(0).getInput().get(0)) )
					inHops.add(hop.getInput().get(0).getInput().get(0));
				if( TemplateUtils.isColVector(cdata1) )
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteGPUSpecificOps.java`
#### Snippet
```java
			ret.add(tRead);
			// Remove old output from roots to avoid unnecessary computation.
			if(roots.contains(oldHops[i])) {
				roots.remove(oldHops[i]);
			}
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `src/main/java/org/apache/sysds/hops/codegen/template/TemplateRow.java`
#### Snippet
```java
			out = TemplateUtils.skipTranspose(tmp.get(hop.getHopID()),
				hop, tmp, compileLiterals);
			if( out instanceof CNodeData && !inHops.contains(hop.getInput().get(0)) )
				inHops.add(hop.getInput().get(0));
		}
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `src/main/java/org/apache/sysds/parser/WhileStatementBlock.java`
#### Snippet
```java
		//remove updated vars from constants
		for( String var : _updated.getVariableNames() )
			if( constVars.containsKey( var ) )
				constVars.remove( var );
		
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `src/main/java/org/apache/sysds/parser/WhileStatementBlock.java`
#### Snippet
```java
			//remove updated vars from constants
			for( String var : _updated.getVariableNames() )
				if( constVars.containsKey( var ) )
					constVars.remove( var );
			
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
		//remove updated vars from constants
		for( String var : _updated.getVariableNames() )
			if( constVars.containsKey( var ) )
				constVars.remove( var );
		
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `src/main/java/org/apache/sysds/parser/ForStatementBlock.java`
#### Snippet
```java
			//remove updated vars from constants
			for( String var : _updated.getVariableNames() )
				if( constVars.containsKey( var ) )
					constVars.remove( var );
			
```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		//check for existing sparse block: return empty list
		if( sparseBlock==null )
			return new ArrayList<IJV>().iterator();
		
		//get iterator over sparse block
```

## RuleId[id=SwitchStatementWithTooFewBranches]
### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
		// parameterized operations to be executed in spark
		if(transitive && _etype == ExecType.CP && _etypeForced != ExecType.CP) {
			switch(_op) {
				case CONTAINS:
					if(getTargetHop().optFindExecType() == ExecType.SPARK)
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/hops/cost/CostEstimationWrapper.java`
#### Snippet
```java

	private static CostEstimator createCostEstimator( CostType type ) {
		switch( type )
		{
			case STATIC:
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteElementwiseMultChainOptimization.java`
#### Snippet
```java

			// o1 and o2 have the same data type
			switch (o1.getDataType()) {
			case MATRIX:
				// two matrices; check for vectors
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
		}
		
		switch (this.getOpCode()) {	
			case TRANSFORMENCODE:
				DataIdentifier out1 = (DataIdentifier) getOutputs()[0];
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			BinaryExpression expr = (BinaryExpression)fileNameExpr;
			Expression.BinaryOp op = expr.getOpCode();
			switch (op){
			case PLUS:
				filename = "";
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
				if (expr2 == null) {
					OpOp1 mathOp2;
					switch (source.getOpCode()) {
					case LOG:
						mathOp2 = OpOp1.LOG;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
				} else {
					OpOp2 mathOp3;
					switch (source.getOpCode()) {
					case LOG:
						mathOp3 = OpOp2.LOG;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
		// Construct Hop for current builtin function expression based on its type
		Hop currBuiltinOp = null;
		switch (source.getOpCode()) {
			case TRANSFORMENCODE:
				ArrayList<Hop> inputs = new ArrayList<>();
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesMM.java`
#### Snippet
```java
		String format = st.nextToken();
		MMFormat fmt = null;
		switch( format ) {
			//case "array": fmt = MMFormat.ARRAY; break;
			case "coordinate": fmt = MMFormat.COORDINATE; break;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/DoubleArray.java`
#### Snippet
```java
			switch(state) {
				case FP32:
					switch(c) {
						case FP64:
							state = c;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/frame/data/columns/StringArray.java`
#### Snippet
```java
		switch(state) {
			case FP32:
				switch(c) {
					case FP64:
						return c;
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		}
		case OP_MULTIPLY : {
			switch (reductionDirection) {
			case REDUCTION_ALL: {
				double result = reduceAll(gCtx, instName, "reduce_prod", in, size);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		}
		case OP_MAXINDEX : {
			switch(reductionDirection) {
			case REDUCTION_COL:
				throw new DMLRuntimeException("Internal Error - Column maxindex of matrix not implemented yet for GPU ");
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCUDA.java`
#### Snippet
```java
		}
		case OP_MININDEX : {
			switch(reductionDirection) {
			case REDUCTION_COL:
				throw new DMLRuntimeException("Internal Error - Column minindex of matrix not implemented yet for GPU ");
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/cost/CostEstimatorBuilder.java`
#### Snippet
```java
				AggUnaryOp agop = (AggUnaryOp) o.getHop();

				switch(agop.getDirection()) {
					case Row:
						counter.incScans(count);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java

		BinaryAccessType atype = LibMatrixBincell.getBinaryAccessType(m1, m2);
		switch(atype) {
			case MATRIX_ROW_VECTOR:
				// Verify if it is okay to include all OuterVectorVector ops here.
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java

	public static AInsertionSorter create(int endLength, int numRows, IntArrayList[] offsets, SORT_TYPE st) {
		switch(st) {
			case MERGE:
				return new MergeSort(endLength, numRows, offsets);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/insertionsort/InsertionSorterFactory.java`
#### Snippet
```java
	public static AInsertionSorter createNegative(int endLength, int numRows, IntArrayList[] offsets, int negativeIndex,
		SORT_TYPE st) {
		switch(st) {
			case MERGE:
				return new MergeSort(endLength, numRows, offsets, negativeIndex);
```

### SwitchStatementWithTooFewBranches
'switch' statement has too few case labels (1), and should probably be replaced with an 'if' statement
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptTreeConverter.java`
#### Snippet
```java
		
		//exec type
		switch( inst.getType() )
		{
			case CONTROL_PROGRAM:
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	 * @param colnames column names
	 * @param group attribute name in json class
	 * @return list of column ids
	 * @throws JSONException if JSONException occurs
	 */
```

### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	 * @param minCol start of columns to ignore (1-based, inclusive, if -1 not used)
	 * @param maxCol end of columns to ignore (1-based, exclusive, if -1 not used)
	 * @return list of column ids
	 * @throws JSONException if JSONException occurs
	 */
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `src/main/java/org/apache/sysds/runtime/transform/encode/LegacyEncoder.java`
#### Snippet
```java
	 *
	 * @param colID column ID
	 * @return true if encoder is applicable for given column
	 */
	public int isApplicable(int colID) {
```

## RuleId[id=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
				sb.append(info.version());
			}
		} catch (MLContextException e) {
		}
		return sb.toString();
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
			schema.fieldIndex(RDDConverterUtils.DF_ID_COLUMN);
			hasID = true;
		} catch (IllegalArgumentException iae) {
		}

```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextConversionUtil.java`
#### Snippet
```java
			schema.fieldIndex(RDDConverterUtils.DF_ID_COLUMN);
			hasID = true;
		} catch (IllegalArgumentException iae) {
		}

```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				return (int)Math.ceil(((double)(to-from+1))/increment);
		}
		catch(Exception ex){}
		return defaultValue;
	}
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/hops/rewrite/HopDagValidator.java`
#### Snippet
```java
				LOG.error("Invalid HOP DAG after rewrite " + rule.getClass().getName() 
						+ ": \n" + Explain.explainHops(roots), ex);
			}catch(DMLRuntimeException e){}
			throw ex;
		}
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/hops/rewrite/HopDagValidator.java`
#### Snippet
```java
				LOG.error("Invalid HOP DAG after rewrite " + rule.getClass().getName() 
						+ ": \n" + Explain.explain(root), ex);
			}catch(DMLRuntimeException e){}
			throw ex;
		}
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/runtime/util/DataConverter.java`
#### Snippet
```java
			mb.init( data, rows, cols );
		}
		catch (Exception e){} //can never happen

		//check and convert internal representation
```

### CatchMayIgnoreException
Unexpected VM exception like 'java.lang.NullPointerException' might be ignored in a `catch` block
in `src/main/java/org/apache/sysds/runtime/util/UtilFunctions.java`
#### Snippet
```java
			date = curr.parse(dateString);
		}
		catch(Exception e) { e.getMessage();}

		Date newDate;
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/ByteBuffer.java`
#### Snippet
```java
		//robust checking until serialized
		while( !_serialized ) {
			try{Thread.sleep(1);} catch(Exception e) {}
		}
	}
```

### CatchMayIgnoreException
Empty `catch` block
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederationUtils.java`
#### Snippet
```java
			return nnz;
		}
		catch(Exception ex) { }
		return -1;
	}
```

## RuleId[id=CollectionAddAllCanBeReplacedWithConstructor]
### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `src/main/java/org/apache/sysds/lops/SortKeys.java`
#### Snippet
```java
		
		HashSet<Lop> set1 = new HashSet<>();
		set1.addAll(input1.getOutputs());
		// find intersection of input1.getOutputs() and input2.getOutputs();
		set1.retainAll(input2.getOutputs());
```

### CollectionAddAllCanBeReplacedWithConstructor
'putAll()' call can be replaced with parametrized constructor call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			//set all defaults
			params = new HashMap<>();
			params.putAll( _paramDefaults );
			predicate.setParForParams(params);
		}
```

### CollectionAddAllCanBeReplacedWithConstructor
'addAll()' call can be replaced with parametrized constructor call
in `src/main/java/org/apache/sysds/runtime/transform/meta/TfMetaUtils.java`
#### Snippet
```java
	public static void checkValidEncoders(JSONObject jSpec) {
		Set<String> validEncoders = new HashSet<>();
		validEncoders.addAll(Arrays.asList("ids","K"));
		for( TfMethod tf : TfMethod.values() )
			validEncoders.add(tf.toString());
```

## RuleId[id=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/java/Ternary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid ternary type: "+this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/java/Binary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid binary type: "+this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/java/Unary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid unary type: "+this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
		}
		sb.append("\n" + ANSI_RESET);
		System.out.println(sb.toString());
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/AggUnaryOp.java`
#### Snippet
```java
				}
			}
			else throw new HopsException("ExecType " + et + " not recognized in " + this.toString() );
		} 
		catch (Exception e) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/SpoofFusedOp.java`
#### Snippet
```java
			default:
				throw new RuntimeException("Failed to refresh size information "
					+ "for type: "+_dimsType.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/SpoofFusedOp.java`
#### Snippet
```java
				default:
					throw new RuntimeException("Failed to infer worst-case size information "
							+ "for type: "+_dimsType.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeUnary.java`
#### Snippet
```java
				break;
			default:
				throw new RuntimeException("Operation " + _type.toString() + " has no "
					+ "output dimensions, dimensions needs to be specified for the CNode " );
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/opt/ReachabilityGraph.java`
#### Snippet
```java
				inputs.append(in._ID);
			}
			return _ID+" ("+inputs.toString()+") "+((_p!=null)?_p:"null");
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeNary.java`
#### Snippet
```java
				}
				default:
					throw new RuntimeException("Invalid nary type: "+this.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeRow.java`
#### Snippet
```java
			case COL_AGG_CONST: return SpoofOutputDimsType.VECT_CONST2;
			default:
				throw new RuntimeException("Unsupported row type: "+_type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
			case FULL_AGG: return SpoofOutputDimsType.SCALAR;
			default:
				throw new RuntimeException("Unsupported cell type: "+_type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeOuterProduct.java`
#### Snippet
```java
				return SpoofOutputDimsType.SCALAR;
			default:
				throw new RuntimeException("Unsupported outer product type: "+_type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/cuda/Ternary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid ternary type: "+this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/cuda/Binary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid binary type: " + this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/cuda/Unary.java`
#### Snippet
```java

			default:
				throw new RuntimeException("Invalid unary type: "+this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNode.java`
#### Snippet
```java
				else return null;
			default:
				throw new RuntimeException("API not supported by code generator: " + api.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNode.java`
#### Snippet
```java
				else return null;
			default:
				throw new RuntimeException("API not supported by code generator: " + api.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/lops/Unary.java`
#### Snippet
```java
		//sanity check
		if ( (op == OpOp1.INVERSE || op == OpOp1.CHOLESKY) && et == ExecType.SPARK )
			throw new LopsException("Invalid exection type "+et.toString()+" for operation "+op.toString());
		
		operation = op;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/lops/Unary.java`
#### Snippet
```java
		//sanity check
		if ( (op == OpOp1.INVERSE || op == OpOp1.CHOLESKY) && et == ExecType.SPARK )
			throw new LopsException("Invalid exection type "+et.toString()+" for operation "+op.toString());
		
		operation = op;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/InstallDependencyForIntegrationTests.java`
#### Snippet
```java
			StringBuilder errorMessage = new StringBuilder();
			errorMessage.append("failed to run script " + executionFile);
			errorMessage.append("\nexception: " + e.toString());
			errorMessage.append("\nmessage: " + e.getMessage());
			errorMessage.append("\nstack trace:");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java
		String rightString;
		if (_left instanceof StringIdentifier) {
			leftString = "\"" + _left.toString() + "\"";
		} else {
			leftString = _left.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/RelationalExpression.java`
#### Snippet
```java
		}
		if (_right instanceof StringIdentifier) {
			rightString = "\"" + _right.toString() + "\"";
		} else {
			rightString = _right.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/PrintStatement.java`
#### Snippet
```java
			if (expression instanceof StringIdentifier) {
				sb.append("\"");
				sb.append(expression.toString());
				sb.append("\"");
			} else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/PrintStatement.java`
#### Snippet
```java
				if (expression instanceof StringIdentifier) {
					sb.append("\"");
					sb.append(expression.toString());
					sb.append("\"");
				} else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
				insts.add(inst);
				if( LOG.isTraceEnabled() )
					LOG.trace("  Adding " + inst.toString());
			}
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/lops/compile/Dag.java`
#### Snippet
```java
			Instruction rmInst = VariableCPInstruction.prepareRemoveInstruction(var);
			if( LOG.isTraceEnabled() )
				LOG.trace("  Adding var_deletions: " + rmInst.toString());
			
			rmInst.setLocation(var_deletionsLineNum.get(var));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BooleanExpression.java`
#### Snippet
```java
	public String toString(){
		if (_opcode == BooleanOp.NOT) {
			return "(" + _opcode.toString() + " " + _left.toString() + ")";
		} else {
			return "(" + _left.toString() + " " + _opcode.toString() + " " + _right.toString() + ")";
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			childs.append(")");
			if( childAdded )
				sb.append(childs.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		//operation string
		sb.append(cnode.toString());

		//input hop references 
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
			childs.append(")");
			if( childAdded )
				sb.append(childs.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java

		sb.append(offset);
		sb.append(li.toString());
		sb.append('\n');

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/utils/Explain.java`
#### Snippet
```java
				|| tmpItem.getInputs().length <= tmpPos.intValue() ) {
				sb.append(createOffset(level));
				sb.append(tmpItem.toString());
				sb.append('\n');
				stackItem.pop(); stackPos.pop();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/OutputStatement.java`
#### Snippet
```java
			if (exp instanceof StringIdentifier) {
				sb.append("\"");
				sb.append(exp.toString());
				sb.append("\"");
			} else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
		String rightString;
		if (_left instanceof StringIdentifier) {
			leftString = "\"" + _left.toString() + "\"";
		} else {
			leftString = _left.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BinaryExpression.java`
#### Snippet
```java
		}
		if (_right instanceof StringIdentifier) {
			rightString = "\"" + _right.toString() + "\"";
		} else {
			rightString = _right.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/AssignmentStatement.java`
#### Snippet
```java
		if (_source instanceof StringIdentifier) {
			sb.append("\"");
			sb.append(_source.toString());
			sb.append("\"");
		} else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IterablePredicate.java`
#### Snippet
```java
		if (_incrementExpr != null) {
			sb.append(",");
			sb.append(_incrementExpr.toString());
		}
		sb.append( ")" );
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IterablePredicate.java`
#### Snippet
```java
			ident.getValueType() == ValueType.STRING)) )
		{
			throw new LanguageException(this.printErrorLocation() + "expression in iterable predicate in for loop '" + expr.toString() + "' must return a numeric scalar");
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					if (targetAsSeen == null){
						raiseValidateError(target.printErrorLocation() + "cannot assign value to indexed identifier " 
							+ target.toString() + " without first initializing " + target.getName(), conditional);
					}
					target.setProperties(targetAsSeen);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					FunctionStatementBlock fblock2 = prog.getFunctionStatementBlock(fcall.getNamespace(), fcall.getName());
					ret &= rIsInlineableFunction(fblock2, prog);
					if( as.getSource().toString().contains(DataExpression.FORMAT_TYPE + "=" + FileFormat.CSV.toString())
						&& as.getSource().toString().contains("read"))
						return false;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
					}
					if( !paramsOkay ) {
						raiseValidateError("Invalid parameters in write statement: " + os.toString(), conditional);
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				AssignmentStatement astmt = (AssignmentStatement)stmt;
				// for now, ensure that an assignment statement containing a read from csv ends up in own statement block
				if(astmt.getSource().toString().contains(DataExpression.FORMAT_TYPE + "=" + FileFormat.CSV.toString()) 
					&& astmt.getSource().toString().contains("read"))
					return false;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			DataIdentifier targetAsSeen = ids.getVariable(target.getName());
			if (targetAsSeen == null){
				target.raiseValidateError("cannot assign value to indexed identifier " + target.toString() + " without first initializing " + target.getName(), conditional);
			}
			target.setProperties(targetAsSeen);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			if( target.getDataType().isMatrixOrFrame() ) {
				if (targetSize._row >= 1 && source.getOutput().getDim1() > 1 && targetSize._row != source.getOutput().getDim1()){
					target.raiseValidateError("Dimension mismatch. Indexed expression " + target.toString() + " can only be assigned matrix with dimensions "
							+ targetSize._row + " rows and " + targetSize._col + " cols. Attempted to assign matrix with dimensions "
							+ source.getOutput().getDim1() + " rows and " + source.getOutput().getDim2() + " cols ", conditional);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
				
				if (targetSize._col >= 1 && source.getOutput().getDim2() > 1 && targetSize._col != source.getOutput().getDim2()){
					target.raiseValidateError("Dimension mismatch. Indexed expression " + target.toString() + " can only be assigned matrix with dimensions "
							+ targetSize._row + " rows and " + targetSize._col + " cols. Attempted to assign matrix with dimensions "
							+ source.getOutput().getDim1() + " rows and " + source.getOutput().getDim2() + " cols ", conditional);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
			sb.append("\n");
		}
		if (_liveOut != null) sb.append("liveout " + _liveOut.toString() + "\n");
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
		}
		if (_liveOut != null) sb.append("liveout " + _liveOut.toString() + "\n");
		if (_liveIn!= null) sb.append("livein " + _liveIn.toString()+ "\n");
		if (_gen != null && !_gen.getVariables().isEmpty()) sb.append("gen " + _gen.toString()+ "\n");
		if (_kill != null && !_kill.getVariables().isEmpty()) sb.append("kill " + _kill.toString()+ "\n");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
		}
		else if( !(margin instanceof DataIdentifier) && !margin.toString().equals("rows") && !margin.toString().equals("cols") ){
			raiseValidateError("Named parameter 'margin' has an invalid value '"+margin.toString()+"'. Please specify 'rows' or 'cols'.", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		}
		else if (_rowLowerBound instanceof ConstIdentifier && !getDataType().isList() ) {
			raiseValidateError("assign lower-bound row index for Indexed Identifier " + this.toString() + " the non-numeric value " + _rowLowerBound.toString(), conditional);
		}
	
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				
				if (!(constValue instanceof IntIdentifier || constValue instanceof DoubleIdentifier ))
					LOG.info(this.printInfoLocation() + "attempted to assign lower row bound for " + this.toString() + "the non-numeric value " + constValue.getOutput().toString() + " assigned to " + identifierName + ". May cause runtime exception ");
	
				else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		}	
		else if (_rowUpperBound instanceof ConstIdentifier && !getDataType().isList()){
			raiseValidateError("assign upper-bound row index for " + this.toString() + " the non-numeric value " + _rowUpperBound.toString(), conditional);
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				
				if (!(constValue instanceof IntIdentifier || constValue instanceof DoubleIdentifier ))
					LOG.info(this.printInfoLocation() + "attempted to assign upper row bound for " + this.toString() + "the non-numeric value " + constValue.getOutput().toString() + " assigned to " + identifierName + ". May cause runtime exception ");
	
				else {						
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
			
		else if (_colLowerBound instanceof ConstIdentifier && !getDataType().isList()) {
			raiseValidateError("assign lower-bound column index for Indexed Identifier " + this.toString() + " the non-numeric value " + _colLowerBound.toString(), conditional);
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				
				if (!(constValue instanceof IntIdentifier || constValue instanceof DoubleIdentifier ))
					LOG.info(this.printInfoLocation() + "attempted to assign lower column bound for " + this.toString() + "the non-numeric value " + constValue.getOutput().toString() + " assigned to " + identifierName + ". May cause runtime exception ");
	
				else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
		}	
		else if (_colUpperBound instanceof ConstIdentifier && !getDataType().isList()){	
			raiseValidateError("assign upper-bound column index for " + this.toString() + " the non-numeric value " + _colUpperBound.toString(), conditional);
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/IndexedIdentifier.java`
#### Snippet
```java
				if (!(constValue instanceof IntIdentifier || constValue instanceof DoubleIdentifier ))
					LOG.info(this.printInfoLocation() + "attempted to assign upper column bound for " 
							+ this.toString() + "the non-numeric value " + constValue.getOutput().toString() 
							+ " assigned to " + identifierName + ". May cause runtime exception ");
	
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				{
					raiseValidateError("Invalid parameters in read statement of a scalar: " +
						toString() + ". Only " + VALUETYPEPARAM + " is allowed.", conditional, LanguageErrorCodes.INVALID_PARAMETERS);
				}
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							String msg = "Only parameters allowed are: " + READ_VALID_PARAM_NAMES;
							raiseValidateError("Invalid parameter " + key + " in read statement: " +
								toString() + ". " + msg, conditional, LanguageErrorCodes.INVALID_PARAMETERS);
						}
					}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							
							raiseValidateError("Invalid parameter " + key + " in read statement: " +
									toString() + ". " + msg, conditional, LanguageErrorCodes.INVALID_PARAMETERS);
						}
					}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				}
				catch(Exception ex) {
					raiseValidateError("Invalid format '" + fmt+ "' in statement: " + toString(), conditional);
				}
				
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
				// because we automatically introduce reblocks if blocksizes don't match
				if ( (getOutput().getFileFormat().isTextFormat() || !isMatrix)  && getOutput().getBlocksize() != -1 ){
					raiseValidateError("Invalid block dimensions (" + getOutput().getBlocksize() + ") when format=" + getVarParam(FORMAT_TYPE) + " in \"" + this.toString() + "\".", conditional);
				}
			
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
			else if( getVarParam(FORMAT_TYPE) instanceof StringIdentifier ) //literal format
				raiseValidateError("Invalid format " + getVarParam(FORMAT_TYPE)
					+ " in statement: " + toString(), conditional);
			break;

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					if( ((DoubleIdentifier) rowsExpr).getValue() < 0 ) {
						raiseValidateError("In rand statement, can only assign rows a long " +
								"(integer) value >= 0 -- attempted to assign value: " + rowsExpr.toString(), conditional);
					}
					rowsLong = UtilFunctions.toLong(Math.floor(((DoubleIdentifier) rowsExpr).getValue()));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if( ((IntIdentifier) constValue).getValue() < 0 ) {
								raiseValidateError("In rand statement, can only assign rows a long " +
										"(integer) value >= 0 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if( ((DoubleIdentifier) constValue).getValue() < 0 ) {
								raiseValidateError("In rand statement, can only assign rows a long " +
										"(double) value >= 0 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					if( ((IntIdentifier) colsExpr).getValue() < 0 ) {
						raiseValidateError("In rand statement, can only assign cols a long " +
								"(integer) value >= 0 -- attempted to assign value: " + colsExpr.toString(), conditional);
					}
					colsLong = ((IntIdentifier) colsExpr).getValue();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					if( ((DoubleIdentifier) colsExpr).getValue() < 0 ) {
						raiseValidateError("In rand statement, can only assign cols a long " +
								"(integer) value >= 0 -- attempted to assign value: " + colsExpr.toString(), conditional);
					}
					colsLong = Double.valueOf((Math.floor(((DoubleIdentifier) colsExpr).getValue()))).longValue();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if( ((IntIdentifier) constValue).getValue() < 0 ) {
								raiseValidateError("In rand statement, can only assign cols a long " +
										"(integer) value >= 0 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if( ((DoubleIdentifier) constValue).getValue() < 0 ) {
								raiseValidateError("In rand statement, can only assign cols a long " +
										"(double) value >= 0 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else {
						raiseValidateError("In matrix statement, can only assign rows a long " +
								"(integer) value >= 1 -- attempted to assign value: " + rowsExpr.toString(), conditional);
					}		
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if (((IntIdentifier)constValue).getValue() < 1){
								raiseValidateError("In matrix statement, can only assign rows a long " +
										"(integer) value >= 1 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier 
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if (((DoubleIdentifier)constValue).getValue() < 1.0){
								raiseValidateError("In matrix statement, can only assign rows a long " +
										"(integer) value >= 1 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else {
						raiseValidateError("In matrix statement, can only assign cols a long " +
								"(integer) value >= 1 -- attempted to assign value: " + colsExpr.toString(), conditional);
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else {
						raiseValidateError("In matrix statement, can only assign rows a long " +
								"(integer) value >= 1 -- attempted to assign value: " + colsExpr.toString(), conditional);
					}
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
								raiseValidateError("In matrix statement, can only assign cols a long " +
										"(integer) value >= 1 -- attempted to assign value: " 
										+ constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier 
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
								raiseValidateError("In matrix statement, can only assign cols a long " +
										"(integer) value >= 1 -- attempted to assign value: " 
										+ constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else
						raiseValidateError("In frame statement, can only assign rows a long " +
							"(integer) value >= 1 -- attempted to assign value: " + rowsExpr.toString(), conditional);
				}
				else if (rowsExpr instanceof DataIdentifier && !(rowsExpr instanceof IndexedIdentifier)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if (((IntIdentifier)constValue).getValue() < 1){
								raiseValidateError("In frame statement, can only assign rows a long " +
									"(integer) value >= 1 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
							if (((DoubleIdentifier)constValue).getValue() < 1.0){
								raiseValidateError("In frame statement, can only assign rows a long " +
									"(integer) value >= 1 -- attempted to assign value: " + constValue.toString(), conditional);
							}
							// update row expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else
						raiseValidateError("In frame statement, can only assign cols a long " +
							"(integer) value >= 1 -- attempted to assign value: " + colsExpr.toString(), conditional);
				}
				else if (colsExpr instanceof DoubleIdentifier) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
					else
						raiseValidateError("In frame statement, can only assign rows a long " +
							"(integer) value >= 1 -- attempted to assign value: " + colsExpr.toString(), conditional);
				}
				else if (colsExpr instanceof DataIdentifier && !(colsExpr instanceof IndexedIdentifier)) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
								raiseValidateError("In frame statement, can only assign cols a long " +
									"(integer) value >= 1 -- attempted to assign value: "
									+ constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
								raiseValidateError("In frame statement, can only assign cols a long " +
									"(integer) value >= 1 -- attempted to assign value: "
									+ constValue.toString(), conditional);
							}
							// update col expr with new IntIdentifier (rounded down)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		if( f1 == null || f1._b.length != f1._vars.length ) {
			if( LOG.isTraceEnabled() && f1!=null ) 
				LOG.trace("PARFOR: f1: "+f1.toString());
			throw new LanguageException("PARFOR loop dependency analysis: " +
				"MATRIX subscripts are not in linear form (a0 + a1*x).");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
			return true; //dependency 
		
		LOG.trace("PARFOR: f1: "+f1.toString());
		
		// no output dependency to itself if no index access will happen twice
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		ret = f1.equals(f2);
		
		LOG.trace("PARFOR: f1: " + f1.toString());
		LOG.trace("PARFOR: f2: " + f2.toString());
		LOG.trace("PARFOR: (f1==f2): " + ret);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				ret = f1p.equals(f2p);
				
				LOG.trace("PARFOR: f1p: " + f1p.toString());
				LOG.trace("PARFOR: f2p: " + f2p.toString());
				LOG.trace("PARFOR: (f1p==f2p): " + ret);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				
				LOG.trace("PARFOR: f1p: " + f1p.toString());
				LOG.trace("PARFOR: f2p: " + f2p.toString());
				LOG.trace("PARFOR: (f1p==f2p): " + ret);
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				raiseValidateError("PARFOR loop dependency analysis: " +
					"inter-iteration (loop-carried) dependencies detected for variable(s): " +
					depVars.toString() +". \n " +
					"Please, ensure independence of iterations.", false);
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		forceConsistency(f1,f2);
		
		LOG.trace("PARFOR: f1: " + f1.toString());
		LOG.trace("PARFOR: f2: " + f2.toString());
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
								+ "Error invoking diag on matrix with dimensions ("
								+ id.getDim1() + "," + id.getDim2()
								+ ") in " + this.toString(), conditional, LanguageErrorCodes.INVALID_PARAMETERS);
					}
					//diag M2V
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				if ( getThirdExpr().getOutput().getDataType() != DataType.SCALAR || _args[3].getOutput().getDataType() != DataType.SCALAR ) {
					raiseValidateError("Invalid argument types to table(): output dimensions must be of type scalar: " 
							+ this.toString(), conditional, LanguageErrorCodes.INVALID_PARAMETERS);
				}
				else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
				if ( _args[3].getOutput().getDataType() != DataType.SCALAR || _args[4].getOutput().getDataType() != DataType.SCALAR ) {
					raiseValidateError("Invalid argument types to table(): output dimensions must be of type scalar: " 
							+ this.toString(), conditional, LanguageErrorCodes.INVALID_PARAMETERS);
				}
				else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java
			default:
				raiseValidateError("Invalid number of arguments to table(): " 
						+ this.toString(), conditional, LanguageErrorCodes.INVALID_PARAMETERS);
			}
			// The dimensions for the output matrix will be known only at the
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarket.java`
#### Snippet
```java
			}
		} else {
			throw new IOException(src.toString() + ": No such file or directory");
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterFactory.java`
#### Snippet
```java
				return new FrameWriterProto();
			default:
				throw new DMLRuntimeException("Failed to create frame writer for unknown format: " + fmt.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderFactory.java`
#### Snippet
```java
				return new FrameReaderProto();
			default:
				throw new DMLRuntimeException("Failed to create frame reader for unknown format: " + fmt.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/MatrixWriterFactory.java`
#### Snippet
```java

			default:
				throw new DMLRuntimeException("Failed to create matrix writer for unknown format: " + fmt.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/InputOutputInfo.java`
#### Snippet
```java
		}
		throw new DMLRuntimeException(
			"Could not obtain input/output info for format: " + dt.toString() + ", " + fmt.toString());
	}
}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java

		if(LOG.isDebugEnabled())
			LOG.debug("FileFormatPropertiesLIBSVM: " + this.toString());
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
		this.indexDelim = indexDelim;
		if(LOG.isDebugEnabled())
			LOG.debug("FileFormatPropertiesLIBSVM full settings: " + this.toString());
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesLIBSVM.java`
#### Snippet
```java
		this.sparse = sparse;
		if(LOG.isDebugEnabled())
			LOG.debug("FileFormatPropertiesLIBSVM full settings: " + this.toString());
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		}
		if(LOG.isDebugEnabled())
			LOG.debug("FileFormatPropertiesCSV full settings: " + this.toString());
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java

		if(LOG.isDebugEnabled())
			LOG.debug("FileFormatPropertiesCSV: " + this.toString());
		
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesCSV.java`
#### Snippet
```java
		this.sparse = sparse;
		if(LOG.isDebugEnabled()){
			LOG.debug("FileFormatPropertiesCSV medium settings: " + this.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/MatrixReaderFactory.java`
#### Snippet
```java
				break;
			default:
				throw new DMLRuntimeException("Failed to create matrix reader for unknown format: " + fmt.toString());
		}
		return reader;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/MatrixReaderFactory.java`
#### Snippet
```java
			
			default:
				throw new DMLRuntimeException("Failed to create matrix reader for unknown format: " + fmt.toString());
		}
		return reader;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java

		if (left == null || right == null) {
			throw new ParseException("Missing input in binary expressions (" + source.toString()+"): "
				+ ((left==null)?source.getLeft():source.getRight())+", line="+source.getBeginLine());
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
			} else if (predicate instanceof IntIdentifier || predicate instanceof DoubleIdentifier) {
				cp.setPredicate(new BooleanIdentifier(true, predicate));
				LOG.warn(predicate.printWarningLocation() + "Numerical value '" + predicate.toString()
						+ "' (!= 0/1) is converted to boolean TRUE by DML");
			} else if (predicate instanceof StringIdentifier) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/parser/DMLTranslator.java`
#### Snippet
```java
						+ "' (!= 0/1) is converted to boolean TRUE by DML");
			} else if (predicate instanceof StringIdentifier) {
				throw new ParseException(predicate.printErrorLocation() + "String value '" + predicate.toString()
						+ "' is not allowed for iterable predicate");
			}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/FileFormatPropertiesMM.java`
#### Snippet
```java
		if( _field == MMField.PATTERN && (_fmt == MMFormat.ARRAY || _symmetry == MMSymmetry.SKEW_SYMMETRIC) ) {
			throw new DMLRuntimeException("MatrixMarket: Invalid combination: "
				+ _fmt.toString() + " " + _field.toString() + " " + _symmetry.toString() +".");
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCSV.java`
#### Snippet
```java
			}
		} else {
			throw new IOException(srcFilePath.toString() + ": No such file or directory");
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
			case COO: return new SparseBlockCOO(sblock);
			default:
				throw new RuntimeException("Unexpected sparse block type: "+type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
			case COO: return SparseBlockCOO.estimateSizeInMemory(nrows, ncols, sparsity);
			default:
				throw new RuntimeException("Unexpected sparse block type: "+type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/data/SparseBlockFactory.java`
#### Snippet
```java
			case COO: return new SparseBlockCOO(rlen);
			default:
				throw new RuntimeException("Unexpected sparse block type: "+type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
			if (varParams.get(key.toString()) != null && (varParams.get(key.toString()) instanceof ConstIdentifier)
				&& !varParams.get(key.toString()).toString().equalsIgnoreCase(val.toString())) {
				raiseValidateError("Parameter '" + key.toString()
					+ "' has conflicting values in metadata and read statement. MTD file value: '"
					+ val.toString() + "'. Read statement value: '" + varParams.get(key.toString()) + "'.", conditional);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
				raiseValidateError("Parameter '" + key.toString()
					+ "' has conflicting values in metadata and read statement. MTD file value: '"
					+ val.toString() + "'. Read statement value: '" + varParams.get(key.toString()) + "'.", conditional);
			} else {
				// if the read method does not specify parameter value, then add MTD metadata file value to parameter list
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			}
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		}
		finally {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			}
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			}
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		} 
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
					zero(), dstTensorDesc, dstData);
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			}
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		} 
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixCuDNN.java`
#### Snippet
```java
			}
		} catch (CudaException e) {
			throw new DMLRuntimeException("Error in conv2d in GPUContext " + gCtx.toString() + " from Thread " + Thread.currentThread().toString(), e);
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/codegen/CodegenUtils.java`
#### Snippet
```java
				for(Diagnostic<? extends JavaFileObject> tmp : diagnostics.getDiagnostics())
					if( tmp.getKind()==Kind.ERROR )
						System.err.println("ERROR: "+tmp.toString());
				if( success == null || !success )
					throw new RuntimeException("Failed to compile class "+name);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		catch(DMLRuntimeException ex)
		{
			throw new IOException("Error reading block of type '"+format.toString()+"'.", ex);
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		if( sparse && sparseBlock != null ) {
			//overloaded implementation in sparse blocks
			sb.append(sparseBlock.toString());
		}
		else if( !sparse && denseBlock!=null ) {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
		else if( !sparse && denseBlock!=null ) {
			//overloaded implementation in dense blocks
			sb.append(denseBlock.toString());
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static void createRowwiseIndexes(MatrixIndexes first, MatrixIndexes last, long ncblks, HashSet<MatrixIndexes> ret) {
		if( first.getRowIndex()<=0 || first.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed first index: "+first.toString());
		if( last.getRowIndex()<=0 || last.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed last index: "+last.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			throw new RuntimeException("Invalid computed first index: "+first.toString());
		if( last.getRowIndex()<=0 || last.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed last index: "+last.toString());
		
		//add first row block
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
	private static void createColwiseIndexes(MatrixIndexes first, MatrixIndexes last, long nrblks, HashSet<MatrixIndexes> ret) {
		if( first.getRowIndex()<=0 || first.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed first index: "+first.toString());
		if( last.getRowIndex()<=0 || last.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed last index: "+last.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixReorg.java`
#### Snippet
```java
			throw new RuntimeException("Invalid computed first index: "+first.toString());
		if( last.getRowIndex()<=0 || last.getColumnIndex()<=0 )
			throw new RuntimeException("Invalid computed last index: "+last.toString());
		
		//add first row block
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/lineage/LineageItemUtils.java`
#### Snippet
```java
		long id = useObjIdent ? System.identityHashCode(current) : current.getId();
		if( probe.contains(id) )
			throw new DMLRuntimeException("Cycle detected for "+current.toString());
		probe.add(id);
		if( current.getInputs() != null )
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/cocode/CoCoderFactory.java`
#### Snippet
```java
				return new CoCodePriorityQue(est, costEstimator, cs);
			default:
				throw new RuntimeException("Unsupported column group partitioner: " + type.toString());
		}
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupUncompressed.java`
#### Snippet
```java
		sb.append("\n");
		if(_data.getNumRows() < 1000)
			sb.append(_data.toString());
		else
			sb.append(" don't print uncompressed matrix because it is to big.");
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/offset/AOffset.java`
#### Snippet
```java
				throw new DMLCompressionException(
					"Invalid iteration of offset when making string, the last offset is not equal to a iteration: "
						+ getOffsetToLast() + " String: " + sb.toString());
		}
		return sb.toString();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/workload/Op.java`
#### Snippet
```java
	@Override
	public String toString() {
		return _op.getHopID() + " " + _op.toString() + " CompressedOutput: " + isCompressedOutput()
			+ " IsDecompressing: " + isDecompressing();
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	public String toString() {
		if(_data.isInSparseFormat() || _data.getNumColumns() > 1)
			return "MatrixBlock Dictionary :\n" + _data.toString();
		else
			return "MatrixBlock Dictionary : " + Arrays.toString(_data.getDenseBlockValues());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/dictionary/MatrixBlockDictionary.java`
#### Snippet
```java
	public String getString(int colIndexes) {
		if(_data.isInSparseFormat() || _data.getNumColumns() > 1)
			return "\n" + _data.toString();
		else
			return Arrays.toString(_data.getDenseBlockValues());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FrameIndexingCPInstruction.java`
#### Snippet
```java
			else { //FRAME<-SCALAR 
				if(!ixrange.isScalar())
					throw new DMLRuntimeException("Invalid index range of scalar leftindexing: "+ixrange.toString()+"." );
				ScalarObject scalar = ec.getScalarInput(input2);
				out = new FrameBlock(lin);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FunctionCallCPInstruction.java`
#### Snippet
```java
	public void processInstruction(ExecutionContext ec) {
		if( LOG.isTraceEnabled() ){
			LOG.trace("Executing instruction : " + toString());
		}
		// get the function program block (stored in the Program object)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/FunctionCallCPInstruction.java`
#### Snippet
```java
	@Override
	public void printMe() {
		LOG.debug("ExternalBuiltInFunction: " + this.toString());
	}
	
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ParameterizedBuiltinCPInstruction.java`
#### Snippet
```java
					sb.append(data.getDim(i)).append("x");
			});
			LOG.warn("Truncating " + data.getClass().getSimpleName() + " of size " + sb.toString() + " to " + rows + "x"
				+ cols + ". " + "Use toString(X, rows=..., cols=...) if necessary.");
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/MatrixIndexingCPInstruction.java`
#### Snippet
```java
			else { //MATRIX<-SCALAR 
				if(!ixrange.isScalar())
					throw new DMLRuntimeException("Invalid index range of scalar leftindexing: "+ixrange.toString()+"." );
				ScalarObject scalar = ec.getScalarInput(input2.getName(), ValueType.FP64, input2.isLiteral());
				resultBlock = matBlock.leftIndexingOperations(scalar, 
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CtableCPInstruction.java`
#### Snippet
```java
			
			default:
				throw new DMLRuntimeException("Encountered an invalid ctable operation ("+ctableOp+") while executing instruction: " + this.toString());
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CM_COV_Object.java`
#### Snippet
```java
				case CM4:
				case VARIANCE:
					throw new DMLRuntimeException("Aggregation operator '"+agg.toString()+"' does not apply to partial aggregation.");
				default:
					throw new DMLRuntimeException("Invalid aggreagte in CM_CV_Object: " + agg);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java
			// example instruction: mvvar <srcVar> <destFile> <format>
			if ( ec.getVariable(getInput1().getName()) == null )
				throw new DMLRuntimeException("Unexpected error: could not find a data object for variable name:" + getInput1().getName() + ", while processing instruction " +this.toString());

			Data object = ec.getVariable(getInput1().getName());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/cp/VariableCPInstruction.java`
#### Snippet
```java

		if ( dd == null )
			throw new DMLRuntimeException("Unexpected error: could not find a data object for variable name:" + getInput1().getName() + ", while processing instruction " +this.toString());

		// remove existing variable bound to target name
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/IndexingFEDInstruction.java`
#### Snippet
```java
		if(input2.getDataType() == DataType.SCALAR) {
			if(!ixrange.isScalar())
				throw new DMLRuntimeException("Invalid index range for leftindexing with scalar: " + ixrange.toString() + ".");

			scalar = ec.getScalarInput(input2);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
	private void processFederatedOutput(FederationMap map, MatrixObject in, ExecutionContext ec){
		if ( output.isScalar() )
			throw new DMLRuntimeException("Output of FED instruction, " + output.toString()
				+ ", is a scalar and the output is set to be federated. Scalars cannot be federated. ");
		FederatedRequest fr1 = FederationUtils.callInstruction(instString, output,
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/fed/AggregateUnaryFEDInstruction.java`
#### Snippet
```java
	private void processVar(ExecutionContext ec){
		if ( _fedOut.isForcedFederated() ){
			throw new DMLRuntimeException("Output of " + toString() + " should not be federated "
				+ "since the instruction requires consolidation of partial results to be computed.");
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			if(printDebugMessage != null && (DMLScript.PRINT_GPU_MEMORY_INFO || LOG.isTraceEnabled()) )  {
				LOG.info("Failed: " + printDebugMessage + ":" + byteCountToDisplaySize(size));
				LOG.info("GPU Memory info " + printDebugMessage + ":" + toString());
			}
			return null;
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			synchronized(allPointers) {
				if (!allPointers.containsKey(toFree)) {
					LOG.info("GPU memory info before failure:" + toString());
					throw new RuntimeException("ERROR : Internal state corrupted, cache block size map is not aware of a block it trying to free up");
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		}
		if(DEBUG_MEMORY_LEAK) {
			LOG.info("GPU Memory info during malloc:" + toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		if(A == null) {
			LOG.warn("Potential fragmentation of the GPU memory. Forcibly evicting all ...");
			LOG.info("Before clearAllUnlocked, GPU Memory info:" + toString());
			matrixMemoryManager.clearAllUnlocked(opcode);
			LOG.info("GPU Memory info after evicting all unlocked matrices:" + toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			LOG.info("Before clearAllUnlocked, GPU Memory info:" + toString());
			matrixMemoryManager.clearAllUnlocked(opcode);
			LOG.info("GPU Memory info after evicting all unlocked matrices:" + toString());
			A = cudaMallocNoWarn(tmpA, size, null);
		}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		if(A == null) {
			throw new DMLRuntimeException("There is not enough memory on device for this matrix, requested = " + byteCountToDisplaySize(size) + ". \n "
					+ toString());
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!mc1.dimsKnown()) {
			throw new DMLRuntimeException("The output dimensions are not specified and "
				+ "cannot be inferred from input:" + mc1.toString() + " " + mcOut.toString());
		}
		else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!mc1.dimsKnown()) {
			throw new DMLRuntimeException("The output dimensions are not specified and "
				+ "cannot be inferred from input:" + mc1.toString() + " " + mcOut.toString());
		}
		else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dcIn1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dcIn1.toString() + " " + dcIn2.toString() + " " + dcOut.toString());
			else
				dcOut.set(dcIn1);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dcIn1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dcIn1.toString() + " " + dcIn2.toString() + " " + dcOut.toString());
			else
				dcOut.set(dcIn1);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dcIn1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dcIn1.toString() + " " + dcIn2.toString() + " " + dcOut.toString());
			else if(outer)
				sec.getDataCharacteristics(output.getName()).set(dcIn1.getRows(), dcIn2.getCols(), dcIn1.getBlocksize(), dcIn2.getBlocksize());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dcIn1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dcIn1.toString() + " " + dcIn2.toString() + " " + dcOut.toString());
			else if(outer)
				sec.getDataCharacteristics(output.getName()).set(dcIn1.getRows(), dcIn2.getCols(), dcIn1.getBlocksize(), dcIn2.getBlocksize());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dc1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dc1.toString() + " " + dcOut.toString());
			else
				dcOut.set(dc1.getRows(), dc1.getCols(), dc1.getBlocksize(), dc1.getBlocksize());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/ComputationSPInstruction.java`
#### Snippet
```java
		if(!dcOut.dimsKnown()) {
			if(!dc1.dimsKnown())
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + dc1.toString() + " " + dcOut.toString());
			else
				dcOut.set(dc1.getRows(), dc1.getCols(), dc1.getBlocksize(), dc1.getBlocksize());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/CtableSPInstruction.java`
#### Snippet
```java
			
			default:
				throw new DMLRuntimeException("Encountered an invalid ctable operation ("+ctableOp+") while executing instruction: " + this.toString());
		}
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		if(!mcOut.dimsKnown()) {
			if(!mc1.dimsKnown()) {
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + mc1.toString() + " " + mcOut.toString());
			}
			else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/UaggOuterChainSPInstruction.java`
#### Snippet
```java
		if(!mcOut.dimsKnown()) {
			if(!mc1.dimsKnown()) {
				throw new DMLRuntimeException("The output dimensions are not specified and cannot be inferred from input:" + mc1.toString() + " " + mcOut.toString());
			}
			else {
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/WriteSPInstruction.java`
#### Snippet
```java
			}
			default:
				throw new DMLRuntimeException("Unexpected data format: " + fmt.toString());
		}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
					sb.append(',');
					sb.append(bigrand.nextLong());
					pw.println(sb.toString());
					sb.setLength(0);
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
						sb.append(blockIx[j]).append(',');
					sb.append(bigrand.nextLong());
					pw.println(sb.toString());
					sb.setLength(0);
					UtilFunctions.computeNextTensorIndexes(tmp, blockIx);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/RandSPInstruction.java`
#### Snippet
```java
					sb.append(',');
					sb.append(bigrand.nextLong());
					pw.println(sb.toString());
					sb.setLength(0);
				}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/RDDConverterUtilsExt.java`
#### Snippet
```java

					default:
						throw new Exception("Invalid converter for IJV data:" + converter.toString());
				}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/instructions/spark/utils/FrameRDDConverterUtils.java`
#### Snippet
```java
				case BOOLEAN: dt = DataTypes.BooleanType; break;
				default:      dt = DataTypes.StringType;
					LOG.warn("Using default type String for " + schema.toString());
			}
			fields.add(DataTypes.createStructField("C"+col++, dt, true));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/LocalParWorker.java`
#### Snippet
```java
					} 
					catch (Exception ex)  {
						LOG.error("Failed to execute "+lTask.toString()+", retry:"+retrys, ex);
						
						if( retrys > 0 )
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLookupTable.java`
#### Snippet
```java
	public void removeECM(String host, long pid) {
		FedUniqueCoordID funCID = new FedUniqueCoordID(host, pid);
		LOG.trace("Removing the ExecutionContextMap of coordinator " + funCID.toString());
		if(_lookup_table.remove(funCID) == null)
			LOG.warn("Removing federated execution context map failed. "
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLookupTable.java`
#### Snippet
```java
		if(_lookup_table.remove(funCID) == null)
			LOG.warn("Removing federated execution context map failed. "
				+ "No valid resolution for " + funCID.toString() + " found.");
	}

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedLookupTable.java`
#### Snippet
```java
		long t0 = DMLScript.STATISTICS ? System.nanoTime() : 0;
		FedUniqueCoordID funCID = new FedUniqueCoordID(host, pid);
		LOG.trace("Getting the ExecutionContextMap for coordinator " + funCID.toString());
		ExecutionContextMap ecm = _lookup_table.computeIfAbsent(funCID,
			k -> createNewECM());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public CSRPointer getGPUSparsePointerAddress(MatrixObject obj) {
		if(obj.getGPUObject(getGPUContext(0)) == null)
			throw new RuntimeException("No CSRPointer for MatrixObject " + obj.toString());
		else
			return obj.getGPUObject(getGPUContext(0)).getJcudaSparseMatrixPtr();
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public String toString(){
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		if(_prog != null)
			sb.append("\nProgram: " + _prog.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
		sb.append(this.getClass().getSimpleName().toString());
		if(_prog != null)
			sb.append("\nProgram: " + _prog.toString());
		if(_variables != null)
			sb.append("\nLocalVariableMap: " + _variables.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
			sb.append("\nProgram: " + _prog.toString());
		if(_variables != null)
			sb.append("\nLocalVariableMap: " + _variables.toString());
		if(_lineage != null)
			sb.append("\nLineage: " + _lineage.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
			sb.append("\nLocalVariableMap: " + _variables.toString());
		if(_lineage != null)
			sb.append("\nLineage: " + _lineage.toString());
		return sb.toString();
	}
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
			throw new DMLRuntimeException("Tried to init already initialized data");
		if(!_dataType.isMatrix() && !_dataType.isFrame())
			throw new DMLRuntimeException("Federated datatype \"" + _dataType.toString() + "\" is not supported.");
		_varID = id;
		FederatedRequest request = new FederatedRequest(RequestType.READ_VAR, id);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append(this.getClass().getSimpleName().toString());
		sb.append(" " + _dataType);
		sb.append(" " + _address.toString());
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedData.java`
#### Snippet
```java
			throw new DMLRuntimeException("Tried to init already initialized data");
		if(!_dataType.isMatrix() && !_dataType.isFrame())
			throw new DMLRuntimeException("Federated datatype \"" + _dataType.toString() + "\" is not supported.");
		_varID = id;
		FederatedRequest request = (mtd != null) ?
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
	
		_numEvaluatedPlans++;
		LOG.debug(getOptMode()+" OPT: rewrite 'set data partitioner' - result="+pdp.toString()+
			" ("+Arrays.toString(partitionedMatrices.keySet().toArray())+")" );
		
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
				int k = (int)Math.min(_N,_rk2);
				
				pn.addParam(ParamType.DATA_PARTITIONER, REMOTE_DPE.toString()+"(fused)");
				pn.setK( k );
				
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedWorkerHandler.java`
#### Snippet
```java
			LOG.debug("Executing command " + (nrRequest + 1) + "/" + totalRequests + ": " + request.getType().name());
			if(LOG.isTraceEnabled()) 
				LOG.trace("full command: " + request.toString());
		}
	}
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
			Object val = e.getValue();

			boolean isValidName = DataExpression.READ_VALID_MTD_PARAM_NAMES.contains(key);

			if (!isValidName){ //wrong parameters always rejected
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `src/main/java/org/apache/sysds/runtime/meta/MetaDataAll.java`
#### Snippet
```java
			Object val = e.getValue();

			boolean isValidName = DataExpression.READ_VALID_MTD_PARAM_NAMES.contains(key);

			if (!isValidName){ //wrong parameters always rejected
```

### SuspiciousMethodCalls
Suspicious call to 'HashSet.contains()'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/MatrixIndexingSPInstruction.java`
#### Snippet
```java
		@Override
		public Boolean apply(Object partIndex) {
			return _filterFlags.contains(partIndex);
		}
	}
```

## RuleId[id=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `ParseIssue` may be 'static'
in `src/main/java/org/apache/sysds/parser/dml/CustomErrorListener.java`
#### Snippet
```java
	 *
	 */
	public class ParseIssue implements Comparable<ParseIssue> {
		/**
		 * Line number of the parse issue
```

### InnerClassMayBeStatic
Inner class `WriteLIBSVMTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/WriterTextLIBSVMParallel.java`
#### Snippet
```java
	}

	private class WriteLIBSVMTask implements Callable<Object>
	{
		private final JobConf _job;
```

### InnerClassMayBeStatic
Inner class `WriteFileTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCSVParallel.java`
#### Snippet
```java
	}
	
	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
```

### InnerClassMayBeStatic
Inner class `ReadRowsTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderJSONLParallel.java`
#### Snippet
```java
	}

	private class ReadRowsTask implements Callable<Object>{
		private InputSplit _split;
		private TextInputFormat _inputFormat;
```

### InnerClassMayBeStatic
Inner class `WriteTextTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/WriterTextCellParallel.java`
#### Snippet
```java
	}

	private class WriteTextTask implements Callable<Object> 
	{
		private JobConf _job = null;
```

### InnerClassMayBeStatic
Inner class `WriteMMTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/WriterMatrixMarketParallel.java`
#### Snippet
```java
	}

	private class WriteMMTask implements Callable<Object> 
	{
		private JobConf _job = null;
```

### InnerClassMayBeStatic
Inner class `WriteFileTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterBinaryBlockParallel.java`
#### Snippet
```java
	}

	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
```

### InnerClassMayBeStatic
Inner class `ReadTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderTextCellParallel.java`
#### Snippet
```java
	}

	public class ReadTask implements Callable<Object> 
	{
		private InputSplit _split = null;
```

### InnerClassMayBeStatic
Inner class `ReadFileTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameReaderBinaryBlockParallel.java`
#### Snippet
```java
	}

	private class ReadFileTask implements Callable<Object> 
	{
		private Path _path = null;
```

### InnerClassMayBeStatic
Inner class `WriteFileTask` may be 'static'
in `src/main/java/org/apache/sysds/runtime/io/FrameWriterTextCellParallel.java`
#### Snippet
```java
	}	

	private class WriteFileTask implements Callable<Object> 
	{
		private Path _path = null;
```

### InnerClassMayBeStatic
Inner class `TokenInfo` may be 'static'
in `src/main/java/org/apache/sysds/runtime/lineage/LineageTokenizer.java`
#### Snippet
```java
	}
	
	private class TokenInfo {
		public final Pattern regex;
		public final String key;
```

### InnerClassMayBeStatic
Inner class `skipPair` may be 'static'
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
	}

	private class skipPair {
		protected final int apos;
		protected final int astart;
```

### InnerClassMayBeStatic
Inner class `EncoderColnames` may be 'static'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
	}

	private class EncoderColnames {
		public final MultiColumnEncoder _encoder;
		public final String[] _colnames;
```

### InnerClassMayBeStatic
Inner class `RdiagResult` may be 'static'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ReorgFEDInstruction.java`
#### Snippet
```java
	}

	private class RdiagResult {
		FederationMap fedMap;
		Map<FederatedRange, int[]> dcs;
```

### InnerClassMayBeStatic
Inner class `DataPartitionerWorker` may be 'static'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/DataPartitionerLocal.java`
#### Snippet
```java
	}

	private abstract class DataPartitionerWorker implements Runnable
	{
		private JobConf _job = null;
```

### InnerClassMayBeStatic
Inner class `Result` may be 'static'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionLocalScheme.java`
#### Snippet
```java
public abstract class DataPartitionLocalScheme {

	public final class Result {
		public final List<MatrixObject> pFeatures;
		public final List<MatrixObject> pLabels;
```

### InnerClassMayBeStatic
Inner class `Result` may be 'static'
in `src/main/java/org/apache/sysds/runtime/controlprogram/paramserv/dp/DataPartitionSparkScheme.java`
#### Snippet
```java
public abstract class DataPartitionSparkScheme implements Serializable {

	protected final class Result {
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pFeatures; // WorkerID => (rowID, matrix)
		protected final List<Tuple2<Integer, Tuple2<Long, MatrixBlock>>> pLabels;
```

## RuleId[id=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `params.output.denseBlock`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNConv2d.java`
#### Snippet
```java
	
	private static void inplaceTransAdd(double[] a, DnnParameters params) {
		synchronized (params.output.denseBlock) {
			// Perform transposed addition: output of size [K, CRS] += input of size [CRS,K]
			double [] c = params.output.getDenseBlockValues();
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `params.output.denseBlock`
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixDNNConv2d.java`
#### Snippet
```java
	
	private static void inplaceAdd(double[] a, DnnParameters params) {
		synchronized (params.output.denseBlock) {
			LibMatrixMult.vectAdd(a, params.output.getDenseBlockValues(), 0, 0, a.length);
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_prefetchMO`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/TriggerPrefetchTask.java`
#### Snippet
```java
		MatrixBlock mb = null;
		long t1 = System.nanoTime();
		synchronized (_prefetchMO) {
			// Having this check inside the critical section
			// safeguards against concurrent rmVar.
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_remoteOperationsRoot`
in `src/main/java/org/apache/sysds/runtime/instructions/cp/TriggerCheckpointTask.java`
#### Snippet
```java
	public void run() {
		boolean triggered = false;
		synchronized (_remoteOperationsRoot) {
			// FIXME: Handle double execution
			if (_remoteOperationsRoot.isPendingRDDOps()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `matrixMemoryManager.gpuObjects`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
		if(A == null) {
			long t0 =  DMLScript.STATISTICS ? System.nanoTime() : 0;
			synchronized (matrixMemoryManager.gpuObjects) {
				Optional<GPUObject> sizeBasedUnlockedGPUObjects = matrixMemoryManager.gpuObjects.stream()
					.filter(gpuObj -> !gpuObj.isLocked()
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `matrixMemoryManager.gpuObjects`
in `src/main/java/org/apache/sysds/runtime/instructions/gpu/context/GPUMemoryManager.java`
#### Snippet
```java
			// ---------------------------------------------------------------
			// Evict unlocked GPU objects one-by-one and try malloc
			synchronized(matrixMemoryManager.gpuObjects) {
				List<GPUObject> unlockedGPUObjects = matrixMemoryManager.gpuObjects.stream()
						.filter(gpuObj -> !gpuObj.isLocked()).collect(Collectors.toList());
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
		// Evict cached objects to make space
		try {
			synchronized(_mQueue) {
				// Evict blobs to make room (by default FIFO)
				while (getUMMFree() < reqSpace && !_mQueue.isEmpty()) {
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java

	public static long getUMMSize() {
		synchronized(_mQueue) {
			return _limit;
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
	// Get the available memory in UMM
	public static long getUMMFree() {
		synchronized(_mQueue) {
			return _limit - (_totCachedSize + _pinnedPhysicalMemSize);
		}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
		System.out.println("UMM STATUS AT "+operation+" --");

		synchronized (_mQueue) {
			// print UMM meta data
			System.out.println("\tUMM: Meta Data: " +
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java
		boolean requiresDelete = true;

		synchronized( _mQueue )
		{
			//remove queue entry
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java

		//probe write buffer
		synchronized (_mQueue)
		{
			ldata = _mQueue.get(fname);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/UnifiedMemoryManager.java`
#### Snippet
```java

			// Modify buffer pool
			synchronized( _mQueue )
			{
				// Evict blocks to make room if required
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
		boolean requiresDelete = true;
		
		synchronized( _mQueue )
		{
			//remove queue entry 
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
		
		//probe write buffer
		synchronized( _mQueue )
		{
			ldata = _mQueue.get(fname);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
	
	public static long getWriteBufferSize() {
		synchronized( _mQueue ) {
			return _size; }
	}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
			
			//modify buffer pool
			synchronized( _mQueue )
			{
				//evict matrices to make room (by default FIFO)
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
	
	public static long getWriteBufferFree() {
		synchronized( _mQueue ) {
			return _limit - _size; }
	}
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_mQueue`
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/LazyWriteBuffer.java`
#### Snippet
```java
		System.out.println("WRITE BUFFER STATUS ("+position+") --");
		
		synchronized( _mQueue ) {
			//print buffer meta data
			System.out.println("\tWB: Buffer Meta Data: " +
```

## RuleId[id=ExcessiveRangeCheck]
### ExcessiveRangeCheck
Can be replaced with 'hops.size() != 1'
in `src/main/java/org/apache/sysds/hops/ipa/IPAPassForwardFunctionCalls.java`
#### Snippet
```java
	
	private static boolean singleFunctionOp(ArrayList<Hop> hops) {
		if( hops==null || hops.isEmpty() || hops.size()!=1 )
			return false;
		return hops.get(0) instanceof FunctionOp;
```

### ExcessiveRangeCheck
Can be replaced with 'out_blen%in_blen == 0'
in `src/main/java/org/apache/sysds/runtime/instructions/spark/functions/ExtractBlockForBinaryReblock.java`
#### Snippet
```java
		final long out_startColBlockIndex = UtilFunctions.computeBlockIndex(startColGlobalCellIndex, out_blen);
		final long out_endColBlockIndex = UtilFunctions.computeBlockIndex(endColGlobalCellIndex, out_blen);
		final boolean aligned = out_blen%in_blen==0 && out_blen%in_blen==0; //e.g, 1K -> 2K
		
		ArrayList<Tuple2<MatrixIndexes, MatrixBlock>> retVal = new ArrayList<>();
```

## RuleId[id=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'data != null' covered by subsequent condition 'data instanceof MatrixObject'
in `src/main/java/org/apache/sysds/api/ScriptExecutorUtils.java`
#### Snippet
```java
					for(String outVar : outputVariables) {
						Data data = ec.getVariable(outVar);
						if(data != null && data instanceof MatrixObject) {
							for(GPUContext gCtx : ec.getGPUContexts()) {
								GPUObject gpuObj = ((MatrixObject)data).getGPUObject(gCtx);
```

### ConditionCoveredByFurtherCondition
Condition 'value != null' covered by subsequent condition 'value instanceof Long'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
	public Script in(String name, Object value, Metadata metadata) {

		if ((value != null) && (value instanceof Long)) {
			// convert Long to Integer since Long not a supported value type
			Long lng = (Long) value;
```

### ConditionCoveredByFurtherCondition
Condition 'value != null' covered by subsequent condition 'value instanceof Float'
in `src/main/java/org/apache/sysds/api/mlcontext/Script.java`
#### Snippet
```java
			Long lng = (Long) value;
			value = lng.intValue();
		} else if ((value != null) && (value instanceof Float)) {
			// convert Float to Double since Float not a supported value type
			Float flt = (Float) value;
```

### ConditionCoveredByFurtherCondition
Condition 'ec != null' covered by subsequent condition 'ec instanceof SparkExecutionContext'
in `src/main/java/org/apache/sysds/api/DMLScript.java`
#### Snippet
```java
			//stop spark context (after cleanup of federated workers and other pools,
			//otherwise federated spark cleanups in local tests throw errors in same JVM)
			if(ec != null && ec instanceof SparkExecutionContext)
				((SparkExecutionContext) ec).close();
			LOG.info("END DML run " + getDateTime() );
```

### ConditionCoveredByFurtherCondition
Condition '!(dataType instanceof org.apache.spark.ml.linalg.VectorUDT)' covered by subsequent condition '!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT)'
in `src/main/java/org/apache/sysds/api/mlcontext/MLContextUtil.java`
#### Snippet
```java
			DataType dataType = field.dataType();
			if ((dataType != DataTypes.DoubleType) && (dataType != DataTypes.IntegerType)
					&& (dataType != DataTypes.LongType) && (!(dataType instanceof org.apache.spark.ml.linalg.VectorUDT))
					&& (!(dataType instanceof org.apache.spark.mllib.linalg.VectorUDT))) {
				// uncomment if we support arrays of doubles for matrices
```

### ConditionCoveredByFurtherCondition
Condition 'rowsHop != null' covered by subsequent condition 'rowsHop instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			try {
			
				if (rowsHop != null && rowsHop instanceof LiteralOp) {
					specifiedRows = ((LiteralOp)rowsHop).getLongValue();
				}
```

### ConditionCoveredByFurtherCondition
Condition 'colsHop != null' covered by subsequent condition 'colsHop instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}
				numRows = numRows < specifiedRows ? numRows : specifiedRows;
				if (colsHop != null && colsHop instanceof LiteralOp){
					specifiedCols = ((LiteralOp)colsHop).getLongValue();
				}
```

### ConditionCoveredByFurtherCondition
Condition 'sparsePrintHOP != null' covered by subsequent condition 'sparsePrintHOP instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				numCols = numCols < specifiedCols ? numCols : specifiedCols;
				
				if (sparsePrintHOP != null && sparsePrintHOP instanceof LiteralOp){
					sparsePrint = ((LiteralOp)sparsePrintHOP).getBooleanValue();
				}
```

### ConditionCoveredByFurtherCondition
Condition 'sepHop != null' covered by subsequent condition 'sepHop instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}
				
				if (sepHop != null && sepHop instanceof LiteralOp){
					sep = ((LiteralOp)sepHop).getStringValue();
				}
```

### ConditionCoveredByFurtherCondition
Condition 'linesepHop != null' covered by subsequent condition 'linesepHop instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				}
				
				if (linesepHop != null && linesepHop instanceof LiteralOp){
					linesep = ((LiteralOp)linesepHop).getStringValue();
				}
```

### ConditionCoveredByFurtherCondition
Condition 'ngroups != null' covered by subsequent condition 'ngroups instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
			if ( _paramIndexMap.get(Statement.GAGG_NUM_GROUPS) != null ) {
				Hop ngroups = getParameterHop(Statement.GAGG_NUM_GROUPS);
				if(ngroups != null && ngroups instanceof LiteralOp) {
					long m = HopRewriteUtils.getIntValueSafe((LiteralOp)ngroups);
					long n = (dc.getRows()==1)?1:dc.getCols();
```

### ConditionCoveredByFurtherCondition
Condition 'ngroups != null' covered by subsequent condition 'ngroups instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
				if ( _paramIndexMap.get(Statement.GAGG_NUM_GROUPS) != null ) {
					Hop ngroups = getParameterHop(Statement.GAGG_NUM_GROUPS);
					if(ngroups != null && ngroups instanceof LiteralOp) {
						ldim1 = HopRewriteUtils.getIntValueSafe((LiteralOp)ngroups);
					}
```

### ConditionCoveredByFurtherCondition
Condition 'ngroups != null' covered by subsequent condition 'ngroups instanceof LiteralOp \| ngroups instanceof DataOp'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
		try {
			Hop ngroups = getParameterHop(Statement.GAGG_NUM_GROUPS);
			return (ngroups != null 
				&& (ngroups instanceof LiteralOp | ngroups instanceof DataOp));
		}
```

### ConditionCoveredByFurtherCondition
Condition 'numGroups != null' covered by subsequent condition 'numGroups instanceof Data'
in `src/main/java/org/apache/sysds/hops/ParameterizedBuiltinOp.java`
#### Snippet
```java
		long outputDim1=-1, outputDim2=-1;
		Lop numGroups = inputlops.get(Statement.GAGG_NUM_GROUPS);
		if ( !dimsKnown() && numGroups != null && numGroups instanceof Data && ((Data)numGroups).isLiteral() ) {
			long ngroups = ((Data)numGroups).getLongValue();
			
```

### ConditionCoveredByFurtherCondition
Condition 'val1 \>= 0' covered by subsequent conditions
in `src/main/java/org/apache/sysds/hops/DnnOp.java`
#### Snippet
```java
	
	private static boolean isEqualAndKnown(int val1, int val2) {
		return val1 >= 0 && val2 >= 0 && val1 == val2;
	}
	
```

### ConditionCoveredByFurtherCondition
Condition 'm2_dim1 \>= 1' covered by subsequent condition 'm2_dim1 \>= 1'
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
			
		// broadcast-based left indexing w/o shuffle for small left/right inputs
		if( m2_dim1 >= 1 && m2_dim2 >= 1 && m2_dim1 >= 1 && m2_dim2 >= 1 ) { //lhs/rhs known
			boolean isAligned = (rhsDt == DataType.MATRIX) &&
					((m1_dim1 == m2_dim1 && m1_dim2 <= m1_blen) || (m1_dim2 == m2_dim2 && m1_dim1 <= m1_blen));
```

### ConditionCoveredByFurtherCondition
Condition 'm2_dim2 \>= 1' covered by subsequent condition 'm2_dim2 \>= 1'
in `src/main/java/org/apache/sysds/hops/LeftIndexingOp.java`
#### Snippet
```java
			
		// broadcast-based left indexing w/o shuffle for small left/right inputs
		if( m2_dim1 >= 1 && m2_dim2 >= 1 && m2_dim1 >= 1 && m2_dim2 >= 1 ) { //lhs/rhs known
			boolean isAligned = (rhsDt == DataType.MATRIX) &&
					((m1_dim1 == m2_dim1 && m1_dim2 <= m1_blen) || (m1_dim2 == m2_dim2 && m1_dim1 <= m1_blen));
```

### ConditionCoveredByFurtherCondition
Condition 'scalar != null' covered by subsequent condition 'scalar instanceof ScalarObject'
in `src/main/java/org/apache/sysds/hops/ipa/InterProceduralAnalysis.java`
#### Snippet
```java
				{
					Data scalar = callvars.get(input.getName()); 
					if( scalar != null && scalar instanceof ScalarObject ) {
						vars.put(dat.getName(), scalar);
					}
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof ScalarObject'
in `src/main/java/org/apache/sysds/hops/OptimizerUtils.java`
#### Snippet
```java
				String name = root.getName();
				Data dat = vars.get(name);
				if( dat!=null && dat instanceof ScalarObject )
					ret = ((ScalarObject)dat).getDoubleValue();
			}
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof CacheableData'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteRemovePersistentReadWrite.java`
#### Snippet
```java
			for( String varname : in ) {
				Data dat = vars.get(varname);
				if( dat != null && dat instanceof CacheableData<?> )
					_inputsMeta.put(varname, ((CacheableData<?>)dat).getMetaData());
			}
```

### ConditionCoveredByFurtherCondition
Condition 'increment != null' covered by subsequent condition 'increment instanceof LiteralOp'
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteForLoopVectorization.java`
#### Snippet
```java
		
		//check missing and supported increment values
		if( !(increment!=null && increment instanceof LiteralOp 
				&& ((LiteralOp)increment).getDoubleValue()==1.0) ) {
			return ret;
```

### ConditionCoveredByFurtherCondition
Condition 'ldat != null' covered by subsequent condition 'ldat instanceof ListObject'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			Data ldat = vars.get(hop.getInput(0).getInput(0).getName()); //list, or matrix during IPA
			Hop rix = hop.getInput(0);
			if( ldat != null && ldat instanceof ListObject
				&& rix.getInput(1) instanceof LiteralOp
				&& rix.getInput(2) instanceof LiteralOp
```

### ConditionCoveredByFurtherCondition
Condition 'dat1 != null' covered by subsequent condition 'dat1 instanceof MatrixObject'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			//compare size and value information (note: by definition both dat1 and dat2 are of same type
			//because we do not allow data type changes)
			if( dat1 != null && dat1 instanceof MatrixObject && dat2!=null )
			{
				//handle matrices
```

### ConditionCoveredByFurtherCondition
Condition 'dat1 != null' covered by subsequent condition 'dat1 instanceof MatrixObject'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			Data dat1 = oldCallVars.get(varname);
			Data dat2 = callVars.get(varname);
			if( dat1!=null && dat1 instanceof MatrixObject && dat2!=null && dat2 instanceof MatrixObject )
			{
				MatrixObject moOld = (MatrixObject) dat1;
```

### ConditionCoveredByFurtherCondition
Condition 'dat2 != null' covered by subsequent condition 'dat2 instanceof MatrixObject'
in `src/main/java/org/apache/sysds/hops/recompile/Recompiler.java`
#### Snippet
```java
			Data dat1 = oldCallVars.get(varname);
			Data dat2 = callVars.get(varname);
			if( dat1!=null && dat1 instanceof MatrixObject && dat2!=null && dat2 instanceof MatrixObject )
			{
				MatrixObject moOld = (MatrixObject) dat1;
```

### ConditionCoveredByFurtherCondition
Condition 'that == null' covered by subsequent condition '!(that instanceof Expression)'
in `src/main/java/org/apache/sysds/parser/Expression.java`
#### Snippet
```java
	{
		//empty check for robustness
		if( that == null || !(that instanceof Expression) )
			return false;
		
```

### ConditionCoveredByFurtherCondition
Condition 'ifConstVersion != null' covered by subsequent condition 'ifConstVersion instanceof IntIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				ConstIdentifier elseConstVersion = constVarsElseCopy.get(updatedVar);
				// IntIdentifier
				if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof IntIdentifier && elseConstVersion instanceof IntIdentifier){
					if ( ((IntIdentifier)ifConstVersion).getValue() == ((IntIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'elseConstVersion != null' covered by subsequent condition 'elseConstVersion instanceof IntIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				ConstIdentifier elseConstVersion = constVarsElseCopy.get(updatedVar);
				// IntIdentifier
				if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof IntIdentifier && elseConstVersion instanceof IntIdentifier){
					if ( ((IntIdentifier)ifConstVersion).getValue() == ((IntIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'ifConstVersion != null' covered by subsequent condition 'ifConstVersion instanceof DoubleIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				}
				// DoubleIdentifier
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof DoubleIdentifier && elseConstVersion instanceof DoubleIdentifier){
					if ( ((DoubleIdentifier)ifConstVersion).getValue() == ((DoubleIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'elseConstVersion != null' covered by subsequent condition 'elseConstVersion instanceof DoubleIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				}
				// DoubleIdentifier
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof DoubleIdentifier && elseConstVersion instanceof DoubleIdentifier){
					if ( ((DoubleIdentifier)ifConstVersion).getValue() == ((DoubleIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'ifConstVersion != null' covered by subsequent condition 'ifConstVersion instanceof BooleanIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				}
				// Boolean 
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof BooleanIdentifier && elseConstVersion instanceof BooleanIdentifier){
					if ( ((BooleanIdentifier)ifConstVersion).getValue() == ((BooleanIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'elseConstVersion != null' covered by subsequent condition 'elseConstVersion instanceof BooleanIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				}
				// Boolean 
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof BooleanIdentifier && elseConstVersion instanceof BooleanIdentifier){
					if ( ((BooleanIdentifier)ifConstVersion).getValue() == ((BooleanIdentifier) elseConstVersion).getValue() )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'ifConstVersion != null' covered by subsequent condition 'ifConstVersion instanceof StringIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				
				// String
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof StringIdentifier && elseConstVersion instanceof StringIdentifier){
					if ( ((StringIdentifier)ifConstVersion).getValue().equals(((StringIdentifier) elseConstVersion).getValue()) )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'elseConstVersion != null' covered by subsequent condition 'elseConstVersion instanceof StringIdentifier'
in `src/main/java/org/apache/sysds/parser/IfStatementBlock.java`
#### Snippet
```java
				
				// String
				else if (ifConstVersion != null && elseConstVersion != null && ifConstVersion instanceof StringIdentifier && elseConstVersion instanceof StringIdentifier){
					if ( ((StringIdentifier)ifConstVersion).getValue().equals(((StringIdentifier) elseConstVersion).getValue()) )
						recConstVars.put(updatedVar, ifConstVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'exprNGroups != null' covered by subsequent condition 'exprNGroups instanceof Identifier'
in `src/main/java/org/apache/sysds/parser/ParameterizedBuiltinFunctionExpression.java`
#### Snippet
```java
		//determine output dimensions
		long outputDim1 = -1, outputDim2 = -1;
		if( exprNGroups != null && exprNGroups instanceof Identifier ) 
		{
			Identifier numGroups = (Identifier) exprNGroups;
```

### ConditionCoveredByFurtherCondition
Condition 'blen \> 0' covered by subsequent condition 'blen \> 0'
in `src/main/java/org/apache/sysds/runtime/io/MatrixReader.java`
#### Snippet
```java
			//create synchronization points for MCSR (start row per block row)
			if( sblock instanceof SparseBlockMCSR && clen > blen      //multiple col blocks 
				&& clen >= 0 && blen > 0 && rlen >= 0 && blen > 0 ) {  //all dims known
				//note: allocate w/ min 2 nnz to ensure allocated row object because
				//adaptive change from scalar to row could cause synchronization issues
```

### ConditionCoveredByFurtherCondition
Condition 'paramExp != null' covered by subsequent condition 'paramExp instanceof DataIdentifier'
in `src/main/java/org/apache/sysds/parser/DataExpression.java`
#### Snippet
```java
		{
			Expression paramExp = getVarParam(paramName);
			if (   paramExp != null && paramExp instanceof DataIdentifier && !(paramExp instanceof IndexedIdentifier) 
				&& currConstVars.containsKey(((DataIdentifier) paramExp).getName()))
			{
```

### ConditionCoveredByFurtherCondition
Condition 'o2 == null' covered by subsequent condition '!(o2 instanceof LinearFunction)'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
		@Override
		public boolean equals( Object o2 ) {
			if( o2 == null || !(o2 instanceof LinearFunction)  )
				return false;
			LinearFunction f2 = (LinearFunction)o2;
```

### ConditionCoveredByFurtherCondition
Condition 'sub1a == null \&\& sub1b == null' covered by subsequent condition '!(sub1a instanceof IntIdentifier)'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				out = new LinearFunction(0, 1L, id);
				
				if( sub1a == null && sub1b == null //: operator
					|| !(sub1a instanceof IntIdentifier) || !(sub1b instanceof IntIdentifier) ) { //for robustness
					_bounds._lower.put(id, 1L);
```

### ConditionCoveredByFurtherCondition
Condition 'sub2a == null \&\& sub2b == null' covered by subsequent condition '!(sub2a instanceof IntIdentifier)'
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
					tmpOut = new LinearFunction(0, 1L, id);
					
					if(   sub2a == null && sub2b == null  //: operator 
					   || !(sub2a instanceof IntIdentifier) || !(sub2b instanceof IntIdentifier) ) //for robustness
					{
```

### ConditionCoveredByFurtherCondition
Condition 'expr != null' covered by subsequent condition 'expr instanceof ConstIdentifier'
in `src/main/java/org/apache/sysds/parser/BuiltinFunctionExpression.java`
#### Snippet
```java

	private static boolean isConstant(Expression expr) {
		return ( expr != null && expr instanceof ConstIdentifier );
	}
	
```

### ConditionCoveredByFurtherCondition
Condition 'shortVt \&\& valueType.equals(...)' covered by subsequent condition 'valueType.equals(...)'
in `src/main/java/org/apache/sysds/parser/dml/DmlSyntacticValidator.java`
#### Snippet
```java
			dataId.setDataType(DataType.SCALAR);

		if( (shortVt && valueType.equals("int"))
			|| valueType.equals("int") || valueType.equals("integer")
			|| valueType.equals("Int") || valueType.equals("Integer")) {
```

### ConditionCoveredByFurtherCondition
Condition 'that == null' covered by subsequent condition '!(that instanceof MatrixCell)'
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixCell.java`
#### Snippet
```java
	@Override
	public void copy(MatrixValue that) {
		if( that==null || !(that instanceof MatrixCell))
			throw new RuntimeException("the Matrix Value is not MatrixCell!");	
		
```

### ConditionCoveredByFurtherCondition
Condition 'thatValue == null' covered by subsequent condition '!(thatValue instanceof CompressedMatrixBlock)'
in `src/main/java/org/apache/sysds/runtime/compress/CompressedMatrixBlock.java`
#### Snippet
```java

	private static CompressedMatrixBlock checkType(MatrixValue thatValue) {
		if(thatValue == null || !(thatValue instanceof CompressedMatrixBlock))
			throw new DMLRuntimeException("Invalid call to copy, require a compressed MatrixBlock to copy to");

```

### ConditionCoveredByFurtherCondition
Condition 'result == null' covered by subsequent condition '!(result instanceof CompressedMatrixBlock)'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibScalar.java`
#### Snippet
```java
	private static CompressedMatrixBlock setupRet(CompressedMatrixBlock m1, MatrixValue result) {
		CompressedMatrixBlock ret;
		if(result == null || !(result instanceof CompressedMatrixBlock))
			ret = new CompressedMatrixBlock(m1.getNumRows(), m1.getNumColumns());
		else {
```

### ConditionCoveredByFurtherCondition
Condition 'result == null' covered by subsequent condition '!(result instanceof CompressedMatrixBlock)'
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibBinaryCellOp.java`
#### Snippet
```java
	private static CompressedMatrixBlock setupCompressedReturnMatrixBlock(CompressedMatrixBlock m1, MatrixValue result) {
		CompressedMatrixBlock ret = null;
		if(result == null || !(result instanceof CompressedMatrixBlock))
			ret = new CompressedMatrixBlock(m1.getNumRows(), m1.getNumColumns());
		else {
```

### ConditionCoveredByFurtherCondition
Condition 'o == null' covered by subsequent condition '!(o instanceof CM_COV_Object)'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CM_COV_Object.java`
#### Snippet
```java
	public boolean equals(Object o)
	{
		if( o == null || !(o instanceof CM_COV_Object) )
			return false;
		
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof MatrixObject'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public boolean isMatrixObject(String varname) {
		Data dat = getVariable(varname);
		return (dat!= null && dat instanceof MatrixObject);
	}
	
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof FrameObject'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
	public boolean isFrameObject(String varname) {
		Data dat = getVariable(varname);
		return (dat!= null && dat instanceof FrameObject);
	}
	
```

### ConditionCoveredByFurtherCondition
Condition 'oldMetaData == null' covered by subsequent condition '!(oldMetaData instanceof MetaDataFormat)'
in `src/main/java/org/apache/sysds/runtime/controlprogram/context/ExecutionContext.java`
#### Snippet
```java
			return;
		MetaData oldMetaData = mo.getMetaData();
		if( oldMetaData == null || !(oldMetaData instanceof MetaDataFormat) )
			throw new DMLRuntimeException("Metadata not available");
		MatrixCharacteristics mc = new MatrixCharacteristics(nrows, ncols, (int) mo.getBlocksize());
```

### ConditionCoveredByFurtherCondition
Condition 'potentialException != null' covered by subsequent condition 'potentialException instanceof Exception'
in `src/main/java/org/apache/sysds/runtime/controlprogram/federated/FederatedResponse.java`
#### Snippet
```java
	public void throwExceptionFromResponse() throws Exception {
		for ( Object potentialException : _data){
			if (potentialException != null && (potentialException instanceof Exception) ){
				throw (Exception) potentialException;
			}
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof MatrixObject'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
				Data dat = vars.get(var);
				
				if( dat!=null && dat instanceof MatrixObject
					&& ((MatrixObject)dat).getRDDHandle()!=null )
				{
```

### ConditionCoveredByFurtherCondition
Condition 'dat == null' covered by subsequent condition '!(dat instanceof MatrixObject)'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		{
			Data dat = vars.get(rVar._name);
			if( dat == null || !(dat instanceof MatrixObject) ) {
				ret = false; 
				break;
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof MatrixObject'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
		//not all intermediates need to be known or existing on optimize
		Data dat = vars.get( varName );
		if( dat != null && dat instanceof MatrixObject )
		{
			MatrixObject mo = (MatrixObject) dat;
```

### ConditionCoveredByFurtherCondition
Condition 'dat != null' covered by subsequent condition 'dat instanceof MatrixObject'
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/opt/OptimizerRuleBased.java`
#### Snippet
```java
			//Potential scalars: for disabled dependency analysis and unbounded scoping
			Data dat = vars.get( var._name );
			if( dat != null && dat instanceof MatrixObject ) 
			{
				MatrixObject mo = (MatrixObject) dat;
```

### ConditionCoveredByFurtherCondition
Condition 'in2 == 0.0' covered by subsequent condition 'in2 == -0.0'
in `src/main/java/org/apache/sysds/runtime/functionobjects/Modulus.java`
#### Snippet
```java
	@Override
	public double execute(double in1, double in2) {
		if( in2==0.0 || in2==-0.0 )
			return Double.NaN;
		return in1 - _intdiv.execute(in1, in2)*in2;
```

## RuleId[id=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/parser/ParForStatementBlock.java`
#### Snippet
```java
				f1.removeVar(i);
				i--; 
				continue;
			}
	}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/util/DMVUtils.java`
#### Snippet
```java
			}
			else if(currently_digit && (ch == outter_char || ch == inner_char))
				continue;
			else if(ch != inner_char && ch != ARBITRARY_LEN) {
				currently_digit = false;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
			}
			else if(g instanceof ColGroupEmpty)
				continue;
			else if(g instanceof ColGroupConst)
				((ColGroupConst) g).addToCommon(constV);
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
		for(AColGroup g : groups) {
			if(g instanceof ColGroupEmpty || g.isEmpty())
				continue;
			else if(g instanceof AMorphingMMColGroup)
				filteredGroups.add(((AMorphingMMColGroup) g).extractCommon(constV));
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibUtils.java`
#### Snippet
```java
				preAggGroups.add((APreAgg) g);
			else if(g instanceof ColGroupEmpty)
				continue;
			else if(g instanceof ColGroupConst)
				throw new NotImplementedException();
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibScalar.java`
#### Snippet
```java
		for(AColGroup grp : m1.getColGroups()) {
			if(grp instanceof ColGroupEmpty)
				continue;
			else if(grp instanceof ColGroupConst) {
				final ColGroupConst g = (ColGroupConst) grp;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/lib/CLALibScalar.java`
#### Snippet
```java
		for(AColGroup grp : old) {
			if(grp instanceof ColGroupEmpty)
				continue;
			else if(grp instanceof ColGroupConst) {
				final ColGroupConst g = (ColGroupConst) grp;
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupOLE.java`
#### Snippet
```java
					return _dict.getValue(k * _colIndexes.size() + colIdx);
				else if(_data[boff + bix + blckIx] > offset)
					continue;
			}
		}
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ADictBasedColGroup.java`
#### Snippet
```java
					if(b[rowIdxOffset + h] != 0.0) {
						aggregateColumnsSet.add(h);
						continue;
					}

```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `src/main/java/org/apache/sysds/runtime/compress/colgroup/ColGroupRLE.java`
#### Snippet
```java
							skip[tk] = tapos;
							skipV[tk] = trs - that._data[tapos];
							continue;
						}
						else if(trs >= re) // if we are past run break.
```

## RuleId[id=CopyConstructorMissesField]
### CopyConstructorMissesField
Copy constructor does not copy field '_outVarLineage'
in `src/main/java/org/apache/sysds/api/jmlc/PreparedScript.java`
#### Snippet
```java
	private HashMap<String, String> _outVarLineage;
	
	private PreparedScript(PreparedScript that) {
		//shallow copy, except for a separate symbol table
		//and related meta data of reused inputs
```

### CopyConstructorMissesField
Copy constructor does not copy 4 fields
in `src/main/java/org/apache/sysds/hops/rewrite/RewriteCompressedReblock.java`
#### Snippet
```java
		}

		private ProbeStatus(ProbeStatus status) {
			startHopID = status.startHopID;
			prog = status.prog;
```

### CopyConstructorMissesField
Copy constructor does not copy 5 fields
in `src/main/java/org/apache/sysds/parser/StatementBlock.java`
#### Snippet
```java
	}
	
	public StatementBlock(StatementBlock sb) {
		this();
		setParseInfo(sb);
```

### CopyConstructorMissesField
Copy constructor does not copy field '_colsdata'
in `src/main/java/org/apache/sysds/runtime/data/DataTensorBlock.java`
#### Snippet
```java
	}

	public DataTensorBlock(DataTensorBlock that) {
		copy(that);
	}
```

### CopyConstructorMissesField
Copy constructor does not copy fields '_denseBlock' and '_sparseBlock'
in `src/main/java/org/apache/sysds/runtime/data/BasicTensorBlock.java`
#### Snippet
```java
	}

	public BasicTensorBlock(BasicTensorBlock that) {
		_vt = that.getValueType();
		copy(that);
```

### CopyConstructorMissesField
Copy constructor does not copy field '_columnLocks'
in `src/main/java/org/apache/sysds/runtime/frame/data/FrameBlock.java`
#### Snippet
```java
	 * @param that frame block
	 */
	public FrameBlock(FrameBlock that) {
		this(that.getSchema(), that.getColumnNames(false));
		copy(that);
```

### CopyConstructorMissesField
Copy constructor does not copy 7 fields
in `src/main/java/org/apache/sysds/runtime/matrix/data/MatrixBlock.java`
#### Snippet
```java
	}
	
	public MatrixBlock(MatrixBlock that) {
		copy(that);
	}
```

### CopyConstructorMissesField
Copy constructor does not copy fields '_activeDedupBlock' and '_initDedupBlock'
in `src/main/java/org/apache/sysds/runtime/lineage/Lineage.java`
#### Snippet
```java
	}
	
	public Lineage(Lineage that) {
		_map = new LineageMap(that._map);
	}
```

### CopyConstructorMissesField
Copy constructor does not copy field '_privacyConstraint'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/CPOperand.java`
#### Snippet
```java
	}

	public CPOperand(CPOperand variable){
		_name = variable._name;
		_valueType = variable._valueType;
```

### CopyConstructorMissesField
Copy constructor does not copy field 'outputs'
in `src/main/java/org/apache/sysds/runtime/controlprogram/LocalVariableMap.java`
#### Snippet
```java
	}
	
	public LocalVariableMap(LocalVariableMap vars) {
		localMap = new ConcurrentHashMap<>(vars.localMap);
		localID = _seq.getNextID();
```

### CopyConstructorMissesField
Copy constructor does not copy field '_partitionInMemory'
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/MatrixObject.java`
#### Snippet
```java
	 * @param mo matrix object
	 */
	public MatrixObject(MatrixObject mo) {
		// base copy constructor
		super(mo);
```

### CopyConstructorMissesField
Copy constructor does not copy 10 fields
in `src/main/java/org/apache/sysds/runtime/controlprogram/caching/CacheableData.java`
#### Snippet
```java
	 * @param that cacheable data object
	 */
	protected CacheableData(CacheableData<T> that) {
		this( that.getDataType(), that.getValueType() );
		_cleanupFlag = that._cleanupFlag;
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `src/main/java/org/apache/sysds/runtime/compress/cocode/ColIndexes.java`
#### Snippet
```java

	@Override
	public boolean equals(Object that) {
		ColIndexes thatGrp = (ColIndexes) that;
		return _indexes.equals(thatGrp._indexes);
```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'csrblock' to 'SparseBlockCSR'
in `src/main/java/org/apache/sysds/runtime/transform/encode/MultiColumnEncoder.java`
#### Snippet
```java
				// Not using the set() methods to 1) avoid binary search and shifting, 
				// 2) reduce thread contentions on the arrays
				int[] rptr = ((SparseBlockCSR)csrblock).rowPointers();
				for (int i=0; i<rptr.length-1; i++) { //TODO: parallelize
					rptr[i+1] = rptr[i] + input.getNumColumns();
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'out' to 'TensorBlock'
in `src/main/java/org/apache/sysds/runtime/instructions/cp/DataGenCPInstruction.java`
#### Snippet
```java
		int[] tDims = DataConverter.getTensorDimensions(ec, dims);
		CacheBlock<?> out = new TensorBlock(output.getValueType(), tDims).allocateBlock();
		TensorBlock outT = (TensorBlock) out;
		if(minValueStr.equals(maxValueStr)) {
			if(minMaxAreDoubles)
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'enc' to 'ColumnEncoderComposite'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
				for(Encoder enc : encoder.getColumnEncoders()) {
					if(enc instanceof ColumnEncoderComposite) {
						for(Encoder compositeEncoder : ((ColumnEncoderComposite) enc).getEncoders()) {
							if(compositeEncoder instanceof ColumnEncoderBin && ((ColumnEncoderBin) compositeEncoder).getBinMethod() == ColumnEncoderBin.BinMethod.EQUI_HEIGHT) {
								double quantilrRange = (double) fin.getNumRows() / ((ColumnEncoderBin) compositeEncoder).getNumBin();
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'enc' to 'ColumnEncoderComposite'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
		for(Encoder enc : globalEncoder.getColumnEncoders()) {
			if(enc instanceof ColumnEncoderComposite) {
				for(Encoder compositeEncoder : ((ColumnEncoderComposite) enc).getEncoders())
					if(compositeEncoder instanceof ColumnEncoderBin && ((ColumnEncoderBin) compositeEncoder)
						.getBinMethod() == ColumnEncoderBin.BinMethod.EQUI_HEIGHT)
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'enc' to 'ColumnEncoderComposite'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/MultiReturnParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
						((ColumnEncoderBin) compositeEncoder).build(null, equiHeightBinsPerColumn
							.get(((ColumnEncoderBin) compositeEncoder).getColID() - 1));
				((ColumnEncoderComposite) enc).updateAllDCEncoders();
			}
		}
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'fout' to 'CacheDataOutput'
in `src/main/java/org/apache/sysds/runtime/instructions/fed/ParameterizedBuiltinFEDInstruction.java`
#### Snippet
```java
				DataOutput fout = new CacheDataOutput(new byte[(int) cbsize]);
				_meta.write(fout);
				byte[] bytes = ((CacheDataOutput) fout).getBytes();
				checksum.update(bytes, 0, bytes.length);
			}
```

## RuleId[id=SuspiciousIndentAfterControlStatement]
### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `src/main/java/org/apache/sysds/hops/codegen/cplan/CNodeCell.java`
#### Snippet
```java
			String initial_value = "(T)1.0";
			if(_aggOp != null)
			switch(_aggOp) {
				case SUM:
				case SUM_SQ:
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'for' statement
in `src/main/java/org/apache/sysds/parser/OutputStatement.java`
#### Snippet
```java
			if (paramName.equals(key))
				return true;
			return false;
	}
	
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'for' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		SparseBlock sblock = in.getSparseBlock();
		for (int j = 0; j < sblock.numRows(); j++)
		if( !sblock.isEmpty(j) ) {
			int apos = sblock.pos(j);
			int alen = sblock.size(j);
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'for' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		SparseBlock sblock = in.getSparseBlock();
		for( int j = 0; j < sblock.numRows(); j++)
		if( !sblock.isEmpty(j) ) {
			int apos = sblock.pos(j);
			int alen = sblock.size(j);
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'for' statement
in `src/main/java/org/apache/sysds/runtime/matrix/data/LibMatrixOuterAgg.java`
#### Snippet
```java
		SparseBlock sblock = in.getSparseBlock();
		for (int j = 0; j < sblock.numRows(); j++)
		if( !sblock.isEmpty(j) ) {
			int apos = sblock.pos(j);
			int alen = sblock.size(j);
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
			}
			else
			l = l.n;
		}
		return addNewBucket(ix, key);
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `src/main/java/org/apache/sysds/runtime/compress/utils/DoubleCountHashMap.java`
#### Snippet
```java
			}
			else
			l = l.n;
		}
		return addNewBucket(ix, key);
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `src/main/java/org/apache/sysds/runtime/instructions/cp/ListObject.java`
#### Snippet
```java
			Data dat = _data.get(i);
			if( dat instanceof CacheableData<?> )
			_dataState[i] = ((CacheableData<?>) dat).isCleanupEnabled();
		}
	}
```

### SuspiciousIndentAfterControlStatement
Suspicious indentation after 'if' statement
in `src/main/java/org/apache/sysds/runtime/controlprogram/parfor/ResultMergeLocalFile.java`
#### Snippet
```java
		
		if( LOG.isTraceEnabled() )
		LOG.trace("ResultMerge (local, file): Execute serial merge for output "
			+_output.hashCode()+" (fname="+_output.getFileName()+")");
		
```

